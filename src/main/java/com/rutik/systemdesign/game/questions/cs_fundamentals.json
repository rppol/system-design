[{"id":"cs_fundamentals/arrays_strings_and_hashing#beca40da2ecd","section":"cs_fundamentals","module":"cs_fundamentals/arrays_strings_and_hashing","moduleName":"arrays strings and hashing","qIndex":1,"difficulty":"core","question":"What is the time complexity of Python dict lookup, and what is the worst case?","answerFull":"O(1) average. Worst case O(n) if all keys hash to the same bucket (e.g., adversarial keys with crafted hash collisions). Python randomises the hash seed per process (since Python 3.3) to make collision attacks impractical. For integer keys, hash(n) == n (for small integers) so there's no randomisation — be aware when using integer-keyed dicts for security-sensitive applications.","correct":"O(1) average. Worst case O(n) if all keys hash to the same bucket (e.g., adversarial keys with crafted hash collisions).","distractors":["When a single bucket's chain grows to ≥ 8 entries, it is converted to a red-black BST, giving O(log n) operations on that bucket instead of O(n).","Iterate through the array; for each element x, check if target - x is in a hash map of previously seen values; if yes, return the pair.","Combine a dict (for O(1) key lookup) with a doubly-linked list (for O(1) LRU eviction)."]},{"id":"cs_fundamentals/arrays_strings_and_hashing#599502edb884","section":"cs_fundamentals","module":"cs_fundamentals/arrays_strings_and_hashing","moduleName":"arrays strings and hashing","qIndex":2,"difficulty":"core","question":"What is the difference between defaultdict and dict.get(key, default)?","answerFull":"defaultdict(list) automatically inserts a new empty list when a key is missing and you access it with d[key]. dict.get(key, []) returns an empty list but does NOT insert it. Use defaultdict when you want to immediately modify the value (e.g., d[key].append(x) without a prior existence check). Use dict.get when you only want to read a default without mutating the dict.","correct":"defaultdict(list) automatically inserts a new empty list when a key is missing and you access it with d[key].","distractors":["This is a Python gotcha related to hashing: def f(lst=[]) — the default lst is created ONCE when the function is defined, not on each call.","Dict lookup uses hash(key) first, then key == stored_key (the eq method).","Two approaches: (a) Floyd's cycle detection — treat the array as a linked list where arr[i] points to the next node."]},{"id":"cs_fundamentals/arrays_strings_and_hashing#0401a9282eac","section":"cs_fundamentals","module":"cs_fundamentals/arrays_strings_and_hashing","moduleName":"arrays strings and hashing","qIndex":3,"difficulty":"core","question":"Why does Java's HashMap resize at 75% capacity and not 100%?","answerFull":"At 100% load factor (with open addressing or long chains), the probability of collision is very high, making average lookup close to O(n). The 0.75 threshold balances time (shorter chains, fewer collisions) against space (table is 25% empty on average). Empirically, 0.75 gives roughly 1.0 extra comparisons per lookup at steady state. The resize doubles the capacity, restoring the load factor to ~0.375.","correct":"At 100% load factor (with open addressing or long chains), the probability of collision is very high, making average lookup close to O(n).","distractors":["Dict lookup uses hash(key) first, then key == stored_key (the eq method).","O(1) average. Worst case O(n) if all keys hash to the same bucket (e.g., adversarial keys with crafted hash collisions).","Combine a dict (for O(1) key lookup) with a doubly-linked list (for O(1) LRU eviction)."]},{"id":"cs_fundamentals/arrays_strings_and_hashing#17da8c480f66","section":"cs_fundamentals","module":"cs_fundamentals/arrays_strings_and_hashing","moduleName":"arrays strings and hashing","qIndex":4,"difficulty":"core","question":"Two Sum — what is the O(n) hash-table solution?","answerFull":"Iterate through the array; for each element x, check if target - x is in a hash map of previously seen values; if yes, return the pair. If no, store x → index in the map. One pass, O(n) time, O(n) space. Key insight: instead of asking \"is there any y such that x + y = target?\", rephrase as \"was target - x seen before?\" — a point lookup, not a search.","correct":"Iterate through the array; for each element x, check if target - x is in a hash map of previously seen values; if yes, return the pair.","distractors":["Build a frequency map (Counter).","Two approaches: (a) Floyd's cycle detection — treat the array as a linked list where arr[i] points to the next node.","list.pop() — O(1) amortized (removes the last element, no shifting)."]},{"id":"cs_fundamentals/arrays_strings_and_hashing#ca872e4f7fa2","section":"cs_fundamentals","module":"cs_fundamentals/arrays_strings_and_hashing","moduleName":"arrays strings and hashing","qIndex":5,"difficulty":"core","question":"How does Java HashMap's treeification (Java 8) help?","answerFull":"When a single bucket's chain grows to ≥ 8 entries, it is converted to a red-black BST, giving O(log n) operations on that bucket instead of O(n). This prevents hash-collision DoS attacks (an attacker sending many keys with the same hash value) from degrading the whole map to O(n) per operation. When the bucket shrinks below 6 entries, it converts back to a linked list.","correct":"When a single bucket's chain grows to ≥ 8 entries, it is converted to a red-black BST, giving O(log n) operations on that bucket instead of O(n).","distractors":["LinkedHashMap extends HashMap with a doubly-linked list connecting all entries in insertion (or access) order.","O(1) average. Worst case O(n) if all keys hash to the same bucket (e.g., adversarial keys with crafted hash collisions).","O(n log n) time (Timsort)."]},{"id":"cs_fundamentals/arrays_strings_and_hashing#51db70ecc3f1","section":"cs_fundamentals","module":"cs_fundamentals/arrays_strings_and_hashing","moduleName":"arrays strings and hashing","qIndex":6,"difficulty":"intermediate","question":"What are the time and space complexities of sorted() in Python?","answerFull":"O(n log n) time (Timsort). O(n) space (a separate list is returned — the original is not modified). For sorting a string: sorted(\"anagram\") → O(k log k) where k = string length; then ''.join(sorted(s)) is the canonical anagram key.","correct":"O(n log n) time (Timsort).","distractors":["This is a Python gotcha related to hashing: def f(lst=[]) — the default lst is created ONCE when the function is defined, not on each call.","When a single bucket's chain grows to ≥ 8 entries, it is converted to a red-black BST, giving O(log n) operations on that bucket instead of O(n).","At 100% load factor (with open addressing or long chains), the probability of collision is very high, making average lookup close to O(n)."]},{"id":"cs_fundamentals/arrays_strings_and_hashing#3ffc6c86830b","section":"cs_fundamentals","module":"cs_fundamentals/arrays_strings_and_hashing","moduleName":"arrays strings and hashing","qIndex":7,"difficulty":"intermediate","question":"What is the longest substring without repeating characters, and what is the approach?","answerFull":"Sliding window. Maintain a set of characters in the current window [left, right]. Expand right; when a duplicate is found, shrink left until the duplicate is removed. Track the maximum window size seen. O(n) time — each character enters and leaves the window at most once.","correct":"Sliding window. Maintain a set of characters in the current window [left, right].","distractors":["Two strings are anagrams if they contain the same characters with the same frequencies.","At 100% load factor (with open addressing or long chains), the probability of collision is very high, making average lookup close to O(n).","Build a frequency map (Counter)."]},{"id":"cs_fundamentals/arrays_strings_and_hashing#7fed8a9745fd","section":"cs_fundamentals","module":"cs_fundamentals/arrays_strings_and_hashing","moduleName":"arrays strings and hashing","qIndex":9,"difficulty":"intermediate","question":"You need to find all pairs in an array that sum to zero. What is the O(n) approach?","answerFull":"Build a frequency map (Counter). For each distinct value x, if -x exists in the map, it forms a pair. Handle duplicates carefully: the pair (0, 0) requires at least two zeros; (x, -x) for x ≠ 0 requires both x and -x to be present. Deduplicate results by storing pairs with x <= -x.","correct":"Build a frequency map (Counter).","distractors":["Iterate through the array; for each element x, check if target - x is in a hash map of previously seen values; if yes, return the pair.","Two approaches: (a) Floyd's cycle detection — treat the array as a linked list where arr[i] points to the next node.","At 100% load factor (with open addressing or long chains), the probability of collision is very high, making average lookup close to O(n)."]},{"id":"cs_fundamentals/arrays_strings_and_hashing#bf38cfba43da","section":"cs_fundamentals","module":"cs_fundamentals/arrays_strings_and_hashing","moduleName":"arrays strings and hashing","qIndex":10,"difficulty":"intermediate","question":"How does Python's set differ from a frozenset?","answerFull":"Both are hash sets with O(1) membership testing. set is mutable (supports add, discard, update). frozenset is immutable and therefore hashable — it can be used as a dictionary key or as an element of another set. frozenset is useful when you need a set as a cache key (e.g., grouping states in a BFS problem).","correct":"Both are hash sets with O(1) membership testing.","distractors":["This is a Python gotcha related to hashing: def f(lst=[]) — the default lst is created ONCE when the function is defined, not on each call.","O(1) average. Worst case O(n) if all keys hash to the same bucket (e.g., adversarial keys with crafted hash collisions).","Sliding window. Maintain a set of characters in the current window [left, right]."]},{"id":"cs_fundamentals/arrays_strings_and_hashing#1e6dcc2a3e21","section":"cs_fundamentals","module":"cs_fundamentals/arrays_strings_and_hashing","moduleName":"arrays strings and hashing","qIndex":11,"difficulty":"advanced","question":"What is the time complexity of in for Python list vs set?","answerFull":"x in list — O(n) linear scan. x in set — O(1) average hash lookup. The mistake of using a list where a set is appropriate is one of the most common sources of accidental O(n²) code: checking membership inside a for loop.","correct":"x in list — O(n) linear scan.","distractors":["LinkedHashMap extends HashMap with a doubly-linked list connecting all entries in insertion (or access) order.","Combine a dict (for O(1) key lookup) with a doubly-linked list (for O(1) LRU eviction).","This is a Python gotcha related to hashing: def f(lst=[]) — the default lst is created ONCE when the function is defined, not on each call."]},{"id":"cs_fundamentals/arrays_strings_and_hashing#de27d6162f6a","section":"cs_fundamentals","module":"cs_fundamentals/arrays_strings_and_hashing","moduleName":"arrays strings and hashing","qIndex":12,"difficulty":"advanced","question":"How would you implement an LRU cache in O(1) time for all operations?","answerFull":"Combine a dict (for O(1) key lookup) with a doubly-linked list (for O(1) LRU eviction). On access: move the node to the head of the list (most recently used). On eviction: remove the tail node (least recently used). Python's OrderedDict has move_to_end and popitem(last=False) which implement this pattern with a single built-in structure. See case_studies/design_lru_cache.md for the full walkthrough.","correct":"Combine a dict (for O(1) key lookup) with a doubly-linked list (for O(1) LRU eviction).","distractors":["list.pop() — O(1) amortized (removes the last element, no shifting).","When a single bucket's chain grows to ≥ 8 entries, it is converted to a red-black BST, giving O(log n) operations on that bucket instead of O(n).","Two approaches: (a) Floyd's cycle detection — treat the array as a linked list where arr[i] points to the next node."]},{"id":"cs_fundamentals/arrays_strings_and_hashing#260c70abb30d","section":"cs_fundamentals","module":"cs_fundamentals/arrays_strings_and_hashing","moduleName":"arrays strings and hashing","qIndex":13,"difficulty":"advanced","question":"What is Java's LinkedHashMap and how does it maintain insertion order?","answerFull":"LinkedHashMap extends HashMap with a doubly-linked list connecting all entries in insertion (or access) order. Each entry stores before and after pointers in addition to next (for the hash chain). This adds O(1) overhead per operation and O(n) extra memory. Iteration is O(n) in insertion order. It is the standard Java building block for an LRU cache (LinkedHashMap(cap, 0.75, true) in access-order mode).","correct":"LinkedHashMap extends HashMap with a doubly-linked list connecting all entries in insertion (or access) order.","distractors":["Sliding window. Maintain a set of characters in the current window [left, right].","defaultdict(list) automatically inserts a new empty list when a key is missing and you access it with d[key].","list.pop() — O(1) amortized (removes the last element, no shifting)."]},{"id":"cs_fundamentals/arrays_strings_and_hashing#37458768c7bc","section":"cs_fundamentals","module":"cs_fundamentals/arrays_strings_and_hashing","moduleName":"arrays strings and hashing","qIndex":14,"difficulty":"advanced","question":"What happens when you use a mutable default argument in Python?","answerFull":"This is a Python gotcha related to hashing: def f(lst=[]) — the default lst is created ONCE when the function is defined, not on each call. All calls that use the default share the same list. Mutations in one call persist in the next. Fix: def f(lst=None): if lst is None: lst = []. The same issue occurs with dicts and sets as default arguments.","correct":"This is a Python gotcha related to hashing: def f(lst=[]) — the default lst is created ONCE when the function is defined, not on each call.","distractors":["At 100% load factor (with open addressing or long chains), the probability of collision is very high, making average lookup close to O(n).","Build a frequency map (Counter).","Combine a dict (for O(1) key lookup) with a doubly-linked list (for O(1) LRU eviction)."]},{"id":"cs_fundamentals/arrays_strings_and_hashing#ec8d46fc59cb","section":"cs_fundamentals","module":"cs_fundamentals/arrays_strings_and_hashing","moduleName":"arrays strings and hashing","qIndex":15,"difficulty":"advanced","question":"Given an array of integers 1..n with one duplicate, find the duplicate in O(n) time and O(1) space without modifying the array.","answerFull":"Two approaches: (a) Floyd's cycle detection — treat the array as a linked list where arr[i] points to the next node. The duplicate creates a cycle; find the cycle entry with fast-slow pointers. (b) Sum: sum(arr) - n(n+1)/2 = the duplicate if exactly one number appears twice (fails if multiple duplicates or numbers outside 1..n).","correct":"Two approaches: (a) Floyd's cycle detection — treat the array as a linked list where arr[i] points to the next node.","distractors":["x in list — O(n) linear scan.","Combine a dict (for O(1) key lookup) with a doubly-linked list (for O(1) LRU eviction).","defaultdict(list) automatically inserts a new empty list when a key is missing and you access it with d[key]."]},{"id":"cs_fundamentals/arrays_strings_and_hashing#c01ca183cf27","section":"cs_fundamentals","module":"cs_fundamentals/arrays_strings_and_hashing","moduleName":"arrays strings and hashing","qIndex":16,"difficulty":"advanced","question":"What is the time complexity of Python's list .pop() vs .pop(0)?","answerFull":"list.pop() — O(1) amortized (removes the last element, no shifting). list.pop(0) — O(n) (removes the first element, shifts all remaining elements left). For a FIFO queue, use collections.deque which gives O(1) popleft. This distinction is a common performance bug in sliding window or BFS implementations.","correct":"list.pop() — O(1) amortized (removes the last element, no shifting).","distractors":["x in list — O(n) linear scan.","Two approaches: (a) Floyd's cycle detection — treat the array as a linked list where arr[i] points to the next node.","Iterate through the array; for each element x, check if target - x is in a hash map of previously seen values; if yes, return the pair."]},{"id":"cs_fundamentals/arrays_strings_and_hashing#38091933c8d2","section":"cs_fundamentals","module":"cs_fundamentals/arrays_strings_and_hashing","moduleName":"arrays strings and hashing","qIndex":17,"difficulty":"advanced","question":"How do you detect if two strings are anagrams in O(n) time?","answerFull":"Two strings are anagrams if they contain the same characters with the same frequencies. Approaches: (a) sort both and compare — O(k log k); (b) build a frequency counter for one, decrement for the other, check all zeros — O(k) time, O(1) space (26 characters for lowercase alphabet). The O(k) approach is preferred.","correct":"Two strings are anagrams if they contain the same characters with the same frequencies.","distractors":["O(n log n) time (Timsort).","O(1) average. Worst case O(n) if all keys hash to the same bucket (e.g., adversarial keys with crafted hash collisions).","Sliding window. Maintain a set of characters in the current window [left, right]."]},{"id":"cs_fundamentals/arrays_strings_and_hashing#ef5db7580ba4","section":"cs_fundamentals","module":"cs_fundamentals/arrays_strings_and_hashing","moduleName":"arrays strings and hashing","qIndex":18,"difficulty":"advanced","question":"What is the difference between == and is for checking key equality in Python dicts?","answerFull":"Dict lookup uses hash(key) first, then key == stored_key (the eq method). is checks identity (same object in memory), not equality. Two distinct objects with the same value compare equal (\"abc\" == \"abc\") but is would be False unless Python interns them. Never use is for value comparisons. For small integers (-5 to 256) and interned strings, Python caches objects so is incidentally returns True, but this is an implementation detail, not a language guarantee.","correct":"Dict lookup uses hash(key) first, then key == stored_key (the eq method).","distractors":["O(1) average. Worst case O(n) if all keys hash to the same bucket (e.g., adversarial keys with crafted hash collisions).","This is a Python gotcha related to hashing: def f(lst=[]) — the default lst is created ONCE when the function is defined, not on each call.","Combine a dict (for O(1) key lookup) with a doubly-linked list (for O(1) LRU eviction)."]},{"id":"cs_fundamentals/complexity_analysis_and_big_o#e12e1a751370","section":"cs_fundamentals","module":"cs_fundamentals/complexity_analysis_and_big_o","moduleName":"complexity analysis and big o","qIndex":1,"difficulty":"core","question":"What is the difference between O, Θ, and Ω?","answerFull":"O is an upper bound (worst case or tighter): f ≤ c·g for large n. Ω is a lower bound: f ≥ c·g. Θ is a tight bound: both O and Ω hold simultaneously. In interviews \"this runs in O(n)\" is often used loosely to mean Θ(n) — clarify if precision matters. The key gotcha: O(n) includes O(1), O(log n), etc. — O is not equality.","correct":"O is an upper bound (worst case or tighter): f ≤ c·g for large n.","distractors":["O(1) amortized, O(n) worst case.","The Ω(n log n) lower bound applies to *comparison-based* sorting — any algorithm that can only compare elements has at least n log n comparisons in the decision tree.","BST: O(log n) lookup, O(log n) insert, always ordered."]},{"id":"cs_fundamentals/complexity_analysis_and_big_o#99d31214b04e","section":"cs_fundamentals","module":"cs_fundamentals/complexity_analysis_and_big_o","moduleName":"complexity analysis and big o","qIndex":2,"difficulty":"core","question":"Is the amortized cost of a dynamic array append O(1) or O(n)?","answerFull":"O(1) amortized, O(n) worst case. A single append may trigger a resize that copies all n elements, costing O(n). But the resize doubles capacity, so the next resize won't happen for another n appends. Spreading the O(n) resize cost over n appends gives O(1) amortized. In real-time / latency-critical systems this distinction matters — a worst-case O(n) spike can miss a deadline even if the average is O(1).","correct":"O(1) amortized, O(n) worst case.","distractors":["O(1) amortized for get, set, and delete.","O is an upper bound (worst case or tighter): f ≤ c·g for large n.","BST: O(log n) lookup, O(log n) insert, always ordered."]},{"id":"cs_fundamentals/complexity_analysis_and_big_o#dd121ccf5678","section":"cs_fundamentals","module":"cs_fundamentals/complexity_analysis_and_big_o","moduleName":"complexity analysis and big o","qIndex":3,"difficulty":"core","question":"What is the time complexity of building a heap from an unsorted array?","answerFull":"O(n), not O(n log n). The naive explanation \"insert n elements, each O(log n)\" gives O(n log n), but heapify (sift-down from the middle) is O(n). The key insight: most nodes are near the leaves and sift-down distance is small; only O(n/4) nodes are height-1, O(n/8) are height-2, etc. The sum ∑k · n/2^(k+1) converges to 2n.","correct":"O(n), not O(n log n). The naive explanation \"insert n elements, each O(log n)\" gives O(n log n), but heapify (sift-down from the middle) is O(n).","distractors":["O(n log n). The outer loop runs n times; the inner loop runs log n times per outer iteration → n × log n total iterations.","Naive: O(n³) for two n×n matrices.","O(n log n). Python uses Timsort, a hybrid merge sort / insertion sort."]},{"id":"cs_fundamentals/complexity_analysis_and_big_o#1c8573cfa788","section":"cs_fundamentals","module":"cs_fundamentals/complexity_analysis_and_big_o","moduleName":"complexity analysis and big o","qIndex":4,"difficulty":"core","question":"Quicksort is O(n log n) average and O(n²) worst case. When does the worst case occur?","answerFull":"When the pivot is always the minimum or maximum element — producing partitions of size 0 and n-1. This happens on already-sorted or reverse-sorted input with a naive \"pick first element\" pivot. Fix: randomise the pivot (expected O(n log n)), use median-of-three, or use introsort (switches to heapsort when recursion depth exceeds 2 log n).","correct":"When the pivot is always the minimum or maximum element — producing partitions of size 0 and n-1.","distractors":["O is an upper bound (worst case or tighter): f ≤ c·g for large n.","O(n log n). Python uses Timsort, a hybrid merge sort / insertion sort.","Solve 1000 × n log n = 1 × n² → 1000 log n = n → n ≈ e^(1000/n), numerically approximately n ≈ 14,000 (check: 1000 × 14000 × 13.4 ≈ 1.88 × 10⁸, 14000² = 1.96 × 10⁸ — they are close)."]},{"id":"cs_fundamentals/complexity_analysis_and_big_o#3ebb5b417422","section":"cs_fundamentals","module":"cs_fundamentals/complexity_analysis_and_big_o","moduleName":"complexity analysis and big o","qIndex":5,"difficulty":"core","question":"Two nested for-loops — is it always O(n²)?","answerFull":"No. The inner loop's range determines the complexity. If the inner loop runs a fixed number of times (constant), the total is O(n). If the inner pointer advances monotonically and never resets (two-pointer), total iterations are O(n). If the inner loop iterates i times for outer index i, the total is n(n-1)/2 = O(n²). Always count total iterations across both loops, not just per iteration of the outer loop.","correct":"No. The inner loop's range determines the complexity.","distractors":["O(n log n). The outer loop runs n times; the inner loop runs log n times per outer iteration → n × log n total iterations.","When the pivot is always the minimum or maximum element — producing partitions of size 0 and n-1.","BST: O(log n) lookup, O(log n) insert, always ordered."]},{"id":"cs_fundamentals/complexity_analysis_and_big_o#46971fcb2465","section":"cs_fundamentals","module":"cs_fundamentals/complexity_analysis_and_big_o","moduleName":"complexity analysis and big o","qIndex":6,"difficulty":"intermediate","question":"What is the time complexity of Python's in operator for a list vs a set?","answerFull":"x in list is O(n) — linear scan. x in set is O(1) average — hash lookup. This is a common source of O(n²) bugs: checking membership in a list inside a loop. Fix: convert the list to a set before the loop.","correct":"x in list is O(n) — linear scan.","distractors":["O(n log n). Python uses Timsort, a hybrid merge sort / insertion sort.","O(n) time, O(1) extra space (two-pointer), or O(n) space if you reverse and compare.","O(1) amortized for get, set, and delete."]},{"id":"cs_fundamentals/complexity_analysis_and_big_o#6a15f0cabbb3","section":"cs_fundamentals","module":"cs_fundamentals/complexity_analysis_and_big_o","moduleName":"complexity analysis and big o","qIndex":7,"difficulty":"intermediate","question":"What is the space complexity of a recursive DFS on a graph with n nodes and E edges?","answerFull":"O(n) for the visited set, O(h) for the call stack where h is the maximum recursion depth. In the worst case (a path graph), h = n, so total space is O(n). For a balanced binary tree, h = O(log n), so stack space is O(log n). The graph's edge list or adjacency matrix is O(E) or O(n²) but is usually considered input space, not auxiliary.","correct":"O(n) for the visited set, O(h) for the call stack where h is the maximum recursion depth.","distractors":["O(n) time, O(1) extra space (two-pointer), or O(n) space if you reverse and compare.","When the pivot is always the minimum or maximum element — producing partitions of size 0 and n-1.","O(1) amortized for get, set, and delete."]},{"id":"cs_fundamentals/complexity_analysis_and_big_o#284cf5ea898c","section":"cs_fundamentals","module":"cs_fundamentals/complexity_analysis_and_big_o","moduleName":"complexity analysis and big o","qIndex":8,"difficulty":"intermediate","question":"Can O(n) be faster than O(1) in practice?","answerFull":"Yes, for small n. An O(1) operation with a large constant (e.g., looking up a value in a huge hash table that exceeds L3 cache) can be slower than an O(n) scan over a small, cache-hot array. The crossover point depends on hardware and cache effects. In practice for n < 8–16, a linear scan often beats a hash table due to cache locality.","correct":"Yes, for small n. An O(1) operation with a large constant (e.g., looking up a value in a huge hash table that exceeds L3 cache) can be slower than an O(n) scan over a small, cache-hot array.","distractors":["x in list is O(n) — linear scan.","O is an upper bound (worst case or tighter): f ≤ c·g for large n.","BST: O(log n) lookup, O(log n) insert, always ordered."]},{"id":"cs_fundamentals/complexity_analysis_and_big_o#40608818f6a5","section":"cs_fundamentals","module":"cs_fundamentals/complexity_analysis_and_big_o","moduleName":"complexity analysis and big o","qIndex":9,"difficulty":"intermediate","question":"What is the Big-O of a loop that runs log n times inside a loop that runs n times?","answerFull":"O(n log n). The outer loop runs n times; the inner loop runs log n times per outer iteration → n × log n total iterations.","correct":"O(n log n). The outer loop runs n times; the inner loop runs log n times per outer iteration → n × log n total iterations.","distractors":["O(n), not O(n log n). The naive explanation \"insert n elements, each O(log n)\" gives O(n log n), but heapify (sift-down from the middle) is O(n).","O(n log n). Python uses Timsort, a hybrid merge sort / insertion sort.","The Ω(n log n) lower bound applies to *comparison-based* sorting — any algorithm that can only compare elements has at least n log n comparisons in the decision tree."]},{"id":"cs_fundamentals/complexity_analysis_and_big_o#a187e4fdc54c","section":"cs_fundamentals","module":"cs_fundamentals/complexity_analysis_and_big_o","moduleName":"complexity analysis and big o","qIndex":10,"difficulty":"intermediate","question":"What is the time complexity of sorted() in Python?","answerFull":"O(n log n). Python uses Timsort, a hybrid merge sort / insertion sort. It is O(n) on already-sorted or nearly-sorted input due to the natural run detection, but O(n log n) in general. It is stable (preserves relative order of equal elements).","correct":"O(n log n). Python uses Timsort, a hybrid merge sort / insertion sort.","distractors":["O(n) time, O(1) extra space (two-pointer), or O(n) space if you reverse and compare.","O(n log n). The outer loop runs n times; the inner loop runs log n times per outer iteration → n × log n total iterations.","Solve 1000 × n log n = 1 × n² → 1000 log n = n → n ≈ e^(1000/n), numerically approximately n ≈ 14,000 (check: 1000 × 14000 × 13.4 ≈ 1.88 × 10⁸, 14000² = 1.96 × 10⁸ — they are close)."]},{"id":"cs_fundamentals/complexity_analysis_and_big_o#9bdb839825f9","section":"cs_fundamentals","module":"cs_fundamentals/complexity_analysis_and_big_o","moduleName":"complexity analysis and big o","qIndex":11,"difficulty":"advanced","question":"What is the amortized time complexity of Python dict operations?","answerFull":"O(1) amortized for get, set, and delete. Python dicts use open addressing with a load factor of roughly 2/3; when exceeded, the dict is resized (rehashed) to a new table. The resize copies all n entries at O(n) cost but is rare — amortized O(1) per operation. Python 3.7+ also preserves insertion order (via a separate compact array), but the complexity is the same.","correct":"O(1) amortized for get, set, and delete.","distractors":["O(n) time, O(1) extra space (two-pointer), or O(n) space if you reverse and compare.","O(n) for the visited set, O(h) for the call stack where h is the maximum recursion depth.","O(n log n). Python uses Timsort, a hybrid merge sort / insertion sort."]},{"id":"cs_fundamentals/complexity_analysis_and_big_o#071ad7e43fa4","section":"cs_fundamentals","module":"cs_fundamentals/complexity_analysis_and_big_o","moduleName":"complexity analysis and big o","qIndex":12,"difficulty":"advanced","question":"You have an O(n²) algorithm and an O(n log n) algorithm. For what value of n do they cross over (assuming constants c₁ = 1000 for O(n log n) and c₂ = 1 for O(n²))?","answerFull":"Solve 1000 × n log n = 1 × n² → 1000 log n = n → n ≈ e^(1000/n), numerically approximately n ≈ 14,000 (check: 1000 × 14000 × 13.4 ≈ 1.88 × 10⁸, 14000² = 1.96 × 10⁸ — they are close). The lesson: constants matter at moderate n; at large n the O(n²) always loses.","correct":"Solve 1000 × n log n = 1 × n² → 1000 log n = n → n ≈ e^(1000/n), numerically approximately n ≈ 14,000 (check: 1000 × 14000 × 13.4 ≈ 1.88 × 10⁸, 14000² = 1.96 × 10⁸ — they are close).","distractors":["O(n log n). The outer loop runs n times; the inner loop runs log n times per outer iteration → n × log n total iterations.","O(n log n). Python uses Timsort, a hybrid merge sort / insertion sort.","The Ω(n log n) lower bound applies to *comparison-based* sorting — any algorithm that can only compare elements has at least n log n comparisons in the decision tree."]},{"id":"cs_fundamentals/complexity_analysis_and_big_o#a97e89d350df","section":"cs_fundamentals","module":"cs_fundamentals/complexity_analysis_and_big_o","moduleName":"complexity analysis and big o","qIndex":14,"difficulty":"advanced","question":"What is the time complexity of checking whether a string is a palindrome?","answerFull":"O(n) time, O(1) extra space (two-pointer), or O(n) space if you reverse and compare. The hidden question is whether you count the input string as space — typically O(n) input space is not counted.","correct":"O(n) time, O(1) extra space (two-pointer), or O(n) space if you reverse and compare.","distractors":["The Ω(n log n) lower bound applies to *comparison-based* sorting — any algorithm that can only compare elements has at least n log n comparisons in the decision tree.","BST: O(log n) lookup, O(log n) insert, always ordered.","Naive: O(n³) for two n×n matrices."]},{"id":"cs_fundamentals/complexity_analysis_and_big_o#94b930b6ab9e","section":"cs_fundamentals","module":"cs_fundamentals/complexity_analysis_and_big_o","moduleName":"complexity analysis and big o","qIndex":15,"difficulty":"advanced","question":"Is merge sort better than quicksort in all cases?","answerFull":"No — merge sort is stable and O(n log n) worst case, but requires O(n) auxiliary space. Quicksort is in-place (O(log n) stack space) and has better cache behaviour in practice due to sequential partition access, making it faster for arrays despite the same asymptotic average. Use merge sort when stability is required or when sorting linked lists (no random access). Use quicksort (with randomised pivot) or introsort for in-memory array sorting.","correct":"No — merge sort is stable and O(n log n) worst case, but requires O(n) auxiliary space.","distractors":["BST: O(log n) lookup, O(log n) insert, always ordered.","Solve 1000 × n log n = 1 × n² → 1000 log n = n → n ≈ e^(1000/n), numerically approximately n ≈ 14,000 (check: 1000 × 14000 × 13.4 ≈ 1.88 × 10⁸, 14000² = 1.96 × 10⁸ — they are close).","O(1) amortized, O(n) worst case."]},{"id":"cs_fundamentals/complexity_analysis_and_big_o#4ab7c1400b68","section":"cs_fundamentals","module":"cs_fundamentals/complexity_analysis_and_big_o","moduleName":"complexity analysis and big o","qIndex":16,"difficulty":"advanced","question":"What is the complexity of looking up a key in a balanced BST vs a hash table, and when would you prefer the BST?","answerFull":"BST: O(log n) lookup, O(log n) insert, always ordered. Hash table: O(1) average lookup and insert, no ordering. Prefer BST (TreeMap, SortedDict) when you need: range queries (all keys between A and B), floor/ceiling (nearest key ≤ / ≥ x), sorted iteration, or deterministic O(log n) without hash collision risk. Prefer hash table for pure point-lookups where order does not matter.","correct":"BST: O(log n) lookup, O(log n) insert, always ordered.","distractors":["Solve 1000 × n log n = 1 × n² → 1000 log n = n → n ≈ e^(1000/n), numerically approximately n ≈ 14,000 (check: 1000 × 14000 × 13.4 ≈ 1.88 × 10⁸, 14000² = 1.96 × 10⁸ — they are close).","O(n), not O(n log n). The naive explanation \"insert n elements, each O(log n)\" gives O(n log n), but heapify (sift-down from the middle) is O(n).","O(n log n). Python uses Timsort, a hybrid merge sort / insertion sort."]},{"id":"cs_fundamentals/complexity_analysis_and_big_o#fa853a46695a","section":"cs_fundamentals","module":"cs_fundamentals/complexity_analysis_and_big_o","moduleName":"complexity analysis and big o","qIndex":17,"difficulty":"advanced","question":"A function calls itself twice with input n-1. What is its time complexity?","answerFull":"The recurrence is T(n) = 2T(n-1) + O(1). This is O(2^n). The call tree has 2^n leaf nodes. Classic example: naive recursive Fibonacci. Fix: memoise (O(n) time and space) or use bottom-up DP (O(n) time, O(1) space with rolling variables).","correct":"The recurrence is T(n) = 2T(n-1) + O(1).","distractors":["O(n) time, O(1) extra space (two-pointer), or O(n) space if you reverse and compare.","BST: O(log n) lookup, O(log n) insert, always ordered.","Naive: O(n³) for two n×n matrices."]},{"id":"cs_fundamentals/complexity_analysis_and_big_o#d489389afeb8","section":"cs_fundamentals","module":"cs_fundamentals/complexity_analysis_and_big_o","moduleName":"complexity analysis and big o","qIndex":18,"difficulty":"advanced","question":"What is the time complexity of matrix multiplication, and is there a better algorithm?","answerFull":"Naive: O(n³) for two n×n matrices. Strassen's algorithm: O(n^2.807) using 7 recursive multiplications instead of 8. The current best theoretical bound is O(n^2.371...) (Williams et al. 2024), but these advanced algorithms have huge constants and are not practical for typical matrix sizes. In practice, highly optimised BLAS libraries approach O(n³) with vectorised AVX/CUDA operations that have tiny constants.","correct":"Naive: O(n³) for two n×n matrices.","distractors":["O(n), not O(n log n). The naive explanation \"insert n elements, each O(log n)\" gives O(n log n), but heapify (sift-down from the middle) is O(n).","O(n) time, O(1) extra space (two-pointer), or O(n) space if you reverse and compare.","The Ω(n log n) lower bound applies to *comparison-based* sorting — any algorithm that can only compare elements has at least n log n comparisons in the decision tree."]},{"id":"cs_fundamentals/complexity_analysis_and_big_o#1ffc406399bf","section":"cs_fundamentals","module":"cs_fundamentals/complexity_analysis_and_big_o","moduleName":"complexity analysis and big o","qIndex":19,"difficulty":"advanced","question":"What makes counting sort/radix sort O(n), when comparison sorts have a lower bound of Ω(n log n)?","answerFull":"The Ω(n log n) lower bound applies to *comparison-based* sorting — any algorithm that can only compare elements has at least n log n comparisons in the decision tree. Counting sort and radix sort are *non-comparison* — they use the numeric value of keys to place them directly. This sidesteps the comparison lower bound. Counting sort: O(n + k) where k is the range of values. Radix sort: O(d(n + k)) where d is digits and k is digit range (10 for decimal). If k = O(n), both are O(n). Trade-off: requires integer keys with a bounded range; cannot sort arbitrary comparable objects.","correct":"The Ω(n log n) lower bound applies to *comparison-based* sorting — any algorithm that can only compare elements has at least n log n comparisons in the decision tree.","distractors":["O(n log n). Python uses Timsort, a hybrid merge sort / insertion sort.","O is an upper bound (worst case or tighter): f ≤ c·g for large n.","BST: O(log n) lookup, O(log n) insert, always ordered."]},{"id":"cs_fundamentals/computer_architecture_and_memory_hierarchy#c8d179eb9a3e","section":"cs_fundamentals","module":"cs_fundamentals/computer_architecture_and_memory_hierarchy","moduleName":"computer architecture and memory hierarchy","qIndex":1,"difficulty":"core","question":"Why does sorting data before a branch-heavy loop speed it up?","answerFull":"Sorting places all values that satisfy a branch condition contiguously, so the CPU's branch predictor learns the pattern (long run of \"not taken\" → long run of \"taken\"). With random data, the predictor mispredicts ~50% of branches; with sorted data, it mispredicts only at the transition point. Each misprediction flushes the pipeline — ~15-cycle penalty on modern CPUs. At millions of iterations, this compounds to a measurable speedup, often 3–6× in C/C++ benchmarks. The canonical example (std::sort before a branch loop) is a standard interview gotcha.","correct":"Sorting places all values that satisfy a branch condition contiguously, so the CPU's branch predictor learns the pattern (long run of \"not taken\" → long run of \"taken\").","distractors":["The BTB is a CPU cache that stores recent branch instruction addresses mapped to their predicted target addresses.","Out-of-order (OoO) execution: the CPU does not execute instructions in strict program order.","In a multi-core CPU, each core has its own L1/L2 cache."]},{"id":"cs_fundamentals/computer_architecture_and_memory_hierarchy#19d78f91e8a6","section":"cs_fundamentals","module":"cs_fundamentals/computer_architecture_and_memory_hierarchy","moduleName":"computer architecture and memory hierarchy","qIndex":2,"difficulty":"core","question":"What is false sharing, and how do you detect and fix it?","answerFull":"False sharing occurs when two threads on different cores write to different variables that happen to occupy the same 64-byte cache line. Thread A's write invalidates Thread B's cache-line copy (MESI protocol), forcing Thread B to re-fetch the line — even though Thread B's variable never changed. Symptom: correct results but poor multi-threaded scalability. Detected with perf c2c (Linux), Intel VTune Memory Access analysis, or by padding structs and observing throughput change. Fixed by aligning each independently-written variable to a cache-line boundary (64 bytes on x86-64): C alignas(64), Java @Contended, Rust #[repr(align(64))].","correct":"False sharing occurs when two threads on different cores write to different variables that happen to occupy the same 64-byte cache line.","distractors":["A cache miss forces the CPU to stall (or switch to other independent instructions) while fetching data from a lower cache level or DRAM.","Write-through: every store writes simultaneously to the cache and to the next level of the hierarchy (L2 or DRAM).","A cache line is the minimum unit of data transfer between cache levels and between cache and DRAM."]},{"id":"cs_fundamentals/computer_architecture_and_memory_hierarchy#5d11df919543","section":"cs_fundamentals","module":"cs_fundamentals/computer_architecture_and_memory_hierarchy","moduleName":"computer architecture and memory hierarchy","qIndex":3,"difficulty":"core","question":"What is a cache miss penalty, and what are the three types of cache misses?","answerFull":"A cache miss forces the CPU to stall (or switch to other independent instructions) while fetching data from a lower cache level or DRAM. Penalty: L1 miss → L2 hit: ~6 extra cycles; L2 miss → L3 hit: ~20 extra cycles; L3 miss → DRAM: ~200–300 cycles. The three types: (1) Compulsory (cold) miss — first access to a line that was never in cache; unavoidable. (2) Capacity miss — working set exceeds cache size; reduce working set or improve temporal locality. (3) Conflict miss — two frequently-used addresses map to the same cache set in a set-associative cache; resolve with data padding or changing array sizes.","correct":"A cache miss forces the CPU to stall (or switch to other independent instructions) while fetching data from a lower cache level or DRAM.","distractors":["A cache line is the minimum unit of data transfer between cache levels and between cache and DRAM.","The hardware prefetcher monitors the stream of cache miss addresses.","Out-of-order (OoO) execution: the CPU does not execute instructions in strict program order."]},{"id":"cs_fundamentals/computer_architecture_and_memory_hierarchy#d64b55277c9e","section":"cs_fundamentals","module":"cs_fundamentals/computer_architecture_and_memory_hierarchy","moduleName":"computer architecture and memory hierarchy","qIndex":4,"difficulty":"core","question":"Explain the MESI protocol and why it is necessary.","answerFull":"In a multi-core CPU, each core has its own L1/L2 cache. Without coordination, two cores could hold contradictory values for the same address. MESI is a cache coherence protocol with four states per cache line: Modified (dirty, only copy), Exclusive (clean, only copy), Shared (clean, multiple copies), Invalid (stale/absent). When a core wants to write a Shared line, it broadcasts an invalidation to all other cores; they transition to Invalid. When they next read the line, they get the updated value. MESI ensures cache coherence with minimal bus traffic.","correct":"In a multi-core CPU, each core has its own L1/L2 cache.","distractors":["Branch misprediction occurs when the CPU's branch predictor guesses the wrong path for a conditional branch.","The BTB is a CPU cache that stores recent branch instruction addresses mapped to their predicted target addresses.","Non-Uniform Memory Access (NUMA): in a multi-socket server, each CPU socket has its own memory controller and local DRAM."]},{"id":"cs_fundamentals/computer_architecture_and_memory_hierarchy#8cd89f2ebe9c","section":"cs_fundamentals","module":"cs_fundamentals/computer_architecture_and_memory_hierarchy","moduleName":"computer architecture and memory hierarchy","qIndex":5,"difficulty":"core","question":"What is the difference between spatial and temporal locality?","answerFull":"Spatial locality: if you access address A, you are likely to access addresses near A (A+1, A+2, ...) soon. The CPU exploits this by loading a full 64-byte cache line on each miss. Sequential array traversal achieves near-perfect spatial locality. Temporal locality: if you access address A, you are likely to access A again soon. Loop bodies, counters, and frequently-used objects exhibit temporal locality and stay in L1/L2 cache between accesses.","correct":"Spatial locality: if you access address A, you are likely to access addresses near A (A+1, A+2, ...) soon.","distractors":["Each node in a linked list is independently heap-allocated at an arbitrary address.","Non-Uniform Memory Access (NUMA): in a multi-socket server, each CPU socket has its own memory controller and local DRAM.","The hardware prefetcher monitors the stream of cache miss addresses."]},{"id":"cs_fundamentals/computer_architecture_and_memory_hierarchy#1608944bb5da","section":"cs_fundamentals","module":"cs_fundamentals/computer_architecture_and_memory_hierarchy","moduleName":"computer architecture and memory hierarchy","qIndex":6,"difficulty":"intermediate","question":"What is a pipeline hazard, and how do modern CPUs handle data hazards?","answerFull":"A pipeline hazard is a situation that prevents the next instruction from executing in the next cycle. Data hazard (RAW — Read After Write): instruction i+1 reads a register that instruction i has not yet written. Solution: register forwarding (bypass) — the output of the Execute stage is wired directly to the input of the next Execute stage, skipping Writeback→Decode. Without forwarding, 1–2 stall cycles are inserted. Modern out-of-order processors also reorder instructions to find independent ones that can execute while a dependent instruction waits.","correct":"A pipeline hazard is a situation that prevents the next instruction from executing in the next cycle.","distractors":["The BTB is a CPU cache that stores recent branch instruction addresses mapped to their predicted target addresses.","Write-through: every store writes simultaneously to the cache and to the next level of the hierarchy (L2 or DRAM).","Registers: < 1 ns (0–1 cycle)."]},{"id":"cs_fundamentals/computer_architecture_and_memory_hierarchy#669f29c426af","section":"cs_fundamentals","module":"cs_fundamentals/computer_architecture_and_memory_hierarchy","moduleName":"computer architecture and memory hierarchy","qIndex":7,"difficulty":"intermediate","question":"What is branch misprediction, and what is its cost?","answerFull":"Branch misprediction occurs when the CPU's branch predictor guesses the wrong path for a conditional branch. The CPU has been executing instructions on the wrong path speculatively; when the branch resolves, the pipeline is flushed and those instructions are discarded. Cost: ~15 cycles on modern Intel/AMD (varies 10–20 cycles by microarchitecture). Modern predictors (TAGE) achieve ~97–99% accuracy in benchmarks. Misprediction rate spikes with random/unpredictable branches (e.g., a search loop terminating at a random position).","correct":"Branch misprediction occurs when the CPU's branch predictor guesses the wrong path for a conditional branch.","distractors":["Out-of-order (OoO) execution: the CPU does not execute instructions in strict program order.","A cache miss forces the CPU to stall (or switch to other independent instructions) while fetching data from a lower cache level or DRAM.","Sorting places all values that satisfy a branch condition contiguously, so the CPU's branch predictor learns the pattern (long run of \"not taken\" → long run of \"taken\")."]},{"id":"cs_fundamentals/computer_architecture_and_memory_hierarchy#cadb3134e2e4","section":"cs_fundamentals","module":"cs_fundamentals/computer_architecture_and_memory_hierarchy","moduleName":"computer architecture and memory hierarchy","qIndex":8,"difficulty":"intermediate","question":"What is a cache line, and how large is it on modern hardware?","answerFull":"A cache line is the minimum unit of data transfer between cache levels and between cache and DRAM. On all modern x86-64 (Intel, AMD) and ARM (Cortex-A, Apple M-series) processors, the cache line size is 64 bytes. When you read one byte, the CPU fetches the entire aligned 64-byte block. When you write one byte, the entire line is loaded (write-allocate policy), modified, and marked dirty. Cache-line alignment is why struct padding and alignas(64) matter for false sharing.","correct":"A cache line is the minimum unit of data transfer between cache levels and between cache and DRAM.","distractors":["In a multi-core CPU, each core has its own L1/L2 cache.","Write-through: every store writes simultaneously to the cache and to the next level of the hierarchy (L2 or DRAM).","Non-Uniform Memory Access (NUMA): in a multi-socket server, each CPU socket has its own memory controller and local DRAM."]},{"id":"cs_fundamentals/computer_architecture_and_memory_hierarchy#8ed36e617e1c","section":"cs_fundamentals","module":"cs_fundamentals/computer_architecture_and_memory_hierarchy","moduleName":"computer architecture and memory hierarchy","qIndex":9,"difficulty":"intermediate","question":"Explain NUMA and why it matters for multi-socket servers.","answerFull":"Non-Uniform Memory Access (NUMA): in a multi-socket server, each CPU socket has its own memory controller and local DRAM. Accessing local memory takes ~100 ns; accessing the other socket's DRAM crosses an inter-socket interconnect (Intel QPI/UPI, AMD Infinity Fabric) and costs ~200–300 ns. A process running threads across both sockets with memory allocated on one socket will incur ~2× memory latency for cross-socket accesses. On 2-socket servers running Elasticsearch, PostgreSQL, or Redis, numactl --interleave=all spreads memory round-robin across nodes; for latency-critical workloads, --membind=0 --cpunodebind=0 keeps everything local.","correct":"Non-Uniform Memory Access (NUMA): in a multi-socket server, each CPU socket has its own memory controller and local DRAM.","distractors":["In a multi-core CPU, each core has its own L1/L2 cache.","The BTB is a CPU cache that stores recent branch instruction addresses mapped to their predicted target addresses.","Branch misprediction occurs when the CPU's branch predictor guesses the wrong path for a conditional branch."]},{"id":"cs_fundamentals/computer_architecture_and_memory_hierarchy#a902b2699ac7","section":"cs_fundamentals","module":"cs_fundamentals/computer_architecture_and_memory_hierarchy","moduleName":"computer architecture and memory hierarchy","qIndex":10,"difficulty":"intermediate","question":"Why are linked lists often slower than arrays in practice, even when O(n) in both cases?","answerFull":"Each node in a linked list is independently heap-allocated at an arbitrary address. Traversal is a pointer-chase: you cannot know the address of node n+1 until you have loaded node n (pointer field). This defeats spatial prefetching — the hardware prefetcher cannot predict the next address. Each node access is a potential L3 miss (~40 ns) or DRAM miss (~100 ns). A sequentially-allocated array allows the prefetcher to stream ahead, keeping data in L1/L2. Traversing 1 million linked-list nodes can be 5–20× slower than traversing an equivalent array, despite identical O(n) complexity.","correct":"Each node in a linked list is independently heap-allocated at an arbitrary address.","distractors":["Spatial locality: if you access address A, you are likely to access addresses near A (A+1, A+2, ...) soon.","A cache line is the minimum unit of data transfer between cache levels and between cache and DRAM.","A cache miss forces the CPU to stall (or switch to other independent instructions) while fetching data from a lower cache level or DRAM."]},{"id":"cs_fundamentals/computer_architecture_and_memory_hierarchy#68f7bd8db9af","section":"cs_fundamentals","module":"cs_fundamentals/computer_architecture_and_memory_hierarchy","moduleName":"computer architecture and memory hierarchy","qIndex":11,"difficulty":"advanced","question":"What is write-through vs write-back caching, and which does x86-64 use?","answerFull":"Write-through: every store writes simultaneously to the cache and to the next level of the hierarchy (L2 or DRAM). Simpler to implement and always consistent, but generates high write bandwidth. Write-back: the store writes only to the cache; the line is marked \"dirty.\" The dirty line is written back to the next level only when evicted. Generates far less write traffic. Modern x86-64 processors use write-back for L2 and L3; L1 may use write-through to L2 (implementation-specific). DRAM writes happen only on cache eviction or explicit clflush.","correct":"Write-through: every store writes simultaneously to the cache and to the next level of the hierarchy (L2 or DRAM).","distractors":["Non-Uniform Memory Access (NUMA): in a multi-socket server, each CPU socket has its own memory controller and local DRAM.","In a multi-core CPU, each core has its own L1/L2 cache.","A pipeline hazard is a situation that prevents the next instruction from executing in the next cycle."]},{"id":"cs_fundamentals/computer_architecture_and_memory_hierarchy#fa940c15dee5","section":"cs_fundamentals","module":"cs_fundamentals/computer_architecture_and_memory_hierarchy","moduleName":"computer architecture and memory hierarchy","qIndex":12,"difficulty":"advanced","question":"How does the hardware prefetcher work, and what patterns does it handle poorly?","answerFull":"The hardware prefetcher monitors the stream of cache miss addresses. If it detects a stride pattern (e.g., misses at +64, +128, +192 bytes), it issues speculative DRAM reads ahead of time, filling the cache before the CPU explicitly requests the data. It handles sequential access and fixed-stride access well. It handles poorly: pointer chasing (linked lists, trees) where the next address is unknown until the current load completes; irregular strides; access patterns that switch stride mid-stream. Software prefetch (builtin_prefetch in GCC, _mm_prefetch intrinsic) can compensate for pointer-chase patterns in performance-critical code.","correct":"The hardware prefetcher monitors the stream of cache miss addresses.","distractors":["Spatial locality: if you access address A, you are likely to access addresses near A (A+1, A+2, ...) soon.","Write-through: every store writes simultaneously to the cache and to the next level of the hierarchy (L2 or DRAM).","A cache miss forces the CPU to stall (or switch to other independent instructions) while fetching data from a lower cache level or DRAM."]},{"id":"cs_fundamentals/computer_architecture_and_memory_hierarchy#61becc21700b","section":"cs_fundamentals","module":"cs_fundamentals/computer_architecture_and_memory_hierarchy","moduleName":"computer architecture and memory hierarchy","qIndex":13,"difficulty":"advanced","question":"What is the Branch Target Buffer (BTB)?","answerFull":"The BTB is a CPU cache that stores recent branch instruction addresses mapped to their predicted target addresses. When the CPU fetches a branch instruction, it looks it up in the BTB before even fully decoding the instruction. If found, the CPU immediately starts fetching from the predicted target. The BTB handles both conditional branches (predicting not-taken/taken) and indirect branches (e.g., virtual function calls, function pointers), where the target address is computed at runtime. Spectre variant 2 (branch target injection) exploits the BTB to leak data across process boundaries.","correct":"The BTB is a CPU cache that stores recent branch instruction addresses mapped to their predicted target addresses.","distractors":["In a multi-core CPU, each core has its own L1/L2 cache.","The hardware prefetcher monitors the stream of cache miss addresses.","Sorting places all values that satisfy a branch condition contiguously, so the CPU's branch predictor learns the pattern (long run of \"not taken\" → long run of \"taken\")."]},{"id":"cs_fundamentals/computer_architecture_and_memory_hierarchy#9df6cbe66007","section":"cs_fundamentals","module":"cs_fundamentals/computer_architecture_and_memory_hierarchy","moduleName":"computer architecture and memory hierarchy","qIndex":14,"difficulty":"advanced","question":"How does row-major vs column-major storage affect NumPy performance?","answerFull":"NumPy defaults to C-order (row-major) storage: elements of a row are contiguous in memory. Iterating row-by-row achieves sequential memory access (spatial locality). Column-by-column iteration jumps by ncols * 8 bytes between accesses — almost certainly a cache miss for large arrays. For column-heavy workloads, use Fortran-order: np.array(data, order='F') or np.asfortranarray(arr). For mixed-access patterns, explicit transposition arr.T returns a Fortran-order view with zero data copy. NumPy's vectorized operations (.sum(), .mean()) internally stride optimally and use SIMD regardless of order for simple reductions.","correct":"NumPy defaults to C-order (row-major) storage: elements of a row are contiguous in memory.","distractors":["Out-of-order (OoO) execution: the CPU does not execute instructions in strict program order.","Non-Uniform Memory Access (NUMA): in a multi-socket server, each CPU socket has its own memory controller and local DRAM.","A cache line is the minimum unit of data transfer between cache levels and between cache and DRAM."]},{"id":"cs_fundamentals/computer_architecture_and_memory_hierarchy#5b57541e286a","section":"cs_fundamentals","module":"cs_fundamentals/computer_architecture_and_memory_hierarchy","moduleName":"computer architecture and memory hierarchy","qIndex":15,"difficulty":"advanced","question":"What are the concrete latency numbers for each level of the memory hierarchy?","answerFull":"Registers: < 1 ns (0–1 cycle). L1 cache: 1–4 ns (4–12 cycles), 32–64 KB per core. L2 cache: ~10 ns (30–40 cycles), 256 KB–1 MB per core. L3 cache: ~40 ns (100–130 cycles), 4–32 MB shared. DRAM: ~100 ns (~300 cycles at 3 GHz). NVMe SSD: ~100 µs (100,000 ns). SATA SSD: ~500 µs. HDD: ~10 ms (10,000,000 ns). These numbers vary slightly by microarchitecture (Intel Golden Cove, AMD Zen 4, Apple M3) but the ratios are stable: L1 is ~100× faster than DRAM; DRAM is ~1000× faster than HDD.","correct":"Registers: < 1 ns (0–1 cycle).","distractors":["A cache miss forces the CPU to stall (or switch to other independent instructions) while fetching data from a lower cache level or DRAM.","Non-Uniform Memory Access (NUMA): in a multi-socket server, each CPU socket has its own memory controller and local DRAM.","Write-through: every store writes simultaneously to the cache and to the next level of the hierarchy (L2 or DRAM)."]},{"id":"cs_fundamentals/computer_architecture_and_memory_hierarchy#6202ce862940","section":"cs_fundamentals","module":"cs_fundamentals/computer_architecture_and_memory_hierarchy","moduleName":"computer architecture and memory hierarchy","qIndex":16,"difficulty":"advanced","question":"What is out-of-order execution, and how does it relate to the memory hierarchy?","answerFull":"Out-of-order (OoO) execution: the CPU does not execute instructions in strict program order. Instead, a scheduler (reservation station) tracks which instructions have all their operands ready and issues them to execution units as soon as they are ready, regardless of program order. The reorder buffer (ROB) holds results until they can be committed in program order (ensuring precise exceptions). OoO execution hides memory latency: while an L3-miss load is pending (~130 cycles), the CPU finds other independent instructions to execute, keeping execution units busy. Without OoO, a single L3 miss would stall the pipeline for 130 cycles.","correct":"Out-of-order (OoO) execution: the CPU does not execute instructions in strict program order.","distractors":["Branch misprediction occurs when the CPU's branch predictor guesses the wrong path for a conditional branch.","A cache miss forces the CPU to stall (or switch to other independent instructions) while fetching data from a lower cache level or DRAM.","Write-through: every store writes simultaneously to the cache and to the next level of the hierarchy (L2 or DRAM)."]},{"id":"cs_fundamentals/cpu_scheduling_algorithms#92a9263d7fd1","section":"cs_fundamentals","module":"cs_fundamentals/cpu_scheduling_algorithms","moduleName":"cpu scheduling algorithms","qIndex":1,"difficulty":"core","question":"What is the convoy effect in FCFS and why is it a problem?","answerFull":"In FCFS, a long CPU-bound process at the head of the queue blocks all subsequent processes until it completes. If a 30-second job arrives just before 100 one-second jobs, all 100 jobs wait 30 seconds before getting any CPU. Average waiting time becomes O(longest_burst). Interactive systems are severely affected because even tiny requests (mouse events, key presses) queue behind long batch jobs. Solution: preemptive scheduling with prioritisation for short tasks.","correct":"In FCFS, a long CPU-bound process at the head of the queue blocks all subsequent processes until it completes.","distractors":["Scheduling latency: time from a thread becoming runnable to when it actually gets CPU.","Preemptive: the OS can forcibly take the CPU from a running thread (on timer interrupt or higher-priority thread wake-up).","Work stealing: each CPU has a local deque of tasks."]},{"id":"cs_fundamentals/cpu_scheduling_algorithms#ce043bc39526","section":"cs_fundamentals","module":"cs_fundamentals/cpu_scheduling_algorithms","moduleName":"cpu scheduling algorithms","qIndex":3,"difficulty":"core","question":"How does Round-Robin differ from FCFS in terms of response time and throughput?","answerFull":"Response time: RR is much better — every process gets CPU within one quantum period (10–50 ms is typical). A new process waits at most (n-1) × quantum if there are n runnable processes. Throughput: RR is slightly lower than FCFS because context-switch overhead is paid frequently. For purely CPU-bound tasks with equal bursts, RR's turnaround time is actually worse than FCFS (each task completes later because it interleaves). RR excels for interactive workloads where response time matters more than turnaround time.","correct":"Response time: RR is much better — every process gets CPU within one quantum period (10–50 ms is typical).","distractors":["Every context switch has a \"cache footprint\" cost: the evicted process's working set is flushed from L1/L2 caches (often 128 KB–1 MB); the incoming process starts with cold caches.","In FCFS, a long CPU-bound process at the head of the queue blocks all subsequent processes until it completes.","CFS tracks each runnable thread's virtual runtime — the total CPU time received, inversely weighted by nice priority."]},{"id":"cs_fundamentals/cpu_scheduling_algorithms#c8bbe090ca2c","section":"cs_fundamentals","module":"cs_fundamentals/cpu_scheduling_algorithms","moduleName":"cpu scheduling algorithms","qIndex":4,"difficulty":"core","question":"What is Multi-Level Feedback Queue (MLFQ) and how does it approximate SRTF?","answerFull":"MLFQ maintains multiple queues with decreasing priority and increasing quantum. New processes start in the highest-priority queue (short quantum). If a process uses its full quantum, it is demoted to a lower queue (assumed to be CPU-bound, given longer quantum). If it blocks before its quantum expires, it stays in the same or higher queue (assumed to be interactive/I/O-bound, needs low latency). CPU-bound processes are demoted over time; I/O-bound processes stay at high priority — approximating \"give short jobs priority\" without knowing burst times. Aging prevents starvation.","correct":"MLFQ maintains multiple queues with decreasing priority and increasing quantum.","distractors":["Response time: RR is much better — every process gets CPU within one quantum period (10–50 ms is typical).","Preemptive: the OS can forcibly take the CPU from a running thread (on timer interrupt or higher-priority thread wake-up).","Nice values: -20 (highest priority) to +19 (lowest)."]},{"id":"cs_fundamentals/cpu_scheduling_algorithms#eaab786b849a","section":"cs_fundamentals","module":"cs_fundamentals/cpu_scheduling_algorithms","moduleName":"cpu scheduling algorithms","qIndex":5,"difficulty":"core","question":"What is the Completely Fair Scheduler (CFS) and how is it different from Round-Robin?","answerFull":"CFS tracks each runnable thread's virtual runtime — the total CPU time received, inversely weighted by nice priority. CFS always runs the thread with the smallest vruntime (minimum in a red-black tree). Unlike RR's fixed quantum, CFS's effective quantum is dynamic: the target latency (8–24 ms) divided by the number of runnable threads. Fairness guarantee: each thread gets exactly its proportional share over time. CFS weights are exponential in nice level: each nice level changes weight by ~25%.","correct":"CFS tracks each runnable thread's virtual runtime — the total CPU time received, inversely weighted by nice priority.","distractors":["Priority inversion: a high-priority thread H waits for a mutex held by low-priority thread L; a medium-priority thread M preempts L and runs indefinitely; H is blocked because M prevents L from releasing the mutex.","Response time: RR is much better — every process gets CPU within one quantum period (10–50 ms is typical).","Short quantum (1 ms): excellent response time (every process gets CPU within n ms); poor throughput because context-switch overhead (~5 µs) becomes a significant fraction of the quantum (0.5%)."]},{"id":"cs_fundamentals/cpu_scheduling_algorithms#843151976116","section":"cs_fundamentals","module":"cs_fundamentals/cpu_scheduling_algorithms","moduleName":"cpu scheduling algorithms","qIndex":6,"difficulty":"intermediate","question":"What is priority inversion and how does priority inheritance solve it?","answerFull":"Priority inversion: a high-priority thread H waits for a mutex held by low-priority thread L; a medium-priority thread M preempts L and runs indefinitely; H is blocked because M prevents L from releasing the mutex. Priority inheritance: when L holds a mutex that H is waiting for, the OS temporarily boosts L's priority to H's level. L finishes quickly, releases the mutex, H unblocks. L's priority returns to its normal level. Example: Mars Pathfinder (1997) — a VxWorks real-time system reset repeatedly due to priority inversion until priority inheritance was re-enabled.","correct":"Priority inversion: a high-priority thread H waits for a mutex held by low-priority thread L; a medium-priority thread M preempts L and runs indefinitely; H is blocked because M prevents L from releasing the mutex.","distractors":["MLFQ maintains multiple queues with decreasing priority and increasing quantum.","Nice values: -20 (highest priority) to +19 (lowest).","CPU affinity (taskset, pthread_setaffinity_np) pins a thread to specific CPU cores."]},{"id":"cs_fundamentals/cpu_scheduling_algorithms#bd53963b11af","section":"cs_fundamentals","module":"cs_fundamentals/cpu_scheduling_algorithms","moduleName":"cpu scheduling algorithms","qIndex":7,"difficulty":"intermediate","question":"What is the time quantum selection tradeoff in Round-Robin?","answerFull":"Short quantum (1 ms): excellent response time (every process gets CPU within n ms); poor throughput because context-switch overhead (~5 µs) becomes a significant fraction of the quantum (0.5%). Long quantum (100 ms): good throughput; poor response time for n > 10 processes (wait up to 1 second). Sweet spot: quantum >> context-switch time, but small enough for interactive responsiveness. Linux CFS uses a minimum granularity of 4 ms and a target latency of 8–24 ms.","correct":"Short quantum (1 ms): excellent response time (every process gets CPU within n ms); poor throughput because context-switch overhead (~5 µs) becomes a significant fraction of the quantum (0.5%).","distractors":["CPU affinity (taskset, pthread_setaffinity_np) pins a thread to specific CPU cores.","MLFQ maintains multiple queues with decreasing priority and increasing quantum.","CFS tracks each runnable thread's virtual runtime — the total CPU time received, inversely weighted by nice priority."]},{"id":"cs_fundamentals/cpu_scheduling_algorithms#7a164b68af19","section":"cs_fundamentals","module":"cs_fundamentals/cpu_scheduling_algorithms","moduleName":"cpu scheduling algorithms","qIndex":8,"difficulty":"intermediate","question":"How does SCHED_DEADLINE work in Linux and when should you use it?","answerFull":"SCHED_DEADLINE implements Earliest Deadline First (EDF) — the theoretically optimal real-time scheduling algorithm. Each task specifies runtime (max CPU time per period), deadline (must finish within deadline of activation), and period. Linux guarantees that the task runs for runtime within every period, preempting other tasks if necessary. Use for hard real-time tasks (audio processing at 48 kHz needs 0.02 ms of work every 0.021 ms). Not suitable for general-purpose scheduling — requires careful capacity planning (sum of runtime/period over all SCHED_DEADLINE tasks must be ≤ 1 per CPU).","correct":"SCHED_DEADLINE implements Earliest Deadline First (EDF) — the theoretically optimal real-time scheduling algorithm.","distractors":["Scheduling latency: time from a thread becoming runnable to when it actually gets CPU.","Short quantum (1 ms): excellent response time (every process gets CPU within n ms); poor throughput because context-switch overhead (~5 µs) becomes a significant fraction of the quantum (0.5%).","Work stealing: each CPU has a local deque of tasks."]},{"id":"cs_fundamentals/cpu_scheduling_algorithms#a380fc858818","section":"cs_fundamentals","module":"cs_fundamentals/cpu_scheduling_algorithms","moduleName":"cpu scheduling algorithms","qIndex":9,"difficulty":"intermediate","question":"What is the difference between CPU burst and I/O burst, and how do they affect scheduling?","answerFull":"A CPU burst is a period of pure computation without I/O. An I/O burst is a period blocked waiting for disk/network. Most interactive processes alternate short CPU bursts with I/O bursts. Most batch/compute processes have long CPU bursts and rare I/O. Schedulers (MLFQ, CFS) try to detect burst patterns: frequent short CPU bursts → high priority (interactive, I/O-bound); infrequent long CPU bursts → lower priority (CPU-bound). The scheduler classifies processes by observed behaviour, not by declaration.","correct":"A CPU burst is a period of pure computation without I/O.","distractors":["SCHED_DEADLINE implements Earliest Deadline First (EDF) — the theoretically optimal real-time scheduling algorithm.","Short quantum (1 ms): excellent response time (every process gets CPU within n ms); poor throughput because context-switch overhead (~5 µs) becomes a significant fraction of the quantum (0.5%).","Preemptive: the OS can forcibly take the CPU from a running thread (on timer interrupt or higher-priority thread wake-up)."]},{"id":"cs_fundamentals/cpu_scheduling_algorithms#c17b799bbb80","section":"cs_fundamentals","module":"cs_fundamentals/cpu_scheduling_algorithms","moduleName":"cpu scheduling algorithms","qIndex":10,"difficulty":"intermediate","question":"How does the Linux nice value map to CFS scheduling priority?","answerFull":"Nice values: -20 (highest priority) to +19 (lowest). CFS maps each nice value to a weight using a fixed table where adjacent nice levels differ by ~25% (factor 1.25). Nice 0 → weight 1024. Nice -1 → weight 1277 (1024 × 1.25). Nice +1 → weight 820 (1024 / 1.25). A process at nice -20 gets 1024 / 15 ≈ 68× more CPU than a process at nice +19 when both are runnable. A process at nice +19 is not \"idle-only\" — it still gets CPU when no higher-priority process wants it.","correct":"Nice values: -20 (highest priority) to +19 (lowest).","distractors":["MLFQ maintains multiple queues with decreasing priority and increasing quantum.","Priority inversion: a high-priority thread H waits for a mutex held by low-priority thread L; a medium-priority thread M preempts L and runs indefinitely; H is blocked because M prevents L from releasing the mutex.","Preemptive: the OS can forcibly take the CPU from a running thread (on timer interrupt or higher-priority thread wake-up)."]},{"id":"cs_fundamentals/cpu_scheduling_algorithms#721bed4bdb7e","section":"cs_fundamentals","module":"cs_fundamentals/cpu_scheduling_algorithms","moduleName":"cpu scheduling algorithms","qIndex":11,"difficulty":"advanced","question":"What is scheduling latency vs throughput, and how do they conflict?","answerFull":"Scheduling latency: time from a thread becoming runnable to when it actually gets CPU. Throughput: tasks completed per second. They conflict because reducing latency requires frequent context switches (preempt the running task quickly for the newly ready task), and context switches have overhead (~1–10 µs) that reduces CPU time available for actual work. Real-time systems minimise latency (preempt immediately); batch systems maximise throughput (run tasks to completion). Servers balance: low latency for interactive requests, high throughput for batch jobs.","correct":"Scheduling latency: time from a thread becoming runnable to when it actually gets CPU.","distractors":["A CPU burst is a period of pure computation without I/O.","CPU affinity (taskset, pthread_setaffinity_np) pins a thread to specific CPU cores.","Response time: RR is much better — every process gets CPU within one quantum period (10–50 ms is typical)."]},{"id":"cs_fundamentals/cpu_scheduling_algorithms#95be282af204","section":"cs_fundamentals","module":"cs_fundamentals/cpu_scheduling_algorithms","moduleName":"cpu scheduling algorithms","qIndex":12,"difficulty":"advanced","question":"How does CPU affinity improve performance and when do you use it?","answerFull":"CPU affinity (taskset, pthread_setaffinity_np) pins a thread to specific CPU cores. Benefits: (1) Warm L1/L2 cache — if a thread always runs on the same core, its working set stays in that core's private L1/L2 (~few µs access) instead of migrating to a different core's cold cache (~40 ns L3 miss). (2) Reduced NUMA overhead on multi-socket servers — pin processes to the socket with local memory. (3) Predictable latency for real-time tasks. Use in: database servers (pin the hot shard to a core), trading systems (pin the order-matching engine to an isolated core), and network packet processing (poll-mode drivers with per-core queues).","correct":"CPU affinity (taskset, pthread_setaffinity_np) pins a thread to specific CPU cores.","distractors":["A CPU burst is a period of pure computation without I/O.","Work stealing: each CPU has a local deque of tasks.","Response time: RR is much better — every process gets CPU within one quantum period (10–50 ms is typical)."]},{"id":"cs_fundamentals/cpu_scheduling_algorithms#0be0b2698bf5","section":"cs_fundamentals","module":"cs_fundamentals/cpu_scheduling_algorithms","moduleName":"cpu scheduling algorithms","qIndex":13,"difficulty":"advanced","question":"What is work stealing in multi-core scheduling and why is it important for task-parallel programs?","answerFull":"Work stealing: each CPU has a local deque of tasks. A CPU that runs out of work steals tasks from the back (LIFO) of another CPU's deque. The owning CPU takes tasks from the front (LIFO for locality). Work stealing achieves near-optimal load balancing with minimal synchronisation — steals are rare (only when idle). Used in: Java's ForkJoinPool, Go's goroutine scheduler, Cilk (MIT), and Intel TBB. It is why ForkJoinPool.commonPool() used by Java's parallel streams is efficient for divide-and-conquer workloads.","correct":"Work stealing: each CPU has a local deque of tasks.","distractors":["Scheduling latency: time from a thread becoming runnable to when it actually gets CPU.","In FCFS, a long CPU-bound process at the head of the queue blocks all subsequent processes until it completes.","CPU affinity (taskset, pthread_setaffinity_np) pins a thread to specific CPU cores."]},{"id":"cs_fundamentals/cpu_scheduling_algorithms#4dacf87723a2","section":"cs_fundamentals","module":"cs_fundamentals/cpu_scheduling_algorithms","moduleName":"cpu scheduling algorithms","qIndex":14,"difficulty":"advanced","question":"What is the difference between preemptive and non-preemptive scheduling and why does it matter for latency?","answerFull":"Preemptive: the OS can forcibly take the CPU from a running thread (on timer interrupt or higher-priority thread wake-up). Non-preemptive: a thread runs until it voluntarily yields. Preemptive scheduling guarantees bounded scheduling latency: a high-priority thread waiting for CPU will get it within one time quantum (4–10 ms). Non-preemptive scheduling can delay arbitrarily — a compute-bound thread that never yields blocks everyone. Modern OS schedulers are fully preemptive. The Linux real-time preemption patch (PREEMPT_RT) makes even kernel code fully preemptible, achieving sub-100 µs scheduling latency for SCHED_FIFO tasks.","correct":"Preemptive: the OS can forcibly take the CPU from a running thread (on timer interrupt or higher-priority thread wake-up).","distractors":["SCHED_DEADLINE implements Earliest Deadline First (EDF) — the theoretically optimal real-time scheduling algorithm.","CFS tracks each runnable thread's virtual runtime — the total CPU time received, inversely weighted by nice priority.","A CPU burst is a period of pure computation without I/O."]},{"id":"cs_fundamentals/cpu_scheduling_algorithms#9144c488bda2","section":"cs_fundamentals","module":"cs_fundamentals/cpu_scheduling_algorithms","moduleName":"cpu scheduling algorithms","qIndex":15,"difficulty":"advanced","question":"How does CPU scheduling interact with cache performance?","answerFull":"Every context switch has a \"cache footprint\" cost: the evicted process's working set is flushed from L1/L2 caches (often 128 KB–1 MB); the incoming process starts with cold caches. On a 4-core machine with 6 MB shared L3: if 10 threads each have 1 MB working sets, only 6 can be \"warm\" at once — the other 4 incur L3 misses (~40 ns each) or even DRAM reads (~100 ns). For latency-sensitive services, limiting the number of runnable threads to fit their combined working sets in L3 improves performance more than scheduler tuning.","correct":"Every context switch has a \"cache footprint\" cost: the evicted process's working set is flushed from L1/L2 caches (often 128 KB–1 MB); the incoming process starts with cold caches.","distractors":["A CPU burst is a period of pure computation without I/O.","Work stealing: each CPU has a local deque of tasks.","Response time: RR is much better — every process gets CPU within one quantum period (10–50 ms is typical)."]},{"id":"cs_fundamentals/cryptography_fundamentals#2430541bd2fa","section":"cs_fundamentals","module":"cs_fundamentals/cryptography_fundamentals","moduleName":"cryptography fundamentals","qIndex":1,"difficulty":"core","question":"Why should you never use SHA-256 directly to store passwords?","answerFull":"SHA-256 is designed to be fast — modern GPUs compute over 1 billion SHA-256 hashes per second. An attacker who steals your password database can attempt billions of guesses per second. Password hashing requires intentional slowness (bcrypt at cost 12 takes ~200 ms, limiting an attacker to ~1,000 guesses/second on a GPU) plus a unique salt per user (defeating rainbow table precomputation). Use bcrypt, scrypt, or Argon2 — never plain hash functions — for passwords.","correct":"SHA-256 is designed to be fast — modern GPUs compute over 1 billion SHA-256 hashes per second.","distractors":["SHA-2 functions are susceptible to length extension attacks: given H(m), an attacker can compute H(m || padding || extra) without knowing m.","RSA encryption / decryption is computationally expensive — roughly 10 ms per operation for RSA-2048 decryption, compared to ~1 GB/s for AES-256-GCM.","A KDF derives one or more cryptographic keys from a source of keying material."]},{"id":"cs_fundamentals/cryptography_fundamentals#9da1d34f1aa1","section":"cs_fundamentals","module":"cs_fundamentals/cryptography_fundamentals","moduleName":"cryptography fundamentals","qIndex":2,"difficulty":"core","question":"What is a timing attack and why does it affect MAC verification?","answerFull":"A timing attack exploits the fact that Python's == operator short-circuits — it returns False as soon as it finds the first mismatched byte. An attacker who can make millions of API calls can statistically measure response times to determine how many bytes of their guessed token match the real token, byte by byte. hmac.compare_digest is specifically designed to compare all bytes in constant time regardless of where (or whether) a mismatch occurs, eliminating this information leak.","correct":"A timing attack exploits the fact that Python's == operator short-circuits — it returns False as soon as it finds the first mismatched byte.","distractors":["HMAC uses a shared secret key — both parties can generate and verify the MAC, so you cannot prove to a third party which party generated it (no non-repudiation).","The 16-byte authentication tag is an AEAD (Authenticated Encryption with Associated Data) component — it is essentially an HMAC over the ciphertext and any additional associated data (e.g., headers, metadata).","A KDF derives one or more cryptographic keys from a source of keying material."]},{"id":"cs_fundamentals/cryptography_fundamentals#63567286220a","section":"cs_fundamentals","module":"cs_fundamentals/cryptography_fundamentals","moduleName":"cryptography fundamentals","qIndex":3,"difficulty":"core","question":"Why is HMAC preferred over H(key || message) for MACs?","answerFull":"SHA-2 functions are susceptible to length extension attacks: given H(m), an attacker can compute H(m || padding || extra) without knowing m. If the MAC is H(key || message), the attacker can compute a valid MAC for (message || padding || extra) without knowing the key. HMAC's nested construction — H(K XOR opad || H(K XOR ipad || message)) — prevents length extension because the outer hash wraps the inner hash output rather than the message. SHA-3 (Keccak sponge) is inherently immune to length extension, making H(key || message) with SHA-3 technically safe, but HMAC with SHA-2 is the universally deployed standard.","correct":"SHA-2 functions are susceptible to length extension attacks: given H(m), an attacker can compute H(m || padding || extra) without knowing m.","distractors":["Forward secrecy (perfect forward secrecy) means that compromise of a long-term private key does not compromise past session recordings.","Hashing is one-way and deterministic: given H(m), you cannot recover m (only verify against it).","The 16-byte authentication tag is an AEAD (Authenticated Encryption with Associated Data) component — it is essentially an HMAC over the ciphertext and any additional associated data (e.g., headers, metadata)."]},{"id":"cs_fundamentals/cryptography_fundamentals#8a403e52d856","section":"cs_fundamentals","module":"cs_fundamentals/cryptography_fundamentals","moduleName":"cryptography fundamentals","qIndex":4,"difficulty":"core","question":"What is the difference between encryption and hashing?","answerFull":"Hashing is one-way and deterministic: given H(m), you cannot recover m (only verify against it). Encryption is two-way: given ciphertext and key, you can recover the original plaintext. Hashing is used for integrity verification and password storage; encryption is used for confidentiality (protecting data you need to read later). A common mistake is \"encrypting\" passwords — if you can decrypt them, so can an attacker who compromises your key.","correct":"Hashing is one-way and deterministic: given H(m), you cannot recover m (only verify against it).","distractors":["A salt is a random value (typically 16–32 bytes) generated uniquely per user and prepended (or combined) with the password before hashing.","RSA encryption / decryption is computationally expensive — roughly 10 ms per operation for RSA-2048 decryption, compared to ~1 GB/s for AES-256-GCM.","SHA-2 functions are susceptible to length extension attacks: given H(m), an attacker can compute H(m || padding || extra) without knowing m."]},{"id":"cs_fundamentals/cryptography_fundamentals#7c7abad9eb5f","section":"cs_fundamentals","module":"cs_fundamentals/cryptography_fundamentals","moduleName":"cryptography fundamentals","qIndex":5,"difficulty":"core","question":"What is a nonce and why must AES-GCM nonces be unique?","answerFull":"A nonce (number used once) is the initialization vector for AES-GCM. AES-GCM generates a keystream: a sequence of pseudorandom bytes derived from key + nonce, then XORs this keystream with the plaintext. If two different plaintexts are encrypted with the same key and same nonce, both ciphertexts are XORed with the same keystream. XORing the two ciphertexts cancels the keystream (C1 XOR C2 = P1 XOR P2), giving the attacker direct XOR of the plaintexts — sufficient to reconstruct both. Additionally, the authentication tag mechanism is completely broken. Always generate a random 12-byte nonce per message with os.urandom(12).","correct":"A nonce (number used once) is the initialization vector for AES-GCM.","distractors":["RSA encryption / decryption is computationally expensive — roughly 10 ms per operation for RSA-2048 decryption, compared to ~1 GB/s for AES-256-GCM.","bcrypt is the Blowfish cipher initialization function (expensive_key_setup) applied to a password.","A KDF derives one or more cryptographic keys from a source of keying material."]},{"id":"cs_fundamentals/cryptography_fundamentals#de2cc2f732d9","section":"cs_fundamentals","module":"cs_fundamentals/cryptography_fundamentals","moduleName":"cryptography fundamentals","qIndex":6,"difficulty":"intermediate","question":"What is the Diffie-Hellman key exchange and what problem does it solve?","answerFull":"DH solves the key distribution problem: how to establish a shared symmetric secret between two parties communicating over an untrusted channel, without ever transmitting that secret. Both parties exchange public values (g^a mod p and g^b mod p), each apply their private value to the other's public value, and arrive at the same shared secret g^ab mod p. An eavesdropper who sees both public values cannot compute g^ab without solving the discrete logarithm problem. ECDH does the same over elliptic curves with smaller keys (256-bit vs 2048-bit) and faster computation (~0.1 ms vs ~10 ms).","correct":"DH solves the key distribution problem: how to establish a shared symmetric secret between two parties communicating over an untrusted channel, without ever transmitting that secret.","distractors":["bcrypt is the Blowfish cipher initialization function (expensive_key_setup) applied to a password.","SHA-2 functions are susceptible to length extension attacks: given H(m), an attacker can compute H(m || padding || extra) without knowing m.","Forward secrecy (perfect forward secrecy) means that compromise of a long-term private key does not compromise past session recordings."]},{"id":"cs_fundamentals/cryptography_fundamentals#4139a63593e3","section":"cs_fundamentals","module":"cs_fundamentals/cryptography_fundamentals","moduleName":"cryptography fundamentals","qIndex":7,"difficulty":"intermediate","question":"What is the difference between HMAC and digital signatures?","answerFull":"HMAC uses a shared secret key — both parties can generate and verify the MAC, so you cannot prove to a third party which party generated it (no non-repudiation). Digital signatures use a public/private key pair — only the private key holder can generate the signature, but anyone with the public key can verify it. If Alice signs a message with her private key, she cannot later deny signing it (non-repudiation). HMAC is faster and simpler; digital signatures are needed when you need to prove authorship to parties who don't share your secret.","correct":"HMAC uses a shared secret key — both parties can generate and verify the MAC, so you cannot prove to a third party which party generated it (no non-repudiation).","distractors":["Hashing is one-way and deterministic: given H(m), you cannot recover m (only verify against it).","DH solves the key distribution problem: how to establish a shared symmetric secret between two parties communicating over an untrusted channel, without ever transmitting that secret.","The 16-byte authentication tag is an AEAD (Authenticated Encryption with Associated Data) component — it is essentially an HMAC over the ciphertext and any additional associated data (e.g., headers, metadata)."]},{"id":"cs_fundamentals/cryptography_fundamentals#4d8f1e8c04e6","section":"cs_fundamentals","module":"cs_fundamentals/cryptography_fundamentals","moduleName":"cryptography fundamentals","qIndex":8,"difficulty":"intermediate","question":"What is a salt, and how does it prevent rainbow table attacks?","answerFull":"A salt is a random value (typically 16–32 bytes) generated uniquely per user and prepended (or combined) with the password before hashing. Without salting, an attacker can precompute a rainbow table: a large precomputed database mapping hashes back to passwords (billions of entries, gigabytes in size). Any user whose password is in the table is cracked instantly. With salting, the attacker must compute H(unique_salt + password) separately for each user — precomputation becomes impossible because each salt makes the hash space unique. Modern algorithms (bcrypt, Argon2) embed the salt automatically in the output string.","correct":"A salt is a random value (typically 16–32 bytes) generated uniquely per user and prepended (or combined) with the password before hashing.","distractors":["SHA-2 functions are susceptible to length extension attacks: given H(m), an attacker can compute H(m || padding || extra) without knowing m.","Hashing is one-way and deterministic: given H(m), you cannot recover m (only verify against it).","bcrypt is the Blowfish cipher initialization function (expensive_key_setup) applied to a password."]},{"id":"cs_fundamentals/cryptography_fundamentals#819e638f2892","section":"cs_fundamentals","module":"cs_fundamentals/cryptography_fundamentals","moduleName":"cryptography fundamentals","qIndex":9,"difficulty":"intermediate","question":"What does AES-GCM's authentication tag protect against?","answerFull":"The 16-byte authentication tag is an AEAD (Authenticated Encryption with Associated Data) component — it is essentially an HMAC over the ciphertext and any additional associated data (e.g., headers, metadata). If the ciphertext is tampered with in transit (bit flipping, truncation, reordering), the tag verification fails and decryption raises an exception before any plaintext is returned. Without authentication (e.g., AES-CBC without a separate MAC), bit-flipping attacks on CBC allow an attacker to make predictable modifications to the decrypted plaintext. Always use an AEAD mode; never use AES-ECB or AES-CBC without a MAC.","correct":"The 16-byte authentication tag is an AEAD (Authenticated Encryption with Associated Data) component — it is essentially an HMAC over the ciphertext and any additional associated data (e.g., headers, metadata).","distractors":["Hashing is one-way and deterministic: given H(m), you cannot recover m (only verify against it).","RSA encryption / decryption is computationally expensive — roughly 10 ms per operation for RSA-2048 decryption, compared to ~1 GB/s for AES-256-GCM.","A timing attack exploits the fact that Python's == operator short-circuits — it returns False as soon as it finds the first mismatched byte."]},{"id":"cs_fundamentals/cryptography_fundamentals#dc0b2b014e53","section":"cs_fundamentals","module":"cs_fundamentals/cryptography_fundamentals","moduleName":"cryptography fundamentals","qIndex":10,"difficulty":"intermediate","question":"Why is RSA used to exchange keys but not to encrypt bulk data?","answerFull":"RSA encryption / decryption is computationally expensive — roughly 10 ms per operation for RSA-2048 decryption, compared to ~1 GB/s for AES-256-GCM. RSA also has a maximum message size (limited to key size minus padding overhead — about 190 bytes for RSA-2048 with OAEP). The standard pattern is hybrid encryption: use RSA to encrypt a randomly generated 32-byte AES key, then use AES-GCM to encrypt the actual data. This gives the key distribution advantages of asymmetric crypto with the speed of symmetric crypto.","correct":"RSA encryption / decryption is computationally expensive — roughly 10 ms per operation for RSA-2048 decryption, compared to ~1 GB/s for AES-256-GCM.","distractors":["The 16-byte authentication tag is an AEAD (Authenticated Encryption with Associated Data) component — it is essentially an HMAC over the ciphertext and any additional associated data (e.g., headers, metadata).","A nonce (number used once) is the initialization vector for AES-GCM.","A KDF derives one or more cryptographic keys from a source of keying material."]},{"id":"cs_fundamentals/cryptography_fundamentals#e9492a030575","section":"cs_fundamentals","module":"cs_fundamentals/cryptography_fundamentals","moduleName":"cryptography fundamentals","qIndex":11,"difficulty":"advanced","question":"What is forward secrecy and how does ECDH provide it?","answerFull":"Forward secrecy (perfect forward secrecy) means that compromise of a long-term private key does not compromise past session recordings. TLS 1.3 achieves this by using ephemeral ECDH: each TLS session generates a new temporary key pair, derives a session key, then discards the private key. Even if an attacker records all traffic and later steals the server's certificate private key, they cannot decrypt past sessions because each session used a unique ephemeral ECDH private key that no longer exists. TLS 1.2 with RSA key exchange (sending the AES key encrypted with the server's long-term RSA key) does not have forward secrecy.","correct":"Forward secrecy (perfect forward secrecy) means that compromise of a long-term private key does not compromise past session recordings.","distractors":["HMAC uses a shared secret key — both parties can generate and verify the MAC, so you cannot prove to a third party which party generated it (no non-repudiation).","DH solves the key distribution problem: how to establish a shared symmetric secret between two parties communicating over an untrusted channel, without ever transmitting that secret.","bcrypt is the Blowfish cipher initialization function (expensive_key_setup) applied to a password."]},{"id":"cs_fundamentals/cryptography_fundamentals#0ffb4f45973f","section":"cs_fundamentals","module":"cs_fundamentals/cryptography_fundamentals","moduleName":"cryptography fundamentals","qIndex":12,"difficulty":"advanced","question":"What is the birthday paradox and how does it apply to hash collision resistance?","answerFull":"The birthday paradox states that in a room of 23 people, there is >50% probability two share a birthday. Analogously, for a hash function with n-bit output, you need approximately 2^(n/2) random inputs to find a collision with 50% probability — not 2^n as intuition suggests. SHA-256 has 256-bit output, so collision resistance requires finding 2^128 hashes — computationally infeasible. MD5 (128-bit output) requires only 2^64 hashes to find a collision — feasible with modern hardware, and actual collision-generating tools exist.","correct":"The birthday paradox states that in a room of 23 people, there is >50% probability two share a birthday.","distractors":["A KDF derives one or more cryptographic keys from a source of keying material.","A nonce (number used once) is the initialization vector for AES-GCM.","A salt is a random value (typically 16–32 bytes) generated uniquely per user and prepended (or combined) with the password before hashing."]},{"id":"cs_fundamentals/cryptography_fundamentals#f64e0ed3053a","section":"cs_fundamentals","module":"cs_fundamentals/cryptography_fundamentals","moduleName":"cryptography fundamentals","qIndex":13,"difficulty":"advanced","question":"What is the difference between entropy and randomness in cryptography?","answerFull":"Cryptographic security requires high-entropy randomness — unpredictability backed by physical randomness sources. Python's random module is a pseudo-random number generator (Mersenne Twister) — its state can be predicted from 624 outputs, making it completely inappropriate for security. secrets.token_bytes(n) uses the OS CSPRNG (on Linux: getrandom() or /dev/urandom; on macOS: arc4random) which gathers entropy from hardware events (interrupt timing, disk activity, thermal noise). Always use secrets for generating keys, salts, tokens, and any security-critical random values.","correct":"Cryptographic security requires high-entropy randomness — unpredictability backed by physical randomness sources.","distractors":["A KDF derives one or more cryptographic keys from a source of keying material.","A nonce (number used once) is the initialization vector for AES-GCM.","A salt is a random value (typically 16–32 bytes) generated uniquely per user and prepended (or combined) with the password before hashing."]},{"id":"cs_fundamentals/cryptography_fundamentals#542728d1db2d","section":"cs_fundamentals","module":"cs_fundamentals/cryptography_fundamentals","moduleName":"cryptography fundamentals","qIndex":14,"difficulty":"advanced","question":"Explain how bcrypt's cost factor provides tunable slowness.","answerFull":"bcrypt is the Blowfish cipher initialization function (expensive_key_setup) applied to a password. The cost factor (typically 10–12) determines the number of iterations: the function performs 2^cost rounds of the expensive key setup. At cost 10: ~100 ms on a modern server; cost 12: ~300–400 ms. As hardware gets faster, you increase the cost factor to maintain the desired verification time. GPUs are relatively ineffective against bcrypt because bcrypt requires large amounts of sequential memory access that GPUs cannot parallelize efficiently, unlike SHA-256.","correct":"bcrypt is the Blowfish cipher initialization function (expensive_key_setup) applied to a password.","distractors":["A nonce (number used once) is the initialization vector for AES-GCM.","HMAC uses a shared secret key — both parties can generate and verify the MAC, so you cannot prove to a third party which party generated it (no non-repudiation).","A salt is a random value (typically 16–32 bytes) generated uniquely per user and prepended (or combined) with the password before hashing."]},{"id":"cs_fundamentals/cryptography_fundamentals#b6c610aff820","section":"cs_fundamentals","module":"cs_fundamentals/cryptography_fundamentals","moduleName":"cryptography fundamentals","qIndex":15,"difficulty":"advanced","question":"What is a key derivation function (KDF) and when would you use PBKDF2 vs HKDF?","answerFull":"A KDF derives one or more cryptographic keys from a source of keying material. PBKDF2 is a password-based KDF: it adds computational hardness (via iteration count) to derive a key from a low-entropy, human-chosen password — its purpose is to make brute-force expensive. HKDF (HMAC-based KDF) is for high-entropy inputs: it derives multiple cryptographically independent keys from an already-secret, high-entropy input like an ECDH shared secret. HKDF adds no computational overhead (it is fast), relying instead on the input's existing entropy. Use PBKDF2 (or bcrypt/Argon2) for passwords; use HKDF for deriving session keys from a key exchange output.","correct":"A KDF derives one or more cryptographic keys from a source of keying material.","distractors":["HMAC uses a shared secret key — both parties can generate and verify the MAC, so you cannot prove to a third party which party generated it (no non-repudiation).","Forward secrecy (perfect forward secrecy) means that compromise of a long-term private key does not compromise past session recordings.","DH solves the key distribution problem: how to establish a shared symmetric secret between two parties communicating over an untrusted channel, without ever transmitting that secret."]},{"id":"cs_fundamentals/cryptography_fundamentals#eb02593bd37e","section":"cs_fundamentals","module":"cs_fundamentals/cryptography_fundamentals","moduleName":"cryptography fundamentals","qIndex":16,"difficulty":"advanced","question":"What is authenticated encryption with associated data (AEAD) and what goes in the \"associated data\"?","answerFull":"AEAD encrypts plaintext (for confidentiality) and also authenticates associated data that is not encrypted (for integrity). Associated data is typically metadata that must be sent in plaintext but must not be tampered with — for example, a packet header, a recipient identifier, or a version number. An attacker cannot modify the associated data without breaking the authentication tag, even though the associated data is not encrypted. In AES-GCM, passing associated data as the aad parameter ensures that both the ciphertext and the metadata are covered by the authentication tag.","correct":"AEAD encrypts plaintext (for confidentiality) and also authenticates associated data that is not encrypted (for integrity).","distractors":["The 16-byte authentication tag is an AEAD (Authenticated Encryption with Associated Data) component — it is essentially an HMAC over the ciphertext and any additional associated data (e.g., headers, metadata).","RSA encryption / decryption is computationally expensive — roughly 10 ms per operation for RSA-2048 decryption, compared to ~1 GB/s for AES-256-GCM.","A KDF derives one or more cryptographic keys from a source of keying material."]},{"id":"cs_fundamentals/database_and_storage_fundamentals#b97ef5d99a0e","section":"cs_fundamentals","module":"cs_fundamentals/database_and_storage_fundamentals","moduleName":"database and storage fundamentals","qIndex":1,"difficulty":"core","question":"What is a dirty read and which isolation level prevents it?","answerFull":"A dirty read occurs when Transaction A reads data written by Transaction B that has not yet committed. If B then rolls back, A has read data that never existed. Read Committed isolation prevents dirty reads by allowing reads only of committed data. Read Uncommitted allows dirty reads and is almost never used in production.","correct":"A dirty read occurs when Transaction A reads data written by Transaction B that has not yet committed.","distractors":["A lost update occurs when two transactions read the same value, each modifies it in application code, and the second COMMIT overwrites the first's change.","MVCC (Multi-Version Concurrency Control) keeps multiple versions of each row, one per committed transaction.","Serializable isolation (via Serializable Snapshot Isolation in PostgreSQL) requires tracking read sets and detecting conflicts, which causes transactions to abort and retry under contention."]},{"id":"cs_fundamentals/database_and_storage_fundamentals#305cc0a2f032","section":"cs_fundamentals","module":"cs_fundamentals/database_and_storage_fundamentals","moduleName":"database and storage fundamentals","qIndex":2,"difficulty":"core","question":"What is the difference between a non-repeatable read and a phantom read?","answerFull":"A non-repeatable read affects a single existing row: T1 reads row R, T2 updates and commits R, T1 reads R again and sees a different value. A phantom read affects a range query result set: T1 runs a range query, T2 inserts a new row that falls in the range and commits, T1 runs the same range query and sees a new row that wasn't there before. Repeatable Read prevents non-repeatable reads but (in the SQL standard) not phantoms; Serializable prevents both.","correct":"A non-repeatable read affects a single existing row: T1 reads row R, T2 updates and commits R, T1 reads R again and sees a different value.","distractors":["A clustered index (InnoDB: the primary key index) stores the actual row data in the leaf nodes; the table IS the index.","A dirty read occurs when Transaction A reads data written by Transaction B that has not yet committed.","A lost update occurs when two transactions read the same value, each modifies it in application code, and the second COMMIT overwrites the first's change."]},{"id":"cs_fundamentals/database_and_storage_fundamentals#06efb3320456","section":"cs_fundamentals","module":"cs_fundamentals/database_and_storage_fundamentals","moduleName":"database and storage fundamentals","qIndex":3,"difficulty":"core","question":"What is a lost update and how do you prevent it?","answerFull":"A lost update occurs when two transactions read the same value, each modifies it in application code, and the second COMMIT overwrites the first's change. Prevention options: (1) atomic SQL expressions (UPDATE t SET x = x + 1), (2) SELECT ... FOR UPDATE to lock the row before reading, (3) optimistic concurrency control with a version/timestamp column checked in the UPDATE WHERE clause, (4) Serializable isolation with automatic conflict detection.","correct":"A lost update occurs when two transactions read the same value, each modifies it in application code, and the second COMMIT overwrites the first's change.","distractors":["The WAL is a sequential append-only log on disk that records the intent of every database change before the change is applied to the data file.","A non-repeatable read affects a single existing row: T1 reads row R, T2 updates and commits R, T1 reads R again and sees a different value.","A deadlock occurs when T1 holds lock A and waits for lock B, while T2 holds lock B and waits for lock A — a cycle."]},{"id":"cs_fundamentals/database_and_storage_fundamentals#38d5346fdd6e","section":"cs_fundamentals","module":"cs_fundamentals/database_and_storage_fundamentals","moduleName":"database and storage fundamentals","qIndex":4,"difficulty":"core","question":"What is MVCC and why does PostgreSQL use it instead of read locks?","answerFull":"MVCC (Multi-Version Concurrency Control) keeps multiple versions of each row, one per committed transaction. Readers see a snapshot consistent with their transaction's start time; they never block writers and writers never block readers. This enables high concurrency on read-heavy workloads. PostgreSQL implements this via a hidden xmin/xmax column on every tuple. The cost is storage bloat (dead tuples) that must be reclaimed by VACUUM. See ../../database/database_fundamentals for full MVCC internals.","correct":"MVCC (Multi-Version Concurrency Control) keeps multiple versions of each row, one per committed transaction.","distractors":["With fanout = 100 (PostgreSQL default 8 KB page), height = ceil(log_100(10,000,000)) = ceil(3.5) = 4.","Serializable isolation (via Serializable Snapshot Isolation in PostgreSQL) requires tracking read sets and detecting conflicts, which causes transactions to abort and retry under contention.","1NF: atomic column values; prevents repeating groups and multi-valued cells."]},{"id":"cs_fundamentals/database_and_storage_fundamentals#8076e4555872","section":"cs_fundamentals","module":"cs_fundamentals/database_and_storage_fundamentals","moduleName":"database and storage fundamentals","qIndex":5,"difficulty":"core","question":"How tall is a B+Tree index on 10 million rows, and how many I/Os does a lookup take?","answerFull":"With fanout = 100 (PostgreSQL default 8 KB page), height = ceil(log_100(10,000,000)) = ceil(3.5) = 4. A single-row lookup traverses 4 levels = 4 page reads in the worst case. In practice the root and upper internal nodes are almost always in the buffer pool (hot pages), so effective I/Os are often 1–2. InnoDB with 16 KB pages has fanout ~200, so height is also 4 for 10 M rows.","correct":"With fanout = 100 (PostgreSQL default 8 KB page), height = ceil(log_100(10,000,000)) = ceil(3.5) = 4.","distractors":["fsync forces the OS to write all dirty pages in the file's page cache to durable storage, bypassing the OS write cache.","The buffer pool (PostgreSQL: shared_buffers; InnoDB: innodb_buffer_pool_size) is the in-memory cache of database pages.","The WAL is a sequential append-only log on disk that records the intent of every database change before the change is applied to the data file."]},{"id":"cs_fundamentals/database_and_storage_fundamentals#ad05e7c566a4","section":"cs_fundamentals","module":"cs_fundamentals/database_and_storage_fundamentals","moduleName":"database and storage fundamentals","qIndex":6,"difficulty":"intermediate","question":"What is the difference between ACID and BASE?","answerFull":"ACID is a set of guarantees (Atomicity, Consistency, Isolation, Durability) for transactions, typically on single-node or synchronously replicated databases. BASE (Basically Available, Soft State, Eventually Consistent) is the alternative contract for distributed systems that prioritise availability and partition tolerance over strict consistency. ACID ensures every read sees the last committed write; BASE accepts that reads may return stale data for a bounded window. The choice is determined by the CAP theorem tradeoff at design time, not a runtime toggle.","correct":"ACID is a set of guarantees (Atomicity, Consistency, Isolation, Durability) for transactions, typically on single-node or synchronously replicated databases.","distractors":["A non-repeatable read affects a single existing row: T1 reads row R, T2 updates and commits R, T1 reads R again and sees a different value.","Serializable isolation (via Serializable Snapshot Isolation in PostgreSQL) requires tracking read sets and detecting conflicts, which causes transactions to abort and retry under contention.","A lost update occurs when two transactions read the same value, each modifies it in application code, and the second COMMIT overwrites the first's change."]},{"id":"cs_fundamentals/database_and_storage_fundamentals#5d479fd7b1ed","section":"cs_fundamentals","module":"cs_fundamentals/database_and_storage_fundamentals","moduleName":"database and storage fundamentals","qIndex":7,"difficulty":"intermediate","question":"What are the three normalisation forms, and what anomaly does each prevent?","answerFull":"1NF: atomic column values; prevents repeating groups and multi-valued cells. 2NF (requires composite PK): every non-key column depends on the full primary key; prevents partial dependencies (where a column depends on only part of a composite key). 3NF: no transitive dependencies (no non-key column determines another non-key column); prevents update anomalies where changing one derived fact requires updating multiple rows.","correct":"1NF: atomic column values; prevents repeating groups and multi-valued cells.","distractors":["MVCC (Multi-Version Concurrency Control) keeps multiple versions of each row, one per committed transaction.","2PC is a distributed transaction protocol.","A clustered index (InnoDB: the primary key index) stores the actual row data in the leaf nodes; the table IS the index."]},{"id":"cs_fundamentals/database_and_storage_fundamentals#ee47e1750f2f","section":"cs_fundamentals","module":"cs_fundamentals/database_and_storage_fundamentals","moduleName":"database and storage fundamentals","qIndex":8,"difficulty":"intermediate","question":"What is the Write-Ahead Log (WAL) and how does it provide durability?","answerFull":"The WAL is a sequential append-only log on disk that records the intent of every database change before the change is applied to the data file. On commit, the WAL record is fsynced to disk; only then is the acknowledgement returned to the client. If the server crashes, recovery replays WAL records from the last checkpoint to reconstruct all committed changes and roll back uncommitted ones. Sequential WAL writes (~0.1–1 ms on SSD) are far faster than random heap file writes (~100–500 µs per page on NVMe), so WAL improves both durability and throughput.","correct":"The WAL is a sequential append-only log on disk that records the intent of every database change before the change is applied to the data file.","distractors":["With fanout = 100 (PostgreSQL default 8 KB page), height = ceil(log_100(10,000,000)) = ceil(3.5) = 4.","A lost update occurs when two transactions read the same value, each modifies it in application code, and the second COMMIT overwrites the first's change.","fsync forces the OS to write all dirty pages in the file's page cache to durable storage, bypassing the OS write cache."]},{"id":"cs_fundamentals/database_and_storage_fundamentals#20ff6a329f12","section":"cs_fundamentals","module":"cs_fundamentals/database_and_storage_fundamentals","moduleName":"database and storage fundamentals","qIndex":9,"difficulty":"intermediate","question":"What is two-phase commit (2PC) and what is its main weakness?","answerFull":"2PC is a distributed transaction protocol. Phase 1 (Prepare): coordinator asks all participants if they can commit; each participant locks its resources and writes a prepare record to its WAL. Phase 2 (Commit): if all reply YES, coordinator writes a commit record and tells all to commit. The main weakness is that 2PC is a blocking protocol: if the coordinator crashes after sending Prepare but before sending Commit, participants hold locks indefinitely waiting for the coordinator to recover. This can cause prolonged blocking in distributed systems, which is why SAGA patterns and idempotent compensation are preferred for long-running distributed transactions.","correct":"2PC is a distributed transaction protocol.","distractors":["MVCC (Multi-Version Concurrency Control) keeps multiple versions of each row, one per committed transaction.","A lost update occurs when two transactions read the same value, each modifies it in application code, and the second COMMIT overwrites the first's change.","A dirty read occurs when Transaction A reads data written by Transaction B that has not yet committed."]},{"id":"cs_fundamentals/database_and_storage_fundamentals#8da6e4c866bb","section":"cs_fundamentals","module":"cs_fundamentals/database_and_storage_fundamentals","moduleName":"database and storage fundamentals","qIndex":10,"difficulty":"intermediate","question":"Why does PostgreSQL default to Read Committed rather than Serializable?","answerFull":"Serializable isolation (via Serializable Snapshot Isolation in PostgreSQL) requires tracking read sets and detecting conflicts, which causes transactions to abort and retry under contention. For most OLTP workloads, Read Committed provides enough safety with significantly higher throughput because readers never block writers (MVCC), and write-write conflicts still abort one transaction. Serializable is recommended for financial ledgers, inventory allocation, or any use case where phantom reads would cause correctness violations.","correct":"Serializable isolation (via Serializable Snapshot Isolation in PostgreSQL) requires tracking read sets and detecting conflicts, which causes transactions to abort and retry under contention.","distractors":["A non-repeatable read affects a single existing row: T1 reads row R, T2 updates and commits R, T1 reads R again and sees a different value.","MVCC (Multi-Version Concurrency Control) keeps multiple versions of each row, one per committed transaction.","The buffer pool (PostgreSQL: shared_buffers; InnoDB: innodb_buffer_pool_size) is the in-memory cache of database pages."]},{"id":"cs_fundamentals/database_and_storage_fundamentals#d7d3862a7904","section":"cs_fundamentals","module":"cs_fundamentals/database_and_storage_fundamentals","moduleName":"database and storage fundamentals","qIndex":11,"difficulty":"advanced","question":"What is the difference between a clustered index and a secondary index?","answerFull":"A clustered index (InnoDB: the primary key index) stores the actual row data in the leaf nodes; the table IS the index. There can be only one clustered index per table. A secondary index stores (secondary key, primary key) in the leaf nodes; to fetch the full row, InnoDB performs a second lookup in the clustered index (bookmark lookup). PostgreSQL uses heap tables for all data and the primary key index is just another index; its \"index-only scan\" avoids the second lookup when all needed columns are in the index. See ../../database/indexing_deep_dive.","correct":"A clustered index (InnoDB: the primary key index) stores the actual row data in the leaf nodes; the table IS the index.","distractors":["A non-repeatable read affects a single existing row: T1 reads row R, T2 updates and commits R, T1 reads R again and sees a different value.","The buffer pool (PostgreSQL: shared_buffers; InnoDB: innodb_buffer_pool_size) is the in-memory cache of database pages.","MVCC (Multi-Version Concurrency Control) keeps multiple versions of each row, one per committed transaction."]},{"id":"cs_fundamentals/database_and_storage_fundamentals#449a3443283c","section":"cs_fundamentals","module":"cs_fundamentals/database_and_storage_fundamentals","moduleName":"database and storage fundamentals","qIndex":12,"difficulty":"advanced","question":"How do you detect and resolve a deadlock in a relational database?","answerFull":"A deadlock occurs when T1 holds lock A and waits for lock B, while T2 holds lock B and waits for lock A — a cycle. Databases run a background deadlock detector (PostgreSQL: cycle detection in the lock graph every deadlock_timeout ms, default 1 s). When a cycle is detected, the database aborts the transaction with the lowest cost (fewest locks held, least work done). The application must catch the deadlock error (PostgreSQL: SQLSTATE 40P01) and retry the transaction. Prevention strategies: always acquire locks in a consistent global order; keep transactions short; use SELECT FOR UPDATE SKIP LOCKED for queue patterns.","correct":"A deadlock occurs when T1 holds lock A and waits for lock B, while T2 holds lock B and waits for lock A — a cycle.","distractors":["The WAL is a sequential append-only log on disk that records the intent of every database change before the change is applied to the data file.","A lost update occurs when two transactions read the same value, each modifies it in application code, and the second COMMIT overwrites the first's change.","The buffer pool (PostgreSQL: shared_buffers; InnoDB: innodb_buffer_pool_size) is the in-memory cache of database pages."]},{"id":"cs_fundamentals/database_and_storage_fundamentals#0b762b711602","section":"cs_fundamentals","module":"cs_fundamentals/database_and_storage_fundamentals","moduleName":"database and storage fundamentals","qIndex":13,"difficulty":"advanced","question":"What is the buffer pool and why is its hit ratio the most important database performance metric?","answerFull":"The buffer pool (PostgreSQL: shared_buffers; InnoDB: innodb_buffer_pool_size) is the in-memory cache of database pages. A cache hit serves a page from RAM (~100 ns); a cache miss fetches from SSD (~100 µs) — a 1000× difference. A 95% hit rate with 4-level B+Tree lookup results in ~0.2 µs expected I/O latency vs. ~400 µs all-miss. PostgreSQL recommends shared_buffers = 25–40% of RAM; InnoDB buffer pool = 70–80% of RAM. Monitor with pg_stat_bgwriter (PostgreSQL) or SHOW ENGINE INNODB STATUS (MySQL).","correct":"The buffer pool (PostgreSQL: shared_buffers; InnoDB: innodb_buffer_pool_size) is the in-memory cache of database pages.","distractors":["The WAL is a sequential append-only log on disk that records the intent of every database change before the change is applied to the data file.","A clustered index (InnoDB: the primary key index) stores the actual row data in the leaf nodes; the table IS the index.","With fanout = 100 (PostgreSQL default 8 KB page), height = ceil(log_100(10,000,000)) = ceil(3.5) = 4."]},{"id":"cs_fundamentals/database_and_storage_fundamentals#59d8a133a8da","section":"cs_fundamentals","module":"cs_fundamentals/database_and_storage_fundamentals","moduleName":"database and storage fundamentals","qIndex":14,"difficulty":"advanced","question":"What happens to uncommitted data during a crash, and how does the database ensure correctness?","answerFull":"The WAL records both committed and uncommitted changes. On crash recovery, the database replays all WAL records from the last checkpoint (redo pass) to reconstruct the state up to the crash point. It then scans for transactions that had not committed at the time of crash and rolls them back using the undo log (or MVCC undo version chain). This two-pass approach (redo + undo) guarantees that committed transactions are fully applied and uncommitted transactions are fully removed.","correct":"The WAL records both committed and uncommitted changes.","distractors":["MVCC (Multi-Version Concurrency Control) keeps multiple versions of each row, one per committed transaction.","A dirty read occurs when Transaction A reads data written by Transaction B that has not yet committed.","The buffer pool (PostgreSQL: shared_buffers; InnoDB: innodb_buffer_pool_size) is the in-memory cache of database pages."]},{"id":"cs_fundamentals/database_and_storage_fundamentals#4126655125b6","section":"cs_fundamentals","module":"cs_fundamentals/database_and_storage_fundamentals","moduleName":"database and storage fundamentals","qIndex":15,"difficulty":"advanced","question":"What is a phantom read in the context of an inventory system, and how do you prevent it?","answerFull":"In a ticket booking system, T1 queries SELECT COUNT(*) FROM tickets WHERE status='available' and gets 1. T2 runs the same query, also gets 1, and both decide to book. T1 inserts a booking and updates the ticket status. T2 also inserts a booking — the event is now double-booked. This is a phantom insert scenario. Prevention: (1) Serializable isolation detects the conflict and aborts T2; (2) SELECT ... FOR UPDATE on the ticket row makes T2 block until T1 commits; (3) a UNIQUE constraint or CHECK on available count turns the second insert into a constraint violation.","correct":"In a ticket booking system, T1 queries SELECT COUNT(*) FROM tickets WHERE status='available' and gets 1.","distractors":["A dirty read occurs when Transaction A reads data written by Transaction B that has not yet committed.","A lost update occurs when two transactions read the same value, each modifies it in application code, and the second COMMIT overwrites the first's change.","Serializable isolation (via Serializable Snapshot Isolation in PostgreSQL) requires tracking read sets and detecting conflicts, which causes transactions to abort and retry under contention."]},{"id":"cs_fundamentals/database_and_storage_fundamentals#4d5cbf2cd47b","section":"cs_fundamentals","module":"cs_fundamentals/database_and_storage_fundamentals","moduleName":"database and storage fundamentals","qIndex":16,"difficulty":"advanced","question":"What is the cost of an fsync, and why does it matter for database commit latency?","answerFull":"fsync forces the OS to write all dirty pages in the file's page cache to durable storage, bypassing the OS write cache. On NVMe SSD: ~0.5–1 ms. On SATA SSD: ~1–5 ms. On HDD: ~5–20 ms. PostgreSQL calls fsync on the WAL before returning COMMIT to the client. With synchronous_commit = on (default), commit latency is bounded by fsync latency. With synchronous_commit = off, commits are acknowledged before fsync, reducing latency to ~0.1 ms but risking loss of the last few hundred milliseconds of committed data on crash.","correct":"fsync forces the OS to write all dirty pages in the file's page cache to durable storage, bypassing the OS write cache.","distractors":["With fanout = 100 (PostgreSQL default 8 KB page), height = ceil(log_100(10,000,000)) = ceil(3.5) = 4.","The WAL is a sequential append-only log on disk that records the intent of every database change before the change is applied to the data file.","A dirty read occurs when Transaction A reads data written by Transaction B that has not yet committed."]},{"id":"cs_fundamentals/deadlocks_and_synchronization#4906839a0076","section":"cs_fundamentals","module":"cs_fundamentals/deadlocks_and_synchronization","moduleName":"deadlocks and synchronization","qIndex":1,"difficulty":"core","question":"What are the four Coffman conditions for deadlock, and why must all four hold?","answerFull":"(1) Mutual exclusion: at least one resource is held exclusively. (2) Hold and wait: a thread holding resources waits for more. (3) No preemption: resources cannot be forcibly taken. (4) Circular wait: a cycle of threads each waiting for the next thread's resource. All four must hold because deadlock requires that every thread in the cycle is blocked (mutual exclusion prevents sharing), unable to proceed (hold and wait), cannot be forced to release (no preemption), and is in a dependency cycle (circular wait). Removing any one breaks the cycle: e.g., breaking circular wait (lock ordering) prevents deadlock even if the other three hold.","correct":"(1) Mutual exclusion: at least one resource is held exclusively.","distractors":["Deadlock: threads are permanently blocked, making no progress.","Prevention: structurally ensure at least one Coffman condition cannot hold (e.g., lock ordering eliminates circular wait).","The Banker's algorithm determines if a resource allocation request can be safely granted."]},{"id":"cs_fundamentals/deadlocks_and_synchronization#c8a2c46263b7","section":"cs_fundamentals","module":"cs_fundamentals/deadlocks_and_synchronization","moduleName":"deadlocks and synchronization","qIndex":2,"difficulty":"core","question":"How does lock ordering prevent deadlock and what are its limitations?","answerFull":"Define a total order over all lock objects (by ID, type, or a programmer-assigned sequence number). Every thread must acquire locks only in increasing order. This breaks circular wait: if thread 1 holds lock A (order 1) and waits for lock B (order 2), and thread 2 must acquire lock A before lock B, then thread 2 cannot hold B and wait for A — it would need to acquire A (lower order) while holding B (higher order), which violates the rule. Limitation: requires all code paths to be aware of the ordering, which is brittle in large codebases; dynamic lock sets (variable number of locks per operation) make ordering difficult.","correct":"Define a total order over all lock objects (by ID, type, or a programmer-assigned sequence number).","distractors":["Pessimistic locking: acquire a lock before accessing the resource; no other thread can access while the lock is held.","Prevention: structurally ensure at least one Coffman condition cannot hold (e.g., lock ordering eliminates circular wait).","Deadlock: threads are permanently blocked, making no progress."]},{"id":"cs_fundamentals/deadlocks_and_synchronization#82902bf2249d","section":"cs_fundamentals","module":"cs_fundamentals/deadlocks_and_synchronization","moduleName":"deadlocks and synchronization","qIndex":3,"difficulty":"core","question":"What is the difference between deadlock prevention, avoidance, and detection/recovery?","answerFull":"Prevention: structurally ensure at least one Coffman condition cannot hold (e.g., lock ordering eliminates circular wait). No runtime overhead; may be overly restrictive. Avoidance: before granting a resource, check if the resulting state is \"safe\" (Banker's algorithm). Prevents deadlock dynamically but requires knowing maximum resource needs upfront — impractical for general-purpose systems. Detection + recovery: allow deadlock to occur; periodically run a cycle-detection algorithm on the wait-for graph; recover by aborting one or more processes (database: rollback transaction). Used by DBMS (PostgreSQL, MySQL) which periodically scan for lock cycles.","correct":"Prevention: structurally ensure at least one Coffman condition cannot hold (e.g., lock ordering eliminates circular wait).","distractors":["A reentrant lock can be acquired multiple times by the same thread without deadlocking.","A monitor is a higher-level synchronization construct that combines a mutex with one or more condition variables.","A spurious wakeup is when a thread blocked in cond.wait() is woken without a corresponding cond.signal() or cond.broadcast()."]},{"id":"cs_fundamentals/deadlocks_and_synchronization#bcb13d17641f","section":"cs_fundamentals","module":"cs_fundamentals/deadlocks_and_synchronization","moduleName":"deadlocks and synchronization","qIndex":4,"difficulty":"core","question":"Explain the Banker's algorithm.","answerFull":"The Banker's algorithm determines if a resource allocation request can be safely granted. \"Safe state\": there exists a sequence in which all processes can eventually finish using the current available resources + resources released by completing processes. Algorithm: (1) Assume the request is granted (update Allocation, Available). (2) Check if the resulting state is safe by finding a process whose remaining needs can be met by available resources; simulate it finishing and releasing; repeat. (3) If a safe sequence exists, grant the request; otherwise, deny and make the thread wait. Limitation: requires processes to declare max resource needs in advance — rarely practical outside real-time embedded systems.","correct":"The Banker's algorithm determines if a resource allocation request can be safely granted.","distractors":["(1) Mutual exclusion: at least one resource is held exclusively.","Pessimistic locking: acquire a lock before accessing the resource; no other thread can access while the lock is held.","Databases detect deadlocks by maintaining a wait-for graph updated on every lock request."]},{"id":"cs_fundamentals/deadlocks_and_synchronization#a39b3cd81ee5","section":"cs_fundamentals","module":"cs_fundamentals/deadlocks_and_synchronization","moduleName":"deadlocks and synchronization","qIndex":5,"difficulty":"core","question":"What are spurious wakeups and why must condition variable waits be inside a while loop?","answerFull":"A spurious wakeup is when a thread blocked in cond.wait() is woken without a corresponding cond.signal() or cond.broadcast(). POSIX explicitly allows them (to simplify implementation on some architectures). If the check is an if, the thread proceeds even though the condition may not be true, leading to bugs (consuming from an empty buffer, incorrect shared state). The pattern while (!condition) { cond.wait(); } re-checks after every wakeup (spurious or legitimate) — the thread only proceeds when the condition is actually true.","correct":"A spurious wakeup is when a thread blocked in cond.wait() is woken without a corresponding cond.signal() or cond.broadcast().","distractors":["Databases detect deadlocks by maintaining a wait-for graph updated on every lock request.","Deadlock: threads are permanently blocked, making no progress.","A monitor is a higher-level synchronization construct that combines a mutex with one or more condition variables."]},{"id":"cs_fundamentals/deadlocks_and_synchronization#0001fd91348a","section":"cs_fundamentals","module":"cs_fundamentals/deadlocks_and_synchronization","moduleName":"deadlocks and synchronization","qIndex":6,"difficulty":"intermediate","question":"Explain the dining philosophers problem and two deadlock-free solutions.","answerFull":"Five philosophers sit at a round table with five forks. Each needs both adjacent forks to eat. (1) Naive: each picks up left then right → all pick up left → all wait for right → circular wait → deadlock. Fix 1: Lock ordering — philosopher N picks up min(left, right) first. The last philosopher (N=4) now picks up fork 0 before fork 4, breaking the circular wait. Fix 2: Limit concurrency — use a semaphore(4) to allow at most 4 philosophers to try eating simultaneously. At most 4 can hold one fork; the 5th fork is always free → at least one can eat.","correct":"Five philosophers sit at a round table with five forks.","distractors":["Deadlock: threads are permanently blocked, making no progress.","(1) Mutual exclusion: at least one resource is held exclusively.","A monitor is a higher-level synchronization construct that combines a mutex with one or more condition variables."]},{"id":"cs_fundamentals/deadlocks_and_synchronization#fa9d50ffac9d","section":"cs_fundamentals","module":"cs_fundamentals/deadlocks_and_synchronization","moduleName":"deadlocks and synchronization","qIndex":7,"difficulty":"intermediate","question":"What is the difference between a mutex and a semaphore?","answerFull":"A mutex is a binary lock with ownership: only the thread that acquired it can release it. A semaphore is a counting integer: any thread can call signal() (release), not just the one that called wait() (acquire). Use mutex for mutual exclusion (critical sections — ownership matters). Use semaphore for signalling (producer-consumer: producer signals, consumer waits — different threads) and resource counting (connection pool of size k: semaphore(k)). Mutex with value 1 ≠ binary semaphore: the ownership rule is different. In Java: ReentrantLock for mutex, java.util.concurrent.Semaphore for counting.","correct":"A mutex is a binary lock with ownership: only the thread that acquired it can release it.","distractors":["Databases detect deadlocks by maintaining a wait-for graph updated on every lock request.","Define a total order over all lock objects (by ID, type, or a programmer-assigned sequence number).","Pessimistic locking: acquire a lock before accessing the resource; no other thread can access while the lock is held."]},{"id":"cs_fundamentals/deadlocks_and_synchronization#4784721a0f08","section":"cs_fundamentals","module":"cs_fundamentals/deadlocks_and_synchronization","moduleName":"deadlocks and synchronization","qIndex":8,"difficulty":"intermediate","question":"What is a monitor and how does it differ from a raw mutex?","answerFull":"A monitor is a higher-level synchronization construct that combines a mutex with one or more condition variables. The mutex ensures at-most-one-thread-inside-the-monitor. Condition variables allow a thread inside the monitor to wait for a condition to become true while releasing the monitor lock — other threads can enter. Java's synchronized + Object.wait()/notify() implements the monitor pattern. The difference from raw mutex: a monitor integrates the \"wait for condition\" mechanism, whereas a raw mutex only provides mutual exclusion; you'd need to add a condition variable manually.","correct":"A monitor is a higher-level synchronization construct that combines a mutex with one or more condition variables.","distractors":["A mutex is a binary lock with ownership: only the thread that acquired it can release it.","Prevention: structurally ensure at least one Coffman condition cannot hold (e.g., lock ordering eliminates circular wait).","(1) Mutual exclusion: at least one resource is held exclusively."]},{"id":"cs_fundamentals/deadlocks_and_synchronization#003dd4886bb2","section":"cs_fundamentals","module":"cs_fundamentals/deadlocks_and_synchronization","moduleName":"deadlocks and synchronization","qIndex":10,"difficulty":"intermediate","question":"How do databases handle deadlocks?","answerFull":"Databases detect deadlocks by maintaining a wait-for graph updated on every lock request. PostgreSQL runs a background deadlock detector every deadlock_timeout (default 1 second). When a cycle is found, PostgreSQL selects the \"cheapest\" transaction to abort (fewest locks held, or youngest transaction) and rolls it back with an error code (ERROR 40P01: deadlock detected). The application must catch this error and retry the transaction. InnoDB (MySQL) runs detection on every lock wait (not just periodically) — faster detection but higher overhead. See database/concurrency_control_and_locking.","correct":"Databases detect deadlocks by maintaining a wait-for graph updated on every lock request.","distractors":["A reentrant lock can be acquired multiple times by the same thread without deadlocking.","A mutex is a binary lock with ownership: only the thread that acquired it can release it.","The Banker's algorithm determines if a resource allocation request can be safely granted."]},{"id":"cs_fundamentals/deadlocks_and_synchronization#26ea280462d0","section":"cs_fundamentals","module":"cs_fundamentals/deadlocks_and_synchronization","moduleName":"deadlocks and synchronization","qIndex":11,"difficulty":"advanced","question":"What is a reentrant (recursive) lock and when do you need one?","answerFull":"A reentrant lock can be acquired multiple times by the same thread without deadlocking. It maintains an acquisition count; the lock is released only when the count reaches 0. Needed when: a method acquires a lock and calls another method that also acquires the same lock (e.g., a recursive method that synchronizes on this, or utility methods called from within a lock). In Java: ReentrantLock and synchronized (intrinsic lock) are both reentrant. Non-reentrant mutex re-acquisition by the same thread deadlocks immediately. Caution: reentrant locks make it easier to accidentally hold locks for too long across nested calls.","correct":"A reentrant lock can be acquired multiple times by the same thread without deadlocking.","distractors":["Pessimistic locking: acquire a lock before accessing the resource; no other thread can access while the lock is held.","Prevention: structurally ensure at least one Coffman condition cannot hold (e.g., lock ordering eliminates circular wait).","A spurious wakeup is when a thread blocked in cond.wait() is woken without a corresponding cond.signal() or cond.broadcast()."]},{"id":"cs_fundamentals/deadlocks_and_synchronization#e166def41838","section":"cs_fundamentals","module":"cs_fundamentals/deadlocks_and_synchronization","moduleName":"deadlocks and synchronization","qIndex":12,"difficulty":"advanced","question":"What is a livelock, and how is it different from deadlock?","answerFull":"Deadlock: threads are permanently blocked, making no progress. Livelock: threads are running and consuming CPU but making no real progress — they keep reacting to each other in a cycle. Example: two threads each release their lock when they see the other is also trying to acquire both locks, then both try again simultaneously, releasing again, forever. Livelock is harder to detect than deadlock (threads appear active in profilers). Prevention: introduce randomised backoff (each thread waits a random time before retrying), or use a priority order to break symmetry.","correct":"Deadlock: threads are permanently blocked, making no progress.","distractors":["A spurious wakeup is when a thread blocked in cond.wait() is woken without a corresponding cond.signal() or cond.broadcast().","Python's GIL prevents true parallelism for CPU-bound code in threads, but I/O-bound threads can interleave.","(1) Mutual exclusion: at least one resource is held exclusively."]},{"id":"cs_fundamentals/deadlocks_and_synchronization#2b8514750f66","section":"cs_fundamentals","module":"cs_fundamentals/deadlocks_and_synchronization","moduleName":"deadlocks and synchronization","qIndex":13,"difficulty":"advanced","question":"How does the ABA problem affect lock-free CAS-based algorithms?","answerFull":"Compare-And-Swap (CAS) checks if a value equals \"expected\" before swapping. ABA problem: a value changes A→B→A between a CAS read and write. The CAS sees A (same as expected) and succeeds, but the underlying state has changed and been changed back — the algorithm may be incorrect. Example: a lock-free stack pop: read head→A, CPU is preempted; another thread pops A, pushes B, pops B, pushes A back; current thread's CAS(A, new) succeeds incorrectly. Solutions: ABA counter (tag with version number; CAS(A:version, new:version+1)); hazard pointers (defer reclamation until no thread holds a pointer to the node); epoch-based reclamation.","correct":"Compare-And-Swap (CAS) checks if a value equals \"expected\" before swapping.","distractors":["Databases detect deadlocks by maintaining a wait-for graph updated on every lock request.","A reentrant lock can be acquired multiple times by the same thread without deadlocking.","Pessimistic locking: acquire a lock before accessing the resource; no other thread can access while the lock is held."]},{"id":"cs_fundamentals/deadlocks_and_synchronization#8a0d6addb10b","section":"cs_fundamentals","module":"cs_fundamentals/deadlocks_and_synchronization","moduleName":"deadlocks and synchronization","qIndex":14,"difficulty":"advanced","question":"What is the difference between optimistic and pessimistic locking?","answerFull":"Pessimistic locking: acquire a lock before accessing the resource; no other thread can access while the lock is held. Guaranteed no conflicts; may cause contention. Example: synchronized, ReentrantLock. Optimistic locking: read without a lock; check if the value changed before writing (CAS or version number). If changed, retry. Efficient when contention is low; degrades under high contention (many retries). Example: Java's AtomicInteger.compareAndSet(), database UPDATE ... WHERE version = ?. Databases use both: MVCC (optimistic reads) + row-level locks (pessimistic writes).","correct":"Pessimistic locking: acquire a lock before accessing the resource; no other thread can access while the lock is held.","distractors":["The Banker's algorithm determines if a resource allocation request can be safely granted.","A mutex is a binary lock with ownership: only the thread that acquired it can release it.","Prevention: structurally ensure at least one Coffman condition cannot hold (e.g., lock ordering eliminates circular wait)."]},{"id":"cs_fundamentals/deadlocks_and_synchronization#d4e93b855bd0","section":"cs_fundamentals","module":"cs_fundamentals/deadlocks_and_synchronization","moduleName":"deadlocks and synchronization","qIndex":15,"difficulty":"advanced","question":"How do you design a lock-free counter in Python?","answerFull":"Python's GIL prevents true parallelism for CPU-bound code in threads, but I/O-bound threads can interleave. For a truly thread-safe counter: (1) Use threading.Lock with a plain integer — correct, simple. (2) Use Python's ctypes with C atomic operations — lock-free, but complex. (3) Use concurrent.futures with each worker updating only local state; aggregate at the end. For async contexts: use asyncio.Lock (not threading.Lock). In Java: AtomicInteger.incrementAndGet() uses a CAS loop — lock-free, linearisable, and efficiently maps to a single CPU instruction (LOCK XADD on x86).","correct":"Python's GIL prevents true parallelism for CPU-bound code in threads, but I/O-bound threads can interleave.","distractors":["Pessimistic locking: acquire a lock before accessing the resource; no other thread can access while the lock is held.","Define a total order over all lock objects (by ID, type, or a programmer-assigned sequence number).","A reentrant lock can be acquired multiple times by the same thread without deadlocking."]},{"id":"cs_fundamentals/dsa_patterns#b5e2327671a0","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"What's the precise difference between \"backtracking\" and \"plain recursive DFS\"?","answerFull":"Plain DFS explores a tree and returns; backtracking explores a tree while mutating shared state (a path, a board, a set of used elements) and explicitly restores that state before trying the next sibling branch. The \"restore\" (undo) step is the defining characteristic — without it, sibling branches would see corrupted state left over from previously-explored branches (exactly the bug in §8).","correct":"Plain DFS explores a tree and returns; backtracking explores a tree while mutating shared state (a path, a board, a set of used elements) and explicitly restores that state before trying the next sibling branch.","distractors":["The recursive calls share a *single* mutable path (or used array) by reference — there's only one list object, repeatedly appended to and read from at different points in the recursion.","A tree has no cycles and exactly one path from the root to any node, so a recursive/BFS walk can never revisit a node.","For *counting* connected components (islands) or *generating a path* (backtracking-style), DFS is simpler and uses less code."]},{"id":"cs_fundamentals/dsa_patterns#cab9255eced0","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"Why is backtracking exponential, and why is that considered \"fine\" for these problems?","answerFull":"Each recursive call branches into multiple choices, and the recursion depth equals the solution length — so the total number of leaves in the search tree is O(branching_factor ^ depth), which is 2^n (subsets) or n! (permutations). This is \"fine\" because problems that call for backtracking explicitly constrain n to be small (typically <= 12-20) — 2^20 ~ 10^6 is fast, but 2^50 would not be. The small constraint *is the signal* that exponential is the intended complexity class.","correct":"Each recursive call branches into multiple choices, and the recursion depth equals the solution length — so the total number of leaves in the search tree is O(branching_factor ^ depth), which is 2^n (subsets) or n!","distractors":["Worst case (all wildcards, e.g., \"......\"), it genuinely is exponential — O(26^L) — because every position branches into all 26 children.","Top-down (recursion + @lru_cache or a dict) is often easier to *write* directly from the recurrence — it mirrors the brute-force solution with one line added.","Plain DFS explores a tree and returns; backtracking explores a tree while mutating shared state (a path, a board, a set of used elements) and explicitly restores that state before trying the next sibling branch."]},{"id":"cs_fundamentals/dsa_patterns#c9f5dcf11a5d","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"Walk through why path.pop() (or used[i] = False) is necessary — what breaks without it?","answerFull":"The recursive calls share a *single* mutable path (or used array) by reference — there's only one list object, repeatedly appended to and read from at different points in the recursion. Without popping, every \"exclude\" or \"try the next choice\" branch starts from whatever state the *previous* branch left behind, rather than the state that branch's parent had. See §8 for a full traced example showing exactly how this corrupts results.","correct":"The recursive calls share a *single* mutable path (or used array) by reference — there's only one list object, repeatedly appended to and read from at different points in the recursion.","distractors":["Walk to the node representing the word's last character and set is_end = False.","The recursive construction repeatedly needs \"where does preorder[0] (the current root) appear in the current inorder slice?\" to split it into left/right subtree slices.","Each recursive call branches into multiple choices, and the recursion depth equals the solution length — so the total number of leaves in the search tree is O(branching_factor ^ depth), which is 2^n (subsets) or n!"]},{"id":"cs_fundamentals/dsa_patterns#3a9a760e02a0","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":4,"difficulty":"core","question":"Subsets II — how do you avoid generating the same subset twice when the input has duplicates, e.g., nums = [1, 2, 2]?","answerFull":"Sort nums first so duplicates are adjacent. In the for i in range(start, len(nums)) loop, add if i > start and nums[i] == nums[i-1]: continue. This specifically skips choosing the *same value* as a sibling choice at the same recursion level (same start) — it does NOT prevent using duplicates at *different* levels (i.e., [2, 2] as a subset is still generated, just only once).","correct":"Sort nums first so duplicates are adjacent.","distractors":["\"132 Pattern\" asks: does there exist i < j < k with nums[i] < nums[k] < nums[j]?","Each recursive call branches into multiple choices, and the recursion depth equals the solution length — so the total number of leaves in the search tree is O(branching_factor ^ depth), which is 2^n (subsets) or n!","Same structure as prefix sum, but using XOR (prefix[i] = nums[0] XOR nums[1] XOR ..."]},{"id":"cs_fundamentals/dsa_patterns#c6acd20e4582","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"Permutations: used[] array vs. swapping elements in place — what's the tradeoff?","answerFull":"The used[] array approach (Template 2) is more intuitive and naturally supports the duplicate-skipping check for Permutations II. The swap-based approach (for i in range(k, n): swap(nums, k, i); backtrack(k+1); swap(nums, k, i) to undo) avoids the extra O(n) used array and an O(n) path list — the permutation IS nums itself at each leaf — but makes duplicate-handling trickier (requires a set() per recursion level to skip repeated swap values).","correct":"The used[] array approach (Template 2) is more intuitive and naturally supports the duplicate-skipping check for Permutations II.","distractors":["Opposite ends (left=0, right=n-1, converging) is for finding a *relationship between two elements* (pair sum, palindrome check, container area) — the search space shrinks from both sides.","Without the sets, validating \"can I place a queen at (row, col)?\" requires scanning all previously-placed queens — O(row) per check, O(n) per row, O(n^2) total per leaf, O(n^2 * n!) overall.","With the standard template (while fast and fast.next), for a list of length 2m, slow ends at the (m+1)-th node — the *second* of the two middle nodes."]},{"id":"cs_fundamentals/dsa_patterns#0ce82b40d559","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"How does N-Queens' constraint propagation (cols, diag1, diag2 sets) make the search dramatically faster than checking the whole board at each step?","answerFull":"Without the sets, validating \"can I place a queen at (row, col)?\" requires scanning all previously-placed queens — O(row) per check, O(n) per row, O(n^2) total per leaf, O(n^2 * n!) overall. With the sets, each check is O(1) (set membership), AND — more importantly — invalid branches are pruned immediately rather than being fully built and then rejected. The row - col and row + col invariants are the standard trick for identifying \"/\" and \"\\\" diagonals in O(1).","correct":"Without the sets, validating \"can I place a queen at (row, col)?\" requires scanning all previously-placed queens — O(row) per check, O(n) per row, O(n^2) total per leaf, O(n^2 * n!) overall.","distractors":["Each insertion requires finding the correct position (binary search on starts, O(log n)) and checking for overlap with neighbors (O(1)), but inserting into a Python list is O(n) due to shifting elements.","Because the core operation is \"place value v at index f(v)\" (where f(v) = v-1 or f(v) = v), and this requires f(v) to be a valid index into the array — i.e., 0 <= f(v) < n.","Dijkstra is O((V+E) log V) — much faster for large graphs, but requires non-negative weights."]},{"id":"cs_fundamentals/dsa_patterns#6c56adbbb2ec","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":8,"difficulty":"intermediate","question":"For Combination Sum, why does the recursive call use backtrack(i, ...) (same index) instead of backtrack(i + 1, ...)?","answerFull":"backtrack(i, ...) allows candidates[i] to be chosen again in the next recursive level — modeling \"unlimited supply\" of each candidate (e.g., [2,2,3] is valid for Combination Sum if 2+2+3 sums to target). Combined with for i in range(start, ...) (not re-trying indices < start), this still avoids generating [2,3,2] as a *different* combination from [2,2,3] — order within a combination doesn't matter, only the multiset of values chosen.","correct":"backtrack(i, ...) allows candidates[i] to be chosen again in the next recursive level — modeling \"unlimited supply\" of each candidate (e.g., [2,2,3] is valid for Combination Sum if 2+2+3 sums to target).","distractors":["The window is valid if you can convert it to all-the-same-character using at most k replacements.","Same structure as prefix sum, but using XOR (prefix[i] = nums[0] XOR nums[1] XOR ...","Because the core operation is \"place value v at index f(v)\" (where f(v) = v-1 or f(v) = v), and this requires f(v) to be a valid index into the array — i.e., 0 <= f(v) < n."]},{"id":"cs_fundamentals/dsa_patterns#a5bdcd2a729c","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":9,"difficulty":"intermediate","question":"Word Search backtracking vs. graph_traversal's flood fill — both mark and unmark grid cells. What's actually different?","answerFull":"Mechanically, almost nothing — both mark board[r][c] before recursing into neighbors and restore it after. The difference is purpose: flood fill (graph_traversal) marks cells *permanently* within one connected-component pass (no restore needed across components) to avoid re-counting. Word Search restores cells because the *same* cell might need to be available again for a *different* candidate path — e.g., if the first DFS path from a starting cell fails to spell the word, a different direction from the same start must see the original board.","correct":"Mechanically, almost nothing — both mark board[r][c] before recursing into neighbors and restore it after.","distractors":["All cells on the same anti-diagonal share a constant r + c (and main diagonals share constant r - c).","board[r][c] = \"#\" is used as the \"currently on this DFS path\" marker — it must be scoped to the current call stack, not permanent.","Each insertion requires finding the correct position (binary search on starts, O(log n)) and checking for overlap with neighbors (O(1)), but inserting into a Python list is O(n) due to shifting elements."]},{"id":"cs_fundamentals/dsa_patterns#e34cb6bdcc26","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":10,"difficulty":"intermediate","question":"Why must result.append(path[:]) use a copy (path[:] or list(path)) instead of result.append(path)?","answerFull":"path is a single mutable list object, repeatedly appended to and popped from throughout the entire recursion. result.append(path) would store a reference to that same object in every position of result — by the time backtracking finishes (and path has been popped back to []), every entry in result would appear as [], since they all point to the same (now-empty) list. path[:] creates an independent snapshot at that moment.","correct":"path is a single mutable list object, repeatedly appended to and popped from throughout the entire recursion.","distractors":["The function's *return value* must be something the parent can use to compute *its own* combination — here, the subtree's height (a single number representing \"how far down does this subtree extend\").","The invariant requires that extending a path can never *help* relative to stopping early — i.e., the \"combine\" operation must be monotonically non-improving.","The recursive calls share a *single* mutable path (or used array) by reference — there's only one list object, repeatedly appended to and read from at different points in the recursion."]},{"id":"cs_fundamentals/dsa_patterns#3651e2139646","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":11,"difficulty":"advanced","question":"For very small n (say n <= 20), when would an iterative bitmask approach beat recursive backtracking for generating subsets?","answerFull":"When you don't need subsets in any particular order and want to avoid Python recursion overhead (function call costs add up across 2^n calls). for mask in range(2n): subset = [nums[i] for i in range(n) if mask & (1 << i)] iterates all 2^n subsets with simple loops and bit tests — often faster in practice for n up to ~20-22, and trivially parallelizable since each mask is independent. See bit_manipulation.md for the bitmask mechanics.","correct":"When you don't need subsets in any particular order and want to avoid Python recursion overhead (function call costs add up across 2^n calls).","distractors":["Each recursive call branches into multiple choices, and the recursion depth equals the solution length — so the total number of leaves in the search tree is O(branching_factor ^ depth), which is 2^n (subsets) or n!","For small, fixed k (say k <= 4), a linear scan over k pointers to find the minimum each step is O(N * k) — simpler code, and for small constant k, O(N * k) and O(N log k) are both effectively O(N).","For \"top-k frequent\" problems, you need O(u) space for the frequency map (u = number of unique elements, u <= n) regardless of approach — that's unavoidable since you must count occurrences."]},{"id":"cs_fundamentals/dsa_patterns#4f4c34f4a31e","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"Why does a ^ a = 0 and a ^ 0 = a, and why does that make XOR useful for \"find the unique element\" problems?","answerFull":"XOR compares each bit independently: two identical bits XOR to 0 (0^0=0, 1^1=0), and a bit XORed with 0 stays unchanged (0^0=0, 1^0=1). Combined with XOR being commutative and associative, XOR-ing a multiset of numbers where every value appears an EVEN number of times produces 0 for those values' contribution — leaving only values that appear an ODD number of times. \"Appears once, others appear twice\" is the simplest case of this.","correct":"XOR compares each bit independently: two identical bits XOR to 0 (0^0=0, 1^1=0), and a bit XORed with 0 stays unchanged (0^0=0, 1^0=1).","distractors":["heapq compares tuples element-by-element; if the first elements (node.val) are equal, it falls through to comparing the second elements — here, ListNode objects, which have no lt defined, raising TypeError.","Iterate mask from 0 to 2^n - 1; for each mask, bit j being set means element j is in this subset.","XOR only cancels in pairs: three copies of a reduce to a (not 0), corrupting the result."]},{"id":"cs_fundamentals/dsa_patterns#cff72dd1c715","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"Single Number II says elements appear THREE times except one — why doesn't XOR work, and what's the fix?","answerFull":"XOR only cancels in pairs: three copies of a reduce to a (not 0), corrupting the result. The fix (§8) counts set bits at each of the 32 bit positions across all numbers and takes the count modulo 3 — for \"noise\" values appearing 3x, their contribution to every bit position's count is a multiple of 3 and vanishes mod 3, leaving only the unique element's bits. This generalizes to \"appears k times\" via mod k. What does n & (n-1) do, and what is it used for? It clears the LOWEST set bit of n. Subtracting 1 flips that bit to 0 and flips all lower bits to 1; ANDing with the original n then zeroes out everything from that bit downward. Two major uses: (1) n & (n-1) == 0 (with n > 0) tests \"is n a power of two\" (powers of two have exactly one set bit), and (2) repeatedly applying it counts set bits in O(popcount(n)) iterations (Brian Kernighan's algorithm) instead of O(bit-width).","correct":"XOR only cancels in pairs: three copies of a reduce to a (not 0), corrupting the result.","distractors":["The function's *return value* must be something the parent can use to compute *its own* combination — here, the subtree's height (a single number representing \"how far down does this subtree extend\").","Since element frequencies are bounded by n (an element can appear at most n times), allocate n + 1 buckets indexed by frequency.","Time is O(n) regardless — each node is visited a constant number of times (once during the \"check k nodes exist\" pass, once during the reversal pass)."]},{"id":"cs_fundamentals/dsa_patterns#cb0a3667b4d4","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"What does n & (-n) do, and how is it different from n & (n-1)?","answerFull":"n & (-n) ISOLATES the lowest set bit (returns a value with only that one bit set), whereas n & (n-1) CLEARS it (returns n with that bit removed). -n in two's complement is ~n + 1; this flips all bits and adds 1, which has the effect that all bits below the lowest set bit of n become 0 in -n too, and ANDing with n leaves only that single bit. This is the core operation in Fenwick tree (Binary Indexed Tree) index traversal.","correct":"n & (-n) ISOLATES the lowest set bit (returns a value with only that one bit set), whereas n & (n-1) CLEARS it (returns n with that bit removed).","distractors":["In languages with fixed-width signed integers (Java, C++), >> preserves the sign bit (fills with 1s for negative numbers) while >>> (Java-only) fills with 0s regardless of sign.","Every node in a cycle has at least one incoming edge *from within the cycle* that can never be \"removed\" (because the node providing that edge can never reach in-degree 0 either — it's circular).","XOR compares each bit independently: two identical bits XOR to 0 (0^0=0, 1^1=0), and a bit XORed with 0 stays unchanged (0^0=0, 1^0=1)."]},{"id":"cs_fundamentals/dsa_patterns#464dce6fc363","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":4,"difficulty":"core","question":"How do I enumerate all subsets of an n-element set using bit manipulation, and what's the complexity?","answerFull":"Iterate mask from 0 to 2^n - 1; for each mask, bit j being set means element j is in this subset. This is O(2^n) masks, and O(n) to materialize each subset, giving O(n * 2^n) total. It's the natural fit when n <= ~20 (since 2^20 ≈ 10^6) — beyond that, 2^n becomes intractable and you need Dynamic Programming or Greedy/pruned Backtracking instead.","correct":"Iterate mask from 0 to 2^n - 1; for each mask, bit j being set means element j is in this subset.","distractors":["Each recursive call branches into multiple choices, and the recursion depth equals the solution length — so the total number of leaves in the search tree is O(branching_factor ^ depth), which is 2^n (subsets) or n!","In languages with fixed-width signed integers (Java, C++), >> preserves the sign bit (fills with 1s for negative numbers) while >>> (Java-only) fills with 0s regardless of sign.","Explicitly enumerate the states (e.g., \"holding a share,\" \"just sold [in cooldown],\" \"not holding, free to buy\") and the transitions between them with their costs/gains."]},{"id":"cs_fundamentals/dsa_patterns#a099c193baf8","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"Why does dp[i] = dp[i >> 1] + (i & 1) correctly compute popcount for Counting Bits?","answerFull":"i >> 1 is i with its lowest bit dropped (integer division by 2) — its popcount, dp[i >> 1], is already computed since i >> 1 < i. (i & 1) is exactly the bit that was dropped (0 or 1). Adding them back together gives the popcount of i. This is a textbook example of DP where the \"smaller subproblem\" comes from a bit-shift rather than i-1.","correct":"i >> 1 is i with its lowest bit dropped (integer division by 2) — its popcount, dp[i >> 1], is already computed since i >> 1 < i.","distractors":["For *counting* connected components (islands) or *generating a path* (backtracking-style), DFS is simpler and uses less code.","For \"top-k frequent\" problems, you need O(u) space for the frequency map (u = number of unique elements, u <= n) regardless of approach — that's unavoidable since you must count occurrences.","For *counting*, the hashmap stores prefix_value -> count of occurrences (a defaultdict(int)), because multiple earlier indices can have the same prefix value, each contributing one valid subarray."]},{"id":"cs_fundamentals/dsa_patterns#78c3fc3be437","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"What's the difference between >> (arithmetic/signed shift) and a logical shift, and does it matter in Python?","answerFull":"In languages with fixed-width signed integers (Java, C++), >> preserves the sign bit (fills with 1s for negative numbers) while >>> (Java-only) fills with 0s regardless of sign. Python integers are arbitrary-precision and conceptually have infinite sign-extended leading bits, so >> on a negative Python int behaves like an arithmetic shift (rounds toward negative infinity) — there's no separate logical shift operator, and bit-manipulation problems that assume 32-bit wraparound (like Single Number II's negative-number handling, or Reverse Bits) need explicit masking (& 0xFFFFFFFF) and sign correction in Python.","correct":"In languages with fixed-width signed integers (Java, C++), >> preserves the sign bit (fills with 1s for negative numbers) while >>> (Java-only) fills with 0s regardless of sign.","distractors":["This is a classic greedy/prefix-sum argument: if the total gas across the whole circuit is >= total cost, then *some* rotation of the prefix-sum sequence is entirely non-negative.","For \"top-k frequent\" problems, you need O(u) space for the frequency map (u = number of unique elements, u <= n) regardless of approach — that's unavoidable since you must count occurrences.","In languages with fixed-width integers (Java, C++), lo + hi can overflow if both are near INT_MAX, wrapping to a negative number and corrupting mid."]},{"id":"cs_fundamentals/dsa_patterns#db017f26ebd9","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"How do I set, clear, toggle, or check a specific bit i of an integer n?","answerFull":"Check: (n >> i) & 1. Set: n |= (1 << i). Clear: n &= ~(1 << i). Toggle: n ^= (1 << i). All four are O(1) and form the vocabulary for almost every bitmask problem — memorize them as a unit.","correct":"Check: (n >> i) & 1. Set: n |= (1 << i).","distractors":["Each level corresponds to one bit position, from most significant to least significant.","XOR compares each bit independently: two identical bits XOR to 0 (0^0=0, 1^1=0), and a bit XORed with 0 stays unchanged (0^0=0, 1^0=1).","n & (-n) ISOLATES the lowest set bit (returns a value with only that one bit set), whereas n & (n-1) CLEARS it (returns n with that bit removed)."]},{"id":"cs_fundamentals/dsa_patterns#dfc5dd86c9be","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"Why is the value range constraint ([1,n] or [0,n-1]) so critical to this pattern?","answerFull":"Because the core operation is \"place value v at index f(v)\" (where f(v) = v-1 or f(v) = v), and this requires f(v) to be a valid index into the array — i.e., 0 <= f(v) < n. If values could be arbitrary (e.g., v = 10^9), f(v) would be out of bounds. The constraint that values come from a range of size n (matching the array's length) is what makes \"use the array itself as a hash table / counting array\" possible.","correct":"Because the core operation is \"place value v at index f(v)\" (where f(v) = v-1 or f(v) = v), and this requires f(v) to be a valid index into the array — i.e., 0 <= f(v) < n.","distractors":["For each interval [start, end, capacity_change], record +capacity_change at index start and -capacity_change at index end in a delta array.","Maintain a dynamic array (list) for O(1) random access (getRandom picks random.choice by index) and a hashmap value -> index_in_array.","Push every node (or value) onto a stack while traversing, then pop to build the reversed list (or overwrite values in place in reverse order)."]},{"id":"cs_fundamentals/dsa_patterns#edccf2b958f8","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"Why does \"Find the Duplicate Number\" have both a cyclic-sort solution and a fast/slow-pointer solution — which should you use?","answerFull":"Both achieve O(n) time, O(1) space. Cyclic sort mutates the array (swaps elements into place) — fine if mutation is allowed (often is, for \"Find the Duplicate\" specifically, since the problem just asks for a number, not the restored array). Fast/slow pointers (Floyd's) treat nums[i] as a pointer to index nums[i], finding the cycle entry without mutating the array. If the problem says \"the array must remain unmodified\" or \"read-only input,\" use fast/slow pointers; otherwise, either works, and cyclic sort is often more intuitive to derive on the spot.","correct":"Both achieve O(n) time, O(1) space.","distractors":["The slow/fast compaction relies on duplicates being *adjacent* — nums[fast] != nums[slow] only correctly identifies \"new\" values if all instances of a value are grouped together.","After slow and fast meet (any meeting point inside the cycle works), keep fast (or either pointer) fixed and advance just one pointer step by step, counting steps, until it returns to the same node.","This follows from the algebra: at the meeting point, slow has traveled L + k steps (L = distance from head to cycle start, k = distance from cycle start to meeting point along the cycle), and fast has traveled 2(L+k)."]},{"id":"cs_fundamentals/dsa_patterns#3d6ab1c2f29d","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"Is cyclic sort a \"real\" sorting algorithm — could you use it to sort an arbitrary array?","answerFull":"Not directly — it's a placement trick that only works when the array's values form (approximately) a permutation of its indices. However, it IS closely related to counting sort / pigeonhole sort: if you generalize \"place value v at index v - min_value\" and allow a separate output array (rather than in-place swapping), you get counting sort, which works for any bounded integer range [min_value, max_value], not just [1,n].","correct":"Not directly — it's a placement trick that only works when the array's values form (approximately) a permutation of its indices.","distractors":["After slow and fast meet (any meeting point inside the cycle works), keep fast (or either pointer) fixed and advance just one pointer step by step, counting steps, until it returns to the same node.","Concatenating and sorting is O(N log N) and throws away the fact that each input is *already sorted*.","The two-pointer scan itself is O(n) (each pointer moves at most n times total, so the loop body runs at most O(n) times even though there are two indices)."]},{"id":"cs_fundamentals/dsa_patterns#602ea3d2ac0c","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":9,"difficulty":"intermediate","question":"Can cyclic sort be parallelized or vectorized?","answerFull":"Not easily — the swaps have sequential dependencies (the value swapped INTO position i must then itself be checked/placed, creating a chain). This is a case where the O(1)-space, O(n)-time guarantee comes at the cost of being inherently sequential, unlike, say, a counting-sort approach (count array can be built with parallel reduction, though the final placement is still sequential in the worst case).","correct":"Not easily — the swaps have sequential dependencies (the value swapped INTO position i must then itself be checked/placed, creating a chain).","distractors":["Main-diagonal reflection (transpose) swaps a[i][j] with a[j][i].","Flatten all employees' intervals into a single list, sort by start time, and run the standard merge to get the union of all busy times.","Sort intervals by start time."]},{"id":"cs_fundamentals/dsa_patterns#73026eacfb26","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":11,"difficulty":"advanced","question":"If the problem constraints say 1 <= nums[i] <= n but DON'T guarantee all values are distinct or that all of 1..n are covered — does cyclic sort still apply?","answerFull":"Yes — this is exactly the \"Find All Numbers Disappeared\" / \"Find All Duplicates\" setup. The placement loop still works (each value v still has a valid home index v-1), but after placement, some indices may have the \"wrong\" value (because their correct value was a duplicate placed elsewhere, or a missing value's home was taken by a duplicate). The second pass (for i: if nums[i] != i+1) is what surfaces these mismatches — it's this second pass, not the placement itself, that handles the \"not a perfect permutation\" case.","correct":"Yes — this is exactly the \"Find All Numbers Disappeared\" / \"Find All Duplicates\" setup.","distractors":["Sort nums first so duplicates are adjacent.","Same structure as prefix sum, but using XOR (prefix[i] = nums[0] XOR nums[1] XOR ...","The slow/fast compaction relies on duplicates being *adjacent* — nums[fast] != nums[slow] only correctly identifies \"new\" values if all instances of a value are grouped together."]},{"id":"cs_fundamentals/dsa_patterns#0c0166df2faf","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"Top-down (memoization) vs. bottom-up (tabulation) — how do you choose?","answerFull":"Top-down (recursion + @lru_cache or a dict) is often easier to *write* directly from the recurrence — it mirrors the brute-force solution with one line added. Bottom-up (iterative table-filling) avoids recursion-depth limits and has no per-call overhead, and makes space optimization (collapsing a 2D table to two 1D rows, or one row) more natural. In interviews, starting top-down to *get the recurrence right*, then converting to bottom-up if asked about stack depth or space, is a strong demonstration of understanding.","correct":"Top-down (recursion + @lru_cache or a dict) is often easier to *write* directly from the recurrence — it mirrors the brute-force solution with one line added.","distractors":["Each recursive call branches into multiple choices, and the recursion depth equals the solution length — so the total number of leaves in the search tree is O(branching_factor ^ depth), which is 2^n (subsets) or n!","A decreasing stack (top-to-bottom values decrease, i.e., pop when a *larger* element arrives) is used to find next greater elements — when you pop, the new element is the popped one's \"next greater\".","The heap doesn't represent \"the answer\" directly; it represents your *current best guess* at the top-k, and its root is the weakest member of that guess — the element most likely to be displaced by a better candidate."]},{"id":"cs_fundamentals/dsa_patterns#cc63e7a439cc","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"Walk through why dp = [0] * (amount+1) is wrong for Coin Change but RIGHT for, say, \"number of ways\" DP (Coin Change II)?","answerFull":"For minimize DP (Coin Change), 0 looks like \"already the best possible value\" to min(), so nothing can ever improve it — 0 must mean \"not yet computed,\" requiring +infinity. For count/sum DP (Coin Change II, \"number of ways\"), 0 correctly means \"zero ways found so far\" — and dp[i] += dp[i - coin] (addition, not min) correctly *accumulates* from that 0 baseline. The right sentinel value depends on the recurrence's combining operator: min/max need +inf/-inf; +=/or need 0/False.","correct":"For minimize DP (Coin Change), 0 looks like \"already the best possible value\" to min(), so nothing can ever improve it — 0 must mean \"not yet computed,\" requiring +infinity.","distractors":["i >> 1 is i with its lowest bit dropped (integer division by 2) — its popcount, dp[i >> 1], is already computed since i >> 1 < i.","The area is min(height[left], height[right]) * (right - left).","Checking every (start, end) pair of nodes along root-to-X paths is O(n^2) (for each node, walk up/down to try all paths through it)."]},{"id":"cs_fundamentals/dsa_patterns#921a64714ab9","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":4,"difficulty":"core","question":"0/1 knapsack vs. unbounded knapsack — why does the capacity-loop DIRECTION matter when space-optimizing to 1D?","answerFull":"In 2D, dp[i][t] (using items 0..i) depends on dp[i-1][t - weight] (previous item's row). Collapsing to 1D dp[t], you need dp[t - weight] to still hold the *previous item's* value when computing the *current* item's dp[t]. Iterating t downward (high to low) ensures dp[t-weight] (a *smaller* index) hasn't been overwritten yet *for this item* — giving 0/1 behavior (each item considered once per capacity). Iterating upward means dp[t-weight] may already reflect *this item* having been used — giving unbounded behavior (item reusable).","correct":"In 2D, dp[i][t] (using items 0..i) depends on dp[i-1][t - weight] (previous item's row).","distractors":["Once both pointers are inside the cycle, the *gap* between them (measured in the direction fast is moving) shrinks by exactly 1 every step, because fast moves 2 and slow moves 1 — net gain of 1 for fast.","Because the algorithm's correctness depends on monotonicity: moving left right strictly increases the candidate sum, and moving right left strictly decreases it.","When the \"best current choice\" changes dynamically as you consume items — i.e., the greedy choice is \"whichever remaining item has the highest frequency/priority *right now*,\" and that ranking shifts after each pick."]},{"id":"cs_fundamentals/dsa_patterns#b299768c553c","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"For LIS, what's the key insight that makes the O(n log n) tails array approach correct — it doesn't even look like it's tracking actual subsequences?","answerFull":"tails[k] = the smallest possible tail value among all increasing subsequences of length k+1 seen so far. Crucially, tails is always sorted — a longer increasing subsequence's tail can't be smaller than a shorter one's *smallest possible* tail (if it were, the longer one's prefix would itself be a shorter subsequence with an even smaller tail, contradiction). This sortedness is what makes binary search (bisect_left) valid: for each new number, binary search finds where it *would* extend or improve some existing subsequence — len(tails) at the end is the LIS length, even though tails itself is generally NOT a valid subsequence of the input.","correct":"tails[k] = the smallest possible tail value among all increasing subsequences of length k+1 seen so far.","distractors":["The heap approach (kth_smallest_in_sorted_matrix) is O(k log n) — great when k is small relative to n^2.","Greedy makes each choice ONCE, in a fixed order, and never revisits it — the algorithm has a clear termination point and a provable bound (when correct).","Because each add(val) must answer \"what is the k-th largest *over all values seen so far*,\" not just over the latest batch."]},{"id":"cs_fundamentals/dsa_patterns#840e4a85fbb7","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"State-machine DP for stock problems — how do you derive the recurrence without memorizing it?","answerFull":"Explicitly enumerate the states (e.g., \"holding a share,\" \"just sold [in cooldown],\" \"not holding, free to buy\") and the transitions between them with their costs/gains. For \"Buy/Sell with Cooldown\": held[i] = max(held[i-1], free[i-1] - price[i]) (keep holding, or buy today from a free state); sold[i] = held[i-1] + price[i] (sell today); free[i] = max(free[i-1], sold[i-1]) (stay free, or cooldown ends). Drawing this as an actual state diagram with labeled edges turns \"memorize the formula\" into \"read off the formula from the diagram.\"","correct":"Explicitly enumerate the states (e.g., \"holding a share,\" \"just sold [in cooldown],\" \"not holding, free to buy\") and the transitions between them with their costs/gains.","distractors":["For \"top-k frequent\" problems, you need O(u) space for the frequency map (u = number of unique elements, u <= n) regardless of approach — that's unavoidable since you must count occurrences.","Plain DFS explores a tree and returns; backtracking explores a tree while mutating shared state (a path, a board, a set of used elements) and explicitly restores that state before trying the next sibling branch.","Read the problem statement's definition of \"adjacent\" — it will explicitly say \"horizontally or vertically adjacent\" (4-directional, the overwhelming majority of problems) or mention diagonal connections (8-directional)."]},{"id":"cs_fundamentals/dsa_patterns#0306c27d75c6","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":8,"difficulty":"intermediate","question":"Interval DP — why must the outer loop iterate over interval LENGTH rather than start index?","answerFull":"dp[i][j] (interval [i,j]) is computed from dp[i][k] and dp[k+1][j] for i <= k < j — both of which are shorter intervals than [i,j]. If the outer loop iterated over i (start index) with j ranging freely, when processing dp[i][j] for a large j, the needed dp[k+1][j] (same end, smaller start k+1 > i) might not be computed yet (it has a *larger* start index, processed *later* in an i-ordered loop). Iterating by length guarantees every dependency (strictly shorter interval) is already filled in.","correct":"dp[i][j] (interval [i,j]) is computed from dp[i][k] and dp[k+1][j] for i <= k < j — both of which are shorter intervals than [i,j].","distractors":["Sort intervals by start time.","For minimize DP (Coin Change), 0 looks like \"already the best possible value\" to min(), so nothing can ever improve it — 0 must mean \"not yet computed,\" requiring +infinity.","\"Merge Intervals\" sorts by start time — you're combining overlapping ranges into larger ranges."]},{"id":"cs_fundamentals/dsa_patterns#894910a9a2ef","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"Why must fast and slow eventually meet if there's a cycle? Could they \"skip past\" each other?","answerFull":"Once both pointers are inside the cycle, the *gap* between them (measured in the direction fast is moving) shrinks by exactly 1 every step, because fast moves 2 and slow moves 1 — net gain of 1 for fast. A gap that shrinks by exactly 1 each step, starting from some finite positive value and operating modulo the cycle length C, must pass through 0 — it cannot jump over 0 because it changes by only 1 at a time. So they are guaranteed to meet within at most C steps after both are in the cycle.","correct":"Once both pointers are inside the cycle, the *gap* between them (measured in the direction fast is moving) shrinks by exactly 1 every step, because fast moves 2 and slow moves 1 — net gain of 1 for fast.","distractors":["LC 55 only asks \"can you reach the end?\" — a single farthest variable suffices, and you fail fast if i > farthest (a gap you can never cross).","This follows from the algebra: at the meeting point, slow has traveled L + k steps (L = distance from head to cycle start, k = distance from cycle start to meeting point along the cycle), and fast has traveled 2(L+k).","After slow and fast meet (any meeting point inside the cycle works), keep fast (or either pointer) fixed and advance just one pointer step by step, counting steps, until it returns to the same node."]},{"id":"cs_fundamentals/dsa_patterns#fc8ec91b93cc","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"Why does resetting one pointer to head and advancing both at speed 1 find the cycle start?","answerFull":"This follows from the algebra: at the meeting point, slow has traveled L + k steps (L = distance from head to cycle start, k = distance from cycle start to meeting point along the cycle), and fast has traveled 2(L+k). Since fast is also (L+k) + n*C for some integer n >= 1 (it lapped the cycle n times), setting these equal gives L = n*C - k. This means walking L steps from head lands you at the cycle start, AND walking L steps from the *meeting point* (which is k steps into the cycle) also lands at the cycle start (since k + L = k + n*C - k = n*C, a multiple of the cycle length, i.e., back to the same position). Both pointers, advancing at the same speed, arrive at the cycle start simultaneously after L steps.","correct":"This follows from the algebra: at the meeting point, slow has traveled L + k steps (L = distance from head to cycle start, k = distance from cycle start to meeting point along the cycle), and fast has traveled 2(L+k).","distractors":["The condition while fast and fast.next handles both: if head is None, fast = None, the loop body never executes, has_cycle returns False correctly.","After slow and fast meet (any meeting point inside the cycle works), keep fast (or either pointer) fixed and advance just one pointer step by step, counting steps, until it returns to the same node.","Within the per-level loop, maintain a prev pointer (reset to None at the start of each level)."]},{"id":"cs_fundamentals/dsa_patterns#cad44c55577d","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"For \"find the middle of a linked list,\" which node is returned for an even-length list — and does it matter?","answerFull":"With the standard template (while fast and fast.next), for a list of length 2m, slow ends at the (m+1)-th node — the *second* of the two middle nodes. This matters for problems like \"Palindrome Linked List\": if you reverse the second half starting from this middle, the two halves being compared have the correct lengths. If a problem wants the *first* middle for even lengths, change the loop condition to while fast.next and fast.next.next.","correct":"With the standard template (while fast and fast.next), for a list of length 2m, slow ends at the (m+1)-th node — the *second* of the two middle nodes.","distractors":["A decreasing stack (top-to-bottom values decrease, i.e., pop when a *larger* element arrives) is used to find next greater elements — when you pop, the new element is the popped one's \"next greater\".","A node is appended to the post-order list only after *all* nodes reachable from it have been appended.","(1) Find the middle using fast/slow pointers — splits the list into two halves."]},{"id":"cs_fundamentals/dsa_patterns#7cc61d3a28e1","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":4,"difficulty":"core","question":"How would you find the length of the cycle once you've detected one?","answerFull":"After slow and fast meet (any meeting point inside the cycle works), keep fast (or either pointer) fixed and advance just one pointer step by step, counting steps, until it returns to the same node. That count is the cycle length C. This is a separate, simple loop appended after cycle detection.","correct":"After slow and fast meet (any meeting point inside the cycle works), keep fast (or either pointer) fixed and advance just one pointer step by step, counting steps, until it returns to the same node.","distractors":["Once both pointers are inside the cycle, the *gap* between them (measured in the direction fast is moving) shrinks by exactly 1 every step, because fast moves 2 and slow moves 1 — net gain of 1 for fast.","The condition while fast and fast.next handles both: if head is None, fast = None, the loop body never executes, has_cycle returns False correctly.","For small, fixed k (say k <= 4), a linear scan over k pointers to find the minimum each step is O(N * k) — simpler code, and for small constant k, O(N * k) and O(N log k) are both effectively O(N)."]},{"id":"cs_fundamentals/dsa_patterns#e0a5130267f1","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"Can fast/slow pointers be applied to something that isn't a linked list?","answerFull":"Yes — anything with an implicit \"next\" function over a domain that *could* cycle. \"Happy Number\" applies it to the sequence of values produced by digit_square_sum. \"Find the Duplicate Number\" applies it to an array by treating nums[i] as \"the next index to visit\" — this only works because values are constrained to [1, n], guaranteeing the \"pointer\" nums[i] is always a valid index.","correct":"Yes — anything with an implicit \"next\" function over a domain that *could* cycle.","distractors":["After slow and fast meet (any meeting point inside the cycle works), keep fast (or either pointer) fixed and advance just one pointer step by step, counting steps, until it returns to the same node.","(1) Find the middle using fast/slow pointers — splits the list into two halves.","With the standard template (while fast and fast.next), for a list of length 2m, slow ends at the (m+1)-th node — the *second* of the two middle nodes."]},{"id":"cs_fundamentals/dsa_patterns#9da19c1f8b02","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"Why is the duplicate found by Floyd's algorithm in \"Find the Duplicate Number\" guaranteed to be the start of the cycle, and why is the \"cycle start\" the duplicate?","answerFull":"Model the array as a function f(i) = nums[i]. Since there are n+1 numbers in range [1, n], by pigeonhole at least one value repeats. In the functional graph where each index i points to nums[i], the duplicate value is the one with two indices pointing into it — making it the entry point of a cycle (the only node with in-degree ≥ 2). Floyd's \"find cycle start\" phase locates exactly this node.","correct":"Model the array as a function f(i) = nums[i].","distractors":["For an undirected edge (u, v): if find(u) == find(v) *before* you union them, then u and v were already connected via some other path — adding edge (u, v) would create a cycle.","The used[] array approach (Template 2) is more intuitive and naturally supports the duplicate-skipping check for Permutations II.","Kahn's algorithm is usually more intuitive to implement and explain in an interview because the in-degree-zero queue directly mirrors \"what can I do right now\" — and cycle detection is a single length check at the end."]},{"id":"cs_fundamentals/dsa_patterns#136bfcb22cd5","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"What's the difference between the \"fast moves 2x\" pattern and the \"fixed gap of k\" pattern? Are they the same technique?","answerFull":"They're related (both use two pointers at different \"positions\" on the same structure) but solve different problems. \"Fast moves 2x\" exploits a *relative speed difference* to detect periodicity (cycles) or find a midpoint (the 2:1 ratio means when fast finishes, slow is at the halfway point). \"Fixed gap of k\" uses a *constant offset* — both pointers move at the same speed (1 step), maintaining a gap of exactly k — to identify \"the node k positions before/after another node\" without needing to know the list's total length in advance.","correct":"They're related (both use two pointers at different \"positions\" on the same structure) but solve different problems.","distractors":["Same structure as prefix sum, but using XOR (prefix[i] = nums[0] XOR nums[1] XOR ...","After slow and fast meet (any meeting point inside the cycle works), keep fast (or either pointer) fixed and advance just one pointer step by step, counting steps, until it returns to the same node.","(1) Find the middle using fast/slow pointers — splits the list into two halves."]},{"id":"cs_fundamentals/dsa_patterns#c5512366b8b9","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":9,"difficulty":"intermediate","question":"How do you handle the edge case of an empty list or single-node list in cycle detection?","answerFull":"The condition while fast and fast.next handles both: if head is None, fast = None, the loop body never executes, has_cycle returns False correctly. If the list has one node with no self-loop, fast.next is None after zero iterations, loop doesn't execute, returns False. If the single node *does* point to itself (node.next = node), then fast and fast.next is true; slow = slow.next = node, fast = fast.next.next = node — slow is fast → True, correctly detected.","correct":"The condition while fast and fast.next handles both: if head is None, fast = None, the loop body never executes, has_cycle returns False correctly.","distractors":["Every node in a cycle has at least one incoming edge *from within the cycle* that can never be \"removed\" (because the node providing that edge can never reach in-degree 0 either — it's circular).","With the standard template (while fast and fast.next), for a list of length 2m, slow ends at the (m+1)-th node — the *second* of the two middle nodes.","Without a dummy node, removing the actual head of the list (when n == length) requires special-casing — there's no \"previous\" node to update .next on."]},{"id":"cs_fundamentals/dsa_patterns#3cba7920c55e","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":10,"difficulty":"intermediate","question":"In \"Remove Nth Node From End of List,\" why use a dummy head node?","answerFull":"Without a dummy node, removing the actual head of the list (when n == length) requires special-casing — there's no \"previous\" node to update .next on. By prepending a dummy node (dummy.next = head), the head becomes \"just another node with a predecessor,\" and the same slow.next = slow.next.next logic uniformly handles removing any node, including the original head. Return dummy.next as the new head.","correct":"Without a dummy node, removing the actual head of the list (when n == length) requires special-casing — there's no \"previous\" node to update .next on.","distractors":["Kahn's algorithm is usually more intuitive to implement and explain in an interview because the in-degree-zero queue directly mirrors \"what can I do right now\" — and cycle detection is a single length check at the end.","A dummy node handles the edge case where left == 1 (the sub-range includes the original head) — without it, there's no \"node before the sub-range\" to update.","Walk to the node representing the word's last character and set is_end = False."]},{"id":"cs_fundamentals/dsa_patterns#795b9c82d6a8","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":11,"difficulty":"advanced","question":"Is there a non-Floyd's way to detect a cycle in O(1) space?","answerFull":"Floyd's (tortoise and hare) is the canonical O(1)-space approach. An alternative — Brent's algorithm — also achieves O(1) space and can be faster in practice (fewer total pointer movements) by using a \"teleporting\" slow pointer with periodically doubling step limits, but it's rarely expected at the interview level; mentioning it shows extra depth but Floyd's is sufficient and more commonly recognized.","correct":"Floyd's (tortoise and hare) is the canonical O(1)-space approach.","distractors":["Use lo <= hi (with hi = mid - 1 / lo = mid + 1) when you're searching for an *exact match* and want to detect \"not found\" (loop exits with lo > hi).","The three-phase approach exploits the precondition that the input is *already sorted and non-overlapping* — a single linear scan suffices.","find(x) recurses up to the root, then on the way back *down* the call stack, sets parent[node] = root for every node visited."]},{"id":"cs_fundamentals/dsa_patterns#cbebf0aa63b7","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"Why do graph traversals need a visited set but tree traversals don't?","answerFull":"A tree has no cycles and exactly one path from the root to any node, so a recursive/BFS walk can never revisit a node. A graph (and a grid, which is just a graph) can have cycles — e.g., a 2x2 block of land has 4 cells each adjacent to 2 others, forming a cycle. Without marking visited cells, a traversal would loop forever, which is exactly the bug in §8.","correct":"A tree has no cycles and exactly one path from the root to any node, so a recursive/BFS walk can never revisit a node.","distractors":["A visited set only answers \"have I processed this node?\" — it doesn't tell you *what the corresponding new node is*.","Any shortest *simple* path (no repeated nodes) in a graph with V nodes has at most V-1 edges.","Preorder (node, left, right) when you need to process a node *before* its children — serialization (the root must be written first to be read first), or copying/cloning a tree top-down."]},{"id":"cs_fundamentals/dsa_patterns#c23e0efb00cb","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"When do you use BFS vs. DFS for grid problems?","answerFull":"For *counting* connected components (islands) or *generating a path* (backtracking-style), DFS is simpler and uses less code. For finding the *shortest* path/distance in an unweighted grid, or for \"spreads simultaneously from multiple sources\" problems (Rotting Oranges), BFS is required because it explores in increasing-distance order — DFS would find *a* path, not necessarily the *shortest* one.","correct":"For *counting* connected components (islands) or *generating a path* (backtracking-style), DFS is simpler and uses less code.","distractors":["Running a separate DFS per word costs O(words · rows·cols·4^L) — the grid is re-explored from scratch for every word.","If you're given the *whole* graph upfront and need to answer connectivity questions once (e.g., \"how many connected components does this static graph have?\"), DFS/BFS is simpler to write and equally efficient — O(V+E).","A tree has no cycles and exactly one path from the root to any node, so a recursive/BFS walk can never revisit a node."]},{"id":"cs_fundamentals/dsa_patterns#0aa3faef5d19","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"Why seed a multi-source BFS with all sources at once instead of running BFS from each source separately?","answerFull":"Running BFS once per source and taking the minimum would be correct but wasteful — O(k · rows · cols) for k sources. Seeding the queue with all k sources at the start means the BFS naturally computes, for every cell, the distance to the *nearest* source in a single O(rows · cols) pass, because BFS processes cells in increasing-distance order regardless of which source they came from.","correct":"Running BFS once per source and taking the minimum would be correct but wasteful — O(k · rows · cols) for k sources.","distractors":["Running a separate DFS per word costs O(words · rows·cols·4^L) — the grid is re-explored from scratch for every word.","Checking, from each cell, whether water can reach both oceans would require a traversal *from* that cell — O(rows · cols) work per cell, O((rows·cols)²) total.","Re-running DFS from scratch after each of k additions costs O(k * rows * cols) in the worst case."]},{"id":"cs_fundamentals/dsa_patterns#0d53e15e4066","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":4,"difficulty":"core","question":"In the BROKEN→FIX example, why exactly does marking visited \"too late\" cause infinite recursion, and not just extra work?","answerFull":"Because the guard condition grid[r][c] != \"1\" is the *only* thing that stops recursion, and it's checked at the *top* of each call using the grid's current state. If grid[r][c] is still \"1\" when a neighbor recurses back into (r, c), the guard passes, and that call recurses back to its own neighbors — including the original caller — again with grid unchanged. There's no decreasing quantity (no \"smaller subproblem\"), so the recursion never reaches a base case — it's not just slow, it never terminates (until Python's stack limit raises RecursionError).","correct":"Because the guard condition grid[r][c] != \"1\" is the *only* thing that stops recursion, and it's checked at the *top* of each call using the grid's current state.","distractors":["find(x) recurses up to the root, then on the way back *down* the call stack, sets parent[node] = root for every node visited.","Top-down (recursion + @lru_cache or a dict) is often easier to *write* directly from the recurrence — it mirrors the brute-force solution with one line added.","WHITE = never visited; GRAY = currently on the DFS call stack (an ancestor of the current node in the DFS tree); BLACK = fully processed (this node and all its descendants are done)."]},{"id":"cs_fundamentals/dsa_patterns#088abb93523e","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"Why does Clone Graph need a hashmap instead of just a visited set?","answerFull":"A visited set only answers \"have I processed this node?\" — it doesn't tell you *what the corresponding new node is*. When neighbor B's clone needs to point back to neighbor A's clone (a cycle), you need to look up \"the new node that corresponds to old node A,\" which requires a mapping (old_to_new), not just a boolean set. Registering the new node in the map *before* recursing into its neighbors is what makes cycles terminate correctly.","correct":"A visited set only answers \"have I processed this node?\" — it doesn't tell you *what the corresponding new node is*.","distractors":["WHITE = never visited; GRAY = currently on the DFS call stack (an ancestor of the current node in the DFS tree); BLACK = fully processed (this node and all its descendants are done).","The BST property is global, not just local: every node in the *entire* left subtree must be less than the current node, not just the immediate left child.","find(x) recurses up to the root, then on the way back *down* the call stack, sets parent[node] = root for every node visited."]},{"id":"cs_fundamentals/dsa_patterns#db5cf84cd40b","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"Why does Word Ladder generate neighbors via 26-letter substitution instead of comparing every pair of words?","answerFull":"Comparing every pair of n words to check \"differs by one letter\" costs O(n² · L) where L is word length. Generating neighbors by trying all 26 letters at each of L positions costs O(L · 26) per word, or O(n · L · 26) total — and a hash-set lookup confirms whether the generated word exists in the dictionary. For n in the thousands, this is the difference between roughly 10^7 and 10^10+ operations.","correct":"Comparing every pair of n words to check \"differs by one letter\" costs O(n² · L) where L is word length.","distractors":["Compare each pair of *adjacent* words in the given (sorted) word list.","Checking every (start, end) pair of nodes along root-to-X paths is O(n^2) (for each node, walk up/down to try all paths through it).","Opposite ends (left=0, right=n-1, converging) is for finding a *relationship between two elements* (pair sum, palindrome check, container area) — the search space shrinks from both sides."]},{"id":"cs_fundamentals/dsa_patterns#8aec432cd869","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"How do you choose between 4-directional and 8-directional movement?","answerFull":"Read the problem statement's definition of \"adjacent\" — it will explicitly say \"horizontally or vertically adjacent\" (4-directional, the overwhelming majority of problems) or mention diagonal connections (8-directional). When in doubt, 4-directional (DIRECTIONS_4) is the default; extending to 8 directions is a one-line change to the directions list.","correct":"Read the problem statement's definition of \"adjacent\" — it will explicitly say \"horizontally or vertically adjacent\" (4-directional, the overwhelming majority of problems) or mention diagonal connections (8-directional).","distractors":["All cells on the same anti-diagonal share a constant r + c (and main diagonals share constant r - c).","For small, fixed k (say k <= 4), a linear scan over k pointers to find the minimum each step is O(N * k) — simpler code, and for small constant k, O(N * k) and O(N log k) are both effectively O(N).","Compare each pair of *adjacent* words in the given (sorted) word list."]},{"id":"cs_fundamentals/dsa_patterns#b5089f3c9166","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":8,"difficulty":"intermediate","question":"Why is Pacific Atlantic Water Flow solved with two traversals from the borders instead of one traversal per cell?","answerFull":"Checking, from each cell, whether water can reach both oceans would require a traversal *from* that cell — O(rows · cols) work per cell, O((rows·cols)²) total. Instead, run two multi-source traversals *backwards* (uphill) from every Pacific-adjacent border cell and every Atlantic-adjacent border cell — O(rows · cols) each. A cell can reach an ocean iff that ocean's backwards-traversal reached the cell. The answer is the intersection of the two reachable sets — O(rows · cols) total instead of quadratic.","correct":"Checking, from each cell, whether water can reach both oceans would require a traversal *from* that cell — O(rows · cols) work per cell, O((rows·cols)²) total.","distractors":["Change dfs from a None-returning side-effecting function to one that *returns* 1 + dfs(up) + dfs(down) + dfs(left) + dfs(right) (with the same \"sink before recurse\" rule, returning 0 for out-of-bounds/water cells).","Running BFS once per source and taking the minimum would be correct but wasteful — O(k · rows · cols) for k sources.","Running a separate DFS per word costs O(words · rows·cols·4^L) — the grid is re-explored from scratch for every word."]},{"id":"cs_fundamentals/dsa_patterns#a0a440312578","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":9,"difficulty":"intermediate","question":"What's the difference between Surrounded Regions and Number of Islands — both involve counting/marking regions of the same character?","answerFull":"Number of Islands counts *all* connected regions independently. Surrounded Regions requires knowing whether a region touches the border — which is a *global* property, not determinable by looking at the region in isolation. The trick is to invert the search: instead of checking \"is this region surrounded,\" find all regions connected to the border (which can never be surrounded) first, mark them safe, then flip everything else.","correct":"Number of Islands counts *all* connected regions independently.","distractors":["Without this check, every number triggers an inner \"count forward\" loop, and for a single long run of length n, this sums to O(n^2) (the 1st number counts n steps, the 2nd counts n-1, etc.).","For *counting* connected components (islands) or *generating a path* (backtracking-style), DFS is simpler and uses less code.","After slow and fast meet (any meeting point inside the cycle works), keep fast (or either pointer) fixed and advance just one pointer step by step, counting steps, until it returns to the same node."]},{"id":"cs_fundamentals/dsa_patterns#b0b44c0a47f5","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":10,"difficulty":"intermediate","question":"Can recursive DFS on a grid cause a stack overflow, and how do you avoid it?","answerFull":"Yes — Python's default recursion limit is ~1000, and a single long, winding island can have a DFS call depth equal to its cell count. A grid as small as 32x32 (1024 cells) can exceed the limit if fully connected in a snake shape. The fix is an iterative DFS using an explicit stack (a list, push/pop) or switching to BFS with a deque — both have identical time/space complexity but no call-stack depth limit (bounded only by available memory).","correct":"Yes — Python's default recursion limit is ~1000, and a single long, winding island can have a DFS call depth equal to its cell count.","distractors":["When you don't need subsets in any particular order and want to avoid Python recursion overhead (function call costs add up across 2^n calls).","WHITE = never visited; GRAY = currently on the DFS call stack (an ancestor of the current node in the DFS tree); BLACK = fully processed (this node and all its descendants are done).","board[r][c] = \"#\" is used as the \"currently on this DFS path\" marker — it must be scoped to the current call stack, not permanent."]},{"id":"cs_fundamentals/dsa_patterns#b0b7c982387b","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":11,"difficulty":"advanced","question":"How would you adapt num_islands to also return the size of the largest island (Max Area of Island, LC 695)?","answerFull":"Change dfs from a None-returning side-effecting function to one that *returns* 1 + dfs(up) + dfs(down) + dfs(left) + dfs(right) (with the same \"sink before recurse\" rule, returning 0 for out-of-bounds/water cells). Track max(area, dfs(r, c)) in the outer loop instead of just incrementing a counter.","correct":"Change dfs from a None-returning side-effecting function to one that *returns* 1 + dfs(up) + dfs(down) + dfs(left) + dfs(right) (with the same \"sink before recurse\" rule, returning 0 for out-of-bounds/water cells).","distractors":["Preorder (node, left, right) when you need to process a node *before* its children — serialization (the root must be written first to be read first), or copying/cloning a tree top-down.","Change the target size ratio: instead of keeping left and right within one element of equal size, keep len(left) ~= p% * n and len(right) ~= (100-p)% * n.","The area is min(height[left], height[right]) * (right - left)."]},{"id":"cs_fundamentals/dsa_patterns#8333a8b2e06d","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"How do I prove a greedy algorithm is correct, on the spot, in an interview?","answerFull":"Use the exchange-argument sketch: assume an optimal solution O that differs from your greedy solution G in its first choice. Show that swapping O's first choice for G's choice produces a solution that is at least as good. By induction over the remaining sub-problem, G is optimal. You don't need a rigorous formal proof — interviewers are looking for \"I considered whether swapping in my choice could ever make things worse, and it can't, because...\".","correct":"Use the exchange-argument sketch: assume an optimal solution O that differs from your greedy solution G in its first choice.","distractors":["Kahn's algorithm is usually more intuitive to implement and explain in an interview because the in-degree-zero queue directly mirrors \"what can I do right now\" — and cycle detection is a single length check at the end.","Merging is O(m + n) — correct but not optimal.","In an interview, rarely — O(1) space is almost always preferred when achievable, and the iterative version isn't meaningfully harder to write once memorized."]},{"id":"cs_fundamentals/dsa_patterns#b3af7413fae8","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"Why does Non-overlapping Intervals sort by END time and not START time?","answerFull":"Because the goal is to keep as many intervals as possible, and the interval that *finishes earliest* leaves the maximum remaining timeline for future intervals — regardless of when it starts. Sorting by start time can lock in a long interval early (because it happens to start first) and force removal of many short intervals that would otherwise fit. See the BROKEN->FIX in §8 for a concrete counter-example.","correct":"Because the goal is to keep as many intervals as possible, and the interval that *finishes earliest* leaves the maximum remaining timeline for future intervals — regardless of when it starts.","distractors":["Think of it as a timeline sweep: sort all start times and all end times independently.","The three-phase approach exploits the precondition that the input is *already sorted and non-overlapping* — a single linear scan suffices.","Flatten all employees' intervals into a single list, sort by start time, and run the standard merge to get the union of all busy times."]},{"id":"cs_fundamentals/dsa_patterns#442786798022","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":4,"difficulty":"core","question":"Jump Game (LC 55) and Jump Game II (LC 45) both use nums[i] as a max jump length — why are the algorithms different?","answerFull":"LC 55 only asks \"can you reach the end?\" — a single farthest variable suffices, and you fail fast if i > farthest (a gap you can never cross). LC 45 asks for the *minimum number of jumps*, which requires tracking jump \"levels\" — hence the extra current_end/jumps bookkeeping that detects when one level's reach is exhausted and the next jump must be counted.","correct":"LC 55 only asks \"can you reach the end?\" — a single farthest variable suffices, and you fail fast if i > farthest (a gap you can never cross).","distractors":["Fixed-size: a single for loop where right and left = right - k + 1 move in lockstep — no inner loop needed, just \"add nums[right], remove nums[right-k]\" each iteration.","With the standard template (while fast and fast.next), for a list of length 2m, slow ends at the (m+1)-th node — the *second* of the two middle nodes.","Every node in a cycle has at least one incoming edge *from within the cycle* that can never be \"removed\" (because the node providing that edge can never reach in-degree 0 either — it's circular)."]},{"id":"cs_fundamentals/dsa_patterns#9f9cd1741d40","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"In the Gas Station problem, why does total_tank >= 0 guarantee a valid starting point exists, without checking every possible start?","answerFull":"This is a classic greedy/prefix-sum argument: if the total gas across the whole circuit is >= total cost, then *some* rotation of the prefix-sum sequence is entirely non-negative. The point where the running tank first dips below zero proves that no station up to and including that point can be the start (any later start has even less cumulative slack at that point) — so you can safely advance the candidate start past it without losing the true answer. This single pass replaces an O(n^2) \"try every start\" brute force.","correct":"This is a classic greedy/prefix-sum argument: if the total gas across the whole circuit is >= total cost, then *some* rotation of the prefix-sum sequence is entirely non-negative.","distractors":["When arr[lo] == arr[mid] == arr[hi], you cannot tell which half is sorted (both could look \"flat\" while the rotation point hides inside either).","For *counting*, the hashmap stores prefix_value -> count of occurrences (a defaultdict(int)), because multiple earlier indices can have the same prefix value, each contributing one valid subarray.","Checking, from each cell, whether water can reach both oceans would require a traversal *from* that cell — O(rows · cols) work per cell, O((rows·cols)²) total."]},{"id":"cs_fundamentals/dsa_patterns#1e1e4bc518ee","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"Why does the Candy problem need TWO passes instead of one?","answerFull":"Each child's required candy count depends on comparisons with BOTH neighbors simultaneously, but a single left-to-right (or right-to-left) pass can only enforce one direction's constraint at a time. The left-to-right pass guarantees \"if ratings[i] > ratings[i-1], then candies[i] > candies[i-1]\"; the right-to-left pass guarantees the symmetric constraint with the right neighbor. Taking max(left_pass[i], right_pass[i]) per index satisfies both constraints simultaneously without violating the other.","correct":"Each child's required candy count depends on comparisons with BOTH neighbors simultaneously, but a single left-to-right (or right-to-left) pass can only enforce one direction's constraint at a time.","distractors":["Change the target size ratio: instead of keeping left and right within one element of equal size, keep len(left) ~= p% * n and len(right) ~= (100-p)% * n.","By the BST invariant, every node's left subtree contains only smaller values and its right subtree only larger values.","Directly, no — sliding window relies on the 1D notion of \"contiguous range\" with a single left/right pair."]},{"id":"cs_fundamentals/dsa_patterns#693ea6ac3d91","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"When would I reach for a heap inside a greedy algorithm (e.g., Task Scheduler, Reorganize String)?","answerFull":"When the \"best current choice\" changes dynamically as you consume items — i.e., the greedy choice is \"whichever remaining item has the highest frequency/priority *right now*,\" and that ranking shifts after each pick. A pre-sorted array can't represent a ranking that changes during the algorithm; a max-heap can pop-and-reinsert in O(log n).","correct":"When the \"best current choice\" changes dynamically as you consume items — i.e., the greedy choice is \"whichever remaining item has the highest frequency/priority *right now*,\" and that ranking shifts after each pick.","distractors":["The heap doesn't represent \"the answer\" directly; it represents your *current best guess* at the top-k, and its root is the weakest member of that guess — the element most likely to be displaced by a better candidate.","Kahn's algorithm is usually more intuitive to implement and explain in an interview because the in-degree-zero queue directly mirrors \"what can I do right now\" — and cycle detection is a single length check at the end.","Use the exchange-argument sketch: assume an optimal solution O that differs from your greedy solution G in its first choice."]},{"id":"cs_fundamentals/dsa_patterns#4f8c304d2fc7","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":10,"difficulty":"intermediate","question":"What's the difference between greedy and \"local search\" / hill climbing?","answerFull":"Greedy makes each choice ONCE, in a fixed order, and never revisits it — the algorithm has a clear termination point and a provable bound (when correct). Local search/hill climbing iteratively improves a *complete* candidate solution by exploring neighboring solutions, can run for many iterations or get stuck in local optima, and generally has no optimality guarantee. Greedy is a single deterministic pass; local search is an iterative refinement loop (more common in optimization/heuristics than in coding interviews).","correct":"Greedy makes each choice ONCE, in a fixed order, and never revisits it — the algorithm has a clear termination point and a provable bound (when correct).","distractors":["Each element is pushed exactly once (once per iteration of the outer loop) and popped at most once (across the entire algorithm's lifetime, total pops <= total pushes <= n).","When the \"best current choice\" changes dynamically as you consume items — i.e., the greedy choice is \"whichever remaining item has the highest frequency/priority *right now*,\" and that ranking shifts after each pick.","Each insertion requires finding the correct position (binary search on starts, O(log n)) and checking for overlap with neighbors (O(1)), but inserting into a Python list is O(n) due to shifting elements."]},{"id":"cs_fundamentals/dsa_patterns#a5e8521d2bfa","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":11,"difficulty":"advanced","question":"Huffman coding is described as \"greedy\" — what's the greedy choice?","answerFull":"At each step, take the two least-frequent remaining nodes/symbols and merge them into a new node whose frequency is their sum, inserting it back into a min-heap. The exchange argument: the two least-frequent symbols can always be placed at the maximum tree depth (longest codes) without increasing the total weighted code length compared to any other arrangement — so merging them first is always safe.","correct":"At each step, take the two least-frequent remaining nodes/symbols and merge them into a new node whose frequency is their sum, inserting it back into a min-heap.","distractors":["Treat it as a fresh Match step on the SAME problem: \"With a stream, I can't build the full frequency map upfront.","The key step is head.next.next = head — this makes the node *after* head point back to head, effectively reversing that one link.","For \"top-k frequent\" problems, you need O(u) space for the frequency map (u = number of unique elements, u <= n) regardless of approach — that's unavoidable since you must count occurrences."]},{"id":"cs_fundamentals/dsa_patterns#8e56517f2064","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":12,"difficulty":"advanced","question":"How do I avoid analysis paralysis between \"is this greedy or DP\" during a 45-minute interview?","answerFull":"Default to attempting the greedy approach first IF you can state the greedy rule in one sentence AND immediately think of a potential counter-example to test. Spend at most ~2 minutes on this gut-check. If you construct a counter-example, say so out loud (\"greedy by X fails when ...\") and pivot to DP — articulating *why* greedy fails is itself a strong signal to the interviewer, and it naturally motivates the DP state you need.","correct":"Default to attempting the greedy approach first IF you can state the greedy rule in one sentence AND immediately think of a potential counter-example to test.","distractors":["O(n*m) (for each of n nodes in the main tree, run an O(m) equality check against the target) is fine for typical interview constraints (n, m <= 1000, giving 10^6 operations).","This is a classic greedy/prefix-sum argument: if the total gas across the whole circuit is >= total cost, then *some* rotation of the prefix-sum sequence is entirely non-negative.","In an interview, rarely — O(1) space is almost always preferred when achievable, and the iterative version isn't meaningfully harder to write once memorized."]},{"id":"cs_fundamentals/dsa_patterns#3daf7049bfbf","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"Two Sum: why can't you just sort the array and use two pointers if the problem requires returning indices?","answerFull":"Sorting rearranges elements, destroying the mapping from value to original index. You'd need to store (value, original_index) pairs and sort those — which works, but then the two-pointer scan finds a pair of *sorted-position* pointers, and you still need O(n) to extract their original indices, plus the sort itself is O(n log n) vs the hashmap's O(n). If indices aren't needed (just \"does a pair exist\"), sort + two pointers is a valid O(n log n) / O(1)-extra-space alternative to hashing's O(n) / O(n).","correct":"Sorting rearranges elements, destroying the mapping from value to original index.","distractors":["The problem states leftover nodes (fewer than k) at the end should remain in their original order, NOT be reversed.","prefix[0] = 0 represents the sum of zero elements — it's the identity element for addition.","Seed a min-heap with the first element of each row (or just row 0's first min(k, n) elements with pointers), each tagged with its row and column."]},{"id":"cs_fundamentals/dsa_patterns#392174561ab2","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":4,"difficulty":"core","question":"How do you choose a \"canonical key\" for grouping (e.g., Group Anagrams)? What makes a good canonical key?","answerFull":"A canonical key must be (1) identical for all items that should be grouped together, and (2) different (with very high probability) for items that shouldn't. For anagrams, sorted(word) (or a tuple of character counts) satisfies both: anagrams produce the same sorted string, non-anagrams produce different ones. The key must also be hashable (immutable) — a list can't be a dict key, but a str (from ''.join(sorted(...))) or a tuple can.","correct":"A canonical key must be (1) identical for all items that should be grouped together, and (2) different (with very high probability) for items that shouldn't.","distractors":["Floyd's (tortoise and hare) is the canonical O(1)-space approach.","In 2D, dp[i][t] (using items 0..i) depends on dp[i-1][t - weight] (previous item's row).","The key step is head.next.next = head — this makes the node *after* head point back to head, effectively reversing that one link."]},{"id":"cs_fundamentals/dsa_patterns#f2daea98c88d","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"In \"Longest Consecutive Sequence,\" why does checking num - 1 not in num_set guarantee O(n) overall?","answerFull":"Without this check, every number triggers an inner \"count forward\" loop, and for a single long run of length n, this sums to O(n^2) (the 1st number counts n steps, the 2nd counts n-1, etc.). With the check, only numbers that are the *start* of a run (no predecessor in the set) trigger the inner loop. Each run is \"counted forward\" exactly once, by its start — so across all runs, the total work done by all inner loops combined is at most n (every number belongs to exactly one run and is visited by exactly one inner loop). Outer loop is O(n), inner loops sum to O(n) — total O(n).","correct":"Without this check, every number triggers an inner \"count forward\" loop, and for a single long run of length n, this sums to O(n^2) (the 1st number counts n steps, the 2nd counts n-1, etc.).","distractors":["Number of Islands counts *all* connected regions independently.","Kahn's algorithm is usually more intuitive to implement and explain in an interview because the in-degree-zero queue directly mirrors \"what can I do right now\" — and cycle detection is a single length check at the end.","Without the sets, validating \"can I place a queen at (row, col)?\" requires scanning all previously-placed queens — O(row) per check, O(n) per row, O(n^2) total per leaf, O(n^2 * n!) overall."]},{"id":"cs_fundamentals/dsa_patterns#908d3529fda4","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"What's the \"two-hashmap meet-in-the-middle\" technique used in 4Sum II?","answerFull":"Given four arrays A, B, C, D, you want to count tuples (a,b,c,d) where a+b+c+d == 0. Brute force is O(n^4). Instead, precompute all pairwise sums a+b for A x B into a hashmap (sumAB -> count), O(n^2). Then for each pair (c,d) from C x D, look up -(c+d) in that hashmap, O(n^2) total. This reduces O(n^4) to O(n^2) — \"meet in the middle\" by splitting the problem into two halves joined via a hashmap.","correct":"Given four arrays A, B, C, D, you want to count tuples (a,b,c,d) where a+b+c+d == 0.","distractors":["For *counting*, the hashmap stores prefix_value -> count of occurrences (a defaultdict(int)), because multiple earlier indices can have the same prefix value, each contributing one valid subarray.","Python compares tuples element-by-element, falling through to the second element only if the first elements are equal.","Use lo <= hi (with hi = mid - 1 / lo = mid + 1) when you're searching for an *exact match* and want to detect \"not found\" (loop exits with lo > hi)."]},{"id":"cs_fundamentals/dsa_patterns#3c6d5828c469","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"How would you implement \"Insert Delete GetRandom O(1)\" — what's the role of the hashmap?","answerFull":"Maintain a dynamic array (list) for O(1) random access (getRandom picks random.choice by index) and a hashmap value -> index_in_array. Insert: append to the array, record its index in the hashmap, O(1). Delete: look up the index of the value to delete, swap it with the *last* element in the array (update the swapped element's index in the hashmap), pop the last element, remove from hashmap — all O(1). The hashmap is what makes \"find the index of an arbitrary value\" O(1) instead of O(n), enabling the swap-and-pop deletion.","correct":"Maintain a dynamic array (list) for O(1) random access (getRandom picks random.choice by index) and a hashmap value -> index_in_array.","distractors":["For a single pair-sum query, a hashmap *is* asymptotically better (O(n) vs O(n log n)).","Use the exchange-argument sketch: assume an optimal solution O that differs from your greedy solution G in its first choice.","The recursive calls share a *single* mutable path (or used array) by reference — there's only one list object, repeatedly appended to and read from at different points in the recursion."]},{"id":"cs_fundamentals/dsa_patterns#f740edc22898","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":9,"difficulty":"intermediate","question":"What's a hash collision, and how do hash tables handle it?","answerFull":"A collision is when two different keys hash to the same bucket/slot. Two main resolution strategies: chaining (each bucket holds a linked list or, in Java 8+, a tree once it has 8+ entries, of all keys hashing there) and open addressing (probe for the next empty slot — used by Python's dict/set). Both add O(1) amortized overhead in the average case but can degrade toward O(n) per operation if collisions are frequent (e.g., adversarial inputs designed to collide, a known DoS vector mitigated by randomized hash seeds).","correct":"A collision is when two different keys hash to the same bucket/slot.","distractors":["\"Cousins\" requires same depth and different parents (siblings have the same parent and are excluded).","They're related (both use two pointers at different \"positions\" on the same structure) but solve different problems.","State it calmly and specifically: \"I see the issue — on line [X], I'm using < but this should be <= to handle the case where [specific scenario from my edge-case example]."]},{"id":"cs_fundamentals/dsa_patterns#a11a411a8b96","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":10,"difficulty":"intermediate","question":"How does a rolling hash (Rabin-Karp) avoid re-hashing the entire window on each shift?","answerFull":"A rolling hash is computed such that hash(window[1:] + new_char) can be derived from hash(window) in O(1), typically via polynomial hashing: hash = (hash - old_char * base^(k-1)) * base + new_char (mod M). This removes the contribution of the character leaving the window and adds the new character, all in O(1), instead of recomputing the hash of all k characters from scratch (O(k) per shift, O(n*k) total). This makes substring search/matching O(n + m) instead of O(n*m).","correct":"A rolling hash is computed such that hash(window[1:] + new_char) can be derived from hash(window) in O(1), typically via polynomial hashing: hash = (hash - old_char * base^(k-1)) * base + new_char (mod M).","distractors":["dp[i][j] (interval [i,j]) is computed from dp[i][k] and dp[k+1][j] for i <= k < j — both of which are shorter intervals than [i,j].","For minimize DP (Coin Change), 0 looks like \"already the best possible value\" to min(), so nothing can ever improve it — 0 must mean \"not yet computed,\" requiring +infinity.","path is a single mutable list object, repeatedly appended to and popped from throughout the entire recursion."]},{"id":"cs_fundamentals/dsa_patterns#2b3af60de372","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":11,"difficulty":"advanced","question":"If two objects are equal (==), must their hash values be equal? What happens if you violate this?","answerFull":"Yes — this is the hash-equality contract: if a == b, then hash(a) == hash(b) must hold (the converse is not required — different objects can have the same hash, a collision). Violating this (e.g., overriding eq without overriding hash in Python, or equals() without hashCode() in Java) causes silent bugs: two \"equal\" objects could be placed in different buckets of a dict/HashMap, so obj in some_set could return False even when an \"equal\" object is present — a notoriously hard-to-debug class of issue.","correct":"Yes — this is the hash-equality contract: if a == b, then hash(a) == hash(b) must hold (the converse is not required — different objects can have the same hash, a collision).","distractors":["O(n*m) (for each of n nodes in the main tree, run an O(m) equality check against the target) is fine for typical interview constraints (n, m <= 1000, giving 10^6 operations).","A canonical key must be (1) identical for all items that should be grouped together, and (2) different (with very high probability) for items that shouldn't.","For *counting*, the hashmap stores prefix_value -> count of occurrences (a defaultdict(int)), because multiple earlier indices can have the same prefix value, each contributing one valid subarray."]},{"id":"cs_fundamentals/dsa_patterns#2a6759b2cb41","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"Why is the order of operations in the reversal loop critical — what happens if you swap the order of curr.next = prev and prev = curr?","answerFull":"The order must be: (1) save next_temp = curr.next, (2) curr.next = prev, (3) prev = curr, (4) curr = next_temp. If you did prev = curr *before* curr.next = prev, then curr.next = prev would set curr.next = curr (self-loop!) since prev and curr are now the same reference — corrupting the list with a cycle. The save-then-rewire-then-advance order is what prevents this.","correct":"The order must be: (1) save next_temp = curr.next, (2) curr.next = prev, (3) prev = curr, (4) curr = next_temp.","distractors":["Time is O(n) regardless — each node is visited a constant number of times (once during the \"check k nodes exist\" pass, once during the reversal pass).","Within the per-level loop, maintain a prev pointer (reset to None at the start of each level).","For each layer l from 0 to n//2 - 1, and each offset i within the layer, cycle four cells: top-left -> top-right -> bottom-right -> bottom-left -> top-left, using a temp."]},{"id":"cs_fundamentals/dsa_patterns#1e9c6cf3ec8a","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"What's the key relinking step in the recursive reversal, and why is head.next = None necessary?","answerFull":"The key step is head.next.next = head — this makes the node *after* head point back to head, effectively reversing that one link. head.next = None is necessary because, before this line, head.next still points *forward* to the node that now points back to head — without breaking this forward link, you'd have a 2-cycle (head <-> head.next). Setting head.next = None makes head correctly become the new *tail* of the reversed list (every node's .next should point toward the new head, except the new tail, whose .next is None).","correct":"The key step is head.next.next = head — this makes the node *after* head point back to head, effectively reversing that one link.","distractors":["Time is O(n) regardless — each node is visited a constant number of times (once during the \"check k nodes exist\" pass, once during the reversal pass).","Greedy makes each choice ONCE, in a fixed order, and never revisits it — the algorithm has a clear termination point and a provable bound (when correct).","The condition while fast and fast.next handles both: if head is None, fast = None, the loop body never executes, has_cycle returns False correctly."]},{"id":"cs_fundamentals/dsa_patterns#2ac6f2dcf4cd","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"In \"Reverse Linked List II\" (reverse a sub-range), why do you need a dummy node, and what are the three pointers you must reconnect?","answerFull":"A dummy node handles the edge case where left == 1 (the sub-range includes the original head) — without it, there's no \"node before the sub-range\" to update. The three reconnections after reversing the sub-range [left, right]: (1) before.next = new_head_of_reversed_segment (was the right-th node), (2) original_head_of_segment.next = curr (the now-tail of the reversed segment connects to whatever comes after right), and (3) implicitly, before itself doesn't move — it's the anchor point for reconnection 1.","correct":"A dummy node handles the edge case where left == 1 (the sub-range includes the original head) — without it, there's no \"node before the sub-range\" to update.","distractors":["The condition while fast and fast.next handles both: if head is None, fast = None, the loop body never executes, has_cycle returns False correctly.","The BST property is global, not just local: every node in the *entire* left subtree must be less than the current node, not just the immediate left child.","Without a dummy node, removing the actual head of the list (when n == length) requires special-casing — there's no \"previous\" node to update .next on."]},{"id":"cs_fundamentals/dsa_patterns#d4d25a98b694","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":4,"difficulty":"core","question":"In \"Reverse Nodes in k-Group,\" why do you need to \"check if k nodes exist\" before reversing?","answerFull":"The problem states leftover nodes (fewer than k) at the end should remain in their original order, NOT be reversed. If you reversed them and then had no way to \"un-reverse\" (or if the recursive call structure doesn't allow easy rollback), you'd produce an incorrect result. By checking *before* reversing (traverse k nodes first; if you run out, return head unchanged), you avoid ever reversing a partial group.","correct":"The problem states leftover nodes (fewer than k) at the end should remain in their original order, NOT be reversed.","distractors":["BFS visits nodes in non-decreasing depth order — the very first leaf it encounters is *guaranteed* to be at the minimum depth across the whole tree, so you can return immediately.","Kahn's algorithm is usually more intuitive to implement and explain in an interview because the in-degree-zero queue directly mirrors \"what can I do right now\" — and cycle detection is a single length check at the end.","Time is O(n) regardless — each node is visited a constant number of times (once during the \"check k nodes exist\" pass, once during the reversal pass)."]},{"id":"cs_fundamentals/dsa_patterns#f67f48cdcf62","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"How would you reverse a linked list using a stack (O(n) space) instead of the pointer-rewiring approach — and why might you mention this in an interview even if you implement the O(1) version?","answerFull":"Push every node (or value) onto a stack while traversing, then pop to build the reversed list (or overwrite values in place in reverse order). This is O(n) space but conceptually simpler and less error-prone. Mentioning it shows you understand the *space-time tradeoff explicitly* — you're choosing the O(1)-space pointer-rewiring approach deliberately, not because it's the only option, which is the kind of articulation L5 interviewers look for.","correct":"Push every node (or value) onto a stack while traversing, then pop to build the reversed list (or overwrite values in place in reverse order).","distractors":["find(x) recurses up to the root, then on the way back *down* the call stack, sets parent[node] = root for every node visited.","In an interview, rarely — O(1) space is almost always preferred when achievable, and the iterative version isn't meaningfully harder to write once memorized.","A deque supports O(1) push/pop from both ends, vs. O(log V) for a heap."]},{"id":"cs_fundamentals/dsa_patterns#35980e3399ac","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"For \"Reorder List\" (L0->L1->...->Ln becomes L0->Ln->L1->Ln-1->...), what are the three sub-routines and in what order are they applied?","answerFull":"(1) Find the middle using fast/slow pointers — splits the list into two halves. (2) Reverse the second half using the standard prev/curr/next_temp triple. (3) Merge the two halves alternately — a two-pointer \"zip\" that interleaves nodes from the first half and the (now-reversed) second half. Each sub-routine is O(n)/O(1), and they compose to O(n) time, O(1) space overall — a great example of pattern composition (see the Pattern Interaction Map in dsa_patterns/README.md §8).","correct":"(1) Find the middle using fast/slow pointers — splits the list into two halves.","distractors":["Push every node (or value) onto a stack while traversing, then pop to build the reversed list (or overwrite values in place in reverse order).","A node is appended to the post-order list only after *all* nodes reachable from it have been appended.","This follows from the algebra: at the meeting point, slow has traveled L + k steps (L = distance from head to cycle start, k = distance from cycle start to meeting point along the cycle), and fast has traveled 2(L+k)."]},{"id":"cs_fundamentals/dsa_patterns#b0a1f03098ac","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"Is recursive linked-list reversal ever preferable to iterative, despite the O(n) space cost?","answerFull":"In an interview, rarely — O(1) space is almost always preferred when achievable, and the iterative version isn't meaningfully harder to write once memorized. However, recursive reversal can be clearer when reversal is *combined* with other recursive logic already operating on the list (e.g., some tree-to-linked-list conversion algorithms), where the call stack is \"already there\" for other reasons and adding reversal logic to it avoids a second pass.","correct":"In an interview, rarely — O(1) space is almost always preferred when achievable, and the iterative version isn't meaningfully harder to write once memorized.","distractors":["For minimize DP (Coin Change), 0 looks like \"already the best possible value\" to min(), so nothing can ever improve it — 0 must mean \"not yet computed,\" requiring +infinity.","O(n*m) (for each of n nodes in the main tree, run an O(m) equality check against the target) is fine for typical interview constraints (n, m <= 1000, giving 10^6 operations).","Because left only ever increases — it is never reset to a smaller value."]},{"id":"cs_fundamentals/dsa_patterns#70590727f027","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":9,"difficulty":"intermediate","question":"What's the time complexity of reverse_k_group, and does the recursion affect it?","answerFull":"Time is O(n) regardless — each node is visited a constant number of times (once during the \"check k nodes exist\" pass, once during the reversal pass). The recursion doesn't add asymptotic time complexity; it's O(n/k) recursive calls, each doing O(k) work, totaling O(n). Space, however, IS affected: O(n/k) call-stack frames for the recursive version, vs O(1) for a fully iterative version (which would use an explicit loop over groups instead of recursive calls).","correct":"Time is O(n) regardless — each node is visited a constant number of times (once during the \"check k nodes exist\" pass, once during the reversal pass).","distractors":["For \"top-k frequent\" problems, you need O(u) space for the frequency map (u = number of unique elements, u <= n) regardless of approach — that's unavoidable since you must count occurrences.","WHITE = never visited; GRAY = currently on the DFS call stack (an ancestor of the current node in the DFS tree); BLACK = fully processed (this node and all its descendants are done).","Each recursive call branches into multiple choices, and the recursion depth equals the solution length — so the total number of leaves in the search tree is O(branching_factor ^ depth), which is 2^n (subsets) or n!"]},{"id":"cs_fundamentals/dsa_patterns#904fc79b773b","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"What if I genuinely don't recognize the pattern within the first 2 minutes?","answerFull":"Say so, and start the brute force. \"I don't see an immediate optimization — let me start with the brute force and look for the bottleneck as I implement it\" is a completely valid statement, and often the brute force itself reveals the optimization (you'll notice you're recomputing something). Silence while you privately search for the \"right\" pattern produces zero signal; a stated brute force produces a working baseline AND signal. How much should I talk while actively typing code? Enough that the interviewer is never unsure what you're doing for more than ~30 seconds. You don't need to narrate every keystroke — narrate at the level of \"now I'm handling the case where the heap exceeds size k\" (one sentence per logical step), not \"now I'm typing h-e-a-p-q-dot-h-e-a-p-p-u-s-h.\"","correct":"Say so, and start the brute force.","distractors":["For small, fixed k (say k <= 4), a linear scan over k pointers to find the minimum each step is O(N * k) — simpler code, and for small constant k, O(N * k) and O(N log k) are both effectively O(N).","Read the problem statement's definition of \"adjacent\" — it will explicitly say \"horizontally or vertically adjacent\" (4-directional, the overwhelming majority of problems) or mention diagonal connections (8-directional).","When you don't need subsets in any particular order and want to avoid Python recursion overhead (function call costs add up across 2^n calls)."]},{"id":"cs_fundamentals/dsa_patterns#c7435c366926","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"What if the interviewer's hint contradicts my current approach?","answerFull":"Repeat the hint back in your own words to confirm you understood it, then re-evaluate: \"So you're suggesting [restated hint] — that would mean [implication for my approach]. Let me reconsider...\" If, after reconsidering, you still believe your approach is correct, it's fine to say so with a brief justification — but check first, since most hints are intentional redirections toward something the interviewer needs to observe.","correct":"Repeat the hint back in your own words to confirm you understood it, then re-evaluate: \"So you're suggesting [restated hint] — that would mean [implication for my approach].","distractors":["For \"top-k frequent\" problems, you need O(u) space for the frequency map (u = number of unique elements, u <= n) regardless of approach — that's unavoidable since you must count occurrences.","For minimize DP (Coin Change), 0 looks like \"already the best possible value\" to min(), so nothing can ever improve it — 0 must mean \"not yet computed,\" requiring +infinity.","Floyd's (tortoise and hare) is the canonical O(1)-space approach."]},{"id":"cs_fundamentals/dsa_patterns#2c94a8a2b85b","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"Is it OK to ask the interviewer to confirm my complexity analysis?","answerFull":"Yes, briefly — \"So that's O(n log k) time, O(n) space — does that match what you'd expect?\" is a good wrap-up question. Don't ask it as \"is that right?\" with no analysis attached; ask it AFTER giving your justified analysis, as a final confirmation/segue, not as a substitute for the analysis.","correct":"Yes, briefly — \"So that's O(n log k) time, O(n) space — does that match what you'd expect?\" is a good wrap-up question.","distractors":["Repeat the hint back in your own words to confirm you understood it, then re-evaluate: \"So you're suggesting [restated hint] — that would mean [implication for my approach].","Treat it as a fresh Match step on the SAME problem: \"With a stream, I can't build the full frequency map upfront.","Floyd's (tortoise and hare) is the canonical O(1)-space approach."]},{"id":"cs_fundamentals/dsa_patterns#3a4a2355c4f5","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"How do I handle a follow-up like \"what if the input doesn't fit in memory\" or \"what if this needs to run on a stream\"?","answerFull":"Treat it as a fresh Match step on the SAME problem: \"With a stream, I can't build the full frequency map upfront. For approximate top-k over a stream, a Count-Min Sketch combined with a heap is the standard approach — it trades exact counts for bounded memory.\" You're not expected to implement a Count-Min Sketch live, but naming it (and why it solves the new constraint) is a strong signal. See hashing_patterns.md §6 for related approximate-counting structures.","correct":"Treat it as a fresh Match step on the SAME problem: \"With a stream, I can't build the full frequency map upfront.","distractors":["After slow and fast meet (any meeting point inside the cycle works), keep fast (or either pointer) fixed and advance just one pointer step by step, counting steps, until it returns to the same node.","When the problem is explicitly \"find the k-th largest element\" (singular, LC 215) on a static array with a follow-up \"can you do better than O(n log n)?\" — quickselect's average O(n) is the textbook answer.","State it calmly and specifically: \"I see the issue — on line [X], I'm using < but this should be <= to handle the case where [specific scenario from my edge-case example]."]},{"id":"cs_fundamentals/dsa_patterns#117ac7023e51","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":8,"difficulty":"intermediate","question":"What's the right way to test my code at the end, given limited time?","answerFull":"Hand-trace the 1-2 examples from the Understand phase, maintaining actual variable values as you go (as in §5's Review step) — this is faster than writing assert statements and demonstrates the same rigor. If significant time remains (see §7 \"When You Finish Early\"), THEN write 1-2 actual test cases as code, especially for edge cases (empty input, single element).","correct":"Hand-trace the 1-2 examples from the Understand phase, maintaining actual variable values as you go (as in §5's Review step) — this is faster than writing assert statements and demonstrates the same rigor.","distractors":["Kahn's algorithm is usually more intuitive to implement and explain in an interview because the in-degree-zero queue directly mirrors \"what can I do right now\" — and cycle detection is a single length check at the end.","Change dfs from a None-returning side-effecting function to one that *returns* 1 + dfs(up) + dfs(down) + dfs(left) + dfs(right) (with the same \"sink before recurse\" rule, returning 0 for out-of-bounds/water cells).","LC 55 only asks \"can you reach the end?\" — a single farthest variable suffices, and you fail fast if i > farthest (a gap you can never cross)."]},{"id":"cs_fundamentals/dsa_patterns#7928081274d8","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":9,"difficulty":"intermediate","question":"How do I recover gracefully from a bug found during Review?","answerFull":"State it calmly and specifically: \"I see the issue — on line [X], I'm using < but this should be <= to handle the case where [specific scenario from my edge-case example]. Let me fix that.\" Then re-trace ONLY the part of the example affected by the fix, not the whole trace from scratch (unless the fix changes earlier logic too). Finding your own bug during Review, calmly, is a POSITIVE signal — it's the testing/verification skill in §2's rubric in action.","correct":"State it calmly and specifically: \"I see the issue — on line [X], I'm using < but this should be <= to handle the case where [specific scenario from my edge-case example].","distractors":["Use lo <= hi (with hi = mid - 1 / lo = mid + 1) when you're searching for an *exact match* and want to detect \"not found\" (loop exits with lo > hi).","Hand-trace the 1-2 examples from the Understand phase, maintaining actual variable values as you go (as in §5's Review step) — this is faster than writing assert statements and demonstrates the same rigor.","lowest_common_ancestor returns non-None from a subtree iff that subtree contains p, q, or (already-found) their LCA."]},{"id":"cs_fundamentals/dsa_patterns#c1b9706657c6","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"Why a heap of size k instead of just concatenating all lists and sorting?","answerFull":"Concatenating and sorting is O(N log N) and throws away the fact that each input is *already sorted*. A heap of size k exploits that: at any moment, the global minimum among all unconsumed elements must be the minimum of the k \"frontier\" elements (one per list) — so you never need to compare against elements deeper in any list. This gives O(N log k), which is asymptotically better whenever k < N.","correct":"Concatenating and sorting is O(N log N) and throws away the fact that each input is *already sorted*.","distractors":["A min-heap of *all* n points sorted by distance and popping k times is O(n + k log n) — fine, but it processes and heapifies all n points upfront.","Seed a min-heap with the first element of each row (or just row 0's first min(k, n) elements with pointers), each tagged with its row and column.","Fixed-size: a single for loop where right and left = right - k + 1 move in lockstep — no inner loop needed, just \"add nums[right], remove nums[right-k]\" each iteration."]},{"id":"cs_fundamentals/dsa_patterns#dc708d2ffae8","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"Why does pushing (node.val, node) into a heap crash on duplicate values, and how do you fix it?","answerFull":"heapq compares tuples element-by-element; if the first elements (node.val) are equal, it falls through to comparing the second elements — here, ListNode objects, which have no lt defined, raising TypeError. The fix is to insert a unique, always-comparable tiebreaker (commonly the source list's index, or a monotonically increasing counter) as the *second* tuple element: (node.val, i, node). Ties on node.val then resolve via i (always distinct), and Python never needs to compare the ListNodes themselves.","correct":"heapq compares tuples element-by-element; if the first elements (node.val) are equal, it falls through to comparing the second elements — here, ListNode objects, which have no lt defined, raising TypeError.","distractors":["Seed a min-heap with the first element of each row (or just row 0's first min(k, n) elements with pointers), each tagged with its row and column.","prefix[0] = 0 represents the sum of zero elements — it's the identity element for addition.","Skip it during heap seeding — only push frontier elements for non-empty sources (if node: / if arr: in the templates above)."]},{"id":"cs_fundamentals/dsa_patterns#4dcab75c1f3c","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"For k = 2, why is a two-pointer merge better than a 2-element heap?","answerFull":"A 2-element heap still pays O(log 2) = O(1) overhead per operation plus the constant-factor cost of heap push/pop machinery (list operations, sift-up/down). A direct if a[i] <= b[j] comparison is a single branch with no auxiliary data structure — strictly cheaper in practice, and for Merge Sorted Array (LC 88) it also enables an in-place merge (writing from the back) that a heap-based approach can't easily replicate.","correct":"A 2-element heap still pays O(log 2) = O(1) overhead per operation plus the constant-factor cost of heap push/pop machinery (list operations, sift-up/down).","distractors":["The heap holds exactly one element per *currently active* source — never more than k regardless of how large each individual list is.","The heap doesn't represent \"the answer\" directly; it represents your *current best guess* at the top-k, and its root is the weakest member of that guess — the element most likely to be displaced by a better candidate.","Skip it during heap seeding — only push frontier elements for non-empty sources (if node: / if arr: in the templates above)."]},{"id":"cs_fundamentals/dsa_patterns#ed113615534d","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"How is \"the n-th ugly number\" a k-way merge problem when there's only one input array?","answerFull":"There is no input array — the \"lists\" are *generated on the fly* as multiples of the result sequence itself. Maintain three pointers p2, p3, p5 into the growing result array; the next ugly number is min(result[p2]*2, result[p3]*3, result[p5]*5). Whichever pointer(s) produced that minimum advance by one. This is k-way merge with k=3 streams where each stream's \"next element\" is computed lazily instead of read from a pre-built array.","correct":"There is no input array — the \"lists\" are *generated on the fly* as multiples of the result sequence itself.","distractors":["At each step, take the two least-frequent remaining nodes/symbols and merge them into a new node whose frequency is their sum, inserting it back into a min-heap.","It means the thing you binary search over is not the input array but a *range of possible output values*.","The recursive calls share a *single* mutable path (or used array) by reference — there's only one list object, repeatedly appended to and read from at different points in the recursion."]},{"id":"cs_fundamentals/dsa_patterns#f7701f777cd9","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"Kth Smallest in a Sorted Matrix — heap-based partial merge vs. binary search on the answer: when is each better?","answerFull":"The heap approach (kth_smallest_in_sorted_matrix) is O(k log n) — great when k is small relative to n^2. Binary search on the value range is O(n log(max-min)) (using a per-row binary search to count elements <= mid) — its cost is *independent of k*, so it wins when k is large (close to n^2). If asked \"what if k is n^2 / 2?\", binary search on the answer is the better follow-up.","correct":"The heap approach (kth_smallest_in_sorted_matrix) is O(k log n) — great when k is small relative to n^2.","distractors":["The heap doesn't represent \"the answer\" directly; it represents your *current best guess* at the top-k, and its root is the weakest member of that guess — the element most likely to be displaced by a better candidate.","Each insertion requires finding the correct position (binary search on starts, O(log n)) and checking for overlap with neighbors (O(1)), but inserting into a Python list is O(n) due to shifting elements.","At each step, take the two least-frequent remaining nodes/symbols and merge them into a new node whose frequency is their sum, inserting it back into a min-heap."]},{"id":"cs_fundamentals/dsa_patterns#6edff9d28418","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"What if one of the k input lists/arrays is empty?","answerFull":"Skip it during heap seeding — only push frontier elements for non-empty sources (if node: / if arr: in the templates above). An empty source contributes nothing to the heap and is naturally never selected; no special casing is needed beyond the initial guard.","correct":"Skip it during heap seeding — only push frontier elements for non-empty sources (if node: / if arr: in the templates above).","distractors":["If k == n, the heap-of-size-k approach degenerates to holding the entire input — equivalent to sorting, so O(n log n); prefer sorting directly in that case.","A 2-element heap still pays O(log 2) = O(1) overhead per operation plus the constant-factor cost of heap push/pop machinery (list operations, sift-up/down).","Seed a min-heap with the first element of each row (or just row 0's first min(k, n) elements with pointers), each tagged with its row and column."]},{"id":"cs_fundamentals/dsa_patterns#8404e3aa5bc3","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":8,"difficulty":"intermediate","question":"Why is the heap's space complexity O(k) and not O(N)?","answerFull":"The heap holds exactly one element per *currently active* source — never more than k regardless of how large each individual list is. Each pop immediately triggers at most one push (the popped source's next element), so the heap size is invariant at <= k throughout. The O(N) cost is for the *output*, not the heap.","correct":"The heap holds exactly one element per *currently active* source — never more than k regardless of how large each individual list is.","distractors":["Heap: O(n log k) time, O(k) space, works on streams, gives you all k elements (unordered).","The heap doesn't represent \"the answer\" directly; it represents your *current best guess* at the top-k, and its root is the weakest member of that guess — the element most likely to be displaced by a better candidate.","Seed a min-heap with the first element of each row (or just row 0's first min(k, n) elements with pointers), each tagged with its row and column."]},{"id":"cs_fundamentals/dsa_patterns#4416f18d1bad","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":11,"difficulty":"advanced","question":"Is a heap always required, or can k-way merge be done with k pointers and a linear scan?","answerFull":"For small, fixed k (say k <= 4), a linear scan over k pointers to find the minimum each step is O(N * k) — simpler code, and for small constant k, O(N * k) and O(N log k) are both effectively O(N). The heap only provides an asymptotic win once k is large enough that log k is meaningfully cheaper than a linear scan over k candidates (roughly k > 8 in practice).","correct":"For small, fixed k (say k <= 4), a linear scan over k pointers to find the minimum each step is O(N * k) — simpler code, and for small constant k, O(N * k) and O(N log k) are both effectively O(N).","distractors":["find(x) recurses up to the root, then on the way back *down* the call stack, sets parent[node] = root for every node visited.","At each step, take the two least-frequent remaining nodes/symbols and merge them into a new node whose frequency is their sum, inserting it back into a min-heap.","Seed a min-heap with the first element of each row (or just row 0's first min(k, n) elements with pointers), each tagged with its row and column."]},{"id":"cs_fundamentals/dsa_patterns#396f219a9a09","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"Why is matrix manipulation a separate pattern from graph traversal if both operate on a grid?","answerFull":"Because the *mechanism* differs. Graph traversal explores adjacency/reachability and needs a frontier (queue/stack) plus a visited-set; matrix manipulation reads or rewrites cells at computed coordinates with no notion of \"neighbor reachable from here,\" frequently in O(1) extra space. \"Number of islands\" is graph traversal; \"rotate the image in place\" is matrix manipulation. The giveaway is the O(1)-space follow-up — you cannot do connectivity in O(1) space, but you can rotate, spiral, and zero in O(1) space via coordinate arithmetic.","correct":"Because the *mechanism* differs.","distractors":["Running a separate DFS per word costs O(words · rows·cols·4^L) — the grid is re-explored from scratch for every word.","Rebalancing moves elements between heaps based purely on counts (len(left) vs len(right)) to restore the size invariant — it has no mechanism to verify max(left) <= min(right).","Because the guard condition grid[r][c] != \"1\" is the *only* thing that stops recursion, and it's checked at the *top* of each call using the grid's current state."]},{"id":"cs_fundamentals/dsa_patterns#da97055ddaeb","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"How do you rotate an n×n matrix 90° clockwise in place, and why does transpose + reverse work?","answerFull":"Transpose swaps a[i][j] with a[j][i], mirroring across the main diagonal; reversing each row then sends column j to column n-1-j. Composing a diagonal mirror with a horizontal mirror yields a 90° rotation. For counter-clockwise, transpose then reverse the *order of the rows* (vertical flip) instead. Both are O(n²) time, O(1) space, and avoid the four-way corner-cycle bookkeeping that is easy to get wrong under pressure.","correct":"Transpose swaps a[i][j] with a[j][i], mirroring across the main diagonal; reversing each row then sends column j to column n-1-j.","distractors":["The key step is head.next.next = head — this makes the node *after* head point back to head, effectively reversing that one link.","After walking the top row and right column you shrink top and right.","Push every node (or value) onto a stack while traversing, then pop to build the reversed list (or overwrite values in place in reverse order)."]},{"id":"cs_fundamentals/dsa_patterns#998eec495980","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"In \"Set Matrix Zeroes,\" what breaks if you zero rows/columns as you scan, and how do you fix it to O(1) space?","answerFull":"Zeroing live corrupts cells you haven't read, so the zeros cascade and over-zero the matrix (see §8). The two-pass fix detects first, applies second. To reach O(1) space, use row 0 and column 0 as the marker arrays: a 0 at (r,c) sets matrix[r][0]=0 and matrix[0][c]=0. Handle the first row and first column separately (with two boolean flags) because they double as storage and as real data.","correct":"Zeroing live corrupts cells you haven't read, so the zeros cascade and over-zero the matrix (see §8).","distractors":["Build a 2D prefix array where prefix[i][j] = sum of all cells (r, c) with r < i and c < j — i.e., the sum of the submatrix from (0,0) to (i-1, j-1).","in_degree[v] must count *every* edge (u, v) in the input — initialize it to all zeros, then for each edge (u, v), do in_degree[v] += 1.","Change dfs from a None-returning side-effecting function to one that *returns* 1 + dfs(up) + dfs(down) + dfs(left) + dfs(right) (with the same \"sink before recurse\" rule, returning 0 for out-of-bounds/water cells)."]},{"id":"cs_fundamentals/dsa_patterns#7f206501aa66","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":4,"difficulty":"core","question":"How do you traverse a matrix diagonally, and what's the key indexing fact?","answerFull":"All cells on the same anti-diagonal share a constant r + c (and main diagonals share constant r - c). Bucket cells by r + c into groups 0 .. m+n-2; within each group, emit top-to-bottom or bottom-to-top. For LC 498's zig-zag, reverse every other diagonal — e.g., emit even-sum diagonals upward and odd-sum diagonals downward.","correct":"All cells on the same anti-diagonal share a constant r + c (and main diagonals share constant r - c).","distractors":["Main-diagonal reflection (transpose) swaps a[i][j] with a[j][i].","Read the problem statement's definition of \"adjacent\" — it will explicitly say \"horizontally or vertically adjacent\" (4-directional, the overwhelming majority of problems) or mention diagonal connections (8-directional).","Zeroing live corrupts cells you haven't read, so the zeros cascade and over-zero the matrix (see §8)."]},{"id":"cs_fundamentals/dsa_patterns#4d72e5e92998","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"Explain the 2-bit encoding trick for Game of Life.","answerFull":"You must compute the next generation from the current one without a second matrix. Reserve bit 0 for the current state and bit 1 for the next state. In pass 1, compute each cell's next state by counting neighbors using only cell & 1 (the untouched current bit), and write the next state into bit 1 via cell |= next_state << 1. In pass 2, do cell >>= 1 everywhere to commit. This is the in-place answer to the standard follow-up; the general principle is \"encode two states per cell when you must read the old while writing the new.\"","correct":"You must compute the next generation from the current one without a second matrix.","distractors":["Kahn's algorithm is usually more intuitive to implement and explain in an interview because the in-degree-zero queue directly mirrors \"what can I do right now\" — and cycle detection is a single length check at the end.","Without a dummy node, removing the actual head of the list (when n == length) requires special-casing — there's no \"previous\" node to update .next on.","Because the inner loop both pops nodes from the current level and pushes their children (next level) onto the same queue."]},{"id":"cs_fundamentals/dsa_patterns#f09228aa079e","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"Why does the spiral template need those two if guards, and what bug do they prevent?","answerFull":"After walking the top row and right column you shrink top and right. The guards if top <= bottom and if left <= right ensure the bottom row and left column still exist before walking them. Without the guards, a single remaining row or column (common in non-square or odd-dimension matrices) gets walked twice and elements are emitted in duplicate. Concretely, for a single-row matrix the bottom-row pass would re-emit the row you already consumed.","correct":"After walking the top row and right column you shrink top and right.","distractors":["For \"top-k frequent\" problems, you need O(u) space for the frequency map (u = number of unique elements, u <= n) regardless of approach — that's unavoidable since you must count occurrences.","Transpose swaps a[i][j] with a[j][i], mirroring across the main diagonal; reversing each row then sends column j to column n-1-j.","Preorder (node, left, right) when you need to process a node *before* its children — serialization (the root must be written first to be read first), or copying/cloning a tree top-down."]},{"id":"cs_fundamentals/dsa_patterns#380b9ba7a095","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":8,"difficulty":"intermediate","question":"What's the time and space complexity of these problems, and can space always be O(1)?","answerFull":"Time is O(m·n) — you must touch every cell at least once, so this is optimal. Extra space is O(1) for in-place transforms (rotate, set-zeroes with marker rows, Game of Life), since the grid stores its own bookkeeping. Read-only traversals (spiral, diagonal) are O(1) *auxiliary* but produce an O(m·n) output list, which is required, not overhead. The only time you truly can't hit O(1) extra space is when the problem fundamentally needs a separate structure — but most \"rewrite the grid\" problems can.","correct":"Time is O(m·n) — you must touch every cell at least once, so this is optimal.","distractors":["In an interview, rarely — O(1) space is almost always preferred when achievable, and the iterative version isn't meaningfully harder to write once memorized.","The BST property is global, not just local: every node in the *entire* left subtree must be less than the current node, not just the immediate left child.","For \"top-k frequent\" problems, you need O(u) space for the frequency map (u = number of unique elements, u <= n) regardless of approach — that's unavoidable since you must count occurrences."]},{"id":"cs_fundamentals/dsa_patterns#29ac77141105","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":9,"difficulty":"intermediate","question":"How do you map a 1D index to 2D coordinates and back, and where does that matter?","answerFull":"For a matrix with cols columns, cell (r, c) is linear index r * cols + c; inversely, r = k // cols and c = k % cols. This underpins flatten-and-binary-search (LC 74) and any \"treat the grid as a flat array\" trick. The analogous decomposition (r // 3) * 3 + (c // 3) maps a Sudoku cell to its 3×3 box id — the same index-arithmetic idea applied to sub-blocks.","correct":"For a matrix with cols columns, cell (r, c) is linear index r * cols + c; inversely, r = k // cols and c = k % cols.","distractors":["For \"top-k frequent\" problems, you need O(u) space for the frequency map (u = number of unique elements, u <= n) regardless of approach — that's unavoidable since you must count occurrences.","Running a separate DFS per word costs O(words · rows·cols·4^L) — the grid is re-explored from scratch for every word.","Checking, from each cell, whether water can reach both oceans would require a traversal *from* that cell — O(rows · cols) work per cell, O((rows·cols)²) total."]},{"id":"cs_fundamentals/dsa_patterns#9b7bdfdf23e5","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":10,"difficulty":"intermediate","question":"How would you rotate the four corners of a layer in a single pass instead of transpose + reverse?","answerFull":"For each layer l from 0 to n//2 - 1, and each offset i within the layer, cycle four cells: top-left -> top-right -> bottom-right -> bottom-left -> top-left, using a temp. The index map for clockwise is a[l][l+i] <- a[n-1-l-i][l] <- a[n-1-l][n-1-l-i] <- a[l+i][n-1-l] <- (saved top-left). It's O(1) space like transpose+reverse but touches each cell exactly once; most engineers find transpose+reverse easier to write correctly, so state both and pick the clearer one aloud.","correct":"For each layer l from 0 to n//2 - 1, and each offset i within the layer, cycle four cells: top-left -> top-right -> bottom-right -> bottom-left -> top-left, using a temp.","distractors":["A decreasing stack (top-to-bottom values decrease, i.e., pop when a *larger* element arrives) is used to find next greater elements — when you pop, the new element is the popped one's \"next greater\".","Each child's required candy count depends on comparisons with BOTH neighbors simultaneously, but a single left-to-right (or right-to-left) pass can only enforce one direction's constraint at a time.","Change the target size ratio: instead of keeping left and right within one element of equal size, keep len(left) ~= p% * n and len(right) ~= (100-p)% * n."]},{"id":"cs_fundamentals/dsa_patterns#a618d8eb2473","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":11,"difficulty":"advanced","question":"What's the difference between reflecting across the main diagonal versus the anti-diagonal?","answerFull":"Main-diagonal reflection (transpose) swaps a[i][j] with a[j][i]. Anti-diagonal reflection swaps a[i][j] with a[n-1-j][n-1-i]. They are different mirror lines, and composing each with a row/column reverse yields different rotations — main-diagonal + row-reverse gives 90° CW, while anti-diagonal + row-reverse gives 90° CCW. Naming the exact mirror prevents the classic \"rotated the wrong direction\" bug.","correct":"Main-diagonal reflection (transpose) swaps a[i][j] with a[j][i].","distractors":["Transpose swaps a[i][j] with a[j][i], mirroring across the main diagonal; reversing each row then sends column j to column n-1-j.","All cells on the same anti-diagonal share a constant r + c (and main diagonals share constant r - c).","Not easily — the swaps have sequential dependencies (the value swapped INTO position i must then itself be checked/placed, creating a chain)."]},{"id":"cs_fundamentals/dsa_patterns#665d56888d85","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"What's the difference in sort key between \"Merge Intervals\" and \"Non-overlapping Intervals\" (LC 435)?","answerFull":"\"Merge Intervals\" sorts by start time — you're combining overlapping ranges into larger ranges. \"Non-overlapping Intervals\" (a greedy problem) sorts by end time — you're *selecting* a maximum-size subset of mutually non-overlapping intervals, and greedily picking the interval that ends earliest leaves the most room for future selections (an exchange-argument proof). Same data shape, different sort key, different algorithm family.","correct":"\"Merge Intervals\" sorts by start time — you're combining overlapping ranges into larger ranges.","distractors":["Flatten all employees' intervals into a single list, sort by start time, and run the standard merge to get the union of all busy times.","This depends on whether intervals are treated as closed ([1,3] includes the point 3) or half-open ([1,3) excludes 3).","Because the goal is to keep as many intervals as possible, and the interval that *finishes earliest* leaves the maximum remaining timeline for future intervals — regardless of when it starts."]},{"id":"cs_fundamentals/dsa_patterns#d9d5f121dbf2","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"How do you decide whether overlapping should use < or <= when comparing current.end and next.start?","answerFull":"This depends on whether intervals are treated as closed ([1,3] includes the point 3) or half-open ([1,3) excludes 3). If closed, [1,3] and [3,5] share the point 3 and are typically considered overlapping/mergeable → use <=. If half-open, they don't share any point → use <. Real-world calendar systems usually treat meeting times as half-open ([start, end)) so a meeting ending at 3:00 and one starting at 3:00 don't conflict — but always clarify the problem's convention rather than assuming.","correct":"This depends on whether intervals are treated as closed ([1,3] includes the point 3) or half-open ([1,3) excludes 3).","distractors":["When arr[lo] == arr[mid] == arr[hi], you cannot tell which half is sorted (both could look \"flat\" while the rotation point hides inside either).","Flatten all employees' intervals into a single list, sort by start time, and run the standard merge to get the union of all busy times.","In principle, you could model overlapping intervals as a graph (edge between intervals i and j if they overlap) and use union_find.md to find connected components, then merge each component."]},{"id":"cs_fundamentals/dsa_patterns#f496441cbde4","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":4,"difficulty":"core","question":"For \"Insert Interval,\" why is the three-phase approach O(n) while \"append and re-sort\" is O(n log n) — and when would the O(n log n) approach still be acceptable?","answerFull":"The three-phase approach exploits the precondition that the input is *already sorted and non-overlapping* — a single linear scan suffices. \"Append and re-sort\" treats the input as arbitrary, paying O(n log n) for the sort. The O(n log n) approach is acceptable if (a) the interviewer doesn't emphasize the pre-sorted constraint, (b) n is small enough that the constant-factor difference doesn't matter, or (c) code simplicity is prioritized over asymptotic optimality — but you should *mention* the O(n) alternative to demonstrate you noticed the precondition.","correct":"The three-phase approach exploits the precondition that the input is *already sorted and non-overlapping* — a single linear scan suffices.","distractors":["If k == n, the heap-of-size-k approach degenerates to holding the entire input — equivalent to sorting, so O(n log n); prefer sorting directly in that case.","A 2-element heap still pays O(log 2) = O(1) overhead per operation plus the constant-factor cost of heap push/pop machinery (list operations, sift-up/down).","Concatenating and sorting is O(N log N) and throws away the fact that each input is *already sorted*."]},{"id":"cs_fundamentals/dsa_patterns#1ca54e3fea69","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"How does the \"minimum meeting rooms\" two-pointer (separate sorted starts/ends) approach work, intuitively?","answerFull":"Think of it as a timeline sweep: sort all start times and all end times independently. Walk through time using two pointers — whenever the next event is a \"start\" that occurs *before* the next \"end\", a new room is needed (increment rooms, track the max). Whenever the next event is an \"end\" (or a start that's not before the next end), a room frees up (decrement rooms, advance the end pointer). The maximum value rooms reaches during this sweep is the answer — it represents the peak concurrent meeting count.","correct":"Think of it as a timeline sweep: sort all start times and all end times independently.","distractors":["After slow and fast meet (any meeting point inside the cycle works), keep fast (or either pointer) fixed and advance just one pointer step by step, counting steps, until it returns to the same node.","Default to attempting the greedy approach first IF you can state the greedy rule in one sentence AND immediately think of a potential counter-example to test.","The heap approach (kth_smallest_in_sorted_matrix) is O(k log n) — great when k is small relative to n^2."]},{"id":"cs_fundamentals/dsa_patterns#47a64f4b3807","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"What's the heap-based alternative for \"minimum meeting rooms,\" and when might you prefer it?","answerFull":"Sort intervals by start time. Maintain a min-heap of \"end times of currently occupied rooms.\" For each interval, if the heap's minimum end time is <= current.start (the earliest-freeing room is free by now), pop it (reuse that room) and push the new end time; otherwise, push the new end time without popping (allocate a new room). The heap's final size is the answer. This is more intuitive for some people (it directly models \"rooms\" as heap entries) and generalizes more easily if rooms have different *types* or *costs* (you could use a priority based on room cost, not just end time).","correct":"Sort intervals by start time.","distractors":["Flatten all employees' intervals into a single list, sort by start time, and run the standard merge to get the union of all busy times.","\"Merge Intervals\" sorts by start time — you're combining overlapping ranges into larger ranges.","This follows from the algebra: at the meeting point, slow has traveled L + k steps (L = distance from head to cycle start, k = distance from cycle start to meeting point along the cycle), and fast has traveled 2(L+k)."]},{"id":"cs_fundamentals/dsa_patterns#898ae5cce1a9","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"How would \"Employee Free Time\" combine merge-intervals with another technique?","answerFull":"Flatten all employees' intervals into a single list, sort by start time, and run the standard merge to get the union of all busy times. Then, the \"free time\" is the gaps between consecutive merged intervals — for each pair of adjacent merged intervals (a, b), if a.end < b.start, then [a.end, b.start] is a free-time slot common to everyone. This is \"merge intervals\" followed by a \"find gaps\" post-processing pass.","correct":"Flatten all employees' intervals into a single list, sort by start time, and run the standard merge to get the union of all busy times.","distractors":["path is a single mutable list object, repeatedly appended to and popped from throughout the entire recursion.","Time is O(n) regardless — each node is visited a constant number of times (once during the \"check k nodes exist\" pass, once during the reversal pass).","Think of it as a timeline sweep: sort all start times and all end times independently."]},{"id":"cs_fundamentals/dsa_patterns#be61bccae99d","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":8,"difficulty":"intermediate","question":"What's the \"delta array\" / \"difference array\" technique used in \"Car Pooling,\" and how does it relate to prefix sums?","answerFull":"For each interval [start, end, capacity_change], record +capacity_change at index start and -capacity_change at index end in a delta array. Then compute the prefix sum of this delta array — prefix_sum[i] gives the \"net effect\" (e.g., number of passengers) at point i. This converts \"does any point exceed capacity across all intervals\" into a single O(n + range) prefix-sum computation, avoiding O(n^2) pairwise overlap checks. It's a direct application of prefix_sum.md to interval data.","correct":"For each interval [start, end, capacity_change], record +capacity_change at index start and -capacity_change at index end in a delta array.","distractors":["Maintain a dynamic array (list) for O(1) random access (getRandom picks random.choice by index) and a hashmap value -> index_in_array.","Without this check, every number triggers an inner \"count forward\" loop, and for a single long run of length n, this sums to O(n^2) (the 1st number counts n steps, the 2nd counts n-1, etc.).","Think of it as a timeline sweep: sort all start times and all end times independently."]},{"id":"cs_fundamentals/dsa_patterns#15053f5a6579","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":9,"difficulty":"intermediate","question":"Can \"Merge Intervals\" be done without sorting, e.g., using a Union-Find structure?","answerFull":"In principle, you could model overlapping intervals as a graph (edge between intervals i and j if they overlap) and use union_find.md to find connected components, then merge each component. But determining *which* intervals overlap without sorting still requires O(n^2) pairwise checks (or a sweep-line, which itself requires sorting events) — so sorting is effectively unavoidable for an efficient solution. Union-Find doesn't eliminate the need to sort; it would only be useful if \"overlap\" relationships were given to you directly (e.g., as a graph), which is not the typical problem framing.","correct":"In principle, you could model overlapping intervals as a graph (edge between intervals i and j if they overlap) and use union_find.md to find connected components, then merge each component.","distractors":["If you're given the *whole* graph upfront and need to answer connectivity questions once (e.g., \"how many connected components does this static graph have?\"), DFS/BFS is simpler to write and equally efficient — O(V+E).","For an undirected edge (u, v): if find(u) == find(v) *before* you union them, then u and v were already connected via some other path — adding edge (u, v) would create a cycle.","self.count is the number of disjoint sets (connected components) currently."]},{"id":"cs_fundamentals/dsa_patterns#2e94f0d8a21f","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":11,"difficulty":"advanced","question":"What's the time complexity if you need to repeatedly insert intervals one at a time (online), maintaining a merged, sorted state — like \"My Calendar I\"?","answerFull":"Each insertion requires finding the correct position (binary search on starts, O(log n)) and checking for overlap with neighbors (O(1)), but inserting into a Python list is O(n) due to shifting elements. For n insertions, this is O(n^2) total. To do better, a balanced BST or skip-list-backed sorted container (e.g., sortedcontainers.SortedList in Python) gives O(log n) insertion, for O(n log n) total — relevant if asked about \"online\" / \"streaming\" variants of interval problems.","correct":"Each insertion requires finding the correct position (binary search on starts, O(log n)) and checking for overlap with neighbors (O(1)), but inserting into a Python list is O(n) due to shifting elements.","distractors":["Opposite ends (left=0, right=n-1, converging) is for finding a *relationship between two elements* (pair sum, palindrome check, container area) — the search space shrinks from both sides.","Binary search on the answer is O(n * log(range)).","In median-finding, the two heaps represent a size-balanced split of one sorted sequence (smaller half / larger half), and elements move between them to *maintain* that balance."]},{"id":"cs_fundamentals/dsa_patterns#70646d6c3e75","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"How do I know whether to use lo <= hi or lo < hi as the loop condition?","answerFull":"Use lo <= hi (with hi = mid - 1 / lo = mid + 1) when you're searching for an *exact match* and want to detect \"not found\" (loop exits with lo > hi). Use lo < hi (with hi = mid / lo = mid + 1) when you're searching for a *boundary* — the loop exits with lo == hi pointing exactly at the boundary, which is always a valid answer (assuming [lo, hi] is chosen so the answer is guaranteed to exist in range).","correct":"Use lo <= hi (with hi = mid - 1 / lo = mid + 1) when you're searching for an *exact match* and want to detect \"not found\" (loop exits with lo > hi).","distractors":["When arr[lo] == arr[mid] == arr[hi], you cannot tell which half is sorted (both could look \"flat\" while the rotation point hides inside either).","Compare arr[lo] to arr[mid].","The condition while fast and fast.next handles both: if head is None, fast = None, the loop body never executes, has_cycle returns False correctly."]},{"id":"cs_fundamentals/dsa_patterns#1c31cba28b96","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"What does \"binary search on the answer\" actually mean, and how do I spot it?","answerFull":"It means the thing you binary search over is not the input array but a *range of possible output values*. The tell is a problem phrased as \"minimize/maximize X such that some condition on the whole input holds,\" where checking the condition for a *fixed* X is easy (often a greedy O(n) scan), and the condition flips monotonically as X increases. If you can write feasible(x) -> bool and argue it's monotonic, you can binary search on x even if x was never an index into anything.","correct":"It means the thing you binary search over is not the input array but a *range of possible output values*.","distractors":["Because the goal is to keep as many intervals as possible, and the interval that *finishes earliest* leaves the maximum remaining timeline for future intervals — regardless of when it starts.","Because the guard condition grid[r][c] != \"1\" is the *only* thing that stops recursion, and it's checked at the *top* of each call using the grid's current state.","Binary search on the answer is O(n * log(range))."]},{"id":"cs_fundamentals/dsa_patterns#321bbb010a29","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":4,"difficulty":"core","question":"Walk through Search in Rotated Sorted Array — how do you find which half is sorted?","answerFull":"Compare arr[lo] to arr[mid]. If arr[lo] <= arr[mid], the left half [lo, mid] is internally sorted (no rotation point inside it) — check if target falls in [arr[lo], arr[mid]); if so, search left, else search right. If arr[lo] > arr[mid], the rotation point is inside [lo, mid], so the *right* half [mid, hi] must be sorted instead — apply the symmetric check. Exactly one half is always guaranteed sorted (absent duplicates), which is what keeps this O(log n).","correct":"Compare arr[lo] to arr[mid].","distractors":["A tree has no cycles and exactly one path from the root to any node, so a recursive/BFS walk can never revisit a node.","Because the array is sorted ascending.","Checking every (start, end) pair of nodes along root-to-X paths is O(n^2) (for each node, walk up/down to try all paths through it)."]},{"id":"cs_fundamentals/dsa_patterns#01183525701c","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"Why mid = lo + (hi - lo) // 2 instead of (lo + hi) // 2?","answerFull":"In languages with fixed-width integers (Java, C++), lo + hi can overflow if both are near INT_MAX, wrapping to a negative number and corrupting mid. lo + (hi - lo) // 2 never exceeds hi, so it can't overflow. Python integers are arbitrary precision, so this specific bug can't occur here — but writing it the overflow-safe way is a habit worth keeping since interviewers often ask \"does this work in Java/C++ too?\"","correct":"In languages with fixed-width integers (Java, C++), lo + hi can overflow if both are near INT_MAX, wrapping to a negative number and corrupting mid.","distractors":["LeetCode's MedianFinder.findMedian() is typed to return double (Python: float) because the even-size case averages two integers and may produce a .5 result.","In languages with fixed-width signed integers (Java, C++), >> preserves the sign bit (fills with 1s for negative numbers) while >>> (Java-only) fills with 0s regardless of sign.","When arr[lo] == arr[mid] == arr[hi], you cannot tell which half is sorted (both could look \"flat\" while the rotation point hides inside either)."]},{"id":"cs_fundamentals/dsa_patterns#8428a8e74fa0","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"What's the difference between bisect_left and bisect_right, and when do I use each?","answerFull":"bisect_left(arr, x) returns the leftmost position where x can be inserted — i.e., the first index i with arr[i] >= x (this is lower_bound). bisect_right(arr, x) returns the rightmost such position — the first index i with arr[i] > x (this is upper_bound). Use bisect_left to find \"the first occurrence of x\" or \"how many elements are strictly less than x\"; use bisect_right to find \"the count of elements <= x\" or \"the insertion point that places x *after* any duplicates\" (useful for maintaining a sorted list of non-decreasing values via insort_right, the default).","correct":"bisect_left(arr, x) returns the leftmost position where x can be inserted — i.e., the first index i with arr[i] >= x (this is lower_bound).","distractors":["Each child's required candy count depends on comparisons with BOTH neighbors simultaneously, but a single left-to-right (or right-to-left) pass can only enforce one direction's constraint at a time.","Fixed-size: a single for loop where right and left = right - k + 1 move in lockstep — no inner loop needed, just \"add nums[right], remove nums[right-k]\" each iteration.","By the BST invariant, every node's left subtree contains only smaller values and its right subtree only larger values."]},{"id":"cs_fundamentals/dsa_patterns#1ff6ca0eaaf0","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"Binary search on floating-point answers — how do you decide when to stop?","answerFull":"Two options: (1) fixed iteration count — for _ in range(100): mid = (lo+hi)/2; ... (100 iterations of halving a reasonable range converges far past double precision, ~10^-30); or (2) epsilon termination — while hi - lo > 1e-7: .... Fixed iteration count is preferred in interviews because it sidesteps debates about what epsilon is \"small enough\" and always terminates in a known number of steps.","correct":"Two options: (1) fixed iteration count — for _ in range(100): mid = (lo+hi)/2; ...","distractors":["When arr[lo] == arr[mid] == arr[hi], you cannot tell which half is sorted (both could look \"flat\" while the rotation point hides inside either).","Decide the final iteration order first: you want the result sorted by (frequency descending, word ascending).","Fixed-size: a single for loop where right and left = right - k + 1 move in lockstep — no inner loop needed, just \"add nums[right], remove nums[right-k]\" each iteration."]},{"id":"cs_fundamentals/dsa_patterns#dd19383a1cb1","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":8,"difficulty":"intermediate","question":"Why is Median of Two Sorted Arrays a binary search problem and not a merge problem?","answerFull":"Merging is O(m + n) — correct but not optimal. The O(log(min(m,n))) insight is: the median is defined by a *partition* that splits the combined array into two halves of equal size where every element on the left is <= every element on the right. You binary search for the partition index in the *smaller* array (the partition index in the larger array is then determined: total_left - i). Each candidate partition is checked in O(1) by comparing four boundary elements — that's what makes it O(log(min(m,n))) instead of O(m+n).","correct":"Merging is O(m + n) — correct but not optimal.","distractors":["In median-finding, the two heaps represent a size-balanced split of one sorted sequence (smaller half / larger half), and elements move between them to *maintain* that balance.","The median sits at the *boundary* between the two halves.","A min-heap of *all* n points sorted by distance and popping k times is O(n + k log n) — fine, but it processes and heapifies all n points upfront."]},{"id":"cs_fundamentals/dsa_patterns#4b0c93a3636b","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":9,"difficulty":"intermediate","question":"How do you handle Search in Rotated Sorted Array with duplicates (LC 81)?","answerFull":"When arr[lo] == arr[mid] == arr[hi], you cannot tell which half is sorted (both could look \"flat\" while the rotation point hides inside either). The standard fix is to shrink the search space by one: lo += 1 (or hi -= 1), degrading worst-case complexity to O(n) (e.g., for an array of all-equal values with one different element), but average case remains close to O(log n).","correct":"When arr[lo] == arr[mid] == arr[hi], you cannot tell which half is sorted (both could look \"flat\" while the rotation point hides inside either).","distractors":["In median-finding, the two heaps represent a size-balanced split of one sorted sequence (smaller half / larger half), and elements move between them to *maintain* that balance.","It means the thing you binary search over is not the input array but a *range of possible output values*.","Compare arr[lo] to arr[mid]."]},{"id":"cs_fundamentals/dsa_patterns#1800e9225d1f","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":10,"difficulty":"intermediate","question":"Find Peak Element finds *a* peak — why does \"go uphill\" guarantee correctness for *any* peak, not just the global max?","answerFull":"The problem only asks for *a* local peak (arr[i] > arr[i-1] and arr[i] > arr[i+1], with arr[-1] = arr[n] = -infinity conceptually). If arr[mid] < arr[mid+1], the sequence is \"still climbing\" at mid — there must be a peak somewhere to the right (worst case, the climb continues to arr[n-1], which is then a peak relative to the implicit -infinity boundary). Symmetric logic applies leftward. This guarantees *some* peak exists in the direction you move, even though you skip over the rest of the array.","correct":"The problem only asks for *a* local peak (arr[i] > arr[i-1] and arr[i] > arr[i+1], with arr[-1] = arr[n] = -infinity conceptually).","distractors":["Seed a min-heap with the first element of each row (or just row 0's first min(k, n) elements with pointers), each tagged with its row and column.","O(n) — every inserted element lives in exactly one of the two heaps, forever (for the streaming median problem, you must retain all history to support the running median).","A decreasing stack (top-to-bottom values decrease, i.e., pop when a *larger* element arrives) is used to find next greater elements — when you pop, the new element is the popped one's \"next greater\"."]},{"id":"cs_fundamentals/dsa_patterns#9b204e3c621d","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":12,"difficulty":"advanced","question":"What's the complexity tradeoff of \"binary search on the answer + O(n) feasibility check\" versus \"sort + two pointers\"?","answerFull":"Binary search on the answer is O(n * log(range)). If the same problem can be solved by sorting once (O(n log n)) and then a single linear pass with two pointers (O(n)), the sort-based approach is O(n log n) total — often *faster* than O(n * log(range)) when range is very large (e.g., 10^9). Always compare log(range) to log n: if range >> n, prefer a sort-based or counting-based approach if one exists; if range is small or the array can't be usefully sorted (e.g., it represents independent items, not comparable values), binary search on the answer wins.","correct":"Binary search on the answer is O(n * log(range)).","distractors":["When the problem is explicitly \"find the k-th largest element\" (singular, LC 215) on a static array with a follow-up \"can you do better than O(n log n)?\" — quickselect's average O(n) is the textbook answer.","(1) Find the middle using fast/slow pointers — splits the list into two halves.","Opposite ends (left=0, right=n-1, converging) is for finding a *relationship between two elements* (pair sum, palindrome check, container area) — the search space shrinks from both sides."]},{"id":"cs_fundamentals/dsa_patterns#a5727bd84d04","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"Why is a monotonic stack O(n) when there's a while loop inside a for loop?","answerFull":"Each element is pushed exactly once (once per iteration of the outer loop) and popped at most once (across the entire algorithm's lifetime, total pops <= total pushes <= n). So the total number of stack operations (pushes + pops) is bounded by 2n, regardless of how the pops are distributed across iterations of the outer loop. This is the same amortized argument used in sliding window and cyclic sort.","correct":"Each element is pushed exactly once (once per iteration of the outer loop) and popped at most once (across the entire algorithm's lifetime, total pops <= total pushes <= n).","distractors":["Fixed-size: a single for loop where right and left = right - k + 1 move in lockstep — no inner loop needed, just \"add nums[right], remove nums[right-k]\" each iteration.","A decreasing stack (top-to-bottom values decrease, i.e., pop when a *larger* element arrives) is used to find next greater elements — when you pop, the new element is the popped one's \"next greater\".","The two-pointer scan itself is O(n) (each pointer moves at most n times total, so the loop body runs at most O(n) times even though there are two indices)."]},{"id":"cs_fundamentals/dsa_patterns#13b9eb5a7c61","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"What's the difference between an \"increasing\" and \"decreasing\" monotonic stack, and how do you choose?","answerFull":"A decreasing stack (top-to-bottom values decrease, i.e., pop when a *larger* element arrives) is used to find next greater elements — when you pop, the new element is the popped one's \"next greater\". An increasing stack (pop when a *smaller* element arrives) finds next smaller elements. Choose based on what you're looking for: \"next greater\" → decreasing stack; \"next smaller\" / \"largest rectangle\" (which needs \"next/previous *smaller*\" to bound width) → increasing stack.","correct":"A decreasing stack (top-to-bottom values decrease, i.e., pop when a *larger* element arrives) is used to find next greater elements — when you pop, the new element is the popped one's \"next greater\".","distractors":["A 2-element heap still pays O(log 2) = O(1) overhead per operation plus the constant-factor cost of heap push/pop machinery (list operations, sift-up/down).","Each element is pushed exactly once (once per iteration of the outer loop) and popped at most once (across the entire algorithm's lifetime, total pops <= total pushes <= n).","When the problem is explicitly \"find the k-th largest element\" (singular, LC 215) on a static array with a follow-up \"can you do better than O(n log n)?\" — quickselect's average O(n) is the textbook answer."]},{"id":"cs_fundamentals/dsa_patterns#c85892a227f9","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"In \"Next Greater Element,\" does it matter whether you scan left-to-right or right-to-left?","answerFull":"Both work, but the bookkeeping differs. Left-to-right (as in the template): when you pop an element, the *current* element is its \"next greater\" — you resolve answers for *past* elements as you go. Right-to-left: the stack represents \"candidates for *my* next greater\", and when you process element i, you pop everything <= nums[i] (they can never be the answer for anything to i's left, since nums[i] is closer and at least as large), then the new stack top (if any) is nums[i]'s next greater, then push nums[i]. Both are O(n); left-to-right is more common because it resolves answers eagerly.","correct":"Both work, but the bookkeeping differs.","distractors":["A decreasing stack (top-to-bottom values decrease, i.e., pop when a *larger* element arrives) is used to find next greater elements — when you pop, the new element is the popped one's \"next greater\".","By the BST invariant, every node's left subtree contains only smaller values and its right subtree only larger values.","Checking, from each cell, whether water can reach both oceans would require a traversal *from* that cell — O(rows · cols) work per cell, O((rows·cols)²) total."]},{"id":"cs_fundamentals/dsa_patterns#d57f64da8b5d","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":4,"difficulty":"core","question":"Why does Largest Rectangle in Histogram append a sentinel value of 0?","answerFull":"The algorithm only computes the area for a bar when a *shorter* bar causes it to be popped. Without a sentinel, bars that are part of an increasing sequence at the *end* of the array (e.g., [1,2,3]) are never popped — their rectangles are never computed. Appending a 0 (shorter than any real height) guarantees that every remaining bar on the stack gets popped and evaluated during the final iteration, \"flushing\" the stack.","correct":"The algorithm only computes the area for a bar when a *shorter* bar causes it to be popped.","distractors":["When the problem is explicitly \"find the k-th largest element\" (singular, LC 215) on a static array with a follow-up \"can you do better than O(n log n)?\" — quickselect's average O(n) is the textbook answer.","A decreasing stack (top-to-bottom values decrease, i.e., pop when a *larger* element arrives) is used to find next greater elements — when you pop, the new element is the popped one's \"next greater\".","dp[i][j] (interval [i,j]) is computed from dp[i][k] and dp[k+1][j] for i <= k < j — both of which are shorter intervals than [i,j]."]},{"id":"cs_fundamentals/dsa_patterns#83084ea751b7","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":10,"difficulty":"intermediate","question":"What's the \"132 Pattern\" problem, and why does it need a non-trivial monotonic stack variant?","answerFull":"\"132 Pattern\" asks: does there exist i < j < k with nums[i] < nums[k] < nums[j]? The trick: scan right to left, maintaining a decreasing stack AND a variable third = -infinity representing the best candidate for \"nums[k]\" (the \"2\" in \"132\"). When popping elements smaller than nums[i] from the stack (because they're smaller than the current element, meaning the current element could be a \"3\" for them), update third = max(third, popped_value). If at any point nums[i] < third, you've found nums[i] as the \"1\" with valid \"3\" and \"2\" to its right — pattern found. This shows monotonic stacks can track auxiliary \"best discarded value\" state, not just the stack contents themselves.","correct":"\"132 Pattern\" asks: does there exist i < j < k with nums[i] < nums[k] < nums[j]?","distractors":["LC 55 only asks \"can you reach the end?\" — a single farthest variable suffices, and you fail fast if i > farthest (a gap you can never cross).","Sort nums first so duplicates are adjacent.","The problem only asks for *a* local peak (arr[i] > arr[i-1] and arr[i] > arr[i+1], with arr[-1] = arr[n] = -infinity conceptually)."]},{"id":"cs_fundamentals/dsa_patterns#ee389a961c64","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":11,"difficulty":"advanced","question":"Is the monotonic stack pattern ever combined with binary search?","answerFull":"Yes — since the stack maintains a sorted (monotonic) sequence of values, you *can* binary search within it. A canonical example is \"Longest Increasing Subsequence\" (patience sorting): maintain a list of \"pile tops\" that is sorted, and binary search for where each new element belongs (bisect_left) — this is structurally similar to a monotonic stack but the \"pop\" is replaced with \"overwrite at the binary-searched position\", giving O(n log n) instead of O(n^2).","correct":"Yes — since the stack maintains a sorted (monotonic) sequence of values, you *can* binary search within it.","distractors":["For minimize DP (Coin Change), 0 looks like \"already the best possible value\" to min(), so nothing can ever improve it — 0 must mean \"not yet computed,\" requiring +infinity.","Without any optimization, yes — a degenerate union sequence can produce an O(n)-deep tree.","Each insertion requires finding the correct position (binary search on starts, O(log n)) and checking for overlap with neighbors (O(1)), but inserting into a Python list is O(n) due to shifting elements."]},{"id":"cs_fundamentals/dsa_patterns#2e8685d00711","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"Why is prefix[0] = 0 (\"the empty prefix\") so important, and what does it represent?","answerFull":"prefix[0] = 0 represents the sum of zero elements — it's the identity element for addition. It allows the formula sum(nums[i..j]) = prefix[j+1] - prefix[i] to work uniformly even when i = 0 (a subarray starting at the very first element): sum(nums[0..j]) = prefix[j+1] - prefix[0] = prefix[j+1] - 0 = prefix[j+1]. In the hashmap variant, seeding seen = {0: 1} before the loop is the same idea — it lets subarrays starting at index 0 be \"found\" as a match against this sentinel.","correct":"prefix[0] = 0 represents the sum of zero elements — it's the identity element for addition.","distractors":["Opposite ends (left=0, right=n-1, converging) is for finding a *relationship between two elements* (pair sum, palindrome check, container area) — the search space shrinks from both sides.","The heap doesn't represent \"the answer\" directly; it represents your *current best guess* at the top-k, and its root is the weakest member of that guess — the element most likely to be displaced by a better candidate.","In median-finding, the two heaps represent a size-balanced split of one sorted sequence (smaller half / larger half), and elements move between them to *maintain* that balance."]},{"id":"cs_fundamentals/dsa_patterns#ec41a06c1b42","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"How do you adapt the template for \"subarray sum divisible by k\"?","answerFull":"Track prefix % k instead of raw prefix. Two prefixes with the same remainder mod k mean the subarray between them has a sum divisible by k (since (a - b) % k == 0 iff a % k == b % k). Watch out for negative remainders in languages where % can return negative values for negative operands (Python's % always returns non-negative for positive k, but other languages like Java/C++ may not — normalize with ((x % k) + k) % k).","correct":"Track prefix % k instead of raw prefix.","distractors":["The used[] array approach (Template 2) is more intuitive and naturally supports the duplicate-skipping check for Permutations II.","For *counting*, the hashmap stores prefix_value -> count of occurrences (a defaultdict(int)), because multiple earlier indices can have the same prefix value, each contributing one valid subarray.","A hashmap gives O(1) for exact-match lookups, but \"does any key start with 'app'?\" requires either iterating all keys (O(n·L)) or maintaining a separate prefix index."]},{"id":"cs_fundamentals/dsa_patterns#0a3de053d749","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":4,"difficulty":"core","question":"What's the difference between \"count subarrays\" and \"find the maximum length subarray\" in terms of what you store in the hashmap?","answerFull":"For *counting*, the hashmap stores prefix_value -> count of occurrences (a defaultdict(int)), because multiple earlier indices can have the same prefix value, each contributing one valid subarray. For *maximum length*, the hashmap stores prefix_value -> earliest index where it occurred (a regular dict, only the first occurrence matters, since you want the longest — i.e., earliest-starting — subarray for a given prefix match).","correct":"For *counting*, the hashmap stores prefix_value -> count of occurrences (a defaultdict(int)), because multiple earlier indices can have the same prefix value, each contributing one valid subarray.","distractors":["Each recursive call branches into multiple choices, and the recursion depth equals the solution length — so the total number of leaves in the search tree is O(branching_factor ^ depth), which is 2^n (subsets) or n!","Without this check, every number triggers an inner \"count forward\" loop, and for a single long run of length n, this sums to O(n^2) (the 1st number counts n steps, the 2nd counts n-1, etc.).","UnionFindBroken(3) allocates parent = [0, 1, 2] — valid indices are 0, 1, 2."]},{"id":"cs_fundamentals/dsa_patterns#b49a985ba8d4","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"How would you extend prefix sums to 2D (range sum of a submatrix)?","answerFull":"Build a 2D prefix array where prefix[i][j] = sum of all cells (r, c) with r < i and c < j — i.e., the sum of the submatrix from (0,0) to (i-1, j-1). Compute it via prefix[i][j] = matrix[i-1][j-1] + prefix[i-1][j] + prefix[i][j-1] - prefix[i-1][j-1] (inclusion-exclusion to avoid double-counting the overlap). A range query for rows [r1,r2] and cols [c1,c2] is prefix[r2+1][c2+1] - prefix[r1][c2+1] - prefix[r2+1][c1] + prefix[r1][c1].","correct":"Build a 2D prefix array where prefix[i][j] = sum of all cells (r, c) with r < i and c < j — i.e., the sum of the submatrix from (0,0) to (i-1, j-1).","distractors":["This is a classic greedy/prefix-sum argument: if the total gas across the whole circuit is >= total cost, then *some* rotation of the prefix-sum sequence is entirely non-negative.","Same structure as prefix sum, but using XOR (prefix[i] = nums[0] XOR nums[1] XOR ...","It means the thing you binary search over is not the input array but a *range of possible output values*."]},{"id":"cs_fundamentals/dsa_patterns#397385fdf8d2","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"When would you choose a Fenwick tree over a prefix sum array?","answerFull":"When the array is mutated between queries (point updates: nums[i] = new_value). A plain prefix sum array requires O(n) to recompute everything after a single update (everything from index i onward shifts). A Fenwick tree (Binary Indexed Tree) supports both point updates and prefix-sum queries in O(log n) each, at the cost of slightly more complex implementation and O(n) extra space (similar to the prefix array's space, but structured as an implicit tree).","correct":"When the array is mutated between queries (point updates: nums[i] = new_value).","distractors":["prefix[0] = 0 represents the sum of zero elements — it's the identity element for addition.","Model the array as a function f(i) = nums[i].","Compute prefix_product[i] = product(nums[0..i-1]) (product of everything to the left of i) and suffix_product[i] = product(nums[i+1..n-1]) (product of everything to the right)."]},{"id":"cs_fundamentals/dsa_patterns#a7578cebff53","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"Can prefix sum be combined with binary search?","answerFull":"Yes — if the array is non-negative, the prefix sum array is *monotonically non-decreasing*, which means you can binary search over it. For example, \"find the smallest subarray length with sum >= target\" can binary search for the first prefix[j] that is >= prefix[i] + target, for each i. (Though for non-negative arrays, sliding window is usually simpler and equally efficient.)","correct":"Yes — if the array is non-negative, the prefix sum array is *monotonically non-decreasing*, which means you can binary search over it.","distractors":["Build a 2D prefix array where prefix[i][j] = sum of all cells (r, c) with r < i and c < j — i.e., the sum of the submatrix from (0,0) to (i-1, j-1).","This is a classic greedy/prefix-sum argument: if the total gas across the whole circuit is >= total cost, then *some* rotation of the prefix-sum sequence is entirely non-negative.","BFS visits nodes in non-decreasing depth order — the very first leaf it encounters is *guaranteed* to be at the minimum depth across the whole tree, so you can return immediately."]},{"id":"cs_fundamentals/dsa_patterns#04c13b7f25e2","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":8,"difficulty":"intermediate","question":"What's the \"prefix XOR\" variant, and when is it used?","answerFull":"Same structure as prefix sum, but using XOR (prefix[i] = nums[0] XOR nums[1] XOR ... XOR nums[i-1]). The key property is a XOR a = 0, so prefix[j] XOR prefix[i] = nums[i] XOR ... XOR nums[j-1] (the XOR of the subarray). Used for problems like \"find the XOR of a range\" or \"count subarrays with a given XOR value\" — same hashmap structure as sum-based problems, but with XOR as the combining operator.","correct":"Same structure as prefix sum, but using XOR (prefix[i] = nums[0] XOR nums[1] XOR ...","distractors":["prefix[0] = 0 represents the sum of zero elements — it's the identity element for addition.","This is a classic greedy/prefix-sum argument: if the total gas across the whole circuit is >= total cost, then *some* rotation of the prefix-sum sequence is entirely non-negative.","Build a 2D prefix array where prefix[i][j] = sum of all cells (r, c) with r < i and c < j — i.e., the sum of the submatrix from (0,0) to (i-1, j-1)."]},{"id":"cs_fundamentals/dsa_patterns#b575f5a31196","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":9,"difficulty":"intermediate","question":"How do you compute \"Product of Array Except Self\" without division, using a prefix-style approach?","answerFull":"Compute prefix_product[i] = product(nums[0..i-1]) (product of everything to the left of i) and suffix_product[i] = product(nums[i+1..n-1]) (product of everything to the right). The answer for index i is prefix_product[i] * suffix_product[i]. This avoids division (which would fail if any element is 0) and can be done in O(1) extra space by computing the suffix product in a second pass that overwrites the output array (which initially holds the prefix products).","correct":"Compute prefix_product[i] = product(nums[0..i-1]) (product of everything to the left of i) and suffix_product[i] = product(nums[i+1..n-1]) (product of everything to the right).","distractors":["Build a 2D prefix array where prefix[i][j] = sum of all cells (r, c) with r < i and c < j — i.e., the sum of the submatrix from (0,0) to (i-1, j-1).","Same structure as prefix sum, but using XOR (prefix[i] = nums[0] XOR nums[1] XOR ...","Because the algorithm's correctness depends on monotonicity: moving left right strictly increases the candidate sum, and moving right left strictly decreases it."]},{"id":"cs_fundamentals/dsa_patterns#44dd6fe8d1be","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":10,"difficulty":"intermediate","question":"What's the time complexity of building a prefix sum array, and is it ever worth it for a single query?","answerFull":"Building is O(n). For a *single* range-sum query, this is no better than just summing the range directly (also O(n) in the worst case for a large range). Prefix sums pay off when there are multiple queries on a static array — O(n) to build once, then O(1) per query, versus O(n) per query without precomputation. If asked \"what if there's only one query,\" say so explicitly — it signals you understand the amortization argument.","correct":"Building is O(n). For a *single* range-sum query, this is no better than just summing the range directly (also O(n) in the worst case for a large range).","distractors":["Build a 2D prefix array where prefix[i][j] = sum of all cells (r, c) with r < i and c < j — i.e., the sum of the submatrix from (0,0) to (i-1, j-1).","When the problem is explicitly \"find the k-th largest element\" (singular, LC 215) on a static array with a follow-up \"can you do better than O(n log n)?\" — quickselect's average O(n) is the textbook answer.","Directly, no — sliding window relies on the 1D notion of \"contiguous range\" with a single left/right pair."]},{"id":"cs_fundamentals/dsa_patterns#47ae0ae7ab0f","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"Dijkstra vs. Bellman-Ford — beyond \"negative weights,\" what's the practical tradeoff?","answerFull":"Dijkstra is O((V+E) log V) — much faster for large graphs, but requires non-negative weights. Bellman-Ford is O(V*E) — works with negative weights and can *detect* negative cycles (a cycle whose total weight is negative, meaning \"shortest path\" is undefined — you could loop forever decreasing cost). If you know weights are non-negative (the overwhelming majority of real-world \"distance\"/\"time\"/\"cost\" problems), always prefer Dijkstra.","correct":"Dijkstra is O((V+E) log V) — much faster for large graphs, but requires non-negative weights.","distractors":["A min-heap of *all* n points sorted by distance and popping k times is O(n + k log n) — fine, but it processes and heapifies all n points upfront.","A DAG has no cycles, so \"negative cycle\" is impossible by definition — Bellman-Ford's only advantage over Dijkstra (negative-weight tolerance) comes \"for free\" with no extra cost.","The invariant requires that extending a path can never *help* relative to stopping early — i.e., the \"combine\" operation must be monotonically non-improving."]},{"id":"cs_fundamentals/dsa_patterns#ddab4ec49c6e","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"Why is 0-1 BFS O(V+E) instead of O((V+E) log V) like Dijkstra?","answerFull":"A deque supports O(1) push/pop from both ends, vs. O(log V) for a heap. 0-1 BFS maintains the same \"process nodes in non-decreasing distance order\" invariant as Dijkstra, but because weights are only 0 or 1, a 0-weight edge keeps a node in the *same* distance \"tier\" (push to front) and a 1-weight edge moves it to the *next* tier (push to back) — the deque's two ends naturally represent \"current tier\" and \"next tier\" without needing a heap's full ordering capability.","correct":"A deque supports O(1) push/pop from both ends, vs. O(log V) for a heap.","distractors":["A 2-element heap still pays O(log 2) = O(1) overhead per operation plus the constant-factor cost of heap push/pop machinery (list operations, sift-up/down).","Heap: O(n log k) time, O(k) space, works on streams, gives you all k elements (unordered).","Replace the FIFO deque in Kahn's algorithm with a min-heap (heapq)."]},{"id":"cs_fundamentals/dsa_patterns#fa6ed6ac4f53","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":4,"difficulty":"core","question":"When is Floyd-Warshall's O(V^3) actually acceptable, and what extra information does it give you that running Dijkstra V times doesn't?","answerFull":"O(V^3) is fine for V up to a few hundred (10^6-10^8 operations). Running Dijkstra from every node is O(V * (E log V)) — for dense graphs (E ~ V^2), that's O(V^3 log V), actually *worse* than Floyd-Warshall's O(V^3). Floyd-Warshall also handles negative edges (though not negative cycles) more simply than running Bellman-Ford from every node (O(V^2 * E)).","correct":"O(V^3) is fine for V up to a few hundred (10^6-10^8 operations).","distractors":["Dijkstra is O((V+E) log V) — much faster for large graphs, but requires non-negative weights.","A DAG has no cycles, so \"negative cycle\" is impossible by definition — Bellman-Ford's only advantage over Dijkstra (negative-weight tolerance) comes \"for free\" with no extra cost.","The heap doesn't represent \"the answer\" directly; it represents your *current best guess* at the top-k, and its root is the weakest member of that guess — the element most likely to be displaced by a better candidate."]},{"id":"cs_fundamentals/dsa_patterns#b9951bec07ea","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"Why use a min-heap in Dijkstra instead of just scanning all unvisited nodes for the minimum each iteration (the textbook O(V^2) version)?","answerFull":"Both are correct. The O(V^2) scan is actually *better* for dense graphs (E ~ V^2) because O(V^2) beats O((V+E) log V) = O(V^2 log V). The heap-based version wins for sparse graphs (E ~ V), where O((V+E) log V) = O(V log V) beats O(V^2). Most LeetCode graphs are sparse, so the heap version is the default — but knowing the dense-graph alternative is a strong signal of depth in an interview.","correct":"Both are correct. The O(V^2) scan is actually *better* for dense graphs (E ~ V^2) because O(V^2) beats O((V+E) log V) = O(V^2 log V).","distractors":["For small, fixed k (say k <= 4), a linear scan over k pointers to find the minimum each step is O(N * k) — simpler code, and for small constant k, O(N * k) and O(N log k) are both effectively O(N).","Fixed-size: a single for loop where right and left = right - k + 1 move in lockstep — no inner loop needed, just \"add nums[right], remove nums[right-k]\" each iteration.","At each step, take the two least-frequent remaining nodes/symbols and merge them into a new node whose frequency is their sum, inserting it back into a min-heap."]},{"id":"cs_fundamentals/dsa_patterns#281c5877d6a9","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"How does Bellman-Ford detect a negative cycle, and why does it take exactly V-1 + 1 rounds?","answerFull":"Any shortest *simple* path (no repeated nodes) in a graph with V nodes has at most V-1 edges. So after V-1 rounds of relaxing all edges, every shortest simple path's distance must be final — unless a negative cycle exists, in which case \"shortest path\" is unbounded (you can loop the cycle infinitely to keep decreasing cost). The V-th round (one extra relaxation pass) checks: if *anything* still improves after V-1 rounds, that improvement can only come from a negative cycle.","correct":"Any shortest *simple* path (no repeated nodes) in a graph with V nodes has at most V-1 edges.","distractors":["A tree has no cycles and exactly one path from the root to any node, so a recursive/BFS walk can never revisit a node.","This follows from the algebra: at the meeting point, slow has traveled L + k steps (L = distance from head to cycle start, k = distance from cycle start to meeting point along the cycle), and fast has traveled 2(L+k).","Every node in a cycle has at least one incoming edge *from within the cycle* that can never be \"removed\" (because the node providing that edge can never reach in-degree 0 either — it's circular)."]},{"id":"cs_fundamentals/dsa_patterns#4b41ac422214","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":8,"difficulty":"intermediate","question":"Path with Maximum Probability uses a max-heap and multiplies probabilities — why does the greedy \"pop is final\" invariant still hold here?","answerFull":"The invariant requires that extending a path can never *help* relative to stopping early — i.e., the \"combine\" operation must be monotonically non-improving. For sums with non-negative weights, dist[u] + w >= dist[u]. For products of probabilities in [0,1], prob[u] * w <= prob[u] (since w <= 1). Both satisfy \"extending can't make it look better than the prefix already was\" — which is the real requirement, not \"addition\" specifically.","correct":"The invariant requires that extending a path can never *help* relative to stopping early — i.e., the \"combine\" operation must be monotonically non-improving.","distractors":["The heap approach (kth_smallest_in_sorted_matrix) is O(k log n) — great when k is small relative to n^2.","Rebalancing moves elements between heaps based purely on counts (len(left) vs len(right)) to restore the size invariant — it has no mechanism to verify max(left) <= min(right).","The function's *return value* must be something the parent can use to compute *its own* combination — here, the subtree's height (a single number representing \"how far down does this subtree extend\")."]},{"id":"cs_fundamentals/dsa_patterns#8bca7784a019","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":9,"difficulty":"intermediate","question":"Is BFS a \"special case\" of Dijkstra? In what sense?","answerFull":"Yes — BFS is Dijkstra where all edge weights equal 1 (or 0, for 0-1 BFS). A plain deque-based queue, where every node is appended to one end, IS implicitly maintaining \"process in non-decreasing distance order\" — because each BFS layer corresponds to exactly one unit of distance. Dijkstra generalizes this to arbitrary non-negative weights by replacing the queue's implicit ordering with an explicit min-heap.","correct":"Yes — BFS is Dijkstra where all edge weights equal 1 (or 0, for 0-1 BFS).","distractors":["A tree has no cycles and exactly one path from the root to any node, so a recursive/BFS walk can never revisit a node.","If you're given the *whole* graph upfront and need to answer connectivity questions once (e.g., \"how many connected components does this static graph have?\"), DFS/BFS is simpler to write and equally efficient — O(V+E).","Dijkstra is O((V+E) log V) — much faster for large graphs, but requires non-negative weights."]},{"id":"cs_fundamentals/dsa_patterns#b34e4b0e9c27","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":11,"difficulty":"advanced","question":"For DAG shortest path via topological sort, why is it both faster AND more general (handles negative weights) than Dijkstra?","answerFull":"A DAG has no cycles, so \"negative cycle\" is impossible by definition — Bellman-Ford's only advantage over Dijkstra (negative-weight tolerance) comes \"for free\" with no extra cost. Processing nodes in topological order guarantees that when you relax u's outgoing edges, dist[u] is *already final* — every predecessor of u was processed earlier in topo order, so nothing can improve dist[u] after the fact. This gives Dijkstra's \"pop is final\" guarantee *without* needing a heap or non-negative weights — hence O(V+E).","correct":"A DAG has no cycles, so \"negative cycle\" is impossible by definition — Bellman-Ford's only advantage over Dijkstra (negative-weight tolerance) comes \"for free\" with no extra cost.","distractors":["Dijkstra is O((V+E) log V) — much faster for large graphs, but requires non-negative weights.","The condition while fast and fast.next handles both: if head is None, fast = None, the loop body never executes, has_cycle returns False correctly.","A topological order requires that for every edge u -> v, u comes before v."]},{"id":"cs_fundamentals/dsa_patterns#6112fc4129af","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"Why is the nested while loop inside a for loop still O(n) overall, not O(n^2)?","answerFull":"Because left only ever increases — it is never reset to a smaller value. Across the *entire* run of the algorithm, left moves from 0 to at most n, so the while loop body executes at most n times *in total* (summed across all iterations of the outer for loop), not n times *per* outer iteration. This is amortized analysis: the total cost is O(n) + O(n) = O(n), even though the code structurally looks like it could be O(n^2).","correct":"Because left only ever increases — it is never reset to a smaller value.","distractors":["Within the per-level loop, maintain a prev pointer (reset to None at the start of each level).","Without the sets, validating \"can I place a queen at (row, col)?\" requires scanning all previously-placed queens — O(row) per check, O(n) per row, O(n^2) total per leaf, O(n^2 * n!) overall.","Fixed-size: a single for loop where right and left = right - k + 1 move in lockstep — no inner loop needed, just \"add nums[right], remove nums[right-k]\" each iteration."]},{"id":"cs_fundamentals/dsa_patterns#1c89b894ea04","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"How do you decide whether to shrink \"while valid\" or \"while invalid\"?","answerFull":"It depends on what you're optimizing. For \"smallest subarray satisfying X\" (e.g., sum ≥ target), you expand until valid, then shrink *while it remains valid* to find the minimum — record the length right before it becomes invalid. For \"longest subarray satisfying X\" (e.g., no repeating characters), you expand, and shrink *only when it becomes invalid* (i.e., while NOT valid) — record the length while it's valid, after each expansion.","correct":"It depends on what you're optimizing.","distractors":["For *counting*, the hashmap stores prefix_value -> count of occurrences (a defaultdict(int)), because multiple earlier indices can have the same prefix value, each contributing one valid subarray.","Because the core operation is \"place value v at index f(v)\" (where f(v) = v-1 or f(v) = v), and this requires f(v) to be a valid index into the array — i.e., 0 <= f(v) < n.","Course Schedule II just needs *a* valid ordering."]},{"id":"cs_fundamentals/dsa_patterns#b6a54938ff62","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":4,"difficulty":"core","question":"Why does \"Longest Repeating Character Replacement\" use windowLen - maxFreq <= k as the validity check?","answerFull":"The window is valid if you can convert it to all-the-same-character using at most k replacements. The cheapest strategy is to keep the most frequent character (maxFreq occurrences) and replace everything else — that costs windowLen - maxFreq replacements. If that's <= k, the window is achievable. Note maxFreq is allowed to be \"stale\" (computed from a larger window that's since shrunk) — this doesn't break correctness because we're looking for the *maximum* valid window length, and a stale (too-large) maxFreq can only make the validity check *stricter*, never falsely permissive in a way that inflates the final answer.","correct":"The window is valid if you can convert it to all-the-same-character using at most k replacements.","distractors":["Course Schedule II just needs *a* valid ordering.","Walk to the node representing the word's last character and set is_end = False.","backtrack(i, ...) allows candidates[i] to be chosen again in the next recursive level — modeling \"unlimited supply\" of each candidate (e.g., [2,2,3] is valid for Combination Sum if 2+2+3 sums to target)."]},{"id":"cs_fundamentals/dsa_patterns#ae2d5edec4de","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"Can sliding window be used on a 2D grid?","answerFull":"Directly, no — sliding window relies on the 1D notion of \"contiguous range\" with a single left/right pair. For 2D problems (e.g., \"max sum of a k×k submatrix\"), you typically apply sliding window along one dimension after reducing the other dimension via prefix sums (compute column-sums for a band of rows, then slide a 1D window across columns).","correct":"Directly, no — sliding window relies on the 1D notion of \"contiguous range\" with a single left/right pair.","distractors":["A dummy node handles the edge case where left == 1 (the sub-range includes the original head) — without it, there's no \"node before the sub-range\" to update.","Opposite ends (left=0, right=n-1, converging) is for finding a *relationship between two elements* (pair sum, palindrome check, container area) — the search space shrinks from both sides.","Because the algorithm's correctness depends on monotonicity: moving left right strictly increases the candidate sum, and moving right left strictly decreases it."]},{"id":"cs_fundamentals/dsa_patterns#2a283cfc058a","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"What's the difference between a fixed-size and variable-size window in terms of code structure?","answerFull":"Fixed-size: a single for loop where right and left = right - k + 1 move in lockstep — no inner loop needed, just \"add nums[right], remove nums[right-k]\" each iteration. Variable-size: right is the outer loop variable, and left advances inside an inner while loop whose condition depends on the current window's aggregate — left and right are *not* synchronized by a fixed offset.","correct":"Fixed-size: a single for loop where right and left = right - k + 1 move in lockstep — no inner loop needed, just \"add nums[right], remove nums[right-k]\" each iteration.","distractors":["Change the target size ratio: instead of keeping left and right within one element of equal size, keep len(left) ~= p% * n and len(right) ~= (100-p)% * n.","For \"longest\", you record max(best, right - left + 1) once the window is valid (or just before it becomes invalid).","Compute prefix_product[i] = product(nums[0..i-1]) (product of everything to the left of i) and suffix_product[i] = product(nums[i+1..n-1]) (product of everything to the right)."]},{"id":"cs_fundamentals/dsa_patterns#f336ff5e88b4","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"How do you initialize the frequency map for \"Permutation in String\" and what do you compare?","answerFull":"Build a Counter (or 26-length array) for the pattern string s1. Slide a fixed-size window of length len(s1) over s2, maintaining a Counter of the current window. At each position, compare the two counters for equality (== on Counter objects works in Python, or compare the 26-length arrays). A match means the current window is a permutation of s1.","correct":"Build a Counter (or 26-length array) for the pattern string s1.","distractors":["Not directly — it's a placement trick that only works when the array's values form (approximately) a permutation of its indices.","Compare arr[lo] to arr[mid].","Compare each pair of *adjacent* words in the given (sorted) word list."]},{"id":"cs_fundamentals/dsa_patterns#3e53c89e3237","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":9,"difficulty":"intermediate","question":"Is sliding window always O(n) space, or can it be O(1)?","answerFull":"Space depends on what's tracked, not on the pointer mechanism itself. A running sum (fixed/variable window over numeric sums) is O(1) extra space. A frequency map (Counter) is O(|Σ|) — bounded by alphabet size, often treated as O(1) if the alphabet is fixed (e.g., 26 lowercase letters). It is *not* O(n) unless you're storing per-index data structures, which is unusual for this pattern.","correct":"Space depends on what's tracked, not on the pointer mechanism itself.","distractors":["Within the per-level loop, maintain a prev pointer (reset to None at the start of each level).","Rebalancing moves elements between heaps based purely on counts (len(left) vs len(right)) to restore the size invariant — it has no mechanism to verify max(left) <= min(right).","In an interview, rarely — O(1) space is almost always preferred when achievable, and the iterative version isn't meaningfully harder to write once memorized."]},{"id":"cs_fundamentals/dsa_patterns#8d7f81060ae2","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":10,"difficulty":"intermediate","question":"How would you find the *number* of subarrays satisfying a constraint, versus the *longest* one?","answerFull":"For \"longest\", you record max(best, right - left + 1) once the window is valid (or just before it becomes invalid). For \"count\", every time the window is valid for a given right, *all* subarrays ending at right with start >= left are valid — so you add (right - left + 1) to the count, not just 1. This is a common off-by-one source: counting \"1 per valid window\" vs \"all valid windows ending at this position\".","correct":"For \"longest\", you record max(best, right - left + 1) once the window is valid (or just before it becomes invalid).","distractors":["When the \"best current choice\" changes dynamically as you consume items — i.e., the greedy choice is \"whichever remaining item has the highest frequency/priority *right now*,\" and that ranking shifts after each pick.","Rebalancing moves elements between heaps based purely on counts (len(left) vs len(right)) to restore the size invariant — it has no mechanism to verify max(left) <= min(right).","Directly, no — sliding window relies on the 1D notion of \"contiguous range\" with a single left/right pair."]},{"id":"cs_fundamentals/dsa_patterns#5f57c1feadd2","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"Why does finding the \"k largest\" elements use a MIN-heap — isn't that backwards?","answerFull":"The heap doesn't represent \"the answer\" directly; it represents your *current best guess* at the top-k, and its root is the weakest member of that guess — the element most likely to be displaced by a better candidate. A min-heap keeps that weakest member at the root in O(1), so each new element only needs one comparison (new > heap[0]) to decide whether it belongs. Using a max-heap would put the *strongest* member at the root, which tells you nothing about whether a new element should be added.","correct":"The heap doesn't represent \"the answer\" directly; it represents your *current best guess* at the top-k, and its root is the weakest member of that guess — the element most likely to be displaced by a better candidate.","distractors":["Opposite ends (left=0, right=n-1, converging) is for finding a *relationship between two elements* (pair sum, palindrome check, container area) — the search space shrinks from both sides.","Seed a min-heap with the first element of each row (or just row 0's first min(k, n) elements with pointers), each tagged with its row and column.","Each insertion requires finding the correct position (binary search on starts, O(log n)) and checking for overlap with neighbors (O(1)), but inserting into a Python list is O(n) due to shifting elements."]},{"id":"cs_fundamentals/dsa_patterns#f736ffa21895","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"How does the heap-of-size-k approach compare to sorting and to quickselect?","answerFull":"Heap: O(n log k) time, O(k) space, works on streams, gives you all k elements (unordered). Full sort: O(n log n) time, O(n) space (or O(1) in-place), gives total order — better when k is close to n or you need the order anyway. Quickselect: O(n) average / O(n^2) worst case, O(1) extra space, but requires the full array up front (no streaming) and mutates it in place — best when you need only the k-th value (or an unordered top-k group) from a static array.","correct":"Heap: O(n log k) time, O(k) space, works on streams, gives you all k elements (unordered).","distractors":["Sorting rearranges elements, destroying the mapping from value to original index.","Concatenating and sorting is O(N log N) and throws away the fact that each input is *already sorted*.","For \"top-k frequent\" problems, you need O(u) space for the frequency map (u = number of unique elements, u <= n) regardless of approach — that's unavoidable since you must count occurrences."]},{"id":"cs_fundamentals/dsa_patterns#6e7122db46fc","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"When would an interviewer expect quickselect instead of a heap?","answerFull":"When the problem is explicitly \"find the k-th largest element\" (singular, LC 215) on a static array with a follow-up \"can you do better than O(n log n)?\" — quickselect's average O(n) is the textbook answer. If the problem mentions a stream, repeated queries, or \"return the k elements\" (not just the k-th), the heap is usually the cleaner and intended answer because quickselect doesn't naturally support incremental updates.","correct":"When the problem is explicitly \"find the k-th largest element\" (singular, LC 215) on a static array with a follow-up \"can you do better than O(n log n)?\" — quickselect's average O(n) is the textbook answer.","distractors":["If you're given the *whole* graph upfront and need to answer connectivity questions once (e.g., \"how many connected components does this static graph have?\"), DFS/BFS is simpler to write and equally efficient — O(V+E).","Seed a min-heap with the first element of each row (or just row 0's first min(k, n) elements with pointers), each tagged with its row and column.","O(n) — every inserted element lives in exactly one of the two heaps, forever (for the streaming median problem, you must retain all history to support the running median)."]},{"id":"cs_fundamentals/dsa_patterns#389c438b6a40","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":4,"difficulty":"core","question":"Top K Frequent Words requires lexicographic tie-breaking — how do you encode that in a heap comparison?","answerFull":"Decide the final iteration order first: you want the result sorted by (frequency descending, word ascending). If using a min-heap of size k that *evicts* the worst candidate, \"worst\" means (lowest frequency, then lexicographically *largest* word) — so push (count, word) won't directly work because Python compares tuples lexicographically and you'd need to invert the word ordering for ties. A common trick: push (count, ReverseStr(word)) with a small wrapper class implementing reversed lt, or push (-count, word) into a min-heap of *all* items and just take the first k (no eviction) — simpler when k is close to n anyway.","correct":"Decide the final iteration order first: you want the result sorted by (frequency descending, word ascending).","distractors":["A min-heap of *all* n points sorted by distance and popping k times is O(n + k log n) — fine, but it processes and heapifies all n points upfront.","At each step, take the two least-frequent remaining nodes/symbols and merge them into a new node whose frequency is their sum, inserting it back into a min-heap.","Because the array is sorted ascending."]},{"id":"cs_fundamentals/dsa_patterns#1f0389763dd2","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"Why does the streaming KthLargest class need to keep the heap between calls?","answerFull":"Because each add(val) must answer \"what is the k-th largest *over all values seen so far*,\" not just over the latest batch. Recomputing from scratch on every call would be O(n log n) per call. By keeping a persistent min-heap of size k, each add is O(log k): push the new value, and if the heap now has more than k elements, pop the smallest — the root is always the current k-th largest.","correct":"Because each add(val) must answer \"what is the k-th largest *over all values seen so far*,\" not just over the latest batch.","distractors":["O(n) — every inserted element lives in exactly one of the two heaps, forever (for the streaming median problem, you must retain all history to support the running median).","When you don't need subsets in any particular order and want to avoid Python recursion overhead (function call costs add up across 2^n calls).","The heap doesn't represent \"the answer\" directly; it represents your *current best guess* at the top-k, and its root is the weakest member of that guess — the element most likely to be displaced by a better candidate."]},{"id":"cs_fundamentals/dsa_patterns#2b8240404e41","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"What's the bucket-sort alternative to a heap for Top K Frequent Elements, and when is it strictly better?","answerFull":"Since element frequencies are bounded by n (an element can appear at most n times), allocate n + 1 buckets indexed by frequency. Place each unique value into buckets[frequency]. Then iterate buckets from index n down to 1, collecting values until you have k. This is O(n) total — strictly better than O(n log k) — and is a strong \"optimize further\" answer once the heap solution is established.","correct":"Since element frequencies are bounded by n (an element can appear at most n times), allocate n + 1 buckets indexed by frequency.","distractors":["Think of it as a timeline sweep: sort all start times and all end times independently.","Seed a min-heap with the first element of each row (or just row 0's first min(k, n) elements with pointers), each tagged with its row and column.","At each step, take the two least-frequent remaining nodes/symbols and merge them into a new node whose frequency is their sum, inserting it back into a min-heap."]},{"id":"cs_fundamentals/dsa_patterns#eae5b3fe199a","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"K Closest Points to Origin — why a max-heap of size k instead of a min-heap of all points?","answerFull":"A min-heap of *all* n points sorted by distance and popping k times is O(n + k log n) — fine, but it processes and heapifies all n points upfront. A max-heap of size k mirrors the \"k largest\" pattern but inverted: keep the k *closest* (smallest distances) by evicting the *farthest* (largest distance) when the heap exceeds size k. This is O(n log k), better when k << n.","correct":"A min-heap of *all* n points sorted by distance and popping k times is O(n + k log n) — fine, but it processes and heapifies all n points upfront.","distractors":["If k == n, the heap-of-size-k approach degenerates to holding the entire input — equivalent to sorting, so O(n log n); prefer sorting directly in that case.","In median-finding, the two heaps represent a size-balanced split of one sorted sequence (smaller half / larger half), and elements move between them to *maintain* that balance.","Concatenating and sorting is O(N log N) and throws away the fact that each input is *already sorted*."]},{"id":"cs_fundamentals/dsa_patterns#612d1ba31163","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":8,"difficulty":"intermediate","question":"What's the space complexity tradeoff — O(k) heap vs O(n) frequency map?","answerFull":"For \"top-k frequent\" problems, you need O(u) space for the frequency map (u = number of unique elements, u <= n) regardless of approach — that's unavoidable since you must count occurrences. The heap itself only adds O(k) on top of that. The \"O(k) space\" claim usually refers to the *additional* structure beyond any necessary preprocessing, not the total memory footprint.","correct":"For \"top-k frequent\" problems, you need O(u) space for the frequency map (u = number of unique elements, u <= n) regardless of approach — that's unavoidable since you must count occurrences.","distractors":["For *counting*, the hashmap stores prefix_value -> count of occurrences (a defaultdict(int)), because multiple earlier indices can have the same prefix value, each contributing one valid subarray.","At each step, take the two least-frequent remaining nodes/symbols and merge them into a new node whose frequency is their sum, inserting it back into a min-heap.","Treat it as a fresh Match step on the SAME problem: \"With a stream, I can't build the full frequency map upfront."]},{"id":"cs_fundamentals/dsa_patterns#983d0a981873","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":9,"difficulty":"intermediate","question":"How does the K-th Smallest in a Sorted Matrix heap approach work, and how does it compare to binary search on the answer?","answerFull":"Seed a min-heap with the first element of each row (or just row 0's first min(k, n) elements with pointers), each tagged with its row and column. Pop the smallest k times, each time pushing that element's right neighbor in the same row. After k pops, the last popped value is the answer — O(k log n). Binary search on the answer instead searches the *value range* [matrix[0][0], matrix[n-1][n-1]], counting elements <= mid via a per-row binary search or staircase walk — O(n log(max-min)) or O(n log n log(range)). For large k (close to n^2), binary search on the answer is usually faster since its cost doesn't scale with k.","correct":"Seed a min-heap with the first element of each row (or just row 0's first min(k, n) elements with pointers), each tagged with its row and column.","distractors":["For \"top-k frequent\" problems, you need O(u) space for the frequency map (u = number of unique elements, u <= n) regardless of approach — that's unavoidable since you must count occurrences.","The three-phase approach exploits the precondition that the input is *already sorted and non-overlapping* — a single linear scan suffices.","Binary search on the answer is O(n * log(range))."]},{"id":"cs_fundamentals/dsa_patterns#655ba1cfb08f","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":10,"difficulty":"intermediate","question":"What happens if k == n or k > n?","answerFull":"If k == n, the heap-of-size-k approach degenerates to holding the entire input — equivalent to sorting, so O(n log n); prefer sorting directly in that case. If k > n (more requested than available), most problems either guarantee 1 <= k <= n as a constraint, or expect you to clamp k = min(k, n) and return everything — always check the constraints section for this edge case before coding.","correct":"If k == n, the heap-of-size-k approach degenerates to holding the entire input — equivalent to sorting, so O(n log n); prefer sorting directly in that case.","distractors":["Concatenating and sorting is O(N log N) and throws away the fact that each input is *already sorted*.","The three-phase approach exploits the precondition that the input is *already sorted and non-overlapping* — a single linear scan suffices.","Heap: O(n log k) time, O(k) space, works on streams, gives you all k elements (unordered)."]},{"id":"cs_fundamentals/dsa_patterns#77599f299f35","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":11,"difficulty":"advanced","question":"Python's heapq only provides a min-heap — how do tuple comparisons behave when the second element is a non-comparable object (e.g., a list of points)?","answerFull":"Python compares tuples element-by-element, falling through to the second element only if the first elements are equal. If two points have the *exact same* (-dist_sq) value and the second tuple element is a list (as in k_closest_points), Python *will* attempt to compare the lists, which works for lists of comparable types (ints) but raises TypeError for non-comparable objects (e.g., custom class instances without lt). The fix is to break ties explicitly — push a third tiebreaker element (e.g., an insertion index) into the tuple, e.g., (-dist_sq, i, point), so the heap never needs to compare point objects directly.","correct":"Python compares tuples element-by-element, falling through to the second element only if the first elements are equal.","distractors":["A min-heap of *all* n points sorted by distance and popping k times is O(n + k log n) — fine, but it processes and heapifies all n points upfront.","The recursive calls share a *single* mutable path (or used array) by reference — there's only one list object, repeatedly appended to and read from at different points in the recursion.","heapq compares tuples element-by-element; if the first elements (node.val) are equal, it falls through to comparing the second elements — here, ListNode objects, which have no lt defined, raising TypeError."]},{"id":"cs_fundamentals/dsa_patterns#f59cade42482","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"Why does topological sort only make sense for DAGs?","answerFull":"A topological order requires that for every edge u -> v, u comes before v. If the graph has a cycle a -> b -> c -> a, then a must come before b, b before c, and c before a — a contradiction. No linear ordering can satisfy all three simultaneously, so a cycle makes topological order *undefined*. This is also why both algorithms double as cycle detectors: \"can't produce a full ordering\" == \"graph has a cycle.\"","correct":"A topological order requires that for every edge u -> v, u comes before v.","distractors":["Every node in a cycle has at least one incoming edge *from within the cycle* that can never be \"removed\" (because the node providing that edge can never reach in-degree 0 either — it's circular).","Flatten all employees' intervals into a single list, sort by start time, and run the standard merge to get the union of all busy times.","Process nodes in topological order, maintaining dist[node] = the longest path ending at node."]},{"id":"cs_fundamentals/dsa_patterns#0afa70f41caa","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"Kahn's (BFS) vs. DFS post-order — when would you prefer one over the other?","answerFull":"Kahn's algorithm is usually more intuitive to implement and explain in an interview because the in-degree-zero queue directly mirrors \"what can I do right now\" — and cycle detection is a single length check at the end. DFS post-order is preferable when you're already doing a DFS for another reason (e.g., combined with other graph computations) and want to bolt on ordering without a separate pass. Both are O(V+E); pick whichever maps more naturally to how you're already modeling the problem.","correct":"Kahn's algorithm is usually more intuitive to implement and explain in an interview because the in-degree-zero queue directly mirrors \"what can I do right now\" — and cycle detection is a single length check at the end.","distractors":["Top-down (recursion + @lru_cache or a dict) is often easier to *write* directly from the recurrence — it mirrors the brute-force solution with one line added.","Replace the FIFO deque in Kahn's algorithm with a min-heap (heapq).","The used[] array approach (Template 2) is more intuitive and naturally supports the duplicate-skipping check for Permutations II."]},{"id":"cs_fundamentals/dsa_patterns#1ad83f5a9d4b","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"How exactly does Kahn's algorithm detect a cycle?","answerFull":"Every node in a cycle has at least one incoming edge *from within the cycle* that can never be \"removed\" (because the node providing that edge can never reach in-degree 0 either — it's circular). So nodes inside a cycle (and anything only reachable through them) never reach in-degree 0 and are never enqueued. At the end, len(order) < num_nodes reveals this — the missing nodes are exactly those involved in or downstream of a cycle.","correct":"Every node in a cycle has at least one incoming edge *from within the cycle* that can never be \"removed\" (because the node providing that edge can never reach in-degree 0 either — it's circular).","distractors":["For an undirected edge (u, v): if find(u) == find(v) *before* you union them, then u and v were already connected via some other path — adding edge (u, v) would create a cycle.","in_degree[v] must count *every* edge (u, v) in the input — initialize it to all zeros, then for each edge (u, v), do in_degree[v] += 1.","Greedy makes each choice ONCE, in a fixed order, and never revisits it — the algorithm has a clear termination point and a provable bound (when correct)."]},{"id":"cs_fundamentals/dsa_patterns#dba308f7f943","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":4,"difficulty":"core","question":"What do the three colors (white/gray/black) mean in DFS-based cycle detection, and why isn't a simple boolean visited enough?","answerFull":"WHITE = never visited; GRAY = currently on the DFS call stack (an ancestor of the current node in the DFS tree); BLACK = fully processed (this node and all its descendants are done). A boolean visited can't distinguish \"currently being explored\" (GRAY) from \"fully done\" (BLACK) — but that distinction is exactly what detects a cycle: an edge to a GRAY node is a back edge to an ancestor, meaning a cycle exists. An edge to a BLACK node is a cross/forward edge — perfectly fine, just means you reached an already-finished subtree via a different path (not a cycle).","correct":"WHITE = never visited; GRAY = currently on the DFS call stack (an ancestor of the current node in the DFS tree); BLACK = fully processed (this node and all its descendants are done).","distractors":["board[r][c] = \"#\" is used as the \"currently on this DFS path\" marker — it must be scoped to the current call stack, not permanent.","A tree has no cycles and exactly one path from the root to any node, so a recursive/BFS walk can never revisit a node.","O(h) for the call stack, where h is the tree's height."]},{"id":"cs_fundamentals/dsa_patterns#f2e26e75fdc7","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"Why must the DFS-based approach reverse the post-order list?","answerFull":"A node is appended to the post-order list only after *all* nodes reachable from it have been appended. So the node that \"unlocks\" the most other nodes (i.e., has the most things depending on it, transitively) finishes last and ends up at the end of the unreversed list — but it actually belongs first, since everything depends on it. Reversing flips \"finished last\" into \"appears first,\" which is what topological order requires. See the BROKEN->FIX in §8 for a concrete trace.","correct":"A node is appended to the post-order list only after *all* nodes reachable from it have been appended.","distractors":["board[r][c] = \"#\" is used as the \"currently on this DFS path\" marker — it must be scoped to the current call stack, not permanent.","With the standard template (while fast and fast.next), for a list of length 2m, slow ends at the (m+1)-th node — the *second* of the two middle nodes.","path is a single mutable list object, repeatedly appended to and popped from throughout the entire recursion."]},{"id":"cs_fundamentals/dsa_patterns#efb0b3c6fc1f","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"If multiple valid topological orderings exist, how do you produce a specific one (e.g., the lexicographically smallest)?","answerFull":"Replace the FIFO deque in Kahn's algorithm with a min-heap (heapq). At each step, instead of popping arbitrary order, pop the smallest-valued node currently at in-degree 0. This greedily picks the smallest available choice at every step, which produces the lexicographically smallest valid ordering overall — a classic greedy-with-topo-sort combination.","correct":"Replace the FIFO deque in Kahn's algorithm with a min-heap (heapq).","distractors":["Kahn's algorithm is usually more intuitive to implement and explain in an interview because the in-degree-zero queue directly mirrors \"what can I do right now\" — and cycle detection is a single length check at the end.","A deque supports O(1) push/pop from both ends, vs. O(log V) for a heap.","Seed a min-heap with the first element of each row (or just row 0's first min(k, n) elements with pointers), each tagged with its row and column."]},{"id":"cs_fundamentals/dsa_patterns#89f6cebfc5db","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"How do you build the graph for Alien Dictionary when you're not given edges directly?","answerFull":"Compare each pair of *adjacent* words in the given (sorted) word list. Find the first index where the two words differ — that pair of characters gives you one directed edge (earlier-word's character -> later-word's character). You only get information from the *first* differing character of each adjacent pair; characters after that point give no ordering information. Special case: if word A is a prefix of word B but A is *longer* than B (e.g., \"abc\" before \"ab\"), no valid ordering exists — return immediately.","correct":"Compare each pair of *adjacent* words in the given (sorted) word list.","distractors":["If you're given the *whole* graph upfront and need to answer connectivity questions once (e.g., \"how many connected components does this static graph have?\"), DFS/BFS is simpler to write and equally efficient — O(V+E).","Comparing every pair of n words to check \"differs by one letter\" costs O(n² · L) where L is word length.","Any shortest *simple* path (no repeated nodes) in a graph with V nodes has at most V-1 edges."]},{"id":"cs_fundamentals/dsa_patterns#a4929a979d00","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":8,"difficulty":"intermediate","question":"How does topological sort enable \"longest path in a DAG\" (Parallel Courses)?","answerFull":"Process nodes in topological order, maintaining dist[node] = the longest path ending at node. For each edge u -> v processed when you pop u, update dist[v] = max(dist[v], dist[u] + 1). Because u is processed *before* v in topo order (guaranteed by the definition of topological order), dist[u] is already final by the time it's used to update dist[v] — this is the key invariant that makes the DP correct in a single pass.","correct":"Process nodes in topological order, maintaining dist[node] = the longest path ending at node.","distractors":["The problem states leftover nodes (fewer than k) at the end should remain in their original order, NOT be reversed.","A tree has no cycles and exactly one path from the root to any node, so a recursive/BFS walk can never revisit a node.","BFS visits nodes in non-decreasing depth order — the very first leaf it encounters is *guaranteed* to be at the minimum depth across the whole tree, so you can return immediately."]},{"id":"cs_fundamentals/dsa_patterns#3f42f454c60c","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":10,"difficulty":"intermediate","question":"A common bug: initializing in_degree incorrectly. What's the right way?","answerFull":"in_degree[v] must count *every* edge (u, v) in the input — initialize it to all zeros, then for each edge (u, v), do in_degree[v] += 1. A common mistake is initializing in_degree to 1 for all nodes (confusing it with \"at least one prerequisite exists for everything\") or forgetting to count multi-edges (if the input can contain duplicate prerequisite pairs, each duplicate must still increment in-degree, or the queue will release a node \"too early\" relative to a duplicate edge that was never decremented).","correct":"in_degree[v] must count *every* edge (u, v) in the input — initialize it to all zeros, then for each edge (u, v), do in_degree[v] += 1.","distractors":["find(x) recurses up to the root, then on the way back *down* the call stack, sets parent[node] = root for every node visited.","A topological order requires that for every edge u -> v, u comes before v.","Every node in a cycle has at least one incoming edge *from within the cycle* that can never be \"removed\" (because the node providing that edge can never reach in-degree 0 either — it's circular)."]},{"id":"cs_fundamentals/dsa_patterns#f68e65ac94be","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":11,"difficulty":"advanced","question":"How would Sequence Reconstruction (LC 444) differ from Course Schedule II — both build a graph from pairwise orderings?","answerFull":"Course Schedule II just needs *a* valid ordering. Sequence Reconstruction needs to verify the *unique* valid ordering equals a specific target sequence. This requires checking, at every step of Kahn's algorithm, that the queue contains exactly one node with in-degree 0 (if more than one node is ready simultaneously, the ordering isn't uniquely determined) — and that the resulting order matches the target exactly.","correct":"Course Schedule II just needs *a* valid ordering.","distractors":["The window is valid if you can convert it to all-the-same-character using at most k replacements.","For *counting*, the hashmap stores prefix_value -> count of occurrences (a defaultdict(int)), because multiple earlier indices can have the same prefix value, each contributing one valid subarray.","UnionFindBroken(3) allocates parent = [0, 1, 2] — valid indices are 0, 1, 2."]},{"id":"cs_fundamentals/dsa_patterns#fe4822d993bf","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":12,"difficulty":"advanced","question":"Why doesn't Union-Find work for detecting cycles in directed graphs the way it does for undirected graphs?","answerFull":"Union-Find merges two nodes into the same set when an edge connects them, treating the edge as bidirectional for connectivity purposes. But a directed cycle a -> b -> a and two separate directed edges a -> b and b -> a that *aren't* part of a cycle in context look identical to Union-Find — it can't distinguish edge *direction*. Detecting a directed cycle requires tracking the current DFS path (the GRAY state in 3-color DFS) — information Union-Find doesn't maintain.","correct":"Union-Find merges two nodes into the same set when an edge connects them, treating the edge as bidirectional for connectivity purposes.","distractors":["find(x) recurses up to the root, then on the way back *down* the call stack, sets parent[node] = root for every node visited.","A visited set only answers \"have I processed this node?\" — it doesn't tell you *what the corresponding new node is*.","A DAG has no cycles, so \"negative cycle\" is impossible by definition — Bellman-Ford's only advantage over Dijkstra (negative-weight tolerance) comes \"for free\" with no extra cost."]},{"id":"cs_fundamentals/dsa_patterns#5a9bd2985c63","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"Why must level_size = len(queue) be captured *before* the inner loop, and what happens if you don't?","answerFull":"Because the inner loop both pops nodes from the current level and pushes their children (next level) onto the same queue. If the loop condition re-evaluates len(queue) on each iteration (or uses while queue:), it sees the growing queue and never stops at the level boundary — collapsing all levels into one flat list (see §8). Snapshotting level_size once gives the inner loop a fixed iteration count, decoupling \"how many to pop this round\" from \"how many got pushed.\"","correct":"Because the inner loop both pops nodes from the current level and pushes their children (next level) onto the same queue.","distractors":["Each element is pushed exactly once (once per iteration of the outer loop) and popped at most once (across the entire algorithm's lifetime, total pops <= total pushes <= n).","A dict only allocates entries for characters actually present — better for sparse alphabets (Unicode, mixed case) or when most nodes have few children.","Fixed-size: a single for loop where right and left = right - k + 1 move in lockstep — no inner loop needed, just \"add nums[right], remove nums[right-k]\" each iteration."]},{"id":"cs_fundamentals/dsa_patterns#b34a30284264","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"Zigzag level order — why use a deque for the per-level result instead of a list with reverse()?","answerFull":"Both work, but deque.appendleft() is O(1), while building a list normally and then calling .reverse() on alternate levels is O(level_size) extra work per reversed level. Using a deque and choosing append vs appendleft based on the current direction avoids the separate reversal pass entirely — marginal for small trees, but it's the \"why settle for O(n) extra work when O(1) per element suffices\" instinct interviewers like to see.","correct":"Both work, but deque.appendleft() is O(1), while building a list normally and then calling .reverse() on alternate levels is O(level_size) extra work per reversed level.","distractors":["Decide the final iteration order first: you want the result sorted by (frequency descending, word ascending).","A deque supports O(1) push/pop from both ends, vs. O(log V) for a heap.","Push every node (or value) onto a stack while traversing, then pop to build the reversed list (or overwrite values in place in reverse order)."]},{"id":"cs_fundamentals/dsa_patterns#ad660bd25d3b","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"Why does BFS find min_depth correctly while a naive DFS might not, without extra logic?","answerFull":"BFS visits nodes in non-decreasing depth order — the very first leaf it encounters is *guaranteed* to be at the minimum depth across the whole tree, so you can return immediately. A naive DFS that returns 1 + min(left_depth, right_depth) would incorrectly treat a missing child (None) as depth 0, potentially reporting a shorter \"depth\" through a non-existent branch — DFS solutions for this problem need an explicit check for \"this node has only one child\" to avoid that trap, which BFS sidesteps entirely by construction.","correct":"BFS visits nodes in non-decreasing depth order — the very first leaf it encounters is *guaranteed* to be at the minimum depth across the whole tree, so you can return immediately.","distractors":["For small, fixed k (say k <= 4), a linear scan over k pointers to find the minimum each step is O(N * k) — simpler code, and for small constant k, O(N * k) and O(N log k) are both effectively O(N).","A tree has no cycles and exactly one path from the root to any node, so a recursive/BFS walk can never revisit a node.","If you're given the *whole* graph upfront and need to answer connectivity questions once (e.g., \"how many connected components does this static graph have?\"), DFS/BFS is simpler to write and equally efficient — O(V+E)."]},{"id":"cs_fundamentals/dsa_patterns#3f152de71627","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"How do you link \"next\" pointers between siblings using only the level-size loop?","answerFull":"Within the per-level loop, maintain a prev pointer (reset to None at the start of each level). For each node popped, if prev is not None: prev.next = node, then set prev = node. After the level's loop, the last node's next remains None (its default), correctly marking the end of that level's chain. No extra queue or array is needed beyond the BFS queue itself.","correct":"Within the per-level loop, maintain a prev pointer (reset to None at the start of each level).","distractors":["Because the inner loop both pops nodes from the current level and pushes their children (next level) onto the same queue.","The two-pointer scan itself is O(n) (each pointer moves at most n times total, so the loop body runs at most O(n) times even though there are two indices).","The order must be: (1) save next_temp = curr.next, (2) curr.next = prev, (3) prev = curr, (4) curr = next_temp."]},{"id":"cs_fundamentals/dsa_patterns#d8d9dc0648e7","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":8,"difficulty":"intermediate","question":"How does the template generalize to N-ary trees (LC 429)?","answerFull":"Replace if node.left: queue.append(node.left) / if node.right: ... with a single loop: for child in node.children: queue.append(child). Everything else — the level_size snapshot, the per-level result list — is unchanged. This is a good example of how the *shape* of the pattern (queue + level-size snapshot) is independent of the tree's branching factor.","correct":"Replace if node.left: queue.append(node.left) / if node.right: ...","distractors":["Because the algorithm's correctness depends on monotonicity: moving left right strictly increases the candidate sum, and moving right left strictly decreases it.","By the BST invariant, every node's left subtree contains only smaller values and its right subtree only larger values.","Change dfs from a None-returning side-effecting function to one that *returns* 1 + dfs(up) + dfs(down) + dfs(left) + dfs(right) (with the same \"sink before recurse\" rule, returning 0 for out-of-bounds/water cells)."]},{"id":"cs_fundamentals/dsa_patterns#68b38acead45","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":9,"difficulty":"intermediate","question":"Cousins in Binary Tree — why track both parent and depth in the queue, and isn't depth alone enough?","answerFull":"\"Cousins\" requires same depth and different parents (siblings have the same parent and are excluded). Depth alone can't distinguish siblings from cousins — two nodes at the same depth could be siblings (same parent, disqualified) or cousins (different parents, qualified). Carrying (node, parent, depth) triples in the queue lets you check both conditions in O(1) once you've located the two target nodes during the single BFS pass.","correct":"\"Cousins\" requires same depth and different parents (siblings have the same parent and are excluded).","distractors":["For *counting*, the hashmap stores prefix_value -> count of occurrences (a defaultdict(int)), because multiple earlier indices can have the same prefix value, each contributing one valid subarray.","Each recursive call branches into multiple choices, and the recursion depth equals the solution length — so the total number of leaves in the search tree is O(branching_factor ^ depth), which is 2^n (subsets) or n!","They're related (both use two pointers at different \"positions\" on the same structure) but solve different problems."]},{"id":"cs_fundamentals/dsa_patterns#16a82fba1f46","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":10,"difficulty":"intermediate","question":"Maximum Width of Binary Tree — why use positional indices, and how do you prevent them from overflowing for deep trees?","answerFull":"The \"width\" of a level is defined by the *positions* of the leftmost and rightmost nodes as if the tree were complete — including gaps from missing nodes. Assigning root = 1, left child = 2*i, right child = 2*i + 1 encodes this complete-tree position. For deep, sparse trees these indices can grow exponentially (2^depth), risking overflow in fixed-width integer languages. The fix: at the start of each level, subtract the first index of that level from every index before using it — this re-bases indices to start near 0 each level, keeping them small while preserving relative differences (which is all last - first + 1 needs).","correct":"The \"width\" of a level is defined by the *positions* of the leftmost and rightmost nodes as if the tree were complete — including gaps from missing nodes.","distractors":["The two-pointer scan itself is O(n) (each pointer moves at most n times total, so the loop body runs at most O(n) times even though there are two indices).","bisect_left(arr, x) returns the leftmost position where x can be inserted — i.e., the first index i with arr[i] >= x (this is lower_bound).","For *counting*, the hashmap stores prefix_value -> count of occurrences (a defaultdict(int)), because multiple earlier indices can have the same prefix value, each contributing one valid subarray."]},{"id":"cs_fundamentals/dsa_patterns#a8a3af1cf18e","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"When do you use preorder, inorder, or postorder traversal?","answerFull":"Preorder (node, left, right) when you need to process a node *before* its children — serialization (the root must be written first to be read first), or copying/cloning a tree top-down. Inorder (left, node, right) specifically for BSTs — it visits nodes in sorted key order, useful for \"k-th smallest,\" validation, or converting a BST to a sorted list. Postorder (left, right, node) when a node's computation *depends on* its children's results — heights, sums, diameters, deletions (you must process children before you can safely detach/free the parent).","correct":"Preorder (node, left, right) when you need to process a node *before* its children — serialization (the root must be written first to be read first), or copying/cloning a tree top-down.","distractors":["The recursive construction repeatedly needs \"where does preorder[0] (the current root) appear in the current inorder slice?\" to split it into left/right subtree slices.","find(x) recurses up to the root, then on the way back *down* the call stack, sets parent[node] = root for every node visited.","By the BST invariant, every node's left subtree contains only smaller values and its right subtree only larger values."]},{"id":"cs_fundamentals/dsa_patterns#32624d050aba","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"Why does inorder traversal of a BST yield sorted order?","answerFull":"By the BST invariant, every node's left subtree contains only smaller values and its right subtree only larger values. Inorder visits \"everything smaller than this node\" (left subtree, recursively sorted), \"this node,\" then \"everything larger\" (right subtree, recursively sorted) — by induction, the entire sequence is sorted.","correct":"By the BST invariant, every node's left subtree contains only smaller values and its right subtree only larger values.","distractors":["The BST property is global, not just local: every node in the *entire* left subtree must be less than the current node, not just the immediate left child.","Rebalancing moves elements between heaps based purely on counts (len(left) vs len(right)) to restore the size invariant — it has no mechanism to verify max(left) <= min(right).","Preorder (node, left, right) when you need to process a node *before* its children — serialization (the root must be written first to be read first), or copying/cloning a tree top-down."]},{"id":"cs_fundamentals/dsa_patterns#d267defc8f4e","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"Diameter of Binary Tree — why track a separate nonlocal diameter instead of just returning the diameter from each call?","answerFull":"The function's *return value* must be something the parent can use to compute *its own* combination — here, the subtree's height (a single number representing \"how far down does this subtree extend\"). The diameter *through* a node (left_height + right_height) is a different quantity that the parent can't directly use (the parent only cares about height for its own diameter-through-itself calculation). Two distinct purposes need two distinct channels: the return value (height, for parents) and a side-channel (nonlocal diameter, for the final answer).","correct":"The function's *return value* must be something the parent can use to compute *its own* combination — here, the subtree's height (a single number representing \"how far down does this subtree extend\").","distractors":["O(h) for the call stack, where h is the tree's height.","The BST property is global, not just local: every node in the *entire* left subtree must be less than the current node, not just the immediate left child.","Each recursive call branches into multiple choices, and the recursion depth equals the solution length — so the total number of leaves in the search tree is O(branching_factor ^ depth), which is 2^n (subsets) or n!"]},{"id":"cs_fundamentals/dsa_patterns#79788f553a14","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":4,"difficulty":"core","question":"How does the recursive LCA algorithm work — why does \"found in both subtrees\" mean the current node is the answer?","answerFull":"lowest_common_ancestor returns non-None from a subtree iff that subtree contains p, q, or (already-found) their LCA. If the left recursive call returns non-None AND the right does too, that means p is in one subtree and q is in the other — the current node is exactly the point where their paths from the root diverge, which is the definition of LCA. If only one side returns non-None, both p and q (or their LCA) are in that single subtree, so bubble that result up unchanged.","correct":"lowest_common_ancestor returns non-None from a subtree iff that subtree contains p, q, or (already-found) their LCA.","distractors":["WHITE = never visited; GRAY = currently on the DFS call stack (an ancestor of the current node in the DFS tree); BLACK = fully processed (this node and all its descendants are done).","n & (-n) ISOLATES the lowest set bit (returns a value with only that one bit set), whereas n & (n-1) CLEARS it (returns n with that bit removed).","The recursive construction repeatedly needs \"where does preorder[0] (the current root) appear in the current inorder slice?\" to split it into left/right subtree slices."]},{"id":"cs_fundamentals/dsa_patterns#38953fec5226","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"Validate BST — why is checking node.val > node.left.val and node.val < node.right.val (immediate children only) insufficient?","answerFull":"The BST property is global, not just local: every node in the *entire* left subtree must be less than the current node, not just the immediate left child. A tree like 5 -> (left: 1 -> (right: 6)) has 1 < 5 (immediate check passes) but 6 (in 5's left subtree) is > 5, violating the BST property. The (low, high) bounds-passing approach correctly propagates *all* ancestor constraints down the recursion, not just the immediate parent's value.","correct":"The BST property is global, not just local: every node in the *entire* left subtree must be less than the current node, not just the immediate left child.","distractors":["Checking every (start, end) pair of nodes along root-to-X paths is O(n^2) (for each node, walk up/down to try all paths through it).","Each child's required candy count depends on comparisons with BOTH neighbors simultaneously, but a single left-to-right (or right-to-left) pass can only enforce one direction's constraint at a time.","The recursive construction repeatedly needs \"where does preorder[0] (the current root) appear in the current inorder slice?\" to split it into left/right subtree slices."]},{"id":"cs_fundamentals/dsa_patterns#abf8ed0fcb3c","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"Maximum Path Sum — why must you clip negative subtree results to 0?","answerFull":"A \"path\" extending into a subtree adds that subtree's contribution to the running sum. If a subtree's best downward path sum is *negative*, including it would make the total *worse* than not extending into it at all — so the optimal choice is to not extend (contribute 0). max(dfs(child), 0) encodes \"either extend into this subtree if it helps, or stop here\" — this is why both left and right are clamped before being used in either the best update or the returned value.","correct":"A \"path\" extending into a subtree adds that subtree's contribution to the running sum.","distractors":["The function's *return value* must be something the parent can use to compute *its own* combination — here, the subtree's height (a single number representing \"how far down does this subtree extend\").","O(n) — every inserted element lives in exactly one of the two heaps, forever (for the streaming median problem, you must retain all history to support the running median).","The BST property is global, not just local: every node in the *entire* left subtree must be less than the current node, not just the immediate left child."]},{"id":"cs_fundamentals/dsa_patterns#add0a4ad0d9b","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"Path Sum III — why prefix sum + hashmap instead of checking every possible path directly?","answerFull":"Checking every (start, end) pair of nodes along root-to-X paths is O(n^2) (for each node, walk up/down to try all paths through it). The prefix-sum trick treats the root-to-current-node path like a 1D array: maintain a running sum S from the root to the current node in a hashmap of {prefix_sum: count}; at each node, the number of valid paths *ending here* with sum target equals count[S - target] (mirroring subarray sum equals k). Increment count[S] on the way down, decrement it on the way back up (backtracking) so sibling subtrees don't see each other's path sums — giving O(n) overall.","correct":"Checking every (start, end) pair of nodes along root-to-X paths is O(n^2) (for each node, walk up/down to try all paths through it).","distractors":["For a single pair-sum query, a hashmap *is* asymptotically better (O(n) vs O(n log n)).","find(x) recurses up to the root, then on the way back *down* the call stack, sets parent[node] = root for every node visited.","At each step, take the two least-frequent remaining nodes/symbols and merge them into a new node whose frequency is their sum, inserting it back into a min-heap."]},{"id":"cs_fundamentals/dsa_patterns#766db169b975","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":8,"difficulty":"intermediate","question":"What's the space complexity of recursive tree DFS, and how would you avoid hitting Python's recursion limit on a skewed tree?","answerFull":"O(h) for the call stack, where h is the tree's height. For a balanced tree, h = O(log n) — fine. For a skewed tree (effectively a linked list), h = O(n), and Python's default recursion limit (~1000) can be exceeded for n > ~1000, raising RecursionError. The fix is an iterative DFS with an explicit stack (stack = [root], push/pop manually) — same O(n) space, but on the heap (a Python list) rather than the interpreter's call stack, which has no comparable hard limit.","correct":"O(h) for the call stack, where h is the tree's height.","distractors":["When you don't need subsets in any particular order and want to avoid Python recursion overhead (function call costs add up across 2^n calls).","Because the guard condition grid[r][c] != \"1\" is the *only* thing that stops recursion, and it's checked at the *top* of each call using the grid's current state.","WHITE = never visited; GRAY = currently on the DFS call stack (an ancestor of the current node in the DFS tree); BLACK = fully processed (this node and all its descendants are done)."]},{"id":"cs_fundamentals/dsa_patterns#ca10c16d492d","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":9,"difficulty":"intermediate","question":"Construct Binary Tree from Preorder and Inorder — why precompute a hashmap of inorder value-to-index, and what breaks without it?","answerFull":"The recursive construction repeatedly needs \"where does preorder[0] (the current root) appear in the current inorder slice?\" to split it into left/right subtree slices. A linear search for this index on every call makes the overall algorithm O(n^2) (one O(n) search per node). Precomputing {value: index} once makes each lookup O(1), bringing the total to O(n). This requires all values to be unique — a constraint the problem guarantees.","correct":"The recursive construction repeatedly needs \"where does preorder[0] (the current root) appear in the current inorder slice?\" to split it into left/right subtree slices.","distractors":["A hashmap gives O(1) for exact-match lookups, but \"does any key start with 'app'?\" requires either iterating all keys (O(n·L)) or maintaining a separate prefix index.","A tree has no cycles and exactly one path from the root to any node, so a recursive/BFS walk can never revisit a node.","Maintain a dynamic array (list) for O(1) random access (getRandom picks random.choice by index) and a hashmap value -> index_in_array."]},{"id":"cs_fundamentals/dsa_patterns#d5f159293e64","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":10,"difficulty":"intermediate","question":"Subtree of Another Tree — when is the naive O(n*m) approach acceptable, and what's the O(n+m) alternative?","answerFull":"O(n*m) (for each of n nodes in the main tree, run an O(m) equality check against the target) is fine for typical interview constraints (n, m <= 1000, giving 10^6 operations). The O(n+m) alternative serializes both trees to strings with explicit null markers and delimiters (to avoid \"12\" ambiguously matching both a node 12 and nodes 1, 2), then checks if the target's serialization is a substring of the main tree's — substring search (e.g., KMP) is O(n+m). Mention both; the naive approach is usually sufficient unless the interviewer explicitly asks for better.","correct":"O(n*m) (for each of n nodes in the main tree, run an O(m) equality check against the target) is fine for typical interview constraints (n, m <= 1000, giving 10^6 operations).","distractors":["O(V^3) is fine for V up to a few hundred (10^6-10^8 operations).","In an interview, rarely — O(1) space is almost always preferred when achievable, and the iterative version isn't meaningfully harder to write once memorized.","Kahn's algorithm is usually more intuitive to implement and explain in an interview because the in-degree-zero queue directly mirrors \"what can I do right now\" — and cycle detection is a single length check at the end."]},{"id":"cs_fundamentals/dsa_patterns#e7a76510775d","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"Why is a trie faster than a hashmap for prefix queries, given that hashmap lookups are O(1)?","answerFull":"A hashmap gives O(1) for exact-match lookups, but \"does any key start with 'app'?\" requires either iterating all keys (O(n·L)) or maintaining a separate prefix index. A trie answers \"does any word start with 'app'?\" in O(L) — just walk 4 characters from the root and check if the resulting node exists — *regardless* of how many words are stored. The hashmap's O(1) only applies to the exact query it was built for; prefix queries are not what hashing optimizes for.","correct":"A hashmap gives O(1) for exact-match lookups, but \"does any key start with 'app'?\" requires either iterating all keys (O(n·L)) or maintaining a separate prefix index.","distractors":["Use lo <= hi (with hi = mid - 1 / lo = mid + 1) when you're searching for an *exact match* and want to detect \"not found\" (loop exits with lo > hi).","Maintain a dynamic array (list) for O(1) random access (getRandom picks random.choice by index) and a hashmap value -> index_in_array.","For *counting*, the hashmap stores prefix_value -> count of occurrences (a defaultdict(int)), because multiple earlier indices can have the same prefix value, each contributing one valid subarray."]},{"id":"cs_fundamentals/dsa_patterns#911de402d512","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"When is a trie actually worse than a hashmap (memory-wise)?","answerFull":"When words share few or no prefixes — e.g., a dictionary of random UUIDs. Each TrieNode carries the overhead of a dict (or array) of children plus a boolean flag, multiplied across every character of every word with no sharing. A hashset of strings would store each string once, more compactly. Tries win specifically when prefix-sharing is high (natural-language dictionaries, URLs, IP routing tables).","correct":"When words share few or no prefixes — e.g., a dictionary of random UUIDs.","distractors":["Comparing every pair of n words to check \"differs by one letter\" costs O(n² · L) where L is word length.","Maintain a dynamic array (list) for O(1) random access (getRandom picks random.choice by index) and a hashmap value -> index_in_array.","For a single pair-sum query, a hashmap *is* asymptotically better (O(n) vs O(n log n))."]},{"id":"cs_fundamentals/dsa_patterns#3a8152eba2c7","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"In Word Search II, why build ONE trie from all dictionary words instead of running a separate grid-search for each word?","answerFull":"Running a separate DFS per word costs O(words · rows·cols·4^L) — the grid is re-explored from scratch for every word. With a single shared trie, one DFS pass per starting cell explores all words simultaneously: at each step, ch not in node.children prunes that branch for *every word that doesn't share this prefix*, all at once. Shared prefixes across words (e.g., \"oa\" and \"oat\") mean shared trie traversal too.","correct":"Running a separate DFS per word costs O(words · rows·cols·4^L) — the grid is re-explored from scratch for every word.","distractors":["Running BFS once per source and taking the minimum would be correct but wasteful — O(k · rows · cols) for k sources.","Compare each pair of *adjacent* words in the given (sorted) word list.","When words share few or no prefixes — e.g., a dictionary of random UUIDs."]},{"id":"cs_fundamentals/dsa_patterns#ff758aa4d9c8","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":4,"difficulty":"core","question":"Walk through the BROKEN -> FIX in §8 — why does the bug cause MISSED words rather than just extra time/space?","answerFull":"board[r][c] = \"#\" is used as the \"currently on this DFS path\" marker — it must be scoped to the current call stack, not permanent. If it's never restored, cells visited by the *first* DFS call (from the first outer-loop starting cell) remain \"#\" for *all subsequent* outer-loop calls. Those cells then look like walls to every other DFS, even though they hold perfectly valid letters needed to spell other words — so words requiring those cells are never found. It's a correctness bug, not just an efficiency one.","correct":"board[r][c] = \"#\" is used as the \"currently on this DFS path\" marker — it must be scoped to the current call stack, not permanent.","distractors":["Plain DFS explores a tree and returns; backtracking explores a tree while mutating shared state (a path, a board, a set of used elements) and explicitly restores that state before trying the next sibling branch.","The BST property is global, not just local: every node in the *entire* left subtree must be less than the current node, not just the immediate left child.","WHITE = never visited; GRAY = currently on the DFS call stack (an ancestor of the current node in the DFS tree); BLACK = fully processed (this node and all its descendants are done)."]},{"id":"cs_fundamentals/dsa_patterns#d46e7bac5f8c","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"How does the wildcard . search in Template 2 avoid being O(26^L) for every query?","answerFull":"Worst case (all wildcards, e.g., \"......\"), it genuinely is exponential — O(26^L) — because every position branches into all 26 children. In practice, most branches dead-end quickly (ch not in node.children for most of the 26 options at any real node, since real dictionaries are sparse), so actual runtime is far better than the theoretical worst case. The recursion depth is bounded by O(L) regardless.","correct":"Worst case (all wildcards, e.g., \"......\"), it genuinely is exponential — O(26^L) — because every position branches into all 26 children.","distractors":["Re-running DFS from scratch after each of k additions costs O(k * rows * cols) in the worst case.","Each insertion requires finding the correct position (binary search on starts, O(log n)) and checking for overlap with neighbors (O(1)), but inserting into a Python list is O(n) due to shifting elements.","Building is O(n). For a *single* range-sum query, this is no better than just summing the range directly (also O(n) in the worst case for a large range)."]},{"id":"cs_fundamentals/dsa_patterns#4ff5d90dbf0d","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"For Maximum XOR of Two Numbers, what does each \"level\" of the binary trie represent, and why insert most-significant-bit first?","answerFull":"Each level corresponds to one bit position, from most significant to least significant. Inserting MSB-first means two numbers that differ in a high bit diverge near the *root* of the trie — and high bits contribute more to the XOR's magnitude than low bits (just like decimal place value). The greedy \"pick the opposite bit if available\" strategy at each level is only correct because higher bits are decided first — a greedy choice at the MSB can never be \"undone\" by a better choice at a lower bit, since 2^k > sum(2^0..2^(k-1)).","correct":"Each level corresponds to one bit position, from most significant to least significant.","distractors":["Binary search on the answer is O(n * log(range)).","In median-finding, the two heaps represent a size-balanced split of one sorted sequence (smaller half / larger half), and elements move between them to *maintain* that balance.","The heap doesn't represent \"the answer\" directly; it represents your *current best guess* at the top-k, and its root is the weakest member of that guess — the element most likely to be displaced by a better candidate."]},{"id":"cs_fundamentals/dsa_patterns#fee56f8c300a","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":8,"difficulty":"intermediate","question":"children: dict[str, TrieNode] vs. children: list[Optional[TrieNode]] of size 26 — what's the tradeoff?","answerFull":"A dict only allocates entries for characters actually present — better for sparse alphabets (Unicode, mixed case) or when most nodes have few children. A fixed-size array ([None] * 26) gives O(1) *guaranteed* (no hashing) access and is more cache-friendly, at the cost of allocating 26 slots per node even if only 1-2 are used. For pure lowercase-English problems, the array is common in competitive programming for speed; the dict is more general and common in interview-style Python code.","correct":"A dict only allocates entries for characters actually present — better for sparse alphabets (Unicode, mixed case) or when most nodes have few children.","distractors":["Building is O(n). For a *single* range-sum query, this is no better than just summing the range directly (also O(n) in the worst case for a large range).","Because the inner loop both pops nodes from the current level and pushes their children (next level) onto the same queue.","Top-down (recursion + @lru_cache or a dict) is often easier to *write* directly from the recurrence — it mirrors the brute-force solution with one line added."]},{"id":"cs_fundamentals/dsa_patterns#db692b441cac","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":9,"difficulty":"intermediate","question":"Why do we need an explicit is_end flag — can't we just check len(node.children) == 0 to mean \"this is a complete word\"?","answerFull":"No — \"car\" and \"card\" are both valid words in the same trie, where \"car\" is a prefix of \"card\". The node for \"car\"'s last 'r' has a child ('d', leading to \"card\"), so len(children) != 0, yet \"car\" is itself a complete word. is_end is the *only* way to mark \"a complete word ends here, even though the trie continues beyond this point.\"","correct":"No — \"car\" and \"card\" are both valid words in the same trie, where \"car\" is a prefix of \"card\".","distractors":["For *counting*, the hashmap stores prefix_value -> count of occurrences (a defaultdict(int)), because multiple earlier indices can have the same prefix value, each contributing one valid subarray.","Walk to the node representing the word's last character and set is_end = False.","Compare each pair of *adjacent* words in the given (sorted) word list."]},{"id":"cs_fundamentals/dsa_patterns#be048a638f1f","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":11,"difficulty":"advanced","question":"How would you delete a word from a trie?","answerFull":"Walk to the node representing the word's last character and set is_end = False. If that node now has no children AND is_end == False, it's \"dead weight\" — recursively remove it from its parent's children dict, and repeat the check at the parent (it might now also become dead weight if it has no other children and isn't itself a word's end). This cleanup is optional for correctness (an unreachable is_end=False leaf doesn't affect search/startsWith), but matters for memory if deletions are frequent.","correct":"Walk to the node representing the word's last character and set is_end = False.","distractors":["Decide the final iteration order first: you want the result sorted by (frequency descending, word ascending).","A tree has no cycles and exactly one path from the root to any node, so a recursive/BFS walk can never revisit a node.","Compare each pair of *adjacent* words in the given (sorted) word list."]},{"id":"cs_fundamentals/dsa_patterns#3bb7daf2c301","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"Why a max-heap for the smaller half and a min-heap for the larger half — why not the other way around?","answerFull":"The median sits at the *boundary* between the two halves. To read the boundary in O(1), you need the largest element of the smaller half (closest to the boundary from below) and the smallest element of the larger half (closest from above). A max-heap exposes its largest element at the root in O(1); a min-heap exposes its smallest. Swapping them would put the *farthest-from-the-median* elements at the roots, useless for computing the median.","correct":"The median sits at the *boundary* between the two halves.","distractors":["(1) Find the middle using fast/slow pointers — splits the list into two halves.","Rebalancing moves elements between heaps based purely on counts (len(left) vs len(right)) to restore the size invariant — it has no mechanism to verify max(left) <= min(right).","A min-heap of *all* n points sorted by distance and popping k times is O(n + k log n) — fine, but it processes and heapifies all n points upfront."]},{"id":"cs_fundamentals/dsa_patterns#8f02e92faece","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"In the BROKEN→FIX trace, why does rebalancing (which only depends on sizes) fail to fix a values-based invariant violation?","answerFull":"Rebalancing moves elements between heaps based purely on counts (len(left) vs len(right)) to restore the size invariant — it has no mechanism to verify max(left) <= min(right). If the *initial routing* (before rebalancing) puts a value on the wrong side, rebalancing can restore the correct *sizes* while leaving the *wrong elements* on each side, silently violating max(left) <= min(right) without throwing any error. This is why the routing comparison (num <= -self.left[0]) must be correct independent of rebalancing — rebalancing is a size-correction mechanism, not a value-correction mechanism.","correct":"Rebalancing moves elements between heaps based purely on counts (len(left) vs len(right)) to restore the size invariant — it has no mechanism to verify max(left) <= min(right).","distractors":["In median-finding, the two heaps represent a size-balanced split of one sorted sequence (smaller half / larger half), and elements move between them to *maintain* that balance.","The area is min(height[left], height[right]) * (right - left).","Change the target size ratio: instead of keeping left and right within one element of equal size, keep len(left) ~= p% * n and len(right) ~= (100-p)% * n."]},{"id":"cs_fundamentals/dsa_patterns#63c11b51f70b","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"IPO/Maximize Capital — how is \"two heaps\" used differently here than in median-finding?","answerFull":"In median-finding, the two heaps represent a size-balanced split of one sorted sequence (smaller half / larger half), and elements move between them to *maintain* that balance. In IPO, the two heaps represent two different states of the same item set — \"not yet affordable\" (ordered by capital requirement, min-heap) and \"currently affordable\" (ordered by profit, max-heap). Items move *permanently* from the first to the second as your capital grows; there's no \"balance\" invariant, just a one-way migration driven by a threshold.","correct":"In median-finding, the two heaps represent a size-balanced split of one sorted sequence (smaller half / larger half), and elements move between them to *maintain* that balance.","distractors":["By the BST invariant, every node's left subtree contains only smaller values and its right subtree only larger values.","Each insertion requires finding the correct position (binary search on starts, O(log n)) and checking for overlap with neighbors (O(1)), but inserting into a Python list is O(n) due to shifting elements.","O(n) — every inserted element lives in exactly one of the two heaps, forever (for the streaming median problem, you must retain all history to support the running median)."]},{"id":"cs_fundamentals/dsa_patterns#ce5ef30f13c9","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"What's the time/space complexity of add_num and find_median, and why the asymmetry?","answerFull":"add_num is O(log n): one heappush (O(log n)) plus at most one rebalance (heappop + heappush, also O(log n)). find_median is O(1): it only reads heap[0] from each heap, which is a plain array index, not a heap operation. The asymmetry is the entire point of the pattern — you pay O(log n) *once per insertion* so that *every* median query afterward is free, which is far better than re-sorting (O(n log n)) on every query.","correct":"add_num is O(log n): one heappush (O(log n)) plus at most one rebalance (heappop + heappush, also O(log n)).","distractors":["Heap: O(n log k) time, O(k) space, works on streams, gives you all k elements (unordered).","In median-finding, the two heaps represent a size-balanced split of one sorted sequence (smaller half / larger half), and elements move between them to *maintain* that balance.","For small, fixed k (say k <= 4), a linear scan over k pointers to find the minimum each step is O(N * k) — simpler code, and for small constant k, O(N * k) and O(N log k) are both effectively O(N)."]},{"id":"cs_fundamentals/dsa_patterns#5e89a9c20809","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"Why is Median of Two Sorted Arrays not typically solved with two heaps in the optimal solution?","answerFull":"Both input arrays are *already fully sorted and static* — there's no streaming. Pouring m + n elements into two heaps costs O((m+n) log(m+n)), which is worse than just merging the two arrays directly (O(m+n)), and far worse than the optimal O(log(min(m,n))) binary-search-on-a-partition approach (see modified_binary_search). Two heaps shine when data *arrives incrementally*; for static, pre-sorted data, a one-shot algorithm tailored to that structure wins.","correct":"Both input arrays are *already fully sorted and static* — there's no streaming.","distractors":["Concatenating and sorting is O(N log N) and throws away the fact that each input is *already sorted*.","O(n) — every inserted element lives in exactly one of the two heaps, forever (for the streaming median problem, you must retain all history to support the running median).","The three-phase approach exploits the precondition that the input is *already sorted and non-overlapping* — a single linear scan suffices."]},{"id":"cs_fundamentals/dsa_patterns#3477279e5843","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":8,"difficulty":"intermediate","question":"How would you extend this to track the running p-th percentile instead of the median?","answerFull":"Change the target size ratio: instead of keeping left and right within one element of equal size, keep len(left) ~= p% * n and len(right) ~= (100-p)% * n. The rebalancing logic is structurally identical — move the boundary element between heaps whenever the ratio drifts outside tolerance. The median is the special case p = 50.","correct":"Change the target size ratio: instead of keeping left and right within one element of equal size, keep len(left) ~= p% * n and len(right) ~= (100-p)% * n.","distractors":["Fixed-size: a single for loop where right and left = right - k + 1 move in lockstep — no inner loop needed, just \"add nums[right], remove nums[right-k]\" each iteration.","Track prefix % k instead of raw prefix.","Change dfs from a None-returning side-effecting function to one that *returns* 1 + dfs(up) + dfs(down) + dfs(left) + dfs(right) (with the same \"sink before recurse\" rule, returning 0 for out-of-bounds/water cells)."]},{"id":"cs_fundamentals/dsa_patterns#bbb21d8c423d","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":10,"difficulty":"intermediate","question":"Single-Threaded CPU and Process Tasks Using Servers — how do these \"two heap\" problems differ structurally from median-finding?","answerFull":"These are discrete-event simulations, not balanced-partition problems. One heap orders items by *when they become available* (arrival time, or server free-time); the other orders *available* items by *selection priority* (shortest processing time, or smallest server index). Each simulation step: pop newly-available items from the first heap into the second, then pop the second heap to pick the next item to process. There is no size-balance invariant between the two heaps — they represent \"not yet eligible\" vs. \"eligible and ranked,\" a fundamentally different relationship than \"smaller half / larger half.\"","correct":"These are discrete-event simulations, not balanced-partition problems.","distractors":["O(n) — every inserted element lives in exactly one of the two heaps, forever (for the streaming median problem, you must retain all history to support the running median).","The median sits at the *boundary* between the two halves.","For \"top-k frequent\" problems, you need O(u) space for the frequency map (u = number of unique elements, u <= n) regardless of approach — that's unavoidable since you must count occurrences."]},{"id":"cs_fundamentals/dsa_patterns#a9526f45bd9c","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":11,"difficulty":"advanced","question":"What's the space complexity, and can it be reduced?","answerFull":"O(n) — every inserted element lives in exactly one of the two heaps, forever (for the streaming median problem, you must retain all history to support the running median). This cannot be reduced for *exact* medians of an unbounded stream; approximate streaming-median algorithms (e.g., t-digest, P-square) trade exactness for O(1) or O(log n) space when only an approximate percentile is acceptable — relevant for production metrics systems tracking p50/p99 latencies over millions of events.","correct":"O(n) — every inserted element lives in exactly one of the two heaps, forever (for the streaming median problem, you must retain all history to support the running median).","distractors":["The BST property is global, not just local: every node in the *entire* left subtree must be less than the current node, not just the immediate left child.","In median-finding, the two heaps represent a size-balanced split of one sorted sequence (smaller half / larger half), and elements move between them to *maintain* that balance.","For \"top-k frequent\" problems, you need O(u) space for the frequency map (u = number of unique elements, u <= n) regardless of approach — that's unavoidable since you must count occurrences."]},{"id":"cs_fundamentals/dsa_patterns#3d9008a1f7ea","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":12,"difficulty":"advanced","question":"find_median returns a float even when len(left) > len(right) (an integer value) — is that necessary?","answerFull":"LeetCode's MedianFinder.findMedian() is typed to return double (Python: float) because the even-size case averages two integers and may produce a .5 result. For consistency (a single return type regardless of parity), the odd-size case is also cast to float. If your language or problem statement allows a union return type, you could return int for the odd case and float for the even case — but matching the declared return type is usually required by the test harness.","correct":"LeetCode's MedianFinder.findMedian() is typed to return double (Python: float) because the even-size case averages two integers and may produce a .5 result.","distractors":["In median-finding, the two heaps represent a size-balanced split of one sorted sequence (smaller half / larger half), and elements move between them to *maintain* that balance.","Rebalancing moves elements between heaps based purely on counts (len(left) vs len(right)) to restore the size invariant — it has no mechanism to verify max(left) <= min(right).","Fixed-size: a single for loop where right and left = right - k + 1 move in lockstep — no inner loop needed, just \"add nums[right], remove nums[right-k]\" each iteration."]},{"id":"cs_fundamentals/dsa_patterns#ce5ad5dd2f6a","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"Why does two pointers require the array to be sorted (or sortable)?","answerFull":"Because the algorithm's correctness depends on monotonicity: moving left right strictly increases the candidate sum, and moving right left strictly decreases it. Without sorted order, you cannot conclude that \"the current pair sum is too small, so the *only* way to increase it is to move left\" — there could be a larger value anywhere in the array. Sorting establishes the monotonic structure that lets you discard half the remaining search space at each step, which is what gives O(n) instead of O(n^2).","correct":"Because the algorithm's correctness depends on monotonicity: moving left right strictly increases the candidate sum, and moving right left strictly decreases it.","distractors":["Each child's required candy count depends on comparisons with BOTH neighbors simultaneously, but a single left-to-right (or right-to-left) pass can only enforce one direction's constraint at a time.","By the BST invariant, every node's left subtree contains only smaller values and its right subtree only larger values.","Because the array is sorted ascending."]},{"id":"cs_fundamentals/dsa_patterns#2052e7b5542a","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"If sorting costs O(n log n), how is two pointers ever better than a hashmap approach which is O(n)?","answerFull":"For a single pair-sum query, a hashmap *is* asymptotically better (O(n) vs O(n log n)). Two pointers wins when (a) the input is already sorted, (b) you need O(1) extra space (hashmap is O(n)), or (c) you need to find *all* pairs/triplets without duplicates — the sorted order makes deduplication trivial (skip adjacent equal elements), whereas deduplicating hashmap-based results requires extra set bookkeeping.","correct":"For a single pair-sum query, a hashmap *is* asymptotically better (O(n) vs O(n log n)).","distractors":["For small, fixed k (say k <= 4), a linear scan over k pointers to find the minimum each step is O(N * k) — simpler code, and for small constant k, O(N * k) and O(N log k) are both effectively O(N).","Opposite ends (left=0, right=n-1, converging) is for finding a *relationship between two elements* (pair sum, palindrome check, container area) — the search space shrinks from both sides.","If k == n, the heap-of-size-k approach degenerates to holding the entire input — equivalent to sorting, so O(n log n); prefer sorting directly in that case."]},{"id":"cs_fundamentals/dsa_patterns#28692e3a9540","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"In 3Sum, why do we break early when nums[i] > 0?","answerFull":"Because the array is sorted ascending. If the smallest \"first\" element under consideration is already positive, then nums[i] + nums[left] + nums[right] (where left > i and right > left, both also positive or larger) can only be positive — it can never sum to zero. This is a monotonicity-based pruning step that avoids wasted iterations.","correct":"Because the array is sorted ascending.","distractors":["Compute prefix_product[i] = product(nums[0..i-1]) (product of everything to the left of i) and suffix_product[i] = product(nums[i+1..n-1]) (product of everything to the right).","Model the array as a function f(i) = nums[i].","Same structure as prefix sum, but using XOR (prefix[i] = nums[0] XOR nums[1] XOR ..."]},{"id":"cs_fundamentals/dsa_patterns#9dbc9706304e","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"What's the difference between the \"opposite ends\" and \"same direction\" two-pointer shapes?","answerFull":"Opposite ends (left=0, right=n-1, converging) is for finding a *relationship between two elements* (pair sum, palindrome check, container area) — the search space shrinks from both sides. Same direction (slow and fast, both starting near 0) is for *in-place filtering/compaction* — fast explores, slow marks the boundary of the \"good\" region built so far. They solve different problem shapes and should not be confused.","correct":"Opposite ends (left=0, right=n-1, converging) is for finding a *relationship between two elements* (pair sum, palindrome check, container area) — the search space shrinks from both sides.","distractors":["4Sum is O(n^3): two nested loops to fix the first two elements (O(n^2)), then O(n) two-pointer scan for the last pair.","Rebalancing moves elements between heaps based purely on counts (len(left) vs len(right)) to restore the size invariant — it has no mechanism to verify max(left) <= min(right).","Directly, no — sliding window relies on the 1D notion of \"contiguous range\" with a single left/right pair."]},{"id":"cs_fundamentals/dsa_patterns#b80ac6bc41e6","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"For Container With Most Water, why do you always move the pointer at the *shorter* line, not the taller one?","answerFull":"The area is min(height[left], height[right]) * (right - left). If you move the pointer at the *taller* line inward, the width decreases and the height is still bounded by the shorter line (or worse) — the area cannot increase. If you move the pointer at the *shorter* line, the width decreases but there's a *chance* the new height is taller, potentially increasing the area. Moving the shorter pointer is the only choice that doesn't provably make things worse, so it's the only direction that needs to be explored — this is itself a small greedy/exchange argument.","correct":"The area is min(height[left], height[right]) * (right - left).","distractors":["Because the algorithm's correctness depends on monotonicity: moving left right strictly increases the candidate sum, and moving right left strictly decreases it.","Fixed-size: a single for loop where right and left = right - k + 1 move in lockstep — no inner loop needed, just \"add nums[right], remove nums[right-k]\" each iteration.","Change the target size ratio: instead of keeping left and right within one element of equal size, keep len(left) ~= p% * n and len(right) ~= (100-p)% * n."]},{"id":"cs_fundamentals/dsa_patterns#3444f48b7ff0","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"Can two pointers be used on two different arrays/strings?","answerFull":"Yes — this is the \"two-sequence\" shape. Examples: merging two sorted arrays (LC 88), checking if one string is a subsequence of another (LC 392), or comparing two strings with backspace characters from the end (LC 844). Each pointer advances independently based on a comparison between seq1[i] and seq2[j].","correct":"Yes — this is the \"two-sequence\" shape.","distractors":["In median-finding, the two heaps represent a size-balanced split of one sorted sequence (smaller half / larger half), and elements move between them to *maintain* that balance.","(1) Find the middle using fast/slow pointers — splits the list into two halves.","For small, fixed k (say k <= 4), a linear scan over k pointers to find the minimum each step is O(N * k) — simpler code, and for small constant k, O(N * k) and O(N log k) are both effectively O(N)."]},{"id":"cs_fundamentals/dsa_patterns#abd4e0a69e07","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":8,"difficulty":"intermediate","question":"Is two pointers always O(n)? What if there's a sort first?","answerFull":"The two-pointer scan itself is O(n) (each pointer moves at most n times total, so the loop body runs at most O(n) times even though there are two indices). If a sort is required first, the *overall* complexity is dominated by the sort: O(n log n). If the input is already guaranteed sorted (a common interview constraint), the overall complexity is O(n).","correct":"The two-pointer scan itself is O(n) (each pointer moves at most n times total, so the loop body runs at most O(n) times even though there are two indices).","distractors":["Space depends on what's tracked, not on the pointer mechanism itself.","There is no input array — the \"lists\" are *generated on the fly* as multiples of the result sequence itself.","4Sum is O(n^3): two nested loops to fix the first two elements (O(n^2)), then O(n) two-pointer scan for the last pair."]},{"id":"cs_fundamentals/dsa_patterns#77c8f17a1a4f","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":10,"difficulty":"intermediate","question":"What's the time/space complexity of 4Sum, and how does it generalize to kSum?","answerFull":"4Sum is O(n^3): two nested loops to fix the first two elements (O(n^2)), then O(n) two-pointer scan for the last pair. In general, kSum is O(n^(k-1)): (k-2) nested loops plus one O(n) two-pointer pass. Space is O(1) extra (excluding the sort and the output list). Many implementations write a recursive kSum that bottoms out at twoSum for k=2.","correct":"4Sum is O(n^3): two nested loops to fix the first two elements (O(n^2)), then O(n) two-pointer scan for the last pair.","distractors":["The two-pointer scan itself is O(n) (each pointer moves at most n times total, so the loop body runs at most O(n) times even though there are two indices).","Space depends on what's tracked, not on the pointer mechanism itself.","Heap: O(n log k) time, O(k) space, works on streams, gives you all k elements (unordered)."]},{"id":"cs_fundamentals/dsa_patterns#9fca3063be1d","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":11,"difficulty":"advanced","question":"When would \"remove duplicates from sorted array\" two-pointer solution fail if the array were NOT sorted?","answerFull":"The slow/fast compaction relies on duplicates being *adjacent* — nums[fast] != nums[slow] only correctly identifies \"new\" values if all instances of a value are grouped together. On an unsorted array, duplicates could be scattered (e.g., [1, 2, 1]), and the same in-place scan would fail to remove the second 1. You'd need a hash set instead, which costs O(n) space.","correct":"The slow/fast compaction relies on duplicates being *adjacent* — nums[fast] != nums[slow] only correctly identifies \"new\" values if all instances of a value are grouped together.","distractors":["The condition while fast and fast.next handles both: if head is None, fast = None, the loop body never executes, has_cycle returns False correctly.","Fixed-size: a single for loop where right and left = right - k + 1 move in lockstep — no inner loop needed, just \"add nums[right], remove nums[right-k]\" each iteration.","When the array is mutated between queries (point updates: nums[i] = new_value)."]},{"id":"cs_fundamentals/dsa_patterns#8ec4828b6da1","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":12,"difficulty":"advanced","question":"How do you adapt the pair-sum template if the array can contain negative numbers?","answerFull":"No adaptation needed — the two-pointer pair-sum template relies only on the array being *sorted*, not on values being non-negative. Negative numbers are handled naturally because sorted order still gives you the monotonicity guarantee (nums[left] + nums[right] still increases as left increases, decreases as right decreases). Where non-negativity *does* matter is in sliding window problems (e.g., subarray sum < k), where a negative value would break the \"shrink when too big\" invariant.","correct":"No adaptation needed — the two-pointer pair-sum template relies only on the array being *sorted*, not on values being non-negative.","distractors":["For a single pair-sum query, a hashmap *is* asymptotically better (O(n) vs O(n log n)).","Directly, no — sliding window relies on the 1D notion of \"contiguous range\" with a single left/right pair.","Compare each pair of *adjacent* words in the given (sorted) word list."]},{"id":"cs_fundamentals/dsa_patterns#88cd49062d1f","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":1,"difficulty":"core","question":"Why is Union-Find described as \"near O(1)\" when find walks up a tree — isn't that O(log n) or worse?","answerFull":"Without any optimization, yes — a degenerate union sequence can produce an O(n)-deep tree. But path compression (every find call rewires nodes on its path directly to the root) combined with union by rank/size (always attach the smaller tree under the larger) together bound the amortized cost per operation to O(α(n)), where α is the inverse Ackermann function. α(n) <= 4 for any n smaller than the number of atoms in the observable universe — so \"near O(1)\" is not an exaggeration, it's a tight, proven bound.","correct":"Without any optimization, yes — a degenerate union sequence can produce an O(n)-deep tree.","distractors":["In principle, you could model overlapping intervals as a graph (edge between intervals i and j if they overlap) and use union_find.md to find connected components, then merge each component.","For small, fixed k (say k <= 4), a linear scan over k pointers to find the minimum each step is O(N * k) — simpler code, and for small constant k, O(N * k) and O(N log k) are both effectively O(N).","In an interview, rarely — O(1) space is almost always preferred when achievable, and the iterative version isn't meaningfully harder to write once memorized."]},{"id":"cs_fundamentals/dsa_patterns#135ac3c52a7f","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":2,"difficulty":"core","question":"When would you choose DFS/BFS over Union-Find for a connectivity problem, and vice versa?","answerFull":"If you're given the *whole* graph upfront and need to answer connectivity questions once (e.g., \"how many connected components does this static graph have?\"), DFS/BFS is simpler to write and equally efficient — O(V+E). Reach for Union-Find when: (1) edges/connections arrive incrementally and you need the answer to update after each one (Number of Islands II), or (2) you need to answer many \"are X and Y connected?\" queries efficiently without re-traversing the graph each time.","correct":"If you're given the *whole* graph upfront and need to answer connectivity questions once (e.g., \"how many connected components does this static graph have?\"), DFS/BFS is simpler to write and equally efficient — O(V+E).","distractors":["For *counting* connected components (islands) or *generating a path* (backtracking-style), DFS is simpler and uses less code.","For small, fixed k (say k <= 4), a linear scan over k pointers to find the minimum each step is O(N * k) — simpler code, and for small constant k, O(N * k) and O(N log k) are both effectively O(N).","For an undirected edge (u, v): if find(u) == find(v) *before* you union them, then u and v were already connected via some other path — adding edge (u, v) would create a cycle."]},{"id":"cs_fundamentals/dsa_patterns#b3dc6dee62ee","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":3,"difficulty":"core","question":"Walk through path compression step by step — what actually changes in the parent array?","answerFull":"find(x) recurses up to the root, then on the way back *down* the call stack, sets parent[node] = root for every node visited. So if 3 -> 2 -> 0 (0 is root), after find(3): parent[3] = 0 AND parent[2] = 0 (both nodes visited during the call get repointed directly to the root) — not just the node you originally called find on.","correct":"find(x) recurses up to the root, then on the way back *down* the call stack, sets parent[node] = root for every node visited.","distractors":["WHITE = never visited; GRAY = currently on the DFS call stack (an ancestor of the current node in the DFS tree); BLACK = fully processed (this node and all its descendants are done).","Checking every (start, end) pair of nodes along root-to-X paths is O(n^2) (for each node, walk up/down to try all paths through it).","A tree has no cycles and exactly one path from the root to any node, so a recursive/BFS walk can never revisit a node."]},{"id":"cs_fundamentals/dsa_patterns#277e697437d0","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":4,"difficulty":"core","question":"Union by rank vs. union by size — does the choice matter?","answerFull":"Both achieve the same asymptotic bound (O(α(n)) with path compression). Rank tracks an *upper bound on tree height*; size tracks the *number of elements* in the tree. Size is sometimes more directly useful (e.g., \"what's the size of the largest connected component?\" falls out for free). In practice, either is fine — pick whichever the problem's follow-up questions make more convenient.","correct":"Both achieve the same asymptotic bound (O(α(n)) with path compression).","distractors":["Without any optimization, yes — a degenerate union sequence can produce an O(n)-deep tree.","For an undirected edge (u, v): if find(u) == find(v) *before* you union them, then u and v were already connected via some other path — adding edge (u, v) would create a cycle.","Maintain a dynamic array (list) for O(1) random access (getRandom picks random.choice by index) and a hashmap value -> index_in_array."]},{"id":"cs_fundamentals/dsa_patterns#2998d48802b6","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":5,"difficulty":"core","question":"Why initialize parent[i] = i for every i?","answerFull":"Each element starts in its own singleton set, and the representative (root) of a singleton set {i} is i itself. parent[i] == i is exactly the base case / termination condition for find — \"if I am my own parent, I am the root, stop here.\"","correct":"Each element starts in its own singleton set, and the representative (root) of a singleton set {i} is i itself.","distractors":["in_degree[v] must count *every* edge (u, v) in the input — initialize it to all zeros, then for each edge (u, v), do in_degree[v] += 1.","Because the goal is to keep as many intervals as possible, and the interval that *finishes earliest* leaves the maximum remaining timeline for future intervals — regardless of when it starts.","Iterate mask from 0 to 2^n - 1; for each mask, bit j being set means element j is in this subset."]},{"id":"cs_fundamentals/dsa_patterns#2497eea5f213","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":6,"difficulty":"intermediate","question":"Walk through the off-by-one bug in §8 — why specifically does find(3) crash, and what's the general lesson?","answerFull":"UnionFindBroken(3) allocates parent = [0, 1, 2] — valid indices are 0, 1, 2. But the problem's nodes are 1-indexed up to n=3, so node 3 is a legal input — and parent[3] is out of bounds. The general lesson: before writing UnionFind(n), check the problem's node-numbering convention and size the array to max_possible_node_value + 1, regardless of what n \"means\" in the problem statement.","correct":"UnionFindBroken(3) allocates parent = [0, 1, 2] — valid indices are 0, 1, 2.","distractors":["A tree has no cycles and exactly one path from the root to any node, so a recursive/BFS walk can never revisit a node.","For *counting*, the hashmap stores prefix_value -> count of occurrences (a defaultdict(int)), because multiple earlier indices can have the same prefix value, each contributing one valid subarray.","State it calmly and specifically: \"I see the issue — on line [X], I'm using < but this should be <= to handle the case where [specific scenario from my edge-case example]."]},{"id":"cs_fundamentals/dsa_patterns#01a1d7e0f0fd","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":7,"difficulty":"intermediate","question":"How does Union-Find detect a cycle, and why does that NOT generalize to directed graphs?","answerFull":"For an undirected edge (u, v): if find(u) == find(v) *before* you union them, then u and v were already connected via some other path — adding edge (u, v) would create a cycle. This works because undirected connectivity is symmetric and transitive — exactly what Union-Find's equivalence-class structure models. Directed graphs need direction-aware cycle detection (3-color DFS in topological_sort.md) because a -> b and b -> a together form a cycle, but a -> b and a -> c (with b and c unrelated) do not — Union-Find would incorrectly treat both scenarios the same way (both unions of {a,b,c}).","correct":"For an undirected edge (u, v): if find(u) == find(v) *before* you union them, then u and v were already connected via some other path — adding edge (u, v) would create a cycle.","distractors":["In principle, you could model overlapping intervals as a graph (edge between intervals i and j if they overlap) and use union_find.md to find connected components, then merge each component.","For *counting* connected components (islands) or *generating a path* (backtracking-style), DFS is simpler and uses less code.","Every node in a cycle has at least one incoming edge *from within the cycle* that can never be \"removed\" (because the node providing that edge can never reach in-degree 0 either — it's circular)."]},{"id":"cs_fundamentals/dsa_patterns#2e1b58575a8b","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":8,"difficulty":"intermediate","question":"How does Union-Find fit into Kruskal's MST algorithm?","answerFull":"Sort all edges by weight ascending. For each edge (u, v, w) in that order, call union(u, v). If it returns True (they were in different components), this edge doesn't create a cycle — accept it into the MST and add w to the total cost. If it returns False, accepting this edge would create a cycle — skip it. The greedy correctness (cheapest non-cycle-forming edge is always safe to add) is the Cut Property of MSTs; Union-Find is just the efficient mechanism for the cycle check.","correct":"Sort all edges by weight ascending.","distractors":["Union-Find merges two nodes into the same set when an edge connects them, treating the edge as bidirectional for connectivity purposes.","In principle, you could model overlapping intervals as a graph (edge between intervals i and j if they overlap) and use union_find.md to find connected components, then merge each component.","Decide the final iteration order first: you want the result sorted by (frequency descending, word ascending)."]},{"id":"cs_fundamentals/dsa_patterns#90062c1ec8ec","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":9,"difficulty":"intermediate","question":"What extra state does weighted Union-Find (Evaluate Division) need beyond plain Union-Find?","answerFull":"A weight[x] array, where weight[x] represents the ratio x / parent[x]. find(x) must accumulate the product of weight values along the path to the root (this is the ratio x / root), and during path compression, update weight[x] to that accumulated product directly (so future lookups are O(1) again). union(x, y, value) — given x / y == value — computes the relative weight between x's root and y's root using the already-known ratios x/root_x and y/root_y.","correct":"A weight[x] array, where weight[x] represents the ratio x / parent[x].","distractors":["For an undirected edge (u, v): if find(u) == find(v) *before* you union them, then u and v were already connected via some other path — adding edge (u, v) would create a cycle.","In principle, you could model overlapping intervals as a graph (edge between intervals i and j if they overlap) and use union_find.md to find connected components, then merge each component.","Union-Find merges two nodes into the same set when an edge connects them, treating the edge as bidirectional for connectivity purposes."]},{"id":"cs_fundamentals/dsa_patterns#117b655c900a","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":10,"difficulty":"intermediate","question":"For Number of Islands II, why is Union-Find preferred over re-running DFS after every cell addition?","answerFull":"Re-running DFS from scratch after each of k additions costs O(k * rows * cols) in the worst case. With Union-Find, each addition is one union call per neighboring land cell — O(α(rows*cols)) amortized — so k additions cost roughly O(k * α(rows*cols)), which is dramatically cheaper for large grids with many incremental updates.","correct":"Re-running DFS from scratch after each of k additions costs O(k * rows * cols) in the worst case.","distractors":["For an undirected edge (u, v): if find(u) == find(v) *before* you union them, then u and v were already connected via some other path — adding edge (u, v) would create a cycle.","For a matrix with cols columns, cell (r, c) is linear index r * cols + c; inversely, r = k // cols and c = k % cols.","Running BFS once per source and taking the minimum would be correct but wasteful — O(k · rows · cols) for k sources."]},{"id":"cs_fundamentals/dsa_patterns#c41011df5714","section":"cs_fundamentals","module":"cs_fundamentals/dsa_patterns","moduleName":"dsa patterns","qIndex":11,"difficulty":"advanced","question":"What does self.count track, and what's the subtlety in maintaining it correctly?","answerFull":"self.count is the number of disjoint sets (connected components) currently. It starts at n (everything isolated) and decrements by exactly 1 only when union actually merges two previously-different sets — i.e., only when find(x) != find(y) *before* the union. If you decrement unconditionally on every union call (even when x and y were already connected), count will undercount the true number of components.","correct":"self.count is the number of disjoint sets (connected components) currently.","distractors":["For \"top-k frequent\" problems, you need O(u) space for the frequency map (u = number of unique elements, u <= n) regardless of approach — that's unavoidable since you must count occurrences.","Without this check, every number triggers an inner \"count forward\" loop, and for a single long run of length n, this sums to O(n^2) (the 1st number counts n steps, the 2nd counts n-1, etc.).","A hashmap gives O(1) for exact-match lookups, but \"does any key start with 'app'?\" requires either iterating all keys (O(n·L)) or maintaining a separate prefix index."]},{"id":"cs_fundamentals/dynamic_programming#5314a139f5eb","section":"cs_fundamentals","module":"cs_fundamentals/dynamic_programming","moduleName":"dynamic programming","qIndex":2,"difficulty":"core","question":"What is the difference between top-down memoisation and bottom-up tabulation?","answerFull":"Memoisation: write the recursive solution, add a cache (dict or lru_cache). Computes only reachable states; natural to code; risks stack overflow for large n. Tabulation: fill a table iteratively in dependency order. Computes all states; no stack risk; amenable to space optimisation (rolling array). Both have the same asymptotic complexity; tabulation usually has better constant factors due to cache-friendly array access vs dict lookup.","correct":"Memoisation: write the recursive solution, add a cache (dict or lru_cache).","distractors":["(1) 0/1 knapsack family: subset selection with constraints (0/1 knapsack, partition equal subset sum, target sum).","0/1 knapsack with the greedy heuristic \"take highest value-to-weight ratio first.\" Example: capacity=10, items={(weight=6,value=8), (weight=5,value=5), (weight=5,value=5)}.","Coin change is unbounded knapsack: each coin can be used unlimited times."]},{"id":"cs_fundamentals/dynamic_programming#e2b416d0585a","section":"cs_fundamentals","module":"cs_fundamentals/dynamic_programming","moduleName":"dynamic programming","qIndex":3,"difficulty":"core","question":"Explain the 0/1 knapsack problem and its DP recurrence.","answerFull":"Given n items each with weight w_i and value v_i, and a capacity W, maximise total value with total weight ≤ W. Each item is used at most once. Recurrence: dp[i][w] = max(dp[i-1][w], dp[i-1][w - w_i] + v_i) if w >= w_i, else dp[i-1][w]. Complexity: O(n × W) time, O(W) space with rolling array. This is pseudo-polynomial (depends on W's numeric value, not its bit-length).","correct":"Given n items each with weight w_i and value v_i, and a capacity W, maximise total value with total weight ≤ W.","distractors":["(1) 0/1 knapsack family: subset selection with constraints (0/1 knapsack, partition equal subset sum, target sum).","0/1 knapsack with the greedy heuristic \"take highest value-to-weight ratio first.\" Example: capacity=10, items={(weight=6,value=8), (weight=5,value=5), (weight=5,value=5)}.","Coin change is unbounded knapsack: each coin can be used unlimited times."]},{"id":"cs_fundamentals/dynamic_programming#aa78aaaf5f35","section":"cs_fundamentals","module":"cs_fundamentals/dynamic_programming","moduleName":"dynamic programming","qIndex":4,"difficulty":"core","question":"Why is knapsack NP-hard if it has a polynomial DP solution?","answerFull":"The DP runs in O(nW) which depends on the numeric value of W. If W = 2^30, that's ~10^9 operations — exponential in the bit-length of the input. \"Pseudo-polynomial\" means polynomial in the input value, not the input size. True polynomial algorithms run in time polynomial in the number of input bits (log W). No such algorithm is known for knapsack, which is why it is NP-hard in the strong sense.","correct":"The DP runs in O(nW) which depends on the numeric value of W.","distractors":["Coin change is unbounded knapsack: each coin can be used unlimited times.","(1) 0/1 knapsack family: subset selection with constraints (0/1 knapsack, partition equal subset sum, target sum).","0/1 knapsack with the greedy heuristic \"take highest value-to-weight ratio first.\" Example: capacity=10, items={(weight=6,value=8), (weight=5,value=5), (weight=5,value=5)}."]},{"id":"cs_fundamentals/dynamic_programming#cd84611254e8","section":"cs_fundamentals","module":"cs_fundamentals/dynamic_programming","moduleName":"dynamic programming","qIndex":5,"difficulty":"core","question":"What is the recurrence for edit distance and what do the three choices represent?","answerFull":"dp[i][j] = min(dp[i-1][j-1] + cost, dp[i-1][j] + 1, dp[i][j-1] + 1) where cost = 0 if word1[i-1] == word2[j-1] else 1. Three choices: replace (dp[i-1][j-1] + cost — align characters i and j), delete from word1 (dp[i-1][j] + 1 — consume character i from word1 with a deletion), insert into word1 (dp[i][j-1] + 1 — consume character j from word2 with an insertion).","correct":"dp[i][j] = min(dp[i-1][j-1] + cost, dp[i-1][j] + 1, dp[i][j-1] + 1) where cost = 0 if word1[i-1] == word2[j-1] else 1.","distractors":["Use DP when: locally optimal choices may not lead to global optimum (test with a counterexample); backtracking is needed.","LCS finds the length of the longest common subsequence; edit distance (with insert/delete only, no replace) equals m + n - 2 × LCS(s1, s2).","Git's diff algorithm (Myers diff) finds the shortest edit script — minimum number of insertions and deletions to transform one file version into another."]},{"id":"cs_fundamentals/dynamic_programming#d8cab144544e","section":"cs_fundamentals","module":"cs_fundamentals/dynamic_programming","moduleName":"dynamic programming","qIndex":6,"difficulty":"intermediate","question":"What is the key difference between LCS and edit distance?","answerFull":"LCS finds the length of the longest common subsequence; edit distance (with insert/delete only, no replace) equals m + n - 2 × LCS(s1, s2). With replace allowed, they diverge. LCS uses dp[i][j] = dp[i-1][j-1] + 1 when characters match; edit distance adds the replace transition. Both are O(mn) time and space.","correct":"LCS finds the length of the longest common subsequence; edit distance (with insert/delete only, no replace) equals m + n - 2 × LCS(s1, s2).","distractors":["Git's diff algorithm (Myers diff) finds the shortest edit script — minimum number of insertions and deletions to transform one file version into another.","Maintain an array tails where tails[k] is the smallest tail element of all increasing subsequences of length k+1 seen so far.","(1) 0/1 knapsack family: subset selection with constraints (0/1 knapsack, partition equal subset sum, target sum)."]},{"id":"cs_fundamentals/dynamic_programming#65add042f21b","section":"cs_fundamentals","module":"cs_fundamentals/dynamic_programming","moduleName":"dynamic programming","qIndex":7,"difficulty":"intermediate","question":"How does the coin change DP differ from 0/1 knapsack?","answerFull":"Coin change is unbounded knapsack: each coin can be used unlimited times. The difference in code: the 0/1 knapsack rolls the inner loop right-to-left (so dp[cap - w] refers to the previous item's row — can't use current item again). The unbounded knapsack iterates left-to-right (dp[cap - w] already incorporates the current item — can use it again). The coin change count variant (number of ways) also uses left-to-right iteration.","correct":"Coin change is unbounded knapsack: each coin can be used unlimited times.","distractors":["(1) 0/1 knapsack family: subset selection with constraints (0/1 knapsack, partition equal subset sum, target sum).","0/1 knapsack with the greedy heuristic \"take highest value-to-weight ratio first.\" Example: capacity=10, items={(weight=6,value=8), (weight=5,value=5), (weight=5,value=5)}.","Git's diff algorithm (Myers diff) finds the shortest edit script — minimum number of insertions and deletions to transform one file version into another."]},{"id":"cs_fundamentals/dynamic_programming#d0604bfa1a0f","section":"cs_fundamentals","module":"cs_fundamentals/dynamic_programming","moduleName":"dynamic programming","qIndex":8,"difficulty":"intermediate","question":"What is patience sorting and how does it give O(n log n) LIS?","answerFull":"Maintain an array tails where tails[k] is the smallest tail element of all increasing subsequences of length k+1 seen so far. For each new element x: binary search (lower_bound) for its insertion position in tails. If past the end, extend tails (LIS length increases). Otherwise, replace tails[pos] = x (maintain the invariant). The length of tails at the end is the LIS length. Each of n elements requires O(log n) binary search → O(n log n) total.","correct":"Maintain an array tails where tails[k] is the smallest tail element of all increasing subsequences of length k+1 seen so far.","distractors":["LCS finds the length of the longest common subsequence; edit distance (with insert/delete only, no replace) equals m + n - 2 × LCS(s1, s2).","(1) 0/1 knapsack family: subset selection with constraints (0/1 knapsack, partition equal subset sum, target sum).","0/1 knapsack with the greedy heuristic \"take highest value-to-weight ratio first.\" Example: capacity=10, items={(weight=6,value=8), (weight=5,value=5), (weight=5,value=5)}."]},{"id":"cs_fundamentals/dynamic_programming#bd059a581723","section":"cs_fundamentals","module":"cs_fundamentals/dynamic_programming","moduleName":"dynamic programming","qIndex":9,"difficulty":"intermediate","question":"How do you reconstruct the actual DP solution (not just the value)?","answerFull":"Keep a choice table alongside the dp table. At each state, record which transition was chosen (e.g., for knapsack: taken[i][w] = True if item i was taken). After filling, trace back from dp[n][W]: if taken[i][w] is True, include item i and move to (i-1, w - w_i), else move to (i-1, w). For rolling-array optimisations, reconstruction requires keeping the full choice table — you cannot reconstruct from the rolling dp row alone.","correct":"Keep a choice table alongside the dp table.","distractors":["Memoisation: write the recursive solution, add a cache (dict or lru_cache).","Standard: O(m × n) space (full table).","(1) 0/1 knapsack family: subset selection with constraints (0/1 knapsack, partition equal subset sum, target sum)."]},{"id":"cs_fundamentals/dynamic_programming#d6ed5eaf0b8a","section":"cs_fundamentals","module":"cs_fundamentals/dynamic_programming","moduleName":"dynamic programming","qIndex":10,"difficulty":"intermediate","question":"What is interval DP and give an example?","answerFull":"Interval DP: dp[l][r] = optimal answer for the subproblem on range [l, r]. Fill in order of increasing interval length. Example: Matrix Chain Multiplication — dp[l][r] = minimum multiplications to compute matrices l through r. Transition: try every split k in [l, r-1]: dp[l][r] = min over k of (dp[l][k] + dp[k+1][r] + dims[l-1]*dims[k]*dims[r]). O(n³) time. Other examples: Burst Balloons (LeetCode 312), Zuma Game, Palindrome Partitioning.","correct":"Interval DP: dp[l][r] = optimal answer for the subproblem on range [l, r].","distractors":["Use DP when: locally optimal choices may not lead to global optimum (test with a counterexample); backtracking is needed.","(1) 0/1 knapsack family: subset selection with constraints (0/1 knapsack, partition equal subset sum, target sum).","0/1 knapsack with the greedy heuristic \"take highest value-to-weight ratio first.\" Example: capacity=10, items={(weight=6,value=8), (weight=5,value=5), (weight=5,value=5)}."]},{"id":"cs_fundamentals/dynamic_programming#838cffc89482","section":"cs_fundamentals","module":"cs_fundamentals/dynamic_programming","moduleName":"dynamic programming","qIndex":11,"difficulty":"advanced","question":"When should you use DP vs greedy?","answerFull":"Use DP when: locally optimal choices may not lead to global optimum (test with a counterexample); backtracking is needed. Use greedy when: an exchange argument proves local = global (the greedy choice never leaves you worse off); e.g., interval scheduling (earliest finish first), Huffman coding (minimum frequency characters merged first). Rule of thumb: try greedy first (simpler), find a counterexample, fall back to DP if one exists.","correct":"Use DP when: locally optimal choices may not lead to global optimum (test with a counterexample); backtracking is needed.","distractors":["0/1 knapsack with the greedy heuristic \"take highest value-to-weight ratio first.\" Example: capacity=10, items={(weight=6,value=8), (weight=5,value=5), (weight=5,value=5)}.","Interval DP: dp[l][r] = optimal answer for the subproblem on range [l, r].","(1) 0/1 knapsack family: subset selection with constraints (0/1 knapsack, partition equal subset sum, target sum)."]},{"id":"cs_fundamentals/dynamic_programming#1bcce039efc2","section":"cs_fundamentals","module":"cs_fundamentals/dynamic_programming","moduleName":"dynamic programming","qIndex":12,"difficulty":"advanced","question":"What is bitmask DP and when is it used?","answerFull":"State includes a bitmask encoding a subset of elements. Used when n ≤ 20 and the state is \"which subset has been visited/used.\" Example: Travelling Salesman Problem — dp[mask][v] = minimum cost path visiting exactly the cities in mask and ending at v. Transition: dp[mask | (1 << u)][u] = min(..., dp[mask][v] + dist[v][u]). O(2^n × n²) time, O(2^n × n) space. Bitmask DP is also used for assignment problems and scheduling with state constraints.","correct":"State includes a bitmask encoding a subset of elements.","distractors":["(1) 0/1 knapsack family: subset selection with constraints (0/1 knapsack, partition equal subset sum, target sum).","0/1 knapsack with the greedy heuristic \"take highest value-to-weight ratio first.\" Example: capacity=10, items={(weight=6,value=8), (weight=5,value=5), (weight=5,value=5)}.","Coin change is unbounded knapsack: each coin can be used unlimited times."]},{"id":"cs_fundamentals/dynamic_programming#80115da2a364","section":"cs_fundamentals","module":"cs_fundamentals/dynamic_programming","moduleName":"dynamic programming","qIndex":14,"difficulty":"advanced","question":"Explain the relationship between edit distance and version control.","answerFull":"Git's diff algorithm (Myers diff) finds the shortest edit script — minimum number of insertions and deletions to transform one file version into another. It works on the edit distance DAG where diagonal moves (matching lines) are free and horizontal/vertical moves (delete/insert) cost 1. Myers algorithm finds the shortest D-path (path using D non-diagonal moves) in O(D × n) time using the \"furthest reaching D-path\" observation. The human-readable diff output is the path reconstruction.","correct":"Git's diff algorithm (Myers diff) finds the shortest edit script — minimum number of insertions and deletions to transform one file version into another.","distractors":["LCS finds the length of the longest common subsequence; edit distance (with insert/delete only, no replace) equals m + n - 2 × LCS(s1, s2).","(1) 0/1 knapsack family: subset selection with constraints (0/1 knapsack, partition equal subset sum, target sum).","0/1 knapsack with the greedy heuristic \"take highest value-to-weight ratio first.\" Example: capacity=10, items={(weight=6,value=8), (weight=5,value=5), (weight=5,value=5)}."]},{"id":"cs_fundamentals/dynamic_programming#3830b575568c","section":"cs_fundamentals","module":"cs_fundamentals/dynamic_programming","moduleName":"dynamic programming","qIndex":15,"difficulty":"advanced","question":"What is the space complexity of edit distance and how can it be improved?","answerFull":"Standard: O(m × n) space (full table). Space-optimised with rolling array: O(min(m, n)) — only keep two rows. For actual path reconstruction with O(m + n) space: Hirschberg's algorithm divides the problem at the midpoint, computes score from both ends (O(n) each), finds the optimal midpoint in O(mn) time, then recurses on two halves of O(n/2) space each — total O(mn) time, O(m + n) space.","correct":"Standard: O(m × n) space (full table).","distractors":["Git's diff algorithm (Myers diff) finds the shortest edit script — minimum number of insertions and deletions to transform one file version into another.","LCS finds the length of the longest common subsequence; edit distance (with insert/delete only, no replace) equals m + n - 2 × LCS(s1, s2).","Keep a choice table alongside the dp table."]},{"id":"cs_fundamentals/dynamic_programming#c1e4bb2f1eb9","section":"cs_fundamentals","module":"cs_fundamentals/dynamic_programming","moduleName":"dynamic programming","qIndex":16,"difficulty":"advanced","question":"What are the four DP problem families and their canonical representatives?","answerFull":"(1) 0/1 knapsack family: subset selection with constraints (0/1 knapsack, partition equal subset sum, target sum). (2) LCS/sequence alignment family: comparing two sequences character-by-character (LCS, edit distance, regex matching, wildcard matching). (3) Unbounded selection family: unlimited repetition of choices (coin change, perfect squares, climbing stairs). (4) Interval DP family: range-based sub-problems (matrix chain multiplication, burst balloons, palindrome partitioning). Most DP problems in interviews belong to one of these four shapes.","correct":"(1) 0/1 knapsack family: subset selection with constraints (0/1 knapsack, partition equal subset sum, target sum).","distractors":["0/1 knapsack with the greedy heuristic \"take highest value-to-weight ratio first.\" Example: capacity=10, items={(weight=6,value=8), (weight=5,value=5), (weight=5,value=5)}.","Coin change is unbounded knapsack: each coin can be used unlimited times.","State includes a bitmask encoding a subset of elements."]},{"id":"cs_fundamentals/dynamic_programming#a8951bab69b6","section":"cs_fundamentals","module":"cs_fundamentals/dynamic_programming","moduleName":"dynamic programming","qIndex":17,"difficulty":"advanced","question":"Describe a DP problem you can solve greedily by accident and explain the failure.","answerFull":"0/1 knapsack with the greedy heuristic \"take highest value-to-weight ratio first.\" Example: capacity=10, items={(weight=6,value=8), (weight=5,value=5), (weight=5,value=5)}. Greedy takes the first item (ratio 1.33) and cannot fit either remaining item → value=8. Optimal: take items 2 and 3 → value=10. Greedy fails because picking one item blocks the combination that provides higher total value. The exchange argument breaks down because items are indivisible (0/1) — fractional knapsack (where you can take partial items) is solvable by greedy.","correct":"0/1 knapsack with the greedy heuristic \"take highest value-to-weight ratio first.\" Example: capacity=10, items={(weight=6,value=8), (weight=5,value=5), (weight=5,value=5)}.","distractors":["Coin change is unbounded knapsack: each coin can be used unlimited times.","Given n items each with weight w_i and value v_i, and a capacity W, maximise total value with total weight ≤ W.","(1) 0/1 knapsack family: subset selection with constraints (0/1 knapsack, partition equal subset sum, target sum)."]},{"id":"cs_fundamentals/dynamic_programming#2dfb8ac5c4fa","section":"cs_fundamentals","module":"cs_fundamentals/dynamic_programming","moduleName":"dynamic programming","qIndex":18,"difficulty":"advanced","question":"How does DP apply in string problems like regex or wildcard matching?","answerFull":"Regex matching (. = any char, * = zero or more of previous): dp[i][j] = True if pattern[:j] matches string[:i]. Transition for pattern[j-1] == '*': either use zero times (dp[i][j] = dp[i][j-2]) or use one more time (dp[i][j] = dp[i-1][j] if pattern[j-2] matches string[i-1]). The key insight: * can match zero or more — the \"zero\" case is the base; the \"more\" case consumes a character from the string. O(mn) time, O(mn) space.","correct":"Regex matching (. = any char, * = zero or more of previous): dp[i][j] = True if pattern[:j] matches string[:i].","distractors":["(1) 0/1 knapsack family: subset selection with constraints (0/1 knapsack, partition equal subset sum, target sum).","0/1 knapsack with the greedy heuristic \"take highest value-to-weight ratio first.\" Example: capacity=10, items={(weight=6,value=8), (weight=5,value=5), (weight=5,value=5)}.","Coin change is unbounded knapsack: each coin can be used unlimited times."]},{"id":"cs_fundamentals/graph_and_string_algorithms#8fb593388b2c","section":"cs_fundamentals","module":"cs_fundamentals/graph_and_string_algorithms","moduleName":"graph and string algorithms","qIndex":1,"difficulty":"core","question":"What is the difference between BFS and DFS for shortest path? When does BFS give the correct shortest path and DFS does not?","answerFull":"BFS gives the correct shortest path in terms of hop count (for unweighted graphs) because it processes nodes in non-decreasing order of distance — the first time a node is reached is via the shortest path. DFS explores one path to its end before backtracking; it may find a path that is not shortest. For weighted graphs, neither BFS nor DFS gives shortest weighted paths — use Dijkstra or Bellman-Ford.","correct":"BFS gives the correct shortest path in terms of hop count (for unweighted graphs) because it processes nodes in non-decreasing order of distance — the first time a node is reached is via the shortest path.","distractors":["A* extends Dijkstra by adding a heuristic h(v) that estimates the remaining distance from v to the target.","Floyd-Warshall is O(V³) and computes all-pairs shortest paths (APSP).","After V-1 relaxation passes, all shortest paths (using at most V-1 edges, since shortest paths in a graph without negative cycles have no repeated vertices) have been found."]},{"id":"cs_fundamentals/graph_and_string_algorithms#c5c3de20760e","section":"cs_fundamentals","module":"cs_fundamentals/graph_and_string_algorithms","moduleName":"graph and string algorithms","qIndex":2,"difficulty":"core","question":"Why can't Dijkstra handle negative edge weights? Give a concrete counterexample.","answerFull":"Dijkstra's invariant: once a vertex u is extracted from the heap, dist[u] is final. With negative weights, a path through an unvisited vertex might use a negative edge to produce a shorter path to u after u has been finalised. Counterexample: vertices {0,1,2}, edges 0->1 (w=2), 0->2 (w=3), 2->1 (w=-2). Dijkstra finalises dist[1]=2 (shortest via 0->1), but the true shortest is 0->2->1 = 1. Bellman-Ford correctly finds this by relaxing edges in two passes.","correct":"Dijkstra's invariant: once a vertex u is extracted from the heap, dist[u] is final.","distractors":["O((V + E) log V). Each vertex is extracted from the heap once: V extractions × O(log V) each = O(V log V).","A* extends Dijkstra by adding a heuristic h(v) that estimates the remaining distance from v to the target.","Undirected: during DFS, if a visited neighbour is not the direct parent of the current vertex (i.e., it's a back edge to an ancestor other than the parent), a cycle exists."]},{"id":"cs_fundamentals/graph_and_string_algorithms#c65db8e47a85","section":"cs_fundamentals","module":"cs_fundamentals/graph_and_string_algorithms","moduleName":"graph and string algorithms","qIndex":3,"difficulty":"core","question":"What is the time complexity of Dijkstra with a binary heap and why?","answerFull":"O((V + E) log V). Each vertex is extracted from the heap once: V extractions × O(log V) each = O(V log V). Each edge triggers at most one relaxation and heap push: E pushes × O(log V) each = O(E log V). Total: O((V + E) log V). With a Fibonacci heap, decrease-key is O(1) amortised, giving O(V log V + E). In practice, the binary heap version is faster due to better cache behaviour.","correct":"O((V + E) log V). Each vertex is extracted from the heap once: V extractions × O(log V) each = O(V log V).","distractors":["Dijkstra's invariant: once a vertex u is extracted from the heap, dist[u] is final.","A* extends Dijkstra by adding a heuristic h(v) that estimates the remaining distance from v to the target.","Prim's and Dijkstra's use the same greedy structure: maintain a priority queue of (cost, vertex), extract the minimum-cost vertex, relax its edges."]},{"id":"cs_fundamentals/graph_and_string_algorithms#fdc2e681160f","section":"cs_fundamentals","module":"cs_fundamentals/graph_and_string_algorithms","moduleName":"graph and string algorithms","qIndex":4,"difficulty":"core","question":"How does Bellman-Ford detect negative cycles?","answerFull":"After V-1 relaxation passes, all shortest paths (using at most V-1 edges, since shortest paths in a graph without negative cycles have no repeated vertices) have been found. In the V-th pass, if any edge (u, v, w) still satisfies dist[u] + w < dist[v], then a shorter path with V edges exists — which means a cycle of negative total weight is reachable from the source.","correct":"After V-1 relaxation passes, all shortest paths (using at most V-1 edges, since shortest paths in a graph without negative cycles have no repeated vertices) have been found.","distractors":["BFS gives the correct shortest path in terms of hop count (for unweighted graphs) because it processes nodes in non-decreasing order of distance — the first time a node is reached is via the shortest path.","Kahn's BFS: compute in-degrees of all vertices.","Floyd-Warshall is O(V³) and computes all-pairs shortest paths (APSP)."]},{"id":"cs_fundamentals/graph_and_string_algorithms#322e3c3ce30b","section":"cs_fundamentals","module":"cs_fundamentals/graph_and_string_algorithms","moduleName":"graph and string algorithms","qIndex":5,"difficulty":"core","question":"Explain Kahn's algorithm for topological sort and how it detects cycles.","answerFull":"Kahn's BFS: compute in-degrees of all vertices. Enqueue vertices with in-degree 0. Repeatedly dequeue a vertex u, add it to the result, and decrement in-degrees of u's neighbours. If any neighbour's in-degree becomes 0, enqueue it. After processing, if the result has fewer than V vertices, the remaining vertices form one or more cycles (they were never reached by the 0-in-degree queue). This is the correct, direct cycle detection.","correct":"Kahn's BFS: compute in-degrees of all vertices.","distractors":["The Z-algorithm maintains a window [l, r] that is the rightmost Z-box (substring matching a prefix).","Aho-Corasick is a multi-pattern search algorithm.","After V-1 relaxation passes, all shortest paths (using at most V-1 edges, since shortest paths in a graph without negative cycles have no repeated vertices) have been found."]},{"id":"cs_fundamentals/graph_and_string_algorithms#7f2cd68d62a3","section":"cs_fundamentals","module":"cs_fundamentals/graph_and_string_algorithms","moduleName":"graph and string algorithms","qIndex":6,"difficulty":"intermediate","question":"What is the KMP failure function and what does fail[i] represent?","answerFull":"fail[i] = length of the longest proper prefix of pattern[:i+1] that is also a suffix of pattern[:i+1]. \"Proper\" means strictly shorter than the string itself. On mismatch at pattern position k, jump to pattern position fail[k-1] instead of 0 — preserving the longest already-matched prefix. This ensures the text pointer never moves backward. The failure function is computed in O(m) using the same two-pointer technique as the search.","correct":"fail[i] = length of the longest proper prefix of pattern[:i+1] that is also a suffix of pattern[:i+1].","distractors":["Aho-Corasick is a multi-pattern search algorithm.","Z[i] = length of the longest substring starting at s[i] that matches a prefix of s.","For a 2D text (T × R matrix) and 2D pattern (t × r matrix): compute rolling hash of each row of the pattern and the rolling hash of r×t sub-blocks."]},{"id":"cs_fundamentals/graph_and_string_algorithms#57b60a0ba167","section":"cs_fundamentals","module":"cs_fundamentals/graph_and_string_algorithms","moduleName":"graph and string algorithms","qIndex":7,"difficulty":"intermediate","question":"What is the Z-array and how is it related to the failure function?","answerFull":"Z[i] = length of the longest substring starting at s[i] that matches a prefix of s. For pattern search, concatenate pattern + \"$\" + text; positions i > len(pattern) where Z[i] == len(pattern) are occurrence starts. The Z-array and failure function contain equivalent information but differ in indexing. Z-algorithm is often preferred for teaching because its semantics are more direct (Z[i] directly says \"this many characters match from the start\").","correct":"Z[i] = length of the longest substring starting at s[i] that matches a prefix of s.","distractors":["fail[i] = length of the longest proper prefix of pattern[:i+1] that is also a suffix of pattern[:i+1].","The Z-algorithm maintains a window [l, r] that is the rightmost Z-box (substring matching a prefix).","0-1 BFS: graph where each edge has weight 0 or 1."]},{"id":"cs_fundamentals/graph_and_string_algorithms#65a0acc78170","section":"cs_fundamentals","module":"cs_fundamentals/graph_and_string_algorithms","moduleName":"graph and string algorithms","qIndex":8,"difficulty":"intermediate","question":"What is Rabin-Karp's rolling hash and what is its worst-case complexity?","answerFull":"Rolling hash: the hash of window[i+1..i+m] is derived from the hash of window[i..i+m-1] in O(1): subtract the contribution of the leftmost character, multiply by base, add the new character (all modulo a prime). This makes sliding the window O(1) per step vs O(m) for recomputing. Worst-case O(nm): if every window hash matches the pattern hash (all characters identical), every window requires O(m) character-level verification. Expected O(n + m) with a good hash.","correct":"Rolling hash: the hash of window[i+1..i+m] is derived from the hash of window[i..i+m-1] in O(1): subtract the contribution of the leftmost character, multiply by base, add the new character (all modulo a prime).","distractors":["For a 2D text (T × R matrix) and 2D pattern (t × r matrix): compute rolling hash of each row of the pattern and the rolling hash of r×t sub-blocks.","The Z-algorithm maintains a window [l, r] that is the rightmost Z-box (substring matching a prefix).","0-1 BFS: graph where each edge has weight 0 or 1."]},{"id":"cs_fundamentals/graph_and_string_algorithms#fbd242c82c29","section":"cs_fundamentals","module":"cs_fundamentals/graph_and_string_algorithms","moduleName":"graph and string algorithms","qIndex":9,"difficulty":"intermediate","question":"How does the Z-algorithm achieve O(n) despite having nested loops?","answerFull":"The Z-algorithm maintains a window [l, r] that is the rightmost Z-box (substring matching a prefix). For i inside the box, Z[i] ≥ Z[i-l], so we skip those characters. For characters at or past r, we extend by comparing — but every comparison that succeeds pushes r rightward, and r can advance at most n times total. Characters outside the box and failed comparisons together account for O(n) work. The amortised analysis is the same as the \"two-pointer never moves backward\" argument.","correct":"The Z-algorithm maintains a window [l, r] that is the rightmost Z-box (substring matching a prefix).","distractors":["fail[i] = length of the longest proper prefix of pattern[:i+1] that is also a suffix of pattern[:i+1].","Rolling hash: the hash of window[i+1..i+m] is derived from the hash of window[i..i+m-1] in O(1): subtract the contribution of the leftmost character, multiply by base, add the new character (all modulo a prime).","Z[i] = length of the longest substring starting at s[i] that matches a prefix of s."]},{"id":"cs_fundamentals/graph_and_string_algorithms#d5bc6dc045c4","section":"cs_fundamentals","module":"cs_fundamentals/graph_and_string_algorithms","moduleName":"graph and string algorithms","qIndex":11,"difficulty":"advanced","question":"How do you detect a cycle in an undirected vs a directed graph using DFS?","answerFull":"Undirected: during DFS, if a visited neighbour is not the direct parent of the current vertex (i.e., it's a back edge to an ancestor other than the parent), a cycle exists. Use parent tracking in the DFS call. Directed: use 3-colour DFS (white/unvisited, gray/in-stack, black/done). A gray neighbour is a back edge = cycle. Black neighbours are safe (already fully explored). Undirected cycle detection is simpler because any DFS tree back edge creates a cycle; in directed graphs, only back edges (to gray nodes) create directed cycles — cross edges and forward edges do not.","correct":"Undirected: during DFS, if a visited neighbour is not the direct parent of the current vertex (i.e., it's a back edge to an ancestor other than the parent), a cycle exists.","distractors":["O((V + E) log V). Each vertex is extracted from the heap once: V extractions × O(log V) each = O(V log V).","Dijkstra's invariant: once a vertex u is extracted from the heap, dist[u] is final.","Prim's and Dijkstra's use the same greedy structure: maintain a priority queue of (cost, vertex), extract the minimum-cost vertex, relax its edges."]},{"id":"cs_fundamentals/graph_and_string_algorithms#20476b240911","section":"cs_fundamentals","module":"cs_fundamentals/graph_and_string_algorithms","moduleName":"graph and string algorithms","qIndex":12,"difficulty":"advanced","question":"When would you use Floyd-Warshall over Dijkstra for shortest paths?","answerFull":"Floyd-Warshall is O(V³) and computes all-pairs shortest paths (APSP). Use it when: you need APSP and V is small (≤500 — beyond that, 500³ = 1.25×10^8 operations becomes slow); the graph has negative weights but no negative cycles. Dijkstra with re-running from each source is O(V(V+E) log V) — faster than Floyd-Warshall for sparse graphs but equally fast for dense. Floyd-Warshall is also trivially simple to implement (three nested loops).","correct":"Floyd-Warshall is O(V³) and computes all-pairs shortest paths (APSP).","distractors":["Prim's and Dijkstra's use the same greedy structure: maintain a priority queue of (cost, vertex), extract the minimum-cost vertex, relax its edges.","A* extends Dijkstra by adding a heuristic h(v) that estimates the remaining distance from v to the target.","BFS gives the correct shortest path in terms of hop count (for unweighted graphs) because it processes nodes in non-decreasing order of distance — the first time a node is reached is via the shortest path."]},{"id":"cs_fundamentals/graph_and_string_algorithms#36c37ee9194b","section":"cs_fundamentals","module":"cs_fundamentals/graph_and_string_algorithms","moduleName":"graph and string algorithms","qIndex":13,"difficulty":"advanced","question":"Describe 0-1 BFS and when it applies.","answerFull":"0-1 BFS: graph where each edge has weight 0 or 1. Use a deque instead of a queue. For weight-0 edges, push the neighbour to the front (it's at the same distance); for weight-1 edges, push to the back. The deque is maintained in non-decreasing distance order. O(V + E) time — faster than Dijkstra's O((V+E) log V) for this special case. Application: minimum number of flips to get from a corrupted binary string to a target (each flip is cost 1; matching characters are cost 0).","correct":"0-1 BFS: graph where each edge has weight 0 or 1.","distractors":["Adjacency matrix: O(V²) space; O(1) edge lookup (u, v); O(V) to iterate all neighbours of u.","Kahn's BFS: compute in-degrees of all vertices.","After V-1 relaxation passes, all shortest paths (using at most V-1 edges, since shortest paths in a graph without negative cycles have no repeated vertices) have been found."]},{"id":"cs_fundamentals/graph_and_string_algorithms#37f5ff61f133","section":"cs_fundamentals","module":"cs_fundamentals/graph_and_string_algorithms","moduleName":"graph and string algorithms","qIndex":14,"difficulty":"advanced","question":"How is Dijkstra's algorithm related to Prim's MST algorithm?","answerFull":"Prim's and Dijkstra's use the same greedy structure: maintain a priority queue of (cost, vertex), extract the minimum-cost vertex, relax its edges. The difference: Dijkstra stores the shortest path distance from the source (cumulative). Prim's stores the minimum edge weight to connect the vertex to the current MST (local, non-cumulative). Implementation differs only in the key stored in the heap. Both are O((V+E) log V) with a binary heap.","correct":"Prim's and Dijkstra's use the same greedy structure: maintain a priority queue of (cost, vertex), extract the minimum-cost vertex, relax its edges.","distractors":["Undirected: during DFS, if a visited neighbour is not the direct parent of the current vertex (i.e., it's a back edge to an ancestor other than the parent), a cycle exists.","A* extends Dijkstra by adding a heuristic h(v) that estimates the remaining distance from v to the target.","Aho-Corasick is a multi-pattern search algorithm."]},{"id":"cs_fundamentals/graph_and_string_algorithms#c8894d693e07","section":"cs_fundamentals","module":"cs_fundamentals/graph_and_string_algorithms","moduleName":"graph and string algorithms","qIndex":15,"difficulty":"advanced","question":"Explain the Aho-Corasick algorithm at a high level.","answerFull":"Aho-Corasick is a multi-pattern search algorithm. Build: construct a trie of all patterns; add failure links (analogous to KMP failure function) that point to the longest proper suffix of the current prefix that is also a prefix in the trie. Search: scan the text once; at each character, follow the failure links to find all matching patterns in O(1) amortised per character. Total: O(n + M + k) where n = text length, M = total pattern length, k = number of matches. Used in Snort IDS, grep -F (multiple fixed strings), and spam filters.","correct":"Aho-Corasick is a multi-pattern search algorithm.","distractors":["fail[i] = length of the longest proper prefix of pattern[:i+1] that is also a suffix of pattern[:i+1].","The Z-algorithm maintains a window [l, r] that is the rightmost Z-box (substring matching a prefix).","For a 2D text (T × R matrix) and 2D pattern (t × r matrix): compute rolling hash of each row of the pattern and the rolling hash of r×t sub-blocks."]},{"id":"cs_fundamentals/graph_and_string_algorithms#9dd5e2b65c63","section":"cs_fundamentals","module":"cs_fundamentals/graph_and_string_algorithms","moduleName":"graph and string algorithms","qIndex":16,"difficulty":"advanced","question":"What are the time and space trade-offs between adjacency list and adjacency matrix for graph algorithms?","answerFull":"Adjacency matrix: O(V²) space; O(1) edge lookup (u, v); O(V) to iterate all neighbours of u. Suitable for dense graphs (E ≈ V²) or when edge existence queries are frequent. Adjacency list: O(V + E) space; O(degree) to check edge existence; O(degree) to iterate neighbours. Suitable for sparse graphs (E ≈ O(V)) — most real-world graphs (social networks, road networks). BFS and Dijkstra run in O(V²) with adjacency matrix (all-pairs neighbour scan), but O(V+E) with adjacency list + heap — a critical difference for sparse graphs.","correct":"Adjacency matrix: O(V²) space; O(1) edge lookup (u, v); O(V) to iterate all neighbours of u.","distractors":["0-1 BFS: graph where each edge has weight 0 or 1.","After V-1 relaxation passes, all shortest paths (using at most V-1 edges, since shortest paths in a graph without negative cycles have no repeated vertices) have been found.","For a 2D text (T × R matrix) and 2D pattern (t × r matrix): compute rolling hash of each row of the pattern and the rolling hash of r×t sub-blocks."]},{"id":"cs_fundamentals/graph_and_string_algorithms#9162e8158f12","section":"cs_fundamentals","module":"cs_fundamentals/graph_and_string_algorithms","moduleName":"graph and string algorithms","qIndex":17,"difficulty":"advanced","question":"How does the Rabin-Karp algorithm extend to 2D pattern matching?","answerFull":"For a 2D text (T × R matrix) and 2D pattern (t × r matrix): compute rolling hash of each row of the pattern and the rolling hash of r×t sub-blocks. Step 1: for each row in the text, compute the 1D rolling hash of every r-length window → produces a T×(R-r+1) hash matrix. Step 2: treat each column of this hash matrix as a 1D string; apply rolling hash over t-length windows → positions where both row-hash and column-hash match are candidate positions; verify with O(tr) character comparison. Total O(TR + tr) expected.","correct":"For a 2D text (T × R matrix) and 2D pattern (t × r matrix): compute rolling hash of each row of the pattern and the rolling hash of r×t sub-blocks.","distractors":["fail[i] = length of the longest proper prefix of pattern[:i+1] that is also a suffix of pattern[:i+1].","Rolling hash: the hash of window[i+1..i+m] is derived from the hash of window[i..i+m-1] in O(1): subtract the contribution of the leftmost character, multiply by base, add the new character (all modulo a prime).","Adjacency matrix: O(V²) space; O(1) edge lookup (u, v); O(V) to iterate all neighbours of u."]},{"id":"cs_fundamentals/graph_and_string_algorithms#166874b4b1ae","section":"cs_fundamentals","module":"cs_fundamentals/graph_and_string_algorithms","moduleName":"graph and string algorithms","qIndex":18,"difficulty":"advanced","question":"Explain why A* is faster than Dijkstra for grid pathfinding and what makes a heuristic admissible.","answerFull":"A* extends Dijkstra by adding a heuristic h(v) that estimates the remaining distance from v to the target. The priority key becomes f(v) = dist[v] + h(v) instead of dist[v]. A* explores nodes in order of estimated total path cost — nodes that are closer to the target on the heuristic get explored first, pruning large regions of the graph. Admissibility: h(v) ≤ true remaining distance. If h ever overestimates, A* may miss the shortest path. Consistency (monotone): h(u) ≤ cost(u,v) + h(v) for all edges (u,v) — ensures each node is finalised at most once (same as Dijkstra's invariant, extended with the heuristic). Manhattan distance on a grid is admissible and consistent.","correct":"A* extends Dijkstra by adding a heuristic h(v) that estimates the remaining distance from v to the target.","distractors":["Prim's and Dijkstra's use the same greedy structure: maintain a priority queue of (cost, vertex), extract the minimum-cost vertex, relax its edges.","BFS gives the correct shortest path in terms of hop count (for unweighted graphs) because it processes nodes in non-decreasing order of distance — the first time a node is reached is via the shortest path.","Dijkstra's invariant: once a vertex u is extracted from the heap, dist[u] is final."]},{"id":"cs_fundamentals/graphs_tries_and_advanced_structures#2b4ce2d7b310","section":"cs_fundamentals","module":"cs_fundamentals/graphs_tries_and_advanced_structures","moduleName":"graphs tries and advanced structures","qIndex":1,"difficulty":"core","question":"What is the difference between BFS and DFS and when do you choose each?","answerFull":"BFS explores level by level using a queue — finds the shortest path in unweighted graphs. DFS explores depth-first using a stack (or recursion) — finds any path, detects cycles, and computes topological order. Choose BFS for shortest path / minimum hops. Choose DFS for cycle detection, topological sort, connected components, and backtracking problems.","correct":"BFS explores level by level using a queue — finds the shortest path in unweighted graphs.","distractors":["Use DFS with three node states: WHITE (unvisited), GRAY (in current recursion stack), BLACK (fully processed).","A Bloom filter is a bit array of m bits, initialised to 0.","Given a 2D grid of '1' (land) and '0' (water), count connected land components."]},{"id":"cs_fundamentals/graphs_tries_and_advanced_structures#cef2e9c70642","section":"cs_fundamentals","module":"cs_fundamentals/graphs_tries_and_advanced_structures","moduleName":"graphs tries and advanced structures","qIndex":2,"difficulty":"core","question":"When would you use a trie instead of a hash map?","answerFull":"When you need prefix queries, autocomplete, or longest-prefix match. A hash map gives O(L) exact lookup but O(n×L) for \"all words starting with prefix X\" (full scan). A trie gives O(L + results) for the same prefix query. Also choose trie when the keyspace is dense (many shared prefixes) — the shared prefix chains save memory vs storing each full key.","correct":"When you need prefix queries, autocomplete, or longest-prefix match.","distractors":["Insert all dictionary words into a trie.","Segment tree. A Fenwick tree directly supports prefix sums but NOT range minimum (min is not invertible — you can't un-subtract a minimum).","A Bloom filter is a bit array of m bits, initialised to 0."]},{"id":"cs_fundamentals/graphs_tries_and_advanced_structures#008ddcadf234","section":"cs_fundamentals","module":"cs_fundamentals/graphs_tries_and_advanced_structures","moduleName":"graphs tries and advanced structures","qIndex":3,"difficulty":"core","question":"Explain Union-Find path compression. Why does it make find() nearly O(1)?","answerFull":"Without compression, find(x) walks up a chain to the root: O(depth). Path compression rewrites all nodes on that chain to point directly to the root. Subsequent finds for those same nodes are O(1). Combined with union by rank (which bounds initial depth to O(log n)), the amortised cost per operation becomes O(α(n)) — the inverse Ackermann function, effectively ≤ 4 for any n ≤ 10^80. In practice, treat it as O(1).","correct":"Without compression, find(x) walks up a chain to the root: O(depth).","distractors":["Union-Find / DSU. For each edge addition (u, v), call union(u, v).","BFS explores level by level using a queue — finds the shortest path in unweighted graphs.","A Bloom filter is a bit array of m bits, initialised to 0."]},{"id":"cs_fundamentals/graphs_tries_and_advanced_structures#3d5a5fe05123","section":"cs_fundamentals","module":"cs_fundamentals/graphs_tries_and_advanced_structures","moduleName":"graphs tries and advanced structures","qIndex":4,"difficulty":"core","question":"What is the time complexity of Kruskal's MST algorithm and why?","answerFull":"O(E log E) dominated by sorting edges by weight. The DSU operations (V-1 unions + E finds) run in O(E × α(V)) ≈ O(E). Sorting dominates. Space is O(V) for the DSU structure.","correct":"O(E log E) dominated by sorting edges by weight.","distractors":["Kruskal: sort all edges by weight, greedily add an edge if it doesn't form a cycle (DSU for cycle detection).","A Bloom filter is a bit array of m bits, initialised to 0.","BFS explores level by level using a queue — finds the shortest path in unweighted graphs."]},{"id":"cs_fundamentals/graphs_tries_and_advanced_structures#89d2606291b3","section":"cs_fundamentals","module":"cs_fundamentals/graphs_tries_and_advanced_structures","moduleName":"graphs tries and advanced structures","qIndex":5,"difficulty":"core","question":"How do you detect a cycle in a directed graph?","answerFull":"Use DFS with three node states: WHITE (unvisited), GRAY (in current recursion stack), BLACK (fully processed). If DFS encounters a GRAY node, a back edge exists — cycle detected. This is distinct from undirected cycle detection, where you only track visited/unvisited. O(V+E).","correct":"Use DFS with three node states: WHITE (unvisited), GRAY (in current recursion stack), BLACK (fully processed).","distractors":["Given n courses and prerequisite pairs, return an ordering or detect impossibility.","Kruskal: sort all edges by weight, greedily add an edge if it doesn't form a cycle (DSU for cycle detection).","O(n). Build proceeds by calling _build on each of the 2n-1 nodes of the complete binary tree — O(1) work per node (just a sum of two children)."]},{"id":"cs_fundamentals/graphs_tries_and_advanced_structures#569cfe263a0a","section":"cs_fundamentals","module":"cs_fundamentals/graphs_tries_and_advanced_structures","moduleName":"graphs tries and advanced structures","qIndex":6,"difficulty":"intermediate","question":"What is topological sort and when is it impossible?","answerFull":"Topological sort is a linear ordering of vertices in a DAG such that every directed edge u→v has u before v. Impossible if the graph contains a cycle. Kahn's algorithm (BFS with in-degree tracking) produces a topological order and detects cycles: if the final order has fewer than V nodes, a cycle exists.","correct":"Topological sort is a linear ordering of vertices in a DAG such that every directed edge u→v has u before v.","distractors":["Kruskal: sort all edges by weight, greedily add an edge if it doesn't form a cycle (DSU for cycle detection).","Given n courses and prerequisite pairs, return an ordering or detect impossibility.","Union-Find / DSU. For each edge addition (u, v), call union(u, v)."]},{"id":"cs_fundamentals/graphs_tries_and_advanced_structures#fa65d9ea3289","section":"cs_fundamentals","module":"cs_fundamentals/graphs_tries_and_advanced_structures","moduleName":"graphs tries and advanced structures","qIndex":7,"difficulty":"intermediate","question":"How does a Bloom filter work and what guarantees does it give?","answerFull":"A Bloom filter is a bit array of m bits, initialised to 0. To add an element: compute k independent hash functions → set k bits. To query: check if all k bits are 1. Guarantees: (1) No false negatives — if an element is in the set, all its bits are set. (2) Possible false positives — k bits may be set by different elements. FPR ≈ (1 - e^(-kn/m))^k; optimal k = (m/n) ln 2. Cannot delete elements in a standard Bloom filter (use Counting Bloom filter instead).","correct":"A Bloom filter is a bit array of m bits, initialised to 0.","distractors":["In two's complement, -i inverts all bits of i and adds 1.","O(n). Build proceeds by calling _build on each of the 2n-1 nodes of the complete binary tree — O(1) work per node (just a sum of two children).","O(n × L × ALPHABET_SIZE) in the worst case (no shared prefixes, array-based children)."]},{"id":"cs_fundamentals/graphs_tries_and_advanced_structures#9130c98c1053","section":"cs_fundamentals","module":"cs_fundamentals/graphs_tries_and_advanced_structures","moduleName":"graphs tries and advanced structures","qIndex":8,"difficulty":"intermediate","question":"Segment tree vs Fenwick tree — which would you use for range minimum queries with point updates?","answerFull":"Segment tree. A Fenwick tree directly supports prefix sums but NOT range minimum (min is not invertible — you can't un-subtract a minimum). Segment trees support any associative aggregate (sum, min, max, GCD) for range queries with point updates. Use Fenwick when you specifically need prefix sums and want simpler code.","correct":"Segment tree. A Fenwick tree directly supports prefix sums but NOT range minimum (min is not invertible — you can't un-subtract a minimum).","distractors":["O(n). Build proceeds by calling _build on each of the 2n-1 nodes of the complete binary tree — O(1) work per node (just a sum of two children).","Lazy propagation handles range updates efficiently.","When you need prefix queries, autocomplete, or longest-prefix match."]},{"id":"cs_fundamentals/graphs_tries_and_advanced_structures#68924ed77ccc","section":"cs_fundamentals","module":"cs_fundamentals/graphs_tries_and_advanced_structures","moduleName":"graphs tries and advanced structures","qIndex":9,"difficulty":"intermediate","question":"What is the space complexity of a trie with n words of average length L?","answerFull":"O(n × L × ALPHABET_SIZE) in the worst case (no shared prefixes, array-based children). With dictionary-based children (only existing edges stored): O(n × L). For English words with shared prefixes, practical space is much lower — roughly O(total unique characters). A 250K-word dictionary uses ~10 MB as a trie.","correct":"O(n × L × ALPHABET_SIZE) in the worst case (no shared prefixes, array-based children).","distractors":["A Bloom filter is a bit array of m bits, initialised to 0.","O(n). Build proceeds by calling _build on each of the 2n-1 nodes of the complete binary tree — O(1) work per node (just a sum of two children).","Insert all dictionary words into a trie."]},{"id":"cs_fundamentals/graphs_tries_and_advanced_structures#24b6b6962168","section":"cs_fundamentals","module":"cs_fundamentals/graphs_tries_and_advanced_structures","moduleName":"graphs tries and advanced structures","qIndex":10,"difficulty":"intermediate","question":"How does a Fenwick tree's i & (-i) operation work?","answerFull":"In two's complement, -i inverts all bits of i and adds 1. i & (-i) isolates the lowest set bit. This lowest set bit encodes the range of indices that position i is responsible for in the BIT. For update, adding i & (-i) moves to the next ancestor that covers a larger range. For query, subtracting i & (-i) moves to the parent responsible for the prefix ending just before i's range.","correct":"In two's complement, -i inverts all bits of i and adds 1.","distractors":["Segment tree. A Fenwick tree directly supports prefix sums but NOT range minimum (min is not invertible — you can't un-subtract a minimum).","A Bloom filter is a bit array of m bits, initialised to 0.","O(n). Build proceeds by calling _build on each of the 2n-1 nodes of the complete binary tree — O(1) work per node (just a sum of two children)."]},{"id":"cs_fundamentals/graphs_tries_and_advanced_structures#f42ea5b6c52f","section":"cs_fundamentals","module":"cs_fundamentals/graphs_tries_and_advanced_structures","moduleName":"graphs tries and advanced structures","qIndex":11,"difficulty":"advanced","question":"You need to find if there is a path between two nodes in a dynamic graph where edges are added over time. Which data structure do you use?","answerFull":"Union-Find / DSU. For each edge addition (u, v), call union(u, v). To query connectivity: find(u) == find(v). Each operation is O(α(n)) ≈ O(1). If edges were also removed (dynamic connectivity with deletions), DSU cannot be used — you need link-cut trees (significantly more complex).","correct":"Union-Find / DSU. For each edge addition (u, v), call union(u, v).","distractors":["BFS explores level by level using a queue — finds the shortest path in unweighted graphs.","Kruskal: sort all edges by weight, greedily add an edge if it doesn't form a cycle (DSU for cycle detection).","O(n). Build proceeds by calling _build on each of the 2n-1 nodes of the complete binary tree — O(1) work per node (just a sum of two children)."]},{"id":"cs_fundamentals/graphs_tries_and_advanced_structures#5e762da9c33d","section":"cs_fundamentals","module":"cs_fundamentals/graphs_tries_and_advanced_structures","moduleName":"graphs tries and advanced structures","qIndex":12,"difficulty":"advanced","question":"What is the \"number of islands\" problem and what are the two main approaches?","answerFull":"Given a 2D grid of '1' (land) and '0' (water), count connected land components. Approach 1: BFS/DFS — for each unvisited '1', do BFS/DFS to mark the entire island. O(m×n). Approach 2: Union-Find — treat each '1' cell as a DSU node, union adjacent land cells, count final components. O(m×n × α(m×n)). BFS is simpler; DSU handles dynamic additions elegantly.","correct":"Given a 2D grid of '1' (land) and '0' (water), count connected land components.","distractors":["Given n courses and prerequisite pairs, return an ordering or detect impossibility.","A Bloom filter is a bit array of m bits, initialised to 0.","BFS explores level by level using a queue — finds the shortest path in unweighted graphs."]},{"id":"cs_fundamentals/graphs_tries_and_advanced_structures#addd65c4a52a","section":"cs_fundamentals","module":"cs_fundamentals/graphs_tries_and_advanced_structures","moduleName":"graphs tries and advanced structures","qIndex":13,"difficulty":"advanced","question":"How would you implement an autocomplete system using a trie?","answerFull":"Insert all dictionary words into a trie. For a query prefix: traverse to the prefix's terminal node, then DFS from that node to collect all words. To rank by frequency: augment each trie node with a max-frequency of words in its subtree — then use a heap-based DFS to return only the top-k completions without visiting all words. Query time: O(L + top-k log k) where L = prefix length.","correct":"Insert all dictionary words into a trie.","distractors":["When you need prefix queries, autocomplete, or longest-prefix match.","A Bloom filter is a bit array of m bits, initialised to 0.","BFS explores level by level using a queue — finds the shortest path in unweighted graphs."]},{"id":"cs_fundamentals/graphs_tries_and_advanced_structures#fc217b717158","section":"cs_fundamentals","module":"cs_fundamentals/graphs_tries_and_advanced_structures","moduleName":"graphs tries and advanced structures","qIndex":14,"difficulty":"advanced","question":"What is the time complexity of building a segment tree and why?","answerFull":"O(n). Build proceeds by calling _build on each of the 2n-1 nodes of the complete binary tree — O(1) work per node (just a sum of two children). Total: O(n). This is analogous to heapify — work decreases exponentially toward the leaves and sums to O(n) via geometric series.","correct":"O(n). Build proceeds by calling _build on each of the 2n-1 nodes of the complete binary tree — O(1) work per node (just a sum of two children).","distractors":["O(n × L × ALPHABET_SIZE) in the worst case (no shared prefixes, array-based children).","Use DFS with three node states: WHITE (unvisited), GRAY (in current recursion stack), BLACK (fully processed).","Segment tree. A Fenwick tree directly supports prefix sums but NOT range minimum (min is not invertible — you can't un-subtract a minimum)."]},{"id":"cs_fundamentals/graphs_tries_and_advanced_structures#39d51fccb7a6","section":"cs_fundamentals","module":"cs_fundamentals/graphs_tries_and_advanced_structures","moduleName":"graphs tries and advanced structures","qIndex":15,"difficulty":"advanced","question":"Can you explain lazy propagation in segment trees?","answerFull":"Lazy propagation handles range updates efficiently. Instead of updating all O(n) leaves for a range-add operation, attach a \"pending\" delta to each covered segment tree node. When you later query or update a child of that node, you first \"push down\" the pending delta to both children. This defers work to when it is actually needed — range update and range query both remain O(log n) instead of O(n).","correct":"Lazy propagation handles range updates efficiently.","distractors":["Segment tree. A Fenwick tree directly supports prefix sums but NOT range minimum (min is not invertible — you can't un-subtract a minimum).","A Bloom filter is a bit array of m bits, initialised to 0.","BFS explores level by level using a queue — finds the shortest path in unweighted graphs."]},{"id":"cs_fundamentals/graphs_tries_and_advanced_structures#cac1722d7670","section":"cs_fundamentals","module":"cs_fundamentals/graphs_tries_and_advanced_structures","moduleName":"graphs tries and advanced structures","qIndex":16,"difficulty":"advanced","question":"What is the difference between Kruskal's and Prim's MST algorithms?","answerFull":"Kruskal: sort all edges by weight, greedily add an edge if it doesn't form a cycle (DSU for cycle detection). Best for sparse graphs: O(E log E). Prim: start from any node, greedily add the cheapest edge crossing the cut between visited and unvisited nodes (min-heap). Best for dense graphs: O(E log V) with a heap, O(V²) with an array. Both produce a valid MST.","correct":"Kruskal: sort all edges by weight, greedily add an edge if it doesn't form a cycle (DSU for cycle detection).","distractors":["Union-Find / DSU. For each edge addition (u, v), call union(u, v).","Topological sort is a linear ordering of vertices in a DAG such that every directed edge u→v has u before v.","O(E log E) dominated by sorting edges by weight."]},{"id":"cs_fundamentals/graphs_tries_and_advanced_structures#50930a20364f","section":"cs_fundamentals","module":"cs_fundamentals/graphs_tries_and_advanced_structures","moduleName":"graphs tries and advanced structures","qIndex":17,"difficulty":"advanced","question":"How does a trie differ from a hash map for IP routing (longest-prefix match)?","answerFull":"IP addresses have a hierarchical structure (CIDR blocks like 192.168.0.0/16). A trie supports longest-prefix match naturally: traverse bit-by-bit and track the last matching prefix. A hash map can only do exact-match — you'd need to check all 32 possible prefix lengths, querying the map 32 times. Routers use radix/Patricia tries for O(32) = O(1) lookup.","correct":"IP addresses have a hierarchical structure (CIDR blocks like 192.168.0.0/16).","distractors":["Segment tree. A Fenwick tree directly supports prefix sums but NOT range minimum (min is not invertible — you can't un-subtract a minimum).","Insert all dictionary words into a trie.","When you need prefix queries, autocomplete, or longest-prefix match."]},{"id":"cs_fundamentals/graphs_tries_and_advanced_structures#07cf8de8620a","section":"cs_fundamentals","module":"cs_fundamentals/graphs_tries_and_advanced_structures","moduleName":"graphs tries and advanced structures","qIndex":18,"difficulty":"advanced","question":"Walk through the complexity of the \"Course Schedule II\" problem (topological sort).","answerFull":"Given n courses and prerequisite pairs, return an ordering or detect impossibility. Build an adjacency list and compute in-degrees: O(V+E). Run Kahn's BFS: each node is enqueued once (O(V)), each edge is processed once to decrement in-degrees (O(E)). Total: O(V+E). If the output length < n, a cycle exists (circular prerequisites) — return empty. Space: O(V+E) for the graph, O(V) for the queue and in-degree array.","correct":"Given n courses and prerequisite pairs, return an ordering or detect impossibility.","distractors":["Topological sort is a linear ordering of vertices in a DAG such that every directed edge u→v has u before v.","Kruskal: sort all edges by weight, greedily add an edge if it doesn't form a cycle (DSU for cycle detection).","Given a 2D grid of '1' (land) and '0' (water), count connected land components."]},{"id":"cs_fundamentals/greedy_and_divide_and_conquer#e49428b626dc","section":"cs_fundamentals","module":"cs_fundamentals/greedy_and_divide_and_conquer","moduleName":"greedy and divide and conquer","qIndex":1,"difficulty":"core","question":"What is the exchange argument and how do you apply it to prove greedy correctness?","answerFull":"The exchange argument proves that the greedy choice can always be swapped into any optimal solution without making it worse. Steps: (1) Assume there is an optimal solution O that differs from the greedy solution G. (2) Find the first point of difference — greedy made choice x, optimal made choice y. (3) Show that swapping y for x in O produces a solution O' that is at least as good as O. (4) By induction, the fully swapped solution equals G and is optimal.","correct":"The exchange argument proves that the greedy choice can always be swapped into any optimal solution without making it worse.","distractors":["Dijkstra's is a greedy algorithm: at each step, extract the unvisited vertex with the minimum known distance (greedy choice), then relax its outgoing edges.","Activity selection (unweighted): maximise the count of non-overlapping intervals — solvable by greedy (earliest finish time) in O(n log n).","Greedy by value/weight ratio fails for 0/1 (indivisible items) knapsack."]},{"id":"cs_fundamentals/greedy_and_divide_and_conquer#7641241d6f81","section":"cs_fundamentals","module":"cs_fundamentals/greedy_and_divide_and_conquer","moduleName":"greedy and divide and conquer","qIndex":2,"difficulty":"core","question":"Why does earliest-finish-time greedy work for interval scheduling?","answerFull":"Proof by exchange argument: take any optimal solution O. If O includes a different first interval than greedy, swap in the greedy's first interval (earliest finish time). Because the greedy interval finishes no later, it cannot block more future intervals than the original choice — the number of intervals selected stays the same or increases. Repeat until O equals the greedy solution. Therefore greedy achieves the maximum count.","correct":"Proof by exchange argument: take any optimal solution O.","distractors":["Dijkstra's is a greedy algorithm: at each step, extract the unvisited vertex with the minimum known distance (greedy choice), then relax its outgoing edges.","The exchange argument proves that the greedy choice can always be swapped into any optimal solution without making it worse.","Greedy by value/weight ratio fails for 0/1 (indivisible items) knapsack."]},{"id":"cs_fundamentals/greedy_and_divide_and_conquer#b8a795730e21","section":"cs_fundamentals","module":"cs_fundamentals/greedy_and_divide_and_conquer","moduleName":"greedy and divide and conquer","qIndex":3,"difficulty":"core","question":"When does greedy fail for knapsack and why?","answerFull":"Greedy by value/weight ratio fails for 0/1 (indivisible items) knapsack. Counterexample: capacity=10, items={(v=6,w=4), (v=5,w=3), (v=5,w=3)}. Greedy (ratio: 1.5, 1.67, 1.67) takes the two ratio-1.67 items (total w=6, v=10). Optimal: all three items, total w=10, v=16. The greedy fails because committing to the highest-ratio item may leave capacity that combines better with lower-ratio items. Fractional knapsack (items divisible) is solvable by greedy.","correct":"Greedy by value/weight ratio fails for 0/1 (indivisible items) knapsack.","distractors":["Activity selection (unweighted): maximise the count of non-overlapping intervals — solvable by greedy (earliest finish time) in O(n log n).","Fractional knapsack allows taking fractions of items, solved greedily by value/weight ratio.","Dijkstra's is a greedy algorithm: at each step, extract the unvisited vertex with the minimum known distance (greedy choice), then relax its outgoing edges."]},{"id":"cs_fundamentals/greedy_and_divide_and_conquer#e8dcc8338404","section":"cs_fundamentals","module":"cs_fundamentals/greedy_and_divide_and_conquer","moduleName":"greedy and divide and conquer","qIndex":4,"difficulty":"core","question":"State the Master theorem and apply it to merge sort and binary search.","answerFull":"T(n) = aT(n/b) + O(n^d). Compare d vs log_b(a). Merge sort: a=2, b=2, d=1. log_2(2)=1=d → Case 2 → O(n log n). Binary search: a=1, b=2, d=0. log_2(1)=0=d → Case 2 → O(log n). Karatsuba: a=3, b=2, d=1. log_2(3)≈1.585 > 1=d → Case 3 → O(n^1.585).","correct":"T(n) = aT(n/b) + O(n^d). Compare d vs log_b(a).","distractors":["Activity selection (unweighted): maximise the count of non-overlapping intervals — solvable by greedy (earliest finish time) in O(n log n).","Building the Huffman tree: O(n log n) using a min-heap of size n.","Case 3: d < log_b(a) → T(n) = O(n^log_b(a))."]},{"id":"cs_fundamentals/greedy_and_divide_and_conquer#cb5b63322cc7","section":"cs_fundamentals","module":"cs_fundamentals/greedy_and_divide_and_conquer","moduleName":"greedy and divide and conquer","qIndex":5,"difficulty":"core","question":"What is Huffman coding and why is it optimal?","answerFull":"Huffman assigns shorter bit codes to higher-frequency symbols. It is optimal among prefix-free codes (no code is a prefix of another). Optimality proof: suppose a non-Huffman code is better. The two symbols with lowest frequency must have the longest codes (exchange argument: swapping a longer code to a lower-frequency symbol cannot increase average code length). Huffman always assigns the two lowest-frequency symbols the longest codes (siblings at max depth) — this is the greedy induction step. By induction, the greedy tree minimises expected code length.","correct":"Huffman assigns shorter bit codes to higher-frequency symbols.","distractors":["Proof by exchange argument: take any optimal solution O.","Building the Huffman tree: O(n log n) using a min-heap of size n.","The exchange argument proves that the greedy choice can always be swapped into any optimal solution without making it worse."]},{"id":"cs_fundamentals/greedy_and_divide_and_conquer#ea48589320bb","section":"cs_fundamentals","module":"cs_fundamentals/greedy_and_divide_and_conquer","moduleName":"greedy and divide and conquer","qIndex":6,"difficulty":"intermediate","question":"What is Karatsuba multiplication and why is it faster than the naive O(n²) algorithm?","answerFull":"Naive multiplication of two n-digit numbers: n² multiply-and-add operations. Karatsuba's insight: break each number into two halves (high and low digits). Instead of 4 multiplications of n/2-digit numbers (still O(n²)), use 3 multiplications via algebraic identity: (a+b)(c+d) - ac - bd = ad + bc. T(n) = 3T(n/2) + O(n) → O(n^log_2(3)) ≈ O(n^1.585). Used in GMP (GNU Multiple Precision) for large integer arithmetic in cryptography.","correct":"Naive multiplication of two n-digit numbers: n² multiply-and-add operations.","distractors":["Dijkstra's is a greedy algorithm: at each step, extract the unvisited vertex with the minimum known distance (greedy choice), then relax its outgoing edges.","Activity selection (unweighted): maximise the count of non-overlapping intervals — solvable by greedy (earliest finish time) in O(n log n).","Building the Huffman tree: O(n log n) using a min-heap of size n."]},{"id":"cs_fundamentals/greedy_and_divide_and_conquer#80c0445a7ec1","section":"cs_fundamentals","module":"cs_fundamentals/greedy_and_divide_and_conquer","moduleName":"greedy and divide and conquer","qIndex":7,"difficulty":"intermediate","question":"How do you count inversions in an array in O(n log n)?","answerFull":"Piggyback on merge sort. An inversion is a pair (i, j) with i < j and arr[i] > arr[j]. During the merge step: when an element from the right subarray is placed before all remaining left subarray elements, the number of inversions is the count of remaining left elements. This can be accumulated during the merge pass. Total inversions = left inversions + right inversions + split inversions (counted during merge). O(n log n) — same as merge sort.","correct":"Piggyback on merge sort. An inversion is a pair (i, j) with i < j and arr[i] > arr[j].","distractors":["Given n points, find the pair with minimum Euclidean distance.","Building the Huffman tree: O(n log n) using a min-heap of size n.","Case 3: d < log_b(a) → T(n) = O(n^log_b(a))."]},{"id":"cs_fundamentals/greedy_and_divide_and_conquer#88efa1a34171","section":"cs_fundamentals","module":"cs_fundamentals/greedy_and_divide_and_conquer","moduleName":"greedy and divide and conquer","qIndex":8,"difficulty":"intermediate","question":"What is the closest pair of points problem and why is the divide step non-trivial?","answerFull":"Given n points, find the pair with minimum Euclidean distance. Brute force: O(n²). D&C: split points by x-coordinate, find min dist in each half, then check the strip around the dividing line. The key insight is that within the strip of width 2d (where d = min of two halves), each point has at most 7 other points within distance d — a geometric packing argument (points in a d×2d rectangle, each pair ≥ d apart, fit at most 8 points). This makes the strip scan O(n) rather than O(n²).","correct":"Given n points, find the pair with minimum Euclidean distance.","distractors":["Dijkstra's is a greedy algorithm: at each step, extract the unvisited vertex with the minimum known distance (greedy choice), then relax its outgoing edges.","Piggyback on merge sort. An inversion is a pair (i, j) with i < j and arr[i] > arr[j].","Activity selection (unweighted): maximise the count of non-overlapping intervals — solvable by greedy (earliest finish time) in O(n log n)."]},{"id":"cs_fundamentals/greedy_and_divide_and_conquer#f0db755e59fc","section":"cs_fundamentals","module":"cs_fundamentals/greedy_and_divide_and_conquer","moduleName":"greedy and divide and conquer","qIndex":9,"difficulty":"intermediate","question":"Explain the D&C approach to the maximum subarray problem (Kadane's context).","answerFull":"D&C: split the array at midpoint. Maximum subarray is either fully in the left half, fully in the right half, or crosses the midpoint. The crossing case: find the maximum suffix sum of the left half and the maximum prefix sum of the right half in O(n) by scanning from midpoint outward. Recurrence: T(n) = 2T(n/2) + O(n) → O(n log n). Kadane's algorithm is O(n) (greedy/DP). D&C is useful when the query must be answered for arbitrary sub-ranges (offline segment tree variant).","correct":"D&C: split the array at midpoint.","distractors":["Activity selection (unweighted): maximise the count of non-overlapping intervals — solvable by greedy (earliest finish time) in O(n log n).","Building the Huffman tree: O(n log n) using a min-heap of size n.","Case 3: d < log_b(a) → T(n) = O(n^log_b(a))."]},{"id":"cs_fundamentals/greedy_and_divide_and_conquer#bb1bd0b2cd50","section":"cs_fundamentals","module":"cs_fundamentals/greedy_and_divide_and_conquer","moduleName":"greedy and divide and conquer","qIndex":10,"difficulty":"intermediate","question":"How does the activity selection problem differ from the weighted interval scheduling problem?","answerFull":"Activity selection (unweighted): maximise the count of non-overlapping intervals — solvable by greedy (earliest finish time) in O(n log n). Weighted interval scheduling: each interval has a weight; maximise total weight of non-overlapping intervals. Greedy fails (a high-weight interval may block many medium-weight intervals). Requires DP: sort by finish time, dp[i] = max weight using intervals up to i, with binary search to find the last non-conflicting interval. O(n log n).","correct":"Activity selection (unweighted): maximise the count of non-overlapping intervals — solvable by greedy (earliest finish time) in O(n log n).","distractors":["T(n) = aT(n/b) + O(n^d). Compare d vs log_b(a).","The exchange argument proves that the greedy choice can always be swapped into any optimal solution without making it worse.","Dijkstra's is a greedy algorithm: at each step, extract the unvisited vertex with the minimum known distance (greedy choice), then relax its outgoing edges."]},{"id":"cs_fundamentals/greedy_and_divide_and_conquer#c2dfd88d5ee5","section":"cs_fundamentals","module":"cs_fundamentals/greedy_and_divide_and_conquer","moduleName":"greedy and divide and conquer","qIndex":11,"difficulty":"advanced","question":"What is the fractional relaxation of 0/1 knapsack, and why is its LP value an upper bound?","answerFull":"Fractional knapsack allows taking fractions of items, solved greedily by value/weight ratio. This is the linear programming (LP) relaxation of 0/1 knapsack. The LP value ≥ the integer (0/1) optimum because the feasible set of the LP strictly contains the integer feasible set. This is exploited in branch-and-bound algorithms: LP relaxation gives an upper bound at each node; prune branches where the LP bound < current best integer solution.","correct":"Fractional knapsack allows taking fractions of items, solved greedily by value/weight ratio.","distractors":["Greedy by value/weight ratio fails for 0/1 (indivisible items) knapsack.","Activity selection (unweighted): maximise the count of non-overlapping intervals — solvable by greedy (earliest finish time) in O(n log n).","Building the Huffman tree: O(n log n) using a min-heap of size n."]},{"id":"cs_fundamentals/greedy_and_divide_and_conquer#7ae99e5c2cd8","section":"cs_fundamentals","module":"cs_fundamentals/greedy_and_divide_and_conquer","moduleName":"greedy and divide and conquer","qIndex":12,"difficulty":"advanced","question":"What is the Master theorem's Case 3 (leaves dominate) and give an example?","answerFull":"Case 3: d < log_b(a) → T(n) = O(n^log_b(a)). The number of leaves dominates — the recursion tree fans out so fast that the work at the leaf level overwhelms all other levels. Example: Strassen's matrix multiplication: a=7, b=2, d=2. log_2(7) ≈ 2.807 > 2 → Case 3 → O(n^2.807). The 7 recursive calls on n/2 sub-matrices (instead of 8 in naive) produce a sub-quadratic algorithm.","correct":"Case 3: d < log_b(a) → T(n) = O(n^log_b(a)).","distractors":["Building the Huffman tree: O(n log n) using a min-heap of size n.","T(n) = aT(n/b) + O(n^d). Compare d vs log_b(a).","Activity selection (unweighted): maximise the count of non-overlapping intervals — solvable by greedy (earliest finish time) in O(n log n)."]},{"id":"cs_fundamentals/greedy_and_divide_and_conquer#86600d2d01a2","section":"cs_fundamentals","module":"cs_fundamentals/greedy_and_divide_and_conquer","moduleName":"greedy and divide and conquer","qIndex":13,"difficulty":"advanced","question":"How does greedy apply to Dijkstra's shortest path algorithm?","answerFull":"Dijkstra's is a greedy algorithm: at each step, extract the unvisited vertex with the minimum known distance (greedy choice), then relax its outgoing edges. The greedy choice property holds because: once a vertex is extracted with distance d, no shorter path can be found to it later — any alternative path through an unvisited vertex must pass through a vertex with distance ≥ d (assuming non-negative weights), so the alternative total path length ≥ d + non-negative = ≥ d. This fails with negative weights, which is why Bellman-Ford (which is not greedy) is required for negative-weight graphs.","correct":"Dijkstra's is a greedy algorithm: at each step, extract the unvisited vertex with the minimum known distance (greedy choice), then relax its outgoing edges.","distractors":["Greedy by value/weight ratio fails for 0/1 (indivisible items) knapsack.","Activity selection (unweighted): maximise the count of non-overlapping intervals — solvable by greedy (earliest finish time) in O(n log n).","Given n points, find the pair with minimum Euclidean distance."]},{"id":"cs_fundamentals/greedy_and_divide_and_conquer#55c1ddd54a2a","section":"cs_fundamentals","module":"cs_fundamentals/greedy_and_divide_and_conquer","moduleName":"greedy and divide and conquer","qIndex":14,"difficulty":"advanced","question":"What is the time complexity of Huffman encoding and why?","answerFull":"Building the Huffman tree: O(n log n) using a min-heap of size n. Each of the n-1 merge steps requires two heap pops and one push, each O(log n) → total O(n log n). Encoding: O(L) per symbol where L is the code length. The average code length is O(H) where H is the entropy of the source — Huffman achieves within 1 bit of the entropy (Shannon's source coding theorem).","correct":"Building the Huffman tree: O(n log n) using a min-heap of size n.","distractors":["Case 3: d < log_b(a) → T(n) = O(n^log_b(a)).","Activity selection (unweighted): maximise the count of non-overlapping intervals — solvable by greedy (earliest finish time) in O(n log n).","Huffman assigns shorter bit codes to higher-frequency symbols."]},{"id":"cs_fundamentals/greedy_and_divide_and_conquer#523b328d001b","section":"cs_fundamentals","module":"cs_fundamentals/greedy_and_divide_and_conquer","moduleName":"greedy and divide and conquer","qIndex":15,"difficulty":"advanced","question":"Explain divide and conquer in the context of parallel algorithms.","answerFull":"D&C maps directly to fork-join parallelism. Divide the problem, spawn a thread/task per sub-problem (fork), wait for all to finish (join), then combine. Speedup: if each level takes O(n) work and there are log n levels, and we have P processors, the parallel time is O(n/P × log n + combine time). MapReduce is exactly this pattern at cluster scale. The independence of sub-problems is the key property — it is what distinguishes parallelisable D&C from DP (which has data dependencies between states).","correct":"D&C maps directly to fork-join parallelism.","distractors":["Activity selection (unweighted): maximise the count of non-overlapping intervals — solvable by greedy (earliest finish time) in O(n log n).","Building the Huffman tree: O(n log n) using a min-heap of size n.","Case 3: d < log_b(a) → T(n) = O(n^log_b(a))."]},{"id":"cs_fundamentals/heaps_and_priority_queues#5e6a12b5bfdc","section":"cs_fundamentals","module":"cs_fundamentals/heaps_and_priority_queues","moduleName":"heaps and priority queues","qIndex":1,"difficulty":"core","question":"What is the time complexity of building a heap from n elements?","answerFull":"O(n). Calling heapify (sift-down from every internal node) costs O(n) because half the nodes are leaves (O(1) work each), a quarter cost O(log 2), etc. — the geometric sum converges to 2n. Inserting n elements one by one is O(n log n). This is one of the most commonly mis-stated complexities.","correct":"O(n). Calling heapify (sift-down from every internal node) costs O(n) because half the nodes are leaves (O(1) work each), a quarter cost O(log 2), etc. — the geometric sum converges to 2n.","distractors":["Maintain a min-heap of size k.","Python compares tuples lexicographically — if the first elements tie, it compares the second.","Maintain two heaps: a max-heap lower (lower half) and min-heap upper (upper half)."]},{"id":"cs_fundamentals/heaps_and_priority_queues#9e5970cb144a","section":"cs_fundamentals","module":"cs_fundamentals/heaps_and_priority_queues","moduleName":"heaps and priority queues","qIndex":2,"difficulty":"core","question":"Why does Python's heapq.nlargest(k, arr) use a heap instead of sorting?","answerFull":"For k << n, maintaining a size-k min-heap costs O(n log k) — you scan all n items but only maintain k items in the heap. Sorting costs O(n log n). When k=1 it is O(n), same as a linear scan. nlargest switches internally to sorted() when k is close to n.","correct":"For k << n, maintaining a size-k min-heap costs O(n log k) — you scan all n items but only maintain k items in the heap.","distractors":["Maintain a min-heap of size k.","Python's heapq (and Java's PriorityQueue) do not support decrease-key in O(log n) without additional bookkeeping.","O(N log k). Initialise a min-heap with the first element of each list (k elements)."]},{"id":"cs_fundamentals/heaps_and_priority_queues#94cc6a48cfcb","section":"cs_fundamentals","module":"cs_fundamentals/heaps_and_priority_queues","moduleName":"heaps and priority queues","qIndex":3,"difficulty":"core","question":"How do you find the median of a streaming dataset efficiently?","answerFull":"Maintain two heaps: a max-heap lower (lower half) and min-heap upper (upper half). Keep sizes balanced (differ by at most 1). After each insertion, the median is either the root of lower (odd total) or the average of both roots (even total). Each insert is O(log n), each median query O(1).","correct":"Maintain two heaps: a max-heap lower (lower half) and min-heap upper (upper half).","distractors":["Given array nums and window size k, find the max in every k-size window.","For k << n, maintaining a size-k min-heap costs O(n log k) — you scan all n items but only maintain k items in the heap.","A binary heap gives O((V+E) log V) with simple implementation — good enough for most graphs."]},{"id":"cs_fundamentals/heaps_and_priority_queues#3af22f1a34a5","section":"cs_fundamentals","module":"cs_fundamentals/heaps_and_priority_queues","moduleName":"heaps and priority queues","qIndex":4,"difficulty":"core","question":"You need to implement Dijkstra. What heap variant should you choose and why?","answerFull":"A binary heap gives O((V+E) log V) with simple implementation — good enough for most graphs. A 4-ary heap gives ~30% fewer comparisons for Dijkstra (you push more than you pop). A Fibonacci heap gives O(E + V log V) theoretically but has huge constants; only worth it for extremely dense graphs where E >> V log V.","correct":"A binary heap gives O((V+E) log V) with simple implementation — good enough for most graphs.","distractors":["For k << n, maintaining a size-k min-heap costs O(n log k) — you scan all n items but only maintain k items in the heap.","A d-ary heap gives each node d children.","Maintain two heaps: a max-heap lower (lower half) and min-heap upper (upper half)."]},{"id":"cs_fundamentals/heaps_and_priority_queues#4a606edd4648","section":"cs_fundamentals","module":"cs_fundamentals/heaps_and_priority_queues","moduleName":"heaps and priority queues","qIndex":5,"difficulty":"core","question":"What is the difference between a heap and a BST?","answerFull":"A heap only guarantees the root is extreme; arbitrary search is O(n). A BST guarantees full sorted order and supports O(log n) search for any key. Heap push/pop is O(log n) and cache-friendly (array). BST insert/find is O(log h) but pointer-heavy (cache-unfriendly). Choose heap for priority-queue use cases, BST for sorted-set use cases.","correct":"A heap only guarantees the root is extreme; arbitrary search is O(n).","distractors":["Maintain two heaps: a max-heap lower (lower half) and min-heap upper (upper half).","Perfectly fine — Python allows duplicates in a heap and compares by value.","A binary heap gives O((V+E) log V) with simple implementation — good enough for most graphs."]},{"id":"cs_fundamentals/heaps_and_priority_queues#a72de6e51945","section":"cs_fundamentals","module":"cs_fundamentals/heaps_and_priority_queues","moduleName":"heaps and priority queues","qIndex":6,"difficulty":"intermediate","question":"How does Java's PriorityQueue behave when you iterate it?","answerFull":"Iteration order is NOT sorted. Java's PriorityQueue is backed by a binary heap array; iterating gives heap-storage order, which is not a sorted traversal. To get sorted output you must drain via poll() or copy to an array and sort. This surprises many Java developers who assume the collection is \"sorted.\"","correct":"Iteration order is NOT sorted.","distractors":["Python's heapq (and Java's PriorityQueue) do not support decrease-key in O(log n) without additional bookkeeping.","A binary heap gives O((V+E) log V) with simple implementation — good enough for most graphs.","A d-ary heap gives each node d children."]},{"id":"cs_fundamentals/heaps_and_priority_queues#f2546867b424","section":"cs_fundamentals","module":"cs_fundamentals/heaps_and_priority_queues","moduleName":"heaps and priority queues","qIndex":7,"difficulty":"intermediate","question":"A heap's pop() is O(log n). Can you implement a heap that supports O(1) delete-any-element?","answerFull":"Yes — augment with a HashMap {value → index_in_heap_array}. When you want to delete element x: look up its index i, swap A[i] with A[last], pop the last, then sift-up or sift-down from i. This is called an \"indexed heap\" and is used in Prim's MST and some Dijkstra implementations.","correct":"Yes — augment with a HashMap {value → index_in_heap_array}.","distractors":["For k << n, maintaining a size-k min-heap costs O(n log k) — you scan all n items but only maintain k items in the heap.","Given array nums and window size k, find the max in every k-size window.","O(N log k). Initialise a min-heap with the first element of each list (k elements)."]},{"id":"cs_fundamentals/heaps_and_priority_queues#873dc2c0e692","section":"cs_fundamentals","module":"cs_fundamentals/heaps_and_priority_queues","moduleName":"heaps and priority queues","qIndex":8,"difficulty":"intermediate","question":"Why is heapsort not used in practice even though it is O(n log n) worst-case?","answerFull":"Cache performance. Heapsort's access pattern jumps between parent and children in a large array — cache-unfriendly. Quicksort has O(n²) worst case but its sequential access pattern produces fewer cache misses in practice, making it 2–5× faster on real hardware. Introsort (used in C++ std::sort) uses quicksort with heapsort as a fallback to avoid the O(n²) case.","correct":"Cache performance. Heapsort's access pattern jumps between parent and children in a large array — cache-unfriendly.","distractors":["Python's heapq (and Java's PriorityQueue) do not support decrease-key in O(log n) without additional bookkeeping.","For k << n, maintaining a size-k min-heap costs O(n log k) — you scan all n items but only maintain k items in the heap.","O(n). Calling heapify (sift-down from every internal node) costs O(n) because half the nodes are leaves (O(1) work each), a quarter cost O(log 2), etc. — the geometric sum converges to 2n."]},{"id":"cs_fundamentals/heaps_and_priority_queues#31b230bc6645","section":"cs_fundamentals","module":"cs_fundamentals/heaps_and_priority_queues","moduleName":"heaps and priority queues","qIndex":9,"difficulty":"intermediate","question":"You have k sorted lists with a total of N elements. What is the merge complexity and why?","answerFull":"O(N log k). Initialise a min-heap with the first element of each list (k elements). Each of the N pops takes O(log k), and each pop is followed by one push (also O(log k)). Total: 2N operations × O(log k) each = O(N log k). Space is O(k) for the heap, O(N) for the output.","correct":"O(N log k). Initialise a min-heap with the first element of each list (k elements).","distractors":["Python compares tuples lexicographically — if the first elements tie, it compares the second.","heappushpop: pushes item first, then pops min.","A d-ary heap gives each node d children."]},{"id":"cs_fundamentals/heaps_and_priority_queues#6e33b6985f6f","section":"cs_fundamentals","module":"cs_fundamentals/heaps_and_priority_queues","moduleName":"heaps and priority queues","qIndex":10,"difficulty":"intermediate","question":"What does Python's heapq module push to a heap of tuples when priorities are equal?","answerFull":"Python compares tuples lexicographically — if the first elements tie, it compares the second. If the second element is not comparable (e.g., a custom object without lt), Python raises TypeError. The standard fix is inserting a monotonically increasing counter as the second element of the tuple, so ties are broken by insertion order without ever reaching the object comparison.","correct":"Python compares tuples lexicographically — if the first elements tie, it compares the second.","distractors":["Python's heapq (and Java's PriorityQueue) do not support decrease-key in O(log n) without additional bookkeeping.","A binary heap gives O((V+E) log V) with simple implementation — good enough for most graphs.","For k << n, maintaining a size-k min-heap costs O(n log k) — you scan all n items but only maintain k items in the heap."]},{"id":"cs_fundamentals/heaps_and_priority_queues#c81bdfe906ea","section":"cs_fundamentals","module":"cs_fundamentals/heaps_and_priority_queues","moduleName":"heaps and priority queues","qIndex":11,"difficulty":"advanced","question":"Can you use a heap to sort in descending order in O(n log n) with O(1) extra space?","answerFull":"Yes — this is heapsort. Build a max-heap in-place in O(n), then repeatedly swap the root (maximum) with the last element and sift-down on the reduced heap. After n swaps, the array is sorted ascending. For descending: build a min-heap instead. Space is O(1) extra (heapify is in-place).","correct":"Yes — this is heapsort. Build a max-heap in-place in O(n), then repeatedly swap the root (maximum) with the last element and sift-down on the reduced heap.","distractors":["Maintain two heaps: a max-heap lower (lower half) and min-heap upper (upper half).","O(n). Calling heapify (sift-down from every internal node) costs O(n) because half the nodes are leaves (O(1) work each), a quarter cost O(log 2), etc. — the geometric sum converges to 2n.","O(N log k). Initialise a min-heap with the first element of each list (k elements)."]},{"id":"cs_fundamentals/heaps_and_priority_queues#fcc7dcf2a456","section":"cs_fundamentals","module":"cs_fundamentals/heaps_and_priority_queues","moduleName":"heaps and priority queues","qIndex":12,"difficulty":"advanced","question":"What is the meeting rooms II problem and how does a heap solve it?","answerFull":"Given intervals [start, end], find the minimum number of conference rooms required. Sort by start time. Use a min-heap of end times (one per active room). For each new meeting: if its start ≥ min end-time in the heap, reuse that room (pop and push new end-time). Else open a new room (push). The heap size at the end = answer. O(n log n).","correct":"Given intervals [start, end], find the minimum number of conference rooms required.","distractors":["A binary heap gives O((V+E) log V) with simple implementation — good enough for most graphs.","Maintain two heaps: a max-heap lower (lower half) and min-heap upper (upper half).","For k << n, maintaining a size-k min-heap costs O(n log k) — you scan all n items but only maintain k items in the heap."]},{"id":"cs_fundamentals/heaps_and_priority_queues#5ca6a7c288cb","section":"cs_fundamentals","module":"cs_fundamentals/heaps_and_priority_queues","moduleName":"heaps and priority queues","qIndex":13,"difficulty":"advanced","question":"How does Python heapq handle duplicate values?","answerFull":"Perfectly fine — Python allows duplicates in a heap and compares by value. The heap property remains consistent. If you push 5 twice, you will pop 5 twice. No deduplication happens.","correct":"Perfectly fine — Python allows duplicates in a heap and compares by value.","distractors":["Maintain two heaps: a max-heap lower (lower half) and min-heap upper (upper half).","A binary heap gives O((V+E) log V) with simple implementation — good enough for most graphs.","A heap only guarantees the root is extreme; arbitrary search is O(n)."]},{"id":"cs_fundamentals/heaps_and_priority_queues#10c037327a7b","section":"cs_fundamentals","module":"cs_fundamentals/heaps_and_priority_queues","moduleName":"heaps and priority queues","qIndex":14,"difficulty":"advanced","question":"What is the \"sliding window maximum\" problem and can a heap solve it efficiently?","answerFull":"Given array nums and window size k, find the max in every k-size window. A max-heap can solve it in O(n log n) with lazy deletion: push (value, index), when you pop, discard entries with index < window_start. A monotonic deque solves it in O(n) — preferred. The heap approach is simpler to implement in an interview when optimal complexity is not required.","correct":"Given array nums and window size k, find the max in every k-size window.","distractors":["Maintain a min-heap of size k.","Given intervals [start, end], find the minimum number of conference rooms required.","Maintain two heaps: a max-heap lower (lower half) and min-heap upper (upper half)."]},{"id":"cs_fundamentals/heaps_and_priority_queues#15715df21a62","section":"cs_fundamentals","module":"cs_fundamentals/heaps_and_priority_queues","moduleName":"heaps and priority queues","qIndex":15,"difficulty":"advanced","question":"Describe the two-heap pattern for finding the k-th largest element dynamically.","answerFull":"Maintain a min-heap of size k. For each new element: push it. If size > k, pop the minimum. The root of the size-k min-heap is the k-th largest element seen so far. Each operation O(log k). This is the basis for LeetCode 703 \"Kth Largest Element in a Stream.\"","correct":"Maintain a min-heap of size k.","distractors":["A d-ary heap gives each node d children.","Maintain two heaps: a max-heap lower (lower half) and min-heap upper (upper half).","Perfectly fine — Python allows duplicates in a heap and compares by value."]},{"id":"cs_fundamentals/heaps_and_priority_queues#cbd6b8e4ea74","section":"cs_fundamentals","module":"cs_fundamentals/heaps_and_priority_queues","moduleName":"heaps and priority queues","qIndex":16,"difficulty":"advanced","question":"What happens internally when you call heapq.heappushpop(heap, item) vs heapq.heapreplace(heap, item)?","answerFull":"heappushpop: pushes item first, then pops min. Equivalent to push+pop but implemented as one operation (avoids sifting twice when item is already ≤ current min). heapreplace: pops min first, then pushes item — assumes heap is non-empty. heapreplace is faster than a pop+push pair by roughly a factor of 2. Use heapreplace in the k-largest window pattern.","correct":"heappushpop: pushes item first, then pops min.","distractors":["A heap only guarantees the root is extreme; arbitrary search is O(n).","A binary heap gives O((V+E) log V) with simple implementation — good enough for most graphs.","O(N log k). Initialise a min-heap with the first element of each list (k elements)."]},{"id":"cs_fundamentals/heaps_and_priority_queues#b1f224ec144d","section":"cs_fundamentals","module":"cs_fundamentals/heaps_and_priority_queues","moduleName":"heaps and priority queues","qIndex":17,"difficulty":"advanced","question":"In Dijkstra's algorithm, why do we \"lazy delete\" stale heap entries rather than updating them in place?","answerFull":"Python's heapq (and Java's PriorityQueue) do not support decrease-key in O(log n) without additional bookkeeping. The lazy approach: simply push a new (shorter_dist, node) entry when you find a shorter path. When you pop an entry, check if the stored distance matches dist[node]; if not, skip it (it is stale). This adds at most E entries to the heap (one per edge relaxation) — still O((V+E) log V).","correct":"Python's heapq (and Java's PriorityQueue) do not support decrease-key in O(log n) without additional bookkeeping.","distractors":["For k << n, maintaining a size-k min-heap costs O(n log k) — you scan all n items but only maintain k items in the heap.","Maintain a min-heap of size k.","Maintain two heaps: a max-heap lower (lower half) and min-heap upper (upper half)."]},{"id":"cs_fundamentals/heaps_and_priority_queues#109ae9fea8fd","section":"cs_fundamentals","module":"cs_fundamentals/heaps_and_priority_queues","moduleName":"heaps and priority queues","qIndex":18,"difficulty":"advanced","question":"What is a d-ary heap and when does increasing d improve performance?","answerFull":"A d-ary heap gives each node d children. Height = log_d(n) so sift-up takes O(log_d n) = O(log n / log d). Sift-down examines d children per level: O(d × log_d n). Rule of thumb: if the workload is push-heavy (more sift-ups than sift-downs) — e.g., Dijkstra on sparse graphs — a 4-ary heap reduces comparisons by ~50% vs a binary heap. If the workload is pop-heavy (heapsort), stick with d=2 to keep sift-down cheap.","correct":"A d-ary heap gives each node d children.","distractors":["Maintain two heaps: a max-heap lower (lower half) and min-heap upper (upper half).","Maintain a min-heap of size k.","A binary heap gives O((V+E) log V) with simple implementation — good enough for most graphs."]},{"id":"cs_fundamentals/linked_lists_stacks_and_queues#59d2864cce95","section":"cs_fundamentals","module":"cs_fundamentals/linked_lists_stacks_and_queues","moduleName":"linked lists stacks and queues","qIndex":1,"difficulty":"core","question":"How do you reverse a linked list in O(n) time and O(1) space?","answerFull":"Three-pointer iteration: prev = None, curr = head. Each iteration: save nxt = curr.next, set curr.next = prev, advance prev = curr and curr = nxt. Return prev (the new head). The key discipline: save curr.next before overwriting it — losing this reference is the most common bug.","correct":"Three-pointer iteration: prev = None, curr = head.","distractors":["After slow and fast meet (inside the cycle), reset slow to head.","Explicit stack: O(h) where h is the maximum depth (stored in a Python list or deque).","A fixed array with head and tail integer indices that wrap around: tail = (tail + 1) % capacity for enqueue, head = (head + 1) % capacity for dequeue."]},{"id":"cs_fundamentals/linked_lists_stacks_and_queues#a612aae56479","section":"cs_fundamentals","module":"cs_fundamentals/linked_lists_stacks_and_queues","moduleName":"linked lists stacks and queues","qIndex":2,"difficulty":"core","question":"How do you detect a cycle in a linked list?","answerFull":"Floyd's cycle detection (tortoise and hare). Two pointers start at head; slow moves 1 step, fast moves 2 steps per iteration. If fast reaches null, no cycle. If slow == fast, there is a cycle. O(n) time, O(1) space. The meeting happens because fast \"laps\" slow inside the cycle; the time to lap is bounded by the cycle length.","correct":"Floyd's cycle detection (tortoise and hare).","distractors":["After slow and fast meet (inside the cycle), reset slow to head.","Explicit stack: O(h) where h is the maximum depth (stored in a Python list or deque).","A dummy node is a placeholder node prepended to the list that is never returned as part of the answer."]},{"id":"cs_fundamentals/linked_lists_stacks_and_queues#239893fb5cf4","section":"cs_fundamentals","module":"cs_fundamentals/linked_lists_stacks_and_queues","moduleName":"linked lists stacks and queues","qIndex":3,"difficulty":"core","question":"How do you find the start of the cycle?","answerFull":"After slow and fast meet (inside the cycle), reset slow to head. Advance both slow and fast one step at a time. They meet at the cycle start. Mathematical proof: if the cycle start is distance F from the head, slow travels F steps from head; fast was F steps behind the start inside the cycle (provable from the meeting point calculation), so advancing both one-by-one brings them to the start simultaneously.","correct":"After slow and fast meet (inside the cycle), reset slow to head.","distractors":["Fast-slow pointer. Advance fast 2 steps and slow 1 step per iteration.","Floyd's cycle detection (tortoise and hare).","A fixed array with head and tail integer indices that wrap around: tail = (tail + 1) % capacity for enqueue, head = (head + 1) % capacity for dequeue."]},{"id":"cs_fundamentals/linked_lists_stacks_and_queues#bd11ba3a5440","section":"cs_fundamentals","module":"cs_fundamentals/linked_lists_stacks_and_queues","moduleName":"linked lists stacks and queues","qIndex":4,"difficulty":"core","question":"How do you find the middle of a linked list?","answerFull":"Fast-slow pointer. Advance fast 2 steps and slow 1 step per iteration. When fast reaches the end (fast is None or fast.next is None), slow is at the middle. For even-length lists, slow ends at the first of the two middle nodes. This is used as the split point in merge sort for linked lists.","correct":"Fast-slow pointer. Advance fast 2 steps and slow 1 step per iteration.","distractors":["Explicit stack: O(h) where h is the maximum depth (stored in a Python list or deque).","After slow and fast meet (inside the cycle), reset slow to head.","Find the middle (fast-slow pointer), reverse the second half in place, compare element-by-element with the first half, then restore the second half."]},{"id":"cs_fundamentals/linked_lists_stacks_and_queues#52767ca2a9ca","section":"cs_fundamentals","module":"cs_fundamentals/linked_lists_stacks_and_queues","moduleName":"linked lists stacks and queues","qIndex":5,"difficulty":"core","question":"How do you remove the Nth node from the end of a linked list in one pass?","answerFull":"Two pointers with a gap of n. Advance the first pointer n steps ahead. Then advance both one step at a time until the first pointer reaches the end. The second pointer is now at the (N+1)th node from the end — update its .next to skip the target. Edge case: use a dummy head to handle removal of the actual head node. O(n) time, O(1) space.","correct":"Two pointers with a gap of n.","distractors":["Explicit stack: O(h) where h is the maximum depth (stored in a Python list or deque).","A dummy node is a placeholder node prepended to the list that is never returned as part of the answer.","O(1) deletion of a node given a reference to it, without traversing from the head to find the predecessor."]},{"id":"cs_fundamentals/linked_lists_stacks_and_queues#68f2cdff3bbd","section":"cs_fundamentals","module":"cs_fundamentals/linked_lists_stacks_and_queues","moduleName":"linked lists stacks and queues","qIndex":6,"difficulty":"intermediate","question":"What is a monotonic stack and how is it used for \"next greater element\"?","answerFull":"A stack that maintains elements in monotonically increasing or decreasing order (by value). For \"next greater element\": iterate left to right; for each element, pop the stack while the top is smaller than the current element — those popped elements have found their next greater element (the current). Push the current index. O(n) total — each element pushed and popped at most once.","correct":"A stack that maintains elements in monotonically increasing or decreasing order (by value).","distractors":["Find the middle (fast-slow pointer), reverse the second half in place, compare element-by-element with the first half, then restore the second half.","Explicit stack: O(h) where h is the maximum depth (stored in a Python list or deque).","Maintain two stacks: the main stack and an auxiliary min-stack."]},{"id":"cs_fundamentals/linked_lists_stacks_and_queues#4410ffd77e0c","section":"cs_fundamentals","module":"cs_fundamentals/linked_lists_stacks_and_queues","moduleName":"linked lists stacks and queues","qIndex":7,"difficulty":"intermediate","question":"How do you implement a queue using two stacks?","answerFull":"Stack1 for enqueue, stack2 for dequeue. On dequeue: if stack2 is empty, move all elements from stack1 to stack2 (reversing order → FIFO). Amortized O(1) per operation: each element is pushed to stack1 once, moved to stack2 once, and popped from stack2 once — 3 operations total = O(1) amortized. Worst-case single dequeue is O(n) (when stack2 is empty and all elements must be moved).","correct":"Stack1 for enqueue, stack2 for dequeue.","distractors":["A fixed array with head and tail integer indices that wrap around: tail = (tail + 1) % capacity for enqueue, head = (head + 1) % capacity for dequeue.","Maintain two stacks: the main stack and an auxiliary min-stack.","A dummy node is a placeholder node prepended to the list that is never returned as part of the answer."]},{"id":"cs_fundamentals/linked_lists_stacks_and_queues#2935df54cfa1","section":"cs_fundamentals","module":"cs_fundamentals/linked_lists_stacks_and_queues","moduleName":"linked lists stacks and queues","qIndex":8,"difficulty":"intermediate","question":"Why is ArrayDeque preferred over LinkedList in Java for stack/queue usage?","answerFull":"ArrayDeque uses a circular array — all elements are contiguous in memory, giving excellent cache performance. LinkedList allocates a node object per element — each dequeue or enqueue involves object creation/GC and a pointer dereference (cache miss). Benchmarks show ArrayDeque is 2–5× faster than LinkedList for stack/queue workloads due to cache efficiency and reduced GC pressure.","correct":"ArrayDeque uses a circular array — all elements are contiguous in memory, giving excellent cache performance.","distractors":["Maintain two stacks: the main stack and an auxiliary min-stack.","A stack that maintains elements in monotonically increasing or decreasing order (by value).","A fixed array with head and tail integer indices that wrap around: tail = (tail + 1) % capacity for enqueue, head = (head + 1) % capacity for dequeue."]},{"id":"cs_fundamentals/linked_lists_stacks_and_queues#e693216c4228","section":"cs_fundamentals","module":"cs_fundamentals/linked_lists_stacks_and_queues","moduleName":"linked lists stacks and queues","qIndex":9,"difficulty":"intermediate","question":"What is the space complexity of DFS using an explicit stack versus recursion?","answerFull":"Explicit stack: O(h) where h is the maximum depth (stored in a Python list or deque). Recursion: O(h) call stack frames. Both are O(h). However, the explicit stack avoids CPython's 1000-frame recursion limit and can handle graphs with millions of nodes. Each explicit stack entry is just the node reference; each recursive stack frame includes local variables, return address, and the frame object overhead (~few hundred bytes in Python).","correct":"Explicit stack: O(h) where h is the maximum depth (stored in a Python list or deque).","distractors":["A dummy node is a placeholder node prepended to the list that is never returned as part of the answer.","A stack that maintains elements in monotonically increasing or decreasing order (by value).","Maintain two stacks: the main stack and an auxiliary min-stack."]},{"id":"cs_fundamentals/linked_lists_stacks_and_queues#cef823cd7a70","section":"cs_fundamentals","module":"cs_fundamentals/linked_lists_stacks_and_queues","moduleName":"linked lists stacks and queues","qIndex":10,"difficulty":"intermediate","question":"How do you merge K sorted linked lists in O(n log k) time?","answerFull":"Use a min-heap of size k. Initialize with the head of each list. Repeat: extract the minimum, append to result, push that node's successor into the heap. Total: n extractions, each O(log k) → O(n log k). Alternative: divide-and-conquer merge (merge pairs, repeat log k times) — also O(n log k).","correct":"Use a min-heap of size k.","distractors":["Maintain two stacks: the main stack and an auxiliary min-stack.","A dummy node is a placeholder node prepended to the list that is never returned as part of the answer.","A fixed array with head and tail integer indices that wrap around: tail = (tail + 1) % capacity for enqueue, head = (head + 1) % capacity for dequeue."]},{"id":"cs_fundamentals/linked_lists_stacks_and_queues#0683e17811d0","section":"cs_fundamentals","module":"cs_fundamentals/linked_lists_stacks_and_queues","moduleName":"linked lists stacks and queues","qIndex":11,"difficulty":"advanced","question":"What is the advantage of a doubly linked list over a singly linked list?","answerFull":"O(1) deletion of a node given a reference to it, without traversing from the head to find the predecessor. In a singly linked list, deleting a node requires knowing the predecessor, which takes O(n) to find (traverse from head). Doubly linked lists are used in LRU caches (OrderedDict, LinkedHashMap) where O(1) deletion of the least-recently-used node is required.","correct":"O(1) deletion of a node given a reference to it, without traversing from the head to find the predecessor.","distractors":["Three-pointer iteration: prev = None, curr = head.","A fixed array with head and tail integer indices that wrap around: tail = (tail + 1) % capacity for enqueue, head = (head + 1) % capacity for dequeue.","Explicit stack: O(h) where h is the maximum depth (stored in a Python list or deque)."]},{"id":"cs_fundamentals/linked_lists_stacks_and_queues#32a0949ceee0","section":"cs_fundamentals","module":"cs_fundamentals/linked_lists_stacks_and_queues","moduleName":"linked lists stacks and queues","qIndex":12,"difficulty":"advanced","question":"How do you check if a linked list is a palindrome in O(n) time and O(1) space?","answerFull":"Find the middle (fast-slow pointer), reverse the second half in place, compare element-by-element with the first half, then restore the second half. The O(1) space comes from reversing in place — no auxiliary storage for the second half. Carefully restore the list after comparison if the original must be preserved.","correct":"Find the middle (fast-slow pointer), reverse the second half in place, compare element-by-element with the first half, then restore the second half.","distractors":["Explicit stack: O(h) where h is the maximum depth (stored in a Python list or deque).","O(1) deletion of a node given a reference to it, without traversing from the head to find the predecessor.","Fast-slow pointer. Advance fast 2 steps and slow 1 step per iteration."]},{"id":"cs_fundamentals/linked_lists_stacks_and_queues#3f23725b5da2","section":"cs_fundamentals","module":"cs_fundamentals/linked_lists_stacks_and_queues","moduleName":"linked lists stacks and queues","qIndex":13,"difficulty":"advanced","question":"What is a sentinel/dummy node and why use it?","answerFull":"A dummy node is a placeholder node prepended to the list that is never returned as part of the answer. It simplifies code by eliminating special cases for operating on the head: insertions and deletions always have a predecessor (the dummy) so the if head is None and if prev is None checks disappear. Standard pattern: dummy = ListNode(0); dummy.next = head; ... return dummy.next.","correct":"A dummy node is a placeholder node prepended to the list that is never returned as part of the answer.","distractors":["Explicit stack: O(h) where h is the maximum depth (stored in a Python list or deque).","O(1) deletion of a node given a reference to it, without traversing from the head to find the predecessor.","A fixed array with head and tail integer indices that wrap around: tail = (tail + 1) % capacity for enqueue, head = (head + 1) % capacity for dequeue."]},{"id":"cs_fundamentals/linked_lists_stacks_and_queues#709616a8e02b","section":"cs_fundamentals","module":"cs_fundamentals/linked_lists_stacks_and_queues","moduleName":"linked lists stacks and queues","qIndex":14,"difficulty":"advanced","question":"How does a circular buffer implement a queue with O(1) operations?","answerFull":"A fixed array with head and tail integer indices that wrap around: tail = (tail + 1) % capacity for enqueue, head = (head + 1) % capacity for dequeue. Full condition: (tail + 1) % capacity == head. Empty condition: head == tail. All operations are O(1) arithmetic, no allocation. Used in OS kernel ring buffers (network packets, I/O events), audio processing, and Python's collections.deque internal implementation.","correct":"A fixed array with head and tail integer indices that wrap around: tail = (tail + 1) % capacity for enqueue, head = (head + 1) % capacity for dequeue.","distractors":["Three-pointer iteration: prev = None, curr = head.","Stack1 for enqueue, stack2 for dequeue.","ArrayDeque uses a circular array — all elements are contiguous in memory, giving excellent cache performance."]},{"id":"cs_fundamentals/linked_lists_stacks_and_queues#041b22114353","section":"cs_fundamentals","module":"cs_fundamentals/linked_lists_stacks_and_queues","moduleName":"linked lists stacks and queues","qIndex":15,"difficulty":"advanced","question":"How do you implement a min-stack (push, pop, top, getMin all in O(1))?","answerFull":"Maintain two stacks: the main stack and an auxiliary min-stack. On push: push to main; if the value is ≤ min-stack's top (or min-stack is empty), also push to min-stack. On pop: pop from main; if the popped value equals min-stack's top, also pop from min-stack. getMin() returns min-stack's top. The min-stack tracks the minimum at each \"level\" of the main stack.","correct":"Maintain two stacks: the main stack and an auxiliary min-stack.","distractors":["Explicit stack: O(h) where h is the maximum depth (stored in a Python list or deque).","A stack that maintains elements in monotonically increasing or decreasing order (by value).","Use a min-heap of size k."]},{"id":"cs_fundamentals/memory_management_and_virtual_memory#a74a26cc5843","section":"cs_fundamentals","module":"cs_fundamentals/memory_management_and_virtual_memory","moduleName":"memory management and virtual memory","qIndex":1,"difficulty":"core","question":"What is the difference between virtual memory and physical memory?","answerFull":"Physical memory is the actual DRAM in the machine. Virtual memory is an abstraction: each process sees a private virtual address space (0 to ~128 TiB on x86-64). The MMU translates virtual addresses to physical addresses on every access using the page table. Virtual address spaces can be larger than physical RAM — pages not currently needed are stored on swap disk. The key benefit is isolation: two processes can use the same virtual address (e.g., code loaded at 0x400000) without conflict — they map to different physical frames.","correct":"Physical memory is the actual DRAM in the machine.","distractors":["The Clock algorithm maintains a circular buffer of physical frames, each with a use-bit.","Bélády's anomaly: adding more physical frames causes more page faults, which is counterintuitive.","After fork(), the kernel marks all parent's writable pages as read-only in both parent and child page tables but still pointing to the same physical frames."]},{"id":"cs_fundamentals/memory_management_and_virtual_memory#69c9364c3494","section":"cs_fundamentals","module":"cs_fundamentals/memory_management_and_virtual_memory","moduleName":"memory management and virtual memory","qIndex":2,"difficulty":"core","question":"What is a page fault, and what is the difference between a soft and hard page fault?","answerFull":"A page fault is a hardware exception raised by the MMU when the accessed page's present-bit is 0. Soft (minor) page fault: the page is in physical memory but not mapped in the page table (e.g., first anonymous allocation via mmap, copy-on-write trigger, shared library already loaded for another process). The OS updates the page table entry with no I/O — handled in microseconds. Hard (major) page fault: the page must be read from swap disk or a file — takes ~100 µs (SSD) to ~10 ms (HDD). Frequent hard page faults cause \"thrashing\" — the system spends more time swapping than computing.","correct":"A page fault is a hardware exception raised by the MMU when the accessed page's present-bit is 0.","distractors":["The Clock algorithm maintains a circular buffer of physical frames, each with a use-bit.","Bélády's anomaly: adding more physical frames causes more page faults, which is counterintuitive.","After fork(), the kernel marks all parent's writable pages as read-only in both parent and child page tables but still pointing to the same physical frames."]},{"id":"cs_fundamentals/memory_management_and_virtual_memory#1ee4ed6d99ee","section":"cs_fundamentals","module":"cs_fundamentals/memory_management_and_virtual_memory","moduleName":"memory management and virtual memory","qIndex":3,"difficulty":"core","question":"How does the TLB work and what is a TLB miss?","answerFull":"The TLB (Translation Lookaside Buffer) is a small, fully associative hardware cache (64–1024 entries) that stores recent VPN→PFN translations. On every memory access, the MMU checks the TLB first. TLB hit: translation found in ~1 cycle. TLB miss: walk the multi-level page table in memory — 4 DRAM accesses on x86-64 (~400 ns total). Spatial and temporal locality keep TLB hit rates high (>99% for typical programs). Large working sets (hundreds of MB with random access) cause TLB thrashing — hit rate drops, every access pays 400 ns. Huge pages (2 MB each) reduce TLB entry consumption by 512×, mitigating this.","correct":"The TLB (Translation Lookaside Buffer) is a small, fully associative hardware cache (64–1024 entries) that stores recent VPN→PFN translations.","distractors":["Linux's default overcommit policy (vm.overcommit_memory=0) allows processes to allocate more virtual memory than available RAM + swap, because most allocations are never fully used.","The Clock algorithm maintains a circular buffer of physical frames, each with a use-bit.","A page fault is a hardware exception raised by the MMU when the accessed page's present-bit is 0."]},{"id":"cs_fundamentals/memory_management_and_virtual_memory#acf8e1dc53ae","section":"cs_fundamentals","module":"cs_fundamentals/memory_management_and_virtual_memory","moduleName":"memory management and virtual memory","qIndex":4,"difficulty":"core","question":"What is the Clock page-replacement algorithm and how does it approximate LRU?","answerFull":"The Clock algorithm maintains a circular buffer of physical frames, each with a use-bit. When a page is accessed, its use-bit is set. When a page must be evicted: the clock hand advances from its current position; if a frame's use-bit is 0, evict it; if the use-bit is 1, clear it (second chance) and advance. This approximates LRU: recently accessed pages have use-bit=1 (protected for one full revolution); pages not accessed in a full revolution are evicted. Linux uses a two-list variant (active/inactive lists) with similar second-chance semantics.","correct":"The Clock algorithm maintains a circular buffer of physical frames, each with a use-bit.","distractors":["After fork(), the kernel marks all parent's writable pages as read-only in both parent and child page tables but still pointing to the same physical frames.","The TLB (Translation Lookaside Buffer) is a small, fully associative hardware cache (64–1024 entries) that stores recent VPN→PFN translations.","The OS maintains a global physical frame allocator (buddy system)."]},{"id":"cs_fundamentals/memory_management_and_virtual_memory#91ad16f5f38d","section":"cs_fundamentals","module":"cs_fundamentals/memory_management_and_virtual_memory","moduleName":"memory management and virtual memory","qIndex":5,"difficulty":"core","question":"What is Bélády's anomaly and which algorithm exhibits it?","answerFull":"Bélády's anomaly: adding more physical frames causes more page faults, which is counterintuitive. It is observed with FIFO page replacement. Intuition: FIFO evicts the \"oldest\" loaded page, which may be a frequently-used page. Adding a frame changes the eviction pattern in a way that evicts pages needed sooner. LRU, OPT, and Clock do not exhibit Bélády's anomaly — they are \"stack algorithms\" (the set of pages in memory for n frames is always a subset of the pages for n+1 frames).","correct":"Bélády's anomaly: adding more physical frames causes more page faults, which is counterintuitive.","distractors":["A page fault is a hardware exception raised by the MMU when the accessed page's present-bit is 0.","The Clock algorithm maintains a circular buffer of physical frames, each with a use-bit.","The OS maintains a global physical frame allocator (buddy system)."]},{"id":"cs_fundamentals/memory_management_and_virtual_memory#05cbf3185b86","section":"cs_fundamentals","module":"cs_fundamentals/memory_management_and_virtual_memory","moduleName":"memory management and virtual memory","qIndex":6,"difficulty":"intermediate","question":"How does copy-on-write (COW) make fork() efficient?","answerFull":"After fork(), the kernel marks all parent's writable pages as read-only in both parent and child page tables but still pointing to the same physical frames. If neither process writes, no copying occurs — ideal for fork+exec (child immediately replaces its address space with exec). When either process writes to a shared page, the write causes a write-protection fault; the OS copies the page into a new physical frame and updates the faulting process's page table. Only actually-modified pages are copied. For a 2 GB process that fork-execs, only the few modified pages (environment, arguments) are copied — typically <1 MB.","correct":"After fork(), the kernel marks all parent's writable pages as read-only in both parent and child page tables but still pointing to the same physical frames.","distractors":["The working set W(t, Δ) is the set of pages referenced by a process in the time interval [t-Δ, t].","The Clock algorithm maintains a circular buffer of physical frames, each with a use-bit.","File-backed pages are backed by a file on disk — reads are initially page faults that load from the file; writes are dirty pages that can be flushed back to the file."]},{"id":"cs_fundamentals/memory_management_and_virtual_memory#6475e6171197","section":"cs_fundamentals","module":"cs_fundamentals/memory_management_and_virtual_memory","moduleName":"memory management and virtual memory","qIndex":7,"difficulty":"intermediate","question":"What is thrashing and how do you prevent it?","answerFull":"Thrashing occurs when the combined working sets of all runnable processes exceed physical RAM. The OS spends more time loading pages from swap and evicting pages than running processes. Prevention: (1) reduce the number of concurrent processes (working set model: only admit processes whose total working set fits in RAM); (2) add RAM; (3) swap to SSD instead of HDD (100 µs vs 10 ms latency reduces thrashing severity); (4) reduce process memory footprints (smaller JVM heaps, fewer concurrent connections per process); (5) use Linux's PSI (Pressure Stall Information) monitoring to detect memory pressure before thrashing begins.","correct":"Thrashing occurs when the combined working sets of all runnable processes exceed physical RAM.","distractors":["Physical memory is the actual DRAM in the machine.","Linux's default overcommit policy (vm.overcommit_memory=0) allows processes to allocate more virtual memory than available RAM + swap, because most allocations are never fully used.","The working set W(t, Δ) is the set of pages referenced by a process in the time interval [t-Δ, t]."]},{"id":"cs_fundamentals/memory_management_and_virtual_memory#0b4a0cc6c7b7","section":"cs_fundamentals","module":"cs_fundamentals/memory_management_and_virtual_memory","moduleName":"memory management and virtual memory","qIndex":8,"difficulty":"intermediate","question":"What is memory overcommit and what are its risks?","answerFull":"Linux's default overcommit policy (vm.overcommit_memory=0) allows processes to allocate more virtual memory than available RAM + swap, because most allocations are never fully used. Risk: if processes actually access their full allocations, the OOM killer terminates processes to reclaim memory — possibly killing critical services. Mitigation: (1) vm.overcommit_memory=2 (strict mode — refuse allocations that would exceed RAM + swap × overcommit_ratio); (2) set OOM score adjustments (oom_score_adj) to protect critical processes; (3) use container memory limits to bound individual processes.","correct":"Linux's default overcommit policy (vm.overcommit_memory=0) allows processes to allocate more virtual memory than available RAM + swap, because most allocations are never fully used.","distractors":["Physical memory is the actual DRAM in the machine.","The TLB (Translation Lookaside Buffer) is a small, fully associative hardware cache (64–1024 entries) that stores recent VPN→PFN translations.","VSZ (Virtual Size): total virtual address space reserved by the process — includes all mmap regions, stack reservations, JVM heap reservations, even pages not yet physically mapped."]},{"id":"cs_fundamentals/memory_management_and_virtual_memory#64bc79b7cfcf","section":"cs_fundamentals","module":"cs_fundamentals/memory_management_and_virtual_memory","moduleName":"memory management and virtual memory","qIndex":9,"difficulty":"intermediate","question":"Explain the difference between RSS and VSZ and which one actually matters.","answerFull":"VSZ (Virtual Size): total virtual address space reserved by the process — includes all mmap regions, stack reservations, JVM heap reservations, even pages not yet physically mapped. VSZ can be gigabytes for JVM processes that reserve heap upfront. RSS (Resident Set Size): physical memory actually mapped to the process's virtual address space right now — the number that actually consumes RAM. RSS matters for capacity planning; VSZ is usually misleading. cat /proc/<pid>/status shows VmRSS (RSS) and VmSize (VSZ). VmSwap shows how much of the RSS has been swapped out.","correct":"VSZ (Virtual Size): total virtual address space reserved by the process — includes all mmap regions, stack reservations, JVM heap reservations, even pages not yet physically mapped.","distractors":["After fork(), the kernel marks all parent's writable pages as read-only in both parent and child page tables but still pointing to the same physical frames.","Paging: memory divided into fixed-size pages (4 KB).","File-backed pages are backed by a file on disk — reads are initially page faults that load from the file; writes are dirty pages that can be flushed back to the file."]},{"id":"cs_fundamentals/memory_management_and_virtual_memory#956817bf4faa","section":"cs_fundamentals","module":"cs_fundamentals/memory_management_and_virtual_memory","moduleName":"memory management and virtual memory","qIndex":10,"difficulty":"intermediate","question":"How does the OS manage memory for multiple processes with limited RAM?","answerFull":"The OS maintains a global physical frame allocator (buddy system). When a process allocates a page and there are no free frames, the OS must evict an existing page. For a clean page (not modified since last loaded from file/disk), simply unmap it. For a dirty page (modified anonymous page), write it to swap first, then unmap. The page-replacement algorithm (Clock/LRU approximation) selects the victim. Modern Linux uses a two-list (active/inactive) scheme with second-chance promotion/demotion between lists.","correct":"The OS maintains a global physical frame allocator (buddy system).","distractors":["Thrashing occurs when the combined working sets of all runnable processes exceed physical RAM.","After fork(), the kernel marks all parent's writable pages as read-only in both parent and child page tables but still pointing to the same physical frames.","NUMA (Non-Uniform Memory Access) applies to multi-socket servers where each CPU socket has its own RAM bank."]},{"id":"cs_fundamentals/memory_management_and_virtual_memory#11fbc346d16b","section":"cs_fundamentals","module":"cs_fundamentals/memory_management_and_virtual_memory","moduleName":"memory management and virtual memory","qIndex":11,"difficulty":"advanced","question":"What is NUMA and how does it affect memory allocation?","answerFull":"NUMA (Non-Uniform Memory Access) applies to multi-socket servers where each CPU socket has its own RAM bank. Accessing local memory takes ~100 ns; accessing remote memory (via QPI/UPI interconnect) takes ~200–300 ns. The OS NUMA allocator (numactl --localalloc) tries to allocate memory on the NUMA node local to the CPU running the process. For latency-sensitive services, pin the process to a NUMA node and allocate from that node. numactl --hardware shows NUMA topology. NUMA-unaware JVMs may allocate objects on a remote node, adding latency to every GC and allocation.","correct":"NUMA (Non-Uniform Memory Access) applies to multi-socket servers where each CPU socket has its own RAM bank.","distractors":["Linux's default overcommit policy (vm.overcommit_memory=0) allows processes to allocate more virtual memory than available RAM + swap, because most allocations are never fully used.","Paging: memory divided into fixed-size pages (4 KB).","Physical memory is the actual DRAM in the machine."]},{"id":"cs_fundamentals/memory_management_and_virtual_memory#10db165bc112","section":"cs_fundamentals","module":"cs_fundamentals/memory_management_and_virtual_memory","moduleName":"memory management and virtual memory","qIndex":12,"difficulty":"advanced","question":"How do anonymous pages differ from file-backed pages?","answerFull":"File-backed pages are backed by a file on disk — reads are initially page faults that load from the file; writes are dirty pages that can be flushed back to the file. They can be evicted without going to swap (just discard — re-read from the file on next access). Anonymous pages have no file backing (heap, stack) — reads may be zero-filled on first access; dirty anonymous pages must go to swap if evicted. The OS prefers evicting file-backed pages (especially clean ones) before anonymous pages because file eviction has no write cost.","correct":"File-backed pages are backed by a file on disk — reads are initially page faults that load from the file; writes are dirty pages that can be flushed back to the file.","distractors":["VSZ (Virtual Size): total virtual address space reserved by the process — includes all mmap regions, stack reservations, JVM heap reservations, even pages not yet physically mapped.","A page fault is a hardware exception raised by the MMU when the accessed page's present-bit is 0.","The working set W(t, Δ) is the set of pages referenced by a process in the time interval [t-Δ, t]."]},{"id":"cs_fundamentals/memory_management_and_virtual_memory#b89122b9bdf8","section":"cs_fundamentals","module":"cs_fundamentals/memory_management_and_virtual_memory","moduleName":"memory management and virtual memory","qIndex":13,"difficulty":"advanced","question":"What is the working set model and how does it guide OS memory policy?","answerFull":"The working set W(t, Δ) is the set of pages referenced by a process in the time interval [t-Δ, t]. The working-set model says: a process should only be admitted to run when its working set fits in available RAM. If not enough RAM is available, a process is suspended (swapped out entirely). This prevents thrashing by ensuring no process runs without sufficient memory. Linux's page reclaim uses a recency-based approximation: pages in the \"active\" list are assumed to be in the working set; pages in the \"inactive\" list are candidates for eviction.","correct":"The working set W(t, Δ) is the set of pages referenced by a process in the time interval [t-Δ, t].","distractors":["Paging: memory divided into fixed-size pages (4 KB).","Thrashing occurs when the combined working sets of all runnable processes exceed physical RAM.","NUMA (Non-Uniform Memory Access) applies to multi-socket servers where each CPU socket has its own RAM bank."]},{"id":"cs_fundamentals/memory_management_and_virtual_memory#6a7f07775563","section":"cs_fundamentals","module":"cs_fundamentals/memory_management_and_virtual_memory","moduleName":"memory management and virtual memory","qIndex":14,"difficulty":"advanced","question":"What is the difference between paging and segmentation?","answerFull":"Paging: memory divided into fixed-size pages (4 KB). No external fragmentation; internal fragmentation in the last page. Address = virtual page number + offset. Supported by hardware MMU. Segmentation: memory divided into variable-size segments (code, data, stack). No internal fragmentation; external fragmentation creates holes. Address = segment number + offset. x86 originally used segmentation (CS, DS, SS, ES registers); modern 64-bit Linux uses flat segments (base=0, limit=max) effectively disabling segmentation in favour of paging. Segmentation is largely a legacy concept.","correct":"Paging: memory divided into fixed-size pages (4 KB).","distractors":["VSZ (Virtual Size): total virtual address space reserved by the process — includes all mmap regions, stack reservations, JVM heap reservations, even pages not yet physically mapped.","NUMA (Non-Uniform Memory Access) applies to multi-socket servers where each CPU socket has its own RAM bank.","File-backed pages are backed by a file on disk — reads are initially page faults that load from the file; writes are dirty pages that can be flushed back to the file."]},{"id":"cs_fundamentals/memory_management_and_virtual_memory#bc79eedccc32","section":"cs_fundamentals","module":"cs_fundamentals/memory_management_and_virtual_memory","moduleName":"memory management and virtual memory","qIndex":15,"difficulty":"advanced","question":"How does the Linux OOM killer decide which process to kill?","answerFull":"The OOM killer selects the process with the highest oom_score. oom_score = proportion of physical memory used by the process + adjustments. oom_score_adj (-1000 to +1000) lets you manually tune: -1000 = never kill this process (use for system-critical processes), +1000 = kill first. The OOM killer prefers processes that: (1) use the most memory; (2) have been running the shortest; (3) have the most children; (4) are not kernel threads. To protect a service: set oom_score_adj = -900 in its systemd unit file (OOMScoreAdjust=-900).","correct":"The OOM killer selects the process with the highest oom_score.","distractors":["Linux's default overcommit policy (vm.overcommit_memory=0) allows processes to allocate more virtual memory than available RAM + swap, because most allocations are never fully used.","The working set W(t, Δ) is the set of pages referenced by a process in the time interval [t-Δ, t].","VSZ (Virtual Size): total virtual address space reserved by the process — includes all mmap regions, stack reservations, JVM heap reservations, even pages not yet physically mapped."]},{"id":"cs_fundamentals/networking_fundamentals#239b5df23430","section":"cs_fundamentals","module":"cs_fundamentals/networking_fundamentals","moduleName":"networking fundamentals","qIndex":1,"difficulty":"core","question":"What happens if you call recv() once and assume you've received the full message?","answerFull":"You'll get a partial read. TCP is a byte stream; the OS splits data across segments based on MSS (~1460 B on Ethernet) and network conditions. A single send() of 4096 B may arrive as three recv() calls returning 1460, 1460, and 1176 bytes. The fix is a length-prefixed framing protocol and a loop that calls recv() until all expected bytes arrive.","correct":"You'll get a partial read.","distractors":["The SYN takes 0.5 RTT to reach the server, the SYN-ACK takes 0.5 RTT to return (total 1 RTT) — but the server cannot deliver data until the third ACK arrives (confirming the client received the SYN-ACK).","0-RTT allows the client to send application data with the first ClientHello, using keying material from a prior session ticket.","A DNS query and response each fit in a single datagram (~50–100 bytes), well under the 1500-byte MTU."]},{"id":"cs_fundamentals/networking_fundamentals#2cec2d2b35ec","section":"cs_fundamentals","module":"cs_fundamentals/networking_fundamentals","moduleName":"networking fundamentals","qIndex":2,"difficulty":"core","question":"Why does TIME_WAIT exist, and why can it cause problems in production?","answerFull":"TIME_WAIT ensures delayed duplicates from the closed connection don't corrupt a new connection reusing the same 5-tuple. It lasts 2×MSL (typically 60 s on Linux). Problem: a service making many short-lived outbound connections exhausts the ~28,000 ephemeral port range. Fixes: connection pooling, SO_REUSEADDR, reducing TIME_WAIT duration via net.ipv4.tcp_tw_reuse, or using HTTP keep-alive.","correct":"TIME_WAIT ensures delayed duplicates from the closed connection don't corrupt a new connection reusing the same 5-tuple.","distractors":["In HTTP/1.1, a connection processes one request-response pair at a time; if the first response is slow (large object, slow origin), all subsequent requests queue behind it.","0-RTT allows the client to send application data with the first ClientHello, using keying material from a prior session ticket.","A DNS query and response each fit in a single datagram (~50–100 bytes), well under the 1500-byte MTU."]},{"id":"cs_fundamentals/networking_fundamentals#ca65148435d5","section":"cs_fundamentals","module":"cs_fundamentals/networking_fundamentals","moduleName":"networking fundamentals","qIndex":3,"difficulty":"core","question":"Why is TCP's 3-way handshake 1.5 RTTs, not 1 RTT?","answerFull":"The SYN takes 0.5 RTT to reach the server, the SYN-ACK takes 0.5 RTT to return (total 1 RTT) — but the server cannot deliver data until the third ACK arrives (confirming the client received the SYN-ACK). So the client can send data with the ACK, but the first server response arrives 1.5 RTTs after the SYN. TCP Fast Open allows data in the initial SYN, reducing this to 0.5 RTT on cached connections.","correct":"The SYN takes 0.5 RTT to reach the server, the SYN-ACK takes 0.5 RTT to return (total 1 RTT) — but the server cannot deliver data until the third ACK arrives (confirming the client received the SYN-ACK).","distractors":["0-RTT allows the client to send application data with the first ClientHello, using keying material from a prior session ticket.","Slow start initializes TCP's congestion window (cwnd) to 1 MSS (later defaults raised to 10 MSS by RFC 6928).","TLS 1.3 merges the key exchange into the ClientHello (sending the ECDHE key share immediately) and the server responds with its key share, certificate, and Finished in one flight."]},{"id":"cs_fundamentals/networking_fundamentals#0ddf84e84943","section":"cs_fundamentals","module":"cs_fundamentals/networking_fundamentals","moduleName":"networking fundamentals","qIndex":4,"difficulty":"core","question":"What is the difference between flow control and congestion control in TCP?","answerFull":"Flow control (receiver window, rwnd) prevents a fast sender from overrunning a slow receiver's buffer — it is end-to-end between sender and receiver. Congestion control (cwnd: slow start, AIMD, CUBIC) prevents the sender from overloading intermediate routers — it is a response to network conditions inferred from packet loss or delay. The effective send window is min(rwnd, cwnd).","correct":"Flow control (receiver window, rwnd) prevents a fast sender from overrunning a slow receiver's buffer — it is end-to-end between sender and receiver.","distractors":["Slow start initializes TCP's congestion window (cwnd) to 1 MSS (later defaults raised to 10 MSS by RFC 6928).","MSS is the maximum amount of data in a single TCP segment, negotiated during the handshake.","In HTTP/1.1, a connection processes one request-response pair at a time; if the first response is slow (large object, slow origin), all subsequent requests queue behind it."]},{"id":"cs_fundamentals/networking_fundamentals#b2253070d95d","section":"cs_fundamentals","module":"cs_fundamentals/networking_fundamentals","moduleName":"networking fundamentals","qIndex":5,"difficulty":"core","question":"Why would you choose UDP over TCP for a DNS query?","answerFull":"A DNS query and response each fit in a single datagram (~50–100 bytes), well under the 1500-byte MTU. No connection is needed. If no response arrives within ~200 ms, the stub resolver retransmits. The overhead of a TCP handshake (1.5 RTT) would dominate the actual DNS query (which should resolve in <10 ms from a nearby resolver). UDP is used by default; TCP is used when responses exceed 512 bytes (DNSSEC, large zone transfers).","correct":"A DNS query and response each fit in a single datagram (~50–100 bytes), well under the 1500-byte MTU.","distractors":["Slow start initializes TCP's congestion window (cwnd) to 1 MSS (later defaults raised to 10 MSS by RFC 6928).","MSS is the maximum amount of data in a single TCP segment, negotiated during the handshake.","In HTTP/1.1, a connection processes one request-response pair at a time; if the first response is slow (large object, slow origin), all subsequent requests queue behind it."]},{"id":"cs_fundamentals/networking_fundamentals#104991029046","section":"cs_fundamentals","module":"cs_fundamentals/networking_fundamentals","moduleName":"networking fundamentals","qIndex":6,"difficulty":"intermediate","question":"What does TLS 1.3 do differently than TLS 1.2 to achieve 1 RTT?","answerFull":"TLS 1.3 merges the key exchange into the ClientHello (sending the ECDHE key share immediately) and the server responds with its key share, certificate, and Finished in one flight. In TLS 1.2, the server sent the certificate in a separate flight, requiring the client to verify it before sending key material. TLS 1.3 also removed RSA key exchange (which lacks forward secrecy) and deprecated all weak cipher suites.","correct":"TLS 1.3 merges the key exchange into the ClientHello (sending the ECDHE key share immediately) and the server responds with its key share, certificate, and Finished in one flight.","distractors":["0-RTT allows the client to send application data with the first ClientHello, using keying material from a prior session ticket.","The SYN takes 0.5 RTT to reach the server, the SYN-ACK takes 0.5 RTT to return (total 1 RTT) — but the server cannot deliver data until the third ACK arrives (confirming the client received the SYN-ACK).","A DNS query and response each fit in a single datagram (~50–100 bytes), well under the 1500-byte MTU."]},{"id":"cs_fundamentals/networking_fundamentals#09557b04f683","section":"cs_fundamentals","module":"cs_fundamentals/networking_fundamentals","moduleName":"networking fundamentals","qIndex":7,"difficulty":"intermediate","question":"What is 0-RTT resumption in TLS 1.3, and what is the security risk?","answerFull":"0-RTT allows the client to send application data with the first ClientHello, using keying material from a prior session ticket. This saves 1 RTT for repeat connections. The risk is replay attacks: a network attacker can capture and re-send the early data. 0-RTT is safe only for idempotent operations (GET, not POST with side effects). Servers must check for replay via a nonce database or restrict 0-RTT to safe methods.","correct":"0-RTT allows the client to send application data with the first ClientHello, using keying material from a prior session ticket.","distractors":["TLS 1.3 merges the key exchange into the ClientHello (sending the ECDHE key share immediately) and the server responds with its key share, certificate, and Finished in one flight.","The SYN takes 0.5 RTT to reach the server, the SYN-ACK takes 0.5 RTT to return (total 1 RTT) — but the server cannot deliver data until the third ACK arrives (confirming the client received the SYN-ACK).","A DNS query and response each fit in a single datagram (~50–100 bytes), well under the 1500-byte MTU."]},{"id":"cs_fundamentals/networking_fundamentals#e98a36c2d39c","section":"cs_fundamentals","module":"cs_fundamentals/networking_fundamentals","moduleName":"networking fundamentals","qIndex":8,"difficulty":"intermediate","question":"What is CIDR notation and how do you calculate usable hosts in a /24 subnet?","answerFull":"CIDR (Classless Inter-Domain Routing) notation expresses an IP address and its subnet prefix length as IP/bits. A /24 has 24 bits for the network and 8 bits for hosts: 2^8 = 256 addresses. Subtract 2 for network address (.0) and broadcast (.255) = 254 usable hosts. A /16 gives 65,536 total - 2 = 65,534 usable hosts. A /32 is a single host.","correct":"CIDR (Classless Inter-Domain Routing) notation expresses an IP address and its subnet prefix length as IP/bits.","distractors":["An A record maps a hostname directly to an IPv4 address.","NAT (Network Address Translation) rewrites the source IP (and optionally port) of outgoing packets from private addresses to a public IP, maintaining a translation table to route responses back.","0-RTT allows the client to send application data with the first ClientHello, using keying material from a prior session ticket."]},{"id":"cs_fundamentals/networking_fundamentals#bfbbff7345a5","section":"cs_fundamentals","module":"cs_fundamentals/networking_fundamentals","moduleName":"networking fundamentals","qIndex":9,"difficulty":"intermediate","question":"How does NAT work, and what protocol does it break?","answerFull":"NAT (Network Address Translation) rewrites the source IP (and optionally port) of outgoing packets from private addresses to a public IP, maintaining a translation table to route responses back. It breaks any protocol that embeds IP addresses in the payload (FTP active mode, SIP, IPsec AH) because the NAT device does not rewrite the payload. It also complicates peer-to-peer connections (Skype, WebRTC require STUN/TURN/ICE to punch through NAT).","correct":"NAT (Network Address Translation) rewrites the source IP (and optionally port) of outgoing packets from private addresses to a public IP, maintaining a translation table to route responses back.","distractors":["An A record maps a hostname directly to an IPv4 address.","CIDR (Classless Inter-Domain Routing) notation expresses an IP address and its subnet prefix length as IP/bits.","0-RTT allows the client to send application data with the first ClientHello, using keying material from a prior session ticket."]},{"id":"cs_fundamentals/networking_fundamentals#ec504fa5f332","section":"cs_fundamentals","module":"cs_fundamentals/networking_fundamentals","moduleName":"networking fundamentals","qIndex":10,"difficulty":"intermediate","question":"What is the difference between a CNAME and an A record, and when does a CNAME cause problems?","answerFull":"An A record maps a hostname directly to an IPv4 address. A CNAME maps a hostname to another hostname (an alias), which must itself resolve to an A/AAAA record via a subsequent lookup. CNAME causes problems: (1) you cannot use a CNAME at the zone apex (example.com, not www.example.com) — RFC prohibits it because the apex must have NS and SOA records; (2) each CNAME hop adds a DNS lookup round-trip; (3) the ultimate A record TTL governs caching, not the CNAME TTL.","correct":"An A record maps a hostname directly to an IPv4 address.","distractors":["CIDR (Classless Inter-Domain Routing) notation expresses an IP address and its subnet prefix length as IP/bits.","NAT (Network Address Translation) rewrites the source IP (and optionally port) of outgoing packets from private addresses to a public IP, maintaining a translation table to route responses back.","0-RTT allows the client to send application data with the first ClientHello, using keying material from a prior session ticket."]},{"id":"cs_fundamentals/networking_fundamentals#6b3550d724b3","section":"cs_fundamentals","module":"cs_fundamentals/networking_fundamentals","moduleName":"networking fundamentals","qIndex":11,"difficulty":"advanced","question":"What is head-of-line blocking in HTTP/1.1 and how does HTTP/2 address it?","answerFull":"In HTTP/1.1, a connection processes one request-response pair at a time; if the first response is slow (large object, slow origin), all subsequent requests queue behind it. Browsers open 6–8 parallel connections to work around this, but each has handshake cost. HTTP/2 multiplexes requests over a single TCP connection via numbered streams; the server can interleave responses. However, a single lost TCP packet causes all HTTP/2 streams to stall (TCP-level HoL blocking). HTTP/3 over QUIC solves this with per-stream packet loss handling.","correct":"In HTTP/1.1, a connection processes one request-response pair at a time; if the first response is slow (large object, slow origin), all subsequent requests queue behind it.","distractors":["An A record maps a hostname directly to an IPv4 address.","CIDR (Classless Inter-Domain Routing) notation expresses an IP address and its subnet prefix length as IP/bits.","Slow start initializes TCP's congestion window (cwnd) to 1 MSS (later defaults raised to 10 MSS by RFC 6928)."]},{"id":"cs_fundamentals/networking_fundamentals#f2315445c27a","section":"cs_fundamentals","module":"cs_fundamentals/networking_fundamentals","moduleName":"networking fundamentals","qIndex":12,"difficulty":"advanced","question":"What is the listen() backlog, and what happens when it fills up?","answerFull":"The backlog argument to listen(N) sets the maximum size of the kernel's queue of completed (but not yet accept()-ed) connections. When the queue is full, new incoming SYNs are dropped (the kernel does not send SYN-ACK). This causes the client to experience a connection timeout rather than a connection refused, which is confusing to diagnose. Fix: increase the backlog (listen(1024)) and ensure the application calls accept() quickly (non-blocking I/O or a dedicated accept thread).","correct":"The backlog argument to listen(N) sets the maximum size of the kernel's queue of completed (but not yet accept()-ed) connections.","distractors":["The SYN takes 0.5 RTT to reach the server, the SYN-ACK takes 0.5 RTT to return (total 1 RTT) — but the server cannot deliver data until the third ACK arrives (confirming the client received the SYN-ACK).","In HTTP/1.1, a connection processes one request-response pair at a time; if the first response is slow (large object, slow origin), all subsequent requests queue behind it.","MSS is the maximum amount of data in a single TCP segment, negotiated during the handshake."]},{"id":"cs_fundamentals/networking_fundamentals#034d10faf173","section":"cs_fundamentals","module":"cs_fundamentals/networking_fundamentals","moduleName":"networking fundamentals","qIndex":13,"difficulty":"advanced","question":"What is the MSS (Maximum Segment Size) and how does it relate to MTU?","answerFull":"MSS is the maximum amount of data in a single TCP segment, negotiated during the handshake. It is typically MTU - IP header (20 B) - TCP header (20 B) = 1500 - 40 = 1460 bytes on Ethernet. Sending more than the MSS causes IP fragmentation, which is expensive and fragile (firewalls drop fragments). TCP uses MSS to avoid fragmentation by design. PMTUD (Path MTU Discovery) finds the minimum MTU along a path for VPNs or tunnels with smaller MTUs.","correct":"MSS is the maximum amount of data in a single TCP segment, negotiated during the handshake.","distractors":["The backlog argument to listen(N) sets the maximum size of the kernel's queue of completed (but not yet accept()-ed) connections.","A DNS query and response each fit in a single datagram (~50–100 bytes), well under the 1500-byte MTU.","Slow start initializes TCP's congestion window (cwnd) to 1 MSS (later defaults raised to 10 MSS by RFC 6928)."]},{"id":"cs_fundamentals/networking_fundamentals#0905bc7f73ba","section":"cs_fundamentals","module":"cs_fundamentals/networking_fundamentals","moduleName":"networking fundamentals","qIndex":14,"difficulty":"advanced","question":"How does slow start work, and why does it matter for large file transfers?","answerFull":"Slow start initializes TCP's congestion window (cwnd) to 1 MSS (later defaults raised to 10 MSS by RFC 6928). Per RTT, cwnd doubles until it reaches ssthresh, then grows linearly (AIMD). On a 10 Gbps link with 100 ms RTT, it takes ~15 RTTs (1.5 s) for cwnd to reach the bandwidth-delay product (10 Gbps × 0.1 s / 8 = 125 MB). This is why small, long-lived connections (database connection pools) dramatically outperform many short-lived connections for bulk transfers.","correct":"Slow start initializes TCP's congestion window (cwnd) to 1 MSS (later defaults raised to 10 MSS by RFC 6928).","distractors":["MSS is the maximum amount of data in a single TCP segment, negotiated during the handshake.","In HTTP/1.1, a connection processes one request-response pair at a time; if the first response is slow (large object, slow origin), all subsequent requests queue behind it.","Flow control (receiver window, rwnd) prevents a fast sender from overrunning a slow receiver's buffer — it is end-to-end between sender and receiver."]},{"id":"cs_fundamentals/number_systems_and_bit_manipulation#30ff04cc13e2","section":"cs_fundamentals","module":"cs_fundamentals/number_systems_and_bit_manipulation","moduleName":"number systems and bit manipulation","qIndex":1,"difficulty":"core","question":"What is two's complement, and why do modern CPUs use it?","answerFull":"Two's complement represents -k as 2ⁿ - k (equivalently: flip all bits of |k| and add 1). It is used because the same adder circuit works for both signed and unsigned addition — no special-casing. One's complement and sign-magnitude representations require different adder logic for positive and negative operands and have a confusing \"-0\" representation.","correct":"Two's complement represents -k as 2ⁿ - k (equivalently: flip all bits of |k| and add 1).","distractors":["A Unix file mode is a 12-bit number: 3 bits for setuid/setgid/sticky, then 3×3 bits for owner/group/other (read/write/execute).","+Inf / -Inf: result of dividing by zero or overflow.","0.30000000000000004, not 0.3."]},{"id":"cs_fundamentals/number_systems_and_bit_manipulation#85efee257184","section":"cs_fundamentals","module":"cs_fundamentals/number_systems_and_bit_manipulation","moduleName":"number systems and bit manipulation","qIndex":2,"difficulty":"core","question":"How do you check if a number is a power of 2 in O(1)?","answerFull":"n > 0 and (n & (n-1)) == 0. Powers of 2 have exactly one set bit (e.g., 8 = 0b1000). n-1 clears that bit and sets all lower bits (7 = 0b0111). ANDing gives 0 if and only if n is a power of 2. The n > 0 guard handles the n = 0 edge case (0 & -1 = 0 would be a false positive).","correct":"n > 0 and (n & (n-1)) == 0.","distractors":["A Unix file mode is a 12-bit number: 3 bits for setuid/setgid/sticky, then 3×3 bits for owner/group/other (read/write/execute).","+Inf / -Inf: result of dividing by zero or overflow.","0.30000000000000004, not 0.3."]},{"id":"cs_fundamentals/number_systems_and_bit_manipulation#26c2392f22f4","section":"cs_fundamentals","module":"cs_fundamentals/number_systems_and_bit_manipulation","moduleName":"number systems and bit manipulation","qIndex":3,"difficulty":"core","question":"Given an array where every element appears exactly twice except one, find the unique element in O(n) time and O(1) space.","answerFull":"XOR all elements. Each pair cancels (a ^ a = 0), leaving only the unique element. XOR is commutative and associative so order doesn't matter. Code: result = 0; for x in arr: result ^= x; return result.","correct":"XOR all elements. Each pair cancels (a ^ a = 0), leaving only the unique element.","distractors":["With XOR alone you cannot distinguish two missing numbers (the XOR of the pair is ambiguous).","Python integers are arbitrary-precision (backed by a C long array that grows as needed) — they never overflow.","+Inf / -Inf: result of dividing by zero or overflow."]},{"id":"cs_fundamentals/number_systems_and_bit_manipulation#a13d91dd0206","section":"cs_fundamentals","module":"cs_fundamentals/number_systems_and_bit_manipulation","moduleName":"number systems and bit manipulation","qIndex":5,"difficulty":"core","question":"Explain endianness and when it matters.","answerFull":"Endianness is the byte order for multi-byte values in memory. Little-endian (x86/x86-64) stores the least-significant byte at the lowest address. Big-endian stores the most-significant byte first. It matters when: serialising data to disk or network (use explicit byte-order convention, e.g., network byte order = big-endian, htonl/ntohl), writing binary file parsers (JPEG/PNG/WAV headers have specific endianness), or casting an integer pointer to a byte pointer.","correct":"Endianness is the byte order for multi-byte values in memory.","distractors":["+Inf / -Inf: result of dividing by zero or overflow.","0.30000000000000004, not 0.3.","A Unix file mode is a 12-bit number: 3 bits for setuid/setgid/sticky, then 3×3 bits for owner/group/other (read/write/execute)."]},{"id":"cs_fundamentals/number_systems_and_bit_manipulation#7587d3f6e734","section":"cs_fundamentals","module":"cs_fundamentals/number_systems_and_bit_manipulation","moduleName":"number systems and bit manipulation","qIndex":6,"difficulty":"intermediate","question":"What is x & (x-1)? What is x & (-x)?","answerFull":"x & (x-1) clears the lowest set bit of x. Used in Kernighan's bit-count and to check for powers of 2. x & (-x) (equivalently x & (~x + 1)) isolates (returns) the lowest set bit. Used in the Fenwick/Binary Indexed Tree for the \"responsible range\" computation: i += i & (-i) advances to the next update position.","correct":"x & (x-1) clears the lowest set bit of x.","distractors":["Arithmetic right shift (>> in Java/Python for signed ints) fills the vacated high bit with the sign bit — preserving the sign for negative numbers.","Shift result left and OR the last bit of n, then shift n right, repeating 32 times.","Set: n | (1 << k). Clear: n & ~(1 << k)."]},{"id":"cs_fundamentals/number_systems_and_bit_manipulation#c02c09b39845","section":"cs_fundamentals","module":"cs_fundamentals/number_systems_and_bit_manipulation","moduleName":"number systems and bit manipulation","qIndex":7,"difficulty":"intermediate","question":"How does Python handle integer overflow compared to Java/C?","answerFull":"Python integers are arbitrary-precision (backed by a C long array that grows as needed) — they never overflow. Java int is 32-bit two's complement and wraps silently on overflow (e.g., Integer.MAX_VALUE + 1 == Integer.MIN_VALUE). C integer overflow is *undefined behaviour* for signed types. This matters in interview problems: a Python solution a + b never overflows; the same Java solution might.","correct":"Python integers are arbitrary-precision (backed by a C long array that grows as needed) — they never overflow.","distractors":["Arithmetic right shift (>> in Java/Python for signed ints) fills the vacated high bit with the sign bit — preserving the sign for negative numbers.","+Inf / -Inf: result of dividing by zero or overflow.","0.30000000000000004, not 0.3."]},{"id":"cs_fundamentals/number_systems_and_bit_manipulation#ffbdef4ef13d","section":"cs_fundamentals","module":"cs_fundamentals/number_systems_and_bit_manipulation","moduleName":"number systems and bit manipulation","qIndex":8,"difficulty":"intermediate","question":"What is 0.1 + 0.2 in floating-point arithmetic?","answerFull":"0.30000000000000004, not 0.3. Neither 0.1 nor 0.2 has an exact binary float representation — they are rounded to the nearest representable value. Addition compounds the rounding error. Fix: use math.isclose for comparison, the Decimal module for exact decimal arithmetic, or integer arithmetic scaled by a power of 10 (store amounts in cents, not dollars).","correct":"0.30000000000000004, not 0.3.","distractors":["Arithmetic right shift (>> in Java/Python for signed ints) fills the vacated high bit with the sign bit — preserving the sign for negative numbers.","+Inf / -Inf: result of dividing by zero or overflow.","A Unix file mode is a 12-bit number: 3 bits for setuid/setgid/sticky, then 3×3 bits for owner/group/other (read/write/execute)."]},{"id":"cs_fundamentals/number_systems_and_bit_manipulation#7ed282774e72","section":"cs_fundamentals","module":"cs_fundamentals/number_systems_and_bit_manipulation","moduleName":"number systems and bit manipulation","qIndex":9,"difficulty":"intermediate","question":"How do you set, clear, and toggle the kth bit of an integer?","answerFull":"Set: n | (1 << k). Clear: n & ~(1 << k). Toggle: n ^ (1 << k). Check: (n >> k) & 1. These are O(1) operations. In Python, ~(1 << k) produces a negative number (arbitrary-precision NOT), so for a 32-bit context use n & ~(1 << k) & 0xFFFFFFFF or use IntFlag.","correct":"Set: n | (1 << k). Clear: n & ~(1 << k).","distractors":["Shift result left and OR the last bit of n, then shift n right, repeating 32 times.","x & (x-1) clears the lowest set bit of x.","Arithmetic right shift (>> in Java/Python for signed ints) fills the vacated high bit with the sign bit — preserving the sign for negative numbers."]},{"id":"cs_fundamentals/number_systems_and_bit_manipulation#9b75656e3a56","section":"cs_fundamentals","module":"cs_fundamentals/number_systems_and_bit_manipulation","moduleName":"number systems and bit manipulation","qIndex":10,"difficulty":"intermediate","question":"What is a XOR swap and what are its limitations?","answerFull":"a ^= b; b ^= a; a ^= b swaps a and b without a temporary variable. Works because XOR is its own inverse. Limitation: if a and b point to the same memory location, all three operations produce 0 (XORing a value with itself). Always use a temp variable in production code — it is clearer and the compiler optimises it identically. XOR swap is only useful on systems with no temporary registers (rare).","correct":"a ^= b; b ^= a; a ^= b swaps a and b without a temporary variable.","distractors":["With XOR alone you cannot distinguish two missing numbers (the XOR of the pair is ambiguous).","XOR all elements. Each pair cancels (a ^ a = 0), leaving only the unique element.","+Inf / -Inf: result of dividing by zero or overflow."]},{"id":"cs_fundamentals/number_systems_and_bit_manipulation#4d1f9e7242c1","section":"cs_fundamentals","module":"cs_fundamentals/number_systems_and_bit_manipulation","moduleName":"number systems and bit manipulation","qIndex":11,"difficulty":"advanced","question":"How do you reverse the bits of a 32-bit integer?","answerFull":"Shift result left and OR the last bit of n, then shift n right, repeating 32 times. O(32) = O(1). Can also be done with a lookup table (precompute 8-bit reversal for each byte) for higher throughput. Used in bit-reversal permutations in the FFT algorithm.","correct":"Shift result left and OR the last bit of n, then shift n right, repeating 32 times.","distractors":["Two's complement represents -k as 2ⁿ - k (equivalently: flip all bits of |k| and add 1).","A Unix file mode is a 12-bit number: 3 bits for setuid/setgid/sticky, then 3×3 bits for owner/group/other (read/write/execute).","+Inf / -Inf: result of dividing by zero or overflow."]},{"id":"cs_fundamentals/number_systems_and_bit_manipulation#3352fb740b47","section":"cs_fundamentals","module":"cs_fundamentals/number_systems_and_bit_manipulation","moduleName":"number systems and bit manipulation","qIndex":12,"difficulty":"advanced","question":"What is the difference between arithmetic and logical right shift?","answerFull":"Arithmetic right shift (>> in Java/Python for signed ints) fills the vacated high bit with the sign bit — preserving the sign for negative numbers. Logical right shift (>>> in Java) fills with zero regardless of sign. For positive numbers they are identical. In Python, >> is always arithmetic. Use >>> 0 in JavaScript or int32 >>> 0 to get unsigned semantics.","correct":"Arithmetic right shift (>> in Java/Python for signed ints) fills the vacated high bit with the sign bit — preserving the sign for negative numbers.","distractors":["Python integers are arbitrary-precision (backed by a C long array that grows as needed) — they never overflow.","A Unix file mode is a 12-bit number: 3 bits for setuid/setgid/sticky, then 3×3 bits for owner/group/other (read/write/execute).","x & (x-1) clears the lowest set bit of x."]},{"id":"cs_fundamentals/number_systems_and_bit_manipulation#3a488908d32a","section":"cs_fundamentals","module":"cs_fundamentals/number_systems_and_bit_manipulation","moduleName":"number systems and bit manipulation","qIndex":13,"difficulty":"advanced","question":"How are permissions encoded in Linux file modes, and how do you test them?","answerFull":"A Unix file mode is a 12-bit number: 3 bits for setuid/setgid/sticky, then 3×3 bits for owner/group/other (read/write/execute). 0o755 = owner can do anything, group and other can read+execute. Test: mode & stat.S_IRUSR (0o400) is non-zero if owner has read permission. This is why chmod 644 (= 0o644 = 0b110 100 100) is the standard for web files: owner read+write, group and other read-only.","correct":"A Unix file mode is a 12-bit number: 3 bits for setuid/setgid/sticky, then 3×3 bits for owner/group/other (read/write/execute).","distractors":["x & (x-1) clears the lowest set bit of x.","Shift result left and OR the last bit of n, then shift n right, repeating 32 times.","Two's complement represents -k as 2ⁿ - k (equivalently: flip all bits of |k| and add 1)."]},{"id":"cs_fundamentals/number_systems_and_bit_manipulation#38214916b935","section":"cs_fundamentals","module":"cs_fundamentals/number_systems_and_bit_manipulation","moduleName":"number systems and bit manipulation","qIndex":14,"difficulty":"advanced","question":"You need to find two missing numbers from 1..n. How do you use XOR or math?","answerFull":"With XOR alone you cannot distinguish two missing numbers (the XOR of the pair is ambiguous). Use two properties: (a) sum of 1..n = n(n+1)/2; subtract the array sum → sum of the two missing numbers. (b) product or XOR of the pair can partition them. The standard O(n) O(1)-space approach: compute XOR of all elements and all 1..n — call it xor_all. Find any set bit in xor_all. Use that bit to split elements into two groups; XOR each group with the corresponding half of 1..n → recovers each missing number.","correct":"With XOR alone you cannot distinguish two missing numbers (the XOR of the pair is ambiguous).","distractors":["XOR all elements. Each pair cancels (a ^ a = 0), leaving only the unique element.","Arithmetic right shift (>> in Java/Python for signed ints) fills the vacated high bit with the sign bit — preserving the sign for negative numbers.","+Inf / -Inf: result of dividing by zero or overflow."]},{"id":"cs_fundamentals/number_systems_and_bit_manipulation#216240e5e15f","section":"cs_fundamentals","module":"cs_fundamentals/number_systems_and_bit_manipulation","moduleName":"number systems and bit manipulation","qIndex":15,"difficulty":"advanced","question":"What are IEEE-754 special values and when do they appear?","answerFull":"+Inf / -Inf: result of dividing by zero or overflow. NaN (Not a Number): result of 0.0/0.0, sqrt(-1), or Inf - Inf. -0.0: distinct from 0.0 in IEEE-754 but compares equal (0.0 == -0.0 is True). float('inf') > any_finite is always True. math.isnan(float('nan')) required to check NaN — x != x is also True only for NaN but is obscure. These appear in scientific computing and ML (loss = NaN is a common gradient explosion symptom).","correct":"+Inf / -Inf: result of dividing by zero or overflow.","distractors":["Python integers are arbitrary-precision (backed by a C long array that grows as needed) — they never overflow.","Shift result left and OR the last bit of n, then shift n right, repeating 32 times.","0.30000000000000004, not 0.3."]},{"id":"cs_fundamentals/processes_threads_and_context_switching#793b8c9199c5","section":"cs_fundamentals","module":"cs_fundamentals/processes_threads_and_context_switching","moduleName":"processes threads and context switching","qIndex":1,"difficulty":"core","question":"What is the difference between a process and a thread?","answerFull":"A process is an isolated instance of a running program with its own virtual address space, file descriptors, and security context. A thread is an execution context within a process — threads share the process's address space (code, heap, file descriptors) but each has its own stack and register state. Creating a process requires copying (or copy-on-write) the page table, costing ~1–10 ms; creating a thread requires only stack allocation (~10–100 µs). The key tradeoff: processes have fault isolation (one process crash doesn't kill others); threads have lower overhead and shared memory communication.","correct":"A process is an isolated instance of a running program with its own virtual address space, file descriptors, and security context.","distractors":["(1) The running process's CPU registers (all general-purpose registers, SP, PC, flags) are saved to its TCB in kernel memory.","Python's CPython interpreter has a Global Interpreter Lock — a mutex that allows only one thread to execute Python bytecode at a time.","TLS provides per-thread storage that looks like a global variable but has a separate value for each thread."]},{"id":"cs_fundamentals/processes_threads_and_context_switching#a48953aad55c","section":"cs_fundamentals","module":"cs_fundamentals/processes_threads_and_context_switching","moduleName":"processes threads and context switching","qIndex":2,"difficulty":"core","question":"What happens during a context switch? Walk through the steps.","answerFull":"(1) The running process's CPU registers (all general-purpose registers, SP, PC, flags) are saved to its TCB in kernel memory. (2) If switching to a different process, the page table register (CR3 on x86) is updated to point to the new process's page table, which flushes the TLB (~200–300 ns per subsequent TLB miss). (3) The new process's registers are loaded from its TCB. (4) The CPU resumes execution at the new process's saved PC. For same-process thread switches, step 2 is skipped — no TLB flush, making it ~5× faster.","correct":"(1) The running process's CPU registers (all general-purpose registers, SP, PC, flags) are saved to its TCB in kernel memory.","distractors":["After fork(), the parent and child initially share the same physical pages — the kernel marks all pages read-only.","A zombie process has exited but its entry in the process table has not been cleaned up — the parent has not yet called wait() to collect the exit status.","A process is an isolated instance of a running program with its own virtual address space, file descriptors, and security context."]},{"id":"cs_fundamentals/processes_threads_and_context_switching#11546d8aa628","section":"cs_fundamentals","module":"cs_fundamentals/processes_threads_and_context_switching","moduleName":"processes threads and context switching","qIndex":3,"difficulty":"core","question":"What is the cost of a context switch and why does it matter for server design?","answerFull":"Direct cost: ~1–10 µs (saving/restoring registers + TLB flush + cache cold-start after switch). At 10,000 concurrent OS threads, the scheduler cycles through them hundreds of times per second — context-switch overhead can consume 10–20% of CPU time just for scheduling. This is why high-concurrency servers use event loops (single thread, non-blocking I/O), goroutines (M:N scheduling), or virtual threads — they avoid OS-level context switches for I/O-bound work, reducing per-connection cost from 1–10 µs to ~0.1 µs.","correct":"Direct cost: ~1–10 µs (saving/restoring registers + TLB flush + cache cold-start after switch).","distractors":["Concurrency: multiple tasks are in progress at the same time — they may interleave on a single CPU (via context switching).","A process is an isolated instance of a running program with its own virtual address space, file descriptors, and security context.","(1) The running process's CPU registers (all general-purpose registers, SP, PC, flags) are saved to its TCB in kernel memory."]},{"id":"cs_fundamentals/processes_threads_and_context_switching#8bacdede85c1","section":"cs_fundamentals","module":"cs_fundamentals/processes_threads_and_context_switching","moduleName":"processes threads and context switching","qIndex":4,"difficulty":"core","question":"What are user mode and kernel mode, and why does the distinction exist?","answerFull":"The CPU has two privilege levels. User-mode code cannot execute privileged instructions (port I/O, modifying page tables, disabling interrupts). Kernel mode has full CPU privileges. The distinction is a security and stability boundary: malicious or buggy user code cannot corrupt OS data structures or access another process's memory directly. A syscall transitions from user to kernel mode via a software interrupt (x86: syscall instruction) — it saves user registers, switches to the kernel stack, and runs the OS handler. Modern Spectre mitigations (KPTI) add ~200 ns overhead to every syscall.","correct":"The CPU has two privilege levels.","distractors":["A zombie process has exited but its entry in the process table has not been cleaned up — the parent has not yet called wait() to collect the exit status.","(1) The running process's CPU registers (all general-purpose registers, SP, PC, flags) are saved to its TCB in kernel memory.","TLS provides per-thread storage that looks like a global variable but has a separate value for each thread."]},{"id":"cs_fundamentals/processes_threads_and_context_switching#3f4d803c1482","section":"cs_fundamentals","module":"cs_fundamentals/processes_threads_and_context_switching","moduleName":"processes threads and context switching","qIndex":5,"difficulty":"core","question":"What is a zombie process and how do you prevent it?","answerFull":"A zombie process has exited but its entry in the process table has not been cleaned up — the parent has not yet called wait() to collect the exit status. The zombie holds no resources except a PCB entry and a PID. To prevent zombies: always call waitpid() after fork() in the parent (synchronous cleanup), or install a SIGCHLD handler that calls wait() asynchronously, or use subprocess / multiprocessing abstractions that handle reaping automatically. If the parent exits before reaping, the zombie is adopted by init (PID 1), which reaps it.","correct":"A zombie process has exited but its entry in the process table has not been cleaned up — the parent has not yet called wait() to collect the exit status.","distractors":["(1) The running process's CPU registers (all general-purpose registers, SP, PC, flags) are saved to its TCB in kernel memory.","Python's CPython interpreter has a Global Interpreter Lock — a mutex that allows only one thread to execute Python bytecode at a time.","A process is an isolated instance of a running program with its own virtual address space, file descriptors, and security context."]},{"id":"cs_fundamentals/processes_threads_and_context_switching#f2c360908449","section":"cs_fundamentals","module":"cs_fundamentals/processes_threads_and_context_switching","moduleName":"processes threads and context switching","qIndex":6,"difficulty":"intermediate","question":"What is the fork-exec model and why is exec necessary?","answerFull":"fork() creates a copy of the calling process (copy-on-write virtual address space). The child is an exact duplicate. exec() replaces the child's address space with a new program image (new code, new data, fresh stack). Together they implement \"launch a new program\": fork + exec. They are separate calls because: (1) some programs need to do setup between fork and exec (redirect file descriptors, set environment variables); (2) the separation allows fork() alone for process isolation (run code in a sandbox without replacing the image).","correct":"fork() creates a copy of the calling process (copy-on-write virtual address space).","distractors":["(1) The running process's CPU registers (all general-purpose registers, SP, PC, flags) are saved to its TCB in kernel memory.","When all non-daemon threads in a Java or Python program finish, the process exits — even if daemon threads are still running.","A zombie process has exited but its entry in the process table has not been cleaned up — the parent has not yet called wait() to collect the exit status."]},{"id":"cs_fundamentals/processes_threads_and_context_switching#175d988a4d06","section":"cs_fundamentals","module":"cs_fundamentals/processes_threads_and_context_switching","moduleName":"processes threads and context switching","qIndex":7,"difficulty":"intermediate","question":"What is copy-on-write (COW) in fork, and why is it important?","answerFull":"After fork(), the parent and child initially share the same physical pages — the kernel marks all pages read-only. When either process writes to a page, the MMU raises a write-protection fault; the kernel then creates a private copy for the writing process and remaps it. COW makes fork() O(1) in the common case (if the child immediately calls exec(), no pages are copied at all — exec replaces the address space entirely). Without COW, forking a 4 GB process would require copying 4 GB of memory — prohibitively expensive.","correct":"After fork(), the parent and child initially share the same physical pages — the kernel marks all pages read-only.","distractors":["A zombie process has exited but its entry in the process table has not been cleaned up — the parent has not yet called wait() to collect the exit status.","Creating an OS thread: ~10–100 µs (stack allocation, TCB initialisation, kernel data structures).","(1) The running process's CPU registers (all general-purpose registers, SP, PC, flags) are saved to its TCB in kernel memory."]},{"id":"cs_fundamentals/processes_threads_and_context_switching#1173a3582000","section":"cs_fundamentals","module":"cs_fundamentals/processes_threads_and_context_switching","moduleName":"processes threads and context switching","qIndex":8,"difficulty":"intermediate","question":"How does Python's GIL affect threading?","answerFull":"Python's CPython interpreter has a Global Interpreter Lock — a mutex that allows only one thread to execute Python bytecode at a time. I/O operations (file read, network) release the GIL, so I/O-bound threads can run concurrently. CPU-bound operations (computation) hold the GIL, so CPU-bound threads cannot parallelise. For CPU-bound parallelism in Python: use multiprocessing (separate processes, no GIL), use C extensions that release the GIL (NumPy, pandas), or use PyPy which has a different (optional) GIL design. See python/threading_and_multiprocessing for applied depth.","correct":"Python's CPython interpreter has a Global Interpreter Lock — a mutex that allows only one thread to execute Python bytecode at a time.","distractors":["When all non-daemon threads in a Java or Python program finish, the process exits — even if daemon threads are still running.","The thread stays in Blocked state indefinitely — this is a \"stuck thread\" or I/O hang.","A zombie process has exited but its entry in the process table has not been cleaned up — the parent has not yet called wait() to collect the exit status."]},{"id":"cs_fundamentals/processes_threads_and_context_switching#d20ebe5f658e","section":"cs_fundamentals","module":"cs_fundamentals/processes_threads_and_context_switching","moduleName":"processes threads and context switching","qIndex":9,"difficulty":"intermediate","question":"What is a thread-local storage (TLS) and when is it used?","answerFull":"TLS provides per-thread storage that looks like a global variable but has a separate value for each thread. Used for: database connection pools (each thread holds its own connection), user session data in request-handling threads, per-thread random number generator state (avoid lock contention on shared rand state), transaction context in frameworks (Spring's TransactionSynchronizationManager uses TLS to bind the transaction to the current thread). In Python: threading.local(). In Java: ThreadLocal<T>. Caution: TLS must be explicitly cleaned up to avoid memory leaks in thread-pool scenarios.","correct":"TLS provides per-thread storage that looks like a global variable but has a separate value for each thread.","distractors":["Five states: (1) New — created but not started.","The CPU has two privilege levels.","A zombie process has exited but its entry in the process table has not been cleaned up — the parent has not yet called wait() to collect the exit status."]},{"id":"cs_fundamentals/processes_threads_and_context_switching#505583de2a7c","section":"cs_fundamentals","module":"cs_fundamentals/processes_threads_and_context_switching","moduleName":"processes threads and context switching","qIndex":10,"difficulty":"intermediate","question":"What is the difference between a daemon thread and a regular thread?","answerFull":"When all non-daemon threads in a Java or Python program finish, the process exits — even if daemon threads are still running. Daemon threads are \"background tasks\" that do not prevent the JVM/interpreter from shutting down. Use daemon threads for background monitoring, GC-related tasks, or heartbeat timers that should not block process exit. Regular threads represent \"units of work\" that must complete before the application terminates. In Python: thread.daemon = True before start(). In Java: thread.setDaemon(true).","correct":"When all non-daemon threads in a Java or Python program finish, the process exits — even if daemon threads are still running.","distractors":["The thread stays in Blocked state indefinitely — this is a \"stuck thread\" or I/O hang.","A process is an isolated instance of a running program with its own virtual address space, file descriptors, and security context.","Creating an OS thread: ~10–100 µs (stack allocation, TCB initialisation, kernel data structures)."]},{"id":"cs_fundamentals/processes_threads_and_context_switching#1ba73e4b305c","section":"cs_fundamentals","module":"cs_fundamentals/processes_threads_and_context_switching","moduleName":"processes threads and context switching","qIndex":11,"difficulty":"advanced","question":"What is the select/epoll/kqueue model and how does it avoid thread-per-connection overhead?","answerFull":"select, epoll (Linux), and kqueue (macOS/BSD) are OS primitives for multiplexing I/O events. Instead of one thread per connection (blocked on read()), a single thread registers all socket file descriptors with epoll and calls epoll_wait(). The kernel returns only the file descriptors that are ready for I/O. The thread handles each ready socket, then loops back to epoll_wait. With 100,000 connections, one OS thread handles all I/O events at ~0 context-switch overhead. Python's asyncio, Node.js, and Nginx all use this pattern. Trade-off: only works for I/O-bound work; CPU-bound tasks block the event loop and must be offloaded to worker threads/processes.","correct":"select, epoll (Linux), and kqueue (macOS/BSD) are OS primitives for multiplexing I/O events.","distractors":["The thread stays in Blocked state indefinitely — this is a \"stuck thread\" or I/O hang.","Creating an OS thread: ~10–100 µs (stack allocation, TCB initialisation, kernel data structures).","TLS provides per-thread storage that looks like a global variable but has a separate value for each thread."]},{"id":"cs_fundamentals/processes_threads_and_context_switching#20f4c2e7c75c","section":"cs_fundamentals","module":"cs_fundamentals/processes_threads_and_context_switching","moduleName":"processes threads and context switching","qIndex":12,"difficulty":"advanced","question":"What are the thread states and what transitions cause them?","answerFull":"Five states: (1) New — created but not started. (2) Ready/Runnable — eligible to run, waiting for CPU. (3) Running — executing on a CPU core. (4) Blocked/Waiting — waiting for a lock, I/O, sleep, or another thread. (5) Terminated — execution complete. Transitions: New→Ready (start()); Running→Ready (preemption by scheduler or Thread.yield()); Running→Blocked (I/O call, lock acquisition, wait(), sleep()); Blocked→Ready (I/O completion, lock release, notify()); Running→Terminated (return from thread function or exit()).","correct":"Five states: (1) New — created but not started.","distractors":["The thread stays in Blocked state indefinitely — this is a \"stuck thread\" or I/O hang.","Creating an OS thread: ~10–100 µs (stack allocation, TCB initialisation, kernel data structures).","A zombie process has exited but its entry in the process table has not been cleaned up — the parent has not yet called wait() to collect the exit status."]},{"id":"cs_fundamentals/processes_threads_and_context_switching#a2606b53a694","section":"cs_fundamentals","module":"cs_fundamentals/processes_threads_and_context_switching","moduleName":"processes threads and context switching","qIndex":13,"difficulty":"advanced","question":"How does the OS handle the case where a thread is waiting for I/O that never completes?","answerFull":"The thread stays in Blocked state indefinitely — this is a \"stuck thread\" or I/O hang. Prevention: always set timeouts on I/O operations (socket.settimeout(), connect() with SO_TIMEOUT in Java). Detection: watchdog threads, health check endpoints, or async timeouts (asyncio.wait_for()). Recovery: send a signal to the thread (POSIX pthread_kill), or interrupt the blocking syscall (Java's Thread.interrupt() raises InterruptedException in most blocking calls). Production systems always pair I/O with a timeout and a circuit breaker.","correct":"The thread stays in Blocked state indefinitely — this is a \"stuck thread\" or I/O hang.","distractors":["Python's CPython interpreter has a Global Interpreter Lock — a mutex that allows only one thread to execute Python bytecode at a time.","Creating an OS thread: ~10–100 µs (stack allocation, TCB initialisation, kernel data structures).","TLS provides per-thread storage that looks like a global variable but has a separate value for each thread."]},{"id":"cs_fundamentals/processes_threads_and_context_switching#060f1ca94d61","section":"cs_fundamentals","module":"cs_fundamentals/processes_threads_and_context_switching","moduleName":"processes threads and context switching","qIndex":14,"difficulty":"advanced","question":"What is the difference between concurrency and parallelism?","answerFull":"Concurrency: multiple tasks are in progress at the same time — they may interleave on a single CPU (via context switching). Parallelism: multiple tasks execute simultaneously on multiple CPUs/cores. All parallel execution is concurrent; not all concurrent execution is parallel. A single-core machine can have concurrency (via time-slicing) but not true parallelism. Python threads give concurrency (GIL prevents parallelism for CPU-bound code) but not CPU parallelism. Go goroutines on GOMAXPROCS=4 give both concurrency and parallelism.","correct":"Concurrency: multiple tasks are in progress at the same time — they may interleave on a single CPU (via context switching).","distractors":["The CPU has two privilege levels.","After fork(), the parent and child initially share the same physical pages — the kernel marks all pages read-only.","(1) The running process's CPU registers (all general-purpose registers, SP, PC, flags) are saved to its TCB in kernel memory."]},{"id":"cs_fundamentals/processes_threads_and_context_switching#3aea3dcb138f","section":"cs_fundamentals","module":"cs_fundamentals/processes_threads_and_context_switching","moduleName":"processes threads and context switching","qIndex":15,"difficulty":"advanced","question":"What is the cost of creating and destroying threads vs using a thread pool?","answerFull":"Creating an OS thread: ~10–100 µs (stack allocation, TCB initialisation, kernel data structures). Thread pool (pre-created threads): ~0–1 µs per task submission (just enqueue a work item). For short-lived tasks (e.g., handling a 1 ms HTTP request), creating a new thread per request adds ~10% overhead and creates GC pressure from TCB objects. Thread pools recycle threads — the creation cost is paid once. Java's Executors.newFixedThreadPool(n), Python's ThreadPoolExecutor, and Go's goroutine pool (worker pattern) all apply this principle.","correct":"Creating an OS thread: ~10–100 µs (stack allocation, TCB initialisation, kernel data structures).","distractors":["After fork(), the parent and child initially share the same physical pages — the kernel marks all pages read-only.","(1) The running process's CPU registers (all general-purpose registers, SP, PC, flags) are saved to its TCB in kernel memory.","When all non-daemon threads in a Java or Python program finish, the process exits — even if daemon threads are still running."]},{"id":"cs_fundamentals/recursion_and_problem_solving_patterns#8f835a6657bf","section":"cs_fundamentals","module":"cs_fundamentals/recursion_and_problem_solving_patterns","moduleName":"recursion and problem solving patterns","qIndex":1,"difficulty":"core","question":"What is a base case and why is it mandatory?","answerFull":"A base case is the condition under which the recursive function returns a result directly without making another recursive call. It is mandatory because without a base case, the recursion never terminates — each call generates another call forever until the call stack exhausts memory (stack overflow / RecursionError). Every recursive path must reach a base case. A common interview mistake is writing a base case that is unreachable (e.g., if n < 0: return when n decrements toward 0 from a positive value — needs if n == 0).","correct":"A base case is the condition under which the recursive function returns a result directly without making another recursive call.","distractors":["Two-pointer requires the array to be sorted (or have a monotonic property) so that moving a pointer in one direction is guaranteed to improve or worsen the condition being tested.","(1) Base case — problem is small enough to solve directly.","Tail recursion is when the recursive call is the very last operation before returning — there is no work left to do with the result."]},{"id":"cs_fundamentals/recursion_and_problem_solving_patterns#d1d16ae7ed8d","section":"cs_fundamentals","module":"cs_fundamentals/recursion_and_problem_solving_patterns","moduleName":"recursion and problem solving patterns","qIndex":2,"difficulty":"core","question":"What is the time and space complexity of naive recursive Fibonacci?","answerFull":"Time: O(2^n) — the call tree is a nearly-complete binary tree of height n; the number of nodes is ~2^n. Space: O(n) — the maximum depth of the call stack is n (the deepest path is fib(n) → fib(n-1) → ... → fib(1)). Fix with memoisation: O(n) time and O(n) space.","correct":"Time: O(2^n) — the call tree is a nearly-complete binary tree of height n; the number of nodes is ~2^n.","distractors":["Binary search variant: maintain [lo, hi] = [0, len(arr)] (inclusive of one past the end).","A base case is the condition under which the recursive function returns a result directly without making another recursive call.","Pruning is the early termination of a recursive branch when you can determine that no valid solution exists in the subtree."]},{"id":"cs_fundamentals/recursion_and_problem_solving_patterns#6526e0b84bab","section":"cs_fundamentals","module":"cs_fundamentals/recursion_and_problem_solving_patterns","moduleName":"recursion and problem solving patterns","qIndex":3,"difficulty":"core","question":"When is two-pointer applicable versus a brute-force nested loop?","answerFull":"Two-pointer requires the array to be sorted (or have a monotonic property) so that moving a pointer in one direction is guaranteed to improve or worsen the condition being tested. In a sorted array, if the current pair sums to less than the target, moving the left pointer right increases the sum; moving the right pointer left decreases it. Without this monotonic property, you cannot prune and must use a nested loop (O(n²)). Two-pointer reduces to O(n).","correct":"Two-pointer requires the array to be sorted (or have a monotonic property) so that moving a pointer in one direction is guaranteed to improve or worsen the condition being tested.","distractors":["Sliding window requires the problem to have a \"monotonic\" property: once you expand or shrink the window, you can determine whether the window is valid or invalid without re-examining internal structure from scratch.","A base case is the condition under which the recursive function returns a result directly without making another recursive call.","Maintain a window [left, right] into an array/string."]},{"id":"cs_fundamentals/recursion_and_problem_solving_patterns#26bb4607a627","section":"cs_fundamentals","module":"cs_fundamentals/recursion_and_problem_solving_patterns","moduleName":"recursion and problem solving patterns","qIndex":4,"difficulty":"core","question":"What is the sliding window technique and when does it apply?","answerFull":"Maintain a window [left, right] into an array/string. Expand right unconditionally (add element to window). Shrink left when the window violates a constraint (remove element from window). Track the best valid window seen. Applies when: (a) you need a contiguous subarray/substring, and (b) the constraint is monotonic — adding elements can only make the window \"worse\" (exceed a budget), and removing elements can only make it \"better\". Problems: longest substring without repeating chars, minimum window substring, maximum sum subarray of size k.","correct":"Maintain a window [left, right] into an array/string.","distractors":["Sliding window requires the problem to have a \"monotonic\" property: once you expand or shrink the window, you can determine whether the window is valid or invalid without re-examining internal structure from scratch.","Binary search variant: maintain [lo, hi] = [0, len(arr)] (inclusive of one past the end).","Two-pointer requires the array to be sorted (or have a monotonic property) so that moving a pointer in one direction is guaranteed to improve or worsen the condition being tested."]},{"id":"cs_fundamentals/recursion_and_problem_solving_patterns#3f58365ee3f6","section":"cs_fundamentals","module":"cs_fundamentals/recursion_and_problem_solving_patterns","moduleName":"recursion and problem solving patterns","qIndex":5,"difficulty":"core","question":"What is the difference between backtracking and DFS?","answerFull":"DFS is a traversal strategy for a known graph: visit all reachable nodes. Backtracking is DFS on an *implicit* solution-space tree that you build on the fly: at each node, you try all valid extensions of the current partial solution, recurse, then undo the extension. The key difference is the \"undo\" step — DFS on a given graph does not need to undo (the graph edges are fixed); backtracking must undo because the choices it makes (adding an element to a path) change the state that subsequent choices see.","correct":"DFS is a traversal strategy for a known graph: visit all reachable nodes.","distractors":["Time: O(2^n) — the call tree is a nearly-complete binary tree of height n; the number of nodes is ~2^n.","(1) Base case — problem is small enough to solve directly.","A base case is the condition under which the recursive function returns a result directly without making another recursive call."]},{"id":"cs_fundamentals/recursion_and_problem_solving_patterns#d7e0f2c2723e","section":"cs_fundamentals","module":"cs_fundamentals/recursion_and_problem_solving_patterns","moduleName":"recursion and problem solving patterns","qIndex":6,"difficulty":"intermediate","question":"How does Floyd's cycle detection algorithm (tortoise and hare) work?","answerFull":"Two pointers, fast (2 steps/iter) and slow (1 step/iter). If no cycle, fast exits the list. If there is a cycle, both pointers enter the cycle; fast is faster, so it \"laps\" slow inside the cycle. They must meet because fast gains 1 step per iteration on slow inside a cycle of length L — they meet after at most L iterations. To find the cycle start: after they meet, reset slow to head, keep fast at the meeting point, advance both one step at a time — they meet at the cycle start.","correct":"Two pointers, fast (2 steps/iter) and slow (1 step/iter).","distractors":["Start both at head. Advance fast 2 steps and slow 1 step per iteration.","Tail recursion is when the recursive call is the very last operation before returning — there is no work left to do with the result.","(1) Base case — problem is small enough to solve directly."]},{"id":"cs_fundamentals/recursion_and_problem_solving_patterns#d62752a86a68","section":"cs_fundamentals","module":"cs_fundamentals/recursion_and_problem_solving_patterns","moduleName":"recursion and problem solving patterns","qIndex":7,"difficulty":"intermediate","question":"Why is it important to copy the path in backtracking before appending to the result?","answerFull":"The path is a mutable list. If you append path (a reference) to result, all result entries point to the same list — when you later modify path (pop/push), all previously appended results change. Always append path[:] (a copy) or list(path) to capture the state at the time of appending.","correct":"The path is a mutable list.","distractors":["A base case is the condition under which the recursive function returns a result directly without making another recursive call.","Tail recursion is when the recursive call is the very last operation before returning — there is no work left to do with the result.","(1) Base case — problem is small enough to solve directly."]},{"id":"cs_fundamentals/recursion_and_problem_solving_patterns#dee3bc5225fc","section":"cs_fundamentals","module":"cs_fundamentals/recursion_and_problem_solving_patterns","moduleName":"recursion and problem solving patterns","qIndex":8,"difficulty":"intermediate","question":"When would you convert a recursive solution to iterative?","answerFull":"When the recursion depth can exceed the stack limit (> 1000 in Python, > ~10000 in Java). Deep DFS on a large graph or a long linked list can hit this limit. Convert by maintaining an explicit stack (a Python list or collections.deque) and simulating the call stack manually. Also convert for performance — iterative avoids function-call overhead (~100 ns per call in Python).","correct":"When the recursion depth can exceed the stack limit (> 1000 in Python, > ~10000 in Java).","distractors":["Tail recursion is when the recursive call is the very last operation before returning — there is no work left to do with the result.","Pruning is the early termination of a recursive branch when you can determine that no valid solution exists in the subtree.","A base case is the condition under which the recursive function returns a result directly without making another recursive call."]},{"id":"cs_fundamentals/recursion_and_problem_solving_patterns#369501c9b91c","section":"cs_fundamentals","module":"cs_fundamentals/recursion_and_problem_solving_patterns","moduleName":"recursion and problem solving patterns","qIndex":9,"difficulty":"intermediate","question":"What is tail recursion and why doesn't Python optimise it?","answerFull":"Tail recursion is when the recursive call is the very last operation before returning — there is no work left to do with the result. Many languages (Scheme, Scala, Swift) transform tail calls to jumps (tail-call optimisation / TCO), reusing the caller's stack frame and giving O(1) stack space. Python's CPython interpreter deliberately does NOT implement TCO — Guido van Rossum decided that stack traces should always show the full call chain for debuggability. Java's JVM also does not guarantee TCO. Convert to an iterative loop if stack space matters.","correct":"Tail recursion is when the recursive call is the very last operation before returning — there is no work left to do with the result.","distractors":["A base case is the condition under which the recursive function returns a result directly without making another recursive call.","When the recursion depth can exceed the stack limit (> 1000 in Python, > ~10000 in Java).","Time: O(2^n) — the call tree is a nearly-complete binary tree of height n; the number of nodes is ~2^n."]},{"id":"cs_fundamentals/recursion_and_problem_solving_patterns#81e39bed1450","section":"cs_fundamentals","module":"cs_fundamentals/recursion_and_problem_solving_patterns","moduleName":"recursion and problem solving patterns","qIndex":10,"difficulty":"intermediate","question":"You have a sorted array. How do you find the leftmost position where a target could be inserted (lower bound)?","answerFull":"Binary search variant: maintain [lo, hi] = [0, len(arr)] (inclusive of one past the end). While lo < hi: mid = (lo + hi) // 2; if arr[mid] < target, lo = mid + 1; else hi = mid. Return lo. This is Python's bisect_left. The invariant: at termination, lo = hi = the first position where arr[pos] >= target.","correct":"Binary search variant: maintain [lo, hi] = [0, len(arr)] (inclusive of one past the end).","distractors":["Two-pointer requires the array to be sorted (or have a monotonic property) so that moving a pointer in one direction is guaranteed to improve or worsen the condition being tested.","Time: O(2^n) — the call tree is a nearly-complete binary tree of height n; the number of nodes is ~2^n.","Sliding window requires the problem to have a \"monotonic\" property: once you expand or shrink the window, you can determine whether the window is valid or invalid without re-examining internal structure from scratch."]},{"id":"cs_fundamentals/recursion_and_problem_solving_patterns#7177d818c8a6","section":"cs_fundamentals","module":"cs_fundamentals/recursion_and_problem_solving_patterns","moduleName":"recursion and problem solving patterns","qIndex":11,"difficulty":"advanced","question":"What is the key pattern for divide-and-conquer?","answerFull":"(1) Base case — problem is small enough to solve directly. (2) Divide — split the problem into two or more *independent* subproblems of roughly equal size. (3) Conquer — recursively solve each subproblem. (4) Combine — merge the subproblem solutions. The combine step determines the complexity: if combining is O(n) and we split into 2 halves → T(n) = 2T(n/2) + O(n) → O(n log n) by Master Theorem Case 2.","correct":"(1) Base case — problem is small enough to solve directly.","distractors":["A base case is the condition under which the recursive function returns a result directly without making another recursive call.","Sliding window requires the problem to have a \"monotonic\" property: once you expand or shrink the window, you can determine whether the window is valid or invalid without re-examining internal structure from scratch.","Binary search variant: maintain [lo, hi] = [0, len(arr)] (inclusive of one past the end)."]},{"id":"cs_fundamentals/recursion_and_problem_solving_patterns#36fab04af975","section":"cs_fundamentals","module":"cs_fundamentals/recursion_and_problem_solving_patterns","moduleName":"recursion and problem solving patterns","qIndex":12,"difficulty":"advanced","question":"How do you use fast-slow pointer to find the middle of a linked list?","answerFull":"Start both at head. Advance fast 2 steps and slow 1 step per iteration. When fast reaches the end (fast is None or fast.next is None), slow is at the middle. For an even-length list, slow will be at the first of the two middle nodes. This is used in merge sort for linked lists (split at the middle) and in palindrome checking (reverse the second half starting from the middle).","correct":"Start both at head. Advance fast 2 steps and slow 1 step per iteration.","distractors":["Two pointers, fast (2 steps/iter) and slow (1 step/iter).","Two-pointer requires the array to be sorted (or have a monotonic property) so that moving a pointer in one direction is guaranteed to improve or worsen the condition being tested.","The path is a mutable list."]},{"id":"cs_fundamentals/recursion_and_problem_solving_patterns#9b81209209da","section":"cs_fundamentals","module":"cs_fundamentals/recursion_and_problem_solving_patterns","moduleName":"recursion and problem solving patterns","qIndex":13,"difficulty":"advanced","question":"What is the pruning in backtracking and why is it critical for performance?","answerFull":"Pruning is the early termination of a recursive branch when you can determine that no valid solution exists in the subtree. Example: in N-Queens, if placing a queen in a column puts it in conflict with an already-placed queen, skip all configurations in that subtree. Without pruning, backtracking explores the entire O(n!) solution space. With aggressive pruning, practical performance can be orders of magnitude faster — N-Queens with 15 queens goes from 15! ≈ 1.3 trillion to a tractable solution in milliseconds.","correct":"Pruning is the early termination of a recursive branch when you can determine that no valid solution exists in the subtree.","distractors":["Tail recursion is when the recursive call is the very last operation before returning — there is no work left to do with the result.","A base case is the condition under which the recursive function returns a result directly without making another recursive call.","Sliding window requires the problem to have a \"monotonic\" property: once you expand or shrink the window, you can determine whether the window is valid or invalid without re-examining internal structure from scratch."]},{"id":"cs_fundamentals/recursion_and_problem_solving_patterns#44bae0728382","section":"cs_fundamentals","module":"cs_fundamentals/recursion_and_problem_solving_patterns","moduleName":"recursion and problem solving patterns","qIndex":14,"difficulty":"advanced","question":"Explain the time complexity of the backtracking algorithm for generating all subsets of n elements.","answerFull":"T = O(2^n × n). There are 2^n subsets. For each subset, we copy the current path of length O(n) into the result. The number of nodes in the backtracking tree is also 2^n. So the total work is O(2^n × n) for copying and O(2^n) for the tree traversal — dominated by O(2^n × n).","correct":"T = O(2^n × n). There are 2^n subsets.","distractors":["Tail recursion is when the recursive call is the very last operation before returning — there is no work left to do with the result.","Time: O(2^n) — the call tree is a nearly-complete binary tree of height n; the number of nodes is ~2^n.","(1) Base case — problem is small enough to solve directly."]},{"id":"cs_fundamentals/recursion_and_problem_solving_patterns#56ad47288b63","section":"cs_fundamentals","module":"cs_fundamentals/recursion_and_problem_solving_patterns","moduleName":"recursion and problem solving patterns","qIndex":15,"difficulty":"advanced","question":"When is a sliding window approach not applicable?","answerFull":"Sliding window requires the problem to have a \"monotonic\" property: once you expand or shrink the window, you can determine whether the window is valid or invalid without re-examining internal structure from scratch. It does not apply when: the constraint involves non-contiguous elements, the array is unsorted and you need global properties (use a hash map instead), or the \"valid window\" condition is non-monotonic (adding elements could make it valid or invalid in an unpredictable way).","correct":"Sliding window requires the problem to have a \"monotonic\" property: once you expand or shrink the window, you can determine whether the window is valid or invalid without re-examining internal structure from scratch.","distractors":["(1) Base case — problem is small enough to solve directly.","Two-pointer requires the array to be sorted (or have a monotonic property) so that moving a pointer in one direction is guaranteed to improve or worsen the condition being tested.","Pruning is the early termination of a recursive branch when you can determine that no valid solution exists in the subtree."]},{"id":"cs_fundamentals/sorting_and_searching#6ec0b17e87df","section":"cs_fundamentals","module":"cs_fundamentals/sorting_and_searching","moduleName":"sorting and searching","qIndex":1,"difficulty":"core","question":"What is the comparison-sort lower bound and how is it proved?","answerFull":"Ω(n log n). Any comparison-based sort's execution can be modeled as a decision tree where each internal node is a comparison and each leaf is a permutation outcome. To distinguish all n! permutations, the tree needs n! leaves, so height ≥ log₂(n!) ≈ n log₂n − n = Ω(n log n). This is a worst-case bound — no algorithm can do better for arbitrary data.","correct":"Ω(n log n). Any comparison-based sort's execution can be modeled as a decision tree where each internal node is a comparison and each leaf is a permutation outcome.","distractors":["lower_bound(target) returns the first index where arr[i] >= target — the leftmost position target could be inserted to keep order.","A sort is stable if equal elements preserve their input order.","bisect.bisect_left(arr, x) returns the leftmost index where x could be inserted to keep arr sorted — equivalent to lower_bound."]},{"id":"cs_fundamentals/sorting_and_searching#dbb71fb7e644","section":"cs_fundamentals","module":"cs_fundamentals/sorting_and_searching","moduleName":"sorting and searching","qIndex":2,"difficulty":"core","question":"Why does quicksort perform better than merge sort in practice despite the same asymptotic complexity?","answerFull":"Two reasons: cache behaviour and constant factors. Quicksort partitions in-place on contiguous memory — the inner loop accesses sequential addresses, which maximises L1/L2 cache hits. Merge sort allocates an O(n) auxiliary array and copies data across two buffers, causing more cache misses. Additionally, quicksort has no memory allocation overhead, and modern CPUs can pipeline its comparisons efficiently. Measured constant factor for quicksort is ~2–3× smaller than merge sort's.","correct":"Two reasons: cache behaviour and constant factors.","distractors":["In external sort, sorted runs are written to disk and then k-way merged using a min-heap of size k.","Counting sort is O(n + k) where k is the key range.","Dual-pivot quicksort uses two pivots (p1 ≤ p2) to partition into three sections: < p1, between p1 and p2, > p2."]},{"id":"cs_fundamentals/sorting_and_searching#ff7dc430f9fe","section":"cs_fundamentals","module":"cs_fundamentals/sorting_and_searching","moduleName":"sorting and searching","qIndex":3,"difficulty":"core","question":"What are the four properties needed for a stable sort, and which standard algorithms are stable?","answerFull":"A sort is stable if equal elements preserve their input order. Stable: merge sort, insertion sort, counting sort (with right-to-left scan), LSD radix sort, TimSort. Not stable: quicksort, heapsort. Stability is required for multi-key sort (e.g., sort records by name then by department — sort by department first with a stable sort, then sort by name; the department order is preserved within equal names).","correct":"A sort is stable if equal elements preserve their input order.","distractors":["Counting sort is O(n + k) where k is the key range.","lower_bound(target) returns the first index where arr[i] >= target — the leftmost position target could be inserted to keep order.","Radix sort is counting sort applied digit-by-digit."]},{"id":"cs_fundamentals/sorting_and_searching#b9599bd0f2b5","section":"cs_fundamentals","module":"cs_fundamentals/sorting_and_searching","moduleName":"sorting and searching","qIndex":4,"difficulty":"core","question":"When does counting sort beat merge sort and when does it not?","answerFull":"Counting sort is O(n + k) where k is the key range. It beats merge sort's O(n log n) when k = O(n log n), i.e., roughly k ≤ 10n–100n. For sorting 1M integers in [0, 10M], counting sort uses 40 MB (10M × 4B) and completes in one pass — faster than O(n log n). For sorting 1K 64-bit integers, k = 2^64 — completely infeasible; use comparison sort.","correct":"Counting sort is O(n + k) where k is the key range.","distractors":["Ω(n log n). Any comparison-based sort's execution can be modeled as a decision tree where each internal node is a comparison and each leaf is a permutation outcome.","In external sort, sorted runs are written to disk and then k-way merged using a min-heap of size k.","TimSort is a hybrid merge + insertion sort."]},{"id":"cs_fundamentals/sorting_and_searching#391e0e7e505a","section":"cs_fundamentals","module":"cs_fundamentals/sorting_and_searching","moduleName":"sorting and searching","qIndex":5,"difficulty":"core","question":"What is the difference between lower_bound and upper_bound, and when do you need each?","answerFull":"lower_bound(target) returns the first index where arr[i] >= target — the leftmost position target could be inserted to keep order. upper_bound(target) returns the first index where arr[i] > target — one past the last occurrence. Number of occurrences = upper_bound - lower_bound. Use lower_bound to find the first occurrence or an insertion point; use upper_bound to find the end of a range or count duplicates.","correct":"lower_bound(target) returns the first index where arr[i] >= target — the leftmost position target could be inserted to keep order.","distractors":["A sort is stable if equal elements preserve their input order.","bisect.bisect_left(arr, x) returns the leftmost index where x could be inserted to keep arr sorted — equivalent to lower_bound.","Quickselect: partition around a random pivot; if pivot lands at position k, return it; otherwise recurse only on the side containing rank k."]},{"id":"cs_fundamentals/sorting_and_searching#fcac4a6a2178","section":"cs_fundamentals","module":"cs_fundamentals/sorting_and_searching","moduleName":"sorting and searching","qIndex":6,"difficulty":"intermediate","question":"How does randomised quicksort avoid the O(n²) worst case on sorted input?","answerFull":"By randomly shuffling the pivot choice. In each partition call, swap a random element into the pivot position before partitioning. The expected depth of the recursion tree becomes O(log n) because the probability of picking the globally worst pivot at every level is (1/n)^(n/2) — exponentially small. The expected number of comparisons is 2n ln n ≈ 1.39 n log₂ n, matching the best-case performance of deterministic quicksort.","correct":"By randomly shuffling the pivot choice.","distractors":["Quickselect: partition around a random pivot; if pivot lands at position k, return it; otherwise recurse only on the side containing rank k.","In external sort, sorted runs are written to disk and then k-way merged using a min-heap of size k.","2n ln n ≈ 1.39 n log₂ n. Each pair (i, j) where i < j is compared at most once (when one of them is chosen as a pivot before the other)."]},{"id":"cs_fundamentals/sorting_and_searching#8d7327e0b949","section":"cs_fundamentals","module":"cs_fundamentals/sorting_and_searching","moduleName":"sorting and searching","qIndex":7,"difficulty":"intermediate","question":"What is 3-way quicksort and when should you use it?","answerFull":"3-way quicksort (Dutch National Flag partition) partitions the array into three sections: < pivot, == pivot, > pivot. Recursion only proceeds on the < and > sections, skipping all equal elements. If the input has many duplicates (e.g., sorting 10M integers where values are in [0, 1000]), standard 2-way quicksort still recurses into equal-element sub-arrays — O(n log n). 3-way quicksort degrades to O(n) for fully identical arrays and to O(n k log k) where k is the number of distinct values — a significant practical win for low-cardinality data.","correct":"3-way quicksort (Dutch National Flag partition) partitions the array into three sections: < pivot, == pivot, > pivot.","distractors":["2n ln n ≈ 1.39 n log₂ n. Each pair (i, j) where i < j is compared at most once (when one of them is chosen as a pivot before the other).","Instead of searching for a value in an array, binary search over the space of possible answers.","In external sort, sorted runs are written to disk and then k-way merged using a min-heap of size k."]},{"id":"cs_fundamentals/sorting_and_searching#2e960e50dad3","section":"cs_fundamentals","module":"cs_fundamentals/sorting_and_searching","moduleName":"sorting and searching","qIndex":8,"difficulty":"intermediate","question":"Explain the answer-space binary search pattern with a concrete example.","answerFull":"Instead of searching for a value in an array, binary search over the space of possible answers. The condition: \"is X a feasible answer?\" must be monotone (if X works, X+1 works). Example: \"Find the minimum number of days to ship all packages within a weight capacity.\" Feasibility check: simulate loading packages day by day with capacity mid — if it finishes in ≤ D days, mid is feasible. Binary search over capacity [max(weight), sum(weights)] in O(log(sum) × n) = O(n log(sum)) — far faster than linear scan over O(sum) values.","correct":"Instead of searching for a value in an array, binary search over the space of possible answers.","distractors":["3-way quicksort (Dutch National Flag partition) partitions the array into three sections: < pivot, == pivot, > pivot.","2n ln n ≈ 1.39 n log₂ n. Each pair (i, j) where i < j is compared at most once (when one of them is chosen as a pivot before the other).","A B+Tree index on 10 million rows with branching factor 100 has height ≤ 4."]},{"id":"cs_fundamentals/sorting_and_searching#e17a1ae167ec","section":"cs_fundamentals","module":"cs_fundamentals/sorting_and_searching","moduleName":"sorting and searching","qIndex":9,"difficulty":"intermediate","question":"What is TimSort and why is it the practical choice for general-purpose sorting?","answerFull":"TimSort is a hybrid merge + insertion sort. It scans the input for \"natural runs\" (ascending or descending sequences) and merges them with a strategy that is O(n) for already-sorted input and O(n log n) for random input. The galloping mode accelerates merges when one run dominates the other. Invented by Tim Peters for CPython in 2002. Also used in Java (for object arrays), V8 (JavaScript), Swift, and Rust. Its advantage is adaptive behaviour: real-world data is rarely random — logs, timestamps, and records often have significant existing order.","correct":"TimSort is a hybrid merge + insertion sort.","distractors":["A sort is stable if equal elements preserve their input order.","In external sort, sorted runs are written to disk and then k-way merged using a min-heap of size k.","Radix sort is counting sort applied digit-by-digit."]},{"id":"cs_fundamentals/sorting_and_searching#67d176bed1a1","section":"cs_fundamentals","module":"cs_fundamentals/sorting_and_searching","moduleName":"sorting and searching","qIndex":10,"difficulty":"intermediate","question":"How does binary search apply to database index lookups?","answerFull":"A B+Tree index on 10 million rows with branching factor 100 has height ≤ 4. Each lookup is 4 node reads (4 disk I/Os or 4 cache lookups). Equivalent to binary search on a sorted 10M-element array (log₂(10M) ≈ 23 comparisons) — but B+Tree is better because each node is a full 4–8 KB disk page; the branching factor trades comparisons for I/O. The leaf page contains the sorted values, so range queries walk the leaf level sequentially rather than doing repeated binary searches.","correct":"A B+Tree index on 10 million rows with branching factor 100 has height ≤ 4.","distractors":["Instead of searching for a value in an array, binary search over the space of possible answers.","Ω(n log n). Any comparison-based sort's execution can be modeled as a decision tree where each internal node is a comparison and each leaf is a permutation outcome.","lower_bound(target) returns the first index where arr[i] >= target — the leftmost position target could be inserted to keep order."]},{"id":"cs_fundamentals/sorting_and_searching#c0874dd4ff25","section":"cs_fundamentals","module":"cs_fundamentals/sorting_and_searching","moduleName":"sorting and searching","qIndex":11,"difficulty":"advanced","question":"Can you sort a linked list in O(n log n) with O(1) extra space?","answerFull":"Yes — bottom-up merge sort. Instead of top-down recursive splitting (which requires O(log n) call stack), iterate with run widths 1, 2, 4, 8, ... Scan the list in pairs of runs of current width and merge in-place by pointer relinking. Each level takes O(n), log n levels → O(n log n) total, O(1) extra space (just a few pointers). This is not possible with quicksort, which requires random access for pivot selection.","correct":"Yes — bottom-up merge sort.","distractors":["Instead of searching for a value in an array, binary search over the space of possible answers.","In external sort, sorted runs are written to disk and then k-way merged using a min-heap of size k.","A sort is stable if equal elements preserve their input order."]},{"id":"cs_fundamentals/sorting_and_searching#2b7a78a3015c","section":"cs_fundamentals","module":"cs_fundamentals/sorting_and_searching","moduleName":"sorting and searching","qIndex":12,"difficulty":"advanced","question":"What is the expected number of comparisons in quicksort and how is it derived?","answerFull":"2n ln n ≈ 1.39 n log₂ n. Each pair (i, j) where i < j is compared at most once (when one of them is chosen as a pivot before the other). The probability that element i is compared with element j equals 2/(j − i + 1) (either i or j is the first pivot chosen among them). Summing over all pairs: Σ_{i<j} 2/(j−i+1) = 2 Σ_{k=1}^{n} (n−k+1)/k ≈ 2n Σ 1/k ≈ 2n ln n.","correct":"2n ln n ≈ 1.39 n log₂ n. Each pair (i, j) where i < j is compared at most once (when one of them is chosen as a pivot before the other).","distractors":["Ω(n log n). Any comparison-based sort's execution can be modeled as a decision tree where each internal node is a comparison and each leaf is a permutation outcome.","3-way quicksort (Dutch National Flag partition) partitions the array into three sections: < pivot, == pivot, > pivot.","By randomly shuffling the pivot choice."]},{"id":"cs_fundamentals/sorting_and_searching#ce05c2521b25","section":"cs_fundamentals","module":"cs_fundamentals/sorting_and_searching","moduleName":"sorting and searching","qIndex":13,"difficulty":"advanced","question":"How do you handle integer overflow in the midpoint calculation for binary search?","answerFull":"The naive mid = (lo + hi) / 2 overflows when lo + hi > INT_MAX (2^31 − 1 in 32-bit integers). Fix: mid = lo + (hi - lo) / 2. In Python this is not a practical concern (arbitrary precision integers), but in Java/C/C++ it is critical. Overflow-safe alternative: mid = (lo + hi) >>> 1 (unsigned right shift in Java). This is a famous bug that existed in Java's standard library Arrays.binarySearch for over a decade (reported by Joshua Bloch in 2006).","correct":"The naive mid = (lo + hi) / 2 overflows when lo + hi > INT_MAX (2^31 − 1 in 32-bit integers).","distractors":["Instead of searching for a value in an array, binary search over the space of possible answers.","2n ln n ≈ 1.39 n log₂ n. Each pair (i, j) where i < j is compared at most once (when one of them is chosen as a pivot before the other).","3-way quicksort (Dutch National Flag partition) partitions the array into three sections: < pivot, == pivot, > pivot."]},{"id":"cs_fundamentals/sorting_and_searching#1dcae646d476","section":"cs_fundamentals","module":"cs_fundamentals/sorting_and_searching","moduleName":"sorting and searching","qIndex":14,"difficulty":"advanced","question":"When should you use radix sort over counting sort?","answerFull":"Radix sort is counting sort applied digit-by-digit. Use radix sort when: keys have a large range but small number of digits (e.g., 64-bit integers have 8 bytes = 8 radix-256 digits); you need a stable sort of multi-field keys (LSD radix sort: sort by least significant field first, then more significant — stability propagates the order). Counting sort is simpler and sufficient when the range k is small enough directly (e.g., grades 0–100). Radix sort's complexity is O(d × (n + b)) where b is the base (256 is typical); for 32-bit integers with b=256, d=4 passes.","correct":"Radix sort is counting sort applied digit-by-digit.","distractors":["Counting sort is O(n + k) where k is the key range.","In external sort, sorted runs are written to disk and then k-way merged using a min-heap of size k.","TimSort is a hybrid merge + insertion sort."]},{"id":"cs_fundamentals/sorting_and_searching#8b484336fa03","section":"cs_fundamentals","module":"cs_fundamentals/sorting_and_searching","moduleName":"sorting and searching","qIndex":15,"difficulty":"advanced","question":"How do you find the k-th smallest element efficiently?","answerFull":"Quickselect: partition around a random pivot; if pivot lands at position k, return it; otherwise recurse only on the side containing rank k. Expected O(n) time, O(n²) worst case. Median-of-medians pivot selection guarantees O(n) worst case but with large constants — rarely used in practice. For streaming k-th smallest, use a max-heap of size k: push each element; if size > k, pop the max. After the stream, the heap root is the k-th smallest. Cost: O(n log k).","correct":"Quickselect: partition around a random pivot; if pivot lands at position k, return it; otherwise recurse only on the side containing rank k.","distractors":["2n ln n ≈ 1.39 n log₂ n. Each pair (i, j) where i < j is compared at most once (when one of them is chosen as a pivot before the other).","lower_bound(target) returns the first index where arr[i] >= target — the leftmost position target could be inserted to keep order.","3-way quicksort (Dutch National Flag partition) partitions the array into three sections: < pivot, == pivot, > pivot."]},{"id":"cs_fundamentals/sorting_and_searching#5ef8b3e4d6dd","section":"cs_fundamentals","module":"cs_fundamentals/sorting_and_searching","moduleName":"sorting and searching","qIndex":16,"difficulty":"advanced","question":"What is the significance of the merge step in external sort?","answerFull":"In external sort, sorted runs are written to disk and then k-way merged using a min-heap of size k. Each heap operation is O(log k); the total number of elements is N, so merge cost is O(N log k). The I/O benefit: sequential reads from k files into a small buffer — the I/O pattern is sequential (fast), not random. This is why merge sort, not quicksort, is used for external sorting: merge can be driven by sequential I/O, while quicksort's partitioning requires random writes.","correct":"In external sort, sorted runs are written to disk and then k-way merged using a min-heap of size k.","distractors":["bisect.bisect_left(arr, x) returns the leftmost index where x could be inserted to keep arr sorted — equivalent to lower_bound.","Radix sort is counting sort applied digit-by-digit.","3-way quicksort (Dutch National Flag partition) partitions the array into three sections: < pivot, == pivot, > pivot."]},{"id":"cs_fundamentals/sorting_and_searching#bdc287887870","section":"cs_fundamentals","module":"cs_fundamentals/sorting_and_searching","moduleName":"sorting and searching","qIndex":17,"difficulty":"advanced","question":"How does Python's bisect module work and what are its pitfalls?","answerFull":"bisect.bisect_left(arr, x) returns the leftmost index where x could be inserted to keep arr sorted — equivalent to lower_bound. bisect.bisect_right(arr, x) (or bisect.bisect(arr, x)) returns one past the rightmost — equivalent to upper_bound. Pitfall: bisect assumes the list is already sorted; it does not check. Calling bisect_left on an unsorted list returns a meaningless result with no error. Second pitfall: bisect works on any comparable type, but the comparison must be consistent with the sort key used.","correct":"bisect.bisect_left(arr, x) returns the leftmost index where x could be inserted to keep arr sorted — equivalent to lower_bound.","distractors":["In external sort, sorted runs are written to disk and then k-way merged using a min-heap of size k.","A B+Tree index on 10 million rows with branching factor 100 has height ≤ 4.","lower_bound(target) returns the first index where arr[i] >= target — the leftmost position target could be inserted to keep order."]},{"id":"cs_fundamentals/sorting_and_searching#074908639632","section":"cs_fundamentals","module":"cs_fundamentals/sorting_and_searching","moduleName":"sorting and searching","qIndex":18,"difficulty":"advanced","question":"How does dual-pivot quicksort (Java's Arrays.sort for primitives) differ from single-pivot?","answerFull":"Dual-pivot quicksort uses two pivots (p1 ≤ p2) to partition into three sections: < p1, between p1 and p2, > p2. This reduces the average number of comparisons from 2n ln n to ~1.9n ln n and produces more balanced partitions. Java's implementation (by Vladimir Yaroslavskiy) is empirically 10–20% faster than single-pivot quicksort for primitive arrays due to fewer swaps and better branch prediction. It is not stable and cannot be used for Object[] (where stability is required by Java spec — hence Objects use TimSort).","correct":"Dual-pivot quicksort uses two pivots (p1 ≤ p2) to partition into three sections: < p1, between p1 and p2, > p2.","distractors":["Counting sort is O(n + k) where k is the key range.","A sort is stable if equal elements preserve their input order.","3-way quicksort (Dutch National Flag partition) partitions the array into three sections: < pivot, == pivot, > pivot."]},{"id":"cs_fundamentals/trees_and_binary_search_trees#56e129d98f11","section":"cs_fundamentals","module":"cs_fundamentals/trees_and_binary_search_trees","moduleName":"trees and binary search trees","qIndex":1,"difficulty":"core","question":"What is the in-order traversal of a BST and why is it important?","answerFull":"In-order traversal visits nodes in left-root-right order. For a BST, this produces keys in ascending sorted order. This property is used to: (a) validate a BST (in-order should produce a strictly increasing sequence), (b) find the k-th smallest element (traverse until k elements visited), (c) convert a BST to a sorted array. It is also the basis for the in-order successor/predecessor operations.","correct":"In-order traversal visits nodes in left-root-right order.","distractors":["It traverses the red-black tree from the root, maintaining a running candidate for the largest key ≤ k.","In-order traversal counting up to k.","Pre-order traversal with explicit null markers."]},{"id":"cs_fundamentals/trees_and_binary_search_trees#f08acfe37372","section":"cs_fundamentals","module":"cs_fundamentals/trees_and_binary_search_trees","moduleName":"trees and binary search trees","qIndex":2,"difficulty":"core","question":"What is the time complexity of BST search, insert, and delete?","answerFull":"O(h) where h is the height. For a balanced BST (AVL, red-black): h = O(log n), so all operations are O(log n). For a degenerate BST (sorted insertion): h = O(n), so all operations are O(n). The key point: the BST is only O(log n) if balanced.","correct":"O(h) where h is the height.","distractors":["Inserting elements in sorted (or reverse-sorted) order always adds to the rightmost (or leftmost) path, creating a linked list of height n.","After an insertion, an ancestor node may become unbalanced (|left_height - right_height| > 1).","An order-statistic tree is a BST where each node also stores the size of its subtree."]},{"id":"cs_fundamentals/trees_and_binary_search_trees#b841d98be7f0","section":"cs_fundamentals","module":"cs_fundamentals/trees_and_binary_search_trees","moduleName":"trees and binary search trees","qIndex":3,"difficulty":"core","question":"How does a BST degenerate, and how do self-balancing trees prevent it?","answerFull":"Inserting elements in sorted (or reverse-sorted) order always adds to the rightmost (or leftmost) path, creating a linked list of height n. Self-balancing trees (AVL, red-black) perform rotations after each insert/delete to restore the height invariant. AVL rotates when a node's subtree height difference exceeds 1. Red-black rotates and recolors to maintain the 4-5 color invariants.","correct":"Inserting elements in sorted (or reverse-sorted) order always adds to the rightmost (or leftmost) path, creating a linked list of height n.","distractors":["In-order traversal counting up to k.","After an insertion, an ancestor node may become unbalanced (|left_height - right_height| > 1).","O(h) where h is the height."]},{"id":"cs_fundamentals/trees_and_binary_search_trees#965cfa151fd7","section":"cs_fundamentals","module":"cs_fundamentals/trees_and_binary_search_trees","moduleName":"trees and binary search trees","qIndex":4,"difficulty":"core","question":"What is the difference between a B-tree and a B+tree?","answerFull":"In a B-tree, every node (internal and leaf) stores key-value pairs. In a B+tree, only leaf nodes store data pointers; internal nodes store only keys for routing. All leaf nodes in a B+tree are linked in a sorted doubly-linked list, enabling efficient range scans. Databases use B+trees because (a) higher branching factor (internal nodes can store more keys, fewer disk reads per lookup) and (b) range scans are O(k + log n) by following the leaf chain.","correct":"In a B-tree, every node (internal and leaf) stores key-value pairs.","distractors":["An order-statistic tree is a BST where each node also stores the size of its subtree.","The depth of a node is the number of edges from the root to that node (root has depth 0).","After an insertion, an ancestor node may become unbalanced (|left_height - right_height| > 1)."]},{"id":"cs_fundamentals/trees_and_binary_search_trees#a8430e26c0ec","section":"cs_fundamentals","module":"cs_fundamentals/trees_and_binary_search_trees","moduleName":"trees and binary search trees","qIndex":5,"difficulty":"core","question":"How do you find the lowest common ancestor (LCA) of two nodes in a binary tree?","answerFull":"Post-order traversal with early termination. Base case: if the current node is null, p, or q, return it. Recurse on both children. If both return non-null, the current node is the LCA. If only one returns non-null, propagate that upward. O(n) time, O(h) space. For a BST, the LCA is the first node where p and q diverge: traverse from root; if both p and q are less than the current node, go left; if both are greater, go right; otherwise the current node is the LCA. O(h) time.","correct":"Post-order traversal with early termination.","distractors":["In-order traversal counting up to k.","After an insertion, an ancestor node may become unbalanced (|left_height - right_height| > 1).","A complete binary tree where each node stores an aggregate (sum, min, max) of a range of array elements."]},{"id":"cs_fundamentals/trees_and_binary_search_trees#93143d0ecb95","section":"cs_fundamentals","module":"cs_fundamentals/trees_and_binary_search_trees","moduleName":"trees and binary search trees","qIndex":6,"difficulty":"intermediate","question":"What is a segment tree and when do you need it?","answerFull":"A complete binary tree where each node stores an aggregate (sum, min, max) of a range of array elements. Build: O(n). Point update: O(log n). Range query: O(log n). Use when you need both range queries and point updates on an array — a sorted array gives O(log n) query but O(n) update. A segment tree is the right tool for: range sum, range minimum/maximum, and problems combining queries and updates. Space: O(4n) for the tree array.","correct":"A complete binary tree where each node stores an aggregate (sum, min, max) of a range of array elements.","distractors":["It traverses the red-black tree from the root, maintaining a running candidate for the largest key ≤ k.","Height = floor(log₂ n). Number of nodes in a perfect binary tree of height h = 2^(h+1) - 1.","In a B-tree, every node (internal and leaf) stores key-value pairs."]},{"id":"cs_fundamentals/trees_and_binary_search_trees#da6dd4507894","section":"cs_fundamentals","module":"cs_fundamentals/trees_and_binary_search_trees","moduleName":"trees and binary search trees","qIndex":7,"difficulty":"intermediate","question":"How does Java's TreeMap.floorKey(k) work?","answerFull":"It traverses the red-black tree from the root, maintaining a running candidate for the largest key ≤ k. At each node: if the node's key ≤ k, update the candidate and go right (there may be a larger valid key). If the node's key > k, go left. Return the last valid candidate. O(log n). The same pattern applies to ceilingKey (smallest key ≥ k), lowerKey (<), and higherKey (>).","correct":"It traverses the red-black tree from the root, maintaining a running candidate for the largest key ≤ k.","distractors":["The depth of a node is the number of edges from the root to that node (root has depth 0).","The Completely Fair Scheduler (CFS) uses a red-black tree (rb_tree) as the run queue.","In-order traversal visits nodes in left-root-right order."]},{"id":"cs_fundamentals/trees_and_binary_search_trees#fe057db628f5","section":"cs_fundamentals","module":"cs_fundamentals/trees_and_binary_search_trees","moduleName":"trees and binary search trees","qIndex":8,"difficulty":"intermediate","question":"What is the time complexity of building a BST from a sorted array?","answerFull":"O(n) if done correctly (insert the middle element as root, recurse on left and right halves — same as the \"convert sorted array to BST\" problem). If you insert elements one by one in sorted order: O(n²) because each insert appends to the end of a degenerate tree. The recursive balanced-build approach produces a balanced BST of height log n.","correct":"O(n) if done correctly (insert the middle element as root, recurse on left and right halves — same as the \"convert sorted array to BST\" problem).","distractors":["The depth of a node is the number of edges from the root to that node (root has depth 0).","After an insertion, an ancestor node may become unbalanced (|left_height - right_height| > 1).","An order-statistic tree is a BST where each node also stores the size of its subtree."]},{"id":"cs_fundamentals/trees_and_binary_search_trees#88a41c97e926","section":"cs_fundamentals","module":"cs_fundamentals/trees_and_binary_search_trees","moduleName":"trees and binary search trees","qIndex":9,"difficulty":"intermediate","question":"How do you serialise and deserialise a binary tree?","answerFull":"Pre-order traversal with explicit null markers. Serialise: append node.val for non-null nodes, append \"#\" for null. Deserialise: consume the sequence recursively — if the first token is \"#\", return null; otherwise create a node with that value and recurse for left and right. O(n) time and space. BFS-based serialisation (level order) is also valid and is the format used by LeetCode.","correct":"Pre-order traversal with explicit null markers.","distractors":["In-order traversal visits nodes in left-root-right order.","Pre-order: first element is the root.","An order-statistic tree is a BST where each node also stores the size of its subtree."]},{"id":"cs_fundamentals/trees_and_binary_search_trees#5ab6ef3725c4","section":"cs_fundamentals","module":"cs_fundamentals/trees_and_binary_search_trees","moduleName":"trees and binary search trees","qIndex":10,"difficulty":"intermediate","question":"What is the height and number of nodes in a complete binary tree?","answerFull":"Height = floor(log₂ n). Number of nodes in a perfect binary tree of height h = 2^(h+1) - 1. A complete binary tree has all levels full except possibly the last, which is filled left to right. Checking if a binary tree is complete can be done in O(n) with BFS: level-order traversal; once a null node is encountered, all subsequent nodes must also be null.","correct":"Height = floor(log₂ n). Number of nodes in a perfect binary tree of height h = 2^(h+1) - 1.","distractors":["It traverses the red-black tree from the root, maintaining a running candidate for the largest key ≤ k.","After an insertion, an ancestor node may become unbalanced (|left_height - right_height| > 1).","O(log n + k) where k is the number of nodes in the range."]},{"id":"cs_fundamentals/trees_and_binary_search_trees#b47cfcc771d8","section":"cs_fundamentals","module":"cs_fundamentals/trees_and_binary_search_trees","moduleName":"trees and binary search trees","qIndex":11,"difficulty":"advanced","question":"How do you find the k-th smallest element in a BST efficiently?","answerFull":"In-order traversal counting up to k. O(h + k) time — traverse down to the leftmost node, then visit k nodes in order. With an augmented BST (each node stores the size of its subtree), you can do it in O(h) = O(log n): if k ≤ left-subtree-size, recurse left; if k == left-subtree-size + 1, the root is the k-th element; otherwise recurse right with k - left-subtree-size - 1.","correct":"In-order traversal counting up to k.","distractors":["Post-order traversal with early termination.","In-order traversal visits nodes in left-root-right order.","Pre-order traversal with explicit null markers."]},{"id":"cs_fundamentals/trees_and_binary_search_trees#64f7449e2072","section":"cs_fundamentals","module":"cs_fundamentals/trees_and_binary_search_trees","moduleName":"trees and binary search trees","qIndex":12,"difficulty":"advanced","question":"What is a self-balancing BST used for in the Linux kernel?","answerFull":"The Completely Fair Scheduler (CFS) uses a red-black tree (rb_tree) as the run queue. Each runnable task is a node keyed by virtual runtime (vruntime). The leftmost node is always the next task to schedule (smallest vruntime = least CPU time received). rb_leftmost is O(log n); insertion after a task preempts is O(log n). The red-black tree is preferred over a heap because it supports efficient deletion of arbitrary nodes (when a task becomes unrunnable) in O(log n), which a heap cannot do efficiently.","correct":"The Completely Fair Scheduler (CFS) uses a red-black tree (rb_tree) as the run queue.","distractors":["Height = floor(log₂ n). Number of nodes in a perfect binary tree of height h = 2^(h+1) - 1.","O(n) if done correctly (insert the middle element as root, recurse on left and right halves — same as the \"convert sorted array to BST\" problem).","It traverses the red-black tree from the root, maintaining a running candidate for the largest key ≤ k."]},{"id":"cs_fundamentals/trees_and_binary_search_trees#35f92f0add8b","section":"cs_fundamentals","module":"cs_fundamentals/trees_and_binary_search_trees","moduleName":"trees and binary search trees","qIndex":13,"difficulty":"advanced","question":"What is the difference between a tree's depth and its height?","answerFull":"The depth of a node is the number of edges from the root to that node (root has depth 0). The height of a node is the number of edges on the longest downward path from that node to a leaf (leaves have height 0). The height of the tree is the height of the root. Some textbooks define these as the number of nodes rather than edges — always clarify in interviews. Both are equivalent for complexity analysis.","correct":"The depth of a node is the number of edges from the root to that node (root has depth 0).","distractors":["Inserting elements in sorted (or reverse-sorted) order always adds to the rightmost (or leftmost) path, creating a linked list of height n.","A complete binary tree where each node stores an aggregate (sum, min, max) of a range of array elements.","Height = floor(log₂ n). Number of nodes in a perfect binary tree of height h = 2^(h+1) - 1."]},{"id":"cs_fundamentals/trees_and_binary_search_trees#8d6b803a8884","section":"cs_fundamentals","module":"cs_fundamentals/trees_and_binary_search_trees","moduleName":"trees and binary search trees","qIndex":14,"difficulty":"advanced","question":"How would you implement an order-statistic tree?","answerFull":"An order-statistic tree is a BST where each node also stores the size of its subtree. Operations: select (find k-th smallest), rank (find the rank of a key). Select: O(log n) by comparing k to the left-subtree size. Rank: O(log n) by accumulating left-subtree sizes along the path. Implemented by augmenting a red-black tree (Java has no built-in; Python's sortedcontainers.SortedList provides similar functionality).","correct":"An order-statistic tree is a BST where each node also stores the size of its subtree.","distractors":["O(n) if done correctly (insert the middle element as root, recurse on left and right halves — same as the \"convert sorted array to BST\" problem).","In-order traversal visits nodes in left-root-right order.","In-order traversal counting up to k."]},{"id":"cs_fundamentals/trees_and_binary_search_trees#39b06f88eda3","section":"cs_fundamentals","module":"cs_fundamentals/trees_and_binary_search_trees","moduleName":"trees and binary search trees","qIndex":15,"difficulty":"advanced","question":"When would you choose a skip list over a balanced BST?","answerFull":"A skip list is a probabilistic data structure with O(log n) expected operations and O(n) space. Advantages over BST: simpler to implement correctly (no rotations), easy to make lock-free for concurrent access, supports efficient range scans. Redis sorted sets use a skip list for exactly this: simpler concurrent implementation with O(log n) ZADD/ZRANK/ZRANGE. Disadvantages: probabilistic worst case; more memory per element (multiple forward pointers).","correct":"A skip list is a probabilistic data structure with O(log n) expected operations and O(n) space.","distractors":["An order-statistic tree is a BST where each node also stores the size of its subtree.","O(log n + k) where k is the number of nodes in the range.","Height = floor(log₂ n). Number of nodes in a perfect binary tree of height h = 2^(h+1) - 1."]},{"id":"cs_fundamentals/trees_and_binary_search_trees#1fc16884456a","section":"cs_fundamentals","module":"cs_fundamentals/trees_and_binary_search_trees","moduleName":"trees and binary search trees","qIndex":16,"difficulty":"advanced","question":"What is the complexity of finding all nodes in a BST within a range [lo, hi]?","answerFull":"O(log n + k) where k is the number of nodes in the range. Navigate to the leftmost node ≥ lo in O(log n), then in-order traverse until the value exceeds hi, collecting k nodes in O(k). The B+tree's linked leaf list gives the same O(log n + k) complexity for database range scans.","correct":"O(log n + k) where k is the number of nodes in the range.","distractors":["A complete binary tree where each node stores an aggregate (sum, min, max) of a range of array elements.","The depth of a node is the number of edges from the root to that node (root has depth 0).","Height = floor(log₂ n). Number of nodes in a perfect binary tree of height h = 2^(h+1) - 1."]},{"id":"cs_fundamentals/trees_and_binary_search_trees#fe7ef78fc9f6","section":"cs_fundamentals","module":"cs_fundamentals/trees_and_binary_search_trees","moduleName":"trees and binary search trees","qIndex":17,"difficulty":"advanced","question":"How do you construct a binary tree from its pre-order and in-order traversals?","answerFull":"Pre-order: first element is the root. In-order: the root divides it into left and right subtrees. Algorithm: pop the first element of pre-order (it's the root); find its position in in-order; elements to the left form the left subtree, right side forms the right subtree; recurse. O(n²) with linear search or O(n) with a hash map for O(1) index lookups. The hash map from value to in-order index reduces the construction to O(n).","correct":"Pre-order: first element is the root.","distractors":["A complete binary tree where each node stores an aggregate (sum, min, max) of a range of array elements.","The depth of a node is the number of edges from the root to that node (root has depth 0).","Pre-order traversal with explicit null markers."]},{"id":"cs_fundamentals/trees_and_binary_search_trees#b1ce378ae98f","section":"cs_fundamentals","module":"cs_fundamentals/trees_and_binary_search_trees","moduleName":"trees and binary search trees","qIndex":18,"difficulty":"advanced","question":"Explain the four cases of AVL tree insertion imbalance and how to fix them.","answerFull":"After an insertion, an ancestor node may become unbalanced (|left_height - right_height| > 1). Four cases: (1) Left-Left: new node in left child's left subtree — single right rotation at the unbalanced node. (2) Right-Right: new node in right child's right subtree — single left rotation. (3) Left-Right: new node in left child's right subtree — left rotate the left child, then right rotate the unbalanced node. (4) Right-Left: new node in right child's left subtree — right rotate the right child, then left rotate the unbalanced node. After insertion, only O(1) rotations needed (AVL: at most 2).","correct":"After an insertion, an ancestor node may become unbalanced (|left_height - right_height| > 1).","distractors":["An order-statistic tree is a BST where each node also stores the size of its subtree.","In a B-tree, every node (internal and leaf) stores key-value pairs.","Height = floor(log₂ n). Number of nodes in a perfect binary tree of height h = 2^(h+1) - 1."]}]