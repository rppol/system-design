[{"id":"spring/bean_lifecycle#47793137f127","section":"spring","module":"spring/bean_lifecycle","moduleName":"bean lifecycle","sourceFile":"README.md","difficulty":"core","question":"Why is the AOP proxy created after @PostConstruct and not before?","answerFull":"BeanPostProcessor.postProcessAfterInitialization runs after all initialization hooks complete. AnnotationAwareAspectJAutoProxyCreator is a BeanPostProcessor that creates CGLIB/JDK proxies. This means @PostConstruct always runs on the raw bean, not the proxy. Consequently, @Transactional in @PostConstruct has no effect. Use @EventListener(ContextRefreshedEvent.class) for initialization logic that needs to run within a transaction.","correct":"BeanPostProcessor.postProcessAfterInitialization runs after all initialization hooks complete.","distractors":["Singleton: one instance per ApplicationContext, created eagerly at startup (unless @Lazy), managed for its full lifecycle including destruction.","@PostConstruct is declared on a method inside the bean class — requires access to modify the source.","The prototype bean is injected exactly once at singleton initialization time."],"distractorIds":["spring/bean_lifecycle#76b32b622bb3","spring/bean_lifecycle#cf14fb87e0be","spring/bean_lifecycle#04bac10006a6"],"concepts":["postprocessafterinitialization","beanpostprocessor","postconstruct","hooks","aop","initialization"],"correctMd":"`BeanPostProcessor.postProcessAfterInitialization` runs after all initialization hooks complete.","answerFullMd":"`BeanPostProcessor.postProcessAfterInitialization` runs after all initialization hooks complete. `AnnotationAwareAspectJAutoProxyCreator` is a `BeanPostProcessor` that creates CGLIB/JDK proxies. This means `@PostConstruct` always runs on the raw bean, not the proxy. Consequently, `@Transactional` in `@PostConstruct` has no effect. Use `@EventListener(ContextRefreshedEvent.class)` for initialization logic that needs to run within a transaction.","distractorsMd":["Singleton: one instance per ApplicationContext, created eagerly at startup (unless `@Lazy`), managed for its full lifecycle including destruction.","`@PostConstruct` is declared on a method inside the bean class — requires access to modify the source.",null]},{"id":"spring/bean_lifecycle#04bac10006a6","section":"spring","module":"spring/bean_lifecycle","moduleName":"bean lifecycle","sourceFile":"README.md","difficulty":"core","question":"What happens when you inject a prototype bean into a singleton?","answerFull":"The prototype bean is injected exactly once at singleton initialization time. All subsequent calls use that same instance. This defeats the purpose of prototype scope. Fix options: ObjectProvider<T> for lazy per-call retrieval, @Lookup method injection (Spring overrides the method body), or scoped proxy with @Scope(proxyMode = ScopedProxyMode.TARGET_CLASS). The proxy approach is most transparent for callers.","correct":"The prototype bean is injected exactly once at singleton initialization time.","distractors":["@PostConstruct is declared on a method inside the bean class — requires access to modify the source.","@Lazy on a @Component or @Bean delays instantiation until first requested.","ObjectProvider<T> provides lazy, optional, and multi-bean access to Spring-managed beans."],"distractorIds":["spring/bean_lifecycle#cf14fb87e0be","spring/bean_lifecycle#1b13a1d42140","spring/bean_lifecycle#3aea5fbd948e"],"concepts":["prototype","inject","initialization","injected","singleton","happens"],"answerFullMd":"The prototype bean is injected exactly once at singleton initialization time. All subsequent calls use that same instance. This defeats the purpose of prototype scope. Fix options: `ObjectProvider<T>` for lazy per-call retrieval, `@Lookup` method injection (Spring overrides the method body), or scoped proxy with `@Scope(proxyMode = ScopedProxyMode.TARGET_CLASS)`. The proxy approach is most transparent for callers.","distractorsMd":["`@PostConstruct` is declared on a method inside the bean class — requires access to modify the source.","`@Lazy` on a `@Component` or `@Bean` delays instantiation until first requested.","`ObjectProvider<T>` provides lazy, optional, and multi-bean access to Spring-managed beans."]},{"id":"spring/bean_lifecycle#9bd9b8f7770d","section":"spring","module":"spring/bean_lifecycle","moduleName":"bean lifecycle","sourceFile":"README.md","difficulty":"core","question":"What is the difference between @PostConstruct and InitializingBean.afterPropertiesSet()?","answerFull":"Both execute at the same lifecycle phase, with @PostConstruct running first if both are present. @PostConstruct is preferred because it is a JSR-250 standard annotation that does not tie your code to the Spring API. InitializingBean requires your class to implement a Spring interface and allows checked exceptions. @Bean(initMethod=...) is the best choice for third-party classes you cannot annotate. For your own code, use @PostConstruct.","correct":"Both execute at the same lifecycle phase, with @PostConstruct running first if both are present.","distractors":["SmartLifecycle extends Lifecycle with a getPhase() method.","@PostConstruct is declared on a method inside the bean class — requires access to modify the source.","Singleton: one instance per ApplicationContext, created eagerly at startup (unless @Lazy), managed for its full lifecycle including destruction."],"distractorIds":["spring/bean_lifecycle#a42c711d9c3a","spring/bean_lifecycle#cf14fb87e0be","spring/bean_lifecycle#76b32b622bb3"],"concepts":["postconstruct","difference","present","lifecycle","phase","execute"],"correctMd":"Both execute at the same lifecycle phase, with `@PostConstruct` running first if both are present.","answerFullMd":"Both execute at the same lifecycle phase, with `@PostConstruct` running first if both are present. `@PostConstruct` is preferred because it is a JSR-250 standard annotation that does not tie your code to the Spring API. `InitializingBean` requires your class to implement a Spring interface and allows checked exceptions. `@Bean(initMethod=...)` is the best choice for third-party classes you cannot annotate. For your own code, use `@PostConstruct`.","distractorsMd":["`SmartLifecycle` extends `Lifecycle` with a `getPhase()` method.","`@PostConstruct` is declared on a method inside the bean class — requires access to modify the source.","Singleton: one instance per ApplicationContext, created eagerly at startup (unless `@Lazy`), managed for its full lifecycle including destruction."]},{"id":"spring/bean_lifecycle#6c300c3a6544","section":"spring","module":"spring/bean_lifecycle","moduleName":"bean lifecycle","sourceFile":"README.md","difficulty":"core","question":"Does @PreDestroy get called for prototype beans?","answerFull":"No. The container creates prototype beans but does not retain a reference to them after delivery. The container cannot call @PreDestroy on objects it does not track. Only singleton beans (and other container-managed scoped beans like request/session) have their destroy lifecycle called. If your prototype bean holds resources (connections, file handles), the caller is responsible for cleanup. This is a common memory/resource leak in Spring applications.","correct":"No. The container creates prototype beans but does not retain a reference to them after delivery.","distractors":["The prototype bean is injected exactly once at singleton initialization time.","BeanNameAware, BeanFactoryAware, ApplicationContextAware (and others) provide callbacks for beans to obtain framework infrastructure references.","ObjectProvider<T> provides lazy, optional, and multi-bean access to Spring-managed beans."],"distractorIds":["spring/bean_lifecycle#04bac10006a6","spring/bean_lifecycle#556cad6354e3","spring/bean_lifecycle#3aea5fbd948e"],"concepts":["predestroy","prototype","retain","delivery","beans","called"],"answerFullMd":"No. The container creates prototype beans but does not retain a reference to them after delivery. The container cannot call `@PreDestroy` on objects it does not track. Only singleton beans (and other container-managed scoped beans like request/session) have their destroy lifecycle called. If your prototype bean holds resources (connections, file handles), the caller is responsible for cleanup. This is a common memory/resource leak in Spring applications.","distractorsMd":[null,"`BeanNameAware`, `BeanFactoryAware`, `ApplicationContextAware` (and others) provide callbacks for beans to obtain framework infrastructure references.","`ObjectProvider<T>` provides lazy, optional, and multi-bean access to Spring-managed beans."]},{"id":"spring/bean_lifecycle#556cad6354e3","section":"spring","module":"spring/bean_lifecycle","moduleName":"bean lifecycle","sourceFile":"README.md","difficulty":"intermediate","question":"What are the Aware interfaces and when should you implement them?","answerFull":"BeanNameAware, BeanFactoryAware, ApplicationContextAware (and others) provide callbacks for beans to obtain framework infrastructure references. They run after property injection but before BeanPostProcessor. You should implement them only when writing framework extensions, not application beans. Application beans should never hold an ApplicationContext reference — that is the Service Locator anti-pattern. Use @Autowired injection for application-level dependencies.","correct":"BeanNameAware, BeanFactoryAware, ApplicationContextAware (and others) provide callbacks for beans to obtain framework infrastructure references.","distractors":["ObjectProvider<T> provides lazy, optional, and multi-bean access to Spring-managed beans.","@Lazy on a @Component or @Bean delays instantiation until first requested.","@PostConstruct is declared on a method inside the bean class — requires access to modify the source."],"distractorIds":["spring/bean_lifecycle#3aea5fbd948e","spring/bean_lifecycle#1b13a1d42140","spring/bean_lifecycle#cf14fb87e0be"],"concepts":["applicationcontextaware","beanfactoryaware","beannameaware","obtain","callbacks","aware"],"correctMd":"`BeanNameAware`, `BeanFactoryAware`, `ApplicationContextAware` (and others) provide callbacks for beans to obtain framework infrastructure references.","answerFullMd":"`BeanNameAware`, `BeanFactoryAware`, `ApplicationContextAware` (and others) provide callbacks for beans to obtain framework infrastructure references. They run after property injection but before `BeanPostProcessor`. You should implement them only when writing framework extensions, not application beans. Application beans should never hold an `ApplicationContext` reference — that is the Service Locator anti-pattern. Use `@Autowired` injection for application-level dependencies.","distractorsMd":["`ObjectProvider<T>` provides lazy, optional, and multi-bean access to Spring-managed beans.","`@Lazy` on a `@Component` or `@Bean` delays instantiation until first requested.","`@PostConstruct` is declared on a method inside the bean class — requires access to modify the source."]},{"id":"spring/bean_lifecycle#cf14fb87e0be","section":"spring","module":"spring/bean_lifecycle","moduleName":"bean lifecycle","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between @Bean(initMethod=...) and @PostConstruct?","answerFull":"@PostConstruct is declared on a method inside the bean class — requires access to modify the source. @Bean(initMethod=\"methodName\") is declared in the @Configuration class — works for third-party classes you cannot modify. Both execute at the same lifecycle point (after InitializingBean.afterPropertiesSet()). For Spring libraries integrating external resources (HikariCP, Flyway), initMethod is the standard approach.","correct":"@PostConstruct is declared on a method inside the bean class — requires access to modify the source.","distractors":["The prototype bean is injected exactly once at singleton initialization time.","SmartLifecycle extends Lifecycle with a getPhase() method.","ObjectProvider<T> provides lazy, optional, and multi-bean access to Spring-managed beans."],"distractorIds":["spring/bean_lifecycle#04bac10006a6","spring/bean_lifecycle#a42c711d9c3a","spring/bean_lifecycle#3aea5fbd948e"],"concepts":["postconstruct","modify","difference","declared","inside","bean"],"correctMd":"`@PostConstruct` is declared on a method inside the bean class — requires access to modify the source.","answerFullMd":"`@PostConstruct` is declared on a method inside the bean class — requires access to modify the source. `@Bean(initMethod=\"methodName\")` is declared in the `@Configuration` class — works for third-party classes you cannot modify. Both execute at the same lifecycle point (after `InitializingBean.afterPropertiesSet()`). For Spring libraries integrating external resources (HikariCP, Flyway), `initMethod` is the standard approach.","distractorsMd":[null,"`SmartLifecycle` extends `Lifecycle` with a `getPhase()` method.","`ObjectProvider<T>` provides lazy, optional, and multi-bean access to Spring-managed beans."]},{"id":"spring/bean_lifecycle#a42c711d9c3a","section":"spring","module":"spring/bean_lifecycle","moduleName":"bean lifecycle","sourceFile":"README.md","difficulty":"intermediate","question":"What is SmartLifecycle and how does phase ordering work?","answerFull":"SmartLifecycle extends Lifecycle with a getPhase() method. During start(), lower phase numbers start first. During stop(), higher phase numbers stop first (reverse order). This ensures that infrastructure beans (phase 0) start before application beans (phase MAX_VALUE) and stop after them. Spring Boot's embedded Tomcat uses a high phase so that it starts after all application beans are ready and stops before beans are destroyed — ensuring no requests are processed against a partially destroyed context.","correct":"SmartLifecycle extends Lifecycle with a getPhase() method.","distractors":["Singleton: one instance per ApplicationContext, created eagerly at startup (unless @Lazy), managed for its full lifecycle including destruction.","@PostConstruct is declared on a method inside the bean class — requires access to modify the source.","@Lazy on a @Component or @Bean delays instantiation until first requested."],"distractorIds":["spring/bean_lifecycle#76b32b622bb3","spring/bean_lifecycle#cf14fb87e0be","spring/bean_lifecycle#1b13a1d42140"],"concepts":["getphase","smartlifecycle","extends","lifecycle","ordering","phase"],"correctMd":"`SmartLifecycle` extends `Lifecycle` with a `getPhase()` method.","answerFullMd":"`SmartLifecycle` extends `Lifecycle` with a `getPhase()` method. During `start()`, lower phase numbers start first. During `stop()`, higher phase numbers stop first (reverse order). This ensures that infrastructure beans (phase 0) start before application beans (phase MAX_VALUE) and stop after them. Spring Boot's embedded Tomcat uses a high phase so that it starts after all application beans are ready and stops before beans are destroyed — ensuring no requests are processed against a partially destroyed context.","distractorsMd":["Singleton: one instance per ApplicationContext, created eagerly at startup (unless `@Lazy`), managed for its full lifecycle including destruction.","`@PostConstruct` is declared on a method inside the bean class — requires access to modify the source.","`@Lazy` on a `@Component` or `@Bean` delays instantiation until first requested."]},{"id":"spring/bean_lifecycle#350f8ea5d9c2","section":"spring","module":"spring/bean_lifecycle","moduleName":"bean lifecycle","sourceFile":"README.md","difficulty":"intermediate","question":"Can a @Configuration class be a BeanPostProcessor?","answerFull":"Yes, but with caveats. BeanPostProcessor beans are instantiated very early in the container lifecycle — before BeanFactoryPostProcessors run and before other singleton beans are ready. A BeanPostProcessor that @Autowired non-infrastructure beans may trigger those beans' premature instantiation, causing them to miss certain BeanFactoryPostProcessor processing (like property placeholder resolution). Spring logs a warning: \"Bean X is not eligible for processing by all BeanPostProcessors\". Prefer keeping BeanPostProcessor implementations simple with minimal dependencies.","correct":"Yes, but with caveats. BeanPostProcessor beans are instantiated very early in the container lifecycle — before BeanFactoryPostProcessors run and before other singleton beans are ready.","distractors":["BeanPostProcessor.postProcessAfterInitialization runs after all initialization hooks complete.","The prototype bean is injected exactly once at singleton initialization time.","@PostConstruct is declared on a method inside the bean class — requires access to modify the source."],"distractorIds":["spring/bean_lifecycle#47793137f127","spring/bean_lifecycle#04bac10006a6","spring/bean_lifecycle#cf14fb87e0be"],"concepts":["beanfactorypostprocessors","caveats","beanpostprocessor","instantiated","ready","singleton"],"correctMd":"Yes, but with caveats. `BeanPostProcessor` beans are instantiated very early in the container lifecycle — before `BeanFactoryPostProcessors` run and before other singleton beans are ready.","answerFullMd":"Yes, but with caveats. `BeanPostProcessor` beans are instantiated very early in the container lifecycle — before `BeanFactoryPostProcessors` run and before other singleton beans are ready. A `BeanPostProcessor` that `@Autowired` non-infrastructure beans may trigger those beans' premature instantiation, causing them to miss certain `BeanFactoryPostProcessor` processing (like property placeholder resolution). Spring logs a warning: \"Bean X is not eligible for processing by all BeanPostProcessors\". Prefer keeping `BeanPostProcessor` implementations simple with minimal dependencies.","distractorsMd":["`BeanPostProcessor.postProcessAfterInitialization` runs after all initialization hooks complete.",null,"`@PostConstruct` is declared on a method inside the bean class — requires access to modify the source."]},{"id":"spring/bean_lifecycle#c95651f8d9b3","section":"spring","module":"spring/bean_lifecycle","moduleName":"bean lifecycle","sourceFile":"README.md","difficulty":"advanced","question":"What happens during context shutdown? What order do beans get destroyed?","answerFull":"During AbstractApplicationContext.close(), Spring calls destroySingletons() on the BeanFactory. Beans are destroyed in reverse dependency order: a bean that depends on others is destroyed first, then its dependencies. For explicit ordering, implement SmartLifecycle with phase values (higher phase = destroyed first) or use @DependsOn. @PreDestroy runs first, then DisposableBean.destroy(), then destroy-method. This orderly shutdown ensures connections are not closed while other beans are still using them.","correct":"During AbstractApplicationContext.close(), Spring calls destroySingletons() on the BeanFactory.","distractors":["BeanNameAware, BeanFactoryAware, ApplicationContextAware (and others) provide callbacks for beans to obtain framework infrastructure references.","ObjectProvider<T> provides lazy, optional, and multi-bean access to Spring-managed beans.","Spring builds a dependency graph from injection relationships (@Autowired fields, constructor parameters, @DependsOn annotations)."],"distractorIds":["spring/bean_lifecycle#556cad6354e3","spring/bean_lifecycle#3aea5fbd948e","spring/bean_lifecycle#9df7097b5cf0"],"concepts":["abstractapplicationcontext","destroysingletons","beanfactory","destroyed","shutdown","close"],"correctMd":"During `AbstractApplicationContext.close()`, Spring calls `destroySingletons()` on the `BeanFactory`.","answerFullMd":"During `AbstractApplicationContext.close()`, Spring calls `destroySingletons()` on the `BeanFactory`. Beans are destroyed in reverse dependency order: a bean that depends on others is destroyed first, then its dependencies. For explicit ordering, implement `SmartLifecycle` with phase values (higher phase = destroyed first) or use `@DependsOn`. `@PreDestroy` runs first, then `DisposableBean.destroy()`, then `destroy-method`. This orderly shutdown ensures connections are not closed while other beans are still using them.","distractorsMd":["`BeanNameAware`, `BeanFactoryAware`, `ApplicationContextAware` (and others) provide callbacks for beans to obtain framework infrastructure references.","`ObjectProvider<T>` provides lazy, optional, and multi-bean access to Spring-managed beans.","Spring builds a dependency graph from injection relationships (`@Autowired` fields, constructor parameters, `@DependsOn` annotations)."]},{"id":"spring/bean_lifecycle#76b32b622bb3","section":"spring","module":"spring/bean_lifecycle","moduleName":"bean lifecycle","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between singleton scope and prototype scope?","answerFull":"Singleton: one instance per ApplicationContext, created eagerly at startup (unless @Lazy), managed for its full lifecycle including destruction. Prototype: new instance created for every getBean() call or injection point, initialization lifecycle runs fully, but the container never calls @PreDestroy or destroy(). Prototype beans must have their resources cleaned up by their consumers. For web apps, request and session scopes provide scoped lifecycle management with automatic destruction.","correct":"Singleton: one instance per ApplicationContext, created eagerly at startup (unless @Lazy), managed for its full lifecycle including destruction.","distractors":["SmartLifecycle extends Lifecycle with a getPhase() method.","ObjectProvider<T> provides lazy, optional, and multi-bean access to Spring-managed beans.","The prototype bean is injected exactly once at singleton initialization time."],"distractorIds":["spring/bean_lifecycle#a42c711d9c3a","spring/bean_lifecycle#3aea5fbd948e","spring/bean_lifecycle#04bac10006a6"],"concepts":["destruction","eagerly","prototype","difference","unless","singleton"],"correctMd":"Singleton: one instance per ApplicationContext, created eagerly at startup (unless `@Lazy`), managed for its full lifecycle including destruction.","answerFullMd":"Singleton: one instance per ApplicationContext, created eagerly at startup (unless `@Lazy`), managed for its full lifecycle including destruction. Prototype: new instance created for every `getBean()` call or injection point, initialization lifecycle runs fully, but the container never calls `@PreDestroy` or `destroy()`. Prototype beans must have their resources cleaned up by their consumers. For web apps, `request` and `session` scopes provide scoped lifecycle management with automatic destruction.","distractorsMd":["`SmartLifecycle` extends `Lifecycle` with a `getPhase()` method.","`ObjectProvider<T>` provides lazy, optional, and multi-bean access to Spring-managed beans.",null]},{"id":"spring/bean_lifecycle#9df7097b5cf0","section":"spring","module":"spring/bean_lifecycle","moduleName":"bean lifecycle","sourceFile":"README.md","difficulty":"advanced","question":"How does Spring resolve the initialization order of singleton beans?","answerFull":"Spring builds a dependency graph from injection relationships (@Autowired fields, constructor parameters, @DependsOn annotations). Beans are initialized in dependency-first order using a depth-first traversal. If bean A requires bean B and C, B and C are fully initialized before A. Cycles in constructor injection are detected immediately. Cycles in setter/field injection are resolved by exposing partially initialized beans via the early singleton object cache (earlySingletonObjects).","correct":"Spring builds a dependency graph from injection relationships (@Autowired fields, constructor parameters, @DependsOn annotations).","distractors":["BeanNameAware, BeanFactoryAware, ApplicationContextAware (and others) provide callbacks for beans to obtain framework infrastructure references.","ObjectProvider<T> provides lazy, optional, and multi-bean access to Spring-managed beans.","The prototype bean is injected exactly once at singleton initialization time."],"distractorIds":["spring/bean_lifecycle#556cad6354e3","spring/bean_lifecycle#3aea5fbd948e","spring/bean_lifecycle#04bac10006a6"],"concepts":["resolve","dependson","autowired","annotations","relationships","initialization"],"correctMd":"Spring builds a dependency graph from injection relationships (`@Autowired` fields, constructor parameters, `@DependsOn` annotations).","answerFullMd":"Spring builds a dependency graph from injection relationships (`@Autowired` fields, constructor parameters, `@DependsOn` annotations). Beans are initialized in dependency-first order using a depth-first traversal. If bean A requires bean B and C, B and C are fully initialized before A. Cycles in constructor injection are detected immediately. Cycles in setter/field injection are resolved by exposing partially initialized beans via the early singleton object cache (`earlySingletonObjects`).","distractorsMd":["`BeanNameAware`, `BeanFactoryAware`, `ApplicationContextAware` (and others) provide callbacks for beans to obtain framework infrastructure references.","`ObjectProvider<T>` provides lazy, optional, and multi-bean access to Spring-managed beans.",null]},{"id":"spring/bean_lifecycle#3aea5fbd948e","section":"spring","module":"spring/bean_lifecycle","moduleName":"bean lifecycle","sourceFile":"README.md","difficulty":"advanced","question":"What is the purpose of ObjectProvider<T>?","answerFull":"ObjectProvider<T> provides lazy, optional, and multi-bean access to Spring-managed beans. Key methods: getObject() (throws if not found), getIfAvailable() (returns null if not found), getIfUnique() (returns null if multiple beans), stream() (iterate all matching beans). It is more explicit than @Autowired(required=false) and avoids the need for Optional<T> workarounds. Primarily used for optional dependencies and prototype beans that should be retrieved on each call.","correct":"ObjectProvider<T> provides lazy, optional, and multi-bean access to Spring-managed beans.","distractors":["Singleton: one instance per ApplicationContext, created eagerly at startup (unless @Lazy), managed for its full lifecycle including destruction.","During AbstractApplicationContext.close(), Spring calls destroySingletons() on the BeanFactory.","BeanNameAware, BeanFactoryAware, ApplicationContextAware (and others) provide callbacks for beans to obtain framework infrastructure references."],"distractorIds":["spring/bean_lifecycle#76b32b622bb3","spring/bean_lifecycle#c95651f8d9b3","spring/bean_lifecycle#556cad6354e3"],"concepts":["objectprovider","purpose","lazy","managed","beans","optional"],"correctMd":"`ObjectProvider<T>` provides lazy, optional, and multi-bean access to Spring-managed beans.","answerFullMd":"`ObjectProvider<T>` provides lazy, optional, and multi-bean access to Spring-managed beans. Key methods: `getObject()` (throws if not found), `getIfAvailable()` (returns null if not found), `getIfUnique()` (returns null if multiple beans), `stream()` (iterate all matching beans). It is more explicit than `@Autowired(required=false)` and avoids the need for `Optional<T>` workarounds. Primarily used for optional dependencies and prototype beans that should be retrieved on each call.","distractorsMd":["Singleton: one instance per ApplicationContext, created eagerly at startup (unless `@Lazy`), managed for its full lifecycle including destruction.","During `AbstractApplicationContext.close()`, Spring calls `destroySingletons()` on the `BeanFactory`.","`BeanNameAware`, `BeanFactoryAware`, `ApplicationContextAware` (and others) provide callbacks for beans to obtain framework infrastructure references."]},{"id":"spring/bean_lifecycle#1b13a1d42140","section":"spring","module":"spring/bean_lifecycle","moduleName":"bean lifecycle","sourceFile":"README.md","difficulty":"advanced","question":"What is the @Lazy annotation and when does it apply?","answerFull":"@Lazy on a @Component or @Bean delays instantiation until first requested. @Lazy on an @Autowired injection point creates a CGLIB proxy at injection time but delays actual bean instantiation until the proxy is first invoked. spring.main.lazy-initialization=true makes all beans lazy globally. Lazy init reduces startup time but moves initialization errors from startup to first-use, which is undesirable in production. Use selectively for beans with expensive initialization that may not be needed (e.g., rarely-used admin endpoints).","correct":"@Lazy on a @Component or @Bean delays instantiation until first requested.","distractors":["Singleton: one instance per ApplicationContext, created eagerly at startup (unless @Lazy), managed for its full lifecycle including destruction.","ObjectProvider<T> provides lazy, optional, and multi-bean access to Spring-managed beans.","The prototype bean is injected exactly once at singleton initialization time."],"distractorIds":["spring/bean_lifecycle#76b32b622bb3","spring/bean_lifecycle#3aea5fbd948e","spring/bean_lifecycle#04bac10006a6"],"concepts":["delays","instantiation","requested","annotation","apply","lazy"],"correctMd":"`@Lazy` on a `@Component` or `@Bean` delays instantiation until first requested.","answerFullMd":"`@Lazy` on a `@Component` or `@Bean` delays instantiation until first requested. `@Lazy` on an `@Autowired` injection point creates a CGLIB proxy at injection time but delays actual bean instantiation until the proxy is first invoked. `spring.main.lazy-initialization=true` makes all beans lazy globally. Lazy init reduces startup time but moves initialization errors from startup to first-use, which is undesirable in production. Use selectively for beans with expensive initialization that may not be needed (e.g., rarely-used admin endpoints).","distractorsMd":["Singleton: one instance per ApplicationContext, created eagerly at startup (unless `@Lazy`), managed for its full lifecycle including destruction.","`ObjectProvider<T>` provides lazy, optional, and multi-bean access to Spring-managed beans.",null]},{"id":"spring/dependency_injection#02ed8b089288","section":"spring","module":"spring/dependency_injection","moduleName":"dependency injection","sourceFile":"README.md","difficulty":"core","question":"What are the three types of dependency injection in Spring and which is preferred?","answerFull":"Constructor injection (via constructor parameters), setter injection (via @Autowired setter methods), and field injection (via @Autowired on instance fields). Constructor injection is strongly preferred because it makes dependencies explicit (visible in method signature), allows final fields (immutability), enables testing without Spring (just call new), and provides fail-fast behavior when a required dependency is missing. Field injection is an anti-pattern despite its convenience.","correct":"Constructor injection (via constructor parameters), setter injection (via @Autowired setter methods), and field injection (via @Autowired on instance fields).","distractors":["Declare the injection point as List<T> to get all matching beans in @Order order, or as Map<String, T> to get a map of bean name to bean instance.","Not directly via @Autowired.","ObjectProvider<T> (Spring 4.3) is an injection point that provides lazy, on-demand access to a bean with safe handling of optional and multiple-bean scenarios."],"distractorIds":["spring/dependency_injection#0307e3410443","spring/dependency_injection#9e0b878be8ef","spring/dependency_injection#d32ef04a08a2"],"concepts":["setter","autowired","preferred","constructor","injection","dependency"],"correctMd":"Constructor injection (via constructor parameters), setter injection (via `@Autowired` setter methods), and field injection (via `@Autowired` on instance fields).","answerFullMd":"Constructor injection (via constructor parameters), setter injection (via `@Autowired` setter methods), and field injection (via `@Autowired` on instance fields). Constructor injection is strongly preferred because it makes dependencies explicit (visible in method signature), allows `final` fields (immutability), enables testing without Spring (just call `new`), and provides fail-fast behavior when a required dependency is missing. Field injection is an anti-pattern despite its convenience.","distractorsMd":["Declare the injection point as `List<T>` to get all matching beans in `@Order` order, or as `Map<String, T>` to get a map of bean name to bean instance.","Not directly via `@Autowired`.","`ObjectProvider<T>` (Spring 4.3) is an injection point that provides lazy, on-demand access to a bean with safe handling of optional and multiple-bean scenarios."]},{"id":"spring/dependency_injection#52ac565afe30","section":"spring","module":"spring/dependency_injection","moduleName":"dependency injection","sourceFile":"README.md","difficulty":"core","question":"How does Spring resolve ambiguity when multiple beans of the same type exist?","answerFull":"Spring first checks for @Primary on one of the candidates. If that fails or multiple have @Primary, it checks for a @Qualifier annotation on the injection point. If still ambiguous, it tries matching by field/parameter name against bean names. If all resolution strategies fail, NoUniqueBeanDefinitionException is thrown at startup. Best practice: use @Primary for the default implementation, @Qualifier for explicit overrides.","correct":"Spring first checks for @Primary on one of the candidates.","distractors":["@Autowired (Spring) resolves by type first, then by name if ambiguous; supports required=false.","With required=true (default), Spring throws NoSuchBeanDefinitionException at startup during finishBeanFactoryInitialization, immediately failing the context refresh.","ObjectProvider<T> (Spring 4.3) is an injection point that provides lazy, on-demand access to a bean with safe handling of optional and multiple-bean scenarios."],"distractorIds":["spring/dependency_injection#32cff11c64b1","spring/dependency_injection#fb9f57c009d5","spring/dependency_injection#d32ef04a08a2"],"concepts":["resolve","ambiguity","candidates","exist","beans","checks"],"correctMd":"Spring first checks for `@Primary` on one of the candidates.","answerFullMd":"Spring first checks for `@Primary` on one of the candidates. If that fails or multiple have `@Primary`, it checks for a `@Qualifier` annotation on the injection point. If still ambiguous, it tries matching by field/parameter name against bean names. If all resolution strategies fail, `NoUniqueBeanDefinitionException` is thrown at startup. Best practice: use `@Primary` for the default implementation, `@Qualifier` for explicit overrides.","distractorsMd":["`@Autowired` (Spring) resolves by type first, then by name if ambiguous; supports `required=false`.","With `required=true` (default), Spring throws `NoSuchBeanDefinitionException` at startup during `finishBeanFactoryInitialization`, immediately failing the context refresh.","`ObjectProvider<T>` (Spring 4.3) is an injection point that provides lazy, on-demand access to a bean with safe handling of optional and multiple-bean scenarios."]},{"id":"spring/dependency_injection#32cff11c64b1","section":"spring","module":"spring/dependency_injection","moduleName":"dependency injection","sourceFile":"README.md","difficulty":"core","question":"What is the difference between @Autowired, @Resource, and @Inject?","answerFull":"@Autowired (Spring) resolves by type first, then by name if ambiguous; supports required=false. @Resource (JSR-250) resolves by name first, then by type; no required attribute. @Inject (JSR-330) resolves by type like @Autowired but without required=false support. For maximum portability, use @Inject (JSR-330). For Spring-specific features like required=false, use @Autowired. For name-based lookup, use @Resource.","correct":"@Autowired (Spring) resolves by type first, then by name if ambiguous; supports required=false.","distractors":["With required=true (default), Spring throws NoSuchBeanDefinitionException at startup during finishBeanFactoryInitialization, immediately failing the context refresh.","Constructor injection (via constructor parameters), setter injection (via @Autowired setter methods), and field injection (via @Autowired on instance fields).","Declare the injection point as List<T> to get all matching beans in @Order order, or as Map<String, T> to get a map of bean name to bean instance."],"distractorIds":["spring/dependency_injection#fb9f57c009d5","spring/dependency_injection#02ed8b089288","spring/dependency_injection#0307e3410443"],"concepts":["autowired","difference","ambiguous","inject","resolves","supports"],"correctMd":"`@Autowired` (Spring) resolves by type first, then by name if ambiguous; supports `required=false`.","answerFullMd":"`@Autowired` (Spring) resolves by type first, then by name if ambiguous; supports `required=false`. `@Resource` (JSR-250) resolves by name first, then by type; no `required` attribute. `@Inject` (JSR-330) resolves by type like `@Autowired` but without `required=false` support. For maximum portability, use `@Inject` (JSR-330). For Spring-specific features like `required=false`, use `@Autowired`. For name-based lookup, use `@Resource`.","distractorsMd":["With `required=true` (default), Spring throws `NoSuchBeanDefinitionException` at startup during `finishBeanFactoryInitialization`, immediately failing the context refresh.","Constructor injection (via constructor parameters), setter injection (via `@Autowired` setter methods), and field injection (via `@Autowired` on instance fields).","Declare the injection point as `List<T>` to get all matching beans in `@Order` order, or as `Map<String, T>` to get a map of bean name to bean instance."]},{"id":"spring/dependency_injection#fb9f57c009d5","section":"spring","module":"spring/dependency_injection","moduleName":"dependency injection","sourceFile":"README.md","difficulty":"core","question":"What happens when @Autowired cannot find a required bean?","answerFull":"With required=true (default), Spring throws NoSuchBeanDefinitionException at startup during finishBeanFactoryInitialization, immediately failing the context refresh. With required=false or @Autowired(required=false), the field is left null and the application starts. ObjectProvider<T> is the preferred alternative to required=false because it is explicit about optionality and provides null-safe methods like getIfAvailable().","correct":"With required=true (default), Spring throws NoSuchBeanDefinitionException at startup during finishBeanFactoryInitialization, immediately failing the context refresh.","distractors":["ObjectProvider<T> (Spring 4.3) is an injection point that provides lazy, on-demand access to a bean with safe handling of optional and multiple-bean scenarios.","Not directly via @Autowired.","@Primary is declared on the bean class/method and makes that bean the default whenever type resolution is ambiguous — it is a global preference."],"distractorIds":["spring/dependency_injection#d32ef04a08a2","spring/dependency_injection#9e0b878be8ef","spring/dependency_injection#3418d8c534b2"],"concepts":["finishbeanfactoryinitialization","nosuchbeandefinitionexception","autowired","failing","refresh","happens"],"correctMd":"With `required=true` (default), Spring throws `NoSuchBeanDefinitionException` at startup during `finishBeanFactoryInitialization`, immediately failing the context refresh.","answerFullMd":"With `required=true` (default), Spring throws `NoSuchBeanDefinitionException` at startup during `finishBeanFactoryInitialization`, immediately failing the context refresh. With `required=false` or `@Autowired(required=false)`, the field is left `null` and the application starts. `ObjectProvider<T>` is the preferred alternative to `required=false` because it is explicit about optionality and provides null-safe methods like `getIfAvailable()`.","distractorsMd":["`ObjectProvider<T>` (Spring 4.3) is an injection point that provides lazy, on-demand access to a bean with safe handling of optional and multiple-bean scenarios.","Not directly via `@Autowired`.","`@Primary` is declared on the bean class/method and makes that bean the default whenever type resolution is ambiguous — it is a global preference."]},{"id":"spring/dependency_injection#0307e3410443","section":"spring","module":"spring/dependency_injection","moduleName":"dependency injection","sourceFile":"README.md","difficulty":"intermediate","question":"How do you inject all beans of a given type into one injection point?","answerFull":"Declare the injection point as List<T> to get all matching beans in @Order order, or as Map<String, T> to get a map of bean name to bean instance. Spring automatically collects all beans of type T from the context and injects them. This is extremely useful for plugin patterns (strategy pattern), notification channels, validators, or processing pipelines where adding a new bean automatically plugs it in.","correct":"Declare the injection point as List<T> to get all matching beans in @Order order, or as Map<String, T> to get a map of bean name to bean instance.","distractors":["ObjectProvider<T> provides explicit optional injection with null-safe methods (getIfAvailable(), getIfUnique(), stream()).","@Lazy on a bean definition (class or @Bean method) defers instantiation until first access — the proxy is injected at wiring time, but the actual bean is created on first method call.","ObjectProvider<T> (Spring 4.3) is an injection point that provides lazy, on-demand access to a bean with safe handling of optional and multiple-bean scenarios."],"distractorIds":["spring/dependency_injection#c2286b5d6c60","spring/dependency_injection#5fdc6d409284","spring/dependency_injection#d32ef04a08a2"],"concepts":["inject","declare","beans","injection","matching","given"],"correctMd":"Declare the injection point as `List<T>` to get all matching beans in `@Order` order, or as `Map<String, T>` to get a map of bean name to bean instance.","answerFullMd":"Declare the injection point as `List<T>` to get all matching beans in `@Order` order, or as `Map<String, T>` to get a map of bean name to bean instance. Spring automatically collects all beans of type `T` from the context and injects them. This is extremely useful for plugin patterns (strategy pattern), notification channels, validators, or processing pipelines where adding a new bean automatically plugs it in.","distractorsMd":["`ObjectProvider<T>` provides explicit optional injection with null-safe methods (`getIfAvailable()`, `getIfUnique()`, `stream()`).","`@Lazy` on a bean definition (class or `@Bean` method) defers instantiation until first access — the proxy is injected at wiring time, but the actual bean is created on first method call.","`ObjectProvider<T>` (Spring 4.3) is an injection point that provides lazy, on-demand access to a bean with safe handling of optional and multiple-bean scenarios."]},{"id":"spring/dependency_injection#c2286b5d6c60","section":"spring","module":"spring/dependency_injection","moduleName":"dependency injection","sourceFile":"README.md","difficulty":"intermediate","question":"What is ObjectProvider and when is it better than @Autowired(required=false)?","answerFull":"ObjectProvider<T> provides explicit optional injection with null-safe methods (getIfAvailable(), getIfUnique(), stream()). It is better than required=false because it is self-documenting (makes optionality explicit in the code), provides access to multiple beans, and enables lazy initialization (the bean is not requested until getObject() is called). It is also the correct solution for injecting prototype beans into singleton beans without the singleton becoming stale.","correct":"ObjectProvider<T> provides explicit optional injection with null-safe methods (getIfAvailable(), getIfUnique(), stream()).","distractors":["Not directly via @Autowired.","@Autowired (Spring) resolves by type first, then by name if ambiguous; supports required=false.","ObjectProvider<T> (Spring 4.3) is an injection point that provides lazy, on-demand access to a bean with safe handling of optional and multiple-bean scenarios."],"distractorIds":["spring/dependency_injection#9e0b878be8ef","spring/dependency_injection#32cff11c64b1","spring/dependency_injection#d32ef04a08a2"],"concepts":["getifavailable","getifunique","objectprovider","autowired","null","optional"],"correctMd":"`ObjectProvider<T>` provides explicit optional injection with null-safe methods (`getIfAvailable()`, `getIfUnique()`, `stream()`).","answerFullMd":"`ObjectProvider<T>` provides explicit optional injection with null-safe methods (`getIfAvailable()`, `getIfUnique()`, `stream()`). It is better than `required=false` because it is self-documenting (makes optionality explicit in the code), provides access to multiple beans, and enables lazy initialization (the bean is not requested until `getObject()` is called). It is also the correct solution for injecting prototype beans into singleton beans without the singleton becoming stale.","distractorsMd":["Not directly via `@Autowired`.","`@Autowired` (Spring) resolves by type first, then by name if ambiguous; supports `required=false`.","`ObjectProvider<T>` (Spring 4.3) is an injection point that provides lazy, on-demand access to a bean with safe handling of optional and multiple-bean scenarios."]},{"id":"spring/dependency_injection#3418d8c534b2","section":"spring","module":"spring/dependency_injection","moduleName":"dependency injection","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between @Primary and @Qualifier?","answerFull":"@Primary is declared on the bean class/method and makes that bean the default whenever type resolution is ambiguous — it is a global preference. @Qualifier is declared at the injection point and overrides the default for that specific injection — it is a local override. Use @Primary for \"this is the main implementation\"; use @Qualifier to explicitly select a specific non-primary bean at a specific injection point. Both can coexist: @Primary wins when no @Qualifier is specified.","correct":"@Primary is declared on the bean class/method and makes that bean the default whenever type resolution is ambiguous — it is a global preference.","distractors":["Declare the injection point as List<T> to get all matching beans in @Order order, or as Map<String, T> to get a map of bean name to bean instance.","@Lazy on a bean definition (class or @Bean method) defers instantiation until first access — the proxy is injected at wiring time, but the actual bean is created on first method call.","Since Spring 4.3, @Autowired is implicit on a class with a single constructor."],"distractorIds":["spring/dependency_injection#0307e3410443","spring/dependency_injection#5fdc6d409284","spring/dependency_injection#daf7e43a0291"],"concepts":["difference","ambiguous","whenever","preference","declared","resolution"],"correctMd":"`@Primary` is declared on the bean class/method and makes that bean the default whenever type resolution is ambiguous — it is a global preference.","answerFullMd":"`@Primary` is declared on the bean class/method and makes that bean the default whenever type resolution is ambiguous — it is a global preference. `@Qualifier` is declared at the injection point and overrides the default for that specific injection — it is a local override. Use `@Primary` for \"this is the main implementation\"; use `@Qualifier` to explicitly select a specific non-primary bean at a specific injection point. Both can coexist: `@Primary` wins when no `@Qualifier` is specified.","distractorsMd":["Declare the injection point as `List<T>` to get all matching beans in `@Order` order, or as `Map<String, T>` to get a map of bean name to bean instance.","`@Lazy` on a bean definition (class or `@Bean` method) defers instantiation until first access — the proxy is injected at wiring time, but the actual bean is created on first method call.","Since Spring 4.3, `@Autowired` is implicit on a class with a single constructor."]},{"id":"spring/dependency_injection#171ff0b74d3c","section":"spring","module":"spring/dependency_injection","moduleName":"dependency injection","sourceFile":"README.md","difficulty":"intermediate","question":"How does @Value injection work and what are its limitations?","answerFull":"@Value is processed by AutowiredAnnotationBeanPostProcessor using PropertySourcesPlaceholderConfigurer (for ${...} expressions) and ExpressionEvaluator (for #{...} SpEL expressions). Limitations: (1) cannot inject complex bean graphs (only primitives, Strings, and simple collections); (2) if a property is missing without a default, context fails to start; (3) type conversion happens automatically but fails with cryptic errors for malformed values; (4) @Value does not support reloading at runtime (use @ConfigurationProperties with @RefreshScope for that).","correct":"@Value is processed by AutowiredAnnotationBeanPostProcessor using PropertySourcesPlaceholderConfigurer (for ${...} expressions) and ExpressionEvaluator (for #{...} SpEL expressions).","distractors":["With field injection, if the bean is not available and required=false, the field stays null — your code must null-check everywhere.","ObjectProvider<T> provides explicit optional injection with null-safe methods (getIfAvailable(), getIfUnique(), stream()).","Declare the injection point as List<T> to get all matching beans in @Order order, or as Map<String, T> to get a map of bean name to bean instance."],"distractorIds":["spring/dependency_injection#70ac13b33789","spring/dependency_injection#c2286b5d6c60","spring/dependency_injection#0307e3410443"],"concepts":["autowiredannotationbeanpostprocessor","expressionevaluator","propertysourcesplaceholderconfigurer","limitations","spel","expressions"],"correctMd":"`@Value` is processed by `AutowiredAnnotationBeanPostProcessor` using `PropertySourcesPlaceholderConfigurer` (for `${...}` expressions) and `ExpressionEvaluator` (for `#{...}` SpEL expressions).","answerFullMd":"`@Value` is processed by `AutowiredAnnotationBeanPostProcessor` using `PropertySourcesPlaceholderConfigurer` (for `${...}` expressions) and `ExpressionEvaluator` (for `#{...}` SpEL expressions). Limitations: (1) cannot inject complex bean graphs (only primitives, Strings, and simple collections); (2) if a property is missing without a default, context fails to start; (3) type conversion happens automatically but fails with cryptic errors for malformed values; (4) `@Value` does not support reloading at runtime (use `@ConfigurationProperties` with `@RefreshScope` for that).","distractorsMd":["With field injection, if the bean is not available and `required=false`, the field stays `null` — your code must null-check everywhere.","`ObjectProvider<T>` provides explicit optional injection with null-safe methods (`getIfAvailable()`, `getIfUnique()`, `stream()`).","Declare the injection point as `List<T>` to get all matching beans in `@Order` order, or as `Map<String, T>` to get a map of bean name to bean instance."]},{"id":"spring/dependency_injection#9e0b878be8ef","section":"spring","module":"spring/dependency_injection","moduleName":"dependency injection","sourceFile":"README.md","difficulty":"intermediate","question":"Can you inject a Spring bean into a non-Spring-managed object?","answerFull":"Not directly via @Autowired. Options: (1) use @Configurable with AspectJ load-time weaving (complex setup); (2) call SpringBeanAutowiringSupport.processInjectionBasedOnCurrentContext(this) in the object's constructor (legacy); (3) redesign so the non-Spring object is created by a Spring-managed factory; (4) use ApplicationContext.getBean() (service locator, anti-pattern). The cleanest solution is usually option 3 — make the factory a Spring bean and have it create the instances.","correct":"Not directly via @Autowired.","distractors":["@Autowired (Spring) resolves by type first, then by name if ambiguous; supports required=false.","With field injection, if the bean is not available and required=false, the field stays null — your code must null-check everywhere.","@Primary is declared on the bean class/method and makes that bean the default whenever type resolution is ambiguous — it is a global preference."],"distractorIds":["spring/dependency_injection#32cff11c64b1","spring/dependency_injection#70ac13b33789","spring/dependency_injection#3418d8c534b2"],"concepts":["autowired","inject","managed","bean","directly","non"],"correctMd":"Not directly via `@Autowired`.","answerFullMd":"Not directly via `@Autowired`. Options: (1) use `@Configurable` with AspectJ load-time weaving (complex setup); (2) call `SpringBeanAutowiringSupport.processInjectionBasedOnCurrentContext(this)` in the object's constructor (legacy); (3) redesign so the non-Spring object is created by a Spring-managed factory; (4) use `ApplicationContext.getBean()` (service locator, anti-pattern). The cleanest solution is usually option 3 — make the factory a Spring bean and have it create the instances.","distractorsMd":["`@Autowired` (Spring) resolves by type first, then by name if ambiguous; supports `required=false`.","With field injection, if the bean is not available and `required=false`, the field stays `null` — your code must null-check everywhere.","`@Primary` is declared on the bean class/method and makes that bean the default whenever type resolution is ambiguous — it is a global preference."]},{"id":"spring/dependency_injection#daf7e43a0291","section":"spring","module":"spring/dependency_injection","moduleName":"dependency injection","sourceFile":"README.md","difficulty":"advanced","question":"What is the effect of @Autowired on a constructor with multiple parameters?","answerFull":"Since Spring 4.3, @Autowired is implicit on a class with a single constructor. With multiple constructors, @Autowired must be placed explicitly on the preferred constructor. If no constructor is annotated and none has no-arg form, Spring throws NoSuchMethodException. Each constructor parameter is resolved as a separate injection point. This is why constructor injection is explicit about all required dependencies — missing one fails with a clear error at context startup.","correct":"Since Spring 4.3, @Autowired is implicit on a class with a single constructor.","distractors":["ObjectProvider<T> (Spring 4.3) is an injection point that provides lazy, on-demand access to a bean with safe handling of optional and multiple-bean scenarios.","Spring first checks for @Primary on one of the candidates.","@Lazy on a bean definition (class or @Bean method) defers instantiation until first access — the proxy is injected at wiring time, but the actual bean is created on first method call."],"distractorIds":["spring/dependency_injection#d32ef04a08a2","spring/dependency_injection#52ac565afe30","spring/dependency_injection#5fdc6d409284"],"concepts":["autowired","implicit","constructor","since","effect","parameters"],"correctMd":"Since Spring 4.3, `@Autowired` is implicit on a class with a single constructor.","answerFullMd":"Since Spring 4.3, `@Autowired` is implicit on a class with a single constructor. With multiple constructors, `@Autowired` must be placed explicitly on the preferred constructor. If no constructor is annotated and none has no-arg form, Spring throws `NoSuchMethodException`. Each constructor parameter is resolved as a separate injection point. This is why constructor injection is explicit about all required dependencies — missing one fails with a clear error at context startup.","distractorsMd":["`ObjectProvider<T>` (Spring 4.3) is an injection point that provides lazy, on-demand access to a bean with safe handling of optional and multiple-bean scenarios.","Spring first checks for `@Primary` on one of the candidates.","`@Lazy` on a bean definition (class or `@Bean` method) defers instantiation until first access — the proxy is injected at wiring time, but the actual bean is created on first method call."]},{"id":"spring/dependency_injection#df0851cb30d0","section":"spring","module":"spring/dependency_injection","moduleName":"dependency injection","sourceFile":"README.md","difficulty":"advanced","question":"How does Spring handle circular dependencies with constructor injection?","answerFull":"It throws BeanCurrentlyInCreationException immediately at startup. Spring tracks in-progress beans in singletonsCurrentlyInCreation. When bean A's constructor requires B, and B's constructor requires A, Spring detects the cycle and fails. There is no way to resolve constructor circular dependencies in Spring — the design must be changed. Common fixes: extract a third bean with shared logic, use events, or change one injection to setter/field injection (which Spring can resolve via early references, though this is still a design smell).","correct":"It throws BeanCurrentlyInCreationException immediately at startup.","distractors":["ObjectProvider<T> provides explicit optional injection with null-safe methods (getIfAvailable(), getIfUnique(), stream()).","Since Spring 4.3, @Autowired is implicit on a class with a single constructor.","With required=true (default), Spring throws NoSuchBeanDefinitionException at startup during finishBeanFactoryInitialization, immediately failing the context refresh."],"distractorIds":["spring/dependency_injection#c2286b5d6c60","spring/dependency_injection#daf7e43a0291","spring/dependency_injection#fb9f57c009d5"],"concepts":["beancurrentlyincreationexception","circular","handle","constructor","dependencies","startup"],"correctMd":"It throws `BeanCurrentlyInCreationException` immediately at startup.","answerFullMd":"It throws `BeanCurrentlyInCreationException` immediately at startup. Spring tracks in-progress beans in `singletonsCurrentlyInCreation`. When bean A's constructor requires B, and B's constructor requires A, Spring detects the cycle and fails. There is no way to resolve constructor circular dependencies in Spring — the design must be changed. Common fixes: extract a third bean with shared logic, use events, or change one injection to setter/field injection (which Spring can resolve via early references, though this is still a design smell).","distractorsMd":["`ObjectProvider<T>` provides explicit optional injection with null-safe methods (`getIfAvailable()`, `getIfUnique()`, `stream()`).","Since Spring 4.3, `@Autowired` is implicit on a class with a single constructor.","With `required=true` (default), Spring throws `NoSuchBeanDefinitionException` at startup during `finishBeanFactoryInitialization`, immediately failing the context refresh."]},{"id":"spring/dependency_injection#70ac13b33789","section":"spring","module":"spring/dependency_injection","moduleName":"dependency injection","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between @Autowired on a field vs @Autowired on a constructor when a dependency is null?","answerFull":"With field injection, if the bean is not available and required=false, the field stays null — your code must null-check everywhere. With constructor injection, there is no required=false (the parameter is either resolved or startup fails). This makes constructor injection safer: a missing required dependency causes an informative startup failure rather than a silent null that causes a NullPointerException during a production request at 2am.","correct":"With field injection, if the bean is not available and required=false, the field stays null — your code must null-check everywhere.","distractors":["Declare the injection point as List<T> to get all matching beans in @Order order, or as Map<String, T> to get a map of bean name to bean instance.","Since Spring 4.3, @Autowired is implicit on a class with a single constructor.","Not directly via @Autowired."],"distractorIds":["spring/dependency_injection#0307e3410443","spring/dependency_injection#daf7e43a0291","spring/dependency_injection#9e0b878be8ef"],"concepts":["everywhere","autowired","difference","stays","null","constructor"],"correctMd":"With field injection, if the bean is not available and `required=false`, the field stays `null` — your code must null-check everywhere.","answerFullMd":"With field injection, if the bean is not available and `required=false`, the field stays `null` — your code must null-check everywhere. With constructor injection, there is no `required=false` (the parameter is either resolved or startup fails). This makes constructor injection safer: a missing required dependency causes an informative startup failure rather than a silent `null` that causes a `NullPointerException` during a production request at 2am.","distractorsMd":["Declare the injection point as `List<T>` to get all matching beans in `@Order` order, or as `Map<String, T>` to get a map of bean name to bean instance.","Since Spring 4.3, `@Autowired` is implicit on a class with a single constructor.","Not directly via `@Autowired`."]},{"id":"spring/dependency_injection#d32ef04a08a2","section":"spring","module":"spring/dependency_injection","moduleName":"dependency injection","sourceFile":"README.md","difficulty":"advanced","question":"What is ObjectProvider<T> and when should you use it over @Autowired(required=false)?","answerFull":"ObjectProvider<T> (Spring 4.3) is an injection point that provides lazy, on-demand access to a bean with safe handling of optional and multiple-bean scenarios. Unlike @Autowired(required=false) which silently injects null when the bean is absent, ObjectProvider exposes getIfAvailable(), getIfUnique(), and stream(). Use cases: (1) Optional dependency — provider.getIfAvailable() returns null cleanly without NullPointerException surprises. (2) Deferred resolution — provider.getObject() resolves the bean lazily on first call, avoiding circular dependency issues. (3) Multiple candidates — provider.stream() iterates all matching beans, ordered by @Order. ObjectProvider is preferred over @Autowired(required=false) Optional<T> because it works for both single and multiple beans and gives the injection point a name that is visible in IDE warnings and Spring's actuator beans report.","correct":"ObjectProvider<T> (Spring 4.3) is an injection point that provides lazy, on-demand access to a bean with safe handling of optional and multiple-bean scenarios.","distractors":["With required=true (default), Spring throws NoSuchBeanDefinitionException at startup during finishBeanFactoryInitialization, immediately failing the context refresh.","@Autowired (Spring) resolves by type first, then by name if ambiguous; supports required=false.","Constructor injection (via constructor parameters), setter injection (via @Autowired setter methods), and field injection (via @Autowired on instance fields)."],"distractorIds":["spring/dependency_injection#fb9f57c009d5","spring/dependency_injection#32cff11c64b1","spring/dependency_injection#02ed8b089288"],"concepts":["objectprovider","autowired","scenarios","demand","handling","lazy"],"questionMd":"What is `ObjectProvider<T>` and when should you use it over `@Autowired(required=false)`?","correctMd":"`ObjectProvider<T>` (Spring 4.3) is an injection point that provides lazy, on-demand access to a bean with safe handling of optional and multiple-bean scenarios.","answerFullMd":"`ObjectProvider<T>` (Spring 4.3) is an injection point that provides lazy, on-demand access to a bean with safe handling of optional and multiple-bean scenarios. Unlike `@Autowired(required=false)` which silently injects null when the bean is absent, `ObjectProvider` exposes `getIfAvailable()`, `getIfUnique()`, and `stream()`. Use cases: (1) **Optional dependency** — `provider.getIfAvailable()` returns null cleanly without `NullPointerException` surprises. (2) **Deferred resolution** — `provider.getObject()` resolves the bean lazily on first call, avoiding circular dependency issues. (3) **Multiple candidates** — `provider.stream()` iterates all matching beans, ordered by `@Order`. `ObjectProvider` is preferred over `@Autowired(required=false) Optional<T>` because it works for both single and multiple beans and gives the injection point a name that is visible in IDE warnings and Spring's actuator beans report.","distractorsMd":["With `required=true` (default), Spring throws `NoSuchBeanDefinitionException` at startup during `finishBeanFactoryInitialization`, immediately failing the context refresh.","`@Autowired` (Spring) resolves by type first, then by name if ambiguous; supports `required=false`.","Constructor injection (via constructor parameters), setter injection (via `@Autowired` setter methods), and field injection (via `@Autowired` on instance fields)."]},{"id":"spring/dependency_injection#5fdc6d409284","section":"spring","module":"spring/dependency_injection","moduleName":"dependency injection","sourceFile":"README.md","difficulty":"advanced","question":"What is @Lazy and how does it interact with circular dependencies?","answerFull":"@Lazy on a bean definition (class or @Bean method) defers instantiation until first access — the proxy is injected at wiring time, but the actual bean is created on first method call. On an injection point (@Autowired @Lazy), Spring injects a lazy-resolution proxy instead of the real bean. This can break circular dependency cycles: if A requires B (constructor) and B requires A (constructor), add @Lazy to one constructor parameter. Spring injects a proxy for A into B's constructor — B is fully created, then A's constructor completes using the real B, then the proxy for A in B's dependency resolves to the real A on first call. Caution: @Lazy on a class means it is not initialized during application startup, so startup-time validation (e.g., configuration binding errors) won't be caught until the first access. Use @Lazy only for genuinely expensive optional beans and circular dependency resolution of last resort.","correct":"@Lazy on a bean definition (class or @Bean method) defers instantiation until first access — the proxy is injected at wiring time, but the actual bean is created on first method call.","distractors":["ObjectProvider<T> (Spring 4.3) is an injection point that provides lazy, on-demand access to a bean with safe handling of optional and multiple-bean scenarios.","@Primary is declared on the bean class/method and makes that bean the default whenever type resolution is ambiguous — it is a global preference.","With field injection, if the bean is not available and required=false, the field stays null — your code must null-check everywhere."],"distractorIds":["spring/dependency_injection#d32ef04a08a2","spring/dependency_injection#3418d8c534b2","spring/dependency_injection#70ac13b33789"],"concepts":["wiring","defers","interact","instantiation","circular","definition"],"questionMd":"What is `@Lazy` and how does it interact with circular dependencies?","correctMd":"`@Lazy` on a bean definition (class or `@Bean` method) defers instantiation until first access — the proxy is injected at wiring time, but the actual bean is created on first method call.","answerFullMd":"`@Lazy` on a bean definition (class or `@Bean` method) defers instantiation until first access — the proxy is injected at wiring time, but the actual bean is created on first method call. On an injection point (`@Autowired @Lazy`), Spring injects a lazy-resolution proxy instead of the real bean. This can break circular dependency cycles: if A requires B (constructor) and B requires A (constructor), add `@Lazy` to one constructor parameter. Spring injects a proxy for A into B's constructor — B is fully created, then A's constructor completes using the real B, then the proxy for A in B's dependency resolves to the real A on first call. Caution: `@Lazy` on a class means it is not initialized during application startup, so startup-time validation (e.g., configuration binding errors) won't be caught until the first access. Use `@Lazy` only for genuinely expensive optional beans and circular dependency resolution of last resort.","distractorsMd":["`ObjectProvider<T>` (Spring 4.3) is an injection point that provides lazy, on-demand access to a bean with safe handling of optional and multiple-bean scenarios.","`@Primary` is declared on the bean class/method and makes that bean the default whenever type resolution is ambiguous — it is a global preference.","With field injection, if the bean is not available and `required=false`, the field stays `null` — your code must null-check everywhere."]},{"id":"spring/filters_and_interceptors#9454193637cb","section":"spring","module":"spring/filters_and_interceptors","moduleName":"filters and interceptors","sourceFile":"README.md","difficulty":"core","question":"What is the difference between a Servlet Filter and a Spring HandlerInterceptor?","answerFull":"A Filter operates at the servlet container level, before DispatcherServlet processes the request, and can intercept any HTTP request regardless of whether Spring handles it. A HandlerInterceptor operates inside DispatcherServlet, after Spring has resolved the target handler, giving access to HandlerMethod and its annotations. Filters use the doFilter(request, response, chain) method; interceptors use three hooks: preHandle, postHandle, and afterCompletion. Use filters for authentication and CORS; use interceptors when you need controller-level metadata.","correct":"A Filter operates at the servlet container level, before DispatcherServlet processes the request, and can intercept any HTTP request regardless of whether Spring handles it.","distractors":["Spring Security's SecurityFilterChain is itself a Filter registered in the Servlet filter chain at SecurityProperties.DEFAULT_FILTER_ORDER (-100 by default).","Returning false from preHandle halts the handler chain — DispatcherServlet stops processing and returns the response as-is (typically empty 200 if you haven't written anything, or whatever you wrote in preHandle).","When a controller starts async processing (DeferredResult, Callable, @Async with SseEmitter), the servlet container thread that handled the request is released before the response is committed."],"distractorIds":["spring/filters_and_interceptors#081be0da86e9","spring/filters_and_interceptors#c63320c586e5","spring/filters_and_interceptors#ea3131aa6504"],"concepts":["handlerinterceptor","intercept","dispatcherservlet","difference","operates","servlet"],"answerFullMd":"A Filter operates at the servlet container level, before DispatcherServlet processes the request, and can intercept any HTTP request regardless of whether Spring handles it. A HandlerInterceptor operates inside DispatcherServlet, after Spring has resolved the target handler, giving access to `HandlerMethod` and its annotations. Filters use the `doFilter(request, response, chain)` method; interceptors use three hooks: `preHandle`, `postHandle`, and `afterCompletion`. Use filters for authentication and CORS; use interceptors when you need controller-level metadata.","distractorsMd":["Spring Security's `SecurityFilterChain` is itself a `Filter` registered in the Servlet filter chain at `SecurityProperties.DEFAULT_FILTER_ORDER` (-100 by default).","Returning `false` from `preHandle` halts the handler chain — DispatcherServlet stops processing and returns the response as-is (typically empty 200 if you haven't written anything, or whatever you wrote in `preHandle`).","When a controller starts async processing (`DeferredResult`, `Callable`, `@Async` with `SseEmitter`), the servlet container thread that handled the request is released before the response is committed."]},{"id":"spring/filters_and_interceptors#4cd3e4d3019d","section":"spring","module":"spring/filters_and_interceptors","moduleName":"filters and interceptors","sourceFile":"README.md","difficulty":"core","question":"Why does Spring Security use filters instead of interceptors?","answerFull":"Spring Security must intercept requests before any handler resolution happens — including before any Spring MVC route is matched. If an unauthenticated request reaches DispatcherServlet, Spring MVC might expose information through error handling before the interceptor even fires. Additionally, filters apply uniformly to all requests (static files, actuator, error pages), while interceptors only apply to DispatcherServlet-mapped requests. FilterChainProxy is a single filter that manages a complete security filter chain independently of Spring MVC.","correct":"Spring Security must intercept requests before any handler resolution happens — including before any Spring MVC route is matched.","distractors":["OncePerRequestFilter (Spring's abstract class) guarantees the filter executes exactly once per request, even when the request is forwarded internally via RequestDispatcher.forward().","DelegatingFilterProxy is a javax.servlet.Filter registered with the servlet container that looks up a Spring bean by name from the ApplicationContext and delegates all doFilter() calls to it.","Returning false from preHandle halts the handler chain — DispatcherServlet stops processing and returns the response as-is (typically empty 200 if you haven't written anything, or whatever you wrote in preHandle)."],"distractorIds":["spring/filters_and_interceptors#ae5a27a78993","spring/filters_and_interceptors#668a5e046ab3","spring/filters_and_interceptors#c63320c586e5"],"concepts":["interceptors","matched","intercept","mvc","filters","happens"],"answerFullMd":"Spring Security must intercept requests before any handler resolution happens — including before any Spring MVC route is matched. If an unauthenticated request reaches DispatcherServlet, Spring MVC might expose information through error handling before the interceptor even fires. Additionally, filters apply uniformly to all requests (static files, actuator, error pages), while interceptors only apply to DispatcherServlet-mapped requests. `FilterChainProxy` is a single filter that manages a complete security filter chain independently of Spring MVC.","distractorsMd":["`OncePerRequestFilter` (Spring's abstract class) guarantees the filter executes exactly once per request, even when the request is forwarded internally via `RequestDispatcher.forward()`.","`DelegatingFilterProxy` is a `javax.servlet.Filter` registered with the servlet container that looks up a Spring bean by name from the `ApplicationContext` and delegates all `doFilter()` calls to it.","Returning `false` from `preHandle` halts the handler chain — DispatcherServlet stops processing and returns the response as-is (typically empty 200 if you haven't written anything, or whatever you wrote in `preHandle`)."]},{"id":"spring/filters_and_interceptors#fa8aac28da47","section":"spring","module":"spring/filters_and_interceptors","moduleName":"filters and interceptors","sourceFile":"README.md","difficulty":"core","question":"When is postHandle NOT called?","answerFull":"postHandle is not called when the handler method throws an exception. Spring MVC skips postHandle and jumps directly to afterCompletion. This means any cleanup or resource release that relies on postHandle will be silently skipped on error paths. Always use afterCompletion for cleanup code, checking the Exception ex parameter for null to detect whether the request succeeded.","correct":"postHandle is not called when the handler method throws an exception.","distractors":["Returning false from preHandle halts the handler chain — DispatcherServlet stops processing and returns the response as-is (typically empty 200 if you haven't written anything, or whatever you wrote in preHandle).","Spring Security must intercept requests before any handler resolution happens — including before any Spring MVC route is matched.","A Filter operates at the servlet container level, before DispatcherServlet processes the request, and can intercept any HTTP request regardless of whether Spring handles it."],"distractorIds":["spring/filters_and_interceptors#c63320c586e5","spring/filters_and_interceptors#4cd3e4d3019d","spring/filters_and_interceptors#9454193637cb"],"concepts":["posthandle","throws","called","handler","exception","method"],"correctMd":"`postHandle` is not called when the handler method throws an exception.","answerFullMd":"`postHandle` is not called when the handler method throws an exception. Spring MVC skips `postHandle` and jumps directly to `afterCompletion`. This means any cleanup or resource release that relies on `postHandle` will be silently skipped on error paths. Always use `afterCompletion` for cleanup code, checking the `Exception ex` parameter for null to detect whether the request succeeded.","distractorsMd":["Returning `false` from `preHandle` halts the handler chain — DispatcherServlet stops processing and returns the response as-is (typically empty 200 if you haven't written anything, or whatever you wrote in `preHandle`).",null,null]},{"id":"spring/filters_and_interceptors#74cb444439e7","section":"spring","module":"spring/filters_and_interceptors","moduleName":"filters and interceptors","sourceFile":"README.md","difficulty":"intermediate","question":"How do you read the request body in a filter without breaking @RequestBody deserialization?","answerFull":"Wrap the request in a ContentCachingRequestWrapper and pass the wrapper to chain.doFilter(). The wrapper intercepts reads by downstream components (including Jackson's HttpMessageConverter) and caches the bytes. After chain.doFilter() returns, call wrapper.getContentAsByteArray() to read the cached body. The key insight is that the body is available for inspection only after the chain completes, not before, because the wrapper caches bytes as they are read — not upfront. If you need the body before the chain executes, you must read the original stream yourself and create a new HttpServletRequestWrapper that replays those bytes.","correct":"Wrap the request in a ContentCachingRequestWrapper and pass the wrapper to chain.doFilter().","distractors":["A Filter operates at the servlet container level, before DispatcherServlet processes the request, and can intercept any HTTP request regardless of whether Spring handles it.","The client receives an empty response body.","Returning false from preHandle halts the handler chain — DispatcherServlet stops processing and returns the response as-is (typically empty 200 if you haven't written anything, or whatever you wrote in preHandle)."],"distractorIds":["spring/filters_and_interceptors#9454193637cb","spring/filters_and_interceptors#185d986c0900","spring/filters_and_interceptors#c63320c586e5"],"concepts":["contentcachingrequestwrapper","dofilter","requestbody","deserialization","breaking","wrapper"],"correctMd":"Wrap the request in a `ContentCachingRequestWrapper` and pass the wrapper to `chain.doFilter()`.","answerFullMd":"Wrap the request in a `ContentCachingRequestWrapper` and pass the wrapper to `chain.doFilter()`. The wrapper intercepts reads by downstream components (including Jackson's `HttpMessageConverter`) and caches the bytes. After `chain.doFilter()` returns, call `wrapper.getContentAsByteArray()` to read the cached body. The key insight is that the body is available for inspection only after the chain completes, not before, because the wrapper caches bytes as they are read — not upfront. If you need the body before the chain executes, you must read the original stream yourself and create a new `HttpServletRequestWrapper` that replays those bytes.","distractorsMd":[null,null,"Returning `false` from `preHandle` halts the handler chain — DispatcherServlet stops processing and returns the response as-is (typically empty 200 if you haven't written anything, or whatever you wrote in `preHandle`)."]},{"id":"spring/filters_and_interceptors#185d986c0900","section":"spring","module":"spring/filters_and_interceptors","moduleName":"filters and interceptors","sourceFile":"README.md","difficulty":"intermediate","question":"What happens if you call chain.doFilter() with a ContentCachingResponseWrapper but forget to call copyBodyToResponse()?","answerFull":"The client receives an empty response body. ContentCachingResponseWrapper intercepts writes to the response output stream and stores them in an internal buffer instead of writing them to the actual response. copyBodyToResponse() flushes that buffer to the real response. This is a silent bug — the status code is correct, Content-Type is set, but the body is empty. Load balancer health checks will often still pass (HTTP 200 with no body), masking the bug in monitoring.","correct":"The client receives an empty response body.","distractors":["Wrap the request in a ContentCachingRequestWrapper and pass the wrapper to chain.doFilter().","Spring Security must intercept requests before any handler resolution happens — including before any Spring MVC route is matched.","Spring Security's SecurityFilterChain is itself a Filter registered in the Servlet filter chain at SecurityProperties.DEFAULT_FILTER_ORDER (-100 by default)."],"distractorIds":["spring/filters_and_interceptors#74cb444439e7","spring/filters_and_interceptors#4cd3e4d3019d","spring/filters_and_interceptors#081be0da86e9"],"concepts":["dofilter","forget","empty","happens","body","receives"],"answerFullMd":"The client receives an empty response body. `ContentCachingResponseWrapper` intercepts writes to the response output stream and stores them in an internal buffer instead of writing them to the actual response. `copyBodyToResponse()` flushes that buffer to the real response. This is a silent bug — the status code is correct, Content-Type is set, but the body is empty. Load balancer health checks will often still pass (HTTP 200 with no body), masking the bug in monitoring.","distractorsMd":["Wrap the request in a `ContentCachingRequestWrapper` and pass the wrapper to `chain.doFilter()`.",null,"Spring Security's `SecurityFilterChain` is itself a `Filter` registered in the Servlet filter chain at `SecurityProperties.DEFAULT_FILTER_ORDER` (-100 by default)."]},{"id":"spring/filters_and_interceptors#668a5e046ab3","section":"spring","module":"spring/filters_and_interceptors","moduleName":"filters and interceptors","sourceFile":"README.md","difficulty":"intermediate","question":"How does DelegatingFilterProxy work?","answerFull":"DelegatingFilterProxy is a javax.servlet.Filter registered with the servlet container that looks up a Spring bean by name from the ApplicationContext and delegates all doFilter() calls to it. This bridges the lifecycle gap: the servlet container initializes filters at startup before the Spring context is ready, but DelegatingFilterProxy defers the actual bean lookup to the first request. Spring Security's FilterChainProxy is registered through DelegatingFilterProxy under the bean name \"springSecurityFilterChain\".","correct":"DelegatingFilterProxy is a javax.servlet.Filter registered with the servlet container that looks up a Spring bean by name from the ApplicationContext and delegates all doFilter() calls to it.","distractors":["OncePerRequestFilter (Spring's abstract class) guarantees the filter executes exactly once per request, even when the request is forwarded internally via RequestDispatcher.forward().","Timing in a filter wraps chain.doFilter(request, response) with System.nanoTime() measurements.","Spring Security's SecurityFilterChain is itself a Filter registered in the Servlet filter chain at SecurityProperties.DEFAULT_FILTER_ORDER (-100 by default)."],"distractorIds":["spring/filters_and_interceptors#ae5a27a78993","spring/filters_and_interceptors#917ae67a60ca","spring/filters_and_interceptors#081be0da86e9"],"concepts":["delegatingfilterproxy","dofilter","javax","delegates","looks","servlet"],"correctMd":"`DelegatingFilterProxy` is a `javax.servlet.Filter` registered with the servlet container that looks up a Spring bean by name from the `ApplicationContext` and delegates all `doFilter()` calls to it.","answerFullMd":"`DelegatingFilterProxy` is a `javax.servlet.Filter` registered with the servlet container that looks up a Spring bean by name from the `ApplicationContext` and delegates all `doFilter()` calls to it. This bridges the lifecycle gap: the servlet container initializes filters at startup before the Spring context is ready, but `DelegatingFilterProxy` defers the actual bean lookup to the first request. Spring Security's `FilterChainProxy` is registered through `DelegatingFilterProxy` under the bean name `\"springSecurityFilterChain\"`.","distractorsMd":["`OncePerRequestFilter` (Spring's abstract class) guarantees the filter executes exactly once per request, even when the request is forwarded internally via `RequestDispatcher.forward()`.","Timing in a filter wraps `chain.doFilter(request, response)` with `System.nanoTime()` measurements.","Spring Security's `SecurityFilterChain` is itself a `Filter` registered in the Servlet filter chain at `SecurityProperties.DEFAULT_FILTER_ORDER` (-100 by default)."]},{"id":"spring/filters_and_interceptors#abd94037a32a","section":"spring","module":"spring/filters_and_interceptors","moduleName":"filters and interceptors","sourceFile":"README.md","difficulty":"intermediate","question":"Can an interceptor's preHandle method access path variables?","answerFull":"Not directly from the HttpServletRequest. Path variables are extracted by Spring MVC after routing and stored in a request attribute: request.getAttribute(HandlerMapping.URI_TEMPLATE_VARIABLES_ATTRIBUTE). In preHandle, this attribute is already populated because handler mapping has completed before the interceptor fires. Cast the result to Map<String, String> to access individual path variable values.","correct":"Not directly from the HttpServletRequest.","distractors":["Returning false from preHandle halts the handler chain — DispatcherServlet stops processing and returns the response as-is (typically empty 200 if you haven't written anything, or whatever you wrote in preHandle).","postHandle is not called when the handler method throws an exception.","A Filter operates at the servlet container level, before DispatcherServlet processes the request, and can intercept any HTTP request regardless of whether Spring handles it."],"distractorIds":["spring/filters_and_interceptors#c63320c586e5","spring/filters_and_interceptors#fa8aac28da47","spring/filters_and_interceptors#9454193637cb"],"concepts":["prehandle","httpservletrequest","interceptor","variables","path","directly"],"correctMd":"Not directly from the `HttpServletRequest`.","answerFullMd":"Not directly from the `HttpServletRequest`. Path variables are extracted by Spring MVC after routing and stored in a request attribute: `request.getAttribute(HandlerMapping.URI_TEMPLATE_VARIABLES_ATTRIBUTE)`. In `preHandle`, this attribute is already populated because handler mapping has completed before the interceptor fires. Cast the result to `Map<String, String>` to access individual path variable values.","distractorsMd":["Returning `false` from `preHandle` halts the handler chain — DispatcherServlet stops processing and returns the response as-is (typically empty 200 if you haven't written anything, or whatever you wrote in `preHandle`).","`postHandle` is not called when the handler method throws an exception.",null]},{"id":"spring/filters_and_interceptors#7f168d1343de","section":"spring","module":"spring/filters_and_interceptors","moduleName":"filters and interceptors","sourceFile":"README.md","difficulty":"intermediate","question":"How do you exclude specific URL patterns from a HandlerInterceptor?","answerFull":"Use InterceptorRegistration.excludePathPatterns() in WebMvcConfigurer.addInterceptors(). The exclusion patterns use Ant-style path matching: \"/api/public/\" excludes all sub-paths, \"/api/v1/health\" excludes a specific path. Exclusion is evaluated before addPathPatterns, so excluded paths are always skipped regardless of include patterns. For complex conditional logic, implement the check inside preHandle itself using shouldNotFilter semantics.","correct":"Use InterceptorRegistration.excludePathPatterns() in WebMvcConfigurer.addInterceptors().","distractors":["A Filter operates at the servlet container level, before DispatcherServlet processes the request, and can intercept any HTTP request regardless of whether Spring handles it.","DelegatingFilterProxy is a javax.servlet.Filter registered with the servlet container that looks up a Spring bean by name from the ApplicationContext and delegates all doFilter() calls to it.","Not directly from the HttpServletRequest."],"distractorIds":["spring/filters_and_interceptors#9454193637cb","spring/filters_and_interceptors#668a5e046ab3","spring/filters_and_interceptors#abd94037a32a"],"concepts":["addinterceptors","excludepathpatterns","handlerinterceptor","interceptorregistration","exclude","webmvcconfigurer"],"correctMd":"Use `InterceptorRegistration.excludePathPatterns()` in `WebMvcConfigurer.addInterceptors()`.","answerFullMd":"Use `InterceptorRegistration.excludePathPatterns()` in `WebMvcConfigurer.addInterceptors()`. The exclusion patterns use Ant-style path matching: `\"/api/public/**\"` excludes all sub-paths, `\"/api/v1/health\"` excludes a specific path. Exclusion is evaluated before `addPathPatterns`, so excluded paths are always skipped regardless of include patterns. For complex conditional logic, implement the check inside `preHandle` itself using `shouldNotFilter` semantics.","distractorsMd":[null,"`DelegatingFilterProxy` is a `javax.servlet.Filter` registered with the servlet container that looks up a Spring bean by name from the `ApplicationContext` and delegates all `doFilter()` calls to it.","Not directly from the `HttpServletRequest`."]},{"id":"spring/filters_and_interceptors#c63320c586e5","section":"spring","module":"spring/filters_and_interceptors","moduleName":"filters and interceptors","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between preHandle returning false vs. throwing an exception?","answerFull":"Returning false from preHandle halts the handler chain — DispatcherServlet stops processing and returns the response as-is (typically empty 200 if you haven't written anything, or whatever you wrote in preHandle). You are responsible for writing a meaningful error response before returning false. Throwing an exception triggers Spring MVC's exception handling pipeline (@ExceptionHandler, @ControllerAdvice) and results in a structured error response. For API error responses with proper JSON bodies and status codes, throw an appropriate exception rather than returning false.","correct":"Returning false from preHandle halts the handler chain — DispatcherServlet stops processing and returns the response as-is (typically empty 200 if you haven't written anything, or whatever you wrote in preHandle).","distractors":["Spring Security must intercept requests before any handler resolution happens — including before any Spring MVC route is matched.","postHandle is not called when the handler method throws an exception.","Wrap the request in a ContentCachingRequestWrapper and pass the wrapper to chain.doFilter()."],"distractorIds":["spring/filters_and_interceptors#4cd3e4d3019d","spring/filters_and_interceptors#fa8aac28da47","spring/filters_and_interceptors#74cb444439e7"],"concepts":["halts","prehandle","haven","whatever","wrote","dispatcherservlet"],"correctMd":"Returning `false` from `preHandle` halts the handler chain — DispatcherServlet stops processing and returns the response as-is (typically empty 200 if you haven't written anything, or whatever you wrote in `preHandle`).","answerFullMd":"Returning `false` from `preHandle` halts the handler chain — DispatcherServlet stops processing and returns the response as-is (typically empty 200 if you haven't written anything, or whatever you wrote in `preHandle`). You are responsible for writing a meaningful error response before returning `false`. Throwing an exception triggers Spring MVC's exception handling pipeline (`@ExceptionHandler`, `@ControllerAdvice`) and results in a structured error response. For API error responses with proper JSON bodies and status codes, throw an appropriate exception rather than returning `false`.","distractorsMd":[null,"`postHandle` is not called when the handler method throws an exception.","Wrap the request in a `ContentCachingRequestWrapper` and pass the wrapper to `chain.doFilter()`."]},{"id":"spring/filters_and_interceptors#ae5a27a78993","section":"spring","module":"spring/filters_and_interceptors","moduleName":"filters and interceptors","sourceFile":"README.md","difficulty":"advanced","question":"What is OncePerRequestFilter and why is it preferred over implementing Filter directly?","answerFull":"OncePerRequestFilter (Spring's abstract class) guarantees the filter executes exactly once per request, even when the request is forwarded internally via RequestDispatcher.forward(). A plain javax.servlet.Filter is called on every dispatch — including FORWARD and INCLUDE dispatches within the same request, which means it runs multiple times for a request involving RequestDispatcher. OncePerRequestFilter achieves once-per-request by marking the request with a request attribute on first execution and skipping if the attribute is already set. Override shouldNotFilter(HttpServletRequest) to exclude specific requests (e.g., static resources). Almost all Spring Security filters extend OncePerRequestFilter for this reason.","correct":"OncePerRequestFilter (Spring's abstract class) guarantees the filter executes exactly once per request, even when the request is forwarded internally via RequestDispatcher.forward().","distractors":["When a controller starts async processing (DeferredResult, Callable, @Async with SseEmitter), the servlet container thread that handled the request is released before the response is committed.","Spring Security must intercept requests before any handler resolution happens — including before any Spring MVC route is matched.","DelegatingFilterProxy is a javax.servlet.Filter registered with the servlet container that looks up a Spring bean by name from the ApplicationContext and delegates all doFilter() calls to it."],"distractorIds":["spring/filters_and_interceptors#ea3131aa6504","spring/filters_and_interceptors#4cd3e4d3019d","spring/filters_and_interceptors#668a5e046ab3"],"concepts":["forwarded","requestdispatcher","onceperrequestfilter","implementing","preferred","internally"],"questionMd":"What is `OncePerRequestFilter` and why is it preferred over implementing `Filter` directly?","correctMd":"`OncePerRequestFilter` (Spring's abstract class) guarantees the filter executes exactly once per request, even when the request is forwarded internally via `RequestDispatcher.forward()`.","answerFullMd":"`OncePerRequestFilter` (Spring's abstract class) guarantees the filter executes exactly once per request, even when the request is forwarded internally via `RequestDispatcher.forward()`. A plain `javax.servlet.Filter` is called on every dispatch — including `FORWARD` and `INCLUDE` dispatches within the same request, which means it runs multiple times for a request involving `RequestDispatcher`. `OncePerRequestFilter` achieves once-per-request by marking the request with a request attribute on first execution and skipping if the attribute is already set. Override `shouldNotFilter(HttpServletRequest)` to exclude specific requests (e.g., static resources). Almost all Spring Security filters extend `OncePerRequestFilter` for this reason.","distractorsMd":["When a controller starts async processing (`DeferredResult`, `Callable`, `@Async` with `SseEmitter`), the servlet container thread that handled the request is released before the response is committed.",null,"`DelegatingFilterProxy` is a `javax.servlet.Filter` registered with the servlet container that looks up a Spring bean by name from the `ApplicationContext` and delegates all `doFilter()` calls to it."]},{"id":"spring/filters_and_interceptors#917ae67a60ca","section":"spring","module":"spring/filters_and_interceptors","moduleName":"filters and interceptors","sourceFile":"README.md","difficulty":"advanced","question":"How do you measure and record per-request latency in a Filter without blocking the response body write?","answerFull":"Timing in a filter wraps chain.doFilter(request, response) with System.nanoTime() measurements. However, for streaming responses, the response body write happens after doFilter returns — the afterCompletion timing in an interceptor or a ContentCachingResponseWrapper is needed for body size. For simple request latency (time to send the last byte of headers + body): use a ContentCachingResponseWrapper in the filter to buffer the response, record nanoTime() before and after doFilter, then write the cached response. For real production latency instrumentation, use Micrometer's WebMvcMetricsFilter (auto-configured by Spring Boot Actuator) which uses HttpServletResponse.setStatus() callback via HandlerInterceptor.afterCompletion to accurately attribute time.","correct":"Timing in a filter wraps chain.doFilter(request, response) with System.nanoTime() measurements.","distractors":["The client receives an empty response body.","Wrap the request in a ContentCachingRequestWrapper and pass the wrapper to chain.doFilter().","When a controller starts async processing (DeferredResult, Callable, @Async with SseEmitter), the servlet container thread that handled the request is released before the response is committed."],"distractorIds":["spring/filters_and_interceptors#185d986c0900","spring/filters_and_interceptors#74cb444439e7","spring/filters_and_interceptors#ea3131aa6504"],"concepts":["nanotime","measurements","dofilter","timing","measure","body"],"questionMd":"How do you measure and record per-request latency in a `Filter` without blocking the response body write?","correctMd":"Timing in a filter wraps `chain.doFilter(request, response)` with `System.nanoTime()` measurements.","answerFullMd":"Timing in a filter wraps `chain.doFilter(request, response)` with `System.nanoTime()` measurements. However, for streaming responses, the response body write happens after `doFilter` returns — the `afterCompletion` timing in an interceptor or a `ContentCachingResponseWrapper` is needed for body size. For simple request latency (time to send the last byte of headers + body): use a `ContentCachingResponseWrapper` in the filter to buffer the response, record `nanoTime()` before and after `doFilter`, then write the cached response. For real production latency instrumentation, use Micrometer's `WebMvcMetricsFilter` (auto-configured by Spring Boot Actuator) which uses `HttpServletResponse.setStatus()` callback via `HandlerInterceptor.afterCompletion` to accurately attribute time.","distractorsMd":[null,"Wrap the request in a `ContentCachingRequestWrapper` and pass the wrapper to `chain.doFilter()`.","When a controller starts async processing (`DeferredResult`, `Callable`, `@Async` with `SseEmitter`), the servlet container thread that handled the request is released before the response is committed."]},{"id":"spring/filters_and_interceptors#081be0da86e9","section":"spring","module":"spring/filters_and_interceptors","moduleName":"filters and interceptors","sourceFile":"README.md","difficulty":"advanced","question":"What happens to a Spring Security filter that is ordered too early in the security filter chain?","answerFull":"Spring Security's SecurityFilterChain is itself a Filter registered in the Servlet filter chain at SecurityProperties.DEFAULT_FILTER_ORDER (-100 by default). Within the security chain, filters have a fixed order defined by FilterOrderRegistration. If you insert a custom filter too early (e.g., addFilterBefore(myFilter, UsernamePasswordAuthenticationFilter.class)) when your filter depends on the SecurityContext being populated, the context will not be set yet — SecurityContextHolder.getContext().getAuthentication() returns null. The correct pattern: know the security filter order, place authentication-dependent filters after SecurityContextHolderFilter (which restores the context from the session). Use HttpSecurity.addFilterAfter / addFilterBefore with precise reference filters.","correct":"Spring Security's SecurityFilterChain is itself a Filter registered in the Servlet filter chain at SecurityProperties.DEFAULT_FILTER_ORDER (-100 by default).","distractors":["Returning false from preHandle halts the handler chain — DispatcherServlet stops processing and returns the response as-is (typically empty 200 if you haven't written anything, or whatever you wrote in preHandle).","Wrap the request in a ContentCachingRequestWrapper and pass the wrapper to chain.doFilter().","OncePerRequestFilter (Spring's abstract class) guarantees the filter executes exactly once per request, even when the request is forwarded internally via RequestDispatcher.forward()."],"distractorIds":["spring/filters_and_interceptors#c63320c586e5","spring/filters_and_interceptors#74cb444439e7","spring/filters_and_interceptors#ae5a27a78993"],"concepts":["securityproperties","securityfilterchain","servlet","ordered","registered","happens"],"correctMd":"Spring Security's `SecurityFilterChain` is itself a `Filter` registered in the Servlet filter chain at `SecurityProperties.DEFAULT_FILTER_ORDER` (-100 by default).","answerFullMd":"Spring Security's `SecurityFilterChain` is itself a `Filter` registered in the Servlet filter chain at `SecurityProperties.DEFAULT_FILTER_ORDER` (-100 by default). Within the security chain, filters have a fixed order defined by `FilterOrderRegistration`. If you insert a custom filter too early (e.g., `addFilterBefore(myFilter, UsernamePasswordAuthenticationFilter.class)`) when your filter depends on the `SecurityContext` being populated, the context will not be set yet — `SecurityContextHolder.getContext().getAuthentication()` returns null. The correct pattern: know the security filter order, place authentication-dependent filters after `SecurityContextHolderFilter` (which restores the context from the session). Use `HttpSecurity.addFilterAfter` / `addFilterBefore` with precise reference filters.","distractorsMd":["Returning `false` from `preHandle` halts the handler chain — DispatcherServlet stops processing and returns the response as-is (typically empty 200 if you haven't written anything, or whatever you wrote in `preHandle`).","Wrap the request in a `ContentCachingRequestWrapper` and pass the wrapper to `chain.doFilter()`.","`OncePerRequestFilter` (Spring's abstract class) guarantees the filter executes exactly once per request, even when the request is forwarded internally via `RequestDispatcher.forward()`."]},{"id":"spring/filters_and_interceptors#ea3131aa6504","section":"spring","module":"spring/filters_and_interceptors","moduleName":"filters and interceptors","sourceFile":"README.md","difficulty":"advanced","question":"How does AsyncContext and async servlet processing affect HandlerInterceptor.afterCompletion?","answerFull":"When a controller starts async processing (DeferredResult, Callable, @Async with SseEmitter), the servlet container thread that handled the request is released before the response is committed. Spring MVC's HandlerInterceptor.afterCompletion is called when the response is finally committed — but on a *different* thread than preHandle and postHandle. Implication: any thread-local state set in preHandle (e.g., in MDC) is not automatically available in afterCompletion unless you use CallableProcessingInterceptor or DeferredResultProcessingInterceptor, which provide explicit callbacks for the async processing lifecycle. For accurate async request timing, instrument DeferredResult.setResult() or the Callable body directly rather than relying on afterCompletion timing.","correct":"When a controller starts async processing (DeferredResult, Callable, @Async with SseEmitter), the servlet container thread that handled the request is released before the response is committed.","distractors":["The client receives an empty response body.","OncePerRequestFilter (Spring's abstract class) guarantees the filter executes exactly once per request, even when the request is forwarded internally via RequestDispatcher.forward().","Wrap the request in a ContentCachingRequestWrapper and pass the wrapper to chain.doFilter()."],"distractorIds":["spring/filters_and_interceptors#185d986c0900","spring/filters_and_interceptors#ae5a27a78993","spring/filters_and_interceptors#74cb444439e7"],"concepts":["deferredresult","handlerinterceptor","sseemitter","released","affect","handled"],"questionMd":"How does `AsyncContext` and async servlet processing affect `HandlerInterceptor.afterCompletion`?","correctMd":"When a controller starts async processing (`DeferredResult`, `Callable`, `@Async` with `SseEmitter`), the servlet container thread that handled the request is released before the response is committed.","answerFullMd":"When a controller starts async processing (`DeferredResult`, `Callable`, `@Async` with `SseEmitter`), the servlet container thread that handled the request is released before the response is committed. Spring MVC's `HandlerInterceptor.afterCompletion` is called when the response is finally committed — but on a *different* thread than `preHandle` and `postHandle`. Implication: any thread-local state set in `preHandle` (e.g., in `MDC`) is not automatically available in `afterCompletion` unless you use `CallableProcessingInterceptor` or `DeferredResultProcessingInterceptor`, which provide explicit callbacks for the async processing lifecycle. For accurate async request timing, instrument `DeferredResult.setResult()` or the `Callable` body directly rather than relying on `afterCompletion` timing.","distractorsMd":[null,"`OncePerRequestFilter` (Spring's abstract class) guarantees the filter executes exactly once per request, even when the request is forwarded internally via `RequestDispatcher.forward()`.","Wrap the request in a `ContentCachingRequestWrapper` and pass the wrapper to `chain.doFilter()`."]},{"id":"spring/ioc_container#00b64010a0ed","section":"spring","module":"spring/ioc_container","moduleName":"ioc container","sourceFile":"README.md","difficulty":"core","question":"What is the difference between BeanFactory and ApplicationContext?","answerFull":"BeanFactory is the basic IoC container with lazy initialization; ApplicationContext extends it with eager singleton initialization, event publishing, i18n support, and @PostConstruct/@PreDestroy lifecycle. ApplicationContext is used in all production applications; BeanFactory is only used for testing or resource-constrained environments. In practice, always use ApplicationContext unless you have a specific reason not to.","correct":"BeanFactory is the basic IoC container with lazy initialization; ApplicationContext extends it with eager singleton initialization, event publishing, i18n support, and @PostConstruct/@PreDestroy lifecycle.","distractors":["Without a shutdown hook, JVM exit does not trigger @PreDestroy methods, DisposableBean.destroy(), or destroyMethod on beans.","@Lazy on a @Component or @Bean delays that bean's instantiation until first requested (via injection or getBean()).","@DependsOn(\"beanName\") tells the container to initialize the specified bean before the current one, even when there is no direct injection dependency."],"distractorIds":["spring/ioc_container#010684f68aa7","spring/ioc_container#7f6d38dc0a0c","spring/ioc_container#7579d814556d"],"concepts":["i18n","ioc","beanfactory","predestroy","basic","postconstruct"],"correctMd":"`BeanFactory` is the basic IoC container with lazy initialization; `ApplicationContext` extends it with eager singleton initialization, event publishing, i18n support, and `@PostConstruct`/`@PreDestroy` lifecycle.","answerFullMd":"`BeanFactory` is the basic IoC container with lazy initialization; `ApplicationContext` extends it with eager singleton initialization, event publishing, i18n support, and `@PostConstruct`/`@PreDestroy` lifecycle. `ApplicationContext` is used in all production applications; `BeanFactory` is only used for testing or resource-constrained environments. In practice, always use `ApplicationContext` unless you have a specific reason not to.","distractorsMd":["Without a shutdown hook, JVM exit does not trigger `@PreDestroy` methods, `DisposableBean.destroy()`, or `destroyMethod` on beans.","`@Lazy` on a `@Component` or `@Bean` delays that bean's instantiation until first requested (via injection or `getBean()`).","`@DependsOn(\"beanName\")` tells the container to initialize the specified bean before the current one, even when there is no direct injection dependency."]},{"id":"spring/ioc_container#5f701d8d64ad","section":"spring","module":"spring/ioc_container","moduleName":"ioc container","sourceFile":"README.md","difficulty":"core","question":"What is the difference between @Import and @ComponentScan?","answerFull":"@ComponentScan discovers @Component-annotated classes by classpath scanning within specified packages. @Import explicitly registers specific @Configuration classes, ImportSelector results, or ImportBeanDefinitionRegistrar programmatic registrations. @Import is more precise and does not require classpath scanning; it is how Spring Boot auto-configuration works internally. Use @Import for library/framework integration; use @ComponentScan for your own application code.","correct":"@ComponentScan discovers @Component-annotated classes by classpath scanning within specified packages.","distractors":["ClassPathXmlApplicationContext reads XML files from the classpath and builds BeanDefinitions from XML element tags (<bean>, <property>).","@DependsOn(\"beanName\") tells the container to initialize the specified bean before the current one, even when there is no direct injection dependency.","@Lazy on a @Component or @Bean delays that bean's instantiation until first requested (via injection or getBean())."],"distractorIds":["spring/ioc_container#df88f87e72f3","spring/ioc_container#7579d814556d","spring/ioc_container#7f6d38dc0a0c"],"concepts":["componentscan","scanning","discovers","packages","classpath","difference"],"correctMd":"`@ComponentScan` discovers `@Component`-annotated classes by classpath scanning within specified packages.","answerFullMd":"`@ComponentScan` discovers `@Component`-annotated classes by classpath scanning within specified packages. `@Import` explicitly registers specific `@Configuration` classes, `ImportSelector` results, or `ImportBeanDefinitionRegistrar` programmatic registrations. `@Import` is more precise and does not require classpath scanning; it is how Spring Boot auto-configuration works internally. Use `@Import` for library/framework integration; use `@ComponentScan` for your own application code.","distractorsMd":["`ClassPathXmlApplicationContext` reads XML files from the classpath and builds `BeanDefinition`s from XML element tags (`<bean>`, `<property>`).","`@DependsOn(\"beanName\")` tells the container to initialize the specified bean before the current one, even when there is no direct injection dependency.","`@Lazy` on a `@Component` or `@Bean` delays that bean's instantiation until first requested (via injection or `getBean()`)."]},{"id":"spring/ioc_container#07349035ac19","section":"spring","module":"spring/ioc_container","moduleName":"ioc container","sourceFile":"README.md","difficulty":"core","question":"What is the ApplicationContext parent/child hierarchy? Where is it used?","answerFull":"A child ApplicationContext can see all beans from its parent but the parent cannot see child beans. Classic Spring MVC uses this: the root context (parent) holds services and repositories, while the servlet context (child) holds MVC infrastructure (controllers, view resolvers). Spring Boot collapses this into a single context for simplicity. This hierarchy is still useful for multi-tenant systems or plugin architectures.","correct":"A child ApplicationContext can see all beans from its parent but the parent cannot see child beans.","distractors":["Without a shutdown hook, JVM exit does not trigger @PreDestroy methods, DisposableBean.destroy(), or destroyMethod on beans.","Lifecycle beans have start() and stop() methods called when the ApplicationContext starts and stops.","Spring tracks beans currently being created in a Set<String> singletonsCurrentlyInCreation."],"distractorIds":["spring/ioc_container#010684f68aa7","spring/ioc_container#6d0b81dc122f","spring/ioc_container#3de6e2b34021"],"concepts":["hierarchy","applicationcontext","child","beans","see","parent"],"correctMd":"A child `ApplicationContext` can see all beans from its parent but the parent cannot see child beans.","answerFullMd":"A child `ApplicationContext` can see all beans from its parent but the parent cannot see child beans. Classic Spring MVC uses this: the root context (parent) holds services and repositories, while the servlet context (child) holds MVC infrastructure (controllers, view resolvers). Spring Boot collapses this into a single context for simplicity. This hierarchy is still useful for multi-tenant systems or plugin architectures.","distractorsMd":["Without a shutdown hook, JVM exit does not trigger `@PreDestroy` methods, `DisposableBean.destroy()`, or `destroyMethod` on beans.","`Lifecycle` beans have `start()` and `stop()` methods called when the `ApplicationContext` starts and stops.","Spring tracks beans currently being created in a `Set<String> singletonsCurrentlyInCreation`."]},{"id":"spring/ioc_container#010684f68aa7","section":"spring","module":"spring/ioc_container","moduleName":"ioc container","sourceFile":"README.md","difficulty":"intermediate","question":"Why should you register a shutdown hook with ApplicationContext?","answerFull":"Without a shutdown hook, JVM exit does not trigger @PreDestroy methods, DisposableBean.destroy(), or destroyMethod on beans. This leaks resources: database connections remain open, Kafka consumers do not commit final offsets, scheduled tasks continue in background threads. ctx.registerShutdownHook() registers a JVM shutdown hook that calls ctx.close(), triggering orderly bean destruction. Spring Boot registers this automatically via SpringApplication.","correct":"Without a shutdown hook, JVM exit does not trigger @PreDestroy methods, DisposableBean.destroy(), or destroyMethod on beans.","distractors":["FactoryBean<T> is a special bean that produces other beans of type T.","Spring tracks beans currently being created in a Set<String> singletonsCurrentlyInCreation.","A child ApplicationContext can see all beans from its parent but the parent cannot see child beans."],"distractorIds":["spring/ioc_container#ccfb4ff2defe","spring/ioc_container#3de6e2b34021","spring/ioc_container#07349035ac19"],"concepts":["destroymethod","disposablebean","predestroy","hook","destroy","shutdown"],"correctMd":"Without a shutdown hook, JVM exit does not trigger `@PreDestroy` methods, `DisposableBean.destroy()`, or `destroyMethod` on beans.","answerFullMd":"Without a shutdown hook, JVM exit does not trigger `@PreDestroy` methods, `DisposableBean.destroy()`, or `destroyMethod` on beans. This leaks resources: database connections remain open, Kafka consumers do not commit final offsets, scheduled tasks continue in background threads. `ctx.registerShutdownHook()` registers a JVM shutdown hook that calls `ctx.close()`, triggering orderly bean destruction. Spring Boot registers this automatically via `SpringApplication`.","distractorsMd":["`FactoryBean<T>` is a special bean that produces other beans of type `T`.","Spring tracks beans currently being created in a `Set<String> singletonsCurrentlyInCreation`.","A child `ApplicationContext` can see all beans from its parent but the parent cannot see child beans."]},{"id":"spring/ioc_container#03e04fddc4c7","section":"spring","module":"spring/ioc_container","moduleName":"ioc container","sourceFile":"README.md","difficulty":"intermediate","question":"What is the BeanFactoryPostProcessor and when does it run?","answerFull":"BeanFactoryPostProcessor runs after all BeanDefinitions are loaded but before any bean is instantiated. It receives the ConfigurableListableBeanFactory and can modify or add BeanDefinitions. The most important built-in example is PropertySourcesPlaceholderConfigurer, which resolves ${property.key} placeholders in bean definitions. Do not instantiate application beans inside a BeanFactoryPostProcessor; it runs too early in the lifecycle.","correct":"BeanFactoryPostProcessor runs after all BeanDefinitions are loaded but before any bean is instantiated.","distractors":["@Lazy on a @Component or @Bean delays that bean's instantiation until first requested (via injection or getBean()).","FactoryBean<T> is a special bean that produces other beans of type T.","ClassPathXmlApplicationContext reads XML files from the classpath and builds BeanDefinitions from XML element tags (<bean>, <property>)."],"distractorIds":["spring/ioc_container#7f6d38dc0a0c","spring/ioc_container#ccfb4ff2defe","spring/ioc_container#df88f87e72f3"],"concepts":["beanfactorypostprocessor","beandefinitions","instantiated","loaded","bean","run"],"correctMd":"`BeanFactoryPostProcessor` runs after all `BeanDefinition`s are loaded but before any bean is instantiated.","answerFullMd":"`BeanFactoryPostProcessor` runs after all `BeanDefinition`s are loaded but before any bean is instantiated. It receives the `ConfigurableListableBeanFactory` and can modify or add `BeanDefinition`s. The most important built-in example is `PropertySourcesPlaceholderConfigurer`, which resolves `${property.key}` placeholders in bean definitions. Do not instantiate application beans inside a `BeanFactoryPostProcessor`; it runs too early in the lifecycle.","distractorsMd":["`@Lazy` on a `@Component` or `@Bean` delays that bean's instantiation until first requested (via injection or `getBean()`).","`FactoryBean<T>` is a special bean that produces other beans of type `T`.","`ClassPathXmlApplicationContext` reads XML files from the classpath and builds `BeanDefinition`s from XML element tags (`<bean>`, `<property>`)."]},{"id":"spring/ioc_container#aa43b05449a7","section":"spring","module":"spring/ioc_container","moduleName":"ioc container","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between BeanFactoryPostProcessor and BeanPostProcessor?","answerFull":"BeanFactoryPostProcessor operates on BeanDefinitions (metadata) before any bean is created. BeanPostProcessor operates on fully constructed bean instances, with hooks before and after initialization (postProcessBeforeInitialization / postProcessAfterInitialization). BeanPostProcessor is how @Autowired, @Value, @PostConstruct, and AOP proxying are implemented. BeanFactoryPostProcessor modifies the recipe; BeanPostProcessor modifies the cooked dish.","correct":"BeanFactoryPostProcessor operates on BeanDefinitions (metadata) before any bean is created.","distractors":["BeanFactoryPostProcessor runs after all BeanDefinitions are loaded but before any bean is instantiated.","ClassPathXmlApplicationContext reads XML files from the classpath and builds BeanDefinitions from XML element tags (<bean>, <property>).","@DependsOn(\"beanName\") tells the container to initialize the specified bean before the current one, even when there is no direct injection dependency."],"distractorIds":["spring/ioc_container#03e04fddc4c7","spring/ioc_container#df88f87e72f3","spring/ioc_container#7579d814556d"],"concepts":["beanfactorypostprocessor","beandefinitions","beanpostprocessor","difference","operates","created"],"correctMd":"`BeanFactoryPostProcessor` operates on `BeanDefinition`s (metadata) before any bean is created.","answerFullMd":"`BeanFactoryPostProcessor` operates on `BeanDefinition`s (metadata) before any bean is created. `BeanPostProcessor` operates on fully constructed bean instances, with hooks before and after initialization (`postProcessBeforeInitialization` / `postProcessAfterInitialization`). `BeanPostProcessor` is how `@Autowired`, `@Value`, `@PostConstruct`, and AOP proxying are implemented. `BeanFactoryPostProcessor` modifies the recipe; `BeanPostProcessor` modifies the cooked dish.","distractorsMd":["`BeanFactoryPostProcessor` runs after all `BeanDefinition`s are loaded but before any bean is instantiated.","`ClassPathXmlApplicationContext` reads XML files from the classpath and builds `BeanDefinition`s from XML element tags (`<bean>`, `<property>`).","`@DependsOn(\"beanName\")` tells the container to initialize the specified bean before the current one, even when there is no direct injection dependency."]},{"id":"spring/ioc_container#3de6e2b34021","section":"spring","module":"spring/ioc_container","moduleName":"ioc container","sourceFile":"README.md","difficulty":"intermediate","question":"How does Spring detect circular dependencies at startup?","answerFull":"Spring tracks beans currently being created in a Set<String> singletonsCurrentlyInCreation. When creating bean A requires bean B, which requires bean A (constructor injection), Spring detects that A is already in the \"currently creating\" set and throws BeanCurrentlyInCreationException. For setter/field injection, Spring can resolve circular deps by exposing the partially initialized bean via earlySingletonObjects. Spring Boot 2.6+ disables circular dependency resolution by default.","correct":"Spring tracks beans currently being created in a Set<String> singletonsCurrentlyInCreation.","distractors":["Lifecycle beans have start() and stop() methods called when the ApplicationContext starts and stops.","Without a shutdown hook, JVM exit does not trigger @PreDestroy methods, DisposableBean.destroy(), or destroyMethod on beans.","BeanFactoryPostProcessor operates on BeanDefinitions (metadata) before any bean is created."],"distractorIds":["spring/ioc_container#6d0b81dc122f","spring/ioc_container#010684f68aa7","spring/ioc_container#aa43b05449a7"],"concepts":["singletonscurrentlyincreation","circular","tracks","currently","detect","beans"],"correctMd":"Spring tracks beans currently being created in a `Set<String> singletonsCurrentlyInCreation`.","answerFullMd":"Spring tracks beans currently being created in a `Set<String> singletonsCurrentlyInCreation`. When creating bean A requires bean B, which requires bean A (constructor injection), Spring detects that A is already in the \"currently creating\" set and throws `BeanCurrentlyInCreationException`. For setter/field injection, Spring can resolve circular deps by exposing the partially initialized bean via `earlySingletonObjects`. Spring Boot 2.6+ disables circular dependency resolution by default.","distractorsMd":["`Lifecycle` beans have `start()` and `stop()` methods called when the `ApplicationContext` starts and stops.","Without a shutdown hook, JVM exit does not trigger `@PreDestroy` methods, `DisposableBean.destroy()`, or `destroyMethod` on beans.","`BeanFactoryPostProcessor` operates on `BeanDefinition`s (metadata) before any bean is created."]},{"id":"spring/ioc_container#7579d814556d","section":"spring","module":"spring/ioc_container","moduleName":"ioc container","sourceFile":"README.md","difficulty":"intermediate","question":"What is @DependsOn and when would you use it?","answerFull":"@DependsOn(\"beanName\") tells the container to initialize the specified bean before the current one, even when there is no direct injection dependency. This is needed when a bean relies on a side effect of another bean's initialization — for example, a bean that registers a JNDI resource or sets a system property that the current bean reads during its own initialization. Without @DependsOn, the order is non-deterministic. Use sparingly; a direct injection dependency is usually better design.","correct":"@DependsOn(\"beanName\") tells the container to initialize the specified bean before the current one, even when there is no direct injection dependency.","distractors":["BeanFactoryPostProcessor runs after all BeanDefinitions are loaded but before any bean is instantiated.","The container publishes: ContextRefreshedEvent (refresh complete), ContextStartedEvent, ContextStoppedEvent, ContextClosedEvent, and request-scoped events in web apps.","@ComponentScan discovers @Component-annotated classes by classpath scanning within specified packages."],"distractorIds":["spring/ioc_container#03e04fddc4c7","spring/ioc_container#6a3606926976","spring/ioc_container#5f701d8d64ad"],"concepts":["beanname","dependson","initialize","tells","specified","direct"],"correctMd":"`@DependsOn(\"beanName\")` tells the container to initialize the specified bean before the current one, even when there is no direct injection dependency.","answerFullMd":"`@DependsOn(\"beanName\")` tells the container to initialize the specified bean before the current one, even when there is no direct injection dependency. This is needed when a bean relies on a side effect of another bean's initialization — for example, a bean that registers a JNDI resource or sets a system property that the current bean reads during its own initialization. Without `@DependsOn`, the order is non-deterministic. Use sparingly; a direct injection dependency is usually better design.","distractorsMd":["`BeanFactoryPostProcessor` runs after all `BeanDefinition`s are loaded but before any bean is instantiated.","The container publishes: `ContextRefreshedEvent` (refresh complete), `ContextStartedEvent`, `ContextStoppedEvent`, `ContextClosedEvent`, and request-scoped events in web apps.","`@ComponentScan` discovers `@Component`-annotated classes by classpath scanning within specified packages."]},{"id":"spring/ioc_container#6d0b81dc122f","section":"spring","module":"spring/ioc_container","moduleName":"ioc container","sourceFile":"README.md","difficulty":"advanced","question":"What are SmartLifecycle and Lifecycle interfaces used for?","answerFull":"Lifecycle beans have start() and stop() methods called when the ApplicationContext starts and stops. SmartLifecycle extends this with a getPhase() method (lower phase = starts first, stops last) and isAutoStartup(). Spring Boot uses SmartLifecycle for the embedded server (Tomcat starts after all beans are initialized, stops before they are destroyed). Use SmartLifecycle when you have a resource (server, connection, scheduler) that should start after all beans are ready.","correct":"Lifecycle beans have start() and stop() methods called when the ApplicationContext starts and stops.","distractors":["Without a shutdown hook, JVM exit does not trigger @PreDestroy methods, DisposableBean.destroy(), or destroyMethod on beans.","A child ApplicationContext can see all beans from its parent but the parent cannot see child beans.","BeanFactory is the basic IoC container with lazy initialization; ApplicationContext extends it with eager singleton initialization, event publishing, i18n support, and @PostConstruct/@PreDestroy lifecycle."],"distractorIds":["spring/ioc_container#010684f68aa7","spring/ioc_container#07349035ac19","spring/ioc_container#00b64010a0ed"],"concepts":["smartlifecycle","stops","interfaces","stop","applicationcontext","lifecycle"],"correctMd":"`Lifecycle` beans have `start()` and `stop()` methods called when the `ApplicationContext` starts and stops.","answerFullMd":"`Lifecycle` beans have `start()` and `stop()` methods called when the `ApplicationContext` starts and stops. `SmartLifecycle` extends this with a `getPhase()` method (lower phase = starts first, stops last) and `isAutoStartup()`. Spring Boot uses `SmartLifecycle` for the embedded server (Tomcat starts after all beans are initialized, stops before they are destroyed). Use `SmartLifecycle` when you have a resource (server, connection, scheduler) that should start after all beans are ready.","distractorsMd":["Without a shutdown hook, JVM exit does not trigger `@PreDestroy` methods, `DisposableBean.destroy()`, or `destroyMethod` on beans.","A child `ApplicationContext` can see all beans from its parent but the parent cannot see child beans.","`BeanFactory` is the basic IoC container with lazy initialization; `ApplicationContext` extends it with eager singleton initialization, event publishing, i18n support, and `@PostConstruct`/`@PreDestroy` lifecycle."]},{"id":"spring/ioc_container#ccfb4ff2defe","section":"spring","module":"spring/ioc_container","moduleName":"ioc container","sourceFile":"README.md","difficulty":"advanced","question":"What is the FactoryBean<T> and how does it differ from a @Bean method?","answerFull":"FactoryBean<T> is a special bean that produces other beans of type T. Requesting beanName from the container returns the product (T), not the factory. Requesting &beanName returns the FactoryBean itself. This pattern pre-dates Java config and is used in legacy Spring integrations (e.g., LocalSessionFactoryBean for Hibernate). A @Bean method is the modern equivalent and is simpler for most cases. Know FactoryBean for legacy codebase interviews.","correct":"FactoryBean<T> is a special bean that produces other beans of type T.","distractors":["BeanFactoryPostProcessor runs after all BeanDefinitions are loaded but before any bean is instantiated.","Spring tracks beans currently being created in a Set<String> singletonsCurrentlyInCreation.","A child ApplicationContext can see all beans from its parent but the parent cannot see child beans."],"distractorIds":["spring/ioc_container#03e04fddc4c7","spring/ioc_container#3de6e2b34021","spring/ioc_container#07349035ac19"],"concepts":["factorybean","differ","special","beans","bean","produces"],"correctMd":"`FactoryBean<T>` is a special bean that produces other beans of type `T`.","answerFullMd":"`FactoryBean<T>` is a special bean that produces other beans of type `T`. Requesting `beanName` from the container returns the product (`T`), not the factory. Requesting `&beanName` returns the `FactoryBean` itself. This pattern pre-dates Java config and is used in legacy Spring integrations (e.g., `LocalSessionFactoryBean` for Hibernate). A `@Bean` method is the modern equivalent and is simpler for most cases. Know `FactoryBean` for legacy codebase interviews.","distractorsMd":["`BeanFactoryPostProcessor` runs after all `BeanDefinition`s are loaded but before any bean is instantiated.","Spring tracks beans currently being created in a `Set<String> singletonsCurrentlyInCreation`.","A child `ApplicationContext` can see all beans from its parent but the parent cannot see child beans."]},{"id":"spring/ioc_container#7f6d38dc0a0c","section":"spring","module":"spring/ioc_container","moduleName":"ioc container","sourceFile":"README.md","difficulty":"advanced","question":"How does @Lazy affect ApplicationContext startup?","answerFull":"@Lazy on a @Component or @Bean delays that bean's instantiation until first requested (via injection or getBean()). spring.main.lazy-initialization=true makes all beans lazy globally. This reduces startup time (useful for fast-test iteration) but moves initialization errors from startup to first-request time, which is undesirable in production where Kubernetes readiness probes check startup health. Spring Boot 2.4+ introduced a startup actuator endpoint to profile which beans are slow to initialize.","correct":"@Lazy on a @Component or @Bean delays that bean's instantiation until first requested (via injection or getBean()).","distractors":["@DependsOn(\"beanName\") tells the container to initialize the specified bean before the current one, even when there is no direct injection dependency.","@ComponentScan discovers @Component-annotated classes by classpath scanning within specified packages.","ClassPathXmlApplicationContext reads XML files from the classpath and builds BeanDefinitions from XML element tags (<bean>, <property>)."],"distractorIds":["spring/ioc_container#7579d814556d","spring/ioc_container#5f701d8d64ad","spring/ioc_container#df88f87e72f3"],"concepts":["getbean","delays","instantiation","affect","requested","applicationcontext"],"correctMd":"`@Lazy` on a `@Component` or `@Bean` delays that bean's instantiation until first requested (via injection or `getBean()`).","answerFullMd":"`@Lazy` on a `@Component` or `@Bean` delays that bean's instantiation until first requested (via injection or `getBean()`). `spring.main.lazy-initialization=true` makes all beans lazy globally. This reduces startup time (useful for fast-test iteration) but moves initialization errors from startup to first-request time, which is undesirable in production where Kubernetes readiness probes check startup health. Spring Boot 2.4+ introduced a startup actuator endpoint to profile which beans are slow to initialize.","distractorsMd":["`@DependsOn(\"beanName\")` tells the container to initialize the specified bean before the current one, even when there is no direct injection dependency.","`@ComponentScan` discovers `@Component`-annotated classes by classpath scanning within specified packages.","`ClassPathXmlApplicationContext` reads XML files from the classpath and builds `BeanDefinition`s from XML element tags (`<bean>`, `<property>`)."]},{"id":"spring/ioc_container#6a3606926976","section":"spring","module":"spring/ioc_container","moduleName":"ioc container","sourceFile":"README.md","difficulty":"advanced","question":"What events does ApplicationContext publish and how do you listen to them?","answerFull":"The container publishes: ContextRefreshedEvent (refresh complete), ContextStartedEvent, ContextStoppedEvent, ContextClosedEvent, and request-scoped events in web apps. Listen with @EventListener on any bean method: @EventListener public void onRefresh(ContextRefreshedEvent e) {...}. For async events, add @Async. For ordering, add @Order. Custom events extend ApplicationEvent and are published via applicationContext.publishEvent(new MyEvent(this)). Events are synchronous by default; all listeners execute in the publisher's thread.","correct":"The container publishes: ContextRefreshedEvent (refresh complete), ContextStartedEvent, ContextStoppedEvent, ContextClosedEvent, and request-scoped events in web apps.","distractors":["BeanFactory is the basic IoC container with lazy initialization; ApplicationContext extends it with eager singleton initialization, event publishing, i18n support, and @PostConstruct/@PreDestroy lifecycle.","@DependsOn(\"beanName\") tells the container to initialize the specified bean before the current one, even when there is no direct injection dependency.","A child ApplicationContext can see all beans from its parent but the parent cannot see child beans."],"distractorIds":["spring/ioc_container#00b64010a0ed","spring/ioc_container#7579d814556d","spring/ioc_container#07349035ac19"],"concepts":["contextclosedevent","contextrefreshedevent","contextstartedevent","contextstoppedevent","listen","apps"],"correctMd":"The container publishes: `ContextRefreshedEvent` (refresh complete), `ContextStartedEvent`, `ContextStoppedEvent`, `ContextClosedEvent`, and request-scoped events in web apps.","answerFullMd":"The container publishes: `ContextRefreshedEvent` (refresh complete), `ContextStartedEvent`, `ContextStoppedEvent`, `ContextClosedEvent`, and request-scoped events in web apps. Listen with `@EventListener` on any bean method: `@EventListener public void onRefresh(ContextRefreshedEvent e) {...}`. For async events, add `@Async`. For ordering, add `@Order`. Custom events extend `ApplicationEvent` and are published via `applicationContext.publishEvent(new MyEvent(this))`. Events are synchronous by default; all listeners execute in the publisher's thread.","distractorsMd":["`BeanFactory` is the basic IoC container with lazy initialization; `ApplicationContext` extends it with eager singleton initialization, event publishing, i18n support, and `@PostConstruct`/`@PreDestroy` lifecycle.","`@DependsOn(\"beanName\")` tells the container to initialize the specified bean before the current one, even when there is no direct injection dependency.","A child `ApplicationContext` can see all beans from its parent but the parent cannot see child beans."]},{"id":"spring/ioc_container#df88f87e72f3","section":"spring","module":"spring/ioc_container","moduleName":"ioc container","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between ClassPathXmlApplicationContext and AnnotationConfigApplicationContext?","answerFull":"ClassPathXmlApplicationContext reads XML files from the classpath and builds BeanDefinitions from XML element tags (<bean>, <property>). AnnotationConfigApplicationContext reads @Configuration classes and @Component-annotated classes (via scanning) and builds BeanDefinitions from annotations. Both implement ApplicationContext and behave identically after startup. XML config is legacy and rarely used in new code; annotated Java config is the modern standard.","correct":"ClassPathXmlApplicationContext reads XML files from the classpath and builds BeanDefinitions from XML element tags (<bean>, <property>).","distractors":["@ComponentScan discovers @Component-annotated classes by classpath scanning within specified packages.","FactoryBean<T> is a special bean that produces other beans of type T.","@DependsOn(\"beanName\") tells the container to initialize the specified bean before the current one, even when there is no direct injection dependency."],"distractorIds":["spring/ioc_container#5f701d8d64ad","spring/ioc_container#ccfb4ff2defe","spring/ioc_container#7579d814556d"],"concepts":["classpathxmlapplicationcontext","beandefinitions","xml","classpath","difference","property"],"correctMd":"`ClassPathXmlApplicationContext` reads XML files from the classpath and builds `BeanDefinition`s from XML element tags (`<bean>`, `<property>`).","answerFullMd":"`ClassPathXmlApplicationContext` reads XML files from the classpath and builds `BeanDefinition`s from XML element tags (`<bean>`, `<property>`). `AnnotationConfigApplicationContext` reads `@Configuration` classes and `@Component`-annotated classes (via scanning) and builds `BeanDefinition`s from annotations. Both implement `ApplicationContext` and behave identically after startup. XML config is legacy and rarely used in new code; annotated Java config is the modern standard.","distractorsMd":["`@ComponentScan` discovers `@Component`-annotated classes by classpath scanning within specified packages.","`FactoryBean<T>` is a special bean that produces other beans of type `T`.","`@DependsOn(\"beanName\")` tells the container to initialize the specified bean before the current one, even when there is no direct injection dependency."]},{"id":"spring/observability_and_tracing#eb552dc3f94a","section":"spring","module":"spring/observability_and_tracing","moduleName":"observability and tracing","sourceFile":"README.md","difficulty":"core","question":"What are the three pillars of observability, and why is each insufficient alone?","answerFull":"Metrics, traces, and logs. Metrics (counters, histograms, gauges) are aggregate — they tell you that p99 latency is 2 seconds but not which specific requests are slow or why. Traces are per-request causality maps — they show which service and which code path took the 2 seconds, but individual traces don't tell you how widespread the problem is. Logs are event records — they capture detailed context per event but are not structured for aggregation or causality analysis. Together: metrics alert you that something is wrong, traces tell you why a specific request was slow, and logs provide the fine-grained context (SQL queries, error messages, business fields) for debugging.","correct":"Metrics, traces, and logs.","distractors":["Add dependencies: micrometer-tracing-bridge-otel + opentelemetry-exporter-otlp.","An exemplar is a sample data point attached to a Prometheus histogram/summary observation that carries additional metadata — specifically the traceId of the request that contributed to that observation.","Each unique combination of label values in a Prometheus metric creates one time series in Prometheus's memory."],"distractorIds":["spring/observability_and_tracing#20e5bee3b54c","spring/observability_and_tracing#3de05a54fca0","spring/observability_and_tracing#094a9fa1b51c"],"concepts":["pillars","insufficient","alone","observability","traces","logs"],"distractorsMd":["Add dependencies: `micrometer-tracing-bridge-otel` + `opentelemetry-exporter-otlp`.","An exemplar is a sample data point attached to a Prometheus histogram/summary observation that carries additional metadata — specifically the `traceId` of the request that contributed to that observation.",null]},{"id":"spring/observability_and_tracing#c97daa2aca8d","section":"spring","module":"spring/observability_and_tracing","moduleName":"observability and tracing","sourceFile":"README.md","difficulty":"core","question":"What is the W3C traceparent header and what are its fields?","answerFull":"traceparent is the W3C Trace Context standard for distributed trace propagation. Format: {version}-{traceId}-{parentSpanId}-{flags}. Example: 00-abc123def456789012345678901234-0123456789abcdef-01. Fields: version (always 00); traceId (128-bit hex, unique per distributed request — same across all services in one request); parentSpanId (64-bit hex, the calling span's ID — enables parent-child span linking); flags (8-bit, bit 0 = sampling flag: 01 means this trace is sampled). When Service A calls Service B, A sets traceparent with its own spanId as parentSpanId — Service B creates a child span with A's span as parent, building the trace tree.","correct":"traceparent is the W3C Trace Context standard for distributed trace propagation.","distractors":["Micrometer Tracing uses a context propagation mechanism (backed by io.micrometer:context-propagation) that captures all registered context values (trace context, MDC, Reactor context) into a ContextSnapshot.","Head-based sampling decides at the root span (first service) whether to record the trace — all downstream services inherit the decision.","MeterRegistry.counter(\"name\").increment() creates a metric only — no trace span, no MDC context."],"distractorIds":["spring/observability_and_tracing#580cc87ff302","spring/observability_and_tracing#bc5d899c7a8a","spring/observability_and_tracing#5a3ce08b9666"],"concepts":["w3c","traceparent","propagation","distributed","header","trace"],"questionMd":"What is the W3C `traceparent` header and what are its fields?","correctMd":"`traceparent` is the W3C Trace Context standard for distributed trace propagation.","answerFullMd":"`traceparent` is the W3C Trace Context standard for distributed trace propagation. Format: `{version}-{traceId}-{parentSpanId}-{flags}`. Example: `00-abc123def456789012345678901234-0123456789abcdef-01`. Fields: `version` (always `00`); `traceId` (128-bit hex, unique per distributed request — same across all services in one request); `parentSpanId` (64-bit hex, the calling span's ID — enables parent-child span linking); `flags` (8-bit, bit 0 = sampling flag: `01` means this trace is sampled). When Service A calls Service B, A sets `traceparent` with its own `spanId` as `parentSpanId` — Service B creates a child span with A's span as parent, building the trace tree.","distractorsMd":["Micrometer Tracing uses a context propagation mechanism (backed by `io.micrometer:context-propagation`) that captures all registered context values (trace context, MDC, Reactor context) into a `ContextSnapshot`.",null,"`MeterRegistry.counter(\"name\").increment()` creates a metric only — no trace span, no MDC context."]},{"id":"spring/observability_and_tracing#5a3ce08b9666","section":"spring","module":"spring/observability_and_tracing","moduleName":"observability and tracing","sourceFile":"README.md","difficulty":"core","question":"What is the Micrometer Observation API, and how does it differ from directly calling MeterRegistry?","answerFull":"MeterRegistry.counter(\"name\").increment() creates a metric only — no trace span, no MDC context. The Observation API (Observation.createNotStarted(\"name\", registry).observe(...)) creates all three simultaneously: a timer metric (name with outcome and exception tags), a trace span (added as a child to the current active span), and populates MDC with traceId/spanId for the duration of the observed block. This ensures all three signals share the same name and context — when you look at a metric, a trace, and a log, they all agree on what happened. Using raw MeterRegistry calls scatters the instrumentation and loses correlation.","correct":"MeterRegistry.counter(\"name\").increment() creates a metric only — no trace span, no MDC context.","distractors":["Spring Batch 5 + Micrometer Tracing (Spring Boot 3.1+) auto-creates spans for: JobExecution (root span), each StepExecution (child span), and each chunk (child of step).","An exemplar is a sample data point attached to a Prometheus histogram/summary observation that carries additional metadata — specifically the traceId of the request that contributed to that observation.","Micrometer Tracing auto-populates MDC keys traceId and spanId for the duration of every span (HTTP request, JDBC call, Observation block)."],"distractorIds":["spring/observability_and_tracing#ae7d360d489c","spring/observability_and_tracing#3de05a54fca0","spring/observability_and_tracing#c1bb909e0d84"],"concepts":["meterregistry","increment","mdc","micrometer","differ","observation"],"questionMd":"What is the Micrometer Observation API, and how does it differ from directly calling `MeterRegistry`?","correctMd":"`MeterRegistry.counter(\"name\").increment()` creates a metric only — no trace span, no MDC context.","answerFullMd":"`MeterRegistry.counter(\"name\").increment()` creates a metric only — no trace span, no MDC context. The Observation API (`Observation.createNotStarted(\"name\", registry).observe(...)`) creates all three simultaneously: a timer metric (`name` with `outcome` and `exception` tags), a trace span (added as a child to the current active span), and populates MDC with `traceId`/`spanId` for the duration of the observed block. This ensures all three signals share the same name and context — when you look at a metric, a trace, and a log, they all agree on what happened. Using raw `MeterRegistry` calls scatters the instrumentation and loses correlation.","distractorsMd":["Spring Batch 5 + Micrometer Tracing (Spring Boot 3.1+) auto-creates spans for: `JobExecution` (root span), each `StepExecution` (child span), and each chunk (child of step).","An exemplar is a sample data point attached to a Prometheus histogram/summary observation that carries additional metadata — specifically the `traceId` of the request that contributed to that observation.","Micrometer Tracing auto-populates MDC keys `traceId` and `spanId` for the duration of every span (HTTP request, JDBC call, Observation block)."]},{"id":"spring/observability_and_tracing#bc5d899c7a8a","section":"spring","module":"spring/observability_and_tracing","moduleName":"observability and tracing","sourceFile":"README.md","difficulty":"core","question":"What is trace sampling and what are the tradeoffs between head-based and tail-based sampling?","answerFull":"Head-based sampling decides at the root span (first service) whether to record the trace — all downstream services inherit the decision. It is computationally cheap but cannot make intelligent decisions: a 1% head-based sampler drops 99% of traces, including some error traces. Tail-based sampling records all trace data initially and makes the keep/drop decision after the trace is complete — allowing \"always keep errors or traces > 1 second.\" Tail-based sampling requires a trace collector (OTel Collector, Tempo with trace-aware receiver) to buffer spans and correlate them by traceId before applying the sampling policy. Cost: higher collector memory + CPU. Best practice: head-based 1–10% for normal traffic, tail-based rule to always keep error traces.","correct":"Head-based sampling decides at the root span (first service) whether to record the trace — all downstream services inherit the decision.","distractors":["Spring Batch 5 + Micrometer Tracing (Spring Boot 3.1+) auto-creates spans for: JobExecution (root span), each StepExecution (child span), and each chunk (child of step).","Micrometer Tracing auto-populates MDC keys traceId and spanId for the duration of every span (HTTP request, JDBC call, Observation block).","traceparent is the W3C Trace Context standard for distributed trace propagation."],"distractorIds":["spring/observability_and_tracing#ae7d360d489c","spring/observability_and_tracing#c1bb909e0d84","spring/observability_and_tracing#c97daa2aca8d"],"concepts":["tradeoffs","inherit","tail","decides","downstream","span"],"answerFullMd":"Head-based sampling decides at the root span (first service) whether to record the trace — all downstream services inherit the decision. It is computationally cheap but cannot make intelligent decisions: a 1% head-based sampler drops 99% of traces, including some error traces. Tail-based sampling records all trace data initially and makes the keep/drop decision after the trace is complete — allowing \"always keep errors or traces > 1 second.\" Tail-based sampling requires a trace collector (OTel Collector, Tempo with trace-aware receiver) to buffer spans and correlate them by `traceId` before applying the sampling policy. Cost: higher collector memory + CPU. Best practice: head-based 1–10% for normal traffic, tail-based rule to always keep error traces.","distractorsMd":["Spring Batch 5 + Micrometer Tracing (Spring Boot 3.1+) auto-creates spans for: `JobExecution` (root span), each `StepExecution` (child span), and each chunk (child of step).","Micrometer Tracing auto-populates MDC keys `traceId` and `spanId` for the duration of every span (HTTP request, JDBC call, Observation block).","`traceparent` is the W3C Trace Context standard for distributed trace propagation."]},{"id":"spring/observability_and_tracing#094a9fa1b51c","section":"spring","module":"spring/observability_and_tracing","moduleName":"observability and tracing","sourceFile":"README.md","difficulty":"core","question":"What is cardinality in Prometheus metrics, and why is it dangerous to use request-level IDs as labels?","answerFull":"Each unique combination of label values in a Prometheus metric creates one time series in Prometheus's memory. Prometheus stores all active series in memory for fast queries. Adding a label with 1 million distinct values (e.g., userId, orderId, traceId) creates 1 million time series — each consuming ~1–2 KB of memory = 1–2 GB per metric just for that label. This \"cardinality explosion\" causes Prometheus OOM and query timeouts. Rule: Prometheus labels must be low cardinality (< 1,000 distinct values): status (ok/error), tier (free/pro/enterprise), region (us-east/eu-west), endpoint (path template, not full URL). High-cardinality identifiers (userId, orderId, traceId) belong in trace span attributes — the trace backend indexes them efficiently.","correct":"Each unique combination of label values in a Prometheus metric creates one time series in Prometheus's memory.","distractors":["Micrometer Tracing auto-populates MDC keys traceId and spanId for the duration of every span (HTTP request, JDBC call, Observation block).","Spring Batch 5 + Micrometer Tracing (Spring Boot 3.1+) auto-creates spans for: JobExecution (root span), each StepExecution (child span), and each chunk (child of step).","lowCardinalityKeyValue(key, value) produces a Prometheus metric tag (label) — it must have few distinct values (< 100)."],"distractorIds":["spring/observability_and_tracing#c1bb909e0d84","spring/observability_and_tracing#ae7d360d489c","spring/observability_and_tracing#257eeb797155"],"concepts":["dangerous","cardinality","series","combination","prometheus","ids"],"answerFullMd":"Each unique combination of label values in a Prometheus metric creates one time series in Prometheus's memory. Prometheus stores all active series in memory for fast queries. Adding a label with 1 million distinct values (e.g., `userId`, `orderId`, `traceId`) creates 1 million time series — each consuming ~1–2 KB of memory = 1–2 GB per metric just for that label. This \"cardinality explosion\" causes Prometheus OOM and query timeouts. Rule: Prometheus labels must be low cardinality (< 1,000 distinct values): `status` (ok/error), `tier` (free/pro/enterprise), `region` (us-east/eu-west), `endpoint` (path template, not full URL). High-cardinality identifiers (userId, orderId, traceId) belong in trace span attributes — the trace backend indexes them efficiently.","distractorsMd":["Micrometer Tracing auto-populates MDC keys `traceId` and `spanId` for the duration of every span (HTTP request, JDBC call, Observation block).","Spring Batch 5 + Micrometer Tracing (Spring Boot 3.1+) auto-creates spans for: `JobExecution` (root span), each `StepExecution` (child span), and each chunk (child of step).","`lowCardinalityKeyValue(key, value)` produces a Prometheus metric tag (label) — it must have few distinct values (< 100)."]},{"id":"spring/observability_and_tracing#580cc87ff302","section":"spring","module":"spring/observability_and_tracing","moduleName":"observability and tracing","sourceFile":"README.md","difficulty":"intermediate","question":"How does Micrometer Tracing propagate the trace context to virtual threads or CompletableFuture?","answerFull":"Micrometer Tracing uses a context propagation mechanism (backed by io.micrometer:context-propagation) that captures all registered context values (trace context, MDC, Reactor context) into a ContextSnapshot. When wrapping an executor with ContextExecutorService.wrap(executor, ContextSnapshot::captureAll), each task submitted to the executor restores the captured context before running — including the active trace span and MDC fields. For CompletableFuture: use ContextSnapshot.captureAll(observationRegistry).wrap(runnable/callable) to ensure the trace context is available in async callback chains. For virtual threads via Executors.newVirtualThreadPerTaskExecutor(): the same wrapping pattern applies.","correct":"Micrometer Tracing uses a context propagation mechanism (backed by io.micrometer:context-propagation) that captures all registered context values (trace context, MDC, Reactor context) into a ContextSnapshot.","distractors":["MeterRegistry.counter(\"name\").increment() creates a metric only — no trace span, no MDC context.","Add dependencies: micrometer-tracing-bridge-otel + opentelemetry-exporter-otlp.","traceparent is the W3C Trace Context standard for distributed trace propagation."],"distractorIds":["spring/observability_and_tracing#5a3ce08b9666","spring/observability_and_tracing#20e5bee3b54c","spring/observability_and_tracing#c97daa2aca8d"],"concepts":["contextsnapshot","completablefuture","propagate","mdc","propagation","reactor"],"correctMd":"Micrometer Tracing uses a context propagation mechanism (backed by `io.micrometer:context-propagation`) that captures all registered context values (trace context, MDC, Reactor context) into a `ContextSnapshot`.","answerFullMd":"Micrometer Tracing uses a context propagation mechanism (backed by `io.micrometer:context-propagation`) that captures all registered context values (trace context, MDC, Reactor context) into a `ContextSnapshot`. When wrapping an executor with `ContextExecutorService.wrap(executor, ContextSnapshot::captureAll)`, each task submitted to the executor restores the captured context before running — including the active trace span and MDC fields. For `CompletableFuture`: use `ContextSnapshot.captureAll(observationRegistry).wrap(runnable/callable)` to ensure the trace context is available in async callback chains. For virtual threads via `Executors.newVirtualThreadPerTaskExecutor()`: the same wrapping pattern applies.","distractorsMd":["`MeterRegistry.counter(\"name\").increment()` creates a metric only — no trace span, no MDC context.","Add dependencies: `micrometer-tracing-bridge-otel` + `opentelemetry-exporter-otlp`.","`traceparent` is the W3C Trace Context standard for distributed trace propagation."]},{"id":"spring/observability_and_tracing#3de05a54fca0","section":"spring","module":"spring/observability_and_tracing","moduleName":"observability and tracing","sourceFile":"README.md","difficulty":"intermediate","question":"What is an exemplar, and how does it link Prometheus metrics to distributed traces?","answerFull":"An exemplar is a sample data point attached to a Prometheus histogram/summary observation that carries additional metadata — specifically the traceId of the request that contributed to that observation. When a request with traceId=abc123 is recorded in the http.server.requests histogram with a value of 0.8 seconds, the exemplar attaches traceId=abc123 to that bucket boundary. In Grafana, a histogram visualization can display exemplars as orange dots — clicking a dot opens the Tempo trace for abc123. This is the \"metric to trace\" drill-down path. Requirements: Prometheus must be configured with --enable-feature=exemplar-storage; Micrometer must have both a tracing bridge and Prometheus registry active.","correct":"An exemplar is a sample data point attached to a Prometheus histogram/summary observation that carries additional metadata — specifically the traceId of the request that contributed to that observation.","distractors":["Micrometer Tracing auto-populates MDC keys traceId and spanId for the duration of every span (HTTP request, JDBC call, Observation block).","Metrics, traces, and logs.","lowCardinalityKeyValue(key, value) produces a Prometheus metric tag (label) — it must have few distinct values (< 100)."],"distractorIds":["spring/observability_and_tracing#c1bb909e0d84","spring/observability_and_tracing#eb552dc3f94a","spring/observability_and_tracing#257eeb797155"],"concepts":["contributed","exemplar","traceid","histogram","summary","attached"],"correctMd":"An exemplar is a sample data point attached to a Prometheus histogram/summary observation that carries additional metadata — specifically the `traceId` of the request that contributed to that observation.","answerFullMd":"An exemplar is a sample data point attached to a Prometheus histogram/summary observation that carries additional metadata — specifically the `traceId` of the request that contributed to that observation. When a request with `traceId=abc123` is recorded in the `http.server.requests` histogram with a value of 0.8 seconds, the exemplar attaches `traceId=abc123` to that bucket boundary. In Grafana, a histogram visualization can display exemplars as orange dots — clicking a dot opens the Tempo trace for `abc123`. This is the \"metric to trace\" drill-down path. Requirements: Prometheus must be configured with `--enable-feature=exemplar-storage`; Micrometer must have both a tracing bridge and Prometheus registry active.","distractorsMd":["Micrometer Tracing auto-populates MDC keys `traceId` and `spanId` for the duration of every span (HTTP request, JDBC call, Observation block).",null,"`lowCardinalityKeyValue(key, value)` produces a Prometheus metric tag (label) — it must have few distinct values (< 100)."]},{"id":"spring/observability_and_tracing#20e5bee3b54c","section":"spring","module":"spring/observability_and_tracing","moduleName":"observability and tracing","sourceFile":"README.md","difficulty":"intermediate","question":"How do you configure distributed tracing with Spring Boot 3 to export spans to Jaeger via OTLP?","answerFull":"Add dependencies: micrometer-tracing-bridge-otel + opentelemetry-exporter-otlp. Configure: yaml management: tracing: sampling: probability: 1.0 # 100% in dev otlp: tracing: endpoint: http://jaeger:4318/v1/traces # Jaeger OTLP HTTP endpoint spring: application: name: my-service # becomes the \"service.name\" span attribute Spring Boot 3 auto-configures the OTel SdkTracerProvider, the OtlpHttpSpanExporter, and the MicrometerTracingAutoConfiguration. Every HTTP request, JDBC query, and Kafka consumer invocation is automatically traced and exported to Jaeger. No manual Tracer.spanBuilder() code is needed for auto-instrumented paths.","correct":"Add dependencies: micrometer-tracing-bridge-otel + opentelemetry-exporter-otlp.","distractors":["Spring Batch 5 + Micrometer Tracing (Spring Boot 3.1+) auto-creates spans for: JobExecution (root span), each StepExecution (child span), and each chunk (child of step).","Micrometer Tracing auto-populates MDC keys traceId and spanId for the duration of every span (HTTP request, JDBC call, Observation block).","Micrometer Tracing uses a context propagation mechanism (backed by io.micrometer:context-propagation) that captures all registered context values (trace context, MDC, Reactor context) into a ContextSnapshot."],"distractorIds":["spring/observability_and_tracing#ae7d360d489c","spring/observability_and_tracing#c1bb909e0d84","spring/observability_and_tracing#580cc87ff302"],"concepts":["exporter","jaeger","bridge","export","otel","otlp"],"correctMd":"Add dependencies: `micrometer-tracing-bridge-otel` + `opentelemetry-exporter-otlp`.","answerFullMd":"Add dependencies: `micrometer-tracing-bridge-otel` + `opentelemetry-exporter-otlp`. Configure: ```yaml management: tracing: sampling: probability: 1.0 # 100% in dev otlp: tracing: endpoint: http://jaeger:4318/v1/traces # Jaeger OTLP HTTP endpoint spring: application: name: my-service # becomes the \"service.name\" span attribute ``` Spring Boot 3 auto-configures the OTel `SdkTracerProvider`, the `OtlpHttpSpanExporter`, and the `MicrometerTracingAutoConfiguration`. Every HTTP request, JDBC query, and Kafka consumer invocation is automatically traced and exported to Jaeger. No manual `Tracer.spanBuilder()` code is needed for auto-instrumented paths.","distractorsMd":["Spring Batch 5 + Micrometer Tracing (Spring Boot 3.1+) auto-creates spans for: `JobExecution` (root span), each `StepExecution` (child span), and each chunk (child of step).","Micrometer Tracing auto-populates MDC keys `traceId` and `spanId` for the duration of every span (HTTP request, JDBC call, Observation block).","Micrometer Tracing uses a context propagation mechanism (backed by `io.micrometer:context-propagation`) that captures all registered context values (trace context, MDC, Reactor context) into a `ContextSnapshot`."]},{"id":"spring/observability_and_tracing#36302b6ca549","section":"spring","module":"spring/observability_and_tracing","moduleName":"observability and tracing","sourceFile":"README.md","difficulty":"intermediate","question":"What is structured logging, and how does it differ from traditional log messages?","answerFull":"Traditional logging: log.info(\"Order 12345 created for user john@example.com in 245ms\") — a human-readable string. Searching for all slow orders requires regex: grep \"created.*[0-9]{4,}ms\". Structured logging: log.info(\"Order created\", kv(\"orderId\", 12345), kv(\"userId\", \"john\"), kv(\"durationMs\", 245)) — a JSON object {\"message\": \"Order created\", \"orderId\": 12345, \"userId\": \"john\", \"durationMs\": 245, \"traceId\": \"abc123\"}. Searching for all orders > 200ms: level=INFO AND message=\"Order created\" AND durationMs>200. Structured logs are queryable, aggregatable, and linkable to traces via traceId. They are also parsable by log aggregation systems (Elasticsearch, Loki) without fragile regex extraction.","correct":"Traditional logging: log.info(\"Order 12345 created for user john@example.com in 245ms\") — a human-readable string.","distractors":["Add dependencies: micrometer-tracing-bridge-otel + opentelemetry-exporter-otlp.","An exemplar is a sample data point attached to a Prometheus histogram/summary observation that carries additional metadata — specifically the traceId of the request that contributed to that observation.","Each unique combination of label values in a Prometheus metric creates one time series in Prometheus's memory."],"distractorIds":["spring/observability_and_tracing#20e5bee3b54c","spring/observability_and_tracing#3de05a54fca0","spring/observability_and_tracing#094a9fa1b51c"],"concepts":["12345","245ms","john","readable","info","com"],"correctMd":"Traditional logging: `log.info(\"Order 12345 created for user john@example.com in 245ms\")` — a human-readable string.","answerFullMd":"Traditional logging: `log.info(\"Order 12345 created for user john@example.com in 245ms\")` — a human-readable string. Searching for all slow orders requires regex: `grep \"created.*[0-9]{4,}ms\"`. Structured logging: `log.info(\"Order created\", kv(\"orderId\", 12345), kv(\"userId\", \"john\"), kv(\"durationMs\", 245))` — a JSON object `{\"message\": \"Order created\", \"orderId\": 12345, \"userId\": \"john\", \"durationMs\": 245, \"traceId\": \"abc123\"}`. Searching for all orders > 200ms: `level=INFO AND message=\"Order created\" AND durationMs>200`. Structured logs are queryable, aggregatable, and linkable to traces via `traceId`. They are also parsable by log aggregation systems (Elasticsearch, Loki) without fragile regex extraction.","distractorsMd":["Add dependencies: `micrometer-tracing-bridge-otel` + `opentelemetry-exporter-otlp`.","An exemplar is a sample data point attached to a Prometheus histogram/summary observation that carries additional metadata — specifically the `traceId` of the request that contributed to that observation.",null]},{"id":"spring/observability_and_tracing#9551d202f28c","section":"spring","module":"spring/observability_and_tracing","moduleName":"observability and tracing","sourceFile":"README.md","difficulty":"intermediate","question":"How do you expose and secure the /actuator/prometheus endpoint?","answerFull":"Never expose Actuator endpoints on the public port in production. Configure a separate management port: management.server.port=8081. The load balancer routes port 8080 (public) to the internet; port 8081 is internal-only (VPC security group or Kubernetes NetworkPolicy blocks external access). On port 8081, expose only the needed endpoints: management.endpoints.web.exposure.include=health,prometheus,loggers,metrics. For authenticated scraping: configure management.endpoint.prometheus.enabled=true and add Spring Security to the management port with HttpSecurity.requestMatcher(EndpointRequest.toAnyEndpoint()).authorizeHttpRequests(auth -> auth.anyRequest().hasRole(\"PROMETHEUS\")).","correct":"Never expose Actuator endpoints on the public port in production.","distractors":["lowCardinalityKeyValue(key, value) produces a Prometheus metric tag (label) — it must have few distinct values (< 100).","Each unique combination of label values in a Prometheus metric creates one time series in Prometheus's memory.","An exemplar is a sample data point attached to a Prometheus histogram/summary observation that carries additional metadata — specifically the traceId of the request that contributed to that observation."],"distractorIds":["spring/observability_and_tracing#257eeb797155","spring/observability_and_tracing#094a9fa1b51c","spring/observability_and_tracing#3de05a54fca0"],"concepts":["secure","actuator","expose","endpoint","endpoints","prometheus"],"questionMd":"How do you expose and secure the `/actuator/prometheus` endpoint?","answerFullMd":"Never expose Actuator endpoints on the public port in production. Configure a separate management port: `management.server.port=8081`. The load balancer routes port 8080 (public) to the internet; port 8081 is internal-only (VPC security group or Kubernetes NetworkPolicy blocks external access). On port 8081, expose only the needed endpoints: `management.endpoints.web.exposure.include=health,prometheus,loggers,metrics`. For authenticated scraping: configure `management.endpoint.prometheus.enabled=true` and add Spring Security to the management port with `HttpSecurity.requestMatcher(EndpointRequest.toAnyEndpoint()).authorizeHttpRequests(auth -> auth.anyRequest().hasRole(\"PROMETHEUS\"))`.","distractorsMd":["`lowCardinalityKeyValue(key, value)` produces a Prometheus metric tag (label) — it must have few distinct values (< 100).",null,"An exemplar is a sample data point attached to a Prometheus histogram/summary observation that carries additional metadata — specifically the `traceId` of the request that contributed to that observation."]},{"id":"spring/observability_and_tracing#257eeb797155","section":"spring","module":"spring/observability_and_tracing","moduleName":"observability and tracing","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between lowCardinalityKeyValue and highCardinalityKeyValue in the Observation API?","answerFull":"lowCardinalityKeyValue(key, value) produces a Prometheus metric tag (label) — it must have few distinct values (< 100). Example: product.category=electronics. These appear as dimensions in Prometheus time series. highCardinalityKeyValue(key, value) produces a span attribute only — it is never added to the Prometheus metric. Example: product.id=12345. High-cardinality values in Prometheus labels cause cardinality explosion; in span attributes they are fine because the trace backend indexes them efficiently (by traceId). Using the wrong method — putting high-cardinality data in lowCardinalityKeyValue — causes Prometheus OOM.","correct":"lowCardinalityKeyValue(key, value) produces a Prometheus metric tag (label) — it must have few distinct values (< 100).","distractors":["Each unique combination of label values in a Prometheus metric creates one time series in Prometheus's memory.","An exemplar is a sample data point attached to a Prometheus histogram/summary observation that carries additional metadata — specifically the traceId of the request that contributed to that observation.","MeterRegistry.counter(\"name\").increment() creates a metric only — no trace span, no MDC context."],"distractorIds":["spring/observability_and_tracing#094a9fa1b51c","spring/observability_and_tracing#3de05a54fca0","spring/observability_and_tracing#5a3ce08b9666"],"concepts":["lowcardinalitykeyvalue","difference","distinct","observation","prometheus","tag"],"questionMd":"What is the difference between `lowCardinalityKeyValue` and `highCardinalityKeyValue` in the Observation API?","correctMd":"`lowCardinalityKeyValue(key, value)` produces a Prometheus metric tag (label) — it must have few distinct values (< 100).","answerFullMd":"`lowCardinalityKeyValue(key, value)` produces a Prometheus metric tag (label) — it must have few distinct values (< 100). Example: `product.category=electronics`. These appear as dimensions in Prometheus time series. `highCardinalityKeyValue(key, value)` produces a span attribute only — it is never added to the Prometheus metric. Example: `product.id=12345`. High-cardinality values in Prometheus labels cause cardinality explosion; in span attributes they are fine because the trace backend indexes them efficiently (by `traceId`). Using the wrong method — putting high-cardinality data in `lowCardinalityKeyValue` — causes Prometheus OOM.","distractorsMd":[null,"An exemplar is a sample data point attached to a Prometheus histogram/summary observation that carries additional metadata — specifically the `traceId` of the request that contributed to that observation.","`MeterRegistry.counter(\"name\").increment()` creates a metric only — no trace span, no MDC context."]},{"id":"spring/observability_and_tracing#c1bb909e0d84","section":"spring","module":"spring/observability_and_tracing","moduleName":"observability and tracing","sourceFile":"README.md","difficulty":"advanced","question":"How do you ensure the traceId appears in every log line, including logs from async methods?","answerFull":"Micrometer Tracing auto-populates MDC keys traceId and spanId for the duration of every span (HTTP request, JDBC call, Observation block). For synchronous code, the MDC is automatically populated and cleared. For async code: the trace context is ThreadLocal-based — it must be explicitly propagated to the async thread. Use ContextExecutorService.wrap(...) for executor-based async (§6.7). For Spring's @Async: configure a DelegatingContextTaskExecutor or use spring.threads.virtual.enabled=true (Spring Boot 3.2 with virtual thread context propagation). Verify: add %X{traceId} to your Logback pattern and write an integration test that calls an async service method and checks that the log output contains a non-empty traceId.","correct":"Micrometer Tracing auto-populates MDC keys traceId and spanId for the duration of every span (HTTP request, JDBC call, Observation block).","distractors":["Traditional logging: log.info(\"Order 12345 created for user john@example.com in 245ms\") — a human-readable string.","Spring Batch 5 + Micrometer Tracing (Spring Boot 3.1+) auto-creates spans for: JobExecution (root span), each StepExecution (child span), and each chunk (child of step).","Add dependencies: micrometer-tracing-bridge-otel + opentelemetry-exporter-otlp."],"distractorIds":["spring/observability_and_tracing#36302b6ca549","spring/observability_and_tracing#ae7d360d489c","spring/observability_and_tracing#20e5bee3b54c"],"concepts":["spanid","traceid","populates","ensure","mdc","jdbc"],"questionMd":"How do you ensure the `traceId` appears in every log line, including logs from async methods?","correctMd":"Micrometer Tracing auto-populates MDC keys `traceId` and `spanId` for the duration of every span (HTTP request, JDBC call, Observation block).","answerFullMd":"Micrometer Tracing auto-populates MDC keys `traceId` and `spanId` for the duration of every span (HTTP request, JDBC call, Observation block). For synchronous code, the MDC is automatically populated and cleared. For async code: the trace context is `ThreadLocal`-based — it must be explicitly propagated to the async thread. Use `ContextExecutorService.wrap(...)` for executor-based async (§6.7). For Spring's `@Async`: configure a `DelegatingContextTaskExecutor` or use `spring.threads.virtual.enabled=true` (Spring Boot 3.2 with virtual thread context propagation). Verify: add `%X{traceId}` to your Logback pattern and write an integration test that calls an async service method and checks that the log output contains a non-empty `traceId`.","distractorsMd":["Traditional logging: `log.info(\"Order 12345 created for user john@example.com in 245ms\")` — a human-readable string.","Spring Batch 5 + Micrometer Tracing (Spring Boot 3.1+) auto-creates spans for: `JobExecution` (root span), each `StepExecution` (child span), and each chunk (child of step).","Add dependencies: `micrometer-tracing-bridge-otel` + `opentelemetry-exporter-otlp`."]},{"id":"spring/observability_and_tracing#5b01f8bdd0ba","section":"spring","module":"spring/observability_and_tracing","moduleName":"observability and tracing","sourceFile":"README.md","difficulty":"advanced","question":"What are SLIs, SLOs, and how does Micrometer help implement them?","answerFull":"SLI (Service Level Indicator) is a metric that measures service quality — e.g., p99 latency, error rate. SLO (Service Level Objective) is the target — e.g., \"99% of requests complete in < 500ms\". Micrometer provides: http.server.requests histogram (for latency SLIs), http.server.requests{outcome=SERVER_ERROR} counter (for error rate SLIs). In Prometheus/Grafana: histogram_quantile(0.99, rate(http_server_requests_seconds_bucket[5m])) computes p99 latency. An alerting rule fires when p99 > 500ms over 5 minutes — violating the SLO. Spring Boot Actuator's automatic http.server.requests instrumentation provides SLI data with zero custom code; SLO alerting lives in Prometheus/Grafana.","correct":"SLI (Service Level Indicator) is a metric that measures service quality — e.g., p99 latency, error rate.","distractors":["Add dependencies: micrometer-tracing-bridge-otel + opentelemetry-exporter-otlp.","Spring Batch 5 + Micrometer Tracing (Spring Boot 3.1+) auto-creates spans for: JobExecution (root span), each StepExecution (child span), and each chunk (child of step).","Micrometer Tracing auto-populates MDC keys traceId and spanId for the duration of every span (HTTP request, JDBC call, Observation block)."],"distractorIds":["spring/observability_and_tracing#20e5bee3b54c","spring/observability_and_tracing#ae7d360d489c","spring/observability_and_tracing#c1bb909e0d84"],"concepts":["slos","sli","indicator","help","micrometer","p99"],"answerFullMd":"SLI (Service Level Indicator) is a metric that measures service quality — e.g., p99 latency, error rate. SLO (Service Level Objective) is the target — e.g., \"99% of requests complete in < 500ms\". Micrometer provides: `http.server.requests` histogram (for latency SLIs), `http.server.requests{outcome=SERVER_ERROR}` counter (for error rate SLIs). In Prometheus/Grafana: `histogram_quantile(0.99, rate(http_server_requests_seconds_bucket[5m]))` computes p99 latency. An alerting rule fires when p99 > 500ms over 5 minutes — violating the SLO. Spring Boot Actuator's automatic `http.server.requests` instrumentation provides SLI data with zero custom code; SLO alerting lives in Prometheus/Grafana.","distractorsMd":["Add dependencies: `micrometer-tracing-bridge-otel` + `opentelemetry-exporter-otlp`.","Spring Batch 5 + Micrometer Tracing (Spring Boot 3.1+) auto-creates spans for: `JobExecution` (root span), each `StepExecution` (child span), and each chunk (child of step).","Micrometer Tracing auto-populates MDC keys `traceId` and `spanId` for the duration of every span (HTTP request, JDBC call, Observation block)."]},{"id":"spring/observability_and_tracing#ae7d360d489c","section":"spring","module":"spring/observability_and_tracing","moduleName":"observability and tracing","sourceFile":"README.md","difficulty":"advanced","question":"How would you trace a Spring Batch job end-to-end?","answerFull":"Spring Batch 5 + Micrometer Tracing (Spring Boot 3.1+) auto-creates spans for: JobExecution (root span), each StepExecution (child span), and each chunk (child of step). The job trace shows: total job duration, per-step duration, and identifies slow steps. To add business context: use Observation.createNotStarted(\"inventory.check\", registry).observe(...) inside ItemProcessor — creates a child span of the current step span. The full trace in Jaeger shows: importJob (root) → importStep → chunk-0 → inventory.check × N. Configure management.tracing.sampling.probability=1.0 for scheduled batch jobs (they're low-frequency, so 100% sampling is affordable).","correct":"Spring Batch 5 + Micrometer Tracing (Spring Boot 3.1+) auto-creates spans for: JobExecution (root span), each StepExecution (child span), and each chunk (child of step).","distractors":["traceparent is the W3C Trace Context standard for distributed trace propagation.","Micrometer Tracing uses a context propagation mechanism (backed by io.micrometer:context-propagation) that captures all registered context values (trace context, MDC, Reactor context) into a ContextSnapshot.","Micrometer Tracing auto-populates MDC keys traceId and spanId for the duration of every span (HTTP request, JDBC call, Observation block)."],"distractorIds":["spring/observability_and_tracing#c97daa2aca8d","spring/observability_and_tracing#580cc87ff302","spring/observability_and_tracing#c1bb909e0d84"],"concepts":["jobexecution","stepexecution","spans","micrometer","chunk","job"],"correctMd":"Spring Batch 5 + Micrometer Tracing (Spring Boot 3.1+) auto-creates spans for: `JobExecution` (root span), each `StepExecution` (child span), and each chunk (child of step).","answerFullMd":"Spring Batch 5 + Micrometer Tracing (Spring Boot 3.1+) auto-creates spans for: `JobExecution` (root span), each `StepExecution` (child span), and each chunk (child of step). The job trace shows: total job duration, per-step duration, and identifies slow steps. To add business context: use `Observation.createNotStarted(\"inventory.check\", registry).observe(...)` inside `ItemProcessor` — creates a child span of the current step span. The full trace in Jaeger shows: `importJob` (root) → `importStep` → `chunk-0` → `inventory.check` × N. Configure `management.tracing.sampling.probability=1.0` for scheduled batch jobs (they're low-frequency, so 100% sampling is affordable).","distractorsMd":["`traceparent` is the W3C Trace Context standard for distributed trace propagation.","Micrometer Tracing uses a context propagation mechanism (backed by `io.micrometer:context-propagation`) that captures all registered context values (trace context, MDC, Reactor context) into a `ContextSnapshot`.","Micrometer Tracing auto-populates MDC keys `traceId` and `spanId` for the duration of every span (HTTP request, JDBC call, Observation block)."]},{"id":"spring/observability_and_tracing#1dd0ea8737fa","section":"spring","module":"spring/observability_and_tracing","moduleName":"observability and tracing","sourceFile":"README.md","difficulty":"advanced","question":"Describe a production incident where observability tools helped diagnose the root cause.","answerFull":"Scenario: Black Friday sale, checkout p99 latency spikes from 180ms to 4.2 seconds at 18:35. Grafana alert fires. Diagnosis in 4 minutes: (1) Grafana dashboard: http.server.requests{uri=/checkout} p99 histogram shows spike at 18:35; (2) exemplar on the spike bucket → jumps to a Tempo trace for traceId=abc123; (3) trace shows: checkout span 4.1s, JDBC inventory.check span 3.8s, DB connection await span 3.5s; (4) HikariCP metric hikaricp.connections.pending shows pool exhaustion (all 10 connections busy) at 18:35; (5) Loki query traceId=abc123: log line shows connection acquire timed out after 3500ms — HikariCP pool of 10 connections too small for Black Friday traffic; (6) fix: spring.datasource.hikari.maximum-pool-size=25. Resolved in 7 minutes. Without observability, this would have required hours of reading server logs.","correct":"Scenario: Black Friday sale, checkout p99 latency spikes from 180ms to 4.2 seconds at 18:35.","distractors":["SLI (Service Level Indicator) is a metric that measures service quality — e.g., p99 latency, error rate.","Never expose Actuator endpoints on the public port in production.","Head-based sampling decides at the root span (first service) whether to record the trace — all downstream services inherit the decision."],"distractorIds":["spring/observability_and_tracing#5b01f8bdd0ba","spring/observability_and_tracing#9551d202f28c","spring/observability_and_tracing#bc5d899c7a8a"],"concepts":["180ms","checkout","describe","diagnose","friday","sale"],"answerFullMd":"Scenario: Black Friday sale, checkout p99 latency spikes from 180ms to 4.2 seconds at 18:35. Grafana alert fires. Diagnosis in 4 minutes: (1) Grafana dashboard: `http.server.requests{uri=/checkout}` p99 histogram shows spike at 18:35; (2) exemplar on the spike bucket → jumps to a Tempo trace for `traceId=abc123`; (3) trace shows: checkout span 4.1s, JDBC `inventory.check` span 3.8s, DB connection `await` span 3.5s; (4) HikariCP metric `hikaricp.connections.pending` shows pool exhaustion (all 10 connections busy) at 18:35; (5) Loki query `traceId=abc123`: log line shows `connection acquire timed out after 3500ms` — HikariCP pool of 10 connections too small for Black Friday traffic; (6) fix: `spring.datasource.hikari.maximum-pool-size=25`. Resolved in 7 minutes. Without observability, this would have required hours of reading server logs."},{"id":"spring/request_handling#cfb6d5a2ba88","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"README.md","difficulty":"core","question":"What is the difference between @RequestParam and @PathVariable?","answerFull":"@PathVariable extracts a value from the URI path template segment: GET /users/{id} with @PathVariable Long id extracts the path segment. @RequestParam extracts from query string or form data: GET /users?page=0 with @RequestParam int page. @PathVariable is required by default (missing segment = no route match = 404). @RequestParam can have required=false or defaultValue. Path variables are used for resource identification; query params for filtering, sorting, or pagination.","correct":"@PathVariable extracts a value from the URI path template segment: GET /users/{id} with @PathVariable Long id extracts the path segment.","distractors":["@GetMapping is a composed annotation: @RequestMapping(method = RequestMethod.GET).","@CrossOrigin on a controller or handler method enables CORS for that specific endpoint, configuring allowed origins, methods, and headers per-mapping.","@ExceptionHandler declared in a @Controller class only handles exceptions from that specific controller."],"distractorIds":["spring/request_handling#c206144c4d33","spring/request_handling#c01910a198ec","spring/request_handling#839eeba55c39"],"concepts":["pathvariable","uri","extracts","difference","segment","template"],"correctMd":"`@PathVariable` extracts a value from the URI path template segment: `GET /users/{id}` with `@PathVariable Long id` extracts the path segment.","answerFullMd":"`@PathVariable` extracts a value from the URI path template segment: `GET /users/{id}` with `@PathVariable Long id` extracts the path segment. `@RequestParam` extracts from query string or form data: `GET /users?page=0` with `@RequestParam int page`. `@PathVariable` is required by default (missing segment = no route match = 404). `@RequestParam` can have `required=false` or `defaultValue`. Path variables are used for resource identification; query params for filtering, sorting, or pagination.","distractorsMd":["`@GetMapping` is a composed annotation: `@RequestMapping(method = RequestMethod.GET)`.","`@CrossOrigin` on a controller or handler method enables CORS for that specific endpoint, configuring allowed origins, methods, and headers per-mapping.","`@ExceptionHandler` declared in a `@Controller` class only handles exceptions from that specific controller."]},{"id":"spring/request_handling#58e8c6f01cf5","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"README.md","difficulty":"core","question":"What happens when @Valid validation fails on a @RequestBody?","answerFull":"Spring throws MethodArgumentNotValidException before the controller method executes. The exception contains BindingResult with all field and object errors. Without a @ExceptionHandler for it, Spring's DefaultHandlerExceptionResolver returns a generic 400 response. In a @ControllerAdvice, catch MethodArgumentNotValidException, iterate ex.getBindingResult().getFieldErrors(), and return a structured JSON error response with field names and messages. For @RequestParam and @PathVariable validation, catch ConstraintViolationException (from MethodValidationPostProcessor).","correct":"Spring throws MethodArgumentNotValidException before the controller method executes.","distractors":["ResponseBodyAdvice<T> intercepts controller return values before they are written to the response, allowing modification.","By default the MessageSource throws NoSuchMessageException when no bundle in the fallback chain has the code.","SimpleDateFormat is mutable and not thread-safe, so a shared static instance corrupts output or throws under concurrent requests, and a fixed locale ignores the user's locale."],"distractorIds":["spring/request_handling#dd0d6290656e","spring/request_handling#bfccf13deb9f","spring/request_handling#1ed37d652582"],"concepts":["methodargumentnotvalidexception","requestbody","happens","controller","throws","valid"],"correctMd":"Spring throws `MethodArgumentNotValidException` before the controller method executes.","answerFullMd":"Spring throws `MethodArgumentNotValidException` before the controller method executes. The exception contains `BindingResult` with all field and object errors. Without a `@ExceptionHandler` for it, Spring's `DefaultHandlerExceptionResolver` returns a generic 400 response. In a `@ControllerAdvice`, catch `MethodArgumentNotValidException`, iterate `ex.getBindingResult().getFieldErrors()`, and return a structured JSON error response with field names and messages. For `@RequestParam` and `@PathVariable` validation, catch `ConstraintViolationException` (from `MethodValidationPostProcessor`).","distractorsMd":["`ResponseBodyAdvice<T>` intercepts controller return values before they are written to the response, allowing modification.","By default the `MessageSource` throws `NoSuchMessageException` when no bundle in the fallback chain has the code.","`SimpleDateFormat` is mutable and not thread-safe, so a shared static instance corrupts output or throws under concurrent requests, and a fixed locale ignores the user's locale."]},{"id":"spring/request_handling#dd0d6290656e","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"README.md","difficulty":"core","question":"What is ResponseBodyAdvice and when would you use it?","answerFull":"ResponseBodyAdvice<T> intercepts controller return values before they are written to the response, allowing modification. supports() filters which controller methods or converter types it applies to. beforeBodyWrite() can wrap, replace, or inspect the body. Common uses: wrapping all responses in a standard ApiResponse<T> envelope, adding security headers to response bodies, or filtering sensitive fields. Register by annotating with @ControllerAdvice. Avoid using it to hide controller design problems — prefer returning ApiResponse<T> directly for clarity.","correct":"ResponseBodyAdvice<T> intercepts controller return values before they are written to the response, allowing modification.","distractors":["Spring throws MethodArgumentNotValidException before the controller method executes.","@CrossOrigin on a controller or handler method enables CORS for that specific endpoint, configuring allowed origins, methods, and headers per-mapping.","ProblemDetail (Spring 6.0 / Boot 3.0) is a standardised error response body conforming to RFC 7807 Problem Details for HTTP APIs."],"distractorIds":["spring/request_handling#58e8c6f01cf5","spring/request_handling#c01910a198ec","spring/request_handling#95b68d253171"],"concepts":["responsebodyadvice","modification","intercepts","allowing","controller","written"],"correctMd":"`ResponseBodyAdvice<T>` intercepts controller return values before they are written to the response, allowing modification.","answerFullMd":"`ResponseBodyAdvice<T>` intercepts controller return values before they are written to the response, allowing modification. `supports()` filters which controller methods or converter types it applies to. `beforeBodyWrite()` can wrap, replace, or inspect the body. Common uses: wrapping all responses in a standard `ApiResponse<T>` envelope, adding security headers to response bodies, or filtering sensitive fields. Register by annotating with `@ControllerAdvice`. Avoid using it to hide controller design problems — prefer returning `ApiResponse<T>` directly for clarity.","distractorsMd":["Spring throws `MethodArgumentNotValidException` before the controller method executes.","`@CrossOrigin` on a controller or handler method enables CORS for that specific endpoint, configuring allowed origins, methods, and headers per-mapping.","`ProblemDetail` (Spring 6.0 / Boot 3.0) is a standardised error response body conforming to RFC 7807 Problem Details for HTTP APIs."]},{"id":"spring/request_handling#8c1a7f06d44e","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"README.md","difficulty":"core","question":"How does @RequestBody deserialization work?","answerFull":"RequestResponseBodyMethodProcessor finds the HttpMessageConverter supporting the request's Content-Type and the parameter type. For application/json, MappingJackson2HttpMessageConverter.read() uses Jackson's ObjectMapper to deserialize. Jackson creates the object via the default constructor (or @JsonCreator), then sets fields via setters or direct field access. If the body is invalid JSON, HttpMessageNotReadableException is thrown (400). If the body is valid JSON but fails schema mapping, Jackson may silently ignore unknown fields (default) or throw UnrecognizedPropertyException (if FAIL_ON_UNKNOWN_PROPERTIES is enabled).","correct":"RequestResponseBodyMethodProcessor finds the HttpMessageConverter supporting the request's Content-Type and the parameter type.","distractors":["@RequestBody reads the HTTP request body and deserialises it using a HttpMessageConverter.","@ResponseBody (included in @RestController) instructs Spring MVC to write the return value directly to the HTTP response body via a HttpMessageConverter, bypassing view resolution.","@Valid is a JSR-303 standard annotation that triggers full Bean Validation on the annotated parameter — all constraints on the object's fields are checked."],"distractorIds":["spring/request_handling#5c2ac42929ab","spring/request_handling#ad356f6ad055","spring/request_handling#ca9e5ab40194"],"concepts":["requestresponsebodymethodprocessor","requestbody","supporting","deserialization","httpmessageconverter","finds"],"correctMd":"`RequestResponseBodyMethodProcessor` finds the `HttpMessageConverter` supporting the request's `Content-Type` and the parameter type.","answerFullMd":"`RequestResponseBodyMethodProcessor` finds the `HttpMessageConverter` supporting the request's `Content-Type` and the parameter type. For `application/json`, `MappingJackson2HttpMessageConverter.read()` uses Jackson's `ObjectMapper` to deserialize. Jackson creates the object via the default constructor (or `@JsonCreator`), then sets fields via setters or direct field access. If the body is invalid JSON, `HttpMessageNotReadableException` is thrown (400). If the body is valid JSON but fails schema mapping, Jackson may silently ignore unknown fields (default) or throw `UnrecognizedPropertyException` (if `FAIL_ON_UNKNOWN_PROPERTIES` is enabled).","distractorsMd":["`@RequestBody` reads the HTTP request body and deserialises it using a `HttpMessageConverter`.","`@ResponseBody` (included in `@RestController`) instructs Spring MVC to write the return value directly to the HTTP response body via a `HttpMessageConverter`, bypassing view resolution.","`@Valid` is a JSR-303 standard annotation that triggers full Bean Validation on the annotated parameter — all constraints on the object's fields are checked."]},{"id":"spring/request_handling#ca9e5ab40194","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"README.md","difficulty":"core","question":"What is the difference between @Valid and @Validated?","answerFull":"@Valid is a JSR-303 standard annotation that triggers full Bean Validation on the annotated parameter — all constraints on the object's fields are checked. @Validated is Spring's extension that adds support for validation groups — only constraints belonging to the specified group(s) are checked. This enables different validation rules for create vs update scenarios. @Validated is also required (instead of @Valid) to enable method-level validation via MethodValidationPostProcessor.","correct":"@Valid is a JSR-303 standard annotation that triggers full Bean Validation on the annotated parameter — all constraints on the object's fields are checked.","distractors":["Boot's MessageSourceAutoConfiguration creates a ResourceBundleMessageSource unless you define your own MessageSource bean.","LocaleContextHolder is a ThreadLocal holding the current request's LocaleContext — its Locale and TimeZone — so any code in the request thread can read the ambient locale without it being passed as a parameter.","It is a HandlerInterceptor whose preHandle reads a request parameter (default locale, commonly reconfigured to lang) and, if present, calls LocaleResolver.setLocale(request, response, parsed)."],"distractorIds":["spring/request_handling#471ca77b554c","spring/request_handling#6f333956c861","spring/request_handling#67a2c6f2ea7f"],"concepts":["303","jsr","checked","difference","validated","annotated"],"correctMd":"`@Valid` is a JSR-303 standard annotation that triggers full Bean Validation on the annotated parameter — all constraints on the object's fields are checked.","answerFullMd":"`@Valid` is a JSR-303 standard annotation that triggers full Bean Validation on the annotated parameter — all constraints on the object's fields are checked. `@Validated` is Spring's extension that adds support for validation groups — only constraints belonging to the specified group(s) are checked. This enables different validation rules for create vs update scenarios. `@Validated` is also required (instead of `@Valid`) to enable method-level validation via `MethodValidationPostProcessor`.","distractorsMd":["Boot's `MessageSourceAutoConfiguration` creates a `ResourceBundleMessageSource` unless you define your own `MessageSource` bean.","`LocaleContextHolder` is a `ThreadLocal` holding the current request's `LocaleContext` — its `Locale` and `TimeZone` — so any code in the request thread can read the ambient locale without it being passed as a parameter.","It is a `HandlerInterceptor` whose `preHandle` reads a request parameter (default `locale`, commonly reconfigured to `lang`) and, if present, calls `LocaleResolver.setLocale(request, response, parsed)`."]},{"id":"spring/request_handling#b4e3ae8bedf4","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"README.md","difficulty":"intermediate","question":"How does HandlerMethodArgumentResolver allow extending Spring MVC?","answerFull":"HandlerMethodArgumentResolver with supportsParameter() returning true for a custom annotation/type and resolveArgument() extracting the value from the NativeWebRequest. Register via WebMvcConfigurer.addArgumentResolvers(). Spring MVC calls supportsParameter() for each registered resolver in order; the first match wins. Built-in resolvers handle @RequestBody, @PathVariable, Principal, HttpSession, Pageable (Spring Data). Custom resolvers cover authenticated user injection, tenant context, or any cross-cutting parameter pattern.","correct":"HandlerMethodArgumentResolver with supportsParameter() returning true for a custom annotation/type and resolveArgument() extracting the value from the NativeWebRequest.","distractors":["Spring ships four: AcceptHeader, Session, Cookie, and Fixed.","By default Hibernate Validator interpolates from ValidationMessages.properties on the classpath, independent of Spring's locale.","@GetMapping is a composed annotation: @RequestMapping(method = RequestMethod.GET)."],"distractorIds":["spring/request_handling#9f2ddd49f157","spring/request_handling#3bc97f5267ee","spring/request_handling#c206144c4d33"],"concepts":["nativewebrequest","extending","handlermethodargumentresolver","resolveargument","supportsparameter","extracting"],"correctMd":"`HandlerMethodArgumentResolver` with `supportsParameter()` returning true for a custom annotation/type and `resolveArgument()` extracting the value from the `NativeWebRequest`.","answerFullMd":"`HandlerMethodArgumentResolver` with `supportsParameter()` returning true for a custom annotation/type and `resolveArgument()` extracting the value from the `NativeWebRequest`. Register via `WebMvcConfigurer.addArgumentResolvers()`. Spring MVC calls `supportsParameter()` for each registered resolver in order; the first match wins. Built-in resolvers handle `@RequestBody`, `@PathVariable`, `Principal`, `HttpSession`, `Pageable` (Spring Data). Custom resolvers cover authenticated user injection, tenant context, or any cross-cutting parameter pattern.","distractorsMd":[null,"By default Hibernate Validator interpolates from `ValidationMessages.properties` on the classpath, independent of Spring's locale.","`@GetMapping` is a composed annotation: `@RequestMapping(method = RequestMethod.GET)`."]},{"id":"spring/request_handling#839eeba55c39","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between @ExceptionHandler in a controller vs @ControllerAdvice?","answerFull":"@ExceptionHandler declared in a @Controller class only handles exceptions from that specific controller. @ControllerAdvice (or @RestControllerAdvice) applies its @ExceptionHandler methods globally to all controllers matching its scope (all by default, or limited by basePackages, annotations, assignableTypes). Global exception handling should always be in @ControllerAdvice for consistent error responses across the API. Controller-local handlers are only useful when one controller has genuinely unique exception handling requirements.","correct":"@ExceptionHandler declared in a @Controller class only handles exceptions from that specific controller.","distractors":["Move the text into messages_xx.properties keyed by a code, and have controllers/exceptions carry the code (plus args) instead of literal prose.","Spring throws MethodArgumentNotValidException before the controller method executes.","@CrossOrigin on a controller or handler method enables CORS for that specific endpoint, configuring allowed origins, methods, and headers per-mapping."],"distractorIds":["spring/request_handling#4b832e1503fa","spring/request_handling#58e8c6f01cf5","spring/request_handling#c01910a198ec"],"concepts":["controlleradvice","exceptionhandler","difference","exceptions","declared","handles"],"correctMd":"`@ExceptionHandler` declared in a `@Controller` class only handles exceptions from that specific controller.","answerFullMd":"`@ExceptionHandler` declared in a `@Controller` class only handles exceptions from that specific controller. `@ControllerAdvice` (or `@RestControllerAdvice`) applies its `@ExceptionHandler` methods globally to all controllers matching its scope (all by default, or limited by `basePackages`, `annotations`, `assignableTypes`). Global exception handling should always be in `@ControllerAdvice` for consistent error responses across the API. Controller-local handlers are only useful when one controller has genuinely unique exception handling requirements.","distractorsMd":["Move the text into `messages_xx.properties` keyed by a code, and have controllers/exceptions carry the code (plus args) instead of literal prose.","Spring throws `MethodArgumentNotValidException` before the controller method executes.","`@CrossOrigin` on a controller or handler method enables CORS for that specific endpoint, configuring allowed origins, methods, and headers per-mapping."]},{"id":"spring/request_handling#7437c8852572","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"README.md","difficulty":"intermediate","question":"How do you handle multipart file uploads in Spring MVC?","answerFull":"Annotate the method with @PostMapping(consumes = MediaType.MULTIPART_FORM_DATA_VALUE). Declare @RequestParam(\"file\") MultipartFile file as a parameter. Spring's StandardServletMultipartResolver parses the multipart request; RequestPartMethodArgumentResolver resolves MultipartFile. Access file.getOriginalFilename(), file.getContentType(), file.getSize(), file.getInputStream(). For multiple files, use List<MultipartFile>. Configure spring.servlet.multipart.max-file-size and spring.servlet.multipart.max-request-size to prevent OOM from oversized uploads.","correct":"Annotate the method with @PostMapping(consumes = MediaType.MULTIPART_FORM_DATA_VALUE).","distractors":["Spring throws MethodArgumentNotValidException before the controller method executes.","When a message has arguments, Spring runs it through java.text.MessageFormat, where a single quote is an escape character, so L'utilisateur {0} drops the {0} and renders Lutilisateur.","@GetMapping is a composed annotation: @RequestMapping(method = RequestMethod.GET)."],"distractorIds":["spring/request_handling#58e8c6f01cf5","spring/request_handling#cd495e71ad4a","spring/request_handling#c206144c4d33"],"concepts":["mediatype","postmapping","uploads","multipart","consumes","annotate"],"correctMd":"Annotate the method with `@PostMapping(consumes = MediaType.MULTIPART_FORM_DATA_VALUE)`.","answerFullMd":"Annotate the method with `@PostMapping(consumes = MediaType.MULTIPART_FORM_DATA_VALUE)`. Declare `@RequestParam(\"file\") MultipartFile file` as a parameter. Spring's `StandardServletMultipartResolver` parses the multipart request; `RequestPartMethodArgumentResolver` resolves `MultipartFile`. Access `file.getOriginalFilename()`, `file.getContentType()`, `file.getSize()`, `file.getInputStream()`. For multiple files, use `List<MultipartFile>`. Configure `spring.servlet.multipart.max-file-size` and `spring.servlet.multipart.max-request-size` to prevent OOM from oversized uploads.","distractorsMd":["Spring throws `MethodArgumentNotValidException` before the controller method executes.","When a message has arguments, Spring runs it through `java.text.MessageFormat`, where a single quote is an escape character, so `L'utilisateur {0}` drops the `{0}` and renders `Lutilisateur`.","`@GetMapping` is a composed annotation: `@RequestMapping(method = RequestMethod.GET)`."]},{"id":"spring/request_handling#c206144c4d33","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between @RequestMapping(method=GET) and @GetMapping?","answerFull":"@GetMapping is a composed annotation: @RequestMapping(method = RequestMethod.GET). It is shorter, more readable, and statically typed (IDE warns if you use @GetMapping with method = RequestMethod.POST). All @GetMapping, @PostMapping, @PutMapping, @PatchMapping, @DeleteMapping are composed annotations added in Spring 4.3. Use them instead of @RequestMapping with method= for all new code. Reserve @RequestMapping for class-level base path declarations where the method is not yet known.","correct":"@GetMapping is a composed annotation: @RequestMapping(method = RequestMethod.GET).","distractors":["Annotate the method with @PostMapping(consumes = MediaType.MULTIPART_FORM_DATA_VALUE).","HandlerMethodArgumentResolver with supportsParameter() returning true for a custom annotation/type and resolveArgument() extracting the value from the NativeWebRequest.","@CrossOrigin on a controller or handler method enables CORS for that specific endpoint, configuring allowed origins, methods, and headers per-mapping."],"distractorIds":["spring/request_handling#7437c8852572","spring/request_handling#b4e3ae8bedf4","spring/request_handling#c01910a198ec"],"concepts":["requestmethod","getmapping","composed","requestmapping","difference","annotation"],"correctMd":"`@GetMapping` is a composed annotation: `@RequestMapping(method = RequestMethod.GET)`.","answerFullMd":"`@GetMapping` is a composed annotation: `@RequestMapping(method = RequestMethod.GET)`. It is shorter, more readable, and statically typed (IDE warns if you use `@GetMapping` with `method = RequestMethod.POST`). All `@GetMapping`, `@PostMapping`, `@PutMapping`, `@PatchMapping`, `@DeleteMapping` are composed annotations added in Spring 4.3. Use them instead of `@RequestMapping` with `method=` for all new code. Reserve `@RequestMapping` for class-level base path declarations where the method is not yet known.","distractorsMd":["Annotate the method with `@PostMapping(consumes = MediaType.MULTIPART_FORM_DATA_VALUE)`.","`HandlerMethodArgumentResolver` with `supportsParameter()` returning true for a custom annotation/type and `resolveArgument()` extracting the value from the `NativeWebRequest`.","`@CrossOrigin` on a controller or handler method enables CORS for that specific endpoint, configuring allowed origins, methods, and headers per-mapping."]},{"id":"spring/request_handling#95b68d253171","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"README.md","difficulty":"advanced","question":"What is ProblemDetail (RFC 7807) and how do you return it from a Spring MVC controller in Boot 3.x?","answerFull":"ProblemDetail (Spring 6.0 / Boot 3.0) is a standardised error response body conforming to RFC 7807 Problem Details for HTTP APIs. It includes: type (URI identifier of the problem type), title (human-readable summary), status (HTTP status code), detail (specific problem description for this occurrence), instance (URI of the specific request). Enable it with spring.mvc.problemdetails.enabled=true. All built-in Spring exceptions (MethodArgumentNotValidException, HttpMessageNotReadableException, etc.) are automatically mapped to ProblemDetail responses. Throw ResponseStatusException with a problem detail or implement ErrorResponseException for custom problems: java // Boot 3.x: throw a structured problem detail throw new ResponseStatusException(HttpStatus.NOT_FOUND, \"Order \" + id + \" not found\"); // Or use ErrorResponseException for richer customisation: // class OrderNotFoundException extends ErrorResponseException { ... }","correct":"ProblemDetail (Spring 6.0 / Boot 3.0) is a standardised error response body conforming to RFC 7807 Problem Details for HTTP APIs.","distractors":["It is a HandlerInterceptor whose preHandle reads a request parameter (default locale, commonly reconfigured to lang) and, if present, calls LocaleResolver.setLocale(request, response, parsed).","Spring throws MethodArgumentNotValidException before the controller method executes.","ResponseBodyAdvice<T> intercepts controller return values before they are written to the response, allowing modification."],"distractorIds":["spring/request_handling#67a2c6f2ea7f","spring/request_handling#58e8c6f01cf5","spring/request_handling#dd0d6290656e"],"concepts":["conforming","standardised","problemdetail","7807","rfc","details"],"questionMd":"What is `ProblemDetail` (RFC 7807) and how do you return it from a Spring MVC controller in Boot 3.x?","correctMd":"`ProblemDetail` (Spring 6.0 / Boot 3.0) is a standardised error response body conforming to RFC 7807 Problem Details for HTTP APIs.","answerFullMd":"`ProblemDetail` (Spring 6.0 / Boot 3.0) is a standardised error response body conforming to RFC 7807 Problem Details for HTTP APIs. It includes: `type` (URI identifier of the problem type), `title` (human-readable summary), `status` (HTTP status code), `detail` (specific problem description for this occurrence), `instance` (URI of the specific request). Enable it with `spring.mvc.problemdetails.enabled=true`. All built-in Spring exceptions (`MethodArgumentNotValidException`, `HttpMessageNotReadableException`, etc.) are automatically mapped to `ProblemDetail` responses. Throw `ResponseStatusException` with a problem detail or implement `ErrorResponseException` for custom problems: ```java // Boot 3.x: throw a structured problem detail throw new ResponseStatusException(HttpStatus.NOT_FOUND, \"Order \" + id + \" not found\"); // Or use ErrorResponseException for richer customisation: // class OrderNotFoundException extends ErrorResponseException { ... } ```","distractorsMd":["It is a `HandlerInterceptor` whose `preHandle` reads a request parameter (default `locale`, commonly reconfigured to `lang`) and, if present, calls `LocaleResolver.setLocale(request, response, parsed)`.","Spring throws `MethodArgumentNotValidException` before the controller method executes.","`ResponseBodyAdvice<T>` intercepts controller return values before they are written to the response, allowing modification."]},{"id":"spring/request_handling#ad356f6ad055","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"README.md","difficulty":"advanced","question":"How does @ResponseBody work with message converters, and what determines which converter is used?","answerFull":"@ResponseBody (included in @RestController) instructs Spring MVC to write the return value directly to the HTTP response body via a HttpMessageConverter, bypassing view resolution. The converter is selected by content negotiation: (1) Check the Accept header from the client; (2) Match against the produces attribute of @RequestMapping; (3) Find the first registered HttpMessageConverter that supports the return type AND the accepted media type. Built-in converters (in order): ByteArrayHttpMessageConverter → StringHttpMessageConverter → MappingJackson2HttpMessageConverter (for JSON, if Jackson is on classpath) → others. If no converter matches, Spring returns 406 Not Acceptable. Custom converters: implement HttpMessageConverter<T> and register via WebMvcConfigurer.configureMessageConverters().","correct":"@ResponseBody (included in @RestController) instructs Spring MVC to write the return value directly to the HTTP response body via a HttpMessageConverter, bypassing view resolution.","distractors":["By default Hibernate Validator interpolates from ValidationMessages.properties on the classpath, independent of Spring's locale.","RequestResponseBodyMethodProcessor finds the HttpMessageConverter supporting the request's Content-Type and the parameter type.","When a message has arguments, Spring runs it through java.text.MessageFormat, where a single quote is an escape character, so L'utilisateur {0} drops the {0} and renders Lutilisateur."],"distractorIds":["spring/request_handling#3bc97f5267ee","spring/request_handling#8c1a7f06d44e","spring/request_handling#cd495e71ad4a"],"concepts":["converters","converter","responsebody","restcontroller","bypassing","httpmessageconverter"],"questionMd":"How does `@ResponseBody` work with message converters, and what determines which converter is used?","correctMd":"`@ResponseBody` (included in `@RestController`) instructs Spring MVC to write the return value directly to the HTTP response body via a `HttpMessageConverter`, bypassing view resolution.","answerFullMd":"`@ResponseBody` (included in `@RestController`) instructs Spring MVC to write the return value directly to the HTTP response body via a `HttpMessageConverter`, bypassing view resolution. The converter is selected by content negotiation: (1) Check the `Accept` header from the client; (2) Match against the `produces` attribute of `@RequestMapping`; (3) Find the first registered `HttpMessageConverter` that supports the return type AND the accepted media type. Built-in converters (in order): `ByteArrayHttpMessageConverter` → `StringHttpMessageConverter` → `MappingJackson2HttpMessageConverter` (for JSON, if Jackson is on classpath) → others. If no converter matches, Spring returns `406 Not Acceptable`. Custom converters: implement `HttpMessageConverter<T>` and register via `WebMvcConfigurer.configureMessageConverters()`.","distractorsMd":["By default Hibernate Validator interpolates from `ValidationMessages.properties` on the classpath, independent of Spring's locale.","`RequestResponseBodyMethodProcessor` finds the `HttpMessageConverter` supporting the request's `Content-Type` and the parameter type.","When a message has arguments, Spring runs it through `java.text.MessageFormat`, where a single quote is an escape character, so `L'utilisateur {0}` drops the `{0}` and renders `Lutilisateur`."]},{"id":"spring/request_handling#5c2ac42929ab","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"README.md","difficulty":"advanced","question":"What is @RequestBody deserialization and what exception is thrown for malformed JSON?","answerFull":"@RequestBody reads the HTTP request body and deserialises it using a HttpMessageConverter. For JSON bodies, MappingJackson2HttpMessageConverter uses Jackson's ObjectMapper. If the JSON is syntactically invalid (not well-formed JSON), Jackson throws JsonParseException, wrapped by Spring as HttpMessageNotReadableException — resulting in a 400 Bad Request. If the JSON is valid but the type does not match the annotated parameter (e.g., a string where an integer is expected), Jackson throws InvalidDefinitionException or MismatchedInputException, also wrapped as HttpMessageNotReadableException. Handle it in @ExceptionHandler(HttpMessageNotReadableException.class) to return a structured error response with details about the malformed input.","correct":"@RequestBody reads the HTTP request body and deserialises it using a HttpMessageConverter.","distractors":["RequestResponseBodyMethodProcessor finds the HttpMessageConverter supporting the request's Content-Type and the parameter type.","@ResponseBody (included in @RestController) instructs Spring MVC to write the return value directly to the HTTP response body via a HttpMessageConverter, bypassing view resolution.","ProblemDetail (Spring 6.0 / Boot 3.0) is a standardised error response body conforming to RFC 7807 Problem Details for HTTP APIs."],"distractorIds":["spring/request_handling#8c1a7f06d44e","spring/request_handling#ad356f6ad055","spring/request_handling#95b68d253171"],"concepts":["deserialises","malformed","requestbody","deserialization","httpmessageconverter","thrown"],"questionMd":"What is `@RequestBody` deserialization and what exception is thrown for malformed JSON?","correctMd":"`@RequestBody` reads the HTTP request body and deserialises it using a `HttpMessageConverter`.","answerFullMd":"`@RequestBody` reads the HTTP request body and deserialises it using a `HttpMessageConverter`. For JSON bodies, `MappingJackson2HttpMessageConverter` uses Jackson's `ObjectMapper`. If the JSON is syntactically invalid (not well-formed JSON), Jackson throws `JsonParseException`, wrapped by Spring as `HttpMessageNotReadableException` — resulting in a `400 Bad Request`. If the JSON is valid but the type does not match the annotated parameter (e.g., a string where an integer is expected), Jackson throws `InvalidDefinitionException` or `MismatchedInputException`, also wrapped as `HttpMessageNotReadableException`. Handle it in `@ExceptionHandler(HttpMessageNotReadableException.class)` to return a structured error response with details about the malformed input.","distractorsMd":["`RequestResponseBodyMethodProcessor` finds the `HttpMessageConverter` supporting the request's `Content-Type` and the parameter type.","`@ResponseBody` (included in `@RestController`) instructs Spring MVC to write the return value directly to the HTTP response body via a `HttpMessageConverter`, bypassing view resolution.","`ProblemDetail` (Spring 6.0 / Boot 3.0) is a standardised error response body conforming to RFC 7807 Problem Details for HTTP APIs."]},{"id":"spring/request_handling#c01910a198ec","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"README.md","difficulty":"advanced","question":"How does Spring MVC's @CrossOrigin annotation compare to a global CORS configuration?","answerFull":"@CrossOrigin on a controller or handler method enables CORS for that specific endpoint, configuring allowed origins, methods, and headers per-mapping. Global CORS configuration in WebMvcConfigurer.addCorsMappings() applies across all matching URL patterns without modifying any controller. Use global CORS for consistent policy across all endpoints — avoids scatter and ensures the policy is in one place. @CrossOrigin is useful for exceptions: a public endpoint in an otherwise restricted API. Important: @CrossOrigin and WebMvcConfigurer CORS config are additive — both can apply to the same endpoint. Spring Security's CORS integration must also be enabled (http.cors(Customizer.withDefaults())) — Spring MVC CORS config is ignored if Spring Security intercepts the preflight OPTIONS request before it reaches Spring MVC.","correct":"@CrossOrigin on a controller or handler method enables CORS for that specific endpoint, configuring allowed origins, methods, and headers per-mapping.","distractors":["Spring throws MethodArgumentNotValidException before the controller method executes.","The MessageSource tries the most specific bundle first and widens: messages_fr_FR.properties, then messages_fr.properties, then the default messages.properties.","@GetMapping is a composed annotation: @RequestMapping(method = RequestMethod.GET)."],"distractorIds":["spring/request_handling#58e8c6f01cf5","spring/request_handling#06a816debfb3","spring/request_handling#c206144c4d33"],"concepts":["cors","crossorigin","configuring","origins","mvc","mapping"],"questionMd":"How does Spring MVC's `@CrossOrigin` annotation compare to a global CORS configuration?","correctMd":"`@CrossOrigin` on a controller or handler method enables CORS for that specific endpoint, configuring allowed origins, methods, and headers per-mapping.","answerFullMd":"`@CrossOrigin` on a controller or handler method enables CORS for that specific endpoint, configuring allowed origins, methods, and headers per-mapping. Global CORS configuration in `WebMvcConfigurer.addCorsMappings()` applies across all matching URL patterns without modifying any controller. Use global CORS for consistent policy across all endpoints — avoids scatter and ensures the policy is in one place. `@CrossOrigin` is useful for exceptions: a public endpoint in an otherwise restricted API. Important: `@CrossOrigin` and `WebMvcConfigurer` CORS config are additive — both can apply to the same endpoint. Spring Security's CORS integration must also be enabled (`http.cors(Customizer.withDefaults())`) — Spring MVC CORS config is ignored if Spring Security intercepts the preflight `OPTIONS` request before it reaches Spring MVC.","distractorsMd":["Spring throws `MethodArgumentNotValidException` before the controller method executes.","The `MessageSource` tries the most specific bundle first and widens: `messages_fr_FR.properties`, then `messages_fr.properties`, then the default `messages.properties`.","`@GetMapping` is a composed annotation: `@RequestMapping(method = RequestMethod.GET)`."]},{"id":"spring/request_handling#6595bc34dc6e","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"i18n_and_localization.md","difficulty":"core","question":"Why does ?lang=fr with LocaleChangeInterceptor sometimes have no effect?","answerFull":"Because the default AcceptHeaderLocaleResolver is read-only and its setLocale throws UnsupportedOperationException, so the interceptor cannot persist the change. LocaleChangeInterceptor calls LocaleResolver.setLocale, which only mutable resolvers (SessionLocaleResolver, CookieLocaleResolver) implement. Fix by registering a Cookie or Session resolver as the LocaleResolver bean so the switch is stored for subsequent requests.","correct":"Because the default AcceptHeaderLocaleResolver is read-only and its setLocale throws UnsupportedOperationException, so the interceptor cannot persist the change.","distractors":["By default the MessageSource throws NoSuchMessageException when no bundle in the fallback chain has the code.","The Accept-Language header is the standard signal: AcceptHeaderLocaleResolver parses its quality-weighted list and resolves the best match, so a client sending Accept-Language: fr-FR, en;q=0.8 gets French.","Move the text into messages_xx.properties keyed by a code, and have controllers/exceptions carry the code (plus args) instead of literal prose."],"distractorIds":["spring/request_handling#bfccf13deb9f","spring/request_handling#01f22bcdf027","spring/request_handling#4b832e1503fa"],"concepts":["acceptheaderlocaleresolver","setlocale","unsupportedoperationexception","sometimes","interceptor","persist"],"questionMd":"Why does `?lang=fr` with `LocaleChangeInterceptor` sometimes have no effect?","correctMd":"Because the default `AcceptHeaderLocaleResolver` is read-only and its `setLocale` throws `UnsupportedOperationException`, so the interceptor cannot persist the change.","answerFullMd":"Because the default `AcceptHeaderLocaleResolver` is read-only and its `setLocale` throws `UnsupportedOperationException`, so the interceptor cannot persist the change. `LocaleChangeInterceptor` calls `LocaleResolver.setLocale`, which only mutable resolvers (`SessionLocaleResolver`, `CookieLocaleResolver`) implement. Fix by registering a Cookie or Session resolver as the `LocaleResolver` bean so the switch is stored for subsequent requests.","distractorsMd":["By default the `MessageSource` throws `NoSuchMessageException` when no bundle in the fallback chain has the code.","The `Accept-Language` header is the standard signal: `AcceptHeaderLocaleResolver` parses its quality-weighted list and resolves the best match, so a client sending `Accept-Language: fr-FR, en;q=0.8` gets French.","Move the text into `messages_xx.properties` keyed by a code, and have controllers/exceptions carry the code (plus args) instead of literal prose."]},{"id":"spring/request_handling#1ed37d652582","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"i18n_and_localization.md","difficulty":"core","question":"What is the SimpleDateFormat bug in a Spring app and how do you fix it?","answerFull":"SimpleDateFormat is mutable and not thread-safe, so a shared static instance corrupts output or throws under concurrent requests, and a fixed locale ignores the user's locale. Replace it with java.time.DateTimeFormatter, which is immutable and thread-safe, bound to the current locale via .withLocale(LocaleContextHolder.getLocale()). This both eliminates the race and makes formatting locale-aware.","correct":"SimpleDateFormat is mutable and not thread-safe, so a shared static instance corrupts output or throws under concurrent requests, and a fixed locale ignores the user's locale.","distractors":["Spring ships four: AcceptHeader, Session, Cookie, and Fixed.","Use locale-aware formatters bound to the current locale, never string concatenation.","By default Hibernate Validator interpolates from ValidationMessages.properties on the classpath, independent of Spring's locale."],"distractorIds":["spring/request_handling#9f2ddd49f157","spring/request_handling#2db8c719640f","spring/request_handling#3bc97f5267ee"],"concepts":["simpledateformat","corrupts","ignores","locale","bug","fix"],"questionMd":"What is the `SimpleDateFormat` bug in a Spring app and how do you fix it?","correctMd":"`SimpleDateFormat` is mutable and not thread-safe, so a shared static instance corrupts output or throws under concurrent requests, and a fixed locale ignores the user's locale.","answerFullMd":"`SimpleDateFormat` is mutable and not thread-safe, so a shared static instance corrupts output or throws under concurrent requests, and a fixed locale ignores the user's locale. Replace it with `java.time.DateTimeFormatter`, which is immutable and thread-safe, bound to the current locale via `.withLocale(LocaleContextHolder.getLocale())`. This both eliminates the race and makes formatting locale-aware.","distractorsMd":[null,null,"By default Hibernate Validator interpolates from `ValidationMessages.properties` on the classpath, independent of Spring's locale."]},{"id":"spring/request_handling#4b832e1503fa","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"i18n_and_localization.md","difficulty":"core","question":"How do you localize hardcoded strings in controllers and exceptions?","answerFull":"Move the text into messages_xx.properties keyed by a code, and have controllers/exceptions carry the code (plus args) instead of literal prose. Resolve it with messageSource.getMessage(code, args, LocaleContextHolder.getLocale()) — typically in a @ControllerAdvice for exceptions, producing a localized ProblemDetail. The exception stores the code as its message and an args array, so the handler renders it in the user's language.","correct":"Move the text into messages_xx.properties keyed by a code, and have controllers/exceptions carry the code (plus args) instead of literal prose.","distractors":["LocaleContextHolder is a ThreadLocal holding the current request's LocaleContext — its Locale and TimeZone — so any code in the request thread can read the ambient locale without it being passed as a parameter.","The MessageSource tries the most specific bundle first and widens: messages_fr_FR.properties, then messages_fr.properties, then the default messages.properties.","By default Hibernate Validator interpolates from ValidationMessages.properties on the classpath, independent of Spring's locale."],"distractorIds":["spring/request_handling#6f333956c861","spring/request_handling#06a816debfb3","spring/request_handling#3bc97f5267ee"],"concepts":["hardcoded","prose","controllers","args","carry","literal"],"correctMd":"Move the text into `messages_xx.properties` keyed by a code, and have controllers/exceptions carry the code (plus args) instead of literal prose.","answerFullMd":"Move the text into `messages_xx.properties` keyed by a code, and have controllers/exceptions carry the code (plus args) instead of literal prose. Resolve it with `messageSource.getMessage(code, args, LocaleContextHolder.getLocale())` — typically in a `@ControllerAdvice` for exceptions, producing a localized `ProblemDetail`. The exception stores the code as its message and an `args` array, so the handler renders it in the user's language.","distractorsMd":["`LocaleContextHolder` is a `ThreadLocal` holding the current request's `LocaleContext` — its `Locale` and `TimeZone` — so any code in the request thread can read the ambient locale without it being passed as a parameter.","The `MessageSource` tries the most specific bundle first and widens: `messages_fr_FR.properties`, then `messages_fr.properties`, then the default `messages.properties`.","By default Hibernate Validator interpolates from `ValidationMessages.properties` on the classpath, independent of Spring's locale."]},{"id":"spring/request_handling#afcf035fb51b","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"i18n_and_localization.md","difficulty":"core","question":"How is the Spring ApplicationContext related to MessageSource?","answerFull":"ApplicationContext extends the MessageSource interface, so the container itself resolves messages — you can call context.getMessage(...) directly. Boot registers a MessageSource bean (named messageSource) that the context delegates to, and any bean can inject MessageSource or implement MessageSourceAware. This is why localization needs no special infrastructure beyond a properties file and the standard container.","correct":"ApplicationContext extends the MessageSource interface, so the container itself resolves messages — you can call context.getMessage(...) directly.","distractors":["ProblemDetail (Spring 6.0 / Boot 3.0) is a standardised error response body conforming to RFC 7807 Problem Details for HTTP APIs.","The Accept-Language header is the standard signal: AcceptHeaderLocaleResolver parses its quality-weighted list and resolves the best match, so a client sending Accept-Language: fr-FR, en;q=0.8 gets French.","The MessageSource tries the most specific bundle first and widens: messages_fr_FR.properties, then messages_fr.properties, then the default messages.properties."],"distractorIds":["spring/request_handling#95b68d253171","spring/request_handling#01f22bcdf027","spring/request_handling#06a816debfb3"],"concepts":["getmessage","messagesource","resolves","extends","applicationcontext","related"],"questionMd":"How is the Spring `ApplicationContext` related to `MessageSource`?","correctMd":"`ApplicationContext` extends the `MessageSource` interface, so the container itself resolves messages — you can call `context.getMessage(...)` directly.","answerFullMd":"`ApplicationContext` extends the `MessageSource` interface, so the container itself resolves messages — you can call `context.getMessage(...)` directly. Boot registers a `MessageSource` bean (named `messageSource`) that the context delegates to, and any bean can inject `MessageSource` or implement `MessageSourceAware`. This is why localization needs no special infrastructure beyond a properties file and the standard container.","distractorsMd":["`ProblemDetail` (Spring 6.0 / Boot 3.0) is a standardised error response body conforming to RFC 7807 Problem Details for HTTP APIs.","The `Accept-Language` header is the standard signal: `AcceptHeaderLocaleResolver` parses its quality-weighted list and resolves the best match, so a client sending `Accept-Language: fr-FR, en;q=0.8` gets French.","The `MessageSource` tries the most specific bundle first and widens: `messages_fr_FR.properties`, then `messages_fr.properties`, then the default `messages.properties`."]},{"id":"spring/request_handling#cd495e71ad4a","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"i18n_and_localization.md","difficulty":"core","question":"What is the gotcha with single quotes in message templates?","answerFull":"When a message has arguments, Spring runs it through java.text.MessageFormat, where a single quote is an escape character, so L'utilisateur {0} drops the {0} and renders Lutilisateur. You must double the quote: L''utilisateur {0}. The trap is that no-argument lookups skip MessageFormat, so the same string can look correct until an argument is passed.","correct":"When a message has arguments, Spring runs it through java.text.MessageFormat, where a single quote is an escape character, so L'utilisateur {0} drops the {0} and renders Lutilisateur.","distractors":["@ResponseBody (included in @RestController) instructs Spring MVC to write the return value directly to the HTTP response body via a HttpMessageConverter, bypassing view resolution.","By default Hibernate Validator interpolates from ValidationMessages.properties on the classpath, independent of Spring's locale.","Move the text into messages_xx.properties keyed by a code, and have controllers/exceptions carry the code (plus args) instead of literal prose."],"distractorIds":["spring/request_handling#ad356f6ad055","spring/request_handling#3bc97f5267ee","spring/request_handling#4b832e1503fa"],"concepts":["gotcha","lutilisateur","messageformat","quote","utilisateur","renders"],"correctMd":"When a message has arguments, Spring runs it through `java.text.MessageFormat`, where a single quote is an escape character, so `L'utilisateur {0}` drops the `{0}` and renders `Lutilisateur`.","answerFullMd":"When a message has arguments, Spring runs it through `java.text.MessageFormat`, where a single quote is an escape character, so `L'utilisateur {0}` drops the `{0}` and renders `Lutilisateur`. You must double the quote: `L''utilisateur {0}`. The trap is that no-argument lookups skip `MessageFormat`, so the same string can look correct until an argument is passed.","distractorsMd":["`@ResponseBody` (included in `@RestController`) instructs Spring MVC to write the return value directly to the HTTP response body via a `HttpMessageConverter`, bypassing view resolution.","By default Hibernate Validator interpolates from `ValidationMessages.properties` on the classpath, independent of Spring's locale.","Move the text into `messages_xx.properties` keyed by a code, and have controllers/exceptions carry the code (plus args) instead of literal prose."]},{"id":"spring/request_handling#7ead5ca31908","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"i18n_and_localization.md","difficulty":"intermediate","question":"What is the difference between ResourceBundleMessageSource and ReloadableResourceBundleMessageSource?","answerFull":"ResourceBundleMessageSource wraps java.util.ResourceBundle, reads only from the classpath, and caches bundles for the JVM's lifetime, so translations change only on restart. ReloadableResourceBundleMessageSource reads from any Spring Resource (including file:), supports explicit encoding, and reloads edited files on a cacheSeconds interval. Use the reloadable one when translators or ops must edit copy without a redeploy; otherwise the classpath one is Boot's simpler default.","correct":"ResourceBundleMessageSource wraps java.util.ResourceBundle, reads only from the classpath, and caches bundles for the JVM's lifetime, so translations change only on restart.","distractors":["Boot's MessageSourceAutoConfiguration creates a ResourceBundleMessageSource unless you define your own MessageSource bean.","Because the default AcceptHeaderLocaleResolver is read-only and its setLocale throws UnsupportedOperationException, so the interceptor cannot persist the change.","By default Hibernate Validator interpolates from ValidationMessages.properties on the classpath, independent of Spring's locale."],"distractorIds":["spring/request_handling#471ca77b554c","spring/request_handling#6595bc34dc6e","spring/request_handling#3bc97f5267ee"],"concepts":["resourcebundle","bundles","resourcebundlemessagesource","translations","util","classpath"],"questionMd":"What is the difference between `ResourceBundleMessageSource` and `ReloadableResourceBundleMessageSource`?","correctMd":"`ResourceBundleMessageSource` wraps `java.util.ResourceBundle`, reads only from the classpath, and caches bundles for the JVM's lifetime, so translations change only on restart.","answerFullMd":"`ResourceBundleMessageSource` wraps `java.util.ResourceBundle`, reads only from the classpath, and caches bundles for the JVM's lifetime, so translations change only on restart. `ReloadableResourceBundleMessageSource` reads from any Spring `Resource` (including `file:`), supports explicit encoding, and reloads edited files on a `cacheSeconds` interval. Use the reloadable one when translators or ops must edit copy without a redeploy; otherwise the classpath one is Boot's simpler default.","distractorsMd":["Boot's `MessageSourceAutoConfiguration` creates a `ResourceBundleMessageSource` unless you define your own `MessageSource` bean.","Because the default `AcceptHeaderLocaleResolver` is read-only and its `setLocale` throws `UnsupportedOperationException`, so the interceptor cannot persist the change.","By default Hibernate Validator interpolates from `ValidationMessages.properties` on the classpath, independent of Spring's locale."]},{"id":"spring/request_handling#9f2ddd49f157","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"i18n_and_localization.md","difficulty":"intermediate","question":"What are the LocaleResolver implementations and how do they differ?","answerFull":"Spring ships four: AcceptHeader, Session, Cookie, and Fixed. AcceptHeaderLocaleResolver (Boot's default) reads the Accept-Language header and is stateless/read-only; SessionLocaleResolver stores the locale in the HttpSession; CookieLocaleResolver stores it in a cookie surviving across sessions; FixedLocaleResolver forces one constant locale. Only Session and Cookie resolvers support setLocale, so only they work with LocaleChangeInterceptor for user-driven language switching. Choose Accept-Header for stateless APIs and Cookie/Session when users must override their browser language.","correct":"Spring ships four: AcceptHeader, Session, Cookie, and Fixed.","distractors":["Spring throws MethodArgumentNotValidException before the controller method executes.","ProblemDetail (Spring 6.0 / Boot 3.0) is a standardised error response body conforming to RFC 7807 Problem Details for HTTP APIs.","Store all instants in UTC and render them in the user's zone read from LocaleContextHolder.getTimeZone(), which is populated when the resolver implements TimeZoneAwareLocaleContext (Cookie/Session resolvers do)."],"distractorIds":["spring/request_handling#58e8c6f01cf5","spring/request_handling#95b68d253171","spring/request_handling#0c445d4f0504"],"concepts":["acceptheader","localeresolver","ships","cookie","implementations","differ"],"answerFullMd":"Spring ships four: AcceptHeader, Session, Cookie, and Fixed. `AcceptHeaderLocaleResolver` (Boot's default) reads the `Accept-Language` header and is stateless/read-only; `SessionLocaleResolver` stores the locale in the `HttpSession`; `CookieLocaleResolver` stores it in a cookie surviving across sessions; `FixedLocaleResolver` forces one constant locale. Only Session and Cookie resolvers support `setLocale`, so only they work with `LocaleChangeInterceptor` for user-driven language switching. Choose Accept-Header for stateless APIs and Cookie/Session when users must override their browser language.","distractorsMd":["Spring throws `MethodArgumentNotValidException` before the controller method executes.","`ProblemDetail` (Spring 6.0 / Boot 3.0) is a standardised error response body conforming to RFC 7807 Problem Details for HTTP APIs.","Store all instants in UTC and render them in the user's zone read from `LocaleContextHolder.getTimeZone()`, which is populated when the resolver implements `TimeZoneAwareLocaleContext` (Cookie/Session resolvers do)."]},{"id":"spring/request_handling#67a2c6f2ea7f","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"i18n_and_localization.md","difficulty":"intermediate","question":"How does LocaleChangeInterceptor work?","answerFull":"It is a HandlerInterceptor whose preHandle reads a request parameter (default locale, commonly reconfigured to lang) and, if present, calls LocaleResolver.setLocale(request, response, parsed). On ?lang=fr it sets the locale on a mutable resolver, which then persists it (session attribute or cookie) so later requests keep it. It must be registered via WebMvcConfigurer.addInterceptors and paired with a mutable resolver.","correct":"It is a HandlerInterceptor whose preHandle reads a request parameter (default locale, commonly reconfigured to lang) and, if present, calls LocaleResolver.setLocale(request, response, parsed).","distractors":["@Valid is a JSR-303 standard annotation that triggers full Bean Validation on the annotated parameter — all constraints on the object's fields are checked.","LocaleContextHolder is a ThreadLocal holding the current request's LocaleContext — its Locale and TimeZone — so any code in the request thread can read the ambient locale without it being passed as a parameter.","Use locale-aware formatters bound to the current locale, never string concatenation."],"distractorIds":["spring/request_handling#ca9e5ab40194","spring/request_handling#6f333956c861","spring/request_handling#2db8c719640f"],"concepts":["handlerinterceptor","localeresolver","parsed","reconfigured","prehandle","setlocale"],"questionMd":"How does `LocaleChangeInterceptor` work?","correctMd":"It is a `HandlerInterceptor` whose `preHandle` reads a request parameter (default `locale`, commonly reconfigured to `lang`) and, if present, calls `LocaleResolver.setLocale(request, response, parsed)`.","answerFullMd":"It is a `HandlerInterceptor` whose `preHandle` reads a request parameter (default `locale`, commonly reconfigured to `lang`) and, if present, calls `LocaleResolver.setLocale(request, response, parsed)`. On `?lang=fr` it sets the locale on a mutable resolver, which then persists it (session attribute or cookie) so later requests keep it. It must be registered via `WebMvcConfigurer.addInterceptors` and paired with a mutable resolver.","distractorsMd":["`@Valid` is a JSR-303 standard annotation that triggers full Bean Validation on the annotated parameter — all constraints on the object's fields are checked.","`LocaleContextHolder` is a `ThreadLocal` holding the current request's `LocaleContext` — its `Locale` and `TimeZone` — so any code in the request thread can read the ambient locale without it being passed as a parameter.",null]},{"id":"spring/request_handling#6f333956c861","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"i18n_and_localization.md","difficulty":"intermediate","question":"What is LocaleContextHolder and why is it used?","answerFull":"LocaleContextHolder is a ThreadLocal holding the current request's LocaleContext — its Locale and TimeZone — so any code in the request thread can read the ambient locale without it being passed as a parameter. The DispatcherServlet populates it per request from the LocaleResolver before invoking the controller. This is what lets a formatter deep in the call stack render per-locale without threading a Locale argument through every method.","correct":"LocaleContextHolder is a ThreadLocal holding the current request's LocaleContext — its Locale and TimeZone — so any code in the request thread can read the ambient locale without it being passed as a parameter.","distractors":["SimpleDateFormat is mutable and not thread-safe, so a shared static instance corrupts output or throws under concurrent requests, and a fixed locale ignores the user's locale.","It is a HandlerInterceptor whose preHandle reads a request parameter (default locale, commonly reconfigured to lang) and, if present, calls LocaleResolver.setLocale(request, response, parsed).","By default Hibernate Validator interpolates from ValidationMessages.properties on the classpath, independent of Spring's locale."],"distractorIds":["spring/request_handling#1ed37d652582","spring/request_handling#67a2c6f2ea7f","spring/request_handling#3bc97f5267ee"],"concepts":["ambient","localecontext","localecontextholder","timezone","locale","threadlocal"],"questionMd":"What is `LocaleContextHolder` and why is it used?","correctMd":"`LocaleContextHolder` is a `ThreadLocal` holding the current request's `LocaleContext` — its `Locale` and `TimeZone` — so any code in the request thread can read the ambient locale without it being passed as a parameter.","answerFullMd":"`LocaleContextHolder` is a `ThreadLocal` holding the current request's `LocaleContext` — its `Locale` and `TimeZone` — so any code in the request thread can read the ambient locale without it being passed as a parameter. The `DispatcherServlet` populates it per request from the `LocaleResolver` before invoking the controller. This is what lets a formatter deep in the call stack render per-locale without threading a `Locale` argument through every method.","distractorsMd":["`SimpleDateFormat` is mutable and not thread-safe, so a shared static instance corrupts output or throws under concurrent requests, and a fixed locale ignores the user's locale.","It is a `HandlerInterceptor` whose `preHandle` reads a request parameter (default `locale`, commonly reconfigured to `lang`) and, if present, calls `LocaleResolver.setLocale(request, response, parsed)`.","By default Hibernate Validator interpolates from `ValidationMessages.properties` on the classpath, independent of Spring's locale."]},{"id":"spring/request_handling#471ca77b554c","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"i18n_and_localization.md","difficulty":"intermediate","question":"How does Spring Boot auto-configure the MessageSource?","answerFull":"Boot's MessageSourceAutoConfiguration creates a ResourceBundleMessageSource unless you define your own MessageSource bean. It is configured from spring.messages.* — basename (default messages), encoding (UTF-8), cache-duration, and fallback-to-system-locale. So placing messages.properties and messages_fr.properties on the classpath is enough for i18n to work with no Java config. Define an explicit bean only to switch to the reloadable implementation or tune behaviors like use-code-as-default.","correct":"Boot's MessageSourceAutoConfiguration creates a ResourceBundleMessageSource unless you define your own MessageSource bean.","distractors":["By default the MessageSource throws NoSuchMessageException when no bundle in the fallback chain has the code.","ProblemDetail (Spring 6.0 / Boot 3.0) is a standardised error response body conforming to RFC 7807 Problem Details for HTTP APIs.","ResourceBundleMessageSource wraps java.util.ResourceBundle, reads only from the classpath, and caches bundles for the JVM's lifetime, so translations change only on restart."],"distractorIds":["spring/request_handling#bfccf13deb9f","spring/request_handling#95b68d253171","spring/request_handling#7ead5ca31908"],"concepts":["messagesourceautoconfiguration","resourcebundlemessagesource","messagesource","configure","unless","boot"],"questionMd":"How does Spring Boot auto-configure the `MessageSource`?","correctMd":"Boot's `MessageSourceAutoConfiguration` creates a `ResourceBundleMessageSource` unless you define your own `MessageSource` bean.","answerFullMd":"Boot's `MessageSourceAutoConfiguration` creates a `ResourceBundleMessageSource` unless you define your own `MessageSource` bean. It is configured from `spring.messages.*` — `basename` (default `messages`), `encoding` (UTF-8), `cache-duration`, and `fallback-to-system-locale`. So placing `messages.properties` and `messages_fr.properties` on the classpath is enough for i18n to work with no Java config. Define an explicit bean only to switch to the reloadable implementation or tune behaviors like use-code-as-default.","distractorsMd":["By default the `MessageSource` throws `NoSuchMessageException` when no bundle in the fallback chain has the code.","`ProblemDetail` (Spring 6.0 / Boot 3.0) is a standardised error response body conforming to RFC 7807 Problem Details for HTTP APIs.","`ResourceBundleMessageSource` wraps `java.util.ResourceBundle`, reads only from the classpath, and caches bundles for the JVM's lifetime, so translations change only on restart."]},{"id":"spring/request_handling#3bc97f5267ee","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"i18n_and_localization.md","difficulty":"advanced","question":"How do you make Bean Validation messages locale-aware?","answerFull":"By default Hibernate Validator interpolates from ValidationMessages.properties on the classpath, independent of Spring's locale. To route validation through Spring's MessageSource and the resolved locale, define a LocalValidatorFactoryBean and call setValidationMessageSource(messageSource), then reference codes in constraints like @NotBlank(message = \"{user.name.required}\"). Now validation errors come from messages_xx.properties and honor the request's locale like everything else.","correct":"By default Hibernate Validator interpolates from ValidationMessages.properties on the classpath, independent of Spring's locale.","distractors":["The MessageSource tries the most specific bundle first and widens: messages_fr_FR.properties, then messages_fr.properties, then the default messages.properties.","It is a HandlerInterceptor whose preHandle reads a request parameter (default locale, commonly reconfigured to lang) and, if present, calls LocaleResolver.setLocale(request, response, parsed).","LocaleContextHolder is a ThreadLocal holding the current request's LocaleContext — its Locale and TimeZone — so any code in the request thread can read the ambient locale without it being passed as a parameter."],"distractorIds":["spring/request_handling#06a816debfb3","spring/request_handling#67a2c6f2ea7f","spring/request_handling#6f333956c861"],"concepts":["interpolates","validationmessages","hibernate","locale","classpath","validator"],"correctMd":"By default Hibernate Validator interpolates from `ValidationMessages.properties` on the classpath, independent of Spring's locale.","answerFullMd":"By default Hibernate Validator interpolates from `ValidationMessages.properties` on the classpath, independent of Spring's locale. To route validation through Spring's `MessageSource` and the resolved locale, define a `LocalValidatorFactoryBean` and call `setValidationMessageSource(messageSource)`, then reference codes in constraints like `@NotBlank(message = \"{user.name.required}\")`. Now validation errors come from `messages_xx.properties` and honor the request's locale like everything else.","distractorsMd":["The `MessageSource` tries the most specific bundle first and widens: `messages_fr_FR.properties`, then `messages_fr.properties`, then the default `messages.properties`.","It is a `HandlerInterceptor` whose `preHandle` reads a request parameter (default `locale`, commonly reconfigured to `lang`) and, if present, calls `LocaleResolver.setLocale(request, response, parsed)`.","`LocaleContextHolder` is a `ThreadLocal` holding the current request's `LocaleContext` — its `Locale` and `TimeZone` — so any code in the request thread can read the ambient locale without it being passed as a parameter."]},{"id":"spring/request_handling#2db8c719640f","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"i18n_and_localization.md","difficulty":"advanced","question":"How do you format numbers, dates, and currency per locale in Spring?","answerFull":"Use locale-aware formatters bound to the current locale, never string concatenation. Examples are DateTimeFormatter.ofLocalizedDate(FormatStyle.MEDIUM).withLocale(locale), NumberFormat.getCurrencyInstance(locale), and Spring's @DateTimeFormat/@NumberFormat on bound fields. Spring's FormattingConversionService reads LocaleContextHolder, so binding and rendering pick up the request locale automatically. This yields 1 234,56 EUR in fr_FR versus $1,234.56 in en_US from the same value.","correct":"Use locale-aware formatters bound to the current locale, never string concatenation.","distractors":["ProblemDetail (Spring 6.0 / Boot 3.0) is a standardised error response body conforming to RFC 7807 Problem Details for HTTP APIs.","@ResponseBody (included in @RestController) instructs Spring MVC to write the return value directly to the HTTP response body via a HttpMessageConverter, bypassing view resolution.","By default Hibernate Validator interpolates from ValidationMessages.properties on the classpath, independent of Spring's locale."],"distractorIds":["spring/request_handling#95b68d253171","spring/request_handling#ad356f6ad055","spring/request_handling#3bc97f5267ee"],"concepts":["formatters","concatenation","currency","dates","locale","aware"],"answerFullMd":"Use locale-aware formatters bound to the current locale, never string concatenation. Examples are `DateTimeFormatter.ofLocalizedDate(FormatStyle.MEDIUM).withLocale(locale)`, `NumberFormat.getCurrencyInstance(locale)`, and Spring's `@DateTimeFormat`/`@NumberFormat` on bound fields. Spring's `FormattingConversionService` reads `LocaleContextHolder`, so binding and rendering pick up the request locale automatically. This yields `1 234,56 EUR` in `fr_FR` versus `$1,234.56` in `en_US` from the same value.","distractorsMd":["`ProblemDetail` (Spring 6.0 / Boot 3.0) is a standardised error response body conforming to RFC 7807 Problem Details for HTTP APIs.","`@ResponseBody` (included in `@RestController`) instructs Spring MVC to write the return value directly to the HTTP response body via a `HttpMessageConverter`, bypassing view resolution.","By default Hibernate Validator interpolates from `ValidationMessages.properties` on the classpath, independent of Spring's locale."]},{"id":"spring/request_handling#0c445d4f0504","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"i18n_and_localization.md","difficulty":"advanced","question":"How do you handle per-user timezone?","answerFull":"Store all instants in UTC and render them in the user's zone read from LocaleContextHolder.getTimeZone(), which is populated when the resolver implements TimeZoneAwareLocaleContext (Cookie/Session resolvers do). Convert with instant.atZone(LocaleContextHolder.getTimeZone().toZoneId()) at display time. This keeps storage unambiguous while showing each user their local wall-clock time.","correct":"Store all instants in UTC and render them in the user's zone read from LocaleContextHolder.getTimeZone(), which is populated when the resolver implements TimeZoneAwareLocaleContext (Cookie/Session resolvers do).","distractors":["WebFlux cannot rely on the thread-bound LocaleContextHolder because operators switch threads.","LocaleContextHolder is a ThreadLocal holding the current request's LocaleContext — its Locale and TimeZone — so any code in the request thread can read the ambient locale without it being passed as a parameter.","SimpleDateFormat is mutable and not thread-safe, so a shared static instance corrupts output or throws under concurrent requests, and a fixed locale ignores the user's locale."],"distractorIds":["spring/request_handling#4f5e80d593e1","spring/request_handling#6f333956c861","spring/request_handling#1ed37d652582"],"concepts":["gettimezone","instants","populated","timezoneawarelocalecontext","localecontextholder","render"],"correctMd":"Store all instants in UTC and render them in the user's zone read from `LocaleContextHolder.getTimeZone()`, which is populated when the resolver implements `TimeZoneAwareLocaleContext` (Cookie/Session resolvers do).","answerFullMd":"Store all instants in UTC and render them in the user's zone read from `LocaleContextHolder.getTimeZone()`, which is populated when the resolver implements `TimeZoneAwareLocaleContext` (Cookie/Session resolvers do). Convert with `instant.atZone(LocaleContextHolder.getTimeZone().toZoneId())` at display time. This keeps storage unambiguous while showing each user their local wall-clock time.","distractorsMd":["WebFlux cannot rely on the thread-bound `LocaleContextHolder` because operators switch threads.","`LocaleContextHolder` is a `ThreadLocal` holding the current request's `LocaleContext` — its `Locale` and `TimeZone` — so any code in the request thread can read the ambient locale without it being passed as a parameter.","`SimpleDateFormat` is mutable and not thread-safe, so a shared static instance corrupts output or throws under concurrent requests, and a fixed locale ignores the user's locale."]},{"id":"spring/request_handling#4f5e80d593e1","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"i18n_and_localization.md","difficulty":"advanced","question":"How does i18n differ in WebFlux versus MVC?","answerFull":"WebFlux cannot rely on the thread-bound LocaleContextHolder because operators switch threads. Instead it uses a LocaleContextResolver (default AcceptHeaderLocaleContextResolver) and reads the locale from ServerWebExchange.getLocaleContext().getLocale(). You pass that locale explicitly to MessageSource.getMessage, or propagate it via the Reactor Context. Using LocaleContextHolder inside a reactive pipeline risks reading a default or stale locale.","correct":"WebFlux cannot rely on the thread-bound LocaleContextHolder because operators switch threads.","distractors":["Because the default AcceptHeaderLocaleResolver is read-only and its setLocale throws UnsupportedOperationException, so the interceptor cannot persist the change.","SimpleDateFormat is mutable and not thread-safe, so a shared static instance corrupts output or throws under concurrent requests, and a fixed locale ignores the user's locale.","Use locale-aware formatters bound to the current locale, never string concatenation."],"distractorIds":["spring/request_handling#6595bc34dc6e","spring/request_handling#1ed37d652582","spring/request_handling#2db8c719640f"],"concepts":["i18n","localecontextholder","operators","rely","webflux","mvc"],"correctMd":"WebFlux cannot rely on the thread-bound `LocaleContextHolder` because operators switch threads.","answerFullMd":"WebFlux cannot rely on the thread-bound `LocaleContextHolder` because operators switch threads. Instead it uses a `LocaleContextResolver` (default `AcceptHeaderLocaleContextResolver`) and reads the locale from `ServerWebExchange.getLocaleContext().getLocale()`. You pass that locale explicitly to `MessageSource.getMessage`, or propagate it via the Reactor `Context`. Using `LocaleContextHolder` inside a reactive pipeline risks reading a default or stale locale.","distractorsMd":["Because the default `AcceptHeaderLocaleResolver` is read-only and its `setLocale` throws `UnsupportedOperationException`, so the interceptor cannot persist the change.","`SimpleDateFormat` is mutable and not thread-safe, so a shared static instance corrupts output or throws under concurrent requests, and a fixed locale ignores the user's locale.",null]},{"id":"spring/request_handling#01f22bcdf027","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"i18n_and_localization.md","difficulty":"advanced","question":"How does content negotiation for language work?","answerFull":"The Accept-Language header is the standard signal: AcceptHeaderLocaleResolver parses its quality-weighted list and resolves the best match, so a client sending Accept-Language: fr-FR, en;q=0.8 gets French. It is stateless and requires no per-user storage, which is why it is the default for APIs. For user overrides beyond the browser preference, layer a Cookie/Session resolver plus LocaleChangeInterceptor.","correct":"The Accept-Language header is the standard signal: AcceptHeaderLocaleResolver parses its quality-weighted list and resolves the best match, so a client sending Accept-Language: fr-FR, en;q=0.8 gets French.","distractors":["Because the default AcceptHeaderLocaleResolver is read-only and its setLocale throws UnsupportedOperationException, so the interceptor cannot persist the change.","RequestResponseBodyMethodProcessor finds the HttpMessageConverter supporting the request's Content-Type and the parameter type.","@Valid is a JSR-303 standard annotation that triggers full Bean Validation on the annotated parameter — all constraints on the object's fields are checked."],"distractorIds":["spring/request_handling#6595bc34dc6e","spring/request_handling#8c1a7f06d44e","spring/request_handling#ca9e5ab40194"],"concepts":["french","acceptheaderlocaleresolver","parses","negotiation","resolves","accept"],"correctMd":"The `Accept-Language` header is the standard signal: `AcceptHeaderLocaleResolver` parses its quality-weighted list and resolves the best match, so a client sending `Accept-Language: fr-FR, en;q=0.8` gets French.","answerFullMd":"The `Accept-Language` header is the standard signal: `AcceptHeaderLocaleResolver` parses its quality-weighted list and resolves the best match, so a client sending `Accept-Language: fr-FR, en;q=0.8` gets French. It is stateless and requires no per-user storage, which is why it is the default for APIs. For user overrides beyond the browser preference, layer a Cookie/Session resolver plus `LocaleChangeInterceptor`.","distractorsMd":["Because the default `AcceptHeaderLocaleResolver` is read-only and its `setLocale` throws `UnsupportedOperationException`, so the interceptor cannot persist the change.","`RequestResponseBodyMethodProcessor` finds the `HttpMessageConverter` supporting the request's `Content-Type` and the parameter type.","`@Valid` is a JSR-303 standard annotation that triggers full Bean Validation on the annotated parameter — all constraints on the object's fields are checked."]},{"id":"spring/request_handling#bfccf13deb9f","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"i18n_and_localization.md","difficulty":"advanced","question":"What happens on a missing message code and how do you control it?","answerFull":"By default the MessageSource throws NoSuchMessageException when no bundle in the fallback chain has the code. Setting setUseCodeAsDefaultMessage(true) returns the raw code instead of throwing, and getMessage(code, args, defaultMessage, locale) lets you supply an inline default. The robust approach is a complete default messages.properties so the hierarchical messages_fr_FR -> messages_fr -> messages fallback always resolves.","correct":"By default the MessageSource throws NoSuchMessageException when no bundle in the fallback chain has the code.","distractors":["When a message has arguments, Spring runs it through java.text.MessageFormat, where a single quote is an escape character, so L'utilisateur {0} drops the {0} and renders Lutilisateur.","The MessageSource tries the most specific bundle first and widens: messages_fr_FR.properties, then messages_fr.properties, then the default messages.properties.","SimpleDateFormat is mutable and not thread-safe, so a shared static instance corrupts output or throws under concurrent requests, and a fixed locale ignores the user's locale."],"distractorIds":["spring/request_handling#cd495e71ad4a","spring/request_handling#06a816debfb3","spring/request_handling#1ed37d652582"],"concepts":["nosuchmessageexception","messagesource","bundle","fallback","missing","happens"],"correctMd":"By default the `MessageSource` throws `NoSuchMessageException` when no bundle in the fallback chain has the code.","answerFullMd":"By default the `MessageSource` throws `NoSuchMessageException` when no bundle in the fallback chain has the code. Setting `setUseCodeAsDefaultMessage(true)` returns the raw code instead of throwing, and `getMessage(code, args, defaultMessage, locale)` lets you supply an inline default. The robust approach is a complete default `messages.properties` so the hierarchical `messages_fr_FR -> messages_fr -> messages` fallback always resolves.","distractorsMd":["When a message has arguments, Spring runs it through `java.text.MessageFormat`, where a single quote is an escape character, so `L'utilisateur {0}` drops the `{0}` and renders `Lutilisateur`.","The `MessageSource` tries the most specific bundle first and widens: `messages_fr_FR.properties`, then `messages_fr.properties`, then the default `messages.properties`.","`SimpleDateFormat` is mutable and not thread-safe, so a shared static instance corrupts output or throws under concurrent requests, and a fixed locale ignores the user's locale."]},{"id":"spring/request_handling#06a816debfb3","section":"spring","module":"spring/request_handling","moduleName":"request handling","sourceFile":"i18n_and_localization.md","difficulty":"advanced","question":"What is the fallback order when resolving a message for locale fr_FR?","answerFull":"The MessageSource tries the most specific bundle first and widens: messages_fr_FR.properties, then messages_fr.properties, then the default messages.properties. Within each, the code is looked up; the first hit wins. This is why a complete default bundle guarantees resolution even when a specific translation is missing a key, and why you only need to translate the deltas per locale.","correct":"The MessageSource tries the most specific bundle first and widens: messages_fr_FR.properties, then messages_fr.properties, then the default messages.properties.","distractors":["It is a HandlerInterceptor whose preHandle reads a request parameter (default locale, commonly reconfigured to lang) and, if present, calls LocaleResolver.setLocale(request, response, parsed).","LocaleContextHolder is a ThreadLocal holding the current request's LocaleContext — its Locale and TimeZone — so any code in the request thread can read the ambient locale without it being passed as a parameter.","By default the MessageSource throws NoSuchMessageException when no bundle in the fallback chain has the code."],"distractorIds":["spring/request_handling#67a2c6f2ea7f","spring/request_handling#6f333956c861","spring/request_handling#bfccf13deb9f"],"concepts":["widens","resolving","messagesource","bundle","locale","tries"],"questionMd":"What is the fallback order when resolving a message for locale `fr_FR`?","correctMd":"The `MessageSource` tries the most specific bundle first and widens: `messages_fr_FR.properties`, then `messages_fr.properties`, then the default `messages.properties`.","answerFullMd":"The `MessageSource` tries the most specific bundle first and widens: `messages_fr_FR.properties`, then `messages_fr.properties`, then the default `messages.properties`. Within each, the code is looked up; the first hit wins. This is why a complete default bundle guarantees resolution even when a specific translation is missing a key, and why you only need to translate the deltas per locale.","distractorsMd":["It is a `HandlerInterceptor` whose `preHandle` reads a request parameter (default `locale`, commonly reconfigured to `lang`) and, if present, calls `LocaleResolver.setLocale(request, response, parsed)`.","`LocaleContextHolder` is a `ThreadLocal` holding the current request's `LocaleContext` — its `Locale` and `TimeZone` — so any code in the request thread can read the ambient locale without it being passed as a parameter.","By default the `MessageSource` throws `NoSuchMessageException` when no bundle in the fallback chain has the code."]},{"id":"spring/spring_ai#0537f1a0dcb7","section":"spring","module":"spring/spring_ai","moduleName":"spring ai","sourceFile":"README.md","difficulty":"core","question":"What is ChatClient and how does it differ from ChatModel?","answerFull":"ChatModel is the low-level, portable interface representing a chat model call (synchronous call and streaming variants) — analogous to JdbcTemplate. ChatClient is a higher-level fluent API built on top of a ChatModel, analogous to RestClient: it supports a builder, default system prompts, prompt templates, an advisor chain (RAG, memory, logging), tools, and structured output via .entity(...). You typically inject the auto-configured ChatClient.Builder, customize defaults per bean, and call .prompt().user(...).call().content(). Use ChatModel directly only for low-level control.","correct":"ChatModel is the low-level, portable interface representing a chat model call (synchronous call and streaming variants) — analogous to JdbcTemplate.","distractors":["ChatMemory persists conversation turns so the model has context across requests — MessageChatMemoryAdvisor injects prior messages into each prompt, keyed by a conversation id (analogous to HttpSession).","LLM calls are slow (hundreds of ms to seconds), non-deterministic, costly per token, and can fail or return unsafe/hallucinated content — unlike a deterministic DB call.","Program against the abstractions (ChatClient, ChatModel, EmbeddingModel, VectorStore), never a vendor's concrete SDK type, and supply the provider via a spring-ai-<vendor>-spring-boot-starter plus properties."],"distractorIds":["spring/spring_ai#ddb4fd497a79","spring/spring_ai#d480577fbd85","spring/spring_ai#92761c754348"],"concepts":["jdbctemplate","chatclient","chatmodel","portable","analogous","chat"],"questionMd":"What is `ChatClient` and how does it differ from `ChatModel`?","correctMd":"`ChatModel` is the low-level, portable interface representing a chat model call (synchronous `call` and streaming variants) — analogous to `JdbcTemplate`.","answerFullMd":"`ChatModel` is the low-level, portable interface representing a chat model call (synchronous `call` and streaming variants) — analogous to `JdbcTemplate`. `ChatClient` is a higher-level fluent API built on top of a `ChatModel`, analogous to `RestClient`: it supports a builder, default system prompts, prompt templates, an **advisor chain** (RAG, memory, logging), tools, and structured output via `.entity(...)`. You typically inject the auto-configured `ChatClient.Builder`, customize defaults per bean, and call `.prompt().user(...).call().content()`. Use `ChatModel` directly only for low-level control.","distractorsMd":["`ChatMemory` persists conversation turns so the model has context across requests — `MessageChatMemoryAdvisor` injects prior messages into each prompt, keyed by a conversation id (analogous to `HttpSession`).",null,"Program against the abstractions (`ChatClient`, `ChatModel`, `EmbeddingModel`, `VectorStore`), never a vendor's concrete SDK type, and supply the provider via a `spring-ai-<vendor>-spring-boot-starter` plus properties."]},{"id":"spring/spring_ai#5521eaf2dcd7","section":"spring","module":"spring/spring_ai","moduleName":"spring ai","sourceFile":"README.md","difficulty":"core","question":"What is a VectorStore and how do you choose a backend?","answerFull":"VectorStore is Spring AI's SPI for storing embeddings and performing similarity search — the Repository analogue for vectors. Implementations include PgVector (reuse your existing Postgres, good default), Redis (low latency), Chroma/Milvus/Weaviate (purpose-built vector DBs), and an in-memory SimpleVectorStore for tests. Choose PgVector when you already run Postgres and want one operational surface; choose a dedicated vector DB at very large scale or when you need advanced indexing (HNSW tuning, hybrid search). All are swappable behind the same interface, so you can start with PgVector and migrate later without changing application code.","correct":"VectorStore is Spring AI's SPI for storing embeddings and performing similarity search — the Repository analogue for vectors.","distractors":["Program against the abstractions (ChatClient, ChatModel, EmbeddingModel, VectorStore), never a vendor's concrete SDK type, and supply the provider via a spring-ai-<vendor>-spring-boot-starter plus properties.","The provider starter contributes an AutoConfiguration class registered in AutoConfiguration.imports, gated by @ConditionalOnClass (the vendor classes are on the classpath) and properties like spring.ai.openai.api-key.","The ETL pipeline is DocumentReader (extract: PDF/HTML/Markdown/Tika → Documents) → DocumentTransformer/TextSplitter (transform: chunk and enrich metadata) → VectorStore (load: embed and store)."],"distractorIds":["spring/spring_ai#92761c754348","spring/spring_ai#35f0a704ff9d","spring/spring_ai#49f29ae9860f"],"concepts":["analogue","performing","spi","vectorstore","repository","storing"],"questionMd":"What is a `VectorStore` and how do you choose a backend?","correctMd":"`VectorStore` is Spring AI's SPI for storing embeddings and performing similarity search — the `Repository` analogue for vectors.","answerFullMd":"`VectorStore` is Spring AI's SPI for storing embeddings and performing similarity search — the `Repository` analogue for vectors. Implementations include PgVector (reuse your existing Postgres, good default), Redis (low latency), Chroma/Milvus/Weaviate (purpose-built vector DBs), and an in-memory `SimpleVectorStore` for tests. Choose PgVector when you already run Postgres and want one operational surface; choose a dedicated vector DB at very large scale or when you need advanced indexing (HNSW tuning, hybrid search). All are swappable behind the same interface, so you can start with PgVector and migrate later without changing application code.","distractorsMd":["Program against the abstractions (`ChatClient`, `ChatModel`, `EmbeddingModel`, `VectorStore`), never a vendor's concrete SDK type, and supply the provider via a `spring-ai-<vendor>-spring-boot-starter` plus properties.","The provider starter contributes an `AutoConfiguration` class registered in `AutoConfiguration.imports`, gated by `@ConditionalOnClass` (the vendor classes are on the classpath) and properties like `spring.ai.openai.api-key`.","The ETL pipeline is `DocumentReader` (extract: PDF/HTML/Markdown/Tika → `Document`s) → `DocumentTransformer`/`TextSplitter` (transform: chunk and enrich metadata) → `VectorStore` (load: embed and store)."]},{"id":"spring/spring_ai#ae697ee153f6","section":"spring","module":"spring/spring_ai","moduleName":"spring ai","sourceFile":"README.md","difficulty":"intermediate","question":"What is tool/function calling and how does Spring AI implement it?","answerFull":"Tool calling lets the *model* decide to invoke your code. You annotate a Spring bean method with @Tool(description=...); when registered on a ChatClient call via .tools(...), Spring AI advertises the method's name, description, and parameter schema to the model. If the model returns a tool-call request, Spring AI invokes your Java method, feeds the result back, and lets the model compose the final answer — possibly across multiple tool calls. This is the foundation of agentic behavior. Critically, the business logic stays in ordinary, individually testable Spring beans; Spring AI only orchestrates the function-calling round trips.","correct":"Tool calling lets the *model* decide to invoke your code.","distractors":["Program against the abstractions (ChatClient, ChatModel, EmbeddingModel, VectorStore), never a vendor's concrete SDK type, and supply the provider via a spring-ai-<vendor>-spring-boot-starter plus properties.","VectorStore is Spring AI's SPI for storing embeddings and performing similarity search — the Repository analogue for vectors.","The provider starter contributes an AutoConfiguration class registered in AutoConfiguration.imports, gated by @ConditionalOnClass (the vendor classes are on the classpath) and properties like spring.ai.openai.api-key."],"distractorIds":["spring/spring_ai#92761c754348","spring/spring_ai#5521eaf2dcd7","spring/spring_ai#35f0a704ff9d"],"concepts":["invoke","decide","implement","calling","lets","function"],"answerFullMd":"Tool calling lets the *model* decide to invoke your code. You annotate a Spring bean method with `@Tool(description=...)`; when registered on a `ChatClient` call via `.tools(...)`, Spring AI advertises the method's name, description, and parameter schema to the model. If the model returns a tool-call request, Spring AI invokes your Java method, feeds the result back, and lets the model compose the final answer — possibly across multiple tool calls. This is the foundation of agentic behavior. Critically, the business logic stays in ordinary, individually testable Spring beans; Spring AI only orchestrates the function-calling round trips.","distractorsMd":["Program against the abstractions (`ChatClient`, `ChatModel`, `EmbeddingModel`, `VectorStore`), never a vendor's concrete SDK type, and supply the provider via a `spring-ai-<vendor>-spring-boot-starter` plus properties.","`VectorStore` is Spring AI's SPI for storing embeddings and performing similarity search — the `Repository` analogue for vectors.","The provider starter contributes an `AutoConfiguration` class registered in `AutoConfiguration.imports`, gated by `@ConditionalOnClass` (the vendor classes are on the classpath) and properties like `spring.ai.openai.api-key`."]},{"id":"spring/spring_ai#92761c754348","section":"spring","module":"spring/spring_ai","moduleName":"spring ai","sourceFile":"README.md","difficulty":"intermediate","question":"How do you make a Spring AI application portable across model providers?","answerFull":"Program against the abstractions (ChatClient, ChatModel, EmbeddingModel, VectorStore), never a vendor's concrete SDK type, and supply the provider via a spring-ai-<vendor>-spring-boot-starter plus properties. Auto-configuration creates the right ChatModel bean from the classpath and config. For multi-model routing, define multiple ChatClient beans with @Qualifiers (e.g., a cheap \"fast\" model and a capable \"smart\" model) and select per request. Because everything is bean-wired, you can also mock the ChatModel in tests. The anti-pattern is new OpenAiChatModel(...) in application code, which re-introduces lock-in and untestability.","correct":"Program against the abstractions (ChatClient, ChatModel, EmbeddingModel, VectorStore), never a vendor's concrete SDK type, and supply the provider via a spring-ai-<vendor>-spring-boot-starter plus properties.","distractors":["VectorStore is Spring AI's SPI for storing embeddings and performing similarity search — the Repository analogue for vectors.","The provider starter contributes an AutoConfiguration class registered in AutoConfiguration.imports, gated by @ConditionalOnClass (the vendor classes are on the classpath) and properties like spring.ai.openai.api-key.","chatClient.prompt().user(q).stream().content() returns a Flux<String> that emits tokens as the model produces them, ideal for text/event-stream SSE endpoints so users see output incrementally."],"distractorIds":["spring/spring_ai#5521eaf2dcd7","spring/spring_ai#35f0a704ff9d","spring/spring_ai#fe26d411183a"],"concepts":["embeddingmodel","chatclient","chatmodel","portable","vectorstore","starter"],"correctMd":"Program against the abstractions (`ChatClient`, `ChatModel`, `EmbeddingModel`, `VectorStore`), never a vendor's concrete SDK type, and supply the provider via a `spring-ai-<vendor>-spring-boot-starter` plus properties.","answerFullMd":"Program against the abstractions (`ChatClient`, `ChatModel`, `EmbeddingModel`, `VectorStore`), never a vendor's concrete SDK type, and supply the provider via a `spring-ai-<vendor>-spring-boot-starter` plus properties. Auto-configuration creates the right `ChatModel` bean from the classpath and config. For multi-model routing, define multiple `ChatClient` beans with `@Qualifier`s (e.g., a cheap \"fast\" model and a capable \"smart\" model) and select per request. Because everything is bean-wired, you can also mock the `ChatModel` in tests. The anti-pattern is `new OpenAiChatModel(...)` in application code, which re-introduces lock-in and untestability.","distractorsMd":["`VectorStore` is Spring AI's SPI for storing embeddings and performing similarity search — the `Repository` analogue for vectors.","The provider starter contributes an `AutoConfiguration` class registered in `AutoConfiguration.imports`, gated by `@ConditionalOnClass` (the vendor classes are on the classpath) and properties like `spring.ai.openai.api-key`.","`chatClient.prompt().user(q).stream().content()` returns a `Flux<String>` that emits tokens as the model produces them, ideal for `text/event-stream` SSE endpoints so users see output incrementally."]},{"id":"spring/spring_ai#fe26d411183a","section":"spring","module":"spring/spring_ai","moduleName":"spring ai","sourceFile":"README.md","difficulty":"intermediate","question":"How does Spring AI handle streaming, and what is the underlying mechanism?","answerFull":"chatClient.prompt().user(q).stream().content() returns a Flux<String> that emits tokens as the model produces them, ideal for text/event-stream SSE endpoints so users see output incrementally. Under the hood this is Reactor (Project Reactor) — the same reactive foundation as Spring WebFlux. The implication is that streaming handlers must remain non-blocking: doing blocking work in the subscriber on a Netty event-loop thread stalls other connections. For non-streaming use cases, .call() is the simpler synchronous path.","correct":"chatClient.prompt().user(q).stream().content() returns a Flux<String> that emits tokens as the model produces them, ideal for text/event-stream SSE endpoints so users see output incrementally.","distractors":["Program against the abstractions (ChatClient, ChatModel, EmbeddingModel, VectorStore), never a vendor's concrete SDK type, and supply the provider via a spring-ai-<vendor>-spring-boot-starter plus properties.","ChatMemory persists conversation turns so the model has context across requests — MessageChatMemoryAdvisor injects prior messages into each prompt, keyed by a conversation id (analogous to HttpSession).","LLM calls are slow (hundreds of ms to seconds), non-deterministic, costly per token, and can fail or return unsafe/hallucinated content — unlike a deterministic DB call."],"distractorIds":["spring/spring_ai#92761c754348","spring/spring_ai#ddb4fd497a79","spring/spring_ai#d480577fbd85"],"concepts":["chatclient","sse","ideal","incrementally","emits","flux"],"correctMd":"`chatClient.prompt().user(q).stream().content()` returns a `Flux<String>` that emits tokens as the model produces them, ideal for `text/event-stream` SSE endpoints so users see output incrementally.","answerFullMd":"`chatClient.prompt().user(q).stream().content()` returns a `Flux<String>` that emits tokens as the model produces them, ideal for `text/event-stream` SSE endpoints so users see output incrementally. Under the hood this is Reactor (Project Reactor) — the same reactive foundation as Spring WebFlux. The implication is that streaming handlers must remain non-blocking: doing blocking work in the subscriber on a Netty event-loop thread stalls other connections. For non-streaming use cases, `.call()` is the simpler synchronous path.","distractorsMd":["Program against the abstractions (`ChatClient`, `ChatModel`, `EmbeddingModel`, `VectorStore`), never a vendor's concrete SDK type, and supply the provider via a `spring-ai-<vendor>-spring-boot-starter` plus properties.","`ChatMemory` persists conversation turns so the model has context across requests — `MessageChatMemoryAdvisor` injects prior messages into each prompt, keyed by a conversation id (analogous to `HttpSession`).",null]},{"id":"spring/spring_ai#49f29ae9860f","section":"spring","module":"spring/spring_ai","moduleName":"spring ai","sourceFile":"README.md","difficulty":"advanced","question":"What is the ETL pipeline in Spring AI and why split documents into chunks?","answerFull":"The ETL pipeline is DocumentReader (extract: PDF/HTML/Markdown/Tika → Documents) → DocumentTransformer/TextSplitter (transform: chunk and enrich metadata) → VectorStore (load: embed and store). Documents are split into chunks of a few hundred tokens because (1) embedding models have input limits, (2) retrieval is more precise when each chunk is a focused unit of meaning, and (3) you can fit several relevant chunks into the prompt's context budget rather than one giant document. Chunk size and overlap are tuning knobs: too large dilutes relevance, too small loses context. The TokenTextSplitter handles this with configurable size/overlap.","correct":"The ETL pipeline is DocumentReader (extract: PDF/HTML/Markdown/Tika → Documents) → DocumentTransformer/TextSplitter (transform: chunk and enrich metadata) → VectorStore (load: embed and store).","distractors":["Program against the abstractions (ChatClient, ChatModel, EmbeddingModel, VectorStore), never a vendor's concrete SDK type, and supply the provider via a spring-ai-<vendor>-spring-boot-starter plus properties.","The provider starter contributes an AutoConfiguration class registered in AutoConfiguration.imports, gated by @ConditionalOnClass (the vendor classes are on the classpath) and properties like spring.ai.openai.api-key.","VectorStore is Spring AI's SPI for storing embeddings and performing similarity search — the Repository analogue for vectors."],"distractorIds":["spring/spring_ai#92761c754348","spring/spring_ai#35f0a704ff9d","spring/spring_ai#5521eaf2dcd7"],"concepts":["documentreader","documenttransformer","enrich","markdown","pdf","textsplitter"],"correctMd":"The ETL pipeline is `DocumentReader` (extract: PDF/HTML/Markdown/Tika → `Document`s) → `DocumentTransformer`/`TextSplitter` (transform: chunk and enrich metadata) → `VectorStore` (load: embed and store).","answerFullMd":"The ETL pipeline is `DocumentReader` (extract: PDF/HTML/Markdown/Tika → `Document`s) → `DocumentTransformer`/`TextSplitter` (transform: chunk and enrich metadata) → `VectorStore` (load: embed and store). Documents are split into chunks of a few hundred tokens because (1) embedding models have input limits, (2) retrieval is more precise when each chunk is a focused unit of meaning, and (3) you can fit several relevant chunks into the prompt's context budget rather than one giant document. Chunk size and overlap are tuning knobs: too large dilutes relevance, too small loses context. The `TokenTextSplitter` handles this with configurable size/overlap.","distractorsMd":["Program against the abstractions (`ChatClient`, `ChatModel`, `EmbeddingModel`, `VectorStore`), never a vendor's concrete SDK type, and supply the provider via a `spring-ai-<vendor>-spring-boot-starter` plus properties.","The provider starter contributes an `AutoConfiguration` class registered in `AutoConfiguration.imports`, gated by `@ConditionalOnClass` (the vendor classes are on the classpath) and properties like `spring.ai.openai.api-key`.","`VectorStore` is Spring AI's SPI for storing embeddings and performing similarity search — the `Repository` analogue for vectors."]},{"id":"spring/spring_ai#ddb4fd497a79","section":"spring","module":"spring/spring_ai","moduleName":"spring ai","sourceFile":"README.md","difficulty":"advanced","question":"What is chat memory and how do you keep it from blowing the context window?","answerFull":"ChatMemory persists conversation turns so the model has context across requests — MessageChatMemoryAdvisor injects prior messages into each prompt, keyed by a conversation id (analogous to HttpSession). The risk is unbounded growth: appending every turn eventually exceeds the model's context window and inflates token cost linearly. The fix is a windowed memory (MessageWindowChatMemory) that retains only the last N turns, optionally with a summarization step that compresses older history into a short summary message. Persist memory in Redis/JDBC for multi-instance services rather than in-memory.","correct":"ChatMemory persists conversation turns so the model has context across requests — MessageChatMemoryAdvisor injects prior messages into each prompt, keyed by a conversation id (analogous to HttpSession).","distractors":["ChatModel is the low-level, portable interface representing a chat model call (synchronous call and streaming variants) — analogous to JdbcTemplate.","chatClient.prompt().user(q).stream().content() returns a Flux<String> that emits tokens as the model produces them, ideal for text/event-stream SSE endpoints so users see output incrementally.","LLM calls are slow (hundreds of ms to seconds), non-deterministic, costly per token, and can fail or return unsafe/hallucinated content — unlike a deterministic DB call."],"distractorIds":["spring/spring_ai#0537f1a0dcb7","spring/spring_ai#fe26d411183a","spring/spring_ai#d480577fbd85"],"concepts":["chatmemory","messagechatmemoryadvisor","persists","httpsession","analogous","chat"],"correctMd":"`ChatMemory` persists conversation turns so the model has context across requests — `MessageChatMemoryAdvisor` injects prior messages into each prompt, keyed by a conversation id (analogous to `HttpSession`).","answerFullMd":"`ChatMemory` persists conversation turns so the model has context across requests — `MessageChatMemoryAdvisor` injects prior messages into each prompt, keyed by a conversation id (analogous to `HttpSession`). The risk is unbounded growth: appending every turn eventually exceeds the model's context window and inflates token cost linearly. The fix is a windowed memory (`MessageWindowChatMemory`) that retains only the last N turns, optionally with a summarization step that compresses older history into a short summary message. Persist memory in Redis/JDBC for multi-instance services rather than in-memory.","distractorsMd":["`ChatModel` is the low-level, portable interface representing a chat model call (synchronous `call` and streaming variants) — analogous to `JdbcTemplate`.","`chatClient.prompt().user(q).stream().content()` returns a `Flux<String>` that emits tokens as the model produces them, ideal for `text/event-stream` SSE endpoints so users see output incrementally.",null]},{"id":"spring/spring_ai#35f0a704ff9d","section":"spring","module":"spring/spring_ai","moduleName":"spring ai","sourceFile":"README.md","difficulty":"advanced","question":"How does Spring AI's auto-configuration decide which model bean to create, and how do you override it?","answerFull":"The provider starter contributes an AutoConfiguration class registered in AutoConfiguration.imports, gated by @ConditionalOnClass (the vendor classes are on the classpath) and properties like spring.ai.openai.api-key. It creates the ChatModel/EmbeddingModel and a ChatClient.Builder bean, each guarded by @ConditionalOnMissingBean so you can override by simply defining your own bean of that type. This is the standard Boot back-off pattern (see spring_boot_autoconfiguration). With multiple provider starters on the classpath you disambiguate via properties and @Qualifiers.","correct":"The provider starter contributes an AutoConfiguration class registered in AutoConfiguration.imports, gated by @ConditionalOnClass (the vendor classes are on the classpath) and properties like spring.ai.openai.api-key.","distractors":["VectorStore is Spring AI's SPI for storing embeddings and performing similarity search — the Repository analogue for vectors.","Program against the abstractions (ChatClient, ChatModel, EmbeddingModel, VectorStore), never a vendor's concrete SDK type, and supply the provider via a spring-ai-<vendor>-spring-boot-starter plus properties.","Tool calling lets the *model* decide to invoke your code."],"distractorIds":["spring/spring_ai#5521eaf2dcd7","spring/spring_ai#92761c754348","spring/spring_ai#ae697ee153f6"],"concepts":["conditionalonclass","autoconfiguration","gated","starter","vendor","contributes"],"correctMd":"The provider starter contributes an `AutoConfiguration` class registered in `AutoConfiguration.imports`, gated by `@ConditionalOnClass` (the vendor classes are on the classpath) and properties like `spring.ai.openai.api-key`.","answerFullMd":"The provider starter contributes an `AutoConfiguration` class registered in `AutoConfiguration.imports`, gated by `@ConditionalOnClass` (the vendor classes are on the classpath) and properties like `spring.ai.openai.api-key`. It creates the `ChatModel`/`EmbeddingModel` and a `ChatClient.Builder` bean, each guarded by `@ConditionalOnMissingBean` so you can override by simply defining your own bean of that type. This is the standard Boot back-off pattern (see spring_boot_autoconfiguration). With multiple provider starters on the classpath you disambiguate via properties and `@Qualifier`s.","distractorsMd":["`VectorStore` is Spring AI's SPI for storing embeddings and performing similarity search — the `Repository` analogue for vectors.","Program against the abstractions (`ChatClient`, `ChatModel`, `EmbeddingModel`, `VectorStore`), never a vendor's concrete SDK type, and supply the provider via a `spring-ai-<vendor>-spring-boot-starter` plus properties.",null]},{"id":"spring/spring_ai#d480577fbd85","section":"spring","module":"spring/spring_ai","moduleName":"spring ai","sourceFile":"README.md","difficulty":"advanced","question":"Describe a production concern unique to LLM-backed Spring services and how you'd address it.","answerFull":"LLM calls are slow (hundreds of ms to seconds), non-deterministic, costly per token, and can fail or return unsafe/hallucinated content — unlike a deterministic DB call. Address these with: timeouts and retries (Resilience4j) around model calls; a circuit breaker and a graceful fallback (\"I'm unable to answer right now\"); a similarityThreshold plus \"answer only from context\" prompting to curb hallucination; token-usage metrics and budget alerts via Micrometer; SafeGuardAdvisor/content filtering for safety; and caching deterministic prompts. Treat the model as an unreliable, expensive remote dependency and apply the same resilience patterns you'd use for any flaky third-party API.","correct":"LLM calls are slow (hundreds of ms to seconds), non-deterministic, costly per token, and can fail or return unsafe/hallucinated content — unlike a deterministic DB call.","distractors":["ChatModel is the low-level, portable interface representing a chat model call (synchronous call and streaming variants) — analogous to JdbcTemplate.","VectorStore is Spring AI's SPI for storing embeddings and performing similarity search — the Repository analogue for vectors.","Program against the abstractions (ChatClient, ChatModel, EmbeddingModel, VectorStore), never a vendor's concrete SDK type, and supply the provider via a spring-ai-<vendor>-spring-boot-starter plus properties."],"distractorIds":["spring/spring_ai#0537f1a0dcb7","spring/spring_ai#5521eaf2dcd7","spring/spring_ai#92761c754348"],"concepts":["describe","concern","costly","hallucinated","unsafe","unlike"],"answerFullMd":"LLM calls are slow (hundreds of ms to seconds), non-deterministic, costly per token, and can fail or return unsafe/hallucinated content — unlike a deterministic DB call. Address these with: timeouts and retries (Resilience4j) around model calls; a circuit breaker and a graceful fallback (\"I'm unable to answer right now\"); a `similarityThreshold` plus \"answer only from context\" prompting to curb hallucination; token-usage metrics and budget alerts via Micrometer; `SafeGuardAdvisor`/content filtering for safety; and caching deterministic prompts. Treat the model as an unreliable, expensive remote dependency and apply the same resilience patterns you'd use for any flaky third-party API.","distractorsMd":["`ChatModel` is the low-level, portable interface representing a chat model call (synchronous `call` and streaming variants) — analogous to `JdbcTemplate`.","`VectorStore` is Spring AI's SPI for storing embeddings and performing similarity search — the `Repository` analogue for vectors.","Program against the abstractions (`ChatClient`, `ChatModel`, `EmbeddingModel`, `VectorStore`), never a vendor's concrete SDK type, and supply the provider via a `spring-ai-<vendor>-spring-boot-starter` plus properties."]},{"id":"spring/spring_aop#ec1c5ff92679","section":"spring","module":"spring/spring_aop","moduleName":"spring aop","sourceFile":"README.md","difficulty":"core","question":"What is the difference between Spring AOP and AspectJ?","answerFull":"Spring AOP is proxy-based runtime weaving limited to method execution on Spring-managed beans. AspectJ is a full AOP framework supporting compile-time, post-compile, and load-time weaving, applicable to any Java object, including constructors, field access, and static initializers. Spring AOP is simpler (zero setup) but limited to Spring beans and public methods. AspectJ requires the AspectJ compiler or load-time weaving agent but can intercept anything. Most Spring applications use Spring AOP; AspectJ is reserved for cases requiring field interception or non-Spring object advice.","correct":"Spring AOP is proxy-based runtime weaving limited to method execution on Spring-managed beans.","distractors":["execution(public * com.example.service..*(..)) — matches: public methods (any return type, any method name) in any class within the com.example.service package and sub-packages, with any parameters.","@Before runs before the method and cannot prevent execution (only throwing an exception aborts).","Because the advice lives on the *proxy*, not on the target object."],"distractorIds":["spring/spring_aop#37c1342cd58c","spring/spring_aop#91fbad65cc08","spring/spring_aop#692efd09abdd"],"concepts":["weaving","difference","aop","limited","managed","beans"],"distractorsMd":["`execution(public * com.example.service..*(..))` — matches: public methods (any return type, any method name) in any class within the `com.example.service` package and sub-packages, with any parameters.","`@Before` runs before the method and cannot prevent execution (only throwing an exception aborts).",null]},{"id":"spring/spring_aop#0eac9b544030","section":"spring","module":"spring/spring_aop","moduleName":"spring aop","sourceFile":"README.md","difficulty":"core","question":"What are the five types of advice in Spring AOP?","answerFull":"@Before runs before the method (cannot prevent execution unless throwing an exception). @AfterReturning runs after normal return (can access return value). @AfterThrowing runs after an exception (can access the exception). @After runs always, like a finally block. @Around wraps the entire method, calling pjp.proceed() to invoke the target — most powerful and most error-prone. For most cases @Around is preferred because it handles both normal and exceptional flows in one place.","correct":"@Before runs before the method (cannot prevent execution unless throwing an exception).","distractors":["A join point is a *specific point* during program execution where advice could be applied; a pointcut is a *predicate/expression* that selects a set of join points.","If @Around advice calls pjp.proceed() and the target throws an exception, and the advice does not catch it (or re-throws), the exception propagates normally to the caller.","@Transactional is implemented by TransactionInterceptor, which is registered as an AOP @Around advice via AnnotationTransactionAttributeSource."],"distractorIds":["spring/spring_aop#73c2cfabb3a9","spring/spring_aop#35db50476b94","spring/spring_aop#585c2ec31bc5"],"concepts":["throwing","advice","five","prevent","aop","unless"],"correctMd":"`@Before` runs before the method (cannot prevent execution unless throwing an exception).","answerFullMd":"`@Before` runs before the method (cannot prevent execution unless throwing an exception). `@AfterReturning` runs after normal return (can access return value). `@AfterThrowing` runs after an exception (can access the exception). `@After` runs always, like a finally block. `@Around` wraps the entire method, calling `pjp.proceed()` to invoke the target — most powerful and most error-prone. For most cases `@Around` is preferred because it handles both normal and exceptional flows in one place.","distractorsMd":[null,"If `@Around` advice calls `pjp.proceed()` and the target throws an exception, and the advice does not catch it (or re-throws), the exception propagates normally to the caller.","`@Transactional` is implemented by `TransactionInterceptor`, which is registered as an AOP `@Around` advice via `AnnotationTransactionAttributeSource`."]},{"id":"spring/spring_aop#37c1342cd58c","section":"spring","module":"spring/spring_aop","moduleName":"spring aop","sourceFile":"README.md","difficulty":"core","question":"How do you write a pointcut expression for all public methods in a service package?","answerFull":"execution(public * com.example.service..*(..)) — matches: public methods (any return type, any method name) in any class within the com.example.service package and sub-packages, with any parameters. Alternatively, within(com.example.service..*) matches all method executions within those classes regardless of visibility (though Spring AOP only intercepts methods that can be overridden). @annotation(com.example.Timed) matches methods annotated with @Timed regardless of package.","correct":"execution(public * com.example.service..*(..)) — matches: public methods (any return type, any method name) in any class within the com.example.service package and sub-packages, with any parameters.","distractors":["The target method is never invoked.","@Before runs before the method and cannot prevent execution (only throwing an exception aborts).","Spring AOP is proxy-based runtime weaving limited to method execution on Spring-managed beans."],"distractorIds":["spring/spring_aop#bb7d968bfaf2","spring/spring_aop#91fbad65cc08","spring/spring_aop#ec1c5ff92679"],"concepts":["pointcut","packages","com","expression","package","public"],"correctMd":"`execution(public * com.example.service..*(..))` — matches: public methods (any return type, any method name) in any class within the `com.example.service` package and sub-packages, with any parameters.","answerFullMd":"`execution(public * com.example.service..*(..))` — matches: public methods (any return type, any method name) in any class within the `com.example.service` package and sub-packages, with any parameters. Alternatively, `within(com.example.service..*)` matches all method executions within those classes regardless of visibility (though Spring AOP only intercepts methods that can be overridden). `@annotation(com.example.Timed)` matches methods annotated with `@Timed` regardless of package.","distractorsMd":[null,"`@Before` runs before the method and cannot prevent execution (only throwing an exception aborts).",null]},{"id":"spring/spring_aop#585c2ec31bc5","section":"spring","module":"spring/spring_aop","moduleName":"spring aop","sourceFile":"README.md","difficulty":"core","question":"How does @Transactional use Spring AOP?","answerFull":"@Transactional is implemented by TransactionInterceptor, which is registered as an AOP @Around advice via AnnotationTransactionAttributeSource. When the container detects a @Transactional bean, AnnotationAwareAspectJAutoProxyCreator wraps it in a proxy. On each method call, TransactionInterceptor.invoke() checks the method's transaction attributes, starts/joins/suspends a transaction, calls proceed(), then commits or rolls back. This is why @Transactional on private methods is silently ignored — the proxy cannot intercept them.","correct":"@Transactional is implemented by TransactionInterceptor, which is registered as an AOP @Around advice via AnnotationTransactionAttributeSource.","distractors":["Use @Order(n) on the aspect class — lower numbers have higher priority and run as the outermost wrapper in @Around advice (their before logic runs first; their after logic runs last).","If @Around advice calls pjp.proceed() and the target throws an exception, and the advice does not catch it (or re-throws), the exception propagates normally to the caller.","Because the advice lives on the *proxy*, not on the target object."],"distractorIds":["spring/spring_aop#876d31f28c17","spring/spring_aop#35db50476b94","spring/spring_aop#692efd09abdd"],"concepts":["annotationtransactionattributesource","transactioninterceptor","advice","implemented","aop","transactional"],"correctMd":"`@Transactional` is implemented by `TransactionInterceptor`, which is registered as an AOP `@Around` advice via `AnnotationTransactionAttributeSource`.","answerFullMd":"`@Transactional` is implemented by `TransactionInterceptor`, which is registered as an AOP `@Around` advice via `AnnotationTransactionAttributeSource`. When the container detects a `@Transactional` bean, `AnnotationAwareAspectJAutoProxyCreator` wraps it in a proxy. On each method call, `TransactionInterceptor.invoke()` checks the method's transaction attributes, starts/joins/suspends a transaction, calls `proceed()`, then commits or rolls back. This is why `@Transactional` on private methods is silently ignored — the proxy cannot intercept them.","distractorsMd":["Use `@Order(n)` on the aspect class — lower numbers have higher priority and run as the outermost wrapper in `@Around` advice (their `before` logic runs first; their `after` logic runs last).","If `@Around` advice calls `pjp.proceed()` and the target throws an exception, and the advice does not catch it (or re-throws), the exception propagates normally to the caller.",null]},{"id":"spring/spring_aop#876d31f28c17","section":"spring","module":"spring/spring_aop","moduleName":"spring aop","sourceFile":"README.md","difficulty":"intermediate","question":"How do you control the order in which multiple aspects apply to the same method?","answerFull":"Use @Order(n) on the aspect class — lower numbers have higher priority and run as the outermost wrapper in @Around advice (their before logic runs first; their after logic runs last). If no @Order is specified, order is undefined and may differ across JVM runs. For Spring's built-in aspects: Security (highest priority) → Transaction (middle) → Application aspects (varies). Use Ordered.HIGHEST_PRECEDENCE and Ordered.LOWEST_PRECEDENCE for clear semantics.","correct":"Use @Order(n) on the aspect class — lower numbers have higher priority and run as the outermost wrapper in @Around advice (their before logic runs first; their after logic runs last).","distractors":["If @Around advice calls pjp.proceed() and the target throws an exception, and the advice does not catch it (or re-throws), the exception propagates normally to the caller.","Bind the annotation type as a parameter to the advice method and reference it in the pointcut.","@Before runs before the method (cannot prevent execution unless throwing an exception)."],"distractorIds":["spring/spring_aop#35db50476b94","spring/spring_aop#311160398a1e","spring/spring_aop#0eac9b544030"],"concepts":["aspects","aspect","outermost","advice","wrapper","numbers"],"correctMd":"Use `@Order(n)` on the aspect class — lower numbers have higher priority and run as the outermost wrapper in `@Around` advice (their `before` logic runs first; their `after` logic runs last).","answerFullMd":"Use `@Order(n)` on the aspect class — lower numbers have higher priority and run as the outermost wrapper in `@Around` advice (their `before` logic runs first; their `after` logic runs last). If no `@Order` is specified, order is undefined and may differ across JVM runs. For Spring's built-in aspects: Security (highest priority) → Transaction (middle) → Application aspects (varies). Use `Ordered.HIGHEST_PRECEDENCE` and `Ordered.LOWEST_PRECEDENCE` for clear semantics.","distractorsMd":["If `@Around` advice calls `pjp.proceed()` and the target throws an exception, and the advice does not catch it (or re-throws), the exception propagates normally to the caller.",null,"`@Before` runs before the method (cannot prevent execution unless throwing an exception)."]},{"id":"spring/spring_aop#91fbad65cc08","section":"spring","module":"spring/spring_aop","moduleName":"spring aop","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between @Before and the \"before\" part of @Around?","answerFull":"@Before runs before the method and cannot prevent execution (only throwing an exception aborts). The \"before\" part of @Around (code before pjp.proceed()) can prevent execution by not calling proceed(), by calling proceed() with modified arguments, or by returning a short-circuit value. @Around has full control: it can modify arguments, modify the return value, catch exceptions and return a fallback, or retry the call. Use @Before for simple pre-invocation logic; use @Around when you need control over execution.","correct":"@Before runs before the method and cannot prevent execution (only throwing an exception aborts).","distractors":["Bind the annotation type as a parameter to the advice method and reference it in the pointcut.","A join point is a *specific point* during program execution where advice could be applied; a pointcut is a *predicate/expression* that selects a set of join points.","@Transactional is implemented by TransactionInterceptor, which is registered as an AOP @Around advice via AnnotationTransactionAttributeSource."],"distractorIds":["spring/spring_aop#311160398a1e","spring/spring_aop#73c2cfabb3a9","spring/spring_aop#585c2ec31bc5"],"concepts":["aborts","throwing","difference","prevent","part","around"],"correctMd":"`@Before` runs before the method and cannot prevent execution (only throwing an exception aborts).","answerFullMd":"`@Before` runs before the method and cannot prevent execution (only throwing an exception aborts). The \"before\" part of `@Around` (code before `pjp.proceed()`) can prevent execution by not calling `proceed()`, by calling `proceed()` with modified arguments, or by returning a short-circuit value. `@Around` has full control: it can modify arguments, modify the return value, catch exceptions and return a fallback, or retry the call. Use `@Before` for simple pre-invocation logic; use `@Around` when you need control over execution.","distractorsMd":[null,null,"`@Transactional` is implemented by `TransactionInterceptor`, which is registered as an AOP `@Around` advice via `AnnotationTransactionAttributeSource`."]},{"id":"spring/spring_aop#eb50d5748ad0","section":"spring","module":"spring/spring_aop","moduleName":"spring aop","sourceFile":"README.md","difficulty":"intermediate","question":"Can a pointcut match beans in other Spring contexts (parent/child)?","answerFull":"Spring AOP only operates within a single ApplicationContext. A BeanPostProcessor in the child context (servlet context) creates proxies for beans in the child context. Beans in the parent context (root context) are proxied by post-processors in the parent context. An aspect declared in the child context does NOT apply to parent context beans. This is why aspects for service beans should be defined in the root context, while aspects for MVC components are in the servlet context.","correct":"Spring AOP only operates within a single ApplicationContext.","distractors":["Spring AOP is proxy-based runtime weaving limited to method execution on Spring-managed beans.","An aspect bean is itself a Spring bean and can in principle be matched by another aspect's pointcut, but you normally exclude aspects from pointcuts to avoid recursion and surprises.","@Transactional is implemented by TransactionInterceptor, which is registered as an AOP @Around advice via AnnotationTransactionAttributeSource."],"distractorIds":["spring/spring_aop#ec1c5ff92679","spring/spring_aop#baa769e3cd64","spring/spring_aop#585c2ec31bc5"],"concepts":["pointcut","operates","contexts","aop","applicationcontext","child"],"correctMd":"Spring AOP only operates within a single `ApplicationContext`.","answerFullMd":"Spring AOP only operates within a single `ApplicationContext`. A `BeanPostProcessor` in the child context (servlet context) creates proxies for beans in the child context. Beans in the parent context (root context) are proxied by post-processors in the parent context. An aspect declared in the child context does NOT apply to parent context beans. This is why aspects for service beans should be defined in the root context, while aspects for MVC components are in the servlet context.","distractorsMd":[null,null,"`@Transactional` is implemented by `TransactionInterceptor`, which is registered as an AOP `@Around` advice via `AnnotationTransactionAttributeSource`."]},{"id":"spring/spring_aop#bb7d968bfaf2","section":"spring","module":"spring/spring_aop","moduleName":"spring aop","sourceFile":"README.md","difficulty":"intermediate","question":"What happens if @Around advice does not call proceed()?","answerFull":"The target method is never invoked. The advice return value (or null if no return statement) is returned to the caller. No exception from the method propagates. This silently breaks the application — all methods matching the pointcut return null and execute no business logic. This is a dangerous bug with no compile-time or startup warning. Always call pjp.proceed() in @Around advice unless intentionally short-circuiting (e.g., cache hit, circuit breaker open, circuit is returning cached response).","correct":"The target method is never invoked.","distractors":["@Transactional is implemented by TransactionInterceptor, which is registered as an AOP @Around advice via AnnotationTransactionAttributeSource.","Use @Order(n) on the aspect class — lower numbers have higher priority and run as the outermost wrapper in @Around advice (their before logic runs first; their after logic runs last).","@Order controls the *outer-to-inner* nesting of aspects: a lower order value wraps further outside, so its @Around/@Before runs first and its @After/@AfterReturning runs last (like nested try-finally blocks)."],"distractorIds":["spring/spring_aop#585c2ec31bc5","spring/spring_aop#876d31f28c17","spring/spring_aop#f7b76020c972"],"concepts":["invoked","advice","proceed","happens","around","target"],"answerFullMd":"The target method is never invoked. The advice return value (or null if no return statement) is returned to the caller. No exception from the method propagates. This silently breaks the application — all methods matching the pointcut return null and execute no business logic. This is a dangerous bug with no compile-time or startup warning. Always call `pjp.proceed()` in `@Around` advice unless intentionally short-circuiting (e.g., cache hit, circuit breaker open, circuit is returning cached response).","distractorsMd":["`@Transactional` is implemented by `TransactionInterceptor`, which is registered as an AOP `@Around` advice via `AnnotationTransactionAttributeSource`.","Use `@Order(n)` on the aspect class — lower numbers have higher priority and run as the outermost wrapper in `@Around` advice (their `before` logic runs first; their `after` logic runs last).","`@Order` controls the *outer-to-inner* nesting of aspects: a lower order value wraps further outside, so its `@Around`/`@Before` runs first and its `@After`/`@AfterReturning` runs last (like nested try-finally blocks)."]},{"id":"spring/spring_aop#35db50476b94","section":"spring","module":"spring/spring_aop","moduleName":"spring aop","sourceFile":"README.md","difficulty":"intermediate","question":"How does Spring AOP handle exceptions thrown by @Around advice?","answerFull":"If @Around advice calls pjp.proceed() and the target throws an exception, and the advice does not catch it (or re-throws), the exception propagates normally to the caller. Other advice types (@AfterThrowing, @After) are also triggered. If the @Around advice catches the exception and does not re-throw, the caller sees a normal return (or the advice's return value). @AfterThrowing advice only runs if the exception propagates past all @Around advice. To ensure proper @Transactional rollback, never swallow exceptions in @Around advice.","correct":"If @Around advice calls pjp.proceed() and the target throws an exception, and the advice does not catch it (or re-throws), the exception propagates normally to the caller.","distractors":["@Transactional is implemented by TransactionInterceptor, which is registered as an AOP @Around advice via AnnotationTransactionAttributeSource.","Spring AOP is proxy-based runtime weaving limited to method execution on Spring-managed beans.","Spring AOP adds two costs: proxy creation (one-time at startup, typically negligible) and per-call proxy dispatch overhead."],"distractorIds":["spring/spring_aop#585c2ec31bc5","spring/spring_aop#ec1c5ff92679","spring/spring_aop#40a5aa40b157"],"concepts":["pjp","thrown","advice","proceed","catch","propagates"],"correctMd":"If `@Around` advice calls `pjp.proceed()` and the target throws an exception, and the advice does not catch it (or re-throws), the exception propagates normally to the caller.","answerFullMd":"If `@Around` advice calls `pjp.proceed()` and the target throws an exception, and the advice does not catch it (or re-throws), the exception propagates normally to the caller. Other advice types (`@AfterThrowing`, `@After`) are also triggered. If the `@Around` advice catches the exception and does not re-throw, the caller sees a normal return (or the advice's return value). `@AfterThrowing` advice only runs if the exception propagates past all `@Around` advice. To ensure proper `@Transactional` rollback, never swallow exceptions in `@Around` advice.","distractorsMd":["`@Transactional` is implemented by `TransactionInterceptor`, which is registered as an AOP `@Around` advice via `AnnotationTransactionAttributeSource`.",null,null]},{"id":"spring/spring_aop#c33b053de646","section":"spring","module":"spring/spring_aop","moduleName":"spring aop","sourceFile":"README.md","difficulty":"advanced","question":"What is the @within pointcut designator and how does it differ from within?","answerFull":"within(com.example.service..*) matches all method executions in classes within the specified package. @within(com.example.annotation.Monitored) matches all method executions in classes that are annotated with @Monitored. The difference: within matches by package/class name; @within matches by annotation on the class. Similarly, @annotation matches by annotation on the method. Use @within when you want to apply advice to all methods of annotated classes (e.g., all methods in @RestController classes).","correct":"within(com.example.service..*) matches all method executions in classes within the specified package.","distractors":["A join point is a *specific point* during program execution where advice could be applied; a pointcut is a *predicate/expression* that selects a set of join points.","Spring AOP is proxy-based runtime weaving limited to method execution on Spring-managed beans.","@Before runs before the method and cannot prevent execution (only throwing an exception aborts)."],"distractorIds":["spring/spring_aop#73c2cfabb3a9","spring/spring_aop#ec1c5ff92679","spring/spring_aop#91fbad65cc08"],"concepts":["executions","pointcut","com","differ","package","specified"],"questionMd":"What is the `@within` pointcut designator and how does it differ from `within`?","correctMd":"`within(com.example.service..*)` matches all method executions in classes within the specified package.","answerFullMd":"`within(com.example.service..*)` matches all method executions in classes within the specified package. `@within(com.example.annotation.Monitored)` matches all method executions in classes that are annotated with `@Monitored`. The difference: `within` matches by package/class name; `@within` matches by annotation on the class. Similarly, `@annotation` matches by annotation on the method. Use `@within` when you want to apply advice to all methods of annotated classes (e.g., all methods in `@RestController` classes).","distractorsMd":[null,null,"`@Before` runs before the method and cannot prevent execution (only throwing an exception aborts)."]},{"id":"spring/spring_aop#311160398a1e","section":"spring","module":"spring/spring_aop","moduleName":"spring aop","sourceFile":"README.md","difficulty":"advanced","question":"How do you get access to the annotation instance's attributes inside advice?","answerFull":"Bind the annotation type as a parameter to the advice method and reference it in the pointcut. For @Around(\"@annotation(myAnnotation)\") where myAnnotation is the parameter name (must match!), Spring binds the actual annotation instance to that parameter. Then myAnnotation.attribute() accesses the value. This is how @Retryable(maxAttempts=3) works — the retry aspect reads maxAttempts directly from the annotation instance passed to @Around.","correct":"Bind the annotation type as a parameter to the advice method and reference it in the pointcut.","distractors":["If @Around advice calls pjp.proceed() and the target throws an exception, and the advice does not catch it (or re-throws), the exception propagates normally to the caller.","Define a custom @Auditable annotation with attributes for action and resource.","@Transactional is implemented by TransactionInterceptor, which is registered as an AOP @Around advice via AnnotationTransactionAttributeSource."],"distractorIds":["spring/spring_aop#35db50476b94","spring/spring_aop#37b391f486f3","spring/spring_aop#585c2ec31bc5"],"concepts":["pointcut","advice","bind","attributes","annotation","inside"],"answerFullMd":"Bind the annotation type as a parameter to the advice method and reference it in the pointcut. For `@Around(\"@annotation(myAnnotation)\")` where `myAnnotation` is the parameter name (must match!), Spring binds the actual annotation instance to that parameter. Then `myAnnotation.attribute()` accesses the value. This is how `@Retryable(maxAttempts=3)` works — the retry aspect reads `maxAttempts` directly from the annotation instance passed to `@Around`.","distractorsMd":["If `@Around` advice calls `pjp.proceed()` and the target throws an exception, and the advice does not catch it (or re-throws), the exception propagates normally to the caller.","Define a custom `@Auditable` annotation with attributes for action and resource.","`@Transactional` is implemented by `TransactionInterceptor`, which is registered as an AOP `@Around` advice via `AnnotationTransactionAttributeSource`."]},{"id":"spring/spring_aop#40a5aa40b157","section":"spring","module":"spring/spring_aop","moduleName":"spring aop","sourceFile":"README.md","difficulty":"advanced","question":"What is the performance cost of Spring AOP?","answerFull":"Spring AOP adds two costs: proxy creation (one-time at startup, typically negligible) and per-call proxy dispatch overhead. For CGLIB proxies, MethodProxy.invokeSuper() is faster than reflection. For JDK proxies, Method.invoke() uses reflection. Measured overhead is typically 1-10 microseconds per call depending on JIT optimization. For hot paths (millions of calls/second), this can matter. Pointcut evaluation is also a cost — complex execution() expressions evaluated on every method call can add up. Mitigation: use @Pointcut method-level caching (Spring does this), or use AspectJ compile-time weaving (near-zero overhead).","correct":"Spring AOP adds two costs: proxy creation (one-time at startup, typically negligible) and per-call proxy dispatch overhead.","distractors":["@Transactional is implemented by TransactionInterceptor, which is registered as an AOP @Around advice via AnnotationTransactionAttributeSource.","Spring AOP is proxy-based runtime weaving limited to method execution on Spring-managed beans.","Because the advice lives on the *proxy*, not on the target object."],"distractorIds":["spring/spring_aop#585c2ec31bc5","spring/spring_aop#ec1c5ff92679","spring/spring_aop#692efd09abdd"],"concepts":["negligible","creation","dispatch","aop","startup","costs"],"answerFullMd":"Spring AOP adds two costs: proxy creation (one-time at startup, typically negligible) and per-call proxy dispatch overhead. For CGLIB proxies, `MethodProxy.invokeSuper()` is faster than reflection. For JDK proxies, `Method.invoke()` uses reflection. Measured overhead is typically 1-10 microseconds per call depending on JIT optimization. For hot paths (millions of calls/second), this can matter. Pointcut evaluation is also a cost — complex `execution()` expressions evaluated on every method call can add up. Mitigation: use `@Pointcut` method-level caching (Spring does this), or use AspectJ compile-time weaving (near-zero overhead).","distractorsMd":["`@Transactional` is implemented by `TransactionInterceptor`, which is registered as an AOP `@Around` advice via `AnnotationTransactionAttributeSource`.",null,null]},{"id":"spring/spring_aop#37b391f486f3","section":"spring","module":"spring/spring_aop","moduleName":"spring aop","sourceFile":"README.md","difficulty":"advanced","question":"How would you implement method-level audit logging using AOP?","answerFull":"Define a custom @Auditable annotation with attributes for action and resource. Write an @Aspect with @Around(\"@annotation(auditable)\"). In the advice: extract the annotation's action/resource attributes, capture the authenticated user from SecurityContextHolder, record the attempt, call pjp.proceed(), record success (including return value summary), catch exceptions to record failure. Annotate service methods with @Auditable(action=\"CREATE_ORDER\", resource=\"orders\"). This produces a complete audit trail for any method with zero change to business logic.","correct":"Define a custom @Auditable annotation with attributes for action and resource.","distractors":["@Before runs before the method (cannot prevent execution unless throwing an exception).","Spring AOP only operates within a single ApplicationContext.","Spring AOP adds two costs: proxy creation (one-time at startup, typically negligible) and per-call proxy dispatch overhead."],"distractorIds":["spring/spring_aop#0eac9b544030","spring/spring_aop#eb50d5748ad0","spring/spring_aop#40a5aa40b157"],"concepts":["auditable","audit","aop","logging","attributes","annotation"],"correctMd":"Define a custom `@Auditable` annotation with attributes for action and resource.","answerFullMd":"Define a custom `@Auditable` annotation with attributes for action and resource. Write an `@Aspect` with `@Around(\"@annotation(auditable)\")`. In the advice: extract the annotation's action/resource attributes, capture the authenticated user from `SecurityContextHolder`, record the attempt, call `pjp.proceed()`, record success (including return value summary), catch exceptions to record failure. Annotate service methods with `@Auditable(action=\"CREATE_ORDER\", resource=\"orders\")`. This produces a complete audit trail for any method with zero change to business logic.","distractorsMd":["`@Before` runs before the method (cannot prevent execution unless throwing an exception).","Spring AOP only operates within a single `ApplicationContext`.",null]},{"id":"spring/spring_aop#6218d65f130e","section":"spring","module":"spring/spring_aop","moduleName":"spring aop","sourceFile":"README.md","difficulty":"advanced","question":"What is the @EnableAspectJAutoProxy annotation and what does it do?","answerFull":"@EnableAspectJAutoProxy registers AnnotationAwareAspectJAutoProxyCreator as a BeanPostProcessor. This post-processor inspects every bean during initialization and, if any declared @Aspect matches the bean via a pointcut, creates a proxy (CGLIB or JDK) for that bean. Without this annotation, @Aspect classes are created as regular beans but their advice is never applied. Spring Boot's AopAutoConfiguration auto-configures this when spring-aop is on the classpath and spring.aop.auto=true (the default), so explicit @EnableAspectJAutoProxy is rarely needed in Boot applications.","correct":"@EnableAspectJAutoProxy registers AnnotationAwareAspectJAutoProxyCreator as a BeanPostProcessor.","distractors":["Bind the annotation type as a parameter to the advice method and reference it in the pointcut.","Define a custom @Auditable annotation with attributes for action and resource.","@Before runs before the method (cannot prevent execution unless throwing an exception)."],"distractorIds":["spring/spring_aop#311160398a1e","spring/spring_aop#37b391f486f3","spring/spring_aop#0eac9b544030"],"concepts":["annotationawareaspectjautoproxycreator","enableaspectjautoproxy","beanpostprocessor","registers","annotation"],"correctMd":"`@EnableAspectJAutoProxy` registers `AnnotationAwareAspectJAutoProxyCreator` as a `BeanPostProcessor`.","answerFullMd":"`@EnableAspectJAutoProxy` registers `AnnotationAwareAspectJAutoProxyCreator` as a `BeanPostProcessor`. This post-processor inspects every bean during initialization and, if any declared `@Aspect` matches the bean via a pointcut, creates a proxy (CGLIB or JDK) for that bean. Without this annotation, `@Aspect` classes are created as regular beans but their advice is never applied. Spring Boot's `AopAutoConfiguration` auto-configures this when `spring-aop` is on the classpath and `spring.aop.auto=true` (the default), so explicit `@EnableAspectJAutoProxy` is rarely needed in Boot applications.","distractorsMd":[null,"Define a custom `@Auditable` annotation with attributes for action and resource.","`@Before` runs before the method (cannot prevent execution unless throwing an exception)."]},{"id":"spring/spring_aop#692efd09abdd","section":"spring","module":"spring/spring_aop","moduleName":"spring aop","sourceFile":"README.md","difficulty":"advanced","question":"Why does self-invocation bypass AOP advice, and how do you fix it?","answerFull":"Because the advice lives on the *proxy*, not on the target object. When method a() calls this.b() inside the same bean, this is the raw target instance, not the proxy, so the call never passes through the interceptor chain and b()'s advice (e.g. @Transactional, @Cacheable, a custom aspect) silently does not run. The cleanest fix is to move b() into a separate bean so the call crosses a proxy boundary; alternatives are injecting a self-reference (@Autowired of your own type, or ObjectProvider), or AopContext.currentProxy() with exposeProxy=true. This is the single most common AOP bug in production and applies to every proxy-based annotation, not just AOP aspects.","correct":"Because the advice lives on the *proxy*, not on the target object.","distractors":["Use @Order(n) on the aspect class — lower numbers have higher priority and run as the outermost wrapper in @Around advice (their before logic runs first; their after logic runs last).","Spring AOP only operates within a single ApplicationContext.","Spring AOP is proxy-based runtime weaving limited to method execution on Spring-managed beans."],"distractorIds":["spring/spring_aop#876d31f28c17","spring/spring_aop#eb50d5748ad0","spring/spring_aop#ec1c5ff92679"],"concepts":["bypass","advice","invocation","fix","aop","lives"],"answerFullMd":"Because the advice lives on the *proxy*, not on the target object. When method `a()` calls `this.b()` inside the same bean, `this` is the raw target instance, not the proxy, so the call never passes through the interceptor chain and `b()`'s advice (e.g. `@Transactional`, `@Cacheable`, a custom aspect) silently does not run. The cleanest fix is to move `b()` into a separate bean so the call crosses a proxy boundary; alternatives are injecting a self-reference (`@Autowired` of your own type, or `ObjectProvider`), or `AopContext.currentProxy()` with `exposeProxy=true`. This is the single most common AOP bug in production and applies to every proxy-based annotation, not just AOP aspects.","distractorsMd":["Use `@Order(n)` on the aspect class — lower numbers have higher priority and run as the outermost wrapper in `@Around` advice (their `before` logic runs first; their `after` logic runs last).","Spring AOP only operates within a single `ApplicationContext`.",null]},{"id":"spring/spring_aop#f7b76020c972","section":"spring","module":"spring/spring_aop","moduleName":"spring aop","sourceFile":"README.md","difficulty":"advanced","question":"What does the @Order / Ordered annotation control when multiple aspects match the same join point, and how do advice types nest?","answerFull":"@Order controls the *outer-to-inner* nesting of aspects: a lower order value wraps further outside, so its @Around/@Before runs first and its @After/@AfterReturning runs last (like nested try-finally blocks). Within a single aspect, the advice precedence is @Around (before proceed) → @Before → target method → @AfterReturning/@AfterThrowing → @After. Without explicit ordering, the order of aspects is undefined, which causes subtle bugs — e.g. a transaction aspect and a caching aspect whose relative order changes whether the cache sees committed data. Always order interacting aspects explicitly.","correct":"@Order controls the *outer-to-inner* nesting of aspects: a lower order value wraps further outside, so its @Around/@Before runs first and its @After/@AfterReturning runs last (like nested try-finally blocks).","distractors":["Use @Order(n) on the aspect class — lower numbers have higher priority and run as the outermost wrapper in @Around advice (their before logic runs first; their after logic runs last).","If @Around advice calls pjp.proceed() and the target throws an exception, and the advice does not catch it (or re-throws), the exception propagates normally to the caller.","Bind the annotation type as a parameter to the advice method and reference it in the pointcut."],"distractorIds":["spring/spring_aop#876d31f28c17","spring/spring_aop#35db50476b94","spring/spring_aop#311160398a1e"],"concepts":["afterreturning","nest","aspects","further","nesting","advice"],"correctMd":"`@Order` controls the *outer-to-inner* nesting of aspects: a lower order value wraps further outside, so its `@Around`/`@Before` runs first and its `@After`/`@AfterReturning` runs last (like nested try-finally blocks).","answerFullMd":"`@Order` controls the *outer-to-inner* nesting of aspects: a lower order value wraps further outside, so its `@Around`/`@Before` runs first and its `@After`/`@AfterReturning` runs last (like nested try-finally blocks). Within a single aspect, the advice precedence is `@Around` (before `proceed`) → `@Before` → target method → `@AfterReturning`/`@AfterThrowing` → `@After`. Without explicit ordering, the order of aspects is undefined, which causes subtle bugs — e.g. a transaction aspect and a caching aspect whose relative order changes whether the cache sees committed data. Always order interacting aspects explicitly.","distractorsMd":["Use `@Order(n)` on the aspect class — lower numbers have higher priority and run as the outermost wrapper in `@Around` advice (their `before` logic runs first; their `after` logic runs last).","If `@Around` advice calls `pjp.proceed()` and the target throws an exception, and the advice does not catch it (or re-throws), the exception propagates normally to the caller.",null]},{"id":"spring/spring_aop#baa769e3cd64","section":"spring","module":"spring/spring_aop","moduleName":"spring aop","sourceFile":"README.md","difficulty":"advanced","question":"Can an aspect advise another aspect, and what happens with proxying of final classes or methods?","answerFull":"An aspect bean is itself a Spring bean and can in principle be matched by another aspect's pointcut, but you normally exclude aspects from pointcuts to avoid recursion and surprises. The proxying limitation is more important in practice: CGLIB subclasses the target, so it cannot proxy a final class or override a final/private/static method — advice on those simply does not apply (and a final class with no interface fails proxy creation). JDK dynamic proxies only proxy interface methods, so a public method not declared on an interface is not advised. Knowing these limits explains many \"my aspect didn't fire\" mysteries.","correct":"An aspect bean is itself a Spring bean and can in principle be matched by another aspect's pointcut, but you normally exclude aspects from pointcuts to avoid recursion and surprises.","distractors":["Bind the annotation type as a parameter to the advice method and reference it in the pointcut.","@Order controls the *outer-to-inner* nesting of aspects: a lower order value wraps further outside, so its @Around/@Before runs first and its @After/@AfterReturning runs last (like nested try-finally blocks).","Use @Order(n) on the aspect class — lower numbers have higher priority and run as the outermost wrapper in @Around advice (their before logic runs first; their after logic runs last)."],"distractorIds":["spring/spring_aop#311160398a1e","spring/spring_aop#f7b76020c972","spring/spring_aop#876d31f28c17"],"concepts":["pointcuts","proxying","surprises","aspects","exclude","pointcut"],"answerFullMd":"An aspect bean is itself a Spring bean and can in principle be matched by another aspect's pointcut, but you normally exclude aspects from pointcuts to avoid recursion and surprises. The proxying limitation is more important in practice: CGLIB subclasses the target, so it cannot proxy a `final` class or override a `final`/`private`/`static` method — advice on those simply does not apply (and a `final` class with no interface fails proxy creation). JDK dynamic proxies only proxy interface methods, so a `public` method not declared on an interface is not advised. Knowing these limits explains many \"my aspect didn't fire\" mysteries.","distractorsMd":[null,"`@Order` controls the *outer-to-inner* nesting of aspects: a lower order value wraps further outside, so its `@Around`/`@Before` runs first and its `@After`/`@AfterReturning` runs last (like nested try-finally blocks).","Use `@Order(n)` on the aspect class — lower numbers have higher priority and run as the outermost wrapper in `@Around` advice (their `before` logic runs first; their `after` logic runs last)."]},{"id":"spring/spring_aop#73c2cfabb3a9","section":"spring","module":"spring/spring_aop","moduleName":"spring aop","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between a join point and a pointcut, and what can be a join point in Spring AOP?","answerFull":"A join point is a *specific point* during program execution where advice could be applied; a pointcut is a *predicate/expression* that selects a set of join points. In full AspectJ, join points include method calls, method executions, constructor calls, field reads/writes, and exception handlers. Spring AOP, being proxy-based, supports only *method-execution* join points on Spring-managed beans — no field access, no constructor interception, no advising of calls made from within the target. This restriction is why interview answers must distinguish \"AspectJ can do X\" from \"Spring AOP can do X\": when you need field or constructor join points you must drop to AspectJ weaving.","correct":"A join point is a *specific point* during program execution where advice could be applied; a pointcut is a *predicate/expression* that selects a set of join points.","distractors":["Spring AOP adds two costs: proxy creation (one-time at startup, typically negligible) and per-call proxy dispatch overhead.","@Transactional is implemented by TransactionInterceptor, which is registered as an AOP @Around advice via AnnotationTransactionAttributeSource.","If @Around advice calls pjp.proceed() and the target throws an exception, and the advice does not catch it (or re-throws), the exception propagates normally to the caller."],"distractorIds":["spring/spring_aop#40a5aa40b157","spring/spring_aop#585c2ec31bc5","spring/spring_aop#35db50476b94"],"concepts":["pointcut","advice","predicate","difference","aop","program"],"distractorsMd":[null,"`@Transactional` is implemented by `TransactionInterceptor`, which is registered as an AOP `@Around` advice via `AnnotationTransactionAttributeSource`.","If `@Around` advice calls `pjp.proceed()` and the target throws an exception, and the advice does not catch it (or re-throws), the exception propagates normally to the caller."]},{"id":"spring/spring_batch#537d89e78332","section":"spring","module":"spring/spring_batch","moduleName":"spring batch","sourceFile":"README.md","difficulty":"core","question":"What is the chunk-oriented processing model in Spring Batch, and why does it matter for transactional safety?","answerFull":"In chunk-oriented processing, items flow through three phases — ItemReader, ItemProcessor, ItemWriter — in configurable lots called chunks (e.g., chunk(500)). The framework reads and processes 500 items, then writes them all in a single transaction. If the write fails, only those 500 items roll back; previously committed chunks (items 1–499, 500–999) are not affected. This is fundamentally different from wrapping an entire million-row loop in one transaction (which holds locks for minutes) or processing one item per transaction (which creates 1M DB round trips). Chunk size is the key tuning parameter: too small = excessive commit overhead; too large = big rollback penalty on failure.","correct":"In chunk-oriented processing, items flow through three phases — ItemReader, ItemProcessor, ItemWriter — in configurable lots called chunks (e.g., chunk(500)).","distractors":["JobInstanceAlreadyCompleteException is thrown when JobLauncher.run() is called with Job + JobParameters that match an already-COMPLETED JobInstance.","In remote chunking, the manager step runs the ItemReader and sends items over a message queue (JMS/RabbitMQ) to worker JVMs, which run the ItemProcessor and ItemWriter.","The chunk's transaction is rolled back."],"distractorIds":["spring/spring_batch#6a6ee4875adf","spring/spring_batch#3acb7e35c6a7","spring/spring_batch#a55a89d119c3"],"concepts":["lots","itemprocessor","itemreader","itemwriter","matter","oriented"],"correctMd":"In chunk-oriented processing, items flow through three phases — `ItemReader`, `ItemProcessor`, `ItemWriter` — in configurable lots called chunks (e.g., `chunk(500)`).","answerFullMd":"In chunk-oriented processing, items flow through three phases — `ItemReader`, `ItemProcessor`, `ItemWriter` — in configurable lots called chunks (e.g., `chunk(500)`). The framework reads and processes 500 items, then writes them all in a single transaction. If the write fails, only those 500 items roll back; previously committed chunks (items 1–499, 500–999) are not affected. This is fundamentally different from wrapping an entire million-row loop in one transaction (which holds locks for minutes) or processing one item per transaction (which creates 1M DB round trips). Chunk size is the key tuning parameter: too small = excessive commit overhead; too large = big rollback penalty on failure.","distractorsMd":["`JobInstanceAlreadyCompleteException` is thrown when `JobLauncher.run()` is called with `Job + JobParameters` that match an already-COMPLETED `JobInstance`.","In remote chunking, the manager step runs the `ItemReader` and sends items over a message queue (JMS/RabbitMQ) to worker JVMs, which run the `ItemProcessor` and `ItemWriter`.",null]},{"id":"spring/spring_batch#aeb338d9f1ef","section":"spring","module":"spring/spring_batch","moduleName":"spring batch","sourceFile":"README.md","difficulty":"core","question":"What is the difference between @StepScope and @JobScope, and when do you need each?","answerFull":"@StepScope is a Spring Batch custom scope that creates a new bean instance per StepExecution. It enables SpEL late-binding of jobParameters and stepExecutionContext — the values are only available when the step starts, not at application context refresh. @JobScope creates a new instance per JobExecution — available for beans that need jobParameters but are shared across steps. Typical usage: @StepScope for ItemReader/ItemWriter that need the step's partition context (stepExecutionContext['minId']); @JobScope for a shared ItemProcessor that needs a jobParameter but is reused by multiple steps. Without the appropriate scope, SpEL resolves to null because the execution context does not yet exist.","correct":"@StepScope is a Spring Batch custom scope that creates a new bean instance per StepExecution.","distractors":["Partitioning splits a large dataset into N non-overlapping sub-ranges (partitions), each processed by an independent StepExecution.","CompositeItemProcessor<I, O> chains multiple ItemProcessors sequentially: the output of processor-1 becomes the input of processor-2.","In chunk-oriented processing, items flow through three phases — ItemReader, ItemProcessor, ItemWriter — in configurable lots called chunks (e.g., chunk(500))."],"distractorIds":["spring/spring_batch#689731e00002","spring/spring_batch#45e078a41cb3","spring/spring_batch#537d89e78332"],"concepts":["stepscope","stepexecution","difference","scope","custom","bean"],"questionMd":"What is the difference between `@StepScope` and `@JobScope`, and when do you need each?","correctMd":"`@StepScope` is a Spring Batch custom scope that creates a new bean instance per `StepExecution`.","answerFullMd":"`@StepScope` is a Spring Batch custom scope that creates a new bean instance per `StepExecution`. It enables SpEL late-binding of `jobParameters` and `stepExecutionContext` — the values are only available when the step starts, not at application context refresh. `@JobScope` creates a new instance per `JobExecution` — available for beans that need `jobParameters` but are shared across steps. Typical usage: `@StepScope` for `ItemReader`/`ItemWriter` that need the step's partition context (`stepExecutionContext['minId']`); `@JobScope` for a shared `ItemProcessor` that needs a `jobParameter` but is reused by multiple steps. Without the appropriate scope, SpEL resolves to null because the execution context does not yet exist.","distractorsMd":["Partitioning splits a large dataset into N non-overlapping sub-ranges (partitions), each processed by an independent `StepExecution`.","`CompositeItemProcessor<I, O>` chains multiple `ItemProcessor`s sequentially: the output of processor-1 becomes the input of processor-2.","In chunk-oriented processing, items flow through three phases — `ItemReader`, `ItemProcessor`, `ItemWriter` — in configurable lots called chunks (e.g., `chunk(500)`)."]},{"id":"spring/spring_batch#d26b300f8a8a","section":"spring","module":"spring/spring_batch","moduleName":"spring batch","sourceFile":"README.md","difficulty":"core","question":"Describe the skip and retry policies. How does Spring Batch handle a bad item that causes an exception?","answerFull":"faultTolerant().skip(ExceptionClass.class).skipLimit(N) configures item-level skipping. When an exception occurs during read or process, Spring Batch increments the skip count and marks the item as skipped (logged to BATCH_STEP_EXECUTION.SKIP_COUNT). faultTolerant().retry(ExceptionClass.class).retryLimit(M).backOffPolicy(policy) configures writer-level retry: if the writer throws a retryable exception, the entire chunk is retried up to M times with the specified back-off. If all retries are exhausted, Spring Batch performs a \"binary search\" — it re-processes each item in the chunk individually to identify the bad one, then skips it (up to skipLimit). This item-scan pass ensures bad items are isolated without re-committing all other items.","correct":"faultTolerant().skip(ExceptionClass.class).skipLimit(N) configures item-level skipping.","distractors":["@StepScope is a Spring Batch custom scope that creates a new bean instance per StepExecution.","CompositeItemProcessor<I, O> chains multiple ItemProcessors sequentially: the output of processor-1 becomes the input of processor-2.","In chunk-oriented processing, items flow through three phases — ItemReader, ItemProcessor, ItemWriter — in configurable lots called chunks (e.g., chunk(500))."],"distractorIds":["spring/spring_batch#aeb338d9f1ef","spring/spring_batch#45e078a41cb3","spring/spring_batch#537d89e78332"],"concepts":["describe","exceptionclass","faulttolerant","skiplimit","skipping","bad"],"correctMd":"`faultTolerant().skip(ExceptionClass.class).skipLimit(N)` configures item-level skipping.","answerFullMd":"`faultTolerant().skip(ExceptionClass.class).skipLimit(N)` configures item-level skipping. When an exception occurs during read or process, Spring Batch increments the skip count and marks the item as skipped (logged to `BATCH_STEP_EXECUTION.SKIP_COUNT`). `faultTolerant().retry(ExceptionClass.class).retryLimit(M).backOffPolicy(policy)` configures writer-level retry: if the writer throws a retryable exception, the entire chunk is retried up to M times with the specified back-off. If all retries are exhausted, Spring Batch performs a \"binary search\" — it re-processes each item in the chunk individually to identify the bad one, then skips it (up to `skipLimit`). This item-scan pass ensures bad items are isolated without re-committing all other items.","distractorsMd":["`@StepScope` is a Spring Batch custom scope that creates a new bean instance per `StepExecution`.","`CompositeItemProcessor<I, O>` chains multiple `ItemProcessor`s sequentially: the output of processor-1 becomes the input of processor-2.","In chunk-oriented processing, items flow through three phases — `ItemReader`, `ItemProcessor`, `ItemWriter` — in configurable lots called chunks (e.g., `chunk(500)`)."]},{"id":"spring/spring_batch#6a6ee4875adf","section":"spring","module":"spring/spring_batch","moduleName":"spring batch","sourceFile":"README.md","difficulty":"core","question":"What causes JobInstanceAlreadyCompleteException and how do you prevent it for repeatable jobs?","answerFull":"JobInstanceAlreadyCompleteException is thrown when JobLauncher.run() is called with Job + JobParameters that match an already-COMPLETED JobInstance. Spring Batch uses this uniqueness to guarantee idempotency: re-running a completed job with the same parameters is rejected to prevent accidental double-processing. To run the same logical job repeatedly (nightly batch): add a time-varying parameter like addLong(\"run.id\", System.currentTimeMillis()) or addString(\"runDate\", today). For true idempotency (re-runnable for the same date), use JobParametersIncrementer to automatically add a sequence number to the parameters.","correct":"JobInstanceAlreadyCompleteException is thrown when JobLauncher.run() is called with Job + JobParameters that match an already-COMPLETED JobInstance.","distractors":["In chunk-oriented processing, items flow through three phases — ItemReader, ItemProcessor, ItemWriter — in configurable lots called chunks (e.g., chunk(500)).","In remote chunking, the manager step runs the ItemReader and sends items over a message queue (JMS/RabbitMQ) to worker JVMs, which run the ItemProcessor and ItemWriter.","@StepScope is a Spring Batch custom scope that creates a new bean instance per StepExecution."],"distractorIds":["spring/spring_batch#537d89e78332","spring/spring_batch#3acb7e35c6a7","spring/spring_batch#aeb338d9f1ef"],"concepts":["jobinstance","jobinstancealreadycompleteexception","joblauncher","jobparameters","repeatable","thrown"],"questionMd":"What causes `JobInstanceAlreadyCompleteException` and how do you prevent it for repeatable jobs?","correctMd":"`JobInstanceAlreadyCompleteException` is thrown when `JobLauncher.run()` is called with `Job + JobParameters` that match an already-COMPLETED `JobInstance`.","answerFullMd":"`JobInstanceAlreadyCompleteException` is thrown when `JobLauncher.run()` is called with `Job + JobParameters` that match an already-COMPLETED `JobInstance`. Spring Batch uses this uniqueness to guarantee idempotency: re-running a completed job with the same parameters is rejected to prevent accidental double-processing. To run the same logical job repeatedly (nightly batch): add a time-varying parameter like `addLong(\"run.id\", System.currentTimeMillis())` or `addString(\"runDate\", today)`. For true idempotency (re-runnable for the same date), use `JobParametersIncrementer` to automatically add a sequence number to the parameters.","distractorsMd":["In chunk-oriented processing, items flow through three phases — `ItemReader`, `ItemProcessor`, `ItemWriter` — in configurable lots called chunks (e.g., `chunk(500)`).","In remote chunking, the manager step runs the `ItemReader` and sends items over a message queue (JMS/RabbitMQ) to worker JVMs, which run the `ItemProcessor` and `ItemWriter`.","`@StepScope` is a Spring Batch custom scope that creates a new bean instance per `StepExecution`."]},{"id":"spring/spring_batch#689731e00002","section":"spring","module":"spring/spring_batch","moduleName":"spring batch","sourceFile":"README.md","difficulty":"intermediate","question":"How does partitioning improve throughput, and what is the difference between local and remote partitioning?","answerFull":"Partitioning splits a large dataset into N non-overlapping sub-ranges (partitions), each processed by an independent StepExecution. Local partitioning (TaskExecutorPartitionHandler) runs all partition steps in parallel on N threads of the same JVM — limited to available CPU cores. Remote partitioning runs partition steps on separate JVM instances (workers), communicating via a message broker (JMS/RabbitMQ/Kafka). Remote partitioning allows horizontal scale-out: a dataset of 1 billion records can be spread across 100 worker pods, each handling 10 million. Each worker's progress is independently tracked in JobRepository, so individual partitions can be restarted without reprocessing the others.","correct":"Partitioning splits a large dataset into N non-overlapping sub-ranges (partitions), each processed by an independent StepExecution.","distractors":["@StepScope is a Spring Batch custom scope that creates a new bean instance per StepExecution.","In remote chunking, the manager step runs the ItemReader and sends items over a message queue (JMS/RabbitMQ) to worker JVMs, which run the ItemProcessor and ItemWriter.","CompositeItemProcessor<I, O> chains multiple ItemProcessors sequentially: the output of processor-1 becomes the input of processor-2."],"distractorIds":["spring/spring_batch#aeb338d9f1ef","spring/spring_batch#3acb7e35c6a7","spring/spring_batch#45e078a41cb3"],"concepts":["stepexecution","partitioning","difference","ranges","improve","overlapping"],"correctMd":"Partitioning splits a large dataset into N non-overlapping sub-ranges (partitions), each processed by an independent `StepExecution`.","answerFullMd":"Partitioning splits a large dataset into N non-overlapping sub-ranges (partitions), each processed by an independent `StepExecution`. Local partitioning (`TaskExecutorPartitionHandler`) runs all partition steps in parallel on N threads of the same JVM — limited to available CPU cores. Remote partitioning runs partition steps on separate JVM instances (workers), communicating via a message broker (JMS/RabbitMQ/Kafka). Remote partitioning allows horizontal scale-out: a dataset of 1 billion records can be spread across 100 worker pods, each handling 10 million. Each worker's progress is independently tracked in `JobRepository`, so individual partitions can be restarted without reprocessing the others.","distractorsMd":["`@StepScope` is a Spring Batch custom scope that creates a new bean instance per `StepExecution`.","In remote chunking, the manager step runs the `ItemReader` and sends items over a message queue (JMS/RabbitMQ) to worker JVMs, which run the `ItemProcessor` and `ItemWriter`.","`CompositeItemProcessor<I, O>` chains multiple `ItemProcessor`s sequentially: the output of processor-1 becomes the input of processor-2."]},{"id":"spring/spring_batch#bcb6df21d443","section":"spring","module":"spring/spring_batch","moduleName":"spring batch","sourceFile":"README.md","difficulty":"intermediate","question":"What is JdbcPagingItemReader and why must the sort key be unique?","answerFull":"JdbcPagingItemReader reads a database table in pages using keyset (cursor-based) pagination. It queries SELECT ... WHERE id > :lastId ORDER BY id LIMIT pageSize — using the sort key's last value to start the next page. If the sort key is not unique (e.g., status column), page boundaries may overlap or gap: if multiple rows have status = 'PENDING' that straddle a page boundary, some rows may appear in two pages (duplicates) or be missed after a re-sort. Always append a unique primary key as the final sort dimension: Map.of(\"createdAt\", ASCENDING, \"id\", ASCENDING). On restart, the reader re-reads lastReadCount from ExecutionContext and reconstructs the correct WHERE clause.","correct":"JdbcPagingItemReader reads a database table in pages using keyset (cursor-based) pagination.","distractors":["@StepScope is a Spring Batch custom scope that creates a new bean instance per StepExecution.","CompositeItemProcessor<I, O> chains multiple ItemProcessors sequentially: the output of processor-1 becomes the input of processor-2.","In chunk-oriented processing, items flow through three phases — ItemReader, ItemProcessor, ItemWriter — in configurable lots called chunks (e.g., chunk(500))."],"distractorIds":["spring/spring_batch#aeb338d9f1ef","spring/spring_batch#45e078a41cb3","spring/spring_batch#537d89e78332"],"concepts":["jdbcpagingitemreader","keyset","pagination","cursor","sort","unique"],"questionMd":"What is `JdbcPagingItemReader` and why must the sort key be unique?","correctMd":"`JdbcPagingItemReader` reads a database table in pages using keyset (cursor-based) pagination.","answerFullMd":"`JdbcPagingItemReader` reads a database table in pages using keyset (cursor-based) pagination. It queries `SELECT ... WHERE id > :lastId ORDER BY id LIMIT pageSize` — using the sort key's last value to start the next page. If the sort key is not unique (e.g., `status` column), page boundaries may overlap or gap: if multiple rows have `status = 'PENDING'` that straddle a page boundary, some rows may appear in two pages (duplicates) or be missed after a re-sort. Always append a unique primary key as the final sort dimension: `Map.of(\"createdAt\", ASCENDING, \"id\", ASCENDING)`. On restart, the reader re-reads `lastReadCount` from `ExecutionContext` and reconstructs the correct WHERE clause.","distractorsMd":["`@StepScope` is a Spring Batch custom scope that creates a new bean instance per `StepExecution`.","`CompositeItemProcessor<I, O>` chains multiple `ItemProcessor`s sequentially: the output of processor-1 becomes the input of processor-2.","In chunk-oriented processing, items flow through three phases — `ItemReader`, `ItemProcessor`, `ItemWriter` — in configurable lots called chunks (e.g., `chunk(500)`)."]},{"id":"spring/spring_batch#a55a89d119c3","section":"spring","module":"spring/spring_batch","moduleName":"spring batch","sourceFile":"README.md","difficulty":"intermediate","question":"What happens to the current chunk's transaction when an ItemWriter throws a non-retryable exception?","answerFull":"The chunk's transaction is rolled back. All write operations for that chunk (typically chunk-size items) are undone. Spring Batch increments StepExecution.rollbackCount. If the exception is classified as skippable and the skip limit is not exceeded, Spring Batch attempts to identify the offending item via item-scanning (retry each item individually). Items that succeed in isolation are committed one-by-one; the single offending item is skipped. If the exception is not skippable, the StepExecution is marked FAILED, the JobExecution is marked FAILED, and processing stops.","correct":"The chunk's transaction is rolled back.","distractors":["Partitioning splits a large dataset into N non-overlapping sub-ranges (partitions), each processed by an independent StepExecution.","In remote chunking, the manager step runs the ItemReader and sends items over a message queue (JMS/RabbitMQ) to worker JVMs, which run the ItemProcessor and ItemWriter.","In chunk-oriented processing, items flow through three phases — ItemReader, ItemProcessor, ItemWriter — in configurable lots called chunks (e.g., chunk(500))."],"distractorIds":["spring/spring_batch#689731e00002","spring/spring_batch#3acb7e35c6a7","spring/spring_batch#537d89e78332"],"concepts":["itemwriter","retryable","rolled","chunk","happens","throws"],"answerFullMd":"The chunk's transaction is rolled back. All write operations for that chunk (typically `chunk-size` items) are undone. Spring Batch increments `StepExecution.rollbackCount`. If the exception is classified as skippable and the skip limit is not exceeded, Spring Batch attempts to identify the offending item via item-scanning (retry each item individually). Items that succeed in isolation are committed one-by-one; the single offending item is skipped. If the exception is not skippable, the `StepExecution` is marked `FAILED`, the `JobExecution` is marked `FAILED`, and processing stops.","distractorsMd":["Partitioning splits a large dataset into N non-overlapping sub-ranges (partitions), each processed by an independent `StepExecution`.","In remote chunking, the manager step runs the `ItemReader` and sends items over a message queue (JMS/RabbitMQ) to worker JVMs, which run the `ItemProcessor` and `ItemWriter`.","In chunk-oriented processing, items flow through three phases — `ItemReader`, `ItemProcessor`, `ItemWriter` — in configurable lots called chunks (e.g., `chunk(500)`)."]},{"id":"spring/spring_batch#45e078a41cb3","section":"spring","module":"spring/spring_batch","moduleName":"spring batch","sourceFile":"README.md","difficulty":"intermediate","question":"How does CompositeItemProcessor work, and what is a ClassifierCompositeItemWriter?","answerFull":"CompositeItemProcessor<I, O> chains multiple ItemProcessors sequentially: the output of processor-1 becomes the input of processor-2. Type alignment is required (O of step N must be assignable to I of step N+1). Use case: validate → enrich → transform, where each concern is in its own processor class. ClassifierCompositeItemWriter<T> routes items to different ItemWriters based on a Classifier<T, ItemWriter<T>> — a strategy function. Use case: write premium customers to PostgreSQL and standard customers to Redis, both in the same step. This avoids forking the pipeline into two steps when the routing logic is item-level.","correct":"CompositeItemProcessor<I, O> chains multiple ItemProcessors sequentially: the output of processor-1 becomes the input of processor-2.","distractors":["@StepScope is a Spring Batch custom scope that creates a new bean instance per StepExecution.","In chunk-oriented processing, items flow through three phases — ItemReader, ItemProcessor, ItemWriter — in configurable lots called chunks (e.g., chunk(500)).","In remote chunking, the manager step runs the ItemReader and sends items over a message queue (JMS/RabbitMQ) to worker JVMs, which run the ItemProcessor and ItemWriter."],"distractorIds":["spring/spring_batch#aeb338d9f1ef","spring/spring_batch#537d89e78332","spring/spring_batch#3acb7e35c6a7"],"concepts":["compositeitemprocessor","itemprocessors","sequentially","processor","chains","becomes"],"questionMd":"How does `CompositeItemProcessor` work, and what is a `ClassifierCompositeItemWriter`?","correctMd":"`CompositeItemProcessor<I, O>` chains multiple `ItemProcessor`s sequentially: the output of processor-1 becomes the input of processor-2.","answerFullMd":"`CompositeItemProcessor<I, O>` chains multiple `ItemProcessor`s sequentially: the output of processor-1 becomes the input of processor-2. Type alignment is required (`O` of step N must be assignable to `I` of step N+1). Use case: validate → enrich → transform, where each concern is in its own processor class. `ClassifierCompositeItemWriter<T>` routes items to different `ItemWriter`s based on a `Classifier<T, ItemWriter<T>>` — a strategy function. Use case: write premium customers to PostgreSQL and standard customers to Redis, both in the same step. This avoids forking the pipeline into two steps when the routing logic is item-level.","distractorsMd":["`@StepScope` is a Spring Batch custom scope that creates a new bean instance per `StepExecution`.","In chunk-oriented processing, items flow through three phases — `ItemReader`, `ItemProcessor`, `ItemWriter` — in configurable lots called chunks (e.g., `chunk(500)`).","In remote chunking, the manager step runs the `ItemReader` and sends items over a message queue (JMS/RabbitMQ) to worker JVMs, which run the `ItemProcessor` and `ItemWriter`."]},{"id":"spring/spring_batch#3acb7e35c6a7","section":"spring","module":"spring/spring_batch","moduleName":"spring batch","sourceFile":"README.md","difficulty":"advanced","question":"What is remote chunking and when would you choose it over remote partitioning?","answerFull":"In remote chunking, the manager step runs the ItemReader and sends items over a message queue (JMS/RabbitMQ) to worker JVMs, which run the ItemProcessor and ItemWriter. The manager waits for all workers to acknowledge before committing. Use remote chunking when the reader is a bottleneck-free singleton (e.g., one file) but processing is expensive and horizontally scalable (e.g., ML enrichment, external API calls). In remote partitioning, both reading AND processing happen on workers — the manager only partitions the key space. Use remote partitioning when the dataset is in a horizontally partitioned store (sharded DB, partitioned Kafka topic) and the reader itself can be distributed. Remote chunking requires exactly-once delivery guarantees from the message broker; remote partitioning requires only the partition key assignment to be correct.","correct":"In remote chunking, the manager step runs the ItemReader and sends items over a message queue (JMS/RabbitMQ) to worker JVMs, which run the ItemProcessor and ItemWriter.","distractors":["JobInstanceAlreadyCompleteException is thrown when JobLauncher.run() is called with Job + JobParameters that match an already-COMPLETED JobInstance.","Partitioning splits a large dataset into N non-overlapping sub-ranges (partitions), each processed by an independent StepExecution.","In chunk-oriented processing, items flow through three phases — ItemReader, ItemProcessor, ItemWriter — in configurable lots called chunks (e.g., chunk(500))."],"distractorIds":["spring/spring_batch#6a6ee4875adf","spring/spring_batch#689731e00002","spring/spring_batch#537d89e78332"],"concepts":["itemprocessor","itemreader","itemwriter","jvms","jms","rabbitmq"],"correctMd":"In remote chunking, the manager step runs the `ItemReader` and sends items over a message queue (JMS/RabbitMQ) to worker JVMs, which run the `ItemProcessor` and `ItemWriter`.","answerFullMd":"In remote chunking, the manager step runs the `ItemReader` and sends items over a message queue (JMS/RabbitMQ) to worker JVMs, which run the `ItemProcessor` and `ItemWriter`. The manager waits for all workers to acknowledge before committing. Use remote chunking when the reader is a bottleneck-free singleton (e.g., one file) but processing is expensive and horizontally scalable (e.g., ML enrichment, external API calls). In remote partitioning, both reading AND processing happen on workers — the manager only partitions the key space. Use remote partitioning when the dataset is in a horizontally partitioned store (sharded DB, partitioned Kafka topic) and the reader itself can be distributed. Remote chunking requires exactly-once delivery guarantees from the message broker; remote partitioning requires only the partition key assignment to be correct.","distractorsMd":["`JobInstanceAlreadyCompleteException` is thrown when `JobLauncher.run()` is called with `Job + JobParameters` that match an already-COMPLETED `JobInstance`.","Partitioning splits a large dataset into N non-overlapping sub-ranges (partitions), each processed by an independent `StepExecution`.","In chunk-oriented processing, items flow through three phases — `ItemReader`, `ItemProcessor`, `ItemWriter` — in configurable lots called chunks (e.g., `chunk(500)`)."]},{"id":"spring/spring_batch#293b3c0449ab","section":"spring","module":"spring/spring_batch","moduleName":"spring batch","sourceFile":"README.md","difficulty":"advanced","question":"What are ItemStream and ExecutionContext, and why are they important for restartability?","answerFull":"ItemStream is an interface with three methods: open(ExecutionContext), update(ExecutionContext), close(). ExecutionContext is a key-value map persisted per StepExecution. On startup, open() is called with the saved context (may be empty for a new execution, or populated for a restart). After each chunk, update() is called — the reader/writer serialises its progress (e.g., \"I've read 5,000 records\") into the context, which is written to BATCH_STEP_EXECUTION_CONTEXT. On restart, open() receives the saved context and can fast-forward to the correct starting position. Most built-in readers (FlatFileItemReader, JdbcPagingItemReader) implement ItemStream automatically. Custom readers must implement it to enable restartability.","correct":"ItemStream is an interface with three methods: open(ExecutionContext), update(ExecutionContext), close().","distractors":["@StepScope is a Spring Batch custom scope that creates a new bean instance per StepExecution.","CompositeItemProcessor<I, O> chains multiple ItemProcessors sequentially: the output of processor-1 becomes the input of processor-2.","In chunk-oriented processing, items flow through three phases — ItemReader, ItemProcessor, ItemWriter — in configurable lots called chunks (e.g., chunk(500))."],"distractorIds":["spring/spring_batch#aeb338d9f1ef","spring/spring_batch#45e078a41cb3","spring/spring_batch#537d89e78332"],"concepts":["executioncontext","itemstream","important","close","open","interface"],"questionMd":"What are `ItemStream` and `ExecutionContext`, and why are they important for restartability?","correctMd":"`ItemStream` is an interface with three methods: `open(ExecutionContext)`, `update(ExecutionContext)`, `close()`.","answerFullMd":"`ItemStream` is an interface with three methods: `open(ExecutionContext)`, `update(ExecutionContext)`, `close()`. `ExecutionContext` is a key-value map persisted per `StepExecution`. On startup, `open()` is called with the saved context (may be empty for a new execution, or populated for a restart). After each chunk, `update()` is called — the reader/writer serialises its progress (e.g., \"I've read 5,000 records\") into the context, which is written to `BATCH_STEP_EXECUTION_CONTEXT`. On restart, `open()` receives the saved context and can fast-forward to the correct starting position. Most built-in readers (`FlatFileItemReader`, `JdbcPagingItemReader`) implement `ItemStream` automatically. Custom readers must implement it to enable restartability.","distractorsMd":["`@StepScope` is a Spring Batch custom scope that creates a new bean instance per `StepExecution`.","`CompositeItemProcessor<I, O>` chains multiple `ItemProcessor`s sequentially: the output of processor-1 becomes the input of processor-2.","In chunk-oriented processing, items flow through three phases — `ItemReader`, `ItemProcessor`, `ItemWriter` — in configurable lots called chunks (e.g., `chunk(500)`)."]},{"id":"spring/spring_boot_actuator#834c221a4610","section":"spring","module":"spring/spring_boot_actuator","moduleName":"spring boot actuator","sourceFile":"README.md","difficulty":"core","question":"What is the difference between liveness and readiness probes in Spring Boot Actuator?","answerFull":"Liveness probe (/actuator/health/liveness) indicates whether the application is in a broken state that requires a restart. If liveness fails, Kubernetes kills and restarts the pod. Readiness probe (/actuator/health/readiness) indicates whether the application is ready to receive traffic. If readiness fails, Kubernetes removes the pod from the load balancer but does not restart it — it waits for readiness to recover. Liveness should only fail for truly unrecoverable states (deadlock, corrupted state). Readiness should fail during startup (before all beans are ready) and when dependent services are unavailable.","correct":"Liveness probe (/actuator/health/liveness) indicates whether the application is in a broken state that requires a restart.","distractors":["/actuator/startup shows a timeline of ApplicationContext startup steps with timing for each bean initialization.","GET /actuator/loggers returns all configured loggers and their current levels.","@Timed(\"my.operation\") on a Spring bean method (or class, to instrument all methods) instruments it via AOP — a Timer is automatically started/stopped around the method invocation."],"distractorIds":["spring/spring_boot_actuator#ce2df6045558","spring/spring_boot_actuator#8442308e22a1","spring/spring_boot_actuator#2e25bb4cbcd3"],"concepts":["indicates","probes","broken","liveness","actuator","probe"],"correctMd":"Liveness probe (`/actuator/health/liveness`) indicates whether the application is in a broken state that requires a restart.","answerFullMd":"Liveness probe (`/actuator/health/liveness`) indicates whether the application is in a broken state that requires a restart. If liveness fails, Kubernetes kills and restarts the pod. Readiness probe (`/actuator/health/readiness`) indicates whether the application is ready to receive traffic. If readiness fails, Kubernetes removes the pod from the load balancer but does not restart it — it waits for readiness to recover. Liveness should only fail for truly unrecoverable states (deadlock, corrupted state). Readiness should fail during startup (before all beans are ready) and when dependent services are unavailable.","distractorsMd":["`/actuator/startup` shows a timeline of ApplicationContext startup steps with timing for each bean initialization.","`GET /actuator/loggers` returns all configured loggers and their current levels.","`@Timed(\"my.operation\")` on a Spring bean method (or class, to instrument all methods) instruments it via AOP — a `Timer` is automatically started/stopped around the method invocation."]},{"id":"spring/spring_boot_actuator#17c4896daf57","section":"spring","module":"spring/spring_boot_actuator","moduleName":"spring boot actuator","sourceFile":"README.md","difficulty":"core","question":"What is Micrometer and how does it relate to Spring Boot Actuator?","answerFull":"Micrometer is a metrics instrumentation library that provides a vendor-neutral API for recording application metrics. Spring Boot Actuator auto-configures a MeterRegistry based on what's on the classpath. Micrometer supports multiple backends (Prometheus, Datadog, CloudWatch, Graphite, InfluxDB) — switching backends requires only changing the registry dependency, not the application code. Actuator exposes /actuator/metrics (JSON) and /actuator/prometheus (Prometheus text format) endpoints backed by Micrometer.","correct":"Micrometer is a metrics instrumentation library that provides a vendor-neutral API for recording application metrics.","distractors":["Liveness probe (/actuator/health/liveness) indicates whether the application is in a broken state that requires a restart.","Configure Spring Security to require authentication for /actuator/, expose only safe endpoints, and use a separate management port.","/actuator/startup shows a timeline of ApplicationContext startup steps with timing for each bean initialization."],"distractorIds":["spring/spring_boot_actuator#834c221a4610","spring/spring_boot_actuator#5c7155e13951","spring/spring_boot_actuator#ce2df6045558"],"concepts":["neutral","instrumentation","vendor","recording","actuator","micrometer"],"answerFullMd":"Micrometer is a metrics instrumentation library that provides a vendor-neutral API for recording application metrics. Spring Boot Actuator auto-configures a `MeterRegistry` based on what's on the classpath. Micrometer supports multiple backends (Prometheus, Datadog, CloudWatch, Graphite, InfluxDB) — switching backends requires only changing the registry dependency, not the application code. Actuator exposes `/actuator/metrics` (JSON) and `/actuator/prometheus` (Prometheus text format) endpoints backed by Micrometer.","distractorsMd":["Liveness probe (`/actuator/health/liveness`) indicates whether the application is in a broken state that requires a restart.","Configure Spring Security to require authentication for `/actuator/**`, expose only safe endpoints, and use a separate management port.","`/actuator/startup` shows a timeline of ApplicationContext startup steps with timing for each bean initialization."]},{"id":"spring/spring_boot_actuator#7c1744d9af71","section":"spring","module":"spring/spring_boot_actuator","moduleName":"spring boot actuator","sourceFile":"README.md","difficulty":"core","question":"What are the four main Micrometer meter types?","answerFull":"Counter is a monotonically increasing value (total requests, errors). Timer records both count and duration with percentile support (request processing time). Gauge represents a value that can go up and down (active connections, queue size, cache size). DistributionSummary is similar to Timer but records values that aren't durations (request body size, payload size). All meters support tags (dimensions) for slicing metrics by region, service, status code. Tags must be low-cardinality — avoid user IDs, request IDs, or other high-cardinality values.","correct":"Counter is a monotonically increasing value (total requests, errors).","distractors":["HikariCP auto-registers metrics with Micrometer when micrometer-core is on the classpath and metricRegistry is configured.","Micrometer Tracing (formerly Spring Cloud Sleuth) is a tracing facade over concrete implementations (Brave/Zipkin or OpenTelemetry).","Micrometer is a metrics instrumentation library that provides a vendor-neutral API for recording application metrics."],"distractorIds":["spring/spring_boot_actuator#0b99fdd469ab","spring/spring_boot_actuator#491e6c4a2a7e","spring/spring_boot_actuator#17c4896daf57"],"concepts":["meter","micrometer","monotonically","counter","four","increasing"],"correctMd":"`Counter` is a monotonically increasing value (total requests, errors).","answerFullMd":"`Counter` is a monotonically increasing value (total requests, errors). `Timer` records both count and duration with percentile support (request processing time). `Gauge` represents a value that can go up and down (active connections, queue size, cache size). `DistributionSummary` is similar to Timer but records values that aren't durations (request body size, payload size). All meters support tags (dimensions) for slicing metrics by region, service, status code. Tags must be low-cardinality — avoid user IDs, request IDs, or other high-cardinality values.","distractorsMd":["HikariCP auto-registers metrics with Micrometer when `micrometer-core` is on the classpath and `metricRegistry` is configured.",null,null]},{"id":"spring/spring_boot_actuator#5c7155e13951","section":"spring","module":"spring/spring_boot_actuator","moduleName":"spring boot actuator","sourceFile":"README.md","difficulty":"core","question":"How do you secure Spring Boot Actuator endpoints in production?","answerFull":"Configure Spring Security to require authentication for /actuator/, expose only safe endpoints, and use a separate management port. Set management.endpoints.web.exposure.include=health,info,metrics as a whitelist. Require ACTUATOR_ADMIN role for sensitive operations like /actuator/loggers and /actuator/beans. Use a separate management port (management.server.port=8081) not exposed by the load balancer. Never expose /actuator/heapdump, /actuator/env, or /actuator/shutdown without strong authentication and audit logging.","correct":"Configure Spring Security to require authentication for /actuator/, expose only safe endpoints, and use a separate management port.","distractors":["Liveness probe (/actuator/health/liveness) indicates whether the application is in a broken state that requires a restart.","Micrometer Tracing (formerly Spring Cloud Sleuth) is a tracing facade over concrete implementations (Brave/Zipkin or OpenTelemetry).","GET /actuator/loggers returns all configured loggers and their current levels."],"distractorIds":["spring/spring_boot_actuator#834c221a4610","spring/spring_boot_actuator#491e6c4a2a7e","spring/spring_boot_actuator#8442308e22a1"],"concepts":["secure","actuator","configure","expose","endpoints","management"],"correctMd":"Configure Spring Security to require authentication for `/actuator/**`, expose only safe endpoints, and use a separate management port.","answerFullMd":"Configure Spring Security to require authentication for `/actuator/**`, expose only safe endpoints, and use a separate management port. Set `management.endpoints.web.exposure.include=health,info,metrics` as a whitelist. Require `ACTUATOR_ADMIN` role for sensitive operations like `/actuator/loggers` and `/actuator/beans`. Use a separate management port (`management.server.port=8081`) not exposed by the load balancer. Never expose `/actuator/heapdump`, `/actuator/env`, or `/actuator/shutdown` without strong authentication and audit logging.","distractorsMd":["Liveness probe (`/actuator/health/liveness`) indicates whether the application is in a broken state that requires a restart.",null,"`GET /actuator/loggers` returns all configured loggers and their current levels."]},{"id":"spring/spring_boot_actuator#67adf3326143","section":"spring","module":"spring/spring_boot_actuator","moduleName":"spring boot actuator","sourceFile":"README.md","difficulty":"core","question":"What is the CompositeHealth structure in Actuator?","answerFull":"CompositeHealth aggregates the results of multiple HealthIndicator or HealthContributor beans. The overall status is determined by the worst status among all contributors: DOWN > OUT_OF_SERVICE > UNKNOWN > UP. Configure status order via management.endpoint.health.status.order. Individual components are shown under components key when show-components=always. Health groups allow separate health endpoints with different subsets of indicators — for example, a readiness group including only db and redis but not slow external APIs.","correct":"CompositeHealth aggregates the results of multiple HealthIndicator or HealthContributor beans.","distractors":["Liveness probe (/actuator/health/liveness) indicates whether the application is in a broken state that requires a restart.","Configure Spring Security to require authentication for /actuator/, expose only safe endpoints, and use a separate management port.","/actuator/startup shows a timeline of ApplicationContext startup steps with timing for each bean initialization."],"distractorIds":["spring/spring_boot_actuator#834c221a4610","spring/spring_boot_actuator#5c7155e13951","spring/spring_boot_actuator#ce2df6045558"],"concepts":["compositehealth","healthcontributor","healthindicator","actuator","aggregates","beans"],"correctMd":"`CompositeHealth` aggregates the results of multiple `HealthIndicator` or `HealthContributor` beans.","answerFullMd":"`CompositeHealth` aggregates the results of multiple `HealthIndicator` or `HealthContributor` beans. The overall status is determined by the worst status among all contributors: DOWN > OUT_OF_SERVICE > UNKNOWN > UP. Configure status order via `management.endpoint.health.status.order`. Individual components are shown under `components` key when `show-components=always`. Health groups allow separate health endpoints with different subsets of indicators — for example, a `readiness` group including only `db` and `redis` but not slow external APIs.","distractorsMd":["Liveness probe (`/actuator/health/liveness`) indicates whether the application is in a broken state that requires a restart.","Configure Spring Security to require authentication for `/actuator/**`, expose only safe endpoints, and use a separate management port.","`/actuator/startup` shows a timeline of ApplicationContext startup steps with timing for each bean initialization."]},{"id":"spring/spring_boot_actuator#71e2e4954ffb","section":"spring","module":"spring/spring_boot_actuator","moduleName":"spring boot actuator","sourceFile":"README.md","difficulty":"intermediate","question":"How would you implement a custom Actuator endpoint?","answerFull":"Annotate a Spring bean with @Endpoint(id=\"my-endpoint\"). Define @ReadOperation methods (HTTP GET, returns JSON-serializable objects), @WriteOperation methods (HTTP POST), and @DeleteOperation methods. Use @Selector parameter annotation for path variables (/actuator/my-endpoint/{name}). The endpoint is automatically accessible at /actuator/my-endpoint. Expose it via management.endpoints.web.exposure.include=my-endpoint. For web-specific operations, use @WebEndpoint (HTTP only) or @JmxEndpoint (JMX only) instead of @Endpoint (both).","correct":"Annotate a Spring bean with @Endpoint(id=\"my-endpoint\").","distractors":["Liveness probe (/actuator/health/liveness) indicates whether the application is in a broken state that requires a restart.","HealthIndicator is the simple interface: implement health() returning a Health object with status and optional details.","Micrometer Tracing (formerly Spring Cloud Sleuth) is a tracing facade over concrete implementations (Brave/Zipkin or OpenTelemetry)."],"distractorIds":["spring/spring_boot_actuator#834c221a4610","spring/spring_boot_actuator#26653151c513","spring/spring_boot_actuator#491e6c4a2a7e"],"concepts":["annotate","actuator","endpoint","custom","implement","bean"],"correctMd":"Annotate a Spring bean with `@Endpoint(id=\"my-endpoint\")`.","answerFullMd":"Annotate a Spring bean with `@Endpoint(id=\"my-endpoint\")`. Define `@ReadOperation` methods (HTTP GET, returns JSON-serializable objects), `@WriteOperation` methods (HTTP POST), and `@DeleteOperation` methods. Use `@Selector` parameter annotation for path variables (`/actuator/my-endpoint/{name}`). The endpoint is automatically accessible at `/actuator/my-endpoint`. Expose it via `management.endpoints.web.exposure.include=my-endpoint`. For web-specific operations, use `@WebEndpoint` (HTTP only) or `@JmxEndpoint` (JMX only) instead of `@Endpoint` (both).","distractorsMd":["Liveness probe (`/actuator/health/liveness`) indicates whether the application is in a broken state that requires a restart.","`HealthIndicator` is the simple interface: implement `health()` returning a `Health` object with status and optional details.",null]},{"id":"spring/spring_boot_actuator#ce2df6045558","section":"spring","module":"spring/spring_boot_actuator","moduleName":"spring boot actuator","sourceFile":"README.md","difficulty":"intermediate","question":"What does /actuator/startup show and how is it useful?","answerFull":"/actuator/startup shows a timeline of ApplicationContext startup steps with timing for each bean initialization. It requires a BufferingApplicationStartup configured on SpringApplication. This endpoint reveals which beans are slow to initialize (e.g., a @PostConstruct making HTTP calls) and their contribution to total startup time. Essential for diagnosing slow startup in Kubernetes environments where readiness probe timeout must be met. After analysis, the data should be cleared with a DELETE to /actuator/startup to free memory.","correct":"/actuator/startup shows a timeline of ApplicationContext startup steps with timing for each bean initialization.","distractors":["Liveness probe (/actuator/health/liveness) indicates whether the application is in a broken state that requires a restart.","Configure Spring Security to require authentication for /actuator/, expose only safe endpoints, and use a separate management port.","@Timed(\"my.operation\") on a Spring bean method (or class, to instrument all methods) instruments it via AOP — a Timer is automatically started/stopped around the method invocation."],"distractorIds":["spring/spring_boot_actuator#834c221a4610","spring/spring_boot_actuator#5c7155e13951","spring/spring_boot_actuator#2e25bb4cbcd3"],"concepts":["useful","timing","actuator","timeline","show","initialization"],"correctMd":"`/actuator/startup` shows a timeline of ApplicationContext startup steps with timing for each bean initialization.","answerFullMd":"`/actuator/startup` shows a timeline of ApplicationContext startup steps with timing for each bean initialization. It requires a `BufferingApplicationStartup` configured on `SpringApplication`. This endpoint reveals which beans are slow to initialize (e.g., a `@PostConstruct` making HTTP calls) and their contribution to total startup time. Essential for diagnosing slow startup in Kubernetes environments where readiness probe timeout must be met. After analysis, the data should be cleared with a DELETE to `/actuator/startup` to free memory.","distractorsMd":["Liveness probe (`/actuator/health/liveness`) indicates whether the application is in a broken state that requires a restart.","Configure Spring Security to require authentication for `/actuator/**`, expose only safe endpoints, and use a separate management port.","`@Timed(\"my.operation\")` on a Spring bean method (or class, to instrument all methods) instruments it via AOP — a `Timer` is automatically started/stopped around the method invocation."]},{"id":"spring/spring_boot_actuator#af4d20eb6548","section":"spring","module":"spring/spring_boot_actuator","moduleName":"spring boot actuator","sourceFile":"README.md","difficulty":"intermediate","question":"What is the risk of exposing /actuator/heapdump?","answerFull":"A heap dump contains a full snapshot of the JVM heap including all object instances in memory. Sensitive data stored as Java objects — database passwords from DataSource configuration, JWT tokens from SecurityContext, user PII from cached entities — is captured in the dump. Anyone with access to /actuator/heapdump can download a HPROF file and use tools like Eclipse Memory Analyzer (MAT) to extract plaintext credentials. Always restrict this endpoint to authenticated operators via a separate management network. In production, prefer using jmap or jcmd from an operator console rather than exposing it via HTTP.","correct":"A heap dump contains a full snapshot of the JVM heap including all object instances in memory.","distractors":["Configure Spring Security to require authentication for /actuator/, expose only safe endpoints, and use a separate management port.","/actuator/info returns arbitrary application information as JSON.","GET /actuator/loggers returns all configured loggers and their current levels."],"distractorIds":["spring/spring_boot_actuator#5c7155e13951","spring/spring_boot_actuator#3a86762b2c5a","spring/spring_boot_actuator#8442308e22a1"],"concepts":["exposing","actuator","dump","risk","contains","snapshot"],"answerFullMd":"A heap dump contains a full snapshot of the JVM heap including all object instances in memory. Sensitive data stored as Java objects — database passwords from `DataSource` configuration, JWT tokens from `SecurityContext`, user PII from cached entities — is captured in the dump. Anyone with access to `/actuator/heapdump` can download a HPROF file and use tools like Eclipse Memory Analyzer (MAT) to extract plaintext credentials. Always restrict this endpoint to authenticated operators via a separate management network. In production, prefer using `jmap` or `jcmd` from an operator console rather than exposing it via HTTP.","distractorsMd":["Configure Spring Security to require authentication for `/actuator/**`, expose only safe endpoints, and use a separate management port.","`/actuator/info` returns arbitrary application information as JSON.","`GET /actuator/loggers` returns all configured loggers and their current levels."]},{"id":"spring/spring_boot_actuator#0b99fdd469ab","section":"spring","module":"spring/spring_boot_actuator","moduleName":"spring boot actuator","sourceFile":"README.md","difficulty":"intermediate","question":"How do you configure HikariCP pool metrics in Actuator?","answerFull":"HikariCP auto-registers metrics with Micrometer when micrometer-core is on the classpath and metricRegistry is configured. Spring Boot auto-configures this binding via HikariDataSourceMetricsAutoConfiguration. Metrics include: hikaricp.connections.active (connections in use), hikaricp.connections.idle (available connections), hikaricp.connections.pending (threads waiting for connection), hikaricp.connections.timeout.total (connection timeouts), and hikaricp.connections.usage (connection checkout duration timer). Pool saturation — active approaching maximum-pool-size — is the first signal of a database throughput problem.","correct":"HikariCP auto-registers metrics with Micrometer when micrometer-core is on the classpath and metricRegistry is configured.","distractors":["Liveness probe (/actuator/health/liveness) indicates whether the application is in a broken state that requires a restart.","GET /actuator/loggers returns all configured loggers and their current levels.","/actuator/info returns arbitrary application information as JSON."],"distractorIds":["spring/spring_boot_actuator#834c221a4610","spring/spring_boot_actuator#8442308e22a1","spring/spring_boot_actuator#3a86762b2c5a"],"concepts":["metricregistry","actuator","classpath","configure","hikaricp","micrometer"],"correctMd":"HikariCP auto-registers metrics with Micrometer when `micrometer-core` is on the classpath and `metricRegistry` is configured.","answerFullMd":"HikariCP auto-registers metrics with Micrometer when `micrometer-core` is on the classpath and `metricRegistry` is configured. Spring Boot auto-configures this binding via `HikariDataSourceMetricsAutoConfiguration`. Metrics include: `hikaricp.connections.active` (connections in use), `hikaricp.connections.idle` (available connections), `hikaricp.connections.pending` (threads waiting for connection), `hikaricp.connections.timeout.total` (connection timeouts), and `hikaricp.connections.usage` (connection checkout duration timer). Pool saturation — `active` approaching `maximum-pool-size` — is the first signal of a database throughput problem.","distractorsMd":["Liveness probe (`/actuator/health/liveness`) indicates whether the application is in a broken state that requires a restart.","`GET /actuator/loggers` returns all configured loggers and their current levels.","`/actuator/info` returns arbitrary application information as JSON."]},{"id":"spring/spring_boot_actuator#8442308e22a1","section":"spring","module":"spring/spring_boot_actuator","moduleName":"spring boot actuator","sourceFile":"README.md","difficulty":"intermediate","question":"How does /actuator/loggers work and why is it valuable in production?","answerFull":"GET /actuator/loggers returns all configured loggers and their current levels. GET /actuator/loggers/{name} shows the level for a specific logger. POST /actuator/loggers/{name} with body {\"configuredLevel\": \"DEBUG\"} changes the level at runtime without restart. This is invaluable during production incidents: switch a specific package to DEBUG to capture detailed trace without restarting (which would clear in-flight requests and change timing). Changes are in-memory only and reset on restart, so there is no permanent side effect. The endpoint should require authentication because excessive DEBUG logging can expose sensitive data.","correct":"GET /actuator/loggers returns all configured loggers and their current levels.","distractors":["Liveness probe (/actuator/health/liveness) indicates whether the application is in a broken state that requires a restart.","HikariCP auto-registers metrics with Micrometer when micrometer-core is on the classpath and metricRegistry is configured.","/actuator/info returns arbitrary application information as JSON."],"distractorIds":["spring/spring_boot_actuator#834c221a4610","spring/spring_boot_actuator#0b99fdd469ab","spring/spring_boot_actuator#3a86762b2c5a"],"concepts":["loggers","actuator","configured","levels","work","get"],"correctMd":"`GET /actuator/loggers` returns all configured loggers and their current levels.","answerFullMd":"`GET /actuator/loggers` returns all configured loggers and their current levels. `GET /actuator/loggers/{name}` shows the level for a specific logger. `POST /actuator/loggers/{name}` with body `{\"configuredLevel\": \"DEBUG\"}` changes the level at runtime without restart. This is invaluable during production incidents: switch a specific package to DEBUG to capture detailed trace without restarting (which would clear in-flight requests and change timing). Changes are in-memory only and reset on restart, so there is no permanent side effect. The endpoint should require authentication because excessive DEBUG logging can expose sensitive data.","distractorsMd":["Liveness probe (`/actuator/health/liveness`) indicates whether the application is in a broken state that requires a restart.","HikariCP auto-registers metrics with Micrometer when `micrometer-core` is on the classpath and `metricRegistry` is configured.","`/actuator/info` returns arbitrary application information as JSON."]},{"id":"spring/spring_boot_actuator#3a86762b2c5a","section":"spring","module":"spring/spring_boot_actuator","moduleName":"spring boot actuator","sourceFile":"README.md","difficulty":"advanced","question":"What is the Info endpoint, how do you populate it, and what value does it provide in an automated deploy pipeline?","answerFull":"/actuator/info returns arbitrary application information as JSON. Populate it via: (1) management.info.git.mode=full — injects Git commit hash, branch, commit time from git.properties (generated by git-commit-id-plugin). (2) management.info.build.enabled=true — injects build version, artifact ID from META-INF/build-info.properties (generated by Spring Boot Maven/Gradle plugins). (3) Custom InfoContributor beans. In an automated pipeline, /actuator/info lets monitoring systems, deployment dashboards, and support staff verify exactly which commit hash and build version is running — critical for correlating a production incident with the deployment that caused it.","correct":"/actuator/info returns arbitrary application information as JSON.","distractors":["Configure Spring Security to require authentication for /actuator/, expose only safe endpoints, and use a separate management port.","Liveness probe (/actuator/health/liveness) indicates whether the application is in a broken state that requires a restart.","Annotate a Spring bean with @Endpoint(id=\"my-endpoint\")."],"distractorIds":["spring/spring_boot_actuator#5c7155e13951","spring/spring_boot_actuator#834c221a4610","spring/spring_boot_actuator#71e2e4954ffb"],"concepts":["actuator","info","automated","provide","endpoint","deploy"],"questionMd":"What is the `Info` endpoint, how do you populate it, and what value does it provide in an automated deploy pipeline?","correctMd":"`/actuator/info` returns arbitrary application information as JSON.","answerFullMd":"`/actuator/info` returns arbitrary application information as JSON. Populate it via: (1) `management.info.git.mode=full` — injects Git commit hash, branch, commit time from `git.properties` (generated by `git-commit-id-plugin`). (2) `management.info.build.enabled=true` — injects build version, artifact ID from `META-INF/build-info.properties` (generated by Spring Boot Maven/Gradle plugins). (3) Custom `InfoContributor` beans. In an automated pipeline, `/actuator/info` lets monitoring systems, deployment dashboards, and support staff verify exactly which commit hash and build version is running — critical for correlating a production incident with the deployment that caused it.","distractorsMd":["Configure Spring Security to require authentication for `/actuator/**`, expose only safe endpoints, and use a separate management port.","Liveness probe (`/actuator/health/liveness`) indicates whether the application is in a broken state that requires a restart.","Annotate a Spring bean with `@Endpoint(id=\"my-endpoint\")`."]},{"id":"spring/spring_boot_actuator#2e25bb4cbcd3","section":"spring","module":"spring/spring_boot_actuator","moduleName":"spring boot actuator","sourceFile":"README.md","difficulty":"advanced","question":"How does Micrometer's @Timed annotation work and when should you use programmatic recording instead?","answerFull":"@Timed(\"my.operation\") on a Spring bean method (or class, to instrument all methods) instruments it via AOP — a Timer is automatically started/stopped around the method invocation. Tags can be added via extraTags or a TimedAspect bean with custom TagsProvider. Use programmatic recording (registry.timer(\"my.op\", \"tag\", value).record(() -> doWork())) when: you need dynamic tags derived from the method's arguments or return value (impossible with @Timed), you need to record partial durations inside a method, or you want to record custom outcomes (success vs failure as separate tag values). @Timed is convenient for coarse-grained external API latency; programmatic timers are necessary for internal business logic with rich context.","correct":"@Timed(\"my.operation\") on a Spring bean method (or class, to instrument all methods) instruments it via AOP — a Timer is automatically started/stopped around the method invocation.","distractors":["Micrometer is a metrics instrumentation library that provides a vendor-neutral API for recording application metrics.","Configure Spring Security to require authentication for /actuator/, expose only safe endpoints, and use a separate management port.","Micrometer Tracing (formerly Spring Cloud Sleuth) is a tracing facade over concrete implementations (Brave/Zipkin or OpenTelemetry)."],"distractorIds":["spring/spring_boot_actuator#17c4896daf57","spring/spring_boot_actuator#5c7155e13951","spring/spring_boot_actuator#491e6c4a2a7e"],"concepts":["instrument","instruments","timed","programmatic","stopped","timer"],"questionMd":"How does Micrometer's `@Timed` annotation work and when should you use programmatic recording instead?","correctMd":"`@Timed(\"my.operation\")` on a Spring bean method (or class, to instrument all methods) instruments it via AOP — a `Timer` is automatically started/stopped around the method invocation.","answerFullMd":"`@Timed(\"my.operation\")` on a Spring bean method (or class, to instrument all methods) instruments it via AOP — a `Timer` is automatically started/stopped around the method invocation. Tags can be added via `extraTags` or a `TimedAspect` bean with custom `TagsProvider`. Use programmatic recording (`registry.timer(\"my.op\", \"tag\", value).record(() -> doWork())`) when: you need dynamic tags derived from the method's arguments or return value (impossible with `@Timed`), you need to record partial durations inside a method, or you want to record custom outcomes (success vs failure as separate tag values). `@Timed` is convenient for coarse-grained external API latency; programmatic timers are necessary for internal business logic with rich context.","distractorsMd":[null,"Configure Spring Security to require authentication for `/actuator/**`, expose only safe endpoints, and use a separate management port.",null]},{"id":"spring/spring_boot_actuator#491e6c4a2a7e","section":"spring","module":"spring/spring_boot_actuator","moduleName":"spring boot actuator","sourceFile":"README.md","difficulty":"advanced","question":"What is Micrometer Tracing and how does it integrate with Actuator in Spring Boot 3.x?","answerFull":"Micrometer Tracing (formerly Spring Cloud Sleuth) is a tracing facade over concrete implementations (Brave/Zipkin or OpenTelemetry). Spring Boot 3.x auto-configures tracing when micrometer-tracing-bridge-otel or micrometer-tracing-bridge-brave is on the classpath. It provides: (1) Observation API — a unified abstraction over metrics + tracing that records both a Timer and a distributed trace span in one call. (2) Auto-instrumentation — @Observed on beans, RestClient/WebClient/RestTemplate interceptors, Spring MVC/WebFlux server filters, Kafka listeners. (3) TraceId/SpanId injection into MDC for structured logging correlation. The /actuator/health endpoint propagates trace context when management.tracing.sampling.probability=1.0.","correct":"Micrometer Tracing (formerly Spring Cloud Sleuth) is a tracing facade over concrete implementations (Brave/Zipkin or OpenTelemetry).","distractors":["Configure Spring Security to require authentication for /actuator/, expose only safe endpoints, and use a separate management port.","Liveness probe (/actuator/health/liveness) indicates whether the application is in a broken state that requires a restart.","HikariCP auto-registers metrics with Micrometer when micrometer-core is on the classpath and metricRegistry is configured."],"distractorIds":["spring/spring_boot_actuator#5c7155e13951","spring/spring_boot_actuator#834c221a4610","spring/spring_boot_actuator#0b99fdd469ab"],"concepts":["formerly","integrate","brave","facade","sleuth","zipkin"],"answerFullMd":"Micrometer Tracing (formerly Spring Cloud Sleuth) is a tracing facade over concrete implementations (Brave/Zipkin or OpenTelemetry). Spring Boot 3.x auto-configures tracing when `micrometer-tracing-bridge-otel` or `micrometer-tracing-bridge-brave` is on the classpath. It provides: (1) `Observation` API — a unified abstraction over metrics + tracing that records both a `Timer` and a distributed trace span in one call. (2) Auto-instrumentation — `@Observed` on beans, `RestClient`/`WebClient`/`RestTemplate` interceptors, Spring MVC/WebFlux server filters, Kafka listeners. (3) `TraceId`/`SpanId` injection into MDC for structured logging correlation. The `/actuator/health` endpoint propagates trace context when `management.tracing.sampling.probability=1.0`.","distractorsMd":["Configure Spring Security to require authentication for `/actuator/**`, expose only safe endpoints, and use a separate management port.","Liveness probe (`/actuator/health/liveness`) indicates whether the application is in a broken state that requires a restart.","HikariCP auto-registers metrics with Micrometer when `micrometer-core` is on the classpath and `metricRegistry` is configured."]},{"id":"spring/spring_boot_actuator#26653151c513","section":"spring","module":"spring/spring_boot_actuator","moduleName":"spring boot actuator","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between HealthIndicator and HealthContributor, and when do you use CompositeHealthContributor?","answerFull":"HealthIndicator is the simple interface: implement health() returning a Health object with status and optional details. Spring auto-discovers all HealthIndicator beans and aggregates them. HealthContributor is a marker interface for both HealthIndicator (leaf contributor, returns a health result directly) and CompositeHealthContributor (named group of sub-contributors). Use CompositeHealthContributor when you want to group multiple related checks under a named hierarchy: e.g., a DatabaseHealthContributor composed of separate ReadReplicaHealthIndicator and PrimaryHealthIndicator. Each sub-contributor gets its own named entry under components in the health response, giving fine-grained visibility into which specific component is unhealthy.","correct":"HealthIndicator is the simple interface: implement health() returning a Health object with status and optional details.","distractors":["CompositeHealth aggregates the results of multiple HealthIndicator or HealthContributor beans.","Liveness probe (/actuator/health/liveness) indicates whether the application is in a broken state that requires a restart.","A heap dump contains a full snapshot of the JVM heap including all object instances in memory."],"distractorIds":["spring/spring_boot_actuator#67adf3326143","spring/spring_boot_actuator#834c221a4610","spring/spring_boot_actuator#af4d20eb6548"],"concepts":["healthcontributor","healthindicator","details","difference","health","returning"],"questionMd":"What is the difference between `HealthIndicator` and `HealthContributor`, and when do you use `CompositeHealthContributor`?","correctMd":"`HealthIndicator` is the simple interface: implement `health()` returning a `Health` object with status and optional details.","answerFullMd":"`HealthIndicator` is the simple interface: implement `health()` returning a `Health` object with status and optional details. Spring auto-discovers all `HealthIndicator` beans and aggregates them. `HealthContributor` is a marker interface for both `HealthIndicator` (leaf contributor, returns a health result directly) and `CompositeHealthContributor` (named group of sub-contributors). Use `CompositeHealthContributor` when you want to group multiple related checks under a named hierarchy: e.g., a `DatabaseHealthContributor` composed of separate `ReadReplicaHealthIndicator` and `PrimaryHealthIndicator`. Each sub-contributor gets its own named entry under `components` in the health response, giving fine-grained visibility into which specific component is unhealthy.","distractorsMd":["`CompositeHealth` aggregates the results of multiple `HealthIndicator` or `HealthContributor` beans.","Liveness probe (`/actuator/health/liveness`) indicates whether the application is in a broken state that requires a restart.",null]},{"id":"spring/spring_boot_actuator#28d9a580b41e","section":"spring","module":"spring/spring_boot_actuator","moduleName":"spring boot actuator","sourceFile":"README.md","difficulty":"advanced","question":"How do you configure a management server on a different port and why is this the recommended production pattern?","answerFull":"Set management.server.port=8081 (and optionally management.server.address=127.0.0.1 to bind to localhost only). With a separate port: the load balancer / API gateway exposes only port 8080 (business traffic) to the internet; port 8081 is only reachable from within the cluster or through an internal VPN. This means /actuator/heapdump, /actuator/env, and /actuator/beans (which expose configuration, credentials, and class structure) are never reachable from outside the trust boundary. In Kubernetes, the liveness/readiness probes are configured to hit port 8081 directly on the pod IP — the probes bypass the service load balancer and check each pod individually.","correct":"Set management.server.port=8081 (and optionally management.server.address=127.0.0.1 to bind to localhost only).","distractors":["Configure Spring Security to require authentication for /actuator/, expose only safe endpoints, and use a separate management port.","/actuator/info returns arbitrary application information as JSON.","/actuator/startup shows a timeline of ApplicationContext startup steps with timing for each bean initialization."],"distractorIds":["spring/spring_boot_actuator#5c7155e13951","spring/spring_boot_actuator#3a86762b2c5a","spring/spring_boot_actuator#ce2df6045558"],"concepts":["8081","recommended","127","localhost","optionally","configure"],"correctMd":"Set `management.server.port=8081` (and optionally `management.server.address=127.0.0.1` to bind to localhost only).","answerFullMd":"Set `management.server.port=8081` (and optionally `management.server.address=127.0.0.1` to bind to localhost only). With a separate port: the load balancer / API gateway exposes only port 8080 (business traffic) to the internet; port 8081 is only reachable from within the cluster or through an internal VPN. This means `/actuator/heapdump`, `/actuator/env`, and `/actuator/beans` (which expose configuration, credentials, and class structure) are never reachable from outside the trust boundary. In Kubernetes, the liveness/readiness probes are configured to hit port 8081 directly on the pod IP — the probes bypass the service load balancer and check each pod individually.","distractorsMd":["Configure Spring Security to require authentication for `/actuator/**`, expose only safe endpoints, and use a separate management port.","`/actuator/info` returns arbitrary application information as JSON.","`/actuator/startup` shows a timeline of ApplicationContext startup steps with timing for each bean initialization."]},{"id":"spring/spring_boot_autoconfiguration#9261d8d4d4c5","section":"spring","module":"spring/spring_boot_autoconfiguration","moduleName":"spring boot autoconfiguration","sourceFile":"README.md","difficulty":"core","question":"What is the difference between spring.factories and AutoConfiguration.imports?","answerFull":"Both register auto-configuration classes for discovery. spring.factories (Boot 2.x) is a generic key-value properties file supporting multiple Spring extension points. AutoConfiguration.imports (Boot 2.7+, required in Boot 3.x) is a dedicated file for auto-configuration class names only. Boot 3.x removed support for auto-configuration registration via spring.factories to improve startup performance and clarity. The spring.factories file still works for other extension points (failure analyzers, environment post-processors).","correct":"Both register auto-configuration classes for discovery.","distractors":["Spring Boot 3.x introduced an AOT (Ahead-of-Time) processing phase that runs at build time.","Four steps: (1) Create an autoconfigure module with @AutoConfiguration class containing @Conditional-guarded @Bean methods and @ConfigurationProperties for user-overridable settings.","@ImportAutoConfiguration imports specific auto-configuration classes explicitly, bypassing the full autoconfiguration discovery mechanism."],"distractorIds":["spring/spring_boot_autoconfiguration#ad1be5092f13","spring/spring_boot_autoconfiguration#481dad97bfa8","spring/spring_boot_autoconfiguration#597092c46a72"],"concepts":["autoconfiguration","imports","discovery","difference","register","classes"],"answerFullMd":"Both register auto-configuration classes for discovery. `spring.factories` (Boot 2.x) is a generic key-value properties file supporting multiple Spring extension points. `AutoConfiguration.imports` (Boot 2.7+, required in Boot 3.x) is a dedicated file for auto-configuration class names only. Boot 3.x removed support for auto-configuration registration via `spring.factories` to improve startup performance and clarity. The `spring.factories` file still works for other extension points (failure analyzers, environment post-processors).","distractorsMd":[null,"Four steps: (1) Create an autoconfigure module with `@AutoConfiguration` class containing `@Conditional`-guarded `@Bean` methods and `@ConfigurationProperties` for user-overridable settings.","`@ImportAutoConfiguration` imports specific auto-configuration classes explicitly, bypassing the full autoconfiguration discovery mechanism."]},{"id":"spring/spring_boot_autoconfiguration#d30b5be09444","section":"spring","module":"spring/spring_boot_autoconfiguration","moduleName":"spring boot autoconfiguration","sourceFile":"README.md","difficulty":"core","question":"Why does @ConditionalOnMissingBean work correctly even though Spring beans are initialized in dependency order?","answerFull":"@ConditionalOnMissingBean is evaluated during the BeanFactoryPostProcessor phase, after all BeanDefinitions are loaded but before any beans are instantiated. It checks the BeanDefinitionRegistry for existing definitions (not instances). This means: if you define a @Bean DataSource in your @Configuration class, the BeanDefinition for it exists in the registry when DataSourceAutoConfiguration evaluates @ConditionalOnMissingBean(DataSource.class) — the condition fails and auto-configuration backs off correctly.","correct":"@ConditionalOnMissingBean is evaluated during the BeanFactoryPostProcessor phase, after all BeanDefinitions are loaded but before any beans are instantiated.","distractors":["The processor runs at compile time and generates META-INF/spring-autoconfigure-metadata.properties.","ApplicationContextRunner is a test utility from spring-boot-test that creates a minimal ApplicationContext for testing auto-configuration.","Spring Boot 3.x introduced an AOT (Ahead-of-Time) processing phase that runs at build time."],"distractorIds":["spring/spring_boot_autoconfiguration#b60425d541a2","spring/spring_boot_autoconfiguration#f4a292cc4963","spring/spring_boot_autoconfiguration#ad1be5092f13"],"concepts":["conditionalonmissingbean","beanfactorypostprocessor","beandefinitions","instantiated","evaluated","though"],"correctMd":"`@ConditionalOnMissingBean` is evaluated during the `BeanFactoryPostProcessor` phase, after all `BeanDefinition`s are loaded but before any beans are instantiated.","answerFullMd":"`@ConditionalOnMissingBean` is evaluated during the `BeanFactoryPostProcessor` phase, after all `BeanDefinition`s are loaded but before any beans are instantiated. It checks the `BeanDefinitionRegistry` for existing definitions (not instances). This means: if you define a `@Bean DataSource` in your `@Configuration` class, the `BeanDefinition` for it exists in the registry when `DataSourceAutoConfiguration` evaluates `@ConditionalOnMissingBean(DataSource.class)` — the condition fails and auto-configuration backs off correctly.","distractorsMd":["The processor runs at compile time and generates `META-INF/spring-autoconfigure-metadata.properties`.","`ApplicationContextRunner` is a test utility from `spring-boot-test` that creates a minimal `ApplicationContext` for testing auto-configuration.",null]},{"id":"spring/spring_boot_autoconfiguration#481dad97bfa8","section":"spring","module":"spring/spring_boot_autoconfiguration","moduleName":"spring boot autoconfiguration","sourceFile":"README.md","difficulty":"core","question":"How would you write a custom Spring Boot starter?","answerFull":"Four steps: (1) Create an autoconfigure module with @AutoConfiguration class containing @Conditional-guarded @Bean methods and @ConfigurationProperties for user-overridable settings. (2) Register the class in META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports. (3) Do NOT use @ComponentScan — register all beans explicitly. (4) Create a starter POM that depends on your autoconfigure module and any required third-party JARs. Users add the starter dependency and get auto-configured behavior immediately.","correct":"Four steps: (1) Create an autoconfigure module with @AutoConfiguration class containing @Conditional-guarded @Bean methods and @ConfigurationProperties for user-overridable settings.","distractors":["The processor runs at compile time and generates META-INF/spring-autoconfigure-metadata.properties.","The class name is added to an exclusion set checked by AutoConfigurationImportSelector.","@ImportAutoConfiguration imports specific auto-configuration classes explicitly, bypassing the full autoconfiguration discovery mechanism."],"distractorIds":["spring/spring_boot_autoconfiguration#b60425d541a2","spring/spring_boot_autoconfiguration#f72a885c6463","spring/spring_boot_autoconfiguration#597092c46a72"],"concepts":["guarded","autoconfigure","overridable","autoconfiguration","configurationproperties","starter"],"correctMd":"Four steps: (1) Create an autoconfigure module with `@AutoConfiguration` class containing `@Conditional`-guarded `@Bean` methods and `@ConfigurationProperties` for user-overridable settings.","answerFullMd":"Four steps: (1) Create an autoconfigure module with `@AutoConfiguration` class containing `@Conditional`-guarded `@Bean` methods and `@ConfigurationProperties` for user-overridable settings. (2) Register the class in `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`. (3) Do NOT use `@ComponentScan` — register all beans explicitly. (4) Create a starter POM that depends on your autoconfigure module and any required third-party JARs. Users add the starter dependency and get auto-configured behavior immediately.","distractorsMd":["The processor runs at compile time and generates `META-INF/spring-autoconfigure-metadata.properties`.","The class name is added to an exclusion set checked by `AutoConfigurationImportSelector`.","`@ImportAutoConfiguration` imports specific auto-configuration classes explicitly, bypassing the full autoconfiguration discovery mechanism."]},{"id":"spring/spring_boot_autoconfiguration#9a4a4b513fdd","section":"spring","module":"spring/spring_boot_autoconfiguration","moduleName":"spring boot autoconfiguration","sourceFile":"README.md","difficulty":"core","question":"How do you debug which auto-configurations are applied and which are skipped?","answerFull":"Start the application with --debug flag: java -jar app.jar --debug. This prints the ConditionEvaluationReport to the console showing positive matches (applied), negative matches (skipped with reasons), and exclusions. Alternatively, expose the /actuator/conditions endpoint (require spring-boot-actuator on classpath and management.endpoints.web.exposure.include=conditions). For unit testing auto-configuration, use ApplicationContextRunner from spring-boot-test to test specific configurations in isolation.","correct":"Start the application with --debug flag: java -jar app.jar --debug.","distractors":["@ImportAutoConfiguration imports specific auto-configuration classes explicitly, bypassing the full autoconfiguration discovery mechanism.","ApplicationContextRunner is a test utility from spring-boot-test that creates a minimal ApplicationContext for testing auto-configuration.","@AutoConfigureAfter(OtherAutoConfig.class) ensures OtherAutoConfig is processed before the current class."],"distractorIds":["spring/spring_boot_autoconfiguration#597092c46a72","spring/spring_boot_autoconfiguration#f4a292cc4963","spring/spring_boot_autoconfiguration#7f1c7f2ceee5"],"concepts":["jar","configurations","debug","flag","applied","auto"],"correctMd":"Start the application with `--debug` flag: `java -jar app.jar --debug`.","answerFullMd":"Start the application with `--debug` flag: `java -jar app.jar --debug`. This prints the `ConditionEvaluationReport` to the console showing positive matches (applied), negative matches (skipped with reasons), and exclusions. Alternatively, expose the `/actuator/conditions` endpoint (require `spring-boot-actuator` on classpath and `management.endpoints.web.exposure.include=conditions`). For unit testing auto-configuration, use `ApplicationContextRunner` from `spring-boot-test` to test specific configurations in isolation.","distractorsMd":["`@ImportAutoConfiguration` imports specific auto-configuration classes explicitly, bypassing the full autoconfiguration discovery mechanism.","`ApplicationContextRunner` is a test utility from `spring-boot-test` that creates a minimal `ApplicationContext` for testing auto-configuration.","`@AutoConfigureAfter(OtherAutoConfig.class)` ensures `OtherAutoConfig` is processed before the current class."]},{"id":"spring/spring_boot_autoconfiguration#7f1c7f2ceee5","section":"spring","module":"spring/spring_boot_autoconfiguration","moduleName":"spring boot autoconfiguration","sourceFile":"README.md","difficulty":"intermediate","question":"What is @AutoConfigureAfter and why does order matter?","answerFull":"@AutoConfigureAfter(OtherAutoConfig.class) ensures OtherAutoConfig is processed before the current class. This matters when your auto-configuration needs beans from another auto-configuration to be available. For example, JdbcTemplateAutoConfiguration is @AutoConfigureAfter(DataSourceAutoConfiguration.class) because it needs the DataSource bean. Without ordering, @ConditionalOnBean(DataSource.class) in JdbcTemplateAutoConfiguration might evaluate before DataSourceAutoConfiguration runs, failing the condition incorrectly.","correct":"@AutoConfigureAfter(OtherAutoConfig.class) ensures OtherAutoConfig is processed before the current class.","distractors":["Four steps: (1) Create an autoconfigure module with @AutoConfiguration class containing @Conditional-guarded @Bean methods and @ConfigurationProperties for user-overridable settings.","The class name is added to an exclusion set checked by AutoConfigurationImportSelector.","@ConditionalOnBean(DataSource.class) registers the bean only if a DataSource bean already exists."],"distractorIds":["spring/spring_boot_autoconfiguration#481dad97bfa8","spring/spring_boot_autoconfiguration#f72a885c6463","spring/spring_boot_autoconfiguration#523c33abda8c"],"concepts":["autoconfigureafter","otherautoconfig","matter","ensures","processed","current"],"correctMd":"`@AutoConfigureAfter(OtherAutoConfig.class)` ensures `OtherAutoConfig` is processed before the current class.","answerFullMd":"`@AutoConfigureAfter(OtherAutoConfig.class)` ensures `OtherAutoConfig` is processed before the current class. This matters when your auto-configuration needs beans from another auto-configuration to be available. For example, `JdbcTemplateAutoConfiguration` is `@AutoConfigureAfter(DataSourceAutoConfiguration.class)` because it needs the `DataSource` bean. Without ordering, `@ConditionalOnBean(DataSource.class)` in `JdbcTemplateAutoConfiguration` might evaluate before `DataSourceAutoConfiguration` runs, failing the condition incorrectly.","distractorsMd":["Four steps: (1) Create an autoconfigure module with `@AutoConfiguration` class containing `@Conditional`-guarded `@Bean` methods and `@ConfigurationProperties` for user-overridable settings.","The class name is added to an exclusion set checked by `AutoConfigurationImportSelector`.","`@ConditionalOnBean(DataSource.class)` registers the bean only if a `DataSource` bean already exists."]},{"id":"spring/spring_boot_autoconfiguration#f72a885c6463","section":"spring","module":"spring/spring_boot_autoconfiguration","moduleName":"spring boot autoconfiguration","sourceFile":"README.md","difficulty":"intermediate","question":"What happens when you exclude an auto-configuration class?","answerFull":"The class name is added to an exclusion set checked by AutoConfigurationImportSelector. The class is removed from the import list before any condition evaluation. This is a hard exclusion — even if all conditions would have passed, the class is never imported and its beans are never registered. This is useful when: you want to completely replace auto-configuration with your own, the auto-configuration conflicts with another library, or you want to disable a feature entirely (like Spring Security auto-config in tests).","correct":"The class name is added to an exclusion set checked by AutoConfigurationImportSelector.","distractors":["@AutoConfigureAfter(OtherAutoConfig.class) ensures OtherAutoConfig is processed before the current class.","Four steps: (1) Create an autoconfigure module with @AutoConfiguration class containing @Conditional-guarded @Bean methods and @ConfigurationProperties for user-overridable settings.","@ImportAutoConfiguration imports specific auto-configuration classes explicitly, bypassing the full autoconfiguration discovery mechanism."],"distractorIds":["spring/spring_boot_autoconfiguration#7f1c7f2ceee5","spring/spring_boot_autoconfiguration#481dad97bfa8","spring/spring_boot_autoconfiguration#597092c46a72"],"concepts":["autoconfigurationimportselector","exclude","exclusion","checked","added","happens"],"correctMd":"The class name is added to an exclusion set checked by `AutoConfigurationImportSelector`.","answerFullMd":"The class name is added to an exclusion set checked by `AutoConfigurationImportSelector`. The class is removed from the import list before any condition evaluation. This is a hard exclusion — even if all conditions would have passed, the class is never imported and its beans are never registered. This is useful when: you want to completely replace auto-configuration with your own, the auto-configuration conflicts with another library, or you want to disable a feature entirely (like Spring Security auto-config in tests).","distractorsMd":["`@AutoConfigureAfter(OtherAutoConfig.class)` ensures `OtherAutoConfig` is processed before the current class.","Four steps: (1) Create an autoconfigure module with `@AutoConfiguration` class containing `@Conditional`-guarded `@Bean` methods and `@ConfigurationProperties` for user-overridable settings.","`@ImportAutoConfiguration` imports specific auto-configuration classes explicitly, bypassing the full autoconfiguration discovery mechanism."]},{"id":"spring/spring_boot_autoconfiguration#b60425d541a2","section":"spring","module":"spring/spring_boot_autoconfiguration","moduleName":"spring boot autoconfiguration","sourceFile":"README.md","difficulty":"intermediate","question":"How does spring-boot-autoconfigure-processor improve performance?","answerFull":"The processor runs at compile time and generates META-INF/spring-autoconfigure-metadata.properties. This file lists the conditions for each auto-configuration class (class names for @ConditionalOnClass, property names for @ConditionalOnProperty). Spring Boot reads this metadata at startup and filters out auto-configuration classes whose conditions obviously fail (e.g., required class not on classpath) WITHOUT loading and processing the configuration class. This makes startup faster because fewer classes are loaded and parsed.","correct":"The processor runs at compile time and generates META-INF/spring-autoconfigure-metadata.properties.","distractors":["Spring Boot 3.x introduced an AOT (Ahead-of-Time) processing phase that runs at build time.","Four steps: (1) Create an autoconfigure module with @AutoConfiguration class containing @Conditional-guarded @Bean methods and @ConfigurationProperties for user-overridable settings.","ApplicationContextRunner is a test utility from spring-boot-test that creates a minimal ApplicationContext for testing auto-configuration."],"distractorIds":["spring/spring_boot_autoconfiguration#ad1be5092f13","spring/spring_boot_autoconfiguration#481dad97bfa8","spring/spring_boot_autoconfiguration#f4a292cc4963"],"concepts":["autoconfigure","inf","processor","improve","boot","meta"],"correctMd":"The processor runs at compile time and generates `META-INF/spring-autoconfigure-metadata.properties`.","answerFullMd":"The processor runs at compile time and generates `META-INF/spring-autoconfigure-metadata.properties`. This file lists the conditions for each auto-configuration class (class names for `@ConditionalOnClass`, property names for `@ConditionalOnProperty`). Spring Boot reads this metadata at startup and filters out auto-configuration classes whose conditions obviously fail (e.g., required class not on classpath) WITHOUT loading and processing the configuration class. This makes startup faster because fewer classes are loaded and parsed.","distractorsMd":[null,"Four steps: (1) Create an autoconfigure module with `@AutoConfiguration` class containing `@Conditional`-guarded `@Bean` methods and `@ConfigurationProperties` for user-overridable settings.","`ApplicationContextRunner` is a test utility from `spring-boot-test` that creates a minimal `ApplicationContext` for testing auto-configuration."]},{"id":"spring/spring_boot_autoconfiguration#f4a292cc4963","section":"spring","module":"spring/spring_boot_autoconfiguration","moduleName":"spring boot autoconfiguration","sourceFile":"README.md","difficulty":"intermediate","question":"What is the ApplicationContextRunner and how is it used for testing?","answerFull":"ApplicationContextRunner is a test utility from spring-boot-test that creates a minimal ApplicationContext for testing auto-configuration. It does not start a full Spring Boot application — just evaluates specific configurations in isolation. Example: new ApplicationContextRunner().withConfiguration(AutoConfigurations.of(MyAutoConfig.class)).withPropertyValues(\"my.property=value\").run(context -> { assertThat(context).hasSingleBean(MyBean.class); }). This is the recommended way to test auto-configuration without requiring @SpringBootTest.","correct":"ApplicationContextRunner is a test utility from spring-boot-test that creates a minimal ApplicationContext for testing auto-configuration.","distractors":["Spring Boot 3.x introduced an AOT (Ahead-of-Time) processing phase that runs at build time.","@ImportAutoConfiguration imports specific auto-configuration classes explicitly, bypassing the full autoconfiguration discovery mechanism.","The processor runs at compile time and generates META-INF/spring-autoconfigure-metadata.properties."],"distractorIds":["spring/spring_boot_autoconfiguration#ad1be5092f13","spring/spring_boot_autoconfiguration#597092c46a72","spring/spring_boot_autoconfiguration#b60425d541a2"],"concepts":["applicationcontextrunner","utility","minimal","applicationcontext","boot","testing"],"correctMd":"`ApplicationContextRunner` is a test utility from `spring-boot-test` that creates a minimal `ApplicationContext` for testing auto-configuration.","answerFullMd":"`ApplicationContextRunner` is a test utility from `spring-boot-test` that creates a minimal `ApplicationContext` for testing auto-configuration. It does not start a full Spring Boot application — just evaluates specific configurations in isolation. Example: `new ApplicationContextRunner().withConfiguration(AutoConfigurations.of(MyAutoConfig.class)).withPropertyValues(\"my.property=value\").run(context -> { assertThat(context).hasSingleBean(MyBean.class); })`. This is the recommended way to test auto-configuration without requiring `@SpringBootTest`.","distractorsMd":[null,"`@ImportAutoConfiguration` imports specific auto-configuration classes explicitly, bypassing the full autoconfiguration discovery mechanism.","The processor runs at compile time and generates `META-INF/spring-autoconfigure-metadata.properties`."]},{"id":"spring/spring_boot_autoconfiguration#523c33abda8c","section":"spring","module":"spring/spring_boot_autoconfiguration","moduleName":"spring boot autoconfiguration","sourceFile":"README.md","difficulty":"advanced","question":"What is @ConditionalOnBean and how does it differ from @ConditionalOnMissingBean, and which ordering trap can affect both?","answerFull":"@ConditionalOnBean(DataSource.class) registers the bean only if a DataSource bean already exists. @ConditionalOnMissingBean(DataSource.class) registers the bean only if a DataSource does NOT exist — this is the \"sensible default / override\" pattern. The ordering trap: conditions are evaluated in the order auto-configuration classes are processed. If JdbcTemplateAutoConfiguration evaluates @ConditionalOnBean(DataSource.class) before DataSourceAutoConfiguration registers the DataSource, the condition incorrectly fails. Fix: declare @AutoConfigureAfter(DataSourceAutoConfiguration.class) to guarantee DataSource is registered first. Key principle: @ConditionalOnBean is inherently ordering-sensitive; @ConditionalOnMissingBean is less so (it only needs the bean definition to be absent at evaluation time).","correct":"@ConditionalOnBean(DataSource.class) registers the bean only if a DataSource bean already exists.","distractors":["@ConditionalOnMissingBean is evaluated during the BeanFactoryPostProcessor phase, after all BeanDefinitions are loaded but before any beans are instantiated.","The class name is added to an exclusion set checked by AutoConfigurationImportSelector.","@AutoConfigureAfter(OtherAutoConfig.class) ensures OtherAutoConfig is processed before the current class."],"distractorIds":["spring/spring_boot_autoconfiguration#d30b5be09444","spring/spring_boot_autoconfiguration#f72a885c6463","spring/spring_boot_autoconfiguration#7f1c7f2ceee5"],"concepts":["conditionalonbean","conditionalonmissingbean","trap","datasource","affect","registers"],"questionMd":"What is `@ConditionalOnBean` and how does it differ from `@ConditionalOnMissingBean`, and which ordering trap can affect both?","correctMd":"`@ConditionalOnBean(DataSource.class)` registers the bean only if a `DataSource` bean already exists.","answerFullMd":"`@ConditionalOnBean(DataSource.class)` registers the bean only if a `DataSource` bean already exists. `@ConditionalOnMissingBean(DataSource.class)` registers the bean only if a `DataSource` does NOT exist — this is the \"sensible default / override\" pattern. The ordering trap: conditions are evaluated in the order auto-configuration classes are processed. If `JdbcTemplateAutoConfiguration` evaluates `@ConditionalOnBean(DataSource.class)` before `DataSourceAutoConfiguration` registers the `DataSource`, the condition incorrectly fails. Fix: declare `@AutoConfigureAfter(DataSourceAutoConfiguration.class)` to guarantee `DataSource` is registered first. Key principle: `@ConditionalOnBean` is inherently ordering-sensitive; `@ConditionalOnMissingBean` is less so (it only needs the bean definition to be absent at evaluation time).","distractorsMd":["`@ConditionalOnMissingBean` is evaluated during the `BeanFactoryPostProcessor` phase, after all `BeanDefinition`s are loaded but before any beans are instantiated.","The class name is added to an exclusion set checked by `AutoConfigurationImportSelector`.","`@AutoConfigureAfter(OtherAutoConfig.class)` ensures `OtherAutoConfig` is processed before the current class."]},{"id":"spring/spring_boot_autoconfiguration#880fcc8b33b2","section":"spring","module":"spring/spring_boot_autoconfiguration","moduleName":"spring boot autoconfiguration","sourceFile":"README.md","difficulty":"advanced","question":"What is a Spring Boot \"failure analyser\" and how do you write a custom one?","answerFull":"A failure analyser is a component that intercepts a startup failure, diagnoses the root cause, and displays a human-readable message with a suggested action — replacing a raw stack trace with a concise explanation. Examples: PortInUseFailureAnalyzer prints \"Web server failed to start. Port 8080 was already in use\" when BindException is thrown. To write one: implement AbstractFailureAnalyzer<T extends Throwable>, override analyze(Throwable rootFailure, T cause), return a FailureAnalysis with a description and action, and register it in META-INF/spring.factories under org.springframework.boot.diagnostics.FailureAnalyzer. @FailureAnalyzer beans in the application context are also auto-discovered.","correct":"A failure analyser is a component that intercepts a startup failure, diagnoses the root cause, and displays a human-readable message with a suggested action — replacing a raw stack trace with a concise explanation.","distractors":["ApplicationContextRunner is a test utility from spring-boot-test that creates a minimal ApplicationContext for testing auto-configuration.","The processor runs at compile time and generates META-INF/spring-autoconfigure-metadata.properties.","Spring Boot 3.x introduced an AOT (Ahead-of-Time) processing phase that runs at build time."],"distractorIds":["spring/spring_boot_autoconfiguration#f4a292cc4963","spring/spring_boot_autoconfiguration#b60425d541a2","spring/spring_boot_autoconfiguration#ad1be5092f13"],"concepts":["analyser","concise","diagnoses","displays","suggested","replacing"],"answerFullMd":"A failure analyser is a component that intercepts a startup failure, diagnoses the root cause, and displays a human-readable message with a suggested action — replacing a raw stack trace with a concise explanation. Examples: `PortInUseFailureAnalyzer` prints \"Web server failed to start. Port 8080 was already in use\" when `BindException` is thrown. To write one: implement `AbstractFailureAnalyzer<T extends Throwable>`, override `analyze(Throwable rootFailure, T cause)`, return a `FailureAnalysis` with a description and action, and register it in `META-INF/spring.factories` under `org.springframework.boot.diagnostics.FailureAnalyzer`. `@FailureAnalyzer` beans in the application context are also auto-discovered.","distractorsMd":["`ApplicationContextRunner` is a test utility from `spring-boot-test` that creates a minimal `ApplicationContext` for testing auto-configuration.","The processor runs at compile time and generates `META-INF/spring-autoconfigure-metadata.properties`.",null]},{"id":"spring/spring_boot_autoconfiguration#597092c46a72","section":"spring","module":"spring/spring_boot_autoconfiguration","moduleName":"spring boot autoconfiguration","sourceFile":"README.md","difficulty":"advanced","question":"What does @ImportAutoConfiguration do and when is it used instead of @EnableAutoConfiguration?","answerFull":"@ImportAutoConfiguration imports specific auto-configuration classes explicitly, bypassing the full autoconfiguration discovery mechanism. It is used in test slices (@WebMvcTest, @DataJpaTest) to import only the subset of auto-configurations relevant to the tested layer — avoiding loading the entire application context for a narrow test. For example, @DataJpaTest applies @ImportAutoConfiguration({JpaRepositoriesAutoConfiguration.class, DataSourceAutoConfiguration.class, ...}) — it only loads JPA-related auto-configurations, not security, web, or messaging. Use @ImportAutoConfiguration in your own test slices or when you need to import a specific subset of auto-configurations for integration tests.","correct":"@ImportAutoConfiguration imports specific auto-configuration classes explicitly, bypassing the full autoconfiguration discovery mechanism.","distractors":["ApplicationContextRunner is a test utility from spring-boot-test that creates a minimal ApplicationContext for testing auto-configuration.","Four steps: (1) Create an autoconfigure module with @AutoConfiguration class containing @Conditional-guarded @Bean methods and @ConfigurationProperties for user-overridable settings.","@AutoConfigureAfter(OtherAutoConfig.class) ensures OtherAutoConfig is processed before the current class."],"distractorIds":["spring/spring_boot_autoconfiguration#f4a292cc4963","spring/spring_boot_autoconfiguration#481dad97bfa8","spring/spring_boot_autoconfiguration#7f1c7f2ceee5"],"concepts":["importautoconfiguration","autoconfiguration","bypassing","imports","discovery","explicitly"],"questionMd":"What does `@ImportAutoConfiguration` do and when is it used instead of `@EnableAutoConfiguration`?","correctMd":"`@ImportAutoConfiguration` imports specific auto-configuration classes explicitly, bypassing the full autoconfiguration discovery mechanism.","answerFullMd":"`@ImportAutoConfiguration` imports specific auto-configuration classes explicitly, bypassing the full autoconfiguration discovery mechanism. It is used in test slices (`@WebMvcTest`, `@DataJpaTest`) to import only the subset of auto-configurations relevant to the tested layer — avoiding loading the entire application context for a narrow test. For example, `@DataJpaTest` applies `@ImportAutoConfiguration({JpaRepositoriesAutoConfiguration.class, DataSourceAutoConfiguration.class, ...})` — it only loads JPA-related auto-configurations, not security, web, or messaging. Use `@ImportAutoConfiguration` in your own test slices or when you need to import a specific subset of auto-configurations for integration tests.","distractorsMd":["`ApplicationContextRunner` is a test utility from `spring-boot-test` that creates a minimal `ApplicationContext` for testing auto-configuration.","Four steps: (1) Create an autoconfigure module with `@AutoConfiguration` class containing `@Conditional`-guarded `@Bean` methods and `@ConfigurationProperties` for user-overridable settings.","`@AutoConfigureAfter(OtherAutoConfig.class)` ensures `OtherAutoConfig` is processed before the current class."]},{"id":"spring/spring_boot_autoconfiguration#ad1be5092f13","section":"spring","module":"spring/spring_boot_autoconfiguration","moduleName":"spring boot autoconfiguration","sourceFile":"README.md","difficulty":"advanced","question":"How does Spring Boot 3.x GraalVM native image affect auto-configuration, and what is AOT processing?","answerFull":"Spring Boot 3.x introduced an AOT (Ahead-of-Time) processing phase that runs at build time. AOT processes the ApplicationContext at build time: evaluates conditions, determines which beans will be created for the active profile, and generates Java source code + reflection/proxy/resource hints that GraalVM's native-image tool needs. This means auto-configuration conditions are resolved at build time, not at runtime — the native image knows exactly which beans it will create. This removes the reflection overhead of runtime condition evaluation and enables native startup in ~50ms (vs ~2s for JVM). For your custom starter: if it uses reflection or dynamic proxies, you must provide RuntimeHintsRegistrar or @RegisterReflectionForBinding annotations to tell the AOT phase to preserve those types.","correct":"Spring Boot 3.x introduced an AOT (Ahead-of-Time) processing phase that runs at build time.","distractors":["ApplicationContextRunner is a test utility from spring-boot-test that creates a minimal ApplicationContext for testing auto-configuration.","@ConditionalOnMissingBean is evaluated during the BeanFactoryPostProcessor phase, after all BeanDefinitions are loaded but before any beans are instantiated.","The processor runs at compile time and generates META-INF/spring-autoconfigure-metadata.properties."],"distractorIds":["spring/spring_boot_autoconfiguration#f4a292cc4963","spring/spring_boot_autoconfiguration#d30b5be09444","spring/spring_boot_autoconfiguration#b60425d541a2"],"concepts":["aot","graalvm","ahead","affect","introduced","boot"],"answerFullMd":"Spring Boot 3.x introduced an AOT (Ahead-of-Time) processing phase that runs at build time. AOT processes the `ApplicationContext` at build time: evaluates conditions, determines which beans will be created for the active profile, and generates Java source code + reflection/proxy/resource hints that GraalVM's `native-image` tool needs. This means auto-configuration conditions are resolved at build time, not at runtime — the native image knows exactly which beans it will create. This removes the reflection overhead of runtime condition evaluation and enables native startup in ~50ms (vs ~2s for JVM). For your custom starter: if it uses reflection or dynamic proxies, you must provide `RuntimeHintsRegistrar` or `@RegisterReflectionForBinding` annotations to tell the AOT phase to preserve those types.","distractorsMd":["`ApplicationContextRunner` is a test utility from `spring-boot-test` that creates a minimal `ApplicationContext` for testing auto-configuration.","`@ConditionalOnMissingBean` is evaluated during the `BeanFactoryPostProcessor` phase, after all `BeanDefinition`s are loaded but before any beans are instantiated.","The processor runs at compile time and generates `META-INF/spring-autoconfigure-metadata.properties`."]},{"id":"spring/spring_boot_configuration#01c0f62733b1","section":"spring","module":"spring/spring_boot_configuration","moduleName":"spring boot configuration","sourceFile":"README.md","difficulty":"core","question":"What is relaxed binding and how does it help with Kubernetes secrets?","answerFull":"Relaxed binding maps multiple property name formats to the same Java field. maxConnections, max-connections, max_connections, and MAX_CONNECTIONS all bind to a Java field named maxConnections. This is critical for Kubernetes: Kubernetes Secret env vars must use uppercase and underscores (SPRING_DATASOURCE_PASSWORD), but Java code uses camelCase (password) under prefix spring.datasource. Relaxed binding handles the translation automatically. @Value does NOT support relaxed binding — only @ConfigurationProperties.","correct":"Relaxed binding maps multiple property name formats to the same Java field.","distractors":["Relaxed binding means Spring automatically matches property keys in multiple formats to the same @ConfigurationProperties field.","Profiles activate profile-specific property sources loaded after (and overriding) the base application.yml.","/actuator/env exposes the full Environment including all property source values, showing which property source provides each property."],"distractorIds":["spring/spring_boot_configuration#75de7f2c09d8","spring/spring_boot_configuration#9dcadaa98838","spring/spring_boot_configuration#a97e5af7efa9"],"concepts":["relaxed","help","formats","binding","secrets","property"],"answerFullMd":"Relaxed binding maps multiple property name formats to the same Java field. `maxConnections`, `max-connections`, `max_connections`, and `MAX_CONNECTIONS` all bind to a Java field named `maxConnections`. This is critical for Kubernetes: Kubernetes Secret env vars must use uppercase and underscores (`SPRING_DATASOURCE_PASSWORD`), but Java code uses camelCase (`password`) under prefix `spring.datasource`. Relaxed binding handles the translation automatically. `@Value` does NOT support relaxed binding — only `@ConfigurationProperties`.","distractorsMd":["Relaxed binding means Spring automatically matches property keys in multiple formats to the same `@ConfigurationProperties` field.","Profiles activate profile-specific property sources loaded after (and overriding) the base `application.yml`.","`/actuator/env` exposes the full `Environment` including all property source values, showing which property source provides each property."]},{"id":"spring/spring_boot_configuration#84fbc0bb1dcc","section":"spring","module":"spring/spring_boot_configuration","moduleName":"spring boot configuration","sourceFile":"README.md","difficulty":"core","question":"What is the difference between profile-specific files inside and outside the JAR?","answerFull":"Profile-specific files outside the JAR (filesystem, current directory) have higher priority than those inside the JAR. This allows operations teams to place an application-production.properties file alongside the JAR to override packaged defaults. Spring Boot checks ./config/, ./, then classpath:/config/, then classpath:/ in order. The convention enables the same JAR to be configured differently per environment without rebuilding.","correct":"Profile-specific files outside the JAR (filesystem, current directory) have higher priority than those inside the JAR.","distractors":["spring.config.import (Spring Boot 2.4+) is a property that specifies additional configuration files or sources to import.","Profiles activate profile-specific property sources loaded after (and overriding) the base application.yml.","Without @ConfigurationPropertiesScan, @ConfigurationProperties classes are only registered if they have @Component (not recommended) or are referenced via @EnableConfigurationProperties(MyProps.class)."],"distractorIds":["spring/spring_boot_configuration#259bdbb8a8d4","spring/spring_boot_configuration#9dcadaa98838","spring/spring_boot_configuration#7ddb6acc15ed"],"concepts":["jar","difference","directory","filesystem","outside","priority"],"answerFullMd":"Profile-specific files outside the JAR (filesystem, current directory) have higher priority than those inside the JAR. This allows operations teams to place an `application-production.properties` file alongside the JAR to override packaged defaults. Spring Boot checks `./config/`, `./`, then `classpath:/config/`, then `classpath:/` in order. The convention enables the same JAR to be configured differently per environment without rebuilding.","distractorsMd":["`spring.config.import` (Spring Boot 2.4+) is a property that specifies additional configuration files or sources to import.","Profiles activate profile-specific property sources loaded after (and overriding) the base `application.yml`.","Without `@ConfigurationPropertiesScan`, `@ConfigurationProperties` classes are only registered if they have `@Component` (not recommended) or are referenced via `@EnableConfigurationProperties(MyProps.class)`."]},{"id":"spring/spring_boot_configuration#b440e44ec768","section":"spring","module":"spring/spring_boot_configuration","moduleName":"spring boot configuration","sourceFile":"README.md","difficulty":"core","question":"How does @ConstructorBinding work and why is it preferred for immutable classes?","answerFull":"@ConstructorBinding (Spring Boot 2.x) or automatic detection of single constructor (Spring Boot 3.x) tells Spring to bind properties via constructor parameters instead of setters. This enables final fields — the bound object is immutable after creation. Immutable configuration is safer: no code can accidentally modify configuration post-initialization, and the object can be safely shared across threads. In Spring Boot 3.x, if @ConfigurationProperties class has only one constructor, @ConstructorBinding is implicit.","correct":"@ConstructorBinding (Spring Boot 2.x) or automatic detection of single constructor (Spring Boot 3.x) tells Spring to bind properties via constructor parameters instead of setters.","distractors":["Without @ConfigurationPropertiesScan, @ConfigurationProperties classes are only registered if they have @Component (not recommended) or are referenced via @EnableConfigurationProperties(MyProps.class).","spring.config.import (Spring Boot 2.4+) is a property that specifies additional configuration files or sources to import.","Relaxed binding means Spring automatically matches property keys in multiple formats to the same @ConfigurationProperties field."],"distractorIds":["spring/spring_boot_configuration#7ddb6acc15ed","spring/spring_boot_configuration#259bdbb8a8d4","spring/spring_boot_configuration#75de7f2c09d8"],"concepts":["constructorbinding","setters","preferred","bind","automatic","tells"],"correctMd":"`@ConstructorBinding` (Spring Boot 2.x) or automatic detection of single constructor (Spring Boot 3.x) tells Spring to bind properties via constructor parameters instead of setters.","answerFullMd":"`@ConstructorBinding` (Spring Boot 2.x) or automatic detection of single constructor (Spring Boot 3.x) tells Spring to bind properties via constructor parameters instead of setters. This enables `final` fields — the bound object is immutable after creation. Immutable configuration is safer: no code can accidentally modify configuration post-initialization, and the object can be safely shared across threads. In Spring Boot 3.x, if `@ConfigurationProperties` class has only one constructor, `@ConstructorBinding` is implicit.","distractorsMd":["Without `@ConfigurationPropertiesScan`, `@ConfigurationProperties` classes are only registered if they have `@Component` (not recommended) or are referenced via `@EnableConfigurationProperties(MyProps.class)`.","`spring.config.import` (Spring Boot 2.4+) is a property that specifies additional configuration files or sources to import.","Relaxed binding means Spring automatically matches property keys in multiple formats to the same `@ConfigurationProperties` field."]},{"id":"spring/spring_boot_configuration#259bdbb8a8d4","section":"spring","module":"spring/spring_boot_configuration","moduleName":"spring boot configuration","sourceFile":"README.md","difficulty":"core","question":"What is spring.config.import and how does it replace bootstrap.yml?","answerFull":"spring.config.import (Spring Boot 2.4+) is a property that specifies additional configuration files or sources to import. It supports file paths, classpath resources, and protocol handlers like configserver: (Spring Cloud Config) and vault: (Vault). Before 2.4, Spring Cloud Config required a separate bootstrap.yml file loaded by a bootstrap ApplicationContext. spring.config.import=configserver: replaces this, importing Config Server properties into the main context with proper priority ordering. It also supports optional: prefix to silently skip missing imports.","correct":"spring.config.import (Spring Boot 2.4+) is a property that specifies additional configuration files or sources to import.","distractors":["Relaxed binding means Spring automatically matches property keys in multiple formats to the same @ConfigurationProperties field.","/actuator/env exposes the full Environment including all property source values, showing which property source provides each property.","@ConstructorBinding (Spring Boot 2.x) or automatic detection of single constructor (Spring Boot 3.x) tells Spring to bind properties via constructor parameters instead of setters."],"distractorIds":["spring/spring_boot_configuration#75de7f2c09d8","spring/spring_boot_configuration#a97e5af7efa9","spring/spring_boot_configuration#b440e44ec768"],"concepts":["yml","replace","specifies","bootstrap","additional","import"],"correctMd":"`spring.config.import` (Spring Boot 2.4+) is a property that specifies additional configuration files or sources to import.","answerFullMd":"`spring.config.import` (Spring Boot 2.4+) is a property that specifies additional configuration files or sources to import. It supports file paths, classpath resources, and protocol handlers like `configserver:` (Spring Cloud Config) and `vault:` (Vault). Before 2.4, Spring Cloud Config required a separate `bootstrap.yml` file loaded by a bootstrap ApplicationContext. `spring.config.import=configserver:` replaces this, importing Config Server properties into the main context with proper priority ordering. It also supports `optional:` prefix to silently skip missing imports.","distractorsMd":["Relaxed binding means Spring automatically matches property keys in multiple formats to the same `@ConfigurationProperties` field.","`/actuator/env` exposes the full `Environment` including all property source values, showing which property source provides each property.","`@ConstructorBinding` (Spring Boot 2.x) or automatic detection of single constructor (Spring Boot 3.x) tells Spring to bind properties via constructor parameters instead of setters."]},{"id":"spring/spring_boot_configuration#6bed50d39d34","section":"spring","module":"spring/spring_boot_configuration","moduleName":"spring boot configuration","sourceFile":"README.md","difficulty":"intermediate","question":"How does @ConfigurationProperties validation work?","answerFull":"Add @Validated to the @ConfigurationProperties class and JSR-303 annotations (@NotNull, @Min, @Max, @NotEmpty, @Pattern) to fields. Spring Boot runs Bean Validation on bound properties during context startup. A validation failure throws BindValidationException with the full list of constraint violations, preventing the application from starting with invalid configuration. This is the correct \"fail fast\" behavior for misconfiguration. Without @Validated, invalid values like poolSize=-5 or url=null are silently accepted and cause failures much later.","correct":"Add @Validated to the @ConfigurationProperties class and JSR-303 annotations (@NotNull, @Min, @Max, @NotEmpty, @Pattern) to fields.","distractors":["Relaxed binding means Spring automatically matches property keys in multiple formats to the same @ConfigurationProperties field.","Add spring-boot-configuration-processor to build dependencies (optional compile scope).","Without @ConfigurationPropertiesScan, @ConfigurationProperties classes are only registered if they have @Component (not recommended) or are referenced via @EnableConfigurationProperties(MyProps.class)."],"distractorIds":["spring/spring_boot_configuration#75de7f2c09d8","spring/spring_boot_configuration#904b8824e615","spring/spring_boot_configuration#7ddb6acc15ed"],"concepts":["303","notempty","notnull","configurationproperties","jsr","validated"],"correctMd":"Add `@Validated` to the `@ConfigurationProperties` class and JSR-303 annotations (`@NotNull`, `@Min`, `@Max`, `@NotEmpty`, `@Pattern`) to fields.","answerFullMd":"Add `@Validated` to the `@ConfigurationProperties` class and JSR-303 annotations (`@NotNull`, `@Min`, `@Max`, `@NotEmpty`, `@Pattern`) to fields. Spring Boot runs Bean Validation on bound properties during context startup. A validation failure throws `BindValidationException` with the full list of constraint violations, preventing the application from starting with invalid configuration. This is the correct \"fail fast\" behavior for misconfiguration. Without `@Validated`, invalid values like `poolSize=-5` or `url=null` are silently accepted and cause failures much later.","distractorsMd":["Relaxed binding means Spring automatically matches property keys in multiple formats to the same `@ConfigurationProperties` field.","Add `spring-boot-configuration-processor` to build dependencies (optional compile scope).","Without `@ConfigurationPropertiesScan`, `@ConfigurationProperties` classes are only registered if they have `@Component` (not recommended) or are referenced via `@EnableConfigurationProperties(MyProps.class)`."]},{"id":"spring/spring_boot_configuration#1052c01205ac","section":"spring","module":"spring/spring_boot_configuration","moduleName":"spring boot configuration","sourceFile":"README.md","difficulty":"intermediate","question":"Can YAML files be used with @PropertySource?","answerFull":"No. @PropertySource only supports .properties files by default. It uses PropertiesPropertySourceLoader which parses key=value format. YAML requires YamlPropertySourceLoader. You can register a custom PropertySourceFactory on @PropertySource to handle YAML: @PropertySource(value=\"classpath:extra.yml\", factory=YamlPropertySourceFactory.class). Alternatively, use spring.config.import=classpath:extra.yml (Boot 2.4+), which uses the full config loading pipeline supporting YAML natively.","correct":"No. @PropertySource only supports .properties files by default.","distractors":["spring.config.import (Spring Boot 2.4+) is a property that specifies additional configuration files or sources to import.","A YAML file can contain multiple documents separated by ---.","@ConstructorBinding (Spring Boot 2.x) or automatic detection of single constructor (Spring Boot 3.x) tells Spring to bind properties via constructor parameters instead of setters."],"distractorIds":["spring/spring_boot_configuration#259bdbb8a8d4","spring/spring_boot_configuration#39da257715b9","spring/spring_boot_configuration#b440e44ec768"],"concepts":["propertysource","yaml","properties","supports","files","default"],"correctMd":"No. `@PropertySource` only supports `.properties` files by default.","answerFullMd":"No. `@PropertySource` only supports `.properties` files by default. It uses `PropertiesPropertySourceLoader` which parses key=value format. YAML requires `YamlPropertySourceLoader`. You can register a custom `PropertySourceFactory` on `@PropertySource` to handle YAML: `@PropertySource(value=\"classpath:extra.yml\", factory=YamlPropertySourceFactory.class)`. Alternatively, use `spring.config.import=classpath:extra.yml` (Boot 2.4+), which uses the full config loading pipeline supporting YAML natively.","distractorsMd":["`spring.config.import` (Spring Boot 2.4+) is a property that specifies additional configuration files or sources to import.","A YAML file can contain multiple documents separated by `---`.","`@ConstructorBinding` (Spring Boot 2.x) or automatic detection of single constructor (Spring Boot 3.x) tells Spring to bind properties via constructor parameters instead of setters."]},{"id":"spring/spring_boot_configuration#904b8824e615","section":"spring","module":"spring/spring_boot_configuration","moduleName":"spring boot configuration","sourceFile":"README.md","difficulty":"intermediate","question":"What is the @ConfigurationProperties metadata processor and why should you use it?","answerFull":"Add spring-boot-configuration-processor to build dependencies (optional compile scope). It runs at compile time and generates META-INF/spring-configuration-metadata.json describing all @ConfigurationProperties classes. IDE plugins (IntelliJ, Eclipse) read this metadata to provide: property name autocompletion in application.properties, documentation tooltips showing field descriptions, type information, and default values. Without this, application.properties is edited without any IDE assistance. For library/starter authors, this metadata is what makes their configuration discoverable and documented.","correct":"Add spring-boot-configuration-processor to build dependencies (optional compile scope).","distractors":["spring.config.import (Spring Boot 2.4+) is a property that specifies additional configuration files or sources to import.","Add @Validated to the @ConfigurationProperties class and use JSR-380 annotations (@NotNull, @Min, @Max, @Pattern, @NotEmpty, etc.) on the fields.","Relaxed binding means Spring automatically matches property keys in multiple formats to the same @ConfigurationProperties field."],"distractorIds":["spring/spring_boot_configuration#259bdbb8a8d4","spring/spring_boot_configuration#168157fd3047","spring/spring_boot_configuration#75de7f2c09d8"],"concepts":["configurationproperties","processor","boot","compile","optional","scope"],"correctMd":"Add `spring-boot-configuration-processor` to build dependencies (optional compile scope).","answerFullMd":"Add `spring-boot-configuration-processor` to build dependencies (optional compile scope). It runs at compile time and generates `META-INF/spring-configuration-metadata.json` describing all `@ConfigurationProperties` classes. IDE plugins (IntelliJ, Eclipse) read this metadata to provide: property name autocompletion in `application.properties`, documentation tooltips showing field descriptions, type information, and default values. Without this, application.properties is edited without any IDE assistance. For library/starter authors, this metadata is what makes their configuration discoverable and documented.","distractorsMd":["`spring.config.import` (Spring Boot 2.4+) is a property that specifies additional configuration files or sources to import.","Add `@Validated` to the `@ConfigurationProperties` class and use JSR-380 annotations (`@NotNull`, `@Min`, `@Max`, `@Pattern`, `@NotEmpty`, etc.) on the fields.","Relaxed binding means Spring automatically matches property keys in multiple formats to the same `@ConfigurationProperties` field."]},{"id":"spring/spring_boot_configuration#39da257715b9","section":"spring","module":"spring/spring_boot_configuration","moduleName":"spring boot configuration","sourceFile":"README.md","difficulty":"intermediate","question":"How do multi-document YAML files work with profiles?","answerFull":"A YAML file can contain multiple documents separated by ---. Each document can be conditionally activated using spring.config.activate.on-profile. When a profile is active, only the matching documents (plus documents with no activation condition) are applied. Documents are applied in order — later documents override earlier ones for the same key. This allows a single application.yml to contain all environment configurations, which some teams prefer over multiple files. Multi-document .properties files are not supported; only YAML supports this.","correct":"A YAML file can contain multiple documents separated by ---.","distractors":["Profile-specific files outside the JAR (filesystem, current directory) have higher priority than those inside the JAR.","Relaxed binding means Spring automatically matches property keys in multiple formats to the same @ConfigurationProperties field.","Relaxed binding maps multiple property name formats to the same Java field."],"distractorIds":["spring/spring_boot_configuration#84fbc0bb1dcc","spring/spring_boot_configuration#75de7f2c09d8","spring/spring_boot_configuration#01c0f62733b1"],"concepts":["separated","profiles","contain","yaml","files","documents"],"correctMd":"A YAML file can contain multiple documents separated by `---`.","answerFullMd":"A YAML file can contain multiple documents separated by `---`. Each document can be conditionally activated using `spring.config.activate.on-profile`. When a profile is active, only the matching documents (plus documents with no activation condition) are applied. Documents are applied in order — later documents override earlier ones for the same key. This allows a single `application.yml` to contain all environment configurations, which some teams prefer over multiple files. Multi-document `.properties` files are not supported; only YAML supports this.","distractorsMd":[null,"Relaxed binding means Spring automatically matches property keys in multiple formats to the same `@ConfigurationProperties` field.",null]},{"id":"spring/spring_boot_configuration#4d316a11fddb","section":"spring","module":"spring/spring_boot_configuration","moduleName":"spring boot configuration","sourceFile":"README.md","difficulty":"intermediate","question":"What is EnvironmentPostProcessor and when would you use it?","answerFull":"EnvironmentPostProcessor is an SPI that runs before the ApplicationContext is created, allowing programmatic manipulation of the ConfigurableEnvironment. Register via spring.factories (key: org.springframework.boot.env.EnvironmentPostProcessor) or META-INF/spring/... (Boot 3.x). Use cases: loading secrets from Vault or AWS Secrets Manager before other config is bound (ensuring secrets are available at highest priority), decrypting encrypted property values, or adding environment-specific property sources based on system detection.","correct":"EnvironmentPostProcessor is an SPI that runs before the ApplicationContext is created, allowing programmatic manipulation of the ConfigurableEnvironment.","distractors":["/actuator/env exposes the full Environment including all property source values, showing which property source provides each property.","@ConstructorBinding (Spring Boot 2.x) or automatic detection of single constructor (Spring Boot 3.x) tells Spring to bind properties via constructor parameters instead of setters.","A YAML file can contain multiple documents separated by ---."],"distractorIds":["spring/spring_boot_configuration#a97e5af7efa9","spring/spring_boot_configuration#b440e44ec768","spring/spring_boot_configuration#39da257715b9"],"concepts":["configurableenvironment","environmentpostprocessor","manipulation","programmatic","spi","allowing"],"correctMd":"`EnvironmentPostProcessor` is an SPI that runs before the ApplicationContext is created, allowing programmatic manipulation of the `ConfigurableEnvironment`.","answerFullMd":"`EnvironmentPostProcessor` is an SPI that runs before the ApplicationContext is created, allowing programmatic manipulation of the `ConfigurableEnvironment`. Register via `spring.factories` (key: `org.springframework.boot.env.EnvironmentPostProcessor`) or `META-INF/spring/...` (Boot 3.x). Use cases: loading secrets from Vault or AWS Secrets Manager before other config is bound (ensuring secrets are available at highest priority), decrypting encrypted property values, or adding environment-specific property sources based on system detection.","distractorsMd":["`/actuator/env` exposes the full `Environment` including all property source values, showing which property source provides each property.","`@ConstructorBinding` (Spring Boot 2.x) or automatic detection of single constructor (Spring Boot 3.x) tells Spring to bind properties via constructor parameters instead of setters.","A YAML file can contain multiple documents separated by `---`."]},{"id":"spring/spring_boot_configuration#a97e5af7efa9","section":"spring","module":"spring/spring_boot_configuration","moduleName":"spring boot configuration","sourceFile":"README.md","difficulty":"advanced","question":"What does /actuator/env expose and what are the security risks?","answerFull":"/actuator/env exposes the full Environment including all property source values, showing which property source provides each property. The response masks values matching patterns like *password*, *secret*, *key* with . However, this masking is pattern-based and not exhaustive — less obvious secret names may be exposed. In production, always secure actuator endpoints with Spring Security: management.endpoints.web.exposure.include=health,info (whitelist), and require authentication for the rest via SecurityFilterChain. Never expose /actuator/env without authentication.","correct":"/actuator/env exposes the full Environment including all property source values, showing which property source provides each property.","distractors":["Profiles activate profile-specific property sources loaded after (and overriding) the base application.yml.","spring.config.import (Spring Boot 2.4+) is a property that specifies additional configuration files or sources to import.","Relaxed binding means Spring automatically matches property keys in multiple formats to the same @ConfigurationProperties field."],"distractorIds":["spring/spring_boot_configuration#9dcadaa98838","spring/spring_boot_configuration#259bdbb8a8d4","spring/spring_boot_configuration#75de7f2c09d8"],"concepts":["showing","actuator","env","risks","expose","exposes"],"correctMd":"`/actuator/env` exposes the full `Environment` including all property source values, showing which property source provides each property.","answerFullMd":"`/actuator/env` exposes the full `Environment` including all property source values, showing which property source provides each property. The response masks values matching patterns like `*password*`, `*secret*`, `*key*` with `******`. However, this masking is pattern-based and not exhaustive — less obvious secret names may be exposed. In production, always secure actuator endpoints with Spring Security: `management.endpoints.web.exposure.include=health,info` (whitelist), and require authentication for the rest via `SecurityFilterChain`. Never expose `/actuator/env` without authentication.","distractorsMd":["Profiles activate profile-specific property sources loaded after (and overriding) the base `application.yml`.","`spring.config.import` (Spring Boot 2.4+) is a property that specifies additional configuration files or sources to import.","Relaxed binding means Spring automatically matches property keys in multiple formats to the same `@ConfigurationProperties` field."]},{"id":"spring/spring_boot_configuration#75de7f2c09d8","section":"spring","module":"spring/spring_boot_configuration","moduleName":"spring boot configuration","sourceFile":"README.md","difficulty":"advanced","question":"What is relaxed binding in @ConfigurationProperties and what are the four binding conventions it supports?","answerFull":"Relaxed binding means Spring automatically matches property keys in multiple formats to the same @ConfigurationProperties field. For a field private String myApiKey, all four formats bind to it: (1) my.api-key (kebab-case — recommended for .yml/.properties files), (2) MY_API_KEY (upper-case with underscore — OS environment variables, also allows MY.API_KEY), (3) my.apiKey (camelCase — older convention), (4) my.api_key (underscore — legacy). This relaxed binding is crucial for containerised deployments: MY_DATABASE_URL=jdbc:... in a Kubernetes Secret mounts as an environment variable and maps cleanly to spring.datasource.url without any custom mapping code.","correct":"Relaxed binding means Spring automatically matches property keys in multiple formats to the same @ConfigurationProperties field.","distractors":["Without @ConfigurationPropertiesScan, @ConfigurationProperties classes are only registered if they have @Component (not recommended) or are referenced via @EnableConfigurationProperties(MyProps.class).","Profiles activate profile-specific property sources loaded after (and overriding) the base application.yml.","spring.config.import (Spring Boot 2.4+) is a property that specifies additional configuration files or sources to import."],"distractorIds":["spring/spring_boot_configuration#7ddb6acc15ed","spring/spring_boot_configuration#9dcadaa98838","spring/spring_boot_configuration#259bdbb8a8d4"],"concepts":["conventions","relaxed","configurationproperties","formats","binding","property"],"questionMd":"What is relaxed binding in `@ConfigurationProperties` and what are the four binding conventions it supports?","correctMd":"Relaxed binding means Spring automatically matches property keys in multiple formats to the same `@ConfigurationProperties` field.","answerFullMd":"Relaxed binding means Spring automatically matches property keys in multiple formats to the same `@ConfigurationProperties` field. For a field `private String myApiKey`, all four formats bind to it: (1) `my.api-key` (kebab-case — recommended for `.yml`/`.properties` files), (2) `MY_API_KEY` (upper-case with underscore — OS environment variables, also allows `MY.API_KEY`), (3) `my.apiKey` (camelCase — older convention), (4) `my.api_key` (underscore — legacy). This relaxed binding is crucial for containerised deployments: `MY_DATABASE_URL=jdbc:...` in a Kubernetes Secret mounts as an environment variable and maps cleanly to `spring.datasource.url` without any custom mapping code.","distractorsMd":["Without `@ConfigurationPropertiesScan`, `@ConfigurationProperties` classes are only registered if they have `@Component` (not recommended) or are referenced via `@EnableConfigurationProperties(MyProps.class)`.","Profiles activate profile-specific property sources loaded after (and overriding) the base `application.yml`.","`spring.config.import` (Spring Boot 2.4+) is a property that specifies additional configuration files or sources to import."]},{"id":"spring/spring_boot_configuration#7ddb6acc15ed","section":"spring","module":"spring/spring_boot_configuration","moduleName":"spring boot configuration","sourceFile":"README.md","difficulty":"advanced","question":"What is @ConfigurationPropertiesScan and when is it needed?","answerFull":"Without @ConfigurationPropertiesScan, @ConfigurationProperties classes are only registered if they have @Component (not recommended) or are referenced via @EnableConfigurationProperties(MyProps.class). @ConfigurationPropertiesScan(\"com.example\") (Spring Boot 2.2+, analogous to @ComponentScan for @ConfigurationProperties) automatically registers all @ConfigurationProperties classes found in the specified packages without needing @EnableConfigurationProperties per class. In practice: use @ConfigurationPropertiesScan at the application level (alongside @SpringBootApplication) for automatic discovery; use @EnableConfigurationProperties inside autoconfiguration classes where you want explicit, self-contained registration.","correct":"Without @ConfigurationPropertiesScan, @ConfigurationProperties classes are only registered if they have @Component (not recommended) or are referenced via @EnableConfigurationProperties(MyProps.class).","distractors":["Relaxed binding means Spring automatically matches property keys in multiple formats to the same @ConfigurationProperties field.","Add @Validated to the @ConfigurationProperties class and use JSR-380 annotations (@NotNull, @Min, @Max, @Pattern, @NotEmpty, etc.) on the fields.","Add @Validated to the @ConfigurationProperties class and JSR-303 annotations (@NotNull, @Min, @Max, @NotEmpty, @Pattern) to fields."],"distractorIds":["spring/spring_boot_configuration#75de7f2c09d8","spring/spring_boot_configuration#168157fd3047","spring/spring_boot_configuration#6bed50d39d34"],"concepts":["configurationpropertiesscan","enableconfigurationproperties","myprops","recommended","configurationproperties","referenced"],"questionMd":"What is `@ConfigurationPropertiesScan` and when is it needed?","correctMd":"Without `@ConfigurationPropertiesScan`, `@ConfigurationProperties` classes are only registered if they have `@Component` (not recommended) or are referenced via `@EnableConfigurationProperties(MyProps.class)`.","answerFullMd":"Without `@ConfigurationPropertiesScan`, `@ConfigurationProperties` classes are only registered if they have `@Component` (not recommended) or are referenced via `@EnableConfigurationProperties(MyProps.class)`. `@ConfigurationPropertiesScan(\"com.example\")` (Spring Boot 2.2+, analogous to `@ComponentScan` for `@ConfigurationProperties`) automatically registers all `@ConfigurationProperties` classes found in the specified packages without needing `@EnableConfigurationProperties` per class. In practice: use `@ConfigurationPropertiesScan` at the application level (alongside `@SpringBootApplication`) for automatic discovery; use `@EnableConfigurationProperties` inside autoconfiguration classes where you want explicit, self-contained registration.","distractorsMd":["Relaxed binding means Spring automatically matches property keys in multiple formats to the same `@ConfigurationProperties` field.","Add `@Validated` to the `@ConfigurationProperties` class and use JSR-380 annotations (`@NotNull`, `@Min`, `@Max`, `@Pattern`, `@NotEmpty`, etc.) on the fields.","Add `@Validated` to the `@ConfigurationProperties` class and JSR-303 annotations (`@NotNull`, `@Min`, `@Max`, `@NotEmpty`, `@Pattern`) to fields."]},{"id":"spring/spring_boot_configuration#168157fd3047","section":"spring","module":"spring/spring_boot_configuration","moduleName":"spring boot configuration","sourceFile":"README.md","difficulty":"advanced","question":"How do you validate @ConfigurationProperties fields, and what happens if validation fails at startup?","answerFull":"Add @Validated to the @ConfigurationProperties class and use JSR-380 annotations (@NotNull, @Min, @Max, @Pattern, @NotEmpty, etc.) on the fields. Spring Boot evaluates validation during ApplicationContext refresh (before the app starts serving traffic). If any constraint fails, a BindValidationException is thrown with a clear message listing every failing constraint — the application does not start. Custom validators: implement Validator (Spring's interface) or javax.validation.ConstraintValidator and annotate the class with @Validated. Example: @NotNull @URL private String externalApiUrl — the app refuses to start if external-api.url is missing or not a valid URL, preventing deployment of misconfigured images.","correct":"Add @Validated to the @ConfigurationProperties class and use JSR-380 annotations (@NotNull, @Min, @Max, @Pattern, @NotEmpty, etc.) on the fields.","distractors":["Without @ConfigurationPropertiesScan, @ConfigurationProperties classes are only registered if they have @Component (not recommended) or are referenced via @EnableConfigurationProperties(MyProps.class).","Relaxed binding means Spring automatically matches property keys in multiple formats to the same @ConfigurationProperties field.","Add spring-boot-configuration-processor to build dependencies (optional compile scope)."],"distractorIds":["spring/spring_boot_configuration#7ddb6acc15ed","spring/spring_boot_configuration#75de7f2c09d8","spring/spring_boot_configuration#904b8824e615"],"concepts":["380","notempty","notnull","configurationproperties","jsr","validated"],"questionMd":"How do you validate `@ConfigurationProperties` fields, and what happens if validation fails at startup?","correctMd":"Add `@Validated` to the `@ConfigurationProperties` class and use JSR-380 annotations (`@NotNull`, `@Min`, `@Max`, `@Pattern`, `@NotEmpty`, etc.) on the fields.","answerFullMd":"Add `@Validated` to the `@ConfigurationProperties` class and use JSR-380 annotations (`@NotNull`, `@Min`, `@Max`, `@Pattern`, `@NotEmpty`, etc.) on the fields. Spring Boot evaluates validation during `ApplicationContext` refresh (before the app starts serving traffic). If any constraint fails, a `BindValidationException` is thrown with a clear message listing every failing constraint — the application does not start. Custom validators: implement `Validator` (Spring's interface) or `javax.validation.ConstraintValidator` and annotate the class with `@Validated`. Example: `@NotNull @URL private String externalApiUrl` — the app refuses to start if `external-api.url` is missing or not a valid URL, preventing deployment of misconfigured images.","distractorsMd":["Without `@ConfigurationPropertiesScan`, `@ConfigurationProperties` classes are only registered if they have `@Component` (not recommended) or are referenced via `@EnableConfigurationProperties(MyProps.class)`.","Relaxed binding means Spring automatically matches property keys in multiple formats to the same `@ConfigurationProperties` field.","Add `spring-boot-configuration-processor` to build dependencies (optional compile scope)."]},{"id":"spring/spring_boot_configuration#9dcadaa98838","section":"spring","module":"spring/spring_boot_configuration","moduleName":"spring boot configuration","sourceFile":"README.md","difficulty":"advanced","question":"How do Spring profiles interact with @ConfigurationProperties and what is the profile-specific properties file convention?","answerFull":"Profiles activate profile-specific property sources loaded after (and overriding) the base application.yml. Convention: application-{profile}.yml (e.g., application-prod.yml, application-staging.yml) is automatically loaded when that profile is active. For @ConfigurationProperties, there is no per-profile annotation — the binding applies to whatever the active Environment contains. The active profiles' property files override the base file for matching keys. Multi-document YAML with spring.config.activate.on-profile achieves the same in a single file. Key pitfall: @Profile(\"prod\") on a @ConfigurationProperties class means the class is not registered in non-prod environments, so non-prod code that injects it will fail. Prefer single always-registered @ConfigurationProperties class with profile-specific values handled by different application-{profile}.yml files.","correct":"Profiles activate profile-specific property sources loaded after (and overriding) the base application.yml.","distractors":["Relaxed binding means Spring automatically matches property keys in multiple formats to the same @ConfigurationProperties field.","@ConstructorBinding (Spring Boot 2.x) or automatic detection of single constructor (Spring Boot 3.x) tells Spring to bind properties via constructor parameters instead of setters.","Add @Validated to the @ConfigurationProperties class and use JSR-380 annotations (@NotNull, @Min, @Max, @Pattern, @NotEmpty, etc.) on the fields."],"distractorIds":["spring/spring_boot_configuration#75de7f2c09d8","spring/spring_boot_configuration#b440e44ec768","spring/spring_boot_configuration#168157fd3047"],"concepts":["activate","convention","interact","overriding","yml","configurationproperties"],"questionMd":"How do Spring profiles interact with `@ConfigurationProperties` and what is the profile-specific properties file convention?","correctMd":"Profiles activate profile-specific property sources loaded after (and overriding) the base `application.yml`.","answerFullMd":"Profiles activate profile-specific property sources loaded after (and overriding) the base `application.yml`. Convention: `application-{profile}.yml` (e.g., `application-prod.yml`, `application-staging.yml`) is automatically loaded when that profile is active. For `@ConfigurationProperties`, there is no per-profile annotation — the binding applies to whatever the active `Environment` contains. The active profiles' property files override the base file for matching keys. Multi-document YAML with `spring.config.activate.on-profile` achieves the same in a single file. Key pitfall: `@Profile(\"prod\")` on a `@ConfigurationProperties` class means the class is not registered in non-prod environments, so non-prod code that injects it will fail. Prefer single always-registered `@ConfigurationProperties` class with profile-specific values handled by different `application-{profile}.yml` files.","distractorsMd":["Relaxed binding means Spring automatically matches property keys in multiple formats to the same `@ConfigurationProperties` field.","`@ConstructorBinding` (Spring Boot 2.x) or automatic detection of single constructor (Spring Boot 3.x) tells Spring to bind properties via constructor parameters instead of setters.","Add `@Validated` to the `@ConfigurationProperties` class and use JSR-380 annotations (`@NotNull`, `@Min`, `@Max`, `@Pattern`, `@NotEmpty`, etc.) on the fields."]},{"id":"spring/spring_caching#ad5d6bcdf719","section":"spring","module":"spring/spring_caching","moduleName":"spring caching","sourceFile":"README.md","difficulty":"core","question":"What does @EnableCaching actually do under the hood?","answerFull":"It imports CachingConfigurationSelector, which registers BeanFactoryCacheOperationSourceAdvisor into the application context. This advisor scans beans for @Cacheable/@CachePut/@CacheEvict annotations and wraps matching beans in CGLIB (or JDK dynamic) proxies with a CacheInterceptor. Without @EnableCaching, all cache annotations are silently ignored — no errors, no caching.","correct":"It imports CachingConfigurationSelector, which registers BeanFactoryCacheOperationSourceAdvisor into the application context.","distractors":["TTL expiration is passive and time-based, configured once on the CacheManager, while @CacheEvict is an active removal triggered by application code at a specific write.","Annotate the test with @SpringBootTest to load the full context.","@CacheConfig at the class level sets default cacheNames, keyGenerator, cacheManager, and cacheResolver for all cache annotations in the class."],"distractorIds":["spring/spring_caching#b9a600e64b55","spring/spring_caching#c0bbfaba9e85","spring/spring_caching#642165f7e99b"],"concepts":["beanfactorycacheoperationsourceadvisor","cachingconfigurationselector","hood","imports","registers","actually"],"questionMd":"What does `@EnableCaching` actually do under the hood?","correctMd":"It imports `CachingConfigurationSelector`, which registers `BeanFactoryCacheOperationSourceAdvisor` into the application context.","answerFullMd":"It imports `CachingConfigurationSelector`, which registers `BeanFactoryCacheOperationSourceAdvisor` into the application context. This advisor scans beans for `@Cacheable`/`@CachePut`/`@CacheEvict` annotations and wraps matching beans in CGLIB (or JDK dynamic) proxies with a `CacheInterceptor`. Without `@EnableCaching`, all cache annotations are silently ignored — no errors, no caching.","distractorsMd":["TTL expiration is passive and time-based, configured once on the `CacheManager`, while `@CacheEvict` is an active removal triggered by application code at a specific write.","Annotate the test with `@SpringBootTest` to load the full context.","`@CacheConfig` at the class level sets default `cacheNames`, `keyGenerator`, `cacheManager`, and `cacheResolver` for all cache annotations in the class."]},{"id":"spring/spring_caching#97b57e4de0cd","section":"spring","module":"spring/spring_caching","moduleName":"spring caching","sourceFile":"README.md","difficulty":"core","question":"What is the difference between @Cacheable and @CachePut?","answerFull":"@Cacheable skips method execution on a cache hit and populates the cache only on a miss — it is a read optimization. @CachePut always executes the method and always writes the result to the cache regardless of existing entries — it is a write-through pattern used after data mutations to keep the cache consistent. Mixing them on the same method with the same key is undefined behavior and should be avoided.","correct":"@Cacheable skips method execution on a cache hit and populates the cache only on a miss — it is a read optimization.","distractors":["By default, eviction happens after the method returns successfully.","@Cacheable performs a cache-read: it checks the cache first; if a hit is found, returns the cached value without executing the method.","Spring's cache abstraction is not transaction-aware by default."],"distractorIds":["spring/spring_caching#e1bede849e48","spring/spring_caching#bc9e884ccf42","spring/spring_caching#4bb17120145f"],"concepts":["cacheput","populates","skips","difference","cacheable","hit"],"questionMd":"What is the difference between `@Cacheable` and `@CachePut`?","correctMd":"`@Cacheable` skips method execution on a cache hit and populates the cache only on a miss — it is a read optimization.","answerFullMd":"`@Cacheable` skips method execution on a cache hit and populates the cache only on a miss — it is a read optimization. `@CachePut` always executes the method and always writes the result to the cache regardless of existing entries — it is a write-through pattern used after data mutations to keep the cache consistent. Mixing them on the same method with the same key is undefined behavior and should be avoided.","distractorsMd":[null,"`@Cacheable` performs a cache-read: it checks the cache first; if a hit is found, returns the cached value without executing the method.",null]},{"id":"spring/spring_caching#9c999058dd81","section":"spring","module":"spring/spring_caching","moduleName":"spring caching","sourceFile":"README.md","difficulty":"core","question":"How does SimpleKeyGenerator generate cache keys?","answerFull":"Zero parameters produce SimpleKey.EMPTY. One parameter produces the parameter itself as the key. Multiple parameters produce a SimpleKey wrapping the parameter array. It relies on equals and hashCode of parameters — if the parameter does not override these (e.g., a mutable POJO), every call may appear as a cache miss. Always prefer SpEL expressions for complex types.","correct":"Zero parameters produce SimpleKey.EMPTY.","distractors":["Spring's cache abstraction is not transaction-aware by default.","@CacheConfig at the class level sets default cacheNames, keyGenerator, cacheManager, and cacheResolver for all cache annotations in the class.","@Cacheable performs a cache-read: it checks the cache first; if a hit is found, returns the cached value without executing the method."],"distractorIds":["spring/spring_caching#4bb17120145f","spring/spring_caching#642165f7e99b","spring/spring_caching#bc9e884ccf42"],"concepts":["simplekey","empty","generate","produce","keys","parameters"],"questionMd":"How does `SimpleKeyGenerator` generate cache keys?","correctMd":"Zero parameters produce `SimpleKey.EMPTY`.","answerFullMd":"Zero parameters produce `SimpleKey.EMPTY`. One parameter produces the parameter itself as the key. Multiple parameters produce a `SimpleKey` wrapping the parameter array. It relies on `equals` and `hashCode` of parameters — if the parameter does not override these (e.g., a mutable POJO), every call may appear as a cache miss. Always prefer SpEL expressions for complex types.","distractorsMd":[null,"`@CacheConfig` at the class level sets default `cacheNames`, `keyGenerator`, `cacheManager`, and `cacheResolver` for all cache annotations in the class.","`@Cacheable` performs a cache-read: it checks the cache first; if a hit is found, returns the cached value without executing the method."]},{"id":"spring/spring_caching#0569df0b5c56","section":"spring","module":"spring/spring_caching","moduleName":"spring caching","sourceFile":"README.md","difficulty":"core","question":"What is a cache stampede and how does sync=true address it?","answerFull":"A cache stampede occurs when many threads simultaneously observe a cache miss and all call the underlying data source concurrently, multiplying load. sync=true on @Cacheable causes CacheInterceptor to use Cache.get(key, Callable), which in Caffeine and ConcurrentMap serializes computation for the same key — only one thread calls the method, others wait. This is a JVM-local lock only; for distributed stampede protection across multiple instances, a distributed lock (Redisson) is required.","correct":"A cache stampede occurs when many threads simultaneously observe a cache miss and all call the underlying data source concurrently, multiplying load.","distractors":["@CacheConfig at the class level sets default cacheNames, keyGenerator, cacheManager, and cacheResolver for all cache annotations in the class.","Annotate the test with @SpringBootTest to load the full context.","Cache-aside populates the cache lazily on a read miss, while write-through refreshes the cache eagerly on every write, trading staleness risk against wasted work."],"distractorIds":["spring/spring_caching#642165f7e99b","spring/spring_caching#c0bbfaba9e85","spring/spring_caching#1f894a8b8314"],"concepts":["stampede","multiplying","observe","concurrently","sync","miss"],"questionMd":"What is a cache stampede and how does `sync=true` address it?","answerFullMd":"A cache stampede occurs when many threads simultaneously observe a cache miss and all call the underlying data source concurrently, multiplying load. `sync=true` on `@Cacheable` causes `CacheInterceptor` to use `Cache.get(key, Callable)`, which in Caffeine and ConcurrentMap serializes computation for the same key — only one thread calls the method, others wait. This is a JVM-local lock only; for distributed stampede protection across multiple instances, a distributed lock (Redisson) is required.","distractorsMd":["`@CacheConfig` at the class level sets default `cacheNames`, `keyGenerator`, `cacheManager`, and `cacheResolver` for all cache annotations in the class.","Annotate the test with `@SpringBootTest` to load the full context.",null]},{"id":"spring/spring_caching#b13ee4547038","section":"spring","module":"spring/spring_caching","moduleName":"spring caching","sourceFile":"README.md","difficulty":"core","question":"Explain condition vs unless in @Cacheable.","answerFull":"condition is evaluated before the method executes using SpEL; if it returns false, caching is completely bypassed for this invocation (no lookup, no store). unless is evaluated after the method returns and can inspect #result; if it returns true, the result is not stored in the cache (but the method did execute). Use condition to skip caching for invalid inputs (e.g., #id > 0) and unless to skip caching null or empty results (e.g., #result == null).","correct":"condition is evaluated before the method executes using SpEL; if it returns false, caching is completely bypassed for this invocation (no lookup, no store).","distractors":["Spring caching uses AOP proxies.","By default, eviction happens after the method returns successfully.","@Cacheable performs a cache-read: it checks the cache first; if a hit is found, returns the cached value without executing the method."],"distractorIds":["spring/spring_caching#ae5466cce8fb","spring/spring_caching#e1bede849e48","spring/spring_caching#bc9e884ccf42"],"concepts":["bypassed","spel","explain","completely","evaluated","cacheable"],"questionMd":"Explain `condition` vs `unless` in `@Cacheable`.","correctMd":"`condition` is evaluated before the method executes using SpEL; if it returns false, caching is completely bypassed for this invocation (no lookup, no store).","answerFullMd":"`condition` is evaluated before the method executes using SpEL; if it returns false, caching is completely bypassed for this invocation (no lookup, no store). `unless` is evaluated after the method returns and can inspect `#result`; if it returns true, the result is not stored in the cache (but the method did execute). Use `condition` to skip caching for invalid inputs (e.g., `#id > 0`) and `unless` to skip caching null or empty results (e.g., `#result == null`).","distractorsMd":[null,null,"`@Cacheable` performs a cache-read: it checks the cache first; if a hit is found, returns the cached value without executing the method."]},{"id":"spring/spring_caching#4bb17120145f","section":"spring","module":"spring/spring_caching","moduleName":"spring caching","sourceFile":"README.md","difficulty":"intermediate","question":"Why can @Cacheable and @Transactional interact badly?","answerFull":"Spring's cache abstraction is not transaction-aware by default. When both annotations are on the same method, CacheInterceptor populates the cache when the method returns, but the transaction may not have committed yet. If the transaction rolls back, the cache contains data that was never persisted to the database. The fix is to separate read (cacheable) from write (transactional) operations, or to use TransactionSynchronizationManager.registerSynchronization() to schedule cache population as an afterCommit callback.","correct":"Spring's cache abstraction is not transaction-aware by default.","distractors":["@CacheConfig at the class level sets default cacheNames, keyGenerator, cacheManager, and cacheResolver for all cache annotations in the class.","A cache stampede occurs when many threads simultaneously observe a cache miss and all call the underlying data source concurrently, multiplying load.","Spring caching uses AOP proxies."],"distractorIds":["spring/spring_caching#642165f7e99b","spring/spring_caching#0569df0b5c56","spring/spring_caching#ae5466cce8fb"],"concepts":["badly","interact","cacheable","abstraction","aware","transactional"],"questionMd":"Why can `@Cacheable` and `@Transactional` interact badly?","answerFullMd":"Spring's cache abstraction is not transaction-aware by default. When both annotations are on the same method, `CacheInterceptor` populates the cache when the method returns, but the transaction may not have committed yet. If the transaction rolls back, the cache contains data that was never persisted to the database. The fix is to separate read (cacheable) from write (transactional) operations, or to use `TransactionSynchronizationManager.registerSynchronization()` to schedule cache population as an `afterCommit` callback.","distractorsMd":["`@CacheConfig` at the class level sets default `cacheNames`, `keyGenerator`, `cacheManager`, and `cacheResolver` for all cache annotations in the class.",null,null]},{"id":"spring/spring_caching#e1bede849e48","section":"spring","module":"spring/spring_caching","moduleName":"spring caching","sourceFile":"README.md","difficulty":"intermediate","question":"What is @CacheEvict(beforeInvocation = true) used for?","answerFull":"By default, eviction happens after the method returns successfully. If the method throws, the cache is not cleared. Setting beforeInvocation = true evicts the cache entry before the method runs, ensuring the entry is removed regardless of whether the method succeeds or throws. Use this when it is safer to serve a cache miss than to serve potentially stale data during a failed update.","correct":"By default, eviction happens after the method returns successfully.","distractors":["Spring's cache abstraction is not transaction-aware by default.","@CacheConfig at the class level sets default cacheNames, keyGenerator, cacheManager, and cacheResolver for all cache annotations in the class.","A key collision happens when two methods share the same cache name and produce identical computed keys, so one method's cached value is silently returned for the other."],"distractorIds":["spring/spring_caching#4bb17120145f","spring/spring_caching#642165f7e99b","spring/spring_caching#1c32a9de334a"],"concepts":["cacheevict","successfully","eviction","happens","true","returns"],"questionMd":"What is `@CacheEvict(beforeInvocation = true)` used for?","answerFullMd":"By default, eviction happens after the method returns successfully. If the method throws, the cache is not cleared. Setting `beforeInvocation = true` evicts the cache entry before the method runs, ensuring the entry is removed regardless of whether the method succeeds or throws. Use this when it is safer to serve a cache miss than to serve potentially stale data during a failed update.","distractorsMd":[null,"`@CacheConfig` at the class level sets default `cacheNames`, `keyGenerator`, `cacheManager`, and `cacheResolver` for all cache annotations in the class.",null]},{"id":"spring/spring_caching#1e02ff846cd4","section":"spring","module":"spring/spring_caching","moduleName":"spring caching","sourceFile":"README.md","difficulty":"intermediate","question":"How do you prevent storing null values in Redis cache?","answerFull":"Call .disableCachingNullValues() on RedisCacheConfiguration. This throws an IllegalArgumentException if a null value reaches the cache. Pair this with @Cacheable(unless = \"#result == null\") so nulls are filtered out before reaching the cache store, or have methods return Optional<T> instead of nullable types.","correct":"Call .disableCachingNullValues() on RedisCacheConfiguration.","distractors":["@Cacheable skips method execution on a cache hit and populates the cache only on a miss — it is a read optimization.","Spring's cache abstraction is not transaction-aware by default.","A key collision happens when two methods share the same cache name and produce identical computed keys, so one method's cached value is silently returned for the other."],"distractorIds":["spring/spring_caching#97b57e4de0cd","spring/spring_caching#4bb17120145f","spring/spring_caching#1c32a9de334a"],"concepts":["disablecachingnullvalues","rediscacheconfiguration","prevent","storing","null","redis"],"correctMd":"Call `.disableCachingNullValues()` on `RedisCacheConfiguration`.","answerFullMd":"Call `.disableCachingNullValues()` on `RedisCacheConfiguration`. This throws an `IllegalArgumentException` if a null value reaches the cache. Pair this with `@Cacheable(unless = \"#result == null\")` so nulls are filtered out before reaching the cache store, or have methods return `Optional<T>` instead of nullable types.","distractorsMd":["`@Cacheable` skips method execution on a cache hit and populates the cache only on a miss — it is a read optimization.",null,null]},{"id":"spring/spring_caching#ae5466cce8fb","section":"spring","module":"spring/spring_caching","moduleName":"spring caching","sourceFile":"README.md","difficulty":"intermediate","question":"Why does the cache not work when calling a @Cacheable method from within the same class?","answerFull":"Spring caching uses AOP proxies. When a method calls this.method(), it bypasses the proxy and invokes the method directly on the target object. The CacheInterceptor is never invoked, so no cache lookup or population occurs. The fix is to either inject the bean into itself (@Autowired @Lazy private MyService self) and call self.method(), or refactor the cached method into a separate Spring bean.","correct":"Spring caching uses AOP proxies.","distractors":["@CacheConfig at the class level sets default cacheNames, keyGenerator, cacheManager, and cacheResolver for all cache annotations in the class.","A key collision happens when two methods share the same cache name and produce identical computed keys, so one method's cached value is silently returned for the other.","Spring's cache abstraction is not transaction-aware by default."],"distractorIds":["spring/spring_caching#642165f7e99b","spring/spring_caching#1c32a9de334a","spring/spring_caching#4bb17120145f"],"concepts":["cacheable","aop","proxies","caching","calling","work"],"questionMd":"Why does the cache not work when calling a `@Cacheable` method from within the same class?","answerFullMd":"Spring caching uses AOP proxies. When a method calls `this.method()`, it bypasses the proxy and invokes the method directly on the target object. The `CacheInterceptor` is never invoked, so no cache lookup or population occurs. The fix is to either inject the bean into itself (`@Autowired @Lazy private MyService self`) and call `self.method()`, or refactor the cached method into a separate Spring bean.","distractorsMd":["`@CacheConfig` at the class level sets default `cacheNames`, `keyGenerator`, `cacheManager`, and `cacheResolver` for all cache annotations in the class.",null,null]},{"id":"spring/spring_caching#642165f7e99b","section":"spring","module":"spring/spring_caching","moduleName":"spring caching","sourceFile":"README.md","difficulty":"advanced","question":"How does @CacheConfig help reduce boilerplate?","answerFull":"@CacheConfig at the class level sets default cacheNames, keyGenerator, cacheManager, and cacheResolver for all cache annotations in the class. Instead of repeating cacheNames = \"products\" on every method, declare @CacheConfig(cacheNames = \"products\") on the class. Individual method annotations can still override the class-level defaults.","correct":"@CacheConfig at the class level sets default cacheNames, keyGenerator, cacheManager, and cacheResolver for all cache annotations in the class.","distractors":["Spring's cache abstraction is not transaction-aware by default.","A key collision happens when two methods share the same cache name and produce identical computed keys, so one method's cached value is silently returned for the other.","A cache stampede occurs when many threads simultaneously observe a cache miss and all call the underlying data source concurrently, multiplying load."],"distractorIds":["spring/spring_caching#4bb17120145f","spring/spring_caching#1c32a9de334a","spring/spring_caching#0569df0b5c56"],"concepts":["cacheconfig","cachenames","cacheresolver","keygenerator","boilerplate","cachemanager"],"questionMd":"How does `@CacheConfig` help reduce boilerplate?","correctMd":"`@CacheConfig` at the class level sets default `cacheNames`, `keyGenerator`, `cacheManager`, and `cacheResolver` for all cache annotations in the class.","answerFullMd":"`@CacheConfig` at the class level sets default `cacheNames`, `keyGenerator`, `cacheManager`, and `cacheResolver` for all cache annotations in the class. Instead of repeating `cacheNames = \"products\"` on every method, declare `@CacheConfig(cacheNames = \"products\")` on the class. Individual method annotations can still override the class-level defaults."},{"id":"spring/spring_caching#c0bbfaba9e85","section":"spring","module":"spring/spring_caching","moduleName":"spring caching","sourceFile":"README.md","difficulty":"advanced","question":"How do you test @Cacheable behavior in a Spring Boot integration test?","answerFull":"Annotate the test with @SpringBootTest to load the full context. Inject the service under test and call the method twice. Verify the underlying dependency (repository mock) was called exactly once using Mockito.verify(repository, times(1)).findById(anyLong()). Alternatively, use @CacheConfig on the test configuration with a ConcurrentMapCacheManager to avoid Redis dependency in unit tests. Use CacheManager.getCache(\"products\").clear() in @BeforeEach to reset state between tests.","correct":"Annotate the test with @SpringBootTest to load the full context.","distractors":["It imports CachingConfigurationSelector, which registers BeanFactoryCacheOperationSourceAdvisor into the application context.","@Cacheable performs a cache-read: it checks the cache first; if a hit is found, returns the cached value without executing the method.","@Cacheable skips method execution on a cache hit and populates the cache only on a miss — it is a read optimization."],"distractorIds":["spring/spring_caching#ad5d6bcdf719","spring/spring_caching#bc9e884ccf42","spring/spring_caching#97b57e4de0cd"],"concepts":["integration","springboottest","annotate","cacheable","boot","behavior"],"questionMd":"How do you test `@Cacheable` behavior in a Spring Boot integration test?","correctMd":"Annotate the test with `@SpringBootTest` to load the full context.","answerFullMd":"Annotate the test with `@SpringBootTest` to load the full context. Inject the service under test and call the method twice. Verify the underlying dependency (repository mock) was called exactly once using `Mockito.verify(repository, times(1)).findById(anyLong())`. Alternatively, use `@CacheConfig` on the test configuration with a `ConcurrentMapCacheManager` to avoid Redis dependency in unit tests. Use `CacheManager.getCache(\"products\").clear()` in `@BeforeEach` to reset state between tests.","distractorsMd":["It imports `CachingConfigurationSelector`, which registers `BeanFactoryCacheOperationSourceAdvisor` into the application context.","`@Cacheable` performs a cache-read: it checks the cache first; if a hit is found, returns the cached value without executing the method.","`@Cacheable` skips method execution on a cache hit and populates the cache only on a miss — it is a read optimization."]},{"id":"spring/spring_caching#bc9e884ccf42","section":"spring","module":"spring/spring_caching","moduleName":"spring caching","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between @CachePut and @Cacheable, and when should you use each?","answerFull":"@Cacheable performs a cache-read: it checks the cache first; if a hit is found, returns the cached value without executing the method. If a miss, executes the method and populates the cache. @CachePut always executes the method AND always writes the result to the cache — no cache read is performed. Use @CachePut on update/create operations where you want to refresh the cache with the newly written value without requiring a subsequent cache miss to repopulate it. Example: a save(Product) method annotated with @CachePut(key=\"#product.id\") ensures the cache is always consistent with what was just persisted. Never put @Cacheable and @CachePut with the same key on the same method — the write path would be skipped by @Cacheable if a cached value already exists.","correct":"@Cacheable performs a cache-read: it checks the cache first; if a hit is found, returns the cached value without executing the method.","distractors":["Cache-aside populates the cache lazily on a read miss, while write-through refreshes the cache eagerly on every write, trading staleness risk against wasted work.","Spring's cache abstraction is not transaction-aware by default.","condition is evaluated before the method executes using SpEL; if it returns false, caching is completely bypassed for this invocation (no lookup, no store)."],"distractorIds":["spring/spring_caching#1f894a8b8314","spring/spring_caching#4bb17120145f","spring/spring_caching#b13ee4547038"],"concepts":["cacheput","difference","cacheable","hit","executing","found"],"questionMd":"What is the difference between `@CachePut` and `@Cacheable`, and when should you use each?","correctMd":"`@Cacheable` performs a cache-read: it checks the cache first; if a hit is found, returns the cached value without executing the method.","answerFullMd":"`@Cacheable` performs a cache-read: it checks the cache first; if a hit is found, returns the cached value without executing the method. If a miss, executes the method and populates the cache. `@CachePut` always executes the method AND always writes the result to the cache — no cache read is performed. Use `@CachePut` on update/create operations where you want to refresh the cache with the newly written value without requiring a subsequent cache miss to repopulate it. Example: a `save(Product)` method annotated with `@CachePut(key=\"#product.id\")` ensures the cache is always consistent with what was just persisted. Never put `@Cacheable` and `@CachePut` with the same key on the same method — the write path would be skipped by `@Cacheable` if a cached value already exists.","distractorsMd":[null,null,"`condition` is evaluated before the method executes using SpEL; if it returns false, caching is completely bypassed for this invocation (no lookup, no store)."]},{"id":"spring/spring_caching#1c32a9de334a","section":"spring","module":"spring/spring_caching","moduleName":"spring caching","sourceFile":"README.md","difficulty":"advanced","question":"What causes a cache key collision between two different @Cacheable methods, and how do you prevent it?","answerFull":"A key collision happens when two methods share the same cache name and produce identical computed keys, so one method's cached value is silently returned for the other. For example, findById(1L) and findByVariantId(1L) both default to key 1 under SimpleKeyGenerator when they share the products cache — the second call returns the first entity's cached result with no error or warning of any kind. The fix is to always prefix the key with #root.methodName (e.g. key = \"#root.methodName + ':' + #id\") or give each method its own cache name, so keys are namespaced and cannot collide across method boundaries.","correct":"A key collision happens when two methods share the same cache name and produce identical computed keys, so one method's cached value is silently returned for the other.","distractors":["@Cacheable performs a cache-read: it checks the cache first; if a hit is found, returns the cached value without executing the method.","By default, eviction happens after the method returns successfully.","@CacheConfig at the class level sets default cacheNames, keyGenerator, cacheManager, and cacheResolver for all cache annotations in the class."],"distractorIds":["spring/spring_caching#bc9e884ccf42","spring/spring_caching#e1bede849e48","spring/spring_caching#642165f7e99b"],"concepts":["collision","cacheable","prevent","returned","silently","happens"],"questionMd":"What causes a cache key collision between two different `@Cacheable` methods, and how do you prevent it?","answerFullMd":"A key collision happens when two methods share the same cache name and produce identical computed keys, so one method's cached value is silently returned for the other. For example, `findById(1L)` and `findByVariantId(1L)` both default to key `1` under `SimpleKeyGenerator` when they share the `products` cache — the second call returns the first entity's cached result with no error or warning of any kind. The fix is to always prefix the key with `#root.methodName` (e.g. `key = \"#root.methodName + ':' + #id\"`) or give each method its own cache name, so keys are namespaced and cannot collide across method boundaries.","distractorsMd":["`@Cacheable` performs a cache-read: it checks the cache first; if a hit is found, returns the cached value without executing the method.",null,"`@CacheConfig` at the class level sets default `cacheNames`, `keyGenerator`, `cacheManager`, and `cacheResolver` for all cache annotations in the class."]},{"id":"spring/spring_caching#1f894a8b8314","section":"spring","module":"spring/spring_caching","moduleName":"spring caching","sourceFile":"README.md","difficulty":"advanced","question":"When should you choose cache-aside (@Cacheable) over write-through (@CachePut), and what consistency risk does each carry?","answerFull":"Cache-aside populates the cache lazily on a read miss, while write-through refreshes the cache eagerly on every write, trading staleness risk against wasted work. Cache-aside is cheaper when reads vastly outnumber writes, because only the keys actually requested are ever cached, but data can go stale between a write and the next read until the TTL expires or an explicit @CacheEvict fires. Write-through keeps the cache in lockstep with every write and eliminates that staleness window, but it does unnecessary work for entries nobody reads and does nothing for a cold cache, since nothing is cached until the first write happens. Most read-heavy services default to cache-aside with a short TTL and add @CachePut only on the specific write paths where an immediately consistent read matters.","correct":"Cache-aside populates the cache lazily on a read miss, while write-through refreshes the cache eagerly on every write, trading staleness risk against wasted work.","distractors":["A key collision happens when two methods share the same cache name and produce identical computed keys, so one method's cached value is silently returned for the other.","TTL expiration is passive and time-based, configured once on the CacheManager, while @CacheEvict is an active removal triggered by application code at a specific write.","A cache stampede occurs when many threads simultaneously observe a cache miss and all call the underlying data source concurrently, multiplying load."],"distractorIds":["spring/spring_caching#1c32a9de334a","spring/spring_caching#b9a600e64b55","spring/spring_caching#0569df0b5c56"],"concepts":["cacheput","refreshes","staleness","eagerly","aside","lazily"],"questionMd":"When should you choose cache-aside (`@Cacheable`) over write-through (`@CachePut`), and what consistency risk does each carry?","answerFullMd":"Cache-aside populates the cache lazily on a read miss, while write-through refreshes the cache eagerly on every write, trading staleness risk against wasted work. Cache-aside is cheaper when reads vastly outnumber writes, because only the keys actually requested are ever cached, but data can go stale between a write and the next read until the TTL expires or an explicit `@CacheEvict` fires. Write-through keeps the cache in lockstep with every write and eliminates that staleness window, but it does unnecessary work for entries nobody reads and does nothing for a cold cache, since nothing is cached until the first write happens. Most read-heavy services default to cache-aside with a short TTL and add `@CachePut` only on the specific write paths where an immediately consistent read matters.","distractorsMd":[null,"TTL expiration is passive and time-based, configured once on the `CacheManager`, while `@CacheEvict` is an active removal triggered by application code at a specific write.",null]},{"id":"spring/spring_caching#b9a600e64b55","section":"spring","module":"spring/spring_caching","moduleName":"spring caching","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between letting a Redis entry expire via TTL and explicitly removing it with @CacheEvict?","answerFull":"TTL expiration is passive and time-based, configured once on the CacheManager, while @CacheEvict is an active removal triggered by application code at a specific write. TTL guarantees an upper bound on staleness with zero code changes — if a developer forgets to evict, the entry still disappears once the TTL elapses, which is why every production cache should carry one. @CacheEvict closes the staleness window immediately when the underlying data changes, rather than waiting out the full TTL. Relying on TTL alone means readers can see stale data for up to the entire TTL window after every write; relying on eviction alone means an entry that is never explicitly evicted — say, after a missed code path or an out-of-band bulk DB update — is cached indefinitely with ConcurrentMapCacheManager or until an operator clears it by hand. Production systems should combine both: @CacheEvict on known write paths, TTL as the safety net for everything else.","correct":"TTL expiration is passive and time-based, configured once on the CacheManager, while @CacheEvict is an active removal triggered by application code at a specific write.","distractors":["@CacheConfig at the class level sets default cacheNames, keyGenerator, cacheManager, and cacheResolver for all cache annotations in the class.","Cache-aside populates the cache lazily on a read miss, while write-through refreshes the cache eagerly on every write, trading staleness risk against wasted work.","It imports CachingConfigurationSelector, which registers BeanFactoryCacheOperationSourceAdvisor into the application context."],"distractorIds":["spring/spring_caching#642165f7e99b","spring/spring_caching#1f894a8b8314","spring/spring_caching#ad5d6bcdf719"],"concepts":["expire","cacheevict","cachemanager","removal","passive","expiration"],"questionMd":"What is the difference between letting a Redis entry expire via TTL and explicitly removing it with `@CacheEvict`?","correctMd":"TTL expiration is passive and time-based, configured once on the `CacheManager`, while `@CacheEvict` is an active removal triggered by application code at a specific write.","answerFullMd":"TTL expiration is passive and time-based, configured once on the `CacheManager`, while `@CacheEvict` is an active removal triggered by application code at a specific write. TTL guarantees an upper bound on staleness with zero code changes — if a developer forgets to evict, the entry still disappears once the TTL elapses, which is why every production cache should carry one. `@CacheEvict` closes the staleness window immediately when the underlying data changes, rather than waiting out the full TTL. Relying on TTL alone means readers can see stale data for up to the entire TTL window after every write; relying on eviction alone means an entry that is never explicitly evicted — say, after a missed code path or an out-of-band bulk DB update — is cached indefinitely with `ConcurrentMapCacheManager` or until an operator clears it by hand. Production systems should combine both: `@CacheEvict` on known write paths, TTL as the safety net for everything else.","distractorsMd":["`@CacheConfig` at the class level sets default `cacheNames`, `keyGenerator`, `cacheManager`, and `cacheResolver` for all cache annotations in the class.",null,"It imports `CachingConfigurationSelector`, which registers `BeanFactoryCacheOperationSourceAdvisor` into the application context."]},{"id":"spring/spring_cloud_config#8562ec17642a","section":"spring","module":"spring/spring_cloud_config","moduleName":"spring cloud config","sourceFile":"README.md","difficulty":"core","question":"What is Spring Cloud Config Server and what problem does it solve?","answerFull":"Spring Cloud Config Server is a centralized configuration service that externalizes application configuration from the artifact. It solves configuration drift between environments: instead of each microservice maintaining its own application.yml baked into the Docker image, all services fetch their configuration from a single versioned source (typically a Git repository) at startup. Changes to configuration are auditable, reviewable via PRs, and can be applied at runtime without redeployment using @RefreshScope.","correct":"Spring Cloud Config Server is a centralized configuration service that externalizes application configuration from the artifact.","distractors":["Use @SpringBootTest with spring.config.import=optional:configserver: to make the server optional for tests, then provide test properties via @TestPropertySource or application-test.yml.","If spring.cloud.config.fail-fast=true (recommended), the service throws an exception during startup and does not proceed.","Deploy multiple Config Server instances behind a load balancer."],"distractorIds":["spring/spring_cloud_config#ba0d6a7d64bb","spring/spring_cloud_config#54ab74fd8743","spring/spring_cloud_config#a5f0054b4cf5"],"concepts":["externalizes","solve","centralized","artifact","config","cloud"],"answerFullMd":"Spring Cloud Config Server is a centralized configuration service that externalizes application configuration from the artifact. It solves configuration drift between environments: instead of each microservice maintaining its own `application.yml` baked into the Docker image, all services fetch their configuration from a single versioned source (typically a Git repository) at startup. Changes to configuration are auditable, reviewable via PRs, and can be applied at runtime without redeployment using `@RefreshScope`.","distractorsMd":["Use `@SpringBootTest` with `spring.config.import=optional:configserver:` to make the server optional for tests, then provide test properties via `@TestPropertySource` or `application-test.yml`.","If `spring.cloud.config.fail-fast=true` (recommended), the service throws an exception during startup and does not proceed.",null]},{"id":"spring/spring_cloud_config#54dc587f3674","section":"spring","module":"spring/spring_cloud_config","moduleName":"spring cloud config","sourceFile":"README.md","difficulty":"core","question":"How does a Config Client fetch configuration in Spring Boot 3.x?","answerFull":"In Boot 3.x, the client declares spring.config.import=configserver:http://config-server:8888 in application.yml. During the ApplicationContext startup, Spring's ConfigDataLocationResolver sees the configserver: import, connects to the Config Server, and fetches properties for the current spring.application.name and active profiles. These remote properties are merged into the Environment with higher priority than local application.yml. In Boot 2.x, this was done via a separate bootstrap.yml loaded in a bootstrap context before the main context.","correct":"In Boot 3.x, the client declares spring.config.import=configserver:http://config-server:8888 in application.yml.","distractors":["Spring Cloud Config Server is a centralized configuration service that externalizes application configuration from the artifact.","The Config Server supports symmetric (AES) and asymmetric (RSA) encryption.","The Config Server loads properties by combining three application names in order: the specific application's files, then application-{profile}.{yml|properties}, then application.{yml|properties}."],"distractorIds":["spring/spring_cloud_config#8562ec17642a","spring/spring_cloud_config#397a75d963d1","spring/spring_cloud_config#53962addf2a5"],"concepts":["8888","configserver","declares","yml","fetch","import"],"correctMd":"In Boot 3.x, the client declares `spring.config.import=configserver:http://config-server:8888` in `application.yml`.","answerFullMd":"In Boot 3.x, the client declares `spring.config.import=configserver:http://config-server:8888` in `application.yml`. During the `ApplicationContext` startup, Spring's `ConfigDataLocationResolver` sees the `configserver:` import, connects to the Config Server, and fetches properties for the current `spring.application.name` and active profiles. These remote properties are merged into the `Environment` with higher priority than local `application.yml`. In Boot 2.x, this was done via a separate `bootstrap.yml` loaded in a bootstrap context before the main context.","distractorsMd":[null,null,"The Config Server loads properties by combining three application names in order: the specific application's files, then `application-{profile}.{yml|properties}`, then `application.{yml|properties}`."]},{"id":"spring/spring_cloud_config#9f9b9763cef3","section":"spring","module":"spring/spring_cloud_config","moduleName":"spring cloud config","sourceFile":"README.md","difficulty":"core","question":"What is the difference between bootstrap context and spring.config.import?","answerFull":"Bootstrap context (Boot 2.x) is a parent ApplicationContext that loads before the main context, specifically to resolve configuration from external sources. It requires a separate bootstrap.yml and the spring-cloud-starter-bootstrap dependency. spring.config.import (Boot 3.x) achieves the same goal in a single phase without a separate context: Spring's ConfigData infrastructure processes import declarations during the main context startup. The new approach is simpler (one config file), but the behavior on failure differs — optional:configserver: continues without the remote config, while the required form (default) fails the startup.","correct":"Bootstrap context (Boot 2.x) is a parent ApplicationContext that loads before the main context, specifically to resolve configuration from external sources.","distractors":["In Boot 3.x, the client declares spring.config.import=configserver:http://config-server:8888 in application.yml.","If spring.cloud.config.fail-fast=true (recommended), the service throws an exception during startup and does not proceed.","Use @SpringBootTest with spring.config.import=optional:configserver: to make the server optional for tests, then provide test properties via @TestPropertySource or application-test.yml."],"distractorIds":["spring/spring_cloud_config#54dc587f3674","spring/spring_cloud_config#54ab74fd8743","spring/spring_cloud_config#ba0d6a7d64bb"],"concepts":["resolve","specifically","difference","bootstrap","import","applicationcontext"],"correctMd":"Bootstrap context (Boot 2.x) is a parent `ApplicationContext` that loads before the main context, specifically to resolve configuration from external sources.","answerFullMd":"Bootstrap context (Boot 2.x) is a parent `ApplicationContext` that loads before the main context, specifically to resolve configuration from external sources. It requires a separate `bootstrap.yml` and the `spring-cloud-starter-bootstrap` dependency. `spring.config.import` (Boot 3.x) achieves the same goal in a single phase without a separate context: Spring's `ConfigData` infrastructure processes import declarations during the main context startup. The new approach is simpler (one config file), but the behavior on failure differs — `optional:configserver:` continues without the remote config, while the required form (default) fails the startup.","distractorsMd":["In Boot 3.x, the client declares `spring.config.import=configserver:http://config-server:8888` in `application.yml`.","If `spring.cloud.config.fail-fast=true` (recommended), the service throws an exception during startup and does not proceed.","Use `@SpringBootTest` with `spring.config.import=optional:configserver:` to make the server optional for tests, then provide test properties via `@TestPropertySource` or `application-test.yml`."]},{"id":"spring/spring_cloud_config#aa99fb01f536","section":"spring","module":"spring/spring_cloud_config","moduleName":"spring cloud config","sourceFile":"README.md","difficulty":"core","question":"How does @RefreshScope work and what beans should NOT use it?","answerFull":"@RefreshScope marks a bean as refresh-scoped. Spring wraps it in a proxy. When a refresh event is triggered (via /actuator/refresh or Spring Cloud Bus), the proxy destroys the underlying bean instance and re-creates it on the next method call, re-injecting all @Value fields and @ConfigurationProperties with current Environment values. Beans that should NOT use @RefreshScope include DataSource (re-creating destroys the connection pool and kills active transactions), EntityManagerFactory, PlatformTransactionManager, and any infrastructure beans whose lifecycle is tied to expensive resources. Apply @RefreshScope only to lightweight configuration holders and feature-flag beans.","correct":"@RefreshScope marks a bean as refresh-scoped.","distractors":["When refresh is triggered, @RefreshScope destroys the underlying bean instance and marks it for re-creation on next access.","Bootstrap context (Boot 2.x) is a parent ApplicationContext that loads before the main context, specifically to resolve configuration from external sources.","Deploy multiple Config Server instances behind a load balancer."],"distractorIds":["spring/spring_cloud_config#223487beb520","spring/spring_cloud_config#9f9b9763cef3","spring/spring_cloud_config#a5f0054b4cf5"],"concepts":["refreshscope","refresh","scoped","marks","beans","work"],"correctMd":"`@RefreshScope` marks a bean as refresh-scoped.","answerFullMd":"`@RefreshScope` marks a bean as refresh-scoped. Spring wraps it in a proxy. When a refresh event is triggered (via `/actuator/refresh` or Spring Cloud Bus), the proxy destroys the underlying bean instance and re-creates it on the next method call, re-injecting all `@Value` fields and `@ConfigurationProperties` with current Environment values. Beans that should NOT use `@RefreshScope` include `DataSource` (re-creating destroys the connection pool and kills active transactions), `EntityManagerFactory`, `PlatformTransactionManager`, and any infrastructure beans whose lifecycle is tied to expensive resources. Apply `@RefreshScope` only to lightweight configuration holders and feature-flag beans.","distractorsMd":["When refresh is triggered, `@RefreshScope` destroys the underlying bean instance and marks it for re-creation on next access.","Bootstrap context (Boot 2.x) is a parent `ApplicationContext` that loads before the main context, specifically to resolve configuration from external sources.",null]},{"id":"spring/spring_cloud_config#b3592e38d009","section":"spring","module":"spring/spring_cloud_config","moduleName":"spring cloud config","sourceFile":"README.md","difficulty":"core","question":"How does Spring Cloud Bus work to broadcast configuration refresh?","answerFull":"Spring Cloud Bus connects all service instances to a shared message broker topic (Kafka or RabbitMQ). When /actuator/busrefresh is called on any instance, that instance publishes a RefreshRemoteApplicationEvent to the bus topic. Every instance subscribed to the topic receives the event, calls its local ContextRefresher, and re-creates all @RefreshScope beans. This avoids calling /actuator/refresh on every pod individually — a single HTTP call triggers a coordinated refresh across the entire cluster. The destination can be scoped: /actuator/busrefresh/order-service: only refreshes order-service instances.","correct":"Spring Cloud Bus connects all service instances to a shared message broker topic (Kafka or RabbitMQ).","distractors":["Spring Cloud Config Server is a centralized configuration service that externalizes application configuration from the artifact.","When refresh is triggered, @RefreshScope destroys the underlying bean instance and marks it for re-creation on next access.","The native profile (spring.profiles.active=native) serves configuration from the local file system or classpath rather than a Git repository."],"distractorIds":["spring/spring_cloud_config#8562ec17642a","spring/spring_cloud_config#223487beb520","spring/spring_cloud_config#453a1bcc12b2"],"concepts":["rabbitmq","bus","broadcast","connects","refresh","topic"],"answerFullMd":"Spring Cloud Bus connects all service instances to a shared message broker topic (Kafka or RabbitMQ). When `/actuator/busrefresh` is called on any instance, that instance publishes a `RefreshRemoteApplicationEvent` to the bus topic. Every instance subscribed to the topic receives the event, calls its local `ContextRefresher`, and re-creates all `@RefreshScope` beans. This avoids calling `/actuator/refresh` on every pod individually — a single HTTP call triggers a coordinated refresh across the entire cluster. The destination can be scoped: `/actuator/busrefresh/order-service:**` only refreshes `order-service` instances.","distractorsMd":[null,"When refresh is triggered, `@RefreshScope` destroys the underlying bean instance and marks it for re-creation on next access.","The native profile (`spring.profiles.active=native`) serves configuration from the local file system or classpath rather than a Git repository."]},{"id":"spring/spring_cloud_config#397a75d963d1","section":"spring","module":"spring/spring_cloud_config","moduleName":"spring cloud config","sourceFile":"README.md","difficulty":"intermediate","question":"How do you encrypt sensitive properties in the Config Server?","answerFull":"The Config Server supports symmetric (AES) and asymmetric (RSA) encryption. Configure encrypt.key for symmetric or a JKS keystore for asymmetric. Call POST /encrypt with the plaintext value; the server returns the ciphertext. Store it in the config repo as '{cipher}<ciphertext>'. When a Config Client requests properties, the server (or client, depending on configuration) decrypts {cipher} prefixed values before delivery. The private key never leaves the Config Server. For production, RSA asymmetric encryption is preferred: the public key can be distributed to Config Clients for client-side decryption.","correct":"The Config Server supports symmetric (AES) and asymmetric (RSA) encryption.","distractors":["When the Vault backend is configured, Config Server connects to Vault using an authentication token (or AppRole, Kubernetes auth, etc.) and fetches secrets from the configured paths.","Deploy multiple Config Server instances behind a load balancer.","If spring.cloud.config.fail-fast=true (recommended), the service throws an exception during startup and does not proceed."],"distractorIds":["spring/spring_cloud_config#c307cccb30f2","spring/spring_cloud_config#a5f0054b4cf5","spring/spring_cloud_config#54ab74fd8743"],"concepts":["encrypt","asymmetric","rsa","aes","symmetric","encryption"],"answerFullMd":"The Config Server supports symmetric (AES) and asymmetric (RSA) encryption. Configure `encrypt.key` for symmetric or a JKS keystore for asymmetric. Call `POST /encrypt` with the plaintext value; the server returns the ciphertext. Store it in the config repo as `'{cipher}<ciphertext>'`. When a Config Client requests properties, the server (or client, depending on configuration) decrypts `{cipher}` prefixed values before delivery. The private key never leaves the Config Server. For production, RSA asymmetric encryption is preferred: the public key can be distributed to Config Clients for client-side decryption.","distractorsMd":[null,null,"If `spring.cloud.config.fail-fast=true` (recommended), the service throws an exception during startup and does not proceed."]},{"id":"spring/spring_cloud_config#c307cccb30f2","section":"spring","module":"spring/spring_cloud_config","moduleName":"spring cloud config","sourceFile":"README.md","difficulty":"intermediate","question":"How does Vault integration work with Config Server for dynamic secrets?","answerFull":"When the Vault backend is configured, Config Server connects to Vault using an authentication token (or AppRole, Kubernetes auth, etc.) and fetches secrets from the configured paths. For dynamic secrets (e.g., database credentials via Vault's database secret engine), Vault issues short-lived credentials with a lease TTL. Config Server holds a lease and renews it before expiry. When credentials are rotated (lease expires and a new lease is created), the Config Server can trigger a refresh event to push new credentials to clients. This creates a fully automated password rotation pipeline without any manual steps.","correct":"When the Vault backend is configured, Config Server connects to Vault using an authentication token (or AppRole, Kubernetes auth, etc.) and fetches secrets from the configured paths.","distractors":["In Boot 3.x, the client declares spring.config.import=configserver:http://config-server:8888 in application.yml.","The Config Server supports symmetric (AES) and asymmetric (RSA) encryption.","Deploy multiple Config Server instances behind a load balancer."],"distractorIds":["spring/spring_cloud_config#54dc587f3674","spring/spring_cloud_config#397a75d963d1","spring/spring_cloud_config#a5f0054b4cf5"],"concepts":["approle","vault","integration","connects","auth","fetches"],"distractorsMd":["In Boot 3.x, the client declares `spring.config.import=configserver:http://config-server:8888` in `application.yml`.",null,null]},{"id":"spring/spring_cloud_config#54ab74fd8743","section":"spring","module":"spring/spring_cloud_config","moduleName":"spring cloud config","sourceFile":"README.md","difficulty":"intermediate","question":"What happens if the Config Server is unavailable when a microservice starts?","answerFull":"If spring.cloud.config.fail-fast=true (recommended), the service throws an exception during startup and does not proceed. Combined with retry configuration (spring.cloud.config.retry.*), the client retries with exponential backoff for a configurable number of attempts before failing. If fail-fast=false (or optional:configserver: import), the service starts with local defaults or no externalized config — which can lead to running in a misconfigured state silently. In production, fail-fast=true with retry is the correct setting because it is better to fail loudly than to run silently misconfigured.","correct":"If spring.cloud.config.fail-fast=true (recommended), the service throws an exception during startup and does not proceed.","distractors":["Use @SpringBootTest with spring.config.import=optional:configserver: to make the server optional for tests, then provide test properties via @TestPropertySource or application-test.yml.","The Config Server supports symmetric (AES) and asymmetric (RSA) encryption.","In Boot 3.x, the client declares spring.config.import=configserver:http://config-server:8888 in application.yml."],"distractorIds":["spring/spring_cloud_config#ba0d6a7d64bb","spring/spring_cloud_config#397a75d963d1","spring/spring_cloud_config#54dc587f3674"],"concepts":["microservice","recommended","proceed","unavailable","happens","fail"],"correctMd":"If `spring.cloud.config.fail-fast=true` (recommended), the service throws an exception during startup and does not proceed.","answerFullMd":"If `spring.cloud.config.fail-fast=true` (recommended), the service throws an exception during startup and does not proceed. Combined with retry configuration (`spring.cloud.config.retry.*`), the client retries with exponential backoff for a configurable number of attempts before failing. If `fail-fast=false` (or `optional:configserver:` import), the service starts with local defaults or no externalized config — which can lead to running in a misconfigured state silently. In production, `fail-fast=true` with retry is the correct setting because it is better to fail loudly than to run silently misconfigured.","distractorsMd":["Use `@SpringBootTest` with `spring.config.import=optional:configserver:` to make the server optional for tests, then provide test properties via `@TestPropertySource` or `application-test.yml`.",null,"In Boot 3.x, the client declares `spring.config.import=configserver:http://config-server:8888` in `application.yml`."]},{"id":"spring/spring_cloud_config#a5f0054b4cf5","section":"spring","module":"spring/spring_cloud_config","moduleName":"spring cloud config","sourceFile":"README.md","difficulty":"intermediate","question":"How would you make the Config Server itself highly available?","answerFull":"Deploy multiple Config Server instances behind a load balancer. Each instance independently clones the Git repository to a local working copy (configure a unique basedir per instance or use a shared filesystem). Config Server is stateless with respect to client connections — any instance can serve any client. For Git backend, the main concern is Git clone/pull latency; configure clone-on-start: true and an appropriate timeout. Register Config Server with a service registry (Eureka) so clients can discover it via load-balanced URL (http://config-server/) rather than a hardcoded IP.","correct":"Deploy multiple Config Server instances behind a load balancer.","distractors":["Spring Cloud Config Server is a centralized configuration service that externalizes application configuration from the artifact.","The Config Server supports symmetric (AES) and asymmetric (RSA) encryption.","Use @SpringBootTest with spring.config.import=optional:configserver: to make the server optional for tests, then provide test properties via @TestPropertySource or application-test.yml."],"distractorIds":["spring/spring_cloud_config#8562ec17642a","spring/spring_cloud_config#397a75d963d1","spring/spring_cloud_config#ba0d6a7d64bb"],"concepts":["highly","balancer","deploy","behind","make","config"],"answerFullMd":"Deploy multiple Config Server instances behind a load balancer. Each instance independently clones the Git repository to a local working copy (configure a unique `basedir` per instance or use a shared filesystem). Config Server is stateless with respect to client connections — any instance can serve any client. For Git backend, the main concern is Git clone/pull latency; configure `clone-on-start: true` and an appropriate timeout. Register Config Server with a service registry (Eureka) so clients can discover it via load-balanced URL (`http://config-server/`) rather than a hardcoded IP.","distractorsMd":[null,null,"Use `@SpringBootTest` with `spring.config.import=optional:configserver:` to make the server optional for tests, then provide test properties via `@TestPropertySource` or `application-test.yml`."]},{"id":"spring/spring_cloud_config#fe399bb84329","section":"spring","module":"spring/spring_cloud_config","moduleName":"spring cloud config","sourceFile":"README.md","difficulty":"advanced","question":"What is the spring.cloud.config.label property and how does it map to Git branches/tags?","answerFull":"spring.cloud.config.label specifies the Git branch, tag, or commit hash that the Config Server uses when cloning the repository. By default it is the backend's default branch (usually main). In a multi-environment setup, point different profiles to different branches: dev → develop, prod → main, a hotfix to a release tag. Clients set spring.cloud.config.label=main or the value can be embedded in the server configuration as spring.cloud.config.server.git.default-label=main. Using labels for environment isolation avoids the need for separate Config Server instances per environment — one server, multiple branches.","correct":"spring.cloud.config.label specifies the Git branch, tag, or commit hash that the Config Server uses when cloning the repository.","distractors":["Spring Cloud Config Server is a centralized configuration service that externalizes application configuration from the artifact.","Use @SpringBootTest with spring.config.import=optional:configserver: to make the server optional for tests, then provide test properties via @TestPropertySource or application-test.yml.","The Config Server loads properties by combining three application names in order: the specific application's files, then application-{profile}.{yml|properties}, then application.{yml|properties}."],"distractorIds":["spring/spring_cloud_config#8562ec17642a","spring/spring_cloud_config#ba0d6a7d64bb","spring/spring_cloud_config#53962addf2a5"],"concepts":["cloning","branches","repository","specifies","branch","property"],"questionMd":"What is the `spring.cloud.config.label` property and how does it map to Git branches/tags?","correctMd":"`spring.cloud.config.label` specifies the Git branch, tag, or commit hash that the Config Server uses when cloning the repository.","answerFullMd":"`spring.cloud.config.label` specifies the Git branch, tag, or commit hash that the Config Server uses when cloning the repository. By default it is the backend's default branch (usually `main`). In a multi-environment setup, point different profiles to different branches: `dev` → `develop`, `prod` → `main`, a hotfix to a release tag. Clients set `spring.cloud.config.label=main` or the value can be embedded in the server configuration as `spring.cloud.config.server.git.default-label=main`. Using labels for environment isolation avoids the need for separate Config Server instances per environment — one server, multiple branches.","distractorsMd":[null,"Use `@SpringBootTest` with `spring.config.import=optional:configserver:` to make the server optional for tests, then provide test properties via `@TestPropertySource` or `application-test.yml`.","The Config Server loads properties by combining three application names in order: the specific application's files, then `application-{profile}.{yml|properties}`, then `application.{yml|properties}`."]},{"id":"spring/spring_cloud_config#223487beb520","section":"spring","module":"spring/spring_cloud_config","moduleName":"spring cloud config","sourceFile":"README.md","difficulty":"advanced","question":"What are the risks of using @RefreshScope on beans that hold expensive resources, and how do you avoid in-production outages?","answerFull":"When refresh is triggered, @RefreshScope destroys the underlying bean instance and marks it for re-creation on next access. For DataSource, this means: (1) all current HikariCP connections are destroyed mid-flight, causing Connection is closed for in-flight requests; (2) the new DataSource creates a fresh pool, causing a connection-acquisition burst to the database; (3) any thread holding an open Connection gets Connection is closed and throws. Avoid by never applying @RefreshScope to DataSource, EntityManagerFactory, PlatformTransactionManager, or Kafka consumers. For database credential rotation, use Vault dynamic secrets with credential renewal rather than Config Server refresh. Apply @RefreshScope only to thin configuration-holder beans (feature flags, rate-limit thresholds, external URL strings) where re-creation is instantaneous and safe.","correct":"When refresh is triggered, @RefreshScope destroys the underlying bean instance and marks it for re-creation on next access.","distractors":["@RefreshScope marks a bean as refresh-scoped.","Bootstrap context (Boot 2.x) is a parent ApplicationContext that loads before the main context, specifically to resolve configuration from external sources.","Deploy multiple Config Server instances behind a load balancer."],"distractorIds":["spring/spring_cloud_config#aa99fb01f536","spring/spring_cloud_config#9f9b9763cef3","spring/spring_cloud_config#a5f0054b4cf5"],"concepts":["refreshscope","destroys","risks","creation","triggered","refresh"],"questionMd":"What are the risks of using `@RefreshScope` on beans that hold expensive resources, and how do you avoid in-production outages?","correctMd":"When refresh is triggered, `@RefreshScope` destroys the underlying bean instance and marks it for re-creation on next access.","answerFullMd":"When refresh is triggered, `@RefreshScope` destroys the underlying bean instance and marks it for re-creation on next access. For `DataSource`, this means: (1) all current HikariCP connections are destroyed mid-flight, causing `Connection is closed` for in-flight requests; (2) the new `DataSource` creates a fresh pool, causing a connection-acquisition burst to the database; (3) any thread holding an open `Connection` gets `Connection is closed` and throws. Avoid by never applying `@RefreshScope` to `DataSource`, `EntityManagerFactory`, `PlatformTransactionManager`, or Kafka consumers. For database credential rotation, use Vault dynamic secrets with credential renewal rather than Config Server refresh. Apply `@RefreshScope` only to thin configuration-holder beans (feature flags, rate-limit thresholds, external URL strings) where re-creation is instantaneous and safe.","distractorsMd":["`@RefreshScope` marks a bean as refresh-scoped.","Bootstrap context (Boot 2.x) is a parent `ApplicationContext` that loads before the main context, specifically to resolve configuration from external sources.",null]},{"id":"spring/spring_cloud_config#ba0d6a7d64bb","section":"spring","module":"spring/spring_cloud_config","moduleName":"spring cloud config","sourceFile":"README.md","difficulty":"advanced","question":"How do you test configuration loading order and property overriding in Config Client integration tests?","answerFull":"Use @SpringBootTest with spring.config.import=optional:configserver: to make the server optional for tests, then provide test properties via @TestPropertySource or application-test.yml. To test against a real Config Server in CI: use @SpringBootTest with Testcontainers to spin up a Config Server container, or use Wiremock to mock the Config Server's HTTP endpoints and return controlled JSON responses. For unit testing @ConfigurationProperties classes independently of the server: use ApplicationContextRunner.withPropertyValues(\"prefix.key=value\") to test binding without any HTTP call. Verify @RefreshScope behaviour with ContextRefresher.refresh() called programmatically in the test.","correct":"Use @SpringBootTest with spring.config.import=optional:configserver: to make the server optional for tests, then provide test properties via @TestPropertySource or application-test.yml.","distractors":["In Boot 3.x, the client declares spring.config.import=configserver:http://config-server:8888 in application.yml.","The Config Server loads properties by combining three application names in order: the specific application's files, then application-{profile}.{yml|properties}, then application.{yml|properties}.","spring.cloud.config.label specifies the Git branch, tag, or commit hash that the Config Server uses when cloning the repository."],"distractorIds":["spring/spring_cloud_config#54dc587f3674","spring/spring_cloud_config#53962addf2a5","spring/spring_cloud_config#fe399bb84329"],"concepts":["testpropertysource","configserver","overriding","yml","integration","springboottest"],"correctMd":"Use `@SpringBootTest` with `spring.config.import=optional:configserver:` to make the server optional for tests, then provide test properties via `@TestPropertySource` or `application-test.yml`.","answerFullMd":"Use `@SpringBootTest` with `spring.config.import=optional:configserver:` to make the server optional for tests, then provide test properties via `@TestPropertySource` or `application-test.yml`. To test against a real Config Server in CI: use `@SpringBootTest` with Testcontainers to spin up a Config Server container, or use Wiremock to mock the Config Server's HTTP endpoints and return controlled JSON responses. For unit testing `@ConfigurationProperties` classes independently of the server: use `ApplicationContextRunner.withPropertyValues(\"prefix.key=value\")` to test binding without any HTTP call. Verify `@RefreshScope` behaviour with `ContextRefresher.refresh()` called programmatically in the test.","distractorsMd":["In Boot 3.x, the client declares `spring.config.import=configserver:http://config-server:8888` in `application.yml`.","The Config Server loads properties by combining three application names in order: the specific application's files, then `application-{profile}.{yml|properties}`, then `application.{yml|properties}`.","`spring.cloud.config.label` specifies the Git branch, tag, or commit hash that the Config Server uses when cloning the repository."]},{"id":"spring/spring_cloud_config#453a1bcc12b2","section":"spring","module":"spring/spring_cloud_config","moduleName":"spring cloud config","sourceFile":"README.md","difficulty":"advanced","question":"What is the native profile in Config Server and how does it differ from the Git backend?","answerFull":"The native profile (spring.profiles.active=native) serves configuration from the local file system or classpath rather than a Git repository. Files are searched in search-locations (default: classpath:/, classpath:/config/, file:./, file:./config/). This is primarily for: (1) Local development — no need for a running Git server. (2) Kubernetes ConfigMap integration — mount a ConfigMap as a file at a known path, point Config Server's search-locations to that path, and the native profile serves those YAML files. Each file follows the naming convention {application}-{profile}.yml. The native backend does not support encryption, history, or audit trail — use Git for production.","correct":"The native profile (spring.profiles.active=native) serves configuration from the local file system or classpath rather than a Git repository.","distractors":["Deploy multiple Config Server instances behind a load balancer.","If spring.cloud.config.fail-fast=true (recommended), the service throws an exception during startup and does not proceed.","spring.cloud.config.label specifies the Git branch, tag, or commit hash that the Config Server uses when cloning the repository."],"distractorIds":["spring/spring_cloud_config#a5f0054b4cf5","spring/spring_cloud_config#54ab74fd8743","spring/spring_cloud_config#fe399bb84329"],"concepts":["profiles","serves","classpath","repository","differ","backend"],"correctMd":"The native profile (`spring.profiles.active=native`) serves configuration from the local file system or classpath rather than a Git repository.","answerFullMd":"The native profile (`spring.profiles.active=native`) serves configuration from the local file system or classpath rather than a Git repository. Files are searched in `search-locations` (default: `classpath:/`, `classpath:/config/`, `file:./`, `file:./config/`). This is primarily for: (1) **Local development** — no need for a running Git server. (2) **Kubernetes ConfigMap integration** — mount a `ConfigMap` as a file at a known path, point Config Server's `search-locations` to that path, and the native profile serves those YAML files. Each file follows the naming convention `{application}-{profile}.yml`. The native backend does not support encryption, history, or audit trail — use Git for production.","distractorsMd":[null,"If `spring.cloud.config.fail-fast=true` (recommended), the service throws an exception during startup and does not proceed.","`spring.cloud.config.label` specifies the Git branch, tag, or commit hash that the Config Server uses when cloning the repository."]},{"id":"spring/spring_cloud_config#53962addf2a5","section":"spring","module":"spring/spring_cloud_config","moduleName":"spring cloud config","sourceFile":"README.md","difficulty":"advanced","question":"How does Spring Cloud Config handle configuration for multiple applications sharing a common base?","answerFull":"The Config Server loads properties by combining three application names in order: the specific application's files, then application-{profile}.{yml|properties}, then application.{yml|properties}. Files named application.* (without a specific app name) are shared defaults loaded for all clients. A service named order-service requesting the prod profile gets: application.yml (shared base) → application-prod.yml (shared prod override) → order-service.yml (service-specific base) → order-service-prod.yml (service-specific prod override). Later files in the sequence override earlier ones. Use this layering to DRY configuration: common DB pool sizes in application.yml, environment-specific database URLs in application-{env}.yml, service-specific tuning in {service}.yml.","correct":"The Config Server loads properties by combining three application names in order: the specific application's files, then application-{profile}.{yml|properties}, then application.{yml|properties}.","distractors":["In Boot 3.x, the client declares spring.config.import=configserver:http://config-server:8888 in application.yml.","The native profile (spring.profiles.active=native) serves configuration from the local file system or classpath rather than a Git repository.","spring.cloud.config.label specifies the Git branch, tag, or commit hash that the Config Server uses when cloning the repository."],"distractorIds":["spring/spring_cloud_config#54dc587f3674","spring/spring_cloud_config#453a1bcc12b2","spring/spring_cloud_config#fe399bb84329"],"concepts":["combining","yml","applications","handle","sharing","profile"],"correctMd":"The Config Server loads properties by combining three application names in order: the specific application's files, then `application-{profile}.{yml|properties}`, then `application.{yml|properties}`.","answerFullMd":"The Config Server loads properties by combining three application names in order: the specific application's files, then `application-{profile}.{yml|properties}`, then `application.{yml|properties}`. Files named `application.*` (without a specific app name) are shared defaults loaded for all clients. A service named `order-service` requesting the `prod` profile gets: `application.yml` (shared base) → `application-prod.yml` (shared prod override) → `order-service.yml` (service-specific base) → `order-service-prod.yml` (service-specific prod override). Later files in the sequence override earlier ones. Use this layering to DRY configuration: common DB pool sizes in `application.yml`, environment-specific database URLs in `application-{env}.yml`, service-specific tuning in `{service}.yml`.","distractorsMd":["In Boot 3.x, the client declares `spring.config.import=configserver:http://config-server:8888` in `application.yml`.","The native profile (`spring.profiles.active=native`) serves configuration from the local file system or classpath rather than a Git repository.","`spring.cloud.config.label` specifies the Git branch, tag, or commit hash that the Config Server uses when cloning the repository."]},{"id":"spring/spring_cloud_patterns#5aa2bd5949f6","section":"spring","module":"spring/spring_cloud_patterns","moduleName":"spring cloud patterns","sourceFile":"README.md","difficulty":"core","question":"What is the role of Spring Cloud Gateway and how does it differ from a traditional reverse proxy like Nginx?","answerFull":"Spring Cloud Gateway is a programmatic, reactive API gateway built on Spring WebFlux and Project Reactor. Unlike Nginx, which is configured with static declarative rules, Spring Cloud Gateway executes Java code in filters and predicates, enabling dynamic routing, deep Spring Security integration, service-discovery-aware load balancing via lb:// URIs, and full access to the Spring application context. The tradeoff is performance: Nginx has lower latency (C vs JVM) for simple proxying. Gateway is preferred when routing logic requires custom Java code or deep Spring ecosystem integration.","correct":"Spring Cloud Gateway is a programmatic, reactive API gateway built on Spring WebFlux and Project Reactor.","distractors":["Set spring.cloud.openfeign.circuitbreaker.enabled=true.","A GlobalFilter applies to all routes defined in the gateway.","Use @WebFluxTest with WebTestClient and mock the downstream service using WireMock (via spring-cloud-contract-wiremock) or a mock bean."],"distractorIds":["spring/spring_cloud_patterns#7a81141c79cb","spring/spring_cloud_patterns#54c5338f7f9d","spring/spring_cloud_patterns#0f3914644bd8"],"concepts":["nginx","programmatic","webflux","reactor","reverse","differ"],"answerFullMd":"Spring Cloud Gateway is a programmatic, reactive API gateway built on Spring WebFlux and Project Reactor. Unlike Nginx, which is configured with static declarative rules, Spring Cloud Gateway executes Java code in filters and predicates, enabling dynamic routing, deep Spring Security integration, service-discovery-aware load balancing via `lb://` URIs, and full access to the Spring application context. The tradeoff is performance: Nginx has lower latency (C vs JVM) for simple proxying. Gateway is preferred when routing logic requires custom Java code or deep Spring ecosystem integration.","distractorsMd":["Set `spring.cloud.openfeign.circuitbreaker.enabled=true`.","A `GlobalFilter` applies to all routes defined in the gateway.","Use `@WebFluxTest` with `WebTestClient` and mock the downstream service using `WireMock` (via `spring-cloud-contract-wiremock`) or a mock bean."]},{"id":"spring/spring_cloud_patterns#54c5338f7f9d","section":"spring","module":"spring/spring_cloud_patterns","moduleName":"spring cloud patterns","sourceFile":"README.md","difficulty":"core","question":"Explain the difference between GlobalFilter and GatewayFilter in Spring Cloud Gateway.","answerFull":"A GlobalFilter applies to all routes defined in the gateway. It is implemented as a Spring bean and executes for every matched request. Common uses: authentication, correlation ID injection, request/response logging. A GatewayFilter applies only to specific routes where it is declared in the route configuration. Built-in GatewayFilter implementations (RewritePath, AddRequestHeader, CircuitBreaker, RequestRateLimiter) are configured per-route in application.yml or Java route definitions. Custom GatewayFilter implementations can be reused across routes by referencing them by name in the filter configuration.","correct":"A GlobalFilter applies to all routes defined in the gateway.","distractors":["Spring Cloud LoadBalancer is the official replacement for Netflix Ribbon (removed in Spring Cloud 2020.0).","Use @WebFluxTest with WebTestClient and mock the downstream service using WireMock (via spring-cloud-contract-wiremock) or a mock bean.","Set spring.cloud.openfeign.circuitbreaker.enabled=true."],"distractorIds":["spring/spring_cloud_patterns#fe0637e6bd23","spring/spring_cloud_patterns#0f3914644bd8","spring/spring_cloud_patterns#7a81141c79cb"],"concepts":["globalfilter","explain","difference","routes","gateway","defined"],"correctMd":"A `GlobalFilter` applies to all routes defined in the gateway.","answerFullMd":"A `GlobalFilter` applies to all routes defined in the gateway. It is implemented as a Spring bean and executes for every matched request. Common uses: authentication, correlation ID injection, request/response logging. A `GatewayFilter` applies only to specific routes where it is declared in the route configuration. Built-in `GatewayFilter` implementations (RewritePath, AddRequestHeader, CircuitBreaker, RequestRateLimiter) are configured per-route in `application.yml` or Java route definitions. Custom `GatewayFilter` implementations can be reused across routes by referencing them by name in the filter configuration.","distractorsMd":[null,"Use `@WebFluxTest` with `WebTestClient` and mock the downstream service using `WireMock` (via `spring-cloud-contract-wiremock`) or a mock bean.","Set `spring.cloud.openfeign.circuitbreaker.enabled=true`."]},{"id":"spring/spring_cloud_patterns#69df3de20d37","section":"spring","module":"spring/spring_cloud_patterns","moduleName":"spring cloud patterns","sourceFile":"README.md","difficulty":"core","question":"Describe the circuit breaker states and the role of the HALF_OPEN state.","answerFull":"A Resilience4j circuit breaker has three states. CLOSED is the normal operating state: all calls proceed and results are measured against the sliding window. OPEN means the failure (or slow-call) rate has crossed the configured threshold: all calls fail immediately without reaching the downstream service, and the configured fallback is returned. After waitDurationInOpenState elapses, the circuit automatically transitions to HALF_OPEN. In HALF_OPEN, a limited number of test calls (permittedNumberOfCallsInHalfOpenState, e.g., 5) are allowed through. If the failure rate among test calls is below the threshold, the circuit closes; otherwise it returns to OPEN. HALF_OPEN implements the probe-and-recover pattern: it avoids thundering herd on a recovering service by limiting test traffic.","correct":"A Resilience4j circuit breaker has three states.","distractors":["In addition to failure rate thresholds, Resilience4j allows configuring a slowCallRateThreshold and slowCallDurationThreshold.","A Bulkhead limits the number of concurrent calls to a dependency.","A GlobalFilter applies to all routes defined in the gateway."],"distractorIds":["spring/spring_cloud_patterns#bb2580c42770","spring/spring_cloud_patterns#29fe369c209a","spring/spring_cloud_patterns#54c5338f7f9d"],"concepts":["describe","resilience4j","breaker","half","circuit","role"],"answerFullMd":"A Resilience4j circuit breaker has three states. CLOSED is the normal operating state: all calls proceed and results are measured against the sliding window. OPEN means the failure (or slow-call) rate has crossed the configured threshold: all calls fail immediately without reaching the downstream service, and the configured fallback is returned. After `waitDurationInOpenState` elapses, the circuit automatically transitions to HALF_OPEN. In HALF_OPEN, a limited number of test calls (`permittedNumberOfCallsInHalfOpenState`, e.g., 5) are allowed through. If the failure rate among test calls is below the threshold, the circuit closes; otherwise it returns to OPEN. HALF_OPEN implements the probe-and-recover pattern: it avoids thundering herd on a recovering service by limiting test traffic.","distractorsMd":["In addition to failure rate thresholds, Resilience4j allows configuring a `slowCallRateThreshold` and `slowCallDurationThreshold`.",null,"A `GlobalFilter` applies to all routes defined in the gateway."]},{"id":"spring/spring_cloud_patterns#fe0637e6bd23","section":"spring","module":"spring/spring_cloud_patterns","moduleName":"spring cloud patterns","sourceFile":"README.md","difficulty":"core","question":"How does Spring Cloud LoadBalancer work and what replaced Netflix Ribbon?","answerFull":"Spring Cloud LoadBalancer is the official replacement for Netflix Ribbon (removed in Spring Cloud 2020.0). When a RestClient, WebClient, or Feign client uses a lb://service-name URI, Spring Cloud LoadBalancer intercepts the request, queries the local service instance cache (populated from Eureka, Consul, or Kubernetes), selects an instance using the configured strategy (default: RoundRobin), and substitutes the real host/port. The instance list is cached locally and refreshed from the registry at a configurable interval (default 35 seconds). Custom strategies (zone-aware, response-time-weighted) are implemented by providing a custom ReactorLoadBalancer<ServiceInstance> bean.","correct":"Spring Cloud LoadBalancer is the official replacement for Netflix Ribbon (removed in Spring Cloud 2020.0).","distractors":["The built-in RequestRateLimiter filter uses a token bucket algorithm backed by Redis (via spring-boot-starter-data-redis-reactive).","Use @WebFluxTest with WebTestClient and mock the downstream service using WireMock (via spring-cloud-contract-wiremock) or a mock bean.","Spring Cloud Sleuth (Spring Cloud 2020.x and earlier) provided distributed tracing by auto-configuring Brave (B3 propagation) and automatically injecting trace and span IDs into log MDC and HTTP headers."],"distractorIds":["spring/spring_cloud_patterns#807473bafa9f","spring/spring_cloud_patterns#0f3914644bd8","spring/spring_cloud_patterns#3a044ad4ab62"],"concepts":["netflix","ribbon","2020","loadbalancer","official","replaced"],"answerFullMd":"Spring Cloud LoadBalancer is the official replacement for Netflix Ribbon (removed in Spring Cloud 2020.0). When a `RestClient`, `WebClient`, or Feign client uses a `lb://service-name` URI, Spring Cloud LoadBalancer intercepts the request, queries the local service instance cache (populated from Eureka, Consul, or Kubernetes), selects an instance using the configured strategy (default: RoundRobin), and substitutes the real host/port. The instance list is cached locally and refreshed from the registry at a configurable interval (default 35 seconds). Custom strategies (zone-aware, response-time-weighted) are implemented by providing a custom `ReactorLoadBalancer<ServiceInstance>` bean.","distractorsMd":["The built-in `RequestRateLimiter` filter uses a token bucket algorithm backed by Redis (via `spring-boot-starter-data-redis-reactive`).","Use `@WebFluxTest` with `WebTestClient` and mock the downstream service using `WireMock` (via `spring-cloud-contract-wiremock`) or a mock bean.",null]},{"id":"spring/spring_cloud_patterns#7a81141c79cb","section":"spring","module":"spring/spring_cloud_patterns","moduleName":"spring cloud patterns","sourceFile":"README.md","difficulty":"core","question":"How do you configure OpenFeign to integrate with Resilience4j circuit breaker?","answerFull":"Set spring.cloud.openfeign.circuitbreaker.enabled=true. This wraps every @FeignClient method with a Resilience4j circuit breaker named by the pattern <FeignClientName>#<methodName>(<paramTypes>). Override the default name by adding @CircuitBreaker(name = \"customName\") on the interface method. Provide a fallback by setting fallback = FallbackClass.class or fallbackFactory = FallbackFactory.class on the @FeignClient annotation. The fallback class must implement the Feign interface and be registered as a Spring bean. Timeouts are configured separately via feign.client.config.<clientName>.connect-timeout and read-timeout.","correct":"Set spring.cloud.openfeign.circuitbreaker.enabled=true.","distractors":["Spring Cloud Sleuth (Spring Cloud 2020.x and earlier) provided distributed tracing by auto-configuring Brave (B3 propagation) and automatically injecting trace and span IDs into log MDC and HTTP headers.","Spring Cloud Gateway is a programmatic, reactive API gateway built on Spring WebFlux and Project Reactor.","In addition to failure rate thresholds, Resilience4j allows configuring a slowCallRateThreshold and slowCallDurationThreshold."],"distractorIds":["spring/spring_cloud_patterns#3a044ad4ab62","spring/spring_cloud_patterns#5aa2bd5949f6","spring/spring_cloud_patterns#bb2580c42770"],"concepts":["integrate","openfeign","circuitbreaker","resilience4j","breaker","configure"],"correctMd":"Set `spring.cloud.openfeign.circuitbreaker.enabled=true`.","answerFullMd":"Set `spring.cloud.openfeign.circuitbreaker.enabled=true`. This wraps every `@FeignClient` method with a Resilience4j circuit breaker named by the pattern `<FeignClientName>#<methodName>(<paramTypes>)`. Override the default name by adding `@CircuitBreaker(name = \"customName\")` on the interface method. Provide a fallback by setting `fallback = FallbackClass.class` or `fallbackFactory = FallbackFactory.class` on the `@FeignClient` annotation. The fallback class must implement the Feign interface and be registered as a Spring bean. Timeouts are configured separately via `feign.client.config.<clientName>.connect-timeout` and `read-timeout`.","distractorsMd":[null,null,"In addition to failure rate thresholds, Resilience4j allows configuring a `slowCallRateThreshold` and `slowCallDurationThreshold`."]},{"id":"spring/spring_cloud_patterns#6572bfb04e1f","section":"spring","module":"spring/spring_cloud_patterns","moduleName":"spring cloud patterns","sourceFile":"README.md","difficulty":"intermediate","question":"Why is Eureka described as an AP system in CAP theorem terms?","answerFull":"Eureka prioritizes Availability and Partition Tolerance over Consistency. Each Eureka server node maintains a full copy of the registry and replicates to peers. During a network partition, nodes do not stop serving registry data — they continue serving potentially stale information rather than refusing responses (which would sacrifice Availability). This is the correct trade-off for service discovery: a client that receives a stale (but mostly correct) registry and has a circuit breaker is more resilient than a client that gets no registry data at all. Eureka's self-preservation mode further reinforces the AP stance by refusing to evict instances when heartbeat loss exceeds a threshold, assuming a network issue rather than mass instance failure.","correct":"Eureka prioritizes Availability and Partition Tolerance over Consistency.","distractors":["A Bulkhead limits the number of concurrent calls to a dependency.","A GlobalFilter applies to all routes defined in the gateway.","A Resilience4j circuit breaker has three states."],"distractorIds":["spring/spring_cloud_patterns#29fe369c209a","spring/spring_cloud_patterns#54c5338f7f9d","spring/spring_cloud_patterns#69df3de20d37"],"concepts":["described","eureka","prioritizes","theorem","tolerance","cap"],"distractorsMd":[null,"A `GlobalFilter` applies to all routes defined in the gateway.",null]},{"id":"spring/spring_cloud_patterns#3a044ad4ab62","section":"spring","module":"spring/spring_cloud_patterns","moduleName":"spring cloud patterns","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between Micrometer Tracing and Spring Cloud Sleuth?","answerFull":"Spring Cloud Sleuth (Spring Cloud 2020.x and earlier) provided distributed tracing by auto-configuring Brave (B3 propagation) and automatically injecting trace and span IDs into log MDC and HTTP headers. From Spring Boot 3.0 and Spring Cloud 2022.0 onward, Sleuth is replaced by Micrometer Tracing. Micrometer Tracing is vendor-neutral: it supports both Brave (Zipkin/B3) and OpenTelemetry bridges. The API (Tracer, Span, Observation) is part of Micrometer core. Auto-configuration is handled by Spring Boot actuator auto-configuration, not a separate Spring Cloud module. Migration from Sleuth to Micrometer Tracing requires changing dependencies and replacing spring.sleuth.* properties with management.tracing.*.","correct":"Spring Cloud Sleuth (Spring Cloud 2020.x and earlier) provided distributed tracing by auto-configuring Brave (B3 propagation) and automatically injecting trace and span IDs into log MDC and HTTP headers.","distractors":["Set spring.cloud.openfeign.circuitbreaker.enabled=true.","Spring Cloud LoadBalancer is the official replacement for Netflix Ribbon (removed in Spring Cloud 2020.0).","Use @WebFluxTest with WebTestClient and mock the downstream service using WireMock (via spring-cloud-contract-wiremock) or a mock bean."],"distractorIds":["spring/spring_cloud_patterns#7a81141c79cb","spring/spring_cloud_patterns#fe0637e6bd23","spring/spring_cloud_patterns#0f3914644bd8"],"concepts":["brave","sleuth","2020","configuring","injecting","provided"],"answerFullMd":"Spring Cloud Sleuth (Spring Cloud 2020.x and earlier) provided distributed tracing by auto-configuring Brave (B3 propagation) and automatically injecting trace and span IDs into log MDC and HTTP headers. From Spring Boot 3.0 and Spring Cloud 2022.0 onward, Sleuth is replaced by Micrometer Tracing. Micrometer Tracing is vendor-neutral: it supports both Brave (Zipkin/B3) and OpenTelemetry bridges. The API (`Tracer`, `Span`, `Observation`) is part of Micrometer core. Auto-configuration is handled by Spring Boot actuator auto-configuration, not a separate Spring Cloud module. Migration from Sleuth to Micrometer Tracing requires changing dependencies and replacing `spring.sleuth.*` properties with `management.tracing.*`.","distractorsMd":["Set `spring.cloud.openfeign.circuitbreaker.enabled=true`.",null,"Use `@WebFluxTest` with `WebTestClient` and mock the downstream service using `WireMock` (via `spring-cloud-contract-wiremock`) or a mock bean."]},{"id":"spring/spring_cloud_patterns#59fba3c9f34a","section":"spring","module":"spring/spring_cloud_patterns","moduleName":"spring cloud patterns","sourceFile":"README.md","difficulty":"intermediate","question":"How does B3 propagation work and what headers are involved?","answerFull":"B3 is a trace context propagation format originated by Zipkin. Four headers carry context across HTTP calls: X-B3-TraceId — a 64-bit or 128-bit hex string identifying the entire distributed trace (same across all services for one logical request); X-B3-SpanId — a 64-bit hex string identifying the current unit of work (changes at each service boundary); X-B3-ParentSpanId — the span ID of the caller (used to build the trace tree); X-B3-Sampled — 1 or 0, indicating whether this trace should be recorded. Every service must propagate these headers to all downstream calls; missing propagation creates disconnected traces. In async code (CompletableFuture, reactive pipelines), the trace context must be explicitly transferred to the new thread/scheduler.","correct":"B3 is a trace context propagation format originated by Zipkin.","distractors":["Spring Cloud Sleuth (Spring Cloud 2020.x and earlier) provided distributed tracing by auto-configuring Brave (B3 propagation) and automatically injecting trace and span IDs into log MDC and HTTP headers.","Implement a custom KeyResolver bean that extracts the user identifier from the request context.","A Bulkhead limits the number of concurrent calls to a dependency."],"distractorIds":["spring/spring_cloud_patterns#3a044ad4ab62","spring/spring_cloud_patterns#9cf937200619","spring/spring_cloud_patterns#29fe369c209a"],"concepts":["originated","involved","zipkin","propagation","format","headers"],"answerFullMd":"B3 is a trace context propagation format originated by Zipkin. Four headers carry context across HTTP calls: `X-B3-TraceId` — a 64-bit or 128-bit hex string identifying the entire distributed trace (same across all services for one logical request); `X-B3-SpanId` — a 64-bit hex string identifying the current unit of work (changes at each service boundary); `X-B3-ParentSpanId` — the span ID of the caller (used to build the trace tree); `X-B3-Sampled` — 1 or 0, indicating whether this trace should be recorded. Every service must propagate these headers to all downstream calls; missing propagation creates disconnected traces. In async code (CompletableFuture, reactive pipelines), the trace context must be explicitly transferred to the new thread/scheduler.","distractorsMd":[null,"Implement a custom `KeyResolver` bean that extracts the user identifier from the request context.",null]},{"id":"spring/spring_cloud_patterns#7262cd36272c","section":"spring","module":"spring/spring_cloud_patterns","moduleName":"spring cloud patterns","sourceFile":"README.md","difficulty":"intermediate","question":"How would you implement weighted canary routing in Spring Cloud Gateway?","answerFull":"Use the Weight predicate. Define two routes with the same path predicate but different URIs and weights: yaml routes: - id: stable uri: lb://order-service-v1 predicates: - Path=/api/orders/ - Weight=orders-group, 90 - id: canary uri: lb://order-service-v2 predicates: - Path=/api/orders/ - Weight=orders-group, 10 Gateway distributes 90% of traffic to v1 and 10% to v2. The weight is resolved per-request using a consistent random selection within the group. This enables incremental rollout: start at 5%, monitor error rates and latency via Micrometer metrics, increase to 50%, then 100%.","correct":"Use the Weight predicate.","distractors":["A GlobalFilter applies to all routes defined in the gateway.","Implement a custom KeyResolver bean that extracts the user identifier from the request context.","Spring Cloud Sleuth (Spring Cloud 2020.x and earlier) provided distributed tracing by auto-configuring Brave (B3 propagation) and automatically injecting trace and span IDs into log MDC and HTTP headers."],"distractorIds":["spring/spring_cloud_patterns#54c5338f7f9d","spring/spring_cloud_patterns#9cf937200619","spring/spring_cloud_patterns#3a044ad4ab62"],"concepts":["predicate","canary","weighted","implement","gateway","cloud"],"answerFullMd":"Use the Weight predicate. Define two routes with the same path predicate but different URIs and weights: ```yaml routes: - id: stable uri: lb://order-service-v1 predicates: - Path=/api/orders/** - Weight=orders-group, 90 - id: canary uri: lb://order-service-v2 predicates: - Path=/api/orders/** - Weight=orders-group, 10 ``` Gateway distributes 90% of traffic to v1 and 10% to v2. The weight is resolved per-request using a consistent random selection within the group. This enables incremental rollout: start at 5%, monitor error rates and latency via Micrometer metrics, increase to 50%, then 100%.","distractorsMd":["A `GlobalFilter` applies to all routes defined in the gateway.","Implement a custom `KeyResolver` bean that extracts the user identifier from the request context.",null]},{"id":"spring/spring_cloud_patterns#29fe369c209a","section":"spring","module":"spring/spring_cloud_patterns","moduleName":"spring cloud patterns","sourceFile":"README.md","difficulty":"intermediate","question":"What is a Bulkhead in Resilience4j and when would you use it over a circuit breaker?","answerFull":"A Bulkhead limits the number of concurrent calls to a dependency. The semaphore bulkhead blocks callers that exceed the limit (up to a configurable wait duration) and rejects them with BulkheadFullException. The thread pool bulkhead provides isolation via a dedicated thread pool (similar to Hystrix). A circuit breaker is reactive — it opens after failures are detected. A bulkhead is proactive — it prevents thread pool exhaustion before failures occur. Use both together: the bulkhead prevents the thread pool from being consumed by slow calls; the circuit breaker stops calling a service that has started failing. For services with high latency variance, a bulkhead with a tight concurrency limit (10–20) combined with a circuit breaker provides defense in depth.","correct":"A Bulkhead limits the number of concurrent calls to a dependency.","distractors":["A Resilience4j circuit breaker has three states.","In addition to failure rate thresholds, Resilience4j allows configuring a slowCallRateThreshold and slowCallDurationThreshold.","A GlobalFilter applies to all routes defined in the gateway."],"distractorIds":["spring/spring_cloud_patterns#69df3de20d37","spring/spring_cloud_patterns#bb2580c42770","spring/spring_cloud_patterns#54c5338f7f9d"],"concepts":["resilience4j","bulkhead","breaker","circuit","limits","concurrent"],"answerFullMd":"A Bulkhead limits the number of concurrent calls to a dependency. The semaphore bulkhead blocks callers that exceed the limit (up to a configurable wait duration) and rejects them with `BulkheadFullException`. The thread pool bulkhead provides isolation via a dedicated thread pool (similar to Hystrix). A circuit breaker is reactive — it opens after failures are detected. A bulkhead is proactive — it prevents thread pool exhaustion before failures occur. Use both together: the bulkhead prevents the thread pool from being consumed by slow calls; the circuit breaker stops calling a service that has started failing. For services with high latency variance, a bulkhead with a tight concurrency limit (10–20) combined with a circuit breaker provides defense in depth.","distractorsMd":[null,"In addition to failure rate thresholds, Resilience4j allows configuring a `slowCallRateThreshold` and `slowCallDurationThreshold`.","A `GlobalFilter` applies to all routes defined in the gateway."]},{"id":"spring/spring_cloud_patterns#807473bafa9f","section":"spring","module":"spring/spring_cloud_patterns","moduleName":"spring cloud patterns","sourceFile":"README.md","difficulty":"advanced","question":"How does the RequestRateLimiter filter work in Spring Cloud Gateway?","answerFull":"The built-in RequestRateLimiter filter uses a token bucket algorithm backed by Redis (via spring-boot-starter-data-redis-reactive). Configuration: replenishRate (tokens added per second), burstCapacity (maximum tokens in the bucket), requestedTokens (tokens consumed per request, default 1). A KeyResolver bean determines the rate limit key (by IP, by user ID, by API key). The filter uses a Lua script in Redis to atomically check and decrement the token count. Requests that exceed the rate return 429 Too Many Requests. Redis ensures consistent rate limiting across multiple Gateway instances sharing the same Redis instance.","correct":"The built-in RequestRateLimiter filter uses a token bucket algorithm backed by Redis (via spring-boot-starter-data-redis-reactive).","distractors":["Spring Cloud Gateway is a programmatic, reactive API gateway built on Spring WebFlux and Project Reactor.","Use @WebFluxTest with WebTestClient and mock the downstream service using WireMock (via spring-cloud-contract-wiremock) or a mock bean.","Spring Cloud LoadBalancer is the official replacement for Netflix Ribbon (removed in Spring Cloud 2020.0)."],"distractorIds":["spring/spring_cloud_patterns#5aa2bd5949f6","spring/spring_cloud_patterns#0f3914644bd8","spring/spring_cloud_patterns#fe0637e6bd23"],"concepts":["requestratelimiter","starter","reactive","bucket","boot","backed"],"correctMd":"The built-in `RequestRateLimiter` filter uses a token bucket algorithm backed by Redis (via `spring-boot-starter-data-redis-reactive`).","answerFullMd":"The built-in `RequestRateLimiter` filter uses a token bucket algorithm backed by Redis (via `spring-boot-starter-data-redis-reactive`). Configuration: `replenishRate` (tokens added per second), `burstCapacity` (maximum tokens in the bucket), `requestedTokens` (tokens consumed per request, default 1). A `KeyResolver` bean determines the rate limit key (by IP, by user ID, by API key). The filter uses a Lua script in Redis to atomically check and decrement the token count. Requests that exceed the rate return 429 Too Many Requests. Redis ensures consistent rate limiting across multiple Gateway instances sharing the same Redis instance.","distractorsMd":[null,"Use `@WebFluxTest` with `WebTestClient` and mock the downstream service using `WireMock` (via `spring-cloud-contract-wiremock`) or a mock bean.",null]},{"id":"spring/spring_cloud_patterns#85946df2ae9e","section":"spring","module":"spring/spring_cloud_patterns","moduleName":"spring cloud patterns","sourceFile":"README.md","difficulty":"advanced","question":"What happens when a Feign client method throws an exception vs. returns an error HTTP response?","answerFull":"By default, Feign maps HTTP 4xx/5xx responses to FeignException subclasses. This does NOT trigger Resilience4j retry by default because FeignException is not in the retry-exceptions list. To retry on 503, you must configure a custom ErrorDecoder that maps 503 to a RetryableException. Conversely, if the underlying HTTP connection throws a SocketTimeoutException (checked exception wrapping), Feign propagates it, and if SocketTimeoutException is in retry-exceptions, Resilience4j will retry. The gotcha: if you configure retry on FeignException, ALL Feign errors trigger retry, including 400 Bad Request — which is never retryable and will just waste resources. Always configure specific exception types and ignore client-error exceptions explicitly.","correct":"By default, Feign maps HTTP 4xx/5xx responses to FeignException subclasses.","distractors":["Spring Cloud Sleuth (Spring Cloud 2020.x and earlier) provided distributed tracing by auto-configuring Brave (B3 propagation) and automatically injecting trace and span IDs into log MDC and HTTP headers.","A Bulkhead limits the number of concurrent calls to a dependency.","A GlobalFilter applies to all routes defined in the gateway."],"distractorIds":["spring/spring_cloud_patterns#3a044ad4ab62","spring/spring_cloud_patterns#29fe369c209a","spring/spring_cloud_patterns#54c5338f7f9d"],"concepts":["feign","feignexception","4xx","5xx","subclasses","happens"],"correctMd":"By default, Feign maps HTTP 4xx/5xx responses to `FeignException` subclasses.","answerFullMd":"By default, Feign maps HTTP 4xx/5xx responses to `FeignException` subclasses. This does NOT trigger Resilience4j retry by default because `FeignException` is not in the `retry-exceptions` list. To retry on 503, you must configure a custom `ErrorDecoder` that maps 503 to a `RetryableException`. Conversely, if the underlying HTTP connection throws a `SocketTimeoutException` (checked exception wrapping), Feign propagates it, and if `SocketTimeoutException` is in `retry-exceptions`, Resilience4j will retry. The gotcha: if you configure retry on `FeignException`, ALL Feign errors trigger retry, including 400 Bad Request — which is never retryable and will just waste resources. Always configure specific exception types and ignore client-error exceptions explicitly.","distractorsMd":[null,null,"A `GlobalFilter` applies to all routes defined in the gateway."]},{"id":"spring/spring_cloud_patterns#9cf937200619","section":"spring","module":"spring/spring_cloud_patterns","moduleName":"spring cloud patterns","sourceFile":"README.md","difficulty":"advanced","question":"How would you implement rate limiting per user rather than per IP in Spring Cloud Gateway?","answerFull":"Implement a custom KeyResolver bean that extracts the user identifier from the request context. If the gateway validates JWT tokens (via a GlobalFilter), the user ID is available as a request attribute: java @Bean public KeyResolver userKeyResolver() { return exchange -> { String userId = exchange.getRequest().getHeaders().getFirst(\"X-User-Id\"); // X-User-Id injected by the auth GlobalFilter after JWT validation return userId != null ? Mono.just(userId) : Mono.just(\"anonymous:\" + exchange.getRequest().getRemoteAddress().getAddress().getHostAddress()); }; } Configure the RequestRateLimiter filter to reference this bean via SpEL: key-resolver: \"#{@userKeyResolver}\". Authenticated users get per-user limits (e.g., 100 req/s); anonymous requests get per-IP limits.","correct":"Implement a custom KeyResolver bean that extracts the user identifier from the request context.","distractors":["Use @WebFluxTest with WebTestClient and mock the downstream service using WireMock (via spring-cloud-contract-wiremock) or a mock bean.","The built-in RequestRateLimiter filter uses a token bucket algorithm backed by Redis (via spring-boot-starter-data-redis-reactive).","Set spring.cloud.openfeign.circuitbreaker.enabled=true."],"distractorIds":["spring/spring_cloud_patterns#0f3914644bd8","spring/spring_cloud_patterns#807473bafa9f","spring/spring_cloud_patterns#7a81141c79cb"],"concepts":["keyresolver","extracts","identifier","limiting","custom","implement"],"correctMd":"Implement a custom `KeyResolver` bean that extracts the user identifier from the request context.","answerFullMd":"Implement a custom `KeyResolver` bean that extracts the user identifier from the request context. If the gateway validates JWT tokens (via a GlobalFilter), the user ID is available as a request attribute: ```java @Bean public KeyResolver userKeyResolver() { return exchange -> { String userId = exchange.getRequest().getHeaders().getFirst(\"X-User-Id\"); // X-User-Id injected by the auth GlobalFilter after JWT validation return userId != null ? Mono.just(userId) : Mono.just(\"anonymous:\" + exchange.getRequest().getRemoteAddress().getAddress().getHostAddress()); }; } ``` Configure the `RequestRateLimiter` filter to reference this bean via SpEL: `key-resolver: \"#{@userKeyResolver}\"`. Authenticated users get per-user limits (e.g., 100 req/s); anonymous requests get per-IP limits.","distractorsMd":["Use `@WebFluxTest` with `WebTestClient` and mock the downstream service using `WireMock` (via `spring-cloud-contract-wiremock`) or a mock bean.","The built-in `RequestRateLimiter` filter uses a token bucket algorithm backed by Redis (via `spring-boot-starter-data-redis-reactive`).","Set `spring.cloud.openfeign.circuitbreaker.enabled=true`."]},{"id":"spring/spring_cloud_patterns#0f3914644bd8","section":"spring","module":"spring/spring_cloud_patterns","moduleName":"spring cloud patterns","sourceFile":"README.md","difficulty":"advanced","question":"How do you test a Spring Cloud Gateway route without starting a real downstream service?","answerFull":"Use @WebFluxTest with WebTestClient and mock the downstream service using WireMock (via spring-cloud-contract-wiremock) or a mock bean. Spring Cloud Gateway supports MockWebServer (OkHttp) for unit tests of individual filters. For integration testing the full filter chain: java @SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT) @AutoConfigureWireMock(port = 0) // starts WireMock on a random port class GatewayRouteTest { @Test void orderRouteForwardsCorrectly() { stubFor(get(urlEqualTo(\"/orders/123\")).willReturn(okJson(\"{\\\"id\\\":\\\"123\\\"}\"))); webTestClient.get().uri(\"/api/orders/123\") .header(HttpHeaders.AUTHORIZATION, \"Bearer \" + validToken) .exchange() .expectStatus().isOk() .expectBody().jsonPath(\"$.id\").isEqualTo(\"123\"); } }","correct":"Use @WebFluxTest with WebTestClient and mock the downstream service using WireMock (via spring-cloud-contract-wiremock) or a mock bean.","distractors":["Set spring.cloud.openfeign.circuitbreaker.enabled=true.","Spring Cloud LoadBalancer is the official replacement for Netflix Ribbon (removed in Spring Cloud 2020.0).","The built-in RequestRateLimiter filter uses a token bucket algorithm backed by Redis (via spring-boot-starter-data-redis-reactive)."],"distractorIds":["spring/spring_cloud_patterns#7a81141c79cb","spring/spring_cloud_patterns#fe0637e6bd23","spring/spring_cloud_patterns#807473bafa9f"],"concepts":["webfluxtest","webtestclient","wiremock","contract","mock","downstream"],"correctMd":"Use `@WebFluxTest` with `WebTestClient` and mock the downstream service using `WireMock` (via `spring-cloud-contract-wiremock`) or a mock bean.","answerFullMd":"Use `@WebFluxTest` with `WebTestClient` and mock the downstream service using `WireMock` (via `spring-cloud-contract-wiremock`) or a mock bean. Spring Cloud Gateway supports `MockWebServer` (OkHttp) for unit tests of individual filters. For integration testing the full filter chain: ```java @SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT) @AutoConfigureWireMock(port = 0) // starts WireMock on a random port class GatewayRouteTest { @Test void orderRouteForwardsCorrectly() { stubFor(get(urlEqualTo(\"/orders/123\")).willReturn(okJson(\"{\\\"id\\\":\\\"123\\\"}\"))); webTestClient.get().uri(\"/api/orders/123\") .header(HttpHeaders.AUTHORIZATION, \"Bearer \" + validToken) .exchange() .expectStatus().isOk() .expectBody().jsonPath(\"$.id\").isEqualTo(\"123\"); } } ```","distractorsMd":["Set `spring.cloud.openfeign.circuitbreaker.enabled=true`.",null,"The built-in `RequestRateLimiter` filter uses a token bucket algorithm backed by Redis (via `spring-boot-starter-data-redis-reactive`)."]},{"id":"spring/spring_cloud_patterns#bb2580c42770","section":"spring","module":"spring/spring_cloud_patterns","moduleName":"spring cloud patterns","sourceFile":"README.md","difficulty":"advanced","question":"Explain the concept of slow call circuit breaking and why it matters for production.","answerFull":"In addition to failure rate thresholds, Resilience4j allows configuring a slowCallRateThreshold and slowCallDurationThreshold. A call is considered \"slow\" (and counted as a negative signal) if it exceeds the duration threshold (e.g., 2000ms) even if it returns a 200 OK. This matters because a dependency that consistently responds in 3 seconds is effectively a failure from the caller's perspective — it ties up resources, adds latency to the user response, and can exhaust thread pools just as a full failure would. By treating slow calls like failures, the circuit breaker opens before the downstream service fully fails, enabling the fallback to serve fast degraded responses rather than slow partial responses.","correct":"In addition to failure rate thresholds, Resilience4j allows configuring a slowCallRateThreshold and slowCallDurationThreshold.","distractors":["A Resilience4j circuit breaker has three states.","Spring Cloud Sleuth (Spring Cloud 2020.x and earlier) provided distributed tracing by auto-configuring Brave (B3 propagation) and automatically injecting trace and span IDs into log MDC and HTTP headers.","A Bulkhead limits the number of concurrent calls to a dependency."],"distractorIds":["spring/spring_cloud_patterns#69df3de20d37","spring/spring_cloud_patterns#3a044ad4ab62","spring/spring_cloud_patterns#29fe369c209a"],"concepts":["slowcallratethreshold","slowcalldurationthreshold","configuring","resilience4j","concept","explain"],"correctMd":"In addition to failure rate thresholds, Resilience4j allows configuring a `slowCallRateThreshold` and `slowCallDurationThreshold`.","answerFullMd":"In addition to failure rate thresholds, Resilience4j allows configuring a `slowCallRateThreshold` and `slowCallDurationThreshold`. A call is considered \"slow\" (and counted as a negative signal) if it exceeds the duration threshold (e.g., 2000ms) even if it returns a 200 OK. This matters because a dependency that consistently responds in 3 seconds is effectively a failure from the caller's perspective — it ties up resources, adds latency to the user response, and can exhaust thread pools just as a full failure would. By treating slow calls like failures, the circuit breaker opens before the downstream service fully fails, enabling the fallback to serve fast degraded responses rather than slow partial responses."},{"id":"spring/spring_configuration#1ab321c63809","section":"spring","module":"spring/spring_configuration","moduleName":"spring configuration","sourceFile":"README.md","difficulty":"core","question":"What is the difference between @Configuration and @Component for classes with @Bean methods?","answerFull":"@Configuration creates a CGLIB subclass (full mode) where calls to @Bean methods return the singleton from the container. @Component uses no proxy (lite mode) — calls to @Bean methods are plain Java method invocations that create new instances. The difference only matters when one @Bean method calls another within the same class. Always use @Configuration for configuration classes that define inter-dependent beans.","correct":"@Configuration creates a CGLIB subclass (full mode) where calls to @Bean methods return the singleton from the container.","distractors":["@Profile(\"name\") on a @Configuration class or @Bean method registers the bean only when the named profile is active.","ImportBeanDefinitionRegistrar is an interface that allows programmatic registration of BeanDefinition objects during configuration processing.","@ComponentScan discovers classes by scanning package paths at runtime."],"distractorIds":["spring/spring_configuration#6e5d0b9ee432","spring/spring_configuration#b1030106eb73","spring/spring_configuration#7ed44efaacef"],"concepts":["difference","cglib","singleton","subclass","component","classes"],"correctMd":"`@Configuration` creates a CGLIB subclass (full mode) where calls to `@Bean` methods return the singleton from the container.","answerFullMd":"`@Configuration` creates a CGLIB subclass (full mode) where calls to `@Bean` methods return the singleton from the container. `@Component` uses no proxy (lite mode) — calls to `@Bean` methods are plain Java method invocations that create new instances. The difference only matters when one `@Bean` method calls another within the same class. Always use `@Configuration` for configuration classes that define inter-dependent beans.","distractorsMd":["`@Profile(\"name\")` on a `@Configuration` class or `@Bean` method registers the bean only when the named profile is active.","`ImportBeanDefinitionRegistrar` is an interface that allows programmatic registration of `BeanDefinition` objects during configuration processing.","`@ComponentScan` discovers classes by scanning package paths at runtime."]},{"id":"spring/spring_configuration#25c44022b0b8","section":"spring","module":"spring/spring_configuration","moduleName":"spring configuration","sourceFile":"README.md","difficulty":"core","question":"What is the order of @Conditional evaluation?","answerFull":"@Conditional conditions are evaluated in the order they are declared on the class. Spring Boot's @ConditionalOnClass runs first (cheapest check — classpath scan), then @ConditionalOnMissingBean (requires partial context initialization), then @ConditionalOnProperty. If any condition fails, the class is skipped and no beans are registered. The --debug startup flag prints the ConditionEvaluationReport showing which conditions passed or failed.","correct":"@Conditional conditions are evaluated in the order they are declared on the class.","distractors":["A @Configuration class (full mode) is CGLIB-proxied: @Bean method calls from within the class are intercepted and return the same singleton instance from the bean factory.","@Conditional(MyCondition.class) is the meta-annotation that powers all @ConditionalOn* variants.","@Configuration(proxyBeanMethods=false) disables the CGLIB proxy for the configuration class (making it behave like lite mode)."],"distractorIds":["spring/spring_configuration#3f5a70900711","spring/spring_configuration#1548ad1c1164","spring/spring_configuration#c4e0e9225e62"],"concepts":["evaluated","conditional","conditions","declared","evaluation","order"],"correctMd":"`@Conditional` conditions are evaluated in the order they are declared on the class.","answerFullMd":"`@Conditional` conditions are evaluated in the order they are declared on the class. Spring Boot's `@ConditionalOnClass` runs first (cheapest check — classpath scan), then `@ConditionalOnMissingBean` (requires partial context initialization), then `@ConditionalOnProperty`. If any condition fails, the class is skipped and no beans are registered. The `--debug` startup flag prints the `ConditionEvaluationReport` showing which conditions passed or failed.","distractorsMd":["A `@Configuration` class (full mode) is CGLIB-proxied: `@Bean` method calls from within the class are intercepted and return the same singleton instance from the bean factory.","`@Conditional(MyCondition.class)` is the meta-annotation that powers all `@ConditionalOn*` variants.","`@Configuration(proxyBeanMethods=false)` disables the CGLIB proxy for the configuration class (making it behave like lite mode)."]},{"id":"spring/spring_configuration#327600f724b0","section":"spring","module":"spring/spring_configuration","moduleName":"spring configuration","sourceFile":"README.md","difficulty":"core","question":"How does @PropertySource differ from @ConfigurationProperties?","answerFull":"@PropertySource adds a .properties file to the Spring Environment, making its properties accessible via @Value and Environment.getProperty(). It does not bind properties to a Java object. @ConfigurationProperties takes a prefix and binds all matching properties from the Environment to a typed Java class with getters/setters. @ConfigurationProperties supports relaxed binding (camelCase, kebab-case, SCREAMING_SNAKE_CASE all map to the same property), JSR-303 validation, and IDE autocompletion. Prefer @ConfigurationProperties for any group of related properties.","correct":"@PropertySource adds a .properties file to the Spring Environment, making its properties accessible via @Value and Environment.getProperty().","distractors":["@PropertySource is a Java annotation that loads a .properties file into the Environment during the configuration class processing phase.","@Configuration(proxyBeanMethods=false) disables the CGLIB proxy for the configuration class (making it behave like lite mode).","@ComponentScan discovers classes by scanning package paths at runtime."],"distractorIds":["spring/spring_configuration#b4da7c7dd241","spring/spring_configuration#c4e0e9225e62","spring/spring_configuration#7ed44efaacef"],"concepts":["getproperty","propertysource","configurationproperties","accessible","differ","environment"],"correctMd":"`@PropertySource` adds a `.properties` file to the Spring `Environment`, making its properties accessible via `@Value` and `Environment.getProperty()`.","answerFullMd":"`@PropertySource` adds a `.properties` file to the Spring `Environment`, making its properties accessible via `@Value` and `Environment.getProperty()`. It does not bind properties to a Java object. `@ConfigurationProperties` takes a prefix and binds all matching properties from the `Environment` to a typed Java class with getters/setters. `@ConfigurationProperties` supports relaxed binding (camelCase, kebab-case, SCREAMING_SNAKE_CASE all map to the same property), JSR-303 validation, and IDE autocompletion. Prefer `@ConfigurationProperties` for any group of related properties.","distractorsMd":["`@PropertySource` is a Java annotation that loads a `.properties` file into the Environment during the configuration class processing phase.","`@Configuration(proxyBeanMethods=false)` disables the CGLIB proxy for the configuration class (making it behave like lite mode).","`@ComponentScan` discovers classes by scanning package paths at runtime."]},{"id":"spring/spring_configuration#b14ccc46042e","section":"spring","module":"spring/spring_configuration","moduleName":"spring configuration","sourceFile":"README.md","difficulty":"core","question":"What is an ImportSelector and when would you use it?","answerFull":"ImportSelector is an interface with selectImports(AnnotationMetadata) that returns an array of fully-qualified class names to import. The Spring container calls this at configuration processing time. AutoConfigurationImportSelector (which powers @EnableAutoConfiguration) is the most important example. You write an ImportSelector when building a library that needs to conditionally import different configurations based on annotation attributes — for example, @EnableMyFeature(mode=Mode.SYNC) importing different config classes based on the mode attribute.","correct":"ImportSelector is an interface with selectImports(AnnotationMetadata) that returns an array of fully-qualified class names to import.","distractors":["A @Configuration class (full mode) is CGLIB-proxied: @Bean method calls from within the class are intercepted and return the same singleton instance from the bean factory.","@Profile(\"name\") on a @Configuration class or @Bean method registers the bean only when the named profile is active.","@PropertySource is a Java annotation that loads a .properties file into the Environment during the configuration class processing phase."],"distractorIds":["spring/spring_configuration#3f5a70900711","spring/spring_configuration#6e5d0b9ee432","spring/spring_configuration#b4da7c7dd241"],"concepts":["annotationmetadata","importselector","selectimports","qualified","import","names"],"correctMd":"`ImportSelector` is an interface with `selectImports(AnnotationMetadata)` that returns an array of fully-qualified class names to import.","answerFullMd":"`ImportSelector` is an interface with `selectImports(AnnotationMetadata)` that returns an array of fully-qualified class names to import. The Spring container calls this at configuration processing time. `AutoConfigurationImportSelector` (which powers `@EnableAutoConfiguration`) is the most important example. You write an `ImportSelector` when building a library that needs to conditionally import different configurations based on annotation attributes — for example, `@EnableMyFeature(mode=Mode.SYNC)` importing different config classes based on the `mode` attribute.","distractorsMd":["A `@Configuration` class (full mode) is CGLIB-proxied: `@Bean` method calls from within the class are intercepted and return the same singleton instance from the bean factory.","`@Profile(\"name\")` on a `@Configuration` class or `@Bean` method registers the bean only when the named profile is active.","`@PropertySource` is a Java annotation that loads a `.properties` file into the Environment during the configuration class processing phase."]},{"id":"spring/spring_configuration#7ed44efaacef","section":"spring","module":"spring/spring_configuration","moduleName":"spring configuration","sourceFile":"README.md","difficulty":"core","question":"What is the difference between @Import and @ComponentScan?","answerFull":"@ComponentScan discovers classes by scanning package paths at runtime. @Import explicitly registers specific classes by name (compile-time reference). @Import is faster (no classpath scanning) and more explicit. Use @ComponentScan for your application's own classes; use @Import in library/starter code to programmatically register configuration without requiring the user to scan specific packages.","correct":"@ComponentScan discovers classes by scanning package paths at runtime.","distractors":["ImportSelector is an interface with selectImports(AnnotationMetadata) that returns an array of fully-qualified class names to import.","@Order(n) on @Configuration classes controls the order in which they are processed.","@Conditional conditions are evaluated in the order they are declared on the class."],"distractorIds":["spring/spring_configuration#b14ccc46042e","spring/spring_configuration#1283c4156319","spring/spring_configuration#25c44022b0b8"],"concepts":["componentscan","scanning","discovers","difference","import","package"],"correctMd":"`@ComponentScan` discovers classes by scanning package paths at runtime.","answerFullMd":"`@ComponentScan` discovers classes by scanning package paths at runtime. `@Import` explicitly registers specific classes by name (compile-time reference). `@Import` is faster (no classpath scanning) and more explicit. Use `@ComponentScan` for your application's own classes; use `@Import` in library/starter code to programmatically register configuration without requiring the user to scan specific packages.","distractorsMd":["`ImportSelector` is an interface with `selectImports(AnnotationMetadata)` that returns an array of fully-qualified class names to import.","`@Order(n)` on `@Configuration` classes controls the order in which they are processed.","`@Conditional` conditions are evaluated in the order they are declared on the class."]},{"id":"spring/spring_configuration#6e5d0b9ee432","section":"spring","module":"spring/spring_configuration","moduleName":"spring configuration","sourceFile":"README.md","difficulty":"intermediate","question":"How does @Profile work and how can you activate multiple profiles?","answerFull":"@Profile(\"name\") on a @Configuration class or @Bean method registers the bean only when the named profile is active. Activate via spring.profiles.active property (comma-separated for multiple), the SPRING_PROFILES_ACTIVE environment variable, or SpringApplication.setAdditionalProfiles() programmatically. spring.profiles.include always activates additional profiles regardless of the primary active profile. A bean with @Profile(\"!production\") is active when production is NOT active.","correct":"@Profile(\"name\") on a @Configuration class or @Bean method registers the bean only when the named profile is active.","distractors":["@Conditional(MyCondition.class) is the meta-annotation that powers all @ConditionalOn* variants.","@Configuration(proxyBeanMethods=false) disables the CGLIB proxy for the configuration class (making it behave like lite mode).","@Configuration creates a CGLIB subclass (full mode) where calls to @Bean methods return the singleton from the container."],"distractorIds":["spring/spring_configuration#1548ad1c1164","spring/spring_configuration#c4e0e9225e62","spring/spring_configuration#1ab321c63809"],"concepts":["activate","profiles","registers","named","profile","active"],"correctMd":"`@Profile(\"name\")` on a `@Configuration` class or `@Bean` method registers the bean only when the named profile is active.","answerFullMd":"`@Profile(\"name\")` on a `@Configuration` class or `@Bean` method registers the bean only when the named profile is active. Activate via `spring.profiles.active` property (comma-separated for multiple), the `SPRING_PROFILES_ACTIVE` environment variable, or `SpringApplication.setAdditionalProfiles()` programmatically. `spring.profiles.include` always activates additional profiles regardless of the primary active profile. A bean with `@Profile(\"!production\")` is active when production is NOT active.","distractorsMd":["`@Conditional(MyCondition.class)` is the meta-annotation that powers all `@ConditionalOn*` variants.","`@Configuration(proxyBeanMethods=false)` disables the CGLIB proxy for the configuration class (making it behave like lite mode).","`@Configuration` creates a CGLIB subclass (full mode) where calls to `@Bean` methods return the singleton from the container."]},{"id":"spring/spring_configuration#1283c4156319","section":"spring","module":"spring/spring_configuration","moduleName":"spring configuration","sourceFile":"README.md","difficulty":"intermediate","question":"What is @Order and when does it matter for @Configuration classes?","answerFull":"@Order(n) on @Configuration classes controls the order in which they are processed. Lower values are processed first. This affects which @ConditionalOnMissingBean evaluations see existing beans. More importantly, @Order on BeanPostProcessor beans controls the order they are applied to each bean. For ApplicationListener beans, it controls which listener handles events first. For @Configuration classes in user code, order matters when multiple classes provide the same bean type and conditional logic depends on processing order.","correct":"@Order(n) on @Configuration classes controls the order in which they are processed.","distractors":["ImportBeanDefinitionRegistrar is an interface that allows programmatic registration of BeanDefinition objects during configuration processing.","@ComponentScan discovers classes by scanning package paths at runtime.","@PropertySource is a Java annotation that loads a .properties file into the Environment during the configuration class processing phase."],"distractorIds":["spring/spring_configuration#b1030106eb73","spring/spring_configuration#7ed44efaacef","spring/spring_configuration#b4da7c7dd241"],"concepts":["matter","processed","controls","classes","configuration","order"],"correctMd":"`@Order(n)` on `@Configuration` classes controls the order in which they are processed.","answerFullMd":"`@Order(n)` on `@Configuration` classes controls the order in which they are processed. Lower values are processed first. This affects which `@ConditionalOnMissingBean` evaluations see existing beans. More importantly, `@Order` on `BeanPostProcessor` beans controls the order they are applied to each bean. For `ApplicationListener` beans, it controls which listener handles events first. For `@Configuration` classes in user code, order matters when multiple classes provide the same bean type and conditional logic depends on processing order.","distractorsMd":["`ImportBeanDefinitionRegistrar` is an interface that allows programmatic registration of `BeanDefinition` objects during configuration processing.","`@ComponentScan` discovers classes by scanning package paths at runtime.","`@PropertySource` is a Java annotation that loads a `.properties` file into the Environment during the configuration class processing phase."]},{"id":"spring/spring_configuration#c4e0e9225e62","section":"spring","module":"spring/spring_configuration","moduleName":"spring configuration","sourceFile":"README.md","difficulty":"intermediate","question":"What is proxyBeanMethods=false and when should you use it?","answerFull":"@Configuration(proxyBeanMethods=false) disables the CGLIB proxy for the configuration class (making it behave like lite mode). Use it when the @Bean methods in the class are never called from other @Bean methods in the same class (independent beans), to improve startup performance (CGLIB proxying adds overhead). Spring Boot's own auto-configuration classes use proxyBeanMethods=false for most configurations since they define independent beans. Use proxyBeanMethods=true (the default) whenever inter-@Bean method calls are needed.","correct":"@Configuration(proxyBeanMethods=false) disables the CGLIB proxy for the configuration class (making it behave like lite mode).","distractors":["@Profile(\"name\") on a @Configuration class or @Bean method registers the bean only when the named profile is active.","@Conditional conditions are evaluated in the order they are declared on the class.","@PropertySource adds a .properties file to the Spring Environment, making its properties accessible via @Value and Environment.getProperty()."],"distractorIds":["spring/spring_configuration#6e5d0b9ee432","spring/spring_configuration#25c44022b0b8","spring/spring_configuration#327600f724b0"],"concepts":["proxybeanmethods","lite","disables","behave","cglib","configuration"],"correctMd":"`@Configuration(proxyBeanMethods=false)` disables the CGLIB proxy for the configuration class (making it behave like lite mode).","answerFullMd":"`@Configuration(proxyBeanMethods=false)` disables the CGLIB proxy for the configuration class (making it behave like lite mode). Use it when the `@Bean` methods in the class are never called from other `@Bean` methods in the same class (independent beans), to improve startup performance (CGLIB proxying adds overhead). Spring Boot's own auto-configuration classes use `proxyBeanMethods=false` for most configurations since they define independent beans. Use `proxyBeanMethods=true` (the default) whenever inter-`@Bean` method calls are needed.","distractorsMd":["`@Profile(\"name\")` on a `@Configuration` class or `@Bean` method registers the bean only when the named profile is active.","`@Conditional` conditions are evaluated in the order they are declared on the class.","`@PropertySource` adds a `.properties` file to the Spring `Environment`, making its properties accessible via `@Value` and `Environment.getProperty()`."]},{"id":"spring/spring_configuration#b4da7c7dd241","section":"spring","module":"spring/spring_configuration","moduleName":"spring configuration","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between @PropertySource and spring.config.import?","answerFull":"@PropertySource is a Java annotation that loads a .properties file into the Environment during the configuration class processing phase. It does not work with YAML files and processes at a fixed point in the config loading lifecycle. spring.config.import (Spring Boot 2.4+) is a property that loads additional config files or config tree directories, supports YAML, and participates fully in the config priority ordering. spring.config.import=configserver: is how Spring Cloud Config Server is imported in Boot 3.x, replacing the bootstrap context approach.","correct":"@PropertySource is a Java annotation that loads a .properties file into the Environment during the configuration class processing phase.","distractors":["@Configuration(proxyBeanMethods=false) disables the CGLIB proxy for the configuration class (making it behave like lite mode).","ImportSelector is an interface with selectImports(AnnotationMetadata) that returns an array of fully-qualified class names to import.","ImportBeanDefinitionRegistrar is an interface that allows programmatic registration of BeanDefinition objects during configuration processing."],"distractorIds":["spring/spring_configuration#c4e0e9225e62","spring/spring_configuration#b14ccc46042e","spring/spring_configuration#b1030106eb73"],"concepts":["propertysource","difference","import","environment","annotation","properties"],"correctMd":"`@PropertySource` is a Java annotation that loads a `.properties` file into the Environment during the configuration class processing phase.","answerFullMd":"`@PropertySource` is a Java annotation that loads a `.properties` file into the Environment during the configuration class processing phase. It does not work with YAML files and processes at a fixed point in the config loading lifecycle. `spring.config.import` (Spring Boot 2.4+) is a property that loads additional config files or config tree directories, supports YAML, and participates fully in the config priority ordering. `spring.config.import=configserver:` is how Spring Cloud Config Server is imported in Boot 3.x, replacing the bootstrap context approach.","distractorsMd":["`@Configuration(proxyBeanMethods=false)` disables the CGLIB proxy for the configuration class (making it behave like lite mode).","`ImportSelector` is an interface with `selectImports(AnnotationMetadata)` that returns an array of fully-qualified class names to import.","`ImportBeanDefinitionRegistrar` is an interface that allows programmatic registration of `BeanDefinition` objects during configuration processing."]},{"id":"spring/spring_configuration#b1030106eb73","section":"spring","module":"spring/spring_configuration","moduleName":"spring configuration","sourceFile":"README.md","difficulty":"advanced","question":"What is @ImportBeanDefinitionRegistrar?","answerFull":"ImportBeanDefinitionRegistrar is an interface that allows programmatic registration of BeanDefinition objects during configuration processing. Unlike ImportSelector (which returns class names), the registrar directly calls registry.registerBeanDefinition(). It receives the annotation metadata of the @Import annotation's declaring class, enabling dynamic bean registration based on annotation attributes. Spring Data JPA uses this internally to register repository proxy bean definitions for each @Repository interface found during component scanning.","correct":"ImportBeanDefinitionRegistrar is an interface that allows programmatic registration of BeanDefinition objects during configuration processing.","distractors":["@Configuration(proxyBeanMethods=false) disables the CGLIB proxy for the configuration class (making it behave like lite mode).","@Configuration creates a CGLIB subclass (full mode) where calls to @Bean methods return the singleton from the container.","A @Configuration class (full mode) is CGLIB-proxied: @Bean method calls from within the class are intercepted and return the same singleton instance from the bean factory."],"distractorIds":["spring/spring_configuration#c4e0e9225e62","spring/spring_configuration#1ab321c63809","spring/spring_configuration#3f5a70900711"],"concepts":["beandefinition","importbeandefinitionregistrar","programmatic","registration","allows","configuration"],"correctMd":"`ImportBeanDefinitionRegistrar` is an interface that allows programmatic registration of `BeanDefinition` objects during configuration processing.","answerFullMd":"`ImportBeanDefinitionRegistrar` is an interface that allows programmatic registration of `BeanDefinition` objects during configuration processing. Unlike `ImportSelector` (which returns class names), the registrar directly calls `registry.registerBeanDefinition()`. It receives the annotation metadata of the `@Import` annotation's declaring class, enabling dynamic bean registration based on annotation attributes. Spring Data JPA uses this internally to register repository proxy bean definitions for each `@Repository` interface found during component scanning.","distractorsMd":["`@Configuration(proxyBeanMethods=false)` disables the CGLIB proxy for the configuration class (making it behave like lite mode).","`@Configuration` creates a CGLIB subclass (full mode) where calls to `@Bean` methods return the singleton from the container.","A `@Configuration` class (full mode) is CGLIB-proxied: `@Bean` method calls from within the class are intercepted and return the same singleton instance from the bean factory."]},{"id":"spring/spring_configuration#3f5a70900711","section":"spring","module":"spring/spring_configuration","moduleName":"spring configuration","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between @Configuration full mode and lite mode, and when does it matter?","answerFull":"A @Configuration class (full mode) is CGLIB-proxied: @Bean method calls from within the class are intercepted and return the same singleton instance from the bean factory. A @Component or @ComponentScan-discovered class containing @Bean methods (lite mode) is NOT proxied: inter-@Bean calls create plain Java object instances, bypassing the container. This matters when one @Bean method calls another: java @Configuration // FULL MODE — safe class AppConfig { @Bean DataSource dataSource() { return new HikariDataSource(); } @Bean JdbcTemplate jdbc() { return new JdbcTemplate(dataSource()); } // returns THE SAME singleton } @Component // LITE MODE — dangerous class AppConfig { @Bean DataSource dataSource() { return new HikariDataSource(); } @Bean JdbcTemplate jdbc() { return new JdbcTemplate(dataSource()); } // creates a NEW DataSource each call } In lite mode, dataSource() creates a second HikariDataSource outside the container — a connection pool leak. Always use @Configuration for classes where @Bean methods call each other. Lite mode is appropriate only for simple factories that have no inter-@Bean dependencies.","correct":"A @Configuration class (full mode) is CGLIB-proxied: @Bean method calls from within the class are intercepted and return the same singleton instance from the bean factory.","distractors":["@Order(n) on @Configuration classes controls the order in which they are processed.","ImportBeanDefinitionRegistrar is an interface that allows programmatic registration of BeanDefinition objects during configuration processing.","@Configuration(proxyBeanMethods=false) disables the CGLIB proxy for the configuration class (making it behave like lite mode)."],"distractorIds":["spring/spring_configuration#1283c4156319","spring/spring_configuration#b1030106eb73","spring/spring_configuration#c4e0e9225e62"],"concepts":["intercepted","lite","proxied","matter","difference","cglib"],"questionMd":"What is the difference between `@Configuration` full mode and lite mode, and when does it matter?","correctMd":"A `@Configuration` class (full mode) is CGLIB-proxied: `@Bean` method calls from within the class are intercepted and return the same singleton instance from the bean factory.","answerFullMd":"A `@Configuration` class (full mode) is CGLIB-proxied: `@Bean` method calls from within the class are intercepted and return the same singleton instance from the bean factory. A `@Component` or `@ComponentScan`-discovered class containing `@Bean` methods (lite mode) is NOT proxied: inter-`@Bean` calls create plain Java object instances, bypassing the container. This matters when one `@Bean` method calls another: ```java @Configuration // FULL MODE — safe class AppConfig { @Bean DataSource dataSource() { return new HikariDataSource(); } @Bean JdbcTemplate jdbc() { return new JdbcTemplate(dataSource()); } // returns THE SAME singleton } @Component // LITE MODE — dangerous class AppConfig { @Bean DataSource dataSource() { return new HikariDataSource(); } @Bean JdbcTemplate jdbc() { return new JdbcTemplate(dataSource()); } // creates a NEW DataSource each call } ``` In lite mode, `dataSource()` creates a second `HikariDataSource` outside the container — a connection pool leak. Always use `@Configuration` for classes where `@Bean` methods call each other. Lite mode is appropriate only for simple factories that have no inter-`@Bean` dependencies.","distractorsMd":["`@Order(n)` on `@Configuration` classes controls the order in which they are processed.","`ImportBeanDefinitionRegistrar` is an interface that allows programmatic registration of `BeanDefinition` objects during configuration processing.","`@Configuration(proxyBeanMethods=false)` disables the CGLIB proxy for the configuration class (making it behave like lite mode)."]},{"id":"spring/spring_configuration#1548ad1c1164","section":"spring","module":"spring/spring_configuration","moduleName":"spring configuration","sourceFile":"README.md","difficulty":"advanced","question":"What is @Conditional and how does it compare to @ConditionalOnProperty / @ConditionalOnClass?","answerFull":"@Conditional(MyCondition.class) is the meta-annotation that powers all @ConditionalOn* variants. The referenced Condition implementation receives ConditionContext (access to Environment, BeanDefinitionRegistry, ClassLoader) and AnnotatedTypeMetadata and returns true (include) or false (skip). @ConditionalOnProperty, @ConditionalOnClass, @ConditionalOnBean are Spring Boot's pre-built Condition implementations registered as composed annotations for common use cases. Write a custom @Conditional when the built-in variants don't cover your logic — e.g., enabling a bean only when running in Kubernetes (check for KUBERNETES_SERVICE_HOST env var) or only on a specific OS. Custom conditions are registered with @Conditional(MyKubernetesCondition.class) and can be composed into a custom @ConditionalOnKubernetes annotation.","correct":"@Conditional(MyCondition.class) is the meta-annotation that powers all @ConditionalOn* variants.","distractors":["@Conditional conditions are evaluated in the order they are declared on the class.","@Configuration(proxyBeanMethods=false) disables the CGLIB proxy for the configuration class (making it behave like lite mode).","A @Configuration class (full mode) is CGLIB-proxied: @Bean method calls from within the class are intercepted and return the same singleton instance from the bean factory."],"distractorIds":["spring/spring_configuration#25c44022b0b8","spring/spring_configuration#c4e0e9225e62","spring/spring_configuration#3f5a70900711"],"concepts":["conditionalon","conditionalonclass","mycondition","powers","variants","conditional"],"questionMd":"What is `@Conditional` and how does it compare to `@ConditionalOnProperty` / `@ConditionalOnClass`?","correctMd":"`@Conditional(MyCondition.class)` is the meta-annotation that powers all `@ConditionalOn*` variants.","answerFullMd":"`@Conditional(MyCondition.class)` is the meta-annotation that powers all `@ConditionalOn*` variants. The referenced `Condition` implementation receives `ConditionContext` (access to `Environment`, `BeanDefinitionRegistry`, `ClassLoader`) and `AnnotatedTypeMetadata` and returns `true` (include) or `false` (skip). `@ConditionalOnProperty`, `@ConditionalOnClass`, `@ConditionalOnBean` are Spring Boot's pre-built `Condition` implementations registered as composed annotations for common use cases. Write a custom `@Conditional` when the built-in variants don't cover your logic — e.g., enabling a bean only when running in Kubernetes (check for `KUBERNETES_SERVICE_HOST` env var) or only on a specific OS. Custom conditions are registered with `@Conditional(MyKubernetesCondition.class)` and can be composed into a custom `@ConditionalOnKubernetes` annotation.","distractorsMd":["`@Conditional` conditions are evaluated in the order they are declared on the class.","`@Configuration(proxyBeanMethods=false)` disables the CGLIB proxy for the configuration class (making it behave like lite mode).","A `@Configuration` class (full mode) is CGLIB-proxied: `@Bean` method calls from within the class are intercepted and return the same singleton instance from the bean factory."]},{"id":"spring/spring_data_jpa#ced7c8823613","section":"spring","module":"spring/spring_data_jpa","moduleName":"spring data jpa","sourceFile":"README.md","difficulty":"core","question":"What is the difference between CrudRepository, JpaRepository, and PagingAndSortingRepository?","answerFull":"CrudRepository provides basic CRUD: save, findById, findAll, delete, count, existsById. PagingAndSortingRepository adds findAll(Pageable) and findAll(Sort). JpaRepository extends both and adds JPA-specific operations: flush, saveAndFlush, deleteInBatch, getById (reference without SELECT), and Example-based queries. In practice, you almost always extend JpaRepository unless you are writing a library that should work with non-JPA Spring Data modules (MongoDB, Redis) where extending CrudRepository keeps the interface provider-agnostic.","correct":"CrudRepository provides basic CRUD: save, findById, findAll, delete, count, existsById.","distractors":["JpaRepository.save() checks if the entity is new using SimpleJpaRepository.isNew(), which by default checks if the ID field is null.","findById(id) executes SELECT immediately and returns Optional<T>.","Page<T> extends Slice<T> and additionally includes a COUNT(*) query to determine the total number of elements and pages."],"distractorIds":["spring/spring_data_jpa#929b5d17b6f7","spring/spring_data_jpa#17143ce7643f","spring/spring_data_jpa#303a47ebc7dc"],"concepts":["crudrepository","existsbyid","findall","jparepository","crud","findbyid"]},{"id":"spring/spring_data_jpa#b491b53e342b","section":"spring","module":"spring/spring_data_jpa","moduleName":"spring data jpa","sourceFile":"README.md","difficulty":"core","question":"What is the N+1 select problem and how do you fix it?","answerFull":"N+1 occurs when loading N parent entities triggers N additional SELECT statements to load their lazy-fetched child collections. For example, loading 100 Orders and then accessing each Order's items fires 1 + 100 = 101 queries. Fix options: (A) JOIN FETCH in JPQL loads everything in one query using a Cartesian JOIN, but can produce duplicate parent rows (use DISTINCT). (B) @EntityGraph generates a LEFT OUTER JOIN. (C) Setting hibernate.default_batch_fetch_size=25 causes Hibernate to use IN (?1,?2,...,?25) clauses, reducing 100 queries to 4. Choice depends on cardinality: JOIN FETCH for single collections; batch fetching for multiple or large collections.","correct":"N+1 occurs when loading N parent entities triggers N additional SELECT statements to load their lazy-fetched child collections.","distractors":["findById(id) executes SELECT immediately and returns Optional<T>.","Option A: Load the managed entity, set only the changed fields, and let dirty checking generate a partial UPDATE (Hibernate with @DynamicUpdate only updates changed columns).","LAZY means the association is loaded on first access (generates an extra SELECT or proxy stub)."],"distractorIds":["spring/spring_data_jpa#17143ce7643f","spring/spring_data_jpa#613ce53090ae","spring/spring_data_jpa#e83898856372"],"concepts":["fetched","entities","collections","fix","loading","statements"]},{"id":"spring/spring_data_jpa#e83898856372","section":"spring","module":"spring/spring_data_jpa","moduleName":"spring data jpa","sourceFile":"README.md","difficulty":"core","question":"Explain FetchType.LAZY vs FetchType.EAGER and the risks of each.","answerFull":"LAZY means the association is loaded on first access (generates an extra SELECT or proxy stub). EAGER means the association is always loaded with the parent, usually via a JOIN or subselect. LAZY is the default for @OneToMany and @ManyToMany; EAGER is the default for @ManyToOne and @OneToOne. The risk of LAZY is LazyInitializationException when accessing the association after the persistence context closes. The risk of EAGER on collections is Cartesian product explosion when multiple EAGER @OneToMany associations exist on the same entity. Best practice: always use LAZY on collections and fetch explicitly per use case with @EntityGraph or JOIN FETCH.","correct":"LAZY means the association is loaded on first access (generates an extra SELECT or proxy stub).","distractors":["findById(id) executes SELECT immediately and returns Optional<T>.","N+1 occurs when loading N parent entities triggers N additional SELECT statements to load their lazy-fetched child collections.","findById issues a SELECT immediately and returns Optional<T> (empty if absent)."],"distractorIds":["spring/spring_data_jpa#17143ce7643f","spring/spring_data_jpa#b491b53e342b","spring/spring_data_jpa#c5b673c539f9"],"concepts":["association","eager","explain","stub","risks","loaded"],"distractorsMd":[null,null,"`findById` issues a SELECT immediately and returns `Optional<T>` (empty if absent)."]},{"id":"spring/spring_data_jpa#1ce1c8428821","section":"spring","module":"spring/spring_data_jpa","moduleName":"spring data jpa","sourceFile":"README.md","difficulty":"core","question":"What is a persistence context and what are the four entity states?","answerFull":"The persistence context is a first-level cache managed by the EntityManager within a transaction. It tracks all entities loaded or saved during that transaction. The four states are: Transient (new object, not associated with any row or context), Managed (associated with a row and tracked for dirty checking), Detached (was managed, context closed or entity evicted — changes not tracked), and Removed (scheduled for deletion on flush). Understanding states is critical: calling save() on a detached entity triggers a SELECT + UPDATE even if nothing changed; mutating a managed entity without calling save() still persists via dirty checking on flush.","correct":"The persistence context is a first-level cache managed by the EntityManager within a transaction.","distractors":["OSIV keeps the EntityManager (Session) open for the entire HTTP request lifecycle, including the view rendering phase.","You declare a @Version field (Long, Integer, or Timestamp) on the entity.","Interface-based projections are interfaces declaring getters for a subset of entity fields."],"distractorIds":["spring/spring_data_jpa#235f1f442ed5","spring/spring_data_jpa#39714fb1ac15","spring/spring_data_jpa#e60e9b296e91"],"concepts":["entitymanager","persistence","managed","four","entity","states"]},{"id":"spring/spring_data_jpa#ca24b649eb71","section":"spring","module":"spring/spring_data_jpa","moduleName":"spring data jpa","sourceFile":"README.md","difficulty":"intermediate","question":"What does @Modifying do, and when should you set clearAutomatically=true?","answerFull":"@Modifying marks a @Query method as an UPDATE or DELETE operation, which causes Spring Data to execute it as an executeUpdate() call instead of getResultList(). Without it, Hibernate throws an exception for write operations in JPQL. clearAutomatically=true is required when you perform a bulk update and then query entities in the same transaction, because the bulk UPDATE bypasses the persistence context (first-level cache). Without clearing, subsequently fetched entities may show stale values from the L1 cache. flushAutomatically=true ensures any pending dirty-checked changes are flushed before the bulk update executes, preventing the bulk query from operating on out-of-date DB state.","correct":"@Modifying marks a @Query method as an UPDATE or DELETE operation, which causes Spring Data to execute it as an executeUpdate() call instead of getResultList().","distractors":["Option A: Load the managed entity, set only the changed fields, and let dirty checking generate a partial UPDATE (Hibernate with @DynamicUpdate only updates changed columns).","Specification is a Spring Data abstraction over JPA Criteria API.","Page<T> extends Slice<T> and additionally includes a COUNT(*) query to determine the total number of elements and pages."],"distractorIds":["spring/spring_data_jpa#613ce53090ae","spring/spring_data_jpa#40a55af735f8","spring/spring_data_jpa#303a47ebc7dc"],"concepts":["executeupdate","getresultlist","modifying","marks","delete","execute"]},{"id":"spring/spring_data_jpa#39714fb1ac15","section":"spring","module":"spring/spring_data_jpa","moduleName":"spring data jpa","sourceFile":"README.md","difficulty":"intermediate","question":"How does optimistic locking work with @Version?","answerFull":"You declare a @Version field (Long, Integer, or Timestamp) on the entity. Hibernate includes the version in every UPDATE: UPDATE user SET email=?, version=version+1 WHERE id=? AND version=?. If the WHERE clause matches zero rows (another transaction already incremented the version), Hibernate throws ObjectOptimisticLockingFailureException. The caller must catch this and retry or surface a conflict error to the user. This avoids holding a DB lock but requires application-level retry logic. Use optimistic locking when concurrent modification of the same row is rare. The @Version field is also used in @Modifying updates automatically if you include the entity's version in your query.","correct":"You declare a @Version field (Long, Integer, or Timestamp) on the entity.","distractors":["Interface-based projections are interfaces declaring getters for a subset of entity fields.","Option A: Load the managed entity, set only the changed fields, and let dirty checking generate a partial UPDATE (Hibernate with @DynamicUpdate only updates changed columns).","Use @DataJpaTest which loads only the JPA slice: entity classes, repositories, Flyway/Liquibase, and an embedded H2 database (unless replaced)."],"distractorIds":["spring/spring_data_jpa#e60e9b296e91","spring/spring_data_jpa#613ce53090ae","spring/spring_data_jpa#50ae5029ab96"],"concepts":["optimistic","declare","locking","integer","timestamp","entity"]},{"id":"spring/spring_data_jpa#b55bba210018","section":"spring","module":"spring/spring_data_jpa","moduleName":"spring data jpa","sourceFile":"README.md","difficulty":"intermediate","question":"When would you choose pessimistic locking over optimistic locking?","answerFull":"Pessimistic locking is appropriate when contention is high and retry overhead would degrade user experience or cause thundering herd issues. For example, a seat reservation system where many users simultaneously try to book the last seat — optimistic locking would cause repeated failures and retries. PESSIMISTIC_WRITE (@Lock(LockModeType.PESSIMISTIC_WRITE)) issues SELECT FOR UPDATE, blocking other writers for the duration of the transaction. The trade-off is reduced throughput and deadlock risk. Pessimistic locking should be scoped to the narrowest possible transaction to minimize lock hold time.","correct":"Pessimistic locking is appropriate when contention is high and retry overhead would degrade user experience or cause thundering herd issues.","distractors":["findById issues a SELECT immediately and returns Optional<T> (empty if absent).","@EnableJpaAuditing activates the AuditingEntityListener.","@Modifying marks a @Query method as an UPDATE or DELETE operation, which causes Spring Data to execute it as an executeUpdate() call instead of getResultList()."],"distractorIds":["spring/spring_data_jpa#c5b673c539f9","spring/spring_data_jpa#8e625aef3bae","spring/spring_data_jpa#ca24b649eb71"],"concepts":["herd","thundering","degrade","pessimistic","appropriate","contention"],"distractorsMd":["`findById` issues a SELECT immediately and returns `Optional<T>` (empty if absent).",null,null]},{"id":"spring/spring_data_jpa#303a47ebc7dc","section":"spring","module":"spring/spring_data_jpa","moduleName":"spring data jpa","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between Page<T> and Slice<T>?","answerFull":"Page<T> extends Slice<T> and additionally includes a COUNT(*) query to determine the total number of elements and pages. This makes it suitable for UI pagination with \"Page 3 of 47\" indicators but expensive on large tables where COUNT scans the full index. Slice<T> fetches pageSize+1 rows, uses the extra row to determine if a next page exists, then discards it — no COUNT query. Use Slice<T> for infinite scroll, mobile feeds, or any scenario where total count is unnecessary. For very large datasets (100M+ rows), even Slice with offset-based pagination is slow; cursor-based pagination (keyset pagination) using a WHERE id > lastSeenId is the production solution.","correct":"Page<T> extends Slice<T> and additionally includes a COUNT(*) query to determine the total number of elements and pages.","distractors":["CrudRepository provides basic CRUD: save, findById, findAll, delete, count, existsById.","Use @DataJpaTest which loads only the JPA slice: entity classes, repositories, Flyway/Liquibase, and an embedded H2 database (unless replaced).","@Modifying marks a @Query method as an UPDATE or DELETE operation, which causes Spring Data to execute it as an executeUpdate() call instead of getResultList()."],"distractorIds":["spring/spring_data_jpa#ced7c8823613","spring/spring_data_jpa#50ae5029ab96","spring/spring_data_jpa#ca24b649eb71"],"concepts":["additionally","slice","difference","determine","includes","extends"]},{"id":"spring/spring_data_jpa#e60e9b296e91","section":"spring","module":"spring/spring_data_jpa","moduleName":"spring data jpa","sourceFile":"README.md","difficulty":"intermediate","question":"Explain interface-based projections and how Spring implements them.","answerFull":"Interface-based projections are interfaces declaring getters for a subset of entity fields. Spring Data generates a JDK dynamic proxy at runtime that implements the interface. When the query executes, Hibernate fetches only the columns needed to populate the declared getters (using a constructor-style result mapping). This reduces data transfer and avoids hydrating full entity objects. Open projections use @Value(\"#{target.field1 + ' ' + target.field2}\") for computed properties using SpEL. The limitation of open projections is that Spring must load the full entity to evaluate SpEL expressions, negating the column-reduction benefit of closed projections.","correct":"Interface-based projections are interfaces declaring getters for a subset of entity fields.","distractors":["Use @DataJpaTest which loads only the JPA slice: entity classes, repositories, Flyway/Liquibase, and an embedded H2 database (unless replaced).","JpaRepository.save() checks if the entity is new using SimpleJpaRepository.isNew(), which by default checks if the ID field is null.","Specification is a Spring Data abstraction over JPA Criteria API."],"distractorIds":["spring/spring_data_jpa#50ae5029ab96","spring/spring_data_jpa#929b5d17b6f7","spring/spring_data_jpa#40a55af735f8"],"concepts":["getters","declaring","projections","explain","interfaces","subset"]},{"id":"spring/spring_data_jpa#40a55af735f8","section":"spring","module":"spring/spring_data_jpa","moduleName":"spring data jpa","sourceFile":"README.md","difficulty":"advanced","question":"What is a Specification and when should you use it instead of derived query methods?","answerFull":"Specification is a Spring Data abstraction over JPA Criteria API. It encapsulates a single predicate as a functional interface: Specification<T> is (Root<T>, CriteriaQuery<?>, CriteriaBuilder) -> Predicate. Specifications compose with .and(), .or(), .not(). Use Specifications when query predicates are dynamic (optional filters in a search form), when the combination of conditions is not known at compile time, or when you want to unit test individual predicates in isolation. Avoid derived query methods beyond 3-4 conditions — a method like findByStatusAndRegionAndAgeGreaterThanAndEmailLike becomes unmaintainable. Specifications keep that logic readable and composable.","correct":"Specification is a Spring Data abstraction over JPA Criteria API.","distractors":["Page<T> extends Slice<T> and additionally includes a COUNT(*) query to determine the total number of elements and pages.","@Modifying marks a @Query method as an UPDATE or DELETE operation, which causes Spring Data to execute it as an executeUpdate() call instead of getResultList().","Use @DataJpaTest which loads only the JPA slice: entity classes, repositories, Flyway/Liquibase, and an embedded H2 database (unless replaced)."],"distractorIds":["spring/spring_data_jpa#303a47ebc7dc","spring/spring_data_jpa#ca24b649eb71","spring/spring_data_jpa#50ae5029ab96"],"concepts":["specification","jpa","criteria","abstraction","derived","methods"]},{"id":"spring/spring_data_jpa#929b5d17b6f7","section":"spring","module":"spring/spring_data_jpa","moduleName":"spring data jpa","sourceFile":"README.md","difficulty":"advanced","question":"What happens if you call save() on an entity with an ID that already exists in the database?","answerFull":"JpaRepository.save() checks if the entity is new using SimpleJpaRepository.isNew(), which by default checks if the ID field is null. If the ID is non-null, save() calls EntityManager.merge() (not persist()). merge() issues a SELECT to load the current state, then merges the passed object's state over it and returns a managed copy. This means two things: (1) you must use the returned entity, not the passed-in argument; (2) if you construct an entity with an ID but do not populate all fields, merge() will overwrite existing fields with nulls. The correct pattern for updates is to load the managed entity, mutate it, and rely on dirty checking.","correct":"JpaRepository.save() checks if the entity is new using SimpleJpaRepository.isNew(), which by default checks if the ID field is null.","distractors":["You declare a @Version field (Long, Integer, or Timestamp) on the entity.","Use @DataJpaTest which loads only the JPA slice: entity classes, repositories, Flyway/Liquibase, and an embedded H2 database (unless replaced).","@Modifying marks a @Query method as an UPDATE or DELETE operation, which causes Spring Data to execute it as an executeUpdate() call instead of getResultList()."],"distractorIds":["spring/spring_data_jpa#39714fb1ac15","spring/spring_data_jpa#50ae5029ab96","spring/spring_data_jpa#ca24b649eb71"],"concepts":["isnew","jparepository","simplejparepository","save","null","exists"]},{"id":"spring/spring_data_jpa#80818fda217d","section":"spring","module":"spring/spring_data_jpa","moduleName":"spring data jpa","sourceFile":"README.md","difficulty":"advanced","question":"How do you implement soft deletes with Spring Data JPA?","answerFull":"Use @SQLDelete and @Where annotations from Hibernate. @SQLDelete overrides the DELETE SQL with an UPDATE that sets a deleted_at column. @Where appends a condition to all SELECT queries for the entity. Combined, every delete becomes a soft delete and every query automatically filters deleted records. Example: java @Entity @SQLDelete(sql = \"UPDATE user SET deleted_at = NOW() WHERE id = ?\") @Where(clause = \"deleted_at IS NULL\") public class User { ... } For Spring Data 3.x with Hibernate 6, use @SoftDelete (new in Hibernate 6.4) which provides first-class soft delete support. Custom repositories can expose a findAllIncludingDeleted method using EntityManager with a different query that bypasses the @Where filter.","correct":"Use @SQLDelete and @Where annotations from Hibernate.","distractors":["@Modifying marks a @Query method as an UPDATE or DELETE operation, which causes Spring Data to execute it as an executeUpdate() call instead of getResultList().","Use @DataJpaTest which loads only the JPA slice: entity classes, repositories, Flyway/Liquibase, and an embedded H2 database (unless replaced).","Option A: Load the managed entity, set only the changed fields, and let dirty checking generate a partial UPDATE (Hibernate with @DynamicUpdate only updates changed columns)."],"distractorIds":["spring/spring_data_jpa#ca24b649eb71","spring/spring_data_jpa#50ae5029ab96","spring/spring_data_jpa#613ce53090ae"],"concepts":["sqldelete","hibernate","deletes","jpa","annotations","soft"],"answerFullMd":"Use @SQLDelete and @Where annotations from Hibernate. @SQLDelete overrides the DELETE SQL with an UPDATE that sets a deleted_at column. @Where appends a condition to all SELECT queries for the entity. Combined, every delete becomes a soft delete and every query automatically filters deleted records. Example: ```java @Entity @SQLDelete(sql = \"UPDATE user SET deleted_at = NOW() WHERE id = ?\") @Where(clause = \"deleted_at IS NULL\") public class User { ... } ``` For Spring Data 3.x with Hibernate 6, use @SoftDelete (new in Hibernate 6.4) which provides first-class soft delete support. Custom repositories can expose a findAllIncludingDeleted method using EntityManager with a different query that bypasses the @Where filter."},{"id":"spring/spring_data_jpa#8e625aef3bae","section":"spring","module":"spring/spring_data_jpa","moduleName":"spring data jpa","sourceFile":"README.md","difficulty":"advanced","question":"How does Spring Data JPA auditing work and what is AuditorAware?","answerFull":"@EnableJpaAuditing activates the AuditingEntityListener. Fields annotated with @CreatedDate, @LastModifiedDate, @CreatedBy, @LastModifiedBy on @MappedSuperclass or the entity itself are populated automatically before insert/update via JPA entity lifecycle callbacks (@PrePersist, @PreUpdate). @CreatedDate and @LastModifiedDate use the clock configured in @EnableJpaAuditing(dateTimeProviderRef=). @CreatedBy and @LastModifiedBy call AuditorAware.getCurrentAuditor(), which you implement to return the current user (typically from SecurityContextHolder). The key constraint is that @CreatedDate and @CreatedBy fields should be @Column(updatable=false) to prevent modification after initial creation.","correct":"@EnableJpaAuditing activates the AuditingEntityListener.","distractors":["Use @DataJpaTest which loads only the JPA slice: entity classes, repositories, Flyway/Liquibase, and an embedded H2 database (unless replaced).","Specification is a Spring Data abstraction over JPA Criteria API.","@Modifying marks a @Query method as an UPDATE or DELETE operation, which causes Spring Data to execute it as an executeUpdate() call instead of getResultList()."],"distractorIds":["spring/spring_data_jpa#50ae5029ab96","spring/spring_data_jpa#40a55af735f8","spring/spring_data_jpa#ca24b649eb71"],"concepts":["auditing","auditingentitylistener","enablejpaauditing","activates","jpa","work"]},{"id":"spring/spring_data_jpa#17143ce7643f","section":"spring","module":"spring/spring_data_jpa","moduleName":"spring data jpa","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between getById/getOne and findById?","answerFull":"findById(id) executes SELECT immediately and returns Optional<T>. It returns Optional.empty() if the row does not exist. getById(id) (formerly getOne in older Spring Data) returns a Hibernate proxy without hitting the database — the SELECT fires lazily when you access a non-id field. If the row does not exist and you access the proxy, Hibernate throws EntityNotFoundException. Use findById when you need the entity immediately or want to check existence. Use getById/getReferenceById when setting a foreign key relationship (e.g., setting order.setCustomer(customerRepository.getReferenceById(customerId))) to avoid an unnecessary SELECT — you just need the proxy reference for the FK column.","correct":"findById(id) executes SELECT immediately and returns Optional<T>.","distractors":["N+1 occurs when loading N parent entities triggers N additional SELECT statements to load their lazy-fetched child collections.","findById issues a SELECT immediately and returns Optional<T> (empty if absent).","CrudRepository provides basic CRUD: save, findById, findAll, delete, count, existsById."],"distractorIds":["spring/spring_data_jpa#b491b53e342b","spring/spring_data_jpa#c5b673c539f9","spring/spring_data_jpa#ced7c8823613"],"concepts":["findbyid","difference","optional","select","executes","immediately"],"distractorsMd":[null,"`findById` issues a SELECT immediately and returns `Optional<T>` (empty if absent).",null]},{"id":"spring/spring_data_jpa#613ce53090ae","section":"spring","module":"spring/spring_data_jpa","moduleName":"spring data jpa","sourceFile":"README.md","difficulty":"advanced","question":"How would you handle a scenario where you need to update only specific fields of an entity?","answerFull":"Option A: Load the managed entity, set only the changed fields, and let dirty checking generate a partial UPDATE (Hibernate with @DynamicUpdate only updates changed columns). Option B: Use @Modifying @Query with explicit SET clause — precise but requires separate queries per update pattern. Option C: Use DTO-based partial updates with a custom repository method using EntityManager.createQuery(). Option D (Hibernate 6+): Use the HQL UPDATE statement with SET. @DynamicUpdate on the entity class makes Hibernate include only dirty columns in the SQL, reducing bandwidth and avoiding overwriting concurrent changes to other fields: java @Entity @DynamicUpdate public class User { // Hibernate will only UPDATE columns that actually changed }","correct":"Option A: Load the managed entity, set only the changed fields, and let dirty checking generate a partial UPDATE (Hibernate with @DynamicUpdate only updates changed columns).","distractors":["Use @SQLDelete and @Where annotations from Hibernate.","The persistence context is a first-level cache managed by the EntityManager within a transaction.","JpaRepository.save() checks if the entity is new using SimpleJpaRepository.isNew(), which by default checks if the ID field is null."],"distractorIds":["spring/spring_data_jpa#80818fda217d","spring/spring_data_jpa#1ce1c8428821","spring/spring_data_jpa#929b5d17b6f7"],"concepts":["dynamicupdate","hibernate","scenario","option","dirty","checking"],"answerFullMd":"Option A: Load the managed entity, set only the changed fields, and let dirty checking generate a partial UPDATE (Hibernate with @DynamicUpdate only updates changed columns). Option B: Use @Modifying @Query with explicit SET clause — precise but requires separate queries per update pattern. Option C: Use DTO-based partial updates with a custom repository method using EntityManager.createQuery(). Option D (Hibernate 6+): Use the HQL UPDATE statement with SET. @DynamicUpdate on the entity class makes Hibernate include only dirty columns in the SQL, reducing bandwidth and avoiding overwriting concurrent changes to other fields: ```java @Entity @DynamicUpdate public class User { // Hibernate will only UPDATE columns that actually changed } ```"},{"id":"spring/spring_data_jpa#50ae5029ab96","section":"spring","module":"spring/spring_data_jpa","moduleName":"spring data jpa","sourceFile":"README.md","difficulty":"advanced","question":"How do you test Spring Data JPA repositories?","answerFull":"Use @DataJpaTest which loads only the JPA slice: entity classes, repositories, Flyway/Liquibase, and an embedded H2 database (unless replaced). It wraps each test in a transaction that rolls back after the test, keeping tests isolated. For testing against the real database engine (recommended for complex queries, JSON columns, window functions), use Testcontainers with @DynamicPropertySource to point spring.datasource.url at the container. Use AssertJ for fluent assertions on Optional and collections. Test projection methods by asserting that the returned DTO/interface only exposes declared fields. Test Specifications by verifying they return correct results for each predicate in isolation.","correct":"Use @DataJpaTest which loads only the JPA slice: entity classes, repositories, Flyway/Liquibase, and an embedded H2 database (unless replaced).","distractors":["Interface-based projections are interfaces declaring getters for a subset of entity fields.","Page<T> extends Slice<T> and additionally includes a COUNT(*) query to determine the total number of elements and pages.","@Modifying marks a @Query method as an UPDATE or DELETE operation, which causes Spring Data to execute it as an executeUpdate() call instead of getResultList()."],"distractorIds":["spring/spring_data_jpa#e60e9b296e91","spring/spring_data_jpa#303a47ebc7dc","spring/spring_data_jpa#ca24b649eb71"],"concepts":["liquibase","repositories","datajpatest","flyway","jpa","replaced"]},{"id":"spring/spring_data_jpa#235f1f442ed5","section":"spring","module":"spring/spring_data_jpa","moduleName":"spring data jpa","sourceFile":"README.md","difficulty":"advanced","question":"What is the Open Session in View (OSIV) pattern and why is it controversial?","answerFull":"OSIV keeps the EntityManager (Session) open for the entire HTTP request lifecycle, including the view rendering phase. In Spring Boot, spring.jpa.open-in-view=true is the default (a warning is logged). This allows lazy loading in view templates without LazyInitializationException. It is controversial because: (1) database connections are held for the full request duration including slow rendering, starving the connection pool under load; (2) it encourages lazy-loaded queries scattered throughout the view layer, making N+1 problems invisible during development; (3) it mixes infrastructure concerns into the presentation layer. The correct approach is to explicitly fetch all needed data in the service layer (within a transaction), project to DTOs, and return closed/immutable objects to the controller. Disable OSIV in production: spring.jpa.open-in-view=false.","correct":"OSIV keeps the EntityManager (Session) open for the entire HTTP request lifecycle, including the view rendering phase.","distractors":["The persistence context is a first-level cache managed by the EntityManager within a transaction.","@EnableJpaAuditing activates the AuditingEntityListener.","@Modifying marks a @Query method as an UPDATE or DELETE operation, which causes Spring Data to execute it as an executeUpdate() call instead of getResultList()."],"distractorIds":["spring/spring_data_jpa#1ce1c8428821","spring/spring_data_jpa#8e625aef3bae","spring/spring_data_jpa#ca24b649eb71"],"concepts":["osiv","entitymanager","rendering","lifecycle","view","phase"]},{"id":"spring/spring_data_jpa#c5b673c539f9","section":"spring","module":"spring/spring_data_jpa","moduleName":"spring data jpa","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between getReferenceById (the old getOne) and findById, and when does it bite you?","answerFull":"findById issues a SELECT immediately and returns Optional<T> (empty if absent). getReferenceById returns a lazy *proxy* without hitting the database, deferring the SELECT until you first access a non-id property; it is used to set an association (order.setCustomer(repo.getReferenceById(id))) without loading the customer. The trap: if the id does not exist, you get no error at call time — instead a EntityNotFoundException is thrown later, often outside the transaction or during serialization, where it is confusing to diagnose. Also, accessing the proxy after the session closes throws LazyInitializationException. Use getReferenceById only to wire a foreign key you trust exists; use findById when you actually need the data or must validate existence.","correct":"findById issues a SELECT immediately and returns Optional<T> (empty if absent).","distractors":["CrudRepository provides basic CRUD: save, findById, findAll, delete, count, existsById.","N+1 occurs when loading N parent entities triggers N additional SELECT statements to load their lazy-fetched child collections.","Pessimistic locking is appropriate when contention is high and retry overhead would degrade user experience or cause thundering herd issues."],"distractorIds":["spring/spring_data_jpa#ced7c8823613","spring/spring_data_jpa#b491b53e342b","spring/spring_data_jpa#b55bba210018"],"concepts":["findbyid","absent","difference","empty","issues","optional"],"questionMd":"What is the difference between `getReferenceById` (the old `getOne`) and `findById`, and when does it bite you?","correctMd":"`findById` issues a SELECT immediately and returns `Optional<T>` (empty if absent).","answerFullMd":"`findById` issues a SELECT immediately and returns `Optional<T>` (empty if absent). `getReferenceById` returns a lazy *proxy* without hitting the database, deferring the SELECT until you first access a non-id property; it is used to set an association (`order.setCustomer(repo.getReferenceById(id))`) without loading the customer. The trap: if the id does not exist, you get no error at call time — instead a `EntityNotFoundException` is thrown later, often outside the transaction or during serialization, where it is confusing to diagnose. Also, accessing the proxy after the session closes throws `LazyInitializationException`. Use `getReferenceById` only to wire a foreign key you trust exists; use `findById` when you actually need the data or must validate existence."},{"id":"spring/spring_events_and_scheduling#ae99335712dd","section":"spring","module":"spring/spring_events_and_scheduling","moduleName":"spring events and scheduling","sourceFile":"README.md","difficulty":"core","question":"What is the difference between @EventListener and @TransactionalEventListener?","answerFull":"@EventListener fires synchronously inline within the publisher's call stack, participating in the same transaction (if any). @TransactionalEventListener binds the listener to a transaction lifecycle phase — defaulting to AFTER_COMMIT. If the publisher's transaction rolls back, @TransactionalEventListener(AFTER_COMMIT) listeners never fire; @EventListener listeners would have already fired and cannot be rolled back. Use @TransactionalEventListener for any side effect that must only happen if the primary operation persisted — sending an email after a successful order, pushing an event to Kafka after a DB write.","correct":"@EventListener fires synchronously inline within the publisher's call stack, participating in the same transaction (if any).","distractors":["By default, async exceptions are logged by Spring's SimpleAsyncUncaughtExceptionHandler at WARN level and silently swallowed — the publisher's thread never sees them.","@PostConstruct fires during the Spring context refresh, before the embedded server (Tomcat/Netty) starts accepting requests.","The new transaction's failure does not affect the original transaction — it has already committed."],"distractorIds":["spring/spring_events_and_scheduling#323245810cdf","spring/spring_events_and_scheduling#e7870be57238","spring/spring_events_and_scheduling#cbec804b7b1e"],"concepts":["eventlistener","participating","transactionaleventlistener","synchronously","difference","fires"],"questionMd":"What is the difference between `@EventListener` and `@TransactionalEventListener`?","correctMd":"`@EventListener` fires synchronously inline within the publisher's call stack, participating in the same transaction (if any).","answerFullMd":"`@EventListener` fires synchronously inline within the publisher's call stack, participating in the same transaction (if any). `@TransactionalEventListener` binds the listener to a transaction lifecycle phase — defaulting to `AFTER_COMMIT`. If the publisher's transaction rolls back, `@TransactionalEventListener(AFTER_COMMIT)` listeners never fire; `@EventListener` listeners would have already fired and cannot be rolled back. Use `@TransactionalEventListener` for any side effect that must only happen if the primary operation persisted — sending an email after a successful order, pushing an event to Kafka after a DB write.","distractorsMd":["By default, async exceptions are logged by Spring's `SimpleAsyncUncaughtExceptionHandler` at `WARN` level and silently swallowed — the publisher's thread never sees them.","`@PostConstruct` fires during the Spring context refresh, before the embedded server (Tomcat/Netty) starts accepting requests.",null]},{"id":"spring/spring_events_and_scheduling#4fee9a1bfa84","section":"spring","module":"spring/spring_events_and_scheduling","moduleName":"spring events and scheduling","sourceFile":"README.md","difficulty":"core","question":"Why does @TransactionalEventListener silently not fire in some situations, and how do you fix it?","answerFull":"@TransactionalEventListener requires an active transaction to bind to. If the event is published outside a transaction (from a test, a scheduler, or a non-@Transactional method), the event is silently discarded. Diagnosis: add fallbackExecution = true to the annotation — this makes it fire even without a surrounding transaction. Alternatively, ensure the publishing code is always called within a @Transactional method. The silent discard is a frequent source of \"event listener not called\" bugs in test environments where the service method is called without the transaction proxy active.","correct":"@TransactionalEventListener requires an active transaction to bind to.","distractors":["@EventListener fires synchronously inline within the publisher's call stack, participating in the same transaction (if any).","By default, async exceptions are logged by Spring's SimpleAsyncUncaughtExceptionHandler at WARN level and silently swallowed — the publisher's thread never sees them.","The new transaction's failure does not affect the original transaction — it has already committed."],"distractorIds":["spring/spring_events_and_scheduling#ae99335712dd","spring/spring_events_and_scheduling#323245810cdf","spring/spring_events_and_scheduling#cbec804b7b1e"],"concepts":["situations","transactionaleventlistener","fire","bind","fix","silently"],"questionMd":"Why does `@TransactionalEventListener` silently not fire in some situations, and how do you fix it?","correctMd":"`@TransactionalEventListener` requires an active transaction to bind to.","answerFullMd":"`@TransactionalEventListener` requires an active transaction to bind to. If the event is published outside a transaction (from a test, a scheduler, or a non-`@Transactional` method), the event is silently discarded. Diagnosis: add `fallbackExecution = true` to the annotation — this makes it fire even without a surrounding transaction. Alternatively, ensure the publishing code is always called within a `@Transactional` method. The silent discard is a frequent source of \"event listener not called\" bugs in test environments where the service method is called without the transaction proxy active.","distractorsMd":["`@EventListener` fires synchronously inline within the publisher's call stack, participating in the same transaction (if any).","By default, async exceptions are logged by Spring's `SimpleAsyncUncaughtExceptionHandler` at `WARN` level and silently swallowed — the publisher's thread never sees them.",null]},{"id":"spring/spring_events_and_scheduling#323245810cdf","section":"spring","module":"spring/spring_events_and_scheduling","moduleName":"spring events and scheduling","sourceFile":"README.md","difficulty":"core","question":"What happens when an @Async listener throws an exception?","answerFull":"By default, async exceptions are logged by Spring's SimpleAsyncUncaughtExceptionHandler at WARN level and silently swallowed — the publisher's thread never sees them. This can cause data inconsistencies: the primary transaction committed, the async listener failed, and the side effect never happened. Fix: implement AsyncConfigurer.getAsyncUncaughtExceptionHandler() to return a handler that alerts on failure, retries, or writes to a DLQ. For critical side effects, consider using @TransactionalEventListener with a persistent message broker (Kafka, RabbitMQ) instead of @Async.","correct":"By default, async exceptions are logged by Spring's SimpleAsyncUncaughtExceptionHandler at WARN level and silently swallowed — the publisher's thread never sees them.","distractors":["SecurityContextHolder is ThreadLocal-based — the async thread has a fresh, empty security context by default.","Spring's default scheduler uses a single thread (ScheduledThreadPoolExecutor with corePoolSize=1).","With spring.threads.virtual.enabled=true, Spring Boot 3.2 replaces the default ThreadPoolTaskExecutor for @Async with a VirtualThreadTaskExecutor (Executors.newVirtualThreadPerTaskExecutor())."],"distractorIds":["spring/spring_events_and_scheduling#6e11c871178a","spring/spring_events_and_scheduling#f9e1126f3f7a","spring/spring_events_and_scheduling#41505c76d33e"],"concepts":["simpleasyncuncaughtexceptionhandler","swallowed","listener","warn","publisher","logged"],"questionMd":"What happens when an `@Async` listener throws an exception?","correctMd":"By default, async exceptions are logged by Spring's `SimpleAsyncUncaughtExceptionHandler` at `WARN` level and silently swallowed — the publisher's thread never sees them.","answerFullMd":"By default, async exceptions are logged by Spring's `SimpleAsyncUncaughtExceptionHandler` at `WARN` level and silently swallowed — the publisher's thread never sees them. This can cause data inconsistencies: the primary transaction committed, the async listener failed, and the side effect never happened. Fix: implement `AsyncConfigurer.getAsyncUncaughtExceptionHandler()` to return a handler that alerts on failure, retries, or writes to a DLQ. For critical side effects, consider using `@TransactionalEventListener` with a persistent message broker (Kafka, RabbitMQ) instead of `@Async`.","distractorsMd":["`SecurityContextHolder` is `ThreadLocal`-based — the async thread has a fresh, empty security context by default.","Spring's default scheduler uses a single thread (`ScheduledThreadPoolExecutor` with `corePoolSize=1`).","With `spring.threads.virtual.enabled=true`, Spring Boot 3.2 replaces the default `ThreadPoolTaskExecutor` for `@Async` with a `VirtualThreadTaskExecutor` (`Executors.newVirtualThreadPerTaskExecutor()`)."]},{"id":"spring/spring_events_and_scheduling#19cb5ccfadcc","section":"spring","module":"spring/spring_events_and_scheduling","moduleName":"spring events and scheduling","sourceFile":"README.md","difficulty":"core","question":"How does ShedLock prevent multiple instances from running a scheduled task, and what are lockAtLeastFor and lockAtMostFor?","answerFull":"ShedLock uses an INSERT ... WHERE lock_until < NOW() statement (or equivalent) in the backing store (JDBC table, Redis key, MongoDB document). Only one JVM instance succeeds in inserting/updating the lock row; all others get a constraint violation and skip the task for that scheduled run. lockAtLeastFor is the minimum duration the lock is held — even if the task finishes quickly, other instances cannot start until this duration expires (prevents near-simultaneous re-runs from two pods). lockAtMostFor is the safety valve: if the lock-holder JVM crashes without releasing the lock, the lock is released after this duration so other instances can take over. Always set lockAtMostFor to prevent permanent lock-up.","correct":"ShedLock uses an INSERT ...","distractors":["@EventListener fires synchronously inline within the publisher's call stack, participating in the same transaction (if any).","@PostConstruct fires during the Spring context refresh, before the embedded server (Tomcat/Netty) starts accepting requests.","@TransactionalEventListener requires an active transaction to bind to."],"distractorIds":["spring/spring_events_and_scheduling#ae99335712dd","spring/spring_events_and_scheduling#e7870be57238","spring/spring_events_and_scheduling#4fee9a1bfa84"],"concepts":["shedlock","prevent","scheduled","insert","instances","running"],"questionMd":"How does ShedLock prevent multiple instances from running a scheduled task, and what are `lockAtLeastFor` and `lockAtMostFor`?","answerFullMd":"ShedLock uses an `INSERT ... WHERE lock_until < NOW()` statement (or equivalent) in the backing store (JDBC table, Redis key, MongoDB document). Only one JVM instance succeeds in inserting/updating the lock row; all others get a constraint violation and skip the task for that scheduled run. `lockAtLeastFor` is the minimum duration the lock is held — even if the task finishes quickly, other instances cannot start until this duration expires (prevents near-simultaneous re-runs from two pods). `lockAtMostFor` is the safety valve: if the lock-holder JVM crashes without releasing the lock, the lock is released after this duration so other instances can take over. Always set `lockAtMostFor` to prevent permanent lock-up.","distractorsMd":["`@EventListener` fires synchronously inline within the publisher's call stack, participating in the same transaction (if any).","`@PostConstruct` fires during the Spring context refresh, before the embedded server (Tomcat/Netty) starts accepting requests.","`@TransactionalEventListener` requires an active transaction to bind to."]},{"id":"spring/spring_events_and_scheduling#7fa02801888c","section":"spring","module":"spring/spring_events_and_scheduling","moduleName":"spring events and scheduling","sourceFile":"README.md","difficulty":"core","question":"Can you use @Transactional on an @Async method, and what are the pitfalls?","answerFull":"Yes, but with important caveats. An @Async method runs on a different thread with no transaction context from the caller. Adding @Transactional on the async method opens a new transaction on that thread — the async method's transaction is completely independent. Pitfall 1: the caller's transaction commits before the async method starts, so the async method sees the committed state (usually desired). Pitfall 2: if the async method's transaction fails, the caller's transaction is NOT rolled back — they are independent. Pitfall 3: SecurityContextHolder (which is ThreadLocal) is not automatically propagated to the async thread — configure DelegatingSecurityContextAsyncTaskExecutor to propagate security context.","correct":"Yes, but with important caveats.","distractors":["With spring.threads.virtual.enabled=true, Spring Boot 3.2 replaces the default ThreadPoolTaskExecutor for @Async with a VirtualThreadTaskExecutor (Executors.newVirtualThreadPerTaskExecutor()).","Spring application events are in-process, in-memory, synchronous (or async via @Async) — they live and die with the JVM.","SecurityContextHolder is ThreadLocal-based — the async thread has a fresh, empty security context by default."],"distractorIds":["spring/spring_events_and_scheduling#41505c76d33e","spring/spring_events_and_scheduling#5bb0f0891e51","spring/spring_events_and_scheduling#6e11c871178a"],"concepts":["caveats","important","transactional","async","yes","method"],"questionMd":"Can you use `@Transactional` on an `@Async` method, and what are the pitfalls?","answerFullMd":"Yes, but with important caveats. An `@Async` method runs on a different thread with no transaction context from the caller. Adding `@Transactional` on the async method opens a new transaction on that thread — the async method's transaction is completely independent. Pitfall 1: the caller's transaction commits before the async method starts, so the async method sees the committed state (usually desired). Pitfall 2: if the async method's transaction fails, the caller's transaction is NOT rolled back — they are independent. Pitfall 3: `SecurityContextHolder` (which is `ThreadLocal`) is not automatically propagated to the async thread — configure `DelegatingSecurityContextAsyncTaskExecutor` to propagate security context.","distractorsMd":["With `spring.threads.virtual.enabled=true`, Spring Boot 3.2 replaces the default `ThreadPoolTaskExecutor` for `@Async` with a `VirtualThreadTaskExecutor` (`Executors.newVirtualThreadPerTaskExecutor()`).","Spring application events are in-process, in-memory, synchronous (or async via `@Async`) — they live and die with the JVM.","`SecurityContextHolder` is `ThreadLocal`-based — the async thread has a fresh, empty security context by default."]},{"id":"spring/spring_events_and_scheduling#f9e1126f3f7a","section":"spring","module":"spring/spring_events_and_scheduling","moduleName":"spring events and scheduling","sourceFile":"README.md","difficulty":"intermediate","question":"What is the default scheduling thread pool size, and why does it matter?","answerFull":"Spring's default scheduler uses a single thread (ScheduledThreadPoolExecutor with corePoolSize=1). This means all @Scheduled methods share one thread and execute sequentially. If one task is long-running or slow, it delays all other tasks. Configure a ThreadPoolTaskScheduler bean with setPoolSize(N) to allow N tasks to run concurrently. Choose N based on expected concurrent scheduled tasks — typically 5–20 for a service with a dozen scheduled jobs. Without explicit configuration, a slow nightly job can delay a critical 30-second health check.","correct":"Spring's default scheduler uses a single thread (ScheduledThreadPoolExecutor with corePoolSize=1).","distractors":["@PostConstruct fires during the Spring context refresh, before the embedded server (Tomcat/Netty) starts accepting requests.","By default, async exceptions are logged by Spring's SimpleAsyncUncaughtExceptionHandler at WARN level and silently swallowed — the publisher's thread never sees them.","With spring.threads.virtual.enabled=true, Spring Boot 3.2 replaces the default ThreadPoolTaskExecutor for @Async with a VirtualThreadTaskExecutor (Executors.newVirtualThreadPerTaskExecutor())."],"distractorIds":["spring/spring_events_and_scheduling#e7870be57238","spring/spring_events_and_scheduling#323245810cdf","spring/spring_events_and_scheduling#41505c76d33e"],"concepts":["corepoolsize","scheduledthreadpoolexecutor","matter","scheduling","scheduler","pool"],"correctMd":"Spring's default scheduler uses a single thread (`ScheduledThreadPoolExecutor` with `corePoolSize=1`).","answerFullMd":"Spring's default scheduler uses a single thread (`ScheduledThreadPoolExecutor` with `corePoolSize=1`). This means all `@Scheduled` methods share one thread and execute sequentially. If one task is long-running or slow, it delays all other tasks. Configure a `ThreadPoolTaskScheduler` bean with `setPoolSize(N)` to allow N tasks to run concurrently. Choose N based on expected concurrent scheduled tasks — typically 5–20 for a service with a dozen scheduled jobs. Without explicit configuration, a slow nightly job can delay a critical 30-second health check.","distractorsMd":["`@PostConstruct` fires during the Spring context refresh, before the embedded server (Tomcat/Netty) starts accepting requests.","By default, async exceptions are logged by Spring's `SimpleAsyncUncaughtExceptionHandler` at `WARN` level and silently swallowed — the publisher's thread never sees them.","With `spring.threads.virtual.enabled=true`, Spring Boot 3.2 replaces the default `ThreadPoolTaskExecutor` for `@Async` with a `VirtualThreadTaskExecutor` (`Executors.newVirtualThreadPerTaskExecutor()`)."]},{"id":"spring/spring_events_and_scheduling#e7870be57238","section":"spring","module":"spring/spring_events_and_scheduling","moduleName":"spring events and scheduling","sourceFile":"README.md","difficulty":"intermediate","question":"What is ApplicationReadyEvent and why should it be preferred over @PostConstruct for post-startup work?","answerFull":"@PostConstruct fires during the Spring context refresh, before the embedded server (Tomcat/Netty) starts accepting requests. Code in @PostConstruct that takes a long time delays server startup. More importantly, if @PostConstruct code makes HTTP calls or accesses beans that haven't been initialised yet, it fails silently. ApplicationReadyEvent fires after the full application context is refreshed, all beans are initialised, and the embedded server is accepting requests. It is the correct hook for: cache pre-loading, connectivity checks, background initialisation threads, or any work that should start after the application is genuinely \"ready.\"","correct":"@PostConstruct fires during the Spring context refresh, before the embedded server (Tomcat/Netty) starts accepting requests.","distractors":["By default, async exceptions are logged by Spring's SimpleAsyncUncaughtExceptionHandler at WARN level and silently swallowed — the publisher's thread never sees them.","Spring application events are in-process, in-memory, synchronous (or async via @Async) — they live and die with the JVM.","With spring.threads.virtual.enabled=true, Spring Boot 3.2 replaces the default ThreadPoolTaskExecutor for @Async with a VirtualThreadTaskExecutor (Executors.newVirtualThreadPerTaskExecutor())."],"distractorIds":["spring/spring_events_and_scheduling#323245810cdf","spring/spring_events_and_scheduling#5bb0f0891e51","spring/spring_events_and_scheduling#41505c76d33e"],"concepts":["netty","postconstruct","accepting","tomcat","preferred","fires"],"questionMd":"What is `ApplicationReadyEvent` and why should it be preferred over `@PostConstruct` for post-startup work?","correctMd":"`@PostConstruct` fires during the Spring context refresh, before the embedded server (Tomcat/Netty) starts accepting requests.","answerFullMd":"`@PostConstruct` fires during the Spring context refresh, before the embedded server (Tomcat/Netty) starts accepting requests. Code in `@PostConstruct` that takes a long time delays server startup. More importantly, if `@PostConstruct` code makes HTTP calls or accesses beans that haven't been initialised yet, it fails silently. `ApplicationReadyEvent` fires after the full application context is refreshed, all beans are initialised, and the embedded server is accepting requests. It is the correct hook for: cache pre-loading, connectivity checks, background initialisation threads, or any work that should start after the application is genuinely \"ready.\"","distractorsMd":["By default, async exceptions are logged by Spring's `SimpleAsyncUncaughtExceptionHandler` at `WARN` level and silently swallowed — the publisher's thread never sees them.","Spring application events are in-process, in-memory, synchronous (or async via `@Async`) — they live and die with the JVM.","With `spring.threads.virtual.enabled=true`, Spring Boot 3.2 replaces the default `ThreadPoolTaskExecutor` for `@Async` with a `VirtualThreadTaskExecutor` (`Executors.newVirtualThreadPerTaskExecutor()`)."]},{"id":"spring/spring_events_and_scheduling#619108bc9b50","section":"spring","module":"spring/spring_events_and_scheduling","moduleName":"spring events and scheduling","sourceFile":"README.md","difficulty":"intermediate","question":"Explain the fixedRate vs fixedDelay difference and when an overlap can occur.","answerFull":"fixedDelay = N starts the next run N milliseconds after the previous run completes — if a run takes 8 seconds and fixedDelay=5000, the next run starts at 8+5=13s after the first started. fixedRate = N tries to maintain N milliseconds between start times — if a run takes longer than N, the next run starts immediately after the previous finishes (no overlap, but the schedule slips). If the scheduler has multiple threads (via ThreadPoolTaskScheduler), fixedRate can actually run multiple instances simultaneously if a run takes longer than the rate. This can cause concurrent modification issues in stateful tasks. Use fixedDelay for tasks that must not overlap; use fixedRate with a pool size of 1 (default) if you want constant-rate scheduling with sequential execution.","correct":"fixedDelay = N starts the next run N milliseconds after the previous run completes — if a run takes 8 seconds and fixedDelay=5000, the next run starts at 8+5=13s after the first started.","distractors":["@PostConstruct fires during the Spring context refresh, before the embedded server (Tomcat/Netty) starts accepting requests.","@EventListener fires synchronously inline within the publisher's call stack, participating in the same transaction (if any).","@TransactionalEventListener requires an active transaction to bind to."],"distractorIds":["spring/spring_events_and_scheduling#e7870be57238","spring/spring_events_and_scheduling#ae99335712dd","spring/spring_events_and_scheduling#4fee9a1bfa84"],"concepts":["13s","5000","fixeddelay","explain","started","milliseconds"],"questionMd":"Explain the `fixedRate` vs `fixedDelay` difference and when an overlap can occur.","correctMd":"`fixedDelay = N` starts the next run N milliseconds after the previous run completes — if a run takes 8 seconds and `fixedDelay=5000`, the next run starts at 8+5=13s after the first started.","answerFullMd":"`fixedDelay = N` starts the next run N milliseconds after the previous run completes — if a run takes 8 seconds and `fixedDelay=5000`, the next run starts at 8+5=13s after the first started. `fixedRate = N` tries to maintain N milliseconds between start times — if a run takes longer than N, the next run starts immediately after the previous finishes (no overlap, but the schedule slips). If the scheduler has multiple threads (via `ThreadPoolTaskScheduler`), `fixedRate` can actually run multiple instances simultaneously if a run takes longer than the rate. This can cause concurrent modification issues in stateful tasks. Use `fixedDelay` for tasks that must not overlap; use `fixedRate` with a pool size of 1 (default) if you want constant-rate scheduling with sequential execution.","distractorsMd":["`@PostConstruct` fires during the Spring context refresh, before the embedded server (Tomcat/Netty) starts accepting requests.","`@EventListener` fires synchronously inline within the publisher's call stack, participating in the same transaction (if any).","`@TransactionalEventListener` requires an active transaction to bind to."]},{"id":"spring/spring_events_and_scheduling#6e11c871178a","section":"spring","module":"spring/spring_events_and_scheduling","moduleName":"spring events and scheduling","sourceFile":"README.md","difficulty":"intermediate","question":"How does Spring's @Async propagate security context to async methods?","answerFull":"SecurityContextHolder is ThreadLocal-based — the async thread has a fresh, empty security context by default. To propagate the caller's Authentication: configure a DelegatingSecurityContextAsyncTaskExecutor wrapping your task executor. Spring Security provides this out of the box: java @Bean public Executor asyncExecutor() { return new DelegatingSecurityContextAsyncTaskExecutor( new ThreadPoolTaskExecutor() // your underlying executor ); } The DelegatingSecurityContextAsyncTaskExecutor captures the SecurityContext at submission time and sets it in the async thread before running the task, then clears it afterwards. Without this, async methods calling SecurityContextHolder.getContext() see AnonymousAuthenticationToken.","correct":"SecurityContextHolder is ThreadLocal-based — the async thread has a fresh, empty security context by default.","distractors":["By default, async exceptions are logged by Spring's SimpleAsyncUncaughtExceptionHandler at WARN level and silently swallowed — the publisher's thread never sees them.","Spring's default scheduler uses a single thread (ScheduledThreadPoolExecutor with corePoolSize=1).","With spring.threads.virtual.enabled=true, Spring Boot 3.2 replaces the default ThreadPoolTaskExecutor for @Async with a VirtualThreadTaskExecutor (Executors.newVirtualThreadPerTaskExecutor())."],"distractorIds":["spring/spring_events_and_scheduling#323245810cdf","spring/spring_events_and_scheduling#f9e1126f3f7a","spring/spring_events_and_scheduling#41505c76d33e"],"concepts":["securitycontextholder","propagate","threadlocal","fresh","empty","security"],"questionMd":"How does Spring's `@Async` propagate security context to async methods?","correctMd":"`SecurityContextHolder` is `ThreadLocal`-based — the async thread has a fresh, empty security context by default.","answerFullMd":"`SecurityContextHolder` is `ThreadLocal`-based — the async thread has a fresh, empty security context by default. To propagate the caller's `Authentication`: configure a `DelegatingSecurityContextAsyncTaskExecutor` wrapping your task executor. Spring Security provides this out of the box: ```java @Bean public Executor asyncExecutor() { return new DelegatingSecurityContextAsyncTaskExecutor( new ThreadPoolTaskExecutor() // your underlying executor ); } ``` The `DelegatingSecurityContextAsyncTaskExecutor` captures the `SecurityContext` at submission time and sets it in the async thread before running the task, then clears it afterwards. Without this, async methods calling `SecurityContextHolder.getContext()` see `AnonymousAuthenticationToken`.","distractorsMd":["By default, async exceptions are logged by Spring's `SimpleAsyncUncaughtExceptionHandler` at `WARN` level and silently swallowed — the publisher's thread never sees them.","Spring's default scheduler uses a single thread (`ScheduledThreadPoolExecutor` with `corePoolSize=1`).","With `spring.threads.virtual.enabled=true`, Spring Boot 3.2 replaces the default `ThreadPoolTaskExecutor` for `@Async` with a `VirtualThreadTaskExecutor` (`Executors.newVirtualThreadPerTaskExecutor()`)."]},{"id":"spring/spring_events_and_scheduling#5bb0f0891e51","section":"spring","module":"spring/spring_events_and_scheduling","moduleName":"spring events and scheduling","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between publishing a Spring event and sending a message to Kafka/RabbitMQ?","answerFull":"Spring application events are in-process, in-memory, synchronous (or async via @Async) — they live and die with the JVM. If the application crashes between publishing and a listener firing, the event is lost. Kafka/RabbitMQ messages are durable, cross-process, and replayed after failure. Use Spring events for: in-process decoupling, cache invalidation, audit logging, and side effects that do not need durability. Use Kafka/RabbitMQ for: cross-service communication, durable event sourcing, at-least-once delivery guarantees, and fan-out to multiple services. The outbox pattern bridges the gap: write the event to a DB table in the same transaction, then a relay process publishes it to Kafka — giving transactional guarantees + durability.","correct":"Spring application events are in-process, in-memory, synchronous (or async via @Async) — they live and die with the JVM.","distractors":["With spring.threads.virtual.enabled=true, Spring Boot 3.2 replaces the default ThreadPoolTaskExecutor for @Async with a VirtualThreadTaskExecutor (Executors.newVirtualThreadPerTaskExecutor()).","By default, async exceptions are logged by Spring's SimpleAsyncUncaughtExceptionHandler at WARN level and silently swallowed — the publisher's thread never sees them.","SecurityContextHolder is ThreadLocal-based — the async thread has a fresh, empty security context by default."],"distractorIds":["spring/spring_events_and_scheduling#41505c76d33e","spring/spring_events_and_scheduling#323245810cdf","spring/spring_events_and_scheduling#6e11c871178a"],"concepts":["die","rabbitmq","publishing","difference","live","sending"],"correctMd":"Spring application events are in-process, in-memory, synchronous (or async via `@Async`) — they live and die with the JVM.","answerFullMd":"Spring application events are in-process, in-memory, synchronous (or async via `@Async`) — they live and die with the JVM. If the application crashes between publishing and a listener firing, the event is lost. Kafka/RabbitMQ messages are durable, cross-process, and replayed after failure. Use Spring events for: in-process decoupling, cache invalidation, audit logging, and side effects that do not need durability. Use Kafka/RabbitMQ for: cross-service communication, durable event sourcing, at-least-once delivery guarantees, and fan-out to multiple services. The outbox pattern bridges the gap: write the event to a DB table in the same transaction, then a relay process publishes it to Kafka — giving transactional guarantees + durability.","distractorsMd":["With `spring.threads.virtual.enabled=true`, Spring Boot 3.2 replaces the default `ThreadPoolTaskExecutor` for `@Async` with a `VirtualThreadTaskExecutor` (`Executors.newVirtualThreadPerTaskExecutor()`).","By default, async exceptions are logged by Spring's `SimpleAsyncUncaughtExceptionHandler` at `WARN` level and silently swallowed — the publisher's thread never sees them.","`SecurityContextHolder` is `ThreadLocal`-based — the async thread has a fresh, empty security context by default."]},{"id":"spring/spring_events_and_scheduling#cbec804b7b1e","section":"spring","module":"spring/spring_events_and_scheduling","moduleName":"spring events and scheduling","sourceFile":"README.md","difficulty":"advanced","question":"What happens if a @TransactionalEventListener listener method opens a new transaction (REQUIRES_NEW) and that transaction fails?","answerFull":"The new transaction's failure does not affect the original transaction — it has already committed. The new transaction rolls back atomically. The result is a partial state: the original data is committed, but the secondary update (e.g., analytics insert) is not. This is the fundamental tradeoff of @TransactionalEventListener — it provides post-commit fire guarantee but cannot participate in the original atomicity. For cases where both changes must succeed or fail together, use an explicit @Transactional service that performs both writes in one transaction, or use the outbox pattern + a reliable message relay to ensure the secondary effect eventually happens (at-least-once, with idempotent consumer).","correct":"The new transaction's failure does not affect the original transaction — it has already committed.","distractors":["@TransactionalEventListener requires an active transaction to bind to.","@EventListener fires synchronously inline within the publisher's call stack, participating in the same transaction (if any).","SecurityContextHolder is ThreadLocal-based — the async thread has a fresh, empty security context by default."],"distractorIds":["spring/spring_events_and_scheduling#4fee9a1bfa84","spring/spring_events_and_scheduling#ae99335712dd","spring/spring_events_and_scheduling#6e11c871178a"],"concepts":["transactionaleventlistener","opens","listener","affect","committed","happens"],"questionMd":"What happens if a `@TransactionalEventListener` listener method opens a new transaction (`REQUIRES_NEW`) and that transaction fails?","answerFullMd":"The new transaction's failure does not affect the original transaction — it has already committed. The new transaction rolls back atomically. The result is a partial state: the original data is committed, but the secondary update (e.g., analytics insert) is not. This is the fundamental tradeoff of `@TransactionalEventListener` — it provides post-commit fire guarantee but cannot participate in the original atomicity. For cases where both changes must succeed or fail together, use an explicit `@Transactional` service that performs both writes in one transaction, or use the outbox pattern + a reliable message relay to ensure the secondary effect eventually happens (at-least-once, with idempotent consumer).","distractorsMd":["`@TransactionalEventListener` requires an active transaction to bind to.","`@EventListener` fires synchronously inline within the publisher's call stack, participating in the same transaction (if any).","`SecurityContextHolder` is `ThreadLocal`-based — the async thread has a fresh, empty security context by default."]},{"id":"spring/spring_events_and_scheduling#41505c76d33e","section":"spring","module":"spring/spring_events_and_scheduling","moduleName":"spring events and scheduling","sourceFile":"README.md","difficulty":"advanced","question":"How does spring.threads.virtual.enabled=true affect @Async and @Scheduled in Spring Boot 3.2?","answerFull":"With spring.threads.virtual.enabled=true, Spring Boot 3.2 replaces the default ThreadPoolTaskExecutor for @Async with a VirtualThreadTaskExecutor (Executors.newVirtualThreadPerTaskExecutor()). For @Scheduled, it replaces the scheduler with a SimpleAsyncTaskScheduler backed by virtual threads. This means: (1) @Async no longer needs a bounded thread pool — each invocation gets its own virtual thread; (2) blocking I/O in async methods does not pin OS threads; (3) @Scheduled tasks no longer share a fixed thread pool — each scheduled run gets a virtual thread. Important: virtual threads still respect synchronized pinning — if scheduled tasks use synchronized blocks, they may pin carrier threads. Monitor with -Djdk.tracePinnedThreads=full.","correct":"With spring.threads.virtual.enabled=true, Spring Boot 3.2 replaces the default ThreadPoolTaskExecutor for @Async with a VirtualThreadTaskExecutor (Executors.newVirtualThreadPerTaskExecutor()).","distractors":["Spring's default scheduler uses a single thread (ScheduledThreadPoolExecutor with corePoolSize=1).","The new transaction's failure does not affect the original transaction — it has already committed.","By default, async exceptions are logged by Spring's SimpleAsyncUncaughtExceptionHandler at WARN level and silently swallowed — the publisher's thread never sees them."],"distractorIds":["spring/spring_events_and_scheduling#f9e1126f3f7a","spring/spring_events_and_scheduling#cbec804b7b1e","spring/spring_events_and_scheduling#323245810cdf"],"concepts":["executors","newvirtualthreadpertaskexecutor","threadpooltaskexecutor","virtualthreadtaskexecutor","affect","enabled"],"questionMd":"How does `spring.threads.virtual.enabled=true` affect `@Async` and `@Scheduled` in Spring Boot 3.2?","correctMd":"With `spring.threads.virtual.enabled=true`, Spring Boot 3.2 replaces the default `ThreadPoolTaskExecutor` for `@Async` with a `VirtualThreadTaskExecutor` (`Executors.newVirtualThreadPerTaskExecutor()`).","answerFullMd":"With `spring.threads.virtual.enabled=true`, Spring Boot 3.2 replaces the default `ThreadPoolTaskExecutor` for `@Async` with a `VirtualThreadTaskExecutor` (`Executors.newVirtualThreadPerTaskExecutor()`). For `@Scheduled`, it replaces the scheduler with a `SimpleAsyncTaskScheduler` backed by virtual threads. This means: (1) `@Async` no longer needs a bounded thread pool — each invocation gets its own virtual thread; (2) blocking I/O in async methods does not pin OS threads; (3) `@Scheduled` tasks no longer share a fixed thread pool — each scheduled run gets a virtual thread. Important: virtual threads still respect `synchronized` pinning — if scheduled tasks use `synchronized` blocks, they may pin carrier threads. Monitor with `-Djdk.tracePinnedThreads=full`.","distractorsMd":["Spring's default scheduler uses a single thread (`ScheduledThreadPoolExecutor` with `corePoolSize=1`).",null,"By default, async exceptions are logged by Spring's `SimpleAsyncUncaughtExceptionHandler` at `WARN` level and silently swallowed — the publisher's thread never sees them."]},{"id":"spring/spring_events_and_scheduling#60f4990a6116","section":"spring","module":"spring/spring_events_and_scheduling","moduleName":"spring events and scheduling","sourceFile":"README.md","difficulty":"advanced","question":"Describe the outbox pattern and how it solves the dual-write problem for events.","answerFull":"The dual-write problem: after a DB write, publishing a Kafka message in a separate network call is non-atomic. If the app crashes between the commit and the publish, the event is lost. The outbox pattern solves this: (1) within the same DB transaction, write both the business entity and an outbox_events row; (2) a separate relay process (Debezium CDC, Quartz job, or @Scheduled) reads from outbox_events and publishes to Kafka; (3) on successful Kafka publish, delete or mark the outbox row as processed. Both writes are in one DB transaction — they succeed or fail atomically. The relay retries on failure — providing at-least-once delivery. Consumers must be idempotent (use event ID deduplication). Spring Batch's chunk-oriented model is well-suited for the relay: read pending outbox rows, publish, write completion — with restart-on-failure built in.","correct":"The dual-write problem: after a DB write, publishing a Kafka message in a separate network call is non-atomic.","distractors":["Spring application events are in-process, in-memory, synchronous (or async via @Async) — they live and die with the JVM.","@EventListener fires synchronously inline within the publisher's call stack, participating in the same transaction (if any).","@PostConstruct fires during the Spring context refresh, before the embedded server (Tomcat/Netty) starts accepting requests."],"distractorIds":["spring/spring_events_and_scheduling#5bb0f0891e51","spring/spring_events_and_scheduling#ae99335712dd","spring/spring_events_and_scheduling#e7870be57238"],"concepts":["describe","publishing","outbox","dual","solves","atomic"],"answerFullMd":"The dual-write problem: after a DB write, publishing a Kafka message in a separate network call is non-atomic. If the app crashes between the commit and the publish, the event is lost. The outbox pattern solves this: (1) within the same DB transaction, write both the business entity and an `outbox_events` row; (2) a separate relay process (Debezium CDC, Quartz job, or `@Scheduled`) reads from `outbox_events` and publishes to Kafka; (3) on successful Kafka publish, delete or mark the outbox row as processed. Both writes are in one DB transaction — they succeed or fail atomically. The relay retries on failure — providing at-least-once delivery. Consumers must be idempotent (use event ID deduplication). Spring Batch's chunk-oriented model is well-suited for the relay: read pending outbox rows, publish, write completion — with restart-on-failure built in.","distractorsMd":["Spring application events are in-process, in-memory, synchronous (or async via `@Async`) — they live and die with the JVM.","`@EventListener` fires synchronously inline within the publisher's call stack, participating in the same transaction (if any).","`@PostConstruct` fires during the Spring context refresh, before the embedded server (Tomcat/Netty) starts accepting requests."]},{"id":"spring/spring_graphql#ff0fe23811d7","section":"spring","module":"spring/spring_graphql","moduleName":"spring graphql","sourceFile":"README.md","difficulty":"core","question":"What is the N+1 problem in GraphQL and why is it the default rather than an accident?","answerFull":"GraphQL resolves a query field-by-field, parent-by-parent: a query like books { author { name } } resolves the list of books (1 query), then resolves author *once per book* (N queries) — 1+N. It is the default because the engine inherently calls each field's resolver for each parent in the selection set, so a naive association resolver that does one lookup per call produces N+1 automatically. Avoiding it requires deliberately batching, which is why it is the defining GraphQL performance concern.","correct":"GraphQL resolves a query field-by-field, parent-by-parent: a query like books { author { name } } resolves the list of books (1 query), then resolves author *once per book* (N queries) — 1+N.","distractors":["Cursor pagination returns a Connection with edges { node cursor } and pageInfo { hasNextPage endCursor }; the client pages by passing the last cursor (after) rather than a numeric offset.","A DataLoader defers individual load(key) calls made during the resolution pass, collects all the keys, and then dispatches a single batch load (e.g. WHERE id IN (...)) instead of one query per key — turning 1+N into 1+1.","A GraphQL BFF is a single graph that aggregates data from many backend services so a client issues one query instead of fanning out to many REST calls — the server does the fan-out and assembly."],"distractorIds":["spring/spring_graphql#629b21790830","spring/spring_graphql#4180cf8b2942","spring/spring_graphql#518644409058"],"concepts":["book","books","author","graphql","resolves","parent"],"correctMd":"GraphQL resolves a query field-by-field, parent-by-parent: a query like `books { author { name } }` resolves the list of books (1 query), then resolves `author` *once per book* (N queries) — 1+N.","answerFullMd":"GraphQL resolves a query field-by-field, parent-by-parent: a query like `books { author { name } }` resolves the list of books (1 query), then resolves `author` *once per book* (N queries) — 1+N. It is the default because the engine inherently calls each field's resolver for each parent in the selection set, so a naive association resolver that does one lookup per call produces N+1 automatically. Avoiding it requires deliberately batching, which is why it is the defining GraphQL performance concern.","distractorsMd":["Cursor pagination returns a `Connection` with `edges { node cursor }` and `pageInfo { hasNextPage endCursor }`; the client pages by passing the last cursor (`after`) rather than a numeric offset.","A `DataLoader` defers individual `load(key)` calls made during the resolution pass, collects all the keys, and then dispatches a single batch load (e.g. `WHERE id IN (...)`) instead of one query per key — turning 1+N into 1+1.",null]},{"id":"spring/spring_graphql#4180cf8b2942","section":"spring","module":"spring/spring_graphql","moduleName":"spring graphql","sourceFile":"README.md","difficulty":"core","question":"How does DataLoader fix N+1?","answerFull":"A DataLoader defers individual load(key) calls made during the resolution pass, collects all the keys, and then dispatches a single batch load (e.g. WHERE id IN (...)) instead of one query per key — turning 1+N into 1+1. It also caches results within a single request, so the same key is never loaded twice. In Spring for GraphQL you either register a batch loader and inject DataLoader into a @SchemaMapping, or use @BatchMapping where the framework creates the loader and your method receives the full list of parents at once.","correct":"A DataLoader defers individual load(key) calls made during the resolution pass, collects all the keys, and then dispatches a single batch load (e.g. WHERE id IN (...)) instead of one query per key — turning 1+N into 1+1.","distractors":["Because the query language is open, a client can request deeply nested or cyclic selections that are exponentially expensive — a denial-of-service vector.","@SchemaMapping resolves a single field for a single parent — it is called once per parent object, so using it for an association naively causes N+1.","GraphQL resolves a query field-by-field, parent-by-parent: a query like books { author { name } } resolves the list of books (1 query), then resolves author *once per book* (N queries) — 1+N."],"distractorIds":["spring/spring_graphql#57ccc53830b5","spring/spring_graphql#01b84f678c07","spring/spring_graphql#ff0fe23811d7"],"concepts":["dataloader","defers","dispatches","turning","collects","fix"],"correctMd":"A `DataLoader` defers individual `load(key)` calls made during the resolution pass, collects all the keys, and then dispatches a single batch load (e.g. `WHERE id IN (...)`) instead of one query per key — turning 1+N into 1+1.","answerFullMd":"A `DataLoader` defers individual `load(key)` calls made during the resolution pass, collects all the keys, and then dispatches a single batch load (e.g. `WHERE id IN (...)`) instead of one query per key — turning 1+N into 1+1. It also caches results within a single request, so the same key is never loaded twice. In Spring for GraphQL you either register a batch loader and inject `DataLoader` into a `@SchemaMapping`, or use `@BatchMapping` where the framework creates the loader and your method receives the full list of parents at once.","distractorsMd":[null,"`@SchemaMapping` resolves a single field for a single parent — it is called once per parent object, so using it for an association naively causes N+1.","GraphQL resolves a query field-by-field, parent-by-parent: a query like `books { author { name } }` resolves the list of books (1 query), then resolves `author` *once per book* (N queries) — 1+N."]},{"id":"spring/spring_graphql#01b84f678c07","section":"spring","module":"spring/spring_graphql","moduleName":"spring graphql","sourceFile":"README.md","difficulty":"core","question":"What is the difference between @SchemaMapping and @BatchMapping?","answerFull":"@SchemaMapping resolves a single field for a single parent — it is called once per parent object, so using it for an association naively causes N+1. @BatchMapping resolves the field for *all* parents at once: Spring auto-creates a DataLoader, calls your method with the List (or Collection) of parents, and you return a Map of parent→value (or a parallel list), executing one batched backend call. @BatchMapping is the low-boilerplate way to batch; explicit DataLoader + @SchemaMapping gives finer control over keys and caching.","correct":"@SchemaMapping resolves a single field for a single parent — it is called once per parent object, so using it for an association naively causes N+1.","distractors":["A GraphQL BFF is a single graph that aggregates data from many backend services so a client issues one query instead of fanning out to many REST calls — the server does the fan-out and assembly.","GraphQL resolves a query field-by-field, parent-by-parent: a query like books { author { name } } resolves the list of books (1 query), then resolves author *once per book* (N queries) — 1+N.","A DataLoader defers individual load(key) calls made during the resolution pass, collects all the keys, and then dispatches a single batch load (e.g. WHERE id IN (...)) instead of one query per key — turning 1+N into 1+1."],"distractorIds":["spring/spring_graphql#518644409058","spring/spring_graphql#ff0fe23811d7","spring/spring_graphql#4180cf8b2942"],"concepts":["naively","schemamapping","association","difference","resolves","called"],"questionMd":"What is the difference between `@SchemaMapping` and `@BatchMapping`?","correctMd":"`@SchemaMapping` resolves a single field for a single parent — it is called once per parent object, so using it for an association naively causes N+1.","answerFullMd":"`@SchemaMapping` resolves a single field for a single parent — it is called once per parent object, so using it for an association naively causes N+1. `@BatchMapping` resolves the field for *all* parents at once: Spring auto-creates a DataLoader, calls your method with the `List` (or `Collection`) of parents, and you return a `Map` of parent→value (or a parallel list), executing one batched backend call. `@BatchMapping` is the low-boilerplate way to batch; explicit `DataLoader` + `@SchemaMapping` gives finer control over keys and caching.","distractorsMd":[null,"GraphQL resolves a query field-by-field, parent-by-parent: a query like `books { author { name } }` resolves the list of books (1 query), then resolves `author` *once per book* (N queries) — 1+N.","A `DataLoader` defers individual `load(key)` calls made during the resolution pass, collects all the keys, and then dispatches a single batch load (e.g. `WHERE id IN (...)`) instead of one query per key — turning 1+N into 1+1."]},{"id":"spring/spring_graphql#524a78869c02","section":"spring","module":"spring/spring_graphql","moduleName":"spring graphql","sourceFile":"README.md","difficulty":"intermediate","question":"How do GraphQL subscriptions work and what transport do they use?","answerFull":"A subscription is a long-lived operation where the server *pushes* events to the client as they occur (e.g. bookAdded). It cannot use plain request/response HTTP, so it runs over a persistent transport — WebSocket (the graphql-ws protocol) or Server-Sent Events. In Spring for GraphQL a @SubscriptionMapping method returns a Flux<T>, and the framework streams each emitted element to the subscribed client over the chosen transport. Queries and mutations, by contrast, stay on ordinary HTTP POST.","correct":"A subscription is a long-lived operation where the server *pushes* events to the client as they occur (e.g. bookAdded).","distractors":["GraphQL resolves a query field-by-field, parent-by-parent: a query like books { author { name } } resolves the list of books (1 query), then resolves author *once per book* (N queries) — 1+N.","Introspection is a built-in GraphQL feature where the schema itself is queryable (schema, type), enabling tooling like GraphiQL, auto-generated clients, and documentation.","@Argument binds a GraphQL field argument to a controller-method parameter, converting the incoming value to the parameter type (including binding complex input types to a POJO)."],"distractorIds":["spring/spring_graphql#ff0fe23811d7","spring/spring_graphql#4f7ed14c16f8","spring/spring_graphql#b47bd8f2e84a"],"concepts":["bookadded","subscriptions","subscription","pushes","graphql","transport"],"correctMd":"A subscription is a long-lived operation where the server *pushes* events to the client as they occur (e.g. `bookAdded`).","answerFullMd":"A subscription is a long-lived operation where the server *pushes* events to the client as they occur (e.g. `bookAdded`). It cannot use plain request/response HTTP, so it runs over a persistent transport — WebSocket (the `graphql-ws` protocol) or Server-Sent Events. In Spring for GraphQL a `@SubscriptionMapping` method returns a `Flux<T>`, and the framework streams each emitted element to the subscribed client over the chosen transport. Queries and mutations, by contrast, stay on ordinary HTTP POST.","distractorsMd":["GraphQL resolves a query field-by-field, parent-by-parent: a query like `books { author { name } }` resolves the list of books (1 query), then resolves `author` *once per book* (N queries) — 1+N.","Introspection is a built-in GraphQL feature where the schema itself is queryable (`__schema`, `__type`), enabling tooling like GraphiQL, auto-generated clients, and documentation.","`@Argument` binds a GraphQL field argument to a controller-method parameter, converting the incoming value to the parameter type (including binding complex input types to a POJO)."]},{"id":"spring/spring_graphql#57ccc53830b5","section":"spring","module":"spring/spring_graphql","moduleName":"spring graphql","sourceFile":"README.md","difficulty":"intermediate","question":"How do you prevent expensive or malicious queries?","answerFull":"Because the query language is open, a client can request deeply nested or cyclic selections that are exponentially expensive — a denial-of-service vector. You guard it with query *depth* limits (maxQueryDepth), query *complexity*/cost analysis that assigns a cost to fields and rejects queries over a budget, pagination limits (cap first/limit), timeouts, and optionally allow-listing persisted queries so only vetted queries run. These controls are mandatory for any public GraphQL API.","correct":"Because the query language is open, a client can request deeply nested or cyclic selections that are exponentially expensive — a denial-of-service vector.","distractors":["A subscription is a long-lived operation where the server *pushes* events to the client as they occur (e.g. bookAdded).","GraphQL resolves a query field-by-field, parent-by-parent: a query like books { author { name } } resolves the list of books (1 query), then resolves author *once per book* (N queries) — 1+N.","Cursor pagination returns a Connection with edges { node cursor } and pageInfo { hasNextPage endCursor }; the client pages by passing the last cursor (after) rather than a numeric offset."],"distractorIds":["spring/spring_graphql#524a78869c02","spring/spring_graphql#ff0fe23811d7","spring/spring_graphql#629b21790830"],"concepts":["deeply","denial","selections","cyclic","exponentially","malicious"],"answerFullMd":"Because the query language is open, a client can request deeply nested or cyclic selections that are exponentially expensive — a denial-of-service vector. You guard it with query *depth* limits (`maxQueryDepth`), query *complexity*/cost analysis that assigns a cost to fields and rejects queries over a budget, pagination limits (cap `first`/`limit`), timeouts, and optionally allow-listing persisted queries so only vetted queries run. These controls are mandatory for any public GraphQL API.","distractorsMd":["A subscription is a long-lived operation where the server *pushes* events to the client as they occur (e.g. `bookAdded`).","GraphQL resolves a query field-by-field, parent-by-parent: a query like `books { author { name } }` resolves the list of books (1 query), then resolves `author` *once per book* (N queries) — 1+N.","Cursor pagination returns a `Connection` with `edges { node cursor }` and `pageInfo { hasNextPage endCursor }`; the client pages by passing the last cursor (`after`) rather than a numeric offset."]},{"id":"spring/spring_graphql#629b21790830","section":"spring","module":"spring/spring_graphql","moduleName":"spring graphql","sourceFile":"README.md","difficulty":"intermediate","question":"What is cursor-based (Relay Connection) pagination and why prefer it over offset?","answerFull":"Cursor pagination returns a Connection with edges { node cursor } and pageInfo { hasNextPage endCursor }; the client pages by passing the last cursor (after) rather than a numeric offset. It is preferred over offset/limit because deep offsets are slow (the DB must scan and skip rows) and unstable under concurrent inserts/deletes (rows shift, causing skips or duplicates), whereas a cursor anchors to a stable position. Spring for GraphQL supports it via Spring Data's Window/ScrollPosition and adapts the result to the Connection shape automatically.","correct":"Cursor pagination returns a Connection with edges { node cursor } and pageInfo { hasNextPage endCursor }; the client pages by passing the last cursor (after) rather than a numeric offset.","distractors":["Because the query language is open, a client can request deeply nested or cyclic selections that are exponentially expensive — a denial-of-service vector.","A subscription is a long-lived operation where the server *pushes* events to the client as they occur (e.g. bookAdded).","A GraphQL BFF is a single graph that aggregates data from many backend services so a client issues one query instead of fanning out to many REST calls — the server does the fan-out and assembly."],"distractorIds":["spring/spring_graphql#57ccc53830b5","spring/spring_graphql#524a78869c02","spring/spring_graphql#518644409058"],"concepts":["endcursor","hasnextpage","pageinfo","relay","numeric","pagination"],"correctMd":"Cursor pagination returns a `Connection` with `edges { node cursor }` and `pageInfo { hasNextPage endCursor }`; the client pages by passing the last cursor (`after`) rather than a numeric offset.","answerFullMd":"Cursor pagination returns a `Connection` with `edges { node cursor }` and `pageInfo { hasNextPage endCursor }`; the client pages by passing the last cursor (`after`) rather than a numeric offset. It is preferred over offset/limit because deep offsets are slow (the DB must scan and skip rows) and unstable under concurrent inserts/deletes (rows shift, causing skips or duplicates), whereas a cursor anchors to a stable position. Spring for GraphQL supports it via Spring Data's `Window`/`ScrollPosition` and adapts the result to the Connection shape automatically.","distractorsMd":[null,"A subscription is a long-lived operation where the server *pushes* events to the client as they occur (e.g. `bookAdded`).",null]},{"id":"spring/spring_graphql#1230c5ab0c63","section":"spring","module":"spring/spring_graphql","moduleName":"spring graphql","sourceFile":"README.md","difficulty":"intermediate","question":"Schema-first vs code-first — what does Spring for GraphQL use?","answerFull":"Spring for GraphQL is schema-first: you author the schema in SDL (.graphqls files), and Spring binds handler methods (@QueryMapping, etc.) to schema fields, similar to how @RequestMapping binds to HTTP routes. Code-first approaches (some other frameworks) generate the schema from code/annotations instead. Schema-first keeps the contract explicit and reviewable as a single artifact and lets frontend/backend agree on the schema independently of implementation, which is the dominant approach in the Spring ecosystem.","correct":"Spring for GraphQL is schema-first: you author the schema in SDL (.graphqls files), and Spring binds handler methods (@QueryMapping, etc.) to schema fields, similar to how @RequestMapping binds to HTTP routes.","distractors":["GraphQL resolves a query field-by-field, parent-by-parent: a query like books { author { name } } resolves the list of books (1 query), then resolves author *once per book* (N queries) — 1+N.","A GraphQL BFF is a single graph that aggregates data from many backend services so a client issues one query instead of fanning out to many REST calls — the server does the fan-out and assembly.","Introspection is a built-in GraphQL feature where the schema itself is queryable (schema, type), enabling tooling like GraphiQL, auto-generated clients, and documentation."],"distractorIds":["spring/spring_graphql#ff0fe23811d7","spring/spring_graphql#518644409058","spring/spring_graphql#4f7ed14c16f8"],"concepts":["graphqls","querymapping","sdl","binds","requestmapping","author"],"correctMd":"Spring for GraphQL is schema-first: you author the schema in SDL (`.graphqls` files), and Spring binds handler methods (`@QueryMapping`, etc.) to schema fields, similar to how `@RequestMapping` binds to HTTP routes.","answerFullMd":"Spring for GraphQL is schema-first: you author the schema in SDL (`.graphqls` files), and Spring binds handler methods (`@QueryMapping`, etc.) to schema fields, similar to how `@RequestMapping` binds to HTTP routes. Code-first approaches (some other frameworks) generate the schema from code/annotations instead. Schema-first keeps the contract explicit and reviewable as a single artifact and lets frontend/backend agree on the schema independently of implementation, which is the dominant approach in the Spring ecosystem.","distractorsMd":["GraphQL resolves a query field-by-field, parent-by-parent: a query like `books { author { name } }` resolves the list of books (1 query), then resolves `author` *once per book* (N queries) — 1+N.",null,"Introspection is a built-in GraphQL feature where the schema itself is queryable (`__schema`, `__type`), enabling tooling like GraphiQL, auto-generated clients, and documentation."]},{"id":"spring/spring_graphql#b47bd8f2e84a","section":"spring","module":"spring/spring_graphql","moduleName":"spring graphql","sourceFile":"README.md","difficulty":"advanced","question":"What does the @Argument annotation do, and how are inputs handled?","answerFull":"@Argument binds a GraphQL field argument to a controller-method parameter, converting the incoming value to the parameter type (including binding complex input types to a POJO). For structured inputs you define an input type in the schema and bind it to a DTO. Spring also supports @Arguments for binding the whole argument map and validation via Bean Validation on the bound objects. It is the GraphQL analogue of @RequestParam/@RequestBody binding in MVC.","correct":"@Argument binds a GraphQL field argument to a controller-method parameter, converting the incoming value to the parameter type (including binding complex input types to a POJO).","distractors":["Introspection is a built-in GraphQL feature where the schema itself is queryable (schema, type), enabling tooling like GraphiQL, auto-generated clients, and documentation.","A GraphQL BFF is a single graph that aggregates data from many backend services so a client issues one query instead of fanning out to many REST calls — the server does the fan-out and assembly.","Spring for GraphQL is schema-first: you author the schema in SDL (.graphqls files), and Spring binds handler methods (@QueryMapping, etc.) to schema fields, similar to how @RequestMapping binds to HTTP routes."],"distractorIds":["spring/spring_graphql#4f7ed14c16f8","spring/spring_graphql#518644409058","spring/spring_graphql#1230c5ab0c63"],"concepts":["pojo","converting","binds","handled","graphql","binding"],"questionMd":"What does the `@Argument` annotation do, and how are inputs handled?","correctMd":"`@Argument` binds a GraphQL field argument to a controller-method parameter, converting the incoming value to the parameter type (including binding complex input types to a POJO).","answerFullMd":"`@Argument` binds a GraphQL field argument to a controller-method parameter, converting the incoming value to the parameter type (including binding complex input types to a POJO). For structured inputs you define an `input` type in the schema and bind it to a DTO. Spring also supports `@Arguments` for binding the whole argument map and validation via Bean Validation on the bound objects. It is the GraphQL analogue of `@RequestParam`/`@RequestBody` binding in MVC.","distractorsMd":["Introspection is a built-in GraphQL feature where the schema itself is queryable (`__schema`, `__type`), enabling tooling like GraphiQL, auto-generated clients, and documentation.",null,"Spring for GraphQL is schema-first: you author the schema in SDL (`.graphqls` files), and Spring binds handler methods (`@QueryMapping`, etc.) to schema fields, similar to how `@RequestMapping` binds to HTTP routes."]},{"id":"spring/spring_graphql#80e52cc9d43c","section":"spring","module":"spring/spring_graphql","moduleName":"spring graphql","sourceFile":"README.md","difficulty":"advanced","question":"Can Spring for GraphQL resolvers be reactive, and how does that interact with DataLoader?","answerFull":"Yes — handler methods can return Mono/Flux (and run on WebFlux) or plain blocking values (on WebMVC); subscriptions specifically return Flux. DataLoader fits naturally because it is inherently asynchronous: loader.load(key) returns a CompletableFuture/reactive type that completes when the batch dispatches, so reactive resolvers compose with batched loading without blocking. This lets you do non-blocking, batched resolution end to end, which matters under high concurrency.","correct":"Yes — handler methods can return Mono/Flux (and run on WebFlux) or plain blocking values (on WebMVC); subscriptions specifically return Flux.","distractors":["GraphQL resolves a query field-by-field, parent-by-parent: a query like books { author { name } } resolves the list of books (1 query), then resolves author *once per book* (N queries) — 1+N.","A GraphQL BFF is a single graph that aggregates data from many backend services so a client issues one query instead of fanning out to many REST calls — the server does the fan-out and assembly.","@Argument binds a GraphQL field argument to a controller-method parameter, converting the incoming value to the parameter type (including binding complex input types to a POJO)."],"distractorIds":["spring/spring_graphql#ff0fe23811d7","spring/spring_graphql#518644409058","spring/spring_graphql#b47bd8f2e84a"],"concepts":["webmvc","subscriptions","dataloader","interact","resolvers","webflux"],"correctMd":"Yes — handler methods can return `Mono`/`Flux` (and run on WebFlux) or plain blocking values (on WebMVC); subscriptions specifically return `Flux`.","answerFullMd":"Yes — handler methods can return `Mono`/`Flux` (and run on WebFlux) or plain blocking values (on WebMVC); subscriptions specifically return `Flux`. DataLoader fits naturally because it is inherently asynchronous: `loader.load(key)` returns a `CompletableFuture`/reactive type that completes when the batch dispatches, so reactive resolvers compose with batched loading without blocking. This lets you do non-blocking, batched resolution end to end, which matters under high concurrency.","distractorsMd":["GraphQL resolves a query field-by-field, parent-by-parent: a query like `books { author { name } }` resolves the list of books (1 query), then resolves `author` *once per book* (N queries) — 1+N.",null,"`@Argument` binds a GraphQL field argument to a controller-method parameter, converting the incoming value to the parameter type (including binding complex input types to a POJO)."]},{"id":"spring/spring_graphql#518644409058","section":"spring","module":"spring/spring_graphql","moduleName":"spring graphql","sourceFile":"README.md","difficulty":"advanced","question":"How does GraphQL relate to a backend-for-frontend (BFF) and federation?","answerFull":"A GraphQL BFF is a single graph that aggregates data from many backend services so a client issues one query instead of fanning out to many REST calls — the server does the fan-out and assembly. Federation (Apollo Federation, Netflix DGS) extends this across teams: each service owns a *subgraph* of the schema, and a gateway composes them into one supergraph, so a single query can be resolved by stitching fields from multiple services. This is how large organizations scale GraphQL across many teams without one monolithic resolver layer.","correct":"A GraphQL BFF is a single graph that aggregates data from many backend services so a client issues one query instead of fanning out to many REST calls — the server does the fan-out and assembly.","distractors":["Because the query language is open, a client can request deeply nested or cyclic selections that are exponentially expensive — a denial-of-service vector.","Cursor pagination returns a Connection with edges { node cursor } and pageInfo { hasNextPage endCursor }; the client pages by passing the last cursor (after) rather than a numeric offset.","A DataLoader defers individual load(key) calls made during the resolution pass, collects all the keys, and then dispatches a single batch load (e.g. WHERE id IN (...)) instead of one query per key — turning 1+N into 1+1."],"distractorIds":["spring/spring_graphql#57ccc53830b5","spring/spring_graphql#629b21790830","spring/spring_graphql#4180cf8b2942"],"concepts":["assembly","bff","fanning","federation","frontend","fan"],"distractorsMd":[null,"Cursor pagination returns a `Connection` with `edges { node cursor }` and `pageInfo { hasNextPage endCursor }`; the client pages by passing the last cursor (`after`) rather than a numeric offset.","A `DataLoader` defers individual `load(key)` calls made during the resolution pass, collects all the keys, and then dispatches a single batch load (e.g. `WHERE id IN (...)`) instead of one query per key — turning 1+N into 1+1."]},{"id":"spring/spring_graphql#4f7ed14c16f8","section":"spring","module":"spring/spring_graphql","moduleName":"spring graphql","sourceFile":"README.md","difficulty":"advanced","question":"What is query introspection and what is its security consideration?","answerFull":"Introspection is a built-in GraphQL feature where the schema itself is queryable (schema, type), enabling tooling like GraphiQL, auto-generated clients, and documentation. The security consideration is that on a sensitive/internal API, introspection exposes your entire schema (every type and field) to anyone, which aids attackers in crafting expensive or probing queries; many teams disable introspection in production for non-public APIs or gate it behind auth, while keeping it on in development.","correct":"Introspection is a built-in GraphQL feature where the schema itself is queryable (schema, type), enabling tooling like GraphiQL, auto-generated clients, and documentation.","distractors":["GraphQL resolves a query field-by-field, parent-by-parent: a query like books { author { name } } resolves the list of books (1 query), then resolves author *once per book* (N queries) — 1+N.","Because the query language is open, a client can request deeply nested or cyclic selections that are exponentially expensive — a denial-of-service vector.","A GraphQL BFF is a single graph that aggregates data from many backend services so a client issues one query instead of fanning out to many REST calls — the server does the fan-out and assembly."],"distractorIds":["spring/spring_graphql#ff0fe23811d7","spring/spring_graphql#57ccc53830b5","spring/spring_graphql#518644409058"],"concepts":["graphiql","queryable","introspection","documentation","tooling","graphql"],"correctMd":"Introspection is a built-in GraphQL feature where the schema itself is queryable (`__schema`, `__type`), enabling tooling like GraphiQL, auto-generated clients, and documentation.","answerFullMd":"Introspection is a built-in GraphQL feature where the schema itself is queryable (`__schema`, `__type`), enabling tooling like GraphiQL, auto-generated clients, and documentation. The security consideration is that on a sensitive/internal API, introspection exposes your entire schema (every type and field) to anyone, which aids attackers in crafting expensive or probing queries; many teams disable introspection in production for non-public APIs or gate it behind auth, while keeping it on in development.","distractorsMd":["GraphQL resolves a query field-by-field, parent-by-parent: a query like `books { author { name } }` resolves the list of books (1 query), then resolves `author` *once per book* (N queries) — 1+N.",null,null]},{"id":"spring/spring_grpc#c94d76046734","section":"spring","module":"spring/spring_grpc","moduleName":"spring grpc","sourceFile":"README.md","difficulty":"core","question":"Why does a gRPC client call hang forever, and how do you prevent it?","answerFull":"Because a blocking stub with no deadline waits indefinitely for a response that a slow or dead-but-listening peer never sends. gRPC does not impose a default timeout, so the call blocks its thread until the peer replies; under load this exhausts the caller's thread pool with zero errors — just silence. Always call stub.withDeadlineAfter(200, TimeUnit.MILLISECONDS), and set a channel-level deadline backstop so a forgotten per-call deadline still fails fast with DEADLINE_EXCEEDED.","correct":"Because a blocking stub with no deadline waits indefinitely for a response that a slow or dead-but-listening peer never sends.","distractors":["Because grpc-java caps inbound messages at 4 MiB by default on both the server and the client, so a 6 MB response throws RESOURCE_EXHAUSTED: gRPC message exceeds maximum size 4194304.","A channel deadline is a backstop that bounds any call whose code path forgets withDeadlineAfter, so a single missed per-call deadline cannot hang.","@GrpcService binds a bean to the HTTP/2 gRPC server instead of the servlet dispatcher, so none of Spring MVC's servlet-scoped machinery applies."],"distractorIds":["spring/spring_grpc#0c6fb2fc7e3f","spring/spring_grpc#b011cb4c977a","spring/spring_grpc#2b5a8ce1b7eb"],"concepts":["listening","forever","hang","peer","indefinitely","stub"],"answerFullMd":"Because a blocking stub with no deadline waits indefinitely for a response that a slow or dead-but-listening peer never sends. gRPC does not impose a default timeout, so the call blocks its thread until the peer replies; under load this exhausts the caller's thread pool with zero errors — just silence. Always call `stub.withDeadlineAfter(200, TimeUnit.MILLISECONDS)`, and set a channel-level `deadline` backstop so a forgotten per-call deadline still fails fast with `DEADLINE_EXCEEDED`.","distractorsMd":["Because grpc-java caps inbound messages at 4 MiB by default on both the server and the client, so a 6 MB response throws `RESOURCE_EXHAUSTED: gRPC message exceeds maximum size 4194304`.","A channel `deadline` is a backstop that bounds any call whose code path forgets `withDeadlineAfter`, so a single missed per-call deadline cannot hang.","`@GrpcService` binds a bean to the HTTP/2 gRPC server instead of the servlet dispatcher, so none of Spring MVC's servlet-scoped machinery applies."]},{"id":"spring/spring_grpc#580e0b113272","section":"spring","module":"spring/spring_grpc","moduleName":"spring grpc","sourceFile":"README.md","difficulty":"core","question":"What is the difference between a gRPC deadline and a per-hop timeout?","answerFull":"A deadline is an absolute wall-clock instant that propagates through the entire call tree, whereas a timeout is a per-call relative duration. When a client sets withDeadlineAfter(300ms), that instant travels through the gRPC Context to user → pricing → inventory; each hop sees the *remaining* budget via Context.current().getDeadline(). This prevents the \"reset timeout at each hop\" bug where a 300ms budget silently becomes 900ms across three services. Set the deadline once at the edge of a request and let it flow.","correct":"A deadline is an absolute wall-clock instant that propagates through the entire call tree, whereas a timeout is a per-call relative duration.","distractors":["The response stream never closes, so the client blocks until its own deadline fires and then reports DEADLINE_EXCEEDED — a misleading error that hides a pure server bug.","When a client cancels or its deadline expires, the server's gRPC Context becomes cancelled and a ServerCallStreamObserver.setOnCancelHandler callback fires.","Because grpc-java caps inbound messages at 4 MiB by default on both the server and the client, so a 6 MB response throws RESOURCE_EXHAUSTED: gRPC message exceeds maximum size 4194304."],"distractorIds":["spring/spring_grpc#7fee1ffdec0a","spring/spring_grpc#4aea66cfbf5f","spring/spring_grpc#0c6fb2fc7e3f"],"concepts":["hop","difference","propagates","wall","instant","deadline"],"answerFullMd":"A deadline is an absolute wall-clock instant that propagates through the entire call tree, whereas a timeout is a per-call relative duration. When a client sets `withDeadlineAfter(300ms)`, that instant travels through the gRPC `Context` to user → pricing → inventory; each hop sees the *remaining* budget via `Context.current().getDeadline()`. This prevents the \"reset timeout at each hop\" bug where a 300ms budget silently becomes 900ms across three services. Set the deadline once at the edge of a request and let it flow.","distractorsMd":["The response stream never closes, so the client blocks until its own deadline fires and then reports `DEADLINE_EXCEEDED` — a misleading error that hides a pure server bug.","When a client cancels or its deadline expires, the server's gRPC `Context` becomes cancelled and a `ServerCallStreamObserver.setOnCancelHandler` callback fires.","Because grpc-java caps inbound messages at 4 MiB by default on both the server and the client, so a 6 MB response throws `RESOURCE_EXHAUSTED: gRPC message exceeds maximum size 4194304`."]},{"id":"spring/spring_grpc#2b5a8ce1b7eb","section":"spring","module":"spring/spring_grpc","moduleName":"spring grpc","sourceFile":"README.md","difficulty":"core","question":"How does @GrpcService differ from @RestController, and what machinery does it lose?","answerFull":"@GrpcService binds a bean to the HTTP/2 gRPC server instead of the servlet dispatcher, so none of Spring MVC's servlet-scoped machinery applies. There is no HttpServletRequest, no HandlerInterceptor, no servlet SecurityContextHolder auto-population. Cross-cutting concerns move to gRPC ServerInterceptors, and identity/trace context move to gRPC Metadata and Context rather than servlet request attributes. You rebuild security, tracing, and logging on those two gRPC primitives.","correct":"@GrpcService binds a bean to the HTTP/2 gRPC server instead of the servlet dispatcher, so none of Spring MVC's servlet-scoped machinery applies.","distractors":["Read the token from Metadata in a ServerInterceptor, build an Authentication, and bind it into the gRPC Context with Contexts.interceptCall.","Because gRPC strips stack traces for security and maps any unmapped exception to Status.UNKNOWN with no description, so the caller cannot distinguish a bug from \"not found\" from bad input.","With net.devh, annotate a stub field with @GrpcClient(\"channel-name\") and Spring injects a stub bound to the named channel from grpc.client.<name>.*."],"distractorIds":["spring/spring_grpc#c377c8d6c2f5","spring/spring_grpc#ccfffc958e7e","spring/spring_grpc#8744f76b81c5"],"concepts":["dispatcher","grpcservice","machinery","binds","restcontroller","mvc"],"questionMd":"How does `@GrpcService` differ from `@RestController`, and what machinery does it lose?","correctMd":"`@GrpcService` binds a bean to the HTTP/2 gRPC server instead of the servlet dispatcher, so none of Spring MVC's servlet-scoped machinery applies.","answerFullMd":"`@GrpcService` binds a bean to the HTTP/2 gRPC server instead of the servlet dispatcher, so none of Spring MVC's servlet-scoped machinery applies. There is no `HttpServletRequest`, no `HandlerInterceptor`, no servlet `SecurityContextHolder` auto-population. Cross-cutting concerns move to gRPC `ServerInterceptor`s, and identity/trace context move to gRPC `Metadata` and `Context` rather than servlet request attributes. You rebuild security, tracing, and logging on those two gRPC primitives.","distractorsMd":["Read the token from `Metadata` in a `ServerInterceptor`, build an `Authentication`, and bind it into the gRPC `Context` with `Contexts.interceptCall`.","Because gRPC strips stack traces for security and maps any unmapped exception to `Status.UNKNOWN` with no description, so the caller cannot distinguish a bug from \"not found\" from bad input.","With net.devh, annotate a stub field with `@GrpcClient(\"channel-name\")` and Spring injects a stub bound to the named channel from `grpc.client.<name>.*`."]},{"id":"spring/spring_grpc#c377c8d6c2f5","section":"spring","module":"spring/spring_grpc","moduleName":"spring grpc","sourceFile":"README.md","difficulty":"core","question":"How do you propagate the security identity into a gRPC service method?","answerFull":"Read the token from Metadata in a ServerInterceptor, build an Authentication, and bind it into the gRPC Context with Contexts.interceptCall. Because gRPC calls are not servlet-scoped, the servlet SecurityContextHolder is not populated automatically; the interceptor either stores identity in the gRPC Context (read directly downstream) or, with net.devh's security bridge, copies it into SecurityContextHolder so @Secured/@PreAuthorize work as in MVC. Do this in one global interceptor, not per method.","correct":"Read the token from Metadata in a ServerInterceptor, build an Authentication, and bind it into the gRPC Context with Contexts.interceptCall.","distractors":["When a client cancels or its deadline expires, the server's gRPC Context becomes cancelled and a ServerCallStreamObserver.setOnCancelHandler callback fires.","@GrpcService binds a bean to the HTTP/2 gRPC server instead of the servlet dispatcher, so none of Spring MVC's servlet-scoped machinery applies.","Authenticate in a ServerInterceptor that populates identity, then guard methods with @Secured or @PreAuthorize."],"distractorIds":["spring/spring_grpc#4aea66cfbf5f","spring/spring_grpc#2b5a8ce1b7eb","spring/spring_grpc#23881dc3767a"],"concepts":["interceptcall","serverinterceptor","propagate","bind","contexts","authentication"],"correctMd":"Read the token from `Metadata` in a `ServerInterceptor`, build an `Authentication`, and bind it into the gRPC `Context` with `Contexts.interceptCall`.","answerFullMd":"Read the token from `Metadata` in a `ServerInterceptor`, build an `Authentication`, and bind it into the gRPC `Context` with `Contexts.interceptCall`. Because gRPC calls are not servlet-scoped, the servlet `SecurityContextHolder` is not populated automatically; the interceptor either stores identity in the gRPC `Context` (read directly downstream) or, with net.devh's security bridge, copies it into `SecurityContextHolder` so `@Secured`/`@PreAuthorize` work as in MVC. Do this in one global interceptor, not per method.","distractorsMd":["When a client cancels or its deadline expires, the server's gRPC `Context` becomes cancelled and a `ServerCallStreamObserver.setOnCancelHandler` callback fires.","`@GrpcService` binds a bean to the HTTP/2 gRPC server instead of the servlet dispatcher, so none of Spring MVC's servlet-scoped machinery applies.","Authenticate in a `ServerInterceptor` that populates identity, then guard methods with `@Secured` or `@PreAuthorize`."]},{"id":"spring/spring_grpc#ccfffc958e7e","section":"spring","module":"spring/spring_grpc","moduleName":"spring grpc","sourceFile":"README.md","difficulty":"core","question":"Why does an unhandled server exception reach the client as UNKNOWN, and how do you fix it?","answerFull":"Because gRPC strips stack traces for security and maps any unmapped exception to Status.UNKNOWN with no description, so the caller cannot distinguish a bug from \"not found\" from bad input. Fix it with a central @GrpcAdvice class whose @GrpcExceptionHandler methods map each domain exception to a canonical Status (EntityNotFoundException → NOT_FOUND, IllegalArgumentException → INVALID_ARGUMENT), and always include a catch-all so nothing silently degrades to INTERNAL.","correct":"Because gRPC strips stack traces for security and maps any unmapped exception to Status.UNKNOWN with no description, so the caller cannot distinguish a bug from \"not found\" from bad input.","distractors":["With net.devh, annotate a stub field with @GrpcClient(\"channel-name\") and Spring injects a stub bound to the named channel from grpc.client.<name>.*.","The response stream never closes, so the client blocks until its own deadline fires and then reports DEADLINE_EXCEEDED — a misleading error that hides a pure server bug.","Put a gRPC-Web proxy (Envoy's gRPC-Web filter, or grpcwebproxy) in front, because browsers cannot control HTTP/2 framing and cannot speak native gRPC."],"distractorIds":["spring/spring_grpc#8744f76b81c5","spring/spring_grpc#7fee1ffdec0a","spring/spring_grpc#a38c8a35b871"],"concepts":["unmapped","strips","bad","distinguish","unknown","bug"],"correctMd":"Because gRPC strips stack traces for security and maps any unmapped exception to `Status.UNKNOWN` with no description, so the caller cannot distinguish a bug from \"not found\" from bad input.","answerFullMd":"Because gRPC strips stack traces for security and maps any unmapped exception to `Status.UNKNOWN` with no description, so the caller cannot distinguish a bug from \"not found\" from bad input. Fix it with a central `@GrpcAdvice` class whose `@GrpcExceptionHandler` methods map each domain exception to a canonical `Status` (`EntityNotFoundException` → `NOT_FOUND`, `IllegalArgumentException` → `INVALID_ARGUMENT`), and always include a catch-all so nothing silently degrades to `INTERNAL`.","distractorsMd":["With net.devh, annotate a stub field with `@GrpcClient(\"channel-name\")` and Spring injects a stub bound to the named channel from `grpc.client.<name>.*`.","The response stream never closes, so the client blocks until its own deadline fires and then reports `DEADLINE_EXCEEDED` — a misleading error that hides a pure server bug.",null]},{"id":"spring/spring_grpc#7fee1ffdec0a","section":"spring","module":"spring/spring_grpc","moduleName":"spring grpc","sourceFile":"README.md","difficulty":"intermediate","question":"What happens if a server-side handler calls onNext but forgets onCompleted?","answerFull":"The response stream never closes, so the client blocks until its own deadline fires and then reports DEADLINE_EXCEEDED — a misleading error that hides a pure server bug. Every StreamObserver must be terminated exactly once with onCompleted() (success) or onError() (failure). This is the server-side twin of the missing-deadline client hang; both manifest as a \"timeout\" that points at the wrong cause.","correct":"The response stream never closes, so the client blocks until its own deadline fires and then reports DEADLINE_EXCEEDED — a misleading error that hides a pure server bug.","distractors":["Lower @Order runs first and outermost, so the observation/tracing interceptor must be order 0 to wrap and time every inner interceptor plus the handler.","A deadline is an absolute wall-clock instant that propagates through the entire call tree, whereas a timeout is a per-call relative duration.","A channel deadline is a backstop that bounds any call whose code path forgets withDeadlineAfter, so a single missed per-call deadline cannot hang."],"distractorIds":["spring/spring_grpc#7bce7d081286","spring/spring_grpc#580e0b113272","spring/spring_grpc#b011cb4c977a"],"concepts":["onnext","forgets","misleading","closes","hides","reports"],"correctMd":"The response stream never closes, so the client blocks until its own deadline fires and then reports `DEADLINE_EXCEEDED` — a misleading error that hides a pure server bug.","answerFullMd":"The response stream never closes, so the client blocks until its own deadline fires and then reports `DEADLINE_EXCEEDED` — a misleading error that hides a pure server bug. Every `StreamObserver` must be terminated exactly once with `onCompleted()` (success) or `onError()` (failure). This is the server-side twin of the missing-deadline client hang; both manifest as a \"timeout\" that points at the wrong cause.","distractorsMd":["Lower `@Order` runs first and outermost, so the observation/tracing interceptor must be order 0 to wrap and time every inner interceptor plus the handler.",null,"A channel `deadline` is a backstop that bounds any call whose code path forgets `withDeadlineAfter`, so a single missed per-call deadline cannot hang."]},{"id":"spring/spring_grpc#7bce7d081286","section":"spring","module":"spring/spring_grpc","moduleName":"spring grpc","sourceFile":"README.md","difficulty":"intermediate","question":"How does interceptor ordering affect tracing and metrics correctness?","answerFull":"Lower @Order runs first and outermost, so the observation/tracing interceptor must be order 0 to wrap and time every inner interceptor plus the handler. If metrics or auth run outside the span, their cost is invisible in the trace and even unauthenticated calls get timed. The correct chain is observation (0) → auth (100) → authorization (150) → logging (200) → metrics (300, innermost), so the span measures the full inbound cost.","correct":"Lower @Order runs first and outermost, so the observation/tracing interceptor must be order 0 to wrap and time every inner interceptor plus the handler.","distractors":["The client interceptor serializes the current trace into the W3C traceparent metadata key on the outgoing call.","Unary and server-streaming both take a (Request, StreamObserver<Response>) signature, differing only in how many times the handler calls onNext.","@GrpcService binds a bean to the HTTP/2 gRPC server instead of the servlet dispatcher, so none of Spring MVC's servlet-scoped machinery applies."],"distractorIds":["spring/spring_grpc#e56be286bf95","spring/spring_grpc#748534fe8f6b","spring/spring_grpc#2b5a8ce1b7eb"],"concepts":["outermost","interceptor","affect","observation","correctness","wrap"],"correctMd":"Lower `@Order` runs first and outermost, so the observation/tracing interceptor must be order 0 to wrap and time every inner interceptor plus the handler.","answerFullMd":"Lower `@Order` runs first and outermost, so the observation/tracing interceptor must be order 0 to wrap and time every inner interceptor plus the handler. If metrics or auth run outside the span, their cost is invisible in the trace and even unauthenticated calls get timed. The correct chain is observation (0) → auth (100) → authorization (150) → logging (200) → metrics (300, innermost), so the span measures the full inbound cost.","distractorsMd":["The client interceptor serializes the current trace into the W3C `traceparent` metadata key on the outgoing call.","Unary and server-streaming both take a `(Request, StreamObserver<Response>)` signature, differing only in how many times the handler calls `onNext`.","`@GrpcService` binds a bean to the HTTP/2 gRPC server instead of the servlet dispatcher, so none of Spring MVC's servlet-scoped machinery applies."]},{"id":"spring/spring_grpc#e56be286bf95","section":"spring","module":"spring/spring_grpc","moduleName":"spring grpc","sourceFile":"README.md","difficulty":"intermediate","question":"How is trace context propagated across a gRPC call?","answerFull":"The client interceptor serializes the current trace into the W3C traceparent metadata key on the outgoing call. The ObservationGrpcServerInterceptor then deserializes that key and continues the span on the server, so one trace ID spans the entire call graph. Because it travels in gRPC Metadata (the header analog), this is identical in spirit to HTTP header propagation, just over gRPC. Register both interceptors from micrometer-core's binder.grpc package.","correct":"The client interceptor serializes the current trace into the W3C traceparent metadata key on the outgoing call.","distractors":["Lower @Order runs first and outermost, so the observation/tracing interceptor must be order 0 to wrap and time every inner interceptor plus the handler.","Because gRPC strips stack traces for security and maps any unmapped exception to Status.UNKNOWN with no description, so the caller cannot distinguish a bug from \"not found\" from bad input.","Because Metadata is the transport-level header channel — cross-cutting context like tokens, trace IDs, and tenant IDs belong there so interceptors can read them without parsing the domain message."],"distractorIds":["spring/spring_grpc#7bce7d081286","spring/spring_grpc#ccfffc958e7e","spring/spring_grpc#c6b0555c05b7"],"concepts":["propagated","w3c","traceparent","interceptor","outgoing","serializes"],"correctMd":"The client interceptor serializes the current trace into the W3C `traceparent` metadata key on the outgoing call.","answerFullMd":"The client interceptor serializes the current trace into the W3C `traceparent` metadata key on the outgoing call. The `ObservationGrpcServerInterceptor` then deserializes that key and continues the span on the server, so one trace ID spans the entire call graph. Because it travels in gRPC `Metadata` (the header analog), this is identical in spirit to HTTP header propagation, just over gRPC. Register both interceptors from `micrometer-core`'s `binder.grpc` package.","distractorsMd":["Lower `@Order` runs first and outermost, so the observation/tracing interceptor must be order 0 to wrap and time every inner interceptor plus the handler.","Because gRPC strips stack traces for security and maps any unmapped exception to `Status.UNKNOWN` with no description, so the caller cannot distinguish a bug from \"not found\" from bad input.",null]},{"id":"spring/spring_grpc#0c6fb2fc7e3f","section":"spring","module":"spring/spring_grpc","moduleName":"spring grpc","sourceFile":"README.md","difficulty":"intermediate","question":"Why do gRPC calls fail with RESOURCE_EXHAUSTED on large payloads?","answerFull":"Because grpc-java caps inbound messages at 4 MiB by default on both the server and the client, so a 6 MB response throws RESOURCE_EXHAUSTED: gRPC message exceeds maximum size 4194304. The right fix is to paginate or switch to server streaming so no single message is huge; only if unavoidable, raise max-inbound-message-size on both ends — a large limit invites memory pressure and makes a single call able to OOM the process.","correct":"Because grpc-java caps inbound messages at 4 MiB by default on both the server and the client, so a 6 MB response throws RESOURCE_EXHAUSTED: gRPC message exceeds maximum size 4194304.","distractors":["When a client cancels or its deadline expires, the server's gRPC Context becomes cancelled and a ServerCallStreamObserver.setOnCancelHandler callback fires.","Choose gRPC for internal service-to-service traffic where a strict typed contract, small binary payloads, low latency, and first-class streaming matter more than human readability.","The response stream never closes, so the client blocks until its own deadline fires and then reports DEADLINE_EXCEEDED — a misleading error that hides a pure server bug."],"distractorIds":["spring/spring_grpc#4aea66cfbf5f","spring/spring_grpc#13c04eacd5fa","spring/spring_grpc#7fee1ffdec0a"],"concepts":["4194304","mib","caps","exhausted","payloads","inbound"],"correctMd":"Because grpc-java caps inbound messages at 4 MiB by default on both the server and the client, so a 6 MB response throws `RESOURCE_EXHAUSTED: gRPC message exceeds maximum size 4194304`.","answerFullMd":"Because grpc-java caps inbound messages at 4 MiB by default on both the server and the client, so a 6 MB response throws `RESOURCE_EXHAUSTED: gRPC message exceeds maximum size 4194304`. The right fix is to paginate or switch to server streaming so no single message is huge; only if unavoidable, raise `max-inbound-message-size` on both ends — a large limit invites memory pressure and makes a single call able to OOM the process.","distractorsMd":["When a client cancels or its deadline expires, the server's gRPC `Context` becomes cancelled and a `ServerCallStreamObserver.setOnCancelHandler` callback fires.",null,"The response stream never closes, so the client blocks until its own deadline fires and then reports `DEADLINE_EXCEEDED` — a misleading error that hides a pure server bug."]},{"id":"spring/spring_grpc#ad50ff8aa727","section":"spring","module":"spring/spring_grpc","moduleName":"spring grpc","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between the official Spring gRPC and net.devh's starter?","answerFull":"The official Spring gRPC (1.0) is portfolio-backed and configured under the spring.grpc properties, while net.devh's starter is the mature community project under the grpc prefix. net.devh adds a richer built-in security module (GrpcSecurity, @Secured, @GrpcGlobalServerInterceptor), while both auto-configure the Netty server, register @GrpcService beans, and support global interceptors. Choose the official one for greenfield on the latest Boot; keep net.devh for existing systems that lean on its security bridge.","correct":"The official Spring gRPC (1.0) is portfolio-backed and configured under the spring.grpc properties, while net.devh's starter is the mature community project under the grpc prefix.","distractors":["Put a gRPC-Web proxy (Envoy's gRPC-Web filter, or grpcwebproxy) in front, because browsers cannot control HTTP/2 framing and cannot speak native gRPC.","With net.devh, annotate a stub field with @GrpcClient(\"channel-name\") and Spring injects a stub bound to the named channel from grpc.client.<name>.*.","Because gRPC strips stack traces for security and maps any unmapped exception to Status.UNKNOWN with no description, so the caller cannot distinguish a bug from \"not found\" from bad input."],"distractorIds":["spring/spring_grpc#a38c8a35b871","spring/spring_grpc#8744f76b81c5","spring/spring_grpc#ccfffc958e7e"],"concepts":["devh","portfolio","official","mature","starter","community"],"correctMd":"The official Spring gRPC (1.0) is portfolio-backed and configured under the `spring.grpc` properties, while net.devh's starter is the mature community project under the `grpc` prefix.","answerFullMd":"The official Spring gRPC (1.0) is portfolio-backed and configured under the `spring.grpc` properties, while net.devh's starter is the mature community project under the `grpc` prefix. net.devh adds a richer built-in security module (`GrpcSecurity`, `@Secured`, `@GrpcGlobalServerInterceptor`), while both auto-configure the Netty server, register `@GrpcService` beans, and support global interceptors. Choose the official one for greenfield on the latest Boot; keep net.devh for existing systems that lean on its security bridge.","distractorsMd":[null,"With net.devh, annotate a stub field with `@GrpcClient(\"channel-name\")` and Spring injects a stub bound to the named channel from `grpc.client.<name>.*`.","Because gRPC strips stack traces for security and maps any unmapped exception to `Status.UNKNOWN` with no description, so the caller cannot distinguish a bug from \"not found\" from bad input."]},{"id":"spring/spring_grpc#4aea66cfbf5f","section":"spring","module":"spring/spring_grpc","moduleName":"spring grpc","sourceFile":"README.md","difficulty":"advanced","question":"How does cancellation propagate, and how should a streaming handler react?","answerFull":"When a client cancels or its deadline expires, the server's gRPC Context becomes cancelled and a ServerCallStreamObserver.setOnCancelHandler callback fires. A long-running or streaming handler should check Context.current().isCancelled() before expensive steps and register an on-cancel handler to release resources (close cursors, cancel downstream calls). Ignoring cancellation wastes CPU producing results no one will read and can leak connections on long streams.","correct":"When a client cancels or its deadline expires, the server's gRPC Context becomes cancelled and a ServerCallStreamObserver.setOnCancelHandler callback fires.","distractors":["The response stream never closes, so the client blocks until its own deadline fires and then reports DEADLINE_EXCEEDED — a misleading error that hides a pure server bug.","With net.devh, annotate a stub field with @GrpcClient(\"channel-name\") and Spring injects a stub bound to the named channel from grpc.client.<name>.*.","Because grpc-java caps inbound messages at 4 MiB by default on both the server and the client, so a 6 MB response throws RESOURCE_EXHAUSTED: gRPC message exceeds maximum size 4194304."],"distractorIds":["spring/spring_grpc#7fee1ffdec0a","spring/spring_grpc#8744f76b81c5","spring/spring_grpc#0c6fb2fc7e3f"],"concepts":["servercallstreamobserver","setoncancelhandler","callback","cancellation","cancelled","propagate"],"correctMd":"When a client cancels or its deadline expires, the server's gRPC `Context` becomes cancelled and a `ServerCallStreamObserver.setOnCancelHandler` callback fires.","answerFullMd":"When a client cancels or its deadline expires, the server's gRPC `Context` becomes cancelled and a `ServerCallStreamObserver.setOnCancelHandler` callback fires. A long-running or streaming handler should check `Context.current().isCancelled()` before expensive steps and register an on-cancel handler to release resources (close cursors, cancel downstream calls). Ignoring cancellation wastes CPU producing results no one will read and can leak connections on long streams.","distractorsMd":["The response stream never closes, so the client blocks until its own deadline fires and then reports `DEADLINE_EXCEEDED` — a misleading error that hides a pure server bug.","With net.devh, annotate a stub field with `@GrpcClient(\"channel-name\")` and Spring injects a stub bound to the named channel from `grpc.client.<name>.*`.","Because grpc-java caps inbound messages at 4 MiB by default on both the server and the client, so a 6 MB response throws `RESOURCE_EXHAUSTED: gRPC message exceeds maximum size 4194304`."]},{"id":"spring/spring_grpc#8744f76b81c5","section":"spring","module":"spring/spring_grpc","moduleName":"spring grpc","sourceFile":"README.md","difficulty":"advanced","question":"How do you inject a gRPC stub as a Spring bean?","answerFull":"With net.devh, annotate a stub field with @GrpcClient(\"channel-name\") and Spring injects a stub bound to the named channel from grpc.client.<name>.*. With the official starter, build the stub in a @Bean from a GrpcChannelFactory.createChannel(\"name\"). Prefer a blocking stub for unary request/response, an async stub for streaming, and always apply withDeadlineAfter on each call rather than trusting a global default.","correct":"With net.devh, annotate a stub field with @GrpcClient(\"channel-name\") and Spring injects a stub bound to the named channel from grpc.client.<name>.*.","distractors":["Because grpc-java caps inbound messages at 4 MiB by default on both the server and the client, so a 6 MB response throws RESOURCE_EXHAUSTED: gRPC message exceeds maximum size 4194304.","Because gRPC strips stack traces for security and maps any unmapped exception to Status.UNKNOWN with no description, so the caller cannot distinguish a bug from \"not found\" from bad input.","@GrpcService binds a bean to the HTTP/2 gRPC server instead of the servlet dispatcher, so none of Spring MVC's servlet-scoped machinery applies."],"distractorIds":["spring/spring_grpc#0c6fb2fc7e3f","spring/spring_grpc#ccfffc958e7e","spring/spring_grpc#2b5a8ce1b7eb"],"concepts":["grpcclient","devh","annotate","stub","injects","net"],"correctMd":"With net.devh, annotate a stub field with `@GrpcClient(\"channel-name\")` and Spring injects a stub bound to the named channel from `grpc.client.<name>.*`.","answerFullMd":"With net.devh, annotate a stub field with `@GrpcClient(\"channel-name\")` and Spring injects a stub bound to the named channel from `grpc.client.<name>.*`. With the official starter, build the stub in a `@Bean` from a `GrpcChannelFactory.createChannel(\"name\")`. Prefer a blocking stub for unary request/response, an async stub for streaming, and always apply `withDeadlineAfter` on each call rather than trusting a global default.","distractorsMd":["Because grpc-java caps inbound messages at 4 MiB by default on both the server and the client, so a 6 MB response throws `RESOURCE_EXHAUSTED: gRPC message exceeds maximum size 4194304`.","Because gRPC strips stack traces for security and maps any unmapped exception to `Status.UNKNOWN` with no description, so the caller cannot distinguish a bug from \"not found\" from bad input.","`@GrpcService` binds a bean to the HTTP/2 gRPC server instead of the servlet dispatcher, so none of Spring MVC's servlet-scoped machinery applies."]},{"id":"spring/spring_grpc#748534fe8f6b","section":"spring","module":"spring/spring_grpc","moduleName":"spring grpc","sourceFile":"README.md","difficulty":"advanced","question":"What are the four gRPC streaming modes and their Spring method shapes?","answerFull":"Unary and server-streaming both take a (Request, StreamObserver<Response>) signature, differing only in how many times the handler calls onNext. Client-streaming and bidirectional-streaming both return a StreamObserver<Request> that receives the incoming messages. Client-streaming emits one final response after the client half-closes; bidirectional keeps both directions open concurrently. In Spring you override the generated ...ImplBase method matching the mode declared in the .proto.","correct":"Unary and server-streaming both take a (Request, StreamObserver<Response>) signature, differing only in how many times the handler calls onNext.","distractors":["Because gRPC strips stack traces for security and maps any unmapped exception to Status.UNKNOWN with no description, so the caller cannot distinguish a bug from \"not found\" from bad input.","Choose gRPC for internal service-to-service traffic where a strict typed contract, small binary payloads, low latency, and first-class streaming matter more than human readability.","@GrpcService binds a bean to the HTTP/2 gRPC server instead of the servlet dispatcher, so none of Spring MVC's servlet-scoped machinery applies."],"distractorIds":["spring/spring_grpc#ccfffc958e7e","spring/spring_grpc#13c04eacd5fa","spring/spring_grpc#2b5a8ce1b7eb"],"concepts":["onnext","streamobserver","differing","shapes","unary","modes"],"correctMd":"Unary and server-streaming both take a `(Request, StreamObserver<Response>)` signature, differing only in how many times the handler calls `onNext`.","answerFullMd":"Unary and server-streaming both take a `(Request, StreamObserver<Response>)` signature, differing only in how many times the handler calls `onNext`. Client-streaming and bidirectional-streaming both return a `StreamObserver<Request>` that receives the incoming messages. Client-streaming emits one final response after the client half-closes; bidirectional keeps both directions open concurrently. In Spring you override the generated `...ImplBase` method matching the mode declared in the `.proto`.","distractorsMd":["Because gRPC strips stack traces for security and maps any unmapped exception to `Status.UNKNOWN` with no description, so the caller cannot distinguish a bug from \"not found\" from bad input.",null,"`@GrpcService` binds a bean to the HTTP/2 gRPC server instead of the servlet dispatcher, so none of Spring MVC's servlet-scoped machinery applies."]},{"id":"spring/spring_grpc#23881dc3767a","section":"spring","module":"spring/spring_grpc","moduleName":"spring grpc","sourceFile":"README.md","difficulty":"advanced","question":"How do you secure gRPC endpoints with method-level authorization?","answerFull":"Authenticate in a ServerInterceptor that populates identity, then guard methods with @Secured or @PreAuthorize. net.devh's security bridge enforces those annotations by copying the gRPC-context identity into SecurityContextHolder before the handler runs. The interceptor answers \"who are you?\" (UNAUTHENTICATED on failure) and the annotation answers \"may you?\" (PERMISSION_DENIED on failure). Keeping the two separate mirrors Spring Security's authentication-vs-authorization split.","correct":"Authenticate in a ServerInterceptor that populates identity, then guard methods with @Secured or @PreAuthorize.","distractors":["Choose gRPC for internal service-to-service traffic where a strict typed contract, small binary payloads, low latency, and first-class streaming matter more than human readability.","Put a gRPC-Web proxy (Envoy's gRPC-Web filter, or grpcwebproxy) in front, because browsers cannot control HTTP/2 framing and cannot speak native gRPC.","@GrpcService binds a bean to the HTTP/2 gRPC server instead of the servlet dispatcher, so none of Spring MVC's servlet-scoped machinery applies."],"distractorIds":["spring/spring_grpc#13c04eacd5fa","spring/spring_grpc#a38c8a35b871","spring/spring_grpc#2b5a8ce1b7eb"],"concepts":["secured","authenticate","preauthorize","serverinterceptor","guard","secure"],"correctMd":"Authenticate in a `ServerInterceptor` that populates identity, then guard methods with `@Secured` or `@PreAuthorize`.","answerFullMd":"Authenticate in a `ServerInterceptor` that populates identity, then guard methods with `@Secured` or `@PreAuthorize`. net.devh's security bridge enforces those annotations by copying the gRPC-context identity into `SecurityContextHolder` before the handler runs. The interceptor answers \"who are you?\" (`UNAUTHENTICATED` on failure) and the annotation answers \"may you?\" (`PERMISSION_DENIED` on failure). Keeping the two separate mirrors Spring Security's authentication-vs-authorization split.","distractorsMd":[null,null,"`@GrpcService` binds a bean to the HTTP/2 gRPC server instead of the servlet dispatcher, so none of Spring MVC's servlet-scoped machinery applies."]},{"id":"spring/spring_grpc#a38c8a35b871","section":"spring","module":"spring/spring_grpc","moduleName":"spring grpc","sourceFile":"README.md","difficulty":"advanced","question":"How do you call a Spring gRPC service from a browser?","answerFull":"Put a gRPC-Web proxy (Envoy's gRPC-Web filter, or grpcwebproxy) in front, because browsers cannot control HTTP/2 framing and cannot speak native gRPC. The proxy translates the browser's application/grpc-web+proto over HTTP/1.1 into native gRPC to the Spring @GrpcService. Alternatively expose a REST façade via transcoding (a gateway maps JSON/HTTP to the gRPC method using google.api.http annotations) when you want plain REST for external clients.","correct":"Put a gRPC-Web proxy (Envoy's gRPC-Web filter, or grpcwebproxy) in front, because browsers cannot control HTTP/2 framing and cannot speak native gRPC.","distractors":["A channel deadline is a backstop that bounds any call whose code path forgets withDeadlineAfter, so a single missed per-call deadline cannot hang.","Choose gRPC for internal service-to-service traffic where a strict typed contract, small binary payloads, low latency, and first-class streaming matter more than human readability.","The official Spring gRPC (1.0) is portfolio-backed and configured under the spring.grpc properties, while net.devh's starter is the mature community project under the grpc prefix."],"distractorIds":["spring/spring_grpc#b011cb4c977a","spring/spring_grpc#13c04eacd5fa","spring/spring_grpc#ad50ff8aa727"],"concepts":["grpcwebproxy","framing","speak","envoy","browsers","front"],"answerFullMd":"Put a gRPC-Web proxy (Envoy's gRPC-Web filter, or grpcwebproxy) in front, because browsers cannot control HTTP/2 framing and cannot speak native gRPC. The proxy translates the browser's `application/grpc-web+proto` over HTTP/1.1 into native gRPC to the Spring `@GrpcService`. Alternatively expose a REST façade via transcoding (a gateway maps JSON/HTTP to the gRPC method using `google.api.http` annotations) when you want plain REST for external clients.","distractorsMd":["A channel `deadline` is a backstop that bounds any call whose code path forgets `withDeadlineAfter`, so a single missed per-call deadline cannot hang.",null,"The official Spring gRPC (1.0) is portfolio-backed and configured under the `spring.grpc` properties, while net.devh's starter is the mature community project under the `grpc` prefix."]},{"id":"spring/spring_grpc#c6b0555c05b7","section":"spring","module":"spring/spring_grpc","moduleName":"spring grpc","sourceFile":"README.md","difficulty":"advanced","question":"Why is Metadata used for auth tokens instead of the Protobuf message?","answerFull":"Because Metadata is the transport-level header channel — cross-cutting context like tokens, trace IDs, and tenant IDs belong there so interceptors can read them without parsing the domain message. Putting an auth token in the request message couples every method's schema to security concerns and forces the handler (not an interceptor) to enforce auth. Keys use Metadata.Key.of(name, ASCII_STRING_MARSHALLER); binary values use a -bin suffix key.","correct":"Because Metadata is the transport-level header channel — cross-cutting context like tokens, trace IDs, and tenant IDs belong there so interceptors can read them without parsing the domain message.","distractors":["The client interceptor serializes the current trace into the W3C traceparent metadata key on the outgoing call.","A channel deadline is a backstop that bounds any call whose code path forgets withDeadlineAfter, so a single missed per-call deadline cannot hang.","With net.devh, annotate a stub field with @GrpcClient(\"channel-name\") and Spring injects a stub bound to the named channel from grpc.client.<name>.*."],"distractorIds":["spring/spring_grpc#e56be286bf95","spring/spring_grpc#b011cb4c977a","spring/spring_grpc#8744f76b81c5"],"concepts":["belong","interceptors","protobuf","cutting","tenant","parsing"],"answerFullMd":"Because Metadata is the transport-level header channel — cross-cutting context like tokens, trace IDs, and tenant IDs belong there so interceptors can read them without parsing the domain message. Putting an auth token in the request message couples every method's schema to security concerns and forces the handler (not an interceptor) to enforce auth. Keys use `Metadata.Key.of(name, ASCII_STRING_MARSHALLER)`; binary values use a `-bin` suffix key.","distractorsMd":["The client interceptor serializes the current trace into the W3C `traceparent` metadata key on the outgoing call.","A channel `deadline` is a backstop that bounds any call whose code path forgets `withDeadlineAfter`, so a single missed per-call deadline cannot hang.","With net.devh, annotate a stub field with `@GrpcClient(\"channel-name\")` and Spring injects a stub bound to the named channel from `grpc.client.<name>.*`."]},{"id":"spring/spring_grpc#b011cb4c977a","section":"spring","module":"spring/spring_grpc","moduleName":"spring grpc","sourceFile":"README.md","difficulty":"advanced","question":"What does a channel-level default deadline give you over a per-call deadline?","answerFull":"A channel deadline is a backstop that bounds any call whose code path forgets withDeadlineAfter, so a single missed per-call deadline cannot hang. The per-call deadline is still preferred because it budgets the whole downstream tree for that specific request, while the channel default is a blunt catch-all. Configure both: a strict per-call deadline for correctness and a generous channel deadline as a safety net.","correct":"A channel deadline is a backstop that bounds any call whose code path forgets withDeadlineAfter, so a single missed per-call deadline cannot hang.","distractors":["A deadline is an absolute wall-clock instant that propagates through the entire call tree, whereas a timeout is a per-call relative duration.","Because a blocking stub with no deadline waits indefinitely for a response that a slow or dead-but-listening peer never sends.","The response stream never closes, so the client blocks until its own deadline fires and then reports DEADLINE_EXCEEDED — a misleading error that hides a pure server bug."],"distractorIds":["spring/spring_grpc#580e0b113272","spring/spring_grpc#c94d76046734","spring/spring_grpc#7fee1ffdec0a"],"concepts":["backstop","withdeadlineafter","forgets","hang","missed","bounds"],"correctMd":"A channel `deadline` is a backstop that bounds any call whose code path forgets `withDeadlineAfter`, so a single missed per-call deadline cannot hang.","answerFullMd":"A channel `deadline` is a backstop that bounds any call whose code path forgets `withDeadlineAfter`, so a single missed per-call deadline cannot hang. The per-call deadline is still preferred because it budgets the whole downstream tree for that specific request, while the channel default is a blunt catch-all. Configure both: a strict per-call deadline for correctness and a generous channel deadline as a safety net.","distractorsMd":[null,null,"The response stream never closes, so the client blocks until its own deadline fires and then reports `DEADLINE_EXCEEDED` — a misleading error that hides a pure server bug."]},{"id":"spring/spring_grpc#37a33dd42e4c","section":"spring","module":"spring/spring_grpc","moduleName":"spring grpc","sourceFile":"README.md","difficulty":"advanced","question":"How do you keep gRPC serialization out of your service's critical CPU path under load?","answerFull":"Rely on Protobuf's compact binary encoding, which is far smaller than JSON and parses faster, so serialization rarely dominates. Then avoid oversized messages by paginating or streaming instead of returning multi-MB blobs. Reuse generated builders rather than reflection, keep field numbers stable to avoid re-encoding surprises, and let the Netty transport multiplex many calls over one HTTP/2 connection so you are not paying per-call connection setup. The wire-level detail lives in gRPC & Protobuf (pure Java).","correct":"Rely on Protobuf's compact binary encoding, which is far smaller than JSON and parses faster, so serialization rarely dominates.","distractors":["When a client cancels or its deadline expires, the server's gRPC Context becomes cancelled and a ServerCallStreamObserver.setOnCancelHandler callback fires.","@GrpcService binds a bean to the HTTP/2 gRPC server instead of the servlet dispatcher, so none of Spring MVC's servlet-scoped machinery applies.","Choose gRPC for internal service-to-service traffic where a strict typed contract, small binary payloads, low latency, and first-class streaming matter more than human readability."],"distractorIds":["spring/spring_grpc#4aea66cfbf5f","spring/spring_grpc#2b5a8ce1b7eb","spring/spring_grpc#13c04eacd5fa"],"concepts":["parses","protobuf","dominates","rely","rarely","critical"],"distractorsMd":["When a client cancels or its deadline expires, the server's gRPC `Context` becomes cancelled and a `ServerCallStreamObserver.setOnCancelHandler` callback fires.","`@GrpcService` binds a bean to the HTTP/2 gRPC server instead of the servlet dispatcher, so none of Spring MVC's servlet-scoped machinery applies.",null]},{"id":"spring/spring_grpc#13c04eacd5fa","section":"spring","module":"spring/spring_grpc","moduleName":"spring grpc","sourceFile":"README.md","difficulty":"advanced","question":"When would you choose gRPC over REST or GraphQL for a new API?","answerFull":"Choose gRPC for internal service-to-service traffic where a strict typed contract, small binary payloads, low latency, and first-class streaming matter more than human readability. Choose REST for public or browser-facing APIs that need the broadest client reach and curl-level debuggability, and GraphQL when clients need to shape aggregated responses across many resources. gRPC's browser gap (needs gRPC-Web) and binary opacity are the usual reasons it stays internal.","correct":"Choose gRPC for internal service-to-service traffic where a strict typed contract, small binary payloads, low latency, and first-class streaming matter more than human readability.","distractors":["Put a gRPC-Web proxy (Envoy's gRPC-Web filter, or grpcwebproxy) in front, because browsers cannot control HTTP/2 framing and cannot speak native gRPC.","Read the token from Metadata in a ServerInterceptor, build an Authentication, and bind it into the gRPC Context with Contexts.interceptCall.","When a client cancels or its deadline expires, the server's gRPC Context becomes cancelled and a ServerCallStreamObserver.setOnCancelHandler callback fires."],"distractorIds":["spring/spring_grpc#a38c8a35b871","spring/spring_grpc#c377c8d6c2f5","spring/spring_grpc#4aea66cfbf5f"],"concepts":["readability","matter","payloads","strict","graphql","contract"],"answerFullMd":"Choose gRPC for internal service-to-service traffic where a strict typed contract, small binary payloads, low latency, and first-class streaming matter more than human readability. Choose REST for public or browser-facing APIs that need the broadest client reach and `curl`-level debuggability, and GraphQL when clients need to shape aggregated responses across many resources. gRPC's browser gap (needs gRPC-Web) and binary opacity are the usual reasons it stays internal.","distractorsMd":[null,"Read the token from `Metadata` in a `ServerInterceptor`, build an `Authentication`, and bind it into the gRPC `Context` with `Contexts.interceptCall`.","When a client cancels or its deadline expires, the server's gRPC `Context` becomes cancelled and a `ServerCallStreamObserver.setOnCancelHandler` callback fires."]},{"id":"spring/spring_hateoas_rest_maturity#67151300340a","section":"spring","module":"spring/spring_hateoas_rest_maturity","moduleName":"spring hateoas rest maturity","sourceFile":"README.md","difficulty":"core","question":"What does HATEOAS actually add over a normal REST API, and why is it Level 3?","answerFull":"HATEOAS adds hypermedia links to responses so the client discovers available actions instead of hardcoding URLs. In the Richardson Maturity Model, Level 1 adds resource URIs, Level 2 adds proper verbs and status codes, and Level 3 (HATEOAS) adds the _links that let the response itself drive the client's next request. The practical payoff is that the server can relocate endpoints or gate transitions by state without breaking clients — but only if the client is written to follow links.","correct":"HATEOAS adds hypermedia links to responses so the client discovers available actions instead of hardcoding URLs.","distractors":["Once clients navigate by relation name, a rel like pay is part of the published contract exactly as a URL is, so renaming it to payment breaks every client that looked up _links.pay.","In a hypermedia API the assembler adds a cancel or pay link only when the resource's current state (and the caller's permissions) actually allow that action, so the link set *is* the state machine.","Spring Data REST is built on Spring HATEOAS and auto-exposes JPA/Mongo repositories as a HAL API, generating _links, _embedded, pagination links, and an ALPS/HAL-FORMS profile with no hand-written assemblers."],"distractorIds":["spring/spring_hateoas_rest_maturity#a0033506a076","spring/spring_hateoas_rest_maturity#1000e511020d","spring/spring_hateoas_rest_maturity#2a1e91158ed2"],"concepts":["hardcoding","hypermedia","discovers","hateoas","urls","links"],"answerFullMd":"HATEOAS adds hypermedia links to responses so the client discovers available actions instead of hardcoding URLs. In the Richardson Maturity Model, Level 1 adds resource URIs, Level 2 adds proper verbs and status codes, and Level 3 (HATEOAS) adds the `_links` that let the response itself drive the client's next request. The practical payoff is that the server can relocate endpoints or gate transitions by state without breaking clients — but only if the client is written to follow links.","distractorsMd":["Once clients navigate by relation name, a `rel` like `pay` is part of the published contract exactly as a URL is, so renaming it to `payment` breaks every client that looked up `_links.pay`.","In a hypermedia API the assembler adds a `cancel` or `pay` link only when the resource's current state (and the caller's permissions) actually allow that action, so the link set *is* the state machine.","Spring Data REST is built on Spring HATEOAS and auto-exposes JPA/Mongo repositories as a HAL API, generating `_links`, `_embedded`, pagination links, and an ALPS/HAL-FORMS profile with no hand-written assemblers."]},{"id":"spring/spring_hateoas_rest_maturity#2a8ad7f05f9b","section":"spring","module":"spring/spring_hateoas_rest_maturity","moduleName":"spring hateoas rest maturity","sourceFile":"README.md","difficulty":"core","question":"Why is hardcoding \"/orders/\" + id in a response body considered broken in Spring HATEOAS?","answerFull":"Because a concatenated string bakes in the host, scheme, and context path, so it breaks behind a load balancer, under a different context path, or when versioning changes the route. linkTo(methodOn(OrderController.class).getOrder(id)).withSelfRel() instead reads the mapping off the controller method and resolves the current request's base URL, producing a correct absolute href everywhere. This is the single most common HATEOAS mistake in code review.","correct":"Because a concatenated string bakes in the host, scheme, and context path, so it breaks behind a load balancer, under a different context path, or when versioning changes the route.","distractors":["ProblemDetail (Spring 6 / Boot 3) is Spring's implementation of RFC 7807, a standardized JSON error body with type, title, status, detail, and instance, served as application/problem+json.","Traverson is Spring HATEOAS's Java client for navigating a HAL API by following rel names rather than building URLs, e.g. traverson.follow(\"orders\", \"pay\").","Spring Data REST is built on Spring HATEOAS and auto-exposes JPA/Mongo repositories as a HAL API, generating _links, _embedded, pagination links, and an ALPS/HAL-FORMS profile with no hand-written assemblers."],"distractorIds":["spring/spring_hateoas_rest_maturity#efc85e62779f","spring/spring_hateoas_rest_maturity#eb998bd4820c","spring/spring_hateoas_rest_maturity#2a1e91158ed2"],"concepts":["hardcoding","considered","bakes","scheme","broken","concatenated"],"questionMd":"Why is hardcoding `\"/orders/\" + id` in a response body considered broken in Spring HATEOAS?","answerFullMd":"Because a concatenated string bakes in the host, scheme, and context path, so it breaks behind a load balancer, under a different context path, or when versioning changes the route. `linkTo(methodOn(OrderController.class).getOrder(id)).withSelfRel()` instead reads the mapping off the controller method and resolves the current request's base URL, producing a correct absolute href everywhere. This is the single most common HATEOAS mistake in code review.","distractorsMd":["`ProblemDetail` (Spring 6 / Boot 3) is Spring's implementation of RFC 7807, a standardized JSON error body with `type`, `title`, `status`, `detail`, and `instance`, served as `application/problem+json`.","Traverson is Spring HATEOAS's Java client for navigating a HAL API by following `rel` names rather than building URLs, e.g. `traverson.follow(\"orders\", \"pay\")`.","Spring Data REST is built on Spring HATEOAS and auto-exposes JPA/Mongo repositories as a HAL API, generating `_links`, `_embedded`, pagination links, and an ALPS/HAL-FORMS profile with no hand-written assemblers."]},{"id":"spring/spring_hateoas_rest_maturity#5cf907ff028e","section":"spring","module":"spring/spring_hateoas_rest_maturity","moduleName":"spring hateoas rest maturity","sourceFile":"README.md","difficulty":"core","question":"Most APIs call themselves REST but sit at Level 2 — what does that mean and is it wrong?","answerFull":"It means they use resource URIs plus correct HTTP verbs and status codes but do not return hypermedia links, so clients still template URLs. It is not wrong: Level 2 is a perfectly reasonable, widely-recommended target, and HATEOAS is only worth its extra payload and client complexity when you have a real state machine or evolvability requirement. Roy Fielding argues true REST requires Level 3, but pragmatically most teams stop at Level 2 deliberately.","correct":"It means they use resource URIs plus correct HTTP verbs and status codes but do not return hypermedia links, so clients still template URLs.","distractors":["HATEOAS adds hypermedia links to responses so the client discovers available actions instead of hardcoding URLs.","URI versioning (/api/v2/orders) is the most visible and cache-friendly but puts a version in the resource identity, which purists dislike.","HAL (application/hal+json) describes *where* you can go via _links and embeds related resources in _embedded."],"distractorIds":["spring/spring_hateoas_rest_maturity#67151300340a","spring/spring_hateoas_rest_maturity#cd5292c9acc8","spring/spring_hateoas_rest_maturity#eb9c4e2040ff"],"concepts":["uris","verbs","sit","themselves","hypermedia","codes"],"distractorsMd":[null,"URI versioning (`/api/v2/orders`) is the most visible and cache-friendly but puts a version in the resource identity, which purists dislike.","HAL (`application/hal+json`) describes *where* you can go via `_links` and embeds related resources in `_embedded`."]},{"id":"spring/spring_hateoas_rest_maturity#abd0caedb9ce","section":"spring","module":"spring/spring_hateoas_rest_maturity","moduleName":"spring hateoas rest maturity","sourceFile":"README.md","difficulty":"core","question":"What is the difference between EntityModel, CollectionModel, and PagedModel?","answerFull":"EntityModel<T> wraps a single resource plus its links, CollectionModel<T> wraps a list of models, and PagedModel<T> adds page metadata plus navigation links. CollectionModel carries collection-level links, while PagedModel extends it with a page block (size, totalElements, totalPages, number) and first/prev/next/last links. You return EntityModel from a single-item GET, CollectionModel from a list, and PagedModel (via PagedResourcesAssembler) from a paginated list — the client pages by following next rather than constructing ?page=N.","correct":"EntityModel<T> wraps a single resource plus its links, CollectionModel<T> wraps a list of models, and PagedModel<T> adds page metadata plus navigation links.","distractors":["Spring Data REST is built on Spring HATEOAS and auto-exposes JPA/Mongo repositories as a HAL API, generating _links, _embedded, pagination links, and an ALPS/HAL-FORMS profile with no hand-written assemblers.","HATEOAS adds hypermedia links to responses so the client discovers available actions instead of hardcoding URLs.","Once clients navigate by relation name, a rel like pay is part of the published contract exactly as a URL is, so renaming it to payment breaks every client that looked up _links.pay."],"distractorIds":["spring/spring_hateoas_rest_maturity#2a1e91158ed2","spring/spring_hateoas_rest_maturity#67151300340a","spring/spring_hateoas_rest_maturity#a0033506a076"],"concepts":["collectionmodel","pagedmodel","entitymodel","navigation","difference","links"],"questionMd":"What is the difference between `EntityModel`, `CollectionModel`, and `PagedModel`?","correctMd":"`EntityModel<T>` wraps a single resource plus its links, `CollectionModel<T>` wraps a list of models, and `PagedModel<T>` adds page metadata plus navigation links.","answerFullMd":"`EntityModel<T>` wraps a single resource plus its links, `CollectionModel<T>` wraps a list of models, and `PagedModel<T>` adds page metadata plus navigation links. `CollectionModel` carries collection-level links, while `PagedModel` extends it with a `page` block (`size`, `totalElements`, `totalPages`, `number`) and `first`/`prev`/`next`/`last` links. You return `EntityModel` from a single-item GET, `CollectionModel` from a list, and `PagedModel` (via `PagedResourcesAssembler`) from a paginated list — the client pages by following `next` rather than constructing `?page=N`.","distractorsMd":["Spring Data REST is built on Spring HATEOAS and auto-exposes JPA/Mongo repositories as a HAL API, generating `_links`, `_embedded`, pagination links, and an ALPS/HAL-FORMS profile with no hand-written assemblers.",null,"Once clients navigate by relation name, a `rel` like `pay` is part of the published contract exactly as a URL is, so renaming it to `payment` breaks every client that looked up `_links.pay`."]},{"id":"spring/spring_hateoas_rest_maturity#aa8a5e6b106f","section":"spring","module":"spring/spring_hateoas_rest_maturity","moduleName":"spring hateoas rest maturity","sourceFile":"README.md","difficulty":"core","question":"What does linkTo(methodOn(...)) do under the hood, and what is its main limitation?","answerFull":"methodOn returns a proxy that records a dummy invocation of the controller method without running its body, letting linkTo read the method's @RequestMapping metadata and build the URL. The limitation is that anything the real method computes from the current request or thread (SecurityContext, request attributes) is invisible, because the body never executes. Pass all URL-affecting values as method arguments, or fall back to linkTo(Controller.class).slash(id).","correct":"methodOn returns a proxy that records a dummy invocation of the controller method without running its body, letting linkTo read the method's @RequestMapping metadata and build the URL.","distractors":["EntityModel<T> wraps a single resource plus its links, CollectionModel<T> wraps a list of models, and PagedModel<T> adds page metadata plus navigation links.","It is an interface with a toModel(entity) method that centralizes conversion of a domain object into a linked EntityModel, keeping link logic out of controllers.","ProblemDetail (Spring 6 / Boot 3) is Spring's implementation of RFC 7807, a standardized JSON error body with type, title, status, detail, and instance, served as application/problem+json."],"distractorIds":["spring/spring_hateoas_rest_maturity#abd0caedb9ce","spring/spring_hateoas_rest_maturity#a64fe49d30da","spring/spring_hateoas_rest_maturity#efc85e62779f"],"concepts":["hood","linkto","methodon","requestmapping","dummy","invocation"],"questionMd":"What does `linkTo(methodOn(...))` do under the hood, and what is its main limitation?","correctMd":"`methodOn` returns a proxy that records a dummy invocation of the controller method without running its body, letting `linkTo` read the method's `@RequestMapping` metadata and build the URL.","answerFullMd":"`methodOn` returns a proxy that records a dummy invocation of the controller method without running its body, letting `linkTo` read the method's `@RequestMapping` metadata and build the URL. The limitation is that anything the real method computes from the current request or thread (SecurityContext, request attributes) is invisible, because the body never executes. Pass all URL-affecting values as method arguments, or fall back to `linkTo(Controller.class).slash(id)`.","distractorsMd":["`EntityModel<T>` wraps a single resource plus its links, `CollectionModel<T>` wraps a list of models, and `PagedModel<T>` adds page metadata plus navigation links.","It is an interface with a `toModel(entity)` method that centralizes conversion of a domain object into a linked `EntityModel`, keeping link logic out of controllers.","`ProblemDetail` (Spring 6 / Boot 3) is Spring's implementation of RFC 7807, a standardized JSON error body with `type`, `title`, `status`, `detail`, and `instance`, served as `application/problem+json`."]},{"id":"spring/spring_hateoas_rest_maturity#eb9c4e2040ff","section":"spring","module":"spring/spring_hateoas_rest_maturity","moduleName":"spring hateoas rest maturity","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between HAL and HAL-FORMS, and which media type triggers each?","answerFull":"HAL (application/hal+json) describes *where* you can go via _links and embeds related resources in _embedded. HAL-FORMS (application/prs.hal-forms+json) adds _templates that describe *how* to invoke an action — its HTTP method, target URL, and expected input fields. You get HAL-FORMS only when the client sends Accept: application/prs.hal-forms+json and you attached affordances to the link; otherwise you get plain HAL.","correct":"HAL (application/hal+json) describes *where* you can go via _links and embeds related resources in _embedded.","distractors":["Once clients navigate by relation name, a rel like pay is part of the published contract exactly as a URL is, so renaming it to payment breaks every client that looked up _links.pay.","ProblemDetail (Spring 6 / Boot 3) is Spring's implementation of RFC 7807, a standardized JSON error body with type, title, status, detail, and instance, served as application/problem+json.","EntityModel<T> wraps a single resource plus its links, CollectionModel<T> wraps a list of models, and PagedModel<T> adds page metadata plus navigation links."],"distractorIds":["spring/spring_hateoas_rest_maturity#a0033506a076","spring/spring_hateoas_rest_maturity#efc85e62779f","spring/spring_hateoas_rest_maturity#abd0caedb9ce"],"concepts":["hal","media","forms","embeds","describes","difference"],"correctMd":"HAL (`application/hal+json`) describes *where* you can go via `_links` and embeds related resources in `_embedded`.","answerFullMd":"HAL (`application/hal+json`) describes *where* you can go via `_links` and embeds related resources in `_embedded`. HAL-FORMS (`application/prs.hal-forms+json`) adds `_templates` that describe *how* to invoke an action — its HTTP method, target URL, and expected input fields. You get HAL-FORMS only when the client sends `Accept: application/prs.hal-forms+json` and you attached affordances to the link; otherwise you get plain HAL.","distractorsMd":["Once clients navigate by relation name, a `rel` like `pay` is part of the published contract exactly as a URL is, so renaming it to `payment` breaks every client that looked up `_links.pay`.","`ProblemDetail` (Spring 6 / Boot 3) is Spring's implementation of RFC 7807, a standardized JSON error body with `type`, `title`, `status`, `detail`, and `instance`, served as `application/problem+json`.","`EntityModel<T>` wraps a single resource plus its links, `CollectionModel<T>` wraps a list of models, and `PagedModel<T>` adds page metadata plus navigation links."]},{"id":"spring/spring_hateoas_rest_maturity#a64fe49d30da","section":"spring","module":"spring/spring_hateoas_rest_maturity","moduleName":"spring hateoas rest maturity","sourceFile":"README.md","difficulty":"intermediate","question":"What is RepresentationModelAssembler and why use it instead of building links in the controller?","answerFull":"It is an interface with a toModel(entity) method that centralizes conversion of a domain object into a linked EntityModel, keeping link logic out of controllers. This avoids duplicating link-building across every endpoint that returns the same resource and gives you one place to encode state-dependent links (show pay/cancel only when NEW). Controllers then just call assembler.toModel(order) and stay thin.","correct":"It is an interface with a toModel(entity) method that centralizes conversion of a domain object into a linked EntityModel, keeping link logic out of controllers.","distractors":["It means they use resource URIs plus correct HTTP verbs and status codes but do not return hypermedia links, so clients still template URLs.","Spring Data REST is built on Spring HATEOAS and auto-exposes JPA/Mongo repositories as a HAL API, generating _links, _embedded, pagination links, and an ALPS/HAL-FORMS profile with no hand-written assemblers.","In a hypermedia API the assembler adds a cancel or pay link only when the resource's current state (and the caller's permissions) actually allow that action, so the link set *is* the state machine."],"distractorIds":["spring/spring_hateoas_rest_maturity#5cf907ff028e","spring/spring_hateoas_rest_maturity#2a1e91158ed2","spring/spring_hateoas_rest_maturity#1000e511020d"],"concepts":["tomodel","entitymodel","centralizes","controllers","conversion","link"],"questionMd":"What is `RepresentationModelAssembler` and why use it instead of building links in the controller?","correctMd":"It is an interface with a `toModel(entity)` method that centralizes conversion of a domain object into a linked `EntityModel`, keeping link logic out of controllers.","answerFullMd":"It is an interface with a `toModel(entity)` method that centralizes conversion of a domain object into a linked `EntityModel`, keeping link logic out of controllers. This avoids duplicating link-building across every endpoint that returns the same resource and gives you one place to encode state-dependent links (show `pay`/`cancel` only when NEW). Controllers then just call `assembler.toModel(order)` and stay thin.","distractorsMd":[null,"Spring Data REST is built on Spring HATEOAS and auto-exposes JPA/Mongo repositories as a HAL API, generating `_links`, `_embedded`, pagination links, and an ALPS/HAL-FORMS profile with no hand-written assemblers.","In a hypermedia API the assembler adds a `cancel` or `pay` link only when the resource's current state (and the caller's permissions) actually allow that action, so the link set *is* the state machine."]},{"id":"spring/spring_hateoas_rest_maturity#efc85e62779f","section":"spring","module":"spring/spring_hateoas_rest_maturity","moduleName":"spring hateoas rest maturity","sourceFile":"README.md","difficulty":"intermediate","question":"What is ProblemDetail and what problem does it solve?","answerFull":"ProblemDetail (Spring 6 / Boot 3) is Spring's implementation of RFC 7807, a standardized JSON error body with type, title, status, detail, and instance, served as application/problem+json. It replaces every team inventing its own error shape, so a client writes one error parser for all endpoints. You create one with ProblemDetail.forStatus(...) in an @ExceptionHandler and can add custom properties like a fieldErrors map.","correct":"ProblemDetail (Spring 6 / Boot 3) is Spring's implementation of RFC 7807, a standardized JSON error body with type, title, status, detail, and instance, served as application/problem+json.","distractors":["methodOn returns a proxy that records a dummy invocation of the controller method without running its body, letting linkTo read the method's @RequestMapping metadata and build the URL.","Traverson is Spring HATEOAS's Java client for navigating a HAL API by following rel names rather than building URLs, e.g. traverson.follow(\"orders\", \"pay\").","You declare a Java interface with methods annotated @GetExchange/@PostExchange, and HttpServiceProxyFactory generates an implementation at runtime."],"distractorIds":["spring/spring_hateoas_rest_maturity#aa8a5e6b106f","spring/spring_hateoas_rest_maturity#eb998bd4820c","spring/spring_hateoas_rest_maturity#47860ff685cd"],"concepts":["problemdetail","7807","title","detail","rfc","served"],"questionMd":"What is `ProblemDetail` and what problem does it solve?","correctMd":"`ProblemDetail` (Spring 6 / Boot 3) is Spring's implementation of RFC 7807, a standardized JSON error body with `type`, `title`, `status`, `detail`, and `instance`, served as `application/problem+json`.","answerFullMd":"`ProblemDetail` (Spring 6 / Boot 3) is Spring's implementation of RFC 7807, a standardized JSON error body with `type`, `title`, `status`, `detail`, and `instance`, served as `application/problem+json`. It replaces every team inventing its own error shape, so a client writes one error parser for all endpoints. You create one with `ProblemDetail.forStatus(...)` in an `@ExceptionHandler` and can add custom properties like a `fieldErrors` map.","distractorsMd":["`methodOn` returns a proxy that records a dummy invocation of the controller method without running its body, letting `linkTo` read the method's `@RequestMapping` metadata and build the URL.","Traverson is Spring HATEOAS's Java client for navigating a HAL API by following `rel` names rather than building URLs, e.g. `traverson.follow(\"orders\", \"pay\")`.","You declare a Java interface with methods annotated `@GetExchange`/`@PostExchange`, and `HttpServiceProxyFactory` generates an implementation at runtime."]},{"id":"spring/spring_hateoas_rest_maturity#912856ffc893","section":"spring","module":"spring/spring_hateoas_rest_maturity","moduleName":"spring hateoas rest maturity","sourceFile":"README.md","difficulty":"intermediate","question":"When would you choose RestClient over RestTemplate or WebClient?","answerFull":"Use RestClient (GA in Boot 3.2) for synchronous, blocking HTTP in a servlet application — it is the modern fluent replacement for RestTemplate, which is now in maintenance mode. Use WebClient when you need reactive, non-blocking calls or very high concurrency fan-out. RestTemplate is only for legacy code you are not modernizing; it is supported but receives no new features.","correct":"Use RestClient (GA in Boot 3.2) for synchronous, blocking HTTP in a servlet application — it is the modern fluent replacement for RestTemplate, which is now in maintenance mode.","distractors":["ProblemDetail (Spring 6 / Boot 3) is Spring's implementation of RFC 7807, a standardized JSON error body with type, title, status, detail, and instance, served as application/problem+json.","HAL (application/hal+json) describes *where* you can go via _links and embeds related resources in _embedded.","It means they use resource URIs plus correct HTTP verbs and status codes but do not return hypermedia links, so clients still template URLs."],"distractorIds":["spring/spring_hateoas_rest_maturity#efc85e62779f","spring/spring_hateoas_rest_maturity#eb9c4e2040ff","spring/spring_hateoas_rest_maturity#5cf907ff028e"],"concepts":["restclient","resttemplate","webclient","maintenance","fluent","modern"],"questionMd":"When would you choose `RestClient` over `RestTemplate` or `WebClient`?","correctMd":"Use `RestClient` (GA in Boot 3.2) for synchronous, blocking HTTP in a servlet application — it is the modern fluent replacement for `RestTemplate`, which is now in maintenance mode.","answerFullMd":"Use `RestClient` (GA in Boot 3.2) for synchronous, blocking HTTP in a servlet application — it is the modern fluent replacement for `RestTemplate`, which is now in maintenance mode. Use `WebClient` when you need reactive, non-blocking calls or very high concurrency fan-out. `RestTemplate` is only for legacy code you are not modernizing; it is supported but receives no new features.","distractorsMd":["`ProblemDetail` (Spring 6 / Boot 3) is Spring's implementation of RFC 7807, a standardized JSON error body with `type`, `title`, `status`, `detail`, and `instance`, served as `application/problem+json`.","HAL (`application/hal+json`) describes *where* you can go via `_links` and embeds related resources in `_embedded`.",null]},{"id":"spring/spring_hateoas_rest_maturity#47860ff685cd","section":"spring","module":"spring/spring_hateoas_rest_maturity","moduleName":"spring hateoas rest maturity","sourceFile":"README.md","difficulty":"intermediate","question":"How does @HttpExchange create a declarative HTTP client, and what backs it?","answerFull":"You declare a Java interface with methods annotated @GetExchange/@PostExchange, and HttpServiceProxyFactory generates an implementation at runtime. The factory is given an adapter — RestClientAdapter (Spring 6.1 / Boot 3.2), WebClientAdapter, or RestTemplateAdapter — which determines the underlying transport. This gives you a typed, testable client contract, analogous to Spring Data repositories or OpenFeign, without hand-writing HTTP calls.","correct":"You declare a Java interface with methods annotated @GetExchange/@PostExchange, and HttpServiceProxyFactory generates an implementation at runtime.","distractors":["It is an interface with a toModel(entity) method that centralizes conversion of a domain object into a linked EntityModel, keeping link logic out of controllers.","Use RestClient (GA in Boot 3.2) for synchronous, blocking HTTP in a servlet application — it is the modern fluent replacement for RestTemplate, which is now in maintenance mode.","HATEOAS adds hypermedia links to responses so the client discovers available actions instead of hardcoding URLs."],"distractorIds":["spring/spring_hateoas_rest_maturity#a64fe49d30da","spring/spring_hateoas_rest_maturity#912856ffc893","spring/spring_hateoas_rest_maturity#67151300340a"],"concepts":["getexchange","httpserviceproxyfactory","postexchange","declarative","declare","annotated"],"questionMd":"How does `@HttpExchange` create a declarative HTTP client, and what backs it?","correctMd":"You declare a Java interface with methods annotated `@GetExchange`/`@PostExchange`, and `HttpServiceProxyFactory` generates an implementation at runtime.","answerFullMd":"You declare a Java interface with methods annotated `@GetExchange`/`@PostExchange`, and `HttpServiceProxyFactory` generates an implementation at runtime. The factory is given an adapter — `RestClientAdapter` (Spring 6.1 / Boot 3.2), `WebClientAdapter`, or `RestTemplateAdapter` — which determines the underlying transport. This gives you a typed, testable client contract, analogous to Spring Data repositories or OpenFeign, without hand-writing HTTP calls.","distractorsMd":["It is an interface with a `toModel(entity)` method that centralizes conversion of a domain object into a linked `EntityModel`, keeping link logic out of controllers.","Use `RestClient` (GA in Boot 3.2) for synchronous, blocking HTTP in a servlet application — it is the modern fluent replacement for `RestTemplate`, which is now in maintenance mode.",null]},{"id":"spring/spring_hateoas_rest_maturity#cd5292c9acc8","section":"spring","module":"spring/spring_hateoas_rest_maturity","moduleName":"spring hateoas rest maturity","sourceFile":"README.md","difficulty":"advanced","question":"What are the three main API versioning strategies and their tradeoffs?","answerFull":"URI versioning (/api/v2/orders) is the most visible and cache-friendly but puts a version in the resource identity, which purists dislike. Header versioning (X-API-Version: 2) keeps URLs clean but is invisible in logs and needs Vary for caching. Media-type versioning (Accept: application/vnd.acme.v2+json) is the most RESTful because the representation, not the resource, is versioned, but it is the most complex to tool. Most teams pick URI versioning for pragmatism.","correct":"URI versioning (/api/v2/orders) is the most visible and cache-friendly but puts a version in the resource identity, which purists dislike.","distractors":["It means they use resource URIs plus correct HTTP verbs and status codes but do not return hypermedia links, so clients still template URLs.","Traverson is Spring HATEOAS's Java client for navigating a HAL API by following rel names rather than building URLs, e.g. traverson.follow(\"orders\", \"pay\").","Because a concatenated string bakes in the host, scheme, and context path, so it breaks behind a load balancer, under a different context path, or when versioning changes the route."],"distractorIds":["spring/spring_hateoas_rest_maturity#5cf907ff028e","spring/spring_hateoas_rest_maturity#eb998bd4820c","spring/spring_hateoas_rest_maturity#2a8ad7f05f9b"],"concepts":["dislike","purists","friendly","tradeoffs","puts","uri"],"correctMd":"URI versioning (`/api/v2/orders`) is the most visible and cache-friendly but puts a version in the resource identity, which purists dislike.","answerFullMd":"URI versioning (`/api/v2/orders`) is the most visible and cache-friendly but puts a version in the resource identity, which purists dislike. Header versioning (`X-API-Version: 2`) keeps URLs clean but is invisible in logs and needs `Vary` for caching. Media-type versioning (`Accept: application/vnd.acme.v2+json`) is the most RESTful because the representation, not the resource, is versioned, but it is the most complex to tool. Most teams pick URI versioning for pragmatism.","distractorsMd":[null,"Traverson is Spring HATEOAS's Java client for navigating a HAL API by following `rel` names rather than building URLs, e.g. `traverson.follow(\"orders\", \"pay\")`.",null]},{"id":"spring/spring_hateoas_rest_maturity#1000e511020d","section":"spring","module":"spring/spring_hateoas_rest_maturity","moduleName":"spring hateoas rest maturity","sourceFile":"README.md","difficulty":"advanced","question":"How does the presence or absence of a link encode authorization or state?","answerFull":"In a hypermedia API the assembler adds a cancel or pay link only when the resource's current state (and the caller's permissions) actually allow that action, so the link set *is* the state machine. A client reads what it may do off _links instead of hardcoding business rules, and an already-shipped order simply stops offering cancel. This keeps transition logic on the server and prevents clients from attempting illegal actions.","correct":"In a hypermedia API the assembler adds a cancel or pay link only when the resource's current state (and the caller's permissions) actually allow that action, so the link set *is* the state machine.","distractors":["Once clients navigate by relation name, a rel like pay is part of the published contract exactly as a URL is, so renaming it to payment breaks every client that looked up _links.pay.","Spring Data REST is built on Spring HATEOAS and auto-exposes JPA/Mongo repositories as a HAL API, generating _links, _embedded, pagination links, and an ALPS/HAL-FORMS profile with no hand-written assemblers.","Traverson is Spring HATEOAS's Java client for navigating a HAL API by following rel names rather than building URLs, e.g. traverson.follow(\"orders\", \"pay\")."],"distractorIds":["spring/spring_hateoas_rest_maturity#a0033506a076","spring/spring_hateoas_rest_maturity#2a1e91158ed2","spring/spring_hateoas_rest_maturity#eb998bd4820c"],"concepts":["assembler","absence","presence","hypermedia","encode","cancel"],"correctMd":"In a hypermedia API the assembler adds a `cancel` or `pay` link only when the resource's current state (and the caller's permissions) actually allow that action, so the link set *is* the state machine.","answerFullMd":"In a hypermedia API the assembler adds a `cancel` or `pay` link only when the resource's current state (and the caller's permissions) actually allow that action, so the link set *is* the state machine. A client reads what it may do off `_links` instead of hardcoding business rules, and an already-shipped order simply stops offering `cancel`. This keeps transition logic on the server and prevents clients from attempting illegal actions.","distractorsMd":["Once clients navigate by relation name, a `rel` like `pay` is part of the published contract exactly as a URL is, so renaming it to `payment` breaks every client that looked up `_links.pay`.","Spring Data REST is built on Spring HATEOAS and auto-exposes JPA/Mongo repositories as a HAL API, generating `_links`, `_embedded`, pagination links, and an ALPS/HAL-FORMS profile with no hand-written assemblers.","Traverson is Spring HATEOAS's Java client for navigating a HAL API by following `rel` names rather than building URLs, e.g. `traverson.follow(\"orders\", \"pay\")`."]},{"id":"spring/spring_hateoas_rest_maturity#a0033506a076","section":"spring","module":"spring/spring_hateoas_rest_maturity","moduleName":"spring hateoas rest maturity","sourceFile":"README.md","difficulty":"advanced","question":"Why is renaming a link rel a breaking change?","answerFull":"Once clients navigate by relation name, a rel like pay is part of the published contract exactly as a URL is, so renaming it to payment breaks every client that looked up _links.pay. Hypermedia moves the coupling from URLs to rel names — it does not eliminate coupling. Treat rel names with the same versioning discipline you apply to endpoints.","correct":"Once clients navigate by relation name, a rel like pay is part of the published contract exactly as a URL is, so renaming it to payment breaks every client that looked up _links.pay.","distractors":["It is an interface with a toModel(entity) method that centralizes conversion of a domain object into a linked EntityModel, keeping link logic out of controllers.","In a hypermedia API the assembler adds a cancel or pay link only when the resource's current state (and the caller's permissions) actually allow that action, so the link set *is* the state machine.","Spring Data REST is built on Spring HATEOAS and auto-exposes JPA/Mongo repositories as a HAL API, generating _links, _embedded, pagination links, and an ALPS/HAL-FORMS profile with no hand-written assemblers."],"distractorIds":["spring/spring_hateoas_rest_maturity#a64fe49d30da","spring/spring_hateoas_rest_maturity#1000e511020d","spring/spring_hateoas_rest_maturity#2a1e91158ed2"],"concepts":["looked","rel","renaming","navigate","relation","breaking"],"questionMd":"Why is renaming a link `rel` a breaking change?","correctMd":"Once clients navigate by relation name, a `rel` like `pay` is part of the published contract exactly as a URL is, so renaming it to `payment` breaks every client that looked up `_links.pay`.","answerFullMd":"Once clients navigate by relation name, a `rel` like `pay` is part of the published contract exactly as a URL is, so renaming it to `payment` breaks every client that looked up `_links.pay`. Hypermedia moves the coupling from URLs to `rel` names — it does not eliminate coupling. Treat `rel` names with the same versioning discipline you apply to endpoints.","distractorsMd":["It is an interface with a `toModel(entity)` method that centralizes conversion of a domain object into a linked `EntityModel`, keeping link logic out of controllers.","In a hypermedia API the assembler adds a `cancel` or `pay` link only when the resource's current state (and the caller's permissions) actually allow that action, so the link set *is* the state machine.","Spring Data REST is built on Spring HATEOAS and auto-exposes JPA/Mongo repositories as a HAL API, generating `_links`, `_embedded`, pagination links, and an ALPS/HAL-FORMS profile with no hand-written assemblers."]},{"id":"spring/spring_hateoas_rest_maturity#7d67d09627e0","section":"spring","module":"spring/spring_hateoas_rest_maturity","moduleName":"spring hateoas rest maturity","sourceFile":"README.md","difficulty":"advanced","question":"Can you have a Level 3 server with a Level 2 client, and what happens?","answerFull":"Yes, and it is a common anti-pattern: the server emits _links but the client ignores them and templates URLs itself. You then pay the full cost of HATEOAS — larger payloads, assembler complexity, link maintenance — while getting none of the decoupling benefit, because the client is still hardwired to your URL layout. Decide whether the client will genuinely follow links *before* committing to Level 3.","correct":"Yes, and it is a common anti-pattern: the server emits _links but the client ignores them and templates URLs itself.","distractors":["EntityModel<T> wraps a single resource plus its links, CollectionModel<T> wraps a list of models, and PagedModel<T> adds page metadata plus navigation links.","It means they use resource URIs plus correct HTTP verbs and status codes but do not return hypermedia links, so clients still template URLs.","Traverson is Spring HATEOAS's Java client for navigating a HAL API by following rel names rather than building URLs, e.g. traverson.follow(\"orders\", \"pay\")."],"distractorIds":["spring/spring_hateoas_rest_maturity#abd0caedb9ce","spring/spring_hateoas_rest_maturity#5cf907ff028e","spring/spring_hateoas_rest_maturity#eb998bd4820c"],"concepts":["anti","ignores","urls","links","templates","emits"],"correctMd":"Yes, and it is a common anti-pattern: the server emits `_links` but the client ignores them and templates URLs itself.","answerFullMd":"Yes, and it is a common anti-pattern: the server emits `_links` but the client ignores them and templates URLs itself. You then pay the full cost of HATEOAS — larger payloads, assembler complexity, link maintenance — while getting none of the decoupling benefit, because the client is still hardwired to your URL layout. Decide whether the client will genuinely follow links *before* committing to Level 3.","distractorsMd":["`EntityModel<T>` wraps a single resource plus its links, `CollectionModel<T>` wraps a list of models, and `PagedModel<T>` adds page metadata plus navigation links.",null,"Traverson is Spring HATEOAS's Java client for navigating a HAL API by following `rel` names rather than building URLs, e.g. `traverson.follow(\"orders\", \"pay\")`."]},{"id":"spring/spring_hateoas_rest_maturity#2a1e91158ed2","section":"spring","module":"spring/spring_hateoas_rest_maturity","moduleName":"spring hateoas rest maturity","sourceFile":"README.md","difficulty":"advanced","question":"How does Spring Data REST relate to Spring HATEOAS?","answerFull":"Spring Data REST is built on Spring HATEOAS and auto-exposes JPA/Mongo repositories as a HAL API, generating _links, _embedded, pagination links, and an ALPS/HAL-FORMS profile with no hand-written assemblers. It is the fastest way to ship a Level 3 API, at the cost of tightly coupling your HTTP surface to your persistence model. For a curated public API you usually still hand-write assemblers to control the exposed shape.","correct":"Spring Data REST is built on Spring HATEOAS and auto-exposes JPA/Mongo repositories as a HAL API, generating _links, _embedded, pagination links, and an ALPS/HAL-FORMS profile with no hand-written assemblers.","distractors":["HAL (application/hal+json) describes *where* you can go via _links and embeds related resources in _embedded.","HATEOAS adds hypermedia links to responses so the client discovers available actions instead of hardcoding URLs.","Traverson is Spring HATEOAS's Java client for navigating a HAL API by following rel names rather than building URLs, e.g. traverson.follow(\"orders\", \"pay\")."],"distractorIds":["spring/spring_hateoas_rest_maturity#eb9c4e2040ff","spring/spring_hateoas_rest_maturity#67151300340a","spring/spring_hateoas_rest_maturity#eb998bd4820c"],"concepts":["alps","assemblers","mongo","hal","repositories","forms"],"correctMd":"Spring Data REST is built on Spring HATEOAS and auto-exposes JPA/Mongo repositories as a HAL API, generating `_links`, `_embedded`, pagination links, and an ALPS/HAL-FORMS profile with no hand-written assemblers.","answerFullMd":"Spring Data REST is built on Spring HATEOAS and auto-exposes JPA/Mongo repositories as a HAL API, generating `_links`, `_embedded`, pagination links, and an ALPS/HAL-FORMS profile with no hand-written assemblers. It is the fastest way to ship a Level 3 API, at the cost of tightly coupling your HTTP surface to your persistence model. For a curated public API you usually still hand-write assemblers to control the exposed shape.","distractorsMd":["HAL (`application/hal+json`) describes *where* you can go via `_links` and embeds related resources in `_embedded`.",null,"Traverson is Spring HATEOAS's Java client for navigating a HAL API by following `rel` names rather than building URLs, e.g. `traverson.follow(\"orders\", \"pay\")`."]},{"id":"spring/spring_hateoas_rest_maturity#eb998bd4820c","section":"spring","module":"spring/spring_hateoas_rest_maturity","moduleName":"spring hateoas rest maturity","sourceFile":"README.md","difficulty":"advanced","question":"What is Traverson and when would you use it?","answerFull":"Traverson is Spring HATEOAS's Java client for navigating a HAL API by following rel names rather than building URLs, e.g. traverson.follow(\"orders\", \"pay\"). You use it on the consumer side to write a genuinely hypermedia-driven client that only knows the API root and traverses links from there. It is the client-side counterpart that makes a Level 3 server actually pay off.","correct":"Traverson is Spring HATEOAS's Java client for navigating a HAL API by following rel names rather than building URLs, e.g. traverson.follow(\"orders\", \"pay\").","distractors":["It means they use resource URIs plus correct HTTP verbs and status codes but do not return hypermedia links, so clients still template URLs.","Spring Data REST is built on Spring HATEOAS and auto-exposes JPA/Mongo repositories as a HAL API, generating _links, _embedded, pagination links, and an ALPS/HAL-FORMS profile with no hand-written assemblers.","Once clients navigate by relation name, a rel like pay is part of the published contract exactly as a URL is, so renaming it to payment breaks every client that looked up _links.pay."],"distractorIds":["spring/spring_hateoas_rest_maturity#5cf907ff028e","spring/spring_hateoas_rest_maturity#2a1e91158ed2","spring/spring_hateoas_rest_maturity#a0033506a076"],"concepts":["navigating","traverson","rel","hal","hateoas","follow"],"correctMd":"Traverson is Spring HATEOAS's Java client for navigating a HAL API by following `rel` names rather than building URLs, e.g. `traverson.follow(\"orders\", \"pay\")`.","answerFullMd":"Traverson is Spring HATEOAS's Java client for navigating a HAL API by following `rel` names rather than building URLs, e.g. `traverson.follow(\"orders\", \"pay\")`. You use it on the consumer side to write a genuinely hypermedia-driven client that only knows the API root and traverses links from there. It is the client-side counterpart that makes a Level 3 server actually pay off.","distractorsMd":[null,"Spring Data REST is built on Spring HATEOAS and auto-exposes JPA/Mongo repositories as a HAL API, generating `_links`, `_embedded`, pagination links, and an ALPS/HAL-FORMS profile with no hand-written assemblers.","Once clients navigate by relation name, a `rel` like `pay` is part of the published contract exactly as a URL is, so renaming it to `payment` breaks every client that looked up `_links.pay`."]},{"id":"spring/spring_hateoas_rest_maturity#65a3067f5458","section":"spring","module":"spring/spring_hateoas_rest_maturity","moduleName":"spring hateoas rest maturity","sourceFile":"README.md","difficulty":"advanced","question":"Does adding HATEOAS change your status codes or verbs?","answerFull":"No — HATEOAS sits on top of Level 2, so you still return 201 on create, 200 on read, 404 on missing, 409 on conflict, and use GET/POST/PUT/PATCH/DELETE correctly. HATEOAS only adds the _links (and optionally HAL-FORMS _templates) to the body; it does not replace correct HTTP semantics. A broken Level 2 API does not become correct by bolting links onto it.","correct":"No — HATEOAS sits on top of Level 2, so you still return 201 on create, 200 on read, 404 on missing, 409 on conflict, and use GET/POST/PUT/PATCH/DELETE correctly.","distractors":["ProblemDetail (Spring 6 / Boot 3) is Spring's implementation of RFC 7807, a standardized JSON error body with type, title, status, detail, and instance, served as application/problem+json.","It means they use resource URIs plus correct HTTP verbs and status codes but do not return hypermedia links, so clients still template URLs.","HATEOAS adds hypermedia links to responses so the client discovers available actions instead of hardcoding URLs."],"distractorIds":["spring/spring_hateoas_rest_maturity#efc85e62779f","spring/spring_hateoas_rest_maturity#5cf907ff028e","spring/spring_hateoas_rest_maturity#67151300340a"],"concepts":["verbs","409","201","404","codes","hateoas"],"answerFullMd":"No — HATEOAS sits on top of Level 2, so you still return 201 on create, 200 on read, 404 on missing, 409 on conflict, and use GET/POST/PUT/PATCH/DELETE correctly. HATEOAS only adds the `_links` (and optionally HAL-FORMS `_templates`) to the body; it does not replace correct HTTP semantics. A broken Level 2 API does not become correct by bolting links onto it.","distractorsMd":["`ProblemDetail` (Spring 6 / Boot 3) is Spring's implementation of RFC 7807, a standardized JSON error body with `type`, `title`, `status`, `detail`, and `instance`, served as `application/problem+json`.",null,null]},{"id":"spring/spring_integration#e48a23899f70","section":"spring","module":"spring/spring_integration","moduleName":"spring integration","sourceFile":"README.md","difficulty":"core","question":"What are the core abstractions in Spring Integration?","answerFull":"Messages (payload + headers), channels (the pipes connecting components), and endpoints (the components: filters, transformers, routers, splitters, aggregators, service activators, adapters). Endpoints never call each other directly — they only read from and write to channels, which is the pipes-and-filters decoupling. Adapters and gateways bridge this messaging model to external protocols.","correct":"Messages (payload + headers), channels (the pipes connecting components), and endpoints (the components: filters, transformers, routers, splitters, aggregators, service activators, adapters).","distractors":["Headers carry out-of-band metadata — message id, timestamp, correlation id, sequence number, reply channel, and custom keys — that drive routing, aggregation, and request-reply correlation.","A splitter turns one message into many (e.g. an order into its line items); an aggregator collects many correlated messages back into one.","Spring Cloud Stream is built on top of Spring Integration's channel/adapter model; its binder abstraction sits on SI's messaging core, which is why they share concepts like channels and message handlers."],"distractorIds":["spring/spring_integration#ba9164e99dc7","spring/spring_integration#6f828296f1dc","spring/spring_integration#f69f31d54212"],"concepts":["activators","pipes","splitters","aggregators","connecting","routers"]},{"id":"spring/spring_integration#f219c76ed2ab","section":"spring","module":"spring/spring_integration","moduleName":"spring integration","sourceFile":"README.md","difficulty":"core","question":"Explain the main channel types and how they affect threading.","answerFull":"DirectChannel (the default) invokes the next endpoint synchronously on the sender's thread, so the whole flow runs inline and can share one transaction. QueueChannel buffers messages and requires a downstream poller, decoupling producer and consumer rates asynchronously. ExecutorChannel hands off to a thread pool for parallelism. PublishSubscribeChannel fans a message out to all subscribers. Choosing the channel sets the concurrency, transaction boundary, and back-pressure behavior of that seam.","correct":"DirectChannel (the default) invokes the next endpoint synchronously on the sender's thread, so the whole flow runs inline and can share one transaction.","distractors":["A channel adapter is one-directional — an inbound adapter brings external data into a flow, an outbound adapter sends flow messages to an external system — with no reply semantics.","Failures are wrapped in a MessagingException that carries the failed Message and routed to an error channel (the default errorChannel, or a flow-specific one).","Because endpoints communicate only through channels, you can change threading, buffering, persistence, and transport by swapping the channel without modifying any business logic."],"distractorIds":["spring/spring_integration#e0544daf527e","spring/spring_integration#87075c8ef0b4","spring/spring_integration#bf3d182a1c75"],"concepts":["directchannel","explain","invokes","sender","threading","affect"],"correctMd":"`DirectChannel` (the default) invokes the next endpoint synchronously on the sender's thread, so the whole flow runs inline and can share one transaction.","answerFullMd":"`DirectChannel` (the default) invokes the next endpoint synchronously on the sender's thread, so the whole flow runs inline and can share one transaction. `QueueChannel` buffers messages and requires a downstream poller, decoupling producer and consumer rates asynchronously. `ExecutorChannel` hands off to a thread pool for parallelism. `PublishSubscribeChannel` fans a message out to all subscribers. Choosing the channel sets the concurrency, transaction boundary, and back-pressure behavior of that seam.","distractorsMd":[null,"Failures are wrapped in a `MessagingException` that carries the failed `Message` and routed to an error channel (the default `errorChannel`, or a flow-specific one).",null]},{"id":"spring/spring_integration#bf3d182a1c75","section":"spring","module":"spring/spring_integration","moduleName":"spring integration","sourceFile":"README.md","difficulty":"core","question":"Why is the channel being a first-class, swappable component important?","answerFull":"Because endpoints communicate only through channels, you can change threading, buffering, persistence, and transport by swapping the channel without modifying any business logic. For example, replacing a DirectChannel with a QueueChannel turns a synchronous flow asynchronous, and the endpoints on either side are unchanged. That decoupling is the central design benefit of the framework.","correct":"Because endpoints communicate only through channels, you can change threading, buffering, persistence, and transport by swapping the channel without modifying any business logic.","distractors":["A channel adapter is one-directional — an inbound adapter brings external data into a flow, an outbound adapter sends flow messages to an external system — with no reply semantics.","Headers carry out-of-band metadata — message id, timestamp, correlation id, sequence number, reply channel, and custom keys — that drive routing, aggregation, and request-reply correlation.","Failures are wrapped in a MessagingException that carries the failed Message and routed to an error channel (the default errorChannel, or a flow-specific one)."],"distractorIds":["spring/spring_integration#e0544daf527e","spring/spring_integration#ba9164e99dc7","spring/spring_integration#87075c8ef0b4"],"concepts":["swapping","buffering","communicate","important","threading","modifying"],"answerFullMd":"Because endpoints communicate only through channels, you can change threading, buffering, persistence, and transport by swapping the channel without modifying any business logic. For example, replacing a `DirectChannel` with a `QueueChannel` turns a synchronous flow asynchronous, and the endpoints on either side are unchanged. That decoupling is the central design benefit of the framework.","distractorsMd":[null,null,"Failures are wrapped in a `MessagingException` that carries the failed `Message` and routed to an error channel (the default `errorChannel`, or a flow-specific one)."]},{"id":"spring/spring_integration#6f828296f1dc","section":"spring","module":"spring/spring_integration","moduleName":"spring integration","sourceFile":"README.md","difficulty":"core","question":"What does a splitter and an aggregator do, and what makes the aggregator tricky?","answerFull":"A splitter turns one message into many (e.g. an order into its line items); an aggregator collects many correlated messages back into one. The aggregator is tricky because it must correlate messages into groups (correlation strategy), decide when a group is complete (release strategy), buffer partial groups somewhere, and handle parts that never arrive (group timeout). Getting correlation, release, and timeout right — and persisting the buffer so groups survive restarts — is exactly the logic SI provides so you do not hand-roll it.","correct":"A splitter turns one message into many (e.g. an order into its line items); an aggregator collects many correlated messages back into one.","distractors":["A channel adapter is one-directional — an inbound adapter brings external data into a flow, an outbound adapter sends flow messages to an external system — with no reply semantics.","Failures are wrapped in a MessagingException that carries the failed Message and routed to an error channel (the default errorChannel, or a flow-specific one).","Headers carry out-of-band metadata — message id, timestamp, correlation id, sequence number, reply channel, and custom keys — that drive routing, aggregation, and request-reply correlation."],"distractorIds":["spring/spring_integration#e0544daf527e","spring/spring_integration#87075c8ef0b4","spring/spring_integration#ba9164e99dc7"],"concepts":["aggregator","splitter","correlated","collects","turns","line"],"distractorsMd":[null,"Failures are wrapped in a `MessagingException` that carries the failed `Message` and routed to an error channel (the default `errorChannel`, or a flow-specific one).",null]},{"id":"spring/spring_integration#e0544daf527e","section":"spring","module":"spring/spring_integration","moduleName":"spring integration","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between a channel adapter and a gateway?","answerFull":"A channel adapter is one-directional — an inbound adapter brings external data into a flow, an outbound adapter sends flow messages to an external system — with no reply semantics. A gateway is request-reply: it sends a message and correlates a response back, used when the caller needs an answer (inbound gateway: external request → flow → response; messaging gateway: Java call → flow → return value).","correct":"A channel adapter is one-directional — an inbound adapter brings external data into a flow, an outbound adapter sends flow messages to an external system — with no reply semantics.","distractors":["DirectChannel (the default) invokes the next endpoint synchronously on the sender's thread, so the whole flow runs inline and can share one transaction.","Headers carry out-of-band metadata — message id, timestamp, correlation id, sequence number, reply channel, and custom keys — that drive routing, aggregation, and request-reply correlation.","A poller drives pollable sources — QueueChannels, file/JDBC inbound adapters — by periodically pulling messages at a configured rate, batch size, and (optionally) within a transaction."],"distractorIds":["spring/spring_integration#f219c76ed2ab","spring/spring_integration#ba9164e99dc7","spring/spring_integration#65164ea5ea04"],"concepts":["brings","directional","reply","outbound","difference","inbound"],"distractorsMd":["`DirectChannel` (the default) invokes the next endpoint synchronously on the sender's thread, so the whole flow runs inline and can share one transaction.",null,"A poller drives pollable sources — `QueueChannel`s, file/JDBC inbound adapters — by periodically pulling messages at a configured rate, batch size, and (optionally) within a transaction."]},{"id":"spring/spring_integration#65164ea5ea04","section":"spring","module":"spring/spring_integration","moduleName":"spring integration","sourceFile":"README.md","difficulty":"intermediate","question":"What is a poller and when do you need one?","answerFull":"A poller drives pollable sources — QueueChannels, file/JDBC inbound adapters — by periodically pulling messages at a configured rate, batch size, and (optionally) within a transaction. You need one on any pollable endpoint; without it, the source silently never consumes. The poller is also your back-pressure and scheduling knob: it controls how fast and in what batch size work enters the flow.","correct":"A poller drives pollable sources — QueueChannels, file/JDBC inbound adapters — by periodically pulling messages at a configured rate, batch size, and (optionally) within a transaction.","distractors":["A splitter turns one message into many (e.g. an order into its line items); an aggregator collects many correlated messages back into one.","DirectChannel (the default) invokes the next endpoint synchronously on the sender's thread, so the whole flow runs inline and can share one transaction.","A channel adapter is one-directional — an inbound adapter brings external data into a flow, an outbound adapter sends flow messages to an external system — with no reply semantics."],"distractorIds":["spring/spring_integration#6f828296f1dc","spring/spring_integration#f219c76ed2ab","spring/spring_integration#e0544daf527e"],"concepts":["pollable","poller","queuechannels","pulling","periodically","optionally"],"correctMd":"A poller drives pollable sources — `QueueChannel`s, file/JDBC inbound adapters — by periodically pulling messages at a configured rate, batch size, and (optionally) within a transaction.","answerFullMd":"A poller drives pollable sources — `QueueChannel`s, file/JDBC inbound adapters — by periodically pulling messages at a configured rate, batch size, and (optionally) within a transaction. You need one on any pollable endpoint; without it, the source silently never consumes. The poller is also your back-pressure and scheduling knob: it controls how fast and in what batch size work enters the flow.","distractorsMd":[null,"`DirectChannel` (the default) invokes the next endpoint synchronously on the sender's thread, so the whole flow runs inline and can share one transaction.",null]},{"id":"spring/spring_integration#87075c8ef0b4","section":"spring","module":"spring/spring_integration","moduleName":"spring integration","sourceFile":"README.md","difficulty":"intermediate","question":"How does error handling work in a Spring Integration flow?","answerFull":"Failures are wrapped in a MessagingException that carries the failed Message and routed to an error channel (the default errorChannel, or a flow-specific one). You attach a flow to that channel to log, alert, or dead-letter the failure. For transactional polled sources you can add retry/advice on the poller so transient failures roll back and are re-polled. The failed message being preserved is what lets you recover or DLQ it.","correct":"Failures are wrapped in a MessagingException that carries the failed Message and routed to an error channel (the default errorChannel, or a flow-specific one).","distractors":["Headers carry out-of-band metadata — message id, timestamp, correlation id, sequence number, reply channel, and custom keys — that drive routing, aggregation, and request-reply correlation.","A channel adapter is one-directional — an inbound adapter brings external data into a flow, an outbound adapter sends flow messages to an external system — with no reply semantics.","DirectChannel (the default) invokes the next endpoint synchronously on the sender's thread, so the whole flow runs inline and can share one transaction."],"distractorIds":["spring/spring_integration#ba9164e99dc7","spring/spring_integration#e0544daf527e","spring/spring_integration#f219c76ed2ab"],"concepts":["errorchannel","messagingexception","routed","wrapped","integration","handling"],"correctMd":"Failures are wrapped in a `MessagingException` that carries the failed `Message` and routed to an error channel (the default `errorChannel`, or a flow-specific one).","answerFullMd":"Failures are wrapped in a `MessagingException` that carries the failed `Message` and routed to an error channel (the default `errorChannel`, or a flow-specific one). You attach a flow to that channel to log, alert, or dead-letter the failure. For transactional polled sources you can add retry/advice on the poller so transient failures roll back and are re-polled. The failed message being preserved is what lets you recover or DLQ it.","distractorsMd":[null,null,"`DirectChannel` (the default) invokes the next endpoint synchronously on the sender's thread, so the whole flow runs inline and can share one transaction."]},{"id":"spring/spring_integration#f69f31d54212","section":"spring","module":"spring/spring_integration","moduleName":"spring integration","sourceFile":"README.md","difficulty":"advanced","question":"What is the relationship between Spring Integration and Spring Cloud Stream?","answerFull":"Spring Cloud Stream is built on top of Spring Integration's channel/adapter model; its binder abstraction sits on SI's messaging core, which is why they share concepts like channels and message handlers. Spring Cloud Stream specializes SI for broker-backed streaming (Kafka/Rabbit) with binders and partitioning, whereas SI is the general EIP engine underneath. Knowing SI helps you understand what Spring Cloud Stream does under the hood.","correct":"Spring Cloud Stream is built on top of Spring Integration's channel/adapter model; its binder abstraction sits on SI's messaging core, which is why they share concepts like channels and message handlers.","distractors":["Headers carry out-of-band metadata — message id, timestamp, correlation id, sequence number, reply channel, and custom keys — that drive routing, aggregation, and request-reply correlation.","DirectChannel (the default) invokes the next endpoint synchronously on the sender's thread, so the whole flow runs inline and can share one transaction.","Messages (payload + headers), channels (the pipes connecting components), and endpoints (the components: filters, transformers, routers, splitters, aggregators, service activators, adapters)."],"distractorIds":["spring/spring_integration#ba9164e99dc7","spring/spring_integration#f219c76ed2ab","spring/spring_integration#e48a23899f70"],"concepts":["binder","concepts","messaging","integration","channels","handlers"],"distractorsMd":[null,"`DirectChannel` (the default) invokes the next endpoint synchronously on the sender's thread, so the whole flow runs inline and can share one transaction.",null]},{"id":"spring/spring_integration#8ff733b94a12","section":"spring","module":"spring/spring_integration","moduleName":"spring integration","sourceFile":"README.md","difficulty":"advanced","question":"How does Spring Integration relate to Apache Camel?","answerFull":"Both are EIP-based integration engines implementing the same pattern catalogue (routers, transformers, aggregators, adapters). Spring Integration is a Spring-native library expressed in beans and a Java DSL, ideal when you are already in a Spring app. Apache Camel is a broader, standalone integration runtime with its own DSLs and an enormous component set, often chosen when integration is the primary concern or you want a framework-agnostic engine. They overlap heavily; the choice is mostly ecosystem fit.","correct":"Both are EIP-based integration engines implementing the same pattern catalogue (routers, transformers, aggregators, adapters).","distractors":["Messages (payload + headers), channels (the pipes connecting components), and endpoints (the components: filters, transformers, routers, splitters, aggregators, service activators, adapters).","Spring Cloud Stream is built on top of Spring Integration's channel/adapter model; its binder abstraction sits on SI's messaging core, which is why they share concepts like channels and message handlers.","A poller drives pollable sources — QueueChannels, file/JDBC inbound adapters — by periodically pulling messages at a configured rate, batch size, and (optionally) within a transaction."],"distractorIds":["spring/spring_integration#e48a23899f70","spring/spring_integration#f69f31d54212","spring/spring_integration#65164ea5ea04"],"concepts":["catalogue","eip","aggregators","routers","transformers","implementing"],"distractorsMd":[null,null,"A poller drives pollable sources — `QueueChannel`s, file/JDBC inbound adapters — by periodically pulling messages at a configured rate, batch size, and (optionally) within a transaction."]},{"id":"spring/spring_integration#ba9164e99dc7","section":"spring","module":"spring/spring_integration","moduleName":"spring integration","sourceFile":"README.md","difficulty":"advanced","question":"What are message headers used for, and what is the risk when transforming messages?","answerFull":"Headers carry out-of-band metadata — message id, timestamp, correlation id, sequence number, reply channel, and custom keys — that drive routing, aggregation, and request-reply correlation. The risk is that a custom transformer which constructs a brand-new Message without copying headers silently drops correlation/reply headers, breaking aggregation or gateways. The fix is to use MessageBuilder.copyHeaders(...) or simply return the payload and let SI preserve the existing headers.","correct":"Headers carry out-of-band metadata — message id, timestamp, correlation id, sequence number, reply channel, and custom keys — that drive routing, aggregation, and request-reply correlation.","distractors":["Because endpoints communicate only through channels, you can change threading, buffering, persistence, and transport by swapping the channel without modifying any business logic.","Spring Cloud Stream is built on top of Spring Integration's channel/adapter model; its binder abstraction sits on SI's messaging core, which is why they share concepts like channels and message handlers.","A poller drives pollable sources — QueueChannels, file/JDBC inbound adapters — by periodically pulling messages at a configured rate, batch size, and (optionally) within a transaction."],"distractorIds":["spring/spring_integration#bf3d182a1c75","spring/spring_integration#f69f31d54212","spring/spring_integration#65164ea5ea04"],"concepts":["band","drive","reply","aggregation","carry","risk"],"answerFullMd":"Headers carry out-of-band metadata — message id, timestamp, correlation id, sequence number, reply channel, and custom keys — that drive routing, aggregation, and request-reply correlation. The risk is that a custom transformer which constructs a brand-new `Message` without copying headers silently drops correlation/reply headers, breaking aggregation or gateways. The fix is to use `MessageBuilder.copyHeaders(...)` or simply return the payload and let SI preserve the existing headers.","distractorsMd":[null,null,"A poller drives pollable sources — `QueueChannel`s, file/JDBC inbound adapters — by periodically pulling messages at a configured rate, batch size, and (optionally) within a transaction."]},{"id":"spring/spring_messaging#01311ebf8984","section":"spring","module":"spring/spring_messaging","moduleName":"spring messaging","sourceFile":"README.md","difficulty":"core","question":"What is the difference between at-least-once and exactly-once delivery in Kafka, and how does Spring Kafka achieve exactly-once?","answerFull":"At-least-once means the broker may deliver a message more than once — a duplicate will occur if the producer retries or the consumer crashes after processing but before committing the offset. Exactly-once in Kafka is achieved by combining an idempotent producer (enable.idempotence=true, which assigns sequence numbers to deduplicate retries at the broker), a transactional producer (transactional.id configured, which wraps sends in atomic transactions), and consumers with isolation.level=read_committed (which skip uncommitted messages). Spring Kafka configures all three via KafkaTransactionManager and appropriate ProducerFactory settings. The overhead is roughly 20-30% lower throughput compared to at-least-once. Use exactly-once only for financial or inventory updates where duplicates cause real-world harm.","correct":"At-least-once means the broker may deliver a message more than once — a duplicate will occur if the producer retries or the consumer crashes after processing but before committing the offset.","distractors":["The Outbox pattern solves the dual-write problem: when you need to write to a database and publish a Kafka event atomically.","The most common cause is the consumer taking longer than max.poll.interval.ms to process a batch.","Use Testcontainers with KafkaContainer to start a real Kafka broker in Docker."],"distractorIds":["spring/spring_messaging#ac63682ef801","spring/spring_messaging#c0dd18e5c809","spring/spring_messaging#c6187db40072"],"concepts":["deliver","crashes","duplicate","committing","difference","delivery"]},{"id":"spring/spring_messaging#c0dd18e5c809","section":"spring","module":"spring/spring_messaging","moduleName":"spring messaging","sourceFile":"README.md","difficulty":"core","question":"A Kafka consumer in production is causing frequent rebalances. What are the likely causes and how do you fix them?","answerFull":"The most common cause is the consumer taking longer than max.poll.interval.ms to process a batch. The broker interprets this as a dead consumer and triggers a rebalance, causing all in-progress messages to be redelivered — producing duplicates. The fix is to reduce max.poll.records so that total processing time per poll stays well below max.poll.interval.ms. A safe formula is: max.poll.records = max.poll.interval.ms / avg_processing_time_ms, with a 50% safety margin. Other causes include session.timeout.ms being too low for the network, GC pauses causing the poll loop to stall, or a consumer crashing. Monitoring consumer lag and rebalance events via Micrometer is essential for early detection.","correct":"The most common cause is the consumer taking longer than max.poll.interval.ms to process a batch.","distractors":["A high prefetchCount (e.g., 250, the old default) causes the broker to send up to 250 unacknowledged messages to each consumer before waiting for acks.","At-least-once means the broker may deliver a message more than once — a duplicate will occur if the producer retries or the consumer crashes after processing but before committing the offset.","Kafka's exactly-once semantics (EOS) prevents duplicates and lost messages across a produce → consume → produce pipeline."],"distractorIds":["spring/spring_messaging#95634f73fd80","spring/spring_messaging#01311ebf8984","spring/spring_messaging#1267060923d9"],"concepts":["poll","likely","frequent","fix","taking","cause"]},{"id":"spring/spring_messaging#78fe908829be","section":"spring","module":"spring/spring_messaging","moduleName":"spring messaging","sourceFile":"README.md","difficulty":"core","question":"Explain the role of the Dead Letter Topic (DLT) in Spring Kafka and how @RetryableTopic implements retry semantics.","answerFull":"A Dead Letter Topic holds messages that could not be processed successfully after all retry attempts. @RetryableTopic creates a chain of retry topics (e.g., orders-retry-0, orders-retry-1) each with a configured delay. When processing fails, the framework publishes the message to the next retry topic rather than immediately retrying on the original topic. This prevents blocking the main partition while waiting for a backoff delay. After all retry attempts are exhausted, the message is published to the DLT. A @DltHandler method can alert operators or store the message for manual replay. The key advantage over synchronous retry is that the main consumer continues processing other messages during the backoff period.","correct":"A Dead Letter Topic holds messages that could not be processed successfully after all retry attempts.","distractors":["The Outbox pattern solves the dual-write problem: when you need to write to a database and publish a Kafka event atomically.","A high prefetchCount (e.g., 250, the old default) causes the broker to send up to 250 unacknowledged messages to each consumer before waiting for acks.","In AUTO mode, Spring AMQP automatically acknowledges the message when the listener method returns without exception, and automatically NACK's (with requeue configured by the container) on exception."],"distractorIds":["spring/spring_messaging#ac63682ef801","spring/spring_messaging#95634f73fd80","spring/spring_messaging#5dd3aa13db31"],"concepts":["explain","successfully","letter","attempts","semantics","role"]},{"id":"spring/spring_messaging#ac63682ef801","section":"spring","module":"spring/spring_messaging","moduleName":"spring messaging","sourceFile":"README.md","difficulty":"core","question":"What is the Outbox pattern and why is it needed with Kafka?","answerFull":"The Outbox pattern solves the dual-write problem: when you need to write to a database and publish a Kafka event atomically. Without it, the two operations are independent — a crash between DB commit and Kafka send leaves the system in an inconsistent state. The Outbox pattern stores the event as a row in an outbox table within the same database transaction as the business write. A separate relay process (polling or Debezium CDC) reads pending outbox rows and publishes them to Kafka, then marks them as published. Because the business write and the outbox row are in the same transaction, they always succeed or fail together. Debezium CDC-based relay is preferred in production because it reads from the database write-ahead log rather than polling, providing near-real-time publishing.","correct":"The Outbox pattern solves the dual-write problem: when you need to write to a database and publish a Kafka event atomically.","distractors":["Kafka's exactly-once semantics (EOS) prevents duplicates and lost messages across a produce → consume → produce pipeline.","Use Testcontainers with KafkaContainer to start a real Kafka broker in Docker.","@Async executes the annotated method on a separate thread from a TaskExecutor."],"distractorIds":["spring/spring_messaging#1267060923d9","spring/spring_messaging#c6187db40072","spring/spring_messaging#65381f08ebbd"],"concepts":["publish","atomically","outbox","dual","solves","needed"],"distractorsMd":[null,null,"`@Async` executes the annotated method on a separate thread from a `TaskExecutor`."]},{"id":"spring/spring_messaging#e9947d650e20","section":"spring","module":"spring/spring_messaging","moduleName":"spring messaging","sourceFile":"README.md","difficulty":"core","question":"What is the difference between concurrency in @KafkaListener and partition count?","answerFull":"The concurrency attribute on @KafkaListener creates multiple KafkaMessageListenerContainer instances, each running its own poll loop. However, Kafka's consumer group protocol assigns at most one consumer per partition. So if concurrency=3 but the topic has only 2 partitions, one container will be idle. To utilize all three containers, the topic must have at least 3 partitions. In practice, partition count should be set to the maximum number of consumers you anticipate across all instances of the service. Adding partitions after the fact is possible but requires a rebalance.","correct":"The concurrency attribute on @KafkaListener creates multiple KafkaMessageListenerContainer instances, each running its own poll loop.","distractors":["The most common cause is the consumer taking longer than max.poll.interval.ms to process a batch.","@Async executes the annotated method on a separate thread from a TaskExecutor.","@Async is implemented via Spring AOP."],"distractorIds":["spring/spring_messaging#c0dd18e5c809","spring/spring_messaging#65381f08ebbd","spring/spring_messaging#f7bce73a7c10"],"concepts":["kafkalistener","kafkamessagelistenercontainer","poll","difference","attribute","concurrency"],"distractorsMd":[null,"`@Async` executes the annotated method on a separate thread from a `TaskExecutor`.",null]},{"id":"spring/spring_messaging#bd9a681fd3cb","section":"spring","module":"spring/spring_messaging","moduleName":"spring messaging","sourceFile":"README.md","difficulty":"intermediate","question":"How does RabbitMQ's Dead Letter Exchange (DLX) work, and what triggers a message to be dead-lettered?","answerFull":"A DLX is a regular exchange designated to receive messages that are rejected by a queue. A message is sent to the DLX when it is NACK'd with requeue=false, when it expires (message TTL or queue-level x-message-ttl), or when the queue is full and x-overflow=reject-publish-dlx is set. The dead-lettered message is published to the DLX with the original routing key (or an x-dead-letter-routing-key if configured on the source queue) and routed to the Dead Letter Queue (DLQ). From the DLQ, operators can inspect, replay, or discard messages. Without a DLX, NACK'd messages with requeue=false are simply discarded.","correct":"A DLX is a regular exchange designated to receive messages that are rejected by a queue.","distractors":["A dead letter exchange receives messages that are rejected (NACK + requeue=false), expired (TTL), or overflow a queue limit.","In AUTO mode, Spring AMQP automatically acknowledges the message when the listener method returns without exception, and automatically NACK's (with requeue configured by the container) on exception.","A high prefetchCount (e.g., 250, the old default) causes the broker to send up to 250 unacknowledged messages to each consumer before waiting for acks."],"distractorIds":["spring/spring_messaging#7c8d73a56dae","spring/spring_messaging#5dd3aa13db31","spring/spring_messaging#95634f73fd80"],"concepts":["dlx","designated","rabbitmq","rejected","letter","regular"]},{"id":"spring/spring_messaging#5dd3aa13db31","section":"spring","module":"spring/spring_messaging","moduleName":"spring messaging","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between @RabbitListener with AcknowledgeMode.AUTO vs MANUAL?","answerFull":"In AUTO mode, Spring AMQP automatically acknowledges the message when the listener method returns without exception, and automatically NACK's (with requeue configured by the container) on exception. This is simpler but less flexible — you cannot conditionally NACK based on the exception type. In MANUAL mode, the consumer controls acknowledgement by injecting Channel and the delivery tag, then explicitly calling channel.basicAck() or channel.basicNack(). MANUAL mode is required when you need to differentiate transient failures (requeue=true) from permanent failures (requeue=false, route to DLX), or when you need to ack only after a side-effect (DB write) succeeds.","correct":"In AUTO mode, Spring AMQP automatically acknowledges the message when the listener method returns without exception, and automatically NACK's (with requeue configured by the container) on exception.","distractors":["At-least-once means the broker may deliver a message more than once — a duplicate will occur if the producer retries or the consumer crashes after processing but before committing the offset.","@Async is implemented via Spring AOP.","@Async executes the annotated method on a separate thread from a TaskExecutor."],"distractorIds":["spring/spring_messaging#01311ebf8984","spring/spring_messaging#f7bce73a7c10","spring/spring_messaging#65381f08ebbd"],"concepts":["nack","requeue","amqp","acknowledges","listener","difference"],"distractorsMd":[null,null,"`@Async` executes the annotated method on a separate thread from a `TaskExecutor`."]},{"id":"spring/spring_messaging#44fba7d17dac","section":"spring","module":"spring/spring_messaging","moduleName":"spring messaging","sourceFile":"README.md","difficulty":"intermediate","question":"Explain Spring Cloud Stream's functional programming model. How does it differ from @StreamListener?","answerFull":"The functional model replaced @StreamListener (deprecated in Spring Cloud Stream 3.x). Instead of annotation-driven listener methods, you expose standard Java functional beans: Consumer<T> for consuming messages, Supplier<T> for producing messages, and Function<T,R> for consuming and producing. The binder discovers these beans and wires them to topics or queues based on the bean name and spring.cloud.stream.bindings configuration. The advantage is that the business logic is pure Java functions with no framework annotations — they are easily unit-testable without a Spring context. The binder handles serialization, error handling, and retry. The functional model also supports reactive types (Flux<T>, Mono<T>) for reactive stream processing.","correct":"The functional model replaced @StreamListener (deprecated in Spring Cloud Stream 3.x).","distractors":["@Async is implemented via Spring AOP.","In AUTO mode, Spring AMQP automatically acknowledges the message when the listener method returns without exception, and automatically NACK's (with requeue configured by the container) on exception.","@Async executes the annotated method on a separate thread from a TaskExecutor."],"distractorIds":["spring/spring_messaging#f7bce73a7c10","spring/spring_messaging#5dd3aa13db31","spring/spring_messaging#65381f08ebbd"],"concepts":["streamlistener","deprecated","explain","functional","replaced","differ"],"distractorsMd":[null,null,"`@Async` executes the annotated method on a separate thread from a `TaskExecutor`."]},{"id":"spring/spring_messaging#f7bce73a7c10","section":"spring","module":"spring/spring_messaging","moduleName":"spring messaging","sourceFile":"README.md","difficulty":"intermediate","question":"How does @Async work internally in Spring? What happens if @EnableAsync is missing?","answerFull":"@Async is implemented via Spring AOP. When @EnableAsync is present, Spring creates a proxy for every bean that has @Async methods. When the @Async method is called through the proxy, the proxy submits a Runnable to the configured TaskExecutor and returns immediately with a CompletableFuture (or void). The actual method executes in the executor thread. If @EnableAsync is missing, no proxy is created — the @Async annotation is silently ignored and the method executes synchronously in the caller's thread. There is no exception or warning. This is a common source of silent bugs in production.","correct":"@Async is implemented via Spring AOP.","distractors":["@Async executes the annotated method on a separate thread from a TaskExecutor.","In AUTO mode, Spring AMQP automatically acknowledges the message when the listener method returns without exception, and automatically NACK's (with requeue configured by the container) on exception.","The functional model replaced @StreamListener (deprecated in Spring Cloud Stream 3.x)."],"distractorIds":["spring/spring_messaging#65381f08ebbd","spring/spring_messaging#5dd3aa13db31","spring/spring_messaging#44fba7d17dac"],"concepts":["implemented","internally","aop","missing","happens","work"],"distractorsMd":["`@Async` executes the annotated method on a separate thread from a `TaskExecutor`.",null,null]},{"id":"spring/spring_messaging#39a079d25620","section":"spring","module":"spring/spring_messaging","moduleName":"spring messaging","sourceFile":"README.md","difficulty":"intermediate","question":"What is the SockJS fallback in Spring WebSocket, and when is it needed?","answerFull":"SockJS is a JavaScript library and protocol that provides a WebSocket-like API with automatic fallback to HTTP long-polling or HTTP streaming when WebSocket is unavailable. This is needed in corporate environments with proxies or firewalls that block WebSocket upgrades. When SockJS is configured via registry.addEndpoint(\"/ws\").withSockJS(), the Spring backend supports multiple transports on the same endpoint: native WebSocket, XHR-streaming, and XHR-polling. The client SockJS library negotiates the best available transport. In production, prefer an external STOMP broker relay (RabbitMQ with STOMP plugin) over the in-memory SimpleBroker for persistence and horizontal scalability.","correct":"SockJS is a JavaScript library and protocol that provides a WebSocket-like API with automatic fallback to HTTP long-polling or HTTP streaming when WebSocket is unavailable.","distractors":["@Async is implemented via Spring AOP.","The functional model replaced @StreamListener (deprecated in Spring Cloud Stream 3.x).","In AUTO mode, Spring AMQP automatically acknowledges the message when the listener method returns without exception, and automatically NACK's (with requeue configured by the container) on exception."],"distractorIds":["spring/spring_messaging#f7bce73a7c10","spring/spring_messaging#44fba7d17dac","spring/spring_messaging#5dd3aa13db31"],"concepts":["sockjs","javascript","unavailable","polling","fallback","automatic"]},{"id":"spring/spring_messaging#95634f73fd80","section":"spring","module":"spring/spring_messaging","moduleName":"spring messaging","sourceFile":"README.md","difficulty":"advanced","question":"What is the risk of prefetchCount in RabbitMQ being too high?","answerFull":"A high prefetchCount (e.g., 250, the old default) causes the broker to send up to 250 unacknowledged messages to each consumer before waiting for acks. If the consumer is slow or crashes, all 250 messages are held in memory unprocessed, and other consumers cannot receive them. This reduces throughput by concentrating load on slow consumers. In Spring AMQP, the default prefetchCount was changed from 250 to 1 (fair dispatch) in older versions, but Spring Boot auto-configuration sets it to 250 unless overridden. For fair dispatch, set prefetchCount=1. For higher throughput with fast consumers, increase it — but monitor unacked message counts to find the optimal value.","correct":"A high prefetchCount (e.g., 250, the old default) causes the broker to send up to 250 unacknowledged messages to each consumer before waiting for acks.","distractors":["Use Testcontainers with KafkaContainer to start a real Kafka broker in Docker.","A dead letter exchange receives messages that are rejected (NACK + requeue=false), expired (TTL), or overflow a queue limit.","A Dead Letter Topic holds messages that could not be processed successfully after all retry attempts."],"distractorIds":["spring/spring_messaging#c6187db40072","spring/spring_messaging#7c8d73a56dae","spring/spring_messaging#78fe908829be"],"concepts":["prefetchcount","250","unacknowledged","acks","rabbitmq","risk"]},{"id":"spring/spring_messaging#c6187db40072","section":"spring","module":"spring/spring_messaging","moduleName":"spring messaging","sourceFile":"README.md","difficulty":"advanced","question":"How do you test a Kafka consumer in a Spring Boot integration test?","answerFull":"Use Testcontainers with KafkaContainer to start a real Kafka broker in Docker. Annotate the test class with @Testcontainers and @SpringBootTest. Use @DynamicPropertySource to set spring.kafka.bootstrap-servers to the container's mapped port. Publish messages using KafkaTemplate in the test and verify consumer behavior via assertions on the database or a CountDownLatch that the listener decrements. For unit tests of listener logic, test the handler method directly without Spring context overhead. The EmbeddedKafkaBroker (@EmbeddedKafka) is an alternative that avoids Docker but is less production-representative.","correct":"Use Testcontainers with KafkaContainer to start a real Kafka broker in Docker.","distractors":["The Outbox pattern solves the dual-write problem: when you need to write to a database and publish a Kafka event atomically.","Kafka's exactly-once semantics (EOS) prevents duplicates and lost messages across a produce → consume → produce pipeline.","A high prefetchCount (e.g., 250, the old default) causes the broker to send up to 250 unacknowledged messages to each consumer before waiting for acks."],"distractorIds":["spring/spring_messaging#ac63682ef801","spring/spring_messaging#1267060923d9","spring/spring_messaging#95634f73fd80"],"concepts":["kafkacontainer","testcontainers","integration","docker","boot","broker"]},{"id":"spring/spring_messaging#1267060923d9","section":"spring","module":"spring/spring_messaging","moduleName":"spring messaging","sourceFile":"README.md","difficulty":"advanced","question":"What is exactly-once semantics in Kafka and how does Spring Kafka's @Transactional integration achieve it?","answerFull":"Kafka's exactly-once semantics (EOS) prevents duplicates and lost messages across a produce → consume → produce pipeline. Spring Kafka supports EOS via KafkaTransactionManager and Spring's @Transactional. When enabled: (1) The KafkaProducer is configured as an idempotent transactional producer (enable.idempotence=true, transactional.id=...). (2) @Transactional on a @KafkaListener method wraps the entire consume-process-produce in a single Kafka transaction — the consumer offset commit and the downstream produce are both included in the transaction. If the method throws, both the offset commit and any produced messages are rolled back. On retry, the consumer re-processes the same message. Config: spring.kafka.producer.transaction-id-prefix=tx- and spring.kafka.consumer.isolation-level=read_committed (consumers only see committed messages). Limitation: EOS adds ~20–30% throughput overhead and requires a Kafka broker ≥ 0.11.","correct":"Kafka's exactly-once semantics (EOS) prevents duplicates and lost messages across a produce → consume → produce pipeline.","distractors":["At-least-once means the broker may deliver a message more than once — a duplicate will occur if the producer retries or the consumer crashes after processing but before committing the offset.","A high prefetchCount (e.g., 250, the old default) causes the broker to send up to 250 unacknowledged messages to each consumer before waiting for acks.","@Async is implemented via Spring AOP."],"distractorIds":["spring/spring_messaging#01311ebf8984","spring/spring_messaging#95634f73fd80","spring/spring_messaging#f7bce73a7c10"],"concepts":["eos","integration","consume","duplicates","achieve","semantics"],"questionMd":"What is exactly-once semantics in Kafka and how does Spring Kafka's `@Transactional` integration achieve it?","answerFullMd":"Kafka's exactly-once semantics (EOS) prevents duplicates and lost messages across a produce → consume → produce pipeline. Spring Kafka supports EOS via `KafkaTransactionManager` and Spring's `@Transactional`. When enabled: (1) The `KafkaProducer` is configured as an idempotent transactional producer (`enable.idempotence=true`, `transactional.id=...`). (2) `@Transactional` on a `@KafkaListener` method wraps the entire consume-process-produce in a single Kafka transaction — the consumer offset commit and the downstream produce are both included in the transaction. If the method throws, both the offset commit and any produced messages are rolled back. On retry, the consumer re-processes the same message. Config: `spring.kafka.producer.transaction-id-prefix=tx-` and `spring.kafka.consumer.isolation-level=read_committed` (consumers only see committed messages). Limitation: EOS adds ~20–30% throughput overhead and requires a Kafka broker ≥ 0.11."},{"id":"spring/spring_messaging#7c8d73a56dae","section":"spring","module":"spring/spring_messaging","moduleName":"spring messaging","sourceFile":"README.md","difficulty":"advanced","question":"What is Spring AMQP's dead letter exchange (DLX) pattern and how do you implement retry with backoff?","answerFull":"A dead letter exchange receives messages that are rejected (NACK + requeue=false), expired (TTL), or overflow a queue limit. Configure a per-queue DLX: when a @RabbitListener throws and exhausts retries, Spring AMQP sends the message to the DLX with the original exchange/routing key as dead-letter headers. Retry with backoff: configure RetryInterceptorBuilder.stateless() (or stateful for commit semantics) on the listener container with exponential back-off (ExponentialBackOffPolicy). After max attempts, the interceptor calls the recoverer which publishes to the DLQ. Production pattern: yaml # Queue definition: original queue with 3-retry DLX queue: orders.processing arguments: x-dead-letter-exchange: orders.dlx x-dead-letter-routing-key: orders.failed The DLQ is consumed by a separate listener for manual review or republish after the downstream issue is resolved. Always set defaultRequeueRejected=false on the listener container when using DLX — otherwise rejected messages re-enter the queue indefinitely.","correct":"A dead letter exchange receives messages that are rejected (NACK + requeue=false), expired (TTL), or overflow a queue limit.","distractors":["The functional model replaced @StreamListener (deprecated in Spring Cloud Stream 3.x).","A DLX is a regular exchange designated to receive messages that are rejected by a queue.","Kafka's exactly-once semantics (EOS) prevents duplicates and lost messages across a produce → consume → produce pipeline."],"distractorIds":["spring/spring_messaging#44fba7d17dac","spring/spring_messaging#bd9a681fd3cb","spring/spring_messaging#1267060923d9"],"concepts":["dlx","expired","nack","requeue","amqp","rejected"],"answerFullMd":"A dead letter exchange receives messages that are rejected (NACK + requeue=false), expired (TTL), or overflow a queue limit. Configure a per-queue DLX: when a `@RabbitListener` throws and exhausts retries, Spring AMQP sends the message to the DLX with the original exchange/routing key as dead-letter headers. Retry with backoff: configure `RetryInterceptorBuilder.stateless()` (or stateful for commit semantics) on the listener container with exponential back-off (`ExponentialBackOffPolicy`). After max attempts, the interceptor calls the recoverer which publishes to the DLQ. Production pattern: ```yaml # Queue definition: original queue with 3-retry DLX queue: orders.processing arguments: x-dead-letter-exchange: orders.dlx x-dead-letter-routing-key: orders.failed ``` The DLQ is consumed by a separate listener for manual review or republish after the downstream issue is resolved. Always set `defaultRequeueRejected=false` on the listener container when using DLX — otherwise rejected messages re-enter the queue indefinitely."},{"id":"spring/spring_messaging#65381f08ebbd","section":"spring","module":"spring/spring_messaging","moduleName":"spring messaging","sourceFile":"README.md","difficulty":"advanced","question":"How do @Async methods interact with Spring transactions and what is the \"lost transaction context\" problem?","answerFull":"@Async executes the annotated method on a separate thread from a TaskExecutor. Spring's @Transactional stores the transaction in a ThreadLocal (via TransactionSynchronizationManager). When a @Transactional method calls an @Async method, the async method runs on a different thread — it has no access to the caller's ThreadLocal transaction context. Consequences: (1) The async method starts a new transaction (if @Transactional is present), independent of the caller's transaction. (2) If the caller rolls back, the async method's already-committed transaction is NOT rolled back — you have an orphaned write. Fix for tight coupling: use @TransactionalEventListener(phase = AFTER_COMMIT) — the event listener fires only after the outer transaction commits successfully, and runs asynchronously via @Async without needing to participate in the outer transaction. This is the correct pattern for triggering async side effects (emails, notifications) after a database write.","correct":"@Async executes the annotated method on a separate thread from a TaskExecutor.","distractors":["In AUTO mode, Spring AMQP automatically acknowledges the message when the listener method returns without exception, and automatically NACK's (with requeue configured by the container) on exception.","Kafka's exactly-once semantics (EOS) prevents duplicates and lost messages across a produce → consume → produce pipeline.","The functional model replaced @StreamListener (deprecated in Spring Cloud Stream 3.x)."],"distractorIds":["spring/spring_messaging#5dd3aa13db31","spring/spring_messaging#1267060923d9","spring/spring_messaging#44fba7d17dac"],"concepts":["taskexecutor","interact","lost","annotated","transactions","executes"],"questionMd":"How do `@Async` methods interact with Spring transactions and what is the \"lost transaction context\" problem?","correctMd":"`@Async` executes the annotated method on a separate thread from a `TaskExecutor`.","answerFullMd":"`@Async` executes the annotated method on a separate thread from a `TaskExecutor`. Spring's `@Transactional` stores the transaction in a `ThreadLocal` (via `TransactionSynchronizationManager`). When a `@Transactional` method calls an `@Async` method, the async method runs on a different thread — it has no access to the caller's `ThreadLocal` transaction context. Consequences: (1) The async method starts a new transaction (if `@Transactional` is present), independent of the caller's transaction. (2) If the caller rolls back, the async method's already-committed transaction is NOT rolled back — you have an orphaned write. Fix for tight coupling: use `@TransactionalEventListener(phase = AFTER_COMMIT)` — the event listener fires only after the outer transaction commits successfully, and runs asynchronously via `@Async` without needing to participate in the outer transaction. This is the correct pattern for triggering async side effects (emails, notifications) after a database write."},{"id":"spring/spring_modulith#642807a69b3a","section":"spring","module":"spring/spring_modulith","moduleName":"spring modulith","sourceFile":"README.md","difficulty":"core","question":"What problem does Spring Modulith solve, and where does a modular monolith sit between a plain monolith and microservices?","answerFull":"It solves boundary erosion: in a plain monolith every class can reach every other, so modules decay into a big ball of mud. Microservices fix this by making boundaries network boundaries — at the cost of latency, partial failure, and operational overhead. A modular monolith (what Modulith enables) keeps one deployable but *enforces* internal module boundaries via build-time verification and event-based decoupling. So it gives microservices' internal discipline without the distributed- systems tax, and a clean seam to extract a real service later if needed.","correct":"It solves boundary erosion: in a plain monolith every class can reach every other, so modules decay into a big ball of mud.","distractors":["A modular monolith is the ideal starting point and the ideal extraction source: you build with enforced module boundaries and event contracts from day one, so each module is already a candidate bounded context.","Because a cycle means two modules cannot be understood, tested, changed, or extracted independently — they are effectively one tangled unit, which defeats modularity.","Direct calls create dependencies in the direction of the call, so two-way interaction produces a cycle."],"distractorIds":["spring/spring_modulith#4c05d6abd470","spring/spring_modulith#7fbebc425079","spring/spring_modulith#c295e742241a"],"concepts":["erosion","modular","mud","ball","big","sit"]},{"id":"spring/spring_modulith#7fbebc425079","section":"spring","module":"spring/spring_modulith","moduleName":"spring modulith","sourceFile":"README.md","difficulty":"core","question":"Why are cyclic dependencies between modules disallowed?","answerFull":"Because a cycle means two modules cannot be understood, tested, changed, or extracted independently — they are effectively one tangled unit, which defeats modularity. Modulith's verify() fails on cycles. The standard fix is dependency inversion via events: instead of A calling B and B calling A, one module publishes a domain event and the other listens, so both depend only on the event type and the graph becomes a DAG.","correct":"Because a cycle means two modules cannot be understood, tested, changed, or extracted independently — they are effectively one tangled unit, which defeats modularity.","distractors":["The Documenter API produces PlantUML/C4 component diagrams (whole-system and per-module) and a \"module canvas\" listing each module's API, published/consumed events, and dependencies — derived directly from the code.","It solves boundary erosion: in a plain monolith every class can reach every other, so modules decay into a big ball of mud.","Direct calls create dependencies in the direction of the call, so two-way interaction produces a cycle."],"distractorIds":["spring/spring_modulith#5b9c1f29aa36","spring/spring_modulith#642807a69b3a","spring/spring_modulith#c295e742241a"],"concepts":["defeats","modularity","tangled","understood","cyclic","extracted"],"answerFullMd":"Because a cycle means two modules cannot be understood, tested, changed, or extracted independently — they are effectively one tangled unit, which defeats modularity. Modulith's `verify()` fails on cycles. The standard fix is dependency inversion via events: instead of A calling B and B calling A, one module publishes a domain event and the other listens, so both depend only on the event type and the graph becomes a DAG.","distractorsMd":["The `Documenter` API produces PlantUML/C4 component diagrams (whole-system and per-module) and a \"module canvas\" listing each module's API, published/consumed events, and dependencies — derived directly from the code.",null,null]},{"id":"spring/spring_modulith#a8f939a3e879","section":"spring","module":"spring/spring_modulith","moduleName":"spring modulith","sourceFile":"README.md","difficulty":"intermediate","question":"How do you test a single module in isolation?","answerFull":"Use @ApplicationModuleTest, which bootstraps only the module under test (and its declared dependencies) rather than the whole application, making tests fast and scoped. Combined with the Scenario API you can drive event-based behavior: publish an inbound event, await a resulting state change or outbound event, and assert on it. This verifies a module's contract (events in → effects out) without standing up unrelated modules.","correct":"Use @ApplicationModuleTest, which bootstraps only the module under test (and its declared dependencies) rather than the whole application, making tests fast and scoped.","distractors":["Direct calls create dependencies in the direction of the call, so two-way interaction produces a cycle.","A modular monolith is the ideal starting point and the ideal extraction source: you build with enforced module boundaries and event contracts from day one, so each module is already a candidate bounded context.","It does not give independent deployment, independent scaling, fault isolation, or technology heterogeneity."],"distractorIds":["spring/spring_modulith#c295e742241a","spring/spring_modulith#4c05d6abd470","spring/spring_modulith#05a557da4151"],"concepts":["applicationmoduletest","bootstraps","scoped","declared","module","tests"],"correctMd":"Use `@ApplicationModuleTest`, which bootstraps only the module under test (and its declared dependencies) rather than the whole application, making tests fast and scoped.","answerFullMd":"Use `@ApplicationModuleTest`, which bootstraps only the module under test (and its declared dependencies) rather than the whole application, making tests fast and scoped. Combined with the `Scenario` API you can drive event-based behavior: publish an inbound event, await a resulting state change or outbound event, and assert on it. This verifies a module's contract (events in → effects out) without standing up unrelated modules."},{"id":"spring/spring_modulith#05a557da4151","section":"spring","module":"spring/spring_modulith","moduleName":"spring modulith","sourceFile":"README.md","difficulty":"intermediate","question":"What are the limits of a modular monolith — what does it NOT give you?","answerFull":"It does not give independent deployment, independent scaling, fault isolation, or technology heterogeneity. It is one process with shared fate: an OOM, a crash, or a runaway thread in one module takes the whole app down, and you scale the entire deployable, not a single module. Modulith gives *structural* modularity (boundaries, decoupling, testability, docs) but not *operational* independence — those require true service extraction.","correct":"It does not give independent deployment, independent scaling, fault isolation, or technology heterogeneity.","distractors":["A modular monolith is the ideal starting point and the ideal extraction source: you build with enforced module boundaries and event contracts from day one, so each module is already a candidate bounded context.","It solves boundary erosion: in a plain monolith every class can reach every other, so modules decay into a big ball of mud.","Because a cycle means two modules cannot be understood, tested, changed, or extracted independently — they are effectively one tangled unit, which defeats modularity."],"distractorIds":["spring/spring_modulith#4c05d6abd470","spring/spring_modulith#642807a69b3a","spring/spring_modulith#7fbebc425079"],"concepts":["modular","heterogeneity","technology","monolith","fault","give"]},{"id":"spring/spring_modulith#5b9c1f29aa36","section":"spring","module":"spring/spring_modulith","moduleName":"spring modulith","sourceFile":"README.md","difficulty":"advanced","question":"How does Modulith generate documentation, and why is that valuable?","answerFull":"The Documenter API produces PlantUML/C4 component diagrams (whole-system and per-module) and a \"module canvas\" listing each module's API, published/consumed events, and dependencies — derived directly from the code. Running it inside the verification test means the docs regenerate on every build and therefore never drift from reality, solving the perennial problem of architecture diagrams that are stale the day after they are drawn.","correct":"The Documenter API produces PlantUML/C4 component diagrams (whole-system and per-module) and a \"module canvas\" listing each module's API, published/consumed events, and dependencies — derived directly from the code.","distractors":["Use @ApplicationModuleTest, which bootstraps only the module under test (and its declared dependencies) rather than the whole application, making tests fast and scoped.","Direct calls create dependencies in the direction of the call, so two-way interaction produces a cycle.","A modular monolith is the ideal starting point and the ideal extraction source: you build with enforced module boundaries and event contracts from day one, so each module is already a candidate bounded context."],"distractorIds":["spring/spring_modulith#a8f939a3e879","spring/spring_modulith#c295e742241a","spring/spring_modulith#4c05d6abd470"],"concepts":["diagrams","documenter","plantuml","canvas","documentation","listing"],"correctMd":"The `Documenter` API produces PlantUML/C4 component diagrams (whole-system and per-module) and a \"module canvas\" listing each module's API, published/consumed events, and dependencies — derived directly from the code.","answerFullMd":"The `Documenter` API produces PlantUML/C4 component diagrams (whole-system and per-module) and a \"module canvas\" listing each module's API, published/consumed events, and dependencies — derived directly from the code. Running it inside the verification test means the docs regenerate on every build and therefore never drift from reality, solving the perennial problem of architecture diagrams that are stale the day after they are drawn.","distractorsMd":["Use `@ApplicationModuleTest`, which bootstraps only the module under test (and its declared dependencies) rather than the whole application, making tests fast and scoped.",null,null]},{"id":"spring/spring_modulith#c295e742241a","section":"spring","module":"spring/spring_modulith","moduleName":"spring modulith","sourceFile":"README.md","difficulty":"advanced","question":"How do events help keep the module dependency graph acyclic?","answerFull":"Direct calls create dependencies in the direction of the call, so two-way interaction produces a cycle. Events invert the consumer's dependency: the publisher emits an event without knowing who consumes it (it depends only on the event type it owns), and consumers depend on that event type rather than on the publisher's beans. So a would-be back-edge (B notifying A) becomes \"A listens to an event B publishes,\" and the module graph stays a DAG that passes verification.","correct":"Direct calls create dependencies in the direction of the call, so two-way interaction produces a cycle.","distractors":["Because a cycle means two modules cannot be understood, tested, changed, or extracted independently — they are effectively one tangled unit, which defeats modularity.","A modular monolith is the ideal starting point and the ideal extraction source: you build with enforced module boundaries and event contracts from day one, so each module is already a candidate bounded context.","The Documenter API produces PlantUML/C4 component diagrams (whole-system and per-module) and a \"module canvas\" listing each module's API, published/consumed events, and dependencies — derived directly from the code."],"distractorIds":["spring/spring_modulith#7fbebc425079","spring/spring_modulith#4c05d6abd470","spring/spring_modulith#5b9c1f29aa36"],"concepts":["help","way","direction","interaction","cycle","module"],"distractorsMd":[null,null,"The `Documenter` API produces PlantUML/C4 component diagrams (whole-system and per-module) and a \"module canvas\" listing each module's API, published/consumed events, and dependencies — derived directly from the code."]},{"id":"spring/spring_modulith#907e1d58656c","section":"spring","module":"spring/spring_modulith","moduleName":"spring modulith","sourceFile":"README.md","difficulty":"advanced","question":"Can a module expose more than one public API package?","answerFull":"Yes — by default only the module's base package is the public API, but you can mark additional sub-packages with @NamedInterface to expose them as named public surfaces that other modules are allowed to depend on. This lets you, for example, separate a module's \"spi\" (for extensions) from its main API while still keeping the rest of its internals hidden. Everything not in the base package or a named interface remains module-private.","correct":"Yes — by default only the module's base package is the public API, but you can mark additional sub-packages with @NamedInterface to expose them as named public surfaces that other modules are allowed to depend on.","distractors":["Because a cycle means two modules cannot be understood, tested, changed, or extracted independently — they are effectively one tangled unit, which defeats modularity.","The Documenter API produces PlantUML/C4 component diagrams (whole-system and per-module) and a \"module canvas\" listing each module's API, published/consumed events, and dependencies — derived directly from the code.","It solves boundary erosion: in a plain monolith every class can reach every other, so modules decay into a big ball of mud."],"distractorIds":["spring/spring_modulith#7fbebc425079","spring/spring_modulith#5b9c1f29aa36","spring/spring_modulith#642807a69b3a"],"concepts":["namedinterface","surfaces","mark","packages","expose","depend"],"correctMd":"Yes — by default only the module's base package is the public API, but you can mark additional sub-packages with `@NamedInterface` to expose them as named public surfaces that other modules are allowed to depend on.","answerFullMd":"Yes — by default only the module's base package is the public API, but you can mark additional sub-packages with `@NamedInterface` to expose them as named public surfaces that other modules are allowed to depend on. This lets you, for example, separate a module's \"spi\" (for extensions) from its main API while still keeping the rest of its internals hidden. Everything not in the base package or a named interface remains module-private.","distractorsMd":[null,"The `Documenter` API produces PlantUML/C4 component diagrams (whole-system and per-module) and a \"module canvas\" listing each module's API, published/consumed events, and dependencies — derived directly from the code.",null]},{"id":"spring/spring_modulith#4c05d6abd470","section":"spring","module":"spring/spring_modulith","moduleName":"spring modulith","sourceFile":"README.md","difficulty":"advanced","question":"How does Modulith fit a strangler-fig or monolith-first migration strategy?","answerFull":"A modular monolith is the ideal starting point and the ideal extraction source: you build with enforced module boundaries and event contracts from day one, so each module is already a candidate bounded context. When a module needs to become a service, its API and the events it publishes/consumes are already defined, so you can replace the in-process event transport with a real broker (Modulith even supports event externalization to Kafka/AMQP) and extract the module behind its existing contract — far less risky than carving boundaries out of a tangled monolith.","correct":"A modular monolith is the ideal starting point and the ideal extraction source: you build with enforced module boundaries and event contracts from day one, so each module is already a candidate bounded context.","distractors":["Use @ApplicationModuleTest, which bootstraps only the module under test (and its declared dependencies) rather than the whole application, making tests fast and scoped.","The Documenter API produces PlantUML/C4 component diagrams (whole-system and per-module) and a \"module canvas\" listing each module's API, published/consumed events, and dependencies — derived directly from the code.","It solves boundary erosion: in a plain monolith every class can reach every other, so modules decay into a big ball of mud."],"distractorIds":["spring/spring_modulith#a8f939a3e879","spring/spring_modulith#5b9c1f29aa36","spring/spring_modulith#642807a69b3a"],"concepts":["modular","contracts","fig","strangler","ideal","monolith"],"distractorsMd":["Use `@ApplicationModuleTest`, which bootstraps only the module under test (and its declared dependencies) rather than the whole application, making tests fast and scoped.","The `Documenter` API produces PlantUML/C4 component diagrams (whole-system and per-module) and a \"module canvas\" listing each module's API, published/consumed events, and dependencies — derived directly from the code.",null]},{"id":"spring/spring_mvc_architecture#d441716926d4","section":"spring","module":"spring/spring_mvc_architecture","moduleName":"spring mvc architecture","sourceFile":"README.md","difficulty":"core","question":"What is the role of DispatcherServlet in Spring MVC?","answerFull":"DispatcherServlet is the Front Controller — a single servlet that handles all HTTP requests. It orchestrates the request pipeline by delegating to HandlerMapping (find the right controller method), HandlerAdapter (invoke it with resolved arguments), ViewResolver or HttpMessageConverter (render the response), and HandlerExceptionResolver (handle exceptions). Every Spring MVC request flows through this pipeline. In Spring Boot, DispatcherServlet is auto-configured and mapped to \"/\".","correct":"DispatcherServlet is the Front Controller — a single servlet that handles all HTTP requests.","distractors":["@RestController is @Controller + @ResponseBody — every handler method's return value is serialized directly to the response body via HttpMessageConverter.","HttpMessageConverter<T> handles reading a request body into a Java object (@RequestBody) and writing a Java object to the response body (@ResponseBody).","HandlerMapping resolves the request URL (and method, headers, params) to a handler — in most cases a HandlerMethod representing a @RequestMapping-annotated controller method."],"distractorIds":["spring/spring_mvc_architecture#ad7871d0ede5","spring/spring_mvc_architecture#ef51204da5ca","spring/spring_mvc_architecture#2a3ef954baf8"],"concepts":["dispatcherservlet","mvc","servlet","front","role","handles"],"correctMd":"`DispatcherServlet` is the Front Controller — a single servlet that handles all HTTP requests.","answerFullMd":"`DispatcherServlet` is the Front Controller — a single servlet that handles all HTTP requests. It orchestrates the request pipeline by delegating to `HandlerMapping` (find the right controller method), `HandlerAdapter` (invoke it with resolved arguments), `ViewResolver` or `HttpMessageConverter` (render the response), and `HandlerExceptionResolver` (handle exceptions). Every Spring MVC request flows through this pipeline. In Spring Boot, `DispatcherServlet` is auto-configured and mapped to `\"/\"`.","distractorsMd":["`@RestController` is `@Controller` + `@ResponseBody` — every handler method's return value is serialized directly to the response body via `HttpMessageConverter`.","`HttpMessageConverter<T>` handles reading a request body into a Java object (`@RequestBody`) and writing a Java object to the response body (`@ResponseBody`).","`HandlerMapping` resolves the request URL (and method, headers, params) to a handler — in most cases a `HandlerMethod` representing a `@RequestMapping`-annotated controller method."]},{"id":"spring/spring_mvc_architecture#2a3ef954baf8","section":"spring","module":"spring/spring_mvc_architecture","moduleName":"spring mvc architecture","sourceFile":"README.md","difficulty":"core","question":"What is the difference between HandlerMapping and HandlerAdapter?","answerFull":"HandlerMapping resolves the request URL (and method, headers, params) to a handler — in most cases a HandlerMethod representing a @RequestMapping-annotated controller method. HandlerAdapter knows how to invoke a particular type of handler — RequestMappingHandlerAdapter handles HandlerMethod objects, resolving method parameters (@RequestBody, @PathVariable, etc.) and converting the return value. The separation allows non-standard handlers (WebSocket handlers, HTTP function handlers) to integrate via custom adapter implementations.","correct":"HandlerMapping resolves the request URL (and method, headers, params) to a handler — in most cases a HandlerMethod representing a @RequestMapping-annotated controller method.","distractors":["Content negotiation is the process by which Spring MVC selects the response media type (e.g., application/json, application/xml) based on the client's Accept header and the handler's declared capabilities.","Implement HandlerMethodArgumentResolver with supportsParameter(MethodParameter p) (return true for your custom type) and resolveArgument(...) (extract and return the argument from the request).","HttpMessageConverter<T> handles reading a request body into a Java object (@RequestBody) and writing a Java object to the response body (@ResponseBody)."],"distractorIds":["spring/spring_mvc_architecture#02819114985b","spring/spring_mvc_architecture#e2167e7ffa54","spring/spring_mvc_architecture#ef51204da5ca"],"concepts":["handlermapping","handlermethod","requestmapping","difference","params","resolves"],"correctMd":"`HandlerMapping` resolves the request URL (and method, headers, params) to a handler — in most cases a `HandlerMethod` representing a `@RequestMapping`-annotated controller method.","answerFullMd":"`HandlerMapping` resolves the request URL (and method, headers, params) to a handler — in most cases a `HandlerMethod` representing a `@RequestMapping`-annotated controller method. `HandlerAdapter` knows how to invoke a particular type of handler — `RequestMappingHandlerAdapter` handles `HandlerMethod` objects, resolving method parameters (`@RequestBody`, `@PathVariable`, etc.) and converting the return value. The separation allows non-standard handlers (WebSocket handlers, HTTP function handlers) to integrate via custom adapter implementations.","distractorsMd":["Content negotiation is the process by which Spring MVC selects the response media type (e.g., `application/json`, `application/xml`) based on the client's `Accept` header and the handler's declared capabilities.","Implement `HandlerMethodArgumentResolver` with `supportsParameter(MethodParameter p)` (return true for your custom type) and `resolveArgument(...)` (extract and return the argument from the request).","`HttpMessageConverter<T>` handles reading a request body into a Java object (`@RequestBody`) and writing a Java object to the response body (`@ResponseBody`)."]},{"id":"spring/spring_mvc_architecture#ef51204da5ca","section":"spring","module":"spring/spring_mvc_architecture","moduleName":"spring mvc architecture","sourceFile":"README.md","difficulty":"core","question":"What are HTTP message converters and when are they used?","answerFull":"HttpMessageConverter<T> handles reading a request body into a Java object (@RequestBody) and writing a Java object to the response body (@ResponseBody). Selection is based on: the Java type being converted, and the Content-Type (for reading) or Accept header (for writing). MappingJackson2HttpMessageConverter handles application/json. StringHttpMessageConverter handles text/plain. If no converter can handle the type/media-type combination, Spring returns 406 Not Acceptable (write) or 415 Unsupported Media Type (read).","correct":"HttpMessageConverter<T> handles reading a request body into a Java object (@RequestBody) and writing a Java object to the response body (@ResponseBody).","distractors":["Content negotiation determines the response format based on: (1) Accept header in the request (Accept: application/json), (2) request parameter (?format=json, if configured), or (3) configured default content type.","Content negotiation is the process by which Spring MVC selects the response media type (e.g., application/json, application/xml) based on the client's Accept header and the handler's declared capabilities.","DispatcherServlet throws NoHandlerFoundException (if throwExceptionIfNoHandlerFound=true) or sends a 404 response directly."],"distractorIds":["spring/spring_mvc_architecture#f79be14ada7a","spring/spring_mvc_architecture#02819114985b","spring/spring_mvc_architecture#864160c0cb46"],"concepts":["converters","responsebody","requestbody","httpmessageconverter","reading","writing"],"correctMd":"`HttpMessageConverter<T>` handles reading a request body into a Java object (`@RequestBody`) and writing a Java object to the response body (`@ResponseBody`).","answerFullMd":"`HttpMessageConverter<T>` handles reading a request body into a Java object (`@RequestBody`) and writing a Java object to the response body (`@ResponseBody`). Selection is based on: the Java type being converted, and the `Content-Type` (for reading) or `Accept` header (for writing). `MappingJackson2HttpMessageConverter` handles `application/json`. `StringHttpMessageConverter` handles `text/plain`. If no converter can handle the type/media-type combination, Spring returns `406 Not Acceptable` (write) or `415 Unsupported Media Type` (read).","distractorsMd":["Content negotiation determines the response format based on: (1) `Accept` header in the request (`Accept: application/json`), (2) request parameter (`?format=json`, if configured), or (3) configured default content type.","Content negotiation is the process by which Spring MVC selects the response media type (e.g., `application/json`, `application/xml`) based on the client's `Accept` header and the handler's declared capabilities.","`DispatcherServlet` throws `NoHandlerFoundException` (if `throwExceptionIfNoHandlerFound=true`) or sends a 404 response directly."]},{"id":"spring/spring_mvc_architecture#f79be14ada7a","section":"spring","module":"spring/spring_mvc_architecture","moduleName":"spring mvc architecture","sourceFile":"README.md","difficulty":"intermediate","question":"What is content negotiation in Spring MVC?","answerFull":"Content negotiation determines the response format based on: (1) Accept header in the request (Accept: application/json), (2) request parameter (?format=json, if configured), or (3) configured default content type. ContentNegotiatingViewResolver selects the best view resolver for the negotiated type. For @ResponseBody, the appropriate HttpMessageConverter is selected. URL suffix negotiation (.json, .xml) was deprecated in Spring 5.3 and removed in Spring 6.","correct":"Content negotiation determines the response format based on: (1) Accept header in the request (Accept: application/json), (2) request parameter (?format=json, if configured), or (3) configured default content type.","distractors":["DispatcherServlet throws NoHandlerFoundException (if throwExceptionIfNoHandlerFound=true) or sends a 404 response directly.","Implement HandlerMethodArgumentResolver with supportsParameter(MethodParameter p) (return true for your custom type) and resolveArgument(...) (extract and return the argument from the request).","HandlerMapping resolves the request URL (and method, headers, params) to a handler — in most cases a HandlerMethod representing a @RequestMapping-annotated controller method."],"distractorIds":["spring/spring_mvc_architecture#864160c0cb46","spring/spring_mvc_architecture#e2167e7ffa54","spring/spring_mvc_architecture#2a3ef954baf8"],"concepts":["negotiation","mvc","accept","configured","determines","format"],"correctMd":"Content negotiation determines the response format based on: (1) `Accept` header in the request (`Accept: application/json`), (2) request parameter (`?format=json`, if configured), or (3) configured default content type.","answerFullMd":"Content negotiation determines the response format based on: (1) `Accept` header in the request (`Accept: application/json`), (2) request parameter (`?format=json`, if configured), or (3) configured default content type. `ContentNegotiatingViewResolver` selects the best view resolver for the negotiated type. For `@ResponseBody`, the appropriate `HttpMessageConverter` is selected. URL suffix negotiation (`.json`, `.xml`) was deprecated in Spring 5.3 and removed in Spring 6.","distractorsMd":["`DispatcherServlet` throws `NoHandlerFoundException` (if `throwExceptionIfNoHandlerFound=true`) or sends a 404 response directly.","Implement `HandlerMethodArgumentResolver` with `supportsParameter(MethodParameter p)` (return true for your custom type) and `resolveArgument(...)` (extract and return the argument from the request).","`HandlerMapping` resolves the request URL (and method, headers, params) to a handler — in most cases a `HandlerMethod` representing a `@RequestMapping`-annotated controller method."]},{"id":"spring/spring_mvc_architecture#e2167e7ffa54","section":"spring","module":"spring/spring_mvc_architecture","moduleName":"spring mvc architecture","sourceFile":"README.md","difficulty":"intermediate","question":"How do you add a custom HandlerMethodArgumentResolver?","answerFull":"Implement HandlerMethodArgumentResolver with supportsParameter(MethodParameter p) (return true for your custom type) and resolveArgument(...) (extract and return the argument from the request). Register via WebMvcConfigurer.addArgumentResolvers(). Example: a @CurrentUser annotation on a controller parameter that extracts the authenticated user from SecurityContextHolder. resolveArgument calls SecurityContextHolder.getContext().getAuthentication().getPrincipal() and casts it to your User type.","correct":"Implement HandlerMethodArgumentResolver with supportsParameter(MethodParameter p) (return true for your custom type) and resolveArgument(...) (extract and return the argument from the request).","distractors":["Content negotiation determines the response format based on: (1) Accept header in the request (Accept: application/json), (2) request parameter (?format=json, if configured), or (3) configured default content type.","HandlerMapping resolves the request URL (and method, headers, params) to a handler — in most cases a HandlerMethod representing a @RequestMapping-annotated controller method.","HttpMessageConverter<T> handles reading a request body into a Java object (@RequestBody) and writing a Java object to the response body (@ResponseBody)."],"distractorIds":["spring/spring_mvc_architecture#f79be14ada7a","spring/spring_mvc_architecture#2a3ef954baf8","spring/spring_mvc_architecture#ef51204da5ca"],"concepts":["methodparameter","handlermethodargumentresolver","resolveargument","supportsparameter","extract","argument"],"correctMd":"Implement `HandlerMethodArgumentResolver` with `supportsParameter(MethodParameter p)` (return true for your custom type) and `resolveArgument(...)` (extract and return the argument from the request).","answerFullMd":"Implement `HandlerMethodArgumentResolver` with `supportsParameter(MethodParameter p)` (return true for your custom type) and `resolveArgument(...)` (extract and return the argument from the request). Register via `WebMvcConfigurer.addArgumentResolvers()`. Example: a `@CurrentUser` annotation on a controller parameter that extracts the authenticated user from `SecurityContextHolder`. `resolveArgument` calls `SecurityContextHolder.getContext().getAuthentication().getPrincipal()` and casts it to your `User` type.","distractorsMd":["Content negotiation determines the response format based on: (1) `Accept` header in the request (`Accept: application/json`), (2) request parameter (`?format=json`, if configured), or (3) configured default content type.","`HandlerMapping` resolves the request URL (and method, headers, params) to a handler — in most cases a `HandlerMethod` representing a `@RequestMapping`-annotated controller method.","`HttpMessageConverter<T>` handles reading a request body into a Java object (`@RequestBody`) and writing a Java object to the response body (`@ResponseBody`)."]},{"id":"spring/spring_mvc_architecture#664d990918e6","section":"spring","module":"spring/spring_mvc_architecture","moduleName":"spring mvc architecture","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between DispatcherServlet and a Filter?","answerFull":"Filter (javax/jakarta Servlet API) runs before the DispatcherServlet in the servlet container's filter chain — it sees the raw HttpServletRequest/HttpServletResponse before Spring MVC touches them. DispatcherServlet is a servlet, subject to filter wrapping. Spring's HandlerInterceptor runs inside the DispatcherServlet pipeline after handler mapping but before/after handler execution. Implication: Spring Security (FilterChainProxy) is a filter and runs before DispatcherServlet — it can block requests before they reach any controller.","correct":"Filter (javax/jakarta Servlet API) runs before the DispatcherServlet in the servlet container's filter chain — it sees the raw HttpServletRequest/HttpServletResponse before Spring MVC touches them.","distractors":["Content negotiation is the process by which Spring MVC selects the response media type (e.g., application/json, application/xml) based on the client's Accept header and the handler's declared capabilities.","DispatcherServlet throws NoHandlerFoundException (if throwExceptionIfNoHandlerFound=true) or sends a 404 response directly.","DispatcherServlet is the Front Controller — a single servlet that handles all HTTP requests."],"distractorIds":["spring/spring_mvc_architecture#02819114985b","spring/spring_mvc_architecture#864160c0cb46","spring/spring_mvc_architecture#d441716926d4"],"concepts":["httpservletresponse","jakarta","httpservletrequest","javax","touches","dispatcherservlet"],"correctMd":"`Filter` (javax/jakarta Servlet API) runs before the `DispatcherServlet` in the servlet container's filter chain — it sees the raw `HttpServletRequest`/`HttpServletResponse` before Spring MVC touches them.","answerFullMd":"`Filter` (javax/jakarta Servlet API) runs before the `DispatcherServlet` in the servlet container's filter chain — it sees the raw `HttpServletRequest`/`HttpServletResponse` before Spring MVC touches them. `DispatcherServlet` is a servlet, subject to filter wrapping. Spring's `HandlerInterceptor` runs inside the `DispatcherServlet` pipeline after handler mapping but before/after handler execution. Implication: Spring Security (`FilterChainProxy`) is a filter and runs before `DispatcherServlet` — it can block requests before they reach any controller.","distractorsMd":["Content negotiation is the process by which Spring MVC selects the response media type (e.g., `application/json`, `application/xml`) based on the client's `Accept` header and the handler's declared capabilities.","`DispatcherServlet` throws `NoHandlerFoundException` (if `throwExceptionIfNoHandlerFound=true`) or sends a 404 response directly.","`DispatcherServlet` is the Front Controller — a single servlet that handles all HTTP requests."]},{"id":"spring/spring_mvc_architecture#864160c0cb46","section":"spring","module":"spring/spring_mvc_architecture","moduleName":"spring mvc architecture","sourceFile":"README.md","difficulty":"intermediate","question":"What happens when no HandlerMapping matches a request?","answerFull":"DispatcherServlet throws NoHandlerFoundException (if throwExceptionIfNoHandlerFound=true) or sends a 404 response directly. With the default DefaultServletHttpRequestHandler fallback enabled (Spring Boot default), unmapped requests are forwarded to the servlet container's default servlet for static resource serving. In REST APIs, @EnableWebMvc or spring.mvc.throw-exception-if-no-handler-found=true should be set to receive NoHandlerFoundException in @ControllerAdvice for proper 404 JSON responses.","correct":"DispatcherServlet throws NoHandlerFoundException (if throwExceptionIfNoHandlerFound=true) or sends a 404 response directly.","distractors":["@RestController is @Controller + @ResponseBody — every handler method's return value is serialized directly to the response body via HttpMessageConverter.","Content negotiation is the process by which Spring MVC selects the response media type (e.g., application/json, application/xml) based on the client's Accept header and the handler's declared capabilities.","HttpMessageConverter<T> handles reading a request body into a Java object (@RequestBody) and writing a Java object to the response body (@ResponseBody)."],"distractorIds":["spring/spring_mvc_architecture#ad7871d0ede5","spring/spring_mvc_architecture#02819114985b","spring/spring_mvc_architecture#ef51204da5ca"],"concepts":["handlermapping","nohandlerfoundexception","throwexceptionifnohandlerfound","404","dispatcherservlet","happens"],"correctMd":"`DispatcherServlet` throws `NoHandlerFoundException` (if `throwExceptionIfNoHandlerFound=true`) or sends a 404 response directly.","answerFullMd":"`DispatcherServlet` throws `NoHandlerFoundException` (if `throwExceptionIfNoHandlerFound=true`) or sends a 404 response directly. With the default `DefaultServletHttpRequestHandler` fallback enabled (Spring Boot default), unmapped requests are forwarded to the servlet container's default servlet for static resource serving. In REST APIs, `@EnableWebMvc` or `spring.mvc.throw-exception-if-no-handler-found=true` should be set to receive `NoHandlerFoundException` in `@ControllerAdvice` for proper 404 JSON responses.","distractorsMd":["`@RestController` is `@Controller` + `@ResponseBody` — every handler method's return value is serialized directly to the response body via `HttpMessageConverter`.","Content negotiation is the process by which Spring MVC selects the response media type (e.g., `application/json`, `application/xml`) based on the client's `Accept` header and the handler's declared capabilities.","`HttpMessageConverter<T>` handles reading a request body into a Java object (`@RequestBody`) and writing a Java object to the response body (`@ResponseBody`)."]},{"id":"spring/spring_mvc_architecture#be5970cd073e","section":"spring","module":"spring/spring_mvc_architecture","moduleName":"spring mvc architecture","sourceFile":"README.md","difficulty":"intermediate","question":"What is @EnableWebMvc and why should you NOT use it in Spring Boot?","answerFull":"@EnableWebMvc imports DelegatingWebMvcConfiguration which extends WebMvcConfigurationSupport. Spring Boot's WebMvcAutoConfiguration has @ConditionalOnMissingBean(WebMvcConfigurationSupport.class) — it backs off when WebMvcConfigurationSupport is detected. Using @EnableWebMvc in Spring Boot therefore disables ALL of Boot's MVC auto-configuration: Jackson customization, Actuator MVC endpoints, static resource serving, welcome page, content negotiation defaults. Instead, implement WebMvcConfigurer (without @EnableWebMvc) to extend Spring Boot's auto-configuration.","correct":"@EnableWebMvc imports DelegatingWebMvcConfiguration which extends WebMvcConfigurationSupport.","distractors":["Spring Boot auto-configures an embedded Tomcat with a thread pool (default max 200 threads via server.tomcat.threads.max).","Filter (javax/jakarta Servlet API) runs before the DispatcherServlet in the servlet container's filter chain — it sees the raw HttpServletRequest/HttpServletResponse before Spring MVC touches them.","Content negotiation is the process by which Spring MVC selects the response media type (e.g., application/json, application/xml) based on the client's Accept header and the handler's declared capabilities."],"distractorIds":["spring/spring_mvc_architecture#29ac593eb6d3","spring/spring_mvc_architecture#664d990918e6","spring/spring_mvc_architecture#02819114985b"],"concepts":["delegatingwebmvcconfiguration","enablewebmvc","webmvcconfigurationsupport","imports","extends","boot"],"correctMd":"`@EnableWebMvc` imports `DelegatingWebMvcConfiguration` which extends `WebMvcConfigurationSupport`.","answerFullMd":"`@EnableWebMvc` imports `DelegatingWebMvcConfiguration` which extends `WebMvcConfigurationSupport`. Spring Boot's `WebMvcAutoConfiguration` has `@ConditionalOnMissingBean(WebMvcConfigurationSupport.class)` — it backs off when `WebMvcConfigurationSupport` is detected. Using `@EnableWebMvc` in Spring Boot therefore disables ALL of Boot's MVC auto-configuration: Jackson customization, Actuator MVC endpoints, static resource serving, welcome page, content negotiation defaults. Instead, implement `WebMvcConfigurer` (without `@EnableWebMvc`) to extend Spring Boot's auto-configuration.","distractorsMd":["Spring Boot auto-configures an embedded Tomcat with a thread pool (default max 200 threads via `server.tomcat.threads.max`).","`Filter` (javax/jakarta Servlet API) runs before the `DispatcherServlet` in the servlet container's filter chain — it sees the raw `HttpServletRequest`/`HttpServletResponse` before Spring MVC touches them.","Content negotiation is the process by which Spring MVC selects the response media type (e.g., `application/json`, `application/xml`) based on the client's `Accept` header and the handler's declared capabilities."]},{"id":"spring/spring_mvc_architecture#ebdcc867a1e6","section":"spring","module":"spring/spring_mvc_architecture","moduleName":"spring mvc architecture","sourceFile":"README.md","difficulty":"advanced","question":"How does multipart file upload work in Spring MVC?","answerFull":"Multipart support is handled by MultipartResolver. Spring Boot auto-configures StandardServletMultipartResolver (delegates to the servlet container, supports Servlet 3.0 API). When a request has Content-Type: multipart/form-data, the resolver parses the request into parts. RequestMappingHandlerAdapter resolves @RequestParam MultipartFile file parameters using RequestPartMethodArgumentResolver. Size limits are configured via spring.servlet.multipart.max-file-size and spring.servlet.multipart.max-request-size. Exceeding limits throws MaxUploadSizeExceededException, catchable in @ControllerAdvice.","correct":"Multipart support is handled by MultipartResolver.","distractors":["Content negotiation is the process by which Spring MVC selects the response media type (e.g., application/json, application/xml) based on the client's Accept header and the handler's declared capabilities.","Filter (javax/jakarta Servlet API) runs before the DispatcherServlet in the servlet container's filter chain — it sees the raw HttpServletRequest/HttpServletResponse before Spring MVC touches them.","Spring Boot auto-configures an embedded Tomcat with a thread pool (default max 200 threads via server.tomcat.threads.max)."],"distractorIds":["spring/spring_mvc_architecture#02819114985b","spring/spring_mvc_architecture#664d990918e6","spring/spring_mvc_architecture#29ac593eb6d3"],"concepts":["multipartresolver","upload","multipart","handled","mvc","support"],"correctMd":"Multipart support is handled by `MultipartResolver`.","answerFullMd":"Multipart support is handled by `MultipartResolver`. Spring Boot auto-configures `StandardServletMultipartResolver` (delegates to the servlet container, supports Servlet 3.0 API). When a request has `Content-Type: multipart/form-data`, the resolver parses the request into parts. `RequestMappingHandlerAdapter` resolves `@RequestParam MultipartFile file` parameters using `RequestPartMethodArgumentResolver`. Size limits are configured via `spring.servlet.multipart.max-file-size` and `spring.servlet.multipart.max-request-size`. Exceeding limits throws `MaxUploadSizeExceededException`, catchable in `@ControllerAdvice`.","distractorsMd":["Content negotiation is the process by which Spring MVC selects the response media type (e.g., `application/json`, `application/xml`) based on the client's `Accept` header and the handler's declared capabilities.","`Filter` (javax/jakarta Servlet API) runs before the `DispatcherServlet` in the servlet container's filter chain — it sees the raw `HttpServletRequest`/`HttpServletResponse` before Spring MVC touches them.","Spring Boot auto-configures an embedded Tomcat with a thread pool (default max 200 threads via `server.tomcat.threads.max`)."]},{"id":"spring/spring_mvc_architecture#29ac593eb6d3","section":"spring","module":"spring/spring_mvc_architecture","moduleName":"spring mvc architecture","sourceFile":"README.md","difficulty":"advanced","question":"How does Spring MVC integrate with Tomcat's thread pool?","answerFull":"Spring Boot auto-configures an embedded Tomcat with a thread pool (default max 200 threads via server.tomcat.threads.max). Each HTTP request acquires a thread for its entire duration. Blocking database calls, external API calls, and file I/O occupy the thread while waiting. When all 200 threads are busy, new requests queue (Tomcat's acceptCount, default 100) or are rejected (connection refused). This is the fundamental scalability limit of the thread-per-request model. WebFlux with Netty uses 2×CPU-core event loop threads and handles thousands of concurrent I/O-bound requests.","correct":"Spring Boot auto-configures an embedded Tomcat with a thread pool (default max 200 threads via server.tomcat.threads.max).","distractors":["Content negotiation determines the response format based on: (1) Accept header in the request (Accept: application/json), (2) request parameter (?format=json, if configured), or (3) configured default content type.","Filter (javax/jakarta Servlet API) runs before the DispatcherServlet in the servlet container's filter chain — it sees the raw HttpServletRequest/HttpServletResponse before Spring MVC touches them.","Content negotiation is the process by which Spring MVC selects the response media type (e.g., application/json, application/xml) based on the client's Accept header and the handler's declared capabilities."],"distractorIds":["spring/spring_mvc_architecture#f79be14ada7a","spring/spring_mvc_architecture#664d990918e6","spring/spring_mvc_architecture#02819114985b"],"concepts":["integrate","tomcat","configures","mvc","boot","embedded"],"correctMd":"Spring Boot auto-configures an embedded Tomcat with a thread pool (default max 200 threads via `server.tomcat.threads.max`).","answerFullMd":"Spring Boot auto-configures an embedded Tomcat with a thread pool (default max 200 threads via `server.tomcat.threads.max`). Each HTTP request acquires a thread for its entire duration. Blocking database calls, external API calls, and file I/O occupy the thread while waiting. When all 200 threads are busy, new requests queue (Tomcat's `acceptCount`, default 100) or are rejected (connection refused). This is the fundamental scalability limit of the thread-per-request model. WebFlux with Netty uses 2×CPU-core event loop threads and handles thousands of concurrent I/O-bound requests.","distractorsMd":["Content negotiation determines the response format based on: (1) `Accept` header in the request (`Accept: application/json`), (2) request parameter (`?format=json`, if configured), or (3) configured default content type.","`Filter` (javax/jakarta Servlet API) runs before the `DispatcherServlet` in the servlet container's filter chain — it sees the raw `HttpServletRequest`/`HttpServletResponse` before Spring MVC touches them.","Content negotiation is the process by which Spring MVC selects the response media type (e.g., `application/json`, `application/xml`) based on the client's `Accept` header and the handler's declared capabilities."]},{"id":"spring/spring_mvc_architecture#ad7871d0ede5","section":"spring","module":"spring/spring_mvc_architecture","moduleName":"spring mvc architecture","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between @Controller and @RestController?","answerFull":"@RestController is @Controller + @ResponseBody — every handler method's return value is serialized directly to the response body via HttpMessageConverter. @Controller is for MVC controllers that return view names (String) or ModelAndView objects resolved by ViewResolver. In REST API development, always use @RestController. Use @Controller only when rendering server-side templates (Thymeleaf, FreeMarker, JSP).","correct":"@RestController is @Controller + @ResponseBody — every handler method's return value is serialized directly to the response body via HttpMessageConverter.","distractors":["Content negotiation is the process by which Spring MVC selects the response media type (e.g., application/json, application/xml) based on the client's Accept header and the handler's declared capabilities.","HandlerMapping resolves the request URL (and method, headers, params) to a handler — in most cases a HandlerMethod representing a @RequestMapping-annotated controller method.","Implement HandlerMethodArgumentResolver with supportsParameter(MethodParameter p) (return true for your custom type) and resolveArgument(...) (extract and return the argument from the request)."],"distractorIds":["spring/spring_mvc_architecture#02819114985b","spring/spring_mvc_architecture#2a3ef954baf8","spring/spring_mvc_architecture#e2167e7ffa54"],"concepts":["responsebody","restcontroller","httpmessageconverter","serialized","difference","body"],"correctMd":"`@RestController` is `@Controller` + `@ResponseBody` — every handler method's return value is serialized directly to the response body via `HttpMessageConverter`.","answerFullMd":"`@RestController` is `@Controller` + `@ResponseBody` — every handler method's return value is serialized directly to the response body via `HttpMessageConverter`. `@Controller` is for MVC controllers that return view names (String) or `ModelAndView` objects resolved by `ViewResolver`. In REST API development, always use `@RestController`. Use `@Controller` only when rendering server-side templates (Thymeleaf, FreeMarker, JSP).","distractorsMd":["Content negotiation is the process by which Spring MVC selects the response media type (e.g., `application/json`, `application/xml`) based on the client's `Accept` header and the handler's declared capabilities.","`HandlerMapping` resolves the request URL (and method, headers, params) to a handler — in most cases a `HandlerMethod` representing a `@RequestMapping`-annotated controller method.","Implement `HandlerMethodArgumentResolver` with `supportsParameter(MethodParameter p)` (return true for your custom type) and `resolveArgument(...)` (extract and return the argument from the request)."]},{"id":"spring/spring_mvc_architecture#02819114985b","section":"spring","module":"spring/spring_mvc_architecture","moduleName":"spring mvc architecture","sourceFile":"README.md","difficulty":"advanced","question":"What is content negotiation in Spring MVC and how does produces on @RequestMapping control it?","answerFull":"Content negotiation is the process by which Spring MVC selects the response media type (e.g., application/json, application/xml) based on the client's Accept header and the handler's declared capabilities. produces = \"application/json\" on @GetMapping narrows the handler to only handle requests that accept JSON — if the client sends Accept: application/xml, this mapping is skipped and Spring looks for another handler. If no handler matches, Spring returns 406 Not Acceptable. Content negotiation order: Accept header → path extension (deprecated) → query parameter (?format=json, disabled by default). In REST APIs, always declare produces = MediaType.APPLICATION_JSON_VALUE on all handlers for clear documentation and to prevent accidental content type negotiation fall-through.","correct":"Content negotiation is the process by which Spring MVC selects the response media type (e.g., application/json, application/xml) based on the client's Accept header and the handler's declared capabilities.","distractors":["@RestController is @Controller + @ResponseBody — every handler method's return value is serialized directly to the response body via HttpMessageConverter.","DispatcherServlet throws NoHandlerFoundException (if throwExceptionIfNoHandlerFound=true) or sends a 404 response directly.","Content negotiation determines the response format based on: (1) Accept header in the request (Accept: application/json), (2) request parameter (?format=json, if configured), or (3) configured default content type."],"distractorIds":["spring/spring_mvc_architecture#ad7871d0ede5","spring/spring_mvc_architecture#864160c0cb46","spring/spring_mvc_architecture#f79be14ada7a"],"concepts":["media","requestmapping","negotiation","xml","mvc","capabilities"],"questionMd":"What is content negotiation in Spring MVC and how does `produces` on `@RequestMapping` control it?","correctMd":"Content negotiation is the process by which Spring MVC selects the response media type (e.g., `application/json`, `application/xml`) based on the client's `Accept` header and the handler's declared capabilities.","answerFullMd":"Content negotiation is the process by which Spring MVC selects the response media type (e.g., `application/json`, `application/xml`) based on the client's `Accept` header and the handler's declared capabilities. `produces = \"application/json\"` on `@GetMapping` narrows the handler to only handle requests that accept JSON — if the client sends `Accept: application/xml`, this mapping is skipped and Spring looks for another handler. If no handler matches, Spring returns `406 Not Acceptable`. Content negotiation order: `Accept` header → path extension (deprecated) → query parameter (`?format=json`, disabled by default). In REST APIs, always declare `produces = MediaType.APPLICATION_JSON_VALUE` on all handlers for clear documentation and to prevent accidental content type negotiation fall-through.","distractorsMd":["`@RestController` is `@Controller` + `@ResponseBody` — every handler method's return value is serialized directly to the response body via `HttpMessageConverter`.","`DispatcherServlet` throws `NoHandlerFoundException` (if `throwExceptionIfNoHandlerFound=true`) or sends a 404 response directly.","Content negotiation determines the response format based on: (1) `Accept` header in the request (`Accept: application/json`), (2) request parameter (`?format=json`, if configured), or (3) configured default content type."]},{"id":"spring/spring_native_graalvm#45ae944f0499","section":"spring","module":"spring/spring_native_graalvm","moduleName":"spring native graalvm","sourceFile":"README.md","difficulty":"core","question":"Why can't GraalVM native image just use reflection like the JVM does?","answerFull":"Native image uses a closed-world assumption: it statically analyzes everything reachable from main at build time and discards the rest to produce a small, fast executable. Reflection (and dynamic proxies, JNI, runtime resource loading) reaches code the analyzer cannot follow, so that code is dropped unless you explicitly declare it via reachability metadata (hints). The JVM, by contrast, loads and links classes lazily at runtime, so reflection just works. Native trades that runtime dynamism for startup speed and small footprint.","correct":"Native image uses a closed-world assumption: it statically analyzes everything reachable from main at build time and discards the rest to produce a small, fast executable.","distractors":["Native compilation runs whole-program static analysis (points-to analysis across the entire reachable graph) and then AOT-compiles everything, which is far heavier than javac and memory-hungry.","At build time it processes the ApplicationContext — resolving bean definitions, configuration classes, and proxies — and emits generated Java code plus GraalVM hint files (reflect-config.json, etc.).","Quarkus and Micronaut were designed native-first: they do dependency injection and metadata generation at compile time and avoid runtime reflection by architecture, so they need fewer hints and build leaner."],"distractorIds":["spring/spring_native_graalvm#d340288c794d","spring/spring_native_graalvm#5dfdc970e381","spring/spring_native_graalvm#91117c389182"],"concepts":["statically","graalvm","analyzes","discards","executable","reachable"],"correctMd":"Native image uses a closed-world assumption: it statically analyzes everything reachable from `main` at build time and discards the rest to produce a small, fast executable.","answerFullMd":"Native image uses a closed-world assumption: it statically analyzes everything reachable from `main` at build time and discards the rest to produce a small, fast executable. Reflection (and dynamic proxies, JNI, runtime resource loading) reaches code the analyzer cannot follow, so that code is dropped unless you explicitly declare it via reachability metadata (hints). The JVM, by contrast, loads and links classes lazily at runtime, so reflection just works. Native trades that runtime dynamism for startup speed and small footprint.","distractorsMd":[null,"At build time it processes the `ApplicationContext` — resolving bean definitions, configuration classes, and proxies — and emits generated Java code plus GraalVM hint files (`reflect-config.json`, etc.).",null]},{"id":"spring/spring_native_graalvm#5dfdc970e381","section":"spring","module":"spring/spring_native_graalvm","moduleName":"spring native graalvm","sourceFile":"README.md","difficulty":"core","question":"What does Spring Boot's AOT engine actually do?","answerFull":"At build time it processes the ApplicationContext — resolving bean definitions, configuration classes, and proxies — and emits generated Java code plus GraalVM hint files (reflect-config.json, etc.). At runtime the context replays this generated code instead of scanning the classpath and reading annotations reflectively. So AOT moves the expensive context-building work from startup to build time, which is what collapses startup from seconds to tens of milliseconds, and it auto-generates most of the metadata GraalVM needs.","correct":"At build time it processes the ApplicationContext — resolving bean definitions, configuration classes, and proxies — and emits generated Java code plus GraalVM hint files (reflect-config.json, etc.).","distractors":["Spring normally creates CGLIB/JDK dynamic proxies at runtime; under native that is impossible (no runtime bytecode generation for CGLIB and proxies must be known up front).","Quarkus and Micronaut were designed native-first: they do dependency injection and metadata generation at compile time and avoid runtime reflection by architecture, so they need fewer hints and build leaner.","Native image uses a closed-world assumption: it statically analyzes everything reachable from main at build time and discards the rest to produce a small, fast executable."],"distractorIds":["spring/spring_native_graalvm#876be7508bf3","spring/spring_native_graalvm#91117c389182","spring/spring_native_graalvm#45ae944f0499"],"concepts":["hint","aot","resolving","graalvm","definitions","reflect"],"correctMd":"At build time it processes the `ApplicationContext` — resolving bean definitions, configuration classes, and proxies — and emits generated Java code plus GraalVM hint files (`reflect-config.json`, etc.).","answerFullMd":"At build time it processes the `ApplicationContext` — resolving bean definitions, configuration classes, and proxies — and emits generated Java code plus GraalVM hint files (`reflect-config.json`, etc.). At runtime the context replays this generated code instead of scanning the classpath and reading annotations reflectively. So AOT moves the expensive context-building work from startup to build time, which is what collapses startup from seconds to tens of milliseconds, and it auto-generates most of the metadata GraalVM needs.","distractorsMd":[null,null,"Native image uses a closed-world assumption: it statically analyzes everything reachable from `main` at build time and discards the rest to produce a small, fast executable."]},{"id":"spring/spring_native_graalvm#f02f20876e42","section":"spring","module":"spring/spring_native_graalvm","moduleName":"spring native graalvm","sourceFile":"README.md","difficulty":"core","question":"What is reachability metadata and what are the kinds of hints?","answerFull":"Reachability metadata tells GraalVM about dynamic behavior its static analysis cannot see, so the affected code is retained and configured in the image. The main kinds are reflection hints (classes/methods/fields accessed reflectively), resource hints (files/patterns loaded at runtime), proxy hints (interfaces for JDK dynamic proxies), and serialization hints. Spring auto-generates most from its annotations; you add the rest via RuntimeHintsRegistrar/@ImportRuntimeHints or @RegisterReflectionForBinding.","correct":"Reachability metadata tells GraalVM about dynamic behavior its static analysis cannot see, so the affected code is retained and configured in the image.","distractors":["It is a JVM agent (native-image-agent) that observes a normal JVM run and records all reflective, resource, proxy, and serialization access, writing the corresponding metadata JSON files.","Native compilation runs whole-program static analysis (points-to analysis across the entire reachable graph) and then AOT-compiles everything, which is far heavier than javac and memory-hungry.","Build-time initialization runs a class's static initializer during the native build and bakes the resulting state into the image heap, giving faster startup."],"distractorIds":["spring/spring_native_graalvm#56e35184f275","spring/spring_native_graalvm#d340288c794d","spring/spring_native_graalvm#e0e6f827ecd5"],"concepts":["kinds","graalvm","reachability","retained","affected","hints"],"answerFullMd":"Reachability metadata tells GraalVM about dynamic behavior its static analysis cannot see, so the affected code is retained and configured in the image. The main kinds are reflection hints (classes/methods/fields accessed reflectively), resource hints (files/patterns loaded at runtime), proxy hints (interfaces for JDK dynamic proxies), and serialization hints. Spring auto-generates most from its annotations; you add the rest via `RuntimeHintsRegistrar`/`@ImportRuntimeHints` or `@RegisterReflectionForBinding`.","distractorsMd":["It is a JVM agent (`native-image-agent`) that observes a normal JVM run and records all reflective, resource, proxy, and serialization access, writing the corresponding metadata JSON files.",null,null]},{"id":"spring/spring_native_graalvm#e0e6f827ecd5","section":"spring","module":"spring/spring_native_graalvm","moduleName":"spring native graalvm","sourceFile":"README.md","difficulty":"core","question":"What is the difference between build-time and runtime initialization?","answerFull":"Build-time initialization runs a class's static initializer during the native build and bakes the resulting state into the image heap, giving faster startup. Runtime initialization defers it to when the executable starts. The danger is initializing at build time something that depends on the runtime environment — a random seed, the current time, an open file/socket — which then freezes a build-time value into every deployment. Such classes must be marked --initialize-at-run-time.","correct":"Build-time initialization runs a class's static initializer during the native build and bakes the resulting state into the image heap, giving faster startup.","distractors":["Native compilation runs whole-program static analysis (points-to analysis across the entire reachable graph) and then AOT-compiles everything, which is far heavier than javac and memory-hungry.","They are evaluated at build time, not runtime, because the set of beans is frozen into the image.","Quarkus and Micronaut were designed native-first: they do dependency injection and metadata generation at compile time and avoid runtime reflection by architecture, so they need fewer hints and build leaner."],"distractorIds":["spring/spring_native_graalvm#d340288c794d","spring/spring_native_graalvm#fe6b7a991e4f","spring/spring_native_graalvm#91117c389182"],"concepts":["bakes","initializer","resulting","difference","initialization","giving"],"answerFullMd":"Build-time initialization runs a class's static initializer during the native build and bakes the resulting state into the image heap, giving faster startup. Runtime initialization defers it to when the executable starts. The danger is initializing at build time something that depends on the runtime environment — a random seed, the current time, an open file/socket — which then freezes a build-time value into every deployment. Such classes must be marked `--initialize-at-run-time`."},{"id":"spring/spring_native_graalvm#ac200a9f87ea","section":"spring","module":"spring/spring_native_graalvm","moduleName":"spring native graalvm","sourceFile":"README.md","difficulty":"core","question":"Why is peak throughput often lower for a native image than a warmed-up JVM?","answerFull":"A native image has no runtime JIT compiler, so it cannot profile hot paths and re-optimize them with aggressive inlining and speculative optimizations the way HotSpot does after warmup. AOT compiles ahead of time with less runtime profile information, so steady-state throughput is typically lower (often ~70-85% of a warmed JVM). Profile-Guided Optimization narrows the gap by feeding a profiling run back into the build, but native's strength is startup and memory, not peak throughput.","correct":"A native image has no runtime JIT compiler, so it cannot profile hot paths and re-optimize them with aggressive inlining and speculative optimizations the way HotSpot does after warmup.","distractors":["It is a JVM agent (native-image-agent) that observes a normal JVM run and records all reflective, resource, proxy, and serialization access, writing the corresponding metadata JSON files.","Spring normally creates CGLIB/JDK dynamic proxies at runtime; under native that is impossible (no runtime bytecode generation for CGLIB and proxies must be known up front).","Build-time initialization runs a class's static initializer during the native build and bakes the resulting state into the image heap, giving faster startup."],"distractorIds":["spring/spring_native_graalvm#56e35184f275","spring/spring_native_graalvm#876be7508bf3","spring/spring_native_graalvm#e0e6f827ecd5"],"concepts":["inlining","speculative","warmed","optimizations","aggressive","hotspot"],"distractorsMd":["It is a JVM agent (`native-image-agent`) that observes a normal JVM run and records all reflective, resource, proxy, and serialization access, writing the corresponding metadata JSON files.",null,null]},{"id":"spring/spring_native_graalvm#aa8d034f6312","section":"spring","module":"spring/spring_native_graalvm","moduleName":"spring native graalvm","sourceFile":"README.md","difficulty":"intermediate","question":"When would you choose native over the JVM, and when not?","answerFull":"Choose native when startup latency and memory dominate cost or SLA — serverless/FaaS, scale-to-zero, high-density containers, CLIs, short-lived jobs. Avoid it for long-lived, throughput-bound services where a warm JIT wins, for apps that depend heavily on runtime reflection/agents that are hard to hint, and where slow builds or less-mature debugging tooling would hurt. The slogan: native optimizes the first second of the process; the JVM optimizes the millionth request.","correct":"Choose native when startup latency and memory dominate cost or SLA — serverless/FaaS, scale-to-zero, high-density containers, CLIs, short-lived jobs.","distractors":["Not freely — code that relies on unbounded runtime reflection, dynamic class generation, or JVM agents is hostile to the closed world and will need extensive hints or refactoring.","Native image uses a closed-world assumption: it statically analyzes everything reachable from main at build time and discards the rest to produce a small, fast executable.","Spring normally creates CGLIB/JDK dynamic proxies at runtime; under native that is impossible (no runtime bytecode generation for CGLIB and proxies must be known up front)."],"distractorIds":["spring/spring_native_graalvm#6638c360903c","spring/spring_native_graalvm#45ae944f0499","spring/spring_native_graalvm#876be7508bf3"],"concepts":["clis","faas","sla","serverless","density","jobs"],"distractorsMd":[null,"Native image uses a closed-world assumption: it statically analyzes everything reachable from `main` at build time and discards the rest to produce a small, fast executable.",null]},{"id":"spring/spring_native_graalvm#703be8733f63","section":"spring","module":"spring/spring_native_graalvm","moduleName":"spring native graalvm","sourceFile":"README.md","difficulty":"intermediate","question":"How do you discover the hints your application needs?","answerFull":"First rely on Spring AOT (covers Spring's own annotations) and the GraalVM Reachability Metadata Repository (covers many popular libraries). For your own reflective code, run your application/tests on the JVM under the GraalVM tracing agent, which records reflection/resource/proxy access and writes the metadata files. Crucially, the agent only captures paths your tests execute, so you must exercise all code paths and back it up with native tests — uncovered paths produce no hints and fail at runtime.","correct":"First rely on Spring AOT (covers Spring's own annotations) and the GraalVM Reachability Metadata Repository (covers many popular libraries).","distractors":["It is a JVM agent (native-image-agent) that observes a normal JVM run and records all reflective, resource, proxy, and serialization access, writing the corresponding metadata JSON files.","Quarkus and Micronaut were designed native-first: they do dependency injection and metadata generation at compile time and avoid runtime reflection by architecture, so they need fewer hints and build leaner.","Reachability metadata tells GraalVM about dynamic behavior its static analysis cannot see, so the affected code is retained and configured in the image."],"distractorIds":["spring/spring_native_graalvm#56e35184f275","spring/spring_native_graalvm#91117c389182","spring/spring_native_graalvm#f02f20876e42"],"concepts":["aot","discover","graalvm","reachability","libraries","covers"],"distractorsMd":["It is a JVM agent (`native-image-agent`) that observes a normal JVM run and records all reflective, resource, proxy, and serialization access, writing the corresponding metadata JSON files.",null,null]},{"id":"spring/spring_native_graalvm#b10ee9cceaa0","section":"spring","module":"spring/spring_native_graalvm","moduleName":"spring native graalvm","sourceFile":"README.md","difficulty":"intermediate","question":"Why might a native app build successfully but crash at runtime?","answerFull":"Because missing reachability metadata is not a build error — the build simply omits the un-referenced (reflective) code. The failure surfaces only when execution first reaches a reflective/resource path that was never hinted, throwing ClassNotFoundException, NoSuchMethodException, or a missing-resource error on that specific request. This is why native tests and full-coverage tracing-agent runs matter: the goal is to fail at build/test time, not on a production request.","correct":"Because missing reachability metadata is not a build error — the build simply omits the un-referenced (reflective) code.","distractors":["Build-time initialization runs a class's static initializer during the native build and bakes the resulting state into the image heap, giving faster startup.","A native image has no runtime JIT compiler, so it cannot profile hot paths and re-optimize them with aggressive inlining and speculative optimizations the way HotSpot does after warmup.","Spring normally creates CGLIB/JDK dynamic proxies at runtime; under native that is impossible (no runtime bytecode generation for CGLIB and proxies must be known up front)."],"distractorIds":["spring/spring_native_graalvm#e0e6f827ecd5","spring/spring_native_graalvm#ac200a9f87ea","spring/spring_native_graalvm#876be7508bf3"],"concepts":["omits","reflective","reachability","successfully","referenced","simply"],"answerFullMd":"Because missing reachability metadata is not a build error — the build simply omits the un-referenced (reflective) code. The failure surfaces only when execution first reaches a reflective/resource path that was never hinted, throwing `ClassNotFoundException`, `NoSuchMethodException`, or a missing-resource error on that specific request. This is why native tests and full-coverage tracing-agent runs matter: the goal is to fail at build/test time, not on a production request."},{"id":"spring/spring_native_graalvm#876be7508bf3","section":"spring","module":"spring/spring_native_graalvm","moduleName":"spring native graalvm","sourceFile":"README.md","difficulty":"intermediate","question":"How are @Transactional and other proxies handled in native image?","answerFull":"Spring normally creates CGLIB/JDK dynamic proxies at runtime; under native that is impossible (no runtime bytecode generation for CGLIB and proxies must be known up front). The AOT engine generates the required proxy classes and proxy hints at build time, so the proxy exists in the image. For JDK dynamic proxies you may need to register the interface set via a proxy hint if it is not auto-detected. The behavior is the same; only the *when* of proxy creation moves to build time.","correct":"Spring normally creates CGLIB/JDK dynamic proxies at runtime; under native that is impossible (no runtime bytecode generation for CGLIB and proxies must be known up front).","distractors":["Not freely — code that relies on unbounded runtime reflection, dynamic class generation, or JVM agents is hostile to the closed world and will need extensive hints or refactoring.","They are evaluated at build time, not runtime, because the set of beans is frozen into the image.","Quarkus and Micronaut were designed native-first: they do dependency injection and metadata generation at compile time and avoid runtime reflection by architecture, so they need fewer hints and build leaner."],"distractorIds":["spring/spring_native_graalvm#6638c360903c","spring/spring_native_graalvm#fe6b7a991e4f","spring/spring_native_graalvm#91117c389182"],"concepts":["impossible","handled","jdk","normally","cglib","front"],"questionMd":"How are `@Transactional` and other proxies handled in native image?"},{"id":"spring/spring_native_graalvm#fe6b7a991e4f","section":"spring","module":"spring/spring_native_graalvm","moduleName":"spring native graalvm","sourceFile":"README.md","difficulty":"intermediate","question":"What happens to @ConditionalOnClass and profiles in a native image?","answerFull":"They are evaluated at build time, not runtime, because the set of beans is frozen into the image. So a conditional that depends on a class being present, or a profile-specific bean graph, is decided when the image is built — flipping a profile at runtime will not re-wire the context. If you need different wiring per environment, decide it at build time or produce separate per-profile images.","correct":"They are evaluated at build time, not runtime, because the set of beans is frozen into the image.","distractors":["It is a JVM agent (native-image-agent) that observes a normal JVM run and records all reflective, resource, proxy, and serialization access, writing the corresponding metadata JSON files.","Choose native when startup latency and memory dominate cost or SLA — serverless/FaaS, scale-to-zero, high-density containers, CLIs, short-lived jobs.","At build time it processes the ApplicationContext — resolving bean definitions, configuration classes, and proxies — and emits generated Java code plus GraalVM hint files (reflect-config.json, etc.)."],"distractorIds":["spring/spring_native_graalvm#56e35184f275","spring/spring_native_graalvm#aa8d034f6312","spring/spring_native_graalvm#5dfdc970e381"],"concepts":["conditionalonclass","profiles","evaluated","frozen","happens","beans"],"questionMd":"What happens to `@ConditionalOnClass` and profiles in a native image?","distractorsMd":["It is a JVM agent (`native-image-agent`) that observes a normal JVM run and records all reflective, resource, proxy, and serialization access, writing the corresponding metadata JSON files.",null,"At build time it processes the `ApplicationContext` — resolving bean definitions, configuration classes, and proxies — and emits generated Java code plus GraalVM hint files (`reflect-config.json`, etc.)."]},{"id":"spring/spring_native_graalvm#56e35184f275","section":"spring","module":"spring/spring_native_graalvm","moduleName":"spring native graalvm","sourceFile":"README.md","difficulty":"advanced","question":"What is the GraalVM tracing agent and its main limitation?","answerFull":"It is a JVM agent (native-image-agent) that observes a normal JVM run and records all reflective, resource, proxy, and serialization access, writing the corresponding metadata JSON files. Its main limitation is coverage: it only records code paths that actually execute during the run, so any branch your tests miss yields no metadata and breaks in the native image. You therefore run it under the full test suite and treat its output as a starting point, not a guarantee.","correct":"It is a JVM agent (native-image-agent) that observes a normal JVM run and records all reflective, resource, proxy, and serialization access, writing the corresponding metadata JSON files.","distractors":["Because missing reachability metadata is not a build error — the build simply omits the un-referenced (reflective) code.","First rely on Spring AOT (covers Spring's own annotations) and the GraalVM Reachability Metadata Repository (covers many popular libraries).","Build-time initialization runs a class's static initializer during the native build and bakes the resulting state into the image heap, giving faster startup."],"distractorIds":["spring/spring_native_graalvm#b10ee9cceaa0","spring/spring_native_graalvm#703be8733f63","spring/spring_native_graalvm#e0e6f827ecd5"],"concepts":["reflective","graalvm","observes","corresponding","normal","writing"],"correctMd":"It is a JVM agent (`native-image-agent`) that observes a normal JVM run and records all reflective, resource, proxy, and serialization access, writing the corresponding metadata JSON files.","answerFullMd":"It is a JVM agent (`native-image-agent`) that observes a normal JVM run and records all reflective, resource, proxy, and serialization access, writing the corresponding metadata JSON files. Its main limitation is coverage: it only records code paths that actually execute during the run, so any branch your tests miss yields no metadata and breaks in the native image. You therefore run it under the full test suite and treat its output as a starting point, not a guarantee."},{"id":"spring/spring_native_graalvm#081889b04f9e","section":"spring","module":"spring/spring_native_graalvm","moduleName":"spring native graalvm","sourceFile":"README.md","difficulty":"advanced","question":"How does native image reduce memory footprint so much?","answerFull":"There is no JVM: no separate interpreter/JIT/metaspace overhead, no large reusable runtime, and the closed-world cut removes unreachable classes so only what you use is included. The application's initialized heap can also be partly built at image-build time. The result is an RSS measured in tens of MB versus hundreds for an equivalent JVM process, which is what enables high pod density.","correct":"There is no JVM: no separate interpreter/JIT/metaspace overhead, no large reusable runtime, and the closed-world cut removes unreachable classes so only what you use is included.","distractors":["It is a JVM agent (native-image-agent) that observes a normal JVM run and records all reflective, resource, proxy, and serialization access, writing the corresponding metadata JSON files.","A native image has no runtime JIT compiler, so it cannot profile hot paths and re-optimize them with aggressive inlining and speculative optimizations the way HotSpot does after warmup.","Quarkus and Micronaut were designed native-first: they do dependency injection and metadata generation at compile time and avoid runtime reflection by architecture, so they need fewer hints and build leaner."],"distractorIds":["spring/spring_native_graalvm#56e35184f275","spring/spring_native_graalvm#ac200a9f87ea","spring/spring_native_graalvm#91117c389182"],"concepts":["metaspace","unreachable","cut","footprint","included","interpreter"],"distractorsMd":["It is a JVM agent (`native-image-agent`) that observes a normal JVM run and records all reflective, resource, proxy, and serialization access, writing the corresponding metadata JSON files.",null,null]},{"id":"spring/spring_native_graalvm#d340288c794d","section":"spring","module":"spring/spring_native_graalvm","moduleName":"spring native graalvm","sourceFile":"README.md","difficulty":"advanced","question":"Why does the build take so long and what can you do about it?","answerFull":"Native compilation runs whole-program static analysis (points-to analysis across the entire reachable graph) and then AOT-compiles everything, which is far heavier than javac and memory-hungry. Mitigations: dedicate a native build stage in CI with ample RAM, cache aggressively, use buildpacks for reproducible builds, and only build native images for the artifacts that need them rather than every commit/branch.","correct":"Native compilation runs whole-program static analysis (points-to analysis across the entire reachable graph) and then AOT-compiles everything, which is far heavier than javac and memory-hungry.","distractors":["A native image has no runtime JIT compiler, so it cannot profile hot paths and re-optimize them with aggressive inlining and speculative optimizations the way HotSpot does after warmup.","Build-time initialization runs a class's static initializer during the native build and bakes the resulting state into the image heap, giving faster startup.","Quarkus and Micronaut were designed native-first: they do dependency injection and metadata generation at compile time and avoid runtime reflection by architecture, so they need fewer hints and build leaner."],"distractorIds":["spring/spring_native_graalvm#ac200a9f87ea","spring/spring_native_graalvm#e0e6f827ecd5","spring/spring_native_graalvm#91117c389182"],"concepts":["heavier","hungry","aot","javac","compilation","reachable"]},{"id":"spring/spring_native_graalvm#91117c389182","section":"spring","module":"spring/spring_native_graalvm","moduleName":"spring native graalvm","sourceFile":"README.md","difficulty":"advanced","question":"How is Spring's native support different from Quarkus/Micronaut?","answerFull":"Quarkus and Micronaut were designed native-first: they do dependency injection and metadata generation at compile time and avoid runtime reflection by architecture, so they need fewer hints and build leaner. Spring retained its powerful runtime programming model and added an AOT layer on top to make that model expressible under the closed world. The tradeoff: Spring keeps its rich ecosystem and runtime flexibility (on the JVM) while supporting native; the others start leaner but with a different (compile-time) programming model.","correct":"Quarkus and Micronaut were designed native-first: they do dependency injection and metadata generation at compile time and avoid runtime reflection by architecture, so they need fewer hints and build leaner.","distractors":["Not freely — code that relies on unbounded runtime reflection, dynamic class generation, or JVM agents is hostile to the closed world and will need extensive hints or refactoring.","It is a JVM agent (native-image-agent) that observes a normal JVM run and records all reflective, resource, proxy, and serialization access, writing the corresponding metadata JSON files.","Build-time initialization runs a class's static initializer during the native build and bakes the resulting state into the image heap, giving faster startup."],"distractorIds":["spring/spring_native_graalvm#6638c360903c","spring/spring_native_graalvm#56e35184f275","spring/spring_native_graalvm#e0e6f827ecd5"],"concepts":["leaner","micronaut","quarkus","hints","fewer","reflection"],"distractorsMd":[null,"It is a JVM agent (`native-image-agent`) that observes a normal JVM run and records all reflective, resource, proxy, and serialization access, writing the corresponding metadata JSON files.",null]},{"id":"spring/spring_native_graalvm#6638c360903c","section":"spring","module":"spring/spring_native_graalvm","moduleName":"spring native graalvm","sourceFile":"README.md","difficulty":"advanced","question":"Can you use the JVM's full reflection and dynamic features and still go native later?","answerFull":"Not freely — code that relies on unbounded runtime reflection, dynamic class generation, or JVM agents is hostile to the closed world and will need extensive hints or refactoring. The pragmatic approach is to write \"native-friendly\" code from the start (constructor injection, minimal reflection, no runtime bytecode gen), lean on Spring AOT and the metadata repository, and validate continuously with native tests so you do not discover a wall of missing metadata at the end.","correct":"Not freely — code that relies on unbounded runtime reflection, dynamic class generation, or JVM agents is hostile to the closed world and will need extensive hints or refactoring.","distractors":["It is a JVM agent (native-image-agent) that observes a normal JVM run and records all reflective, resource, proxy, and serialization access, writing the corresponding metadata JSON files.","Spring normally creates CGLIB/JDK dynamic proxies at runtime; under native that is impossible (no runtime bytecode generation for CGLIB and proxies must be known up front).","Reachability metadata tells GraalVM about dynamic behavior its static analysis cannot see, so the affected code is retained and configured in the image."],"distractorIds":["spring/spring_native_graalvm#56e35184f275","spring/spring_native_graalvm#876be7508bf3","spring/spring_native_graalvm#f02f20876e42"],"concepts":["hostile","extensive","refactoring","freely","hints","relies"],"distractorsMd":["It is a JVM agent (`native-image-agent`) that observes a normal JVM run and records all reflective, resource, proxy, and serialization access, writing the corresponding metadata JSON files.",null,null]},{"id":"spring/spring_performance#c3c875d58df7","section":"spring","module":"spring/spring_performance","moduleName":"spring performance","sourceFile":"README.md","difficulty":"core","question":"What is the correct formula for sizing a HikariCP connection pool, and what happens if it is too small or too large?","answerFull":"The HikariCP-recommended formula for OLTP workloads is: poolSize = (numCores * 2) + effectiveSpindleCount. For a 4-core application server using an SSD-backed database, this yields 9 connections. The throughput formula is: poolSize = (requestsPerSecond * avgQueryDurationMs) / 1000. If the pool is too small, threads queue for connections — hikaricp.connections.pending rises, P99 latency spikes, and eventually connection-timeout exceptions occur. If the pool is too large, the database server handles too many concurrent connections — lock contention increases, context switches multiply, and the DB's own connection overhead (each PostgreSQL connection spawns a backend process) becomes expensive. The right size is validated empirically by load testing and monitoring the pending metric: it should stay near zero under target load.","correct":"The HikariCP-recommended formula for OLTP workloads is: poolSize = (numCores * 2) + effectiveSpindleCount.","distractors":["server.tomcat.threads.max controls the maximum number of platform threads in Tomcat's executor thread pool.","maxLifetime is the maximum amount of time a connection is allowed to exist in the pool, whether idle or in use.","@Transactional(readOnly=true) communicates two things: to Hibernate (disable dirty checking, skip flush on commit) and to the JDBC driver (set the connection's read-only flag)."],"distractorIds":["spring/spring_performance#d11f8f87c4c2","spring/spring_performance#b77e82f14d3c","spring/spring_performance#c05ffbfeccef"],"concepts":["effectivespindlecount","numcores","poolsize","recommended","oltp","formula"],"answerFullMd":"The HikariCP-recommended formula for OLTP workloads is: poolSize = (numCores * 2) + effectiveSpindleCount. For a 4-core application server using an SSD-backed database, this yields 9 connections. The throughput formula is: poolSize = (requestsPerSecond * avgQueryDurationMs) / 1000. If the pool is too small, threads queue for connections — `hikaricp.connections.pending` rises, P99 latency spikes, and eventually connection-timeout exceptions occur. If the pool is too large, the database server handles too many concurrent connections — lock contention increases, context switches multiply, and the DB's own connection overhead (each PostgreSQL connection spawns a backend process) becomes expensive. The right size is validated empirically by load testing and monitoring the pending metric: it should stay near zero under target load."},{"id":"spring/spring_performance#ca0bac2ea306","section":"spring","module":"spring/spring_performance","moduleName":"spring performance","sourceFile":"README.md","difficulty":"core","question":"What does spring.main.lazy-initialization=true do, and what is the risk of enabling it?","answerFull":"It configures all Spring beans to be created on first access rather than at ApplicationContext startup. This reduces startup time because the majority of beans are never needed during JVM initialization — they are created on the first request that exercises their code path. Typical startup time reduction is 40–70% for large applications. The risk is late-fail behavior: misconfigurations (missing required properties, unsatisfied dependencies, failed @PostConstruct methods) that would normally surface as startup errors are deferred until the first request triggers the problematic bean's creation. This means a misconfigured deployment may appear healthy (readiness probe passes) but fail on the first real request. Mitigate by running integration tests in CI that exercise all beans before production deployment, or by selectively annotating critical beans with @Lazy(false) to force eager initialization.","correct":"It configures all Spring beans to be created on first access rather than at ApplicationContext startup.","distractors":["@Transactional(readOnly=true) communicates two things: to Hibernate (disable dirty checking, skip flush on commit) and to the JDBC driver (set the connection's read-only flag).","spring-context-indexer is an annotation processor that runs at compile time and generates a META-INF/spring.components file listing all @Component-annotated classes and their stereotype annotations.","@Cacheable is implemented using Spring AOP."],"distractorIds":["spring/spring_performance#c05ffbfeccef","spring/spring_performance#e5407c9fb91e","spring/spring_performance#1353a81b2099"],"concepts":["configures","risk","enabling","initialization","applicationcontext","lazy"]},{"id":"spring/spring_performance#1353a81b2099","section":"spring","module":"spring/spring_performance","moduleName":"spring performance","sourceFile":"README.md","difficulty":"core","question":"Explain how @Cacheable works internally and what the limitations are for self-invocation.","answerFull":"@Cacheable is implemented using Spring AOP. Spring creates a proxy wrapping the bean. When an external caller invokes a @Cacheable method, the call goes through the proxy, which checks the cache. On a hit, the proxy returns the cached value without invoking the real method. On a miss, the proxy invokes the real method, stores the result in the cache, and returns it. Self-invocation bypasses the proxy: when a method in the same bean calls another @Cacheable method using this.method(), it directly invokes the target class, not the proxy. The cache is never consulted. To fix this, inject a self-reference to the proxied bean (via @Autowired private ServiceClass self), or extract the cached method to a separate Spring bean so all calls go through a proxy.","correct":"@Cacheable is implemented using Spring AOP.","distractors":["It configures all Spring beans to be created on first access rather than at ApplicationContext startup.","spring-context-indexer is an annotation processor that runs at compile time and generates a META-INF/spring.components file listing all @Component-annotated classes and their stereotype annotations.","@Transactional(readOnly=true) communicates two things: to Hibernate (disable dirty checking, skip flush on commit) and to the JDBC driver (set the connection's read-only flag)."],"distractorIds":["spring/spring_performance#ca0bac2ea306","spring/spring_performance#e5407c9fb91e","spring/spring_performance#c05ffbfeccef"],"concepts":["limitations","explain","cacheable","implemented","internally","invocation"]},{"id":"spring/spring_performance#fd74bd819d70","section":"spring","module":"spring/spring_performance","moduleName":"spring performance","sourceFile":"README.md","difficulty":"core","question":"What is the difference between virtual threads and WebFlux for achieving high concurrency in Spring Boot?","answerFull":"Both solve the same problem — too many platform threads blocking on I/O — but with different approaches. Virtual threads (Project Loom) are lightweight JVM threads. A platform thread blocked on I/O parks its virtual thread and frees the carrier platform thread to run other virtual threads. The programming model remains synchronous and imperative — existing JDBC, RestTemplate, and service code works without modification. WebFlux uses Reactor's non-blocking, event-driven model: a small pool of event-loop threads (typically 2 * CPU cores) handles many concurrent requests by never blocking — all I/O is callback-driven or reactive (Flux/Mono). WebFlux requires rewriting application code in a reactive functional style and requires R2DBC instead of JDBC. Virtual threads are easier to adopt (one config flag in Spring Boot 3.2); WebFlux provides more control for streaming use cases and pure reactive architectures.","correct":"Both solve the same problem — too many platform threads blocking on I/O — but with different approaches.","distractors":["It configures all Spring beans to be created on first access rather than at ApplicationContext startup.","server.tomcat.threads.max controls the maximum number of platform threads in Tomcat's executor thread pool.","spring-context-indexer is an annotation processor that runs at compile time and generates a META-INF/spring.components file listing all @Component-annotated classes and their stereotype annotations."],"distractorIds":["spring/spring_performance#ca0bac2ea306","spring/spring_performance#d11f8f87c4c2","spring/spring_performance#e5407c9fb91e"],"concepts":["achieving","webflux","solve","difference","approaches","boot"]},{"id":"spring/spring_performance#c05ffbfeccef","section":"spring","module":"spring/spring_performance","moduleName":"spring performance","sourceFile":"README.md","difficulty":"core","question":"Why does @Transactional(readOnly=true) not route queries to a read replica, and how do you achieve read-replica routing?","answerFull":"@Transactional(readOnly=true) communicates two things: to Hibernate (disable dirty checking, skip flush on commit) and to the JDBC driver (set the connection's read-only flag). Neither of these changes which DataSource the connection comes from. Routing to a read replica requires a RoutingDataSource — a DataSource implementation that extends AbstractRoutingDataSource and overrides determineCurrentLookupKey() to return \"replica\" when TransactionSynchronizationManager.isCurrentTransactionReadOnly() returns true. The RoutingDataSource holds references to both the primary and replica DataSource instances. Spring Data and JDBC use the RoutingDataSource without knowing about replicas. This pattern requires that the application is disciplined about @Transactional(readOnly=true) annotations — a read-only transaction accidentally calling a method that writes will succeed (replica typically allows reads) but the write will fail, not be silently ignored.","correct":"@Transactional(readOnly=true) communicates two things: to Hibernate (disable dirty checking, skip flush on commit) and to the JDBC driver (set the connection's read-only flag).","distractors":["maxLifetime is the maximum amount of time a connection is allowed to exist in the pool, whether idle or in use.","@Cacheable is implemented using Spring AOP.","Enable JFR (Java Flight Recorder) with -XX:+FlightRecorder and -XX:StartFlightRecording=filename=app.jfr."],"distractorIds":["spring/spring_performance#b77e82f14d3c","spring/spring_performance#1353a81b2099","spring/spring_performance#eefad48429b1"],"concepts":["readonly","communicates","hibernate","things","disable","jdbc"]},{"id":"spring/spring_performance#b77e82f14d3c","section":"spring","module":"spring/spring_performance","moduleName":"spring performance","sourceFile":"README.md","difficulty":"intermediate","question":"What is the HikariCP maxLifetime property, and what can go wrong if it is set incorrectly?","answerFull":"maxLifetime is the maximum amount of time a connection is allowed to exist in the pool, whether idle or in use. When a connection reaches maxLifetime, HikariCP retires it from the pool and creates a new one to replace it. This is necessary because long-lived database connections can become stale: firewalls may silently drop idle TCP connections, database servers may kill old connections, or connection state (e.g., transaction isolation level, character set) may drift. The critical rule is that maxLifetime must be several minutes less than the database or network proxy's connection idle timeout. AWS RDS Proxy has a default idle timeout of 1800 seconds (30 minutes). HikariCP's default maxLifetime is also 1800 seconds. If a connection approaches its maxLifetime exactly when the RDS Proxy times it out, HikariCP may hand out the connection to a thread after the proxy has already closed it, resulting in a broken pipe exception on the first query. Set maxLifetime to 1500s (25 minutes) with a 5-minute buffer.","correct":"maxLifetime is the maximum amount of time a connection is allowed to exist in the pool, whether idle or in use.","distractors":["GraalVM's native-image compiler performs ahead-of-time compilation by running a closed-world analysis starting from the main method, tracing all reachable code paths, and compiling them to native machine code.","The HikariCP-recommended formula for OLTP workloads is: poolSize = (numCores * 2) + effectiveSpindleCount.","server.tomcat.threads.max controls the maximum number of platform threads in Tomcat's executor thread pool."],"distractorIds":["spring/spring_performance#b0109b3923c2","spring/spring_performance#c3c875d58df7","spring/spring_performance#d11f8f87c4c2"],"concepts":["incorrectly","maxlifetime","amount","hikaricp","idle","property"]},{"id":"spring/spring_performance#e5407c9fb91e","section":"spring","module":"spring/spring_performance","moduleName":"spring performance","sourceFile":"README.md","difficulty":"intermediate","question":"How does spring-context-indexer improve startup time and what are its constraints?","answerFull":"spring-context-indexer is an annotation processor that runs at compile time and generates a META-INF/spring.components file listing all @Component-annotated classes and their stereotype annotations. At runtime, Spring reads this index file instead of scanning the entire classpath JAR by JAR. This eliminates the classpath scanning phase, which for large applications with hundreds of JARs can take 1–2 seconds. The constraint is that all components must be compiled with the annotation processor on the classpath — dynamically added components (loaded via URLClassLoader at runtime, OSGi bundles, etc.) are not indexed and will not be found. Also, the index covers only the current module's compilation; components in dependencies are still found normally.","correct":"spring-context-indexer is an annotation processor that runs at compile time and generates a META-INF/spring.components file listing all @Component-annotated classes and their stereotype annotations.","distractors":["Lazy initialisation defers bean creation until the bean is first requested (first HTTP request, first use) instead of initialising all beans at startup.","GraalVM's native-image compiler performs ahead-of-time compilation by running a closed-world analysis starting from the main method, tracing all reachable code paths, and compiling them to native machine code.","maxLifetime is the maximum amount of time a connection is allowed to exist in the pool, whether idle or in use."],"distractorIds":["spring/spring_performance#c963f0610d20","spring/spring_performance#b0109b3923c2","spring/spring_performance#b77e82f14d3c"],"concepts":["indexer","stereotype","inf","listing","processor","improve"]},{"id":"spring/spring_performance#b0109b3923c2","section":"spring","module":"spring/spring_performance","moduleName":"spring performance","sourceFile":"README.md","difficulty":"intermediate","question":"What is GraalVM AOT compilation and what Spring Boot features require special configuration (hints) to work natively?","answerFull":"GraalVM's native-image compiler performs ahead-of-time compilation by running a closed-world analysis starting from the main method, tracing all reachable code paths, and compiling them to native machine code. It eliminates all unreachable code and bakes in the call graph statically. Anything that adds to the call graph dynamically at runtime — reflection, dynamic class loading, JDK proxies, serialization — must be declared explicitly via hints (JSON configuration files or RuntimeHintsRegistrar). Spring Boot's spring-aot-maven-plugin generates most hints automatically for Spring beans, @Autowired, @Value, and common patterns. Areas requiring manual hints: custom Jackson modules, third-party libraries using reflection internally, JNI calls, and runtime-generated proxy classes not recognized by Spring's AOT analysis. Test native images with mvn -Pnative test using GraalVM's native test runner.","correct":"GraalVM's native-image compiler performs ahead-of-time compilation by running a closed-world analysis starting from the main method, tracing all reachable code paths, and compiling them to native machine code.","distractors":["spring-context-indexer is an annotation processor that runs at compile time and generates a META-INF/spring.components file listing all @Component-annotated classes and their stereotype annotations.","maxLifetime is the maximum amount of time a connection is allowed to exist in the pool, whether idle or in use.","It configures all Spring beans to be created on first access rather than at ApplicationContext startup."],"distractorIds":["spring/spring_performance#e5407c9fb91e","spring/spring_performance#b77e82f14d3c","spring/spring_performance#ca0bac2ea306"],"concepts":["compiling","aot","graalvm","ahead","compilation","hints"]},{"id":"spring/spring_performance#0ca30b24672e","section":"spring","module":"spring/spring_performance","moduleName":"spring performance","sourceFile":"README.md","difficulty":"intermediate","question":"What are the implications of synchronized blocks and methods on virtual thread performance?","answerFull":"Java's synchronized keyword acquires a monitor associated with a specific object. In JDK 21, a virtual thread that enters a synchronized block is pinned to its carrier platform thread — it cannot be unmounted even if it blocks on I/O inside the synchronized block. This means the carrier platform thread is blocked for the duration, eliminating the scalability benefit of virtual threads for that code path. Synchronized blocks that call blocking I/O (database queries, network calls) are particularly harmful. The fix is to replace synchronized with ReentrantLock, which supports virtual thread unmounting: the virtual thread can park while waiting for the lock or for I/O, freeing the carrier thread. Spring Boot 3.2 with virtual threads enabled logs warnings when pinning is detected. Libraries using synchronized internally (some legacy JDBC drivers, older connection pools) can degrade virtual thread scalability — this is one reason Loom-friendly versions of common libraries were updated for Java 21.","correct":"Java's synchronized keyword acquires a monitor associated with a specific object.","distractors":["Enable JFR (Java Flight Recorder) with -XX:+FlightRecorder and -XX:StartFlightRecording=filename=app.jfr.","server.tomcat.threads.max controls the maximum number of platform threads in Tomcat's executor thread pool.","@Cacheable is implemented using Spring AOP."],"distractorIds":["spring/spring_performance#eefad48429b1","spring/spring_performance#d11f8f87c4c2","spring/spring_performance#1353a81b2099"],"concepts":["associated","acquires","synchronized","monitor","keyword","performance"]},{"id":"spring/spring_performance#c9af82ec20fb","section":"spring","module":"spring/spring_performance","moduleName":"spring performance","sourceFile":"README.md","difficulty":"advanced","question":"How does the @Cacheable TTL strategy affect consistency and what patterns reduce stale read risk?","answerFull":"TTL-based cache expiry means cached values are served for up to TTL seconds after they are written — during this window, readers may see stale data if the source record changed. The risk is proportional to TTL length and update frequency. Patterns to reduce stale reads: (1) @CacheEvict on write operations — immediately removes the cache entry when data changes, forcing the next read to go to the database. This provides strong consistency for single-service scenarios. (2) Short TTL for volatile data — exchange rates at 30 seconds, product prices at 5 minutes, user profiles at 30 minutes. (3) Event-driven cache invalidation — another service publishing a change event triggers a cache eviction listener. (4) Cache-aside with version checking — include a version number in the cache key; when data changes, the version increments and old keys are naturally abandoned. Never cache data that must be strongly consistent (bank balances, inventory counts during checkout) — the complexity of maintaining cache consistency under concurrent writes exceeds the performance benefit.","correct":"TTL-based cache expiry means cached values are served for up to TTL seconds after they are written — during this window, readers may see stale data if the source record changed.","distractors":["@Cacheable is implemented using Spring AOP.","@Transactional(readOnly=true) communicates two things: to Hibernate (disable dirty checking, skip flush on commit) and to the JDBC driver (set the connection's read-only flag).","Enable JFR (Java Flight Recorder) with -XX:+FlightRecorder and -XX:StartFlightRecording=filename=app.jfr."],"distractorIds":["spring/spring_performance#1353a81b2099","spring/spring_performance#c05ffbfeccef","spring/spring_performance#eefad48429b1"],"concepts":["readers","affect","served","cacheable","risk","expiry"]},{"id":"spring/spring_performance#d11f8f87c4c2","section":"spring","module":"spring/spring_performance","moduleName":"spring performance","sourceFile":"README.md","difficulty":"advanced","question":"What does server.tomcat.threads.max control and when should it be changed?","answerFull":"server.tomcat.threads.max controls the maximum number of platform threads in Tomcat's executor thread pool. Each concurrent HTTP request in flight occupies one thread. The default is 200. Threads waiting on blocking I/O (JDBC, external REST calls) are blocked but still occupy their slot — so 200 threads means at most 200 concurrent requests regardless of actual CPU usage. Reasons to increase it: high I/O-bound concurrency requirements where you want more concurrent in-flight requests without switching to WebFlux or virtual threads. Reasons to decrease it: if the application is CPU-bound, more threads than 2 * CPU cores adds context-switch overhead with no throughput benefit. With virtual threads enabled (Spring Boot 3.2, Java 21), this property becomes largely irrelevant — virtual threads are created per request and the platform thread pool (number of CPU cores) handles I/O parking automatically.","correct":"server.tomcat.threads.max controls the maximum number of platform threads in Tomcat's executor thread pool.","distractors":["maxLifetime is the maximum amount of time a connection is allowed to exist in the pool, whether idle or in use.","TTL-based cache expiry means cached values are served for up to TTL seconds after they are written — during this window, readers may see stale data if the source record changed.","@Cacheable is implemented using Spring AOP."],"distractorIds":["spring/spring_performance#b77e82f14d3c","spring/spring_performance#c9af82ec20fb","spring/spring_performance#1353a81b2099"],"concepts":["tomcat","executor","changed","platform","controls","pool"]},{"id":"spring/spring_performance#eefad48429b1","section":"spring","module":"spring/spring_performance","moduleName":"spring performance","sourceFile":"README.md","difficulty":"advanced","question":"How do you detect and diagnose a GC pause contributing to high latency P99 in a Spring Boot application?","answerFull":"Enable JFR (Java Flight Recorder) with -XX:+FlightRecorder and -XX:StartFlightRecording=filename=app.jfr. JFR records GC events with pause durations at microsecond resolution and zero production overhead. In JFR output, look for GC pause events that correlate with latency spikes. Also useful: -XX:+PrintGCDetails -Xlog:gc*:file=gc.log for traditional GC logging. For G1GC (default since Java 9), a target pause time of 200ms is the default — tune with -XX:MaxGCPauseMillis=50 for lower latency at the cost of some throughput. ZGC (Java 15+ production ready) provides sub-millisecond pauses at the cost of slightly higher CPU overhead — add -XX:+UseZGC. In Spring Boot applications, common GC pressure sources are: large response objects materialized in memory, Jackson serialization of deep object graphs, and Hibernate first-level cache accumulating entities in long-running transactions. Use async-profiler in allocation mode (-e alloc) to find the top allocation sites.","correct":"Enable JFR (Java Flight Recorder) with -XX:+FlightRecorder and -XX:StartFlightRecording=filename=app.jfr.","distractors":["spring-context-indexer is an annotation processor that runs at compile time and generates a META-INF/spring.components file listing all @Component-annotated classes and their stereotype annotations.","@Cacheable is implemented using Spring AOP.","Java's synchronized keyword acquires a monitor associated with a specific object."],"distractorIds":["spring/spring_performance#e5407c9fb91e","spring/spring_performance#1353a81b2099","spring/spring_performance#0ca30b24672e"],"concepts":["diagnose","filename","flightrecorder","recorder","startflightrecording","contributing"]},{"id":"spring/spring_performance#c963f0610d20","section":"spring","module":"spring/spring_performance","moduleName":"spring performance","sourceFile":"README.md","difficulty":"advanced","question":"What is Spring Boot lazy initialisation (spring.main.lazy-initialization=true) and what are its risks?","answerFull":"Lazy initialisation defers bean creation until the bean is first requested (first HTTP request, first use) instead of initialising all beans at startup. Benefits: faster startup (relevant for tests and development), lower initial memory. Risks: (1) Deferred failure — a misconfigured bean that would fail at startup now fails on the first user request, potentially in production. (2) Slow first request — initialising expensive beans (HikariCP pool establishment, cache warm-up) on the first request adds latency visible to the user. (3) Kubernetes readiness probes — the pod reports ready before it is actually ready to serve traffic efficiently. Mitigation: only enable lazy initialisation for development and test; use it in production only for clearly isolated optional feature modules. For production startup-time optimisation, prefer GraalVM native image or class data sharing (-XX:+UseSharedSpaces) over lazy initialisation.","correct":"Lazy initialisation defers bean creation until the bean is first requested (first HTTP request, first use) instead of initialising all beans at startup.","distractors":["GraalVM's native-image compiler performs ahead-of-time compilation by running a closed-world analysis starting from the main method, tracing all reachable code paths, and compiling them to native machine code.","spring-context-indexer is an annotation processor that runs at compile time and generates a META-INF/spring.components file listing all @Component-annotated classes and their stereotype annotations.","@Transactional(readOnly=true) communicates two things: to Hibernate (disable dirty checking, skip flush on commit) and to the JDBC driver (set the connection's read-only flag)."],"distractorIds":["spring/spring_performance#b0109b3923c2","spring/spring_performance#e5407c9fb91e","spring/spring_performance#c05ffbfeccef"],"concepts":["initialising","initialisation","defers","requested","risks","creation"],"questionMd":"What is Spring Boot lazy initialisation (`spring.main.lazy-initialization=true`) and what are its risks?","answerFullMd":"Lazy initialisation defers bean creation until the bean is first requested (first HTTP request, first use) instead of initialising all beans at startup. Benefits: faster startup (relevant for tests and development), lower initial memory. Risks: (1) **Deferred failure** — a misconfigured bean that would fail at startup now fails on the first user request, potentially in production. (2) **Slow first request** — initialising expensive beans (HikariCP pool establishment, cache warm-up) on the first request adds latency visible to the user. (3) **Kubernetes readiness probes** — the pod reports ready before it is actually ready to serve traffic efficiently. Mitigation: only enable lazy initialisation for development and test; use it in production only for clearly isolated optional feature modules. For production startup-time optimisation, prefer GraalVM native image or class data sharing (`-XX:+UseSharedSpaces`) over lazy initialisation."},{"id":"spring/spring_proxies#0f61c489f42b","section":"spring","module":"spring/spring_proxies","moduleName":"spring proxies","sourceFile":"README.md","difficulty":"core","question":"What are the two proxy types Spring uses and when does it use each?","answerFull":"JDK dynamic proxy (uses java.lang.reflect.Proxy) is used when the target bean implements at least one interface and proxyTargetClass=false. CGLIB proxy (generates a subclass via bytecode) is used when the target has no interface, or when proxyTargetClass=true is configured. Spring Boot sets proxyTargetClass=true by default, so CGLIB is used for all beans in Spring Boot applications even when interfaces exist.","correct":"JDK dynamic proxy (uses java.lang.reflect.Proxy) is used when the target bean implements at least one interface and proxyTargetClass=false.","distractors":["AopContext.currentProxy() retrieves the current proxy from a ThreadLocal, so calling the returned proxy re-enters the interceptor chain instead of calling this directly.","proxyTargetClass=false (default): Spring creates a JDK dynamic proxy that implements the same interfaces as the target bean.","A request enters Spring Security's FilterChainProxy (a Filter, not a Spring AOP proxy), which runs security filters."],"distractorIds":["spring/spring_proxies#b35bbf8981da","spring/spring_proxies#a1bddf490113","spring/spring_proxies#263e0bdd2149"],"concepts":["proxytargetclass","lang","reflect","jdk","implements","dynamic"],"correctMd":"JDK dynamic proxy (uses `java.lang.reflect.Proxy`) is used when the target bean implements at least one interface and `proxyTargetClass=false`.","answerFullMd":"JDK dynamic proxy (uses `java.lang.reflect.Proxy`) is used when the target bean implements at least one interface and `proxyTargetClass=false`. CGLIB proxy (generates a subclass via bytecode) is used when the target has no interface, or when `proxyTargetClass=true` is configured. Spring Boot sets `proxyTargetClass=true` by default, so CGLIB is used for all beans in Spring Boot applications even when interfaces exist.","distractorsMd":["`AopContext.currentProxy()` retrieves the current proxy from a `ThreadLocal`, so calling the returned proxy re-enters the interceptor chain instead of calling `this` directly.","`proxyTargetClass=false` (default): Spring creates a JDK dynamic proxy that implements the same interfaces as the target bean.","A request enters Spring Security's `FilterChainProxy` (a `Filter`, not a Spring AOP proxy), which runs security filters."]},{"id":"spring/spring_proxies#eacaab9c670f","section":"spring","module":"spring/spring_proxies","moduleName":"spring proxies","sourceFile":"README.md","difficulty":"core","question":"Why does self-invocation break @Transactional?","answerFull":"When you call this.method() inside a Spring bean, this refers to the raw bean object, not the proxy. The proxy wrapping the bean is only involved when the method is called from outside the bean. Since @Transactional is implemented as a proxy interceptor, internal calls bypass it entirely. The fix is to restructure code so transactional operations are called from a different Spring bean (which goes through its proxy), or as a last resort, inject the bean into itself (@Autowired private MyService self).","correct":"When you call this.method() inside a Spring bean, this refers to the raw bean object, not the proxy.","distractors":["When a bean injects itself via @Autowired private MyService self, Spring injects the proxy bean (from the context), not this.","A request enters Spring Security's FilterChainProxy (a Filter, not a Spring AOP proxy), which runs security filters.","A CGLIB-proxied bean and a plain instance of the same class are never equal() by default, because the proxy's runtime class differs from the original class."],"distractorIds":["spring/spring_proxies#0add8ae04409","spring/spring_proxies#263e0bdd2149","spring/spring_proxies#af723f0f1cd3"],"concepts":["break","invocation","refers","transactional","inside","raw"],"correctMd":"When you call `this.method()` inside a Spring bean, `this` refers to the raw bean object, not the proxy.","answerFullMd":"When you call `this.method()` inside a Spring bean, `this` refers to the raw bean object, not the proxy. The proxy wrapping the bean is only involved when the method is called from outside the bean. Since `@Transactional` is implemented as a proxy interceptor, internal calls bypass it entirely. The fix is to restructure code so transactional operations are called from a different Spring bean (which goes through its proxy), or as a last resort, inject the bean into itself (`@Autowired private MyService self`).","distractorsMd":["When a bean injects itself via `@Autowired private MyService self`, Spring injects the proxy bean (from the context), not `this`.","A request enters Spring Security's `FilterChainProxy` (a `Filter`, not a Spring AOP proxy), which runs security filters.","A CGLIB-proxied bean and a plain instance of the same class are never `equal()` by default, because the proxy's runtime class differs from the original class."]},{"id":"spring/spring_proxies#38b517c71985","section":"spring","module":"spring/spring_proxies","moduleName":"spring proxies","sourceFile":"README.md","difficulty":"core","question":"What happens when you make a method final in a Spring service that uses @Transactional?","answerFull":"CGLIB cannot override final methods. If @Transactional is on a final method, the advice is silently skipped — no transaction is started, no exception is thrown, the method simply runs without Spring's transaction management. This is extremely dangerous because there is no indication anything is wrong at startup. Similarly, @Cacheable and @Async on final methods are silently ignored. Always run your tests to verify transactional behavior.","correct":"CGLIB cannot override final methods.","distractors":["A request enters Spring Security's FilterChainProxy (a Filter, not a Spring AOP proxy), which runs security filters.","It forces CGLIB proxies for all Spring AOP-proxied beans, even those implementing interfaces.","When you call this.method() inside a Spring bean, this refers to the raw bean object, not the proxy."],"distractorIds":["spring/spring_proxies#263e0bdd2149","spring/spring_proxies#d4d5ad9dd9b6","spring/spring_proxies#eacaab9c670f"],"concepts":["cglib","override","transactional","happens","make","final"],"correctMd":"CGLIB cannot override `final` methods.","answerFullMd":"CGLIB cannot override `final` methods. If `@Transactional` is on a `final` method, the advice is silently skipped — no transaction is started, no exception is thrown, the method simply runs without Spring's transaction management. This is extremely dangerous because there is no indication anything is wrong at startup. Similarly, `@Cacheable` and `@Async` on `final` methods are silently ignored. Always run your tests to verify transactional behavior.","distractorsMd":["A request enters Spring Security's `FilterChainProxy` (a `Filter`, not a Spring AOP proxy), which runs security filters.",null,"When you call `this.method()` inside a Spring bean, `this` refers to the raw bean object, not the proxy."]},{"id":"spring/spring_proxies#72323708f962","section":"spring","module":"spring/spring_proxies","moduleName":"spring proxies","sourceFile":"README.md","difficulty":"core","question":"What is the difference between AOP proxy behavior with JDK vs CGLIB?","answerFull":"Both proxies intercept method calls from outside the bean. The key difference: JDK proxy implements interfaces and can only be cast to those interfaces; CGLIB proxy extends the class and can be cast to the class type. With JDK proxy, calling bean.getClass() returns a $Proxy class; with CGLIB it returns ClassName$$EnhancerBySpringCGLIB$$.... CGLIB proxies can be used where interface-based proxies cannot (concrete class injection, casting to class type).","correct":"Both proxies intercept method calls from outside the bean.","distractors":["A CGLIB-proxied bean and a plain instance of the same class are never equal() by default, because the proxy's runtime class differs from the original class.","When you call this.method() inside a Spring bean, this refers to the raw bean object, not the proxy.","proxyTargetClass=false (default): Spring creates a JDK dynamic proxy that implements the same interfaces as the target bean."],"distractorIds":["spring/spring_proxies#af723f0f1cd3","spring/spring_proxies#eacaab9c670f","spring/spring_proxies#a1bddf490113"],"concepts":["intercept","difference","jdk","cglib","aop","outside"],"answerFullMd":"Both proxies intercept method calls from outside the bean. The key difference: JDK proxy implements interfaces and can only be cast to those interfaces; CGLIB proxy extends the class and can be cast to the class type. With JDK proxy, calling `bean.getClass()` returns a `$Proxy` class; with CGLIB it returns `ClassName$$EnhancerBySpringCGLIB$$...`. CGLIB proxies can be used where interface-based proxies cannot (concrete class injection, casting to class type).","distractorsMd":["A CGLIB-proxied bean and a plain instance of the same class are never `equal()` by default, because the proxy's runtime class differs from the original class.","When you call `this.method()` inside a Spring bean, `this` refers to the raw bean object, not the proxy.","`proxyTargetClass=false` (default): Spring creates a JDK dynamic proxy that implements the same interfaces as the target bean."]},{"id":"spring/spring_proxies#2debc4553258","section":"spring","module":"spring/spring_proxies","moduleName":"spring proxies","sourceFile":"README.md","difficulty":"core","question":"How would you verify that a Spring bean is proxied?","answerFull":"Check bean.getClass().getName(). For CGLIB proxy, the name contains $$EnhancerBySpringCGLIB$$. For JDK proxy, it starts with com.sun.proxy.$Proxy. Alternatively, use AopUtils.isAopProxy(bean) (returns true for both types) and AopUtils.isCglibProxy(bean) or AopUtils.isJdkDynamicProxy(bean) for specific type. In tests, use AopTestUtils.getTargetObject(bean) to unwrap the proxy and get the underlying bean.","correct":"Check bean.getClass().getName().","distractors":["A CGLIB-proxied bean and a plain instance of the same class are never equal() by default, because the proxy's runtime class differs from the original class.","When you call this.method() inside a Spring bean, this refers to the raw bean object, not the proxy.","JDK dynamic proxy (uses java.lang.reflect.Proxy) is used when the target bean implements at least one interface and proxyTargetClass=false."],"distractorIds":["spring/spring_proxies#af723f0f1cd3","spring/spring_proxies#eacaab9c670f","spring/spring_proxies#0f61c489f42b"],"concepts":["getclass","getname","proxied","verify","check","bean"],"correctMd":"Check `bean.getClass().getName()`.","answerFullMd":"Check `bean.getClass().getName()`. For CGLIB proxy, the name contains `$$EnhancerBySpringCGLIB$$`. For JDK proxy, it starts with `com.sun.proxy.$Proxy`. Alternatively, use `AopUtils.isAopProxy(bean)` (returns true for both types) and `AopUtils.isCglibProxy(bean)` or `AopUtils.isJdkDynamicProxy(bean)` for specific type. In tests, use `AopTestUtils.getTargetObject(bean)` to unwrap the proxy and get the underlying bean.","distractorsMd":["A CGLIB-proxied bean and a plain instance of the same class are never `equal()` by default, because the proxy's runtime class differs from the original class.","When you call `this.method()` inside a Spring bean, `this` refers to the raw bean object, not the proxy.","JDK dynamic proxy (uses `java.lang.reflect.Proxy`) is used when the target bean implements at least one interface and `proxyTargetClass=false`."]},{"id":"spring/spring_proxies#d4d5ad9dd9b6","section":"spring","module":"spring/spring_proxies","moduleName":"spring proxies","sourceFile":"README.md","difficulty":"intermediate","question":"How does Spring Boot's proxyTargetClass=true affect the application?","answerFull":"It forces CGLIB proxies for all Spring AOP-proxied beans, even those implementing interfaces. This eliminates the JDK proxy / interface requirement distinction, simplifies injection (you can always inject by class type, not just interface), and avoids ClassCastException when code tries to cast a bean to its concrete class. The downside: CGLIB cannot proxy final classes or final methods. Most Spring Boot applications should not change this default.","correct":"It forces CGLIB proxies for all Spring AOP-proxied beans, even those implementing interfaces.","distractors":["proxyTargetClass=false (default): Spring creates a JDK dynamic proxy that implements the same interfaces as the target bean.","CGLIB works by generating a subclass of the target class at runtime using bytecode generation.","@Configuration CGLIB proxy's sole purpose is to intercept @Bean method calls and return the singleton from the container (caching semantics)."],"distractorIds":["spring/spring_proxies#a1bddf490113","spring/spring_proxies#b582032f8c0a","spring/spring_proxies#f1a984c3bee3"],"concepts":["proxytargetclass","proxied","implementing","affect","cglib","aop"],"answerFullMd":"It forces CGLIB proxies for all Spring AOP-proxied beans, even those implementing interfaces. This eliminates the JDK proxy / interface requirement distinction, simplifies injection (you can always inject by class type, not just interface), and avoids `ClassCastException` when code tries to cast a bean to its concrete class. The downside: CGLIB cannot proxy `final` classes or `final` methods. Most Spring Boot applications should not change this default.","distractorsMd":["`proxyTargetClass=false` (default): Spring creates a JDK dynamic proxy that implements the same interfaces as the target bean.",null,"`@Configuration` CGLIB proxy's sole purpose is to intercept `@Bean` method calls and return the singleton from the container (caching semantics)."]},{"id":"spring/spring_proxies#263e0bdd2149","section":"spring","module":"spring/spring_proxies","moduleName":"spring proxies","sourceFile":"README.md","difficulty":"intermediate","question":"What is the proxy chain in a Spring MVC request?","answerFull":"A request enters Spring Security's FilterChainProxy (a Filter, not a Spring AOP proxy), which runs security filters. After passing security, the request reaches the DispatcherServlet, which dispatches to a controller method. The controller may call a @Service method — this goes through the service's AOP proxy (CGLIB or JDK), which applies @Transactional, @Cacheable, or other AOP advice. Method security (@PreAuthorize) is also applied via AOP proxy on the service. This chain is important to understand for debugging.","correct":"A request enters Spring Security's FilterChainProxy (a Filter, not a Spring AOP proxy), which runs security filters.","distractors":["A CGLIB-proxied bean and a plain instance of the same class are never equal() by default, because the proxy's runtime class differs from the original class.","@Configuration CGLIB proxy's sole purpose is to intercept @Bean method calls and return the singleton from the container (caching semantics).","CGLIB subclasses cannot override final methods — they are not intercepted by the proxy."],"distractorIds":["spring/spring_proxies#af723f0f1cd3","spring/spring_proxies#f1a984c3bee3","spring/spring_proxies#e29161ee4c48"],"concepts":["filterchainproxy","enters","mvc","aop","filters","filter"],"correctMd":"A request enters Spring Security's `FilterChainProxy` (a `Filter`, not a Spring AOP proxy), which runs security filters.","answerFullMd":"A request enters Spring Security's `FilterChainProxy` (a `Filter`, not a Spring AOP proxy), which runs security filters. After passing security, the request reaches the `DispatcherServlet`, which dispatches to a controller method. The controller may call a `@Service` method — this goes through the service's AOP proxy (CGLIB or JDK), which applies `@Transactional`, `@Cacheable`, or other AOP advice. Method security (`@PreAuthorize`) is also applied via AOP proxy on the service. This chain is important to understand for debugging.","distractorsMd":["A CGLIB-proxied bean and a plain instance of the same class are never `equal()` by default, because the proxy's runtime class differs from the original class.","`@Configuration` CGLIB proxy's sole purpose is to intercept `@Bean` method calls and return the singleton from the container (caching semantics).","CGLIB subclasses cannot override `final` methods — they are not intercepted by the proxy."]},{"id":"spring/spring_proxies#b582032f8c0a","section":"spring","module":"spring/spring_proxies","moduleName":"spring proxies","sourceFile":"README.md","difficulty":"intermediate","question":"Why can't CGLIB proxy final classes?","answerFull":"CGLIB works by generating a subclass of the target class at runtime using bytecode generation. Java's final keyword prevents subclassing. When Spring tries to create a CGLIB proxy for a final class, the bytecode generation fails with BeanCreationException: Cannot subclass final class.... The fix is to remove the final modifier. If you genuinely need a final class (immutability concern), avoid annotations that require proxying, or use AspectJ compile-time weaving instead.","correct":"CGLIB works by generating a subclass of the target class at runtime using bytecode generation.","distractors":["@Configuration CGLIB proxy's sole purpose is to intercept @Bean method calls and return the singleton from the container (caching semantics).","CGLIB cannot override final methods.","JDK dynamic proxy (uses java.lang.reflect.Proxy) is used when the target bean implements at least one interface and proxyTargetClass=false."],"distractorIds":["spring/spring_proxies#f1a984c3bee3","spring/spring_proxies#38b517c71985","spring/spring_proxies#0f61c489f42b"],"concepts":["cglib","generating","subclass","bytecode","works","classes"],"answerFullMd":"CGLIB works by generating a subclass of the target class at runtime using bytecode generation. Java's `final` keyword prevents subclassing. When Spring tries to create a CGLIB proxy for a `final` class, the bytecode generation fails with `BeanCreationException: Cannot subclass final class...`. The fix is to remove the `final` modifier. If you genuinely need a `final` class (immutability concern), avoid annotations that require proxying, or use AspectJ compile-time weaving instead.","distractorsMd":["`@Configuration` CGLIB proxy's sole purpose is to intercept `@Bean` method calls and return the singleton from the container (caching semantics).","CGLIB cannot override `final` methods.","JDK dynamic proxy (uses `java.lang.reflect.Proxy`) is used when the target bean implements at least one interface and `proxyTargetClass=false`."]},{"id":"spring/spring_proxies#f1a984c3bee3","section":"spring","module":"spring/spring_proxies","moduleName":"spring proxies","sourceFile":"README.md","difficulty":"intermediate","question":"How does @Configuration's CGLIB proxy differ from a @Transactional proxy?","answerFull":"@Configuration CGLIB proxy's sole purpose is to intercept @Bean method calls and return the singleton from the container (caching semantics). It wraps the entire configuration class. @Transactional (or @Cacheable) proxy wraps an individual service bean and intercepts every method call to apply advice. @Configuration proxying happens during context startup for configuration classes; @Transactional proxying happens via AnnotationAwareAspectJAutoProxyCreator as part of the BeanPostProcessor phase for each bean.","correct":"@Configuration CGLIB proxy's sole purpose is to intercept @Bean method calls and return the singleton from the container (caching semantics).","distractors":["CGLIB subclasses cannot override final methods — they are not intercepted by the proxy.","CGLIB works by generating a subclass of the target class at runtime using bytecode generation.","A CGLIB-proxied bean and a plain instance of the same class are never equal() by default, because the proxy's runtime class differs from the original class."],"distractorIds":["spring/spring_proxies#e29161ee4c48","spring/spring_proxies#b582032f8c0a","spring/spring_proxies#af723f0f1cd3"],"concepts":["sole","intercept","differ","cglib","purpose","semantics"],"correctMd":"`@Configuration` CGLIB proxy's sole purpose is to intercept `@Bean` method calls and return the singleton from the container (caching semantics).","answerFullMd":"`@Configuration` CGLIB proxy's sole purpose is to intercept `@Bean` method calls and return the singleton from the container (caching semantics). It wraps the entire configuration class. `@Transactional` (or `@Cacheable`) proxy wraps an individual service bean and intercepts every method call to apply advice. `@Configuration` proxying happens during context startup for configuration classes; `@Transactional` proxying happens via `AnnotationAwareAspectJAutoProxyCreator` as part of the `BeanPostProcessor` phase for each bean.","distractorsMd":["CGLIB subclasses cannot override `final` methods — they are not intercepted by the proxy.",null,"A CGLIB-proxied bean and a plain instance of the same class are never `equal()` by default, because the proxy's runtime class differs from the original class."]},{"id":"spring/spring_proxies#3666069a43d7","section":"spring","module":"spring/spring_proxies","moduleName":"spring proxies","sourceFile":"README.md","difficulty":"intermediate","question":"What is Objenesis and why does Spring use it?","answerFull":"Objenesis is a library that creates Java objects without calling their constructors, using JVM internals (e.g., sun.reflect.ReflectionFactory). Spring uses it when creating CGLIB proxy subclasses — the proxy subclass does not need to match the target class's constructor signature. Without Objenesis, CGLIB would require the target class to have a no-arg constructor (or matching constructor parameters). With Objenesis (bundled in Spring since 4.0), any class can be CGLIB-proxied regardless of constructor shape.","correct":"Objenesis is a library that creates Java objects without calling their constructors, using JVM internals (e.g., sun.reflect.ReflectionFactory).","distractors":["A request enters Spring Security's FilterChainProxy (a Filter, not a Spring AOP proxy), which runs security filters.","When you call this.method() inside a Spring bean, this refers to the raw bean object, not the proxy.","It forces CGLIB proxies for all Spring AOP-proxied beans, even those implementing interfaces."],"distractorIds":["spring/spring_proxies#263e0bdd2149","spring/spring_proxies#eacaab9c670f","spring/spring_proxies#d4d5ad9dd9b6"],"concepts":["objenesis","reflectionfactory","sun","constructors","internals","reflect"],"correctMd":"Objenesis is a library that creates Java objects without calling their constructors, using JVM internals (e.g., `sun.reflect.ReflectionFactory`).","answerFullMd":"Objenesis is a library that creates Java objects without calling their constructors, using JVM internals (e.g., `sun.reflect.ReflectionFactory`). Spring uses it when creating CGLIB proxy subclasses — the proxy subclass does not need to match the target class's constructor signature. Without Objenesis, CGLIB would require the target class to have a no-arg constructor (or matching constructor parameters). With Objenesis (bundled in Spring since 4.0), any class can be CGLIB-proxied regardless of constructor shape.","distractorsMd":["A request enters Spring Security's `FilterChainProxy` (a `Filter`, not a Spring AOP proxy), which runs security filters.","When you call `this.method()` inside a Spring bean, `this` refers to the raw bean object, not the proxy.",null]},{"id":"spring/spring_proxies#0add8ae04409","section":"spring","module":"spring/spring_proxies","moduleName":"spring proxies","sourceFile":"README.md","difficulty":"advanced","question":"How does the self-injection trick work for avoiding self-invocation?","answerFull":"When a bean injects itself via @Autowired private MyService self, Spring injects the proxy bean (from the context), not this. When application code calls self.method(), it goes through the proxy (which applies AOP advice) rather than directly calling the method on this. The reason Spring can inject the bean's own proxy is that the proxy is already in the singleton registry when field injection runs (phase 2 of lifecycle). This is a valid workaround but is considered a code smell — restructuring to separate services is preferred.","correct":"When a bean injects itself via @Autowired private MyService self, Spring injects the proxy bean (from the context), not this.","distractors":["proxyTargetClass=false (default): Spring creates a JDK dynamic proxy that implements the same interfaces as the target bean.","@Configuration CGLIB proxy's sole purpose is to intercept @Bean method calls and return the singleton from the container (caching semantics).","A request enters Spring Security's FilterChainProxy (a Filter, not a Spring AOP proxy), which runs security filters."],"distractorIds":["spring/spring_proxies#a1bddf490113","spring/spring_proxies#f1a984c3bee3","spring/spring_proxies#263e0bdd2149"],"concepts":["myservice","autowired","trick","avoiding","injects","invocation"],"correctMd":"When a bean injects itself via `@Autowired private MyService self`, Spring injects the proxy bean (from the context), not `this`.","answerFullMd":"When a bean injects itself via `@Autowired private MyService self`, Spring injects the proxy bean (from the context), not `this`. When application code calls `self.method()`, it goes through the proxy (which applies AOP advice) rather than directly calling the method on `this`. The reason Spring can inject the bean's own proxy is that the proxy is already in the singleton registry when field injection runs (phase 2 of lifecycle). This is a valid workaround but is considered a code smell — restructuring to separate services is preferred.","distractorsMd":["`proxyTargetClass=false` (default): Spring creates a JDK dynamic proxy that implements the same interfaces as the target bean.","`@Configuration` CGLIB proxy's sole purpose is to intercept `@Bean` method calls and return the singleton from the container (caching semantics).","A request enters Spring Security's `FilterChainProxy` (a `Filter`, not a Spring AOP proxy), which runs security filters."]},{"id":"spring/spring_proxies#a1bddf490113","section":"spring","module":"spring/spring_proxies","moduleName":"spring proxies","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between proxyTargetClass=true and proxyTargetClass=false, and when does Spring choose each automatically?","answerFull":"proxyTargetClass=false (default): Spring creates a JDK dynamic proxy that implements the same interfaces as the target bean. The proxy is injected anywhere the interface type is expected. If you try to inject by concrete class, NoSuchBeanDefinitionException or a ClassCastException occurs. proxyTargetClass=true: Spring creates a CGLIB subclass proxy that extends the concrete class. Injection by both interface and concrete type works. Spring Boot sets spring.aop.proxy-target-class=true by default (Boot 2.0+), meaning CGLIB is used everywhere unless the bean implements an interface and proxy-target-class is explicitly set to false. The implication: in Spring Boot, beans can always be injected by concrete type safely.","correct":"proxyTargetClass=false (default): Spring creates a JDK dynamic proxy that implements the same interfaces as the target bean.","distractors":["A CGLIB-proxied bean and a plain instance of the same class are never equal() by default, because the proxy's runtime class differs from the original class.","A request enters Spring Security's FilterChainProxy (a Filter, not a Spring AOP proxy), which runs security filters.","@Configuration CGLIB proxy's sole purpose is to intercept @Bean method calls and return the singleton from the container (caching semantics)."],"distractorIds":["spring/spring_proxies#af723f0f1cd3","spring/spring_proxies#263e0bdd2149","spring/spring_proxies#f1a984c3bee3"],"concepts":["proxytargetclass","difference","jdk","interfaces","implements","dynamic"],"questionMd":"What is the difference between `proxyTargetClass=true` and `proxyTargetClass=false`, and when does Spring choose each automatically?","correctMd":"`proxyTargetClass=false` (default): Spring creates a JDK dynamic proxy that implements the same interfaces as the target bean.","answerFullMd":"`proxyTargetClass=false` (default): Spring creates a JDK dynamic proxy that implements the same interfaces as the target bean. The proxy is injected anywhere the interface type is expected. If you try to inject by concrete class, `NoSuchBeanDefinitionException` or a `ClassCastException` occurs. `proxyTargetClass=true`: Spring creates a CGLIB subclass proxy that extends the concrete class. Injection by both interface and concrete type works. Spring Boot sets `spring.aop.proxy-target-class=true` by default (Boot 2.0+), meaning CGLIB is used everywhere unless the bean implements an interface and `proxy-target-class` is explicitly set to `false`. The implication: in Spring Boot, beans can always be injected by concrete type safely.","distractorsMd":["A CGLIB-proxied bean and a plain instance of the same class are never `equal()` by default, because the proxy's runtime class differs from the original class.","A request enters Spring Security's `FilterChainProxy` (a `Filter`, not a Spring AOP proxy), which runs security filters.","`@Configuration` CGLIB proxy's sole purpose is to intercept `@Bean` method calls and return the singleton from the container (caching semantics)."]},{"id":"spring/spring_proxies#e29161ee4c48","section":"spring","module":"spring/spring_proxies","moduleName":"spring proxies","sourceFile":"README.md","difficulty":"advanced","question":"How does Spring's proxy mechanism interact with @Transactional on a final method?","answerFull":"CGLIB subclasses cannot override final methods — they are not intercepted by the proxy. A @Transactional annotation on a final method is silently ignored: the method is called directly on the target object without any transaction management. Spring does not log a warning by default. Diagnosis: add logging.level.org.springframework.aop=DEBUG — Spring logs when it cannot proxy a method. Fix: remove final from the method (or use @Transactional on the class and ensure no methods are final). The same applies to any AOP advice on final methods — @Cacheable, @Async, @Secured all fail silently on final methods.","correct":"CGLIB subclasses cannot override final methods — they are not intercepted by the proxy.","distractors":["A request enters Spring Security's FilterChainProxy (a Filter, not a Spring AOP proxy), which runs security filters.","CGLIB cannot override final methods.","proxyTargetClass=false (default): Spring creates a JDK dynamic proxy that implements the same interfaces as the target bean."],"distractorIds":["spring/spring_proxies#263e0bdd2149","spring/spring_proxies#38b517c71985","spring/spring_proxies#a1bddf490113"],"concepts":["intercepted","interact","cglib","override","transactional","subclasses"],"questionMd":"How does Spring's proxy mechanism interact with `@Transactional` on a final method?","correctMd":"CGLIB subclasses cannot override `final` methods — they are not intercepted by the proxy.","answerFullMd":"CGLIB subclasses cannot override `final` methods — they are not intercepted by the proxy. A `@Transactional` annotation on a `final` method is silently ignored: the method is called directly on the target object without any transaction management. Spring does not log a warning by default. Diagnosis: add `logging.level.org.springframework.aop=DEBUG` — Spring logs when it cannot proxy a method. Fix: remove `final` from the method (or use `@Transactional` on the class and ensure no methods are final). The same applies to any AOP advice on `final` methods — `@Cacheable`, `@Async`, `@Secured` all fail silently on `final` methods.","distractorsMd":["A request enters Spring Security's `FilterChainProxy` (a `Filter`, not a Spring AOP proxy), which runs security filters.","CGLIB cannot override `final` methods.","`proxyTargetClass=false` (default): Spring creates a JDK dynamic proxy that implements the same interfaces as the target bean."]},{"id":"spring/spring_proxies#af7adcdf4f76","section":"spring","module":"spring/spring_proxies","moduleName":"spring proxies","sourceFile":"README.md","difficulty":"advanced","question":"What is InfrastructureAdvisorAutoProxyCreator and how does it differ from AnnotationAwareAspectJAutoProxyCreator?","answerFull":"Both are BeanPostProcessor implementations that create AOP proxies. InfrastructureAdvisorAutoProxyCreator only applies advisor beans with the ROLE_INFRASTRUCTURE role — internal Spring framework advisors (transaction advisor, async advisor, caching advisor). It is registered first and handles Spring's built-in AOP. AnnotationAwareAspectJAutoProxyCreator handles all advisors including user-defined @Aspect classes. It replaces InfrastructureAdvisorAutoProxyCreator when @EnableAspectJAutoProxy is present. Key implication: if both infrastructure advisors and user @Aspect classes apply to the same bean, they all go through AnnotationAwareAspectJAutoProxyCreator, and their relative order is controlled by @Order on the advisors.","correct":"Both are BeanPostProcessor implementations that create AOP proxies.","distractors":["It forces CGLIB proxies for all Spring AOP-proxied beans, even those implementing interfaces.","A request enters Spring Security's FilterChainProxy (a Filter, not a Spring AOP proxy), which runs security filters.","@Configuration CGLIB proxy's sole purpose is to intercept @Bean method calls and return the singleton from the container (caching semantics)."],"distractorIds":["spring/spring_proxies#d4d5ad9dd9b6","spring/spring_proxies#263e0bdd2149","spring/spring_proxies#f1a984c3bee3"],"concepts":["annotationawareaspectjautoproxycreator","beanpostprocessor","implementations","differ","aop","proxies"],"questionMd":"What is `InfrastructureAdvisorAutoProxyCreator` and how does it differ from `AnnotationAwareAspectJAutoProxyCreator`?","correctMd":"Both are `BeanPostProcessor` implementations that create AOP proxies.","answerFullMd":"Both are `BeanPostProcessor` implementations that create AOP proxies. `InfrastructureAdvisorAutoProxyCreator` only applies advisor beans with the `ROLE_INFRASTRUCTURE` role — internal Spring framework advisors (transaction advisor, async advisor, caching advisor). It is registered first and handles Spring's built-in AOP. `AnnotationAwareAspectJAutoProxyCreator` handles all advisors including user-defined `@Aspect` classes. It replaces `InfrastructureAdvisorAutoProxyCreator` when `@EnableAspectJAutoProxy` is present. Key implication: if both infrastructure advisors and user `@Aspect` classes apply to the same bean, they all go through `AnnotationAwareAspectJAutoProxyCreator`, and their relative order is controlled by `@Order` on the advisors.","distractorsMd":[null,"A request enters Spring Security's `FilterChainProxy` (a `Filter`, not a Spring AOP proxy), which runs security filters.","`@Configuration` CGLIB proxy's sole purpose is to intercept `@Bean` method calls and return the singleton from the container (caching semantics)."]},{"id":"spring/spring_proxies#b35bbf8981da","section":"spring","module":"spring/spring_proxies","moduleName":"spring proxies","sourceFile":"README.md","difficulty":"advanced","question":"How does AopContext.currentProxy() fix self-invocation, and what must you enable for it to work?","answerFull":"AopContext.currentProxy() retrieves the current proxy from a ThreadLocal, so calling the returned proxy re-enters the interceptor chain instead of calling this directly. It only works when exposeProxy=true is set on @EnableAspectJAutoProxy (or the XML equivalent), which makes the auto-proxy creator publish the proxy to the ThreadLocal before invoking the target — without it, AopContext.currentProxy() throws IllegalStateException. The cast ((MyService) AopContext.currentProxy()) couples business code to Spring AOP internals, so most teams prefer extracting the advised method into a separate bean instead of relying on this workaround.","correct":"AopContext.currentProxy() retrieves the current proxy from a ThreadLocal, so calling the returned proxy re-enters the interceptor chain instead of calling this directly.","distractors":["@Configuration CGLIB proxy's sole purpose is to intercept @Bean method calls and return the singleton from the container (caching semantics).","JDK dynamic proxy (uses java.lang.reflect.Proxy) is used when the target bean implements at least one interface and proxyTargetClass=false.","CGLIB subclasses cannot override final methods — they are not intercepted by the proxy."],"distractorIds":["spring/spring_proxies#f1a984c3bee3","spring/spring_proxies#0f61c489f42b","spring/spring_proxies#e29161ee4c48"],"concepts":["aopcontext","currentproxy","enters","interceptor","retrieves","invocation"],"questionMd":"How does `AopContext.currentProxy()` fix self-invocation, and what must you enable for it to work?","correctMd":"`AopContext.currentProxy()` retrieves the current proxy from a `ThreadLocal`, so calling the returned proxy re-enters the interceptor chain instead of calling `this` directly.","answerFullMd":"`AopContext.currentProxy()` retrieves the current proxy from a `ThreadLocal`, so calling the returned proxy re-enters the interceptor chain instead of calling `this` directly. It only works when `exposeProxy=true` is set on `@EnableAspectJAutoProxy` (or the XML equivalent), which makes the auto-proxy creator publish the proxy to the `ThreadLocal` before invoking the target — without it, `AopContext.currentProxy()` throws `IllegalStateException`. The cast (`(MyService) AopContext.currentProxy()`) couples business code to Spring AOP internals, so most teams prefer extracting the advised method into a separate bean instead of relying on this workaround.","distractorsMd":["`@Configuration` CGLIB proxy's sole purpose is to intercept `@Bean` method calls and return the singleton from the container (caching semantics).","JDK dynamic proxy (uses `java.lang.reflect.Proxy`) is used when the target bean implements at least one interface and `proxyTargetClass=false`.","CGLIB subclasses cannot override `final` methods — they are not intercepted by the proxy."]},{"id":"spring/spring_proxies#f0376b951c0f","section":"spring","module":"spring/spring_proxies","moduleName":"spring proxies","sourceFile":"README.md","difficulty":"advanced","question":"Can CGLIB advise static or private methods, and why not?","answerFull":"No: CGLIB proxies subclass the target and override methods, and neither static methods (resolved at compile time) nor private methods (invisible to a subclass) can be overridden this way. This is a distinct limitation from final methods — a final method exists on the instance and could in principle be dispatched dynamically, but the JVM specifically forbids overriding it, whereas static/private methods are excluded by Java's dispatch rules regardless of any modifier. Any @Transactional, @Cacheable, or @Async on a static or private method is silently ignored, with no warning at startup — always keep advised methods public and non-static.","correct":"No: CGLIB proxies subclass the target and override methods, and neither static methods (resolved at compile time) nor private methods (invisible to a subclass) can be overridden this way.","distractors":["CGLIB cannot override final methods.","A CGLIB-proxied bean and a plain instance of the same class are never equal() by default, because the proxy's runtime class differs from the original class.","When a bean injects itself via @Autowired private MyService self, Spring injects the proxy bean (from the context), not this."],"distractorIds":["spring/spring_proxies#38b517c71985","spring/spring_proxies#af723f0f1cd3","spring/spring_proxies#0add8ae04409"],"concepts":["invisible","nor","overridden","resolved","cglib","override"],"questionMd":"Can CGLIB advise `static` or `private` methods, and why not?","answerFullMd":"No: CGLIB proxies subclass the target and override methods, and neither static methods (resolved at compile time) nor private methods (invisible to a subclass) can be overridden this way. This is a distinct limitation from `final` methods — a `final` method exists on the instance and could in principle be dispatched dynamically, but the JVM specifically forbids overriding it, whereas `static`/`private` methods are excluded by Java's dispatch rules regardless of any modifier. Any `@Transactional`, `@Cacheable`, or `@Async` on a `static` or `private` method is silently ignored, with no warning at startup — always keep advised methods public and non-static.","distractorsMd":["CGLIB cannot override `final` methods.","A CGLIB-proxied bean and a plain instance of the same class are never `equal()` by default, because the proxy's runtime class differs from the original class.","When a bean injects itself via `@Autowired private MyService self`, Spring injects the proxy bean (from the context), not `this`."]},{"id":"spring/spring_proxies#af723f0f1cd3","section":"spring","module":"spring/spring_proxies","moduleName":"spring proxies","sourceFile":"README.md","difficulty":"advanced","question":"How does a CGLIB proxy affect equals()/hashCode(), and why can that break HashSet/HashMap lookups?","answerFull":"A CGLIB-proxied bean and a plain instance of the same class are never equal() by default, because the proxy's runtime class differs from the original class. Unless the target overrides equals()/hashCode() based on business fields, comparisons fall back to identity semantics inherited through the generated subclass, so storing a proxied bean in a HashSet/HashMap and later probing it with a manually constructed instance silently misses. The fix is to compare by a stable identifier field or unwrap the proxy first with AopUtils.getTargetClass() / AopTestUtils.getTargetObject(), rather than relying on default object equality across proxied and non-proxied instances.","correct":"A CGLIB-proxied bean and a plain instance of the same class are never equal() by default, because the proxy's runtime class differs from the original class.","distractors":["When you call this.method() inside a Spring bean, this refers to the raw bean object, not the proxy.","CGLIB works by generating a subclass of the target class at runtime using bytecode generation.","proxyTargetClass=false (default): Spring creates a JDK dynamic proxy that implements the same interfaces as the target bean."],"distractorIds":["spring/spring_proxies#eacaab9c670f","spring/spring_proxies#b582032f8c0a","spring/spring_proxies#a1bddf490113"],"concepts":["hashset","proxied","hashcode","lookups","affect","differs"],"questionMd":"How does a CGLIB proxy affect `equals()`/`hashCode()`, and why can that break `HashSet`/`HashMap` lookups?","correctMd":"A CGLIB-proxied bean and a plain instance of the same class are never `equal()` by default, because the proxy's runtime class differs from the original class.","answerFullMd":"A CGLIB-proxied bean and a plain instance of the same class are never `equal()` by default, because the proxy's runtime class differs from the original class. Unless the target overrides `equals()`/`hashCode()` based on business fields, comparisons fall back to identity semantics inherited through the generated subclass, so storing a proxied bean in a `HashSet`/`HashMap` and later probing it with a manually constructed instance silently misses. The fix is to compare by a stable identifier field or unwrap the proxy first with `AopUtils.getTargetClass()` / `AopTestUtils.getTargetObject()`, rather than relying on default object equality across proxied and non-proxied instances.","distractorsMd":["When you call `this.method()` inside a Spring bean, `this` refers to the raw bean object, not the proxy.",null,"`proxyTargetClass=false` (default): Spring creates a JDK dynamic proxy that implements the same interfaces as the target bean."]},{"id":"spring/spring_security_architecture#58d75f2a1377","section":"spring","module":"spring/spring_security_architecture","moduleName":"spring security architecture","sourceFile":"README.md","difficulty":"core","question":"What is FilterChainProxy and how does it differ from a regular servlet filter?","answerFull":"FilterChainProxy is a single javax.servlet.Filter registered in the servlet container that internally delegates to one or more SecurityFilterChain instances based on request matching. Unlike a regular filter registered directly in the container, FilterChainProxy allows multiple independent filter chains, each with different URL matchers. Only the first matching chain processes the request. This enables distinct security configurations for, say, /api/ (stateless JWT) and /admin/ (form login with session).","correct":"FilterChainProxy is a single javax.servlet.Filter registered in the servlet container that internally delegates to one or more SecurityFilterChain instances based on request matching.","distractors":["WebSecurityConfigurerAdapter was removed; all configuration must now be SecurityFilterChain beans.","You can define several SecurityFilterChain beans, each with its own securityMatcher (e.g. one for /api/ that is stateless + JWT, one for everything else that is form-login + sessions).","Annotate each SecurityFilterChain @Bean with @Order to set priority."],"distractorIds":["spring/spring_security_architecture#22d8a4c9c0bf","spring/spring_security_architecture#cefdfd66d3a7","spring/spring_security_architecture#3ce54a8c67b2"],"concepts":["filterchainproxy","javax","delegates","securityfilterchain","internally","regular"],"correctMd":"`FilterChainProxy` is a single `javax.servlet.Filter` registered in the servlet container that internally delegates to one or more `SecurityFilterChain` instances based on request matching.","answerFullMd":"`FilterChainProxy` is a single `javax.servlet.Filter` registered in the servlet container that internally delegates to one or more `SecurityFilterChain` instances based on request matching. Unlike a regular filter registered directly in the container, `FilterChainProxy` allows multiple independent filter chains, each with different URL matchers. Only the first matching chain processes the request. This enables distinct security configurations for, say, `/api/**` (stateless JWT) and `/admin/**` (form login with session).","distractorsMd":["`WebSecurityConfigurerAdapter` was removed; all configuration must now be `SecurityFilterChain` beans.","You can define several `SecurityFilterChain` beans, each with its own `securityMatcher` (e.g. one for `/api/**` that is stateless + JWT, one for everything else that is form-login + sessions).","Annotate each `SecurityFilterChain` `@Bean` with `@Order` to set priority."]},{"id":"spring/spring_security_architecture#09ac506a344c","section":"spring","module":"spring/spring_security_architecture","moduleName":"spring security architecture","sourceFile":"README.md","difficulty":"core","question":"Walk through a successful username/password authentication in Spring Security.","answerFull":"The request hits UsernamePasswordAuthenticationFilter, which extracts credentials and creates an unauthenticated UsernamePasswordAuthenticationToken. This token is passed to ProviderManager.authenticate(), which iterates its list of AuthenticationProviders. DaoAuthenticationProvider calls UserDetailsService.loadUserByUsername() to retrieve the UserDetails, then calls PasswordEncoder.matches(rawPassword, storedHash). On success, a fully authenticated token is created with granted authorities. SecurityContextHolder.getContext().setAuthentication(token) stores it. AuthenticationSuccessHandler redirects or returns a response.","correct":"The request hits UsernamePasswordAuthenticationFilter, which extracts credentials and creates an unauthenticated UsernamePasswordAuthenticationToken.","distractors":["Spring Security evaluates authorization matchers top-to-bottom and applies the *first* one that matches the request, ignoring the rest.","STATELESS instructs Spring Security to never create or read an HttpSession.","By default, Spring Security generates a CSRF token per session stored in HttpSession and expects it in subsequent state-changing requests (POST, PUT, DELETE, PATCH) as either a form field or request header X-CSRF-Token."],"distractorIds":["spring/spring_security_architecture#e1abc45b83d7","spring/spring_security_architecture#07d95efe0117","spring/spring_security_architecture#0f831958c07d"],"concepts":["usernamepasswordauthenticationfilter","usernamepasswordauthenticationtoken","username","walk","unauthenticated","extracts"],"correctMd":"The request hits `UsernamePasswordAuthenticationFilter`, which extracts credentials and creates an unauthenticated `UsernamePasswordAuthenticationToken`.","answerFullMd":"The request hits `UsernamePasswordAuthenticationFilter`, which extracts credentials and creates an unauthenticated `UsernamePasswordAuthenticationToken`. This token is passed to `ProviderManager.authenticate()`, which iterates its list of `AuthenticationProvider`s. `DaoAuthenticationProvider` calls `UserDetailsService.loadUserByUsername()` to retrieve the `UserDetails`, then calls `PasswordEncoder.matches(rawPassword, storedHash)`. On success, a fully authenticated token is created with granted authorities. `SecurityContextHolder.getContext().setAuthentication(token)` stores it. `AuthenticationSuccessHandler` redirects or returns a response.","distractorsMd":[null,"`STATELESS` instructs Spring Security to never create or read an `HttpSession`.","By default, Spring Security generates a CSRF token per session stored in `HttpSession` and expects it in subsequent state-changing requests (POST, PUT, DELETE, PATCH) as either a form field or request header `X-CSRF-Token`."]},{"id":"spring/spring_security_architecture#9099ce4b9282","section":"spring","module":"spring/spring_security_architecture","moduleName":"spring security architecture","sourceFile":"README.md","difficulty":"core","question":"What is the difference between hasRole() and hasAuthority() in Spring Security?","answerFull":"hasRole('ADMIN') automatically prepends the ROLE_ prefix and checks for the authority ROLE_ADMIN. hasAuthority('ADMIN') checks for the exact string ADMIN without any prefix manipulation. When using User.builder().roles(\"ADMIN\"), the authority is stored as ROLE_ADMIN, so hasRole('ADMIN') is the correct match. When using User.builder().authorities(\"ADMIN\"), use hasAuthority('ADMIN'). Mixing them (e.g., storing with authorities(\"ADMIN\") but checking with hasRole(\"ADMIN\")) results in authorization always failing silently.","correct":"hasRole('ADMIN') automatically prepends the ROLE_ prefix and checks for the authority ROLE_ADMIN.","distractors":["STATELESS instructs Spring Security to never create or read an HttpSession.","By default, Spring Security generates a CSRF token per session stored in HttpSession and expects it in subsequent state-changing requests (POST, PUT, DELETE, PATCH) as either a form field or request header X-CSRF-Token.","Spring Security evaluates authorization matchers top-to-bottom and applies the *first* one that matches the request, ignoring the rest."],"distractorIds":["spring/spring_security_architecture#07d95efe0117","spring/spring_security_architecture#0f831958c07d","spring/spring_security_architecture#e1abc45b83d7"],"concepts":["hasrole","prepends","authority","admin","difference","role"],"questionMd":"What is the difference between `hasRole()` and `hasAuthority()` in Spring Security?","correctMd":"`hasRole('ADMIN')` automatically prepends the `ROLE_` prefix and checks for the authority `ROLE_ADMIN`.","answerFullMd":"`hasRole('ADMIN')` automatically prepends the `ROLE_` prefix and checks for the authority `ROLE_ADMIN`. `hasAuthority('ADMIN')` checks for the exact string `ADMIN` without any prefix manipulation. When using `User.builder().roles(\"ADMIN\")`, the authority is stored as `ROLE_ADMIN`, so `hasRole('ADMIN')` is the correct match. When using `User.builder().authorities(\"ADMIN\")`, use `hasAuthority('ADMIN')`. Mixing them (e.g., storing with `authorities(\"ADMIN\")` but checking with `hasRole(\"ADMIN\")`) results in authorization always failing silently.","distractorsMd":["`STATELESS` instructs Spring Security to never create or read an `HttpSession`.","By default, Spring Security generates a CSRF token per session stored in `HttpSession` and expects it in subsequent state-changing requests (POST, PUT, DELETE, PATCH) as either a form field or request header `X-CSRF-Token`.",null]},{"id":"spring/spring_security_architecture#22d8a4c9c0bf","section":"spring","module":"spring/spring_security_architecture","moduleName":"spring security architecture","sourceFile":"README.md","difficulty":"core","question":"What changed between Spring Security 5.x and 6.x (Spring Boot 2.x vs 3.x)?","answerFull":"WebSecurityConfigurerAdapter was removed; all configuration must now be SecurityFilterChain beans. antMatchers() was replaced by requestMatchers(). Lambda DSL is required; method-chaining without lambdas no longer works. @EnableGlobalMethodSecurity was deprecated in 5.6 and removed conceptually in favor of @EnableMethodSecurity. authorizeRequests() was replaced by authorizeHttpRequests(). LDAP, CAS, and various adapters moved to separate modules.","correct":"WebSecurityConfigurerAdapter was removed; all configuration must now be SecurityFilterChain beans.","distractors":["STATELESS instructs Spring Security to never create or read an HttpSession.","By default, Spring Security generates a CSRF token per session stored in HttpSession and expects it in subsequent state-changing requests (POST, PUT, DELETE, PATCH) as either a form field or request header X-CSRF-Token.","Spring Security evaluates authorization matchers top-to-bottom and applies the *first* one that matches the request, ignoring the rest."],"distractorIds":["spring/spring_security_architecture#07d95efe0117","spring/spring_security_architecture#0f831958c07d","spring/spring_security_architecture#e1abc45b83d7"],"concepts":["websecurityconfigureradapter","securityfilterchain","removed","now","changed","boot"],"correctMd":"`WebSecurityConfigurerAdapter` was removed; all configuration must now be `SecurityFilterChain` beans.","answerFullMd":"`WebSecurityConfigurerAdapter` was removed; all configuration must now be `SecurityFilterChain` beans. `antMatchers()` was replaced by `requestMatchers()`. Lambda DSL is required; method-chaining without lambdas no longer works. `@EnableGlobalMethodSecurity` was deprecated in 5.6 and removed conceptually in favor of `@EnableMethodSecurity`. `authorizeRequests()` was replaced by `authorizeHttpRequests()`. LDAP, CAS, and various adapters moved to separate modules.","distractorsMd":["`STATELESS` instructs Spring Security to never create or read an `HttpSession`.","By default, Spring Security generates a CSRF token per session stored in `HttpSession` and expects it in subsequent state-changing requests (POST, PUT, DELETE, PATCH) as either a form field or request header `X-CSRF-Token`.",null]},{"id":"spring/spring_security_architecture#b9a18c177dcd","section":"spring","module":"spring/spring_security_architecture","moduleName":"spring security architecture","sourceFile":"README.md","difficulty":"core","question":"How does ProviderManager work when multiple AuthenticationProviders are registered?","answerFull":"ProviderManager iterates its List<AuthenticationProvider> and calls supports(Class) on each to find compatible providers. For each compatible provider, it calls authenticate(authentication). If the provider returns a non-null fully authenticated token, iteration stops and that result is returned. If the provider throws AuthenticationException, it is recorded but iteration continues to the next provider. If all providers fail or abstain, ProviderManager throws the last AuthenticationException or delegates to a parent ProviderManager if configured.","correct":"ProviderManager iterates its List<AuthenticationProvider> and calls supports(Class) on each to find compatible providers.","distractors":["FilterChainProxy is a single javax.servlet.Filter registered in the servlet container that internally delegates to one or more SecurityFilterChain instances based on request matching.","@PreAuthorize evaluates its SpEL expression before the method executes; if the expression returns false, AccessDeniedException is thrown immediately and the method body never runs.","@PreFilter filters elements of an input collection before the method receives it; only elements where the SpEL expression returns true are passed to the method."],"distractorIds":["spring/spring_security_architecture#58d75f2a1377","spring/spring_security_architecture#8e50905247b4","spring/spring_security_architecture#a41a97283c28"],"concepts":["authenticationprovider","providermanager","iterates","providers","compatible","registered"],"questionMd":"How does `ProviderManager` work when multiple `AuthenticationProvider`s are registered?","correctMd":"`ProviderManager` iterates its `List<AuthenticationProvider>` and calls `supports(Class)` on each to find compatible providers.","answerFullMd":"`ProviderManager` iterates its `List<AuthenticationProvider>` and calls `supports(Class)` on each to find compatible providers. For each compatible provider, it calls `authenticate(authentication)`. If the provider returns a non-null fully authenticated token, iteration stops and that result is returned. If the provider throws `AuthenticationException`, it is recorded but iteration continues to the next provider. If all providers fail or abstain, `ProviderManager` throws the last `AuthenticationException` or delegates to a parent `ProviderManager` if configured.","distractorsMd":["`FilterChainProxy` is a single `javax.servlet.Filter` registered in the servlet container that internally delegates to one or more `SecurityFilterChain` instances based on request matching.","`@PreAuthorize` evaluates its SpEL expression before the method executes; if the expression returns false, `AccessDeniedException` is thrown immediately and the method body never runs.","`@PreFilter` filters elements of an input collection before the method receives it; only elements where the SpEL expression returns true are passed to the method."]},{"id":"spring/spring_security_architecture#0f831958c07d","section":"spring","module":"spring/spring_security_architecture","moduleName":"spring security architecture","sourceFile":"README.md","difficulty":"intermediate","question":"How does Spring Security protect against CSRF attacks?","answerFull":"By default, Spring Security generates a CSRF token per session stored in HttpSession and expects it in subsequent state-changing requests (POST, PUT, DELETE, PATCH) as either a form field or request header X-CSRF-Token. On each request, CsrfFilter compares the submitted token with the session-stored token; mismatch results in 403. For REST APIs using stateless JWT (no session, no cookies), CSRF is not a risk and csrf.disable() is appropriate. SameSite=Strict cookies are an alternative protection that prevents cross-origin form submissions.","correct":"By default, Spring Security generates a CSRF token per session stored in HttpSession and expects it in subsequent state-changing requests (POST, PUT, DELETE, PATCH) as either a form field or request header X-CSRF-Token.","distractors":["SecurityContextHolder uses ThreadLocal by default, so spawned threads see an empty context.","You can define several SecurityFilterChain beans, each with its own securityMatcher (e.g. one for /api/ that is stateless + JWT, one for everything else that is form-login + sessions).","FilterChainProxy is a single javax.servlet.Filter registered in the servlet container that internally delegates to one or more SecurityFilterChain instances based on request matching."],"distractorIds":["spring/spring_security_architecture#ab2ec8b9e751","spring/spring_security_architecture#cefdfd66d3a7","spring/spring_security_architecture#58d75f2a1377"],"concepts":["protect","csrf","expects","httpsession","changing","patch"],"correctMd":"By default, Spring Security generates a CSRF token per session stored in `HttpSession` and expects it in subsequent state-changing requests (POST, PUT, DELETE, PATCH) as either a form field or request header `X-CSRF-Token`.","answerFullMd":"By default, Spring Security generates a CSRF token per session stored in `HttpSession` and expects it in subsequent state-changing requests (POST, PUT, DELETE, PATCH) as either a form field or request header `X-CSRF-Token`. On each request, `CsrfFilter` compares the submitted token with the session-stored token; mismatch results in 403. For REST APIs using stateless JWT (no session, no cookies), CSRF is not a risk and `csrf.disable()` is appropriate. `SameSite=Strict` cookies are an alternative protection that prevents cross-origin form submissions.","distractorsMd":["`SecurityContextHolder` uses `ThreadLocal` by default, so spawned threads see an empty context.","You can define several `SecurityFilterChain` beans, each with its own `securityMatcher` (e.g. one for `/api/**` that is stateless + JWT, one for everything else that is form-login + sessions).","`FilterChainProxy` is a single `javax.servlet.Filter` registered in the servlet container that internally delegates to one or more `SecurityFilterChain` instances based on request matching."]},{"id":"spring/spring_security_architecture#ab2ec8b9e751","section":"spring","module":"spring/spring_security_architecture","moduleName":"spring security architecture","sourceFile":"README.md","difficulty":"intermediate","question":"How do you propagate the SecurityContext to threads spawned by @Async methods?","answerFull":"SecurityContextHolder uses ThreadLocal by default, so spawned threads see an empty context. Use DelegatingSecurityContextAsyncTaskExecutor which wraps the delegate Executor and copies the SecurityContext from the calling thread to each submitted task before execution. Configure it as the @Async executor in an AsyncConfigurer implementation or qualify it on the @Async(\"executorName\") annotation.","correct":"SecurityContextHolder uses ThreadLocal by default, so spawned threads see an empty context.","distractors":["By default, Spring Security generates a CSRF token per session stored in HttpSession and expects it in subsequent state-changing requests (POST, PUT, DELETE, PATCH) as either a form field or request header X-CSRF-Token.","@PreAuthorize evaluates its SpEL expression before the method executes; if the expression returns false, AccessDeniedException is thrown immediately and the method body never runs.","@PreFilter filters elements of an input collection before the method receives it; only elements where the SpEL expression returns true are passed to the method."],"distractorIds":["spring/spring_security_architecture#0f831958c07d","spring/spring_security_architecture#8e50905247b4","spring/spring_security_architecture#a41a97283c28"],"concepts":["securitycontext","securitycontextholder","spawned","propagate","threadlocal","empty"],"questionMd":"How do you propagate the SecurityContext to threads spawned by `@Async` methods?","correctMd":"`SecurityContextHolder` uses `ThreadLocal` by default, so spawned threads see an empty context.","answerFullMd":"`SecurityContextHolder` uses `ThreadLocal` by default, so spawned threads see an empty context. Use `DelegatingSecurityContextAsyncTaskExecutor` which wraps the delegate `Executor` and copies the `SecurityContext` from the calling thread to each submitted task before execution. Configure it as the `@Async` executor in an `AsyncConfigurer` implementation or qualify it on the `@Async(\"executorName\")` annotation.","distractorsMd":["By default, Spring Security generates a CSRF token per session stored in `HttpSession` and expects it in subsequent state-changing requests (POST, PUT, DELETE, PATCH) as either a form field or request header `X-CSRF-Token`.","`@PreAuthorize` evaluates its SpEL expression before the method executes; if the expression returns false, `AccessDeniedException` is thrown immediately and the method body never runs.","`@PreFilter` filters elements of an input collection before the method receives it; only elements where the SpEL expression returns true are passed to the method."]},{"id":"spring/spring_security_architecture#32fd3620b2eb","section":"spring","module":"spring/spring_security_architecture","moduleName":"spring security architecture","sourceFile":"README.md","difficulty":"intermediate","question":"What is DelegatingPasswordEncoder and when is it necessary?","answerFull":"DelegatingPasswordEncoder stores a prefix with each hashed password indicating which algorithm was used (e.g., {bcrypt}$2a$12$..., {argon2}...). On matches(), it reads the prefix, selects the appropriate PasswordEncoder, and delegates. It is essential for password migration — existing hashed passwords stored with an older algorithm (MD5, SHA-1, plain BCrypt cost 4) continue to work while new passwords are stored with the current preferred algorithm. PasswordEncoderFactories.createDelegatingPasswordEncoder() provides a production-ready instance with BCrypt as the default.","correct":"DelegatingPasswordEncoder stores a prefix with each hashed password indicating which algorithm was used (e.g., {bcrypt}$2a$12$..., {argon2}...).","distractors":["hasRole('ADMIN') automatically prepends the ROLE_ prefix and checks for the authority ROLE_ADMIN.","WebSecurityConfigurerAdapter was removed; all configuration must now be SecurityFilterChain beans.","@PreAuthorize evaluates its SpEL expression before the method executes; if the expression returns false, AccessDeniedException is thrown immediately and the method body never runs."],"distractorIds":["spring/spring_security_architecture#9099ce4b9282","spring/spring_security_architecture#22d8a4c9c0bf","spring/spring_security_architecture#8e50905247b4"],"concepts":["delegatingpasswordencoder","argon2","hashed","bcrypt","indicating","password"],"questionMd":"What is `DelegatingPasswordEncoder` and when is it necessary?","correctMd":"`DelegatingPasswordEncoder` stores a prefix with each hashed password indicating which algorithm was used (e.g., `{bcrypt}$2a$12$...`, `{argon2}...`).","answerFullMd":"`DelegatingPasswordEncoder` stores a prefix with each hashed password indicating which algorithm was used (e.g., `{bcrypt}$2a$12$...`, `{argon2}...`). On `matches()`, it reads the prefix, selects the appropriate `PasswordEncoder`, and delegates. It is essential for password migration — existing hashed passwords stored with an older algorithm (MD5, SHA-1, plain BCrypt cost 4) continue to work while new passwords are stored with the current preferred algorithm. `PasswordEncoderFactories.createDelegatingPasswordEncoder()` provides a production-ready instance with BCrypt as the default.","distractorsMd":["`hasRole('ADMIN')` automatically prepends the `ROLE_` prefix and checks for the authority `ROLE_ADMIN`.","`WebSecurityConfigurerAdapter` was removed; all configuration must now be `SecurityFilterChain` beans.","`@PreAuthorize` evaluates its SpEL expression before the method executes; if the expression returns false, `AccessDeniedException` is thrown immediately and the method body never runs."]},{"id":"spring/spring_security_architecture#c513b38bb293","section":"spring","module":"spring/spring_security_architecture","moduleName":"spring security architecture","sourceFile":"README.md","difficulty":"intermediate","question":"Explain the role of ExceptionTranslationFilter in the security filter chain.","answerFull":"ExceptionTranslationFilter wraps the remaining filter chain (primarily AuthorizationFilter) in a try-catch block. It catches two exception types: AuthenticationException triggers AuthenticationEntryPoint (which redirects to login or returns 401 for APIs) and AccessDeniedException checks whether the current user is anonymous — if anonymous, triggers AuthenticationEntryPoint; if authenticated, triggers AccessDeniedHandler (returns 403). Without this filter, AccessDeniedException from the authorization layer would propagate as a 500 error instead of a meaningful 403/401.","correct":"ExceptionTranslationFilter wraps the remaining filter chain (primarily AuthorizationFilter) in a try-catch block.","distractors":["FilterChainProxy is a single javax.servlet.Filter registered in the servlet container that internally delegates to one or more SecurityFilterChain instances based on request matching.","Spring Security evaluates authorization matchers top-to-bottom and applies the *first* one that matches the request, ignoring the rest.","STATELESS instructs Spring Security to never create or read an HttpSession."],"distractorIds":["spring/spring_security_architecture#58d75f2a1377","spring/spring_security_architecture#e1abc45b83d7","spring/spring_security_architecture#07d95efe0117"],"concepts":["authorizationfilter","exceptiontranslationfilter","primarily","explain","catch","remaining"],"questionMd":"Explain the role of `ExceptionTranslationFilter` in the security filter chain.","correctMd":"`ExceptionTranslationFilter` wraps the remaining filter chain (primarily `AuthorizationFilter`) in a try-catch block.","answerFullMd":"`ExceptionTranslationFilter` wraps the remaining filter chain (primarily `AuthorizationFilter`) in a try-catch block. It catches two exception types: `AuthenticationException` triggers `AuthenticationEntryPoint` (which redirects to login or returns 401 for APIs) and `AccessDeniedException` checks whether the current user is anonymous — if anonymous, triggers `AuthenticationEntryPoint`; if authenticated, triggers `AccessDeniedHandler` (returns 403). Without this filter, `AccessDeniedException` from the authorization layer would propagate as a 500 error instead of a meaningful 403/401.","distractorsMd":["`FilterChainProxy` is a single `javax.servlet.Filter` registered in the servlet container that internally delegates to one or more `SecurityFilterChain` instances based on request matching.",null,"`STATELESS` instructs Spring Security to never create or read an `HttpSession`."]},{"id":"spring/spring_security_architecture#8e50905247b4","section":"spring","module":"spring/spring_security_architecture","moduleName":"spring security architecture","sourceFile":"README.md","difficulty":"advanced","question":"How does @PostAuthorize differ from @PreAuthorize in terms of execution timing and use case?","answerFull":"@PreAuthorize evaluates its SpEL expression before the method executes; if the expression returns false, AccessDeniedException is thrown immediately and the method body never runs. @PostAuthorize evaluates after the method returns, with access to returnObject via #result; this allows authorizing based on the returned data (e.g., verifying the fetched document belongs to the current user). The method always executes for @PostAuthorize; the side effect (e.g., a database read) already occurred even if access is subsequently denied.","correct":"@PreAuthorize evaluates its SpEL expression before the method executes; if the expression returns false, AccessDeniedException is thrown immediately and the method body never runs.","distractors":["@PreFilter filters elements of an input collection before the method receives it; only elements where the SpEL expression returns true are passed to the method.","Spring Security evaluates authorization matchers top-to-bottom and applies the *first* one that matches the request, ignoring the rest.","STATELESS instructs Spring Security to never create or read an HttpSession."],"distractorIds":["spring/spring_security_architecture#a41a97283c28","spring/spring_security_architecture#e1abc45b83d7","spring/spring_security_architecture#07d95efe0117"],"concepts":["accessdeniedexception","preauthorize","spel","timing","thrown","differ"],"questionMd":"How does `@PostAuthorize` differ from `@PreAuthorize` in terms of execution timing and use case?","correctMd":"`@PreAuthorize` evaluates its SpEL expression before the method executes; if the expression returns false, `AccessDeniedException` is thrown immediately and the method body never runs.","answerFullMd":"`@PreAuthorize` evaluates its SpEL expression before the method executes; if the expression returns false, `AccessDeniedException` is thrown immediately and the method body never runs. `@PostAuthorize` evaluates after the method returns, with access to `returnObject` via `#result`; this allows authorizing based on the returned data (e.g., verifying the fetched document belongs to the current user). The method always executes for `@PostAuthorize`; the side effect (e.g., a database read) already occurred even if access is subsequently denied.","distractorsMd":["`@PreFilter` filters elements of an input collection before the method receives it; only elements where the SpEL expression returns true are passed to the method.",null,"`STATELESS` instructs Spring Security to never create or read an `HttpSession`."]},{"id":"spring/spring_security_architecture#3ce54a8c67b2","section":"spring","module":"spring/spring_security_architecture","moduleName":"spring security architecture","sourceFile":"README.md","difficulty":"advanced","question":"How do you implement multiple SecurityFilterChain instances for different URL prefixes?","answerFull":"Annotate each SecurityFilterChain @Bean with @Order to set priority. Attach a securityMatcher() to each chain to restrict which requests it handles. Spring Security selects the first matching chain. java @Bean @Order(1) public SecurityFilterChain apiChain(HttpSecurity http) throws Exception { http .securityMatcher(\"/api/\") .sessionManagement(sm -> sm.sessionCreationPolicy(SessionCreationPolicy.STATELESS)) .csrf(csrf -> csrf.disable()) .authorizeHttpRequests(auth -> auth.anyRequest().authenticated()) .addFilterBefore(jwtFilter, UsernamePasswordAuthenticationFilter.class); return http.build(); } @Bean @Order(2) public SecurityFilterChain webChain(HttpSecurity http) throws Exception { http .authorizeHttpRequests(auth -> auth .requestMatchers(\"/public/\").permitAll() .anyRequest().authenticated()) .formLogin(Customizer.withDefaults()); return http.build(); }","correct":"Annotate each SecurityFilterChain @Bean with @Order to set priority.","distractors":["WebSecurityConfigurerAdapter was removed; all configuration must now be SecurityFilterChain beans.","FilterChainProxy is a single javax.servlet.Filter registered in the servlet container that internally delegates to one or more SecurityFilterChain instances based on request matching.","You can define several SecurityFilterChain beans, each with its own securityMatcher (e.g. one for /api/ that is stateless + JWT, one for everything else that is form-login + sessions)."],"distractorIds":["spring/spring_security_architecture#22d8a4c9c0bf","spring/spring_security_architecture#58d75f2a1377","spring/spring_security_architecture#cefdfd66d3a7"],"concepts":["annotate","prefixes","securityfilterchain","url","priority","implement"],"correctMd":"Annotate each `SecurityFilterChain` `@Bean` with `@Order` to set priority.","answerFullMd":"Annotate each `SecurityFilterChain` `@Bean` with `@Order` to set priority. Attach a `securityMatcher()` to each chain to restrict which requests it handles. Spring Security selects the first matching chain. ```java @Bean @Order(1) public SecurityFilterChain apiChain(HttpSecurity http) throws Exception { http .securityMatcher(\"/api/**\") .sessionManagement(sm -> sm.sessionCreationPolicy(SessionCreationPolicy.STATELESS)) .csrf(csrf -> csrf.disable()) .authorizeHttpRequests(auth -> auth.anyRequest().authenticated()) .addFilterBefore(jwtFilter, UsernamePasswordAuthenticationFilter.class); return http.build(); } @Bean @Order(2) public SecurityFilterChain webChain(HttpSecurity http) throws Exception { http .authorizeHttpRequests(auth -> auth .requestMatchers(\"/public/**\").permitAll() .anyRequest().authenticated()) .formLogin(Customizer.withDefaults()); return http.build(); } ```","distractorsMd":["`WebSecurityConfigurerAdapter` was removed; all configuration must now be `SecurityFilterChain` beans.","`FilterChainProxy` is a single `javax.servlet.Filter` registered in the servlet container that internally delegates to one or more `SecurityFilterChain` instances based on request matching.","You can define several `SecurityFilterChain` beans, each with its own `securityMatcher` (e.g. one for `/api/**` that is stateless + JWT, one for everything else that is form-login + sessions)."]},{"id":"spring/spring_security_architecture#5d3b7dbd54e8","section":"spring","module":"spring/spring_security_architecture","moduleName":"spring security architecture","sourceFile":"README.md","difficulty":"advanced","question":"What is the AuthorizationManager API introduced in Spring Security 5.6 / 6.x?","answerFull":"AuthorizationManager<T> replaces AccessDecisionManager and its voter pattern. It provides a single check(Supplier<Authentication>, T object) method returning AuthorizationDecision. For HTTP requests, RequestMatcherDelegatingAuthorizationManager maps URL patterns to AuthorizationManager instances. For method security, PreAuthorizeAuthorizationManager evaluates SpEL. The new API is simpler (no voter aggregation logic), lazy (authentication is a Supplier — not resolved unless needed), and directly testable.","correct":"AuthorizationManager<T> replaces AccessDecisionManager and its voter pattern.","distractors":["By default, Spring Security generates a CSRF token per session stored in HttpSession and expects it in subsequent state-changing requests (POST, PUT, DELETE, PATCH) as either a form field or request header X-CSRF-Token.","STATELESS instructs Spring Security to never create or read an HttpSession.","Spring Security evaluates authorization matchers top-to-bottom and applies the *first* one that matches the request, ignoring the rest."],"distractorIds":["spring/spring_security_architecture#0f831958c07d","spring/spring_security_architecture#07d95efe0117","spring/spring_security_architecture#e1abc45b83d7"],"concepts":["accessdecisionmanager","authorizationmanager","voter","introduced","replaces","security"],"questionMd":"What is the `AuthorizationManager` API introduced in Spring Security 5.6 / 6.x?","correctMd":"`AuthorizationManager<T>` replaces `AccessDecisionManager` and its voter pattern.","answerFullMd":"`AuthorizationManager<T>` replaces `AccessDecisionManager` and its voter pattern. It provides a single `check(Supplier<Authentication>, T object)` method returning `AuthorizationDecision`. For HTTP requests, `RequestMatcherDelegatingAuthorizationManager` maps URL patterns to `AuthorizationManager` instances. For method security, `PreAuthorizeAuthorizationManager` evaluates SpEL. The new API is simpler (no voter aggregation logic), lazy (authentication is a `Supplier` — not resolved unless needed), and directly testable.","distractorsMd":["By default, Spring Security generates a CSRF token per session stored in `HttpSession` and expects it in subsequent state-changing requests (POST, PUT, DELETE, PATCH) as either a form field or request header `X-CSRF-Token`.","`STATELESS` instructs Spring Security to never create or read an `HttpSession`.",null]},{"id":"spring/spring_security_architecture#072df083eacc","section":"spring","module":"spring/spring_security_architecture","moduleName":"spring security architecture","sourceFile":"README.md","difficulty":"advanced","question":"How does Spring Security handle OAuth2 JWT validation in a resource server?","answerFull":"Configure http.oauth2ResourceServer(oauth2 -> oauth2.jwt(Customizer.withDefaults())). Spring Security fetches the authorization server's public keys from the JWKS URI (spring.security.oauth2.resourceserver.jwt.jwks-uri), caches them, and validates each incoming JWT's signature, expiry, issuer, and audience. A valid JWT results in a JwtAuthenticationToken placed in the SecurityContextHolder. Authorities are extracted from the scope or roles claim using a configurable JwtAuthenticationConverter. No session is required.","correct":"Configure http.oauth2ResourceServer(oauth2 -> oauth2.jwt(Customizer.withDefaults())).","distractors":["You can define several SecurityFilterChain beans, each with its own securityMatcher (e.g. one for /api/ that is stateless + JWT, one for everything else that is form-login + sessions).","Spring Security evaluates authorization matchers top-to-bottom and applies the *first* one that matches the request, ignoring the rest.","STATELESS instructs Spring Security to never create or read an HttpSession."],"distractorIds":["spring/spring_security_architecture#cefdfd66d3a7","spring/spring_security_architecture#e1abc45b83d7","spring/spring_security_architecture#07d95efe0117"],"concepts":["customizer","withdefaults","oauth2resourceserver","oauth2","configure","handle"],"correctMd":"Configure `http.oauth2ResourceServer(oauth2 -> oauth2.jwt(Customizer.withDefaults()))`.","answerFullMd":"Configure `http.oauth2ResourceServer(oauth2 -> oauth2.jwt(Customizer.withDefaults()))`. Spring Security fetches the authorization server's public keys from the JWKS URI (`spring.security.oauth2.resourceserver.jwt.jwks-uri`), caches them, and validates each incoming JWT's signature, expiry, issuer, and audience. A valid JWT results in a `JwtAuthenticationToken` placed in the `SecurityContextHolder`. Authorities are extracted from the `scope` or `roles` claim using a configurable `JwtAuthenticationConverter`. No session is required.","distractorsMd":["You can define several `SecurityFilterChain` beans, each with its own `securityMatcher` (e.g. one for `/api/**` that is stateless + JWT, one for everything else that is form-login + sessions).",null,"`STATELESS` instructs Spring Security to never create or read an `HttpSession`."]},{"id":"spring/spring_security_architecture#a41a97283c28","section":"spring","module":"spring/spring_security_architecture","moduleName":"spring security architecture","sourceFile":"README.md","difficulty":"advanced","question":"What are @PreFilter and @PostFilter and when do you use them?","answerFull":"@PreFilter filters elements of an input collection before the method receives it; only elements where the SpEL expression returns true are passed to the method. @PostFilter filters elements of the returned collection after execution; only elements where the expression is true remain in the returned list. Use them to enforce row-level security on collection parameters or results — e.g., @PostFilter(\"filterObject.tenantId == authentication.details.tenantId\") ensures a service method can only return objects belonging to the caller's tenant, regardless of what the underlying query returns.","correct":"@PreFilter filters elements of an input collection before the method receives it; only elements where the SpEL expression returns true are passed to the method.","distractors":["@PreAuthorize evaluates its SpEL expression before the method executes; if the expression returns false, AccessDeniedException is thrown immediately and the method body never runs.","Annotate each SecurityFilterChain @Bean with @Order to set priority.","AuthorizationManager<T> replaces AccessDecisionManager and its voter pattern."],"distractorIds":["spring/spring_security_architecture#8e50905247b4","spring/spring_security_architecture#3ce54a8c67b2","spring/spring_security_architecture#5d3b7dbd54e8"],"concepts":["prefilter","spel","collection","filters","expression","passed"],"questionMd":"What are `@PreFilter` and `@PostFilter` and when do you use them?","correctMd":"`@PreFilter` filters elements of an input collection before the method receives it; only elements where the SpEL expression returns true are passed to the method.","answerFullMd":"`@PreFilter` filters elements of an input collection before the method receives it; only elements where the SpEL expression returns true are passed to the method. `@PostFilter` filters elements of the returned collection after execution; only elements where the expression is true remain in the returned list. Use them to enforce row-level security on collection parameters or results — e.g., `@PostFilter(\"filterObject.tenantId == authentication.details.tenantId\")` ensures a service method can only return objects belonging to the caller's tenant, regardless of what the underlying query returns.","distractorsMd":["`@PreAuthorize` evaluates its SpEL expression before the method executes; if the expression returns false, `AccessDeniedException` is thrown immediately and the method body never runs.","Annotate each `SecurityFilterChain` `@Bean` with `@Order` to set priority.","`AuthorizationManager<T>` replaces `AccessDecisionManager` and its voter pattern."]},{"id":"spring/spring_security_architecture#104ef7631850","section":"spring","module":"spring/spring_security_architecture","moduleName":"spring security architecture","sourceFile":"README.md","difficulty":"advanced","question":"How do you write an integration test for a Spring Security-protected endpoint?","answerFull":"Use @SpringBootTest with MockMvc. Import SecurityMockMvcRequestPostProcessors for helpers. java @SpringBootTest @AutoConfigureMockMvc class AdminControllerTest { @Autowired private MockMvc mockMvc; @Test @WithMockUser(roles = \"ADMIN\") // sets up SecurityContext with ROLE_ADMIN void adminEndpoint_shouldReturn200() throws Exception { mockMvc.perform(get(\"/admin/dashboard\")) .andExpect(status().isOk()); } @Test @WithMockUser(roles = \"USER\") void adminEndpoint_asUser_shouldReturn403() throws Exception { mockMvc.perform(get(\"/admin/dashboard\")) .andExpect(status().isForbidden()); } @Test void adminEndpoint_unauthenticated_shouldReturn401() throws Exception { mockMvc.perform(get(\"/admin/dashboard\")) .andExpect(status().isUnauthorized()); } }","correct":"Use @SpringBootTest with MockMvc.","distractors":["Spring Security evaluates authorization matchers top-to-bottom and applies the *first* one that matches the request, ignoring the rest.","By default, Spring Security generates a CSRF token per session stored in HttpSession and expects it in subsequent state-changing requests (POST, PUT, DELETE, PATCH) as either a form field or request header X-CSRF-Token.","STATELESS instructs Spring Security to never create or read an HttpSession."],"distractorIds":["spring/spring_security_architecture#e1abc45b83d7","spring/spring_security_architecture#0f831958c07d","spring/spring_security_architecture#07d95efe0117"],"concepts":["mockmvc","integration","protected","springboottest","endpoint","security"],"correctMd":"Use `@SpringBootTest` with `MockMvc`.","answerFullMd":"Use `@SpringBootTest` with `MockMvc`. Import `SecurityMockMvcRequestPostProcessors` for helpers. ```java @SpringBootTest @AutoConfigureMockMvc class AdminControllerTest { @Autowired private MockMvc mockMvc; @Test @WithMockUser(roles = \"ADMIN\") // sets up SecurityContext with ROLE_ADMIN void adminEndpoint_shouldReturn200() throws Exception { mockMvc.perform(get(\"/admin/dashboard\")) .andExpect(status().isOk()); } @Test @WithMockUser(roles = \"USER\") void adminEndpoint_asUser_shouldReturn403() throws Exception { mockMvc.perform(get(\"/admin/dashboard\")) .andExpect(status().isForbidden()); } @Test void adminEndpoint_unauthenticated_shouldReturn401() throws Exception { mockMvc.perform(get(\"/admin/dashboard\")) .andExpect(status().isUnauthorized()); } } ```","distractorsMd":[null,"By default, Spring Security generates a CSRF token per session stored in `HttpSession` and expects it in subsequent state-changing requests (POST, PUT, DELETE, PATCH) as either a form field or request header `X-CSRF-Token`.","`STATELESS` instructs Spring Security to never create or read an `HttpSession`."]},{"id":"spring/spring_security_architecture#07d95efe0117","section":"spring","module":"spring/spring_security_architecture","moduleName":"spring security architecture","sourceFile":"README.md","difficulty":"advanced","question":"What is the security implication of using SessionCreationPolicy.STATELESS and when is it safe?","answerFull":"STATELESS instructs Spring Security to never create or read an HttpSession. On each request, authentication must be re-established from the request itself (e.g., JWT Bearer token, API key). This is safe for purely machine-to-machine APIs or mobile/SPA clients that send a token on every request. It eliminates CSRF risk (no session cookie), enables horizontal scaling without session replication, but also eliminates automatic logout via session invalidation — token expiry and token blacklisting must be managed explicitly.","correct":"STATELESS instructs Spring Security to never create or read an HttpSession.","distractors":["By default, Spring Security generates a CSRF token per session stored in HttpSession and expects it in subsequent state-changing requests (POST, PUT, DELETE, PATCH) as either a form field or request header X-CSRF-Token.","@PreAuthorize evaluates its SpEL expression before the method executes; if the expression returns false, AccessDeniedException is thrown immediately and the method body never runs.","Spring Security evaluates authorization matchers top-to-bottom and applies the *first* one that matches the request, ignoring the rest."],"distractorIds":["spring/spring_security_architecture#0f831958c07d","spring/spring_security_architecture#8e50905247b4","spring/spring_security_architecture#e1abc45b83d7"],"concepts":["implication","httpsession","instructs","stateless","safe","security"],"questionMd":"What is the security implication of using `SessionCreationPolicy.STATELESS` and when is it safe?","correctMd":"`STATELESS` instructs Spring Security to never create or read an `HttpSession`.","answerFullMd":"`STATELESS` instructs Spring Security to never create or read an `HttpSession`. On each request, authentication must be re-established from the request itself (e.g., JWT Bearer token, API key). This is safe for purely machine-to-machine APIs or mobile/SPA clients that send a token on every request. It eliminates CSRF risk (no session cookie), enables horizontal scaling without session replication, but also eliminates automatic logout via session invalidation — token expiry and token blacklisting must be managed explicitly.","distractorsMd":["By default, Spring Security generates a CSRF token per session stored in `HttpSession` and expects it in subsequent state-changing requests (POST, PUT, DELETE, PATCH) as either a form field or request header `X-CSRF-Token`.","`@PreAuthorize` evaluates its SpEL expression before the method executes; if the expression returns false, `AccessDeniedException` is thrown immediately and the method body never runs.",null]},{"id":"spring/spring_security_architecture#e1abc45b83d7","section":"spring","module":"spring/spring_security_architecture","moduleName":"spring security architecture","sourceFile":"README.md","difficulty":"advanced","question":"Why is the order of security matchers significant, and what is the \"first match wins\" trap?","answerFull":"Spring Security evaluates authorization matchers top-to-bottom and applies the *first* one that matches the request, ignoring the rest. If you put anyRequest().permitAll() (or a broad / pattern) before a specific /admin/ rule, every request matches the broad rule first and the admin restriction never applies — a silent privilege-escalation hole. The fix is to order from most-specific to least-specific and finish with a catch-all anyRequest(). This is a frequent real-world misconfiguration because the app still \"works\" — it just authorizes too much.","correct":"Spring Security evaluates authorization matchers top-to-bottom and applies the *first* one that matches the request, ignoring the rest.","distractors":["Annotate each SecurityFilterChain @Bean with @Order to set priority.","The request hits UsernamePasswordAuthenticationFilter, which extracts credentials and creates an unauthenticated UsernamePasswordAuthenticationToken.","@PreAuthorize evaluates its SpEL expression before the method executes; if the expression returns false, AccessDeniedException is thrown immediately and the method body never runs."],"distractorIds":["spring/spring_security_architecture#3ce54a8c67b2","spring/spring_security_architecture#09ac506a344c","spring/spring_security_architecture#8e50905247b4"],"concepts":["matchers","trap","ignoring","wins","significant","bottom"],"answerFullMd":"Spring Security evaluates authorization matchers top-to-bottom and applies the *first* one that matches the request, ignoring the rest. If you put `anyRequest().permitAll()` (or a broad `/**` pattern) before a specific `/admin/**` rule, every request matches the broad rule first and the admin restriction never applies — a silent privilege-escalation hole. The fix is to order from most-specific to least-specific and finish with a catch-all `anyRequest()`. This is a frequent real-world misconfiguration because the app still \"works\" — it just authorizes too much.","distractorsMd":["Annotate each `SecurityFilterChain` `@Bean` with `@Order` to set priority.","The request hits `UsernamePasswordAuthenticationFilter`, which extracts credentials and creates an unauthenticated `UsernamePasswordAuthenticationToken`.","`@PreAuthorize` evaluates its SpEL expression before the method executes; if the expression returns false, `AccessDeniedException` is thrown immediately and the method body never runs."]},{"id":"spring/spring_security_architecture#cefdfd66d3a7","section":"spring","module":"spring/spring_security_architecture","moduleName":"spring security architecture","sourceFile":"README.md","difficulty":"advanced","question":"How do multiple SecurityFilterChain beans coexist, and how does Spring pick which one handles a request?","answerFull":"You can define several SecurityFilterChain beans, each with its own securityMatcher (e.g. one for /api/ that is stateless + JWT, one for everything else that is form-login + sessions). FilterChainProxy holds the ordered list and, per request, selects the *first* chain whose securityMatcher matches — only that chain's filters run. Use @Order to control evaluation order, and make the most specific matcher first; a chain with no securityMatcher matches everything and must come last. This is the modern way (Spring Security 5.7+/6.x) to apply different security models to different parts of one application after WebSecurityConfigurerAdapter was removed.","correct":"You can define several SecurityFilterChain beans, each with its own securityMatcher (e.g. one for /api/ that is stateless + JWT, one for everything else that is form-login + sessions).","distractors":["The request hits UsernamePasswordAuthenticationFilter, which extracts credentials and creates an unauthenticated UsernamePasswordAuthenticationToken.","WebSecurityConfigurerAdapter was removed; all configuration must now be SecurityFilterChain beans.","Configure http.oauth2ResourceServer(oauth2 -> oauth2.jwt(Customizer.withDefaults()))."],"distractorIds":["spring/spring_security_architecture#09ac506a344c","spring/spring_security_architecture#22d8a4c9c0bf","spring/spring_security_architecture#072df083eacc"],"concepts":["coexist","securitymatcher","login","securityfilterchain","sessions","several"],"questionMd":"How do multiple `SecurityFilterChain` beans coexist, and how does Spring pick which one handles a request?","correctMd":"You can define several `SecurityFilterChain` beans, each with its own `securityMatcher` (e.g. one for `/api/**` that is stateless + JWT, one for everything else that is form-login + sessions).","answerFullMd":"You can define several `SecurityFilterChain` beans, each with its own `securityMatcher` (e.g. one for `/api/**` that is stateless + JWT, one for everything else that is form-login + sessions). `FilterChainProxy` holds the ordered list and, per request, selects the *first* chain whose `securityMatcher` matches — only that chain's filters run. Use `@Order` to control evaluation order, and make the most specific matcher first; a chain with no `securityMatcher` matches everything and must come last. This is the modern way (Spring Security 5.7+/6.x) to apply different security models to different parts of one application after `WebSecurityConfigurerAdapter` was removed.","distractorsMd":["The request hits `UsernamePasswordAuthenticationFilter`, which extracts credentials and creates an unauthenticated `UsernamePasswordAuthenticationToken`.","`WebSecurityConfigurerAdapter` was removed; all configuration must now be `SecurityFilterChain` beans.","Configure `http.oauth2ResourceServer(oauth2 -> oauth2.jwt(Customizer.withDefaults()))`."]},{"id":"spring/spring_security_jwt_oauth#e8a34252e893","section":"spring","module":"spring/spring_security_jwt_oauth","moduleName":"spring security jwt oauth","sourceFile":"README.md","difficulty":"core","question":"What is the structure of a JWT and what does each part contain?","answerFull":"A JWT consists of three base64url-encoded parts separated by dots: header, payload, and signature. The header declares the token type (\"JWT\") and signing algorithm (e.g., \"RS256\"). The payload carries claims: registered claims (sub — subject, iss — issuer, aud — audience, exp — expiry, iat — issued-at), public claims, and private claims. The signature is computed over the encoded header and payload using the specified algorithm and key. Only the signature provides integrity; the header and payload are visible to anyone.","correct":"A JWT consists of three base64url-encoded parts separated by dots: header, payload, and signature.","distractors":["JwtAuthenticationConverter is a Converter<Jwt, AbstractAuthenticationToken> called by the security framework after successful JWT validation.","A JWT is self-contained and stateless.","When configured with http.oauth2ResourceServer(oauth2 -> oauth2.jwt(...)), Spring auto-configures a BearerTokenAuthenticationFilter."],"distractorIds":["spring/spring_security_jwt_oauth#c07f7cb26fd5","spring/spring_security_jwt_oauth#f526ce397cb0","spring/spring_security_jwt_oauth#13686f64866b"],"concepts":["base64url","dots","consists","parts","separated","payload"],"distractorsMd":["`JwtAuthenticationConverter` is a `Converter<Jwt, AbstractAuthenticationToken>` called by the security framework after successful JWT validation.",null,"When configured with `http.oauth2ResourceServer(oauth2 -> oauth2.jwt(...))`, Spring auto-configures a `BearerTokenAuthenticationFilter`."]},{"id":"spring/spring_security_jwt_oauth#b06524b3e6bd","section":"spring","module":"spring/spring_security_jwt_oauth","moduleName":"spring security jwt oauth","sourceFile":"README.md","difficulty":"core","question":"What is the difference between HS256 and RS256, and when would you choose each?","answerFull":"HS256 uses a single shared secret for both signing and verification. RS256 uses an RSA private key to sign and the corresponding public key to verify. In a microservices architecture, RS256 is strongly preferred: only the Authorization Server holds the private key, while every Resource Server only needs the public key (fetched from the JWKS endpoint). With HS256, every service that needs to validate tokens must possess the secret, meaning a compromise of any service compromises the signing secret for all services.","correct":"HS256 uses a single shared secret for both signing and verification.","distractors":["Signature verification runs first against the cached JWKS public key, then timestamp checks (exp, nbf), then issuer (iss), then any custom validators such as audience (aud).","A JWT consists of three base64url-encoded parts separated by dots: header, payload, and signature.","A JWT is self-contained and stateless."],"distractorIds":["spring/spring_security_jwt_oauth#ee53c405023b","spring/spring_security_jwt_oauth#e8a34252e893","spring/spring_security_jwt_oauth#f526ce397cb0"],"concepts":["rs256","hs256","signing","verification","difference","secret"],"distractorsMd":["Signature verification runs first against the cached JWKS public key, then timestamp checks (`exp`, `nbf`), then issuer (`iss`), then any custom validators such as audience (`aud`).",null,null]},{"id":"spring/spring_security_jwt_oauth#13686f64866b","section":"spring","module":"spring/spring_security_jwt_oauth","moduleName":"spring security jwt oauth","sourceFile":"README.md","difficulty":"core","question":"How does Spring Security's OAuth2 Resource Server validate JWTs?","answerFull":"When configured with http.oauth2ResourceServer(oauth2 -> oauth2.jwt(...)), Spring auto-configures a BearerTokenAuthenticationFilter. For each request with a Bearer token, it calls the configured JwtDecoder (typically NimbusJwtDecoder). Nimbus fetches the JWKS from the configured URI, caches the public keys (default 5-minute TTL), and verifies the token signature and standard claims (exp, nbf). The decoded JWT is then passed to a JwtAuthenticationConverter which maps claims to GrantedAuthority objects and populates the SecurityContext.","correct":"When configured with http.oauth2ResourceServer(oauth2 -> oauth2.jwt(...)), Spring auto-configures a BearerTokenAuthenticationFilter.","distractors":["NimbusJwtDecoder fetches the JWKS from the configured URI and caches the keys.","A JWT is self-contained and stateless.","The JWT specification originally allowed \"alg\": \"none\" to indicate an unsigned token."],"distractorIds":["spring/spring_security_jwt_oauth#d978eed82d24","spring/spring_security_jwt_oauth#f526ce397cb0","spring/spring_security_jwt_oauth#875891aafbeb"],"concepts":["bearertokenauthenticationfilter","oauth2resourceserver","jwts","oauth2","configures","validate"],"correctMd":"When configured with `http.oauth2ResourceServer(oauth2 -> oauth2.jwt(...))`, Spring auto-configures a `BearerTokenAuthenticationFilter`.","answerFullMd":"When configured with `http.oauth2ResourceServer(oauth2 -> oauth2.jwt(...))`, Spring auto-configures a `BearerTokenAuthenticationFilter`. For each request with a Bearer token, it calls the configured `JwtDecoder` (typically `NimbusJwtDecoder`). Nimbus fetches the JWKS from the configured URI, caches the public keys (default 5-minute TTL), and verifies the token signature and standard claims (exp, nbf). The decoded JWT is then passed to a `JwtAuthenticationConverter` which maps claims to `GrantedAuthority` objects and populates the `SecurityContext`.","distractorsMd":[null,null,"The JWT specification originally allowed `\"alg\": \"none\"` to indicate an unsigned token."]},{"id":"spring/spring_security_jwt_oauth#f526ce397cb0","section":"spring","module":"spring/spring_security_jwt_oauth","moduleName":"spring security jwt oauth","sourceFile":"README.md","difficulty":"core","question":"Why can't you simply delete a JWT to revoke it?","answerFull":"A JWT is self-contained and stateless. The Resource Server validates it locally without contacting any central store. There is no mechanism in the JWT specification for a server to \"forget\" a token. Once issued, a valid (unexpired) JWT will be accepted by any Resource Server that trusts the issuer. Revocation requires out-of-band infrastructure: a token blacklist (Redis set containing revoked JTI claims, checked on every request) or sufficiently short expiry windows (5–15 minutes) combined with refresh token invalidation.","correct":"A JWT is self-contained and stateless.","distractors":["A JWT consists of three base64url-encoded parts separated by dots: header, payload, and signature.","JwtAuthenticationConverter is a Converter<Jwt, AbstractAuthenticationToken> called by the security framework after successful JWT validation.","When configured with http.oauth2ResourceServer(oauth2 -> oauth2.jwt(...)), Spring auto-configures a BearerTokenAuthenticationFilter."],"distractorIds":["spring/spring_security_jwt_oauth#e8a34252e893","spring/spring_security_jwt_oauth#c07f7cb26fd5","spring/spring_security_jwt_oauth#13686f64866b"],"concepts":["contained","revoke","simply","stateless","jwt","delete"],"distractorsMd":[null,"`JwtAuthenticationConverter` is a `Converter<Jwt, AbstractAuthenticationToken>` called by the security framework after successful JWT validation.","When configured with `http.oauth2ResourceServer(oauth2 -> oauth2.jwt(...))`, Spring auto-configures a `BearerTokenAuthenticationFilter`."]},{"id":"spring/spring_security_jwt_oauth#2ffcb20af489","section":"spring","module":"spring/spring_security_jwt_oauth","moduleName":"spring security jwt oauth","sourceFile":"README.md","difficulty":"intermediate","question":"What is refresh token rotation and why does it improve security?","answerFull":"Refresh token rotation means each use of a refresh token invalidates that token and issues a new one. If an attacker steals a refresh token and attempts to use it after the legitimate client has already used it, the server detects the reuse (the token has already been rotated) and can revoke the entire token family — invalidating all active sessions for that user. Without rotation, a stolen refresh token is valid until its expiry date (often days or weeks).","correct":"Refresh token rotation means each use of a refresh token invalidates that token and issues a new one.","distractors":["JwtAuthenticationConverter is a Converter<Jwt, AbstractAuthenticationToken> called by the security framework after successful JWT validation.","Several strategies: (1) Maintain short access token expiry (5–15 min) so the window of risk for stolen tokens is small; rely on refresh token rotation and a compact blacklist only for refresh tokens.","The Authorization Server authenticates the user and issues tokens, while the Resource Server only validates tokens and serves protected data."],"distractorIds":["spring/spring_security_jwt_oauth#c07f7cb26fd5","spring/spring_security_jwt_oauth#d1f9dc977980","spring/spring_security_jwt_oauth#a87e7c23c926"],"concepts":["invalidates","rotation","improve","refresh","issues","security"],"distractorsMd":["`JwtAuthenticationConverter` is a `Converter<Jwt, AbstractAuthenticationToken>` called by the security framework after successful JWT validation.",null,null]},{"id":"spring/spring_security_jwt_oauth#d1f9dc977980","section":"spring","module":"spring/spring_security_jwt_oauth","moduleName":"spring security jwt oauth","sourceFile":"README.md","difficulty":"intermediate","question":"How would you implement JWT revocation without a blacklist lookup on every request?","answerFull":"Several strategies: (1) Maintain short access token expiry (5–15 min) so the window of risk for stolen tokens is small; rely on refresh token rotation and a compact blacklist only for refresh tokens. (2) Store the user's token version (a counter) in the JWT claim and in the database; on token use, check if the JWT version matches the current version in the database — increment the version to invalidate all tokens. (3) Use opaque tokens for endpoints that require immediate revocation (e.g., privileged operations) and JWTs for low-risk endpoints.","correct":"Several strategies: (1) Maintain short access token expiry (5–15 min) so the window of risk for stolen tokens is small; rely on refresh token rotation and a compact blacklist only for refresh tokens.","distractors":["JwtAuthenticationConverter is a Converter<Jwt, AbstractAuthenticationToken> called by the security framework after successful JWT validation.","A JWT is self-contained and stateless.","An access token authorizes API calls directly and is deliberately short-lived (5–15 minutes), while a refresh token only exchanges for a new access token and is long-lived (hours to days)."],"distractorIds":["spring/spring_security_jwt_oauth#c07f7cb26fd5","spring/spring_security_jwt_oauth#f526ce397cb0","spring/spring_security_jwt_oauth#fb3afb0ccc65"],"concepts":["revocation","blacklist","stolen","rely","rotation","risk"],"distractorsMd":["`JwtAuthenticationConverter` is a `Converter<Jwt, AbstractAuthenticationToken>` called by the security framework after successful JWT validation.",null,null]},{"id":"spring/spring_security_jwt_oauth#875891aafbeb","section":"spring","module":"spring/spring_security_jwt_oauth","moduleName":"spring security jwt oauth","sourceFile":"README.md","difficulty":"intermediate","question":"Explain the alg:none vulnerability and how Spring Security protects against it.","answerFull":"The JWT specification originally allowed \"alg\": \"none\" to indicate an unsigned token. Vulnerable implementations trusted the algorithm declared in the header and would accept a token with no signature if alg was \"none\". An attacker could strip the signature from any valid token, set alg to \"none\", and the server would accept it. Spring Security's NimbusJwtDecoder uses a strict allow-list of algorithms (configured via jwsAlgorithm()); it does not support the \"none\" algorithm and will reject any token claiming it.","correct":"The JWT specification originally allowed \"alg\": \"none\" to indicate an unsigned token.","distractors":["Refresh token rotation means each use of a refresh token invalidates that token and issues a new one.","Signature verification runs first against the cached JWKS public key, then timestamp checks (exp, nbf), then issuer (iss), then any custom validators such as audience (aud).","Several strategies: (1) Maintain short access token expiry (5–15 min) so the window of risk for stolen tokens is small; rely on refresh token rotation and a compact blacklist only for refresh tokens."],"distractorIds":["spring/spring_security_jwt_oauth#2ffcb20af489","spring/spring_security_jwt_oauth#ee53c405023b","spring/spring_security_jwt_oauth#d1f9dc977980"],"concepts":["originally","indicate","unsigned","vulnerability","alg","specification"],"correctMd":"The JWT specification originally allowed `\"alg\": \"none\"` to indicate an unsigned token.","answerFullMd":"The JWT specification originally allowed `\"alg\": \"none\"` to indicate an unsigned token. Vulnerable implementations trusted the algorithm declared in the header and would accept a token with no signature if alg was \"none\". An attacker could strip the signature from any valid token, set alg to \"none\", and the server would accept it. Spring Security's `NimbusJwtDecoder` uses a strict allow-list of algorithms (configured via `jwsAlgorithm()`); it does not support the \"none\" algorithm and will reject any token claiming it.","distractorsMd":[null,"Signature verification runs first against the cached JWKS public key, then timestamp checks (`exp`, `nbf`), then issuer (`iss`), then any custom validators such as audience (`aud`).",null]},{"id":"spring/spring_security_jwt_oauth#de8a3cde2064","section":"spring","module":"spring/spring_security_jwt_oauth","moduleName":"spring security jwt oauth","sourceFile":"README.md","difficulty":"intermediate","question":"What is multi-tenant JWT validation and how would you implement it in Spring?","answerFull":"In a multi-tenant SaaS, each tenant typically has its own issuer or JWKS endpoint. An AuthenticationManagerResolver<HttpServletRequest> resolves the correct JwtDecoder based on a tenant identifier extracted from the request (subdomain, custom header, or unverified claim in the token). The decoder is cached per tenant to avoid re-fetching JWKS on every request. The security config wires this resolver via .oauth2ResourceServer(oauth2 -> oauth2.authenticationManagerResolver(resolver)).","correct":"In a multi-tenant SaaS, each tenant typically has its own issuer or JWKS endpoint.","distractors":["Signature verification runs first against the cached JWKS public key, then timestamp checks (exp, nbf), then issuer (iss), then any custom validators such as audience (aud).","NimbusJwtDecoder fetches the JWKS from the configured URI and caches the keys.","JwtAuthenticationConverter is a Converter<Jwt, AbstractAuthenticationToken> called by the security framework after successful JWT validation."],"distractorIds":["spring/spring_security_jwt_oauth#ee53c405023b","spring/spring_security_jwt_oauth#d978eed82d24","spring/spring_security_jwt_oauth#c07f7cb26fd5"],"concepts":["saas","jwks","issuer","tenant","endpoint","jwt"],"answerFullMd":"In a multi-tenant SaaS, each tenant typically has its own issuer or JWKS endpoint. An `AuthenticationManagerResolver<HttpServletRequest>` resolves the correct `JwtDecoder` based on a tenant identifier extracted from the request (subdomain, custom header, or unverified claim in the token). The decoder is cached per tenant to avoid re-fetching JWKS on every request. The security config wires this resolver via `.oauth2ResourceServer(oauth2 -> oauth2.authenticationManagerResolver(resolver))`.","distractorsMd":["Signature verification runs first against the cached JWKS public key, then timestamp checks (`exp`, `nbf`), then issuer (`iss`), then any custom validators such as audience (`aud`).",null,"`JwtAuthenticationConverter` is a `Converter<Jwt, AbstractAuthenticationToken>` called by the security framework after successful JWT validation."]},{"id":"spring/spring_security_jwt_oauth#9b5efc1e2c97","section":"spring","module":"spring/spring_security_jwt_oauth","moduleName":"spring security jwt oauth","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between OAuth2 and OIDC?","answerFull":"OAuth2 is an authorization framework that defines how a resource owner can delegate access to a third party via access tokens. It does not specify anything about identity. OIDC (OpenID Connect) is an identity layer built on top of OAuth2: it adds an ID token (always a JWT) containing identity claims about the authenticated user (sub, name, email, etc.) and defines a UserInfo endpoint. OAuth2 answers \"what can this client do?\"; OIDC answers \"who is this user?\".","correct":"OAuth2 is an authorization framework that defines how a resource owner can delegate access to a third party via access tokens.","distractors":["An access token authorizes API calls directly and is deliberately short-lived (5–15 minutes), while a refresh token only exchanges for a new access token and is long-lived (hours to days).","The Authorization Server authenticates the user and issues tokens, while the Resource Server only validates tokens and serves protected data.","exp (expiry): skipping allows use of indefinitely old tokens after they should have been invalidated."],"distractorIds":["spring/spring_security_jwt_oauth#fb3afb0ccc65","spring/spring_security_jwt_oauth#a87e7c23c926","spring/spring_security_jwt_oauth#df929ce69085"],"concepts":["delegate","oidc","owner","oauth2","party","third"],"distractorsMd":[null,null,"`exp` (expiry): skipping allows use of indefinitely old tokens after they should have been invalidated."]},{"id":"spring/spring_security_jwt_oauth#c07f7cb26fd5","section":"spring","module":"spring/spring_security_jwt_oauth","moduleName":"spring security jwt oauth","sourceFile":"README.md","difficulty":"advanced","question":"How does JwtAuthenticationConverter work and how would you customize it?","answerFull":"JwtAuthenticationConverter is a Converter<Jwt, AbstractAuthenticationToken> called by the security framework after successful JWT validation. Its default behavior extracts the \"scope\" claim from the JWT and creates GrantedAuthority objects with a \"SCOPE_\" prefix. To customize, you configure a JwtGrantedAuthoritiesConverter with a different claims name (e.g., \"roles\") and a different prefix (e.g., \"ROLE_\"), or you implement JwtAuthenticationConverter directly to handle complex claim structures such as nested role objects or multiple claim sources.","correct":"JwtAuthenticationConverter is a Converter<Jwt, AbstractAuthenticationToken> called by the security framework after successful JWT validation.","distractors":["The JWT specification originally allowed \"alg\": \"none\" to indicate an unsigned token.","OAuth2 is an authorization framework that defines how a resource owner can delegate access to a third party via access tokens.","A JWT consists of three base64url-encoded parts separated by dots: header, payload, and signature."],"distractorIds":["spring/spring_security_jwt_oauth#875891aafbeb","spring/spring_security_jwt_oauth#9b5efc1e2c97","spring/spring_security_jwt_oauth#e8a34252e893"],"concepts":["abstractauthenticationtoken","jwtauthenticationconverter","converter","successful","jwt","framework"],"correctMd":"`JwtAuthenticationConverter` is a `Converter<Jwt, AbstractAuthenticationToken>` called by the security framework after successful JWT validation.","answerFullMd":"`JwtAuthenticationConverter` is a `Converter<Jwt, AbstractAuthenticationToken>` called by the security framework after successful JWT validation. Its default behavior extracts the \"scope\" claim from the JWT and creates `GrantedAuthority` objects with a \"SCOPE_\" prefix. To customize, you configure a `JwtGrantedAuthoritiesConverter` with a different claims name (e.g., \"roles\") and a different prefix (e.g., \"ROLE_\"), or you implement `JwtAuthenticationConverter` directly to handle complex claim structures such as nested role objects or multiple claim sources.","distractorsMd":["The JWT specification originally allowed `\"alg\": \"none\"` to indicate an unsigned token.",null,null]},{"id":"spring/spring_security_jwt_oauth#371a683906f7","section":"spring","module":"spring/spring_security_jwt_oauth","moduleName":"spring security jwt oauth","sourceFile":"README.md","difficulty":"advanced","question":"What is the security implication of storing a JWT in an httpOnly cookie versus localStorage?","answerFull":"localStorage is accessible to JavaScript running on the page. Any successful XSS attack can extract the token, enabling token theft and session hijacking. An httpOnly cookie cannot be read by JavaScript; it is only sent by the browser to the server automatically on requests. The tradeoff is CSRF vulnerability: a malicious site can trick the browser into making a request that includes the httpOnly cookie. Mitigations include SameSite=Strict cookies (browser will not send cookie on cross-site navigated requests), CSRF double-submit tokens, or the Synchronizer Token Pattern. For most modern apps with properly configured CORS and SameSite cookies, httpOnly is the safer choice.","correct":"localStorage is accessible to JavaScript running on the page.","distractors":["A JWT is self-contained and stateless.","The JWT specification originally allowed \"alg\": \"none\" to indicate an unsigned token.","When configured with http.oauth2ResourceServer(oauth2 -> oauth2.jwt(...)), Spring auto-configures a BearerTokenAuthenticationFilter."],"distractorIds":["spring/spring_security_jwt_oauth#f526ce397cb0","spring/spring_security_jwt_oauth#875891aafbeb","spring/spring_security_jwt_oauth#13686f64866b"],"concepts":["implication","httponly","localstorage","cookie","javascript","accessible"],"answerFullMd":"localStorage is accessible to JavaScript running on the page. Any successful XSS attack can extract the token, enabling token theft and session hijacking. An httpOnly cookie cannot be read by JavaScript; it is only sent by the browser to the server automatically on requests. The tradeoff is CSRF vulnerability: a malicious site can trick the browser into making a request that includes the httpOnly cookie. Mitigations include `SameSite=Strict` cookies (browser will not send cookie on cross-site navigated requests), CSRF double-submit tokens, or the Synchronizer Token Pattern. For most modern apps with properly configured CORS and SameSite cookies, httpOnly is the safer choice.","distractorsMd":[null,"The JWT specification originally allowed `\"alg\": \"none\"` to indicate an unsigned token.","When configured with `http.oauth2ResourceServer(oauth2 -> oauth2.jwt(...))`, Spring auto-configures a `BearerTokenAuthenticationFilter`."]},{"id":"spring/spring_security_jwt_oauth#84a4dd08f631","section":"spring","module":"spring/spring_security_jwt_oauth","moduleName":"spring security jwt oauth","sourceFile":"README.md","difficulty":"advanced","question":"How would you configure Spring Security to trust tokens from multiple issuers?","answerFull":"Use AuthenticationManagerResolver<HttpServletRequest>. Each issuer gets its own JwtDecoder (typically a NimbusJwtDecoder configured with that issuer's JWKS URI or issuer-uri for auto-discovery). The resolver extracts the iss claim from the unverified JWT header/payload (NOT trusting it — just routing), looks up the corresponding decoder, and the decoder performs full signature and claims validation. This pattern is also used for federated identity (allow tokens from Google AND Auth0 AND internal issuer).","correct":"Use AuthenticationManagerResolver<HttpServletRequest>.","distractors":["Several strategies: (1) Maintain short access token expiry (5–15 min) so the window of risk for stolen tokens is small; rely on refresh token rotation and a compact blacklist only for refresh tokens.","When configured with http.oauth2ResourceServer(oauth2 -> oauth2.jwt(...)), Spring auto-configures a BearerTokenAuthenticationFilter.","JwtAuthenticationConverter is a Converter<Jwt, AbstractAuthenticationToken> called by the security framework after successful JWT validation."],"distractorIds":["spring/spring_security_jwt_oauth#d1f9dc977980","spring/spring_security_jwt_oauth#13686f64866b","spring/spring_security_jwt_oauth#c07f7cb26fd5"],"concepts":["authenticationmanagerresolver","httpservletrequest","trust","configure","security","multiple"],"correctMd":"Use `AuthenticationManagerResolver<HttpServletRequest>`.","answerFullMd":"Use `AuthenticationManagerResolver<HttpServletRequest>`. Each issuer gets its own `JwtDecoder` (typically a `NimbusJwtDecoder` configured with that issuer's JWKS URI or issuer-uri for auto-discovery). The resolver extracts the `iss` claim from the unverified JWT header/payload (NOT trusting it — just routing), looks up the corresponding decoder, and the decoder performs full signature and claims validation. This pattern is also used for federated identity (allow tokens from Google AND Auth0 AND internal issuer).","distractorsMd":[null,"When configured with `http.oauth2ResourceServer(oauth2 -> oauth2.jwt(...))`, Spring auto-configures a `BearerTokenAuthenticationFilter`.","`JwtAuthenticationConverter` is a `Converter<Jwt, AbstractAuthenticationToken>` called by the security framework after successful JWT validation."]},{"id":"spring/spring_security_jwt_oauth#df929ce69085","section":"spring","module":"spring/spring_security_jwt_oauth","moduleName":"spring security jwt oauth","sourceFile":"README.md","difficulty":"advanced","question":"What claims must you always validate in a JWT, and what are the security consequences of skipping each?","answerFull":"exp (expiry): skipping allows use of indefinitely old tokens after they should have been invalidated. iss (issuer): skipping allows tokens from a different authorization server to be accepted (e.g., a test environment token in production). aud (audience): skipping allows a token issued for service A to be replayed against service B. nbf (not before, optional): skipping allows tokens to be used before their intended validity window. In production, always validate exp, iss, and aud at minimum.","correct":"exp (expiry): skipping allows use of indefinitely old tokens after they should have been invalidated.","distractors":["JwtAuthenticationConverter is a Converter<Jwt, AbstractAuthenticationToken> called by the security framework after successful JWT validation.","OAuth2 is an authorization framework that defines how a resource owner can delegate access to a third party via access tokens.","When configured with http.oauth2ResourceServer(oauth2 -> oauth2.jwt(...)), Spring auto-configures a BearerTokenAuthenticationFilter."],"distractorIds":["spring/spring_security_jwt_oauth#c07f7cb26fd5","spring/spring_security_jwt_oauth#9b5efc1e2c97","spring/spring_security_jwt_oauth#13686f64866b"],"concepts":["invalidated","skipping","indefinitely","exp","expiry","validate"],"correctMd":"`exp` (expiry): skipping allows use of indefinitely old tokens after they should have been invalidated.","answerFullMd":"`exp` (expiry): skipping allows use of indefinitely old tokens after they should have been invalidated. `iss` (issuer): skipping allows tokens from a different authorization server to be accepted (e.g., a test environment token in production). `aud` (audience): skipping allows a token issued for service A to be replayed against service B. `nbf` (not before, optional): skipping allows tokens to be used before their intended validity window. In production, always validate exp, iss, and aud at minimum.","distractorsMd":["`JwtAuthenticationConverter` is a `Converter<Jwt, AbstractAuthenticationToken>` called by the security framework after successful JWT validation.",null,"When configured with `http.oauth2ResourceServer(oauth2 -> oauth2.jwt(...))`, Spring auto-configures a `BearerTokenAuthenticationFilter`."]},{"id":"spring/spring_security_jwt_oauth#d978eed82d24","section":"spring","module":"spring/spring_security_jwt_oauth","moduleName":"spring security jwt oauth","sourceFile":"README.md","difficulty":"advanced","question":"How does NimbusJwtDecoder handle JWKS key rotation without downtime?","answerFull":"NimbusJwtDecoder fetches the JWKS from the configured URI and caches the keys. When a JWT arrives with a kid (key ID) claim not found in the cache, Nimbus automatically re-fetches the JWKS to pick up new keys. Authorization Servers that rotate keys publish the new key alongside the old key for a grace period (typically 24–48 hours), so tokens signed with the old key continue to validate while clients refresh their JWKS cache. This is why JWKS endpoints should never remove old keys the moment a new key is published.","correct":"NimbusJwtDecoder fetches the JWKS from the configured URI and caches the keys.","distractors":["Refresh token rotation means each use of a refresh token invalidates that token and issues a new one.","In a multi-tenant SaaS, each tenant typically has its own issuer or JWKS endpoint.","Signature verification runs first against the cached JWKS public key, then timestamp checks (exp, nbf), then issuer (iss), then any custom validators such as audience (aud)."],"distractorIds":["spring/spring_security_jwt_oauth#2ffcb20af489","spring/spring_security_jwt_oauth#de8a3cde2064","spring/spring_security_jwt_oauth#ee53c405023b"],"concepts":["nimbusjwtdecoder","downtime","jwks","uri","rotation","fetches"],"answerFullMd":"NimbusJwtDecoder fetches the JWKS from the configured URI and caches the keys. When a JWT arrives with a `kid` (key ID) claim not found in the cache, Nimbus automatically re-fetches the JWKS to pick up new keys. Authorization Servers that rotate keys publish the new key alongside the old key for a grace period (typically 24–48 hours), so tokens signed with the old key continue to validate while clients refresh their JWKS cache. This is why JWKS endpoints should never remove old keys the moment a new key is published.","distractorsMd":[null,null,"Signature verification runs first against the cached JWKS public key, then timestamp checks (`exp`, `nbf`), then issuer (`iss`), then any custom validators such as audience (`aud`)."]},{"id":"spring/spring_security_jwt_oauth#fb3afb0ccc65","section":"spring","module":"spring/spring_security_jwt_oauth","moduleName":"spring security jwt oauth","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between an access token and a refresh token, and why does rotating the refresh token matter more?","answerFull":"An access token authorizes API calls directly and is deliberately short-lived (5–15 minutes), while a refresh token only exchanges for a new access token and is long-lived (hours to days). Because the access token rides on every request, its blast radius is bounded purely by its short expiry; the refresh token is used far less often but grants renewable access, so a leaked refresh token is much more valuable to an attacker. This asymmetry is exactly why refresh token rotation — invalidating the old refresh token on every use and issuing a new one — is the primary defense: a single reuse of an already-rotated refresh token is a reliable compromise signal, whereas a stolen access token simply expires on its own within minutes.","correct":"An access token authorizes API calls directly and is deliberately short-lived (5–15 minutes), while a refresh token only exchanges for a new access token and is long-lived (hours to days).","distractors":["OAuth2 is an authorization framework that defines how a resource owner can delegate access to a third party via access tokens.","Several strategies: (1) Maintain short access token expiry (5–15 min) so the window of risk for stolen tokens is small; rely on refresh token rotation and a compact blacklist only for refresh tokens.","Refresh token rotation means each use of a refresh token invalidates that token and issues a new one."],"distractorIds":["spring/spring_security_jwt_oauth#9b5efc1e2c97","spring/spring_security_jwt_oauth#d1f9dc977980","spring/spring_security_jwt_oauth#2ffcb20af489"],"concepts":["authorizes","deliberately","exchanges","rotating","matter","days"]},{"id":"spring/spring_security_jwt_oauth#a87e7c23c926","section":"spring","module":"spring/spring_security_jwt_oauth","moduleName":"spring security jwt oauth","sourceFile":"README.md","difficulty":"advanced","question":"What is the architectural difference between an OAuth2 Authorization Server and a Resource Server?","answerFull":"The Authorization Server authenticates the user and issues tokens, while the Resource Server only validates tokens and serves protected data. Spring Security lets a single Boot application play both roles for small deployments, but production systems usually separate them: Keycloak, Auth0, or Spring Authorization Server plays the Authorization Server, and every microservice adds spring-boot-starter-oauth2-resource-server to become a Resource Server that trusts that issuer's public keys. A Resource Server has no login forms, consent screens, or credential storage — it only knows how to fetch the JWKS and check claims — which is precisely what makes horizontal scaling of the API tier trivial: any number of Resource Server instances validate the same tokens without ever calling the Authorization Server on the request hot path.","correct":"The Authorization Server authenticates the user and issues tokens, while the Resource Server only validates tokens and serves protected data.","distractors":["Refresh token rotation means each use of a refresh token invalidates that token and issues a new one.","OAuth2 is an authorization framework that defines how a resource owner can delegate access to a third party via access tokens.","Several strategies: (1) Maintain short access token expiry (5–15 min) so the window of risk for stolen tokens is small; rely on refresh token rotation and a compact blacklist only for refresh tokens."],"distractorIds":["spring/spring_security_jwt_oauth#2ffcb20af489","spring/spring_security_jwt_oauth#9b5efc1e2c97","spring/spring_security_jwt_oauth#d1f9dc977980"],"concepts":["protected","architectural","authenticates","oauth2","serves","difference"],"answerFullMd":"The Authorization Server authenticates the user and issues tokens, while the Resource Server only validates tokens and serves protected data. Spring Security lets a single Boot application play both roles for small deployments, but production systems usually separate them: Keycloak, Auth0, or Spring Authorization Server plays the Authorization Server, and every microservice adds `spring-boot-starter-oauth2-resource-server` to become a Resource Server that trusts that issuer's public keys. A Resource Server has no login forms, consent screens, or credential storage — it only knows how to fetch the JWKS and check claims — which is precisely what makes horizontal scaling of the API tier trivial: any number of Resource Server instances validate the same tokens without ever calling the Authorization Server on the request hot path."},{"id":"spring/spring_security_jwt_oauth#ee53c405023b","section":"spring","module":"spring/spring_security_jwt_oauth","moduleName":"spring security jwt oauth","sourceFile":"README.md","difficulty":"advanced","question":"What are the exact steps Spring Security's OAuth2 Resource Server performs to validate an incoming JWT, and in what order?","answerFull":"Signature verification runs first against the cached JWKS public key, then timestamp checks (exp, nbf), then issuer (iss), then any custom validators such as audience (aud). Signature verification comes first because there is no point evaluating claims on a token that could have been tampered with — a failed signature check short-circuits immediately. JwtTimestampValidator handles expiry, JwtIssuerValidator confirms the token came from the trusted Authorization Server, and any additional checks are composed on top via DelegatingOAuth2TokenValidator. The gotcha: replacing the default validator to add a custom check (instead of composing it with JwtValidators.createDefaultWithIssuer(...)) silently drops the exp/iss checks — exactly the mistake in Pitfall 2 of this module's case study.","correct":"Signature verification runs first against the cached JWKS public key, then timestamp checks (exp, nbf), then issuer (iss), then any custom validators such as audience (aud).","distractors":["A JWT consists of three base64url-encoded parts separated by dots: header, payload, and signature.","HS256 uses a single shared secret for both signing and verification.","OAuth2 is an authorization framework that defines how a resource owner can delegate access to a third party via access tokens."],"distractorIds":["spring/spring_security_jwt_oauth#e8a34252e893","spring/spring_security_jwt_oauth#b06524b3e6bd","spring/spring_security_jwt_oauth#9b5efc1e2c97"],"concepts":["aud","iss","nbf","validators","audience","jwks"],"correctMd":"Signature verification runs first against the cached JWKS public key, then timestamp checks (`exp`, `nbf`), then issuer (`iss`), then any custom validators such as audience (`aud`).","answerFullMd":"Signature verification runs first against the cached JWKS public key, then timestamp checks (`exp`, `nbf`), then issuer (`iss`), then any custom validators such as audience (`aud`). Signature verification comes first because there is no point evaluating claims on a token that could have been tampered with — a failed signature check short-circuits immediately. `JwtTimestampValidator` handles expiry, `JwtIssuerValidator` confirms the token came from the trusted Authorization Server, and any additional checks are composed on top via `DelegatingOAuth2TokenValidator`. The gotcha: replacing the default validator to add a custom check (instead of composing it with `JwtValidators.createDefaultWithIssuer(...)`) silently drops the exp/iss checks — exactly the mistake in Pitfall 2 of this module's case study."},{"id":"spring/spring_session#60892b2926ea","section":"spring","module":"spring/spring_session","moduleName":"spring session","sourceFile":"README.md","difficulty":"core","question":"Why does the default HttpSession break when you scale a Spring Boot app to multiple instances?","answerFull":"The default HttpSession lives in the JVM heap of the single instance that created it, so a request the load balancer routes to a different instance cannot see it. That node finds no session, so the user's cart empties or they appear logged out, and if the original node restarts every session it held is lost. Spring Session fixes this by moving session state to an external shared store (Redis) that every instance can read. Reach for it the moment you run more than one instance.","correct":"The default HttpSession lives in the JVM heap of the single instance that created it, so a request the load balancer routes to a different instance cannot see it.","distractors":["Spring Session's CookieHttpSessionIdResolver uses a cookie named SESSION holding a Base64-encoded id, not the container's JSESSIONID.","WebFlux uses WebSession (not HttpSession) accessed non-blockingly via exchange.getSession(), which returns a Mono<WebSession>, backed by a ReactiveSessionRepository.","RedisSessionRepository (the Spring Session 3.0 default) stores one key per session and relies purely on Redis TTL — it is cheapest but offers no principal index, no expiration events, and no concurrent-session control."],"distractorIds":["spring/spring_session#924a81ccbc0e","spring/spring_session#9a6cda394411","spring/spring_session#3356eaebf8c7"],"concepts":["httpsession","break","lives","balancer","boot","routes"],"questionMd":"Why does the default `HttpSession` break when you scale a Spring Boot app to multiple instances?","correctMd":"The default `HttpSession` lives in the JVM heap of the single instance that created it, so a request the load balancer routes to a different instance cannot see it.","answerFullMd":"The default `HttpSession` lives in the JVM heap of the single instance that created it, so a request the load balancer routes to a different instance cannot see it. That node finds no session, so the user's cart empties or they appear logged out, and if the original node restarts every session it held is lost. Spring Session fixes this by moving session state to an external shared store (Redis) that every instance can read. Reach for it the moment you run more than one instance.","distractorsMd":["Spring Session's `CookieHttpSessionIdResolver` uses a cookie named `SESSION` holding a Base64-encoded id, not the container's `JSESSIONID`.","WebFlux uses `WebSession` (not `HttpSession`) accessed non-blockingly via `exchange.getSession()`, which returns a `Mono<WebSession>`, backed by a `ReactiveSessionRepository`.","`RedisSessionRepository` (the Spring Session 3.0 default) stores one key per session and relies purely on Redis TTL — it is cheapest but offers no principal index, no expiration events, and no concurrent-session control."]},{"id":"spring/spring_session#9d353cccbd2e","section":"spring","module":"spring/spring_session","moduleName":"spring session","sourceFile":"README.md","difficulty":"core","question":"Why aren't sticky sessions a real solution to multi-instance session loss?","answerFull":"Sticky sessions pin a user to one node, so they \"work\" until that node fails or is redeployed, at which point all its sessions vanish and the balancer cannot fail those users over. Affinity makes every node a single point of failure for its pinned users and blocks zero-downtime rolling deploys. The correct fix is externalizing session state to a shared store so any node can serve any request. Use sticky sessions only as a stopgap, never as the architecture.","correct":"Sticky sessions pin a user to one node, so they \"work\" until that node fails or is redeployed, at which point all its sessions vanish and the balancer cannot fail those users over.","distractors":["Register HeaderHttpSessionIdResolver.xAuthToken(), which transports the session id in the X-Auth-Token header instead of the SESSION cookie.","Use FindByIndexNameSessionRepository.findByPrincipalName(username) to enumerate all of that user's session ids across the cluster, then call deleteById on each.","Back Spring Security's concurrent-session control with a SpringSessionBackedSessionRegistry instead of the default SessionRegistryImpl."],"distractorIds":["spring/spring_session#a65460dafda0","spring/spring_session#9b72ba92ab3b","spring/spring_session#6843bee69730"],"concepts":["redeployed","aren","vanish","pin","sticky","solution"],"distractorsMd":["Register `HeaderHttpSessionIdResolver.xAuthToken()`, which transports the session id in the `X-Auth-Token` header instead of the `SESSION` cookie.","Use `FindByIndexNameSessionRepository.findByPrincipalName(username)` to enumerate all of that user's session ids across the cluster, then call `deleteById` on each.","Back Spring Security's concurrent-session control with a `SpringSessionBackedSessionRegistry` instead of the default `SessionRegistryImpl`."]},{"id":"spring/spring_session#1d13be62f1f1","section":"spring","module":"spring/spring_session","moduleName":"spring session","sourceFile":"README.md","difficulty":"core","question":"What is the core trick Spring Session uses to replace the container's session?","answerFull":"SessionRepositoryFilter wraps the incoming HttpServletRequest and overrides getSession() so it resolves against a SessionRepository instead of the servlet container. Because it wraps the request very early — before Spring Security and your controllers — all downstream code keeps calling the standard Servlet API while transparently reading and writing the external store. The container's own HttpSession is never used. This is why adopting Spring Session needs no application code changes.","correct":"SessionRepositoryFilter wraps the incoming HttpServletRequest and overrides getSession() so it resolves against a SessionRepository instead of the servlet container.","distractors":["Spring Session's CookieHttpSessionIdResolver uses a cookie named SESSION holding a Base64-encoded id, not the container's JSESSIONID.","On login, Spring Security's default sessionFixation().changeSessionId() rotates the session id, defeating fixation.","The session data key (spring:session:sessions:<id>) is given a TTL of the max inactive interval plus a 5-minute buffer, while a separate expires:<id> key holds the exact TTL and triggers the expiration event."],"distractorIds":["spring/spring_session#924a81ccbc0e","spring/spring_session#2f2f21de13d0","spring/spring_session#3927bf72efa6"],"concepts":["sessionrepository","sessionrepositoryfilter","getsession","httpservletrequest","trick","replace"],"correctMd":"`SessionRepositoryFilter` wraps the incoming `HttpServletRequest` and overrides `getSession()` so it resolves against a `SessionRepository` instead of the servlet container.","answerFullMd":"`SessionRepositoryFilter` wraps the incoming `HttpServletRequest` and overrides `getSession()` so it resolves against a `SessionRepository` instead of the servlet container. Because it wraps the request very early — before Spring Security and your controllers — all downstream code keeps calling the standard Servlet API while transparently reading and writing the external store. The container's own `HttpSession` is never used. This is why adopting Spring Session needs no application code changes.","distractorsMd":["Spring Session's `CookieHttpSessionIdResolver` uses a cookie named `SESSION` holding a Base64-encoded id, not the container's `JSESSIONID`.","On login, Spring Security's default `sessionFixation().changeSessionId()` rotates the session id, defeating fixation.","The session data key (`spring:session:sessions:<id>`) is given a TTL of the max inactive interval plus a 5-minute buffer, while a separate `expires:<id>` key holds the exact TTL and triggers the expiration event."]},{"id":"spring/spring_session#924a81ccbc0e","section":"spring","module":"spring/spring_session","moduleName":"spring session","sourceFile":"README.md","difficulty":"core","question":"What is the default cookie name Spring Session uses, and why did it change from JSESSIONID?","answerFull":"Spring Session's CookieHttpSessionIdResolver uses a cookie named SESSION holding a Base64-encoded id, not the container's JSESSIONID. The rename makes explicit that the container is no longer managing the session and avoids confusion with any residual container cookie. The default max inactive interval is 1800 seconds (30 minutes). You can switch the id to a header (X-Auth-Token) for non-browser clients.","correct":"Spring Session's CookieHttpSessionIdResolver uses a cookie named SESSION holding a Base64-encoded id, not the container's JSESSIONID.","distractors":["Spring Security reads the SecurityContext from the session, so the session must already be swapped to the repository-backed one before Security runs.","Register HeaderHttpSessionIdResolver.xAuthToken(), which transports the session id in the X-Auth-Token header instead of the SESSION cookie.","On login, Spring Security's default sessionFixation().changeSessionId() rotates the session id, defeating fixation."],"distractorIds":["spring/spring_session#ec874a9019a3","spring/spring_session#a65460dafda0","spring/spring_session#2f2f21de13d0"],"concepts":["cookiehttpsessionidresolver","jsessionid","cookie","base64","did","holding"],"questionMd":"What is the default cookie name Spring Session uses, and why did it change from `JSESSIONID`?","correctMd":"Spring Session's `CookieHttpSessionIdResolver` uses a cookie named `SESSION` holding a Base64-encoded id, not the container's `JSESSIONID`.","answerFullMd":"Spring Session's `CookieHttpSessionIdResolver` uses a cookie named `SESSION` holding a Base64-encoded id, not the container's `JSESSIONID`. The rename makes explicit that the container is no longer managing the session and avoids confusion with any residual container cookie. The default max inactive interval is 1800 seconds (30 minutes). You can switch the id to a header (`X-Auth-Token`) for non-browser clients.","distractorsMd":["Spring Security reads the `SecurityContext` from the session, so the session must already be swapped to the repository-backed one before Security runs.","Register `HeaderHttpSessionIdResolver.xAuthToken()`, which transports the session id in the `X-Auth-Token` header instead of the `SESSION` cookie.","On login, Spring Security's default `sessionFixation().changeSessionId()` rotates the session id, defeating fixation."]},{"id":"spring/spring_session#14959b2d09a2","section":"spring","module":"spring/spring_session","moduleName":"spring session","sourceFile":"README.md","difficulty":"core","question":"Why might a SessionExpiredEvent never fire even though sessions do expire?","answerFull":"Because RedisIndexedSessionRepository publishes expiration events from Redis keyspace notifications, which are disabled by default (notify-keyspace-events is empty). The session data still expires via TTL, but no event reaches your @EventListener. Enable notifications with notify-keyspace-events Egx and use the indexed repository (repository-type=indexed); the plain RedisSessionRepository publishes no events at all. Also note Redis delivers expired notifications lazily, so never rely on them for precise real-time timing.","correct":"Because RedisIndexedSessionRepository publishes expiration events from Redis keyspace notifications, which are disabled by default (notify-keyspace-events is empty).","distractors":["Sticky sessions pin a user to one node, so they \"work\" until that node fails or is redeployed, at which point all its sessions vanish and the balancer cannot fail those users over.","The session data key (spring:session:sessions:<id>) is given a TTL of the max inactive interval plus a 5-minute buffer, while a separate expires:<id> key holds the exact TTL and triggers the expiration event.","The default HttpSession lives in the JVM heap of the single instance that created it, so a request the load balancer routes to a different instance cannot see it."],"distractorIds":["spring/spring_session#9d353cccbd2e","spring/spring_session#3927bf72efa6","spring/spring_session#60892b2926ea"],"concepts":["disabled","expire","redisindexedsessionrepository","fire","notify","expiration"],"questionMd":"Why might a `SessionExpiredEvent` never fire even though sessions do expire?","correctMd":"Because `RedisIndexedSessionRepository` publishes expiration events from Redis keyspace notifications, which are disabled by default (`notify-keyspace-events` is empty).","answerFullMd":"Because `RedisIndexedSessionRepository` publishes expiration events from Redis keyspace notifications, which are disabled by default (`notify-keyspace-events` is empty). The session data still expires via TTL, but no event reaches your `@EventListener`. Enable notifications with `notify-keyspace-events Egx` and use the indexed repository (`repository-type=indexed`); the plain `RedisSessionRepository` publishes no events at all. Also note Redis delivers expired notifications lazily, so never rely on them for precise real-time timing.","distractorsMd":[null,"The session data key (`spring:session:sessions:<id>`) is given a TTL of the max inactive interval plus a 5-minute buffer, while a separate `expires:<id>` key holds the exact TTL and triggers the expiration event.","The default `HttpSession` lives in the JVM heap of the single instance that created it, so a request the load balancer routes to a different instance cannot see it."]},{"id":"spring/spring_session#3356eaebf8c7","section":"spring","module":"spring/spring_session","moduleName":"spring session","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between RedisSessionRepository and RedisIndexedSessionRepository?","answerFull":"RedisSessionRepository (the Spring Session 3.0 default) stores one key per session and relies purely on Redis TTL — it is cheapest but offers no principal index, no expiration events, and no concurrent-session control. RedisIndexedSessionRepository stores extra index keys and runs a cleanup task, enabling findByPrincipalName, SessionExpiredEvent, max-sessions, and logout-everywhere, at the cost of more Redis writes and required keyspace notifications. Choose indexed only when you need those cluster-wide features.","correct":"RedisSessionRepository (the Spring Session 3.0 default) stores one key per session and relies purely on Redis TTL — it is cheapest but offers no principal index, no expiration events, and no concurrent-session control.","distractors":["The session data key (spring:session:sessions:<id>) is given a TTL of the max inactive interval plus a 5-minute buffer, while a separate expires:<id> key holds the exact TTL and triggers the expiration event.","With HttpSessionCsrfTokenRepository, the CSRF token lives inside the Spring Session in Redis, so it survives node failover.","Back Spring Security's concurrent-session control with a SpringSessionBackedSessionRegistry instead of the default SessionRegistryImpl."],"distractorIds":["spring/spring_session#3927bf72efa6","spring/spring_session#50665b4d81da","spring/spring_session#6843bee69730"],"concepts":["redisindexedsessionrepository","redissessionrepository","cheapest","principal","expiration","offers"],"questionMd":"What is the difference between `RedisSessionRepository` and `RedisIndexedSessionRepository`?","correctMd":"`RedisSessionRepository` (the Spring Session 3.0 default) stores one key per session and relies purely on Redis TTL — it is cheapest but offers no principal index, no expiration events, and no concurrent-session control.","answerFullMd":"`RedisSessionRepository` (the Spring Session 3.0 default) stores one key per session and relies purely on Redis TTL — it is cheapest but offers no principal index, no expiration events, and no concurrent-session control. `RedisIndexedSessionRepository` stores extra index keys and runs a cleanup task, enabling `findByPrincipalName`, `SessionExpiredEvent`, max-sessions, and logout-everywhere, at the cost of more Redis writes and required keyspace notifications. Choose indexed only when you need those cluster-wide features.","distractorsMd":["The session data key (`spring:session:sessions:<id>`) is given a TTL of the max inactive interval plus a 5-minute buffer, while a separate `expires:<id>` key holds the exact TTL and triggers the expiration event.","With `HttpSessionCsrfTokenRepository`, the CSRF token lives inside the Spring Session in Redis, so it survives node failover.","Back Spring Security's concurrent-session control with a `SpringSessionBackedSessionRegistry` instead of the default `SessionRegistryImpl`."]},{"id":"spring/spring_session#2f2f21de13d0","section":"spring","module":"spring/spring_session","moduleName":"spring session","sourceFile":"README.md","difficulty":"intermediate","question":"How does Spring Session protect against session fixation, and does changeSessionId work with an external store?","answerFull":"On login, Spring Security's default sessionFixation().changeSessionId() rotates the session id, defeating fixation. Spring Session implements that rotation against the external store by generating a new id, copying attributes, deleting the old Redis key, and saving under the new one, so any id an attacker fixed before login is discarded. Modern Spring Session fully supports changeSessionId(); you do not need to fall back to migrateSession(). The rotated id then resolves from Redis on subsequent requests on any node.","correct":"On login, Spring Security's default sessionFixation().changeSessionId() rotates the session id, defeating fixation.","distractors":["Back Spring Security's concurrent-session control with a SpringSessionBackedSessionRegistry instead of the default SessionRegistryImpl.","Spring Security reads the SecurityContext from the session, so the session must already be swapped to the repository-backed one before Security runs.","With HttpSessionCsrfTokenRepository, the CSRF token lives inside the Spring Session in Redis, so it survives node failover."],"distractorIds":["spring/spring_session#6843bee69730","spring/spring_session#ec874a9019a3","spring/spring_session#50665b4d81da"],"concepts":["changesessionid","fixation","protect","sessionfixation","rotates","defeating"],"questionMd":"How does Spring Session protect against session fixation, and does `changeSessionId` work with an external store?","correctMd":"On login, Spring Security's default `sessionFixation().changeSessionId()` rotates the session id, defeating fixation.","answerFullMd":"On login, Spring Security's default `sessionFixation().changeSessionId()` rotates the session id, defeating fixation. Spring Session implements that rotation against the external store by generating a new id, copying attributes, deleting the old Redis key, and saving under the new one, so any id an attacker fixed before login is discarded. Modern Spring Session fully supports `changeSessionId()`; you do not need to fall back to `migrateSession()`. The rotated id then resolves from Redis on subsequent requests on any node.","distractorsMd":["Back Spring Security's concurrent-session control with a `SpringSessionBackedSessionRegistry` instead of the default `SessionRegistryImpl`.","Spring Security reads the `SecurityContext` from the session, so the session must already be swapped to the repository-backed one before Security runs.","With `HttpSessionCsrfTokenRepository`, the CSRF token lives inside the Spring Session in Redis, so it survives node failover."]},{"id":"spring/spring_session#6843bee69730","section":"spring","module":"spring/spring_session","moduleName":"spring session","sourceFile":"README.md","difficulty":"intermediate","question":"How do you enforce \"at most one active session per user\" across a cluster of instances?","answerFull":"Back Spring Security's concurrent-session control with a SpringSessionBackedSessionRegistry instead of the default SessionRegistryImpl. The default registry only tracks sessions in the local JVM, so maximumSessions(1) is meaningless across nodes; the Spring Session-backed registry reads the principal index (findByPrincipalName) from Redis, making the limit cluster-wide. This requires the indexed Redis repository. Set maxSessionsPreventsLogin(false) to evict the oldest session rather than block the new login.","correct":"Back Spring Security's concurrent-session control with a SpringSessionBackedSessionRegistry instead of the default SessionRegistryImpl.","distractors":["Use FindByIndexNameSessionRepository.findByPrincipalName(username) to enumerate all of that user's session ids across the cluster, then call deleteById on each.","With HttpSessionCsrfTokenRepository, the CSRF token lives inside the Spring Session in Redis, so it survives node failover.","Spring Security reads the SecurityContext from the session, so the session must already be swapped to the repository-backed one before Security runs."],"distractorIds":["spring/spring_session#9b72ba92ab3b","spring/spring_session#50665b4d81da","spring/spring_session#ec874a9019a3"],"concepts":["sessionregistryimpl","springsessionbackedsessionregistry","enforce","active","instances","concurrent"],"correctMd":"Back Spring Security's concurrent-session control with a `SpringSessionBackedSessionRegistry` instead of the default `SessionRegistryImpl`.","answerFullMd":"Back Spring Security's concurrent-session control with a `SpringSessionBackedSessionRegistry` instead of the default `SessionRegistryImpl`. The default registry only tracks sessions in the local JVM, so `maximumSessions(1)` is meaningless across nodes; the Spring Session-backed registry reads the principal index (`findByPrincipalName`) from Redis, making the limit cluster-wide. This requires the indexed Redis repository. Set `maxSessionsPreventsLogin(false)` to evict the oldest session rather than block the new login.","distractorsMd":["Use `FindByIndexNameSessionRepository.findByPrincipalName(username)` to enumerate all of that user's session ids across the cluster, then call `deleteById` on each.","With `HttpSessionCsrfTokenRepository`, the CSRF token lives inside the Spring Session in Redis, so it survives node failover.","Spring Security reads the `SecurityContext` from the session, so the session must already be swapped to the repository-backed one before Security runs."]},{"id":"spring/spring_session#9b72ba92ab3b","section":"spring","module":"spring/spring_session","moduleName":"spring session","sourceFile":"README.md","difficulty":"intermediate","question":"How do you implement \"log out everywhere\" for a user?","answerFull":"Use FindByIndexNameSessionRepository.findByPrincipalName(username) to enumerate all of that user's session ids across the cluster, then call deleteById on each. Because the sessions are deleted from the shared store, revocation is instant and global — no blacklist and no waiting for token expiry. This requires the indexed repository, which maintains the principal-to-sessions index. This instant revocation is a headline advantage of stateful sessions over JWT.","correct":"Use FindByIndexNameSessionRepository.findByPrincipalName(username) to enumerate all of that user's session ids across the cluster, then call deleteById on each.","distractors":["RedisSessionRepository (the Spring Session 3.0 default) stores one key per session and relies purely on Redis TTL — it is cheapest but offers no principal index, no expiration events, and no concurrent-session control.","Spring Session's CookieHttpSessionIdResolver uses a cookie named SESSION holding a Base64-encoded id, not the container's JSESSIONID.","With HttpSessionCsrfTokenRepository, the CSRF token lives inside the Spring Session in Redis, so it survives node failover."],"distractorIds":["spring/spring_session#3356eaebf8c7","spring/spring_session#924a81ccbc0e","spring/spring_session#50665b4d81da"],"concepts":["deletebyid","findbyindexnamesessionrepository","findbyprincipalname","enumerate","username","everywhere"],"correctMd":"Use `FindByIndexNameSessionRepository.findByPrincipalName(username)` to enumerate all of that user's session ids across the cluster, then call `deleteById` on each.","answerFullMd":"Use `FindByIndexNameSessionRepository.findByPrincipalName(username)` to enumerate all of that user's session ids across the cluster, then call `deleteById` on each. Because the sessions are deleted from the shared store, revocation is instant and global — no blacklist and no waiting for token expiry. This requires the indexed repository, which maintains the principal-to-sessions index. This instant revocation is a headline advantage of stateful sessions over JWT.","distractorsMd":["`RedisSessionRepository` (the Spring Session 3.0 default) stores one key per session and relies purely on Redis TTL — it is cheapest but offers no principal index, no expiration events, and no concurrent-session control.","Spring Session's `CookieHttpSessionIdResolver` uses a cookie named `SESSION` holding a Base64-encoded id, not the container's `JSESSIONID`.","With `HttpSessionCsrfTokenRepository`, the CSRF token lives inside the Spring Session in Redis, so it survives node failover."]},{"id":"spring/spring_session#7d3bd7b6bfe5","section":"spring","module":"spring/spring_session","moduleName":"spring session","sourceFile":"README.md","difficulty":"intermediate","question":"What is the tradeoff between stateless JWT and stateful Spring Session?","answerFull":"JWT is stateless (no server store, trivial scaling, ~0.1ms local verification) but hard to revoke, larger on the wire (500–2000 bytes per request), and needs a blacklist for early invalidation. Spring Session keeps a small opaque id client-side and full state server-side, giving instant revocation, logout-everywhere, and tiny client payloads, at the cost of a shared store on the request path and one ~0.5–1ms round-trip per request. Choose sessions when revocation and server-side state matter; choose JWT for stateless machine-to-machine APIs.","correct":"JWT is stateless (no server store, trivial scaling, ~0.1ms local verification) but hard to revoke, larger on the wire (500–2000 bytes per request), and needs a blacklist for early invalidation.","distractors":["Register HeaderHttpSessionIdResolver.xAuthToken(), which transports the session id in the X-Auth-Token header instead of the SESSION cookie.","With HttpSessionCsrfTokenRepository, the CSRF token lives inside the Spring Session in Redis, so it survives node failover.","RedisSessionRepository (the Spring Session 3.0 default) stores one key per session and relies purely on Redis TTL — it is cheapest but offers no principal index, no expiration events, and no concurrent-session control."],"distractorIds":["spring/spring_session#a65460dafda0","spring/spring_session#50665b4d81da","spring/spring_session#3356eaebf8c7"],"concepts":["trivial","2000","blacklist","revoke","1ms","invalidation"],"distractorsMd":["Register `HeaderHttpSessionIdResolver.xAuthToken()`, which transports the session id in the `X-Auth-Token` header instead of the `SESSION` cookie.","With `HttpSessionCsrfTokenRepository`, the CSRF token lives inside the Spring Session in Redis, so it survives node failover.","`RedisSessionRepository` (the Spring Session 3.0 default) stores one key per session and relies purely on Redis TTL — it is cheapest but offers no principal index, no expiration events, and no concurrent-session control."]},{"id":"spring/spring_session#a7c4a2a90d17","section":"spring","module":"spring/spring_session","moduleName":"spring session","sourceFile":"README.md","difficulty":"advanced","question":"What does flush-mode control and when would you change it from the default?","answerFull":"flush-mode controls when session changes are written to Redis. The default ON_SAVE buffers all attribute changes and writes once when the request completes (via commitSession()), while IMMEDIATE writes through on every setAttribute. ON_SAVE minimizes Redis round-trips and is right for most apps; switch to IMMEDIATE only when you must not lose an attribute set mid-request if the node crashes before the response commits. IMMEDIATE is markedly chattier on Redis.","correct":"flush-mode controls when session changes are written to Redis.","distractors":["RedisSessionRepository (the Spring Session 3.0 default) stores one key per session and relies purely on Redis TTL — it is cheapest but offers no principal index, no expiration events, and no concurrent-session control.","Register HeaderHttpSessionIdResolver.xAuthToken(), which transports the session id in the X-Auth-Token header instead of the SESSION cookie.","Because RedisIndexedSessionRepository publishes expiration events from Redis keyspace notifications, which are disabled by default (notify-keyspace-events is empty)."],"distractorIds":["spring/spring_session#3356eaebf8c7","spring/spring_session#a65460dafda0","spring/spring_session#14959b2d09a2"],"concepts":["flush","controls","mode","session","written","control"],"questionMd":"What does `flush-mode` control and when would you change it from the default?","correctMd":"`flush-mode` controls when session changes are written to Redis.","answerFullMd":"`flush-mode` controls when session changes are written to Redis. The default `ON_SAVE` buffers all attribute changes and writes once when the request completes (via `commitSession()`), while `IMMEDIATE` writes through on every `setAttribute`. `ON_SAVE` minimizes Redis round-trips and is right for most apps; switch to `IMMEDIATE` only when you must not lose an attribute set mid-request if the node crashes before the response commits. `IMMEDIATE` is markedly chattier on Redis.","distractorsMd":["`RedisSessionRepository` (the Spring Session 3.0 default) stores one key per session and relies purely on Redis TTL — it is cheapest but offers no principal index, no expiration events, and no concurrent-session control.","Register `HeaderHttpSessionIdResolver.xAuthToken()`, which transports the session id in the `X-Auth-Token` header instead of the `SESSION` cookie.","Because `RedisIndexedSessionRepository` publishes expiration events from Redis keyspace notifications, which are disabled by default (`notify-keyspace-events` is empty)."]},{"id":"spring/spring_session#a65460dafda0","section":"spring","module":"spring/spring_session","moduleName":"spring session","sourceFile":"README.md","difficulty":"advanced","question":"How does Spring Session support SPA and mobile clients that do not want cookies?","answerFull":"Register HeaderHttpSessionIdResolver.xAuthToken(), which transports the session id in the X-Auth-Token header instead of the SESSION cookie. The server returns the id in that header on session creation; the client stores it (keychain, memory) and echoes it on each request. This keeps server-side session state while avoiding cookie, CSRF, and CORS friction for non-browser clients.","correct":"Register HeaderHttpSessionIdResolver.xAuthToken(), which transports the session id in the X-Auth-Token header instead of the SESSION cookie.","distractors":["On login, Spring Security's default sessionFixation().changeSessionId() rotates the session id, defeating fixation.","Use FindByIndexNameSessionRepository.findByPrincipalName(username) to enumerate all of that user's session ids across the cluster, then call deleteById on each.","Spring Security reads the SecurityContext from the session, so the session must already be swapped to the repository-backed one before Security runs."],"distractorIds":["spring/spring_session#2f2f21de13d0","spring/spring_session#9b72ba92ab3b","spring/spring_session#ec874a9019a3"],"concepts":["headerhttpsessionidresolver","xauthtoken","mobile","transports","cookies","cookie"],"correctMd":"Register `HeaderHttpSessionIdResolver.xAuthToken()`, which transports the session id in the `X-Auth-Token` header instead of the `SESSION` cookie.","answerFullMd":"Register `HeaderHttpSessionIdResolver.xAuthToken()`, which transports the session id in the `X-Auth-Token` header instead of the `SESSION` cookie. The server returns the id in that header on session creation; the client stores it (keychain, memory) and echoes it on each request. This keeps server-side session state while avoiding cookie, CSRF, and CORS friction for non-browser clients.","distractorsMd":["On login, Spring Security's default `sessionFixation().changeSessionId()` rotates the session id, defeating fixation.","Use `FindByIndexNameSessionRepository.findByPrincipalName(username)` to enumerate all of that user's session ids across the cluster, then call `deleteById` on each.","Spring Security reads the `SecurityContext` from the session, so the session must already be swapped to the repository-backed one before Security runs."]},{"id":"spring/spring_session#a4fa8dcd0339","section":"spring","module":"spring/spring_session","moduleName":"spring session","sourceFile":"README.md","difficulty":"advanced","question":"How is Java serialization a deploy-time risk with Spring Session, and how do you avoid it?","answerFull":"With the default JdkSerializationRedisSerializer, session attributes are stored as Java binary, so a class change between deploys can raise InvalidClassException. It happens when old sessions in Redis are deserialized into the new class shape (serialVersionUID mismatch), 500-ing already-logged-in users. Avoid it by using GenericJackson2JsonRedisSerializer (tolerant of additive field changes and language-neutral) or, if you keep JDK serialization, pinning serialVersionUID and making only backward-compatible changes. Never store non-Serializable attributes under JDK serialization.","correct":"With the default JdkSerializationRedisSerializer, session attributes are stored as Java binary, so a class change between deploys can raise InvalidClassException.","distractors":["Register HeaderHttpSessionIdResolver.xAuthToken(), which transports the session id in the X-Auth-Token header instead of the SESSION cookie.","Spring Security reads the SecurityContext from the session, so the session must already be swapped to the repository-backed one before Security runs.","On login, Spring Security's default sessionFixation().changeSessionId() rotates the session id, defeating fixation."],"distractorIds":["spring/spring_session#a65460dafda0","spring/spring_session#ec874a9019a3","spring/spring_session#2f2f21de13d0"],"concepts":["invalidclassexception","jdkserializationredisserializer","deploys","raise","risk","attributes"],"correctMd":"With the default `JdkSerializationRedisSerializer`, session attributes are stored as Java binary, so a class change between deploys can raise `InvalidClassException`.","answerFullMd":"With the default `JdkSerializationRedisSerializer`, session attributes are stored as Java binary, so a class change between deploys can raise `InvalidClassException`. It happens when old sessions in Redis are deserialized into the new class shape (serialVersionUID mismatch), 500-ing already-logged-in users. Avoid it by using `GenericJackson2JsonRedisSerializer` (tolerant of additive field changes and language-neutral) or, if you keep JDK serialization, pinning `serialVersionUID` and making only backward-compatible changes. Never store non-`Serializable` attributes under JDK serialization.","distractorsMd":["Register `HeaderHttpSessionIdResolver.xAuthToken()`, which transports the session id in the `X-Auth-Token` header instead of the `SESSION` cookie.","Spring Security reads the `SecurityContext` from the session, so the session must already be swapped to the repository-backed one before Security runs.","On login, Spring Security's default `sessionFixation().changeSessionId()` rotates the session id, defeating fixation."]},{"id":"spring/spring_session#50665b4d81da","section":"spring","module":"spring/spring_session","moduleName":"spring session","sourceFile":"README.md","difficulty":"advanced","question":"Where is the CSRF token stored with Spring Session, and does it survive node failover?","answerFull":"With HttpSessionCsrfTokenRepository, the CSRF token lives inside the Spring Session in Redis, so it survives node failover. A form submitted after the user's request is rerouted to a different instance still validates because the token travels with the shared session. A CookieCsrfTokenRepository also survives failover but stores the token on the client instead. Either way, moving the session to a shared store is what keeps CSRF protection intact across a scaled-out cluster.","correct":"With HttpSessionCsrfTokenRepository, the CSRF token lives inside the Spring Session in Redis, so it survives node failover.","distractors":["flush-mode controls when session changes are written to Redis.","With the default JdkSerializationRedisSerializer, session attributes are stored as Java binary, so a class change between deploys can raise InvalidClassException.","The session data key (spring:session:sessions:<id>) is given a TTL of the max inactive interval plus a 5-minute buffer, while a separate expires:<id> key holds the exact TTL and triggers the expiration event."],"distractorIds":["spring/spring_session#a7c4a2a90d17","spring/spring_session#a4fa8dcd0339","spring/spring_session#3927bf72efa6"],"concepts":["httpsessioncsrftokenrepository","survive","csrf","failover","survives","lives"],"correctMd":"With `HttpSessionCsrfTokenRepository`, the CSRF token lives inside the Spring Session in Redis, so it survives node failover.","answerFullMd":"With `HttpSessionCsrfTokenRepository`, the CSRF token lives inside the Spring Session in Redis, so it survives node failover. A form submitted after the user's request is rerouted to a different instance still validates because the token travels with the shared session. A `CookieCsrfTokenRepository` also survives failover but stores the token on the client instead. Either way, moving the session to a shared store is what keeps CSRF protection intact across a scaled-out cluster.","distractorsMd":["`flush-mode` controls when session changes are written to Redis.","With the default `JdkSerializationRedisSerializer`, session attributes are stored as Java binary, so a class change between deploys can raise `InvalidClassException`.","The session data key (`spring:session:sessions:<id>`) is given a TTL of the max inactive interval plus a 5-minute buffer, while a separate `expires:<id>` key holds the exact TTL and triggers the expiration event."]},{"id":"spring/spring_session#9a6cda394411","section":"spring","module":"spring/spring_session","moduleName":"spring session","sourceFile":"README.md","difficulty":"advanced","question":"How does WebFlux session support differ from the servlet model?","answerFull":"WebFlux uses WebSession (not HttpSession) accessed non-blockingly via exchange.getSession(), which returns a Mono<WebSession>, backed by a ReactiveSessionRepository. You enable it with @EnableRedisWebSession and a ReactiveRedisSessionRepository, and a WebSessionManager coordinates resolution. The storage concept is identical — externalized, shared session state — but the API is reactive end to end so no thread blocks on the Redis round-trip.","correct":"WebFlux uses WebSession (not HttpSession) accessed non-blockingly via exchange.getSession(), which returns a Mono<WebSession>, backed by a ReactiveSessionRepository.","distractors":["On login, Spring Security's default sessionFixation().changeSessionId() rotates the session id, defeating fixation.","Back Spring Security's concurrent-session control with a SpringSessionBackedSessionRegistry instead of the default SessionRegistryImpl.","The default HttpSession lives in the JVM heap of the single instance that created it, so a request the load balancer routes to a different instance cannot see it."],"distractorIds":["spring/spring_session#2f2f21de13d0","spring/spring_session#6843bee69730","spring/spring_session#60892b2926ea"],"concepts":["blockingly","reactivesessionrepository","websession","getsession","httpsession","webflux"],"correctMd":"WebFlux uses `WebSession` (not `HttpSession`) accessed non-blockingly via `exchange.getSession()`, which returns a `Mono<WebSession>`, backed by a `ReactiveSessionRepository`.","answerFullMd":"WebFlux uses `WebSession` (not `HttpSession`) accessed non-blockingly via `exchange.getSession()`, which returns a `Mono<WebSession>`, backed by a `ReactiveSessionRepository`. You enable it with `@EnableRedisWebSession` and a `ReactiveRedisSessionRepository`, and a `WebSessionManager` coordinates resolution. The storage concept is identical — externalized, shared session state — but the API is reactive end to end so no thread blocks on the Redis round-trip.","distractorsMd":["On login, Spring Security's default `sessionFixation().changeSessionId()` rotates the session id, defeating fixation.","Back Spring Security's concurrent-session control with a `SpringSessionBackedSessionRegistry` instead of the default `SessionRegistryImpl`.","The default `HttpSession` lives in the JVM heap of the single instance that created it, so a request the load balancer routes to a different instance cannot see it."]},{"id":"spring/spring_session#ec874a9019a3","section":"spring","module":"spring/spring_session","moduleName":"spring session","sourceFile":"README.md","difficulty":"advanced","question":"Why must SessionRepositoryFilter run before Spring Security's filters?","answerFull":"Spring Security reads the SecurityContext from the session, so the session must already be swapped to the repository-backed one before Security runs. Otherwise SecurityContextHolderFilter reads the container session (or nothing) and the user appears unauthenticated despite a valid SESSION cookie. Spring Boot wires springSessionRepositoryFilter at SessionRepositoryFilter.DEFAULT_ORDER (Integer.MIN_VALUE + 50), well ahead of the security chain. Do not override this ordering unless you fully understand the filter chain.","correct":"Spring Security reads the SecurityContext from the session, so the session must already be swapped to the repository-backed one before Security runs.","distractors":["WebFlux uses WebSession (not HttpSession) accessed non-blockingly via exchange.getSession(), which returns a Mono<WebSession>, backed by a ReactiveSessionRepository.","RedisSessionRepository (the Spring Session 3.0 default) stores one key per session and relies purely on Redis TTL — it is cheapest but offers no principal index, no expiration events, and no concurrent-session control.","Back Spring Security's concurrent-session control with a SpringSessionBackedSessionRegistry instead of the default SessionRegistryImpl."],"distractorIds":["spring/spring_session#9a6cda394411","spring/spring_session#3356eaebf8c7","spring/spring_session#6843bee69730"],"concepts":["sessionrepositoryfilter","securitycontext","swapped","repository","filters","backed"],"questionMd":"Why must `SessionRepositoryFilter` run before Spring Security's filters?","correctMd":"Spring Security reads the `SecurityContext` from the session, so the session must already be swapped to the repository-backed one before Security runs.","answerFullMd":"Spring Security reads the `SecurityContext` from the session, so the session must already be swapped to the repository-backed one before Security runs. Otherwise `SecurityContextHolderFilter` reads the container session (or nothing) and the user appears unauthenticated despite a valid `SESSION` cookie. Spring Boot wires `springSessionRepositoryFilter` at `SessionRepositoryFilter.DEFAULT_ORDER` (`Integer.MIN_VALUE + 50`), well ahead of the security chain. Do not override this ordering unless you fully understand the filter chain.","distractorsMd":["WebFlux uses `WebSession` (not `HttpSession`) accessed non-blockingly via `exchange.getSession()`, which returns a `Mono<WebSession>`, backed by a `ReactiveSessionRepository`.","`RedisSessionRepository` (the Spring Session 3.0 default) stores one key per session and relies purely on Redis TTL — it is cheapest but offers no principal index, no expiration events, and no concurrent-session control.","Back Spring Security's concurrent-session control with a `SpringSessionBackedSessionRegistry` instead of the default `SessionRegistryImpl`."]},{"id":"spring/spring_session#3927bf72efa6","section":"spring","module":"spring/spring_session","moduleName":"spring session","sourceFile":"README.md","difficulty":"advanced","question":"Why does RedisIndexedSessionRepository keep the main session key alive ~5 minutes past its TTL?","answerFull":"The session data key (spring:session:sessions:<id>) is given a TTL of the max inactive interval plus a 5-minute buffer, while a separate expires:<id> key holds the exact TTL and triggers the expiration event. The buffer guarantees the session data is still retrievable when the expiration event fires, so the @EventListener can inspect the expiring principal instead of finding an already-deleted key. It is a deliberate consequence of Redis not deleting keys at the precise expiry instant. This is why observed Redis memory holds sessions slightly longer than their nominal timeout.","correct":"The session data key (spring:session:sessions:<id>) is given a TTL of the max inactive interval plus a 5-minute buffer, while a separate expires:<id> key holds the exact TTL and triggers the expiration event.","distractors":["Spring Session's CookieHttpSessionIdResolver uses a cookie named SESSION holding a Base64-encoded id, not the container's JSESSIONID.","RedisSessionRepository (the Spring Session 3.0 default) stores one key per session and relies purely on Redis TTL — it is cheapest but offers no principal index, no expiration events, and no concurrent-session control.","Sticky sessions pin a user to one node, so they \"work\" until that node fails or is redeployed, at which point all its sessions vanish and the balancer cannot fail those users over."],"distractorIds":["spring/spring_session#924a81ccbc0e","spring/spring_session#3356eaebf8c7","spring/spring_session#9d353cccbd2e"],"concepts":["inactive","redisindexedsessionrepository","expiration","alive","minute","sessions"],"questionMd":"Why does `RedisIndexedSessionRepository` keep the main session key alive ~5 minutes past its TTL?","correctMd":"The session data key (`spring:session:sessions:<id>`) is given a TTL of the max inactive interval plus a 5-minute buffer, while a separate `expires:<id>` key holds the exact TTL and triggers the expiration event.","answerFullMd":"The session data key (`spring:session:sessions:<id>`) is given a TTL of the max inactive interval plus a 5-minute buffer, while a separate `expires:<id>` key holds the exact TTL and triggers the expiration event. The buffer guarantees the session data is still retrievable when the expiration event fires, so the `@EventListener` can inspect the expiring principal instead of finding an already-deleted key. It is a deliberate consequence of Redis not deleting keys at the precise expiry instant. This is why observed Redis memory holds sessions slightly longer than their nominal timeout.","distractorsMd":["Spring Session's `CookieHttpSessionIdResolver` uses a cookie named `SESSION` holding a Base64-encoded id, not the container's `JSESSIONID`.","`RedisSessionRepository` (the Spring Session 3.0 default) stores one key per session and relies purely on Redis TTL — it is cheapest but offers no principal index, no expiration events, and no concurrent-session control.",null]},{"id":"spring/spring_session#f5a827c5b2b6","section":"spring","module":"spring/spring_session","moduleName":"spring session","sourceFile":"README.md","difficulty":"advanced","question":"Can multiple applications share one Spring Session store, and what controls isolation?","answerFull":"Yes — apps pointing at the same Redis with the same namespace (default spring:session) read the same sessions, which enables lightweight shared-session SSO across a trusted cluster. Isolation is controlled by the namespace (and the Redis instance/database): give unrelated apps distinct namespaces so their session keys never collide. Sharing sessions this way requires compatible serialization and attribute classes on both sides, so it is best kept within a single team's trusted apps rather than as a general SSO mechanism.","correct":"Yes — apps pointing at the same Redis with the same namespace (default spring:session) read the same sessions, which enables lightweight shared-session SSO across a trusted cluster.","distractors":["Use FindByIndexNameSessionRepository.findByPrincipalName(username) to enumerate all of that user's session ids across the cluster, then call deleteById on each.","The session data key (spring:session:sessions:<id>) is given a TTL of the max inactive interval plus a 5-minute buffer, while a separate expires:<id> key holds the exact TTL and triggers the expiration event.","RedisSessionRepository (the Spring Session 3.0 default) stores one key per session and relies purely on Redis TTL — it is cheapest but offers no principal index, no expiration events, and no concurrent-session control."],"distractorIds":["spring/spring_session#9b72ba92ab3b","spring/spring_session#3927bf72efa6","spring/spring_session#3356eaebf8c7"],"concepts":["sso","apps","pointing","trusted","lightweight","sessions"],"correctMd":"Yes — apps pointing at the same Redis with the same namespace (default `spring:session`) read the same sessions, which enables lightweight shared-session SSO across a trusted cluster.","answerFullMd":"Yes — apps pointing at the same Redis with the same namespace (default `spring:session`) read the same sessions, which enables lightweight shared-session SSO across a trusted cluster. Isolation is controlled by the `namespace` (and the Redis instance/database): give unrelated apps distinct namespaces so their session keys never collide. Sharing sessions this way requires compatible serialization and attribute classes on both sides, so it is best kept within a single team's trusted apps rather than as a general SSO mechanism.","distractorsMd":["Use `FindByIndexNameSessionRepository.findByPrincipalName(username)` to enumerate all of that user's session ids across the cluster, then call `deleteById` on each.","The session data key (`spring:session:sessions:<id>`) is given a TTL of the max inactive interval plus a 5-minute buffer, while a separate `expires:<id>` key holds the exact TTL and triggers the expiration event.","`RedisSessionRepository` (the Spring Session 3.0 default) stores one key per session and relies purely on Redis TTL — it is cheapest but offers no principal index, no expiration events, and no concurrent-session control."]},{"id":"spring/spring_testing#b695820f8446","section":"spring","module":"spring/spring_testing","moduleName":"spring testing","sourceFile":"README.md","difficulty":"core","question":"What is the difference between @MockBean and @Mock in Spring Boot tests?","answerFull":"@Mock is a pure Mockito annotation that creates a mock object managed by MockitoExtension — it has no interaction with the Spring ApplicationContext. @MockBean creates a Mockito mock AND registers it as a Spring bean, replacing any existing bean of that type in the ApplicationContext. Use @Mock in pure unit tests with no Spring context. Use @MockBean in integration tests where the mock must be injected into Spring-managed beans via the context. The critical side-effect of @MockBean is that it changes the ApplicationContext configuration signature, preventing the test from sharing a cached context with tests that do not have that @MockBean.","correct":"@Mock is a pure Mockito annotation that creates a mock object managed by MockitoExtension — it has no interaction with the Spring ApplicationContext.","distractors":["@WebMvcTest loads the Spring MVC layer: @Controller, @ControllerAdvice, @JsonComponent, Converter, Filter, and WebMvcConfigurer beans.","@DynamicPropertySource is a static method annotated with @DynamicPropertySource that runs before the Spring ApplicationContext is created.","@SpyBean wraps an existing real Spring bean with a Mockito spy."],"distractorIds":["spring/spring_testing#71f64034e498","spring/spring_testing#45c7195bde35","spring/spring_testing#e561999b5887"],"concepts":["mockbean","mockitoextension","mockito","difference","mock","applicationcontext"]},{"id":"spring/spring_testing#bb8b27306ba1","section":"spring","module":"spring/spring_testing","moduleName":"spring testing","sourceFile":"README.md","difficulty":"core","question":"Explain the difference between @SpringBootTest(webEnvironment=MOCK) and RANDOM_PORT.","answerFull":"MOCK (the default) loads the full WebApplicationContext but creates a MockServletEnvironment rather than starting a real embedded Tomcat server. No TCP socket is opened. MockMvc operates directly against the DispatcherServlet in memory — no real HTTP is involved. This is faster and simpler. RANDOM_PORT starts a real embedded Tomcat on a randomly selected available port (accessible via @LocalServerPort). HTTP requests go through real TCP sockets, through real Tomcat thread pools, through real HTTP parsing. Use MOCK for most integration tests. Use RANDOM_PORT when testing HTTP-level concerns (redirect handling, HTTPS, actual HTTP headers, WebSocket upgrades, TestRestTemplate).","correct":"MOCK (the default) loads the full WebApplicationContext but creates a MockServletEnvironment rather than starting a real embedded Tomcat server.","distractors":["@Mock is a pure Mockito annotation that creates a mock object managed by MockitoExtension — it has no interaction with the Spring ApplicationContext.","By default, if the test method is annotated with @Transactional, it participates in the same transaction as the service method (via REQUIRED propagation), and the entire transaction is rolled back at test completion.","@DataJpaTest auto-configures an embedded H2 in-memory database by default."],"distractorIds":["spring/spring_testing#b695820f8446","spring/spring_testing#e97010683162","spring/spring_testing#fbba11afb22e"],"concepts":["mockservletenvironment","webapplicationcontext","explain","springboottest","tomcat","difference"]},{"id":"spring/spring_testing#71f64034e498","section":"spring","module":"spring/spring_testing","moduleName":"spring testing","sourceFile":"README.md","difficulty":"core","question":"What is the purpose of @WebMvcTest and what does it NOT load?","answerFull":"@WebMvcTest loads the Spring MVC layer: @Controller, @ControllerAdvice, @JsonComponent, Converter, Filter, and WebMvcConfigurer beans. It also loads Spring Security if present. It does NOT load @Service, @Repository, @Component beans, or auto-configuration unrelated to MVC (DataSource, JPA, Kafka, etc.). This makes @WebMvcTest contexts fast to initialize — typically under 2 seconds. Because services and repositories are not loaded, all dependencies of the controller must be provided as @MockBean. @WebMvcTest is the primary tool for testing controller routing, request validation, exception handler responses, and Spring Security rules at the HTTP layer.","correct":"@WebMvcTest loads the Spring MVC layer: @Controller, @ControllerAdvice, @JsonComponent, Converter, Filter, and WebMvcConfigurer beans.","distractors":["@WithMockUser populates the Spring Security SecurityContext with a UsernamePasswordAuthenticationToken containing the specified username, password, and roles before the test method executes.","@WebMvcTest tests the server side — it loads the DispatcherServlet and tests how your controllers respond to incoming requests.","@Mock is a pure Mockito annotation that creates a mock object managed by MockitoExtension — it has no interaction with the Spring ApplicationContext."],"distractorIds":["spring/spring_testing#150d2ac0e1c1","spring/spring_testing#f0516794e15f","spring/spring_testing#b695820f8446"],"concepts":["controlleradvice","converter","jsoncomponent","webmvcconfigurer","webmvctest","mvc"]},{"id":"spring/spring_testing#fbba11afb22e","section":"spring","module":"spring/spring_testing","moduleName":"spring testing","sourceFile":"README.md","difficulty":"core","question":"How does @DataJpaTest configure the database, and when should you override it?","answerFull":"@DataJpaTest auto-configures an embedded H2 in-memory database by default. It loads JPA auto-configuration, @Entity classes, and Spring Data JPA repositories. It does NOT load @Service or web-layer beans. Each test method is wrapped in a transaction that is rolled back at the end, ensuring isolation. Override the default database with @AutoConfigureTestDatabase(replace=NONE) when your queries use PostgreSQL-specific features (jsonb, window functions, pg_trgm, array operators) that H2 does not support. In this case, combine with Testcontainers to run a real PostgreSQL container, or configure a shared PostgreSQL test instance via DynamicPropertySource.","correct":"@DataJpaTest auto-configures an embedded H2 in-memory database by default.","distractors":["@Sql executes SQL scripts against the test database at a specified execution phase (BEFORE_TEST_METHOD by default, or AFTER_TEST_METHOD for cleanup).","By default, if the test method is annotated with @Transactional, it participates in the same transaction as the service method (via REQUIRED propagation), and the entire transaction is rolled back at test completion.","MOCK (the default) loads the full WebApplicationContext but creates a MockServletEnvironment rather than starting a real embedded Tomcat server."],"distractorIds":["spring/spring_testing#597f555c87b0","spring/spring_testing#e97010683162","spring/spring_testing#bb8b27306ba1"],"concepts":["datajpatest","configures","configure","override","embedded","auto"]},{"id":"spring/spring_testing#e561999b5887","section":"spring","module":"spring/spring_testing","moduleName":"spring testing","sourceFile":"README.md","difficulty":"intermediate","question":"What is @SpyBean and when should you use it instead of @MockBean?","answerFull":"@SpyBean wraps an existing real Spring bean with a Mockito spy. Unlike @MockBean (which replaces the bean with a pure mock that returns null by default), @SpyBean keeps the real implementation — only stubbed methods are intercepted, all others delegate to the real object. Use @SpyBean when you want to verify interactions on a real bean (verify it was called with specific arguments) but do not want to replace its behavior. Use it also when you want to override only one method of a complex real bean. The performance implication is the same as @MockBean — it creates a unique ApplicationContext configuration, potentially breaking context caching.","correct":"@SpyBean wraps an existing real Spring bean with a Mockito spy.","distractors":["@JsonTest loads a minimal Spring ApplicationContext containing only Jackson (or Gson, or Jsonb) auto-configuration and any @JsonComponent beans you have defined.","@Mock is a pure Mockito annotation that creates a mock object managed by MockitoExtension — it has no interaction with the Spring ApplicationContext.","When @MockBean is in a base class, every test class that extends that base shares the same @MockBean declaration."],"distractorIds":["spring/spring_testing#e802e2783052","spring/spring_testing#b695820f8446","spring/spring_testing#0890c63b433c"],"concepts":["mockbean","spybean","spy","mockito","wraps","existing"]},{"id":"spring/spring_testing#e97010683162","section":"spring","module":"spring/spring_testing","moduleName":"spring testing","sourceFile":"README.md","difficulty":"intermediate","question":"How do you test a @Transactional service method to verify that it actually commits to the database?","answerFull":"By default, if the test method is annotated with @Transactional, it participates in the same transaction as the service method (via REQUIRED propagation), and the entire transaction is rolled back at test completion. The data appears in the EntityManager cache during the test but is never committed. To verify a real DB commit: remove @Transactional from the test method (use @Sql or @AfterEach for cleanup instead), or add @Commit to force the transaction to commit. For critical tests that verify ACID properties or trigger database constraints (unique index violations, cascades), remove @Transactional from the test and rely on explicit cleanup. Use @Transactional on tests only when you explicitly want auto-rollback isolation.","correct":"By default, if the test method is annotated with @Transactional, it participates in the same transaction as the service method (via REQUIRED propagation), and the entire transaction is rolled back at test completion.","distractors":["When @MockBean is in a base class, every test class that extends that base shares the same @MockBean declaration.","@WithMockUser populates the Spring Security SecurityContext with a UsernamePasswordAuthenticationToken containing the specified username, password, and roles before the test method executes.","A static @Container field is started once before any test in the class runs and stopped after the last test completes (lifecycle: test class)."],"distractorIds":["spring/spring_testing#0890c63b433c","spring/spring_testing#150d2ac0e1c1","spring/spring_testing#3fefc4d2bbd5"],"concepts":["participates","rolled","commits","propagation","transactional","annotated"]},{"id":"spring/spring_testing#45c7195bde35","section":"spring","module":"spring/spring_testing","moduleName":"spring testing","sourceFile":"README.md","difficulty":"intermediate","question":"How do Testcontainers containers integrate with Spring Boot's ApplicationContext startup?","answerFull":"@DynamicPropertySource is a static method annotated with @DynamicPropertySource that runs before the Spring ApplicationContext is created. It receives a DynamicPropertyRegistry where you can add properties whose values come from the container (getJdbcUrl(), getMappedPort(), etc.). Because the container is a @Container static field (lifecycle tied to the test class or suite), it starts before the @DynamicPropertySource method runs, and the Spring context starts after, using the correct container-assigned ports. For sharing containers across the entire test suite (avoiding restart per class), use a base test class with a @Container static field and inherit it. Spring Boot 3.1+ also supports a ServiceConnection abstraction that eliminates @DynamicPropertySource boilerplate.","correct":"@DynamicPropertySource is a static method annotated with @DynamicPropertySource that runs before the Spring ApplicationContext is created.","distractors":["By default, if the test method is annotated with @Transactional, it participates in the same transaction as the service method (via REQUIRED propagation), and the entire transaction is rolled back at test completion.","A static @Container field is started once before any test in the class runs and stopped after the last test completes (lifecycle: test class).","@JsonTest loads a minimal Spring ApplicationContext containing only Jackson (or Gson, or Jsonb) auto-configuration and any @JsonComponent beans you have defined."],"distractorIds":["spring/spring_testing#e97010683162","spring/spring_testing#3fefc4d2bbd5","spring/spring_testing#e802e2783052"],"concepts":["dynamicpropertysource","integrate","testcontainers","annotated","applicationcontext","containers"]},{"id":"spring/spring_testing#0890c63b433c","section":"spring","module":"spring/spring_testing","moduleName":"spring testing","sourceFile":"README.md","difficulty":"intermediate","question":"What is the risk of using @MockBean in a base test class?","answerFull":"When @MockBean is in a base class, every test class that extends that base shares the same @MockBean declaration. This is actually desirable — it means all those test classes use the same ApplicationContext key, which maximizes cache hits. The risk is that the base class's @MockBean declarations may be overly broad: mocking beans that some tests want to be real, forcing every subclass to work with mocked versions of those beans. A secondary risk is that adding @MockBean to the base class invalidates the cache for all existing tests that previously shared a context without that @MockBean. Design the base class carefully — it should contain the minimal set of @MockBeans that all subclasses require.","correct":"When @MockBean is in a base class, every test class that extends that base shares the same @MockBean declaration.","distractors":["By default, if the test method is annotated with @Transactional, it participates in the same transaction as the service method (via REQUIRED propagation), and the entire transaction is rolled back at test completion.","@Sql executes SQL scripts against the test database at a specified execution phase (BEFORE_TEST_METHOD by default, or AFTER_TEST_METHOD for cleanup).","A static @Container field is started once before any test in the class runs and stopped after the last test completes (lifecycle: test class)."],"distractorIds":["spring/spring_testing#e97010683162","spring/spring_testing#597f555c87b0","spring/spring_testing#3fefc4d2bbd5"],"concepts":["mockbean","declaration","risk","shares","extends","base"]},{"id":"spring/spring_testing#23edf459426d","section":"spring","module":"spring/spring_testing","moduleName":"spring testing","sourceFile":"README.md","difficulty":"intermediate","question":"How do you test asynchronous behavior (e.g., @Async methods or Kafka consumers) in Spring Boot?","answerFull":"For @Async methods, inject the result as a CompletableFuture and call .get(timeout, TimeUnit) to block until completion, or use Awaitility (await().atMost(5, SECONDS).until(() -> condition)). Never use Thread.sleep() — it makes tests brittle. For Kafka consumers in @SpringBootTest with @EmbeddedKafka or Testcontainers, publish a message via KafkaTemplate and use Awaitility to poll the database or a CountDownLatch in the listener for up to a few seconds. The CountDownLatch approach: inject a test-controlled CountDownLatch into the listener bean, decrement it when a message is processed, and await it in the test. Reset the latch with @BeforeEach.","correct":"For @Async methods, inject the result as a CompletableFuture and call .get(timeout, TimeUnit) to block until completion, or use Awaitility (await().atMost(5, SECONDS).until(() -> condition)).","distractors":["@DynamicPropertySource is a static method annotated with @DynamicPropertySource that runs before the Spring ApplicationContext is created.","@WithMockUser populates the Spring Security SecurityContext with a UsernamePasswordAuthenticationToken containing the specified username, password, and roles before the test method executes.","When @MockBean is in a base class, every test class that extends that base shares the same @MockBean declaration."],"distractorIds":["spring/spring_testing#45c7195bde35","spring/spring_testing#150d2ac0e1c1","spring/spring_testing#0890c63b433c"],"concepts":["atmost","awaitility","timeunit","asynchronous","completablefuture","consumers"]},{"id":"spring/spring_testing#6f5936b6c623","section":"spring","module":"spring/spring_testing","moduleName":"spring testing","sourceFile":"README.md","difficulty":"advanced","question":"What is MockMvc's jsonPath() assertion and what library powers it?","answerFull":"jsonPath() uses the Jayway JsonPath library (json-path), which applies XPath-like path expressions to JSON documents. Common patterns: jsonPath(\"$.field\").value(\"expected\") for exact match, jsonPath(\"$.array\").isArray() for type check, jsonPath(\"$.array\", hasSize(3)) for collection size (using Hamcrest matcher), jsonPath(\"$.nested.field\").exists() for presence check. For testing large or complex JSON structures, prefer ResponseBody.content().json(\"{}\") with AssertJ or rest-assured's JsonPath. Avoid overly specific jsonPath assertions on fields that frequently change (e.g., timestamps, generated IDs) — use isNotEmpty() or exists() instead of exact value matching for volatile fields.","correct":"jsonPath() uses the Jayway JsonPath library (json-path), which applies XPath-like path expressions to JSON documents.","distractors":["@DataJpaTest auto-configures an embedded H2 in-memory database by default.","@DynamicPropertySource is a static method annotated with @DynamicPropertySource that runs before the Spring ApplicationContext is created.","@JsonTest loads a minimal Spring ApplicationContext containing only Jackson (or Gson, or Jsonb) auto-configuration and any @JsonComponent beans you have defined."],"distractorIds":["spring/spring_testing#fbba11afb22e","spring/spring_testing#45c7195bde35","spring/spring_testing#e802e2783052"],"concepts":["jayway","jsonpath","mockmvc","xpath","powers","expressions"]},{"id":"spring/spring_testing#597f555c87b0","section":"spring","module":"spring/spring_testing","moduleName":"spring testing","sourceFile":"README.md","difficulty":"advanced","question":"What does @Sql do and how does it differ from using @BeforeEach with JdbcTemplate?","answerFull":"@Sql executes SQL scripts against the test database at a specified execution phase (BEFORE_TEST_METHOD by default, or AFTER_TEST_METHOD for cleanup). It is declarative — the script file is loaded from the classpath and executed as a unit. @BeforeEach with JdbcTemplate is imperative — Java code constructs and executes SQL statements. @Sql is preferred for complex test data setup (large INSERT scripts, schema changes) because the SQL is readable, version-controlled, and reusable across tests. @BeforeEach with JdbcTemplate is preferred when test data depends on runtime values or when you want type-safe construction of test records using repositories. Both approaches work — choose based on data complexity.","correct":"@Sql executes SQL scripts against the test database at a specified execution phase (BEFORE_TEST_METHOD by default, or AFTER_TEST_METHOD for cleanup).","distractors":["A static @Container field is started once before any test in the class runs and stopped after the last test completes (lifecycle: test class).","@DynamicPropertySource is a static method annotated with @DynamicPropertySource that runs before the Spring ApplicationContext is created.","By default, if the test method is annotated with @Transactional, it participates in the same transaction as the service method (via REQUIRED propagation), and the entire transaction is rolled back at test completion."],"distractorIds":["spring/spring_testing#3fefc4d2bbd5","spring/spring_testing#45c7195bde35","spring/spring_testing#e97010683162"],"concepts":["jdbctemplate","cleanup","scripts","differ","specified","phase"]},{"id":"spring/spring_testing#f0516794e15f","section":"spring","module":"spring/spring_testing","moduleName":"spring testing","sourceFile":"README.md","difficulty":"advanced","question":"How does @RestClientTest differ from @WebMvcTest?","answerFull":"@WebMvcTest tests the server side — it loads the DispatcherServlet and tests how your controllers respond to incoming requests. @RestClientTest tests the client side — it loads beans that make outgoing HTTP calls via RestTemplate or RestTemplateBuilder, and provides a MockRestServiceServer to stub and verify those outbound calls. Use @RestClientTest when testing service beans that call external REST APIs, verifying that they construct the correct HTTP requests, handle error responses correctly (404 -> NotFoundException, 503 -> retry), and deserialize responses properly. It does NOT load controllers, services unrelated to the HTTP client, or the JPA layer.","correct":"@WebMvcTest tests the server side — it loads the DispatcherServlet and tests how your controllers respond to incoming requests.","distractors":["@WebMvcTest loads the Spring MVC layer: @Controller, @ControllerAdvice, @JsonComponent, Converter, Filter, and WebMvcConfigurer beans.","MOCK (the default) loads the full WebApplicationContext but creates a MockServletEnvironment rather than starting a real embedded Tomcat server.","@JsonTest loads a minimal Spring ApplicationContext containing only Jackson (or Gson, or Jsonb) auto-configuration and any @JsonComponent beans you have defined."],"distractorIds":["spring/spring_testing#71f64034e498","spring/spring_testing#bb8b27306ba1","spring/spring_testing#e802e2783052"],"concepts":["respond","webmvctest","controllers","dispatcherservlet","differ","incoming"]},{"id":"spring/spring_testing#3fefc4d2bbd5","section":"spring","module":"spring/spring_testing","moduleName":"spring testing","sourceFile":"README.md","difficulty":"advanced","question":"Explain the difference between a static @Container field and an instance @Container field in Testcontainers.","answerFull":"A static @Container field is started once before any test in the class runs and stopped after the last test completes (lifecycle: test class). All test methods in the class share the same container instance. A non-static @Container field is started before each test method and stopped after each — creating and destroying a container per test. Non-static containers are slower but provide complete isolation. In practice, use static containers for performance, and rely on @Transactional rollback or @Sql cleanup scripts for data isolation between tests. For container reuse across multiple test classes, use Ryuk-based singleton containers or a shared base class with a static @Container.","correct":"A static @Container field is started once before any test in the class runs and stopped after the last test completes (lifecycle: test class).","distractors":["When @MockBean is in a base class, every test class that extends that base shares the same @MockBean declaration.","@WithMockUser populates the Spring Security SecurityContext with a UsernamePasswordAuthenticationToken containing the specified username, password, and roles before the test method executes.","@Sql executes SQL scripts against the test database at a specified execution phase (BEFORE_TEST_METHOD by default, or AFTER_TEST_METHOD for cleanup)."],"distractorIds":["spring/spring_testing#0890c63b433c","spring/spring_testing#150d2ac0e1c1","spring/spring_testing#597f555c87b0"],"concepts":["stopped","testcontainers","explain","started","difference","completes"]},{"id":"spring/spring_testing#e802e2783052","section":"spring","module":"spring/spring_testing","moduleName":"spring testing","sourceFile":"README.md","difficulty":"advanced","question":"What is @JsonTest and what does it actually configure?","answerFull":"@JsonTest loads a minimal Spring ApplicationContext containing only Jackson (or Gson, or Jsonb) auto-configuration and any @JsonComponent beans you have defined. It does NOT load controllers, services, repositories, or any other infrastructure. It provides JacksonTester<T>, GsonTester<T>, etc. for fluent assertion on serialized and deserialized JSON. Use @JsonTest to verify custom serializers, custom deserializers, Jackson module configuration (e.g., JavaTimeModule, custom mixins), and @JsonProperty mappings. This is far faster than loading a full context to test JSON behavior and catches serialization regressions early.","correct":"@JsonTest loads a minimal Spring ApplicationContext containing only Jackson (or Gson, or Jsonb) auto-configuration and any @JsonComponent beans you have defined.","distractors":["@DynamicPropertySource is a static method annotated with @DynamicPropertySource that runs before the Spring ApplicationContext is created.","@WebMvcTest loads the Spring MVC layer: @Controller, @ControllerAdvice, @JsonComponent, Converter, Filter, and WebMvcConfigurer beans.","MOCK (the default) loads the full WebApplicationContext but creates a MockServletEnvironment rather than starting a real embedded Tomcat server."],"distractorIds":["spring/spring_testing#45c7195bde35","spring/spring_testing#71f64034e498","spring/spring_testing#bb8b27306ba1"],"concepts":["gson","jackson","jsontest","jsoncomponent","jsonb","configure"]},{"id":"spring/spring_testing#150d2ac0e1c1","section":"spring","module":"spring/spring_testing","moduleName":"spring testing","sourceFile":"README.md","difficulty":"advanced","question":"How do you use @WithMockUser and @WithSecurityContext in security tests?","answerFull":"@WithMockUser populates the Spring Security SecurityContext with a UsernamePasswordAuthenticationToken containing the specified username, password, and roles before the test method executes. The roles are automatically prefixed with ROLE_ (so roles=\"USER\" creates ROLE_USER). Use it to test secured endpoints in @WebMvcTest without an actual authentication flow. @WithSecurityContext is lower-level — you implement a WithSecurityContextFactory that builds a SecurityContext from a custom annotation. Use @WithSecurityContext when you need non-standard authentication types (OAuth2 tokens, JWT claims, multi-tenant principal objects) that @WithMockUser cannot express.","correct":"@WithMockUser populates the Spring Security SecurityContext with a UsernamePasswordAuthenticationToken containing the specified username, password, and roles before the test method executes.","distractors":["A static @Container field is started once before any test in the class runs and stopped after the last test completes (lifecycle: test class).","@Sql executes SQL scripts against the test database at a specified execution phase (BEFORE_TEST_METHOD by default, or AFTER_TEST_METHOD for cleanup).","When @MockBean is in a base class, every test class that extends that base shares the same @MockBean declaration."],"distractorIds":["spring/spring_testing#3fefc4d2bbd5","spring/spring_testing#597f555c87b0","spring/spring_testing#0890c63b433c"],"concepts":["securitycontext","usernamepasswordauthenticationtoken","withmockuser","username","populates","roles"]},{"id":"spring/spring_transactions#b177c2844c70","section":"spring","module":"spring/spring_transactions","moduleName":"spring transactions","sourceFile":"README.md","difficulty":"core","question":"What does @Transactional actually do at runtime?","answerFull":"Spring creates a CGLIB (or JDK) proxy around the bean class at ApplicationContext startup. When a method annotated with @Transactional is invoked through the proxy, the TransactionInterceptor AOP advice runs before and after the method. It calls PlatformTransactionManager.getTransaction() to begin or join a transaction, invokes the real method, then calls commit() or rollback() based on whether an exception was thrown and whether it matches the rollbackFor rules. The proxy approach means any call not going through the proxy — such as a direct internal call using this — will not be intercepted.","correct":"Spring creates a CGLIB (or JDK) proxy around the bean class at ApplicationContext startup.","distractors":["JpaTransactionManager bridges Spring transactions and JPA.","By default, Spring rolls back for RuntimeException (and its subclasses) and java.lang.Error.","When the second service bean is a separate Spring-managed bean, the call goes through the proxy."],"distractorIds":["spring/spring_transactions#dde0c37397df","spring/spring_transactions#4be0ee5752e3","spring/spring_transactions#b11bc6841b09"],"concepts":["jdk","cglib","transactional","applicationcontext","actually","around"]},{"id":"spring/spring_transactions#aa4eace569f6","section":"spring","module":"spring/spring_transactions","moduleName":"spring transactions","sourceFile":"README.md","difficulty":"core","question":"Explain the self-invocation problem and all the ways to fix it.","answerFull":"Self-invocation occurs when a method in a Spring bean calls another method in the same object using this. The call goes directly to the real object, bypassing the AOP proxy entirely, so any @Transactional annotation on the called method has no effect. Fix options: (A) Extract the called method into a separate Spring bean — cleaner, preferred. (B) Inject the bean into itself using @Autowired @Lazy on the same field, then call via the injected reference (the proxy). (C) Use ApplicationContext.getBean() to retrieve the proxy reference at runtime. (D) Use AspectJ compile-time or load-time weaving instead of Spring AOP proxies — weaving modifies the bytecode directly, so internal calls are intercepted. For most production codebases, option A (extraction) is the right answer.","correct":"Self-invocation occurs when a method in a Spring bean calls another method in the same object using this.","distractors":["When the second service bean is a separate Spring-managed bean, the call goes through the proxy.","Spring creates a CGLIB (or JDK) proxy around the bean class at ApplicationContext startup.","If only one PlatformTransactionManager bean exists in the context, Spring uses it automatically."],"distractorIds":["spring/spring_transactions#b11bc6841b09","spring/spring_transactions#b177c2844c70","spring/spring_transactions#b51e76cf2da4"],"concepts":["explain","ways","invocation","fix","another","problem"]},{"id":"spring/spring_transactions#33b97ff67f3b","section":"spring","module":"spring/spring_transactions","moduleName":"spring transactions","sourceFile":"README.md","difficulty":"core","question":"What is the difference between REQUIRES_NEW and NESTED propagation?","answerFull":"REQUIRES_NEW suspends the current transaction entirely and starts a completely independent new transaction with its own connection. The inner transaction commits or rolls back independently of the outer. The outer transaction resumes after the inner completes. NESTED creates a savepoint within the current transaction rather than starting a new one. If the nested block rolls back, it rolls back only to the savepoint — the outer transaction remains intact. NESTED uses the same connection and shares the outer transaction, so the inner rollback does not prevent an eventual outer commit. A critical limitation: NESTED requires JDBC savepoint support (works with DataSourceTransactionManager) but is NOT supported by JTA. REQUIRES_NEW works with JTA.","correct":"REQUIRES_NEW suspends the current transaction entirely and starts a completely independent new transaction with its own connection.","distractors":["@Async causes the method to run in a separate thread pool thread, completely disconnecting from the caller's thread and its transaction context.","If an inner method runs in a *new* physical transaction (REQUIRES_NEW), its rollback is independent: it rolls back its own transaction and the outer one can catch the exception and continue/commit normally.","The exception propagates normally to the caller, but Spring does NOT roll back the transaction — it commits instead."],"distractorIds":["spring/spring_transactions#c34d9c661761","spring/spring_transactions#1089c56a8e46","spring/spring_transactions#d3440b4ff9b6"],"concepts":["suspends","completely","propagation","difference","entirely","nested"],"distractorsMd":[null,"If an inner method runs in a *new* physical transaction (`REQUIRES_NEW`), its rollback is independent: it rolls back its own transaction and the outer one can catch the exception and continue/commit normally.",null]},{"id":"spring/spring_transactions#acc76f31f61a","section":"spring","module":"spring/spring_transactions","moduleName":"spring transactions","sourceFile":"README.md","difficulty":"core","question":"Why does readOnly=true improve performance and what exactly does it affect?","answerFull":"readOnly=true affects three layers. First, Hibernate: it sets FlushMode.NEVER on the session, skipping dirty checking entirely on flush — this avoids the per-entity snapshot comparison that normally runs before every commit. For a transaction loading 500 entities, dirty checking can be a significant CPU cost. Second, the JDBC driver: many drivers propagate Connection.setReadOnly(true) to the DB, which may disable certain lock acquisitions. Third, at the infrastructure level: PostgreSQL and some connection pool proxies (like PgBouncer in replica mode or AWS RDS Proxy) use the read-only hint to route the connection to a read replica. The hint is not a guarantee — a developer calling save() inside a readOnly transaction will likely get an exception from Hibernate, which is the intended behavior.","correct":"readOnly=true affects three layers.","distractors":["@Async causes the method to run in a separate thread pool thread, completely disconnecting from the caller's thread and its transaction context.","@Transactional is declarative — Spring wraps the entire method in a transaction via AOP proxy.","By default, Spring rolls back for RuntimeException (and its subclasses) and java.lang.Error."],"distractorIds":["spring/spring_transactions#c34d9c661761","spring/spring_transactions#42c1de206775","spring/spring_transactions#4be0ee5752e3"],"concepts":["readonly","affect","affects","improve","performance","layers"]},{"id":"spring/spring_transactions#4be0ee5752e3","section":"spring","module":"spring/spring_transactions","moduleName":"spring transactions","sourceFile":"README.md","difficulty":"core","question":"By default, which exceptions trigger a rollback in @Transactional?","answerFull":"By default, Spring rolls back for RuntimeException (and its subclasses) and java.lang.Error. Checked exceptions (those that extend Exception but not RuntimeException) do NOT trigger rollback by default. This default was inherited from EJB semantics and reflects the philosophy that checked exceptions represent expected business conditions, not unexpected failures. In practice, this default is dangerous for business operations. The safer pattern is to declare rollbackFor = Exception.class on any service method that performs multiple DB writes, ensuring that ANY exception — including checked ones from third-party libraries — triggers a rollback.","correct":"By default, Spring rolls back for RuntimeException (and its subclasses) and java.lang.Error.","distractors":["Spring creates a CGLIB (or JDK) proxy around the bean class at ApplicationContext startup.","@Transactional is declarative — Spring wraps the entire method in a transaction via AOP proxy.","JpaTransactionManager bridges Spring transactions and JPA."],"distractorIds":["spring/spring_transactions#b177c2844c70","spring/spring_transactions#42c1de206775","spring/spring_transactions#dde0c37397df"],"concepts":["runtimeexception","rolls","lang","trigger","transactional","subclasses"]},{"id":"spring/spring_transactions#b51e76cf2da4","section":"spring","module":"spring/spring_transactions","moduleName":"spring transactions","sourceFile":"README.md","difficulty":"intermediate","question":"How does Spring determine which PlatformTransactionManager to use when multiple are configured?","answerFull":"If only one PlatformTransactionManager bean exists in the context, Spring uses it automatically. If multiple exist (e.g., DataSourceTransactionManager for one DB and JpaTransactionManager for another), you must either: (A) declare one as @Primary, which Spring picks by default; or (B) reference a specific manager by name in the annotation: @Transactional(\"secondaryTransactionManager\"). TransactionManagementConfigurer interface can also programmatically designate the default. In tests, @Transactional picks up the single transaction manager in the test context unless overridden.","correct":"If only one PlatformTransactionManager bean exists in the context, Spring uses it automatically.","distractors":["The exception propagates normally to the caller, but Spring does NOT roll back the transaction — it commits instead.","Self-invocation occurs when a method in a Spring bean calls another method in the same object using this.","When the second service bean is a separate Spring-managed bean, the call goes through the proxy."],"distractorIds":["spring/spring_transactions#d3440b4ff9b6","spring/spring_transactions#aa4eace569f6","spring/spring_transactions#b11bc6841b09"],"concepts":["platformtransactionmanager","determine","exists","configured","automatically","bean"]},{"id":"spring/spring_transactions#d3440b4ff9b6","section":"spring","module":"spring/spring_transactions","moduleName":"spring transactions","sourceFile":"README.md","difficulty":"intermediate","question":"What happens if a @Transactional method throws an exception that is listed in noRollbackFor?","answerFull":"The exception propagates normally to the caller, but Spring does NOT roll back the transaction — it commits instead. This is the reverse of rollbackFor. Use noRollbackFor for specific RuntimeException subclasses that represent non-fatal conditions: for example, an inventory warning that is a RuntimeException but should not roll back the entire order creation. Example: @Transactional(noRollbackFor = InventoryWarningException.class). This is used rarely; most engineers should default to committing on success and rolling back on any exception.","correct":"The exception propagates normally to the caller, but Spring does NOT roll back the transaction — it commits instead.","distractors":["If only one PlatformTransactionManager bean exists in the context, Spring uses it automatically.","@Transactional is declarative — Spring wraps the entire method in a transaction via AOP proxy.","REQUIRES_NEW suspends the current transaction entirely and starts a completely independent new transaction with its own connection."],"distractorIds":["spring/spring_transactions#b51e76cf2da4","spring/spring_transactions#42c1de206775","spring/spring_transactions#33b97ff67f3b"],"concepts":["listed","roll","commits","propagates","normally","transactional"]},{"id":"spring/spring_transactions#42c1de206775","section":"spring","module":"spring/spring_transactions","moduleName":"spring transactions","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between TransactionTemplate and using @Transactional?","answerFull":"@Transactional is declarative — Spring wraps the entire method in a transaction via AOP proxy. It is the right choice for service layer methods where the entire method should be transactional. TransactionTemplate is programmatic — you define a code block and pass it as a lambda/callback. The advantage is fine-grained control: you can have multiple transaction boundaries within a single method, set rollback-only programmatically via TransactionStatus, vary timeouts or propagation per block, and use it in contexts where AOP proxies are unavailable (e.g., abstract base classes with overriding edge cases). TransactionTemplate is also useful in batch processing where you want to commit per chunk within a loop.","correct":"@Transactional is declarative — Spring wraps the entire method in a transaction via AOP proxy.","distractors":["REQUIRES_NEW suspends the current transaction entirely and starts a completely independent new transaction with its own connection.","XA (two-phase commit) provides ACID consistency across multiple resources within a single distributed transaction.","When the second service bean is a separate Spring-managed bean, the call goes through the proxy."],"distractorIds":["spring/spring_transactions#33b97ff67f3b","spring/spring_transactions#afce2cdab4b0","spring/spring_transactions#b11bc6841b09"],"concepts":["declarative","difference","aop","transactional","wraps","proxy"]},{"id":"spring/spring_transactions#dde0c37397df","section":"spring","module":"spring/spring_transactions","moduleName":"spring transactions","sourceFile":"README.md","difficulty":"intermediate","question":"How does Spring transaction management integrate with JPA's EntityManager?","answerFull":"JpaTransactionManager bridges Spring transactions and JPA. When a @Transactional method is entered, JpaTransactionManager calls EntityManagerFactory.createEntityManager(), binds it to the current thread via ThreadLocal in EntityManagerFactoryUtils, and begins a JDBC transaction on the underlying connection. Repository code that calls EntityManagerHolder.getEntityManager() receives this bound EntityManager. On commit, JpaTransactionManager calls EntityManager.flush() (runs dirty checking, sends SQL), then the JDBC commit, then closes the EntityManager. This means the EntityManager lifecycle is tied to the transaction — entity managed state is valid only within the transaction boundary.","correct":"JpaTransactionManager bridges Spring transactions and JPA.","distractors":["If an inner method runs in a *new* physical transaction (REQUIRES_NEW), its rollback is independent: it rolls back its own transaction and the outer one can catch the exception and continue/commit normally.","@Async causes the method to run in a separate thread pool thread, completely disconnecting from the caller's thread and its transaction context.","@Transactional is declarative — Spring wraps the entire method in a transaction via AOP proxy."],"distractorIds":["spring/spring_transactions#1089c56a8e46","spring/spring_transactions#c34d9c661761","spring/spring_transactions#42c1de206775"],"concepts":["bridges","integrate","jpatransactionmanager","entitymanager","jpa","management"],"distractorsMd":["If an inner method runs in a *new* physical transaction (`REQUIRES_NEW`), its rollback is independent: it rolls back its own transaction and the outer one can catch the exception and continue/commit normally.",null,null]},{"id":"spring/spring_transactions#b11bc6841b09","section":"spring","module":"spring/spring_transactions","moduleName":"spring transactions","sourceFile":"README.md","difficulty":"advanced","question":"How do you handle the case where a @Transactional service method calls another @Transactional method in a different service bean?","answerFull":"When the second service bean is a separate Spring-managed bean, the call goes through the proxy. The propagation behavior is determined by the @Transactional annotation on the called method. With default REQUIRED, the called method joins the existing transaction — both methods share the same transaction, commit, and rollback. With REQUIRES_NEW, the called method gets its own transaction (current one suspended). With NESTED, a savepoint is created. The key is that cross-bean calls respect propagation correctly because both beans are proxied, unlike same-bean self-invocation. However, both methods sharing a REQUIRED transaction means any unhandled exception in the inner call rolls back the entire transaction including the outer method's work.","correct":"When the second service bean is a separate Spring-managed bean, the call goes through the proxy.","distractors":["If an inner method runs in a *new* physical transaction (REQUIRES_NEW), its rollback is independent: it rolls back its own transaction and the outer one can catch the exception and continue/commit normally.","@Transactional is declarative — Spring wraps the entire method in a transaction via AOP proxy.","By default, Spring rolls back for RuntimeException (and its subclasses) and java.lang.Error."],"distractorIds":["spring/spring_transactions#1089c56a8e46","spring/spring_transactions#42c1de206775","spring/spring_transactions#4be0ee5752e3"],"concepts":["goes","transactional","handle","managed","another","proxy"],"distractorsMd":["If an inner method runs in a *new* physical transaction (`REQUIRES_NEW`), its rollback is independent: it rolls back its own transaction and the outer one can catch the exception and continue/commit normally.",null,null]},{"id":"spring/spring_transactions#bef907eee0cd","section":"spring","module":"spring/spring_transactions","moduleName":"spring transactions","sourceFile":"README.md","difficulty":"advanced","question":"Describe the Outbox Pattern and how it relates to Spring transactions.","answerFull":"The Outbox Pattern solves the dual-write problem: you need to write to the DB and publish a message to a broker atomically. XA transactions can do this but are operationally expensive. The Outbox Pattern writes the \"message to be sent\" as a row in an outbox table in the SAME DB transaction as the business data. A separate background process (Debezium CDC, a scheduled Spring task) reads unprocessed outbox rows and publishes them to Kafka/RabbitMQ, then marks them as processed. Spring implementation: in the @Transactional service method, save the business entity and save an OutboxEvent row in the same transaction. Use TransactionSynchronizationManager.afterCommit to trigger the publisher immediately after commit (for low latency), or rely on a @Scheduled poller for reliability.","correct":"The Outbox Pattern solves the dual-write problem: you need to write to the DB and publish a message to a broker atomically.","distractors":["The exception propagates normally to the caller, but Spring does NOT roll back the transaction — it commits instead.","When the second service bean is a separate Spring-managed bean, the call goes through the proxy.","If only one PlatformTransactionManager bean exists in the context, Spring uses it automatically."],"distractorIds":["spring/spring_transactions#d3440b4ff9b6","spring/spring_transactions#b11bc6841b09","spring/spring_transactions#b51e76cf2da4"],"concepts":["describe","publish","atomically","outbox","dual","solves"]},{"id":"spring/spring_transactions#c34d9c661761","section":"spring","module":"spring/spring_transactions","moduleName":"spring transactions","sourceFile":"README.md","difficulty":"advanced","question":"What happens to @Transactional when used with @Async?","answerFull":"@Async causes the method to run in a separate thread pool thread, completely disconnecting from the caller's thread and its transaction context. Spring's transaction infrastructure uses ThreadLocal to bind transaction state to the current thread. When @Async spawns a new thread, the new thread has no transaction bound to it. If the async method also has @Transactional, Spring starts a NEW transaction on the async thread (Propagation.REQUIRED → no existing tx → create new). This new transaction is completely independent of the caller's. The implication: data written in the caller's uncommitted transaction is NOT visible to the async thread until it commits. This creates a race condition if the async method reads data the caller just wrote.","correct":"@Async causes the method to run in a separate thread pool thread, completely disconnecting from the caller's thread and its transaction context.","distractors":["REQUIRES_NEW suspends the current transaction entirely and starts a completely independent new transaction with its own connection.","Self-invocation occurs when a method in a Spring bean calls another method in the same object using this.","If an inner method runs in a *new* physical transaction (REQUIRES_NEW), its rollback is independent: it rolls back its own transaction and the outer one can catch the exception and continue/commit normally."],"distractorIds":["spring/spring_transactions#33b97ff67f3b","spring/spring_transactions#aa4eace569f6","spring/spring_transactions#1089c56a8e46"],"concepts":["disconnecting","completely","transactional","caller","happens","causes"],"distractorsMd":[null,null,"If an inner method runs in a *new* physical transaction (`REQUIRES_NEW`), its rollback is independent: it rolls back its own transaction and the outer one can catch the exception and continue/commit normally."]},{"id":"spring/spring_transactions#a387e30a8e7c","section":"spring","module":"spring/spring_transactions","moduleName":"spring transactions","sourceFile":"README.md","difficulty":"advanced","question":"How do isolation levels map to database locking behavior in PostgreSQL?","answerFull":"PostgreSQL uses MVCC (Multi-Version Concurrency Control) rather than read locks. READ_UNCOMMITTED behaves the same as READ_COMMITTED in PostgreSQL — the engine never returns dirty reads. READ_COMMITTED (default) takes a new snapshot at the start of each statement; non-repeatable reads are possible. REPEATABLE_READ takes a snapshot at transaction start; the same query returns identical results within the transaction; phantom reads on aggregate queries are still theoretically possible but rare in MVCC. SERIALIZABLE uses Serializable Snapshot Isolation (SSI) — no locks for reads, but tracks read/write dependencies and aborts transactions with serialization conflicts. PostgreSQL's implementation means SERIALIZABLE has less throughput impact than traditional range-locking databases, but serialization failures (40001 error code) must be handled with retry logic.","correct":"PostgreSQL uses MVCC (Multi-Version Concurrency Control) rather than read locks.","distractors":["Use @DataJpaTest or @SpringBootTest with a real database (Testcontainers).","@Async causes the method to run in a separate thread pool thread, completely disconnecting from the caller's thread and its transaction context.","@Transactional is declarative — Spring wraps the entire method in a transaction via AOP proxy."],"distractorIds":["spring/spring_transactions#61ab32678881","spring/spring_transactions#c34d9c661761","spring/spring_transactions#42c1de206775"],"concepts":["locking","mvcc","concurrency","levels","locks","isolation"]},{"id":"spring/spring_transactions#61ab32678881","section":"spring","module":"spring/spring_transactions","moduleName":"spring transactions","sourceFile":"README.md","difficulty":"advanced","question":"How do you test transaction boundaries and rollback behavior in Spring Boot tests?","answerFull":"Use @DataJpaTest or @SpringBootTest with a real database (Testcontainers). @Transactional on the test class wraps each test in a transaction that rolls back at the end, keeping tests isolated. To test rollback behavior: remove @Transactional from the test, call the service method that should roll back, then assert the DB state using a separate non-transactional read. For testing REQUIRES_NEW (inner tx must persist even if outer rolls back), use TestTransaction.flagForRollback() programmatically. For testing afterCommit callbacks, use a TransactionSynchronizationManager test spy or verify side effects (events fired, emails sent). Testing with embedded H2 misses PostgreSQL-specific isolation behaviors — always run isolation tests against a real Postgres instance via Testcontainers.","correct":"Use @DataJpaTest or @SpringBootTest with a real database (Testcontainers).","distractors":["@Async causes the method to run in a separate thread pool thread, completely disconnecting from the caller's thread and its transaction context.","If only one PlatformTransactionManager bean exists in the context, Spring uses it automatically.","REQUIRES_NEW suspends the current transaction entirely and starts a completely independent new transaction with its own connection."],"distractorIds":["spring/spring_transactions#c34d9c661761","spring/spring_transactions#b51e76cf2da4","spring/spring_transactions#33b97ff67f3b"],"concepts":["testcontainers","datajpatest","springboottest","boundaries","rollback","boot"]},{"id":"spring/spring_transactions#90cca40c2285","section":"spring","module":"spring/spring_transactions","moduleName":"spring transactions","sourceFile":"README.md","difficulty":"advanced","question":"Explain a scenario where a deadlock can occur in Spring transactions and how to prevent it.","answerFull":"Classic deadlock scenario: Thread A holds a lock on Row 1 and waits for Row 2. Thread B holds a lock on Row 2 and waits for Row 1. Both wait forever. In Spring: if two concurrent REQUIRES_NEW transactions update the same rows in different order, a deadlock is possible. For example, TransferService.transfer(A→B) locks account A then B; simultaneously, TransferService.transfer(B→A) locks account B then A — deadlock. Prevention strategies: (A) consistent lock ordering — always acquire locks in the same order (e.g., by ascending account ID). (B) timeout: spring.jpa.properties.javax.persistence.lock.timeout=5000 (ms) — Hibernate throws LockTimeoutException instead of waiting indefinitely. (C) reduce lock scope: use optimistic locking with @Version to avoid DB locks entirely at low contention. (D) narrow the transaction: hold the pessimistic lock for the shortest time possible.","correct":"Classic deadlock scenario: Thread A holds a lock on Row 1 and waits for Row 2.","distractors":["Self-invocation occurs when a method in a Spring bean calls another method in the same object using this.","@Async causes the method to run in a separate thread pool thread, completely disconnecting from the caller's thread and its transaction context.","JpaTransactionManager bridges Spring transactions and JPA."],"distractorIds":["spring/spring_transactions#aa4eace569f6","spring/spring_transactions#c34d9c661761","spring/spring_transactions#dde0c37397df"],"concepts":["explain","scenario","classic","deadlock","occur","prevent"]},{"id":"spring/spring_transactions#afce2cdab4b0","section":"spring","module":"spring/spring_transactions","moduleName":"spring transactions","sourceFile":"README.md","difficulty":"advanced","question":"How does the Saga pattern differ from XA transactions and when should each be used?","answerFull":"XA (two-phase commit) provides ACID consistency across multiple resources within a single distributed transaction. The transaction coordinator (JtaTransactionManager) ensures all resources commit or all roll back. It is synchronous, strongly consistent, but has high latency (two round trips), requires XA-capable drivers (not all NoSQL/cloud DBs support XA), and the coordinator is a single point of failure. The Saga pattern decomposes a distributed business transaction into a sequence of local transactions, each publishing an event or message to trigger the next step. On failure, compensating transactions undo previous steps (e.g., release inventory reservation if payment fails). Sagas provide eventual consistency, not ACID. They have no coordinator SPoF and work with any data store. Use XA when ACID is non-negotiable and the resource set is small and XA-capable (e.g., DB + JMS in a monolith). Use Saga for microservices, high-scale systems, or when using non-XA data stores (Kafka, DynamoDB, MongoDB). Spring Modulith and Eventuate Tram provide Saga orchestration support on top of Spring. (See ../../java/microservices_patterns/ for the saga/outbox mechanics.)","correct":"XA (two-phase commit) provides ACID consistency across multiple resources within a single distributed transaction.","distractors":["The Outbox Pattern solves the dual-write problem: you need to write to the DB and publish a message to a broker atomically.","If an inner method runs in a *new* physical transaction (REQUIRES_NEW), its rollback is independent: it rolls back its own transaction and the outer one can catch the exception and continue/commit normally.","JpaTransactionManager bridges Spring transactions and JPA."],"distractorIds":["spring/spring_transactions#bef907eee0cd","spring/spring_transactions#1089c56a8e46","spring/spring_transactions#dde0c37397df"],"concepts":["acid","differ","saga","phase","transactions","distributed"],"distractorsMd":[null,"If an inner method runs in a *new* physical transaction (`REQUIRES_NEW`), its rollback is independent: it rolls back its own transaction and the outer one can catch the exception and continue/commit normally.",null]},{"id":"spring/spring_transactions#40d5cb587e65","section":"spring","module":"spring/spring_transactions","moduleName":"spring transactions","sourceFile":"README.md","difficulty":"advanced","question":"Why is @Transactional(readOnly = true) more than a hint, and what does it actually change?","answerFull":"It is not just documentation. For Hibernate/JPA it sets the flush mode to MANUAL, so the persistence context performs *no dirty-checking and no automatic flush* at commit — eliminating the cost of snapshotting and comparing every loaded entity, which is a real performance win on read-heavy paths and prevents accidental writes. It also propagates to the JDBC connection as Connection.setReadOnly(true), which some drivers/databases use to route to read replicas or optimize (e.g. Postgres can reject writes). The trap: because there is no dirty-check flush, modifications to managed entities inside a readOnly transaction are silently *not* persisted — developers occasionally \"update\" an entity in a read-only method and are baffled that nothing changes. Use it for all pure-read service methods, never for anything that mutates.","correct":"It is not just documentation.","distractors":["readOnly=true affects three layers.","@Transactional is declarative — Spring wraps the entire method in a transaction via AOP proxy.","@Async causes the method to run in a separate thread pool thread, completely disconnecting from the caller's thread and its transaction context."],"distractorIds":["spring/spring_transactions#acc76f31f61a","spring/spring_transactions#42c1de206775","spring/spring_transactions#c34d9c661761"],"concepts":["hint","readonly","documentation","transactional","actually","just"],"questionMd":"Why is `@Transactional(readOnly = true)` more than a hint, and what does it actually change?","answerFullMd":"It is not just documentation. For Hibernate/JPA it sets the flush mode to `MANUAL`, so the persistence context performs *no dirty-checking and no automatic flush* at commit — eliminating the cost of snapshotting and comparing every loaded entity, which is a real performance win on read-heavy paths and prevents accidental writes. It also propagates to the JDBC connection as `Connection.setReadOnly(true)`, which some drivers/databases use to route to read replicas or optimize (e.g. Postgres can reject writes). The trap: because there is no dirty-check flush, modifications to managed entities inside a `readOnly` transaction are silently *not* persisted — developers occasionally \"update\" an entity in a read-only method and are baffled that nothing changes. Use it for all pure-read service methods, never for anything that mutates."},{"id":"spring/spring_transactions#1089c56a8e46","section":"spring","module":"spring/spring_transactions","moduleName":"spring transactions","sourceFile":"README.md","difficulty":"advanced","question":"What happens to a transaction when an exception is caught inside a nested REQUIRES_NEW call, and what is the \"marked rollback-only\" trap?","answerFull":"If an inner method runs in a *new* physical transaction (REQUIRES_NEW), its rollback is independent: it rolls back its own transaction and the outer one can catch the exception and continue/commit normally. But if the inner method participates in the *same* transaction (default REQUIRED) and throws a rollback-triggering exception, Spring marks the shared transaction rollback-only; even if the outer method catches the exception and tries to proceed, the eventual commit fails with UnexpectedRollbackException (\"Transaction silently rolled back because it has been marked as rollback-only\"). The lesson: catching an exception does not un-mark a transaction for rollback within the same physical transaction — to truly isolate a failing sub-operation so the caller can recover, run it in REQUIRES_NEW, or do not let it mark the shared transaction rollback-only.","correct":"If an inner method runs in a *new* physical transaction (REQUIRES_NEW), its rollback is independent: it rolls back its own transaction and the outer one can catch the exception and continue/commit normally.","distractors":["@Transactional is declarative — Spring wraps the entire method in a transaction via AOP proxy.","When the second service bean is a separate Spring-managed bean, the call goes through the proxy.","Self-invocation occurs when a method in a Spring bean calls another method in the same object using this."],"distractorIds":["spring/spring_transactions#42c1de206775","spring/spring_transactions#b11bc6841b09","spring/spring_transactions#aa4eace569f6"],"concepts":["continue","trap","marked","rolls","caught","catch"],"questionMd":"What happens to a transaction when an exception is caught inside a nested `REQUIRES_NEW` call, and what is the \"marked rollback-only\" trap?","correctMd":"If an inner method runs in a *new* physical transaction (`REQUIRES_NEW`), its rollback is independent: it rolls back its own transaction and the outer one can catch the exception and continue/commit normally.","answerFullMd":"If an inner method runs in a *new* physical transaction (`REQUIRES_NEW`), its rollback is independent: it rolls back its own transaction and the outer one can catch the exception and continue/commit normally. But if the inner method participates in the *same* transaction (default `REQUIRED`) and throws a rollback-triggering exception, Spring marks the shared transaction `rollback-only`; even if the outer method catches the exception and tries to proceed, the eventual commit fails with `UnexpectedRollbackException` (\"Transaction silently rolled back because it has been marked as rollback-only\"). The lesson: catching an exception does not un-mark a transaction for rollback within the same physical transaction — to truly isolate a failing sub-operation so the caller can recover, run it in `REQUIRES_NEW`, or do not let it mark the shared transaction rollback-only."},{"id":"spring/spring_webflux#3b3020209ce5","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"README.md","difficulty":"core","question":"What is the difference between Mono and Flux?","answerFull":"Mono<T> is a publisher that emits 0 or 1 item and then signals completion or error. Flux<T> emits 0 to N items and then signals completion or error. Both are lazy — no computation runs until something subscribes. Use Mono for single-value operations like a database lookup by ID or a POST that returns the created resource. Use Flux for multi-value operations like streaming query results or Server-Sent Events.","correct":"Mono<T> is a publisher that emits 0 or 1 item and then signals completion or error.","distractors":["A Mono or Flux represents a description of a computation, not the execution of it.","Use .timeout(Duration) to emit a TimeoutException if the publisher does not complete within the duration, and .retryWhen(Retry.backoff(maxAttempts, minBackoff)) for exponential backoff retries.","Backpressure is a flow-control mechanism by which a downstream subscriber signals to an upstream publisher how many items it is ready to consume, preventing the publisher from overwhelming the subscriber."],"distractorIds":["spring/spring_webflux#c20711b17bb6","spring/spring_webflux#268a4ad23577","spring/spring_webflux#a5b037dc01d1"],"concepts":["difference","mono","publisher","emits","flux","signals"],"correctMd":"`Mono<T>` is a publisher that emits 0 or 1 item and then signals completion or error.","answerFullMd":"`Mono<T>` is a publisher that emits 0 or 1 item and then signals completion or error. `Flux<T>` emits 0 to N items and then signals completion or error. Both are lazy — no computation runs until something subscribes. Use `Mono` for single-value operations like a database lookup by ID or a POST that returns the created resource. Use `Flux` for multi-value operations like streaming query results or Server-Sent Events.","distractorsMd":["A `Mono` or `Flux` represents a description of a computation, not the execution of it.","Use `.timeout(Duration)` to emit a `TimeoutException` if the publisher does not complete within the duration, and `.retryWhen(Retry.backoff(maxAttempts, minBackoff))` for exponential backoff retries.",null]},{"id":"spring/spring_webflux#c20711b17bb6","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"README.md","difficulty":"core","question":"What does \"lazy\" mean in the context of Reactor, and why does it matter?","answerFull":"A Mono or Flux represents a description of a computation, not the execution of it. Calling Mono.fromSupplier(() -> someExpensiveCall()) does not invoke someExpensiveCall — it only wraps it. The call executes only when subscribe() is invoked. This matters because: (1) the pipeline is composable without triggering side effects, (2) each subscription triggers a fresh execution — important for retry logic and request-scoped operations, and (3) operators can be assembled in one place and subscribed in another, enabling clean separation between pipeline construction and execution.","correct":"A Mono or Flux represents a description of a computation, not the execution of it.","distractors":["Request-Response (Mono in, Mono out), Request-Stream (Mono in, Flux out), Fire-and-Forget (Mono in, Mono<Void> out, no reply), and Request-Channel (Flux in, Flux out, bidirectional).","The handler runs on a Reactor event-loop thread, and blocking it stalls every multiplexed stream on that connection, not just the one request.","Use Mono.fromCallable(() -> blockingCall()).subscribeOn(Schedulers.boundedElastic())."],"distractorIds":["spring/spring_webflux#769fb85554ef","spring/spring_webflux#024f55c2b35d","spring/spring_webflux#57139cee862e"],"concepts":["matter","mono","reactor","flux","description","represents"],"correctMd":"A `Mono` or `Flux` represents a description of a computation, not the execution of it.","answerFullMd":"A `Mono` or `Flux` represents a description of a computation, not the execution of it. Calling `Mono.fromSupplier(() -> someExpensiveCall())` does not invoke `someExpensiveCall` — it only wraps it. The call executes only when `subscribe()` is invoked. This matters because: (1) the pipeline is composable without triggering side effects, (2) each subscription triggers a fresh execution — important for retry logic and request-scoped operations, and (3) operators can be assembled in one place and subscribed in another, enabling clean separation between pipeline construction and execution.","distractorsMd":["Request-Response (`Mono` in, `Mono` out), Request-Stream (`Mono` in, `Flux` out), Fire-and-Forget (`Mono` in, `Mono<Void>` out, no reply), and Request-Channel (`Flux` in, `Flux` out, bidirectional).",null,"Use `Mono.fromCallable(() -> blockingCall()).subscribeOn(Schedulers.boundedElastic())`."]},{"id":"spring/spring_webflux#a5b037dc01d1","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"README.md","difficulty":"core","question":"What is backpressure, and how does Reactor implement it?","answerFull":"Backpressure is a flow-control mechanism by which a downstream subscriber signals to an upstream publisher how many items it is ready to consume, preventing the publisher from overwhelming the subscriber. Reactor implements the Reactive Streams specification: when a subscriber calls subscription.request(n), the upstream publishes at most n items. Operators like limitRate(n) request n items at a time and replenish at 75% consumed. For sources that cannot honor backpressure (like Flux.interval), overflow strategies like onBackpressureDrop() or onBackpressureBuffer(maxSize) are applied.","correct":"Backpressure is a flow-control mechanism by which a downstream subscriber signals to an upstream publisher how many items it is ready to consume, preventing the publisher from overwhelming the subscriber.","distractors":["StepVerifier from reactor-test creates a synchronous test for asynchronous publishers.","Mono<T> is a publisher that emits 0 or 1 item and then signals completion or error.","subscribeOn(scheduler) affects where the subscription-time code (the source's subscribe() call and any upstream code) runs."],"distractorIds":["spring/spring_webflux#86d6a4155fa8","spring/spring_webflux#3b3020209ce5","spring/spring_webflux#62dcb472fd9e"],"concepts":["upstream","overwhelming","subscriber","backpressure","consume","reactor"],"answerFullMd":"Backpressure is a flow-control mechanism by which a downstream subscriber signals to an upstream publisher how many items it is ready to consume, preventing the publisher from overwhelming the subscriber. Reactor implements the Reactive Streams specification: when a subscriber calls `subscription.request(n)`, the upstream publishes at most `n` items. Operators like `limitRate(n)` request `n` items at a time and replenish at 75% consumed. For sources that cannot honor backpressure (like `Flux.interval`), overflow strategies like `onBackpressureDrop()` or `onBackpressureBuffer(maxSize)` are applied.","distractorsMd":["`StepVerifier` from `reactor-test` creates a synchronous test for asynchronous publishers.","`Mono<T>` is a publisher that emits 0 or 1 item and then signals completion or error.","`subscribeOn(scheduler)` affects where the subscription-time code (the source's `subscribe()` call and any upstream code) runs."]},{"id":"spring/spring_webflux#9dab77bab1ab","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"README.md","difficulty":"core","question":"What backpressure strategies does Reactor provide when a publisher outpaces its subscriber?","answerFull":"Reactor provides five overflow strategies for backpressure: limitRate, buffered (bounded or unbounded), onBackpressureDrop, and onBackpressureLatest. limitRate(n) paces demand by requesting n items at a time and replenishing once 75% are consumed — the right default for well-behaved consumers. Unbounded onBackpressureBuffer() queues every item and risks an OutOfMemoryError under sustained overload, while bounded onBackpressureBuffer(n) caps the queue and errors on overflow instead. onBackpressureDrop() discards items the subscriber cannot keep up with, and onBackpressureLatest() keeps only the newest item — both are lossy but appropriate for metrics streams or live UI updates where freshness matters more than completeness. Sources that cannot honor backpressure natively, such as Flux.interval, must have one of these strategies applied explicitly or they will eventually exhaust memory.","correct":"Reactor provides five overflow strategies for backpressure: limitRate, buffered (bounded or unbounded), onBackpressureDrop, and onBackpressureLatest.","distractors":["Backpressure is a flow-control mechanism by which a downstream subscriber signals to an upstream publisher how many items it is ready to consume, preventing the publisher from overwhelming the subscriber.","Reactor Context is an immutable, read-only key-value store that propagates upstream through the reactive chain (against the data flow direction).","RSocket wins when you need application-level backpressure (REQUEST_N), symmetric bidirectional streaming where the server can initiate, transport flexibility (UDP/Aeron), or resumable sessions."],"distractorIds":["spring/spring_webflux#a5b037dc01d1","spring/spring_webflux#2ce051a8d372","spring/spring_webflux#f4762dd4b2b0"],"concepts":["buffered","limitrate","onbackpressuredrop","onbackpressurelatest","subscriber","backpressure"],"correctMd":"Reactor provides five overflow strategies for backpressure: `limitRate`, buffered (bounded or unbounded), `onBackpressureDrop`, and `onBackpressureLatest`.","answerFullMd":"Reactor provides five overflow strategies for backpressure: `limitRate`, buffered (bounded or unbounded), `onBackpressureDrop`, and `onBackpressureLatest`. `limitRate(n)` paces demand by requesting n items at a time and replenishing once 75% are consumed — the right default for well-behaved consumers. Unbounded `onBackpressureBuffer()` queues every item and risks an `OutOfMemoryError` under sustained overload, while bounded `onBackpressureBuffer(n)` caps the queue and errors on overflow instead. `onBackpressureDrop()` discards items the subscriber cannot keep up with, and `onBackpressureLatest()` keeps only the newest item — both are lossy but appropriate for metrics streams or live UI updates where freshness matters more than completeness. Sources that cannot honor backpressure natively, such as `Flux.interval`, must have one of these strategies applied explicitly or they will eventually exhaust memory.","distractorsMd":[null,"Reactor `Context` is an immutable, read-only key-value store that propagates upstream through the reactive chain (against the data flow direction).","RSocket wins when you need application-level backpressure (`REQUEST_N`), symmetric bidirectional streaming where the server can initiate, transport flexibility (UDP/Aeron), or resumable sessions."]},{"id":"spring/spring_webflux#4f1aa40eccb3","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"README.md","difficulty":"core","question":"What is the difference between flatMap and concatMap?","answerFull":"flatMap subscribes to all inner publishers concurrently as each item arrives from the source, emitting results as they arrive — so output order may differ from input order. concatMap subscribes to the next inner publisher only after the previous one completes, preserving order but sacrificing concurrency. Use flatMap when maximum throughput matters and order is irrelevant (parallel HTTP calls). Use concatMap when operations must be sequential and ordered (processing financial events where order is contractually required).","correct":"flatMap subscribes to all inner publishers concurrently as each item arrives from the source, emitting results as they arrive — so output order may differ from input order.","distractors":["Throwing or emitting onError in an @MessageMapping handler sends an ERROR frame to the requester, surfacing as an errored Mono/Flux on the client.","StepVerifier from reactor-test creates a synchronous test for asynchronous publishers.","Mono<T> is a publisher that emits 0 or 1 item and then signals completion or error."],"distractorIds":["spring/spring_webflux#f73053efd636","spring/spring_webflux#86d6a4155fa8","spring/spring_webflux#3b3020209ce5"],"concepts":["publishers","subscribes","emitting","arrive","arrives","flatmap"],"correctMd":"`flatMap` subscribes to all inner publishers concurrently as each item arrives from the source, emitting results as they arrive — so output order may differ from input order.","answerFullMd":"`flatMap` subscribes to all inner publishers concurrently as each item arrives from the source, emitting results as they arrive — so output order may differ from input order. `concatMap` subscribes to the next inner publisher only after the previous one completes, preserving order but sacrificing concurrency. Use `flatMap` when maximum throughput matters and order is irrelevant (parallel HTTP calls). Use `concatMap` when operations must be sequential and ordered (processing financial events where order is contractually required).","distractorsMd":["Throwing or emitting `onError` in an `@MessageMapping` handler sends an `ERROR` frame to the requester, surfacing as an errored `Mono`/`Flux` on the client.","`StepVerifier` from `reactor-test` creates a synchronous test for asynchronous publishers.","`Mono<T>` is a publisher that emits 0 or 1 item and then signals completion or error."]},{"id":"spring/spring_webflux#4ae9a6875b1c","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"README.md","difficulty":"intermediate","question":"Why should you never call block() inside a reactive pipeline running on a Netty event-loop thread?","answerFull":"Calling block() suspends the current thread until the inner publisher completes. If the current thread is a Netty event-loop thread, blocking it prevents Netty from processing any other I/O events on that loop — including the I/O completion that the inner publisher is waiting for. This creates a deadlock. Additionally, Reactor 3.x detects blocking calls on non-blocking scheduler threads and throws a BlockingOperationError by default when this guard is enabled. block() is only safe at the application boundary — in main(), in tests, or in code explicitly scheduled on boundedElastic().","correct":"Calling block() suspends the current thread until the inner publisher completes.","distractors":["Mono<T> is a publisher that emits 0 or 1 item and then signals completion or error.","Backpressure is a flow-control mechanism by which a downstream subscriber signals to an upstream publisher how many items it is ready to consume, preventing the publisher from overwhelming the subscriber.","Use .timeout(Duration) to emit a TimeoutException if the publisher does not complete within the duration, and .retryWhen(Retry.backoff(maxAttempts, minBackoff)) for exponential backoff retries."],"distractorIds":["spring/spring_webflux#3b3020209ce5","spring/spring_webflux#a5b037dc01d1","spring/spring_webflux#268a4ad23577"],"concepts":["netty","suspends","publisher","reactive","completes","inner"],"correctMd":"Calling `block()` suspends the current thread until the inner publisher completes.","answerFullMd":"Calling `block()` suspends the current thread until the inner publisher completes. If the current thread is a Netty event-loop thread, blocking it prevents Netty from processing any other I/O events on that loop — including the I/O completion that the inner publisher is waiting for. This creates a deadlock. Additionally, Reactor 3.x detects blocking calls on non-blocking scheduler threads and throws a `BlockingOperationError` by default when this guard is enabled. `block()` is only safe at the application boundary — in `main()`, in tests, or in code explicitly scheduled on `boundedElastic()`.","distractorsMd":["`Mono<T>` is a publisher that emits 0 or 1 item and then signals completion or error.",null,"Use `.timeout(Duration)` to emit a `TimeoutException` if the publisher does not complete within the duration, and `.retryWhen(Retry.backoff(maxAttempts, minBackoff))` for exponential backoff retries."]},{"id":"spring/spring_webflux#2ce051a8d372","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"README.md","difficulty":"intermediate","question":"What is Reactor Context, and why is it needed in reactive programming?","answerFull":"Reactor Context is an immutable, read-only key-value store that propagates upstream through the reactive chain (against the data flow direction). It replaces ThreadLocal for per-request state (trace IDs, security context, locale) in reactive pipelines where operators can execute on different threads and ThreadLocal values are therefore unreliable. You write to the context with .contextWrite(Context.of(key, value)) and read it with Mono.deferContextual(ctx -> ...). Spring Security Reactive uses this to propagate Authentication through reactive chains via ReactiveSecurityContextHolder.","correct":"Reactor Context is an immutable, read-only key-value store that propagates upstream through the reactive chain (against the data flow direction).","distractors":["Routing metadata (MIME type message/x.rsocket.routing.v0) carries a route string that RSocketMessageHandler matches against @MessageMapping route patterns.","WebClient is the non-blocking HTTP client for reactive applications.","Backpressure is a flow-control mechanism by which a downstream subscriber signals to an upstream publisher how many items it is ready to consume, preventing the publisher from overwhelming the subscriber."],"distractorIds":["spring/spring_webflux#09bd037e7030","spring/spring_webflux#843899fa735a","spring/spring_webflux#a5b037dc01d1"],"concepts":["upstream","reactor","propagates","reactive","direction","flow"],"correctMd":"Reactor `Context` is an immutable, read-only key-value store that propagates upstream through the reactive chain (against the data flow direction).","answerFullMd":"Reactor `Context` is an immutable, read-only key-value store that propagates upstream through the reactive chain (against the data flow direction). It replaces `ThreadLocal` for per-request state (trace IDs, security context, locale) in reactive pipelines where operators can execute on different threads and `ThreadLocal` values are therefore unreliable. You write to the context with `.contextWrite(Context.of(key, value))` and read it with `Mono.deferContextual(ctx -> ...)`. Spring Security Reactive uses this to propagate `Authentication` through reactive chains via `ReactiveSecurityContextHolder`.","distractorsMd":["Routing metadata (MIME type `message/x.rsocket.routing.v0`) carries a route string that `RSocketMessageHandler` matches against `@MessageMapping` route patterns.","`WebClient` is the non-blocking HTTP client for reactive applications.",null]},{"id":"spring/spring_webflux#57139cee862e","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"README.md","difficulty":"intermediate","question":"How do you offload blocking I/O when you must use it inside a reactive chain?","answerFull":"Use Mono.fromCallable(() -> blockingCall()).subscribeOn(Schedulers.boundedElastic()). The subscribeOn operator moves the subscription (and therefore the execution of the callable) to a thread from boundedElastic(), which is a pool of expandable threads designed for blocking I/O. The result is then published back to the event loop for downstream processing. boundedElastic() defaults to 10 * CPU cores threads and an unbounded task queue. For latency-sensitive paths, set an explicit bound with Schedulers.newBoundedElastic(threadCap, queueSize, name).","correct":"Use Mono.fromCallable(() -> blockingCall()).subscribeOn(Schedulers.boundedElastic()).","distractors":["Throwing or emitting onError in an @MessageMapping handler sends an ERROR frame to the requester, surfacing as an errored Mono/Flux on the client.","WebClient is the non-blocking HTTP client for reactive applications.","subscribeOn(scheduler) affects where the subscription-time code (the source's subscribe() call and any upstream code) runs."],"distractorIds":["spring/spring_webflux#f73053efd636","spring/spring_webflux#843899fa735a","spring/spring_webflux#62dcb472fd9e"],"concepts":["blockingcall","boundedelastic","fromcallable","offload","schedulers","subscribeon"],"correctMd":"Use `Mono.fromCallable(() -> blockingCall()).subscribeOn(Schedulers.boundedElastic())`.","answerFullMd":"Use `Mono.fromCallable(() -> blockingCall()).subscribeOn(Schedulers.boundedElastic())`. The `subscribeOn` operator moves the subscription (and therefore the execution of the callable) to a thread from `boundedElastic()`, which is a pool of expandable threads designed for blocking I/O. The result is then published back to the event loop for downstream processing. `boundedElastic()` defaults to `10 * CPU cores` threads and an unbounded task queue. For latency-sensitive paths, set an explicit bound with `Schedulers.newBoundedElastic(threadCap, queueSize, name)`.","distractorsMd":["Throwing or emitting `onError` in an `@MessageMapping` handler sends an `ERROR` frame to the requester, surfacing as an errored `Mono`/`Flux` on the client.","`WebClient` is the non-blocking HTTP client for reactive applications.","`subscribeOn(scheduler)` affects where the subscription-time code (the source's `subscribe()` call and any upstream code) runs."]},{"id":"spring/spring_webflux#b77156e962a9","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"README.md","difficulty":"intermediate","question":"Why does Spring WebFlux need R2DBC instead of JDBC to be fully non-blocking?","answerFull":"JDBC is fundamentally blocking, which is incompatible with WebFlux's non-blocking event-loop threads. Every JDBC Connection, Statement, and ResultSet call parks the calling thread until the database responds; running that call directly on a Netty event-loop thread stalls every other request sharing that loop. Wrapping JDBC in subscribeOn(Schedulers.boundedElastic()) makes it safe but not actually non-blocking — a thread is still held per in-flight query, just from a separate elastic pool, so you avoid deadlock without gaining the event loop's concurrency benefit. R2DBC (Reactive Relational Database Connectivity) is a driver specification built on the Reactive Streams contract from the start, so a query yields the thread while waiting for I/O and Reactor resumes it via callback when data arrives — no thread is held during the wait. The tradeoff is ecosystem maturity: R2DBC drivers still lag JDBC on stored procedures, some complex transaction features, and vendor-specific behavior, which is why teams sometimes settle for the boundedElastic wrapper instead of a full migration.","correct":"JDBC is fundamentally blocking, which is incompatible with WebFlux's non-blocking event-loop threads.","distractors":["RSocketStrategies holds the encoders/decoders, route matcher, and metadata extractor for an RSocket connection — the RSocket analog of Spring MVC's HttpMessageConverter configuration.","The handler runs on a Reactor event-loop thread, and blocking it stalls every multiplexed stream on that connection, not just the one request.","Spring Security for WebFlux uses SecurityWebFilterChain instead of Spring MVC's SecurityFilterChain."],"distractorIds":["spring/spring_webflux#104926918f72","spring/spring_webflux#024f55c2b35d","spring/spring_webflux#bc362b24bf6e"],"concepts":["fundamentally","incompatible","webflux","jdbc","blocking","fully"],"answerFullMd":"JDBC is fundamentally blocking, which is incompatible with WebFlux's non-blocking event-loop threads. Every JDBC `Connection`, `Statement`, and `ResultSet` call parks the calling thread until the database responds; running that call directly on a Netty event-loop thread stalls every other request sharing that loop. Wrapping JDBC in `subscribeOn(Schedulers.boundedElastic())` makes it safe but not actually non-blocking — a thread is still held per in-flight query, just from a separate elastic pool, so you avoid deadlock without gaining the event loop's concurrency benefit. R2DBC (Reactive Relational Database Connectivity) is a driver specification built on the Reactive Streams contract from the start, so a query yields the thread while waiting for I/O and Reactor resumes it via callback when data arrives — no thread is held during the wait. The tradeoff is ecosystem maturity: R2DBC drivers still lag JDBC on stored procedures, some complex transaction features, and vendor-specific behavior, which is why teams sometimes settle for the `boundedElastic` wrapper instead of a full migration.","distractorsMd":["`RSocketStrategies` holds the encoders/decoders, route matcher, and metadata extractor for an RSocket connection — the RSocket analog of Spring MVC's `HttpMessageConverter` configuration.",null,"Spring Security for WebFlux uses `SecurityWebFilterChain` instead of Spring MVC's `SecurityFilterChain`."]},{"id":"spring/spring_webflux#843899fa735a","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"README.md","difficulty":"advanced","question":"What is the WebClient alternative to RestTemplate and why is RestTemplate deprecated for WebFlux?","answerFull":"WebClient is the non-blocking HTTP client for reactive applications. RestTemplate is synchronous — it blocks the calling thread for the duration of the HTTP call. Inside a WebFlux application, using RestTemplate blocks the event-loop thread, serializing I/O that should be concurrent. WebClient returns Mono<T> or Flux<T>, integrating natively with reactive chains. In Spring MVC applications, you can also use WebClient and call .block() at the boundary, replacing RestTemplate uniformly. Spring has officially deprecated RestTemplate for new development since Spring 5.","correct":"WebClient is the non-blocking HTTP client for reactive applications.","distractors":["The handler runs on a Reactor event-loop thread, and blocking it stalls every multiplexed stream on that connection, not just the one request.","Throwing or emitting onError in an @MessageMapping handler sends an ERROR frame to the requester, surfacing as an errored Mono/Flux on the client.","Reactor Context is an immutable, read-only key-value store that propagates upstream through the reactive chain (against the data flow direction)."],"distractorIds":["spring/spring_webflux#024f55c2b35d","spring/spring_webflux#f73053efd636","spring/spring_webflux#2ce051a8d372"],"concepts":["resttemplate","webclient","deprecated","webflux","alternative","reactive"],"correctMd":"`WebClient` is the non-blocking HTTP client for reactive applications.","answerFullMd":"`WebClient` is the non-blocking HTTP client for reactive applications. `RestTemplate` is synchronous — it blocks the calling thread for the duration of the HTTP call. Inside a WebFlux application, using `RestTemplate` blocks the event-loop thread, serializing I/O that should be concurrent. `WebClient` returns `Mono<T>` or `Flux<T>`, integrating natively with reactive chains. In Spring MVC applications, you can also use `WebClient` and call `.block()` at the boundary, replacing `RestTemplate` uniformly. Spring has officially deprecated `RestTemplate` for new development since Spring 5.","distractorsMd":[null,"Throwing or emitting `onError` in an `@MessageMapping` handler sends an `ERROR` frame to the requester, surfacing as an errored `Mono`/`Flux` on the client.","Reactor `Context` is an immutable, read-only key-value store that propagates upstream through the reactive chain (against the data flow direction)."]},{"id":"spring/spring_webflux#5da2905e0aa6","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"README.md","difficulty":"advanced","question":"How does WebFlux's annotated controller model differ from Spring MVC's?","answerFull":"The annotations are largely the same (@RestController, @GetMapping, @PathVariable, @RequestBody). The key difference is the return type: MVC methods return T (or ResponseEntity<T>), while WebFlux methods return Mono<T> (or Flux<T>, or Mono<ResponseEntity<T>>). WebFlux also supports Flux<T> for streaming responses (SSE, NDJson). Internally, WebFlux's DispatcherHandler subscribes to the returned publisher and writes items to the response as they arrive. Parameter binding, validation, and exception handling (@ExceptionHandler, @ControllerAdvice) work the same way but return reactive types.","correct":"The annotations are largely the same (@RestController, @GetMapping, @PathVariable, @RequestBody).","distractors":["flatMap subscribes to all inner publishers concurrently as each item arrives from the source, emitting results as they arrive — so output order may differ from input order.","RSocketStrategies holds the encoders/decoders, route matcher, and metadata extractor for an RSocket connection — the RSocket analog of Spring MVC's HttpMessageConverter configuration.","Spring Security for WebFlux uses SecurityWebFilterChain instead of Spring MVC's SecurityFilterChain."],"distractorIds":["spring/spring_webflux#4f1aa40eccb3","spring/spring_webflux#104926918f72","spring/spring_webflux#bc362b24bf6e"],"concepts":["getmapping","pathvariable","requestbody","restcontroller","largely","webflux"],"correctMd":"The annotations are largely the same (`@RestController`, `@GetMapping`, `@PathVariable`, `@RequestBody`).","answerFullMd":"The annotations are largely the same (`@RestController`, `@GetMapping`, `@PathVariable`, `@RequestBody`). The key difference is the return type: MVC methods return `T` (or `ResponseEntity<T>`), while WebFlux methods return `Mono<T>` (or `Flux<T>`, or `Mono<ResponseEntity<T>>`). WebFlux also supports `Flux<T>` for streaming responses (SSE, NDJson). Internally, WebFlux's `DispatcherHandler` subscribes to the returned publisher and writes items to the response as they arrive. Parameter binding, validation, and exception handling (`@ExceptionHandler`, `@ControllerAdvice`) work the same way but return reactive types.","distractorsMd":["`flatMap` subscribes to all inner publishers concurrently as each item arrives from the source, emitting results as they arrive — so output order may differ from input order.","`RSocketStrategies` holds the encoders/decoders, route matcher, and metadata extractor for an RSocket connection — the RSocket analog of Spring MVC's `HttpMessageConverter` configuration.","Spring Security for WebFlux uses `SecurityWebFilterChain` instead of Spring MVC's `SecurityFilterChain`."]},{"id":"spring/spring_webflux#86d6a4155fa8","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"README.md","difficulty":"advanced","question":"How do you test reactive code with StepVerifier?","answerFull":"StepVerifier from reactor-test creates a synchronous test for asynchronous publishers. You create a verifier, declare the expected events in order (expectNext, expectNextMatches, expectError, expectComplete), and call verify() which subscribes and blocks until all expected events arrive or a timeout is reached. Example: java StepVerifier.create(userService.findById(\"123\")) .expectNextMatches(user -> user.getName().equals(\"Alice\")) .expectComplete() .verify(Duration.ofSeconds(5)); StepVerifier.withVirtualTime() enables testing time-based operators (like delayElements, timeout, interval) without actually waiting by manipulating a virtual clock.","correct":"StepVerifier from reactor-test creates a synchronous test for asynchronous publishers.","distractors":["WebClient is the non-blocking HTTP client for reactive applications.","The handler runs on a Reactor event-loop thread, and blocking it stalls every multiplexed stream on that connection, not just the one request.","Reactor Context is an immutable, read-only key-value store that propagates upstream through the reactive chain (against the data flow direction)."],"distractorIds":["spring/spring_webflux#843899fa735a","spring/spring_webflux#024f55c2b35d","spring/spring_webflux#2ce051a8d372"],"concepts":["stepverifier","publishers","asynchronous","reactor","reactive","synchronous"],"correctMd":"`StepVerifier` from `reactor-test` creates a synchronous test for asynchronous publishers.","answerFullMd":"`StepVerifier` from `reactor-test` creates a synchronous test for asynchronous publishers. You create a verifier, declare the expected events in order (`expectNext`, `expectNextMatches`, `expectError`, `expectComplete`), and call `verify()` which subscribes and blocks until all expected events arrive or a timeout is reached. Example: ```java StepVerifier.create(userService.findById(\"123\")) .expectNextMatches(user -> user.getName().equals(\"Alice\")) .expectComplete() .verify(Duration.ofSeconds(5)); ``` `StepVerifier.withVirtualTime()` enables testing time-based operators (like `delayElements`, `timeout`, `interval`) without actually waiting by manipulating a virtual clock.","distractorsMd":["`WebClient` is the non-blocking HTTP client for reactive applications.",null,"Reactor `Context` is an immutable, read-only key-value store that propagates upstream through the reactive chain (against the data flow direction)."]},{"id":"spring/spring_webflux#d57a6baf602a","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"README.md","difficulty":"advanced","question":"What is switchMap and when would you use it?","answerFull":"switchMap subscribes to a new inner publisher each time the source emits, and cancels any previously active inner publisher. Only the results of the most recent inner publisher are emitted. Use it for search-as-you-type scenarios: when a user types a new character, the previous search request is cancelled and a new one starts. Without switchMap, using flatMap, all in-flight requests would complete and results from outdated queries could overwrite results from the latest query. The cancellation propagates upstream through the reactive streams cancel signal, potentially aborting HTTP requests or DB queries mid-flight.","correct":"switchMap subscribes to a new inner publisher each time the source emits, and cancels any previously active inner publisher.","distractors":["Use .timeout(Duration) to emit a TimeoutException if the publisher does not complete within the duration, and .retryWhen(Retry.backoff(maxAttempts, minBackoff)) for exponential backoff retries.","subscribeOn(scheduler) affects where the subscription-time code (the source's subscribe() call and any upstream code) runs.","Backpressure is a flow-control mechanism by which a downstream subscriber signals to an upstream publisher how many items it is ready to consume, preventing the publisher from overwhelming the subscriber."],"distractorIds":["spring/spring_webflux#268a4ad23577","spring/spring_webflux#62dcb472fd9e","spring/spring_webflux#a5b037dc01d1"],"concepts":["switchmap","subscribes","cancels","previously","publisher","emits"],"correctMd":"`switchMap` subscribes to a new inner publisher each time the source emits, and cancels any previously active inner publisher.","answerFullMd":"`switchMap` subscribes to a new inner publisher each time the source emits, and cancels any previously active inner publisher. Only the results of the most recent inner publisher are emitted. Use it for search-as-you-type scenarios: when a user types a new character, the previous search request is cancelled and a new one starts. Without `switchMap`, using `flatMap`, all in-flight requests would complete and results from outdated queries could overwrite results from the latest query. The cancellation propagates upstream through the reactive streams cancel signal, potentially aborting HTTP requests or DB queries mid-flight.","distractorsMd":["Use `.timeout(Duration)` to emit a `TimeoutException` if the publisher does not complete within the duration, and `.retryWhen(Retry.backoff(maxAttempts, minBackoff))` for exponential backoff retries.","`subscribeOn(scheduler)` affects where the subscription-time code (the source's `subscribe()` call and any upstream code) runs.",null]},{"id":"spring/spring_webflux#bc362b24bf6e","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"README.md","difficulty":"advanced","question":"How does Spring Security integrate with WebFlux?","answerFull":"Spring Security for WebFlux uses SecurityWebFilterChain instead of Spring MVC's SecurityFilterChain. Authentication and authorization are implemented as WebFilter instances (WebFlux's equivalent of servlet filters). Security context is stored in Reactor Context and accessed via ReactiveSecurityContextHolder.getContext(). Configuration uses @EnableWebFluxSecurity and a SecurityWebFilterChain bean created with ServerHttpSecurity. Method security uses @EnableReactiveMethodSecurity and @PreAuthorize with SpEL that can reference the authenticated principal from the reactive context.","correct":"Spring Security for WebFlux uses SecurityWebFilterChain instead of Spring MVC's SecurityFilterChain.","distractors":["RSocketStrategies holds the encoders/decoders, route matcher, and metadata extractor for an RSocket connection — the RSocket analog of Spring MVC's HttpMessageConverter configuration.","JDBC is fundamentally blocking, which is incompatible with WebFlux's non-blocking event-loop threads.","A Mono or Flux represents a description of a computation, not the execution of it."],"distractorIds":["spring/spring_webflux#104926918f72","spring/spring_webflux#b77156e962a9","spring/spring_webflux#c20711b17bb6"],"concepts":["integrate","securitywebfilterchain","securityfilterchain","webflux","mvc","security"],"correctMd":"Spring Security for WebFlux uses `SecurityWebFilterChain` instead of Spring MVC's `SecurityFilterChain`.","answerFullMd":"Spring Security for WebFlux uses `SecurityWebFilterChain` instead of Spring MVC's `SecurityFilterChain`. Authentication and authorization are implemented as `WebFilter` instances (WebFlux's equivalent of servlet filters). Security context is stored in Reactor `Context` and accessed via `ReactiveSecurityContextHolder.getContext()`. Configuration uses `@EnableWebFluxSecurity` and a `SecurityWebFilterChain` bean created with `ServerHttpSecurity`. Method security uses `@EnableReactiveMethodSecurity` and `@PreAuthorize` with SpEL that can reference the authenticated principal from the reactive context.","distractorsMd":["`RSocketStrategies` holds the encoders/decoders, route matcher, and metadata extractor for an RSocket connection — the RSocket analog of Spring MVC's `HttpMessageConverter` configuration.",null,"A `Mono` or `Flux` represents a description of a computation, not the execution of it."]},{"id":"spring/spring_webflux#62dcb472fd9e","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between publishOn and subscribeOn?","answerFull":"subscribeOn(scheduler) affects where the subscription-time code (the source's subscribe() call and any upstream code) runs. It determines the thread on which the source begins emitting. publishOn(scheduler) affects where downstream operators and the subscriber run — it inserts a thread switch for all operators that appear after it in the chain. If you have a blocking source and want to run it on boundedElastic, use subscribeOn. If you want to run specific downstream operators on a different scheduler (e.g., move CPU-intensive processing to parallel()), use publishOn. In most real-world scenarios for offloading blocking I/O, subscribeOn(Schedulers.boundedElastic()) is the correct choice.","correct":"subscribeOn(scheduler) affects where the subscription-time code (the source's subscribe() call and any upstream code) runs.","distractors":["switchMap subscribes to a new inner publisher each time the source emits, and cancels any previously active inner publisher.","Use Mono.fromCallable(() -> blockingCall()).subscribeOn(Schedulers.boundedElastic()).","The handler runs on a Reactor event-loop thread, and blocking it stalls every multiplexed stream on that connection, not just the one request."],"distractorIds":["spring/spring_webflux#d57a6baf602a","spring/spring_webflux#57139cee862e","spring/spring_webflux#024f55c2b35d"],"concepts":["publishon","subscribeon","subscription","upstream","subscribe","affects"],"correctMd":"`subscribeOn(scheduler)` affects where the subscription-time code (the source's `subscribe()` call and any upstream code) runs.","answerFullMd":"`subscribeOn(scheduler)` affects where the subscription-time code (the source's `subscribe()` call and any upstream code) runs. It determines the thread on which the source begins emitting. `publishOn(scheduler)` affects where downstream operators and the subscriber run — it inserts a thread switch for all operators that appear after it in the chain. If you have a blocking source and want to run it on `boundedElastic`, use `subscribeOn`. If you want to run specific downstream operators on a different scheduler (e.g., move CPU-intensive processing to `parallel()`), use `publishOn`. In most real-world scenarios for offloading blocking I/O, `subscribeOn(Schedulers.boundedElastic())` is the correct choice.","distractorsMd":["`switchMap` subscribes to a new inner publisher each time the source emits, and cancels any previously active inner publisher.","Use `Mono.fromCallable(() -> blockingCall()).subscribeOn(Schedulers.boundedElastic())`.",null]},{"id":"spring/spring_webflux#268a4ad23577","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"README.md","difficulty":"advanced","question":"How do you handle timeout and retry in a WebClient call?","answerFull":"Use .timeout(Duration) to emit a TimeoutException if the publisher does not complete within the duration, and .retryWhen(Retry.backoff(maxAttempts, minBackoff)) for exponential backoff retries. The Retry.backoff variant supports jitter() to spread retry timing, .filter(e -> e instanceof NetworkException) to retry only on specific errors, and .maxBackoff(maxDuration) to cap the backoff delay. Example: retryWhen(Retry.backoff(3, Duration.ofMillis(100)).maxBackoff(Duration.ofSeconds(2)).jitter(0.5)) retries up to 3 times with 100ms base delay, up to 2 seconds max, with 50% jitter. Place .timeout() before .retryWhen() if you want each attempt to have its own timeout.","correct":"Use .timeout(Duration) to emit a TimeoutException if the publisher does not complete within the duration, and .retryWhen(Retry.backoff(maxAttempts, minBackoff)) for exponential backoff retries.","distractors":["Calling block() suspends the current thread until the inner publisher completes.","switchMap subscribes to a new inner publisher each time the source emits, and cancels any previously active inner publisher.","subscribeOn(scheduler) affects where the subscription-time code (the source's subscribe() call and any upstream code) runs."],"distractorIds":["spring/spring_webflux#4ae9a6875b1c","spring/spring_webflux#d57a6baf602a","spring/spring_webflux#62dcb472fd9e"],"concepts":["minbackoff","retrywhen","webclient","maxattempts","timeoutexception","emit"],"correctMd":"Use `.timeout(Duration)` to emit a `TimeoutException` if the publisher does not complete within the duration, and `.retryWhen(Retry.backoff(maxAttempts, minBackoff))` for exponential backoff retries.","answerFullMd":"Use `.timeout(Duration)` to emit a `TimeoutException` if the publisher does not complete within the duration, and `.retryWhen(Retry.backoff(maxAttempts, minBackoff))` for exponential backoff retries. The `Retry.backoff` variant supports `jitter()` to spread retry timing, `.filter(e -> e instanceof NetworkException)` to retry only on specific errors, and `.maxBackoff(maxDuration)` to cap the backoff delay. Example: `retryWhen(Retry.backoff(3, Duration.ofMillis(100)).maxBackoff(Duration.ofSeconds(2)).jitter(0.5))` retries up to 3 times with 100ms base delay, up to 2 seconds max, with 50% jitter. Place `.timeout()` before `.retryWhen()` if you want each attempt to have its own timeout.","distractorsMd":["Calling `block()` suspends the current thread until the inner publisher completes.","`switchMap` subscribes to a new inner publisher each time the source emits, and cancels any previously active inner publisher.","`subscribeOn(scheduler)` affects where the subscription-time code (the source's `subscribe()` call and any upstream code) runs."]},{"id":"spring/spring_webflux#024f55c2b35d","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"rsocket_reactive_messaging.md","difficulty":"core","question":"Why does a blocking JDBC call inside a reactive @MessageMapping handler cripple an RSocket server?","answerFull":"The handler runs on a Reactor event-loop thread, and blocking it stalls every multiplexed stream on that connection, not just the one request. RSocket carries many logical streams over one connection dispatched by a small pool of event-loop threads; parking a thread on a JDBC call means no other stream on that loop is serviced until the query returns, so p99 latency spikes 10-100x under load. Fix by using R2DBC, or offload the blocking call with Mono.fromCallable(...).subscribeOn(Schedulers.boundedElastic()), and catch violations with BlockHound in tests.","correct":"The handler runs on a Reactor event-loop thread, and blocking it stalls every multiplexed stream on that connection, not just the one request.","distractors":["RSocket wins when you need application-level backpressure (REQUEST_N), symmetric bidirectional streaming where the server can initiate, transport flexibility (UDP/Aeron), or resumable sessions.","RSocket uses one long-lived, multiplexed connection, so all of its logical streams are pinned to whichever backend that connection landed on.","Throwing or emitting onError in an @MessageMapping handler sends an ERROR frame to the requester, surfacing as an errored Mono/Flux on the client."],"distractorIds":["spring/spring_webflux#f4762dd4b2b0","spring/spring_webflux#76755de407a2","spring/spring_webflux#f73053efd636"],"concepts":["messagemapping","multiplexed","stalls","rsocket","jdbc","reactor"],"questionMd":"Why does a blocking JDBC call inside a reactive `@MessageMapping` handler cripple an RSocket server?","answerFullMd":"The handler runs on a Reactor event-loop thread, and blocking it stalls every multiplexed stream on that connection, not just the one request. RSocket carries many logical streams over one connection dispatched by a small pool of event-loop threads; parking a thread on a JDBC call means no other stream on that loop is serviced until the query returns, so p99 latency spikes 10-100x under load. Fix by using R2DBC, or offload the blocking call with `Mono.fromCallable(...).subscribeOn(Schedulers.boundedElastic())`, and catch violations with BlockHound in tests.","distractorsMd":["RSocket wins when you need application-level backpressure (`REQUEST_N`), symmetric bidirectional streaming where the server can initiate, transport flexibility (UDP/Aeron), or resumable sessions.",null,"Throwing or emitting `onError` in an `@MessageMapping` handler sends an `ERROR` frame to the requester, surfacing as an errored `Mono`/`Flux` on the client."]},{"id":"spring/spring_webflux#35948fb53bc2","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"rsocket_reactive_messaging.md","difficulty":"core","question":"How is RSocket's backpressure different from HTTP's flow control?","answerFull":"RSocket serializes the subscriber's request(n) demand into REQUEST_N frames sent to the remote producer, so backpressure is application-level and per-logical-stream. HTTP/1.1 has no flow control at all, and HTTP/2 flow control is a transport-level byte window shared per connection/stream that knows nothing about message boundaries or consumer demand. This is why a slow RSocket consumer can throttle a fast remote producer message-by-message, whereas an HTTP consumer must resort to polling, pagination, or an unbounded buffer.","correct":"RSocket serializes the subscriber's request(n) demand into REQUEST_N frames sent to the remote producer, so backpressure is application-level and per-logical-stream.","distractors":["Request-Response (Mono in, Mono out), Request-Stream (Mono in, Flux out), Fire-and-Forget (Mono in, Mono<Void> out, no reply), and Request-Channel (Flux in, Flux out, bidirectional).","Backpressure is a flow-control mechanism by which a downstream subscriber signals to an upstream publisher how many items it is ready to consume, preventing the publisher from overwhelming the subscriber.","Leasing is connection-level admission control: a responder grants the requester a lease of N requests valid for T milliseconds, and the requester must not exceed it."],"distractorIds":["spring/spring_webflux#769fb85554ef","spring/spring_webflux#a5b037dc01d1","spring/spring_webflux#0d189969bd8c"],"concepts":["subscriber","backpressure","rsocket","serializes","demand","remote"],"correctMd":"RSocket serializes the subscriber's `request(n)` demand into `REQUEST_N` frames sent to the remote producer, so backpressure is application-level and per-logical-stream.","answerFullMd":"RSocket serializes the subscriber's `request(n)` demand into `REQUEST_N` frames sent to the remote producer, so backpressure is application-level and per-logical-stream. HTTP/1.1 has no flow control at all, and HTTP/2 flow control is a transport-level byte window shared per connection/stream that knows nothing about message boundaries or consumer demand. This is why a slow RSocket consumer can throttle a fast remote producer message-by-message, whereas an HTTP consumer must resort to polling, pagination, or an unbounded buffer.","distractorsMd":["Request-Response (`Mono` in, `Mono` out), Request-Stream (`Mono` in, `Flux` out), Fire-and-Forget (`Mono` in, `Mono<Void>` out, no reply), and Request-Channel (`Flux` in, `Flux` out, bidirectional).",null,null]},{"id":"spring/spring_webflux#76755de407a2","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"rsocket_reactive_messaging.md","difficulty":"core","question":"Why can't you put a normal per-request L4/L7 load balancer in front of RSocket?","answerFull":"RSocket uses one long-lived, multiplexed connection, so all of its logical streams are pinned to whichever backend that connection landed on. A per-request load balancer assumes each request opens a fresh connection it can route independently, which does not hold — every stream after connect goes to the same backend. Use client-side load balancing (LoadbalanceRSocketClient over a pool of targets) or an RSocket broker that terminates connections and routes logical streams.","correct":"RSocket uses one long-lived, multiplexed connection, so all of its logical streams are pinned to whichever backend that connection landed on.","distractors":["RSocket serializes the subscriber's request(n) demand into REQUEST_N frames sent to the remote producer, so backpressure is application-level and per-logical-stream.","transport: tcp with a port starts a standalone RSocket server on that port, independent of any web server.","Routing metadata (MIME type message/x.rsocket.routing.v0) carries a route string that RSocketMessageHandler matches against @MessageMapping route patterns."],"distractorIds":["spring/spring_webflux#35948fb53bc2","spring/spring_webflux#9ec49bb90c8e","spring/spring_webflux#09bd037e7030"],"concepts":["landed","multiplexed","whichever","rsocket","pinned","front"],"answerFullMd":"RSocket uses one long-lived, multiplexed connection, so all of its logical streams are pinned to whichever backend that connection landed on. A per-request load balancer assumes each request opens a fresh connection it can route independently, which does not hold — every stream after connect goes to the same backend. Use client-side load balancing (`LoadbalanceRSocketClient` over a pool of targets) or an RSocket broker that terminates connections and routes logical streams.","distractorsMd":["RSocket serializes the subscriber's `request(n)` demand into `REQUEST_N` frames sent to the remote producer, so backpressure is application-level and per-logical-stream.","`transport: tcp` with a `port` starts a standalone RSocket server on that port, independent of any web server.","Routing metadata (MIME type `message/x.rsocket.routing.v0`) carries a route string that `RSocketMessageHandler` matches against `@MessageMapping` route patterns."]},{"id":"spring/spring_webflux#90e199899db7","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"rsocket_reactive_messaging.md","difficulty":"core","question":"Does fire-and-forget guarantee the server received the message?","answerFull":"No — fire-and-forget completes as soon as the frame is written to the transport, with no response frame and no server-side acknowledgment. On a reconnect or a server-side error the message can be silently lost, so it suits high-volume, loss-tolerant signals like metrics. For anything that must not be lost, use request-response with an ack or a durable broker.","correct":"No — fire-and-forget completes as soon as the frame is written to the transport, with no response frame and no server-side acknowledgment.","distractors":["Request-Response (Mono in, Mono out), Request-Stream (Mono in, Flux out), Fire-and-Forget (Mono in, Mono<Void> out, no reply), and Request-Channel (Flux in, Flux out, bidirectional).","Routing metadata (MIME type message/x.rsocket.routing.v0) carries a route string that RSocketMessageHandler matches against @MessageMapping route patterns.","RSocket wins when you need application-level backpressure (REQUEST_N), symmetric bidirectional streaming where the server can initiate, transport flexibility (UDP/Aeron), or resumable sessions."],"distractorIds":["spring/spring_webflux#769fb85554ef","spring/spring_webflux#09bd037e7030","spring/spring_webflux#f4762dd4b2b0"],"concepts":["acknowledgment","fire","forget","soon","transport","frame"],"distractorsMd":["Request-Response (`Mono` in, `Mono` out), Request-Stream (`Mono` in, `Flux` out), Fire-and-Forget (`Mono` in, `Mono<Void>` out, no reply), and Request-Channel (`Flux` in, `Flux` out, bidirectional).","Routing metadata (MIME type `message/x.rsocket.routing.v0`) carries a route string that `RSocketMessageHandler` matches against `@MessageMapping` route patterns.","RSocket wins when you need application-level backpressure (`REQUEST_N`), symmetric bidirectional streaming where the server can initiate, transport flexibility (UDP/Aeron), or resumable sessions."]},{"id":"spring/spring_webflux#e67005a992d8","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"rsocket_reactive_messaging.md","difficulty":"core","question":"What happens to in-flight streams when the TCP connection briefly drops?","answerFull":"With resumption enabled, in-flight streams survive a transient drop. Each side tracks the position of frames sent and acknowledged, and after reconnect a RESUME frame declares the last received position so the peer replays unacknowledged frames from its buffer. Keepalive frames detect the dead connection within the configured max-lifetime and trigger the reconnect-and-resume. Without resumption, a drop errors all active streams and the client must resubscribe from scratch.","correct":"With resumption enabled, in-flight streams survive a transient drop.","distractors":["RSocketStrategies holds the encoders/decoders, route matcher, and metadata extractor for an RSocket connection — the RSocket analog of Spring MVC's HttpMessageConverter configuration.","transport: tcp with a port starts a standalone RSocket server on that port, independent of any web server.","RSocket uses one long-lived, multiplexed connection, so all of its logical streams are pinned to whichever backend that connection landed on."],"distractorIds":["spring/spring_webflux#104926918f72","spring/spring_webflux#9ec49bb90c8e","spring/spring_webflux#76755de407a2"],"concepts":["briefly","resumption","survive","transient","enabled","flight"],"answerFullMd":"With resumption enabled, in-flight streams survive a transient drop. Each side tracks the position of frames sent and acknowledged, and after reconnect a `RESUME` frame declares the last received position so the peer replays unacknowledged frames from its buffer. Keepalive frames detect the dead connection within the configured max-lifetime and trigger the reconnect-and-resume. Without resumption, a drop errors all active streams and the client must resubscribe from scratch.","distractorsMd":["`RSocketStrategies` holds the encoders/decoders, route matcher, and metadata extractor for an RSocket connection — the RSocket analog of Spring MVC's `HttpMessageConverter` configuration.","`transport: tcp` with a `port` starts a standalone RSocket server on that port, independent of any web server.",null]},{"id":"spring/spring_webflux#769fb85554ef","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"rsocket_reactive_messaging.md","difficulty":"intermediate","question":"What are the four RSocket interaction models and their Spring signatures?","answerFull":"Request-Response (Mono in, Mono out), Request-Stream (Mono in, Flux out), Fire-and-Forget (Mono in, Mono<Void> out, no reply), and Request-Channel (Flux in, Flux out, bidirectional). In Spring you express each as an @MessageMapping method whose return and parameter types match that cardinality, and on the client the terminal operator (retrieveMono, retrieveFlux, send) selects the model. There is also metadata push, which sends metadata with no data and no response.","correct":"Request-Response (Mono in, Mono out), Request-Stream (Mono in, Flux out), Fire-and-Forget (Mono in, Mono<Void> out, no reply), and Request-Channel (Flux in, Flux out, bidirectional).","distractors":["Throwing or emitting onError in an @MessageMapping handler sends an ERROR frame to the requester, surfacing as an errored Mono/Flux on the client.","A Mono or Flux represents a description of a computation, not the execution of it.","RSocket serializes the subscriber's request(n) demand into REQUEST_N frames sent to the remote producer, so backpressure is application-level and per-logical-stream."],"distractorIds":["spring/spring_webflux#f73053efd636","spring/spring_webflux#c20711b17bb6","spring/spring_webflux#35948fb53bc2"],"concepts":["fire","void","forget","reply","signatures","rsocket"],"correctMd":"Request-Response (`Mono` in, `Mono` out), Request-Stream (`Mono` in, `Flux` out), Fire-and-Forget (`Mono` in, `Mono<Void>` out, no reply), and Request-Channel (`Flux` in, `Flux` out, bidirectional).","answerFullMd":"Request-Response (`Mono` in, `Mono` out), Request-Stream (`Mono` in, `Flux` out), Fire-and-Forget (`Mono` in, `Mono<Void>` out, no reply), and Request-Channel (`Flux` in, `Flux` out, bidirectional). In Spring you express each as an `@MessageMapping` method whose return and parameter types match that cardinality, and on the client the terminal operator (`retrieveMono`, `retrieveFlux`, `send`) selects the model. There is also metadata push, which sends metadata with no data and no response.","distractorsMd":["Throwing or emitting `onError` in an `@MessageMapping` handler sends an `ERROR` frame to the requester, surfacing as an errored `Mono`/`Flux` on the client.","A `Mono` or `Flux` represents a description of a computation, not the execution of it.","RSocket serializes the subscriber's `request(n)` demand into `REQUEST_N` frames sent to the remote producer, so backpressure is application-level and per-logical-stream."]},{"id":"spring/spring_webflux#f024dc7db7f3","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"rsocket_reactive_messaging.md","difficulty":"intermediate","question":"What is metadata push and when do you use it?","answerFull":"Metadata push sends a metadata-only frame with no data payload and no response, used for out-of-band signals like cache invalidation, a config change nudge, or a lease update. It bypasses normal routing to a business handler and is handled at the connection level. Use it for lightweight control-plane messages, not for carrying business data.","correct":"Metadata push sends a metadata-only frame with no data payload and no response, used for out-of-band signals like cache invalidation, a config change nudge, or a lease update.","distractors":["Request-Response (Mono in, Mono out), Request-Stream (Mono in, Flux out), Fire-and-Forget (Mono in, Mono<Void> out, no reply), and Request-Channel (Flux in, Flux out, bidirectional).","Mono<T> is a publisher that emits 0 or 1 item and then signals completion or error.","Leasing is connection-level admission control: a responder grants the requester a lease of N requests valid for T milliseconds, and the requester must not exceed it."],"distractorIds":["spring/spring_webflux#769fb85554ef","spring/spring_webflux#3b3020209ce5","spring/spring_webflux#0d189969bd8c"],"concepts":["nudge","band","lease","invalidation","frame","payload"],"distractorsMd":["Request-Response (`Mono` in, `Mono` out), Request-Stream (`Mono` in, `Flux` out), Fire-and-Forget (`Mono` in, `Mono<Void>` out, no reply), and Request-Channel (`Flux` in, `Flux` out, bidirectional).","`Mono<T>` is a publisher that emits 0 or 1 item and then signals completion or error.",null]},{"id":"spring/spring_webflux#09bd037e7030","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"rsocket_reactive_messaging.md","difficulty":"intermediate","question":"How does routing work in Spring RSocket if there are no URLs?","answerFull":"Routing metadata (MIME type message/x.rsocket.routing.v0) carries a route string that RSocketMessageHandler matches against @MessageMapping route patterns. On the client you set it with requester.route(\"prices.AAPL\"), and route templates like prices.{symbol} bind segments via @DestinationVariable, mirroring @PathVariable in MVC. A MetadataExtractor and PathPatternRouteMatcher (configured in RSocketStrategies) perform the extraction and matching.","correct":"Routing metadata (MIME type message/x.rsocket.routing.v0) carries a route string that RSocketMessageHandler matches against @MessageMapping route patterns.","distractors":["RSocket wins when you need application-level backpressure (REQUEST_N), symmetric bidirectional streaming where the server can initiate, transport flexibility (UDP/Aeron), or resumable sessions.","RSocket serializes the subscriber's request(n) demand into REQUEST_N frames sent to the remote producer, so backpressure is application-level and per-logical-stream.","transport: tcp with a port starts a standalone RSocket server on that port, independent of any web server."],"distractorIds":["spring/spring_webflux#f4762dd4b2b0","spring/spring_webflux#35948fb53bc2","spring/spring_webflux#9ec49bb90c8e"],"concepts":["mime","rsocketmessagehandler","messagemapping","rsocket","urls","carries"],"correctMd":"Routing metadata (MIME type `message/x.rsocket.routing.v0`) carries a route string that `RSocketMessageHandler` matches against `@MessageMapping` route patterns.","answerFullMd":"Routing metadata (MIME type `message/x.rsocket.routing.v0`) carries a route string that `RSocketMessageHandler` matches against `@MessageMapping` route patterns. On the client you set it with `requester.route(\"prices.AAPL\")`, and route templates like `prices.{symbol}` bind segments via `@DestinationVariable`, mirroring `@PathVariable` in MVC. A `MetadataExtractor` and `PathPatternRouteMatcher` (configured in `RSocketStrategies`) perform the extraction and matching.","distractorsMd":["RSocket wins when you need application-level backpressure (`REQUEST_N`), symmetric bidirectional streaming where the server can initiate, transport flexibility (UDP/Aeron), or resumable sessions.","RSocket serializes the subscriber's `request(n)` demand into `REQUEST_N` frames sent to the remote producer, so backpressure is application-level and per-logical-stream.","`transport: tcp` with a `port` starts a standalone RSocket server on that port, independent of any web server."]},{"id":"spring/spring_webflux#170db41e9c56","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"rsocket_reactive_messaging.md","difficulty":"intermediate","question":"What does RSocketRequester do and how do you create one in Boot 3.x?","answerFull":"RSocketRequester is the fluent client that opens a connection and issues requests in any of the four models. You inject the auto-configured RSocketRequester.Builder, configure the connector (keepalive, resume), set a setup route/data, and terminate with .tcp(host, port) or .websocket(uri). Then requester.route(\"...\").data(...).retrieveMono(T.class) (or retrieveFlux, or send()) performs the interaction.","correct":"RSocketRequester is the fluent client that opens a connection and issues requests in any of the four models.","distractors":["WebClient is the non-blocking HTTP client for reactive applications.","Leasing is connection-level admission control: a responder grants the requester a lease of N requests valid for T milliseconds, and the requester must not exceed it.","RSocketStrategies holds the encoders/decoders, route matcher, and metadata extractor for an RSocket connection — the RSocket analog of Spring MVC's HttpMessageConverter configuration."],"distractorIds":["spring/spring_webflux#843899fa735a","spring/spring_webflux#0d189969bd8c","spring/spring_webflux#104926918f72"],"concepts":["rsocketrequester","opens","fluent","boot","issues","four"],"questionMd":"What does `RSocketRequester` do and how do you create one in Boot 3.x?","correctMd":"`RSocketRequester` is the fluent client that opens a connection and issues requests in any of the four models.","answerFullMd":"`RSocketRequester` is the fluent client that opens a connection and issues requests in any of the four models. You inject the auto-configured `RSocketRequester.Builder`, configure the connector (keepalive, resume), set a setup route/data, and terminate with `.tcp(host, port)` or `.websocket(uri)`. Then `requester.route(\"...\").data(...).retrieveMono(T.class)` (or `retrieveFlux`, or `send()`) performs the interaction.","distractorsMd":["`WebClient` is the non-blocking HTTP client for reactive applications.",null,"`RSocketStrategies` holds the encoders/decoders, route matcher, and metadata extractor for an RSocket connection — the RSocket analog of Spring MVC's `HttpMessageConverter` configuration."]},{"id":"spring/spring_webflux#104926918f72","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"rsocket_reactive_messaging.md","difficulty":"intermediate","question":"What is RSocketStrategies responsible for?","answerFull":"RSocketStrategies holds the encoders/decoders, route matcher, and metadata extractor for an RSocket connection — the RSocket analog of Spring MVC's HttpMessageConverter configuration. It determines how payloads are serialized (CBOR, Protobuf, JSON), how routes are matched (PathPatternRouteMatcher), and how composite metadata is parsed. Both server (RSocketMessageHandler) and client (RSocketRequester.Builder) are configured with it.","correct":"RSocketStrategies holds the encoders/decoders, route matcher, and metadata extractor for an RSocket connection — the RSocket analog of Spring MVC's HttpMessageConverter configuration.","distractors":["Authentication can occur at SETUP (a token in the connection metadata authenticating the whole session) or per-route (a token in each request's composite metadata).","RSocket serializes the subscriber's request(n) demand into REQUEST_N frames sent to the remote producer, so backpressure is application-level and per-logical-stream.","RSocket runs over TCP, WebSocket, and UDP via Aeron."],"distractorIds":["spring/spring_webflux#38f4be115fae","spring/spring_webflux#35948fb53bc2","spring/spring_webflux#df9c4dcdca1f"],"concepts":["extractor","rsocketstrategies","decoders","analog","matcher","responsible"],"questionMd":"What is `RSocketStrategies` responsible for?","correctMd":"`RSocketStrategies` holds the encoders/decoders, route matcher, and metadata extractor for an RSocket connection — the RSocket analog of Spring MVC's `HttpMessageConverter` configuration.","answerFullMd":"`RSocketStrategies` holds the encoders/decoders, route matcher, and metadata extractor for an RSocket connection — the RSocket analog of Spring MVC's `HttpMessageConverter` configuration. It determines how payloads are serialized (CBOR, Protobuf, JSON), how routes are matched (`PathPatternRouteMatcher`), and how composite metadata is parsed. Both server (`RSocketMessageHandler`) and client (`RSocketRequester.Builder`) are configured with it.","distractorsMd":[null,"RSocket serializes the subscriber's `request(n)` demand into `REQUEST_N` frames sent to the remote producer, so backpressure is application-level and per-logical-stream.",null]},{"id":"spring/spring_webflux#c2694bfc2da0","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"rsocket_reactive_messaging.md","difficulty":"advanced","question":"How do keepalive and connection liveness work in RSocket?","answerFull":"Both peers exchange KEEPALIVE frames on a configured interval, and each expects an ack within a configured max-lifetime; if none arrives, the connection is declared dead. In Spring you set this via connector.keepAlive(interval, maxLifetime) — for example every 20s with a 90s lifetime. This detects half-open connections that TCP alone would leave hanging and, combined with resumption, drives reconnect.","correct":"Both peers exchange KEEPALIVE frames on a configured interval, and each expects an ack within a configured max-lifetime; if none arrives, the connection is declared dead.","distractors":["RSocket runs over TCP, WebSocket, and UDP via Aeron.","RSocket uses one long-lived, multiplexed connection, so all of its logical streams are pinned to whichever backend that connection landed on.","RSocket serializes the subscriber's request(n) demand into REQUEST_N frames sent to the remote producer, so backpressure is application-level and per-logical-stream."],"distractorIds":["spring/spring_webflux#df9c4dcdca1f","spring/spring_webflux#76755de407a2","spring/spring_webflux#35948fb53bc2"],"concepts":["keepalive","expects","peers","liveness","rsocket","ack"],"correctMd":"Both peers exchange `KEEPALIVE` frames on a configured interval, and each expects an ack within a configured max-lifetime; if none arrives, the connection is declared dead.","answerFullMd":"Both peers exchange `KEEPALIVE` frames on a configured interval, and each expects an ack within a configured max-lifetime; if none arrives, the connection is declared dead. In Spring you set this via `connector.keepAlive(interval, maxLifetime)` — for example every 20s with a 90s lifetime. This detects half-open connections that TCP alone would leave hanging and, combined with resumption, drives reconnect.","distractorsMd":[null,null,"RSocket serializes the subscriber's `request(n)` demand into `REQUEST_N` frames sent to the remote producer, so backpressure is application-level and per-logical-stream."]},{"id":"spring/spring_webflux#38f4be115fae","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"rsocket_reactive_messaging.md","difficulty":"advanced","question":"Where can JWT authentication happen in RSocket, and how does Spring enforce it?","answerFull":"Authentication can occur at SETUP (a token in the connection metadata authenticating the whole session) or per-route (a token in each request's composite metadata). Spring Security's @EnableRSocketSecurity with RSocketSecurity.authorizePayload(...) and .jwt(...) installs a PayloadSocketAcceptorInterceptor that authorizes routes (e.g. route(\"admin.*\").hasRole(\"ADMIN\")), and @PreAuthorize on @MessageMapping methods adds method-level authz. SETUP suits stable session identity; per-route suits fine-grained, per-message authorization.","correct":"Authentication can occur at SETUP (a token in the connection metadata authenticating the whole session) or per-route (a token in each request's composite metadata).","distractors":["transport: tcp with a port starts a standalone RSocket server on that port, independent of any web server.","Routing metadata (MIME type message/x.rsocket.routing.v0) carries a route string that RSocketMessageHandler matches against @MessageMapping route patterns.","The handler runs on a Reactor event-loop thread, and blocking it stalls every multiplexed stream on that connection, not just the one request."],"distractorIds":["spring/spring_webflux#9ec49bb90c8e","spring/spring_webflux#09bd037e7030","spring/spring_webflux#024f55c2b35d"],"concepts":["authenticating","composite","happen","rsocket","enforce","setup"],"answerFullMd":"Authentication can occur at SETUP (a token in the connection metadata authenticating the whole session) or per-route (a token in each request's composite metadata). Spring Security's `@EnableRSocketSecurity` with `RSocketSecurity.authorizePayload(...)` and `.jwt(...)` installs a `PayloadSocketAcceptorInterceptor` that authorizes routes (e.g. `route(\"admin.*\").hasRole(\"ADMIN\")`), and `@PreAuthorize` on `@MessageMapping` methods adds method-level authz. SETUP suits stable session identity; per-route suits fine-grained, per-message authorization.","distractorsMd":["`transport: tcp` with a `port` starts a standalone RSocket server on that port, independent of any web server.","Routing metadata (MIME type `message/x.rsocket.routing.v0`) carries a route string that `RSocketMessageHandler` matches against `@MessageMapping` route patterns.",null]},{"id":"spring/spring_webflux#df9c4dcdca1f","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"rsocket_reactive_messaging.md","difficulty":"advanced","question":"What transports does RSocket support and when would you choose each?","answerFull":"RSocket runs over TCP, WebSocket, and UDP via Aeron. TCP is the default and lowest-overhead for service-to-service; WebSocket traverses HTTP infrastructure and reaches browsers; Aeron suits very high-throughput, low-latency, loss-tolerant flows like telemetry. In Spring you select the server transport with spring.rsocket.server.transport=tcp|websocket; WebSocket additionally requires spring-boot-starter-webflux because it rides inside the reactive web server. Choose TCP for internal meshes, WebSocket for browser/proxy reach, Aeron for extreme throughput.","correct":"RSocket runs over TCP, WebSocket, and UDP via Aeron.","distractors":["RSocketStrategies holds the encoders/decoders, route matcher, and metadata extractor for an RSocket connection — the RSocket analog of Spring MVC's HttpMessageConverter configuration.","RSocket serializes the subscriber's request(n) demand into REQUEST_N frames sent to the remote producer, so backpressure is application-level and per-logical-stream.","RSocket uses one long-lived, multiplexed connection, so all of its logical streams are pinned to whichever backend that connection landed on."],"distractorIds":["spring/spring_webflux#104926918f72","spring/spring_webflux#35948fb53bc2","spring/spring_webflux#76755de407a2"],"concepts":["aeron","transports","rsocket","udp","websocket","tcp"],"answerFullMd":"RSocket runs over TCP, WebSocket, and UDP via Aeron. TCP is the default and lowest-overhead for service-to-service; WebSocket traverses HTTP infrastructure and reaches browsers; Aeron suits very high-throughput, low-latency, loss-tolerant flows like telemetry. In Spring you select the server transport with `spring.rsocket.server.transport=tcp|websocket`; WebSocket additionally requires `spring-boot-starter-webflux` because it rides inside the reactive web server. Choose TCP for internal meshes, WebSocket for browser/proxy reach, Aeron for extreme throughput.","distractorsMd":["`RSocketStrategies` holds the encoders/decoders, route matcher, and metadata extractor for an RSocket connection — the RSocket analog of Spring MVC's `HttpMessageConverter` configuration.","RSocket serializes the subscriber's `request(n)` demand into `REQUEST_N` frames sent to the remote producer, so backpressure is application-level and per-logical-stream.",null]},{"id":"spring/spring_webflux#f4762dd4b2b0","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"rsocket_reactive_messaging.md","difficulty":"advanced","question":"When does RSocket win over gRPC, and when does gRPC win?","answerFull":"RSocket wins when you need application-level backpressure (REQUEST_N), symmetric bidirectional streaming where the server can initiate, transport flexibility (UDP/Aeron), or resumable sessions. gRPC wins for polyglot RPC with strict Protobuf contracts and its mature ecosystem, where HTTP/2 flow control suffices and you want broad language and tooling support. gRPC is HTTP/2-only and its \"backpressure\" is a transport byte window, not per-message demand.","correct":"RSocket wins when you need application-level backpressure (REQUEST_N), symmetric bidirectional streaming where the server can initiate, transport flexibility (UDP/Aeron), or resumable sessions.","distractors":["RSocket runs over TCP, WebSocket, and UDP via Aeron.","Request-Response (Mono in, Mono out), Request-Stream (Mono in, Flux out), Fire-and-Forget (Mono in, Mono<Void> out, no reply), and Request-Channel (Flux in, Flux out, bidirectional).","RSocketStrategies holds the encoders/decoders, route matcher, and metadata extractor for an RSocket connection — the RSocket analog of Spring MVC's HttpMessageConverter configuration."],"distractorIds":["spring/spring_webflux#df9c4dcdca1f","spring/spring_webflux#769fb85554ef","spring/spring_webflux#104926918f72"],"concepts":["resumable","aeron","initiate","flexibility","wins","backpressure"],"correctMd":"RSocket wins when you need application-level backpressure (`REQUEST_N`), symmetric bidirectional streaming where the server can initiate, transport flexibility (UDP/Aeron), or resumable sessions.","answerFullMd":"RSocket wins when you need application-level backpressure (`REQUEST_N`), symmetric bidirectional streaming where the server can initiate, transport flexibility (UDP/Aeron), or resumable sessions. gRPC wins for polyglot RPC with strict Protobuf contracts and its mature ecosystem, where HTTP/2 flow control suffices and you want broad language and tooling support. gRPC is HTTP/2-only and its \"backpressure\" is a transport byte window, not per-message demand.","distractorsMd":[null,"Request-Response (`Mono` in, `Mono` out), Request-Stream (`Mono` in, `Flux` out), Fire-and-Forget (`Mono` in, `Mono<Void>` out, no reply), and Request-Channel (`Flux` in, `Flux` out, bidirectional).","`RSocketStrategies` holds the encoders/decoders, route matcher, and metadata extractor for an RSocket connection — the RSocket analog of Spring MVC's `HttpMessageConverter` configuration."]},{"id":"spring/spring_webflux#9ec49bb90c8e","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"rsocket_reactive_messaging.md","difficulty":"advanced","question":"How does the spring.rsocket.server.* config change what server starts?","answerFull":"transport: tcp with a port starts a standalone RSocket server on that port, independent of any web server. transport: websocket with a mapping-path embeds RSocket inside the existing WebFlux web server on the HTTP port, so browsers and HTTP-aware proxies can reach it. Choosing websocket without spring-boot-starter-webflux on the classpath fails to start because there is no reactive web server to host the mapping path.","correct":"transport: tcp with a port starts a standalone RSocket server on that port, independent of any web server.","distractors":["Routing metadata (MIME type message/x.rsocket.routing.v0) carries a route string that RSocketMessageHandler matches against @MessageMapping route patterns.","RSocket wins when you need application-level backpressure (REQUEST_N), symmetric bidirectional streaming where the server can initiate, transport flexibility (UDP/Aeron), or resumable sessions.","RSocket uses one long-lived, multiplexed connection, so all of its logical streams are pinned to whichever backend that connection landed on."],"distractorIds":["spring/spring_webflux#09bd037e7030","spring/spring_webflux#f4762dd4b2b0","spring/spring_webflux#76755de407a2"],"concepts":["standalone","rsocket","transport","port","tcp","web"],"questionMd":"How does the `spring.rsocket.server.*` config change what server starts?","correctMd":"`transport: tcp` with a `port` starts a standalone RSocket server on that port, independent of any web server.","answerFullMd":"`transport: tcp` with a `port` starts a standalone RSocket server on that port, independent of any web server. `transport: websocket` with a `mapping-path` embeds RSocket inside the existing WebFlux web server on the HTTP port, so browsers and HTTP-aware proxies can reach it. Choosing websocket without `spring-boot-starter-webflux` on the classpath fails to start because there is no reactive web server to host the mapping path.","distractorsMd":["Routing metadata (MIME type `message/x.rsocket.routing.v0`) carries a route string that `RSocketMessageHandler` matches against `@MessageMapping` route patterns.","RSocket wins when you need application-level backpressure (`REQUEST_N`), symmetric bidirectional streaming where the server can initiate, transport flexibility (UDP/Aeron), or resumable sessions.",null]},{"id":"spring/spring_webflux#0d189969bd8c","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"rsocket_reactive_messaging.md","difficulty":"advanced","question":"What is leasing in RSocket and how does it differ from per-stream backpressure?","answerFull":"Leasing is connection-level admission control: a responder grants the requester a lease of N requests valid for T milliseconds, and the requester must not exceed it. It differs from REQUEST_N backpressure, which paces items *within* an already-accepted stream — leasing instead limits *how many requests* may be started at all, letting an overloaded responder shed load before work begins. It is opt-in and configured on the connector.","correct":"Leasing is connection-level admission control: a responder grants the requester a lease of N requests valid for T milliseconds, and the requester must not exceed it.","distractors":["RSocketStrategies holds the encoders/decoders, route matcher, and metadata extractor for an RSocket connection — the RSocket analog of Spring MVC's HttpMessageConverter configuration.","RSocket serializes the subscriber's request(n) demand into REQUEST_N frames sent to the remote producer, so backpressure is application-level and per-logical-stream.","RSocket uses one long-lived, multiplexed connection, so all of its logical streams are pinned to whichever backend that connection landed on."],"distractorIds":["spring/spring_webflux#104926918f72","spring/spring_webflux#35948fb53bc2","spring/spring_webflux#76755de407a2"],"concepts":["leasing","requester","responder","grants","lease","admission"],"answerFullMd":"Leasing is connection-level admission control: a responder grants the requester a lease of N requests valid for T milliseconds, and the requester must not exceed it. It differs from `REQUEST_N` backpressure, which paces items *within* an already-accepted stream — leasing instead limits *how many requests* may be started at all, letting an overloaded responder shed load before work begins. It is opt-in and configured on the connector.","distractorsMd":["`RSocketStrategies` holds the encoders/decoders, route matcher, and metadata extractor for an RSocket connection — the RSocket analog of Spring MVC's `HttpMessageConverter` configuration.","RSocket serializes the subscriber's `request(n)` demand into `REQUEST_N` frames sent to the remote producer, so backpressure is application-level and per-logical-stream.",null]},{"id":"spring/spring_webflux#98ab53f03a9a","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"rsocket_reactive_messaging.md","difficulty":"advanced","question":"How does request-channel handle backpressure in both directions?","answerFull":"Request-channel is fully symmetric: the outbound Flux (requester → responder) and the inbound Flux (responder → requester) each carry independent REQUEST_N demand, so each side paces the other. A slow responder throttles the requester's uploads while a slow requester throttles the responder's downloads, all over the one stream. This makes it correct for bidirectional sync or collaborative editing where neither side should overwhelm the other.","correct":"Request-channel is fully symmetric: the outbound Flux (requester → responder) and the inbound Flux (responder → requester) each carry independent REQUEST_N demand, so each side paces the other.","distractors":["Reactor provides five overflow strategies for backpressure: limitRate, buffered (bounded or unbounded), onBackpressureDrop, and onBackpressureLatest.","Throwing or emitting onError in an @MessageMapping handler sends an ERROR frame to the requester, surfacing as an errored Mono/Flux on the client.","RSocket serializes the subscriber's request(n) demand into REQUEST_N frames sent to the remote producer, so backpressure is application-level and per-logical-stream."],"distractorIds":["spring/spring_webflux#9dab77bab1ab","spring/spring_webflux#f73053efd636","spring/spring_webflux#35948fb53bc2"],"concepts":["paces","requester","responder","directions","backpressure","symmetric"],"correctMd":"Request-channel is fully symmetric: the outbound `Flux` (requester → responder) and the inbound `Flux` (responder → requester) each carry independent `REQUEST_N` demand, so each side paces the other.","answerFullMd":"Request-channel is fully symmetric: the outbound `Flux` (requester → responder) and the inbound `Flux` (responder → requester) each carry independent `REQUEST_N` demand, so each side paces the other. A slow responder throttles the requester's uploads while a slow requester throttles the responder's downloads, all over the one stream. This makes it correct for bidirectional sync or collaborative editing where neither side should overwhelm the other.","distractorsMd":["Reactor provides five overflow strategies for backpressure: `limitRate`, buffered (bounded or unbounded), `onBackpressureDrop`, and `onBackpressureLatest`.","Throwing or emitting `onError` in an `@MessageMapping` handler sends an `ERROR` frame to the requester, surfacing as an errored `Mono`/`Flux` on the client.","RSocket serializes the subscriber's `request(n)` demand into `REQUEST_N` frames sent to the remote producer, so backpressure is application-level and per-logical-stream."]},{"id":"spring/spring_webflux#f73053efd636","section":"spring","module":"spring/spring_webflux","moduleName":"spring webflux","sourceFile":"rsocket_reactive_messaging.md","difficulty":"advanced","question":"How do you handle errors in an RSocket handler and propagate them to the client?","answerFull":"Throwing or emitting onError in an @MessageMapping handler sends an ERROR frame to the requester, surfacing as an errored Mono/Flux on the client. Use @MessageExceptionHandler methods in the controller (or a @ControllerAdvice-style bean) to map specific exceptions to a structured error payload, and use Reactor operators (onErrorResume, onErrorMap, timeout) in the pipeline for fallbacks. Always set .timeout(...) on client request-response calls, since a live connection will not make a hung responder fail fast on its own.","correct":"Throwing or emitting onError in an @MessageMapping handler sends an ERROR frame to the requester, surfacing as an errored Mono/Flux on the client.","distractors":["Request-Response (Mono in, Mono out), Request-Stream (Mono in, Flux out), Fire-and-Forget (Mono in, Mono<Void> out, no reply), and Request-Channel (Flux in, Flux out, bidirectional).","Metadata push sends a metadata-only frame with no data payload and no response, used for out-of-band signals like cache invalidation, a config change nudge, or a lease update.","A Mono or Flux represents a description of a computation, not the execution of it."],"distractorIds":["spring/spring_webflux#769fb85554ef","spring/spring_webflux#f024dc7db7f3","spring/spring_webflux#c20711b17bb6"],"concepts":["errored","messagemapping","onerror","surfacing","requester","emitting"],"correctMd":"Throwing or emitting `onError` in an `@MessageMapping` handler sends an `ERROR` frame to the requester, surfacing as an errored `Mono`/`Flux` on the client.","answerFullMd":"Throwing or emitting `onError` in an `@MessageMapping` handler sends an `ERROR` frame to the requester, surfacing as an errored `Mono`/`Flux` on the client. Use `@MessageExceptionHandler` methods in the controller (or a `@ControllerAdvice`-style bean) to map specific exceptions to a structured error payload, and use Reactor operators (`onErrorResume`, `onErrorMap`, `timeout`) in the pipeline for fallbacks. Always set `.timeout(...)` on client request-response calls, since a live connection will not make a hung responder fail fast on its own.","distractorsMd":["Request-Response (`Mono` in, `Mono` out), Request-Stream (`Mono` in, `Flux` out), Fire-and-Forget (`Mono` in, `Mono<Void>` out, no reply), and Request-Channel (`Flux` in, `Flux` out, bidirectional).",null,"A `Mono` or `Flux` represents a description of a computation, not the execution of it."]},{"id":"spring/validation_and_error_handling#de1f88fb84b7","section":"spring","module":"spring/validation_and_error_handling","moduleName":"validation and error handling","sourceFile":"README.md","difficulty":"core","question":"What is the difference between @Valid and @Validated in Spring?","answerFull":"@Valid is the standard JSR-380 annotation (from jakarta.validation). It triggers constraint validation on the annotated parameter, including cascading to nested @Valid fields. It does not support validation groups. @Validated is Spring's extension (org.springframework.validation.annotation.Validated). It adds group selection — @Validated(OnCreate.class) activates only constraints in that group. At the class level, @Validated marks a bean for method-level validation via MethodValidationPostProcessor — enabling @NotNull, @Size, etc. on service and repository method parameters and return values. In Spring MVC controllers, both are interchangeable for non-group cases; only @Validated supports groups.","correct":"@Valid is the standard JSR-380 annotation (from jakarta.validation).","distractors":["Request params and path variables are primitive or String types, not POJOs — @Valid cascade does not apply.","@AssertTrue(message = \"End date must be after start date\") on a method isEndAfterStart() that returns boolean provides lightweight cross-field validation without a custom ConstraintValidator.","Create an annotation (e.g., @UniqueEmail) with @Constraint(validatedBy = UniqueEmailValidator.class)."],"distractorIds":["spring/validation_and_error_handling#e8646fe2481c","spring/validation_and_error_handling#ab72f9f554d7","spring/validation_and_error_handling#a67738c91789"],"concepts":["380","jakarta","jsr","difference","validated","annotation"],"questionMd":"What is the difference between `@Valid` and `@Validated` in Spring?","correctMd":"`@Valid` is the standard JSR-380 annotation (from `jakarta.validation`).","answerFullMd":"`@Valid` is the standard JSR-380 annotation (from `jakarta.validation`). It triggers constraint validation on the annotated parameter, including cascading to nested `@Valid` fields. It does not support validation groups. `@Validated` is Spring's extension (`org.springframework.validation.annotation.Validated`). It adds group selection — `@Validated(OnCreate.class)` activates only constraints in that group. At the class level, `@Validated` marks a bean for method-level validation via `MethodValidationPostProcessor` — enabling `@NotNull`, `@Size`, etc. on service and repository method parameters and return values. In Spring MVC controllers, both are interchangeable for non-group cases; only `@Validated` supports groups.","distractorsMd":["Request params and path variables are primitive or String types, not POJOs — `@Valid` cascade does not apply.","`@AssertTrue(message = \"End date must be after start date\")` on a method `isEndAfterStart()` that returns `boolean` provides lightweight cross-field validation without a custom `ConstraintValidator`.","Create an annotation (e.g., `@UniqueEmail`) with `@Constraint(validatedBy = UniqueEmailValidator.class)`."]},{"id":"spring/validation_and_error_handling#2ab81ff92caa","section":"spring","module":"spring/validation_and_error_handling","moduleName":"validation and error handling","sourceFile":"README.md","difficulty":"core","question":"What exception is thrown for MVC validation failure vs service-layer validation failure, and how do you handle both?","answerFull":"When @Valid / @Validated fails on a @Controller @RequestBody parameter, Spring MVC throws MethodArgumentNotValidException (which extends BindException). When @Validated + MethodValidationPostProcessor fails on a service method parameter, Spring throws ConstraintViolationException (from jakarta.validation). Handle both in a @ControllerAdvice: handleMethodArgumentNotValid() via ResponseEntityExceptionHandler override for MVC-level errors, and a separate @ExceptionHandler(ConstraintViolationException.class) method for service-level errors. Map both to HTTP 400 with ProblemDetail bodies listing all violations.","correct":"When @Valid / @Validated fails on a @Controller @RequestBody parameter, Spring MVC throws MethodArgumentNotValidException (which extends BindException).","distractors":["If the @ExceptionHandler method itself throws an exception, Spring falls back to the DefaultHandlerExceptionResolver, which typically returns a 500 Internal Server Error with a minimal response body.","Validation groups are marker interfaces (empty, by convention).","@Valid is the standard JSR-380 annotation (from jakarta.validation)."],"distractorIds":["spring/validation_and_error_handling#4b529c502611","spring/validation_and_error_handling#f7d527e46c12","spring/validation_and_error_handling#de1f88fb84b7"],"concepts":["bindexception","methodargumentnotvalidexception","requestbody","thrown","mvc","validated"],"correctMd":"When `@Valid` / `@Validated` fails on a `@Controller` `@RequestBody` parameter, Spring MVC throws `MethodArgumentNotValidException` (which extends `BindException`).","answerFullMd":"When `@Valid` / `@Validated` fails on a `@Controller` `@RequestBody` parameter, Spring MVC throws `MethodArgumentNotValidException` (which extends `BindException`). When `@Validated` + `MethodValidationPostProcessor` fails on a service method parameter, Spring throws `ConstraintViolationException` (from `jakarta.validation`). Handle both in a `@ControllerAdvice`: `handleMethodArgumentNotValid()` via `ResponseEntityExceptionHandler` override for MVC-level errors, and a separate `@ExceptionHandler(ConstraintViolationException.class)` method for service-level errors. Map both to HTTP 400 with `ProblemDetail` bodies listing all violations.","distractorsMd":["If the `@ExceptionHandler` method itself throws an exception, Spring falls back to the `DefaultHandlerExceptionResolver`, which typically returns a 500 Internal Server Error with a minimal response body.",null,"`@Valid` is the standard JSR-380 annotation (from `jakarta.validation`)."]},{"id":"spring/validation_and_error_handling#f7d527e46c12","section":"spring","module":"spring/validation_and_error_handling","moduleName":"validation and error handling","sourceFile":"README.md","difficulty":"core","question":"How do validation groups work, and what is a common use case?","answerFull":"Validation groups are marker interfaces (empty, by convention). Each constraint annotation has a groups attribute — e.g., @NotNull(groups = OnUpdate.class). Using @Validated(OnUpdate.class) activates only constraints whose groups include OnUpdate.class. The default group is Default.class. Use case: a ProductRequest record used for both POST /products (create) and PUT /products/{id} (update). For create: id must be @Null (not provided by client). For update: id must be @NotNull (must reference existing product). Without groups, you'd need two different request classes or manual if checks.","correct":"Validation groups are marker interfaces (empty, by convention).","distractors":["@Valid is the standard JSR-380 annotation (from jakarta.validation).","@AssertTrue(message = \"End date must be after start date\") on a method isEndAfterStart() that returns boolean provides lightweight cross-field validation without a custom ConstraintValidator.","Bean Validation uses ValidationMessages.properties in the classpath root for constraint message templates (e.g., {jakarta.validation.constraints.NotBlank.message})."],"distractorIds":["spring/validation_and_error_handling#de1f88fb84b7","spring/validation_and_error_handling#ab72f9f554d7","spring/validation_and_error_handling#88e6a6cc33e1"],"concepts":["convention","marker","interfaces","empty","groups","common"],"answerFullMd":"Validation groups are marker interfaces (empty, by convention). Each constraint annotation has a `groups` attribute — e.g., `@NotNull(groups = OnUpdate.class)`. Using `@Validated(OnUpdate.class)` activates only constraints whose `groups` include `OnUpdate.class`. The default group is `Default.class`. Use case: a `ProductRequest` record used for both `POST /products` (create) and `PUT /products/{id}` (update). For create: `id` must be `@Null` (not provided by client). For update: `id` must be `@NotNull` (must reference existing product). Without groups, you'd need two different request classes or manual `if` checks.","distractorsMd":["`@Valid` is the standard JSR-380 annotation (from `jakarta.validation`).","`@AssertTrue(message = \"End date must be after start date\")` on a method `isEndAfterStart()` that returns `boolean` provides lightweight cross-field validation without a custom `ConstraintValidator`.","Bean Validation uses `ValidationMessages.properties` in the classpath root for constraint message templates (e.g., `{jakarta.validation.constraints.NotBlank.message}`)."]},{"id":"spring/validation_and_error_handling#a67738c91789","section":"spring","module":"spring/validation_and_error_handling","moduleName":"validation and error handling","sourceFile":"README.md","difficulty":"core","question":"How would you write a custom ConstraintValidator that checks a DB-level uniqueness constraint?","answerFull":"Create an annotation (e.g., @UniqueEmail) with @Constraint(validatedBy = UniqueEmailValidator.class). The validator class implements ConstraintValidator<UniqueEmail, String>. Because the validator is managed by Spring (via @Component + Hibernate Validator's Spring integration), @Autowired works inside it. The isValid() method calls userRepository.existsByEmail(email) — if the email exists, return false (violation). Important: return true for null values (let @NotNull handle null — compose constraints rather than duplicating logic). Gotcha: the validator runs inside a request transaction if invoked via Spring MVC, but may need @Transactional(propagation = SUPPORTS) if called from a non-transactional context.","correct":"Create an annotation (e.g., @UniqueEmail) with @Constraint(validatedBy = UniqueEmailValidator.class).","distractors":["@Valid is the standard JSR-380 annotation (from jakarta.validation).","Bean Validation uses ValidationMessages.properties in the classpath root for constraint message templates (e.g., {jakarta.validation.constraints.NotBlank.message}).","@AssertTrue(message = \"End date must be after start date\") on a method isEndAfterStart() that returns boolean provides lightweight cross-field validation without a custom ConstraintValidator."],"distractorIds":["spring/validation_and_error_handling#de1f88fb84b7","spring/validation_and_error_handling#88e6a6cc33e1","spring/validation_and_error_handling#ab72f9f554d7"],"concepts":["constraintvalidator","uniqueemail","uniqueemailvalidator","uniqueness","validatedby","annotation"],"questionMd":"How would you write a custom `ConstraintValidator` that checks a DB-level uniqueness constraint?","correctMd":"Create an annotation (e.g., `@UniqueEmail`) with `@Constraint(validatedBy = UniqueEmailValidator.class)`.","answerFullMd":"Create an annotation (e.g., `@UniqueEmail`) with `@Constraint(validatedBy = UniqueEmailValidator.class)`. The validator class implements `ConstraintValidator<UniqueEmail, String>`. Because the validator is managed by Spring (via `@Component` + Hibernate Validator's Spring integration), `@Autowired` works inside it. The `isValid()` method calls `userRepository.existsByEmail(email)` — if the email exists, return `false` (violation). Important: return `true` for null values (let `@NotNull` handle null — compose constraints rather than duplicating logic). Gotcha: the validator runs inside a request transaction if invoked via Spring MVC, but may need `@Transactional(propagation = SUPPORTS)` if called from a non-transactional context.","distractorsMd":["`@Valid` is the standard JSR-380 annotation (from `jakarta.validation`).","Bean Validation uses `ValidationMessages.properties` in the classpath root for constraint message templates (e.g., `{jakarta.validation.constraints.NotBlank.message}`).","`@AssertTrue(message = \"End date must be after start date\")` on a method `isEndAfterStart()` that returns `boolean` provides lightweight cross-field validation without a custom `ConstraintValidator`."]},{"id":"spring/validation_and_error_handling#bf8f9b696e60","section":"spring","module":"spring/validation_and_error_handling","moduleName":"validation and error handling","sourceFile":"README.md","difficulty":"intermediate","question":"How does @ControllerAdvice determine which controllers it applies to?","answerFull":"By default, @ControllerAdvice applies to all @Controller and @RestController beans in the application context (within the component-scan path). It can be scoped: @ControllerAdvice(basePackages = \"com.example.api\") — only controllers in that package; @ControllerAdvice(assignableTypes = {OrderController.class}) — only that controller; @ControllerAdvice(annotations = RestController.class) — only REST controllers. Multiple @ControllerAdvice beans are ordered by @Order — lower order value = higher precedence. ResponseEntityExceptionHandler is typically extended as one @ControllerAdvice; additional specific handlers can be added with higher order priority.","correct":"By default, @ControllerAdvice applies to all @Controller and @RestController beans in the application context (within the component-scan path).","distractors":["When @Valid / @Validated fails on a @Controller @RequestBody parameter, Spring MVC throws MethodArgumentNotValidException (which extends BindException).","Request params and path variables are primitive or String types, not POJOs — @Valid cascade does not apply.","@AssertTrue(message = \"End date must be after start date\") on a method isEndAfterStart() that returns boolean provides lightweight cross-field validation without a custom ConstraintValidator."],"distractorIds":["spring/validation_and_error_handling#2ab81ff92caa","spring/validation_and_error_handling#e8646fe2481c","spring/validation_and_error_handling#ab72f9f554d7"],"concepts":["controlleradvice","restcontroller","controllers","determine","beans","scan"],"questionMd":"How does `@ControllerAdvice` determine which controllers it applies to?","correctMd":"By default, `@ControllerAdvice` applies to all `@Controller` and `@RestController` beans in the application context (within the component-scan path).","answerFullMd":"By default, `@ControllerAdvice` applies to all `@Controller` and `@RestController` beans in the application context (within the component-scan path). It can be scoped: `@ControllerAdvice(basePackages = \"com.example.api\")` — only controllers in that package; `@ControllerAdvice(assignableTypes = {OrderController.class})` — only that controller; `@ControllerAdvice(annotations = RestController.class)` — only REST controllers. Multiple `@ControllerAdvice` beans are ordered by `@Order` — lower order value = higher precedence. `ResponseEntityExceptionHandler` is typically extended as one `@ControllerAdvice`; additional specific handlers can be added with higher order priority.","distractorsMd":["When `@Valid` / `@Validated` fails on a `@Controller` `@RequestBody` parameter, Spring MVC throws `MethodArgumentNotValidException` (which extends `BindException`).","Request params and path variables are primitive or String types, not POJOs — `@Valid` cascade does not apply.","`@AssertTrue(message = \"End date must be after start date\")` on a method `isEndAfterStart()` that returns `boolean` provides lightweight cross-field validation without a custom `ConstraintValidator`."]},{"id":"spring/validation_and_error_handling#4b529c502611","section":"spring","module":"spring/validation_and_error_handling","moduleName":"validation and error handling","sourceFile":"README.md","difficulty":"intermediate","question":"What happens if an exception is thrown inside a @ControllerAdvice @ExceptionHandler method?","answerFull":"If the @ExceptionHandler method itself throws an exception, Spring falls back to the DefaultHandlerExceptionResolver, which typically returns a 500 Internal Server Error with a minimal response body. The exception from the handler is propagated to the container (Tomcat/Jetty). To prevent this: wrap @ExceptionHandler bodies in try/catch; return a safe default ProblemDetail with status 500 if the handler itself encounters an unexpected error. This is particularly important for database calls inside exception handlers (e.g., UniqueEmailValidator calling the DB in response to a validation error).","correct":"If the @ExceptionHandler method itself throws an exception, Spring falls back to the DefaultHandlerExceptionResolver, which typically returns a 500 Internal Server Error with a minimal response body.","distractors":["When an exception escapes all HandlerExceptionResolver instances (or is thrown in a filter before the servlet dispatches), Tomcat/Jetty forward the request to /error.","By default, @ControllerAdvice applies to all @Controller and @RestController beans in the application context (within the component-scan path).","When @Valid / @Validated fails on a @Controller @RequestBody parameter, Spring MVC throws MethodArgumentNotValidException (which extends BindException)."],"distractorIds":["spring/validation_and_error_handling#eecb5403b4e3","spring/validation_and_error_handling#bf8f9b696e60","spring/validation_and_error_handling#2ab81ff92caa"],"concepts":["defaulthandlerexceptionresolver","controlleradvice","exceptionhandler","thrown","falls","minimal"],"questionMd":"What happens if an exception is thrown inside a `@ControllerAdvice @ExceptionHandler` method?","correctMd":"If the `@ExceptionHandler` method itself throws an exception, Spring falls back to the `DefaultHandlerExceptionResolver`, which typically returns a 500 Internal Server Error with a minimal response body.","answerFullMd":"If the `@ExceptionHandler` method itself throws an exception, Spring falls back to the `DefaultHandlerExceptionResolver`, which typically returns a 500 Internal Server Error with a minimal response body. The exception from the handler is propagated to the container (Tomcat/Jetty). To prevent this: wrap `@ExceptionHandler` bodies in try/catch; return a safe default `ProblemDetail` with status 500 if the handler itself encounters an unexpected error. This is particularly important for database calls inside exception handlers (e.g., `UniqueEmailValidator` calling the DB in response to a validation error).","distractorsMd":["When an exception escapes all `HandlerExceptionResolver` instances (or is thrown in a filter before the servlet dispatches), Tomcat/Jetty forward the request to `/error`.","By default, `@ControllerAdvice` applies to all `@Controller` and `@RestController` beans in the application context (within the component-scan path).","When `@Valid` / `@Validated` fails on a `@Controller` `@RequestBody` parameter, Spring MVC throws `MethodArgumentNotValidException` (which extends `BindException`)."]},{"id":"spring/validation_and_error_handling#e8646fe2481c","section":"spring","module":"spring/validation_and_error_handling","moduleName":"validation and error handling","sourceFile":"README.md","difficulty":"intermediate","question":"How would you validate a @RequestParam or @PathVariable (not a request body)?","answerFull":"Request params and path variables are primitive or String types, not POJOs — @Valid cascade does not apply. Two approaches: (1) @Validated at the class level + MethodValidationPostProcessor — put constraints directly on the method parameters: public ResponseEntity<User> getUser(@PathVariable @Positive long id). When id <= 0, Spring throws ConstraintViolationException (map to 400 in @ControllerAdvice). (2) Custom @ExceptionHandler(MethodArgumentTypeMismatchException.class) for type conversion failures (e.g., \"abc\" as a long path variable). Spring Boot 3 with problemdetails.enabled=true handles type mismatch automatically.","correct":"Request params and path variables are primitive or String types, not POJOs — @Valid cascade does not apply.","distractors":["By default, @ControllerAdvice applies to all @Controller and @RestController beans in the application context (within the component-scan path).","When an exception escapes all HandlerExceptionResolver instances (or is thrown in a filter before the servlet dispatches), Tomcat/Jetty forward the request to /error.","When @Valid / @Validated fails on a @Controller @RequestBody parameter, Spring MVC throws MethodArgumentNotValidException (which extends BindException)."],"distractorIds":["spring/validation_and_error_handling#bf8f9b696e60","spring/validation_and_error_handling#eecb5403b4e3","spring/validation_and_error_handling#2ab81ff92caa"],"concepts":["pojos","pathvariable","primitive","cascade","params","validate"],"questionMd":"How would you validate a `@RequestParam` or `@PathVariable` (not a request body)?","correctMd":"Request params and path variables are primitive or String types, not POJOs — `@Valid` cascade does not apply.","answerFullMd":"Request params and path variables are primitive or String types, not POJOs — `@Valid` cascade does not apply. Two approaches: (1) `@Validated` at the class level + `MethodValidationPostProcessor` — put constraints directly on the method parameters: `public ResponseEntity<User> getUser(@PathVariable @Positive long id)`. When `id <= 0`, Spring throws `ConstraintViolationException` (map to 400 in `@ControllerAdvice`). (2) Custom `@ExceptionHandler(MethodArgumentTypeMismatchException.class)` for type conversion failures (e.g., `\"abc\"` as a `long` path variable). Spring Boot 3 with `problemdetails.enabled=true` handles type mismatch automatically.","distractorsMd":["By default, `@ControllerAdvice` applies to all `@Controller` and `@RestController` beans in the application context (within the component-scan path).","When an exception escapes all `HandlerExceptionResolver` instances (or is thrown in a filter before the servlet dispatches), Tomcat/Jetty forward the request to `/error`.","When `@Valid` / `@Validated` fails on a `@Controller` `@RequestBody` parameter, Spring MVC throws `MethodArgumentNotValidException` (which extends `BindException`)."]},{"id":"spring/validation_and_error_handling#eecb5403b4e3","section":"spring","module":"spring/validation_and_error_handling","moduleName":"validation and error handling","sourceFile":"README.md","difficulty":"advanced","question":"How does Spring Boot's /error fallback endpoint work and how do you customise it?","answerFull":"When an exception escapes all HandlerExceptionResolver instances (or is thrown in a filter before the servlet dispatches), Tomcat/Jetty forward the request to /error. Spring Boot's BasicErrorController serves this endpoint, returning a Map of {timestamp, status, error, message, path}. Customise by: (1) extending BasicErrorController or implementing ErrorController; (2) implementing ErrorAttributes to change the attributes map; (3) adding a DefaultErrorViewResolver for HTML error pages (Thymeleaf). For REST APIs, override errorHtmlAttributes() and error() in BasicErrorController to return ProblemDetail consistently. This is the last line of defence — errors from filters (AuthenticationException, AccessDeniedException) end up here if not handled by the security filter chain.","correct":"When an exception escapes all HandlerExceptionResolver instances (or is thrown in a filter before the servlet dispatches), Tomcat/Jetty forward the request to /error.","distractors":["If the @ExceptionHandler method itself throws an exception, Spring falls back to the DefaultHandlerExceptionResolver, which typically returns a 500 Internal Server Error with a minimal response body.","When @Valid / @Validated fails on a @Controller @RequestBody parameter, Spring MVC throws MethodArgumentNotValidException (which extends BindException).","Request params and path variables are primitive or String types, not POJOs — @Valid cascade does not apply."],"distractorIds":["spring/validation_and_error_handling#4b529c502611","spring/validation_and_error_handling#2ab81ff92caa","spring/validation_and_error_handling#e8646fe2481c"],"concepts":["handlerexceptionresolver","jetty","dispatches","tomcat","escapes","thrown"],"questionMd":"How does Spring Boot's `/error` fallback endpoint work and how do you customise it?","correctMd":"When an exception escapes all `HandlerExceptionResolver` instances (or is thrown in a filter before the servlet dispatches), Tomcat/Jetty forward the request to `/error`.","answerFullMd":"When an exception escapes all `HandlerExceptionResolver` instances (or is thrown in a filter before the servlet dispatches), Tomcat/Jetty forward the request to `/error`. Spring Boot's `BasicErrorController` serves this endpoint, returning a `Map` of `{timestamp, status, error, message, path}`. Customise by: (1) extending `BasicErrorController` or implementing `ErrorController`; (2) implementing `ErrorAttributes` to change the attributes map; (3) adding a `DefaultErrorViewResolver` for HTML error pages (Thymeleaf). For REST APIs, override `errorHtmlAttributes()` and `error()` in `BasicErrorController` to return `ProblemDetail` consistently. This is the last line of defence — errors from filters (`AuthenticationException`, `AccessDeniedException`) end up here if not handled by the security filter chain.","distractorsMd":["If the `@ExceptionHandler` method itself throws an exception, Spring falls back to the `DefaultHandlerExceptionResolver`, which typically returns a 500 Internal Server Error with a minimal response body.","When `@Valid` / `@Validated` fails on a `@Controller` `@RequestBody` parameter, Spring MVC throws `MethodArgumentNotValidException` (which extends `BindException`).","Request params and path variables are primitive or String types, not POJOs — `@Valid` cascade does not apply."]},{"id":"spring/validation_and_error_handling#ab72f9f554d7","section":"spring","module":"spring/validation_and_error_handling","moduleName":"validation and error handling","sourceFile":"README.md","difficulty":"advanced","question":"What is @AssertTrue / @AssertFalse and when is it useful for cross-field validation?","answerFull":"@AssertTrue(message = \"End date must be after start date\") on a method isEndAfterStart() that returns boolean provides lightweight cross-field validation without a custom ConstraintValidator. The method accesses this.startDate and this.endDate. This is appropriate for simple temporal and logical relationships. For complex cross-field rules (involving external lookups or multiple conditions), a class-level @Constraint validator is cleaner because it separates the validation logic from the data class. The @AssertTrue approach couples validation logic with the domain object, which violates separation of concerns in domain-driven designs.","correct":"@AssertTrue(message = \"End date must be after start date\") on a method isEndAfterStart() that returns boolean provides lightweight cross-field validation without a custom ConstraintValidator.","distractors":["Bean Validation uses ValidationMessages.properties in the classpath root for constraint message templates (e.g., {jakarta.validation.constraints.NotBlank.message}).","If the @ExceptionHandler method itself throws an exception, Spring falls back to the DefaultHandlerExceptionResolver, which typically returns a 500 Internal Server Error with a minimal response body.","@Valid is the standard JSR-380 annotation (from jakarta.validation)."],"distractorIds":["spring/validation_and_error_handling#88e6a6cc33e1","spring/validation_and_error_handling#4b529c502611","spring/validation_and_error_handling#de1f88fb84b7"],"concepts":["asserttrue","constraintvalidator","isendafterstart","boolean","useful","lightweight"],"questionMd":"What is `@AssertTrue` / `@AssertFalse` and when is it useful for cross-field validation?","correctMd":"`@AssertTrue(message = \"End date must be after start date\")` on a method `isEndAfterStart()` that returns `boolean` provides lightweight cross-field validation without a custom `ConstraintValidator`.","answerFullMd":"`@AssertTrue(message = \"End date must be after start date\")` on a method `isEndAfterStart()` that returns `boolean` provides lightweight cross-field validation without a custom `ConstraintValidator`. The method accesses `this.startDate` and `this.endDate`. This is appropriate for simple temporal and logical relationships. For complex cross-field rules (involving external lookups or multiple conditions), a class-level `@Constraint` validator is cleaner because it separates the validation logic from the data class. The `@AssertTrue` approach couples validation logic with the domain object, which violates separation of concerns in domain-driven designs.","distractorsMd":["Bean Validation uses `ValidationMessages.properties` in the classpath root for constraint message templates (e.g., `{jakarta.validation.constraints.NotBlank.message}`).","If the `@ExceptionHandler` method itself throws an exception, Spring falls back to the `DefaultHandlerExceptionResolver`, which typically returns a 500 Internal Server Error with a minimal response body.","`@Valid` is the standard JSR-380 annotation (from `jakarta.validation`)."]},{"id":"spring/validation_and_error_handling#88e6a6cc33e1","section":"spring","module":"spring/validation_and_error_handling","moduleName":"validation and error handling","sourceFile":"README.md","difficulty":"advanced","question":"How would you globally configure the message source for constraint violation messages?","answerFull":"Bean Validation uses ValidationMessages.properties in the classpath root for constraint message templates (e.g., {jakarta.validation.constraints.NotBlank.message}). Override messages by creating src/main/resources/ValidationMessages.properties: properties jakarta.validation.constraints.NotBlank.message=This field is required com.example.UniqueEmail.message=This email is already taken Spring Boot also integrates with MessageSource — configure spring.validation.message-source or override the ValidatorFactory's MessageInterpolator with a MessageSourceMessageInterpolator to use messages.properties (the standard Spring i18n file) for all constraint messages. This enables locale-aware error messages for internationalised APIs.","correct":"Bean Validation uses ValidationMessages.properties in the classpath root for constraint message templates (e.g., {jakarta.validation.constraints.NotBlank.message}).","distractors":["@Valid is the standard JSR-380 annotation (from jakarta.validation).","@AssertTrue(message = \"End date must be after start date\") on a method isEndAfterStart() that returns boolean provides lightweight cross-field validation without a custom ConstraintValidator.","Validation groups are marker interfaces (empty, by convention)."],"distractorIds":["spring/validation_and_error_handling#de1f88fb84b7","spring/validation_and_error_handling#ab72f9f554d7","spring/validation_and_error_handling#f7d527e46c12"],"concepts":["notblank","validationmessages","jakarta","globally","violation","classpath"],"correctMd":"Bean Validation uses `ValidationMessages.properties` in the classpath root for constraint message templates (e.g., `{jakarta.validation.constraints.NotBlank.message}`).","answerFullMd":"Bean Validation uses `ValidationMessages.properties` in the classpath root for constraint message templates (e.g., `{jakarta.validation.constraints.NotBlank.message}`). Override messages by creating `src/main/resources/ValidationMessages.properties`: ```properties jakarta.validation.constraints.NotBlank.message=This field is required com.example.UniqueEmail.message=This email is already taken ``` Spring Boot also integrates with `MessageSource` — configure `spring.validation.message-source` or override the `ValidatorFactory`'s `MessageInterpolator` with a `MessageSourceMessageInterpolator` to use `messages.properties` (the standard Spring i18n file) for all constraint messages. This enables locale-aware error messages for internationalised APIs.","distractorsMd":["`@Valid` is the standard JSR-380 annotation (from `jakarta.validation`).","`@AssertTrue(message = \"End date must be after start date\")` on a method `isEndAfterStart()` that returns `boolean` provides lightweight cross-field validation without a custom `ConstraintValidator`.",null]}]