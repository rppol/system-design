[{"id":"python/the_type_system_and_typing#Q1-2508","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":1,"difficulty":"core","question":"What is the difference between TypeVar with a bound and one with constraints?","answerFull":"A bounded TypeVar (T = TypeVar(\"T\", bound=Foo)) accepts Foo and any subtype of Foo — an open set. A constrained TypeVar (T = TypeVar(\"T\", str, bytes)) accepts exactly those listed types (closed set) and treats subtypes as their parent constraint. Use bounds when you need any subtype; use constraints when you need an exact one-of selection.","correct":"A bounded TypeVar (T = TypeVar(\"T\", bound=Foo)) accepts Foo and any subtype of Foo — an open set.","distractors":["Covariant (T_co = TypeVar(\"T_co\", covariant=True)): safe for *producer / output* positions.","Use the producer/consumer mnemonic: if a class only *produces* (returns) values of type T, mark T covariant — it is safe to substitute a subtype.","PEP 695 introduces inline type parameter syntax: def fT -> T: instead of T = TypeVar(\"T\"); def f(x: T) -> T:."]},{"id":"python/the_type_system_and_typing#Q2-2509","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":2,"difficulty":"core","question":"Why is list invariant while Sequence is covariant in Python's type system?","answerFull":"list is invariant because it supports both reads (getitem) and writes (setitem, append). If list[Dog] were covariant and accepted where list[Animal] is expected, you could append(Cat()) to a list[Dog], corrupting it. Sequence is read-only (no mutation methods), so accepting Sequence[Dog] where Sequence[Animal] is expected is safe — you only ever read Animals, and every Dog is an Animal.","correct":"list is invariant because it supports both reads (getitem) and writes (setitem, append).","distractors":["Callable[[Param], Return] is contravariant in its parameter types and covariant in its return type, matching Liskov Substitution Principle.","Covariant (T_co = TypeVar(\"T_co\", covariant=True)): safe for *producer / output* positions.","They are pre-defined Protocols in the typing module that formalize Python's existing dunder- method contracts."]},{"id":"python/the_type_system_and_typing#Q3-2510","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":3,"difficulty":"core","question":"How does Protocol differ from an ABC, and when do you choose one over the other?","answerFull":"Protocol is structurally typed — any class with the required attributes/methods satisfies it without inheritance. ABC is nominally typed — subclasses must explicitly inherit. Choose Protocol when defining interfaces for third-party classes you do not control, for plugin architectures, and for testing (mock objects satisfy protocols without inheriting base classes). Use ABC when you want to enforce inheritance hierarchy and provide shared implementation via @abstractmethod.","correct":"Protocol is structurally typed — any class with the required attributes/methods satisfies it without inheritance.","distractors":["ABC requires explicit inheritance (nominal subtyping) and can provide default method bodies; it raises TypeError at instantiation if abstract methods are missing.","At each call site where a Protocol is expected, mypy checks that the actual argument's type has all Protocol members with compatible signatures.","python from typing import Protocol, Self class AsyncContextManager(Protocol): async def aenter(self) -> Self: ..."]},{"id":"python/the_type_system_and_typing#Q4-2511","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":4,"difficulty":"core","question":"What is ParamSpec and what problem does it solve that TypeVar cannot?","answerFull":"ParamSpec [3.10] captures the full parameter list (positional + keyword) of a callable, not just a single type. A regular TypeVar bound to Callable[..., R] loses parameter names and types in the wrapper, breaking IDE autocomplete and mypy checks on arguments. ParamSpec preserves the exact signature through the wrapper function via the P.args / P.kwargs components.","correct":"ParamSpec [3.10] captures the full parameter list (positional + keyword) of a callable, not just a single type.","distractors":["FastAPI calls get_type_hints(endpoint, include_extras=True) to retrieve the full annotated types (including Annotated metadata) and inspect.signature() to retrieve defaults.","A bounded TypeVar (T = TypeVar(\"T\", bound=Foo)) accepts Foo and any subtype of Foo — an open set.","Use a constrained TypeVar: AnyStr = TypeVar(\"AnyStr\", str, bytes)."]},{"id":"python/the_type_system_and_typing#Q5-2512","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":5,"difficulty":"core","question":"What does @runtime_checkable do and what is its limitation?","answerFull":"It enables isinstance() checks against a Protocol at runtime. Without it, isinstance(x, MyProtocol) raises TypeError. The limitation: the check is shallow — it only verifies that attributes and methods with the required names exist, not that their signatures match. A method with the wrong signature or even a non-callable attribute with the right name passes the check. Static checkers (mypy) are stricter and check signatures.","correct":"It enables isinstance() checks against a Protocol at runtime.","distractors":["Without Protocol in the MRO, Python (and mypy) treats the composed class as a concrete class inheriting from Protocol bases.","Protocol is structurally typed — any class with the required attributes/methods satisfies it without inheritance.","python from typing import Protocol, Self class AsyncContextManager(Protocol): async def aenter(self) -> Self: ..."]},{"id":"python/the_type_system_and_typing#Q6-2513","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":6,"difficulty":"intermediate","question":"Explain how FastAPI uses type annotations to build its request validation logic.","answerFull":"FastAPI calls get_type_hints(endpoint, include_extras=True) to retrieve the full annotated types (including Annotated metadata) and inspect.signature() to retrieve defaults. It maps each parameter annotation to a Pydantic field, then generates a validation model for query parameters, path parameters, body, and headers. The include_extras=True flag is critical — without it, Annotated[int, Query(ge=1)] reduces to int and the Query constraint is dropped silently.","correct":"FastAPI calls get_type_hints(endpoint, include_extras=True) to retrieve the full annotated types (including Annotated metadata) and inspect.signature() to retrieve defaults.","distractors":["ParamSpec [3.10] captures the full parameter list (positional + keyword) of a callable, not just a single type.","Self refers to the type of the current class in a method signature, including subclasses.","Covariant (T_co = TypeVar(\"T_co\", covariant=True)): safe for *producer / output* positions."]},{"id":"python/the_type_system_and_typing#Q8-2514","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":8,"difficulty":"intermediate","question":"How does Annotated enable Pydantic v2's field-level validation without subclassing?","answerFull":"Annotated[int, Field(gt=0)] stores Field(gt=0) as metadata alongside the base type int. Pydantic v2 calls get_type_hints(model, include_extras=True), extracts the FieldInfo object from the Annotated metadata tuple, and builds a JSON Schema validator from it. This lets you define reusable validated type aliases (PositiveInt = Annotated[int, Field(gt=0)]) that compose across multiple models without inheritance, unlike Pydantic v1 validators which required @validator classmethods.","correct":"Annotated[int, Field(gt=0)] stores Field(gt=0) as metadata alongside the base type int.","distractors":["FastAPI calls get_type_hints(endpoint, include_extras=True) to retrieve the full annotated types (including Annotated metadata) and inspect.signature() to retrieve defaults.","Without Protocol in the MRO, Python (and mypy) treats the composed class as a concrete class inheriting from Protocol bases.","High-level modules define the interface as a Protocol."]},{"id":"python/the_type_system_and_typing#Q9-2515","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":9,"difficulty":"intermediate","question":"What is TypeGuard and how does it differ from a plain boolean check?","answerFull":"A plain if isinstance(x, str) allows mypy to narrow x to str inside the block because mypy has built-in knowledge of isinstance. TypeGuard [3.10] extends this to *user-defined* predicates — if a function returns TypeGuard[list[str]], mypy narrows the checked variable to list[str] in the if branch. Without TypeGuard, user-defined predicates that return bool do not trigger narrowing and the original broad type persists.","correct":"A plain if isinstance(x, str) allows mypy to narrow x to str inside the block because mypy has built-in knowledge of isinstance.","distractors":["Structural subtyping means a type is compatible based on its shape — the methods and attributes it has — rather than its class hierarchy.","Use a constrained TypeVar: AnyStr = TypeVar(\"AnyStr\", str, bytes).","At each call site where a Protocol is expected, mypy checks that the actual argument's type has all Protocol members with compatible signatures."]},{"id":"python/the_type_system_and_typing#Q10-2516","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":10,"difficulty":"intermediate","question":"What are the rules for when a TypeVar should be covariant vs contravariant vs invariant?","answerFull":"Use the producer/consumer mnemonic: if a class only *produces* (returns) values of type T, mark T covariant — it is safe to substitute a subtype. If a class only *consumes* (accepts) values of type T, mark T contravariant — it is safe to substitute a supertype. If a class does both (e.g., a read-write container), T must be invariant. A common heuristic: if you can describe the generic container as \"a source of T\", use covariant; if \"a sink for T\", use contravariant; if both, leave invariant.","correct":"Use the producer/consumer mnemonic: if a class only *produces* (returns) values of type T, mark T covariant — it is safe to substitute a subtype.","distractors":["A bounded TypeVar (T = TypeVar(\"T\", bound=Foo)) accepts Foo and any subtype of Foo — an open set.","Use a constrained TypeVar: AnyStr = TypeVar(\"AnyStr\", str, bytes).","Self refers to the type of the current class in a method signature, including subclasses."]},{"id":"python/the_type_system_and_typing#Q11-2517","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":11,"difficulty":"advanced","question":"How does PEP 695 [3.12] improve generic syntax over the pre-3.12 approach?","answerFull":"PEP 695 introduces inline type parameter syntax: def fT -> T: instead of T = TypeVar(\"T\"); def f(x: T) -> T:. This eliminates module-level TypeVar declarations, removes the need for Generic[T] base class, avoids TypeVar name string duplication (TypeVar(\"T\") where \"T\" must match the variable name), and scopes the type parameter to its function or class. The type statement (type Vector[T] = list[T]) also replaces the verbose TypeAlias annotation.","correct":"PEP 695 introduces inline type parameter syntax: def fT -> T: instead of T = TypeVar(\"T\"); def f(x: T) -> T:.","distractors":["A bounded TypeVar (T = TypeVar(\"T\", bound=Foo)) accepts Foo and any subtype of Foo — an open set.","Covariant (T_co = TypeVar(\"T_co\", covariant=True)): safe for *producer / output* positions.","Use a constrained TypeVar: AnyStr = TypeVar(\"AnyStr\", str, bytes)."]},{"id":"python/the_type_system_and_typing#Q12-2518","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":12,"difficulty":"advanced","question":"What is LiteralString [3.11] and why does it matter for security?","answerFull":"LiteralString is a type that can only be satisfied by actual string literals or concatenations of literals — not by dynamically constructed strings. This allows static checkers to reject code like safe_query(conn, f\"SELECT * FROM {user_input}\") where the function signature requires LiteralString, preventing SQL injection and similar injection attacks at analysis time rather than relying solely on runtime sanitization.","correct":"LiteralString is a type that can only be satisfied by actual string literals or concatenations of literals — not by dynamically constructed strings.","distractors":["At each call site where a Protocol is expected, mypy checks that the actual argument's type has all Protocol members with compatible signatures.","A bounded TypeVar (T = TypeVar(\"T\", bound=Foo)) accepts Foo and any subtype of Foo — an open set.","list is invariant because it supports both reads (getitem) and writes (setitem, append)."]},{"id":"python/the_type_system_and_typing#Q13-2519","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":13,"difficulty":"advanced","question":"How do you type a function that accepts either str or bytes and returns the same type?","answerFull":"Use a constrained TypeVar: AnyStr = TypeVar(\"AnyStr\", str, bytes). This is different from str | bytes as a return type — with str | bytes, a function accepting str could be declared as returning bytes. With AnyStr, mypy enforces that the return type matches the input type: def decode(x: AnyStr) -> AnyStr: will return str when given str and bytes when given bytes.","correct":"Use a constrained TypeVar: AnyStr = TypeVar(\"AnyStr\", str, bytes).","distractors":["Use the producer/consumer mnemonic: if a class only *produces* (returns) values of type T, mark T covariant — it is safe to substitute a subtype.","PEP 695 introduces inline type parameter syntax: def fT -> T: instead of T = TypeVar(\"T\"); def f(x: T) -> T:.","Define a composed Protocol: python from typing import Protocol class ReadWritable(Protocol): def read(self, n: int = -1) -> bytes: ..."]},{"id":"python/the_type_system_and_typing#Q14-2520","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":14,"difficulty":"advanced","question":"What is Self [3.11] and when does it replace a TypeVar?","answerFull":"Self refers to the type of the current class in a method signature, including subclasses. Before 3.11, fluent builder patterns required SelfT = TypeVar(\"SelfT\", bound=\"Builder\") and using SelfT as both the parameter type of cls and the return type. Self makes this implicit — def clone(self) -> Self: automatically resolves to the concrete subclass type when called on a subclass. This is important for inheritance: if SubBuilder(Builder) inherits clone, the return type is SubBuilder, not Builder.","correct":"Self refers to the type of the current class in a method signature, including subclasses.","distractors":["python from typing import Protocol, Self class AsyncContextManager(Protocol): async def aenter(self) -> Self: ...","Use a constrained TypeVar: AnyStr = TypeVar(\"AnyStr\", str, bytes).","ABC requires explicit inheritance (nominal subtyping) and can provide default method bodies; it raises TypeError at instantiation if abstract methods are missing."]},{"id":"python/the_type_system_and_typing#Q15-2521","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":15,"difficulty":"advanced","question":"How does variance interact with Callable types?","answerFull":"Callable[[Param], Return] is contravariant in its parameter types and covariant in its return type, matching Liskov Substitution Principle. A Callable[[Animal], str] is a subtype of Callable[[Dog], str] — a function that handles any Animal can certainly handle a Dog (more general parameter = subtype of callable). A Callable[[Dog], Dog] is a subtype of Callable[[Dog], Animal] — a function returning a Dog satisfies anywhere a function returning Animal is expected (return types are covariant). This mirrors how function types work mathematically and is enforced by mypy's callable variance rules.","correct":"Callable[[Param], Return] is contravariant in its parameter types and covariant in its return type, matching Liskov Substitution Principle.","distractors":["When the callable has keyword-only arguments, *args, or kwargs that Callable cannot express.","Covariant (T_co = TypeVar(\"T_co\", covariant=True)): safe for *producer / output* positions.","PEP 695 introduces inline type parameter syntax: def fT -> T: instead of T = TypeVar(\"T\"); def f(x: T) -> T:."]},{"id":"python/the_type_system_and_typing#Q1-2522","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":1,"difficulty":"core","question":"What is structural subtyping and how does typing.Protocol implement it?","answerFull":"Structural subtyping means a type is compatible based on its shape — the methods and attributes it has — rather than its class hierarchy. Protocol [PEP 544, 3.8] defines a set of required members; any class that provides those members (with compatible signatures) satisfies the Protocol, verified by mypy/pyright at analysis time without runtime enforcement unless @runtime_checkable is added.","correct":"Structural subtyping means a type is compatible based on its shape — the methods and attributes it has — rather than its class hierarchy.","distractors":["ABC requires explicit inheritance (nominal subtyping) and can provide default method bodies; it raises TypeError at instantiation if abstract methods are missing.","python from typing import Protocol, Self class AsyncContextManager(Protocol): async def aenter(self) -> Self: ...","At each call site where a Protocol is expected, mypy checks that the actual argument's type has all Protocol members with compatible signatures."]},{"id":"python/the_type_system_and_typing#Q2-2523","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":2,"difficulty":"core","question":"How does Protocol differ from ABC?","answerFull":"ABC requires explicit inheritance (nominal subtyping) and can provide default method bodies; it raises TypeError at instantiation if abstract methods are missing. Protocol uses structural matching — no inheritance needed — and is verified statically, not at instantiation. Prefer Protocol for library boundaries and dependency injection; prefer ABC when you need shared implementation or runtime instantiation enforcement.","correct":"ABC requires explicit inheritance (nominal subtyping) and can provide default method bodies; it raises TypeError at instantiation if abstract methods are missing.","distractors":["Each call performs a hasattr() lookup for every Protocol member.","Define a composed Protocol: python from typing import Protocol class ReadWritable(Protocol): def read(self, n: int = -1) -> bytes: ...","Without Protocol in the MRO, Python (and mypy) treats the composed class as a concrete class inheriting from Protocol bases."]},{"id":"python/the_type_system_and_typing#Q3-2524","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":3,"difficulty":"core","question":"What does @runtime_checkable do and what are its limitations?","answerFull":"It allows isinstance(obj, SomeProtocol) at runtime. The check is *shallow*: it only verifies that each Protocol member name exists on the object via hasattr(). It does NOT verify that the attribute is callable, has the right signature, or returns the right type. A class attribute close = 42 satisfies @runtime_checkable Closeable(Protocol) at runtime even though close is not a method. For security-sensitive checks, use beartype or explicit attribute + callable guards.","correct":"It allows isinstance(obj, SomeProtocol) at runtime.","distractors":["A plain if isinstance(x, str) allows mypy to narrow x to str inside the block because mypy has built-in knowledge of isinstance.","It enables isinstance() checks against a Protocol at runtime.","A bounded TypeVar (T = TypeVar(\"T\", bound=Foo)) accepts Foo and any subtype of Foo — an open set."]},{"id":"python/the_type_system_and_typing#Q4-2525","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":4,"difficulty":"core","question":"When would you use a callable Protocol instead of Callable[[X], Y]?","answerFull":"When the callable has keyword-only arguments, *args, or kwargs that Callable cannot express. Callable[[str], str] matches any one-argument str-to-str function. A callable Protocol can require def call(self, text: str, *, lowercase: bool = False) -> str, restricting to functions that accept that exact keyword argument.","correct":"When the callable has keyword-only arguments, *args, or kwargs that Callable cannot express.","distractors":["ParamSpec [3.10] captures the full parameter list (positional + keyword) of a callable, not just a single type.","Each call performs a hasattr() lookup for every Protocol member.","Callable[[Param], Return] is contravariant in its parameter types and covariant in its return type, matching Liskov Substitution Principle."]},{"id":"python/the_type_system_and_typing#Q5-2526","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":5,"difficulty":"core","question":"What is the difference between a covariant and contravariant TypeVar in a Protocol?","answerFull":"Covariant (T_co = TypeVar(\"T_co\", covariant=True)): safe for *producer / output* positions. A Container[Dog] satisfies Container[Animal] — you can use the more specific output where the less specific is expected. Contravariant (T_contra, contravariant=True): safe for *consumer / input* positions. A Handler[Animal] satisfies Handler[Dog] — a handler that accepts any animal also handles dogs. Invariant (default): must match exactly; suitable when the type is both read and written (mutable containers).","correct":"Covariant (T_co = TypeVar(\"T_co\", covariant=True)): safe for *producer / output* positions.","distractors":["A bounded TypeVar (T = TypeVar(\"T\", bound=Foo)) accepts Foo and any subtype of Foo — an open set.","Use the producer/consumer mnemonic: if a class only *produces* (returns) values of type T, mark T covariant — it is safe to substitute a subtype.","Protocol is structurally typed — any class with the required attributes/methods satisfies it without inheritance."]},{"id":"python/the_type_system_and_typing#Q6-2527","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":6,"difficulty":"intermediate","question":"Why must Protocol appear in the MRO when composing multiple Protocols?","answerFull":"Without Protocol in the MRO, Python (and mypy) treats the composed class as a concrete class inheriting from Protocol bases. The structural-typing semantics are lost — mypy requires explicit inheritance to satisfy the combined type, defeating the purpose. Adding Protocol explicitly (e.g., class ReadWritable(Readable, Writable, Protocol)) preserves structural typing for the composed interface.","correct":"Without Protocol in the MRO, Python (and mypy) treats the composed class as a concrete class inheriting from Protocol bases.","distractors":["python from typing import Protocol, Self class AsyncContextManager(Protocol): async def aenter(self) -> Self: ...","Python raises TypeError: Protocols with non-method members don't support issubclass() (or a similar message).","It enables isinstance() checks against a Protocol at runtime."]},{"id":"python/the_type_system_and_typing#Q7-2528","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":7,"difficulty":"intermediate","question":"Can Protocol method bodies be inherited by conforming classes?","answerFull":"No. Method bodies written inside a Protocol class exist only on the Protocol class itself. They do not propagate to conforming classes. If you want shared implementation, use an ABC mixin, a concrete base class, or a separate utility function alongside the Protocol.","correct":"No. Method bodies written inside a Protocol class exist only on the Protocol class itself.","distractors":["Protocol is structurally typed — any class with the required attributes/methods satisfies it without inheritance.","A plain if isinstance(x, str) allows mypy to narrow x to str inside the block because mypy has built-in knowledge of isinstance.","ABC requires explicit inheritance (nominal subtyping) and can provide default method bodies; it raises TypeError at instantiation if abstract methods are missing."]},{"id":"python/the_type_system_and_typing#Q8-2529","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":8,"difficulty":"intermediate","question":"How does mypy verify that a class satisfies a Protocol?","answerFull":"At each call site where a Protocol is expected, mypy checks that the actual argument's type has all Protocol members with compatible signatures. It does not check this at class definition time. This means you can define PostgresRepository before writing the Repository Protocol and mypy will verify compatibility when you first pass a PostgresRepository where Repository is expected.","correct":"At each call site where a Protocol is expected, mypy checks that the actual argument's type has all Protocol members with compatible signatures.","distractors":["Each call performs a hasattr() lookup for every Protocol member.","python from typing import Protocol, Self class AsyncContextManager(Protocol): async def aenter(self) -> Self: ...","Structural subtyping means a type is compatible based on its shape — the methods and attributes it has — rather than its class hierarchy."]},{"id":"python/the_type_system_and_typing#Q9-2530","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":9,"difficulty":"intermediate","question":"What happens with isinstance(x, P) if P is a non-@runtime_checkable Protocol?","answerFull":"Python raises TypeError: Protocols with non-method members don't support issubclass() (or a similar message). You must decorate the Protocol with @runtime_checkable to use it with isinstance(). Without the decorator, the Protocol is a purely static construct.","correct":"Python raises TypeError: Protocols with non-method members don't support issubclass() (or a similar message).","distractors":["python from typing import Protocol, Self class AsyncContextManager(Protocol): async def aenter(self) -> Self: ...","It enables isinstance() checks against a Protocol at runtime.","Without Protocol in the MRO, Python (and mypy) treats the composed class as a concrete class inheriting from Protocol bases."]},{"id":"python/the_type_system_and_typing#Q10-2531","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":10,"difficulty":"intermediate","question":"How do standard-library SupportsInt, Sized, and Iterable relate to Protocol?","answerFull":"They are pre-defined Protocols in the typing module that formalize Python's existing dunder- method contracts. SupportsInt requires int; Sized requires len; Iterable[T] requires iter. They allow existing built-in types (int, list, dict, str) to satisfy structural type annotations without inheriting from anything.","correct":"They are pre-defined Protocols in the typing module that formalize Python's existing dunder- method contracts.","distractors":["Protocol is structurally typed — any class with the required attributes/methods satisfies it without inheritance.","Without Protocol in the MRO, Python (and mypy) treats the composed class as a concrete class inheriting from Protocol bases.","At each call site where a Protocol is expected, mypy checks that the actual argument's type has all Protocol members with compatible signatures."]},{"id":"python/the_type_system_and_typing#Q11-2532","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":11,"difficulty":"advanced","question":"How would you type-hint a function that accepts any file-like object supporting read and write?","answerFull":"Define a composed Protocol: python from typing import Protocol class ReadWritable(Protocol): def read(self, n: int = -1) -> bytes: ... def write(self, data: bytes) -> int: ... Any io.BytesIO, io.FileIO, or custom buffer satisfies this. Alternatively, use typing.BinaryIO from the standard library, which provides a broader file-like Protocol.","correct":"Define a composed Protocol: python from typing import Protocol class ReadWritable(Protocol): def read(self, n: int = -1) -> bytes: ...","distractors":["Self refers to the type of the current class in a method signature, including subclasses.","A bounded TypeVar (T = TypeVar(\"T\", bound=Foo)) accepts Foo and any subtype of Foo — an open set.","High-level modules define the interface as a Protocol."]},{"id":"python/the_type_system_and_typing#Q12-2533","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":12,"difficulty":"advanced","question":"What is the performance cost of @runtime_checkable isinstance() checks?","answerFull":"Each call performs a hasattr() lookup for every Protocol member. For a 3-member Protocol this is roughly 3-5 µs per call. CPython 3.12 added internal caching so repeat checks on the same type are near-zero after the first. In hot loops (millions of iterations), prefer EAFP (try/except AttributeError) or cache the boolean result outside the loop.","correct":"Each call performs a hasattr() lookup for every Protocol member.","distractors":["It enables isinstance() checks against a Protocol at runtime.","python from typing import Protocol, Self class AsyncContextManager(Protocol): async def aenter(self) -> Self: ...","It allows isinstance(obj, SomeProtocol) at runtime."]},{"id":"python/the_type_system_and_typing#Q13-2534","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":13,"difficulty":"advanced","question":"How do you express a Protocol for an object that must support async with (async context manager)?","answerFull":"python from typing import Protocol, Self class AsyncContextManager(Protocol): async def aenter(self) -> Self: ... async def aexit(self, exc_type, exc_val, exc_tb) -> bool | None: ... Any class that implements both dunder methods satisfies the Protocol structurally.","correct":"python from typing import Protocol, Self class AsyncContextManager(Protocol): async def aenter(self) -> Self: ...","distractors":["Python raises TypeError: Protocols with non-method members don't support issubclass() (or a similar message).","Define a composed Protocol: python from typing import Protocol class ReadWritable(Protocol): def read(self, n: int = -1) -> bytes: ...","Protocol is structurally typed — any class with the required attributes/methods satisfies it without inheritance."]},{"id":"python/the_type_system_and_typing#Q15-2535","section":"python","module":"python/the_type_system_and_typing","moduleName":"the type system and typing","qIndex":15,"difficulty":"advanced","question":"How does Protocol support the Dependency Inversion Principle in Python?","answerFull":"High-level modules define the interface as a Protocol. Low-level modules implement it without importing the Protocol. The type checker verifies the match at the injection site. This inverts the dependency: the interface is owned by the consumer (high-level module), not the implementor. Test doubles implement the same Protocol without inheriting any production code, giving clean isolation with zero mocking overhead.","correct":"High-level modules define the interface as a Protocol.","distractors":["At each call site where a Protocol is expected, mypy checks that the actual argument's type has all Protocol members with compatible signatures.","Callable[[Param], Return] is contravariant in its parameter types and covariant in its return type, matching Liskov Substitution Principle.","python from typing import Protocol, Self class AsyncContextManager(Protocol): async def aenter(self) -> Self: ..."]},{"id":"python/performance_and_profiling#Q1-2536","section":"python","module":"python/performance_and_profiling","moduleName":"performance and profiling","qIndex":1,"difficulty":"core","question":"What is the difference between tottime and cumtime in cProfile, and which do you use to find the actual bottleneck?","answerFull":"tottime is time spent inside a function excluding callees; cumtime is total time including all nested calls. To find the function actually burning CPU cycles, sort by tottime — a function with high cumtime but low tottime is slow only because it calls slow children, not because its own code is inefficient.","correct":"tottime is time spent inside a function excluding callees; cumtime is total time including all nested calls.","distractors":["memory_profiler measures RSS (resident set size) delta per line using OS-level memory queries — it shows the total process memory footprint change.","Cython requires a C compiler toolchain at build time and produces platform-specific .so files, complicating packaging and cross-platform distribution.","attrgetter is implemented in C and performs attribute access without creating a Python function frame per comparison."]},{"id":"python/performance_and_profiling#Q2-2537","section":"python","module":"python/performance_and_profiling","moduleName":"performance and profiling","qIndex":2,"difficulty":"core","question":"Why is LOAD_GLOBAL slower than LOAD_FAST, and how can you exploit this in a hot loop?","answerFull":"LOAD_GLOBAL performs a dictionary lookup in globals() and falls through to builtins if not found — two dict operations per access. LOAD_FAST is a direct array index into the frame's fastlocals array. In a tight loop, hoist frequently-accessed globals and built-ins into local variables at function entry: sqrt = math.sqrt before the loop reduces each access to a LOAD_FAST.","correct":"LOAD_GLOBAL performs a dictionary lookup in globals() and falls through to builtins if not found — two dict operations per access.","distractors":["For a local variable, x += 1 compiles to LOAD_FAST x, LOAD_CONST 1, BINARY_OP +=, STORE_FAST x — 4 opcodes, 2 of which are fast array operations.","attrgetter is implemented in C and performs attribute access without creating a Python function frame per comparison.","Call dis.dis(original_func) and dis.dis(optimized_func), count the number of opcodes in the hot section, and confirm that the optimized version has fewer or cheaper opcodes."]},{"id":"python/performance_and_profiling#Q3-2538","section":"python","module":"python/performance_and_profiling","moduleName":"performance and profiling","qIndex":3,"difficulty":"core","question":"How does CPython 3.11's specializing adaptive interpreter work, and what speedup does it deliver?","answerFull":"The interpreter observes each bytecode instruction at runtime. After approximately 8 executions of the same instruction path, it replaces the generic opcode with a specialized variant — for example, LOAD_ATTR becomes LOAD_ATTR_INSTANCE_VALUE when the attribute is consistently found in the instance dict at offset 0. If the type changes, the instruction deoptimizes back to generic. This delivered ~25% throughput improvement over Python 3.10 on the pyperformance benchmark suite, with hot integer loops seeing 2-3x speedup. [3.11]","correct":"The interpreter observes each bytecode instruction at runtime.","distractors":["tottime is time spent inside a function excluding callees; cumtime is total time including all nested calls.","LOAD_GLOBAL performs a dictionary lookup in globals() and falls through to builtins if not found — two dict operations per access.","Use py-spy when you need to profile a running production process without modifying code or restarting it, and when you cannot afford >1% latency overhead."]},{"id":"python/performance_and_profiling#Q4-2539","section":"python","module":"python/performance_and_profiling","moduleName":"performance and profiling","qIndex":4,"difficulty":"core","question":"When would you choose py-spy over cProfile, and why?","answerFull":"Use py-spy when you need to profile a running production process without modifying code or restarting it, and when you cannot afford >1% latency overhead. py-spy uses OS-level process inspection (ptrace on Linux, task_for_pid on macOS) to sample the call stack at 100 Hz — overhead is under 1%. cProfile hooks every function call via sys.setprofile, adding 1.5-3x overhead and requiring code instrumentation, making it unsuitable for production traffic.","correct":"Use py-spy when you need to profile a running production process without modifying code or restarting it, and when you cannot afford >1% latency overhead.","distractors":["memory_profiler measures RSS (resident set size) delta per line using OS-level memory queries — it shows the total process memory footprint change.","attrgetter is implemented in C and performs attribute access without creating a Python function frame per comparison.","Step 1: attach py-spy to the production worker to get a flamegraph without disruption — identify the top frame."]},{"id":"python/performance_and_profiling#Q5-2540","section":"python","module":"python/performance_and_profiling","moduleName":"performance and profiling","qIndex":5,"difficulty":"core","question":"What is the performance difference between sorted(key=lambda x: x.attr) and sorted(key=attrgetter(\"attr\")), and why?","answerFull":"attrgetter is implemented in C and performs attribute access without creating a Python function frame per comparison. The lambda creates a Python closure and incurs frame push/pop, argument marshaling, and CALL opcode overhead on every comparison. For 10,000 records, attrgetter is approximately 30-40% faster. The difference grows with list size because key is called O(n) times during sort.","correct":"attrgetter is implemented in C and performs attribute access without creating a Python function frame per comparison.","distractors":["LOAD_GLOBAL performs a dictionary lookup in globals() and falls through to builtins if not found — two dict operations per access.","Use py-spy when you need to profile a running production process without modifying code or restarting it, and when you cannot afford >1% latency overhead.","Python strings are immutable."]},{"id":"python/performance_and_profiling#Q6-2541","section":"python","module":"python/performance_and_profiling","moduleName":"performance and profiling","qIndex":6,"difficulty":"intermediate","question":"How does tracemalloc differ from memory_profiler, and when would you use each?","answerFull":"memory_profiler measures RSS (resident set size) delta per line using OS-level memory queries — it shows the total process memory footprint change. tracemalloc hooks Python's memory allocator and records allocation traces with file and line number, enabling you to identify which code path allocated specific objects and track peak allocation. Use memory_profiler for quick \"which function is bloating RSS\" investigation; use tracemalloc for precise allocation auditing and leak detection by comparing snapshots over time.","correct":"memory_profiler measures RSS (resident set size) delta per line using OS-level memory queries — it shows the total process memory footprint change.","distractors":["tottime is time spent inside a function excluding callees; cumtime is total time including all nested calls.","timeit.repeat(stmt, number=N, repeat=R) runs the statement N times per trial and repeats for R trials, returning a list of R total times.","Use py-spy when you need to profile a running production process without modifying code or restarting it, and when you cannot afford >1% latency overhead."]},{"id":"python/performance_and_profiling#Q7-2542","section":"python","module":"python/performance_and_profiling","moduleName":"performance and profiling","qIndex":7,"difficulty":"intermediate","question":"Explain the timeit.repeat() pattern and why you should take the minimum, not the mean.","answerFull":"timeit.repeat(stmt, number=N, repeat=R) runs the statement N times per trial and repeats for R trials, returning a list of R total times. You take the minimum because it represents the fastest your hardware can execute the code path with minimal OS interference. The mean is skewed upward by GC pauses, OS scheduling preemptions, and cache misses that are not intrinsic to the code being measured. The minimum is the stable lower bound; variance above it is noise.","correct":"timeit.repeat(stmt, number=N, repeat=R) runs the statement N times per trial and repeats for R trials, returning a list of R total times.","distractors":["memory_profiler measures RSS (resident set size) delta per line using OS-level memory queries — it shows the total process memory footprint change.","tottime is time spent inside a function excluding callees; cumtime is total time including all nested calls.","Call dis.dis(original_func) and dis.dis(optimized_func), count the number of opcodes in the hot section, and confirm that the optimized version has fewer or cheaper opcodes."]},{"id":"python/performance_and_profiling#Q8-2543","section":"python","module":"python/performance_and_profiling","moduleName":"performance and profiling","qIndex":8,"difficulty":"intermediate","question":"What are the practical limitations of Cython, and when is mypyc a better choice?","answerFull":"Cython requires a C compiler toolchain at build time and produces platform-specific .so files, complicating packaging and cross-platform distribution. Cython syntax (cdef, cpdef) is non-standard Python and requires separate .pyx files. mypyc compiles standard mypy-annotated Python — no syntax changes, same source file, easier CI integration. mypyc is better for well-typed, non-dynamic Python codebases where you want speedup without a build system change. Cython is better for tight numeric loops that need explicit C type declarations and Numpy buffer protocol integration.","correct":"Cython requires a C compiler toolchain at build time and produces platform-specific .so files, complicating packaging and cross-platform distribution.","distractors":["tottime is time spent inside a function excluding callees; cumtime is total time including all nested calls.","LOAD_GLOBAL performs a dictionary lookup in globals() and falls through to builtins if not found — two dict operations per access.","The interpreter observes each bytecode instruction at runtime."]},{"id":"python/performance_and_profiling#Q9-2544","section":"python","module":"python/performance_and_profiling","moduleName":"performance and profiling","qIndex":9,"difficulty":"intermediate","question":"How would you use dis to verify that a Python optimization actually changes the bytecode?","answerFull":"Call dis.dis(original_func) and dis.dis(optimized_func), count the number of opcodes in the hot section, and confirm that the optimized version has fewer or cheaper opcodes. For example, confirming that global_x += 1 generates LOAD_GLOBAL + STORE_GLOBAL while local_x += 1 generates LOAD_FAST + STORE_FAST proves the optimization is real at the bytecode level before benchmarking.","correct":"Call dis.dis(original_func) and dis.dis(optimized_func), count the number of opcodes in the hot section, and confirm that the optimized version has fewer or cheaper opcodes.","distractors":["attrgetter is implemented in C and performs attribute access without creating a Python function frame per comparison.","Python strings are immutable.","timeit.repeat(stmt, number=N, repeat=R) runs the statement N times per trial and repeats for R trials, returning a list of R total times."]},{"id":"python/performance_and_profiling#Q10-2545","section":"python","module":"python/performance_and_profiling","moduleName":"performance and profiling","qIndex":10,"difficulty":"intermediate","question":"A FastAPI endpoint takes 600ms P99. Walk through your profiling workflow.","answerFull":"Step 1: attach py-spy to the production worker to get a flamegraph without disruption — identify the top frame. Step 2: if the hot frame is in Python code (not I/O), run cProfile on a staging replica with a realistic request payload to get function-level tottime. Step 3: use line_profiler on the identified hot function to find the exact line. Step 4: check if the bottleneck is algorithmic (fix logic), a slow library call (switch to faster library like orjson), or a repeated attribute lookup in a loop (local alias). Step 5: benchmark the fix with timeit and load-test on staging to confirm P99 improvement before deploying.","correct":"Step 1: attach py-spy to the production worker to get a flamegraph without disruption — identify the top frame.","distractors":["attrgetter is implemented in C and performs attribute access without creating a Python function frame per comparison.","Use py-spy when you need to profile a running production process without modifying code or restarting it, and when you cannot afford >1% latency overhead.","LOAD_GLOBAL performs a dictionary lookup in globals() and falls through to builtins if not found — two dict operations per access."]},{"id":"python/performance_and_profiling#Q11-2546","section":"python","module":"python/performance_and_profiling","moduleName":"performance and profiling","qIndex":11,"difficulty":"advanced","question":"What does the dis module reveal about x += 1 for a global vs local variable, and what is the opcode count difference?","answerFull":"For a local variable, x += 1 compiles to LOAD_FAST x, LOAD_CONST 1, BINARY_OP +=, STORE_FAST x — 4 opcodes, 2 of which are fast array operations. For a global variable, it compiles to LOAD_GLOBAL x, LOAD_CONST 1, BINARY_OP +=, STORE_GLOBAL x — 4 opcodes but LOAD_GLOBAL and STORE_GLOBAL each perform a dictionary operation. In a loop executing 10 million iterations, this difference accounts for approximately 15-20% slower execution.","correct":"For a local variable, x += 1 compiles to LOAD_FAST x, LOAD_CONST 1, BINARY_OP +=, STORE_FAST x — 4 opcodes, 2 of which are fast array operations.","distractors":["Call dis.dis(original_func) and dis.dis(optimized_func), count the number of opcodes in the hot section, and confirm that the optimized version has fewer or cheaper opcodes.","LOAD_GLOBAL performs a dictionary lookup in globals() and falls through to builtins if not found — two dict operations per access.","tottime is time spent inside a function excluding callees; cumtime is total time including all nested calls."]},{"id":"python/performance_and_profiling#Q12-2547","section":"python","module":"python/performance_and_profiling","moduleName":"performance and profiling","qIndex":12,"difficulty":"advanced","question":"How does the join() pattern for string building avoid the O(n^2) behavior of + concatenation?","answerFull":"Python strings are immutable. Each s += fragment allocates a new string object of length len(s) + len(fragment), copies both strings into it, and discards the old s. Over n fragments, total bytes copied sum to 0 + 1 + 2 + ... + n = O(n^2). \"\".join(fragments) first computes the total length in one pass, allocates one buffer of that size, then copies each fragment once — O(n) total bytes copied. For 1,000 strings of average length 10, join is approximately 10x faster and eliminates quadratic memory allocation.","correct":"Python strings are immutable.","distractors":["attrgetter is implemented in C and performs attribute access without creating a Python function frame per comparison.","tottime is time spent inside a function excluding callees; cumtime is total time including all nested calls.","LOAD_GLOBAL performs a dictionary lookup in globals() and falls through to builtins if not found — two dict operations per access."]},{"id":"python/stdlib_datetime_and_logging#Q1-2548","section":"python","module":"python/stdlib_datetime_and_logging","moduleName":"stdlib datetime and logging","qIndex":1,"difficulty":"core","question":"What is the difference between a naive and an aware datetime in Python, and why does it matter in production?","answerFull":"A naive datetime has tzinfo=None; it represents a \"floating\" local time with no timezone context. An aware datetime has a tzinfo object attached; it represents an unambiguous instant on the UTC timeline. In production, storing naive datetimes in a database and comparing them with aware datetimes raises TypeError. More subtly, datetime.now().timestamp() returns the Unix epoch relative to the local machine's timezone, producing wrong results when the service runs on UTC-configured servers versus developer laptops in UTC-5.","correct":"A naive datetime has tzinfo=None; it represents a \"floating\" local time with no timezone context.","distractors":["basicConfig() is idempotent — it is a no-op if the root logger already has handlers.","timezone(timedelta(hours=-5)) is a fixed-offset timezone that never changes.","naive_dt.timestamp() assumes the naive datetime is in the local system timezone and converts to Unix epoch accordingly."]},{"id":"python/stdlib_datetime_and_logging#Q2-2549","section":"python","module":"python/stdlib_datetime_and_logging","moduleName":"stdlib datetime and logging","qIndex":2,"difficulty":"core","question":"How does zoneinfo.ZoneInfo differ from datetime.timezone(timedelta(hours=-5))?","answerFull":"timezone(timedelta(hours=-5)) is a fixed-offset timezone that never changes. It does not know about daylight saving time. ZoneInfo(\"America/New_York\") [3.9] loads the IANA timezone database and correctly applies EST (-5) in winter and EDT (-4) in summer, including the exact transition moments. Always use named ZoneInfo timezones for wall-clock scheduling and fixed offsets only when you truly mean a fixed UTC offset.","correct":"timezone(timedelta(hours=-5)) is a fixed-offset timezone that never changes.","distractors":["A naive datetime has tzinfo=None; it represents a \"floating\" local time with no timezone context.","naive_dt.timestamp() assumes the naive datetime is in the local system timezone and converts to Unix epoch accordingly.","logging.getLogger(\"app.api.users\") creates (or retrieves) a logger whose parent is app.api, whose parent is app, whose parent is the root logger."]},{"id":"python/stdlib_datetime_and_logging#Q3-2550","section":"python","module":"python/stdlib_datetime_and_logging","moduleName":"stdlib datetime and logging","qIndex":3,"difficulty":"core","question":"Describe the logging hierarchy and how propagation works.","answerFull":"logging.getLogger(\"app.api.users\") creates (or retrieves) a logger whose parent is app.api, whose parent is app, whose parent is the root logger. When a record is emitted, the logger checks its own level; if the record passes, it is passed to each handler on that logger, then the record walks up to the parent logger and repeats — unless propagate=False is set on a logger in the chain. This means you can attach a single JSON handler to the \"app\" logger and all child loggers automatically use it.","correct":"logging.getLogger(\"app.api.users\") creates (or retrieves) a logger whose parent is app.api, whose parent is app, whose parent is the root logger.","distractors":["basicConfig() is idempotent — it is a no-op if the root logger already has handlers.","logger.isEnabledFor(logging.DEBUG) returns True only if the effective level of the logger is DEBUG or lower.","Duplicate lines almost always mean a handler is attached at multiple levels of the logger hierarchy while propagation is still enabled."]},{"id":"python/stdlib_datetime_and_logging#Q4-2551","section":"python","module":"python/stdlib_datetime_and_logging","moduleName":"stdlib datetime and logging","qIndex":4,"difficulty":"core","question":"Why should libraries never call logging.basicConfig()?","answerFull":"basicConfig() is idempotent — it is a no-op if the root logger already has handlers. If a library calls it first, it installs a plain-text StreamHandler on the root logger before the application has a chance to configure JSON handlers or file handlers. Subsequent basicConfig() or dictConfig() calls from the application may then find the root logger already configured and fail silently or produce duplicate plain-text lines. Library code should only call logging.getLogger(name) and let applications own the root configuration.","correct":"basicConfig() is idempotent — it is a no-op if the root logger already has handlers.","distractors":["Call parser.add_subparsers(dest=\"command\", required=True) to create a subparser group.","Duplicate lines almost always mean a handler is attached at multiple levels of the logger hierarchy while propagation is still enabled.","A naive datetime has tzinfo=None; it represents a \"floating\" local time with no timezone context."]},{"id":"python/stdlib_datetime_and_logging#Q5-2552","section":"python","module":"python/stdlib_datetime_and_logging","moduleName":"stdlib datetime and logging","qIndex":5,"difficulty":"core","question":"How do you inject per-request context (such as request_id) into every log line in a FastAPI service?","answerFull":"Use contextvars.ContextVar to store the request_id for the current async task context. Create a logging.LoggerAdapter subclass whose process() method reads from the ContextVar and injects it into extra. In a FastAPI HTTP middleware, set the ContextVar at the start of each request and reset it in a finally block. Because asyncio copies the context for each task, concurrent requests do not bleed request_id into each other.","correct":"Use contextvars.ContextVar to store the request_id for the current async task context.","distractors":["A naive datetime has tzinfo=None; it represents a \"floating\" local time with no timezone context.","timezone(timedelta(hours=-5)) is a fixed-offset timezone that never changes.","logging.getLogger(\"app.api.users\") creates (or retrieves) a logger whose parent is app.api, whose parent is app, whose parent is the root logger."]},{"id":"python/stdlib_datetime_and_logging#Q6-2553","section":"python","module":"python/stdlib_datetime_and_logging","moduleName":"stdlib datetime and logging","qIndex":6,"difficulty":"intermediate","question":"What does check=True do in subprocess.run() and when would you omit it?","answerFull":"check=True raises subprocess.CalledProcessError if the child process exits with a non-zero return code. Omit it only when you explicitly want to inspect result.returncode yourself — for example, when you run a linter and want to distinguish exit code 0 (no issues), 1 (issues found), and 2 (fatal error). In all other cases, check=True prevents silently swallowing failures.","correct":"check=True raises subprocess.CalledProcessError if the child process exits with a non-zero return code.","distractors":["With shell=True, the command string is passed to /bin/sh -c \"...\".","Call parser.add_subparsers(dest=\"command\", required=True) to create a subparser group.","Use asyncio.create_subprocess_exec(*cmd, stdout=asyncio.subprocess.PIPE) to get an asyncio.subprocess.Process."]},{"id":"python/stdlib_datetime_and_logging#Q7-2554","section":"python","module":"python/stdlib_datetime_and_logging","moduleName":"stdlib datetime and logging","qIndex":7,"difficulty":"intermediate","question":"Why is shell=True dangerous, and what is the correct alternative?","answerFull":"With shell=True, the command string is passed to /bin/sh -c \"...\". The shell interprets metacharacters: semicolons, backticks, $(), pipes, and redirects. If any part of the command includes user-supplied data, an attacker can inject arbitrary shell commands. The fix is to pass arguments as a Python list to subprocess.run(): the OS calls execve directly with the list elements as argv, so no shell is involved and no metacharacter interpretation occurs.","correct":"With shell=True, the command string is passed to /bin/sh -c \"...\".","distractors":["check=True raises subprocess.CalledProcessError if the child process exits with a non-zero return code.","logger.isEnabledFor(logging.DEBUG) returns True only if the effective level of the logger is DEBUG or lower.","Call parser.add_subparsers(dest=\"command\", required=True) to create a subparser group."]},{"id":"python/stdlib_datetime_and_logging#Q8-2555","section":"python","module":"python/stdlib_datetime_and_logging","moduleName":"stdlib datetime and logging","qIndex":8,"difficulty":"intermediate","question":"How do you stream subprocess output in an async FastAPI endpoint?","answerFull":"Use asyncio.create_subprocess_exec(*cmd, stdout=asyncio.subprocess.PIPE) to get an asyncio.subprocess.Process. Then iterate async for line in proc.stdout: to yield lines as they arrive without blocking the event loop. Use a StreamingResponse with an async generator in FastAPI to push those lines to the client over HTTP chunked transfer encoding.","correct":"Use asyncio.create_subprocess_exec(*cmd, stdout=asyncio.subprocess.PIPE) to get an asyncio.subprocess.Process.","distractors":["Call parser.add_subparsers(dest=\"command\", required=True) to create a subparser group.","Use contextvars.ContextVar to store the request_id for the current async task context.","check=True raises subprocess.CalledProcessError if the child process exits with a non-zero return code."]},{"id":"python/stdlib_datetime_and_logging#Q9-2556","section":"python","module":"python/stdlib_datetime_and_logging","moduleName":"stdlib datetime and logging","qIndex":9,"difficulty":"intermediate","question":"How do you add a subcommand (like git commit or git push) to an argparse parser?","answerFull":"Call parser.add_subparsers(dest=\"command\", required=True) to create a subparser group. Then call subparsers.add_parser(\"commit\") and subparsers.add_parser(\"push\") to register each subcommand, optionally adding their own arguments. After args = parser.parse_args(), branch on args.command. Setting required=True ensures the parser exits with a usage error if no subcommand is given.","correct":"Call parser.add_subparsers(dest=\"command\", required=True) to create a subparser group.","distractors":["Use asyncio.create_subprocess_exec(*cmd, stdout=asyncio.subprocess.PIPE) to get an asyncio.subprocess.Process.","logger.isEnabledFor(logging.DEBUG) returns True only if the effective level of the logger is DEBUG or lower.","With shell=True, the command string is passed to /bin/sh -c \"...\"."]},{"id":"python/stdlib_datetime_and_logging#Q10-2557","section":"python","module":"python/stdlib_datetime_and_logging","moduleName":"stdlib datetime and logging","qIndex":10,"difficulty":"intermediate","question":"What is the isEnabledFor pattern and when is it critical?","answerFull":"logger.isEnabledFor(logging.DEBUG) returns True only if the effective level of the logger is DEBUG or lower. Without this guard, any expression used to construct the log message — f-strings, repr() calls, JSON serialization — is evaluated unconditionally before logger.debug() decides whether to emit the record. In a tight loop processing thousands of items per second, this constant evaluation can add measurable CPU overhead. The guard makes the expensive work conditional on the level actually being active.","correct":"logger.isEnabledFor(logging.DEBUG) returns True only if the effective level of the logger is DEBUG or lower.","distractors":["With shell=True, the command string is passed to /bin/sh -c \"...\".","basicConfig() is idempotent — it is a no-op if the root logger already has handlers.","logging.getLogger(\"app.api.users\") creates (or retrieves) a logger whose parent is app.api, whose parent is app, whose parent is the root logger."]},{"id":"python/stdlib_datetime_and_logging#Q11-2558","section":"python","module":"python/stdlib_datetime_and_logging","moduleName":"stdlib datetime and logging","qIndex":11,"difficulty":"advanced","question":"How does datetime.timestamp() behave on a naive datetime, and what is the safer alternative?","answerFull":"naive_dt.timestamp() assumes the naive datetime is in the local system timezone and converts to Unix epoch accordingly. If the server is in UTC and the developer's machine is in UTC-5, the same naive datetime produces different epoch values. The safe alternative is to always work with aware datetimes: aware_dt.timestamp() uses the attached tzinfo and produces a deterministic result regardless of the system timezone.","correct":"naive_dt.timestamp() assumes the naive datetime is in the local system timezone and converts to Unix epoch accordingly.","distractors":["A naive datetime has tzinfo=None; it represents a \"floating\" local time with no timezone context.","timezone(timedelta(hours=-5)) is a fixed-offset timezone that never changes.","logging.getLogger(\"app.api.users\") creates (or retrieves) a logger whose parent is app.api, whose parent is app, whose parent is the root logger."]},{"id":"python/stdlib_datetime_and_logging#Q12-2559","section":"python","module":"python/stdlib_datetime_and_logging","moduleName":"stdlib datetime and logging","qIndex":12,"difficulty":"advanced","question":"How do you prevent log records from being emitted multiple times (duplicate log lines)?","answerFull":"Duplicate lines almost always mean a handler is attached at multiple levels of the logger hierarchy while propagation is still enabled. Fix: attach handlers to exactly one logger (typically \"app\" or the root), and set propagate=False on any logger that has its own handlers to prevent the record from walking up to a parent that also has a handler. Alternatively, attach handlers only to the root logger and rely entirely on propagation.","correct":"Duplicate lines almost always mean a handler is attached at multiple levels of the logger hierarchy while propagation is still enabled.","distractors":["basicConfig() is idempotent — it is a no-op if the root logger already has handlers.","logging.getLogger(\"app.api.users\") creates (or retrieves) a logger whose parent is app.api, whose parent is app, whose parent is the root logger.","logger.isEnabledFor(logging.DEBUG) returns True only if the effective level of the logger is DEBUG or lower."]},{"id":"python/collections_and_data_structures#Q1-2560","section":"python","module":"python/collections_and_data_structures","moduleName":"collections and data structures","qIndex":1,"difficulty":"core","question":"What is the time complexity of list.append() and why is it amortized O(1) rather than O(1)?","answerFull":"list.append() is O(1) amortized because CPython occasionally performs an O(n) reallocation when the underlying array is full. The reallocation doubles (approximately) the allocated capacity, so the total cost of n appends is O(n), giving O(1) per append on average. The growth factor is approximately 1.125 for large lists (8 + allocated // 8 new slots in CPython source). In practice, you see a realloc roughly at sizes 1, 5, 9, 17, 25, ... Never use list.insert(0, x) in a loop — each insert is O(n) with no amortization.","correct":"list.append() is O(1) amortized because CPython occasionally performs an O(n) reallocation when the underlying array is full.","distractors":["CPython 3.6 introduced a compact dict representation using two parallel arrays: a sparse index array (maps hash slot to entry index) and a dense entries array (stores key, value, hash in insertion order).","Use deque whenever you remove elements from the front.","Counter subtraction (a - b) keeps only positive counts and silently drops keys with zero or negative results."]},{"id":"python/collections_and_data_structures#Q2-2561","section":"python","module":"python/collections_and_data_structures","moduleName":"collections and data structures","qIndex":2,"difficulty":"core","question":"How does Python 3.7+ guarantee dict insertion order, and does this affect performance?","answerFull":"CPython 3.6 introduced a compact dict representation using two parallel arrays: a sparse index array (maps hash slot to entry index) and a dense entries array (stores key, value, hash in insertion order). Insertion order follows the entries array, which is always appended to. Python 3.7 made this ordering part of the language specification. The performance impact is negligible — the index array lookup is O(1) and the extra memory per dict is fixed overhead. Compact dicts use 20–25% less memory than the old combined-table layout.","correct":"CPython 3.6 introduced a compact dict representation using two parallel arrays: a sparse index array (maps hash slot to entry index) and a dense entries array (stores key, value, hash in insertion order).","distractors":["list.append() is O(1) amortized because CPython occasionally performs an O(n) reallocation when the underlying array is full.","Accessing d[key] on a defaultdict when key is missing triggers default_factory() and stores the result, creating the key as a side effect.","set uses an open-addressing hash table identical in structure to dict, but stores only keys."]},{"id":"python/collections_and_data_structures#Q3-2562","section":"python","module":"python/collections_and_data_structures","moduleName":"collections and data structures","qIndex":3,"difficulty":"core","question":"When should you use deque instead of list as a queue?","answerFull":"Use deque whenever you remove elements from the front. list.pop(0) is O(n) because it shifts all remaining elements one position left in the underlying C array. deque.popleft() is O(1) because the deque is a doubly-linked structure of fixed blocks — removing from the front updates a pointer, not n elements. For BFS, task queues, and any FIFO pattern with frequent front removals, deque is the correct choice. Use list only for stacks (append + pop from the same end), where both operations are O(1).","correct":"Use deque whenever you remove elements from the front.","distractors":["namedtuple generates an immutable tuple subclass — elements are stored in a C tuple (contiguous memory, no per-field overhead).","list.append() is O(1) amortized because CPython occasionally performs an O(n) reallocation when the underlying array is full.","CPython 3.6 introduced a compact dict representation using two parallel arrays: a sparse index array (maps hash slot to entry index) and a dense entries array (stores key, value, hash in insertion order)."]},{"id":"python/collections_and_data_structures#Q4-2563","section":"python","module":"python/collections_and_data_structures","moduleName":"collections and data structures","qIndex":4,"difficulty":"core","question":"What is the difference between Counter subtraction and regular dict subtraction?","answerFull":"Counter subtraction (a - b) keeps only positive counts and silently drops keys with zero or negative results. Regular dict has no subtraction operator. Counter also overloads + (union, keeps positives), & (intersection, takes min), and | (union, takes max). These make Counter a true multiset implementation. Note: Counter.update() adds counts; Counter.subtract() subtracts but keeps zero and negative counts (unlike the - operator which drops them).","correct":"Counter subtraction (a - b) keeps only positive counts and silently drops keys with zero or negative results.","distractors":["set uses an open-addressing hash table identical in structure to dict, but stores only keys.","CPython 3.6 introduced a compact dict representation using two parallel arrays: a sparse index array (maps hash slot to entry index) and a dense entries array (stores key, value, hash in insertion order).","list.append() is O(1) amortized because CPython occasionally performs an O(n) reallocation when the underlying array is full."]},{"id":"python/collections_and_data_structures#Q5-2564","section":"python","module":"python/collections_and_data_structures","moduleName":"collections and data structures","qIndex":5,"difficulty":"core","question":"Explain the gotcha with defaultdict and key existence checks.","answerFull":"Accessing d[key] on a defaultdict when key is missing triggers default_factory() and stores the result, creating the key as a side effect. This means if d[key] silently creates key → default_value in the dict even if your intent was a read-only existence check. Use d.get(key) (returns None without creating) or key in d for existence checks without side effects. This bug commonly appears in cache-hit detection or graph-membership tests where spurious empty entries corrupt the data structure's logical state.","correct":"Accessing d[key] on a defaultdict when key is missing triggers default_factory() and stores the result, creating the key as a side effect.","distractors":["CPython 3.6 introduced a compact dict representation using two parallel arrays: a sparse index array (maps hash slot to entry index) and a dense entries array (stores key, value, hash in insertion order).","set uses an open-addressing hash table identical in structure to dict, but stores only keys.","list.append() is O(1) amortized because CPython occasionally performs an O(n) reallocation when the underlying array is full."]},{"id":"python/collections_and_data_structures#Q6-2565","section":"python","module":"python/collections_and_data_structures","moduleName":"collections and data structures","qIndex":6,"difficulty":"intermediate","question":"What is heapq.heapify()'s time complexity and why is it O(n) rather than O(n log n)?","answerFull":"heapify() is O(n) because it uses a bottom-up algorithm: it starts at the last non-leaf node (n//2 - 1) and sifts down each node. Leaf nodes (half the tree) require no work. Non-leaf nodes at depth d from the bottom require at most d sifts. Summing across all levels, the total work is bounded by the sum of a geometric series: O(n). Calling heappush() n times would be O(n log n). The distinction matters when building a heap from a large existing collection — always use heapify().","correct":"heapify() is O(n) because it uses a bottom-up algorithm: it starts at the last non-leaf node (n//2 - 1) and sifts down each node.","distractors":["heapq.nlargest(k, iterable) is O(n log k); sorted(iterable)[-k:] is O(n log n).","list.append() is O(1) amortized because CPython occasionally performs an O(n) reallocation when the underlying array is full.","CPython 3.6 introduced a compact dict representation using two parallel arrays: a sparse index array (maps hash slot to entry index) and a dense entries array (stores key, value, hash in insertion order)."]},{"id":"python/collections_and_data_structures#Q7-2566","section":"python","module":"python/collections_and_data_structures","moduleName":"collections and data structures","qIndex":7,"difficulty":"intermediate","question":"How does frozenset differ from set, and when would you use it as a dict key?","answerFull":"frozenset is an immutable, hashable version of set. Because it is immutable, Python can compute its hash once and cache it. Use frozenset as a dict key when your key represents a group of items where order does not matter — for example, caching results of a query over a set of user IDs: cache[frozenset(user_ids)] = result. You cannot add or remove elements from a frozenset after creation. frozenset supports all set read operations: in, issubset, union, intersection, but not add, remove, or discard.","correct":"frozenset is an immutable, hashable version of set.","distractors":["CPython 3.6 introduced a compact dict representation using two parallel arrays: a sparse index array (maps hash slot to entry index) and a dense entries array (stores key, value, hash in insertion order).","Accessing d[key] on a defaultdict when key is missing triggers default_factory() and stores the result, creating the key as a side effect.","namedtuple generates an immutable tuple subclass — elements are stored in a C tuple (contiguous memory, no per-field overhead)."]},{"id":"python/collections_and_data_structures#Q8-2567","section":"python","module":"python/collections_and_data_structures","moduleName":"collections and data structures","qIndex":8,"difficulty":"intermediate","question":"What is the difference between bisect_left and bisect_right?","answerFull":"Both functions return an insertion index in a sorted list, but they differ in handling duplicates. bisect_left(a, x) returns the leftmost index where x can be inserted without violating sort order — equal elements are to the right of the insertion point. bisect_right(a, x) (also called bisect) returns the rightmost such index — equal elements are to the left. Use bisect_left when you want to find the first occurrence of x; use bisect_right when you want to insert after all existing equal elements. Example: a = [1,2,2,3]; bisect_left(a,2)=1, bisect_right(a,2)=3.","correct":"Both functions return an insertion index in a sorted list, but they differ in handling duplicates.","distractors":["heapq.nlargest(k, iterable) is O(n log k); sorted(iterable)[-k:] is O(n log n).","list.append() is O(1) amortized because CPython occasionally performs an O(n) reallocation when the underlying array is full.","CPython 3.6 introduced a compact dict representation using two parallel arrays: a sparse index array (maps hash slot to entry index) and a dense entries array (stores key, value, hash in insertion order)."]},{"id":"python/collections_and_data_structures#Q9-2568","section":"python","module":"python/collections_and_data_structures","moduleName":"collections and data structures","qIndex":9,"difficulty":"intermediate","question":"Describe the internal structure of a Python set. How does it handle collisions?","answerFull":"set uses an open-addressing hash table identical in structure to dict, but stores only keys. Each slot in the table is either empty, a deleted tombstone, or a live entry. Collision resolution uses a perturbation-based probe sequence: slot = (5*slot + 1 + perturb) & mask, where perturb starts as the full hash value and is right-shifted by 5 each probe. This mixes high-order bits of the hash into the probe sequence, producing pseudo-random probing that avoids long chains. The table resizes (doubles) when load factor exceeds ~2/3.","correct":"set uses an open-addressing hash table identical in structure to dict, but stores only keys.","distractors":["Accessing d[key] on a defaultdict when key is missing triggers default_factory() and stores the result, creating the key as a side effect.","CPython 3.6 introduced a compact dict representation using two parallel arrays: a sparse index array (maps hash slot to entry index) and a dense entries array (stores key, value, hash in insertion order).","Counter subtraction (a - b) keeps only positive counts and silently drops keys with zero or negative results."]},{"id":"python/collections_and_data_structures#Q10-2569","section":"python","module":"python/collections_and_data_structures","moduleName":"collections and data structures","qIndex":10,"difficulty":"intermediate","question":"When is sorted() + list better than heapq for top-K queries?","answerFull":"heapq.nlargest(k, iterable) is O(n log k); sorted(iterable)[-k:] is O(n log n). For small k relative to n (e.g., top 10 from 1 million), heapq.nlargest is significantly faster. For large k (e.g., top half of n), sorted() is faster because its constant factor is lower and the log n term becomes comparable to log k. CPython's heapq.nlargest implementation switches to sorted() automatically when k is large relative to n. For k=1, use max() — O(n) with minimal overhead.","correct":"heapq.nlargest(k, iterable) is O(n log k); sorted(iterable)[-k:] is O(n log n).","distractors":["list.append() is O(1) amortized because CPython occasionally performs an O(n) reallocation when the underlying array is full.","CPython 3.6 introduced a compact dict representation using two parallel arrays: a sparse index array (maps hash slot to entry index) and a dense entries array (stores key, value, hash in insertion order).","Use deque whenever you remove elements from the front."]},{"id":"python/collections_and_data_structures#Q11-2570","section":"python","module":"python/collections_and_data_structures","moduleName":"collections and data structures","qIndex":11,"difficulty":"advanced","question":"What is ChainMap and how does it differ from merging dicts?","answerFull":"ChainMap groups multiple dicts into a single view without copying data. Lookups search the maps in order and return the first hit. Writes go to the first (primary) map only. Merging with {d1, d2} or d1 | d2 (Python 3.9+) creates a new dict — O(n) time and memory. ChainMap(d1, d2) is O(1) to create and O(k) per lookup where k is the number of maps. The primary use case is layered configuration: ChainMap(cli_args, env_vars, config_file, defaults) — each layer overrides the next without mutation.","correct":"ChainMap groups multiple dicts into a single view without copying data.","distractors":["list.append() is O(1) amortized because CPython occasionally performs an O(n) reallocation when the underlying array is full.","CPython 3.6 introduced a compact dict representation using two parallel arrays: a sparse index array (maps hash slot to entry index) and a dense entries array (stores key, value, hash in insertion order).","Use deque whenever you remove elements from the front."]},{"id":"python/collections_and_data_structures#Q12-2571","section":"python","module":"python/collections_and_data_structures","moduleName":"collections and data structures","qIndex":12,"difficulty":"advanced","question":"How does namedtuple compare to @dataclass for lightweight records?","answerFull":"namedtuple generates an immutable tuple subclass — elements are stored in a C tuple (contiguous memory, no per-field overhead). Access by name is O(1) via index. @dataclass generates a regular Python class with instance dict — mutable by default, more overhead per instance. Use namedtuple when: immutability is required, you want tuple compatibility (unpacking, CSV row mapping), or memory is critical (millions of instances). Use @dataclass when: you need mutability, default values with complex expressions, post_init, validators, or inheritance. Python 3.10+ @dataclass(slots=True) closes much of the memory gap by using slots instead of dict.","correct":"namedtuple generates an immutable tuple subclass — elements are stored in a C tuple (contiguous memory, no per-field overhead).","distractors":["frozenset is an immutable, hashable version of set.","Use deque whenever you remove elements from the front.","list.append() is O(1) amortized because CPython occasionally performs an O(n) reallocation when the underlying array is full."]},{"id":"python/data_model_and_objects#Q1-2572","section":"python","module":"python/data_model_and_objects","moduleName":"data model and objects","qIndex":1,"difficulty":"core","question":"What is a data descriptor versus a non-data descriptor, and why does the distinction matter for attribute lookup?","answerFull":"A data descriptor defines both get and at least one of set or delete; a non-data descriptor defines only get. Data descriptors take priority over the instance dict, while non-data descriptors are shadowed by it. This distinction is why property (a data descriptor) prevents instance dictionary bypass but plain functions (non-data descriptors) can be overridden per-instance.","correct":"A data descriptor defines both get and at least one of set or delete; a non-data descriptor defines only get.","distractors":["getattribute is called on *every* attribute access and is the entry point for the full lookup mechanism.","set_name(self, owner, name) [3.6] is called by type.new on each descriptor found in the class body, passing the class being created (owner) and the attribute name the descriptor is assigned to (name).","A function object's class defines get but not set or delete."]},{"id":"python/data_model_and_objects#Q2-2573","section":"python","module":"python/data_model_and_objects","moduleName":"data model and objects","qIndex":2,"difficulty":"core","question":"What happens to hash when you define eq on a class?","answerFull":"Python automatically sets hash = None on the class, making instances unhashable. This enforces the contract that equal objects must have equal hashes. To keep instances hashable, you must explicitly define hash returning a value consistent with eq, typically hash(tuple_of_fields_used_in_eq).","correct":"Python automatically sets hash = None on the class, making instances unhashable.","distractors":["super() returns a proxy that delegates method calls to the *next* class in the current instance's MRO, not necessarily the direct parent.","set_name(self, owner, name) [3.6] is called by type.new on each descriptor found in the class body, passing the class being created (owner) and the attribute name the descriptor is assigned to (name).","Python calls bool first; if absent, it calls len and treats 0 as falsy and non-zero as truthy; if neither is defined, the object is always truthy."]},{"id":"python/data_model_and_objects#Q3-2574","section":"python","module":"python/data_model_and_objects","moduleName":"data model and objects","qIndex":3,"difficulty":"core","question":"Why does repr use repr() on contained objects, but str uses str()?","answerFull":"repr is for developer-facing output and should be unambiguous; when a container like list renders its elements, it calls repr() on each to get the unambiguous form. str is for end-user display and may omit type information. Implement repr on every class; str is optional and falls back to repr if absent.","correct":"repr is for developer-facing output and should be unambiguous; when a container like list renders its elements, it calls repr() on each to get the unambiguous form.","distractors":["A data descriptor defines both get and at least one of set or delete; a non-data descriptor defines only get.","Python calls bool first; if absent, it calls len and treats 0 as falsy and non-zero as truthy; if neither is defined, the object is always truthy.","A function object's class defines get but not set or delete."]},{"id":"python/data_model_and_objects#Q4-2575","section":"python","module":"python/data_model_and_objects","moduleName":"data model and objects","qIndex":4,"difficulty":"core","question":"Explain how super() works in Python's MRO and why you must use it in cooperative multiple inheritance.","answerFull":"super() returns a proxy that delegates method calls to the *next* class in the current instance's MRO, not necessarily the direct parent. In a diamond hierarchy D(B, C) with MRO D → B → C → A, super() in B.method calls C.method, not A.method. If B calls A.method() directly, C.method is skipped entirely. Every class must call super() to guarantee cooperative chaining where each class in the MRO runs exactly once.","correct":"super() returns a proxy that delegates method calls to the *next* class in the current instance's MRO, not necessarily the direct parent.","distractors":["repr is for developer-facing output and should be unambiguous; when a container like list renders its elements, it calls repr() on each to get the unambiguous form.","Python calls bool first; if absent, it calls len and treats 0 as falsy and non-zero as truthy; if neither is defined, the object is always truthy.","functools.total_ordering is a class decorator that fills in missing rich comparison methods from eq and one ordering method."]},{"id":"python/data_model_and_objects#Q5-2576","section":"python","module":"python/data_model_and_objects","moduleName":"data model and objects","qIndex":5,"difficulty":"core","question":"How does slots reduce memory, and when does it fail to save memory?","answerFull":"slots replaces the per-instance dict (a hash table costing ~184 bytes in CPython 3.11) with a fixed C-level array of slot pointers (~8 bytes per slot). For a 3-attribute object, total size drops from ~232 to ~56 bytes. It fails to save memory when any class in the MRO does not define slots, because that ancestor still contributes a dict to every instance.","correct":"slots replaces the per-instance dict (a hash table costing ~184 bytes in CPython 3.11) with a fixed C-level array of slot pointers (~8 bytes per slot).","distractors":["Python automatically sets hash = None on the class, making instances unhashable.","super() returns a proxy that delegates method calls to the *next* class in the current instance's MRO, not necessarily the direct parent.","A data descriptor defines both get and at least one of set or delete; a non-data descriptor defines only get."]},{"id":"python/data_model_and_objects#Q6-2577","section":"python","module":"python/data_model_and_objects","moduleName":"data model and objects","qIndex":6,"difficulty":"intermediate","question":"What is the attribute lookup order in Python?","answerFull":"For obj.attr: (1) check if type(obj).mro contains a data descriptor named attr; if yes, call its get. (2) Check obj.dict for attr; if found, return it. (3) Check type(obj).mro for a non-data descriptor or plain class attribute named attr; if found, call get or return the value. (4) Raise AttributeError. This four-step order is fixed and implemented in object.getattribute.","correct":"For obj.attr: (1) check if type(obj).mro contains a data descriptor named attr; if yes, call its get.","distractors":["Python automatically sets hash = None on the class, making instances unhashable.","set_name(self, owner, name) [3.6] is called by type.new on each descriptor found in the class body, passing the class being created (owner) and the attribute name the descriptor is assigned to (name).","super() returns a proxy that delegates method calls to the *next* class in the current instance's MRO, not necessarily the direct parent."]},{"id":"python/data_model_and_objects#Q7-2578","section":"python","module":"python/data_model_and_objects","moduleName":"data model and objects","qIndex":7,"difficulty":"intermediate","question":"What does returning NotImplemented from add do, and how is it different from raising NotImplementedError?","answerFull":"Returning NotImplemented (a singleton, not an exception) tells Python that the current type cannot handle the operand, so Python should try the reflected method (radd) on the right-hand operand. Raising NotImplementedError is an unrecoverable exception that immediately propagates. Always return NotImplemented from numeric dunders for unsupported types; never raise NotImplementedError.","correct":"Returning NotImplemented (a singleton, not an exception) tells Python that the current type cannot handle the operand, so Python should try the reflected method (radd) on the right-hand operand.","distractors":["functools.total_ordering is a class decorator that fills in missing rich comparison methods from eq and one ordering method.","Python automatically sets hash = None on the class, making instances unhashable.","super() returns a proxy that delegates method calls to the *next* class in the current instance's MRO, not necessarily the direct parent."]},{"id":"python/data_model_and_objects#Q8-2579","section":"python","module":"python/data_model_and_objects","moduleName":"data model and objects","qIndex":8,"difficulty":"intermediate","question":"How does bool interact with len for truthiness testing?","answerFull":"Python calls bool first; if absent, it calls len and treats 0 as falsy and non-zero as truthy; if neither is defined, the object is always truthy. A common bug is defining len on a container without bool, then seeing empty containers evaluate as falsy — usually correct, but if bool has different semantics (e.g., a matrix is never \"empty\"), you must define it explicitly.","correct":"Python calls bool first; if absent, it calls len and treats 0 as falsy and non-zero as truthy; if neither is defined, the object is always truthy.","distractors":["A data descriptor defines both get and at least one of set or delete; a non-data descriptor defines only get.","Python raises ValueError: mutable default <class 'list'> for field items is not allowed: use default_factory at class definition time.","repr is for developer-facing output and should be unambiguous; when a container like list renders its elements, it calls repr() on each to get the unambiguous form."]},{"id":"python/data_model_and_objects#Q9-2580","section":"python","module":"python/data_model_and_objects","moduleName":"data model and objects","qIndex":9,"difficulty":"intermediate","question":"Explain the set_name hook on descriptors.","answerFull":"set_name(self, owner, name) [3.6] is called by type.new on each descriptor found in the class body, passing the class being created (owner) and the attribute name the descriptor is assigned to (name). This allows a descriptor to self-configure with its attribute name without requiring the programmer to pass it explicitly, eliminating the repetition of width = Validator(\"width\").","correct":"set_name(self, owner, name) [3.6] is called by type.new on each descriptor found in the class body, passing the class being created (owner) and the attribute name the descriptor is assigned to (name).","distractors":["A function object's class defines get but not set or delete.","Python automatically sets hash = None on the class, making instances unhashable.","A data descriptor defines both get and at least one of set or delete; a non-data descriptor defines only get."]},{"id":"python/data_model_and_objects#Q10-2581","section":"python","module":"python/data_model_and_objects","moduleName":"data model and objects","qIndex":10,"difficulty":"intermediate","question":"What is total_ordering and what is its performance cost?","answerFull":"functools.total_ordering is a class decorator that fills in missing rich comparison methods from eq and one ordering method. The generated methods add approximately 2 microseconds per comparison because they call the implemented method via a wrapper function and handle NotImplemented. For code that compares millions of objects (sorting large datasets), defining all six methods explicitly eliminates this overhead.","correct":"functools.total_ordering is a class decorator that fills in missing rich comparison methods from eq and one ordering method.","distractors":["super() returns a proxy that delegates method calls to the *next* class in the current instance's MRO, not necessarily the direct parent.","A function object's class defines get but not set or delete.","set_name(self, owner, name) [3.6] is called by type.new on each descriptor found in the class body, passing the class being created (owner) and the attribute name the descriptor is assigned to (name)."]},{"id":"python/data_model_and_objects#Q11-2582","section":"python","module":"python/data_model_and_objects","moduleName":"data model and objects","qIndex":11,"difficulty":"advanced","question":"How are functions non-data descriptors, and how does this enable bound methods?","answerFull":"A function object's class defines get but not set or delete. When you access instance.method, Python calls function.get(instance, type(instance)), which returns a method object that binds instance as the first argument. Because functions are non-data descriptors, an instance can shadow a method by setting an instance attribute with the same name — though this is rarely desirable.","correct":"A function object's class defines get but not set or delete.","distractors":["Python automatically sets hash = None on the class, making instances unhashable.","For obj.attr: (1) check if type(obj).mro contains a data descriptor named attr; if yes, call its get.","A data descriptor defines both get and at least one of set or delete; a non-data descriptor defines only get."]},{"id":"python/data_model_and_objects#Q12-2583","section":"python","module":"python/data_model_and_objects","moduleName":"data model and objects","qIndex":12,"difficulty":"advanced","question":"When would you use init_subclass instead of a metaclass?","answerFull":"init_subclass suffices when you need to run logic at subclass creation time without controlling the metaclass call chain or modifying new arguments. Use a metaclass when you need to intercept type.new itself — for example, to transform the class namespace before the class object is created (as Pydantic v2's ModelMetaclass does). init_subclass is simpler, composable via super(), and avoids metaclass conflicts.","correct":"init_subclass suffices when you need to run logic at subclass creation time without controlling the metaclass call chain or modifying new arguments.","distractors":["For obj.attr: (1) check if type(obj).mro contains a data descriptor named attr; if yes, call its get.","set_name(self, owner, name) [3.6] is called by type.new on each descriptor found in the class body, passing the class being created (owner) and the attribute name the descriptor is assigned to (name).","Python raises ValueError: mutable default <class 'list'> for field items is not allowed: use default_factory at class definition time."]},{"id":"python/data_model_and_objects#Q13-2584","section":"python","module":"python/data_model_and_objects","moduleName":"data model and objects","qIndex":13,"difficulty":"advanced","question":"What is the difference between getattr and getattribute?","answerFull":"getattribute is called on *every* attribute access and is the entry point for the full lookup mechanism. Overriding it lets you intercept all attribute reads. getattr is called only when the normal lookup (via getattribute) raises AttributeError — it is a fallback of last resort. Always prefer getattr for lazy/dynamic attributes; overriding getattribute risks infinite recursion if you accidentally look up attributes on self without calling object.getattribute.","correct":"getattribute is called on *every* attribute access and is the entry point for the full lookup mechanism.","distractors":["set_name(self, owner, name) [3.6] is called by type.new on each descriptor found in the class body, passing the class being created (owner) and the attribute name the descriptor is assigned to (name).","A data descriptor defines both get and at least one of set or delete; a non-data descriptor defines only get.","Python automatically sets hash = None on the class, making instances unhashable."]},{"id":"python/data_model_and_objects#Q14-2585","section":"python","module":"python/data_model_and_objects","moduleName":"data model and objects","qIndex":14,"difficulty":"advanced","question":"Can you add slots to a class that uses @dataclass?","answerFull":"Yes, with @dataclass(slots=True) [3.10]. The decorator creates a new class with slots populated from field annotations. For older Python versions (3.7–3.9), you must manually define slots on a @dataclass class — but because @dataclass writes dict-based access in generated init, you should verify slots takes effect with sys.getsizeof. The slots=True parameter is the correct approach from 3.10 onward.","correct":"Yes, with @dataclass(slots=True) [3.10].","distractors":["A function object's class defines get but not set or delete.","functools.total_ordering is a class decorator that fills in missing rich comparison methods from eq and one ordering method.","super() returns a proxy that delegates method calls to the *next* class in the current instance's MRO, not necessarily the direct parent."]},{"id":"python/data_model_and_objects#Q15-2586","section":"python","module":"python/data_model_and_objects","moduleName":"data model and objects","qIndex":15,"difficulty":"advanced","question":"What happens if you put a mutable default value as a dataclass field?","answerFull":"Python raises ValueError: mutable default <class 'list'> for field items is not allowed: use default_factory at class definition time. @dataclass protects against the mutable-default footgun by detecting list, dict, and set defaults and refusing them. Use field(default_factory=list) instead. This is enforced via post_init inspection and the Field descriptor machinery inside dataclasses.","correct":"Python raises ValueError: mutable default <class 'list'> for field items is not allowed: use default_factory at class definition time.","distractors":["Python automatically sets hash = None on the class, making instances unhashable.","Returning NotImplemented (a singleton, not an exception) tells Python that the current type cannot handle the operand, so Python should try the reflected method (radd) on the right-hand operand.","repr is for developer-facing output and should be unambiguous; when a container like list renders its elements, it calls repr() on each to get the unambiguous form."]},{"id":"python/the_gil_and_free_threading#Q1-2587","section":"python","module":"python/the_gil_and_free_threading","moduleName":"the gil and free threading","qIndex":1,"difficulty":"core","question":"What is the GIL and why does CPython have one?","answerFull":"The GIL is a mutex that ensures only one thread executes Python bytecode at a time inside a single CPython process. It was introduced because CPython uses reference counting for memory management, and incrementing/decrementing reference counts is not thread-safe without some form of locking. Adding fine-grained per-object locks was considered but would have been slower for single-threaded programs (which are the majority) because every object access would acquire a lock. The GIL is the pragmatic compromise: one global lock, fast single-threaded performance, limited multi-threaded CPU parallelism. In interviews, always frame the GIL as an implementation choice of CPython, not a Python language requirement.","correct":"The GIL is a mutex that ensures only one thread executes Python bytecode at a time inside a single CPython process.","distractors":["First, determine whether the work is CPU-bound or I/O-bound by running with time and observing CPU usage — if CPU is pinned at ~100% with 4 threads instead of ~400%, the GIL is the bottleneck.","threading.Lock is a simple non-reentrant mutex: a thread that already holds the lock and tries to acquire it again will deadlock.","It returns 0.005 (5 milliseconds), the maximum time a thread holds the GIL before being forced to release it and signal waiting threads."]},{"id":"python/the_gil_and_free_threading#Q3-2588","section":"python","module":"python/the_gil_and_free_threading","moduleName":"the gil and free threading","qIndex":3,"difficulty":"core","question":"Does the GIL make Python's built-in data structures thread-safe?","answerFull":"No, not for compound operations. Individual bytecode instructions are uninterruptible while the GIL is held, so a single list.append or a single dict.setitem is effectively atomic. But any multi-step operation — read-modify-write, check-then-act, iteration with mutation — is not atomic. The GIL can be released between the bytecodes of counter += 1 (three bytecodes: LOAD, ADD, STORE). Always use threading.Lock or threading.RLock for compound operations on shared mutable state.","correct":"No, not for compound operations.","distractors":["For single-key reads and writes (single bytecode operations) the GIL provides incidental atomicity in CPython — a d[key] read or d[key] = value write will not be partially executed.","It returns 0.005 (5 milliseconds), the maximum time a thread holds the GIL before being forced to release it and signal waiting threads.","PEP 703, accepted for Python 3.13, defines an optional GIL-free build of CPython (python3.13t)."]},{"id":"python/the_gil_and_free_threading#Q4-2589","section":"python","module":"python/the_gil_and_free_threading","moduleName":"the gil and free threading","qIndex":4,"difficulty":"core","question":"I have a CPU-bound program. Will using more threads speed it up?","answerFull":"No, and it will likely slow it down slightly. With the standard CPython GIL, adding threads to a CPU-bound program produces no parallel execution. Instead, threads spend time contending for the GIL — each thread runs for at most 5 ms before being forced to drop it, causing context switches and cache evictions. Benchmarks typically show 2-thread CPU programs running 10–20% slower than single-threaded. The fix is multiprocessing.Pool or ProcessPoolExecutor, which use separate processes with separate GILs.","correct":"No, and it will likely slow it down slightly.","distractors":["It returns 0.005 (5 milliseconds), the maximum time a thread holds the GIL before being forced to release it and signal waiting threads.","Use asyncio when you have hundreds or thousands of concurrent I/O-bound tasks and memory overhead matters — each coroutine costs ~1–2 KB of stack vs ~8 MB default for a threading.Thread.","Default to asyncio + async def handlers for I/O-bound work (database calls, external APIs, cache reads) — this covers 80% of web service workloads."]},{"id":"python/the_gil_and_free_threading#Q5-2590","section":"python","module":"python/the_gil_and_free_threading","moduleName":"the gil and free threading","qIndex":5,"difficulty":"core","question":"What is \"GIL thrashing\" and when does it occur?","answerFull":"GIL thrashing is when multiple threads spend more wall-clock time fighting to acquire the GIL than doing actual work. It is worst when threads execute short Python bursts interleaved with no real I/O — every 5 ms the current thread must give up the GIL, signal a waiting thread, and the new thread acquires it only to be interrupted again after 5 ms. The overhead of the mutex round-trip (signal, wake-up, context switch) can dominate. The symptom is CPU-bound programs running slower with more threads. The diagnosis is measuring wall time vs CPU time with time.perf_counter and resource.getrusage.","correct":"GIL thrashing is when multiple threads spend more wall-clock time fighting to acquire the GIL than doing actual work.","distractors":["First, determine whether the work is CPU-bound or I/O-bound by running with time and observing CPU usage — if CPU is pinned at ~100% with 4 threads instead of ~400%, the GIL is the bottleneck.","PEP 703, accepted for Python 3.13, defines an optional GIL-free build of CPython (python3.13t).","threading.Lock is a simple non-reentrant mutex: a thread that already holds the lock and tries to acquire it again will deadlock."]},{"id":"python/the_gil_and_free_threading#Q6-2591","section":"python","module":"python/the_gil_and_free_threading","moduleName":"the gil and free threading","qIndex":6,"difficulty":"intermediate","question":"How does NumPy achieve parallelism despite the GIL?","answerFull":"NumPy's heavy computational routines (matrix multiplication, FFT, sort) are implemented in C and BLAS libraries that call Py_BEGIN_ALLOW_THREADS before entering the compute block. While those C functions run, the GIL is free and other Python threads can execute bytecode. If you launch four threads each calling np.matmul on independent arrays, all four C-level computations can run simultaneously on four cores. The GIL is only re-acquired when returning Python objects. The same pattern applies to OpenCV, SciPy, pydantic-core (Rust via PyO3), and any C extension doing its own heavy work.","correct":"NumPy's heavy computational routines (matrix multiplication, FFT, sort) are implemented in C and BLAS libraries that call Py_BEGIN_ALLOW_THREADS before entering the compute block.","distractors":["Many C extensions check at import time whether the GIL is enabled and raise an error or automatically re-enable it if not.","GIL thrashing is when multiple threads spend more wall-clock time fighting to acquire the GIL than doing actual work.","PEP 703, accepted for Python 3.13, defines an optional GIL-free build of CPython (python3.13t)."]},{"id":"python/the_gil_and_free_threading#Q7-2592","section":"python","module":"python/the_gil_and_free_threading","moduleName":"the gil and free threading","qIndex":7,"difficulty":"intermediate","question":"What is PEP 703 and what problem does it solve?","answerFull":"PEP 703, accepted for Python 3.13, defines an optional GIL-free build of CPython (python3.13t). It solves the core problem: CPU-bound multi-threaded Python programs cannot use multiple cores. The implementation replaces the global reference count (which required the GIL for thread safety) with biased reference counting, where each object tracks a \"local\" refcount owned by the creating thread and a \"shared\" refcount updated with atomic operations by other threads. The GIL is removed; true parallel CPU execution is possible. The cost: all shared mutable state now requires explicit synchronisation, many C extensions that assumed the GIL will break or auto-re-enable it, and performance of single-threaded code is slightly lower due to atomic operations on the shared refcount shard.","correct":"PEP 703, accepted for Python 3.13, defines an optional GIL-free build of CPython (python3.13t).","distractors":["GIL thrashing is when multiple threads spend more wall-clock time fighting to acquire the GIL than doing actual work.","Many C extensions check at import time whether the GIL is enabled and raise an error or automatically re-enable it if not.","It returns 0.005 (5 milliseconds), the maximum time a thread holds the GIL before being forced to release it and signal waiting threads."]},{"id":"python/the_gil_and_free_threading#Q8-2593","section":"python","module":"python/the_gil_and_free_threading","moduleName":"the gil and free threading","qIndex":8,"difficulty":"intermediate","question":"What is PEP 684 (sub-interpreters) and how does it differ from PEP 703?","answerFull":"PEP 684, shipped in Python 3.12, gives each sub-interpreter its own independent GIL. Multiple sub-interpreters in the same process can run Python bytecode truly in parallel, each with its own GIL. Unlike PEP 703, the GIL still exists per interpreter; the change is that multiple GILs coexist in one process. Sub-interpreters have stricter isolation requirements (no shared mutable Python objects between them), and the interpreters module API is still experimental. PEP 684 is a stepping stone toward PEP 703: it proves the per-interpreter GIL architecture before removing the GIL entirely.","correct":"PEP 684, shipped in Python 3.12, gives each sub-interpreter its own independent GIL.","distractors":["It returns 0.005 (5 milliseconds), the maximum time a thread holds the GIL before being forced to release it and signal waiting threads.","GIL thrashing is when multiple threads spend more wall-clock time fighting to acquire the GIL than doing actual work.","Many C extensions check at import time whether the GIL is enabled and raise an error or automatically re-enable it if not."]},{"id":"python/the_gil_and_free_threading#Q9-2594","section":"python","module":"python/the_gil_and_free_threading","moduleName":"the gil and free threading","qIndex":9,"difficulty":"intermediate","question":"How would you diagnose a program that is slower with 4 threads than with 1 thread?","answerFull":"First, determine whether the work is CPU-bound or I/O-bound by running with time and observing CPU usage — if CPU is pinned at ~100% with 4 threads instead of ~400%, the GIL is the bottleneck. Use py-spy to sample the call stack and see if threads are frequently blocked on GIL acquisition. Measure with time.perf_counter in single-threaded vs multi-threaded mode to confirm the regression. If confirmed CPU-bound, the fix is ProcessPoolExecutor or multiprocessing.Pool. Also check the switch interval: decreasing it (more switches) will make thrashing worse; increasing it reduces overhead but hurts responsiveness.","correct":"First, determine whether the work is CPU-bound or I/O-bound by running with time and observing CPU usage — if CPU is pinned at ~100% with 4 threads instead of ~400%, the GIL is the bottleneck.","distractors":["Default to asyncio + async def handlers for I/O-bound work (database calls, external APIs, cache reads) — this covers 80% of web service workloads.","GIL thrashing is when multiple threads spend more wall-clock time fighting to acquire the GIL than doing actual work.","PEP 684, shipped in Python 3.12, gives each sub-interpreter its own independent GIL."]},{"id":"python/the_gil_and_free_threading#Q10-2595","section":"python","module":"python/the_gil_and_free_threading","moduleName":"the gil and free threading","qIndex":10,"difficulty":"intermediate","question":"When should you use asyncio instead of threading for I/O-bound work?","answerFull":"Use asyncio when you have hundreds or thousands of concurrent I/O-bound tasks and memory overhead matters — each coroutine costs ~1–2 KB of stack vs ~8 MB default for a threading.Thread. asyncio also avoids race conditions entirely for I/O-bound code because there is no preemption; context switches only happen at explicit await points. Use threading when you need to call synchronous blocking libraries that have no async equivalent, or when the number of concurrent tasks is small (fewer than ~100) and thread overhead is acceptable. In FastAPI, handler functions decorated with async def run on the event loop; those decorated with def are automatically run in a thread pool.","correct":"Use asyncio when you have hundreds or thousands of concurrent I/O-bound tasks and memory overhead matters — each coroutine costs ~1–2 KB of stack vs ~8 MB default for a threading.Thread.","distractors":["The GIL is a mutex that ensures only one thread executes Python bytecode at a time inside a single CPython process.","GIL thrashing is when multiple threads spend more wall-clock time fighting to acquire the GIL than doing actual work.","threading.Lock is a simple non-reentrant mutex: a thread that already holds the lock and tries to acquire it again will deadlock."]},{"id":"python/the_gil_and_free_threading#Q11-2596","section":"python","module":"python/the_gil_and_free_threading","moduleName":"the gil and free threading","qIndex":11,"difficulty":"advanced","question":"What is the difference between threading.Lock and threading.RLock?","answerFull":"threading.Lock is a simple non-reentrant mutex: a thread that already holds the lock and tries to acquire it again will deadlock. threading.RLock (reentrant lock) allows the same thread to acquire it multiple times without blocking; it tracks an acquisition count and only releases when the count reaches zero. Use RLock when a function that acquires a lock can call another function that also acquires the same lock (recursive data structures, decorator chains). Use plain Lock in all other cases — it is slightly faster and the non-reentrancy often catches programming mistakes.","correct":"threading.Lock is a simple non-reentrant mutex: a thread that already holds the lock and tries to acquire it again will deadlock.","distractors":["Use asyncio when you have hundreds or thousands of concurrent I/O-bound tasks and memory overhead matters — each coroutine costs ~1–2 KB of stack vs ~8 MB default for a threading.Thread.","GIL thrashing is when multiple threads spend more wall-clock time fighting to acquire the GIL than doing actual work.","It returns 0.005 (5 milliseconds), the maximum time a thread holds the GIL before being forced to release it and signal waiting threads."]},{"id":"python/the_gil_and_free_threading#Q12-2597","section":"python","module":"python/the_gil_and_free_threading","moduleName":"the gil and free threading","qIndex":12,"difficulty":"advanced","question":"How does concurrent.futures.ProcessPoolExecutor compare to multiprocessing.Pool?","answerFull":"Both spawn worker processes and bypass the GIL for CPU-bound work. ProcessPoolExecutor provides a higher-level Future-based API, integrates with asyncio via loop.run_in_executor, and raises exceptions cleanly through the Future. multiprocessing.Pool provides map, starmap, imap_unordered, and apply_async, which are more convenient for bulk data parallelism with streaming results. For new code, prefer ProcessPoolExecutor; it is part of the standard library's stable high-level futures API and cooperates with asyncio. Use multiprocessing.Pool when you need imap_unordered for streaming or when you need multiprocessing.shared_memory for zero-copy shared buffers.","correct":"Both spawn worker processes and bypass the GIL for CPU-bound work.","distractors":["Default to asyncio + async def handlers for I/O-bound work (database calls, external APIs, cache reads) — this covers 80% of web service workloads.","First, determine whether the work is CPU-bound or I/O-bound by running with time and observing CPU usage — if CPU is pinned at ~100% with 4 threads instead of ~400%, the GIL is the bottleneck.","PEP 684, shipped in Python 3.12, gives each sub-interpreter its own independent GIL."]},{"id":"python/the_gil_and_free_threading#Q13-2598","section":"python","module":"python/the_gil_and_free_threading","moduleName":"the gil and free threading","qIndex":13,"difficulty":"advanced","question":"What does sys.getswitchinterval() return and what does it control?","answerFull":"It returns 0.005 (5 milliseconds), the maximum time a thread holds the GIL before being forced to release it and signal waiting threads. In Python 2 this was controlled by sys.getcheckinterval() which returned 100 (bytecodes). The switch to wall-clock-based switching in Python 3.2 improved fairness: a thread doing heavy computation (many bytecodes per ms) and a thread doing light computation (few bytecodes per ms) now get equal wall-clock time instead of equal bytecode counts. You can tune this for specific workloads, but the default 5 ms is appropriate for most production applications.","correct":"It returns 0.005 (5 milliseconds), the maximum time a thread holds the GIL before being forced to release it and signal waiting threads.","distractors":["PEP 703, accepted for Python 3.13, defines an optional GIL-free build of CPython (python3.13t).","threading.Lock is a simple non-reentrant mutex: a thread that already holds the lock and tries to acquire it again will deadlock.","For single-key reads and writes (single bytecode operations) the GIL provides incidental atomicity in CPython — a d[key] read or d[key] = value write will not be partially executed."]},{"id":"python/the_gil_and_free_threading#Q14-2599","section":"python","module":"python/the_gil_and_free_threading","moduleName":"the gil and free threading","qIndex":14,"difficulty":"advanced","question":"Can you make a Python dict operation thread-safe without an explicit lock?","answerFull":"For single-key reads and writes (single bytecode operations) the GIL provides incidental atomicity in CPython — a d[key] read or d[key] = value write will not be partially executed. However, CPython's implementation details are not part of the language specification, and this atomicity is not guaranteed across Python versions or implementations. More importantly, any logic that reads a value and then writes a derived value (d[k] = d[k] + 1) is never atomic. The correct answer for interviews: do not rely on GIL atomicity for correctness; use threading.Lock for any compound dict operation.","correct":"For single-key reads and writes (single bytecode operations) the GIL provides incidental atomicity in CPython — a d[key] read or d[key] = value write will not be partially executed.","distractors":["PEP 684, shipped in Python 3.12, gives each sub-interpreter its own independent GIL.","It returns 0.005 (5 milliseconds), the maximum time a thread holds the GIL before being forced to release it and signal waiting threads.","GIL thrashing is when multiple threads spend more wall-clock time fighting to acquire the GIL than doing actual work."]},{"id":"python/the_gil_and_free_threading#Q15-2600","section":"python","module":"python/the_gil_and_free_threading","moduleName":"the gil and free threading","qIndex":15,"difficulty":"advanced","question":"How does free-threading affect C extension compatibility?","answerFull":"Many C extensions check at import time whether the GIL is enabled and raise an error or automatically re-enable it if not. CPython 3.13 includes a mechanism: if an extension module does not declare Py_mod_gil = Py_MOD_GIL_NOT_USED, CPython re-enables the GIL when that module is imported, even in python3.13t -X gil=0 mode. This means even in free-threading mode, importing an incompatible C extension silently re-enables the GIL for the entire process. Check via sys._is_gil_enabled() after imports to verify the GIL is actually off. The ecosystem is gradually being audited; NumPy 2.x has added free-threading support.","correct":"Many C extensions check at import time whether the GIL is enabled and raise an error or automatically re-enable it if not.","distractors":["It returns 0.005 (5 milliseconds), the maximum time a thread holds the GIL before being forced to release it and signal waiting threads.","First, determine whether the work is CPU-bound or I/O-bound by running with time and observing CPU usage — if CPU is pinned at ~100% with 4 threads instead of ~400%, the GIL is the bottleneck.","threading.Lock is a simple non-reentrant mutex: a thread that already holds the lock and tries to acquire it again will deadlock."]},{"id":"python/the_gil_and_free_threading#Q17-2601","section":"python","module":"python/the_gil_and_free_threading","moduleName":"the gil and free threading","qIndex":17,"difficulty":"advanced","question":"How do you share large data between processes in Python without pickle overhead?","answerFull":"Use multiprocessing.shared_memory (Python 3.8+) to allocate a named shared memory block accessible from multiple processes. Build a NumPy array backed by shared memory with np.ndarray(..., buffer=shm.buf) — zero-copy read access from all worker processes. For read-only data (model weights, lookup tables), consider multiprocessing.Pool with initializer to load data once per worker process. Alternatively, use memory-mapped files (mmap) or Apache Arrow's shared-memory IPC for structured data. Avoid passing large objects through Pool.map return values — they are pickled and unpickled per task.","correct":"Use multiprocessing.shared_memory (Python 3.8+) to allocate a named shared memory block accessible from multiple processes.","distractors":["Use asyncio when you have hundreds or thousands of concurrent I/O-bound tasks and memory overhead matters — each coroutine costs ~1–2 KB of stack vs ~8 MB default for a threading.Thread.","The GIL is a mutex that ensures only one thread executes Python bytecode at a time inside a single CPython process.","GIL thrashing is when multiple threads spend more wall-clock time fighting to acquire the GIL than doing actual work."]},{"id":"python/the_gil_and_free_threading#Q18-2602","section":"python","module":"python/the_gil_and_free_threading","moduleName":"the gil and free threading","qIndex":18,"difficulty":"advanced","question":"What is the practical advice for a team starting a new Python microservice that needs parallelism?","answerFull":"Default to asyncio + async def handlers for I/O-bound work (database calls, external APIs, cache reads) — this covers 80% of web service workloads. For CPU-bound operations (report generation, image processing, ML inference), use a dedicated task queue (Celery with prefork workers, or ProcessPoolExecutor) to offload work from the event loop. Avoid raw threading.Thread in application code except for wrapping legacy synchronous libraries. Do not adopt free-threading (python3.13t) in production until the extension ecosystem matures (target: Python 3.15–3.16 timeframe). Profile before optimising: most \"slow\" services are I/O-bound, not GIL-bound.","correct":"Default to asyncio + async def handlers for I/O-bound work (database calls, external APIs, cache reads) — this covers 80% of web service workloads.","distractors":["For single-key reads and writes (single bytecode operations) the GIL provides incidental atomicity in CPython — a d[key] read or d[key] = value write will not be partially executed.","Use multiprocessing.shared_memory (Python 3.8+) to allocate a named shared memory block accessible from multiple processes.","The GIL is a mutex that ensures only one thread executes Python bytecode at a time inside a single CPython process."]},{"id":"python/context_managers_and_exceptions#Q1-2603","section":"python","module":"python/context_managers_and_exceptions","moduleName":"context managers and exceptions","qIndex":1,"difficulty":"core","question":"What does exit returning True do, and when should you return True?","answerFull":"Returning a truthy value from exit suppresses the exception — the with block exits cleanly as if no exception occurred. You should almost never return True. The only legitimate use is a CM specifically designed to swallow expected errors (e.g., contextlib.suppress). Returning False or None lets the exception propagate, which is the correct default.","correct":"Returning a truthy value from exit suppresses the exception — the with block exits cleanly as if no exception occurred.","distractors":["raise X from Y sets X.cause = Y and X.suppress_context = True; the traceback display shows \"The above exception was the direct cause of the following exception\".","Without try/finally, an exception in the with body causes _GeneratorContextManager.exit to call generator.throw(exc), which re-raises inside the generator at the yield point.","raise X from None sets cause = None and suppress_context = True, which suppresses the exception chain in the traceback display."]},{"id":"python/context_managers_and_exceptions#Q2-2604","section":"python","module":"python/context_managers_and_exceptions","moduleName":"context managers and exceptions","qIndex":2,"difficulty":"core","question":"Explain how @contextlib.contextmanager converts a generator into a CM.","answerFull":"The decorator wraps the generator function so that calling it returns a _GeneratorContextManager object. enter calls next() on the generator to run it up to the yield and returns the yielded value. exit resumes the generator after the yield; if the block raised an exception, throw() is called on the generator so the except clause in the generator body can handle it. If the block was clean, the generator runs its finally block and StopIteration is raised, signalling normal exit.","correct":"The decorator wraps the generator function so that calling it returns a _GeneratorContextManager object.","distractors":["Starlette (FastAPI's ASGI foundation) wraps each yield dependency in contextlib.asynccontextmanager internally.","Without try/finally, an exception in the with body causes _GeneratorContextManager.exit to call generator.throw(exc), which re-raises inside the generator at the yield point.","Returning a truthy value from exit suppresses the exception — the with block exits cleanly as if no exception occurred."]},{"id":"python/context_managers_and_exceptions#Q3-2605","section":"python","module":"python/context_managers_and_exceptions","moduleName":"context managers and exceptions","qIndex":3,"difficulty":"core","question":"Why must you wrap yield in a try/finally inside a @contextmanager function?","answerFull":"Without try/finally, an exception in the with body causes _GeneratorContextManager.exit to call generator.throw(exc), which re-raises inside the generator at the yield point. If there is no except or finally around the yield, the exception propagates out of the generator and exit re-raises it — the cleanup code below the yield is never reached. A finally block guarantees cleanup code runs regardless of whether an exception was thrown into the generator.","correct":"Without try/finally, an exception in the with body causes _GeneratorContextManager.exit to call generator.throw(exc), which re-raises inside the generator at the yield point.","distractors":["Starlette (FastAPI's ASGI foundation) wraps each yield dependency in contextlib.asynccontextmanager internally.","raise X from None sets cause = None and suppress_context = True, which suppresses the exception chain in the traceback display.","The decorator wraps the generator function so that calling it returns a _GeneratorContextManager object."]},{"id":"python/context_managers_and_exceptions#Q4-2606","section":"python","module":"python/context_managers_and_exceptions","moduleName":"context managers and exceptions","qIndex":4,"difficulty":"core","question":"What is ExitStack and why is it safer than nested with statements when the number of resources is dynamic?","answerFull":"ExitStack is a context manager that maintains a stack of other context managers and cleanup callbacks, unwinding them in LIFO order when it exits. When the number of resources is unknown at write time (e.g., N files from a list), you cannot write N nested with statements. ExitStack lets you enter_context() in a loop. If any later enter_context() call fails, already-entered managers are cleaned up automatically, preventing partial resource leaks.","correct":"ExitStack is a context manager that maintains a stack of other context managers and cleanup callbacks, unwinding them in LIFO order when it exits.","distractors":["raise X from None sets cause = None and suppress_context = True, which suppresses the exception chain in the traceback display.","Returning a truthy value from exit suppresses the exception — the with block exits cleanly as if no exception occurred.","AsyncExitStack (Python 3.7+) is the async equivalent of ExitStack."]},{"id":"python/context_managers_and_exceptions#Q5-2607","section":"python","module":"python/context_managers_and_exceptions","moduleName":"context managers and exceptions","qIndex":5,"difficulty":"core","question":"What is the difference between raise ValueError(\"msg\") from original and raise ValueError(\"msg\")?","answerFull":"raise X from Y sets X.cause = Y and X.suppress_context = True; the traceback display shows \"The above exception was the direct cause of the following exception\". Plain raise X inside an except block sets X.context to the active exception but leaves suppress_context = False; the display shows \"During handling of the above exception, another exception occurred\". Use explicit chaining (from) when you deliberately wrap an error; implicit chaining is automatic but less clear about intent.","correct":"raise X from Y sets X.cause = Y and X.suppress_context = True; the traceback display shows \"The above exception was the direct cause of the following exception\".","distractors":["ExitStack is a context manager that maintains a stack of other context managers and cleanup callbacks, unwinding them in LIFO order when it exits.","The new exception from exit replaces the original exception — the original is lost unless explicitly re-raised or chained.","Returning a truthy value from exit suppresses the exception — the with block exits cleanly as if no exception occurred."]},{"id":"python/context_managers_and_exceptions#Q6-2608","section":"python","module":"python/context_managers_and_exceptions","moduleName":"context managers and exceptions","qIndex":6,"difficulty":"intermediate","question":"When and why would you use raise X from None?","answerFull":"raise X from None sets cause = None and suppress_context = True, which suppresses the exception chain in the traceback display. Use it in library code when the internal implementation detail (e.g., a third-party SDK exception class name, an internal file path) would confuse your callers. The trade-off is that the original root cause is harder to find; only use this at stable API boundaries, and always log the original exception internally before suppressing the chain.","correct":"raise X from None sets cause = None and suppress_context = True, which suppresses the exception chain in the traceback display.","distractors":["Without try/finally, an exception in the with body causes _GeneratorContextManager.exit to call generator.throw(exc), which re-raises inside the generator at the yield point.","ExceptionGroup(\"label\", [exc1, exc2]) is a container exception holding multiple child exceptions.","Returning a truthy value from exit suppresses the exception — the with block exits cleanly as if no exception occurred."]},{"id":"python/context_managers_and_exceptions#Q7-2609","section":"python","module":"python/context_managers_and_exceptions","moduleName":"context managers and exceptions","qIndex":7,"difficulty":"intermediate","question":"Why does except Exception not catch KeyboardInterrupt or SystemExit?","answerFull":"Both KeyboardInterrupt and SystemExit inherit directly from BaseException, not from Exception. Python's exception hierarchy was deliberately split so that signals and interpreter exit events cannot be accidentally swallowed by broad exception handlers in user code. To intercept them you must explicitly catch BaseException, KeyboardInterrupt, or SystemExit — which is only appropriate in top-level shutdown hooks.","correct":"Both KeyboardInterrupt and SystemExit inherit directly from BaseException, not from Exception.","distractors":["raise X from None sets cause = None and suppress_context = True, which suppresses the exception chain in the traceback display.","raise X from Y sets X.cause = Y and X.suppress_context = True; the traceback display shows \"The above exception was the direct cause of the following exception\".","Returning a truthy value from exit suppresses the exception — the with block exits cleanly as if no exception occurred."]},{"id":"python/context_managers_and_exceptions#Q8-2610","section":"python","module":"python/context_managers_and_exceptions","moduleName":"context managers and exceptions","qIndex":8,"difficulty":"intermediate","question":"Describe ExceptionGroup and except* [3.11]. When does asyncio.TaskGroup raise one?","answerFull":"ExceptionGroup(\"label\", [exc1, exc2]) is a container exception holding multiple child exceptions. except* SomeType as eg in a try block extracts only the children matching SomeType into eg.exceptions; unmatched children are re-raised as a new ExceptionGroup. asyncio.TaskGroup collects all task failures and raises a single ExceptionGroup at the end of the async with block so callers can handle different failure types independently. This is the canonical use case for except*.","correct":"ExceptionGroup(\"label\", [exc1, exc2]) is a container exception holding multiple child exceptions.","distractors":["raise X from None sets cause = None and suppress_context = True, which suppresses the exception chain in the traceback display.","Without try/finally, an exception in the with body causes _GeneratorContextManager.exit to call generator.throw(exc), which re-raises inside the generator at the yield point.","raise X from Y sets X.cause = Y and X.suppress_context = True; the traceback display shows \"The above exception was the direct cause of the following exception\"."]},{"id":"python/context_managers_and_exceptions#Q9-2611","section":"python","module":"python/context_managers_and_exceptions","moduleName":"context managers and exceptions","qIndex":9,"difficulty":"intermediate","question":"How does FastAPI's yield dependency relate to async context managers?","answerFull":"Starlette (FastAPI's ASGI foundation) wraps each yield dependency in contextlib.asynccontextmanager internally. Code before yield runs during request setup; code after yield (optionally in try/finally) runs during response teardown — after the route handler returns but before the response is fully flushed. This gives you deterministic resource cleanup that is scoped exactly to the HTTP request lifetime, with no risk of forgetting to release the resource.","correct":"Starlette (FastAPI's ASGI foundation) wraps each yield dependency in contextlib.asynccontextmanager internally.","distractors":["raise X from None sets cause = None and suppress_context = True, which suppresses the exception chain in the traceback display.","The decorator wraps the generator function so that calling it returns a _GeneratorContextManager object.","raise X from Y sets X.cause = Y and X.suppress_context = True; the traceback display shows \"The above exception was the direct cause of the following exception\"."]},{"id":"python/context_managers_and_exceptions#Q10-2612","section":"python","module":"python/context_managers_and_exceptions","moduleName":"context managers and exceptions","qIndex":10,"difficulty":"intermediate","question":"What happens if exit itself raises an exception?","answerFull":"The new exception from exit replaces the original exception — the original is lost unless explicitly re-raised or chained. This is a common, subtle bug: a DB session's exit that raises during rollback hides the original application error. The fix is to wrap cleanup logic in exit in its own try/except and log the secondary failure while re-raising the original (or raising a combined exception).","correct":"The new exception from exit replaces the original exception — the original is lost unless explicitly re-raised or chained.","distractors":["raise X from Y sets X.cause = Y and X.suppress_context = True; the traceback display shows \"The above exception was the direct cause of the following exception\".","ExceptionGroup(\"label\", [exc1, exc2]) is a container exception holding multiple child exceptions.","Returning a truthy value from exit suppresses the exception — the with block exits cleanly as if no exception occurred."]},{"id":"python/context_managers_and_exceptions#Q11-2613","section":"python","module":"python/context_managers_and_exceptions","moduleName":"context managers and exceptions","qIndex":11,"difficulty":"advanced","question":"How do you use contextlib.AsyncExitStack in an async FastAPI lifespan to manage multiple async resources?","answerFull":"AsyncExitStack (Python 3.7+) is the async equivalent of ExitStack. In a lifespan function, you await stack.enter_async_context(SomeAsyncCM()) for each async resource. The stack ensures all resources are torn down in LIFO order when the application shuts down, even if some teardowns raise. This pattern avoids deeply nested async with blocks. python from contextlib import AsyncExitStack, asynccontextmanager from fastapi import FastAPI @asynccontextmanager async def lifespan(app: FastAPI): async with AsyncExitStack() as stack: app.state.db = await stack.enter_async_context(create_db_pool()) app.state.redis = await stack.enter_async_context(create_redis_pool()) app.state.http = await stack.enter_async_context(create_http_client()) yield # All three cleaned up in reverse order automatically","correct":"AsyncExitStack (Python 3.7+) is the async equivalent of ExitStack.","distractors":["ExitStack is a context manager that maintains a stack of other context managers and cleanup callbacks, unwinding them in LIFO order when it exits.","ExceptionGroup(\"label\", [exc1, exc2]) is a container exception holding multiple child exceptions.","Starlette (FastAPI's ASGI foundation) wraps each yield dependency in contextlib.asynccontextmanager internally."]},{"id":"python/file_io_and_serialization#Q1-2614","section":"python","module":"python/file_io_and_serialization","moduleName":"file io and serialization","qIndex":1,"difficulty":"core","question":"Why should you always specify encoding=\"utf-8\" when opening text files in Python?","answerFull":"The default encoding is locale.getpreferredencoding(False), which varies by OS (UTF-8 on Linux/macOS, CP1252 on many Windows systems). Omitting it makes your code behave differently across environments, silently corrupting any non-ASCII characters on Windows. Always hardcode encoding=\"utf-8\" for portability.","correct":"The default encoding is locale.getpreferredencoding(False), which varies by OS (UTF-8 on Linux/macOS, CP1252 on many Windows systems).","distractors":["FastAPI's default JSONResponse uses the stdlib json encoder, which raises TypeError for Decimal.","f.flush() moves data from Python's user-space buffer to the OS kernel buffer.","Setting newline=\"\" disables Python's universal newline translation, leaving the raw bytes (including \\r\\n) unchanged."]},{"id":"python/file_io_and_serialization#Q2-2615","section":"python","module":"python/file_io_and_serialization","moduleName":"file io and serialization","qIndex":2,"difficulty":"core","question":"What is the difference between Path.rename() and Path.replace()?","answerFull":"rename() raises FileExistsError if the destination exists (on Windows; POSIX allows overwrite). replace() atomically overwrites the destination on all platforms. Use replace() for atomic file writes: write to a .tmp file, then replace() into the final path. This pattern prevents readers from observing a partially-written file.","correct":"rename() raises FileExistsError if the destination exists (on Windows; POSIX allows overwrite).","distractors":["Path overrides truediv (the / division operator) to return a new Path representing the concatenated path: Path(\"/data\") / \"uploads\" / \"file.csv\" returns Path(\"/data/uploads/file.csv\").","The default encoding is locale.getpreferredencoding(False), which varies by OS (UTF-8 on Linux/macOS, CP1252 on many Windows systems).","FastAPI's default JSONResponse uses the stdlib json encoder, which raises TypeError for Decimal."]},{"id":"python/file_io_and_serialization#Q3-2616","section":"python","module":"python/file_io_and_serialization","moduleName":"file io and serialization","qIndex":3,"difficulty":"core","question":"Why is pickle.loads(untrusted_bytes) a critical security vulnerability?","answerFull":"The pickle protocol can encode arbitrary Python opcodes, including calls to os.system(), exec(), or subprocess.Popen(). Deserializing attacker-controlled bytes runs those opcodes in the process — arbitrary remote code execution. Use json or msgpack for untrusted data, or verify a HMAC signature before loading pickle from a trusted cache.","correct":"The pickle protocol can encode arbitrary Python opcodes, including calls to os.system(), exec(), or subprocess.Popen().","distractors":["Setting newline=\"\" disables Python's universal newline translation, leaving the raw bytes (including \\r\\n) unchanged.","f.flush() moves data from Python's user-space buffer to the OS kernel buffer.","The default encoding is locale.getpreferredencoding(False), which varies by OS (UTF-8 on Linux/macOS, CP1252 on many Windows systems)."]},{"id":"python/file_io_and_serialization#Q4-2617","section":"python","module":"python/file_io_and_serialization","moduleName":"file io and serialization","qIndex":4,"difficulty":"core","question":"How does io.BytesIO improve testability?","answerFull":"It implements the same read(), write(), seek(), and tell() interface as a real file object, but stores data in memory. Code that accepts a file-like object can be tested by passing a BytesIO with pre-loaded content, eliminating disk I/O from tests and making them faster and deterministic.","correct":"It implements the same read(), write(), seek(), and tell() interface as a real file object, but stores data in memory.","distractors":["Path overrides truediv (the / division operator) to return a new Path representing the concatenated path: Path(\"/data\") / \"uploads\" / \"file.csv\" returns Path(\"/data/uploads/file.csv\").","struct.Struct(format) pre-compiles the format string into a C-level object, avoiding re-parsing the format on every call.","The default encoding is locale.getpreferredencoding(False), which varies by OS (UTF-8 on Linux/macOS, CP1252 on many Windows systems)."]},{"id":"python/file_io_and_serialization#Q5-2618","section":"python","module":"python/file_io_and_serialization","moduleName":"file io and serialization","qIndex":5,"difficulty":"core","question":"What does newline=\"\" do in open(), and why does it matter for csv?","answerFull":"Setting newline=\"\" disables Python's universal newline translation, leaving the raw bytes (including \\r\\n) unchanged. The csv module expects to handle line endings itself. Without newline=\"\", on Windows Python translates \\r\\n to \\n on read and adds an extra \\r on write, producing double blank lines between rows in the output CSV.","correct":"Setting newline=\"\" disables Python's universal newline translation, leaving the raw bytes (including \\r\\n) unchanged.","distractors":["f.flush() moves data from Python's user-space buffer to the OS kernel buffer.","Path overrides truediv (the / division operator) to return a new Path representing the concatenated path: Path(\"/data\") / \"uploads\" / \"file.csv\" returns Path(\"/data/uploads/file.csv\").","The pickle protocol can encode arbitrary Python opcodes, including calls to os.system(), exec(), or subprocess.Popen()."]},{"id":"python/file_io_and_serialization#Q6-2619","section":"python","module":"python/file_io_and_serialization","moduleName":"file io and serialization","qIndex":6,"difficulty":"intermediate","question":"Explain the difference between f.flush() and os.fsync(f.fileno()).","answerFull":"f.flush() moves data from Python's user-space buffer to the OS kernel buffer. The data is now visible to other processes but still in volatile RAM. os.fsync() instructs the kernel to commit its buffer to physical storage, guaranteeing durability through a power loss. Use both when writing critical data (database write-ahead log, checksum file).","correct":"f.flush() moves data from Python's user-space buffer to the OS kernel buffer.","distractors":["The pickle protocol can encode arbitrary Python opcodes, including calls to os.system(), exec(), or subprocess.Popen().","Setting newline=\"\" disables Python's universal newline translation, leaving the raw bytes (including \\r\\n) unchanged.","The default encoding is locale.getpreferredencoding(False), which varies by OS (UTF-8 on Linux/macOS, CP1252 on many Windows systems)."]},{"id":"python/file_io_and_serialization#Q8-2620","section":"python","module":"python/file_io_and_serialization","moduleName":"file io and serialization","qIndex":8,"difficulty":"intermediate","question":"What is struct.Struct, and why is it preferable to repeated struct.pack() calls?","answerFull":"struct.Struct(format) pre-compiles the format string into a C-level object, avoiding re-parsing the format on every call. For hot paths that pack/unpack thousands of messages per second, this can reduce CPU time by 30-50%. Instantiate once at module level: HEADER = struct.Struct(\">IH\").","correct":"struct.Struct(format) pre-compiles the format string into a C-level object, avoiding re-parsing the format on every call.","distractors":["It implements the same read(), write(), seek(), and tell() interface as a real file object, but stores data in memory.","The pickle protocol can encode arbitrary Python opcodes, including calls to os.system(), exec(), or subprocess.Popen().","The default encoding is locale.getpreferredencoding(False), which varies by OS (UTF-8 on Linux/macOS, CP1252 on many Windows systems)."]},{"id":"python/file_io_and_serialization#Q9-2621","section":"python","module":"python/file_io_and_serialization","moduleName":"file io and serialization","qIndex":9,"difficulty":"intermediate","question":"How do you safely create a temp file that persists after the with block exits?","answerFull":"Use tempfile.NamedTemporaryFile(delete=False). With delete=True (the default), the OS deletes the file when the CM exits. With delete=False, the file persists; you are responsible for calling path.unlink() in a finally block or background task. Always write to the temp file, close it, then pass its path to downstream code.","correct":"Use tempfile.NamedTemporaryFile(delete=False).","distractors":["Path overrides truediv (the / division operator) to return a new Path representing the concatenated path: Path(\"/data\") / \"uploads\" / \"file.csv\" returns Path(\"/data/uploads/file.csv\").","It implements the same read(), write(), seek(), and tell() interface as a real file object, but stores data in memory.","The default encoding is locale.getpreferredencoding(False), which varies by OS (UTF-8 on Linux/macOS, CP1252 on many Windows systems)."]},{"id":"python/file_io_and_serialization#Q10-2622","section":"python","module":"python/file_io_and_serialization","moduleName":"file io and serialization","qIndex":10,"difficulty":"intermediate","question":"What is the difference between path.glob(\"*.py\") and path.rglob(\"/*.py\")?","answerFull":"glob() matches only in the immediate directory; rglob() is recursive, equivalent to glob(\"/*.py\"). rglob can be slow on large directory trees; prefer glob with an explicit depth when you know the structure.","correct":"glob() matches only in the immediate directory; rglob() is recursive, equivalent to glob(\"/*.py\").","distractors":["Path overrides truediv (the / division operator) to return a new Path representing the concatenated path: Path(\"/data\") / \"uploads\" / \"file.csv\" returns Path(\"/data/uploads/file.csv\").","The default encoding is locale.getpreferredencoding(False), which varies by OS (UTF-8 on Linux/macOS, CP1252 on many Windows systems).","rename() raises FileExistsError if the destination exists (on Windows; POSIX allows overwrite)."]},{"id":"python/file_io_and_serialization#Q11-2623","section":"python","module":"python/file_io_and_serialization","moduleName":"file io and serialization","qIndex":11,"difficulty":"advanced","question":"How would you handle a JSON payload with Decimal values in a FastAPI endpoint?","answerFull":"FastAPI's default JSONResponse uses the stdlib json encoder, which raises TypeError for Decimal. Two options: (1) subclass json.JSONEncoder and pass cls=MyEncoder to json.dumps(); (2) use orjson, which serializes Decimal as a string by default. Always use str or Decimal representation in JSON — never convert to float, which loses precision for financial values.","correct":"FastAPI's default JSONResponse uses the stdlib json encoder, which raises TypeError for Decimal.","distractors":["rename() raises FileExistsError if the destination exists (on Windows; POSIX allows overwrite).","The default encoding is locale.getpreferredencoding(False), which varies by OS (UTF-8 on Linux/macOS, CP1252 on many Windows systems).","The pickle protocol can encode arbitrary Python opcodes, including calls to os.system(), exec(), or subprocess.Popen()."]},{"id":"python/file_io_and_serialization#Q12-2624","section":"python","module":"python/file_io_and_serialization","moduleName":"file io and serialization","qIndex":12,"difficulty":"advanced","question":"Explain the pathlib / operator. What Python feature does it use?","answerFull":"Path overrides truediv (the / division operator) to return a new Path representing the concatenated path: Path(\"/data\") / \"uploads\" / \"file.csv\" returns Path(\"/data/uploads/file.csv\"). This uses Python's operator overloading mechanism. The result is always a Path object regardless of which side is a string, as long as the left operand is a Path.","correct":"Path overrides truediv (the / division operator) to return a new Path representing the concatenated path: Path(\"/data\") / \"uploads\" / \"file.csv\" returns Path(\"/data/uploads/file.csv\").","distractors":["Setting newline=\"\" disables Python's universal newline translation, leaving the raw bytes (including \\r\\n) unchanged.","f.flush() moves data from Python's user-space buffer to the OS kernel buffer.","It implements the same read(), write(), seek(), and tell() interface as a real file object, but stores data in memory."]},{"id":"python/strings_bytes_encoding_and_regex#Q1-2625","section":"python","module":"python/strings_bytes_encoding_and_regex","moduleName":"strings bytes encoding and regex","qIndex":1,"difficulty":"core","question":"What is the difference between str and bytes in Python 3, and why does the distinction matter for a FastAPI service?","answerFull":"str is a sequence of Unicode code points; bytes is a sequence of raw octets. The distinction matters because the ASGI interface delivers HTTP bodies as bytes. FastAPI (via Starlette) decodes the body to str only for JSON and form data endpoints. A file upload or a streaming endpoint works with raw bytes. Confusing the two causes TypeError at runtime and can silently corrupt data if an implicit coercion somehow occurs (it never does in Python 3, which raises immediately).","correct":"str is a sequence of Unicode code points; bytes is a sequence of raw octets.","distractors":["PEP 393 (CPython 3.3) introduced \"compact\" string storage: CPython picks the narrowest width (1, 2, or 4 bytes per code point) based on the highest code point in the string.","memoryview is a zero-copy view into any buffer-protocol object (bytes, bytearray, array.array, NumPy).","It returns 2. len() on a str counts Unicode code points, not bytes."]},{"id":"python/strings_bytes_encoding_and_regex#Q2-2626","section":"python","module":"python/strings_bytes_encoding_and_regex","moduleName":"strings bytes encoding and regex","qIndex":2,"difficulty":"core","question":"What does len(\"你好\") return and why?","answerFull":"It returns 2. len() on a str counts Unicode code points, not bytes. \"你\" is U+4F60 and \"好\" is U+597D — two code points. Their UTF-8 encoding is 3 bytes each (6 bytes total), but len() does not know or care about the encoding. Use len(s.encode(\"utf-8\")) to get the byte count.","correct":"It returns 2. len() on a str counts Unicode code points, not bytes.","distractors":["str is a sequence of Unicode code points; bytes is a sequence of raw octets.","PEP 393 (CPython 3.3) introduced \"compact\" string storage: CPython picks the narrowest width (1, 2, or 4 bytes per code point) based on the highest code point in the string.","memoryview is a zero-copy view into any buffer-protocol object (bytes, bytearray, array.array, NumPy)."]},{"id":"python/strings_bytes_encoding_and_regex#Q3-2627","section":"python","module":"python/strings_bytes_encoding_and_regex","moduleName":"strings bytes encoding and regex","qIndex":3,"difficulty":"core","question":"What is PEP 393 and why does it matter for memory efficiency?","answerFull":"PEP 393 (CPython 3.3) introduced \"compact\" string storage: CPython picks the narrowest width (1, 2, or 4 bytes per code point) based on the highest code point in the string. A pure-ASCII 1 000-char string uses 1 000 bytes of data; adding a single emoji widens every character to 4 bytes, costing 4 000 bytes. A corpus of ASCII product names is 4x smaller than if stored as UCS-4.","correct":"PEP 393 (CPython 3.3) introduced \"compact\" string storage: CPython picks the narrowest width (1, 2, or 4 bytes per code point) based on the highest code point in the string.","distractors":["str is a sequence of Unicode code points; bytes is a sequence of raw octets.","memoryview is a zero-copy view into any buffer-protocol object (bytes, bytearray, array.array, NumPy).","It returns 2. len() on a str counts Unicode code points, not bytes."]},{"id":"python/strings_bytes_encoding_and_regex#Q4-2628","section":"python","module":"python/strings_bytes_encoding_and_regex","moduleName":"strings bytes encoding and regex","qIndex":4,"difficulty":"core","question":"What is the UTF-8 BOM and how do you handle it in Python?","answerFull":"The BOM is U+FEFF, encoded as \\xef\\xbb\\xbf in UTF-8. Windows tools (Notepad, Excel) prepend it. Decoding with \"utf-8\" leaves a ﻿ character at position 0; \"utf-8-sig\" strips it on read and adds it on write. Use \"utf-8-sig\" when consuming Windows CSV files or producing Excel-compatible output.","correct":"The BOM is U+FEFF, encoded as \\xef\\xbb\\xbf in UTF-8.","distractors":["str is a sequence of Unicode code points; bytes is a sequence of raw octets.","It returns 2. len() on a str counts Unicode code points, not bytes.","PEP 393 (CPython 3.3) introduced \"compact\" string storage: CPython picks the narrowest width (1, 2, or 4 bytes per code point) based on the highest code point in the string."]},{"id":"python/strings_bytes_encoding_and_regex#Q5-2629","section":"python","module":"python/strings_bytes_encoding_and_regex","moduleName":"strings bytes encoding and regex","qIndex":5,"difficulty":"core","question":"Explain memoryview and when you would use it instead of slicing a bytes object.","answerFull":"memoryview is a zero-copy view into any buffer-protocol object (bytes, bytearray, array.array, NumPy). Slicing a memoryview creates a new view into the same memory — no allocation. Slicing bytes allocates and copies each time. Use it when making many slices of a large buffer: binary protocol parsing, video frames, socket.send() of sub-ranges. Break-even is roughly 1 KB; below that, copy overhead is negligible.","correct":"memoryview is a zero-copy view into any buffer-protocol object (bytes, bytearray, array.array, NumPy).","distractors":["It returns 2. len() on a str counts Unicode code points, not bytes.","PEP 393 (CPython 3.3) introduced \"compact\" string storage: CPython picks the narrowest width (1, 2, or 4 bytes per code point) based on the highest code point in the string.","re.compile() translates the pattern into a compiled re.Pattern object once."]},{"id":"python/strings_bytes_encoding_and_regex#Q6-2630","section":"python","module":"python/strings_bytes_encoding_and_regex","moduleName":"strings bytes encoding and regex","qIndex":6,"difficulty":"intermediate","question":"What is the difference between re.match(), re.search(), and re.fullmatch()?","answerFull":"re.match() anchors only at the start but does not require consuming the full string. re.search() scans for the first match anywhere in the string. re.fullmatch() requires the pattern to span the entire string. For input validation always use re.fullmatch() — re.match() silently accepts trailing garbage after a valid prefix.","correct":"re.match() anchors only at the start but does not require consuming the full string.","distractors":["(?:...) groups sub-expressions without allocating a numbered back-reference or storing the match.","PEP 393 (CPython 3.3) introduced \"compact\" string storage: CPython picks the narrowest width (1, 2, or 4 bytes per code point) based on the highest code point in the string.","str is a sequence of Unicode code points; bytes is a sequence of raw octets."]},{"id":"python/strings_bytes_encoding_and_regex#Q7-2631","section":"python","module":"python/strings_bytes_encoding_and_regex","moduleName":"strings bytes encoding and regex","qIndex":7,"difficulty":"intermediate","question":"What are named groups in regex and how do you use them?","answerFull":"Named groups use the syntax (?P<name>...). After a match, m.group(\"name\") and m.groupdict() return the captured text by name. Named groups make patterns self-documenting and protect code from breaking when groups are reordered. Example: r\"(?P<year>\\d{4})-(?P<month>\\d{2})-(?P<day>\\d{2})\". In re.sub(), named back-references are written as \\g<name>.","correct":"Named groups use the syntax (?P<name>...).","distractors":["Catastrophic backtracking occurs when an NFA regex engine explores O(2^n) paths on a non-matching input, caused by ambiguous nested quantifiers like (a+)+.","(?:...) groups sub-expressions without allocating a numbered back-reference or storing the match.","str is a sequence of Unicode code points; bytes is a sequence of raw octets."]},{"id":"python/strings_bytes_encoding_and_regex#Q8-2632","section":"python","module":"python/strings_bytes_encoding_and_regex","moduleName":"strings bytes encoding and regex","qIndex":8,"difficulty":"intermediate","question":"What is catastrophic backtracking and how do you prevent it?","answerFull":"Catastrophic backtracking occurs when an NFA regex engine explores O(2^n) paths on a non-matching input, caused by ambiguous nested quantifiers like (a+)+. Prevention: (1) rewrite the pattern — (a+)+ becomes a+; (2) use the regex library's atomic groups (?>a+)+ to prevent backtracking into a committed match; (3) for user-supplied patterns, run matching in a subprocess with a hard wall-clock timeout and kill the process if it exceeds it.","correct":"Catastrophic backtracking occurs when an NFA regex engine explores O(2^n) paths on a non-matching input, caused by ambiguous nested quantifiers like (a+)+.","distractors":["str is a sequence of Unicode code points; bytes is a sequence of raw octets.","It returns 2. len() on a str counts Unicode code points, not bytes.","PEP 393 (CPython 3.3) introduced \"compact\" string storage: CPython picks the narrowest width (1, 2, or 4 bytes per code point) based on the highest code point in the string."]},{"id":"python/strings_bytes_encoding_and_regex#Q9-2633","section":"python","module":"python/strings_bytes_encoding_and_regex","moduleName":"strings bytes encoding and regex","qIndex":9,"difficulty":"intermediate","question":"Why should re.compile() be called at module level, and what is the internal cache in the re module?","answerFull":"re.compile() translates the pattern into a compiled re.Pattern object once. Without it, re.match(pattern_str, text) checks an internal LRU cache (512 entries in CPython 3.11); a cache hit still pays a dict-lookup cost, and a miss triggers full recompilation. Module-level re.compile() is zero overhead at call time, enables re.VERBOSE for inline documentation, and makes the intent explicit to readers.","correct":"re.compile() translates the pattern into a compiled re.Pattern object once.","distractors":["memoryview is a zero-copy view into any buffer-protocol object (bytes, bytearray, array.array, NumPy).","str is a sequence of Unicode code points; bytes is a sequence of raw octets.","It returns 2. len() on a str counts Unicode code points, not bytes."]},{"id":"python/strings_bytes_encoding_and_regex#Q10-2634","section":"python","module":"python/strings_bytes_encoding_and_regex","moduleName":"strings bytes encoding and regex","qIndex":10,"difficulty":"intermediate","question":"What are non-capturing groups and why use them over capturing groups?","answerFull":"(?:...) groups sub-expressions without allocating a numbered back-reference or storing the match. Use them for alternation or repetition when the captured text is not needed: (?:jpg|png)+. They are slightly faster (no capture bookkeeping), keep m.groups() uncluttered, and prevent consumers from accidentally depending on a specific group number.","correct":"(?:...) groups sub-expressions without allocating a numbered back-reference or storing the match.","distractors":["Catastrophic backtracking occurs when an NFA regex engine explores O(2^n) paths on a non-matching input, caused by ambiguous nested quantifiers like (a+)+.","re.match() anchors only at the start but does not require consuming the full string.","Named groups use the syntax (?P<name>...)."]},{"id":"python/strings_bytes_encoding_and_regex#Q11-2635","section":"python","module":"python/strings_bytes_encoding_and_regex","moduleName":"strings bytes encoding and regex","qIndex":11,"difficulty":"advanced","question":"What does the re.DOTALL flag do and when is it needed?","answerFull":"By default . matches any character except \\n. With re.DOTALL (alias re.S), . also matches newlines. Use it when the match target spans multiple lines — extracting a multi-line block from HTML, or capturing a JSON value that contains embedded newlines. Without it, .+ truncates at the first \\n.","correct":"By default . matches any character except \\n.","distractors":["str is a sequence of Unicode code points; bytes is a sequence of raw octets.","It returns 2. len() on a str counts Unicode code points, not bytes.","PEP 393 (CPython 3.3) introduced \"compact\" string storage: CPython picks the narrowest width (1, 2, or 4 bytes per code point) based on the highest code point in the string."]},{"id":"python/strings_bytes_encoding_and_regex#Q12-2636","section":"python","module":"python/strings_bytes_encoding_and_regex","moduleName":"strings bytes encoding and regex","qIndex":12,"difficulty":"advanced","question":"How does f-string performance compare to .format() and % formatting?","answerFull":"f-strings are the fastest of the three. On CPython 3.11, micro-benchmarks show f-strings are roughly 3x faster than .format() and 1.5–2x faster than %. f-strings compile to FORMAT_VALUE / BUILD_STRING bytecode opcodes; .format() requires a method call plus runtime format-string parsing; % scans the format string at runtime. For most web services the delta is negligible, but in tight loops generating thousands of log lines or CSV rows per second it adds up.","correct":"f-strings are the fastest of the three.","distractors":["PEP 393 (CPython 3.3) introduced \"compact\" string storage: CPython picks the narrowest width (1, 2, or 4 bytes per code point) based on the highest code point in the string.","re.match() anchors only at the start but does not require consuming the full string.","str is a sequence of Unicode code points; bytes is a sequence of raw octets."]},{"id":"python/threading_and_multiprocessing#Q1-2637","section":"python","module":"python/threading_and_multiprocessing","moduleName":"threading and multiprocessing","qIndex":1,"difficulty":"core","question":"What is the GIL and how does it affect threading vs multiprocessing?","answerFull":"The GIL (Global Interpreter Lock) is a mutex in CPython that allows only one thread to execute Python bytecode at a time. For I/O-bound work, threads still provide speedup because the GIL is released during blocking I/O system calls. For CPU-bound work, threads offer no parallelism — only multiprocessing bypasses the GIL by running separate interpreter instances in separate OS processes. If your profiling shows > 80% CPU time with < 20% I/O wait, choose ProcessPoolExecutor.","correct":"The GIL (Global Interpreter Lock) is a mutex in CPython that allows only one thread to execute Python bytecode at a time.","distractors":["multiprocessing.Value('i', 0) provides a single typed C value (int, float, etc.) backed by shared memory, with a built-in RLock for safe concurrent access.","threading.Barrier(n) provides a synchronization point where exactly n threads must call barrier.wait() before any of them proceeds past it.","Thread-local storage (threading.local()) gives each thread its own independent copy of a variable — reads and writes in thread A do not affect thread B's view."]},{"id":"python/threading_and_multiprocessing#Q2-2638","section":"python","module":"python/threading_and_multiprocessing","moduleName":"threading and multiprocessing","qIndex":2,"difficulty":"core","question":"When should you use ThreadPoolExecutor over ProcessPoolExecutor?","answerFull":"Use ThreadPoolExecutor for I/O-bound work (HTTP calls, database queries, file reads) where threads spend most of their time waiting on system calls and the GIL is released. Use ProcessPoolExecutor for CPU-bound work (image processing, compression, ML inference) where Python bytecode execution is the bottleneck. ThreadPoolExecutor starts in < 1 ms per task; ProcessPoolExecutor pays a ~100–300 ms spawn overhead but achieves true CPU parallelism.","correct":"Use ThreadPoolExecutor for I/O-bound work (HTTP calls, database queries, file reads) where threads spend most of their time waiting on system calls and the GIL is released.","distractors":["threading.Barrier(n) provides a synchronization point where exactly n threads must call barrier.wait() before any of them proceeds past it.","A race condition occurs when the outcome depends on the non-deterministic scheduling order of threads.","The GIL (Global Interpreter Lock) is a mutex in CPython that allows only one thread to execute Python bytecode at a time."]},{"id":"python/threading_and_multiprocessing#Q3-2639","section":"python","module":"python/threading_and_multiprocessing","moduleName":"threading and multiprocessing","qIndex":3,"difficulty":"core","question":"What is a deadlock and how do you avoid it?","answerFull":"A deadlock occurs when two or more threads are each waiting for a lock held by another, creating a circular dependency where none can proceed. To avoid deadlocks: (1) always acquire multiple locks in a consistent global order, (2) use lock.acquire(timeout=...) with retry logic, (3) use threading.RLock when the same thread needs to re-acquire a lock, and (4) prefer higher-level abstractions like queue.Queue that eliminate explicit lock management.","correct":"A deadlock occurs when two or more threads are each waiting for a lock held by another, creating a circular dependency where none can proceed.","distractors":["Use ThreadPoolExecutor for I/O-bound work (HTTP calls, database queries, file reads) where threads spend most of their time waiting on system calls and the GIL is released.","The GIL (Global Interpreter Lock) is a mutex in CPython that allows only one thread to execute Python bytecode at a time.","A race condition occurs when the outcome depends on the non-deterministic scheduling order of threads."]},{"id":"python/threading_and_multiprocessing#Q4-2640","section":"python","module":"python/threading_and_multiprocessing","moduleName":"threading and multiprocessing","qIndex":4,"difficulty":"core","question":"What is a race condition and how do you detect one?","answerFull":"A race condition occurs when the outcome depends on the non-deterministic scheduling order of threads. Example: two threads read x = 0, both increment to 1, both write x = 1 — one increment is lost. Detection methods: (1) Python's threading.settrace or the racedetector mode in PyPy, (2) deliberately introduce time.sleep(0) in critical sections to increase scheduling probability, (3) use stress testing with ThreadSanitizer via CPython debug builds, (4) audit all shared mutable state access.","correct":"A race condition occurs when the outcome depends on the non-deterministic scheduling order of threads.","distractors":["A deadlock occurs when two or more threads are each waiting for a lock held by another, creating a circular dependency where none can proceed.","queue.Queue internally uses a collections.deque protected by a threading.Condition (which wraps a threading.Lock).","Use ThreadPoolExecutor for I/O-bound work (HTTP calls, database queries, file reads) where threads spend most of their time waiting on system calls and the GIL is released."]},{"id":"python/threading_and_multiprocessing#Q5-2641","section":"python","module":"python/threading_and_multiprocessing","moduleName":"threading and multiprocessing","qIndex":5,"difficulty":"core","question":"Why is lambda not picklable for multiprocessing.Pool?","answerFull":"pickle serializes objects by reference — it stores the module path and qualified name of a callable, then reconstructs it in the worker process by importing that name. lambda functions are anonymous and have no qualified name in any module, so pickle cannot store a reference to them. Module-level named functions work because workers can import module; getattr(module, \"function_name\"). functools.partial wraps a picklable function with bound arguments and is itself picklable.","correct":"pickle serializes objects by reference — it stores the module path and qualified name of a callable, then reconstructs it in the worker process by importing that name.","distractors":["multiprocessing.Value('i', 0) provides a single typed C value (int, float, etc.) backed by shared memory, with a built-in RLock for safe concurrent access.","The exception is caught in the worker, pickled, and sent back to the main process.","as_completed(futures) returns an iterator that yields Future objects in the order they complete — not the order they were submitted."]},{"id":"python/threading_and_multiprocessing#Q6-2642","section":"python","module":"python/threading_and_multiprocessing","moduleName":"threading and multiprocessing","qIndex":6,"difficulty":"intermediate","question":"What is thread-local storage and when should you use it in FastAPI?","answerFull":"Thread-local storage (threading.local()) gives each thread its own independent copy of a variable — reads and writes in thread A do not affect thread B's view. Use it when: (1) synchronous FastAPI endpoints run in a thread pool and each request needs its own context (request ID, database session), (2) wrapping thread-unsafe libraries that have per-thread state (SQLAlchemy scoped sessions). For async FastAPI handlers, prefer contextvars.ContextVar instead — threading.local does not propagate across await boundaries.","correct":"Thread-local storage (threading.local()) gives each thread its own independent copy of a variable — reads and writes in thread A do not affect thread B's view.","distractors":["The GIL (Global Interpreter Lock) is a mutex in CPython that allows only one thread to execute Python bytecode at a time.","Use ThreadPoolExecutor for I/O-bound work (HTTP calls, database queries, file reads) where threads spend most of their time waiting on system calls and the GIL is released.","queue.Queue internally uses a collections.deque protected by a threading.Condition (which wraps a threading.Lock)."]},{"id":"python/threading_and_multiprocessing#Q7-2643","section":"python","module":"python/threading_and_multiprocessing","moduleName":"threading and multiprocessing","qIndex":7,"difficulty":"intermediate","question":"How does queue.Queue achieve thread safety?","answerFull":"queue.Queue internally uses a collections.deque protected by a threading.Condition (which wraps a threading.Lock). put() acquires the lock, appends to the deque, and calls notify() to wake a waiting consumer. get() acquires the lock, calls wait() if the deque is empty (atomically releasing the lock), and pops once notified. The task_done() / join() mechanism uses a separate Condition and a pending-tasks counter to block join() until all items are processed.","correct":"queue.Queue internally uses a collections.deque protected by a threading.Condition (which wraps a threading.Lock).","distractors":["The GIL (Global Interpreter Lock) is a mutex in CPython that allows only one thread to execute Python bytecode at a time.","A race condition occurs when the outcome depends on the non-deterministic scheduling order of threads.","threading.Barrier(n) provides a synchronization point where exactly n threads must call barrier.wait() before any of them proceeds past it."]},{"id":"python/threading_and_multiprocessing#Q9-2644","section":"python","module":"python/threading_and_multiprocessing","moduleName":"threading and multiprocessing","qIndex":9,"difficulty":"intermediate","question":"How does multiprocessing.shared_memory differ from multiprocessing.Value?","answerFull":"multiprocessing.Value('i', 0) provides a single typed C value (int, float, etc.) backed by shared memory, with a built-in RLock for safe concurrent access. It is limited to primitive types and fixed size. multiprocessing.shared_memory.SharedMemory creates a raw bytes buffer of arbitrary size in OS shared memory, accessible via memoryview. Combined with NumPy, it enables zero-copy sharing of large arrays — a 100 MB NumPy array takes ~0s to \"pass\" to a worker via shared memory vs ~0.5s via pickle. SharedMemory requires explicit close() and unlink() lifecycle management.","correct":"multiprocessing.Value('i', 0) provides a single typed C value (int, float, etc.) backed by shared memory, with a built-in RLock for safe concurrent access.","distractors":["Option 1: multiprocessing.shared_memory.SharedMemory — write array into shared buffer once, pass only the shm.name string to workers, reconstruct with np.ndarray(..., buffer=shm.buf).","threading.Barrier(n) provides a synchronization point where exactly n threads must call barrier.wait() before any of them proceeds past it.","The GIL (Global Interpreter Lock) is a mutex in CPython that allows only one thread to execute Python bytecode at a time."]},{"id":"python/threading_and_multiprocessing#Q10-2645","section":"python","module":"python/threading_and_multiprocessing","moduleName":"threading and multiprocessing","qIndex":10,"difficulty":"intermediate","question":"What is concurrent.futures.as_completed and when do you use it?","answerFull":"as_completed(futures) returns an iterator that yields Future objects in the order they complete — not the order they were submitted. Use it when: (1) you want to process results as soon as they arrive (streaming dashboard), (2) tasks have variable duration and you don't want fast tasks to wait for slow ones, (3) you need early termination on first error. In contrast, executor.map() preserves submission order by buffering completed results until their turn.","correct":"as_completed(futures) returns an iterator that yields Future objects in the order they complete — not the order they were submitted.","distractors":["multiprocessing.Value('i', 0) provides a single typed C value (int, float, etc.) backed by shared memory, with a built-in RLock for safe concurrent access.","pickle serializes objects by reference — it stores the module path and qualified name of a callable, then reconstructs it in the worker process by importing that name.","A race condition occurs when the outcome depends on the non-deterministic scheduling order of threads."]},{"id":"python/threading_and_multiprocessing#Q11-2646","section":"python","module":"python/threading_and_multiprocessing","moduleName":"threading and multiprocessing","qIndex":11,"difficulty":"advanced","question":"How do you safely pass large NumPy arrays to worker processes?","answerFull":"Option 1: multiprocessing.shared_memory.SharedMemory — write array into shared buffer once, pass only the shm.name string to workers, reconstruct with np.ndarray(..., buffer=shm.buf). Zero-copy for reads; coordination needed for writes. Option 2: memory-mapped files (np.memmap) — workers open the same file path, OS page cache handles sharing. Good for read-only datasets. Option 3: pickle (default) — avoid for arrays > 1 MB due to ~0.5s/100 MB overhead. Rule of thumb: if worker startup time (0.1–0.3s) exceeds pickle time for your array size, SharedMemory is worth the complexity.","correct":"Option 1: multiprocessing.shared_memory.SharedMemory — write array into shared buffer once, pass only the shm.name string to workers, reconstruct with np.ndarray(..., buffer=shm.buf).","distractors":["The exception is caught in the worker, pickled, and sent back to the main process.","pickle serializes objects by reference — it stores the module path and qualified name of a callable, then reconstructs it in the worker process by importing that name.","multiprocessing.Value('i', 0) provides a single typed C value (int, float, etc.) backed by shared memory, with a built-in RLock for safe concurrent access."]},{"id":"python/threading_and_multiprocessing#Q12-2647","section":"python","module":"python/threading_and_multiprocessing","moduleName":"threading and multiprocessing","qIndex":12,"difficulty":"advanced","question":"What happens if a worker process in ProcessPoolExecutor raises an exception?","answerFull":"The exception is caught in the worker, pickled, and sent back to the main process. When you call future.result(), the exception is re-raised in the main thread. If the worker crashes hard (segfault, OOM kill), the future raises concurrent.futures.process.BrokenProcessPool. You should catch Exception from future.result() individually per future, and wrap the entire executor in a try/finally to ensure executor.shutdown(wait=True) is called (the context manager does this automatically).","correct":"The exception is caught in the worker, pickled, and sent back to the main process.","distractors":["pickle serializes objects by reference — it stores the module path and qualified name of a callable, then reconstructs it in the worker process by importing that name.","The GIL (Global Interpreter Lock) is a mutex in CPython that allows only one thread to execute Python bytecode at a time.","Use ThreadPoolExecutor for I/O-bound work (HTTP calls, database queries, file reads) where threads spend most of their time waiting on system calls and the GIL is released."]},{"id":"python/threading_and_multiprocessing#Q13-2648","section":"python","module":"python/threading_and_multiprocessing","moduleName":"threading and multiprocessing","qIndex":13,"difficulty":"advanced","question":"How does threading.Barrier work and what is it useful for?","answerFull":"threading.Barrier(n) provides a synchronization point where exactly n threads must call barrier.wait() before any of them proceeds past it. Each call decrements an internal counter; when it reaches zero, all waiting threads are released simultaneously. Useful for: parallel test setup (all workers must finish initialization before any starts the actual load), phased parallel algorithms (all workers complete phase 1 before any starts phase 2), and tournament-style parallel benchmarks.","correct":"threading.Barrier(n) provides a synchronization point where exactly n threads must call barrier.wait() before any of them proceeds past it.","distractors":["Thread-local storage (threading.local()) gives each thread its own independent copy of a variable — reads and writes in thread A do not affect thread B's view.","multiprocessing.Value('i', 0) provides a single typed C value (int, float, etc.) backed by shared memory, with a built-in RLock for safe concurrent access.","queue.Queue internally uses a collections.deque protected by a threading.Condition (which wraps a threading.Lock)."]},{"id":"python/threading_and_multiprocessing#Q15-2649","section":"python","module":"python/threading_and_multiprocessing","moduleName":"threading and multiprocessing","qIndex":15,"difficulty":"advanced","question":"How does the multiprocessing start method affect behavior on macOS and Windows?","answerFull":"Python 3.8+ changed the default start method on macOS from fork to spawn. fork duplicates the parent process memory (cheap, copy-on-write) but can cause deadlocks in forked processes that inherited lock state from the parent — particularly with Objective-C frameworks on macOS. spawn starts a fresh Python interpreter and re-imports the main module (adds ~0.1–0.3s per worker but is safe). Windows only supports spawn. Use if name == \"main\": guard on Windows/macOS to prevent recursive spawning. forkserver is a third option: one fork server holds a clean state and forks from it.","correct":"Python 3.8+ changed the default start method on macOS from fork to spawn.","distractors":["Thread-local storage (threading.local()) gives each thread its own independent copy of a variable — reads and writes in thread A do not affect thread B's view.","multiprocessing.Value('i', 0) provides a single typed C value (int, float, etc.) backed by shared memory, with a built-in RLock for safe concurrent access.","The GIL (Global Interpreter Lock) is a mutex in CPython that allows only one thread to execute Python bytecode at a time."]},{"id":"python/core_language_idioms#Q2-2650","section":"python","module":"python/core_language_idioms","moduleName":"core language idioms","qIndex":2,"difficulty":"core","question":"Why can a = 257; b = 257; a is b return False?","answerFull":"CPython caches integers only in the range -5 to 256 as singletons. Values outside this range are freshly allocated objects each time the literal appears in a new code object. In a script, 257 on line 1 and 257 on line 2 may be the same compile-time constant (the compiler folds constants within a code object), but across a REPL session boundary or separate function bodies they will be different objects. Always use == for numeric equality.","correct":"CPython caches integers only in the range -5 to 256 as singletons.","distractors":["Python calls bool first; if that is not defined, it calls len and treats a return value of 0 as False.","Default argument values are evaluated once when the def statement executes, not once per call.","It eliminates double computation when you need to both assign a value and test it in one expression."]},{"id":"python/core_language_idioms#Q4-2651","section":"python","module":"python/core_language_idioms","moduleName":"core language idioms","qIndex":4,"difficulty":"core","question":"What is the mutable-default-argument trap, and how do you fix it?","answerFull":"Default argument values are evaluated once when the def statement executes, not once per call. A mutable default (like [] or {}) is therefore shared across all calls that use the default. The fix is to use None as the default and create a fresh mutable object inside the function body: def f(items: list | None = None): if items is None: items = []. This pattern is so common that forgetting it in a code review is considered a significant oversight.","correct":"Default argument values are evaluated once when the def statement executes, not once per call.","distractors":["Use a generator expression when the result is consumed exactly once and you do not need its length or random access.","CPython caches integers only in the range -5 to 256 as singletons.","It eliminates double computation when you need to both assign a value and test it in one expression."]},{"id":"python/core_language_idioms#Q6-2652","section":"python","module":"python/core_language_idioms","moduleName":"core language idioms","qIndex":6,"difficulty":"intermediate","question":"Does a list comprehension variable leak into the enclosing scope in Python 3?","answerFull":"No. In Python 3, list, dict, and set comprehensions and generator expressions each execute in their own implicit scope. The iteration variable is local to the comprehension. This is the opposite of Python 2 behaviour. Plain for loops still leak their variable into the enclosing scope in both Python 2 and 3.","correct":"No. In Python 3, list, dict, and set comprehensions and generator expressions each execute in their own implicit scope.","distractors":["Python calls bool first; if that is not defined, it calls len and treats a return value of 0 as False.","Use a generator expression when the result is consumed exactly once and you do not need its length or random access.","In assignment (first, *rest = items), the starred variable collects zero or more elements from the iterable as a list."]},{"id":"python/core_language_idioms#Q7-2653","section":"python","module":"python/core_language_idioms","moduleName":"core language idioms","qIndex":7,"difficulty":"intermediate","question":"What problem does the walrus operator := [3.8] solve?","answerFull":"It eliminates double computation when you need to both assign a value and test it in one expression. The canonical example is while chunk := file.read(8192): — without walrus you either assign before the loop (making it less readable) or call read() twice. It also enables comprehension filters that use the computed value: [c for s in data if (c := s.strip())].","correct":"It eliminates double computation when you need to both assign a value and test it in one expression.","distractors":["Use a generator expression when the result is consumed exactly once and you do not need its length or random access.","is not None is a guaranteed identity test against the singleton None object — it cannot be overridden.","CPython caches integers only in the range -5 to 256 as singletons."]},{"id":"python/core_language_idioms#Q8-2654","section":"python","module":"python/core_language_idioms","moduleName":"core language idioms","qIndex":8,"difficulty":"intermediate","question":"What are the four main pattern kinds in match/case [3.10]?","answerFull":"Literal patterns match exact values (case 404:). Capture patterns bind a name to the matched value (case x:). Class patterns match the type and optionally destructure attributes (case Command(action=\"quit\"):). Sequence patterns match tuple or list structure (case (x, y):). Additionally, OR patterns combine alternatives (case 200 | 201:), and any pattern can have a guard clause (case n if n > 0:).","correct":"Literal patterns match exact values (case 404:).","distractors":["CPython caches integers only in the range -5 to 256 as singletons.","Default argument values are evaluated once when the def statement executes, not once per call.","It eliminates double computation when you need to both assign a value and test it in one expression."]},{"id":"python/core_language_idioms#Q9-2655","section":"python","module":"python/core_language_idioms","moduleName":"core language idioms","qIndex":9,"difficulty":"intermediate","question":"When should you use a generator expression instead of a list comprehension?","answerFull":"Use a generator expression when the result is consumed exactly once and you do not need its length or random access. Passing (x2 for x in data) to sum(), max(), or any() avoids materialising an intermediate list in memory — for 10 million elements this saves roughly 80 MB (8 bytes per Python int reference). Use a list comprehension when you need to iterate the result multiple times, check len(), index into it, or pass it to a function that requires a sequence.","correct":"Use a generator expression when the result is consumed exactly once and you do not need its length or random access.","distractors":["Default argument values are evaluated once when the def statement executes, not once per call.","It eliminates double computation when you need to both assign a value and test it in one expression.","In assignment (first, *rest = items), the starred variable collects zero or more elements from the iterable as a list."]},{"id":"python/core_language_idioms#Q10-2656","section":"python","module":"python/core_language_idioms","moduleName":"core language idioms","qIndex":10,"difficulty":"intermediate","question":"How does Python determine the truthiness of a custom class instance?","answerFull":"Python calls bool first; if that is not defined, it calls len and treats a return value of 0 as False. If neither is defined, the instance is always truthy (the object exists). To make a class semantically Boolean (a BankAccount with zero balance should be falsy), implement bool. To make a container class behave like built-in containers, implement len.","correct":"Python calls bool first; if that is not defined, it calls len and treats a return value of 0 as False.","distractors":["CPython caches integers only in the range -5 to 256 as singletons.","Default argument values are evaluated once when the def statement executes, not once per call.","It eliminates double computation when you need to both assign a value and test it in one expression."]},{"id":"python/core_language_idioms#Q11-2657","section":"python","module":"python/core_language_idioms","moduleName":"core language idioms","qIndex":11,"difficulty":"advanced","question":"Why is if x is not None preferred over if x != None?","answerFull":"is not None is a guaranteed identity test against the singleton None object — it cannot be overridden. != None calls x.ne(None), which can return any value from a custom eq or ne. Third-party library objects (NumPy arrays, SQLAlchemy column expressions) override eq in ways that return non-Boolean objects, making x != None raise an exception or produce unexpected results in a Boolean context. is not None is always safe.","correct":"is not None is a guaranteed identity test against the singleton None object — it cannot be overridden.","distractors":["It eliminates double computation when you need to both assign a value and test it in one expression.","CPython caches integers only in the range -5 to 256 as singletons.","Default argument values are evaluated once when the def statement executes, not once per call."]},{"id":"python/core_language_idioms#Q12-2658","section":"python","module":"python/core_language_idioms","moduleName":"core language idioms","qIndex":12,"difficulty":"advanced","question":"What is the difference between starred unpacking in assignment and in function calls?","answerFull":"In assignment (first, *rest = items), the starred variable collects zero or more elements from the iterable as a list. It can appear in any position but only once per assignment target. In function calls (func(*args)), the star unpacks an iterable into positional arguments. kwargs in a call unpacks a mapping into keyword arguments. Both use the same * syntax but operate in different contexts — the assignment form is a language feature; the call form is an operator on the argument list.","correct":"In assignment (first, *rest = items), the starred variable collects zero or more elements from the iterable as a list.","distractors":["Python calls bool first; if that is not defined, it calls len and treats a return value of 0 as False.","CPython caches integers only in the range -5 to 256 as singletons.","Default argument values are evaluated once when the def statement executes, not once per call."]},{"id":"python/decorators_and_closures#Q2-2659","section":"python","module":"python/decorators_and_closures","moduleName":"decorators and closures","qIndex":2,"difficulty":"core","question":"What is the difference between global and nonlocal?","answerFull":"global declares that a name refers to the module-level namespace; nonlocal declares that a name refers to the nearest enclosing function scope (not global). Use nonlocal when you need to *rebind* (assign to) a variable in a closure — mere mutation of a mutable object does not require it. global is needed to rebind module-level names from within a function.","correct":"global declares that a name refers to the module-level namespace; nonlocal declares that a name refers to the nearest enclosing function scope (not global).","distractors":["functools.wraps sets wrapper.wrapped = func (the original function).","Most decorators return a wrapper that calls the original function (transparent wrapping).","@decorator above def f is syntactic sugar for f = decorator(f) which Python executes when it encounters the def statement during module parsing."]},{"id":"python/decorators_and_closures#Q3-2660","section":"python","module":"python/decorators_and_closures","moduleName":"decorators and closures","qIndex":3,"difficulty":"core","question":"Why does @decorator syntax run at import time, not at call time?","answerFull":"@decorator above def f is syntactic sugar for f = decorator(f) which Python executes when it encounters the def statement during module parsing. This means decorator setup code (outer function body) runs once at import time. The wrapper's body runs per call. This matters in FastAPI: @app.get(\"/path\") registers the route immediately when the module is imported, regardless of whether the app has started.","correct":"@decorator above def f is syntactic sugar for f = decorator(f) which Python executes when it encounters the def statement during module parsing.","distractors":["global declares that a name refers to the module-level namespace; nonlocal declares that a name refers to the nearest enclosing function scope (not global).","The bug: closures capture the *variable* (cell reference), not its *value* at creation time.","Application order is bottom-up: the decorator closest to def is applied first, the topmost decorator last."]},{"id":"python/decorators_and_closures#Q4-2661","section":"python","module":"python/decorators_and_closures","moduleName":"decorators and closures","qIndex":4,"difficulty":"core","question":"What does functools.wraps actually do under the hood?","answerFull":"It calls functools.update_wrapper(wrapper, wrapped, assigned=WRAPPER_ASSIGNMENTS, updated=WRAPPER_UPDATES). This copies module, name, qualname, annotations, doc from wrapped to wrapper, updates wrapper.dict with wrapped.dict, and sets wrapper.wrapped = wrapped. Without it, tools like help(), Sphinx, and FastAPI's OpenAPI generator see the wrapper's attributes instead of the original function's, producing wrong documentation and schema.","correct":"It calls functools.update_wrapper(wrapper, wrapped, assigned=WRAPPER_ASSIGNMENTS, updated=WRAPPER_UPDATES).","distractors":["Use a class-based decorator when the decorator needs per-function mutable state that must persist across calls and be externally inspectable or resettable (e.g., process.call_count, circuit breaker state).","functools.partial(func, *args, kwargs) returns a new callable with some arguments pre-filled.","Most decorators return a wrapper that calls the original function (transparent wrapping)."]},{"id":"python/decorators_and_closures#Q5-2662","section":"python","module":"python/decorators_and_closures","moduleName":"decorators and closures","qIndex":5,"difficulty":"core","question":"Explain the three-level structure of a parametrized decorator.","answerFull":"Level 1 is the *factory function* (e.g., retry(max_attempts=3)) — it receives configuration and returns the decorator. Level 2 is the *decorator* — it receives the function to wrap and returns the wrapper. Level 3 is the *wrapper* — it runs on every invocation, implementing the actual behavior. The factory exists solely to create a closure over the configuration values so the decorator and wrapper can access them without being passed those values explicitly.","correct":"Level 1 is the *factory function* (e.g., retry(max_attempts=3)) — it receives configuration and returns the decorator.","distractors":["functools.partial(func, *args, kwargs) returns a new callable with some arguments pre-filled.","functools.wraps sets wrapper.wrapped = func (the original function).","Most decorators return a wrapper that calls the original function (transparent wrapping)."]},{"id":"python/decorators_and_closures#Q6-2663","section":"python","module":"python/decorators_and_closures","moduleName":"decorators and closures","qIndex":6,"difficulty":"intermediate","question":"When should you use a class-based decorator instead of a function-based one?","answerFull":"Use a class-based decorator when the decorator needs per-function mutable state that must persist across calls and be externally inspectable or resettable (e.g., process.call_count, circuit breaker state). Implement init to store the wrapped function and configuration, call for the wrapping behavior, and get if the decorator should work correctly on instance methods (to bind self). Function-based decorators are simpler for stateless concerns.","correct":"Use a class-based decorator when the decorator needs per-function mutable state that must persist across calls and be externally inspectable or resettable (e.g., process.call_count, circuit breaker state).","distractors":["@decorator above def f is syntactic sugar for f = decorator(f) which Python executes when it encounters the def statement during module parsing.","It calls functools.update_wrapper(wrapper, wrapped, assigned=WRAPPER_ASSIGNMENTS, updated=WRAPPER_UPDATES).","global declares that a name refers to the module-level namespace; nonlocal declares that a name refers to the nearest enclosing function scope (not global)."]},{"id":"python/decorators_and_closures#Q7-2664","section":"python","module":"python/decorators_and_closures","moduleName":"decorators and closures","qIndex":7,"difficulty":"intermediate","question":"What happens if you apply @lru_cache to an instance method without any workaround?","answerFull":"The cache key becomes (self, *args, kwargs). The lru_cache dictionary holds a strong reference to self, preventing garbage collection even after all other references to the instance are dropped. In a web server that creates short-lived objects per request, this is a memory leak — objects accumulate in the cache until maxsize evicts them or the process restarts. Fix by using cached_property for single computed values, methodtools.lru_cache (weak-reference keying), or restructuring the cache to class level with an explicit key that does not include self.","correct":"The cache key becomes (self, *args, kwargs).","distractors":["functools.partial(func, *args, kwargs) returns a new callable with some arguments pre-filled.","global declares that a name refers to the module-level namespace; nonlocal declares that a name refers to the nearest enclosing function scope (not global).","@decorator above def f is syntactic sugar for f = decorator(f) which Python executes when it encounters the def statement during module parsing."]},{"id":"python/decorators_and_closures#Q9-2665","section":"python","module":"python/decorators_and_closures","moduleName":"decorators and closures","qIndex":9,"difficulty":"intermediate","question":"Describe the late-binding closure bug and two ways to fix it.","answerFull":"The bug: closures capture the *variable* (cell reference), not its *value* at creation time. In a loop [lambda: i for i in range(5)], all five lambdas share one cell pointing to i; after the loop i == 4, so all return 4. Fix 1 — default argument binding: lambda i=i: i — default args are evaluated at function definition time, creating a fresh binding per iteration. Fix 2 — factory function: def make_f(i): return lambda: i — each call creates a new closure scope with its own i cell.","correct":"The bug: closures capture the *variable* (cell reference), not its *value* at creation time.","distractors":["global declares that a name refers to the module-level namespace; nonlocal declares that a name refers to the nearest enclosing function scope (not global).","@decorator above def f is syntactic sugar for f = decorator(f) which Python executes when it encounters the def statement during module parsing.","It calls functools.update_wrapper(wrapper, wrapped, assigned=WRAPPER_ASSIGNMENTS, updated=WRAPPER_UPDATES)."]},{"id":"python/decorators_and_closures#Q10-2666","section":"python","module":"python/decorators_and_closures","moduleName":"decorators and closures","qIndex":10,"difficulty":"intermediate","question":"How does stacking decorators work, and what order are they applied and executed?","answerFull":"Application order is bottom-up: the decorator closest to def is applied first, the topmost decorator last. This means @a on top and @b below is equivalent to f = a(b(f)). Execution order is the reverse — top-down: a's wrapper runs first, calls b's wrapper, which calls the original function. For FastAPI routes, @app.get must be topmost (outermost) so it receives the final wrapped function and can inspect its signature for dependency injection and schema generation.","correct":"Application order is bottom-up: the decorator closest to def is applied first, the topmost decorator last.","distractors":["Use a class-based decorator when the decorator needs per-function mutable state that must persist across calls and be externally inspectable or resettable (e.g., process.call_count, circuit breaker state).","@decorator above def f is syntactic sugar for f = decorator(f) which Python executes when it encounters the def statement during module parsing.","Most decorators return a wrapper that calls the original function (transparent wrapping)."]},{"id":"python/decorators_and_closures#Q12-2667","section":"python","module":"python/decorators_and_closures","moduleName":"decorators and closures","qIndex":12,"difficulty":"advanced","question":"What is functools.partial and when is it better than a closure?","answerFull":"functools.partial(func, *args, kwargs) returns a new callable with some arguments pre-filled. Prefer it over a closure when you only need to bind arguments (not add behavior), when you want the result to be introspectable (partial_obj.func, .args, .keywords), or when performance is critical (partial is C-implemented, ~15% faster than a Python wrapper). Use a closure when you need to add behavior around the call.","correct":"functools.partial(func, *args, kwargs) returns a new callable with some arguments pre-filled.","distractors":["Level 1 is the *factory function* (e.g., retry(max_attempts=3)) — it receives configuration and returns the decorator.","It calls functools.update_wrapper(wrapper, wrapped, assigned=WRAPPER_ASSIGNMENTS, updated=WRAPPER_UPDATES).","The cache key becomes (self, *args, kwargs)."]},{"id":"python/decorators_and_closures#Q14-2668","section":"python","module":"python/decorators_and_closures","moduleName":"decorators and closures","qIndex":14,"difficulty":"advanced","question":"Explain wrapped and why it matters.","answerFull":"functools.wraps sets wrapper.wrapped = func (the original function). This chain allows tooling to unwrap decorators: inspect.unwrap(f) follows wrapped links until it reaches a function with no wrapped. FastAPI uses inspect.unwrap to find the original handler's signature for dependency injection parsing. Without wrapped, FastAPI cannot discover path parameters and query dependencies, causing runtime errors or incorrect OpenAPI schemas.","correct":"functools.wraps sets wrapper.wrapped = func (the original function).","distractors":["Use a class-based decorator when the decorator needs per-function mutable state that must persist across calls and be externally inspectable or resettable (e.g., process.call_count, circuit breaker state).","Level 1 is the *factory function* (e.g., retry(max_attempts=3)) — it receives configuration and returns the decorator.","global declares that a name refers to the module-level namespace; nonlocal declares that a name refers to the nearest enclosing function scope (not global)."]},{"id":"python/decorators_and_closures#Q15-2669","section":"python","module":"python/decorators_and_closures","moduleName":"decorators and closures","qIndex":15,"difficulty":"advanced","question":"What is the difference between a decorator that wraps a function and one that replaces it entirely?","answerFull":"Most decorators return a wrapper that calls the original function (transparent wrapping). A *replacement decorator* returns something entirely different — e.g., @functools.total_ordering returns a modified class, @dataclass returns a new class, @property returns a descriptor object. For class decorators, replacement is common: @dataclass inspects and modifies the class, then returns it (or a new class). Understanding whether a decorator wraps or replaces is critical for debugging: if isinstance(obj, OriginalClass) returns False after decoration, the decorator replaced the class.","correct":"Most decorators return a wrapper that calls the original function (transparent wrapping).","distractors":["@decorator above def f is syntactic sugar for f = decorator(f) which Python executes when it encounters the def statement during module parsing.","It calls functools.update_wrapper(wrapper, wrapped, assigned=WRAPPER_ASSIGNMENTS, updated=WRAPPER_UPDATES).","Use a class-based decorator when the decorator needs per-function mutable state that must persist across calls and be externally inspectable or resettable (e.g., process.call_count, circuit breaker state)."]},{"id":"python/asyncio_and_event_loop#Q1-2670","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":1,"difficulty":"core","question":"What is a Python coroutine and how does it differ from a regular function?","answerFull":"A coroutine is a function declared with async def that can suspend execution at await points and resume later, preserving its local state between suspensions. Unlike a regular function which runs to completion when called, calling an async def function returns a coroutine object that does not execute until driven by an event loop or awaited by another coroutine. Under the hood, async def compiles to a generator-based state machine; await expr desugars to yield from expr.await(). The practical guidance: never call a coroutine without await or create_task — you will get a RuntimeWarning and the body will never run.","correct":"A coroutine is a function declared with async def that can suspend execution at await points and resume later, preserving its local state between suspensions.","distractors":["create_task schedules a coroutine as a Task but does not hold a reference to the Task object if the caller discards it.","anyio.TaskGroup.start(coro) is an awaitable that starts coro and waits until the coroutine calls task_status.started(value).","Future.await is a generator-based coroutine method."]},{"id":"python/asyncio_and_event_loop#Q2-2671","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":2,"difficulty":"core","question":"How does the asyncio event loop schedule and run coroutines?","answerFull":"The event loop maintains two queues: _ready (callbacks to run immediately) and _scheduled (timers). Each iteration it calls epoll_wait() (Linux), kqueue (macOS), or select() with a timeout equal to the nearest scheduled callback, collects I/O-ready file descriptors, resolves their associated Futures, runs all _ready callbacks (which includes Task.step calls that drive coroutines forward), and then processes expired timers. One event loop iteration takes roughly 5–50 µs on an idle modern server; a heavily loaded loop with thousands of ready sockets can stretch to low milliseconds. The key point for interviews: it is single-threaded and cooperative — there are no preemptions, only voluntary await-point yields.","correct":"The event loop maintains two queues: _ready (callbacks to run immediately) and _scheduled (timers).","distractors":["In Jupyter notebooks or libraries that cannot own the loop, use asyncio.ensure_future(coro) or loop.create_task(coro) to schedule without blocking the current thread.","Callbacks in _ready represent work that is already known to be runnable (either explicitly scheduled or awoken by a past I/O event).","Start with PYTHONASYNCIODEBUG=1 or loop.set_debug(True) — this logs slow callbacks (>100 ms by default), coroutines that were created but never awaited, and resources not properly closed."]},{"id":"python/asyncio_and_event_loop#Q3-2672","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":3,"difficulty":"core","question":"What is the difference between asyncio.gather() and asyncio.TaskGroup?","answerFull":"gather() runs awaitables concurrently and returns an ordered list of results; on the first exception it cancels all remaining awaitables and re-raises only that one exception (unless return_exceptions=True). TaskGroup (3.11+) is a structured concurrency scope: all tasks spawned inside the async with block are cancelled when any one fails, and all exceptions are collected into a single ExceptionGroup so callers can handle multiple simultaneous failures with except*. The practical guidance: prefer TaskGroup for new Python 3.11+ code because it enforces task lifetimes, makes error handling exhaustive, and composes correctly with cancellation.","correct":"gather() runs awaitables concurrently and returns an ordered list of results; on the first exception it cancels all remaining awaitables and re-raises only that one exception (unless return_exceptions=True).","distractors":["Enabled via asyncio.run(main(), debug=True) or PYTHONASYNCIODEBUG=1.","Apply asyncio.timeout() inside each individual task's coroutine, not around the entire TaskGroup.","asyncio.TaskGroup.create_task() returns an asyncio.Task handle; call .result() after the group exits to get the return value."]},{"id":"python/asyncio_and_event_loop#Q4-2673","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":4,"difficulty":"core","question":"What exactly happens when task.cancel() is called?","answerFull":"task.cancel() does not immediately stop the task. It schedules a CancelledError to be raised at the task's next await point. If the coroutine is currently suspended waiting on a Future, the Future is cancelled and CancelledError is thrown into the coroutine on its next send(). The coroutine can catch CancelledError in a try/finally block to perform cleanup, but must re-raise it so the Task marks itself as cancelled. task.cancelling() (3.11+) returns the count of pending cancel requests. The practical guidance: always use try/finally (not try/except CancelledError: return) for resource cleanup in long-running tasks.","correct":"task.cancel() does not immediately stop the task.","distractors":["Three features landed simultaneously: (1) asyncio.TaskGroup — the structured concurrency primitive, (2) asyncio.timeout() — composable cancel scope, (3) ExceptionGroup and except* — multi-exception handling syntax.","asyncio.timeout() is a context manager (a cancel scope) that can wrap any block of code, including a TaskGroup.","asyncio.timeout() creates a cancel scope that, when its deadline expires, cancels the current task."]},{"id":"python/asyncio_and_event_loop#Q5-2674","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":5,"difficulty":"core","question":"What is structured concurrency and why does it matter?","answerFull":"Structured concurrency is the principle that a spawned task cannot outlive the scope in which it was created. In asyncio, TaskGroup enforces this: the async with asyncio.TaskGroup() block does not exit until every task spawned inside it has finished or been cancelled. This eliminates entire classes of bugs: dangling tasks that consume memory after the parent has returned, fire-and-forget tasks whose exceptions are silently lost, and tasks that hold resources beyond their intended lifetime. Without structured concurrency (bare create_task everywhere), tracking which tasks are still running and ensuring clean shutdown requires manual bookkeeping. The practical guidance: use TaskGroup for any concurrent fan-out where you need to wait for all results or guarantee cleanup.","correct":"Structured concurrency is the principle that a spawned task cannot outlive the scope in which it was created.","distractors":["asyncio.timeout() is a context manager (a cancel scope) that can wrap any block of code, including a TaskGroup.","Structured concurrency means every spawned task has a well-defined scope: the scope does not exit until all tasks inside it complete or are cancelled.","This warning means a coroutine object was created (by calling async def fn()) but .send(None) was never called on it — the function body never executed."]},{"id":"python/asyncio_and_event_loop#Q6-2675","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":6,"difficulty":"intermediate","question":"How does asyncio handle CPU-bound work?","answerFull":"It does not handle CPU-bound work well by default. A CPU-bound operation inside async def blocks the entire event loop thread for its duration, preventing any other coroutine from running. The correct approach is to offload CPU work to a thread pool via asyncio.to_thread(fn, *args) (3.9+) or loop.run_in_executor(executor, fn, *args) with a ProcessPoolExecutor for true parallelism. For ML inference at scale, a common pattern is a dedicated inference process that communicates over a queue, with the event loop acting only as a dispatcher. The practical guidance: if a function takes more than ~5 ms of CPU time, do not call it directly in async def; wrap it with asyncio.to_thread.","correct":"It does not handle CPU-bound work well by default.","distractors":["Task.step is a bound method registered as a Handle in _ready (via call_soon).","anyio is an async compatibility layer that provides a unified API over asyncio and trio backends.","Structured concurrency means every spawned task has a well-defined scope: the scope does not exit until all tasks inside it complete or are cancelled."]},{"id":"python/asyncio_and_event_loop#Q7-2676","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":7,"difficulty":"intermediate","question":"What is the difference between asyncio.sleep(0) and asyncio.sleep(n)?","answerFull":"asyncio.sleep(0) yields control to the event loop for exactly one iteration and reschedules the current coroutine on _ready immediately. It has no timer overhead and resumes as fast as the event loop can cycle (~5–50 µs). asyncio.sleep(n) registers a timer callback at now + n seconds and the coroutine does not resume until that timer fires, regardless of how quickly the event loop iterates. Use sleep(0) inside CPU-intensive async def loops to prevent task starvation (give other tasks a chance to run). Use sleep(n) for actual time-based delays. The practical guidance: sprinkle await asyncio.sleep(0) in tight async loops that do not otherwise contain natural I/O yield points.","correct":"asyncio.sleep(0) yields control to the event loop for exactly one iteration and reschedules the current coroutine on _ready immediately.","distractors":["A coroutine is a function declared with async def that can suspend execution at await points and resume later, preserving its local state between suspensions.","asyncio.timeout() creates a cancel scope that, when its deadline expires, cancels the current task.","asyncio.shield prevents the inner coroutine from receiving CancelledError, but the outer awaiter does receive it."]},{"id":"python/asyncio_and_event_loop#Q8-2677","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":8,"difficulty":"intermediate","question":"How do async generators work and where are they useful?","answerFull":"An async def function containing yield is an async generator. It returns an AsyncGenerator[YieldType, SendType] object consumed with async for. Each iteration calls anext(), which drives the generator until the next yield, allowing await inside the body. They are useful for lazy streaming: paginated API responses, database cursor iteration, SSE event streams, and WebSocket message feeds. Unlike regular generators, they cannot be used with next() — only async for or explicit await gen.anext(). The practical guidance: use AsyncGenerator[T, None] as the return type annotation and always ensure the consumer iterates to exhaustion or explicitly closes the generator to trigger cleanup in finally blocks.","correct":"An async def function containing yield is an async generator.","distractors":["Future.await is a generator-based coroutine method.","asyncio.sleep(0) calls the @types.coroutine-decorated sleep0(), which executes a bare yield.","This warning means a coroutine object was created (by calling async def fn()) but .send(None) was never called on it — the function body never executed."]},{"id":"python/asyncio_and_event_loop#Q9-2678","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":9,"difficulty":"intermediate","question":"What is anyio and why would you use it instead of asyncio directly?","answerFull":"anyio is an async compatibility layer that provides a unified API over asyncio and trio backends. Its TaskGroup, move_on_after, and fail_after primitives work identically on both runtimes. FastAPI and Starlette use anyio internally, so application code that uses anyio primitives runs correctly regardless of which backend uvicorn or hypercorn is configured with. anyio also provides better cancellation semantics aligned with structured concurrency. The practical guidance: use anyio in library code that should remain backend-agnostic; in application code that is strictly asyncio (e.g., a service that runs only under uvicorn), direct asyncio is fine and simpler.","correct":"anyio is an async compatibility layer that provides a unified API over asyncio and trio backends.","distractors":["asyncio.Future is a low-level result placeholder: it can be in pending, done, or cancelled state, and callbacks can be added to it with add_done_callback.","On Linux, asyncio uses epoll via the selectors.EpollSelector; on macOS and BSD, it uses kqueue; on Windows, it uses IOCP (I/O Completion Ports) via the ProactorEventLoop (default on Windows since 3.8).","asyncio.Queue(maxsize=N) is the primary backpressure mechanism."]},{"id":"python/asyncio_and_event_loop#Q10-2679","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":10,"difficulty":"intermediate","question":"How do you debug a misbehaving async application?","answerFull":"Start with PYTHONASYNCIODEBUG=1 or loop.set_debug(True) — this logs slow callbacks (>100 ms by default), coroutines that were created but never awaited, and resources not properly closed. Set sys.set_coroutine_origin_tracking_depth(10) before creating coroutines to get full tracebacks in \"coroutine was never awaited\" warnings. For Jupyter notebooks, import nest_asyncio; nest_asyncio.apply() patches the loop to allow asyncio.run() inside a running loop. For production, instrument with OpenTelemetry spans around create_task calls to trace task fan-out. The practical guidance: always run with debug mode on in development; in production, set slow_callback_duration = 0.05 (50 ms) to catch accidental blocking code early.","correct":"Start with PYTHONASYNCIODEBUG=1 or loop.set_debug(True) — this logs slow callbacks (>100 ms by default), coroutines that were created but never awaited, and resources not properly closed.","distractors":["This warning means a coroutine object was created (by calling async def fn()) but .send(None) was never called on it — the function body never executed.","asyncio.Future is a low-level result placeholder: it can be in pending, done, or cancelled state, and callbacks can be added to it with add_done_callback.","_run_once() is the inner loop body in BaseEventLoop; the loop calls it in a while True."]},{"id":"python/asyncio_and_event_loop#Q12-2680","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":12,"difficulty":"advanced","question":"What causes \"RuntimeWarning: coroutine was never awaited\"?","answerFull":"This warning means a coroutine object was created (by calling async def fn()) but .send(None) was never called on it — the function body never executed. The most common cause is calling a coroutine function without await: result = fetch(url) instead of result = await fetch(url). In Python 3.11+, the traceback includes the creation site (if sys.set_coroutine_origin_tracking_depth is set). The warning does not become an error by default, so it can hide bugs silently. The practical guidance: treat this warning as an error in CI by setting PYTHONWARNINGS=error::RuntimeWarning.","correct":"This warning means a coroutine object was created (by calling async def fn()) but .send(None) was never called on it — the function body never executed.","distractors":["asyncio.sleep(0) yields control to the event loop for exactly one iteration and reschedules the current coroutine on _ready immediately.","A coroutine is a function declared with async def that can suspend execution at await points and resume later, preserving its local state between suspensions.","create_task schedules a coroutine as a Task but does not hold a reference to the Task object if the caller discards it."]},{"id":"python/asyncio_and_event_loop#Q13-2681","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":13,"difficulty":"advanced","question":"How does backpressure work in asyncio?","answerFull":"asyncio.Queue(maxsize=N) is the primary backpressure mechanism. When the queue is full, await queue.put(item) suspends the producer until a consumer calls queue.get(). This prevents a fast producer from buffering unlimited items in memory. For network-level backpressure, asyncio.StreamWriter.write() buffers data internally; await writer.drain() suspends the coroutine if the transport's write buffer exceeds its high-water mark. The practical guidance: any producer/consumer pipeline with mismatched speeds should use a bounded Queue; calling write() in a loop without drain() is a common memory leak pattern in WebSocket servers.","correct":"asyncio.Queue(maxsize=N) is the primary backpressure mechanism.","distractors":["asyncio.timeout(n) (3.11+) returns a context manager.","asyncio.Future is a low-level result placeholder: it can be in pending, done, or cancelled state, and callbacks can be added to it with add_done_callback.","asyncio.shield(coro_or_future) wraps an awaitable so that external cancellation of the outer Task does not propagate into the shielded operation."]},{"id":"python/asyncio_and_event_loop#Q14-2682","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":14,"difficulty":"advanced","question":"What is asyncio.timeout() and how does it differ from asyncio.wait_for()?","answerFull":"asyncio.timeout(delay) (3.11+) is a context manager that cancels any awaitable inside its block after delay seconds, raising asyncio.TimeoutError. asyncio.wait_for(coro, timeout) wraps a single coroutine and raises TimeoutError on expiry. The key behavioral difference: timeout() is composable — you can nest multiple timeout() context managers, and the inner one raises TimeoutError independently of the outer one. wait_for also has a historical bug in Python < 3.12 where cancellation of the outer task could cause the wrapped coroutine to be swallowed rather than properly cancelled. The practical guidance: prefer asyncio.timeout() for new 3.11+ code; use anyio.fail_after() for portable code.","correct":"asyncio.timeout(delay) (3.11+) is a context manager that cancels any awaitable inside its block after delay seconds, raising asyncio.TimeoutError.","distractors":["asyncio.shield(coro_or_future) wraps an awaitable so that external cancellation of the outer Task does not propagate into the shielded operation.","asyncio.timeout() is a context manager (a cancel scope) that can wrap any block of code, including a TaskGroup.","asyncio.timeout(n) (3.11+) returns a context manager."]},{"id":"python/asyncio_and_event_loop#Q15-2683","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":15,"difficulty":"advanced","question":"How do you implement a graceful shutdown for a FastAPI application?","answerFull":"FastAPI exposes a lifespan context manager (3.0+) where you initialize resources on entry and perform cleanup on exit. During shutdown, uvicorn sends SIGTERM, which triggers the lifespan context manager's exit. Inside the exit, cancel all background tasks, wait for in-flight requests to complete with a deadline, and close database connection pools. Use asyncio.gather(*tasks, return_exceptions=True) to wait for all background tasks, then flush any pending queue items. The practical guidance: always store asyncio.Task references in a set and call task.cancel() with await asyncio.gather in the shutdown path — stray tasks that hold DB connections are the most common cause of connection pool exhaustion on redeploys.","correct":"FastAPI exposes a lifespan context manager (3.0+) where you initialize resources on entry and perform cleanup on exit.","distractors":["asyncio.timeout() is a context manager (a cancel scope) that can wrap any block of code, including a TaskGroup.","Start with PYTHONASYNCIODEBUG=1 or loop.set_debug(True) — this logs slow callbacks (>100 ms by default), coroutines that were created but never awaited, and resources not properly closed.","Structured concurrency means every spawned task has a well-defined scope: the scope does not exit until all tasks inside it complete or are cancelled."]},{"id":"python/asyncio_and_event_loop#Q16-2684","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":16,"difficulty":"advanced","question":"What is the difference between a Future and a Task?","answerFull":"asyncio.Future is a low-level result placeholder: it can be in pending, done, or cancelled state, and callbacks can be added to it with add_done_callback. It is manually resolved by calling future.set_result(value) or future.set_exception(exc). asyncio.Task is a Future subclass that wraps a coroutine and drives it automatically by calling coro.send(None) in Task.step; the Task resolves itself when the coroutine returns or raises. In practice, application code almost never creates bare Futures; they are created by low-level transport and protocol implementations. The practical guidance: always use create_task for coroutines; only use bare Future when bridging with callback-based code (e.g., wrapping a callback-style API into an awaitable).","correct":"asyncio.Future is a low-level result placeholder: it can be in pending, done, or cancelled state, and callbacks can be added to it with add_done_callback.","distractors":["The event loop maintains two queues: _ready (callbacks to run immediately) and _scheduled (timers).","asyncio.shield(coro_or_future) wraps an awaitable so that external cancellation of the outer Task does not propagate into the shielded operation.","In Jupyter notebooks or libraries that cannot own the loop, use asyncio.ensure_future(coro) or loop.create_task(coro) to schedule without blocking the current thread."]},{"id":"python/asyncio_and_event_loop#Q17-2685","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":17,"difficulty":"advanced","question":"How does asyncio perform I/O multiplexing?","answerFull":"On Linux, asyncio uses epoll via the selectors.EpollSelector; on macOS and BSD, it uses kqueue; on Windows, it uses IOCP (I/O Completion Ports) via the ProactorEventLoop (default on Windows since 3.8). The event loop registers file descriptors with the selector and calls select() with a timeout. When a file descriptor becomes readable or writable, the selector returns it, the loop resolves the associated Future (e.g., a StreamReader future), which schedules the waiting coroutine. The latency from a file descriptor becoming ready to the coroutine resuming is roughly one event loop iteration: 5–50 µs on idle, dominated by the Python overhead of Task.step. The practical guidance: uvloop replaces the selector with libuv's event loop, reducing per-connection overhead by 2–4x on I/O-heavy workloads.","correct":"On Linux, asyncio uses epoll via the selectors.EpollSelector; on macOS and BSD, it uses kqueue; on Windows, it uses IOCP (I/O Completion Ports) via the ProactorEventLoop (default on Windows since 3.8).","distractors":["FastAPI exposes a lifespan context manager (3.0+) where you initialize resources on entry and perform cleanup on exit.","asyncio.Queue(maxsize=N) is the primary backpressure mechanism.","asyncio.shield(coro_or_future) wraps an awaitable so that external cancellation of the outer Task does not propagate into the shielded operation."]},{"id":"python/asyncio_and_event_loop#Q18-2686","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":18,"difficulty":"advanced","question":"What is shield() and when should you use it?","answerFull":"asyncio.shield(coro_or_future) wraps an awaitable so that external cancellation of the outer Task does not propagate into the shielded operation. If the outer task is cancelled, the await asyncio.shield(inner) raises CancelledError in the outer task, but inner continues running. This is useful for operations that must complete regardless of the caller's lifecycle: database commits, audit log writes, payment captures. Important caveat: the shielded task keeps running even after the outer task is cancelled, potentially leaking resources. The practical guidance: use shield sparingly and only for truly non-cancellable operations; prefer designing around structured concurrency where possible, and always ensure the shielded task is tracked and awaited during shutdown.","correct":"asyncio.shield(coro_or_future) wraps an awaitable so that external cancellation of the outer Task does not propagate into the shielded operation.","distractors":["anyio.TaskGroup.start(coro) is an awaitable that starts coro and waits until the coroutine calls task_status.started(value).","asyncio.timeout(delay) (3.11+) is a context manager that cancels any awaitable inside its block after delay seconds, raising asyncio.TimeoutError.","In Jupyter notebooks or libraries that cannot own the loop, use asyncio.ensure_future(coro) or loop.create_task(coro) to schedule without blocking the current thread."]},{"id":"python/asyncio_and_event_loop#Q1-2687","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":1,"difficulty":"core","question":"What is _run_once() and what are the four phases it executes?","answerFull":"_run_once() is the inner loop body in BaseEventLoop; the loop calls it in a while True. Phase 1 drains expired TimerHandles from the min-heap into _ready. Phase 2 computes a poll timeout (0 if _ready is non-empty, otherwise time to next timer). Phase 3 calls selector.select(timeout) — the only blocking syscall. Phase 4 translates returned I/O events into Handle objects appended to _ready and then drains _ready up to the pre-snapshot count.","correct":"_run_once() is the inner loop body in BaseEventLoop; the loop calls it in a while True.","distractors":["asyncio.sleep(0) yields control to the event loop for exactly one iteration and reschedules the current coroutine on _ready immediately.","Start with PYTHONASYNCIODEBUG=1 or loop.set_debug(True) — this logs slow callbacks (>100 ms by default), coroutines that were created but never awaited, and resources not properly closed.","asyncio.shield prevents the inner coroutine from receiving CancelledError, but the outer awaiter does receive it."]},{"id":"python/asyncio_and_event_loop#Q2-2688","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":2,"difficulty":"core","question":"Why does asyncio process the entire _ready deque before calling selector.select?","answerFull":"Callbacks in _ready represent work that is already known to be runnable (either explicitly scheduled or awoken by a past I/O event). Skipping to the selector before draining them would mean re-entering the kernel unnecessarily and potentially missing an already-enqueued done-callback. The cost is that a burst of call_soon callbacks can starve the selector — there is no preemption.","correct":"Callbacks in _ready represent work that is already known to be runnable (either explicitly scheduled or awoken by a past I/O event).","distractors":["Task.step is a bound method registered as a Handle in _ready (via call_soon).","It calls self._selector.register(fd, selectors.EVENT_READ, (cb, None)) (or modify if already registered for write).","Apply asyncio.timeout() inside each individual task's coroutine, not around the entire TaskGroup."]},{"id":"python/asyncio_and_event_loop#Q3-2689","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":3,"difficulty":"core","question":"Explain how Future.await suspends a coroutine.","answerFull":"Future.await is a generator-based coroutine method. When not self.done(), it executes yield self — the SEND bytecode opcode sees a yielded value from the iterator and passes it up through the coroutine chain to Task.step. The Task checks result._asyncio_future_blocking, registers Task.wakeup as a done-callback via add_done_callback, and returns. When Future.set_result() fires, it calls loop.call_soon(cb) for each done-callback, re-enqueueing the Task in _ready.","correct":"Future.await is a generator-based coroutine method.","distractors":["asyncio.shield prevents the inner coroutine from receiving CancelledError, but the outer awaiter does receive it.","asyncio.sleep(0) calls the @types.coroutine-decorated sleep0(), which executes a bare yield.","This warning means a coroutine object was created (by calling async def fn()) but .send(None) was never called on it — the function body never executed."]},{"id":"python/asyncio_and_event_loop#Q4-2690","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":4,"difficulty":"core","question":"What is asyncio.sleep(0) doing at the bytecode level?","answerFull":"asyncio.sleep(0) calls the @types.coroutine-decorated sleep0(), which executes a bare yield. This causes Task.step to receive None as the yielded result (not a Future), which triggers the branch result is None → loop.call_soon(self.step). The Task is re-added to _ready and resumes on the *next* tick. Exactly one selector pass happens between the suspend and the resume (assuming nothing else is in _ready).","correct":"asyncio.sleep(0) calls the @types.coroutine-decorated sleep0(), which executes a bare yield.","distractors":["A coroutine is a function declared with async def that can suspend execution at await points and resume later, preserving its local state between suspensions.","Future.await is a generator-based coroutine method.","asyncio.shield prevents the inner coroutine from receiving CancelledError, but the outer awaiter does receive it."]},{"id":"python/asyncio_and_event_loop#Q5-2691","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":5,"difficulty":"core","question":"Why is call_soon not thread-safe, and how does call_soon_threadsafe fix it?","answerFull":"call_soon appends a Handle to _ready (a deque) and does not hold a lock. A concurrent write from another thread while the event loop thread is iterating _ready in Phase 5 would corrupt the deque (CPython's deque append is thread-safe at the C level, but the loop's ntodo snapshot would miss the appended item). call_soon_threadsafe appends under self._write_lock and then writes one byte to self._ssock (a loopback socket pair). The event loop's selector watches self._csock; the byte makes select() return immediately, so the new callback runs in the next tick without waiting for the full timeout.","correct":"call_soon appends a Handle to _ready (a deque) and does not hold a lock.","distractors":["It does not handle CPU-bound work well by default.","The event loop maintains two queues: _ready (callbacks to run immediately) and _scheduled (timers).","Task.step is a bound method registered as a Handle in _ready (via call_soon)."]},{"id":"python/asyncio_and_event_loop#Q6-2692","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":6,"difficulty":"intermediate","question":"What is Task.step and when is it called?","answerFull":"Task.step is a bound method registered as a Handle in _ready (via call_soon). When the Handle fires, step calls coro.send(None) to drive the coroutine one step. If the coroutine yields a pending Future, step registers itself as a done-callback on that Future. If the coroutine raises StopIteration, step calls Future.set_result. If the coroutine raises any exception, step calls Future.set_exception. The Task is never in _ready while it is waiting on a Future — it is \"parked\" on the Future's _callbacks list.","correct":"Task.step is a bound method registered as a Handle in _ready (via call_soon).","distractors":["call_soon appends a Handle to _ready (a deque) and does not hold a lock.","It does not handle CPU-bound work well by default.","Callbacks in _ready represent work that is already known to be runnable (either explicitly scheduled or awoken by a past I/O event)."]},{"id":"python/asyncio_and_event_loop#Q8-2693","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":8,"difficulty":"intermediate","question":"Why is uvloop 2-4x faster than SelectorEventLoop?","answerFull":"uvloop replaces the Python-level _run_once with a Cython extension calling libuv's uv_run(). All timer management, I/O polling, and callback dispatch happen in C with no Python frame overhead per callback. DNS resolution uses uv_getaddrinfo (truly async, non-blocking), while SelectorEventLoop calls socket.getaddrinfo via a thread-pool executor. The GIL is released for the entire libuv loop iteration, not just the select() syscall.","correct":"uvloop replaces the Python-level _run_once with a Cython extension calling libuv's uv_run().","distractors":["asyncio.Future is a low-level result placeholder: it can be in pending, done, or cancelled state, and callbacks can be added to it with add_done_callback.","Enabled via asyncio.run(main(), debug=True) or PYTHONASYNCIODEBUG=1.","_run_once() is the inner loop body in BaseEventLoop; the loop calls it in a while True."]},{"id":"python/asyncio_and_event_loop#Q9-2694","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":9,"difficulty":"intermediate","question":"What happens when two coroutines await the same Future?","answerFull":"Both register their Task.wakeup as done-callbacks via add_done_callback. When Future.set_result(v) fires, the loop calls call_soon for each callback in self._callbacks, appending all waiting Tasks to _ready. All awaiting Tasks will resume in the next (or same, depending on snapshot) tick. The Future stores one result; all awaiters receive the same value. This is the mechanism behind asyncio.Event — multiple Tasks wait on a single Future (the event's internal future).","correct":"Both register their Task.wakeup as done-callbacks via add_done_callback.","distractors":["Future.await is a generator-based coroutine method.","Callbacks in _ready represent work that is already known to be runnable (either explicitly scheduled or awoken by a past I/O event).","It calls self._selector.register(fd, selectors.EVENT_READ, (cb, None)) (or modify if already registered for write)."]},{"id":"python/asyncio_and_event_loop#Q11-2695","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":11,"difficulty":"advanced","question":"How does asyncio.timeout(seconds) work at the loop level?","answerFull":"asyncio.timeout(n) (3.11+) returns a context manager. On entry, it schedules a call_later(n, cancel_task) callback in _scheduled. If the inner coroutine finishes before the deadline, it calls h.cancel() on the TimerHandle (marks it cancelled so _run_once skips it when dequeued). If time expires, cancel_task fires task.cancel() which raises CancelledError into the task via Task.step(exc=CancelledError()).","correct":"asyncio.timeout(n) (3.11+) returns a context manager.","distractors":["Three features landed simultaneously: (1) asyncio.TaskGroup — the structured concurrency primitive, (2) asyncio.timeout() — composable cancel scope, (3) ExceptionGroup and except* — multi-exception handling syntax.","asyncio.timeout() creates a cancel scope that, when its deadline expires, cancels the current task.","asyncio.sleep(0) yields control to the event loop for exactly one iteration and reschedules the current coroutine on _ready immediately."]},{"id":"python/asyncio_and_event_loop#Q12-2696","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":12,"difficulty":"advanced","question":"What does loop.add_reader(fd, cb) do internally?","answerFull":"It calls self._selector.register(fd, selectors.EVENT_READ, (cb, None)) (or modify if already registered for write). The key–data pair maps the file descriptor to (reader_handle, writer_handle). In Phase 4 of _run_once, _process_events iterates the list returned by selector.select, looks up the key data, and appends the appropriate handle to _ready.","correct":"It calls self._selector.register(fd, selectors.EVENT_READ, (cb, None)) (or modify if already registered for write).","distractors":["The event loop maintains two queues: _ready (callbacks to run immediately) and _scheduled (timers).","Task.step is a bound method registered as a Handle in _ready (via call_soon).","Start with PYTHONASYNCIODEBUG=1 or loop.set_debug(True) — this logs slow callbacks (>100 ms by default), coroutines that were created but never awaited, and resources not properly closed."]},{"id":"python/asyncio_and_event_loop#Q13-2697","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":13,"difficulty":"advanced","question":"How do you run asyncio code from a synchronous context when a loop is already running?","answerFull":"In Jupyter notebooks or libraries that cannot own the loop, use asyncio.ensure_future(coro) or loop.create_task(coro) to schedule without blocking the current thread. To *get a result synchronously* from outside a running loop, use nest_asyncio.apply() (patches the loop to allow re-entry) or run the coroutine in a new thread with its own loop: asyncio.run_coroutine_threadsafe(coro, loop).result().","correct":"In Jupyter notebooks or libraries that cannot own the loop, use asyncio.ensure_future(coro) or loop.create_task(coro) to schedule without blocking the current thread.","distractors":["asyncio.TaskGroup.create_task() returns an asyncio.Task handle; call .result() after the group exits to get the return value.","asyncio.shield(coro_or_future) wraps an awaitable so that external cancellation of the outer Task does not propagate into the shielded operation.","Apply asyncio.timeout() inside each individual task's coroutine, not around the entire TaskGroup."]},{"id":"python/asyncio_and_event_loop#Q14-2698","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":14,"difficulty":"advanced","question":"What is the asyncio debug mode and what does it catch?","answerFull":"Enabled via asyncio.run(main(), debug=True) or PYTHONASYNCIODEBUG=1. It: (1) logs all callbacks taking longer than 0.1 s with a traceback; (2) records the source traceback when Futures and Tasks are created (expensive but invaluable for \"Task was destroyed but it is pending\" warnings); (3) enables ResourceWarning for unclosed transports and event loops; (4) checks that coroutines are awaited (warns on unawaited coroutines). Performance overhead is ~10-15%.","correct":"Enabled via asyncio.run(main(), debug=True) or PYTHONASYNCIODEBUG=1.","distractors":["_run_once() is the inner loop body in BaseEventLoop; the loop calls it in a while True.","On Linux, asyncio uses epoll via the selectors.EpollSelector; on macOS and BSD, it uses kqueue; on Windows, it uses IOCP (I/O Completion Ports) via the ProactorEventLoop (default on Windows since 3.8).","asyncio.Queue(maxsize=N) is the primary backpressure mechanism."]},{"id":"python/asyncio_and_event_loop#Q15-2699","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":15,"difficulty":"advanced","question":"Why can asyncio not parallelize CPU-bound work with threads, and what is the fix?","answerFull":"The GIL ensures only one Python thread runs bytecode at any instant. Two threads running pure Python are serialized — one waits while the other holds the GIL. loop.run_in_executor(ThreadPoolExecutor) is only effective for I/O-bound work that releases the GIL during the syscall (file read, network read). For CPU-bound work (NumPy with GIL held, regex, JSON parsing), the fix is ProcessPoolExecutor — separate processes have independent GILs. In CPython 3.13+ with PYTHON_GIL=0 (PEP 703 free-threading), multiple threads can run Python bytecode concurrently, but asyncio itself is still single-threaded by design.","correct":"The GIL ensures only one Python thread runs bytecode at any instant.","distractors":["anyio is an async compatibility layer that provides a unified API over asyncio and trio backends.","On Linux, asyncio uses epoll via the selectors.EpollSelector; on macOS and BSD, it uses kqueue; on Windows, it uses IOCP (I/O Completion Ports) via the ProactorEventLoop (default on Windows since 3.8).","asyncio.timeout(delay) (3.11+) is a context manager that cancels any awaitable inside its block after delay seconds, raising asyncio.TimeoutError."]},{"id":"python/asyncio_and_event_loop#Q16-2700","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":16,"difficulty":"advanced","question":"How does structured concurrency in asyncio.TaskGroup differ from asyncio.gather?","answerFull":"TaskGroup (3.11+) is a context manager that owns its child tasks. If any child raises an unhandled exception, TaskGroup cancels all remaining siblings and re-raises as an ExceptionGroup. The parent waits for all siblings to finish cancellation before propagating the exception. asyncio.gather by default swallows exceptions unless return_exceptions=False, and does not automatically cancel siblings on failure; you must inspect results manually. TaskGroup follows the structured concurrency principle: tasks cannot outlive the scope that created them.","correct":"TaskGroup (3.11+) is a context manager that owns its child tasks.","distractors":["Three features landed simultaneously: (1) asyncio.TaskGroup — the structured concurrency primitive, (2) asyncio.timeout() — composable cancel scope, (3) ExceptionGroup and except* — multi-exception handling syntax.","Apply asyncio.timeout() inside each individual task's coroutine, not around the entire TaskGroup.","asyncio.timeout() is a context manager (a cancel scope) that can wrap any block of code, including a TaskGroup."]},{"id":"python/asyncio_and_event_loop#Q1-2701","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":1,"difficulty":"core","question":"What is structured concurrency and how does it differ from unstructured concurrency?","answerFull":"Structured concurrency means every spawned task has a well-defined scope: the scope does not exit until all tasks inside it complete or are cancelled. Unstructured concurrency (bare create_task) lets tasks outlive their launch site, silently swallowing exceptions and preventing reliable cancellation. The difference is analogous to structured vs goto-based control flow in imperative code.","correct":"Structured concurrency means every spawned task has a well-defined scope: the scope does not exit until all tasks inside it complete or are cancelled.","distractors":["Three features landed simultaneously: (1) asyncio.TaskGroup — the structured concurrency primitive, (2) asyncio.timeout() — composable cancel scope, (3) ExceptionGroup and except* — multi-exception handling syntax.","CancelledError is delivered to the task currently awaiting inside the TaskGroup.","Structured concurrency is the principle that a spawned task cannot outlive the scope in which it was created."]},{"id":"python/asyncio_and_event_loop#Q2-2702","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":2,"difficulty":"core","question":"Why is asyncio.create_task() dangerous without tracking?","answerFull":"create_task schedules a coroutine as a Task but does not hold a reference to the Task object if the caller discards it. Python's garbage collector can destroy the Task before it completes, and any exception raised by the Task is only logged to stderr via a warning — it is never raised to the caller. Use TaskGroup or explicitly store and await task handles to avoid this.","correct":"create_task schedules a coroutine as a Task but does not hold a reference to the Task object if the caller discards it.","distractors":["Apply asyncio.timeout() inside each individual task's coroutine, not around the entire TaskGroup.","anyio.TaskGroup.start(coro) is an awaitable that starts coro and waits until the coroutine calls task_status.started(value).","asyncio.shield prevents the inner coroutine from receiving CancelledError, but the outer awaiter does receive it."]},{"id":"python/asyncio_and_event_loop#Q4-2703","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":4,"difficulty":"core","question":"What is ExceptionGroup and how do you handle it with except*?","answerFull":"ExceptionGroup [3.11] is a new built-in that wraps multiple exceptions into a single value. except* ValueError as eg catches all ValueError instances inside any nested ExceptionGroup (recursively), giving them to you as eg.exceptions — a tuple of matching exceptions. Multiple except* clauses can handle different types from the same group. Unlike regular except, except* does not short-circuit after the first match; all clauses run.","correct":"ExceptionGroup [3.11] is a new built-in that wraps multiple exceptions into a single value.","distractors":["It does not handle CPU-bound work well by default.","call_soon appends a Handle to _ready (a deque) and does not hold a lock.","asyncio.shield(coro_or_future) wraps an awaitable so that external cancellation of the outer Task does not propagate into the shielded operation."]},{"id":"python/asyncio_and_event_loop#Q5-2704","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":5,"difficulty":"core","question":"How does asyncio.timeout() [3.11] differ from asyncio.wait_for()?","answerFull":"asyncio.timeout() is a context manager (a cancel scope) that can wrap any block of code, including a TaskGroup. wait_for() is a function that wraps a single coroutine and does not compose well with TaskGroup. Additionally, timeout() correctly interacts with the cancellation mechanism — it translates only its own cancellation to TimeoutError, not external cancellations, avoiding double-cancel edge cases present in older wait_for implementations.","correct":"asyncio.timeout() is a context manager (a cancel scope) that can wrap any block of code, including a TaskGroup.","distractors":["TaskGroup (3.11+) is a context manager that owns its child tasks.","Combine asyncio.Semaphore with TaskGroup.","anyio.CancelScope is anyio's cancel scope primitive."]},{"id":"python/asyncio_and_event_loop#Q6-2705","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":6,"difficulty":"intermediate","question":"What happens to a TaskGroup when the outer task is cancelled (e.g., client disconnects)?","answerFull":"CancelledError is delivered to the task currently awaiting inside the TaskGroup. The TaskGroup.aexit handler catches this, cancels all child tasks, waits for them to finish, then re-raises CancelledError. The result is that client disconnect reliably cleans up all in-flight sub-tasks — database cursors, HTTP connections, locks — before the request coroutine exits.","correct":"CancelledError is delivered to the task currently awaiting inside the TaskGroup.","distractors":["Check scope.cancelled_caught after the with block exits.","anyio.TaskGroup.start(coro) is an awaitable that starts coro and waits until the coroutine calls task_status.started(value).","Apply asyncio.timeout() inside each individual task's coroutine, not around the entire TaskGroup."]},{"id":"python/asyncio_and_event_loop#Q7-2706","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":7,"difficulty":"intermediate","question":"What is anyio.CancelScope and when should you use it over asyncio.timeout()?","answerFull":"anyio.CancelScope is anyio's cancel scope primitive. Use it when you need: (a) backend-agnostic code (anyio supports both asyncio and Trio), (b) scope.cancel() to cancel a block from within (early-exit pattern), (c) scope.shield = True to protect from external cancellation, or (d) move_on_after / fail_after convenience wrappers. Use asyncio.timeout() in asyncio-only code where anyio is not a dependency.","correct":"anyio.CancelScope is anyio's cancel scope primitive.","distractors":["asyncio.timeout() is a context manager (a cancel scope) that can wrap any block of code, including a TaskGroup.","anyio is an async compatibility layer that provides a unified API over asyncio and trio backends.","Apply asyncio.timeout() inside each individual task's coroutine, not around the entire TaskGroup."]},{"id":"python/asyncio_and_event_loop#Q8-2707","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":8,"difficulty":"intermediate","question":"How do you run tasks with bounded concurrency using TaskGroup?","answerFull":"Combine asyncio.Semaphore with TaskGroup. The semaphore limits how many tasks run concurrently while the TaskGroup ensures all tasks complete before the scope exits. Do not use a separate semaphore in conjunction with gather; that pattern does not propagate exceptions reliably.","correct":"Combine asyncio.Semaphore with TaskGroup.","distractors":["TaskGroup (3.11+) is a context manager that owns its child tasks.","Structured concurrency means every spawned task has a well-defined scope: the scope does not exit until all tasks inside it complete or are cancelled.","Three features landed simultaneously: (1) asyncio.TaskGroup — the structured concurrency primitive, (2) asyncio.timeout() — composable cancel scope, (3) ExceptionGroup and except* — multi-exception handling syntax."]},{"id":"python/asyncio_and_event_loop#Q9-2708","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":9,"difficulty":"intermediate","question":"What is the tg.start() pattern in anyio and when is it needed?","answerFull":"anyio.TaskGroup.start(coro) is an awaitable that starts coro and waits until the coroutine calls task_status.started(value). This is the standard pattern for servers and background workers that need to signal \"I am ready and listening\" before the caller proceeds. asyncio.TaskGroup does not have a direct equivalent; the closest is using an asyncio.Event to synchronize readiness.","correct":"anyio.TaskGroup.start(coro) is an awaitable that starts coro and waits until the coroutine calls task_status.started(value).","distractors":["In Jupyter notebooks or libraries that cannot own the loop, use asyncio.ensure_future(coro) or loop.create_task(coro) to schedule without blocking the current thread.","Structured concurrency means every spawned task has a well-defined scope: the scope does not exit until all tasks inside it complete or are cancelled.","asyncio.TaskGroup.create_task() returns an asyncio.Task handle; call .result() after the group exits to get the return value."]},{"id":"python/asyncio_and_event_loop#Q10-2709","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":10,"difficulty":"intermediate","question":"Why should asyncio.shield not be used for long operations?","answerFull":"asyncio.shield prevents the inner coroutine from receiving CancelledError, but the outer awaiter does receive it. If the outer task is cancelled, the awaiter exits immediately; the shielded task continues running as a detached fire-and-forget task. This is exactly the unstructured concurrency problem TaskGroup is designed to prevent. Use shield only for short, critical cleanup operations (milliseconds, not seconds).","correct":"asyncio.shield prevents the inner coroutine from receiving CancelledError, but the outer awaiter does receive it.","distractors":["CancelledError is delivered to the task currently awaiting inside the TaskGroup.","asyncio.sleep(0) calls the @types.coroutine-decorated sleep0(), which executes a bare yield.","Apply asyncio.timeout() inside each individual task's coroutine, not around the entire TaskGroup."]},{"id":"python/asyncio_and_event_loop#Q11-2710","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":11,"difficulty":"advanced","question":"How do you detect that a CancelScope absorbed a cancellation in anyio?","answerFull":"Check scope.cancelled_caught after the with block exits. If True, the scope's deadline or explicit cancel absorbed a cancellation. Use this to implement fallback logic: python with anyio.move_on_after(1.0) as scope: result = await slow_api_call() if scope.cancelled_caught: result = default_value # Deadline hit; use fallback","correct":"Check scope.cancelled_caught after the with block exits.","distractors":["anyio.TaskGroup.start(coro) is an awaitable that starts coro and waits until the coroutine calls task_status.started(value).","anyio is an async compatibility layer that provides a unified API over asyncio and trio backends.","asyncio.TaskGroup.create_task() returns an asyncio.Task handle; call .result() after the group exits to get the return value."]},{"id":"python/asyncio_and_event_loop#Q12-2711","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":12,"difficulty":"advanced","question":"What is the difference between start_soon and create_task return values?","answerFull":"asyncio.TaskGroup.create_task() returns an asyncio.Task handle; call .result() after the group exits to get the return value. anyio.TaskGroup.start_soon() returns None — there is no handle. To collect results with anyio, use a shared container (list, dict) mutated from inside each task, which is safe because asyncio is single-threaded and tasks only run one at a time.","correct":"asyncio.TaskGroup.create_task() returns an asyncio.Task handle; call .result() after the group exits to get the return value.","distractors":["anyio.TaskGroup.start(coro) is an awaitable that starts coro and waits until the coroutine calls task_status.started(value).","Combine asyncio.Semaphore with TaskGroup.","In Jupyter notebooks or libraries that cannot own the loop, use asyncio.ensure_future(coro) or loop.create_task(coro) to schedule without blocking the current thread."]},{"id":"python/asyncio_and_event_loop#Q13-2712","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":13,"difficulty":"advanced","question":"How does TaskGroup interact with asyncio.timeout() when both are nested?","answerFull":"asyncio.timeout() creates a cancel scope that, when its deadline expires, cancels the current task. If the current task is waiting inside a TaskGroup, the TaskGroup's aexit receives the CancelledError, cancels all child tasks, waits for them, then re-raises CancelledError. The asyncio.timeout() context manager then translates this specific CancelledError (from its own deadline) into TimeoutError. The result: a single TimeoutError is raised to the caller with all child tasks cleanly cancelled.","correct":"asyncio.timeout() creates a cancel scope that, when its deadline expires, cancels the current task.","distractors":["Apply asyncio.timeout() inside each individual task's coroutine, not around the entire TaskGroup.","asyncio.timeout() is a context manager (a cancel scope) that can wrap any block of code, including a TaskGroup.","anyio.CancelScope is anyio's cancel scope primitive."]},{"id":"python/asyncio_and_event_loop#Q14-2713","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":14,"difficulty":"advanced","question":"In production, how do you prevent a single slow downstream from holding up all tasks in a group?","answerFull":"Apply asyncio.timeout() inside each individual task's coroutine, not around the entire TaskGroup. Each task gets its own deadline. If one task times out, it raises TimeoutError inside the group, which cancels all siblings — the caller gets an ExceptionGroup containing one TimeoutError. If you want the group to complete even when one task times out, use return_exceptions-style try/except inside each task and collect errors manually.","correct":"Apply asyncio.timeout() inside each individual task's coroutine, not around the entire TaskGroup.","distractors":["asyncio.TaskGroup.create_task() returns an asyncio.Task handle; call .result() after the group exits to get the return value.","asyncio.timeout(delay) (3.11+) is a context manager that cancels any awaitable inside its block after delay seconds, raising asyncio.TimeoutError.","CancelledError is delivered to the task currently awaiting inside the TaskGroup."]},{"id":"python/asyncio_and_event_loop#Q15-2714","section":"python","module":"python/asyncio_and_event_loop","moduleName":"asyncio and event loop","qIndex":15,"difficulty":"advanced","question":"What changed in Python 3.11 that makes it the baseline for structured concurrency?","answerFull":"Three features landed simultaneously: (1) asyncio.TaskGroup — the structured concurrency primitive, (2) asyncio.timeout() — composable cancel scope, (3) ExceptionGroup and except* — multi-exception handling syntax. All three are interdependent; TaskGroup raises ExceptionGroup, which requires except* to handle properly. Backporting is possible via exceptiongroup on PyPI, but the native 3.11 implementation is tightly integrated with the CPython C extension layer.","correct":"Three features landed simultaneously: (1) asyncio.TaskGroup — the structured concurrency primitive, (2) asyncio.timeout() — composable cancel scope, (3) ExceptionGroup and except* — multi-exception handling syntax.","distractors":["asyncio.timeout() is a context manager (a cancel scope) that can wrap any block of code, including a TaskGroup.","anyio.CancelScope is anyio's cancel scope primitive.","Apply asyncio.timeout() inside each individual task's coroutine, not around the entire TaskGroup."]},{"id":"python/functional_programming#Q1-2715","section":"python","module":"python/functional_programming","moduleName":"functional programming","qIndex":1,"difficulty":"core","question":"What is a pure function, and why does it matter for caching with lru_cache?","answerFull":"A pure function always returns the same output for the same input and has no side effects — no I/O, no global state mutation, no randomness. This matters for lru_cache because the cache stores (args, kwargs) -> result mappings. If the function reads external state (a global variable, a database, the clock), the cached result may be stale on subsequent calls. Only pure functions are safe to cache because their output is entirely determined by their arguments, so the cached mapping is always valid.","correct":"A pure function always returns the same output for the same input and has no side effects — no I/O, no global state mutation, no randomness.","distractors":["When an exception is raised inside a stage of a toolz.pipe call, the traceback shows the anonymous lambda or the inner stage function, not the pipeline structure, making it harder to identify which stage failed.","operator.itemgetter(\"score\") returns a C-level callable that performs a single getitem call directly.","@singledispatch registers a generic function that routes to type-specific implementations based on the type of its first argument, stored in a dispatch dictionary keyed by type."]},{"id":"python/functional_programming#Q2-2716","section":"python","module":"python/functional_programming","moduleName":"functional programming","qIndex":2,"difficulty":"core","question":"Why is map(int, strings) faster than [int(s) for s in strings]?","answerFull":"map(int, strings) calls the C-implemented int type directly for each element with no Python bytecode per-element overhead. The list comprehension must execute LOAD_GLOBAL int, LOAD_FAST s, CALL_FUNCTION 1 bytecodes for every element. When the callable is a C built-in, map avoids that overhead. The difference is roughly 25-35% faster for type conversions at scale. The advantage disappears or reverses when the callable is a Python lambda, because map must then invoke Python function call machinery on each iteration.","correct":"map(int, strings) calls the C-implemented int type directly for each element with no Python bytecode per-element overhead.","distractors":["sum(), any(), all(), max(), min() are implemented in C and operate over the iterable in a tight C loop, never entering Python per-element.","operator.itemgetter(\"score\") returns a C-level callable that performs a single getitem call directly.","In Python 3.9+, functools.cache is equivalent to lru_cache(maxsize=None) but is implemented without the LRU overhead (no doubly-linked list maintenance, no lock per access in CPython)."]},{"id":"python/functional_programming#Q4-2717","section":"python","module":"python/functional_programming","moduleName":"functional programming","qIndex":4,"difficulty":"core","question":"What does functools.singledispatch do, and how does dispatch work for subclasses?","answerFull":"@singledispatch registers a generic function that routes to type-specific implementations based on the type of its first argument, stored in a dispatch dictionary keyed by type. When you call serialize(obj), it looks up type(obj) in the registry. If not found, it walks the MRO of type(obj) and uses the first registered ancestor — so if you register list and call with a deque, it will use the list implementation if deque is not separately registered. This gives you method-overloading semantics without class hierarchies.","correct":"@singledispatch registers a generic function that routes to type-specific implementations based on the type of its first argument, stored in a dispatch dictionary keyed by type.","distractors":["When an exception is raised inside a stage of a toolz.pipe call, the traceback shows the anonymous lambda or the inner stage function, not the pipeline structure, making it harder to identify which stage failed.","In Python 3.9+, functools.cache is equivalent to lru_cache(maxsize=None) but is implemented without the LRU overhead (no doubly-linked list maintenance, no lock per access in CPython).","A pure function always returns the same output for the same input and has no side effects — no I/O, no global state mutation, no randomness."]},{"id":"python/functional_programming#Q5-2718","section":"python","module":"python/functional_programming","moduleName":"functional programming","qIndex":5,"difficulty":"core","question":"What is the memory difference between a list comprehension and a generator expression for 1 million elements?","answerFull":"A list comprehension allocates all elements at once: for 1 million Python int objects (8 bytes each on a 64-bit CPython), the list itself uses roughly 8 MB. A generator expression is a generator object that occupies approximately 200 bytes regardless of the sequence length — it computes one element at a time on each next() call. For a single-pass aggregation like sum(x*x for x in range(1_000_000)), the generator is the correct choice. If you need multiple passes or random access, materialize to a list once.","correct":"A list comprehension allocates all elements at once: for 1 million Python int objects (8 bytes each on a 64-bit CPython), the list itself uses roughly 8 MB.","distractors":["map(int, strings) calls the C-implemented int type directly for each element with no Python bytecode per-element overhead.","A tuple is hashable if and only if all its elements are hashable.","sum(), any(), all(), max(), min() are implemented in C and operate over the iterable in a tight C loop, never entering Python per-element."]},{"id":"python/functional_programming#Q6-2719","section":"python","module":"python/functional_programming","moduleName":"functional programming","qIndex":6,"difficulty":"intermediate","question":"How does operator.itemgetter outperform lambda r: r[\"score\"] in a sort?","answerFull":"operator.itemgetter(\"score\") returns a C-level callable that performs a single getitem call directly. lambda r: r[\"score\"] is a Python function object: each invocation requires Python to set up a frame, execute LOAD_FAST r, LOAD_CONST \"score\", BINARY_SUBSCR, and RETURN_VALUE. For a sort of N records, this overhead is incurred N × log N times (the number of comparisons). On 1 million records, itemgetter is approximately 25-30% faster in benchmarks.","correct":"operator.itemgetter(\"score\") returns a C-level callable that performs a single getitem call directly.","distractors":["map(int, strings) calls the C-implemented int type directly for each element with no Python bytecode per-element overhead.","A pure function always returns the same output for the same input and has no side effects — no I/O, no global state mutation, no randomness.","When an exception is raised inside a stage of a toolz.pipe call, the traceback shows the anonymous lambda or the inner stage function, not the pipeline structure, making it harder to identify which stage failed."]},{"id":"python/functional_programming#Q7-2720","section":"python","module":"python/functional_programming","moduleName":"functional programming","qIndex":7,"difficulty":"intermediate","question":"What makes dataclass(frozen=True) different from a regular dataclass, and what does it generate?","answerFull":"@dataclass(frozen=True) sets setattr and delattr to raise FrozenInstanceError on any attribute write, effectively making instances immutable. It also auto-generates hash (which is suppressed in regular mutable dataclasses because mutable objects should not be hashable). The generated hash is based on all fields. This makes frozen dataclass instances safe to use as dictionary keys and in sets, and safe to share across threads without locks.","correct":"@dataclass(frozen=True) sets setattr and delattr to raise FrozenInstanceError on any attribute write, effectively making instances immutable.","distractors":["When an exception is raised inside a stage of a toolz.pipe call, the traceback shows the anonymous lambda or the inner stage function, not the pipeline structure, making it harder to identify which stage failed.","A pure function always returns the same output for the same input and has no side effects — no I/O, no global state mutation, no randomness.","map(int, strings) calls the C-implemented int type directly for each element with no Python bytecode per-element overhead."]},{"id":"python/functional_programming#Q8-2721","section":"python","module":"python/functional_programming","moduleName":"functional programming","qIndex":8,"difficulty":"intermediate","question":"Why does Python's reduce hide performance issues that built-ins like sum() or any() avoid?","answerFull":"sum(), any(), all(), max(), min() are implemented in C and operate over the iterable in a tight C loop, never entering Python per-element. functools.reduce calls a Python callable on every pair of accumulated values, which means the Python function call machinery fires N-1 times. For reduce(lambda a, b: a + b, numbers) vs sum(numbers), the built-in is roughly 5-10x faster on large sequences. The semantic issue is readability: reduce also hides short-circuit behavior — any() stops as soon as it finds True, but reduce(lambda a, b: a or b, flags) evaluates every element.","correct":"sum(), any(), all(), max(), min() are implemented in C and operate over the iterable in a tight C loop, never entering Python per-element.","distractors":["map(int, strings) calls the C-implemented int type directly for each element with no Python bytecode per-element overhead.","A list comprehension allocates all elements at once: for 1 million Python int objects (8 bytes each on a 64-bit CPython), the list itself uses roughly 8 MB.","In Python 3.9+, functools.cache is equivalent to lru_cache(maxsize=None) but is implemented without the LRU overhead (no doubly-linked list maintenance, no lock per access in CPython)."]},{"id":"python/functional_programming#Q9-2722","section":"python","module":"python/functional_programming","moduleName":"functional programming","qIndex":9,"difficulty":"intermediate","question":"What is toolz.pipe and how does it differ from toolz.compose?","answerFull":"Both compose functions, but differ in argument order and evaluation direction. pipe(data, f, g, h) applies f first, then g, then h — data flows left to right, matching reading order. compose(h, g, f)(data) applies f first — right to left, matching mathematical notation f∘g∘h. pipe is more readable for data pipelines; compose is more natural when constructing reusable function objects. Both are semantically equivalent: pipe(x, f, g, h) == compose(h, g, f)(x).","correct":"Both compose functions, but differ in argument order and evaluation direction.","distractors":["@singledispatch registers a generic function that routes to type-specific implementations based on the type of its first argument, stored in a dispatch dictionary keyed by type.","When an exception is raised inside a stage of a toolz.pipe call, the traceback shows the anonymous lambda or the inner stage function, not the pipeline structure, making it harder to identify which stage failed.","In Python 3.9+, functools.cache is equivalent to lru_cache(maxsize=None) but is implemented without the LRU overhead (no doubly-linked list maintenance, no lock per access in CPython)."]},{"id":"python/functional_programming#Q10-2723","section":"python","module":"python/functional_programming","moduleName":"functional programming","qIndex":10,"difficulty":"intermediate","question":"How do you safely use a frozenset or tuple as a dictionary key when the elements contain custom objects?","answerFull":"A tuple is hashable if and only if all its elements are hashable. For custom objects, Python uses id() by default for hash and identity for eq, which is usually wrong for value semantics. To make a custom object hashable with value semantics: (1) define eq based on the object's fields, then (2) define hash as a hash of the same fields, ensuring the contract a == b implies hash(a) == hash(b). @dataclass(frozen=True) handles both automatically. A frozenset requires all its elements to be hashable, so you can use frozenset of frozenset for nested sets but not frozenset of list.","correct":"A tuple is hashable if and only if all its elements are hashable.","distractors":["A list comprehension allocates all elements at once: for 1 million Python int objects (8 bytes each on a 64-bit CPython), the list itself uses roughly 8 MB.","@singledispatch registers a generic function that routes to type-specific implementations based on the type of its first argument, stored in a dispatch dictionary keyed by type.","A pure function always returns the same output for the same input and has no side effects — no I/O, no global state mutation, no randomness."]},{"id":"python/functional_programming#Q11-2724","section":"python","module":"python/functional_programming","moduleName":"functional programming","qIndex":11,"difficulty":"advanced","question":"When does using a functional pipeline with toolz.pipe become harder to debug than a for loop?","answerFull":"When an exception is raised inside a stage of a toolz.pipe call, the traceback shows the anonymous lambda or the inner stage function, not the pipeline structure, making it harder to identify which stage failed. Additionally, lazy generators inside a pipe delay exceptions until iteration, which can be far from the pipeline definition. Mitigation strategies: name each stage function (avoid anonymous lambdas inside pipe), add a tap/do stage that logs intermediate values, and prefer cytoolz.pipe with named functions so tracebacks include meaningful function names.","correct":"When an exception is raised inside a stage of a toolz.pipe call, the traceback shows the anonymous lambda or the inner stage function, not the pipeline structure, making it harder to identify which stage failed.","distractors":["@dataclass(frozen=True) sets setattr and delattr to raise FrozenInstanceError on any attribute write, effectively making instances immutable.","sum(), any(), all(), max(), min() are implemented in C and operate over the iterable in a tight C loop, never entering Python per-element.","operator.itemgetter(\"score\") returns a C-level callable that performs a single getitem call directly."]},{"id":"python/functional_programming#Q12-2725","section":"python","module":"python/functional_programming","moduleName":"functional programming","qIndex":12,"difficulty":"advanced","question":"How does functools.cache differ from functools.lru_cache(maxsize=None)?","answerFull":"In Python 3.9+, functools.cache is equivalent to lru_cache(maxsize=None) but is implemented without the LRU overhead (no doubly-linked list maintenance, no lock per access in CPython). This makes cache slightly faster per call for pure memoization when you do not need eviction. The tradeoff is unbounded memory growth — cache never evicts entries. Use cache for functions with a small, finite input space (e.g., a few dozen distinct values); use lru_cache(maxsize=N) when the input space is large and you need to bound memory usage.","correct":"In Python 3.9+, functools.cache is equivalent to lru_cache(maxsize=None) but is implemented without the LRU overhead (no doubly-linked list maintenance, no lock per access in CPython).","distractors":["map(int, strings) calls the C-implemented int type directly for each element with no Python bytecode per-element overhead.","A list comprehension allocates all elements at once: for 1 million Python int objects (8 bytes each on a 64-bit CPython), the list itself uses roughly 8 MB.","sum(), any(), all(), max(), min() are implemented in C and operate over the iterable in a tight C loop, never entering Python per-element."]},{"id":"python/fastapi#Q1-2726","section":"python","module":"python/fastapi","moduleName":"fastapi","qIndex":1,"difficulty":"core","question":"What is ASGI and how does it differ from WSGI?","answerFull":"ASGI (Asynchronous Server Gateway Interface) defines an async callable app(scope, receive, send) that handles arbitrary connection types — HTTP/1.1, HTTP/2, WebSockets, lifespan events — on a single event loop thread. WSGI (Web Server Gateway Interface) defines a synchronous callable app(environ, start_response) that handles one HTTP request per OS thread. WSGI cannot natively handle WebSockets or long-lived connections without hacks like gevent monkey-patching. The practical consequence: a WSGI server running 200 threads can handle 200 concurrent blocking requests; a single ASGI event loop can handle tens of thousands of concurrent I/O-bound requests.","correct":"ASGI (Asynchronous Server Gateway Interface) defines an async callable app(scope, receive, send) that handles arbitrary connection types — HTTP/1.1, HTTP/2, WebSockets, lifespan events — on a single event loop thread.","distractors":["The lifespan context manager (@asynccontextmanager async def lifespan(app)) makes startup and shutdown logic a single coherent unit.","Use httpx.AsyncClient with ASGITransport for async tests, and override the get_db dependency to return a session bound to a test transaction.","async def routes run directly on the event loop thread."]},{"id":"python/fastapi#Q2-2727","section":"python","module":"python/fastapi","moduleName":"fastapi","qIndex":2,"difficulty":"core","question":"What happens when you call a blocking function inside an async def FastAPI route?","answerFull":"The blocking call executes on the event loop thread — the same thread driving all other coroutines. No exception is raised. Every other request on that worker stalls until the blocking call returns. A 5ms blocking DB call at 1,000 requests/second means the event loop is blocked 5 seconds of every second — effectively making the worker single-threaded with sequential request processing. The fix is to use async I/O libraries (httpx, asyncpg, asyncio) or offload to a thread pool executor via run_in_executor for unavoidable sync code.","correct":"The blocking call executes on the event loop thread — the same thread driving all other coroutines.","distractors":["FastAPI workers (Gunicorn processes) scale horizontally behind a load balancer.","response_model is the Pydantic model FastAPI uses to serialise the route handler's return value before sending the response.","ASGI (Asynchronous Server Gateway Interface) defines an async callable app(scope, receive, send) that handles arbitrary connection types — HTTP/1.1, HTTP/2, WebSockets, lifespan events — on a single event loop thread."]},{"id":"python/fastapi#Q3-2728","section":"python","module":"python/fastapi","moduleName":"fastapi","qIndex":3,"difficulty":"core","question":"How does FastAPI's dependency injection work? How is it different from Spring's IoC?","answerFull":"FastAPI's DI is request-scoped and resolved at function call time. At startup, FastAPI introspects route function signatures and identifies Depends() parameters. At request time it builds and walks the dependency DAG, calling each dependency in topological order and caching results by callable object within the request. Spring's IoC resolves dependencies at application context creation (or prototype scope at call time) and injects them via constructor, field, or setter injection using class-level scanning and XML/annotation metadata. FastAPI's model is lighter: no reflection of class hierarchies, no circular dependency detection at startup (it fails at request time), no application context object — just function composition.","correct":"FastAPI's DI is request-scoped and resolved at function call time.","distractors":["Pydantic v1 executed validation in pure Python: for each field, it called a Python validator function.","A dependency with yield acts as a context manager.","Use httpx.AsyncClient with ASGITransport for async tests, and override the get_db dependency to return a session bound to a test transaction."]},{"id":"python/fastapi#Q4-2729","section":"python","module":"python/fastapi","moduleName":"fastapi","qIndex":4,"difficulty":"core","question":"What is the lifecycle of a request in FastAPI from bytes-in to bytes-out?","answerFull":"(1) Uvicorn parses HTTP bytes with httptools. (2) An ASGI scope dict is created. (3) Starlette's middleware chain executes top-to-bottom; each middleware calls await call_next(request). (4) The router matches path and method, extracts path parameters. (5) FastAPI resolves the dependency DAG, executing and caching Depends() callables in dependency order. (6) Pydantic v2 validates all incoming data — a 422 is raised here on failure. (7) The route handler coroutine is awaited. (8) The return value is serialised through response_model by Pydantic, then converted to JSON bytes. (9) The response bytes are sent via Uvicorn. (10) Yield-based dependency cleanup runs (finally blocks after yield).","correct":"(1) Uvicorn parses HTTP bytes with httptools.","distractors":["FastAPI's DI is request-scoped and resolved at function call time.","BackgroundTasks runs a function in the same process after the HTTP response is sent.","FastAPI raises RequestValidationError (a Starlette/FastAPI exception) when Pydantic validation fails for any incoming data — path params, query params, headers, or request body."]},{"id":"python/fastapi#Q5-2730","section":"python","module":"python/fastapi","moduleName":"fastapi","qIndex":5,"difficulty":"core","question":"How does Pydantic v2 improve on v1? What changed in the validation architecture?","answerFull":"Pydantic v1 executed validation in pure Python: for each field, it called a Python validator function. Pydantic v2 compiles a validation schema from the model class definition into Rust data structures in pydantic-core. At validation time, the Rust extension processes the input with no Python-level per-field dispatch. Benchmarks show 5–50x speedups for complex models. API changes: @validator becomes @field_validator, class Config becomes model_config = ConfigDict(...), .dict() becomes .model_dump(), .json() becomes .model_dump_json().","correct":"Pydantic v1 executed validation in pure Python: for each field, it called a Python validator function.","distractors":["BackgroundTasks runs a function in the same process after the HTTP response is sent.","FastAPI's DI is request-scoped and resolved at function call time.","FastAPI raises RequestValidationError (a Starlette/FastAPI exception) when Pydantic validation fails for any incoming data — path params, query params, headers, or request body."]},{"id":"python/fastapi#Q6-2731","section":"python","module":"python/fastapi","moduleName":"fastapi","qIndex":6,"difficulty":"intermediate","question":"What is the difference between BackgroundTasks and Celery/ARQ?","answerFull":"BackgroundTasks runs a function in the same process after the HTTP response is sent. It is in-process, has no persistence, no retry on failure, and no visibility into task status. If the process restarts, the task is lost. Celery and ARQ are distributed task queues: tasks are serialised and stored in a broker (Redis, RabbitMQ); workers pick them up independently; retries, dead-letter queues, and task status tracking are built in. Use BackgroundTasks for lightweight fire-and-forget work (sending a non-critical email, logging). Use Celery/ARQ for work that must complete reliably, requires retry, or takes more than a few seconds.","correct":"BackgroundTasks runs a function in the same process after the HTTP response is sent.","distractors":["The blocking call executes on the event loop thread — the same thread driving all other coroutines.","response_model is the Pydantic model FastAPI uses to serialise the route handler's return value before sending the response.","FastAPI's DI is request-scoped and resolved at function call time."]},{"id":"python/fastapi#Q7-2732","section":"python","module":"python/fastapi","moduleName":"fastapi","qIndex":7,"difficulty":"intermediate","question":"How do you handle database transactions in FastAPI with SQLAlchemy async?","answerFull":"Use an AsyncSession injected via a yield-based Depends(). The session is created at the start of the dependency, the yield hands it to the route handler, and cleanup in the finally block handles commit-or-rollback: python from sqlalchemy.ext.asyncio import AsyncSession async def get_db() -> AsyncSession: async with app.state.session_factory() as session: async with session.begin(): yield session # session.begin() commits on successful exit, rolls back on exception For explicit control: call await session.commit() in the handler and await session.rollback() in the except block. Never call session.commit() from inside a Depends() that wraps multiple route calls — each request should own exactly one transaction.","correct":"Use an AsyncSession injected via a yield-based Depends().","distractors":["ASGI (Asynchronous Server Gateway Interface) defines an async callable app(scope, receive, send) that handles arbitrary connection types — HTTP/1.1, HTTP/2, WebSockets, lifespan events — on a single event loop thread.","FastAPI workers (Gunicorn processes) scale horizontally behind a load balancer.","Use httpx.AsyncClient with ASGITransport for async tests, and override the get_db dependency to return a session bound to a test transaction."]},{"id":"python/fastapi#Q8-2733","section":"python","module":"python/fastapi","moduleName":"fastapi","qIndex":8,"difficulty":"intermediate","question":"What is the lifespan event and why is it better than @app.on_event('startup')?","answerFull":"The lifespan context manager (@asynccontextmanager async def lifespan(app)) makes startup and shutdown logic a single coherent unit. The yield separates setup from teardown; exceptions in startup propagate naturally; cleanup always runs because it is in a finally block (implicit in the context manager protocol). @app.on_event registered two separate functions with no guaranteed pairing — if the startup handler created a resource, the shutdown handler had to know the variable name to clean it up. The lifespan pattern also works in tests: async with LifespanManager(app): exercises both sides in one context.","correct":"The lifespan context manager (@asynccontextmanager async def lifespan(app)) makes startup and shutdown logic a single coherent unit.","distractors":["Use httpx.AsyncClient with ASGITransport for async tests, and override the get_db dependency to return a session bound to a test transaction.","A dependency with yield acts as a context manager.","async def routes run directly on the event loop thread."]},{"id":"python/fastapi#Q9-2734","section":"python","module":"python/fastapi","moduleName":"fastapi","qIndex":9,"difficulty":"intermediate","question":"How do you test FastAPI endpoints with a real (rolled-back) database?","answerFull":"Use httpx.AsyncClient with ASGITransport for async tests, and override the get_db dependency to return a session bound to a test transaction. Roll back the transaction after each test: python @pytest.fixture async def client(db_session: AsyncSession) -> AsyncClient: async def override_get_db() -> AsyncSession: yield db_session app.dependency_overrides[get_db] = override_get_db async with AsyncClient( transport=ASGITransport(app=app), base_url=\"http://test\" ) as ac: yield ac app.dependency_overrides = {} @pytest.fixture async def db_session(engine: AsyncEngine) -> AsyncSession: async with engine.connect() as conn: await conn.begin() async with AsyncSession(bind=conn) as session: yield session await conn.rollback() # all test data discarded","correct":"Use httpx.AsyncClient with ASGITransport for async tests, and override the get_db dependency to return a session bound to a test transaction.","distractors":["The lifespan context manager (@asynccontextmanager async def lifespan(app)) makes startup and shutdown logic a single coherent unit.","FastAPI raises RequestValidationError (a Starlette/FastAPI exception) when Pydantic validation fails for any incoming data — path params, query params, headers, or request body.","async def routes run directly on the event loop thread."]},{"id":"python/fastapi#Q10-2735","section":"python","module":"python/fastapi","moduleName":"fastapi","qIndex":10,"difficulty":"intermediate","question":"What is BOLA (Broken Object Level Authorization) and how do you prevent it?","answerFull":"BOLA (OWASP API Security Top 10 #1) occurs when an endpoint accepts a user-supplied object ID and fetches the object without verifying the requesting user is authorised to access it. Example: GET /orders/{order_id} returns the order for any authenticated user if the handler does db.get(Order, order_id) without checking order.user_id == current_user.id. Prevention: always filter queries by the authenticated user's identity, never trust the client-supplied ID alone. In SQLAlchemy: select(Order).where(Order.id == order_id, Order.user_id == current_user.id) — if the result is None, return 404 (not 403, to avoid leaking existence).","correct":"BOLA (OWASP API Security Top 10 #1) occurs when an endpoint accepts a user-supplied object ID and fetches the object without verifying the requesting user is authorised to access it.","distractors":["ASGI (Asynchronous Server Gateway Interface) defines an async callable app(scope, receive, send) that handles arbitrary connection types — HTTP/1.1, HTTP/2, WebSockets, lifespan events — on a single event loop thread.","The blocking call executes on the event loop thread — the same thread driving all other coroutines.","FastAPI's DI is request-scoped and resolved at function call time."]},{"id":"python/fastapi#Q11-2736","section":"python","module":"python/fastapi","moduleName":"fastapi","qIndex":11,"difficulty":"advanced","question":"How do you scale FastAPI horizontally? What must be stateless?","answerFull":"FastAPI workers (Gunicorn processes) scale horizontally behind a load balancer. Each worker is an independent process with no shared memory. Anything stored in app.state, module-level variables, or in-process caches is local to one worker and invisible to others. For horizontal scaling, all shared state must move to external stores: user sessions to Redis, rate limit counters to Redis, database state to PostgreSQL, pub/sub channels to Redis or Kafka. Kubernetes HPA scales the Deployment replica count based on CPU utilisation (target ~70%); each pod runs one Gunicorn process with (2 x cores) + 1 Uvicorn workers.","correct":"FastAPI workers (Gunicorn processes) scale horizontally behind a load balancer.","distractors":["Formula: workers = (2 x CPU_cores) + 1.","FastAPI raises RequestValidationError (a Starlette/FastAPI exception) when Pydantic validation fails for any incoming data — path params, query params, headers, or request body.","response_model is the Pydantic model FastAPI uses to serialise the route handler's return value before sending the response."]},{"id":"python/fastapi#Q12-2737","section":"python","module":"python/fastapi","moduleName":"fastapi","qIndex":12,"difficulty":"advanced","question":"What is the def vs async def route distinction and when does it matter?","answerFull":"async def routes run directly on the event loop thread. Any blocking call in them blocks the entire loop. def routes are run by FastAPI in a thread pool executor (default: concurrent.futures.ThreadPoolExecutor with up to 40 threads), so they do not block the event loop. However, 40 concurrent blocking def routes exhaust the threadpool and new requests queue. The correct rule: use async def for all I/O-bound routes (DB, HTTP, Redis) with async libraries; use def for genuinely CPU-bound routes (number crunching, image processing) where thread-level parallelism is acceptable.","correct":"async def routes run directly on the event loop thread.","distractors":["response_model is the Pydantic model FastAPI uses to serialise the route handler's return value before sending the response.","ASGI (Asynchronous Server Gateway Interface) defines an async callable app(scope, receive, send) that handles arbitrary connection types — HTTP/1.1, HTTP/2, WebSockets, lifespan events — on a single event loop thread.","The blocking call executes on the event loop thread — the same thread driving all other coroutines."]},{"id":"python/fastapi#Q13-2738","section":"python","module":"python/fastapi","moduleName":"fastapi","qIndex":13,"difficulty":"advanced","question":"How do you configure Gunicorn + Uvicorn workers for production? What is the formula?","answerFull":"Formula: workers = (2 x CPU_cores) + 1. For a 4-core machine: 9 workers. Each worker is a separate process with its own event loop and memory space. Start Gunicorn with: bash gunicorn app.main:app \\ --worker-class uvicorn.workers.UvicornWorker \\ --workers 9 \\ --bind 0.0.0.0:8000 \\ --timeout 120 \\ --graceful-timeout 30 \\ --keep-alive 5 In Kubernetes, run one Gunicorn process per pod (workers = (2 x pod_cpu_limit) + 1) and scale pods with HPA. Do not over-provision workers beyond available CPU — beyond 2N+1 workers, context switching overhead reduces throughput.","correct":"Formula: workers = (2 x CPU_cores) + 1.","distractors":["FastAPI workers (Gunicorn processes) scale horizontally behind a load balancer.","(1) Uvicorn parses HTTP bytes with httptools.","ASGI (Asynchronous Server Gateway Interface) defines an async callable app(scope, receive, send) that handles arbitrary connection types — HTTP/1.1, HTTP/2, WebSockets, lifespan events — on a single event loop thread."]},{"id":"python/fastapi#Q15-2739","section":"python","module":"python/fastapi","moduleName":"fastapi","qIndex":15,"difficulty":"advanced","question":"What is response_model and why should you always set it?","answerFull":"response_model is the Pydantic model FastAPI uses to serialise the route handler's return value before sending the response. It filters the output to only the fields declared in the model, applies field aliases, excludes None values if configured, and documents the response schema in OpenAPI. Without response_model, FastAPI returns the raw Python object serialised as-is — no field filtering, no documentation. This risks leaking internal fields (password hashes, internal IDs, audit timestamps). Always set response_model on every route, even if the return type annotation is the same model — the annotation only helps type checkers, not runtime serialisation.","correct":"response_model is the Pydantic model FastAPI uses to serialise the route handler's return value before sending the response.","distractors":["Use httpx.AsyncClient with ASGITransport for async tests, and override the get_db dependency to return a session bound to a test transaction.","FastAPI raises RequestValidationError (a Starlette/FastAPI exception) when Pydantic validation fails for any incoming data — path params, query params, headers, or request body.","Pydantic v1 executed validation in pure Python: for each field, it called a Python validator function."]},{"id":"python/fastapi#Q16-2740","section":"python","module":"python/fastapi","moduleName":"fastapi","qIndex":16,"difficulty":"advanced","question":"How does yield-based dependency cleanup work and when does it run?","answerFull":"A dependency with yield acts as a context manager. FastAPI awaits the dependency up to the yield, injects the yielded value, then after the response is sent, resumes the dependency's generator to run the code after yield. This cleanup always runs — both on successful responses and on unhandled exceptions. It is the correct place for session.close(), releasing locks, and logging request completion. The cleanup runs after middleware teardown (the middleware's call_next has already returned), so middleware cannot observe the dependency cleanup.","correct":"A dependency with yield acts as a context manager.","distractors":["The lifespan context manager (@asynccontextmanager async def lifespan(app)) makes startup and shutdown logic a single coherent unit.","Use httpx.AsyncClient with ASGITransport for async tests, and override the get_db dependency to return a session bound to a test transaction.","async def routes run directly on the event loop thread."]},{"id":"python/fastapi#Q17-2741","section":"python","module":"python/fastapi","moduleName":"fastapi","qIndex":17,"difficulty":"advanced","question":"How does FastAPI handle 422 Unprocessable Entity errors?","answerFull":"FastAPI raises RequestValidationError (a Starlette/FastAPI exception) when Pydantic validation fails for any incoming data — path params, query params, headers, or request body. The default exception handler converts this to a 422 response with a JSON body listing every validation error with its location (body, query, path), field name, and error message. You can override the handler to produce RFC 7807 Problem Details format: python from fastapi.exception_handlers import request_validation_exception_handler from fastapi.exceptions import RequestValidationError @app.exception_handler(RequestValidationError) async def validation_exception_handler(request: Request, exc: RequestValidationError): return JSONResponse( status_code=422, content={ \"type\": \"https://example.com/errors/validation\", \"title\": \"Validation Error\", \"detail\": exc.errors(), }, )","correct":"FastAPI raises RequestValidationError (a Starlette/FastAPI exception) when Pydantic validation fails for any incoming data — path params, query params, headers, or request body.","distractors":["FastAPI's DI is request-scoped and resolved at function call time.","FastAPI workers (Gunicorn processes) scale horizontally behind a load balancer.","response_model is the Pydantic model FastAPI uses to serialise the route handler's return value before sending the response."]},{"id":"python/fastapi/authentication_and_security#Q1-2742","section":"python","module":"python/fastapi/authentication_and_security","moduleName":"authentication and security","qIndex":1,"difficulty":"core","question":"What is the difference between authentication and authorization?","answerFull":"Authentication answers \"who are you?\" — it verifies identity by validating a credential (password, token, certificate). Authorization answers \"what can you do?\" — it checks whether the authenticated principal has permission to perform a specific action. In FastAPI, get_current_user handles authentication; scope checks or role checks handle authorization. Never conflate them in a single dependency.","correct":"Authentication answers \"who are you?\" — it verifies identity by validating a credential (password, token, certificate).","distractors":["OAuth2 is an authorization framework — it issues access tokens that say \"you may call this API.\" It does not define how to get user identity.","Token family tracking groups all refresh tokens issued in a single login session under a family ID.","Never include token decoding details in HTTP error responses."]},{"id":"python/fastapi/authentication_and_security#Q2-2743","section":"python","module":"python/fastapi/authentication_and_security","moduleName":"authentication and security","qIndex":2,"difficulty":"core","question":"Why use JWTs instead of opaque tokens?","answerFull":"JWTs are self-contained: the payload carries the user ID, expiry, and scopes, so the verifier needs only the public key or shared secret — no database round-trip per request. Opaque tokens require a lookup against a token store on every request. JWTs are preferred for stateless microservices and high-throughput APIs. The trade-off is revocation: you cannot revoke a JWT without a blocklist, whereas opaque tokens can simply be deleted from the store.","correct":"JWTs are self-contained: the payload carries the user ID, expiry, and scopes, so the verifier needs only the public key or shared secret — no database round-trip per request.","distractors":["When using HttpOnly cookies for session IDs or refresh tokens, the browser automatically attaches the cookie to same-origin and cross-origin requests.","Use RS256 when multiple independent services need to verify tokens, or when tokens are consumed by third parties.","Never store the raw API key."]},{"id":"python/fastapi/authentication_and_security#Q3-2744","section":"python","module":"python/fastapi/authentication_and_security","moduleName":"authentication and security","qIndex":3,"difficulty":"core","question":"What claims should a production access token always include?","answerFull":"At minimum: sub (subject, the user identifier), exp (expiry as Unix timestamp), iat (issued-at, for detecting replayed tokens), and jti (unique token ID, for blacklisting). Optionally scopes (a list of granted permissions). Never embed passwords, raw emails, or other PII — JWT payloads are base64-encoded but not encrypted, visible to anyone who intercepts the token.","correct":"At minimum: sub (subject, the user identifier), exp (expiry as Unix timestamp), iat (issued-at, for detecting replayed tokens), and jti (unique token ID, for blacklisting).","distractors":["Token family tracking groups all refresh tokens issued in a single login session under a family ID.","Never include token decoding details in HTTP error responses.","Use RS256 when multiple independent services need to verify tokens, or when tokens are consumed by third parties."]},{"id":"python/fastapi/authentication_and_security#Q4-2745","section":"python","module":"python/fastapi/authentication_and_security","moduleName":"authentication and security","qIndex":4,"difficulty":"core","question":"Why is bcrypt's default cost factor 10 inadequate for production auth in 2025?","answerFull":"Cost factor 10 produces ~100 ms on 2015-era hardware but less than 50 ms on a modern CPU. A bcrypt hash is parallelizable to ~1000 attempts/second per GPU. Cost factor 12 (~250 ms) increases GPU attack cost by 4× over factor 10. For high-security applications, use argon2id (memory-hard) which defeats GPU/ASIC parallelism regardless of cost factor by requiring large amounts of RAM per attempt. The OWASP recommendation as of 2024 is bcrypt cost ≥ 12 or argon2id with m=65536.","correct":"Cost factor 10 produces ~100 ms on 2015-era hardware but less than 50 ms on a modern CPU.","distractors":["When a CryptContext is configured with schemes=[\"argon2\", \"bcrypt\"] and deprecated=\"auto\", the first scheme (argon2) is the active scheme.","SecretStr stores the underlying string value but overrides repr and str to return ''.","Authentication answers \"who are you?\" — it verifies identity by validating a credential (password, token, certificate)."]},{"id":"python/fastapi/authentication_and_security#Q5-2746","section":"python","module":"python/fastapi/authentication_and_security","moduleName":"authentication and security","qIndex":5,"difficulty":"core","question":"Explain refresh token rotation and why it detects token theft.","answerFull":"On each use of a refresh token, the server deletes the old token and issues a new one. If an attacker steals the refresh token and uses it first, the server invalidates it. When the legitimate client later presents the (now invalid) token, the mismatch signals a potential theft — the server should revoke the entire token family (all refresh tokens for that user). This is the \"refresh token rotation with reuse detection\" pattern. Without rotation, a stolen refresh token grants indefinite access.","correct":"On each use of a refresh token, the server deletes the old token and issues a new one.","distractors":["Add a jti (JWT ID) claim — a UUID — to every issued access token.","Token family tracking groups all refresh tokens issued in a single login session under a family ID.","Never include token decoding details in HTTP error responses."]},{"id":"python/fastapi/authentication_and_security#Q6-2747","section":"python","module":"python/fastapi/authentication_and_security","moduleName":"authentication and security","qIndex":6,"difficulty":"intermediate","question":"What is the alg: none JWT attack and how does PyJWT 2.x prevent it?","answerFull":"An attacker strips the JWT signature, sets \"alg\": \"none\" in the header, and sends it. JWT libraries that do not enforce an algorithm allowlist may accept the unsigned token as valid. PyJWT 2.x raises InvalidAlgorithmError if algorithms is not explicitly passed to decode(). Always pass algorithms=[\"HS256\"] (or [\"RS256\"] for asymmetric). Never pass algorithms=jwt.algorithms.get_default_algorithms() unless you fully understand what it includes.","correct":"An attacker strips the JWT signature, sets \"alg\": \"none\" in the header, and sends it.","distractors":["A stolen JWT with a 24-hour or 7-day expiry gives an attacker a large window of access.","Add a jti (JWT ID) claim — a UUID — to every issued access token.","Authentication answers \"who are you?\" — it verifies identity by validating a credential (password, token, certificate)."]},{"id":"python/fastapi/authentication_and_security#Q7-2748","section":"python","module":"python/fastapi/authentication_and_security","moduleName":"authentication and security","qIndex":7,"difficulty":"intermediate","question":"When should you use RS256 instead of HS256?","answerFull":"Use RS256 when multiple independent services need to verify tokens, or when tokens are consumed by third parties. With RS256, the private key stays on the auth server; services fetch the public key from a JWKS endpoint. A compromised microservice cannot forge tokens because it only has the public key. Use HS256 for single-service applications where the secret can be kept on one server. Never share HS256 secrets across untrusted boundaries.","correct":"Use RS256 when multiple independent services need to verify tokens, or when tokens are consumed by third parties.","distractors":["OAuth2 is an authorization framework — it issues access tokens that say \"you may call this API.\" It does not define how to get user identity.","When using HttpOnly cookies for session IDs or refresh tokens, the browser automatically attaches the cookie to same-origin and cross-origin requests.","Token family tracking groups all refresh tokens issued in a single login session under a family ID."]},{"id":"python/fastapi/authentication_and_security#Q8-2749","section":"python","module":"python/fastapi/authentication_and_security","moduleName":"authentication and security","qIndex":8,"difficulty":"intermediate","question":"What is SecurityScopes and why use it over a plain Depends?","answerFull":"SecurityScopes is a FastAPI-specific class injected into dependencies declared via Security(). It carries the list of scopes required by the current route, allowing a single get_current_user function to handle different permission requirements across routes without duplicating the function. It also generates correct WWW-Authenticate response headers listing required scopes, which is part of the RFC 6750 Bearer token spec. A plain Depends cannot carry this route-specific scope metadata.","correct":"SecurityScopes is a FastAPI-specific class injected into dependencies declared via Security().","distractors":["Authentication answers \"who are you?\" — it verifies identity by validating a credential (password, token, certificate).","JWTs are self-contained: the payload carries the user ID, expiry, and scopes, so the verifier needs only the public key or shared secret — no database round-trip per request.","At minimum: sub (subject, the user identifier), exp (expiry as Unix timestamp), iat (issued-at, for detecting replayed tokens), and jti (unique token ID, for blacklisting)."]},{"id":"python/fastapi/authentication_and_security#Q9-2750","section":"python","module":"python/fastapi/authentication_and_security","moduleName":"authentication and security","qIndex":9,"difficulty":"intermediate","question":"How would you implement explicit logout for a JWT-based API?","answerFull":"Add a jti (JWT ID) claim — a UUID — to every issued access token. On logout, compute the remaining TTL from the exp claim and store the jti in Redis with that TTL as the key expiry. On every authenticated request, check whether the jti exists in the Redis blacklist before completing auth. The Redis key auto-expires when the token would have expired naturally, so the blocklist does not grow unboundedly. Logout also revokes the refresh token by deleting it from the refresh token store.","correct":"Add a jti (JWT ID) claim — a UUID — to every issued access token.","distractors":["At minimum: sub (subject, the user identifier), exp (expiry as Unix timestamp), iat (issued-at, for detecting replayed tokens), and jti (unique token ID, for blacklisting).","Authentication answers \"who are you?\" — it verifies identity by validating a credential (password, token, certificate).","OAuth2 is an authorization framework — it issues access tokens that say \"you may call this API.\" It does not define how to get user identity."]},{"id":"python/fastapi/authentication_and_security#Q10-2751","section":"python","module":"python/fastapi/authentication_and_security","moduleName":"authentication and security","qIndex":10,"difficulty":"intermediate","question":"What is the difference between OAuth2PasswordBearer and HTTPBearer?","answerFull":"Both extract a Bearer token from the Authorization header. OAuth2PasswordBearer declares an OAuth2 password flow in the OpenAPI spec, which renders a username/password form in /docs. HTTPBearer is a simpler scheme that just extracts the token and declares a generic HTTP Bearer scheme in OpenAPI — it shows a raw token field in /docs. Use OAuth2PasswordBearer when you want the interactive /docs login form. Use HTTPBearer for API key or custom token schemes where the token acquisition flow is external.","correct":"Both extract a Bearer token from the Authorization header.","distractors":["Authentication answers \"who are you?\" — it verifies identity by validating a credential (password, token, certificate).","On each use of a refresh token, the server deletes the old token and issues a new one.","At minimum: sub (subject, the user identifier), exp (expiry as Unix timestamp), iat (issued-at, for detecting replayed tokens), and jti (unique token ID, for blacklisting)."]},{"id":"python/fastapi/authentication_and_security#Q11-2752","section":"python","module":"python/fastapi/authentication_and_security","moduleName":"authentication and security","qIndex":11,"difficulty":"advanced","question":"How do you store API keys securely?","answerFull":"Never store the raw API key. On issuance, return the raw key to the user once (it will not be shown again — like GitHub personal access tokens). Immediately compute and store SHA-256(raw_key). On each request, compute SHA-256(incoming_key) and compare against stored hashes. Use a constant-time comparison (hmac.compare_digest) to prevent timing attacks. For rotation: issue a new key, allow a grace period where both are valid, then revoke the old one.","correct":"Never store the raw API key.","distractors":["OAuth2 is an authorization framework — it issues access tokens that say \"you may call this API.\" It does not define how to get user identity.","Never include token decoding details in HTTP error responses.","PKCE (Proof Key for Code Exchange, RFC 7636) prevents authorization code interception attacks in public clients (SPAs, mobile apps) that cannot safely store a client secret."]},{"id":"python/fastapi/authentication_and_security#Q12-2753","section":"python","module":"python/fastapi/authentication_and_security","moduleName":"authentication and security","qIndex":12,"difficulty":"advanced","question":"Explain the CSRF risk with cookie-based auth and how to mitigate it.","answerFull":"When using HttpOnly cookies for session IDs or refresh tokens, the browser automatically attaches the cookie to same-origin and cross-origin requests. A malicious site can craft a form that submits to your API — the browser includes the cookie, so the request appears authenticated. Mitigation: set SameSite=Strict (or Lax for OAuth flows requiring redirects). For APIs consumed by SPAs on different origins, use the double-submit cookie pattern: the server sets a non-HttpOnly CSRF token cookie; the client reads it via JavaScript and sends it in a header; the server validates header matches cookie. CSRF does not apply to JWT-in-Authorization-header flows because browsers do not auto-attach custom headers.","correct":"When using HttpOnly cookies for session IDs or refresh tokens, the browser automatically attaches the cookie to same-origin and cross-origin requests.","distractors":["Token family tracking groups all refresh tokens issued in a single login session under a family ID.","On each use of a refresh token, the server deletes the old token and issues a new one.","Use RS256 when multiple independent services need to verify tokens, or when tokens are consumed by third parties."]},{"id":"python/fastapi/authentication_and_security#Q13-2754","section":"python","module":"python/fastapi/authentication_and_security","moduleName":"authentication and security","qIndex":13,"difficulty":"advanced","question":"What is PKCE and when is it required?","answerFull":"PKCE (Proof Key for Code Exchange, RFC 7636) prevents authorization code interception attacks in public clients (SPAs, mobile apps) that cannot safely store a client secret. The client generates a random code_verifier, computes code_challenge = SHA-256(code_verifier), and sends the challenge with the authorization request. When exchanging the code for tokens, the client sends the raw code_verifier; the server verifies the hash matches. An attacker who intercepts the code cannot exchange it without the verifier. PKCE is mandatory for SPAs and mobile apps per OAuth 2.1 draft.","correct":"PKCE (Proof Key for Code Exchange, RFC 7636) prevents authorization code interception attacks in public clients (SPAs, mobile apps) that cannot safely store a client secret.","distractors":["Never store the raw API key.","OAuth2 is an authorization framework — it issues access tokens that say \"you may call this API.\" It does not define how to get user identity.","JWTs are self-contained: the payload carries the user ID, expiry, and scopes, so the verifier needs only the public key or shared secret — no database round-trip per request."]},{"id":"python/fastapi/authentication_and_security#Q14-2755","section":"python","module":"python/fastapi/authentication_and_security","moduleName":"authentication and security","qIndex":14,"difficulty":"advanced","question":"How does OIDC differ from plain OAuth2?","answerFull":"OAuth2 is an authorization framework — it issues access tokens that say \"you may call this API.\" It does not define how to get user identity. OIDC (OpenID Connect) is a thin identity layer on top of OAuth2: the token response includes an id_token (JWT) containing user claims (sub, email, name, picture). Your app verifies the id_token signature using the provider's public key from its JWKS endpoint. OIDC also standardizes the discovery endpoint (/.well-known/openid-configuration) so you can fetch all configuration automatically.","correct":"OAuth2 is an authorization framework — it issues access tokens that say \"you may call this API.\" It does not define how to get user identity.","distractors":["Never store the raw API key.","PKCE (Proof Key for Code Exchange, RFC 7636) prevents authorization code interception attacks in public clients (SPAs, mobile apps) that cannot safely store a client secret.","Use RS256 when multiple independent services need to verify tokens, or when tokens are consumed by third parties."]},{"id":"python/fastapi/authentication_and_security#Q15-2756","section":"python","module":"python/fastapi/authentication_and_security","moduleName":"authentication and security","qIndex":15,"difficulty":"advanced","question":"Why should you use SecretStr from pydantic for secrets?","answerFull":"SecretStr stores the underlying string value but overrides repr and str to return ''. This means that if a Settings object is accidentally logged, printed in a traceback, or serialized to JSON, the secret value is never exposed. The raw value is only accessible via .get_secret_value(), which forces an explicit intent to use the secret. This provides a thin but effective defence-in-depth layer against accidental secret leakage in logs.","correct":"SecretStr stores the underlying string value but overrides repr and str to return ''.","distractors":["Cost factor 10 produces ~100 ms on 2015-era hardware but less than 50 ms on a modern CPU.","Authentication answers \"who are you?\" — it verifies identity by validating a credential (password, token, certificate).","JWTs are self-contained: the payload carries the user ID, expiry, and scopes, so the verifier needs only the public key or shared secret — no database round-trip per request."]},{"id":"python/fastapi/authentication_and_security#Q16-2757","section":"python","module":"python/fastapi/authentication_and_security","moduleName":"authentication and security","qIndex":16,"difficulty":"advanced","question":"What are the security implications of long-lived JWTs?","answerFull":"A stolen JWT with a 24-hour or 7-day expiry gives an attacker a large window of access. If you use short-lived access tokens (15 min) with refresh token rotation, the maximum exposure window for a stolen access token is 15 minutes. The attacker cannot silently extend access via the refresh endpoint because they do not have the refresh token (or if they do, rotation detects reuse). Long-lived JWTs are never appropriate for user-facing APIs. Use them only for inter-service calls where the services are in the same trust boundary.","correct":"A stolen JWT with a 24-hour or 7-day expiry gives an attacker a large window of access.","distractors":["OAuth2 is an authorization framework — it issues access tokens that say \"you may call this API.\" It does not define how to get user identity.","SecurityScopes is a FastAPI-specific class injected into dependencies declared via Security().","An attacker strips the JWT signature, sets \"alg\": \"none\" in the header, and sends it."]},{"id":"python/fastapi/authentication_and_security#Q17-2758","section":"python","module":"python/fastapi/authentication_and_security","moduleName":"authentication and security","qIndex":17,"difficulty":"advanced","question":"How does passlib's deprecated=\"auto\" feature work?","answerFull":"When a CryptContext is configured with schemes=[\"argon2\", \"bcrypt\"] and deprecated=\"auto\", the first scheme (argon2) is the active scheme. All existing bcrypt hashes are flagged as deprecated. When pwd_context.verify(plain, hashed) is called for a bcrypt hash, it returns a tuple (True, new_hash) where new_hash is the argon2 hash of the same password. The application must detect this case and update the stored hash. This allows zero-downtime algorithm migration: users transparently get argon2 hashes on their next login without ever being logged out.","correct":"When a CryptContext is configured with schemes=[\"argon2\", \"bcrypt\"] and deprecated=\"auto\", the first scheme (argon2) is the active scheme.","distractors":["Authentication answers \"who are you?\" — it verifies identity by validating a credential (password, token, certificate).","JWTs are self-contained: the payload carries the user ID, expiry, and scopes, so the verifier needs only the public key or shared secret — no database round-trip per request.","At minimum: sub (subject, the user identifier), exp (expiry as Unix timestamp), iat (issued-at, for detecting replayed tokens), and jti (unique token ID, for blacklisting)."]},{"id":"python/fastapi/authentication_and_security#Q18-2759","section":"python","module":"python/fastapi/authentication_and_security","moduleName":"authentication and security","qIndex":18,"difficulty":"advanced","question":"How do you prevent JWT secret leakage via error messages?","answerFull":"Never include token decoding details in HTTP error responses. The response body should be generic (\"detail\": \"Could not validate credentials\") regardless of whether the failure was an expired token, wrong signature, or missing claim. Detailed JWT errors belong in server-side structured logs at DEBUG level — and even then, log the failure reason (e.g., \"jwt_error\": \"ExpiredSignatureError\") without logging the raw token. Include a correlation ID in the response so operators can trace the specific request in logs if needed.","correct":"Never include token decoding details in HTTP error responses.","distractors":["Add a jti (JWT ID) claim — a UUID — to every issued access token.","At minimum: sub (subject, the user identifier), exp (expiry as Unix timestamp), iat (issued-at, for detecting replayed tokens), and jti (unique token ID, for blacklisting).","A stolen JWT with a 24-hour or 7-day expiry gives an attacker a large window of access."]},{"id":"python/fastapi/authentication_and_security#Q19-2760","section":"python","module":"python/fastapi/authentication_and_security","moduleName":"authentication and security","qIndex":19,"difficulty":"advanced","question":"What is token family tracking and why does it matter for refresh tokens?","answerFull":"Token family tracking groups all refresh tokens issued in a single login session under a family ID. When a refresh token is presented that has already been used (indicating potential theft), the server invalidates the entire family — all refresh tokens from that login session. Without family tracking, an attacker who steals a refresh token can keep rotating it independently of the legitimate client. With family tracking, the first detected reuse triggers a full session revocation, forcing the legitimate user to log in again. Implement by storing a family_id with each refresh token in Redis and using a Lua script to atomically check-and-revoke.","correct":"Token family tracking groups all refresh tokens issued in a single login session under a family ID.","distractors":["OAuth2 is an authorization framework — it issues access tokens that say \"you may call this API.\" It does not define how to get user identity.","Use RS256 when multiple independent services need to verify tokens, or when tokens are consumed by third parties.","Authentication answers \"who are you?\" — it verifies identity by validating a credential (password, token, certificate)."]},{"id":"python/fastapi/background_jobs_and_task_queues#Q1-2761","section":"python","module":"python/fastapi/background_jobs_and_task_queues","moduleName":"background jobs and task queues","qIndex":1,"difficulty":"core","question":"What is the difference between BackgroundTasks and a durable task queue like Celery or ARQ?","answerFull":"BackgroundTasks runs after the response in the same process with no persistence — a worker restart silently drops any queued tasks. Durable queues (Celery, ARQ) persist the task message in an external broker (Redis, RabbitMQ); workers in separate processes consume independently, so crashes do not lose work. Use BackgroundTasks only for truly non-critical fire-and-forget work where occasional drops are explicitly acceptable.","correct":"BackgroundTasks runs after the response in the same process with no persistence — a worker restart silently drops any queued tasks.","distractors":["The prefetch multiplier controls how many unacknowledged tasks each worker process fetches from the broker at once.","At-least-once delivery means the broker guarantees a task message will be delivered to a worker *at least* once, but may deliver it multiple times on retry after a crash or timeout.","Three layers: (1) Flower — the official Celery web monitor, shows active workers, task rates, success/failure counts, and lets you revoke tasks."]},{"id":"python/fastapi/background_jobs_and_task_queues#Q2-2762","section":"python","module":"python/fastapi/background_jobs_and_task_queues","moduleName":"background jobs and task queues","qIndex":2,"difficulty":"core","question":"What is at-least-once delivery and why does it require idempotent tasks?","answerFull":"At-least-once delivery means the broker guarantees a task message will be delivered to a worker *at least* once, but may deliver it multiple times on retry after a crash or timeout. If a task is not idempotent — meaning running it twice produces a different outcome than running it once — duplicate execution causes bugs like double charges or duplicate emails. Idempotency (using a stable key such as order-{order_id} to detect and skip duplicate execution) is the application-level complement to at-least-once delivery.","correct":"At-least-once delivery means the broker guarantees a task message will be delivered to a worker *at least* once, but may deliver it multiple times on retry after a crash or timeout.","distractors":["By default Celery acknowledges (removes) the task message from the broker before executing the task handler.","BackgroundTasks runs after the response in the same process with no persistence — a worker restart silently drops any queued tasks.","Three layers: (1) Flower — the official Celery web monitor, shows active workers, task rates, success/failure counts, and lets you revoke tasks."]},{"id":"python/fastapi/background_jobs_and_task_queues#Q3-2763","section":"python","module":"python/fastapi/background_jobs_and_task_queues","moduleName":"background jobs and task queues","qIndex":3,"difficulty":"core","question":"How does task_acks_late=True improve Celery durability?","answerFull":"By default Celery acknowledges (removes) the task message from the broker before executing the task handler. If the worker is killed mid-execution (SIGKILL from OOM killer, deploy), the message is already gone. With task_acks_late=True, Celery acks only after the handler returns successfully. Combined with task_reject_on_worker_lost=True, any task interrupted by a crash is re-queued. The trade-off is that tasks may run twice if the handler completes but the ack message is lost, reinforcing the need for idempotency.","correct":"By default Celery acknowledges (removes) the task message from the broker before executing the task handler.","distractors":["Inside the task handler, catch the retryable exception and call self.retry(exc=exc, countdown=...) where countdown = (2 self.request.retries) * base_delay + random.uniform(-jitter, jitter).","At-least-once delivery means the broker guarantees a task message will be delivered to a worker *at least* once, but may deliver it multiple times on retry after a crash or timeout.","Three layers: (1) Flower — the official Celery web monitor, shows active workers, task rates, success/failure counts, and lets you revoke tasks."]},{"id":"python/fastapi/background_jobs_and_task_queues#Q4-2764","section":"python","module":"python/fastapi/background_jobs_and_task_queues","moduleName":"background jobs and task queues","qIndex":4,"difficulty":"core","question":"What is a dead-letter queue and when should you use one?","answerFull":"A dead-letter queue (DLQ) is a special queue that receives messages that have been rejected or have exhausted all retries. It prevents poison messages from blocking the main queue indefinitely and provides a recoverable holding area for investigation. Configure Celery DLQ via task_reject_on_worker_lost + a dedicated DLQ queue name; monitor its depth with an alert on LLEN dlq:notifications > 10. Without a DLQ, failed tasks either retry forever (starving other work) or disappear silently.","correct":"A dead-letter queue (DLQ) is a special queue that receives messages that have been rejected or have exhausted all retries.","distractors":["Inside the task handler, catch the retryable exception and call self.retry(exc=exc, countdown=...) where countdown = (2 self.request.retries) * base_delay + random.uniform(-jitter, jitter).","BackgroundTasks runs after the response in the same process with no persistence — a worker restart silently drops any queued tasks.","At-least-once delivery means the broker guarantees a task message will be delivered to a worker *at least* once, but may deliver it multiple times on retry after a crash or timeout."]},{"id":"python/fastapi/background_jobs_and_task_queues#Q5-2765","section":"python","module":"python/fastapi/background_jobs_and_task_queues","moduleName":"background jobs and task queues","qIndex":5,"difficulty":"core","question":"How do you implement exponential backoff with jitter in Celery?","answerFull":"Inside the task handler, catch the retryable exception and call self.retry(exc=exc, countdown=...) where countdown = (2 self.request.retries) * base_delay + random.uniform(-jitter, jitter). The self.request.retries counter is 0 on the first retry, 1 on the second, etc. Jitter (random offset) prevents thundering-herd: without it, a batch of tasks that all fail simultaneously all retry at the same instant, overwhelming the downstream service again.","correct":"Inside the task handler, catch the retryable exception and call self.retry(exc=exc, countdown=...) where countdown = (2 self.request.retries) * base_delay + random.uniform(-jitter, jitter).","distractors":["At-least-once delivery means the broker guarantees a task message will be delivered to a worker *at least* once, but may deliver it multiple times on retry after a crash or timeout.","Three layers: (1) Flower — the official Celery web monitor, shows active workers, task rates, success/failure counts, and lets you revoke tasks.","Canvas is Celery's workflow composition API."]},{"id":"python/fastapi/background_jobs_and_task_queues#Q6-2766","section":"python","module":"python/fastapi/background_jobs_and_task_queues","moduleName":"background jobs and task queues","qIndex":6,"difficulty":"intermediate","question":"Why is ARQ a better fit for FastAPI than Celery for I/O-bound tasks?","answerFull":"ARQ workers are native asyncio event loops. Each worker can run up to max_jobs=50 concurrent coroutines without threads, matching FastAPI's async model exactly. A single ARQ worker handles 50 concurrent HTTP-call tasks with the overhead of one OS thread. Celery's prefork workers spawn one OS process per concurrency slot; 50 concurrent I/O-bound tasks would require 50 processes (~50MB each = 2.5GB RAM). For CPU-bound tasks the calculus reverses: Celery prefork gives true parallelism; ARQ's event loop is single-core.","correct":"ARQ workers are native asyncio event loops.","distractors":["Canvas is Celery's workflow composition API.","BackgroundTasks runs after the response in the same process with no persistence — a worker restart silently drops any queued tasks.","By default Celery acknowledges (removes) the task message from the broker before executing the task handler."]},{"id":"python/fastapi/background_jobs_and_task_queues#Q7-2767","section":"python","module":"python/fastapi/background_jobs_and_task_queues","moduleName":"background jobs and task queues","qIndex":7,"difficulty":"intermediate","question":"What is the Redis SET NX EX pattern and how does it provide idempotency?","answerFull":"SET key value NX EX seconds is an atomic Redis command: set the key only if it does not exist (NX), with a TTL of seconds (EX). Used as an idempotency check before enqueuing a job: if SET NX returns nil, the key already exists, meaning a job with this idempotency key was already enqueued, so skip re-enqueuing. The TTL ensures the key expires after the idempotency window (typically 24 hours), preventing the Redis keyspace from growing unbounded. The atomicity of the command prevents TOCTOU races where two concurrent requests both check-and-set simultaneously.","correct":"SET key value NX EX seconds is an atomic Redis command: set the key only if it does not exist (NX), with a TTL of seconds (EX).","distractors":["The Outbox Pattern solves the dual-write problem: you cannot atomically write to a database and enqueue a message to Redis in a single transaction.","BackgroundTasks runs after the response in the same process with no persistence — a worker restart silently drops any queued tasks.","At-least-once delivery means the broker guarantees a task message will be delivered to a worker *at least* once, but may deliver it multiple times on retry after a crash or timeout."]},{"id":"python/fastapi/background_jobs_and_task_queues#Q8-2768","section":"python","module":"python/fastapi/background_jobs_and_task_queues","moduleName":"background jobs and task queues","qIndex":8,"difficulty":"intermediate","question":"Explain Celery Canvas — what are chain, group, and chord used for?","answerFull":"Canvas is Celery's workflow composition API. A chain(a.s(), b.s()) runs tasks sequentially, passing the result of a as the first argument to b. A group(a.s(), b.s(), c.s()) runs tasks in parallel and returns a list of results. A chord(group, callback) runs a group in parallel then calls callback with the list of results once all group tasks complete — the standard pattern for fan-out/fan-in. Use chains for dependent multi-step pipelines, groups for independent parallel work, and chords for map-reduce patterns (e.g., generate 10 report sections in parallel, then assemble them).","correct":"Canvas is Celery's workflow composition API.","distractors":["By default Celery acknowledges (removes) the task message from the broker before executing the task handler.","Three layers: (1) Flower — the official Celery web monitor, shows active workers, task rates, success/failure counts, and lets you revoke tasks.","BackgroundTasks runs after the response in the same process with no persistence — a worker restart silently drops any queued tasks."]},{"id":"python/fastapi/background_jobs_and_task_queues#Q9-2769","section":"python","module":"python/fastapi/background_jobs_and_task_queues","moduleName":"background jobs and task queues","qIndex":9,"difficulty":"intermediate","question":"What is the worker prefetch multiplier and why should it be set to 1 for long tasks?","answerFull":"The prefetch multiplier controls how many unacknowledged tasks each worker process fetches from the broker at once. The default is 4 (fetch 4 tasks per concurrent slot). If one task takes 10 minutes and another takes 1 second, a worker might hold 3 fast tasks idle while executing the long task — those 3 tasks are invisible to other free workers. Setting worker_prefetch_multiplier=1 ensures each worker fetches only one task at a time, enabling fair distribution across workers. For homogeneous short tasks, a higher prefetch improves throughput by reducing broker round-trips.","correct":"The prefetch multiplier controls how many unacknowledged tasks each worker process fetches from the broker at once.","distractors":["BackgroundTasks runs after the response in the same process with no persistence — a worker restart silently drops any queued tasks.","Three layers: (1) Flower — the official Celery web monitor, shows active workers, task rates, success/failure counts, and lets you revoke tasks.","SET key value NX EX seconds is an atomic Redis command: set the key only if it does not exist (NX), with a TTL of seconds (EX)."]},{"id":"python/fastapi/background_jobs_and_task_queues#Q10-2770","section":"python","module":"python/fastapi/background_jobs_and_task_queues","moduleName":"background jobs and task queues","qIndex":10,"difficulty":"intermediate","question":"How do you monitor a Celery task queue in production?","answerFull":"Three layers: (1) Flower — the official Celery web monitor, shows active workers, task rates, success/failure counts, and lets you revoke tasks. Run as a separate process celery -A tasks flower --port=5555. (2) Prometheus metrics via celery-prometheus- exporter — exposes queue lengths, task duration histograms, and worker state as metrics for alerting. (3) DLQ depth alert — monitor the DLQ Redis key length; alert if LLEN dlq:notifications > 0 for more than 5 minutes, indicating tasks are failing persistently.","correct":"Three layers: (1) Flower — the official Celery web monitor, shows active workers, task rates, success/failure counts, and lets you revoke tasks.","distractors":["Canvas is Celery's workflow composition API.","Inside the task handler, catch the retryable exception and call self.retry(exc=exc, countdown=...) where countdown = (2 self.request.retries) * base_delay + random.uniform(-jitter, jitter).","At-least-once delivery means the broker guarantees a task message will be delivered to a worker *at least* once, but may deliver it multiple times on retry after a crash or timeout."]},{"id":"python/fastapi/background_jobs_and_task_queues#Q11-2771","section":"python","module":"python/fastapi/background_jobs_and_task_queues","moduleName":"background jobs and task queues","qIndex":11,"difficulty":"advanced","question":"What is the Outbox Pattern and when is it needed with task queues?","answerFull":"The Outbox Pattern solves the dual-write problem: you cannot atomically write to a database and enqueue a message to Redis in a single transaction. If you write the DB row and then crash before enqueueing, the event is lost. The outbox approach: write both the DB row and an outbox_events row in the *same database transaction*. A separate poller reads outbox_events, enqueues the message to Redis/RabbitMQ, and marks the event as processed. This guarantees at-least-once delivery even if the application crashes between the DB commit and the enqueue call. Use it for payment events, user lifecycle events, or anything where the database state and the queue must stay consistent.","correct":"The Outbox Pattern solves the dual-write problem: you cannot atomically write to a database and enqueue a message to Redis in a single transaction.","distractors":["By default Celery acknowledges (removes) the task message from the broker before executing the task handler.","Three layers: (1) Flower — the official Celery web monitor, shows active workers, task rates, success/failure counts, and lets you revoke tasks.","At-least-once delivery means the broker guarantees a task message will be delivered to a worker *at least* once, but may deliver it multiple times on retry after a crash or timeout."]},{"id":"python/fastapi/background_jobs_and_task_queues#Q12-2772","section":"python","module":"python/fastapi/background_jobs_and_task_queues","moduleName":"background jobs and task queues","qIndex":12,"difficulty":"advanced","question":"How do you gracefully shut down a Celery or ARQ worker during a deployment?","answerFull":"Send SIGTERM (not SIGKILL). Celery workers catch SIGTERM, stop fetching new tasks, and wait for current tasks to complete (up to --time-limit seconds) before exiting. ARQ workers behave similarly — the event loop processes its current job batch and exits. For Kubernetes, set terminationGracePeriodSeconds to at least your longest expected task duration (e.g., 120 seconds), and configure preStop hooks to send SIGTERM before the pod is killed. Never send SIGKILL to workers directly — it bypasses graceful shutdown and can leave tasks in an inconsistent state (partially processed, already acked).","correct":"Send SIGTERM (not SIGKILL).","distractors":["Three layers: (1) Flower — the official Celery web monitor, shows active workers, task rates, success/failure counts, and lets you revoke tasks.","BackgroundTasks runs after the response in the same process with no persistence — a worker restart silently drops any queued tasks.","At-least-once delivery means the broker guarantees a task message will be delivered to a worker *at least* once, but may deliver it multiple times on retry after a crash or timeout."]},{"id":"python/fastapi/observability_and_monitoring#Q2-2773","section":"python","module":"python/fastapi/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":2,"difficulty":"core","question":"Why is print() wrong in production and what should replace it?","answerFull":"print() bypasses the Python logging hierarchy, cannot be filtered by level, has no structured fields, is not thread-safe by default, and its output cannot be dynamically silenced or redirected without monkey-patching. Replace it with logging.getLogger(name) at minimum, or structlog for structured JSON output. Structured logs are queryable in aggregators like Loki or Elasticsearch; plain print output is a string that requires regex parsing.","correct":"print() bypasses the Python logging hierarchy, cannot be filtered by level, has no structured fields, is not thread-safe by default, and its output cannot be dynamically silenced or redirected without monkey-patching.","distractors":["BatchSpanProcessor queues spans in memory and flushes them to the exporter in batches on a background thread — default batch size 512, flush interval 5 s.","Head-based sampling makes the keep/drop decision at trace ingestion (before the trace completes) — it is fast but cannot preferentially keep slow or erroneous traces.","SRE teams commonly use: 5xx error rate > 1% over 5 minutes as a page-level alert; p99 latency > 500 ms over 5 minutes as a warn-level alert; p99 latency > 2 s over 2 minutes as a page-level alert."]},{"id":"python/fastapi/observability_and_monitoring#Q3-2774","section":"python","module":"python/fastapi/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":3,"difficulty":"core","question":"What is a ContextVar and why is it preferred over a global variable for request-scoped state?","answerFull":"contextvars.ContextVar stores a value per-execution-context. In asyncio, each asyncio.Task has its own context copy, so two concurrent requests cannot overwrite each other's request_id stored in a ContextVar. A module-level global variable has one value shared across all concurrent requests — request A's ID would overwrite request B's ID, producing incorrect correlation. ContextVar is the correct primitive for request-scoped data in async Python.","correct":"contextvars.ContextVar stores a value per-execution-context.","distractors":["Prometheus stores every unique label combination as a separate in-memory time-series.","It wraps the FastAPI/Starlette ASGI application as a middleware and hooks into the request/response lifecycle.","The OTel context is stored per-asyncio.Task via ContextVar."]},{"id":"python/fastapi/observability_and_monitoring#Q4-2775","section":"python","module":"python/fastapi/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":4,"difficulty":"core","question":"What is the difference between a liveness probe and a readiness probe? What happens if you conflate them?","answerFull":"A liveness probe answers \"is the process deadlocked or crashed?\" — failure triggers a pod restart. A readiness probe answers \"are external dependencies ready?\" — failure removes the pod from the load-balancer without restarting it. Conflating them by putting a database check in /health causes pod restart loops when the database is temporarily down: K8s restarts the pod, the pod tries to reconnect to a still-down database, fails, gets restarted again — a thundering herd that exacerbates the outage. The correct split: /health returns 200 if the process is alive (zero external calls); /ready returns 503 until all dependencies respond.","correct":"A liveness probe answers \"is the process deadlocked or crashed?\" — failure triggers a pod restart.","distractors":["print() bypasses the Python logging hierarchy, cannot be filtered by level, has no structured fields, is not thread-safe by default, and its output cannot be dynamically silenced or redirected without monkey-patching.","contextvars.ContextVar stores a value per-execution-context.","Prometheus stores every unique label combination as a separate in-memory time-series."]},{"id":"python/fastapi/observability_and_monitoring#Q5-2776","section":"python","module":"python/fastapi/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":5,"difficulty":"core","question":"Why can you not use user_id or order_id as a Prometheus label?","answerFull":"Prometheus stores every unique label combination as a separate in-memory time-series. With 1 million users, the label user_id creates 1 million time-series for a single counter. At ~256 bytes per series, that is 256 MB for one metric. Prometheus is designed for low-cardinality labels (method, status class, endpoint) with at most a few hundred unique values. High-cardinality identifiers belong in logs (queryable by field) and traces (span attributes have no cardinality limit).","correct":"Prometheus stores every unique label combination as a separate in-memory time-series.","distractors":["contextvars.ContextVar stores a value per-execution-context.","BatchSpanProcessor queues spans in memory and flushes them to the exporter in batches on a background thread — default batch size 512, flush interval 5 s.","print() bypasses the Python logging hierarchy, cannot be filtered by level, has no structured fields, is not thread-safe by default, and its output cannot be dynamically silenced or redirected without monkey-patching."]},{"id":"python/fastapi/observability_and_monitoring#Q6-2777","section":"python","module":"python/fastapi/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":6,"difficulty":"intermediate","question":"Describe the W3C TraceContext header format and why it matters.","answerFull":"The traceparent header format is {version}-{trace_id}-{parent_span_id}-{trace_flags}, for example 00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01. It provides a vendor-neutral standard for propagating trace context across HTTP service boundaries. When every service in your stack reads and writes traceparent, all spans across all services join into a single distributed trace regardless of which tracing backend each service uses. Without it, traces fragment at service boundaries and you cannot see cross-service latency.","correct":"The traceparent header format is {version}-{trace_id}-{parent_span_id}-{trace_flags}, for example 00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01.","distractors":["Head-based sampling makes the keep/drop decision at trace ingestion (before the trace completes) — it is fast but cannot preferentially keep slow or erroneous traces.","print() bypasses the Python logging hierarchy, cannot be filtered by level, has no structured fields, is not thread-safe by default, and its output cannot be dynamically silenced or redirected without monkey-patching.","contextvars.ContextVar stores a value per-execution-context."]},{"id":"python/fastapi/observability_and_monitoring#Q7-2778","section":"python","module":"python/fastapi/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":7,"difficulty":"intermediate","question":"What is tail-based sampling and when is it better than head-based sampling?","answerFull":"Head-based sampling makes the keep/drop decision at trace ingestion (before the trace completes) — it is fast but cannot preferentially keep slow or erroneous traces. Tail-based sampling buffers all spans for a trace until the trace completes, then applies rules: keep all traces with errors, keep all traces with p99 > 1 s, sample 1% of healthy fast traces. This preserves 100% of actionable signals at a fraction of the storage cost. The downside: the tail-based collector must buffer in-flight spans in memory, requiring more collector resources.","correct":"Head-based sampling makes the keep/drop decision at trace ingestion (before the trace completes) — it is fast but cannot preferentially keep slow or erroneous traces.","distractors":["The traceparent header format is {version}-{trace_id}-{parent_span_id}-{trace_flags}, for example 00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01.","print() bypasses the Python logging hierarchy, cannot be filtered by level, has no structured fields, is not thread-safe by default, and its output cannot be dynamically silenced or redirected without monkey-patching.","contextvars.ContextVar stores a value per-execution-context."]},{"id":"python/fastapi/observability_and_monitoring#Q8-2779","section":"python","module":"python/fastapi/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":8,"difficulty":"intermediate","question":"How does prometheus-fastapi-instrumentator work and what does it instrument automatically?","answerFull":"It wraps the FastAPI/Starlette ASGI application as a middleware and hooks into the request/response lifecycle. On each request it records start time; on each response it computes duration and increments starlette_requests_total (labels: method, handler, status) and observes starlette_request_duration_seconds (histogram with default latency buckets). It also exposes a /metrics endpoint that Prometheus scrapes. It does not instrument background tasks, outbound HTTP calls, or database queries — those require OTel auto-instrumentation or manual instrumentation.","correct":"It wraps the FastAPI/Starlette ASGI application as a middleware and hooks into the request/response lifecycle.","distractors":["Prometheus stores every unique label combination as a separate in-memory time-series.","print() bypasses the Python logging hierarchy, cannot be filtered by level, has no structured fields, is not thread-safe by default, and its output cannot be dynamically silenced or redirected without monkey-patching.","contextvars.ContextVar stores a value per-execution-context."]},{"id":"python/fastapi/observability_and_monitoring#Q9-2780","section":"python","module":"python/fastapi/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":9,"difficulty":"intermediate","question":"What is the BatchSpanProcessor and when would you use SimpleSpanProcessor instead?","answerFull":"BatchSpanProcessor queues spans in memory and flushes them to the exporter in batches on a background thread — default batch size 512, flush interval 5 s. This minimises the latency impact of exporting on the request path. SimpleSpanProcessor exports each span synchronously before returning — it adds the exporter's network RTT to every request. Use SimpleSpanProcessor only in development or testing where you want immediate span visibility. In production, always use BatchSpanProcessor.","correct":"BatchSpanProcessor queues spans in memory and flushes them to the exporter in batches on a background thread — default batch size 512, flush interval 5 s.","distractors":["print() bypasses the Python logging hierarchy, cannot be filtered by level, has no structured fields, is not thread-safe by default, and its output cannot be dynamically silenced or redirected without monkey-patching.","Prometheus stores every unique label combination as a separate in-memory time-series.","contextvars.ContextVar stores a value per-execution-context."]},{"id":"python/fastapi/observability_and_monitoring#Q10-2781","section":"python","module":"python/fastapi/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":10,"difficulty":"intermediate","question":"How do you propagate OpenTelemetry trace context into a Celery or asyncio background task?","answerFull":"The OTel context is stored per-asyncio.Task via ContextVar. When you spawn a background task (via asyncio.create_task, BackgroundTasks, or Celery), the new execution context does not automatically inherit the parent's OTel context. The fix is to capture the context with otel_context.get_current() before spawning the task, pass it as an argument, and restore it with otel_context.attach(ctx) at the start of the task — followed by otel_context.detach(token) in a finally block. For Celery, OpenTelemetry provides a CeleryInstrumentor that handles propagation via task headers.","correct":"The OTel context is stored per-asyncio.Task via ContextVar.","distractors":["Head-based sampling makes the keep/drop decision at trace ingestion (before the trace completes) — it is fast but cannot preferentially keep slow or erroneous traces.","The traceparent header format is {version}-{trace_id}-{parent_span_id}-{trace_flags}, for example 00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01.","contextvars.ContextVar stores a value per-execution-context."]},{"id":"python/fastapi/observability_and_monitoring#Q11-2782","section":"python","module":"python/fastapi/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":11,"difficulty":"advanced","question":"What alerting thresholds are industry-standard for a web API?","answerFull":"SRE teams commonly use: 5xx error rate > 1% over 5 minutes as a page-level alert; p99 latency > 500 ms over 5 minutes as a warn-level alert; p99 latency > 2 s over 2 minutes as a page-level alert. Availability SLO of 99.9% (43 min downtime/month) is typical for non-critical APIs; 99.95% for customer-facing paths. These are Prometheus recording rules evaluated by Alertmanager with a for: 5m clause to prevent noise from transient spikes.","correct":"SRE teams commonly use: 5xx error rate > 1% over 5 minutes as a page-level alert; p99 latency > 500 ms over 5 minutes as a warn-level alert; p99 latency > 2 s over 2 minutes as a page-level alert.","distractors":["print() bypasses the Python logging hierarchy, cannot be filtered by level, has no structured fields, is not thread-safe by default, and its output cannot be dynamically silenced or redirected without monkey-patching.","contextvars.ContextVar stores a value per-execution-context.","A liveness probe answers \"is the process deadlocked or crashed?\" — failure triggers a pod restart."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q1-2783","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":1,"difficulty":"core","question":"What is pydantic-core and why is Pydantic v2 significantly faster than v1?","answerFull":"Pydantic-core is a compiled extension module written in Rust (via PyO3) that implements the entire validation and serialization pipeline. In v1, validation was pure Python: every field check was a Python function call with associated bytecode overhead, attribute lookups, and GC pressure. In v2, the schema is compiled to a SchemaValidator Rust struct at class-definition time, and validation calls drop into native code. The result is 5–50x faster throughput (e.g., ~312 k/s in v1 vs ~5.6 M/s in v2 for simple flat models). Speedup is smaller (~5x) for models with many Python @field_validator callbacks because each callback crosses the Python/Rust boundary.","correct":"Pydantic-core is a compiled extension module written in Rust (via PyO3) that implements the entire validation and serialization pipeline.","distractors":["By default (revalidate_instances=\"never\"), if you pass a BaseModel instance where a BaseModel type is expected, Pydantic trusts it and skips re-validation.","model_rebuild() re-compiles the Pydantic core schema for a model.","mode=\"wrap\" gives the Python callable a handler argument, which is a callable that invokes the compiled Rust validator."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q2-2784","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":2,"difficulty":"core","question":"What is the difference between @field_validator modes \"before\", \"after\", and \"wrap\"?","answerFull":"mode=\"before\" runs before type coercion and receives the raw input; use it for normalization (strip, lowercase). mode=\"after\" runs after coercion and receives the typed Python value; use it for business logic (age >= 18). mode=\"wrap\" receives the raw input plus a handler callable that invokes the rest of the pipeline; use it when you need to decide whether to call the default pipeline, replace it, or post-process its result. \"wrap\" is the most powerful but also the most complex to reason about.","correct":"mode=\"before\" runs before type coercion and receives the raw input; use it for normalization (strip, lowercase).","distractors":["CoreSchema is a Python TypedDict that describes a type tree — it is the intermediate representation between your Python type annotations and the Rust validator.","When from_attributes=True is set in ConfigDict, model_validate(obj) reads field values from obj.field_name attributes (using getattr) instead of treating the input as a dict.","model_dump(mode=\"json\") returns a Python dict where all values have been converted to JSON-compatible types (e.g., datetime → ISO string, UUID → string)."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q3-2785","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":3,"difficulty":"core","question":"How does from_attributes=True work and when do you need it?","answerFull":"When from_attributes=True is set in ConfigDict, model_validate(obj) reads field values from obj.field_name attributes (using getattr) instead of treating the input as a dict. This is required when constructing Pydantic schemas from SQLAlchemy ORM instances, ORMs from other frameworks, or any Python object that exposes data as attributes rather than as a mapping. Without this flag, passing an ORM row to model_validate raises a ValidationError because the row object is not subscriptable.","correct":"When from_attributes=True is set in ConfigDict, model_validate(obj) reads field values from obj.field_name attributes (using getattr) instead of treating the input as a dict.","distractors":["TypeAdapter validates arbitrary types without defining a model class: TypeAdapter(list[int]).validate_python([\"1\",\"2\"]) returns [1, 2].","With from_attributes=True in ConfigDict, model_validate accepts objects that expose values as attributes (e.g., SQLAlchemy Row objects) instead of requiring a dict.","Setting frozen=True makes the model immutable after creation: any attempt to set an attribute raises a ValidationError."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q4-2786","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":4,"difficulty":"core","question":"How do you perform cross-field validation — for example, ensuring end_date > start_date?","answerFull":"Use @model_validator(mode=\"after\"). This runs after all individual field validators have passed and gives you access to self (the partially constructed model). Raise ValueError if the constraint is violated. Do not use @field_validator for cross-field logic because validators for one field cannot reliably access another field's final validated value.","correct":"Use @model_validator(mode=\"after\").","distractors":["TypeAdapter wraps the CoreSchema compiler and Rust validator for any type expression — not just BaseModel subclasses.","Pydantic-core is a compiled extension module written in Rust (via PyO3) that implements the entire validation and serialization pipeline.","model_dump(mode=\"json\") returns a Python dict where all values have been converted to JSON-compatible types (e.g., datetime → ISO string, UUID → string)."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q5-2787","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":5,"difficulty":"core","question":"How do nested Pydantic models work during validation and serialization?","answerFull":"Nested models are declared as field types directly: address: Address. During validation, the nested dict is automatically passed to Address.init and validated recursively. If validation fails in the nested model, the error path in ValidationError includes the nested field name (e.g., address.city). During serialization, model_dump() recursively converts nested models to dicts; model_dump_json() serializes the entire tree to JSON in one Rust call.","correct":"Nested models are declared as field types directly: address: Address.","distractors":["Pydantic provides a PYDANTIC_V1_COMPAT compatibility layer in v2 — many v1 patterns still work but emit deprecation warnings.","By default (revalidate_instances=\"never\"), if you pass a BaseModel instance where a BaseModel type is expected, Pydantic trusts it and skips re-validation.","pydantic-core is a Rust extension (compiled via PyO3) that implements Pydantic v2's validation and serialization engine."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q6-2788","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":6,"difficulty":"intermediate","question":"How do you create reusable field constraints in Pydantic v2?","answerFull":"Use Annotated[T, Field(...)] to define a named type alias: PositiveInt = Annotated[int, Field(gt=0)]. This type can be used as a field annotation in any model without subclassing. Multiple constraints can be stacked: Annotated[str, Field(min_length=1, max_length=100, pattern=r\"^[a-z]+$\")]. This is the idiomatic v2 approach; do not subclass int or str just to add constraints.","correct":"Use Annotated[T, Field(...)] to define a named type alias: PositiveInt = Annotated[int, Field(gt=0)].","distractors":["model_rebuild() re-compiles the Pydantic core schema for a model.","Field(gt=0, lt=100) constraints are compiled into the Rust SchemaValidator — they execute entirely in Rust with no Python round-trip.","Inherit from pydantic_settings.BaseSettings instead of BaseModel."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q8-2789","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":8,"difficulty":"intermediate","question":"What is a discriminated union and why is it more efficient than a plain Union?","answerFull":"A discriminated union uses a Literal field (the discriminator) as a fast lookup key to select the correct union branch before attempting full validation. With a plain Union[A, B, C], Pydantic tries each branch in order until one succeeds — O(n) attempts. With Annotated[Union[A, B, C], Field(discriminator=\"type\")], Pydantic reads the discriminator value and maps it directly to the correct model in O(1). This also produces clearer validation errors: instead of \"none of the union variants matched\", you get \"type='unknown_value' is not valid\".","correct":"A discriminated union uses a Literal field (the discriminator) as a fast lookup key to select the correct union branch before attempting full validation.","distractors":["pydantic-core is a Rust extension (compiled via PyO3) that implements Pydantic v2's validation and serialization engine.","Pydantic-core is a compiled extension module written in Rust (via PyO3) that implements the entire validation and serialization pipeline.","Optional[str] is equivalent to str | None — it changes the type but does NOT make the field optional in the sense of having a default."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q10-2790","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":10,"difficulty":"intermediate","question":"How do you validate environment variables with BaseSettings?","answerFull":"Inherit from pydantic_settings.BaseSettings instead of BaseModel. Declare fields with their expected types and defaults. Pydantic reads matching environment variable names (case-insensitive by default). Set model_config = SettingsConfigDict(env_file=\".env\") to also read from a .env file. Use env_nested_delimiter=\"\" to populate nested models from env vars like DBHOST. Use SecretStr for sensitive values — the secret is never exposed in repr or model_dump() unless .get_secret_value() is called explicitly.","correct":"Inherit from pydantic_settings.BaseSettings instead of BaseModel.","distractors":["By default (revalidate_instances=\"never\"), if you pass a BaseModel instance where a BaseModel type is expected, Pydantic trusts it and skips re-validation.","Pydantic-core is a compiled extension module written in Rust (via PyO3) that implements the entire validation and serialization pipeline.","A BaseModel instance with 10 fields occupies approximately 400–600 bytes, compared to 280–320 bytes for a stdlib @dataclass and 220–260 bytes for @dataclass(slots=True)."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q11-2791","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":11,"difficulty":"advanced","question":"How do you write a custom type in Pydantic v2?","answerFull":"Implement get_pydantic_core_schema as a classmethod on your type. This method receives the source type and a handler, and must return a core_schema.CoreSchema instance (from pydantic_core). Use core_schema.no_info_plain_validator_function(cls._validate) for a simple validate-and-return pattern. The validator is called with the raw input and must return an instance of the custom type or raise ValueError. Add a serialization key to the schema to control output format.","correct":"Implement get_pydantic_core_schema as a classmethod on your type.","distractors":["model_rebuild() re-compiles the Pydantic core schema for a model.","By default, Pydantic v2 does NOT run validators on fields that use their default value.","Two reasons: (1) model_validate_json uses pydantic-core's built-in Rust JSON parser (sonic-rs based), which is faster than CPython's json.loads for payloads over ~500 bytes."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q12-2792","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":12,"difficulty":"advanced","question":"What does model_config = ConfigDict(validate_default=True) do?","answerFull":"By default, Pydantic v2 does NOT run validators on fields that use their default value. Setting validate_default=True forces validators to run even when the default is used. This is important when your defaults are computed values that might fail validation (e.g., a default derived from an environment variable) or when your @field_validator has side effects that must always execute.","correct":"By default, Pydantic v2 does NOT run validators on fields that use their default value.","distractors":["When from_attributes=True is set in ConfigDict, model_validate(obj) reads field values from obj.field_name attributes (using getattr) instead of treating the input as a dict.","With from_attributes=True in ConfigDict, model_validate accepts objects that expose values as attributes (e.g., SQLAlchemy Row objects) instead of requiring a dict.","TypeAdapter validates arbitrary types without defining a model class: TypeAdapter(list[int]).validate_python([\"1\",\"2\"]) returns [1, 2]."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q13-2793","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":13,"difficulty":"advanced","question":"How do you handle optional fields vs fields with a default of None?","answerFull":"Optional[str] is equivalent to str | None — it changes the type but does NOT make the field optional in the sense of having a default. name: Optional[str] still requires name to be present in the input (as None or a string). name: Optional[str] = None makes the field optional with a None default — it can be omitted from input entirely. Use model_dump(exclude_none=True) to suppress None fields in output.","correct":"Optional[str] is equivalent to str | None — it changes the type but does NOT make the field optional in the sense of having a default.","distractors":["Use model_construct() only for data you already own and have validated — for example, copying fields from one validated model to another, or reading from a trusted in-process cache.","A discriminated (tagged) union specifies a literal-typed field (Literal[\"click\"], Literal[\"purchase\"], etc.) as a discriminator.","A BaseModel instance with 10 fields occupies approximately 400–600 bytes, compared to 280–320 bytes for a stdlib @dataclass and 220–260 bytes for @dataclass(slots=True)."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q14-2794","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":14,"difficulty":"advanced","question":"How do you use TypeAdapter and when is it preferable to a full BaseModel?","answerFull":"TypeAdapter validates arbitrary types without defining a model class: TypeAdapter(list[int]).validate_python([\"1\",\"2\"]) returns [1, 2]. Use it when you need to validate a single value, a generic container (list[MyModel]), or a union type without the overhead of a full model class definition. It also exposes validate_json, dump_python, and json_schema methods matching the BaseModel API.","correct":"TypeAdapter validates arbitrary types without defining a model class: TypeAdapter(list[int]).validate_python([\"1\",\"2\"]) returns [1, 2].","distractors":["TypeAdapter.init triggers CoreSchema construction and Rust SchemaValidator compilation.","When from_attributes=True is set in ConfigDict, model_validate(obj) reads field values from obj.field_name attributes (using getattr) instead of treating the input as a dict.","TypeAdapter wraps the CoreSchema compiler and Rust validator for any type expression — not just BaseModel subclasses."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q15-2795","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":15,"difficulty":"advanced","question":"What is model_dump(mode=\"json\") vs model_dump_json()?","answerFull":"model_dump(mode=\"json\") returns a Python dict where all values have been converted to JSON-compatible types (e.g., datetime → ISO string, UUID → string). model_dump_json() returns a bytes/str JSON string directly using the Rust serializer, which is faster. Use model_dump(mode=\"json\") when you need a dict for further manipulation before serializing; use model_dump_json() for the fastest direct JSON output.","correct":"model_dump(mode=\"json\") returns a Python dict where all values have been converted to JSON-compatible types (e.g., datetime → ISO string, UUID → string).","distractors":["mode=\"wrap\" gives the Python callable a handler argument, which is a callable that invokes the compiled Rust validator.","Use @model_validator(mode=\"after\").","TypeAdapter validates arbitrary types without defining a model class: TypeAdapter(list[int]).validate_python([\"1\",\"2\"]) returns [1, 2]."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q16-2796","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":16,"difficulty":"advanced","question":"How does ConfigDict(frozen=True) affect a model?","answerFull":"Setting frozen=True makes the model immutable after creation: any attempt to set an attribute raises a ValidationError. It also makes the model hashable (implements hash), so instances can be used as dict keys or set members. Internally, Pydantic sets setattr and delattr to raise exceptions. Use frozen=True for value objects — DTOs that are passed around but never modified.","correct":"Setting frozen=True makes the model immutable after creation: any attempt to set an attribute raises a ValidationError.","distractors":["defer_build=True in ConfigDict delays CoreSchema compilation and Rust validator construction until the model is first used.","When from_attributes=True is set in ConfigDict, model_validate(obj) reads field values from obj.field_name attributes (using getattr) instead of treating the input as a dict.","With from_attributes=True in ConfigDict, model_validate accepts objects that expose values as attributes (e.g., SQLAlchemy Row objects) instead of requiring a dict."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q17-2797","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":17,"difficulty":"advanced","question":"How do you handle v1 → v2 migration for a large codebase?","answerFull":"Pydantic provides a PYDANTIC_V1_COMPAT compatibility layer in v2 — many v1 patterns still work but emit deprecation warnings. A safe migration path: (1) install Pydantic v2, (2) run your test suite and collect all PydanticDeprecatedSince20 warnings, (3) replace @validator with @field_validator, class Config with ConfigDict, .dict() with .model_dump(), .json() with .model_dump_json(), parse_obj with model_validate, (4) replace fields accesses with model_fields. The bump-pydantic CLI tool automates most of these rewrites.","correct":"Pydantic provides a PYDANTIC_V1_COMPAT compatibility layer in v2 — many v1 patterns still work but emit deprecation warnings.","distractors":["By default, Pydantic v2 does NOT run validators on fields that use their default value.","Pydantic-core is a compiled extension module written in Rust (via PyO3) that implements the entire validation and serialization pipeline.","Two reasons: (1) model_validate_json uses pydantic-core's built-in Rust JSON parser (sonic-rs based), which is faster than CPython's json.loads for payloads over ~500 bytes."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q18-2798","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":18,"difficulty":"advanced","question":"What is the purpose of model_rebuild() and when is it needed?","answerFull":"model_rebuild() re-compiles the Pydantic core schema for a model. It is needed when a model has forward references (from future import annotations or string annotations) that were not resolved at class definition time, or when you add fields dynamically after class creation. In practice, you call MyModel.model_rebuild() at the end of a module after all referenced types are defined. FastAPI calls it automatically during app startup for models registered as request/response bodies.","correct":"model_rebuild() re-compiles the Pydantic core schema for a model.","distractors":["Two reasons: (1) model_validate_json uses pydantic-core's built-in Rust JSON parser (sonic-rs based), which is faster than CPython's json.loads for payloads over ~500 bytes.","By default (revalidate_instances=\"never\"), if you pass a BaseModel instance where a BaseModel type is expected, Pydantic trusts it and skips re-validation.","Pydantic-core is a compiled extension module written in Rust (via PyO3) that implements the entire validation and serialization pipeline."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q1-2799","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":1,"difficulty":"core","question":"What is pydantic-core and what problem does it solve?","answerFull":"pydantic-core is a Rust extension (compiled via PyO3) that implements Pydantic v2's validation and serialization engine. It solves the performance bottleneck of v1, where validation was a recursive Python function call per field per request. By moving the inner loop to compiled Rust, v2 achieves 5–50x faster throughput on validation-heavy workloads. The schema is compiled once at class definition time; subsequent validations are FFI calls into the Rust engine with no Python interpreter involvement per field.","correct":"pydantic-core is a Rust extension (compiled via PyO3) that implements Pydantic v2's validation and serialization engine.","distractors":["Two reasons: (1) model_validate_json uses pydantic-core's built-in Rust JSON parser (sonic-rs based), which is faster than CPython's json.loads for payloads over ~500 bytes.","model_dump_json() produces JSON bytes entirely in Rust — no Python dict is allocated, and types like datetime, UUID, and Decimal are serialized by the Rust engine directly.","Implement get_pydantic_core_schema as a classmethod on your type."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q2-2800","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":2,"difficulty":"core","question":"What is a CoreSchema and when is it built?","answerFull":"CoreSchema is a Python TypedDict that describes a type tree — it is the intermediate representation between your Python type annotations and the Rust validator. It is built by ModelMetaclass.new at class definition time (i.e., when Python executes the class statement), not at validation time. Once built, it is passed to pydantic_core.SchemaValidator, which compiles it into a Rust object stored as pydantic_validator on the class.","correct":"CoreSchema is a Python TypedDict that describes a type tree — it is the intermediate representation between your Python type annotations and the Rust validator.","distractors":["TypeAdapter.init triggers CoreSchema construction and Rust SchemaValidator compilation.","defer_build=True in ConfigDict delays CoreSchema compilation and Rust validator construction until the model is first used.","mode=\"wrap\" gives the Python callable a handler argument, which is a callable that invokes the compiled Rust validator."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q3-2801","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":3,"difficulty":"core","question":"Why is model_validate_json(raw_bytes) faster than model_validate(json.loads(raw_bytes))?","answerFull":"Two reasons: (1) model_validate_json uses pydantic-core's built-in Rust JSON parser (sonic-rs based), which is faster than CPython's json.loads for payloads over ~500 bytes. (2) The parsed values are never materialised as a Python dict — they flow directly from the JSON token stream into the Rust field validators and then into the model instance. The two-step path allocates a Python dict and then copies every value through the FFI boundary a second time. The speedup is approximately 2x for large payloads and 30–40% for small ones.","correct":"Two reasons: (1) model_validate_json uses pydantic-core's built-in Rust JSON parser (sonic-rs based), which is faster than CPython's json.loads for payloads over ~500 bytes.","distractors":["TypeAdapter wraps the CoreSchema compiler and Rust validator for any type expression — not just BaseModel subclasses.","CoreSchema is a Python TypedDict that describes a type tree — it is the intermediate representation between your Python type annotations and the Rust validator.","Field(gt=0, lt=100) constraints are compiled into the Rust SchemaValidator — they execute entirely in Rust with no Python round-trip."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q4-2802","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":4,"difficulty":"core","question":"When should you use model_construct() and what are the risks?","answerFull":"Use model_construct() only for data you already own and have validated — for example, copying fields from one validated model to another, or reading from a trusted in-process cache. It is 8–10x faster than model_validate() because it bypasses the Rust validator entirely and assigns attributes directly. The risk is silent data corruption: model_construct(count=-5) on a model with Annotated[int, Field(gt=0)] will store -5 without raising any error. Never use it for external input.","correct":"Use model_construct() only for data you already own and have validated — for example, copying fields from one validated model to another, or reading from a trusted in-process cache.","distractors":["By default, Pydantic v2 does NOT run validators on fields that use their default value.","A BaseModel instance with 10 fields occupies approximately 400–600 bytes, compared to 280–320 bytes for a stdlib @dataclass and 220–260 bytes for @dataclass(slots=True).","model_dump(mode=\"json\") returns a Python dict where all values have been converted to JSON-compatible types (e.g., datetime → ISO string, UUID → string)."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q5-2803","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":5,"difficulty":"core","question":"What is a discriminated union and why does it matter for performance?","answerFull":"A discriminated (tagged) union specifies a literal-typed field (Literal[\"click\"], Literal[\"purchase\"], etc.) as a discriminator. The Rust TaggedUnionValidator reads that one field and performs an O(1) hash-map lookup to the correct sub-validator. An untagged union attempts each branch in order until one succeeds — O(n) in the number of branches, where each failed branch pays partial validation cost. For a union of 20 models, the discriminated approach is 10–15x faster in the worst case.","correct":"A discriminated (tagged) union specifies a literal-typed field (Literal[\"click\"], Literal[\"purchase\"], etc.) as a discriminator.","distractors":["Field(gt=0, lt=100) constraints are compiled into the Rust SchemaValidator — they execute entirely in Rust with no Python round-trip.","Nested models are declared as field types directly: address: Address.","A discriminated union uses a Literal field (the discriminator) as a fast lookup key to select the correct union branch before attempting full validation."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q6-2804","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":6,"difficulty":"intermediate","question":"What does TypeAdapter do and when do you prefer it over BaseModel?","answerFull":"TypeAdapter wraps the CoreSchema compiler and Rust validator for any type expression — not just BaseModel subclasses. Use it for validating list[T], dict[str, T], or Annotated types at application boundaries. It avoids the overhead of defining a wrapper BaseModel and is slightly lighter in memory. Always create TypeAdapter instances at module level — construction triggers schema compilation, which costs 0.5–2 ms; reuse is O(validation cost only).","correct":"TypeAdapter wraps the CoreSchema compiler and Rust validator for any type expression — not just BaseModel subclasses.","distractors":["TypeAdapter validates arbitrary types without defining a model class: TypeAdapter(list[int]).validate_python([\"1\",\"2\"]) returns [1, 2].","A BaseModel instance with 10 fields occupies approximately 400–600 bytes, compared to 280–320 bytes for a stdlib @dataclass and 220–260 bytes for @dataclass(slots=True).","TypeAdapter.init triggers CoreSchema construction and Rust SchemaValidator compilation."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q7-2805","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":7,"difficulty":"intermediate","question":"How do @field_validator and Annotated[T, Field(...)] differ in performance?","answerFull":"Field(gt=0, lt=100) constraints are compiled into the Rust SchemaValidator — they execute entirely in Rust with no Python round-trip. @field_validator decorates a Python callable; the Rust engine calls back into Python (acquiring the GIL) once per decorated field per validation call. For hot-path models validated millions of times, moving structural constraints from @field_validator to Annotated types with Field eliminates the Python FFI round-trips and is measurably faster. Reserve @field_validator for business logic that cannot be expressed as a declarable constraint.","correct":"Field(gt=0, lt=100) constraints are compiled into the Rust SchemaValidator — they execute entirely in Rust with no Python round-trip.","distractors":["model_dump_json() produces JSON bytes entirely in Rust — no Python dict is allocated, and types like datetime, UUID, and Decimal are serialized by the Rust engine directly.","Pydantic-core is a compiled extension module written in Rust (via PyO3) that implements the entire validation and serialization pipeline.","CoreSchema is a Python TypedDict that describes a type tree — it is the intermediate representation between your Python type annotations and the Rust validator."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q8-2806","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":8,"difficulty":"intermediate","question":"What is defer_build=True and when should you use it?","answerFull":"defer_build=True in ConfigDict delays CoreSchema compilation and Rust validator construction until the model is first used. This reduces import time for applications with many models where only a subset is used on any given code path (e.g., Lambda functions, microservices with shared model libraries). The tradeoff is that the first validation call for a deferred model pays the compilation cost (~0.5–4 ms depending on model complexity). After the first call, the compiled validator is cached and reused normally. Call Model.model_rebuild() explicitly if you need to ensure compilation completes before the first request arrives.","correct":"defer_build=True in ConfigDict delays CoreSchema compilation and Rust validator construction until the model is first used.","distractors":["Use @model_validator(mode=\"after\").","Pydantic-core is a compiled extension module written in Rust (via PyO3) that implements the entire validation and serialization pipeline.","TypeAdapter wraps the CoreSchema compiler and Rust validator for any type expression — not just BaseModel subclasses."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q9-2807","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":9,"difficulty":"intermediate","question":"How does model_dump_json() differ from json.dumps(model.model_dump())?","answerFull":"model_dump_json() produces JSON bytes entirely in Rust — no Python dict is allocated, and types like datetime, UUID, and Decimal are serialized by the Rust engine directly. json.dumps(model.model_dump()) first materialises a Python dict (one allocation per model), then calls CPython's JSON encoder, which will raise TypeError for non-JSON-serializable types like datetime. model_dump_json() is ~1.5–2x faster and handles special types correctly without manual default handlers.","correct":"model_dump_json() produces JSON bytes entirely in Rust — no Python dict is allocated, and types like datetime, UUID, and Decimal are serialized by the Rust engine directly.","distractors":["model_dump(mode=\"json\") returns a Python dict where all values have been converted to JSON-compatible types (e.g., datetime → ISO string, UUID → string).","Field(gt=0, lt=100) constraints are compiled into the Rust SchemaValidator — they execute entirely in Rust with no Python round-trip.","TypeAdapter wraps the CoreSchema compiler and Rust validator for any type expression — not just BaseModel subclasses."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q10-2808","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":10,"difficulty":"intermediate","question":"How does revalidate_instances affect performance and when is it necessary?","answerFull":"By default (revalidate_instances=\"never\"), if you pass a BaseModel instance where a BaseModel type is expected, Pydantic trusts it and skips re-validation. This is correct for internal data flow where you control the source. Set revalidate_instances=\"always\" when models travel through adapters that may bypass validation (ORM row constructors, model_construct calls, YAML loaders that assign attributes directly). The cost is one full Rust validation pass per nested model on every outer model creation — measure before enabling on hot paths.","correct":"By default (revalidate_instances=\"never\"), if you pass a BaseModel instance where a BaseModel type is expected, Pydantic trusts it and skips re-validation.","distractors":["Implement get_pydantic_core_schema as a classmethod on your type.","By default, Pydantic v2 does NOT run validators on fields that use their default value.","Pydantic provides a PYDANTIC_V1_COMPAT compatibility layer in v2 — many v1 patterns still work but emit deprecation warnings."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q11-2809","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":11,"difficulty":"advanced","question":"What happens at import time when you define a Pydantic model?","answerFull":"Python executes the class statement, which invokes ModelMetaclass.new. This method: (1) collects all annotations via annotations, (2) resolves forward references if possible, (3) constructs a CoreSchema dict describing the full type tree, (4) calls pydantic_core.SchemaValidator(schema) to compile the Rust validator, (5) calls pydantic_core.SchemaSerializer(schema) to compile the Rust serializer, and (6) stores both objects as pydantic_validator and pydantic_serializer on the class. Import time is O(model complexity). For a model with 50 fields and nested sub-models, this typically takes 2–8 ms.","correct":"Python executes the class statement, which invokes ModelMetaclass.new.","distractors":["Inherit from pydantic_settings.BaseSettings instead of BaseModel.","Pydantic provides a PYDANTIC_V1_COMPAT compatibility layer in v2 — many v1 patterns still work but emit deprecation warnings.","Use Annotated[T, Field(...)] to define a named type alias: PositiveInt = Annotated[int, Field(gt=0)]."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q12-2810","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":12,"difficulty":"advanced","question":"How does model_validator(mode=\"wrap\") interact with the CoreSchema validator?","answerFull":"mode=\"wrap\" gives the Python callable a handler argument, which is a callable that invokes the compiled Rust validator. The wrap validator runs in Python, calls handler(data) to run the full Rust validation pass, then can modify or replace the result. This means every validation call for the model pays one Python function call overhead plus the Rust validation cost. Use mode=\"wrap\" for cross-cutting concerns like audit logging or caching of validation results — not for constraints that can be expressed in CoreSchema directly.","correct":"mode=\"wrap\" gives the Python callable a handler argument, which is a callable that invokes the compiled Rust validator.","distractors":["pydantic-core is a Rust extension (compiled via PyO3) that implements Pydantic v2's validation and serialization engine.","TypeAdapter.init triggers CoreSchema construction and Rust SchemaValidator compilation.","Use @model_validator(mode=\"after\")."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q13-2811","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":13,"difficulty":"advanced","question":"Why does creating TypeAdapter inside a hot loop cause performance problems?","answerFull":"TypeAdapter.init triggers CoreSchema construction and Rust SchemaValidator compilation. This work takes 0.5–2 ms depending on type complexity. At 1000 calls/s, constructing TypeAdapter per call adds 500–2000 ms of overhead per second — enough to saturate a CPU core. The fix is to construct TypeAdapter once at module level and reuse it, since the compiled SchemaValidator is thread-safe and can be used concurrently without locking.","correct":"TypeAdapter.init triggers CoreSchema construction and Rust SchemaValidator compilation.","distractors":["Field(gt=0, lt=100) constraints are compiled into the Rust SchemaValidator — they execute entirely in Rust with no Python round-trip.","Two reasons: (1) model_validate_json uses pydantic-core's built-in Rust JSON parser (sonic-rs based), which is faster than CPython's json.loads for payloads over ~500 bytes.","CoreSchema is a Python TypedDict that describes a type tree — it is the intermediate representation between your Python type annotations and the Rust validator."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q14-2812","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":14,"difficulty":"advanced","question":"What is the memory overhead of a Pydantic v2 BaseModel instance versus a dataclass?","answerFull":"A BaseModel instance with 10 fields occupies approximately 400–600 bytes, compared to 280–320 bytes for a stdlib @dataclass and 220–260 bytes for @dataclass(slots=True). The extra overhead in BaseModel comes from pydantic_fields_set (a set tracking which fields were explicitly provided), dict (unless slots is used), and the internal pydantic_extra dict if model_config = ConfigDict(extra=\"allow\"). For high-throughput pipelines creating millions of short-lived instances, prefer model_construct() with pre-validated data or use TypeAdapter with raw dicts if you never need model methods.","correct":"A BaseModel instance with 10 fields occupies approximately 400–600 bytes, compared to 280–320 bytes for a stdlib @dataclass and 220–260 bytes for @dataclass(slots=True).","distractors":["Two reasons: (1) model_validate_json uses pydantic-core's built-in Rust JSON parser (sonic-rs based), which is faster than CPython's json.loads for payloads over ~500 bytes.","Implement get_pydantic_core_schema as a classmethod on your type.","Inherit from pydantic_settings.BaseSettings instead of BaseModel."]},{"id":"python/fastapi/pydantic_v2_deep_dive#Q15-2813","section":"python","module":"python/fastapi/pydantic_v2_deep_dive","moduleName":"pydantic v2 deep dive","qIndex":15,"difficulty":"advanced","question":"How does model_validate handle ORM objects (from_attributes=True)?","answerFull":"With from_attributes=True in ConfigDict, model_validate accepts objects that expose values as attributes (e.g., SQLAlchemy Row objects) instead of requiring a dict. The Rust validator calls getattr(obj, field_name) for each field — these are Python attribute access calls, one per field, crossing the FFI boundary. This is somewhat slower than dict-based validation because dict lookups are pure C operations while getattr may invoke descriptors, lazy loading, or getattr. For SQLAlchemy models, ensure all fields are eagerly loaded before passing to model_validate to avoid N+1 lazy-load penalties inside the Rust validator.","correct":"With from_attributes=True in ConfigDict, model_validate accepts objects that expose values as attributes (e.g., SQLAlchemy Row objects) instead of requiring a dict.","distractors":["When from_attributes=True is set in ConfigDict, model_validate(obj) reads field values from obj.field_name attributes (using getattr) instead of treating the input as a dict.","model_dump_json() produces JSON bytes entirely in Rust — no Python dict is allocated, and types like datetime, UUID, and Decimal are serialized by the Rust engine directly.","TypeAdapter validates arbitrary types without defining a model class: TypeAdapter(list[int]).validate_python([\"1\",\"2\"]) returns [1, 2]."]},{"id":"python/fastapi/caching_and_performance#Q1-2814","section":"python","module":"python/fastapi/caching_and_performance","moduleName":"caching and performance","qIndex":1,"difficulty":"core","question":"What is the thundering herd problem in caching, and how do you prevent it in Redis?","answerFull":"When a popular cache key expires, all concurrent requests simultaneously detect a miss and attempt to recompute the value, overwhelming the database. Prevent it with a Redis mutex: SET lock:<key> 1 NX EX 5 — the first caller acquires the lock atomically; others either return a stale value or wait briefly before re-checking. In practice, returning a slightly stale value (\"soft TTL\") is often better than making all callers wait, because it maintains availability without adding lock wait time.","correct":"When a popular cache key expires, all concurrent requests simultaneously detect a miss and attempt to recompute the value, overwhelming the database.","distractors":["XFetch extends a key's TTL probabilistically before it expires.","In the same DB transaction (or immediately after a successful write), issue await r.delete(key).","When max-age expires the browser sends a conditional GET with If-None-Match: \"<etag>\"."]},{"id":"python/fastapi/caching_and_performance#Q2-2815","section":"python","module":"python/fastapi/caching_and_performance","moduleName":"caching and performance","qIndex":2,"difficulty":"core","question":"Why can't you use functools.lru_cache on an async def function?","answerFull":"lru_cache stores whatever the decorated callable returns. An async def function returns a coroutine object, not the result. The first call caches the coroutine; the second call returns the same exhausted coroutine object, which raises RuntimeError: cannot reuse already awaited coroutine. Use cachetools.TTLCache with asyncio.Lock for async-safe in-process caching, or use aiocache/cashews which are async-native.","correct":"lru_cache stores whatever the decorated callable returns.","distractors":["When a popular cache key expires, all concurrent requests simultaneously detect a miss and attempt to recompute the value, overwhelming the database.","Write-behind writes to the cache immediately and flushes to the database asynchronously via a background job.","Each worker process has an independent memory space."]},{"id":"python/fastapi/caching_and_performance#Q3-2816","section":"python","module":"python/fastapi/caching_and_performance","moduleName":"caching and performance","qIndex":3,"difficulty":"core","question":"In a Gunicorn + Uvicorn deployment with 4 worker processes, why is in-process caching often insufficient for invalidation?","answerFull":"Each worker process has an independent memory space. When user data changes (e.g., profile update), you can invalidate the cache entry in the process that handled the write, but the other three processes still hold the stale entry until their TTL expires. For data that requires prompt invalidation, Redis is required because it is a single shared store visible to all workers. In-process caches are still useful as an L1 layer in front of Redis for high-frequency reads where per-process staleness of a few seconds is acceptable.","correct":"Each worker process has an independent memory space.","distractors":["The formula is: max_connections ≥ num_workers × max_simultaneous_redis_ops_per_worker.","When a popular cache key expires, all concurrent requests simultaneously detect a miss and attempt to recompute the value, overwhelming the database.","lru_cache stores whatever the decorated callable returns."]},{"id":"python/fastapi/caching_and_performance#Q4-2817","section":"python","module":"python/fastapi/caching_and_performance","moduleName":"caching and performance","qIndex":4,"difficulty":"core","question":"What is the difference between Cache-Control: max-age and s-maxage?","answerFull":"max-age sets the freshness lifetime for all caches, including the browser. s-maxage overrides max-age for shared caches only (CDNs, reverse proxies) while leaving the browser behavior unchanged. Use Cache-Control: max-age=300, s-maxage=60 when you want the browser to cache for 5 minutes but the CDN edge to refresh every 60 seconds — useful for content that CDN purge scripts control but browsers should cache longer.","correct":"max-age sets the freshness lifetime for all caches, including the browser.","distractors":["When max-age expires the browser sends a conditional GET with If-None-Match: \"<etag>\".","Write-behind writes to the cache immediately and flushes to the database asynchronously via a background job.","lru_cache stores whatever the decorated callable returns."]},{"id":"python/fastapi/caching_and_performance#Q5-2818","section":"python","module":"python/fastapi/caching_and_performance","moduleName":"caching and performance","qIndex":5,"difficulty":"core","question":"How does ETag reduce bandwidth even when max-age has expired?","answerFull":"When max-age expires the browser sends a conditional GET with If-None-Match: \"<etag>\". The server computes the current ETag; if it matches, it returns 304 Not Modified with no body. The browser uses its cached copy. This saves the response body transfer (potentially hundreds of KB) even though the cache is technically \"expired\". Round-trip still occurs, but bandwidth and server serialization cost are eliminated.","correct":"When max-age expires the browser sends a conditional GET with If-None-Match: \"<etag>\".","distractors":["max-age sets the freshness lifetime for all caches, including the browser.","XFetch extends a key's TTL probabilistically before it expires.","Each worker process has an independent memory space."]},{"id":"python/fastapi/caching_and_performance#Q6-2819","section":"python","module":"python/fastapi/caching_and_performance","moduleName":"caching and performance","qIndex":6,"difficulty":"intermediate","question":"How do you design a cache key that is both collision-free and invalidation-friendly?","answerFull":"Use a hierarchical namespace: {version}:{entity}:{id}:{sub-resource}. Example: v1:user:42:profile. The version prefix allows a \"schema invalidation\" by bumping v1 to v2 — new writes go to v2:*, old v1:* keys expire naturally. The entity and sub-resource segments allow targeted invalidation: delete v1:user:42:* to invalidate all cached data for user 42 using Redis SCAN + DEL, or use a tagging system like cashews.","correct":"Use a hierarchical namespace: {version}:{entity}:{id}:{sub-resource}.","distractors":["When a popular cache key expires, all concurrent requests simultaneously detect a miss and attempt to recompute the value, overwhelming the database.","lru_cache stores whatever the decorated callable returns.","In the same DB transaction (or immediately after a successful write), issue await r.delete(key)."]},{"id":"python/fastapi/caching_and_performance#Q7-2820","section":"python","module":"python/fastapi/caching_and_performance","moduleName":"caching and performance","qIndex":7,"difficulty":"intermediate","question":"What is the write-behind (write-back) cache pattern and when is it dangerous?","answerFull":"Write-behind writes to the cache immediately and flushes to the database asynchronously via a background job. Write latency to the client is minimized because the DB round trip is removed from the critical path. It is dangerous because if the cache crashes before the flush, writes since the last flush are lost. It is appropriate only for data where some loss is tolerable (event counters, analytics) or where the cache has AOF/RDB persistence with durability guarantees (Redis with appendfsync always).","correct":"Write-behind writes to the cache immediately and flushes to the database asynchronously via a background job.","distractors":["In the same DB transaction (or immediately after a successful write), issue await r.delete(key).","lru_cache stores whatever the decorated callable returns.","When a popular cache key expires, all concurrent requests simultaneously detect a miss and attempt to recompute the value, overwhelming the database."]},{"id":"python/fastapi/caching_and_performance#Q8-2821","section":"python","module":"python/fastapi/caching_and_performance","moduleName":"caching and performance","qIndex":8,"difficulty":"intermediate","question":"How does orjson improve FastAPI performance, and when does it not help?","answerFull":"orjson serializes Python objects to JSON 3-5x faster than stdlib json because it is implemented in Rust. It natively handles datetime, UUID, and dataclass without custom encoders. Set default_response_class=ORJSONResponse on the FastAPI constructor to apply globally. It does not help when the bottleneck is database I/O or network transfer rather than serialization — profile first with py-spy to confirm serialization is the actual hot path.","correct":"orjson serializes Python objects to JSON 3-5x faster than stdlib json because it is implemented in Rust.","distractors":["When a popular cache key expires, all concurrent requests simultaneously detect a miss and attempt to recompute the value, overwhelming the database.","lru_cache stores whatever the decorated callable returns.","Each worker process has an independent memory space."]},{"id":"python/fastapi/caching_and_performance#Q9-2822","section":"python","module":"python/fastapi/caching_and_performance","moduleName":"caching and performance","qIndex":9,"difficulty":"intermediate","question":"What is probabilistic early expiration (XFetch) and how does it differ from a mutex?","answerFull":"XFetch extends a key's TTL probabilistically before it expires. When a key has TTL remaining of delta, each fetch has a probability of recomputing equal to exp(-remaining / (delta × beta)). As expiry approaches, more requests volunteer to recompute, spreading the load. No explicit lock is needed; the key is never fully expired, so no stampede window exists. A mutex is simpler but creates a \"lock wait\" period; XFetch eliminates this at the cost of occasionally doing redundant recomputations.","correct":"XFetch extends a key's TTL probabilistically before it expires.","distractors":["When max-age expires the browser sends a conditional GET with If-None-Match: \"<etag>\".","In the same DB transaction (or immediately after a successful write), issue await r.delete(key).","When a popular cache key expires, all concurrent requests simultaneously detect a miss and attempt to recompute the value, overwhelming the database."]},{"id":"python/fastapi/caching_and_performance#Q10-2823","section":"python","module":"python/fastapi/caching_and_performance","moduleName":"caching and performance","qIndex":10,"difficulty":"intermediate","question":"How do you size a Redis connection pool for a FastAPI application?","answerFull":"The formula is: max_connections ≥ num_workers × max_simultaneous_redis_ops_per_worker. For 4 Uvicorn workers each handling 200 concurrent requests, where 10% of requests touch Redis: 4 × 200 × 0.1 = 80 minimum connections. Add 20% headroom: pool of 100. Monitor redis_connected_clients in Prometheus; if it regularly exceeds 80% of max_connections, scale the pool or add Redis replicas for read traffic. Setting max_connections too low causes redis.exceptions.ConnectionError: too many connections.","correct":"The formula is: max_connections ≥ num_workers × max_simultaneous_redis_ops_per_worker.","distractors":["Each worker process has an independent memory space.","max-age sets the freshness lifetime for all caches, including the browser.","When max-age expires the browser sends a conditional GET with If-None-Match: \"<etag>\"."]},{"id":"python/fastapi/caching_and_performance#Q12-2824","section":"python","module":"python/fastapi/caching_and_performance","moduleName":"caching and performance","qIndex":12,"difficulty":"advanced","question":"How do you implement cache invalidation on write without a dedicated invalidation service?","answerFull":"In the same DB transaction (or immediately after a successful write), issue await r.delete(key). This removes the stale cache entry; the next read will miss and repopulate. For complex invalidation (e.g., \"invalidate all keys for user 42\"), use a Redis key tag scheme: prefix all related keys with user:42:* and run SCAN with MATCH user:42:* + DEL. Alternatively, bump the version component in the key (v1: → v2:) at deploy time to mass-invalidate without SCAN. Note: SCAN is O(N) where N is the total keyspace; on large Redis instances this can be slow — prefer key-tagging libraries (cashews, custom sets of tagged keys) for production-grade selective invalidation.","correct":"In the same DB transaction (or immediately after a successful write), issue await r.delete(key).","distractors":["Write-behind writes to the cache immediately and flushes to the database asynchronously via a background job.","When a popular cache key expires, all concurrent requests simultaneously detect a miss and attempt to recompute the value, overwhelming the database.","lru_cache stores whatever the decorated callable returns."]},{"id":"python/fastapi/middleware_and_lifecycle#Q1-2825","section":"python","module":"python/fastapi/middleware_and_lifecycle","moduleName":"middleware and lifecycle","qIndex":1,"difficulty":"core","question":"What is the execution order of middleware in FastAPI when you call add_middleware three times?","answerFull":"The last middleware added is the outermost layer and executes first on the request. If you call add_middleware(A), add_middleware(B), add_middleware(C), the effective wrap is C(B(A(app))). On a request: C runs first, then B, then A, then the router. On a response: A processes first, then B, then C. The mnemonic is a stack — last in, first out on the request side. Always trace the add_middleware call order carefully when debugging middleware interactions.","correct":"The last middleware added is the outermost layer and executes first on the request.","distractors":["Request.state is a starlette.datastructures.State instance — a simple object that accepts arbitrary attribute assignments (request.state.foo = \"bar\").","Check scope[\"type\"] at the start of call.","allow_credentials=True causes CORSMiddleware to add Access-Control-Allow-Credentials: true to responses, which instructs browsers to include cookies and Authorization headers in cross-origin requests."]},{"id":"python/fastapi/middleware_and_lifecycle#Q2-2826","section":"python","module":"python/fastapi/middleware_and_lifecycle","moduleName":"middleware and lifecycle","qIndex":2,"difficulty":"core","question":"Why does BaseHTTPMiddleware break streaming responses, and what is the fix?","answerFull":"BaseHTTPMiddleware.dispatch() calls call_next(request) which returns only after all response chunks have been collected — the framework buffers the entire body before returning a Response object. For StreamingResponse or SSE, this means the client waits until the generator is exhausted, defeating the purpose of streaming. The fix is to write a pure ASGI middleware that intercepts the send callable directly — it fires once per chunk and passes each chunk downstream immediately without buffering.","correct":"BaseHTTPMiddleware.dispatch() calls call_next(request) which returns only after all response chunks have been collected — the framework buffers the entire body before returning a Response object.","distractors":["allow_credentials=True causes CORSMiddleware to add Access-Control-Allow-Credentials: true to responses, which instructs browsers to include cookies and Authorization headers in cross-origin requests.","Check scope[\"type\"] at the start of call.","Attach data to request.state in middleware (e.g., request.state.user_id = decoded_jwt[\"sub\"])."]},{"id":"python/fastapi/middleware_and_lifecycle#Q3-2827","section":"python","module":"python/fastapi/middleware_and_lifecycle","moduleName":"middleware and lifecycle","qIndex":3,"difficulty":"core","question":"How do you pass data from middleware to a route handler?","answerFull":"Attach data to request.state in middleware (e.g., request.state.user_id = decoded_jwt[\"sub\"]). The route handler reads it via request: Request parameter as request.state.user_id. Starlette's State is a simple attribute bag scoped to the request lifetime. Avoid thread-locals or module-level globals — they break under async concurrency and multiple worker processes.","correct":"Attach data to request.state in middleware (e.g., request.state.user_id = decoded_jwt[\"sub\"]).","distractors":["BaseHTTPMiddleware.dispatch() calls call_next(request) which returns only after all response chunks have been collected — the framework buffers the entire body before returning a Response object.","Request.state is a starlette.datastructures.State instance — a simple object that accepts arbitrary attribute assignments (request.state.foo = \"bar\").","@app.exception_handler hooks into Starlette's exception handling chain, which runs inside the routing layer after middleware."]},{"id":"python/fastapi/middleware_and_lifecycle#Q4-2828","section":"python","module":"python/fastapi/middleware_and_lifecycle","moduleName":"middleware and lifecycle","qIndex":4,"difficulty":"core","question":"What is the difference between @app.exception_handler and a try/except in middleware?","answerFull":"@app.exception_handler hooks into Starlette's exception handling chain, which runs inside the routing layer after middleware. It only catches exceptions raised by route handlers and inner exception handlers. Middleware wraps the entire routing layer, so a try/except in middleware can catch exceptions that escape all @app.exception_handler registrations — including ones raised by other middleware layers below it. For a global last-resort 500 handler, use middleware; for HTTP semantics tied to specific routes, use @app.exception_handler.","correct":"@app.exception_handler hooks into Starlette's exception handling chain, which runs inside the routing layer after middleware.","distractors":["Use the lifespan context manager: create the pool before yield and assign it to app.state.db.","BackgroundTasks runs the task in the same Uvicorn worker process after the response is sent, blocking that worker for the duration of the background work.","Use Starlette's TestClient or httpx.AsyncClient as an async context manager: async with AsyncClient(app=app, base_url=\"http://test\") as client: — entering the context triggers lifespan startup; exiting triggers shutdown."]},{"id":"python/fastapi/middleware_and_lifecycle#Q6-2829","section":"python","module":"python/fastapi/middleware_and_lifecycle","moduleName":"middleware and lifecycle","qIndex":6,"difficulty":"intermediate","question":"How do you initialize a database connection pool at startup and make it available to all route handlers?","answerFull":"Use the lifespan context manager: create the pool before yield and assign it to app.state.db. Route handlers access it via request.app.state.db. Alternatively, define a dependency async def get_db(request: Request) that returns request.app.state.db — this makes the pool injectable through the standard Depends() mechanism, which is easier to mock in tests via dependency_overrides.","correct":"Use the lifespan context manager: create the pool before yield and assign it to app.state.db.","distractors":["Attach data to request.state in middleware (e.g., request.state.user_id = decoded_jwt[\"sub\"]).","@app.exception_handler hooks into Starlette's exception handling chain, which runs inside the routing layer after middleware.","Use Starlette's TestClient or httpx.AsyncClient as an async context manager: async with AsyncClient(app=app, base_url=\"http://test\") as client: — entering the context triggers lifespan startup; exiting triggers shutdown."]},{"id":"python/fastapi/middleware_and_lifecycle#Q7-2830","section":"python","module":"python/fastapi/middleware_and_lifecycle","moduleName":"middleware and lifecycle","qIndex":7,"difficulty":"intermediate","question":"What does allow_credentials=True in CORSMiddleware do, and why can it not be combined with allow_origins=[\"*\"]?","answerFull":"allow_credentials=True causes CORSMiddleware to add Access-Control-Allow-Credentials: true to responses, which instructs browsers to include cookies and Authorization headers in cross-origin requests. The CORS specification forbids the wildcard \"*\" value for Access-Control-Allow-Origin when credentials are enabled — the browser requires a specific origin to be reflected. Combining both is a browser-enforced security restriction to prevent credential theft by malicious origins. Always specify an explicit origin list when using allow_credentials=True.","correct":"allow_credentials=True causes CORSMiddleware to add Access-Control-Allow-Credentials: true to responses, which instructs browsers to include cookies and Authorization headers in cross-origin requests.","distractors":["The last middleware added is the outermost layer and executes first on the request.","BaseHTTPMiddleware.dispatch() calls call_next(request) which returns only after all response chunks have been collected — the framework buffers the entire body before returning a Response object.","Attach data to request.state in middleware (e.g., request.state.user_id = decoded_jwt[\"sub\"])."]},{"id":"python/fastapi/middleware_and_lifecycle#Q8-2831","section":"python","module":"python/fastapi/middleware_and_lifecycle","moduleName":"middleware and lifecycle","qIndex":8,"difficulty":"intermediate","question":"What is the overhead of BaseHTTPMiddleware compared to pure ASGI middleware?","answerFull":"BaseHTTPMiddleware wraps the receive and send callables twice — once internally to collect the response body and once to present a Response object to dispatch(). This adds approximately 20-30 µs of overhead per request on fast paths. For a service handling 10,000 req/s, that is 200-300 ms of cumulative CPU overhead per second. Pure ASGI middleware adds roughly 1-2 µs. The difference is rarely the bottleneck (database queries dominate at 1-50 ms), but matters for high-frequency internal services where P99 latency targets are tight.","correct":"BaseHTTPMiddleware wraps the receive and send callables twice — once internally to collect the response body and once to present a Response object to dispatch().","distractors":["Attach data to request.state in middleware (e.g., request.state.user_id = decoded_jwt[\"sub\"]).","The last middleware added is the outermost layer and executes first on the request.","BaseHTTPMiddleware.dispatch() calls call_next(request) which returns only after all response chunks have been collected — the framework buffers the entire body before returning a Response object."]},{"id":"python/fastapi/middleware_and_lifecycle#Q9-2832","section":"python","module":"python/fastapi/middleware_and_lifecycle","moduleName":"middleware and lifecycle","qIndex":9,"difficulty":"intermediate","question":"How do you write middleware that correctly handles both HTTP and WebSocket connections?","answerFull":"Check scope[\"type\"] at the start of call. For \"http\" connections, apply HTTP-specific logic. For \"websocket\" connections, either pass through unchanged (await self.app(scope, receive, send)) or apply WebSocket-specific logic. Always handle the \"lifespan\" type as a pass-through — failing to do so prevents the lifespan startup/shutdown protocol from running. BaseHTTPMiddleware handles this automatically; pure ASGI middleware requires explicit type checking.","correct":"Check scope[\"type\"] at the start of call.","distractors":["The last middleware added is the outermost layer and executes first on the request.","Use Starlette's TestClient or httpx.AsyncClient as an async context manager: async with AsyncClient(app=app, base_url=\"http://test\") as client: — entering the context triggers lifespan startup; exiting triggers shutdown.","Attach data to request.state in middleware (e.g., request.state.user_id = decoded_jwt[\"sub\"])."]},{"id":"python/fastapi/middleware_and_lifecycle#Q10-2833","section":"python","module":"python/fastapi/middleware_and_lifecycle","moduleName":"middleware and lifecycle","qIndex":10,"difficulty":"intermediate","question":"How do you test a FastAPI application that uses lifespan for startup?","answerFull":"Use Starlette's TestClient or httpx.AsyncClient as an async context manager: async with AsyncClient(app=app, base_url=\"http://test\") as client: — entering the context triggers lifespan startup; exiting triggers shutdown. For pytest-asyncio, use the @pytest.fixture(scope=\"module\") pattern with async with AsyncClient(...) to share a single initialized app across all tests in a module, avoiding repeated startup/shutdown overhead.","correct":"Use Starlette's TestClient or httpx.AsyncClient as an async context manager: async with AsyncClient(app=app, base_url=\"http://test\") as client: — entering the context triggers lifespan startup; exiting triggers shutdown.","distractors":["Request.state is a starlette.datastructures.State instance — a simple object that accepts arbitrary attribute assignments (request.state.foo = \"bar\").","@app.exception_handler hooks into Starlette's exception handling chain, which runs inside the routing layer after middleware.","Use the lifespan context manager: create the pool before yield and assign it to app.state.db."]},{"id":"python/fastapi/middleware_and_lifecycle#Q11-2834","section":"python","module":"python/fastapi/middleware_and_lifecycle","moduleName":"middleware and lifecycle","qIndex":11,"difficulty":"advanced","question":"What is Request.state and what is its scope?","answerFull":"Request.state is a starlette.datastructures.State instance — a simple object that accepts arbitrary attribute assignments (request.state.foo = \"bar\"). It is created fresh for each request and destroyed when the request completes. It is not shared between requests, not thread-safe (because each async request has its own coroutine stack), and not accessible outside the request lifecycle. Use it to pass middleware-set values (correlation IDs, authenticated user objects) to route handlers without function-signature coupling.","correct":"Request.state is a starlette.datastructures.State instance — a simple object that accepts arbitrary attribute assignments (request.state.foo = \"bar\").","distractors":["BaseHTTPMiddleware.dispatch() calls call_next(request) which returns only after all response chunks have been collected — the framework buffers the entire body before returning a Response object.","BaseHTTPMiddleware wraps the receive and send callables twice — once internally to collect the response body and once to present a Response object to dispatch().","Attach data to request.state in middleware (e.g., request.state.user_id = decoded_jwt[\"sub\"])."]},{"id":"python/fastapi/middleware_and_lifecycle#Q12-2835","section":"python","module":"python/fastapi/middleware_and_lifecycle","moduleName":"middleware and lifecycle","qIndex":12,"difficulty":"advanced","question":"When should you use BackgroundTasks in a route handler vs a dedicated task queue?","answerFull":"BackgroundTasks runs the task in the same Uvicorn worker process after the response is sent, blocking that worker for the duration of the background work. It is appropriate for fast, non-critical tasks: sending a confirmation email (50-200 ms), writing an audit log entry, or invalidating a cache key. Use a dedicated task queue (Celery, ARQ, Dramatiq) when: the task takes longer than 1-2 seconds, failure must be retried with exponential backoff, tasks must survive process restarts, or task volume exceeds what a single worker can handle. BackgroundTasks in middleware is an anti-pattern — middleware's call_next must complete before the response is sent, so tasks registered there block the response.","correct":"BackgroundTasks runs the task in the same Uvicorn worker process after the response is sent, blocking that worker for the duration of the background work.","distractors":["BaseHTTPMiddleware.dispatch() calls call_next(request) which returns only after all response chunks have been collected — the framework buffers the entire body before returning a Response object.","@app.exception_handler hooks into Starlette's exception handling chain, which runs inside the routing layer after middleware.","BaseHTTPMiddleware wraps the receive and send callables twice — once internally to collect the response body and once to present a Response object to dispatch()."]},{"id":"python/fastapi/async_database_sqlalchemy#Q1-2836","section":"python","module":"python/fastapi/async_database_sqlalchemy","moduleName":"async database sqlalchemy","qIndex":1,"difficulty":"core","question":"Why does accessing a lazy relationship in async SQLAlchemy raise MissingGreenlet instead of just blocking?","answerFull":"SQLAlchemy's lazy loader is implemented using greenlets — lightweight coroutines provided by the greenlet library — to issue synchronous SQL from within async execution contexts. When there is no active greenlet context (which is the case in native async code that runs on Python's asyncio event loop), the lazy loader cannot execute and raises MissingGreenlet. The root cause is that implicit I/O requires a synchronous execution context, which does not exist in a pure asyncio coroutine. Fix: always declare relationship loading strategies (selectinload, joinedload) in the query before execution.","correct":"SQLAlchemy's lazy loader is implemented using greenlets — lightweight coroutines provided by the greenlet library — to issue synchronous SQL from within async execution contexts.","distractors":["Alembic's migration runner is synchronous.","After session.commit(), SQLAlchemy by default marks all loaded ORM attributes as \"expired\".","pool_pre_ping=True causes SQLAlchemy to issue a lightweight SELECT 1 query each time a connection is checked out from the pool."]},{"id":"python/fastapi/async_database_sqlalchemy#Q2-2837","section":"python","module":"python/fastapi/async_database_sqlalchemy","moduleName":"async database sqlalchemy","qIndex":2,"difficulty":"core","question":"What is the difference between selectinload and joinedload, and when do you choose each?","answerFull":"selectinload issues a second SELECT ... WHERE id IN (...) query after loading the parent objects. joinedload issues a single LEFT OUTER JOIN query. Use joinedload for many-to-one (or one-to-one) relationships where there is at most one related object per parent row — a join does not multiply rows. Use selectinload for one-to-many and many-to-many relationships where a join would produce a cartesian product, inflating result set size and wasting bandwidth. Practical rule: if loading 100 users each with 50 posts, a join returns 5,000 rows; selectinload returns 100 + 100 = 200 rows in two queries.","correct":"selectinload issues a second SELECT ...","distractors":["pool_recycle=N closes and recreates connections that have been open for more than N seconds, preventing issues with MySQL's wait_timeout (default 8 hours) or PostgreSQL idle connection limits.","pool_pre_ping=True causes SQLAlchemy to issue a lightweight SELECT 1 query each time a connection is checked out from the pool.","SQLAlchemy's lazy loader is implemented using greenlets — lightweight coroutines provided by the greenlet library — to issue synchronous SQL from within async execution contexts."]},{"id":"python/fastapi/async_database_sqlalchemy#Q3-2838","section":"python","module":"python/fastapi/async_database_sqlalchemy","moduleName":"async database sqlalchemy","qIndex":3,"difficulty":"core","question":"Why must expire_on_commit=False be set for async sessions?","answerFull":"After session.commit(), SQLAlchemy by default marks all loaded ORM attributes as \"expired\". In synchronous code, accessing an expired attribute transparently issues a new SELECT to refresh the value. In async code, this implicit reload has no await and raises MissingGreenlet. With expire_on_commit=False, attributes retain their in-memory values post-commit and no implicit reload is attempted. The tradeoff is that if another process modifies the same row after your commit, you will read stale data — acceptable for request-response cycles where the session closes immediately after the commit.","correct":"After session.commit(), SQLAlchemy by default marks all loaded ORM attributes as \"expired\".","distractors":["Celery tasks are synchronous by default and run in a thread pool, not an asyncio event loop.","Set echo=True on the engine during development: all SQL statements are logged to stdout.","SQLAlchemy raises sqlalchemy.exc.OperationalError (subclass of DBAPIError) for connection-level failures."]},{"id":"python/fastapi/async_database_sqlalchemy#Q4-2839","section":"python","module":"python/fastapi/async_database_sqlalchemy","moduleName":"async database sqlalchemy","qIndex":4,"difficulty":"core","question":"How do you configure connection pool size for an async SQLAlchemy engine in a multi-worker FastAPI deployment?","answerFull":"The formula is: total connections = (pool_size + max_overflow) × num_workers. With pool_size=5, max_overflow=10, and 4 workers, the maximum is 60 connections. PostgreSQL's default max_connections is 100; subtract 10 for admin and monitoring, leaving 90 usable connections for the application. For 4 workers: (pool_size + max_overflow) × 4 ≤ 90 → pool_size + max_overflow ≤ 22 → pool_size=5, max_overflow=10 is safe with two workers of headroom. For high-concurrency deployments, add PgBouncer in transaction mode to multiplex more application connections onto fewer server connections.","correct":"The formula is: total connections = (pool_size + max_overflow) × num_workers.","distractors":["pool_recycle=N closes and recreates connections that have been open for more than N seconds, preventing issues with MySQL's wait_timeout (default 8 hours) or PostgreSQL idle connection limits.","SQLAlchemy's lazy loader is implemented using greenlets — lightweight coroutines provided by the greenlet library — to issue synchronous SQL from within async execution contexts.","Set echo=True on the engine during development: all SQL statements are logged to stdout."]},{"id":"python/fastapi/async_database_sqlalchemy#Q5-2840","section":"python","module":"python/fastapi/async_database_sqlalchemy","moduleName":"async database sqlalchemy","qIndex":5,"difficulty":"core","question":"What does pool_pre_ping=True do and when should you enable it?","answerFull":"pool_pre_ping=True causes SQLAlchemy to issue a lightweight SELECT 1 query each time a connection is checked out from the pool. If the query fails (because the server restarted, the TCP connection was silently dropped, or a firewall timeout closed the connection), the driver discards that connection and checks out a fresh one. It prevents OperationalError: server closed the connection unexpectedly errors that occur when stale connections are used. Enable it for any production deployment where the database or network may close idle connections. The cost is one extra round-trip per checkout, typically under 1 ms on a local network.","correct":"pool_pre_ping=True causes SQLAlchemy to issue a lightweight SELECT 1 query each time a connection is checked out from the pool.","distractors":["pool_recycle=N closes and recreates connections that have been open for more than N seconds, preventing issues with MySQL's wait_timeout (default 8 hours) or PostgreSQL idle connection limits.","selectinload issues a second SELECT ...","Celery tasks are synchronous by default and run in a thread pool, not an asyncio event loop."]},{"id":"python/fastapi/async_database_sqlalchemy#Q6-2841","section":"python","module":"python/fastapi/async_database_sqlalchemy","moduleName":"async database sqlalchemy","qIndex":6,"difficulty":"intermediate","question":"How does pool_recycle differ from pool_pre_ping?","answerFull":"pool_recycle=N closes and recreates connections that have been open for more than N seconds, preventing issues with MySQL's wait_timeout (default 8 hours) or PostgreSQL idle connection limits. pool_pre_ping=True validates connections at checkout time regardless of age. They solve different problems: pool_recycle handles maximum connection lifetime; pool_pre_ping handles connections that died unexpectedly between uses. Use both together in production: pool_recycle=1800 (30 minutes) with pool_pre_ping=True.","correct":"pool_recycle=N closes and recreates connections that have been open for more than N seconds, preventing issues with MySQL's wait_timeout (default 8 hours) or PostgreSQL idle connection limits.","distractors":["The default isolation level is determined by the database driver and server configuration.","After session.commit(), SQLAlchemy by default marks all loaded ORM attributes as \"expired\".","selectinload issues a second SELECT ..."]},{"id":"python/fastapi/async_database_sqlalchemy#Q7-2842","section":"python","module":"python/fastapi/async_database_sqlalchemy","moduleName":"async database sqlalchemy","qIndex":7,"difficulty":"intermediate","question":"How do you perform Alembic migrations with an async engine?","answerFull":"Alembic's migration runner is synchronous. The bridge is connection.run_sync(do_run_migrations), which executes the migration function synchronously within an async database connection. In alembic/env.py, replace engine_from_config with async_engine_from_config, open an async connection with async with connectable.connect() as connection, and call await connection.run_sync(do_run_migrations). Use asyncio.run(run_migrations_online()) at the bottom of env.py to drive the async function from Alembic's synchronous context.","correct":"Alembic's migration runner is synchronous.","distractors":["SQLAlchemy's lazy loader is implemented using greenlets — lightweight coroutines provided by the greenlet library — to issue synchronous SQL from within async execution contexts.","Set echo=True on the engine during development: all SQL statements are logged to stdout.","Celery tasks are synchronous by default and run in a thread pool, not an asyncio event loop."]},{"id":"python/fastapi/async_database_sqlalchemy#Q8-2843","section":"python","module":"python/fastapi/async_database_sqlalchemy","moduleName":"async database sqlalchemy","qIndex":8,"difficulty":"intermediate","question":"What is the difference between session.flush() and session.commit()?","answerFull":"flush() sends pending SQL statements (INSERTs, UPDATEs, DELETEs) to the database within the current transaction, making changes visible to subsequent queries in the same session. The transaction is not committed; changes are invisible to other connections. commit() finalizes the transaction, making changes visible to all connections. Use flush() to obtain a database-assigned primary key (e.g., user.id) before committing, or to enforce constraints before a larger operation. In async code, both are awaited: await session.flush(), await session.commit().","correct":"flush() sends pending SQL statements (INSERTs, UPDATEs, DELETEs) to the database within the current transaction, making changes visible to subsequent queries in the same session.","distractors":["The default isolation level is determined by the database driver and server configuration.","SQLAlchemy's lazy loader is implemented using greenlets — lightweight coroutines provided by the greenlet library — to issue synchronous SQL from within async execution contexts.","In SQLAlchemy 2.0, AsyncSession uses \"autobegin\" behavior: the first DML or execute() call automatically begins a transaction."]},{"id":"python/fastapi/async_database_sqlalchemy#Q10-2844","section":"python","module":"python/fastapi/async_database_sqlalchemy","moduleName":"async database sqlalchemy","qIndex":10,"difficulty":"intermediate","question":"How do you detect N+1 queries in a SQLAlchemy application?","answerFull":"Set echo=True on the engine during development: all SQL statements are logged to stdout. Count how many SELECT statements are emitted per request — if a request that loads 50 users emits 51 queries, you have N+1. For production profiling, use sqlalchemy-utils or middleware that wraps the session and counts queries. The pytest-sqlalchemy-mock library can assert query counts in tests: assert query_counter.count == 2. For continuous monitoring, emit a custom metric from a SQLAlchemy event listener on after_execute.","correct":"Set echo=True on the engine during development: all SQL statements are logged to stdout.","distractors":["In SQLAlchemy 2.0, AsyncSession uses \"autobegin\" behavior: the first DML or execute() call automatically begins a transaction.","SQLAlchemy's lazy loader is implemented using greenlets — lightweight coroutines provided by the greenlet library — to issue synchronous SQL from within async execution contexts.","flush() sends pending SQL statements (INSERTs, UPDATEs, DELETEs) to the database within the current transaction, making changes visible to subsequent queries in the same session."]},{"id":"python/fastapi/async_database_sqlalchemy#Q11-2845","section":"python","module":"python/fastapi/async_database_sqlalchemy","moduleName":"async database sqlalchemy","qIndex":11,"difficulty":"advanced","question":"Can you use AsyncSession inside a Celery task?","answerFull":"Celery tasks are synchronous by default and run in a thread pool, not an asyncio event loop. Running async code in a Celery task requires asyncio.run(my_async_function()) inside the task, which creates a new event loop per task call. This works but is inefficient. Better alternatives: (1) use sync SQLAlchemy in Celery tasks, (2) use celery[gevent] or celery[eventlet] for async workers, or (3) migrate background tasks to ARQ or Dramatiq which have native async support.","correct":"Celery tasks are synchronous by default and run in a thread pool, not an asyncio event loop.","distractors":["The default isolation level is determined by the database driver and server configuration.","Alembic's migration runner is synchronous.","The formula is: total connections = (pool_size + max_overflow) × num_workers."]},{"id":"python/fastapi/async_database_sqlalchemy#Q12-2846","section":"python","module":"python/fastapi/async_database_sqlalchemy","moduleName":"async database sqlalchemy","qIndex":12,"difficulty":"advanced","question":"What is the transaction isolation level for AsyncSession by default, and how do you change it?","answerFull":"The default isolation level is determined by the database driver and server configuration. For PostgreSQL via asyncpg, the default is READ COMMITTED. To change isolation for a specific transaction: python async with engine.connect() as conn: await conn.execution_options(isolation_level=\"REPEATABLE READ\") # or \"SERIALIZABLE\", \"READ UNCOMMITTED\" async with AsyncSession(bind=conn) as session: ... For the entire engine: create_async_engine(url, isolation_level=\"REPEATABLE READ\").","correct":"The default isolation level is determined by the database driver and server configuration.","distractors":["pool_recycle=N closes and recreates connections that have been open for more than N seconds, preventing issues with MySQL's wait_timeout (default 8 hours) or PostgreSQL idle connection limits.","In SQLAlchemy 2.0, AsyncSession uses \"autobegin\" behavior: the first DML or execute() call automatically begins a transaction.","SQLAlchemy raises sqlalchemy.exc.OperationalError (subclass of DBAPIError) for connection-level failures."]},{"id":"python/fastapi/async_database_sqlalchemy#Q14-2847","section":"python","module":"python/fastapi/async_database_sqlalchemy","moduleName":"async database sqlalchemy","qIndex":14,"difficulty":"advanced","question":"What happens when you call session.execute() without an active transaction in SQLAlchemy 2.0?","answerFull":"In SQLAlchemy 2.0, AsyncSession uses \"autobegin\" behavior: the first DML or execute() call automatically begins a transaction. There is no need to explicitly call session.begin() before the first query. However, you must explicitly call await session.commit() or await session.rollback() when done — there is no autocommit. If you discard the session without committing, the transaction is rolled back when the session is closed.","correct":"In SQLAlchemy 2.0, AsyncSession uses \"autobegin\" behavior: the first DML or execute() call automatically begins a transaction.","distractors":["lazy=\"raise\" configures a relationship so that accessing it without eager loading raises InvalidRequestError immediately with a clear message like \"Mapped attribute 'User.posts' is not available due to lazy='raise'\".","After session.commit(), SQLAlchemy by default marks all loaded ORM attributes as \"expired\".","SQLAlchemy raises sqlalchemy.exc.OperationalError (subclass of DBAPIError) for connection-level failures."]},{"id":"python/fastapi/async_database_sqlalchemy#Q15-2848","section":"python","module":"python/fastapi/async_database_sqlalchemy","moduleName":"async database sqlalchemy","qIndex":15,"difficulty":"advanced","question":"How do you handle database connection errors and retry logic with async SQLAlchemy?","answerFull":"SQLAlchemy raises sqlalchemy.exc.OperationalError (subclass of DBAPIError) for connection-level failures. Implement retry at the service layer using tenacity: python from tenacity import retry, stop_after_attempt, wait_exponential, retry_if_exception_type from sqlalchemy.exc import OperationalError @retry( retry=retry_if_exception_type(OperationalError), stop=stop_after_attempt(3), wait=wait_exponential(multiplier=0.1, min=0.1, max=2), ) async def resilient_query(session: AsyncSession, user_id: int) -> User | None: result = await session.execute(select(User).where(User.id == user_id)) return result.scalar_one_or_none() Note: do not retry within a failed transaction — roll back first, then retry. The yield dependency pattern handles this by rolling back on exception before the session is closed.","correct":"SQLAlchemy raises sqlalchemy.exc.OperationalError (subclass of DBAPIError) for connection-level failures.","distractors":["In SQLAlchemy 2.0, AsyncSession uses \"autobegin\" behavior: the first DML or execute() call automatically begins a transaction.","lazy=\"raise\" configures a relationship so that accessing it without eager loading raises InvalidRequestError immediately with a clear message like \"Mapped attribute 'User.posts' is not available due to lazy='raise'\".","The default isolation level is determined by the database driver and server configuration."]},{"id":"python/fastapi/async_database_sqlalchemy#Q16-2849","section":"python","module":"python/fastapi/async_database_sqlalchemy","moduleName":"async database sqlalchemy","qIndex":16,"difficulty":"advanced","question":"What is the lazy=\"raise\" relationship strategy and when should you use it?","answerFull":"lazy=\"raise\" configures a relationship so that accessing it without eager loading raises InvalidRequestError immediately with a clear message like \"Mapped attribute 'User.posts' is not available due to lazy='raise'\". This is used as a defensive default in large teams: setting lazy=\"raise\" on all relationships forces developers to explicitly list every relationship they need in query options, preventing accidental N+1 queries from being silently introduced. Use it in production code where strict access patterns are required; revert to lazy=\"select\" in data exploration or admin tools where flexibility is more important.","correct":"lazy=\"raise\" configures a relationship so that accessing it without eager loading raises InvalidRequestError immediately with a clear message like \"Mapped attribute 'User.posts' is not available due to lazy='raise'\".","distractors":["SQLAlchemy raises sqlalchemy.exc.OperationalError (subclass of DBAPIError) for connection-level failures.","SQLAlchemy's lazy loader is implemented using greenlets — lightweight coroutines provided by the greenlet library — to issue synchronous SQL from within async execution contexts.","selectinload issues a second SELECT ..."]},{"id":"python/fastapi/security_hardening_and_owasp#Q1-2850","section":"python","module":"python/fastapi/security_hardening_and_owasp","moduleName":"security hardening and owasp","qIndex":1,"difficulty":"core","question":"What is BOLA and why is it the most common API vulnerability?","answerFull":"BOLA (Broken Object Level Authorization) occurs when an API allows a user to access any resource by ID without verifying ownership. It is the most common vulnerability because authentication (checking identity) is often correctly implemented while authorization (checking ownership of the specific object) is forgotten at the route level. Middleware cannot perform this check because it runs before the object is fetched. Every route that fetches a resource by user-controlled ID must explicitly verify resource.owner_id == current_user.id.","correct":"BOLA (Broken Object Level Authorization) occurs when an API allows a user to access any resource by ID without verifying ownership.","distractors":["Returning 404 leaks the existence of the resource: an attacker who receives a different error code for \"exists but not mine\" versus \"does not exist\" can enumerate all resource IDs in the system.","Improper inventory management means old, forgotten, or shadow API endpoints remain accessible after newer versions ship.","BOPLA (Broken Object Property Level Authorization) occurs when a user can read or write fields they should not access — for example, reading another user's stripe_customer_id or writing to is_admin."]},{"id":"python/fastapi/security_hardening_and_owasp#Q2-2851","section":"python","module":"python/fastapi/security_hardening_and_owasp","moduleName":"security hardening and owasp","qIndex":2,"difficulty":"core","question":"Why should you return HTTP 403 (not 404) when an authenticated user requests a resource they don't own?","answerFull":"Returning 404 leaks the existence of the resource: an attacker who receives a different error code for \"exists but not mine\" versus \"does not exist\" can enumerate all resource IDs in the system. The correct pattern is: return 404 when the object does not exist for any caller; return 403 when it exists but the caller is unauthorized. This is the principle of not leaking more information than necessary.","correct":"Returning 404 leaks the existence of the resource: an attacker who receives a different error code for \"exists but not mine\" versus \"does not exist\" can enumerate all resource IDs in the system.","distractors":["BOPLA (Broken Object Property Level Authorization) occurs when a user can read or write fields they should not access — for example, reading another user's stripe_customer_id or writing to is_admin.","SQLAlchemy ORM queries (.where(User.name == value)) are always parameterized — the value is never interpolated into the SQL string.","SecretStr stores the value internally but returns \"\" from repr, str, and model_dump()."]},{"id":"python/fastapi/security_hardening_and_owasp#Q3-2852","section":"python","module":"python/fastapi/security_hardening_and_owasp","moduleName":"security hardening and owasp","qIndex":3,"difficulty":"core","question":"What is BOPLA and how does response_model in FastAPI prevent it?","answerFull":"BOPLA (Broken Object Property Level Authorization) occurs when a user can read or write fields they should not access — for example, reading another user's stripe_customer_id or writing to is_admin. Declaring a dedicated ResponseSchema (for GET) and UpdateSchema (for PATCH/PUT) with only the permitted fields, then using response_model=ResponseSchema in the route decorator, ensures Pydantic strips unauthorized fields at serialization time. The ORM model may contain 30 fields; the response model exposes 8.","correct":"BOPLA (Broken Object Property Level Authorization) occurs when a user can read or write fields they should not access — for example, reading another user's stripe_customer_id or writing to is_admin.","distractors":["Pass docs_url=None, redoc_url=None to the FastAPI() constructor.","Application-level middleware runs in a single process.","SQLAlchemy ORM queries (.where(User.name == value)) are always parameterized — the value is never interpolated into the SQL string."]},{"id":"python/fastapi/security_hardening_and_owasp#Q5-2853","section":"python","module":"python/fastapi/security_hardening_and_owasp","moduleName":"security hardening and owasp","qIndex":5,"difficulty":"core","question":"What is the security risk of pickle.loads() on user-supplied data, and what is the safe alternative?","answerFull":"pickle deserialization executes arbitrary Python code embedded in the byte stream via reduce. A crafted pickle payload can execute any system command with the application's privileges. The safe alternative is JSON (json.loads()), which has no code execution semantics, or Pydantic model validation which enforces schema and type safety on top of JSON. Never deserialize untrusted data with pickle, marshal, or yaml.load() (unsafe loader).","correct":"pickle deserialization executes arbitrary Python code embedded in the byte stream via reduce.","distractors":["Authentication establishes who the caller is (verifies the JWT signature and returns the User object).","BOPLA (Broken Object Property Level Authorization) occurs when a user can read or write fields they should not access — for example, reading another user's stripe_customer_id or writing to is_admin.","Returning 404 leaks the existence of the resource: an attacker who receives a different error code for \"exists but not mine\" versus \"does not exist\" can enumerate all resource IDs in the system."]},{"id":"python/fastapi/security_hardening_and_owasp#Q6-2854","section":"python","module":"python/fastapi/security_hardening_and_owasp","moduleName":"security hardening and owasp","qIndex":6,"difficulty":"intermediate","question":"How should you configure CORS in a production FastAPI application?","answerFull":"Set allow_origins to an explicit list of permitted origins from configuration (e.g., [\"https://app.example.com\"]), never [\"*\"]. Set allow_methods and allow_headers explicitly. The combination of allow_origins=[\"*\"] and allow_credentials=True is rejected by browsers but signals a configuration error. Credentials (cookies, Authorization header) should only cross origins when the origin is explicitly trusted.","correct":"Set allow_origins to an explicit list of permitted origins from configuration (e.g., [\"https://app.example.com\"]), never [\"*\"].","distractors":["SQLAlchemy ORM queries (.where(User.name == value)) are always parameterized — the value is never interpolated into the SQL string.","Pass docs_url=None, redoc_url=None to the FastAPI() constructor.","Application-level middleware runs in a single process."]},{"id":"python/fastapi/security_hardening_and_owasp#Q7-2855","section":"python","module":"python/fastapi/security_hardening_and_owasp","moduleName":"security hardening and owasp","qIndex":7,"difficulty":"intermediate","question":"How does SecretStr from Pydantic protect secrets, and what is the safe way to access the underlying value?","answerFull":"SecretStr stores the value internally but returns \"\" from repr, str, and model_dump(). This prevents accidental logging of database passwords and API keys when printing settings objects or serializing config to JSON. Access the raw value only at the point of use with .get_secret_value(), not at import time or in a module-level variable.","correct":"SecretStr stores the value internally but returns \"\" from repr, str, and model_dump().","distractors":["Returning 404 leaks the existence of the resource: an attacker who receives a different error code for \"exists but not mine\" versus \"does not exist\" can enumerate all resource IDs in the system.","BOPLA (Broken Object Property Level Authorization) occurs when a user can read or write fields they should not access — for example, reading another user's stripe_customer_id or writing to is_admin.","BOLA (Broken Object Level Authorization) occurs when an API allows a user to access any resource by ID without verifying ownership."]},{"id":"python/fastapi/security_hardening_and_owasp#Q8-2856","section":"python","module":"python/fastapi/security_hardening_and_owasp","moduleName":"security hardening and owasp","qIndex":8,"difficulty":"intermediate","question":"Why can't rate limiting be implemented purely in application middleware for high-traffic services?","answerFull":"Application-level middleware runs in a single process. Under multiple Uvicorn workers or horizontal pod scaling, each instance has its own counter. An attacker can bypass per-process rate limits by distributing requests across instances. Production rate limiting requires a shared external store — typically Redis with atomic INCR/EXPIRE or a token-bucket LUA script — so all instances share the same counter for a given IP or account.","correct":"Application-level middleware runs in a single process.","distractors":["bandit performs static analysis for common Python security issues (hardcoded passwords, subprocess shell injection, pickle.loads, eval, weak cryptography) and runs in under 10 seconds on most codebases.","BOPLA (Broken Object Property Level Authorization) occurs when a user can read or write fields they should not access — for example, reading another user's stripe_customer_id or writing to is_admin.","BOLA (Broken Object Level Authorization) occurs when an API allows a user to access any resource by ID without verifying ownership."]},{"id":"python/fastapi/security_hardening_and_owasp#Q9-2857","section":"python","module":"python/fastapi/security_hardening_and_owasp","moduleName":"security hardening and owasp","qIndex":9,"difficulty":"intermediate","question":"What is the difference between authentication and authorization, and why does FastAPI's dependency injection system make authorization easier to implement correctly?","answerFull":"Authentication establishes who the caller is (verifies the JWT signature and returns the User object). Authorization determines what that caller may do (checks ownership, role, or scope for the specific operation). FastAPI dependencies compose: current_user = Depends(get_current_user) handles authentication once; Depends(require_role(\"admin\")) handles role authorization; the ownership check inside the route handler handles object-level authorization. Each layer is independently testable and reusable via dependency_overrides.","correct":"Authentication establishes who the caller is (verifies the JWT signature and returns the User object).","distractors":["BOLA (Broken Object Level Authorization) occurs when an API allows a user to access any resource by ID without verifying ownership.","SQLAlchemy ORM queries (.where(User.name == value)) are always parameterized — the value is never interpolated into the SQL string.","Pass docs_url=None, redoc_url=None to the FastAPI() constructor."]},{"id":"python/fastapi/security_hardening_and_owasp#Q10-2858","section":"python","module":"python/fastapi/security_hardening_and_owasp","moduleName":"security hardening and owasp","qIndex":10,"difficulty":"intermediate","question":"How do you handle SQL injection in SQLAlchemy async ORM vs raw SQL?","answerFull":"SQLAlchemy ORM queries (.where(User.name == value)) are always parameterized — the value is never interpolated into the SQL string. For raw SQL, use text() with bound parameters: text(\"SELECT * FROM users WHERE name = :name\") executed with {\"name\": value}. The dangerous anti-pattern is text(f\"SELECT ... WHERE name = '{user_input}'\") — string interpolation into text() bypasses parameterization. The LIKE/ILIKE pattern uses .ilike(f\"%{value}%\") on the ORM column, which parameterizes the entire pattern string.","correct":"SQLAlchemy ORM queries (.where(User.name == value)) are always parameterized — the value is never interpolated into the SQL string.","distractors":["BOPLA (Broken Object Property Level Authorization) occurs when a user can read or write fields they should not access — for example, reading another user's stripe_customer_id or writing to is_admin.","BOLA (Broken Object Level Authorization) occurs when an API allows a user to access any resource by ID without verifying ownership.","bandit performs static analysis for common Python security issues (hardcoded passwords, subprocess shell injection, pickle.loads, eval, weak cryptography) and runs in under 10 seconds on most codebases."]},{"id":"python/fastapi/security_hardening_and_owasp#Q11-2859","section":"python","module":"python/fastapi/security_hardening_and_owasp","moduleName":"security hardening and owasp","qIndex":11,"difficulty":"advanced","question":"How would you remove Swagger UI in production without breaking the OpenAPI spec generation?","answerFull":"Pass docs_url=None, redoc_url=None to the FastAPI() constructor. This disables the /docs and /redoc HTML UIs while leaving the /openapi.json spec endpoint active. If the spec endpoint must also be restricted, additionally pass openapi_url=None or protect it with an HTTPBasic dependency. Internal developer portals can mount Swagger UI on a separate path behind VPN or authentication.","correct":"Pass docs_url=None, redoc_url=None to the FastAPI() constructor.","distractors":["BOLA (Broken Object Level Authorization) occurs when an API allows a user to access any resource by ID without verifying ownership.","Returning 404 leaks the existence of the resource: an attacker who receives a different error code for \"exists but not mine\" versus \"does not exist\" can enumerate all resource IDs in the system.","BOPLA (Broken Object Property Level Authorization) occurs when a user can read or write fields they should not access — for example, reading another user's stripe_customer_id or writing to is_admin."]},{"id":"python/fastapi/security_hardening_and_owasp#Q12-2860","section":"python","module":"python/fastapi/security_hardening_and_owasp","moduleName":"security hardening and owasp","qIndex":12,"difficulty":"advanced","question":"What tools belong in a Python security CI pipeline and what does each check?","answerFull":"bandit performs static analysis for common Python security issues (hardcoded passwords, subprocess shell injection, pickle.loads, eval, weak cryptography) and runs in under 10 seconds on most codebases. pip-audit compares installed packages against the OSV and PyPI Advisory databases and blocks deploys on HIGH/CRITICAL CVEs. detect-secrets runs as a pre-commit hook to prevent committing API keys, passwords, and tokens. Together these three cover the supply chain (pip-audit), code patterns (bandit), and accidental secret commits (detect-secrets).","correct":"bandit performs static analysis for common Python security issues (hardcoded passwords, subprocess shell injection, pickle.loads, eval, weak cryptography) and runs in under 10 seconds on most codebases.","distractors":["pickle deserialization executes arbitrary Python code embedded in the byte stream via reduce.","Application-level middleware runs in a single process.","BOLA (Broken Object Level Authorization) occurs when an API allows a user to access any resource by ID without verifying ownership."]},{"id":"python/fastapi/security_hardening_and_owasp#Q13-2861","section":"python","module":"python/fastapi/security_hardening_and_owasp","moduleName":"security hardening and owasp","qIndex":13,"difficulty":"advanced","question":"What is improper inventory management (OWASP API9) and how do versioned APIRouters address it?","answerFull":"Improper inventory management means old, forgotten, or shadow API endpoints remain accessible after newer versions ship. Attackers probe /v1/, /v0/, /internal/, /debug/ paths. The FastAPI fix is explicit APIRouter versioning with a deprecation lifecycle: add a Deprecated: true header and Sunset: <date> header to v1 routes when v2 ships, and remove them on the sunset date. CI can count public routes via app.routes and alert on unexpected additions.","correct":"Improper inventory management means old, forgotten, or shadow API endpoints remain accessible after newer versions ship.","distractors":["BOLA (Broken Object Level Authorization) occurs when an API allows a user to access any resource by ID without verifying ownership.","Returning 404 leaks the existence of the resource: an attacker who receives a different error code for \"exists but not mine\" versus \"does not exist\" can enumerate all resource IDs in the system.","BOPLA (Broken Object Property Level Authorization) occurs when a user can read or write fields they should not access — for example, reading another user's stripe_customer_id or writing to is_admin."]},{"id":"python/fastapi/api_design_and_versioning#Q1-2862","section":"python","module":"python/fastapi/api_design_and_versioning","moduleName":"api design and versioning","qIndex":1,"difficulty":"core","question":"What is the difference between 401 and 403?","answerFull":"401 means the request lacks valid authentication credentials — the server does not know who you are. 403 means the server knows who you are but you do not have permission to access the resource. Return 401 when the Authorization header is missing or invalid; return 403 when the token is valid but the user lacks the required role. Mixing them leaks information about whether a resource exists.","correct":"401 means the request lacks valid authentication credentials — the server does not know who you are.","distractors":["409 Conflict. The request was valid (400 is wrong — nothing is malformed), the resource exists (201 would be wrong), and the server is not to blame (500 is wrong).","Offset pagination uses OFFSET n which is recomputed on each query.","created_at timestamps are not unique — under load, multiple rows can share the same millisecond."]},{"id":"python/fastapi/api_design_and_versioning#Q2-2863","section":"python","module":"python/fastapi/api_design_and_versioning","moduleName":"api design and versioning","qIndex":2,"difficulty":"core","question":"Why is cursor pagination better than offset pagination for large, live datasets?","answerFull":"Offset pagination uses OFFSET n which is recomputed on each query. If rows are inserted between page 1 and page 2, the offset shifts rows, causing duplicates or gaps. Cursor pagination uses a keyset condition (WHERE (ts, id) < (cursor_ts, cursor_id)) bound to the actual last-seen row, making it immune to concurrent inserts. Additionally, OFFSET 50000 on Postgres requires scanning and discarding 50,000 rows; keyset pagination uses the index directly, keeping query latency constant regardless of depth.","correct":"Offset pagination uses OFFSET n which is recomputed on each query.","distractors":["401 means the request lacks valid authentication credentials — the server does not know who you are.","created_at timestamps are not unique — under load, multiple rows can share the same millisecond.","Accept an Idempotency-Key header from the client."]},{"id":"python/fastapi/api_design_and_versioning#Q3-2864","section":"python","module":"python/fastapi/api_design_and_versioning","moduleName":"api design and versioning","qIndex":3,"difficulty":"core","question":"What makes a composite (created_at, id) cursor more reliable than a single created_at cursor?","answerFull":"created_at timestamps are not unique — under load, multiple rows can share the same millisecond. Using id (a sequence-generated integer) as a tiebreaker ensures the condition (created_at = ts AND id < last_id) OR (created_at < ts) uniquely identifies a stable position in the result set, even across ties.","correct":"created_at timestamps are not unique — under load, multiple rows can share the same millisecond.","distractors":["401 means the request lacks valid authentication credentials — the server does not know who you are.","Offset pagination uses OFFSET n which is recomputed on each query.","Accept an Idempotency-Key header from the client."]},{"id":"python/fastapi/api_design_and_versioning#Q4-2865","section":"python","module":"python/fastapi/api_design_and_versioning","moduleName":"api design and versioning","qIndex":4,"difficulty":"core","question":"How do you implement idempotency for a POST endpoint that creates a payment?","answerFull":"Accept an Idempotency-Key header from the client. On first receipt, attempt to SET idempotency:<key> NX EX 86400 in Redis before executing the mutation. If the SET returns nil (key exists), return the cached response. If SET succeeds, proceed, persist the result, then overwrite the Redis key with the final response. This covers the race where two parallel retries arrive simultaneously — both see the key at the same time, but SET NX is atomic so only one proceeds. TTL of 24 hours matches Stripe's and Shopify's standard.","correct":"Accept an Idempotency-Key header from the client.","distractors":["Require an Idempotency-Key UUID header.","Model the relationship as a sub-resource: POST /teams/42/members to add a user to a team, DELETE /teams/42/members/7 to remove them.","401 means the request lacks valid authentication credentials — the server does not know who you are."]},{"id":"python/fastapi/api_design_and_versioning#Q5-2866","section":"python","module":"python/fastapi/api_design_and_versioning","moduleName":"api design and versioning","qIndex":5,"difficulty":"core","question":"What are the trade-offs between URL path versioning and Accept header versioning?","answerFull":"URL path versioning (/v1/) is visible in logs, browser-friendly, cacheable by CDN per path prefix, and simple to route at the gateway layer. Its downside is that it technically violates REST — the same resource lives at two different URLs. Header versioning keeps a single canonical URL and is more RESTfully pure, but requires Vary: Accept on CDN responses, is invisible in browser address bars, and harder to route in gateways. For public APIs (Stripe, GitHub, Twilio), URL path versioning dominates because operational simplicity outweighs REST purity.","correct":"URL path versioning (/v1/) is visible in logs, browser-friendly, cacheable by CDN per path prefix, and simple to route at the gateway layer.","distractors":["Accept an Idempotency-Key header from the client.","Require an Idempotency-Key UUID header.","401 means the request lacks valid authentication credentials — the server does not know who you are."]},{"id":"python/fastapi/api_design_and_versioning#Q6-2867","section":"python","module":"python/fastapi/api_design_and_versioning","moduleName":"api design and versioning","qIndex":6,"difficulty":"intermediate","question":"What is the sliding window rate limiting algorithm and why does it outperform fixed window?","answerFull":"A fixed window counts requests in a fixed calendar interval (e.g., 09:00:00–09:01:00). A request burst at 09:00:59 and another at 09:01:01 consumes two full windows back-to-back, effectively doubling the allowed rate at the boundary. Sliding window maintains a per-user sorted set of request timestamps in Redis and counts entries within the last 60 seconds on each request. It eliminates the boundary burst at the cost of O(log N) Redis operations per request and O(N) memory per user, where N is the request count in the window.","correct":"A fixed window counts requests in a fixed calendar interval (e.g., 09:00:00–09:01:00).","distractors":["401 means the request lacks valid authentication credentials — the server does not know who you are.","Offset pagination uses OFFSET n which is recomputed on each query.","created_at timestamps are not unique — under load, multiple rows can share the same millisecond."]},{"id":"python/fastapi/api_design_and_versioning#Q7-2868","section":"python","module":"python/fastapi/api_design_and_versioning","moduleName":"api design and versioning","qIndex":7,"difficulty":"intermediate","question":"How does FastAPI's deprecated=True in a path operation affect clients?","answerFull":"FastAPI marks the operation in the generated OpenAPI spec with \"deprecated\": true. Swagger UI renders the endpoint with a strikethrough label. SDK generators (like openapi-python-client) typically add a deprecation warning to the generated method's docstring. The response itself is unchanged — deprecated=True is metadata only. To actively signal deprecation at runtime, also add a Deprecated: true and Sunset response header via a middleware or in the handler.","correct":"FastAPI marks the operation in the generated OpenAPI spec with \"deprecated\": true.","distractors":["Return 204 when the operation succeeded and there is genuinely nothing to return: DELETE of a resource, PUT when you do not want to return the updated body, acknowledgement of a webhook.","URL path versioning (/v1/) is visible in logs, browser-friendly, cacheable by CDN per path prefix, and simple to route at the gateway layer.","401 means the request lacks valid authentication credentials — the server does not know who you are."]},{"id":"python/fastapi/api_design_and_versioning#Q8-2869","section":"python","module":"python/fastapi/api_design_and_versioning","moduleName":"api design and versioning","qIndex":8,"difficulty":"intermediate","question":"How would you prevent a client from calling a POST /orders endpoint twice and charging a user twice?","answerFull":"Require an Idempotency-Key UUID header. Store it in Redis with SET NX EX 86400 before touching the database. If the key already exists, return the original response (from cache) without re-running the mutation. If the key does not exist, set it as a placeholder, process the order, store the completed response in Redis, then return it. On the client side, generate a new UUID per distinct user intent (not per retry) and reuse the same UUID on every retry for that intent.","correct":"Require an Idempotency-Key UUID header.","distractors":["Accept an Idempotency-Key header from the client.","Model the relationship as a sub-resource: POST /teams/42/members to add a user to a team, DELETE /teams/42/members/7 to remove them.","401 means the request lacks valid authentication credentials — the server does not know who you are."]},{"id":"python/fastapi/api_design_and_versioning#Q9-2870","section":"python","module":"python/fastapi/api_design_and_versioning","moduleName":"api design and versioning","qIndex":9,"difficulty":"intermediate","question":"Your API has 10 replicas. A user has a rate limit of 100 req/min. With in-process rate limiting, what actually happens?","answerFull":"Each replica maintains its own counter. The user can send 100 requests to each replica, making the effective limit 1,000 req/min — 10× the intended limit. The fix is to push all counter state to a shared Redis instance (slowapi with storage_uri=\"redis://...\" or fastapi-limiter). The tradeoff is that every request now incurs a Redis round trip (~0.5–1 ms on a local Redis). For very high-throughput endpoints, use token bucket with a local refill approximation, syncing to Redis every 100ms instead of per-request.","correct":"Each replica maintains its own counter.","distractors":["Model the relationship as a sub-resource: POST /teams/42/members to add a user to a team, DELETE /teams/42/members/7 to remove them.","401 means the request lacks valid authentication credentials — the server does not know who you are.","Offset pagination uses OFFSET n which is recomputed on each query."]},{"id":"python/fastapi/api_design_and_versioning#Q10-2871","section":"python","module":"python/fastapi/api_design_and_versioning","moduleName":"api design and versioning","qIndex":10,"difficulty":"intermediate","question":"What HTTP status code should you return when a client submits a duplicate order (same external order reference already exists)?","answerFull":"409 Conflict. The request was valid (400 is wrong — nothing is malformed), the resource exists (201 would be wrong), and the server is not to blame (500 is wrong). Return the existing resource in the response body alongside the 409 so the client does not need a second round trip to fetch it.","correct":"409 Conflict. The request was valid (400 is wrong — nothing is malformed), the resource exists (201 would be wrong), and the server is not to blame (500 is wrong).","distractors":["401 means the request lacks valid authentication credentials — the server does not know who you are.","Accept an Idempotency-Key header from the client.","created_at timestamps are not unique — under load, multiple rows can share the same millisecond."]},{"id":"python/fastapi/api_design_and_versioning#Q11-2872","section":"python","module":"python/fastapi/api_design_and_versioning","moduleName":"api design and versioning","qIndex":11,"difficulty":"advanced","question":"How would you design a REST API for a resource that has a many-to-many relationship, such as users and teams?","answerFull":"Model the relationship as a sub-resource: POST /teams/42/members to add a user to a team, DELETE /teams/42/members/7 to remove them. Avoid deep nesting beyond two levels. For queries that cross both sides (\"all teams for user 7\"), use a query param on the collection: GET /teams?member_id=7. If membership has its own attributes (join date, role), model it as a first-class resource: POST /memberships, GET /memberships/{id}.","correct":"Model the relationship as a sub-resource: POST /teams/42/members to add a user to a team, DELETE /teams/42/members/7 to remove them.","distractors":["Return 204 when the operation succeeded and there is genuinely nothing to return: DELETE of a resource, PUT when you do not want to return the updated body, acknowledgement of a webhook.","409 Conflict. The request was valid (400 is wrong — nothing is malformed), the resource exists (201 would be wrong), and the server is not to blame (500 is wrong).","401 means the request lacks valid authentication credentials — the server does not know who you are."]},{"id":"python/fastapi/api_design_and_versioning#Q12-2873","section":"python","module":"python/fastapi/api_design_and_versioning","moduleName":"api design and versioning","qIndex":12,"difficulty":"advanced","question":"When should you return 204 No Content versus 200 OK?","answerFull":"Return 204 when the operation succeeded and there is genuinely nothing to return: DELETE of a resource, PUT when you do not want to return the updated body, acknowledgement of a webhook. Return 200 when you return a body — the updated resource after PUT/PATCH, the collection after GET, the confirmation object after a POST that creates and returns. Never return 200 with an empty body — that confuses clients that buffer the body expecting JSON.","correct":"Return 204 when the operation succeeded and there is genuinely nothing to return: DELETE of a resource, PUT when you do not want to return the updated body, acknowledgement of a webhook.","distractors":["409 Conflict. The request was valid (400 is wrong — nothing is malformed), the resource exists (201 would be wrong), and the server is not to blame (500 is wrong).","FastAPI marks the operation in the generated OpenAPI spec with \"deprecated\": true.","Model the relationship as a sub-resource: POST /teams/42/members to add a user to a team, DELETE /teams/42/members/7 to remove them."]},{"id":"python/fastapi/testing_fastapi#Q1-2874","section":"python","module":"python/fastapi/testing_fastapi","moduleName":"testing fastapi","qIndex":1,"difficulty":"core","question":"What is the difference between TestClient and httpx.AsyncClient for FastAPI tests?","answerFull":"TestClient is synchronous — it wraps the ASGI app via anyio.from_thread.run_sync and allows def test functions. httpx.AsyncClient is asynchronous — it must be await-ed and is used inside async def test functions with pytest-asyncio. TestClient flushes BackgroundTasks synchronously before returning; AsyncClient does not. Use TestClient for simplicity; use AsyncClient when fixtures or assertions require await.","correct":"TestClient is synchronous — it wraps the ASGI app via anyio.from_thread.run_sync and allows def test functions.","distractors":["dependency_overrides is a dict on the FastAPI instance mapping a dependency callable to a replacement callable.","Each test acquires a database connection, begins an outer transaction, then begins a SAVEPOINT (BEGIN NESTED).","Use the respx library, which intercepts httpx transport at the session level without patching globals."]},{"id":"python/fastapi/testing_fastapi#Q2-2875","section":"python","module":"python/fastapi/testing_fastapi","moduleName":"testing fastapi","qIndex":2,"difficulty":"core","question":"Why must TestClient be used as a context manager when the app uses lifespan?","answerFull":"FastAPI's lifespan function runs startup logic inside aenter and shutdown logic inside aexit. Constructing TestClient(app) directly does not call aenter, so startup never runs — any state set in lifespan (e.g., app.state.db) is missing. Using with TestClient(app) as c: triggers the lifespan on entry and shuts it down on exit, matching production behaviour exactly.","correct":"FastAPI's lifespan function runs startup logic inside aenter and shutdown logic inside aexit.","distractors":["Use client.websocket_connect(\"/ws/path\") as a context manager.","Patch the email function with unittest.mock.AsyncMock (for async functions) or MagicMock (for sync), then call the endpoint via TestClient.","Run pytest --cov=app --cov-report=term-missing --cov-fail-under=80."]},{"id":"python/fastapi/testing_fastapi#Q3-2876","section":"python","module":"python/fastapi/testing_fastapi","moduleName":"testing fastapi","qIndex":3,"difficulty":"core","question":"How does app.dependency_overrides work and why is it better than monkeypatch?","answerFull":"dependency_overrides is a dict on the FastAPI instance mapping a dependency callable to a replacement callable. Before each request, FastAPI resolves the dependency graph and substitutes any callable found in dependency_overrides. It is better than monkeypatch because it operates at the contract level (the function signature FastAPI resolves) rather than the module level (a name in a namespace). The override survives refactors that rename or move the implementation but keep the same dependency function.","correct":"dependency_overrides is a dict on the FastAPI instance mapping a dependency callable to a replacement callable.","distractors":["By default, SQLAlchemy expires all ORM instance attributes after a commit(), forcing a new SELECT on next access.","Run pytest --cov=app --cov-report=term-missing --cov-fail-under=80.","TestClient is synchronous — it wraps the ASGI app via anyio.from_thread.run_sync and allows def test functions."]},{"id":"python/fastapi/testing_fastapi#Q4-2877","section":"python","module":"python/fastapi/testing_fastapi","moduleName":"testing fastapi","qIndex":4,"difficulty":"core","question":"Explain the transaction rollback strategy for test database isolation.","answerFull":"Each test acquires a database connection, begins an outer transaction, then begins a SAVEPOINT (BEGIN NESTED). The test body — including all ORM flushes and even explicit commit() calls on the test session — executes within this savepoint. After the test, the fixture rolls back the outer connection, discarding all changes. No rows persist to disk; the schema remains intact. This avoids table truncation or re-migration and keeps each test under 5 ms of overhead.","correct":"Each test acquires a database connection, begins an outer transaction, then begins a SAVEPOINT (BEGIN NESTED).","distractors":["TestClient is synchronous — it wraps the ASGI app via anyio.from_thread.run_sync and allows def test functions.","A session-scoped database session persists across the entire test run.","Each worker process gets its own database connection and its own savepoint."]},{"id":"python/fastapi/testing_fastapi#Q5-2878","section":"python","module":"python/fastapi/testing_fastapi","moduleName":"testing fastapi","qIndex":5,"difficulty":"core","question":"Why set expire_on_commit=False on the test AsyncSession?","answerFull":"By default, SQLAlchemy expires all ORM instance attributes after a commit(), forcing a new SELECT on next access. In tests, the session is rolled back after the test — the connection is gone — so any lazy-load attempt on an expired attribute raises DetachedInstanceError. Setting expire_on_commit=False keeps attribute values in memory, safe to access after commit within the test.","correct":"By default, SQLAlchemy expires all ORM instance attributes after a commit(), forcing a new SELECT on next access.","distractors":["TestClient is synchronous — it wraps the ASGI app via anyio.from_thread.run_sync and allows def test functions.","dependency_overrides is a dict on the FastAPI instance mapping a dependency callable to a replacement callable.","Each test acquires a database connection, begins an outer transaction, then begins a SAVEPOINT (BEGIN NESTED)."]},{"id":"python/fastapi/testing_fastapi#Q6-2879","section":"python","module":"python/fastapi/testing_fastapi","moduleName":"testing fastapi","qIndex":6,"difficulty":"intermediate","question":"How do you test an endpoint that sends an email as a BackgroundTask?","answerFull":"Patch the email function with unittest.mock.AsyncMock (for async functions) or MagicMock (for sync), then call the endpoint via TestClient. Because TestClient executes BackgroundTasks synchronously before returning the response object, you can call mock_email.assert_called_once_with(expected_args) immediately after the response.","correct":"Patch the email function with unittest.mock.AsyncMock (for async functions) or MagicMock (for sync), then call the endpoint via TestClient.","distractors":["A session-scoped database session persists across the entire test run.","Each test acquires a database connection, begins an outer transaction, then begins a SAVEPOINT (BEGIN NESTED).","TestClient is synchronous — it wraps the ASGI app via anyio.from_thread.run_sync and allows def test functions."]},{"id":"python/fastapi/testing_fastapi#Q7-2880","section":"python","module":"python/fastapi/testing_fastapi","moduleName":"testing fastapi","qIndex":7,"difficulty":"intermediate","question":"How would you test a WebSocket endpoint with TestClient?","answerFull":"Use client.websocket_connect(\"/ws/path\") as a context manager. Inside the block, call ws.send_text() / ws.send_json() to send messages and ws.receive_text() / ws.receive_json() to read responses. TestClient handles the HTTP upgrade handshake internally. Exit the context manager to close the connection and trigger any server-side disconnect handlers.","correct":"Use client.websocket_connect(\"/ws/path\") as a context manager.","distractors":["TestClient is synchronous — it wraps the ASGI app via anyio.from_thread.run_sync and allows def test functions.","Each test acquires a database connection, begins an outer transaction, then begins a SAVEPOINT (BEGIN NESTED).","Patch the email function with unittest.mock.AsyncMock (for async functions) or MagicMock (for sync), then call the endpoint via TestClient."]},{"id":"python/fastapi/testing_fastapi#Q9-2881","section":"python","module":"python/fastapi/testing_fastapi","moduleName":"testing fastapi","qIndex":9,"difficulty":"intermediate","question":"How do you mock outbound HTTP calls made by your FastAPI routes?","answerFull":"Use the respx library, which intercepts httpx transport at the session level without patching globals. Decorate the test with @respx.mock and configure routes with respx.get(url).mock(return_value=httpx.Response(200, json={...})). Any httpx call matching the pattern returns the mocked response; unmatched calls raise respx.MockNotFoundError, preventing accidental real network calls.","correct":"Use the respx library, which intercepts httpx transport at the session level without patching globals.","distractors":["FastAPI's lifespan function runs startup logic inside aenter and shutdown logic inside aexit.","A session-scoped database session persists across the entire test run.","dependency_overrides is a dict on the FastAPI instance mapping a dependency callable to a replacement callable."]},{"id":"python/fastapi/testing_fastapi#Q10-2882","section":"python","module":"python/fastapi/testing_fastapi","moduleName":"testing fastapi","qIndex":10,"difficulty":"intermediate","question":"What is the danger of using scope=\"session\" for db_session fixtures?","answerFull":"A session-scoped database session persists across the entire test run. Tests that insert rows accumulate state — test B sees rows created by test A, and tests that create the same primary key twice get IntegrityError. Test order becomes significant, and tests are no longer independent. The engine can be session-scoped (expensive setup once), but the session itself must be function-scoped with the rollback strategy to ensure isolation.","correct":"A session-scoped database session persists across the entire test run.","distractors":["Each test acquires a database connection, begins an outer transaction, then begins a SAVEPOINT (BEGIN NESTED).","Run pytest --cov=app --cov-report=term-missing --cov-fail-under=80.","Each worker process gets its own database connection and its own savepoint."]},{"id":"python/fastapi/testing_fastapi#Q12-2883","section":"python","module":"python/fastapi/testing_fastapi","moduleName":"testing fastapi","qIndex":12,"difficulty":"advanced","question":"How do you measure test coverage and what should you target?","answerFull":"Run pytest --cov=app --cov-report=term-missing --cov-fail-under=80. Target 80–90 % line coverage for the app/ package. Prioritise covering: route handlers (all status codes), dependency functions (get_db, get_current_user), error paths (HTTPException branches), and Pydantic validation rejections. Do not chase 100 % — generated code (Pydantic model fields, SQLAlchemy columns), migration scripts, and main blocks add noise without value.","correct":"Run pytest --cov=app --cov-report=term-missing --cov-fail-under=80.","distractors":["TestClient is synchronous — it wraps the ASGI app via anyio.from_thread.run_sync and allows def test functions.","Each test acquires a database connection, begins an outer transaction, then begins a SAVEPOINT (BEGIN NESTED).","A session-scoped database session persists across the entire test run."]},{"id":"python/fastapi/testing_fastapi#Q13-2884","section":"python","module":"python/fastapi/testing_fastapi","moduleName":"testing fastapi","qIndex":13,"difficulty":"advanced","question":"How do you handle pytest-xdist parallel execution with the rollback strategy?","answerFull":"Each worker process gets its own database connection and its own savepoint. Because the rollback strategy never commits to shared tables, workers do not interfere. The only risk is if two workers use a sequence that generates the same primary key — avoid hard-coded IDs in factories; use db.flush() to let the sequence assign PKs. For the SQLite in-memory engine use StaticPool with a single connection; for PostgreSQL use a per-worker schema prefix or a separate test database per worker (--dist=loadscope with schema isolation).","correct":"Each worker process gets its own database connection and its own savepoint.","distractors":["Run pytest --cov=app --cov-report=term-missing --cov-fail-under=80.","Each test acquires a database connection, begins an outer transaction, then begins a SAVEPOINT (BEGIN NESTED).","A session-scoped database session persists across the entire test run."]},{"id":"python/fastapi/fastapi_fundamentals_asgi#Q1-2885","section":"python","module":"python/fastapi/fastapi_fundamentals_asgi","moduleName":"fastapi fundamentals asgi","qIndex":1,"difficulty":"core","question":"What is the difference between WSGI and ASGI?","answerFull":"WSGI is a synchronous interface: app(environ, start_response) is a regular Python callable that blocks the server thread until the response is ready. ASGI is an asynchronous interface: await app(scope, receive, send) is a coroutine that yields control back to the event loop while waiting for I/O. WSGI requires one thread per concurrent request; ASGI can handle thousands of concurrent connections on a single thread. ASGI also supports WebSockets and Server-Sent Events natively; WSGI does not.","correct":"WSGI is a synchronous interface: app(environ, start_response) is a regular Python callable that blocks the server thread until the response is ready.","distractors":["FastAPI passes the route handler's return value to response_model.model_validate(return_value), producing a Pydantic instance containing only the fields declared in response_model.","StreamingResponse wraps an async generator (or sync generator run in a thread) that yields bytes or str chunks.","FileResponse uses aiofiles (or anyio) to send a file from disk using the OS's sendfile syscall when available, which copies data from disk to the socket buffer without passing through Python's heap."]},{"id":"python/fastapi/fastapi_fundamentals_asgi#Q2-2886","section":"python","module":"python/fastapi/fastapi_fundamentals_asgi","moduleName":"fastapi fundamentals asgi","qIndex":2,"difficulty":"core","question":"What are the three arguments in the ASGI 3 callable signature, and what does each carry?","answerFull":"scope is a plain dict describing the connection — type (http, websocket, lifespan), method, path, headers, and query string. receive is an async callable that returns the next incoming event (request body chunk or WebSocket message) as a dict. send is an async callable that accepts an outgoing event dict (http.response.start with status and headers, then http.response.body with the body bytes). The application awaits receive to read input and awaits send for each piece of output.","correct":"scope is a plain dict describing the connection — type (http, websocket, lifespan), method, path, headers, and query string.","distractors":["Use async def when the handler calls other async functions — awaiting an async DB driver, making async HTTP calls with httpx, or reading from a Redis async client.","FastAPI resolves the path parameter at startup, noting that item_id maps to int.","Each middleware is an ASGI callable added via app.add_middleware(SomeMiddleware, kwargs)."]},{"id":"python/fastapi/fastapi_fundamentals_asgi#Q3-2887","section":"python","module":"python/fastapi/fastapi_fundamentals_asgi","moduleName":"fastapi fundamentals asgi","qIndex":3,"difficulty":"core","question":"What is Starlette and how does FastAPI relate to it?","answerFull":"Starlette is a lightweight ASGI framework providing routing, middleware, request/response abstractions, sessions, background tasks, and test client. FastAPI subclasses Starlette's APIRouter and Request and adds automatic Pydantic validation, dependency injection, and OpenAPI generation on top. You can add Starlette middleware directly to a FastAPI app, and a FastAPI route can return any Starlette Response subclass.","correct":"Starlette is a lightweight ASGI framework providing routing, middleware, request/response abstractions, sessions, background tasks, and test client.","distractors":["FastAPI determines the source of each parameter at startup.","WSGI is a synchronous interface: app(environ, start_response) is a regular Python callable that blocks the server thread until the response is ready.","It tells FastAPI to wrap the handler's return value in an HTMLResponse instead of the default JSONResponse."]},{"id":"python/fastapi/fastapi_fundamentals_asgi#Q4-2888","section":"python","module":"python/fastapi/fastapi_fundamentals_asgi","moduleName":"fastapi fundamentals asgi","qIndex":4,"difficulty":"core","question":"Why use lifespan instead of on_startup / on_shutdown?","answerFull":"The lifespan context manager was introduced to replace the deprecated event handlers in FastAPI 0.95. It solves two problems the old API could not: (1) it provides a yield-based structure so resources created in startup are automatically closed in shutdown even if an exception occurs; (2) the dict yielded from the context manager is injected into app.state and accessible via request.state in every route handler, removing the need for module-level globals. The old handlers could not share state with routes without globals.","correct":"The lifespan context manager was introduced to replace the deprecated event handlers in FastAPI 0.95.","distractors":["scope is a plain dict describing the connection — type (http, websocket, lifespan), method, path, headers, and query string.","Uvicorn runs Python's asyncio event loop in a single thread.","FastAPI passes the route handler's return value to response_model.model_validate(return_value), producing a Pydantic instance containing only the fields declared in response_model."]},{"id":"python/fastapi/fastapi_fundamentals_asgi#Q5-2889","section":"python","module":"python/fastapi/fastapi_fundamentals_asgi","moduleName":"fastapi fundamentals asgi","qIndex":5,"difficulty":"core","question":"What happens when FastAPI receives a request for /items/abc with a route defined as @app.get(\"/items/{item_id}\") where item_id: int?","answerFull":"FastAPI resolves the path parameter at startup, noting that item_id maps to int. At request time, Pydantic v2 attempts to coerce the string \"abc\" to int. Coercion fails; FastAPI raises RequestValidationError, which is caught by the built-in exception handler and serialized as a 422 Unprocessable Entity JSON body with a detail array listing the exact field, location (path), and message (Input should be a valid integer). The route handler function is never called.","correct":"FastAPI resolves the path parameter at startup, noting that item_id maps to int.","distractors":["Each middleware is an ASGI callable added via app.add_middleware(SomeMiddleware, kwargs).","root_path is the URL prefix that the reverse proxy strips before forwarding to Uvicorn.","WSGI is a synchronous interface: app(environ, start_response) is a regular Python callable that blocks the server thread until the response is ready."]},{"id":"python/fastapi/fastapi_fundamentals_asgi#Q6-2890","section":"python","module":"python/fastapi/fastapi_fundamentals_asgi","moduleName":"fastapi fundamentals asgi","qIndex":6,"difficulty":"intermediate","question":"How does FastAPI filter extra fields when response_model is set?","answerFull":"FastAPI passes the route handler's return value to response_model.model_validate(return_value), producing a Pydantic instance containing only the fields declared in response_model. It then calls .model_dump() and serializes the resulting dict to JSON. Any field present in the handler's return value but absent from response_model is silently dropped. This is Pydantic v2 behaviour; in v1 the method was .dict().","correct":"FastAPI passes the route handler's return value to response_model.model_validate(return_value), producing a Pydantic instance containing only the fields declared in response_model.","distractors":["FastAPI determines the source of each parameter at startup.","Use async def when the handler calls other async functions — awaiting an async DB driver, making async HTTP calls with httpx, or reading from a Redis async client.","It tells FastAPI to wrap the handler's return value in an HTMLResponse instead of the default JSONResponse."]},{"id":"python/fastapi/fastapi_fundamentals_asgi#Q7-2891","section":"python","module":"python/fastapi/fastapi_fundamentals_asgi","moduleName":"fastapi fundamentals asgi","qIndex":7,"difficulty":"intermediate","question":"When should you use def versus async def for a route handler in FastAPI?","answerFull":"Use async def when the handler calls other async functions — awaiting an async DB driver, making async HTTP calls with httpx, or reading from a Redis async client. FastAPI runs async def handlers directly on the event loop. Use def when calling a synchronous library that cannot be made async — synchronous ORM calls, CPU-intensive work, legacy SDKs. FastAPI runs def handlers in a thread pool executor (anyio.to_thread.run_sync), preventing event loop blockage. Never call a blocking function directly inside async def.","correct":"Use async def when the handler calls other async functions — awaiting an async DB driver, making async HTTP calls with httpx, or reading from a Redis async client.","distractors":["scope is a plain dict describing the connection — type (http, websocket, lifespan), method, path, headers, and query string.","FastAPI determines the source of each parameter at startup.","It tells FastAPI to wrap the handler's return value in an HTMLResponse instead of the default JSONResponse."]},{"id":"python/fastapi/fastapi_fundamentals_asgi#Q8-2892","section":"python","module":"python/fastapi/fastapi_fundamentals_asgi","moduleName":"fastapi fundamentals asgi","qIndex":8,"difficulty":"intermediate","question":"What does response_class=HTMLResponse do on a route decorator?","answerFull":"It tells FastAPI to wrap the handler's return value in an HTMLResponse instead of the default JSONResponse. This sets Content-Type: text/html; charset=utf-8. It also updates the OpenAPI schema to show text/html as the response media type. The handler can return a plain str and FastAPI will wrap it; or it can return an HTMLResponse instance directly for full control over status code and headers.","correct":"It tells FastAPI to wrap the handler's return value in an HTMLResponse instead of the default JSONResponse.","distractors":["FastAPI passes the route handler's return value to response_model.model_validate(return_value), producing a Pydantic instance containing only the fields declared in response_model.","Starlette is a lightweight ASGI framework providing routing, middleware, request/response abstractions, sessions, background tasks, and test client.","The lifespan context manager was introduced to replace the deprecated event handlers in FastAPI 0.95."]},{"id":"python/fastapi/fastapi_fundamentals_asgi#Q9-2893","section":"python","module":"python/fastapi/fastapi_fundamentals_asgi","moduleName":"fastapi fundamentals asgi","qIndex":9,"difficulty":"intermediate","question":"How does Uvicorn handle concurrent requests without multiple threads?","answerFull":"Uvicorn runs Python's asyncio event loop in a single thread. When an await expression suspends a coroutine (waiting for a socket read, a DB query, or an external HTTP response), the event loop picks up the next ready coroutine from its queue and resumes it. This cooperative multitasking means thousands of coroutines can be \"in flight\" simultaneously with one thread, as long as none of them block synchronously. The OS handles the underlying non-blocking I/O via select/epoll/kqueue; asyncio wraps these in awaitable Future objects.","correct":"Uvicorn runs Python's asyncio event loop in a single thread.","distractors":["A single Uvicorn worker runs one asyncio event loop and handles thousands of concurrent I/O-bound requests efficiently.","FileResponse uses aiofiles (or anyio) to send a file from disk using the OS's sendfile syscall when available, which copies data from disk to the socket buffer without passing through Python's heap.","root_path is the URL prefix that the reverse proxy strips before forwarding to Uvicorn."]},{"id":"python/fastapi/fastapi_fundamentals_asgi#Q11-2894","section":"python","module":"python/fastapi/fastapi_fundamentals_asgi","moduleName":"fastapi fundamentals asgi","qIndex":11,"difficulty":"advanced","question":"What is StreamingResponse used for and how does it work under ASGI?","answerFull":"StreamingResponse wraps an async generator (or sync generator run in a thread) that yields bytes or str chunks. When FastAPI serializes a StreamingResponse, it calls send({\"type\": \"http.response.start\", ...}) once, then iterates the generator, calling send({\"type\": \"http.response.body\", \"body\": chunk, \"more_body\": True}) for each chunk, and finally send({\"type\": \"http.response.body\", \"body\": b\"\", \"more_body\": False}). The client receives chunks incrementally. This is the mechanism behind Server-Sent Events, large file downloads, and real-time LLM token streaming.","correct":"StreamingResponse wraps an async generator (or sync generator run in a thread) that yields bytes or str chunks.","distractors":["Starlette is a lightweight ASGI framework providing routing, middleware, request/response abstractions, sessions, background tasks, and test client.","Each middleware is an ASGI callable added via app.add_middleware(SomeMiddleware, kwargs).","Uvicorn runs Python's asyncio event loop in a single thread."]},{"id":"python/fastapi/fastapi_fundamentals_asgi#Q12-2895","section":"python","module":"python/fastapi/fastapi_fundamentals_asgi","moduleName":"fastapi fundamentals asgi","qIndex":12,"difficulty":"advanced","question":"How does Starlette middleware interact with FastAPI routes?","answerFull":"Each middleware is an ASGI callable added via app.add_middleware(SomeMiddleware, kwargs). FastAPI wraps the middleware around the application in reverse order of addition — last added is outermost. The middleware receives the same (scope, receive, send) triple. It can read and modify scope, wrap receive to inspect the request body, wrap send to inspect or modify response headers, or short-circuit the chain by sending a response directly without calling the inner app. This mechanism is identical to how Django middleware works conceptually, but purely async.","correct":"Each middleware is an ASGI callable added via app.add_middleware(SomeMiddleware, kwargs).","distractors":["The lifespan context manager was introduced to replace the deprecated event handlers in FastAPI 0.95.","FastAPI passes the route handler's return value to response_model.model_validate(return_value), producing a Pydantic instance containing only the fields declared in response_model.","Starlette is a lightweight ASGI framework providing routing, middleware, request/response abstractions, sessions, background tasks, and test client."]},{"id":"python/fastapi/fastapi_fundamentals_asgi#Q13-2896","section":"python","module":"python/fastapi/fastapi_fundamentals_asgi","moduleName":"fastapi fundamentals asgi","qIndex":13,"difficulty":"advanced","question":"What is root_path in the ASGI scope and when does it matter?","answerFull":"root_path is the URL prefix that the reverse proxy strips before forwarding to Uvicorn. If Nginx is configured to proxy https://api.example.com/v1/ to http://localhost:8000/, the root_path is /v1. Without it, FastAPI generates OpenAPI paths starting at /, and the Swagger UI's \"Try it out\" buttons send requests to /items/1 instead of /v1/items/1, failing. Set root_path via app = FastAPI(root_path=\"/v1\") or via Uvicorn's --root-path flag. Alternatively, configure the proxy to pass the X-Forwarded-Prefix header and use Starlette's ProxyHeadersMiddleware.","correct":"root_path is the URL prefix that the reverse proxy strips before forwarding to Uvicorn.","distractors":["A single Uvicorn worker runs one asyncio event loop and handles thousands of concurrent I/O-bound requests efficiently.","FastAPI resolves the path parameter at startup, noting that item_id maps to int.","Starlette is a lightweight ASGI framework providing routing, middleware, request/response abstractions, sessions, background tasks, and test client."]},{"id":"python/fastapi/fastapi_fundamentals_asgi#Q14-2897","section":"python","module":"python/fastapi/fastapi_fundamentals_asgi","moduleName":"fastapi fundamentals asgi","qIndex":14,"difficulty":"advanced","question":"How are path parameters differentiated from query parameters in a FastAPI route signature?","answerFull":"FastAPI determines the source of each parameter at startup. A parameter is a path parameter if its name appears as a {placeholder} in the path string of the route decorator. Every other parameter becomes a query parameter (if it has no default or a non-Body default) or a request body field (if its type is a Pydantic BaseModel with no default). This resolution is purely positional: changing @app.get(\"/items/{item_id}\") to @app.get(\"/items\") turns item_id from a path parameter into a required query parameter with no code change to the function body.","correct":"FastAPI determines the source of each parameter at startup.","distractors":["scope is a plain dict describing the connection — type (http, websocket, lifespan), method, path, headers, and query string.","FastAPI resolves the path parameter at startup, noting that item_id maps to int.","root_path is the URL prefix that the reverse proxy strips before forwarding to Uvicorn."]},{"id":"python/fastapi/fastapi_fundamentals_asgi#Q15-2898","section":"python","module":"python/fastapi/fastapi_fundamentals_asgi","moduleName":"fastapi fundamentals asgi","qIndex":15,"difficulty":"advanced","question":"What is the difference between FileResponse and StreamingResponse for serving large files?","answerFull":"FileResponse uses aiofiles (or anyio) to send a file from disk using the OS's sendfile syscall when available, which copies data from disk to the socket buffer without passing through Python's heap. It also handles Range requests for partial content (required for video seek). StreamingResponse wraps a Python generator and allocates chunks in the Python heap. For files already on disk, FileResponse is more memory-efficient. For dynamically generated content (S3 proxy streaming, LLM token output, live sensor data), use StreamingResponse.","correct":"FileResponse uses aiofiles (or anyio) to send a file from disk using the OS's sendfile syscall when available, which copies data from disk to the socket buffer without passing through Python's heap.","distractors":["Uvicorn runs Python's asyncio event loop in a single thread.","StreamingResponse wraps an async generator (or sync generator run in a thread) that yields bytes or str chunks.","WSGI is a synchronous interface: app(environ, start_response) is a regular Python callable that blocks the server thread until the response is ready."]},{"id":"python/fastapi/fastapi_fundamentals_asgi#Q16-2899","section":"python","module":"python/fastapi/fastapi_fundamentals_asgi","moduleName":"fastapi fundamentals asgi","qIndex":16,"difficulty":"advanced","question":"Why is --workers 1 the recommended default for Uvicorn, and when should you increase it?","answerFull":"A single Uvicorn worker runs one asyncio event loop and handles thousands of concurrent I/O-bound requests efficiently. Adding workers spawns new OS processes, each with its own event loop and memory copy of the application — including ML models, DB connection pools, and in-memory caches. For I/O-bound services, scaling is better achieved horizontally (more pods/containers) via the orchestrator. Increase --workers (or use Gunicorn + UvicornWorker) when the bottleneck is CPU — Python parsing, Pydantic validation of very large payloads, or synchronous business logic — since each extra worker can utilise an additional CPU core independently of the GIL.","correct":"A single Uvicorn worker runs one asyncio event loop and handles thousands of concurrent I/O-bound requests efficiently.","distractors":["Uvicorn runs Python's asyncio event loop in a single thread.","The lifespan context manager was introduced to replace the deprecated event handlers in FastAPI 0.95.","root_path is the URL prefix that the reverse proxy strips before forwarding to Uvicorn."]},{"id":"python/fastapi/http_clients_and_external_apis#Q1-2900","section":"python","module":"python/fastapi/http_clients_and_external_apis","moduleName":"http clients and external apis","qIndex":1,"difficulty":"core","question":"Why is creating httpx.AsyncClient() inside a route handler a production bug, even if it seems to work in tests?","answerFull":"Each instantiation creates a new connection pool with zero pre-warmed connections. At 100 rps, every request incurs a full TCP + TLS handshake (~5-20ms) instead of reusing a keep-alive connection (~0.1ms). At 200 rps the file descriptor limit (1024 by default) is exhausted — OSError: [Errno 24] Too many open files. Tests pass because test throughput is low. Fix by creating one shared client in the FastAPI lifespan context manager.","correct":"Each instantiation creates a new connection pool with zero pre-warmed connections.","distractors":["HTTP/1.1 supports one in-flight request per connection; to achieve 100 concurrent requests you need 100 connections.","max_connections is the hard cap on total open sockets to all hosts combined.","asyncio.create_task schedules background work but does not track the task."]},{"id":"python/fastapi/http_clients_and_external_apis#Q2-2901","section":"python","module":"python/fastapi/http_clients_and_external_apis","moduleName":"http clients and external apis","qIndex":2,"difficulty":"core","question":"What are the four timeout types in httpx.Timeout and what does each guard against?","answerFull":"connect (default 5s) guards the TCP handshake and TLS negotiation. read (default 5s) guards the time waiting for the server to send a response body. write guards the time sending the request body (important for large uploads). pool guards the time waiting to acquire a free connection from the pool when all max_connections slots are occupied. Omitting pool causes indefinite blocking when the pool is saturated.","correct":"connect (default 5s) guards the TCP handshake and TLS negotiation.","distractors":["Use respx for httpx. With respx.mock as a context manager, register routes with respx.get(url).mock(side_effect=[httpx.ConnectError(), httpx.Response(200)]) to simulate a failure on attempt 1 and success on attempt 2.","Each instantiation creates a new connection pool with zero pre-warmed connections.","max_connections is the hard cap on total open sockets to all hosts combined."]},{"id":"python/fastapi/http_clients_and_external_apis#Q3-2902","section":"python","module":"python/fastapi/http_clients_and_external_apis","moduleName":"http clients and external apis","qIndex":3,"difficulty":"core","question":"What is the difference between max_connections and max_keepalive_connections in httpx.Limits?","answerFull":"max_connections is the hard cap on total open sockets to all hosts combined. max_keepalive_connections is the number of idle (keep-alive) connections held in the pool waiting for reuse. Once a request completes, the socket is returned to the keep-alive pool up to this limit; excess sockets are closed. Setting max_keepalive_connections=20 with max_connections=100 means up to 100 simultaneous requests but only 20 idle sockets held open between requests.","correct":"max_connections is the hard cap on total open sockets to all hosts combined.","distractors":["HTTP/1.1 supports one in-flight request per connection; to achieve 100 concurrent requests you need 100 connections.","Use respx for httpx. With respx.mock as a context manager, register routes with respx.get(url).mock(side_effect=[httpx.ConnectError(), httpx.Response(200)]) to simulate a failure on attempt 1 and success on attempt 2.","Choose aiohttp when profiling shows HTTP client throughput is the bottleneck at >200 concurrent outbound connections per process."]},{"id":"python/fastapi/http_clients_and_external_apis#Q5-2903","section":"python","module":"python/fastapi/http_clients_and_external_apis","moduleName":"http clients and external apis","qIndex":5,"difficulty":"core","question":"Explain the three states of a circuit breaker and the transition conditions.","answerFull":"CLOSED (normal operation): all calls pass through, failures are counted. If failures reach the threshold (e.g., 5 in a row), transition to OPEN. OPEN (fail fast): all calls are rejected immediately without touching the downstream. After recovery_timeout seconds, transition to HALF-OPEN. HALF-OPEN (probe): one call is allowed through. If it succeeds, reset to CLOSED and clear failure count. If it fails, return to OPEN and reset the timer. The key design insight is that OPEN protects your service's coroutine pool from being consumed by requests destined to time out.","correct":"CLOSED (normal operation): all calls pass through, failures are counted.","distractors":["String comparison with == in Python is short-circuit: it returns False as soon as the first mismatching byte is found.","Each instantiation creates a new connection pool with zero pre-warmed connections.","connect (default 5s) guards the TCP handshake and TLS negotiation."]},{"id":"python/fastapi/http_clients_and_external_apis#Q6-2904","section":"python","module":"python/fastapi/http_clients_and_external_apis","moduleName":"http clients and external apis","qIndex":6,"difficulty":"intermediate","question":"How do you handle Retry-After headers for 429 responses in an async retry loop?","answerFull":"Read response.headers.get(\"Retry-After\"). The value is either a number of seconds (\"120\") or an HTTP-date string. Parse it, compute the sleep duration (cap it to your maximum wait, e.g., 300s), and await asyncio.sleep(duration) before retrying. If the header is absent, fall back to your standard exponential backoff. Do not retry more than 2-3 times on 429 — if Retry-After specifies 3600s, fail the request rather than blocking a coroutine for an hour.","correct":"Read response.headers.get(\"Retry-After\").","distractors":["Use respx for httpx. With respx.mock as a context manager, register routes with respx.get(url).mock(side_effect=[httpx.ConnectError(), httpx.Response(200)]) to simulate a failure on attempt 1 and success on attempt 2.","Each instantiation creates a new connection pool with zero pre-warmed connections.","connect (default 5s) guards the TCP handshake and TLS negotiation."]},{"id":"python/fastapi/http_clients_and_external_apis#Q7-2905","section":"python","module":"python/fastapi/http_clients_and_external_apis","moduleName":"http clients and external apis","qIndex":7,"difficulty":"intermediate","question":"What is full jitter and why is it preferred over a plain exponential backoff?","answerFull":"Plain exponential backoff: sleep(2 attempt). All clients that fail at the same instant retry at the same intervals, creating a synchronised retry wave that may overwhelm a partially recovered downstream (thundering herd). Full jitter: sleep(random.uniform(0, min(cap, 2 attempt))). Each client picks a random delay within the backoff window, spreading the retry load over time. At 1000 clients retrying attempt=3 (cap=8s), plain backoff generates a spike at t=8s; full jitter spreads those 1000 retries uniformly across [0, 8s].","correct":"Plain exponential backoff: sleep(2 attempt).","distractors":["Use respx for httpx. With respx.mock as a context manager, register routes with respx.get(url).mock(side_effect=[httpx.ConnectError(), httpx.Response(200)]) to simulate a failure on attempt 1 and success on attempt 2.","Each instantiation creates a new connection pool with zero pre-warmed connections.","connect (default 5s) guards the TCP handshake and TLS negotiation."]},{"id":"python/fastapi/http_clients_and_external_apis#Q8-2906","section":"python","module":"python/fastapi/http_clients_and_external_apis","moduleName":"http clients and external apis","qIndex":8,"difficulty":"intermediate","question":"How do you test retry logic and timeout handling without a real server?","answerFull":"Use respx for httpx. With respx.mock as a context manager, register routes with respx.get(url).mock(side_effect=[httpx.ConnectError(), httpx.Response(200)]) to simulate a failure on attempt 1 and success on attempt 2. Verify that the retry function called the URL exactly twice and returned the correct response. For timeout simulation, use httpx.MockTransport with a custom handle_async_request that raises httpx.ReadTimeout. This approach tests retry, backoff calculation, and circuit breaker state without any I/O.","correct":"Use respx for httpx. With respx.mock as a context manager, register routes with respx.get(url).mock(side_effect=[httpx.ConnectError(), httpx.Response(200)]) to simulate a failure on attempt 1 and success on attempt 2.","distractors":["Read response.headers.get(\"Retry-After\").","Plain exponential backoff: sleep(2 attempt).","Choose aiohttp when profiling shows HTTP client throughput is the bottleneck at >200 concurrent outbound connections per process."]},{"id":"python/fastapi/http_clients_and_external_apis#Q9-2907","section":"python","module":"python/fastapi/http_clients_and_external_apis","moduleName":"http clients and external apis","qIndex":9,"difficulty":"intermediate","question":"Why must HMAC webhook verification use hmac.compare_digest instead of ==?","answerFull":"String comparison with == in Python is short-circuit: it returns False as soon as the first mismatching byte is found. The time taken is proportional to the length of the common prefix. An attacker can measure response latency to determine how many leading bytes of their forged signature match the expected value, enabling a timing attack to reconstruct the secret byte-by-byte. hmac.compare_digest always compares all bytes in constant time regardless of where the first mismatch occurs, preventing timing side-channel attacks.","correct":"String comparison with == in Python is short-circuit: it returns False as soon as the first mismatching byte is found.","distractors":["Each instantiation creates a new connection pool with zero pre-warmed connections.","connect (default 5s) guards the TCP handshake and TLS negotiation.","max_connections is the hard cap on total open sockets to all hosts combined."]},{"id":"python/fastapi/http_clients_and_external_apis#Q10-2908","section":"python","module":"python/fastapi/http_clients_and_external_apis","moduleName":"http clients and external apis","qIndex":10,"difficulty":"intermediate","question":"When would you choose aiohttp over httpx for a new service?","answerFull":"Choose aiohttp when profiling shows HTTP client throughput is the bottleneck at >200 concurrent outbound connections per process. aiohttp has ~10-15% lower per-request overhead because it avoids httpx's HTTP/2 negotiation path and has a more direct integration with the event loop's select/epoll calls. Concrete signals: aiohttp benchmark at 500 concurrent fetches shows ~18,000 rps vs httpx ~15,500 rps on the same hardware. For most services (<100 concurrent outbound calls), httpx is preferable because of its sync+async API parity, HTTP/2 support, and first-class FastAPI/Starlette integration.","correct":"Choose aiohttp when profiling shows HTTP client throughput is the bottleneck at >200 concurrent outbound connections per process.","distractors":["max_connections is the hard cap on total open sockets to all hosts combined.","Use respx for httpx. With respx.mock as a context manager, register routes with respx.get(url).mock(side_effect=[httpx.ConnectError(), httpx.Response(200)]) to simulate a failure on attempt 1 and success on attempt 2.","HTTP/1.1 supports one in-flight request per connection; to achieve 100 concurrent requests you need 100 connections."]},{"id":"python/fastapi/http_clients_and_external_apis#Q11-2909","section":"python","module":"python/fastapi/http_clients_and_external_apis","moduleName":"http clients and external apis","qIndex":11,"difficulty":"advanced","question":"How does HTTP/2 multiplexing change connection pool sizing compared to HTTP/1.1?","answerFull":"HTTP/1.1 supports one in-flight request per connection; to achieve 100 concurrent requests you need 100 connections. HTTP/2 multiplexes many streams over a single TCP connection; one connection can carry hundreds of concurrent requests. With httpx and HTTP/2 enabled, you can reduce max_connections significantly (10-20 instead of 100) while maintaining the same concurrency, reducing both memory and OS file descriptor usage. The trade-off is that a TCP-level packet loss event on that single connection stalls all multiplexed streams simultaneously (head-of-line blocking at the TCP layer, which HTTP/3/QUIC addresses with per-stream loss recovery).","correct":"HTTP/1.1 supports one in-flight request per connection; to achieve 100 concurrent requests you need 100 connections.","distractors":["max_connections is the hard cap on total open sockets to all hosts combined.","Choose aiohttp when profiling shows HTTP client throughput is the bottleneck at >200 concurrent outbound connections per process.","Each instantiation creates a new connection pool with zero pre-warmed connections."]},{"id":"python/fastapi/http_clients_and_external_apis#Q12-2910","section":"python","module":"python/fastapi/http_clients_and_external_apis","moduleName":"http clients and external apis","qIndex":12,"difficulty":"advanced","question":"What is the risk of calling asyncio.create_task inside a webhook handler and how do you mitigate it?","answerFull":"asyncio.create_task schedules background work but does not track the task. If the task raises an unhandled exception, it is silently discarded unless a done_callback or asyncio.gather captures it. The task also disappears if the process shuts down mid-execution. Mitigations: (1) store task references — tasks: set[asyncio.Task] = set(); t = asyncio.create_task(fn()); tasks.add(t); t.add_done_callback(tasks.discard), which prevents garbage collection and surfaces exceptions. (2) For durability, publish to a persistent queue (Redis, Kafka) and process with a worker that acknowledges only after success. This survives process restarts.","correct":"asyncio.create_task schedules background work but does not track the task.","distractors":["Each instantiation creates a new connection pool with zero pre-warmed connections.","connect (default 5s) guards the TCP handshake and TLS negotiation.","max_connections is the hard cap on total open sockets to all hosts combined."]},{"id":"python/fastapi/error_handling_and_validation#Q1-2911","section":"python","module":"python/fastapi/error_handling_and_validation","moduleName":"error handling and validation","qIndex":1,"difficulty":"core","question":"What is the difference between HTTPException and a custom domain exception in FastAPI?","answerFull":"HTTPException is a Starlette exception that FastAPI's built-in handler converts directly to an HTTP response — it carries status_code, detail, and headers. A custom domain exception is a plain Python Exception subclass with no HTTP knowledge; you register an @app.exception_handler that translates it to HTTP at the application boundary. Use HTTPException for simple cases in thin route handlers; use custom exceptions when you have a service layer that must remain testable without HTTP.","correct":"HTTPException is a Starlette exception that FastAPI's built-in handler converts directly to an HTTP response — it carries status_code, detail, and headers.","distractors":["Exception handlers are registered inside the router (inner ASGI app).","Starlette resolves handlers by walking the MRO of the raised exception class.","In your exception handler, pass headers to JSONResponse: JSONResponse(status_code=404, content={...}, headers={\"X-Request-ID\": request.state.request_id})."]},{"id":"python/fastapi/error_handling_and_validation#Q2-2912","section":"python","module":"python/fastapi/error_handling_and_validation","moduleName":"error handling and validation","qIndex":2,"difficulty":"core","question":"In what order does Starlette execute exception handlers?","answerFull":"Starlette resolves handlers by walking the MRO of the raised exception class. If UserNotFoundError subclasses AppError, and both have registered handlers, the UserNotFoundError handler fires because it appears first in the MRO. Starlette stores handlers in a plain dict keyed by exception type; the last add_exception_handler call for a given type overwrites the previous one. Register base class handlers first, subclass handlers after.","correct":"Starlette resolves handlers by walking the MRO of the raised exception class.","distractors":["Exception handlers are registered inside the router (inner ASGI app).","HTTPException is a Starlette exception that FastAPI's built-in handler converts directly to an HTTP response — it carries status_code, detail, and headers.","pydantic.ValidationError is not the same class as fastapi.exceptions.RequestValidationError."]},{"id":"python/fastapi/error_handling_and_validation#Q3-2913","section":"python","module":"python/fastapi/error_handling_and_validation","moduleName":"error handling and validation","qIndex":3,"difficulty":"core","question":"What does a Pydantic v2 RequestValidationError look like and how do you reshape it?","answerFull":"Each error in exc.errors() is a dict with type, loc (path tuple starting with \"body\", \"query\", etc.), msg, input, ctx, and url. Override the handler with @app.exception_handler(RequestValidationError), iterate exc.errors(), extract the fields you want, and return a JSONResponse with your own envelope. Always keep loc information so clients know which field failed.","correct":"Each error in exc.errors() is a dict with type, loc (path tuple starting with \"body\", \"query\", etc.), msg, input, ctx, and url.","distractors":["pydantic.ValidationError is not the same class as fastapi.exceptions.RequestValidationError.","FastAPI returns 422 Unprocessable Entity automatically when Pydantic fails to parse or validate the request (missing fields, wrong types, constraint violations).","HTTPException is a Starlette exception that FastAPI's built-in handler converts directly to an HTTP response — it carries status_code, detail, and headers."]},{"id":"python/fastapi/error_handling_and_validation#Q5-2914","section":"python","module":"python/fastapi/error_handling_and_validation","moduleName":"error handling and validation","qIndex":5,"difficulty":"core","question":"Why is it an anti-pattern to raise HTTPException inside a service class?","answerFull":"The service layer becomes coupled to HTTP semantics: it can no longer be instantiated and called in a unit test without a running ASGI app. It cannot be reused in CLI commands, Celery tasks, or WebSocket handlers that may have different error-mapping requirements. The fix is to raise typed domain exceptions in the service and register exception handlers on the FastAPI app to translate them to HTTP responses at the boundary.","correct":"The service layer becomes coupled to HTTP semantics: it can no longer be instantiated and called in a unit test without a running ASGI app.","distractors":["pydantic.ValidationError is not the same class as fastapi.exceptions.RequestValidationError.","Starlette resolves handlers by walking the MRO of the raised exception class.","Exception handlers are registered inside the router (inner ASGI app)."]},{"id":"python/fastapi/error_handling_and_validation#Q6-2915","section":"python","module":"python/fastapi/error_handling_and_validation","moduleName":"error handling and validation","qIndex":6,"difficulty":"intermediate","question":"How do middleware and exception handlers interact in Starlette?","answerFull":"Exception handlers are registered inside the router (inner ASGI app). Middleware wraps the router. If an exception handler converts an exception to a JSONResponse, the middleware sees a normal response and does not see the exception. If all exception handlers re-raise or no handler matches, ServerErrorMiddleware (the outermost Starlette layer, always present) catches it and returns a 500. Adding a catch-all @app.exception_handler(Exception) overrides ServerErrorMiddleware for unhandled exceptions — which is why you must log inside it.","correct":"Exception handlers are registered inside the router (inner ASGI app).","distractors":["Starlette resolves handlers by walking the MRO of the raised exception class.","The service layer becomes coupled to HTTP semantics: it can no longer be instantiated and called in a unit test without a running ASGI app.","In your exception handler, pass headers to JSONResponse: JSONResponse(status_code=404, content={...}, headers={\"X-Request-ID\": request.state.request_id})."]},{"id":"python/fastapi/error_handling_and_validation#Q7-2916","section":"python","module":"python/fastapi/error_handling_and_validation","moduleName":"error handling and validation","qIndex":7,"difficulty":"intermediate","question":"What is the difference between @field_validator and @model_validator in Pydantic v2?","answerFull":"@field_validator validates a single field in isolation; it receives only that field's value. @model_validator(mode=\"after\") validates after all fields are parsed, receiving the entire model instance — use it for cross-field constraints (e.g. end_date > start_date). Field validators run first; if multiple fields fail, all errors are collected before model validators run. A @model_validator(mode=\"before\") receives the raw input dict before any field parsing.","correct":"@field_validator validates a single field in isolation; it receives only that field's value.","distractors":["FastAPI returns 422 Unprocessable Entity automatically when Pydantic fails to parse or validate the request (missing fields, wrong types, constraint violations).","pydantic.ValidationError is not the same class as fastapi.exceptions.RequestValidationError.","HTTPException is a Starlette exception that FastAPI's built-in handler converts directly to an HTTP response — it carries status_code, detail, and headers."]},{"id":"python/fastapi/error_handling_and_validation#Q8-2917","section":"python","module":"python/fastapi/error_handling_and_validation","moduleName":"error handling and validation","qIndex":8,"difficulty":"intermediate","question":"When does FastAPI return 422 vs 400?","answerFull":"FastAPI returns 422 Unprocessable Entity automatically when Pydantic fails to parse or validate the request (missing fields, wrong types, constraint violations). 400 Bad Request must be raised explicitly with HTTPException(status_code=400, ...) or your own domain exception; it is appropriate for semantic errors that Pydantic cannot catch — for example, a discount code that is syntactically valid but has already been used.","correct":"FastAPI returns 422 Unprocessable Entity automatically when Pydantic fails to parse or validate the request (missing fields, wrong types, constraint violations).","distractors":["pydantic.ValidationError is not the same class as fastapi.exceptions.RequestValidationError.","In your exception handler, pass headers to JSONResponse: JSONResponse(status_code=404, content={...}, headers={\"X-Request-ID\": request.state.request_id}).","HTTPException is a Starlette exception that FastAPI's built-in handler converts directly to an HTTP response — it carries status_code, detail, and headers."]},{"id":"python/fastapi/error_handling_and_validation#Q9-2918","section":"python","module":"python/fastapi/error_handling_and_validation","moduleName":"error handling and validation","qIndex":9,"difficulty":"intermediate","question":"How do you add a custom header to all error responses?","answerFull":"In your exception handler, pass headers to JSONResponse: JSONResponse(status_code=404, content={...}, headers={\"X-Request-ID\": request.state.request_id}). For global headers across all errors, use middleware that intercepts the response and adds headers, or override each handler.","correct":"In your exception handler, pass headers to JSONResponse: JSONResponse(status_code=404, content={...}, headers={\"X-Request-ID\": request.state.request_id}).","distractors":["Exception handlers are registered inside the router (inner ASGI app).","Starlette resolves handlers by walking the MRO of the raised exception class.","FastAPI returns 422 Unprocessable Entity automatically when Pydantic fails to parse or validate the request (missing fields, wrong types, constraint violations)."]},{"id":"python/fastapi/error_handling_and_validation#Q12-2919","section":"python","module":"python/fastapi/error_handling_and_validation","moduleName":"error handling and validation","qIndex":12,"difficulty":"advanced","question":"What happens if you raise a pydantic.ValidationError (not RequestValidationError) inside a route handler?","answerFull":"pydantic.ValidationError is not the same class as fastapi.exceptions.RequestValidationError. If raised inside a route handler (e.g., when manually constructing a Pydantic model for a response), it will not be caught by FastAPI's default 422 handler and will propagate as an unhandled exception, resulting in a 500 Internal Server Error. Either catch it inside the handler and convert it to an HTTPException, or register a separate @app.exception_handler(pydantic.ValidationError) handler.","correct":"pydantic.ValidationError is not the same class as fastapi.exceptions.RequestValidationError.","distractors":["HTTPException is a Starlette exception that FastAPI's built-in handler converts directly to an HTTP response — it carries status_code, detail, and headers.","Exception handlers are registered inside the router (inner ASGI app).","Starlette resolves handlers by walking the MRO of the raised exception class."]},{"id":"python/fastapi/websockets_sse_and_streaming#Q1-2920","section":"python","module":"python/fastapi/websockets_sse_and_streaming","moduleName":"websockets sse and streaming","qIndex":1,"difficulty":"core","question":"What happens at the protocol level when a browser opens a WebSocket connection?","answerFull":"The browser sends an HTTP/1.1 GET with Upgrade: websocket, Connection: Upgrade, Sec-WebSocket-Key: <base64-nonce>, and Sec-WebSocket-Version: 13. The server replies 101 Switching Protocols with Sec-WebSocket-Accept: <SHA1 of key + GUID>. After the 101, the TCP socket speaks the WebSocket framing protocol exclusively. Practical implication: WebSockets cannot be initiated from a plain HTTP/2 stream (though HTTP/2 WebSockets via RFC 8441 exist but are rarely deployed in Python stacks as of 2024).","correct":"The browser sends an HTTP/1.1 GET with Upgrade: websocket, Connection: Upgrade, Sec-WebSocket-Key: <base64-nonce>, and Sec-WebSocket-Version: 13.","distractors":["Wrap the WebSocket in a BufferedWebSocket with a bounded asyncio.Queue(maxsize=N).","Three approaches: (a) Application-level heartbeat — server sends {\"type\": \"ping\"} every 30 seconds; client replies {\"type\": \"pong\"}; no pong in 60 seconds → disconnect.","A normal FastAPI JSON response serializes the entire Pydantic model to bytes and sends it in one HTTP response body."]},{"id":"python/fastapi/websockets_sse_and_streaming#Q2-2921","section":"python","module":"python/fastapi/websockets_sse_and_streaming","moduleName":"websockets sse and streaming","qIndex":2,"difficulty":"core","question":"Why does an in-process ConnectionManager fail in a horizontally scaled deployment?","answerFull":"Each pod holds its own list of WebSocket objects in local heap memory. When a message is published — say, a chat message — the pod that receives it broadcasts only to connections on that pod. Connections on other pods receive nothing. Fix: use a shared message bus (Redis pub/sub is the canonical choice). Every pod subscribes to relevant channels; any pod can publish; Redis delivers to all subscribers; each pod then pushes to its local connections.","correct":"Each pod holds its own list of WebSocket objects in local heap memory.","distractors":["The browser sends an HTTP/1.1 GET with Upgrade: websocket, Connection: Upgrade, Sec-WebSocket-Key: <base64-nonce>, and Sec-WebSocket-Version: 13.","Wrap the WebSocket in a BufferedWebSocket with a bounded asyncio.Queue(maxsize=N).","Two approaches: (a) JWT in query parameter — simplest but leaks the token into server access logs."]},{"id":"python/fastapi/websockets_sse_and_streaming#Q3-2922","section":"python","module":"python/fastapi/websockets_sse_and_streaming","moduleName":"websockets sse and streaming","qIndex":3,"difficulty":"core","question":"How do you authenticate a WebSocket connection in FastAPI?","answerFull":"Two approaches: (a) JWT in query parameter — simplest but leaks the token into server access logs. Decoded before websocket.accept() is called; close with code 4001 if invalid. (b) JWT as first message — accept the connection first, await receive_json() with a timeout (5 s), decode the token, close 4001 if invalid. The second approach keeps tokens out of logs and is preferred for production. The browser WebSocket API does not support custom headers, so the Authorization header approach used in REST APIs is not available.","correct":"Two approaches: (a) JWT in query parameter — simplest but leaks the token into server access logs.","distractors":["A normal FastAPI JSON response serializes the entire Pydantic model to bytes and sends it in one HTTP response body.","The browser sends an HTTP/1.1 GET with Upgrade: websocket, Connection: Upgrade, Sec-WebSocket-Key: <base64-nonce>, and Sec-WebSocket-Version: 13.","Each pod holds its own list of WebSocket objects in local heap memory."]},{"id":"python/fastapi/websockets_sse_and_streaming#Q4-2923","section":"python","module":"python/fastapi/websockets_sse_and_streaming","moduleName":"websockets sse and streaming","qIndex":4,"difficulty":"core","question":"What is the difference between SSE and WebSocket, and when would you choose SSE?","answerFull":"SSE is unidirectional (server→client only) over HTTP, auto-reconnects, and works through any HTTP proxy. WebSocket is bidirectional, binary-capable, uses a custom framing protocol, and requires proxy support for the Upgrade handshake. Choose SSE for notification feeds, LLM token streaming, and live dashboards where the client only needs to receive. Choose WebSocket for chat, gaming, and collaborative editing where the client sends messages frequently.","correct":"SSE is unidirectional (server→client only) over HTTP, auto-reconnects, and works through any HTTP proxy.","distractors":["Each pod holds its own list of WebSocket objects in local heap memory.","A normal FastAPI JSON response serializes the entire Pydantic model to bytes and sends it in one HTTP response body.","Nginx buffers proxy responses by default."]},{"id":"python/fastapi/websockets_sse_and_streaming#Q5-2924","section":"python","module":"python/fastapi/websockets_sse_and_streaming","moduleName":"websockets sse and streaming","qIndex":5,"difficulty":"core","question":"What is the StreamingResponse and how does it differ from a normal JSON response?","answerFull":"A normal FastAPI JSON response serializes the entire Pydantic model to bytes and sends it in one HTTP response body. StreamingResponse accepts an async generator; it writes each chunk to the socket as it is yielded, without buffering the full body. This is essential when the total size is unknown at the start (LLM completion, file export) or when reducing time-to-first- byte matters (streaming the first token of an LLM reply in ~100 ms instead of waiting ~5 s for the full completion).","correct":"A normal FastAPI JSON response serializes the entire Pydantic model to bytes and sends it in one HTTP response body.","distractors":["The browser sends an HTTP/1.1 GET with Upgrade: websocket, Connection: Upgrade, Sec-WebSocket-Key: <base64-nonce>, and Sec-WebSocket-Version: 13.","Three approaches: (a) Application-level heartbeat — server sends {\"type\": \"ping\"} every 30 seconds; client replies {\"type\": \"pong\"}; no pong in 60 seconds → disconnect.","SSE is unidirectional (server→client only) over HTTP, auto-reconnects, and works through any HTTP proxy."]},{"id":"python/fastapi/websockets_sse_and_streaming#Q6-2925","section":"python","module":"python/fastapi/websockets_sse_and_streaming","moduleName":"websockets sse and streaming","qIndex":6,"difficulty":"intermediate","question":"How would you implement backpressure for a slow WebSocket consumer?","answerFull":"Wrap the WebSocket in a BufferedWebSocket with a bounded asyncio.Queue(maxsize=N). A dedicated sender task reads from the queue and calls ws.send_text(). The producer calls queue.put_nowait(); if QueueFull is raised, the producer applies backpressure — either drops the message, pauses the upstream source, or disconnects the slow client. Without this, send_text() will internally block waiting for the kernel TCP send buffer to drain, which stalls the event loop task for that connection.","correct":"Wrap the WebSocket in a BufferedWebSocket with a bounded asyncio.Queue(maxsize=N).","distractors":["The browser sends an HTTP/1.1 GET with Upgrade: websocket, Connection: Upgrade, Sec-WebSocket-Key: <base64-nonce>, and Sec-WebSocket-Version: 13.","Each pod holds its own list of WebSocket objects in local heap memory.","Two approaches: (a) JWT in query parameter — simplest but leaks the token into server access logs."]},{"id":"python/fastapi/websockets_sse_and_streaming#Q7-2926","section":"python","module":"python/fastapi/websockets_sse_and_streaming","moduleName":"websockets sse and streaming","qIndex":7,"difficulty":"intermediate","question":"How do you detect and handle stale/zombie WebSocket connections?","answerFull":"Three approaches: (a) Application-level heartbeat — server sends {\"type\": \"ping\"} every 30 seconds; client replies {\"type\": \"pong\"}; no pong in 60 seconds → disconnect. (b) WebSocket protocol ping/pong frames — Starlette does not expose these directly; requires monkey-patching or a lower-level library. (c) Try/except on send_text — catch the exception, remove connection from the manager, cancel the handler task. The heartbeat approach is the most reliable and portable.","correct":"Three approaches: (a) Application-level heartbeat — server sends {\"type\": \"ping\"} every 30 seconds; client replies {\"type\": \"pong\"}; no pong in 60 seconds → disconnect.","distractors":["Wrap the WebSocket in a BufferedWebSocket with a bounded asyncio.Queue(maxsize=N).","Each pod holds its own list of WebSocket objects in local heap memory.","EventSourceResponse from sse-starlette automatically sets Content-Type: text/event-stream, Cache-Control: no-cache, X-Accel-Buffering: no, and the retry: field for client reconnection."]},{"id":"python/fastapi/websockets_sse_and_streaming#Q8-2927","section":"python","module":"python/fastapi/websockets_sse_and_streaming","moduleName":"websockets sse and streaming","qIndex":8,"difficulty":"intermediate","question":"What nginx configuration is required for SSE to work through a reverse proxy?","answerFull":"Nginx buffers proxy responses by default. An SSE stream will appear frozen to the client until the response ends, at which point all events arrive simultaneously. Fix: proxy_buffering off in the nginx location block, and set X-Accel-Buffering: no in the response headers. Similarly, Cloudflare's default buffering must be disabled (set Buffering: no on the origin response). Not setting this is the most common SSE production bug.","correct":"Nginx buffers proxy responses by default.","distractors":["SSE is unidirectional (server→client only) over HTTP, auto-reconnects, and works through any HTTP proxy.","EventSourceResponse from sse-starlette automatically sets Content-Type: text/event-stream, Cache-Control: no-cache, X-Accel-Buffering: no, and the retry: field for client reconnection.","The browser sends an HTTP/1.1 GET with Upgrade: websocket, Connection: Upgrade, Sec-WebSocket-Key: <base64-nonce>, and Sec-WebSocket-Version: 13."]},{"id":"python/fastapi/websockets_sse_and_streaming#Q9-2928","section":"python","module":"python/fastapi/websockets_sse_and_streaming","moduleName":"websockets sse and streaming","qIndex":9,"difficulty":"intermediate","question":"How does the sse-starlette library improve on raw StreamingResponse for SSE?","answerFull":"EventSourceResponse from sse-starlette automatically sets Content-Type: text/event-stream, Cache-Control: no-cache, X-Accel-Buffering: no, and the retry: field for client reconnection. It handles dict-format events ({\"data\": ..., \"event\": ..., \"id\": ...}) and formats them as proper SSE lines. Using raw StreamingResponse requires manual formatting of every event as data: ...\\n\\n, handling the ID and event fields, and setting the headers manually — error-prone boilerplate that sse-starlette eliminates.","correct":"EventSourceResponse from sse-starlette automatically sets Content-Type: text/event-stream, Cache-Control: no-cache, X-Accel-Buffering: no, and the retry: field for client reconnection.","distractors":["SSE is unidirectional (server→client only) over HTTP, auto-reconnects, and works through any HTTP proxy.","Three approaches: (a) Application-level heartbeat — server sends {\"type\": \"ping\"} every 30 seconds; client replies {\"type\": \"pong\"}; no pong in 60 seconds → disconnect.","Starlette detects the broken connection when it tries to read the next frame from the socket."]},{"id":"python/fastapi/websockets_sse_and_streaming#Q10-2929","section":"python","module":"python/fastapi/websockets_sse_and_streaming","moduleName":"websockets sse and streaming","qIndex":10,"difficulty":"intermediate","question":"What happens when a WebSocket client disconnects unexpectedly (network drop, browser close)?","answerFull":"Starlette detects the broken connection when it tries to read the next frame from the socket. The next call to receive_text() / receive_json() raises WebSocketDisconnect. If the handler is currently awaiting, it raises immediately. If the handler is executing synchronous code (e.g., processing a message), the exception is raised at the next await point after the disconnect is detected. Always wrap the receive loop in a try/except WebSocketDisconnect and clean up in a finally block.","correct":"Starlette detects the broken connection when it tries to read the next frame from the socket.","distractors":["Each pod holds its own list of WebSocket objects in local heap memory.","Wrap the WebSocket in a BufferedWebSocket with a bounded asyncio.Queue(maxsize=N).","SSE is unidirectional (server→client only) over HTTP, auto-reconnects, and works through any HTTP proxy."]},{"id":"python/fastapi/websockets_sse_and_streaming#Q11-2930","section":"python","module":"python/fastapi/websockets_sse_and_streaming","moduleName":"websockets sse and streaming","qIndex":11,"difficulty":"advanced","question":"How would you implement a chat room where messages sent to a room are received by all members, even if they are connected to different pods?","answerFull":"Use Redis pub/sub with one channel per room (e.g., chat:room:{room_id}). When a user sends a message, the receiving pod publishes it to that channel with redis.publish(channel, msg). All pods (including the one that published) subscribe to the channel via a long-running redis_listener coroutine. When a message arrives on the subscription, each pod iterates its local ConnectionManager for that room and calls send_text() on each WebSocket. This scales horizontally without sticky sessions.","correct":"Use Redis pub/sub with one channel per room (e.g., chat:room:{room_id}).","distractors":["The browser sends an HTTP/1.1 GET with Upgrade: websocket, Connection: Upgrade, Sec-WebSocket-Key: <base64-nonce>, and Sec-WebSocket-Version: 13.","Each pod holds its own list of WebSocket objects in local heap memory.","Two approaches: (a) JWT in query parameter — simplest but leaks the token into server access logs."]},{"id":"python/fastapi/websockets_sse_and_streaming#Q12-2931","section":"python","module":"python/fastapi/websockets_sse_and_streaming","moduleName":"websockets sse and streaming","qIndex":12,"difficulty":"advanced","question":"What are the HTTP/2 implications for SSE scalability?","answerFull":"HTTP/1.1 browsers cap concurrent connections per domain at 6. Since each SSE stream holds one connection, a single user opening 7 SSE streams on the same domain will queue the 7th. HTTP/2 multiplexes all streams over one TCP connection, removing this limit entirely. Uvicorn supports HTTP/2 via the h2 library (uvicorn --http h2). When SSE runs over HTTP/2, hundreds of SSE streams share a single TCP connection with no browser-side throttling.","correct":"HTTP/1.1 browsers cap concurrent connections per domain at 6.","distractors":["SSE is unidirectional (server→client only) over HTTP, auto-reconnects, and works through any HTTP proxy.","The browser sends an HTTP/1.1 GET with Upgrade: websocket, Connection: Upgrade, Sec-WebSocket-Key: <base64-nonce>, and Sec-WebSocket-Version: 13.","A normal FastAPI JSON response serializes the entire Pydantic model to bytes and sends it in one HTTP response body."]},{"id":"python/fastapi/production_deployment_and_scaling#Q1-2932","section":"python","module":"python/fastapi/production_deployment_and_scaling","moduleName":"production deployment and scaling","qIndex":1,"difficulty":"core","question":"Why should you NOT run Gunicorn inside a Kubernetes pod?","answerFull":"Kubernetes cannot see individual Gunicorn worker processes — it only sees the pod IP. This means HPA scales based on the total pod CPU (which is the sum of all worker CPUs), not per-worker load, making scaling decisions inaccurate. Additionally, readiness and liveness probes hit whichever worker happens to respond, not a reliable single process. In K8s, use one Uvicorn process per pod and let the Deployment replica count plus HPA replace Gunicorn's role.","correct":"Kubernetes cannot see individual Gunicorn worker processes — it only sees the pod IP.","distractors":["preload_app=True loads the FastAPI application module in the Gunicorn master process before forking workers.","--workers 1 instructs Uvicorn to run exactly one worker process per pod.","maxUnavailable=0 means Kubernetes will never terminate an old pod until its replacement is fully ready (passes readinessProbe)."]},{"id":"python/fastapi/production_deployment_and_scaling#Q2-2933","section":"python","module":"python/fastapi/production_deployment_and_scaling","moduleName":"production deployment and scaling","qIndex":2,"difficulty":"core","question":"What is the worker count formula for an I/O-bound FastAPI service, and why?","answerFull":"2 * CPU_count + 1. The constant 2 accounts for the fact that I/O-bound workers spend most of their time waiting on network/DB — two workers per CPU core keep the CPU busy while others wait. The +1 covers scheduling jitter. For a 4-core machine: 9 workers. For CPU-bound work (image encoding, crypto): use CPU_count because adding more workers than cores causes context-switch overhead with no throughput gain.","correct":"2 * CPU_count + 1. The constant 2 accounts for the fact that I/O-bound workers spend most of their time waiting on network/DB — two workers per CPU core keep the CPU busy while others wait.","distractors":["ConfigMap stores non-sensitive key-value pairs (feature flags, log levels, service URLs).","preload_app=True loads the FastAPI application module in the Gunicorn master process before forking workers.","Kubernetes cannot see individual Gunicorn worker processes — it only sees the pod IP."]},{"id":"python/fastapi/production_deployment_and_scaling#Q3-2934","section":"python","module":"python/fastapi/production_deployment_and_scaling","moduleName":"production deployment and scaling","qIndex":3,"difficulty":"core","question":"What does preload_app=True do in Gunicorn, and what is its risk?","answerFull":"preload_app=True loads the FastAPI application module in the Gunicorn master process before forking workers. Each worker inherits a copy-on-write snapshot, saving ~50-100MB per worker. The risk: any file descriptor (DB connection, socket, file handle) opened at module level before the fork is shared across all workers after the fork. Multiple workers writing to the same socket corrupts data. Mitigation: always open DB connections, Redis clients, and similar resources inside lifespan, which runs after forking in each worker's own event loop.","correct":"preload_app=True loads the FastAPI application module in the Gunicorn master process before forking workers.","distractors":["livenessProbe asks \"is this process alive and not deadlocked?\" — failure triggers a pod restart.","Kubernetes cannot see individual Gunicorn worker processes — it only sees the pod IP.","2 * CPU_count + 1. The constant 2 accounts for the fact that I/O-bound workers spend most of their time waiting on network/DB — two workers per CPU core keep the CPU busy while others wait."]},{"id":"python/fastapi/production_deployment_and_scaling#Q4-2935","section":"python","module":"python/fastapi/production_deployment_and_scaling","moduleName":"production deployment and scaling","qIndex":4,"difficulty":"core","question":"Describe the graceful shutdown sequence when Kubernetes sends SIGTERM to a Uvicorn pod.","answerFull":"(1) preStop hook runs (e.g., sleep 5) to let the load balancer drain connections. (2) Kubernetes sends SIGTERM to the pod. (3) Uvicorn stops accepting new connections. (4) In-flight requests continue to process up to --graceful-timeout (default 30s). (5) FastAPI lifespan shutdown block executes: closes DB pool, flushes metrics, cleans up resources. (6) Process exits 0. The terminationGracePeriodSeconds on the pod must be greater than preStop duration + graceful-timeout or Kubernetes sends SIGKILL prematurely.","correct":"(1) preStop hook runs (e.g., sleep 5) to let the load balancer drain connections.","distractors":["Kubernetes cannot see individual Gunicorn worker processes — it only sees the pod IP.","--workers 1 instructs Uvicorn to run exactly one worker process per pod.","maxUnavailable=0 means Kubernetes will never terminate an old pod until its replacement is fully ready (passes readinessProbe)."]},{"id":"python/fastapi/production_deployment_and_scaling#Q5-2936","section":"python","module":"python/fastapi/production_deployment_and_scaling","moduleName":"production deployment and scaling","qIndex":5,"difficulty":"core","question":"What is the difference between readinessProbe, livenessProbe, and startupProbe?","answerFull":"livenessProbe asks \"is this process alive and not deadlocked?\" — failure triggers a pod restart. readinessProbe asks \"is this app ready to serve traffic?\" — failure removes the pod from the Service endpoint list without restarting it. startupProbe grants the application an extended startup budget before liveness begins checking; it prevents liveness from killing a slow-starting pod (e.g., one that loads a large ML model at startup). Once startupProbe succeeds, it stops running and liveness takes over.","correct":"livenessProbe asks \"is this process alive and not deadlocked?\" — failure triggers a pod restart.","distractors":["maxUnavailable=0 means Kubernetes will never terminate an old pod until its replacement is fully ready (passes readinessProbe).","preload_app=True loads the FastAPI application module in the Gunicorn master process before forking workers.","--workers 1 instructs Uvicorn to run exactly one worker process per pod."]},{"id":"python/fastapi/production_deployment_and_scaling#Q6-2937","section":"python","module":"python/fastapi/production_deployment_and_scaling","moduleName":"production deployment and scaling","qIndex":6,"difficulty":"intermediate","question":"How does maxSurge=1, maxUnavailable=0 achieve zero-downtime rolling updates?","answerFull":"maxUnavailable=0 means Kubernetes will never terminate an old pod until its replacement is fully ready (passes readinessProbe). maxSurge=1 allows one extra pod to exist during the rollout (temporarily running replicas + 1 pods). The sequence: start new pod → wait for readiness → terminate one old pod → repeat. This ensures at least replicas pods are serving traffic at all times. The trade-off is a slightly slower rollout and one extra pod's worth of resources during the transition.","correct":"maxUnavailable=0 means Kubernetes will never terminate an old pod until its replacement is fully ready (passes readinessProbe).","distractors":["--workers 1 instructs Uvicorn to run exactly one worker process per pod.","livenessProbe asks \"is this process alive and not deadlocked?\" — failure triggers a pod restart.","Kubernetes cannot see individual Gunicorn worker processes — it only sees the pod IP."]},{"id":"python/fastapi/production_deployment_and_scaling#Q7-2938","section":"python","module":"python/fastapi/production_deployment_and_scaling","moduleName":"production deployment and scaling","qIndex":7,"difficulty":"intermediate","question":"What throughput improvement does uvloop provide, and how do you enable it?","answerFull":"uvloop provides approximately 2-3x throughput improvement on I/O-heavy workloads. In benchmarks: ~28k req/s with the default asyncio event loop vs ~65k req/s with uvloop on a 4-core machine. Enable it with pip install uvicorn[standard], which includes uvloop and httptools; Uvicorn auto-selects uvloop when available. For bare Python scripts: asyncio.set_event_loop_policy(uvloop.EventLoopPolicy()). Limitation: uvloop does not support Windows.","correct":"uvloop provides approximately 2-3x throughput improvement on I/O-heavy workloads.","distractors":["Kubernetes cannot see individual Gunicorn worker processes — it only sees the pod IP.","2 * CPU_count + 1. The constant 2 accounts for the fact that I/O-bound workers spend most of their time waiting on network/DB — two workers per CPU core keep the CPU busy while others wait.","preload_app=True loads the FastAPI application module in the Gunicorn master process before forking workers."]},{"id":"python/fastapi/production_deployment_and_scaling#Q8-2939","section":"python","module":"python/fastapi/production_deployment_and_scaling","moduleName":"production deployment and scaling","qIndex":8,"difficulty":"intermediate","question":"What is the purpose of a multi-stage Dockerfile for a Python service?","answerFull":"A multi-stage Dockerfile separates build-time dependencies (compilers, pip, uv, dev tools) from the runtime image. Stage 1 installs all dependencies and compiles any C extensions. Stage 2 starts from a fresh slim base image and copies only the virtual environment and application source. Result: runtime image contains no build tools, reducing attack surface and image size (typically 180MB vs 900MB). Smaller images reduce K8s pod scheduling time during scale-out events.","correct":"A multi-stage Dockerfile separates build-time dependencies (compilers, pip, uv, dev tools) from the runtime image.","distractors":["ConfigMap stores non-sensitive key-value pairs (feature flags, log levels, service URLs).","2 * CPU_count + 1. The constant 2 accounts for the fact that I/O-bound workers spend most of their time waiting on network/DB — two workers per CPU core keep the CPU busy while others wait.","Kubernetes cannot see individual Gunicorn worker processes — it only sees the pod IP."]},{"id":"python/fastapi/production_deployment_and_scaling#Q9-2940","section":"python","module":"python/fastapi/production_deployment_and_scaling","moduleName":"production deployment and scaling","qIndex":9,"difficulty":"intermediate","question":"Why should the container run as a non-root user?","answerFull":"If an attacker achieves code execution inside the container, a root container maps to UID 0 on the host kernel, enabling potential container escape via kernel vulnerabilities. A non-root user (UID 1001) limits the blast radius: the attacker has no write access to system directories, cannot bind ports below 1024 (bind to 8000+), and cannot escalate privileges. Kubernetes securityContext.runAsNonRoot: true enforces this at the cluster level as a defense-in-depth measure.","correct":"If an attacker achieves code execution inside the container, a root container maps to UID 0 on the host kernel, enabling potential container escape via kernel vulnerabilities.","distractors":["ConfigMap stores non-sensitive key-value pairs (feature flags, log levels, service URLs).","--workers 1 instructs Uvicorn to run exactly one worker process per pod.","Kubernetes cannot see individual Gunicorn worker processes — it only sees the pod IP."]},{"id":"python/fastapi/production_deployment_and_scaling#Q11-2941","section":"python","module":"python/fastapi/production_deployment_and_scaling","moduleName":"production deployment and scaling","qIndex":11,"difficulty":"advanced","question":"How does HPA scale based on a custom RPS metric instead of CPU?","answerFull":"Install the Prometheus adapter (k8s-prometheus-adapter), which queries Prometheus for a metric like http_requests_per_second scraped from your pods' /metrics endpoint. Configure the adapter to expose this as an apiserver custom metrics API. The HPA manifest references it under metrics[].type: Pods with averageValue: \"200\" — meaning scale up when average RPS per pod exceeds 200. This is more accurate than CPU for I/O-bound services where a DB slow query can spike RPS without increasing CPU.","correct":"Install the Prometheus adapter (k8s-prometheus-adapter), which queries Prometheus for a metric like http_requests_per_second scraped from your pods' /metrics endpoint.","distractors":["2 * CPU_count + 1. The constant 2 accounts for the fact that I/O-bound workers spend most of their time waiting on network/DB — two workers per CPU core keep the CPU busy while others wait.","Kubernetes cannot see individual Gunicorn worker processes — it only sees the pod IP.","preload_app=True loads the FastAPI application module in the Gunicorn master process before forking workers."]},{"id":"python/fastapi/production_deployment_and_scaling#Q12-2942","section":"python","module":"python/fastapi/production_deployment_and_scaling","moduleName":"production deployment and scaling","qIndex":12,"difficulty":"advanced","question":"What is the --workers 1 flag for in a K8s Uvicorn command, and what happens if you omit it?","answerFull":"--workers 1 instructs Uvicorn to run exactly one worker process per pod. If omitted, Uvicorn defaults to 1 worker, so the effect is the same — but the flag is explicit documentation of intent. If you pass --workers 4, Uvicorn forks 4 processes, recreating the double-supervision anti-pattern: K8s sees one pod IP but 4 processes serve traffic, HPA cannot measure individual worker load, and probes become unreliable. In K8s, keep --workers 1 and control parallelism through replica count.","correct":"--workers 1 instructs Uvicorn to run exactly one worker process per pod.","distractors":["livenessProbe asks \"is this process alive and not deadlocked?\" — failure triggers a pod restart.","Kubernetes cannot see individual Gunicorn worker processes — it only sees the pod IP.","maxUnavailable=0 means Kubernetes will never terminate an old pod until its replacement is fully ready (passes readinessProbe)."]},{"id":"python/fastapi/production_deployment_and_scaling#Q13-2943","section":"python","module":"python/fastapi/production_deployment_and_scaling","moduleName":"production deployment and scaling","qIndex":13,"difficulty":"advanced","question":"How do ConfigMaps and Secrets differ in Kubernetes, and how do you inject them into FastAPI?","answerFull":"ConfigMap stores non-sensitive key-value pairs (feature flags, log levels, service URLs). Secret stores sensitive data (DB passwords, API keys) encoded as base64 and optionally encrypted at rest with KMS. Both are injected into pods via envFrom (all keys as env vars) or env[].valueFrom (individual keys). In FastAPI, pydantic-settings reads env vars with BaseSettings, providing type validation and .env file fallback for local development. The app code never references ConfigMap or Secret names — it reads from environment variables, keeping it portable. See ../configuration_and_settings_management/README.md for the full BaseSettings pattern.","correct":"ConfigMap stores non-sensitive key-value pairs (feature flags, log levels, service URLs).","distractors":["maxUnavailable=0 means Kubernetes will never terminate an old pod until its replacement is fully ready (passes readinessProbe).","preload_app=True loads the FastAPI application module in the Gunicorn master process before forking workers.","Kubernetes cannot see individual Gunicorn worker processes — it only sees the pod IP."]},{"id":"python/fastapi/configuration_and_settings_management#Q1-2944","section":"python","module":"python/fastapi/configuration_and_settings_management","moduleName":"configuration and settings management","qIndex":1,"difficulty":"core","question":"Why use pydantic-settings instead of calling os.getenv() inline throughout the codebase?","answerFull":"Inline os.getenv() calls scatter configuration reads across the codebase with no central validation, no type coercion, and no startup-time fail-fast guarantee. BaseSettings validates all fields at construction time, so the process refuses to start if any required variable is absent or malformed. This converts a runtime crash mid-request into a deployment failure with a clear error message listing every missing field.","correct":"Inline os.getenv() calls scatter configuration reads across the codebase with no central validation, no type coercion, and no startup-time fail-fast guarantee.","distractors":["By default (case_sensitive=False), pydantic-settings matches APP_DATABASE_URL, app_database_url, and App_Database_Url to the database_url field.","Declare the field as AnyUrl (or PostgresDsn, RedisDsn, etc.) from pydantic.","Pydantic raises a ValidationError at Settings() construction time listing every missing required field."]},{"id":"python/fastapi/configuration_and_settings_management#Q2-2945","section":"python","module":"python/fastapi/configuration_and_settings_management","moduleName":"configuration and settings management","qIndex":2,"difficulty":"core","question":"What does @lru_cache on get_settings() accomplish?","answerFull":"@lru_cache makes get_settings() return the same Settings instance on every subsequent call within the same process. Without it, Settings() would be constructed on each call, re-reading env vars and .env files each time — adding ~2 ms overhead per call and making test overrides fragile. With the cache, pydantic-settings reads and validates configuration exactly once per worker process.","correct":"@lru_cache makes get_settings() return the same Settings instance on every subsequent call within the same process.","distractors":["SecretStr overrides str and repr to return .","Pydantic raises a ValidationError at Settings() construction time listing every missing required field.","Setting env_nested_delimiter=\"\" in SettingsConfigDict allows pydantic-settings to map APP_DBHOST to settings.db.host where db is a nested BaseSettings sub-model."]},{"id":"python/fastapi/configuration_and_settings_management#Q3-2946","section":"python","module":"python/fastapi/configuration_and_settings_management","moduleName":"configuration and settings management","qIndex":3,"difficulty":"core","question":"How do you override settings in tests without modifying environment variables?","answerFull":"Use FastAPI's dependency_overrides dictionary: app.dependency_overrides[get_settings] = lambda: Settings(database_url=\"postgresql://localhost/test\", jwt_secret=\"test\"). The override applies for every request served by the TestClient in that test function. Clear it in teardown with app.dependency_overrides.clear(). This is cleaner than monkeypatch.setenv because it does not mutate the process environment.","correct":"Use FastAPI's dependency_overrides dictionary: app.dependency_overrides[get_settings] = lambda: Settings(database_url=\"postgresql://localhost/test\", jwt_secret=\"test\").","distractors":["Pydantic raises a ValidationError at Settings() construction time listing every missing required field.","By default (case_sensitive=False), pydantic-settings matches APP_DATABASE_URL, app_database_url, and App_Database_Url to the database_url field.","Setting env_nested_delimiter=\"\" in SettingsConfigDict allows pydantic-settings to map APP_DBHOST to settings.db.host where db is a nested BaseSettings sub-model."]},{"id":"python/fastapi/configuration_and_settings_management#Q4-2947","section":"python","module":"python/fastapi/configuration_and_settings_management","moduleName":"configuration and settings management","qIndex":4,"difficulty":"core","question":"What is the priority order when pydantic-settings resolves a field value?","answerFull":"From highest to lowest: (1) environment variable, (2) last .env file in the configured list (later files override earlier ones), (3) secrets_dir file, (4) field default. A value found at a higher priority layer completely replaces a lower-priority value — there is no merging of list or dict values across layers.","correct":"From highest to lowest: (1) environment variable, (2) last .env file in the configured list (later files override earlier ones), (3) secrets_dir file, (4) field default.","distractors":["Declare the field as AnyUrl (or PostgresDsn, RedisDsn, etc.) from pydantic.","@lru_cache makes get_settings() return the same Settings instance on every subsequent call within the same process.","Use FastAPI's dependency_overrides dictionary: app.dependency_overrides[get_settings] = lambda: Settings(database_url=\"postgresql://localhost/test\", jwt_secret=\"test\")."]},{"id":"python/fastapi/configuration_and_settings_management#Q5-2948","section":"python","module":"python/fastapi/configuration_and_settings_management","moduleName":"configuration and settings management","qIndex":5,"difficulty":"core","question":"Why use SecretStr instead of str for sensitive fields?","answerFull":"SecretStr overrides str and repr to return . This means the value never appears in log output, exception messages, or model_dump() by default. The raw value is only accessible via .get_secret_value(), making it explicit in code reviews wherever the plaintext is exposed.","correct":"SecretStr overrides str and repr to return .","distractors":["@lru_cache makes get_settings() return the same Settings instance on every subsequent call within the same process.","By default (case_sensitive=False), pydantic-settings matches APP_DATABASE_URL, app_database_url, and App_Database_Url to the database_url field.","Use FastAPI's dependency_overrides dictionary: app.dependency_overrides[get_settings] = lambda: Settings(database_url=\"postgresql://localhost/test\", jwt_secret=\"test\")."]},{"id":"python/fastapi/configuration_and_settings_management#Q6-2949","section":"python","module":"python/fastapi/configuration_and_settings_management","moduleName":"configuration and settings management","qIndex":6,"difficulty":"intermediate","question":"How do you implement per-environment configuration without environment-specific subclasses?","answerFull":"Pass a tuple of .env file paths to env_file in SettingsConfigDict. The last file in the tuple takes precedence. Set an ENV environment variable to control which overlay is loaded: env_file=(\".env\", f\".env.{os.getenv('ENV', 'development')}\"). Production typically sets no .env overlay at all — all values come from environment variables injected by the deployment platform.","correct":"Pass a tuple of .env file paths to env_file in SettingsConfigDict.","distractors":["Inline os.getenv() calls scatter configuration reads across the codebase with no central validation, no type coercion, and no startup-time fail-fast guarantee.","Setting env_nested_delimiter=\"\" in SettingsConfigDict allows pydantic-settings to map APP_DBHOST to settings.db.host where db is a nested BaseSettings sub-model.","Set secrets_dir in SettingsConfigDict to the mount path (e.g., /run/secrets)."]},{"id":"python/fastapi/configuration_and_settings_management#Q7-2950","section":"python","module":"python/fastapi/configuration_and_settings_management","moduleName":"configuration and settings management","qIndex":7,"difficulty":"intermediate","question":"What is the double-underscore delimiter for nested settings?","answerFull":"Setting env_nested_delimiter=\"\" in SettingsConfigDict allows pydantic-settings to map APP_DBHOST to settings.db.host where db is a nested BaseSettings sub-model. Without this, nested models must be provided as JSON strings, which is error-prone. The delimiter works for arbitrarily deep nesting: APP_DBREPLICAHOST → settings.db.replica.host.","correct":"Setting env_nested_delimiter=\"\" in SettingsConfigDict allows pydantic-settings to map APP_DBHOST to settings.db.host where db is a nested BaseSettings sub-model.","distractors":["Pass a tuple of .env file paths to env_file in SettingsConfigDict.","Pydantic raises a ValidationError at Settings() construction time listing every missing required field.","Declare the field as AnyUrl (or PostgresDsn, RedisDsn, etc.) from pydantic."]},{"id":"python/fastapi/configuration_and_settings_management#Q8-2951","section":"python","module":"python/fastapi/configuration_and_settings_management","moduleName":"configuration and settings management","qIndex":8,"difficulty":"intermediate","question":"How do you handle secrets mounted as files (Kubernetes Secrets / Docker secrets)?","answerFull":"Set secrets_dir in SettingsConfigDict to the mount path (e.g., /run/secrets). Pydantic-settings reads the content of /run/secrets/<field_name> as the field value, stripping trailing whitespace. This is the Kubernetes-native pattern for consuming Secret objects without exposing their values as environment variables (which are visible in kubectl describe pod output).","correct":"Set secrets_dir in SettingsConfigDict to the mount path (e.g., /run/secrets).","distractors":["Setting env_nested_delimiter=\"\" in SettingsConfigDict allows pydantic-settings to map APP_DBHOST to settings.db.host where db is a nested BaseSettings sub-model.","Pass a tuple of .env file paths to env_file in SettingsConfigDict.","From highest to lowest: (1) environment variable, (2) last .env file in the configured list (later files override earlier ones), (3) secrets_dir file, (4) field default."]},{"id":"python/fastapi/configuration_and_settings_management#Q9-2952","section":"python","module":"python/fastapi/configuration_and_settings_management","moduleName":"configuration and settings management","qIndex":9,"difficulty":"intermediate","question":"What happens if a required field has no value from any source?","answerFull":"Pydantic raises a ValidationError at Settings() construction time listing every missing required field. Because get_settings() is typically called at startup (via a lifespan event or the first request), the process fails before serving any traffic. This is the desired fail-fast behaviour compared to a KeyError on the first database call minutes later.","correct":"Pydantic raises a ValidationError at Settings() construction time listing every missing required field.","distractors":["From highest to lowest: (1) environment variable, (2) last .env file in the configured list (later files override earlier ones), (3) secrets_dir file, (4) field default.","Use FastAPI's dependency_overrides dictionary: app.dependency_overrides[get_settings] = lambda: Settings(database_url=\"postgresql://localhost/test\", jwt_secret=\"test\").","By default (case_sensitive=False), pydantic-settings matches APP_DATABASE_URL, app_database_url, and App_Database_Url to the database_url field."]},{"id":"python/fastapi/configuration_and_settings_management#Q10-2953","section":"python","module":"python/fastapi/configuration_and_settings_management","moduleName":"configuration and settings management","qIndex":10,"difficulty":"intermediate","question":"How do you validate that a settings field is a valid URL?","answerFull":"Declare the field as AnyUrl (or PostgresDsn, RedisDsn, etc.) from pydantic. Pydantic v2 validates the URL scheme, host, and structure at construction time. PostgresDsn additionally checks that the scheme is postgresql:// or postgresql+asyncpg://, providing a more specific error than a bare string field would.","correct":"Declare the field as AnyUrl (or PostgresDsn, RedisDsn, etc.) from pydantic.","distractors":["From highest to lowest: (1) environment variable, (2) last .env file in the configured list (later files override earlier ones), (3) secrets_dir file, (4) field default.","@lru_cache makes get_settings() return the same Settings instance on every subsequent call within the same process.","By default (case_sensitive=False), pydantic-settings matches APP_DATABASE_URL, app_database_url, and App_Database_Url to the database_url field."]},{"id":"python/fastapi/configuration_and_settings_management#Q11-2954","section":"python","module":"python/fastapi/configuration_and_settings_management","moduleName":"configuration and settings management","qIndex":11,"difficulty":"advanced","question":"How do you expose a subset of settings to the OpenAPI schema without leaking secrets?","answerFull":"Implement a PublicSettings response model with only the non-sensitive fields (e.g., debug, log_level, allowed_origins). Populate it from the full Settings object in a /config diagnostic endpoint gated behind an admin scope. Never return SecretStr fields in any HTTP response — even if they render as , the field name itself signals what secrets exist.","correct":"Implement a PublicSettings response model with only the non-sensitive fields (e.g., debug, log_level, allowed_origins).","distractors":["Setting env_nested_delimiter=\"\" in SettingsConfigDict allows pydantic-settings to map APP_DBHOST to settings.db.host where db is a nested BaseSettings sub-model.","Pydantic raises a ValidationError at Settings() construction time listing every missing required field.","From highest to lowest: (1) environment variable, (2) last .env file in the configured list (later files override earlier ones), (3) secrets_dir file, (4) field default."]},{"id":"python/fastapi/configuration_and_settings_management#Q12-2955","section":"python","module":"python/fastapi/configuration_and_settings_management","moduleName":"configuration and settings management","qIndex":12,"difficulty":"advanced","question":"What is the difference between model_config case_sensitive=True and the default?","answerFull":"By default (case_sensitive=False), pydantic-settings matches APP_DATABASE_URL, app_database_url, and App_Database_Url to the database_url field. With case_sensitive=True, only the exact casing matches. Linux environment variables are case-sensitive by default, so case_sensitive=False (the default) is safer on Linux production deployments and avoids a common deployment gotcha where the env var is set in uppercase but the field name is lowercase.","correct":"By default (case_sensitive=False), pydantic-settings matches APP_DATABASE_URL, app_database_url, and App_Database_Url to the database_url field.","distractors":["Declare the field as AnyUrl (or PostgresDsn, RedisDsn, etc.) from pydantic.","Pydantic raises a ValidationError at Settings() construction time listing every missing required field.","Setting env_nested_delimiter=\"\" in SettingsConfigDict allows pydantic-settings to map APP_DBHOST to settings.db.host where db is a nested BaseSettings sub-model."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q1-2956","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":1,"difficulty":"core","question":"How does FastAPI resolve dependencies at runtime?","answerFull":"FastAPI builds a Dependant graph at startup by recursively inspecting Depends() markers using inspect.signature() and typing.get_type_hints(). At request time, solve_dependencies() does a depth-first traversal of this pre-built graph, executing each node in topological order. Results are cached in a per-request dict keyed by callable identity so each dependency runs at most once per request. This means the reflection cost is paid at startup, not on every request.","correct":"FastAPI builds a Dependant graph at startup by recursively inspecting Depends() markers using inspect.signature() and typing.get_type_hints().","distractors":["When you write param: MyClass = Depends() (or Depends(MyClass)), FastAPI treats the class itself as the callable.","At app startup, FastAPI inspects each dependency using inspect.isgeneratorfunction() and inspect.isasyncgenfunction().","use_cache=False passed to Depends tells FastAPI to bypass the per-request cache and call the dependency function again, creating a fresh instance."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q2-2957","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":2,"difficulty":"core","question":"What is a yield dependency and when does teardown run?","answerFull":"A yield dependency is a generator function (sync or async) that performs setup before yield, produces the injected value at yield, and performs teardown after yield (typically in a finally block). Teardown runs after the HTTP response has been sent to the client — not before. FastAPI collects all yield dependency generators on a cleanup stack and drains it in LIFO order once the response is committed. This means teardown cannot modify the response, but it is guaranteed to run even if the handler raised an exception.","correct":"A yield dependency is a generator function (sync or async) that performs setup before yield, produces the injected value at yield, and performs teardown after yield (typically in a finally block).","distractors":["FastAPI wraps the generator in an asynccontextmanager equivalent and drives teardown in a finally block.","Use a yield dependency: create a SessionLocal() in the setup phase, yield it to the handler, and call db.close() in a finally block after the yield.","A yield dependency is a generator function used with Depends; code before yield runs during setup and code after yield runs during teardown after the response is sent."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q3-2958","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":3,"difficulty":"core","question":"What is the difference between Depends(fn) and Depends(fn, use_cache=False)?","answerFull":"By default, Depends(fn) caches the result of fn for the lifetime of the request. All sub-dependencies and the main handler that reference the same callable receive the same object. Depends(fn, use_cache=False) bypasses this cache and calls fn fresh every time it appears in the dependency graph for that request. Use use_cache=False for dependencies whose result must be unique per call site — nonces, timestamps, or idempotency-key generators. Use the default caching for DB sessions, auth tokens, and any resource that should be shared within a request.","correct":"By default, Depends(fn) caches the result of fn for the lifetime of the request.","distractors":["FastAPI builds a Dependant graph at startup by recursively inspecting Depends() markers using inspect.signature() and typing.get_type_hints().","FastAPI caches the result of each dependency callable keyed by the function object within a single request.","Depends() creates a new resource instance per request (or per invocation if use_cache=False)."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q4-2959","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":4,"difficulty":"core","question":"How do you inject a DB session per request and ensure cleanup?","answerFull":"Use a yield dependency: create a SessionLocal() in the setup phase, yield it to the handler, and call db.close() in a finally block after the yield. This guarantees the session is closed after every request, regardless of whether the handler succeeds or raises. Rollback on exception should also be added before re-raising so the connection returns to the pool in a clean state. Register with Depends(get_db) on any route that needs DB access.","correct":"Use a yield dependency: create a SessionLocal() in the setup phase, yield it to the handler, and call db.close() in a finally block after the yield.","distractors":["By default, FastAPI caches the result of each dependency callable once per request.","A yield dependency is a generator function (sync or async) that performs setup before yield, produces the injected value at yield, and performs teardown after yield (typically in a finally block).","use_cache=False passed to Depends tells FastAPI to bypass the per-request cache and call the dependency function again, creating a fresh instance."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q5-2960","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":5,"difficulty":"core","question":"How do class-based dependencies work?","answerFull":"When you write param: MyClass = Depends() (or Depends(MyClass)), FastAPI treats the class itself as the callable. It inspects MyClass.init for parameters, resolves them the same way as a function's parameters (including nested Depends() in init), then instantiates the class. The instance is injected into the route handler. Class-based dependencies are ideal for grouping related query parameters (pagination, filters) or for callable classes that need to hold configuration injected at init time.","correct":"When you write param: MyClass = Depends() (or Depends(MyClass)), FastAPI treats the class itself as the callable.","distractors":["FastAPI builds a Dependant graph at startup by recursively inspecting Depends() markers using inspect.signature() and typing.get_type_hints().","Pass dependencies=[Depends(auth_dep)] to APIRouter(...).","Use app.dependency_overrides to replace the original callable with a test generator."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q6-2961","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":6,"difficulty":"intermediate","question":"How does dependency_overrides work in testing?","answerFull":"app.dependency_overrides is a plain dict mapping a callable to a replacement callable. When FastAPI resolves Depends(original_fn) and finds original_fn in dependency_overrides, it calls the replacement instead. This requires no monkey-patching of modules or mocking frameworks. The override must be reset to {} after each test to prevent state leakage. The standard pattern is a pytest yield fixture that sets the override, yields the TestClient, then clears the dict in the fixture's teardown.","correct":"app.dependency_overrides is a plain dict mapping a callable to a replacement callable.","distractors":["A yield dependency is a generator function (sync or async) that performs setup before yield, produces the injected value at yield, and performs teardown after yield (typically in a finally block).","Exceptions raised in the finally block of a yield dependency propagate and can mask the original exception from the handler.","When you write param: MyClass = Depends() (or Depends(MyClass)), FastAPI treats the class itself as the callable."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q7-2962","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":7,"difficulty":"intermediate","question":"How do you add authentication to all routes in a router without repeating it per endpoint?","answerFull":"Pass dependencies=[Depends(auth_dep)] to APIRouter(...). Every route registered on that router will execute auth_dep before the handler, as if it had been declared on each endpoint individually. The dependency can raise HTTPException to abort the request. Router-level dependencies do not need to return a value that the handler consumes; they are commonly used for side-effectful checks like auth, rate limiting, or IP allowlisting.","correct":"Pass dependencies=[Depends(auth_dep)] to APIRouter(...).","distractors":["Depends() creates a new resource instance per request (or per invocation if use_cache=False).","When you write param: MyClass = Depends() (or Depends(MyClass)), FastAPI treats the class itself as the callable.","FastAPI runs sync dependencies in a thread pool executor (using anyio.to_thread.run_sync) and async dependencies on the event loop."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q8-2963","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":8,"difficulty":"intermediate","question":"What is the teardown order when multiple yield dependencies are active in a single request?","answerFull":"Teardown executes in LIFO (last-in, first-out) order, mirroring the nesting semantics of Python's contextlib.ExitStack. If route handler declares a = Depends(dep_a) followed by b = Depends(dep_b), setup runs: dep_a then dep_b. Teardown runs: dep_b first, then dep_a. This is important for resource dependencies — if dep_b holds a lock that dep_a's teardown needs, the LIFO order ensures dep_b releases the lock before dep_a attempts to use it.","correct":"Teardown executes in LIFO (last-in, first-out) order, mirroring the nesting semantics of Python's contextlib.ExitStack.","distractors":["FastAPI propagates it. If teardown raises a different exception than the one thrown in, the new exception replaces the original.","Use a yield dependency: create a SessionLocal() in the setup phase, yield it to the handler, and call db.close() in a finally block after the yield.","A yield dependency is a generator function (sync or async) that performs setup before yield, produces the injected value at yield, and performs teardown after yield (typically in a finally block)."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q9-2964","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":9,"difficulty":"intermediate","question":"How does Security() differ from Depends()?","answerFull":"Security() is a subclass of Depends() that accepts an additional scopes keyword argument (a list of OAuth2 scope strings). At runtime, FastAPI injects a SecurityScopes object as a special parameter to the security dependency function, carrying the scopes declared at the call site. This populates the OpenAPI schema with the correct security requirements for each endpoint. Functionally, Security() behaves identically to Depends() — if you do not need scopes to appear in generated docs, using Depends() for auth functions is fine.","correct":"Security() is a subclass of Depends() that accepts an additional scopes keyword argument (a list of OAuth2 scope strings).","distractors":["Pass dependencies=[Depends(auth_dep)] to APIRouter(...).","By default, Depends(fn) caches the result of fn for the lifetime of the request.","When you write param: MyClass = Depends() (or Depends(MyClass)), FastAPI treats the class itself as the callable."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q10-2965","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":10,"difficulty":"intermediate","question":"How do you handle exceptions in a yield dependency's teardown?","answerFull":"Exceptions raised in the finally block of a yield dependency propagate and can mask the original exception from the handler. Best practice: catch and log in finally, never re-raise unless you intend to replace the original error. For DB sessions, swallow all teardown errors after logging since the session close may fail if the connection is already broken. Use contextlib.suppress or explicit try/except inside finally for robust teardown.","correct":"Exceptions raised in the finally block of a yield dependency propagate and can mask the original exception from the handler.","distractors":["A yield dependency is a generator function (sync or async) that performs setup before yield, produces the injected value at yield, and performs teardown after yield (typically in a finally block).","FastAPI propagates it. If teardown raises a different exception than the one thrown in, the new exception replaces the original.","Teardown executes in LIFO (last-in, first-out) order, mirroring the nesting semantics of Python's contextlib.ExitStack."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q11-2966","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":11,"difficulty":"advanced","question":"What is the difference between Depends and app.on_event(\"startup\") (or lifespan)?","answerFull":"Depends() creates a new resource instance per request (or per invocation if use_cache=False). lifespan (the modern replacement for on_event) creates a resource once for the lifetime of the application process. Use lifespan for expensive singleton resources: DB engine, connection pool, ML model, HTTP client session. Use Depends() for per-request resources: individual DB sessions checked out from the pool, per-request tokens, and context objects derived from the incoming request.","correct":"Depends() creates a new resource instance per request (or per invocation if use_cache=False).","distractors":["When you write param: MyClass = Depends() (or Depends(MyClass)), FastAPI treats the class itself as the callable.","The instance is bound to the request's dependency resolution context.","Pass dependencies=[Depends(auth_dep)] to APIRouter(...)."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q12-2967","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":12,"difficulty":"advanced","question":"Can a dependency raise an HTTPException? What happens?","answerFull":"Yes. An HTTPException raised inside a dependency short-circuits the resolution chain. FastAPI's exception handlers catch it, and the route handler never executes. This is the standard pattern for auth guards: get_current_user raises 401 if the token is missing or invalid, and the protected handler is never called. All yield dependency teardowns that have already been set up still run in LIFO order before the error response is sent.","correct":"Yes. An HTTPException raised inside a dependency short-circuits the resolution chain.","distractors":["By default, FastAPI caches the result of each dependency callable once per request.","Dependency resolution adds approximately 0.05 ms per dependency per request for async generator deps.","Router-level dependencies are attached to the APIRouter and appear in the route's dependency list the same way per-endpoint dependencies do."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q13-2968","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":13,"difficulty":"advanced","question":"How do you share a single DB connection across nested dependencies?","answerFull":"By default, FastAPI caches the result of each dependency callable once per request. If get_db is declared at the innermost level and multiple sub-dependencies all declare Depends(get_db), they all receive the same Session object. This is the correct behavior — one transaction per request, shared across all data-access operations. No additional coordination is needed as long as all dependencies reference the same callable object.","correct":"By default, FastAPI caches the result of each dependency callable once per request.","distractors":["FastAPI caches the result of each dependency callable keyed by the function object within a single request.","app.dependency_overrides is a plain dict mapping a callable to a replacement callable.","FastAPI runs sync dependencies in a thread pool executor (using anyio.to_thread.run_sync) and async dependencies on the event loop."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q14-2969","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":14,"difficulty":"advanced","question":"What happens if you declare a sync yield dependency but the route is async?","answerFull":"FastAPI runs sync dependencies in a thread pool executor (using anyio.to_thread.run_sync) and async dependencies on the event loop. You can freely mix sync and async dependencies in the same DAG. FastAPI handles the thread-pool/event-loop boundary transparently. The one constraint: a sync dependency cannot await — it must be either fully sync or fully async.","correct":"FastAPI runs sync dependencies in a thread pool executor (using anyio.to_thread.run_sync) and async dependencies on the event loop.","distractors":["A yield dependency is a generator function (sync or async) that performs setup before yield, produces the injected value at yield, and performs teardown after yield (typically in a finally block).","By default, FastAPI caches the result of each dependency callable once per request.","Exceptions raised in the finally block of a yield dependency propagate and can mask the original exception from the handler."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q16-2970","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":16,"difficulty":"advanced","question":"How do you test a route that has a router-level dependency?","answerFull":"Router-level dependencies are attached to the APIRouter and appear in the route's dependency list the same way per-endpoint dependencies do. dependency_overrides applies to them identically: app.dependency_overrides[router_dep_fn] = mock_fn. There is no separate mechanism needed for router vs endpoint dependencies.","correct":"Router-level dependencies are attached to the APIRouter and appear in the route's dependency list the same way per-endpoint dependencies do.","distractors":["A yield dependency is a generator function (sync or async) that performs setup before yield, produces the injected value at yield, and performs teardown after yield (typically in a finally block).","Use app.dependency_overrides to replace the original callable with a test generator.","app.dependency_overrides is a plain dict mapping a callable to a replacement callable."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q1-2971","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":1,"difficulty":"core","question":"What is a yield dependency in FastAPI and how does it differ from a plain Depends?","answerFull":"A yield dependency is a generator function used with Depends; code before yield runs during setup and code after yield runs during teardown after the response is sent. A plain Depends callable just returns a value with no teardown phase. Use yield whenever the resource requires cleanup — DB sessions, file handles, HTTP clients.","correct":"A yield dependency is a generator function used with Depends; code before yield runs during setup and code after yield runs during teardown after the response is sent.","distractors":["Use a yield dependency: create a SessionLocal() in the setup phase, yield it to the handler, and call db.close() in a finally block after the yield.","FastAPI caches the result of each dependency callable keyed by the function object within a single request.","use_cache=False passed to Depends tells FastAPI to bypass the per-request cache and call the dependency function again, creating a fresh instance."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q2-2972","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":2,"difficulty":"core","question":"How does FastAPI guarantee teardown even when the route handler raises an exception?","answerFull":"FastAPI wraps the generator in an asynccontextmanager equivalent and drives teardown in a finally block. If the handler raises, FastAPI calls generator.throw(exc) which resumes execution inside the generator's except/finally block. The teardown code always runs as long as it is inside finally:.","correct":"FastAPI wraps the generator in an asynccontextmanager equivalent and drives teardown in a finally block.","distractors":["A yield dependency is a generator function used with Depends; code before yield runs during setup and code after yield runs during teardown after the response is sent.","FastAPI propagates it. If teardown raises a different exception than the one thrown in, the new exception replaces the original.","FastAPI drives the generator with next() (or send()) once to get the yielded value."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q3-2973","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":3,"difficulty":"core","question":"What happens if you raise an exception inside the teardown of a yield dependency?","answerFull":"FastAPI propagates it. If teardown raises a different exception than the one thrown in, the new exception replaces the original. This means careless teardown can mask handler errors. Always log and re-raise or use finally to keep teardown non-raising.","correct":"FastAPI propagates it. If teardown raises a different exception than the one thrown in, the new exception replaces the original.","distractors":["A yield dependency is a generator function (sync or async) that performs setup before yield, produces the injected value at yield, and performs teardown after yield (typically in a finally block).","FastAPI wraps the generator in an asynccontextmanager equivalent and drives teardown in a finally block.","By default, FastAPI caches the result of each dependency callable once per request."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q4-2974","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":4,"difficulty":"core","question":"Explain the caching behavior of yield dependencies. When is a dependency called more than once?","answerFull":"FastAPI caches the result of each dependency callable keyed by the function object within a single request. If three route parameters all declare Depends(get_db), get_db() is called once; all three receive the same session. To force a new instance, pass use_cache=False to the second Depends call.","correct":"FastAPI caches the result of each dependency callable keyed by the function object within a single request.","distractors":["use_cache=False passed to Depends tells FastAPI to bypass the per-request cache and call the dependency function again, creating a fresh instance.","A yield dependency is a generator function used with Depends; code before yield runs during setup and code after yield runs during teardown after the response is sent.","By default, FastAPI caches the result of each dependency callable once per request."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q5-2975","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":5,"difficulty":"core","question":"What is the teardown order for nested yield dependencies?","answerFull":"LIFO — innermost first, outermost last. FastAPI builds a DAG at startup. At teardown, it tears down leaves before roots. If A depends on B which depends on C, teardown order is C → B → A.","correct":"LIFO — innermost first, outermost last.","distractors":["A yield dependency is a generator function used with Depends; code before yield runs during setup and code after yield runs during teardown after the response is sent.","FastAPI propagates it. If teardown raises a different exception than the one thrown in, the new exception replaces the original.","Exceptions raised in the finally block of a yield dependency propagate and can mask the original exception from the handler."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q6-2976","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":6,"difficulty":"intermediate","question":"Why should connection pools live in lifespan rather than in a yield dependency?","answerFull":"Yield dependencies are request-scoped. Placing a pool in a yield dep recreates it on every request — an asyncpg pool takes ~500ms to create and opens 5–20 TCP connections. Under load this exhausts OS file descriptors and adds hundreds of milliseconds to request latency. lifespan creates the pool once at startup and shares it across all requests.","correct":"Yield dependencies are request-scoped.","distractors":["use_cache=False passed to Depends tells FastAPI to bypass the per-request cache and call the dependency function again, creating a fresh instance.","FastAPI caches the result of each dependency callable keyed by the function object within a single request.","A yield dependency is a generator function used with Depends; code before yield runs during setup and code after yield runs during teardown after the response is sent."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q7-2977","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":7,"difficulty":"intermediate","question":"How do you test a route that uses a yield dependency?","answerFull":"Use app.dependency_overrides to replace the original callable with a test generator. The override must also be a generator function if teardown assertions matter. Clean up with app.dependency_overrides.clear() in a pytest fixture teardown. python import pytest from fastapi.testclient import TestClient @pytest.fixture def client(app: FastAPI, db_session: AsyncSession): def override_get_db(): yield db_session # Use the test session app.dependency_overrides[get_db] = override_get_db with TestClient(app) as c: yield c app.dependency_overrides.clear()","correct":"Use app.dependency_overrides to replace the original callable with a test generator.","distractors":["By default, FastAPI caches the result of each dependency callable once per request.","FastAPI caches the result of each dependency callable keyed by the function object within a single request.","Use a yield dependency: create a SessionLocal() in the setup phase, yield it to the handler, and call db.close() in a finally block after the yield."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q8-2978","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":8,"difficulty":"intermediate","question":"Can a yield dependency be synchronous? What are the implications?","answerFull":"Yes. A sync generator function (def get_db(): yield session) works with FastAPI. FastAPI wraps it in a contextmanager instead of asynccontextmanager. The limitation is that blocking I/O in the sync generator (e.g., opening a psycopg2 connection) blocks the event loop. For async apps, prefer async generator functions.","correct":"Yes. A sync generator function (def get_db(): yield session) works with FastAPI.","distractors":["FastAPI drives the generator with next() (or send()) once to get the yielded value.","A yield dependency is a generator function (sync or async) that performs setup before yield, produces the injected value at yield, and performs teardown after yield (typically in a finally block).","A yield dependency is a generator function used with Depends; code before yield runs during setup and code after yield runs during teardown after the response is sent."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q9-2979","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":9,"difficulty":"intermediate","question":"What is use_cache=False and when would you use it?","answerFull":"use_cache=False passed to Depends tells FastAPI to bypass the per-request cache and call the dependency function again, creating a fresh instance. Use it when a route genuinely needs two independent instances — e.g., a transfer endpoint that requires two separate DB sessions for debit and credit operations so each can commit or roll back independently.","correct":"use_cache=False passed to Depends tells FastAPI to bypass the per-request cache and call the dependency function again, creating a fresh instance.","distractors":["By default, FastAPI caches the result of each dependency callable once per request.","FastAPI builds a Dependant graph at startup by recursively inspecting Depends() markers using inspect.signature() and typing.get_type_hints().","When you write param: MyClass = Depends() (or Depends(MyClass)), FastAPI treats the class itself as the callable."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q10-2980","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":10,"difficulty":"intermediate","question":"How does exception propagation differ between a handler raise and a dependency raise?","answerFull":"If the route handler raises, FastAPI calls throw(exc) on the generator — the exception enters the generator's except block and finally block. If the dependency itself raises before yield (during setup), the exception propagates immediately without yielding — no teardown phase runs because the generator never reached the yield point.","correct":"If the route handler raises, FastAPI calls throw(exc) on the generator — the exception enters the generator's except block and finally block.","distractors":["Exceptions raised in the finally block of a yield dependency propagate and can mask the original exception from the handler.","Use app.dependency_overrides to replace the original callable with a test generator.","A yield dependency is a generator function (sync or async) that performs setup before yield, produces the injected value at yield, and performs teardown after yield (typically in a finally block)."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q11-2981","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":11,"difficulty":"advanced","question":"How does FastAPI detect whether a dependency function is a generator?","answerFull":"At app startup, FastAPI inspects each dependency using inspect.isgeneratorfunction() and inspect.isasyncgenfunction(). For class-based dependencies, it checks the call method. This inspection happens once at import/startup; at request time FastAPI follows a pre-computed execution plan.","correct":"At app startup, FastAPI inspects each dependency using inspect.isgeneratorfunction() and inspect.isasyncgenfunction().","distractors":["Dependency resolution adds approximately 0.05 ms per dependency per request for async generator deps.","A yield dependency is a generator function used with Depends; code before yield runs during setup and code after yield runs during teardown after the response is sent.","A yield dependency is a generator function (sync or async) that performs setup before yield, produces the injected value at yield, and performs teardown after yield (typically in a finally block)."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q12-2982","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":12,"difficulty":"advanced","question":"What are the performance characteristics of yield dependencies?","answerFull":"Dependency resolution adds approximately 0.05 ms per dependency per request for async generator deps. Caching makes repeated references to the same dep essentially free (a dict lookup). The dominant cost is always the I/O inside the dep (e.g., acquiring a DB connection from a pool: ~0.1–1ms), not FastAPI's wrapping overhead.","correct":"Dependency resolution adds approximately 0.05 ms per dependency per request for async generator deps.","distractors":["A yield dependency is a generator function (sync or async) that performs setup before yield, produces the injected value at yield, and performs teardown after yield (typically in a finally block).","The instance is bound to the request's dependency resolution context.","Exceptions raised in the finally block of a yield dependency propagate and can mask the original exception from the handler."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q14-2983","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":14,"difficulty":"advanced","question":"Why can't you share a yield dependency instance between requests?","answerFull":"The instance is bound to the request's dependency resolution context. FastAPI creates a fresh dependency graph per request. Sharing instances across requests would introduce concurrency hazards — two concurrent requests modifying the same SQLAlchemy session would corrupt internal state. App-scoped shared state must be thread/async-safe and belongs in lifespan or module-level singletons.","correct":"The instance is bound to the request's dependency resolution context.","distractors":["use_cache=False passed to Depends tells FastAPI to bypass the per-request cache and call the dependency function again, creating a fresh instance.","Yield dependencies are request-scoped.","Use a yield dependency: create a SessionLocal() in the setup phase, yield it to the handler, and call db.close() in a finally block after the yield."]},{"id":"python/fastapi/dependency_injection_in_fastapi#Q15-2984","section":"python","module":"python/fastapi/dependency_injection_in_fastapi","moduleName":"dependency injection in fastapi","qIndex":15,"difficulty":"advanced","question":"What happens if a yield dependency yields more than once?","answerFull":"FastAPI drives the generator with next() (or send()) once to get the yielded value. After handler teardown it calls next() again expecting StopIteration. If the generator yields a second value instead of stopping, FastAPI's wrapping code raises RuntimeError: generator didn't stop after throw(). Always yield exactly once.","correct":"FastAPI drives the generator with next() (or send()) once to get the yielded value.","distractors":["Use app.dependency_overrides to replace the original callable with a test generator.","Exceptions raised in the finally block of a yield dependency propagate and can mask the original exception from the handler.","Dependency resolution adds approximately 0.05 ms per dependency per request for async generator deps."]},{"id":"python/fastapi/routing_and_request_handling#Q1-2985","section":"python","module":"python/fastapi/routing_and_request_handling","moduleName":"routing and request handling","qIndex":1,"difficulty":"core","question":"How does FastAPI determine which function argument gets its value from which HTTP parameter source?","answerFull":"FastAPI inspects the function signature at startup using Python's inspect.signature. Arguments whose names appear in the path template (e.g., {user_id}) are path parameters. Arguments with a Pydantic BaseModel type are treated as JSON body. All others are query parameters by default. You override the default with Annotated[T, Header()], Annotated[T, Cookie()], etc. The key practical point is that FastAPI resolves this once at startup, not on every request, so the per-request cost is just extraction and validation — not reflection.","correct":"FastAPI inspects the function signature at startup using Python's inspect.signature.","distractors":["BackgroundTasks (Starlette) queues callables that Starlette runs after the HTTP response is fully sent.","response_model instructs FastAPI to serialise the return value through that Pydantic model, which means only fields declared on the model are included in the response.","FastAPI detects synchronous functions at startup and wraps them using asyncio.get_event_loop().run_in_executor(None, ...) (Starlette's run_in_threadpool)."]},{"id":"python/fastapi/routing_and_request_handling#Q2-2986","section":"python","module":"python/fastapi/routing_and_request_handling","moduleName":"routing and request handling","qIndex":2,"difficulty":"core","question":"What is the difference between setting a dependency in APIRouter(dependencies=[...]) vs in each handler's signature?","answerFull":"Router-level dependencies run for every route in that router but are not injected into the handler as arguments — they are evaluated purely for side effects (auth check, rate limit, logging). Handler-level dependencies are injected as arguments and their return value is available inside the handler. Use router-level for cross-cutting concerns; handler-level when the dependency's return value is needed. You can mix both: router-level auth + handler-level database session.","correct":"Router-level dependencies run for every route in that router but are not injected into the handler as arguments — they are evaluated purely for side effects (auth check, rate limit, logging).","distractors":["include_in_schema=False prevents the route from appearing in the OpenAPI JSON and the Swagger UI.","HTTP headers use hyphen-separated names (X-Request-Id), but Python identifiers use underscores.","FastAPI inherits Starlette's router which matches routes in registration order — first match wins."]},{"id":"python/fastapi/routing_and_request_handling#Q3-2987","section":"python","module":"python/fastapi/routing_and_request_handling","moduleName":"routing and request handling","qIndex":3,"difficulty":"core","question":"Why does response_model matter for security?","answerFull":"response_model instructs FastAPI to serialise the return value through that Pydantic model, which means only fields declared on the model are included in the response. If a handler returns a SQLAlchemy User ORM object that has a password_hash field and the response_model=UserOut does not include password_hash, it is stripped automatically. Without response_model, all fields of the returned object are serialised, potentially leaking internal data.","correct":"response_model instructs FastAPI to serialise the return value through that Pydantic model, which means only fields declared on the model are included in the response.","distractors":["FastAPI inherits Starlette's router which matches routes in registration order — first match wins.","Use StreamingResponse when the response body is generated incrementally (database cursor, file read, LLM token stream) or when the full content would be too large to hold in memory simultaneously.","FastAPI detects synchronous functions at startup and wraps them using asyncio.get_event_loop().run_in_executor(None, ...) (Starlette's run_in_threadpool)."]},{"id":"python/fastapi/routing_and_request_handling#Q4-2988","section":"python","module":"python/fastapi/routing_and_request_handling","moduleName":"routing and request handling","qIndex":4,"difficulty":"core","question":"What happens when you define a sync def route in FastAPI?","answerFull":"FastAPI detects synchronous functions at startup and wraps them using asyncio.get_event_loop().run_in_executor(None, ...) (Starlette's run_in_threadpool). The route runs in the default ThreadPoolExecutor — by default up to min(32, os.cpu_count() + 4) threads. This means sync routes can make blocking calls safely without stalling the event loop, but they do consume thread pool capacity. Uvicorn's default is 40 worker threads for sync routes.","correct":"FastAPI detects synchronous functions at startup and wraps them using asyncio.get_event_loop().run_in_executor(None, ...) (Starlette's run_in_threadpool).","distractors":["Pass operation_id=\"my_custom_id\" to the decorator: @app.get(\"/users\", operation_id=\"listUsers\").","Router-level dependencies run for every route in that router but are not injected into the handler as arguments — they are evaluated purely for side effects (auth check, rate limit, logging).","FastAPI inherits Starlette's router which matches routes in registration order — first match wins."]},{"id":"python/fastapi/routing_and_request_handling#Q5-2989","section":"python","module":"python/fastapi/routing_and_request_handling","moduleName":"routing and request handling","qIndex":5,"difficulty":"core","question":"How does BackgroundTasks differ from asyncio.create_task?","answerFull":"BackgroundTasks (Starlette) queues callables that Starlette runs after the HTTP response is fully sent. It integrates with the ASGI lifecycle — cleanup dependencies (yield deps) are torn down only after background tasks complete. asyncio.create_task schedules a coroutine immediately on the event loop, before the response is sent, and is not aware of the request lifecycle. For tasks that must not block the response and are lightweight, use BackgroundTasks. For fire-and-forget coroutines that start immediately, use create_task. For durable tasks, use Celery or ARQ.","correct":"BackgroundTasks (Starlette) queues callables that Starlette runs after the HTTP response is fully sent.","distractors":["Use StreamingResponse when the response body is generated incrementally (database cursor, file read, LLM token stream) or when the full content would be too large to hold in memory simultaneously.","FastAPI inherits Starlette's router which matches routes in registration order — first match wins.","response_model instructs FastAPI to serialise the return value through that Pydantic model, which means only fields declared on the model are included in the response."]},{"id":"python/fastapi/routing_and_request_handling#Q6-2990","section":"python","module":"python/fastapi/routing_and_request_handling","moduleName":"routing and request handling","qIndex":6,"difficulty":"intermediate","question":"What is the route ordering rule in FastAPI and why does it exist?","answerFull":"FastAPI inherits Starlette's router which matches routes in registration order — first match wins. If you register GET /users/{user_id} before GET /users/me, the string \"me\" matches {user_id} as a path parameter and get_me() is never called. The fix is to always register specific (non-parameterised) routes before parameterised ones with overlapping prefixes. This is a static router — there is no specificity scoring unlike some other frameworks.","correct":"FastAPI inherits Starlette's router which matches routes in registration order — first match wins.","distractors":["include_in_schema=False prevents the route from appearing in the OpenAPI JSON and the Swagger UI.","response_model instructs FastAPI to serialise the return value through that Pydantic model, which means only fields declared on the model are included in the response.","BackgroundTasks (Starlette) queues callables that Starlette runs after the HTTP response is fully sent."]},{"id":"python/fastapi/routing_and_request_handling#Q7-2991","section":"python","module":"python/fastapi/routing_and_request_handling","moduleName":"routing and request handling","qIndex":7,"difficulty":"intermediate","question":"How do you return a 201 status code for a POST endpoint?","answerFull":"Set status_code=201 in the decorator: @app.post(\"/users\", status_code=201). This is the default HTTP status for that endpoint. You can still override it dynamically by returning a Response subclass with an explicit status_code argument. If you need to return a different status on some code paths, inject Response as a parameter and set response.status_code.","correct":"Set status_code=201 in the decorator: @app.post(\"/users\", status_code=201).","distractors":["Pass operation_id=\"my_custom_id\" to the decorator: @app.get(\"/users\", operation_id=\"listUsers\").","response_model instructs FastAPI to serialise the return value through that Pydantic model, which means only fields declared on the model are included in the response.","FastAPI inspects the function signature at startup using Python's inspect.signature."]},{"id":"python/fastapi/routing_and_request_handling#Q8-2992","section":"python","module":"python/fastapi/routing_and_request_handling","moduleName":"routing and request handling","qIndex":8,"difficulty":"intermediate","question":"What is include_in_schema=False used for and when would you set it?","answerFull":"include_in_schema=False prevents the route from appearing in the OpenAPI JSON and the Swagger UI. Use it for: internal health-check endpoints that should not be documented publicly, debug or introspection endpoints, and endpoints that internal load balancers call but external clients should not know about. The route still functions — it just does not appear in /openapi.json.","correct":"include_in_schema=False prevents the route from appearing in the OpenAPI JSON and the Swagger UI.","distractors":["Router-level dependencies run for every route in that router but are not injected into the handler as arguments — they are evaluated purely for side effects (auth check, rate limit, logging).","Set status_code=201 in the decorator: @app.post(\"/users\", status_code=201).","FastAPI inspects the function signature at startup using Python's inspect.signature."]},{"id":"python/fastapi/routing_and_request_handling#Q9-2993","section":"python","module":"python/fastapi/routing_and_request_handling","moduleName":"routing and request handling","qIndex":9,"difficulty":"intermediate","question":"How do you accept a list of query parameters (e.g., ?tags=a&tags=b)?","answerFull":"Declare the parameter as list[str] with a Query annotation: tags: Annotated[list[str], Query()] = []. FastAPI collects all values for the tags key from the query string and coerces them into a list. Without Query(), FastAPI would not know to collect multiple values and would only capture the first occurrence.","correct":"Declare the parameter as list[str] with a Query annotation: tags: Annotated[list[str], Query()] = [].","distractors":["Use UploadFile as the parameter type, combined with File(): file: Annotated[UploadFile, File()].","Annotated[T, metadata] ([3.9]) separates the base type from validation metadata.","FastAPI inspects the function signature at startup using Python's inspect.signature."]},{"id":"python/fastapi/routing_and_request_handling#Q10-2994","section":"python","module":"python/fastapi/routing_and_request_handling","moduleName":"routing and request handling","qIndex":10,"difficulty":"intermediate","question":"How do you handle file uploads in FastAPI?","answerFull":"Use UploadFile as the parameter type, combined with File(): file: Annotated[UploadFile, File()]. UploadFile wraps a SpooledTemporaryFile — small files are kept in memory (default threshold 1 MB), larger files are spooled to disk. Read the content with await file.read() or stream it with file.file. For multipart forms mixing files and JSON fields, use Form() for text fields and UploadFile for files. You cannot mix a Pydantic body model with File()/Form() in the same handler — use Form() for each field individually.","correct":"Use UploadFile as the parameter type, combined with File(): file: Annotated[UploadFile, File()].","distractors":["FastAPI inspects the function signature at startup using Python's inspect.signature.","Use StreamingResponse when the response body is generated incrementally (database cursor, file read, LLM token stream) or when the full content would be too large to hold in memory simultaneously.","FastAPI inherits Starlette's router which matches routes in registration order — first match wins."]},{"id":"python/fastapi/routing_and_request_handling#Q11-2995","section":"python","module":"python/fastapi/routing_and_request_handling","moduleName":"routing and request handling","qIndex":11,"difficulty":"advanced","question":"What is the purpose of Annotated in parameter declarations?","answerFull":"Annotated[T, metadata] ([3.9]) separates the base type from validation metadata. FastAPI reads the metadata argument (Path(...), Query(...), etc.) to determine: the parameter source, validation constraints (ge, le, min_length, regex), aliases, deprecation flags, and OpenAPI descriptions. The type checker only sees T, so Annotated[int, Path(ge=1)] is treated as int by mypy/pyright. This keeps type annotations valid for static analysis while giving FastAPI rich runtime metadata.","correct":"Annotated[T, metadata] ([3.9]) separates the base type from validation metadata.","distractors":["Declare the parameter as list[str] with a Query annotation: tags: Annotated[list[str], Query()] = [].","Use UploadFile as the parameter type, combined with File(): file: Annotated[UploadFile, File()].","FastAPI inspects the function signature at startup using Python's inspect.signature."]},{"id":"python/fastapi/routing_and_request_handling#Q12-2996","section":"python","module":"python/fastapi/routing_and_request_handling","moduleName":"routing and request handling","qIndex":12,"difficulty":"advanced","question":"How do you customise the operationId in the OpenAPI schema?","answerFull":"Pass operation_id=\"my_custom_id\" to the decorator: @app.get(\"/users\", operation_id=\"listUsers\"). By default FastAPI generates the operation ID from the function name and HTTP method, which produces IDs like get_user_usersuser_idget. Custom operation IDs matter when generating client SDKs — the operation ID becomes the generated method name. Many teams set generate_unique_id_function on the FastAPI instance to control the naming strategy globally.","correct":"Pass operation_id=\"my_custom_id\" to the decorator: @app.get(\"/users\", operation_id=\"listUsers\").","distractors":["FastAPI detects synchronous functions at startup and wraps them using asyncio.get_event_loop().run_in_executor(None, ...) (Starlette's run_in_threadpool).","include_in_schema=False prevents the route from appearing in the OpenAPI JSON and the Swagger UI.","Set status_code=201 in the decorator: @app.post(\"/users\", status_code=201)."]},{"id":"python/fastapi/routing_and_request_handling#Q13-2997","section":"python","module":"python/fastapi/routing_and_request_handling","moduleName":"routing and request handling","qIndex":13,"difficulty":"advanced","question":"When would you use StreamingResponse vs returning a Pydantic model?","answerFull":"Use StreamingResponse when the response body is generated incrementally (database cursor, file read, LLM token stream) or when the full content would be too large to hold in memory simultaneously. A 1 GB CSV file exported row by row via an async generator uses constant memory (~4 KB per yield). Pydantic model responses require the entire object to be in memory to serialise. StreamingResponse cannot use response_model — serialisation is your responsibility inside the generator.","correct":"Use StreamingResponse when the response body is generated incrementally (database cursor, file read, LLM token stream) or when the full content would be too large to hold in memory simultaneously.","distractors":["BackgroundTasks (Starlette) queues callables that Starlette runs after the HTTP response is fully sent.","Use UploadFile as the parameter type, combined with File(): file: Annotated[UploadFile, File()].","response_model instructs FastAPI to serialise the return value through that Pydantic model, which means only fields declared on the model are included in the response."]},{"id":"python/fastapi/routing_and_request_handling#Q14-2998","section":"python","module":"python/fastapi/routing_and_request_handling","moduleName":"routing and request handling","qIndex":14,"difficulty":"advanced","question":"How does FastAPI handle the conversion of header names?","answerFull":"HTTP headers use hyphen-separated names (X-Request-Id), but Python identifiers use underscores. Header() automatically converts underscores in the parameter name to hyphens when looking up the header. So x_request_id: Annotated[str | None, Header()] = None looks up X-Request-Id in the request headers. You can disable this with convert_underscores=False in Header(). This is a common source of None bugs when developers forget the conversion rule.","correct":"HTTP headers use hyphen-separated names (X-Request-Id), but Python identifiers use underscores.","distractors":["response_model instructs FastAPI to serialise the return value through that Pydantic model, which means only fields declared on the model are included in the response.","Router-level dependencies run for every route in that router but are not injected into the handler as arguments — they are evaluated purely for side effects (auth check, rate limit, logging).","FastAPI detects synchronous functions at startup and wraps them using asyncio.get_event_loop().run_in_executor(None, ...) (Starlette's run_in_threadpool)."]},{"id":"python/fastapi/message_queues_and_event_driven#Q1-2999","section":"python","module":"python/fastapi/message_queues_and_event_driven","moduleName":"message queues and event driven","qIndex":1,"difficulty":"core","question":"What is the difference between at-least-once and exactly-once delivery, and when would you pay for exactly-once?","answerFull":"At-least-once commits the offset after successful processing, so a crash between processing and commit causes redelivery. Exactly-once uses Kafka transactions (transactional.id, isolation.level=read_committed) to atomically write output and commit input offsets. Pay the ~30–50 % throughput penalty only for financial debits, inventory deductions, or any side-effect that cannot be made idempotent cheaply.","correct":"At-least-once commits the offset after successful processing, so a crash between processing and commit causes redelivery.","distractors":["Store a unique message ID (Kafka topic-partition-offset or a business-level idempotency key) in Redis using SET NX EX.","prefetch_count sets the maximum number of unacknowledged messages RabbitMQ will push to a consumer at once.","It enables exactly-once producer semantics: the broker assigns a producer ID and sequence number to each batch."]},{"id":"python/fastapi/message_queues_and_event_driven#Q2-3000","section":"python","module":"python/fastapi/message_queues_and_event_driven","moduleName":"message queues and event driven","qIndex":2,"difficulty":"core","question":"How does the outbox pattern prevent dual-write inconsistency?","answerFull":"The outbox pattern writes the business record and the outbox event row in a single database transaction. Because both writes share one commit, either both land or neither does — eliminating the window where the external broker has the message but the DB does not (or vice versa). A separate poller or CDC connector publishes outbox rows to the broker after the fact.","correct":"The outbox pattern writes the business record and the outbox event row in a single database transaction.","distractors":["Store a unique message ID (Kafka topic-partition-offset or a business-level idempotency key) in Redis using SET NX EX.","acks=\"all\" (or acks=-1) requires all in-sync replicas (ISR) to acknowledge the write before the producer call returns.","At-least-once commits the offset after successful processing, so a crash between processing and commit causes redelivery."]},{"id":"python/fastapi/message_queues_and_event_driven#Q3-3001","section":"python","module":"python/fastapi/message_queues_and_event_driven","moduleName":"message queues and event driven","qIndex":3,"difficulty":"core","question":"How do consumer groups work in Kafka, and what happens during partition rebalancing?","answerFull":"All consumers sharing a group_id divide partitions among themselves: each partition is owned by exactly one consumer. When a consumer joins or leaves (including crashes), the group coordinator triggers a rebalance — partitions are redistributed. During rebalance, consumption pauses. Incremental cooperative rebalancing (available since Kafka 2.4) minimises the pause by revoking only the partitions that need to move.","correct":"All consumers sharing a group_id divide partitions among themselves: each partition is owned by exactly one consumer.","distractors":["It enables exactly-once producer semantics: the broker assigns a producer ID and sequence number to each batch.","prefetch_count sets the maximum number of unacknowledged messages RabbitMQ will push to a consumer at once.","Consumer lag is the difference between the latest available offset (log end offset) and the consumer group's committed offset per partition."]},{"id":"python/fastapi/message_queues_and_event_driven#Q4-3002","section":"python","module":"python/fastapi/message_queues_and_event_driven","moduleName":"message queues and event driven","qIndex":4,"difficulty":"core","question":"When should you use a topic exchange vs a direct exchange in RabbitMQ?","answerFull":"Use a direct exchange when routing is 1:1 between routing key and queue — simple task queues. Use a topic exchange when multiple queues need to subscribe based on patterns (e.g., order.* for all order events, *.failed for all failures). Fanout exchange is appropriate when every bound queue must receive every message regardless of routing key — e.g., cache invalidation broadcast.","correct":"Use a direct exchange when routing is 1:1 between routing key and queue — simple task queues.","distractors":["Cancel the consumer asyncio task on SIGTERM, catch CancelledError, and call await consumer.stop() in the finally block.","prefetch_count sets the maximum number of unacknowledged messages RabbitMQ will push to a consumer at once.","Produce messages with the customer ID as the partition key (key=customer_id.encode())."]},{"id":"python/fastapi/message_queues_and_event_driven#Q5-3003","section":"python","module":"python/fastapi/message_queues_and_event_driven","moduleName":"message queues and event driven","qIndex":5,"difficulty":"core","question":"How do you implement an idempotent consumer?","answerFull":"Store a unique message ID (Kafka topic-partition-offset or a business-level idempotency key) in Redis using SET NX EX. Before executing the side-effect, attempt to set the key. If the key already exists the message was already processed — skip and commit the offset. Set TTL to at least the maximum possible retry window (e.g., 24 hours).","correct":"Store a unique message ID (Kafka topic-partition-offset or a business-level idempotency key) in Redis using SET NX EX.","distractors":["Producers register a schema version before first publish; the registry assigns a schema ID embedded in the message header.","Produce messages with the customer ID as the partition key (key=customer_id.encode()).","Cancel the consumer asyncio task on SIGTERM, catch CancelledError, and call await consumer.stop() in the finally block."]},{"id":"python/fastapi/message_queues_and_event_driven#Q6-3004","section":"python","module":"python/fastapi/message_queues_and_event_driven","moduleName":"message queues and event driven","qIndex":6,"difficulty":"intermediate","question":"What is prefetch_count in RabbitMQ and why does it matter?","answerFull":"prefetch_count sets the maximum number of unacknowledged messages RabbitMQ will push to a consumer at once. Without it, RabbitMQ floods the consumer with the entire queue backlog, causing memory exhaustion. A value of 10–100 is typical; higher values improve throughput at the cost of re-queuing more messages if the consumer crashes.","correct":"prefetch_count sets the maximum number of unacknowledged messages RabbitMQ will push to a consumer at once.","distractors":["It enables exactly-once producer semantics: the broker assigns a producer ID and sequence number to each batch.","Consumer lag is the difference between the latest available offset (log end offset) and the consumer group's committed offset per partition.","Produce messages with the customer ID as the partition key (key=customer_id.encode())."]},{"id":"python/fastapi/message_queues_and_event_driven#Q7-3005","section":"python","module":"python/fastapi/message_queues_and_event_driven","moduleName":"message queues and event driven","qIndex":7,"difficulty":"intermediate","question":"What is consumer lag, and how do you alert on it?","answerFull":"Consumer lag is the difference between the latest available offset (log end offset) and the consumer group's committed offset per partition. It measures how far behind consumers are. Expose it via kafka-consumer-groups.sh --describe or the JMX metric kafka.consumer:type=consumer-fetch-manager-metrics,client-id=*,attribute=records-lag-max. Alert when lag exceeds the number of messages that would accumulate during your acceptable recovery time (e.g., > 10_000 for a queue that produces 500 msg/s and must drain within 20 s).","correct":"Consumer lag is the difference between the latest available offset (log end offset) and the consumer group's committed offset per partition.","distractors":["Store a unique message ID (Kafka topic-partition-offset or a business-level idempotency key) in Redis using SET NX EX.","Produce messages with the customer ID as the partition key (key=customer_id.encode()).","prefetch_count sets the maximum number of unacknowledged messages RabbitMQ will push to a consumer at once."]},{"id":"python/fastapi/message_queues_and_event_driven#Q8-3006","section":"python","module":"python/fastapi/message_queues_and_event_driven","moduleName":"message queues and event driven","qIndex":8,"difficulty":"intermediate","question":"How do you handle message ordering in Kafka when you need order per customer?","answerFull":"Produce messages with the customer ID as the partition key (key=customer_id.encode()). Kafka hashes the key to a partition deterministically — all messages for the same customer land in the same partition and are consumed in order. Ordering is guaranteed per partition; if you need a total order across all customers you are limited to a single partition, which caps throughput to ~1 Gbps.","correct":"Produce messages with the customer ID as the partition key (key=customer_id.encode()).","distractors":["All consumers sharing a group_id divide partitions among themselves: each partition is owned by exactly one consumer.","Consumer lag is the difference between the latest available offset (log end offset) and the consumer group's committed offset per partition.","prefetch_count sets the maximum number of unacknowledged messages RabbitMQ will push to a consumer at once."]},{"id":"python/fastapi/message_queues_and_event_driven#Q9-3007","section":"python","module":"python/fastapi/message_queues_and_event_driven","moduleName":"message queues and event driven","qIndex":9,"difficulty":"intermediate","question":"Describe the dead-letter queue pattern and when you would replay from it.","answerFull":"A DLQ captures messages that fail processing after N retry attempts. In Kafka, configure the consumer to produce to <topic>.DLT after retries. In RabbitMQ, bind a DLX exchange and a dead-letter queue on the primary queue declaration. Replay from the DLQ after fixing the bug: for Kafka reset the consumer group offset to the earliest offset in the DLT topic; for RabbitMQ use the management plugin to re-queue DLQ messages back to the main queue.","correct":"A DLQ captures messages that fail processing after N retry attempts.","distractors":["prefetch_count sets the maximum number of unacknowledged messages RabbitMQ will push to a consumer at once.","The outbox pattern writes the business record and the outbox event row in a single database transaction.","At-least-once commits the offset after successful processing, so a crash between processing and commit causes redelivery."]},{"id":"python/fastapi/message_queues_and_event_driven#Q10-3008","section":"python","module":"python/fastapi/message_queues_and_event_driven","moduleName":"message queues and event driven","qIndex":10,"difficulty":"intermediate","question":"What is enable_idempotence=True in the aiokafka producer and what does it guarantee?","answerFull":"It enables exactly-once producer semantics: the broker assigns a producer ID and sequence number to each batch. If the producer retries due to a network timeout, the broker deduplicates by sequence number, ensuring the message is written exactly once even with retries. This does not extend to consumer-side semantics; you still need idempotent consumers for end-to-end exactly-once.","correct":"It enables exactly-once producer semantics: the broker assigns a producer ID and sequence number to each batch.","distractors":["At-least-once commits the offset after successful processing, so a crash between processing and commit causes redelivery.","All consumers sharing a group_id divide partitions among themselves: each partition is owned by exactly one consumer.","prefetch_count sets the maximum number of unacknowledged messages RabbitMQ will push to a consumer at once."]},{"id":"python/fastapi/message_queues_and_event_driven#Q11-3009","section":"python","module":"python/fastapi/message_queues_and_event_driven","moduleName":"message queues and event driven","qIndex":11,"difficulty":"advanced","question":"How does schema evolution work with a schema registry?","answerFull":"Producers register a schema version before first publish; the registry assigns a schema ID embedded in the message header. Consumers fetch the schema by ID before deserialising. The registry enforces compatibility modes: BACKWARD (new schema can read old messages), FORWARD (old schema can read new messages), FULL (both). Additive changes (new optional field) are BACKWARD compatible. Removing a required field is not. Always test schema changes in a staging registry before promoting to production.","correct":"Producers register a schema version before first publish; the registry assigns a schema ID embedded in the message header.","distractors":["It enables exactly-once producer semantics: the broker assigns a producer ID and sequence number to each batch.","Store a unique message ID (Kafka topic-partition-offset or a business-level idempotency key) in Redis using SET NX EX.","At-least-once commits the offset after successful processing, so a crash between processing and commit causes redelivery."]},{"id":"python/fastapi/message_queues_and_event_driven#Q12-3010","section":"python","module":"python/fastapi/message_queues_and_event_driven","moduleName":"message queues and event driven","qIndex":12,"difficulty":"advanced","question":"What is the acks=\"all\" setting and what does it guarantee vs acks=1?","answerFull":"acks=\"all\" (or acks=-1) requires all in-sync replicas (ISR) to acknowledge the write before the producer call returns. If the leader fails, a follower in the ISR that received the message can be elected — no data loss. acks=1 only requires the leader to acknowledge; if the leader fails before replicating, the message is lost. The latency cost of acks=\"all\" is roughly the replication latency (typically 1–5 ms on LAN). For financial events or order creation, always use acks=\"all\".","correct":"acks=\"all\" (or acks=-1) requires all in-sync replicas (ISR) to acknowledge the write before the producer call returns.","distractors":["It enables exactly-once producer semantics: the broker assigns a producer ID and sequence number to each batch.","Cancel the consumer asyncio task on SIGTERM, catch CancelledError, and call await consumer.stop() in the finally block.","At-least-once commits the offset after successful processing, so a crash between processing and commit causes redelivery."]},{"id":"python/fastapi/message_queues_and_event_driven#Q13-3011","section":"python","module":"python/fastapi/message_queues_and_event_driven","moduleName":"message queues and event driven","qIndex":13,"difficulty":"advanced","question":"How do you safely shut down a Kafka consumer in an asyncio application?","answerFull":"Cancel the consumer asyncio task on SIGTERM, catch CancelledError, and call await consumer.stop() in the finally block. consumer.stop() triggers a final offset commit (if using auto-commit) and sends a LeaveGroup request so the broker can rebalance the partition to another consumer immediately rather than waiting for the session timeout (default 10 s). For manual commits, commit before calling stop().","correct":"Cancel the consumer asyncio task on SIGTERM, catch CancelledError, and call await consumer.stop() in the finally block.","distractors":["acks=\"all\" (or acks=-1) requires all in-sync replicas (ISR) to acknowledge the write before the producer call returns.","Consumer lag is the difference between the latest available offset (log end offset) and the consumer group's committed offset per partition.","All consumers sharing a group_id divide partitions among themselves: each partition is owned by exactly one consumer."]},{"id":"python/packaging_and_project_tooling#Q1-3012","section":"python","module":"python/packaging_and_project_tooling","moduleName":"packaging and project tooling","qIndex":1,"difficulty":"core","question":"What problem does pyproject.toml solve, and which PEPs define it?","answerFull":"pyproject.toml provides a single, declarative file that replaces setup.py, setup.cfg, MANIFEST.in, and scattered tool config files. PEP 517 decouples build frontends from backends via a standard interface. PEP 518 introduces the [build-system] table so tools know which backend to invoke. PEP 621 standardizes the [project] metadata table (name, version, dependencies). Before these PEPs, setup.py executed arbitrary Python code during install, making builds non-reproducible and creating security risks.","correct":"pyproject.toml provides a single, declarative file that replaces setup.py, setup.cfg, MANIFEST.in, and scattered tool config files.","distractors":["pre-commit is a framework that installs git hooks (stored in .pre-commit-config.yaml) that run before each commit on the developer's machine.","ruff is a single Rust binary that implements the rules of flake8 (and 50+ plugins), black-compatible formatting, and isort import sorting.","Declare direct dependencies in pyproject.toml under [project.dependencies] with loose lower bounds."]},{"id":"python/packaging_and_project_tooling#Q2-3013","section":"python","module":"python/packaging_and_project_tooling","moduleName":"packaging and project tooling","qIndex":2,"difficulty":"core","question":"What is the difference between a wheel and an sdist, and when does each matter?","answerFull":"A wheel (.whl) is a pre-built archive — install time is just unzipping and copying files. An sdist (.tar.gz) is raw source that requires running the build backend at install time, which may involve compiling C extensions. Pure Python wheels use the tag py3-none-any and work everywhere. C extension wheels are platform-specific (e.g., cp312-cp312-manylinux_2_17_x86_64). In production Docker builds, always install from wheels — missing a wheel for your platform triggers an sdist build that may fail if the build tools are absent.","correct":"A wheel (.whl) is a pre-built archive — install time is just unzipping and copying files.","distractors":["pip install -e . or uv pip install -e .","pre-commit is a framework that installs git hooks (stored in .pre-commit-config.yaml) that run before each commit on the developer's machine.","pyproject.toml provides a single, declarative file that replaces setup.py, setup.cfg, MANIFEST.in, and scattered tool config files."]},{"id":"python/packaging_and_project_tooling#Q4-3014","section":"python","module":"python/packaging_and_project_tooling","moduleName":"packaging and project tooling","qIndex":4,"difficulty":"core","question":"What is an editable install and when do you use it?","answerFull":"pip install -e . or uv pip install -e . installs the package by reference to the source tree instead of copying files into site-packages. Python's import machinery finds the package at src/mypackage/ directly. Changes to source files are immediately reflected without reinstalling. Use editable installs during development when you have multiple internal packages that depend on each other — without it, you would need to reinstall after every change. Never use editable installs in production containers.","correct":"pip install -e . or uv pip install -e .","distractors":["A wheel (.whl) is a pre-built archive — install time is just unzipping and copying files.","pyproject.toml provides a single, declarative file that replaces setup.py, setup.cfg, MANIFEST.in, and scattered tool config files.","A library (published to PyPI) is consumed by many different applications, each with its own set of dependencies."]},{"id":"python/packaging_and_project_tooling#Q5-3015","section":"python","module":"python/packaging_and_project_tooling","moduleName":"packaging and project tooling","qIndex":5,"difficulty":"core","question":"Why should library packages not commit a lock file, while application projects should?","answerFull":"A library (published to PyPI) is consumed by many different applications, each with its own set of dependencies. Committing a lock file would have no effect on consumers (they don't read your lock file) and could falsely imply the exact pinned versions are required. The library should declare loose bounds in [project.dependencies] and let the consumer's resolver find compatible versions. An application (deployed to a specific environment) must be fully reproducible — every developer and every CI run must produce the same environment — so committing the lock file guarantees this.","correct":"A library (published to PyPI) is consumed by many different applications, each with its own set of dependencies.","distractors":["Configure PYPI_TOKEN as a repository secret.","pre-commit is a framework that installs git hooks (stored in .pre-commit-config.yaml) that run before each commit on the developer's machine.","pyproject.toml provides a single, declarative file that replaces setup.py, setup.cfg, MANIFEST.in, and scattered tool config files."]},{"id":"python/packaging_and_project_tooling#Q6-3016","section":"python","module":"python/packaging_and_project_tooling","moduleName":"packaging and project tooling","qIndex":6,"difficulty":"intermediate","question":"What does py.typed do and what happens if you omit it from a library?","answerFull":"py.typed is an empty marker file defined in PEP 561. Its presence in a package's directory signals to type checkers (mypy, pyright) that the package ships type information and that type checking should be applied to it. Without py.typed, mypy treats the entire package as Any — all calls to the library's functions return Any, all type errors in usage are suppressed, and consumers lose all IDE autocompletion and type safety benefits. Fix: create an empty src/mylib/py.typed and ensure it is included in the wheel.","correct":"py.typed is an empty marker file defined in PEP 561.","distractors":["A library (published to PyPI) is consumed by many different applications, each with its own set of dependencies.","pyproject.toml provides a single, declarative file that replaces setup.py, setup.cfg, MANIFEST.in, and scattered tool config files.","A wheel (.whl) is a pre-built archive — install time is just unzipping and copying files."]},{"id":"python/packaging_and_project_tooling#Q7-3017","section":"python","module":"python/packaging_and_project_tooling","moduleName":"packaging and project tooling","qIndex":7,"difficulty":"intermediate","question":"How does ruff differ from flake8 + black + isort, and why would you migrate?","answerFull":"ruff is a single Rust binary that implements the rules of flake8 (and 50+ plugins), black-compatible formatting, and isort import sorting. It is 10-100x faster — checking the entire CPython codebase takes under 1 second vs. 30+ seconds with flake8. It uses the same configuration format (pyproject.toml) for all rules. Migration is a drop-in replacement for most projects: ruff check replaces flake8, ruff format replaces black, and isort configuration maps directly to [tool.ruff.lint.isort]. The main reason not to migrate is if you use a custom flake8 plugin that ruff does not yet support.","correct":"ruff is a single Rust binary that implements the rules of flake8 (and 50+ plugins), black-compatible formatting, and isort import sorting.","distractors":["pyproject.toml provides a single, declarative file that replaces setup.py, setup.cfg, MANIFEST.in, and scattered tool config files.","A wheel (.whl) is a pre-built archive — install time is just unzipping and copying files.","pip install -e . or uv pip install -e ."]},{"id":"python/packaging_and_project_tooling#Q8-3018","section":"python","module":"python/packaging_and_project_tooling","moduleName":"packaging and project tooling","qIndex":8,"difficulty":"intermediate","question":"What is pre-commit and how does it differ from running linters in CI?","answerFull":"pre-commit is a framework that installs git hooks (stored in .pre-commit-config.yaml) that run before each commit on the developer's machine. It creates isolated environments for each hook. The key difference from CI: pre-commit catches issues before they are committed, giving the developer immediate feedback and preventing a CI failure cycle. CI still runs the same checks (via pre-commit run --all-files) as the authoritative gate, but the developer loop is much faster. pre-commit autoupdate keeps hook versions current.","correct":"pre-commit is a framework that installs git hooks (stored in .pre-commit-config.yaml) that run before each commit on the developer's machine.","distractors":["pyproject.toml provides a single, declarative file that replaces setup.py, setup.cfg, MANIFEST.in, and scattered tool config files.","A wheel (.whl) is a pre-built archive — install time is just unzipping and copying files.","pip install -e . or uv pip install -e ."]},{"id":"python/packaging_and_project_tooling#Q9-3019","section":"python","module":"python/packaging_and_project_tooling","moduleName":"packaging and project tooling","qIndex":9,"difficulty":"intermediate","question":"Explain semantic versioning constraints: what is the difference between ^1.2.0 and ~1.2.0 in poetry?","answerFull":"^1.2.0 (caret) allows updates that do not change the left-most non-zero digit: for >=1.0.0, this means >=1.2.0, <2.0.0. It assumes semantic versioning is honored — any 1.x version is backward compatible. ~1.2.0 (tilde) allows only patch-level updates: >=1.2.0, <1.3.0. Use caret for most dependencies where you trust the upstream project follows semver. Use tilde when you are conservative about minor version changes, e.g., a library that has historically made breaking changes in minor releases.","correct":"^1.2.0 (caret) allows updates that do not change the left-most non-zero digit: for >=1.0.0, this means >=1.2.0, <2.0.0.","distractors":["pyproject.toml provides a single, declarative file that replaces setup.py, setup.cfg, MANIFEST.in, and scattered tool config files.","A wheel (.whl) is a pre-built archive — install time is just unzipping and copying files.","pip install -e . or uv pip install -e ."]},{"id":"python/packaging_and_project_tooling#Q10-3020","section":"python","module":"python/packaging_and_project_tooling","moduleName":"packaging and project tooling","qIndex":10,"difficulty":"intermediate","question":"How do you set up a full dependency resolution and lock workflow with uv?","answerFull":"Declare direct dependencies in pyproject.toml under [project.dependencies] with loose lower bounds. Run uv pip compile pyproject.toml --all-extras -o requirements.lock to generate a fully pinned lock file with SHA-256 hashes for every package. Commit requirements.lock to the repository. In CI and Docker, run uv pip install --require-hashes -r requirements.lock — the --require-hashes flag makes the install fail if any package hash does not match, preventing supply chain attacks. To upgrade a specific package: uv pip compile --upgrade-package fastapi pyproject.toml -o requirements.lock.","correct":"Declare direct dependencies in pyproject.toml under [project.dependencies] with loose lower bounds.","distractors":["A library (published to PyPI) is consumed by many different applications, each with its own set of dependencies.","The src layout places package source under src/mypackage/ rather than directly in the project root.","pyproject.toml provides a single, declarative file that replaces setup.py, setup.cfg, MANIFEST.in, and scattered tool config files."]},{"id":"python/packaging_and_project_tooling#Q11-3021","section":"python","module":"python/packaging_and_project_tooling","moduleName":"packaging and project tooling","qIndex":11,"difficulty":"advanced","question":"What is the src layout and why is it preferred over a flat layout?","answerFull":"The src layout places package source under src/mypackage/ rather than directly in the project root. Without the src layout, running pytest from the project root makes import mypackage resolve to the local directory (not the installed package), hiding import errors that would appear in production (e.g., missing init.py, incorrect package data). With the src layout, the local directory is never on sys.path during tests — you must install the package (even as editable) before tests run, making the test environment match production more closely.","correct":"The src layout places package source under src/mypackage/ rather than directly in the project root.","distractors":["Declare direct dependencies in pyproject.toml under [project.dependencies] with loose lower bounds.","pyproject.toml provides a single, declarative file that replaces setup.py, setup.cfg, MANIFEST.in, and scattered tool config files.","A wheel (.whl) is a pre-built archive — install time is just unzipping and copying files."]},{"id":"python/packaging_and_project_tooling#Q12-3022","section":"python","module":"python/packaging_and_project_tooling","moduleName":"packaging and project tooling","qIndex":12,"difficulty":"advanced","question":"How do you publish a package to PyPI in a CI/CD pipeline?","answerFull":"Configure PYPI_TOKEN as a repository secret. In the release workflow (triggered by a version tag), run python -m build to produce the wheel and sdist in dist/. Run twine check dist/* to verify the artifacts meet PyPI requirements (valid metadata, valid README rendering). Run twine upload dist/* --non-interactive --username token --password $PYPI_TOKEN. Alternatively, use uv publish --token $PYPI_TOKEN. Use TestPyPI for staging. For automated version bumping, integrate python-semantic-release which reads conventional commit messages to determine the next version, updates pyproject.toml, creates a git tag, and triggers the release workflow.","correct":"Configure PYPI_TOKEN as a repository secret.","distractors":["A library (published to PyPI) is consumed by many different applications, each with its own set of dependencies.","The src layout places package source under src/mypackage/ rather than directly in the project root.","pyproject.toml provides a single, declarative file that replaces setup.py, setup.cfg, MANIFEST.in, and scattered tool config files."]},{"id":"python/async_patterns_and_pitfalls#Q1-3023","section":"python","module":"python/async_patterns_and_pitfalls","moduleName":"async patterns and pitfalls","qIndex":1,"difficulty":"core","question":"What is the most common async bug in FastAPI services, and how do you detect it?","answerFull":"Calling a blocking synchronous function (like requests.get() or time.sleep()) inside an async def route — this stalls the entire event loop for the duration of the call. Detect it by enabling loop.set_debug(True) (warns when a callback takes > 100ms), by adding Sentry's AsyncioIntegration, or by profiling with yappi which reports coroutine wall time.","correct":"Calling a blocking synchronous function (like requests.get() or time.sleep()) inside an async def route — this stalls the entire event loop for the duration of the call.","distractors":["asyncio.to_thread(fn, *args) is syntactic sugar introduced in Python 3.9 for loop.run_in_executor(None, fn, *args).","CLOSED (normal): requests pass through; failure counter increments on exceptions.","Low CPU + slow requests in an async service is the classic blocking-in-async signature."]},{"id":"python/async_patterns_and_pitfalls#Q2-3024","section":"python","module":"python/async_patterns_and_pitfalls","moduleName":"async patterns and pitfalls","qIndex":2,"difficulty":"core","question":"What is the difference between asyncio.to_thread() and loop.run_in_executor()?","answerFull":"asyncio.to_thread(fn, *args) is syntactic sugar introduced in Python 3.9 for loop.run_in_executor(None, fn, *args). Both submit the callable to the default ThreadPoolExecutor. Use to_thread() for I/O-bound sync work; use run_in_executor(pool) with a ProcessPoolExecutor when you need to bypass the GIL for CPU-bound work.","correct":"asyncio.to_thread(fn, *args) is syntactic sugar introduced in Python 3.9 for loop.run_in_executor(None, fn, *args).","distractors":["The Task object has no strong reference, so Python's garbage collector may collect and cancel it before it finishes.","Regular generators use yield and are consumed with a synchronous for loop.","Use asyncio.Queue(maxsize=N)."]},{"id":"python/async_patterns_and_pitfalls#Q3-3025","section":"python","module":"python/async_patterns_and_pitfalls","moduleName":"async patterns and pitfalls","qIndex":3,"difficulty":"core","question":"How large is the default thread pool used by asyncio.to_thread()?","answerFull":"min(32, os.cpu_count() + 4) — this is the default ThreadPoolExecutor size in CPython. On a 4-core machine: min(32, 8) = 8 threads. If all 8 are occupied with blocking calls, the 9th to_thread() call will block the event loop waiting for a thread to free up. For high-concurrency workloads, create a custom pool and pass it to run_in_executor.","correct":"min(32, os.cpu_count() + 4) — this is the default ThreadPoolExecutor size in CPython.","distractors":["Low CPU + slow requests in an async service is the classic blocking-in-async signature.","Use asyncio.Queue(maxsize=N).","asyncio.to_thread(fn, *args) is syntactic sugar introduced in Python 3.9 for loop.run_in_executor(None, fn, *args)."]},{"id":"python/async_patterns_and_pitfalls#Q4-3026","section":"python","module":"python/async_patterns_and_pitfalls","moduleName":"async patterns and pitfalls","qIndex":4,"difficulty":"core","question":"What is the difference between asyncio.Semaphore and asyncio.Lock?","answerFull":"Lock allows exactly 1 concurrent holder. Semaphore(n) allows up to n concurrent holders. A Lock is a Semaphore(1). Use Semaphore to bound concurrency (e.g., max 20 DB connections at once); use Lock for mutual exclusion (e.g., protecting a shared counter or cache).","correct":"Lock allows exactly 1 concurrent holder.","distractors":["asyncio.to_thread(fn, *args) is syntactic sugar introduced in Python 3.9 for loop.run_in_executor(None, fn, *args).","Use asyncio.Queue(maxsize=N).","Calling a blocking synchronous function (like requests.get() or time.sleep()) inside an async def route — this stalls the entire event loop for the duration of the call."]},{"id":"python/async_patterns_and_pitfalls#Q5-3027","section":"python","module":"python/async_patterns_and_pitfalls","moduleName":"async patterns and pitfalls","qIndex":5,"difficulty":"core","question":"Why does unbounded asyncio.gather() over 10,000 URLs fail?","answerFull":"It creates 10,000 coroutines that all attempt to establish TCP connections simultaneously. This exhausts the OS file-descriptor limit (default 1024 on Linux), triggers 429 Too Many Requests from the target, and can crash the target service. Fix: wrap each coroutine with async with asyncio.Semaphore(50) to bound concurrency to 50 at a time.","correct":"It creates 10,000 coroutines that all attempt to establish TCP connections simultaneously.","distractors":["asyncio.to_thread(fn, *args) is syntactic sugar introduced in Python 3.9 for loop.run_in_executor(None, fn, *args).","Use asyncio.Queue(maxsize=N).","Calling a blocking synchronous function (like requests.get() or time.sleep()) inside an async def route — this stalls the entire event loop for the duration of the call."]},{"id":"python/async_patterns_and_pitfalls#Q6-3028","section":"python","module":"python/async_patterns_and_pitfalls","moduleName":"async patterns and pitfalls","qIndex":6,"difficulty":"intermediate","question":"What is jitter in retry logic, and why does it matter?","answerFull":"Jitter is a random delay added to the exponential backoff interval. Without jitter, N services that fail simultaneously all retry at the same intervals (1s, 2s, 4s, …), creating a thundering herd that repeatedly hammers the recovering service. Full jitter draws the delay from uniform(0, min(max_delay, base * 2^attempt)), spreading retries across the interval and reducing load on the target by a factor of N.","correct":"Jitter is a random delay added to the exponential backoff interval.","distractors":["Calling a blocking synchronous function (like requests.get() or time.sleep()) inside an async def route — this stalls the entire event loop for the duration of the call.","asyncio.to_thread(fn, *args) is syntactic sugar introduced in Python 3.9 for loop.run_in_executor(None, fn, *args).","min(32, os.cpu_count() + 4) — this is the default ThreadPoolExecutor size in CPython."]},{"id":"python/async_patterns_and_pitfalls#Q7-3029","section":"python","module":"python/async_patterns_and_pitfalls","moduleName":"async patterns and pitfalls","qIndex":7,"difficulty":"intermediate","question":"What is the difference between asyncio.timeout() and asyncio.wait_for()?","answerFull":"Both cancel the operation after a specified duration. asyncio.timeout() (3.11) is an async context manager, making it composable with nested context managers and try/except blocks. asyncio.wait_for() wraps a coroutine and must be called at the await site. asyncio.timeout() raises the built-in TimeoutError; asyncio.wait_for() raises asyncio.TimeoutError (which is a subclass of TimeoutError in 3.11+ but distinct in earlier versions).","correct":"Both cancel the operation after a specified duration.","distractors":["The Task object has no strong reference, so Python's garbage collector may collect and cancel it before it finishes.","Calling a blocking synchronous function (like requests.get() or time.sleep()) inside an async def route — this stalls the entire event loop for the duration of the call.","asyncio.to_thread(fn, *args) is syntactic sugar introduced in Python 3.9 for loop.run_in_executor(None, fn, *args)."]},{"id":"python/async_patterns_and_pitfalls#Q8-3030","section":"python","module":"python/async_patterns_and_pitfalls","moduleName":"async patterns and pitfalls","qIndex":8,"difficulty":"intermediate","question":"What happens when you call asyncio.create_task() but don't store the return value?","answerFull":"The Task object has no strong reference, so Python's garbage collector may collect and cancel it before it finishes. CPython logs a warning: \"Task was destroyed but it is pending!\". Fix: store the task in a module-level set and register a done_callback to discard it when complete — this keeps a strong reference for the task's lifetime without causing a leak.","correct":"The Task object has no strong reference, so Python's garbage collector may collect and cancel it before it finishes.","distractors":["Use asyncio.Queue(maxsize=N).","asyncio.to_thread(fn, *args) is syntactic sugar introduced in Python 3.9 for loop.run_in_executor(None, fn, *args).","Calling a blocking synchronous function (like requests.get() or time.sleep()) inside an async def route — this stalls the entire event loop for the duration of the call."]},{"id":"python/async_patterns_and_pitfalls#Q9-3031","section":"python","module":"python/async_patterns_and_pitfalls","moduleName":"async patterns and pitfalls","qIndex":9,"difficulty":"intermediate","question":"How do you implement backpressure in an async producer/consumer system?","answerFull":"Use asyncio.Queue(maxsize=N). The producer calls await queue.put(item), which suspends the producer coroutine when the queue is full (maxsize reached), applying backpressure. Consumer calls await queue.get() and queue.task_done(). The bounded queue acts as a buffer and flow-control mechanism between producers and consumers with different throughputs.","correct":"Use asyncio.Queue(maxsize=N).","distractors":["async for processes items sequentially from a single async generator — each item is awaited in turn.","asyncio.to_thread(fn, *args) is syntactic sugar introduced in Python 3.9 for loop.run_in_executor(None, fn, *args).","Low CPU + slow requests in an async service is the classic blocking-in-async signature."]},{"id":"python/async_patterns_and_pitfalls#Q10-3032","section":"python","module":"python/async_patterns_and_pitfalls","moduleName":"async patterns and pitfalls","qIndex":10,"difficulty":"intermediate","question":"What are the three states of a circuit breaker, and when does it transition between them?","answerFull":"CLOSED (normal): requests pass through; failure counter increments on exceptions. OPEN (fail-fast): after failure_threshold consecutive failures, the breaker opens; all requests immediately raise RuntimeError without calling the downstream service. HALF-OPEN (probing): after recovery_timeout seconds, one probe request is allowed through; if it succeeds the breaker closes; if it fails the breaker reopens with a fresh timeout.","correct":"CLOSED (normal): requests pass through; failure counter increments on exceptions.","distractors":["Calling a blocking synchronous function (like requests.get() or time.sleep()) inside an async def route — this stalls the entire event loop for the duration of the call.","Low CPU + slow requests in an async service is the classic blocking-in-async signature.","asyncio.to_thread(fn, *args) is syntactic sugar introduced in Python 3.9 for loop.run_in_executor(None, fn, *args)."]},{"id":"python/async_patterns_and_pitfalls#Q11-3033","section":"python","module":"python/async_patterns_and_pitfalls","moduleName":"async patterns and pitfalls","qIndex":11,"difficulty":"advanced","question":"How do async generators differ from regular generators, and when should you use them?","answerFull":"Regular generators use yield and are consumed with a synchronous for loop. Async generators use yield inside async def and are consumed with async for, meaning each yield point can suspend at an await call inside the generator body. Use async generators for lazy I/O streaming: HTTP pagination, database cursor iteration, log tailing — anywhere you want to process items as they arrive without loading all into memory first.","correct":"Regular generators use yield and are consumed with a synchronous for loop.","distractors":["Calling a blocking synchronous function (like requests.get() or time.sleep()) inside an async def route — this stalls the entire event loop for the duration of the call.","asyncio.to_thread(fn, *args) is syntactic sugar introduced in Python 3.9 for loop.run_in_executor(None, fn, *args).","Low CPU + slow requests in an async service is the classic blocking-in-async signature."]},{"id":"python/async_patterns_and_pitfalls#Q13-3034","section":"python","module":"python/async_patterns_and_pitfalls","moduleName":"async patterns and pitfalls","qIndex":13,"difficulty":"advanced","question":"How would you debug a FastAPI service where all requests are slow but CPU usage is low?","answerFull":"Low CPU + slow requests in an async service is the classic blocking-in-async signature. Steps: (1) enable loop.set_debug(True) and watch for slow-callback warnings; (2) add yappi profiling with clock_type=WALL to see which coroutines have high wall time; (3) search the codebase for sync I/O calls (requests., open(, time.sleep) inside async def functions; (4) check for sync def routes that might be saturating the default Starlette thread pool (default: 40 threads).","correct":"Low CPU + slow requests in an async service is the classic blocking-in-async signature.","distractors":["min(32, os.cpu_count() + 4) — this is the default ThreadPoolExecutor size in CPython.","CLOSED (normal): requests pass through; failure counter increments on exceptions.","Calling a blocking synchronous function (like requests.get() or time.sleep()) inside an async def route — this stalls the entire event loop for the duration of the call."]},{"id":"python/async_patterns_and_pitfalls#Q14-3035","section":"python","module":"python/async_patterns_and_pitfalls","moduleName":"async patterns and pitfalls","qIndex":14,"difficulty":"advanced","question":"What is the difference between async for and asyncio.gather() for consuming multiple async sources?","answerFull":"async for processes items sequentially from a single async generator — each item is awaited in turn. asyncio.gather() runs multiple coroutines concurrently, collecting all results when all complete. Use async for when you need ordered, lazy streaming from one source. Use gather() (with a semaphore) when you want to fan out to many sources simultaneously and collect results. Combining both: use an async generator as a lazy source, then spawn bounded concurrent consumers with gather().","correct":"async for processes items sequentially from a single async generator — each item is awaited in turn.","distractors":["Calling a blocking synchronous function (like requests.get() or time.sleep()) inside an async def route — this stalls the entire event loop for the duration of the call.","asyncio.to_thread(fn, *args) is syntactic sugar introduced in Python 3.9 for loop.run_in_executor(None, fn, *args).","Use asyncio.Queue(maxsize=N)."]},{"id":"python/async_patterns_and_pitfalls#Q15-3036","section":"python","module":"python/async_patterns_and_pitfalls","moduleName":"async patterns and pitfalls","qIndex":15,"difficulty":"advanced","question":"How do you safely propagate context (e.g., request IDs, auth tokens) across await boundaries in asyncio?","answerFull":"Use contextvars.ContextVar. Unlike threading.local, ContextVar values are inherited by child tasks (copies of the context are made at asyncio.create_task() time). Set the value at the start of a request, and it is accessible in all coroutines spawned within that request's scope without passing it explicitly. Example: request_id: ContextVar[str] = ContextVar(\"request_id\"). This is how Sentry and OpenTelemetry propagate trace context across async calls.","correct":"Use contextvars.ContextVar.","distractors":["asyncio.to_thread(fn, *args) is syntactic sugar introduced in Python 3.9 for loop.run_in_executor(None, fn, *args).","Use asyncio.Queue(maxsize=N).","Calling a blocking synchronous function (like requests.get() or time.sleep()) inside an async def route — this stalls the entire event loop for the duration of the call."]},{"id":"python/cpython_memory_model#Q1-3037","section":"python","module":"python/cpython_memory_model","moduleName":"cpython memory model","qIndex":1,"difficulty":"core","question":"What is the PyObject C struct and why does it matter for Python memory?","answerFull":"Every Python object begins with ob_refcnt (8 bytes, reference count) and ob_type (8 bytes, pointer to the type), for a minimum overhead of 16 bytes per object. Sequences add ob_size for 24 bytes minimum. Because even int(0) is a 24–28 byte heap allocation, Python is significantly more memory-intensive than C for numeric-heavy workloads. Practical guidance: use numpy arrays or array.array for bulk numeric storage to avoid per-element PyObject overhead.","correct":"Every Python object begins with ob_refcnt (8 bytes, reference count) and ob_type (8 bytes, pointer to the type), for a minimum overhead of 16 bytes per object.","distractors":["Check gc.garbage — Python < 3.4 places uncollectable cycles (those containing del) here.","getrefcount receives x as a function argument, which creates a temporary reference in the call frame, incrementing the count by 1 above the count you would expect.","sizeof() returns the raw size of the object without GC bookkeeping overhead."]},{"id":"python/cpython_memory_model#Q2-3038","section":"python","module":"python/cpython_memory_model","moduleName":"cpython memory model","qIndex":2,"difficulty":"core","question":"Why does sys.getrefcount(x) always return at least 2?","answerFull":"getrefcount receives x as a function argument, which creates a temporary reference in the call frame, incrementing the count by 1 above the count you would expect. The count reported is therefore always \"true count + 1\". Practical guidance: subtract 1 from the result when interpreting refcounts from getrefcount.","correct":"getrefcount receives x as a function argument, which creates a temporary reference in the call frame, incrementing the count by 1 above the count you would expect.","distractors":["tracemalloc hooks into PyMem_Malloc and PyMem_RawMalloc to record each allocation's size and up to N frames of the call stack (configured by tracemalloc.start(N)).","The type's tp_dealloc function is called synchronously, immediately freeing the object's memory back to the pool (or OS for large objects).","A weakref.ref stores a reference that does not increment the referent's refcount."]},{"id":"python/cpython_memory_model#Q3-3039","section":"python","module":"python/cpython_memory_model","moduleName":"cpython memory model","qIndex":3,"difficulty":"core","question":"What happens when ob_refcnt reaches zero?","answerFull":"The type's tp_dealloc function is called synchronously, immediately freeing the object's memory back to the pool (or OS for large objects). There is no scheduling delay, no GC pause, and no background thread involved — deallocation is part of the Py_DECREF macro itself. Practical guidance: destructor code in tp_dealloc (e.g., del) runs synchronously on the decrementing thread, which can cause latency spikes if destructors are slow.","correct":"The type's tp_dealloc function is called synchronously, immediately freeing the object's memory back to the pool (or OS for large objects).","distractors":["Gen0 is for newly allocated objects; it is collected when the difference between new allocations and deallocations exceeds 700 (default).","sizeof() returns the raw size of the object without GC bookkeeping overhead.","gc.freeze() moves all currently tracked objects into Gen2 and marks them as permanently frozen — they are never scanned again by the GC."]},{"id":"python/cpython_memory_model#Q4-3040","section":"python","module":"python/cpython_memory_model","moduleName":"cpython memory model","qIndex":4,"difficulty":"core","question":"What kinds of objects can form reference cycles and why can't refcounting handle them?","answerFull":"Any mutable container that can hold references to other objects — list, dict, set, and user-defined instances — can form cycles. In a cycle, each object's refcount is kept above zero by the other objects in the cycle, even when no external code references any of them. Refcounting requires the count to reach zero before freeing, which never happens in a cycle. Practical guidance: avoid long-lived cycles in high-throughput services; use the cyclic GC (keep it enabled) or restructure with weakrefs.","correct":"Any mutable container that can hold references to other objects — list, dict, set, and user-defined instances — can form cycles.","distractors":["Interning means reusing a single canonical instance for all occurrences of a value instead of allocating separate objects.","gc.freeze() moves all currently tracked objects into Gen2 and marks them as permanently frozen — they are never scanned again by the GC.","The type's tp_dealloc function is called synchronously, immediately freeing the object's memory back to the pool (or OS for large objects)."]},{"id":"python/cpython_memory_model#Q5-3041","section":"python","module":"python/cpython_memory_model","moduleName":"cpython memory model","qIndex":5,"difficulty":"core","question":"Describe the three generations of the CPython cyclic GC and their default thresholds.","answerFull":"Gen0 is for newly allocated objects; it is collected when the difference between new allocations and deallocations exceeds 700 (default). Survivors are promoted to Gen1, collected after 10 Gen0 collections. Gen1 survivors go to Gen2, collected after 10 Gen1 collections. This reflects the generational hypothesis: most objects die young, so collecting young objects frequently is efficient. Practical guidance: use gc.freeze() after module loading to pin long-lived objects into Gen2 and exclude them from future scans.","correct":"Gen0 is for newly allocated objects; it is collected when the difference between new allocations and deallocations exceeds 700 (default).","distractors":["Any mutable container that can hold references to other objects — list, dict, set, and user-defined instances — can form cycles.","Interning means reusing a single canonical instance for all occurrences of a value instead of allocating separate objects.","gc.freeze() moves all currently tracked objects into Gen2 and marks them as permanently frozen — they are never scanned again by the GC."]},{"id":"python/cpython_memory_model#Q6-3042","section":"python","module":"python/cpython_memory_model","moduleName":"cpython memory model","qIndex":6,"difficulty":"intermediate","question":"Explain CPython's pymalloc allocator hierarchy: arenas, pools, and blocks.","answerFull":"Arenas are 256 KB regions obtained from the OS. Each arena is divided into 4 KB pools. Each pool serves blocks of exactly one size class (8, 16, 24, ... 512 bytes). Allocation is a pointer bump in the current pool's free-list — very fast. Freed blocks are returned to the pool's free-list, NOT to the OS. An arena is returned to the OS only when all of its pools are fully empty. Practical guidance: allocation spikes inflate RSS permanently (within the process lifetime) because partially-used arenas are held even when blocks are freed.","correct":"Arenas are 256 KB regions obtained from the OS.","distractors":["Every Python object begins with ob_refcnt (8 bytes, reference count) and ob_type (8 bytes, pointer to the type), for a minimum overhead of 16 bytes per object.","getrefcount receives x as a function argument, which creates a temporary reference in the call frame, incrementing the count by 1 above the count you would expect.","The type's tp_dealloc function is called synchronously, immediately freeing the object's memory back to the pool (or OS for large objects)."]},{"id":"python/cpython_memory_model#Q7-3043","section":"python","module":"python/cpython_memory_model","moduleName":"cpython memory model","qIndex":7,"difficulty":"intermediate","question":"What is object interning and which objects are interned by default in CPython?","answerFull":"Interning means reusing a single canonical instance for all occurrences of a value instead of allocating separate objects. By default, CPython interns integers from -5 to 256 (as pre-allocated singletons) and many compile-time string literals that look like identifiers. None, True, and False are always singletons. Practical guidance: use is None for None checks; use == for all other equality comparisons because interning behavior is an implementation detail not guaranteed by the language spec.","correct":"Interning means reusing a single canonical instance for all occurrences of a value instead of allocating separate objects.","distractors":["gc.freeze() moves all currently tracked objects into Gen2 and marks them as permanently frozen — they are never scanned again by the GC.","The type's tp_dealloc function is called synchronously, immediately freeing the object's memory back to the pool (or OS for large objects).","sizeof() returns the raw size of the object without GC bookkeeping overhead."]},{"id":"python/cpython_memory_model#Q8-3044","section":"python","module":"python/cpython_memory_model","moduleName":"cpython memory model","qIndex":8,"difficulty":"intermediate","question":"What is the difference between sys.getsizeof() and sizeof()?","answerFull":"sizeof() returns the raw size of the object without GC bookkeeping overhead. sys.getsizeof() calls sizeof() and adds 24 bytes for the GC header on objects tracked by the cyclic GC. Both are shallow — they do not recurse into referenced objects. Practical guidance: for accurate total memory, use pympler.asizeof or a recursive deep_size() function; never use getsizeof alone to size a nested data structure.","correct":"sizeof() returns the raw size of the object without GC bookkeeping overhead.","distractors":["tracemalloc hooks into PyMem_Malloc and PyMem_RawMalloc to record each allocation's size and up to N frames of the call stack (configured by tracemalloc.start(N)).","del x removes the name binding from the current namespace and decrements the refcount of the bound object by 1.","The type's tp_dealloc function is called synchronously, immediately freeing the object's memory back to the pool (or OS for large objects)."]},{"id":"python/cpython_memory_model#Q9-3045","section":"python","module":"python/cpython_memory_model","moduleName":"cpython memory model","qIndex":9,"difficulty":"intermediate","question":"How does tracemalloc work and what are its limitations?","answerFull":"tracemalloc hooks into PyMem_Malloc and PyMem_RawMalloc to record each allocation's size and up to N frames of the call stack (configured by tracemalloc.start(N)). It can take snapshots and compute diffs to isolate allocations introduced between two points in time. Its limitation is that it cannot see C-extension allocations that bypass CPython's memory API — numpy internal buffers, for example, are invisible to tracemalloc. Practical guidance: use tracemalloc.compare_to between a baseline and a suspected leak window; look at lineno statistics to find the exact line responsible.","correct":"tracemalloc hooks into PyMem_Malloc and PyMem_RawMalloc to record each allocation's size and up to N frames of the call stack (configured by tracemalloc.start(N)).","distractors":["sizeof() returns the raw size of the object without GC bookkeeping overhead.","getrefcount receives x as a function argument, which creates a temporary reference in the call frame, incrementing the count by 1 above the count you would expect.","Every Python object begins with ob_refcnt (8 bytes, reference count) and ob_type (8 bytes, pointer to the type), for a minimum overhead of 16 bytes per object."]},{"id":"python/cpython_memory_model#Q10-3046","section":"python","module":"python/cpython_memory_model","moduleName":"cpython memory model","qIndex":10,"difficulty":"intermediate","question":"What is a weak reference and when should you use one instead of a strong reference?","answerFull":"A weakref.ref stores a reference that does not increment the referent's refcount. If the referent has no remaining strong references, it is collected and the weak reference becomes None. Use weakrefs in caches (so cached objects can be collected when no one else holds them), in observer/listener registries (so listeners don't pin emitters), and to break deliberate cycles. Do not use weakrefs for objects whose lifetime you actively need to control, or for types that don't support weak references (built-in int, str, list do NOT support weakrefs without subclassing). Practical guidance: use weakref.WeakValueDictionary for value-caches and weakref.WeakKeyDictionary for metadata attached to foreign objects.","correct":"A weakref.ref stores a reference that does not increment the referent's refcount.","distractors":["Every Python object begins with ob_refcnt (8 bytes, reference count) and ob_type (8 bytes, pointer to the type), for a minimum overhead of 16 bytes per object.","Interning means reusing a single canonical instance for all occurrences of a value instead of allocating separate objects.","del x removes the name binding from the current namespace and decrements the refcount of the bound object by 1."]},{"id":"python/cpython_memory_model#Q11-3047","section":"python","module":"python/cpython_memory_model","moduleName":"cpython memory model","qIndex":11,"difficulty":"advanced","question":"What is gc.freeze() and when should you call it?","answerFull":"gc.freeze() moves all currently tracked objects into Gen2 and marks them as permanently frozen — they are never scanned again by the GC. This is useful after full module initialization: all module-level objects (class definitions, constants, global dicts) will never be GC'd, so excluding them from future scans reduces GC work. Gunicorn calls gc.freeze() after loading the application before forking workers, which also makes the frozen pages copy-on-write friendly. Practical guidance: call gc.freeze() once, after imports and application initialization, before serving traffic.","correct":"gc.freeze() moves all currently tracked objects into Gen2 and marks them as permanently frozen — they are never scanned again by the GC.","distractors":["getrefcount receives x as a function argument, which creates a temporary reference in the call frame, incrementing the count by 1 above the count you would expect.","Any mutable container that can hold references to other objects — list, dict, set, and user-defined instances — can form cycles.","Interning means reusing a single canonical instance for all occurrences of a value instead of allocating separate objects."]},{"id":"python/cpython_memory_model#Q12-3048","section":"python","module":"python/cpython_memory_model","moduleName":"cpython memory model","qIndex":12,"difficulty":"advanced","question":"How does the GIL interact with reference counting?","answerFull":"The Global Interpreter Lock ensures that only one thread executes Python bytecode at a time. Because Py_INCREF and Py_DECREF are not atomic — they are plain C integer operations — the GIL prevents two threads from corrupting an object's refcount simultaneously. In free-threaded CPython 3.13 (--disable-gil), biased reference counting is used: each object has a per-thread cache of refcount deltas that are merged periodically, avoiding contention. Practical guidance: do not rely on refcount atomicity in C extensions that release the GIL; use Py_INCREF/Py_DECREF only while holding the GIL.","correct":"The Global Interpreter Lock ensures that only one thread executes Python bytecode at a time.","distractors":["getrefcount receives x as a function argument, which creates a temporary reference in the call frame, incrementing the count by 1 above the count you would expect.","A weakref.ref stores a reference that does not increment the referent's refcount.","Python's int is PyLongObject, an arbitrary-precision integer stored as an array of 30-bit \"digits\" plus the standard 24-byte PyVarObject header."]},{"id":"python/cpython_memory_model#Q13-3049","section":"python","module":"python/cpython_memory_model","moduleName":"cpython memory model","qIndex":13,"difficulty":"advanced","question":"Why is Python's int not a C int, and what are the memory implications?","answerFull":"Python's int is PyLongObject, an arbitrary-precision integer stored as an array of 30-bit \"digits\" plus the standard 24-byte PyVarObject header. Even int(0) is 24 bytes on CPython 3.11 64-bit. A C int is 4 bytes. For numerical work, this is a 6x overhead. Practical guidance: use array.array('l', ...) for large lists of fixed-width integers, or numpy arrays, to avoid per-element PyLongObject overhead.","correct":"Python's int is PyLongObject, an arbitrary-precision integer stored as an array of 30-bit \"digits\" plus the standard 24-byte PyVarObject header.","distractors":["Every Python object begins with ob_refcnt (8 bytes, reference count) and ob_type (8 bytes, pointer to the type), for a minimum overhead of 16 bytes per object.","The type's tp_dealloc function is called synchronously, immediately freeing the object's memory back to the pool (or OS for large objects).","Check gc.garbage — Python < 3.4 places uncollectable cycles (those containing del) here."]},{"id":"python/cpython_memory_model#Q14-3050","section":"python","module":"python/cpython_memory_model","moduleName":"cpython memory model","qIndex":14,"difficulty":"advanced","question":"How can you detect that a finalizer (del) is preventing cycle collection?","answerFull":"Check gc.garbage — Python < 3.4 places uncollectable cycles (those containing del) here. In Python 3.4+, the list is populated only by objects that raise exceptions in del. More practically, use gc.set_debug(gc.DEBUG_UNCOLLECTABLE) to print uncollectable objects to stderr. Then use gc.get_referrers(obj) to find what holds a reference to the suspect object. Practical guidance: prefer weakref.finalize over del to keep cleanup logic decoupled from the GC cycle-breaking algorithm.","correct":"Check gc.garbage — Python < 3.4 places uncollectable cycles (those containing del) here.","distractors":["The Global Interpreter Lock ensures that only one thread executes Python bytecode at a time.","del x removes the name binding from the current namespace and decrements the refcount of the bound object by 1.","Every Python object begins with ob_refcnt (8 bytes, reference count) and ob_type (8 bytes, pointer to the type), for a minimum overhead of 16 bytes per object."]},{"id":"python/cpython_memory_model#Q15-3051","section":"python","module":"python/cpython_memory_model","moduleName":"cpython memory model","qIndex":15,"difficulty":"advanced","question":"What is the behavior of del x versus setting x = None?","answerFull":"del x removes the name binding from the current namespace and decrements the refcount of the bound object by 1. If the refcount reaches zero, the object is immediately freed. x = None rebinds the name to the None singleton and also decrements the original object's refcount by 1. The memory effect is identical as long as no other name references the original object. The difference is that after del x, accessing x raises NameError; after x = None, x is still accessible as None. Practical guidance: use del when you want to release a large object AND communicate to readers that the variable is no longer valid; use = None when the variable must remain in scope (e.g., a loop variable that needs to be checked afterward).","correct":"del x removes the name binding from the current namespace and decrements the refcount of the bound object by 1.","distractors":["A weakref.ref stores a reference that does not increment the referent's refcount.","Check gc.garbage — Python < 3.4 places uncollectable cycles (those containing del) here.","sizeof() returns the raw size of the object without GC bookkeeping overhead."]},{"id":"python/iterators_and_generators#Q1-3052","section":"python","module":"python/iterators_and_generators","moduleName":"iterators and generators","qIndex":1,"difficulty":"core","question":"What is the difference between an iterable and an iterator?","answerFull":"An iterable implements iter() and returns an iterator. An iterator implements both iter() (returning self) and next() (returning the next item or raising StopIteration). A list is iterable but not itself an iterator; calling iter(lst) produces a list_iterator which is an iterator. You can call iter() on a list multiple times to get independent traversals; you cannot on an iterator.","correct":"An iterable implements iter() and returns an iterator.","distractors":["groupby(iterable, key) yields consecutive groups — (key_value, group_iterator) pairs for runs of consecutive elements with the same key.","tee(it, n) creates n independent iterators from a single iterable.","yield from transparently delegates send(), throw(), and close() into the sub-generator, and captures its StopIteration.value as the expression result."]},{"id":"python/iterators_and_generators#Q2-3053","section":"python","module":"python/iterators_and_generators","moduleName":"iterators and generators","qIndex":2,"difficulty":"core","question":"What happens when you call a generator function?","answerFull":"No code inside the function runs. Calling a generator function returns a generator object immediately. Execution begins only when next() is called on the generator object. The function body runs until it hits a yield, at which point the yielded value is returned to the caller and the function suspends, preserving all local variables and execution position in its internal frame.","correct":"No code inside the function runs.","distractors":["groupby(iterable, key) yields consecutive groups — (key_value, group_iterator) pairs for runs of consecutive elements with the same key.","Before async/await (Python 3.5), coroutines were written as generator functions using yield and yield from.","send(value) resumes the generator and delivers value as the result of the currently-suspended yield expression inside the generator."]},{"id":"python/iterators_and_generators#Q3-3054","section":"python","module":"python/iterators_and_generators","moduleName":"iterators and generators","qIndex":3,"difficulty":"core","question":"What does yield from do that a simple for x in sub: yield x loop does not?","answerFull":"yield from transparently delegates send(), throw(), and close() into the sub-generator, and captures its StopIteration.value as the expression result. A manual for loop discards sent values (the sub-generator's yield expressions always receive None) and does not propagate throw() or close() into the sub-generator. This transparent delegation is how await is implemented — await coro desugars to yield from coro.","correct":"yield from transparently delegates send(), throw(), and close() into the sub-generator, and captures its StopIteration.value as the expression result.","distractors":["send(value) resumes the generator and delivers value as the result of the currently-suspended yield expression inside the generator.","Use a try/finally block inside the generator.","Generator expressions cannot use send(), throw(), or close() meaningfully — they have no yield that can receive a sent value."]},{"id":"python/iterators_and_generators#Q4-3055","section":"python","module":"python/iterators_and_generators","moduleName":"iterators and generators","qIndex":4,"difficulty":"core","question":"Explain PEP 479. What problem does it solve?","answerFull":"PEP 479, enforced from Python 3.7, converts any StopIteration exception that escapes a generator body into RuntimeError. Before this, if a helper called inside a generator raised StopIteration unexpectedly (for example, calling next() on an exhausted iterator), it would silently terminate the for loop consuming the generator — a very hard-to-debug bug. The fix is to never raise StopIteration inside a generator; use return instead, which raises StopIteration safely through the generator machinery.","correct":"PEP 479, enforced from Python 3.7, converts any StopIteration exception that escapes a generator body into RuntimeError.","distractors":["Generator expressions cannot use send(), throw(), or close() meaningfully — they have no yield that can receive a sent value.","Use a try/finally block inside the generator.","A list comprehension evaluates all elements eagerly and stores them in a contiguous array in memory — 10 million Python int objects, each 28 bytes, plus 8-byte pointers in the list."]},{"id":"python/iterators_and_generators#Q5-3056","section":"python","module":"python/iterators_and_generators","moduleName":"iterators and generators","qIndex":5,"difficulty":"core","question":"How does .send(value) work? What is required before the first send()?","answerFull":"send(value) resumes the generator and delivers value as the result of the currently-suspended yield expression inside the generator. Before the first send(), the generator has not yet reached any yield, so there is no suspended expression to deliver a value to. Therefore, the first call must be next(gen) or gen.send(None) to advance to the first yield. Sending a non-None value before priming raises TypeError: can't send non-None value to a just-started generator.","correct":"send(value) resumes the generator and delivers value as the result of the currently-suspended yield expression inside the generator.","distractors":["PEP 479, enforced from Python 3.7, converts any StopIteration exception that escapes a generator body into RuntimeError.","Generator expressions cannot use send(), throw(), or close() meaningfully — they have no yield that can receive a sent value.","Before async/await (Python 3.5), coroutines were written as generator functions using yield and yield from."]},{"id":"python/iterators_and_generators#Q6-3057","section":"python","module":"python/iterators_and_generators","moduleName":"iterators and generators","qIndex":6,"difficulty":"intermediate","question":"Why does a list comprehension use ~85 MB for 10 million integers while a generator expression uses 128 bytes?","answerFull":"A list comprehension evaluates all elements eagerly and stores them in a contiguous array in memory — 10 million Python int objects, each 28 bytes, plus 8-byte pointers in the list. A generator expression is a generator object — a thin wrapper around a code object and a frame with a few variables. Values are computed on demand and never stored. The generator object size stays constant at ~128 bytes regardless of how many elements it will eventually yield.","correct":"A list comprehension evaluates all elements eagerly and stores them in a contiguous array in memory — 10 million Python int objects, each 28 bytes, plus 8-byte pointers in the list.","distractors":["Before async/await (Python 3.5), coroutines were written as generator functions using yield and yield from.","Use a try/finally block inside the generator.","Generator expressions cannot use send(), throw(), or close() meaningfully — they have no yield that can receive a sent value."]},{"id":"python/iterators_and_generators#Q7-3058","section":"python","module":"python/iterators_and_generators","moduleName":"iterators and generators","qIndex":7,"difficulty":"intermediate","question":"When would you use itertools.tee() and what is the hidden cost?","answerFull":"tee(it, n) creates n independent iterators from a single iterable. Use it when you need to make multiple passes through a one-pass iterator (generator) without materialising it. The hidden cost: internally, tee buffers any elements that one copy has consumed but the other has not yet seen. If one copy races far ahead of the other, the buffer can grow to O(n) in memory — potentially as large as a materialised list. If both copies are consumed in lock-step, the buffer stays small.","correct":"tee(it, n) creates n independent iterators from a single iterable.","distractors":["An iterable implements iter() and returns an iterator.","groupby(iterable, key) yields consecutive groups — (key_value, group_iterator) pairs for runs of consecutive elements with the same key.","yield from transparently delegates send(), throw(), and close() into the sub-generator, and captures its StopIteration.value as the expression result."]},{"id":"python/iterators_and_generators#Q8-3059","section":"python","module":"python/iterators_and_generators","moduleName":"iterators and generators","qIndex":8,"difficulty":"intermediate","question":"What is the generator-based coroutine pattern and how does it relate to async/await?","answerFull":"Before async/await (Python 3.5), coroutines were written as generator functions using yield and yield from. The event loop would send() futures into generators, which would yield from them. await expr in Python 3.5+ is syntactic sugar for yield from expr with additional restrictions (only awaitable objects allowed). Async generators (Python 3.6+) combine async def with yield and are iterated using async for.","correct":"Before async/await (Python 3.5), coroutines were written as generator functions using yield and yield from.","distractors":["PEP 479, enforced from Python 3.7, converts any StopIteration exception that escapes a generator body into RuntimeError.","send(value) resumes the generator and delivers value as the result of the currently-suspended yield expression inside the generator.","A list comprehension evaluates all elements eagerly and stores them in a contiguous array in memory — 10 million Python int objects, each 28 bytes, plus 8-byte pointers in the list."]},{"id":"python/iterators_and_generators#Q9-3060","section":"python","module":"python/iterators_and_generators","moduleName":"iterators and generators","qIndex":9,"difficulty":"intermediate","question":"How do you handle cleanup (releasing resources) in a generator?","answerFull":"Use a try/finally block inside the generator. When gen.close() is called, Python injects a GeneratorExit exception at the suspended yield. The finally block runs whether the generator is closed early or exhausted normally. This guarantees file handles, database connections, and locks are released even if the caller stops iterating before the generator is exhausted.","correct":"Use a try/finally block inside the generator.","distractors":["PEP 479, enforced from Python 3.7, converts any StopIteration exception that escapes a generator body into RuntimeError.","yield from transparently delegates send(), throw(), and close() into the sub-generator, and captures its StopIteration.value as the expression result.","send(value) resumes the generator and delivers value as the result of the currently-suspended yield expression inside the generator."]},{"id":"python/iterators_and_generators#Q10-3061","section":"python","module":"python/iterators_and_generators","moduleName":"iterators and generators","qIndex":10,"difficulty":"intermediate","question":"What are the limitations of generator expressions compared to generator functions?","answerFull":"Generator expressions cannot use send(), throw(), or close() meaningfully — they have no yield that can receive a sent value. They do not support multi-line logic, cannot have try/finally for cleanup, and are harder to debug (no function name, limited tracebacks). Use a generator function when you need bidirectional communication, cleanup guarantees, complex state, or readable multi-step logic.","correct":"Generator expressions cannot use send(), throw(), or close() meaningfully — they have no yield that can receive a sent value.","distractors":["Before async/await (Python 3.5), coroutines were written as generator functions using yield and yield from.","yield from transparently delegates send(), throw(), and close() into the sub-generator, and captures its StopIteration.value as the expression result.","Use a try/finally block inside the generator."]},{"id":"python/iterators_and_generators#Q11-3062","section":"python","module":"python/iterators_and_generators","moduleName":"iterators and generators","qIndex":11,"difficulty":"advanced","question":"Can a generator be used with len()? How do you count elements efficiently?","answerFull":"No. len() raises TypeError: object of type 'generator' has no len(). Generators are lazy and do not know their length in advance. To count elements: sum(1 for _ in gen) — this consumes the generator in O(1) memory. If you need both length and elements, materialise to a list first: items = list(gen); n = len(items).","correct":"No. len() raises TypeError: object of type 'generator' has no len().","distractors":["groupby(iterable, key) yields consecutive groups — (key_value, group_iterator) pairs for runs of consecutive elements with the same key.","yield from transparently delegates send(), throw(), and close() into the sub-generator, and captures its StopIteration.value as the expression result.","Generator expressions cannot use send(), throw(), or close() meaningfully — they have no yield that can receive a sent value."]},{"id":"python/iterators_and_generators#Q12-3063","section":"python","module":"python/iterators_and_generators","moduleName":"iterators and generators","qIndex":12,"difficulty":"advanced","question":"What is itertools.groupby and what is the most common mistake when using it?","answerFull":"groupby(iterable, key) yields consecutive groups — (key_value, group_iterator) pairs for runs of consecutive elements with the same key. The most common mistake is using it on unsorted data: groupby only groups consecutive equal-key elements, so [A, B, A] produces three groups (A, B, A), not two. Always sort by the key before calling groupby. The second common mistake is exhausting the group iterator before moving to the next key — each group iterator becomes invalid when groupby advances to the next key, so you must consume or materialise it immediately.","correct":"groupby(iterable, key) yields consecutive groups — (key_value, group_iterator) pairs for runs of consecutive elements with the same key.","distractors":["tee(it, n) creates n independent iterators from a single iterable.","A list comprehension evaluates all elements eagerly and stores them in a contiguous array in memory — 10 million Python int objects, each 28 bytes, plus 8-byte pointers in the list.","An iterable implements iter() and returns an iterator."]},{"id":"python/design_patterns_in_python#Q1-3064","section":"python","module":"python/design_patterns_in_python","moduleName":"design patterns in python","qIndex":1,"difficulty":"core","question":"Why is a Python module the preferred Singleton implementation over the classic _instance class variable pattern?","answerFull":"A module is initialized exactly once by the import machinery and cached in sys.modules; subsequent imports return the same module object. The classic _instance pattern requires explicit locking to be thread-safe, adding boilerplate that the import system already handles for free.","correct":"A module is initialized exactly once by the import machinery and cached in sys.modules; subsequent imports return the same module object.","distractors":["Borg is useful when external code passes your class to APIs that call the constructor directly (e.g., plugin systems, ORM metaclasses).","Template Method defines a fixed algorithm skeleton in a base class, with abstract hook methods that subclasses override.","Any Python callable — a function, a lambda, a class with call, a functools.partial — satisfies Callable[[InputType], OutputType]."]},{"id":"python/design_patterns_in_python#Q2-3065","section":"python","module":"python/design_patterns_in_python","moduleName":"design patterns in python","qIndex":2,"difficulty":"core","question":"When would you choose the Borg pattern over the module singleton?","answerFull":"Borg is useful when external code passes your class to APIs that call the constructor directly (e.g., plugin systems, ORM metaclasses). All constructed instances share dict, so they behave as one, but type checks like isinstance still pass for each. If you control all call sites, prefer the module singleton.","correct":"Borg is useful when external code passes your class to APIs that call the constructor directly (e.g., plugin systems, ORM metaclasses).","distractors":["New product types register themselves via the @register(\"name\") decorator at module load time.","They are the same pattern at different abstraction levels.","Template Method defines a fixed algorithm skeleton in a base class, with abstract hook methods that subclasses override."]},{"id":"python/design_patterns_in_python#Q3-3066","section":"python","module":"python/design_patterns_in_python","moduleName":"design patterns in python","qIndex":3,"difficulty":"core","question":"How does Python's callable protocol eliminate the need for a Strategy interface class?","answerFull":"Any Python callable — a function, a lambda, a class with call, a functools.partial — satisfies Callable[[InputType], OutputType]. The Strategy pattern's only requirement is that behavior is swappable; Python's type system expresses this directly without a marker interface or abstract base class.","correct":"Any Python callable — a function, a lambda, a class with call, a functools.partial — satisfies Callable[[InputType], OutputType].","distractors":["Template Method defines a fixed algorithm skeleton in a base class, with abstract hook methods that subclasses override.","Lambdas defined inline have no name binding holding a strong reference — the WeakSet is the only reference, so the lambda is collected immediately.","Command encapsulates a call as a first-class object with execute() and undo()."]},{"id":"python/design_patterns_in_python#Q4-3067","section":"python","module":"python/design_patterns_in_python","moduleName":"design patterns in python","qIndex":4,"difficulty":"core","question":"Why should WeakSet be used for Observer listener registries?","answerFull":"If the event bus holds a strong reference to each listener, listener objects will not be garbage-collected even after the subscriber (the object owning the listener method) goes out of scope. This leaks memory proportional to the number of transient subscribers. WeakSet holds a weak reference so the GC can reclaim the listener when the subscriber is no longer referenced by anything else.","correct":"If the event bus holds a strong reference to each listener, listener objects will not be garbage-collected even after the subscriber (the object owning the listener method) goes out of scope.","distractors":["Command encapsulates a call as a first-class object with execute() and undo().","A module is initialized exactly once by the import machinery and cached in sys.modules; subsequent imports return the same module object.","Lambdas defined inline have no name binding holding a strong reference — the WeakSet is the only reference, so the lambda is collected immediately."]},{"id":"python/design_patterns_in_python#Q5-3068","section":"python","module":"python/design_patterns_in_python","moduleName":"design patterns in python","qIndex":5,"difficulty":"core","question":"What is the difference between the Python @decorator syntax and the GoF Decorator pattern?","answerFull":"They are the same pattern at different abstraction levels. GoF Decorator wraps an object to add behavior while preserving the interface. Python's @ syntax wraps a callable (function or class) to add behavior while preserving the callable contract. functools.lru_cache is the clearest example: it wraps a function, intercepts calls, and adds caching — pure GoF Decorator, zero classes required.","correct":"They are the same pattern at different abstraction levels.","distractors":["The @register decorator requires the registering module to be imported before the factory is called — order of imports matters.","A module is initialized exactly once by the import machinery and cached in sys.modules; subsequent imports return the same module object.","Any Python callable — a function, a lambda, a class with call, a functools.partial — satisfies Callable[[InputType], OutputType]."]},{"id":"python/design_patterns_in_python#Q6-3069","section":"python","module":"python/design_patterns_in_python","moduleName":"design patterns in python","qIndex":6,"difficulty":"intermediate","question":"How does the Factory Registry pattern keep code open for extension and closed for modification?","answerFull":"New product types register themselves via the @register(\"name\") decorator at module load time. The factory(name) function does a dict lookup — it never needs to change when a new type is added. The if kind == \"A\" dispatch block that requires editing for each new type is replaced by a one-line registration at the point of definition.","correct":"New product types register themselves via the @register(\"name\") decorator at module load time.","distractors":["The @register decorator requires the registering module to be imported before the factory is called — order of imports matters.","A module is initialized exactly once by the import machinery and cached in sys.modules; subsequent imports return the same module object.","Borg is useful when external code passes your class to APIs that call the constructor directly (e.g., plugin systems, ORM metaclasses)."]},{"id":"python/design_patterns_in_python#Q7-3070","section":"python","module":"python/design_patterns_in_python","moduleName":"design patterns in python","qIndex":7,"difficulty":"intermediate","question":"What is the Template Method pattern and when should you prefer composition over it?","answerFull":"Template Method defines a fixed algorithm skeleton in a base class, with abstract hook methods that subclasses override. Prefer composition when: (a) the algorithm sequence itself can vary (not just the steps), (b) the subclass would only override one method (a callable parameter is simpler), or (c) you want to compose multiple behaviors (a single-inheritance chain cannot compose two independent transforms).","correct":"Template Method defines a fixed algorithm skeleton in a base class, with abstract hook methods that subclasses override.","distractors":["Command encapsulates a call as a first-class object with execute() and undo().","Borg is useful when external code passes your class to APIs that call the constructor directly (e.g., plugin systems, ORM metaclasses).","Any Python callable — a function, a lambda, a class with call, a functools.partial — satisfies Callable[[InputType], OutputType]."]},{"id":"python/design_patterns_in_python#Q8-3071","section":"python","module":"python/design_patterns_in_python","moduleName":"design patterns in python","qIndex":8,"difficulty":"intermediate","question":"How does the Repository pattern integrate with FastAPI's dependency injection?","answerFull":"Define the repository as a typing.Protocol. Write concrete implementations (SQLAlchemyRepo, InMemoryRepo). Write a provider function (get_user_repo) that returns the appropriate implementation — swappable by environment. Declare the repo as a route parameter with Depends(get_user_repo). FastAPI resolves and caches it per request. Tests override the provider with app.dependency_overrides[get_user_repo] = lambda: InMemoryRepo().","correct":"Define the repository as a typing.Protocol.","distractors":["Protocol enables structural (duck-type) subtyping: a class satisfies the Protocol if it has the required methods, with no inheritance.","They are the same pattern at different abstraction levels.","A module is initialized exactly once by the import machinery and cached in sys.modules; subsequent imports return the same module object."]},{"id":"python/design_patterns_in_python#Q9-3072","section":"python","module":"python/design_patterns_in_python","moduleName":"design patterns in python","qIndex":9,"difficulty":"intermediate","question":"What makes the Command pattern useful for undo/redo, and how does FastAPI BackgroundTasks relate to it?","answerFull":"Command encapsulates a call as a first-class object with execute() and undo(). Storing a history deque of executed commands makes undo/redo a matter of popping and calling undo() or re-executing. FastAPI's BackgroundTasks is a simplified Command queue: tasks are callables queued during request handling and fired after the response — the pattern without explicit undo.","correct":"Command encapsulates a call as a first-class object with execute() and undo().","distractors":["Template Method defines a fixed algorithm skeleton in a base class, with abstract hook methods that subclasses override.","Any Python callable — a function, a lambda, a class with call, a functools.partial — satisfies Callable[[InputType], OutputType].","They are the same pattern at different abstraction levels."]},{"id":"python/design_patterns_in_python#Q10-3073","section":"python","module":"python/design_patterns_in_python","moduleName":"design patterns in python","qIndex":10,"difficulty":"intermediate","question":"How do you prevent the \"lambda lost in WeakSet\" bug?","answerFull":"Lambdas defined inline have no name binding holding a strong reference — the WeakSet is the only reference, so the lambda is collected immediately. Always assign lambdas or use named functions when subscribing to a WeakSet-based bus. If you need a quick inline handler, assign it to a local variable with handler = lambda ...: ...; bus.subscribe(\"event\", handler) and hold handler for as long as the subscription should live.","correct":"Lambdas defined inline have no name binding holding a strong reference — the WeakSet is the only reference, so the lambda is collected immediately.","distractors":["New product types register themselves via the @register(\"name\") decorator at module load time.","Any Python callable — a function, a lambda, a class with call, a functools.partial — satisfies Callable[[InputType], OutputType].","If the event bus holds a strong reference to each listener, listener objects will not be garbage-collected even after the subscriber (the object owning the listener method) goes out of scope."]},{"id":"python/design_patterns_in_python#Q11-3074","section":"python","module":"python/design_patterns_in_python","moduleName":"design patterns in python","qIndex":11,"difficulty":"advanced","question":"What Python anti-pattern does deep inheritance chains represent, and how do you refactor it?","answerFull":"Deep inheritance chains encode behavior as a taxonomy of classes rather than as composable behaviors. The symptom is ChildClass(Parent3(Parent2(Parent1(Base)))) where each level adds one small behavior. Refactor by extracting each behavior as a standalone callable or wrapper (with_logging(fn), with_retry(fn)) and composing them explicitly. typing.Protocol defines the interface without requiring any inheritance.","correct":"Deep inheritance chains encode behavior as a taxonomy of classes rather than as composable behaviors.","distractors":["They are the same pattern at different abstraction levels.","Any Python callable — a function, a lambda, a class with call, a functools.partial — satisfies Callable[[InputType], OutputType].","Protocol enables structural (duck-type) subtyping: a class satisfies the Protocol if it has the required methods, with no inheritance."]},{"id":"python/design_patterns_in_python#Q12-3075","section":"python","module":"python/design_patterns_in_python","moduleName":"design patterns in python","qIndex":12,"difficulty":"advanced","question":"How does the @register decorator pattern differ from a plugin-based entry_points approach?","answerFull":"The @register decorator requires the registering module to be imported before the factory is called — order of imports matters. Python packaging entry_points (via importlib.metadata) discovers plugins from installed packages without requiring explicit imports, making the registry extensible across package boundaries. Use @register for in-repo extensibility; entry_points for third-party plugin ecosystems.","correct":"The @register decorator requires the registering module to be imported before the factory is called — order of imports matters.","distractors":["Borg is useful when external code passes your class to APIs that call the constructor directly (e.g., plugin systems, ORM metaclasses).","New product types register themselves via the @register(\"name\") decorator at module load time.","They are the same pattern at different abstraction levels."]},{"id":"python/design_patterns_in_python#Q13-3076","section":"python","module":"python/design_patterns_in_python","moduleName":"design patterns in python","qIndex":13,"difficulty":"advanced","question":"When is a typing.Protocol preferable to an ABC for defining a pattern interface?","answerFull":"Protocol enables structural (duck-type) subtyping: a class satisfies the Protocol if it has the required methods, with no inheritance. Use Protocol when you cannot or should not modify the implementing class (third-party code, stdlib types). Use ABC when you want to enforce the contract at instantiation time (@abstractmethod raises TypeError if a subclass forgets to implement a method) or when you want to share default implementations across subclasses.","correct":"Protocol enables structural (duck-type) subtyping: a class satisfies the Protocol if it has the required methods, with no inheritance.","distractors":["Any Python callable — a function, a lambda, a class with call, a functools.partial — satisfies Callable[[InputType], OutputType].","They are the same pattern at different abstraction levels.","Borg is useful when external code passes your class to APIs that call the constructor directly (e.g., plugin systems, ORM metaclasses)."]},{"id":"python/testing_with_pytest#Q1-3077","section":"python","module":"python/testing_with_pytest","moduleName":"testing with pytest","qIndex":1,"difficulty":"core","question":"What is the difference between scope=\"function\" and scope=\"session\" for a fixture, and when would you choose each?","answerFull":"scope=\"function\" creates a new fixture instance before each test and tears it down after; scope=\"session\" creates the instance once for the entire test run and tears it down after the last test. Choose function scope for any fixture that holds mutable state — database connections that receive writes, mock objects, temporary files with content. Choose session scope for expensive read-only resources: loading a large ML model, establishing a DB schema, compiling a regex corpus. A practical rule: start with function scope; only widen to session when profiling shows setup cost dominates test runtime.","correct":"scope=\"function\" creates a new fixture instance before each test and tears it down after; scope=\"session\" creates the instance once for the entire test run and tears it down after the last test.","distractors":["pytest automatically discovers all conftest.py files in the directory tree from the rootdir down to the test file.","When indirect=True is passed, pytest treats the parametrize value not as a direct argument to the test but as the param attribute of a FixtureRequest object passed to the named fixture.","Three approaches in order of preference: (1) monkeypatch.setattr(\"app.module.time\", lambda: 1_700_000_000.0) — patch the function reference in the module under test."]},{"id":"python/testing_with_pytest#Q2-3078","section":"python","module":"python/testing_with_pytest","moduleName":"testing with pytest","qIndex":2,"difficulty":"core","question":"How does conftest.py work, and why do you not need to import fixtures from it?","answerFull":"pytest automatically discovers all conftest.py files in the directory tree from the rootdir down to the test file. When a test function declares a parameter name, pytest searches for a matching fixture starting from the closest conftest.py and walking upward to the rootdir conftest.py. Because discovery is automatic, fixtures in conftest.py are available to all tests at or below that directory level without any import. This mechanism enables shared fixtures (e.g., a DB connection, an HTTP client) to be defined once and used everywhere without coupling test modules to each other.","correct":"pytest automatically discovers all conftest.py files in the directory tree from the rootdir down to the test file.","distractors":["Three approaches in order of preference: (1) monkeypatch.setattr(\"app.module.time\", lambda: 1_700_000_000.0) — patch the function reference in the module under test.","Module-scoped fixtures are shared across all tests in a file.","When indirect=True is passed, pytest treats the parametrize value not as a direct argument to the test but as the param attribute of a FixtureRequest object passed to the named fixture."]},{"id":"python/testing_with_pytest#Q3-3079","section":"python","module":"python/testing_with_pytest","moduleName":"testing with pytest","qIndex":3,"difficulty":"core","question":"What is the difference between Mock, MagicMock, and AsyncMock?","answerFull":"Mock is the base object that records attribute access and method calls. MagicMock extends Mock by pre-configuring magic (dunder) methods — enter, exit, len, iter, etc. — making it usable as a context manager or iterable without additional setup. AsyncMock makes the object awaitable; calling it returns a coroutine, and assert_awaited_once_with checks awaited calls. Use MagicMock as the default for synchronous code; use AsyncMock for async code. Use Mock only when you explicitly do not want dunder support (to catch accidental iteration or context-manager usage).","correct":"Mock is the base object that records attribute access and method calls.","distractors":["When indirect=True is passed, pytest treats the parametrize value not as a direct argument to the test but as the param attribute of a FixtureRequest object passed to the named fixture.","scope=\"function\" creates a new fixture instance before each test and tears it down after; scope=\"session\" creates the instance once for the entire test run and tears it down after the last test.","pytest automatically discovers all conftest.py files in the directory tree from the rootdir down to the test file."]},{"id":"python/testing_with_pytest#Q4-3080","section":"python","module":"python/testing_with_pytest","moduleName":"testing with pytest","qIndex":4,"difficulty":"core","question":"Explain monkeypatch and how it differs from unittest.mock.patch.","answerFull":"monkeypatch is a pytest fixture that provides methods to temporarily modify attributes, environment variables, dictionary entries, and the working directory. Changes are automatically reverted after the test without a context manager or decorator. unittest.mock.patch is a context manager or decorator that replaces a named attribute with a Mock for the duration of a with block or function call. The key difference: monkeypatch is more Pythonic for simple attribute replacements and environment variables; patch integrates with the Mock ecosystem and is better for full mock objects where you need to verify call counts and arguments.","correct":"monkeypatch is a pytest fixture that provides methods to temporarily modify attributes, environment variables, dictionary entries, and the working directory.","distractors":["Use app.dependency_overrides — a dictionary on the FastAPI application instance.","Mock is the base object that records attribute access and method calls.","scope=\"function\" creates a new fixture instance before each test and tears it down after; scope=\"session\" creates the instance once for the entire test run and tears it down after the last test."]},{"id":"python/testing_with_pytest#Q5-3081","section":"python","module":"python/testing_with_pytest","moduleName":"testing with pytest","qIndex":5,"difficulty":"core","question":"How does @pytest.mark.parametrize with indirect=True work?","answerFull":"When indirect=True is passed, pytest treats the parametrize value not as a direct argument to the test but as the param attribute of a FixtureRequest object passed to the named fixture. The fixture can use request.param to return different objects based on the parameter. This enables parametrizing fixture behavior — for example, a user fixture that returns admin, editor, or viewer user objects based on the parameter string — keeping the test body clean and the parametrize list readable.","correct":"When indirect=True is passed, pytest treats the parametrize value not as a direct argument to the test but as the param attribute of a FixtureRequest object passed to the named fixture.","distractors":["scope=\"function\" creates a new fixture instance before each test and tears it down after; scope=\"session\" creates the instance once for the entire test run and tears it down after the last test.","--cov-fail-under=80 causes pytest to exit with a non-zero status code if the measured line coverage falls below 80%, which fails the CI build.","monkeypatch is a pytest fixture that provides methods to temporarily modify attributes, environment variables, dictionary entries, and the working directory."]},{"id":"python/testing_with_pytest#Q6-3082","section":"python","module":"python/testing_with_pytest","moduleName":"testing with pytest","qIndex":6,"difficulty":"intermediate","question":"What is the difference between a stub and a spy?","answerFull":"A stub replaces a dependency and returns a fixed canned value without executing any real logic. It answers \"what should this dependency return?\" A spy wraps the real implementation, lets it execute, but also records calls so you can assert on them afterward. Use a stub when you do not trust the real dependency in a test (network, DB, time). Use a spy when you want to verify interaction without changing behavior — for example, checking that a cache is read before the DB without replacing the cache implementation.","correct":"A stub replaces a dependency and returns a fixed canned value without executing any real logic.","distractors":["Use app.dependency_overrides — a dictionary on the FastAPI application instance.","scope=\"function\" creates a new fixture instance before each test and tears it down after; scope=\"session\" creates the instance once for the entire test run and tears it down after the last test.","pytest automatically discovers all conftest.py files in the directory tree from the rootdir down to the test file."]},{"id":"python/testing_with_pytest#Q7-3083","section":"python","module":"python/testing_with_pytest","moduleName":"testing with pytest","qIndex":7,"difficulty":"intermediate","question":"How would you test a FastAPI endpoint that has a dependency injection override?","answerFull":"Use app.dependency_overrides — a dictionary on the FastAPI application instance. Map the real dependency callable to a lambda or function that returns a test double. Pass the patched app to httpx.AsyncClient via AsyncClient(app=app, base_url=\"http://test\"). After the test (or in the fixture teardown), call app.dependency_overrides.clear() to restore the original wiring. This approach tests the full request/response cycle including serialization and middleware without hitting real databases or external services.","correct":"Use app.dependency_overrides — a dictionary on the FastAPI application instance.","distractors":["pytest automatically discovers all conftest.py files in the directory tree from the rootdir down to the test file.","monkeypatch is a pytest fixture that provides methods to temporarily modify attributes, environment variables, dictionary entries, and the working directory.","Three approaches in order of preference: (1) monkeypatch.setattr(\"app.module.time\", lambda: 1_700_000_000.0) — patch the function reference in the module under test."]},{"id":"python/testing_with_pytest#Q8-3084","section":"python","module":"python/testing_with_pytest","moduleName":"testing with pytest","qIndex":8,"difficulty":"intermediate","question":"How does hypothesis shrinking work, and why does it matter?","answerFull":"When hypothesis finds a falsifying input, it does not report that raw input immediately. It runs a shrinking phase that systematically reduces the counterexample — removing list elements, decreasing integers, shortening strings — until it finds the minimal input that still fails the property. The result is a small, readable counterexample rather than a 10,000-element list. Shrinking matters because developers cannot debug a 10,000-integer list; they can debug a 2-integer list.","correct":"When hypothesis finds a falsifying input, it does not report that raw input immediately.","distractors":["scope=\"function\" creates a new fixture instance before each test and tears it down after; scope=\"session\" creates the instance once for the entire test run and tears it down after the last test.","pytest automatically discovers all conftest.py files in the directory tree from the rootdir down to the test file.","Mock is the base object that records attribute access and method calls."]},{"id":"python/testing_with_pytest#Q9-3085","section":"python","module":"python/testing_with_pytest","moduleName":"testing with pytest","qIndex":9,"difficulty":"intermediate","question":"What does pytest-cov --cov-fail-under=80 do in CI, and what does 80% coverage miss?","answerFull":"--cov-fail-under=80 causes pytest to exit with a non-zero status code if the measured line coverage falls below 80%, which fails the CI build. This enforces a coverage floor. What 80% coverage misses: branch coverage (whether both sides of every if are exercised), integration paths (a line can be executed without testing its correctness), and concurrency bugs. Coverage is a necessary but not sufficient signal; use it as a floor, not a target.","correct":"--cov-fail-under=80 causes pytest to exit with a non-zero status code if the measured line coverage falls below 80%, which fails the CI build.","distractors":["monkeypatch is a pytest fixture that provides methods to temporarily modify attributes, environment variables, dictionary entries, and the working directory.","When indirect=True is passed, pytest treats the parametrize value not as a direct argument to the test but as the param attribute of a FixtureRequest object passed to the named fixture.","pytest automatically discovers all conftest.py files in the directory tree from the rootdir down to the test file."]},{"id":"python/testing_with_pytest#Q10-3086","section":"python","module":"python/testing_with_pytest","moduleName":"testing with pytest","qIndex":10,"difficulty":"intermediate","question":"How do you test a function that calls time.time() or datetime.now()?","answerFull":"Three approaches in order of preference: (1) monkeypatch.setattr(\"app.module.time\", lambda: 1_700_000_000.0) — patch the function reference in the module under test. (2) freezegun — @freeze_time(\"2024-01-01 12:00:00\") decorator freezes datetime.now(), time.time(), and date.today() globally for the test. (3) Inject a clock callable as a dependency — the production code calls clock() instead of time.time(), and tests pass a lambda. Approach 3 is most design-correct; approach 2 is most convenient for legacy code.","correct":"Three approaches in order of preference: (1) monkeypatch.setattr(\"app.module.time\", lambda: 1_700_000_000.0) — patch the function reference in the module under test.","distractors":["Module-scoped fixtures are shared across all tests in a file.","pytest automatically discovers all conftest.py files in the directory tree from the rootdir down to the test file.","monkeypatch is a pytest fixture that provides methods to temporarily modify attributes, environment variables, dictionary entries, and the working directory."]},{"id":"python/testing_with_pytest#Q11-3087","section":"python","module":"python/testing_with_pytest","moduleName":"testing with pytest","qIndex":11,"difficulty":"advanced","question":"What is capsys and when would you use it over caplog?","answerFull":"capsys captures text written to sys.stdout and sys.stderr. caplog captures Python logging records. Use capsys when testing a function that uses print() or writes directly to stderr — for example, a CLI entry point. Use caplog when testing a function that uses the logging module — FastAPI, Celery workers, database adapters all log via logging. caplog is preferable because it gives access to structured record attributes (levelname, name, message) not available in raw text.","correct":"capsys captures text written to sys.stdout and sys.stderr.","distractors":["scope=\"function\" creates a new fixture instance before each test and tears it down after; scope=\"session\" creates the instance once for the entire test run and tears it down after the last test.","pytest automatically discovers all conftest.py files in the directory tree from the rootdir down to the test file.","Mock is the base object that records attribute access and method calls."]},{"id":"python/testing_with_pytest#Q12-3088","section":"python","module":"python/testing_with_pytest","moduleName":"testing with pytest","qIndex":12,"difficulty":"advanced","question":"How do you avoid test pollution when using @pytest.fixture(scope=\"module\")?","answerFull":"Module-scoped fixtures are shared across all tests in a file. Pollution occurs when one test mutates the fixture's state. Mitigation strategies: (1) design module-scoped fixtures to return read-only objects or frozen dataclasses; (2) if the resource must be mutable, use copy.deepcopy inside the test to get a private copy; (3) document the fixture's contract — \"this fixture is read-only\" — and enforce it with a @property-based wrapper that raises AttributeError on assignment. The safest long-term choice is to prefer function scope and widen to module scope only after measuring a real performance cost.","correct":"Module-scoped fixtures are shared across all tests in a file.","distractors":["When indirect=True is passed, pytest treats the parametrize value not as a direct argument to the test but as the param attribute of a FixtureRequest object passed to the named fixture.","Three approaches in order of preference: (1) monkeypatch.setattr(\"app.module.time\", lambda: 1_700_000_000.0) — patch the function reference in the module under test.","--cov-fail-under=80 causes pytest to exit with a non-zero status code if the measured line coverage falls below 80%, which fails the CI build."]},{"id":"python/metaclasses_and_metaprogramming#Q1-3089","section":"python","module":"python/metaclasses_and_metaprogramming","moduleName":"metaclasses and metaprogramming","qIndex":1,"difficulty":"core","question":"What is a metaclass in Python and how does it relate to type?","answerFull":"A metaclass is the class of a class — it is the factory responsible for creating class objects. type is the default metaclass for all user-defined classes. When you write class Foo: ..., Python calls type(\"Foo\", (object,), namespace) to produce the class object. Every custom metaclass must inherit from type because type.new contains the machinery to allocate and configure class objects. A practical implication: type(MyClass) returns its metaclass, just as type(obj) returns obj's class.","correct":"A metaclass is the class of a class — it is the factory responsible for creating class objects.","distractors":["set_name(self, owner, name) is called by type.new on every descriptor object assigned as a class attribute, passing the owning class and the attribute name.","1. Python calls Meta.prepare(\"Foo\", (Bar,)) to get the class namespace (default {}).","new(mcs, name, bases, namespace) is called first and must return the new class object; it controls allocation."]},{"id":"python/metaclasses_and_metaprogramming#Q2-3090","section":"python","module":"python/metaclasses_and_metaprogramming","moduleName":"metaclasses and metaprogramming","qIndex":2,"difficulty":"core","question":"What is the difference between new and init in a metaclass?","answerFull":"new(mcs, name, bases, namespace) is called first and must return the new class object; it controls allocation. init(cls, name, bases, namespace) is called on the already-created class and performs post-creation configuration. Use new when you need to modify namespace, rename the class, or change bases before allocation. Use init for side effects like registering the class in a global dictionary. If new returns an object that is not an instance of the metaclass, init is skipped.","correct":"new(mcs, name, bases, namespace) is called first and must return the new class object; it controls allocation.","distractors":["A metaclass conflict occurs when a class tries to inherit from two bases whose metaclasses are not related by inheritance — Python cannot determine which metaclass to use for the derived class.","class_getitem(cls, item) is a classmethod called when you write MyClass[SomeType].","set_name(self, owner, name) is called by type.new on every descriptor object assigned as a class attribute, passing the owning class and the attribute name."]},{"id":"python/metaclasses_and_metaprogramming#Q3-3091","section":"python","module":"python/metaclasses_and_metaprogramming","moduleName":"metaclasses and metaprogramming","qIndex":3,"difficulty":"core","question":"What does prepare do and when would you override it?","answerFull":"prepare(mcs, name, bases, kwargs) is called before the class body is executed and must return the namespace dictionary in which the body will run. The default returns {}. Override it to return a custom mapping — for example, collections.OrderedDict to capture definition order (unnecessary in Python 3.7+ where dict is ordered), or a _EnumDict that rejects duplicate member names. prepare must be a classmethod or staticmethod on the metaclass.","correct":"prepare(mcs, name, bases, kwargs) is called before the class body is executed and must return the namespace dictionary in which the body will run.","distractors":["A metaclass is the class of a class — it is the factory responsible for creating class objects.","set_name(self, owner, name) is called by type.new on every descriptor object assigned as a class attribute, passing the owning class and the attribute name.","getattribute is called on every attribute access, before any dictionary lookup."]},{"id":"python/metaclasses_and_metaprogramming#Q4-3092","section":"python","module":"python/metaclasses_and_metaprogramming","moduleName":"metaclasses and metaprogramming","qIndex":4,"difficulty":"core","question":"When should you prefer init_subclass over a metaclass?","answerFull":"Prefer init_subclass when you only need to react to subclass creation — plugin registration, auto-indexing, enforcing required class variables. It requires no Meta class, avoids metaclass conflict, plays well with cooperative inheritance via super(), and is far more readable. Use a metaclass only when you need prepare, need to modify bases or namespace before the class exists, or need to intercept class creation for third-party base classes you do not control.","correct":"Prefer init_subclass when you only need to react to subclass creation — plugin registration, auto-indexing, enforcing required class variables.","distractors":["A metaclass conflict occurs when a class tries to inherit from two bases whose metaclasses are not related by inheritance — Python cannot determine which metaclass to use for the derived class.","class_getitem(cls, item) is a classmethod called when you write MyClass[SomeType].","A metaclass is the class of a class — it is the factory responsible for creating class objects."]},{"id":"python/metaclasses_and_metaprogramming#Q5-3093","section":"python","module":"python/metaclasses_and_metaprogramming","moduleName":"metaclasses and metaprogramming","qIndex":5,"difficulty":"core","question":"What is a data descriptor vs a non-data descriptor?","answerFull":"A data descriptor defines set (and/or delete) in addition to get. A non-data descriptor defines only get. The distinction controls priority in attribute lookup: data descriptors take priority over the instance dict, while the instance dict takes priority over non-data descriptors. property is a data descriptor; staticmethod and classmethod and plain functions are non-data descriptors. This is why f.x = 99 overwrites a function binding on an instance (f.dict[\"x\"] = 99) but cannot shadow a property.","correct":"A data descriptor defines set (and/or delete) in addition to get.","distractors":["property is a built-in type that implements get, set, and delete.","1. Python calls Meta.prepare(\"Foo\", (Bar,)) to get the class namespace (default {}).","When a class defines slots = (\"x\",), Python creates a member_descriptor (a data descriptor) for each slot name on the class."]},{"id":"python/metaclasses_and_metaprogramming#Q6-3094","section":"python","module":"python/metaclasses_and_metaprogramming","moduleName":"metaclasses and metaprogramming","qIndex":6,"difficulty":"intermediate","question":"Explain set_name and what problem it solves.","answerFull":"set_name(self, owner, name) is called by type.new on every descriptor object assigned as a class attribute, passing the owning class and the attribute name. Before [3.6], descriptors had to be constructed with the attribute name explicitly: username = ValidatedField(\"username\", str). With set_name, the descriptor self-registers: username = ValidatedField(str) and the descriptor learns it is stored as \"username\" automatically. This eliminates the DRY violation and prevents mismatches where the variable name differs from the string passed to init.","correct":"set_name(self, owner, name) is called by type.new on every descriptor object assigned as a class attribute, passing the owning class and the attribute name.","distractors":["property is a built-in type that implements get, set, and delete.","ABCMeta.new collects all methods decorated with @abstractmethod across the MRO into cls.abstractmethods (a frozenset).","getattribute is called on every attribute access, before any dictionary lookup."]},{"id":"python/metaclasses_and_metaprogramming#Q7-3095","section":"python","module":"python/metaclasses_and_metaprogramming","moduleName":"metaclasses and metaprogramming","qIndex":7,"difficulty":"intermediate","question":"What is the difference between getattr and getattribute?","answerFull":"getattribute is called on every attribute access, before any dictionary lookup. getattr is called only when the normal attribute lookup (including getattribute) raises AttributeError. Override getattr for proxy patterns, lazy loading, or dynamic attributes — it is safe and low-risk. Override getattribute only when you need to intercept all attribute reads unconditionally; you must call object.getattribute(self, name) internally or risk infinite recursion.","correct":"getattribute is called on every attribute access, before any dictionary lookup.","distractors":["class_getitem(cls, item) is a classmethod called when you write MyClass[SomeType].","prepare(mcs, name, bases, kwargs) is called before the class body is executed and must return the namespace dictionary in which the body will run.","new(mcs, name, bases, namespace) is called first and must return the new class object; it controls allocation."]},{"id":"python/metaclasses_and_metaprogramming#Q8-3096","section":"python","module":"python/metaclasses_and_metaprogramming","moduleName":"metaclasses and metaprogramming","qIndex":8,"difficulty":"intermediate","question":"How does abc.ABCMeta enforce abstract methods?","answerFull":"ABCMeta.new collects all methods decorated with @abstractmethod across the MRO into cls.abstractmethods (a frozenset). object.new checks this set at instantiation time: if it is non-empty, it raises TypeError. Subclasses that override all abstract methods get an empty abstractmethods and can be instantiated. The register() method adds a class as a virtual subclass without inheritance, making isinstance(obj, MyABC) return True even though the class does not inherit from the ABC.","correct":"ABCMeta.new collects all methods decorated with @abstractmethod across the MRO into cls.abstractmethods (a frozenset).","distractors":["The @dataclass decorator inspects cls.annotations at decoration time, builds an init function with positional parameters matching the annotated attributes in definition order, and attaches it to the class.","set_name(self, owner, name) is called by type.new on every descriptor object assigned as a class attribute, passing the owning class and the attribute name.","Protocol (PEP 544, Python 3.8+) enables structural subtyping: a class satisfies a Protocol if it has the required methods and attributes, regardless of inheritance."]},{"id":"python/metaclasses_and_metaprogramming#Q9-3097","section":"python","module":"python/metaclasses_and_metaprogramming","moduleName":"metaclasses and metaprogramming","qIndex":9,"difficulty":"intermediate","question":"How does @dataclass generate init and what is the field() factory for?","answerFull":"The @dataclass decorator inspects cls.annotations at decoration time, builds an init function with positional parameters matching the annotated attributes in definition order, and attaches it to the class. For attributes with field(default_factory=fn), the generated init calls fn() each time a new instance is created, avoiding the shared-mutable-default trap. field() also controls repr=, compare=, hash=, init=, and metadata. The fields(cls) function returns a tuple of Field objects for introspection.","correct":"The @dataclass decorator inspects cls.annotations at decoration time, builds an init function with positional parameters matching the annotated attributes in definition order, and attaches it to the class.","distractors":["Prefer init_subclass when you only need to react to subclass creation — plugin registration, auto-indexing, enforcing required class variables.","Protocol (PEP 544, Python 3.8+) enables structural subtyping: a class satisfies a Protocol if it has the required methods and attributes, regardless of inheritance.","new(mcs, name, bases, namespace) is called first and must return the new class object; it controls allocation."]},{"id":"python/metaclasses_and_metaprogramming#Q10-3098","section":"python","module":"python/metaclasses_and_metaprogramming","moduleName":"metaclasses and metaprogramming","qIndex":10,"difficulty":"intermediate","question":"What is class_getitem and how does it enable generics?","answerFull":"class_getitem(cls, item) is a classmethod called when you write MyClass[SomeType]. Python's built-in list, dict, tuple implement it to return types.GenericAlias objects that carry type information for static type checkers. User-defined classes implement it to support the same Class[T] subscription syntax without inheriting from Generic[T]. For type-checker integration, combining class_getitem with typing.Generic is the standard pattern.","correct":"class_getitem(cls, item) is a classmethod called when you write MyClass[SomeType].","distractors":["A metaclass is the class of a class — it is the factory responsible for creating class objects.","Prefer init_subclass when you only need to react to subclass creation — plugin registration, auto-indexing, enforcing required class variables.","set_name(self, owner, name) is called by type.new on every descriptor object assigned as a class attribute, passing the owning class and the attribute name."]},{"id":"python/metaclasses_and_metaprogramming#Q11-3099","section":"python","module":"python/metaclasses_and_metaprogramming","moduleName":"metaclasses and metaprogramming","qIndex":11,"difficulty":"advanced","question":"What causes a metaclass conflict and how do you resolve it?","answerFull":"A metaclass conflict occurs when a class tries to inherit from two bases whose metaclasses are not related by inheritance — Python cannot determine which metaclass to use for the derived class. The error message is TypeError: metaclass conflict: the metaclass of a derived class must be a (non-strict) subclass of the metaclasses of all its bases. Resolution: create a merged metaclass that inherits from both conflicting metaclasses, or eliminate one of the metaclasses by replacing it with init_subclass or a class decorator.","correct":"A metaclass conflict occurs when a class tries to inherit from two bases whose metaclasses are not related by inheritance — Python cannot determine which metaclass to use for the derived class.","distractors":["1. Python calls Meta.prepare(\"Foo\", (Bar,)) to get the class namespace (default {}).","new(mcs, name, bases, namespace) is called first and must return the new class object; it controls allocation.","Prefer init_subclass when you only need to react to subclass creation — plugin registration, auto-indexing, enforcing required class variables."]},{"id":"python/metaclasses_and_metaprogramming#Q12-3100","section":"python","module":"python/metaclasses_and_metaprogramming","moduleName":"metaclasses and metaprogramming","qIndex":12,"difficulty":"advanced","question":"How does the descriptor protocol interact with slots?","answerFull":"When a class defines slots = (\"x\",), Python creates a member_descriptor (a data descriptor) for each slot name on the class. This data descriptor stores the value in a C-level slot in the instance struct rather than in dict. Because it is a data descriptor, it takes priority over any instance dict entry — but slots eliminates dict entirely for slotted attributes, saving roughly 40-50 bytes per instance for classes with many instances. See ../data_model_and_objects/README.md for slots coverage.","correct":"When a class defines slots = (\"x\",), Python creates a member_descriptor (a data descriptor) for each slot name on the class.","distractors":["prepare(mcs, name, bases, kwargs) is called before the class body is executed and must return the namespace dictionary in which the body will run.","A metaclass is the class of a class — it is the factory responsible for creating class objects.","set_name(self, owner, name) is called by type.new on every descriptor object assigned as a class attribute, passing the owning class and the attribute name."]},{"id":"python/metaclasses_and_metaprogramming#Q13-3101","section":"python","module":"python/metaclasses_and_metaprogramming","moduleName":"metaclasses and metaprogramming","qIndex":13,"difficulty":"advanced","question":"Can a metaclass change what bases a class inherits from?","answerFull":"Yes. In new, the bases tuple can be replaced before calling super().new. This is used by abc.ABCMeta to strip ABC from bases (to avoid diamond problems in the final class MRO) and by some ORMs to inject a mixin automatically. Modifying bases is powerful but fragile — always verify MRO correctness with cls.mro after the fact.","correct":"Yes. In new, the bases tuple can be replaced before calling super().new.","distractors":["ABCMeta.new collects all methods decorated with @abstractmethod across the MRO into cls.abstractmethods (a frozenset).","set_name(self, owner, name) is called by type.new on every descriptor object assigned as a class attribute, passing the owning class and the attribute name.","new(mcs, name, bases, namespace) is called first and must return the new class object; it controls allocation."]},{"id":"python/metaclasses_and_metaprogramming#Q14-3102","section":"python","module":"python/metaclasses_and_metaprogramming","moduleName":"metaclasses and metaprogramming","qIndex":14,"difficulty":"advanced","question":"What is typing.Protocol and how does it differ from abc.ABC?","answerFull":"Protocol (PEP 544, Python 3.8+) enables structural subtyping: a class satisfies a Protocol if it has the required methods and attributes, regardless of inheritance. No register() call or explicit inheritance is needed — static type checkers (mypy, pyright) verify compatibility. abc.ABC uses nominal subtyping: a class must explicitly inherit from the ABC or be registered. See ../the_type_system_and_typing/README.md for full Protocol coverage.","correct":"Protocol (PEP 544, Python 3.8+) enables structural subtyping: a class satisfies a Protocol if it has the required methods and attributes, regardless of inheritance.","distractors":["ABCMeta.new collects all methods decorated with @abstractmethod across the MRO into cls.abstractmethods (a frozenset).","new(mcs, name, bases, namespace) is called first and must return the new class object; it controls allocation.","Prefer init_subclass when you only need to react to subclass creation — plugin registration, auto-indexing, enforcing required class variables."]},{"id":"python/metaclasses_and_metaprogramming#Q15-3103","section":"python","module":"python/metaclasses_and_metaprogramming","moduleName":"metaclasses and metaprogramming","qIndex":15,"difficulty":"advanced","question":"Walk through what Python does when it executes class Foo(Bar, metaclass=Meta): x = 1.","answerFull":"1. Python calls Meta.prepare(\"Foo\", (Bar,)) to get the class namespace (default {}). 2. The class body x = 1 is executed inside that namespace, producing {\"x\": 1, \"module\": ..., \"qualname\": \"Foo\"}. 3. Python calls Meta(\"Foo\", (Bar,), namespace), which invokes Meta.call. 4. Meta.call invokes Meta.new(Meta, \"Foo\", (Bar,), namespace), which allocates the class object and returns it. 5. Meta.call then invokes Meta.init(Foo, \"Foo\", (Bar,), namespace) for post-creation setup. 6. The name Foo in the enclosing scope is bound to the returned class object. 7. For each value in namespace that is a descriptor with set_name, Python calls value.set_name(Foo, attr_name). 8. Bar.init_subclass is called with Foo as the argument.","correct":"1. Python calls Meta.prepare(\"Foo\", (Bar,)) to get the class namespace (default {}).","distractors":["property is a built-in type that implements get, set, and delete.","prepare(mcs, name, bases, kwargs) is called before the class body is executed and must return the namespace dictionary in which the body will run.","new(mcs, name, bases, namespace) is called first and must return the new class object; it controls allocation."]},{"id":"python/metaclasses_and_metaprogramming#Q16-3104","section":"python","module":"python/metaclasses_and_metaprogramming","moduleName":"metaclasses and metaprogramming","qIndex":16,"difficulty":"advanced","question":"How does the property built-in work as a data descriptor?","answerFull":"property is a built-in type that implements get, set, and delete. get calls the getter function; set calls the setter (or raises AttributeError if none is defined); delete calls the deleter. Because it defines set, it is a data descriptor and therefore wins over instance.dict. The @property, @x.setter, @x.deleter decorator syntax creates and replaces property objects on the class with increasingly populated getter/setter/deleter slots.","correct":"property is a built-in type that implements get, set, and delete.","distractors":["When a class defines slots = (\"x\",), Python creates a member_descriptor (a data descriptor) for each slot name on the class.","A data descriptor defines set (and/or delete) in addition to get.","set_name(self, owner, name) is called by type.new on every descriptor object assigned as a class attribute, passing the owning class and the attribute name."]}]