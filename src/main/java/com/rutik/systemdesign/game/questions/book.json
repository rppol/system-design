[{"id":"book/designing_data_intensive_applications/12_future_of_data_systems#Q3-5124","section":"book","module":"book/designing_data_intensive_applications/12_future_of_data_systems","moduleName":"12 future of data systems","qIndex":3,"difficulty":"core","question":"What is the end-to-end argument, and how does it apply to exactly-once processing?","answerFull":"The end-to-end argument states that a correctness guarantee is only truly achieved if it's enforced at the application level, end to end, because lower layers don't understand the application's semantics. Applied to exactly-once: TCP can deduplicate *packets* and a database can retry internally, but neither knows what a duplicate *business operation* is — so a user double-clicking \"pay\" or a client retrying after a timeout will double-charge unless the application itself recognizes the duplicate via an idempotency key or unique request ID. Real exactly-once must be designed at the boundary where the operation's meaning lives.","correct":"The end-to-end argument states that a correctness guarantee is only truly achieved if it's enforced at the application level, end to end, because lower layers don't understand the application's semantics.","distractors":["Causality is a *partial* order: it requires that causally related events keep their order (the question before the answer, a row created before it's updated) while leaving truly concurrent, unrelated events unordered.","It targets engineers who build on top of data systems and want to understand the internals and tradeoffs.","TrueTime represents the current time not as a single value but as an interval [earliest, latest] with a known, narrow bound on uncertainty, achieved using GPS receivers and atomic clocks in each datacenter."]},{"id":"book/designing_data_intensive_applications/12_future_of_data_systems#Q4-5125","section":"book","module":"book/designing_data_intensive_applications/12_future_of_data_systems","moduleName":"12 future of data systems","qIndex":4,"difficulty":"core","question":"Distinguish timeliness from integrity, and explain which one asynchronous dataflow may relax.","answerFull":"Timeliness is whether a read reflects recent writes — being up to date; if violated, the staleness is temporary and self-corrects as derived views catch up. Integrity is whether the data is correct and uncorrupted — no lost or duplicated updates, no contradictions; if violated, the damage is permanent and far more serious. Asynchronous dataflow architectures routinely *relax timeliness* (derived views lag slightly behind the source) but must *never* relax integrity. The encouraging result is that exactly-once processing on a replayable log with idempotent operations preserves integrity even while timeliness is only eventual.","correct":"Timeliness is whether a read reflects recent writes — being up to date; if violated, the staleness is temporary and self-corrects as derived views catch up.","distractors":["Safety means \"nothing bad ever happens\" — for example, no two nodes are leader at once, or no fencing token is issued twice; once violated, a safety property can't be undone.","They're verbose and have ambiguous data types: XML and CSV can't reliably distinguish a number from a string, and JSON can't distinguish integers from floats and loses precision on integers above 2^53.","Batch processing reads a large, *bounded* input of known size and produces derived output, with no user waiting, so success is measured by throughput (how much data per unit time) rather than response time."]},{"id":"book/designing_data_intensive_applications/12_future_of_data_systems#Q5-5126","section":"book","module":"book/designing_data_intensive_applications/12_future_of_data_systems","moduleName":"12 future of data systems","qIndex":5,"difficulty":"core","question":"How can you enforce a uniqueness constraint in a log-based dataflow system without a distributed transaction?","answerFull":"By routing all operations that touch the same constrained value to the *same* log partition and having a single consumer process that partition's events *in order*. This converts a distributed agreement problem into a local, sequential decision: the consumer sees requests for a given username one at a time in a defined order, grants the first, and rejects every later one — achieving the consensus-like guarantee of uniqueness without a global distributed transaction. It works because ordering within a partition plus single-threaded processing gives a total order over all decisions about that value.","correct":"By routing all operations that touch the same constrained value to the *same* log partition and having a single consumer process that partition's events *in order*.","distractors":["Causality is a *partial* order: it requires that causally related events keep their order (the question before the answer, a row created before it's updated) while leaving truly concurrent, unrelated events unordered.","An offset is a monotonically increasing position number identifying a consumer's place within a partition's log.","Because a write to a database *is* an event, and a database's replication log *is* a stream of those events — so the two are dual representations of the same information."]},{"id":"book/designing_data_intensive_applications/12_future_of_data_systems#Q6-5127","section":"book","module":"book/designing_data_intensive_applications/12_future_of_data_systems","moduleName":"12 future of data systems","qIndex":6,"difficulty":"intermediate","question":"Why does the book end on ethics, and what is its central argument there?","answerFull":"Because data systems increasingly make or inform consequential decisions about people — credit, insurance, hiring, policing — so their design has real human impact beyond technical correctness. The central argument is that engineers are responsible for the consequences of what they build: predictive models can encode and amplify historical bias under a false veneer of objectivity; mass data collection is surveillance that users rarely meaningfully consent to; and \"the algorithm decided\" or \"we just build the tools\" is not an absolution. Kleppmann urges data minimization, transparency, consent, and the recognition that something being buildable doesn't make it ethical to build.","correct":"Because data systems increasingly make or inform consequential decisions about people — credit, insurance, hiring, policing — so their design has real human impact beyond technical correctness.","distractors":["The real load parameter is the *fan-out* — the distribution of how many followers each user has — not the raw tweet rate.","Reliability (works correctly despite faults), scalability (has strategies to cope as load grows), and maintainability (people can work on it productively over time).","Hardware faults are largely independent — one disk dying doesn't make another die, so redundancy (RAID, replicas) works well."]},{"id":"book/designing_data_intensive_applications/12_future_of_data_systems#Q14-5128","section":"book","module":"book/designing_data_intensive_applications/12_future_of_data_systems","moduleName":"12 future of data systems","qIndex":14,"difficulty":"advanced","question":"How is an unbundled architecture analogous to the internals of a single database?","answerFull":"In a single database, a write goes to the transaction/replication log, and internal indexes and materialized views are kept in sync with that log automatically. The unbundled architecture does exactly this but at organizational scale and with separate products: the event log plays the role of the transaction log, and each external derived store (Elasticsearch as a \"secondary index,\" Redis as a \"cache,\" a warehouse as an \"analytical view\") is kept in sync by consuming the log via CDC/streams. So the whole heterogeneous system behaves like one big database whose components you assembled from the best tool for each job.","correct":"In a single database, a write goes to the transaction/replication log, and internal indexes and materialized views are kept in sync with that log automatically.","distractors":["By routing all operations that touch the same constrained value to the *same* log partition and having a single consumer process that partition's events *in order*.","Synchronous replication makes the leader wait for a follower to confirm a write before acknowledging success, guaranteeing the follower is up to date but blocking all writes if that follower is slow or down.","It targets engineers who build on top of data systems and want to understand the internals and tradeoffs."]},{"id":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable#Q1-5129","section":"book","module":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable","moduleName":"01 reliable scalable maintainable","qIndex":1,"difficulty":"core","question":"Why should you report response times as percentiles rather than as an average?","answerFull":"Because response-time distributions are heavily skewed by outliers, so the mean describes no actual user — a few very slow requests drag it up while still understating the worst case. The median (p50) tells you the typical experience and the tail (p95/p99/p999) tells you the worst, which is what users actually complain about. SLOs are therefore written as \"p99 < 200 ms,\" never as an average.","correct":"Because response-time distributions are heavily skewed by outliers, so the mean describes no actual user — a few very slow requests drag it up while still understating the worst case.","distractors":["Because a single user-facing request often fans out to many backend services in parallel, and the user waits for the *slowest* of them.","The real load parameter is the *fan-out* — the distribution of how many followers each user has — not the raw tweet rate.","Because the server's view excludes queueing delay, network transit, and time the request spent waiting to be accepted — exactly the components that head-of-line blocking and load inflate."]},{"id":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable#Q2-5130","section":"book","module":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable","moduleName":"01 reliable scalable maintainable","qIndex":2,"difficulty":"core","question":"What is the difference between a fault and a failure, and which one do you design to eliminate?","answerFull":"A fault is one component deviating from spec (a disk dies); a failure is the whole system stopping service. You do *not* try to eliminate faults — they're inevitable — you build fault-tolerance so faults don't escalate into failures. Netflix's Chaos Monkey deliberately injects faults in production to verify the system absorbs them.","correct":"A fault is one component deviating from spec (a disk dies); a failure is the whole system stopping service.","distractors":["Hardware faults (disk/RAM/power) are mostly uncorrelated and handled by redundancy and, increasingly, software fault-tolerance enabling rolling restarts.","Hardware faults are largely independent — one disk dying doesn't make another die, so redundancy (RAID, replicas) works well.","Because response-time distributions are heavily skewed by outliers, so the mean describes no actual user — a few very slow requests drag it up while still understating the worst case."]},{"id":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable#Q3-5131","section":"book","module":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable","moduleName":"01 reliable scalable maintainable","qIndex":3,"difficulty":"core","question":"In the Twitter timeline example, what is the real load parameter, and why does it determine the architecture?","answerFull":"The real load parameter is the *fan-out* — the distribution of how many followers each user has — not the raw tweet rate. Tweets arrive at only ~5k/s, but timeline reads run ~300k/s, so the cost is in delivering each tweet to all followers. This drives the choice between fan-out-on-read (cheap writes, expensive reads) and fan-out-on-write (expensive writes, cheap reads); Twitter uses a hybrid because celebrities make pure fan-out-on-write explode.","correct":"The real load parameter is the *fan-out* — the distribution of how many followers each user has — not the raw tweet rate.","distractors":["Because a single user-facing request often fans out to many backend services in parallel, and the user waits for the *slowest* of them.","Because response-time distributions are heavily skewed by outliers, so the mean describes no actual user — a few very slow requests drag it up while still understating the worst case.","Because the server's view excludes queueing delay, network transit, and time the request spent waiting to be accepted — exactly the components that head-of-line blocking and load inflate."]},{"id":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable#Q4-5132","section":"book","module":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable","moduleName":"01 reliable scalable maintainable","qIndex":4,"difficulty":"core","question":"Why do tail latencies (p99/p999) matter more than the median in a system with high fan-out?","answerFull":"Because a single user-facing request often fans out to many backend services in parallel, and the user waits for the *slowest* of them. The more backends, the higher the probability that at least one is in its slow tail — so even if each service has a good p99, the user-perceived p99 is much worse. This is why services that fan out heavily must drive down tail latency, not just the median.","correct":"Because a single user-facing request often fans out to many backend services in parallel, and the user waits for the *slowest* of them.","distractors":["Head-of-line blocking is when one slow request occupies the server (or sits at the front of a queue) and stalls all the requests queued behind it, even if those would have been fast.","Because the server's view excludes queueing delay, network transit, and time the request spent waiting to be accepted — exactly the components that head-of-line blocking and load inflate.","Because response-time distributions are heavily skewed by outliers, so the mean describes no actual user — a few very slow requests drag it up while still understating the worst case."]},{"id":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable#Q5-5133","section":"book","module":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable","moduleName":"01 reliable scalable maintainable","qIndex":5,"difficulty":"core","question":"Why can't you average the p99 values reported by ten different servers to get the cluster p99?","answerFull":"Percentiles are not linear, so averaging them is mathematically meaningless — a server handling few requests and one handling many contribute unequally. You must aggregate the raw response-time *histograms* (using structures like HdrHistogram or t-digest) and compute the percentile from the combined distribution. Averaging percentiles is a common and silent monitoring bug.","correct":"Percentiles are not linear, so averaging them is mathematically meaningless — a server handling few requests and one handling many contribute unequally.","distractors":["Because response-time distributions are heavily skewed by outliers, so the mean describes no actual user — a few very slow requests drag it up while still understating the worst case.","Because the server's view excludes queueing delay, network transit, and time the request spent waiting to be accepted — exactly the components that head-of-line blocking and load inflate.","Head-of-line blocking is when one slow request occupies the server (or sits at the front of a queue) and stalls all the requests queued behind it, even if those would have been fast."]},{"id":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable#Q6-5134","section":"book","module":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable","moduleName":"01 reliable scalable maintainable","qIndex":6,"difficulty":"intermediate","question":"What is head-of-line blocking and how does it inflate tail latency?","answerFull":"Head-of-line blocking is when one slow request occupies the server (or sits at the front of a queue) and stalls all the requests queued behind it, even if those would have been fast. The result is that a single slow operation makes many requests report high latency. It's a major contributor to tail latency and is why queueing delay must be measured on the client side, where the full wait is visible.","correct":"Head-of-line blocking is when one slow request occupies the server (or sits at the front of a queue) and stalls all the requests queued behind it, even if those would have been fast.","distractors":["Because operability — monitoring, runbooks, fast rollback, capacity headroom — lets a skilled ops team contain and recover from a flawed system, keeping it serving users.","Because the server's view excludes queueing delay, network transit, and time the request spent waiting to be accepted — exactly the components that head-of-line blocking and load inflate.","Percentiles are not linear, so averaging them is mathematically meaningless — a server handling few requests and one handling many contribute unequally."]},{"id":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable#Q7-5135","section":"book","module":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable","moduleName":"01 reliable scalable maintainable","qIndex":7,"difficulty":"intermediate","question":"Distinguish the three categories of faults and the main mitigation for each.","answerFull":"Hardware faults (disk/RAM/power) are mostly uncorrelated and handled by redundancy and, increasingly, software fault-tolerance enabling rolling restarts. Software faults are systematic and *correlated* (a bug that hits all nodes on a trigger), mitigated by testing, process isolation, monitoring, and crash-restart. Human errors are the largest source of outages, mitigated by well-designed interfaces, sandboxes, easy/fast rollback, and detailed telemetry.","correct":"Hardware faults (disk/RAM/power) are mostly uncorrelated and handled by redundancy and, increasingly, software fault-tolerance enabling rolling restarts.","distractors":["Accidental complexity is complexity that arises from the implementation rather than being inherent in the problem the software solves — tangled dependencies, special cases, inconsistent naming.","Hardware faults are largely independent — one disk dying doesn't make another die, so redundancy (RAID, replicas) works well.","A fault is one component deviating from spec (a disk dies); a failure is the whole system stopping service."]},{"id":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable#Q8-5136","section":"book","module":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable","moduleName":"01 reliable scalable maintainable","qIndex":8,"difficulty":"intermediate","question":"Why are correlated software faults more dangerous than uncorrelated hardware faults?","answerFull":"Hardware faults are largely independent — one disk dying doesn't make another die, so redundancy (RAID, replicas) works well. Software faults are correlated: a leap-second bug or a bad input pattern hits every node simultaneously, so your redundant copies all fail at once and redundancy gives no protection. Correlation defeats the statistical independence that hardware redundancy relies on.","correct":"Hardware faults are largely independent — one disk dying doesn't make another die, so redundancy (RAID, replicas) works well.","distractors":["Accidental complexity is complexity that arises from the implementation rather than being inherent in the problem the software solves — tangled dependencies, special cases, inconsistent naming.","A fault is one component deviating from spec (a disk dies); a failure is the whole system stopping service.","Hardware faults (disk/RAM/power) are mostly uncorrelated and handled by redundancy and, increasingly, software fault-tolerance enabling rolling restarts."]},{"id":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable#Q10-5137","section":"book","module":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable","moduleName":"01 reliable scalable maintainable","qIndex":10,"difficulty":"intermediate","question":"What is accidental complexity, and what is the primary tool for reducing it?","answerFull":"Accidental complexity is complexity that arises from the implementation rather than being inherent in the problem the software solves — tangled dependencies, special cases, inconsistent naming. The primary tool against it is good abstraction: a clean, reusable interface that hides implementation detail (SQL hiding storage engines, a high-level language hiding machine code). Removing accidental complexity is the essence of \"simplicity.\"","correct":"Accidental complexity is complexity that arises from the implementation rather than being inherent in the problem the software solves — tangled dependencies, special cases, inconsistent naming.","distractors":["Hardware faults (disk/RAM/power) are mostly uncorrelated and handled by redundancy and, increasingly, software fault-tolerance enabling rolling restarts.","Scaling up (a bigger machine) is operationally simple and avoids distributed-systems complexity but is expensive at the high end and has a hard ceiling.","Because response-time distributions are heavily skewed by outliers, so the mean describes no actual user — a few very slow requests drag it up while still understating the worst case."]},{"id":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable#Q11-5138","section":"book","module":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable","moduleName":"01 reliable scalable maintainable","qIndex":11,"difficulty":"advanced","question":"Compare scaling up and scaling out; when would you still choose to scale up?","answerFull":"Scaling up (a bigger machine) is operationally simple and avoids distributed-systems complexity but is expensive at the high end and has a hard ceiling. Scaling out (more commodity machines, shared-nothing) is cost-effective and tolerates machine loss but imports all the hard problems of Parts II–III. You scale up when the workload fits comfortably on one machine and you value simplicity and strong single-node consistency over a distributed system's complexity.","correct":"Scaling up (a bigger machine) is operationally simple and avoids distributed-systems complexity but is expensive at the high end and has a hard ceiling.","distractors":["Because a single user-facing request often fans out to many backend services in parallel, and the user waits for the *slowest* of them.","The real load parameter is the *fan-out* — the distribution of how many followers each user has — not the raw tweet rate.","Accidental complexity is complexity that arises from the implementation rather than being inherent in the problem the software solves — tangled dependencies, special cases, inconsistent naming."]},{"id":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable#Q12-5139","section":"book","module":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable","moduleName":"01 reliable scalable maintainable","qIndex":12,"difficulty":"advanced","question":"What does Kleppmann mean by \"there is no magic scalable architecture\"?","answerFull":"He means scalability is always relative to specific load parameters, so an architecture is designed around which operations are common and which are rare — not adopted as a universal recipe. A system tuned for 100k small requests/sec looks nothing like one tuned for a few multi-gigabyte requests per minute, even at the same nominal throughput. Copying another company's stack without matching its load profile is how you scale the wrong dimension.","correct":"He means scalability is always relative to specific load parameters, so an architecture is designed around which operations are common and which are rare — not adopted as a universal recipe.","distractors":["Because response-time distributions are heavily skewed by outliers, so the mean describes no actual user — a few very slow requests drag it up while still understating the worst case.","The real load parameter is the *fan-out* — the distribution of how many followers each user has — not the raw tweet rate.","Because the server's view excludes queueing delay, network transit, and time the request spent waiting to be accepted — exactly the components that head-of-line blocking and load inflate."]},{"id":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable#Q13-5140","section":"book","module":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable","moduleName":"01 reliable scalable maintainable","qIndex":13,"difficulty":"advanced","question":"Why does measuring response time on the client matter more than measuring it on the server?","answerFull":"Because the server's view excludes queueing delay, network transit, and time the request spent waiting to be accepted — exactly the components that head-of-line blocking and load inflate. The client sees the full response time the user actually experiences. Server-side timing can look healthy while clients are timing out.","correct":"Because the server's view excludes queueing delay, network transit, and time the request spent waiting to be accepted — exactly the components that head-of-line blocking and load inflate.","distractors":["Because response-time distributions are heavily skewed by outliers, so the mean describes no actual user — a few very slow requests drag it up while still understating the worst case.","Percentiles are not linear, so averaging them is mathematically meaningless — a server handling few requests and one handling many contribute unequally.","The real load parameter is the *fan-out* — the distribution of how many followers each user has — not the raw tweet rate."]},{"id":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable#Q16-5141","section":"book","module":"book/designing_data_intensive_applications/01_reliable_scalable_maintainable","moduleName":"01 reliable scalable maintainable","qIndex":16,"difficulty":"advanced","question":"Why does the chapter say good operations can work around bad software but not vice versa?","answerFull":"Because operability — monitoring, runbooks, fast rollback, capacity headroom — lets a skilled ops team contain and recover from a flawed system, keeping it serving users. But no amount of elegant software survives an environment with no monitoring, no rollback path, and unpredictable manual changes; the software will eventually hit a condition operations can't see or recover from. Hence investing in operability pays off even for imperfect code.","correct":"Because operability — monitoring, runbooks, fast rollback, capacity headroom — lets a skilled ops team contain and recover from a flawed system, keeping it serving users.","distractors":["Head-of-line blocking is when one slow request occupies the server (or sits at the front of a queue) and stalls all the requests queued behind it, even if those would have been fast.","Scaling up (a bigger machine) is operationally simple and avoids distributed-systems complexity but is expensive at the high end and has a hard ceiling.","Accidental complexity is complexity that arises from the implementation rather than being inherent in the problem the software solves — tangled dependencies, special cases, inconsistent naming."]},{"id":"book/designing_data_intensive_applications/02_data_models_and_query_languages#Q2-5142","section":"book","module":"book/designing_data_intensive_applications/02_data_models_and_query_languages","moduleName":"02 data models and query languages","qIndex":2,"difficulty":"core","question":"What is the object-relational impedance mismatch, and how does the document model reduce it?","answerFull":"It's the awkward translation between application objects (nested, with collections) and the flat rows/columns of the relational model, usually bridged by an ORM. The document model reduces it for tree-shaped data because a nested JSON document mirrors the object's structure directly — a person with many jobs and schools is one document, not several joined tables. It does not eliminate the mismatch for many-to-many data, where you still need references.","correct":"It's the awkward translation between application objects (nested, with collections) and the flat rows/columns of the relational model, usually bridged by an ORM.","distractors":["They converge because relational databases have added JSON/XML column types and document-like nested storage, while document databases have added references and join-like lookups.","Locality means a document's related data is stored physically together, so loading it takes one read instead of multiple joins — great when you typically need most of the document at once.","MapReduce is a bulk-processing model where you supply pure map and reduce functions and the framework distributes the work across machines — a middle ground between declarative and imperative."]},{"id":"book/designing_data_intensive_applications/02_data_models_and_query_languages#Q6-5143","section":"book","module":"book/designing_data_intensive_applications/02_data_models_and_query_languages","moduleName":"02 data models and query languages","qIndex":6,"difficulty":"intermediate","question":"When is a graph model clearly better than a relational one, and why is the equivalent SQL awkward?","answerFull":"A graph model is clearly better when many-to-many relationships dominate and you traverse variable-length paths (social networks, fraud rings, routing). The equivalent SQL is awkward because the number of joins isn't known in advance — \"people connected to X within N hops\" needs recursive CTEs (WITH RECURSIVE), which are verbose and hard to read, whereas Cypher expresses the same as a concise node-edge pattern the engine matches at any path length.","correct":"A graph model is clearly better when many-to-many relationships dominate and you traverse variable-length paths (social networks, fraud rings, routing).","distractors":["It's the awkward translation between application objects (nested, with collections) and the flat rows/columns of the relational model, usually bridged by an ORM.","They converge because relational databases have added JSON/XML column types and document-like nested storage, while document databases have added references and join-like lookups.","A property graph has vertices and edges."]},{"id":"book/designing_data_intensive_applications/02_data_models_and_query_languages#Q7-5144","section":"book","module":"book/designing_data_intensive_applications/02_data_models_and_query_languages","moduleName":"02 data models and query languages","qIndex":7,"difficulty":"intermediate","question":"Describe the property-graph model's components.","answerFull":"A property graph has vertices and edges. Each vertex carries a unique ID, a set of outgoing and incoming edges, and a collection of key-value properties. Each edge carries a unique ID, its tail (start) and head (end) vertices, a label naming the relationship type, and its own key-value properties. This structure lets any vertex connect to any other and lets multiple relationship types coexist in one graph, which is what makes traversal so flexible.","correct":"A property graph has vertices and edges.","distractors":["A graph model is clearly better when many-to-many relationships dominate and you traverse variable-length paths (social networks, fraud rings, routing).","It's the awkward translation between application objects (nested, with collections) and the flat rows/columns of the relational model, usually bridged by an ORM.","A triple-store represents all data as three-part statements: (subject, predicate, object), such as (Lucy, age, 33) or (Lucy, marriedTo, Alain)."]},{"id":"book/designing_data_intensive_applications/02_data_models_and_query_languages#Q8-5145","section":"book","module":"book/designing_data_intensive_applications/02_data_models_and_query_languages","moduleName":"02 data models and query languages","qIndex":8,"difficulty":"intermediate","question":"What is a triple-store, and how does it relate to a property graph?","answerFull":"A triple-store represents all data as three-part statements: (subject, predicate, object), such as (Lucy, age, 33) or (Lucy, marriedTo, Alain). When the object is a primitive value the triple is a property; when it's another subject the triple is an edge. This is essentially a compact, uniform encoding of a property graph and underlies RDF and the Semantic Web, queried with SPARQL.","correct":"A triple-store represents all data as three-part statements: (subject, predicate, object), such as (Lucy, age, 33) or (Lucy, marriedTo, Alain).","distractors":["Datalog stores data as simple facts (predicate(subject, object)) and expresses queries as rules that derive new predicates from existing ones, including recursively.","A property graph has vertices and edges.","A graph model is clearly better when many-to-many relationships dominate and you traverse variable-length paths (social networks, fraud rings, routing)."]},{"id":"book/designing_data_intensive_applications/02_data_models_and_query_languages#Q9-5146","section":"book","module":"book/designing_data_intensive_applications/02_data_models_and_query_languages","moduleName":"02 data models and query languages","qIndex":9,"difficulty":"intermediate","question":"What problem does locality solve in document databases, and when does it backfire?","answerFull":"Locality means a document's related data is stored physically together, so loading it takes one read instead of multiple joins — great when you typically need most of the document at once. It backfires for large documents because the database generally loads the *entire* document even to read one field, and updates usually rewrite the whole document, so big documents make reads wasteful and writes expensive. The guidance is to keep documents reasonably small.","correct":"Locality means a document's related data is stored physically together, so loading it takes one read instead of multiple joins — great when you typically need most of the document at once.","distractors":["They converge because relational databases have added JSON/XML column types and document-like nested storage, while document databases have added references and join-like lookups.","Normalization stores each fact once and references it by ID (so a region name lives in one row), which means resolving the human-readable value requires a join back to that row.","It's the awkward translation between application objects (nested, with collections) and the flat rows/columns of the relational model, usually bridged by an ORM."]},{"id":"book/designing_data_intensive_applications/02_data_models_and_query_languages#Q10-5147","section":"book","module":"book/designing_data_intensive_applications/02_data_models_and_query_languages","moduleName":"02 data models and query languages","qIndex":10,"difficulty":"intermediate","question":"Why does normalization require joins, and what's the tradeoff against denormalization?","answerFull":"Normalization stores each fact once and references it by ID (so a region name lives in one row), which means resolving the human-readable value requires a join back to that row. Denormalization duplicates the value into many records to avoid the join, speeding reads but creating update anomalies — changing the value means finding and rewriting every copy, and missing one causes inconsistency. The choice trades read simplicity against write consistency.","correct":"Normalization stores each fact once and references it by ID (so a region name lives in one row), which means resolving the human-readable value requires a join back to that row.","distractors":["Datalog stores data as simple facts (predicate(subject, object)) and expresses queries as rules that derive new predicates from existing ones, including recursively.","Locality means a document's related data is stored physically together, so loading it takes one read instead of multiple joins — great when you typically need most of the document at once.","They converge because relational databases have added JSON/XML column types and document-like nested storage, while document databases have added references and join-like lookups."]},{"id":"book/designing_data_intensive_applications/02_data_models_and_query_languages#Q11-5148","section":"book","module":"book/designing_data_intensive_applications/02_data_models_and_query_languages","moduleName":"02 data models and query languages","qIndex":11,"difficulty":"advanced","question":"What is MapReduce as a query mechanism, and why did MongoDB add a declarative aggregation pipeline alongside it?","answerFull":"MapReduce is a bulk-processing model where you supply pure map and reduce functions and the framework distributes the work across machines — a middle ground between declarative and imperative. MongoDB initially exposed MapReduce for aggregation but added the declarative aggregation pipeline because, for most queries, expressing intent declaratively is more concise and lets the engine optimize, reserving imperative functions for genuinely custom logic.","correct":"MapReduce is a bulk-processing model where you supply pure map and reduce functions and the framework distributes the work across machines — a middle ground between declarative and imperative.","distractors":["It's the awkward translation between application objects (nested, with collections) and the flat rows/columns of the relational model, usually bridged by an ORM.","A graph model is clearly better when many-to-many relationships dominate and you traverse variable-length paths (social networks, fraud rings, routing).","A property graph has vertices and edges."]},{"id":"book/designing_data_intensive_applications/02_data_models_and_query_languages#Q12-5149","section":"book","module":"book/designing_data_intensive_applications/02_data_models_and_query_languages","moduleName":"02 data models and query languages","qIndex":12,"difficulty":"advanced","question":"What is Datalog and what makes it powerful for complex queries?","answerFull":"Datalog stores data as simple facts (predicate(subject, object)) and expresses queries as rules that derive new predicates from existing ones, including recursively. Its power is composability: you build complex queries from small, named, reusable rules that can reference each other, which scales to sophisticated logic better than writing one giant query. It's less convenient for quick one-offs but excellent for reusable analytical logic, and it underlies systems like Datomic.","correct":"Datalog stores data as simple facts (predicate(subject, object)) and expresses queries as rules that derive new predicates from existing ones, including recursively.","distractors":["A triple-store represents all data as three-part statements: (subject, predicate, object), such as (Lucy, age, 33) or (Lucy, marriedTo, Alain).","Normalization stores each fact once and references it by ID (so a region name lives in one row), which means resolving the human-readable value requires a join back to that row.","It's the awkward translation between application objects (nested, with collections) and the flat rows/columns of the relational model, usually bridged by an ORM."]},{"id":"book/designing_data_intensive_applications/02_data_models_and_query_languages#Q15-5150","section":"book","module":"book/designing_data_intensive_applications/02_data_models_and_query_languages","moduleName":"02 data models and query languages","qIndex":15,"difficulty":"advanced","question":"How do relational and document databases converge, and why does that matter for choosing one?","answerFull":"They converge because relational databases have added JSON/XML column types and document-like nested storage, while document databases have added references and join-like lookups. This matters because the choice is less binary than it appears: you can get document-style flexibility inside PostgreSQL or relational-style references inside MongoDB, so you should choose based on which model is *primary* for your dominant access pattern rather than assuming you must forgo the other's features entirely.","correct":"They converge because relational databases have added JSON/XML column types and document-like nested storage, while document databases have added references and join-like lookups.","distractors":["It's the awkward translation between application objects (nested, with collections) and the flat rows/columns of the relational model, usually bridged by an ORM.","Locality means a document's related data is stored physically together, so loading it takes one read instead of multiple joins — great when you typically need most of the document at once.","Normalization stores each fact once and references it by ID (so a region name lives in one row), which means resolving the human-readable value requires a join back to that row."]},{"id":"book/designing_data_intensive_applications/03_storage_and_retrieval#Q1-5151","section":"book","module":"book/designing_data_intensive_applications/03_storage_and_retrieval","moduleName":"03 storage and retrieval","qIndex":1,"difficulty":"core","question":"What is the fundamental tradeoff between LSM-trees and B-trees?","answerFull":"LSM-trees optimize writes and B-trees optimize reads. LSM-trees turn all writes into sequential appends (memtable flush plus WAL) and defer cleanup to background compaction, giving very high write throughput; reads may have to consult the memtable plus several SSTable levels. B-trees update pages in place with random I/O, giving lower write throughput but a single, predictable read path down the tree. So you pick LSM for write-heavy workloads and B-trees for read-heavy or latency-sensitive ones.","correct":"LSM-trees optimize writes and B-trees optimize reads.","distractors":["Write amplification is the number of physical writes caused by one logical write, and it limits sustainable write throughput while wearing out SSDs.","You use the same trick as LSM-trees: buffer incoming writes in an in-memory row-oriented structure (a sorted store), and periodically merge that buffer into the on-disk column files in bulk.","The main speedup is *not* avoiding disk reads — the OS page cache already serves hot data from RAM in disk-backed databases."]},{"id":"book/designing_data_intensive_applications/03_storage_and_retrieval#Q2-5152","section":"book","module":"book/designing_data_intensive_applications/03_storage_and_retrieval","moduleName":"03 storage and retrieval","qIndex":2,"difficulty":"core","question":"What is write amplification, and how does it differ between the two engine families?","answerFull":"Write amplification is the number of physical writes caused by one logical write, and it limits sustainable write throughput while wearing out SSDs. B-trees write each datum at least twice — once to the WAL and once to the page — plus extra writes when pages split. LSM-trees write once sequentially but then rewrite the same data repeatedly during compaction as segments merge. Both pay it; LSM usually still sustains higher write throughput because its writes are sequential and its compaction is tunable.","correct":"Write amplification is the number of physical writes caused by one logical write, and it limits sustainable write throughput while wearing out SSDs.","distractors":["Because each page holds a large number of child references — the branching factor — typically several hundred.","Appending writes the data to the end of a file (and to the WAL) sequentially, never seeking to overwrite existing data in place.","A star schema centers on a large fact table (one row per event, like a sale) with foreign keys radiating out to dimension tables describing the who/what/where/when (product, customer, store, date) — drawn as a star."]},{"id":"book/designing_data_intensive_applications/03_storage_and_retrieval#Q4-5153","section":"book","module":"book/designing_data_intensive_applications/03_storage_and_retrieval","moduleName":"03 storage and retrieval","qIndex":4,"difficulty":"core","question":"Why are all writes in a log-structured store appends, and why is that fast?","answerFull":"Appending writes the data to the end of a file (and to the WAL) sequentially, never seeking to overwrite existing data in place. Sequential writes are dramatically faster than random writes on spinning disks and are also gentler on SSDs (fewer erase cycles, less wear). The cost is deferred: overwritten and deleted values accumulate until background compaction reclaims the space.","correct":"Appending writes the data to the end of a file (and to the WAL) sequentially, never seeking to overwrite existing data in place.","distractors":["Write amplification is the number of physical writes caused by one logical write, and it limits sustainable write throughput while wearing out SSDs.","You use the same trick as LSM-trees: buffer incoming writes in an in-memory row-oriented structure (a sorted store), and periodically merge that buffer into the on-disk column files in bulk.","LSM-trees optimize writes and B-trees optimize reads."]},{"id":"book/designing_data_intensive_applications/03_storage_and_retrieval#Q5-5154","section":"book","module":"book/designing_data_intensive_applications/03_storage_and_retrieval","moduleName":"03 storage and retrieval","qIndex":5,"difficulty":"core","question":"What does compaction do in an LSM-tree, and what happens if it can't keep up?","answerFull":"Compaction merges SSTable segments, discarding overwritten and deleted keys so that only the latest value per key survives, which bounds disk usage and keeps read paths short. If compaction falls behind sustained heavy writes, unmerged segments accumulate, so reads must scan more segments and latency degrades; eventually disk fills. This makes compaction backlog a critical metric to monitor, and it's why LSM tail latency can spike when compaction competes with foreground I/O.","correct":"Compaction merges SSTable segments, discarding overwritten and deleted keys so that only the latest value per key survives, which bounds disk usage and keeps read paths short.","distractors":["LSM-trees optimize writes and B-trees optimize reads.","The main speedup is *not* avoiding disk reads — the OS page cache already serves hot data from RAM in disk-backed databases.","You use the same trick as LSM-trees: buffer incoming writes in an in-memory row-oriented structure (a sorted store), and periodically merge that buffer into the on-disk column files in bulk."]},{"id":"book/designing_data_intensive_applications/03_storage_and_retrieval#Q6-5155","section":"book","module":"book/designing_data_intensive_applications/03_storage_and_retrieval","moduleName":"03 storage and retrieval","qIndex":6,"difficulty":"intermediate","question":"Why do B-trees keep a write-ahead log if they update pages in place?","answerFull":"Because some operations touch multiple pages — most importantly a page split, which splits one full page into two and updates the parent. A crash partway through would leave the tree corrupted (a dangling or orphaned page). The WAL (redo log) records the intended changes before they're applied so that recovery after a crash can replay the log to complete or roll back the multi-page operation and restore a consistent tree.","correct":"Because some operations touch multiple pages — most importantly a page split, which splits one full page into two and updates the parent.","distractors":["Appending writes the data to the end of a file (and to the WAL) sequentially, never seeking to overwrite existing data in place.","You use the same trick as LSM-trees: buffer incoming writes in an in-memory row-oriented structure (a sorted store), and periodically merge that buffer into the on-disk column files in bulk.","LSM-trees optimize writes and B-trees optimize reads."]},{"id":"book/designing_data_intensive_applications/03_storage_and_retrieval#Q7-5156","section":"book","module":"book/designing_data_intensive_applications/03_storage_and_retrieval","moduleName":"03 storage and retrieval","qIndex":7,"difficulty":"intermediate","question":"How can a B-tree with 4 KB pages index a 256 TB dataset in only four levels?","answerFull":"Because each page holds a large number of child references — the branching factor — typically several hundred. With a branching factor of 500, one level addresses 500 pages, two levels 500², three levels 500³, and four levels 500⁴ pages; at 4 KB per page that's on the order of hundreds of terabytes. The high branching factor keeps the tree shallow, so a lookup follows only about four page reads regardless of dataset size.","correct":"Because each page holds a large number of child references — the branching factor — typically several hundred.","distractors":["A covering index stores some of a row's columns directly in the index, so a query that needs only those columns is answered entirely from the index without reading the row from the heap or clustered store.","Write amplification is the number of physical writes caused by one logical write, and it limits sustainable write throughput while wearing out SSDs.","A clustered index stores the actual row data *within* the index structure ordered by the index key — MySQL InnoDB stores rows in the primary-key B-tree, so a primary-key lookup returns the row directly."]},{"id":"book/designing_data_intensive_applications/03_storage_and_retrieval#Q8-5157","section":"book","module":"book/designing_data_intensive_applications/03_storage_and_retrieval","moduleName":"03 storage and retrieval","qIndex":8,"difficulty":"intermediate","question":"What is a clustered index versus a heap file with a non-clustered index?","answerFull":"A clustered index stores the actual row data *within* the index structure ordered by the index key — MySQL InnoDB stores rows in the primary-key B-tree, so a primary-key lookup returns the row directly. The alternative stores rows in a separate heap file and has the index hold a reference (pointer) into the heap; this avoids duplicating row data across multiple secondary indexes but adds an indirection hop. Clustered indexes speed reads on the clustering key but make secondary-index lookups go through it.","correct":"A clustered index stores the actual row data *within* the index structure ordered by the index key — MySQL InnoDB stores rows in the primary-key B-tree, so a primary-key lookup returns the row directly.","distractors":["For a column with few distinct values (like country or product category), bitmap encoding stores one bitmap per distinct value, where bit *i* indicates whether row *i* has that value.","A star schema centers on a large fact table (one row per event, like a sale) with foreign keys radiating out to dimension tables describing the who/what/where/when (product, customer, store, date) — drawn as a star.","You use the same trick as LSM-trees: buffer incoming writes in an in-memory row-oriented structure (a sorted store), and periodically merge that buffer into the on-disk column files in bulk."]},{"id":"book/designing_data_intensive_applications/03_storage_and_retrieval#Q9-5158","section":"book","module":"book/designing_data_intensive_applications/03_storage_and_retrieval","moduleName":"03 storage and retrieval","qIndex":9,"difficulty":"intermediate","question":"What is a covering index and what problem does it solve?","answerFull":"A covering index stores some of a row's columns directly in the index, so a query that needs only those columns is answered entirely from the index without reading the row from the heap or clustered store. It solves the extra-I/O problem of index-then-fetch: the second lookup into the table is eliminated. The cost is a larger index and more write overhead, so it's worth it for hot queries on a stable set of columns.","correct":"A covering index stores some of a row's columns directly in the index, so a query that needs only those columns is answered entirely from the index without reading the row from the heap or clustered store.","distractors":["You use the same trick as LSM-trees: buffer incoming writes in an in-memory row-oriented structure (a sorted store), and periodically merge that buffer into the on-disk column files in bulk.","For a column with few distinct values (like country or product category), bitmap encoding stores one bitmap per distinct value, where bit *i* indicates whether row *i* has that value.","Because their access patterns conflict: OLTP needs low-latency point reads/writes from user requests, while OLAP runs heavy ad-hoc scan-and-aggregate queries over huge datasets."]},{"id":"book/designing_data_intensive_applications/03_storage_and_retrieval#Q10-5159","section":"book","module":"book/designing_data_intensive_applications/03_storage_and_retrieval","moduleName":"03 storage and retrieval","qIndex":10,"difficulty":"intermediate","question":"Why do analytic workloads use column-oriented storage instead of row-oriented?","answerFull":"Analytic queries typically aggregate over millions of rows but touch only a handful of a fact table's many columns. Row-oriented storage forces the engine to read every column of every row off disk and then discard the unneeded ones, wasting most of the I/O. Column-oriented storage keeps each column's values together, so the query reads only the columns it references — often a 90%+ reduction in disk I/O — and the similar values within a column compress extremely well.","correct":"Analytic queries typically aggregate over millions of rows but touch only a handful of a fact table's many columns.","distractors":["A clustered index stores the actual row data *within* the index structure ordered by the index key — MySQL InnoDB stores rows in the primary-key B-tree, so a primary-key lookup returns the row directly.","You use the same trick as LSM-trees: buffer incoming writes in an in-memory row-oriented structure (a sorted store), and periodically merge that buffer into the on-disk column files in bulk.","Because some operations touch multiple pages — most importantly a page split, which splits one full page into two and updates the parent."]},{"id":"book/designing_data_intensive_applications/03_storage_and_retrieval#Q11-5160","section":"book","module":"book/designing_data_intensive_applications/03_storage_and_retrieval","moduleName":"03 storage and retrieval","qIndex":11,"difficulty":"advanced","question":"How does bitmap encoding compress a column, and why is it good for analytics?","answerFull":"For a column with few distinct values (like country or product category), bitmap encoding stores one bitmap per distinct value, where bit *i* indicates whether row *i* has that value. These bitmaps are tiny and often further run-length compressed. They make filters like WHERE product_id IN (30, 68, 69) fast: load those three bitmaps and OR them together to get all matching rows, with no row scan — a perfect fit for analytic filtering on low-cardinality columns.","correct":"For a column with few distinct values (like country or product category), bitmap encoding stores one bitmap per distinct value, where bit *i* indicates whether row *i* has that value.","distractors":["A star schema centers on a large fact table (one row per event, like a sale) with foreign keys radiating out to dimension tables describing the who/what/where/when (product, customer, store, date) — drawn as a star.","A clustered index stores the actual row data *within* the index structure ordered by the index key — MySQL InnoDB stores rows in the primary-key B-tree, so a primary-key lookup returns the row directly.","A covering index stores some of a row's columns directly in the index, so a query that needs only those columns is answered entirely from the index without reading the row from the heap or clustered store."]},{"id":"book/designing_data_intensive_applications/03_storage_and_retrieval#Q12-5161","section":"book","module":"book/designing_data_intensive_applications/03_storage_and_retrieval","moduleName":"03 storage and retrieval","qIndex":12,"difficulty":"advanced","question":"How do you write to a column store given that in-place row inserts are hard?","answerFull":"You use the same trick as LSM-trees: buffer incoming writes in an in-memory row-oriented structure (a sorted store), and periodically merge that buffer into the on-disk column files in bulk. Queries read both the in-memory buffer and the column files and combine the results. This keeps writes cheap and amortized while preserving the column files' read efficiency, which is why systems like Vertica adopt an LSM-style write path.","correct":"You use the same trick as LSM-trees: buffer incoming writes in an in-memory row-oriented structure (a sorted store), and periodically merge that buffer into the on-disk column files in bulk.","distractors":["A covering index stores some of a row's columns directly in the index, so a query that needs only those columns is answered entirely from the index without reading the row from the heap or clustered store.","LSM-trees optimize writes and B-trees optimize reads.","A star schema centers on a large fact table (one row per event, like a sale) with foreign keys radiating out to dimension tables describing the who/what/where/when (product, customer, store, date) — drawn as a star."]},{"id":"book/designing_data_intensive_applications/03_storage_and_retrieval#Q13-5162","section":"book","module":"book/designing_data_intensive_applications/03_storage_and_retrieval","moduleName":"03 storage and retrieval","qIndex":13,"difficulty":"advanced","question":"What are a star schema and a snowflake schema, and how do they differ?","answerFull":"A star schema centers on a large fact table (one row per event, like a sale) with foreign keys radiating out to dimension tables describing the who/what/where/when (product, customer, store, date) — drawn as a star. A snowflake schema goes further and normalizes those dimensions into sub-dimension tables (e.g. product → brand and category tables), which reduces redundancy but adds joins. Star is more common in warehouses because it's simpler and analysts find it easier to query.","correct":"A star schema centers on a large fact table (one row per event, like a sale) with foreign keys radiating out to dimension tables describing the who/what/where/when (product, customer, store, date) — drawn as a star.","distractors":["Analytic queries typically aggregate over millions of rows but touch only a handful of a fact table's many columns.","You use the same trick as LSM-trees: buffer incoming writes in an in-memory row-oriented structure (a sorted store), and periodically merge that buffer into the on-disk column files in bulk.","For a column with few distinct values (like country or product category), bitmap encoding stores one bitmap per distinct value, where bit *i* indicates whether row *i* has that value."]},{"id":"book/designing_data_intensive_applications/03_storage_and_retrieval#Q14-5163","section":"book","module":"book/designing_data_intensive_applications/03_storage_and_retrieval","moduleName":"03 storage and retrieval","qIndex":14,"difficulty":"advanced","question":"Why did OLTP and OLAP split into separate systems instead of sharing one database?","answerFull":"Because their access patterns conflict: OLTP needs low-latency point reads/writes from user requests, while OLAP runs heavy ad-hoc scan-and-aggregate queries over huge datasets. Running analytics on the OLTP database makes those expensive scans compete for resources and spike user-facing latency. So companies extract data into a separate read-only data warehouse via ETL, optimized (column storage, star schemas) for analytics without endangering the transactional system.","correct":"Because their access patterns conflict: OLTP needs low-latency point reads/writes from user requests, while OLAP runs heavy ad-hoc scan-and-aggregate queries over huge datasets.","distractors":["Analytic queries typically aggregate over millions of rows but touch only a handful of a fact table's many columns.","Appending writes the data to the end of a file (and to the WAL) sequentially, never seeking to overwrite existing data in place.","Write amplification is the number of physical writes caused by one logical write, and it limits sustainable write throughput while wearing out SSDs."]},{"id":"book/designing_data_intensive_applications/03_storage_and_retrieval#Q15-5164","section":"book","module":"book/designing_data_intensive_applications/03_storage_and_retrieval","moduleName":"03 storage and retrieval","qIndex":15,"difficulty":"advanced","question":"What actually makes in-memory databases fast, given that the OS already caches disk pages?","answerFull":"The main speedup is *not* avoiding disk reads — the OS page cache already serves hot data from RAM in disk-backed databases. It's that an in-memory database avoids the overhead of encoding/decoding its in-memory data structures into a disk-friendly byte format. It can also offer data models that are awkward on disk, like Redis's sets and priority queues. Durability is added separately via append-only logs, periodic snapshots, or replication to other nodes.","correct":"The main speedup is *not* avoiding disk reads — the OS page cache already serves hot data from RAM in disk-backed databases.","distractors":["Compaction merges SSTable segments, discarding overwritten and deleted keys so that only the latest value per key survives, which bounds disk usage and keeps read paths short.","LSM-trees optimize writes and B-trees optimize reads.","Because each page holds a large number of child references — the branching factor — typically several hundred."]},{"id":"book/designing_data_intensive_applications/06_partitioning#Q3-5165","section":"book","module":"book/designing_data_intensive_applications/06_partitioning","moduleName":"06 partitioning","qIndex":3,"difficulty":"core","question":"What is a hot spot, and why doesn't hash partitioning solve every hot-spot problem?","answerFull":"A hot spot is a partition or key that receives disproportionately more load than others, bottlenecking the system on one node and defeating the purpose of partitioning. Hash partitioning evens out load across *different* keys, but it can't help when a *single* key is extremely hot — a celebrity account or a viral post — because all requests for that one key hash to the same partition. No system auto-corrects this; the application must split the hot key (e.g. append a random suffix) and fan out reads across the resulting keys.","correct":"A hot spot is a partition or key that receives disproportionately more load than others, bottlenecking the system on one node and defeating the purpose of partitioning.","distractors":["Total order broadcast (atomic broadcast) is a protocol that delivers messages to all nodes reliably (none lost) and in exactly the same order on every node.","Skew is an uneven distribution of data or query load across partitions, where some partitions hold far more data or serve far more requests than others.","They're largely independent techniques used together: partitioning decides which records go on which node, while replication keeps multiple copies of each partition for fault tolerance."]},{"id":"book/designing_data_intensive_applications/06_partitioning#Q5-5166","section":"book","module":"book/designing_data_intensive_applications/06_partitioning","moduleName":"06 partitioning","qIndex":5,"difficulty":"core","question":"What is scatter/gather, and why does its latency get worse as you add partitions?","answerFull":"Scatter/gather is the read pattern of a local secondary index: the query is sent to all partitions (\"scatter\"), each searches its local index, and the results are merged (\"gather\"). Its latency is governed by the *slowest* partition to respond, so as the number of partitions grows, the probability that at least one is slow (GC pause, hot, degraded) rises, pushing up the overall p99. This is why secondary-index reads on heavily partitioned local indexes can have surprisingly bad tail latency.","correct":"Scatter/gather is the read pattern of a local secondary index: the query is sent to all partitions (\"scatter\"), each searches its local index, and the results are merged (\"gather\").","distractors":["Skew is an uneven distribution of data or query load across partitions, where some partitions hold far more data or serve far more requests than others.","Databases (store and retrieve), caches (remember expensive results), search indexes (filter/search), stream processing (async messaging), and batch processing (periodic bulk crunching).","The actor model structures concurrency as independent actors that hold local state and communicate only by sending each other asynchronous messages, avoiding shared-memory locking."]},{"id":"book/designing_data_intensive_applications/06_partitioning#Q8-5167","section":"book","module":"book/designing_data_intensive_applications/06_partitioning","moduleName":"06 partitioning","qIndex":8,"difficulty":"intermediate","question":"How does request routing work, and what role does ZooKeeper play?","answerFull":"Request routing directs a client's request to the node currently holding the relevant partition, which is a service-discovery problem because partitions move during rebalancing. The three shapes are: any node forwards to the right one, a routing tier forwards on the client's behalf, or partition-aware clients connect directly. ZooKeeper acts as the authoritative registry of the partition-to-node mapping — nodes register there and routing components subscribe to changes — so everyone shares a consistent, current view; Cassandra and Riak instead gossip this state peer-to-peer.","correct":"Request routing directs a client's request to the node currently holding the relevant partition, which is a service-discovery problem because partitions move during rebalancing.","distractors":["It directs your optimization effort to the actual bottleneck.","Monotonic reads guarantees a user never sees data move *backward* in time across successive reads — once they've seen a value, later reads won't show an older state.","Because a short timeout produces false positives: a node that's merely slow (a GC pause, a transient network blip) gets declared dead while it's actually still doing work."]},{"id":"book/designing_data_intensive_applications/06_partitioning#Q9-5168","section":"book","module":"book/designing_data_intensive_applications/06_partitioning","moduleName":"06 partitioning","qIndex":9,"difficulty":"intermediate","question":"How is partitioning related to and combined with replication?","answerFull":"They're largely independent techniques used together: partitioning decides which records go on which node, while replication keeps multiple copies of each partition for fault tolerance. A record belongs to exactly one partition, but that partition is replicated to several nodes; a node typically stores some partitions as leader and others as follower. Because the two concerns compose cleanly, you can reason about partitioning scheme and replication scheme separately, then combine them (e.g. hash-partitioned with a single leader per partition replicated three ways).","correct":"They're largely independent techniques used together: partitioning decides which records go on which node, while replication keeps multiple copies of each partition for fault tolerance.","distractors":["A Byzantine fault is a node behaving arbitrarily — lying, sending corrupted or contradictory messages, or actively trying to deceive others — as opposed to merely crashing or being slow.","Immutable inputs are what make batch jobs safely re-runnable, which is the foundation of batch fault tolerance and experimentation.","Hardware faults (disk/RAM/power) are mostly uncorrelated and handled by redundancy and, increasingly, software fault-tolerance enabling rolling restarts."]},{"id":"book/designing_data_intensive_applications/06_partitioning#Q10-5169","section":"book","module":"book/designing_data_intensive_applications/06_partitioning","moduleName":"06 partitioning","qIndex":10,"difficulty":"intermediate","question":"How does a compound primary key (as in Cassandra) get the best of both partitioning schemes?","answerFull":"A compound primary key hashes only the first column to choose the partition, then stores the remaining columns *sorted* within that partition. This spreads different partition keys evenly across nodes (avoiding sequential hot spots) while still allowing efficient range scans on the sorted columns *within* a single partition. For example, partition by user ID but sort that user's updates by timestamp — so \"give me this user's posts from last week\" is a fast in-partition range scan, not a scatter/gather.","correct":"A compound primary key hashes only the first column to choose the partition, then stores the remaining columns *sorted* within that partition.","distractors":["A covering index stores some of a row's columns directly in the index, so a query that needs only those columns is answered entirely from the index without reading the row from the heap or clustered store.","The shuffle is the framework-managed step between map and reduce that partitions every mapper's output by key, sorts each partition, and transfers all values for a given key across the network to a single reducer.","A hot spot is a partition or key that receives disproportionately more load than others, bottlenecking the system on one node and defeating the purpose of partitioning."]},{"id":"book/designing_data_intensive_applications/06_partitioning#Q13-5170","section":"book","module":"book/designing_data_intensive_applications/06_partitioning","moduleName":"06 partitioning","qIndex":13,"difficulty":"advanced","question":"What is skew, and how does it undermine the benefits of partitioning?","answerFull":"Skew is an uneven distribution of data or query load across partitions, where some partitions hold far more data or serve far more requests than others. It undermines partitioning because the whole point is to spread work across nodes for scalability; if one partition is a hot spot, that node becomes the bottleneck and the system performs no better than a single machine for that workload, while other nodes sit underused. Avoiding skew — through good key/hash choice and hot-key handling — is central to effective partitioning.","correct":"Skew is an uneven distribution of data or query load across partitions, where some partitions hold far more data or serve far more requests than others.","distractors":["Total order broadcast, a linearizable compare-and-set (or atomic increment) register, leader election, atomic commit, and uniqueness constraints all reduce to consensus — you can build each from the others.","Scatter/gather is the read pattern of a local secondary index: the query is sent to all partitions (\"scatter\"), each searches its local index, and the results are merged (\"gather\").","SSI is better when contention is low to moderate: reads never block and writes never block reads, so it scales read-heavy workloads far better than 2PL, and it avoids 2PL's deadlock storms."]},{"id":"book/designing_data_intensive_applications/05_replication#Q1-5171","section":"book","module":"book/designing_data_intensive_applications/05_replication","moduleName":"05 replication","qIndex":1,"difficulty":"core","question":"What is the difference between synchronous and asynchronous replication, and what does each risk?","answerFull":"Synchronous replication makes the leader wait for a follower to confirm a write before acknowledging success, guaranteeing the follower is up to date but blocking all writes if that follower is slow or down. Asynchronous replication has the leader acknowledge immediately and propagate changes in the background, giving low latency and resilience to slow followers but risking the loss of recently acknowledged writes if the leader fails before they propagate. Semi-synchronous is the common compromise: one synchronous follower guarantees two copies, the rest async.","correct":"Synchronous replication makes the leader wait for a follower to confirm a write before acknowledging success, guaranteeing the follower is up to date but blocking all writes if that follower is slow or down.","distractors":["Consistent-prefix reads guarantees that if a sequence of writes happens in a causal order, any reader sees them in that same order — never an answer before its question.","Because \"last\" is decided by a timestamp, and when two writes are genuinely concurrent, LWW keeps one and silently discards the other with no error — that's data loss, not conflict resolution.","Split brain is when two nodes both believe they are the leader and both accept writes, leading to divergent, conflicting data that's hard to reconcile."]},{"id":"book/designing_data_intensive_applications/05_replication#Q3-5172","section":"book","module":"book/designing_data_intensive_applications/05_replication","moduleName":"05 replication","qIndex":3,"difficulty":"core","question":"What does \"w + r > n\" guarantee in a leaderless quorum, and what does it NOT guarantee?","answerFull":"It guarantees that the set of replicas a write touched and the set a read consults overlap in at least one node, so the read sees at least one replica holding the latest write — i.e. reads are reasonably fresh. It does NOT guarantee linearizability: concurrent writes (which value wins?), writes that partially succeed and aren't rolled back, sloppy quorums during partitions, and a node being restored from a stale replica can all still surface stale or conflicting values. Quorums give strong-ish, not strong, consistency.","correct":"It guarantees that the set of replicas a write touched and the set a read consults overlap in at least one node, so the read sees at least one replica holding the latest write — i.e. reads are reasonably fresh.","distractors":["Because \"eventual\" consistency offers no bound on how stale a read can be — under load or failure, lag can be seconds or minutes, long enough to break user expectations and cause bugs reported as data loss.","A version vector is a set of version numbers, one per replica, that together capture the causal history of a value across all replicas.","Consistent-prefix reads guarantees that if a sequence of writes happens in a causal order, any reader sees them in that same order — never an answer before its question."]},{"id":"book/designing_data_intensive_applications/05_replication#Q5-5173","section":"book","module":"book/designing_data_intensive_applications/05_replication","moduleName":"05 replication","qIndex":5,"difficulty":"core","question":"What is monotonic reads, and what anomaly does it prevent?","answerFull":"Monotonic reads guarantees a user never sees data move *backward* in time across successive reads — once they've seen a value, later reads won't show an older state. It prevents the anomaly where a first read hits an up-to-date follower (showing a new comment) and a second read hits a more-lagging follower (the comment disappears). It's achieved by routing each user consistently to the same replica, for example by hashing the user ID, so they don't bounce between followers at different lag.","correct":"Monotonic reads guarantees a user never sees data move *backward* in time across successive reads — once they've seen a value, later reads won't show an older state.","distractors":["Consistent-prefix reads guarantees that if a sequence of writes happens in a causal order, any reader sees them in that same order — never an answer before its question.","It guarantees that the set of replicas a write touched and the set a read consults overlap in at least one node, so the read sees at least one replica holding the latest write — i.e. reads are reasonably fresh.","Because \"eventual\" consistency offers no bound on how stale a read can be — under load or failure, lag can be seconds or minutes, long enough to break user expectations and cause bugs reported as data loss."]},{"id":"book/designing_data_intensive_applications/05_replication#Q6-5174","section":"book","module":"book/designing_data_intensive_applications/05_replication","moduleName":"05 replication","qIndex":6,"difficulty":"intermediate","question":"What is consistent-prefix reads, and why are partitioned databases especially prone to violating it?","answerFull":"Consistent-prefix reads guarantees that if a sequence of writes happens in a causal order, any reader sees them in that same order — never an answer before its question. Partitioned (sharded) databases are especially prone to violating it because different partitions replicate independently at different speeds, so causally related writes that landed on different partitions can become visible out of order. Preventing it requires tracking causal relationships (e.g. version vectors) so related writes are ordered consistently.","correct":"Consistent-prefix reads guarantees that if a sequence of writes happens in a causal order, any reader sees them in that same order — never an answer before its question.","distractors":["Synchronous replication makes the leader wait for a follower to confirm a write before acknowledging success, guaranteeing the follower is up to date but blocking all writes if that follower is slow or down.","They are the two mechanisms that make replicas converge after a node was unavailable for some writes.","Monotonic reads guarantees a user never sees data move *backward* in time across successive reads — once they've seen a value, later reads won't show an older state."]},{"id":"book/designing_data_intensive_applications/05_replication#Q8-5175","section":"book","module":"book/designing_data_intensive_applications/05_replication","moduleName":"05 replication","qIndex":8,"difficulty":"intermediate","question":"Why is \"last write wins\" dangerous as a conflict-resolution strategy?","answerFull":"Because \"last\" is decided by a timestamp, and when two writes are genuinely concurrent, LWW keeps one and silently discards the other with no error — that's data loss, not conflict resolution. It also depends on clock synchronization across nodes, and clock skew can make a logically earlier write win or an earlier one lose. It's acceptable only when losing concurrent writes is tolerable (e.g. caching); for data you can't afford to drop, use version vectors to preserve siblings or design keys so writes never conflict.","correct":"Because \"last\" is decided by a timestamp, and when two writes are genuinely concurrent, LWW keeps one and silently discards the other with no error — that's data loss, not conflict resolution.","distractors":["Because \"eventual\" consistency offers no bound on how stale a read can be — under load or failure, lag can be seconds or minutes, long enough to break user expectations and cause bugs reported as data loss.","Consistent-prefix reads guarantees that if a sequence of writes happens in a causal order, any reader sees them in that same order — never an answer before its question.","Split brain is when two nodes both believe they are the leader and both accept writes, leading to divergent, conflicting data that's hard to reconcile."]},{"id":"book/designing_data_intensive_applications/05_replication#Q9-5176","section":"book","module":"book/designing_data_intensive_applications/05_replication","moduleName":"05 replication","qIndex":9,"difficulty":"intermediate","question":"Explain \"happens-before\" and concurrency, and why concurrency (not wall-clock time) defines a conflict.","answerFull":"Operation A happens-before B if B could have known about or depended on A (e.g. A's result was read before B was issued); if neither operation knew about the other, they are concurrent. A conflict is defined by concurrency, not real time, because two operations issued at different wall-clock moments can still be concurrent if neither had seen the other — and conversely, true ordering is about causal dependence, which clocks can't reliably capture. The system must therefore track causal history, not timestamps, to know what actually conflicts.","correct":"Operation A happens-before B if B could have known about or depended on A (e.g.","distractors":["Monotonic reads guarantees a user never sees data move *backward* in time across successive reads — once they've seen a value, later reads won't show an older state.","Consistent-prefix reads guarantees that if a sequence of writes happens in a causal order, any reader sees them in that same order — never an answer before its question.","Because \"last\" is decided by a timestamp, and when two writes are genuinely concurrent, LWW keeps one and silently discards the other with no error — that's data loss, not conflict resolution."]},{"id":"book/designing_data_intensive_applications/05_replication#Q10-5177","section":"book","module":"book/designing_data_intensive_applications/05_replication","moduleName":"05 replication","qIndex":10,"difficulty":"intermediate","question":"What is a version vector and what problem does it solve that a single version number can't?","answerFull":"A version vector is a set of version numbers, one per replica, that together capture the causal history of a value across all replicas. A single version number works when one node assigns it, but with multiple replicas accepting writes you need to know what each replica had seen to determine whether two writes are causally ordered or concurrent. The version vector lets the system detect concurrent writes and retain them as siblings for later merging, rather than a single counter forcing it to wrongly conclude one write supersedes another and dropping data.","correct":"A version vector is a set of version numbers, one per replica, that together capture the causal history of a value across all replicas.","distractors":["Consistent-prefix reads guarantees that if a sequence of writes happens in a causal order, any reader sees them in that same order — never an answer before its question.","It guarantees that the set of replicas a write touched and the set a read consults overlap in at least one node, so the read sees at least one replica holding the latest write — i.e. reads are reasonably fresh.","They are the two mechanisms that make replicas converge after a node was unavailable for some writes."]},{"id":"book/designing_data_intensive_applications/05_replication#Q11-5178","section":"book","module":"book/designing_data_intensive_applications/05_replication","moduleName":"05 replication","qIndex":11,"difficulty":"advanced","question":"What are read repair and anti-entropy in leaderless systems?","answerFull":"They are the two mechanisms that make replicas converge after a node was unavailable for some writes. Read repair happens during normal reads: when a client reads from multiple replicas and detects one returned a stale value, it writes the newer value back to that replica. Anti-entropy is a continuous background process that compares data between replicas and copies over anything missing, independent of reads. Read repair fixes frequently read keys quickly; anti-entropy ensures even rarely read keys eventually converge.","correct":"They are the two mechanisms that make replicas converge after a node was unavailable for some writes.","distractors":["A sloppy quorum accepts a write on whatever w reachable nodes exist during a network partition, even nodes outside the key's designated home replicas, to keep accepting writes when the home replicas are unreachable.","Split brain is when two nodes both believe they are the leader and both accept writes, leading to divergent, conflicting data that's hard to reconcile.","A version vector is a set of version numbers, one per replica, that together capture the causal history of a value across all replicas."]},{"id":"book/designing_data_intensive_applications/05_replication#Q12-5179","section":"book","module":"book/designing_data_intensive_applications/05_replication","moduleName":"05 replication","qIndex":12,"difficulty":"advanced","question":"What are sloppy quorums and hinted handoff, and how do they affect the quorum guarantee?","answerFull":"A sloppy quorum accepts a write on whatever w reachable nodes exist during a network partition, even nodes outside the key's designated home replicas, to keep accepting writes when the home replicas are unreachable. Hinted handoff is the follow-up: those temporary nodes hold the write and forward it to the proper home replicas once the partition heals. They increase write availability but weaken the w+r>n guarantee, because during the partition the read set and the write set may no longer overlap, so reads can miss recent writes.","correct":"A sloppy quorum accepts a write on whatever w reachable nodes exist during a network partition, even nodes outside the key's designated home replicas, to keep accepting writes when the home replicas are unreachable.","distractors":["Because \"last\" is decided by a timestamp, and when two writes are genuinely concurrent, LWW keeps one and silently discards the other with no error — that's data loss, not conflict resolution.","Synchronous replication makes the leader wait for a follower to confirm a write before acknowledging success, guaranteeing the follower is up to date but blocking all writes if that follower is slow or down.","A version vector is a set of version numbers, one per replica, that together capture the causal history of a value across all replicas."]},{"id":"book/designing_data_intensive_applications/05_replication#Q13-5180","section":"book","module":"book/designing_data_intensive_applications/05_replication","moduleName":"05 replication","qIndex":13,"difficulty":"advanced","question":"Compare the replication-log implementations and say which feeds change data capture.","answerFull":"Statement-based logs ship the SQL statements but break on nondeterministic functions and side effects, so they're mostly abandoned. WAL shipping sends the storage engine's low-level byte log — efficient but tightly coupled to the storage format and DB version, blocking zero-downtime upgrades. Logical (row-based) logs describe changes at the row level, decoupled from the storage engine, allowing heterogeneous replica versions; this is what change data capture consumes to feed downstream systems like search indexes and stream processors. Trigger-based replication runs in the application layer for flexibility at higher overhead.","correct":"Statement-based logs ship the SQL statements but break on nondeterministic functions and side effects, so they're mostly abandoned.","distractors":["Because \"eventual\" consistency offers no bound on how stale a read can be — under load or failure, lag can be seconds or minutes, long enough to break user expectations and cause bugs reported as data loss.","Synchronous replication makes the leader wait for a follower to confirm a write before acknowledging success, guaranteeing the follower is up to date but blocking all writes if that follower is slow or down.","The leader takes a consistent snapshot of its database at a particular point in the replication log (most databases can do this without locking, using MVCC)."]},{"id":"book/designing_data_intensive_applications/05_replication#Q14-5181","section":"book","module":"book/designing_data_intensive_applications/05_replication","moduleName":"05 replication","qIndex":14,"difficulty":"advanced","question":"How does a new follower bootstrap without locking the leader?","answerFull":"The leader takes a consistent snapshot of its database at a particular point in the replication log (most databases can do this without locking, using MVCC). That snapshot is copied to the new follower, which records the exact log position the snapshot corresponds to. The follower then connects to the leader and requests all changes that occurred *since* that position, replaying them to catch up to the current state, after which it stays current by continuously applying the live replication stream.","correct":"The leader takes a consistent snapshot of its database at a particular point in the replication log (most databases can do this without locking, using MVCC).","distractors":["Synchronous replication makes the leader wait for a follower to confirm a write before acknowledging success, guaranteeing the follower is up to date but blocking all writes if that follower is slow or down.","Split brain is when two nodes both believe they are the leader and both accept writes, leading to divergent, conflicting data that's hard to reconcile.","Consistent-prefix reads guarantees that if a sequence of writes happens in a causal order, any reader sees them in that same order — never an answer before its question."]},{"id":"book/designing_data_intensive_applications/05_replication#Q16-5182","section":"book","module":"book/designing_data_intensive_applications/05_replication","moduleName":"05 replication","qIndex":16,"difficulty":"advanced","question":"Why does the book warn against ignoring replication lag, and what's the practical takeaway?","answerFull":"Because \"eventual\" consistency offers no bound on how stale a read can be — under load or failure, lag can be seconds or minutes, long enough to break user expectations and cause bugs reported as data loss. The practical takeaway is to explicitly decide which operations require freshness and engineer the specific guarantee they need (read-your-writes, monotonic reads, consistent prefix) or route them to the leader, rather than assuming asynchronous followers will \"always be fast enough.\"","correct":"Because \"eventual\" consistency offers no bound on how stale a read can be — under load or failure, lag can be seconds or minutes, long enough to break user expectations and cause bugs reported as data loss.","distractors":["Statement-based logs ship the SQL statements but break on nondeterministic functions and side effects, so they're mostly abandoned.","Monotonic reads guarantees a user never sees data move *backward* in time across successive reads — once they've seen a value, later reads won't show an older state.","The leader takes a consistent snapshot of its database at a particular point in the replication log (most databases can do this without locking, using MVCC)."]},{"id":"book/designing_data_intensive_applications/05_replication#Q17-5183","section":"book","module":"book/designing_data_intensive_applications/05_replication","moduleName":"05 replication","qIndex":17,"difficulty":"advanced","question":"What is split brain, and how should systems guard against it?","answerFull":"Split brain is when two nodes both believe they are the leader and both accept writes, leading to divergent, conflicting data that's hard to reconcile. Systems guard against it by ensuring only one leader can be active — using a consensus-backed source of truth for leadership, fencing tokens that let downstream resources reject writes from a deposed leader, and careful failover logic. Naive mitigations (like automatically shutting down any node that detects a conflict) can backfire by killing the whole cluster, so the mechanism must be principled.","correct":"Split brain is when two nodes both believe they are the leader and both accept writes, leading to divergent, conflicting data that's hard to reconcile.","distractors":["Consistent-prefix reads guarantees that if a sequence of writes happens in a causal order, any reader sees them in that same order — never an answer before its question.","They are the two mechanisms that make replicas converge after a node was unavailable for some writes.","Synchronous replication makes the leader wait for a follower to confirm a write before acknowledging success, guaranteeing the follower is up to date but blocking all writes if that follower is slow or down."]},{"id":"book/designing_data_intensive_applications/11_stream_processing#Q1-5184","section":"book","module":"book/designing_data_intensive_applications/11_stream_processing","moduleName":"11 stream processing","qIndex":1,"difficulty":"core","question":"How does a log-based message broker like Kafka differ from a traditional broker like RabbitMQ?","answerFull":"A traditional broker (JMS/AMQP) holds messages in a queue and *deletes* each one once a consumer acknowledges it, using per-message acknowledgment and redelivery, so messages are transient and can't be replayed. A log-based broker keeps an append-only, partitioned, durable log where messages are *retained* (until a retention limit) and each consumer simply tracks its own offset (position). The crucial difference is replayability: with Kafka a new consumer can read all history and you can reprocess from any past offset, which traditional brokers can't do because the message is gone after acknowledgment.","correct":"A traditional broker (JMS/AMQP) holds messages in a queue and *deletes* each one once a consumer acknowledges it, using per-message acknowledgment and redelivery, so messages are transient and can't be replayed.","distractors":["Because a write to a database *is* an event, and a database's replication log *is* a stream of those events — so the two are dual representations of the same information.","Log compaction is a Kafka retention policy that, instead of deleting old messages by age, keeps only the *latest* value for each key and discards superseded older values for that key.","It means that even though a processor may crash and reprocess some events, the *effect* on the output is as if each event were processed exactly once — no lost and no duplicated effects."]},{"id":"book/designing_data_intensive_applications/11_stream_processing#Q2-5185","section":"book","module":"book/designing_data_intensive_applications/11_stream_processing","moduleName":"11 stream processing","qIndex":2,"difficulty":"core","question":"What is an offset, and why is it a better acknowledgment mechanism for high-throughput streams?","answerFull":"An offset is a monotonically increasing position number identifying a consumer's place within a partition's log. It's a better acknowledgment than per-message acks because a consumer only needs to periodically record a single number (\"I've processed up to offset N\") rather than track and acknowledge each message individually, which is far cheaper and enables sequential, high-throughput reads. The offset also doubles as a replay pointer: to reprocess, you simply reset it backward, something per-message acknowledgment can't support.","correct":"An offset is a monotonically increasing position number identifying a consumer's place within a partition's log.","distractors":["Log compaction is a Kafka retention policy that, instead of deleting old messages by age, keeps only the *latest* value for each key and discards superseded older values for that key.","A traditional broker (JMS/AMQP) holds messages in a queue and *deletes* each one once a consumer acknowledges it, using per-message acknowledgment and redelivery, so messages are transient and can't be replayed.","Because a write to a database *is* an event, and a database's replication log *is* a stream of those events — so the two are dual representations of the same information."]},{"id":"book/designing_data_intensive_applications/11_stream_processing#Q6-5186","section":"book","module":"book/designing_data_intensive_applications/11_stream_processing","moduleName":"11 stream processing","qIndex":6,"difficulty":"intermediate","question":"What is the difference between event time and processing time, and why does it matter?","answerFull":"Event time is when an event actually occurred (stamped by the producer); processing time is when the stream processor handles it. They diverge because of network delays, broker backlogs, and especially consumer restarts that suddenly process a burst of buffered events. It matters because windowed aggregations (counts, rates over the last 5 minutes) computed by *processing* time are simply wrong whenever there's lag — a backlog drains thousands of old events into the \"current\" window. Correct results require bucketing by event time, which then forces you to handle late-arriving events.","correct":"Event time is when an event actually occurred (stamped by the producer); processing time is when the stream processor handles it.","distractors":["Microbatching (Spark Streaming) splits the stream into small batches and applies batch-style retry to each, trading some latency for simplicity.","Because a write to a database *is* an event, and a database's replication log *is* a stream of those events — so the two are dual representations of the same information.","Tumbling windows are fixed-length and non-overlapping, so each event falls into exactly one (e.g."]},{"id":"book/designing_data_intensive_applications/11_stream_processing#Q10-5187","section":"book","module":"book/designing_data_intensive_applications/11_stream_processing","moduleName":"11 stream processing","qIndex":10,"difficulty":"intermediate","question":"What does exactly-once (effectively-once) semantics mean, and why is it hard in streaming?","answerFull":"It means that even though a processor may crash and reprocess some events, the *effect* on the output is as if each event were processed exactly once — no lost and no duplicated effects. It's hard because streams are unbounded, so unlike batch you can't just \"wait for the job to finish and retry\"; a crash mid-stream forces you to resume and reprocess events that may have already had side effects (a written row, a sent email). Achieving it requires the input to be replayable and the output to be idempotent or committed transactionally, so reprocessing doesn't double-apply.","correct":"It means that even though a processor may crash and reprocess some events, the *effect* on the output is as if each event were processed exactly once — no lost and no duplicated effects.","distractors":["Event time is when an event actually occurred (stamped by the producer); processing time is when the stream processor handles it.","Because a write to a database *is* an event, and a database's replication log *is* a stream of those events — so the two are dual representations of the same information.","A traditional broker (JMS/AMQP) holds messages in a queue and *deletes* each one once a consumer acknowledges it, using per-message acknowledgment and redelivery, so messages are transient and can't be replayed."]},{"id":"book/designing_data_intensive_applications/11_stream_processing#Q11-5188","section":"book","module":"book/designing_data_intensive_applications/11_stream_processing","moduleName":"11 stream processing","qIndex":11,"difficulty":"advanced","question":"What techniques achieve exactly-once semantics in stream processors?","answerFull":"Microbatching (Spark Streaming) splits the stream into small batches and applies batch-style retry to each, trading some latency for simplicity. Checkpointing (Flink) periodically snapshots operator state and input offsets durably, so after a crash it restarts from the last checkpoint and replays the log from the saved offset. Idempotence makes reprocessing harmless by keying writes so duplicates overwrite rather than accumulate (or by deduplicating on event ID). Atomic commit (Kafka transactions, Flink) commits the output and the consumed offset together, so a failure can't leave output emitted without the offset advanced or vice versa.","correct":"Microbatching (Spark Streaming) splits the stream into small batches and applies batch-style retry to each, trading some latency for simplicity.","distractors":["It means that even though a processor may crash and reprocess some events, the *effect* on the output is as if each event were processed exactly once — no lost and no duplicated effects.","Because a write to a database *is* an event, and a database's replication log *is* a stream of those events — so the two are dual representations of the same information.","A traditional broker (JMS/AMQP) holds messages in a queue and *deletes* each one once a consumer acknowledges it, using per-message acknowledgment and redelivery, so messages are transient and can't be replayed."]},{"id":"book/designing_data_intensive_applications/11_stream_processing#Q14-5189","section":"book","module":"book/designing_data_intensive_applications/11_stream_processing","moduleName":"11 stream processing","qIndex":14,"difficulty":"advanced","question":"What are the main window types in stream processing?","answerFull":"Tumbling windows are fixed-length and non-overlapping, so each event falls into exactly one (e.g. one-minute buckets). Hopping windows are fixed-length but overlap by advancing in steps smaller than their length, so an event can belong to several. Sliding windows group events that fall within a moving interval of each other rather than fixed boundaries. Session windows group a user's events together until a gap of inactivity longer than a timeout, then start a new session — useful for analyzing bursts of related activity like a browsing session.","correct":"Tumbling windows are fixed-length and non-overlapping, so each event falls into exactly one (e.g.","distractors":["Event time is when an event actually occurred (stamped by the producer); processing time is when the stream processor handles it.","Microbatching (Spark Streaming) splits the stream into small batches and applies batch-style retry to each, trading some latency for simplicity.","It means that even though a processor may crash and reprocess some events, the *effect* on the output is as if each event were processed exactly once — no lost and no duplicated effects."]},{"id":"book/designing_data_intensive_applications/11_stream_processing#Q15-5190","section":"book","module":"book/designing_data_intensive_applications/11_stream_processing","moduleName":"11 stream processing","qIndex":15,"difficulty":"advanced","question":"What does log compaction do, and why is it useful for CDC?","answerFull":"Log compaction is a Kafka retention policy that, instead of deleting old messages by age, keeps only the *latest* value for each key and discards superseded older values for that key. It's useful for CDC because it lets a change-log topic double as a complete, current snapshot of the source table: replaying the compacted topic from the beginning reconstructs the latest state of every key, so a new or rebuilt derived store (a fresh search index or cache) can be fully populated by reading the topic, without needing a separate full database dump.","correct":"Log compaction is a Kafka retention policy that, instead of deleting old messages by age, keeps only the *latest* value for each key and discards superseded older values for that key.","distractors":["An offset is a monotonically increasing position number identifying a consumer's place within a partition's log.","Because a write to a database *is* an event, and a database's replication log *is* a stream of those events — so the two are dual representations of the same information.","A traditional broker (JMS/AMQP) holds messages in a queue and *deletes* each one once a consumer acknowledges it, using per-message acknowledgment and redelivery, so messages are transient and can't be replayed."]},{"id":"book/designing_data_intensive_applications/11_stream_processing#Q16-5191","section":"book","module":"book/designing_data_intensive_applications/11_stream_processing","moduleName":"11 stream processing","qIndex":16,"difficulty":"advanced","question":"How do databases and streams turn out to be \"two sides of the same coin\"?","answerFull":"Because a write to a database *is* an event, and a database's replication log *is* a stream of those events — so the two are dual representations of the same information. You can go from database to stream via change data capture (emit each write as an event) and from stream to database by materializing the stream into a table (apply each event to build current state). Recognizing this unifies the worlds: the immutable event log is the primary truth, and tables, indexes, and caches are all derived, rebuildable materialized views of it — the conceptual bridge to unbundling the database in Chapter 12.","correct":"Because a write to a database *is* an event, and a database's replication log *is* a stream of those events — so the two are dual representations of the same information.","distractors":["It means that even though a processor may crash and reprocess some events, the *effect* on the output is as if each event were processed exactly once — no lost and no duplicated effects.","Microbatching (Spark Streaming) splits the stream into small batches and applies batch-style retry to each, trading some latency for simplicity.","Log compaction is a Kafka retention policy that, instead of deleting old messages by age, keeps only the *latest* value for each key and discards superseded older values for that key."]},{"id":"book/designing_data_intensive_applications/04_encoding_and_evolution#Q1-5192","section":"book","module":"book/designing_data_intensive_applications/04_encoding_and_evolution","moduleName":"04 encoding and evolution","qIndex":1,"difficulty":"core","question":"What are forward and backward compatibility, and why do you need both at once?","answerFull":"Backward compatibility means new code can read data written by old code; forward compatibility means old code can read data written by new code. You need both simultaneously because deployments are rolling — for a window of time, old and new versions of your application run side by side, reading and writing the same database and queues. New code must understand the historical data, and old code must tolerate (and not corrupt) data written by the newer code it hasn't been updated to fully understand.","correct":"Backward compatibility means new code can read data written by old code; forward compatibility means old code can read data written by new code.","distractors":["Reusing a retired tag number means old data encoded with that tag for the *old* field will be misinterpreted by new code as the *new* field of that tag, silently corrupting reads.","Because the data may have been written by a different (often older or newer) version of the code than the one reading it, and Avro's tagless encoding gives no way to interpret the bytes without the exact writing schema.","They encode fields by numeric tag rather than by name, so the schema maps tags to field names and types out of band."]},{"id":"book/designing_data_intensive_applications/04_encoding_and_evolution#Q2-5193","section":"book","module":"book/designing_data_intensive_applications/04_encoding_and_evolution","moduleName":"04 encoding and evolution","qIndex":2,"difficulty":"core","question":"How do Protocol Buffers and Thrift achieve schema evolution, and what are the rules?","answerFull":"They encode fields by numeric tag rather than by name, so the schema maps tags to field names and types out of band. To add a field you assign it a brand-new tag; old readers ignore unknown tags (forward compatible), and the field must be optional or have a default so new readers can handle old data lacking it (backward compatible). The rules: never reuse a tag number, never make a new field required, and only remove optional fields — never reusing their tags.","correct":"They encode fields by numeric tag rather than by name, so the schema maps tags to field names and types out of band.","distractors":["Because the data may have been written by a different (often older or newer) version of the code than the one reading it, and Avro's tagless encoding gives no way to interpret the bytes without the exact writing schema.","Reusing a retired tag number means old data encoded with that tag for the *old* field will be misinterpreted by new code as the *new* field of that tag, silently corrupting reads.","Avro's encoded bytes contain no field tags or type annotations at all — just concatenated values — so the reader must know the schema used to write the data."]},{"id":"book/designing_data_intensive_applications/04_encoding_and_evolution#Q3-5194","section":"book","module":"book/designing_data_intensive_applications/04_encoding_and_evolution","moduleName":"04 encoding and evolution","qIndex":3,"difficulty":"core","question":"How does Avro differ from Protobuf/Thrift, and what is its key advantage?","answerFull":"Avro's encoded bytes contain no field tags or type annotations at all — just concatenated values — so the reader must know the schema used to write the data. It separates the writer's schema (used to encode) from the reader's schema (what the consumer expects) and reconciles them by matching fields by *name*, filling defaults for missing fields and ignoring extra ones. Its key advantage is friendliness to dynamically generated schemas, like auto-deriving a schema from a database table, because there are no manually assigned tag numbers to coordinate.","correct":"Avro's encoded bytes contain no field tags or type annotations at all — just concatenated values — so the reader must know the schema used to write the data.","distractors":["They encode fields by numeric tag rather than by name, so the schema maps tags to field names and types out of band.","Because the data may have been written by a different (often older or newer) version of the code than the one reading it, and Avro's tagless encoding gives no way to interpret the bytes without the exact writing schema.","Reusing a retired tag number means old data encoded with that tag for the *old* field will be misinterpreted by new code as the *new* field of that tag, silently corrupting reads."]},{"id":"book/designing_data_intensive_applications/04_encoding_and_evolution#Q4-5195","section":"book","module":"book/designing_data_intensive_applications/04_encoding_and_evolution","moduleName":"04 encoding and evolution","qIndex":4,"difficulty":"core","question":"Why does Avro distinguish the writer's schema from the reader's schema?","answerFull":"Because the data may have been written by a different (often older or newer) version of the code than the one reading it, and Avro's tagless encoding gives no way to interpret the bytes without the exact writing schema. By carrying or referencing the writer's schema and resolving it against the reader's expected schema by field name, Avro lets the two versions differ yet still communicate — defaults cover fields the reader added, and fields the writer included but the reader dropped are skipped.","correct":"Because the data may have been written by a different (often older or newer) version of the code than the one reading it, and Avro's tagless encoding gives no way to interpret the bytes without the exact writing schema.","distractors":["Avro's encoded bytes contain no field tags or type annotations at all — just concatenated values — so the reader must know the schema used to write the data.","Reusing a retired tag number means old data encoded with that tag for the *old* field will be misinterpreted by new code as the *new* field of that tag, silently corrupting reads.","They encode fields by numeric tag rather than by name, so the schema maps tags to field names and types out of band."]},{"id":"book/designing_data_intensive_applications/04_encoding_and_evolution#Q8-5196","section":"book","module":"book/designing_data_intensive_applications/04_encoding_and_evolution","moduleName":"04 encoding and evolution","qIndex":8,"difficulty":"intermediate","question":"What problems do JSON, XML, and CSV have despite their popularity?","answerFull":"They're verbose and have ambiguous data types: XML and CSV can't reliably distinguish a number from a string, and JSON can't distinguish integers from floats and loses precision on integers above 2^53. They have no native binary support, forcing Base64 (a ~33% size increase), and their schema support is optional or weak (CSV has none). They persist because they're human-readable and universally supported, making them a solid lingua franca for interfaces between organizations.","correct":"They're verbose and have ambiguous data types: XML and CSV can't reliably distinguish a number from a string, and JSON can't distinguish integers from floats and loses precision on integers above 2^53.","distractors":["Reusing a retired tag number means old data encoded with that tag for the *old* field will be misinterpreted by new code as the *new* field of that tag, silently corrupting reads.","Because the data may have been written by a different (often older or newer) version of the code than the one reading it, and Avro's tagless encoding gives no way to interpret the bytes without the exact writing schema.","Backward compatibility means new code can read data written by old code; forward compatibility means old code can read data written by new code."]},{"id":"book/designing_data_intensive_applications/04_encoding_and_evolution#Q9-5197","section":"book","module":"book/designing_data_intensive_applications/04_encoding_and_evolution","moduleName":"04 encoding and evolution","qIndex":9,"difficulty":"intermediate","question":"Why is RPC's \"make a remote call look like a local call\" considered a leaky abstraction?","answerFull":"Because a network call has failure modes a local call never does: it can be lost or delayed unpredictably, and it can time out *after* the server already executed it, leaving the caller unsure whether it happened. Latency varies enormously, all arguments must be serialized, and large objects become problematic across languages. A local function call either returns a value or throws; pretending the network behaves the same way hides exactly the cases that cause production bugs.","correct":"Because a network call has failure modes a local call never does: it can be lost or delayed unpredictably, and it can time out *after* the server already executed it, leaving the caller unsure whether it happened.","distractors":["They encode fields by numeric tag rather than by name, so the schema maps tags to field names and types out of band.","The actor model structures concurrency as independent actors that hold local state and communicate only by sending each other asynchronous messages, avoiding shared-memory locking.","Backward compatibility means new code can read data written by old code; forward compatibility means old code can read data written by new code."]},{"id":"book/designing_data_intensive_applications/04_encoding_and_evolution#Q14-5198","section":"book","module":"book/designing_data_intensive_applications/04_encoding_and_evolution","moduleName":"04 encoding and evolution","qIndex":14,"difficulty":"advanced","question":"What is the actor model, and how does it relate to message compatibility?","answerFull":"The actor model structures concurrency as independent actors that hold local state and communicate only by sending each other asynchronous messages, avoiding shared-memory locking. A distributed actor framework (Akka, Erlang/OTP, Orleans) transparently routes those messages across nodes, so it doubles as a programming and a distribution model. Because actors exchange encoded messages and the system undergoes rolling upgrades, those messages need the same forward/backward compatibility as any other dataflow.","correct":"The actor model structures concurrency as independent actors that hold local state and communicate only by sending each other asynchronous messages, avoiding shared-memory locking.","distractors":["Backward compatibility means new code can read data written by old code; forward compatibility means old code can read data written by new code.","Because a network call has failure modes a local call never does: it can be lost or delayed unpredictably, and it can time out *after* the server already executed it, leaving the caller unsure whether it happened.","They encode fields by numeric tag rather than by name, so the schema maps tags to field names and types out of band."]},{"id":"book/designing_data_intensive_applications/04_encoding_and_evolution#Q16-5199","section":"book","module":"book/designing_data_intensive_applications/04_encoding_and_evolution","moduleName":"04 encoding and evolution","qIndex":16,"difficulty":"advanced","question":"Why must you never reuse a field tag number in Protobuf/Thrift, and what's the analogous Avro concern?","answerFull":"Reusing a retired tag number means old data encoded with that tag for the *old* field will be misinterpreted by new code as the *new* field of that tag, silently corrupting reads. The analogous Avro concern is that since resolution is by field *name*, renaming a field breaks matching unless you use aliases — so you add an alias mapping the new name to the old so the reader can still reconcile data written under the previous name. Both are about preserving the identity link between historical bytes and current code.","correct":"Reusing a retired tag number means old data encoded with that tag for the *old* field will be misinterpreted by new code as the *new* field of that tag, silently corrupting reads.","distractors":["Backward compatibility means new code can read data written by old code; forward compatibility means old code can read data written by new code.","They encode fields by numeric tag rather than by name, so the schema maps tags to field names and types out of band.","Because a network call has failure modes a local call never does: it can be lost or delayed unpredictably, and it can time out *after* the server already executed it, leaving the caller unsure whether it happened."]},{"id":"book/designing_data_intensive_applications/07_transactions#Q1-5200","section":"book","module":"book/designing_data_intensive_applications/07_transactions","moduleName":"07 transactions","qIndex":1,"difficulty":"core","question":"What does each letter of ACID actually guarantee, and which one is the odd one out?","answerFull":"Atomicity means all of a transaction's writes apply or none do, so a failed transaction aborts and is safely retryable (it's about abortability, not concurrency). Isolation means concurrent transactions don't see each other's intermediate state, with serializability being the strongest form. Durability means committed data survives crashes. Consistency is the odd one out: it means preserving application invariants, but those are defined and largely enforced by the *application*, not the database, and Kleppmann notes the \"C\" was somewhat shoehorned in to complete the acronym.","correct":"Atomicity means all of a transaction's writes apply or none do, so a failed transaction aborts and is safely retryable (it's about abortability, not concurrency).","distractors":["SSI is better when contention is low to moderate: reads never block and writes never block reads, so it scales read-heavy workloads far better than 2PL, and it avoids 2PL's deadlock storms.","A phantom is when a write in one transaction changes the result set of a search query in another transaction — a row that matches a condition appears or disappears.","MVCC (multi-version concurrency control) keeps several committed versions of each object, each tagged with the transaction ID that created it."]},{"id":"book/designing_data_intensive_applications/07_transactions#Q2-5201","section":"book","module":"book/designing_data_intensive_applications/07_transactions","moduleName":"07 transactions","qIndex":2,"difficulty":"core","question":"What is the difference between a dirty read and a dirty write, and which level prevents them?","answerFull":"A dirty read is reading data another transaction has written but not yet committed — you might see a value that gets rolled back, or a partial multi-object update. A dirty write is overwriting data another transaction has written but not yet committed, letting concurrent writes interleave and corrupt each other. Both are prevented by the read committed isolation level, which guarantees you only read committed data and only overwrite committed data (typically via row locks for writes plus serving the old committed value to readers).","correct":"A dirty read is reading data another transaction has written but not yet committed — you might see a value that gets rolled back, or a partial multi-object update.","distractors":["Abort-and-retry is the clean recovery model for transient problems (deadlocks, serialization conflicts, brief network issues) because atomicity guarantees a failed transaction left no partial state.","Read committed uses short-lived write locks and lets reads proceed against the last committed value without locking, so readers don't block writers.","A phantom is when a write in one transaction changes the result set of a search query in another transaction — a row that matches a condition appears or disappears."]},{"id":"book/designing_data_intensive_applications/07_transactions#Q4-5202","section":"book","module":"book/designing_data_intensive_applications/07_transactions","moduleName":"07 transactions","qIndex":4,"difficulty":"core","question":"How does MVCC let readers and writers avoid blocking each other?","answerFull":"MVCC (multi-version concurrency control) keeps several committed versions of each object, each tagged with the transaction ID that created it. A reading transaction sees the version that was committed before it started and ignores any created later or still in progress, so it never has to wait for a writer — and a writer creates a new version without overwriting the old one a reader might be using, so it never has to wait for a reader. This \"readers never block writers, writers never block readers\" property is what makes snapshot isolation efficient.","correct":"MVCC (multi-version concurrency control) keeps several committed versions of each object, each tagged with the transaction ID that created it.","distractors":["A dirty read is reading data another transaction has written but not yet committed — you might see a value that gets rolled back, or a partial multi-object update.","SSI is optimistic: transactions read from a consistent MVCC snapshot with no read locks, so nothing blocks during execution.","Abort-and-retry is the clean recovery model for transient problems (deadlocks, serialization conflicts, brief network issues) because atomicity guarantees a failed transaction left no partial state."]},{"id":"book/designing_data_intensive_applications/07_transactions#Q7-5203","section":"book","module":"book/designing_data_intensive_applications/07_transactions","moduleName":"07 transactions","qIndex":7,"difficulty":"intermediate","question":"Walk through the on-call-doctor example of write skew.","answerFull":"The invariant is that at least one doctor must remain on call. Two doctors are on call and both want to take sick leave. Under snapshot isolation, both transactions read \"2 doctors on call, so it's safe for me to leave,\" then each updates its *own* row to off-call, and both commit — leaving zero doctors on call. Neither transaction did a lost update (they wrote different rows), and each saw a consistent snapshot showing two doctors, so snapshot isolation permits it. Only serializable isolation, which considers that both relied on a now-falsified premise, prevents it.","correct":"The invariant is that at least one doctor must remain on call.","distractors":["A phantom is when a write in one transaction changes the result set of a search query in another transaction — a row that matches a condition appears or disappears.","Read committed uses short-lived write locks and lets reads proceed against the last committed value without locking, so readers don't block writers.","Atomicity means all of a transaction's writes apply or none do, so a failed transaction aborts and is safely retryable (it's about abortability, not concurrency)."]},{"id":"book/designing_data_intensive_applications/07_transactions#Q8-5204","section":"book","module":"book/designing_data_intensive_applications/07_transactions","moduleName":"07 transactions","qIndex":8,"difficulty":"intermediate","question":"What is a phantom, and why doesn't SELECT ... FOR UPDATE prevent it?","answerFull":"A phantom is when a write in one transaction changes the result set of a search query in another transaction — a row that matches a condition appears or disappears. SELECT ... FOR UPDATE locks the rows that *currently* match, but it can't lock rows that don't exist yet, so another transaction can still INSERT a new row that would have matched the condition (e.g. booking a meeting room for a time slot that \"looked free\"). Preventing phantoms needs predicate or index-range (next-key) locks, or serializable isolation, or the materialize-conflicts workaround.","correct":"A phantom is when a write in one transaction changes the result set of a search query in another transaction — a row that matches a condition appears or disappears.","distractors":["Atomicity means all of a transaction's writes apply or none do, so a failed transaction aborts and is safely retryable (it's about abortability, not concurrency).","A dirty read is reading data another transaction has written but not yet committed — you might see a value that gets rolled back, or a partial multi-object update.","Abort-and-retry is the clean recovery model for transient problems (deadlocks, serialization conflicts, brief network issues) because atomicity guarantees a failed transaction left no partial state."]},{"id":"book/designing_data_intensive_applications/07_transactions#Q9-5205","section":"book","module":"book/designing_data_intensive_applications/07_transactions","moduleName":"07 transactions","qIndex":9,"difficulty":"intermediate","question":"What are the three ways to implement serializable isolation?","answerFull":"Actual serial execution: run transactions literally one at a time on a single thread, viable now that datasets fit in RAM and OLTP transactions are short (VoltDB, Redis). Two-phase locking (2PL): pessimistic shared/exclusive locks held until commit, where readers block writers and writers block readers, with predicate/index-range locks for phantoms. Serializable snapshot isolation (SSI): optimistic — run on a snapshot with no read locks and, at commit, abort any transaction whose reads were invalidated by another committed write (PostgreSQL serializable, FoundationDB).","correct":"Actual serial execution: run transactions literally one at a time on a single thread, viable now that datasets fit in RAM and OLTP transactions are short (VoltDB, Redis).","distractors":["SSI is optimistic: transactions read from a consistent MVCC snapshot with no read locks, so nothing blocks during execution.","Read committed uses short-lived write locks and lets reads proceed against the last committed value without locking, so readers don't block writers.","Atomicity means all of a transaction's writes apply or none do, so a failed transaction aborts and is safely retryable (it's about abortability, not concurrency)."]},{"id":"book/designing_data_intensive_applications/07_transactions#Q11-5206","section":"book","module":"book/designing_data_intensive_applications/07_transactions","moduleName":"07 transactions","qIndex":11,"difficulty":"advanced","question":"How does two-phase locking differ from the locking in read committed?","answerFull":"Read committed uses short-lived write locks and lets reads proceed against the last committed value without locking, so readers don't block writers. Two-phase locking is far stricter: a transaction acquires a shared lock to read and an exclusive lock to write, holds *all* locks until it ends, and consequently readers block writers and writers block readers. This much stronger locking is what gives 2PL full serializability, but it sharply reduces concurrency and makes deadlocks common.","correct":"Read committed uses short-lived write locks and lets reads proceed against the last committed value without locking, so readers don't block writers.","distractors":["SSI is better when contention is low to moderate: reads never block and writes never block reads, so it scales read-heavy workloads far better than 2PL, and it avoids 2PL's deadlock storms.","SSI is optimistic: transactions read from a consistent MVCC snapshot with no read locks, so nothing blocks during execution.","A phantom is when a write in one transaction changes the result set of a search query in another transaction — a row that matches a condition appears or disappears."]},{"id":"book/designing_data_intensive_applications/07_transactions#Q12-5207","section":"book","module":"book/designing_data_intensive_applications/07_transactions","moduleName":"07 transactions","qIndex":12,"difficulty":"advanced","question":"How does serializable snapshot isolation (SSI) achieve serializability without read locks?","answerFull":"SSI is optimistic: transactions read from a consistent MVCC snapshot with no read locks, so nothing blocks during execution. The database tracks what each transaction read, and at commit time it checks whether another transaction has committed a write to data this transaction read — meaning the premise behind its decisions is now outdated. If so, it aborts this transaction, which then retries. Because it only aborts on a genuine serialization conflict and never takes read locks, it offers serializability with much better concurrency than 2PL when contention is moderate.","correct":"SSI is optimistic: transactions read from a consistent MVCC snapshot with no read locks, so nothing blocks during execution.","distractors":["SSI is better when contention is low to moderate: reads never block and writes never block reads, so it scales read-heavy workloads far better than 2PL, and it avoids 2PL's deadlock storms.","Actual serial execution: run transactions literally one at a time on a single thread, viable now that datasets fit in RAM and OLTP transactions are short (VoltDB, Redis).","MVCC (multi-version concurrency control) keeps several committed versions of each object, each tagged with the transaction ID that created it."]},{"id":"book/designing_data_intensive_applications/07_transactions#Q13-5208","section":"book","module":"book/designing_data_intensive_applications/07_transactions","moduleName":"07 transactions","qIndex":13,"difficulty":"advanced","question":"What are the trade-offs that make SSI better or worse than 2PL?","answerFull":"SSI is better when contention is low to moderate: reads never block and writes never block reads, so it scales read-heavy workloads far better than 2PL, and it avoids 2PL's deadlock storms. SSI is worse when contention is high: many transactions read data that others then modify, so the commit-time conflict checks abort a large fraction of transactions, causing repeated retries and wasted work. SSI also depends on transactions being short, since long ones accumulate more reads and are more likely to be invalidated before they commit.","correct":"SSI is better when contention is low to moderate: reads never block and writes never block reads, so it scales read-heavy workloads far better than 2PL, and it avoids 2PL's deadlock storms.","distractors":["Read committed uses short-lived write locks and lets reads proceed against the last committed value without locking, so readers don't block writers.","A dirty read is reading data another transaction has written but not yet committed — you might see a value that gets rolled back, or a partial multi-object update.","SSI is optimistic: transactions read from a consistent MVCC snapshot with no read locks, so nothing blocks during execution."]},{"id":"book/designing_data_intensive_applications/07_transactions#Q14-5209","section":"book","module":"book/designing_data_intensive_applications/07_transactions","moduleName":"07 transactions","qIndex":14,"difficulty":"advanced","question":"Why is retrying an aborted transaction the right default, but with caveats?","answerFull":"Abort-and-retry is the clean recovery model for transient problems (deadlocks, serialization conflicts, brief network issues) because atomicity guarantees a failed transaction left no partial state. The caveats: the transaction may have actually committed before its acknowledgment was lost, so a blind retry double-applies it unless it's idempotent; retrying during overload makes the overload worse, so you need exponential backoff; retrying a permanent error like a constraint violation is pointless; and any side effects outside the database, such as a sent email, can't be undone by the retry.","correct":"Abort-and-retry is the clean recovery model for transient problems (deadlocks, serialization conflicts, brief network issues) because atomicity guarantees a failed transaction left no partial state.","distractors":["Atomicity means all of a transaction's writes apply or none do, so a failed transaction aborts and is safely retryable (it's about abortability, not concurrency).","MVCC (multi-version concurrency control) keeps several committed versions of each object, each tagged with the transaction ID that created it.","A dirty read is reading data another transaction has written but not yet committed — you might see a value that gets rolled back, or a partial multi-object update."]},{"id":"book/designing_data_intensive_applications/10_batch_processing#Q1-5210","section":"book","module":"book/designing_data_intensive_applications/10_batch_processing","moduleName":"10 batch processing","qIndex":1,"difficulty":"core","question":"What distinguishes batch processing from online services and stream processing?","answerFull":"Batch processing reads a large, *bounded* input of known size and produces derived output, with no user waiting, so success is measured by throughput (how much data per unit time) rather than response time. Online services handle one request at a time and are judged by latency. Stream processing (Chapter 11) is the middle ground: it consumes *unbounded*, never-ending input and produces continuously updated output with low latency. Batch's bounded, no-one-waiting nature is what lets it prioritize throughput and rely on re-running jobs to handle failures.","correct":"Batch processing reads a large, *bounded* input of known size and produces derived output, with no user waiting, so success is measured by throughput (how much data per unit time) rather than response time.","distractors":["Complex computations rarely fit in a single map-and-reduce pass, so they're built as a *workflow*: the output directory of one job on HDFS becomes the input of the next, chaining jobs together.","The shuffle is the framework-managed step between map and reduce that partitions every mapper's output by key, sorts each partition, and transfers all values for a given key across the network to a single reducer.","They let engineers express batch computations — joins, filters, aggregations — declaratively instead of hand-writing low-level map and reduce functions, then compile that down to the underlying execution engine."]},{"id":"book/designing_data_intensive_applications/10_batch_processing#Q3-5211","section":"book","module":"book/designing_data_intensive_applications/10_batch_processing","moduleName":"10 batch processing","qIndex":3,"difficulty":"core","question":"What is the shuffle in MapReduce, and why is it the heart of the model?","answerFull":"The shuffle is the framework-managed step between map and reduce that partitions every mapper's output by key, sorts each partition, and transfers all values for a given key across the network to a single reducer. It's the heart of the model because it's what guarantees a reducer sees *all* values for its keys, grouped and sorted — which is what makes grouping, aggregation, and joins possible. The programmer writes only map and reduce; the shuffle (and its sort) is the expensive, defining machinery MapReduce provides.","correct":"The shuffle is the framework-managed step between map and reduce that partitions every mapper's output by key, sorts each partition, and transfers all values for a given key across the network to a single reducer.","distractors":["Batch processing reads a large, *bounded* input of known size and produces derived output, with no user waiting, so success is measured by throughput (how much data per unit time) rather than response time.","Complex computations rarely fit in a single map-and-reduce pass, so they're built as a *workflow*: the output directory of one job on HDFS becomes the input of the next, chaining jobs together.","They let engineers express batch computations — joins, filters, aggregations — declaratively instead of hand-writing low-level map and reduce functions, then compile that down to the underlying execution engine."]},{"id":"book/designing_data_intensive_applications/10_batch_processing#Q4-5212","section":"book","module":"book/designing_data_intensive_applications/10_batch_processing","moduleName":"10 batch processing","qIndex":4,"difficulty":"core","question":"What does \"bringing computation to the data\" mean and why does it matter?","answerFull":"It means the scheduler tries to run each mapper task on a machine that already stores a replica of the input block that task will read, rather than shipping the input across the network to wherever there's a free CPU. It matters because the input data is large while the program is small, so moving the computation to the data avoids saturating the network with bulk data transfer — only the smaller mapper *output* needs to be shuffled. This locality optimization is central to MapReduce's scalability on commodity clusters.","correct":"It means the scheduler tries to run each mapper task on a machine that already stores a replica of the input block that task will read, rather than shipping the input across the network to wherever there's a free CPU.","distractors":["Batch processing reads a large, *bounded* input of known size and produces derived output, with no user waiting, so success is measured by throughput (how much data per unit time) rather than response time.","The shuffle is the framework-managed step between map and reduce that partitions every mapper's output by key, sorts each partition, and transfers all values for a given key across the network to a single reducer.","Complex computations rarely fit in a single map-and-reduce pass, so they're built as a *workflow*: the output directory of one job on HDFS becomes the input of the next, chaining jobs together."]},{"id":"book/designing_data_intensive_applications/10_batch_processing#Q8-5213","section":"book","module":"book/designing_data_intensive_applications/10_batch_processing","moduleName":"10 batch processing","qIndex":8,"difficulty":"intermediate","question":"How does Spark achieve fault tolerance without materializing every intermediate result?","answerFull":"Spark represents data as RDDs (Resilient Distributed Datasets) that record their *lineage* — the exact, deterministic sequence of operations that produced them from their inputs. When a partition is lost to a machine failure, Spark doesn't need a saved copy; it recomputes that partition by replaying the recorded operations on the (still available or itself recomputable) input data. This trades recomputation cost for avoiding the heavy materialization MapReduce does, but it depends critically on the operators being deterministic.","correct":"Spark represents data as RDDs (Resilient Distributed Datasets) that record their *lineage* — the exact, deterministic sequence of operations that produced them from their inputs.","distractors":["Immutable inputs are what make batch jobs safely re-runnable, which is the foundation of batch fault tolerance and experimentation.","The shuffle is the framework-managed step between map and reduce that partitions every mapper's output by key, sorts each partition, and transfers all values for a given key across the network to a single reducer.","Batch processing reads a large, *bounded* input of known size and produces derived output, with no user waiting, so success is measured by throughput (how much data per unit time) rather than response time."]},{"id":"book/designing_data_intensive_applications/10_batch_processing#Q12-5214","section":"book","module":"book/designing_data_intensive_applications/10_batch_processing","moduleName":"10 batch processing","qIndex":12,"difficulty":"advanced","question":"What kinds of output do batch workflows typically produce, and why build them offline?","answerFull":"Batch jobs produce derived data: search indexes (e.g. Lucene segments), read-only key-value snapshots to bulk-load into serving databases, recommendation results, and trained machine-learning models. They're built offline because generating this derived data involves huge numbers of writes that would overload a live serving database and risk inconsistency on partial failure; instead the job writes immutable output files in one place and the serving layer atomically loads or swaps them in, keeping the production system stable and the operation re-runnable.","correct":"Batch jobs produce derived data: search indexes (e.g.","distractors":["Immutable inputs are what make batch jobs safely re-runnable, which is the foundation of batch fault tolerance and experimentation.","They let engineers express batch computations — joins, filters, aggregations — declaratively instead of hand-writing low-level map and reduce functions, then compile that down to the underlying execution engine.","Batch processing reads a large, *bounded* input of known size and produces derived output, with no user waiting, so success is measured by throughput (how much data per unit time) rather than response time."]},{"id":"book/designing_data_intensive_applications/10_batch_processing#Q13-5215","section":"book","module":"book/designing_data_intensive_applications/10_batch_processing","moduleName":"10 batch processing","qIndex":13,"difficulty":"advanced","question":"Why are immutable inputs so important to batch processing?","answerFull":"Immutable inputs are what make batch jobs safely re-runnable, which is the foundation of batch fault tolerance and experimentation. Because a job never modifies its input, you can retry a failed task, re-run the entire job after fixing a bug, or run it again with different parameters, always getting a well-defined result from the same starting data. If inputs could change underneath a running job, retries would be non-deterministic and a partial failure could leave the system in an unrecoverable, inconsistent state — so immutability underpins the whole model's reliability.","correct":"Immutable inputs are what make batch jobs safely re-runnable, which is the foundation of batch fault tolerance and experimentation.","distractors":["Spark represents data as RDDs (Resilient Distributed Datasets) that record their *lineage* — the exact, deterministic sequence of operations that produced them from their inputs.","Complex computations rarely fit in a single map-and-reduce pass, so they're built as a *workflow*: the output directory of one job on HDFS becomes the input of the next, chaining jobs together.","Batch processing reads a large, *bounded* input of known size and produces derived output, with no user waiting, so success is measured by throughput (how much data per unit time) rather than response time."]},{"id":"book/designing_data_intensive_applications/10_batch_processing#Q14-5216","section":"book","module":"book/designing_data_intensive_applications/10_batch_processing","moduleName":"10 batch processing","qIndex":14,"difficulty":"advanced","question":"What roles do high-level APIs like Hive, Pig, and Spark SQL play?","answerFull":"They let engineers express batch computations — joins, filters, aggregations — declaratively instead of hand-writing low-level map and reduce functions, then compile that down to the underlying execution engine. Crucially, they include query optimizers that automatically choose execution strategies, such as whether to use a broadcast hash join or a partitioned join based on data sizes, bringing the declarative-versus-imperative advantages from Chapter 2 to batch processing: more concise code, and the engine — not the programmer — picking the efficient physical plan.","correct":"They let engineers express batch computations — joins, filters, aggregations — declaratively instead of hand-writing low-level map and reduce functions, then compile that down to the underlying execution engine.","distractors":["Immutable inputs are what make batch jobs safely re-runnable, which is the foundation of batch fault tolerance and experimentation.","The shuffle is the framework-managed step between map and reduce that partitions every mapper's output by key, sorts each partition, and transfers all values for a given key across the network to a single reducer.","Complex computations rarely fit in a single map-and-reduce pass, so they're built as a *workflow*: the output directory of one job on HDFS becomes the input of the next, chaining jobs together."]},{"id":"book/designing_data_intensive_applications/10_batch_processing#Q16-5217","section":"book","module":"book/designing_data_intensive_applications/10_batch_processing","moduleName":"10 batch processing","qIndex":16,"difficulty":"advanced","question":"How is a workflow of multiple MapReduce jobs constructed and managed?","answerFull":"Complex computations rarely fit in a single map-and-reduce pass, so they're built as a *workflow*: the output directory of one job on HDFS becomes the input of the next, chaining jobs together. Because there's no built-in notion of a multi-job pipeline in basic MapReduce, scheduling tools like Oozie, Airflow, and Luigi orchestrate the dependencies — deciding when each job can start (after its inputs are fully written), handling retries, and managing the overall DAG of jobs — which is one of the awkwardnesses that dataflow engines later eliminated by representing the whole workflow as a single job.","correct":"Complex computations rarely fit in a single map-and-reduce pass, so they're built as a *workflow*: the output directory of one job on HDFS becomes the input of the next, chaining jobs together.","distractors":["Batch jobs produce derived data: search indexes (e.g.","They let engineers express batch computations — joins, filters, aggregations — declaratively instead of hand-writing low-level map and reduce functions, then compile that down to the underlying execution engine.","It means the scheduler tries to run each mapper task on a machine that already stores a replica of the input block that task will read, rather than shipping the input across the network to wherever there's a free CPU."]},{"id":"book/designing_data_intensive_applications/09_consistency_and_consensus#Q6-5218","section":"book","module":"book/designing_data_intensive_applications/09_consistency_and_consensus","moduleName":"09 consistency and consensus","qIndex":6,"difficulty":"intermediate","question":"What is causality, and why is causal consistency often preferable to linearizability?","answerFull":"Causality is a *partial* order: it requires that causally related events keep their order (the question before the answer, a row created before it's updated) while leaving truly concurrent, unrelated events unordered. Causal consistency is preferable when you don't need full recency because it's the strongest model achievable *without* the coordination cost of linearizability and *without* becoming unavailable during a network partition — you can preserve cause→effect using cheap logical clocks while still serving requests on both sides of a partition.","correct":"Causality is a *partial* order: it requires that causally related events keep their order (the question before the answer, a row created before it's updated) while leaving truly concurrent, unrelated events unordered.","distractors":["Total order broadcast (atomic broadcast) is a protocol that delivers messages to all nodes reliably (none lost) and in exactly the same order on every node.","Total order broadcast, a linearizable compare-and-set (or atomic increment) register, leader election, atomic commit, and uniqueness constraints all reduce to consensus — you can build each from the others.","Consensus is getting several nodes to agree on a single value."]},{"id":"book/designing_data_intensive_applications/09_consistency_and_consensus#Q8-5219","section":"book","module":"book/designing_data_intensive_applications/09_consistency_and_consensus","moduleName":"09 consistency and consensus","qIndex":8,"difficulty":"intermediate","question":"What is total order broadcast, and why is it so important?","answerFull":"Total order broadcast (atomic broadcast) is a protocol that delivers messages to all nodes reliably (none lost) and in exactly the same order on every node. It's important because it directly implements a replicated state machine — if every replica applies the same operations in the same order, they all stay identical — which is the foundation of consensus-based replication and single-leader logs. Critically, it's *equivalent* to consensus and to a linearizable compare-and-set register, so solving any one solves the others.","correct":"Total order broadcast (atomic broadcast) is a protocol that delivers messages to all nodes reliably (none lost) and in exactly the same order on every node.","distractors":["Total order broadcast, a linearizable compare-and-set (or atomic increment) register, leader election, atomic commit, and uniqueness constraints all reduce to consensus — you can build each from the others.","Consensus is getting several nodes to agree on a single value.","Causality is a *partial* order: it requires that causally related events keep their order (the question before the answer, a row created before it's updated) while leaving truly concurrent, unrelated events unordered."]},{"id":"book/designing_data_intensive_applications/09_consistency_and_consensus#Q9-5220","section":"book","module":"book/designing_data_intensive_applications/09_consistency_and_consensus","moduleName":"09 consistency and consensus","qIndex":9,"difficulty":"intermediate","question":"Define the consensus problem and its four properties.","answerFull":"Consensus is getting several nodes to agree on a single value. Its properties are: uniform agreement (no two nodes decide differently), integrity (no node decides more than once), validity (the decided value was actually proposed by some node, ruling out trivial constant answers), and termination (every node that doesn't crash eventually decides — the liveness property). Termination is what makes consensus hard and is why it requires a majority of nodes to be available to make progress.","correct":"Consensus is getting several nodes to agree on a single value.","distractors":["Total order broadcast, a linearizable compare-and-set (or atomic increment) register, leader election, atomic commit, and uniqueness constraints all reduce to consensus — you can build each from the others.","Total order broadcast (atomic broadcast) is a protocol that delivers messages to all nodes reliably (none lost) and in exactly the same order on every node.","Causality is a *partial* order: it requires that causally related events keep their order (the question before the answer, a row created before it's updated) while leaving truly concurrent, unrelated events unordered."]},{"id":"book/designing_data_intensive_applications/09_consistency_and_consensus#Q11-5221","section":"book","module":"book/designing_data_intensive_applications/09_consistency_and_consensus","moduleName":"09 consistency and consensus","qIndex":11,"difficulty":"advanced","question":"How does two-phase commit work, and what is its fatal flaw?","answerFull":"In phase one (prepare), a coordinator asks all participants if they can commit; each writes the transaction durably and votes yes or no, where a \"yes\" is an irrevocable promise it can no longer abort on its own. In phase two, if all voted yes the coordinator logs and broadcasts \"commit\" (the point of no return); if any voted no it broadcasts \"abort.\" The fatal flaw is that if the coordinator crashes after participants vote yes but before broadcasting the decision, those participants are stuck \"in doubt\" — they've promised to commit but don't know whether to — so they block, holding locks, until the coordinator recovers.","correct":"In phase one (prepare), a coordinator asks all participants if they can commit; each writes the transaction durably and votes yes or no, where a \"yes\" is an irrevocable promise it can no longer abort on its own.","distractors":["Total order broadcast, a linearizable compare-and-set (or atomic increment) register, leader election, atomic commit, and uniqueness constraints all reduce to consensus — you can build each from the others.","A media service writes an uploaded full-size image to blob storage and then enqueues a \"generate thumbnail\" message referencing it.","Causality is a *partial* order: it requires that causally related events keep their order (the question before the answer, a row created before it's updated) while leaving truly concurrent, unrelated events unordered."]},{"id":"book/designing_data_intensive_applications/09_consistency_and_consensus#Q13-5222","section":"book","module":"book/designing_data_intensive_applications/09_consistency_and_consensus","moduleName":"09 consistency and consensus","qIndex":13,"difficulty":"advanced","question":"What role do epoch/term/ballot numbers play in consensus algorithms?","answerFull":"They are monotonically increasing numbers identifying each leadership generation, functioning like fencing tokens. Within a single epoch there's at most one leader, and a value is decided only if a majority votes for it in that epoch; when a leader is suspected dead, a new election produces a *higher* epoch number. Because quorums from different epochs overlap, a new leader is guaranteed to learn of any value an older leader might have committed, and proposals tagged with stale (lower) epoch numbers are rejected — preventing a deposed leader from causing a conflicting decision (split brain).","correct":"They are monotonically increasing numbers identifying each leadership generation, functioning like fencing tokens.","distractors":["Consensus is getting several nodes to agree on a single value.","Total order broadcast, a linearizable compare-and-set (or atomic increment) register, leader election, atomic commit, and uniqueness constraints all reduce to consensus — you can build each from the others.","Causality is a *partial* order: it requires that causally related events keep their order (the question before the answer, a row created before it's updated) while leaving truly concurrent, unrelated events unordered."]},{"id":"book/designing_data_intensive_applications/09_consistency_and_consensus#Q14-5223","section":"book","module":"book/designing_data_intensive_applications/09_consistency_and_consensus","moduleName":"09 consistency and consensus","qIndex":14,"difficulty":"advanced","question":"What problems are equivalent to consensus?","answerFull":"Total order broadcast, a linearizable compare-and-set (or atomic increment) register, leader election, atomic commit, and uniqueness constraints all reduce to consensus — you can build each from the others. This equivalence is a central insight of the chapter: a wide range of seemingly different distributed-systems problems are the *same* underlying problem, which is why a single well-built consensus implementation (packaged as ZooKeeper or etcd) can serve so many use cases like locks, leader election, and membership.","correct":"Total order broadcast, a linearizable compare-and-set (or atomic increment) register, leader election, atomic commit, and uniqueness constraints all reduce to consensus — you can build each from the others.","distractors":["Causality is a *partial* order: it requires that causally related events keep their order (the question before the answer, a row created before it's updated) while leaving truly concurrent, unrelated events unordered.","Total order broadcast (atomic broadcast) is a protocol that delivers messages to all nodes reliably (none lost) and in exactly the same order on every node.","In phase one (prepare), a coordinator asks all participants if they can commit; each writes the transaction durably and votes yes or no, where a \"yes\" is an irrevocable promise it can no longer abort on its own."]},{"id":"book/designing_data_intensive_applications/09_consistency_and_consensus#Q16-5224","section":"book","module":"book/designing_data_intensive_applications/09_consistency_and_consensus","moduleName":"09 consistency and consensus","qIndex":16,"difficulty":"advanced","question":"Give an example of a cross-channel timing dependency that requires linearizability.","answerFull":"A media service writes an uploaded full-size image to blob storage and then enqueues a \"generate thumbnail\" message referencing it. If the message-processing worker fetches the image before the write has propagated to the replica it reads from, it sees a missing or stale image and produces a wrong or failed thumbnail. The two communication channels — the message queue and the storage system — together create a recency requirement that only a linearizable read of the storage (or carrying the image data in the message, or waiting for replication) can satisfy.","correct":"A media service writes an uploaded full-size image to blob storage and then enqueues a \"generate thumbnail\" message referencing it.","distractors":["In phase one (prepare), a coordinator asks all participants if they can commit; each writes the transaction durably and votes yes or no, where a \"yes\" is an irrevocable promise it can no longer abort on its own.","Causality is a *partial* order: it requires that causally related events keep their order (the question before the answer, a row created before it's updated) while leaving truly concurrent, unrelated events unordered.","Total order broadcast (atomic broadcast) is a protocol that delivers messages to all nodes reliably (none lost) and in exactly the same order on every node."]},{"id":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems#Q2-5225","section":"book","module":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems","moduleName":"08 trouble with distributed systems","qIndex":2,"difficulty":"core","question":"What is partial failure, and why does it make distributed systems harder than single machines?","answerFull":"Partial failure is when some components of a system fail while others keep working, and crucially you often can't tell which — and the same operation may succeed sometimes and fail other times nondeterministically. A single machine is mostly deterministic: it works or it cleanly crashes. Distributed systems remove that certainty, so you can't assume an operation either fully happened or fully didn't; you must design every protocol to cope with \"I don't know whether that succeeded,\" which is the root of most distributed-systems complexity.","correct":"Partial failure is when some components of a system fail while others keep working, and crucially you often can't tell which — and the same operation may succeed sometimes and fail other times nondeterministically.","distractors":["A Byzantine fault is a node behaving arbitrarily — lying, sending corrupted or contradictory messages, or actively trying to deceive others — as opposed to merely crashing or being slow.","Because no individual node can reliably know the global state — it may have been wrongly declared dead during a pause, or it may wrongly believe it's the leader — so trusting any single node's self-assessment is unsafe.","A network partition (netsplit) is when the network is otherwise functioning but communication between some sets of nodes is cut off, so each side sees the other as unreachable."]},{"id":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems#Q4-5226","section":"book","module":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems","moduleName":"08 trouble with distributed systems","qIndex":4,"difficulty":"core","question":"Why is using last-write-wins with wall-clock timestamps dangerous across nodes?","answerFull":"Because clocks on different nodes disagree (NTP accuracy is only tens of milliseconds at best), so a write that genuinely happened *later* can receive a *smaller* timestamp if it came from a node whose clock is behind. Last-write-wins then keeps the write with the larger timestamp and silently discards the other, losing data that was actually the most recent — and the loss is invisible and unattributable. Ordering causally related events by wall clock across nodes is unreliable; logical clocks or bounded-uncertainty clocks are needed instead.","correct":"Because clocks on different nodes disagree (NTP accuracy is only tens of milliseconds at best), so a write that genuinely happened *later* can receive a *smaller* timestamp if it came from a node whose clock is behind.","distractors":["TrueTime represents the current time not as a single value but as an interval [earliest, latest] with a known, narrow bound on uncertainty, achieved using GPS receivers and atomic clocks in each datacenter.","Crash-stop assumes a node fails only by crashing and then never comes back — the simplest model.","The synchronous model assumes bounded network delay, bounded clock drift, and bounded process pauses — clean but unrealistic."]},{"id":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems#Q6-5227","section":"book","module":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems","moduleName":"08 trouble with distributed systems","qIndex":6,"difficulty":"intermediate","question":"Why is \"truth defined by the majority\" the foundation of distributed correctness?","answerFull":"Because no individual node can reliably know the global state — it may have been wrongly declared dead during a pause, or it may wrongly believe it's the leader — so trusting any single node's self-assessment is unsafe. Requiring a majority (quorum) to agree on decisions means that even if a minority of nodes are faulty, partitioned, or confused, there's exactly one authoritative version of truth, and a node out of touch with the majority must stop acting unilaterally. This majority principle underpins leader election and consensus in Chapter 9.","correct":"Because no individual node can reliably know the global state — it may have been wrongly declared dead during a pause, or it may wrongly believe it's the leader — so trusting any single node's self-assessment is unsafe.","distractors":["Because a short timeout produces false positives: a node that's merely slow (a GC pause, a transient network blip) gets declared dead while it's actually still doing work.","Safety means \"nothing bad ever happens\" — for example, no two nodes are leader at once, or no fencing token is issued twice; once violated, a safety property can't be undone.","Because clocks on different nodes disagree (NTP accuracy is only tens of milliseconds at best), so a write that genuinely happened *later* can receive a *smaller* timestamp if it came from a node whose clock is behind."]},{"id":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems#Q7-5228","section":"book","module":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems","moduleName":"08 trouble with distributed systems","qIndex":7,"difficulty":"intermediate","question":"What causes variable delay in packet-switched networks, and why don't we just use bounded-delay networks?","answerFull":"The main cause is queueing: when a network link, switch, or receiver is busy, packets wait in queues for an unpredictable time, and TCP's retransmission and congestion control add further variable delay. Packet switching is used because it's optimized for bursty traffic like web requests and file transfers, where it would be wasteful to reserve a fixed slice of bandwidth end-to-end as circuit-switched networks (the old telephone system) do. Bounded-delay networks are technically possible but uneconomical, so datacenters accept asynchronous, best-effort networks and engineer around the variability.","correct":"The main cause is queueing: when a network link, switch, or receiver is busy, packets wait in queues for an unpredictable time, and TCP's retransmission and congestion control add further variable delay.","distractors":["TrueTime represents the current time not as a single value but as an interval [earliest, latest] with a known, narrow bound on uncertainty, achieved using GPS receivers and atomic clocks in each datacenter.","Because a short timeout produces false positives: a node that's merely slow (a GC pause, a transient network blip) gets declared dead while it's actually still doing work.","A network partition (netsplit) is when the network is otherwise functioning but communication between some sets of nodes is cut off, so each side sees the other as unreachable."]},{"id":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems#Q9-5229","section":"book","module":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems","moduleName":"08 trouble with distributed systems","qIndex":9,"difficulty":"intermediate","question":"What is a network partition, and what's the right way to deal with one?","answerFull":"A network partition (netsplit) is when the network is otherwise functioning but communication between some sets of nodes is cut off, so each side sees the other as unreachable. The right approach isn't necessarily to keep operating fully on both sides (that risks split-brain), but to *know and deliberately decide* how your system behaves under a partition — and to test that behavior with fault injection (Jepsen-style). Pretending partitions won't happen leads to silent data loss or two-leaders corruption when they inevitably do.","correct":"A network partition (netsplit) is when the network is otherwise functioning but communication between some sets of nodes is cut off, so each side sees the other as unreachable.","distractors":["Safety means \"nothing bad ever happens\" — for example, no two nodes are leader at once, or no fencing token is issued twice; once violated, a safety property can't be undone.","The synchronous model assumes bounded network delay, bounded clock drift, and bounded process pauses — clean but unrealistic.","Because clocks on different nodes disagree (NTP accuracy is only tens of milliseconds at best), so a write that genuinely happened *later* can receive a *smaller* timestamp if it came from a node whose clock is behind."]},{"id":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems#Q10-5230","section":"book","module":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems","moduleName":"08 trouble with distributed systems","qIndex":10,"difficulty":"intermediate","question":"What is a Byzantine fault, and when is it worth defending against?","answerFull":"A Byzantine fault is a node behaving arbitrarily — lying, sending corrupted or contradictory messages, or actively trying to deceive others — as opposed to merely crashing or being slow. Byzantine fault tolerance is worth the cost in adversarial or trustless settings: aerospace systems where radiation can flip bits, and blockchains where mutually distrusting parties have no central authority (typically requiring more than two-thirds honest nodes). In a normal datacenter where you control all the nodes, full BFT is usually too expensive; you instead assume honest-but-faulty nodes and defend against accidental corruption with checksums and validation.","correct":"A Byzantine fault is a node behaving arbitrarily — lying, sending corrupted or contradictory messages, or actively trying to deceive others — as opposed to merely crashing or being slow.","distractors":["Because no individual node can reliably know the global state — it may have been wrongly declared dead during a pause, or it may wrongly believe it's the leader — so trusting any single node's self-assessment is unsafe.","Crash-stop assumes a node fails only by crashing and then never comes back — the simplest model.","Partial failure is when some components of a system fail while others keep working, and crucially you often can't tell which — and the same operation may succeed sometimes and fail other times nondeterministically."]},{"id":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems#Q11-5231","section":"book","module":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems","moduleName":"08 trouble with distributed systems","qIndex":11,"difficulty":"advanced","question":"Define safety and liveness, and explain the typical guarantee an algorithm makes about each.","answerFull":"Safety means \"nothing bad ever happens\" — for example, no two nodes are leader at once, or no fencing token is issued twice; once violated, a safety property can't be undone. Liveness means \"something good eventually happens\" — for example, a request eventually receives a response. The typical design goal is to guarantee safety *always*, even when timing assumptions break down (network slow, clocks off), while guaranteeing liveness only *under certain conditions*, such as while a majority of nodes are reachable — because you can't promise progress during an indefinite partition.","correct":"Safety means \"nothing bad ever happens\" — for example, no two nodes are leader at once, or no fencing token is issued twice; once violated, a safety property can't be undone.","distractors":["Because no individual node can reliably know the global state — it may have been wrongly declared dead during a pause, or it may wrongly believe it's the leader — so trusting any single node's self-assessment is unsafe.","A network partition (netsplit) is when the network is otherwise functioning but communication between some sets of nodes is cut off, so each side sees the other as unreachable.","Because clocks on different nodes disagree (NTP accuracy is only tens of milliseconds at best), so a write that genuinely happened *later* can receive a *smaller* timestamp if it came from a node whose clock is behind."]},{"id":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems#Q12-5232","section":"book","module":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems","moduleName":"08 trouble with distributed systems","qIndex":12,"difficulty":"advanced","question":"What are the three timing models, and which one reflects reality?","answerFull":"The synchronous model assumes bounded network delay, bounded clock drift, and bounded process pauses — clean but unrealistic. The asynchronous model assumes no timing guarantees at all and can't even use timeouts — very restrictive and overly pessimistic. The partially synchronous model assumes the system behaves synchronously *most* of the time but occasionally exceeds the bounds — and this is the realistic, commonly used model for designing real distributed algorithms, because it captures networks that are usually fine but sometimes spike.","correct":"The synchronous model assumes bounded network delay, bounded clock drift, and bounded process pauses — clean but unrealistic.","distractors":["Because a short timeout produces false positives: a node that's merely slow (a GC pause, a transient network blip) gets declared dead while it's actually still doing work.","The main cause is queueing: when a network link, switch, or receiver is busy, packets wait in queues for an unpredictable time, and TCP's retransmission and congestion control add further variable delay.","Crash-stop assumes a node fails only by crashing and then never comes back — the simplest model."]},{"id":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems#Q13-5233","section":"book","module":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems","moduleName":"08 trouble with distributed systems","qIndex":13,"difficulty":"advanced","question":"What are the crash-stop, crash-recovery, and Byzantine node models?","answerFull":"Crash-stop assumes a node fails only by crashing and then never comes back — the simplest model. Crash-recovery assumes a node may crash and later restart, losing its in-memory state but retaining data on stable storage — the model that matches most real databases, since servers do reboot. Byzantine assumes nodes may behave arbitrarily or maliciously. The model you choose determines what your algorithm must tolerate; most practical systems target crash-recovery with non-Byzantine assumptions.","correct":"Crash-stop assumes a node fails only by crashing and then never comes back — the simplest model.","distractors":["Because a short timeout produces false positives: a node that's merely slow (a GC pause, a transient network blip) gets declared dead while it's actually still doing work.","Because clocks on different nodes disagree (NTP accuracy is only tens of milliseconds at best), so a write that genuinely happened *later* can receive a *smaller* timestamp if it came from a node whose clock is behind.","A Byzantine fault is a node behaving arbitrarily — lying, sending corrupted or contradictory messages, or actively trying to deceive others — as opposed to merely crashing or being slow."]},{"id":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems#Q14-5234","section":"book","module":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems","moduleName":"08 trouble with distributed systems","qIndex":14,"difficulty":"advanced","question":"How does Google Spanner's TrueTime use clock uncertainty instead of pretending it away?","answerFull":"TrueTime represents the current time not as a single value but as an interval [earliest, latest] with a known, narrow bound on uncertainty, achieved using GPS receivers and atomic clocks in each datacenter. To order transactions consistently, Spanner deliberately *waits out* the uncertainty: after a transaction gets its timestamp, it sleeps for the width of the interval before committing, guaranteeing that any later transaction's interval starts after this one's ended. This converts the unavoidable clock uncertainty into a correctness mechanism rather than a hidden source of bugs.","correct":"TrueTime represents the current time not as a single value but as an interval [earliest, latest] with a known, narrow bound on uncertainty, achieved using GPS receivers and atomic clocks in each datacenter.","distractors":["The main cause is queueing: when a network link, switch, or receiver is busy, packets wait in queues for an unpredictable time, and TCP's retransmission and congestion control add further variable delay.","Because clocks on different nodes disagree (NTP accuracy is only tens of milliseconds at best), so a write that genuinely happened *later* can receive a *smaller* timestamp if it came from a node whose clock is behind.","A network partition (netsplit) is when the network is otherwise functioning but communication between some sets of nodes is cut off, so each side sees the other as unreachable."]},{"id":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems#Q16-5235","section":"book","module":"book/designing_data_intensive_applications/08_trouble_with_distributed_systems","moduleName":"08 trouble with distributed systems","qIndex":16,"difficulty":"advanced","question":"Why is detecting a failed node with too short a timeout sometimes worse than a slow detection?","answerFull":"Because a short timeout produces false positives: a node that's merely slow (a GC pause, a transient network blip) gets declared dead while it's actually still doing work. The system then duplicates that work elsewhere, may let the \"dead\" node's in-flight actions still take effect, and redistributes its load onto other nodes that may already be near capacity — which can overload them, trigger more false-positive timeouts, and cascade into a system-wide outage. So aggressive failure detection can manufacture the very failure it was trying to handle.","correct":"Because a short timeout produces false positives: a node that's merely slow (a GC pause, a transient network blip) gets declared dead while it's actually still doing work.","distractors":["Because no individual node can reliably know the global state — it may have been wrongly declared dead during a pause, or it may wrongly believe it's the leader — so trusting any single node's self-assessment is unsafe.","Because clocks on different nodes disagree (NTP accuracy is only tens of milliseconds at best), so a write that genuinely happened *later* can receive a *smaller* timestamp if it came from a node whose clock is behind.","The synchronous model assumes bounded network delay, bounded clock drift, and bounded process pauses — clean but unrealistic."]},{"id":"book/designing_data_intensive_applications/00_preface_and_book_map#Q1-5236","section":"book","module":"book/designing_data_intensive_applications/00_preface_and_book_map","moduleName":"00 preface and book map","qIndex":1,"difficulty":"core","question":"What does \"data-intensive\" mean, and why does the book claim most modern apps are data-intensive rather than compute-intensive?","answerFull":"Data-intensive means the binding constraint is the *data* — its volume, its complexity, or how fast it changes — not CPU cycles. Kleppmann argues most apps hit data limits first: a typical web service spends its effort coordinating databases, caches, and indexes, not doing heavy math. The practical consequence is that the skills that matter are about storage, replication, and consistency, not micro-optimizing algorithms.","correct":"Data-intensive means the binding constraint is the *data* — its volume, its complexity, or how fast it changes — not CPU cycles.","distractors":["Because the tool landscape changes constantly and the categories blur (Redis is a cache and a broker; Kafka is a log and a store).","Reliability (works correctly despite faults), scalability (has strategies to cope as load grows), and maintainability (people can work on it productively over time).","A fault is one component deviating from its spec (a disk dies, a function throws); a failure is the system as a whole stopping service."]},{"id":"book/designing_data_intensive_applications/00_preface_and_book_map#Q2-5237","section":"book","module":"book/designing_data_intensive_applications/00_preface_and_book_map","moduleName":"00 preface and book map","qIndex":2,"difficulty":"core","question":"Name the three core concerns the book is organized around and give a one-line definition of each.","answerFull":"Reliability (works correctly despite faults), scalability (has strategies to cope as load grows), and maintainability (people can work on it productively over time). They're not independent — an unscalable system fails under load (a reliability problem), and an unmaintainable one accumulates bugs. The book returns to this triad in nearly every chapter.","correct":"Reliability (works correctly despite faults), scalability (has strategies to cope as load grows), and maintainability (people can work on it productively over time).","distractors":["Maintainability is the ease with which different people can work on the system over its lifetime.","Different access patterns need different engines: OLTP databases for point reads/writes, search indexes for full-text, caches for hot reads, stream processors for real-time, batch systems for analytics.","Data-intensive means the binding constraint is the *data* — its volume, its complexity, or how fast it changes — not CPU cycles."]},{"id":"book/designing_data_intensive_applications/00_preface_and_book_map#Q3-5238","section":"book","module":"book/designing_data_intensive_applications/00_preface_and_book_map","moduleName":"00 preface and book map","qIndex":3,"difficulty":"core","question":"What is the difference between a fault and a failure, and why does the distinction matter?","answerFull":"A fault is one component deviating from its spec (a disk dies, a function throws); a failure is the system as a whole stopping service. The distinction matters because the goal of reliability engineering is to *tolerate faults so they don't escalate into failures* — you build fault-tolerance, not fault-prevention, because faults are inevitable. Netflix's Chaos Monkey embodies this: inject faults deliberately to prove they don't become failures.","correct":"A fault is one component deviating from its spec (a disk dies, a function throws); a failure is the system as a whole stopping service.","distractors":["Data-intensive means the binding constraint is the *data* — its volume, its complexity, or how fast it changes — not CPU cycles.","Reliability (works correctly despite faults), scalability (has strategies to cope as load grows), and maintainability (people can work on it productively over time).","A system of record holds the authoritative, canonical copy of data — the source of truth, written once."]},{"id":"book/designing_data_intensive_applications/00_preface_and_book_map#Q4-5239","section":"book","module":"book/designing_data_intensive_applications/00_preface_and_book_map","moduleName":"00 preface and book map","qIndex":4,"difficulty":"core","question":"What is the difference between a system of record and derived data?","answerFull":"A system of record holds the authoritative, canonical copy of data — the source of truth, written once. Derived data is computed *from* a system of record: caches, denormalized fields, search indexes, materialized views. The key property is that derived data is redundant and rebuildable — if it's lost or corrupted, you regenerate it from the source. This distinction is the conceptual backbone of Part III.","correct":"A system of record holds the authoritative, canonical copy of data — the source of truth, written once.","distractors":["Because the insight that caches, indexes, and materialized views are all *the same thing* — data derived from a source via a transformation — is genuinely unifying.","Because once data lives in more than one store, no single store can promise the set agrees.","Data-intensive means the binding constraint is the *data* — its volume, its complexity, or how fast it changes — not CPU cycles."]},{"id":"book/designing_data_intensive_applications/00_preface_and_book_map#Q5-5240","section":"book","module":"book/designing_data_intensive_applications/00_preface_and_book_map","moduleName":"00 preface and book map","qIndex":5,"difficulty":"core","question":"Why does the book teach principles instead of specific products?","answerFull":"Because the tool landscape changes constantly and the categories blur (Redis is a cache and a broker; Kafka is a log and a store). If you only know products, you can't evaluate a new one. If you understand the underlying principles — what guarantees a quorum gives, what isolation level prevents which anomaly — you can read any system's docs and know what it actually promises. Principles age slowly; product names age fast.","correct":"Because the tool landscape changes constantly and the categories blur (Redis is a cache and a broker; Kafka is a log and a store).","distractors":["Databases (store and retrieve), caches (remember expensive results), search indexes (filter/search), stream processing (async messaging), and batch processing (periodic bulk crunching).","Because once data lives in more than one store, no single store can promise the set agrees.","Data-intensive means the binding constraint is the *data* — its volume, its complexity, or how fast it changes — not CPU cycles."]},{"id":"book/designing_data_intensive_applications/00_preface_and_book_map#Q6-5241","section":"book","module":"book/designing_data_intensive_applications/00_preface_and_book_map","moduleName":"00 preface and book map","qIndex":6,"difficulty":"intermediate","question":"Why is \"no single tool does it all anymore\" the book's motivating premise?","answerFull":"Different access patterns need different engines: OLTP databases for point reads/writes, search indexes for full-text, caches for hot reads, stream processors for real-time, batch systems for analytics. One tool optimized for all of these would be optimized for none. So applications compose specialized tools — and that composition creates the central problem: keeping them consistent. The whole book follows from this premise.","correct":"Different access patterns need different engines: OLTP databases for point reads/writes, search indexes for full-text, caches for hot reads, stream processors for real-time, batch systems for analytics.","distractors":["Because the insight that caches, indexes, and materialized views are all *the same thing* — data derived from a source via a transformation — is genuinely unifying.","Because the tool landscape changes constantly and the categories blur (Redis is a cache and a broker; Kafka is a log and a store).","Part I (foundations) reasons about a single machine — conceptually rich but no distribution."]},{"id":"book/designing_data_intensive_applications/00_preface_and_book_map#Q7-5242","section":"book","module":"book/designing_data_intensive_applications/00_preface_and_book_map","moduleName":"00 preface and book map","qIndex":7,"difficulty":"intermediate","question":"If you compose several data stores, who is responsible for the consistency between them?","answerFull":"Your application code is. No individual product can guarantee that the database, the cache, and the search index agree with each other, because none of them knows about the others. The application that writes to all of them owns the contract that they stay in sync — which is why Part III spends so much effort on dataflow and exactly-once semantics.","correct":"Your application code is.","distractors":["Data-intensive means the binding constraint is the *data* — its volume, its complexity, or how fast it changes — not CPU cycles.","Reliability (works correctly despite faults), scalability (has strategies to cope as load grows), and maintainability (people can work on it productively over time).","A fault is one component deviating from its spec (a disk dies, a function throws); a failure is the system as a whole stopping service."]},{"id":"book/designing_data_intensive_applications/00_preface_and_book_map#Q8-5243","section":"book","module":"book/designing_data_intensive_applications/00_preface_and_book_map","moduleName":"00 preface and book map","qIndex":8,"difficulty":"intermediate","question":"What does the book mean by \"maintainability\", and what three sub-properties make it up?","answerFull":"Maintainability is the ease with which different people can work on the system over its lifetime. It decomposes into operability (easy for operations to keep it running), simplicity (easy for new engineers to understand — minimizing accidental complexity), and evolvability (easy to change as requirements shift, also called extensibility or plasticity). Most of a system's cost is in maintenance, not initial build, so this concern is economically the largest.","correct":"Maintainability is the ease with which different people can work on the system over its lifetime.","distractors":["Reliability (works correctly despite faults), scalability (has strategies to cope as load grows), and maintainability (people can work on it productively over time).","Different access patterns need different engines: OLTP databases for point reads/writes, search indexes for full-text, caches for hot reads, stream processors for real-time, batch systems for analytics.","Data-intensive means the binding constraint is the *data* — its volume, its complexity, or how fast it changes — not CPU cycles."]},{"id":"book/designing_data_intensive_applications/00_preface_and_book_map#Q9-5244","section":"book","module":"book/designing_data_intensive_applications/00_preface_and_book_map","moduleName":"00 preface and book map","qIndex":9,"difficulty":"intermediate","question":"Why does the preface say correctness becomes \"your problem\" rather than the database's?","answerFull":"Because once data lives in more than one store, no single store can promise the set agrees. A classic bug: you update the database but the cache still serves the old value, or the search index lags. The database did its job correctly; the *system* is still wrong. The application straddling multiple stores is the only place that can enforce cross-store correctness.","correct":"Because once data lives in more than one store, no single store can promise the set agrees.","distractors":["Because the tool landscape changes constantly and the categories blur (Redis is a cache and a broker; Kafka is a log and a store).","Databases (store and retrieve), caches (remember expensive results), search indexes (filter/search), stream processing (async messaging), and batch processing (periodic bulk crunching).","Part I (foundations) reasons about a single machine — conceptually rich but no distribution."]},{"id":"book/designing_data_intensive_applications/00_preface_and_book_map#Q10-5245","section":"book","module":"book/designing_data_intensive_applications/00_preface_and_book_map","moduleName":"00 preface and book map","qIndex":10,"difficulty":"intermediate","question":"What kinds of building blocks does the preface enumerate, and how do they blur together?","answerFull":"Databases (store and retrieve), caches (remember expensive results), search indexes (filter/search), stream processing (async messaging), and batch processing (periodic bulk crunching). They blur because real products span categories — Redis is a key-value store, a cache, and a message broker; Kafka is a message queue and a durable log that can act like a database. This blurring is precisely why category labels are less useful than understanding guarantees.","correct":"Databases (store and retrieve), caches (remember expensive results), search indexes (filter/search), stream processing (async messaging), and batch processing (periodic bulk crunching).","distractors":["Because once data lives in more than one store, no single store can promise the set agrees.","Because the tool landscape changes constantly and the categories blur (Redis is a cache and a broker; Kafka is a log and a store).","Different access patterns need different engines: OLTP databases for point reads/writes, search indexes for full-text, caches for hot reads, stream processors for real-time, batch systems for analytics."]},{"id":"book/designing_data_intensive_applications/00_preface_and_book_map#Q11-5246","section":"book","module":"book/designing_data_intensive_applications/00_preface_and_book_map","moduleName":"00 preface and book map","qIndex":11,"difficulty":"advanced","question":"Who is the intended audience, and what is the book explicitly NOT?","answerFull":"It targets engineers who build on top of data systems and want to understand the internals and tradeoffs. It is not a single-tool tutorial, not an ops runbook, and not introductory — it assumes prior experience with databases and application code. Knowing what it isn't keeps you from expecting step-by-step product instructions.","correct":"It targets engineers who build on top of data systems and want to understand the internals and tradeoffs.","distractors":["Data-intensive means the binding constraint is the *data* — its volume, its complexity, or how fast it changes — not CPU cycles.","Reliability (works correctly despite faults), scalability (has strategies to cope as load grows), and maintainability (people can work on it productively over time).","A fault is one component deviating from its spec (a disk dies, a function throws); a failure is the system as a whole stopping service."]},{"id":"book/designing_data_intensive_applications/00_preface_and_book_map#Q12-5247","section":"book","module":"book/designing_data_intensive_applications/00_preface_and_book_map","moduleName":"00 preface and book map","qIndex":12,"difficulty":"advanced","question":"How does the three-part structure map onto a difficulty curve?","answerFull":"Part I (foundations) reasons about a single machine — conceptually rich but no distribution. Part II (distributed data) is the hard core, where networks and clocks betray you. Part III (derived data) is about composition and is conceptually unifying. Difficulty peaks in Part II (Chapters 7–9), so if you're studying for interviews, that's where to spend the most time.","correct":"Part I (foundations) reasons about a single machine — conceptually rich but no distribution.","distractors":["Because once data lives in more than one store, no single store can promise the set agrees.","Data-intensive means the binding constraint is the *data* — its volume, its complexity, or how fast it changes — not CPU cycles.","Reliability (works correctly despite faults), scalability (has strategies to cope as load grows), and maintainability (people can work on it productively over time)."]},{"id":"book/designing_data_intensive_applications/00_preface_and_book_map#Q13-5248","section":"book","module":"book/designing_data_intensive_applications/00_preface_and_book_map","moduleName":"00 preface and book map","qIndex":13,"difficulty":"advanced","question":"Why does the author put \"derived data\" in its own part rather than treating it as an implementation detail?","answerFull":"Because the insight that caches, indexes, and materialized views are all *the same thing* — data derived from a source via a transformation — is genuinely unifying. Once you see batch and stream processing as two ways of maintaining derived data, the whole zoo of tools (Hadoop, Spark, Kafka Streams, Flink) collapses into one mental model. Part III earns its place by delivering that unification.","correct":"Because the insight that caches, indexes, and materialized views are all *the same thing* — data derived from a source via a transformation — is genuinely unifying.","distractors":["Different access patterns need different engines: OLTP databases for point reads/writes, search indexes for full-text, caches for hot reads, stream processors for real-time, batch systems for analytics.","Part I (foundations) reasons about a single machine — conceptually rich but no distribution.","A system of record holds the authoritative, canonical copy of data — the source of truth, written once."]},{"id":"book/designing_data_intensive_applications/00_preface_and_book_map#Q14-5249","section":"book","module":"book/designing_data_intensive_applications/00_preface_and_book_map","moduleName":"00 preface and book map","qIndex":14,"difficulty":"advanced","question":"Give an example where ignoring the system-of-record vs derived-data distinction causes a production incident.","answerFull":"A team writes user profile edits directly to Elasticsearch (the search index) for speed and \"later\" syncs back to Postgres. The sync breaks; now the index has edits the database never saw, and there's no way to rebuild the index because it *is* the source for those fields. Treating a derived store as authoritative destroyed rebuildability — the cardinal sin the preface warns against.","correct":"A team writes user profile edits directly to Elasticsearch (the search index) for speed and \"later\" syncs back to Postgres.","distractors":["Because the insight that caches, indexes, and materialized views are all *the same thing* — data derived from a source via a transformation — is genuinely unifying.","Databases (store and retrieve), caches (remember expensive results), search indexes (filter/search), stream processing (async messaging), and batch processing (periodic bulk crunching).","Different access patterns need different engines: OLTP databases for point reads/writes, search indexes for full-text, caches for hot reads, stream processors for real-time, batch systems for analytics."]},{"id":"book/designing_data_intensive_applications/00_preface_and_book_map#Q15-5250","section":"book","module":"book/designing_data_intensive_applications/00_preface_and_book_map","moduleName":"00 preface and book map","qIndex":15,"difficulty":"advanced","question":"Why is \"compute-intensive vs data-intensive\" a useful framing even though some systems are both?","answerFull":"It directs your optimization effort to the actual bottleneck. If you assume compute is the limit, you'll micro-optimize code while the real cost is a chatty database or replication lag. Naming the dominant constraint up front — usually data — keeps architectural attention on storage, movement, and consistency, which is where data-intensive systems live or die.","correct":"It directs your optimization effort to the actual bottleneck.","distractors":["Data-intensive means the binding constraint is the *data* — its volume, its complexity, or how fast it changes — not CPU cycles.","Reliability (works correctly despite faults), scalability (has strategies to cope as load grows), and maintainability (people can work on it productively over time).","A fault is one component deviating from its spec (a disk dies, a function throws); a failure is the system as a whole stopping service."]}]