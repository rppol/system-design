[{"id":"java/annotation_processing#66351741f14c","section":"java","module":"java/annotation_processing","moduleName":"annotation processing","sourceFile":"README.md","difficulty":"core","question":"Explain how the rounds model works.","answerFull":"javac invokes processors in rounds. Round 1 sees the original source files; any files a processor writes via the Filer become inputs to a new round, where processors run again; this repeats until a round produces no new files. Then a final round runs with RoundEnvironment.processingOver() returning true, giving processors a last chance to emit (e.g. aggregate files), after which everything is compiled. The rounds let generated code itself trigger further generation, but output must converge or the compiler errors.","correct":"javac invokes processors in rounds.","distractors":["Returning specific annotation type names tells javac to invoke your processor only when those annotations are present, which is efficient and clear.","Because during annotation processing the bytecode does not exist yet — there is no loaded Class object for a type that is still being compiled.","Because native image uses a closed-world assumption and cannot follow runtime reflection without explicit metadata."],"distractorIds":["java/annotation_processing#7e607c37e8a7","java/annotation_processing#01db29b1ee2b","java/annotation_processing#80520b2dee3a"],"concepts":["javac","processors","explain","invokes","rounds","works"],"correctMd":"`javac` invokes processors in rounds.","answerFullMd":"`javac` invokes processors in rounds. Round 1 sees the original source files; any files a processor writes via the `Filer` become inputs to a new round, where processors run again; this repeats until a round produces no new files. Then a final round runs with `RoundEnvironment.processingOver()` returning true, giving processors a last chance to emit (e.g. aggregate files), after which everything is compiled. The rounds let generated code itself trigger further generation, but output must converge or the compiler errors.","distractorsMd":["Returning specific annotation type names tells `javac` to invoke your processor only when those annotations are present, which is efficient and clear.","Because during annotation processing the bytecode does not exist yet — there is no loaded `Class` object for a type that is still being compiled.",null]},{"id":"java/annotation_processing#01db29b1ee2b","section":"java","module":"java/annotation_processing","moduleName":"annotation processing","sourceFile":"README.md","difficulty":"core","question":"Why can't you use Class.forName on a type you are currently compiling?","answerFull":"Because during annotation processing the bytecode does not exist yet — there is no loaded Class object for a type that is still being compiled. Instead you work with the javax.lang.model mirror API: TypeElement for a type, ExecutableElement for a method, VariableElement for a field, and TypeMirror for a type reference, navigated with the Elements and Types utility services. This source-level model is the whole reason processing is decoupled from runtime reflection.","correct":"Because during annotation processing the bytecode does not exist yet — there is no loaded Class object for a type that is still being compiled.","distractors":["MapStruct is spec-compliant: it reads @Mapper interfaces and *generates new files* (a plain implementation class) via the Filer.","The Filer is the API a processor uses to create new source, class, or resource files — the sanctioned output channel.","Returning specific annotation type names tells javac to invoke your processor only when those annotations are present, which is efficient and clear."],"distractorIds":["java/annotation_processing#d149b5799d0c","java/annotation_processing#2786c55c47b0","java/annotation_processing#7e607c37e8a7"],"concepts":["compiling","forname","compiled","loaded","currently","exist"],"questionMd":"Why can't you use `Class.forName` on a type you are currently compiling?","correctMd":"Because during annotation processing the bytecode does not exist yet — there is no loaded `Class` object for a type that is still being compiled.","answerFullMd":"Because during annotation processing the bytecode does not exist yet — there is no loaded `Class` object for a type that is still being compiled. Instead you work with the `javax.lang.model` mirror API: `TypeElement` for a type, `ExecutableElement` for a method, `VariableElement` for a field, and `TypeMirror` for a type reference, navigated with the `Elements` and `Types` utility services. This source-level model is the whole reason processing is decoupled from runtime reflection.","distractorsMd":["MapStruct is spec-compliant: it reads `@Mapper` interfaces and *generates new files* (a plain implementation class) via the `Filer`.","The `Filer` is the API a processor uses to create new source, class, or resource files — the sanctioned output channel.","Returning specific annotation type names tells `javac` to invoke your processor only when those annotations are present, which is efficient and clear."]},{"id":"java/annotation_processing#d149b5799d0c","section":"java","module":"java/annotation_processing","moduleName":"annotation processing","sourceFile":"README.md","difficulty":"core","question":"How does Lombok differ from a normal annotation processor like MapStruct?","answerFull":"MapStruct is spec-compliant: it reads @Mapper interfaces and *generates new files* (a plain implementation class) via the Filer. Lombok registers as a processor but does not generate separate files — it reaches into javac's internal JCTree AST using non-public com.sun.tools.javac.* APIs and *mutates your existing class*, adding getters, equals, builders in place. That AST mutation is outside the JSR 269 spec, which is why Lombok needs IDE plugins, can break on JDK upgrades, and offers delombok to materialize the synthetic code.","correct":"MapStruct is spec-compliant: it reads @Mapper interfaces and *generates new files* (a plain implementation class) via the Filer.","distractors":["The Filer is the API a processor uses to create new source, class, or resource files — the sanctioned output channel.","If a processor is non-deterministic, reads inputs the build system cannot track, or regenerates outputs unpredictably, the build tool must fall back to full recompilation and may produce inconsistent results.","Returning specific annotation type names tells javac to invoke your processor only when those annotations are present, which is efficient and clear."],"distractorIds":["java/annotation_processing#2786c55c47b0","java/annotation_processing#5dc7a23b5ac5","java/annotation_processing#7e607c37e8a7"],"concepts":["compliant","filer","mapstruct","mapper","differ","processor"],"correctMd":"MapStruct is spec-compliant: it reads `@Mapper` interfaces and *generates new files* (a plain implementation class) via the `Filer`.","answerFullMd":"MapStruct is spec-compliant: it reads `@Mapper` interfaces and *generates new files* (a plain implementation class) via the `Filer`. Lombok registers as a processor but does not generate separate files — it reaches into `javac`'s internal `JCTree` AST using non-public `com.sun.tools.javac.*` APIs and *mutates your existing class*, adding getters, `equals`, builders in place. That AST mutation is outside the JSR 269 spec, which is why Lombok needs IDE plugins, can break on JDK upgrades, and offers `delombok` to materialize the synthetic code.","distractorsMd":["The `Filer` is the API a processor uses to create new source, class, or resource files — the sanctioned output channel.",null,"Returning specific annotation type names tells `javac` to invoke your processor only when those annotations are present, which is efficient and clear."]},{"id":"java/annotation_processing#62a6d2cdc770","section":"java","module":"java/annotation_processing","moduleName":"annotation processing","sourceFile":"README.md","difficulty":"intermediate","question":"How should a processor report a usage error to the developer?","answerFull":"Through the Messager, by calling processingEnv.getMessager().printMessage( Diagnostic.Kind.ERROR, \"message\", element), passing the offending Element so the error points at the right source location. A Kind.ERROR fails the compilation, turning what would be a runtime bug into a clean compile error. Throwing an exception from process instead produces an opaque compiler crash, so user-facing problems should always go through Messager.","correct":"Through the Messager, by calling processingEnv.getMessager().printMessage( Diagnostic.Kind.ERROR, \"message\", element), passing the offending Element so the error points at the right source location.","distractors":["If a processor is non-deterministic, reads inputs the build system cannot track, or regenerates outputs unpredictably, the build tool must fall back to full recompilation and may produce inconsistent results.","MapStruct's processor reads a @Mapper interface and generates a concrete implementation that copies fields with plain getter/setter calls — no reflection.","Returning specific annotation type names tells javac to invoke your processor only when those annotations are present, which is efficient and clear."],"distractorIds":["java/annotation_processing#5dc7a23b5ac5","java/annotation_processing#b1efb96e1d82","java/annotation_processing#7e607c37e8a7"],"concepts":["getmessager","messager","printmessage","processingenv","diagnostic","kind"],"correctMd":"Through the `Messager`, by calling `processingEnv.getMessager().printMessage( Diagnostic.Kind.ERROR, \"message\", element)`, passing the offending `Element` so the error points at the right source location.","answerFullMd":"Through the `Messager`, by calling `processingEnv.getMessager().printMessage( Diagnostic.Kind.ERROR, \"message\", element)`, passing the offending `Element` so the error points at the right source location. A `Kind.ERROR` fails the compilation, turning what would be a runtime bug into a clean compile error. Throwing an exception from `process` instead produces an opaque compiler crash, so user-facing problems should always go through `Messager`.","distractorsMd":[null,"MapStruct's processor reads a `@Mapper` interface and generates a concrete implementation that copies fields with plain getter/setter calls — no reflection.","Returning specific annotation type names tells `javac` to invoke your processor only when those annotations are present, which is efficient and clear."]},{"id":"java/annotation_processing#2786c55c47b0","section":"java","module":"java/annotation_processing","moduleName":"annotation processing","sourceFile":"README.md","difficulty":"intermediate","question":"What is the Filer and what restriction does it enforce?","answerFull":"The Filer is the API a processor uses to create new source, class, or resource files — the sanctioned output channel. Its key restriction is that you cannot create the same file twice in a single compilation: doing so throws a FilerException (\"attempt to recreate a file\"). This enforces idempotent, deterministic generation, which is necessary for reproducible builds and for incremental compilation to work correctly.","correct":"The Filer is the API a processor uses to create new source, class, or resource files — the sanctioned output channel.","distractors":["Returning specific annotation type names tells javac to invoke your processor only when those annotations are present, which is efficient and clear.","MapStruct is spec-compliant: it reads @Mapper interfaces and *generates new files* (a plain implementation class) via the Filer.","If a processor is non-deterministic, reads inputs the build system cannot track, or regenerates outputs unpredictably, the build tool must fall back to full recompilation and may produce inconsistent results."],"distractorIds":["java/annotation_processing#7e607c37e8a7","java/annotation_processing#d149b5799d0c","java/annotation_processing#5dc7a23b5ac5"],"concepts":["sanctioned","filer","restriction","enforce","processor","channel"],"correctMd":"The `Filer` is the API a processor uses to create new source, class, or resource files — the sanctioned output channel.","answerFullMd":"The `Filer` is the API a processor uses to create new source, class, or resource files — the sanctioned output channel. Its key restriction is that you cannot create the same file twice in a single compilation: doing so throws a `FilerException` (\"attempt to recreate a file\"). This enforces idempotent, deterministic generation, which is necessary for reproducible builds and for incremental compilation to work correctly.","distractorsMd":["Returning specific annotation type names tells `javac` to invoke your processor only when those annotations are present, which is efficient and clear.","MapStruct is spec-compliant: it reads `@Mapper` interfaces and *generates new files* (a plain implementation class) via the `Filer`.",null]},{"id":"java/annotation_processing#ee538d76a044","section":"java","module":"java/annotation_processing","moduleName":"annotation processing","sourceFile":"README.md","difficulty":"intermediate","question":"What is JavaPoet and why use it?","answerFull":"JavaPoet is a Square library that provides a fluent, type-safe builder API for generating .java source — TypeSpec, MethodSpec, FieldSpec, with ClassName references and $T/$L placeholders. You use it instead of concatenating strings because it handles imports, formatting, and type references correctly, producing clean generated code and avoiding the brittle, error-prone string-building that plagues hand-rolled generators.","correct":"JavaPoet is a Square library that provides a fluent, type-safe builder API for generating .java source — TypeSpec, MethodSpec, FieldSpec, with ClassName references and $T/$L placeholders.","distractors":["The Filer is the API a processor uses to create new source, class, or resource files — the sanctioned output channel.","Through the Messager, by calling processingEnv.getMessager().printMessage( Diagnostic.Kind.ERROR, \"message\", element), passing the offending Element so the error points at the right source location.","Because during annotation processing the bytecode does not exist yet — there is no loaded Class object for a type that is still being compiled."],"distractorIds":["java/annotation_processing#2786c55c47b0","java/annotation_processing#62a6d2cdc770","java/annotation_processing#01db29b1ee2b"],"concepts":["fieldspec","javapoet","methodspec","placeholders","typespec","classname"],"correctMd":"JavaPoet is a Square library that provides a fluent, type-safe builder API for generating `.java` source — `TypeSpec`, `MethodSpec`, `FieldSpec`, with `ClassName` references and `$T`/`$L` placeholders.","answerFullMd":"JavaPoet is a Square library that provides a fluent, type-safe builder API for generating `.java` source — `TypeSpec`, `MethodSpec`, `FieldSpec`, with `ClassName` references and `$T`/`$L` placeholders. You use it instead of concatenating strings because it handles imports, formatting, and type references correctly, producing clean generated code and avoiding the brittle, error-prone string-building that plagues hand-rolled generators.","distractorsMd":["The `Filer` is the API a processor uses to create new source, class, or resource files — the sanctioned output channel.","Through the `Messager`, by calling `processingEnv.getMessager().printMessage( Diagnostic.Kind.ERROR, \"message\", element)`, passing the offending `Element` so the error points at the right source location.","Because during annotation processing the bytecode does not exist yet — there is no loaded `Class` object for a type that is still being compiled."]},{"id":"java/annotation_processing#b1efb96e1d82","section":"java","module":"java/annotation_processing","moduleName":"annotation processing","sourceFile":"README.md","difficulty":"intermediate","question":"What does MapStruct do at compile time and what is the safety benefit?","answerFull":"MapStruct's processor reads a @Mapper interface and generates a concrete implementation that copies fields with plain getter/setter calls — no reflection. The safety benefit is that mapping mismatches are caught at compile time: if a target property has no matching source, MapStruct emits an \"Unmapped target property\" compile error, so mapping bugs surface during the build rather than as nulls or wrong values in production. The generated code is also as fast as hand-written mapping and fully native-compatible.","correct":"MapStruct's processor reads a @Mapper interface and generates a concrete implementation that copies fields with plain getter/setter calls — no reflection.","distractors":["Returning specific annotation type names tells javac to invoke your processor only when those annotations are present, which is efficient and clear.","If a processor is non-deterministic, reads inputs the build system cannot track, or regenerates outputs unpredictably, the build tool must fall back to full recompilation and may produce inconsistent results.","Because native image uses a closed-world assumption and cannot follow runtime reflection without explicit metadata."],"distractorIds":["java/annotation_processing#7e607c37e8a7","java/annotation_processing#5dc7a23b5ac5","java/annotation_processing#80520b2dee3a"],"concepts":["getter","mapstruct","setter","benefit","mapper","processor"],"correctMd":"MapStruct's processor reads a `@Mapper` interface and generates a concrete implementation that copies fields with plain getter/setter calls — no reflection.","answerFullMd":"MapStruct's processor reads a `@Mapper` interface and generates a concrete implementation that copies fields with plain getter/setter calls — no reflection. The safety benefit is that mapping mismatches are caught at compile time: if a target property has no matching source, MapStruct emits an \"Unmapped target property\" compile error, so mapping bugs surface during the build rather than as nulls or wrong values in production. The generated code is also as fast as hand-written mapping and fully native-compatible.","distractorsMd":["Returning specific annotation type names tells `javac` to invoke your processor only when those annotations are present, which is efficient and clear.",null,null]},{"id":"java/annotation_processing#80520b2dee3a","section":"java","module":"java/annotation_processing","moduleName":"annotation processing","sourceFile":"README.md","difficulty":"advanced","question":"Why is annotation processing important for GraalVM native images?","answerFull":"Because native image uses a closed-world assumption and cannot follow runtime reflection without explicit metadata. Code produced by annotation processors is plain, statically-analyzable Java with no reflection, so it is reachable and retained automatically — no hints needed. Frameworks that do their DI/mapping/AOP via processors (or AOT, as Spring Boot 3 does) are therefore native-friendly by construction, whereas reflection-heavy frameworks require extensive reachability metadata.","correct":"Because native image uses a closed-world assumption and cannot follow runtime reflection without explicit metadata.","distractors":["If a processor is non-deterministic, reads inputs the build system cannot track, or regenerates outputs unpredictably, the build tool must fall back to full recompilation and may produce inconsistent results.","MapStruct's processor reads a @Mapper interface and generates a concrete implementation that copies fields with plain getter/setter calls — no reflection.","Returning specific annotation type names tells javac to invoke your processor only when those annotations are present, which is efficient and clear."],"distractorIds":["java/annotation_processing#5dc7a23b5ac5","java/annotation_processing#b1efb96e1d82","java/annotation_processing#7e607c37e8a7"],"concepts":["graalvm","important","follow","assumption","world","reflection"],"distractorsMd":[null,"MapStruct's processor reads a `@Mapper` interface and generates a concrete implementation that copies fields with plain getter/setter calls — no reflection.","Returning specific annotation type names tells `javac` to invoke your processor only when those annotations are present, which is efficient and clear."]},{"id":"java/annotation_processing#5dc7a23b5ac5","section":"java","module":"java/annotation_processing","moduleName":"annotation processing","sourceFile":"README.md","difficulty":"advanced","question":"How can a processor break incremental compilation, and how do you avoid it?","answerFull":"If a processor is non-deterministic, reads inputs the build system cannot track, or regenerates outputs unpredictably, the build tool must fall back to full recompilation and may produce inconsistent results. To avoid it, make the processor deterministic (same inputs → same outputs), declare which inputs it depends on, and mark it incremental for the build tool (Gradle's org.gradle.annotation.processing metadata classifies a processor as isolating or aggregating). Established processors do this; hand-rolled ones often do not.","correct":"If a processor is non-deterministic, reads inputs the build system cannot track, or regenerates outputs unpredictably, the build tool must fall back to full recompilation and may produce inconsistent results.","distractors":["Because native image uses a closed-world assumption and cannot follow runtime reflection without explicit metadata.","The Filer is the API a processor uses to create new source, class, or resource files — the sanctioned output channel.","Returning specific annotation type names tells javac to invoke your processor only when those annotations are present, which is efficient and clear."],"distractorIds":["java/annotation_processing#80520b2dee3a","java/annotation_processing#2786c55c47b0","java/annotation_processing#7e607c37e8a7"],"concepts":["incremental","recompilation","regenerates","inconsistent","unpredictably","compilation"],"answerFullMd":"If a processor is non-deterministic, reads inputs the build system cannot track, or regenerates outputs unpredictably, the build tool must fall back to full recompilation and may produce inconsistent results. To avoid it, make the processor deterministic (same inputs → same outputs), declare which inputs it depends on, and mark it incremental for the build tool (Gradle's `org.gradle.annotation.processing` metadata classifies a processor as isolating or aggregating). Established processors do this; hand-rolled ones often do not.","distractorsMd":[null,"The `Filer` is the API a processor uses to create new source, class, or resource files — the sanctioned output channel.","Returning specific annotation type names tells `javac` to invoke your processor only when those annotations are present, which is efficient and clear."]},{"id":"java/annotation_processing#7e607c37e8a7","section":"java","module":"java/annotation_processing","moduleName":"annotation processing","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between getSupportedAnnotationTypes/@SupportedAnnotationTypes returning a specific set vs \"*\"?","answerFull":"Returning specific annotation type names tells javac to invoke your processor only when those annotations are present, which is efficient and clear. Returning \"*\" makes the processor claim *all* annotations, so it runs on every compilation regardless — useful for processors that inspect everything (e.g. validators) but wasteful and error-prone otherwise. Also note the boolean return of process: returning true *claims* the annotations so no other processor handles them, while false lets them pass on — usually you return true only for annotations you own.","correct":"Returning specific annotation type names tells javac to invoke your processor only when those annotations are present, which is efficient and clear.","distractors":["javac invokes processors in rounds.","Because during annotation processing the bytecode does not exist yet — there is no loaded Class object for a type that is still being compiled.","The Filer is the API a processor uses to create new source, class, or resource files — the sanctioned output channel."],"distractorIds":["java/annotation_processing#66351741f14c","java/annotation_processing#01db29b1ee2b","java/annotation_processing#2786c55c47b0"],"concepts":["javac","efficient","invoke","difference","clear","processor"],"questionMd":"What is the difference between `getSupportedAnnotationTypes`/`@SupportedAnnotationTypes` returning a specific set vs `\"*\"`?","correctMd":"Returning specific annotation type names tells `javac` to invoke your processor only when those annotations are present, which is efficient and clear.","answerFullMd":"Returning specific annotation type names tells `javac` to invoke your processor only when those annotations are present, which is efficient and clear. Returning `\"*\"` makes the processor claim *all* annotations, so it runs on every compilation regardless — useful for processors that inspect everything (e.g. validators) but wasteful and error-prone otherwise. Also note the boolean return of `process`: returning `true` *claims* the annotations so no other processor handles them, while `false` lets them pass on — usually you return `true` only for annotations you own.","distractorsMd":["`javac` invokes processors in rounds.","Because during annotation processing the bytecode does not exist yet — there is no loaded `Class` object for a type that is still being compiled.","The `Filer` is the API a processor uses to create new source, class, or resource files — the sanctioned output channel."]},{"id":"java/annotation_processing#b5409c221554","section":"java","module":"java/annotation_processing","moduleName":"annotation processing","sourceFile":"README.md","difficulty":"advanced","question":"Can annotation processing modify existing methods or classes within the spec?","answerFull":"No — within JSR 269 you can only *add* new files, not modify or delete existing source/bytecode. The spec is intentionally additive to keep compilation predictable. Lombok modifies existing classes only by going outside the spec into compiler internals. If you need to alter existing bytecode, the right tools are bytecode manipulation libraries (ASM, ByteBuddy) at build or load time, not annotation processing.","correct":"No — within JSR 269 you can only *add* new files, not modify or delete existing source/bytecode.","distractors":["Returning specific annotation type names tells javac to invoke your processor only when those annotations are present, which is efficient and clear.","The Filer is the API a processor uses to create new source, class, or resource files — the sanctioned output channel.","Through the Messager, by calling processingEnv.getMessager().printMessage( Diagnostic.Kind.ERROR, \"message\", element), passing the offending Element so the error points at the right source location."],"distractorIds":["java/annotation_processing#7e607c37e8a7","java/annotation_processing#2786c55c47b0","java/annotation_processing#62a6d2cdc770"],"concepts":["269","jsr","modify","bytecode","annotation","spec"],"distractorsMd":["Returning specific annotation type names tells `javac` to invoke your processor only when those annotations are present, which is efficient and clear.","The `Filer` is the API a processor uses to create new source, class, or resource files — the sanctioned output channel.","Through the `Messager`, by calling `processingEnv.getMessager().printMessage( Diagnostic.Kind.ERROR, \"message\", element)`, passing the offending `Element` so the error points at the right source location."]},{"id":"java/bytecode_and_classfile#ad4a9e255588","section":"java","module":"java/bytecode_and_classfile","moduleName":"bytecode and classfile","sourceFile":"README.md","difficulty":"core","question":"You edited a method's bytecode with ASM and now the class throws VerifyError at load. What happened and how do you fix it?","answerFull":"You changed the control flow (added a branch) without updating the StackMapTable, so the verifier's recorded frames no longer match the actual stack/local types at branch targets. Since Java 7 (major 51) the verifier uses type-checking driven by StackMapTable, and any mismatch is a hard VerifyError at link time. The fix is to let ASM regenerate frames: construct the writer with ClassWriter.COMPUTE_FRAMES and read with ClassReader.EXPAND_FRAMES. Byte Buddy computes frames automatically, which is why it's preferred for non-trivial transforms.","correct":"You changed the control flow (added a branch) without updating the StackMapTable, so the verifier's recorded frames no longer match the actual stack/local types at branch targets.","distractors":["Run javap -c -v -p ClassName from the JDK — -c disassembles the bytecode, -v dumps the constant pool, versions, and attributes (StackMapTable, BootstrapMethods), and -p includes private members.","A MethodHandle is a typed, directly-executable reference to a method/field/constructor from java.lang.invoke, resolved once and then invoked at near-native speed via invokeExact.","It cannot add or remove methods or fields, change method signatures or modifiers, or alter the class hierarchy/interfaces — only method bodies (and constant pool as needed) may change."],"distractorIds":["java/bytecode_and_classfile#df72e1f0b8dd","java/bytecode_and_classfile#d35e086ef498","java/bytecode_and_classfile#42044cdb5bef"],"concepts":["stackmaptable","recorded","verifier","happened","updating","fix"],"questionMd":"You edited a method's bytecode with ASM and now the class throws `VerifyError` at load. What happened and how do you fix it?","correctMd":"You changed the control flow (added a branch) without updating the `StackMapTable`, so the verifier's recorded frames no longer match the actual stack/local types at branch targets.","answerFullMd":"You changed the control flow (added a branch) without updating the `StackMapTable`, so the verifier's recorded frames no longer match the actual stack/local types at branch targets. Since Java 7 (major 51) the verifier uses type-checking driven by `StackMapTable`, and any mismatch is a hard `VerifyError` at link time. The fix is to let ASM regenerate frames: construct the writer with `ClassWriter.COMPUTE_FRAMES` and read with `ClassReader.EXPAND_FRAMES`. Byte Buddy computes frames automatically, which is why it's preferred for non-trivial transforms.","distractorsMd":["Run `javap -c -v -p ClassName` from the JDK — `-c` disassembles the bytecode, `-v` dumps the constant pool, versions, and attributes (`StackMapTable`, `BootstrapMethods`), and `-p` includes private members.","A `MethodHandle` is a typed, directly-executable reference to a method/field/constructor from `java.lang.invoke`, resolved once and then invoked at near-native speed via `invokeExact`.",null]},{"id":"java/bytecode_and_classfile#052c368b8014","section":"java","module":"java/bytecode_and_classfile","moduleName":"bytecode and classfile","sourceFile":"README.md","difficulty":"core","question":"What is the magic number and what does the major version tell you?","answerFull":"The first four bytes of every .class file are 0xCAFEBABE, identifying it as a class file; bytes 6-7 are the major_version. Major 52 = Java 8, 61 = Java 17, 65 = Java 21 (major = Java feature version + 44). If a class's major version exceeds what the running JVM supports, you get UnsupportedClassVersionError at load. This is the concrete cause of \"class compiled by a newer version\" errors — check the target --release.","correct":"The first four bytes of every .class file are 0xCAFEBABE, identifying it as a class file; bytes 6-7 are the major_version.","distractors":["It cannot add or remove methods or fields, change method signatures or modifiers, or alter the class hierarchy/interfaces — only method bodies (and constant pool as needed) may change.","Verification is the JVM proving, before execution, that bytecode is type-safe and the operand stack is consistent at every instruction (JVMS §4.10).","A MethodHandle is a typed, directly-executable reference to a method/field/constructor from java.lang.invoke, resolved once and then invoked at near-native speed via invokeExact."],"distractorIds":["java/bytecode_and_classfile#42044cdb5bef","java/bytecode_and_classfile#26b81d214fdf","java/bytecode_and_classfile#d35e086ef498"],"concepts":["0xcafebabe","magic","identifying","tell","major","four"],"correctMd":"The first four bytes of every `.class` file are `0xCAFEBABE`, identifying it as a class file; bytes 6-7 are the `major_version`.","answerFullMd":"The first four bytes of every `.class` file are `0xCAFEBABE`, identifying it as a class file; bytes 6-7 are the `major_version`. Major 52 = Java 8, 61 = Java 17, 65 = Java 21 (major = Java feature version + 44). If a class's major version exceeds what the running JVM supports, you get `UnsupportedClassVersionError` at load. This is the concrete cause of \"class compiled by a newer version\" errors — check the target `--release`.","distractorsMd":[null,null,"A `MethodHandle` is a typed, directly-executable reference to a method/field/constructor from `java.lang.invoke`, resolved once and then invoked at near-native speed via `invokeExact`."]},{"id":"java/bytecode_and_classfile#d32ea76648b5","section":"java","module":"java/bytecode_and_classfile","moduleName":"bytecode and classfile","sourceFile":"README.md","difficulty":"core","question":"Is the constant pool 0-indexed or 1-indexed, and why does that trip people up?","answerFull":"The constant pool is 1-indexed: valid indices run from 1 to constant_pool_count - 1, and index 0 is reserved as a \"no entry\" sentinel. The trap is that CONSTANT_Long and CONSTANT_Double each occupy two consecutive slots, so the entry after a long is at index+2, not index+1. A naive parser loop that increments by 1 corrupts on the first wide constant — the single most common class-file parsing bug.","correct":"The constant pool is 1-indexed: valid indices run from 1 to constant_pool_count - 1, and index 0 is reserved as a \"no entry\" sentinel.","distractors":["Run javap -c -v -p ClassName from the JDK — -c disassembles the bytecode, -v dumps the constant pool, versions, and attributes (StackMapTable, BootstrapMethods), and -p includes private members.","It cannot add or remove methods or fields, change method signatures or modifiers, or alter the class hierarchy/interfaces — only method bodies (and constant pool as needed) may change.","A MethodHandle is a typed, directly-executable reference to a method/field/constructor from java.lang.invoke, resolved once and then invoked at near-native speed via invokeExact."],"distractorIds":["java/bytecode_and_classfile#df72e1f0b8dd","java/bytecode_and_classfile#42044cdb5bef","java/bytecode_and_classfile#d35e086ef498"],"concepts":["sentinel","reserved","people","trip","indices","indexed"],"correctMd":"The constant pool is 1-indexed: valid indices run from 1 to `constant_pool_count - 1`, and index 0 is reserved as a \"no entry\" sentinel.","answerFullMd":"The constant pool is 1-indexed: valid indices run from 1 to `constant_pool_count - 1`, and index 0 is reserved as a \"no entry\" sentinel. The trap is that `CONSTANT_Long` and `CONSTANT_Double` each occupy **two** consecutive slots, so the entry after a `long` is at index+2, not index+1. A naive parser loop that increments by 1 corrupts on the first wide constant — the single most common class-file parsing bug.","distractorsMd":["Run `javap -c -v -p ClassName` from the JDK — `-c` disassembles the bytecode, `-v` dumps the constant pool, versions, and attributes (`StackMapTable`, `BootstrapMethods`), and `-p` includes private members.",null,"A `MethodHandle` is a typed, directly-executable reference to a method/field/constructor from `java.lang.invoke`, resolved once and then invoked at near-native speed via `invokeExact`."]},{"id":"java/bytecode_and_classfile#2af2bb1c7fe6","section":"java","module":"java/bytecode_and_classfile","moduleName":"bytecode and classfile","sourceFile":"README.md","difficulty":"core","question":"How does a Java lambda compile — is it an anonymous inner class?","answerFull":"No — a lambda compiles to an invokedynamic instruction plus a synthetic private method holding the body, not an anonymous class file. At first execution, the bootstrap method LambdaMetafactory.metafactory generates a hidden class implementing the functional interface and returns a CallSite; later calls reuse the cached target. This means no extra Foo$1.class is emitted, a stateless lambda is instantiated once, and linkage is deferred to runtime rather than fixed at compile time.","correct":"No — a lambda compiles to an invokedynamic instruction plus a synthetic private method holding the body, not an anonymous class file.","distractors":["invokedynamic is a call site whose target method is chosen at runtime by a user-specified bootstrap method that returns a CallSite, rather than being fixed at compile time.","invokespecial performs non-virtual dispatch to an exactly-named method: constructors (<init>), explicit super.method() calls, and private instance methods.","Since Java 9 (JEP 280), a + b + c compiles to a single invokedynamic makeConcatWithConstants bootstrapped by StringConcatFactory, which builds the optimal concat strategy at runtime."],"distractorIds":["java/bytecode_and_classfile#cf39700392ba","java/bytecode_and_classfile#e85f2ed4dbb4","java/bytecode_and_classfile#9ea48ec21320"],"concepts":["invokedynamic","anonymous","compiles","holding","synthetic","instruction"],"correctMd":"No — a lambda compiles to an `invokedynamic` instruction plus a synthetic private method holding the body, not an anonymous class file.","answerFullMd":"No — a lambda compiles to an `invokedynamic` instruction plus a synthetic private method holding the body, not an anonymous class file. At first execution, the bootstrap method `LambdaMetafactory.metafactory` generates a hidden class implementing the functional interface and returns a `CallSite`; later calls reuse the cached target. This means no extra `Foo$1.class` is emitted, a stateless lambda is instantiated once, and linkage is deferred to runtime rather than fixed at compile time.","distractorsMd":["`invokedynamic` is a call site whose target method is chosen at runtime by a user-specified bootstrap method that returns a `CallSite`, rather than being fixed at compile time.","`invokespecial` performs non-virtual dispatch to an exactly-named method: constructors (`<init>`), explicit `super.method()` calls, and `private` instance methods.","Since Java 9 (JEP 280), `a + b + c` compiles to a single `invokedynamic makeConcatWithConstants` bootstrapped by `StringConcatFactory`, which builds the optimal concat strategy at runtime."]},{"id":"java/bytecode_and_classfile#9ea48ec21320","section":"java","module":"java/bytecode_and_classfile","moduleName":"bytecode and classfile","sourceFile":"README.md","difficulty":"intermediate","question":"How does string concatenation compile in modern Java, and how did it change?","answerFull":"Since Java 9 (JEP 280), a + b + c compiles to a single invokedynamic makeConcatWithConstants bootstrapped by StringConcatFactory, which builds the optimal concat strategy at runtime. Before Java 9, javac emitted an explicit new StringBuilder().append(...).append(...).toString() chain. The indy form lets the JDK change concatenation internals without recompiling your code and often produces less garbage. The gotcha: + inside a hot loop still allocates per iteration — use an explicit StringBuilder there.","correct":"Since Java 9 (JEP 280), a + b + c compiles to a single invokedynamic makeConcatWithConstants bootstrapped by StringConcatFactory, which builds the optimal concat strategy at runtime.","distractors":["A MethodHandle is a typed, directly-executable reference to a method/field/constructor from java.lang.invoke, resolved once and then invoked at near-native speed via invokeExact.","invokedynamic is a call site whose target method is chosen at runtime by a user-specified bootstrap method that returns a CallSite, rather than being fixed at compile time.","Mockito's default mock-maker uses Byte Buddy to generate a runtime subclass of the target type whose methods are overridden to call Mockito's stubbing interceptor."],"distractorIds":["java/bytecode_and_classfile#d35e086ef498","java/bytecode_and_classfile#cf39700392ba","java/bytecode_and_classfile#f36448e066f5"],"concepts":["bootstrapped","concat","makeconcatwithconstants","stringconcatfactory","280","concatenation"],"correctMd":"Since Java 9 (JEP 280), `a + b + c` compiles to a single `invokedynamic makeConcatWithConstants` bootstrapped by `StringConcatFactory`, which builds the optimal concat strategy at runtime.","answerFullMd":"Since Java 9 (JEP 280), `a + b + c` compiles to a single `invokedynamic makeConcatWithConstants` bootstrapped by `StringConcatFactory`, which builds the optimal concat strategy at runtime. Before Java 9, `javac` emitted an explicit `new StringBuilder().append(...).append(...).toString()` chain. The indy form lets the JDK change concatenation internals without recompiling your code and often produces less garbage. The gotcha: `+` inside a hot loop still allocates per iteration — use an explicit `StringBuilder` there.","distractorsMd":["A `MethodHandle` is a typed, directly-executable reference to a method/field/constructor from `java.lang.invoke`, resolved once and then invoked at near-native speed via `invokeExact`.","`invokedynamic` is a call site whose target method is chosen at runtime by a user-specified bootstrap method that returns a `CallSite`, rather than being fixed at compile time.","Mockito's default mock-maker uses Byte Buddy to generate a runtime **subclass** of the target type whose methods are overridden to call Mockito's stubbing interceptor."]},{"id":"java/bytecode_and_classfile#cf39700392ba","section":"java","module":"java/bytecode_and_classfile","moduleName":"bytecode and classfile","sourceFile":"README.md","difficulty":"intermediate","question":"What is invokedynamic and what problem does it solve?","answerFull":"invokedynamic is a call site whose target method is chosen at runtime by a user-specified bootstrap method that returns a CallSite, rather than being fixed at compile time. It lets the JDK and JVM languages defer and customize method linkage — the first call runs the bootstrap and caches the resulting MethodHandle; later calls are direct and fast. It was added in Java 7 for dynamic languages and is now the backbone of lambdas (LambdaMetafactory), string concat (StringConcatFactory), and record equals/hashCode (ObjectMethods).","correct":"invokedynamic is a call site whose target method is chosen at runtime by a user-specified bootstrap method that returns a CallSite, rather than being fixed at compile time.","distractors":["invokespecial performs non-virtual dispatch to an exactly-named method: constructors (<init>), explicit super.method() calls, and private instance methods.","Mockito's default mock-maker uses Byte Buddy to generate a runtime subclass of the target type whose methods are overridden to call Mockito's stubbing interceptor.","A MethodHandle is a typed, directly-executable reference to a method/field/constructor from java.lang.invoke, resolved once and then invoked at near-native speed via invokeExact."],"distractorIds":["java/bytecode_and_classfile#e85f2ed4dbb4","java/bytecode_and_classfile#f36448e066f5","java/bytecode_and_classfile#d35e086ef498"],"concepts":["callsite","invokedynamic","site","solve","chosen","bootstrap"],"questionMd":"What is `invokedynamic` and what problem does it solve?","correctMd":"`invokedynamic` is a call site whose target method is chosen at runtime by a user-specified bootstrap method that returns a `CallSite`, rather than being fixed at compile time.","answerFullMd":"`invokedynamic` is a call site whose target method is chosen at runtime by a user-specified bootstrap method that returns a `CallSite`, rather than being fixed at compile time. It lets the JDK and JVM languages defer and customize method linkage — the first call runs the bootstrap and caches the resulting `MethodHandle`; later calls are direct and fast. It was added in Java 7 for dynamic languages and is now the backbone of lambdas (`LambdaMetafactory`), string concat (`StringConcatFactory`), and record `equals`/`hashCode` (`ObjectMethods`).","distractorsMd":["`invokespecial` performs non-virtual dispatch to an exactly-named method: constructors (`<init>`), explicit `super.method()` calls, and `private` instance methods.","Mockito's default mock-maker uses Byte Buddy to generate a runtime **subclass** of the target type whose methods are overridden to call Mockito's stubbing interceptor.","A `MethodHandle` is a typed, directly-executable reference to a method/field/constructor from `java.lang.invoke`, resolved once and then invoked at near-native speed via `invokeExact`."]},{"id":"java/bytecode_and_classfile#42044cdb5bef","section":"java","module":"java/bytecode_and_classfile","moduleName":"bytecode and classfile","sourceFile":"README.md","difficulty":"intermediate","question":"What can retransformClasses NOT change about a loaded class?","answerFull":"It cannot add or remove methods or fields, change method signatures or modifiers, or alter the class hierarchy/interfaces — only method bodies (and constant pool as needed) may change. This \"schema-preserving\" constraint exists because live instances, JIT-compiled frames, and vtables assume the old shape. If you need extra per-invocation state, keep it on the operand stack or in a thread-local inside your advice, never by adding a field via retransform (which throws UnsupportedOperationException).","correct":"It cannot add or remove methods or fields, change method signatures or modifiers, or alter the class hierarchy/interfaces — only method bodies (and constant pool as needed) may change.","distractors":["invokedynamic is a call site whose target method is chosen at runtime by a user-specified bootstrap method that returns a CallSite, rather than being fixed at compile time.","Mockito's default mock-maker uses Byte Buddy to generate a runtime subclass of the target type whose methods are overridden to call Mockito's stubbing interceptor.","Run javap -c -v -p ClassName from the JDK — -c disassembles the bytecode, -v dumps the constant pool, versions, and attributes (StackMapTable, BootstrapMethods), and -p includes private members."],"distractorIds":["java/bytecode_and_classfile#cf39700392ba","java/bytecode_and_classfile#f36448e066f5","java/bytecode_and_classfile#df72e1f0b8dd"],"concepts":["modifiers","bodies","alter","signatures","remove","interfaces"],"questionMd":"What can `retransformClasses` NOT change about a loaded class?","answerFullMd":"It cannot add or remove methods or fields, change method signatures or modifiers, or alter the class hierarchy/interfaces — only method bodies (and constant pool as needed) may change. This \"schema-preserving\" constraint exists because live instances, JIT-compiled frames, and vtables assume the old shape. If you need extra per-invocation state, keep it on the operand stack or in a thread-local inside your advice, never by adding a field via retransform (which throws `UnsupportedOperationException`).","distractorsMd":["`invokedynamic` is a call site whose target method is chosen at runtime by a user-specified bootstrap method that returns a `CallSite`, rather than being fixed at compile time.","Mockito's default mock-maker uses Byte Buddy to generate a runtime **subclass** of the target type whose methods are overridden to call Mockito's stubbing interceptor.","Run `javap -c -v -p ClassName` from the JDK — `-c` disassembles the bytecode, `-v` dumps the constant pool, versions, and attributes (`StackMapTable`, `BootstrapMethods`), and `-p` includes private members."]},{"id":"java/bytecode_and_classfile#26b81d214fdf","section":"java","module":"java/bytecode_and_classfile","moduleName":"bytecode and classfile","sourceFile":"README.md","difficulty":"advanced","question":"What is bytecode verification and what role does the StackMapTable play?","answerFull":"Verification is the JVM proving, before execution, that bytecode is type-safe and the operand stack is consistent at every instruction (JVMS §4.10). Since Java 7 it uses type-checking rather than the old data-flow inference, driven by the StackMapTable attribute that javac emits recording the stack/local types at each branch target. This turns verification into a fast linear pass — but it means any tool that changes control flow must regenerate the frames or the class fails to load with VerifyError.","correct":"Verification is the JVM proving, before execution, that bytecode is type-safe and the operand stack is consistent at every instruction (JVMS §4.10).","distractors":["Run javap -c -v -p ClassName from the JDK — -c disassembles the bytecode, -v dumps the constant pool, versions, and attributes (StackMapTable, BootstrapMethods), and -p includes private members.","The first four bytes of every .class file are 0xCAFEBABE, identifying it as a class file; bytes 6-7 are the major_version.","You changed the control flow (added a branch) without updating the StackMapTable, so the verifier's recorded frames no longer match the actual stack/local types at branch targets."],"distractorIds":["java/bytecode_and_classfile#df72e1f0b8dd","java/bytecode_and_classfile#052c368b8014","java/bytecode_and_classfile#ad4a9e255588"],"concepts":["proving","jvms","operand","stackmaptable","verification","role"],"questionMd":"What is bytecode verification and what role does the `StackMapTable` play?","answerFullMd":"Verification is the JVM proving, before execution, that bytecode is type-safe and the operand stack is consistent at every instruction (JVMS §4.10). Since Java 7 it uses type-checking rather than the old data-flow inference, driven by the `StackMapTable` attribute that `javac` emits recording the stack/local types at each branch target. This turns verification into a fast linear pass — but it means any tool that changes control flow must regenerate the frames or the class fails to load with `VerifyError`.","distractorsMd":["Run `javap -c -v -p ClassName` from the JDK — `-c` disassembles the bytecode, `-v` dumps the constant pool, versions, and attributes (`StackMapTable`, `BootstrapMethods`), and `-p` includes private members.","The first four bytes of every `.class` file are `0xCAFEBABE`, identifying it as a class file; bytes 6-7 are the `major_version`.","You changed the control flow (added a branch) without updating the `StackMapTable`, so the verifier's recorded frames no longer match the actual stack/local types at branch targets."]},{"id":"java/bytecode_and_classfile#f36448e066f5","section":"java","module":"java/bytecode_and_classfile","moduleName":"bytecode and classfile","sourceFile":"README.md","difficulty":"advanced","question":"How does Mockito create a mock, and why can't it (by default) mock a final class?","answerFull":"Mockito's default mock-maker uses Byte Buddy to generate a runtime subclass of the target type whose methods are overridden to call Mockito's stubbing interceptor. Because it relies on subclassing/overriding, a final class or final method can't be intercepted this way — there's nothing to override. The workaround is the inline mock maker (mockito-inline), which uses a Java agent + retransformClasses to rewrite the *original* class's method bodies in place, enabling mocking of finals and statics at the cost of agent overhead.","correct":"Mockito's default mock-maker uses Byte Buddy to generate a runtime subclass of the target type whose methods are overridden to call Mockito's stubbing interceptor.","distractors":["Since Java 9 (JEP 280), a + b + c compiles to a single invokedynamic makeConcatWithConstants bootstrapped by StringConcatFactory, which builds the optimal concat strategy at runtime.","invokespecial performs non-virtual dispatch to an exactly-named method: constructors (<init>), explicit super.method() calls, and private instance methods.","The first four bytes of every .class file are 0xCAFEBABE, identifying it as a class file; bytes 6-7 are the major_version."],"distractorIds":["java/bytecode_and_classfile#9ea48ec21320","java/bytecode_and_classfile#e85f2ed4dbb4","java/bytecode_and_classfile#052c368b8014"],"concepts":["stubbing","buddy","maker","overridden","mockito","interceptor"],"questionMd":"How does Mockito create a mock, and why can't it (by default) mock a `final` class?","correctMd":"Mockito's default mock-maker uses Byte Buddy to generate a runtime **subclass** of the target type whose methods are overridden to call Mockito's stubbing interceptor.","answerFullMd":"Mockito's default mock-maker uses Byte Buddy to generate a runtime **subclass** of the target type whose methods are overridden to call Mockito's stubbing interceptor. Because it relies on subclassing/overriding, a `final` class or `final` method can't be intercepted this way — there's nothing to override. The workaround is the **inline mock maker** (`mockito-inline`), which uses a Java agent + `retransformClasses` to rewrite the *original* class's method bodies in place, enabling mocking of finals and statics at the cost of agent overhead.","distractorsMd":["Since Java 9 (JEP 280), `a + b + c` compiles to a single `invokedynamic makeConcatWithConstants` bootstrapped by `StringConcatFactory`, which builds the optimal concat strategy at runtime.","`invokespecial` performs non-virtual dispatch to an exactly-named method: constructors (`<init>`), explicit `super.method()` calls, and `private` instance methods.","The first four bytes of every `.class` file are `0xCAFEBABE`, identifying it as a class file; bytes 6-7 are the `major_version`."]},{"id":"java/bytecode_and_classfile#e85f2ed4dbb4","section":"java","module":"java/bytecode_and_classfile","moduleName":"bytecode and classfile","sourceFile":"README.md","difficulty":"advanced","question":"What is invokespecial used for besides constructors?","answerFull":"invokespecial performs non-virtual dispatch to an exactly-named method: constructors (<init>), explicit super.method() calls, and private instance methods. The point is to bypass override lookup — super.toString() must call the parent's version, not re-dispatch to the current object's override. (Historically private-method calls used invokespecial; on interfaces with private methods and in some Java 11+ cases nestmate access refined this, but the non-virtual semantics remain.)","correct":"invokespecial performs non-virtual dispatch to an exactly-named method: constructors (<init>), explicit super.method() calls, and private instance methods.","distractors":["invokedynamic is a call site whose target method is chosen at runtime by a user-specified bootstrap method that returns a CallSite, rather than being fixed at compile time.","It cannot add or remove methods or fields, change method signatures or modifiers, or alter the class hierarchy/interfaces — only method bodies (and constant pool as needed) may change.","Run javap -c -v -p ClassName from the JDK — -c disassembles the bytecode, -v dumps the constant pool, versions, and attributes (StackMapTable, BootstrapMethods), and -p includes private members."],"distractorIds":["java/bytecode_and_classfile#cf39700392ba","java/bytecode_and_classfile#42044cdb5bef","java/bytecode_and_classfile#df72e1f0b8dd"],"concepts":["invokespecial","constructors","super","init","dispatch","performs"],"questionMd":"What is `invokespecial` used for besides constructors?","correctMd":"`invokespecial` performs non-virtual dispatch to an exactly-named method: constructors (`<init>`), explicit `super.method()` calls, and `private` instance methods.","answerFullMd":"`invokespecial` performs non-virtual dispatch to an exactly-named method: constructors (`<init>`), explicit `super.method()` calls, and `private` instance methods. The point is to bypass override lookup — `super.toString()` must call the parent's version, not re-dispatch to the current object's override. (Historically private-method calls used `invokespecial`; on interfaces with private methods and in some Java 11+ cases nestmate access refined this, but the non-virtual semantics remain.)","distractorsMd":["`invokedynamic` is a call site whose target method is chosen at runtime by a user-specified bootstrap method that returns a `CallSite`, rather than being fixed at compile time.",null,"Run `javap -c -v -p ClassName` from the JDK — `-c` disassembles the bytecode, `-v` dumps the constant pool, versions, and attributes (`StackMapTable`, `BootstrapMethods`), and `-p` includes private members."]},{"id":"java/bytecode_and_classfile#d35e086ef498","section":"java","module":"java/bytecode_and_classfile","moduleName":"bytecode and classfile","sourceFile":"README.md","difficulty":"advanced","question":"What is a MethodHandle and how does it differ from reflection?","answerFull":"A MethodHandle is a typed, directly-executable reference to a method/field/constructor from java.lang.invoke, resolved once and then invoked at near-native speed via invokeExact. Unlike java.lang.reflect.Method, access checks happen at lookup time (not every call), it's JIT-friendly (the JIT can inline through it), and it composes (bind arguments, adapt types, combine handles). It's the runtime plumbing behind invokedynamic — LambdaMetafactory hands back a CallSite wrapping a MethodHandle to the lambda body.","correct":"A MethodHandle is a typed, directly-executable reference to a method/field/constructor from java.lang.invoke, resolved once and then invoked at near-native speed via invokeExact.","distractors":["invokespecial performs non-virtual dispatch to an exactly-named method: constructors (<init>), explicit super.method() calls, and private instance methods.","invokedynamic is a call site whose target method is chosen at runtime by a user-specified bootstrap method that returns a CallSite, rather than being fixed at compile time.","It cannot add or remove methods or fields, change method signatures or modifiers, or alter the class hierarchy/interfaces — only method bodies (and constant pool as needed) may change."],"distractorIds":["java/bytecode_and_classfile#e85f2ed4dbb4","java/bytecode_and_classfile#cf39700392ba","java/bytecode_and_classfile#42044cdb5bef"],"concepts":["invokeexact","methodhandle","resolved","executable","invoked","invoke"],"questionMd":"What is a `MethodHandle` and how does it differ from reflection?","correctMd":"A `MethodHandle` is a typed, directly-executable reference to a method/field/constructor from `java.lang.invoke`, resolved once and then invoked at near-native speed via `invokeExact`.","answerFullMd":"A `MethodHandle` is a typed, directly-executable reference to a method/field/constructor from `java.lang.invoke`, resolved once and then invoked at near-native speed via `invokeExact`. Unlike `java.lang.reflect.Method`, access checks happen at lookup time (not every call), it's JIT-friendly (the JIT can inline through it), and it composes (bind arguments, adapt types, combine handles). It's the runtime plumbing behind `invokedynamic` — `LambdaMetafactory` hands back a `CallSite` wrapping a `MethodHandle` to the lambda body.","distractorsMd":["`invokespecial` performs non-virtual dispatch to an exactly-named method: constructors (`<init>`), explicit `super.method()` calls, and `private` instance methods.","`invokedynamic` is a call site whose target method is chosen at runtime by a user-specified bootstrap method that returns a `CallSite`, rather than being fixed at compile time.",null]},{"id":"java/bytecode_and_classfile#df72e1f0b8dd","section":"java","module":"java/bytecode_and_classfile","moduleName":"bytecode and classfile","sourceFile":"README.md","difficulty":"advanced","question":"How can you read a class file's bytecode without any third-party library?","answerFull":"Run javap -c -v -p ClassName from the JDK — -c disassembles the bytecode, -v dumps the constant pool, versions, and attributes (StackMapTable, BootstrapMethods), and -p includes private members. For programmatic access without external deps you can parse the format by hand against JVMS chapter 4, or (Java 22+ preview) use the java.lang.classfile API (ClassFile.parse). javap is the fastest way to answer \"what did the compiler actually emit?\" for lambdas, records, and switch expressions.","correct":"Run javap -c -v -p ClassName from the JDK — -c disassembles the bytecode, -v dumps the constant pool, versions, and attributes (StackMapTable, BootstrapMethods), and -p includes private members.","distractors":["The constant pool is 1-indexed: valid indices run from 1 to constant_pool_count - 1, and index 0 is reserved as a \"no entry\" sentinel.","invokespecial performs non-virtual dispatch to an exactly-named method: constructors (<init>), explicit super.method() calls, and private instance methods.","The first four bytes of every .class file are 0xCAFEBABE, identifying it as a class file; bytes 6-7 are the major_version."],"distractorIds":["java/bytecode_and_classfile#d32ea76648b5","java/bytecode_and_classfile#e85f2ed4dbb4","java/bytecode_and_classfile#052c368b8014"],"concepts":["bootstrapmethods","disassembles","javap","classname","dumps","stackmaptable"],"correctMd":"Run `javap -c -v -p ClassName` from the JDK — `-c` disassembles the bytecode, `-v` dumps the constant pool, versions, and attributes (`StackMapTable`, `BootstrapMethods`), and `-p` includes private members.","answerFullMd":"Run `javap -c -v -p ClassName` from the JDK — `-c` disassembles the bytecode, `-v` dumps the constant pool, versions, and attributes (`StackMapTable`, `BootstrapMethods`), and `-p` includes private members. For programmatic access without external deps you can parse the format by hand against JVMS chapter 4, or (Java 22+ preview) use the `java.lang.classfile` API (`ClassFile.parse`). `javap` is the fastest way to answer \"what did the compiler actually emit?\" for lambdas, records, and switch expressions.","distractorsMd":["The constant pool is 1-indexed: valid indices run from 1 to `constant_pool_count - 1`, and index 0 is reserved as a \"no entry\" sentinel.","`invokespecial` performs non-virtual dispatch to an exactly-named method: constructors (`<init>`), explicit `super.method()` calls, and `private` instance methods.","The first four bytes of every `.class` file are `0xCAFEBABE`, identifying it as a class file; bytes 6-7 are the `major_version`."]},{"id":"java/collections_internals#cf7114091caf","section":"java","module":"java/collections_internals","moduleName":"collections internals","sourceFile":"README.md","difficulty":"core","question":"How does HashMap handle collisions internally?","answerFull":"When two keys hash to the same bucket, HashMap stores them as a linked list (chain) within that bucket. Each node has (key, value, hash, next). On get(key), it computes the bucket index, then traverses the chain comparing hash and key.equals(). In Java 8+, when a bucket's linked list exceeds 8 entries, it converts to a red-black tree (treeification), reducing worst-case lookup from O(n) to O(log n).","correct":"When two keys hash to the same bucket, HashMap stores them as a linked list (chain) within that bucket.","distractors":["In accessOrder=true mode, every get() moves the accessed entry to the tail of a doubly-linked list overlay (in addition to its bucket).","HashMap has no synchronization.","HashMap: O(1) average get/put/remove via hash table; unordered."],"distractorIds":["java/collections_internals#4c2b89c6daac","java/collections_internals#316887400baf","java/collections_internals#ed972d0277a9"],"concepts":["collisions","internally","linked","hashmap","bucket","handle"],"answerFullMd":"When two keys hash to the same bucket, HashMap stores them as a linked list (chain) within that bucket. Each node has `(key, value, hash, next)`. On `get(key)`, it computes the bucket index, then traverses the chain comparing `hash` and `key.equals()`. In Java 8+, when a bucket's linked list exceeds 8 entries, it converts to a red-black tree (treeification), reducing worst-case lookup from O(n) to O(log n).","distractorsMd":["In `accessOrder=true` mode, every `get()` moves the accessed entry to the tail of a doubly-linked list overlay (in addition to its bucket).",null,null]},{"id":"java/collections_internals#b41f1c3c2619","section":"java","module":"java/collections_internals","moduleName":"collections internals","sourceFile":"README.md","difficulty":"core","question":"What is treeification and when does it happen?","answerFull":"Treeification converts a HashMap bucket's linked list into a red-black tree. Triggered when a single bucket's chain length exceeds TREEIFY_THRESHOLD = 8 AND the table capacity is at least MIN_TREEIFY_CAPACITY = 64 (otherwise resize instead). The tree reverts to a linked list when size drops below UNTREEIFY_THRESHOLD = 6. TreeNodes are larger than regular Nodes (more fields for tree pointers), so only high-collision buckets pay the overhead.","correct":"Treeification converts a HashMap bucket's linked list into a red-black tree.","distractors":["HashMap: O(1) average get/put/remove via hash table; unordered.","When two keys hash to the same bucket, HashMap stores them as a linked list (chain) within that bucket.","List.of() (Java 9): creates an immutable list backed by a compact internal structure (array for small sizes, no ArrayList overhead)."],"distractorIds":["java/collections_internals#ed972d0277a9","java/collections_internals#cf7114091caf","java/collections_internals#fe0420cbd69e"],"concepts":["treeification","happen","converts","black","linked","red"],"answerFullMd":"Treeification converts a HashMap bucket's linked list into a red-black tree. Triggered when a single bucket's chain length exceeds `TREEIFY_THRESHOLD = 8` AND the table capacity is at least `MIN_TREEIFY_CAPACITY = 64` (otherwise resize instead). The tree reverts to a linked list when size drops below `UNTREEIFY_THRESHOLD = 6`. TreeNodes are larger than regular Nodes (more fields for tree pointers), so only high-collision buckets pay the overhead.","distractorsMd":[null,null,"`List.of()` (Java 9): creates an immutable list backed by a compact internal structure (array for small sizes, no `ArrayList` overhead)."]},{"id":"java/collections_internals#6b923da0d24f","section":"java","module":"java/collections_internals","moduleName":"collections internals","sourceFile":"README.md","difficulty":"core","question":"How does HashMap resize without losing entries?","answerFull":"When size > capacity * loadFactor (default: 0.75), the table doubles in size. Java 8 uses a clever bit-split: for each entry, check (hash & oldCapacity). If 0, the entry stays in the same bucket index. If 1, the entry moves to oldIndex + oldCapacity. This requires only a bit check per entry (no re-hashing) and naturally splits the chain into two groups placed in the two new buckets.","correct":"When size > capacity * loadFactor (default: 0.75), the table doubles in size.","distractors":["HashMap has no synchronization.","HashMap: O(1) average get/put/remove via hash table; unordered.","ArrayList's Spliterator has three characteristics: SIZED (knows exact size via size() in O(1)), SUBSIZED (sub-spliterators after trySplit() also have exact known sizes), and ORDERED (encounter order = insertion order)."],"distractorIds":["java/collections_internals#316887400baf","java/collections_internals#ed972d0277a9","java/collections_internals#5eee8f6daba5"],"concepts":["losing","doubles","loadfactor","resize","hashmap","entries"],"correctMd":"When `size > capacity * loadFactor` (default: 0.75), the table doubles in size.","answerFullMd":"When `size > capacity * loadFactor` (default: 0.75), the table doubles in size. Java 8 uses a clever bit-split: for each entry, check `(hash & oldCapacity)`. If 0, the entry stays in the same bucket index. If 1, the entry moves to `oldIndex + oldCapacity`. This requires only a bit check per entry (no re-hashing) and naturally splits the chain into two groups placed in the two new buckets.","distractorsMd":[null,null,"`ArrayList`'s `Spliterator` has three characteristics: `SIZED` (knows exact size via `size()` in O(1)), `SUBSIZED` (sub-spliterators after `trySplit()` also have exact known sizes), and `ORDERED` (encounter order = insertion order)."]},{"id":"java/collections_internals#f52aa0c37ef7","section":"java","module":"java/collections_internals","moduleName":"collections internals","sourceFile":"README.md","difficulty":"core","question":"What is the load factor and how does it affect HashMap performance?","answerFull":"Load factor determines when resize triggers: resize when size / capacity > loadFactor. Default 0.75 balances space vs time: too low (e.g., 0.5) wastes memory (frequent resize, sparse table); too high (e.g., 0.9) increases collision probability, degrading O(1) to O(n) in worst case. Increasing load factor reduces memory but increases average chain length. For known-size maps where no resize is desired, use new HashMap<>(initialCapacity, 1.1f) after computing the right capacity.","correct":"Load factor determines when resize triggers: resize when size / capacity > loadFactor.","distractors":["ArrayList's Spliterator has three characteristics: SIZED (knows exact size via size() in O(1)), SUBSIZED (sub-spliterators after trySplit() also have exact known sizes), and ORDERED (encounter order = insertion order).","Treeification converts a HashMap bucket's linked list into a red-black tree.","HashSet<E> is backed by a HashMap<E, Object> where all values are a single shared dummy object (PRESENT = new Object())."],"distractorIds":["java/collections_internals#5eee8f6daba5","java/collections_internals#b41f1c3c2619","java/collections_internals#6bcbf70241f0"],"concepts":["loadfactor","resize","affect","hashmap","determines","capacity"],"correctMd":"Load factor determines when resize triggers: resize when `size / capacity > loadFactor`.","answerFullMd":"Load factor determines when resize triggers: resize when `size / capacity > loadFactor`. Default 0.75 balances space vs time: too low (e.g., 0.5) wastes memory (frequent resize, sparse table); too high (e.g., 0.9) increases collision probability, degrading O(1) to O(n) in worst case. Increasing load factor reduces memory but increases average chain length. For known-size maps where no resize is desired, use `new HashMap<>(initialCapacity, 1.1f)` after computing the right capacity.","distractorsMd":["`ArrayList`'s `Spliterator` has three characteristics: `SIZED` (knows exact size via `size()` in O(1)), `SUBSIZED` (sub-spliterators after `trySplit()` also have exact known sizes), and `ORDERED` (encounter order = insertion order).",null,"`HashSet<E>` is backed by a `HashMap<E, Object>` where all values are a single shared dummy object (`PRESENT = new Object()`)."]},{"id":"java/collections_internals#316887400baf","section":"java","module":"java/collections_internals","moduleName":"collections internals","sourceFile":"README.md","difficulty":"core","question":"Why is HashMap not thread-safe and what can happen under concurrent access?","answerFull":"HashMap has no synchronization. Two concurrent put() calls can cause: (1) Lost update: both threads read size, both increment to same value, one increment is lost. (2) In Java 6/7: concurrent resize creates a cycle in a bucket's linked list → infinite loop in get(). (3) Visibility: one thread's write is not visible to another without happens-before. Use ConcurrentHashMap for any shared HashMap; it uses CAS for empty buckets and per-bucket synchronization for collisions.","correct":"HashMap has no synchronization.","distractors":["When two keys hash to the same bucket, HashMap stores them as a linked list (chain) within that bucket.","Treeification converts a HashMap bucket's linked list into a red-black tree.","HashMap: O(1) average get/put/remove via hash table; unordered."],"distractorIds":["java/collections_internals#cf7114091caf","java/collections_internals#b41f1c3c2619","java/collections_internals#ed972d0277a9"],"concepts":["happen","synchronization","hashmap","safe","concurrent","access"],"answerFullMd":"HashMap has no synchronization. Two concurrent `put()` calls can cause: (1) Lost update: both threads read `size`, both increment to same value, one increment is lost. (2) In Java 6/7: concurrent resize creates a cycle in a bucket's linked list → infinite loop in `get()`. (3) Visibility: one thread's write is not visible to another without happens-before. Use `ConcurrentHashMap` for any shared HashMap; it uses CAS for empty buckets and per-bucket synchronization for collisions."},{"id":"java/collections_internals#4c2b89c6daac","section":"java","module":"java/collections_internals","moduleName":"collections internals","sourceFile":"README.md","difficulty":"intermediate","question":"How does LinkedHashMap implement an LRU cache?","answerFull":"In accessOrder=true mode, every get() moves the accessed entry to the tail of a doubly-linked list overlay (in addition to its bucket). The head is always the least-recently-used entry. Override removeEldestEntry(Map.Entry eldest) to return true when size() > maxSize — LinkedHashMap will automatically remove the head entry (LRU) on each new put(). This gives an O(1) LRU cache with minimal code.","correct":"In accessOrder=true mode, every get() moves the accessed entry to the tail of a doubly-linked list overlay (in addition to its bucket).","distractors":["When two keys hash to the same bucket, HashMap stores them as a linked list (chain) within that bucket.","List.of() (Java 9): creates an immutable list backed by a compact internal structure (array for small sizes, no ArrayList overhead).","Treeification converts a HashMap bucket's linked list into a red-black tree."],"distractorIds":["java/collections_internals#cf7114091caf","java/collections_internals#fe0420cbd69e","java/collections_internals#b41f1c3c2619"],"concepts":["accessorder","linkedhashmap","overlay","doubly","accessed","addition"],"correctMd":"In `accessOrder=true` mode, every `get()` moves the accessed entry to the tail of a doubly-linked list overlay (in addition to its bucket).","answerFullMd":"In `accessOrder=true` mode, every `get()` moves the accessed entry to the tail of a doubly-linked list overlay (in addition to its bucket). The head is always the least-recently-used entry. Override `removeEldestEntry(Map.Entry eldest)` to return `true` when `size() > maxSize` — `LinkedHashMap` will automatically remove the head entry (LRU) on each new `put()`. This gives an O(1) LRU cache with minimal code.","distractorsMd":[null,"`List.of()` (Java 9): creates an immutable list backed by a compact internal structure (array for small sizes, no `ArrayList` overhead).",null]},{"id":"java/collections_internals#ed972d0277a9","section":"java","module":"java/collections_internals","moduleName":"collections internals","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between TreeMap and HashMap?","answerFull":"HashMap: O(1) average get/put/remove via hash table; unordered. TreeMap: O(log n) get/put/remove via red-black tree; keys maintained in sorted order (natural or by Comparator). TreeMap implements NavigableMap with floorKey, ceilingKey, headMap, tailMap, subMap — essential for range queries. Use HashMap for raw performance; TreeMap for sorted traversal or range lookups.","correct":"HashMap: O(1) average get/put/remove via hash table; unordered.","distractors":["Treeification converts a HashMap bucket's linked list into a red-black tree.","HashMap has no synchronization.","TreeMap and TreeSet locate elements *solely* by the comparator/compareTo, never by equals — two keys are \"the same\" iff compare returns 0."],"distractorIds":["java/collections_internals#b41f1c3c2619","java/collections_internals#316887400baf","java/collections_internals#092ec50568fe"],"concepts":["treemap","unordered","difference","remove","put","hashmap"],"answerFullMd":"HashMap: O(1) average get/put/remove via hash table; unordered. TreeMap: O(log n) get/put/remove via red-black tree; keys maintained in sorted order (natural or by Comparator). TreeMap implements `NavigableMap` with `floorKey`, `ceilingKey`, `headMap`, `tailMap`, `subMap` — essential for range queries. Use HashMap for raw performance; TreeMap for sorted traversal or range lookups.","distractorsMd":[null,null,"`TreeMap` and `TreeSet` locate elements *solely* by the comparator/`compareTo`, never by `equals` — two keys are \"the same\" iff `compare` returns 0."]},{"id":"java/collections_internals#09b8650172bc","section":"java","module":"java/collections_internals","moduleName":"collections internals","sourceFile":"README.md","difficulty":"intermediate","question":"Why is ArrayList preferred over LinkedList in most cases?","answerFull":"(1) Random access: ArrayList O(1), LinkedList O(n). (2) Cache locality: ArrayList stores elements contiguously in memory — CPU cache prefetch works well; LinkedList nodes scattered in heap — cache misses on traversal. (3) Memory: LinkedList nodes have 24B overhead each (object header + 2 pointers + value pointer) vs ArrayList's 4B per element reference. Only use LinkedList when you need O(1) insertions at both ends AND never need random access — and even then, ArrayDeque is usually better.","correct":"(1) Random access: ArrayList O(1), LinkedList O(n).","distractors":["ArrayList's Spliterator has three characteristics: SIZED (knows exact size via size() in O(1)), SUBSIZED (sub-spliterators after trySplit() also have exact known sizes), and ORDERED (encounter order = insertion order).","List.of() (Java 9): creates an immutable list backed by a compact internal structure (array for small sizes, no ArrayList overhead).","Collections track structural modifications with modCount."],"distractorIds":["java/collections_internals#5eee8f6daba5","java/collections_internals#fe0420cbd69e","java/collections_internals#ce45592f5c01"],"concepts":["linkedlist","arraylist","preferred","random","access"],"answerFullMd":"(1) Random access: ArrayList O(1), LinkedList O(n). (2) Cache locality: ArrayList stores elements contiguously in memory — CPU cache prefetch works well; LinkedList nodes scattered in heap — cache misses on traversal. (3) Memory: LinkedList nodes have 24B overhead each (object header + 2 pointers + value pointer) vs ArrayList's 4B per element reference. Only use LinkedList when you need O(1) insertions at both ends AND never need random access — and even then, `ArrayDeque` is usually better.","distractorsMd":["`ArrayList`'s `Spliterator` has three characteristics: `SIZED` (knows exact size via `size()` in O(1)), `SUBSIZED` (sub-spliterators after `trySplit()` also have exact known sizes), and `ORDERED` (encounter order = insertion order).","`List.of()` (Java 9): creates an immutable list backed by a compact internal structure (array for small sizes, no `ArrayList` overhead).","Collections track structural modifications with `modCount`."]},{"id":"java/collections_internals#ce45592f5c01","section":"java","module":"java/collections_internals","moduleName":"collections internals","sourceFile":"README.md","difficulty":"intermediate","question":"What is fail-fast iteration and what causes ConcurrentModificationException?","answerFull":"Collections track structural modifications with modCount. The iterator captures modCount at creation (expectedModCount). Before each next(), it checks modCount == expectedModCount. If the collection was structurally modified outside the iterator (add/remove), modCount changed, and ConcurrentModificationException is thrown. Fix: use Iterator.remove() to remove during iteration; use removeIf() (Java 8); or copy the collection before iterating.","correct":"Collections track structural modifications with modCount.","distractors":["(1) Random access: ArrayList O(1), LinkedList O(n).","ArrayList's Spliterator has three characteristics: SIZED (knows exact size via size() in O(1)), SUBSIZED (sub-spliterators after trySplit() also have exact known sizes), and ORDERED (encounter order = insertion order).","Default initial capacity is 16."],"distractorIds":["java/collections_internals#09b8650172bc","java/collections_internals#5eee8f6daba5","java/collections_internals#ca1934bd8b85"],"concepts":["modcount","modifications","structural","collections","track","iteration"],"correctMd":"Collections track structural modifications with `modCount`.","answerFullMd":"Collections track structural modifications with `modCount`. The iterator captures `modCount` at creation (`expectedModCount`). Before each `next()`, it checks `modCount == expectedModCount`. If the collection was structurally modified outside the iterator (add/remove), `modCount` changed, and `ConcurrentModificationException` is thrown. Fix: use `Iterator.remove()` to remove during iteration; use `removeIf()` (Java 8); or copy the collection before iterating.","distractorsMd":[null,"`ArrayList`'s `Spliterator` has three characteristics: `SIZED` (knows exact size via `size()` in O(1)), `SUBSIZED` (sub-spliterators after `trySplit()` also have exact known sizes), and `ORDERED` (encounter order = insertion order).",null]},{"id":"java/collections_internals#6bcbf70241f0","section":"java","module":"java/collections_internals","moduleName":"collections internals","sourceFile":"README.md","difficulty":"intermediate","question":"How does HashSet work internally?","answerFull":"HashSet<E> is backed by a HashMap<E, Object> where all values are a single shared dummy object (PRESENT = new Object()). add(e) calls map.put(e, PRESENT). contains(e) calls map.containsKey(e). remove(e) calls map.remove(e). All O(1) average complexity. LinkedHashSet is backed by LinkedHashMap; TreeSet is backed by TreeMap. They are thin wrappers — all behavior (null handling, load factor, treeification) follows the backing map.","correct":"HashSet<E> is backed by a HashMap<E, Object> where all values are a single shared dummy object (PRESENT = new Object()).","distractors":["Treeification converts a HashMap bucket's linked list into a red-black tree.","List.of() (Java 9): creates an immutable list backed by a compact internal structure (array for small sizes, no ArrayList overhead).","HashMap has no synchronization."],"distractorIds":["java/collections_internals#b41f1c3c2619","java/collections_internals#fe0420cbd69e","java/collections_internals#316887400baf"],"concepts":["hashset","dummy","internally","hashmap","present","backed"],"correctMd":"`HashSet<E>` is backed by a `HashMap<E, Object>` where all values are a single shared dummy object (`PRESENT = new Object()`).","answerFullMd":"`HashSet<E>` is backed by a `HashMap<E, Object>` where all values are a single shared dummy object (`PRESENT = new Object()`). `add(e)` calls `map.put(e, PRESENT)`. `contains(e)` calls `map.containsKey(e)`. `remove(e)` calls `map.remove(e)`. All O(1) average complexity. `LinkedHashSet` is backed by `LinkedHashMap`; `TreeSet` is backed by `TreeMap`. They are thin wrappers — all behavior (null handling, load factor, treeification) follows the backing map.","distractorsMd":[null,"`List.of()` (Java 9): creates an immutable list backed by a compact internal structure (array for small sizes, no `ArrayList` overhead).",null]},{"id":"java/collections_internals#fe0420cbd69e","section":"java","module":"java/collections_internals","moduleName":"collections internals","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between List.of() and new ArrayList()?","answerFull":"List.of() (Java 9): creates an immutable list backed by a compact internal structure (array for small sizes, no ArrayList overhead). Cannot add/set/remove — throws UnsupportedOperationException. Does not allow null elements. new ArrayList<>() creates a mutable list. Use List.of() for constant lists, API return values you don't want modified; use ArrayList when mutation is needed.","correct":"List.of() (Java 9): creates an immutable list backed by a compact internal structure (array for small sizes, no ArrayList overhead).","distractors":["HashSet<E> is backed by a HashMap<E, Object> where all values are a single shared dummy object (PRESENT = new Object()).","In accessOrder=true mode, every get() moves the accessed entry to the tail of a doubly-linked list overlay (in addition to its bucket).","Java 7's ConcurrentHashMap used segment-level locking: the map was divided into ~16 segments, each a separately-locked sub-map, so concurrency was capped at the segment count."],"distractorIds":["java/collections_internals#6bcbf70241f0","java/collections_internals#4c2b89c6daac","java/collections_internals#f46a27187a07"],"concepts":["arraylist","difference","sizes","compact","backed","internal"],"questionMd":"What is the difference between `List.of()` and `new ArrayList()`?","correctMd":"`List.of()` (Java 9): creates an immutable list backed by a compact internal structure (array for small sizes, no `ArrayList` overhead).","answerFullMd":"`List.of()` (Java 9): creates an immutable list backed by a compact internal structure (array for small sizes, no `ArrayList` overhead). Cannot add/set/remove — throws `UnsupportedOperationException`. Does not allow null elements. `new ArrayList<>()` creates a mutable list. Use `List.of()` for constant lists, API return values you don't want modified; use `ArrayList` when mutation is needed.","distractorsMd":["`HashSet<E>` is backed by a `HashMap<E, Object>` where all values are a single shared dummy object (`PRESENT = new Object()`).","In `accessOrder=true` mode, every `get()` moves the accessed entry to the tail of a doubly-linked list overlay (in addition to its bucket).","Java 7's `ConcurrentHashMap` used segment-level locking: the map was divided into ~16 segments, each a separately-locked sub-map, so concurrency was capped at the segment count."]},{"id":"java/collections_internals#f5237bbdf09b","section":"java","module":"java/collections_internals","moduleName":"collections internals","sourceFile":"README.md","difficulty":"advanced","question":"When would you use EnumMap and EnumSet?","answerFull":"EnumMap<K extends Enum, V>: all keys are enum constants → backed by a plain array indexed by ordinal. O(1) all operations with virtually zero overhead; iteration in declaration order. Use whenever the key type is an enum — it's 5–10× faster than HashMap for this use case. EnumSet: bit-vector backed — an EnumSet with up to 64 enums fits in a single long bitmask. contains, add, remove are &, |, & ~ bitmask operations. Fastest possible set for enums; use for permission/flag sets.","correct":"EnumMap<K extends Enum, V>: all keys are enum constants → backed by a plain array indexed by ordinal.","distractors":["HashSet<E> is backed by a HashMap<E, Object> where all values are a single shared dummy object (PRESENT = new Object()).","TreeMap and TreeSet locate elements *solely* by the comparator/compareTo, never by equals — two keys are \"the same\" iff compare returns 0.","List.of() (Java 9): creates an immutable list backed by a compact internal structure (array for small sizes, no ArrayList overhead)."],"distractorIds":["java/collections_internals#6bcbf70241f0","java/collections_internals#092ec50568fe","java/collections_internals#fe0420cbd69e"],"concepts":["enummap","ordinal","constants","indexed","enum","extends"],"correctMd":"`EnumMap<K extends Enum, V>`: all keys are enum constants → backed by a plain array indexed by ordinal.","answerFullMd":"`EnumMap<K extends Enum, V>`: all keys are enum constants → backed by a plain array indexed by ordinal. O(1) all operations with virtually zero overhead; iteration in declaration order. Use whenever the key type is an enum — it's 5–10× faster than `HashMap` for this use case. `EnumSet`: bit-vector backed — an `EnumSet` with up to 64 enums fits in a single `long` bitmask. `contains`, `add`, `remove` are `&`, `|`, `& ~` bitmask operations. Fastest possible set for enums; use for permission/flag sets.","distractorsMd":["`HashSet<E>` is backed by a `HashMap<E, Object>` where all values are a single shared dummy object (`PRESENT = new Object()`).","`TreeMap` and `TreeSet` locate elements *solely* by the comparator/`compareTo`, never by `equals` — two keys are \"the same\" iff `compare` returns 0.","`List.of()` (Java 9): creates an immutable list backed by a compact internal structure (array for small sizes, no `ArrayList` overhead)."]},{"id":"java/collections_internals#ca1934bd8b85","section":"java","module":"java/collections_internals","moduleName":"collections internals","sourceFile":"README.md","difficulty":"advanced","question":"What is the initial capacity of HashMap and why does it matter for performance?","answerFull":"Default initial capacity is 16. If you know the expected number of entries N, set initial capacity to N / loadFactor + 1 (e.g., 100 entries: 100 / 0.75 + 1 ≈ 134, round up to power of 2 = 256). Providing correct initial capacity avoids multiple expensive resize operations. Each resize is O(n): allocate new array, rehash/redistribute all entries. In tight loops inserting many entries, this matters — a HashMap loaded to 10,000 entries from default 16 resizes ~10 times.","correct":"Default initial capacity is 16.","distractors":["HashMap: O(1) average get/put/remove via hash table; unordered.","Load factor determines when resize triggers: resize when size / capacity > loadFactor.","HashMap has no synchronization."],"distractorIds":["java/collections_internals#ed972d0277a9","java/collections_internals#f52aa0c37ef7","java/collections_internals#316887400baf"],"concepts":["matter","initial","hashmap","capacity","performance","default"],"answerFullMd":"Default initial capacity is 16. If you know the expected number of entries N, set initial capacity to `N / loadFactor + 1` (e.g., 100 entries: `100 / 0.75 + 1 ≈ 134`, round up to power of 2 = 256). Providing correct initial capacity avoids multiple expensive resize operations. Each resize is O(n): allocate new array, rehash/redistribute all entries. In tight loops inserting many entries, this matters — a HashMap loaded to 10,000 entries from default 16 resizes ~10 times.","distractorsMd":[null,"Load factor determines when resize triggers: resize when `size / capacity > loadFactor`.",null]},{"id":"java/collections_internals#c376ae8fbd63","section":"java","module":"java/collections_internals","moduleName":"collections internals","sourceFile":"README.md","difficulty":"advanced","question":"Why is 0.75 the default load factor for HashMap?","answerFull":"The 0.75 default is based on Poisson distribution analysis of hash collision probabilities. At load factor α = 0.75, the expected number of entries per bucket approaches 1 — giving O(1) average lookup. The probability of a bucket having 8 or more entries (treeification threshold) is extremely small (~6×10⁻⁸). Too low (e.g., 0.5): more memory wasted (50% empty buckets), more frequent resizes. Too high (e.g., 0.9): more collisions, longer chains, O(n) degradation. The JDK source comment says the 0.75 value is a compromise between time and space that results in approximately 1 comparison per lookup on average with a good hash function.","correct":"The 0.75 default is based on Poisson distribution analysis of hash collision probabilities.","distractors":["Load factor determines when resize triggers: resize when size / capacity > loadFactor.","HashMap: O(1) average get/put/remove via hash table; unordered.","When size > capacity * loadFactor (default: 0.75), the table doubles in size."],"distractorIds":["java/collections_internals#f52aa0c37ef7","java/collections_internals#ed972d0277a9","java/collections_internals#6b923da0d24f"],"concepts":["poisson","collision","analysis","hashmap","probabilities","factor"],"questionMd":"Why is 0.75 the default load factor for `HashMap`?","distractorsMd":["Load factor determines when resize triggers: resize when `size / capacity > loadFactor`.",null,"When `size > capacity * loadFactor` (default: 0.75), the table doubles in size."]},{"id":"java/collections_internals#5eee8f6daba5","section":"java","module":"java/collections_internals","moduleName":"collections internals","sourceFile":"README.md","difficulty":"advanced","question":"What Spliterator characteristics does ArrayList have, and why do they matter for parallel streams?","answerFull":"ArrayList's Spliterator has three characteristics: SIZED (knows exact size via size() in O(1)), SUBSIZED (sub-spliterators after trySplit() also have exact known sizes), and ORDERED (encounter order = insertion order). These matter for parallel streams because: (1) SIZED + SUBSIZED allow the stream framework to split into exactly equal halves at O(1) cost — each half has known size without traversal. This enables efficient work-stealing in ForkJoinPool. (2) Without SIZED, splitting would require traversal to count elements. (3) ORDERED means the final result must preserve encounter order, which adds merge overhead in parallel — sometimes removing ordering (via .unordered()) improves parallel performance.","correct":"ArrayList's Spliterator has three characteristics: SIZED (knows exact size via size() in O(1)), SUBSIZED (sub-spliterators after trySplit() also have exact known sizes), and ORDERED (encounter order = insertion order).","distractors":["List.of() (Java 9): creates an immutable list backed by a compact internal structure (array for small sizes, no ArrayList overhead).","Java 7's ConcurrentHashMap used segment-level locking: the map was divided into ~16 segments, each a separately-locked sub-map, so concurrency was capped at the segment count.","When size > capacity * loadFactor (default: 0.75), the table doubles in size."],"distractorIds":["java/collections_internals#fe0420cbd69e","java/collections_internals#f46a27187a07","java/collections_internals#6b923da0d24f"],"concepts":["characteristics","encounter","spliterators","subsized","trysplit","arraylist"],"questionMd":"What Spliterator characteristics does `ArrayList` have, and why do they matter for parallel streams?","correctMd":"`ArrayList`'s `Spliterator` has three characteristics: `SIZED` (knows exact size via `size()` in O(1)), `SUBSIZED` (sub-spliterators after `trySplit()` also have exact known sizes), and `ORDERED` (encounter order = insertion order).","answerFullMd":"`ArrayList`'s `Spliterator` has three characteristics: `SIZED` (knows exact size via `size()` in O(1)), `SUBSIZED` (sub-spliterators after `trySplit()` also have exact known sizes), and `ORDERED` (encounter order = insertion order). These matter for parallel streams because: (1) `SIZED + SUBSIZED` allow the stream framework to split into exactly equal halves at O(1) cost — each half has known size without traversal. This enables efficient work-stealing in `ForkJoinPool`. (2) Without `SIZED`, splitting would require traversal to count elements. (3) `ORDERED` means the final result must preserve encounter order, which adds merge overhead in parallel — sometimes removing ordering (via `.unordered()`) improves parallel performance.","distractorsMd":["`List.of()` (Java 9): creates an immutable list backed by a compact internal structure (array for small sizes, no `ArrayList` overhead).","Java 7's `ConcurrentHashMap` used segment-level locking: the map was divided into ~16 segments, each a separately-locked sub-map, so concurrency was capped at the segment count.","When `size > capacity * loadFactor` (default: 0.75), the table doubles in size."]},{"id":"java/collections_internals#f46a27187a07","section":"java","module":"java/collections_internals","moduleName":"collections internals","sourceFile":"README.md","difficulty":"advanced","question":"How does ConcurrentHashMap achieve thread safety in Java 8+, and how does it differ from the old segment design?","answerFull":"Java 7's ConcurrentHashMap used segment-level locking: the map was divided into ~16 segments, each a separately-locked sub-map, so concurrency was capped at the segment count. Java 8 rewrote it to lock at the *bucket (bin) level*: writes use a CAS to install the first node in an empty bin (lock-free for the common case) and synchronized on the *bin's head node* only when a bin already has entries, so threads contend only when they hit the same bin. Reads are fully lock-free because nodes' val and next fields are volatile. size() is maintained via a striped counter (baseCount + a CounterCell[] array, the same idea as LongAdder) to avoid a single hot field. The practical upshot: concurrency now scales with the number of buckets, not a fixed 16, and reads never block — but size()/isEmpty() are approximate under concurrent mutation, and there is no map-wide lock you can hold.","correct":"Java 7's ConcurrentHashMap used segment-level locking: the map was divided into ~16 segments, each a separately-locked sub-map, so concurrency was capped at the segment count.","distractors":["ArrayList's Spliterator has three characteristics: SIZED (knows exact size via size() in O(1)), SUBSIZED (sub-spliterators after trySplit() also have exact known sizes), and ORDERED (encounter order = insertion order).","List.of() (Java 9): creates an immutable list backed by a compact internal structure (array for small sizes, no ArrayList overhead).","(1) Random access: ArrayList O(1), LinkedList O(n)."],"distractorIds":["java/collections_internals#5eee8f6daba5","java/collections_internals#fe0420cbd69e","java/collections_internals#09b8650172bc"],"concepts":["concurrenthashmap","divided","capped","locked","separately","differ"],"questionMd":"How does `ConcurrentHashMap` achieve thread safety in Java 8+, and how does it differ from the old segment design?","correctMd":"Java 7's `ConcurrentHashMap` used segment-level locking: the map was divided into ~16 segments, each a separately-locked sub-map, so concurrency was capped at the segment count.","answerFullMd":"Java 7's `ConcurrentHashMap` used segment-level locking: the map was divided into ~16 segments, each a separately-locked sub-map, so concurrency was capped at the segment count. Java 8 rewrote it to lock at the *bucket (bin) level*: writes use a CAS to install the first node in an empty bin (lock-free for the common case) and `synchronized` on the *bin's head node* only when a bin already has entries, so threads contend only when they hit the same bin. Reads are fully lock-free because nodes' `val` and `next` fields are `volatile`. `size()` is maintained via a striped counter (`baseCount` + a `CounterCell[]` array, the same idea as `LongAdder`) to avoid a single hot field. The practical upshot: concurrency now scales with the number of buckets, not a fixed 16, and reads never block — but `size()`/`isEmpty()` are approximate under concurrent mutation, and there is no map-wide lock you can hold.","distractorsMd":["`ArrayList`'s `Spliterator` has three characteristics: `SIZED` (knows exact size via `size()` in O(1)), `SUBSIZED` (sub-spliterators after `trySplit()` also have exact known sizes), and `ORDERED` (encounter order = insertion order).","`List.of()` (Java 9): creates an immutable list backed by a compact internal structure (array for small sizes, no `ArrayList` overhead).",null]},{"id":"java/collections_internals#092ec50568fe","section":"java","module":"java/collections_internals","moduleName":"collections internals","sourceFile":"README.md","difficulty":"advanced","question":"Why must compareTo/Comparator be consistent with equals for TreeMap/TreeSet, and what breaks if it isn't?","answerFull":"TreeMap and TreeSet locate elements *solely* by the comparator/compareTo, never by equals — two keys are \"the same\" iff compare returns 0. If the ordering is inconsistent with equals (e.g. a Comparator that compares only by one field while equals compares more), the collection still works but *violates the Set/Map contract*: it may treat two equals-distinct objects as duplicates (dropping one), or contains/get may fail to find an element that is equals to a stored one because the comparator routes the search down a different branch. The classic bug is a TreeSet with a comparator on, say, length only — adding two different strings of the same length silently keeps just one. Guidance: make natural ordering consistent with equals (as String, Integer etc. do), and only deliberately diverge when you understand you are redefining \"equal\" for that sorted structure.","correct":"TreeMap and TreeSet locate elements *solely* by the comparator/compareTo, never by equals — two keys are \"the same\" iff compare returns 0.","distractors":["When two keys hash to the same bucket, HashMap stores them as a linked list (chain) within that bucket.","EnumMap<K extends Enum, V>: all keys are enum constants → backed by a plain array indexed by ordinal.","(1) Random access: ArrayList O(1), LinkedList O(n)."],"distractorIds":["java/collections_internals#cf7114091caf","java/collections_internals#f5237bbdf09b","java/collections_internals#09b8650172bc"],"concepts":["treeset","comparator","compareto","iff","treemap","locate"],"questionMd":"Why must `compareTo`/`Comparator` be consistent with `equals` for `TreeMap`/`TreeSet`, and what breaks if it isn't?","correctMd":"`TreeMap` and `TreeSet` locate elements *solely* by the comparator/`compareTo`, never by `equals` — two keys are \"the same\" iff `compare` returns 0.","answerFullMd":"`TreeMap` and `TreeSet` locate elements *solely* by the comparator/`compareTo`, never by `equals` — two keys are \"the same\" iff `compare` returns 0. If the ordering is inconsistent with `equals` (e.g. a `Comparator` that compares only by one field while `equals` compares more), the collection still works but *violates the `Set`/`Map` contract*: it may treat two `equals`-distinct objects as duplicates (dropping one), or `contains`/`get` may fail to find an element that is `equals` to a stored one because the comparator routes the search down a different branch. The classic bug is a `TreeSet` with a comparator on, say, length only — adding two different strings of the same length silently keeps just one. Guidance: make natural ordering consistent with `equals` (as `String`, `Integer` etc. do), and only deliberately diverge when you understand you are redefining \"equal\" for that sorted structure.","distractorsMd":[null,"`EnumMap<K extends Enum, V>`: all keys are enum constants → backed by a plain array indexed by ordinal.",null]},{"id":"java/concurrency#67484dd6aa74","section":"java","module":"java/concurrency","moduleName":"concurrency","sourceFile":"README.md","difficulty":"core","question":"What is the difference between volatile, synchronized, and AtomicInteger — which do you choose?","answerFull":"volatile guarantees visibility (writes flushed immediately) and ordering (no reordering around volatile access) but NOT atomicity for compound operations like i++. Use volatile for single-writer flags. synchronized gives both mutual exclusion and visibility — safe for any critical section but coarser. AtomicInteger uses CAS (hardware compare-and-swap) — lock-free atomicity for single-variable operations, better throughput than synchronized for counters. Choose: simple flag → volatile; compound operation → synchronized or Atomic; high-contention counter → LongAdder.","correct":"volatile guarantees visibility (writes flushed immediately) and ordering (no reordering around volatile access) but NOT atomicity for compound operations like i++.","distractors":["Happens-before is a formal guarantee from the JVM: if action A happens-before action B, B is guaranteed to see all writes by A.","A spurious wakeup is when Object.wait() or Condition.await() returns without being notify()d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it).","The bug: instance = new Service() compiles to 3 operations: (1) allocate memory, (2) initialize fields, (3) assign reference."],"distractorIds":["java/concurrency#5da5bfe5a21d","java/concurrency#9361e75a587d","java/concurrency#c536bc5f7048"],"concepts":["reordering","flushed","compound","atomicity","difference","synchronized"],"questionMd":"What is the difference between `volatile`, `synchronized`, and `AtomicInteger` — which do you choose?","correctMd":"`volatile` guarantees visibility (writes flushed immediately) and ordering (no reordering around volatile access) but NOT atomicity for compound operations like `i++`.","answerFullMd":"`volatile` guarantees visibility (writes flushed immediately) and ordering (no reordering around volatile access) but NOT atomicity for compound operations like `i++`. Use `volatile` for single-writer flags. `synchronized` gives both mutual exclusion and visibility — safe for any critical section but coarser. `AtomicInteger` uses CAS (hardware compare-and-swap) — lock-free atomicity for single-variable operations, better throughput than synchronized for counters. Choose: simple flag → volatile; compound operation → synchronized or Atomic; high-contention counter → `LongAdder`.","distractorsMd":[null,"A spurious wakeup is when `Object.wait()` or `Condition.await()` returns without being `notify()`d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it).","The bug: `instance = new Service()` compiles to 3 operations: (1) allocate memory, (2) initialize fields, (3) assign reference."]},{"id":"java/concurrency#5da5bfe5a21d","section":"java","module":"java/concurrency","moduleName":"concurrency","sourceFile":"README.md","difficulty":"core","question":"Explain the happens-before relationship and give an example.","answerFull":"Happens-before is a formal guarantee from the JVM: if action A happens-before action B, B is guaranteed to see all writes by A. Key edges: program order (each statement hb next in same thread), monitor unlock hb subsequent lock, volatile write hb subsequent volatile read, Thread.start() hb any action in child thread, Thread.join() hb any action after join returns. Example: Thread A writes x=42; flag=true (flag is volatile). Thread B reads if(flag) read x. When B reads flag==true, the volatile read establishes hb after A's volatile write, guaranteeing B sees x=42.","correct":"Happens-before is a formal guarantee from the JVM: if action A happens-before action B, B is guaranteed to see all writes by A.","distractors":["A spurious wakeup is when Object.wait() or Condition.await() returns without being notify()d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it).","volatile guarantees visibility (writes flushed immediately) and ordering (no reordering around volatile access) but NOT atomicity for compound operations like i++.","A virtual thread is \"pinned\" to its carrier thread when it cannot be unmounted while blocking — the OS thread is blocked too, wasting resources."],"distractorIds":["java/concurrency#9361e75a587d","java/concurrency#67484dd6aa74","java/concurrency#ab7f533fdb6b"],"concepts":["formal","explain","relationship","give","guaranteed","guarantee"],"answerFullMd":"Happens-before is a formal guarantee from the JVM: if action A happens-before action B, B is guaranteed to see all writes by A. Key edges: program order (each statement hb next in same thread), monitor unlock hb subsequent lock, volatile write hb subsequent volatile read, Thread.start() hb any action in child thread, Thread.join() hb any action after join returns. Example: Thread A writes `x=42; flag=true` (flag is volatile). Thread B reads `if(flag) read x`. When B reads `flag==true`, the volatile read establishes hb after A's volatile write, guaranteeing B sees `x=42`.","distractorsMd":["A spurious wakeup is when `Object.wait()` or `Condition.await()` returns without being `notify()`d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it).","`volatile` guarantees visibility (writes flushed immediately) and ordering (no reordering around volatile access) but NOT atomicity for compound operations like `i++`.",null]},{"id":"java/concurrency#c536bc5f7048","section":"java","module":"java/concurrency","moduleName":"concurrency","sourceFile":"README.md","difficulty":"core","question":"What is the bug in double-checked locking and how do you fix it?","answerFull":"The bug: instance = new Service() compiles to 3 operations: (1) allocate memory, (2) initialize fields, (3) assign reference. JIT may reorder to (1)→(3)→(2): the reference is assigned before initialization completes. Thread B sees non-null reference, skips the synchronized block, reads partially initialized object, gets NPE. Fix: declare field as volatile — volatile write establishes happens-before, preventing reordering of the initialization steps across the assignment.","correct":"The bug: instance = new Service() compiles to 3 operations: (1) allocate memory, (2) initialize fields, (3) assign reference.","distractors":["volatile guarantees visibility (writes flushed immediately) and ordering (no reordering around volatile access) but NOT atomicity for compound operations like i++.","CompletableFuture.allOf(cf1, cf2, cf3) returns a new CompletableFuture<Void> that completes when ALL input futures complete.","Each worker thread has a deque (double-ended queue) of tasks."],"distractorIds":["java/concurrency#67484dd6aa74","java/concurrency#c7cfa9da844e","java/concurrency#869d0c4218ac"],"concepts":["allocate","checked","initialize","double","bug","compiles"],"correctMd":"The bug: `instance = new Service()` compiles to 3 operations: (1) allocate memory, (2) initialize fields, (3) assign reference.","answerFullMd":"The bug: `instance = new Service()` compiles to 3 operations: (1) allocate memory, (2) initialize fields, (3) assign reference. JIT may reorder to (1)→(3)→(2): the reference is assigned before initialization completes. Thread B sees non-null reference, skips the synchronized block, reads partially initialized object, gets NPE. Fix: declare field as `volatile` — volatile write establishes happens-before, preventing reordering of the initialization steps across the assignment.","distractorsMd":["`volatile` guarantees visibility (writes flushed immediately) and ordering (no reordering around volatile access) but NOT atomicity for compound operations like `i++`.","`CompletableFuture.allOf(cf1, cf2, cf3)` returns a new `CompletableFuture<Void>` that completes when ALL input futures complete.",null]},{"id":"java/concurrency#c80c1c040868","section":"java","module":"java/concurrency","moduleName":"concurrency","sourceFile":"README.md","difficulty":"core","question":"What happens when ThreadPoolExecutor's queue is full?","answerFull":"The executor tries to create a new thread up to maximumPoolSize. If max is also reached, the RejectedExecutionHandler is invoked. Default is AbortPolicy — throws RejectedExecutionException. Production-safe alternatives: CallerRunsPolicy (caller thread runs the task — natural backpressure), DiscardOldestPolicy (drops oldest queued task and retries). With LinkedBlockingQueue (unbounded), the queue never fills — max threads are never created and rejection never happens, but the queue can grow to OOM.","correct":"The executor tries to create a new thread up to maximumPoolSize.","distractors":["Each worker thread has a deque (double-ended queue) of tasks.","A virtual thread is \"pinned\" to its carrier thread when it cannot be unmounted while blocking — the OS thread is blocked too, wasting resources.","ThreadLocal provides a per-thread variable — each thread has its own copy, no synchronization needed."],"distractorIds":["java/concurrency#869d0c4218ac","java/concurrency#ab7f533fdb6b","java/concurrency#133c00e82204"],"concepts":["maximumpoolsize","threadpoolexecutor","tries","executor","happens","queue"],"correctMd":"The executor tries to create a new thread up to `maximumPoolSize`.","answerFullMd":"The executor tries to create a new thread up to `maximumPoolSize`. If max is also reached, the `RejectedExecutionHandler` is invoked. Default is `AbortPolicy` — throws `RejectedExecutionException`. Production-safe alternatives: `CallerRunsPolicy` (caller thread runs the task — natural backpressure), `DiscardOldestPolicy` (drops oldest queued task and retries). With `LinkedBlockingQueue` (unbounded), the queue never fills — max threads are never created and rejection never happens, but the queue can grow to OOM.","distractorsMd":[null,null,"`ThreadLocal` provides a per-thread variable — each thread has its own copy, no synchronization needed."]},{"id":"java/concurrency#69e4423ba20b","section":"java","module":"java/concurrency","moduleName":"concurrency","sourceFile":"README.md","difficulty":"core","question":"What is the difference between Callable and Runnable?","answerFull":"Runnable: void run() — no return value, cannot throw checked exceptions. Callable<V>: V call() throws Exception — returns a value, can throw checked exceptions. Callable is used with ExecutorService.submit() which returns a Future<V>. Runnable with submit() returns Future<?> (always null result). Use Callable whenever you need to retrieve a result or propagate a checked exception from an async task.","correct":"Runnable: void run() — no return value, cannot throw checked exceptions.","distractors":["CompletableFuture.allOf(cf1, cf2, cf3) returns a new CompletableFuture<Void> that completes when ALL input futures complete.","A virtual thread is \"pinned\" to its carrier thread when it cannot be unmounted while blocking — the OS thread is blocked too, wasting resources.","A spurious wakeup is when Object.wait() or Condition.await() returns without being notify()d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it)."],"distractorIds":["java/concurrency#c7cfa9da844e","java/concurrency#ab7f533fdb6b","java/concurrency#9361e75a587d"],"concepts":["void","throw","checked","difference","exceptions","runnable"],"questionMd":"What is the difference between `Callable` and `Runnable`?","correctMd":"`Runnable`: `void run()` — no return value, cannot throw checked exceptions.","answerFullMd":"`Runnable`: `void run()` — no return value, cannot throw checked exceptions. `Callable<V>`: `V call() throws Exception` — returns a value, can throw checked exceptions. `Callable` is used with `ExecutorService.submit()` which returns a `Future<V>`. `Runnable` with `submit()` returns `Future<?>` (always `null` result). Use `Callable` whenever you need to retrieve a result or propagate a checked exception from an async task.","distractorsMd":["`CompletableFuture.allOf(cf1, cf2, cf3)` returns a new `CompletableFuture<Void>` that completes when ALL input futures complete.",null,"A spurious wakeup is when `Object.wait()` or `Condition.await()` returns without being `notify()`d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it)."]},{"id":"java/concurrency#f0b32916b410","section":"java","module":"java/concurrency","moduleName":"concurrency","sourceFile":"README.md","difficulty":"intermediate","question":"What is the ABA problem in CAS and how is it addressed?","answerFull":"ABA: Thread A reads value=A, is preempted. Thread B changes A→B→A. Thread A resumes, CAS(A,C) succeeds because the value is A again — but the state has changed in between. Example: lock-free stack where a node is popped and re-pushed; a CAS on the stack top pointer sees the same address but the stack structure changed. Fix: AtomicStampedReference<V> — stores both value and a version/stamp integer. CAS checks both value AND stamp, so even if value cycles back to A, the stamp increment prevents the ABA scenario.","correct":"ABA: Thread A reads value=A, is preempted.","distractors":["Thread.sleep(ms) suspends the current thread for the specified duration — does NOT release any held locks, is a static method on Thread, throws InterruptedException.","ThreadLocal provides a per-thread variable — each thread has its own copy, no synchronization needed.","The executor tries to create a new thread up to maximumPoolSize."],"distractorIds":["java/concurrency#9eee634b55cd","java/concurrency#133c00e82204","java/concurrency#c80c1c040868"],"concepts":["aba","preempted","cas","addressed","problem","reads"],"answerFullMd":"ABA: Thread A reads value=A, is preempted. Thread B changes A→B→A. Thread A resumes, CAS(A,C) succeeds because the value is A again — but the state has changed in between. Example: lock-free stack where a node is popped and re-pushed; a CAS on the stack top pointer sees the same address but the stack structure changed. Fix: `AtomicStampedReference<V>` — stores both value and a version/stamp integer. CAS checks both value AND stamp, so even if value cycles back to A, the stamp increment prevents the ABA scenario.","distractorsMd":["`Thread.sleep(ms)` suspends the current thread for the specified duration — does NOT release any held locks, is a static method on Thread, throws `InterruptedException`.","`ThreadLocal` provides a per-thread variable — each thread has its own copy, no synchronization needed.","The executor tries to create a new thread up to `maximumPoolSize`."]},{"id":"java/concurrency#dc70839aa640","section":"java","module":"java/concurrency","moduleName":"concurrency","sourceFile":"README.md","difficulty":"intermediate","question":"Why is ConcurrentHashMap 10-20× faster in Java 8 than Java 7?","answerFull":"Java 7 used a striped locking design with 16 segments (each a mini-HashMap), requiring lock acquisition for every write. Java 8 redesigned: for empty buckets, insertion uses CAS (no lock); for non-empty buckets, only the bucket HEAD is synchronized (lock-striping at bucket level, not segment level). Also added LongAdder for the size counter (no global lock for size()). With 16 buckets in Java 7, 16 concurrent threads could run; with Java 8, hundreds of threads can write to different buckets simultaneously.","correct":"Java 7 used a striped locking design with 16 segments (each a mini-HashMap), requiring lock acquisition for every write.","distractors":["Priority inversion: a high-priority thread (H) is blocked waiting for a lock held by a low-priority thread (L), while a medium-priority thread (M) preempts L and prevents it from finishing.","StampedLock.tryOptimisticRead() returns a stamp (version number) without acquiring any lock.","A spurious wakeup is when Object.wait() or Condition.await() returns without being notify()d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it)."],"distractorIds":["java/concurrency#4382a75bf33e","java/concurrency#36545b05e4ab","java/concurrency#9361e75a587d"],"concepts":["concurrenthashmap","striped","acquisition","mini","locking","segments"],"answerFullMd":"Java 7 used a striped locking design with 16 segments (each a mini-HashMap), requiring lock acquisition for every write. Java 8 redesigned: for empty buckets, insertion uses CAS (no lock); for non-empty buckets, only the bucket HEAD is `synchronized` (lock-striping at bucket level, not segment level). Also added `LongAdder` for the size counter (no global lock for `size()`). With 16 buckets in Java 7, 16 concurrent threads could run; with Java 8, hundreds of threads can write to different buckets simultaneously.","distractorsMd":[null,"`StampedLock.tryOptimisticRead()` returns a stamp (version number) without acquiring any lock.","A spurious wakeup is when `Object.wait()` or `Condition.await()` returns without being `notify()`d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it)."]},{"id":"java/concurrency#aa9a38e1cc0d","section":"java","module":"java/concurrency","moduleName":"concurrency","sourceFile":"README.md","difficulty":"intermediate","question":"When would you use CountDownLatch vs CyclicBarrier?","answerFull":"CountDownLatch: one-shot, counts down to zero, waiting threads released once. Use when waiting for N independent events to complete (e.g., N initialization tasks; N is known upfront). Cannot be reset. CyclicBarrier: reusable, all N threads wait at the barrier, then all continue together; a barrier action runs when full. Use for iterative algorithms where N threads synchronize at each phase boundary (e.g., parallel matrix computation phases). Rule: CountDownLatch for \"wait for N events\"; CyclicBarrier for \"all N threads rendezvous at a point and continue together repeatedly.\"","correct":"CountDownLatch: one-shot, counts down to zero, waiting threads released once.","distractors":["Deadlock: threads are BLOCKED, waiting for each other indefinitely — no progress, no CPU usage.","Priority inversion: a high-priority thread (H) is blocked waiting for a lock held by a low-priority thread (L), while a medium-priority thread (M) preempts L and prevents it from finishing.","A spurious wakeup is when Object.wait() or Condition.await() returns without being notify()d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it)."],"distractorIds":["java/concurrency#4fcbc3dc6a16","java/concurrency#4382a75bf33e","java/concurrency#9361e75a587d"],"concepts":["countdownlatch","released","shot","waiting","counts","down"],"questionMd":"When would you use `CountDownLatch` vs `CyclicBarrier`?","correctMd":"`CountDownLatch`: one-shot, counts down to zero, waiting threads released once.","answerFullMd":"`CountDownLatch`: one-shot, counts down to zero, waiting threads released once. Use when waiting for N independent events to complete (e.g., N initialization tasks; N is known upfront). Cannot be reset. `CyclicBarrier`: reusable, all N threads wait at the barrier, then all continue together; a barrier action runs when full. Use for iterative algorithms where N threads synchronize at each phase boundary (e.g., parallel matrix computation phases). Rule: CountDownLatch for \"wait for N events\"; CyclicBarrier for \"all N threads rendezvous at a point and continue together repeatedly.\"","distractorsMd":[null,null,"A spurious wakeup is when `Object.wait()` or `Condition.await()` returns without being `notify()`d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it)."]},{"id":"java/concurrency#869d0c4218ac","section":"java","module":"java/concurrency","moduleName":"concurrency","sourceFile":"README.md","difficulty":"intermediate","question":"How does ForkJoinPool's work-stealing algorithm work?","answerFull":"Each worker thread has a deque (double-ended queue) of tasks. Tasks are split by recursion and pushed to the thread's own deque. The thread pops from the head (LIFO — good for cache). When a thread's deque is empty, it *steals* from the tail (FIFO) of another thread's deque. Stealing from the tail minimizes contention between the owner (working at head) and the thief. Work-stealing efficiently utilizes all CPUs and handles imbalanced workloads where some sub-tasks take longer than others.","correct":"Each worker thread has a deque (double-ended queue) of tasks.","distractors":["ThreadLocal provides a per-thread variable — each thread has its own copy, no synchronization needed.","Priority inversion: a high-priority thread (H) is blocked waiting for a lock held by a low-priority thread (L), while a medium-priority thread (M) preempts L and prevents it from finishing.","The executor tries to create a new thread up to maximumPoolSize."],"distractorIds":["java/concurrency#133c00e82204","java/concurrency#4382a75bf33e","java/concurrency#c80c1c040868"],"concepts":["stealing","ended","forkjoinpool","deque","double","worker"],"questionMd":"How does `ForkJoinPool`'s work-stealing algorithm work?","distractorsMd":["`ThreadLocal` provides a per-thread variable — each thread has its own copy, no synchronization needed.",null,"The executor tries to create a new thread up to `maximumPoolSize`."]},{"id":"java/concurrency#133c00e82204","section":"java","module":"java/concurrency","moduleName":"concurrency","sourceFile":"README.md","difficulty":"intermediate","question":"What is ThreadLocal and when can it cause memory leaks?","answerFull":"ThreadLocal provides a per-thread variable — each thread has its own copy, no synchronization needed. Common uses: request context (userId per request), database connections, SimpleDateFormat (not thread-safe). Memory leak: when used with thread pools, threads live for the JVM lifetime. If ThreadLocal.remove() is not called after use, the value persists in the thread's ThreadLocalMap forever. If the value holds a reference to application objects (e.g., request context holding HTTP response), those objects are never GC'd. Fix: always call threadLocal.remove() in a finally block.","correct":"ThreadLocal provides a per-thread variable — each thread has its own copy, no synchronization needed.","distractors":["A spurious wakeup is when Object.wait() or Condition.await() returns without being notify()d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it).","The executor tries to create a new thread up to maximumPoolSize.","A virtual thread is \"pinned\" to its carrier thread when it cannot be unmounted while blocking — the OS thread is blocked too, wasting resources."],"distractorIds":["java/concurrency#9361e75a587d","java/concurrency#c80c1c040868","java/concurrency#ab7f533fdb6b"],"concepts":["synchronization","threadlocal","leaks","cause","copy","variable"],"questionMd":"What is `ThreadLocal` and when can it cause memory leaks?","correctMd":"`ThreadLocal` provides a per-thread variable — each thread has its own copy, no synchronization needed.","answerFullMd":"`ThreadLocal` provides a per-thread variable — each thread has its own copy, no synchronization needed. Common uses: request context (userId per request), database connections, SimpleDateFormat (not thread-safe). Memory leak: when used with thread pools, threads live for the JVM lifetime. If `ThreadLocal.remove()` is not called after use, the value persists in the thread's `ThreadLocalMap` forever. If the value holds a reference to application objects (e.g., request context holding HTTP response), those objects are never GC'd. **Fix**: always call `threadLocal.remove()` in a `finally` block.","distractorsMd":["A spurious wakeup is when `Object.wait()` or `Condition.await()` returns without being `notify()`d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it).","The executor tries to create a new thread up to `maximumPoolSize`.",null]},{"id":"java/concurrency#9eee634b55cd","section":"java","module":"java/concurrency","moduleName":"concurrency","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between sleep() and wait()?","answerFull":"Thread.sleep(ms) suspends the current thread for the specified duration — does NOT release any held locks, is a static method on Thread, throws InterruptedException. Object.wait() suspends the current thread AND atomically releases the intrinsic lock on the object — the thread re-acquires the lock when awakened. wait() must be called from inside synchronized; sleep() can be called anywhere. Use wait()/notify() for producer-consumer coordination; use sleep() for simple time delays.","correct":"Thread.sleep(ms) suspends the current thread for the specified duration — does NOT release any held locks, is a static method on Thread, throws InterruptedException.","distractors":["The executor tries to create a new thread up to maximumPoolSize.","Each worker thread has a deque (double-ended queue) of tasks.","A virtual thread is \"pinned\" to its carrier thread when it cannot be unmounted while blocking — the OS thread is blocked too, wasting resources."],"distractorIds":["java/concurrency#c80c1c040868","java/concurrency#869d0c4218ac","java/concurrency#ab7f533fdb6b"],"concepts":["interruptedexception","suspends","sleep","difference","release","specified"],"questionMd":"What is the difference between `sleep()` and `wait()`?","correctMd":"`Thread.sleep(ms)` suspends the current thread for the specified duration — does NOT release any held locks, is a static method on Thread, throws `InterruptedException`.","answerFullMd":"`Thread.sleep(ms)` suspends the current thread for the specified duration — does NOT release any held locks, is a static method on Thread, throws `InterruptedException`. `Object.wait()` suspends the current thread AND atomically releases the intrinsic lock on the object — the thread re-acquires the lock when awakened. `wait()` must be called from inside `synchronized`; `sleep()` can be called anywhere. Use `wait()`/`notify()` for producer-consumer coordination; use `sleep()` for simple time delays.","distractorsMd":["The executor tries to create a new thread up to `maximumPoolSize`.",null,null]},{"id":"java/concurrency#4fcbc3dc6a16","section":"java","module":"java/concurrency","moduleName":"concurrency","sourceFile":"README.md","difficulty":"advanced","question":"What is livelock? How does it differ from deadlock?","answerFull":"Deadlock: threads are BLOCKED, waiting for each other indefinitely — no progress, no CPU usage. Livelock: threads are ACTIVE (not blocked), repeatedly changing state in response to each other but making no progress. Example: two threads each back off when they detect a conflict, but they back off simultaneously and try again simultaneously, forever. Both are starvation scenarios; livelock is harder to detect because threads aren't blocked. Fix for livelock: randomized backoff, prioritization, or resource ordering.","correct":"Deadlock: threads are BLOCKED, waiting for each other indefinitely — no progress, no CPU usage.","distractors":["A virtual thread is \"pinned\" to its carrier thread when it cannot be unmounted while blocking — the OS thread is blocked too, wasting resources.","CountDownLatch: one-shot, counts down to zero, waiting threads released once.","Priority inversion: a high-priority thread (H) is blocked waiting for a lock held by a low-priority thread (L), while a medium-priority thread (M) preempts L and prevents it from finishing."],"distractorIds":["java/concurrency#ab7f533fdb6b","java/concurrency#aa9a38e1cc0d","java/concurrency#4382a75bf33e"],"concepts":["indefinitely","progress","deadlock","blocked","differ","usage"],"distractorsMd":[null,"`CountDownLatch`: one-shot, counts down to zero, waiting threads released once.",null]},{"id":"java/concurrency#36545b05e4ab","section":"java","module":"java/concurrency","moduleName":"concurrency","sourceFile":"README.md","difficulty":"advanced","question":"What is StampedLock's optimistic read and how does it work?","answerFull":"StampedLock.tryOptimisticRead() returns a stamp (version number) without acquiring any lock. The thread reads data optimistically. Then validate(stamp) checks if a write occurred during the read — if yes (stamp invalid), fall back to a full read lock. This allows completely lock-free reads when there are no writers, making it significantly faster than ReentrantReadWriteLock for read-heavy, low-write workloads. Caveat: StampedLock is not reentrant; attempting to re-acquire can deadlock.","correct":"StampedLock.tryOptimisticRead() returns a stamp (version number) without acquiring any lock.","distractors":["A spurious wakeup is when Object.wait() or Condition.await() returns without being notify()d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it).","Priority inversion: a high-priority thread (H) is blocked waiting for a lock held by a low-priority thread (L), while a medium-priority thread (M) preempts L and prevents it from finishing.","Java 7 used a striped locking design with 16 segments (each a mini-HashMap), requiring lock acquisition for every write."],"distractorIds":["java/concurrency#9361e75a587d","java/concurrency#4382a75bf33e","java/concurrency#dc70839aa640"],"concepts":["stamp","stampedlock","tryoptimisticread","acquiring","optimistic","lock"],"questionMd":"What is `StampedLock`'s optimistic read and how does it work?","correctMd":"`StampedLock.tryOptimisticRead()` returns a stamp (version number) without acquiring any lock.","answerFullMd":"`StampedLock.tryOptimisticRead()` returns a stamp (version number) without acquiring any lock. The thread reads data optimistically. Then `validate(stamp)` checks if a write occurred during the read — if yes (stamp invalid), fall back to a full read lock. This allows completely lock-free reads when there are no writers, making it significantly faster than `ReentrantReadWriteLock` for read-heavy, low-write workloads. Caveat: `StampedLock` is not reentrant; attempting to re-acquire can deadlock.","distractorsMd":["A spurious wakeup is when `Object.wait()` or `Condition.await()` returns without being `notify()`d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it).",null,null]},{"id":"java/concurrency#c7cfa9da844e","section":"java","module":"java/concurrency","moduleName":"concurrency","sourceFile":"README.md","difficulty":"advanced","question":"How does CompletableFuture.allOf() work?","answerFull":"CompletableFuture.allOf(cf1, cf2, cf3) returns a new CompletableFuture<Void> that completes when ALL input futures complete. It doesn't return the individual results (use cf1.get() etc. after allOf completes). If any future completes exceptionally, the allOf future also completes exceptionally with that exception. The implementation installs a completion handler on each input future; when all complete, the result future is completed. Use .thenRun() or .thenApply() to collect results after allOf.","correct":"CompletableFuture.allOf(cf1, cf2, cf3) returns a new CompletableFuture<Void> that completes when ALL input futures complete.","distractors":["StampedLock.tryOptimisticRead() returns a stamp (version number) without acquiring any lock.","A spurious wakeup is when Object.wait() or Condition.await() returns without being notify()d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it).","The executor tries to create a new thread up to maximumPoolSize."],"distractorIds":["java/concurrency#36545b05e4ab","java/concurrency#9361e75a587d","java/concurrency#c80c1c040868"],"concepts":["cf1","cf2","cf3","allof","void","futures"],"questionMd":"How does `CompletableFuture.allOf()` work?","correctMd":"`CompletableFuture.allOf(cf1, cf2, cf3)` returns a new `CompletableFuture<Void>` that completes when ALL input futures complete.","answerFullMd":"`CompletableFuture.allOf(cf1, cf2, cf3)` returns a new `CompletableFuture<Void>` that completes when ALL input futures complete. It doesn't return the individual results (use `cf1.get()` etc. after `allOf` completes). If any future completes exceptionally, the `allOf` future also completes exceptionally with that exception. The implementation installs a completion handler on each input future; when all complete, the result future is completed. Use `.thenRun()` or `.thenApply()` to collect results after `allOf`.","distractorsMd":["`StampedLock.tryOptimisticRead()` returns a stamp (version number) without acquiring any lock.","A spurious wakeup is when `Object.wait()` or `Condition.await()` returns without being `notify()`d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it).","The executor tries to create a new thread up to `maximumPoolSize`."]},{"id":"java/concurrency#ab7f533fdb6b","section":"java","module":"java/concurrency","moduleName":"concurrency","sourceFile":"README.md","difficulty":"advanced","question":"Explain virtual thread pinning and how to avoid it.","answerFull":"A virtual thread is \"pinned\" to its carrier thread when it cannot be unmounted while blocking — the OS thread is blocked too, wasting resources. Pinning occurs when: (1) the virtual thread is inside a synchronized block/method while waiting; (2) it's inside a native (JNI) method. To avoid: replace synchronized with ReentrantLock in I/O-bound paths. Detect with JVM flag -Djdk.tracePinnedThreads=full. Java 24 removes pinning for synchronized on non-primitive monitor objects.","correct":"A virtual thread is \"pinned\" to its carrier thread when it cannot be unmounted while blocking — the OS thread is blocked too, wasting resources.","distractors":["LockSupport.park() suspends the current thread until a permit is available.","Thread.sleep(ms) suspends the current thread for the specified duration — does NOT release any held locks, is a static method on Thread, throws InterruptedException.","The executor tries to create a new thread up to maximumPoolSize."],"distractorIds":["java/concurrency#78a54587f9a4","java/concurrency#9eee634b55cd","java/concurrency#c80c1c040868"],"concepts":["pinning","unmounted","wasting","explain","carrier","pinned"],"answerFullMd":"A virtual thread is \"pinned\" to its carrier thread when it cannot be unmounted while blocking — the OS thread is blocked too, wasting resources. Pinning occurs when: (1) the virtual thread is inside a `synchronized` block/method while waiting; (2) it's inside a native (JNI) method. To avoid: replace `synchronized` with `ReentrantLock` in I/O-bound paths. Detect with JVM flag `-Djdk.tracePinnedThreads=full`. Java 24 removes pinning for `synchronized` on non-primitive monitor objects.","distractorsMd":["`LockSupport.park()` suspends the current thread until a permit is available.","`Thread.sleep(ms)` suspends the current thread for the specified duration — does NOT release any held locks, is a static method on Thread, throws `InterruptedException`.","The executor tries to create a new thread up to `maximumPoolSize`."]},{"id":"java/concurrency#9361e75a587d","section":"java","module":"java/concurrency","moduleName":"concurrency","sourceFile":"README.md","difficulty":"advanced","question":"What is a spurious wakeup and how do you handle it?","answerFull":"A spurious wakeup is when Object.wait() or Condition.await() returns without being notify()d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it). If you use if (condition) wait(), a spurious wakeup will proceed past the check even when the condition is still false, causing incorrect behavior. Fix: always use while (condition) wait() — re-check the condition after every wakeup. This is why all Java concurrency examples show a while loop.","correct":"A spurious wakeup is when Object.wait() or Condition.await() returns without being notify()d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it).","distractors":["StampedLock.tryOptimisticRead() returns a stamp (version number) without acquiring any lock.","CompletableFuture.allOf(cf1, cf2, cf3) returns a new CompletableFuture<Void> that completes when ALL input futures complete.","volatile guarantees visibility (writes flushed immediately) and ordering (no reordering around volatile access) but NOT atomicity for compound operations like i++."],"distractorIds":["java/concurrency#36545b05e4ab","java/concurrency#c7cfa9da844e","java/concurrency#67484dd6aa74"],"concepts":["interrupted","posix","spurious","notify","wakeup","legal"],"correctMd":"A spurious wakeup is when `Object.wait()` or `Condition.await()` returns without being `notify()`d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it).","answerFullMd":"A spurious wakeup is when `Object.wait()` or `Condition.await()` returns without being `notify()`d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it). If you use `if (condition) wait()`, a spurious wakeup will proceed past the check even when the condition is still false, causing incorrect behavior. **Fix**: always use `while (condition) wait()` — re-check the condition after every wakeup. This is why all Java concurrency examples show a `while` loop.","distractorsMd":["`StampedLock.tryOptimisticRead()` returns a stamp (version number) without acquiring any lock.","`CompletableFuture.allOf(cf1, cf2, cf3)` returns a new `CompletableFuture<Void>` that completes when ALL input futures complete.","`volatile` guarantees visibility (writes flushed immediately) and ordering (no reordering around volatile access) but NOT atomicity for compound operations like `i++`."]},{"id":"java/concurrency#78a54587f9a4","section":"java","module":"java/concurrency","moduleName":"concurrency","sourceFile":"README.md","difficulty":"advanced","question":"What is LockSupport.park() and how does it differ from Object.wait()?","answerFull":"LockSupport.park() suspends the current thread until a permit is available. LockSupport.unpark(thread) grants a permit to the specified thread. Key differences from Object.wait(): (1) No monitor/lock required — park()/unpark() work without any synchronized block; wait() must be called inside synchronized. (2) Permit pre-posting — if unpark() is called BEFORE park(), the park returns immediately (permit consumed); notify() before wait() is lost. (3) No IllegalMonitorStateException risk. LockSupport is the primitive used by AQS (AbstractQueuedSynchronizer) to park/unpark threads in waiting queues — it underlies all JUC locks. It also supports a blocker object for thread dump diagnostics: LockSupport.park(blockingObject).","correct":"LockSupport.park() suspends the current thread until a permit is available.","distractors":["The executor tries to create a new thread up to maximumPoolSize.","Thread.sleep(ms) suspends the current thread for the specified duration — does NOT release any held locks, is a static method on Thread, throws InterruptedException.","A virtual thread is \"pinned\" to its carrier thread when it cannot be unmounted while blocking — the OS thread is blocked too, wasting resources."],"distractorIds":["java/concurrency#c80c1c040868","java/concurrency#9eee634b55cd","java/concurrency#ab7f533fdb6b"],"concepts":["locksupport","park","permit","suspends","differ","wait"],"questionMd":"What is `LockSupport.park()` and how does it differ from `Object.wait()`?","correctMd":"`LockSupport.park()` suspends the current thread until a permit is available.","answerFullMd":"`LockSupport.park()` suspends the current thread until a permit is available. `LockSupport.unpark(thread)` grants a permit to the specified thread. Key differences from `Object.wait()`: (1) No monitor/lock required — `park()`/`unpark()` work without any `synchronized` block; `wait()` must be called inside `synchronized`. (2) Permit pre-posting — if `unpark()` is called BEFORE `park()`, the park returns immediately (permit consumed); `notify()` before `wait()` is lost. (3) No `IllegalMonitorStateException` risk. `LockSupport` is the primitive used by AQS (`AbstractQueuedSynchronizer`) to park/unpark threads in waiting queues — it underlies all JUC locks. It also supports a blocker object for thread dump diagnostics: `LockSupport.park(blockingObject)`.","distractorsMd":["The executor tries to create a new thread up to `maximumPoolSize`.","`Thread.sleep(ms)` suspends the current thread for the specified duration — does NOT release any held locks, is a static method on Thread, throws `InterruptedException`.",null]},{"id":"java/concurrency#4382a75bf33e","section":"java","module":"java/concurrency","moduleName":"concurrency","sourceFile":"README.md","difficulty":"advanced","question":"What is priority inversion and how does it manifest in Java?","answerFull":"Priority inversion: a high-priority thread (H) is blocked waiting for a lock held by a low-priority thread (L), while a medium-priority thread (M) preempts L and prevents it from finishing. H is effectively blocked by M, even though H has higher priority than M. In Java: if L holds ReentrantLock, M's CPU-bound work starves L, which never releases the lock, which starves H indefinitely. Java has no built-in priority inheritance (OS mechanism to temporarily raise L's priority to H's level). Mitigation: avoid priority differences among threads sharing locks; use lock-free data structures for high-priority code; or use equal-priority threads for all critical sections.","correct":"Priority inversion: a high-priority thread (H) is blocked waiting for a lock held by a low-priority thread (L), while a medium-priority thread (M) preempts L and prevents it from finishing.","distractors":["A virtual thread is \"pinned\" to its carrier thread when it cannot be unmounted while blocking — the OS thread is blocked too, wasting resources.","Deadlock: threads are BLOCKED, waiting for each other indefinitely — no progress, no CPU usage.","Each worker thread has a deque (double-ended queue) of tasks."],"distractorIds":["java/concurrency#ab7f533fdb6b","java/concurrency#4fcbc3dc6a16","java/concurrency#869d0c4218ac"],"concepts":["finishing","preempts","manifest","inversion","medium","blocked"],"answerFullMd":"Priority inversion: a high-priority thread (H) is blocked waiting for a lock held by a low-priority thread (L), while a medium-priority thread (M) preempts L and prevents it from finishing. H is effectively blocked by M, even though H has higher priority than M. In Java: if L holds `ReentrantLock`, M's CPU-bound work starves L, which never releases the lock, which starves H indefinitely. Java has no built-in priority inheritance (OS mechanism to temporarily raise L's priority to H's level). Mitigation: avoid priority differences among threads sharing locks; use lock-free data structures for high-priority code; or use equal-priority threads for all critical sections."},{"id":"java/concurrency#75f54e64ddbc","section":"java","module":"java/concurrency","moduleName":"concurrency","sourceFile":"README.md","difficulty":"advanced","question":"What is AbstractQueuedSynchronizer and how does ReentrantLock use it?","answerFull":"AQS is the framework underlying all major JUC synchronizers. Core structure: volatile int state + a CLH (doubly-linked) queue of waiting Node objects. Acquire: tryAcquire(arg) tries a CAS on state; if it fails, the thread is added to the CLH queue tail and parked. Release: tryRelease(arg) updates state; if successful, unpacks the next waiting thread. ReentrantLock uses AQS with state = 0 (unlocked) or N (lock count for reentrant holds). tryAcquire CAS's from 0 to 1; re-entry increments state. tryRelease decrements state; releases when it reaches 0. CountDownLatch uses shared-mode AQS where all waiters are released when state reaches 0. Semaphore uses state as permit count.","correct":"AQS is the framework underlying all major JUC synchronizers.","distractors":["A spurious wakeup is when Object.wait() or Condition.await() returns without being notify()d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it).","A virtual thread is \"pinned\" to its carrier thread when it cannot be unmounted while blocking — the OS thread is blocked too, wasting resources.","ABA: Thread A reads value=A, is preempted."],"distractorIds":["java/concurrency#9361e75a587d","java/concurrency#ab7f533fdb6b","java/concurrency#f0b32916b410"],"concepts":["aqs","juc","synchronizers","major","underlying","framework"],"questionMd":"What is `AbstractQueuedSynchronizer` and how does `ReentrantLock` use it?","answerFullMd":"AQS is the framework underlying all major JUC synchronizers. Core structure: `volatile int state` + a CLH (doubly-linked) queue of waiting `Node` objects. Acquire: `tryAcquire(arg)` tries a CAS on state; if it fails, the thread is added to the CLH queue tail and parked. Release: `tryRelease(arg)` updates state; if successful, unpacks the next waiting thread. `ReentrantLock` uses AQS with `state` = 0 (unlocked) or N (lock count for reentrant holds). `tryAcquire` CAS's from 0 to 1; re-entry increments state. `tryRelease` decrements state; releases when it reaches 0. `CountDownLatch` uses shared-mode AQS where all waiters are released when state reaches 0. `Semaphore` uses state as permit count.","distractorsMd":["A spurious wakeup is when `Object.wait()` or `Condition.await()` returns without being `notify()`d or interrupted — a rare but legal JVM behavior (POSIX condition variable allows it).",null,null]},{"id":"java/core_language#99f90343da6e","section":"java","module":"java/core_language","moduleName":"core language","sourceFile":"README.md","difficulty":"core","question":"What happens when you override equals() but not hashCode() in a class used as a HashMap key?","answerFull":"Two logically equal objects compute different hash codes (since Object.hashCode() is identity-based), so HashMap places them in different buckets. A get() call with a logically-equal key will hash to a different bucket and return null — silent data loss. The fix is to always override both: if a.equals(b) then a.hashCode() == b.hashCode() must hold.","correct":"Two logically equal objects compute different hash codes (since Object.hashCode() is identity-based), so HashMap places them in different buckets.","distractors":["Static dispatch: overloaded method resolution at compile time based on the *declared* parameter type.","equals(), hashCode(), toString(), clone() (protected), finalize() (deprecated), getClass(), wait() (3 overloads), notify(), notifyAll().","(1) Static nested class: no outer reference, behaves like a top-level class."],"distractorIds":["java/core_language#786a8a2904cf","java/core_language#1f4ba304a50e","java/core_language#86a94445cf15"],"concepts":["logically","hashcode","codes","buckets","places","override"],"correctMd":"Two logically equal objects compute different hash codes (since `Object.hashCode()` is identity-based), so `HashMap` places them in different buckets.","answerFullMd":"Two logically equal objects compute different hash codes (since `Object.hashCode()` is identity-based), so `HashMap` places them in different buckets. A `get()` call with a logically-equal key will hash to a different bucket and return `null` — silent data loss. The fix is to always override both: if `a.equals(b)` then `a.hashCode() == b.hashCode()` must hold.","distractorsMd":[null,"`equals()`, `hashCode()`, `toString()`, `clone()` (protected), `finalize()` (deprecated), `getClass()`, `wait()` (3 overloads), `notify()`, `notifyAll()`.",null]},{"id":"java/core_language#022287f5368f","section":"java","module":"java/core_language","moduleName":"core language","sourceFile":"README.md","difficulty":"core","question":"What is the difference between an abstract class and an interface in Java 8+?","answerFull":"An abstract class can have instance state (fields), constructors, and any mix of abstract/concrete methods. An interface can have only static final constants, abstract methods, default methods (with body), and static methods. The key practical difference: a class can implement many interfaces but extend only one abstract class. Use abstract class for shared state + partial implementation; use interface for capability contracts on unrelated types.","correct":"An abstract class can have instance state (fields), constructors, and any mix of abstract/concrete methods.","distractors":["(1) Static nested class: no outer reference, behaves like a top-level class.","Overriding applies to *instance methods*: the runtime type determines which version is called (dynamic dispatch).","Static nested: helper classes that don't need outer state (e.g., Map.Entry, Builder)."],"distractorIds":["java/core_language#86a94445cf15","java/core_language#5cadf1c3bb39","java/core_language#e815aed21a8e"],"concepts":["constructors","mix","difference","concrete","abstract","interface"],"answerFullMd":"An abstract class can have instance state (fields), constructors, and any mix of abstract/concrete methods. An interface can have only `static final` constants, abstract methods, `default` methods (with body), and `static` methods. The key practical difference: a class can implement many interfaces but extend only one abstract class. Use abstract class for shared state + partial implementation; use interface for capability contracts on unrelated types.","distractorsMd":[null,null,"Static nested: helper classes that don't need outer state (e.g., `Map.Entry`, `Builder`)."]},{"id":"java/core_language#56ff92e3283d","section":"java","module":"java/core_language","moduleName":"core language","sourceFile":"README.md","difficulty":"core","question":"Why prefer composition over inheritance (Effective Java Item 18)?","answerFull":"Inheritance exposes implementation details — subclasses break when superclasses change internal behavior. The \"fragile base class\" problem: HashSet wrapping AbstractSet broke when addAll() internally called add(), causing double-counting in an overriding subclass. Composition avoids this: the wrapping class delegates to the composed object via its public API only, so superclass internal changes can't break the wrapper.","correct":"Inheritance exposes implementation details — subclasses break when superclasses change internal behavior.","distractors":["(1) Static nested class: no outer reference, behaves like a top-level class.","A bridge method is a synthetic compiler-generated method that maintains polymorphism after type erasure.","An abstract class can have instance state (fields), constructors, and any mix of abstract/concrete methods."],"distractorIds":["java/core_language#86a94445cf15","java/core_language#c4ddab1ba992","java/core_language#022287f5368f"],"concepts":["superclasses","details","prefer","break","composition","exposes"],"answerFullMd":"Inheritance exposes implementation details — subclasses break when superclasses change internal behavior. The \"fragile base class\" problem: `HashSet` wrapping `AbstractSet` broke when `addAll()` internally called `add()`, causing double-counting in an overriding subclass. Composition avoids this: the wrapping class delegates to the composed object via its public API only, so superclass internal changes can't break the wrapper."},{"id":"java/core_language#786a8a2904cf","section":"java","module":"java/core_language","moduleName":"core language","sourceFile":"README.md","difficulty":"core","question":"Explain static dispatch vs dynamic dispatch with a concrete example.","answerFull":"Static dispatch: overloaded method resolution at compile time based on the *declared* parameter type. Dynamic dispatch: overridden method resolution at runtime based on the *actual* receiver type via vtable. Example: Animal a = new Dog(); a.speak() → JVM looks up Dog.speak() at runtime (dynamic). But void process(Animal a) vs void process(Dog d) — if you call process(a) where a is declared Animal, the first overload is chosen at compile time regardless of the runtime type.","correct":"Static dispatch: overloaded method resolution at compile time based on the *declared* parameter type.","distractors":["A bridge method is a synthetic compiler-generated method that maintains polymorphism after type erasure.","(1) Static nested class: no outer reference, behaves like a top-level class.","Two logically equal objects compute different hash codes (since Object.hashCode() is identity-based), so HashMap places them in different buckets."],"distractorIds":["java/core_language#c4ddab1ba992","java/core_language#86a94445cf15","java/core_language#99f90343da6e"],"concepts":["overloaded","explain","concrete","dispatch","declared","dynamic"],"answerFullMd":"Static dispatch: overloaded method resolution at compile time based on the *declared* parameter type. Dynamic dispatch: overridden method resolution at runtime based on the *actual* receiver type via vtable. Example: `Animal a = new Dog(); a.speak()` → JVM looks up `Dog.speak()` at runtime (dynamic). But `void process(Animal a)` vs `void process(Dog d)` — if you call `process(a)` where `a` is declared `Animal`, the first overload is chosen at compile time regardless of the runtime type.","distractorsMd":[null,null,"Two logically equal objects compute different hash codes (since `Object.hashCode()` is identity-based), so `HashMap` places them in different buckets."]},{"id":"java/core_language#86a94445cf15","section":"java","module":"java/core_language","moduleName":"core language","sourceFile":"README.md","difficulty":"core","question":"What are the four types of inner classes, and what are their memory implications?","answerFull":"(1) Static nested class: no outer reference, behaves like a top-level class. (2) Non-static inner class: holds implicit this$0 reference to outer instance — can cause memory leaks if inner outlives outer. (3) Anonymous class: implicit outer reference + captures effectively-final locals. (4) Local class: same as anonymous but named. For long-lived callbacks/listeners, always prefer static nested or standalone classes to avoid retaining the outer object.","correct":"(1) Static nested class: no outer reference, behaves like a top-level class.","distractors":["On a class: cannot be subclassed (e.g., String, Integer); enables JIT devirtualization.","Static nested: helper classes that don't need outer state (e.g., Map.Entry, Builder).","The JVM initializes classes lazily on first active use."],"distractorIds":["java/core_language#7478aad665cc","java/core_language#e815aed21a8e","java/core_language#bafe7a31379c"],"concepts":["behaves","outer","inner","four","nested","classes"],"answerFullMd":"(1) Static nested class: no outer reference, behaves like a top-level class. (2) Non-static inner class: holds implicit `this$0` reference to outer instance — can cause memory leaks if inner outlives outer. (3) Anonymous class: implicit outer reference + captures effectively-final locals. (4) Local class: same as anonymous but named. For long-lived callbacks/listeners, always prefer static nested or standalone classes to avoid retaining the outer object.","distractorsMd":["On a **class**: cannot be subclassed (e.g., `String`, `Integer`); enables JIT devirtualization.","Static nested: helper classes that don't need outer state (e.g., `Map.Entry`, `Builder`).",null]},{"id":"java/core_language#7478aad665cc","section":"java","module":"java/core_language","moduleName":"core language","sourceFile":"README.md","difficulty":"intermediate","question":"What does final mean on a class, method, and field?","answerFull":"On a class: cannot be subclassed (e.g., String, Integer); enables JIT devirtualization. On a method: cannot be overridden; JIT can inline the call directly. On a field: reference cannot be reassigned after initialization (but the referenced object can still be mutated unless it's also immutable). Note: final field + immutable object = true immutability.","correct":"On a class: cannot be subclassed (e.g., String, Integer); enables JIT devirtualization.","distractors":["A bridge method is a synthetic compiler-generated method that maintains polymorphism after type erasure.","Static dispatch: overloaded method resolution at compile time based on the *declared* parameter type.","An abstract class can have instance state (fields), constructors, and any mix of abstract/concrete methods."],"distractorIds":["java/core_language#c4ddab1ba992","java/core_language#786a8a2904cf","java/core_language#022287f5368f"],"concepts":["devirtualization","subclassed","jit","enables","integer","mean"],"questionMd":"What does `final` mean on a class, method, and field?","correctMd":"On a **class**: cannot be subclassed (e.g., `String`, `Integer`); enables JIT devirtualization.","answerFullMd":"On a **class**: cannot be subclassed (e.g., `String`, `Integer`); enables JIT devirtualization. On a **method**: cannot be overridden; JIT can inline the call directly. On a **field**: reference cannot be reassigned after initialization (but the referenced object can still be mutated unless it's also immutable). Note: `final` field + immutable object = true immutability."},{"id":"java/core_language#00c218952fbb","section":"java","module":"java/core_language","moduleName":"core language","sourceFile":"README.md","difficulty":"intermediate","question":"What is the Liskov Substitution Principle and how can violating it manifest in Java?","answerFull":"LSP says: if S is a subtype of T, objects of type S must be substitutable for objects of type T without altering program correctness. Classic violation: Square extends Rectangle where setWidth() also sets height. Code that calls setWidth(5); setHeight(3); assert area() == 15 fails for a Square. In Java, this manifests as code that works for the declared supertype but breaks when a subtype is substituted — often discovered only in production.","correct":"LSP says: if S is a subtype of T, objects of type S must be substitutable for objects of type T without altering program correctness.","distractors":["Two logically equal objects compute different hash codes (since Object.hashCode() is identity-based), so HashMap places them in different buckets.","(1) Static nested class: no outer reference, behaves like a top-level class.","A bridge method is a synthetic compiler-generated method that maintains polymorphism after type erasure."],"distractorIds":["java/core_language#99f90343da6e","java/core_language#86a94445cf15","java/core_language#c4ddab1ba992"],"concepts":["liskov","substitution","altering","manifest","substitutable","violating"],"correctMd":"LSP says: if `S` is a subtype of `T`, objects of type `S` must be substitutable for objects of type `T` without altering program correctness.","answerFullMd":"LSP says: if `S` is a subtype of `T`, objects of type `S` must be substitutable for objects of type `T` without altering program correctness. Classic violation: `Square extends Rectangle` where `setWidth()` also sets height. Code that calls `setWidth(5); setHeight(3); assert area() == 15` fails for a `Square`. In Java, this manifests as code that works for the declared supertype but breaks when a subtype is substituted — often discovered only in production.","distractorsMd":["Two logically equal objects compute different hash codes (since `Object.hashCode()` is identity-based), so `HashMap` places them in different buckets.",null,null]},{"id":"java/core_language#24f5bfbe28f1","section":"java","module":"java/core_language","moduleName":"core language","sourceFile":"README.md","difficulty":"intermediate","question":"What is the full contract for equals()?","answerFull":"Reflexive: x.equals(x) is true. Symmetric: x.equals(y) iff y.equals(x). Transitive: if x.equals(y) and y.equals(z) then x.equals(z). Consistent: multiple calls return the same result if objects unchanged. Non-null: x.equals(null) is always false. Violating any of these breaks collection contracts.","correct":"Reflexive: x.equals(x) is true.","distractors":["The contract (from Object Javadoc): (1) Reflexivity: x.equals(x) is always true.","equals(), hashCode(), toString(), clone() (protected), finalize() (deprecated), getClass(), wait() (3 overloads), notify(), notifyAll().","(1) Static nested class: no outer reference, behaves like a top-level class."],"distractorIds":["java/core_language#db235b81dea6","java/core_language#1f4ba304a50e","java/core_language#86a94445cf15"],"concepts":["reflexive","contract","equals","true","full"],"correctMd":"Reflexive: `x.equals(x)` is true.","answerFullMd":"Reflexive: `x.equals(x)` is true. Symmetric: `x.equals(y)` iff `y.equals(x)`. Transitive: if `x.equals(y)` and `y.equals(z)` then `x.equals(z)`. Consistent: multiple calls return the same result if objects unchanged. Non-null: `x.equals(null)` is always false. Violating any of these breaks collection contracts.","distractorsMd":["The contract (from `Object` Javadoc): (1) **Reflexivity**: `x.equals(x)` is always `true`.","`equals()`, `hashCode()`, `toString()`, `clone()` (protected), `finalize()` (deprecated), `getClass()`, `wait()` (3 overloads), `notify()`, `notifyAll()`.",null]},{"id":"java/core_language#e815aed21a8e","section":"java","module":"java/core_language","moduleName":"core language","sourceFile":"README.md","difficulty":"intermediate","question":"When would you use each inner class type?","answerFull":"Static nested: helper classes that don't need outer state (e.g., Map.Entry, Builder). Non-static inner: iterator that needs access to the outer collection's internal state (e.g., ArrayList.Itr). Anonymous: short one-off implementation of an interface, especially pre-Java 8 (now replaced by lambdas). Local: complex temporary implementation that needs to access local variables from the enclosing method.","correct":"Static nested: helper classes that don't need outer state (e.g., Map.Entry, Builder).","distractors":["The JVM initializes classes lazily on first active use.","(1) Static nested class: no outer reference, behaves like a top-level class.","Static dispatch: overloaded method resolution at compile time based on the *declared* parameter type."],"distractorIds":["java/core_language#bafe7a31379c","java/core_language#86a94445cf15","java/core_language#786a8a2904cf"],"concepts":["helper","builder","outer","entry","inner","nested"],"correctMd":"Static nested: helper classes that don't need outer state (e.g., `Map.Entry`, `Builder`).","answerFullMd":"Static nested: helper classes that don't need outer state (e.g., `Map.Entry`, `Builder`). Non-static inner: iterator that needs access to the outer collection's internal state (e.g., `ArrayList.Itr`). Anonymous: short one-off implementation of an interface, especially pre-Java 8 (now replaced by lambdas). Local: complex temporary implementation that needs to access local variables from the enclosing method."},{"id":"java/core_language#1f4ba304a50e","section":"java","module":"java/core_language","moduleName":"core language","sourceFile":"README.md","difficulty":"intermediate","question":"What are all the methods defined on java.lang.Object?","answerFull":"equals(), hashCode(), toString(), clone() (protected), finalize() (deprecated), getClass(), wait() (3 overloads), notify(), notifyAll(). Every Java object inherits these. The most important are equals/hashCode (collection contracts), toString (debugging), and wait/notify (low-level concurrency — prefer java.util.concurrent instead).","correct":"equals(), hashCode(), toString(), clone() (protected), finalize() (deprecated), getClass(), wait() (3 overloads), notify(), notifyAll().","distractors":["An abstract class can have instance state (fields), constructors, and any mix of abstract/concrete methods.","Two logically equal objects compute different hash codes (since Object.hashCode() is identity-based), so HashMap places them in different buckets.","Reflexive: x.equals(x) is true."],"distractorIds":["java/core_language#022287f5368f","java/core_language#99f90343da6e","java/core_language#24f5bfbe28f1"],"concepts":["finalize","notifyall","overloads","getclass","clone","notify"],"correctMd":"`equals()`, `hashCode()`, `toString()`, `clone()` (protected), `finalize()` (deprecated), `getClass()`, `wait()` (3 overloads), `notify()`, `notifyAll()`.","answerFullMd":"`equals()`, `hashCode()`, `toString()`, `clone()` (protected), `finalize()` (deprecated), `getClass()`, `wait()` (3 overloads), `notify()`, `notifyAll()`. Every Java object inherits these. The most important are `equals`/`hashCode` (collection contracts), `toString` (debugging), and `wait`/`notify` (low-level concurrency — prefer `java.util.concurrent` instead).","distractorsMd":[null,"Two logically equal objects compute different hash codes (since `Object.hashCode()` is identity-based), so `HashMap` places them in different buckets.","Reflexive: `x.equals(x)` is true."]},{"id":"java/core_language#c4ddab1ba992","section":"java","module":"java/core_language","moduleName":"core language","sourceFile":"README.md","difficulty":"advanced","question":"What is a bridge method and when does the compiler generate one?","answerFull":"A bridge method is a synthetic compiler-generated method that maintains polymorphism after type erasure. When a subclass overrides a generic method with a more specific type, or uses covariant return types, the compiler generates an extra method with the erased signature that delegates to the actual override. Example: class Dog extends Animal { @Override Dog create() {...} } generates synthetic Animal create() { return this.create(); }. You can see them with javap -verbose.","correct":"A bridge method is a synthetic compiler-generated method that maintains polymorphism after type erasure.","distractors":["Static dispatch: overloaded method resolution at compile time based on the *declared* parameter type.","(1) Static nested class: no outer reference, behaves like a top-level class.","An abstract class can have instance state (fields), constructors, and any mix of abstract/concrete methods."],"distractorIds":["java/core_language#786a8a2904cf","java/core_language#86a94445cf15","java/core_language#022287f5368f"],"concepts":["bridge","polymorphism","erasure","compiler","synthetic","generate"],"answerFullMd":"A bridge method is a synthetic compiler-generated method that maintains polymorphism after type erasure. When a subclass overrides a generic method with a more specific type, or uses covariant return types, the compiler generates an extra method with the erased signature that delegates to the actual override. Example: `class Dog extends Animal { @Override Dog create() {...} }` generates `synthetic Animal create() { return this.create(); }`. You can see them with `javap -verbose`."},{"id":"java/core_language#5cadf1c3bb39","section":"java","module":"java/core_language","moduleName":"core language","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between hiding and overriding in Java?","answerFull":"Overriding applies to *instance methods*: the runtime type determines which version is called (dynamic dispatch). Hiding applies to *static methods*: the *declared* type at the call site determines which version is called (static dispatch). A subclass can *hide* a static method from a superclass by defining a static method with the same signature — but this is not overriding and does not participate in polymorphism. Calling the method through a supertype reference always invokes the supertype's static method.","correct":"Overriding applies to *instance methods*: the runtime type determines which version is called (dynamic dispatch).","distractors":["An abstract class can have instance state (fields), constructors, and any mix of abstract/concrete methods.","Static dispatch: overloaded method resolution at compile time based on the *declared* parameter type.","(1) Static nested class: no outer reference, behaves like a top-level class."],"distractorIds":["java/core_language#022287f5368f","java/core_language#786a8a2904cf","java/core_language#86a94445cf15"],"concepts":["overriding","difference","dispatch","dynamic","determines","called"]},{"id":"java/core_language#bafe7a31379c","section":"java","module":"java/core_language","moduleName":"core language","sourceFile":"README.md","difficulty":"advanced","question":"What is the class initialization order when a subclass is instantiated?","answerFull":"The JVM initializes classes lazily on first active use. When new Child() is called: (1) If Parent class is not yet initialized: run Parent's static field initializers and static blocks in textual order. (2) Run Child's static field initializers and static blocks. (3) For the instance: call super() (implicit or explicit) which runs Parent's instance field initializers + instance blocks + Parent constructor body. (4) Run Child's instance field initializers + instance blocks + Child constructor body. Static initialization is once per class load; instance initialization runs for every new.","correct":"The JVM initializes classes lazily on first active use.","distractors":["On a class: cannot be subclassed (e.g., String, Integer); enables JIT devirtualization.","An abstract class can have instance state (fields), constructors, and any mix of abstract/concrete methods.","(1) Static nested class: no outer reference, behaves like a top-level class."],"distractorIds":["java/core_language#7478aad665cc","java/core_language#022287f5368f","java/core_language#86a94445cf15"],"concepts":["instantiated","lazily","initializes","initialization","subclass","active"],"answerFullMd":"The JVM initializes classes lazily on first active use. When `new Child()` is called: (1) If `Parent` class is not yet initialized: run `Parent`'s static field initializers and static blocks in textual order. (2) Run `Child`'s static field initializers and static blocks. (3) For the instance: call `super()` (implicit or explicit) which runs Parent's instance field initializers + instance blocks + Parent constructor body. (4) Run Child's instance field initializers + instance blocks + Child constructor body. Static initialization is once per class load; instance initialization runs for every `new`.","distractorsMd":["On a **class**: cannot be subclassed (e.g., `String`, `Integer`); enables JIT devirtualization.",null,null]},{"id":"java/core_language#696963e57761","section":"java","module":"java/core_language","moduleName":"core language","sourceFile":"README.md","difficulty":"advanced","question":"Can a subclass in a different package call a protected method defined in a superclass?","answerFull":"Yes, but only through a reference of the subclass type (or a more derived type), not through a reference of the superclass type. In a class Child extends Parent (different packages), inside Child you can call this.hook() or new Child().hook() — both compile. But new Parent().hook() from inside Child is a compile error — the protected access through a superclass reference from a different package is not permitted. The rationale: the protected method is inherited by Child and belongs to Child's accessible members, but calling it on an arbitrary Parent reference would expose access that Child doesn't control.","correct":"Yes, but only through a reference of the subclass type (or a more derived type), not through a reference of the superclass type.","distractors":["(1) Static nested class: no outer reference, behaves like a top-level class.","Static dispatch: overloaded method resolution at compile time based on the *declared* parameter type.","equals(), hashCode(), toString(), clone() (protected), finalize() (deprecated), getClass(), wait() (3 overloads), notify(), notifyAll()."],"distractorIds":["java/core_language#86a94445cf15","java/core_language#786a8a2904cf","java/core_language#1f4ba304a50e"],"concepts":["superclass","protected","derived","package","subclass","defined"],"questionMd":"Can a subclass in a different package call a `protected` method defined in a superclass?","answerFullMd":"Yes, but only through a reference of the subclass type (or a more derived type), not through a reference of the superclass type. In a class `Child extends Parent` (different packages), inside `Child` you can call `this.hook()` or `new Child().hook()` — both compile. But `new Parent().hook()` from inside `Child` is a compile error — the protected access through a superclass reference from a different package is not permitted. The rationale: the protected method is inherited by `Child` and belongs to `Child`'s accessible members, but calling it on an arbitrary `Parent` reference would expose access that `Child` doesn't control.","distractorsMd":[null,null,"`equals()`, `hashCode()`, `toString()`, `clone()` (protected), `finalize()` (deprecated), `getClass()`, `wait()` (3 overloads), `notify()`, `notifyAll()`."]},{"id":"java/core_language#db235b81dea6","section":"java","module":"java/core_language","moduleName":"core language","sourceFile":"README.md","difficulty":"advanced","question":"What is the contract between equals() and hashCode(), and what breaks when you violate it?","answerFull":"The contract (from Object Javadoc): (1) Reflexivity: x.equals(x) is always true. (2) Symmetry: x.equals(y) iff y.equals(x). (3) Transitivity: if x.equals(y) and y.equals(z), then x.equals(z). (4) Consistency: multiple calls with unchanged objects return the same result. (5) Null-safety: x.equals(null) is always false. The hashCode contract: objects that are equals() must have the same hashCode() — violating this breaks HashMap, HashSet, Hashtable. Classic violation: override equals() without overriding hashCode(): java // BROKEN: equals() is overridden but hashCode() is not class Point { int x, y; @Override public boolean equals(Object o) { if (!(o instanceof Point p)) return false; return x == p.x && y == p.y; } // hashCode() inherited from Object uses identity -> different for equal points } Set<Point> set = new HashSet<>(); set.add(new Point(1, 2)); set.contains(new Point(1, 2)); // false! Different hash buckets // FIXED @Override public int hashCode() { return Objects.hash(x, y); } Also: never include mutable fields in hashCode() if the object will be stored in a HashMap or HashSet — mutating the key after insertion makes it permanently unreachable in the map.","correct":"The contract (from Object Javadoc): (1) Reflexivity: x.equals(x) is always true.","distractors":["equals(), hashCode(), toString(), clone() (protected), finalize() (deprecated), getClass(), wait() (3 overloads), notify(), notifyAll().","Two logically equal objects compute different hash codes (since Object.hashCode() is identity-based), so HashMap places them in different buckets.","Reflexive: x.equals(x) is true."],"distractorIds":["java/core_language#1f4ba304a50e","java/core_language#99f90343da6e","java/core_language#24f5bfbe28f1"],"concepts":["javadoc","reflexivity","hashcode","violate","breaks","contract"],"questionMd":"What is the contract between `equals()` and `hashCode()`, and what breaks when you violate it?","correctMd":"The contract (from `Object` Javadoc): (1) **Reflexivity**: `x.equals(x)` is always `true`.","answerFullMd":"The contract (from `Object` Javadoc): (1) **Reflexivity**: `x.equals(x)` is always `true`. (2) **Symmetry**: `x.equals(y)` iff `y.equals(x)`. (3) **Transitivity**: if `x.equals(y)` and `y.equals(z)`, then `x.equals(z)`. (4) **Consistency**: multiple calls with unchanged objects return the same result. (5) **Null-safety**: `x.equals(null)` is always `false`. The `hashCode` contract: **objects that are `equals()` must have the same `hashCode()`** — violating this breaks `HashMap`, `HashSet`, `Hashtable`. Classic violation: override `equals()` without overriding `hashCode()`: ```java // BROKEN: equals() is overridden but hashCode() is not class Point { int x, y; @Override public boolean equals(Object o) { if (!(o instanceof Point p)) return false; return x == p.x && y == p.y; } // hashCode() inherited from Object uses identity -> different for equal points } Set<Point> set = new HashSet<>(); set.add(new Point(1, 2)); set.contains(new Point(1, 2)); // false! Different hash buckets // FIXED @Override public int hashCode() { return Objects.hash(x, y); } ``` Also: never include mutable fields in `hashCode()` if the object will be stored in a `HashMap` or `HashSet` — mutating the key after insertion makes it permanently unreachable in the map.","distractorsMd":["`equals()`, `hashCode()`, `toString()`, `clone()` (protected), `finalize()` (deprecated), `getClass()`, `wait()` (3 overloads), `notify()`, `notifyAll()`.","Two logically equal objects compute different hash codes (since `Object.hashCode()` is identity-based), so `HashMap` places them in different buckets.","Reflexive: `x.equals(x)` is true."]},{"id":"java/design_patterns_in_java#537311300dc8","section":"java","module":"java/design_patterns_in_java","moduleName":"design patterns in java","sourceFile":"README.md","difficulty":"core","question":"What is the difference between JDK dynamic proxy and CGLIB?","answerFull":"JDK dynamic proxy works only for interfaces: it generates a class at runtime that implements the target interface and routes all calls to an InvocationHandler. CGLIB (Code Generation Library) works for concrete classes: it generates a subclass at runtime and overrides methods to route calls to an interceptor. Spring uses JDK proxy when the bean implements at least one interface; it falls back to CGLIB otherwise. Key limitation of CGLIB: cannot proxy final classes or final methods. ByteBuddy is the modern alternative, used by Mockito.","correct":"JDK dynamic proxy works only for interfaces: it generates a class at runtime that implements the target interface and routes all calls to an InvocationHandler.","distractors":["Observer (simple event bus): synchronous dispatch, simple listener interface, suitable for in-process events where listeners complete quickly.","Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an accept(Visitor) method on every node class and a concrete Visitor implementation for every type.","Template Method uses inheritance: the base class defines the algorithm skeleton as a final method that calls abstract/overridable \"steps.\" Subclasses override steps, not the algorithm."],"distractorIds":["java/design_patterns_in_java#0b50b694c166","java/design_patterns_in_java#de7d2926e8c0","java/design_patterns_in_java#1747f15368ad"],"concepts":["invocationhandler","difference","jdk","cglib","interfaces","implements"],"correctMd":"JDK dynamic proxy works only for interfaces: it generates a class at runtime that implements the target interface and routes all calls to an `InvocationHandler`.","answerFullMd":"JDK dynamic proxy works only for interfaces: it generates a class at runtime that implements the target interface and routes all calls to an `InvocationHandler`. CGLIB (Code Generation Library) works for concrete classes: it generates a subclass at runtime and overrides methods to route calls to an interceptor. Spring uses JDK proxy when the bean implements at least one interface; it falls back to CGLIB otherwise. Key limitation of CGLIB: cannot proxy `final` classes or `final` methods. ByteBuddy is the modern alternative, used by Mockito.","distractorsMd":[null,"Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an `accept(Visitor)` method on every node class and a concrete `Visitor` implementation for every type.","Template Method uses inheritance: the base class defines the algorithm skeleton as a `final` method that calls abstract/overridable \"steps.\" Subclasses override steps, not the algorithm."]},{"id":"java/design_patterns_in_java#ae5490202b60","section":"java","module":"java/design_patterns_in_java","moduleName":"design patterns in java","sourceFile":"README.md","difficulty":"core","question":"How does the Decorator pattern map to Java I/O streams?","answerFull":"InputStream is the abstract component. FileInputStream and ByteArrayInputStream are concrete components (sources). FilterInputStream is the abstract decorator — it holds a reference to a wrapped InputStream and delegates all methods to it. Concrete decorators like BufferedInputStream (adds 8KB buffer), DataInputStream (adds readInt()/readLong()), and GZIPInputStream each extend FilterInputStream and add one capability. The pattern enables arbitrary combination: new DataInputStream(new GZIPInputStream(new BufferedInputStream(new FileInputStream(\"f.gz\")))).","correct":"InputStream is the abstract component.","distractors":["Factory Method: a single factory method (usually abstract) that subclasses override to return different concrete products.","Runnable IS the Command pattern's command interface: execute() is run().","Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an accept(Visitor) method on every node class and a concrete Visitor implementation for every type."],"distractorIds":["java/design_patterns_in_java#0203a93f8199","java/design_patterns_in_java#e645b4b94e81","java/design_patterns_in_java#de7d2926e8c0"],"concepts":["inputstream","abstract","decorator","streams","component","map"],"correctMd":"`InputStream` is the abstract component.","answerFullMd":"`InputStream` is the abstract component. `FileInputStream` and `ByteArrayInputStream` are concrete components (sources). `FilterInputStream` is the abstract decorator — it holds a reference to a wrapped `InputStream` and delegates all methods to it. Concrete decorators like `BufferedInputStream` (adds 8KB buffer), `DataInputStream` (adds `readInt()`/`readLong()`), and `GZIPInputStream` each extend `FilterInputStream` and add one capability. The pattern enables arbitrary combination: `new DataInputStream(new GZIPInputStream(new BufferedInputStream(new FileInputStream(\"f.gz\"))))`.","distractorsMd":[null,"`Runnable` IS the Command pattern's command interface: `execute()` is `run()`.","Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an `accept(Visitor)` method on every node class and a concrete `Visitor` implementation for every type."]},{"id":"java/design_patterns_in_java#1747f15368ad","section":"java","module":"java/design_patterns_in_java","moduleName":"design patterns in java","sourceFile":"README.md","difficulty":"core","question":"What is the difference between Strategy and Template Method?","answerFull":"Template Method uses inheritance: the base class defines the algorithm skeleton as a final method that calls abstract/overridable \"steps.\" Subclasses override steps, not the algorithm. Strategy uses composition: the algorithm is encapsulated in a separate interface and injected. Template Method is simpler when the algorithm structure is fixed and you just want to vary specific steps. Strategy is more flexible: algorithms can change per-instance at runtime; strategies can be tested independently; classes using strategies can have multiple strategies. Effective Java prefers composition (Strategy) over inheritance (Template Method).","correct":"Template Method uses inheritance: the base class defines the algorithm skeleton as a final method that calls abstract/overridable \"steps.\" Subclasses override steps, not the algorithm.","distractors":["An immutable object has no mutable state after construction: all fields are final, no setters, all methods are pure functions that return new objects rather than modifying state.","InputStream is the abstract component.","Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an accept(Visitor) method on every node class and a concrete Visitor implementation for every type."],"distractorIds":["java/design_patterns_in_java#2d7050479c9d","java/design_patterns_in_java#ae5490202b60","java/design_patterns_in_java#de7d2926e8c0"],"concepts":["overridable","skeleton","difference","inheritance","override","subclasses"],"correctMd":"Template Method uses inheritance: the base class defines the algorithm skeleton as a `final` method that calls abstract/overridable \"steps.\" Subclasses override steps, not the algorithm.","answerFullMd":"Template Method uses inheritance: the base class defines the algorithm skeleton as a `final` method that calls abstract/overridable \"steps.\" Subclasses override steps, not the algorithm. Strategy uses composition: the algorithm is encapsulated in a separate interface and injected. Template Method is simpler when the algorithm structure is fixed and you just want to vary specific steps. Strategy is more flexible: algorithms can change per-instance at runtime; strategies can be tested independently; classes using strategies can have multiple strategies. Effective Java prefers composition (Strategy) over inheritance (Template Method).","distractorsMd":["An immutable object has no mutable state after construction: all fields are `final`, no setters, all methods are pure functions that return new objects rather than modifying state.","`InputStream` is the abstract component.","Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an `accept(Visitor)` method on every node class and a concrete `Visitor` implementation for every type."]},{"id":"java/design_patterns_in_java#0203a93f8199","section":"java","module":"java/design_patterns_in_java","moduleName":"design patterns in java","sourceFile":"README.md","difficulty":"core","question":"What is the difference between Factory Method and Abstract Factory?","answerFull":"Factory Method: a single factory method (usually abstract) that subclasses override to return different concrete products. \"Which class to instantiate\" is deferred to subclasses. Example: Calendar.getInstance() returns GregorianCalendar or Japanese calendar depending on locale. Abstract Factory: a factory interface/class that creates a *family* of related objects, ensuring they are compatible. Example: a UIComponentFactory with createButton(), createTextField(), createDialog() — one implementation for macOS, another for Windows. Use Factory Method when you need one product type with variant implementations; Abstract Factory when you need consistent families of multiple product types.","correct":"Factory Method: a single factory method (usually abstract) that subclasses override to return different concrete products.","distractors":["An immutable object has no mutable state after construction: all fields are final, no setters, all methods are pure functions that return new objects rather than modifying state.","Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an accept(Visitor) method on every node class and a concrete Visitor implementation for every type.","Creational: Singleton (Runtime.getRuntime(), enums), Factory Method (Calendar.getInstance(), Iterator from Iterable), Builder (StringBuilder, Stream.Builder, ProcessBuilder)."],"distractorIds":["java/design_patterns_in_java#2d7050479c9d","java/design_patterns_in_java#de7d2926e8c0","java/design_patterns_in_java#a181ef09f5db"],"concepts":["products","difference","concrete","factory","override","subclasses"],"answerFullMd":"Factory Method: a single factory method (usually abstract) that subclasses override to return different concrete products. \"Which class to instantiate\" is deferred to subclasses. Example: `Calendar.getInstance()` returns `GregorianCalendar` or Japanese calendar depending on locale. Abstract Factory: a factory interface/class that creates a *family* of related objects, ensuring they are compatible. Example: a `UIComponentFactory` with `createButton()`, `createTextField()`, `createDialog()` — one implementation for macOS, another for Windows. Use Factory Method when you need one product type with variant implementations; Abstract Factory when you need consistent families of multiple product types.","distractorsMd":["An immutable object has no mutable state after construction: all fields are `final`, no setters, all methods are pure functions that return new objects rather than modifying state.","Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an `accept(Visitor)` method on every node class and a concrete `Visitor` implementation for every type.","Creational: Singleton (`Runtime.getRuntime()`, enums), Factory Method (`Calendar.getInstance()`, `Iterator` from `Iterable`), Builder (`StringBuilder`, `Stream.Builder`, `ProcessBuilder`)."]},{"id":"java/design_patterns_in_java#8c9d0c2cdfb2","section":"java","module":"java/design_patterns_in_java","moduleName":"design patterns in java","sourceFile":"README.md","difficulty":"core","question":"How does the Flyweight pattern appear in Java's String interning?","answerFull":"The String pool is a canonical registry of String objects. String literals are automatically interned — \"hello\" == \"hello\" is true because both refer to the same pooled instance. String.intern() manually adds a string to the pool. Integer.valueOf() implements Flyweight for integers in [-128, 127]: values in this range always return the same cached Integer object. The Flyweight pattern saves memory when many clients use identical fine-grained objects, at the cost of immutability (shared objects cannot be mutated per-client).","correct":"The String pool is a canonical registry of String objects.","distractors":["Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an accept(Visitor) method on every node class and a concrete Visitor implementation for every type.","Runnable IS the Command pattern's command interface: execute() is run().","Flyweight shares one instance of an object among many contexts that need the same value, saving memory."],"distractorIds":["java/design_patterns_in_java#de7d2926e8c0","java/design_patterns_in_java#e645b4b94e81","java/design_patterns_in_java#33da7e2eac54"],"concepts":["interning","flyweight","canonical","appear","registry","string"],"correctMd":"The String pool is a canonical registry of `String` objects.","answerFullMd":"The String pool is a canonical registry of `String` objects. String literals are automatically interned — `\"hello\" == \"hello\"` is true because both refer to the same pooled instance. `String.intern()` manually adds a string to the pool. `Integer.valueOf()` implements Flyweight for integers in `[-128, 127]`: values in this range always return the same cached `Integer` object. The Flyweight pattern saves memory when many clients use identical fine-grained objects, at the cost of immutability (shared objects cannot be mutated per-client).","distractorsMd":["Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an `accept(Visitor)` method on every node class and a concrete `Visitor` implementation for every type.","`Runnable` IS the Command pattern's command interface: `execute()` is `run()`.",null]},{"id":"java/design_patterns_in_java#e55badafc8b3","section":"java","module":"java/design_patterns_in_java","moduleName":"design patterns in java","sourceFile":"README.md","difficulty":"intermediate","question":"Why is the enum singleton considered the safest singleton implementation?","answerFull":"Enum singleton is safe from four singleton-breaking attacks: (1) Reflection — you cannot call newInstance() on an enum via reflection; the JVM throws IllegalArgumentException. (2) Serialization — enums serialize by name and deserialize by finding the existing constant; no new instance is created. (3) Cloning — Enum overrides clone() to throw CloneNotSupportedException. (4) Class loader — two different class loaders cannot produce two instances of the same enum. None of these protections hold for the synchronized + volatile DCL singleton, which can be broken by reflection (setAccessible(true)) and serialization.","correct":"Enum singleton is safe from four singleton-breaking attacks: (1) Reflection — you cannot call newInstance() on an enum via reflection; the JVM throws IllegalArgumentException.","distractors":["Creational: Singleton (Runtime.getRuntime(), enums), Factory Method (Calendar.getInstance(), Iterator from Iterable), Builder (StringBuilder, Stream.Builder, ProcessBuilder).","Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an accept(Visitor) method on every node class and a concrete Visitor implementation for every type.","An immutable object has no mutable state after construction: all fields are final, no setters, all methods are pure functions that return new objects rather than modifying state."],"distractorIds":["java/design_patterns_in_java#a181ef09f5db","java/design_patterns_in_java#de7d2926e8c0","java/design_patterns_in_java#2d7050479c9d"],"concepts":["safest","considered","illegalargumentexception","newinstance","breaking","attacks"],"correctMd":"Enum singleton is safe from four singleton-breaking attacks: (1) Reflection — you cannot call `newInstance()` on an enum via reflection; the JVM throws `IllegalArgumentException`.","answerFullMd":"Enum singleton is safe from four singleton-breaking attacks: (1) Reflection — you cannot call `newInstance()` on an enum via reflection; the JVM throws `IllegalArgumentException`. (2) Serialization — enums serialize by name and deserialize by finding the existing constant; no new instance is created. (3) Cloning — `Enum` overrides `clone()` to throw `CloneNotSupportedException`. (4) Class loader — two different class loaders cannot produce two instances of the same enum. None of these protections hold for the `synchronized + volatile` DCL singleton, which can be broken by reflection (`setAccessible(true)`) and serialization.","distractorsMd":["Creational: Singleton (`Runtime.getRuntime()`, enums), Factory Method (`Calendar.getInstance()`, `Iterator` from `Iterable`), Builder (`StringBuilder`, `Stream.Builder`, `ProcessBuilder`).","Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an `accept(Visitor)` method on every node class and a concrete `Visitor` implementation for every type.","An immutable object has no mutable state after construction: all fields are `final`, no setters, all methods are pure functions that return new objects rather than modifying state."]},{"id":"java/design_patterns_in_java#e645b4b94e81","section":"java","module":"java/design_patterns_in_java","moduleName":"design patterns in java","sourceFile":"README.md","difficulty":"intermediate","question":"How does the Command pattern relate to Runnable and Callable?","answerFull":"Runnable IS the Command pattern's command interface: execute() is run(). The concrete command is the lambda or anonymous class you create. The invoker is ExecutorService which calls run() at some future time, decoupling task creation from execution. Callable<V> extends this by making the command return a value (and declare checked exceptions). Command's \"history/undo\" capability is not in Runnable but can be added by wrapping commands in a custom class that also implements an undo() method.","correct":"Runnable IS the Command pattern's command interface: execute() is run().","distractors":["JDK dynamic proxy works only for interfaces: it generates a class at runtime that implements the target interface and routes all calls to an InvocationHandler.","Observer (simple event bus): synchronous dispatch, simple listener interface, suitable for in-process events where listeners complete quickly.","Command encapsulates a request as an object, separating the invoker (who triggers the action) from the receiver (who executes it)."],"distractorIds":["java/design_patterns_in_java#537311300dc8","java/design_patterns_in_java#0b50b694c166","java/design_patterns_in_java#ef59f52cc1a6"],"concepts":["runnable","command","callable","execute","interface","pattern"],"questionMd":"How does the Command pattern relate to `Runnable` and `Callable`?","correctMd":"`Runnable` IS the Command pattern's command interface: `execute()` is `run()`.","answerFullMd":"`Runnable` IS the Command pattern's command interface: `execute()` is `run()`. The concrete command is the lambda or anonymous class you create. The invoker is `ExecutorService` which calls `run()` at some future time, decoupling task creation from execution. `Callable<V>` extends this by making the command return a value (and declare checked exceptions). Command's \"history/undo\" capability is not in `Runnable` but can be added by wrapping commands in a custom class that also implements an `undo()` method.","distractorsMd":["JDK dynamic proxy works only for interfaces: it generates a class at runtime that implements the target interface and routes all calls to an `InvocationHandler`.",null,null]},{"id":"java/design_patterns_in_java#a181ef09f5db","section":"java","module":"java/design_patterns_in_java","moduleName":"design patterns in java","sourceFile":"README.md","difficulty":"intermediate","question":"Which GoF patterns appear prominently in the JDK?","answerFull":"Creational: Singleton (Runtime.getRuntime(), enums), Factory Method (Calendar.getInstance(), Iterator from Iterable), Builder (StringBuilder, Stream.Builder, ProcessBuilder). Structural: Decorator (InputStream/OutputStream/Reader/Writer chains), Proxy (JDK Proxy, JDBC connection wrappers), Adapter (Arrays.asList(), InputStreamReader), Flyweight (String pool, Integer.valueOf() cache), Facade (Files, Executors). Behavioral: Iterator (java.util.Iterator), Strategy (Comparator, ThreadFactory, RejectedExecutionHandler), Command (Runnable, Callable), Observer (EventListener hierarchy), Template Method (AbstractList, HttpServlet.service()).","correct":"Creational: Singleton (Runtime.getRuntime(), enums), Factory Method (Calendar.getInstance(), Iterator from Iterable), Builder (StringBuilder, Stream.Builder, ProcessBuilder).","distractors":["Template Method uses inheritance: the base class defines the algorithm skeleton as a final method that calls abstract/overridable \"steps.\" Subclasses override steps, not the algorithm.","Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an accept(Visitor) method on every node class and a concrete Visitor implementation for every type.","Enum singleton is safe from four singleton-breaking attacks: (1) Reflection — you cannot call newInstance() on an enum via reflection; the JVM throws IllegalArgumentException."],"distractorIds":["java/design_patterns_in_java#1747f15368ad","java/design_patterns_in_java#de7d2926e8c0","java/design_patterns_in_java#e55badafc8b3"],"concepts":["creational","getinstance","gof","processbuilder","enums","getruntime"],"correctMd":"Creational: Singleton (`Runtime.getRuntime()`, enums), Factory Method (`Calendar.getInstance()`, `Iterator` from `Iterable`), Builder (`StringBuilder`, `Stream.Builder`, `ProcessBuilder`).","answerFullMd":"Creational: Singleton (`Runtime.getRuntime()`, enums), Factory Method (`Calendar.getInstance()`, `Iterator` from `Iterable`), Builder (`StringBuilder`, `Stream.Builder`, `ProcessBuilder`). Structural: Decorator (`InputStream`/`OutputStream`/`Reader`/`Writer` chains), Proxy (JDK `Proxy`, JDBC connection wrappers), Adapter (`Arrays.asList()`, `InputStreamReader`), Flyweight (`String` pool, `Integer.valueOf()` cache), Facade (`Files`, `Executors`). Behavioral: Iterator (`java.util.Iterator`), Strategy (`Comparator`, `ThreadFactory`, `RejectedExecutionHandler`), Command (`Runnable`, `Callable`), Observer (`EventListener` hierarchy), Template Method (`AbstractList`, `HttpServlet.service()`).","distractorsMd":["Template Method uses inheritance: the base class defines the algorithm skeleton as a `final` method that calls abstract/overridable \"steps.\" Subclasses override steps, not the algorithm.","Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an `accept(Visitor)` method on every node class and a concrete `Visitor` implementation for every type.","Enum singleton is safe from four singleton-breaking attacks: (1) Reflection — you cannot call `newInstance()` on an enum via reflection; the JVM throws `IllegalArgumentException`."]},{"id":"java/design_patterns_in_java#e8d494dbd99c","section":"java","module":"java/design_patterns_in_java","moduleName":"design patterns in java","sourceFile":"README.md","difficulty":"intermediate","question":"What is the Producer-Consumer pattern and how does BlockingQueue implement it?","answerFull":"Producer-Consumer decouples the rate of production from the rate of consumption via a shared bounded buffer. BlockingQueue is the buffer: put(item) blocks when full; take() blocks when empty. This creates natural backpressure — producers slow down automatically when consumers can't keep up. Implementation: one or more producer threads call queue.put(item), one or more consumer threads call queue.take(). No explicit wait()/notify() needed — BlockingQueue handles all synchronization. Use ArrayBlockingQueue(capacity) for bounded buffers, LinkedTransferQueue for high-throughput handoff.","correct":"Producer-Consumer decouples the rate of production from the rate of consumption via a shared bounded buffer.","distractors":["Runnable IS the Command pattern's command interface: execute() is run().","An immutable object has no mutable state after construction: all fields are final, no setters, all methods are pure functions that return new objects rather than modifying state.","Command encapsulates a request as an object, separating the invoker (who triggers the action) from the receiver (who executes it)."],"distractorIds":["java/design_patterns_in_java#e645b4b94e81","java/design_patterns_in_java#2d7050479c9d","java/design_patterns_in_java#ef59f52cc1a6"],"concepts":["blockingqueue","consumption","decouples","bounded","producer","implement"],"questionMd":"What is the Producer-Consumer pattern and how does `BlockingQueue` implement it?","answerFullMd":"Producer-Consumer decouples the rate of production from the rate of consumption via a shared bounded buffer. `BlockingQueue` is the buffer: `put(item)` blocks when full; `take()` blocks when empty. This creates natural backpressure — producers slow down automatically when consumers can't keep up. Implementation: one or more producer threads call `queue.put(item)`, one or more consumer threads call `queue.take()`. No explicit `wait()`/`notify()` needed — `BlockingQueue` handles all synchronization. Use `ArrayBlockingQueue(capacity)` for bounded buffers, `LinkedTransferQueue` for high-throughput handoff.","distractorsMd":["`Runnable` IS the Command pattern's command interface: `execute()` is `run()`.","An immutable object has no mutable state after construction: all fields are `final`, no setters, all methods are pure functions that return new objects rather than modifying state.",null]},{"id":"java/design_patterns_in_java#0b50b694c166","section":"java","module":"java/design_patterns_in_java","moduleName":"design patterns in java","sourceFile":"README.md","difficulty":"intermediate","question":"When would you use Observer (event bus) vs reactive streams?","answerFull":"Observer (simple event bus): synchronous dispatch, simple listener interface, suitable for in-process events where listeners complete quickly. Works well for GUI events, domain events in a monolith. Reactive streams (Flow, RxJava, Project Reactor): asynchronous, backpressure-aware, composable operators (map, filter, flatMap). Use when: consumers can fall behind producers (need backpressure), operations are async (HTTP calls per event), or you need complex event transformations. Rule: if your observer callbacks are simple method calls and you don't need backpressure, stick with simple Observer; reactive is heavier machinery.","correct":"Observer (simple event bus): synchronous dispatch, simple listener interface, suitable for in-process events where listeners complete quickly.","distractors":["JDK dynamic proxy works only for interfaces: it generates a class at runtime that implements the target interface and routes all calls to an InvocationHandler.","Runnable IS the Command pattern's command interface: execute() is run().","An immutable object has no mutable state after construction: all fields are final, no setters, all methods are pure functions that return new objects rather than modifying state."],"distractorIds":["java/design_patterns_in_java#537311300dc8","java/design_patterns_in_java#e645b4b94e81","java/design_patterns_in_java#2d7050479c9d"],"concepts":["listeners","suitable","observer","listener","bus","quickly"],"answerFullMd":"Observer (simple event bus): synchronous dispatch, simple listener interface, suitable for in-process events where listeners complete quickly. Works well for GUI events, domain events in a monolith. Reactive streams (`Flow`, RxJava, Project Reactor): asynchronous, backpressure-aware, composable operators (`map`, `filter`, `flatMap`). Use when: consumers can fall behind producers (need backpressure), operations are async (HTTP calls per event), or you need complex event transformations. Rule: if your observer callbacks are simple method calls and you don't need backpressure, stick with simple Observer; reactive is heavier machinery.","distractorsMd":["JDK dynamic proxy works only for interfaces: it generates a class at runtime that implements the target interface and routes all calls to an `InvocationHandler`.","`Runnable` IS the Command pattern's command interface: `execute()` is `run()`.","An immutable object has no mutable state after construction: all fields are `final`, no setters, all methods are pure functions that return new objects rather than modifying state."]},{"id":"java/design_patterns_in_java#de7d2926e8c0","section":"java","module":"java/design_patterns_in_java","moduleName":"design patterns in java","sourceFile":"README.md","difficulty":"advanced","question":"Why is the Visitor pattern rarely used in Java compared to Haskell/Scala pattern matching?","answerFull":"Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an accept(Visitor) method on every node class and a concrete Visitor implementation for every type. Java 21's pattern matching for switch (switch (shape) { case Circle c -> ...; case Rectangle r -> ...; }) makes Visitor mostly obsolete for closed hierarchies: sealed classes + exhaustive switch expressions provide the same exhaustiveness guarantee with 80% less code. Visitor still has a place for open hierarchies (where you can't add accept() to library classes) and when operations are many and frequently added.","correct":"Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an accept(Visitor) method on every node class and a concrete Visitor implementation for every type.","distractors":["JDK dynamic proxy works only for interfaces: it generates a class at runtime that implements the target interface and routes all calls to an InvocationHandler.","Runnable IS the Command pattern's command interface: execute() is run().","Factory Method: a single factory method (usually abstract) that subclasses override to return different concrete products."],"distractorIds":["java/design_patterns_in_java#537311300dc8","java/design_patterns_in_java#e645b4b94e81","java/design_patterns_in_java#0203a93f8199"],"concepts":["visitor","compared","modifying","rarely","concrete","hierarchy"],"correctMd":"Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an `accept(Visitor)` method on every node class and a concrete `Visitor` implementation for every type.","answerFullMd":"Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an `accept(Visitor)` method on every node class and a concrete `Visitor` implementation for every type. Java 21's pattern matching for switch (`switch (shape) { case Circle c -> ...; case Rectangle r -> ...; }`) makes Visitor mostly obsolete for closed hierarchies: sealed classes + exhaustive switch expressions provide the same exhaustiveness guarantee with 80% less code. Visitor still has a place for open hierarchies (where you can't add `accept()` to library classes) and when operations are many and frequently added.","distractorsMd":["JDK dynamic proxy works only for interfaces: it generates a class at runtime that implements the target interface and routes all calls to an `InvocationHandler`.","`Runnable` IS the Command pattern's command interface: `execute()` is `run()`.",null]},{"id":"java/design_patterns_in_java#2d7050479c9d","section":"java","module":"java/design_patterns_in_java","moduleName":"design patterns in java","sourceFile":"README.md","difficulty":"advanced","question":"What is the Immutable Object concurrency pattern and why does it eliminate synchronization?","answerFull":"An immutable object has no mutable state after construction: all fields are final, no setters, all methods are pure functions that return new objects rather than modifying state. Because the object can never change, threads can share it freely without synchronization — there is no write that could conflict with a read, so the happens-before rules are irrelevant. Examples: String, BigDecimal, Java records. Construction must be done carefully: all fields must be set before the reference escapes the constructor (this must not be passed to other threads inside the constructor).","correct":"An immutable object has no mutable state after construction: all fields are final, no setters, all methods are pure functions that return new objects rather than modifying state.","distractors":["The String pool is a canonical registry of String objects.","Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an accept(Visitor) method on every node class and a concrete Visitor implementation for every type.","Factory Method: a single factory method (usually abstract) that subclasses override to return different concrete products."],"distractorIds":["java/design_patterns_in_java#8c9d0c2cdfb2","java/design_patterns_in_java#de7d2926e8c0","java/design_patterns_in_java#0203a93f8199"],"concepts":["eliminate","setters","synchronization","modifying","construction","pure"],"correctMd":"An immutable object has no mutable state after construction: all fields are `final`, no setters, all methods are pure functions that return new objects rather than modifying state.","answerFullMd":"An immutable object has no mutable state after construction: all fields are `final`, no setters, all methods are pure functions that return new objects rather than modifying state. Because the object can never change, threads can share it freely without synchronization — there is no write that could conflict with a read, so the happens-before rules are irrelevant. Examples: `String`, `BigDecimal`, Java `record`s. Construction must be done carefully: all fields must be set before the reference escapes the constructor (`this` must not be passed to other threads inside the constructor).","distractorsMd":["The String pool is a canonical registry of `String` objects.","Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an `accept(Visitor)` method on every node class and a concrete `Visitor` implementation for every type.",null]},{"id":"java/design_patterns_in_java#ed9b68af70cc","section":"java","module":"java/design_patterns_in_java","moduleName":"design patterns in java","sourceFile":"README.md","difficulty":"advanced","question":"How does the Decorator pattern differ from inheritance, and how does Java I/O use it in practice?","answerFull":"Inheritance is compile-time, static, and \"is-a\" — a subclass permanently adds behaviour. Decorator is runtime, dynamic, and \"wraps-a\" — a Decorator holds a reference to the decorated component and delegates, adding behaviour before/after the delegation. The critical advantage: behaviours can be stacked in any combination without a class explosion. Java I/O is the canonical Decorator example: mermaid flowchart TD classDef io fill:#61afef,stroke:#2e86c1,color:#1a1a1a,font-weight:bold classDef frozen fill:#c678dd,stroke:#9b59b6,color:#fff classDef train fill:#98c379,stroke:#27ae60,color:#1a1a1a classDef mathOp fill:#d19a66,stroke:#e67e22,color:#1a1a1a,font-weight:bold classDef lossN fill:#e06c75,stroke:#c0392b,color:#fff,font-weight:bold classDef req fill:#56b6c2,stroke:#0097a7,color:#1a1a1a classDef base fill:#e5c07b,stroke:#f39c12,color:#1a1a1a IS[\"InputStream\\n(Component interface)\"] --> FIS[\"FileInputStream\\n(Concrete Component - reads bytes from file)\"] IS --> FilterIS[\"FilterInputStream\\n(Abstract Decorator - wraps InputStream)\"] FilterIS --> BIS[\"BufferedInputStream\\n(Concrete Decorator - adds buffering)\"] FilterIS --> GIS[\"GZIPInputStream\\n(Concrete Decorator - adds decompression)\"] class IS base class FIS io class FilterIS,BIS,GIS mathOp java // Stacked: read a gzip-compressed, buffered file InputStream in = new GZIPInputStream(new BufferedInputStream(new FileInputStream(\"data.gz\"))); Pitfall: instanceof checks on a decorated object fail (a BufferedInputStream is not a FileInputStream) — avoid coupling code to concrete types in a Decorator chain.","correct":"Inheritance is compile-time, static, and \"is-a\" — a subclass permanently adds behaviour.","distractors":["Template Method uses inheritance: the base class defines the algorithm skeleton as a final method that calls abstract/overridable \"steps.\" Subclasses override steps, not the algorithm.","Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an accept(Visitor) method on every node class and a concrete Visitor implementation for every type.","Runnable IS the Command pattern's command interface: execute() is run()."],"distractorIds":["java/design_patterns_in_java#1747f15368ad","java/design_patterns_in_java#de7d2926e8c0","java/design_patterns_in_java#e645b4b94e81"],"concepts":["behaviour","permanently","practice","differ","inheritance","decorator"],"answerFullMd":"Inheritance is compile-time, static, and \"is-a\" — a subclass permanently adds behaviour. Decorator is runtime, dynamic, and \"wraps-a\" — a Decorator holds a reference to the decorated component and delegates, adding behaviour before/after the delegation. The critical advantage: behaviours can be stacked in any combination without a class explosion. Java I/O is the canonical Decorator example: ```mermaid flowchart TD classDef io fill:#61afef,stroke:#2e86c1,color:#1a1a1a,font-weight:bold classDef frozen fill:#c678dd,stroke:#9b59b6,color:#fff classDef train fill:#98c379,stroke:#27ae60,color:#1a1a1a classDef mathOp fill:#d19a66,stroke:#e67e22,color:#1a1a1a,font-weight:bold classDef lossN fill:#e06c75,stroke:#c0392b,color:#fff,font-weight:bold classDef req fill:#56b6c2,stroke:#0097a7,color:#1a1a1a classDef base fill:#e5c07b,stroke:#f39c12,color:#1a1a1a IS[\"InputStream\\n(Component interface)\"] --> FIS[\"FileInputStream\\n(Concrete Component - reads bytes from file)\"] IS --> FilterIS[\"FilterInputStream\\n(Abstract Decorator - wraps InputStream)\"] FilterIS --> BIS[\"BufferedInputStream\\n(Concrete Decorator - adds buffering)\"] FilterIS --> GIS[\"GZIPInputStream\\n(Concrete Decorator - adds decompression)\"] class IS base class FIS io class FilterIS,BIS,GIS mathOp ``` ```java // Stacked: read a gzip-compressed, buffered file InputStream in = new GZIPInputStream(new BufferedInputStream(new FileInputStream(\"data.gz\"))); ``` Pitfall: `instanceof` checks on a decorated object fail (a `BufferedInputStream` is not a `FileInputStream`) — avoid coupling code to concrete types in a Decorator chain.","distractorsMd":["Template Method uses inheritance: the base class defines the algorithm skeleton as a `final` method that calls abstract/overridable \"steps.\" Subclasses override steps, not the algorithm.","Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an `accept(Visitor)` method on every node class and a concrete `Visitor` implementation for every type.","`Runnable` IS the Command pattern's command interface: `execute()` is `run()`."]},{"id":"java/design_patterns_in_java#ef59f52cc1a6","section":"java","module":"java/design_patterns_in_java","moduleName":"design patterns in java","sourceFile":"README.md","difficulty":"advanced","question":"What is the Command pattern and how do you add undo/redo functionality to it?","answerFull":"Command encapsulates a request as an object, separating the invoker (who triggers the action) from the receiver (who executes it). For undo/redo, each Command exposes both execute() and undo(), and the invoker maintains an executeStack and an undoStack: java interface Command { void execute(); void undo(); } class TextEditor { private final Deque<Command> history = new ArrayDeque<>(); private final Deque<Command> redoStack = new ArrayDeque<>(); void execute(Command cmd) { cmd.execute(); history.push(cmd); redoStack.clear(); // new action invalidates redo history } void undo() { if (!history.isEmpty()) { Command cmd = history.pop(); cmd.undo(); redoStack.push(cmd); } } void redo() { if (!redoStack.isEmpty()) { Command cmd = redoStack.pop(); cmd.execute(); history.push(cmd); } } } Lambda expressions can implement single-method Command for simple actions. For undo, pair two lambdas: new LambdaCommand(() -> doSomething(), () -> undoSomething()).","correct":"Command encapsulates a request as an object, separating the invoker (who triggers the action) from the receiver (who executes it).","distractors":["Flyweight shares one instance of an object among many contexts that need the same value, saving memory.","Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an accept(Visitor) method on every node class and a concrete Visitor implementation for every type.","Runnable IS the Command pattern's command interface: execute() is run()."],"distractorIds":["java/design_patterns_in_java#33da7e2eac54","java/design_patterns_in_java#de7d2926e8c0","java/design_patterns_in_java#e645b4b94e81"],"concepts":["invoker","separating","encapsulates","redo","functionality","undo"],"answerFullMd":"Command encapsulates a request as an object, separating the invoker (who triggers the action) from the receiver (who executes it). For undo/redo, each `Command` exposes both `execute()` and `undo()`, and the invoker maintains an `executeStack` and an `undoStack`: ```java interface Command { void execute(); void undo(); } class TextEditor { private final Deque<Command> history = new ArrayDeque<>(); private final Deque<Command> redoStack = new ArrayDeque<>(); void execute(Command cmd) { cmd.execute(); history.push(cmd); redoStack.clear(); // new action invalidates redo history } void undo() { if (!history.isEmpty()) { Command cmd = history.pop(); cmd.undo(); redoStack.push(cmd); } } void redo() { if (!redoStack.isEmpty()) { Command cmd = redoStack.pop(); cmd.execute(); history.push(cmd); } } } ``` Lambda expressions can implement single-method `Command` for simple actions. For undo, pair two lambdas: `new LambdaCommand(() -> doSomething(), () -> undoSomething())`.","distractorsMd":[null,"Visitor solves \"add an operation to a type hierarchy without modifying each type.\" In Java, it requires an `accept(Visitor)` method on every node class and a concrete `Visitor` implementation for every type.","`Runnable` IS the Command pattern's command interface: `execute()` is `run()`."]},{"id":"java/design_patterns_in_java#33da7e2eac54","section":"java","module":"java/design_patterns_in_java","moduleName":"design patterns in java","sourceFile":"README.md","difficulty":"advanced","question":"What is the Flyweight pattern, and where does the JDK use it concretely?","answerFull":"Flyweight shares one instance of an object among many contexts that need the same value, saving memory. The shared state (intrinsic) is stored in the flyweight; the per-context state (extrinsic) is passed on each call. JDK examples: (1) String pool — String.intern() returns the canonical instance; all string literals are interned automatically. (2) Integer/Long/Short/Byte/Character cache — Integer.valueOf(n) returns a cached instance for n ∈ [−128, 127]. (3) Boolean.TRUE / Boolean.FALSE — two singletons; Boolean.valueOf(true) never allocates. (4) Font in AWT — glyph bitmaps are shared flyweights; x/y position is extrinsic. Interview angle: the Integer cache is Flyweight in disguise — describe it in those terms and it reads much better than \"it's just a cache.\" Limitation: Flyweights cannot have mutable intrinsic state; any mutation would affect all users simultaneously.","correct":"Flyweight shares one instance of an object among many contexts that need the same value, saving memory.","distractors":["Runnable IS the Command pattern's command interface: execute() is run().","JDK dynamic proxy works only for interfaces: it generates a class at runtime that implements the target interface and routes all calls to an InvocationHandler.","Command encapsulates a request as an object, separating the invoker (who triggers the action) from the receiver (who executes it)."],"distractorIds":["java/design_patterns_in_java#e645b4b94e81","java/design_patterns_in_java#537311300dc8","java/design_patterns_in_java#ef59f52cc1a6"],"concepts":["flyweight","saving","among","contexts","jdk","shares"],"answerFullMd":"Flyweight shares one instance of an object among many contexts that need the same value, saving memory. The shared state (intrinsic) is stored in the flyweight; the per-context state (extrinsic) is passed on each call. JDK examples: (1) **String pool** — `String.intern()` returns the canonical instance; all string literals are interned automatically. (2) **Integer/Long/Short/Byte/Character cache** — `Integer.valueOf(n)` returns a cached instance for n ∈ [−128, 127]. (3) **`Boolean.TRUE` / `Boolean.FALSE`** — two singletons; `Boolean.valueOf(true)` never allocates. (4) **`Font` in AWT** — glyph bitmaps are shared flyweights; x/y position is extrinsic. Interview angle: the Integer cache is Flyweight in disguise — describe it in those terms and it reads much better than \"it's just a cache.\" Limitation: Flyweights cannot have mutable intrinsic state; any mutation would affect all users simultaneously.","distractorsMd":["`Runnable` IS the Command pattern's command interface: `execute()` is `run()`.","JDK dynamic proxy works only for interfaces: it generates a class at runtime that implements the target interface and routes all calls to an `InvocationHandler`.",null]},{"id":"java/exceptions_and_io#29da85ca609b","section":"java","module":"java/exceptions_and_io","moduleName":"exceptions and io","sourceFile":"README.md","difficulty":"core","question":"What is exception chaining and why is it important?","answerFull":"Exception chaining preserves the original cause when wrapping exceptions: new ServiceException(\"DB failed\", originalCause). This is critical because: (1) the catch block at a higher level sees a meaningful high-level exception; (2) the stack trace includes both the wrapper's context AND the root cause, making diagnosis possible. Without chaining, catching a low-level SQLException and rethrowing new ServiceException(\"failed\") loses the original stack trace — you only see where the wrapping occurred, not the actual DB error.","correct":"Exception chaining preserves the original cause when wrapping exceptions: new ServiceException(\"DB failed\", originalCause).","distractors":["The exception thrown in finally *replaces* the original exception from the try block.","When a Runnable submitted to an ExecutorService throws an unchecked exception, it is silently swallowed — the exception is stored in the Future but nothing else happens unless Future.get() is called.","The Decorator pattern adds behavior to objects by wrapping them in objects with the same interface."],"distractorIds":["java/exceptions_and_io#b26c2115fae9","java/exceptions_and_io#3924a75d1b46","java/exceptions_and_io#ba6958461a97"],"concepts":["originalcause","serviceexception","chaining","wrapping","important","preserves"],"correctMd":"Exception chaining preserves the original cause when wrapping exceptions: `new ServiceException(\"DB failed\", originalCause)`.","answerFullMd":"Exception chaining preserves the original cause when wrapping exceptions: `new ServiceException(\"DB failed\", originalCause)`. This is critical because: (1) the catch block at a higher level sees a meaningful high-level exception; (2) the stack trace includes both the wrapper's context AND the root cause, making diagnosis possible. Without chaining, catching a low-level `SQLException` and rethrowing `new ServiceException(\"failed\")` loses the original stack trace — you only see where the wrapping occurred, not the actual DB error.","distractorsMd":["The exception thrown in `finally` *replaces* the original exception from the `try` block.","When a `Runnable` submitted to an `ExecutorService` throws an unchecked exception, it is silently swallowed — the exception is stored in the `Future` but nothing else happens unless `Future.get()` is called.",null]},{"id":"java/exceptions_and_io#b26c2115fae9","section":"java","module":"java/exceptions_and_io","moduleName":"exceptions and io","sourceFile":"README.md","difficulty":"core","question":"What happens if a finally block throws an exception?","answerFull":"The exception thrown in finally *replaces* the original exception from the try block. The original exception is silently lost — it's not added as suppressed (unlike try-with-resources). This is a classic bug in pre-Java 7 code that used try-finally for resource cleanup. try-with-resources solves this correctly: the exception from the try body is primary, and exceptions from close() are added as suppressed exceptions via Throwable.addSuppressed().","correct":"The exception thrown in finally *replaces* the original exception from the try block.","distractors":["A suppressed exception is one that was thrown during the cleanup phase (e.g., close() in try-with-resources) when another exception was already in flight from the try body.","When a Runnable submitted to an ExecutorService throws an unchecked exception, it is silently swallowed — the exception is stored in the Future but nothing else happens unless Future.get() is called.","When finally throws an exception, it suppresses any exception from try or catch — the finally exception propagates and the others are silently discarded."],"distractorIds":["java/exceptions_and_io#1f9b6442e130","java/exceptions_and_io#3924a75d1b46","java/exceptions_and_io#731df99db64d"],"concepts":["thrown","finally","try","happens","original","throws"],"questionMd":"What happens if a `finally` block throws an exception?","correctMd":"The exception thrown in `finally` *replaces* the original exception from the `try` block.","answerFullMd":"The exception thrown in `finally` *replaces* the original exception from the `try` block. The original exception is silently lost — it's not added as suppressed (unlike `try-with-resources`). This is a classic bug in pre-Java 7 code that used try-finally for resource cleanup. `try-with-resources` solves this correctly: the exception from the `try` body is primary, and exceptions from `close()` are added as suppressed exceptions via `Throwable.addSuppressed()`.","distractorsMd":["A suppressed exception is one that was thrown during the cleanup phase (e.g., `close()` in `try-with-resources`) when another exception was already in flight from the `try` body.","When a `Runnable` submitted to an `ExecutorService` throws an unchecked exception, it is silently swallowed — the exception is stored in the `Future` but nothing else happens unless `Future.get()` is called.","When `finally` throws an exception, it **suppresses** any exception from `try` or `catch` — the `finally` exception propagates and the others are silently discarded."]},{"id":"java/exceptions_and_io#3680f2d6e144","section":"java","module":"java/exceptions_and_io","moduleName":"exceptions and io","sourceFile":"README.md","difficulty":"core","question":"How does try-with-resources handle multiple resources and their exceptions?","answerFull":"Resources are closed in LIFO (reverse declaration) order. If the try body throws exception E1, resources are closed. If closing a resource throws exception E2, E1 is the primary exception and E2 is added as a suppressed exception (E1.getSuppressed() returns [E2]). If multiple resources' close() methods throw, each close exception is added as suppressed in close order. The try body exception always takes priority — this preserves the meaningful original error.","correct":"Resources are closed in LIFO (reverse declaration) order.","distractors":["The exception thrown in finally *replaces* the original exception from the try block.","A suppressed exception is one that was thrown during the cleanup phase (e.g., close() in try-with-resources) when another exception was already in flight from the try body.","When finally throws an exception, it suppresses any exception from try or catch — the finally exception propagates and the others are silently discarded."],"distractorIds":["java/exceptions_and_io#b26c2115fae9","java/exceptions_and_io#1f9b6442e130","java/exceptions_and_io#731df99db64d"],"concepts":["declaration","lifo","reverse","exceptions","handle","try"],"questionMd":"How does `try-with-resources` handle multiple resources and their exceptions?","answerFullMd":"Resources are closed in LIFO (reverse declaration) order. If the `try` body throws exception E1, resources are closed. If closing a resource throws exception E2, E1 is the primary exception and E2 is added as a suppressed exception (`E1.getSuppressed()` returns `[E2]`). If multiple resources' `close()` methods throw, each close exception is added as suppressed in close order. The `try` body exception always takes priority — this preserves the meaningful original error.","distractorsMd":["The exception thrown in `finally` *replaces* the original exception from the `try` block.","A suppressed exception is one that was thrown during the cleanup phase (e.g., `close()` in `try-with-resources`) when another exception was already in flight from the `try` body.","When `finally` throws an exception, it **suppresses** any exception from `try` or `catch` — the `finally` exception propagates and the others are silently discarded."]},{"id":"java/exceptions_and_io#1f9b6442e130","section":"java","module":"java/exceptions_and_io","moduleName":"exceptions and io","sourceFile":"README.md","difficulty":"core","question":"What is a suppressed exception?","answerFull":"A suppressed exception is one that was thrown during the cleanup phase (e.g., close() in try-with-resources) when another exception was already in flight from the try body. Java 7 added Throwable.addSuppressed(Throwable) and Throwable.getSuppressed(). The primary exception propagates; suppressed exceptions are attached to it. Accessing them: catch (IOException e) { Throwable[] suppressed = e.getSuppressed(); }. Logging frameworks should log suppressed exceptions too.","correct":"A suppressed exception is one that was thrown during the cleanup phase (e.g., close() in try-with-resources) when another exception was already in flight from the try body.","distractors":["Exception chaining preserves the original cause when wrapping exceptions: new ServiceException(\"DB failed\", originalCause).","When a Runnable submitted to an ExecutorService throws an unchecked exception, it is silently swallowed — the exception is stored in the Future but nothing else happens unless Future.get() is called.","The exception thrown in finally *replaces* the original exception from the try block."],"distractorIds":["java/exceptions_and_io#29da85ca609b","java/exceptions_and_io#3924a75d1b46","java/exceptions_and_io#b26c2115fae9"],"concepts":["suppressed","cleanup","thrown","flight","close","try"],"correctMd":"A suppressed exception is one that was thrown during the cleanup phase (e.g., `close()` in `try-with-resources`) when another exception was already in flight from the `try` body.","answerFullMd":"A suppressed exception is one that was thrown during the cleanup phase (e.g., `close()` in `try-with-resources`) when another exception was already in flight from the `try` body. Java 7 added `Throwable.addSuppressed(Throwable)` and `Throwable.getSuppressed()`. The primary exception propagates; suppressed exceptions are attached to it. Accessing them: `catch (IOException e) { Throwable[] suppressed = e.getSuppressed(); }`. Logging frameworks should log suppressed exceptions too.","distractorsMd":["Exception chaining preserves the original cause when wrapping exceptions: `new ServiceException(\"DB failed\", originalCause)`.","When a `Runnable` submitted to an `ExecutorService` throws an unchecked exception, it is silently swallowed — the exception is stored in the `Future` but nothing else happens unless `Future.get()` is called.","The exception thrown in `finally` *replaces* the original exception from the `try` block."]},{"id":"java/exceptions_and_io#e1b534784978","section":"java","module":"java/exceptions_and_io","moduleName":"exceptions and io","sourceFile":"README.md","difficulty":"intermediate","question":"Why is Java serialization a security risk?","answerFull":"Deserialization invokes readObject() on each object being deserialized, which can execute arbitrary Java code. Attackers craft \"gadget chains\" — sequences of classes in common libraries (Apache Commons Collections, Spring, etc.) whose readObject() methods, when chained together, execute shell commands. This has affected WebLogic, Jenkins, JBoss, Apache Camel. The attack doesn't require any special privileges — just the ability to send bytes to ObjectInputStream.readObject(). Fix: use JSON/protobuf; if Java serialization is required, use ObjectInputFilter allowlisting.","correct":"Deserialization invokes readObject() on each object being deserialized, which can execute arbitrary Java code.","distractors":["transient marks a field that should be excluded from serialization.","After an object is deserialized, if it defines a readResolve() method, the JVM calls it and uses the returned object instead of the deserialized one.","serialVersionUID is a 64-bit long stored in the serialized byte stream that identifies the version of the class used to serialize the object."],"distractorIds":["java/exceptions_and_io#c630852a24b2","java/exceptions_and_io#d86a1de1ecfc","java/exceptions_and_io#5c8ede49af6e"],"concepts":["readobject","deserialized","deserialization","invokes","risk","serialization"],"correctMd":"Deserialization invokes `readObject()` on each object being deserialized, which can execute arbitrary Java code.","answerFullMd":"Deserialization invokes `readObject()` on each object being deserialized, which can execute arbitrary Java code. Attackers craft \"gadget chains\" — sequences of classes in common libraries (Apache Commons Collections, Spring, etc.) whose `readObject()` methods, when chained together, execute shell commands. This has affected WebLogic, Jenkins, JBoss, Apache Camel. The attack doesn't require any special privileges — just the ability to send bytes to `ObjectInputStream.readObject()`. Fix: use JSON/protobuf; if Java serialization is required, use `ObjectInputFilter` allowlisting.","distractorsMd":["`transient` marks a field that should be excluded from serialization.","After an object is deserialized, if it defines a `readResolve()` method, the JVM calls it and uses the returned object instead of the deserialized one.","`serialVersionUID` is a 64-bit long stored in the serialized byte stream that identifies the version of the class used to serialize the object."]},{"id":"java/exceptions_and_io#c630852a24b2","section":"java","module":"java/exceptions_and_io","moduleName":"exceptions and io","sourceFile":"README.md","difficulty":"intermediate","question":"What does transient do?","answerFull":"transient marks a field that should be excluded from serialization. When the object is serialized, transient fields are not written; when deserialized, they get their default value (null for objects, 0 for primitives, false for boolean). Use for: sensitive data (passwords), derived values (cache fields), non-serializable objects (threads, streams). You can provide readObject(ObjectInputStream ois) to reinitialize transient fields after deserialization.","correct":"transient marks a field that should be excluded from serialization.","distractors":["A memory-mapped file maps a region of a file into the process's virtual address space via FileChannel.map(), returning a MappedByteBuffer.","A suppressed exception is one that was thrown during the cleanup phase (e.g., close() in try-with-resources) when another exception was already in flight from the try body.","After an object is deserialized, if it defines a readResolve() method, the JVM calls it and uses the returned object instead of the deserialized one."],"distractorIds":["java/exceptions_and_io#a4c4e55faa57","java/exceptions_and_io#1f9b6442e130","java/exceptions_and_io#d86a1de1ecfc"],"concepts":["excluded","transient","serialization","marks","field"],"questionMd":"What does `transient` do?","correctMd":"`transient` marks a field that should be excluded from serialization.","answerFullMd":"`transient` marks a field that should be excluded from serialization. When the object is serialized, transient fields are not written; when deserialized, they get their default value (null for objects, 0 for primitives, false for boolean). Use for: sensitive data (passwords), derived values (cache fields), non-serializable objects (threads, streams). You can provide `readObject(ObjectInputStream ois)` to reinitialize transient fields after deserialization.","distractorsMd":["A memory-mapped file maps a region of a file into the process's virtual address space via `FileChannel.map()`, returning a `MappedByteBuffer`.","A suppressed exception is one that was thrown during the cleanup phase (e.g., `close()` in `try-with-resources`) when another exception was already in flight from the `try` body.","After an object is deserialized, if it defines a `readResolve()` method, the JVM calls it and uses the returned object instead of the deserialized one."]},{"id":"java/exceptions_and_io#d86a1de1ecfc","section":"java","module":"java/exceptions_and_io","moduleName":"exceptions and io","sourceFile":"README.md","difficulty":"intermediate","question":"How does readResolve() work for singleton deserialization?","answerFull":"After an object is deserialized, if it defines a readResolve() method, the JVM calls it and uses the returned object instead of the deserialized one. For singletons, readResolve() returns the existing singleton instance — the deserialized copy is discarded by GC. This prevents deserialization from breaking the singleton invariant. The method signature must be private Object readResolve(). Better approach: use enum-based singleton (Effective Java Item 3) — the JVM handles enum serialization correctly by design, using the name to look up the existing constant.","correct":"After an object is deserialized, if it defines a readResolve() method, the JVM calls it and uses the returned object instead of the deserialized one.","distractors":["Deserialization invokes readObject() on each object being deserialized, which can execute arbitrary Java code.","serialVersionUID is a 64-bit long stored in the serialized byte stream that identifies the version of the class used to serialize the object.","A memory-mapped file maps a region of a file into the process's virtual address space via FileChannel.map(), returning a MappedByteBuffer."],"distractorIds":["java/exceptions_and_io#e1b534784978","java/exceptions_and_io#5c8ede49af6e","java/exceptions_and_io#a4c4e55faa57"],"concepts":["deserialized","readresolve","deserialization","singleton","returned","defines"],"questionMd":"How does `readResolve()` work for singleton deserialization?","correctMd":"After an object is deserialized, if it defines a `readResolve()` method, the JVM calls it and uses the returned object instead of the deserialized one.","answerFullMd":"After an object is deserialized, if it defines a `readResolve()` method, the JVM calls it and uses the returned object instead of the deserialized one. For singletons, `readResolve()` returns the existing singleton instance — the deserialized copy is discarded by GC. This prevents deserialization from breaking the singleton invariant. The method signature must be `private Object readResolve()`. Better approach: use enum-based singleton (Effective Java Item 3) — the JVM handles enum serialization correctly by design, using the name to look up the existing constant.","distractorsMd":["Deserialization invokes `readObject()` on each object being deserialized, which can execute arbitrary Java code.","`serialVersionUID` is a 64-bit long stored in the serialized byte stream that identifies the version of the class used to serialize the object.","A memory-mapped file maps a region of a file into the process's virtual address space via `FileChannel.map()`, returning a `MappedByteBuffer`."]},{"id":"java/exceptions_and_io#ba6958461a97","section":"java","module":"java/exceptions_and_io","moduleName":"exceptions and io","sourceFile":"README.md","difficulty":"intermediate","question":"What is the Decorator pattern in Java I/O, and give an example?","answerFull":"The Decorator pattern adds behavior to objects by wrapping them in objects with the same interface. Java I/O is built on it: InputStream is the component; FileInputStream is the concrete component; BufferedInputStream, DataInputStream, CipherInputStream are decorators that each wrap an InputStream and add a capability (buffering, primitive reading, encryption). Example: new BufferedReader(new InputStreamReader(new FileInputStream(\"file.txt\"), \"UTF-8\")) — 3 decorators stacked to give buffered, charset-decoded, file-backed character reading.","correct":"The Decorator pattern adds behavior to objects by wrapping them in objects with the same interface.","distractors":["Exception chaining preserves the original cause when wrapping exceptions: new ServiceException(\"DB failed\", originalCause).","Deserialization invokes readObject() on each object being deserialized, which can execute arbitrary Java code.","A memory-mapped file maps a region of a file into the process's virtual address space via FileChannel.map(), returning a MappedByteBuffer."],"distractorIds":["java/exceptions_and_io#29da85ca609b","java/exceptions_and_io#e1b534784978","java/exceptions_and_io#a4c4e55faa57"],"concepts":["wrapping","give","decorator","behavior","interface","objects"],"answerFullMd":"The Decorator pattern adds behavior to objects by wrapping them in objects with the same interface. Java I/O is built on it: `InputStream` is the component; `FileInputStream` is the concrete component; `BufferedInputStream`, `DataInputStream`, `CipherInputStream` are decorators that each wrap an `InputStream` and add a capability (buffering, primitive reading, encryption). Example: `new BufferedReader(new InputStreamReader(new FileInputStream(\"file.txt\"), \"UTF-8\"))` — 3 decorators stacked to give buffered, charset-decoded, file-backed character reading.","distractorsMd":["Exception chaining preserves the original cause when wrapping exceptions: `new ServiceException(\"DB failed\", originalCause)`.","Deserialization invokes `readObject()` on each object being deserialized, which can execute arbitrary Java code.","A memory-mapped file maps a region of a file into the process's virtual address space via `FileChannel.map()`, returning a `MappedByteBuffer`."]},{"id":"java/exceptions_and_io#7143f921ed70","section":"java","module":"java/exceptions_and_io","moduleName":"exceptions and io","sourceFile":"README.md","difficulty":"intermediate","question":"When would you use NIO.2 WatchService?","answerFull":"WatchService provides OS-level filesystem change notifications (inotify on Linux, FSEvents on macOS, ReadDirectoryChangesW on Windows) — more efficient than polling. Use it for: hot-reloading configuration files without restart; monitoring upload directories for new files; log rotation detection; build tool file watchers (IDE hot reload). Example: path.register(watcher, ENTRY_CREATE, ENTRY_MODIFY) then watcher.take() blocks until a change, processes events, resets the key, and loops.","correct":"WatchService provides OS-level filesystem change notifications (inotify on Linux, FSEvents on macOS, ReadDirectoryChangesW on Windows) — more efficient than polling.","distractors":["Files.copy(src, dst, REPLACE_EXISTING) is the idiomatic NIO.2 copy.","A memory-mapped file maps a region of a file into the process's virtual address space via FileChannel.map(), returning a MappedByteBuffer.","A suppressed exception is one that was thrown during the cleanup phase (e.g., close() in try-with-resources) when another exception was already in flight from the try body."],"distractorIds":["java/exceptions_and_io#7e9dc41b787c","java/exceptions_and_io#a4c4e55faa57","java/exceptions_and_io#1f9b6442e130"],"concepts":["fsevents","inotify","readdirectorychangesw","watchservice","nio","macos"],"questionMd":"When would you use NIO.2 `WatchService`?","correctMd":"`WatchService` provides OS-level filesystem change notifications (inotify on Linux, FSEvents on macOS, ReadDirectoryChangesW on Windows) — more efficient than polling.","answerFullMd":"`WatchService` provides OS-level filesystem change notifications (inotify on Linux, FSEvents on macOS, ReadDirectoryChangesW on Windows) — more efficient than polling. Use it for: hot-reloading configuration files without restart; monitoring upload directories for new files; log rotation detection; build tool file watchers (IDE hot reload). Example: `path.register(watcher, ENTRY_CREATE, ENTRY_MODIFY)` then `watcher.take()` blocks until a change, processes events, resets the key, and loops.","distractorsMd":["`Files.copy(src, dst, REPLACE_EXISTING)` is the idiomatic NIO.2 copy.","A memory-mapped file maps a region of a file into the process's virtual address space via `FileChannel.map()`, returning a `MappedByteBuffer`.","A suppressed exception is one that was thrown during the cleanup phase (e.g., `close()` in `try-with-resources`) when another exception was already in flight from the `try` body."]},{"id":"java/exceptions_and_io#7e9dc41b787c","section":"java","module":"java/exceptions_and_io","moduleName":"exceptions and io","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between Files.copy() and manually copying with streams?","answerFull":"Files.copy(src, dst, REPLACE_EXISTING) is the idiomatic NIO.2 copy. The JVM can optimize it via OS-level copy (e.g., sendfile(2) on Linux), which copies data in kernel space without going through user space — zero-copy for large files. Manual stream copy: while ((n = in.read(buf)) != -1) out.write(buf, 0, n) — data goes through user-space buffer (typically 8KB). Files.copy() is also correct with respect to attributes, exceptions, and resource cleanup. Always prefer Files.copy() unless you need to transform data during the copy.","correct":"Files.copy(src, dst, REPLACE_EXISTING) is the idiomatic NIO.2 copy.","distractors":["A memory-mapped file maps a region of a file into the process's virtual address space via FileChannel.map(), returning a MappedByteBuffer.","A suppressed exception is one that was thrown during the cleanup phase (e.g., close() in try-with-resources) when another exception was already in flight from the try body.","After an object is deserialized, if it defines a readResolve() method, the JVM calls it and uses the returned object instead of the deserialized one."],"distractorIds":["java/exceptions_and_io#a4c4e55faa57","java/exceptions_and_io#1f9b6442e130","java/exceptions_and_io#d86a1de1ecfc"],"concepts":["idiomatic","src","nio","dst","manually","copying"],"questionMd":"What is the difference between `Files.copy()` and manually copying with streams?","correctMd":"`Files.copy(src, dst, REPLACE_EXISTING)` is the idiomatic NIO.2 copy.","answerFullMd":"`Files.copy(src, dst, REPLACE_EXISTING)` is the idiomatic NIO.2 copy. The JVM can optimize it via OS-level copy (e.g., `sendfile(2)` on Linux), which copies data in kernel space without going through user space — zero-copy for large files. Manual stream copy: `while ((n = in.read(buf)) != -1) out.write(buf, 0, n)` — data goes through user-space buffer (typically 8KB). `Files.copy()` is also correct with respect to attributes, exceptions, and resource cleanup. Always prefer `Files.copy()` unless you need to transform data during the copy.","distractorsMd":["A memory-mapped file maps a region of a file into the process's virtual address space via `FileChannel.map()`, returning a `MappedByteBuffer`.","A suppressed exception is one that was thrown during the cleanup phase (e.g., `close()` in `try-with-resources`) when another exception was already in flight from the `try` body.","After an object is deserialized, if it defines a `readResolve()` method, the JVM calls it and uses the returned object instead of the deserialized one."]},{"id":"java/exceptions_and_io#3924a75d1b46","section":"java","module":"java/exceptions_and_io","moduleName":"exceptions and io","sourceFile":"README.md","difficulty":"advanced","question":"How do you handle unchecked exceptions thrown inside ExecutorService tasks?","answerFull":"When a Runnable submitted to an ExecutorService throws an unchecked exception, it is silently swallowed — the exception is stored in the Future but nothing else happens unless Future.get() is called. Three approaches: (1) Always call Future.get() after submit() — it re-throws as ExecutionException wrapping the original. (2) Set a Thread.UncaughtExceptionHandler via a custom ThreadFactory — the handler is invoked with the thread and throwable for any uncaught exception. (3) Use a global default: Thread.setDefaultUncaughtExceptionHandler(handler). Note: UncaughtExceptionHandler is NOT invoked for Callable-based tasks (their exceptions are captured in the Future), only for Runnable-based tasks where the exception escapes the thread completely.","correct":"When a Runnable submitted to an ExecutorService throws an unchecked exception, it is silently swallowed — the exception is stored in the Future but nothing else happens unless Future.get() is called.","distractors":["A suppressed exception is one that was thrown during the cleanup phase (e.g., close() in try-with-resources) when another exception was already in flight from the try body.","serialVersionUID is a 64-bit long stored in the serialized byte stream that identifies the version of the class used to serialize the object.","The exception thrown in finally *replaces* the original exception from the try block."],"distractorIds":["java/exceptions_and_io#1f9b6442e130","java/exceptions_and_io#5c8ede49af6e","java/exceptions_and_io#b26c2115fae9"],"concepts":["executorservice","swallowed","unchecked","submitted","thrown","unless"],"questionMd":"How do you handle unchecked exceptions thrown inside `ExecutorService` tasks?","correctMd":"When a `Runnable` submitted to an `ExecutorService` throws an unchecked exception, it is silently swallowed — the exception is stored in the `Future` but nothing else happens unless `Future.get()` is called.","answerFullMd":"When a `Runnable` submitted to an `ExecutorService` throws an unchecked exception, it is silently swallowed — the exception is stored in the `Future` but nothing else happens unless `Future.get()` is called. Three approaches: (1) Always call `Future.get()` after `submit()` — it re-throws as `ExecutionException` wrapping the original. (2) Set a `Thread.UncaughtExceptionHandler` via a custom `ThreadFactory` — the handler is invoked with the thread and throwable for any uncaught exception. (3) Use a global default: `Thread.setDefaultUncaughtExceptionHandler(handler)`. Note: `UncaughtExceptionHandler` is NOT invoked for `Callable`-based tasks (their exceptions are captured in the `Future`), only for `Runnable`-based tasks where the exception escapes the thread completely.","distractorsMd":["A suppressed exception is one that was thrown during the cleanup phase (e.g., `close()` in `try-with-resources`) when another exception was already in flight from the `try` body.","`serialVersionUID` is a 64-bit long stored in the serialized byte stream that identifies the version of the class used to serialize the object.","The exception thrown in `finally` *replaces* the original exception from the `try` block."]},{"id":"java/exceptions_and_io#a4c4e55faa57","section":"java","module":"java/exceptions_and_io","moduleName":"exceptions and io","sourceFile":"README.md","difficulty":"advanced","question":"What is a memory-mapped file and when would you use FileChannel.map()?","answerFull":"A memory-mapped file maps a region of a file into the process's virtual address space via FileChannel.map(), returning a MappedByteBuffer. The OS manages paging: reading a byte triggers a page fault that loads the 4KB OS page from disk into memory. No explicit read() calls needed — the buffer is accessed like a byte array. Benefits: (1) Zero-copy read — no user-space buffer, data goes from OS page cache to application directly. (2) Random access: seeking to position N is O(1) — no stream seeking needed. (3) Shared across processes — multiple JVMs mapping the same file share the OS page cache. Use when: parsing large binary files (log files, database files), implementing file-backed caches, random access to large data sets. Limitation: MappedByteBuffer cannot be explicitly unmapped — it relies on GC which can delay file release.","correct":"A memory-mapped file maps a region of a file into the process's virtual address space via FileChannel.map(), returning a MappedByteBuffer.","distractors":["A suppressed exception is one that was thrown during the cleanup phase (e.g., close() in try-with-resources) when another exception was already in flight from the try body.","After an object is deserialized, if it defines a readResolve() method, the JVM calls it and uses the returned object instead of the deserialized one.","Deserialization invokes readObject() on each object being deserialized, which can execute arbitrary Java code."],"distractorIds":["java/exceptions_and_io#1f9b6442e130","java/exceptions_and_io#d86a1de1ecfc","java/exceptions_and_io#e1b534784978"],"concepts":["filechannel","mappedbytebuffer","mapped","region","returning","address"],"questionMd":"What is a memory-mapped file and when would you use `FileChannel.map()`?","correctMd":"A memory-mapped file maps a region of a file into the process's virtual address space via `FileChannel.map()`, returning a `MappedByteBuffer`.","answerFullMd":"A memory-mapped file maps a region of a file into the process's virtual address space via `FileChannel.map()`, returning a `MappedByteBuffer`. The OS manages paging: reading a byte triggers a page fault that loads the 4KB OS page from disk into memory. No explicit `read()` calls needed — the buffer is accessed like a byte array. Benefits: (1) Zero-copy read — no user-space buffer, data goes from OS page cache to application directly. (2) Random access: seeking to position N is O(1) — no stream seeking needed. (3) Shared across processes — multiple JVMs mapping the same file share the OS page cache. Use when: parsing large binary files (log files, database files), implementing file-backed caches, random access to large data sets. Limitation: `MappedByteBuffer` cannot be explicitly unmapped — it relies on GC which can delay file release.","distractorsMd":["A suppressed exception is one that was thrown during the cleanup phase (e.g., `close()` in `try-with-resources`) when another exception was already in flight from the `try` body.","After an object is deserialized, if it defines a `readResolve()` method, the JVM calls it and uses the returned object instead of the deserialized one.","Deserialization invokes `readObject()` on each object being deserialized, which can execute arbitrary Java code."]},{"id":"java/exceptions_and_io#731df99db64d","section":"java","module":"java/exceptions_and_io","moduleName":"exceptions and io","sourceFile":"README.md","difficulty":"advanced","question":"What happens when try, catch, and finally all throw exceptions, and which one propagates?","answerFull":"When finally throws an exception, it suppresses any exception from try or catch — the finally exception propagates and the others are silently discarded. This is a particularly dangerous failure mode: the original exception that caused the catch block to execute is lost: java // BROKEN: exception from finally silently swallows the try exception try { riskyOperation(); // throws IOException \"disk full\" } finally { closeResource(); // throws IllegalStateException \"already closed\" // IOException is silently discarded; IllegalStateException propagates } // FIXED: catch and suppress, or use try-with-resources try { riskyOperation(); } catch (Exception primary) { try { closeResource(); } catch (Exception suppressed) { primary.addSuppressed(suppressed); // attach, don't replace } throw primary; } // Or: just use try-with-resources — it calls addSuppressed() automatically try-with-resources (Java 7) handles this correctly: if both the body and close() throw, the close exception is added as a suppressed exception via Throwable.addSuppressed() — the primary exception propagates and nothing is lost.","correct":"When finally throws an exception, it suppresses any exception from try or catch — the finally exception propagates and the others are silently discarded.","distractors":["The exception thrown in finally *replaces* the original exception from the try block.","When a Runnable submitted to an ExecutorService throws an unchecked exception, it is silently swallowed — the exception is stored in the Future but nothing else happens unless Future.get() is called.","A suppressed exception is one that was thrown during the cleanup phase (e.g., close() in try-with-resources) when another exception was already in flight from the try body."],"distractorIds":["java/exceptions_and_io#b26c2115fae9","java/exceptions_and_io#3924a75d1b46","java/exceptions_and_io#1f9b6442e130"],"concepts":["discarded","suppresses","throw","catch","propagates","finally"],"questionMd":"What happens when `try`, `catch`, and `finally` all throw exceptions, and which one propagates?","correctMd":"When `finally` throws an exception, it **suppresses** any exception from `try` or `catch` — the `finally` exception propagates and the others are silently discarded.","answerFullMd":"When `finally` throws an exception, it **suppresses** any exception from `try` or `catch` — the `finally` exception propagates and the others are silently discarded. This is a particularly dangerous failure mode: the original exception that caused the `catch` block to execute is lost: ```java // BROKEN: exception from finally silently swallows the try exception try { riskyOperation(); // throws IOException \"disk full\" } finally { closeResource(); // throws IllegalStateException \"already closed\" // IOException is silently discarded; IllegalStateException propagates } // FIXED: catch and suppress, or use try-with-resources try { riskyOperation(); } catch (Exception primary) { try { closeResource(); } catch (Exception suppressed) { primary.addSuppressed(suppressed); // attach, don't replace } throw primary; } // Or: just use try-with-resources — it calls addSuppressed() automatically ``` `try-with-resources` (Java 7) handles this correctly: if both the body and `close()` throw, the close exception is added as a suppressed exception via `Throwable.addSuppressed()` — the primary exception propagates and nothing is lost.","distractorsMd":["The exception thrown in `finally` *replaces* the original exception from the `try` block.","When a `Runnable` submitted to an `ExecutorService` throws an unchecked exception, it is silently swallowed — the exception is stored in the `Future` but nothing else happens unless `Future.get()` is called.","A suppressed exception is one that was thrown during the cleanup phase (e.g., `close()` in `try-with-resources`) when another exception was already in flight from the `try` body."]},{"id":"java/exceptions_and_io#5c8ede49af6e","section":"java","module":"java/exceptions_and_io","moduleName":"exceptions and io","sourceFile":"README.md","difficulty":"advanced","question":"What is serialization serialVersionUID and what happens when it is absent or mismatched?","answerFull":"serialVersionUID is a 64-bit long stored in the serialized byte stream that identifies the version of the class used to serialize the object. On deserialization, the JVM compares the stream's UID with the class's UID; a mismatch throws InvalidClassException. When serialVersionUID is absent, the JVM computes a default UID from the class's structure (fields, methods, access modifiers) via a SHA-1-based algorithm. Any structural change (adding a field, renaming a method) changes the computed UID, breaking deserialization of previously serialized data. Two failure modes: (1) Unintentional break — adding a field to a class without declaring serialVersionUID silently breaks deserialization across deploys. (2) Declared UID mismatch — deliberately changing it signals an incompatible version change. Best practice: always declare private static final long serialVersionUID = 1L; in all Serializable classes, increment it manually only for truly incompatible structural changes.","correct":"serialVersionUID is a 64-bit long stored in the serialized byte stream that identifies the version of the class used to serialize the object.","distractors":["When a Runnable submitted to an ExecutorService throws an unchecked exception, it is silently swallowed — the exception is stored in the Future but nothing else happens unless Future.get() is called.","transient marks a field that should be excluded from serialization.","Deserialization invokes readObject() on each object being deserialized, which can execute arbitrary Java code."],"distractorIds":["java/exceptions_and_io#3924a75d1b46","java/exceptions_and_io#c630852a24b2","java/exceptions_and_io#e1b534784978"],"concepts":["mismatched","serialversionuid","serialize","identifies","absent","serialized"],"questionMd":"What is serialization `serialVersionUID` and what happens when it is absent or mismatched?","correctMd":"`serialVersionUID` is a 64-bit long stored in the serialized byte stream that identifies the version of the class used to serialize the object.","answerFullMd":"`serialVersionUID` is a 64-bit long stored in the serialized byte stream that identifies the version of the class used to serialize the object. On deserialization, the JVM compares the stream's UID with the class's UID; a mismatch throws `InvalidClassException`. When `serialVersionUID` is absent, the JVM computes a default UID from the class's structure (fields, methods, access modifiers) via a SHA-1-based algorithm. Any structural change (adding a field, renaming a method) changes the computed UID, breaking deserialization of previously serialized data. Two failure modes: (1) **Unintentional break** — adding a field to a class without declaring `serialVersionUID` silently breaks deserialization across deploys. (2) **Declared UID mismatch** — deliberately changing it signals an incompatible version change. Best practice: always declare `private static final long serialVersionUID = 1L;` in all `Serializable` classes, increment it manually only for truly incompatible structural changes.","distractorsMd":["When a `Runnable` submitted to an `ExecutorService` throws an unchecked exception, it is silently swallowed — the exception is stored in the `Future` but nothing else happens unless `Future.get()` is called.","`transient` marks a field that should be excluded from serialization.","Deserialization invokes `readObject()` on each object being deserialized, which can execute arbitrary Java code."]},{"id":"java/foreign_function_and_memory_api#9293d6477327","section":"java","module":"java/foreign_function_and_memory_api","moduleName":"foreign function and memory api","sourceFile":"README.md","difficulty":"core","question":"What is the Foreign Function & Memory API and what problems does it solve compared to JNI?","answerFull":"The Foreign Function & Memory API (Project Panama, GA Java 22, JEP 454) lets Java code call native C functions and manage off-heap memory without writing C glue code. JNI requires: (1) writing a C file that maps Java_MyClass_methodName() symbols; (2) compiling a shared library per platform; (3) managing raw jlong pointers with no lifetime tracking. Panama replaces all three with: Linker.downcallHandle() for function calls, Arena for memory lifetime, and MemorySegment for bounds-checked memory access — all in pure Java. Beyond developer ergonomics, Panama's downcall handles can be JIT-compiled to near-zero overhead, while JNI crossings always incur a fixed ~20–50 ns cost per call.","correct":"The Foreign Function & Memory API (Project Panama, GA Java 22, JEP 454) lets Java code call native C functions and manage off-heap memory without writing C glue code.","distractors":["Panama and Valhalla are complementary but orthogonal.","A downcall handle (Linker.downcallHandle()) is a MethodHandle that, when invoked, calls a C function at a native address.","ValueLayout.JAVA_INT assumes 4-byte alignment — the JVM may use a movdqa or equivalent aligned load instruction, which is faster but throws IllegalArgumentException if the offset is not a multiple of 4."],"distractorIds":["java/foreign_function_and_memory_api#4f5b1fcb2f86","java/foreign_function_and_memory_api#68b25be2dbf5","java/foreign_function_and_memory_api#239b8483b219"],"concepts":["454","glue","panama","foreign","jep","manage"],"answerFullMd":"The Foreign Function & Memory API (Project Panama, GA Java 22, JEP 454) lets Java code call native C functions and manage off-heap memory without writing C glue code. JNI requires: (1) writing a C file that maps `Java_MyClass_methodName()` symbols; (2) compiling a shared library per platform; (3) managing raw `jlong` pointers with no lifetime tracking. Panama replaces all three with: `Linker.downcallHandle()` for function calls, `Arena` for memory lifetime, and `MemorySegment` for bounds-checked memory access — all in pure Java. Beyond developer ergonomics, Panama's downcall handles can be JIT-compiled to near-zero overhead, while JNI crossings always incur a fixed ~20–50 ns cost per call.","distractorsMd":[null,"A downcall handle (`Linker.downcallHandle()`) is a `MethodHandle` that, when invoked, calls a C function at a native address.","`ValueLayout.JAVA_INT` assumes 4-byte alignment — the JVM may use a `movdqa` or equivalent aligned load instruction, which is faster but throws `IllegalArgumentException` if the offset is not a multiple of 4."]},{"id":"java/foreign_function_and_memory_api#4473534fb302","section":"java","module":"java/foreign_function_and_memory_api","moduleName":"foreign function and memory api","sourceFile":"README.md","difficulty":"core","question":"What is an Arena, what types exist, and when would you choose each?","answerFull":"An Arena is a scoped memory allocator: segments allocated within it share the arena's lifetime, and closing the arena frees all of them atomically. Three types: Arena.ofConfined() — single-thread ownership, fastest (no synchronisation), use for per-request or per-method allocations; Arena.ofShared() — multi-thread safe, use for buffers shared across threads (e.g., passed to a background I/O thread); Arena.ofAuto() — lifetime managed by GC finalisation, analogous to ByteBuffer.allocateDirect(), use when lifetime tracking is impractical but you can tolerate delayed deallocation. Arena.global() creates segments that live for the JVM's lifetime — use only for true global native state (e.g., a native library's global handle).","correct":"An Arena is a scoped memory allocator: segments allocated within it share the arena's lifetime, and closing the arena frees all of them atomically.","distractors":["Arena.ofAuto() ties segment lifetime to GC finalisation: the memory is freed when the arena object becomes unreachable and the JVM's cleaner thread runs.","The Foreign Function & Memory API (Project Panama, GA Java 22, JEP 454) lets Java code call native C functions and manage off-heap memory without writing C glue code.","A downcall handle (Linker.downcallHandle()) is a MethodHandle that, when invoked, calls a C function at a native address."],"distractorIds":["java/foreign_function_and_memory_api#adf145862e5f","java/foreign_function_and_memory_api#9293d6477327","java/foreign_function_and_memory_api#68b25be2dbf5"],"concepts":["closing","frees","arena","allocator","allocated","lifetime"],"questionMd":"What is an `Arena`, what types exist, and when would you choose each?","correctMd":"An `Arena` is a scoped memory allocator: segments allocated within it share the arena's lifetime, and closing the arena frees all of them atomically.","answerFullMd":"An `Arena` is a scoped memory allocator: segments allocated within it share the arena's lifetime, and closing the arena frees all of them atomically. Three types: `Arena.ofConfined()` — single-thread ownership, fastest (no synchronisation), use for per-request or per-method allocations; `Arena.ofShared()` — multi-thread safe, use for buffers shared across threads (e.g., passed to a background I/O thread); `Arena.ofAuto()` — lifetime managed by GC finalisation, analogous to `ByteBuffer.allocateDirect()`, use when lifetime tracking is impractical but you can tolerate delayed deallocation. `Arena.global()` creates segments that live for the JVM's lifetime — use only for true global native state (e.g., a native library's global handle).","distractorsMd":["`Arena.ofAuto()` ties segment lifetime to GC finalisation: the memory is freed when the arena object becomes unreachable and the JVM's cleaner thread runs.",null,"A downcall handle (`Linker.downcallHandle()`) is a `MethodHandle` that, when invoked, calls a C function at a native address."]},{"id":"java/foreign_function_and_memory_api#8cdf1a7e30a2","section":"java","module":"java/foreign_function_and_memory_api","moduleName":"foreign function and memory api","sourceFile":"README.md","difficulty":"core","question":"How does MemoryLayout and its associated VarHandle ensure safe struct field access?","answerFull":"MemoryLayout describes a C struct's field names, types, sizes, and alignment in pure Java — it is a schema. MemoryLayout.varHandle(PathElement.groupElement(\"fieldName\")) produces a VarHandle pre-bound to the field's byte offset and type. When you call varHandle.get(segment, 0L), the JVM: (1) computes offset = baseOffset + fieldOffset at link time; (2) checks that offset + fieldSize <= segment.byteSize() at runtime (bounds check); (3) emits a typed load instruction with the correct ValueLayout alignment. The result: no manual segment.get(offset + paddingCalculation) arithmetic, no misalignment, no buffer overflow. The JIT inlines the VarHandle access to a single load/store instruction with the same performance as Unsafe but with safety checks.","correct":"MemoryLayout describes a C struct's field names, types, sizes, and alignment in pure Java — it is a schema.","distractors":["C compilers insert padding between struct fields to satisfy alignment requirements (each field must start at a multiple of its natural alignment).","ValueLayout.JAVA_INT assumes 4-byte alignment — the JVM may use a movdqa or equivalent aligned load instruction, which is faster but throws IllegalArgumentException if the offset is not a multiple of 4.","The Foreign Function & Memory API (Project Panama, GA Java 22, JEP 454) lets Java code call native C functions and manage off-heap memory without writing C glue code."],"distractorIds":["java/foreign_function_and_memory_api#7e06363e465f","java/foreign_function_and_memory_api#239b8483b219","java/foreign_function_and_memory_api#9293d6477327"],"concepts":["memorylayout","varhandle","struct","associated","ensure","describes"],"questionMd":"How does `MemoryLayout` and its associated `VarHandle` ensure safe struct field access?","correctMd":"`MemoryLayout` describes a C struct's field names, types, sizes, and alignment in pure Java — it is a schema.","answerFullMd":"`MemoryLayout` describes a C struct's field names, types, sizes, and alignment in pure Java — it is a schema. `MemoryLayout.varHandle(PathElement.groupElement(\"fieldName\"))` produces a `VarHandle` pre-bound to the field's byte offset and type. When you call `varHandle.get(segment, 0L)`, the JVM: (1) computes `offset = baseOffset + fieldOffset` at link time; (2) checks that `offset + fieldSize <= segment.byteSize()` at runtime (bounds check); (3) emits a typed load instruction with the correct `ValueLayout` alignment. The result: no manual `segment.get(offset + paddingCalculation)` arithmetic, no misalignment, no buffer overflow. The JIT inlines the VarHandle access to a single load/store instruction with the same performance as Unsafe but with safety checks.","distractorsMd":[null,"`ValueLayout.JAVA_INT` assumes 4-byte alignment — the JVM may use a `movdqa` or equivalent aligned load instruction, which is faster but throws `IllegalArgumentException` if the offset is not a multiple of 4.",null]},{"id":"java/foreign_function_and_memory_api#68b25be2dbf5","section":"java","module":"java/foreign_function_and_memory_api","moduleName":"foreign function and memory api","sourceFile":"README.md","difficulty":"core","question":"Explain the difference between a downcall handle and an upcall stub.","answerFull":"A downcall handle (Linker.downcallHandle()) is a MethodHandle that, when invoked, calls a C function at a native address. Java passes arguments; the Linker marshals Java types to C ABI conventions (register assignment, stack layout, calling convention) and returns the result. An upcall stub (Linker.upcallStub()) is the reverse: it creates a native function pointer backed by a Java MethodHandle. When C code calls the function pointer, the Linker marshals C arguments to Java types, invokes the Java method, and marshals the return value back to C. Upcall stubs are how Java provides callbacks to C libraries (e.g., qsort comparator, OpenGL error callback, event handler).","correct":"A downcall handle (Linker.downcallHandle()) is a MethodHandle that, when invoked, calls a C function at a native address.","distractors":["The Foreign Function & Memory API (Project Panama, GA Java 22, JEP 454) lets Java code call native C functions and manage off-heap memory without writing C glue code.","An Arena is a scoped memory allocator: segments allocated within it share the arena's lifetime, and closing the arena frees all of them atomically.","Arena.ofAuto() ties segment lifetime to GC finalisation: the memory is freed when the arena object becomes unreachable and the JVM's cleaner thread runs."],"distractorIds":["java/foreign_function_and_memory_api#9293d6477327","java/foreign_function_and_memory_api#4473534fb302","java/foreign_function_and_memory_api#adf145862e5f"],"concepts":["downcall","downcallhandle","linker","methodhandle","explain","invoked"],"correctMd":"A downcall handle (`Linker.downcallHandle()`) is a `MethodHandle` that, when invoked, calls a C function at a native address.","answerFullMd":"A downcall handle (`Linker.downcallHandle()`) is a `MethodHandle` that, when invoked, calls a C function at a native address. Java passes arguments; the Linker marshals Java types to C ABI conventions (register assignment, stack layout, calling convention) and returns the result. An upcall stub (`Linker.upcallStub()`) is the reverse: it creates a native function pointer backed by a Java `MethodHandle`. When C code calls the function pointer, the Linker marshals C arguments to Java types, invokes the Java method, and marshals the return value back to C. Upcall stubs are how Java provides callbacks to C libraries (e.g., `qsort` comparator, OpenGL error callback, event handler).","distractorsMd":[null,"An `Arena` is a scoped memory allocator: segments allocated within it share the arena's lifetime, and closing the arena frees all of them atomically.","`Arena.ofAuto()` ties segment lifetime to GC finalisation: the memory is freed when the arena object becomes unreachable and the JVM's cleaner thread runs."]},{"id":"java/foreign_function_and_memory_api#0ef64fcb15de","section":"java","module":"java/foreign_function_and_memory_api","moduleName":"foreign function and memory api","sourceFile":"README.md","difficulty":"core","question":"Why can't you obtain a stable MemorySegment address for a Java heap object?","answerFull":"The GC (G1, ZGC, Shenandoah) moves live objects during garbage collection — compaction relocates objects to reduce fragmentation, updating all references. If Java passed a raw pointer to a heap object to a native function, the GC might move the object mid-call, invalidating the pointer and causing the native library to read/write arbitrary memory. For this reason, MemorySegment.ofArray() produces a segment whose address changes after each GC cycle. The JVM can pin a heap object during a native call (via Linker.Option.critical() for very short calls, or the GC's \"pinned region\" mechanism), but general-purpose pinning is avoided to preserve GC flexibility. The safe pattern: copy heap data into an off-heap MemorySegment, pass the off-heap address to native code.","correct":"The GC (G1, ZGC, Shenandoah) moves live objects during garbage collection — compaction relocates objects to reduce fragmentation, updating all references.","distractors":["The Foreign Function & Memory API (Project Panama, GA Java 22, JEP 454) lets Java code call native C functions and manage off-heap memory without writing C glue code.","MemoryLayout describes a C struct's field names, types, sizes, and alignment in pure Java — it is a schema.","A downcall handle (Linker.downcallHandle()) is a MethodHandle that, when invoked, calls a C function at a native address."],"distractorIds":["java/foreign_function_and_memory_api#9293d6477327","java/foreign_function_and_memory_api#8cdf1a7e30a2","java/foreign_function_and_memory_api#68b25be2dbf5"],"concepts":["relocates","shenandoah","zgc","fragmentation","obtain","garbage"],"questionMd":"Why can't you obtain a stable `MemorySegment` address for a Java heap object?","answerFullMd":"The GC (G1, ZGC, Shenandoah) moves live objects during garbage collection — compaction relocates objects to reduce fragmentation, updating all references. If Java passed a raw pointer to a heap object to a native function, the GC might move the object mid-call, invalidating the pointer and causing the native library to read/write arbitrary memory. For this reason, `MemorySegment.ofArray()` produces a segment whose address changes after each GC cycle. The JVM can pin a heap object during a native call (via `Linker.Option.critical()` for very short calls, or the GC's \"pinned region\" mechanism), but general-purpose pinning is avoided to preserve GC flexibility. The safe pattern: copy heap data into an off-heap `MemorySegment`, pass the off-heap address to native code.","distractorsMd":[null,"`MemoryLayout` describes a C struct's field names, types, sizes, and alignment in pure Java — it is a schema.","A downcall handle (`Linker.downcallHandle()`) is a `MethodHandle` that, when invoked, calls a C function at a native address."]},{"id":"java/foreign_function_and_memory_api#adf145862e5f","section":"java","module":"java/foreign_function_and_memory_api","moduleName":"foreign function and memory api","sourceFile":"README.md","difficulty":"intermediate","question":"How does Arena.ofAuto() differ from explicit arena management, and when is the difference significant?","answerFull":"Arena.ofAuto() ties segment lifetime to GC finalisation: the memory is freed when the arena object becomes unreachable and the JVM's cleaner thread runs. This is analogous to ByteBuffer.allocateDirect() with Cleaner. The problem: finalisation timing is non-deterministic. Under memory pressure, you may allocate gigabytes of native memory faster than GC reclaims old arenas, causing native OOM or excessive RSS growth. Explicit Arena.ofConfined() / ofShared() with try-with-resources gives deterministic deallocation — the memory is freed exactly when the arena is closed. For any latency-sensitive or memory-intensive workload, explicit arenas are strongly preferred. Arena.ofAuto() is a convenience for exploratory code or cases where the arena is short-lived enough that timing does not matter.","correct":"Arena.ofAuto() ties segment lifetime to GC finalisation: the memory is freed when the arena object becomes unreachable and the JVM's cleaner thread runs.","distractors":["An Arena is a scoped memory allocator: segments allocated within it share the arena's lifetime, and closing the arena frees all of them atomically.","ValueLayout.JAVA_INT assumes 4-byte alignment — the JVM may use a movdqa or equivalent aligned load instruction, which is faster but throws IllegalArgumentException if the offset is not a multiple of 4.","The Foreign Function & Memory API (Project Panama, GA Java 22, JEP 454) lets Java code call native C functions and manage off-heap memory without writing C glue code."],"distractorIds":["java/foreign_function_and_memory_api#4473534fb302","java/foreign_function_and_memory_api#239b8483b219","java/foreign_function_and_memory_api#9293d6477327"],"concepts":["cleaner","finalisation","ofauto","ties","arena","freed"],"questionMd":"How does `Arena.ofAuto()` differ from explicit arena management, and when is the difference significant?","correctMd":"`Arena.ofAuto()` ties segment lifetime to GC finalisation: the memory is freed when the arena object becomes unreachable and the JVM's cleaner thread runs.","answerFullMd":"`Arena.ofAuto()` ties segment lifetime to GC finalisation: the memory is freed when the arena object becomes unreachable and the JVM's cleaner thread runs. This is analogous to `ByteBuffer.allocateDirect()` with `Cleaner`. The problem: finalisation timing is non-deterministic. Under memory pressure, you may allocate gigabytes of native memory faster than GC reclaims old arenas, causing native OOM or excessive RSS growth. Explicit `Arena.ofConfined()` / `ofShared()` with `try-with-resources` gives deterministic deallocation — the memory is freed exactly when the arena is closed. For any latency-sensitive or memory-intensive workload, explicit arenas are strongly preferred. `Arena.ofAuto()` is a convenience for exploratory code or cases where the arena is short-lived enough that timing does not matter.","distractorsMd":["An `Arena` is a scoped memory allocator: segments allocated within it share the arena's lifetime, and closing the arena frees all of them atomically.","`ValueLayout.JAVA_INT` assumes 4-byte alignment — the JVM may use a `movdqa` or equivalent aligned load instruction, which is faster but throws `IllegalArgumentException` if the offset is not a multiple of 4.",null]},{"id":"java/foreign_function_and_memory_api#239b8483b219","section":"java","module":"java/foreign_function_and_memory_api","moduleName":"foreign function and memory api","sourceFile":"README.md","difficulty":"intermediate","question":"What is the significance of ValueLayout.JAVA_INT_UNALIGNED vs ValueLayout.JAVA_INT?","answerFull":"ValueLayout.JAVA_INT assumes 4-byte alignment — the JVM may use a movdqa or equivalent aligned load instruction, which is faster but throws IllegalArgumentException if the offset is not a multiple of 4. ValueLayout.JAVA_INT_UNALIGNED explicitly allows any offset, emitting a safe unaligned load. When scanning a binary file or network packet where integers are not guaranteed to be aligned (e.g., a protocol where a 4-byte int follows a 3-byte field), JAVA_INT_UNALIGNED is required. Using JAVA_INT on an unaligned offset causes an exception or hardware fault on RISC architectures. The tradeoff: unaligned access is 1–3 ns slower per load on modern x86 (negligible) but required for correctness on ARM.","correct":"ValueLayout.JAVA_INT assumes 4-byte alignment — the JVM may use a movdqa or equivalent aligned load instruction, which is faster but throws IllegalArgumentException if the offset is not a multiple of 4.","distractors":["C compilers insert padding between struct fields to satisfy alignment requirements (each field must start at a multiple of its natural alignment).","MemoryLayout describes a C struct's field names, types, sizes, and alignment in pure Java — it is a schema.","Arena.ofAuto() ties segment lifetime to GC finalisation: the memory is freed when the arena object becomes unreachable and the JVM's cleaner thread runs."],"distractorIds":["java/foreign_function_and_memory_api#7e06363e465f","java/foreign_function_and_memory_api#8cdf1a7e30a2","java/foreign_function_and_memory_api#adf145862e5f"],"concepts":["movdqa","valuelayout","illegalargumentexception","significance","aligned","assumes"],"questionMd":"What is the significance of `ValueLayout.JAVA_INT_UNALIGNED` vs `ValueLayout.JAVA_INT`?","correctMd":"`ValueLayout.JAVA_INT` assumes 4-byte alignment — the JVM may use a `movdqa` or equivalent aligned load instruction, which is faster but throws `IllegalArgumentException` if the offset is not a multiple of 4.","answerFullMd":"`ValueLayout.JAVA_INT` assumes 4-byte alignment — the JVM may use a `movdqa` or equivalent aligned load instruction, which is faster but throws `IllegalArgumentException` if the offset is not a multiple of 4. `ValueLayout.JAVA_INT_UNALIGNED` explicitly allows any offset, emitting a safe unaligned load. When scanning a binary file or network packet where integers are not guaranteed to be aligned (e.g., a protocol where a 4-byte int follows a 3-byte field), `JAVA_INT_UNALIGNED` is required. Using `JAVA_INT` on an unaligned offset causes an exception or hardware fault on RISC architectures. The tradeoff: unaligned access is 1–3 ns slower per load on modern x86 (negligible) but required for correctness on ARM.","distractorsMd":[null,"`MemoryLayout` describes a C struct's field names, types, sizes, and alignment in pure Java — it is a schema.","`Arena.ofAuto()` ties segment lifetime to GC finalisation: the memory is freed when the arena object becomes unreachable and the JVM's cleaner thread runs."]},{"id":"java/foreign_function_and_memory_api#d87296479d67","section":"java","module":"java/foreign_function_and_memory_api","moduleName":"foreign function and memory api","sourceFile":"README.md","difficulty":"intermediate","question":"How does Panama improve memory-mapped file handling beyond ByteBuffer.map()?","answerFull":"ByteBuffer.map() (the legacy API) returns a MappedByteBuffer with a maximum size of Integer.MAX_VALUE (2 GB) — limited by ByteBuffer's int position/limit fields. FileChannel.map(mode, position, size, arena) (Java 22+) returns a MemorySegment with long addressing — a 4 GB, 100 GB, or larger file can be mapped as one segment. Additionally, the mapped segment's lifetime is controlled by the arena: closing the arena unmaps the file (munmap) deterministically. With the legacy MappedByteBuffer, unmapping is triggered by Cleaner at GC time — you cannot force it synchronously, which is a problem for file locking and hot reload scenarios.","correct":"ByteBuffer.map() (the legacy API) returns a MappedByteBuffer with a maximum size of Integer.MAX_VALUE (2 GB) — limited by ByteBuffer's int position/limit fields.","distractors":["Arena.ofAuto() ties segment lifetime to GC finalisation: the memory is freed when the arena object becomes unreachable and the JVM's cleaner thread runs.","C compilers insert padding between struct fields to satisfy alignment requirements (each field must start at a multiple of its natural alignment).","The Foreign Function & Memory API (Project Panama, GA Java 22, JEP 454) lets Java code call native C functions and manage off-heap memory without writing C glue code."],"distractorIds":["java/foreign_function_and_memory_api#adf145862e5f","java/foreign_function_and_memory_api#7e06363e465f","java/foreign_function_and_memory_api#9293d6477327"],"concepts":["bytebuffer","mappedbytebuffer","panama","legacy","mapped","beyond"],"questionMd":"How does Panama improve memory-mapped file handling beyond `ByteBuffer.map()`?","correctMd":"`ByteBuffer.map()` (the legacy API) returns a `MappedByteBuffer` with a maximum size of `Integer.MAX_VALUE` (2 GB) — limited by `ByteBuffer`'s `int` position/limit fields.","answerFullMd":"`ByteBuffer.map()` (the legacy API) returns a `MappedByteBuffer` with a maximum size of `Integer.MAX_VALUE` (2 GB) — limited by `ByteBuffer`'s `int` position/limit fields. `FileChannel.map(mode, position, size, arena)` (Java 22+) returns a `MemorySegment` with `long` addressing — a 4 GB, 100 GB, or larger file can be mapped as one segment. Additionally, the mapped segment's lifetime is controlled by the arena: closing the arena unmaps the file (`munmap`) deterministically. With the legacy `MappedByteBuffer`, unmapping is triggered by `Cleaner` at GC time — you cannot force it synchronously, which is a problem for file locking and hot reload scenarios.","distractorsMd":["`Arena.ofAuto()` ties segment lifetime to GC finalisation: the memory is freed when the arena object becomes unreachable and the JVM's cleaner thread runs.",null,null]},{"id":"java/foreign_function_and_memory_api#7e06363e465f","section":"java","module":"java/foreign_function_and_memory_api","moduleName":"foreign function and memory api","sourceFile":"README.md","difficulty":"advanced","question":"How do you handle struct padding to ensure Java MemoryLayout matches a C struct?","answerFull":"C compilers insert padding between struct fields to satisfy alignment requirements (each field must start at a multiple of its natural alignment). Rules: char (1-byte) needs 1-byte alignment, short (2-byte) needs 2-byte alignment, int/float (4-byte) needs 4-byte alignment, double/long (8-byte) needs 8-byte alignment on most platforms. Java's MemoryLayout.structLayout() does NOT automatically insert padding — you must add MemoryLayout.paddingLayout(n) entries to match the C compiler's layout. Best practice: run offsetof(struct, field) in a C test harness and verify it matches the Java layout's byteOffset(PathElement.groupElement(\"field\")). Alternatively, compile with attribute((packed)) in C to eliminate all padding, then the Java layout needs no padding either.","correct":"C compilers insert padding between struct fields to satisfy alignment requirements (each field must start at a multiple of its natural alignment).","distractors":["ValueLayout.JAVA_INT assumes 4-byte alignment — the JVM may use a movdqa or equivalent aligned load instruction, which is faster but throws IllegalArgumentException if the offset is not a multiple of 4.","MemoryLayout describes a C struct's field names, types, sizes, and alignment in pure Java — it is a schema.","A downcall handle (Linker.downcallHandle()) is a MethodHandle that, when invoked, calls a C function at a native address."],"distractorIds":["java/foreign_function_and_memory_api#239b8483b219","java/foreign_function_and_memory_api#8cdf1a7e30a2","java/foreign_function_and_memory_api#68b25be2dbf5"],"concepts":["memorylayout","compilers","struct","satisfy","ensure","padding"],"answerFullMd":"C compilers insert padding between struct fields to satisfy alignment requirements (each field must start at a multiple of its natural alignment). Rules: `char` (1-byte) needs 1-byte alignment, `short` (2-byte) needs 2-byte alignment, `int`/`float` (4-byte) needs 4-byte alignment, `double`/`long` (8-byte) needs 8-byte alignment on most platforms. Java's `MemoryLayout.structLayout()` does NOT automatically insert padding — you must add `MemoryLayout.paddingLayout(n)` entries to match the C compiler's layout. Best practice: run `offsetof(struct, field)` in a C test harness and verify it matches the Java layout's `byteOffset(PathElement.groupElement(\"field\"))`. Alternatively, compile with `__attribute__((packed))` in C to eliminate all padding, then the Java layout needs no padding either.","distractorsMd":["`ValueLayout.JAVA_INT` assumes 4-byte alignment — the JVM may use a `movdqa` or equivalent aligned load instruction, which is faster but throws `IllegalArgumentException` if the offset is not a multiple of 4.","`MemoryLayout` describes a C struct's field names, types, sizes, and alignment in pure Java — it is a schema.","A downcall handle (`Linker.downcallHandle()`) is a `MethodHandle` that, when invoked, calls a C function at a native address."]},{"id":"java/foreign_function_and_memory_api#4f5b1fcb2f86","section":"java","module":"java/foreign_function_and_memory_api","moduleName":"foreign function and memory api","sourceFile":"README.md","difficulty":"advanced","question":"What is the relationship between Panama and Project Valhalla?","answerFull":"Panama and Valhalla are complementary but orthogonal. Panama (java.lang.foreign) addresses foreign memory and foreign functions — calling native C code and managing off-heap data. Valhalla (value types / primitive classes, still in preview as of Java 24) addresses in-heap performance by eliminating object headers for small value types (long, int, and user-defined value class Point { int x; int y; }). The convergence point: a future Java may combine Valhalla value types with Panama MemoryLayout to store arrays of value types as contiguous native memory with zero GC overhead — today achievable with Panama + manual layout, future: automatically via the type system.","correct":"Panama and Valhalla are complementary but orthogonal.","distractors":["The Foreign Function & Memory API (Project Panama, GA Java 22, JEP 454) lets Java code call native C functions and manage off-heap memory without writing C glue code.","ValueLayout.JAVA_INT assumes 4-byte alignment — the JVM may use a movdqa or equivalent aligned load instruction, which is faster but throws IllegalArgumentException if the offset is not a multiple of 4.","A downcall handle (Linker.downcallHandle()) is a MethodHandle that, when invoked, calls a C function at a native address."],"distractorIds":["java/foreign_function_and_memory_api#9293d6477327","java/foreign_function_and_memory_api#239b8483b219","java/foreign_function_and_memory_api#68b25be2dbf5"],"concepts":["valhalla","panama","orthogonal","complementary","relationship","project"],"answerFullMd":"Panama and Valhalla are complementary but orthogonal. Panama (`java.lang.foreign`) addresses foreign memory and foreign functions — calling native C code and managing off-heap data. Valhalla (value types / primitive classes, still in preview as of Java 24) addresses in-heap performance by eliminating object headers for small value types (`long`, `int`, and user-defined `value class Point { int x; int y; }`). The convergence point: a future Java may combine Valhalla value types with Panama `MemoryLayout` to store arrays of value types as contiguous native memory with zero GC overhead — today achievable with Panama + manual layout, future: automatically via the type system.","distractorsMd":[null,"`ValueLayout.JAVA_INT` assumes 4-byte alignment — the JVM may use a `movdqa` or equivalent aligned load instruction, which is faster but throws `IllegalArgumentException` if the offset is not a multiple of 4.","A downcall handle (`Linker.downcallHandle()`) is a `MethodHandle` that, when invoked, calls a C function at a native address."]},{"id":"java/functional_programming#3468a3d6656d","section":"java","module":"java/functional_programming","moduleName":"functional programming","sourceFile":"README.md","difficulty":"core","question":"What is the difference between andThen and compose in Function?","answerFull":"Both compose two functions, but the order differs. f.andThen(g) applies f first, then g: equivalent to x -> g(f(x)). f.compose(g) applies g first, then f: equivalent to x -> f(g(x)). Memory aid: andThen means \"apply f, then do g afterward\"; compose matches mathematical function composition notation f∘g where g is applied first. For Predicate, the equivalents are and(), or(), negate().","correct":"Both compose two functions, but the order differs.","distractors":["Function<T,R> takes one argument of type T, returns R.","Referential transparency means a function's output depends solely on its inputs with no observable side effects — calling f(x) twice always returns the same result and changes nothing external.","Collections.unmodifiableList(list) creates a view — changes to the underlying list are visible through the wrapper, but the wrapper itself rejects modifications."],"distractorIds":["java/functional_programming#2136c35de982","java/functional_programming#20735abc27d8","java/functional_programming#336d67295cdc"],"concepts":["andthen","compose","differs","difference","functions","function"],"questionMd":"What is the difference between `andThen` and `compose` in `Function`?","answerFullMd":"Both compose two functions, but the order differs. `f.andThen(g)` applies `f` first, then `g`: equivalent to `x -> g(f(x))`. `f.compose(g)` applies `g` first, then `f`: equivalent to `x -> f(g(x))`. Memory aid: `andThen` means \"apply f, then do g afterward\"; `compose` matches mathematical function composition notation `f∘g` where g is applied first. For `Predicate`, the equivalents are `and()`, `or()`, `negate()`.","distractorsMd":["`Function<T,R>` takes one argument of type T, returns R.","Referential transparency means a function's output depends solely on its inputs with no observable side effects — calling `f(x)` twice always returns the same result and changes nothing external.","`Collections.unmodifiableList(list)` creates a view — changes to the underlying list are visible through the wrapper, but the wrapper itself rejects modifications."]},{"id":"java/functional_programming#6aead2abcb10","section":"java","module":"java/functional_programming","moduleName":"functional programming","sourceFile":"README.md","difficulty":"core","question":"What is a Spliterator and why does it matter for parallel streams?","answerFull":"A Spliterator (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing. It has tryAdvance() (process one element), trySplit() (split into two halves), estimateSize(), and characteristics() (SIZED, ORDERED, etc.). ArrayList has a Spliterator that supports balanced binary splitting (SIZED+SUBSIZED), making parallel streams very efficient. LinkedList cannot be efficiently split — trySplit() returns null early — so parallel streams over LinkedList fall back to nearly sequential execution.","correct":"A Spliterator (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing.","distractors":["map(f) transforms each element: Stream<T> → Stream<R> where f: T → R.","(1) I/O-bound operations — block ForkJoinPool.commonPool() carrier threads.","A variable is effectively final if it's initialized once and never reassigned."],"distractorIds":["java/functional_programming#990dccde5570","java/functional_programming#84d30b05dc6b","java/functional_programming#5a97ffbe2121"],"concepts":["splittable","spliterator","matter","describes","ranges","iterator"],"correctMd":"A `Spliterator` (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing.","answerFullMd":"A `Spliterator` (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing. It has `tryAdvance()` (process one element), `trySplit()` (split into two halves), `estimateSize()`, and `characteristics()` (SIZED, ORDERED, etc.). ArrayList has a `Spliterator` that supports balanced binary splitting (SIZED+SUBSIZED), making parallel streams very efficient. LinkedList cannot be efficiently split — `trySplit()` returns null early — so parallel streams over LinkedList fall back to nearly sequential execution.","distractorsMd":["`map(f)` transforms each element: `Stream<T> → Stream<R>` where `f: T → R`.",null,null]},{"id":"java/functional_programming#84d30b05dc6b","section":"java","module":"java/functional_programming","moduleName":"functional programming","sourceFile":"README.md","difficulty":"core","question":"When should you NOT use parallel streams?","answerFull":"(1) I/O-bound operations — block ForkJoinPool.commonPool() carrier threads. (2) Small collections (<100 elements) — overhead of splitting and combining exceeds gains. (3) Non-associative reduce — reduce((a,b) -> a-b) is wrong in parallel (subtraction isn't associative). (4) Ordered operations with side effects — forEach ordering is non-deterministic in parallel. (5) When ordering matters — findFirst() in parallel may degrade to findAny() behavior. (6) When collection is not efficiently splittable (LinkedList, iterators).","correct":"(1) I/O-bound operations — block ForkJoinPool.commonPool() carrier threads.","distractors":["A Spliterator (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing.","Stateless operations (filter, map, flatMap, peek) process each element independently — no memory of previous elements.","A variable is effectively final if it's initialized once and never reassigned."],"distractorIds":["java/functional_programming#6aead2abcb10","java/functional_programming#38fb9ff961a8","java/functional_programming#5a97ffbe2121"],"concepts":["commonpool","forkjoinpool","carrier","streams","parallel","threads"],"answerFullMd":"(1) I/O-bound operations — block ForkJoinPool.commonPool() carrier threads. (2) Small collections (<100 elements) — overhead of splitting and combining exceeds gains. (3) Non-associative reduce — `reduce((a,b) -> a-b)` is wrong in parallel (subtraction isn't associative). (4) Ordered operations with side effects — `forEach` ordering is non-deterministic in parallel. (5) When ordering matters — `findFirst()` in parallel may degrade to `findAny()` behavior. (6) When collection is not efficiently splittable (LinkedList, iterators).","distractorsMd":["A `Spliterator` (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing.","Stateless operations (`filter`, `map`, `flatMap`, `peek`) process each element independently — no memory of previous elements.",null]},{"id":"java/functional_programming#38fb9ff961a8","section":"java","module":"java/functional_programming","moduleName":"functional programming","sourceFile":"README.md","difficulty":"core","question":"What makes a stream operation stateful vs stateless, and why does it matter?","answerFull":"Stateless operations (filter, map, flatMap, peek) process each element independently — no memory of previous elements. Stateful operations (sorted, distinct, limit, skip) need to see multiple elements before producing output. In parallel streams, stateful operations are performance hazards: sorted() must materialize all elements; distinct() must track seen elements across threads (synchronized state). For correct parallel execution, prefer stateless pipelines; use stateful ops at the end of the pipeline.","correct":"Stateless operations (filter, map, flatMap, peek) process each element independently — no memory of previous elements.","distractors":["A Spliterator (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing.","(1) I/O-bound operations — block ForkJoinPool.commonPool() carrier threads.","map(f) transforms each element: Stream<T> → Stream<R> where f: T → R."],"distractorIds":["java/functional_programming#6aead2abcb10","java/functional_programming#84d30b05dc6b","java/functional_programming#990dccde5570"],"concepts":["peek","matter","flatmap","stateful","stateless","previous"],"correctMd":"Stateless operations (`filter`, `map`, `flatMap`, `peek`) process each element independently — no memory of previous elements.","answerFullMd":"Stateless operations (`filter`, `map`, `flatMap`, `peek`) process each element independently — no memory of previous elements. Stateful operations (`sorted`, `distinct`, `limit`, `skip`) need to see multiple elements before producing output. In parallel streams, stateful operations are performance hazards: `sorted()` must materialize all elements; `distinct()` must track seen elements across threads (synchronized state). For correct parallel execution, prefer stateless pipelines; use stateful ops at the end of the pipeline.","distractorsMd":["A `Spliterator` (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing.",null,"`map(f)` transforms each element: `Stream<T> → Stream<R>` where `f: T → R`."]},{"id":"java/functional_programming#990dccde5570","section":"java","module":"java/functional_programming","moduleName":"functional programming","sourceFile":"README.md","difficulty":"intermediate","question":"How does flatMap differ from map conceptually?","answerFull":"map(f) transforms each element: Stream<T> → Stream<R> where f: T → R. flatMap(f) transforms and flattens: Stream<T> → Stream<R> where f: T → Stream<R>. Conceptually, flatMap is monadic bind (>>=): it \"flattens\" one level of nesting. Optional.flatMap handles Optional<Optional<T>> → Optional<T>. This is the key primitive for monadic composition — chaining operations that each return a wrapped value.","correct":"map(f) transforms each element: Stream<T> → Stream<R> where f: T → R.","distractors":["Stateless operations (filter, map, flatMap, peek) process each element independently — no memory of previous elements.","A Spliterator (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing.","(1) I/O-bound operations — block ForkJoinPool.commonPool() carrier threads."],"distractorIds":["java/functional_programming#38fb9ff961a8","java/functional_programming#6aead2abcb10","java/functional_programming#84d30b05dc6b"],"concepts":["conceptually","transforms","flatmap","differ","element","stream"],"questionMd":"How does `flatMap` differ from `map` conceptually?","correctMd":"`map(f)` transforms each element: `Stream<T> → Stream<R>` where `f: T → R`.","answerFullMd":"`map(f)` transforms each element: `Stream<T> → Stream<R>` where `f: T → R`. `flatMap(f)` transforms and flattens: `Stream<T> → Stream<R>` where `f: T → Stream<R>`. Conceptually, `flatMap` is monadic bind (>>=): it \"flattens\" one level of nesting. `Optional.flatMap` handles `Optional<Optional<T>> → Optional<T>`. This is the key primitive for monadic composition — chaining operations that each return a wrapped value.","distractorsMd":["Stateless operations (`filter`, `map`, `flatMap`, `peek`) process each element independently — no memory of previous elements.","A `Spliterator` (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing.",null]},{"id":"java/functional_programming#5a97ffbe2121","section":"java","module":"java/functional_programming","moduleName":"functional programming","sourceFile":"README.md","difficulty":"intermediate","question":"What is \"effectively final\" and why does Java require it for lambda captures?","answerFull":"A variable is effectively final if it's initialized once and never reassigned. Java requires it because lambdas can outlive the method scope (submitted to a thread pool, stored in a field). Local variables are on the stack; when the method returns, the stack frame is gone. Java copies the value into the lambda's closure. If the variable could change after capture, the lambda would hold a stale copy — misleading semantics. For mutable state, use AtomicInteger, AtomicReference, or arrays.","correct":"A variable is effectively final if it's initialized once and never reassigned.","distractors":["(1) I/O-bound operations — block ForkJoinPool.commonPool() carrier threads.","A Spliterator (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing.","Collections.unmodifiableList(list) creates a view — changes to the underlying list are visible through the wrapper, but the wrapper itself rejects modifications."],"distractorIds":["java/functional_programming#84d30b05dc6b","java/functional_programming#6aead2abcb10","java/functional_programming#336d67295cdc"],"concepts":["reassigned","initialized","effectively","captures","require","lambda"],"answerFullMd":"A variable is effectively final if it's initialized once and never reassigned. Java requires it because lambdas can outlive the method scope (submitted to a thread pool, stored in a field). Local variables are on the stack; when the method returns, the stack frame is gone. Java copies the value into the lambda's closure. If the variable could change after capture, the lambda would hold a stale copy — misleading semantics. For mutable state, use `AtomicInteger`, `AtomicReference`, or arrays.","distractorsMd":[null,"A `Spliterator` (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing.","`Collections.unmodifiableList(list)` creates a view — changes to the underlying list are visible through the wrapper, but the wrapper itself rejects modifications."]},{"id":"java/functional_programming#2136c35de982","section":"java","module":"java/functional_programming","moduleName":"functional programming","sourceFile":"README.md","difficulty":"intermediate","question":"Explain the difference between Function<A,B> and BiFunction<A,B,C>.","answerFull":"Function<T,R> takes one argument of type T, returns R. BiFunction<T,U,R> takes two arguments (T and U), returns R. Java has no TriFunction in the standard library — for 3+ arguments, use a custom functional interface or compose. BiFunction is commonly used with Map.merge(), Map.replaceAll(), and building binary operators. UnaryOperator<T> is Function<T,T>; BinaryOperator<T> is BiFunction<T,T,T> — useful for reduce operations.","correct":"Function<T,R> takes one argument of type T, returns R.","distractors":["Referential transparency means a function's output depends solely on its inputs with no observable side effects — calling f(x) twice always returns the same result and changes nothing external.","(1) I/O-bound operations — block ForkJoinPool.commonPool() carrier threads.","A Spliterator (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing."],"distractorIds":["java/functional_programming#20735abc27d8","java/functional_programming#84d30b05dc6b","java/functional_programming#6aead2abcb10"],"concepts":["explain","difference","argument","takes","function","returns"],"questionMd":"Explain the difference between `Function<A,B>` and `BiFunction<A,B,C>`.","correctMd":"`Function<T,R>` takes one argument of type T, returns R.","answerFullMd":"`Function<T,R>` takes one argument of type T, returns R. `BiFunction<T,U,R>` takes two arguments (T and U), returns R. Java has no `TriFunction` in the standard library — for 3+ arguments, use a custom functional interface or compose. `BiFunction` is commonly used with `Map.merge()`, `Map.replaceAll()`, and building binary operators. `UnaryOperator<T>` is `Function<T,T>`; `BinaryOperator<T>` is `BiFunction<T,T,T>` — useful for reduce operations.","distractorsMd":["Referential transparency means a function's output depends solely on its inputs with no observable side effects — calling `f(x)` twice always returns the same result and changes nothing external.",null,"A `Spliterator` (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing."]},{"id":"java/functional_programming#b0701508e4cc","section":"java","module":"java/functional_programming","moduleName":"functional programming","sourceFile":"README.md","difficulty":"intermediate","question":"How does Records' auto-generated equals() differ from a typical POJO's?","answerFull":"Record's auto-generated equals() is field-by-field comparison using Objects.equals() for each component — guaranteed correct by the spec. A typical POJO's equals() is whatever the developer wrote (or IDE generated, which may be correct or outdated if fields were added later). The critical difference: if you add a field to a POJO, equals() and hashCode() must be manually updated; for a Record, they're automatically regenerated at compile time — no risk of forgetting.","correct":"Record's auto-generated equals() is field-by-field comparison using Objects.equals() for each component — guaranteed correct by the spec.","distractors":["(1) I/O-bound operations — block ForkJoinPool.commonPool() carrier threads.","A Spliterator (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing.","A variable is effectively final if it's initialized once and never reassigned."],"distractorIds":["java/functional_programming#84d30b05dc6b","java/functional_programming#6aead2abcb10","java/functional_programming#5a97ffbe2121"],"concepts":["pojo","differ","comparison","guaranteed","typical","equals"],"questionMd":"How does Records' auto-generated `equals()` differ from a typical POJO's?","correctMd":"Record's auto-generated `equals()` is field-by-field comparison using `Objects.equals()` for each component — guaranteed correct by the spec.","answerFullMd":"Record's auto-generated `equals()` is field-by-field comparison using `Objects.equals()` for each component — guaranteed correct by the spec. A typical POJO's `equals()` is whatever the developer wrote (or IDE generated, which may be correct or outdated if fields were added later). The critical difference: if you add a field to a POJO, `equals()` and `hashCode()` must be manually updated; for a Record, they're automatically regenerated at compile time — no risk of forgetting.","distractorsMd":[null,"A `Spliterator` (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing.",null]},{"id":"java/functional_programming#336d67295cdc","section":"java","module":"java/functional_programming","moduleName":"functional programming","sourceFile":"README.md","difficulty":"intermediate","question":"When would you choose an unmodifiable wrapper vs List.of()?","answerFull":"Collections.unmodifiableList(list) creates a view — changes to the underlying list are visible through the wrapper, but the wrapper itself rejects modifications. Use when you need to expose a list as read-only while still being able to mutate it internally. List.of() creates a truly immutable, independent list — no backing mutable list. Use for constant data, returning truly immutable results from API methods. Key tradeoff: unmodifiableList is a live view (changes propagate through); List.of() is a snapshot copy (no live relationship).","correct":"Collections.unmodifiableList(list) creates a view — changes to the underlying list are visible through the wrapper, but the wrapper itself rejects modifications.","distractors":["Referential transparency means a function's output depends solely on its inputs with no observable side effects — calling f(x) twice always returns the same result and changes nothing external.","(1) I/O-bound operations — block ForkJoinPool.commonPool() carrier threads.","A Spliterator (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing."],"distractorIds":["java/functional_programming#20735abc27d8","java/functional_programming#84d30b05dc6b","java/functional_programming#6aead2abcb10"],"concepts":["unmodifiablelist","modifications","rejects","wrapper","collections","underlying"],"questionMd":"When would you choose an unmodifiable wrapper vs `List.of()`?","correctMd":"`Collections.unmodifiableList(list)` creates a view — changes to the underlying list are visible through the wrapper, but the wrapper itself rejects modifications.","answerFullMd":"`Collections.unmodifiableList(list)` creates a view — changes to the underlying list are visible through the wrapper, but the wrapper itself rejects modifications. Use when you need to expose a list as read-only while still being able to mutate it internally. `List.of()` creates a truly immutable, independent list — no backing mutable list. Use for constant data, returning truly immutable results from API methods. Key tradeoff: `unmodifiableList` is a live view (changes propagate through); `List.of()` is a snapshot copy (no live relationship).","distractorsMd":["Referential transparency means a function's output depends solely on its inputs with no observable side effects — calling `f(x)` twice always returns the same result and changes nothing external.",null,"A `Spliterator` (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing."]},{"id":"java/functional_programming#b7f4033ef710","section":"java","module":"java/functional_programming","moduleName":"functional programming","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between reduce and collect, and when is using reduce to build a List an anti-pattern?","answerFull":"reduce is designed for immutable accumulation — each step combines two values into a new value (fold). The combiner must be associative and the identity must be correct so parallel splits can merge safely without reordering results. collect is designed for *mutable* reduction — a Supplier creates a fresh container per thread, an accumulator mutates it, and a combiner merges two containers at parallel boundaries. Using reduce to build a List — stream.reduce(new ArrayList<>(), (acc, e) -> { acc.add(e); return acc; }, (a, b) -> { a.addAll(b); return a; }) — mutates the \"identity\" object, which the spec forbids, and creates O(n) intermediate list copies in sequential mode. Use Collectors.toList() or toUnmodifiableList() instead; collect was designed exactly for container accumulation.","correct":"reduce is designed for immutable accumulation — each step combines two values into a new value (fold).","distractors":["Collections.unmodifiableList(list) creates a view — changes to the underlying list are visible through the wrapper, but the wrapper itself rejects modifications.","(1) I/O-bound operations — block ForkJoinPool.commonPool() carrier threads.","A Spliterator (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing."],"distractorIds":["java/functional_programming#336d67295cdc","java/functional_programming#84d30b05dc6b","java/functional_programming#6aead2abcb10"],"concepts":["anti","collect","accumulation","difference","fold","combines"],"questionMd":"What is the difference between `reduce` and `collect`, and when is using `reduce` to build a `List` an anti-pattern?","correctMd":"`reduce` is designed for immutable accumulation — each step combines two values into a new value (fold).","answerFullMd":"`reduce` is designed for immutable accumulation — each step combines two values into a new value (fold). The combiner must be associative and the identity must be correct so parallel splits can merge safely without reordering results. `collect` is designed for *mutable* reduction — a `Supplier` creates a fresh container per thread, an `accumulator` mutates it, and a `combiner` merges two containers at parallel boundaries. Using `reduce` to build a `List` — `stream.reduce(new ArrayList<>(), (acc, e) -> { acc.add(e); return acc; }, (a, b) -> { a.addAll(b); return a; })` — mutates the \"identity\" object, which the spec forbids, and creates O(n) intermediate list copies in sequential mode. Use `Collectors.toList()` or `toUnmodifiableList()` instead; `collect` was designed exactly for container accumulation.","distractorsMd":["`Collections.unmodifiableList(list)` creates a view — changes to the underlying list are visible through the wrapper, but the wrapper itself rejects modifications.",null,"A `Spliterator` (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing."]},{"id":"java/functional_programming#0917a4fb31a5","section":"java","module":"java/functional_programming","moduleName":"functional programming","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between Supplier<T> and Callable<T>, and when does the distinction matter?","answerFull":"Both represent deferred computation that produces a value, but Callable<T> declares throws Exception while Supplier<T> does not. This means Supplier cannot propagate checked exceptions without wrapping them in RuntimeException. Callable integrates with ExecutorService.submit() (returns a Future<T>) and is used wherever the computation may fail with a checked exception. Supplier is the functional-programming default for lazy initialization, factory methods, and Optional.orElseGet(). Practical rule: use Callable for I/O-bound deferred work submitted to a thread pool; use Supplier for pure lazy evaluation. Converting Callable to Supplier requires a try/catch wrapper — a sign the abstraction boundaries are mismatched.","correct":"Both represent deferred computation that produces a value, but Callable<T> declares throws Exception while Supplier<T> does not.","distractors":["Collections.unmodifiableList(list) creates a view — changes to the underlying list are visible through the wrapper, but the wrapper itself rejects modifications.","(1) I/O-bound operations — block ForkJoinPool.commonPool() carrier threads.","A Spliterator (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing."],"distractorIds":["java/functional_programming#336d67295cdc","java/functional_programming#84d30b05dc6b","java/functional_programming#6aead2abcb10"],"concepts":["deferred","distinction","supplier","declares","matter","difference"],"questionMd":"What is the difference between `Supplier<T>` and `Callable<T>`, and when does the distinction matter?","correctMd":"Both represent deferred computation that produces a value, but `Callable<T>` declares `throws Exception` while `Supplier<T>` does not.","answerFullMd":"Both represent deferred computation that produces a value, but `Callable<T>` declares `throws Exception` while `Supplier<T>` does not. This means `Supplier` cannot propagate checked exceptions without wrapping them in `RuntimeException`. `Callable` integrates with `ExecutorService.submit()` (returns a `Future<T>`) and is used wherever the computation may fail with a checked exception. `Supplier` is the functional-programming default for lazy initialization, factory methods, and `Optional.orElseGet()`. Practical rule: use `Callable` for I/O-bound deferred work submitted to a thread pool; use `Supplier` for pure lazy evaluation. Converting `Callable` to `Supplier` requires a try/catch wrapper — a sign the abstraction boundaries are mismatched.","distractorsMd":["`Collections.unmodifiableList(list)` creates a view — changes to the underlying list are visible through the wrapper, but the wrapper itself rejects modifications.",null,"A `Spliterator` (splittable iterator) describes how a stream's source can be split into sub-ranges for parallel processing."]},{"id":"java/functional_programming#20735abc27d8","section":"java","module":"java/functional_programming","moduleName":"functional programming","sourceFile":"README.md","difficulty":"advanced","question":"What is referential transparency, and why does Java's functional interface design not enforce it?","answerFull":"Referential transparency means a function's output depends solely on its inputs with no observable side effects — calling f(x) twice always returns the same result and changes nothing external. Java's Function, Predicate, Consumer etc. do NOT enforce this — a lambda can close over mutable fields, write to a database, call System.out.println, or mutate a shared counter. The Stream API documentation says behavioral parameters \"should be non-interfering and stateless\" but nothing in the type system enforces it. Consequence: a parallel stream with a stateful lambda silently produces incorrect results (broken): java // BROKEN: accumulating into a shared list inside a parallel stream List<Integer> result = new ArrayList<>(); IntStream.range(0, 100).parallel().forEach(result::add); // data race, missing elements // FIXED: use collect(), which is parallel-safe by design List<Integer> result = IntStream.range(0, 100).parallel() .boxed().collect(Collectors.toList()); Practical guidance: treat functional interfaces as trusted contracts; in code review, flag any lambda that reads or writes non-final external state, especially in streams.","correct":"Referential transparency means a function's output depends solely on its inputs with no observable side effects — calling f(x) twice always returns the same result and changes nothing external.","distractors":["Function<T,R> takes one argument of type T, returns R.","Collections.unmodifiableList(list) creates a view — changes to the underlying list are visible through the wrapper, but the wrapper itself rejects modifications.","(1) I/O-bound operations — block ForkJoinPool.commonPool() carrier threads."],"distractorIds":["java/functional_programming#2136c35de982","java/functional_programming#336d67295cdc","java/functional_programming#84d30b05dc6b"],"concepts":["observable","referential","transparency","solely","functional","enforce"],"correctMd":"Referential transparency means a function's output depends solely on its inputs with no observable side effects — calling `f(x)` twice always returns the same result and changes nothing external.","answerFullMd":"Referential transparency means a function's output depends solely on its inputs with no observable side effects — calling `f(x)` twice always returns the same result and changes nothing external. Java's `Function`, `Predicate`, `Consumer` etc. do NOT enforce this — a lambda can close over mutable fields, write to a database, call `System.out.println`, or mutate a shared counter. The Stream API documentation says behavioral parameters \"should be non-interfering and stateless\" but nothing in the type system enforces it. Consequence: a parallel stream with a stateful lambda silently produces incorrect results (broken): ```java // BROKEN: accumulating into a shared list inside a parallel stream List<Integer> result = new ArrayList<>(); IntStream.range(0, 100).parallel().forEach(result::add); // data race, missing elements // FIXED: use collect(), which is parallel-safe by design List<Integer> result = IntStream.range(0, 100).parallel() .boxed().collect(Collectors.toList()); ``` Practical guidance: treat functional interfaces as trusted contracts; in code review, flag any lambda that reads or writes non-final external state, especially in streams.","distractorsMd":["`Function<T,R>` takes one argument of type T, returns R.","`Collections.unmodifiableList(list)` creates a view — changes to the underlying list are visible through the wrapper, but the wrapper itself rejects modifications.",null]},{"id":"java/generics_and_type_system#f8f9feb86580","section":"java","module":"java/generics_and_type_system","moduleName":"generics and type system","sourceFile":"README.md","difficulty":"core","question":"Explain PECS with a real example.","answerFull":"PECS = Producer Extends, Consumer Super. When a collection *produces* values (you read from it), use ? extends T — you can read as T or its supertype. When it *consumes* values (you write into it), use ? super T — you can write T or any subtype. Real example: void addAll(List<? super T> dest, List<? extends T> src) — dest accepts T or supertype (consumer), src provides T or subtype (producer). Practical: copy(List<? super Number> dest, List<? extends Number> src) allows copying List<Integer> into List<Number>.","correct":"PECS = Producer Extends, Consumer Super.","distractors":["@SafeVarargs on a method promises: \"this method does not perform unsafe operations on the varargs parameter\" — specifically, it doesn't write to the varargs array in a way that could cause heap pollution.","A MethodHandle (Java 7, java.lang.invoke.MethodHandles) is a typed, executable reference to a method, constructor, or field, obtained via MethodHandles.Lookup.","A bridge method is a compiler-generated synthetic method that preserves polymorphism after type erasure."],"distractorIds":["java/generics_and_type_system#9f74c8626a56","java/generics_and_type_system#daefce0cea0e","java/generics_and_type_system#aeb0489c97ed"],"concepts":["pecs","super","explain","extends","producer","consumer"],"answerFullMd":"PECS = Producer Extends, Consumer Super. When a collection *produces* values (you read from it), use `? extends T` — you can read as T or its supertype. When it *consumes* values (you write into it), use `? super T` — you can write T or any subtype. Real example: `void addAll(List<? super T> dest, List<? extends T> src)` — dest accepts T or supertype (consumer), src provides T or subtype (producer). Practical: `copy(List<? super Number> dest, List<? extends Number> src)` allows copying `List<Integer>` into `List<Number>`.","distractorsMd":["`@SafeVarargs` on a method promises: \"this method does not perform unsafe operations on the varargs parameter\" — specifically, it doesn't write to the varargs array in a way that could cause heap pollution.","A `MethodHandle` (Java 7, `java.lang.invoke.MethodHandles`) is a typed, executable reference to a method, constructor, or field, obtained via `MethodHandles.Lookup`.",null]},{"id":"java/generics_and_type_system#756fe0cb7ee1","section":"java","module":"java/generics_and_type_system","moduleName":"generics and type system","sourceFile":"README.md","difficulty":"core","question":"What is type erasure and what problems does it cause?","answerFull":"Type erasure removes generic type parameters at compile time, replacing them with bounds (Object for unbounded). Problems: (1) Can't use instanceof with parameterized type: if (obj instanceof List<String>) is illegal. (2) Can't create generic arrays: new T[10] is illegal. (3) Overloading on different generic types is impossible: void method(List<String>) and void method(List<Integer>) have same erasure. (4) Runtime type info is unavailable without TypeToken workarounds. (5) Heap pollution possible through unchecked casts. The benefit: backward compatibility with pre-Java-5 code using raw types.","correct":"Type erasure removes generic type parameters at compile time, replacing them with bounds (Object for unbounded).","distractors":["A bridge method is a compiler-generated synthetic method that preserves polymorphism after type erasure.","Generic arrays would create heap pollution.","A recursive (self-referential) bound constrains T to be comparable to *itself*, which is what lets a generic method sort or compare a list of any Comparable type safely."],"distractorIds":["java/generics_and_type_system#aeb0489c97ed","java/generics_and_type_system#6269106e08c0","java/generics_and_type_system#ffd031e87090"],"concepts":["replacing","erasure","bounds","generic","unbounded","cause"],"correctMd":"Type erasure removes generic type parameters at compile time, replacing them with bounds (`Object` for unbounded).","answerFullMd":"Type erasure removes generic type parameters at compile time, replacing them with bounds (`Object` for unbounded). Problems: (1) Can't use `instanceof` with parameterized type: `if (obj instanceof List<String>)` is illegal. (2) Can't create generic arrays: `new T[10]` is illegal. (3) Overloading on different generic types is impossible: `void method(List<String>)` and `void method(List<Integer>)` have same erasure. (4) Runtime type info is unavailable without TypeToken workarounds. (5) Heap pollution possible through unchecked casts. The benefit: backward compatibility with pre-Java-5 code using raw types.","distractorsMd":[null,null,"A recursive (self-referential) bound constrains `T` to be comparable to *itself*, which is what lets a generic method sort or compare a list of any `Comparable` type safely."]},{"id":"java/generics_and_type_system#aeb0489c97ed","section":"java","module":"java/generics_and_type_system","moduleName":"generics and type system","sourceFile":"README.md","difficulty":"core","question":"What is a bridge method? Show an example.","answerFull":"A bridge method is a compiler-generated synthetic method that preserves polymorphism after type erasure. When a subclass overrides a generic method with a more specific type, or uses covariant return types, the compiler generates a method with the erased signature that delegates to the actual override. Example: class Box<T> { T get() {...} } erases to Object get(). class StringBox extends Box<String> { String get() {...} } declares String get() (covariant). The compiler adds synthetic Object get() { return this.get(); } in StringBox — the bridge. This ensures Box<String> b = new StringBox(); Object o = b.get() works via dynamic dispatch.","correct":"A bridge method is a compiler-generated synthetic method that preserves polymorphism after type erasure.","distractors":["A MethodHandle (Java 7, java.lang.invoke.MethodHandles) is a typed, executable reference to a method, constructor, or field, obtained via MethodHandles.Lookup.","A reified type is one whose full type information survives into the running bytecode; an erased type has that information stripped by the compiler.","Wildcard capture is the compiler binding an unknown ?"],"distractorIds":["java/generics_and_type_system#daefce0cea0e","java/generics_and_type_system#13bdce50220f","java/generics_and_type_system#47011403685f"],"concepts":["bridge","polymorphism","erasure","preserves","show","compiler"],"answerFullMd":"A bridge method is a compiler-generated synthetic method that preserves polymorphism after type erasure. When a subclass overrides a generic method with a more specific type, or uses covariant return types, the compiler generates a method with the erased signature that delegates to the actual override. Example: `class Box<T> { T get() {...} }` erases to `Object get()`. `class StringBox extends Box<String> { String get() {...} }` declares `String get()` (covariant). The compiler adds `synthetic Object get() { return this.get(); }` in StringBox — the bridge. This ensures `Box<String> b = new StringBox(); Object o = b.get()` works via dynamic dispatch.","distractorsMd":["A `MethodHandle` (Java 7, `java.lang.invoke.MethodHandles`) is a typed, executable reference to a method, constructor, or field, obtained via `MethodHandles.Lookup`.",null,null]},{"id":"java/generics_and_type_system#6269106e08c0","section":"java","module":"java/generics_and_type_system","moduleName":"generics and type system","sourceFile":"README.md","difficulty":"core","question":"Why can't you create a generic array (new T[10])?","answerFull":"Generic arrays would create heap pollution. After erasure, new T[10] becomes new Object[10]. Assigning this to a variable declared as T[] looks type-safe, but if exposed externally, another thread could get the T[] reference and store a wrong type into the Object[] — causing a ClassCastException when the array is actually used as T[]. The compiler prevents this by making generic array creation a compile error. Workaround: use (T[]) new Object[10] internally (never expose the raw array) or Array.newInstance(clazz, size) with a Class<T> parameter.","correct":"Generic arrays would create heap pollution.","distractors":["It suppresses the compiler's \"unchecked cast\" warning from generic type erasure.","@SafeVarargs on a method promises: \"this method does not perform unsafe operations on the varargs parameter\" — specifically, it doesn't write to the varargs array in a way that could cause heap pollution.","A recursive (self-referential) bound constrains T to be comparable to *itself*, which is what lets a generic method sort or compare a list of any Comparable type safely."],"distractorIds":["java/generics_and_type_system#bd411280a6cf","java/generics_and_type_system#9f74c8626a56","java/generics_and_type_system#ffd031e87090"],"concepts":["pollution","arrays","generic","create","array","heap"],"questionMd":"Why can't you create a generic array (`new T[10]`)?","answerFullMd":"Generic arrays would create heap pollution. After erasure, `new T[10]` becomes `new Object[10]`. Assigning this to a variable declared as `T[]` looks type-safe, but if exposed externally, another thread could get the `T[]` reference and store a wrong type into the `Object[]` — causing a `ClassCastException` when the array is actually used as `T[]`. The compiler prevents this by making generic array creation a compile error. Workaround: use `(T[]) new Object[10]` internally (never expose the raw array) or `Array.newInstance(clazz, size)` with a `Class<T>` parameter.","distractorsMd":[null,"`@SafeVarargs` on a method promises: \"this method does not perform unsafe operations on the varargs parameter\" — specifically, it doesn't write to the varargs array in a way that could cause heap pollution.","A recursive (self-referential) bound constrains `T` to be comparable to *itself*, which is what lets a generic method sort or compare a list of any `Comparable` type safely."]},{"id":"java/generics_and_type_system#c300329d4c5e","section":"java","module":"java/generics_and_type_system","moduleName":"generics and type system","sourceFile":"README.md","difficulty":"core","question":"What is the difference between getMethod() and getDeclaredMethod()?","answerFull":"getMethod(name, paramTypes) returns a PUBLIC method visible on the class, including inherited public methods from superclasses and interfaces. getDeclaredMethod(name, paramTypes) returns any method (public/protected/package/private) declared *directly on this class*, excluding inherited methods. To access private methods, use getDeclaredMethod() + setAccessible(true). To traverse the full inheritance chain, you must walk getSuperclass() iteratively.","correct":"getMethod(name, paramTypes) returns a PUBLIC method visible on the class, including inherited public methods from superclasses and interfaces.","distractors":["A bridge method is a compiler-generated synthetic method that preserves polymorphism after type erasure.","Arrays are covariant (String[] is an Object[]) for historical reasons: Java 1.0 had no generics and needed to write generic array-processing methods like Arrays.sort(Object[]).","A recursive (self-referential) bound constrains T to be comparable to *itself*, which is what lets a generic method sort or compare a list of any Comparable type safely."],"distractorIds":["java/generics_and_type_system#aeb0489c97ed","java/generics_and_type_system#df8cd5d8cd81","java/generics_and_type_system#ffd031e87090"],"concepts":["getmethod","paramtypes","inherited","superclasses","difference","interfaces"],"questionMd":"What is the difference between `getMethod()` and `getDeclaredMethod()`?","correctMd":"`getMethod(name, paramTypes)` returns a PUBLIC method visible on the class, including inherited public methods from superclasses and interfaces.","answerFullMd":"`getMethod(name, paramTypes)` returns a PUBLIC method visible on the class, including inherited public methods from superclasses and interfaces. `getDeclaredMethod(name, paramTypes)` returns any method (public/protected/package/private) declared *directly on this class*, excluding inherited methods. To access private methods, use `getDeclaredMethod()` + `setAccessible(true)`. To traverse the full inheritance chain, you must walk `getSuperclass()` iteratively.","distractorsMd":[null,"Arrays are covariant (`String[]` is an `Object[]`) for historical reasons: Java 1.0 had no generics and needed to write generic array-processing methods like `Arrays.sort(Object[])`.","A recursive (self-referential) bound constrains `T` to be comparable to *itself*, which is what lets a generic method sort or compare a list of any `Comparable` type safely."]},{"id":"java/generics_and_type_system#52eb805d8269","section":"java","module":"java/generics_and_type_system","moduleName":"generics and type system","sourceFile":"README.md","difficulty":"intermediate","question":"What is heap pollution?","answerFull":"Heap pollution occurs when a variable of a parameterized type refers to an object of the wrong parameterized type — possible because of erasure. Example: List<String> list = (List<String>) (List) new ArrayList<Integer>() — the cast is legal (both are List after erasure), but String s = list.get(0) throws ClassCastException. The JVM inserts a checkcast instruction at the point of *use*, not the point of assignment. The compiler warns about unchecked casts to prevent this; @SafeVarargs suppresses warnings for varargs that don't introduce pollution.","correct":"Heap pollution occurs when a variable of a parameterized type refers to an object of the wrong parameterized type — possible because of erasure.","distractors":["Type erasure removes generic type parameters at runtime — List<String> and List<Integer> are both just List.class.","A bridge method is a compiler-generated synthetic method that preserves polymorphism after type erasure.","Type erasure removes generic type parameters at compile time, replacing them with bounds (Object for unbounded)."],"distractorIds":["java/generics_and_type_system#aea839992353","java/generics_and_type_system#aeb0489c97ed","java/generics_and_type_system#756fe0cb7ee1"],"concepts":["pollution","erasure","parameterized","refers","possible","variable"],"answerFullMd":"Heap pollution occurs when a variable of a parameterized type refers to an object of the wrong parameterized type — possible because of erasure. Example: `List<String> list = (List<String>) (List) new ArrayList<Integer>()` — the cast is legal (both are `List` after erasure), but `String s = list.get(0)` throws `ClassCastException`. The JVM inserts a checkcast instruction at the point of *use*, not the point of assignment. The compiler warns about unchecked casts to prevent this; `@SafeVarargs` suppresses warnings for varargs that don't introduce pollution.","distractorsMd":["Type erasure removes generic type parameters at runtime — `List<String>` and `List<Integer>` are both just `List.class`.",null,"Type erasure removes generic type parameters at compile time, replacing them with bounds (`Object` for unbounded)."]},{"id":"java/generics_and_type_system#887f33da994a","section":"java","module":"java/generics_and_type_system","moduleName":"generics and type system","sourceFile":"README.md","difficulty":"intermediate","question":"What is the TypeToken pattern and why is it needed?","answerFull":"TypeToken exploits the fact that an anonymous class retains its superclass's type parameters in the bytecode as a ParameterizedType. new TypeToken<List<String>>() {} creates an anonymous subclass of TypeToken<List<String>>. Calling getClass().getGenericSuperclass() returns TypeToken<List<String>> as a ParameterizedType, from which getActualTypeArguments()[0] returns the Type for List<String>. Without TypeToken, List<String>.class is illegal and you'd get only List.class — erased. Libraries like Gson, Guice, and Jackson use TypeToken for proper generic type deserialization/injection.","correct":"TypeToken exploits the fact that an anonymous class retains its superclass's type parameters in the bytecode as a ParameterizedType.","distractors":["Type erasure removes generic type parameters at compile time, replacing them with bounds (Object for unbounded).","Arrays are covariant (String[] is an Object[]) for historical reasons: Java 1.0 had no generics and needed to write generic array-processing methods like Arrays.sort(Object[]).","A reified type is one whose full type information survives into the running bytecode; an erased type has that information stripped by the compiler."],"distractorIds":["java/generics_and_type_system#756fe0cb7ee1","java/generics_and_type_system#df8cd5d8cd81","java/generics_and_type_system#13bdce50220f"],"concepts":["parameterizedtype","typetoken","retains","superclass","anonymous","exploits"],"correctMd":"TypeToken exploits the fact that an anonymous class retains its superclass's type parameters in the bytecode as a `ParameterizedType`.","answerFullMd":"TypeToken exploits the fact that an anonymous class retains its superclass's type parameters in the bytecode as a `ParameterizedType`. `new TypeToken<List<String>>() {}` creates an anonymous subclass of `TypeToken<List<String>>`. Calling `getClass().getGenericSuperclass()` returns `TypeToken<List<String>>` as a `ParameterizedType`, from which `getActualTypeArguments()[0]` returns the `Type` for `List<String>`. Without TypeToken, `List<String>.class` is illegal and you'd get only `List.class` — erased. Libraries like Gson, Guice, and Jackson use TypeToken for proper generic type deserialization/injection.","distractorsMd":["Type erasure removes generic type parameters at compile time, replacing them with bounds (`Object` for unbounded).","Arrays are covariant (`String[]` is an `Object[]`) for historical reasons: Java 1.0 had no generics and needed to write generic array-processing methods like `Arrays.sort(Object[])`.",null]},{"id":"java/generics_and_type_system#9f74c8626a56","section":"java","module":"java/generics_and_type_system","moduleName":"generics and type system","sourceFile":"README.md","difficulty":"intermediate","question":"What does @SafeVarargs promise?","answerFull":"@SafeVarargs on a method promises: \"this method does not perform unsafe operations on the varargs parameter\" — specifically, it doesn't write to the varargs array in a way that could cause heap pollution. It suppresses the \"possible heap pollution from parameterized vararg type\" unchecked warning. The annotation is placed on final, static, or private methods (must not be overridable, as overrides might break the promise). You must manually verify the promise — the compiler doesn't check it. Incorrect use means callers get ClassCastException at unexpected points.","correct":"@SafeVarargs on a method promises: \"this method does not perform unsafe operations on the varargs parameter\" — specifically, it doesn't write to the varargs array in a way that could cause heap pollution.","distractors":["A MethodHandle (Java 7, java.lang.invoke.MethodHandles) is a typed, executable reference to a method, constructor, or field, obtained via MethodHandles.Lookup.","getMethod(name, paramTypes) returns a PUBLIC method visible on the class, including inherited public methods from superclasses and interfaces.","A recursive (self-referential) bound constrains T to be comparable to *itself*, which is what lets a generic method sort or compare a list of any Comparable type safely."],"distractorIds":["java/generics_and_type_system#daefce0cea0e","java/generics_and_type_system#c300329d4c5e","java/generics_and_type_system#ffd031e87090"],"concepts":["promises","safevarargs","varargs","pollution","promise","unsafe"],"questionMd":"What does `@SafeVarargs` promise?","correctMd":"`@SafeVarargs` on a method promises: \"this method does not perform unsafe operations on the varargs parameter\" — specifically, it doesn't write to the varargs array in a way that could cause heap pollution.","answerFullMd":"`@SafeVarargs` on a method promises: \"this method does not perform unsafe operations on the varargs parameter\" — specifically, it doesn't write to the varargs array in a way that could cause heap pollution. It suppresses the \"possible heap pollution from parameterized vararg type\" unchecked warning. The annotation is placed on `final`, `static`, or `private` methods (must not be overridable, as overrides might break the promise). You must manually verify the promise — the compiler doesn't check it. Incorrect use means callers get `ClassCastException` at unexpected points.","distractorsMd":["A `MethodHandle` (Java 7, `java.lang.invoke.MethodHandles`) is a typed, executable reference to a method, constructor, or field, obtained via `MethodHandles.Lookup`.","`getMethod(name, paramTypes)` returns a PUBLIC method visible on the class, including inherited public methods from superclasses and interfaces.","A recursive (self-referential) bound constrains `T` to be comparable to *itself*, which is what lets a generic method sort or compare a list of any `Comparable` type safely."]},{"id":"java/generics_and_type_system#abd970b50862","section":"java","module":"java/generics_and_type_system","moduleName":"generics and type system","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between Class.forName() and ClassLoader.loadClass()?","answerFull":"Class.forName(name) loads the class AND initializes it (runs <clinit> static initializers). ClassLoader.loadClass(name) loads the class but does NOT initialize it (deferred until first use). Class.forName(name, initialize, loader) gives full control. Practical implication: Class.forName(\"com.mysql.jdbc.Driver\") was the traditional JDBC driver registration mechanism — it worked because loading the class triggered static {} initialization which registered the driver. If you used classLoader.loadClass() instead, the driver would not be registered.","correct":"Class.forName(name) loads the class AND initializes it (runs <clinit> static initializers).","distractors":["TypeToken exploits the fact that an anonymous class retains its superclass's type parameters in the bytecode as a ParameterizedType.","Type erasure removes generic type parameters at runtime — List<String> and List<Integer> are both just List.class.","getMethod(name, paramTypes) returns a PUBLIC method visible on the class, including inherited public methods from superclasses and interfaces."],"distractorIds":["java/generics_and_type_system#887f33da994a","java/generics_and_type_system#aea839992353","java/generics_and_type_system#c300329d4c5e"],"concepts":["classloader","forname","initializers","clinit","initializes","difference"],"questionMd":"What is the difference between `Class.forName()` and `ClassLoader.loadClass()`?","correctMd":"`Class.forName(name)` loads the class AND initializes it (runs `<clinit>` static initializers).","answerFullMd":"`Class.forName(name)` loads the class AND initializes it (runs `<clinit>` static initializers). `ClassLoader.loadClass(name)` loads the class but does NOT initialize it (deferred until first use). `Class.forName(name, initialize, loader)` gives full control. Practical implication: `Class.forName(\"com.mysql.jdbc.Driver\")` was the traditional JDBC driver registration mechanism — it worked because loading the class triggered `static {}` initialization which registered the driver. If you used `classLoader.loadClass()` instead, the driver would not be registered.","distractorsMd":["TypeToken exploits the fact that an anonymous class retains its superclass's type parameters in the bytecode as a `ParameterizedType`.","Type erasure removes generic type parameters at runtime — `List<String>` and `List<Integer>` are both just `List.class`.","`getMethod(name, paramTypes)` returns a PUBLIC method visible on the class, including inherited public methods from superclasses and interfaces."]},{"id":"java/generics_and_type_system#df8cd5d8cd81","section":"java","module":"java/generics_and_type_system","moduleName":"generics and type system","sourceFile":"README.md","difficulty":"advanced","question":"Why is array assignment covariant in Java but generic type assignment invariant? What runtime mechanism catches array covariance violations?","answerFull":"Arrays are covariant (String[] is an Object[]) for historical reasons: Java 1.0 had no generics and needed to write generic array-processing methods like Arrays.sort(Object[]). The runtime safety net is ArrayStoreException — on every array element write, the JVM checks that the stored type is compatible with the array's actual component type (stored in the array header). This is a runtime check. Generics are invariant (List<String> is NOT List<Object>) because type erasure means no runtime type information exists for the generic parameter — there's no mechanism to perform the equivalent of ArrayStoreException at runtime for generic containers. The compiler performs all checks statically at compile time. Summary: arrays: covariant + runtime ArrayStoreException; generics: invariant + compile-time error.","correct":"Arrays are covariant (String[] is an Object[]) for historical reasons: Java 1.0 had no generics and needed to write generic array-processing methods like Arrays.sort(Object[]).","distractors":["@SafeVarargs on a method promises: \"this method does not perform unsafe operations on the varargs parameter\" — specifically, it doesn't write to the varargs array in a way that could cause heap pollution.","It suppresses the compiler's \"unchecked cast\" warning from generic type erasure.","A MethodHandle (Java 7, java.lang.invoke.MethodHandles) is a typed, executable reference to a method, constructor, or field, obtained via MethodHandles.Lookup."],"distractorIds":["java/generics_and_type_system#9f74c8626a56","java/generics_and_type_system#bd411280a6cf","java/generics_and_type_system#daefce0cea0e"],"concepts":["generics","catches","had","assignment","covariant","arrays"],"correctMd":"Arrays are covariant (`String[]` is an `Object[]`) for historical reasons: Java 1.0 had no generics and needed to write generic array-processing methods like `Arrays.sort(Object[])`.","answerFullMd":"Arrays are covariant (`String[]` is an `Object[]`) for historical reasons: Java 1.0 had no generics and needed to write generic array-processing methods like `Arrays.sort(Object[])`. The runtime safety net is `ArrayStoreException` — on every array element write, the JVM checks that the stored type is compatible with the array's actual component type (stored in the array header). This is a runtime check. Generics are invariant (`List<String>` is NOT `List<Object>`) because type erasure means no runtime type information exists for the generic parameter — there's no mechanism to perform the equivalent of `ArrayStoreException` at runtime for generic containers. The compiler performs all checks statically at compile time. Summary: arrays: covariant + runtime `ArrayStoreException`; generics: invariant + compile-time error.","distractorsMd":["`@SafeVarargs` on a method promises: \"this method does not perform unsafe operations on the varargs parameter\" — specifically, it doesn't write to the varargs array in a way that could cause heap pollution.",null,"A `MethodHandle` (Java 7, `java.lang.invoke.MethodHandles`) is a typed, executable reference to a method, constructor, or field, obtained via `MethodHandles.Lookup`."]},{"id":"java/generics_and_type_system#daefce0cea0e","section":"java","module":"java/generics_and_type_system","moduleName":"generics and type system","sourceFile":"README.md","difficulty":"advanced","question":"What is a MethodHandle and when is it preferable to Method.invoke()?","answerFull":"A MethodHandle (Java 7, java.lang.invoke.MethodHandles) is a typed, executable reference to a method, constructor, or field, obtained via MethodHandles.Lookup. The JIT can optimize MethodHandle invocations much more aggressively than Method.invoke() — it can inline through the handle and even eliminate the call overhead entirely with invokeExact(). Method.invoke() has per-call overhead for access checking, argument boxing into Object[], and dispatch overhead. Benchmark comparison: for 1M calls, Method.invoke() ~80ms, MethodHandle.invoke() ~10ms, invokeExact() ~2ms, direct call ~1ms. Prefer MethodHandle over Method.invoke() when the same method is called repeatedly (frameworks, serialization, DI containers). Use invokeExact() when you can guarantee exact type matches — it's the fastest option.","correct":"A MethodHandle (Java 7, java.lang.invoke.MethodHandles) is a typed, executable reference to a method, constructor, or field, obtained via MethodHandles.Lookup.","distractors":["@SafeVarargs on a method promises: \"this method does not perform unsafe operations on the varargs parameter\" — specifically, it doesn't write to the varargs array in a way that could cause heap pollution.","A bridge method is a compiler-generated synthetic method that preserves polymorphism after type erasure.","A recursive (self-referential) bound constrains T to be comparable to *itself*, which is what lets a generic method sort or compare a list of any Comparable type safely."],"distractorIds":["java/generics_and_type_system#9f74c8626a56","java/generics_and_type_system#aeb0489c97ed","java/generics_and_type_system#ffd031e87090"],"concepts":["methodhandles","obtained","methodhandle","executable","invoke","lang"],"questionMd":"What is a `MethodHandle` and when is it preferable to `Method.invoke()`?","correctMd":"A `MethodHandle` (Java 7, `java.lang.invoke.MethodHandles`) is a typed, executable reference to a method, constructor, or field, obtained via `MethodHandles.Lookup`.","answerFullMd":"A `MethodHandle` (Java 7, `java.lang.invoke.MethodHandles`) is a typed, executable reference to a method, constructor, or field, obtained via `MethodHandles.Lookup`. The JIT can optimize `MethodHandle` invocations much more aggressively than `Method.invoke()` — it can inline through the handle and even eliminate the call overhead entirely with `invokeExact()`. `Method.invoke()` has per-call overhead for access checking, argument boxing into `Object[]`, and dispatch overhead. Benchmark comparison: for 1M calls, `Method.invoke()` ~80ms, `MethodHandle.invoke()` ~10ms, `invokeExact()` ~2ms, direct call ~1ms. Prefer `MethodHandle` over `Method.invoke()` when the same method is called repeatedly (frameworks, serialization, DI containers). Use `invokeExact()` when you can guarantee exact type matches — it's the fastest option.","distractorsMd":["`@SafeVarargs` on a method promises: \"this method does not perform unsafe operations on the varargs parameter\" — specifically, it doesn't write to the varargs array in a way that could cause heap pollution.",null,"A recursive (self-referential) bound constrains `T` to be comparable to *itself*, which is what lets a generic method sort or compare a list of any `Comparable` type safely."]},{"id":"java/generics_and_type_system#0564e8c1df39","section":"java","module":"java/generics_and_type_system","moduleName":"generics and type system","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between getFields() / getMethods() and their getDeclared*() counterparts, and which do framework code use?","answerFull":"getFields() returns all public fields from the class and its entire superclass/interface hierarchy. getDeclaredFields() returns all fields declared directly in that class — any visibility — but nothing inherited. Same pattern applies to getMethods() vs getDeclaredMethods() and constructors. Framework code (serializers, ORMs, DI containers) typically uses getDeclaredFields() to access private fields, then calls field.setAccessible(true). Traversal of the full hierarchy requires a loop up the getSuperclass() chain — stop at Object.class. With JPMS (Java 9+), setAccessible(true) on a field in another module requires the module to open the package (e.g., opens com.example to com.fasterxml.jackson.databind); failing to do so throws InaccessibleObjectException at runtime.","correct":"getFields() returns all public fields from the class and its entire superclass/interface hierarchy.","distractors":["Type erasure removes generic type parameters at runtime — List<String> and List<Integer> are both just List.class.","TypeToken exploits the fact that an anonymous class retains its superclass's type parameters in the bytecode as a ParameterizedType.","Class.forName(name) loads the class AND initializes it (runs <clinit> static initializers)."],"distractorIds":["java/generics_and_type_system#aea839992353","java/generics_and_type_system#887f33da994a","java/generics_and_type_system#abd970b50862"],"concepts":["getfields","superclass","difference","hierarchy","public","framework"],"questionMd":"What is the difference between `getFields()` / `getMethods()` and their `getDeclared*()` counterparts, and which do framework code use?","correctMd":"`getFields()` returns all **public** fields from the class and its entire superclass/interface hierarchy.","answerFullMd":"`getFields()` returns all **public** fields from the class and its entire superclass/interface hierarchy. `getDeclaredFields()` returns **all fields declared directly in that class** — any visibility — but nothing inherited. Same pattern applies to `getMethods()` vs `getDeclaredMethods()` and constructors. Framework code (serializers, ORMs, DI containers) typically uses `getDeclaredFields()` to access private fields, then calls `field.setAccessible(true)`. Traversal of the full hierarchy requires a loop up the `getSuperclass()` chain — stop at `Object.class`. With JPMS (Java 9+), `setAccessible(true)` on a field in another module requires the module to open the package (e.g., `opens com.example to com.fasterxml.jackson.databind`); failing to do so throws `InaccessibleObjectException` at runtime.","distractorsMd":["Type erasure removes generic type parameters at runtime — `List<String>` and `List<Integer>` are both just `List.class`.","TypeToken exploits the fact that an anonymous class retains its superclass's type parameters in the bytecode as a `ParameterizedType`.","`Class.forName(name)` loads the class AND initializes it (runs `<clinit>` static initializers)."]},{"id":"java/generics_and_type_system#bd411280a6cf","section":"java","module":"java/generics_and_type_system","moduleName":"generics and type system","sourceFile":"README.md","difficulty":"advanced","question":"What is @SuppressWarnings(\"unchecked\") and when is it safe vs. dangerous to use it?","answerFull":"It suppresses the compiler's \"unchecked cast\" warning from generic type erasure. Safe to use when you have a provable invariant that the cast cannot fail: a private map you fully control, an explicit instanceof check before cast, or a well-typed internal API contract. Dangerous when external code can insert arbitrary types into the container, because erasure makes the cast invisible at runtime — the ClassCastException fires when the variable is *used*, not when it's cast, making the bug hard to trace: java // DANGEROUS: cast is suppressed but the real ClassCastException fires far from here @SuppressWarnings(\"unchecked\") Map<String, User> users = (Map<String, User>) externalMap; // any value type is silently accepted users.get(\"alice\").getRoles(); // ClassCastException here, not at the cast above Rule: always add a comment documenting the invariant. If you cannot articulate the invariant, the suppression is not safe.","correct":"It suppresses the compiler's \"unchecked cast\" warning from generic type erasure.","distractors":["Type erasure removes generic type parameters at runtime — List<String> and List<Integer> are both just List.class.","A bridge method is a compiler-generated synthetic method that preserves polymorphism after type erasure.","Type erasure removes generic type parameters at compile time, replacing them with bounds (Object for unbounded)."],"distractorIds":["java/generics_and_type_system#aea839992353","java/generics_and_type_system#aeb0489c97ed","java/generics_and_type_system#756fe0cb7ee1"],"concepts":["cast","dangerous","unchecked","suppresses","warning","erasure"],"questionMd":"What is `@SuppressWarnings(\"unchecked\")` and when is it safe vs. dangerous to use it?","answerFullMd":"It suppresses the compiler's \"unchecked cast\" warning from generic type erasure. Safe to use when you have a provable invariant that the cast cannot fail: a private map you fully control, an explicit `instanceof` check before cast, or a well-typed internal API contract. Dangerous when external code can insert arbitrary types into the container, because erasure makes the cast invisible at runtime — the `ClassCastException` fires when the variable is *used*, not when it's cast, making the bug hard to trace: ```java // DANGEROUS: cast is suppressed but the real ClassCastException fires far from here @SuppressWarnings(\"unchecked\") Map<String, User> users = (Map<String, User>) externalMap; // any value type is silently accepted users.get(\"alice\").getRoles(); // ClassCastException here, not at the cast above ``` Rule: always add a comment documenting the invariant. If you cannot articulate the invariant, the suppression is not safe.","distractorsMd":["Type erasure removes generic type parameters at runtime — `List<String>` and `List<Integer>` are both just `List.class`.",null,"Type erasure removes generic type parameters at compile time, replacing them with bounds (`Object` for unbounded)."]},{"id":"java/generics_and_type_system#aea839992353","section":"java","module":"java/generics_and_type_system","moduleName":"generics and type system","sourceFile":"README.md","difficulty":"advanced","question":"What is TypeToken / ParameterizedType and why is it needed to preserve generic type information at runtime?","answerFull":"Type erasure removes generic type parameters at runtime — List<String> and List<Integer> are both just List.class. When a framework (JSON deserializer, DI container) needs the full parameterised type at runtime, it uses ParameterizedType via an anonymous subclass trick: java // The anonymous class retains List<String> as its generic supertype in the .class file Type type = new TypeToken<List<String>>() {}.getType(); // Guava or Gson introspect: type.getClass().getGenericSuperclass() -> ParameterizedType -> String // Gson usage List<String> result = gson.fromJson(json, new TypeToken<List<String>>(){}.getType()); // Without TypeToken: gson.fromJson(json, List.class) -> List<Object> (erasure) Jackson uses TypeReference<T> for the same purpose. This works because the byte code of an anonymous class stores its generic supertype in the Signature attribute, which getGenericSuperclass() reads at runtime — it's the one place erasure doesn't fully erase. Practical rule: whenever you write a method that must produce the right generic instance at runtime, accept TypeToken<T> as a parameter rather than Class<T>.","correct":"Type erasure removes generic type parameters at runtime — List<String> and List<Integer> are both just List.class.","distractors":["TypeToken exploits the fact that an anonymous class retains its superclass's type parameters in the bytecode as a ParameterizedType.","Heap pollution occurs when a variable of a parameterized type refers to an object of the wrong parameterized type — possible because of erasure.","Type erasure removes generic type parameters at compile time, replacing them with bounds (Object for unbounded)."],"distractorIds":["java/generics_and_type_system#887f33da994a","java/generics_and_type_system#52eb805d8269","java/generics_and_type_system#756fe0cb7ee1"],"concepts":["parameterizedtype","typetoken","preserve","erasure","generic","integer"],"questionMd":"What is `TypeToken` / `ParameterizedType` and why is it needed to preserve generic type information at runtime?","correctMd":"Type erasure removes generic type parameters at runtime — `List<String>` and `List<Integer>` are both just `List.class`.","answerFullMd":"Type erasure removes generic type parameters at runtime — `List<String>` and `List<Integer>` are both just `List.class`. When a framework (JSON deserializer, DI container) needs the full parameterised type at runtime, it uses `ParameterizedType` via an anonymous subclass trick: ```java // The anonymous class retains List<String> as its generic supertype in the .class file Type type = new TypeToken<List<String>>() {}.getType(); // Guava or Gson introspect: type.getClass().getGenericSuperclass() -> ParameterizedType -> String // Gson usage List<String> result = gson.fromJson(json, new TypeToken<List<String>>(){}.getType()); // Without TypeToken: gson.fromJson(json, List.class) -> List<Object> (erasure) ``` Jackson uses `TypeReference<T>` for the same purpose. This works because the byte code of an anonymous class stores its generic supertype in the `Signature` attribute, which `getGenericSuperclass()` reads at runtime — it's the one place erasure doesn't fully erase. Practical rule: whenever you write a method that must produce the right generic instance at runtime, accept `TypeToken<T>` as a parameter rather than `Class<T>`.","distractorsMd":["TypeToken exploits the fact that an anonymous class retains its superclass's type parameters in the bytecode as a `ParameterizedType`.",null,"Type erasure removes generic type parameters at compile time, replacing them with bounds (`Object` for unbounded)."]},{"id":"java/generics_and_type_system#13bdce50220f","section":"java","module":"java/generics_and_type_system","moduleName":"generics and type system","sourceFile":"README.md","difficulty":"advanced","question":"What does \"reified\" mean, and which Java types are reified vs erased at runtime?","answerFull":"A reified type is one whose full type information survives into the running bytecode; an erased type has that information stripped by the compiler. Primitives (int, boolean), raw types (List), Class<?> objects, and array component types (String[]) are reified — the JVM can check them at runtime, which is exactly why arr instanceof String[] compiles but list instanceof List<String> does not. Parameterized types (List<String>, Map<K,V>) are erased to their bound (List, Map) — the <String> part exists only in the .class file's Signature attribute for reflection, not as a runtime-checkable type. This distinction is why C#/.NET generics (reified) behave differently from Java generics (erased) despite similar syntax.","correct":"A reified type is one whose full type information survives into the running bytecode; an erased type has that information stripped by the compiler.","distractors":["Arrays are covariant (String[] is an Object[]) for historical reasons: Java 1.0 had no generics and needed to write generic array-processing methods like Arrays.sort(Object[]).","A MethodHandle (Java 7, java.lang.invoke.MethodHandles) is a typed, executable reference to a method, constructor, or field, obtained via MethodHandles.Lookup.","Type erasure removes generic type parameters at runtime — List<String> and List<Integer> are both just List.class."],"distractorIds":["java/generics_and_type_system#df8cd5d8cd81","java/generics_and_type_system#daefce0cea0e","java/generics_and_type_system#aea839992353"],"concepts":["erased","reified","stripped","survives","compiler","bytecode"],"answerFullMd":"A reified type is one whose full type information survives into the running bytecode; an erased type has that information stripped by the compiler. Primitives (`int`, `boolean`), raw types (`List`), `Class<?>` objects, and array component types (`String[]`) are reified — the JVM can check them at runtime, which is exactly why `arr instanceof String[]` compiles but `list instanceof List<String>` does not. Parameterized types (`List<String>`, `Map<K,V>`) are erased to their bound (`List`, `Map`) — the `<String>` part exists only in the `.class` file's `Signature` attribute for reflection, not as a runtime-checkable type. This distinction is why C#/.NET generics (reified) behave differently from Java generics (erased) despite similar syntax.","distractorsMd":["Arrays are covariant (`String[]` is an `Object[]`) for historical reasons: Java 1.0 had no generics and needed to write generic array-processing methods like `Arrays.sort(Object[])`.","A `MethodHandle` (Java 7, `java.lang.invoke.MethodHandles`) is a typed, executable reference to a method, constructor, or field, obtained via `MethodHandles.Lookup`.","Type erasure removes generic type parameters at runtime — `List<String>` and `List<Integer>` are both just `List.class`."]},{"id":"java/generics_and_type_system#ffd031e87090","section":"java","module":"java/generics_and_type_system","moduleName":"generics and type system","sourceFile":"README.md","difficulty":"advanced","question":"What is a recursive generic bound like <T extends Comparable<T>>, and why is it needed?","answerFull":"A recursive (self-referential) bound constrains T to be comparable to *itself*, which is what lets a generic method sort or compare a list of any Comparable type safely. The bound <T extends Comparable<T>> reads as \"T must implement Comparable of its own type\" — so T.compareTo(T) is guaranteed to accept another T, not some unrelated type: java public static <T extends Comparable<T>> T max(List<T> list) { T result = list.get(0); for (T item : list) if (item.compareTo(result) > 0) result = item; return result; } // Without the recursive bound, T extends Comparable would only guarantee // compareTo(Object), losing type safety on the argument. This is the exact bound the JDK uses on Collections.max() and Comparable<T> itself — it is the standard idiom whenever a generic algorithm needs elements to compare against their own type.","correct":"A recursive (self-referential) bound constrains T to be comparable to *itself*, which is what lets a generic method sort or compare a list of any Comparable type safely.","distractors":["@SafeVarargs on a method promises: \"this method does not perform unsafe operations on the varargs parameter\" — specifically, it doesn't write to the varargs array in a way that could cause heap pollution.","It suppresses the compiler's \"unchecked cast\" warning from generic type erasure.","PECS = Producer Extends, Consumer Super."],"distractorIds":["java/generics_and_type_system#9f74c8626a56","java/generics_and_type_system#bd411280a6cf","java/generics_and_type_system#f8f9feb86580"],"concepts":["constrains","referential","comparable","safely","generic","compare"],"questionMd":"What is a recursive generic bound like `<T extends Comparable<T>>`, and why is it needed?","correctMd":"A recursive (self-referential) bound constrains `T` to be comparable to *itself*, which is what lets a generic method sort or compare a list of any `Comparable` type safely.","answerFullMd":"A recursive (self-referential) bound constrains `T` to be comparable to *itself*, which is what lets a generic method sort or compare a list of any `Comparable` type safely. The bound `<T extends Comparable<T>>` reads as \"T must implement Comparable of its own type\" — so `T.compareTo(T)` is guaranteed to accept another `T`, not some unrelated type: ```java public static <T extends Comparable<T>> T max(List<T> list) { T result = list.get(0); for (T item : list) if (item.compareTo(result) > 0) result = item; return result; } // Without the recursive bound, T extends Comparable would only guarantee // compareTo(Object), losing type safety on the argument. ``` This is the exact bound the JDK uses on `Collections.max()` and `Comparable<T>` itself — it is the standard idiom whenever a generic algorithm needs elements to compare against their own type.","distractorsMd":["`@SafeVarargs` on a method promises: \"this method does not perform unsafe operations on the varargs parameter\" — specifically, it doesn't write to the varargs array in a way that could cause heap pollution.",null,null]},{"id":"java/generics_and_type_system#47011403685f","section":"java","module":"java/generics_and_type_system","moduleName":"generics and type system","sourceFile":"README.md","difficulty":"advanced","question":"What is \"wildcard capture\" and why does the compiler reject writing to a List<?> even inside your own method?","answerFull":"Wildcard capture is the compiler binding an unknown ? to one fresh type variable, but only within a single expression, not across separate statements. That narrow scope is why reading an element out of a List<?> and writing it straight back via set() still fails to compile, even though the operation is logically safe — the compiler cannot prove the two ? occurrences denote the same underlying type, so any add/set call is rejected as unsafe. The fix is to extract a private generic helper method with a named type variable, which forces one capture for the whole method body: java // BROKEN: compiler can't prove list.get(0) matches the type list.set() expects static void swapFirst(List<?> list) { list.set(0, list.get(1)); } // compile error // FIX: helper captures the wildcard as a concrete (if unnamed) type T static void swapFirst(List<?> list) { swapHelper(list); } private static <T> void swapHelper(List<T> list) { list.set(0, list.get(1)); }","correct":"Wildcard capture is the compiler binding an unknown ?","distractors":["A recursive (self-referential) bound constrains T to be comparable to *itself*, which is what lets a generic method sort or compare a list of any Comparable type safely.","A MethodHandle (Java 7, java.lang.invoke.MethodHandles) is a typed, executable reference to a method, constructor, or field, obtained via MethodHandles.Lookup.","@SafeVarargs on a method promises: \"this method does not perform unsafe operations on the varargs parameter\" — specifically, it doesn't write to the varargs array in a way that could cause heap pollution."],"distractorIds":["java/generics_and_type_system#ffd031e87090","java/generics_and_type_system#daefce0cea0e","java/generics_and_type_system#9f74c8626a56"],"concepts":["wildcard","reject","unknown","binding","compiler","capture"],"questionMd":"What is \"wildcard capture\" and why does the compiler reject writing to a `List<?>` even inside your own method?","answerFullMd":"Wildcard capture is the compiler binding an unknown `?` to one fresh type variable, but only within a single expression, not across separate statements. That narrow scope is why reading an element out of a `List<?>` and writing it straight back via `set()` still fails to compile, even though the operation is logically safe — the compiler cannot prove the two `?` occurrences denote the same underlying type, so any `add`/`set` call is rejected as unsafe. The fix is to extract a private generic helper method with a named type variable, which forces one capture for the whole method body: ```java // BROKEN: compiler can't prove list.get(0) matches the type list.set() expects static void swapFirst(List<?> list) { list.set(0, list.get(1)); } // compile error // FIX: helper captures the wildcard as a concrete (if unnamed) type T static void swapFirst(List<?> list) { swapHelper(list); } private static <T> void swapHelper(List<T> list) { list.set(0, list.get(1)); } ```","distractorsMd":["A recursive (self-referential) bound constrains `T` to be comparable to *itself*, which is what lets a generic method sort or compare a list of any `Comparable` type safely.","A `MethodHandle` (Java 7, `java.lang.invoke.MethodHandles`) is a typed, executable reference to a method, constructor, or field, obtained via `MethodHandles.Lookup`.","`@SafeVarargs` on a method promises: \"this method does not perform unsafe operations on the varargs parameter\" — specifically, it doesn't write to the varargs array in a way that could cause heap pollution."]},{"id":"java/grpc_protobuf#0809225c9b8c","section":"java","module":"java/grpc_protobuf","moduleName":"grpc protobuf","sourceFile":"README.md","difficulty":"core","question":"What is the difference between gRPC and Protocol Buffers?","answerFull":"Protocol Buffers is the serialization format and interface definition language — it defines how messages are described in .proto and encoded to compact binary; it is usable on its own for storage or messaging. gRPC is the RPC framework layered on top: it maps service methods to HTTP/2 streams and defines metadata, the Status error model, streaming modes, deadlines, and cancellation. Protobuf is the \"what's in the message\"; gRPC is the \"how the call travels.\"","correct":"Protocol Buffers is the serialization format and interface definition language — it defines how messages are described in .proto and encoded to compact binary; it is usable on its own for storage or messaging.","distractors":["The field number, not the name, is the field's identity on the wire — the encoded bytes carry the number, never the name.","When a client cancels (explicitly, or by hitting its deadline, or by disconnecting), gRPC propagates the cancellation down the call tree as a Context cancellation.","A timeout is a per-call relative duration; a deadline is an absolute point in time that gRPC sends with the request (in the grpc-timeout header) and propagates downstream, so the entire call tree shares one budget."],"distractorIds":["java/grpc_protobuf#2921352d2c2b","java/grpc_protobuf#df524fc20995","java/grpc_protobuf#6f3b2e7a6437"],"concepts":["usable","described","proto","messaging","difference","buffers"],"correctMd":"Protocol Buffers is the serialization format and interface definition language — it defines how messages are described in `.proto` and encoded to compact binary; it is usable on its own for storage or messaging.","answerFullMd":"Protocol Buffers is the serialization format and interface definition language — it defines how messages are described in `.proto` and encoded to compact binary; it is usable on its own for storage or messaging. gRPC is the RPC framework layered on top: it maps service methods to HTTP/2 streams and defines metadata, the `Status` error model, streaming modes, deadlines, and cancellation. Protobuf is the \"what's in the message\"; gRPC is the \"how the call travels.\"","distractorsMd":[null,"When a client cancels (explicitly, or by hitting its deadline, or by disconnecting), gRPC propagates the cancellation down the call tree as a `Context` cancellation.","A timeout is a per-call relative duration; a deadline is an absolute point in time that gRPC sends with the request (in the `grpc-timeout` header) and propagates downstream, so the entire call tree shares one budget."]},{"id":"java/grpc_protobuf#2921352d2c2b","section":"java","module":"java/grpc_protobuf","moduleName":"grpc protobuf","sourceFile":"README.md","difficulty":"core","question":"Why are protobuf field numbers so important, and what are the rules around them?","answerFull":"The field number, not the name, is the field's identity on the wire — the encoded bytes carry the number, never the name. So you can freely rename fields (wire- compatible), but you must never reuse or change a number, because old data encoded under that number would be misinterpreted. When you delete a field you should mark its number reserved so the compiler prevents anyone from reusing it later. This one rule underlies almost all of protobuf's schema-evolution guidance.","correct":"The field number, not the name, is the field's identity on the wire — the encoded bytes carry the number, never the name.","distractors":["Protocol Buffers is the serialization format and interface definition language — it defines how messages are described in .proto and encoded to compact binary; it is usable on its own for storage or messaging.","Only add new fields with brand-new numbers; never reuse, renumber, or change the type of existing fields.","A varint is a variable-length encoding where each byte uses 7 bits for the value and 1 continuation bit, so small numbers take one byte and large ones take more — making typical small integers very compact."],"distractorIds":["java/grpc_protobuf#0809225c9b8c","java/grpc_protobuf#6080948b0c0b","java/grpc_protobuf#2da2d8df07be"],"concepts":["important","protobuf","wire","carry","encoded","numbers"],"answerFullMd":"The field number, not the name, is the field's identity on the wire — the encoded bytes carry the number, never the name. So you can freely rename fields (wire- compatible), but you must never reuse or change a number, because old data encoded under that number would be misinterpreted. When you delete a field you should mark its number `reserved` so the compiler prevents anyone from reusing it later. This one rule underlies almost all of protobuf's schema-evolution guidance.","distractorsMd":["Protocol Buffers is the serialization format and interface definition language — it defines how messages are described in `.proto` and encoded to compact binary; it is usable on its own for storage or messaging.",null,null]},{"id":"java/grpc_protobuf#bb805f1fb30e","section":"java","module":"java/grpc_protobuf","moduleName":"grpc protobuf","sourceFile":"README.md","difficulty":"core","question":"How does gRPC use HTTP/2, and why does that matter?","answerFull":"Each gRPC call is an HTTP/2 stream, and many streams multiplex over a single TCP connection with independent flow-control windows and no head-of-line blocking between them. This is what makes streaming and high concurrency cheap — you do not need a connection per concurrent call as you effectively do with HTTP/1.1. It also means gRPC requires end-to-end HTTP/2 support, which is why browsers (no raw HTTP/2 trailer access) need gRPC-Web via a proxy.","correct":"Each gRPC call is an HTTP/2 stream, and many streams multiplex over a single TCP connection with independent flow-control windows and no head-of-line blocking between them.","distractors":["When a client cancels (explicitly, or by hitting its deadline, or by disconnecting), gRPC propagates the cancellation down the call tree as a Context cancellation.","The blocking stub is synchronous: unary returns the value directly and server- streaming returns a blocking Iterator, but it cannot do client or bidirectional streaming.","gRPC defaults to a 4 MB max inbound message size to bound memory per call."],"distractorIds":["java/grpc_protobuf#df524fc20995","java/grpc_protobuf#79259bd9b5cb","java/grpc_protobuf#ed36449ca1ad"],"concepts":["multiplex","matter","windows","flow","streams","tcp"],"distractorsMd":["When a client cancels (explicitly, or by hitting its deadline, or by disconnecting), gRPC propagates the cancellation down the call tree as a `Context` cancellation.","The blocking stub is synchronous: unary returns the value directly and server- streaming returns a blocking `Iterator`, but it cannot do client or bidirectional streaming.",null]},{"id":"java/grpc_protobuf#6f3b2e7a6437","section":"java","module":"java/grpc_protobuf","moduleName":"grpc protobuf","sourceFile":"README.md","difficulty":"core","question":"What is the difference between a deadline and a timeout, and why does it matter?","answerFull":"A timeout is a per-call relative duration; a deadline is an absolute point in time that gRPC sends with the request (in the grpc-timeout header) and propagates downstream, so the entire call tree shares one budget. If service A spends 50ms of a 300ms deadline, B sees only 250ms remaining. This prevents the classic waste where a client times out but downstream services keep working — the deadline cancels the whole chain. Always set deadlines; never call without one in production.","correct":"A timeout is a per-call relative duration; a deadline is an absolute point in time that gRPC sends with the request (in the grpc-timeout header) and propagates downstream, so the entire call tree shares one budget.","distractors":["As a Status: a numeric code from a fixed set of ~17 (e.g. NOT_FOUND, INVALID_ARGUMENT, UNAVAILABLE, DEADLINE_EXCEEDED, UNAUTHENTICATED), an optional message, and optional structured details.","When a client cancels (explicitly, or by hitting its deadline, or by disconnecting), gRPC propagates the cancellation down the call tree as a Context cancellation.","gRPC defaults to a 4 MB max inbound message size to bound memory per call."],"distractorIds":["java/grpc_protobuf#e79891df61ec","java/grpc_protobuf#df524fc20995","java/grpc_protobuf#ed36449ca1ad"],"concepts":["matter","difference","propagates","shares","deadline","downstream"],"correctMd":"A timeout is a per-call relative duration; a deadline is an absolute point in time that gRPC sends with the request (in the `grpc-timeout` header) and propagates downstream, so the entire call tree shares one budget.","answerFullMd":"A timeout is a per-call relative duration; a deadline is an absolute point in time that gRPC sends with the request (in the `grpc-timeout` header) and propagates downstream, so the entire call tree shares one budget. If service A spends 50ms of a 300ms deadline, B sees only 250ms remaining. This prevents the classic waste where a client times out but downstream services keep working — the deadline cancels the whole chain. Always set deadlines; never call without one in production.","distractorsMd":["As a `Status`: a numeric code from a fixed set of ~17 (e.g. `NOT_FOUND`, `INVALID_ARGUMENT`, `UNAVAILABLE`, `DEADLINE_EXCEEDED`, `UNAUTHENTICATED`), an optional message, and optional structured details.","When a client cancels (explicitly, or by hitting its deadline, or by disconnecting), gRPC propagates the cancellation down the call tree as a `Context` cancellation.",null]},{"id":"java/grpc_protobuf#df524fc20995","section":"java","module":"java/grpc_protobuf","moduleName":"grpc protobuf","sourceFile":"README.md","difficulty":"intermediate","question":"How does cancellation work in gRPC?","answerFull":"When a client cancels (explicitly, or by hitting its deadline, or by disconnecting), gRPC propagates the cancellation down the call tree as a Context cancellation. Server code should observe it — e.g. check Context.current().isCancelled() in a streaming produce loop — and stop work, releasing resources. Without honoring cancellation, a server keeps computing results nobody will read, wasting CPU, threads, and DB connections.","correct":"When a client cancels (explicitly, or by hitting its deadline, or by disconnecting), gRPC propagates the cancellation down the call tree as a Context cancellation.","distractors":["gRPC defaults to a 4 MB max inbound message size to bound memory per call.","As a Status: a numeric code from a fixed set of ~17 (e.g. NOT_FOUND, INVALID_ARGUMENT, UNAVAILABLE, DEADLINE_EXCEEDED, UNAUTHENTICATED), an optional message, and optional structured details.","Each gRPC call is an HTTP/2 stream, and many streams multiplex over a single TCP connection with independent flow-control windows and no head-of-line blocking between them."],"distractorIds":["java/grpc_protobuf#ed36449ca1ad","java/grpc_protobuf#e79891df61ec","java/grpc_protobuf#bb805f1fb30e"],"concepts":["disconnecting","hitting","cancellation","cancels","propagates","deadline"],"correctMd":"When a client cancels (explicitly, or by hitting its deadline, or by disconnecting), gRPC propagates the cancellation down the call tree as a `Context` cancellation.","answerFullMd":"When a client cancels (explicitly, or by hitting its deadline, or by disconnecting), gRPC propagates the cancellation down the call tree as a `Context` cancellation. Server code should observe it — e.g. check `Context.current().isCancelled()` in a streaming produce loop — and stop work, releasing resources. Without honoring cancellation, a server keeps computing results nobody will read, wasting CPU, threads, and DB connections.","distractorsMd":[null,"As a `Status`: a numeric code from a fixed set of ~17 (e.g. `NOT_FOUND`, `INVALID_ARGUMENT`, `UNAVAILABLE`, `DEADLINE_EXCEEDED`, `UNAUTHENTICATED`), an optional message, and optional structured details.",null]},{"id":"java/grpc_protobuf#e79891df61ec","section":"java","module":"java/grpc_protobuf","moduleName":"grpc protobuf","sourceFile":"README.md","difficulty":"intermediate","question":"How are errors represented in gRPC?","answerFull":"As a Status: a numeric code from a fixed set of ~17 (e.g. NOT_FOUND, INVALID_ARGUMENT, UNAVAILABLE, DEADLINE_EXCEEDED, UNAUTHENTICATED), an optional message, and optional structured details. Servers signal errors by calling onError with a Status (e.g. Status.NOT_FOUND.asRuntimeException()), not by letting arbitrary exceptions escape. Clients receive a StatusRuntimeException. The fixed code set gives a portable, language-neutral error vocabulary — unlike the open- ended world of HTTP status + JSON bodies.","correct":"As a Status: a numeric code from a fixed set of ~17 (e.g. NOT_FOUND, INVALID_ARGUMENT, UNAVAILABLE, DEADLINE_EXCEEDED, UNAUTHENTICATED), an optional message, and optional structured details.","distractors":["For plain scalar fields you cannot — an unset int32 and one set to 0 both decode to 0, because proto3 omits default-valued scalars from the wire.","Metadata is the key-value header/trailer mechanism that travels alongside the message payload — the place auth tokens, trace context (traceparent), and custom routing hints ride, analogous to HTTP headers.","Each gRPC call is an HTTP/2 stream, and many streams multiplex over a single TCP connection with independent flow-control windows and no head-of-line blocking between them."],"distractorIds":["java/grpc_protobuf#5a30d4f3a538","java/grpc_protobuf#ea5e29eaad21","java/grpc_protobuf#bb805f1fb30e"],"concepts":["represented","unauthenticated","unavailable","details","numeric","exceeded"],"correctMd":"As a `Status`: a numeric code from a fixed set of ~17 (e.g. `NOT_FOUND`, `INVALID_ARGUMENT`, `UNAVAILABLE`, `DEADLINE_EXCEEDED`, `UNAUTHENTICATED`), an optional message, and optional structured details.","answerFullMd":"As a `Status`: a numeric code from a fixed set of ~17 (e.g. `NOT_FOUND`, `INVALID_ARGUMENT`, `UNAVAILABLE`, `DEADLINE_EXCEEDED`, `UNAUTHENTICATED`), an optional message, and optional structured details. Servers signal errors by calling `onError` with a `Status` (e.g. `Status.NOT_FOUND.asRuntimeException()`), not by letting arbitrary exceptions escape. Clients receive a `StatusRuntimeException`. The fixed code set gives a portable, language-neutral error vocabulary — unlike the open- ended world of HTTP status + JSON bodies.","distractorsMd":["For plain scalar fields you cannot — an unset `int32` and one set to 0 both decode to 0, because proto3 omits default-valued scalars from the wire.","Metadata is the key-value header/trailer mechanism that travels alongside the message payload — the place auth tokens, trace context (`traceparent`), and custom routing hints ride, analogous to HTTP headers.",null]},{"id":"java/grpc_protobuf#79259bd9b5cb","section":"java","module":"java/grpc_protobuf","moduleName":"grpc protobuf","sourceFile":"README.md","difficulty":"intermediate","question":"What are the differences between the blocking, async, and future stubs?","answerFull":"The blocking stub is synchronous: unary returns the value directly and server- streaming returns a blocking Iterator, but it cannot do client or bidirectional streaming. The async stub is callback-based via StreamObserver and supports all four modes — it is required for client/bidi streaming. The future stub returns a Guava ListenableFuture and supports unary only. Choose blocking for simple sequential code, async whenever you stream from the client side.","correct":"The blocking stub is synchronous: unary returns the value directly and server- streaming returns a blocking Iterator, but it cannot do client or bidirectional streaming.","distractors":["Each gRPC call is an HTTP/2 stream, and many streams multiplex over a single TCP connection with independent flow-control windows and no head-of-line blocking between them.","Browsers cannot access raw HTTP/2 frames and trailers (where gRPC carries its status) through the fetch/XHR APIs, so they cannot speak native gRPC.","For plain scalar fields you cannot — an unset int32 and one set to 0 both decode to 0, because proto3 omits default-valued scalars from the wire."],"distractorIds":["java/grpc_protobuf#bb805f1fb30e","java/grpc_protobuf#2cd3ebe94612","java/grpc_protobuf#5a30d4f3a538"],"concepts":["unary","stub","differences","iterator","bidirectional","future"],"correctMd":"The blocking stub is synchronous: unary returns the value directly and server- streaming returns a blocking `Iterator`, but it cannot do client or bidirectional streaming.","answerFullMd":"The blocking stub is synchronous: unary returns the value directly and server- streaming returns a blocking `Iterator`, but it cannot do client or bidirectional streaming. The async stub is callback-based via `StreamObserver` and supports all four modes — it is required for client/bidi streaming. The future stub returns a Guava `ListenableFuture` and supports unary only. Choose blocking for simple sequential code, async whenever you stream from the client side.","distractorsMd":[null,null,"For plain scalar fields you cannot — an unset `int32` and one set to 0 both decode to 0, because proto3 omits default-valued scalars from the wire."]},{"id":"java/grpc_protobuf#5a30d4f3a538","section":"java","module":"java/grpc_protobuf","moduleName":"grpc protobuf","sourceFile":"README.md","difficulty":"intermediate","question":"In proto3, how do you tell an unset field from one set to its default value?","answerFull":"For plain scalar fields you cannot — an unset int32 and one set to 0 both decode to 0, because proto3 omits default-valued scalars from the wire. To distinguish, mark the field optional (which re-enables field presence/hasser methods in proto3) or use a wrapper type like google.protobuf.Int32Value. This is a common bug: checking != 0 or !isEmpty() to mean \"provided\" silently mishandles legitimate default-valued inputs.","correct":"For plain scalar fields you cannot — an unset int32 and one set to 0 both decode to 0, because proto3 omits default-valued scalars from the wire.","distractors":["The field number, not the name, is the field's identity on the wire — the encoded bytes carry the number, never the name.","The blocking stub is synchronous: unary returns the value directly and server- streaming returns a blocking Iterator, but it cannot do client or bidirectional streaming.","As a Status: a numeric code from a fixed set of ~17 (e.g. NOT_FOUND, INVALID_ARGUMENT, UNAVAILABLE, DEADLINE_EXCEEDED, UNAUTHENTICATED), an optional message, and optional structured details."],"distractorIds":["java/grpc_protobuf#2921352d2c2b","java/grpc_protobuf#79259bd9b5cb","java/grpc_protobuf#e79891df61ec"],"concepts":["int32","scalars","omits","proto3","unset","scalar"],"correctMd":"For plain scalar fields you cannot — an unset `int32` and one set to 0 both decode to 0, because proto3 omits default-valued scalars from the wire.","answerFullMd":"For plain scalar fields you cannot — an unset `int32` and one set to 0 both decode to 0, because proto3 omits default-valued scalars from the wire. To distinguish, mark the field `optional` (which re-enables field presence/hasser methods in proto3) or use a wrapper type like `google.protobuf.Int32Value`. This is a common bug: checking `!= 0` or `!isEmpty()` to mean \"provided\" silently mishandles legitimate default-valued inputs.","distractorsMd":[null,"The blocking stub is synchronous: unary returns the value directly and server- streaming returns a blocking `Iterator`, but it cannot do client or bidirectional streaming.","As a `Status`: a numeric code from a fixed set of ~17 (e.g. `NOT_FOUND`, `INVALID_ARGUMENT`, `UNAVAILABLE`, `DEADLINE_EXCEEDED`, `UNAUTHENTICATED`), an optional message, and optional structured details."]},{"id":"java/grpc_protobuf#4b411c472aac","section":"java","module":"java/grpc_protobuf","moduleName":"grpc protobuf","sourceFile":"README.md","difficulty":"advanced","question":"What are gRPC interceptors and what do you use them for?","answerFull":"Interceptors wrap calls to apply cross-cutting concerns without touching service logic — authentication, logging, metrics, tracing, retry. A ServerInterceptor inspects incoming Metadata (gRPC's headers) and can short-circuit (e.g. close with UNAUTHENTICATED) or proceed; a ClientInterceptor can attach metadata like trace ids or auth tokens. They are the gRPC analogue of servlet filters / middleware.","correct":"Interceptors wrap calls to apply cross-cutting concerns without touching service logic — authentication, logging, metrics, tracing, retry.","distractors":["Each gRPC call is an HTTP/2 stream, and many streams multiplex over a single TCP connection with independent flow-control windows and no head-of-line blocking between them.","gRPC defaults to a 4 MB max inbound message size to bound memory per call.","When a client cancels (explicitly, or by hitting its deadline, or by disconnecting), gRPC propagates the cancellation down the call tree as a Context cancellation."],"distractorIds":["java/grpc_protobuf#bb805f1fb30e","java/grpc_protobuf#ed36449ca1ad","java/grpc_protobuf#df524fc20995"],"concepts":["touching","interceptors","concerns","cutting","logging","wrap"],"answerFullMd":"Interceptors wrap calls to apply cross-cutting concerns without touching service logic — authentication, logging, metrics, tracing, retry. A `ServerInterceptor` inspects incoming `Metadata` (gRPC's headers) and can short-circuit (e.g. close with `UNAUTHENTICATED`) or proceed; a `ClientInterceptor` can attach metadata like trace ids or auth tokens. They are the gRPC analogue of servlet filters / middleware.","distractorsMd":[null,null,"When a client cancels (explicitly, or by hitting its deadline, or by disconnecting), gRPC propagates the cancellation down the call tree as a `Context` cancellation."]},{"id":"java/grpc_protobuf#ea5e29eaad21","section":"java","module":"java/grpc_protobuf","moduleName":"grpc protobuf","sourceFile":"README.md","difficulty":"advanced","question":"What is Metadata in gRPC?","answerFull":"Metadata is the key-value header/trailer mechanism that travels alongside the message payload — the place auth tokens, trace context (traceparent), and custom routing hints ride, analogous to HTTP headers. Keys ending in -bin carry binary values; others are ASCII. Interceptors are the usual place to read/write metadata.","correct":"Metadata is the key-value header/trailer mechanism that travels alongside the message payload — the place auth tokens, trace context (traceparent), and custom routing hints ride, analogous to HTTP headers.","distractors":["Browsers cannot access raw HTTP/2 frames and trailers (where gRPC carries its status) through the fetch/XHR APIs, so they cannot speak native gRPC.","gRPC defaults to a 4 MB max inbound message size to bound memory per call.","When a client cancels (explicitly, or by hitting its deadline, or by disconnecting), gRPC propagates the cancellation down the call tree as a Context cancellation."],"distractorIds":["java/grpc_protobuf#2cd3ebe94612","java/grpc_protobuf#ed36449ca1ad","java/grpc_protobuf#df524fc20995"],"concepts":["ride","trailer","travels","traceparent","analogous","hints"],"correctMd":"Metadata is the key-value header/trailer mechanism that travels alongside the message payload — the place auth tokens, trace context (`traceparent`), and custom routing hints ride, analogous to HTTP headers.","answerFullMd":"Metadata is the key-value header/trailer mechanism that travels alongside the message payload — the place auth tokens, trace context (`traceparent`), and custom routing hints ride, analogous to HTTP headers. Keys ending in `-bin` carry binary values; others are ASCII. Interceptors are the usual place to read/write metadata.","distractorsMd":[null,null,"When a client cancels (explicitly, or by hitting its deadline, or by disconnecting), gRPC propagates the cancellation down the call tree as a `Context` cancellation."]},{"id":"java/grpc_protobuf#6080948b0c0b","section":"java","module":"java/grpc_protobuf","moduleName":"grpc protobuf","sourceFile":"README.md","difficulty":"advanced","question":"How would you safely evolve a protobuf schema?","answerFull":"Only add new fields with brand-new numbers; never reuse, renumber, or change the type of existing fields. When removing a field, reserved its number (and optionally its name) so it can never be reused. Avoid changing wire types (int32→string breaks); varint-to-varint changes (int32↔int64↔bool) are technically wire-compatible but risk value truncation. Tools like buf breaking mechanically enforce these rules in CI.","correct":"Only add new fields with brand-new numbers; never reuse, renumber, or change the type of existing fields.","distractors":["A varint is a variable-length encoding where each byte uses 7 bits for the value and 1 continuation bit, so small numbers take one byte and large ones take more — making typical small integers very compact.","The field number, not the name, is the field's identity on the wire — the encoded bytes carry the number, never the name.","For plain scalar fields you cannot — an unset int32 and one set to 0 both decode to 0, because proto3 omits default-valued scalars from the wire."],"distractorIds":["java/grpc_protobuf#2da2d8df07be","java/grpc_protobuf#2921352d2c2b","java/grpc_protobuf#5a30d4f3a538"],"concepts":["brand","renumber","evolve","protobuf","safely","reuse"],"answerFullMd":"Only add new fields with brand-new numbers; never reuse, renumber, or change the type of existing fields. When removing a field, `reserved` its number (and optionally its name) so it can never be reused. Avoid changing wire types (int32→string breaks); varint-to-varint changes (int32↔int64↔bool) are technically wire-compatible but risk value truncation. Tools like `buf breaking` mechanically enforce these rules in CI.","distractorsMd":[null,null,"For plain scalar fields you cannot — an unset `int32` and one set to 0 both decode to 0, because proto3 omits default-valued scalars from the wire."]},{"id":"java/grpc_protobuf#2cd3ebe94612","section":"java","module":"java/grpc_protobuf","moduleName":"grpc protobuf","sourceFile":"README.md","difficulty":"advanced","question":"Why can't a browser call a gRPC service directly, and what's the fix?","answerFull":"Browsers cannot access raw HTTP/2 frames and trailers (where gRPC carries its status) through the fetch/XHR APIs, so they cannot speak native gRPC. The fix is gRPC-Web: a browser-compatible variant that a proxy (Envoy, or the Connect/buf stack) translates to/from backend gRPC. Alternatively, expose a REST/JSON gateway (e.g. grpc-gateway) in front of the gRPC services for public consumption.","correct":"Browsers cannot access raw HTTP/2 frames and trailers (where gRPC carries its status) through the fetch/XHR APIs, so they cannot speak native gRPC.","distractors":["Interceptors wrap calls to apply cross-cutting concerns without touching service logic — authentication, logging, metrics, tracing, retry.","A timeout is a per-call relative duration; a deadline is an absolute point in time that gRPC sends with the request (in the grpc-timeout header) and propagates downstream, so the entire call tree shares one budget.","When a client cancels (explicitly, or by hitting its deadline, or by disconnecting), gRPC propagates the cancellation down the call tree as a Context cancellation."],"distractorIds":["java/grpc_protobuf#4b411c472aac","java/grpc_protobuf#6f3b2e7a6437","java/grpc_protobuf#df524fc20995"],"concepts":["trailers","speak","xhr","browsers","fetch","fix"],"distractorsMd":[null,"A timeout is a per-call relative duration; a deadline is an absolute point in time that gRPC sends with the request (in the `grpc-timeout` header) and propagates downstream, so the entire call tree shares one budget.","When a client cancels (explicitly, or by hitting its deadline, or by disconnecting), gRPC propagates the cancellation down the call tree as a `Context` cancellation."]},{"id":"java/grpc_protobuf#ed36449ca1ad","section":"java","module":"java/grpc_protobuf","moduleName":"grpc protobuf","sourceFile":"README.md","difficulty":"advanced","question":"What is the default max message size and why does it matter?","answerFull":"gRPC defaults to a 4 MB max inbound message size to bound memory per call. Trying to return a very large result in a single unary message exceeds it and fails (or OOMs if raised carelessly). The right answer for large/unbounded results is server streaming — page the data as many small messages — rather than inflating the limit. The limit is a deliberate backpressure/safety guard, not an obstacle to remove.","correct":"gRPC defaults to a 4 MB max inbound message size to bound memory per call.","distractors":["For plain scalar fields you cannot — an unset int32 and one set to 0 both decode to 0, because proto3 omits default-valued scalars from the wire.","Browsers cannot access raw HTTP/2 frames and trailers (where gRPC carries its status) through the fetch/XHR APIs, so they cannot speak native gRPC.","When a client cancels (explicitly, or by hitting its deadline, or by disconnecting), gRPC propagates the cancellation down the call tree as a Context cancellation."],"distractorIds":["java/grpc_protobuf#5a30d4f3a538","java/grpc_protobuf#2cd3ebe94612","java/grpc_protobuf#df524fc20995"],"concepts":["matter","inbound","defaults","grpc","bound","message"],"distractorsMd":["For plain scalar fields you cannot — an unset `int32` and one set to 0 both decode to 0, because proto3 omits default-valued scalars from the wire.",null,"When a client cancels (explicitly, or by hitting its deadline, or by disconnecting), gRPC propagates the cancellation down the call tree as a `Context` cancellation."]},{"id":"java/grpc_protobuf#2da2d8df07be","section":"java","module":"java/grpc_protobuf","moduleName":"grpc protobuf","sourceFile":"README.md","difficulty":"advanced","question":"How does protobuf encode an integer, and what is a varint?","answerFull":"A varint is a variable-length encoding where each byte uses 7 bits for the value and 1 continuation bit, so small numbers take one byte and large ones take more — making typical small integers very compact. Each field is encoded as a tag (the field number shifted left 3 bits, OR'd with a 3-bit wire type) followed by the value. Signed numbers use zigzag encoding (sint32/sint64) so small-magnitude negatives stay small, because plain two's-complement negatives would otherwise always take the maximum bytes.","correct":"A varint is a variable-length encoding where each byte uses 7 bits for the value and 1 continuation bit, so small numbers take one byte and large ones take more — making typical small integers very compact.","distractors":["Protocol Buffers is the serialization format and interface definition language — it defines how messages are described in .proto and encoded to compact binary; it is usable on its own for storage or messaging.","Only add new fields with brand-new numbers; never reuse, renumber, or change the type of existing fields.","A timeout is a per-call relative duration; a deadline is an absolute point in time that gRPC sends with the request (in the grpc-timeout header) and propagates downstream, so the entire call tree shares one budget."],"distractorIds":["java/grpc_protobuf#0809225c9b8c","java/grpc_protobuf#6080948b0c0b","java/grpc_protobuf#6f3b2e7a6437"],"concepts":["varint","continuation","protobuf","encode","integers","typical"],"answerFullMd":"A varint is a variable-length encoding where each byte uses 7 bits for the value and 1 continuation bit, so small numbers take one byte and large ones take more — making typical small integers very compact. Each field is encoded as a tag (the field number shifted left 3 bits, OR'd with a 3-bit wire type) followed by the value. Signed numbers use zigzag encoding (`sint32`/`sint64`) so small-magnitude negatives stay small, because plain two's-complement negatives would otherwise always take the maximum bytes.","distractorsMd":["Protocol Buffers is the serialization format and interface definition language — it defines how messages are described in `.proto` and encoded to compact binary; it is usable on its own for storage or messaging.",null,"A timeout is a per-call relative duration; a deadline is an absolute point in time that gRPC sends with the request (in the `grpc-timeout` header) and propagates downstream, so the entire call tree shares one budget."]},{"id":"java/java8_features#b4fc28855a13","section":"java","module":"java/java8_features","moduleName":"java8 features","sourceFile":"README.md","difficulty":"core","question":"What is a functional interface? How does @FunctionalInterface help?","answerFull":"A functional interface has exactly one abstract method (SAM — Single Abstract Method). It is the target type for a lambda or method reference. @FunctionalInterface is a compile-time annotation that causes a compiler error if the interface has more than one abstract method, preventing accidental interface evolution that would break all existing lambdas. Examples: Runnable, Callable, Comparator, Predicate, Function.","correct":"A functional interface has exactly one abstract method (SAM — Single Abstract Method).","distractors":["map() applies a one-to-one function: Stream<T> → Stream<R> where each element maps to exactly one result.","Method references compile to the same invokedynamic mechanism as lambdas.","(1) Hot paths with very small collections — stream pipeline overhead (lambda dispatch, boxing for primitives) can dominate."],"distractorIds":["java/java8_features#e2f498121ba7","java/java8_features#e035711d197d","java/java8_features#9cd952103152"],"concepts":["sam","functional","help","abstract","interface","exactly"],"questionMd":"What is a functional interface? How does `@FunctionalInterface` help?","answerFullMd":"A functional interface has exactly one abstract method (SAM — Single Abstract Method). It is the target type for a lambda or method reference. `@FunctionalInterface` is a compile-time annotation that causes a compiler error if the interface has more than one abstract method, preventing accidental interface evolution that would break all existing lambdas. Examples: `Runnable`, `Callable`, `Comparator`, `Predicate`, `Function`.","distractorsMd":["`map()` applies a one-to-one function: `Stream<T> → Stream<R>` where each element maps to exactly one result.","Method references compile to the same `invokedynamic` mechanism as lambdas.",null]},{"id":"java/java8_features#d43a2868686d","section":"java","module":"java/java8_features","moduleName":"java8 features","sourceFile":"README.md","difficulty":"core","question":"How do lambdas differ from anonymous inner classes?","answerFull":"Four key differences: (1) this — inside a lambda, this refers to the enclosing instance; inside an anonymous class, this refers to the anonymous class instance. (2) No separate .class file — lambdas use invokedynamic + LambdaMetafactory; anonymous classes compile to separate $N.class files. (3) Non-capturing lambdas can be instance-reused by the JVM, reducing allocation. (4) Lambdas cannot have state (no fields); anonymous classes can.","correct":"Four key differences: (1) this — inside a lambda, this refers to the enclosing instance; inside an anonymous class, this refers to the anonymous class instance.","distractors":["groupingBy(classifier) is equivalent to: iterate elements, apply classifier to get a key, insert element into the value list for that key in a HashMap.","Method references compile to the same invokedynamic mechanism as lambdas.","A variable captured by a lambda must be effectively final — either explicitly final or never reassigned after initialization."],"distractorIds":["java/java8_features#3e95c51de9d4","java/java8_features#e035711d197d","java/java8_features#248a563969c0"],"concepts":["lambdas","enclosing","anonymous","differences","refers","differ"],"correctMd":"Four key differences: (1) `this` — inside a lambda, `this` refers to the enclosing instance; inside an anonymous class, `this` refers to the anonymous class instance.","answerFullMd":"Four key differences: (1) `this` — inside a lambda, `this` refers to the enclosing instance; inside an anonymous class, `this` refers to the anonymous class instance. (2) No separate `.class` file — lambdas use `invokedynamic` + `LambdaMetafactory`; anonymous classes compile to separate `$N.class` files. (3) Non-capturing lambdas can be instance-reused by the JVM, reducing allocation. (4) Lambdas cannot have state (no fields); anonymous classes can.","distractorsMd":["`groupingBy(classifier)` is equivalent to: iterate elements, apply classifier to get a key, insert element into the value list for that key in a `HashMap`.","Method references compile to the same `invokedynamic` mechanism as lambdas.","A variable captured by a lambda must be effectively final — either explicitly `final` or never reassigned after initialization."]},{"id":"java/java8_features#e2f498121ba7","section":"java","module":"java/java8_features","moduleName":"java8 features","sourceFile":"README.md","difficulty":"core","question":"What is the difference between map() and flatMap() in Streams?","answerFull":"map() applies a one-to-one function: Stream<T> → Stream<R> where each element maps to exactly one result. flatMap() applies a one-to-many function: Stream<T> → Stream<R> where each element maps to a Stream<R>, and all streams are concatenated (flattened) into one. Think of flatMap as \"map then flatten.\" It's the monadic bind operation — it removes one level of nesting: Optional<Optional<T>> → Optional<T> via flatMap.","correct":"map() applies a one-to-one function: Stream<T> → Stream<R> where each element maps to exactly one result.","distractors":["(1) Hot paths with very small collections — stream pipeline overhead (lambda dispatch, boxing for primitives) can dominate.","A functional interface has exactly one abstract method (SAM — Single Abstract Method).","Intermediate operations are lazy, return a new Stream, and don't trigger computation: filter, map, flatMap, distinct, sorted, peek, limit, skip."],"distractorIds":["java/java8_features#9cd952103152","java/java8_features#b4fc28855a13","java/java8_features#194d85a2b77f"],"concepts":["flatmap","difference","streams","maps","applies","element"],"questionMd":"What is the difference between `map()` and `flatMap()` in Streams?","correctMd":"`map()` applies a one-to-one function: `Stream<T> → Stream<R>` where each element maps to exactly one result.","answerFullMd":"`map()` applies a one-to-one function: `Stream<T> → Stream<R>` where each element maps to exactly one result. `flatMap()` applies a one-to-many function: `Stream<T> → Stream<R>` where each element maps to a `Stream<R>`, and all streams are concatenated (flattened) into one. Think of `flatMap` as \"map then flatten.\" It's the monadic bind operation — it removes one level of nesting: `Optional<Optional<T>>` → `Optional<T>` via `flatMap`.","distractorsMd":[null,null,"Intermediate operations are lazy, return a new `Stream`, and don't trigger computation: `filter`, `map`, `flatMap`, `distinct`, `sorted`, `peek`, `limit`, `skip`."]},{"id":"java/java8_features#006516f771d8","section":"java","module":"java/java8_features","moduleName":"java8 features","sourceFile":"README.md","difficulty":"core","question":"What is lazy evaluation in streams, and why does it matter?","answerFull":"Intermediate operations (filter, map, peek) are lazy — they don't execute until a terminal operation is called. The pipeline is a description of transformations, not the execution. This matters because: (1) Short-circuit operations (findFirst, anyMatch, limit) can stop processing early — e.g., findFirst() processes only enough elements to find one, not all. (2) Stateful operations (sorted, distinct) must materialize the stream but only when forced. (3) No unnecessary computation for elements filtered out early.","correct":"Intermediate operations (filter, map, peek) are lazy — they don't execute until a terminal operation is called.","distractors":["Intermediate operations are lazy, return a new Stream, and don't trigger computation: filter, map, flatMap, distinct, sorted, peek, limit, skip.","map() applies a one-to-one function: Stream<T> → Stream<R> where each element maps to exactly one result.","(1) Hot paths with very small collections — stream pipeline overhead (lambda dispatch, boxing for primitives) can dominate."],"distractorIds":["java/java8_features#194d85a2b77f","java/java8_features#e2f498121ba7","java/java8_features#9cd952103152"],"concepts":["peek","terminal","matter","intermediate","lazy","streams"],"answerFullMd":"Intermediate operations (filter, map, peek) are lazy — they don't execute until a terminal operation is called. The pipeline is a description of transformations, not the execution. This matters because: (1) Short-circuit operations (`findFirst`, `anyMatch`, `limit`) can stop processing early — e.g., `findFirst()` processes only enough elements to find one, not all. (2) Stateful operations (`sorted`, `distinct`) must materialize the stream but only when forced. (3) No unnecessary computation for elements filtered out early.","distractorsMd":["Intermediate operations are lazy, return a new `Stream`, and don't trigger computation: `filter`, `map`, `flatMap`, `distinct`, `sorted`, `peek`, `limit`, `skip`.","`map()` applies a one-to-one function: `Stream<T> → Stream<R>` where each element maps to exactly one result.",null]},{"id":"java/java8_features#c0aa7846e0a1","section":"java","module":"java/java8_features","moduleName":"java8 features","sourceFile":"README.md","difficulty":"core","question":"Why should Optional NOT be used as a method parameter?","answerFull":"Using Optional as a parameter forces the caller to wrap their value in Optional.of() or Optional.empty() — awkward and verbose. It also obscures intent (does null mean \"not provided\" or is it a bug?). The correct patterns are: use method overloading (one method with the parameter, one without), or use a null check with Objects.requireNonNull(). Optional is designed purely as a return type to signal \"this method may return nothing.\"","correct":"Using Optional as a parameter forces the caller to wrap their value in Optional.of() or Optional.empty() — awkward and verbose.","distractors":["A functional interface has exactly one abstract method (SAM — Single Abstract Method).","Optional.of(value) throws NullPointerException if value is null — use when you know the value is non-null and want to fail-fast.","orElse(T value) always evaluates the argument, even if the Optional is present — if computing the default is expensive (DB call, object creation), you pay the cost every time."],"distractorIds":["java/java8_features#b4fc28855a13","java/java8_features#7ededed0b0d3","java/java8_features#0fd942ef7709"],"concepts":["awkward","verbose","caller","wrap","forces","empty"],"correctMd":"Using `Optional` as a parameter forces the caller to wrap their value in `Optional.of()` or `Optional.empty()` — awkward and verbose.","answerFullMd":"Using `Optional` as a parameter forces the caller to wrap their value in `Optional.of()` or `Optional.empty()` — awkward and verbose. It also obscures intent (does `null` mean \"not provided\" or is it a bug?). The correct patterns are: use method overloading (one method with the parameter, one without), or use a null check with `Objects.requireNonNull()`. `Optional` is designed purely as a return type to signal \"this method may return nothing.\"","distractorsMd":[null,"`Optional.of(value)` throws `NullPointerException` if `value` is null — use when you know the value is non-null and want to fail-fast.","`orElse(T value)` always evaluates the argument, even if the Optional is present — if computing the default is expensive (DB call, object creation), you pay the cost every time."]},{"id":"java/java8_features#194d85a2b77f","section":"java","module":"java/java8_features","moduleName":"java8 features","sourceFile":"README.md","difficulty":"intermediate","question":"What are intermediate vs terminal operations? Give examples of each.","answerFull":"Intermediate operations are lazy, return a new Stream, and don't trigger computation: filter, map, flatMap, distinct, sorted, peek, limit, skip. Terminal operations are eager, trigger the pipeline, and produce a non-stream result or side effect: collect, forEach, reduce, count, min, max, findFirst, findAny, anyMatch, allMatch, noneMatch, toArray. A pipeline must end with exactly one terminal operation.","correct":"Intermediate operations are lazy, return a new Stream, and don't trigger computation: filter, map, flatMap, distinct, sorted, peek, limit, skip.","distractors":["Intermediate operations (filter, map, peek) are lazy — they don't execute until a terminal operation is called.","(1) Hot paths with very small collections — stream pipeline overhead (lambda dispatch, boxing for primitives) can dominate.","map() applies a one-to-one function: Stream<T> → Stream<R> where each element maps to exactly one result."],"distractorIds":["java/java8_features#006516f771d8","java/java8_features#9cd952103152","java/java8_features#e2f498121ba7"],"concepts":["peek","terminal","flatmap","trigger","give","distinct"],"correctMd":"Intermediate operations are lazy, return a new `Stream`, and don't trigger computation: `filter`, `map`, `flatMap`, `distinct`, `sorted`, `peek`, `limit`, `skip`.","answerFullMd":"Intermediate operations are lazy, return a new `Stream`, and don't trigger computation: `filter`, `map`, `flatMap`, `distinct`, `sorted`, `peek`, `limit`, `skip`. Terminal operations are eager, trigger the pipeline, and produce a non-stream result or side effect: `collect`, `forEach`, `reduce`, `count`, `min`, `max`, `findFirst`, `findAny`, `anyMatch`, `allMatch`, `noneMatch`, `toArray`. A pipeline must end with exactly one terminal operation.","distractorsMd":[null,null,"`map()` applies a one-to-one function: `Stream<T> → Stream<R>` where each element maps to exactly one result."]},{"id":"java/java8_features#3e95c51de9d4","section":"java","module":"java/java8_features","moduleName":"java8 features","sourceFile":"README.md","difficulty":"intermediate","question":"How does groupingBy() work internally?","answerFull":"groupingBy(classifier) is equivalent to: iterate elements, apply classifier to get a key, insert element into the value list for that key in a HashMap. Internally it uses HashMap<K, List<T>> with a downstream collector (default: toList()). The combiner function is used for parallel streams to merge partial maps. For counting() as downstream: instead of List, it tracks Long counts, making it Map<K, Long>.","correct":"groupingBy(classifier) is equivalent to: iterate elements, apply classifier to get a key, insert element into the value list for that key in a HashMap.","distractors":["map() applies a one-to-one function: Stream<T> → Stream<R> where each element maps to exactly one result.","Four key differences: (1) this — inside a lambda, this refers to the enclosing instance; inside an anonymous class, this refers to the anonymous class instance.","(1) Hot paths with very small collections — stream pipeline overhead (lambda dispatch, boxing for primitives) can dominate."],"distractorIds":["java/java8_features#e2f498121ba7","java/java8_features#d43a2868686d","java/java8_features#9cd952103152"],"concepts":["groupingby","iterate","internally","classifier","equivalent","hashmap"],"questionMd":"How does `groupingBy()` work internally?","correctMd":"`groupingBy(classifier)` is equivalent to: iterate elements, apply classifier to get a key, insert element into the value list for that key in a `HashMap`.","answerFullMd":"`groupingBy(classifier)` is equivalent to: iterate elements, apply classifier to get a key, insert element into the value list for that key in a `HashMap`. Internally it uses `HashMap<K, List<T>>` with a downstream collector (default: `toList()`). The combiner function is used for parallel streams to merge partial maps. For `counting()` as downstream: instead of `List`, it tracks `Long` counts, making it `Map<K, Long>`.","distractorsMd":["`map()` applies a one-to-one function: `Stream<T> → Stream<R>` where each element maps to exactly one result.","Four key differences: (1) `this` — inside a lambda, `this` refers to the enclosing instance; inside an anonymous class, `this` refers to the anonymous class instance.",null]},{"id":"java/java8_features#9cd952103152","section":"java","module":"java/java8_features","moduleName":"java8 features","sourceFile":"README.md","difficulty":"intermediate","question":"When should you avoid streams and prefer a regular for-loop?","answerFull":"(1) Hot paths with very small collections — stream pipeline overhead (lambda dispatch, boxing for primitives) can dominate. (2) Complex control flow requiring break, continue, or checked exception propagation. (3) Multiple side effects or mutations needed per element. (4) Code that's clearer as imperative (sometimes a for-loop is simply more readable). Rule of thumb: for collections under ~50 elements in hot paths, measure with JMH — streams are often comparable but occasionally slower.","correct":"(1) Hot paths with very small collections — stream pipeline overhead (lambda dispatch, boxing for primitives) can dominate.","distractors":["Stream<Integer> requires boxing every primitive int into a heap-allocated Integer object.","Intermediate operations are lazy, return a new Stream, and don't trigger computation: filter, map, flatMap, distinct, sorted, peek, limit, skip.","Four key differences: (1) this — inside a lambda, this refers to the enclosing instance; inside an anonymous class, this refers to the anonymous class instance."],"distractorIds":["java/java8_features#2de54b2550f7","java/java8_features#194d85a2b77f","java/java8_features#d43a2868686d"],"concepts":["boxing","primitives","prefer","regular","collections","dispatch"],"answerFullMd":"(1) Hot paths with very small collections — stream pipeline overhead (lambda dispatch, boxing for primitives) can dominate. (2) Complex control flow requiring `break`, `continue`, or checked exception propagation. (3) Multiple side effects or mutations needed per element. (4) Code that's clearer as imperative (sometimes a for-loop is simply more readable). Rule of thumb: for collections under ~50 elements in hot paths, measure with JMH — streams are often comparable but occasionally slower.","distractorsMd":["`Stream<Integer>` requires boxing every primitive `int` into a heap-allocated `Integer` object.","Intermediate operations are lazy, return a new `Stream`, and don't trigger computation: `filter`, `map`, `flatMap`, `distinct`, `sorted`, `peek`, `limit`, `skip`.","Four key differences: (1) `this` — inside a lambda, `this` refers to the enclosing instance; inside an anonymous class, `this` refers to the anonymous class instance."]},{"id":"java/java8_features#0fd942ef7709","section":"java","module":"java/java8_features","moduleName":"java8 features","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between orElse() and orElseGet()?","answerFull":"orElse(T value) always evaluates the argument, even if the Optional is present — if computing the default is expensive (DB call, object creation), you pay the cost every time. orElseGet(Supplier<T> supplier) only invokes the supplier when the Optional is empty — lazy evaluation. Rule: use orElseGet() whenever the default is computationally expensive or has side effects.","correct":"orElse(T value) always evaluates the argument, even if the Optional is present — if computing the default is expensive (DB call, object creation), you pay the cost every time.","distractors":["Using Optional as a parameter forces the caller to wrap their value in Optional.of() or Optional.empty() — awkward and verbose.","Stream<Integer> requires boxing every primitive int into a heap-allocated Integer object.","Optional.of(value) throws NullPointerException if value is null — use when you know the value is non-null and want to fail-fast."],"distractorIds":["java/java8_features#c0aa7846e0a1","java/java8_features#2de54b2550f7","java/java8_features#7ededed0b0d3"],"concepts":["orelse","difference","creation","pay","evaluates","computing"],"questionMd":"What is the difference between `orElse()` and `orElseGet()`?","correctMd":"`orElse(T value)` always evaluates the argument, even if the Optional is present — if computing the default is expensive (DB call, object creation), you pay the cost every time.","answerFullMd":"`orElse(T value)` always evaluates the argument, even if the Optional is present — if computing the default is expensive (DB call, object creation), you pay the cost every time. `orElseGet(Supplier<T> supplier)` only invokes the supplier when the Optional is empty — lazy evaluation. Rule: use `orElseGet()` whenever the default is computationally expensive or has side effects.","distractorsMd":["Using `Optional` as a parameter forces the caller to wrap their value in `Optional.of()` or `Optional.empty()` — awkward and verbose.","`Stream<Integer>` requires boxing every primitive `int` into a heap-allocated `Integer` object.","`Optional.of(value)` throws `NullPointerException` if `value` is null — use when you know the value is non-null and want to fail-fast."]},{"id":"java/java8_features#e035711d197d","section":"java","module":"java/java8_features","moduleName":"java8 features","sourceFile":"README.md","difficulty":"intermediate","question":"How do method references compile, and what are their four types?","answerFull":"Method references compile to the same invokedynamic mechanism as lambdas. The four types: (1) ClassName::staticMethod → args -> ClassName.staticMethod(args). (2) instance::instanceMethod → args -> instance.method(args) (captures instance). (3) ClassName::instanceMethod → (instance, args) -> instance.method(args) (receiver is first argument). (4) ClassName::new → args -> new ClassName(args) (constructor reference). The compiler infers which functional interface type the method reference matches.","correct":"Method references compile to the same invokedynamic mechanism as lambdas.","distractors":["Four key differences: (1) this — inside a lambda, this refers to the enclosing instance; inside an anonymous class, this refers to the anonymous class instance.","A functional interface has exactly one abstract method (SAM — Single Abstract Method).","(1) Hot paths with very small collections — stream pipeline overhead (lambda dispatch, boxing for primitives) can dominate."],"distractorIds":["java/java8_features#d43a2868686d","java/java8_features#b4fc28855a13","java/java8_features#9cd952103152"],"concepts":["lambdas","invokedynamic","references","compile","four","mechanism"],"correctMd":"Method references compile to the same `invokedynamic` mechanism as lambdas.","answerFullMd":"Method references compile to the same `invokedynamic` mechanism as lambdas. The four types: (1) `ClassName::staticMethod` → `args -> ClassName.staticMethod(args)`. (2) `instance::instanceMethod` → `args -> instance.method(args)` (captures `instance`). (3) `ClassName::instanceMethod` → `(instance, args) -> instance.method(args)` (receiver is first argument). (4) `ClassName::new` → `args -> new ClassName(args)` (constructor reference). The compiler infers which functional interface type the method reference matches.","distractorsMd":["Four key differences: (1) `this` — inside a lambda, `this` refers to the enclosing instance; inside an anonymous class, `this` refers to the anonymous class instance.",null,null]},{"id":"java/java8_features#97bfb2f6c7c5","section":"java","module":"java/java8_features","moduleName":"java8 features","sourceFile":"README.md","difficulty":"advanced","question":"Explain the Date/Time API and why it replaced Calendar.","answerFull":"java.util.Date and Calendar had three critical flaws: mutable (not thread-safe), poor API design (month is 0-indexed, confusing methods), and no separation of concepts. java.time.* (JSR-310): LocalDate (no time, no timezone), LocalTime (no date, no timezone), LocalDateTime, ZonedDateTime (with timezone), Instant (epoch milliseconds), Duration (time-based), Period (date-based). All immutable. Parseable with DateTimeFormatter. Operations like plusDays(), minusMonths() return new instances. Use Instant for timestamps, LocalDate for business dates, ZonedDateTime for user-facing times with timezone.","correct":"java.util.Date and Calendar had three critical flaws: mutable (not thread-safe), poor API design (month is 0-indexed, confusing methods), and no separation of concepts.","distractors":["orElse(T value) always evaluates the argument, even if the Optional is present — if computing the default is expensive (DB call, object creation), you pay the cost every time.","(1) Hot paths with very small collections — stream pipeline overhead (lambda dispatch, boxing for primitives) can dominate.","A functional interface has exactly one abstract method (SAM — Single Abstract Method)."],"distractorIds":["java/java8_features#0fd942ef7709","java/java8_features#9cd952103152","java/java8_features#b4fc28855a13"],"concepts":["confusing","flaws","concepts","had","separation","month"],"correctMd":"`java.util.Date` and `Calendar` had three critical flaws: mutable (not thread-safe), poor API design (month is 0-indexed, confusing methods), and no separation of concepts.","answerFullMd":"`java.util.Date` and `Calendar` had three critical flaws: mutable (not thread-safe), poor API design (month is 0-indexed, confusing methods), and no separation of concepts. `java.time.*` (JSR-310): `LocalDate` (no time, no timezone), `LocalTime` (no date, no timezone), `LocalDateTime`, `ZonedDateTime` (with timezone), `Instant` (epoch milliseconds), `Duration` (time-based), `Period` (date-based). All immutable. Parseable with `DateTimeFormatter`. Operations like `plusDays()`, `minusMonths()` return new instances. Use `Instant` for timestamps, `LocalDate` for business dates, `ZonedDateTime` for user-facing times with timezone.","distractorsMd":["`orElse(T value)` always evaluates the argument, even if the Optional is present — if computing the default is expensive (DB call, object creation), you pay the cost every time.",null,null]},{"id":"java/java8_features#248a563969c0","section":"java","module":"java/java8_features","moduleName":"java8 features","sourceFile":"README.md","difficulty":"advanced","question":"What is the effectively-final requirement for lambda captures, and why does Java require it?","answerFull":"A variable captured by a lambda must be effectively final — either explicitly final or never reassigned after initialization. Java requires this because lambdas can outlive the method that created them (e.g., submitted to a thread pool). If a local variable could be mutated after capture, the lambda might see a stale value (the lambda gets a copy of the primitive/reference, not a live view). Making captured variables final ensures the semantics are clear: the lambda sees the value at capture time, forever. For mutable state, use AtomicInteger, AtomicReference, or an array of size 1.","correct":"A variable captured by a lambda must be effectively final — either explicitly final or never reassigned after initialization.","distractors":["(1) Hot paths with very small collections — stream pipeline overhead (lambda dispatch, boxing for primitives) can dominate.","Four key differences: (1) this — inside a lambda, this refers to the enclosing instance; inside an anonymous class, this refers to the anonymous class instance.","java.util.Date and Calendar had three critical flaws: mutable (not thread-safe), poor API design (month is 0-indexed, confusing methods), and no separation of concepts."],"distractorIds":["java/java8_features#9cd952103152","java/java8_features#d43a2868686d","java/java8_features#97bfb2f6c7c5"],"concepts":["captured","reassigned","requirement","initialization","effectively","captures"],"correctMd":"A variable captured by a lambda must be effectively final — either explicitly `final` or never reassigned after initialization.","answerFullMd":"A variable captured by a lambda must be effectively final — either explicitly `final` or never reassigned after initialization. Java requires this because lambdas can outlive the method that created them (e.g., submitted to a thread pool). If a local variable could be mutated after capture, the lambda might see a stale value (the lambda gets a copy of the primitive/reference, not a live view). Making captured variables final ensures the semantics are clear: the lambda sees the value at capture time, forever. For mutable state, use `AtomicInteger`, `AtomicReference`, or an array of size 1.","distractorsMd":[null,"Four key differences: (1) `this` — inside a lambda, `this` refers to the enclosing instance; inside an anonymous class, `this` refers to the anonymous class instance.","`java.util.Date` and `Calendar` had three critical flaws: mutable (not thread-safe), poor API design (month is 0-indexed, confusing methods), and no separation of concepts."]},{"id":"java/java8_features#ee97f016ec7c","section":"java","module":"java/java8_features","moduleName":"java8 features","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between Function.andThen() and Function.compose()?","answerFull":"andThen(g) returns a function that applies this first, then g: result is g(f(x)) where f is this. compose(g) returns a function that applies g first, then this: result is f(g(x)). Mnemonic: andThen reads left-to-right — \"do f, AND THEN do g.\" compose reads right-to-left — mathematical function composition f ∘ g. Example: trim.andThen(toUpperCase) → trim first, then uppercase. toUpperCase.compose(trim) → same result. They differ in which function is \"outer\": andThen makes the argument the outer function; compose makes this the outer function.","correct":"andThen(g) returns a function that applies this first, then g: result is g(f(x)) where f is this.","distractors":["map() applies a one-to-one function: Stream<T> → Stream<R> where each element maps to exactly one result.","(1) Hot paths with very small collections — stream pipeline overhead (lambda dispatch, boxing for primitives) can dominate.","A functional interface has exactly one abstract method (SAM — Single Abstract Method)."],"distractorIds":["java/java8_features#e2f498121ba7","java/java8_features#9cd952103152","java/java8_features#b4fc28855a13"],"concepts":["andthen","compose","difference","applies","result","function"],"questionMd":"What is the difference between `Function.andThen()` and `Function.compose()`?","correctMd":"`andThen(g)` returns a function that applies `this` first, then `g`: result is `g(f(x))` where `f` is `this`.","answerFullMd":"`andThen(g)` returns a function that applies `this` first, then `g`: result is `g(f(x))` where `f` is `this`. `compose(g)` returns a function that applies `g` first, then `this`: result is `f(g(x))`. Mnemonic: `andThen` reads left-to-right — \"do f, AND THEN do g.\" `compose` reads right-to-left — mathematical function composition `f ∘ g`. Example: `trim.andThen(toUpperCase)` → trim first, then uppercase. `toUpperCase.compose(trim)` → same result. They differ in which function is \"outer\": `andThen` makes the argument the outer function; `compose` makes `this` the outer function.","distractorsMd":["`map()` applies a one-to-one function: `Stream<T> → Stream<R>` where each element maps to exactly one result.",null,null]},{"id":"java/java8_features#2de54b2550f7","section":"java","module":"java/java8_features","moduleName":"java8 features","sourceFile":"README.md","difficulty":"advanced","question":"Why prefer IntStream over Stream<Integer> in performance-critical paths?","answerFull":"Stream<Integer> requires boxing every primitive int into a heap-allocated Integer object. For 1M integers: 1M Integer allocations, GC pressure from short-lived objects, and CPU time for boxing/unboxing at every operation. IntStream stores elements as bare int primitives — no boxing, no heap allocation, cache-friendly. JMH benchmarks consistently show IntStream 5-10x faster than Stream<Integer> for numeric workloads. The standard library provides IntStream, LongStream, and DoubleStream for this reason. Always use mapToInt()/mapToLong()/mapToDouble() to convert to primitive streams when processing numbers.","correct":"Stream<Integer> requires boxing every primitive int into a heap-allocated Integer object.","distractors":["java.util.Date and Calendar had three critical flaws: mutable (not thread-safe), poor API design (month is 0-indexed, confusing methods), and no separation of concepts.","Intermediate operations are lazy, return a new Stream, and don't trigger computation: filter, map, flatMap, distinct, sorted, peek, limit, skip.","orElse(T value) always evaluates the argument, even if the Optional is present — if computing the default is expensive (DB call, object creation), you pay the cost every time."],"distractorIds":["java/java8_features#97bfb2f6c7c5","java/java8_features#194d85a2b77f","java/java8_features#0fd942ef7709"],"concepts":["intstream","boxing","primitive","prefer","allocated","critical"],"questionMd":"Why prefer `IntStream` over `Stream<Integer>` in performance-critical paths?","correctMd":"`Stream<Integer>` requires boxing every primitive `int` into a heap-allocated `Integer` object.","answerFullMd":"`Stream<Integer>` requires boxing every primitive `int` into a heap-allocated `Integer` object. For 1M integers: 1M `Integer` allocations, GC pressure from short-lived objects, and CPU time for boxing/unboxing at every operation. `IntStream` stores elements as bare `int` primitives — no boxing, no heap allocation, cache-friendly. JMH benchmarks consistently show `IntStream` 5-10x faster than `Stream<Integer>` for numeric workloads. The standard library provides `IntStream`, `LongStream`, and `DoubleStream` for this reason. Always use `mapToInt()`/`mapToLong()`/`mapToDouble()` to convert to primitive streams when processing numbers.","distractorsMd":["`java.util.Date` and `Calendar` had three critical flaws: mutable (not thread-safe), poor API design (month is 0-indexed, confusing methods), and no separation of concepts.","Intermediate operations are lazy, return a new `Stream`, and don't trigger computation: `filter`, `map`, `flatMap`, `distinct`, `sorted`, `peek`, `limit`, `skip`.","`orElse(T value)` always evaluates the argument, even if the Optional is present — if computing the default is expensive (DB call, object creation), you pay the cost every time."]},{"id":"java/java8_features#7ededed0b0d3","section":"java","module":"java/java8_features","moduleName":"java8 features","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between Optional.of(), Optional.ofNullable(), and Optional.empty(), and what are the anti-patterns for Optional usage?","answerFull":"Optional.of(value) throws NullPointerException if value is null — use when you know the value is non-null and want to fail-fast. Optional.ofNullable(value) wraps null as Optional.empty() — use when the value may legitimately be null. Optional.empty() returns the canonical empty instance (a singleton). Common anti-patterns: (1) Using Optional.get() without isPresent() check — throws NoSuchElementException, defeating the purpose; use orElse(), orElseGet(), or orElseThrow() instead. (2) Optional as a method parameter — forces callers to wrap values; use overloading or nullable parameters. (3) Optional as a field — Optional is not Serializable, making the class non-serializable; use nullable field with a getX() returning Optional. (4) Unnecessary isPresent() + get() — use map()/flatMap() for transformations. The canonical use case for Optional is as a return type for a method that may have no result, explicitly communicating the absence to the caller.","correct":"Optional.of(value) throws NullPointerException if value is null — use when you know the value is non-null and want to fail-fast.","distractors":["Using Optional as a parameter forces the caller to wrap their value in Optional.of() or Optional.empty() — awkward and verbose.","orElse(T value) always evaluates the argument, even if the Optional is present — if computing the default is expensive (DB call, object creation), you pay the cost every time.","(1) Hot paths with very small collections — stream pipeline overhead (lambda dispatch, boxing for primitives) can dominate."],"distractorIds":["java/java8_features#c0aa7846e0a1","java/java8_features#0fd942ef7709","java/java8_features#9cd952103152"],"concepts":["nullpointerexception","anti","difference","know","want","usage"],"questionMd":"What is the difference between `Optional.of()`, `Optional.ofNullable()`, and `Optional.empty()`, and what are the anti-patterns for `Optional` usage?","correctMd":"`Optional.of(value)` throws `NullPointerException` if `value` is null — use when you know the value is non-null and want to fail-fast.","answerFullMd":"`Optional.of(value)` throws `NullPointerException` if `value` is null — use when you know the value is non-null and want to fail-fast. `Optional.ofNullable(value)` wraps null as `Optional.empty()` — use when the value may legitimately be null. `Optional.empty()` returns the canonical empty instance (a singleton). Common anti-patterns: (1) **Using `Optional.get()` without `isPresent()` check** — throws `NoSuchElementException`, defeating the purpose; use `orElse()`, `orElseGet()`, or `orElseThrow()` instead. (2) **`Optional` as a method parameter** — forces callers to wrap values; use overloading or nullable parameters. (3) **`Optional` as a field** — `Optional` is not `Serializable`, making the class non-serializable; use nullable field with a `getX()` returning `Optional`. (4) **Unnecessary `isPresent()` + `get()`** — use `map()`/`flatMap()` for transformations. The canonical use case for `Optional` is as a return type for a method that may have no result, explicitly communicating the absence to the caller.","distractorsMd":["Using `Optional` as a parameter forces the caller to wrap their value in `Optional.of()` or `Optional.empty()` — awkward and verbose.","`orElse(T value)` always evaluates the argument, even if the Optional is present — if computing the default is expensive (DB call, object creation), you pay the cost every time.",null]},{"id":"java/java9_to_21_features#1eba4d455c91","section":"java","module":"java/java9_to_21_features","moduleName":"java9 to 21 features","sourceFile":"README.md","difficulty":"core","question":"What problem do Records solve compared to plain POJOs?","answerFull":"Records eliminate the boilerplate of data carrier classes: no need to write constructors, equals(), hashCode(), toString(), or accessor methods. A plain POJO with 5 fields requires ~50 lines; the equivalent Record is 1 line. More importantly, Records guarantee immutability (all fields final) and correct equality semantics by default — developers can't forget to update hashCode() when adding a field.","correct":"Records eliminate the boilerplate of data carrier classes: no need to write constructors, equals(), hashCode(), toString(), or accessor methods.","distractors":["A virtual thread is \"pinned\" when it cannot be unmounted from its carrier platform thread while blocked.","Pinning occurs when a virtual thread cannot be unmounted from its carrier thread — the carrier is \"pinned\" and blocked, which wastes an OS thread.","A platform thread maps 1:1 to an OS thread with a fixed stack of ~1MB (configurable via -Xss)."],"distractorIds":["java/java9_to_21_features#c1a8b122edb7","java/java9_to_21_features#06d61eb185b1","java/java9_to_21_features#c6e0b7f8ce0e"],"concepts":["accessor","eliminate","pojos","boilerplate","constructors","hashcode"],"correctMd":"Records eliminate the boilerplate of data carrier classes: no need to write constructors, `equals()`, `hashCode()`, `toString()`, or accessor methods.","answerFullMd":"Records eliminate the boilerplate of data carrier classes: no need to write constructors, `equals()`, `hashCode()`, `toString()`, or accessor methods. A plain POJO with 5 fields requires ~50 lines; the equivalent Record is 1 line. More importantly, Records guarantee immutability (all fields `final`) and correct equality semantics by default — developers can't forget to update `hashCode()` when adding a field.","distractorsMd":[null,null,"A platform thread maps 1:1 to an OS thread with a fixed stack of ~1MB (configurable via `-Xss`)."]},{"id":"java/java9_to_21_features#45f9e7c2e03e","section":"java","module":"java/java9_to_21_features","moduleName":"java9 to 21 features","sourceFile":"README.md","difficulty":"core","question":"Can you extend a Record? Can a Record extend another class?","answerFull":"No to both. Records implicitly extend java.lang.Record (a JDK class), and Java only allows single inheritance — so a Record cannot extend any other class. Similarly, you cannot extend a Record because all its fields are final and the class design is sealed by the compiler. Records *can* implement interfaces, which is how you add behavior polymorphically.","correct":"No to both. Records implicitly extend java.lang.Record (a JDK class), and Java only allows single inheritance — so a Record cannot extend any other class.","distractors":["Records eliminate the boilerplate of data carrier classes: no need to write constructors, equals(), hashCode(), toString(), or accessor methods.","Pinning occurs when a virtual thread cannot be unmounted from its carrier thread — the carrier is \"pinned\" and blocked, which wastes an OS thread.","When a sealed interface/class has a permits clause listing all subtypes, the compiler has complete knowledge of the closed hierarchy at compile time."],"distractorIds":["java/java9_to_21_features#1eba4d455c91","java/java9_to_21_features#06d61eb185b1","java/java9_to_21_features#79e3ad0a8f52"],"concepts":["extend","implicitly","lang","jdk","inheritance","records"],"correctMd":"No to both. Records implicitly extend `java.lang.Record` (a JDK class), and Java only allows single inheritance — so a Record cannot extend any other class.","answerFullMd":"No to both. Records implicitly extend `java.lang.Record` (a JDK class), and Java only allows single inheritance — so a Record cannot extend any other class. Similarly, you cannot extend a Record because all its fields are `final` and the class design is sealed by the compiler. Records *can* implement interfaces, which is how you add behavior polymorphically.","distractorsMd":["Records eliminate the boilerplate of data carrier classes: no need to write constructors, `equals()`, `hashCode()`, `toString()`, or accessor methods.",null,"When a sealed interface/class has a `permits` clause listing all subtypes, the compiler has complete knowledge of the closed hierarchy at compile time."]},{"id":"java/java9_to_21_features#79e3ad0a8f52","section":"java","module":"java/java9_to_21_features","moduleName":"java9 to 21 features","sourceFile":"README.md","difficulty":"core","question":"How do sealed classes enable exhaustive pattern matching?","answerFull":"When a sealed interface/class has a permits clause listing all subtypes, the compiler has complete knowledge of the closed hierarchy at compile time. A switch expression over a sealed type that handles every permitted subtype is considered exhaustive — no default branch needed, and the compiler flags any missing case. This is analogous to ADTs in Haskell/Scala: the type system guarantees you handle every variant.","correct":"When a sealed interface/class has a permits clause listing all subtypes, the compiler has complete knowledge of the closed hierarchy at compile time.","distractors":["Record deconstruction (Java 21) allows extracting a record's components directly in a pattern.","For sealed types, the compiler tracks which subtypes are covered by case patterns.","Records eliminate the boilerplate of data carrier classes: no need to write constructors, equals(), hashCode(), toString(), or accessor methods."],"distractorIds":["java/java9_to_21_features#8be19ee5bbd2","java/java9_to_21_features#6eda9fb99190","java/java9_to_21_features#1eba4d455c91"],"concepts":["exhaustive","permits","sealed","subtypes","clause","listing"],"correctMd":"When a sealed interface/class has a `permits` clause listing all subtypes, the compiler has complete knowledge of the closed hierarchy at compile time.","answerFullMd":"When a sealed interface/class has a `permits` clause listing all subtypes, the compiler has complete knowledge of the closed hierarchy at compile time. A `switch` expression over a sealed type that handles every permitted subtype is considered exhaustive — no `default` branch needed, and the compiler flags any missing case. This is analogous to ADTs in Haskell/Scala: the type system guarantees you handle every variant.","distractorsMd":[null,"For sealed types, the compiler tracks which subtypes are covered by `case` patterns.","Records eliminate the boilerplate of data carrier classes: no need to write constructors, `equals()`, `hashCode()`, `toString()`, or accessor methods."]},{"id":"java/java9_to_21_features#06d61eb185b1","section":"java","module":"java/java9_to_21_features","moduleName":"java9 to 21 features","sourceFile":"README.md","difficulty":"core","question":"What is virtual thread pinning and how do you avoid it?","answerFull":"Pinning occurs when a virtual thread cannot be unmounted from its carrier thread — the carrier is \"pinned\" and blocked, which wastes an OS thread. Pinning is caused by: (1) synchronized block/method while waiting (e.g., blocking I/O inside synchronized); (2) native method calls (JNI). To avoid: replace synchronized with ReentrantLock in I/O-bound code. Detect with -Djdk.tracePinnedThreads=full. Java 24+ removes pinning for synchronized.","correct":"Pinning occurs when a virtual thread cannot be unmounted from its carrier thread — the carrier is \"pinned\" and blocked, which wastes an OS thread.","distractors":["A platform thread maps 1:1 to an OS thread with a fixed stack of ~1MB (configurable via -Xss).","Records eliminate the boilerplate of data carrier classes: no need to write constructors, equals(), hashCode(), toString(), or accessor methods.","A virtual thread is \"pinned\" when it cannot be unmounted from its carrier platform thread while blocked."],"distractorIds":["java/java9_to_21_features#c6e0b7f8ce0e","java/java9_to_21_features#1eba4d455c91","java/java9_to_21_features#c1a8b122edb7"],"concepts":["pinning","wastes","unmounted","carrier","pinned","blocked"],"answerFullMd":"Pinning occurs when a virtual thread cannot be unmounted from its carrier thread — the carrier is \"pinned\" and blocked, which wastes an OS thread. Pinning is caused by: (1) `synchronized` block/method while waiting (e.g., blocking I/O inside `synchronized`); (2) native method calls (JNI). To avoid: replace `synchronized` with `ReentrantLock` in I/O-bound code. Detect with `-Djdk.tracePinnedThreads=full`. Java 24+ removes pinning for `synchronized`.","distractorsMd":["A platform thread maps 1:1 to an OS thread with a fixed stack of ~1MB (configurable via `-Xss`).","Records eliminate the boilerplate of data carrier classes: no need to write constructors, `equals()`, `hashCode()`, `toString()`, or accessor methods.",null]},{"id":"java/java9_to_21_features#c6e0b7f8ce0e","section":"java","module":"java/java9_to_21_features","moduleName":"java9 to 21 features","sourceFile":"README.md","difficulty":"core","question":"What is the difference between a virtual thread and a platform thread in terms of stack size and scheduling?","answerFull":"A platform thread maps 1:1 to an OS thread with a fixed stack of ~1MB (configurable via -Xss). A virtual thread has a dynamically-growing stack starting at ~few KB, stored on the Java heap, and is scheduled by the JVM (via a ForkJoinPool of carrier threads). Platform threads: OS scheduler (preemptive). Virtual threads: cooperative (mounted/unmounted at blocking points). Result: you can have millions of virtual threads vs thousands of platform threads.","correct":"A platform thread maps 1:1 to an OS thread with a fixed stack of ~1MB (configurable via -Xss).","distractors":["A virtual thread is \"pinned\" when it cannot be unmounted from its carrier platform thread while blocked.","JPMS (Java Platform Module System) adds module-info.java files that declare module name, requires (dependencies), and exports (public API packages).","Pinning occurs when a virtual thread cannot be unmounted from its carrier thread — the carrier is \"pinned\" and blocked, which wastes an OS thread."],"distractorIds":["java/java9_to_21_features#c1a8b122edb7","java/java9_to_21_features#cf8523b48960","java/java9_to_21_features#06d61eb185b1"],"concepts":["xss","1mb","configurable","difference","scheduling","terms"],"correctMd":"A platform thread maps 1:1 to an OS thread with a fixed stack of ~1MB (configurable via `-Xss`).","answerFullMd":"A platform thread maps 1:1 to an OS thread with a fixed stack of ~1MB (configurable via `-Xss`). A virtual thread has a dynamically-growing stack starting at ~few KB, stored on the Java heap, and is scheduled by the JVM (via a ForkJoinPool of carrier threads). Platform threads: OS scheduler (preemptive). Virtual threads: cooperative (mounted/unmounted at blocking points). Result: you can have millions of virtual threads vs thousands of platform threads.","distractorsMd":[null,"JPMS (Java Platform Module System) adds `module-info.java` files that declare module name, `requires` (dependencies), and `exports` (public API packages).",null]},{"id":"java/java9_to_21_features#11c71ae858cb","section":"java","module":"java/java9_to_21_features","moduleName":"java9 to 21 features","sourceFile":"README.md","difficulty":"intermediate","question":"What limitations does var have?","answerFull":"var can only be used for: local variables with an initializer, for loop variables, try-with-resources variables. It CANNOT be used for: instance/static fields, method parameters, method return types, lambda parameters (without a cast), catch clause variables. Also, var x = null is illegal (null has no type), and var with an unparameterized diamond new ArrayList<>() loses type info. var is a syntactic sugar — the variable is still strongly typed at compile time.","correct":"var can only be used for: local variables with an initializer, for loop variables, try-with-resources variables.","distractors":["A platform thread maps 1:1 to an OS thread with a fixed stack of ~1MB (configurable via -Xss).","A text block \"\"\"...\"\"\" is a multi-line string literal.","A virtual thread is \"pinned\" when it cannot be unmounted from its carrier platform thread while blocked."],"distractorIds":["java/java9_to_21_features#c6e0b7f8ce0e","java/java9_to_21_features#86d06218accd","java/java9_to_21_features#c1a8b122edb7"],"concepts":["limitations","initializer","var","variables","try","resources"],"questionMd":"What limitations does `var` have?","correctMd":"`var` can only be used for: local variables with an initializer, `for` loop variables, try-with-resources variables.","answerFullMd":"`var` can only be used for: local variables with an initializer, `for` loop variables, try-with-resources variables. It CANNOT be used for: instance/static fields, method parameters, method return types, lambda parameters (without a cast), catch clause variables. Also, `var x = null` is illegal (null has no type), and `var` with an unparameterized diamond `new ArrayList<>()` loses type info. `var` is a syntactic sugar — the variable is still strongly typed at compile time.","distractorsMd":["A platform thread maps 1:1 to an OS thread with a fixed stack of ~1MB (configurable via `-Xss`).","A text block `\"\"\"...\"\"\"` is a multi-line string literal.",null]},{"id":"java/java9_to_21_features#6eda9fb99190","section":"java","module":"java/java9_to_21_features","moduleName":"java9 to 21 features","sourceFile":"README.md","difficulty":"intermediate","question":"How does pattern matching for switch ensure exhaustiveness?","answerFull":"For sealed types, the compiler tracks which subtypes are covered by case patterns. If any permitted subtype is unhandled and there's no default, the compiler emits an error. For non-sealed types (e.g., Object), a default or case null is required. Guarded patterns case Foo f when condition don't count for exhaustiveness (the compiler can't prove the condition always matches), so an unguarded case for the same type is also needed.","correct":"For sealed types, the compiler tracks which subtypes are covered by case patterns.","distractors":["Switch expression (Java 14+) yields a value, uses arrow labels -> (no fall-through by default), and is exhaustive for enum/sealed types.","When a sealed interface/class has a permits clause listing all subtypes, the compiler has complete knowledge of the closed hierarchy at compile time.","Record deconstruction (Java 21) allows extracting a record's components directly in a pattern."],"distractorIds":["java/java9_to_21_features#d887245220e2","java/java9_to_21_features#79e3ad0a8f52","java/java9_to_21_features#8be19ee5bbd2"],"concepts":["covered","sealed","subtypes","ensure","compiler","tracks"],"correctMd":"For sealed types, the compiler tracks which subtypes are covered by `case` patterns.","answerFullMd":"For sealed types, the compiler tracks which subtypes are covered by `case` patterns. If any permitted subtype is unhandled and there's no `default`, the compiler emits an error. For non-sealed types (e.g., `Object`), a `default` or `case null` is required. Guarded patterns `case Foo f when condition` don't count for exhaustiveness (the compiler can't prove the condition always matches), so an unguarded case for the same type is also needed.","distractorsMd":["Switch expression (Java 14+) yields a value, uses arrow labels `->` (no fall-through by default), and is exhaustive for enum/sealed types.","When a sealed interface/class has a `permits` clause listing all subtypes, the compiler has complete knowledge of the closed hierarchy at compile time.",null]},{"id":"java/java9_to_21_features#cf8523b48960","section":"java","module":"java/java9_to_21_features","moduleName":"java9 to 21 features","sourceFile":"README.md","difficulty":"intermediate","question":"What was added to Java 9's module system (JPMS) and why was it created?","answerFull":"JPMS (Java Platform Module System) adds module-info.java files that declare module name, requires (dependencies), and exports (public API packages). It was created to: (1) Strongly encapsulate JDK internals (e.g., sun.misc.Unsafe) that were accidentally accessible via reflection; (2) Enable creation of minimal custom JRE images with jlink; (3) Provide reliable configuration (fail-fast on missing dependencies). Adoption is slow because many popular libraries are not yet fully modularized.","correct":"JPMS (Java Platform Module System) adds module-info.java files that declare module name, requires (dependencies), and exports (public API packages).","distractors":["A platform thread maps 1:1 to an OS thread with a fixed stack of ~1MB (configurable via -Xss).","Record deconstruction (Java 21) allows extracting a record's components directly in a pattern.","Switch expression (Java 14+) yields a value, uses arrow labels -> (no fall-through by default), and is exhaustive for enum/sealed types."],"distractorIds":["java/java9_to_21_features#c6e0b7f8ce0e","java/java9_to_21_features#8be19ee5bbd2","java/java9_to_21_features#d887245220e2"],"concepts":["jpms","exports","packages","info","declare","added"],"correctMd":"JPMS (Java Platform Module System) adds `module-info.java` files that declare module name, `requires` (dependencies), and `exports` (public API packages).","answerFullMd":"JPMS (Java Platform Module System) adds `module-info.java` files that declare module name, `requires` (dependencies), and `exports` (public API packages). It was created to: (1) Strongly encapsulate JDK internals (e.g., `sun.misc.Unsafe`) that were accidentally accessible via reflection; (2) Enable creation of minimal custom JRE images with `jlink`; (3) Provide reliable configuration (fail-fast on missing dependencies). Adoption is slow because many popular libraries are not yet fully modularized.","distractorsMd":["A platform thread maps 1:1 to an OS thread with a fixed stack of ~1MB (configurable via `-Xss`).",null,"Switch expression (Java 14+) yields a value, uses arrow labels `->` (no fall-through by default), and is exhaustive for enum/sealed types."]},{"id":"java/java9_to_21_features#816515180571","section":"java","module":"java/java9_to_21_features","moduleName":"java9 to 21 features","sourceFile":"README.md","difficulty":"intermediate","question":"What is StructuredTaskScope and when would you use it?","answerFull":"StructuredTaskScope (Java 21) implements structured concurrency — ensuring that concurrent sub-tasks live within the lifetime of their parent task. ShutdownOnFailure: if any fork fails, cancel the others and propagate the exception. ShutdownOnSuccess: take the first successful result, cancel the rest. Use it when: you need to parallel-fetch two resources and need both (ShutdownOnFailure), or you want the fastest of N attempts (ShutdownOnSuccess). It prevents orphaned threads and makes cancellation automatic.","correct":"StructuredTaskScope (Java 21) implements structured concurrency — ensuring that concurrent sub-tasks live within the lifetime of their parent task.","distractors":["Record deconstruction (Java 21) allows extracting a record's components directly in a pattern.","Switch expression (Java 14+) yields a value, uses arrow labels -> (no fall-through by default), and is exhaustive for enum/sealed types.","JPMS (Java Platform Module System) adds module-info.java files that declare module name, requires (dependencies), and exports (public API packages)."],"distractorIds":["java/java9_to_21_features#8be19ee5bbd2","java/java9_to_21_features#d887245220e2","java/java9_to_21_features#cf8523b48960"],"concepts":["structuredtaskscope","ensuring","lifetime","implements","live","concurrency"],"correctMd":"`StructuredTaskScope` (Java 21) implements structured concurrency — ensuring that concurrent sub-tasks live within the lifetime of their parent task.","answerFullMd":"`StructuredTaskScope` (Java 21) implements structured concurrency — ensuring that concurrent sub-tasks live within the lifetime of their parent task. `ShutdownOnFailure`: if any fork fails, cancel the others and propagate the exception. `ShutdownOnSuccess`: take the first successful result, cancel the rest. Use it when: you need to parallel-fetch two resources and need both (ShutdownOnFailure), or you want the fastest of N attempts (ShutdownOnSuccess). It prevents orphaned threads and makes cancellation automatic.","distractorsMd":[null,"Switch expression (Java 14+) yields a value, uses arrow labels `->` (no fall-through by default), and is exhaustive for enum/sealed types.","JPMS (Java Platform Module System) adds `module-info.java` files that declare module name, `requires` (dependencies), and `exports` (public API packages)."]},{"id":"java/java9_to_21_features#86d06218accd","section":"java","module":"java/java9_to_21_features","moduleName":"java9 to 21 features","sourceFile":"README.md","difficulty":"advanced","question":"What is a text block and how does it handle indentation?","answerFull":"A text block \"\"\"...\"\"\" is a multi-line string literal. The compiler strips common leading whitespace (incidental whitespace) based on the least-indented line. The trailing \"\"\" position controls the stripping — if it's on a new line indented by N spaces, N spaces are stripped from all lines. Backslash line continuation \\ merges lines. Text blocks support the full string API and are equivalent to regular strings at runtime.","correct":"A text block \"\"\"...\"\"\" is a multi-line string literal.","distractors":["A platform thread maps 1:1 to an OS thread with a fixed stack of ~1MB (configurable via -Xss).","A virtual thread is \"pinned\" when it cannot be unmounted from its carrier platform thread while blocked.","For sealed types, the compiler tracks which subtypes are covered by case patterns."],"distractorIds":["java/java9_to_21_features#c6e0b7f8ce0e","java/java9_to_21_features#c1a8b122edb7","java/java9_to_21_features#6eda9fb99190"],"concepts":["indentation","literal","handle","line","string","block"],"correctMd":"A text block `\"\"\"...\"\"\"` is a multi-line string literal.","answerFullMd":"A text block `\"\"\"...\"\"\"` is a multi-line string literal. The compiler strips common leading whitespace (incidental whitespace) based on the least-indented line. The trailing `\"\"\"` position controls the stripping — if it's on a new line indented by N spaces, N spaces are stripped from all lines. Backslash line continuation `\\` merges lines. Text blocks support the full string API and are equivalent to regular strings at runtime.","distractorsMd":["A platform thread maps 1:1 to an OS thread with a fixed stack of ~1MB (configurable via `-Xss`).",null,"For sealed types, the compiler tracks which subtypes are covered by `case` patterns."]},{"id":"java/java9_to_21_features#d887245220e2","section":"java","module":"java/java9_to_21_features","moduleName":"java9 to 21 features","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between a switch expression and a switch statement?","answerFull":"Switch expression (Java 14+) yields a value, uses arrow labels -> (no fall-through by default), and is exhaustive for enum/sealed types. Switch statement (traditional) doesn't return a value, uses case X: ... break, and has fall-through by default. For default-less switch expressions over non-enum types, the compiler requires a default branch. Use yield inside a block to return from a switch expression: case X -> { ...; yield value; }.","correct":"Switch expression (Java 14+) yields a value, uses arrow labels -> (no fall-through by default), and is exhaustive for enum/sealed types.","distractors":["Record deconstruction (Java 21) allows extracting a record's components directly in a pattern.","For sealed types, the compiler tracks which subtypes are covered by case patterns.","When a sealed interface/class has a permits clause listing all subtypes, the compiler has complete knowledge of the closed hierarchy at compile time."],"distractorIds":["java/java9_to_21_features#8be19ee5bbd2","java/java9_to_21_features#6eda9fb99190","java/java9_to_21_features#79e3ad0a8f52"],"concepts":["arrow","exhaustive","sealed","fall","difference","enum"],"correctMd":"Switch expression (Java 14+) yields a value, uses arrow labels `->` (no fall-through by default), and is exhaustive for enum/sealed types.","answerFullMd":"Switch expression (Java 14+) yields a value, uses arrow labels `->` (no fall-through by default), and is exhaustive for enum/sealed types. Switch statement (traditional) doesn't return a value, uses `case X: ... break`, and has fall-through by default. For `default`-less switch expressions over non-enum types, the compiler requires a `default` branch. Use `yield` inside a block to return from a switch expression: `case X -> { ...; yield value; }`.","distractorsMd":[null,"For sealed types, the compiler tracks which subtypes are covered by `case` patterns.","When a sealed interface/class has a `permits` clause listing all subtypes, the compiler has complete knowledge of the closed hierarchy at compile time."]},{"id":"java/java9_to_21_features#8be19ee5bbd2","section":"java","module":"java/java9_to_21_features","moduleName":"java9 to 21 features","sourceFile":"README.md","difficulty":"advanced","question":"What is record deconstruction and how does pattern matching for switch use it?","answerFull":"Record deconstruction (Java 21) allows extracting a record's components directly in a pattern. case Point(int x, int y) simultaneously matches a Point instance AND binds its x() and y() components to local variables x and y. No explicit casting or accessor calls needed. In switch expressions with sealed types, this creates exhaustive, type-safe dispatch that reads like algebraic data type matching. Nested deconstruction works too: case Line(Point(int x1, int y1), Point(int x2, int y2)) extracts components from a Line and both its nested Point components in one pattern. Guarded patterns add when conditions: case Point(int x, int y) when x > 0.","correct":"Record deconstruction (Java 21) allows extracting a record's components directly in a pattern.","distractors":["StructuredTaskScope (Java 21) implements structured concurrency — ensuring that concurrent sub-tasks live within the lifetime of their parent task.","Switch expression (Java 14+) yields a value, uses arrow labels -> (no fall-through by default), and is exhaustive for enum/sealed types.","JPMS (Java Platform Module System) adds module-info.java files that declare module name, requires (dependencies), and exports (public API packages)."],"distractorIds":["java/java9_to_21_features#816515180571","java/java9_to_21_features#d887245220e2","java/java9_to_21_features#cf8523b48960"],"concepts":["deconstruction","extracting","switch","components","matching","allows"],"answerFullMd":"Record deconstruction (Java 21) allows extracting a record's components directly in a pattern. `case Point(int x, int y)` simultaneously matches a `Point` instance AND binds its `x()` and `y()` components to local variables `x` and `y`. No explicit casting or accessor calls needed. In `switch` expressions with sealed types, this creates exhaustive, type-safe dispatch that reads like algebraic data type matching. Nested deconstruction works too: `case Line(Point(int x1, int y1), Point(int x2, int y2))` extracts components from a `Line` and both its nested `Point` components in one pattern. Guarded patterns add `when` conditions: `case Point(int x, int y) when x > 0`.","distractorsMd":["`StructuredTaskScope` (Java 21) implements structured concurrency — ensuring that concurrent sub-tasks live within the lifetime of their parent task.","Switch expression (Java 14+) yields a value, uses arrow labels `->` (no fall-through by default), and is exhaustive for enum/sealed types.","JPMS (Java Platform Module System) adds `module-info.java` files that declare module name, `requires` (dependencies), and `exports` (public API packages)."]},{"id":"java/java9_to_21_features#c1a8b122edb7","section":"java","module":"java/java9_to_21_features","moduleName":"java9 to 21 features","sourceFile":"README.md","difficulty":"advanced","question":"What is virtual thread \"pinning\" and which constructs cause it in Java 21?","answerFull":"A virtual thread is \"pinned\" when it cannot be unmounted from its carrier platform thread while blocked. During pinning, the underlying platform thread is held — defeating the memory efficiency benefit of virtual threads (which is that thousands of virtual threads share a small pool of platform threads). Two constructs pin the carrier thread: (1) synchronized blocks/methods — if a virtual thread blocks inside synchronized, the carrier thread is pinned for the duration of the block. (2) Native method frames (System.loadLibrary, JNI calls) — the JVM cannot unmount while native code is on the stack. Diagnose pinning with -Djdk.tracePinnedThreads=full which logs stack traces. Fix: replace synchronized with ReentrantLock in code called from virtual threads — ReentrantLock.lock() allows unmounting while waiting. Java 25 is expected to remove the synchronized pinning restriction via opaque monitors. Practical rule: in Spring Boot 3.2+ with spring.threads.virtual.enabled=true, audit your code (and dependencies like JDBC drivers) for heavy synchronized usage in hot paths.","correct":"A virtual thread is \"pinned\" when it cannot be unmounted from its carrier platform thread while blocked.","distractors":["Pinning occurs when a virtual thread cannot be unmounted from its carrier thread — the carrier is \"pinned\" and blocked, which wastes an OS thread.","Switch expression (Java 14+) yields a value, uses arrow labels -> (no fall-through by default), and is exhaustive for enum/sealed types.","Records eliminate the boilerplate of data carrier classes: no need to write constructors, equals(), hashCode(), toString(), or accessor methods."],"distractorIds":["java/java9_to_21_features#06d61eb185b1","java/java9_to_21_features#d887245220e2","java/java9_to_21_features#1eba4d455c91"],"concepts":["constructs","pinning","unmounted","carrier","pinned","blocked"],"answerFullMd":"A virtual thread is \"pinned\" when it cannot be unmounted from its carrier platform thread while blocked. During pinning, the underlying platform thread is held — defeating the memory efficiency benefit of virtual threads (which is that thousands of virtual threads share a small pool of platform threads). Two constructs pin the carrier thread: (1) **`synchronized` blocks/methods** — if a virtual thread blocks inside `synchronized`, the carrier thread is pinned for the duration of the block. (2) **Native method frames** (`System.loadLibrary`, JNI calls) — the JVM cannot unmount while native code is on the stack. Diagnose pinning with `-Djdk.tracePinnedThreads=full` which logs stack traces. Fix: replace `synchronized` with `ReentrantLock` in code called from virtual threads — `ReentrantLock.lock()` allows unmounting while waiting. Java 25 is expected to remove the `synchronized` pinning restriction via opaque monitors. Practical rule: in Spring Boot 3.2+ with `spring.threads.virtual.enabled=true`, audit your code (and dependencies like JDBC drivers) for heavy `synchronized` usage in hot paths.","distractorsMd":[null,"Switch expression (Java 14+) yields a value, uses arrow labels `->` (no fall-through by default), and is exhaustive for enum/sealed types.","Records eliminate the boilerplate of data carrier classes: no need to write constructors, `equals()`, `hashCode()`, `toString()`, or accessor methods."]},{"id":"java/java_interview_patterns#f5b94f668396","section":"java","module":"java/java_interview_patterns","moduleName":"java interview patterns","sourceFile":"README.md","difficulty":"core","question":"How do you write a proper immutable class in Java?","answerFull":"Five rules: (1) Declare the class final — prevents subclasses from adding mutable state. (2) All fields private final — reference cannot be reassigned after construction. (3) No setter methods. (4) Defensive copy of mutable inputs in the constructor: new Date(date.getTime()) or List.copyOf(list). (5) Defensive copy or unmodifiable view on accessor methods that return mutable objects. Bonus: don't let this escape the constructor (don't pass this to external code during initialization).","correct":"Five rules: (1) Declare the class final — prevents subclasses from adding mutable state.","distractors":["Three reasons: (1) Serialization-safe — Java's enum serialization mechanism guarantees that each constant is only deserialized to the existing instance (using the name, not constructor); no need for readResolve().","A subclass can override a method and declare a more specific return type.","Builder solves the \"telescoping constructor\" problem: when a class has many optional parameters, providing all combinations of constructor signatures is impractical."],"distractorIds":["java/java_interview_patterns#4e61025d00f9","java/java_interview_patterns#7a7a50ce0619","java/java_interview_patterns#772d53ace735"],"concepts":["proper","five","declare","subclasses","rules","mutable"],"correctMd":"Five rules: (1) Declare the class `final` — prevents subclasses from adding mutable state.","answerFullMd":"Five rules: (1) Declare the class `final` — prevents subclasses from adding mutable state. (2) All fields `private final` — reference cannot be reassigned after construction. (3) No setter methods. (4) Defensive copy of mutable inputs in the constructor: `new Date(date.getTime())` or `List.copyOf(list)`. (5) Defensive copy or unmodifiable view on accessor methods that return mutable objects. Bonus: don't let `this` escape the constructor (don't pass `this` to external code during initialization).","distractorsMd":["Three reasons: (1) Serialization-safe — Java's enum serialization mechanism guarantees that each constant is only deserialized to the existing instance (using the name, not constructor); no need for `readResolve()`.",null,null]},{"id":"java/java_interview_patterns#772d53ace735","section":"java","module":"java/java_interview_patterns","moduleName":"java interview patterns","sourceFile":"README.md","difficulty":"core","question":"What is the Builder pattern and when do you use it over constructors?","answerFull":"Builder solves the \"telescoping constructor\" problem: when a class has many optional parameters, providing all combinations of constructor signatures is impractical. Builder: client calls a fluent API on the inner Builder class to set desired parameters, then calls build() which validates invariants and constructs the immutable object. Use Builder when: 4+ parameters exist, many are optional, or multiple parameters have the same type (easy to transpose). Static factory methods are an alternative for simpler cases. Builder is preferred for complex objects that must be immutable.","correct":"Builder solves the \"telescoping constructor\" problem: when a class has many optional parameters, providing all combinations of constructor signatures is impractical.","distractors":["Three approaches: (1) Objects.requireNonNull(param, \"name\") in constructor/method — fails fast with a clear NPE message.","Defensive copying means creating a copy of a mutable input or output so the caller cannot affect the class's internal state through a shared reference.","Five rules: (1) Declare the class final — prevents subclasses from adding mutable state."],"distractorIds":["java/java_interview_patterns#43eb220e52e6","java/java_interview_patterns#c754c73342d9","java/java_interview_patterns#f5b94f668396"],"concepts":["telescoping","impractical","constructors","builder","combinations","signatures"],"answerFullMd":"Builder solves the \"telescoping constructor\" problem: when a class has many optional parameters, providing all combinations of constructor signatures is impractical. Builder: client calls a fluent API on the inner `Builder` class to set desired parameters, then calls `build()` which validates invariants and constructs the immutable object. Use Builder when: 4+ parameters exist, many are optional, or multiple parameters have the same type (easy to transpose). Static factory methods are an alternative for simpler cases. Builder is preferred for complex objects that must be immutable.","distractorsMd":["Three approaches: (1) `Objects.requireNonNull(param, \"name\")` in constructor/method — fails fast with a clear NPE message.",null,"Five rules: (1) Declare the class `final` — prevents subclasses from adding mutable state."]},{"id":"java/java_interview_patterns#4e61025d00f9","section":"java","module":"java/java_interview_patterns","moduleName":"java interview patterns","sourceFile":"README.md","difficulty":"core","question":"Why is enum the best singleton implementation? (Effective Java Item 3)","answerFull":"Three reasons: (1) Serialization-safe — Java's enum serialization mechanism guarantees that each constant is only deserialized to the existing instance (using the name, not constructor); no need for readResolve(). (2) Reflection-safe — Constructor.newInstance() on an enum type throws IllegalArgumentException; the JVM prevents reflection-based instantiation. (3) Thread-safe — class initialization is guaranteed single-execution by the JVM. The only disadvantage: cannot use lazy initialization (enum constants are initialized when the class loads).","correct":"Three reasons: (1) Serialization-safe — Java's enum serialization mechanism guarantees that each constant is only deserialized to the existing instance (using the name, not constructor); no need for readResolve().","distractors":["Serialization: implement readResolve() returning the singleton instance — the JVM calls this after deserialization and uses the returned object instead.","Three approaches: (1) Objects.requireNonNull(param, \"name\") in constructor/method — fails fast with a clear NPE message.","Overriding: when a subclass defines an instance method with the same signature as the superclass."],"distractorIds":["java/java_interview_patterns#55eb7811f52e","java/java_interview_patterns#43eb220e52e6","java/java_interview_patterns#b50ed6a1eda6"],"concepts":["deserialized","readresolve","reasons","enum","effective","singleton"],"correctMd":"Three reasons: (1) Serialization-safe — Java's enum serialization mechanism guarantees that each constant is only deserialized to the existing instance (using the name, not constructor); no need for `readResolve()`.","answerFullMd":"Three reasons: (1) Serialization-safe — Java's enum serialization mechanism guarantees that each constant is only deserialized to the existing instance (using the name, not constructor); no need for `readResolve()`. (2) Reflection-safe — `Constructor.newInstance()` on an enum type throws `IllegalArgumentException`; the JVM prevents reflection-based instantiation. (3) Thread-safe — class initialization is guaranteed single-execution by the JVM. The only disadvantage: cannot use lazy initialization (enum constants are initialized when the class loads).","distractorsMd":["Serialization: implement `readResolve()` returning the singleton instance — the JVM calls this after deserialization and uses the returned object instead.","Three approaches: (1) `Objects.requireNonNull(param, \"name\")` in constructor/method — fails fast with a clear NPE message.",null]},{"id":"java/java_interview_patterns#c37ee20d09ef","section":"java","module":"java/java_interview_patterns","moduleName":"java interview patterns","sourceFile":"README.md","difficulty":"core","question":"What is the Integer cache and what bugs can it cause?","answerFull":"Integer.valueOf(n) caches Integer objects for values in [-128, 127]. Values outside this range always create new objects. Bug: comparing cached Integer with == works (same instance), but comparing larger values with == returns false even when values are equal. The fix: always use .equals() for Integer (and all boxed types). The cache exists as a performance optimization — these small integers are used very frequently (loop counters, small IDs, boolean flags).","correct":"Integer.valueOf(n) caches Integer objects for values in [-128, 127].","distractors":["Three approaches: (1) Objects.requireNonNull(param, \"name\") in constructor/method — fails fast with a clear NPE message.","The JVM caches Integer objects for values −128 to 127 (inclusive) — a fixed pool of 256 instances created at JVM startup.","Comparable<T> expresses the natural ordering — the single most obvious ordering for the class (e.g., String lexicographically, Integer numerically, LocalDate chronologically)."],"distractorIds":["java/java_interview_patterns#43eb220e52e6","java/java_interview_patterns#e69bb34955f8","java/java_interview_patterns#68c2c7facbde"],"concepts":["127","valueof","bugs","cause","integer","128"],"correctMd":"`Integer.valueOf(n)` caches `Integer` objects for values in [-128, 127].","answerFullMd":"`Integer.valueOf(n)` caches `Integer` objects for values in [-128, 127]. Values outside this range always create new objects. Bug: comparing cached Integer with `==` works (same instance), but comparing larger values with `==` returns `false` even when values are equal. The fix: always use `.equals()` for `Integer` (and all boxed types). The cache exists as a performance optimization — these small integers are used very frequently (loop counters, small IDs, boolean flags).","distractorsMd":["Three approaches: (1) `Objects.requireNonNull(param, \"name\")` in constructor/method — fails fast with a clear NPE message.","The JVM caches `Integer` objects for values −128 to 127 (inclusive) — a fixed pool of 256 instances created at JVM startup.","`Comparable<T>` expresses the **natural ordering** — the single most obvious ordering for the class (e.g., `String` lexicographically, `Integer` numerically, `LocalDate` chronologically)."]},{"id":"java/java_interview_patterns#041c26a01c27","section":"java","module":"java/java_interview_patterns","moduleName":"java interview patterns","sourceFile":"README.md","difficulty":"intermediate","question":"What is the Comparable subtraction trap?","answerFull":"Using return this.value - other.value in compareTo() can overflow. If this.value = Integer.MAX_VALUE (2,147,483,647) and other.value = -1, then MAX_VALUE - (-1) = MAX_VALUE + 1 which overflows to Integer.MIN_VALUE — a large negative number. The sort now places MAX_VALUE before -1, which is backwards. Fix: always use Integer.compare(this.value, other.value) — no arithmetic, no overflow. Same principle applies to Long.compare, Double.compare.","correct":"Using return this.value - other.value in compareTo() can overflow.","distractors":["Comparable<T> expresses the natural ordering — the single most obvious ordering for the class (e.g., String lexicographically, Integer numerically, LocalDate chronologically).","A subclass can override a method and declare a more specific return type.","A static factory method is a static method that returns an instance of the class."],"distractorIds":["java/java_interview_patterns#68c2c7facbde","java/java_interview_patterns#7a7a50ce0619","java/java_interview_patterns#c8f3a472742e"],"concepts":["subtraction","compareto","trap","comparable","overflow","return"],"correctMd":"Using `return this.value - other.value` in `compareTo()` can overflow.","answerFullMd":"Using `return this.value - other.value` in `compareTo()` can overflow. If `this.value = Integer.MAX_VALUE` (2,147,483,647) and `other.value = -1`, then `MAX_VALUE - (-1) = MAX_VALUE + 1` which overflows to `Integer.MIN_VALUE` — a large negative number. The sort now places `MAX_VALUE` before `-1`, which is backwards. Fix: always use `Integer.compare(this.value, other.value)` — no arithmetic, no overflow. Same principle applies to `Long.compare`, `Double.compare`.","distractorsMd":["`Comparable<T>` expresses the **natural ordering** — the single most obvious ordering for the class (e.g., `String` lexicographically, `Integer` numerically, `LocalDate` chronologically).",null,null]},{"id":"java/java_interview_patterns#b50ed6a1eda6","section":"java","module":"java/java_interview_patterns","moduleName":"java interview patterns","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between hiding (static methods) and overriding (instance methods)?","answerFull":"Overriding: when a subclass defines an instance method with the same signature as the superclass. The JVM uses the *runtime type* to dispatch — dynamic dispatch via vtable. Hiding: when a subclass defines a *static* method with the same signature as the superclass's static method. The JVM uses the *declared type* at the call site — static dispatch. Calling the method through a supertype reference always calls the supertype's static method, regardless of the runtime type. This is why static methods should not be \"overridden\" — the behavior is confusing.","correct":"Overriding: when a subclass defines an instance method with the same signature as the superclass.","distractors":["A static factory method is a static method that returns an instance of the class.","Three approaches: (1) Objects.requireNonNull(param, \"name\") in constructor/method — fails fast with a clear NPE message.","Three reasons: (1) Serialization-safe — Java's enum serialization mechanism guarantees that each constant is only deserialized to the existing instance (using the name, not constructor); no need for readResolve()."],"distractorIds":["java/java_interview_patterns#c8f3a472742e","java/java_interview_patterns#43eb220e52e6","java/java_interview_patterns#4e61025d00f9"],"concepts":["overriding","superclass","difference","subclass","signature","defines"],"distractorsMd":[null,"Three approaches: (1) `Objects.requireNonNull(param, \"name\")` in constructor/method — fails fast with a clear NPE message.","Three reasons: (1) Serialization-safe — Java's enum serialization mechanism guarantees that each constant is only deserialized to the existing instance (using the name, not constructor); no need for `readResolve()`."]},{"id":"java/java_interview_patterns#7a7a50ce0619","section":"java","module":"java/java_interview_patterns","moduleName":"java interview patterns","sourceFile":"README.md","difficulty":"intermediate","question":"What is a covariant return type?","answerFull":"A subclass can override a method and declare a more specific return type. Example: class Animal { Animal create() {...} } and class Dog extends Animal { Dog create() {...} }. Dog.create() returns Dog (more specific) — this is covariant because Dog is a subtype of Animal. This is valid in Java and does not require an explicit cast at the call site when using the Dog reference. The compiler generates a bridge method for the erased signature.","correct":"A subclass can override a method and declare a more specific return type.","distractors":["Five rules: (1) Declare the class final — prevents subclasses from adding mutable state.","Three approaches: (1) Objects.requireNonNull(param, \"name\") in constructor/method — fails fast with a clear NPE message.","A static factory method is a static method that returns an instance of the class."],"distractorIds":["java/java_interview_patterns#f5b94f668396","java/java_interview_patterns#43eb220e52e6","java/java_interview_patterns#c8f3a472742e"],"concepts":["covariant","declare","override","subclass","return","specific"],"answerFullMd":"A subclass can override a method and declare a more specific return type. Example: `class Animal { Animal create() {...} }` and `class Dog extends Animal { Dog create() {...} }`. `Dog.create()` returns `Dog` (more specific) — this is covariant because `Dog` is a subtype of `Animal`. This is valid in Java and does not require an explicit cast at the call site when using the `Dog` reference. The compiler generates a bridge method for the erased signature.","distractorsMd":["Five rules: (1) Declare the class `final` — prevents subclasses from adding mutable state.","Three approaches: (1) `Objects.requireNonNull(param, \"name\")` in constructor/method — fails fast with a clear NPE message.",null]},{"id":"java/java_interview_patterns#55eb7811f52e","section":"java","module":"java/java_interview_patterns","moduleName":"java interview patterns","sourceFile":"README.md","difficulty":"intermediate","question":"How do you prevent a singleton from being broken by serialization and reflection?","answerFull":"Serialization: implement readResolve() returning the singleton instance — the JVM calls this after deserialization and uses the returned object instead. Reflection: check in the constructor: if (instance != null) throw new IllegalStateException(\"Use getInstance()\"). Best solution: use enum — the JVM handles both threats automatically (enum's readResolve is built-in; reflection is blocked by JVM enforcement). There is no need for these tricks with enum singletons.","correct":"Serialization: implement readResolve() returning the singleton instance — the JVM calls this after deserialization and uses the returned object instead.","distractors":["A static factory method is a static method that returns an instance of the class.","Three reasons: (1) Serialization-safe — Java's enum serialization mechanism guarantees that each constant is only deserialized to the existing instance (using the name, not constructor); no need for readResolve().","The JVM caches Integer objects for values −128 to 127 (inclusive) — a fixed pool of 256 instances created at JVM startup."],"distractorIds":["java/java_interview_patterns#c8f3a472742e","java/java_interview_patterns#4e61025d00f9","java/java_interview_patterns#e69bb34955f8"],"concepts":["readresolve","deserialization","broken","prevent","reflection","singleton"],"correctMd":"Serialization: implement `readResolve()` returning the singleton instance — the JVM calls this after deserialization and uses the returned object instead.","answerFullMd":"Serialization: implement `readResolve()` returning the singleton instance — the JVM calls this after deserialization and uses the returned object instead. Reflection: check in the constructor: `if (instance != null) throw new IllegalStateException(\"Use getInstance()\")`. Best solution: use enum — the JVM handles both threats automatically (enum's `readResolve` is built-in; reflection is blocked by JVM enforcement). There is no need for these tricks with enum singletons.","distractorsMd":[null,"Three reasons: (1) Serialization-safe — Java's enum serialization mechanism guarantees that each constant is only deserialized to the existing instance (using the name, not constructor); no need for `readResolve()`.","The JVM caches `Integer` objects for values −128 to 127 (inclusive) — a fixed pool of 256 instances created at JVM startup."]},{"id":"java/java_interview_patterns#c8f3a472742e","section":"java","module":"java/java_interview_patterns","moduleName":"java interview patterns","sourceFile":"README.md","difficulty":"intermediate","question":"What is the static factory method pattern and its advantages over constructors? (Effective Java Item 1)","answerFull":"A static factory method is a static method that returns an instance of the class. Advantages: (1) Named — OptionalInt.empty() is clearer than new OptionalInt(false, 0). (2) Can return cached instances — Boolean.valueOf(true) always returns the same object. (3) Can return a subtype — List.of() can return different internal implementations based on size. (4) Can reduce the verbosity of parameterized type creation (pre-diamond <>). Disadvantages: harder to discover (not in the Javadoc constructor section), can't be extended.","correct":"A static factory method is a static method that returns an instance of the class.","distractors":["Comparable<T> expresses the natural ordering — the single most obvious ordering for the class (e.g., String lexicographically, Integer numerically, LocalDate chronologically).","Three reasons: (1) Serialization-safe — Java's enum serialization mechanism guarantees that each constant is only deserialized to the existing instance (using the name, not constructor); no need for readResolve().","Serialization: implement readResolve() returning the singleton instance — the JVM calls this after deserialization and uses the returned object instead."],"distractorIds":["java/java_interview_patterns#68c2c7facbde","java/java_interview_patterns#4e61025d00f9","java/java_interview_patterns#55eb7811f52e"],"concepts":["advantages","constructors","effective","factory","static","item"],"answerFullMd":"A static factory method is a static method that returns an instance of the class. Advantages: (1) Named — `OptionalInt.empty()` is clearer than `new OptionalInt(false, 0)`. (2) Can return cached instances — `Boolean.valueOf(true)` always returns the same object. (3) Can return a subtype — `List.of()` can return different internal implementations based on size. (4) Can reduce the verbosity of parameterized type creation (pre-diamond `<>`). Disadvantages: harder to discover (not in the Javadoc constructor section), can't be extended.","distractorsMd":["`Comparable<T>` expresses the **natural ordering** — the single most obvious ordering for the class (e.g., `String` lexicographically, `Integer` numerically, `LocalDate` chronologically).","Three reasons: (1) Serialization-safe — Java's enum serialization mechanism guarantees that each constant is only deserialized to the existing instance (using the name, not constructor); no need for `readResolve()`.","Serialization: implement `readResolve()` returning the singleton instance — the JVM calls this after deserialization and uses the returned object instead."]},{"id":"java/java_interview_patterns#1b9a53d9a0c0","section":"java","module":"java/java_interview_patterns","moduleName":"java interview patterns","sourceFile":"README.md","difficulty":"advanced","question":"What does effectively final mean and why does Java require it?","answerFull":"A variable is effectively final if it's initialized once and never reassigned — it *behaves* like a final variable even without the keyword. Lambda expressions and anonymous classes can only capture effectively-final local variables. Reason: captured variables are copied (by value for primitives; by reference for objects) into the closure. If the original could change after capture, the lambda would hold a stale copy — confusing semantics. For mutable state in lambdas, use AtomicReference, AtomicInteger, or a single-element array as a container.","correct":"A variable is effectively final if it's initialized once and never reassigned — it *behaves* like a final variable even without the keyword.","distractors":["Five rules: (1) Declare the class final — prevents subclasses from adding mutable state.","Three reasons: (1) Serialization-safe — Java's enum serialization mechanism guarantees that each constant is only deserialized to the existing instance (using the name, not constructor); no need for readResolve().","A static factory method is a static method that returns an instance of the class."],"distractorIds":["java/java_interview_patterns#f5b94f668396","java/java_interview_patterns#4e61025d00f9","java/java_interview_patterns#c8f3a472742e"],"concepts":["behaves","reassigned","keyword","initialized","effectively","require"],"answerFullMd":"A variable is effectively final if it's initialized once and never reassigned — it *behaves* like a final variable even without the keyword. Lambda expressions and anonymous classes can only capture effectively-final local variables. Reason: captured variables are copied (by value for primitives; by reference for objects) into the closure. If the original could change after capture, the lambda would hold a stale copy — confusing semantics. For mutable state in lambdas, use `AtomicReference`, `AtomicInteger`, or a single-element array as a container.","distractorsMd":["Five rules: (1) Declare the class `final` — prevents subclasses from adding mutable state.","Three reasons: (1) Serialization-safe — Java's enum serialization mechanism guarantees that each constant is only deserialized to the existing instance (using the name, not constructor); no need for `readResolve()`.",null]},{"id":"java/java_interview_patterns#43eb220e52e6","section":"java","module":"java/java_interview_patterns","moduleName":"java interview patterns","sourceFile":"README.md","difficulty":"advanced","question":"What is null handling best practice in Java?","answerFull":"Three approaches: (1) Objects.requireNonNull(param, \"name\") in constructor/method — fails fast with a clear NPE message. (2) Return Optional<T> from methods that may have no result — forces callers to handle the absence case. (3) Use @NonNull/@Nullable annotations (Lombok, IntelliJ, JetBrains) for static analysis documentation. Avoid: passing null as a sentinel value — use Optional or overloading instead. Never swallow NullPointerException — it indicates a programming error that should be fixed.","correct":"Three approaches: (1) Objects.requireNonNull(param, \"name\") in constructor/method — fails fast with a clear NPE message.","distractors":["A static factory method is a static method that returns an instance of the class.","Integer.valueOf(n) caches Integer objects for values in [-128, 127].","Overriding: when a subclass defines an instance method with the same signature as the superclass."],"distractorIds":["java/java_interview_patterns#c8f3a472742e","java/java_interview_patterns#c37ee20d09ef","java/java_interview_patterns#b50ed6a1eda6"],"concepts":["npe","requirenonnull","practice","param","clear","handling"],"correctMd":"Three approaches: (1) `Objects.requireNonNull(param, \"name\")` in constructor/method — fails fast with a clear NPE message.","answerFullMd":"Three approaches: (1) `Objects.requireNonNull(param, \"name\")` in constructor/method — fails fast with a clear NPE message. (2) Return `Optional<T>` from methods that may have no result — forces callers to handle the absence case. (3) Use `@NonNull`/`@Nullable` annotations (Lombok, IntelliJ, JetBrains) for static analysis documentation. Avoid: passing `null` as a sentinel value — use `Optional` or overloading instead. Never swallow `NullPointerException` — it indicates a programming error that should be fixed.","distractorsMd":[null,"`Integer.valueOf(n)` caches `Integer` objects for values in [-128, 127].",null]},{"id":"java/java_interview_patterns#e69bb34955f8","section":"java","module":"java/java_interview_patterns","moduleName":"java interview patterns","sourceFile":"README.md","difficulty":"advanced","question":"What is the Integer cache, what is its exact range, and what common bug does it cause?","answerFull":"The JVM caches Integer objects for values −128 to 127 (inclusive) — a fixed pool of 256 instances created at JVM startup. Integer.valueOf(127) == Integer.valueOf(127) is true (same cached object). Integer.valueOf(128) == Integer.valueOf(128) is false (two distinct heap objects). This causes real bugs when developers compare autoboxed Integer values with == instead of .equals(): java // BROKEN: works for small values (returns from cache), fails for large ones Integer a = someService.getCount(); // e.g. returns 200 Integer b = otherService.getCount(); // also returns 200 if (a == b) { ... } // false! Different Integer objects above 127 // FIXED: always use .equals() for object comparison if (a.equals(b)) { ... } The same cache applies to Long (same range), Short (−128 to 127), Byte (entire range −128 to 127), and Character (0 to 127). The upper bound for Integer can be raised with -XX:AutoBoxCacheMax=N but this is rarely advisable. Practical rule: never use == for Integer, Long, Double, Boolean — always .equals().","correct":"The JVM caches Integer objects for values −128 to 127 (inclusive) — a fixed pool of 256 instances created at JVM startup.","distractors":["Three approaches: (1) Objects.requireNonNull(param, \"name\") in constructor/method — fails fast with a clear NPE message.","Serialization: implement readResolve() returning the singleton instance — the JVM calls this after deserialization and uses the returned object instead.","Integer.valueOf(n) caches Integer objects for values in [-128, 127]."],"distractorIds":["java/java_interview_patterns#43eb220e52e6","java/java_interview_patterns#55eb7811f52e","java/java_interview_patterns#c37ee20d09ef"],"concepts":["inclusive","127","bug","cause","integer","128"],"correctMd":"The JVM caches `Integer` objects for values −128 to 127 (inclusive) — a fixed pool of 256 instances created at JVM startup.","answerFullMd":"The JVM caches `Integer` objects for values −128 to 127 (inclusive) — a fixed pool of 256 instances created at JVM startup. `Integer.valueOf(127) == Integer.valueOf(127)` is `true` (same cached object). `Integer.valueOf(128) == Integer.valueOf(128)` is `false` (two distinct heap objects). This causes real bugs when developers compare autoboxed `Integer` values with `==` instead of `.equals()`: ```java // BROKEN: works for small values (returns from cache), fails for large ones Integer a = someService.getCount(); // e.g. returns 200 Integer b = otherService.getCount(); // also returns 200 if (a == b) { ... } // false! Different Integer objects above 127 // FIXED: always use .equals() for object comparison if (a.equals(b)) { ... } ``` The same cache applies to `Long` (same range), `Short` (−128 to 127), `Byte` (entire range −128 to 127), and `Character` (0 to 127). The upper bound for `Integer` can be raised with `-XX:AutoBoxCacheMax=N` but this is rarely advisable. Practical rule: never use `==` for `Integer`, `Long`, `Double`, `Boolean` — always `.equals()`.","distractorsMd":["Three approaches: (1) `Objects.requireNonNull(param, \"name\")` in constructor/method — fails fast with a clear NPE message.","Serialization: implement `readResolve()` returning the singleton instance — the JVM calls this after deserialization and uses the returned object instead.","`Integer.valueOf(n)` caches `Integer` objects for values in [-128, 127]."]},{"id":"java/java_interview_patterns#c754c73342d9","section":"java","module":"java/java_interview_patterns","moduleName":"java interview patterns","sourceFile":"README.md","difficulty":"advanced","question":"What is defensive copying and when is it required to maintain class immutability?","answerFull":"Defensive copying means creating a copy of a mutable input or output so the caller cannot affect the class's internal state through a shared reference. Required in two places: (1) constructor — if a mutable object is passed in (e.g., Date, byte[], List), copy it before storing; (2) getter — if you return a mutable field, return a copy so callers cannot mutate your state: java // BROKEN: immutable Money class leaking mutable Date field public final class Contract { private final Date signedOn; public Contract(Date d) { this.signedOn = d; } // no copy; caller retains reference public Date getSignedOn() { return signedOn; } // no copy; caller can mutate } // Caller: contract.getSignedOn().setTime(0); -> silently mutates Contract's date // FIXED: defensive copy in and out public Contract(Date d) { this.signedOn = new Date(d.getTime()); } // copy on the way in public Date getSignedOn() { return new Date(signedOn.getTime()); } // copy on the way out In modern Java, prefer Instant / LocalDate (immutable) over java.util.Date. For collections: use List.copyOf() (Java 10) or Collections.unmodifiableList(). Effective Java Item 50: *Make defensive copies when needed*.","correct":"Defensive copying means creating a copy of a mutable input or output so the caller cannot affect the class's internal state through a shared reference.","distractors":["Builder solves the \"telescoping constructor\" problem: when a class has many optional parameters, providing all combinations of constructor signatures is impractical.","Comparable<T> expresses the natural ordering — the single most obvious ordering for the class (e.g., String lexicographically, Integer numerically, LocalDate chronologically).","A static factory method is a static method that returns an instance of the class."],"distractorIds":["java/java_interview_patterns#772d53ace735","java/java_interview_patterns#68c2c7facbde","java/java_interview_patterns#c8f3a472742e"],"concepts":["defensive","immutability","affect","copying","caller","creating"],"answerFullMd":"Defensive copying means creating a copy of a mutable input or output so the caller cannot affect the class's internal state through a shared reference. Required in two places: (1) **constructor** — if a mutable object is passed in (e.g., `Date`, `byte[]`, `List`), copy it before storing; (2) **getter** — if you return a mutable field, return a copy so callers cannot mutate your state: ```java // BROKEN: immutable Money class leaking mutable Date field public final class Contract { private final Date signedOn; public Contract(Date d) { this.signedOn = d; } // no copy; caller retains reference public Date getSignedOn() { return signedOn; } // no copy; caller can mutate } // Caller: contract.getSignedOn().setTime(0); -> silently mutates Contract's date // FIXED: defensive copy in and out public Contract(Date d) { this.signedOn = new Date(d.getTime()); } // copy on the way in public Date getSignedOn() { return new Date(signedOn.getTime()); } // copy on the way out ``` In modern Java, prefer `Instant` / `LocalDate` (immutable) over `java.util.Date`. For collections: use `List.copyOf()` (Java 10) or `Collections.unmodifiableList()`. Effective Java Item 50: *Make defensive copies when needed*.","distractorsMd":[null,"`Comparable<T>` expresses the **natural ordering** — the single most obvious ordering for the class (e.g., `String` lexicographically, `Integer` numerically, `LocalDate` chronologically).",null]},{"id":"java/java_interview_patterns#68c2c7facbde","section":"java","module":"java/java_interview_patterns","moduleName":"java interview patterns","sourceFile":"README.md","difficulty":"advanced","question":"When should a class implement Comparable<T> vs. using an external Comparator<T>?","answerFull":"Comparable<T> expresses the natural ordering — the single most obvious ordering for the class (e.g., String lexicographically, Integer numerically, LocalDate chronologically). Classes that have a natural ordering should implement Comparable. When the ordering is contextual, situational, or you need multiple orderings (e.g., sort people by name OR by age OR by salary), use external Comparator instances. Key rule from Effective Java Item 14: *The natural ordering should be consistent with equals()* — a.compareTo(b) == 0 should imply a.equals(b). Violating this causes subtle bugs in TreeSet, TreeMap, and SortedSet (which use compareTo for equality, not equals). Example violation: BigDecimal(\"1.0\").compareTo(BigDecimal(\"1.00\")) == 0 but BigDecimal(\"1.0\").equals(BigDecimal(\"1.00\")) is false — a TreeSet treats them as the same element; a HashSet treats them as different.","correct":"Comparable<T> expresses the natural ordering — the single most obvious ordering for the class (e.g., String lexicographically, Integer numerically, LocalDate chronologically).","distractors":["Serialization: implement readResolve() returning the singleton instance — the JVM calls this after deserialization and uses the returned object instead.","The JVM caches Integer objects for values −128 to 127 (inclusive) — a fixed pool of 256 instances created at JVM startup.","Builder solves the \"telescoping constructor\" problem: when a class has many optional parameters, providing all combinations of constructor signatures is impractical."],"distractorIds":["java/java_interview_patterns#55eb7811f52e","java/java_interview_patterns#e69bb34955f8","java/java_interview_patterns#772d53ace735"],"concepts":["chronologically","obvious","comparator","lexicographically","localdate","numerically"],"questionMd":"When should a class implement `Comparable<T>` vs. using an external `Comparator<T>`?","correctMd":"`Comparable<T>` expresses the **natural ordering** — the single most obvious ordering for the class (e.g., `String` lexicographically, `Integer` numerically, `LocalDate` chronologically).","answerFullMd":"`Comparable<T>` expresses the **natural ordering** — the single most obvious ordering for the class (e.g., `String` lexicographically, `Integer` numerically, `LocalDate` chronologically). Classes that have a natural ordering should implement `Comparable`. When the ordering is contextual, situational, or you need multiple orderings (e.g., sort people by name OR by age OR by salary), use external `Comparator` instances. Key rule from Effective Java Item 14: *The natural ordering should be consistent with `equals()`* — `a.compareTo(b) == 0` should imply `a.equals(b)`. Violating this causes subtle bugs in `TreeSet`, `TreeMap`, and `SortedSet` (which use `compareTo` for equality, not `equals`). Example violation: `BigDecimal(\"1.0\").compareTo(BigDecimal(\"1.00\")) == 0` but `BigDecimal(\"1.0\").equals(BigDecimal(\"1.00\"))` is `false` — a `TreeSet` treats them as the same element; a `HashSet` treats them as different.","distractorsMd":["Serialization: implement `readResolve()` returning the singleton instance — the JVM calls this after deserialization and uses the returned object instead.","The JVM caches `Integer` objects for values −128 to 127 (inclusive) — a fixed pool of 256 instances created at JVM startup.",null]},{"id":"java/java_memory_model#c9837a235679","section":"java","module":"java/java_memory_model","moduleName":"java memory model","sourceFile":"README.md","difficulty":"core","question":"List all the happens-before edges in the JMM.","answerFull":"(1) Program order: each action in a thread hb every subsequent action in that thread. (2) Monitor unlock hb subsequent lock of same monitor. (3) Volatile write hb subsequent volatile read of same field. (4) Thread.start() hb any action in started thread. (5) Any action in thread hb Thread.join() returning. (6) Final field freeze: write to final field in constructor hb constructor completing (freeze action). (7) Thread.interrupt() hb interrupted thread detecting interrupt. (8) LockSupport.unpark(t) hb park returning in thread t. (9) Default initialization hb constructor. (10) Class <clinit> completion hb any thread accessing static fields. (11) Transitivity.","correct":"(1) Program order: each action in a thread hb every subsequent action in that thread.","distractors":["The JMM defines a \"freeze action\" for final fields: when a constructor completes (without this-escape), all writes to final fields in that constructor are frozen and published.","The JMM explicitly states that non-volatile reads and writes of long and double fields on 32-bit platforms can be treated as two separate 32-bit operations.","The StoreLoad barrier ensures that all stores before the barrier complete and become visible to all processors before any subsequent load executes."],"distractorIds":["java/java_memory_model#a2c8856deb8f","java/java_memory_model#1c963a4d438d","java/java_memory_model#025f5c8c2dcd"],"concepts":["jmm","program","subsequent","edges","happens","action"],"answerFullMd":"(1) Program order: each action in a thread hb every subsequent action in that thread. (2) Monitor unlock hb subsequent lock of same monitor. (3) Volatile write hb subsequent volatile read of same field. (4) `Thread.start()` hb any action in started thread. (5) Any action in thread hb `Thread.join()` returning. (6) Final field freeze: write to final field in constructor hb constructor completing (freeze action). (7) `Thread.interrupt()` hb interrupted thread detecting interrupt. (8) `LockSupport.unpark(t)` hb park returning in thread t. (9) Default initialization hb constructor. (10) Class `<clinit>` completion hb any thread accessing static fields. (11) Transitivity.","distractorsMd":["The JMM defines a \"freeze action\" for `final` fields: when a constructor completes (without this-escape), all writes to `final` fields in that constructor are frozen and published.","The JMM explicitly states that non-volatile reads and writes of `long` and `double` fields on 32-bit platforms can be treated as two separate 32-bit operations.","The `StoreLoad` barrier ensures that all stores before the barrier complete and become visible to all processors before any subsequent load executes."]},{"id":"java/java_memory_model#a2c8856deb8f","section":"java","module":"java/java_memory_model","moduleName":"java memory model","sourceFile":"README.md","difficulty":"core","question":"Why are final fields safe without synchronization after the constructor completes?","answerFull":"The JMM defines a \"freeze action\" for final fields: when a constructor completes (without this-escape), all writes to final fields in that constructor are frozen and published. Any thread that reads the reference of the fully constructed object is guaranteed to see all final fields at their initialized values — even without any synchronization. This is why immutable objects are thread-safe: all their state is in final fields, established before any thread can read them. The guarantee breaks if the constructor publishes this before completing — the freeze action hasn't happened yet.","correct":"The JMM defines a \"freeze action\" for final fields: when a constructor completes (without this-escape), all writes to final fields in that constructor are frozen and published.","distractors":["x86 uses a strong memory model (Total Store Order) that preserves most store/load ordering, so many JMM-violating data races still happen to produce the expected result on that hardware.","The freeze action is the JMM mechanism for final field safety.","The JMM explicitly states that non-volatile reads and writes of long and double fields on 32-bit platforms can be treated as two separate 32-bit operations."],"distractorIds":["java/java_memory_model#8f620debb211","java/java_memory_model#27eab37076ec","java/java_memory_model#1c963a4d438d"],"concepts":["synchronization","freeze","escape","jmm","frozen","published"],"questionMd":"Why are `final` fields safe without synchronization after the constructor completes?","correctMd":"The JMM defines a \"freeze action\" for `final` fields: when a constructor completes (without this-escape), all writes to `final` fields in that constructor are frozen and published.","answerFullMd":"The JMM defines a \"freeze action\" for `final` fields: when a constructor completes (without this-escape), all writes to `final` fields in that constructor are frozen and published. Any thread that reads the reference of the fully constructed object is guaranteed to see all final fields at their initialized values — even without any synchronization. This is why immutable objects are thread-safe: all their state is in `final` fields, established before any thread can read them. The guarantee breaks if the constructor publishes `this` before completing — the freeze action hasn't happened yet.","distractorsMd":[null,null,"The JMM explicitly states that non-volatile reads and writes of `long` and `double` fields on 32-bit platforms can be treated as two separate 32-bit operations."]},{"id":"java/java_memory_model#b7a975a8ba5f","section":"java","module":"java/java_memory_model","moduleName":"java memory model","sourceFile":"README.md","difficulty":"core","question":"What is a data race vs a race condition?","answerFull":"A data race is a specific JMM-level violation: two concurrent accesses to the same variable where at least one is a write, and there is no happens-before ordering between them. Data races put the program in undefined behavior territory — the JMM makes no guarantees. A race condition is a logical concurrency bug: the program produces incorrect results due to timing dependencies, even though operations may individually be atomic. Example: two threads both check if (balance > 0) and both decrement — each decrement is atomic, but the check-then-act as a whole is not. Race conditions can exist even in perfectly synchronized code (though they're harder to create).","correct":"A data race is a specific JMM-level violation: two concurrent accesses to the same variable where at least one is a write, and there is no happens-before ordering between them.","distractors":["The JMM guarantees a read can never observe a value that no thread ever wrote — no value can be conjured from nowhere, even under a data race.","The JMM explicitly states that non-volatile reads and writes of long and double fields on 32-bit platforms can be treated as two separate 32-bit operations.","VarHandle (Java 9, java.lang.invoke.VarHandles) provides field access with explicit memory ordering modes: (1) Plain — no ordering guarantees, same as non-volatile access."],"distractorIds":["java/java_memory_model#e244c5c859b5","java/java_memory_model#1c963a4d438d","java/java_memory_model#b83b0003997e"],"concepts":["accesses","race","jmm","violation","condition","happens"],"answerFullMd":"A data race is a specific JMM-level violation: two concurrent accesses to the same variable where at least one is a write, and there is no happens-before ordering between them. Data races put the program in undefined behavior territory — the JMM makes no guarantees. A race condition is a logical concurrency bug: the program produces incorrect results due to timing dependencies, even though operations may individually be atomic. Example: two threads both check `if (balance > 0)` and both decrement — each decrement is atomic, but the check-then-act as a whole is not. Race conditions can exist even in perfectly synchronized code (though they're harder to create).","distractorsMd":[null,"The JMM explicitly states that non-volatile reads and writes of `long` and `double` fields on 32-bit platforms can be treated as two separate 32-bit operations.","`VarHandle` (Java 9, `java.lang.invoke.VarHandles`) provides field access with explicit memory ordering modes: (1) Plain — no ordering guarantees, same as non-volatile access."]},{"id":"java/java_memory_model#1c963a4d438d","section":"java","module":"java/java_memory_model","moduleName":"java memory model","sourceFile":"README.md","difficulty":"core","question":"Why is volatile long needed on 32-bit JVMs?","answerFull":"The JMM explicitly states that non-volatile reads and writes of long and double fields on 32-bit platforms can be treated as two separate 32-bit operations. This means a 64-bit write from Thread A could be split: Thread B reads the high 32 bits from the new value and the low 32 bits from the old value — producing a completely incorrect 64-bit result (\"word tear\"). volatile long is required to guarantee atomicity of 64-bit operations. On 64-bit x86, non-volatile long is atomically written by hardware (single 64-bit MOV), but this is not a JMM guarantee and should not be relied upon for portability.","correct":"The JMM explicitly states that non-volatile reads and writes of long and double fields on 32-bit platforms can be treated as two separate 32-bit operations.","distractors":["The JMM defines a \"freeze action\" for final fields: when a constructor completes (without this-escape), all writes to final fields in that constructor are frozen and published.","The freeze action is the JMM mechanism for final field safety.","The JMM's \"freeze action\" for final fields: when an object's constructor completes normally, a JMM freeze action fires for every final field written in that constructor."],"distractorIds":["java/java_memory_model#a2c8856deb8f","java/java_memory_model#27eab37076ec","java/java_memory_model#dd51246bd873"],"concepts":["jvms","platforms","jmm","volatile","double","treated"],"questionMd":"Why is `volatile long` needed on 32-bit JVMs?","correctMd":"The JMM explicitly states that non-volatile reads and writes of `long` and `double` fields on 32-bit platforms can be treated as two separate 32-bit operations.","answerFullMd":"The JMM explicitly states that non-volatile reads and writes of `long` and `double` fields on 32-bit platforms can be treated as two separate 32-bit operations. This means a 64-bit write from Thread A could be split: Thread B reads the high 32 bits from the new value and the low 32 bits from the old value — producing a completely incorrect 64-bit result (\"word tear\"). `volatile long` is required to guarantee atomicity of 64-bit operations. On 64-bit x86, non-volatile long is atomically written by hardware (single 64-bit MOV), but this is not a JMM guarantee and should not be relied upon for portability.","distractorsMd":["The JMM defines a \"freeze action\" for `final` fields: when a constructor completes (without this-escape), all writes to `final` fields in that constructor are frozen and published.",null,"The JMM's \"freeze action\" for `final` fields: when an object's constructor completes normally, a JMM freeze action fires for every `final` field written in that constructor."]},{"id":"java/java_memory_model#f860fee3130a","section":"java","module":"java/java_memory_model","moduleName":"java memory model","sourceFile":"README.md","difficulty":"intermediate","question":"Can final fields be observed in inconsistent state? Explain the this-escape caveat.","answerFull":"Yes — if the constructor publishes this before completing. The freeze action for final fields occurs when the constructor exits normally. If the constructor calls a method on another object (or registers this with an observer) before setting all final fields, that other object can see final fields at their default values (0 or null). This is called \"this-escape from constructor.\" Example: EventListener(EventBus bus) { bus.register(this); this.id = 42; } — any listener receiving this reference before the constructor finishes sees id == 0. Fix: use a static factory method that constructs the object completely before publishing.","correct":"Yes — if the constructor publishes this before completing.","distractors":["The JMM explicitly states that non-volatile reads and writes of long and double fields on 32-bit platforms can be treated as two separate 32-bit operations.","The freeze action is the JMM mechanism for final field safety.","(1) Static field initialized in declaration (static final Foo f = new Foo()) — class <clinit> provides hb for all threads, zero runtime overhead."],"distractorIds":["java/java_memory_model#1c963a4d438d","java/java_memory_model#27eab37076ec","java/java_memory_model#610f9a3dd574"],"concepts":["completing","inconsistent","explain","escape","publishes","observed"],"questionMd":"Can `final` fields be observed in inconsistent state? Explain the `this`-escape caveat.","correctMd":"Yes — if the constructor publishes `this` before completing.","answerFullMd":"Yes — if the constructor publishes `this` before completing. The freeze action for `final` fields occurs when the constructor exits normally. If the constructor calls a method on another object (or registers `this` with an observer) before setting all `final` fields, that other object can see `final` fields at their default values (0 or null). This is called \"this-escape from constructor.\" Example: `EventListener(EventBus bus) { bus.register(this); this.id = 42; }` — any listener receiving this reference before the constructor finishes sees `id == 0`. Fix: use a static factory method that constructs the object completely before publishing.","distractorsMd":["The JMM explicitly states that non-volatile reads and writes of `long` and `double` fields on 32-bit platforms can be treated as two separate 32-bit operations.",null,"(1) Static field initialized in declaration (`static final Foo f = new Foo()`) — class `<clinit>` provides hb for all threads, zero runtime overhead."]},{"id":"java/java_memory_model#27eab37076ec","section":"java","module":"java/java_memory_model","moduleName":"java memory model","sourceFile":"README.md","difficulty":"intermediate","question":"What is the freeze action?","answerFull":"The freeze action is the JMM mechanism for final field safety. It occurs at the end of the constructor (or just before it exits, assuming no this-escape). The freeze action creates a happens-before edge from all writes to final fields in the constructor to any subsequent read of those fields by any thread. This is what makes final fields safe without synchronization: after the freeze action, the initialized values of final fields are visible to all threads. Normal (non-final) fields in the same constructor are NOT covered by the freeze action.","correct":"The freeze action is the JMM mechanism for final field safety.","distractors":["(1) Static field initialized in declaration (static final Foo f = new Foo()) — class <clinit> provides hb for all threads, zero runtime overhead.","The JMM explicitly states that non-volatile reads and writes of long and double fields on 32-bit platforms can be treated as two separate 32-bit operations.","x86 uses a strong memory model (Total Store Order) that preserves most store/load ordering, so many JMM-violating data races still happen to produce the expected result on that hardware."],"distractorIds":["java/java_memory_model#610f9a3dd574","java/java_memory_model#1c963a4d438d","java/java_memory_model#8f620debb211"],"concepts":["freeze","jmm","safety","action","mechanism","final"],"answerFullMd":"The freeze action is the JMM mechanism for final field safety. It occurs at the end of the constructor (or just before it exits, assuming no this-escape). The freeze action creates a happens-before edge from all writes to `final` fields in the constructor to any subsequent read of those fields by any thread. This is what makes final fields safe without synchronization: after the freeze action, the initialized values of final fields are visible to all threads. Normal (non-final) fields in the same constructor are NOT covered by the freeze action.","distractorsMd":["(1) Static field initialized in declaration (`static final Foo f = new Foo()`) — class `<clinit>` provides hb for all threads, zero runtime overhead.","The JMM explicitly states that non-volatile reads and writes of `long` and `double` fields on 32-bit platforms can be treated as two separate 32-bit operations.",null]},{"id":"java/java_memory_model#adcc371fee10","section":"java","module":"java/java_memory_model","moduleName":"java memory model","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between memory visibility and instruction ordering?","answerFull":"Memory visibility: whether a write by one thread is visible to a read by another thread. Caused by CPU caches — a write may be buffered in the writer's cache and not flushed to shared memory. Instruction ordering: the order in which a thread executes its own instructions — the JIT compiler and CPU can reorder instructions for optimization. Both problems are addressed by happens-before. A happens-before edge provides both: it ensures (1) all writes up to the edge point are flushed to shared memory (visibility), and (2) all those writes appear in order relative to the synchronized read (ordering). volatile provides both; the x86 TSO memory model provides strong ordering but weak visibility guarantees without barriers.","correct":"Memory visibility: whether a write by one thread is visible to a read by another thread.","distractors":["A data race is a specific JMM-level violation: two concurrent accesses to the same variable where at least one is a write, and there is no happens-before ordering between them.","The StoreLoad barrier ensures that all stores before the barrier complete and become visible to all processors before any subsequent load executes.","synchronized guarantees three things: (1) Mutual exclusion — only one thread holds the monitor at a time."],"distractorIds":["java/java_memory_model#b7a975a8ba5f","java/java_memory_model#025f5c8c2dcd","java/java_memory_model#b21b69a988d3"],"concepts":["difference","visibility","instruction","ordering","visible","another"],"answerFullMd":"Memory visibility: whether a write by one thread is visible to a read by another thread. Caused by CPU caches — a write may be buffered in the writer's cache and not flushed to shared memory. Instruction ordering: the order in which a thread executes its own instructions — the JIT compiler and CPU can reorder instructions for optimization. Both problems are addressed by happens-before. A happens-before edge provides both: it ensures (1) all writes up to the edge point are flushed to shared memory (visibility), and (2) all those writes appear in order relative to the synchronized read (ordering). `volatile` provides both; the x86 TSO memory model provides strong ordering but weak visibility guarantees without barriers.","distractorsMd":[null,"The `StoreLoad` barrier ensures that all stores before the barrier complete and become visible to all processors before any subsequent load executes.","`synchronized` guarantees three things: (1) Mutual exclusion — only one thread holds the monitor at a time."]},{"id":"java/java_memory_model#b21b69a988d3","section":"java","module":"java/java_memory_model","moduleName":"java memory model","sourceFile":"README.md","difficulty":"intermediate","question":"What does synchronized guarantee beyond mutual exclusion?","answerFull":"synchronized guarantees three things: (1) Mutual exclusion — only one thread holds the monitor at a time. (2) Visibility — all writes performed before releasing the lock are visible to any thread that subsequently acquires the same lock. This is the monitor unlock → subsequent lock hb edge. (3) Ordering — actions before the monitor exit are not reordered to appear after it, and actions after monitor entry are not reordered to appear before it (acquire/release semantics). So synchronized also acts as a memory barrier: reading a synchronized field is guaranteed to see the most recent synchronized write, even on weakly-ordered architectures.","correct":"synchronized guarantees three things: (1) Mutual exclusion — only one thread holds the monitor at a time.","distractors":["VarHandle (Java 9, java.lang.invoke.VarHandles) provides field access with explicit memory ordering modes: (1) Plain — no ordering guarantees, same as non-volatile access.","The JMM guarantees a read can never observe a value that no thread ever wrote — no value can be conjured from nowhere, even under a data race.","Memory visibility: whether a write by one thread is visible to a read by another thread."],"distractorIds":["java/java_memory_model#b83b0003997e","java/java_memory_model#e244c5c859b5","java/java_memory_model#adcc371fee10"],"concepts":["exclusion","things","mutual","synchronized","monitor","beyond"],"questionMd":"What does `synchronized` guarantee beyond mutual exclusion?","correctMd":"`synchronized` guarantees three things: (1) Mutual exclusion — only one thread holds the monitor at a time.","answerFullMd":"`synchronized` guarantees three things: (1) Mutual exclusion — only one thread holds the monitor at a time. (2) Visibility — all writes performed before releasing the lock are visible to any thread that subsequently acquires the same lock. This is the monitor unlock → subsequent lock hb edge. (3) Ordering — actions before the monitor exit are not reordered to appear after it, and actions after monitor entry are not reordered to appear before it (acquire/release semantics). So `synchronized` also acts as a memory barrier: reading a synchronized field is guaranteed to see the most recent synchronized write, even on weakly-ordered architectures.","distractorsMd":["`VarHandle` (Java 9, `java.lang.invoke.VarHandles`) provides field access with explicit memory ordering modes: (1) Plain — no ordering guarantees, same as non-volatile access.",null,null]},{"id":"java/java_memory_model#b83b0003997e","section":"java","module":"java/java_memory_model","moduleName":"java memory model","sourceFile":"README.md","difficulty":"intermediate","question":"What is VarHandle and how does it provide finer-grained memory ordering than volatile?","answerFull":"VarHandle (Java 9, java.lang.invoke.VarHandles) provides field access with explicit memory ordering modes: (1) Plain — no ordering guarantees, same as non-volatile access. (2) Opaque — no cross-variable ordering but each individual access is atomic. (3) Acquire/Release — one-way barriers: acquire (read) sees all writes by the release (write), but without the full StoreLoad barrier of volatile. (4) Volatile — full volatile semantics (most expensive). This lets lock-free algorithms pay only for the barriers they actually need — acquire/release is sufficient for a producer-consumer handoff but much cheaper than full volatile StoreLoad on most architectures.","correct":"VarHandle (Java 9, java.lang.invoke.VarHandles) provides field access with explicit memory ordering modes: (1) Plain — no ordering guarantees, same as non-volatile access.","distractors":["jcstress (Java Concurrency Stress Test) is an OpenJDK harness for testing memory model outcomes.","The JMM explicitly states that non-volatile reads and writes of long and double fields on 32-bit platforms can be treated as two separate 32-bit operations.","synchronized guarantees three things: (1) Mutual exclusion — only one thread holds the monitor at a time."],"distractorIds":["java/java_memory_model#bac7447fe90d","java/java_memory_model#1c963a4d438d","java/java_memory_model#b21b69a988d3"],"concepts":["finer","varhandle","varhandles","grained","invoke","lang"],"questionMd":"What is `VarHandle` and how does it provide finer-grained memory ordering than `volatile`?","correctMd":"`VarHandle` (Java 9, `java.lang.invoke.VarHandles`) provides field access with explicit memory ordering modes: (1) Plain — no ordering guarantees, same as non-volatile access.","answerFullMd":"`VarHandle` (Java 9, `java.lang.invoke.VarHandles`) provides field access with explicit memory ordering modes: (1) Plain — no ordering guarantees, same as non-volatile access. (2) Opaque — no cross-variable ordering but each individual access is atomic. (3) Acquire/Release — one-way barriers: acquire (read) sees all writes by the release (write), but without the full StoreLoad barrier of volatile. (4) Volatile — full volatile semantics (most expensive). This lets lock-free algorithms pay only for the barriers they actually need — acquire/release is sufficient for a producer-consumer handoff but much cheaper than full volatile StoreLoad on most architectures.","distractorsMd":["`jcstress` (Java Concurrency Stress Test) is an OpenJDK harness for testing memory model outcomes.","The JMM explicitly states that non-volatile reads and writes of `long` and `double` fields on 32-bit platforms can be treated as two separate 32-bit operations.","`synchronized` guarantees three things: (1) Mutual exclusion — only one thread holds the monitor at a time."]},{"id":"java/java_memory_model#610f9a3dd574","section":"java","module":"java/java_memory_model","moduleName":"java memory model","sourceFile":"README.md","difficulty":"advanced","question":"What are the safe publication idioms in order of preference?","answerFull":"(1) Static field initialized in declaration (static final Foo f = new Foo()) — class <clinit> provides hb for all threads, zero runtime overhead. (2) final fields in constructor — freeze action guarantees visibility; works for immutable objects. (3) volatile field — volatile write hb subsequent read; suitable for mutable references. (4) Properly locked field — monitor unlock hb subsequent lock; necessary when multiple related fields must be consistent. (5) AtomicReference — CAS-based, volatile semantics on the reference. Order reflects preference: most restrictive (static final) provides strongest guarantees and is hardest to misuse.","correct":"(1) Static field initialized in declaration (static final Foo f = new Foo()) — class <clinit> provides hb for all threads, zero runtime overhead.","distractors":["The JMM defines a \"freeze action\" for final fields: when a constructor completes (without this-escape), all writes to final fields in that constructor are frozen and published.","The freeze action is the JMM mechanism for final field safety.","x86 uses a strong memory model (Total Store Order) that preserves most store/load ordering, so many JMM-violating data races still happen to produce the expected result on that hardware."],"distractorIds":["java/java_memory_model#a2c8856deb8f","java/java_memory_model#27eab37076ec","java/java_memory_model#8f620debb211"],"concepts":["clinit","publication","declaration","foo","initialized","preference"],"correctMd":"(1) Static field initialized in declaration (`static final Foo f = new Foo()`) — class `<clinit>` provides hb for all threads, zero runtime overhead.","answerFullMd":"(1) Static field initialized in declaration (`static final Foo f = new Foo()`) — class `<clinit>` provides hb for all threads, zero runtime overhead. (2) `final` fields in constructor — freeze action guarantees visibility; works for immutable objects. (3) `volatile` field — volatile write hb subsequent read; suitable for mutable references. (4) Properly locked field — monitor unlock hb subsequent lock; necessary when multiple related fields must be consistent. (5) `AtomicReference` — CAS-based, `volatile` semantics on the reference. Order reflects preference: most restrictive (static final) provides strongest guarantees and is hardest to misuse.","distractorsMd":["The JMM defines a \"freeze action\" for `final` fields: when a constructor completes (without this-escape), all writes to `final` fields in that constructor are frozen and published.",null,null]},{"id":"java/java_memory_model#025f5c8c2dcd","section":"java","module":"java/java_memory_model","moduleName":"java memory model","sourceFile":"README.md","difficulty":"advanced","question":"What is the StoreLoad barrier and why is volatile write the most expensive operation?","answerFull":"The StoreLoad barrier ensures that all stores before the barrier complete and become visible to all processors before any subsequent load executes. It is the only barrier that prevents store-load reordering — the most significant optimization a processor can do. On x86, StoreLoad requires an MFENCE or a locked instruction, which forces a full memory serialization point — all store buffers are flushed. This is why volatile write is expensive: it requires StoreStore (flush preceding stores) + StoreLoad (flush this store and prevent subsequent loads from reordering before it). Volatile read requires only LoadLoad + LoadStore, which are effectively free on x86.","correct":"The StoreLoad barrier ensures that all stores before the barrier complete and become visible to all processors before any subsequent load executes.","distractors":["volatile only guarantees visibility and ordering for individual reads and writes — it does not make a compound read-modify-write operation atomic.","A data race is a specific JMM-level violation: two concurrent accesses to the same variable where at least one is a write, and there is no happens-before ordering between them.","(1) Program order: each action in a thread hb every subsequent action in that thread."],"distractorIds":["java/java_memory_model#d3bfbfa77789","java/java_memory_model#b7a975a8ba5f","java/java_memory_model#c9837a235679"],"concepts":["storeload","barrier","processors","volatile","become","ensures"],"correctMd":"The `StoreLoad` barrier ensures that all stores before the barrier complete and become visible to all processors before any subsequent load executes.","answerFullMd":"The `StoreLoad` barrier ensures that all stores before the barrier complete and become visible to all processors before any subsequent load executes. It is the only barrier that prevents store-load reordering — the most significant optimization a processor can do. On x86, `StoreLoad` requires an `MFENCE` or a locked instruction, which forces a full memory serialization point — all store buffers are flushed. This is why volatile write is expensive: it requires `StoreStore` (flush preceding stores) + `StoreLoad` (flush this store and prevent subsequent loads from reordering before it). Volatile read requires only `LoadLoad` + `LoadStore`, which are effectively free on x86.","distractorsMd":["`volatile` only guarantees visibility and ordering for individual reads and writes — it does not make a compound read-modify-write operation atomic.",null,null]},{"id":"java/java_memory_model#dd51246bd873","section":"java","module":"java/java_memory_model","moduleName":"java memory model","sourceFile":"README.md","difficulty":"advanced","question":"How does final field freezing provide visibility guarantees, and how does it differ from volatile?","answerFull":"The JMM's \"freeze action\" for final fields: when an object's constructor completes normally, a JMM freeze action fires for every final field written in that constructor. Any thread that obtains a reference to the fully-constructed object is guaranteed to see the final field values as written — this is a strong publication guarantee with zero runtime barrier cost after construction. Contrast with volatile: volatile guarantees visibility on every read and write (runtime barrier), suitable for fields that change after construction. final provides stronger guarantees but only for fields set once in the constructor. Caveat: the guarantee only holds if this does not escape the constructor — if the constructor passes this to another thread, the freeze action has not yet fired when that thread accesses the final fields.","correct":"The JMM's \"freeze action\" for final fields: when an object's constructor completes normally, a JMM freeze action fires for every final field written in that constructor.","distractors":["The JMM guarantees a read can never observe a value that no thread ever wrote — no value can be conjured from nowhere, even under a data race.","The JMM explicitly states that non-volatile reads and writes of long and double fields on 32-bit platforms can be treated as two separate 32-bit operations.","VarHandle (Java 9, java.lang.invoke.VarHandles) provides field access with explicit memory ordering modes: (1) Plain — no ordering guarantees, same as non-volatile access."],"distractorIds":["java/java_memory_model#e244c5c859b5","java/java_memory_model#1c963a4d438d","java/java_memory_model#b83b0003997e"],"concepts":["freezing","freeze","jmm","visibility","volatile","fires"],"questionMd":"How does `final` field freezing provide visibility guarantees, and how does it differ from `volatile`?","correctMd":"The JMM's \"freeze action\" for `final` fields: when an object's constructor completes normally, a JMM freeze action fires for every `final` field written in that constructor.","answerFullMd":"The JMM's \"freeze action\" for `final` fields: when an object's constructor completes normally, a JMM freeze action fires for every `final` field written in that constructor. Any thread that obtains a reference to the fully-constructed object is guaranteed to see the `final` field values as written — this is a strong publication guarantee with **zero runtime barrier cost** after construction. Contrast with `volatile`: `volatile` guarantees visibility on every read and write (runtime barrier), suitable for fields that change after construction. `final` provides stronger guarantees but only for fields set once in the constructor. Caveat: the guarantee only holds if `this` does not escape the constructor — if the constructor passes `this` to another thread, the freeze action has not yet fired when that thread accesses the `final` fields.","distractorsMd":[null,"The JMM explicitly states that non-volatile reads and writes of `long` and `double` fields on 32-bit platforms can be treated as two separate 32-bit operations.","`VarHandle` (Java 9, `java.lang.invoke.VarHandles`) provides field access with explicit memory ordering modes: (1) Plain — no ordering guarantees, same as non-volatile access."]},{"id":"java/java_memory_model#bac7447fe90d","section":"java","module":"java/java_memory_model","moduleName":"java memory model","sourceFile":"README.md","difficulty":"advanced","question":"What is jcstress and what category of JMM violations does it catch that JUnit tests cannot?","answerFull":"jcstress (Java Concurrency Stress Test) is an OpenJDK harness for testing memory model outcomes. It runs tests in tight concurrent loops on many CPU cores with various thread interleavings to probe for JMM violations. Unlike JUnit tests, which run sequentially or with controlled Thread.join() synchronization, jcstress instruments the runtime to observe all possible outcomes of racy concurrent accesses — including outcomes that only manifest on weakly ordered architectures (ARM, Power) but not on x86 (which enforces strong ordering beyond what the JMM requires). It categorises outcomes as: ACCEPTABLE (always valid), FORBIDDEN (never valid under JMM), INTERESTING (valid but surprising). Key uses: (1) Verify that a lock-free data structure is correct under all possible execution orders. (2) Prove that a specific use of volatile or VarHandle establishes the required happens-before edge. (3) Detect torn reads of long/double on 32-bit platforms.","correct":"jcstress (Java Concurrency Stress Test) is an OpenJDK harness for testing memory model outcomes.","distractors":["VarHandle (Java 9, java.lang.invoke.VarHandles) provides field access with explicit memory ordering modes: (1) Plain — no ordering guarantees, same as non-volatile access.","A data race is a specific JMM-level violation: two concurrent accesses to the same variable where at least one is a write, and there is no happens-before ordering between them.","The JMM guarantees a read can never observe a value that no thread ever wrote — no value can be conjured from nowhere, even under a data race."],"distractorIds":["java/java_memory_model#b83b0003997e","java/java_memory_model#b7a975a8ba5f","java/java_memory_model#e244c5c859b5"],"concepts":["jcstress","openjdk","stress","harness","outcomes","violations"],"questionMd":"What is `jcstress` and what category of JMM violations does it catch that JUnit tests cannot?","correctMd":"`jcstress` (Java Concurrency Stress Test) is an OpenJDK harness for testing memory model outcomes.","answerFullMd":"`jcstress` (Java Concurrency Stress Test) is an OpenJDK harness for testing memory model outcomes. It runs tests in tight concurrent loops on many CPU cores with various thread interleavings to probe for JMM violations. Unlike JUnit tests, which run sequentially or with controlled `Thread.join()` synchronization, `jcstress` instruments the runtime to observe all possible outcomes of racy concurrent accesses — including outcomes that only manifest on weakly ordered architectures (ARM, Power) but not on x86 (which enforces strong ordering beyond what the JMM requires). It categorises outcomes as: `ACCEPTABLE` (always valid), `FORBIDDEN` (never valid under JMM), `INTERESTING` (valid but surprising). Key uses: (1) Verify that a lock-free data structure is correct under all possible execution orders. (2) Prove that a specific use of `volatile` or `VarHandle` establishes the required happens-before edge. (3) Detect torn reads of `long`/`double` on 32-bit platforms.","distractorsMd":["`VarHandle` (Java 9, `java.lang.invoke.VarHandles`) provides field access with explicit memory ordering modes: (1) Plain — no ordering guarantees, same as non-volatile access.",null,null]},{"id":"java/java_memory_model#d3bfbfa77789","section":"java","module":"java/java_memory_model","moduleName":"java memory model","sourceFile":"README.md","difficulty":"advanced","question":"Why doesn't volatile make count++ thread-safe, and what fixes it?","answerFull":"volatile only guarantees visibility and ordering for individual reads and writes — it does not make a compound read-modify-write operation atomic. count++ is actually three separate steps: read count, add 1, write the result back; two threads can both read the same value before either writes, so one increment is silently lost even though every individual read and write of count was immediately visible to both threads. This is exactly why volatile is correct for a single-writer flag or reference but wrong for a shared counter or any check-then-act sequence. Fix: use AtomicInteger/AtomicLong (CAS-based atomicity), LongAdder for high-contention counters, or wrap the increment in synchronized.","correct":"volatile only guarantees visibility and ordering for individual reads and writes — it does not make a compound read-modify-write operation atomic.","distractors":["synchronized guarantees three things: (1) Mutual exclusion — only one thread holds the monitor at a time.","A data race is a specific JMM-level violation: two concurrent accesses to the same variable where at least one is a write, and there is no happens-before ordering between them.","Memory visibility: whether a write by one thread is visible to a read by another thread."],"distractorIds":["java/java_memory_model#b21b69a988d3","java/java_memory_model#b7a975a8ba5f","java/java_memory_model#adcc371fee10"],"concepts":["fixes","compound","modify","visibility","volatile","doesn"],"questionMd":"Why doesn't `volatile` make `count++` thread-safe, and what fixes it?","correctMd":"`volatile` only guarantees visibility and ordering for individual reads and writes — it does not make a compound read-modify-write operation atomic.","answerFullMd":"`volatile` only guarantees visibility and ordering for individual reads and writes — it does not make a compound read-modify-write operation atomic. `count++` is actually three separate steps: read `count`, add 1, write the result back; two threads can both read the same value before either writes, so one increment is silently lost even though every individual read and write of `count` was immediately visible to both threads. This is exactly why `volatile` is correct for a single-writer flag or reference but wrong for a shared counter or any check-then-act sequence. Fix: use `AtomicInteger`/`AtomicLong` (CAS-based atomicity), `LongAdder` for high-contention counters, or wrap the increment in `synchronized`.","distractorsMd":["`synchronized` guarantees three things: (1) Mutual exclusion — only one thread holds the monitor at a time.",null,null]},{"id":"java/java_memory_model#e244c5c859b5","section":"java","module":"java/java_memory_model","moduleName":"java memory model","sourceFile":"README.md","difficulty":"advanced","question":"What does \"no out-of-thin-air values\" mean in the JMM, and why does it matter even for racy code?","answerFull":"The JMM guarantees a read can never observe a value that no thread ever wrote — no value can be conjured from nowhere, even under a data race. This matters because without it, an aggressively optimizing JIT or CPU could justify almost any reordering or speculative result as \"technically allowed\" once a race exists; the out-of-thin-air rule draws a hard boundary so racy, undefined-looking executions still remain traceable to real writes somewhere in the program. A classic example: two threads race to write different constants into the same int field — the JMM permits the reader to see either constant, or even an interleaved bit pattern on some hardware, but never a third value neither thread ever assigned. This guarantee doesn't make data races safe to write — it only stops the runtime from making the bug arbitrarily worse — so still eliminate the race with volatile, locks, or atomics rather than relying on this bound.","correct":"The JMM guarantees a read can never observe a value that no thread ever wrote — no value can be conjured from nowhere, even under a data race.","distractors":["The JMM defines a \"freeze action\" for final fields: when a constructor completes (without this-escape), all writes to final fields in that constructor are frozen and published.","The JMM explicitly states that non-volatile reads and writes of long and double fields on 32-bit platforms can be treated as two separate 32-bit operations.","A data race is a specific JMM-level violation: two concurrent accesses to the same variable where at least one is a write, and there is no happens-before ordering between them."],"distractorIds":["java/java_memory_model#a2c8856deb8f","java/java_memory_model#1c963a4d438d","java/java_memory_model#b7a975a8ba5f"],"concepts":["conjured","nowhere","thin","wrote","matter","race"],"answerFullMd":"The JMM guarantees a read can never observe a value that no thread ever wrote — no value can be conjured from nowhere, even under a data race. This matters because without it, an aggressively optimizing JIT or CPU could justify almost any reordering or speculative result as \"technically allowed\" once a race exists; the out-of-thin-air rule draws a hard boundary so racy, undefined-looking executions still remain traceable to real writes somewhere in the program. A classic example: two threads race to write different constants into the same `int` field — the JMM permits the reader to see either constant, or even an interleaved bit pattern on some hardware, but never a third value neither thread ever assigned. This guarantee doesn't make data races safe to write — it only stops the runtime from making the bug arbitrarily worse — so still eliminate the race with `volatile`, locks, or atomics rather than relying on this bound.","distractorsMd":["The JMM defines a \"freeze action\" for `final` fields: when a constructor completes (without this-escape), all writes to `final` fields in that constructor are frozen and published.","The JMM explicitly states that non-volatile reads and writes of `long` and `double` fields on 32-bit platforms can be treated as two separate 32-bit operations.",null]},{"id":"java/java_memory_model#8f620debb211","section":"java","module":"java/java_memory_model","moduleName":"java memory model","sourceFile":"README.md","difficulty":"advanced","question":"Why can code with an unsynchronized read/write \"work fine\" on x86 but fail on ARM?","answerFull":"x86 uses a strong memory model (Total Store Order) that preserves most store/load ordering, so many JMM-violating data races still happen to produce the expected result on that hardware. ARM and POWER use weaker memory models that aggressively reorder independent loads and stores unless an explicit memory barrier forbids it, so the same unsynchronized code can expose stale reads or reordered writes that x86 was silently hiding. This is why concurrency bugs are notorious for passing code review, passing tests on a developer's x86 laptop, and then failing intermittently on ARM-based cloud instances or Android phones. Practical guidance: never rely on \"it works on my machine\" for concurrent code — write to the JMM specification (volatile/synchronized/atomics) rather than to one architecture's observed behavior, and use jcstress or test on ARM hardware to catch model-dependent bugs before production.","correct":"x86 uses a strong memory model (Total Store Order) that preserves most store/load ordering, so many JMM-violating data races still happen to produce the expected result on that hardware.","distractors":["The JMM defines a \"freeze action\" for final fields: when a constructor completes (without this-escape), all writes to final fields in that constructor are frozen and published.","volatile only guarantees visibility and ordering for individual reads and writes — it does not make a compound read-modify-write operation atomic.","The JMM's \"freeze action\" for final fields: when an object's constructor completes normally, a JMM freeze action fires for every final field written in that constructor."],"distractorIds":["java/java_memory_model#a2c8856deb8f","java/java_memory_model#d3bfbfa77789","java/java_memory_model#dd51246bd873"],"concepts":["races","x86","violating","happen","preserves","jmm"],"answerFullMd":"x86 uses a strong memory model (Total Store Order) that preserves most store/load ordering, so many JMM-violating data races still happen to produce the expected result on that hardware. ARM and POWER use weaker memory models that aggressively reorder independent loads and stores unless an explicit memory barrier forbids it, so the same unsynchronized code can expose stale reads or reordered writes that x86 was silently hiding. This is why concurrency bugs are notorious for passing code review, passing tests on a developer's x86 laptop, and then failing intermittently on ARM-based cloud instances or Android phones. Practical guidance: never rely on \"it works on my machine\" for concurrent code — write to the JMM specification (`volatile`/`synchronized`/atomics) rather than to one architecture's observed behavior, and use `jcstress` or test on ARM hardware to catch model-dependent bugs before production.","distractorsMd":["The JMM defines a \"freeze action\" for `final` fields: when a constructor completes (without this-escape), all writes to `final` fields in that constructor are frozen and published.","`volatile` only guarantees visibility and ordering for individual reads and writes — it does not make a compound read-modify-write operation atomic.","The JMM's \"freeze action\" for `final` fields: when an object's constructor completes normally, a JMM freeze action fires for every `final` field written in that constructor."]},{"id":"java/java_streams#5bd51e62970d","section":"java","module":"java/java_streams","moduleName":"java streams","sourceFile":"README.md","difficulty":"core","question":"What is lazy evaluation in streams, and when does execution actually happen?","answerFull":"Intermediate operations (filter, map, flatMap, sorted, etc.) are lazy — they register a transformation in the pipeline but execute no work. Execution happens when a terminal operation (collect, forEach, reduce, count, findFirst, etc.) is invoked. At that point, the JVM fuses the registered operations into a traversal over the source — a single pass through the data, applying each operation in sequence per element. Short-circuit terminals (findFirst, limit(n)) stop the traversal early, so elements after the first match are never processed.","correct":"Intermediate operations (filter, map, flatMap, sorted, etc.) are lazy — they register a transformation in the pipeline but execute no work.","distractors":["Spliterator (splittable iterator) is the source contract for streams.","Parallel streams split work across the common ForkJoinPool (parallelism = CPU count − 1).","mapMulti(BiConsumer<T, Consumer<R>> mapper) is an imperative alternative to flatMap."],"distractorIds":["java/java_streams#c52406bccc59","java/java_streams#676ae710c4fe","java/java_streams#e0ea1f24b7df"],"concepts":["happen","flatmap","transformation","register","intermediate","lazy"],"correctMd":"Intermediate operations (`filter`, `map`, `flatMap`, `sorted`, etc.) are lazy — they register a transformation in the pipeline but execute no work.","answerFullMd":"Intermediate operations (`filter`, `map`, `flatMap`, `sorted`, etc.) are lazy — they register a transformation in the pipeline but execute no work. Execution happens when a terminal operation (`collect`, `forEach`, `reduce`, `count`, `findFirst`, etc.) is invoked. At that point, the JVM fuses the registered operations into a traversal over the source — a single pass through the data, applying each operation in sequence per element. Short-circuit terminals (`findFirst`, `limit(n)`) stop the traversal early, so elements after the first match are never processed.","distractorsMd":["`Spliterator` (splittable iterator) is the source contract for streams.","Parallel streams split work across the common `ForkJoinPool` (parallelism = CPU count − 1).","`mapMulti(BiConsumer<T, Consumer<R>> mapper)` is an imperative alternative to `flatMap`."]},{"id":"java/java_streams#399eb86afb39","section":"java","module":"java/java_streams","moduleName":"java streams","sourceFile":"README.md","difficulty":"core","question":"What is the difference between findFirst() and findAny()?","answerFull":"Both return an Optional of some matching element. findFirst() always returns the first element in the stream's *encounter order* (the order determined by the source and any sorted()/ORDERED characteristic). findAny() returns any matching element — in a sequential stream it usually returns the first, but in a parallel stream it returns whichever element is found first by any thread. Use findAny() in parallel streams when you don't care which element you get — it avoids the coordination cost of enforcing order, giving better parallelism.","correct":"Both return an Optional of some matching element.","distractors":["filter() tests every element — it scans the entire stream even after the predicate becomes false.","sorted() is a fully stateful, ordered operation — it must see *all* elements before emitting the first sorted element.","The two-argument form provides an *identity* value — an element that, combined with any other, returns that other (0 for addition, 1 for multiplication, \"\" for concatenation)."],"distractorIds":["java/java_streams#9f48c03750f7","java/java_streams#e564ba23f702","java/java_streams#5194d5db2e0e"],"concepts":["difference","optional","matching","element","return","findany"],"questionMd":"What is the difference between `findFirst()` and `findAny()`?","correctMd":"Both return an `Optional` of some matching element.","answerFullMd":"Both return an `Optional` of some matching element. `findFirst()` always returns the first element in the stream's *encounter order* (the order determined by the source and any `sorted()`/`ORDERED` characteristic). `findAny()` returns any matching element — in a sequential stream it usually returns the first, but in a parallel stream it returns whichever element is found first by any thread. Use `findAny()` in parallel streams when you don't care which element you get — it avoids the coordination cost of enforcing order, giving better parallelism.","distractorsMd":["`filter()` tests every element — it scans the entire stream even after the predicate becomes false.","`sorted()` is a fully stateful, ordered operation — it must see *all* elements before emitting the first sorted element.","The two-argument form provides an *identity* value — an element that, combined with any other, returns that other (`0` for addition, `1` for multiplication, `\"\"` for concatenation)."]},{"id":"java/java_streams#db4454b1c0d7","section":"java","module":"java/java_streams","moduleName":"java streams","sourceFile":"README.md","difficulty":"core","question":"Why can't you throw checked exceptions from a lambda in a stream pipeline?","answerFull":"The functional interfaces (Predicate, Function, Consumer, etc.) used by stream operations do not declare checked exceptions in their single abstract method. A lambda must match the functional interface's signature exactly — if the method doesn't declare the checked exception, neither can the lambda. Workarounds: (1) catch internally and wrap in RuntimeException; (2) use a utility method like sneakyThrow (Lombok); (3) write a wrapper functional interface that declares the checked exception; (4) prefer forEach with explicit try-catch for loops where exceptions propagate naturally.","correct":"The functional interfaces (Predicate, Function, Consumer, etc.) used by stream operations do not declare checked exceptions in their single abstract method.","distractors":["mapMulti(BiConsumer<T, Consumer<R>> mapper) is an imperative alternative to flatMap.","Stream.iterate(seed, hasNext, next) (Java 9) is a finite stream equivalent to for (T t = seed; hasNext.test(t); t = next.apply(t)) — generates values while the predicate returns true.","Collectors.collectingAndThen(downstream, finisher) applies a downstream collector then transforms its result with a finisher — all in a single collect() call."],"distractorIds":["java/java_streams#e0ea1f24b7df","java/java_streams#4cce23f9f46e","java/java_streams#624332f9248a"],"concepts":["functional","predicate","throw","checked","declare","interfaces"],"correctMd":"The functional interfaces (`Predicate`, `Function`, `Consumer`, etc.) used by stream operations do not declare checked exceptions in their single abstract method.","answerFullMd":"The functional interfaces (`Predicate`, `Function`, `Consumer`, etc.) used by stream operations do not declare checked exceptions in their single abstract method. A lambda must match the functional interface's signature exactly — if the method doesn't declare the checked exception, neither can the lambda. Workarounds: (1) catch internally and wrap in `RuntimeException`; (2) use a utility method like `sneakyThrow` (Lombok); (3) write a wrapper functional interface that declares the checked exception; (4) prefer `forEach` with explicit try-catch for loops where exceptions propagate naturally.","distractorsMd":["`mapMulti(BiConsumer<T, Consumer<R>> mapper)` is an imperative alternative to `flatMap`.","`Stream.iterate(seed, hasNext, next)` (Java 9) is a **finite** stream equivalent to `for (T t = seed; hasNext.test(t); t = next.apply(t))` — generates values while the predicate returns `true`.","`Collectors.collectingAndThen(downstream, finisher)` applies a `downstream` collector then transforms its result with a `finisher` — all in a single `collect()` call."]},{"id":"java/java_streams#5194d5db2e0e","section":"java","module":"java/java_streams","moduleName":"java streams","sourceFile":"README.md","difficulty":"core","question":"What is the difference between reduce(identity, BinaryOp) and reduce(BinaryOp) returning Optional?","answerFull":"The two-argument form provides an *identity* value — an element that, combined with any other, returns that other (0 for addition, 1 for multiplication, \"\" for concatenation). If the stream is empty, the identity is returned directly. The one-argument form returns Optional<T> because with no identity and an empty stream, there is no meaningful result to return — returning a default (like null) would be unsafe. Rule: if you have a valid identity value for your operation, use the two-argument form for simpler code; otherwise use the Optional-returning form and handle the empty case.","correct":"The two-argument form provides an *identity* value — an element that, combined with any other, returns that other (0 for addition, 1 for multiplication, \"\" for concatenation).","distractors":["LinkedList's Spliterator cannot split efficiently — trySplit() traverses to the midpoint (O(n)) and returns null-ish splits for short lists.","filter() tests every element — it scans the entire stream even after the predicate becomes false.","Stream.iterate(seed, hasNext, next) (Java 9) is a finite stream equivalent to for (T t = seed; hasNext.test(t); t = next.apply(t)) — generates values while the predicate returns true."],"distractorIds":["java/java_streams#4bc74be07373","java/java_streams#9f48c03750f7","java/java_streams#4cce23f9f46e"],"concepts":["concatenation","multiplication","addition","difference","combined","reduce"],"questionMd":"What is the difference between `reduce(identity, BinaryOp)` and `reduce(BinaryOp)` returning `Optional`?","correctMd":"The two-argument form provides an *identity* value — an element that, combined with any other, returns that other (`0` for addition, `1` for multiplication, `\"\"` for concatenation).","answerFullMd":"The two-argument form provides an *identity* value — an element that, combined with any other, returns that other (`0` for addition, `1` for multiplication, `\"\"` for concatenation). If the stream is empty, the identity is returned directly. The one-argument form returns `Optional<T>` because with no identity and an empty stream, there is no meaningful result to return — returning a default (like `null`) would be unsafe. Rule: if you have a valid identity value for your operation, use the two-argument form for simpler code; otherwise use the Optional-returning form and handle the empty case.","distractorsMd":["`LinkedList`'s `Spliterator` cannot split efficiently — `trySplit()` traverses to the midpoint (O(n)) and returns null-ish splits for short lists.","`filter()` tests every element — it scans the entire stream even after the predicate becomes false.","`Stream.iterate(seed, hasNext, next)` (Java 9) is a **finite** stream equivalent to `for (T t = seed; hasNext.test(t); t = next.apply(t))` — generates values while the predicate returns `true`."]},{"id":"java/java_streams#c52406bccc59","section":"java","module":"java/java_streams","moduleName":"java streams","sourceFile":"README.md","difficulty":"core","question":"Explain Spliterator and its role in parallel streams.","answerFull":"Spliterator (splittable iterator) is the source contract for streams. It has two key methods: tryAdvance(Consumer) processes one element; trySplit() attempts to divide the source into two halves for parallel processing. The JVM recursively calls trySplit() until sub-ranges are small enough to assign to worker threads. Characteristics flags (SIZED, ORDERED, DISTINCT, etc.) let the stream engine optimize: a SIZED source allows balanced splitting; an ORDERED source requires findFirst() to respect order. ArrayList provides SIZED + SUBSIZED + ORDERED — ideal for parallel. LinkedList returns null from trySplit() — degrades to sequential.","correct":"Spliterator (splittable iterator) is the source contract for streams.","distractors":["Parallel streams split work across the common ForkJoinPool (parallelism = CPU count − 1).","LinkedList's Spliterator cannot split efficiently — trySplit() traverses to the midpoint (O(n)) and returns null-ish splits for short lists.","All three are short-circuit terminal operations."],"distractorIds":["java/java_streams#676ae710c4fe","java/java_streams#4bc74be07373","java/java_streams#ba4474ff3bba"],"concepts":["splittable","spliterator","explain","iterator","contract","role"],"questionMd":"Explain `Spliterator` and its role in parallel streams.","correctMd":"`Spliterator` (splittable iterator) is the source contract for streams.","answerFullMd":"`Spliterator` (splittable iterator) is the source contract for streams. It has two key methods: `tryAdvance(Consumer)` processes one element; `trySplit()` attempts to divide the source into two halves for parallel processing. The JVM recursively calls `trySplit()` until sub-ranges are small enough to assign to worker threads. Characteristics flags (`SIZED`, `ORDERED`, `DISTINCT`, etc.) let the stream engine optimize: a `SIZED` source allows balanced splitting; an `ORDERED` source requires `findFirst()` to respect order. ArrayList provides `SIZED + SUBSIZED + ORDERED` — ideal for parallel. LinkedList returns `null` from `trySplit()` — degrades to sequential.","distractorsMd":["Parallel streams split work across the common `ForkJoinPool` (parallelism = CPU count − 1).","`LinkedList`'s `Spliterator` cannot split efficiently — `trySplit()` traverses to the midpoint (O(n)) and returns null-ish splits for short lists.",null]},{"id":"java/java_streams#4bc74be07373","section":"java","module":"java/java_streams","moduleName":"java streams","sourceFile":"README.md","difficulty":"intermediate","question":"What happens when you call .parallel() on a stream backed by a LinkedList?","answerFull":"LinkedList's Spliterator cannot split efficiently — trySplit() traverses to the midpoint (O(n)) and returns null-ish splits for short lists. The parallel framework tries to split but produces poorly balanced or degenerate sub-tasks. The result: most work happens on one thread (essentially sequential) but with all the overhead of thread scheduling, task stealing, and result merging. Performance is worse than sequential. Rule: only use parallelStream() on collections with efficient splitters: ArrayList, arrays, HashSet, TreeSet, ConcurrentHashMap.","correct":"LinkedList's Spliterator cannot split efficiently — trySplit() traverses to the midpoint (O(n)) and returns null-ish splits for short lists.","distractors":["The functional interfaces (Predicate, Function, Consumer, etc.) used by stream operations do not declare checked exceptions in their single abstract method.","filter() tests every element — it scans the entire stream even after the predicate becomes false.","Collectors.collectingAndThen(downstream, finisher) applies a downstream collector then transforms its result with a finisher — all in a single collect() call."],"distractorIds":["java/java_streams#db4454b1c0d7","java/java_streams#9f48c03750f7","java/java_streams#624332f9248a"],"concepts":["ish","linkedlist","trysplit","midpoint","traverses","spliterator"],"questionMd":"What happens when you call `.parallel()` on a stream backed by a `LinkedList`?","correctMd":"`LinkedList`'s `Spliterator` cannot split efficiently — `trySplit()` traverses to the midpoint (O(n)) and returns null-ish splits for short lists.","answerFullMd":"`LinkedList`'s `Spliterator` cannot split efficiently — `trySplit()` traverses to the midpoint (O(n)) and returns null-ish splits for short lists. The parallel framework tries to split but produces poorly balanced or degenerate sub-tasks. The result: most work happens on one thread (essentially sequential) but with all the overhead of thread scheduling, task stealing, and result merging. Performance is worse than sequential. **Rule**: only use `parallelStream()` on collections with efficient splitters: `ArrayList`, arrays, `HashSet`, `TreeSet`, `ConcurrentHashMap`.","distractorsMd":["The functional interfaces (`Predicate`, `Function`, `Consumer`, etc.) used by stream operations do not declare checked exceptions in their single abstract method.","`filter()` tests every element — it scans the entire stream even after the predicate becomes false.","`Collectors.collectingAndThen(downstream, finisher)` applies a `downstream` collector then transforms its result with a `finisher` — all in a single `collect()` call."]},{"id":"java/java_streams#e0ea1f24b7df","section":"java","module":"java/java_streams","moduleName":"java streams","sourceFile":"README.md","difficulty":"intermediate","question":"What is mapMulti() (Java 16) and when is it better than flatMap()?","answerFull":"mapMulti(BiConsumer<T, Consumer<R>> mapper) is an imperative alternative to flatMap. For each element, you're given a consumer; call it 0 or more times to push elements downstream. Unlike flatMap(), it does not create an intermediate Stream per element — the JVM can fuse it more aggressively. Use mapMulti when: (1) each element expands to a large number of outputs; (2) the expansion logic is complex imperative code; (3) you want to conditionally emit 0 elements (replaces flatMap with Stream.empty() or Stream.ofNullable()). flatMap is still preferred for simple cases — it's more readable.","correct":"mapMulti(BiConsumer<T, Consumer<R>> mapper) is an imperative alternative to flatMap.","distractors":["Collectors.teeing(collector1, collector2, mergeFunction) (Java 12) sends each element to two collectors simultaneously and combines their results with a merge function — all in a single pass.","The functional interfaces (Predicate, Function, Consumer, etc.) used by stream operations do not declare checked exceptions in their single abstract method.","Stream.iterate(seed, hasNext, next) (Java 9) is a finite stream equivalent to for (T t = seed; hasNext.test(t); t = next.apply(t)) — generates values while the predicate returns true."],"distractorIds":["java/java_streams#cd5aa1a51ed7","java/java_streams#db4454b1c0d7","java/java_streams#4cce23f9f46e"],"concepts":["biconsumer","mapmulti","imperative","mapper","alternative","flatmap"],"questionMd":"What is `mapMulti()` (Java 16) and when is it better than `flatMap()`?","correctMd":"`mapMulti(BiConsumer<T, Consumer<R>> mapper)` is an imperative alternative to `flatMap`.","answerFullMd":"`mapMulti(BiConsumer<T, Consumer<R>> mapper)` is an imperative alternative to `flatMap`. For each element, you're given a consumer; call it 0 or more times to push elements downstream. Unlike `flatMap()`, it does not create an intermediate `Stream` per element — the JVM can fuse it more aggressively. Use `mapMulti` when: (1) each element expands to a large number of outputs; (2) the expansion logic is complex imperative code; (3) you want to conditionally emit 0 elements (replaces `flatMap` with `Stream.empty()` or `Stream.ofNullable()`). `flatMap` is still preferred for simple cases — it's more readable.","distractorsMd":["`Collectors.teeing(collector1, collector2, mergeFunction)` (Java 12) sends each element to two collectors simultaneously and combines their results with a merge function — all in a single pass.","The functional interfaces (`Predicate`, `Function`, `Consumer`, etc.) used by stream operations do not declare checked exceptions in their single abstract method.","`Stream.iterate(seed, hasNext, next)` (Java 9) is a **finite** stream equivalent to `for (T t = seed; hasNext.test(t); t = next.apply(t))` — generates values while the predicate returns `true`."]},{"id":"java/java_streams#e564ba23f702","section":"java","module":"java/java_streams","moduleName":"java streams","sourceFile":"README.md","difficulty":"intermediate","question":"How does sorted() affect parallel stream performance?","answerFull":"sorted() is a fully stateful, ordered operation — it must see *all* elements before emitting the first sorted element. In a parallel stream, this means: all worker threads finish processing, partial results are merged into a single collection, the sort runs on the merged collection, then the sorted result feeds downstream. This effectively serializes the sorted portion of the pipeline. The parallel speedup from upstream stages can be negated by the sort bottleneck. If you need the top-N elements, prefer reduce(Comparators.min/max) or collect-then-sort only the result, not the full stream.","correct":"sorted() is a fully stateful, ordered operation — it must see *all* elements before emitting the first sorted element.","distractors":["The functional interfaces (Predicate, Function, Consumer, etc.) used by stream operations do not declare checked exceptions in their single abstract method.","The two-argument form provides an *identity* value — an element that, combined with any other, returns that other (0 for addition, 1 for multiplication, \"\" for concatenation).","Collectors.teeing(collector1, collector2, mergeFunction) (Java 12) sends each element to two collectors simultaneously and combines their results with a merge function — all in a single pass."],"distractorIds":["java/java_streams#db4454b1c0d7","java/java_streams#5194d5db2e0e","java/java_streams#cd5aa1a51ed7"],"concepts":["emitting","affect","stateful","ordered","performance","see"],"questionMd":"How does `sorted()` affect parallel stream performance?","correctMd":"`sorted()` is a fully stateful, ordered operation — it must see *all* elements before emitting the first sorted element.","answerFullMd":"`sorted()` is a fully stateful, ordered operation — it must see *all* elements before emitting the first sorted element. In a parallel stream, this means: all worker threads finish processing, partial results are merged into a single collection, the sort runs on the merged collection, then the sorted result feeds downstream. This effectively serializes the sorted portion of the pipeline. The parallel speedup from upstream stages can be negated by the sort bottleneck. If you need the top-N elements, prefer `reduce(Comparators.min/max)` or collect-then-sort only the result, not the full stream.","distractorsMd":["The functional interfaces (`Predicate`, `Function`, `Consumer`, etc.) used by stream operations do not declare checked exceptions in their single abstract method.","The two-argument form provides an *identity* value — an element that, combined with any other, returns that other (`0` for addition, `1` for multiplication, `\"\"` for concatenation).","`Collectors.teeing(collector1, collector2, mergeFunction)` (Java 12) sends each element to two collectors simultaneously and combines their results with a merge function — all in a single pass."]},{"id":"java/java_streams#9f48c03750f7","section":"java","module":"java/java_streams","moduleName":"java streams","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between takeWhile() and filter() for sorted streams?","answerFull":"filter() tests every element — it scans the entire stream even after the predicate becomes false. takeWhile() (Java 9) stops as soon as the predicate returns false for the first time — it is short-circuit. For a sorted stream where you want elements satisfying a condition x < threshold, takeWhile(x -> x < threshold) stops at the first element that exceeds the threshold; filter(x -> x < threshold) continues scanning the rest. takeWhile is O(k) where k is the matching prefix; filter is always O(n). However, takeWhile on an unordered stream has nondeterministic behavior — only use it on ordered streams.","correct":"filter() tests every element — it scans the entire stream even after the predicate becomes false.","distractors":["Intermediate operations (filter, map, flatMap, sorted, etc.) are lazy — they register a transformation in the pipeline but execute no work.","Collectors.teeing(collector1, collector2, mergeFunction) (Java 12) sends each element to two collectors simultaneously and combines their results with a merge function — all in a single pass.","Parallel streams split work across the common ForkJoinPool (parallelism = CPU count − 1)."],"distractorIds":["java/java_streams#5bd51e62970d","java/java_streams#cd5aa1a51ed7","java/java_streams#676ae710c4fe"],"concepts":["predicate","scans","difference","streams","becomes","tests"],"questionMd":"What is the difference between `takeWhile()` and `filter()` for sorted streams?","correctMd":"`filter()` tests every element — it scans the entire stream even after the predicate becomes false.","answerFullMd":"`filter()` tests every element — it scans the entire stream even after the predicate becomes false. `takeWhile()` (Java 9) stops as soon as the predicate returns `false` for the first time — it is short-circuit. For a sorted stream where you want elements satisfying a condition `x < threshold`, `takeWhile(x -> x < threshold)` stops at the first element that exceeds the threshold; `filter(x -> x < threshold)` continues scanning the rest. `takeWhile` is O(k) where k is the matching prefix; `filter` is always O(n). However, `takeWhile` on an unordered stream has nondeterministic behavior — only use it on ordered streams.","distractorsMd":["Intermediate operations (`filter`, `map`, `flatMap`, `sorted`, etc.) are lazy — they register a transformation in the pipeline but execute no work.","`Collectors.teeing(collector1, collector2, mergeFunction)` (Java 12) sends each element to two collectors simultaneously and combines their results with a merge function — all in a single pass.","Parallel streams split work across the common `ForkJoinPool` (parallelism = CPU count − 1)."]},{"id":"java/java_streams#ba4474ff3bba","section":"java","module":"java/java_streams","moduleName":"java streams","sourceFile":"README.md","difficulty":"advanced","question":"How do anyMatch(), allMatch(), and noneMatch() short-circuit?","answerFull":"All three are short-circuit terminal operations. anyMatch(p): returns true immediately when the first matching element is found; processes no more elements. allMatch(p): returns false immediately when the first non-matching element is found. noneMatch(p): returns false immediately when the first matching element is found. On an empty stream: anyMatch = false, allMatch = true (vacuously true), noneMatch = true. These are equivalent to short-circuit || / && applied to a sequence — they only evaluate as many elements as needed to determine the answer.","correct":"All three are short-circuit terminal operations.","distractors":["Intermediate operations (filter, map, flatMap, sorted, etc.) are lazy — they register a transformation in the pipeline but execute no work.","LinkedList's Spliterator cannot split efficiently — trySplit() traverses to the midpoint (O(n)) and returns null-ish splits for short lists.","The functional interfaces (Predicate, Function, Consumer, etc.) used by stream operations do not declare checked exceptions in their single abstract method."],"distractorIds":["java/java_streams#5bd51e62970d","java/java_streams#4bc74be07373","java/java_streams#db4454b1c0d7"],"concepts":["terminal","circuit","short","operations","allmatch","anymatch"],"questionMd":"How do `anyMatch()`, `allMatch()`, and `noneMatch()` short-circuit?","answerFullMd":"All three are short-circuit terminal operations. `anyMatch(p)`: returns `true` immediately when the first matching element is found; processes no more elements. `allMatch(p)`: returns `false` immediately when the first non-matching element is found. `noneMatch(p)`: returns `false` immediately when the first matching element is found. On an empty stream: `anyMatch` = `false`, `allMatch` = `true` (vacuously true), `noneMatch` = `true`. These are equivalent to short-circuit `||` / `&&` applied to a sequence — they only evaluate as many elements as needed to determine the answer.","distractorsMd":["Intermediate operations (`filter`, `map`, `flatMap`, `sorted`, etc.) are lazy — they register a transformation in the pipeline but execute no work.","`LinkedList`'s `Spliterator` cannot split efficiently — `trySplit()` traverses to the midpoint (O(n)) and returns null-ish splits for short lists.","The functional interfaces (`Predicate`, `Function`, `Consumer`, etc.) used by stream operations do not declare checked exceptions in their single abstract method."]},{"id":"java/java_streams#cd5aa1a51ed7","section":"java","module":"java/java_streams","moduleName":"java streams","sourceFile":"README.md","difficulty":"advanced","question":"What is Collectors.teeing() and give a use case?","answerFull":"Collectors.teeing(collector1, collector2, mergeFunction) (Java 12) sends each element to two collectors simultaneously and combines their results with a merge function — all in a single pass. Use case: compute both count and average in one pass: stream.collect(Collectors.teeing(Collectors.counting(), Collectors.averagingDouble(v -> v), (count, avg) -> new Stats(count, avg))). Without teeing, you'd need two separate stream passes or a custom Collector. Also useful for split-and-combine: partition into two groups and compute something different for each.","correct":"Collectors.teeing(collector1, collector2, mergeFunction) (Java 12) sends each element to two collectors simultaneously and combines their results with a merge function — all in a single pass.","distractors":["sorted() is a fully stateful, ordered operation — it must see *all* elements before emitting the first sorted element.","The two-argument form provides an *identity* value — an element that, combined with any other, returns that other (0 for addition, 1 for multiplication, \"\" for concatenation).","filter() tests every element — it scans the entire stream even after the predicate becomes false."],"distractorIds":["java/java_streams#e564ba23f702","java/java_streams#5194d5db2e0e","java/java_streams#9f48c03750f7"],"concepts":["collector1","collector2","mergefunction","teeing","collectors","give"],"questionMd":"What is `Collectors.teeing()` and give a use case?","correctMd":"`Collectors.teeing(collector1, collector2, mergeFunction)` (Java 12) sends each element to two collectors simultaneously and combines their results with a merge function — all in a single pass.","answerFullMd":"`Collectors.teeing(collector1, collector2, mergeFunction)` (Java 12) sends each element to two collectors simultaneously and combines their results with a merge function — all in a single pass. Use case: compute both count and average in one pass: `stream.collect(Collectors.teeing(Collectors.counting(), Collectors.averagingDouble(v -> v), (count, avg) -> new Stats(count, avg)))`. Without `teeing`, you'd need two separate stream passes or a custom `Collector`. Also useful for split-and-combine: partition into two groups and compute something different for each.","distractorsMd":["`sorted()` is a fully stateful, ordered operation — it must see *all* elements before emitting the first sorted element.","The two-argument form provides an *identity* value — an element that, combined with any other, returns that other (`0` for addition, `1` for multiplication, `\"\"` for concatenation).","`filter()` tests every element — it scans the entire stream even after the predicate becomes false."]},{"id":"java/java_streams#4cce23f9f46e","section":"java","module":"java/java_streams","moduleName":"java streams","sourceFile":"README.md","difficulty":"advanced","question":"How does Stream.iterate(seed, hasNext, next) (Java 9) differ from the two-argument form and from Stream.generate()?","answerFull":"Stream.iterate(seed, hasNext, next) (Java 9) is a finite stream equivalent to for (T t = seed; hasNext.test(t); t = next.apply(t)) — generates values while the predicate returns true. Stream.iterate(0, n -> n < 10, n -> n + 1) yields 0–9 and terminates. The two-argument form Stream.iterate(seed, next) is infinite — use limit() or takeWhile() to bound it. Stream.generate(Supplier) is also always infinite with no \"previous value\" concept — each call to the supplier is independent, making it naturally suited for random sequences or polling. Key difference: iterate is deterministic and ordered (each value derived from the previous, suitable for sequential integers or walks); generate has no inter-call ordering, so parallel generate is safe without coordination. Practical rule: iterate for mathematical recurrences and index ranges; generate for UUID.randomUUID(), sampling, or constant factories.","correct":"Stream.iterate(seed, hasNext, next) (Java 9) is a finite stream equivalent to for (T t = seed; hasNext.test(t); t = next.apply(t)) — generates values while the predicate returns true.","distractors":["LinkedList's Spliterator cannot split efficiently — trySplit() traverses to the midpoint (O(n)) and returns null-ish splits for short lists.","Collectors.teeing(collector1, collector2, mergeFunction) (Java 12) sends each element to two collectors simultaneously and combines their results with a merge function — all in a single pass.","The two-argument form provides an *identity* value — an element that, combined with any other, returns that other (0 for addition, 1 for multiplication, \"\" for concatenation)."],"distractorIds":["java/java_streams#4bc74be07373","java/java_streams#cd5aa1a51ed7","java/java_streams#5194d5db2e0e"],"concepts":["hasnext","finite","iterate","predicate","seed","differ"],"questionMd":"How does `Stream.iterate(seed, hasNext, next)` (Java 9) differ from the two-argument form and from `Stream.generate()`?","correctMd":"`Stream.iterate(seed, hasNext, next)` (Java 9) is a **finite** stream equivalent to `for (T t = seed; hasNext.test(t); t = next.apply(t))` — generates values while the predicate returns `true`.","answerFullMd":"`Stream.iterate(seed, hasNext, next)` (Java 9) is a **finite** stream equivalent to `for (T t = seed; hasNext.test(t); t = next.apply(t))` — generates values while the predicate returns `true`. `Stream.iterate(0, n -> n < 10, n -> n + 1)` yields 0–9 and terminates. The two-argument form `Stream.iterate(seed, next)` is **infinite** — use `limit()` or `takeWhile()` to bound it. `Stream.generate(Supplier)` is also always **infinite** with no \"previous value\" concept — each call to the supplier is independent, making it naturally suited for random sequences or polling. Key difference: `iterate` is deterministic and ordered (each value derived from the previous, suitable for sequential integers or walks); `generate` has no inter-call ordering, so parallel `generate` is safe without coordination. Practical rule: `iterate` for mathematical recurrences and index ranges; `generate` for `UUID.randomUUID()`, sampling, or constant factories.","distractorsMd":["`LinkedList`'s `Spliterator` cannot split efficiently — `trySplit()` traverses to the midpoint (O(n)) and returns null-ish splits for short lists.","`Collectors.teeing(collector1, collector2, mergeFunction)` (Java 12) sends each element to two collectors simultaneously and combines their results with a merge function — all in a single pass.","The two-argument form provides an *identity* value — an element that, combined with any other, returns that other (`0` for addition, `1` for multiplication, `\"\"` for concatenation)."]},{"id":"java/java_streams#624332f9248a","section":"java","module":"java/java_streams","moduleName":"java streams","sourceFile":"README.md","difficulty":"advanced","question":"What is Collectors.collectingAndThen() and what are its two most common production uses?","answerFull":"Collectors.collectingAndThen(downstream, finisher) applies a downstream collector then transforms its result with a finisher — all in a single collect() call. Common use 1 — produce an unmodifiable list (pre-Java 16 pattern): java List<String> names = people.stream() .map(Person::getName) .collect(Collectors.collectingAndThen(Collectors.toList(), Collections::unmodifiableList)); // Java 16+ equivalent: stream.toList() Common use 2 — downcast Long to int from counting(): java int size = stream.collect(Collectors.collectingAndThen(Collectors.counting(), Long::intValue)); Also useful for converting a collected Map to an ImmutableMap, or building a value object from aggregated fields in a single pass. In Java 16+, prefer Stream.toList() for the immutable-list case.","correct":"Collectors.collectingAndThen(downstream, finisher) applies a downstream collector then transforms its result with a finisher — all in a single collect() call.","distractors":["The functional interfaces (Predicate, Function, Consumer, etc.) used by stream operations do not declare checked exceptions in their single abstract method.","Collectors.teeing(collector1, collector2, mergeFunction) (Java 12) sends each element to two collectors simultaneously and combines their results with a merge function — all in a single pass.","Parallel streams split work across the common ForkJoinPool (parallelism = CPU count − 1)."],"distractorIds":["java/java_streams#db4454b1c0d7","java/java_streams#cd5aa1a51ed7","java/java_streams#676ae710c4fe"],"concepts":["collectingandthen","finisher","collectors","collector","collect","transforms"],"questionMd":"What is `Collectors.collectingAndThen()` and what are its two most common production uses?","correctMd":"`Collectors.collectingAndThen(downstream, finisher)` applies a `downstream` collector then transforms its result with a `finisher` — all in a single `collect()` call.","answerFullMd":"`Collectors.collectingAndThen(downstream, finisher)` applies a `downstream` collector then transforms its result with a `finisher` — all in a single `collect()` call. Common use 1 — produce an unmodifiable list (pre-Java 16 pattern): ```java List<String> names = people.stream() .map(Person::getName) .collect(Collectors.collectingAndThen(Collectors.toList(), Collections::unmodifiableList)); // Java 16+ equivalent: stream.toList() ``` Common use 2 — downcast `Long` to `int` from `counting()`: ```java int size = stream.collect(Collectors.collectingAndThen(Collectors.counting(), Long::intValue)); ``` Also useful for converting a collected `Map` to an `ImmutableMap`, or building a value object from aggregated fields in a single pass. In Java 16+, prefer `Stream.toList()` for the immutable-list case.","distractorsMd":["The functional interfaces (`Predicate`, `Function`, `Consumer`, etc.) used by stream operations do not declare checked exceptions in their single abstract method.","`Collectors.teeing(collector1, collector2, mergeFunction)` (Java 12) sends each element to two collectors simultaneously and combines their results with a merge function — all in a single pass.","Parallel streams split work across the common `ForkJoinPool` (parallelism = CPU count − 1)."]},{"id":"java/java_streams#676ae710c4fe","section":"java","module":"java/java_streams","moduleName":"java streams","sourceFile":"README.md","difficulty":"advanced","question":"Under what four conditions does a parallel stream perform *worse* than sequential?","answerFull":"Parallel streams split work across the common ForkJoinPool (parallelism = CPU count − 1). They degrade when: 1. Small data (< ~10,000 simple elements) — fork/join overhead (~microseconds per split/merge) dominates the work. 2. Non-splittable sources — LinkedList, Iterator-backed streams, BufferedReader.lines() — the Spliterator cannot divide the source efficiently; the work stays on one thread anyway. 3. Stateful intermediates — sorted(), distinct(), limit() require global coordination (barriers, sorted merge phases) that can make parallel slower than sequential for moderate-sized data. 4. Blocking inside the lambda — I/O, DB calls, Thread.sleep() starve the shared ForkJoinPool and harm all other tasks using it (e.g., CompletableFuture pipelines). Use Executors.newVirtualThreadPerTaskExecutor() for blocking fan-out instead. Practical rule: benchmark with JMH under realistic data sizes before shipping parallel(); the default answer is \"don't\" unless the data is CPU-bound and large.","correct":"Parallel streams split work across the common ForkJoinPool (parallelism = CPU count − 1).","distractors":["Intermediate operations (filter, map, flatMap, sorted, etc.) are lazy — they register a transformation in the pipeline but execute no work.","Stream.iterate(seed, hasNext, next) (Java 9) is a finite stream equivalent to for (T t = seed; hasNext.test(t); t = next.apply(t)) — generates values while the predicate returns true.","LinkedList's Spliterator cannot split efficiently — trySplit() traverses to the midpoint (O(n)) and returns null-ish splits for short lists."],"distractorIds":["java/java_streams#5bd51e62970d","java/java_streams#4cce23f9f46e","java/java_streams#4bc74be07373"],"concepts":["forkjoinpool","worse","perform","conditions","parallelism","streams"],"correctMd":"Parallel streams split work across the common `ForkJoinPool` (parallelism = CPU count − 1).","answerFullMd":"Parallel streams split work across the common `ForkJoinPool` (parallelism = CPU count − 1). They degrade when: 1. **Small data** (< ~10,000 simple elements) — fork/join overhead (~microseconds per split/merge) dominates the work. 2. **Non-splittable sources** — `LinkedList`, `Iterator`-backed streams, `BufferedReader.lines()` — the `Spliterator` cannot divide the source efficiently; the work stays on one thread anyway. 3. **Stateful intermediates** — `sorted()`, `distinct()`, `limit()` require global coordination (barriers, sorted merge phases) that can make parallel slower than sequential for moderate-sized data. 4. **Blocking inside the lambda** — I/O, DB calls, `Thread.sleep()` starve the shared `ForkJoinPool` and harm all other tasks using it (e.g., `CompletableFuture` pipelines). Use `Executors.newVirtualThreadPerTaskExecutor()` for blocking fan-out instead. Practical rule: benchmark with JMH under realistic data sizes before shipping `parallel()`; the default answer is \"don't\" unless the data is CPU-bound and large.","distractorsMd":["Intermediate operations (`filter`, `map`, `flatMap`, `sorted`, etc.) are lazy — they register a transformation in the pipeline but execute no work.","`Stream.iterate(seed, hasNext, next)` (Java 9) is a **finite** stream equivalent to `for (T t = seed; hasNext.test(t); t = next.apply(t))` — generates values while the predicate returns `true`.","`LinkedList`'s `Spliterator` cannot split efficiently — `trySplit()` traverses to the midpoint (O(n)) and returns null-ish splits for short lists."]},{"id":"java/java_time_datetime#5ffd637a8d17","section":"java","module":"java/java_time_datetime","moduleName":"java time datetime","sourceFile":"README.md","difficulty":"core","question":"Why is LocalDateTime the wrong type to store an event timestamp?","answerFull":"Because LocalDateTime carries no time zone or offset, so it cannot identify a unique moment on the machine timeline. Two LocalDateTime values from different hosts cannot be ordered or diffed correctly, and on DST days a wall-clock value can be ambiguous (fall-back) or nonexistent (spring-forward). Store an Instant (UTC) for events, keep the user's ZoneId separately, and render local only at the edge.","correct":"Because LocalDateTime carries no time zone or offset, so it cannot identify a unique moment on the machine timeline.","distractors":["Instant is a point on the machine timeline — nanoseconds from the 1970 UTC epoch, with no zone — while LocalDateTime is a wall-clock reading with no zone at all.","The local time does not exist, so java.time silently shifts it forward by the DST offset rather than throwing — 02:30 on a US spring-forward night becomes 03:30.","Because they are mutable (hence not thread-safe), have an error-prone API (0-indexed months, so December is 11), and conflate the machine and human timelines with no clear zone model."],"distractorIds":["java/java_time_datetime#20a46b8e2002","java/java_time_datetime#832eea8563c2","java/java_time_datetime#6cdf402af1c8"],"concepts":["localdatetime","timeline","moment","identify","zone","carries"],"questionMd":"Why is `LocalDateTime` the wrong type to store an event timestamp?","correctMd":"Because `LocalDateTime` carries no time zone or offset, so it cannot identify a unique moment on the machine timeline.","answerFullMd":"Because `LocalDateTime` carries no time zone or offset, so it cannot identify a unique moment on the machine timeline. Two `LocalDateTime` values from different hosts cannot be ordered or diffed correctly, and on DST days a wall-clock value can be ambiguous (fall-back) or nonexistent (spring-forward). Store an `Instant` (UTC) for events, keep the user's `ZoneId` separately, and render local only at the edge.","distractorsMd":["`Instant` is a point on the machine timeline — nanoseconds from the 1970 UTC epoch, with no zone — while `LocalDateTime` is a wall-clock reading with no zone at all.","The local time does not exist, so java.time silently shifts it forward by the DST offset rather than throwing — `02:30` on a US spring-forward night becomes `03:30`.","Because they are mutable (hence not thread-safe), have an error-prone API (0-indexed months, so December is `11`), and conflate the machine and human timelines with no clear zone model."]},{"id":"java/java_time_datetime#d2ae73d93183","section":"java","module":"java/java_time_datetime","moduleName":"java time datetime","sourceFile":"README.md","difficulty":"core","question":"Is SimpleDateFormat thread-safe, and what replaces it?","answerFull":"No — SimpleDateFormat keeps mutable parsing state in a Calendar field, so sharing one instance across threads corrupts output or throws intermittently under load. The classic bug is a static final SimpleDateFormat in a high-throughput path. The fix is DateTimeFormatter, which is immutable and thread-safe, so a single static instance can safely serve every request thread.","correct":"No — SimpleDateFormat keeps mutable parsing state in a Calendar field, so sharing one instance across threads corrupts output or throws intermittently under load.","distractors":["Because they are mutable (hence not thread-safe), have an error-prone API (0-indexed months, so December is 11), and conflate the machine and human timelines with no clear zone model.","Because Period is calendar (human) arithmetic and Duration is elapsed real time (machine) arithmetic, and across a DST boundary a calendar day is 23 or 25 real hours, not 24.","A TemporalAdjuster is a reusable strategy that computes a new temporal value from an existing one, such as first day of next month or next Monday."],"distractorIds":["java/java_time_datetime#6cdf402af1c8","java/java_time_datetime#bce5a8646c9e","java/java_time_datetime#9faf95d2fa0a"],"concepts":["intermittently","simpledateformat","corrupts","calendar","parsing","sharing"],"questionMd":"Is `SimpleDateFormat` thread-safe, and what replaces it?","correctMd":"No — `SimpleDateFormat` keeps mutable parsing state in a `Calendar` field, so sharing one instance across threads corrupts output or throws intermittently under load.","answerFullMd":"No — `SimpleDateFormat` keeps mutable parsing state in a `Calendar` field, so sharing one instance across threads corrupts output or throws intermittently under load. The classic bug is a `static final SimpleDateFormat` in a high-throughput path. The fix is `DateTimeFormatter`, which is immutable and thread-safe, so a single static instance can safely serve every request thread.","distractorsMd":["Because they are mutable (hence not thread-safe), have an error-prone API (0-indexed months, so December is `11`), and conflate the machine and human timelines with no clear zone model.","Because `Period` is calendar (human) arithmetic and `Duration` is elapsed real time (machine) arithmetic, and across a DST boundary a calendar day is 23 or 25 real hours, not 24.","A `TemporalAdjuster` is a reusable strategy that computes a new temporal value from an existing one, such as first day of next month or next Monday."]},{"id":"java/java_time_datetime#20a46b8e2002","section":"java","module":"java/java_time_datetime","moduleName":"java time datetime","sourceFile":"README.md","difficulty":"core","question":"What is the difference between Instant and LocalDateTime?","answerFull":"Instant is a point on the machine timeline — nanoseconds from the 1970 UTC epoch, with no zone — while LocalDateTime is a wall-clock reading with no zone at all. Instant identifies a unique moment; LocalDateTime does not until you attach a ZoneId. Use Instant for \"when it happened,\" LocalDateTime only for zone-less human values like a form field or a recurrence rule.","correct":"Instant is a point on the machine timeline — nanoseconds from the 1970 UTC epoch, with no zone — while LocalDateTime is a wall-clock reading with no zone at all.","distractors":["Because they are mutable (hence not thread-safe), have an error-prone API (0-indexed months, so December is 11), and conflate the machine and human timelines with no clear zone model.","Clock is the abstraction java.time uses to read \"now,\" and injecting it makes time-dependent code deterministically testable.","Use date.toInstant() to go to an Instant, and Date.from(instant) to come back."],"distractorIds":["java/java_time_datetime#6cdf402af1c8","java/java_time_datetime#1c687e87e955","java/java_time_datetime#e033c697857a"],"concepts":["1970","epoch","localdatetime","nanoseconds","utc","difference"],"questionMd":"What is the difference between `Instant` and `LocalDateTime`?","correctMd":"`Instant` is a point on the machine timeline — nanoseconds from the 1970 UTC epoch, with no zone — while `LocalDateTime` is a wall-clock reading with no zone at all.","answerFullMd":"`Instant` is a point on the machine timeline — nanoseconds from the 1970 UTC epoch, with no zone — while `LocalDateTime` is a wall-clock reading with no zone at all. `Instant` identifies a unique moment; `LocalDateTime` does not until you attach a `ZoneId`. Use `Instant` for \"when it happened,\" `LocalDateTime` only for zone-less human values like a form field or a recurrence rule.","distractorsMd":["Because they are mutable (hence not thread-safe), have an error-prone API (0-indexed months, so December is `11`), and conflate the machine and human timelines with no clear zone model.","`Clock` is the abstraction java.time uses to read \"now,\" and injecting it makes time-dependent code deterministically testable.","Use `date.toInstant()` to go to an `Instant`, and `Date.from(instant)` to come back."]},{"id":"java/java_time_datetime#bce5a8646c9e","section":"java","module":"java/java_time_datetime","moduleName":"java time datetime","sourceFile":"README.md","difficulty":"core","question":"Why is Period.ofDays(1) not the same as Duration.ofHours(24)?","answerFull":"Because Period is calendar (human) arithmetic and Duration is elapsed real time (machine) arithmetic, and across a DST boundary a calendar day is 23 or 25 real hours, not 24. Adding Period.ofDays(1) to a ZonedDateTime keeps the same wall-clock time next day; adding Duration.ofHours(24) advances exactly 24 real hours, so the wall clock drifts by an hour. Use Period for \"same time tomorrow,\" Duration for timeouts and TTLs.","correct":"Because Period is calendar (human) arithmetic and Duration is elapsed real time (machine) arithmetic, and across a DST boundary a calendar day is 23 or 25 real hours, not 24.","distractors":["Because LocalDateTime carries no time zone or offset, so it cannot identify a unique moment on the machine timeline.","Use ChronoUnit for a single unit — ChronoUnit.DAYS.between(start, end) or ChronoUnit.HOURS.between(a, b) — which returns a long count.","A TemporalAdjuster is a reusable strategy that computes a new temporal value from an existing one, such as first day of next month or next Monday."],"distractorIds":["java/java_time_datetime#5ffd637a8d17","java/java_time_datetime#1c086635b55c","java/java_time_datetime#9faf95d2fa0a"],"concepts":["elapsed","dst","calendar","arithmetic","period","day"],"questionMd":"Why is `Period.ofDays(1)` not the same as `Duration.ofHours(24)`?","correctMd":"Because `Period` is calendar (human) arithmetic and `Duration` is elapsed real time (machine) arithmetic, and across a DST boundary a calendar day is 23 or 25 real hours, not 24.","answerFullMd":"Because `Period` is calendar (human) arithmetic and `Duration` is elapsed real time (machine) arithmetic, and across a DST boundary a calendar day is 23 or 25 real hours, not 24. Adding `Period.ofDays(1)` to a `ZonedDateTime` keeps the same wall-clock time next day; adding `Duration.ofHours(24)` advances exactly 24 real hours, so the wall clock drifts by an hour. Use `Period` for \"same time tomorrow,\" `Duration` for timeouts and TTLs.","distractorsMd":["Because `LocalDateTime` carries no time zone or offset, so it cannot identify a unique moment on the machine timeline.","Use `ChronoUnit` for a single unit — `ChronoUnit.DAYS.between(start, end)` or `ChronoUnit.HOURS.between(a, b)` — which returns a `long` count.","A `TemporalAdjuster` is a reusable strategy that computes a new temporal value from an existing one, such as first day of next month or next Monday."]},{"id":"java/java_time_datetime#5da29b1a01ca","section":"java","module":"java/java_time_datetime","moduleName":"java time datetime","sourceFile":"README.md","difficulty":"core","question":"What is the difference between ZonedDateTime and OffsetDateTime?","answerFull":"ZonedDateTime carries a named ZoneId (like America/New_York) plus its live DST rules, whereas OffsetDateTime carries only a fixed ZoneOffset (like -05:00) with no rules. ZonedDateTime can compute future DST transitions and adjust automatically; OffsetDateTime freezes the offset, which is ideal for wire formats and DB columns but wrong for a future local time whose DST offset is not yet known.","correct":"ZonedDateTime carries a named ZoneId (like America/New_York) plus its live DST rules, whereas OffsetDateTime carries only a fixed ZoneOffset (like -05:00) with no rules.","distractors":["ZoneRules is java.time's view of the IANA tzdata database — the rules that map each zone's wall-clock time to a UTC offset over history.","Store the moment as UTC (an Instant/OffsetDateTime mapped to TIMESTAMP WITH TIME ZONE, i.e. timestamptz), and keep the user's zone separately only if you must reconstruct their local wall-clock intent.","Because LocalDateTime carries no time zone or offset, so it cannot identify a unique moment on the machine timeline."],"distractorIds":["java/java_time_datetime#8476d38dbbc7","java/java_time_datetime#4095836d5b8c","java/java_time_datetime#5ffd637a8d17"],"concepts":["york","zoneddatetime","america","offsetdatetime","zoneid","zoneoffset"],"questionMd":"What is the difference between `ZonedDateTime` and `OffsetDateTime`?","correctMd":"`ZonedDateTime` carries a named `ZoneId` (like `America/New_York`) plus its live DST rules, whereas `OffsetDateTime` carries only a fixed `ZoneOffset` (like `-05:00`) with no rules.","answerFullMd":"`ZonedDateTime` carries a named `ZoneId` (like `America/New_York`) plus its live DST rules, whereas `OffsetDateTime` carries only a fixed `ZoneOffset` (like `-05:00`) with no rules. `ZonedDateTime` can compute future DST transitions and adjust automatically; `OffsetDateTime` freezes the offset, which is ideal for wire formats and DB columns but wrong for a future local time whose DST offset is not yet known.","distractorsMd":["`ZoneRules` is java.time's view of the IANA tzdata database — the rules that map each zone's wall-clock time to a UTC offset over history.","Store the moment as UTC (an `Instant`/`OffsetDateTime` mapped to `TIMESTAMP WITH TIME ZONE`, i.e. `timestamptz`), and keep the user's zone separately only if you must reconstruct their local wall-clock intent.","Because `LocalDateTime` carries no time zone or offset, so it cannot identify a unique moment on the machine timeline."]},{"id":"java/java_time_datetime#13654ead279b","section":"java","module":"java/java_time_datetime","moduleName":"java time datetime","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between ZoneId and ZoneOffset?","answerFull":"A ZoneId is a named region whose UTC offset changes over the year according to tzdata rules; a ZoneOffset is a single fixed gap from UTC with no DST behavior. ZoneOffset is actually a subclass of ZoneId, but it represents only the current offset, not the rules that produced it. Use ZoneId.of(\"Europe/London\") when DST matters and ZoneOffset.ofHours(-5) only when you truly mean a fixed offset.","correct":"A ZoneId is a named region whose UTC offset changes over the year according to tzdata rules; a ZoneOffset is a single fixed gap from UTC with no DST behavior.","distractors":["Instant.now() returns the current moment on the machine timeline in UTC, while LocalDateTime.now() returns the current wall-clock reading in the JVM's default zone with the zone then discarded.","Instant is a point on the machine timeline — nanoseconds from the 1970 UTC epoch, with no zone — while LocalDateTime is a wall-clock reading with no zone at all.","Because Period is calendar (human) arithmetic and Duration is elapsed real time (machine) arithmetic, and across a DST boundary a calendar day is 23 or 25 real hours, not 24."],"distractorIds":["java/java_time_datetime#201dacf0117b","java/java_time_datetime#20a46b8e2002","java/java_time_datetime#bce5a8646c9e"],"concepts":["tzdata","zoneid","zoneoffset","according","year","dst"],"questionMd":"What is the difference between `ZoneId` and `ZoneOffset`?","correctMd":"A `ZoneId` is a named region whose UTC offset changes over the year according to tzdata rules; a `ZoneOffset` is a single fixed gap from UTC with no DST behavior.","answerFullMd":"A `ZoneId` is a named region whose UTC offset changes over the year according to tzdata rules; a `ZoneOffset` is a single fixed gap from UTC with no DST behavior. `ZoneOffset` is actually a subclass of `ZoneId`, but it represents only the current offset, not the rules that produced it. Use `ZoneId.of(\"Europe/London\")` when DST matters and `ZoneOffset.ofHours(-5)` only when you truly mean a fixed offset.","distractorsMd":["`Instant.now()` returns the current moment on the machine timeline in UTC, while `LocalDateTime.now()` returns the current wall-clock reading in the JVM's default zone with the zone then discarded.","`Instant` is a point on the machine timeline — nanoseconds from the 1970 UTC epoch, with no zone — while `LocalDateTime` is a wall-clock reading with no zone at all.","Because `Period` is calendar (human) arithmetic and `Duration` is elapsed real time (machine) arithmetic, and across a DST boundary a calendar day is 23 or 25 real hours, not 24."]},{"id":"java/java_time_datetime#832eea8563c2","section":"java","module":"java/java_time_datetime","moduleName":"java time datetime","sourceFile":"README.md","difficulty":"intermediate","question":"What happens if you construct a ZonedDateTime at a local time that falls in the spring-forward gap?","answerFull":"The local time does not exist, so java.time silently shifts it forward by the DST offset rather than throwing — 02:30 on a US spring-forward night becomes 03:30. ZonedDateTime.of(...) and atZone(...) apply this rule automatically, which makes the bug silent. If exact local firing matters, work in UTC or validate the local time against ZoneRules.getTransition(...).","correct":"The local time does not exist, so java.time silently shifts it forward by the DST offset rather than throwing — 02:30 on a US spring-forward night becomes 03:30.","distractors":["Because Period is calendar (human) arithmetic and Duration is elapsed real time (machine) arithmetic, and across a DST boundary a calendar day is 23 or 25 real hours, not 24.","ZonedDateTime carries a named ZoneId (like America/New_York) plus its live DST rules, whereas OffsetDateTime carries only a fixed ZoneOffset (like -05:00) with no rules.","Store the moment as UTC (an Instant/OffsetDateTime mapped to TIMESTAMP WITH TIME ZONE, i.e. timestamptz), and keep the user's zone separately only if you must reconstruct their local wall-clock intent."],"distractorIds":["java/java_time_datetime#bce5a8646c9e","java/java_time_datetime#5da29b1a01ca","java/java_time_datetime#4095836d5b8c"],"concepts":["night","zoneddatetime","construct","shifts","dst","throwing"],"questionMd":"What happens if you construct a `ZonedDateTime` at a local time that falls in the spring-forward gap?","correctMd":"The local time does not exist, so java.time silently shifts it forward by the DST offset rather than throwing — `02:30` on a US spring-forward night becomes `03:30`.","answerFullMd":"The local time does not exist, so java.time silently shifts it forward by the DST offset rather than throwing — `02:30` on a US spring-forward night becomes `03:30`. `ZonedDateTime.of(...)` and `atZone(...)` apply this rule automatically, which makes the bug silent. If exact local firing matters, work in UTC or validate the local time against `ZoneRules.getTransition(...)`.","distractorsMd":["Because `Period` is calendar (human) arithmetic and `Duration` is elapsed real time (machine) arithmetic, and across a DST boundary a calendar day is 23 or 25 real hours, not 24.","`ZonedDateTime` carries a named `ZoneId` (like `America/New_York`) plus its live DST rules, whereas `OffsetDateTime` carries only a fixed `ZoneOffset` (like `-05:00`) with no rules.","Store the moment as UTC (an `Instant`/`OffsetDateTime` mapped to `TIMESTAMP WITH TIME ZONE`, i.e. `timestamptz`), and keep the user's zone separately only if you must reconstruct their local wall-clock intent."]},{"id":"java/java_time_datetime#df26319bc3a7","section":"java","module":"java/java_time_datetime","moduleName":"java time datetime","sourceFile":"README.md","difficulty":"intermediate","question":"What happens at a fall-back overlap, and how do you disambiguate?","answerFull":"The local time occurs twice (once at the earlier offset, once at the later), and by default java.time picks the earlier offset. For 01:30 on a US fall-back night it chooses EDT (-04:00); call withLaterOffsetAtOverlap() to select the EST (-05:00) occurrence, or withEarlierOffsetAtOverlap() to be explicit about the default. This matters for events like billing runs that must fire exactly once.","correct":"The local time occurs twice (once at the earlier offset, once at the later), and by default java.time picks the earlier offset.","distractors":["Because LocalDateTime carries no time zone or offset, so it cannot identify a unique moment on the machine timeline.","Use date.toInstant() to go to an Instant, and Date.from(instant) to come back.","ZoneRules is java.time's view of the IANA tzdata database — the rules that map each zone's wall-clock time to a UTC offset over history."],"distractorIds":["java/java_time_datetime#5ffd637a8d17","java/java_time_datetime#e033c697857a","java/java_time_datetime#8476d38dbbc7"],"concepts":["fall","picks","earlier","twice","overlap","later"],"answerFullMd":"The local time occurs twice (once at the earlier offset, once at the later), and by default java.time picks the earlier offset. For `01:30` on a US fall-back night it chooses EDT (`-04:00`); call `withLaterOffsetAtOverlap()` to select the EST (`-05:00`) occurrence, or `withEarlierOffsetAtOverlap()` to be explicit about the default. This matters for events like billing runs that must fire exactly once.","distractorsMd":["Because `LocalDateTime` carries no time zone or offset, so it cannot identify a unique moment on the machine timeline.","Use `date.toInstant()` to go to an `Instant`, and `Date.from(instant)` to come back.","`ZoneRules` is java.time's view of the IANA tzdata database — the rules that map each zone's wall-clock time to a UTC offset over history."]},{"id":"java/java_time_datetime#6cdf402af1c8","section":"java","module":"java/java_time_datetime","moduleName":"java time datetime","sourceFile":"README.md","difficulty":"intermediate","question":"Why was java.util.Date/Calendar replaced?","answerFull":"Because they are mutable (hence not thread-safe), have an error-prone API (0-indexed months, so December is 11), and conflate the machine and human timelines with no clear zone model. java.util.Date is really a timestamp misleadingly named \"Date,\" and Calendar mixes representation with mutation. java.time fixes all three: immutable values, 1-based months, and distinct types for each timeline.","correct":"Because they are mutable (hence not thread-safe), have an error-prone API (0-indexed months, so December is 11), and conflate the machine and human timelines with no clear zone model.","distractors":["ZoneRules is java.time's view of the IANA tzdata database — the rules that map each zone's wall-clock time to a UTC offset over history.","Store the moment as UTC (an Instant/OffsetDateTime mapped to TIMESTAMP WITH TIME ZONE, i.e. timestamptz), and keep the user's zone separately only if you must reconstruct their local wall-clock intent.","Because LocalDateTime carries no time zone or offset, so it cannot identify a unique moment on the machine timeline."],"distractorIds":["java/java_time_datetime#8476d38dbbc7","java/java_time_datetime#4095836d5b8c","java/java_time_datetime#5ffd637a8d17"],"concepts":["conflate","hence","timelines","december","months","prone"],"questionMd":"Why was `java.util.Date`/`Calendar` replaced?","correctMd":"Because they are mutable (hence not thread-safe), have an error-prone API (0-indexed months, so December is `11`), and conflate the machine and human timelines with no clear zone model.","answerFullMd":"Because they are mutable (hence not thread-safe), have an error-prone API (0-indexed months, so December is `11`), and conflate the machine and human timelines with no clear zone model. `java.util.Date` is really a timestamp misleadingly named \"Date,\" and `Calendar` mixes representation with mutation. `java.time` fixes all three: immutable values, 1-based months, and distinct types for each timeline.","distractorsMd":["`ZoneRules` is java.time's view of the IANA tzdata database — the rules that map each zone's wall-clock time to a UTC offset over history.","Store the moment as UTC (an `Instant`/`OffsetDateTime` mapped to `TIMESTAMP WITH TIME ZONE`, i.e. `timestamptz`), and keep the user's zone separately only if you must reconstruct their local wall-clock intent.","Because `LocalDateTime` carries no time zone or offset, so it cannot identify a unique moment on the machine timeline."]},{"id":"java/java_time_datetime#e033c697857a","section":"java","module":"java/java_time_datetime","moduleName":"java time datetime","sourceFile":"README.md","difficulty":"intermediate","question":"How do you convert a legacy java.util.Date to modern java.time and back?","answerFull":"Use date.toInstant() to go to an Instant, and Date.from(instant) to come back. From the Instant you attach a zone with instant.atZone(zoneId) to get a ZonedDateTime. For Calendar, cal.toInstant(); for java.sql.Timestamp, ts.toInstant() or ts.toLocalDateTime() — though with JDBC 4.2 you should prefer getObject/setObject with java.time types directly.","correct":"Use date.toInstant() to go to an Instant, and Date.from(instant) to come back.","distractors":["Instant is a point on the machine timeline — nanoseconds from the 1970 UTC epoch, with no zone — while LocalDateTime is a wall-clock reading with no zone at all.","The local time does not exist, so java.time silently shifts it forward by the DST offset rather than throwing — 02:30 on a US spring-forward night becomes 03:30.","The local time occurs twice (once at the earlier offset, once at the later), and by default java.time picks the earlier offset."],"distractorIds":["java/java_time_datetime#20a46b8e2002","java/java_time_datetime#832eea8563c2","java/java_time_datetime#df26319bc3a7"],"concepts":["toinstant","convert","legacy","util","come","modern"],"questionMd":"How do you convert a legacy `java.util.Date` to modern `java.time` and back?","correctMd":"Use `date.toInstant()` to go to an `Instant`, and `Date.from(instant)` to come back.","answerFullMd":"Use `date.toInstant()` to go to an `Instant`, and `Date.from(instant)` to come back. From the `Instant` you attach a zone with `instant.atZone(zoneId)` to get a `ZonedDateTime`. For `Calendar`, `cal.toInstant()`; for `java.sql.Timestamp`, `ts.toInstant()` or `ts.toLocalDateTime()` — though with JDBC 4.2 you should prefer `getObject`/`setObject` with java.time types directly.","distractorsMd":["`Instant` is a point on the machine timeline — nanoseconds from the 1970 UTC epoch, with no zone — while `LocalDateTime` is a wall-clock reading with no zone at all.","The local time does not exist, so java.time silently shifts it forward by the DST offset rather than throwing — `02:30` on a US spring-forward night becomes `03:30`.",null]},{"id":"java/java_time_datetime#1c687e87e955","section":"java","module":"java/java_time_datetime","moduleName":"java time datetime","sourceFile":"README.md","difficulty":"advanced","question":"What is Clock and why would you inject it?","answerFull":"Clock is the abstraction java.time uses to read \"now,\" and injecting it makes time-dependent code deterministically testable. Instead of calling Instant.now() (untestable), you call Instant.now(clock) where clock is injected — production uses Clock.systemUTC() and tests use Clock.fixed(instant, zone) to freeze time. This turns flaky, sleep-based tests into reproducible ones.","correct":"Clock is the abstraction java.time uses to read \"now,\" and injecting it makes time-dependent code deterministically testable.","distractors":["Store the moment as UTC (an Instant/OffsetDateTime mapped to TIMESTAMP WITH TIME ZONE, i.e. timestamptz), and keep the user's zone separately only if you must reconstruct their local wall-clock intent.","Because LocalDateTime carries no time zone or offset, so it cannot identify a unique moment on the machine timeline.","The local time does not exist, so java.time silently shifts it forward by the DST offset rather than throwing — 02:30 on a US spring-forward night becomes 03:30."],"distractorIds":["java/java_time_datetime#4095836d5b8c","java/java_time_datetime#5ffd637a8d17","java/java_time_datetime#832eea8563c2"],"concepts":["deterministically","testable","injecting","dependent","inject","abstraction"],"questionMd":"What is `Clock` and why would you inject it?","correctMd":"`Clock` is the abstraction java.time uses to read \"now,\" and injecting it makes time-dependent code deterministically testable.","answerFullMd":"`Clock` is the abstraction java.time uses to read \"now,\" and injecting it makes time-dependent code deterministically testable. Instead of calling `Instant.now()` (untestable), you call `Instant.now(clock)` where `clock` is injected — production uses `Clock.systemUTC()` and tests use `Clock.fixed(instant, zone)` to freeze time. This turns flaky, sleep-based tests into reproducible ones.","distractorsMd":["Store the moment as UTC (an `Instant`/`OffsetDateTime` mapped to `TIMESTAMP WITH TIME ZONE`, i.e. `timestamptz`), and keep the user's zone separately only if you must reconstruct their local wall-clock intent.","Because `LocalDateTime` carries no time zone or offset, so it cannot identify a unique moment on the machine timeline.","The local time does not exist, so java.time silently shifts it forward by the DST offset rather than throwing — `02:30` on a US spring-forward night becomes `03:30`."]},{"id":"java/java_time_datetime#0462bdf7abe2","section":"java","module":"java/java_time_datetime","moduleName":"java time datetime","sourceFile":"README.md","difficulty":"advanced","question":"Are java.time types immutable and thread-safe?","answerFull":"Yes — every core java.time value type is immutable and thread-safe, so methods like plusDays(1) return a new object and never mutate the receiver. This is the opposite of Calendar, which mutates in place. Immutability means you can freely share instances (including static final formatters and constants) across threads without synchronization.","correct":"Yes — every core java.time value type is immutable and thread-safe, so methods like plusDays(1) return a new object and never mutate the receiver.","distractors":["ZonedDateTime carries a named ZoneId (like America/New_York) plus its live DST rules, whereas OffsetDateTime carries only a fixed ZoneOffset (like -05:00) with no rules.","The local time does not exist, so java.time silently shifts it forward by the DST offset rather than throwing — 02:30 on a US spring-forward night becomes 03:30.","A TemporalAdjuster is a reusable strategy that computes a new temporal value from an existing one, such as first day of next month or next Monday."],"distractorIds":["java/java_time_datetime#5da29b1a01ca","java/java_time_datetime#832eea8563c2","java/java_time_datetime#9faf95d2fa0a"],"concepts":["mutate","plusdays","receiver","core","safe","immutable"],"questionMd":"Are `java.time` types immutable and thread-safe?","correctMd":"Yes — every core `java.time` value type is immutable and thread-safe, so methods like `plusDays(1)` return a new object and never mutate the receiver.","answerFullMd":"Yes — every core `java.time` value type is immutable and thread-safe, so methods like `plusDays(1)` return a new object and never mutate the receiver. This is the opposite of `Calendar`, which mutates in place. Immutability means you can freely share instances (including `static final` formatters and constants) across threads without synchronization.","distractorsMd":["`ZonedDateTime` carries a named `ZoneId` (like `America/New_York`) plus its live DST rules, whereas `OffsetDateTime` carries only a fixed `ZoneOffset` (like `-05:00`) with no rules.","The local time does not exist, so java.time silently shifts it forward by the DST offset rather than throwing — `02:30` on a US spring-forward night becomes `03:30`.","A `TemporalAdjuster` is a reusable strategy that computes a new temporal value from an existing one, such as first day of next month or next Monday."]},{"id":"java/java_time_datetime#4095836d5b8c","section":"java","module":"java/java_time_datetime","moduleName":"java time datetime","sourceFile":"README.md","difficulty":"advanced","question":"How should you store timestamps in a database: UTC or zoned, and which SQL type?","answerFull":"Store the moment as UTC (an Instant/OffsetDateTime mapped to TIMESTAMP WITH TIME ZONE, i.e. timestamptz), and keep the user's zone separately only if you must reconstruct their local wall-clock intent. TIMESTAMP WITHOUT TIME ZONE maps to LocalDateTime and stores no offset, so it cannot identify a moment — avoid it for event times. Postgres timestamptz normalizes to UTC on write, mirroring the Instant pattern at the database layer.","correct":"Store the moment as UTC (an Instant/OffsetDateTime mapped to TIMESTAMP WITH TIME ZONE, i.e. timestamptz), and keep the user's zone separately only if you must reconstruct their local wall-clock intent.","distractors":["ZoneRules is java.time's view of the IANA tzdata database — the rules that map each zone's wall-clock time to a UTC offset over history.","Instant.now() returns the current moment on the machine timeline in UTC, while LocalDateTime.now() returns the current wall-clock reading in the JVM's default zone with the zone then discarded.","The local time occurs twice (once at the earlier offset, once at the later), and by default java.time picks the earlier offset."],"distractorIds":["java/java_time_datetime#8476d38dbbc7","java/java_time_datetime#201dacf0117b","java/java_time_datetime#df26319bc3a7"],"concepts":["offsetdatetime","reconstruct","timestamps","timestamptz","separately","intent"],"correctMd":"Store the moment as UTC (an `Instant`/`OffsetDateTime` mapped to `TIMESTAMP WITH TIME ZONE`, i.e. `timestamptz`), and keep the user's zone separately only if you must reconstruct their local wall-clock intent.","answerFullMd":"Store the moment as UTC (an `Instant`/`OffsetDateTime` mapped to `TIMESTAMP WITH TIME ZONE`, i.e. `timestamptz`), and keep the user's zone separately only if you must reconstruct their local wall-clock intent. `TIMESTAMP WITHOUT TIME ZONE` maps to `LocalDateTime` and stores no offset, so it cannot identify a moment — avoid it for event times. Postgres `timestamptz` normalizes to UTC on write, mirroring the `Instant` pattern at the database layer.","distractorsMd":["`ZoneRules` is java.time's view of the IANA tzdata database — the rules that map each zone's wall-clock time to a UTC offset over history.","`Instant.now()` returns the current moment on the machine timeline in UTC, while `LocalDateTime.now()` returns the current wall-clock reading in the JVM's default zone with the zone then discarded.",null]},{"id":"java/java_time_datetime#8476d38dbbc7","section":"java","module":"java/java_time_datetime","moduleName":"java time datetime","sourceFile":"README.md","difficulty":"advanced","question":"What are tzdata and ZoneRules, and why do updates matter?","answerFull":"ZoneRules is java.time's view of the IANA tzdata database — the rules that map each zone's wall-clock time to a UTC offset over history. When a government changes DST rules (e.g. abolishing DST or shifting a transition date), the JDK ships an updated tzdata file so ZonedDateTime arithmetic stays correct without code changes. Keep the JDK (or apply tzupdater) current, because a stale tzdata file produces wrong offsets for the changed region.","correct":"ZoneRules is java.time's view of the IANA tzdata database — the rules that map each zone's wall-clock time to a UTC offset over history.","distractors":["Instant.now() returns the current moment on the machine timeline in UTC, while LocalDateTime.now() returns the current wall-clock reading in the JVM's default zone with the zone then discarded.","Store the moment as UTC (an Instant/OffsetDateTime mapped to TIMESTAMP WITH TIME ZONE, i.e. timestamptz), and keep the user's zone separately only if you must reconstruct their local wall-clock intent.","Clock is the abstraction java.time uses to read \"now,\" and injecting it makes time-dependent code deterministically testable."],"distractorIds":["java/java_time_datetime#201dacf0117b","java/java_time_datetime#4095836d5b8c","java/java_time_datetime#1c687e87e955"],"concepts":["iana","zonerules","tzdata","matter","utc","wall"],"questionMd":"What are tzdata and `ZoneRules`, and why do updates matter?","correctMd":"`ZoneRules` is java.time's view of the IANA tzdata database — the rules that map each zone's wall-clock time to a UTC offset over history.","answerFullMd":"`ZoneRules` is java.time's view of the IANA tzdata database — the rules that map each zone's wall-clock time to a UTC offset over history. When a government changes DST rules (e.g. abolishing DST or shifting a transition date), the JDK ships an updated tzdata file so `ZonedDateTime` arithmetic stays correct without code changes. Keep the JDK (or apply `tzupdater`) current, because a stale tzdata file produces wrong offsets for the changed region.","distractorsMd":["`Instant.now()` returns the current moment on the machine timeline in UTC, while `LocalDateTime.now()` returns the current wall-clock reading in the JVM's default zone with the zone then discarded.","Store the moment as UTC (an `Instant`/`OffsetDateTime` mapped to `TIMESTAMP WITH TIME ZONE`, i.e. `timestamptz`), and keep the user's zone separately only if you must reconstruct their local wall-clock intent.","`Clock` is the abstraction java.time uses to read \"now,\" and injecting it makes time-dependent code deterministically testable."]},{"id":"java/java_time_datetime#201dacf0117b","section":"java","module":"java/java_time_datetime","moduleName":"java time datetime","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between Instant.now() and LocalDateTime.now()?","answerFull":"Instant.now() returns the current moment on the machine timeline in UTC, while LocalDateTime.now() returns the current wall-clock reading in the JVM's default zone with the zone then discarded. The LocalDateTime result depends on ZoneId.systemDefault(), which varies by host, making it unreliable in server code. Prefer Instant.now() for events and pass an explicit zone (LocalDate.now(zoneId)) when you need a local calendar value.","correct":"Instant.now() returns the current moment on the machine timeline in UTC, while LocalDateTime.now() returns the current wall-clock reading in the JVM's default zone with the zone then discarded.","distractors":["Clock is the abstraction java.time uses to read \"now,\" and injecting it makes time-dependent code deterministically testable.","Because they are mutable (hence not thread-safe), have an error-prone API (0-indexed months, so December is 11), and conflate the machine and human timelines with no clear zone model.","Use date.toInstant() to go to an Instant, and Date.from(instant) to come back."],"distractorIds":["java/java_time_datetime#1c687e87e955","java/java_time_datetime#6cdf402af1c8","java/java_time_datetime#e033c697857a"],"concepts":["discarded","localdatetime","utc","difference","timeline","wall"],"questionMd":"What is the difference between `Instant.now()` and `LocalDateTime.now()`?","correctMd":"`Instant.now()` returns the current moment on the machine timeline in UTC, while `LocalDateTime.now()` returns the current wall-clock reading in the JVM's default zone with the zone then discarded.","answerFullMd":"`Instant.now()` returns the current moment on the machine timeline in UTC, while `LocalDateTime.now()` returns the current wall-clock reading in the JVM's default zone with the zone then discarded. The `LocalDateTime` result depends on `ZoneId.systemDefault()`, which varies by host, making it unreliable in server code. Prefer `Instant.now()` for events and pass an explicit zone (`LocalDate.now(zoneId)`) when you need a local calendar value.","distractorsMd":["`Clock` is the abstraction java.time uses to read \"now,\" and injecting it makes time-dependent code deterministically testable.","Because they are mutable (hence not thread-safe), have an error-prone API (0-indexed months, so December is `11`), and conflate the machine and human timelines with no clear zone model.","Use `date.toInstant()` to go to an `Instant`, and `Date.from(instant)` to come back."]},{"id":"java/java_time_datetime#b7d5db05d06f","section":"java","module":"java/java_time_datetime","moduleName":"java time datetime","sourceFile":"README.md","difficulty":"advanced","question":"Why does LocalDate.of(2026, 1, 31).plusMonths(1) return February 28 instead of throwing?","answerFull":"Because java.time resolves an invalid day-of-month to the last valid day of the target month rather than overflowing or throwing. January 31 plus one month has no February 31, so it clamps to February 28 (or 29 in a leap year). If you actually mean \"end of month,\" use TemporalAdjusters.lastDayOfMonth() to express that intent explicitly instead of relying on the clamp.","correct":"Because java.time resolves an invalid day-of-month to the last valid day of the target month rather than overflowing or throwing.","distractors":["Store the moment as UTC (an Instant/OffsetDateTime mapped to TIMESTAMP WITH TIME ZONE, i.e. timestamptz), and keep the user's zone separately only if you must reconstruct their local wall-clock intent.","The local time does not exist, so java.time silently shifts it forward by the DST offset rather than throwing — 02:30 on a US spring-forward night becomes 03:30.","The local time occurs twice (once at the earlier offset, once at the later), and by default java.time picks the earlier offset."],"distractorIds":["java/java_time_datetime#4095836d5b8c","java/java_time_datetime#832eea8563c2","java/java_time_datetime#df26319bc3a7"],"concepts":["overflowing","localdate","month","throwing","invalid","day"],"questionMd":"Why does `LocalDate.of(2026, 1, 31).plusMonths(1)` return February 28 instead of throwing?","answerFullMd":"Because java.time resolves an invalid day-of-month to the last valid day of the target month rather than overflowing or throwing. January 31 plus one month has no February 31, so it clamps to February 28 (or 29 in a leap year). If you actually mean \"end of month,\" use `TemporalAdjusters.lastDayOfMonth()` to express that intent explicitly instead of relying on the clamp.","distractorsMd":["Store the moment as UTC (an `Instant`/`OffsetDateTime` mapped to `TIMESTAMP WITH TIME ZONE`, i.e. `timestamptz`), and keep the user's zone separately only if you must reconstruct their local wall-clock intent.","The local time does not exist, so java.time silently shifts it forward by the DST offset rather than throwing — `02:30` on a US spring-forward night becomes `03:30`.",null]},{"id":"java/java_time_datetime#9faf95d2fa0a","section":"java","module":"java/java_time_datetime","moduleName":"java time datetime","sourceFile":"README.md","difficulty":"advanced","question":"What is a TemporalAdjuster and when would you use one?","answerFull":"A TemporalAdjuster is a reusable strategy that computes a new temporal value from an existing one, such as first day of next month or next Monday. The TemporalAdjusters factory ships common ones (firstDayOfMonth(), next(DayOfWeek.MONDAY), lastInMonth(...)), and you apply them with date.with(adjuster). They replace ad-hoc loops for calendar navigation and are handy for business rules like \"invoice on the last business day of the month.\"","correct":"A TemporalAdjuster is a reusable strategy that computes a new temporal value from an existing one, such as first day of next month or next Monday.","distractors":["ZonedDateTime carries a named ZoneId (like America/New_York) plus its live DST rules, whereas OffsetDateTime carries only a fixed ZoneOffset (like -05:00) with no rules.","Because java.time resolves an invalid day-of-month to the last valid day of the target month rather than overflowing or throwing.","Because Period is calendar (human) arithmetic and Duration is elapsed real time (machine) arithmetic, and across a DST boundary a calendar day is 23 or 25 real hours, not 24."],"distractorIds":["java/java_time_datetime#5da29b1a01ca","java/java_time_datetime#b7d5db05d06f","java/java_time_datetime#bce5a8646c9e"],"concepts":["monday","temporaladjuster","month","reusable","temporal","day"],"questionMd":"What is a `TemporalAdjuster` and when would you use one?","correctMd":"A `TemporalAdjuster` is a reusable strategy that computes a new temporal value from an existing one, such as first day of next month or next Monday.","answerFullMd":"A `TemporalAdjuster` is a reusable strategy that computes a new temporal value from an existing one, such as first day of next month or next Monday. The `TemporalAdjusters` factory ships common ones (`firstDayOfMonth()`, `next(DayOfWeek.MONDAY)`, `lastInMonth(...)`), and you apply them with `date.with(adjuster)`. They replace ad-hoc loops for calendar navigation and are handy for business rules like \"invoice on the last business day of the month.\"","distractorsMd":["`ZonedDateTime` carries a named `ZoneId` (like `America/New_York`) plus its live DST rules, whereas `OffsetDateTime` carries only a fixed `ZoneOffset` (like `-05:00`) with no rules.",null,"Because `Period` is calendar (human) arithmetic and `Duration` is elapsed real time (machine) arithmetic, and across a DST boundary a calendar day is 23 or 25 real hours, not 24."]},{"id":"java/java_time_datetime#1c086635b55c","section":"java","module":"java/java_time_datetime","moduleName":"java time datetime","sourceFile":"README.md","difficulty":"advanced","question":"How do you compute the number of whole days or hours between two temporal values?","answerFull":"Use ChronoUnit for a single unit — ChronoUnit.DAYS.between(start, end) or ChronoUnit.HOURS.between(a, b) — which returns a long count. For a full breakdown into years/months/days use Period.between(localDate1, localDate2), and for elapsed real time use Duration.between(instant1, instant2). Match the tool to the question: ChronoUnit for one unit, Period for calendar fields, Duration for machine time.","correct":"Use ChronoUnit for a single unit — ChronoUnit.DAYS.between(start, end) or ChronoUnit.HOURS.between(a, b) — which returns a long count.","distractors":["Because Period is calendar (human) arithmetic and Duration is elapsed real time (machine) arithmetic, and across a DST boundary a calendar day is 23 or 25 real hours, not 24.","A ZoneId is a named region whose UTC offset changes over the year according to tzdata rules; a ZoneOffset is a single fixed gap from UTC with no DST behavior.","A TemporalAdjuster is a reusable strategy that computes a new temporal value from an existing one, such as first day of next month or next Monday."],"distractorIds":["java/java_time_datetime#bce5a8646c9e","java/java_time_datetime#13654ead279b","java/java_time_datetime#9faf95d2fa0a"],"concepts":["chronounit","days","temporal","hours","whole","unit"],"correctMd":"Use `ChronoUnit` for a single unit — `ChronoUnit.DAYS.between(start, end)` or `ChronoUnit.HOURS.between(a, b)` — which returns a `long` count.","answerFullMd":"Use `ChronoUnit` for a single unit — `ChronoUnit.DAYS.between(start, end)` or `ChronoUnit.HOURS.between(a, b)` — which returns a `long` count. For a full breakdown into years/months/days use `Period.between(localDate1, localDate2)`, and for elapsed real time use `Duration.between(instant1, instant2)`. Match the tool to the question: `ChronoUnit` for one unit, `Period` for calendar fields, `Duration` for machine time.","distractorsMd":["Because `Period` is calendar (human) arithmetic and `Duration` is elapsed real time (machine) arithmetic, and across a DST boundary a calendar day is 23 or 25 real hours, not 24.","A `ZoneId` is a named region whose UTC offset changes over the year according to tzdata rules; a `ZoneOffset` is a single fixed gap from UTC with no DST behavior.","A `TemporalAdjuster` is a reusable strategy that computes a new temporal value from an existing one, such as first day of next month or next Monday."]},{"id":"java/jdbc_and_database#c611e5f3f518","section":"java","module":"java/jdbc_and_database","moduleName":"jdbc and database","sourceFile":"README.md","difficulty":"core","question":"Why use PreparedStatement over Statement?","answerFull":"Three reasons: (1) SQL injection prevention — parameters are bound separately and never interpreted as SQL, so ' OR 1=1 -- is treated as a literal string value. (2) Server-side query plan caching — the DB parses and optimizes the parameterized query once and reuses the plan for all subsequent executions with different parameter values. For a query executed 1000 times/second, this saves 1000 parse cycles/second. (3) Type safety — setString(), setInt(), setBigDecimal() handle proper type conversion and escaping; string concatenation in Statement is error-prone.","correct":"Three reasons: (1) SQL injection prevention — parameters are bound separately and never interpreted as SQL, so ' OR 1=1 -- is treated as a literal string value.","distractors":["MySQL: create Statement with TYPE_FORWARD_ONLY + CONCUR_READ_ONLY, then stmt.setFetchSize(Integer.MIN_VALUE) — this magic value enables streaming mode where rows are delivered one at a time from the server.","If not using try-with-resources: the Connection is never returned to the pool (if using HikariCP) or the TCP socket to the DB is never closed.","A Savepoint marks a point within a transaction to which you can partially roll back."],"distractorIds":["java/jdbc_and_database#8bf1a1bab076","java/jdbc_and_database#fc77845c6b89","java/jdbc_and_database#0331d2311fdc"],"concepts":["interpreted","preparedstatement","prevention","separately","literal","reasons"],"questionMd":"Why use `PreparedStatement` over `Statement`?","correctMd":"Three reasons: (1) SQL injection prevention — parameters are bound separately and never interpreted as SQL, so `' OR 1=1 --` is treated as a literal string value.","answerFullMd":"Three reasons: (1) SQL injection prevention — parameters are bound separately and never interpreted as SQL, so `' OR 1=1 --` is treated as a literal string value. (2) Server-side query plan caching — the DB parses and optimizes the parameterized query once and reuses the plan for all subsequent executions with different parameter values. For a query executed 1000 times/second, this saves 1000 parse cycles/second. (3) Type safety — `setString()`, `setInt()`, `setBigDecimal()` handle proper type conversion and escaping; string concatenation in `Statement` is error-prone.","distractorsMd":["MySQL: create `Statement` with `TYPE_FORWARD_ONLY + CONCUR_READ_ONLY`, then `stmt.setFetchSize(Integer.MIN_VALUE)` — this magic value enables streaming mode where rows are delivered one at a time from the server.","If not using `try-with-resources`: the `Connection` is never returned to the pool (if using HikariCP) or the TCP socket to the DB is never closed.","A `Savepoint` marks a point within a transaction to which you can partially roll back."]},{"id":"java/jdbc_and_database#923b919fc002","section":"java","module":"java/jdbc_and_database","moduleName":"jdbc and database","sourceFile":"README.md","difficulty":"core","question":"What is connection pool exhaustion and how do you detect it?","answerFull":"Pool exhaustion: all connections in the pool are in use (not returned); new requests block waiting for getConnection() until connectionTimeout is exceeded, then fail with PoolTimeoutException. Detection: (1) HikariCP leakDetectionThreshold logs a warning if a connection is held longer than the threshold. (2) Monitor HikariPoolMXBean.getThreadsAwaitingConnection() — should always be 0. (3) Application latency spikes to exactly connectionTimeout duration. Root causes: unclosed connections (try-with-resources prevents this), long-running transactions holding connections, pool sized too small for load.","correct":"Pool exhaustion: all connections in the pool are in use (not returned); new requests block waiting for getConnection() until connectionTimeout is exceeded, then fail with PoolTimeoutException.","distractors":["A dirty connection is one returned to the pool with uncommitted transaction state — autoCommit=false and an open transaction.","If not using try-with-resources: the Connection is never returned to the pool (if using HikariCP) or the TCP socket to the DB is never closed.","connections = (core_count * 2) + effective_spindle_count."],"distractorIds":["java/jdbc_and_database#dcb5f766d9f3","java/jdbc_and_database#fc77845c6b89","java/jdbc_and_database#bdc45e402dc1"],"concepts":["exhaustion","getconnection","pooltimeoutexception","connectiontimeout","exceeded","detect"],"correctMd":"Pool exhaustion: all connections in the pool are in use (not returned); new requests block waiting for `getConnection()` until `connectionTimeout` is exceeded, then fail with `PoolTimeoutException`.","answerFullMd":"Pool exhaustion: all connections in the pool are in use (not returned); new requests block waiting for `getConnection()` until `connectionTimeout` is exceeded, then fail with `PoolTimeoutException`. Detection: (1) HikariCP `leakDetectionThreshold` logs a warning if a connection is held longer than the threshold. (2) Monitor `HikariPoolMXBean.getThreadsAwaitingConnection()` — should always be 0. (3) Application latency spikes to exactly `connectionTimeout` duration. Root causes: unclosed connections (`try-with-resources` prevents this), long-running transactions holding connections, pool sized too small for load.","distractorsMd":["A dirty connection is one returned to the pool with uncommitted transaction state — `autoCommit=false` and an open transaction.","If not using `try-with-resources`: the `Connection` is never returned to the pool (if using HikariCP) or the TCP socket to the DB is never closed.","`connections = (core_count * 2) + effective_spindle_count`."]},{"id":"java/jdbc_and_database#3920b15c6bfa","section":"java","module":"java/jdbc_and_database","moduleName":"jdbc and database","sourceFile":"README.md","difficulty":"core","question":"Describe the 4 transaction isolation levels and their anomalies.","answerFull":"READ_UNCOMMITTED: allows dirty reads (seeing uncommitted data), non-repeatable reads, and phantom reads. Almost never used. READ_COMMITTED (PostgreSQL, Oracle default): prevents dirty reads; allows non-repeatable reads and phantom reads. Reads only see committed data. REPEATABLE_READ (MySQL InnoDB default): prevents dirty reads and non-repeatable reads; allows phantom reads in standard SQL (MySQL's InnoDB prevents them too with gap locks). A snapshot is taken at transaction start. SERIALIZABLE: prevents all anomalies; transactions appear to execute serially. Uses range locks/predicate locks; lowest throughput; highest deadlock risk.","correct":"READ_UNCOMMITTED: allows dirty reads (seeing uncommitted data), non-repeatable reads, and phantom reads.","distractors":["TYPE_FORWARD_ONLY (default) with CONCUR_READ_ONLY — it is the fastest and most efficient.","MySQL: create Statement with TYPE_FORWARD_ONLY + CONCUR_READ_ONLY, then stmt.setFetchSize(Integer.MIN_VALUE) — this magic value enables streaming mode where rows are delivered one at a time from the server.","A Savepoint marks a point within a transaction to which you can partially roll back."],"distractorIds":["java/jdbc_and_database#acc3ba2bedfd","java/jdbc_and_database#8bf1a1bab076","java/jdbc_and_database#0331d2311fdc"],"concepts":["describe","seeing","phantom","uncommitted","anomalies","repeatable"],"distractorsMd":["`TYPE_FORWARD_ONLY` (default) with `CONCUR_READ_ONLY` — it is the fastest and most efficient.","MySQL: create `Statement` with `TYPE_FORWARD_ONLY + CONCUR_READ_ONLY`, then `stmt.setFetchSize(Integer.MIN_VALUE)` — this magic value enables streaming mode where rows are delivered one at a time from the server.","A `Savepoint` marks a point within a transaction to which you can partially roll back."]},{"id":"java/jdbc_and_database#bdc45e402dc1","section":"java","module":"java/jdbc_and_database","moduleName":"jdbc and database","sourceFile":"README.md","difficulty":"core","question":"What is the HikariCP recommended pool size formula?","answerFull":"connections = (core_count * 2) + effective_spindle_count. For a 4-core application server with SSD storage (1 spindle equivalent): 4×2 + 1 = 9 connections. This is much smaller than most teams assume. The reasoning: more connections than CPU cores creates contention on the DB server — each connection competes for DB CPU time, and the overhead of managing many connections outweighs the parallelism benefit. Start with this formula and tune based on pg_stat_activity or equivalent monitoring. Beyond ~15 connections per service instance, most relational DBs see degraded throughput.","correct":"connections = (core_count * 2) + effective_spindle_count.","distractors":["If not using try-with-resources: the Connection is never returned to the pool (if using HikariCP) or the TCP socket to the DB is never closed.","A dirty connection is one returned to the pool with uncommitted transaction state — autoCommit=false and an open transaction.","Pool exhaustion: all connections in the pool are in use (not returned); new requests block waiting for getConnection() until connectionTimeout is exceeded, then fail with PoolTimeoutException."],"distractorIds":["java/jdbc_and_database#fc77845c6b89","java/jdbc_and_database#dcb5f766d9f3","java/jdbc_and_database#923b919fc002"],"concepts":["spindle","recommended","formula","hikaricp","effective","core"],"correctMd":"`connections = (core_count * 2) + effective_spindle_count`.","answerFullMd":"`connections = (core_count * 2) + effective_spindle_count`. For a 4-core application server with SSD storage (1 spindle equivalent): 4×2 + 1 = 9 connections. This is much smaller than most teams assume. The reasoning: more connections than CPU cores creates contention on the DB server — each connection competes for DB CPU time, and the overhead of managing many connections outweighs the parallelism benefit. Start with this formula and tune based on `pg_stat_activity` or equivalent monitoring. Beyond ~15 connections per service instance, most relational DBs see degraded throughput.","distractorsMd":["If not using `try-with-resources`: the `Connection` is never returned to the pool (if using HikariCP) or the TCP socket to the DB is never closed.","A dirty connection is one returned to the pool with uncommitted transaction state — `autoCommit=false` and an open transaction.","Pool exhaustion: all connections in the pool are in use (not returned); new requests block waiting for `getConnection()` until `connectionTimeout` is exceeded, then fail with `PoolTimeoutException`."]},{"id":"java/jdbc_and_database#8bf1a1bab076","section":"java","module":"java/jdbc_and_database","moduleName":"jdbc and database","sourceFile":"README.md","difficulty":"core","question":"How do you stream large result sets without OOM?","answerFull":"MySQL: create Statement with TYPE_FORWARD_ONLY + CONCUR_READ_ONLY, then stmt.setFetchSize(Integer.MIN_VALUE) — this magic value enables streaming mode where rows are delivered one at a time from the server. PostgreSQL: disable autoCommit (conn.setAutoCommit(false)) then ps.setFetchSize(1000) — PostgreSQL uses a server-side cursor, delivering results in 1000-row batches. Both approaches keep memory at O(fetchSize) rows rather than O(total rows). Trade-off: the transaction/connection must stay open for the duration of iteration; don't stream inside a short-lived connection from a pool without ensuring the connection outlives the iteration.","correct":"MySQL: create Statement with TYPE_FORWARD_ONLY + CONCUR_READ_ONLY, then stmt.setFetchSize(Integer.MIN_VALUE) — this magic value enables streaming mode where rows are delivered one at a time from the server.","distractors":["executeBatch() sends all accumulated statements in one or a few network round trips to the DB server, which processes them in bulk.","READ_UNCOMMITTED: allows dirty reads (seeing uncommitted data), non-repeatable reads, and phantom reads.","TYPE_FORWARD_ONLY (default) with CONCUR_READ_ONLY — it is the fastest and most efficient."],"distractorIds":["java/jdbc_and_database#b4946480550a","java/jdbc_and_database#3920b15c6bfa","java/jdbc_and_database#acc3ba2bedfd"],"concepts":["magic","setfetchsize","concur","stmt","delivered","oom"],"correctMd":"MySQL: create `Statement` with `TYPE_FORWARD_ONLY + CONCUR_READ_ONLY`, then `stmt.setFetchSize(Integer.MIN_VALUE)` — this magic value enables streaming mode where rows are delivered one at a time from the server.","answerFullMd":"MySQL: create `Statement` with `TYPE_FORWARD_ONLY + CONCUR_READ_ONLY`, then `stmt.setFetchSize(Integer.MIN_VALUE)` — this magic value enables streaming mode where rows are delivered one at a time from the server. PostgreSQL: disable autoCommit (`conn.setAutoCommit(false)`) then `ps.setFetchSize(1000)` — PostgreSQL uses a server-side cursor, delivering results in 1000-row batches. Both approaches keep memory at O(fetchSize) rows rather than O(total rows). Trade-off: the transaction/connection must stay open for the duration of iteration; don't stream inside a short-lived connection from a pool without ensuring the connection outlives the iteration.","distractorsMd":["`executeBatch()` sends all accumulated statements in one or a few network round trips to the DB server, which processes them in bulk.",null,"`TYPE_FORWARD_ONLY` (default) with `CONCUR_READ_ONLY` — it is the fastest and most efficient."]},{"id":"java/jdbc_and_database#fc77845c6b89","section":"java","module":"java/jdbc_and_database","moduleName":"jdbc and database","sourceFile":"README.md","difficulty":"intermediate","question":"What happens if you forget to close a Connection?","answerFull":"If not using try-with-resources: the Connection is never returned to the pool (if using HikariCP) or the TCP socket to the DB is never closed. Pool consequence: the connection is \"leaked\" — counted as active, never recycled. After enough leaks, the pool is exhausted and all new requests time out. TCP socket consequence: the OS eventually closes the socket via TCP keepalive (hours), but the pool doesn't know — it still counts the connection as in-use. HikariCP.setLeakDetectionThreshold(2000) logs a stack trace if a connection is held > 2 seconds, pointing to the offending code.","correct":"If not using try-with-resources: the Connection is never returned to the pool (if using HikariCP) or the TCP socket to the DB is never closed.","distractors":["Three reasons: (1) SQL injection prevention — parameters are bound separately and never interpreted as SQL, so ' OR 1=1 -- is treated as a literal string value.","Pool exhaustion: all connections in the pool are in use (not returned); new requests block waiting for getConnection() until connectionTimeout is exceeded, then fail with PoolTimeoutException.","A dirty connection is one returned to the pool with uncommitted transaction state — autoCommit=false and an open transaction."],"distractorIds":["java/jdbc_and_database#c611e5f3f518","java/jdbc_and_database#923b919fc002","java/jdbc_and_database#dcb5f766d9f3"],"concepts":["forget","hikaricp","socket","close","try","returned"],"questionMd":"What happens if you forget to close a `Connection`?","correctMd":"If not using `try-with-resources`: the `Connection` is never returned to the pool (if using HikariCP) or the TCP socket to the DB is never closed.","answerFullMd":"If not using `try-with-resources`: the `Connection` is never returned to the pool (if using HikariCP) or the TCP socket to the DB is never closed. Pool consequence: the connection is \"leaked\" — counted as active, never recycled. After enough leaks, the pool is exhausted and all new requests time out. TCP socket consequence: the OS eventually closes the socket via TCP keepalive (hours), but the pool doesn't know — it still counts the connection as in-use. `HikariCP.setLeakDetectionThreshold(2000)` logs a stack trace if a connection is held > 2 seconds, pointing to the offending code.","distractorsMd":["Three reasons: (1) SQL injection prevention — parameters are bound separately and never interpreted as SQL, so `' OR 1=1 --` is treated as a literal string value.","Pool exhaustion: all connections in the pool are in use (not returned); new requests block waiting for `getConnection()` until `connectionTimeout` is exceeded, then fail with `PoolTimeoutException`.","A dirty connection is one returned to the pool with uncommitted transaction state — `autoCommit=false` and an open transaction."]},{"id":"java/jdbc_and_database#85ff066a9dca","section":"java","module":"java/jdbc_and_database","moduleName":"jdbc and database","sourceFile":"README.md","difficulty":"intermediate","question":"What are distributed transactions (XA) and when do you need them?","answerFull":"XA transactions coordinate a commit/rollback across multiple transactional resources (two databases, a database and a JMS queue) atomically. They use a two-phase commit (2PC) protocol: (1) Prepare phase — coordinator asks all resources if they can commit; (2) Commit phase — if all say yes, coordinator tells all to commit. In Java, implemented via javax.transaction.XAResource, XADataSource, and a JTA transaction manager (Atomikos, Bitronix). When needed: orders must be both saved to DB AND published to MQ atomically (both or neither). Cost: 2PC adds latency (two round trips) and complexity; blocked coordinator causes all resources to wait (heuristic failure). Modern alternatives: saga pattern (compensating transactions), outbox pattern (event stored in same DB as data, then forwarded by CDC).","correct":"XA transactions coordinate a commit/rollback across multiple transactional resources (two databases, a database and a JMS queue) atomically.","distractors":["If not using try-with-resources: the Connection is never returned to the pool (if using HikariCP) or the TCP socket to the DB is never closed.","A Savepoint marks a point within a transaction to which you can partially roll back.","A dirty connection is one returned to the pool with uncommitted transaction state — autoCommit=false and an open transaction."],"distractorIds":["java/jdbc_and_database#fc77845c6b89","java/jdbc_and_database#0331d2311fdc","java/jdbc_and_database#dcb5f766d9f3"],"concepts":["jms","coordinate","atomically","transactional","rollback","databases"],"answerFullMd":"XA transactions coordinate a commit/rollback across multiple transactional resources (two databases, a database and a JMS queue) atomically. They use a two-phase commit (2PC) protocol: (1) Prepare phase — coordinator asks all resources if they can commit; (2) Commit phase — if all say yes, coordinator tells all to commit. In Java, implemented via `javax.transaction.XAResource`, `XADataSource`, and a JTA transaction manager (Atomikos, Bitronix). When needed: orders must be both saved to DB AND published to MQ atomically (both or neither). Cost: 2PC adds latency (two round trips) and complexity; blocked coordinator causes all resources to wait (heuristic failure). Modern alternatives: saga pattern (compensating transactions), outbox pattern (event stored in same DB as data, then forwarded by CDC).","distractorsMd":["If not using `try-with-resources`: the `Connection` is never returned to the pool (if using HikariCP) or the TCP socket to the DB is never closed.","A `Savepoint` marks a point within a transaction to which you can partially roll back.","A dirty connection is one returned to the pool with uncommitted transaction state — `autoCommit=false` and an open transaction."]},{"id":"java/jdbc_and_database#b4946480550a","section":"java","module":"java/jdbc_and_database","moduleName":"jdbc and database","sourceFile":"README.md","difficulty":"intermediate","question":"How does executeBatch() differ from individual executeUpdate() calls in terms of performance?","answerFull":"executeBatch() sends all accumulated statements in one or a few network round trips to the DB server, which processes them in bulk. Individual executeUpdate() sends one statement per round trip — N inserts = N round trips. Network latency dominates: at 1ms round-trip time, 1000 individual inserts take 1000ms minimum; 1000 inserts in one batch take ~1ms + DB processing time. Additionally, the DB may optimize the batch internally (single WAL write for the batch instead of N separate WAL writes). The BatchUpdateException complicates error handling — getUpdateCounts() shows which rows in the batch succeeded before failure, enabling selective retry.","correct":"executeBatch() sends all accumulated statements in one or a few network round trips to the DB server, which processes them in bulk.","distractors":["MySQL: create Statement with TYPE_FORWARD_ONLY + CONCUR_READ_ONLY, then stmt.setFetchSize(Integer.MIN_VALUE) — this magic value enables streaming mode where rows are delivered one at a time from the server.","A Savepoint marks a point within a transaction to which you can partially roll back.","A dirty connection is one returned to the pool with uncommitted transaction state — autoCommit=false and an open transaction."],"distractorIds":["java/jdbc_and_database#8bf1a1bab076","java/jdbc_and_database#0331d2311fdc","java/jdbc_and_database#dcb5f766d9f3"],"concepts":["executebatch","executeupdate","trips","accumulated","bulk","differ"],"questionMd":"How does `executeBatch()` differ from individual `executeUpdate()` calls in terms of performance?","correctMd":"`executeBatch()` sends all accumulated statements in one or a few network round trips to the DB server, which processes them in bulk.","answerFullMd":"`executeBatch()` sends all accumulated statements in one or a few network round trips to the DB server, which processes them in bulk. Individual `executeUpdate()` sends one statement per round trip — N inserts = N round trips. Network latency dominates: at 1ms round-trip time, 1000 individual inserts take 1000ms minimum; 1000 inserts in one batch take ~1ms + DB processing time. Additionally, the DB may optimize the batch internally (single WAL write for the batch instead of N separate WAL writes). The `BatchUpdateException` complicates error handling — `getUpdateCounts()` shows which rows in the batch succeeded before failure, enabling selective retry.","distractorsMd":["MySQL: create `Statement` with `TYPE_FORWARD_ONLY + CONCUR_READ_ONLY`, then `stmt.setFetchSize(Integer.MIN_VALUE)` — this magic value enables streaming mode where rows are delivered one at a time from the server.","A `Savepoint` marks a point within a transaction to which you can partially roll back.","A dirty connection is one returned to the pool with uncommitted transaction state — `autoCommit=false` and an open transaction."]},{"id":"java/jdbc_and_database#0331d2311fdc","section":"java","module":"java/jdbc_and_database","moduleName":"jdbc and database","sourceFile":"README.md","difficulty":"intermediate","question":"What is Savepoint and how does it differ from a regular rollback?","answerFull":"A Savepoint marks a point within a transaction to which you can partially roll back. conn.rollback() undoes ALL work since setAutoCommit(false). conn.rollback(savepoint) undoes work done AFTER the savepoint but keeps work done before it. Use savepoints when a transaction has a main critical path plus optional enrichment steps — if enrichment fails, roll back only the enrichment and commit the main path. Example: save an order (critical), try to add loyalty points (optional). If loyalty service fails, rollback to the savepoint (undo the loyalty points insert), then commit the order. Savepoints are part of the same database transaction and do not release any locks.","correct":"A Savepoint marks a point within a transaction to which you can partially roll back.","distractors":["XA transactions coordinate a commit/rollback across multiple transactional resources (two databases, a database and a JMS queue) atomically.","A dirty connection is one returned to the pool with uncommitted transaction state — autoCommit=false and an open transaction.","By default, most JDBC drivers buffer the entire ResultSet in the client JVM heap."],"distractorIds":["java/jdbc_and_database#85ff066a9dca","java/jdbc_and_database#dcb5f766d9f3","java/jdbc_and_database#bd2453d83819"],"concepts":["savepoint","partially","roll","regular","differ","rollback"],"questionMd":"What is `Savepoint` and how does it differ from a regular rollback?","correctMd":"A `Savepoint` marks a point within a transaction to which you can partially roll back.","answerFullMd":"A `Savepoint` marks a point within a transaction to which you can partially roll back. `conn.rollback()` undoes ALL work since `setAutoCommit(false)`. `conn.rollback(savepoint)` undoes work done AFTER the savepoint but keeps work done before it. Use savepoints when a transaction has a main critical path plus optional enrichment steps — if enrichment fails, roll back only the enrichment and commit the main path. Example: save an order (critical), try to add loyalty points (optional). If loyalty service fails, rollback to the savepoint (undo the loyalty points insert), then commit the order. Savepoints are part of the same database transaction and do not release any locks.","distractorsMd":[null,"A dirty connection is one returned to the pool with uncommitted transaction state — `autoCommit=false` and an open transaction.","By default, most JDBC drivers buffer the entire `ResultSet` in the client JVM heap."]},{"id":"java/jdbc_and_database#acc3ba2bedfd","section":"java","module":"java/jdbc_and_database","moduleName":"jdbc and database","sourceFile":"README.md","difficulty":"intermediate","question":"What ResultSet type should you use for a standard OLTP query?","answerFull":"TYPE_FORWARD_ONLY (default) with CONCUR_READ_ONLY — it is the fastest and most efficient. Forward-only means you can only call next() — no previous(), absolute(), or relative(). Read-only means no updateRow() or insertRow(). The JDBC driver can use a server-side cursor or buffer efficiently without supporting bidirectional navigation. TYPE_SCROLL_INSENSITIVE is needed only if you need random access within the result set (rare for OLTP). CONCUR_UPDATABLE is needed for ResultSet-based updates (almost never used in modern code — use separate UPDATE statements).","correct":"TYPE_FORWARD_ONLY (default) with CONCUR_READ_ONLY — it is the fastest and most efficient.","distractors":["READ_UNCOMMITTED: allows dirty reads (seeing uncommitted data), non-repeatable reads, and phantom reads.","By default, most JDBC drivers buffer the entire ResultSet in the client JVM heap.","MySQL: create Statement with TYPE_FORWARD_ONLY + CONCUR_READ_ONLY, then stmt.setFetchSize(Integer.MIN_VALUE) — this magic value enables streaming mode where rows are delivered one at a time from the server."],"distractorIds":["java/jdbc_and_database#3920b15c6bfa","java/jdbc_and_database#bd2453d83819","java/jdbc_and_database#8bf1a1bab076"],"concepts":["resultset","concur","fastest","efficient","oltp","forward"],"questionMd":"What `ResultSet` type should you use for a standard OLTP query?","correctMd":"`TYPE_FORWARD_ONLY` (default) with `CONCUR_READ_ONLY` — it is the fastest and most efficient.","answerFullMd":"`TYPE_FORWARD_ONLY` (default) with `CONCUR_READ_ONLY` — it is the fastest and most efficient. Forward-only means you can only call `next()` — no `previous()`, `absolute()`, or `relative()`. Read-only means no `updateRow()` or `insertRow()`. The JDBC driver can use a server-side cursor or buffer efficiently without supporting bidirectional navigation. `TYPE_SCROLL_INSENSITIVE` is needed only if you need random access within the result set (rare for OLTP). `CONCUR_UPDATABLE` is needed for `ResultSet`-based updates (almost never used in modern code — use separate UPDATE statements).","distractorsMd":[null,"By default, most JDBC drivers buffer the entire `ResultSet` in the client JVM heap.","MySQL: create `Statement` with `TYPE_FORWARD_ONLY + CONCUR_READ_ONLY`, then `stmt.setFetchSize(Integer.MIN_VALUE)` — this magic value enables streaming mode where rows are delivered one at a time from the server."]},{"id":"java/jdbc_and_database#dcb5f766d9f3","section":"java","module":"java/jdbc_and_database","moduleName":"jdbc and database","sourceFile":"README.md","difficulty":"advanced","question":"What is a JDBC \"dirty connection\" from the pool and how does HikariCP prevent it?","answerFull":"A dirty connection is one returned to the pool with uncommitted transaction state — autoCommit=false and an open transaction. The next borrower inherits that transaction context and may see or commit data it did not intend to. HikariCP prevents this in two ways: (1) on connection return it calls rollback() if autoCommit=false, discarding any uncommitted work; (2) it resets autoCommit to the configured pool default before making the connection available. The broken pattern: java // BROKEN: exception path skips rollback — connection returned dirty Connection conn = dataSource.getConnection(); conn.setAutoCommit(false); try { // ... some work ... conn.commit(); } catch (SQLException e) { // forgot conn.rollback() — conn goes back mid-transaction } finally { conn.close(); // returns to pool without rollback } // FIXED: always rollback in catch before closing } catch (SQLException e) { conn.rollback(); // explicit rollback throw e; } finally { conn.close(); }","correct":"A dirty connection is one returned to the pool with uncommitted transaction state — autoCommit=false and an open transaction.","distractors":["READ_UNCOMMITTED: allows dirty reads (seeing uncommitted data), non-repeatable reads, and phantom reads.","Pool exhaustion: all connections in the pool are in use (not returned); new requests block waiting for getConnection() until connectionTimeout is exceeded, then fail with PoolTimeoutException.","If not using try-with-resources: the Connection is never returned to the pool (if using HikariCP) or the TCP socket to the DB is never closed."],"distractorIds":["java/jdbc_and_database#3920b15c6bfa","java/jdbc_and_database#923b919fc002","java/jdbc_and_database#fc77845c6b89"],"concepts":["autocommit","uncommitted","jdbc","dirty","hikaricp","prevent"],"correctMd":"A dirty connection is one returned to the pool with uncommitted transaction state — `autoCommit=false` and an open transaction.","answerFullMd":"A dirty connection is one returned to the pool with uncommitted transaction state — `autoCommit=false` and an open transaction. The next borrower inherits that transaction context and may see or commit data it did not intend to. HikariCP prevents this in two ways: (1) on connection return it calls `rollback()` if `autoCommit=false`, discarding any uncommitted work; (2) it resets `autoCommit` to the configured pool default before making the connection available. The broken pattern: ```java // BROKEN: exception path skips rollback — connection returned dirty Connection conn = dataSource.getConnection(); conn.setAutoCommit(false); try { // ... some work ... conn.commit(); } catch (SQLException e) { // forgot conn.rollback() — conn goes back mid-transaction } finally { conn.close(); // returns to pool without rollback } // FIXED: always rollback in catch before closing } catch (SQLException e) { conn.rollback(); // explicit rollback throw e; } finally { conn.close(); } ```","distractorsMd":[null,"Pool exhaustion: all connections in the pool are in use (not returned); new requests block waiting for `getConnection()` until `connectionTimeout` is exceeded, then fail with `PoolTimeoutException`.","If not using `try-with-resources`: the `Connection` is never returned to the pool (if using HikariCP) or the TCP socket to the DB is never closed."]},{"id":"java/jdbc_and_database#7bfffcd9187c","section":"java","module":"java/jdbc_and_database","moduleName":"jdbc and database","sourceFile":"README.md","difficulty":"advanced","question":"How does SELECT ... FOR UPDATE SKIP LOCKED implement a work queue on top of a relational database?","answerFull":"SELECT ... FOR UPDATE acquires exclusive row locks. SKIP LOCKED (PostgreSQL, MySQL 8+) extends this: instead of blocking on locked rows, the statement skips them and returns only unlocked rows. Multiple competing consumers can run the same query concurrently — each gets a disjoint set of rows. Pattern: sql -- consumer loop BEGIN; SELECT id, payload FROM job_queue WHERE status = 'PENDING' ORDER BY created_at LIMIT 10 FOR UPDATE SKIP LOCKED; -- process rows; on success: UPDATE job_queue SET status = 'DONE' WHERE id IN (...); COMMIT; -- on failure: ROLLBACK releases locks so another consumer can retry This gives at-least-once delivery semantics (a failed/crashed consumer never commits, so the rows become visible to the next consumer). Throughput scales linearly with concurrent consumers up to the database connection limit.","correct":"SELECT ... FOR UPDATE acquires exclusive row locks.","distractors":["XA transactions coordinate a commit/rollback across multiple transactional resources (two databases, a database and a JMS queue) atomically.","A Savepoint marks a point within a transaction to which you can partially roll back.","A dirty connection is one returned to the pool with uncommitted transaction state — autoCommit=false and an open transaction."],"distractorIds":["java/jdbc_and_database#85ff066a9dca","java/jdbc_and_database#0331d2311fdc","java/jdbc_and_database#dcb5f766d9f3"],"concepts":["exclusive","locked","relational","acquires","skip","locks"],"questionMd":"How does `SELECT ... FOR UPDATE SKIP LOCKED` implement a work queue on top of a relational database?","correctMd":"`SELECT ... FOR UPDATE` acquires exclusive row locks.","answerFullMd":"`SELECT ... FOR UPDATE` acquires exclusive row locks. `SKIP LOCKED` (PostgreSQL, MySQL 8+) extends this: instead of blocking on locked rows, the statement skips them and returns only unlocked rows. Multiple competing consumers can run the same query concurrently — each gets a disjoint set of rows. Pattern: ```sql -- consumer loop BEGIN; SELECT id, payload FROM job_queue WHERE status = 'PENDING' ORDER BY created_at LIMIT 10 FOR UPDATE SKIP LOCKED; -- process rows; on success: UPDATE job_queue SET status = 'DONE' WHERE id IN (...); COMMIT; -- on failure: ROLLBACK releases locks so another consumer can retry ``` This gives at-least-once delivery semantics (a failed/crashed consumer never commits, so the rows become visible to the next consumer). Throughput scales linearly with concurrent consumers up to the database connection limit.","distractorsMd":[null,"A `Savepoint` marks a point within a transaction to which you can partially roll back.","A dirty connection is one returned to the pool with uncommitted transaction state — `autoCommit=false` and an open transaction."]},{"id":"java/jdbc_and_database#bd2453d83819","section":"java","module":"java/jdbc_and_database","moduleName":"jdbc and database","sourceFile":"README.md","difficulty":"advanced","question":"How does setFetchSize() affect memory use and database cursors for large result sets?","answerFull":"By default, most JDBC drivers buffer the entire ResultSet in the client JVM heap. For a query returning 1M rows, this can OOM the process. stmt.setFetchSize(1000) instructs the driver to fetch rows in pages of 1,000. PostgreSQL (org.postgresql.Driver) requires autoCommit=false for server-side cursors to be honoured; MySQL uses the special value Integer.MIN_VALUE to switch to row-by-row streaming. Broken: java // BROKEN: entire 1M-row result loaded into heap — OOM Statement stmt = conn.createStatement(); ResultSet rs = stmt.executeQuery(\"SELECT * FROM events\"); // FIXED (PostgreSQL): use a batch size and a transaction conn.setAutoCommit(false); Statement stmt = conn.createStatement(); stmt.setFetchSize(1000); ResultSet rs = stmt.executeQuery(\"SELECT * FROM events\"); while (rs.next()) { /* process row */ } Rule of thumb: always set fetchSize for any query where the result could exceed ~10,000 rows; default to 500–1,000 as a starting point.","correct":"By default, most JDBC drivers buffer the entire ResultSet in the client JVM heap.","distractors":["TYPE_FORWARD_ONLY (default) with CONCUR_READ_ONLY — it is the fastest and most efficient.","XA transactions coordinate a commit/rollback across multiple transactional resources (two databases, a database and a JMS queue) atomically.","MySQL: create Statement with TYPE_FORWARD_ONLY + CONCUR_READ_ONLY, then stmt.setFetchSize(Integer.MIN_VALUE) — this magic value enables streaming mode where rows are delivered one at a time from the server."],"distractorIds":["java/jdbc_and_database#acc3ba2bedfd","java/jdbc_and_database#85ff066a9dca","java/jdbc_and_database#8bf1a1bab076"],"concepts":["drivers","resultset","setfetchsize","affect","jdbc","sets"],"questionMd":"How does `setFetchSize()` affect memory use and database cursors for large result sets?","correctMd":"By default, most JDBC drivers buffer the entire `ResultSet` in the client JVM heap.","answerFullMd":"By default, most JDBC drivers buffer the entire `ResultSet` in the client JVM heap. For a query returning 1M rows, this can OOM the process. `stmt.setFetchSize(1000)` instructs the driver to fetch rows in pages of 1,000. PostgreSQL (`org.postgresql.Driver`) requires `autoCommit=false` for server-side cursors to be honoured; MySQL uses the special value `Integer.MIN_VALUE` to switch to row-by-row streaming. Broken: ```java // BROKEN: entire 1M-row result loaded into heap — OOM Statement stmt = conn.createStatement(); ResultSet rs = stmt.executeQuery(\"SELECT * FROM events\"); // FIXED (PostgreSQL): use a batch size and a transaction conn.setAutoCommit(false); Statement stmt = conn.createStatement(); stmt.setFetchSize(1000); ResultSet rs = stmt.executeQuery(\"SELECT * FROM events\"); while (rs.next()) { /* process row */ } ``` Rule of thumb: always set `fetchSize` for any query where the result could exceed ~10,000 rows; default to 500–1,000 as a starting point.","distractorsMd":["`TYPE_FORWARD_ONLY` (default) with `CONCUR_READ_ONLY` — it is the fastest and most efficient.",null,"MySQL: create `Statement` with `TYPE_FORWARD_ONLY + CONCUR_READ_ONLY`, then `stmt.setFetchSize(Integer.MIN_VALUE)` — this magic value enables streaming mode where rows are delivered one at a time from the server."]},{"id":"java/jvm_internals#a9b90fc54647","section":"java","module":"java/jvm_internals","moduleName":"jvm internals","sourceFile":"README.md","difficulty":"core","question":"What is a TLAB and why does it exist?","answerFull":"Thread-Local Allocation Buffer: a private sub-region of Eden space allocated to each thread. Object allocation inside the TLAB is a simple pointer bump — O(1) with no synchronization. Without TLAB, every allocation would require synchronized access to the shared Eden pointer, creating contention under many threads. When a TLAB is exhausted, the thread requests a new TLAB from shared Eden (synchronized, but rare). TLAB size adapts based on allocation rate.","correct":"Thread-Local Allocation Buffer: a private sub-region of Eden space allocated to each thread.","distractors":["Escape analysis is a JIT (C2) optimization that determines whether an object \"escapes\" the method that created it — i.e. whether a reference to it can be seen by another thread or outlive the method.","Minor GC collects only the young generation (Eden + Survivors) — typically fast (< 100ms), STW.","A safepoint is a program execution point where all JVM threads are paused for operations requiring a consistent heap state: GC, deoptimization, heap dumps, thread dumps."],"distractorIds":["java/jvm_internals#3105a654ceeb","java/jvm_internals#8146a187384f","java/jvm_internals#6208050b7682"],"concepts":["eden","allocated","allocation","exist","private","region"]},{"id":"java/jvm_internals#120d8c5d3bfa","section":"java","module":"java/jvm_internals","moduleName":"jvm internals","sourceFile":"README.md","difficulty":"core","question":"Describe G1 GC regions and how mixed GC works.","answerFull":"G1 divides the heap into equal-sized regions (1-32MB each). Each region is dynamically assigned as Eden, Survivor, Old, or Humongous. A *young GC* evacuates Eden and Survivor regions to new Survivor/Old regions (STW pause). When the heap occupancy exceeds a threshold (default 45%), G1 starts a *concurrent marking cycle* to identify live objects in old regions. After marking, a *mixed GC* evacuates both young regions AND a subset of old regions with the lowest liveness (most garbage) — hence \"mixed.\" This continues until old region occupancy drops below a threshold.","correct":"G1 divides the heap into equal-sized regions (1-32MB each).","distractors":["A safepoint is a program execution point where all JVM threads are paused for operations requiring a consistent heap state: GC, deoptimization, heap dumps, thread dumps.","Bootstrap ClassLoader (JDK built-in, C++) loads core JDK classes.","Deoptimization is the JIT discarding compiled native code and falling back to interpretation (or lower-tier compilation) when assumptions embedded in the compiled code are violated."],"distractorIds":["java/jvm_internals#6208050b7682","java/jvm_internals#70d148bf9516","java/jvm_internals#97b5bbf06c65"],"concepts":["32mb","describe","divides","sized","mixed","regions"]},{"id":"java/jvm_internals#f3ca3ebbd840","section":"java","module":"java/jvm_internals","moduleName":"jvm internals","sourceFile":"README.md","difficulty":"core","question":"What is tri-color marking and why does it need write barriers?","answerFull":"Tri-color marking classifies objects: white (unvisited), gray (visited but references unscanned), black (fully scanned). GC scans gray objects, marks their references gray, marks them black. When marking is concurrent, the mutator can create a reference from a black object to a white object — breaking the invariant (white objects would be collected even though reachable from black). Write barriers detect this: G1 uses SATB (snapshot-at-the-beginning) barriers to record pre-write values; ZGC uses load barriers on every reference read.","correct":"Tri-color marking classifies objects: white (unvisited), gray (visited but references unscanned), black (fully scanned).","distractors":["ZGC uses *colored pointers* (metadata bits in the 64-bit address) and *load barriers* (code executed on every object reference read).","The JMM guarantees: a *volatile write* to a field happens-before any subsequent *volatile read* of the same field.","Safe publication ensures all threads see the fully constructed state of an object."],"distractorIds":["java/jvm_internals#4ac2da2652d4","java/jvm_internals#61ff35b359eb","java/jvm_internals#46db757ddb7e"],"concepts":["barriers","tri","unscanned","marking","gray","scanned"]},{"id":"java/jvm_internals#4ac2da2652d4","section":"java","module":"java/jvm_internals","moduleName":"jvm internals","sourceFile":"README.md","difficulty":"core","question":"Why does ZGC achieve sub-millisecond pauses?","answerFull":"ZGC uses *colored pointers* (metadata bits in the 64-bit address) and *load barriers* (code executed on every object reference read). When ZGC relocates an object, it updates the color bits. When a thread reads a reference, the load barrier checks the color — if stale (pre-relocation), it transparently loads the new address. This means relocation can happen concurrently with the mutator. The only STW phases are initial mark and remark — both bounded by GC root count, not heap size, giving sub-1ms pauses even on TB heaps.","correct":"ZGC uses *colored pointers* (metadata bits in the 64-bit address) and *load barriers* (code executed on every object reference read).","distractors":["Metaspace stores class metadata.","Escape analysis determines whether an object's reference escapes the current method (assigned to a static field, passed to a method, returned, etc.).","Safe publication ensures all threads see the fully constructed state of an object."],"distractorIds":["java/jvm_internals#c41bb7a12e86","java/jvm_internals#43d0c06f0009","java/jvm_internals#46db757ddb7e"],"concepts":["barriers","colored","zgc","millisecond","pauses","executed"]},{"id":"java/jvm_internals#468828c35507","section":"java","module":"java/jvm_internals","moduleName":"jvm internals","sourceFile":"README.md","difficulty":"core","question":"What happens during class initialization (<clinit>)?","answerFull":"The JVM executes static initializer blocks and static field assignments in textual order. It's guaranteed to run at most once (JVM serializes it). Triggered by: first instance creation, first static method call, first static field access (except constants). A class initialization cycle (A initializes B which initializes A) results in A seeing B partially initialized — a subtle ordering bug. Always prefer lazy-initialized holders or enum-based singletons to avoid class initialization ordering issues.","correct":"The JVM executes static initializer blocks and static field assignments in textual order.","distractors":["StoreLoad is the most expensive memory barrier: it ensures all stores before the barrier complete and are visible to all CPUs before any subsequent load executes.","Escape analysis determines whether an object's reference escapes the current method (assigned to a static field, passed to a method, returned, etc.).","The JMM guarantees: a *volatile write* to a field happens-before any subsequent *volatile read* of the same field."],"distractorIds":["java/jvm_internals#641a59e60348","java/jvm_internals#43d0c06f0009","java/jvm_internals#61ff35b359eb"],"concepts":["clinit","textual","initializer","assignments","initialization","happens"],"questionMd":"What happens during class initialization (`<clinit>`)?","distractorsMd":["`StoreLoad` is the most expensive memory barrier: it ensures all stores before the barrier complete and are visible to all CPUs before any subsequent load executes.",null,null]},{"id":"java/jvm_internals#43d0c06f0009","section":"java","module":"java/jvm_internals","moduleName":"jvm internals","sourceFile":"README.md","difficulty":"intermediate","question":"How does escape analysis enable stack allocation?","answerFull":"Escape analysis determines whether an object's reference escapes the current method (assigned to a static field, passed to a method, returned, etc.). If an object doesn't escape, the JIT can: (1) allocate it on the stack instead of the heap (no GC overhead); (2) apply scalar replacement — decompose the object into its constituent fields, which may be kept in registers. Enabled by default in HotSpot. To verify: use -XX:+PrintEscapeAnalysis or async-profiler allocation profiling.","correct":"Escape analysis determines whether an object's reference escapes the current method (assigned to a static field, passed to a method, returned, etc.).","distractors":["Deoptimization is the JVM discarding compiled (C2) code and falling back to the interpreter for a method, then potentially recompiling later.","The JVM executes static initializer blocks and static field assignments in textual order.","The JMM guarantees: a *volatile write* to a field happens-before any subsequent *volatile read* of the same field."],"distractorIds":["java/jvm_internals#9774766ebd71","java/jvm_internals#468828c35507","java/jvm_internals#61ff35b359eb"],"concepts":["escapes","escape","analysis","allocation","enable","assigned"],"answerFullMd":"Escape analysis determines whether an object's reference escapes the current method (assigned to a static field, passed to a method, returned, etc.). If an object doesn't escape, the JIT can: (1) allocate it on the stack instead of the heap (no GC overhead); (2) apply scalar replacement — decompose the object into its constituent fields, which may be kept in registers. Enabled by default in HotSpot. To verify: use `-XX:+PrintEscapeAnalysis` or async-profiler allocation profiling."},{"id":"java/jvm_internals#97b5bbf06c65","section":"java","module":"java/jvm_internals","moduleName":"jvm internals","sourceFile":"README.md","difficulty":"intermediate","question":"What is deoptimization and when does it happen?","answerFull":"Deoptimization is the JIT discarding compiled native code and falling back to interpretation (or lower-tier compilation) when assumptions embedded in the compiled code are violated. Common triggers: a method was compiled assuming only one concrete type was seen (monomorphic dispatch), then a second type appears (bimorphic/megamorphic); a speculated null check is violated; a final field was changed via reflection. Deoptimization causes a latency spike followed by recompilation. Detected via -XX:+TraceDeoptimization or async-profiler.","correct":"Deoptimization is the JIT discarding compiled native code and falling back to interpretation (or lower-tier compilation) when assumptions embedded in the compiled code are violated.","distractors":["Escape analysis is a JIT (C2) optimization that determines whether an object \"escapes\" the method that created it — i.e. whether a reference to it can be seen by another thread or outlive the method.","ZGC uses *colored pointers* (metadata bits in the 64-bit address) and *load barriers* (code executed on every object reference read).","Deoptimization is the JVM discarding compiled (C2) code and falling back to the interpreter for a method, then potentially recompiling later."],"distractorIds":["java/jvm_internals#3105a654ceeb","java/jvm_internals#4ac2da2652d4","java/jvm_internals#9774766ebd71"],"concepts":["interpretation","assumptions","deoptimization","falling","discarding","happen"],"answerFullMd":"Deoptimization is the JIT discarding compiled native code and falling back to interpretation (or lower-tier compilation) when assumptions embedded in the compiled code are violated. Common triggers: a method was compiled assuming only one concrete type was seen (monomorphic dispatch), then a second type appears (bimorphic/megamorphic); a speculated null check is violated; a final field was changed via reflection. Deoptimization causes a latency spike followed by recompilation. Detected via `-XX:+TraceDeoptimization` or async-profiler."},{"id":"java/jvm_internals#61ff35b359eb","section":"java","module":"java/jvm_internals","moduleName":"jvm internals","sourceFile":"README.md","difficulty":"intermediate","question":"Explain happens-before with a volatile example.","answerFull":"The JMM guarantees: a *volatile write* to a field happens-before any subsequent *volatile read* of the same field. \"Subsequent\" means observed to have read the write's value. So: Thread A writes x = 42; volatile flag = true; and Thread B reads volatile flag; read x; — if Thread B sees flag == true, it is guaranteed to see x == 42. The volatile write on flag establishes the happens-before edge across all preceding writes by Thread A. Without volatile on flag, Thread B might see flag == true but x == 0 (stale cache line).","correct":"The JMM guarantees: a *volatile write* to a field happens-before any subsequent *volatile read* of the same field.","distractors":["StoreLoad is the most expensive memory barrier: it ensures all stores before the barrier complete and are visible to all CPUs before any subsequent load executes.","The JVM executes static initializer blocks and static field assignments in textual order.","ZGC uses *colored pointers* (metadata bits in the 64-bit address) and *load barriers* (code executed on every object reference read)."],"distractorIds":["java/jvm_internals#641a59e60348","java/jvm_internals#468828c35507","java/jvm_internals#4ac2da2652d4"],"concepts":["explain","jmm","volatile","subsequent","happens","guarantees"],"answerFullMd":"The JMM guarantees: a *volatile write* to a field happens-before any subsequent *volatile read* of the same field. \"Subsequent\" means observed to have read the write's value. So: Thread A writes `x = 42; volatile flag = true;` and Thread B reads `volatile flag; read x;` — if Thread B sees `flag == true`, it is guaranteed to see `x == 42`. The volatile write on `flag` establishes the happens-before edge across all preceding writes by Thread A. Without `volatile` on `flag`, Thread B might see `flag == true` but `x == 0` (stale cache line).","distractorsMd":["`StoreLoad` is the most expensive memory barrier: it ensures all stores before the barrier complete and are visible to all CPUs before any subsequent load executes.",null,null]},{"id":"java/jvm_internals#46db757ddb7e","section":"java","module":"java/jvm_internals","moduleName":"jvm internals","sourceFile":"README.md","difficulty":"intermediate","question":"What is safe publication and what are the 4 ways to achieve it in Java?","answerFull":"Safe publication ensures all threads see the fully constructed state of an object. An object is *unsafely published* if a reference is made visible before construction is complete (partial initialization). The 4 safe publication mechanisms: (1) Static initializer: static final Foo f = new Foo(); — JVM class initialization guarantees. (2) final field in constructor: constructors establish happens-before between writing final fields and any thread that observes the reference. (3) volatile field: storing reference to volatile field. (4) Thread-safe collection (AtomicReference, ConcurrentHashMap, etc.).","correct":"Safe publication ensures all threads see the fully constructed state of an object.","distractors":["ZGC uses *colored pointers* (metadata bits in the 64-bit address) and *load barriers* (code executed on every object reference read).","Escape analysis determines whether an object's reference escapes the current method (assigned to a static field, passed to a method, returned, etc.).","StoreLoad is the most expensive memory barrier: it ensures all stores before the barrier complete and are visible to all CPUs before any subsequent load executes."],"distractorIds":["java/jvm_internals#4ac2da2652d4","java/jvm_internals#43d0c06f0009","java/jvm_internals#641a59e60348"],"concepts":["constructed","publication","ways","achieve","ensures","safe"],"answerFullMd":"Safe publication ensures all threads see the fully constructed state of an object. An object is *unsafely published* if a reference is made visible before construction is complete (partial initialization). The 4 safe publication mechanisms: (1) Static initializer: `static final Foo f = new Foo();` — JVM class initialization guarantees. (2) `final` field in constructor: constructors establish happens-before between writing final fields and any thread that observes the reference. (3) `volatile` field: storing reference to `volatile` field. (4) Thread-safe collection (AtomicReference, ConcurrentHashMap, etc.).","distractorsMd":[null,null,"`StoreLoad` is the most expensive memory barrier: it ensures all stores before the barrier complete and are visible to all CPUs before any subsequent load executes."]},{"id":"java/jvm_internals#8146a187384f","section":"java","module":"java/jvm_internals","moduleName":"jvm internals","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between minor GC, major GC, and full GC?","answerFull":"Minor GC collects only the young generation (Eden + Survivors) — typically fast (< 100ms), STW. Major GC collects the old generation — can be concurrent (G1, ZGC) or STW (Parallel GC). Full GC collects everything (young + old + metaspace) — always STW, always slow. Full GC is triggered when: concurrent collection fails to keep up (G1 \"concurrent mode failure\"), explicit System.gc(), metaspace exhausted, or JVM cannot allocate in any region.","correct":"Minor GC collects only the young generation (Eden + Survivors) — typically fast (< 100ms), STW.","distractors":["Thread-Local Allocation Buffer: a private sub-region of Eden space allocated to each thread.","A safepoint is a program execution point where all JVM threads are paused for operations requiring a consistent heap state: GC, deoptimization, heap dumps, thread dumps.","Bootstrap ClassLoader (JDK built-in, C++) loads core JDK classes."],"distractorIds":["java/jvm_internals#a9b90fc54647","java/jvm_internals#6208050b7682","java/jvm_internals#70d148bf9516"],"concepts":["minor","stw","eden","survivors","young","100ms"],"answerFullMd":"Minor GC collects only the young generation (Eden + Survivors) — typically fast (< 100ms), STW. Major GC collects the old generation — can be concurrent (G1, ZGC) or STW (Parallel GC). Full GC collects everything (young + old + metaspace) — always STW, always slow. Full GC is triggered when: concurrent collection fails to keep up (G1 \"concurrent mode failure\"), explicit `System.gc()`, metaspace exhausted, or JVM cannot allocate in any region."},{"id":"java/jvm_internals#c41bb7a12e86","section":"java","module":"java/jvm_internals","moduleName":"jvm internals","sourceFile":"README.md","difficulty":"advanced","question":"What causes OutOfMemoryError: Metaspace?","answerFull":"Metaspace stores class metadata. It grows unbounded by default (unlike PermGen which was fixed size). OOM:Metaspace occurs when: (1) a class generator (CGLIB, ASM, dynamic proxies) creates classes faster than old classloaders are GC'd; (2) ClassLoader leak — a classloader is referenced from a static field, preventing GC of all its loaded classes. Every class loaded by a leaked classloader occupies metaspace permanently. Fix: set -XX:MaxMetaspaceSize to cap it; hunt classloader leaks with jmap -clstats.","correct":"Metaspace stores class metadata.","distractors":["ZGC uses *colored pointers* (metadata bits in the 64-bit address) and *load barriers* (code executed on every object reference read).","StoreLoad is the most expensive memory barrier: it ensures all stores before the barrier complete and are visible to all CPUs before any subsequent load executes.","A safepoint is a program execution point where all JVM threads are paused for operations requiring a consistent heap state: GC, deoptimization, heap dumps, thread dumps."],"distractorIds":["java/jvm_internals#4ac2da2652d4","java/jvm_internals#641a59e60348","java/jvm_internals#6208050b7682"],"concepts":["outofmemoryerror","metaspace","causes","metadata","stores","class"],"answerFullMd":"Metaspace stores class metadata. It grows unbounded by default (unlike PermGen which was fixed size). OOM:Metaspace occurs when: (1) a class generator (CGLIB, ASM, dynamic proxies) creates classes faster than old classloaders are GC'd; (2) ClassLoader leak — a classloader is referenced from a static field, preventing GC of all its loaded classes. Every class loaded by a leaked classloader occupies metaspace permanently. Fix: set `-XX:MaxMetaspaceSize` to cap it; hunt classloader leaks with `jmap -clstats`.","distractorsMd":[null,"`StoreLoad` is the most expensive memory barrier: it ensures all stores before the barrier complete and are visible to all CPUs before any subsequent load executes.",null]},{"id":"java/jvm_internals#70d148bf9516","section":"java","module":"java/jvm_internals","moduleName":"jvm internals","sourceFile":"README.md","difficulty":"advanced","question":"How does the JVM class loader hierarchy work?","answerFull":"Bootstrap ClassLoader (JDK built-in, C++) loads core JDK classes. Platform ClassLoader loads Java SE APIs not in bootstrap. App ClassLoader loads application classpath. Parent delegation model: when asked to load a class, each classloader first delegates to its parent; only loads itself if parent returns null. This prevents user code from replacing java.lang.String. Custom classloaders for hot-deploy or isolation can override loadClass() to invert delegation (load own classes first).","correct":"Bootstrap ClassLoader (JDK built-in, C++) loads core JDK classes.","distractors":["Metaspace stores class metadata.","The JVM executes static initializer blocks and static field assignments in textual order.","Deoptimization is the JVM discarding compiled (C2) code and falling back to the interpreter for a method, then potentially recompiling later."],"distractorIds":["java/jvm_internals#c41bb7a12e86","java/jvm_internals#468828c35507","java/jvm_internals#9774766ebd71"],"concepts":["classloader","loader","bootstrap","jdk","hierarchy","loads"],"answerFullMd":"Bootstrap ClassLoader (JDK built-in, C++) loads core JDK classes. Platform ClassLoader loads Java SE APIs not in bootstrap. App ClassLoader loads application classpath. Parent delegation model: when asked to load a class, each classloader first delegates to its parent; only loads itself if parent returns null. This prevents user code from replacing `java.lang.String`. Custom classloaders for hot-deploy or isolation can override `loadClass()` to invert delegation (load own classes first)."},{"id":"java/jvm_internals#6208050b7682","section":"java","module":"java/jvm_internals","moduleName":"jvm internals","sourceFile":"README.md","difficulty":"advanced","question":"What is a safepoint and why can it cause latency spikes?","answerFull":"A safepoint is a program execution point where all JVM threads are paused for operations requiring a consistent heap state: GC, deoptimization, heap dumps, thread dumps. The JVM sets a safepoint request flag; threads poll this flag at safepoint poll points (method returns, loop back edges). Time-to-safepoint (TTSP) is the latency from request to all threads reaching safepoints. TTSP causes latency spikes because: a thread in a tight loop without poll points may not reach a safepoint for millions of instructions. On Java 8 with counted loops, a loop like for (int i = 0; i < 1_000_000_000; i++) might not have a poll point — the GC request waits until the loop completes. Diagnosis: -Xlog:safepoint (Java 9+). Mitigation: Java 10+ Thread-Local Handshakes allow targeting individual threads; Java 14+ improves polling in loops.","correct":"A safepoint is a program execution point where all JVM threads are paused for operations requiring a consistent heap state: GC, deoptimization, heap dumps, thread dumps.","distractors":["Thread-Local Allocation Buffer: a private sub-region of Eden space allocated to each thread.","G1 divides the heap into equal-sized regions (1-32MB each).","Safe publication ensures all threads see the fully constructed state of an object."],"distractorIds":["java/jvm_internals#a9b90fc54647","java/jvm_internals#120d8c5d3bfa","java/jvm_internals#46db757ddb7e"],"concepts":["dumps","paused","safepoint","deoptimization","spikes","cause"],"answerFullMd":"A safepoint is a program execution point where all JVM threads are paused for operations requiring a consistent heap state: GC, deoptimization, heap dumps, thread dumps. The JVM sets a safepoint request flag; threads poll this flag at safepoint poll points (method returns, loop back edges). Time-to-safepoint (TTSP) is the latency from request to all threads reaching safepoints. TTSP causes latency spikes because: a thread in a tight loop without poll points may not reach a safepoint for millions of instructions. On Java 8 with counted loops, a loop like `for (int i = 0; i < 1_000_000_000; i++)` might not have a poll point — the GC request waits until the loop completes. Diagnosis: `-Xlog:safepoint` (Java 9+). Mitigation: Java 10+ Thread-Local Handshakes allow targeting individual threads; Java 14+ improves polling in loops."},{"id":"java/jvm_internals#641a59e60348","section":"java","module":"java/jvm_internals","moduleName":"jvm internals","sourceFile":"README.md","difficulty":"advanced","question":"What is the StoreLoad memory barrier and what Java operation requires it?","answerFull":"StoreLoad is the most expensive memory barrier: it ensures all stores before the barrier complete and are visible to all CPUs before any subsequent load executes. It requires flushing the store buffer and preventing speculative loads from executing before preceding stores are committed — requiring a full memory fence (MFENCE or locked instruction on x86). In Java, a volatile write requires StoreLoad (plus StoreStore): the volatile write must be visible to all threads before any subsequent load by the same thread can execute. synchronized exit also requires StoreLoad. This is why volatile writes are more expensive than volatile reads — reads only need LoadLoad + LoadStore, while writes need the full StoreStore + StoreLoad pair.","correct":"StoreLoad is the most expensive memory barrier: it ensures all stores before the barrier complete and are visible to all CPUs before any subsequent load executes.","distractors":["The JVM executes static initializer blocks and static field assignments in textual order.","Metaspace stores class metadata.","The JMM guarantees: a *volatile write* to a field happens-before any subsequent *volatile read* of the same field."],"distractorIds":["java/jvm_internals#468828c35507","java/jvm_internals#c41bb7a12e86","java/jvm_internals#61ff35b359eb"],"concepts":["cpus","storeload","barrier","ensures","subsequent","visible"],"questionMd":"What is the `StoreLoad` memory barrier and what Java operation requires it?","correctMd":"`StoreLoad` is the most expensive memory barrier: it ensures all stores before the barrier complete and are visible to all CPUs before any subsequent load executes.","answerFullMd":"`StoreLoad` is the most expensive memory barrier: it ensures all stores before the barrier complete and are visible to all CPUs before any subsequent load executes. It requires flushing the store buffer and preventing speculative loads from executing before preceding stores are committed — requiring a full memory fence (`MFENCE` or locked instruction on x86). In Java, a volatile write requires `StoreLoad` (plus `StoreStore`): the volatile write must be visible to all threads before any subsequent load by the same thread can execute. `synchronized` exit also requires `StoreLoad`. This is why volatile writes are more expensive than volatile reads — reads only need `LoadLoad + LoadStore`, while writes need the full `StoreStore + StoreLoad` pair."},{"id":"java/jvm_internals#3105a654ceeb","section":"java","module":"java/jvm_internals","moduleName":"jvm internals","sourceFile":"README.md","difficulty":"advanced","question":"What is escape analysis and how can it eliminate heap allocation entirely?","answerFull":"Escape analysis is a JIT (C2) optimization that determines whether an object \"escapes\" the method that created it — i.e. whether a reference to it can be seen by another thread or outlive the method. If an object provably does not escape, the JIT can apply scalar replacement: it decomposes the object into its individual fields and keeps them in registers/stack slots, so *no heap allocation happens at all* and there is no GC pressure for that object. A classic case is a short-lived new Point(x,y) used only locally inside a hot method. The gotchas: escape analysis only fires after JIT compilation (interpreted/cold code still allocates), it is fragile — storing the reference in a field, returning it, or passing it to a non-inlined method makes it escape — and it is why microbenchmarks must use a sink (e.g. JMH Blackhole) or the \"allocation\" they measure may have been optimized away. Inlining enables it: the more the JIT inlines, the more allocations it can prove non-escaping.","correct":"Escape analysis is a JIT (C2) optimization that determines whether an object \"escapes\" the method that created it — i.e. whether a reference to it can be seen by another thread or outlive the method.","distractors":["Thread-Local Allocation Buffer: a private sub-region of Eden space allocated to each thread.","Escape analysis determines whether an object's reference escapes the current method (assigned to a static field, passed to a method, returned, etc.).","Deoptimization is the JIT discarding compiled native code and falling back to interpretation (or lower-tier compilation) when assumptions embedded in the compiled code are violated."],"distractorIds":["java/jvm_internals#a9b90fc54647","java/jvm_internals#43d0c06f0009","java/jvm_internals#97b5bbf06c65"],"concepts":["eliminate","outlive","escapes","escape","jit","analysis"],"answerFullMd":"Escape analysis is a JIT (C2) optimization that determines whether an object \"escapes\" the method that created it — i.e. whether a reference to it can be seen by another thread or outlive the method. If an object provably does not escape, the JIT can apply scalar replacement: it decomposes the object into its individual fields and keeps them in registers/stack slots, so *no heap allocation happens at all* and there is no GC pressure for that object. A classic case is a short-lived `new Point(x,y)` used only locally inside a hot method. The gotchas: escape analysis only fires after JIT compilation (interpreted/cold code still allocates), it is fragile — storing the reference in a field, returning it, or passing it to a non-inlined method makes it escape — and it is why microbenchmarks must use a sink (e.g. JMH `Blackhole`) or the \"allocation\" they measure may have been optimized away. Inlining enables it: the more the JIT inlines, the more allocations it can prove non-escaping."},{"id":"java/jvm_internals#9774766ebd71","section":"java","module":"java/jvm_internals","moduleName":"jvm internals","sourceFile":"README.md","difficulty":"advanced","question":"What is deoptimization and why does the JIT speculatively optimize code it may have to throw away?","answerFull":"Deoptimization is the JVM discarding compiled (C2) code and falling back to the interpreter for a method, then potentially recompiling later. C2 makes *speculative* optimizations based on the profile it has observed — e.g. assuming a branch is never taken, a type is always the same (monomorphic call → inlined), or a class is never loaded. These assumptions are guarded by cheap checks (uncommon traps); if an assumption is later violated (the rare branch is taken, a new subclass is loaded), the guard fires, the optimized frame is deoptimized back to the interpreter at a safepoint, and execution continues correctly. This lets the JIT optimize aggressively for the common case while staying correct. The cost: a sudden deopt storm (e.g. a new implementation class loaded into a hot megamorphic call site) can cause a latency spike as hot code reverts to interpreted speed until recompiled — visible with -XX:+PrintCompilation / -Xlog:deoptimization.","correct":"Deoptimization is the JVM discarding compiled (C2) code and falling back to the interpreter for a method, then potentially recompiling later.","distractors":["A safepoint is a program execution point where all JVM threads are paused for operations requiring a consistent heap state: GC, deoptimization, heap dumps, thread dumps.","Escape analysis is a JIT (C2) optimization that determines whether an object \"escapes\" the method that created it — i.e. whether a reference to it can be seen by another thread or outlive the method.","ZGC uses *colored pointers* (metadata bits in the 64-bit address) and *load barriers* (code executed on every object reference read)."],"distractorIds":["java/jvm_internals#6208050b7682","java/jvm_internals#3105a654ceeb","java/jvm_internals#4ac2da2652d4"],"concepts":["recompiling","deoptimization","falling","away","discarding","throw"],"answerFullMd":"Deoptimization is the JVM discarding compiled (C2) code and falling back to the interpreter for a method, then potentially recompiling later. C2 makes *speculative* optimizations based on the profile it has observed — e.g. assuming a branch is never taken, a type is always the same (monomorphic call → inlined), or a class is never loaded. These assumptions are guarded by cheap checks (uncommon traps); if an assumption is later violated (the rare branch is taken, a new subclass is loaded), the guard fires, the optimized frame is deoptimized back to the interpreter at a safepoint, and execution continues correctly. This lets the JIT optimize aggressively for the common case while staying correct. The cost: a sudden deopt storm (e.g. a new implementation class loaded into a hot megamorphic call site) can cause a latency spike as hot code reverts to interpreted speed until recompiled — visible with `-XX:+PrintCompilation` / `-Xlog:deoptimization`."},{"id":"java/microservices_patterns#71f154763e3f","section":"java","module":"java/microservices_patterns","moduleName":"microservices patterns","sourceFile":"README.md","difficulty":"core","question":"How do you maintain data consistency across microservices without a distributed transaction?","answerFull":"Use the Saga pattern: model the operation as a sequence of local ACID transactions, each with a compensating transaction that semantically undoes it. On failure at step k, run compensations for steps k-1 down to 1 in reverse order. You trade strong consistency for eventual consistency and accept that intermediate states are observable. The practical guidance: pick orchestration for complex/branching flows (one place to debug) and choreography for short, loosely coupled ones.","correct":"Use the Saga pattern: model the operation as a sequence of local ACID transactions, each with a compensating transaction that semantically undoes it.","distractors":["Atomicity is replaced by \"all forward steps complete, or all completed steps are compensated\" — but *not* isolation: between steps, other transactions can observe intermediate state (a reserved-but-not-yet-paid order).","A compensation must be idempotent (it may be retried), semantically undo the forward action (release the reservation, refund the charge — not necessarily a literal inverse), and be designed to essentially always succeed.","Record a unique identifier for each processed message (message id or business idempotency key) in a dedup store within the same transaction as the side effect, and skip messages whose id is already present."],"distractorIds":["java/microservices_patterns#6c1bead2c78f","java/microservices_patterns#c5392d352e92","java/microservices_patterns#1ce376b8271b"],"concepts":["undoes","microservices","compensating","acid","semantically","saga"]},{"id":"java/microservices_patterns#d325e28cace4","section":"java","module":"java/microservices_patterns","moduleName":"microservices patterns","sourceFile":"README.md","difficulty":"core","question":"Why does the outbox give at-least-once and not exactly-once delivery?","answerFull":"Because the relay publishes the event and *then* marks the row as sent in two steps; if it crashes between them, the event is published again on restart. You cannot make \"publish to broker\" and \"update outbox row\" atomic — they are again two systems. So you accept at-least-once and require the consumer to be idempotent. Marking sent *before* publishing would instead risk losing events (at-most-once), which is worse.","correct":"Because the relay publishes the event and *then* marks the row as sent in two steps; if it crashes between them, the event is published again on restart.","distractors":["Atomicity is replaced by \"all forward steps complete, or all completed steps are compensated\" — but *not* isolation: between steps, other transactions can observe intermediate state (a reserved-but-not-yet-paid order).","Inject failures at each forward step and assert that exactly the compensations for already-completed steps run, in reverse order, and that they are idempotent under repeated invocation.","Choreography has no central coordinator; each service reacts to events and emits its own, giving maximum decoupling but making the overall flow implicit and hard to trace once it exceeds a few steps."],"distractorIds":["java/microservices_patterns#6c1bead2c78f","java/microservices_patterns#d7dae91ae4e3","java/microservices_patterns#e5edd4fde5b7"],"concepts":["relay","crashes","again","delivery","publishes","published"]},{"id":"java/microservices_patterns#1ce376b8271b","section":"java","module":"java/microservices_patterns","moduleName":"microservices patterns","sourceFile":"README.md","difficulty":"core","question":"How do you make a message consumer idempotent?","answerFull":"Record a unique identifier for each processed message (message id or business idempotency key) in a dedup store within the same transaction as the side effect, and skip messages whose id is already present. The atomic claim is best enforced by a database UNIQUE constraint rather than a check-then-act in code, because concurrent retries would race a check-then-act. For external clients, expose an explicit idempotency key they supply and store key → response to replay results.","correct":"Record a unique identifier for each processed message (message id or business idempotency key) in a dedup store within the same transaction as the side effect, and skip messages whose id is already present.","distractors":["A compensation must be idempotent (it may be retried), semantically undo the forward action (release the reservation, refund the charge — not necessarily a literal inverse), and be designed to essentially always succeed.","Inject failures at each forward step and assert that exactly the compensations for already-completed steps run, in reverse order, and that they are idempotent under repeated invocation.","Use the Saga pattern: model the operation as a sequence of local ACID transactions, each with a compensating transaction that semantically undoes it."],"distractorIds":["java/microservices_patterns#c5392d352e92","java/microservices_patterns#d7dae91ae4e3","java/microservices_patterns#71f154763e3f"],"concepts":["dedup","identifier","idempotent","idempotency","skip","processed"]},{"id":"java/microservices_patterns#e5edd4fde5b7","section":"java","module":"java/microservices_patterns","moduleName":"microservices patterns","sourceFile":"README.md","difficulty":"core","question":"Choreography vs orchestration — when do you choose each?","answerFull":"Choreography has no central coordinator; each service reacts to events and emits its own, giving maximum decoupling but making the overall flow implicit and hard to trace once it exceeds a few steps. Orchestration introduces a coordinator that explicitly drives each step, which centralizes the logic (easy to see, debug, and change) at the cost of a component every step depends on. Rule of thumb: choreography for 2–4 simple steps, orchestration for long or branching workflows.","correct":"Choreography has no central coordinator; each service reacts to events and emits its own, giving maximum decoupling but making the overall flow implicit and hard to trace once it exceeds a few steps.","distractors":["A thread-pool bulkhead runs each dependency on its own threads, which lets calls queue, supports timeouts on the queue, and isolates even blocking calls — at the cost of extra threads and context-switching.","Atomicity is replaced by \"all forward steps complete, or all completed steps are compensated\" — but *not* isolation: between steps, other transactions can observe intermediate state (a reserved-but-not-yet-paid order).","Inject failures at each forward step and assert that exactly the compensations for already-completed steps run, in reverse order, and that they are idempotent under repeated invocation."],"distractorIds":["java/microservices_patterns#f0dd859642b1","java/microservices_patterns#6c1bead2c78f","java/microservices_patterns#d7dae91ae4e3"],"concepts":["decoupling","reacts","choreography","overall","implicit","orchestration"]},{"id":"java/microservices_patterns#c5392d352e92","section":"java","module":"java/microservices_patterns","moduleName":"microservices patterns","sourceFile":"README.md","difficulty":"intermediate","question":"What makes a good compensating transaction, and what if compensation itself fails?","answerFull":"A compensation must be idempotent (it may be retried), semantically undo the forward action (release the reservation, refund the charge — not necessarily a literal inverse), and be designed to essentially always succeed. If compensation fails, you cannot \"compensate the compensation,\" so you retry it with backoff indefinitely and alert a human/operator on permanent failure. This is why money-moving compensations get dead-letter queues and on-call alerts rather than silent catch blocks.","correct":"A compensation must be idempotent (it may be retried), semantically undo the forward action (release the reservation, refund the charge — not necessarily a literal inverse), and be designed to essentially always succeed.","distractors":["Record a unique identifier for each processed message (message id or business idempotency key) in a dedup store within the same transaction as the side effect, and skip messages whose id is already present.","Atomicity is replaced by \"all forward steps complete, or all completed steps are compensated\" — but *not* isolation: between steps, other transactions can observe intermediate state (a reserved-but-not-yet-paid order).","Inject failures at each forward step and assert that exactly the compensations for already-completed steps run, in reverse order, and that they are idempotent under repeated invocation."],"distractorIds":["java/microservices_patterns#1ce376b8271b","java/microservices_patterns#6c1bead2c78f","java/microservices_patterns#d7dae91ae4e3"],"concepts":["refund","charge","reservation","retried","essentially","necessarily"]},{"id":"java/microservices_patterns#be169068faaa","section":"java","module":"java/microservices_patterns","moduleName":"microservices patterns","sourceFile":"README.md","difficulty":"intermediate","question":"Why is 2PC/XA across microservices considered an anti-pattern?","answerFull":"Two-phase commit holds locks from the prepare phase until the coordinator says commit, and across network services that window includes slow/failed remote hops. A coordinator crash leaves participants \"in doubt,\" holding locks and blocking, which turns one slow service into a cascading multi-service outage. It also tightly couples services to a shared transaction manager. Sagas trade the strong guarantee for availability, which is almost always the right call between services.","correct":"Two-phase commit holds locks from the prepare phase until the coordinator says commit, and across network services that window includes slow/failed remote hops.","distractors":["You put a routing facade (proxy/gateway) in front of the monolith and incrementally redirect individual capabilities to new services, leaving the rest in the monolith, until the monolith is \"strangled\" and removable.","Use the Saga pattern: model the operation as a sequence of local ACID transactions, each with a compensating transaction that semantically undoes it.","Choreography has no central coordinator; each service reacts to events and emits its own, giving maximum decoupling but making the overall flow implicit and hard to trace once it exceeds a few steps."],"distractorIds":["java/microservices_patterns#aa786444a391","java/microservices_patterns#71f154763e3f","java/microservices_patterns#e5edd4fde5b7"],"concepts":["considered","anti","hops","microservices","prepare","2pc"]},{"id":"java/microservices_patterns#f7b629c54669","section":"java","module":"java/microservices_patterns","moduleName":"microservices patterns","sourceFile":"README.md","difficulty":"intermediate","question":"How does trace context propagate across an async/thread-pool boundary in Java?","answerFull":"ThreadLocal is bound to the *current* thread, so when you submit work to an executor the worker thread sees nothing. You must capture the context value on the submitting thread, then restore it on the worker thread at the start of the task and clear it in a finally. Frameworks do this by wrapping the Runnable/Callable. Java 21's ScopedValue is the modern fix — it is immutable and auto-unbound at the end of its scope, removing the leak risk inherent in mutable ThreadLocal.","correct":"ThreadLocal is bound to the *current* thread, so when you submit work to an executor the worker thread sees nothing.","distractors":["Choreography has no central coordinator; each service reacts to events and emits its own, giving maximum decoupling but making the overall flow implicit and hard to trace once it exceeds a few steps.","Because pool threads are reused across unrelated requests.","A bulkhead isolates resources so one dependency's saturation cannot consume the whole process — typically a dedicated bounded thread pool (or a semaphore permit count) per downstream."],"distractorIds":["java/microservices_patterns#e5edd4fde5b7","java/microservices_patterns#135309bef0d9","java/microservices_patterns#84413a00cdf2"],"concepts":["submit","propagate","threadlocal","executor","nothing","boundary"],"correctMd":"`ThreadLocal` is bound to the *current* thread, so when you submit work to an executor the worker thread sees nothing.","answerFullMd":"`ThreadLocal` is bound to the *current* thread, so when you submit work to an executor the worker thread sees nothing. You must capture the context value on the submitting thread, then restore it on the worker thread at the start of the task and clear it in a `finally`. Frameworks do this by wrapping the `Runnable`/`Callable`. Java 21's `ScopedValue` is the modern fix — it is immutable and auto-unbound at the end of its scope, removing the leak risk inherent in mutable `ThreadLocal`."},{"id":"java/microservices_patterns#135309bef0d9","section":"java","module":"java/microservices_patterns","moduleName":"microservices patterns","sourceFile":"README.md","difficulty":"intermediate","question":"Why must you clear a ThreadLocal in a finally block in pooled code?","answerFull":"Because pool threads are reused across unrelated requests. If you set a value and do not remove it, the next task that runs on that thread inherits a stale value — causing mis-attributed logs, wrong tenant context, or even data leakage between users. The finally guarantees cleanup even when the task throws. This entire hazard class is why ScopedValue (which cannot outlive its bound scope) was added.","correct":"Because pool threads are reused across unrelated requests.","distractors":["A compensation must be idempotent (it may be retried), semantically undo the forward action (release the reservation, refund the charge — not necessarily a literal inverse), and be designed to essentially always succeed.","ThreadLocal is bound to the *current* thread, so when you submit work to an executor the worker thread sees nothing.","A thread-pool bulkhead runs each dependency on its own threads, which lets calls queue, supports timeouts on the queue, and isolates even blocking calls — at the cost of extra threads and context-switching."],"distractorIds":["java/microservices_patterns#c5392d352e92","java/microservices_patterns#f7b629c54669","java/microservices_patterns#f0dd859642b1"],"concepts":["pooled","unrelated","reused","clear","threadlocal","finally"],"answerFullMd":"Because pool threads are reused across unrelated requests. If you set a value and do not remove it, the next task that runs on that thread inherits a stale value — causing mis-attributed logs, wrong tenant context, or even data leakage between users. The `finally` guarantees cleanup even when the task throws. This entire hazard class is why `ScopedValue` (which cannot outlive its bound scope) was added.","distractorsMd":[null,"`ThreadLocal` is bound to the *current* thread, so when you submit work to an executor the worker thread sees nothing.",null]},{"id":"java/microservices_patterns#84413a00cdf2","section":"java","module":"java/microservices_patterns","moduleName":"microservices patterns","sourceFile":"README.md","difficulty":"intermediate","question":"What is a bulkhead and how is it different from a circuit breaker?","answerFull":"A bulkhead isolates resources so one dependency's saturation cannot consume the whole process — typically a dedicated bounded thread pool (or a semaphore permit count) per downstream. A circuit breaker instead *stops calling* a failing dependency after an error threshold, failing fast and giving it time to recover. They are complementary: the bulkhead contains the blast radius of slowness; the breaker stops you from hammering something already down.","correct":"A bulkhead isolates resources so one dependency's saturation cannot consume the whole process — typically a dedicated bounded thread pool (or a semaphore permit count) per downstream.","distractors":["ThreadLocal is bound to the *current* thread, so when you submit work to an executor the worker thread sees nothing.","A thread-pool bulkhead runs each dependency on its own threads, which lets calls queue, supports timeouts on the queue, and isolates even blocking calls — at the cost of extra threads and context-switching.","Because pool threads are reused across unrelated requests."],"distractorIds":["java/microservices_patterns#f7b629c54669","java/microservices_patterns#f0dd859642b1","java/microservices_patterns#135309bef0d9"],"concepts":["permit","saturation","bulkhead","isolates","semaphore","breaker"],"distractorsMd":["`ThreadLocal` is bound to the *current* thread, so when you submit work to an executor the worker thread sees nothing.",null,null]},{"id":"java/microservices_patterns#f0dd859642b1","section":"java","module":"java/microservices_patterns","moduleName":"microservices patterns","sourceFile":"README.md","difficulty":"advanced","question":"Thread-pool bulkhead vs semaphore bulkhead — tradeoffs?","answerFull":"A thread-pool bulkhead runs each dependency on its own threads, which lets calls queue, supports timeouts on the queue, and isolates even blocking calls — at the cost of extra threads and context-switching. A semaphore bulkhead just caps concurrent calls with a permit counter on the caller's own thread: far cheaper and lower latency, but it cannot queue (callers are rejected immediately when permits run out) and a blocking call still blocks the caller's thread. Use thread pools for slow/blocking downstreams, semaphores for fast in-process limits.","correct":"A thread-pool bulkhead runs each dependency on its own threads, which lets calls queue, supports timeouts on the queue, and isolates even blocking calls — at the cost of extra threads and context-switching.","distractors":["Choreography has no central coordinator; each service reacts to events and emits its own, giving maximum decoupling but making the overall flow implicit and hard to trace once it exceeds a few steps.","A bulkhead isolates resources so one dependency's saturation cannot consume the whole process — typically a dedicated bounded thread pool (or a semaphore permit count) per downstream.","ThreadLocal is bound to the *current* thread, so when you submit work to an executor the worker thread sees nothing."],"distractorIds":["java/microservices_patterns#e5edd4fde5b7","java/microservices_patterns#84413a00cdf2","java/microservices_patterns#f7b629c54669"],"concepts":["timeouts","tradeoffs","switching","bulkhead","isolates","semaphore"],"distractorsMd":[null,null,"`ThreadLocal` is bound to the *current* thread, so when you submit work to an executor the worker thread sees nothing."]},{"id":"java/microservices_patterns#aa786444a391","section":"java","module":"java/microservices_patterns","moduleName":"microservices patterns","sourceFile":"README.md","difficulty":"advanced","question":"What is the strangler fig pattern and why prefer it over a rewrite?","answerFull":"You put a routing facade (proxy/gateway) in front of the monolith and incrementally redirect individual capabilities to new services, leaving the rest in the monolith, until the monolith is \"strangled\" and removable. It is preferred over a big-bang rewrite because each slice is small, independently testable, and reversible (route back to the monolith if the new service misbehaves), so the risk is bounded at every step instead of concentrated in one launch.","correct":"You put a routing facade (proxy/gateway) in front of the monolith and incrementally redirect individual capabilities to new services, leaving the rest in the monolith, until the monolith is \"strangled\" and removable.","distractors":["Two-phase commit holds locks from the prepare phase until the coordinator says commit, and across network services that window includes slow/failed remote hops.","Use the Saga pattern: model the operation as a sequence of local ACID transactions, each with a compensating transaction that semantically undoes it.","A bulkhead isolates resources so one dependency's saturation cannot consume the whole process — typically a dedicated bounded thread pool (or a semaphore permit count) per downstream."],"distractorIds":["java/microservices_patterns#be169068faaa","java/microservices_patterns#71f154763e3f","java/microservices_patterns#84413a00cdf2"],"concepts":["redirect","removable","strangled","facade","rewrite","fig"]},{"id":"java/microservices_patterns#d7dae91ae4e3","section":"java","module":"java/microservices_patterns","moduleName":"microservices patterns","sourceFile":"README.md","difficulty":"advanced","question":"How do you test a saga's failure and compensation paths?","answerFull":"Inject failures at each forward step and assert that exactly the compensations for already-completed steps run, in reverse order, and that they are idempotent under repeated invocation. Use a fault-injecting test double for each service, verify the saga log/state transitions, and include a \"compensation fails\" case to confirm retry/alert behavior rather than silent loss. Property-style tests that fail at a random step are effective at catching missing compensations.","correct":"Inject failures at each forward step and assert that exactly the compensations for already-completed steps run, in reverse order, and that they are idempotent under repeated invocation.","distractors":["Because the relay publishes the event and *then* marks the row as sent in two steps; if it crashes between them, the event is published again on restart.","Choreography has no central coordinator; each service reacts to events and emits its own, giving maximum decoupling but making the overall flow implicit and hard to trace once it exceeds a few steps.","Use the Saga pattern: model the operation as a sequence of local ACID transactions, each with a compensating transaction that semantically undoes it."],"distractorIds":["java/microservices_patterns#d325e28cace4","java/microservices_patterns#e5edd4fde5b7","java/microservices_patterns#71f154763e3f"],"concepts":["compensations","assert","compensation","reverse","inject","invocation"]},{"id":"java/microservices_patterns#6c1bead2c78f","section":"java","module":"java/microservices_patterns","moduleName":"microservices patterns","sourceFile":"README.md","difficulty":"advanced","question":"What consistency guarantees does a saga actually provide?","answerFull":"Atomicity is replaced by \"all forward steps complete, or all completed steps are compensated\" — but *not* isolation: between steps, other transactions can observe intermediate state (a reserved-but-not-yet-paid order). This means you may need semantic locks (e.g., a PENDING status) or commutative updates to avoid anomalies like a customer seeing an order that later gets compensated away. Durability still holds locally per step. In short: ACD without the I.","correct":"Atomicity is replaced by \"all forward steps complete, or all completed steps are compensated\" — but *not* isolation: between steps, other transactions can observe intermediate state (a reserved-but-not-yet-paid order).","distractors":["Because the relay publishes the event and *then* marks the row as sent in two steps; if it crashes between them, the event is published again on restart.","Use the Saga pattern: model the operation as a sequence of local ACID transactions, each with a compensating transaction that semantically undoes it.","Inject failures at each forward step and assert that exactly the compensations for already-completed steps run, in reverse order, and that they are idempotent under repeated invocation."],"distractorIds":["java/microservices_patterns#d325e28cace4","java/microservices_patterns#71f154763e3f","java/microservices_patterns#d7dae91ae4e3"],"concepts":["compensated","paid","reserved","replaced","atomicity","observe"],"answerFullMd":"Atomicity is replaced by \"all forward steps complete, or all completed steps are compensated\" — but *not* isolation: between steps, other transactions can observe intermediate state (a reserved-but-not-yet-paid order). This means you may need semantic locks (e.g., a `PENDING` status) or commutative updates to avoid anomalies like a customer seeing an order that later gets compensated away. Durability still holds locally per step. In short: ACD without the I."},{"id":"java/networking_and_http_client#0e79548e1522","section":"java","module":"java/networking_and_http_client","moduleName":"networking and http client","sourceFile":"README.md","difficulty":"core","question":"How does HttpClient.sendAsync() work internally?","answerFull":"HttpClient.sendAsync() returns a CompletableFuture<HttpResponse<T>> immediately. Internally, the HttpClient uses an Executor (configurable; defaults to ForkJoinPool.commonPool() if not set) for completion callbacks. The actual I/O is handled via the JDK's internal async HTTP implementation which uses NIO (selectors) for HTTP/1.1 and a separate multiplexed connection pool for HTTP/2. When the response arrives, the completion callbacks (.thenApply() etc.) run on the executor. Best practice: provide a custom executor (Executors.newVirtualThreadPerTaskExecutor()) to control thread usage.","correct":"HttpClient.sendAsync() returns a CompletableFuture<HttpResponse<T>> immediately.","distractors":["HttpClient uses the JVM's default SSLContext (configured via javax.net.ssl.* system properties) unless you provide a custom one via HttpClient.newBuilder().sslContext(customSSLContext).","BodyHandlers.ofInputStream() returns a HttpResponse<InputStream> where the body is lazily consumed.","HttpClient uses an internal connection pool with no built-in metrics exposed via JMX or Micrometer."],"distractorIds":["java/networking_and_http_client#b42db1a84768","java/networking_and_http_client#20566b774c33","java/networking_and_http_client#0e2842d0146a"],"concepts":["sendasync","httpresponse","httpclient","completablefuture","internally","immediately"],"questionMd":"How does `HttpClient.sendAsync()` work internally?","correctMd":"`HttpClient.sendAsync()` returns a `CompletableFuture<HttpResponse<T>>` immediately.","answerFullMd":"`HttpClient.sendAsync()` returns a `CompletableFuture<HttpResponse<T>>` immediately. Internally, the `HttpClient` uses an `Executor` (configurable; defaults to `ForkJoinPool.commonPool()` if not set) for completion callbacks. The actual I/O is handled via the JDK's internal async HTTP implementation which uses NIO (selectors) for HTTP/1.1 and a separate multiplexed connection pool for HTTP/2. When the response arrives, the completion callbacks (`.thenApply()` etc.) run on the executor. Best practice: provide a custom executor (`Executors.newVirtualThreadPerTaskExecutor()`) to control thread usage.","distractorsMd":["`HttpClient` uses the JVM's default `SSLContext` (configured via `javax.net.ssl.*` system properties) unless you provide a custom one via `HttpClient.newBuilder().sslContext(customSSLContext)`.","`BodyHandlers.ofInputStream()` returns a `HttpResponse<InputStream>` where the body is lazily consumed.","`HttpClient` uses an internal connection pool with no built-in metrics exposed via JMX or Micrometer."]},{"id":"java/networking_and_http_client#67c91f242001","section":"java","module":"java/networking_and_http_client","moduleName":"networking and http client","sourceFile":"README.md","difficulty":"core","question":"What is the difference between NIO Selector and NIO2 asynchronous channels?","answerFull":"NIO Selector (Java 4): non-blocking polling model. A thread calls selector.select(), which returns when any registered channel has pending I/O. The thread then handles the I/O itself. Still synchronous from the application's perspective — the event-loop thread does the work. NIO2 async channels (Java 7): true async model — the application registers a CompletionHandler callback. The OS notifies the JVM when I/O completes, and the JVM invokes the callback on a system-managed thread pool. NIO2 is conceptually simpler for some patterns but the callback model leads to \"callback hell.\" Most production code uses NIO (via Netty) rather than NIO2 directly.","correct":"NIO Selector (Java 4): non-blocking polling model.","distractors":["With virtual threads (Java 21), blocking I/O calls like socket.read() are reimplemented internally using NIO selectors.","(1) Open ServerSocketChannel, configure non-blocking, bind to port.","HttpClient implements AutoCloseable (Java 21)."],"distractorIds":["java/networking_and_http_client#eddd68b31622","java/networking_and_http_client#6eeef19b440a","java/networking_and_http_client#fdec1e0ab66a"],"concepts":["nio","asynchronous","selector","polling","channels","difference"],"questionMd":"What is the difference between NIO `Selector` and NIO2 asynchronous channels?","correctMd":"NIO `Selector` (Java 4): non-blocking polling model.","answerFullMd":"NIO `Selector` (Java 4): non-blocking polling model. A thread calls `selector.select()`, which returns when any registered channel has pending I/O. The thread then handles the I/O itself. Still synchronous from the application's perspective — the event-loop thread does the work. NIO2 async channels (Java 7): true async model — the application registers a `CompletionHandler` callback. The OS notifies the JVM when I/O completes, and the JVM invokes the callback on a system-managed thread pool. NIO2 is conceptually simpler for some patterns but the callback model leads to \"callback hell.\" Most production code uses NIO (via Netty) rather than NIO2 directly.","distractorsMd":["With virtual threads (Java 21), blocking I/O calls like `socket.read()` are reimplemented internally using NIO selectors.","(1) Open `ServerSocketChannel`, configure non-blocking, bind to port.","`HttpClient` implements `AutoCloseable` (Java 21)."]},{"id":"java/networking_and_http_client#87b9bdab4213","section":"java","module":"java/networking_and_http_client","moduleName":"networking and http client","sourceFile":"README.md","difficulty":"core","question":"What is the Reactor pattern and how does Java NIO map to it?","answerFull":"The Reactor pattern: one or more Reactor threads run event loops, demultiplexing I/O events and dispatching them to registered handlers. Java NIO maps directly: Selector is the Reactor, SelectionKey is the event, handler code in the isReadable()/isWritable() branches is the handler, and SocketChannel is the channel abstraction. Netty extends this with NioEventLoop (Reactor thread) and ChannelHandler pipeline for handler composition. The key invariant: handlers must never block — they must complete quickly and return control to the event loop.","correct":"The Reactor pattern: one or more Reactor threads run event loops, demultiplexing I/O events and dispatching them to registered handlers.","distractors":["NIO Selector (Java 4): non-blocking polling model.","HttpClient implements AutoCloseable (Java 21).","With virtual threads (Java 21), blocking I/O calls like socket.read() are reimplemented internally using NIO selectors."],"distractorIds":["java/networking_and_http_client#67c91f242001","java/networking_and_http_client#fdec1e0ab66a","java/networking_and_http_client#eddd68b31622"],"concepts":["demultiplexing","dispatching","nio","handlers","reactor","loops"],"correctMd":"The Reactor pattern: one or more `Reactor` threads run event loops, demultiplexing I/O events and dispatching them to registered handlers.","answerFullMd":"The Reactor pattern: one or more `Reactor` threads run event loops, demultiplexing I/O events and dispatching them to registered handlers. Java NIO maps directly: `Selector` is the Reactor, `SelectionKey` is the event, handler code in the `isReadable()`/`isWritable()` branches is the handler, and `SocketChannel` is the channel abstraction. Netty extends this with `NioEventLoop` (Reactor thread) and `ChannelHandler` pipeline for handler composition. The key invariant: handlers must never block — they must complete quickly and return control to the event loop.","distractorsMd":["NIO `Selector` (Java 4): non-blocking polling model.","`HttpClient` implements `AutoCloseable` (Java 21).","With virtual threads (Java 21), blocking I/O calls like `socket.read()` are reimplemented internally using NIO selectors."]},{"id":"java/networking_and_http_client#b907aa70cdc3","section":"java","module":"java/networking_and_http_client","moduleName":"networking and http client","sourceFile":"README.md","difficulty":"core","question":"What are the limitations of one-thread-per-connection blocking I/O?","answerFull":"(1) Memory: each platform thread requires ~512KB-1MB stack. 10,000 connections = 5-10GB stack memory alone. (2) Context switches: OS scheduling 10,000 threads causes significant overhead (~microseconds per switch × millions of switches per second). (3) Scheduler thrashing: OS spends more time deciding which thread to run than actually running them. (4) File descriptor limits: OS limits open FDs (default ~1024 on Linux, configurable to ~65536). With Java 21 virtual threads: virtual thread stacks start at ~few KB, scheduler is JVM (not OS), carrier threads = CPU count. Virtual threads restore the simplicity of blocking code at NIO-level concurrency.","correct":"(1) Memory: each platform thread requires ~512KB-1MB stack.","distractors":["OP_WRITE fires whenever the socket's outbound buffer has space — which is almost always true for a healthy connection.","(1) Open ServerSocketChannel, configure non-blocking, bind to port.","Thread-per-connection: one OS platform thread per socket (~1 MB stack, ~2,000 practical limit per JVM)."],"distractorIds":["java/networking_and_http_client#366173b33135","java/networking_and_http_client#6eeef19b440a","java/networking_and_http_client#d2abb3269d5d"],"concepts":["512kb","1mb","limitations","platform","blocking","stack"],"distractorsMd":["`OP_WRITE` fires whenever the socket's outbound buffer has space — which is almost always true for a healthy connection.","(1) Open `ServerSocketChannel`, configure non-blocking, bind to port.",null]},{"id":"java/networking_and_http_client#6eeef19b440a","section":"java","module":"java/networking_and_http_client","moduleName":"networking and http client","sourceFile":"README.md","difficulty":"core","question":"How would you implement a basic NIO server?","answerFull":"(1) Open ServerSocketChannel, configure non-blocking, bind to port. (2) Open Selector. (3) Register ServerSocketChannel with selector for OP_ACCEPT. (4) Event loop: call selector.select(), iterate selectedKeys(). (5) On OP_ACCEPT: call serverChannel.accept(), configure new channel non-blocking, register for OP_READ. (6) On OP_READ: read into ByteBuffer, flip, process data, write response, register for OP_WRITE if write buffer full. (7) On OP_WRITE: write remaining data, deregister OP_WRITE when done. (8) Always remove keys from selectedKeys() iterator. See Section 6 for complete implementation.","correct":"(1) Open ServerSocketChannel, configure non-blocking, bind to port.","distractors":["With virtual threads (Java 21), blocking I/O calls like socket.read() are reimplemented internally using NIO selectors.","HTTP/2 server push allows the server to proactively send resources to the client before the client requests them (e.g., pushing style.css when it sees GET /index.html).","NIO Selector (Java 4): non-blocking polling model."],"distractorIds":["java/networking_and_http_client#eddd68b31622","java/networking_and_http_client#ed9610f4c7a1","java/networking_and_http_client#67c91f242001"],"concepts":["serversocketchannel","basic","nio","configure","bind","port"],"correctMd":"(1) Open `ServerSocketChannel`, configure non-blocking, bind to port.","answerFullMd":"(1) Open `ServerSocketChannel`, configure non-blocking, bind to port. (2) Open `Selector`. (3) Register `ServerSocketChannel` with selector for `OP_ACCEPT`. (4) Event loop: call `selector.select()`, iterate `selectedKeys()`. (5) On `OP_ACCEPT`: call `serverChannel.accept()`, configure new channel non-blocking, register for `OP_READ`. (6) On `OP_READ`: read into `ByteBuffer`, flip, process data, write response, register for `OP_WRITE` if write buffer full. (7) On `OP_WRITE`: write remaining data, deregister `OP_WRITE` when done. (8) Always remove keys from `selectedKeys()` iterator. See Section 6 for complete implementation.","distractorsMd":["With virtual threads (Java 21), blocking I/O calls like `socket.read()` are reimplemented internally using NIO selectors.","HTTP/2 server push allows the server to proactively send resources to the client before the client requests them (e.g., pushing `style.css` when it sees `GET /index.html`).","NIO `Selector` (Java 4): non-blocking polling model."]},{"id":"java/networking_and_http_client#b42db1a84768","section":"java","module":"java/networking_and_http_client","moduleName":"networking and http client","sourceFile":"README.md","difficulty":"intermediate","question":"How does HttpClient handle TLS/SSL?","answerFull":"HttpClient uses the JVM's default SSLContext (configured via javax.net.ssl.* system properties) unless you provide a custom one via HttpClient.newBuilder().sslContext(customSSLContext). For testing, you can create a SSLContext that trusts all certificates — never in production. Certificate pinning requires a custom TrustManager. HTTP/2 over HTTPS uses TLS ALPN (Application Layer Protocol Negotiation) extension — the client and server negotiate \"h2\" during TLS handshake. HTTP/2 over plain text (h2c) is technically possible but rarely used.","correct":"HttpClient uses the JVM's default SSLContext (configured via javax.net.ssl.* system properties) unless you provide a custom one via HttpClient.newBuilder().sslContext(customSSLContext).","distractors":["HttpClient implements AutoCloseable (Java 21).","HttpClient uses an internal connection pool with no built-in metrics exposed via JMX or Micrometer.","Thread-per-connection: one OS platform thread per socket (~1 MB stack, ~2,000 practical limit per JVM)."],"distractorIds":["java/networking_and_http_client#fdec1e0ab66a","java/networking_and_http_client#0e2842d0146a","java/networking_and_http_client#d2abb3269d5d"],"concepts":["customsslcontext","newbuilder","sslcontext","httpclient","javax","net"],"questionMd":"How does `HttpClient` handle TLS/SSL?","correctMd":"`HttpClient` uses the JVM's default `SSLContext` (configured via `javax.net.ssl.*` system properties) unless you provide a custom one via `HttpClient.newBuilder().sslContext(customSSLContext)`.","answerFullMd":"`HttpClient` uses the JVM's default `SSLContext` (configured via `javax.net.ssl.*` system properties) unless you provide a custom one via `HttpClient.newBuilder().sslContext(customSSLContext)`. For testing, you can create a `SSLContext` that trusts all certificates — never in production. Certificate pinning requires a custom `TrustManager`. HTTP/2 over HTTPS uses TLS ALPN (Application Layer Protocol Negotiation) extension — the client and server negotiate \"h2\" during TLS handshake. HTTP/2 over plain text (h2c) is technically possible but rarely used.","distractorsMd":["`HttpClient` implements `AutoCloseable` (Java 21).","`HttpClient` uses an internal connection pool with no built-in metrics exposed via JMX or Micrometer.",null]},{"id":"java/networking_and_http_client#eddd68b31622","section":"java","module":"java/networking_and_http_client","moduleName":"networking and http client","sourceFile":"README.md","difficulty":"intermediate","question":"How does virtual thread I/O differ from NIO at the implementation level?","answerFull":"With virtual threads (Java 21), blocking I/O calls like socket.read() are reimplemented internally using NIO selectors. When a virtual thread calls read() on a socket, the JVM registers the channel with an internal selector and parks (unmounts) the virtual thread. When data arrives, the selector wakes up, the virtual thread is rescheduled, and resumes at the read() call as if it returned normally. The application writes simple blocking code; the JVM uses NIO internally. This is the \"loom\" project vision: blocking API, NIO scalability, no callback complexity.","correct":"With virtual threads (Java 21), blocking I/O calls like socket.read() are reimplemented internally using NIO selectors.","distractors":["(1) Open ServerSocketChannel, configure non-blocking, bind to port.","(1) Memory: each platform thread requires ~512KB-1MB stack.","The Reactor pattern: one or more Reactor threads run event loops, demultiplexing I/O events and dispatching them to registered handlers."],"distractorIds":["java/networking_and_http_client#6eeef19b440a","java/networking_and_http_client#b907aa70cdc3","java/networking_and_http_client#87b9bdab4213"],"concepts":["reimplemented","nio","selectors","internally","differ","socket"],"correctMd":"With virtual threads (Java 21), blocking I/O calls like `socket.read()` are reimplemented internally using NIO selectors.","answerFullMd":"With virtual threads (Java 21), blocking I/O calls like `socket.read()` are reimplemented internally using NIO selectors. When a virtual thread calls `read()` on a socket, the JVM registers the channel with an internal selector and parks (unmounts) the virtual thread. When data arrives, the selector wakes up, the virtual thread is rescheduled, and resumes at the `read()` call as if it returned normally. The application writes simple blocking code; the JVM uses NIO internally. This is the \"loom\" project vision: blocking API, NIO scalability, no callback complexity.","distractorsMd":["(1) Open `ServerSocketChannel`, configure non-blocking, bind to port.",null,"The Reactor pattern: one or more `Reactor` threads run event loops, demultiplexing I/O events and dispatching them to registered handlers."]},{"id":"java/networking_and_http_client#20566b774c33","section":"java","module":"java/networking_and_http_client","moduleName":"networking and http client","sourceFile":"README.md","difficulty":"intermediate","question":"What are BodyHandlers.ofInputStream() and when do you use it?","answerFull":"BodyHandlers.ofInputStream() returns a HttpResponse<InputStream> where the body is lazily consumed. Unlike ofString() (buffers entire response in memory) or ofBytes() (same), ofInputStream() lets you process the response body as a stream without loading it all into memory. Use it for: large file downloads, streaming JSON (combined with a streaming JSON parser like Jackson's StreamingParser), large CSV processing. Remember to close the InputStream when done — try-with-resources on the response handles this if you call response.body().close() or use the response in a try block.","correct":"BodyHandlers.ofInputStream() returns a HttpResponse<InputStream> where the body is lazily consumed.","distractors":["HttpClient.sendAsync() returns a CompletableFuture<HttpResponse<T>> immediately.","(1) Memory: each platform thread requires ~512KB-1MB stack.","(1) Open ServerSocketChannel, configure non-blocking, bind to port."],"distractorIds":["java/networking_and_http_client#0e79548e1522","java/networking_and_http_client#b907aa70cdc3","java/networking_and_http_client#6eeef19b440a"],"concepts":["bodyhandlers","ofinputstream","httpresponse","inputstream","lazily","consumed"],"questionMd":"What are `BodyHandlers.ofInputStream()` and when do you use it?","correctMd":"`BodyHandlers.ofInputStream()` returns a `HttpResponse<InputStream>` where the body is lazily consumed.","answerFullMd":"`BodyHandlers.ofInputStream()` returns a `HttpResponse<InputStream>` where the body is lazily consumed. Unlike `ofString()` (buffers entire response in memory) or `ofBytes()` (same), `ofInputStream()` lets you process the response body as a stream without loading it all into memory. Use it for: large file downloads, streaming JSON (combined with a streaming JSON parser like Jackson's `StreamingParser`), large CSV processing. Remember to close the `InputStream` when done — `try-with-resources` on the response handles this if you call `response.body().close()` or use the `response` in a try block.","distractorsMd":["`HttpClient.sendAsync()` returns a `CompletableFuture<HttpResponse<T>>` immediately.",null,"(1) Open `ServerSocketChannel`, configure non-blocking, bind to port."]},{"id":"java/networking_and_http_client#fdec1e0ab66a","section":"java","module":"java/networking_and_http_client","moduleName":"networking and http client","sourceFile":"README.md","difficulty":"intermediate","question":"What happens to in-flight HttpClient requests when the application shuts down?","answerFull":"HttpClient implements AutoCloseable (Java 21). If not explicitly closed, the client's internal connection pool and executor survive until GC. For clean shutdown: call client.close() (Java 21) which waits for in-flight requests to complete then closes connections, or client.shutdown() (Java 21) which initiates graceful shutdown and returns a CompletableFuture<Void> completing when done. In Java 11-20, there is no close() method — shutdown is handled by the provided executor's shutdown. Best practice: use a custom executor so you control its lifecycle independently of the HttpClient.","correct":"HttpClient implements AutoCloseable (Java 21).","distractors":["With virtual threads (Java 21), blocking I/O calls like socket.read() are reimplemented internally using NIO selectors.","HttpClient.sendAsync() returns a CompletableFuture<HttpResponse<T>> immediately.","HTTP/2 server push allows the server to proactively send resources to the client before the client requests them (e.g., pushing style.css when it sees GET /index.html)."],"distractorIds":["java/networking_and_http_client#eddd68b31622","java/networking_and_http_client#0e79548e1522","java/networking_and_http_client#ed9610f4c7a1"],"concepts":["autocloseable","shuts","httpclient","flight","implements","happens"],"questionMd":"What happens to in-flight `HttpClient` requests when the application shuts down?","correctMd":"`HttpClient` implements `AutoCloseable` (Java 21).","answerFullMd":"`HttpClient` implements `AutoCloseable` (Java 21). If not explicitly closed, the client's internal connection pool and executor survive until GC. For clean shutdown: call `client.close()` (Java 21) which waits for in-flight requests to complete then closes connections, or `client.shutdown()` (Java 21) which initiates graceful shutdown and returns a `CompletableFuture<Void>` completing when done. In Java 11-20, there is no `close()` method — shutdown is handled by the provided executor's shutdown. Best practice: use a custom executor so you control its lifecycle independently of the `HttpClient`.","distractorsMd":["With virtual threads (Java 21), blocking I/O calls like `socket.read()` are reimplemented internally using NIO selectors.","`HttpClient.sendAsync()` returns a `CompletableFuture<HttpResponse<T>>` immediately.","HTTP/2 server push allows the server to proactively send resources to the client before the client requests them (e.g., pushing `style.css` when it sees `GET /index.html`)."]},{"id":"java/networking_and_http_client#d2abb3269d5d","section":"java","module":"java/networking_and_http_client","moduleName":"networking and http client","sourceFile":"README.md","difficulty":"advanced","question":"How does Selector-based NIO differ from a thread-per-connection model, and what did Java 21 virtual threads change?","answerFull":"Thread-per-connection: one OS platform thread per socket (~1 MB stack, ~2,000 practical limit per JVM). Selector-based NIO: one or a few threads call select(), which returns only channels with pending I/O — each thread handles thousands of connections via a state machine per channel. NIO scales to 100k+ connections with fixed threads but requires explicit non-blocking state machines (complex code). Java 21 virtual threads change the calculus: they are lightweight (~few KB stack, millions possible) and automatically yield when blocked on I/O — giving you thread-per-connection's simple programming model with NIO's scalability. New server code should use virtual threads; NIO remains essential for understanding existing high-performance libraries (Netty, gRPC) and situations where sub-millisecond event-loop latency is required.","correct":"Thread-per-connection: one OS platform thread per socket (~1 MB stack, ~2,000 practical limit per JVM).","distractors":["HttpClient uses an internal connection pool with no built-in metrics exposed via JMX or Micrometer.","OP_WRITE fires whenever the socket's outbound buffer has space — which is almost always true for a healthy connection.","HttpClient uses the JVM's default SSLContext (configured via javax.net.ssl.* system properties) unless you provide a custom one via HttpClient.newBuilder().sslContext(customSSLContext)."],"distractorIds":["java/networking_and_http_client#0e2842d0146a","java/networking_and_http_client#366173b33135","java/networking_and_http_client#b42db1a84768"],"concepts":["nio","practical","selector","did","differ","socket"],"questionMd":"How does `Selector`-based NIO differ from a thread-per-connection model, and what did Java 21 virtual threads change?","answerFullMd":"Thread-per-connection: one OS platform thread per socket (~1 MB stack, ~2,000 practical limit per JVM). `Selector`-based NIO: one or a few threads call `select()`, which returns only channels with pending I/O — each thread handles thousands of connections via a state machine per channel. NIO scales to 100k+ connections with fixed threads but requires explicit non-blocking state machines (complex code). Java 21 virtual threads change the calculus: they are lightweight (~few KB stack, millions possible) and automatically yield when blocked on I/O — giving you thread-per-connection's simple programming model with NIO's scalability. New server code should use virtual threads; NIO remains essential for understanding existing high-performance libraries (Netty, gRPC) and situations where sub-millisecond event-loop latency is required.","distractorsMd":["`HttpClient` uses an internal connection pool with no built-in metrics exposed via JMX or Micrometer.","`OP_WRITE` fires whenever the socket's outbound buffer has space — which is almost always true for a healthy connection.","`HttpClient` uses the JVM's default `SSLContext` (configured via `javax.net.ssl.*` system properties) unless you provide a custom one via `HttpClient.newBuilder().sslContext(customSSLContext)`."]},{"id":"java/networking_and_http_client#366173b33135","section":"java","module":"java/networking_and_http_client","moduleName":"networking and http client","sourceFile":"README.md","difficulty":"advanced","question":"What is the SelectionKey.OP_WRITE pitfall and how does it cause a 100 % CPU spin?","answerFull":"OP_WRITE fires whenever the socket's outbound buffer has space — which is almost always true for a healthy connection. If you register OP_WRITE and forget to deregister it after the write completes, Selector.select() returns immediately every iteration, burning 100 % CPU in a tight loop. The correct pattern: java // BROKEN: always registered, spins at 100% CPU channel.register(selector, SelectionKey.OP_READ | SelectionKey.OP_WRITE); // FIXED: register OP_WRITE only when there is data to write; remove it when done // When you have data to send: key.interestOps(key.interestOps() | SelectionKey.OP_WRITE); // Inside the OP_WRITE handler, after writing: if (buffer.remaining() == 0) { key.interestOps(key.interestOps() & ~SelectionKey.OP_WRITE); // deregister } Practical guidance: always profile selector-loop CPU usage in load tests; a 100 % core indicates an always-ready OP_WRITE registration.","correct":"OP_WRITE fires whenever the socket's outbound buffer has space — which is almost always true for a healthy connection.","distractors":["Thread-per-connection: one OS platform thread per socket (~1 MB stack, ~2,000 practical limit per JVM).","With virtual threads (Java 21), blocking I/O calls like socket.read() are reimplemented internally using NIO selectors.","HttpClient uses an internal connection pool with no built-in metrics exposed via JMX or Micrometer."],"distractorIds":["java/networking_and_http_client#d2abb3269d5d","java/networking_and_http_client#eddd68b31622","java/networking_and_http_client#0e2842d0146a"],"concepts":["healthy","outbound","fires","whenever","cause","socket"],"questionMd":"What is the `SelectionKey.OP_WRITE` pitfall and how does it cause a 100 % CPU spin?","correctMd":"`OP_WRITE` fires whenever the socket's outbound buffer has space — which is almost always true for a healthy connection.","answerFullMd":"`OP_WRITE` fires whenever the socket's outbound buffer has space — which is almost always true for a healthy connection. If you register `OP_WRITE` and forget to deregister it after the write completes, `Selector.select()` returns immediately every iteration, burning 100 % CPU in a tight loop. The correct pattern: ```java // BROKEN: always registered, spins at 100% CPU channel.register(selector, SelectionKey.OP_READ | SelectionKey.OP_WRITE); // FIXED: register OP_WRITE only when there is data to write; remove it when done // When you have data to send: key.interestOps(key.interestOps() | SelectionKey.OP_WRITE); // Inside the OP_WRITE handler, after writing: if (buffer.remaining() == 0) { key.interestOps(key.interestOps() & ~SelectionKey.OP_WRITE); // deregister } ``` Practical guidance: always profile selector-loop CPU usage in load tests; a 100 % core indicates an always-ready `OP_WRITE` registration.","distractorsMd":[null,"With virtual threads (Java 21), blocking I/O calls like `socket.read()` are reimplemented internally using NIO selectors.","`HttpClient` uses an internal connection pool with no built-in metrics exposed via JMX or Micrometer."]},{"id":"java/networking_and_http_client#ed9610f4c7a1","section":"java","module":"java/networking_and_http_client","moduleName":"networking and http client","sourceFile":"README.md","difficulty":"advanced","question":"What is HTTP/2 server push and why did most browsers and HttpClient abandon it?","answerFull":"HTTP/2 server push allows the server to proactively send resources to the client before the client requests them (e.g., pushing style.css when it sees GET /index.html). Java HttpClient supported it via PushPromiseHandler. In practice, server push proved harmful: the server often pushes resources the client already has cached, wasting bandwidth and causing cache collisions. Chrome removed server push support in 2022. HttpClient's push API still exists but should not be used for new systems. The more useful HTTP/2 feature is multiplexed request pipelining — many concurrent requests over one connection — which HttpClient uses automatically when negotiated via ALPN.","correct":"HTTP/2 server push allows the server to proactively send resources to the client before the client requests them (e.g., pushing style.css when it sees GET /index.html).","distractors":["HttpClient.sendAsync() returns a CompletableFuture<HttpResponse<T>> immediately.","HttpClient implements AutoCloseable (Java 21).","HttpClient uses an internal connection pool with no built-in metrics exposed via JMX or Micrometer."],"distractorIds":["java/networking_and_http_client#0e79548e1522","java/networking_and_http_client#fdec1e0ab66a","java/networking_and_http_client#0e2842d0146a"],"concepts":["css","proactively","html","httpclient","pushing","did"],"questionMd":"What is HTTP/2 server push and why did most browsers and `HttpClient` abandon it?","correctMd":"HTTP/2 server push allows the server to proactively send resources to the client before the client requests them (e.g., pushing `style.css` when it sees `GET /index.html`).","answerFullMd":"HTTP/2 server push allows the server to proactively send resources to the client before the client requests them (e.g., pushing `style.css` when it sees `GET /index.html`). Java `HttpClient` supported it via `PushPromiseHandler`. In practice, server push proved harmful: the server often pushes resources the client already has cached, wasting bandwidth and causing cache collisions. Chrome removed server push support in 2022. `HttpClient`'s push API still exists but should not be used for new systems. The more useful HTTP/2 feature is multiplexed request pipelining — many concurrent requests over one connection — which `HttpClient` uses automatically when negotiated via ALPN.","distractorsMd":["`HttpClient.sendAsync()` returns a `CompletableFuture<HttpResponse<T>>` immediately.","`HttpClient` implements `AutoCloseable` (Java 21).","`HttpClient` uses an internal connection pool with no built-in metrics exposed via JMX or Micrometer."]},{"id":"java/networking_and_http_client#0e2842d0146a","section":"java","module":"java/networking_and_http_client","moduleName":"networking and http client","sourceFile":"README.md","difficulty":"advanced","question":"How do you diagnose and fix connection pool exhaustion in HttpClient under high concurrency?","answerFull":"HttpClient uses an internal connection pool with no built-in metrics exposed via JMX or Micrometer. Symptoms of exhaustion: requests queue up, p99 latency spikes, HttpTimeoutException at request.timeout(). Diagnosis steps: (1) enable JDK HTTP client logging with -Djdk.httpclient.HttpClient.level=ALL; (2) check if target server closes connections early (look for GOAWAY in HTTP/2 or Connection: close); (3) measure actual concurrency — if sendAsync() is called with 1,000 unbounded CompletableFutures, all 1,000 requests compete for pool slots. Fix: use a Semaphore to bound concurrent in-flight requests to a sensible limit (e.g., 50 per host); drain results before issuing more. For HTTP/2, confirm the server advertises MAX_CONCURRENT_STREAMS and that the client respects it — Java's HttpClient does respect the server's MAX_CONCURRENT_STREAMS setting automatically.","correct":"HttpClient uses an internal connection pool with no built-in metrics exposed via JMX or Micrometer.","distractors":["Thread-per-connection: one OS platform thread per socket (~1 MB stack, ~2,000 practical limit per JVM).","HttpClient uses the JVM's default SSLContext (configured via javax.net.ssl.* system properties) unless you provide a custom one via HttpClient.newBuilder().sslContext(customSSLContext).","HttpClient implements AutoCloseable (Java 21)."],"distractorIds":["java/networking_and_http_client#d2abb3269d5d","java/networking_and_http_client#b42db1a84768","java/networking_and_http_client#fdec1e0ab66a"],"concepts":["diagnose","exhaustion","httpclient","exposed","jmx","micrometer"],"questionMd":"How do you diagnose and fix connection pool exhaustion in `HttpClient` under high concurrency?","correctMd":"`HttpClient` uses an internal connection pool with no built-in metrics exposed via JMX or Micrometer.","answerFullMd":"`HttpClient` uses an internal connection pool with no built-in metrics exposed via JMX or Micrometer. Symptoms of exhaustion: requests queue up, p99 latency spikes, `HttpTimeoutException` at `request.timeout()`. Diagnosis steps: (1) enable JDK HTTP client logging with `-Djdk.httpclient.HttpClient.level=ALL`; (2) check if target server closes connections early (look for `GOAWAY` in HTTP/2 or `Connection: close`); (3) measure actual concurrency — if `sendAsync()` is called with 1,000 unbounded `CompletableFuture`s, all 1,000 requests compete for pool slots. Fix: use a `Semaphore` to bound concurrent in-flight requests to a sensible limit (e.g., 50 per host); drain results before issuing more. For HTTP/2, confirm the server advertises `MAX_CONCURRENT_STREAMS` and that the client respects it — Java's `HttpClient` does respect the server's `MAX_CONCURRENT_STREAMS` setting automatically.","distractorsMd":[null,"`HttpClient` uses the JVM's default `SSLContext` (configured via `javax.net.ssl.*` system properties) unless you provide a custom one via `HttpClient.newBuilder().sslContext(customSSLContext)`.","`HttpClient` implements `AutoCloseable` (Java 21)."]},{"id":"java/performance_and_tuning#8596d27fe55f","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"README.md","difficulty":"core","question":"How do you size a thread pool for CPU-bound vs I/O-bound work?","answerFull":"CPU-bound: # CPU cores + 1 — one extra thread covers rare scheduling gaps; more than this causes context switch overhead with no CPU-time benefit. I/O-bound: # CPU cores × (1 + W/C) where W = average wait time and C = average compute time per task. If tasks spend 90% waiting (DB calls, HTTP): cores × 10. Practical cap: bounded by the DB connection pool size or downstream service limits. On Java 21, use virtual threads for I/O-bound work — the JVM schedules them on carrier threads automatically, eliminating manual sizing.","correct":"CPU-bound: # CPU cores + 1 — one extra thread covers rare scheduling gaps; more than this causes context switch overhead with no CPU-time benefit.","distractors":["(1) Small collections: the ForkJoinPool overhead (task splitting, merging, thread scheduling) exceeds the computation time.","JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while jcmd, jstack, and jmap are command-line tools that use the local attach API and need no open JMX port.","JMX exposes current gauge values like heap-used-now, while JFR records a time-ordered stream of events such as allocations and GC pauses at about 1% overhead."],"distractorIds":["java/performance_and_tuning#e96671111516","java/performance_and_tuning#5c704f3e202b","java/performance_and_tuning#5664500cbd45"],"concepts":["benefit","gaps","covers","cores","scheduling","rare"],"correctMd":"CPU-bound: `# CPU cores + 1` — one extra thread covers rare scheduling gaps; more than this causes context switch overhead with no CPU-time benefit.","answerFullMd":"CPU-bound: `# CPU cores + 1` — one extra thread covers rare scheduling gaps; more than this causes context switch overhead with no CPU-time benefit. I/O-bound: `# CPU cores × (1 + W/C)` where W = average wait time and C = average compute time per task. If tasks spend 90% waiting (DB calls, HTTP): `cores × 10`. Practical cap: bounded by the DB connection pool size or downstream service limits. On Java 21, use virtual threads for I/O-bound work — the JVM schedules them on carrier threads automatically, eliminating manual sizing.","distractorsMd":[null,"JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while `jcmd`, `jstack`, and `jmap` are command-line tools that use the local attach API and need no open JMX port.",null]},{"id":"java/performance_and_tuning#052ae57c764e","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"README.md","difficulty":"core","question":"What is false sharing and how do you detect and fix it?","answerFull":"False sharing: two independently-written variables share a 64-byte CPU cache line. When Thread 1 writes variable A and Thread 2 writes variable B simultaneously, they both invalidate the entire shared cache line — each write forces the other core to reload 64 bytes from L3/memory. Effect: 10-100× slower than non-shared counters. Detect: async-profiler hardware counter mode shows high cache-miss rate on specific fields; or JMH with @Contended on/off. Fix: @jdk.internal.vm.annotation.Contended (enables padding to separate cache lines) or manual padding with unused long fields.","correct":"False sharing: two independently-written variables share a 64-byte CPU cache line.","distractors":["A JVM safepoint is a global stop where all threads pause at \"safe\" positions (method calls, backward branches in byte code) — required for GC, class redefinition, deoptimization.","ThreadMXBean.getThreadCpuTime(threadId) returns the CPU time consumed by a specific thread in nanoseconds, letting you find a thread burning a core.","JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while jcmd, jstack, and jmap are command-line tools that use the local attach API and need no open JMX port."],"distractorIds":["java/performance_and_tuning#e6d95560c042","java/performance_and_tuning#4b61b3705503","java/performance_and_tuning#5c704f3e202b"],"concepts":["fix","detect","variables","sharing","line","share"],"answerFullMd":"False sharing: two independently-written variables share a 64-byte CPU cache line. When Thread 1 writes variable A and Thread 2 writes variable B simultaneously, they both invalidate the entire shared cache line — each write forces the other core to reload 64 bytes from L3/memory. Effect: 10-100× slower than non-shared counters. Detect: async-profiler hardware counter mode shows high cache-miss rate on specific fields; or JMH with `@Contended` on/off. Fix: `@jdk.internal.vm.annotation.Contended` (enables padding to separate cache lines) or manual padding with unused `long` fields.","distractorsMd":[null,"`ThreadMXBean.getThreadCpuTime(threadId)` returns the CPU time consumed by a specific thread in nanoseconds, letting you find a thread burning a core.","JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while `jcmd`, `jstack`, and `jmap` are command-line tools that use the local attach API and need no open JMX port."]},{"id":"java/performance_and_tuning#72bd22ce7821","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"README.md","difficulty":"core","question":"How would you diagnose a memory leak in production?","answerFull":"(1) Observe: heap grows steadily on dashboards (JVM metrics); major GC frequency increases; OOM eventually. (2) Capture heap dump before OOM: configure -XX:+HeapDumpOnOutOfMemoryError or trigger with jmap -dump:live,format=b,file=heap.hprof <pid>. (3) Open in Eclipse MAT: run Leak Suspects Report; examine dominator tree for objects with unexpected retained heap. (4) Find GC root path: \"Path to GC Roots\" for the suspect object — reveals which field chains keep it alive. (5) Fix the holding reference (remove from static collection, call threadLocal.remove(), cancel event listener).","correct":"(1) Observe: heap grows steadily on dashboards (JVM metrics); major GC frequency increases; OOM eventually.","distractors":["A JVM safepoint is a global stop where all threads pause at \"safe\" positions (method calls, backward branches in byte code) — required for GC, class redefinition, deoptimization.","JMX exposes current gauge values like heap-used-now, while JFR records a time-ordered stream of events such as allocations and GC pauses at about 1% overhead.","Unauthenticated remote JMX is a remote-code-execution vector, not just an information leak."],"distractorIds":["java/performance_and_tuning#e6d95560c042","java/performance_and_tuning#5664500cbd45","java/performance_and_tuning#5cdc58a7d1c1"],"concepts":["diagnose","steadily","dashboards","leak","observe","oom"],"answerFullMd":"(1) Observe: heap grows steadily on dashboards (JVM metrics); major GC frequency increases; OOM eventually. (2) Capture heap dump before OOM: configure `-XX:+HeapDumpOnOutOfMemoryError` or trigger with `jmap -dump:live,format=b,file=heap.hprof <pid>`. (3) Open in Eclipse MAT: run Leak Suspects Report; examine dominator tree for objects with unexpected retained heap. (4) Find GC root path: \"Path to GC Roots\" for the suspect object — reveals which field chains keep it alive. (5) Fix the holding reference (remove from static collection, call `threadLocal.remove()`, cancel event listener)."},{"id":"java/performance_and_tuning#5ed530ee640a","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"README.md","difficulty":"core","question":"Why is String concatenation in a loop slow and what is the exact bytecode difference?","answerFull":"result += s inside a loop compiles to: result = new StringBuilder(result).append(s).toString(). Each iteration creates a StringBuilder, copies the existing string into it, appends the new string, and calls toString() which creates a new String. Total for n iterations: n StringBuilder objects + n String objects, each copying the accumulated string — O(n²) total bytes copied. The fix: one StringBuilder outside the loop, append() inside, toString() once — O(n) total. Java 9+ with StringConcatFactory optimizes simple non-loop cases but loop += still has this cost.","correct":"result += s inside a loop compiles to: result = new StringBuilder(result).append(s).toString().","distractors":["Before Java 9, every String was backed by a char[] (UTF-16: 2 bytes per character).","(1) Observe: heap grows steadily on dashboards (JVM metrics); major GC frequency increases; OOM eventually.","(1) Small collections: the ForkJoinPool overhead (task splitting, merging, thread scheduling) exceeds the computation time."],"distractorIds":["java/performance_and_tuning#8b3381200a92","java/performance_and_tuning#72bd22ce7821","java/performance_and_tuning#e96671111516"],"concepts":["concatenation","stringbuilder","tostring","difference","compiles","append"],"correctMd":"`result += s` inside a loop compiles to: `result = new StringBuilder(result).append(s).toString()`.","answerFullMd":"`result += s` inside a loop compiles to: `result = new StringBuilder(result).append(s).toString()`. Each iteration creates a `StringBuilder`, copies the existing string into it, appends the new string, and calls `toString()` which creates a new `String`. Total for n iterations: n `StringBuilder` objects + n `String` objects, each copying the accumulated string — O(n²) total bytes copied. The fix: one `StringBuilder` outside the loop, `append()` inside, `toString()` once — O(n) total. Java 9+ with `StringConcatFactory` optimizes simple non-loop cases but loop `+=` still has this cost.","distractorsMd":["Before Java 9, every `String` was backed by a `char[]` (UTF-16: 2 bytes per character).",null,null]},{"id":"java/performance_and_tuning#8b3381200a92","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"README.md","difficulty":"intermediate","question":"How does compact strings (Java 9) save memory?","answerFull":"Before Java 9, every String was backed by a char[] (UTF-16: 2 bytes per character). Java 9 introduced compact strings: if all characters fit in LATIN-1 (ISO-8859-1, single-byte), the string is stored as byte[] (1 byte per char) with a coder field set to 0. For ASCII-heavy applications (typical server-side Java processing English text, JSON keys, URLs), this reduces String heap usage by ~50%. Controlled by -XX:+CompactStrings (on by default). Mixed-language strings that need UTF-16 fall back to 2-byte storage automatically.","correct":"Before Java 9, every String was backed by a char[] (UTF-16: 2 bytes per character).","distractors":["Use the ManagementFactory factory methods, each returning a live bean that reflects current JVM state on every call.","A Standard MBean exposes an arbitrary Java interface, while an MXBean converts everything to self-describing Open Types like CompositeData, so remote clients need no custom classes.","JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while jcmd, jstack, and jmap are command-line tools that use the local attach API and need no open JMX port."],"distractorIds":["java/performance_and_tuning#bff76891e497","java/performance_and_tuning#2e46794bbfe6","java/performance_and_tuning#5c704f3e202b"],"concepts":["utf","char","save","strings","character","compact"],"correctMd":"Before Java 9, every `String` was backed by a `char[]` (UTF-16: 2 bytes per character).","answerFullMd":"Before Java 9, every `String` was backed by a `char[]` (UTF-16: 2 bytes per character). Java 9 introduced compact strings: if all characters fit in LATIN-1 (ISO-8859-1, single-byte), the string is stored as `byte[]` (1 byte per char) with a `coder` field set to 0. For ASCII-heavy applications (typical server-side Java processing English text, JSON keys, URLs), this reduces String heap usage by ~50%. Controlled by `-XX:+CompactStrings` (on by default). Mixed-language strings that need UTF-16 fall back to 2-byte storage automatically.","distractorsMd":["Use the `ManagementFactory` factory methods, each returning a live bean that reflects current JVM state on every call.",null,"JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while `jcmd`, `jstack`, and `jmap` are command-line tools that use the local attach API and need no open JMX port."]},{"id":"java/performance_and_tuning#e96671111516","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"README.md","difficulty":"intermediate","question":"When is a parallel stream slower than sequential?","answerFull":"(1) Small collections: the ForkJoinPool overhead (task splitting, merging, thread scheduling) exceeds the computation time. Rule of thumb: < 10,000 elements often not worth it for cheap operations. (2) I/O-bound operations: blocking threads in ForkJoinPool.commonPool() — pool starved, sequential would be faster. (3) Non-splittable sources: LinkedList, custom iterables that can't split — parallel degrades to sequential with extra overhead. (4) Stateful/ordered operations: distinct(), sorted() need synchronization in parallel — may be slower than sequential. (5) Computations where the combiner is expensive.","correct":"(1) Small collections: the ForkJoinPool overhead (task splitting, merging, thread scheduling) exceeds the computation time.","distractors":["Attribute getters run real code, so polling expensive ones every second — a getHitRatio() that takes a lock, or getThreadCpuTime across every thread — adds measurable load and can even contend with application work.","ThreadMXBean.getThreadCpuTime(threadId) returns the CPU time consumed by a specific thread in nanoseconds, letting you find a thread burning a core.","CPU-bound: # CPU cores + 1 — one extra thread covers rare scheduling gaps; more than this causes context switch overhead with no CPU-time benefit."],"distractorIds":["java/performance_and_tuning#4c58ce882d3f","java/performance_and_tuning#4b61b3705503","java/performance_and_tuning#8596d27fe55f"],"concepts":["slower","forkjoinpool","merging","scheduling","collections","splitting"],"answerFullMd":"(1) Small collections: the ForkJoinPool overhead (task splitting, merging, thread scheduling) exceeds the computation time. Rule of thumb: < 10,000 elements often not worth it for cheap operations. (2) I/O-bound operations: blocking threads in `ForkJoinPool.commonPool()` — pool starved, sequential would be faster. (3) Non-splittable sources: `LinkedList`, custom iterables that can't split — parallel degrades to sequential with extra overhead. (4) Stateful/ordered operations: `distinct()`, `sorted()` need synchronization in parallel — may be slower than sequential. (5) Computations where the combiner is expensive.","distractorsMd":["Attribute getters run real code, so polling expensive ones every second — a `getHitRatio()` that takes a lock, or `getThreadCpuTime` across every thread — adds measurable load and can even contend with application work.","`ThreadMXBean.getThreadCpuTime(threadId)` returns the CPU time consumed by a specific thread in nanoseconds, letting you find a thread burning a core.","CPU-bound: `# CPU cores + 1` — one extra thread covers rare scheduling gaps; more than this causes context switch overhead with no CPU-time benefit."]},{"id":"java/performance_and_tuning#99715df10366","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between retained heap and shallow heap in a heap dump?","answerFull":"Shallow heap: memory consumed by the object itself — its fields' total size (excluding referenced objects). ArrayList shallow heap = ~40 bytes (header + length + elementData reference), not counting the array. Retained heap: the total memory that would be freed if this object were GC'd — the object plus all objects exclusively reachable through it. ArrayList with 1000 String elements: retained heap = 40B (ArrayList) + array size + 1000 × String sizes. In Eclipse MAT, retained heap is the key metric for identifying leak culprits: high retained heap = deleting this object frees a lot.","correct":"Shallow heap: memory consumed by the object itself — its fields' total size (excluding referenced objects).","distractors":["JMX exposes current gauge values like heap-used-now, while JFR records a time-ordered stream of events such as allocations and GC pauses at about 1% overhead.","ArrayList stores elements in a contiguous Object[] array.","JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while jcmd, jstack, and jmap are command-line tools that use the local attach API and need no open JMX port."],"distractorIds":["java/performance_and_tuning#5664500cbd45","java/performance_and_tuning#25d65dc97332","java/performance_and_tuning#5c704f3e202b"],"concepts":["excluding","retained","shallow","referenced","difference","consumed"],"answerFullMd":"Shallow heap: memory consumed by the object itself — its fields' total size (excluding referenced objects). `ArrayList` shallow heap = ~40 bytes (header + length + elementData reference), not counting the array. Retained heap: the total memory that would be freed if this object were GC'd — the object plus all objects exclusively reachable through it. `ArrayList` with 1000 String elements: retained heap = 40B (ArrayList) + array size + 1000 × String sizes. In Eclipse MAT, retained heap is the key metric for identifying leak culprits: high retained heap = deleting this object frees a lot.","distractorsMd":[null,"`ArrayList` stores elements in a contiguous `Object[]` array.","JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while `jcmd`, `jstack`, and `jmap` are command-line tools that use the local attach API and need no open JMX port."]},{"id":"java/performance_and_tuning#25d65dc97332","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"README.md","difficulty":"advanced","question":"How does CPU cache architecture explain why ArrayList is faster than LinkedList for sequential iteration?","answerFull":"ArrayList stores elements in a contiguous Object[] array. Reading sequentially loads 64-byte cache lines, each containing ~16 object references (4B each with compressed oops). Subsequent reads hit L1 cache (~4 cycles). LinkedList stores each element in a Node object (object header + value + prev/next pointers = ~24 bytes + header). Each node.next pointer points to a heap-allocated Node at an arbitrary address — a different cache line in a different memory location. Accessing it requires: L1 miss → L2 miss → L3 miss → RAM (~200 cycles). For 1M elements: ArrayList traversal ≈ 4M cycles; LinkedList traversal ≈ 200M cycles (50x slower). This is why O(n) complexity is the same but real-world performance differs by orders of magnitude.","correct":"ArrayList stores elements in a contiguous Object[] array.","distractors":["JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while jcmd, jstack, and jmap are command-line tools that use the local attach API and need no open JMX port.","ThreadMXBean.getThreadCpuTime(threadId) returns the CPU time consumed by a specific thread in nanoseconds, letting you find a thread burning a core.","Shallow heap: memory consumed by the object itself — its fields' total size (excluding referenced objects)."],"distractorIds":["java/performance_and_tuning#5c704f3e202b","java/performance_and_tuning#4b61b3705503","java/performance_and_tuning#99715df10366"],"concepts":["linkedlist","arraylist","explain","contiguous","iteration","faster"],"questionMd":"How does CPU cache architecture explain why `ArrayList` is faster than `LinkedList` for sequential iteration?","correctMd":"`ArrayList` stores elements in a contiguous `Object[]` array.","answerFullMd":"`ArrayList` stores elements in a contiguous `Object[]` array. Reading sequentially loads 64-byte cache lines, each containing ~16 object references (4B each with compressed oops). Subsequent reads hit L1 cache (~4 cycles). `LinkedList` stores each element in a `Node` object (object header + value + prev/next pointers = ~24 bytes + header). Each `node.next` pointer points to a heap-allocated `Node` at an arbitrary address — a different cache line in a different memory location. Accessing it requires: L1 miss → L2 miss → L3 miss → RAM (~200 cycles). For 1M elements: `ArrayList` traversal ≈ 4M cycles; `LinkedList` traversal ≈ 200M cycles (50x slower). This is why O(n) complexity is the same but real-world performance differs by orders of magnitude.","distractorsMd":["JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while `jcmd`, `jstack`, and `jmap` are command-line tools that use the local attach API and need no open JMX port.","`ThreadMXBean.getThreadCpuTime(threadId)` returns the CPU time consumed by a specific thread in nanoseconds, letting you find a thread burning a core.",null]},{"id":"java/performance_and_tuning#93d0f273b75b","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"README.md","difficulty":"advanced","question":"What is the JIT inline threshold and how does a megamorphic call site prevent optimization?","answerFull":"The default JIT inline threshold is approximately 35 bytecodes (-XX:MaxInlineSize=35). Methods within this size are candidates for inlining — the JIT replaces the call instruction with the method body, enabling further optimizations like constant folding, escape analysis, and dead code elimination. A megamorphic call site has 3 or more concrete receiver types. The JIT cannot inline megamorphic virtual calls because it would need to enumerate all possible implementations (unlike monomorphic sites where one type is inlined, or bimorphic where two are if-else'd). Megamorphic sites fall back to vtable dispatch (~10-15% slower than inlined monomorphic). Diagnostic: -XX:+UnlockDiagnosticVMOptions -XX:+PrintInlining shows \"not inlineable\" for megamorphic sites. Fix: reduce type diversity at hot call sites, or use final methods where possible.","correct":"The default JIT inline threshold is approximately 35 bytecodes (-XX:MaxInlineSize=35).","distractors":["Use the ManagementFactory factory methods, each returning a live bean that reflects current JVM state on every call.","The RMI connector is JMX's default remote transport, addressed as service:jmx:rmi:///jndi/rmi://host:port/jmxrmi, and it is stateful and uses the two-port scheme that causes firewall issues.","Call ManagementFactory.getThreadMXBean().findDeadlockedThreads(), which returns the ids of threads in a deadlock cycle (or null if none)."],"distractorIds":["java/performance_and_tuning#bff76891e497","java/performance_and_tuning#c7caa80bf415","java/performance_and_tuning#f092c7ee2031"],"concepts":["bytecodes","maxinlinesize","site","jit","inline","prevent"],"correctMd":"The default JIT inline threshold is approximately 35 bytecodes (`-XX:MaxInlineSize=35`).","answerFullMd":"The default JIT inline threshold is approximately 35 bytecodes (`-XX:MaxInlineSize=35`). Methods within this size are candidates for inlining — the JIT replaces the call instruction with the method body, enabling further optimizations like constant folding, escape analysis, and dead code elimination. A megamorphic call site has 3 or more concrete receiver types. The JIT cannot inline megamorphic virtual calls because it would need to enumerate all possible implementations (unlike monomorphic sites where one type is inlined, or bimorphic where two are if-else'd). Megamorphic sites fall back to vtable dispatch (~10-15% slower than inlined monomorphic). Diagnostic: `-XX:+UnlockDiagnosticVMOptions -XX:+PrintInlining` shows \"not inlineable\" for megamorphic sites. Fix: reduce type diversity at hot call sites, or use `final` methods where possible.","distractorsMd":["Use the `ManagementFactory` factory methods, each returning a live bean that reflects current JVM state on every call.","The RMI connector is JMX's default remote transport, addressed as `service:jmx:rmi:///jndi/rmi://host:port/jmxrmi`, and it is stateful and uses the two-port scheme that causes firewall issues.","Call `ManagementFactory.getThreadMXBean().findDeadlockedThreads()`, which returns the ids of threads in a deadlock cycle (or null if none)."]},{"id":"java/performance_and_tuning#4ed2a07729ed","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"README.md","difficulty":"advanced","question":"What is false sharing and how does @Contended prevent it?","answerFull":"False sharing occurs when two threads write to distinct variables that reside on the same CPU cache line (~64 bytes on x86). Each write by one thread invalidates the other thread's cached copy, even though the data is logically independent — the CPU cannot invalidate at sub-cache-line granularity. Symptom: linear throughput degradation as core count increases for a supposedly independent concurrent write workload. Fix — pad fields so each occupies a full cache line: java // BROKEN: counter1 and counter2 share a cache line -> false sharing class Counters { volatile long counter1; volatile long counter2; } // FIXED: @Contended pads the field to a full cache line (requires -XX:-RestrictContended) class Counters { @jdk.internal.vm.annotation.Contended volatile long counter1; @jdk.internal.vm.annotation.Contended volatile long counter2; } // or: manual 7-long padding to reach 64 bytes class PaddedCounter { volatile long value; long p1,p2,p3,p4,p5,p6,p7; } LongAdder uses internal Cell padding to avoid false sharing between its striped accumulators. Detection tool: async-profiler -e cache-misses or perf stat -e cache-misses.","correct":"False sharing occurs when two threads write to distinct variables that reside on the same CPU cache line (~64 bytes on x86).","distractors":["A JVM safepoint is a global stop where all threads pause at \"safe\" positions (method calls, backward branches in byte code) — required for GC, class redefinition, deoptimization.","An ObjectName is an MBean's address in the format domain:key1=value1,key2=value2, such as java.lang:type=Memory or com.example:type=Cache,name=userCache.","CPU-bound: # CPU cores + 1 — one extra thread covers rare scheduling gaps; more than this causes context switch overhead with no CPU-time benefit."],"distractorIds":["java/performance_and_tuning#e6d95560c042","java/performance_and_tuning#bbd7f137cd7d","java/performance_and_tuning#8596d27fe55f"],"concepts":["reside","x86","prevent","distinct","variables","sharing"],"questionMd":"What is false sharing and how does `@Contended` prevent it?","answerFullMd":"False sharing occurs when two threads write to distinct variables that reside on the same CPU cache line (~64 bytes on x86). Each write by one thread invalidates the other thread's cached copy, even though the data is logically independent — the CPU cannot invalidate at sub-cache-line granularity. Symptom: linear throughput degradation as core count increases for a supposedly independent concurrent write workload. Fix — pad fields so each occupies a full cache line: ```java // BROKEN: counter1 and counter2 share a cache line -> false sharing class Counters { volatile long counter1; volatile long counter2; } // FIXED: @Contended pads the field to a full cache line (requires -XX:-RestrictContended) class Counters { @jdk.internal.vm.annotation.Contended volatile long counter1; @jdk.internal.vm.annotation.Contended volatile long counter2; } // or: manual 7-long padding to reach 64 bytes class PaddedCounter { volatile long value; long p1,p2,p3,p4,p5,p6,p7; } ``` `LongAdder` uses internal `Cell` padding to avoid false sharing between its striped accumulators. Detection tool: `async-profiler -e cache-misses` or `perf stat -e cache-misses`.","distractorsMd":[null,"An ObjectName is an MBean's address in the format `domain:key1=value1,key2=value2`, such as `java.lang:type=Memory` or `com.example:type=Cache,name=userCache`.","CPU-bound: `# CPU cores + 1` — one extra thread covers rare scheduling gaps; more than this causes context switch overhead with no CPU-time benefit."]},{"id":"java/performance_and_tuning#e6d95560c042","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"README.md","difficulty":"advanced","question":"What is the \"time to safepoint\" (TTSP) problem and how does a long-running counted loop trigger it?","answerFull":"A JVM safepoint is a global stop where all threads pause at \"safe\" positions (method calls, backward branches in byte code) — required for GC, class redefinition, deoptimization. The \"time to safepoint\" is the interval from when the JVM requests a safepoint to when all threads actually stop. A thread inside a counted integer loop (for (int i = 0; i < N; i++)) only reaches a safepoint at the loop's exit — not at each iteration. If N is large (e.g., 10M iterations of a tight loop), that thread blocks the global safepoint for the duration of the loop, halting all other threads — GC pauses, thread-dump requests, and profiling samples all wait. Diagnose with -XX:+PrintSafepointStatistics -XX:PrintSafepointStatisticsCount=1; look for high \"spin\" values. Fix: use LongStream (checks safepoints more frequently than int loops), or split the loop into bounded chunks.","correct":"A JVM safepoint is a global stop where all threads pause at \"safe\" positions (method calls, backward branches in byte code) — required for GC, class redefinition, deoptimization.","distractors":["JMX is a stateful pull-over-RMI protocol with per-client TCP connections, an ephemeral-port firewall problem, an RCE-prone connector, and no dimensional labels — a poor fit for scraping thousands of time series.","The MBeanServer is the in-JVM registry — essentially a Map<ObjectName, MBean> — that management tools query by ObjectName rather than by object reference.","Use the ManagementFactory factory methods, each returning a live bean that reflects current JVM state on every call."],"distractorIds":["java/performance_and_tuning#218bd0ca3e84","java/performance_and_tuning#dc87ab70b466","java/performance_and_tuning#bff76891e497"],"concepts":["counted","redefinition","safepoint","deoptimization","pause","branches"],"answerFullMd":"A JVM safepoint is a global stop where all threads pause at \"safe\" positions (method calls, backward branches in byte code) — required for GC, class redefinition, deoptimization. The \"time to safepoint\" is the interval from when the JVM requests a safepoint to when all threads actually stop. A thread inside a counted integer loop (`for (int i = 0; i < N; i++)`) only reaches a safepoint at the loop's exit — not at each iteration. If `N` is large (e.g., 10M iterations of a tight loop), that thread blocks the global safepoint for the duration of the loop, halting all other threads — GC pauses, thread-dump requests, and profiling samples all wait. Diagnose with `-XX:+PrintSafepointStatistics -XX:PrintSafepointStatisticsCount=1`; look for high \"spin\" values. Fix: use `LongStream` (checks safepoints more frequently than `int` loops), or split the loop into bounded chunks.","distractorsMd":[null,"The MBeanServer is the in-JVM registry — essentially a `Map<ObjectName, MBean>` — that management tools query by `ObjectName` rather than by object reference.","Use the `ManagementFactory` factory methods, each returning a live bean that reflects current JVM state on every call."]},{"id":"java/performance_and_tuning#5cdc58a7d1c1","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"jmx_and_management.md","difficulty":"core","question":"What makes exposing remote JMX without authentication dangerous?","answerFull":"Unauthenticated remote JMX is a remote-code-execution vector, not just an information leak. An attacker who reaches the port can register an MLet MBean, point it at a remote MBean, and load arbitrary code into the JVM. That is why authenticate=false ssl=false on a routable interface is one of the most-exploited Java misconfigurations. The fix is to bind localhost, require password auth plus TLS, or reach it only through an SSH tunnel.","correct":"Unauthenticated remote JMX is a remote-code-execution vector, not just an information leak.","distractors":["Bind the connector to localhost (com.sun.management.jmxremote.host=127.0.0.1), require authentication with a jmxremote.password/jmxremote.access file pair, and enable TLS (ssl=true, ideally ssl.need.client.auth=true).","Attribute getters run real code, so polling expensive ones every second — a getHitRatio() that takes a lock, or getThreadCpuTime across every thread — adds measurable load and can even contend with application work.","JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while jcmd, jstack, and jmap are command-line tools that use the local attach API and need no open JMX port."],"distractorIds":["java/performance_and_tuning#2ccaf0b67212","java/performance_and_tuning#4c58ce882d3f","java/performance_and_tuning#5c704f3e202b"],"concepts":["dangerous","exposing","leak","unauthenticated","jmx","remote"],"answerFullMd":"Unauthenticated remote JMX is a remote-code-execution vector, not just an information leak. An attacker who reaches the port can register an MLet MBean, point it at a remote MBean, and load arbitrary code into the JVM. That is why `authenticate=false ssl=false` on a routable interface is one of the most-exploited Java misconfigurations. The fix is to bind localhost, require password auth plus TLS, or reach it only through an SSH tunnel.","distractorsMd":["Bind the connector to localhost (`com.sun.management.jmxremote.host=127.0.0.1`), require authentication with a `jmxremote.password`/`jmxremote.access` file pair, and enable TLS (`ssl=true`, ideally `ssl.need.client.auth=true`).","Attribute getters run real code, so polling expensive ones every second — a `getHitRatio()` that takes a lock, or `getThreadCpuTime` across every thread — adds measurable load and can even contend with application work.","JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while `jcmd`, `jstack`, and `jmap` are command-line tools that use the local attach API and need no open JMX port."]},{"id":"java/performance_and_tuning#082149fcbce6","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"jmx_and_management.md","difficulty":"core","question":"Why does remote JMX often fail through a firewall even when the configured port is open?","answerFull":"The RMI connector uses two ports — the registry port you configure and a separate data-channel export port that defaults to a random ephemeral port. Firewalls and container network policies open only the known port, so the handshake succeeds but the actual JMX connection times out, producing the classic \"JConsole connects then hangs.\" Set com.sun.management.jmxremote.rmi.port equal to the registry port and set java.rmi.server.hostname so both channels are known and reachable.","correct":"The RMI connector uses two ports — the registry port you configure and a separate data-channel export port that defaults to a random ephemeral port.","distractors":["A Standard MBean exposes an arbitrary Java interface, while an MXBean converts everything to self-describing Open Types like CompositeData, so remote clients need no custom classes.","Unauthenticated remote JMX is a remote-code-execution vector, not just an information leak.","JMX exposes current gauge values like heap-used-now, while JFR records a time-ordered stream of events such as allocations and GC pauses at about 1% overhead."],"distractorIds":["java/performance_and_tuning#2e46794bbfe6","java/performance_and_tuning#5cdc58a7d1c1","java/performance_and_tuning#5664500cbd45"],"concepts":["firewall","rmi","export","ports","connector","jmx"],"answerFullMd":"The RMI connector uses two ports — the registry port you configure and a separate data-channel export port that defaults to a random ephemeral port. Firewalls and container network policies open only the known port, so the handshake succeeds but the actual JMX connection times out, producing the classic \"JConsole connects then hangs.\" Set `com.sun.management.jmxremote.rmi.port` equal to the registry port and set `java.rmi.server.hostname` so both channels are known and reachable."},{"id":"java/performance_and_tuning#2e46794bbfe6","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"jmx_and_management.md","difficulty":"core","question":"What is the difference between a Standard MBean and an MXBean?","answerFull":"A Standard MBean exposes an arbitrary Java interface, while an MXBean converts everything to self-describing Open Types like CompositeData, so remote clients need no custom classes. A Standard MBean's attribute values may require your own classes on the client classpath to deserialize; an MXBean's CompositeData/TabularData cross the wire fully self-described. That is why every platform bean is an MXBean — JConsole renders HeapMemoryUsage without any JVM-internal class. Both are defined by a naming convention (XMBean vs XMXBean), but MXBean is the safer, preferred flavor.","correct":"A Standard MBean exposes an arbitrary Java interface, while an MXBean converts everything to self-describing Open Types like CompositeData, so remote clients need no custom classes.","distractors":["The RMI connector is JMX's default remote transport, addressed as service:jmx:rmi:///jndi/rmi://host:port/jmxrmi, and it is stateful and uses the two-port scheme that causes firewall issues.","Unauthenticated remote JMX is a remote-code-execution vector, not just an information leak.","The MBeanServer is the in-JVM registry — essentially a Map<ObjectName, MBean> — that management tools query by ObjectName rather than by object reference."],"distractorIds":["java/performance_and_tuning#c7caa80bf415","java/performance_and_tuning#5cdc58a7d1c1","java/performance_and_tuning#dc87ab70b466"],"concepts":["compositedata","mbean","mxbean","describing","converts","difference"],"answerFullMd":"A Standard MBean exposes an arbitrary Java interface, while an MXBean converts everything to self-describing Open Types like CompositeData, so remote clients need no custom classes. A Standard MBean's attribute values may require your own classes on the client classpath to deserialize; an MXBean's `CompositeData`/`TabularData` cross the wire fully self-described. That is why every platform bean is an MXBean — JConsole renders `HeapMemoryUsage` without any JVM-internal class. Both are defined by a naming convention (`XMBean` vs `XMXBean`), but MXBean is the safer, preferred flavor.","distractorsMd":["The RMI connector is JMX's default remote transport, addressed as `service:jmx:rmi:///jndi/rmi://host:port/jmxrmi`, and it is stateful and uses the two-port scheme that causes firewall issues.",null,"The MBeanServer is the in-JVM registry — essentially a `Map<ObjectName, MBean>` — that management tools query by `ObjectName` rather than by object reference."]},{"id":"java/performance_and_tuning#f092c7ee2031","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"jmx_and_management.md","difficulty":"core","question":"How do you detect a deadlock programmatically in Java?","answerFull":"Call ManagementFactory.getThreadMXBean().findDeadlockedThreads(), which returns the ids of threads in a deadlock cycle (or null if none). Pass those ids to getThreadInfo(ids, true, true) to get each thread's held locks and stack trace for logging or alerting. Unlike findMonitorDeadlockedThreads(), it detects cycles involving both synchronized monitors and java.util.concurrent Locks, so running it on a scheduled health check turns silent hangs into pages.","correct":"Call ManagementFactory.getThreadMXBean().findDeadlockedThreads(), which returns the ids of threads in a deadlock cycle (or null if none).","distractors":["An ObjectName is an MBean's address in the format domain:key1=value1,key2=value2, such as java.lang:type=Memory or com.example:type=Cache,name=userCache.","A Standard MBean exposes an arbitrary Java interface, while an MXBean converts everything to self-describing Open Types like CompositeData, so remote clients need no custom classes.","Before Java 9, every String was backed by a char[] (UTF-16: 2 bytes per character)."],"distractorIds":["java/performance_and_tuning#bbd7f137cd7d","java/performance_and_tuning#2e46794bbfe6","java/performance_and_tuning#8b3381200a92"],"concepts":["finddeadlockedthreads","getthreadmxbean","managementfactory","deadlock","detect","null"],"correctMd":"Call `ManagementFactory.getThreadMXBean().findDeadlockedThreads()`, which returns the ids of threads in a deadlock cycle (or null if none).","answerFullMd":"Call `ManagementFactory.getThreadMXBean().findDeadlockedThreads()`, which returns the ids of threads in a deadlock cycle (or null if none). Pass those ids to `getThreadInfo(ids, true, true)` to get each thread's held locks and stack trace for logging or alerting. Unlike `findMonitorDeadlockedThreads()`, it detects cycles involving both `synchronized` monitors and `java.util.concurrent` `Lock`s, so running it on a scheduled health check turns silent hangs into pages.","distractorsMd":["An ObjectName is an MBean's address in the format `domain:key1=value1,key2=value2`, such as `java.lang:type=Memory` or `com.example:type=Cache,name=userCache`.",null,"Before Java 9, every `String` was backed by a `char[]` (UTF-16: 2 bytes per character)."]},{"id":"java/performance_and_tuning#dc87ab70b466","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"jmx_and_management.md","difficulty":"core","question":"What is the MBeanServer and how do you obtain the platform one?","answerFull":"The MBeanServer is the in-JVM registry — essentially a Map<ObjectName, MBean> — that management tools query by ObjectName rather than by object reference. Get the singleton platform server with ManagementFactory.getPlatformMBeanServer(); it already holds all platform MXBeans (Memory, Thread, GC, OS, Runtime). You register your own MBeans into it with registerMBean(bean, objectName). This indirection is what lets a tool introspect an application it was never compiled against.","correct":"The MBeanServer is the in-JVM registry — essentially a Map<ObjectName, MBean> — that management tools query by ObjectName rather than by object reference.","distractors":["A JVM safepoint is a global stop where all threads pause at \"safe\" positions (method calls, backward branches in byte code) — required for GC, class redefinition, deoptimization.","Bind the connector to localhost (com.sun.management.jmxremote.host=127.0.0.1), require authentication with a jmxremote.password/jmxremote.access file pair, and enable TLS (ssl=true, ideally ssl.need.client.auth=true).","A Standard MBean exposes an arbitrary Java interface, while an MXBean converts everything to self-describing Open Types like CompositeData, so remote clients need no custom classes."],"distractorIds":["java/performance_and_tuning#e6d95560c042","java/performance_and_tuning#2ccaf0b67212","java/performance_and_tuning#2e46794bbfe6"],"concepts":["mbean","mbeanserver","objectname","obtain","essentially","management"],"correctMd":"The MBeanServer is the in-JVM registry — essentially a `Map<ObjectName, MBean>` — that management tools query by `ObjectName` rather than by object reference.","answerFullMd":"The MBeanServer is the in-JVM registry — essentially a `Map<ObjectName, MBean>` — that management tools query by `ObjectName` rather than by object reference. Get the singleton platform server with `ManagementFactory.getPlatformMBeanServer()`; it already holds all platform MXBeans (Memory, Thread, GC, OS, Runtime). You register your own MBeans into it with `registerMBean(bean, objectName)`. This indirection is what lets a tool introspect an application it was never compiled against.","distractorsMd":[null,"Bind the connector to localhost (`com.sun.management.jmxremote.host=127.0.0.1`), require authentication with a `jmxremote.password`/`jmxremote.access` file pair, and enable TLS (`ssl=true`, ideally `ssl.need.client.auth=true`).",null]},{"id":"java/performance_and_tuning#bff76891e497","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"jmx_and_management.md","difficulty":"intermediate","question":"How do you access the built-in platform MXBeans, and what does each give you?","answerFull":"Use the ManagementFactory factory methods, each returning a live bean that reflects current JVM state on every call. getMemoryMXBean() gives heap/non-heap usage, getThreadMXBean() thread counts and deadlocks, getGarbageCollectorMXBeans() per-collector counts and times, getOperatingSystemMXBean() CPU and cores, and getRuntimeMXBean() uptime and launch flags. RuntimeMXBean.getInputArguments() in particular is invaluable for confirming which flags a running process actually started with.","correct":"Use the ManagementFactory factory methods, each returning a live bean that reflects current JVM state on every call.","distractors":["(1) Observe: heap grows steadily on dashboards (JVM metrics); major GC frequency increases; OOM eventually.","Define an interface whose name ends in MXBean, implement it, then register the instance under a unique ObjectName in the platform MBeanServer.","The MBeanServer is the in-JVM registry — essentially a Map<ObjectName, MBean> — that management tools query by ObjectName rather than by object reference."],"distractorIds":["java/performance_and_tuning#72bd22ce7821","java/performance_and_tuning#a2dfa9363ead","java/performance_and_tuning#dc87ab70b466"],"concepts":["mxbeans","managementfactory","reflects","factory","give","live"],"correctMd":"Use the `ManagementFactory` factory methods, each returning a live bean that reflects current JVM state on every call.","answerFullMd":"Use the `ManagementFactory` factory methods, each returning a live bean that reflects current JVM state on every call. `getMemoryMXBean()` gives heap/non-heap usage, `getThreadMXBean()` thread counts and deadlocks, `getGarbageCollectorMXBeans()` per-collector counts and times, `getOperatingSystemMXBean()` CPU and cores, and `getRuntimeMXBean()` uptime and launch flags. `RuntimeMXBean.getInputArguments()` in particular is invaluable for confirming which flags a running process actually started with.","distractorsMd":[null,"Define an interface whose name ends in `MXBean`, implement it, then register the instance under a unique `ObjectName` in the platform MBeanServer.","The MBeanServer is the in-JVM registry — essentially a `Map<ObjectName, MBean>` — that management tools query by `ObjectName` rather than by object reference."]},{"id":"java/performance_and_tuning#a2dfa9363ead","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"jmx_and_management.md","difficulty":"intermediate","question":"How do you write and register a custom MXBean?","answerFull":"Define an interface whose name ends in MXBean, implement it, then register the instance under a unique ObjectName in the platform MBeanServer. For example server.registerMBean(new CacheStats(), new ObjectName(\"com.example:type=CacheStats,name=userCache\")). Getters become read-only attributes, setters become writable attributes, and remaining public methods (like clear()) become invokable operations that tools such as JConsole can call.","correct":"Define an interface whose name ends in MXBean, implement it, then register the instance under a unique ObjectName in the platform MBeanServer.","distractors":["An ObjectName is an MBean's address in the format domain:key1=value1,key2=value2, such as java.lang:type=Memory or com.example:type=Cache,name=userCache.","A Standard MBean exposes an arbitrary Java interface, while an MXBean converts everything to self-describing Open Types like CompositeData, so remote clients need no custom classes.","The flavors are Standard, Dynamic, Open, MXBean, and Model, and you should almost always write MXBeans."],"distractorIds":["java/performance_and_tuning#bbd7f137cd7d","java/performance_and_tuning#2e46794bbfe6","java/performance_and_tuning#f5368f57f961"],"concepts":["mbeanserver","mxbean","objectname","ends","register","define"],"correctMd":"Define an interface whose name ends in `MXBean`, implement it, then register the instance under a unique `ObjectName` in the platform MBeanServer.","answerFullMd":"Define an interface whose name ends in `MXBean`, implement it, then register the instance under a unique `ObjectName` in the platform MBeanServer. For example `server.registerMBean(new CacheStats(), new ObjectName(\"com.example:type=CacheStats,name=userCache\"))`. Getters become read-only attributes, setters become writable attributes, and remaining public methods (like `clear()`) become invokable operations that tools such as JConsole can call.","distractorsMd":["An ObjectName is an MBean's address in the format `domain:key1=value1,key2=value2`, such as `java.lang:type=Memory` or `com.example:type=Cache,name=userCache`.",null,null]},{"id":"java/performance_and_tuning#bbd7f137cd7d","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"jmx_and_management.md","difficulty":"intermediate","question":"What is an ObjectName and why does its format matter?","answerFull":"An ObjectName is an MBean's address in the format domain:key1=value1,key2=value2, such as java.lang:type=Memory or com.example:type=Cache,name=userCache. Tools browse, filter, and query MBeans by this name, and it must be unique — duplicate names throw InstanceAlreadyExistsException and malformed ones throw MalformedObjectNameException. Including a distinguishing key like name= lets you register many instances of the same type without collision.","correct":"An ObjectName is an MBean's address in the format domain:key1=value1,key2=value2, such as java.lang:type=Memory or com.example:type=Cache,name=userCache.","distractors":["Before Java 9, every String was backed by a char[] (UTF-16: 2 bytes per character).","Bind the connector to localhost (com.sun.management.jmxremote.host=127.0.0.1), require authentication with a jmxremote.password/jmxremote.access file pair, and enable TLS (ssl=true, ideally ssl.need.client.auth=true).","Define an interface whose name ends in MXBean, implement it, then register the instance under a unique ObjectName in the platform MBeanServer."],"distractorIds":["java/performance_and_tuning#8b3381200a92","java/performance_and_tuning#2ccaf0b67212","java/performance_and_tuning#a2dfa9363ead"],"concepts":["key1","key2","usercache","value1","value2","mbean"],"correctMd":"An ObjectName is an MBean's address in the format `domain:key1=value1,key2=value2`, such as `java.lang:type=Memory` or `com.example:type=Cache,name=userCache`.","answerFullMd":"An ObjectName is an MBean's address in the format `domain:key1=value1,key2=value2`, such as `java.lang:type=Memory` or `com.example:type=Cache,name=userCache`. Tools browse, filter, and query MBeans by this name, and it must be unique — duplicate names throw `InstanceAlreadyExistsException` and malformed ones throw `MalformedObjectNameException`. Including a distinguishing key like `name=` lets you register many instances of the same type without collision.","distractorsMd":["Before Java 9, every `String` was backed by a `char[]` (UTF-16: 2 bytes per character).","Bind the connector to localhost (`com.sun.management.jmxremote.host=127.0.0.1`), require authentication with a `jmxremote.password`/`jmxremote.access` file pair, and enable TLS (`ssl=true`, ideally `ssl.need.client.auth=true`).","Define an interface whose name ends in `MXBean`, implement it, then register the instance under a unique `ObjectName` in the platform MBeanServer."]},{"id":"java/performance_and_tuning#5c704f3e202b","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"jmx_and_management.md","difficulty":"intermediate","question":"What is the difference between JConsole, VisualVM, jcmd, jstack, and jmap?","answerFull":"JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while jcmd, jstack, and jmap are command-line tools that use the local attach API and need no open JMX port. jstack prints thread dumps, jmap produces heap histograms and dumps, and jcmd is the modern umbrella that subsumes both (Thread.print, GC.heap_dump) and also drives JFR. For a JVM you can ssh into, the jcmd family is the safe, port-free way to inspect it.","correct":"JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while jcmd, jstack, and jmap are command-line tools that use the local attach API and need no open JMX port.","distractors":["Bind the connector to localhost (com.sun.management.jmxremote.host=127.0.0.1), require authentication with a jmxremote.password/jmxremote.access file pair, and enable TLS (ssl=true, ideally ssl.need.client.auth=true).","Unauthenticated remote JMX is a remote-code-execution vector, not just an information leak.","The RMI connector uses two ports — the registry port you configure and a separate data-channel export port that defaults to a random ephemeral port."],"distractorIds":["java/performance_and_tuning#2ccaf0b67212","java/performance_and_tuning#5cdc58a7d1c1","java/performance_and_tuning#082149fcbce6"],"concepts":["jconsole","jstack","visualvm","gui","jcmd","jmap"],"correctMd":"JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while `jcmd`, `jstack`, and `jmap` are command-line tools that use the local attach API and need no open JMX port.","answerFullMd":"JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while `jcmd`, `jstack`, and `jmap` are command-line tools that use the local attach API and need no open JMX port. `jstack` prints thread dumps, `jmap` produces heap histograms and dumps, and `jcmd` is the modern umbrella that subsumes both (`Thread.print`, `GC.heap_dump`) and also drives JFR. For a JVM you can `ssh` into, the `jcmd` family is the safe, port-free way to inspect it.","distractorsMd":["Bind the connector to localhost (`com.sun.management.jmxremote.host=127.0.0.1`), require authentication with a `jmxremote.password`/`jmxremote.access` file pair, and enable TLS (`ssl=true`, ideally `ssl.need.client.auth=true`).",null,null]},{"id":"java/performance_and_tuning#218bd0ca3e84","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"jmx_and_management.md","difficulty":"intermediate","question":"Why do modern stacks bridge JMX to Prometheus instead of scraping JMX directly?","answerFull":"JMX is a stateful pull-over-RMI protocol with per-client TCP connections, an ephemeral-port firewall problem, an RCE-prone connector, and no dimensional labels — a poor fit for scraping thousands of time series. jmx_exporter (or Micrometer) reads the MBeans and re-exposes them at an HTTP /metrics endpoint in Prometheus format, which is stateless, single-port, and label-rich. The pattern keeps JMX for interactive operations while giving fleet monitoring a clean HTTP scrape target.","correct":"JMX is a stateful pull-over-RMI protocol with per-client TCP connections, an ephemeral-port firewall problem, an RCE-prone connector, and no dimensional labels — a poor fit for scraping thousands of time series.","distractors":["JMX exposes current gauge values like heap-used-now, while JFR records a time-ordered stream of events such as allocations and GC pauses at about 1% overhead.","(1) Small collections: the ForkJoinPool overhead (task splitting, merging, thread scheduling) exceeds the computation time.","Bind the connector to localhost (com.sun.management.jmxremote.host=127.0.0.1), require authentication with a jmxremote.password/jmxremote.access file pair, and enable TLS (ssl=true, ideally ssl.need.client.auth=true)."],"distractorIds":["java/performance_and_tuning#5664500cbd45","java/performance_and_tuning#e96671111516","java/performance_and_tuning#2ccaf0b67212"],"concepts":["rce","scraping","firewall","prone","rmi","bridge"],"answerFullMd":"JMX is a stateful pull-over-RMI protocol with per-client TCP connections, an ephemeral-port firewall problem, an RCE-prone connector, and no dimensional labels — a poor fit for scraping thousands of time series. `jmx_exporter` (or Micrometer) reads the MBeans and re-exposes them at an HTTP `/metrics` endpoint in Prometheus format, which is stateless, single-port, and label-rich. The pattern keeps JMX for interactive operations while giving fleet monitoring a clean HTTP scrape target.","distractorsMd":[null,null,"Bind the connector to localhost (`com.sun.management.jmxremote.host=127.0.0.1`), require authentication with a `jmxremote.password`/`jmxremote.access` file pair, and enable TLS (`ssl=true`, ideally `ssl.need.client.auth=true`)."]},{"id":"java/performance_and_tuning#5664500cbd45","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"jmx_and_management.md","difficulty":"advanced","question":"How does JMX relate to JFR (Java Flight Recorder)?","answerFull":"JMX exposes current gauge values like heap-used-now, while JFR records a time-ordered stream of events such as allocations and GC pauses at about 1% overhead. JMX is controlled through the MBeanServer; JFR is driven via jcmd JFR.start/dump and the FlightRecorderMXBean, then analyzed in JDK Mission Control. Use JMX for live dashboards and alerting, and JFR for post-incident forensics; they are complementary and both reachable through the same management plane.","correct":"JMX exposes current gauge values like heap-used-now, while JFR records a time-ordered stream of events such as allocations and GC pauses at about 1% overhead.","distractors":["The RMI connector is JMX's default remote transport, addressed as service:jmx:rmi:///jndi/rmi://host:port/jmxrmi, and it is stateful and uses the two-port scheme that causes firewall issues.","(1) Small collections: the ForkJoinPool overhead (task splitting, merging, thread scheduling) exceeds the computation time.","JMX is a stateful pull-over-RMI protocol with per-client TCP connections, an ephemeral-port firewall problem, an RCE-prone connector, and no dimensional labels — a poor fit for scraping thousands of time series."],"distractorIds":["java/performance_and_tuning#c7caa80bf415","java/performance_and_tuning#e96671111516","java/performance_and_tuning#218bd0ca3e84"],"concepts":["recorder","jfr","gauge","allocations","jmx","pauses"],"answerFullMd":"JMX exposes current gauge values like heap-used-now, while JFR records a time-ordered stream of events such as allocations and GC pauses at about 1% overhead. JMX is controlled through the MBeanServer; JFR is driven via `jcmd JFR.start/dump` and the `FlightRecorderMXBean`, then analyzed in JDK Mission Control. Use JMX for live dashboards and alerting, and JFR for post-incident forensics; they are complementary and both reachable through the same management plane.","distractorsMd":["The RMI connector is JMX's default remote transport, addressed as `service:jmx:rmi:///jndi/rmi://host:port/jmxrmi`, and it is stateful and uses the two-port scheme that causes firewall issues.",null,null]},{"id":"java/performance_and_tuning#f5368f57f961","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"jmx_and_management.md","difficulty":"advanced","question":"What are the MBean flavors and which should you use?","answerFull":"The flavors are Standard, Dynamic, Open, MXBean, and Model, and you should almost always write MXBeans. Standard is an interface plus impl with inferred attributes; Dynamic implements DynamicMBean and exposes MBeanInfo at runtime; Open restricts a Dynamic MBean to Open Types; MXBean is Standard-style but auto-converted to Open Types; Model is a metadata-configured legacy bean. MXBeans combine the simplicity of declaring an interface with the wire-safety of Open Types, which is exactly why every platform bean is one. Dynamic/Open MBeans are for cases where the management interface is only known at runtime.","correct":"The flavors are Standard, Dynamic, Open, MXBean, and Model, and you should almost always write MXBeans.","distractors":["The MBeanServer is the in-JVM registry — essentially a Map<ObjectName, MBean> — that management tools query by ObjectName rather than by object reference.","A Standard MBean exposes an arbitrary Java interface, while an MXBean converts everything to self-describing Open Types like CompositeData, so remote clients need no custom classes.","An ObjectName is an MBean's address in the format domain:key1=value1,key2=value2, such as java.lang:type=Memory or com.example:type=Cache,name=userCache."],"distractorIds":["java/performance_and_tuning#dc87ab70b466","java/performance_and_tuning#2e46794bbfe6","java/performance_and_tuning#bbd7f137cd7d"],"concepts":["flavors","mxbeans","mbean","mxbean","almost","dynamic"],"answerFullMd":"The flavors are Standard, Dynamic, Open, MXBean, and Model, and you should almost always write MXBeans. Standard is an interface plus impl with inferred attributes; Dynamic implements `DynamicMBean` and exposes `MBeanInfo` at runtime; Open restricts a Dynamic MBean to Open Types; MXBean is Standard-style but auto-converted to Open Types; Model is a metadata-configured legacy bean. MXBeans combine the simplicity of declaring an interface with the wire-safety of Open Types, which is exactly why every platform bean is one. Dynamic/Open MBeans are for cases where the management interface is only known at runtime.","distractorsMd":["The MBeanServer is the in-JVM registry — essentially a `Map<ObjectName, MBean>` — that management tools query by `ObjectName` rather than by object reference.",null,"An ObjectName is an MBean's address in the format `domain:key1=value1,key2=value2`, such as `java.lang:type=Memory` or `com.example:type=Cache,name=userCache`."]},{"id":"java/performance_and_tuning#2ccaf0b67212","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"jmx_and_management.md","difficulty":"advanced","question":"How do you secure a remote JMX connection properly?","answerFull":"Bind the connector to localhost (com.sun.management.jmxremote.host=127.0.0.1), require authentication with a jmxremote.password/jmxremote.access file pair, and enable TLS (ssl=true, ideally ssl.need.client.auth=true). The password file must be chmod 600 or the JVM refuses to start, and roles in the access file are scoped readonly or readwrite. For access beyond the host, prefer an SSH tunnel (ssh -L 9010:localhost:9010) over opening the port to the network.","correct":"Bind the connector to localhost (com.sun.management.jmxremote.host=127.0.0.1), require authentication with a jmxremote.password/jmxremote.access file pair, and enable TLS (ssl=true, ideally ssl.need.client.auth=true).","distractors":["Unauthenticated remote JMX is a remote-code-execution vector, not just an information leak.","JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while jcmd, jstack, and jmap are command-line tools that use the local attach API and need no open JMX port.","A Standard MBean exposes an arbitrary Java interface, while an MXBean converts everything to self-describing Open Types like CompositeData, so remote clients need no custom classes."],"distractorIds":["java/performance_and_tuning#5cdc58a7d1c1","java/performance_and_tuning#5c704f3e202b","java/performance_and_tuning#2e46794bbfe6"],"concepts":["properly","ideally","jmxremote","sun","secure","127"],"correctMd":"Bind the connector to localhost (`com.sun.management.jmxremote.host=127.0.0.1`), require authentication with a `jmxremote.password`/`jmxremote.access` file pair, and enable TLS (`ssl=true`, ideally `ssl.need.client.auth=true`).","answerFullMd":"Bind the connector to localhost (`com.sun.management.jmxremote.host=127.0.0.1`), require authentication with a `jmxremote.password`/`jmxremote.access` file pair, and enable TLS (`ssl=true`, ideally `ssl.need.client.auth=true`). The password file must be `chmod 600` or the JVM refuses to start, and roles in the access file are scoped `readonly` or `readwrite`. For access beyond the host, prefer an SSH tunnel (`ssh -L 9010:localhost:9010`) over opening the port to the network.","distractorsMd":[null,"JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while `jcmd`, `jstack`, and `jmap` are command-line tools that use the local attach API and need no open JMX port.",null]},{"id":"java/performance_and_tuning#4b61b3705503","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"jmx_and_management.md","difficulty":"advanced","question":"What does getThreadCpuTime measure, and what is its caveat?","answerFull":"ThreadMXBean.getThreadCpuTime(threadId) returns the CPU time consumed by a specific thread in nanoseconds, letting you find a thread burning a core. The caveat is that it can be unsupported or disabled on some platforms, returning -1, so you must check isThreadCpuTimeSupported() and isThreadCpuTimeEnabled() first. Enabling per-thread CPU measurement also carries a small per-context-switch cost, so leave it off unless you need it.","correct":"ThreadMXBean.getThreadCpuTime(threadId) returns the CPU time consumed by a specific thread in nanoseconds, letting you find a thread burning a core.","distractors":["(1) Small collections: the ForkJoinPool overhead (task splitting, merging, thread scheduling) exceeds the computation time.","JMX exposes current gauge values like heap-used-now, while JFR records a time-ordered stream of events such as allocations and GC pauses at about 1% overhead.","Call ManagementFactory.getThreadMXBean().findDeadlockedThreads(), which returns the ids of threads in a deadlock cycle (or null if none)."],"distractorIds":["java/performance_and_tuning#e96671111516","java/performance_and_tuning#5664500cbd45","java/performance_and_tuning#f092c7ee2031"],"concepts":["burning","threadid","threadmxbean","getthreadcputime","nanoseconds","letting"],"questionMd":"What does `getThreadCpuTime` measure, and what is its caveat?","correctMd":"`ThreadMXBean.getThreadCpuTime(threadId)` returns the CPU time consumed by a specific thread in nanoseconds, letting you find a thread burning a core.","answerFullMd":"`ThreadMXBean.getThreadCpuTime(threadId)` returns the CPU time consumed by a specific thread in nanoseconds, letting you find a thread burning a core. The caveat is that it can be unsupported or disabled on some platforms, returning `-1`, so you must check `isThreadCpuTimeSupported()` and `isThreadCpuTimeEnabled()` first. Enabling per-thread CPU measurement also carries a small per-context-switch cost, so leave it off unless you need it.","distractorsMd":[null,null,"Call `ManagementFactory.getThreadMXBean().findDeadlockedThreads()`, which returns the ids of threads in a deadlock cycle (or null if none)."]},{"id":"java/performance_and_tuning#eb57255d3ef5","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"jmx_and_management.md","difficulty":"advanced","question":"Can you monitor and inspect a JVM without opening any remote JMX port?","answerFull":"Yes — local tools (jcmd, jstack, jmap, and same-host VisualVM) use the attach API over a local socket, so they read the MBeanServer with no jmxremote flags and no network listener. This is both the safest and the most common way to inspect a JVM in production: jcmd <pid> Thread.print for a thread dump, jcmd <pid> GC.heap_info for heap state, jcmd <pid> JFR.start to begin a recording. Opening a remote JMX port is only necessary for interactive GUI tools connecting from another host.","correct":"Yes — local tools (jcmd, jstack, jmap, and same-host VisualVM) use the attach API over a local socket, so they read the MBeanServer with no jmxremote flags and no network listener.","distractors":["The MBeanServer is the in-JVM registry — essentially a Map<ObjectName, MBean> — that management tools query by ObjectName rather than by object reference.","JMX exposes current gauge values like heap-used-now, while JFR records a time-ordered stream of events such as allocations and GC pauses at about 1% overhead.","Unauthenticated remote JMX is a remote-code-execution vector, not just an information leak."],"distractorIds":["java/performance_and_tuning#dc87ab70b466","java/performance_and_tuning#5664500cbd45","java/performance_and_tuning#5cdc58a7d1c1"],"concepts":["opening","jmxremote","jstack","mbeanserver","visualvm","jcmd"],"correctMd":"Yes — local tools (`jcmd`, `jstack`, `jmap`, and same-host VisualVM) use the attach API over a local socket, so they read the MBeanServer with no `jmxremote` flags and no network listener.","answerFullMd":"Yes — local tools (`jcmd`, `jstack`, `jmap`, and same-host VisualVM) use the attach API over a local socket, so they read the MBeanServer with no `jmxremote` flags and no network listener. This is both the safest and the most common way to inspect a JVM in production: `jcmd <pid> Thread.print` for a thread dump, `jcmd <pid> GC.heap_info` for heap state, `jcmd <pid> JFR.start` to begin a recording. Opening a remote JMX port is only necessary for interactive GUI tools connecting from another host.","distractorsMd":["The MBeanServer is the in-JVM registry — essentially a `Map<ObjectName, MBean>` — that management tools query by `ObjectName` rather than by object reference.",null,null]},{"id":"java/performance_and_tuning#c7caa80bf415","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"jmx_and_management.md","difficulty":"advanced","question":"What is the RMI connector, and are there alternatives?","answerFull":"The RMI connector is JMX's default remote transport, addressed as service:jmx:rmi:///jndi/rmi://host:port/jmxrmi, and it is stateful and uses the two-port scheme that causes firewall issues. Alternatives include JMXMP (a simpler single-socket connector from the optional JMX Remote API, easier to firewall but requiring an extra jar) and, more commonly today, sidestepping remote JMX entirely by bridging MBeans to an HTTP /metrics endpoint via jmx_exporter. For interactive access many teams keep RMI but bind it to localhost and tunnel over SSH.","correct":"The RMI connector is JMX's default remote transport, addressed as service:jmx:rmi:///jndi/rmi://host:port/jmxrmi, and it is stateful and uses the two-port scheme that causes firewall issues.","distractors":["Bind the connector to localhost (com.sun.management.jmxremote.host=127.0.0.1), require authentication with a jmxremote.password/jmxremote.access file pair, and enable TLS (ssl=true, ideally ssl.need.client.auth=true).","JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while jcmd, jstack, and jmap are command-line tools that use the local attach API and need no open JMX port.","The RMI connector uses two ports — the registry port you configure and a separate data-channel export port that defaults to a random ephemeral port."],"distractorIds":["java/performance_and_tuning#2ccaf0b67212","java/performance_and_tuning#5c704f3e202b","java/performance_and_tuning#082149fcbce6"],"concepts":["alternatives","jmxrmi","jndi","addressed","firewall","rmi"],"correctMd":"The RMI connector is JMX's default remote transport, addressed as `service:jmx:rmi:///jndi/rmi://host:port/jmxrmi`, and it is stateful and uses the two-port scheme that causes firewall issues.","answerFullMd":"The RMI connector is JMX's default remote transport, addressed as `service:jmx:rmi:///jndi/rmi://host:port/jmxrmi`, and it is stateful and uses the two-port scheme that causes firewall issues. Alternatives include JMXMP (a simpler single-socket connector from the optional JMX Remote API, easier to firewall but requiring an extra jar) and, more commonly today, sidestepping remote JMX entirely by bridging MBeans to an HTTP `/metrics` endpoint via jmx_exporter. For interactive access many teams keep RMI but bind it to localhost and tunnel over SSH.","distractorsMd":["Bind the connector to localhost (`com.sun.management.jmxremote.host=127.0.0.1`), require authentication with a `jmxremote.password`/`jmxremote.access` file pair, and enable TLS (`ssl=true`, ideally `ssl.need.client.auth=true`).","JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while `jcmd`, `jstack`, and `jmap` are command-line tools that use the local attach API and need no open JMX port.",null]},{"id":"java/performance_and_tuning#4c58ce882d3f","section":"java","module":"java/performance_and_tuning","moduleName":"performance and tuning","sourceFile":"jmx_and_management.md","difficulty":"advanced","question":"Why is high-frequency JMX polling a potential availability risk?","answerFull":"Attribute getters run real code, so polling expensive ones every second — a getHitRatio() that takes a lock, or getThreadCpuTime across every thread — adds measurable load and can even contend with application work. Because tools poll attributes repeatedly, getters must be cheap, side-effect-free, and free of locks on hot paths. Keep polling intervals coarse and push heavy aggregation out of the getter itself.","correct":"Attribute getters run real code, so polling expensive ones every second — a getHitRatio() that takes a lock, or getThreadCpuTime across every thread — adds measurable load and can even contend with application work.","distractors":["The RMI connector is JMX's default remote transport, addressed as service:jmx:rmi:///jndi/rmi://host:port/jmxrmi, and it is stateful and uses the two-port scheme that causes firewall issues.","Unauthenticated remote JMX is a remote-code-execution vector, not just an information leak.","JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while jcmd, jstack, and jmap are command-line tools that use the local attach API and need no open JMX port."],"distractorIds":["java/performance_and_tuning#c7caa80bf415","java/performance_and_tuning#5cdc58a7d1c1","java/performance_and_tuning#5c704f3e202b"],"concepts":["contend","gethitratio","measurable","getters","getthreadcputime","potential"],"correctMd":"Attribute getters run real code, so polling expensive ones every second — a `getHitRatio()` that takes a lock, or `getThreadCpuTime` across every thread — adds measurable load and can even contend with application work.","answerFullMd":"Attribute getters run real code, so polling expensive ones every second — a `getHitRatio()` that takes a lock, or `getThreadCpuTime` across every thread — adds measurable load and can even contend with application work. Because tools poll attributes repeatedly, getters must be cheap, side-effect-free, and free of locks on hot paths. Keep polling intervals coarse and push heavy aggregation out of the getter itself.","distractorsMd":["The RMI connector is JMX's default remote transport, addressed as `service:jmx:rmi:///jndi/rmi://host:port/jmxrmi`, and it is stateful and uses the two-port scheme that causes firewall issues.",null,"JConsole and VisualVM are GUI tools that connect over a JMX connector (VisualVM also samples CPU/memory), while `jcmd`, `jstack`, and `jmap` are command-line tools that use the local attach API and need no open JMX port."]},{"id":"java/reactive_programming#5e13a388ef27","section":"java","module":"java/reactive_programming","moduleName":"reactive programming","sourceFile":"README.md","difficulty":"core","question":"What is the difference between Mono and Flux, and how do they relate to CompletableFuture and Stream?","answerFull":"Mono<T> emits 0 or 1 element and a completion/error signal; Flux<T> emits 0..N elements. Both are *lazy* — nothing runs until subscribe() — and both support backpressure. A CompletableFuture is *eager* (already executing the moment it is created), holds exactly one result, and has no backpressure or re-subscription. A Stream is lazy but *synchronous* and pull-only with no async or backpressure. So Mono ≈ \"async, lazy, re-subscribable Optional/Future\" and Flux ≈ \"async, backpressured Stream.\" The laziness is the key interview point: re-subscribing a Mono re-runs the work, whereas a CompletableFuture just hands back its already-computed value.","correct":"Mono<T> emits 0 or 1 element and a completion/error signal; Flux<T> emits 0..N elements.","distractors":["A payments service appended ledger entries using Flux.fromIterable(entries).flatMap(this::persist).","A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to Flux.range or an HTTP-call Mono runs the work twice, and each subscriber sees the full sequence from the start.","All three map each element to an inner publisher and flatten the results, but they differ in concurrency and ordering."],"distractorIds":["java/reactive_programming#0402f1709835","java/reactive_programming#b7d99fa81252","java/reactive_programming#1098533cf803"],"concepts":["completablefuture","difference","mono","emits","flux","completion"],"questionMd":"What is the difference between `Mono` and `Flux`, and how do they relate to `CompletableFuture` and `Stream`?","correctMd":"`Mono<T>` emits 0 or 1 element and a completion/error signal; `Flux<T>` emits 0..N elements.","answerFullMd":"`Mono<T>` emits 0 or 1 element and a completion/error signal; `Flux<T>` emits 0..N elements. Both are *lazy* — nothing runs until `subscribe()` — and both support backpressure. A `CompletableFuture` is *eager* (already executing the moment it is created), holds exactly one result, and has no backpressure or re-subscription. A `Stream` is lazy but *synchronous* and pull-only with no async or backpressure. So `Mono` ≈ \"async, lazy, re-subscribable Optional/Future\" and `Flux` ≈ \"async, backpressured Stream.\" The laziness is the key interview point: re-subscribing a `Mono` re-runs the work, whereas a `CompletableFuture` just hands back its already-computed value.","distractorsMd":["A payments service appended ledger entries using `Flux.fromIterable(entries).flatMap(this::persist)`.","A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to `Flux.range` or an HTTP-call `Mono` runs the work twice, and each subscriber sees the full sequence from the start.",null]},{"id":"java/reactive_programming#c238eccb075d","section":"java","module":"java/reactive_programming","moduleName":"reactive programming","sourceFile":"README.md","difficulty":"core","question":"What is backpressure and how does Reactive Streams implement it?","answerFull":"Backpressure is the mechanism by which a slow consumer signals a fast producer to slow down, preventing unbounded buffering and OutOfMemoryError. Reactive Streams implements it through the Subscription.request(n) call: when a subscriber subscribes, it requests a specific number of elements n, and the publisher may emit *at most* n onNext signals before waiting for more demand. This is a pull-push hybrid — demand is pulled upstream, data is pushed downstream within the granted demand. Operators that cannot naturally propagate demand (e.g., Flux.interval, a hot source) must adopt an overflow strategy (onBackpressureBuffer/Drop/Latest/Error).","correct":"Backpressure is the mechanism by which a slow consumer signals a fast producer to slow down, preventing unbounded buffering and OutOfMemoryError.","distractors":["Reactive servers like Netty use a tiny pool of event-loop threads (~2×CPU cores) where each thread multiplexes thousands of connections.","A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to Flux.range or an HTTP-call Mono runs the work twice, and each subscriber sees the full sequence from the start.","A payments service appended ledger entries using Flux.fromIterable(entries).flatMap(this::persist)."],"distractorIds":["java/reactive_programming#88d827141e56","java/reactive_programming#b7d99fa81252","java/reactive_programming#0402f1709835"],"concepts":["outofmemoryerror","buffering","backpressure","preventing","reactive","unbounded"],"correctMd":"Backpressure is the mechanism by which a slow consumer signals a fast producer to slow down, preventing unbounded buffering and `OutOfMemoryError`.","answerFullMd":"Backpressure is the mechanism by which a slow consumer signals a fast producer to slow down, preventing unbounded buffering and `OutOfMemoryError`. Reactive Streams implements it through the `Subscription.request(n)` call: when a subscriber subscribes, it requests a specific number of elements `n`, and the publisher may emit *at most* `n` `onNext` signals before waiting for more demand. This is a pull-push hybrid — demand is pulled upstream, data is pushed downstream within the granted demand. Operators that cannot naturally propagate demand (e.g., `Flux.interval`, a hot source) must adopt an overflow strategy (`onBackpressureBuffer/Drop/Latest/Error`).","distractorsMd":[null,"A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to `Flux.range` or an HTTP-call `Mono` runs the work twice, and each subscriber sees the full sequence from the start.","A payments service appended ledger entries using `Flux.fromIterable(entries).flatMap(this::persist)`."]},{"id":"java/reactive_programming#1098533cf803","section":"java","module":"java/reactive_programming","moduleName":"reactive programming","sourceFile":"README.md","difficulty":"core","question":"What is the difference between flatMap, concatMap, and switchMap?","answerFull":"All three map each element to an inner publisher and flatten the results, but they differ in concurrency and ordering. flatMap subscribes to inner publishers *eagerly and concurrently* (default up to 256 at once) and interleaves their results — order is not preserved, best throughput. concatMap processes inner publishers *one at a time*, fully draining each before the next — order preserved, no concurrency. switchMap subscribes to a new inner publisher and *cancels the previous one* — only the latest matters, ideal for type-ahead search where an in-flight query for stale input should be abandoned. Choosing flatMap where order matters (e.g., ordered writes) is a classic production bug.","correct":"All three map each element to an inner publisher and flatten the results, but they differ in concurrency and ordering.","distractors":["A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to Flux.range or an HTTP-call Mono runs the work twice, and each subscriber sees the full sequence from the start.","Mono<T> emits 0 or 1 element and a completion/error signal; Flux<T> emits 0..N elements.","A payments service appended ledger entries using Flux.fromIterable(entries).flatMap(this::persist)."],"distractorIds":["java/reactive_programming#b7d99fa81252","java/reactive_programming#5e13a388ef27","java/reactive_programming#0402f1709835"],"concepts":["switchmap","flatten","flatmap","difference","differ","publisher"],"questionMd":"What is the difference between `flatMap`, `concatMap`, and `switchMap`?","answerFullMd":"All three map each element to an inner publisher and flatten the results, but they differ in concurrency and ordering. `flatMap` subscribes to inner publishers *eagerly and concurrently* (default up to 256 at once) and interleaves their results — **order is not preserved**, best throughput. `concatMap` processes inner publishers *one at a time*, fully draining each before the next — **order preserved**, no concurrency. `switchMap` subscribes to a new inner publisher and *cancels the previous one* — only the latest matters, ideal for type-ahead search where an in-flight query for stale input should be abandoned. Choosing `flatMap` where order matters (e.g., ordered writes) is a classic production bug.","distractorsMd":["A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to `Flux.range` or an HTTP-call `Mono` runs the work twice, and each subscriber sees the full sequence from the start.","`Mono<T>` emits 0 or 1 element and a completion/error signal; `Flux<T>` emits 0..N elements.","A payments service appended ledger entries using `Flux.fromIterable(entries).flatMap(this::persist)`."]},{"id":"java/reactive_programming#88d827141e56","section":"java","module":"java/reactive_programming","moduleName":"reactive programming","sourceFile":"README.md","difficulty":"core","question":"Why must you never block inside a reactive pipeline running on an event-loop thread?","answerFull":"Reactive servers like Netty use a tiny pool of event-loop threads (~2×CPU cores) where each thread multiplexes thousands of connections. A blocking call (.block(), JDBC, Thread.sleep, a synchronous SDK) holds that thread for the duration of the blocking operation, during which *every connection that thread serves* is frozen. With 4 event-loop threads serving 50,000 connections, a few hundred-millisecond blocking calls can stall the whole server. The fix is to stay non-blocking, or wrap unavoidable blocking work in Mono.fromCallable(...).subscribeOn(Schedulers.boundedElastic()). Use BlockHound in tests to detect accidental blocking.","correct":"Reactive servers like Netty use a tiny pool of event-loop threads (~2×CPU cores) where each thread multiplexes thousands of connections.","distractors":["Schedulers.parallel() is a fixed pool sized to the CPU count, intended for *non-blocking, CPU-bound* work — never block on it.","Because operators hop threads (publishOn/subscribeOn), a ThreadLocal set on the subscribing thread is not visible after a thread switch — trace IDs and MDC values are lost.","A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to Flux.range or an HTTP-call Mono runs the work twice, and each subscriber sees the full sequence from the start."],"distractorIds":["java/reactive_programming#9606a733ce46","java/reactive_programming#16d67222fcc5","java/reactive_programming#b7d99fa81252"],"concepts":["multiplexes","netty","cores","tiny","reactive","thousands"],"answerFullMd":"Reactive servers like Netty use a tiny pool of event-loop threads (~2×CPU cores) where each thread multiplexes thousands of connections. A blocking call (`.block()`, JDBC, `Thread.sleep`, a synchronous SDK) holds that thread for the duration of the blocking operation, during which *every connection that thread serves* is frozen. With 4 event-loop threads serving 50,000 connections, a few hundred-millisecond blocking calls can stall the whole server. The fix is to stay non-blocking, or wrap unavoidable blocking work in `Mono.fromCallable(...).subscribeOn(Schedulers.boundedElastic())`. Use BlockHound in tests to detect accidental blocking.","distractorsMd":["`Schedulers.parallel()` is a fixed pool sized to the CPU count, intended for *non-blocking, CPU-bound* work — never block on it.","Because operators hop threads (`publishOn`/`subscribeOn`), a `ThreadLocal` set on the subscribing thread is not visible after a thread switch — trace IDs and MDC values are lost.","A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to `Flux.range` or an HTTP-call `Mono` runs the work twice, and each subscriber sees the full sequence from the start."]},{"id":"java/reactive_programming#b7d99fa81252","section":"java","module":"java/reactive_programming","moduleName":"reactive programming","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between a cold and a hot publisher?","answerFull":"A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to Flux.range or an HTTP-call Mono runs the work twice, and each subscriber sees the full sequence from the start. A hot publisher produces data *independently of subscribers* and multicasts the same stream — late subscribers miss whatever was already emitted (like tuning into a live broadcast mid-show). You convert cold to hot with share(), publish().refCount(), or cache(n) (which also replays the last n items to late subscribers). Mistaking a cold publisher for hot is why \"my HTTP call fired twice\" bugs happen.","correct":"A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to Flux.range or an HTTP-call Mono runs the work twice, and each subscriber sees the full sequence from the start.","distractors":["All three map each element to an inner publisher and flatten the results, but they differ in concurrency and ordering.","A payments service appended ledger entries using Flux.fromIterable(entries).flatMap(this::persist).","flatMap(mapper) defaults to subscribing to up to 256 inner publishers concurrently and merging their output."],"distractorIds":["java/reactive_programming#1098533cf803","java/reactive_programming#0402f1709835","java/reactive_programming#4aa5fe65af31"],"concepts":["subscribing","subscriber","difference","mono","twice","publisher"],"correctMd":"A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to `Flux.range` or an HTTP-call `Mono` runs the work twice, and each subscriber sees the full sequence from the start.","answerFullMd":"A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to `Flux.range` or an HTTP-call `Mono` runs the work twice, and each subscriber sees the full sequence from the start. A hot publisher produces data *independently of subscribers* and multicasts the same stream — late subscribers miss whatever was already emitted (like tuning into a live broadcast mid-show). You convert cold to hot with `share()`, `publish().refCount()`, or `cache(n)` (which also replays the last `n` items to late subscribers). Mistaking a cold publisher for hot is why \"my HTTP call fired twice\" bugs happen.","distractorsMd":[null,"A payments service appended ledger entries using `Flux.fromIterable(entries).flatMap(this::persist)`.","`flatMap(mapper)` defaults to subscribing to up to 256 inner publishers concurrently and merging their output."]},{"id":"java/reactive_programming#f66aea2b34b9","section":"java","module":"java/reactive_programming","moduleName":"reactive programming","sourceFile":"README.md","difficulty":"intermediate","question":"How do you handle errors in a reactive stream, and what is the difference between onErrorResume and onErrorReturn?","answerFull":"An onError signal is terminal — it stops the sequence. onErrorReturn(value) substitutes a single static fallback value and completes. onErrorResume(ex -> fallbackPublisher) switches to an entirely new publisher, enabling dynamic fallback (e.g., call a backup service). For transient failures use retryWhen(Retry.backoff(n, duration)) to re-subscribe with exponential backoff and jitter. There is also onErrorContinue, which drops the offending element and keeps the sequence alive — but it depends on operator cooperation, can behave surprisingly, and many teams ban it in favor of handling errors inside the flatMap per element.","correct":"An onError signal is terminal — it stops the sequence.","distractors":["For a cold Flux, each subscribe() triggers an independent, full re-execution from the start — two subscribers each get the complete sequence, and any side effects (HTTP calls, DB writes) run twice.","A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to Flux.range or an HTTP-call Mono runs the work twice, and each subscriber sees the full sequence from the start.","Mono<T> emits 0 or 1 element and a completion/error signal; Flux<T> emits 0..N elements."],"distractorIds":["java/reactive_programming#cfe49809c3ca","java/reactive_programming#b7d99fa81252","java/reactive_programming#5e13a388ef27"],"concepts":["onerror","terminal","difference","reactive","stops","handle"],"questionMd":"How do you handle errors in a reactive stream, and what is the difference between `onErrorResume` and `onErrorReturn`?","correctMd":"An `onError` signal is terminal — it stops the sequence.","answerFullMd":"An `onError` signal is terminal — it stops the sequence. `onErrorReturn(value)` substitutes a single static fallback value and completes. `onErrorResume(ex -> fallbackPublisher)` switches to an entirely new publisher, enabling dynamic fallback (e.g., call a backup service). For transient failures use `retryWhen(Retry.backoff(n, duration))` to re-subscribe with exponential backoff and jitter. There is also `onErrorContinue`, which drops the offending element and keeps the sequence alive — but it depends on operator cooperation, can behave surprisingly, and many teams ban it in favor of handling errors inside the `flatMap` per element.","distractorsMd":["For a cold `Flux`, each `subscribe()` triggers an independent, full re-execution from the start — two subscribers each get the complete sequence, and any side effects (HTTP calls, DB writes) run twice.","A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to `Flux.range` or an HTTP-call `Mono` runs the work twice, and each subscriber sees the full sequence from the start.","`Mono<T>` emits 0 or 1 element and a completion/error signal; `Flux<T>` emits 0..N elements."]},{"id":"java/reactive_programming#16d67222fcc5","section":"java","module":"java/reactive_programming","moduleName":"reactive programming","sourceFile":"README.md","difficulty":"intermediate","question":"How does context propagation work in Reactor, and why can't you use ThreadLocal?","answerFull":"Because operators hop threads (publishOn/subscribeOn), a ThreadLocal set on the subscribing thread is not visible after a thread switch — trace IDs and MDC values are lost. Reactor solves this with Context, an immutable key/value map that propagates *upstream* from the subscriber (you write it with contextWrite(...) near the end of the chain, and read it with deferContextual/transformDeferredContextual anywhere upstream). It propagates upstream because subscription itself flows upstream. Reactor 3.5+ adds a ContextRegistry bridge that snapshots Context into ThreadLocal around operator execution so legacy MDC-based logging keeps working.","correct":"Because operators hop threads (publishOn/subscribeOn), a ThreadLocal set on the subscribing thread is not visible after a thread switch — trace IDs and MDC values are lost.","distractors":["flatMap(mapper) defaults to subscribing to up to 256 inner publishers concurrently and merging their output.","Many operators request data from upstream in *batches* rather than one at a time, for efficiency.","A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to Flux.range or an HTTP-call Mono runs the work twice, and each subscriber sees the full sequence from the start."],"distractorIds":["java/reactive_programming#4aa5fe65af31","java/reactive_programming#e1c45cef6c5e","java/reactive_programming#b7d99fa81252"],"concepts":["publishon","subscribeon","subscribing","hop","operators","mdc"],"questionMd":"How does context propagation work in Reactor, and why can't you use `ThreadLocal`?","correctMd":"Because operators hop threads (`publishOn`/`subscribeOn`), a `ThreadLocal` set on the subscribing thread is not visible after a thread switch — trace IDs and MDC values are lost.","answerFullMd":"Because operators hop threads (`publishOn`/`subscribeOn`), a `ThreadLocal` set on the subscribing thread is not visible after a thread switch — trace IDs and MDC values are lost. Reactor solves this with `Context`, an immutable key/value map that propagates *upstream* from the subscriber (you write it with `contextWrite(...)` near the end of the chain, and read it with `deferContextual`/`transformDeferredContextual` anywhere upstream). It propagates upstream because subscription itself flows upstream. Reactor 3.5+ adds a `ContextRegistry` bridge that snapshots `Context` into `ThreadLocal` around operator execution so legacy MDC-based logging keeps working.","distractorsMd":["`flatMap(mapper)` defaults to subscribing to up to 256 inner publishers concurrently and merging their output.",null,"A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to `Flux.range` or an HTTP-call `Mono` runs the work twice, and each subscriber sees the full sequence from the start."]},{"id":"java/reactive_programming#d7d895103407","section":"java","module":"java/reactive_programming","moduleName":"reactive programming","sourceFile":"README.md","difficulty":"advanced","question":"How do you test reactive code deterministically?","answerFull":"Use StepVerifier from reactor-test. It subscribes to your publisher and lets you assert the exact sequence of signals: StepVerifier.create(flux).expectNext(\"a\",\"b\").expectComplete().verify(). For time-based operators (delay, interval, timeout) use StepVerifier.withVirtualTime(() -> ...) together with thenAwait(Duration) so a 1-hour delay verifies in microseconds via a VirtualTimeScheduler — no real waiting. You can also assert errors with expectError(SomeException.class) and inspect backpressure with thenRequest(n). This determinism is essential because reactive streams are otherwise timing-dependent and flaky to test with sleeps.","correct":"Use StepVerifier from reactor-test.","distractors":["Reactive servers like Netty use a tiny pool of event-loop threads (~2×CPU cores) where each thread multiplexes thousands of connections.","A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to Flux.range or an HTTP-call Mono runs the work twice, and each subscriber sees the full sequence from the start.","A payments service appended ledger entries using Flux.fromIterable(entries).flatMap(this::persist)."],"distractorIds":["java/reactive_programming#88d827141e56","java/reactive_programming#b7d99fa81252","java/reactive_programming#0402f1709835"],"concepts":["deterministically","stepverifier","reactor","reactive","test","code"],"correctMd":"Use `StepVerifier` from `reactor-test`.","answerFullMd":"Use `StepVerifier` from `reactor-test`. It subscribes to your publisher and lets you assert the exact sequence of signals: `StepVerifier.create(flux).expectNext(\"a\",\"b\").expectComplete().verify()`. For time-based operators (`delay`, `interval`, `timeout`) use `StepVerifier.withVirtualTime(() -> ...)` together with `thenAwait(Duration)` so a 1-hour delay verifies in microseconds via a `VirtualTimeScheduler` — no real waiting. You can also assert errors with `expectError(SomeException.class)` and inspect backpressure with `thenRequest(n)`. This determinism is essential because reactive streams are otherwise timing-dependent and flaky to test with sleeps.","distractorsMd":[null,"A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to `Flux.range` or an HTTP-call `Mono` runs the work twice, and each subscriber sees the full sequence from the start.","A payments service appended ledger entries using `Flux.fromIterable(entries).flatMap(this::persist)`."]},{"id":"java/reactive_programming#cfe49809c3ca","section":"java","module":"java/reactive_programming","moduleName":"reactive programming","sourceFile":"README.md","difficulty":"advanced","question":"What happens if you call subscribe() multiple times on the same Flux?","answerFull":"For a cold Flux, each subscribe() triggers an independent, full re-execution from the start — two subscribers each get the complete sequence, and any side effects (HTTP calls, DB writes) run twice. This surprises people coming from CompletableFuture, where the result is computed once. To share a single execution across subscribers, convert to hot with share() (multicast, no replay) or cache() (multicast + replay to late subscribers). For a hot Flux, multiple subscribers share the live stream and late ones miss earlier emissions.","correct":"For a cold Flux, each subscribe() triggers an independent, full re-execution from the start — two subscribers each get the complete sequence, and any side effects (HTTP calls, DB writes) run twice.","distractors":["A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to Flux.range or an HTTP-call Mono runs the work twice, and each subscriber sees the full sequence from the start.","Mono<T> emits 0 or 1 element and a completion/error signal; Flux<T> emits 0..N elements.","An onError signal is terminal — it stops the sequence."],"distractorIds":["java/reactive_programming#b7d99fa81252","java/reactive_programming#5e13a388ef27","java/reactive_programming#f66aea2b34b9"],"concepts":["subscribers","subscribe","twice","effects","flux","cold"],"questionMd":"What happens if you call `subscribe()` multiple times on the same `Flux`?","correctMd":"For a cold `Flux`, each `subscribe()` triggers an independent, full re-execution from the start — two subscribers each get the complete sequence, and any side effects (HTTP calls, DB writes) run twice.","answerFullMd":"For a cold `Flux`, each `subscribe()` triggers an independent, full re-execution from the start — two subscribers each get the complete sequence, and any side effects (HTTP calls, DB writes) run twice. This surprises people coming from `CompletableFuture`, where the result is computed once. To share a single execution across subscribers, convert to hot with `share()` (multicast, no replay) or `cache()` (multicast + replay to late subscribers). For a hot `Flux`, multiple subscribers share the live stream and late ones miss earlier emissions.","distractorsMd":["A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to `Flux.range` or an HTTP-call `Mono` runs the work twice, and each subscriber sees the full sequence from the start.","`Mono<T>` emits 0 or 1 element and a completion/error signal; `Flux<T>` emits 0..N elements.","An `onError` signal is terminal — it stops the sequence."]},{"id":"java/reactive_programming#9606a733ce46","section":"java","module":"java/reactive_programming","moduleName":"reactive programming","sourceFile":"README.md","difficulty":"advanced","question":"Explain the Reactor Scheduler types and when to use each.","answerFull":"Schedulers.parallel() is a fixed pool sized to the CPU count, intended for *non-blocking, CPU-bound* work — never block on it. Schedulers.boundedElastic() is an elastic, capped pool (default 10×CPU threads, with a bounded task queue) designed specifically for *wrapping legacy blocking I/O* without exhausting threads. Schedulers.single() is one reused thread for serialized side effects. Schedulers.immediate() runs work on the calling thread (no switch), useful in tests. The cardinal rule: blocking work goes on boundedElastic, CPU work on parallel, and you must never put a blocking call on parallel or a Netty event loop.","correct":"Schedulers.parallel() is a fixed pool sized to the CPU count, intended for *non-blocking, CPU-bound* work — never block on it.","distractors":["Reactive servers like Netty use a tiny pool of event-loop threads (~2×CPU cores) where each thread multiplexes thousands of connections.","Use StepVerifier from reactor-test.","A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to Flux.range or an HTTP-call Mono runs the work twice, and each subscriber sees the full sequence from the start."],"distractorIds":["java/reactive_programming#88d827141e56","java/reactive_programming#d7d895103407","java/reactive_programming#b7d99fa81252"],"concepts":["schedulers","explain","sized","intended","reactor","scheduler"],"questionMd":"Explain the Reactor `Scheduler` types and when to use each.","correctMd":"`Schedulers.parallel()` is a fixed pool sized to the CPU count, intended for *non-blocking, CPU-bound* work — never block on it.","answerFullMd":"`Schedulers.parallel()` is a fixed pool sized to the CPU count, intended for *non-blocking, CPU-bound* work — never block on it. `Schedulers.boundedElastic()` is an elastic, capped pool (default 10×CPU threads, with a bounded task queue) designed specifically for *wrapping legacy blocking I/O* without exhausting threads. `Schedulers.single()` is one reused thread for serialized side effects. `Schedulers.immediate()` runs work on the calling thread (no switch), useful in tests. The cardinal rule: blocking work goes on `boundedElastic`, CPU work on `parallel`, and you must never put a blocking call on `parallel` or a Netty event loop.","distractorsMd":[null,"Use `StepVerifier` from `reactor-test`.","A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to `Flux.range` or an HTTP-call `Mono` runs the work twice, and each subscriber sees the full sequence from the start."]},{"id":"java/reactive_programming#e1c45cef6c5e","section":"java","module":"java/reactive_programming","moduleName":"reactive programming","sourceFile":"README.md","difficulty":"advanced","question":"What is the prefetch / request batching behavior of operators like publishOn and flatMap?","answerFull":"Many operators request data from upstream in *batches* rather than one at a time, for efficiency. publishOn(scheduler, prefetch) and flatMap(mapper, concurrency, prefetch) take a prefetch parameter (default 256, or Queues.SMALL_BUFFER_SIZE) that controls how many elements the operator requests upstream before processing, replenishing demand at 75% consumption (the \"limit rate\" / replenish threshold). Tuning prefetch down (e.g., publishOn(parallel(), 1)) gives tighter backpressure and lower memory at the cost of more request round-trips; tuning it up improves throughput for fast consumers. Flux.limitRate(n) lets you cap demand explicitly regardless of the downstream request.","correct":"Many operators request data from upstream in *batches* rather than one at a time, for efficiency.","distractors":["Because operators hop threads (publishOn/subscribeOn), a ThreadLocal set on the subscribing thread is not visible after a thread switch — trace IDs and MDC values are lost.","A payments service appended ledger entries using Flux.fromIterable(entries).flatMap(this::persist).","Reactive servers like Netty use a tiny pool of event-loop threads (~2×CPU cores) where each thread multiplexes thousands of connections."],"distractorIds":["java/reactive_programming#16d67222fcc5","java/reactive_programming#0402f1709835","java/reactive_programming#88d827141e56"],"concepts":["publishon","prefetch","efficiency","upstream","operators","batches"],"questionMd":"What is the prefetch / request batching behavior of operators like `publishOn` and `flatMap`?","answerFullMd":"Many operators request data from upstream in *batches* rather than one at a time, for efficiency. `publishOn(scheduler, prefetch)` and `flatMap(mapper, concurrency, prefetch)` take a prefetch parameter (default 256, or `Queues.SMALL_BUFFER_SIZE`) that controls how many elements the operator requests upstream before processing, replenishing demand at 75% consumption (the \"limit rate\" / replenish threshold). Tuning prefetch down (e.g., `publishOn(parallel(), 1)`) gives tighter backpressure and lower memory at the cost of more request round-trips; tuning it up improves throughput for fast consumers. `Flux.limitRate(n)` lets you cap demand explicitly regardless of the downstream request.","distractorsMd":["Because operators hop threads (`publishOn`/`subscribeOn`), a `ThreadLocal` set on the subscribing thread is not visible after a thread switch — trace IDs and MDC values are lost.","A payments service appended ledger entries using `Flux.fromIterable(entries).flatMap(this::persist)`.",null]},{"id":"java/reactive_programming#0402f1709835","section":"java","module":"java/reactive_programming","moduleName":"reactive programming","sourceFile":"README.md","difficulty":"advanced","question":"Describe a production incident caused by misuse of reactive operators.","answerFull":"A payments service appended ledger entries using Flux.fromIterable(entries).flatMap(this::persist). Under load, flatMap's default concurrency of 256 caused inner persist calls to race, and entries were committed out of chronological order — breaking the running-balance invariant and causing reconciliation failures. The fix was a one-word change to concatMap, which serializes the inner publishers and preserves order, at the cost of throughput. The lesson: flatMap is an *unordered, concurrent* operator; whenever downstream correctness depends on order, use concatMap (or flatMapSequential if you need parallelism with ordered output). They also added a StepVerifier test asserting output order to prevent regression.","correct":"A payments service appended ledger entries using Flux.fromIterable(entries).flatMap(this::persist).","distractors":["A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to Flux.range or an HTTP-call Mono runs the work twice, and each subscriber sees the full sequence from the start.","Reactive servers like Netty use a tiny pool of event-loop threads (~2×CPU cores) where each thread multiplexes thousands of connections.","Because operators hop threads (publishOn/subscribeOn), a ThreadLocal set on the subscribing thread is not visible after a thread switch — trace IDs and MDC values are lost."],"distractorIds":["java/reactive_programming#b7d99fa81252","java/reactive_programming#88d827141e56","java/reactive_programming#16d67222fcc5"],"concepts":["describe","fromiterable","misuse","payments","caused","operators"],"correctMd":"A payments service appended ledger entries using `Flux.fromIterable(entries).flatMap(this::persist)`.","answerFullMd":"A payments service appended ledger entries using `Flux.fromIterable(entries).flatMap(this::persist)`. Under load, `flatMap`'s default concurrency of 256 caused inner `persist` calls to race, and entries were committed out of chronological order — breaking the running-balance invariant and causing reconciliation failures. The fix was a one-word change to `concatMap`, which serializes the inner publishers and preserves order, at the cost of throughput. The lesson: `flatMap` is an *unordered, concurrent* operator; whenever downstream correctness depends on order, use `concatMap` (or `flatMapSequential` if you need parallelism with ordered output). They also added a `StepVerifier` test asserting output order to prevent regression.","distractorsMd":["A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to `Flux.range` or an HTTP-call `Mono` runs the work twice, and each subscriber sees the full sequence from the start.",null,"Because operators hop threads (`publishOn`/`subscribeOn`), a `ThreadLocal` set on the subscribing thread is not visible after a thread switch — trace IDs and MDC values are lost."]},{"id":"java/reactive_programming#4aa5fe65af31","section":"java","module":"java/reactive_programming","moduleName":"reactive programming","sourceFile":"README.md","difficulty":"advanced","question":"How does flatMap concurrency interact with backpressure, and how do you bound it?","answerFull":"flatMap(mapper) defaults to subscribing to up to 256 inner publishers concurrently and merging their output. Each inner subscription requests its own prefetch from upstream, so an unbounded fan-out can overwhelm a downstream consumer or a rate-limited remote service. You bound it with the concurrency parameter: flatMap(mapper, 8) caps in-flight inner publishers at 8, providing natural backpressure to a downstream API (e.g., \"no more than 8 concurrent calls to the payment gateway\"). This is the idiomatic reactive rate-limiter for fan-out. Combine with delayElements or a RateLimiter for time-based throttling. Forgetting to bound flatMap concurrency is a common cause of overwhelming downstream dependencies.","correct":"flatMap(mapper) defaults to subscribing to up to 256 inner publishers concurrently and merging their output.","distractors":["Schedulers.parallel() is a fixed pool sized to the CPU count, intended for *non-blocking, CPU-bound* work — never block on it.","A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to Flux.range or an HTTP-call Mono runs the work twice, and each subscriber sees the full sequence from the start.","A payments service appended ledger entries using Flux.fromIterable(entries).flatMap(this::persist)."],"distractorIds":["java/reactive_programming#9606a733ce46","java/reactive_programming#b7d99fa81252","java/reactive_programming#0402f1709835"],"concepts":["publishers","subscribing","interact","mapper","backpressure","flatmap"],"questionMd":"How does `flatMap` concurrency interact with backpressure, and how do you bound it?","correctMd":"`flatMap(mapper)` defaults to subscribing to up to 256 inner publishers concurrently and merging their output.","answerFullMd":"`flatMap(mapper)` defaults to subscribing to up to 256 inner publishers concurrently and merging their output. Each inner subscription requests its own prefetch from upstream, so an unbounded fan-out can overwhelm a downstream consumer or a rate-limited remote service. You bound it with the concurrency parameter: `flatMap(mapper, 8)` caps in-flight inner publishers at 8, providing natural backpressure to a downstream API (e.g., \"no more than 8 concurrent calls to the payment gateway\"). This is the idiomatic reactive rate-limiter for fan-out. Combine with `delayElements` or a `RateLimiter` for time-based throttling. Forgetting to bound `flatMap` concurrency is a common cause of overwhelming downstream dependencies.","distractorsMd":["`Schedulers.parallel()` is a fixed pool sized to the CPU count, intended for *non-blocking, CPU-bound* work — never block on it.","A cold publisher starts its data-producing work *fresh for each subscriber* — subscribing twice to `Flux.range` or an HTTP-call `Mono` runs the work twice, and each subscriber sees the full sequence from the start.","A payments service appended ledger entries using `Flux.fromIterable(entries).flatMap(this::persist)`."]},{"id":"java/security_and_cryptography#5015b3867211","section":"java","module":"java/security_and_cryptography","moduleName":"security and cryptography","sourceFile":"README.md","difficulty":"core","question":"Why is Cipher.getInstance(\"AES\") dangerous, and what does it actually give you?","answerFull":"It silently resolves to AES/ECB/PKCS5Padding — ECB mode with no IV. ECB encrypts each 16-byte block independently, so identical plaintext blocks produce identical ciphertext blocks, leaking structure and repeats to anyone watching (the \"ECB penguin\"). Adobe's 2013 breach and Zoom's 2020 media encryption both fell to exactly this. Always name the full transformation and prefer authenticated AES/GCM/NoPadding.","correct":"It silently resolves to AES/ECB/PKCS5Padding — ECB mode with no IV.","distractors":["The engine class (Cipher) asks the provider framework, which walks Security.getProviders() in priority order and returns the first provider whose SPI supplies that algorithm.","Choose AES-GCM by default — it provides confidentiality and integrity in one primitive at AES-NI speed with no padding (so no padding-oracle surface).","A MAC uses a single shared secret key — both parties can compute and verify it, giving integrity and authenticity but *not* non-repudiation (either party could have made it)."],"distractorIds":["java/security_and_cryptography#6dd871533922","java/security_and_cryptography#fbd28cc07a3c","java/security_and_cryptography#94c81a15d700"],"concepts":["dangerous","ecb","getinstance","pkcs5padding","cipher","aes"],"questionMd":"Why is `Cipher.getInstance(\"AES\")` dangerous, and what does it actually give you?","correctMd":"It silently resolves to `AES/ECB/PKCS5Padding` — ECB mode with no IV.","answerFullMd":"It silently resolves to `AES/ECB/PKCS5Padding` — ECB mode with no IV. ECB encrypts each 16-byte block independently, so identical plaintext blocks produce identical ciphertext blocks, leaking structure and repeats to anyone watching (the \"ECB penguin\"). Adobe's 2013 breach and Zoom's 2020 media encryption both fell to exactly this. Always name the full transformation and prefer authenticated `AES/GCM/NoPadding`.","distractorsMd":["The engine class (`Cipher`) asks the provider framework, which walks `Security.getProviders()` in priority order and returns the first provider whose SPI supplies that algorithm.",null,null]},{"id":"java/security_and_cryptography#7cf0546a6d4f","section":"java","module":"java/security_and_cryptography","moduleName":"security and cryptography","sourceFile":"README.md","difficulty":"core","question":"What happens if you reuse a nonce with AES-GCM, and why is it worse than reusing a CBC IV?","answerFull":"Reusing a 96-bit GCM nonce under the same key is catastrophic — it leaks the XOR of the two plaintexts *and* the GCM authentication subkey H, which lets an attacker forge valid tags for arbitrary ciphertext. A repeated CBC IV only weakens confidentiality for identical prefixes; a repeated GCM nonce breaks both confidentiality and authenticity completely. Generate a fresh random nonce (or a guaranteed-unique counter) per message, and rotate the key well before 2^32 messages.","correct":"Reusing a 96-bit GCM nonce under the same key is catastrophic — it leaks the XOR of the two plaintexts *and* the GCM authentication subkey H, which lets an attacker forge valid tags for arbitrary ciphertext.","distractors":["PKCS#1 v1.5 encryption padding is vulnerable to Bleichenbacher's adaptive chosen-ciphertext attack — a padding oracle over many queries can recover the plaintext without the private key.","It's an attack on unauthenticated CBC where the server reveals whether decryption produced valid PKCS#7 padding, letting an attacker decrypt ciphertext byte-by-byte without the key.","RSA can only encrypt a payload smaller than its modulus (a 3072-bit key handles ~318 bytes with OAEP), so you can't encrypt 10 MB directly."],"distractorIds":["java/security_and_cryptography#a5c1051f9111","java/security_and_cryptography#7b7a9d754646","java/security_and_cryptography#44d5e69c0edd"],"concepts":["cbc","plaintexts","subkey","forge","nonce","gcm"]},{"id":"java/security_and_cryptography#b76e79ac49e6","section":"java","module":"java/security_and_cryptography","moduleName":"security and cryptography","sourceFile":"README.md","difficulty":"core","question":"Why must you never use new Random() (or Math.random()) to generate a key or IV?","answerFull":"java.util.Random is a 48-bit linear congruential generator seeded from the clock, so its entire output stream is predictable from a handful of samples. An attacker who sees a few values can reconstruct the state and derive every key/IV you produce. Always use SecureRandom (DRBG); use SecureRandom.getInstanceStrong() for long-lived key material.","correct":"java.util.Random is a 48-bit linear congruential generator seeded from the clock, so its entire output stream is predictable from a handful of samples.","distractors":["With envelope encryption you only re-wrap the DEKs, never the ciphertext: unwrap each stored DEK with the old KEK and re-wrap it with the new KEK, updating the wrapped-DEK column and a key-version tag.","A MAC uses a single shared secret key — both parties can compute and verify it, giving integrity and authenticity but *not* non-repudiation (either party could have made it).","Reusing a 96-bit GCM nonce under the same key is catastrophic — it leaks the XOR of the two plaintexts *and* the GCM authentication subkey H, which lets an attacker forge valid tags for arbitrary ciphertext."],"distractorIds":["java/security_and_cryptography#3451494fce19","java/security_and_cryptography#94c81a15d700","java/security_and_cryptography#7cf0546a6d4f"],"concepts":["congruential","seeded","handful","predictable","math","util"],"questionMd":"Why must you never use `new Random()` (or `Math.random()`) to generate a key or IV?","correctMd":"`java.util.Random` is a 48-bit linear congruential generator seeded from the clock, so its entire output stream is predictable from a handful of samples.","answerFullMd":"`java.util.Random` is a 48-bit linear congruential generator seeded from the clock, so its entire output stream is predictable from a handful of samples. An attacker who sees a few values can reconstruct the state and derive every key/IV you produce. Always use `SecureRandom` (DRBG); use `SecureRandom.getInstanceStrong()` for long-lived key material."},{"id":"java/security_and_cryptography#3700b6edf8e2","section":"java","module":"java/security_and_cryptography","moduleName":"security and cryptography","sourceFile":"README.md","difficulty":"core","question":"Why store a password in char[] instead of String?","answerFull":"Because a String is immutable and possibly interned, so you cannot erase it — the secret lingers in the heap (and in any heap dump) until GC eventually collects it, uncontrollably. A char[] can be overwritten with Arrays.fill(pw, '\\0') the instant you're done. This is why every JCA password API — PBEKeySpec, KeyStore.load, PasswordProtection — takes char[], not String.","correct":"Because a String is immutable and possibly interned, so you cannot erase it — the secret lingers in the heap (and in any heap dump) until GC eventually collects it, uncontrollably.","distractors":["A MAC uses a single shared secret key — both parties can compute and verify it, giving integrity and authenticity but *not* non-repudiation (either party could have made it).","PKCS12 is a standardized, interoperable format (RFC 7292) that can store secret keys and is the JDK default since Java 9, unlike the proprietary Java-only JKS.","SHA-256 is a fast, unsalted, single-pass digest — a GPU can try ~10 billion candidates per second, so any human password falls quickly."],"distractorIds":["java/security_and_cryptography#94c81a15d700","java/security_and_cryptography#04498d8f37c5","java/security_and_cryptography#58977d7bbd92"],"concepts":["erase","lingers","uncontrollably","interned","possibly","char"],"questionMd":"Why store a password in `char[]` instead of `String`?","correctMd":"Because a `String` is immutable and possibly interned, so you cannot erase it — the secret lingers in the heap (and in any heap dump) until GC eventually collects it, uncontrollably.","answerFullMd":"Because a `String` is immutable and possibly interned, so you cannot erase it — the secret lingers in the heap (and in any heap dump) until GC eventually collects it, uncontrollably. A `char[]` can be overwritten with `Arrays.fill(pw, '\\0')` the instant you're done. This is why every JCA password API — `PBEKeySpec`, `KeyStore.load`, `PasswordProtection` — takes `char[]`, not `String`."},{"id":"java/security_and_cryptography#fbd28cc07a3c","section":"java","module":"java/security_and_cryptography","moduleName":"security and cryptography","sourceFile":"README.md","difficulty":"core","question":"When would you choose AES-GCM over AES-CBC, and when is CBC still acceptable?","answerFull":"Choose AES-GCM by default — it provides confidentiality and integrity in one primitive at AES-NI speed with no padding (so no padding-oracle surface). CBC is acceptable only for legacy interop and only as encrypt-then-MAC: a random IV, a separate HMAC key, MAC the IV+ciphertext, and verify the MAC before decrypting. Unauthenticated CBC is the classic padding-oracle setup and should never ship.","correct":"Choose AES-GCM by default — it provides confidentiality and integrity in one primitive at AES-NI speed with no padding (so no padding-oracle surface).","distractors":["It's an attack on unauthenticated CBC where the server reveals whether decryption produced valid PKCS#7 padding, letting an attacker decrypt ciphertext byte-by-byte without the key.","A MAC uses a single shared secret key — both parties can compute and verify it, giving integrity and authenticity but *not* non-repudiation (either party could have made it).","Reusing a 96-bit GCM nonce under the same key is catastrophic — it leaks the XOR of the two plaintexts *and* the GCM authentication subkey H, which lets an attacker forge valid tags for arbitrary ciphertext."],"distractorIds":["java/security_and_cryptography#7b7a9d754646","java/security_and_cryptography#94c81a15d700","java/security_and_cryptography#7cf0546a6d4f"],"concepts":["cbc","confidentiality","gcm","integrity","oracle","surface"]},{"id":"java/security_and_cryptography#58977d7bbd92","section":"java","module":"java/security_and_cryptography","moduleName":"security and cryptography","sourceFile":"README.md","difficulty":"intermediate","question":"Why can't you use plain SHA-256 to hash passwords, and what should you use?","answerFull":"SHA-256 is a fast, unsalted, single-pass digest — a GPU can try ~10 billion candidates per second, so any human password falls quickly. Password hashing needs to be *deliberately slow, salted, and ideally memory-hard*: use Argon2id (best, via BouncyCastle), scrypt, or bcrypt; use PBKDF2-HMAC-SHA256 at 600k+ iterations when you can't add a dependency. The salt defeats rainbow tables; the work factor defeats brute force.","correct":"SHA-256 is a fast, unsalted, single-pass digest — a GPU can try ~10 billion candidates per second, so any human password falls quickly.","distractors":["Because the naive SHA256(key || message) is vulnerable to length-extension attacks that let an attacker forge a digest for an extended message.","Pick EC (ECDSA/ECDH on P-256) for new systems: a 256-bit EC key gives ~128-bit security equivalent to a 3072-bit RSA key, with far smaller keys/signatures and faster keygen and signing.","A MAC uses a single shared secret key — both parties can compute and verify it, giving integrity and authenticity but *not* non-repudiation (either party could have made it)."],"distractorIds":["java/security_and_cryptography#16bca1829586","java/security_and_cryptography#76428b137890","java/security_and_cryptography#94c81a15d700"],"concepts":["passwords","unsalted","digest","billion","quickly","password"],"distractorsMd":["Because the naive `SHA256(key || message)` is vulnerable to length-extension attacks that let an attacker forge a digest for an extended message.",null,null]},{"id":"java/security_and_cryptography#7b7a9d754646","section":"java","module":"java/security_and_cryptography","moduleName":"security and cryptography","sourceFile":"README.md","difficulty":"intermediate","question":"What is a padding-oracle attack and how do you prevent it in Java?","answerFull":"It's an attack on unauthenticated CBC where the server reveals whether decryption produced valid PKCS#7 padding, letting an attacker decrypt ciphertext byte-by-byte without the key. The oracle leaks through an error message, status code, or response timing. Prevent it by using authenticated encryption (AES-GCM has no padding at all) or encrypt-then-MAC where you verify the HMAC in constant time *before* attempting to decrypt, so malformed ciphertext is rejected before the unpadding step runs.","correct":"It's an attack on unauthenticated CBC where the server reveals whether decryption produced valid PKCS#7 padding, letting an attacker decrypt ciphertext byte-by-byte without the key.","distractors":["The tag (128-bit here) authenticates the ciphertext — decryption throws AEADBadTagException if a single bit was flipped, so you get tamper detection for free.","Reusing a 96-bit GCM nonce under the same key is catastrophic — it leaks the XOR of the two plaintexts *and* the GCM authentication subkey H, which lets an attacker forge valid tags for arbitrary ciphertext.","With envelope encryption you only re-wrap the DEKs, never the ciphertext: unwrap each stored DEK with the old KEK and re-wrap it with the new KEK, updating the wrapped-DEK column and a key-version tag."],"distractorIds":["java/security_and_cryptography#e1bb01c3ca9b","java/security_and_cryptography#7cf0546a6d4f","java/security_and_cryptography#3451494fce19"],"concepts":["cbc","decrypt","pkcs","decryption","reveals","unauthenticated"],"distractorsMd":["The tag (128-bit here) authenticates the ciphertext — decryption throws `AEADBadTagException` if a single bit was flipped, so you get tamper detection for free.",null,null]},{"id":"java/security_and_cryptography#76428b137890","section":"java","module":"java/security_and_cryptography","moduleName":"security and cryptography","sourceFile":"README.md","difficulty":"intermediate","question":"RSA vs elliptic curve — which do you pick and why?","answerFull":"Pick EC (ECDSA/ECDH on P-256) for new systems: a 256-bit EC key gives ~128-bit security equivalent to a 3072-bit RSA key, with far smaller keys/signatures and faster keygen and signing. RSA's one edge is faster *verification*, and it's unavoidable when interoperating with RSA-only peers. For key exchange prefer ephemeral ECDH (forward secrecy); for encryption-of-a-key use RSA-OAEP only when the recipient mandates RSA.","correct":"Pick EC (ECDSA/ECDH on P-256) for new systems: a 256-bit EC key gives ~128-bit security equivalent to a 3072-bit RSA key, with far smaller keys/signatures and faster keygen and signing.","distractors":["The TrustManager builds and validates the server's certificate chain up to a trusted CA root in the truststore (checking signatures, validity dates, and revocation if configured).","Reusing a 96-bit GCM nonce under the same key is catastrophic — it leaks the XOR of the two plaintexts *and* the GCM authentication subkey H, which lets an attacker forge valid tags for arbitrary ciphertext.","PKCS12 is a standardized, interoperable format (RFC 7292) that can store secret keys and is the JDK default since Java 9, unlike the proprietary Java-only JKS."],"distractorIds":["java/security_and_cryptography#43bf3cb6ee35","java/security_and_cryptography#7cf0546a6d4f","java/security_and_cryptography#04498d8f37c5"],"concepts":["ecdh","ecdsa","keygen","3072","curve","rsa"],"distractorsMd":["The `TrustManager` builds and validates the server's certificate chain up to a trusted CA root in the truststore (checking signatures, validity dates, and revocation if configured).",null,null]},{"id":"java/security_and_cryptography#6dd871533922","section":"java","module":"java/security_and_cryptography","moduleName":"security and cryptography","sourceFile":"README.md","difficulty":"intermediate","question":"How does the JCA provider architecture resolve getInstance(\"AES/GCM/NoPadding\")?","answerFull":"The engine class (Cipher) asks the provider framework, which walks Security.getProviders() in priority order and returns the first provider whose SPI supplies that algorithm. This indirection makes algorithms swappable — adding BouncyCastle at position 1 changes which implementation you get without touching call sites. It's also why the transformation string is security-critical: the string, not a type, selects the exact behavior.","correct":"The engine class (Cipher) asks the provider framework, which walks Security.getProviders() in priority order and returns the first provider whose SPI supplies that algorithm.","distractors":["It silently resolves to AES/ECB/PKCS5Padding — ECB mode with no IV.","Choose AES-GCM by default — it provides confidentiality and integrity in one primitive at AES-NI speed with no padding (so no padding-oracle surface).","Reusing a 96-bit GCM nonce under the same key is catastrophic — it leaks the XOR of the two plaintexts *and* the GCM authentication subkey H, which lets an attacker forge valid tags for arbitrary ciphertext."],"distractorIds":["java/security_and_cryptography#5015b3867211","java/security_and_cryptography#fbd28cc07a3c","java/security_and_cryptography#7cf0546a6d4f"],"concepts":["getinstance","getproviders","resolve","supplies","cipher","spi"],"questionMd":"How does the JCA provider architecture resolve `getInstance(\"AES/GCM/NoPadding\")`?","correctMd":"The engine class (`Cipher`) asks the provider framework, which walks `Security.getProviders()` in priority order and returns the first provider whose SPI supplies that algorithm.","answerFullMd":"The engine class (`Cipher`) asks the provider framework, which walks `Security.getProviders()` in priority order and returns the first provider whose SPI supplies that algorithm. This indirection makes algorithms swappable — adding BouncyCastle at position 1 changes which implementation you get without touching call sites. It's also why the transformation string is security-critical: the string, not a type, selects the exact behavior.","distractorsMd":["It silently resolves to `AES/ECB/PKCS5Padding` — ECB mode with no IV.",null,null]},{"id":"java/security_and_cryptography#94c81a15d700","section":"java","module":"java/security_and_cryptography","moduleName":"security and cryptography","sourceFile":"README.md","difficulty":"intermediate","question":"What's the difference between a MAC (HMAC) and a digital signature?","answerFull":"A MAC uses a single shared secret key — both parties can compute and verify it, giving integrity and authenticity but *not* non-repudiation (either party could have made it). A signature is asymmetric — only the private-key holder can produce it, and anyone with the public key can verify — giving non-repudiation. Use HMAC when both sides share a key (fast, symmetric); use Signature (ECDSA/RSA-PSS) when a third party must verify authorship or the signer must be held accountable.","correct":"A MAC uses a single shared secret key — both parties can compute and verify it, giving integrity and authenticity but *not* non-repudiation (either party could have made it).","distractors":["Choose AES-GCM by default — it provides confidentiality and integrity in one primitive at AES-NI speed with no padding (so no padding-oracle surface).","Envelope encryption encrypts each data record with a fresh Data Encryption Key (DEK), then wraps (encrypts) that DEK with a long-lived Key Encryption Key (KEK) held in a KeyStore, KMS, or HSM.","PKCS#1 v1.5 encryption padding is vulnerable to Bleichenbacher's adaptive chosen-ciphertext attack — a padding oracle over many queries can recover the plaintext without the private key."],"distractorIds":["java/security_and_cryptography#fbd28cc07a3c","java/security_and_cryptography#70593d615312","java/security_and_cryptography#a5c1051f9111"],"concepts":["authenticity","hmac","repudiation","integrity","mac","parties"],"answerFullMd":"A MAC uses a single shared secret key — both parties can compute and verify it, giving integrity and authenticity but *not* non-repudiation (either party could have made it). A signature is asymmetric — only the private-key holder can produce it, and anyone with the public key can verify — giving non-repudiation. Use HMAC when both sides share a key (fast, symmetric); use `Signature` (ECDSA/RSA-PSS) when a third party must verify authorship or the signer must be held accountable."},{"id":"java/security_and_cryptography#16bca1829586","section":"java","module":"java/security_and_cryptography","moduleName":"security and cryptography","sourceFile":"README.md","difficulty":"advanced","question":"Why is HMAC-SHA256 preferred over SHA256(key || message)?","answerFull":"Because the naive SHA256(key || message) is vulnerable to length-extension attacks that let an attacker forge a digest for an extended message. Knowing only the digest and message length, they can append data and compute a valid digest without the key. HMAC's nested construction, H((k⊕opad) || H((k⊕ipad) || m)), closes that hole. Always use the Mac engine class with HmacSHA256, never a hand-rolled hash of key and message.","correct":"Because the naive SHA256(key || message) is vulnerable to length-extension attacks that let an attacker forge a digest for an extended message.","distractors":["It can block — on Linux it may map to a blocking entropy source, so on entropy-starved boxes like fresh VMs or containers key generation can stall at startup.","Reusing a 96-bit GCM nonce under the same key is catastrophic — it leaks the XOR of the two plaintexts *and* the GCM authentication subkey H, which lets an attacker forge valid tags for arbitrary ciphertext.","A MAC uses a single shared secret key — both parties can compute and verify it, giving integrity and authenticity but *not* non-repudiation (either party could have made it)."],"distractorIds":["java/security_and_cryptography#13f3301f1bc9","java/security_and_cryptography#7cf0546a6d4f","java/security_and_cryptography#94c81a15d700"],"concepts":["forge","hmac","sha256","extended","vulnerable","digest"],"questionMd":"Why is HMAC-SHA256 preferred over `SHA256(key || message)`?","correctMd":"Because the naive `SHA256(key || message)` is vulnerable to length-extension attacks that let an attacker forge a digest for an extended message.","answerFullMd":"Because the naive `SHA256(key || message)` is vulnerable to length-extension attacks that let an attacker forge a digest for an extended message. Knowing only the digest and message length, they can append data and compute a valid digest without the key. HMAC's nested construction, `H((k⊕opad) || H((k⊕ipad) || m))`, closes that hole. Always use the `Mac` engine class with `HmacSHA256`, never a hand-rolled hash of key and message."},{"id":"java/security_and_cryptography#70593d615312","section":"java","module":"java/security_and_cryptography","moduleName":"security and cryptography","sourceFile":"README.md","difficulty":"advanced","question":"What is envelope encryption (KEK/DEK) and why use it?","answerFull":"Envelope encryption encrypts each data record with a fresh Data Encryption Key (DEK), then wraps (encrypts) that DEK with a long-lived Key Encryption Key (KEK) held in a KeyStore, KMS, or HSM. You store the wrapped DEK next to the ciphertext. It limits the blast radius of any single DEK, keeps the KEK off the data path, and — crucially — lets you rotate the KEK by re-wrapping DEKs cheaply instead of re-encrypting terabytes of data.","correct":"Envelope encryption encrypts each data record with a fresh Data Encryption Key (DEK), then wraps (encrypts) that DEK with a long-lived Key Encryption Key (KEK) held in a KeyStore, KMS, or HSM.","distractors":["Pick EC (ECDSA/ECDH on P-256) for new systems: a 256-bit EC key gives ~128-bit security equivalent to a 3072-bit RSA key, with far smaller keys/signatures and faster keygen and signing.","It's an attack on unauthenticated CBC where the server reveals whether decryption produced valid PKCS#7 padding, letting an attacker decrypt ciphertext byte-by-byte without the key.","It can block — on Linux it may map to a blocking entropy source, so on entropy-starved boxes like fresh VMs or containers key generation can stall at startup."],"distractorIds":["java/security_and_cryptography#76428b137890","java/security_and_cryptography#7b7a9d754646","java/security_and_cryptography#13f3301f1bc9"],"concepts":["hsm","keystore","kms","dek","kek","encrypts"]},{"id":"java/security_and_cryptography#13f3301f1bc9","section":"java","module":"java/security_and_cryptography","moduleName":"security and cryptography","sourceFile":"README.md","difficulty":"advanced","question":"Does SecureRandom.getInstanceStrong() block, and how does it differ from new SecureRandom()?","answerFull":"It can block — on Linux it may map to a blocking entropy source, so on entropy-starved boxes like fresh VMs or containers key generation can stall at startup. The strong source is selected by the securerandom.strongAlgorithms security property. new SecureRandom() uses the configured default (NativePRNG/DRBG), which is non-blocking and cryptographically strong for most uses. Use getInstanceStrong() for long-lived keys where you want the strongest source; use the default for per-request nonces/IVs.","correct":"It can block — on Linux it may map to a blocking entropy source, so on entropy-starved boxes like fresh VMs or containers key generation can stall at startup.","distractors":["It's an attack on unauthenticated CBC where the server reveals whether decryption produced valid PKCS#7 padding, letting an attacker decrypt ciphertext byte-by-byte without the key.","With envelope encryption you only re-wrap the DEKs, never the ciphertext: unwrap each stored DEK with the old KEK and re-wrap it with the new KEK, updating the wrapped-DEK column and a key-version tag.","A MAC uses a single shared secret key — both parties can compute and verify it, giving integrity and authenticity but *not* non-repudiation (either party could have made it)."],"distractorIds":["java/security_and_cryptography#7b7a9d754646","java/security_and_cryptography#3451494fce19","java/security_and_cryptography#94c81a15d700"],"concepts":["securerandom","starved","vms","stall","boxes","differ"],"questionMd":"Does `SecureRandom.getInstanceStrong()` block, and how does it differ from `new SecureRandom()`?","answerFullMd":"It can block — on Linux it may map to a blocking entropy source, so on entropy-starved boxes like fresh VMs or containers key generation can stall at startup. The strong source is selected by the `securerandom.strongAlgorithms` security property. `new SecureRandom()` uses the configured default (NativePRNG/DRBG), which is non-blocking and cryptographically strong for most uses. Use `getInstanceStrong()` for long-lived keys where you want the strongest source; use the default for per-request nonces/IVs."},{"id":"java/security_and_cryptography#43bf3cb6ee35","section":"java","module":"java/security_and_cryptography","moduleName":"security and cryptography","sourceFile":"README.md","difficulty":"advanced","question":"How does TLS certificate validation work in Java, and what's the difference between the TrustManager and hostname verification?","answerFull":"The TrustManager builds and validates the server's certificate chain up to a trusted CA root in the truststore (checking signatures, validity dates, and revocation if configured). Hostname verification is a *separate* step that checks the certificate's SAN matches the host you dialed — and a raw SSLSocket skips it unless you set SSLParameters.setEndpointIdentificationAlgorithm(\"HTTPS\"). HttpClient and HttpsURLConnection do both automatically; forget the hostname step on a raw socket and you've left a MITM hole even with a valid chain.","correct":"The TrustManager builds and validates the server's certificate chain up to a trusted CA root in the truststore (checking signatures, validity dates, and revocation if configured).","distractors":["It's an attack on unauthenticated CBC where the server reveals whether decryption produced valid PKCS#7 padding, letting an attacker decrypt ciphertext byte-by-byte without the key.","java.util.Random is a 48-bit linear congruential generator seeded from the clock, so its entire output stream is predictable from a handful of samples.","Pick EC (ECDSA/ECDH on P-256) for new systems: a 256-bit EC key gives ~128-bit security equivalent to a 3072-bit RSA key, with far smaller keys/signatures and faster keygen and signing."],"distractorIds":["java/security_and_cryptography#7b7a9d754646","java/security_and_cryptography#b76e79ac49e6","java/security_and_cryptography#76428b137890"],"concepts":["revocation","trustmanager","dates","truststore","validity","hostname"],"correctMd":"The `TrustManager` builds and validates the server's certificate chain up to a trusted CA root in the truststore (checking signatures, validity dates, and revocation if configured).","answerFullMd":"The `TrustManager` builds and validates the server's certificate chain up to a trusted CA root in the truststore (checking signatures, validity dates, and revocation if configured). Hostname verification is a *separate* step that checks the certificate's SAN matches the host you dialed — and a raw `SSLSocket` skips it unless you set `SSLParameters.setEndpointIdentificationAlgorithm(\"HTTPS\")`. `HttpClient` and `HttpsURLConnection` do both automatically; forget the hostname step on a raw socket and you've left a MITM hole even with a valid chain.","distractorsMd":[null,"`java.util.Random` is a 48-bit linear congruential generator seeded from the clock, so its entire output stream is predictable from a handful of samples.",null]},{"id":"java/security_and_cryptography#967aae5d24f0","section":"java","module":"java/security_and_cryptography","moduleName":"security and cryptography","sourceFile":"README.md","difficulty":"advanced","question":"What is mTLS and how do you configure it in Java?","answerFull":"Mutual TLS has both sides present certificates: the server authenticates to the client as usual, and the client also presents a certificate the server validates against its truststore. In Java you configure the client's SSLContext with a KeyManager (from a KeyStore holding the client's private key + cert) in addition to a TrustManager, and configure the server to require client auth (SSLParameters.setNeedClientAuth(true)). It's the standard for service-to-service auth where both endpoints must prove identity.","correct":"Mutual TLS has both sides present certificates: the server authenticates to the client as usual, and the client also presents a certificate the server validates against its truststore.","distractors":["It's an attack on unauthenticated CBC where the server reveals whether decryption produced valid PKCS#7 padding, letting an attacker decrypt ciphertext byte-by-byte without the key.","The tag (128-bit here) authenticates the ciphertext — decryption throws AEADBadTagException if a single bit was flipped, so you get tamper detection for free.","java.util.Random is a 48-bit linear congruential generator seeded from the clock, so its entire output stream is predictable from a handful of samples."],"distractorIds":["java/security_and_cryptography#7b7a9d754646","java/security_and_cryptography#e1bb01c3ca9b","java/security_and_cryptography#b76e79ac49e6"],"concepts":["usual","mtls","presents","truststore","certificates","sides"],"answerFullMd":"Mutual TLS has both sides present certificates: the server authenticates to the client as usual, and the client also presents a certificate the server validates against its truststore. In Java you configure the client's `SSLContext` with a `KeyManager` (from a KeyStore holding the client's private key + cert) in addition to a `TrustManager`, and configure the server to require client auth (`SSLParameters.setNeedClientAuth(true)`). It's the standard for service-to-service auth where both endpoints must prove identity.","distractorsMd":[null,"The tag (128-bit here) authenticates the ciphertext — decryption throws `AEADBadTagException` if a single bit was flipped, so you get tamper detection for free.","`java.util.Random` is a 48-bit linear congruential generator seeded from the clock, so its entire output stream is predictable from a handful of samples."]},{"id":"java/security_and_cryptography#a5c1051f9111","section":"java","module":"java/security_and_cryptography","moduleName":"security and cryptography","sourceFile":"README.md","difficulty":"advanced","question":"Why is RSA-OAEP preferred over RSA PKCS#1 v1.5 padding?","answerFull":"PKCS#1 v1.5 encryption padding is vulnerable to Bleichenbacher's adaptive chosen-ciphertext attack — a padding oracle over many queries can recover the plaintext without the private key. OAEP (Optimal Asymmetric Encryption Padding) uses a randomized, hash-based construction that is provably secure against that attack. In Java, request RSA/ECB/OAEPWithSHA-256AndMGF1Padding rather than RSA/ECB/PKCS1Padding for any new encryption.","correct":"PKCS#1 v1.5 encryption padding is vulnerable to Bleichenbacher's adaptive chosen-ciphertext attack — a padding oracle over many queries can recover the plaintext without the private key.","distractors":["Envelope encryption encrypts each data record with a fresh Data Encryption Key (DEK), then wraps (encrypts) that DEK with a long-lived Key Encryption Key (KEK) held in a KeyStore, KMS, or HSM.","Reusing a 96-bit GCM nonce under the same key is catastrophic — it leaks the XOR of the two plaintexts *and* the GCM authentication subkey H, which lets an attacker forge valid tags for arbitrary ciphertext.","RSA can only encrypt a payload smaller than its modulus (a 3072-bit key handles ~318 bytes with OAEP), so you can't encrypt 10 MB directly."],"distractorIds":["java/security_and_cryptography#70593d615312","java/security_and_cryptography#7cf0546a6d4f","java/security_and_cryptography#44d5e69c0edd"],"concepts":["bleichenbacher","oaep","pkcs","rsa","vulnerable","oracle"],"answerFullMd":"PKCS#1 v1.5 encryption padding is vulnerable to Bleichenbacher's adaptive chosen-ciphertext attack — a padding oracle over many queries can recover the plaintext without the private key. OAEP (Optimal Asymmetric Encryption Padding) uses a randomized, hash-based construction that is provably secure against that attack. In Java, request `RSA/ECB/OAEPWithSHA-256AndMGF1Padding` rather than `RSA/ECB/PKCS1Padding` for any new encryption."},{"id":"java/security_and_cryptography#04498d8f37c5","section":"java","module":"java/security_and_cryptography","moduleName":"security and cryptography","sourceFile":"README.md","difficulty":"advanced","question":"Why prefer PKCS12 over JKS for keystores?","answerFull":"PKCS12 is a standardized, interoperable format (RFC 7292) that can store secret keys and is the JDK default since Java 9, unlike the proprietary Java-only JKS. JKS has weaker integrity protection and cannot store SecretKey entries at all. PKCS12 files interoperate with OpenSSL and other tooling, whereas JKS is Java-only. Migrate legacy JKS stores with keytool -importkeystore -srcstoretype JKS -deststoretype PKCS12.","correct":"PKCS12 is a standardized, interoperable format (RFC 7292) that can store secret keys and is the JDK default since Java 9, unlike the proprietary Java-only JKS.","distractors":["Pick EC (ECDSA/ECDH on P-256) for new systems: a 256-bit EC key gives ~128-bit security equivalent to a 3072-bit RSA key, with far smaller keys/signatures and faster keygen and signing.","java.util.Random is a 48-bit linear congruential generator seeded from the clock, so its entire output stream is predictable from a handful of samples.","Choose AES-GCM by default — it provides confidentiality and integrity in one primitive at AES-NI speed with no padding (so no padding-oracle surface)."],"distractorIds":["java/security_and_cryptography#76428b137890","java/security_and_cryptography#b76e79ac49e6","java/security_and_cryptography#fbd28cc07a3c"],"concepts":["7292","interoperable","jks","pkcs12","proprietary","rfc"],"answerFullMd":"PKCS12 is a standardized, interoperable format (RFC 7292) that can store secret keys and is the JDK default since Java 9, unlike the proprietary Java-only JKS. JKS has weaker integrity protection and cannot store `SecretKey` entries at all. PKCS12 files interoperate with OpenSSL and other tooling, whereas JKS is Java-only. Migrate legacy JKS stores with `keytool -importkeystore -srcstoretype JKS -deststoretype PKCS12`.","distractorsMd":[null,"`java.util.Random` is a 48-bit linear congruential generator seeded from the clock, so its entire output stream is predictable from a handful of samples.",null]},{"id":"java/security_and_cryptography#e1bb01c3ca9b","section":"java","module":"java/security_and_cryptography","moduleName":"security and cryptography","sourceFile":"README.md","difficulty":"advanced","question":"What does the GCM authentication tag protect, and what's updateAAD for?","answerFull":"The tag (128-bit here) authenticates the ciphertext — decryption throws AEADBadTagException if a single bit was flipped, so you get tamper detection for free. updateAAD adds Additional Authenticated Data — bytes that are authenticated but *not* encrypted (e.g., a record ID, header, or key version) — so the same ciphertext can't be replayed in a different context. If the AAD on decrypt doesn't match the AAD on encrypt, the tag check fails just as if the ciphertext were tampered.","correct":"The tag (128-bit here) authenticates the ciphertext — decryption throws AEADBadTagException if a single bit was flipped, so you get tamper detection for free.","distractors":["With envelope encryption you only re-wrap the DEKs, never the ciphertext: unwrap each stored DEK with the old KEK and re-wrap it with the new KEK, updating the wrapped-DEK column and a key-version tag.","Pick EC (ECDSA/ECDH on P-256) for new systems: a 256-bit EC key gives ~128-bit security equivalent to a 3072-bit RSA key, with far smaller keys/signatures and faster keygen and signing.","PKCS#1 v1.5 encryption padding is vulnerable to Bleichenbacher's adaptive chosen-ciphertext attack — a padding oracle over many queries can recover the plaintext without the private key."],"distractorIds":["java/security_and_cryptography#3451494fce19","java/security_and_cryptography#76428b137890","java/security_and_cryptography#a5c1051f9111"],"concepts":["aeadbadtagexception","flipped","protect","tamper","decryption","gcm"],"questionMd":"What does the GCM authentication tag protect, and what's `updateAAD` for?","correctMd":"The tag (128-bit here) authenticates the ciphertext — decryption throws `AEADBadTagException` if a single bit was flipped, so you get tamper detection for free.","answerFullMd":"The tag (128-bit here) authenticates the ciphertext — decryption throws `AEADBadTagException` if a single bit was flipped, so you get tamper detection for free. `updateAAD` adds Additional Authenticated Data — bytes that are authenticated but *not* encrypted (e.g., a record ID, header, or key version) — so the same ciphertext can't be replayed in a different context. If the AAD on decrypt doesn't match the AAD on encrypt, the tag check fails just as if the ciphertext were tampered."},{"id":"java/security_and_cryptography#44d5e69c0edd","section":"java","module":"java/security_and_cryptography","moduleName":"security and cryptography","sourceFile":"README.md","difficulty":"advanced","question":"You need to encrypt 10 MB of data with a recipient's RSA public key. What's wrong with that, and what's the right approach?","answerFull":"RSA can only encrypt a payload smaller than its modulus (a 3072-bit key handles ~318 bytes with OAEP), so you can't encrypt 10 MB directly. The right approach is hybrid/envelope encryption: generate a random AES-256 DEK, encrypt the 10 MB with AES-GCM, then RSA-OAEP-wrap only the small DEK with the recipient's public key and ship both. This is exactly how TLS and PGP work — asymmetric crypto moves the symmetric key, symmetric crypto moves the bulk data.","correct":"RSA can only encrypt a payload smaller than its modulus (a 3072-bit key handles ~318 bytes with OAEP), so you can't encrypt 10 MB directly.","distractors":["Reusing a 96-bit GCM nonce under the same key is catastrophic — it leaks the XOR of the two plaintexts *and* the GCM authentication subkey H, which lets an attacker forge valid tags for arbitrary ciphertext.","A MAC uses a single shared secret key — both parties can compute and verify it, giving integrity and authenticity but *not* non-repudiation (either party could have made it).","java.util.Random is a 48-bit linear congruential generator seeded from the clock, so its entire output stream is predictable from a handful of samples."],"distractorIds":["java/security_and_cryptography#7cf0546a6d4f","java/security_and_cryptography#94c81a15d700","java/security_and_cryptography#b76e79ac49e6"],"concepts":["318","encrypt","modulus","oaep","recipient","3072"],"distractorsMd":[null,null,"`java.util.Random` is a 48-bit linear congruential generator seeded from the clock, so its entire output stream is predictable from a handful of samples."]},{"id":"java/security_and_cryptography#3451494fce19","section":"java","module":"java/security_and_cryptography","moduleName":"security and cryptography","sourceFile":"README.md","difficulty":"advanced","question":"How do you rotate a KEK without re-encrypting all your data?","answerFull":"With envelope encryption you only re-wrap the DEKs, never the ciphertext: unwrap each stored DEK with the old KEK and re-wrap it with the new KEK, updating the wrapped-DEK column and a key-version tag. The bulk ciphertext is untouched because it was encrypted under the per-record DEK, which didn't change. Keep the old KEK available (marked decrypt-only) until every row is re-wrapped, then retire it — this makes rotation an O(number of keys) operation instead of O(bytes of data).","correct":"With envelope encryption you only re-wrap the DEKs, never the ciphertext: unwrap each stored DEK with the old KEK and re-wrap it with the new KEK, updating the wrapped-DEK column and a key-version tag.","distractors":["It's an attack on unauthenticated CBC where the server reveals whether decryption produced valid PKCS#7 padding, letting an attacker decrypt ciphertext byte-by-byte without the key.","Envelope encryption encrypts each data record with a fresh Data Encryption Key (DEK), then wraps (encrypts) that DEK with a long-lived Key Encryption Key (KEK) held in a KeyStore, KMS, or HSM.","Because the naive SHA256(key || message) is vulnerable to length-extension attacks that let an attacker forge a digest for an extended message."],"distractorIds":["java/security_and_cryptography#7b7a9d754646","java/security_and_cryptography#70593d615312","java/security_and_cryptography#16bca1829586"],"concepts":["deks","rotate","unwrap","dek","kek","envelope"],"distractorsMd":[null,null,"Because the naive `SHA256(key || message)` is vulnerable to length-extension attacks that let an attacker forge a digest for an extended message."]},{"id":"java/strings_and_text#97208101f684","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"README.md","difficulty":"core","question":"Why is String immutable in Java, and what would break if it were mutable?","answerFull":"String is immutable so it can be safely shared across threads, used as a HashMap key, and placed in the string constant pool without copying. If String were mutable, a cached hashCode() would become stale after modification, map lookups would fail silently, and a class name passed to ClassLoader.loadClass() could be mutated mid-load to trigger privilege escalation — a well-known historical security concern. Immutability gives compile-time guarantees the JIT exploits via @Stable on the backing array.","correct":"String is immutable so it can be safely shared across threads, used as a HashMap key, and placed in the string constant pool without copying.","distractors":["Before Java 9, the compiler translated a + b + c into new StringBuilder().append(a).append(b).append(c).toString() — fixed bytecode.","Java 8: String holds char[] value (each char = 2 bytes, regardless of actual content).","MULTILINE ((?m)) makes ^ and $ match at every line boundary rather than only the string's ends; DOTALL ((?s)) makes ."],"distractorIds":["java/strings_and_text#6ed76c7456e5","java/strings_and_text#6b85c1663593","java/strings_and_text#41331adb8e72"],"concepts":["copying","break","safely","placed","hashmap","mutable"],"questionMd":"Why is `String` immutable in Java, and what would break if it were mutable?","correctMd":"`String` is immutable so it can be safely shared across threads, used as a `HashMap` key, and placed in the string constant pool without copying.","answerFullMd":"`String` is immutable so it can be safely shared across threads, used as a `HashMap` key, and placed in the string constant pool without copying. If `String` were mutable, a cached `hashCode()` would become stale after modification, map lookups would fail silently, and a class name passed to `ClassLoader.loadClass()` could be mutated mid-load to trigger privilege escalation — a well-known historical security concern. Immutability gives compile-time guarantees the JIT exploits via `@Stable` on the backing array.","distractorsMd":["Before Java 9, the compiler translated `a + b + c` into `new StringBuilder().append(a).append(b).append(c).toString()` — fixed bytecode.","Java 8: `String` holds `char[] value` (each `char` = 2 bytes, regardless of actual content).",null]},{"id":"java/strings_and_text#13fb844fb6d2","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"README.md","difficulty":"core","question":"What is the difference between new String(\"abc\") and \"abc\"?","answerFull":"\"abc\" is a compile-time literal; the JVM automatically interns it — the first time it is loaded, one object is created in the string table and all references to \"abc\" share it. new String(\"abc\") bypasses the table and allocates a fresh String object on the heap; == against the literal returns false even though .equals() returns true. The literal in the constructor argument is still interned, but the result of new String(...) is not. To force the heap object into the table, call .intern().","correct":"\"abc\" is a compile-time literal; the JVM automatically interns it — the first time it is loaded, one object is created in the string table and all references to \"abc\" share it.","distractors":["Java's matcher implements each node's match recursively, so matching consumes JVM stack frames proportional to match depth.","Java 8: String holds char[] value (each char = 2 bytes, regardless of actual content).","MULTILINE ((?m)) makes ^ and $ match at every line boundary rather than only the string's ends; DOTALL ((?s)) makes ."],"distractorIds":["java/strings_and_text#fc0d1f08025e","java/strings_and_text#6b85c1663593","java/strings_and_text#41331adb8e72"],"concepts":["interns","abc","difference","literal","loaded","references"],"questionMd":"What is the difference between `new String(\"abc\")` and `\"abc\"`?","correctMd":"`\"abc\"` is a compile-time literal; the JVM automatically interns it — the first time it is loaded, one object is created in the string table and all references to `\"abc\"` share it.","answerFullMd":"`\"abc\"` is a compile-time literal; the JVM automatically interns it — the first time it is loaded, one object is created in the string table and all references to `\"abc\"` share it. `new String(\"abc\")` bypasses the table and allocates a fresh `String` object on the heap; `==` against the literal returns `false` even though `.equals()` returns `true`. The literal in the constructor argument is still interned, but the result of `new String(...)` is not. To force the heap object into the table, call `.intern()`.","distractorsMd":["Java's matcher implements each node's `match` recursively, so matching consumes JVM stack frames proportional to match depth.","Java 8: `String` holds `char[] value` (each `char` = 2 bytes, regardless of actual content).",null]},{"id":"java/strings_and_text#6ed76c7456e5","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"README.md","difficulty":"core","question":"What changed in Java 9 regarding how String concatenation is compiled?","answerFull":"Before Java 9, the compiler translated a + b + c into new StringBuilder().append(a).append(b).append(c).toString() — fixed bytecode. In Java 9 (JEP 280), this became a single invokedynamic call-site bootstrapped by StringConcatFactory.makeConcatWithConstants(). The JVM selects the best strategy at link-time (MH_INLINE_COPY by default): it pre-allocates an exact-size byte[], copies all parts in, and wraps it without an intermediate StringBuilder. This is faster for fixed-arity expressions and produces smaller bytecode. However, a + inside a loop still emits a StringConcatFactory call per iteration — StringBuilder is still needed for loops.","correct":"Before Java 9, the compiler translated a + b + c into new StringBuilder().append(a).append(b).append(c).toString() — fixed bytecode.","distractors":["String.matches, String.split, and String.replaceAll all call Pattern.compile internally on every invocation, so using them in a loop recompiles the regex every iteration.","MULTILINE ((?m)) makes ^ and $ match at every line boundary rather than only the string's ends; DOTALL ((?s)) makes .","StringBuilder has no synchronisation — append() is not atomic: it checks capacity, possibly reallocates the buffer, then copies bytes."],"distractorIds":["java/strings_and_text#1e4a51e96f5f","java/strings_and_text#41331adb8e72","java/strings_and_text#4c81bc10615c"],"concepts":["translated","concatenation","stringbuilder","tostring","compiled","compiler"],"correctMd":"Before Java 9, the compiler translated `a + b + c` into `new StringBuilder().append(a).append(b).append(c).toString()` — fixed bytecode.","answerFullMd":"Before Java 9, the compiler translated `a + b + c` into `new StringBuilder().append(a).append(b).append(c).toString()` — fixed bytecode. In Java 9 (JEP 280), this became a single `invokedynamic` call-site bootstrapped by `StringConcatFactory.makeConcatWithConstants()`. The JVM selects the best strategy at link-time (`MH_INLINE_COPY` by default): it pre-allocates an exact-size `byte[]`, copies all parts in, and wraps it without an intermediate `StringBuilder`. This is faster for fixed-arity expressions and produces smaller bytecode. However, a `+` inside a loop still emits a `StringConcatFactory` call per iteration — `StringBuilder` is still needed for loops.","distractorsMd":["`String.matches`, `String.split`, and `String.replaceAll` all call `Pattern.compile` internally on every invocation, so using them in a loop recompiles the regex every iteration.",null,"`StringBuilder` has no synchronisation — `append()` is not atomic: it checks capacity, possibly reallocates the buffer, then copies bytes."]},{"id":"java/strings_and_text#20c0536888f9","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"README.md","difficulty":"core","question":"Explain Compact Strings (JEP 254). How do they save memory, and when are they NOT active?","answerFull":"Compact Strings (Java 9, JEP 254) store String content in a byte[] instead of char[]. When all code points fit in ISO-8859-1 (Latin-1), the coder byte is LATIN1 (0) and each character uses 1 byte. When any character requires UTF-16, coder is UTF16 (1) and each character uses 2 bytes. For typical English-language applications (logs, SQL, HTTP headers, JSON keys), ~90–95% of strings are LATIN1, halving their memory footprint compared to Java 8. Compact Strings are NOT active when: (a) -XX:-CompactStrings is set, (b) the string contains any character outside ISO-8859-1 (code point > 255), or (c) building on Java 8 or earlier.","correct":"Compact Strings (Java 9, JEP 254) store String content in a byte[] instead of char[].","distractors":["== compares object identity (reference equality) — use only when comparing interned strings or the exact same reference.","\"abc\" is a compile-time literal; the JVM automatically interns it — the first time it is loaded, one object is created in the string table and all references to \"abc\" share it.","String.matches, String.split, and String.replaceAll all call Pattern.compile internally on every invocation, so using them in a loop recompiles the regex every iteration."],"distractorIds":["java/strings_and_text#fbc282a9f395","java/strings_and_text#13fb844fb6d2","java/strings_and_text#1e4a51e96f5f"],"concepts":["254","explain","jep","char","save","strings"],"correctMd":"Compact Strings (Java 9, JEP 254) store `String` content in a `byte[]` instead of `char[]`.","answerFullMd":"Compact Strings (Java 9, JEP 254) store `String` content in a `byte[]` instead of `char[]`. When all code points fit in ISO-8859-1 (Latin-1), the `coder` byte is `LATIN1 (0)` and each character uses 1 byte. When any character requires UTF-16, `coder` is `UTF16 (1)` and each character uses 2 bytes. For typical English-language applications (logs, SQL, HTTP headers, JSON keys), ~90–95% of strings are LATIN1, halving their memory footprint compared to Java 8. Compact Strings are NOT active when: (a) `-XX:-CompactStrings` is set, (b) the string contains any character outside ISO-8859-1 (code point > 255), or (c) building on Java 8 or earlier.","distractorsMd":["`==` compares object identity (reference equality) — use only when comparing interned strings or the exact same reference.","`\"abc\"` is a compile-time literal; the JVM automatically interns it — the first time it is loaded, one object is created in the string table and all references to `\"abc\"` share it.","`String.matches`, `String.split`, and `String.replaceAll` all call `Pattern.compile` internally on every invocation, so using them in a loop recompiles the regex every iteration."]},{"id":"java/strings_and_text#6b85c1663593","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"README.md","difficulty":"intermediate","question":"Show the memory layout difference between String in Java 8 and Java 9+.","answerFull":"Java 8: String holds char[] value (each char = 2 bytes, regardless of actual content). \"hello\" backs to char[5] = 10 bytes. Java 9+: String holds byte[] value + byte coder. \"hello\" (pure ASCII): byte[5] = 5 bytes, coder = 0 (LATIN1). \"héllo\" (one non-ASCII): byte[10] = 10 bytes, coder = 1 (UTF16). The object header (~16 bytes) and hash / hashIsZero fields are present in both.","correct":"Java 8: String holds char[] value (each char = 2 bytes, regardless of actual content).","distractors":["MULTILINE ((?m)) makes ^ and $ match at every line boundary rather than only the string's ends; DOTALL ((?s)) makes .","Compact Strings (Java 9, JEP 254) store String content in a byte[] instead of char[].","\"abc\" is a compile-time literal; the JVM automatically interns it — the first time it is loaded, one object is created in the string table and all references to \"abc\" share it."],"distractorIds":["java/strings_and_text#41331adb8e72","java/strings_and_text#20c0536888f9","java/strings_and_text#13fb844fb6d2"],"concepts":["layout","char","difference","show","holds","regardless"],"questionMd":"Show the memory layout difference between `String` in Java 8 and Java 9+.","correctMd":"Java 8: `String` holds `char[] value` (each `char` = 2 bytes, regardless of actual content).","answerFullMd":"Java 8: `String` holds `char[] value` (each `char` = 2 bytes, regardless of actual content). `\"hello\"` backs to `char[5]` = 10 bytes. Java 9+: `String` holds `byte[] value` + `byte coder`. `\"hello\"` (pure ASCII): `byte[5]` = 5 bytes, `coder = 0 (LATIN1)`. `\"héllo\"` (one non-ASCII): `byte[10]` = 10 bytes, `coder = 1 (UTF16)`. The object header (~16 bytes) and `hash` / `hashIsZero` fields are present in both.","distractorsMd":[null,"Compact Strings (Java 9, JEP 254) store `String` content in a `byte[]` instead of `char[]`.","`\"abc\"` is a compile-time literal; the JVM automatically interns it — the first time it is loaded, one object is created in the string table and all references to `\"abc\"` share it."]},{"id":"java/strings_and_text#4c81bc10615c","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"README.md","difficulty":"intermediate","question":"Why is StringBuilder not thread-safe, and how do you handle multi-threaded string building?","answerFull":"StringBuilder has no synchronisation — append() is not atomic: it checks capacity, possibly reallocates the buffer, then copies bytes. Two threads calling append() concurrently can interleave these steps, producing garbled output or an ArrayIndexOutOfBoundsException. For multi-threaded building: (1) use StringBuffer (all methods synchronized, ~20–30% slower), (2) use a per-thread StringBuilder and combine results at the end (best performance), or (3) use a ConcurrentLinkedDeque<String> + single-threaded join step. In practice, option (2) via thread-local or stream-style aggregation is almost always preferred over StringBuffer.","correct":"StringBuilder has no synchronisation — append() is not atomic: it checks capacity, possibly reallocates the buffer, then copies bytes.","distractors":["They disable local backtracking: a possessive quantifier (a++) or atomic group ((?>a+)) matches greedily and then refuses to give characters back.","Before Java 9, the compiler translated a + b + c into new StringBuilder().append(a).append(b).append(c).toString() — fixed bytecode.","MULTILINE ((?m)) makes ^ and $ match at every line boundary rather than only the string's ends; DOTALL ((?s)) makes ."],"distractorIds":["java/strings_and_text#63254fd8fee9","java/strings_and_text#6ed76c7456e5","java/strings_and_text#41331adb8e72"],"concepts":["synchronisation","reallocates","threaded","stringbuilder","possibly","building"],"questionMd":"Why is `StringBuilder` not thread-safe, and how do you handle multi-threaded string building?","correctMd":"`StringBuilder` has no synchronisation — `append()` is not atomic: it checks capacity, possibly reallocates the buffer, then copies bytes.","answerFullMd":"`StringBuilder` has no synchronisation — `append()` is not atomic: it checks capacity, possibly reallocates the buffer, then copies bytes. Two threads calling `append()` concurrently can interleave these steps, producing garbled output or an `ArrayIndexOutOfBoundsException`. For multi-threaded building: (1) use `StringBuffer` (all methods synchronized, ~20–30% slower), (2) use a per-thread `StringBuilder` and combine results at the end (best performance), or (3) use a `ConcurrentLinkedDeque<String>` + single-threaded join step. In practice, option (2) via thread-local or stream-style aggregation is almost always preferred over `StringBuffer`.","distractorsMd":["They disable local backtracking: a possessive quantifier (`a++`) or atomic group (`(?>a+)`) matches greedily and then refuses to give characters back.","Before Java 9, the compiler translated `a + b + c` into `new StringBuilder().append(a).append(b).append(c).toString()` — fixed bytecode.",null]},{"id":"java/strings_and_text#27f253a64bd0","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"README.md","difficulty":"intermediate","question":"What does a text block look like after the compiler processes it, and how is incidental whitespace stripped?","answerFull":"Text blocks are a compile-time transformation (JEP 378). The compiler: (1) line-terminates consistently (CRLF/CR → LF); (2) strips \"incidental whitespace\" — defined as the longest common leading-whitespace prefix across all non-blank content lines and the closing-\"\"\" line; (3) strips trailing whitespace from each line (use \\s escape to preserve intentional trailing spaces); (4) interprets \\<newline> as a line continuation. The result is a regular String constant — text blocks have zero runtime overhead. Two text blocks with the same content after processing are the same compile-time constant and may be interned like any literal.","correct":"Text blocks are a compile-time transformation (JEP 378).","distractors":["Capture groups (...) record the text they matched, retrievable by index via group(n) (group 0 is the whole match); named groups (?<name>...) retrieve by name via group(\"name\").","Catastrophic backtracking is exponential-time matching caused by ambiguity in how quantifiers can split the input.","Before Java 9, the compiler translated a + b + c into new StringBuilder().append(a).append(b).append(c).toString() — fixed bytecode."],"distractorIds":["java/strings_and_text#33615f80a8a7","java/strings_and_text#7044a1da23d0","java/strings_and_text#6ed76c7456e5"],"concepts":["378","incidental","stripped","whitespace","jep","transformation"],"answerFullMd":"Text blocks are a compile-time transformation (JEP 378). The compiler: (1) line-terminates consistently (CRLF/CR → LF); (2) strips \"incidental whitespace\" — defined as the longest common leading-whitespace prefix across all non-blank content lines and the closing-`\"\"\"` line; (3) strips trailing whitespace from each line (use `\\s` escape to preserve intentional trailing spaces); (4) interprets `\\<newline>` as a line continuation. The result is a regular `String` constant — text blocks have zero runtime overhead. Two text blocks with the same content after processing are the same compile-time constant and may be interned like any literal.","distractorsMd":["Capture groups `(...)` record the text they matched, retrievable by index via `group(n)` (group 0 is the whole match); named groups `(?<name>...)` retrieve by name via `group(\"name\")`.",null,"Before Java 9, the compiler translated `a + b + c` into `new StringBuilder().append(a).append(b).append(c).toString()` — fixed bytecode."]},{"id":"java/strings_and_text#e153c11f6f60","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between String.chars() and String.codePoints(), and when does it matter?","answerFull":"chars() returns an IntStream of UTF-16 code units (Java char values, 16-bit). For code points in the Basic Multilingual Plane (U+0000–U+FFFF), one code unit equals one code point. For supplementary characters (e.g., emoji, CJK Extension B), one code point spans two char values (a surrogate pair, U+D800–U+DFFF). codePoints() returns an IntStream of actual Unicode code points — it always gives one int per logical character. Use codePoints() whenever the input may contain emoji, symbols from supplementary planes, or when implementing Unicode-correct string length, reversal, or iteration.","correct":"chars() returns an IntStream of UTF-16 code units (Java char values, 16-bit).","distractors":["Before Java 9, the compiler translated a + b + c into new StringBuilder().append(a).append(b).append(c).toString() — fixed bytecode.","String.matches, String.split, and String.replaceAll all call Pattern.compile internally on every invocation, so using them in a loop recompiles the regex every iteration.","Java 8: String holds char[] value (each char = 2 bytes, regardless of actual content)."],"distractorIds":["java/strings_and_text#6ed76c7456e5","java/strings_and_text#1e4a51e96f5f","java/strings_and_text#6b85c1663593"],"concepts":["chars","intstream","utf","matter","char","difference"],"questionMd":"What is the difference between `String.chars()` and `String.codePoints()`, and when does it matter?","correctMd":"`chars()` returns an `IntStream` of UTF-16 code units (Java `char` values, 16-bit).","answerFullMd":"`chars()` returns an `IntStream` of UTF-16 code units (Java `char` values, 16-bit). For code points in the Basic Multilingual Plane (U+0000–U+FFFF), one code unit equals one code point. For supplementary characters (e.g., emoji, CJK Extension B), one code point spans two `char` values (a surrogate pair, U+D800–U+DFFF). `codePoints()` returns an `IntStream` of actual Unicode code points — it always gives one `int` per logical character. Use `codePoints()` whenever the input may contain emoji, symbols from supplementary planes, or when implementing Unicode-correct string length, reversal, or iteration.","distractorsMd":["Before Java 9, the compiler translated `a + b + c` into `new StringBuilder().append(a).append(b).append(c).toString()` — fixed bytecode.","`String.matches`, `String.split`, and `String.replaceAll` all call `Pattern.compile` internally on every invocation, so using them in a loop recompiles the regex every iteration.","Java 8: `String` holds `char[] value` (each `char` = 2 bytes, regardless of actual content)."]},{"id":"java/strings_and_text#fbc282a9f395","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"README.md","difficulty":"advanced","question":"What are the four String comparison methods and their correct use cases?","answerFull":"== compares object identity (reference equality) — use only when comparing interned strings or the exact same reference. .equals() compares content character by character — use for all user-facing equality. .equalsIgnoreCase() normalises both sides via Character.toLowerCase() with the default locale — avoid for locale-sensitive comparison (\"i\".equalsIgnoreCase(\"I\") is false in Turkish locale). .compareTo() / .compareToIgnoreCase() gives a lexicographic ordering — use for sorting. For locale-correct comparison (sorting names, user input), use java.text.Collator which honours locale-specific ordering rules (e.g., accented characters in French, ß in German).","correct":"== compares object identity (reference equality) — use only when comparing interned strings or the exact same reference.","distractors":["MULTILINE ((?m)) makes ^ and $ match at every line boundary rather than only the string's ends; DOTALL ((?s)) makes .","(1) Class loading: class and package names are interned during class loading.","In the replacement argument, $ introduces a group reference ($1) and \\ escapes, so a literal $ or \\ in the output must be written as \\\\$ and \\\\\\\\."],"distractorIds":["java/strings_and_text#41331adb8e72","java/strings_and_text#1e31d055d1cd","java/strings_and_text#beba697f2064"],"concepts":["interned","equality","comparing","comparison","strings","compares"],"correctMd":"`==` compares object identity (reference equality) — use only when comparing interned strings or the exact same reference.","answerFullMd":"`==` compares object identity (reference equality) — use only when comparing interned strings or the exact same reference. `.equals()` compares content character by character — use for all user-facing equality. `.equalsIgnoreCase()` normalises both sides via `Character.toLowerCase()` with the default locale — avoid for locale-sensitive comparison (`\"i\".equalsIgnoreCase(\"I\")` is `false` in Turkish locale). `.compareTo()` / `.compareToIgnoreCase()` gives a lexicographic ordering — use for sorting. For locale-correct comparison (sorting names, user input), use `java.text.Collator` which honours locale-specific ordering rules (e.g., accented characters in French, ß in German).","distractorsMd":[null,"(1) **Class loading**: class and package names are interned during class loading.","In the replacement argument, `$` introduces a group reference (`$1`) and `\\` escapes, so a literal `$` or `\\` in the output must be written as `\\\\$` and `\\\\\\\\`."]},{"id":"java/strings_and_text#741426edfde2","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"README.md","difficulty":"advanced","question":"How does String.hashCode() work, and why can a hash code be 0 for a non-empty string?","answerFull":"hashCode() computes s[0]*31^(n-1) + s[1]*31^(n-2) + ... + s[n-1] modulo the int overflow boundary (31 is a small prime that reduces collisions). The result is cached in the hash field after first computation. Java 8 has a race (two threads compute it simultaneously) — both compute the same value, making the benign data race safe by the JMM's guarantee that int writes are atomic and the computation is deterministic. In Java 9+, a second flag hashIsZero is added: if the hash computes to exactly 0, it is stored as 0 and hashIsZero = true prevents re-computation on every call. Practical implication: never rely on hash == 0 to mean \"not computed.\"","correct":"hashCode() computes s[0]*31^(n-1) + s[1]*31^(n-2) + ...","distractors":["Java 8: String holds char[] value (each char = 2 bytes, regardless of actual content).","String.matches, String.split, and String.replaceAll all call Pattern.compile internally on every invocation, so using them in a loop recompiles the regex every iteration.","Compact Strings (Java 9, JEP 254) store String content in a byte[] instead of char[]."],"distractorIds":["java/strings_and_text#6b85c1663593","java/strings_and_text#1e4a51e96f5f","java/strings_and_text#20c0536888f9"],"concepts":["hashcode","empty","computes","hash","string","work"],"questionMd":"How does `String.hashCode()` work, and why can a hash code be 0 for a non-empty string?","answerFullMd":"`hashCode()` computes `s[0]*31^(n-1) + s[1]*31^(n-2) + ... + s[n-1]` modulo the `int` overflow boundary (31 is a small prime that reduces collisions). The result is cached in the `hash` field after first computation. Java 8 has a race (two threads compute it simultaneously) — both compute the same value, making the benign data race safe by the JMM's guarantee that `int` writes are atomic and the computation is deterministic. In Java 9+, a second flag `hashIsZero` is added: if the hash computes to exactly 0, it is stored as 0 and `hashIsZero = true` prevents re-computation on every call. Practical implication: never rely on `hash == 0` to mean \"not computed.\"","distractorsMd":["Java 8: `String` holds `char[] value` (each `char` = 2 bytes, regardless of actual content).","`String.matches`, `String.split`, and `String.replaceAll` all call `Pattern.compile` internally on every invocation, so using them in a loop recompiles the regex every iteration.","Compact Strings (Java 9, JEP 254) store `String` content in a `byte[]` instead of `char[]`."]},{"id":"java/strings_and_text#928943501b38","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"README.md","difficulty":"advanced","question":"How does String.strip() differ from String.trim(), and why should you prefer strip() in Java 11+?","answerFull":"trim() removes leading and trailing characters with code point ≤ U+0020 (space and ASCII control characters). strip() (Java 11, JEP 340 Unicode) uses Character.isWhitespace(), which additionally removes Unicode whitespace like U+00A0 (no-break space), U+2009 (thin space), U+3000 (ideographic space), and others. Modern web and database content often includes these Unicode whitespace characters from copy-paste, rich-text editors, or non-English sources. trim() on such a string returns a string that still looks padded to the user. stripLeading() and stripTrailing() are available for asymmetric stripping. Prefer strip() for all new code on Java 11+.","correct":"trim() removes leading and trailing characters with code point ≤ U+0020 (space and ASCII control characters).","distractors":["Compact Strings (Java 9, JEP 254) store String content in a byte[] instead of char[].","MULTILINE ((?m)) makes ^ and $ match at every line boundary rather than only the string's ends; DOTALL ((?s)) makes .","\"abc\" is a compile-time literal; the JVM automatically interns it — the first time it is loaded, one object is created in the string table and all references to \"abc\" share it."],"distractorIds":["java/strings_and_text#20c0536888f9","java/strings_and_text#41331adb8e72","java/strings_and_text#13fb844fb6d2"],"concepts":["0020","ascii","strip","trailing","trim","leading"],"questionMd":"How does `String.strip()` differ from `String.trim()`, and why should you prefer `strip()` in Java 11+?","correctMd":"`trim()` removes leading and trailing characters with code point ≤ U+0020 (space and ASCII control characters).","answerFullMd":"`trim()` removes leading and trailing characters with code point ≤ U+0020 (space and ASCII control characters). `strip()` (Java 11, JEP 340 Unicode) uses `Character.isWhitespace()`, which additionally removes Unicode whitespace like U+00A0 (no-break space), U+2009 (thin space), U+3000 (ideographic space), and others. Modern web and database content often includes these Unicode whitespace characters from copy-paste, rich-text editors, or non-English sources. `trim()` on such a string returns a string that still looks padded to the user. `stripLeading()` and `stripTrailing()` are available for asymmetric stripping. Prefer `strip()` for all new code on Java 11+.","distractorsMd":["Compact Strings (Java 9, JEP 254) store `String` content in a `byte[]` instead of `char[]`.",null,"`\"abc\"` is a compile-time literal; the JVM automatically interns it — the first time it is loaded, one object is created in the string table and all references to `\"abc\"` share it."]},{"id":"java/strings_and_text#1e31d055d1cd","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"README.md","difficulty":"advanced","question":"Describe three places in the JDK where String interning or constant pooling provides a concrete performance benefit.","answerFull":"(1) Class loading: class and package names are interned during class loading. Class.getName() returns an interned string, allowing the class loader's internal hash maps to use == for key comparison. (2) Boolean.toString(): returns \"true\" or \"false\", both compile-time literals that are already interned — no allocation occurs. Similarly Integer.toString(0) through Integer.toString(9) use cached constants. (3) Enum name constants: every enum's .name() field is initialised from a string literal interned at compile time. Two references to Status.ACTIVE.name() return the same String object, so == comparisons in switch expressions on enum names are safe (though .equals() is still safer for external input).","correct":"(1) Class loading: class and package names are interned during class loading.","distractors":["Compact Strings (Java 9, JEP 254) store String content in a byte[] instead of char[].","String.matches, String.split, and String.replaceAll all call Pattern.compile internally on every invocation, so using them in a loop recompiles the regex every iteration.","\"abc\" is a compile-time literal; the JVM automatically interns it — the first time it is loaded, one object is created in the string table and all references to \"abc\" share it."],"distractorIds":["java/strings_and_text#20c0536888f9","java/strings_and_text#1e4a51e96f5f","java/strings_and_text#13fb844fb6d2"],"concepts":["describe","interning","interned","benefit","places","pooling"],"questionMd":"Describe three places in the JDK where `String` interning or constant pooling provides a concrete performance benefit.","correctMd":"(1) **Class loading**: class and package names are interned during class loading.","answerFullMd":"(1) **Class loading**: class and package names are interned during class loading. `Class.getName()` returns an interned string, allowing the class loader's internal hash maps to use `==` for key comparison. (2) **`Boolean.toString()`**: returns `\"true\"` or `\"false\"`, both compile-time literals that are already interned — no allocation occurs. Similarly `Integer.toString(0)` through `Integer.toString(9)` use cached constants. (3) **Enum name constants**: every enum's `.name()` field is initialised from a string literal interned at compile time. Two references to `Status.ACTIVE.name()` return the same `String` object, so `==` comparisons in switch expressions on enum names are safe (though `.equals()` is still safer for external input).","distractorsMd":["Compact Strings (Java 9, JEP 254) store `String` content in a `byte[]` instead of `char[]`.","`String.matches`, `String.split`, and `String.replaceAll` all call `Pattern.compile` internally on every invocation, so using them in a loop recompiles the regex every iteration.","`\"abc\"` is a compile-time literal; the JVM automatically interns it — the first time it is loaded, one object is created in the string table and all references to `\"abc\"` share it."]},{"id":"java/strings_and_text#dec3bcacf841","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"regex_engine_and_redos.md","difficulty":"core","question":"Why does Java's regex engine hang on some patterns when Go's regexp or grep never do?","answerFull":"Java uses a backtracking NFA engine, whereas Go's RE2 and grep -E use a DFA that guarantees linear time. Backtracking explores every way a set of nested quantifiers can partition the input, which is exponential for patterns like (a+)+$. A DFA cannot express backreferences or lookaround but never backtracks, so it is immune to catastrophic blowup. The practical takeaway: with java.util.regex, runtime depends on the pattern *shape*, not just input length.","correct":"Java uses a backtracking NFA engine, whereas Go's RE2 and grep -E use a DFA that guarantees linear time.","distractors":["RE2/J (com.google.re2j) is a pure-Java port of Google's RE2 that runs in guaranteed linear time using an automaton that never backtracks, making it immune to ReDoS.","Compact Strings (Java 9, JEP 254) store String content in a byte[] instead of char[].","They disable local backtracking: a possessive quantifier (a++) or atomic group ((?>a+)) matches greedily and then refuses to give characters back."],"distractorIds":["java/strings_and_text#104c1ede9f42","java/strings_and_text#20c0536888f9","java/strings_and_text#63254fd8fee9"],"concepts":["dfa","grep","nfa","re2","hang","regex"],"questionMd":"Why does Java's regex engine hang on some patterns when Go's `regexp` or `grep` never do?","correctMd":"Java uses a backtracking NFA engine, whereas Go's RE2 and `grep -E` use a DFA that guarantees linear time.","answerFullMd":"Java uses a backtracking NFA engine, whereas Go's RE2 and `grep -E` use a DFA that guarantees linear time. Backtracking explores every way a set of nested quantifiers can partition the input, which is exponential for patterns like `(a+)+$`. A DFA cannot express backreferences or lookaround but never backtracks, so it is immune to catastrophic blowup. The practical takeaway: with `java.util.regex`, runtime depends on the pattern *shape*, not just input length.","distractorsMd":["RE2/J (`com.google.re2j`) is a pure-Java port of Google's RE2 that runs in guaranteed linear time using an automaton that never backtracks, making it immune to ReDoS.","Compact Strings (Java 9, JEP 254) store `String` content in a `byte[]` instead of `char[]`.","They disable local backtracking: a possessive quantifier (`a++`) or atomic group (`(?>a+)`) matches greedily and then refuses to give characters back."]},{"id":"java/strings_and_text#7044a1da23d0","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"regex_engine_and_redos.md","difficulty":"core","question":"What is catastrophic backtracking, and which pattern shapes cause it?","answerFull":"Catastrophic backtracking is exponential-time matching caused by ambiguity in how quantifiers can split the input. The three canonical shapes are nested quantifiers ((a+)+, (a*)*), a quantifier over overlapping alternation ((a|a)*, (a|ab)*), and adjacent flexible quantifiers (\\d+\\d+, .*.*). Each gives the engine many equivalent ways to match a prefix; when the overall match ultimately fails, it tries all 2ⁿ⁻¹ of them. Anchoring, possessive quantifiers, or de-nesting eliminate the ambiguity.","correct":"Catastrophic backtracking is exponential-time matching caused by ambiguity in how quantifiers can split the input.","distractors":["Lookahead (?=...) / (?!...) asserts what follows the current position; lookbehind (?<=...) / (?<!...) asserts what precedes it — neither consumes input.","region(start, end) restricts matching to a sub-range of the input without allocating a substring, so anchors and find() operate only within that window.","Regex injection happens when user input is concatenated into a pattern (Pattern.compile(\"id=\" + userInput)), letting the user inject metacharacters — including ReDoS-triggering nested quantifiers."],"distractorIds":["java/strings_and_text#7cccdd9bfd20","java/strings_and_text#f0cfe69752b0","java/strings_and_text#dddc70e29c95"],"concepts":["ambiguity","quantifiers","shapes","catastrophic","caused","backtracking"],"answerFullMd":"Catastrophic backtracking is exponential-time matching caused by ambiguity in how quantifiers can split the input. The three canonical shapes are nested quantifiers (`(a+)+`, `(a*)*`), a quantifier over overlapping alternation (`(a|a)*`, `(a|ab)*`), and adjacent flexible quantifiers (`\\d+\\d+`, `.*.*`). Each gives the engine many equivalent ways to match a prefix; when the overall match ultimately fails, it tries all 2ⁿ⁻¹ of them. Anchoring, possessive quantifiers, or de-nesting eliminate the ambiguity.","distractorsMd":["Lookahead `(?=...)` / `(?!...)` asserts what follows the current position; lookbehind `(?<=...)` / `(?<!...)` asserts what precedes it — neither consumes input.","`region(start, end)` restricts matching to a sub-range of the input without allocating a substring, so anchors and `find()` operate only within that window.","Regex injection happens when user input is concatenated into a pattern (`Pattern.compile(\"id=\" + userInput)`), letting the user inject metacharacters — including ReDoS-triggering nested quantifiers."]},{"id":"java/strings_and_text#63254fd8fee9","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"regex_engine_and_redos.md","difficulty":"core","question":"How do possessive quantifiers and atomic groups prevent ReDoS?","answerFull":"They disable local backtracking: a possessive quantifier (a++) or atomic group ((?>a+)) matches greedily and then refuses to give characters back. Because there is exactly one way to consume the run, a failing tail cannot force the engine to re-split it, collapsing the 2ⁿ search to O(n). The tradeoff is that possessive matching can *fail* where a greedy version would have succeeded, so you use it only where the sub-expression should never reconsider — which is most validation patterns.","correct":"They disable local backtracking: a possessive quantifier (a++) or atomic group ((?>a+)) matches greedily and then refuses to give characters back.","distractors":["Capture groups (...) record the text they matched, retrievable by index via group(n) (group 0 is the whole match); named groups (?<name>...) retrieve by name via group(\"name\").","Catastrophic backtracking is exponential-time matching caused by ambiguity in how quantifiers can split the input.","Regex injection happens when user input is concatenated into a pattern (Pattern.compile(\"id=\" + userInput)), letting the user inject metacharacters — including ReDoS-triggering nested quantifiers."],"distractorIds":["java/strings_and_text#33615f80a8a7","java/strings_and_text#7044a1da23d0","java/strings_and_text#dddc70e29c95"],"concepts":["quantifier","refuses","possessive","redos","quantifiers","disable"],"correctMd":"They disable local backtracking: a possessive quantifier (`a++`) or atomic group (`(?>a+)`) matches greedily and then refuses to give characters back.","answerFullMd":"They disable local backtracking: a possessive quantifier (`a++`) or atomic group (`(?>a+)`) matches greedily and then refuses to give characters back. Because there is exactly one way to consume the run, a failing tail cannot force the engine to re-split it, collapsing the 2ⁿ search to O(n). The tradeoff is that possessive matching can *fail* where a greedy version would have succeeded, so you use it only where the sub-expression should never reconsider — which is most validation patterns.","distractorsMd":["Capture groups `(...)` record the text they matched, retrievable by index via `group(n)` (group 0 is the whole match); named groups `(?<name>...)` retrieve by name via `group(\"name\")`.",null,"Regex injection happens when user input is concatenated into a pattern (`Pattern.compile(\"id=\" + userInput)`), letting the user inject metacharacters — including ReDoS-triggering nested quantifiers."]},{"id":"java/strings_and_text#54fc8bef7cf5","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"regex_engine_and_redos.md","difficulty":"core","question":"Is Matcher thread-safe? Is Pattern?","answerFull":"Pattern is immutable and fully thread-safe, but Matcher is not — it holds mutable state (current position, region bounds, captured groups). Sharing one Matcher across threads produces corrupted results or exceptions. The correct pattern is a static final Pattern shared everywhere and a fresh pattern.matcher(input) per call, or a ThreadLocal<Matcher> in extremely hot paths.","correct":"Pattern is immutable and fully thread-safe, but Matcher is not — it holds mutable state (current position, region bounds, captured groups).","distractors":["Java 8: String holds char[] value (each char = 2 bytes, regardless of actual content).","Lookahead (?=...) / (?!...) asserts what follows the current position; lookbehind (?<=...) / (?<!...) asserts what precedes it — neither consumes input.","Java's matcher implements each node's match recursively, so matching consumes JVM stack frames proportional to match depth."],"distractorIds":["java/strings_and_text#6b85c1663593","java/strings_and_text#7cccdd9bfd20","java/strings_and_text#fc0d1f08025e"],"concepts":["captured","matcher","bounds","mutable","groups","region"],"questionMd":"Is `Matcher` thread-safe? Is `Pattern`?","correctMd":"`Pattern` is immutable and fully thread-safe, but `Matcher` is not — it holds mutable state (current position, region bounds, captured groups).","answerFullMd":"`Pattern` is immutable and fully thread-safe, but `Matcher` is not — it holds mutable state (current position, region bounds, captured groups). Sharing one `Matcher` across threads produces corrupted results or exceptions. The correct pattern is a `static final Pattern` shared everywhere and a fresh `pattern.matcher(input)` per call, or a `ThreadLocal<Matcher>` in extremely hot paths.","distractorsMd":["Java 8: `String` holds `char[] value` (each `char` = 2 bytes, regardless of actual content).","Lookahead `(?=...)` / `(?!...)` asserts what follows the current position; lookbehind `(?<=...)` / `(?<!...)` asserts what precedes it — neither consumes input.","Java's matcher implements each node's `match` recursively, so matching consumes JVM stack frames proportional to match depth."]},{"id":"java/strings_and_text#b075406eb294","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"regex_engine_and_redos.md","difficulty":"core","question":"What is the difference between matches(), find(), and lookingAt()?","answerFull":"matches() requires the entire input to match (anchored at both ends), lookingAt() requires a match at the start but not to the end, and find() searches for a match anywhere and advances on each call. Validators almost always want matches(); scanners want repeated find(). A frequent bug is using find() for validation, which accepts input like \"abc123xyz\" because \\d{3} matches somewhere inside it.","correct":"matches() requires the entire input to match (anchored at both ends), lookingAt() requires a match at the start but not to the end, and find() searches for a match anywhere and advances on each call.","distractors":["Wrap the input in a custom CharSequence whose charAt throws when the thread is interrupted or a deadline passes, then run the match on a worker thread that a watchdog interrupts.","Java's matcher implements each node's match recursively, so matching consumes JVM stack frames proportional to match depth.","MULTILINE ((?m)) makes ^ and $ match at every line boundary rather than only the string's ends; DOTALL ((?s)) makes ."],"distractorIds":["java/strings_and_text#b5f56acdb7a1","java/strings_and_text#fc0d1f08025e","java/strings_and_text#41331adb8e72"],"concepts":["anchored","lookingat","advances","anywhere","searches","difference"],"questionMd":"What is the difference between `matches()`, `find()`, and `lookingAt()`?","correctMd":"`matches()` requires the entire input to match (anchored at both ends), `lookingAt()` requires a match at the start but not to the end, and `find()` searches for a match anywhere and advances on each call.","answerFullMd":"`matches()` requires the entire input to match (anchored at both ends), `lookingAt()` requires a match at the start but not to the end, and `find()` searches for a match anywhere and advances on each call. Validators almost always want `matches()`; scanners want repeated `find()`. A frequent bug is using `find()` for validation, which accepts input like `\"abc123xyz\"` because `\\d{3}` matches somewhere inside it.","distractorsMd":["Wrap the input in a custom `CharSequence` whose `charAt` throws when the thread is interrupted or a deadline passes, then run the match on a worker thread that a watchdog interrupts.","Java's matcher implements each node's `match` recursively, so matching consumes JVM stack frames proportional to match depth.",null]},{"id":"java/strings_and_text#7f044043be84","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"regex_engine_and_redos.md","difficulty":"intermediate","question":"What is the difference between greedy, lazy, and possessive quantifiers?","answerFull":"Greedy (a+) matches as much as possible then backtracks; lazy (a+?) matches as little as possible then takes more; possessive (a++) matches as much as possible and never gives back. Greedy vs lazy changes *what* text is matched; possessive changes *whether backtracking can happen at all*. A common misconception is that lazy quantifiers fix performance — they still backtrack, just in the other direction; only possessive/atomic cut the search tree.","correct":"Greedy (a+) matches as much as possible then backtracks; lazy (a+?) matches as little as possible then takes more; possessive (a++) matches as much as possible and never gives back.","distractors":["RE2/J (com.google.re2j) is a pure-Java port of Google's RE2 that runs in guaranteed linear time using an automaton that never backtracks, making it immune to ReDoS.","They disable local backtracking: a possessive quantifier (a++) or atomic group ((?>a+)) matches greedily and then refuses to give characters back.","Catastrophic backtracking is exponential-time matching caused by ambiguity in how quantifiers can split the input."],"distractorIds":["java/strings_and_text#104c1ede9f42","java/strings_and_text#63254fd8fee9","java/strings_and_text#7044a1da23d0"],"concepts":["backtracks","possessive","quantifiers","little","difference","greedy"],"correctMd":"Greedy (`a+`) matches as much as possible then backtracks; lazy (`a+?`) matches as little as possible then takes more; possessive (`a++`) matches as much as possible and never gives back.","answerFullMd":"Greedy (`a+`) matches as much as possible then backtracks; lazy (`a+?`) matches as little as possible then takes more; possessive (`a++`) matches as much as possible and never gives back. Greedy vs lazy changes *what* text is matched; possessive changes *whether backtracking can happen at all*. A common misconception is that lazy quantifiers fix performance — they still backtrack, just in the other direction; only possessive/atomic cut the search tree.","distractorsMd":["RE2/J (`com.google.re2j`) is a pure-Java port of Google's RE2 that runs in guaranteed linear time using an automaton that never backtracks, making it immune to ReDoS.","They disable local backtracking: a possessive quantifier (`a++`) or atomic group (`(?>a+)`) matches greedily and then refuses to give characters back.",null]},{"id":"java/strings_and_text#b5f56acdb7a1","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"regex_engine_and_redos.md","difficulty":"intermediate","question":"java.util.regex has no timeout — how do you bound a match's runtime?","answerFull":"Wrap the input in a custom CharSequence whose charAt throws when the thread is interrupted or a deadline passes, then run the match on a worker thread that a watchdog interrupts. The engine calls charAt in its innermost loop, so the exception fires mid-match and unwinds the backtracking. Alternatively, cap input length before matching, or switch to RE2/J which cannot blow up in the first place.","correct":"Wrap the input in a custom CharSequence whose charAt throws when the thread is interrupted or a deadline passes, then run the match on a worker thread that a watchdog interrupts.","distractors":["matches() requires the entire input to match (anchored at both ends), lookingAt() requires a match at the start but not to the end, and find() searches for a match anywhere and advances on each call.","Java's matcher implements each node's match recursively, so matching consumes JVM stack frames proportional to match depth.","String.matches, String.split, and String.replaceAll all call Pattern.compile internally on every invocation, so using them in a loop recompiles the regex every iteration."],"distractorIds":["java/strings_and_text#b075406eb294","java/strings_and_text#fc0d1f08025e","java/strings_and_text#1e4a51e96f5f"],"concepts":["charat","charsequence","watchdog","interrupted","interrupts","util"],"questionMd":"`java.util.regex` has no timeout — how do you bound a match's runtime?","correctMd":"Wrap the input in a custom `CharSequence` whose `charAt` throws when the thread is interrupted or a deadline passes, then run the match on a worker thread that a watchdog interrupts.","answerFullMd":"Wrap the input in a custom `CharSequence` whose `charAt` throws when the thread is interrupted or a deadline passes, then run the match on a worker thread that a watchdog interrupts. The engine calls `charAt` in its innermost loop, so the exception fires mid-match and unwinds the backtracking. Alternatively, cap input length before matching, or switch to RE2/J which cannot blow up in the first place.","distractorsMd":["`matches()` requires the entire input to match (anchored at both ends), `lookingAt()` requires a match at the start but not to the end, and `find()` searches for a match anywhere and advances on each call.","Java's matcher implements each node's `match` recursively, so matching consumes JVM stack frames proportional to match depth.","`String.matches`, `String.split`, and `String.replaceAll` all call `Pattern.compile` internally on every invocation, so using them in a loop recompiles the regex every iteration."]},{"id":"java/strings_and_text#1e4a51e96f5f","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"regex_engine_and_redos.md","difficulty":"intermediate","question":"Why is String.matches(\"...\") a performance trap?","answerFull":"String.matches, String.split, and String.replaceAll all call Pattern.compile internally on every invocation, so using them in a loop recompiles the regex every iteration. Compilation (parsing the pattern into a Node graph) is the expensive step; matching is comparatively cheap. Fix: hoist the pattern into a static final Pattern field and reuse matcher().","correct":"String.matches, String.split, and String.replaceAll all call Pattern.compile internally on every invocation, so using them in a loop recompiles the regex every iteration.","distractors":["matches() requires the entire input to match (anchored at both ends), lookingAt() requires a match at the start but not to the end, and find() searches for a match anywhere and advances on each call.","MULTILINE ((?m)) makes ^ and $ match at every line boundary rather than only the string's ends; DOTALL ((?s)) makes .","\"abc\" is a compile-time literal; the JVM automatically interns it — the first time it is loaded, one object is created in the string table and all references to \"abc\" share it."],"distractorIds":["java/strings_and_text#b075406eb294","java/strings_and_text#41331adb8e72","java/strings_and_text#13fb844fb6d2"],"concepts":["recompiles","replaceall","trap","regex","internally","invocation"],"questionMd":"Why is `String.matches(\"...\")` a performance trap?","correctMd":"`String.matches`, `String.split`, and `String.replaceAll` all call `Pattern.compile` internally on every invocation, so using them in a loop recompiles the regex every iteration.","answerFullMd":"`String.matches`, `String.split`, and `String.replaceAll` all call `Pattern.compile` internally on every invocation, so using them in a loop recompiles the regex every iteration. Compilation (parsing the pattern into a `Node` graph) is the expensive step; matching is comparatively cheap. Fix: hoist the pattern into a `static final Pattern` field and reuse `matcher()`.","distractorsMd":["`matches()` requires the entire input to match (anchored at both ends), `lookingAt()` requires a match at the start but not to the end, and `find()` searches for a match anywhere and advances on each call.",null,"`\"abc\"` is a compile-time literal; the JVM automatically interns it — the first time it is loaded, one object is created in the string table and all references to `\"abc\"` share it."]},{"id":"java/strings_and_text#33615f80a8a7","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"regex_engine_and_redos.md","difficulty":"intermediate","question":"What are capture groups, named groups, and backreferences?","answerFull":"Capture groups (...) record the text they matched, retrievable by index via group(n) (group 0 is the whole match); named groups (?<name>...) retrieve by name via group(\"name\"). A backreference \\1 (or \\k<name>) requires the same captured text to appear again — for example \\b(\\w+)\\s+\\1\\b finds a doubled word. Backreferences are precisely the feature that makes a linear DFA engine impossible, forcing the backtracking design.","correct":"Capture groups (...) record the text they matched, retrievable by index via group(n) (group 0 is the whole match); named groups (?<name>...) retrieve by name via group(\"name\").","distractors":["matches() requires the entire input to match (anchored at both ends), lookingAt() requires a match at the start but not to the end, and find() searches for a match anywhere and advances on each call.","Java's matcher implements each node's match recursively, so matching consumes JVM stack frames proportional to match depth.","MULTILINE ((?m)) makes ^ and $ match at every line boundary rather than only the string's ends; DOTALL ((?s)) makes ."],"distractorIds":["java/strings_and_text#b075406eb294","java/strings_and_text#fc0d1f08025e","java/strings_and_text#41331adb8e72"],"concepts":["retrievable","matched","retrieve","capture","named","groups"],"correctMd":"Capture groups `(...)` record the text they matched, retrievable by index via `group(n)` (group 0 is the whole match); named groups `(?<name>...)` retrieve by name via `group(\"name\")`.","answerFullMd":"Capture groups `(...)` record the text they matched, retrievable by index via `group(n)` (group 0 is the whole match); named groups `(?<name>...)` retrieve by name via `group(\"name\")`. A backreference `\\1` (or `\\k<name>`) requires the same captured text to appear again — for example `\\b(\\w+)\\s+\\1\\b` finds a doubled word. Backreferences are precisely the feature that makes a linear DFA engine impossible, forcing the backtracking design.","distractorsMd":["`matches()` requires the entire input to match (anchored at both ends), `lookingAt()` requires a match at the start but not to the end, and `find()` searches for a match anywhere and advances on each call.","Java's matcher implements each node's `match` recursively, so matching consumes JVM stack frames proportional to match depth.",null]},{"id":"java/strings_and_text#7cccdd9bfd20","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"regex_engine_and_redos.md","difficulty":"intermediate","question":"What is the difference between lookahead and lookbehind, and what do they cost?","answerFull":"Lookahead (?=...) / (?!...) asserts what follows the current position; lookbehind (?<=...) / (?<!...) asserts what precedes it — neither consumes input. They are zero-width assertions but still execute their sub-pattern, so a lookahead containing an inner quantifier ((?=.*a+)) can itself be a ReDoS source. Java supports arbitrary-length lookbehind, but wide lookbehind re-scans preceding text and is expensive.","correct":"Lookahead (?=...) / (?!...) asserts what follows the current position; lookbehind (?<=...) / (?<!...) asserts what precedes it — neither consumes input.","distractors":["Java's matcher implements each node's match recursively, so matching consumes JVM stack frames proportional to match depth.","Regex injection happens when user input is concatenated into a pattern (Pattern.compile(\"id=\" + userInput)), letting the user inject metacharacters — including ReDoS-triggering nested quantifiers.","Catastrophic backtracking is exponential-time matching caused by ambiguity in how quantifiers can split the input."],"distractorIds":["java/strings_and_text#fc0d1f08025e","java/strings_and_text#dddc70e29c95","java/strings_and_text#7044a1da23d0"],"concepts":["asserts","lookbehind","precedes","lookahead","consumes","difference"],"correctMd":"Lookahead `(?=...)` / `(?!...)` asserts what follows the current position; lookbehind `(?<=...)` / `(?<!...)` asserts what precedes it — neither consumes input.","answerFullMd":"Lookahead `(?=...)` / `(?!...)` asserts what follows the current position; lookbehind `(?<=...)` / `(?<!...)` asserts what precedes it — neither consumes input. They are zero-width assertions but still execute their sub-pattern, so a lookahead containing an inner quantifier (`(?=.*a+)`) can itself be a ReDoS source. Java supports arbitrary-length lookbehind, but wide lookbehind re-scans preceding text and is expensive.","distractorsMd":["Java's matcher implements each node's `match` recursively, so matching consumes JVM stack frames proportional to match depth.","Regex injection happens when user input is concatenated into a pattern (`Pattern.compile(\"id=\" + userInput)`), letting the user inject metacharacters — including ReDoS-triggering nested quantifiers.",null]},{"id":"java/strings_and_text#66482273f367","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"regex_engine_and_redos.md","difficulty":"advanced","question":"How do \\p{...} classes and UNICODE_CHARACTER_CLASS change matching?","answerFull":"\\p{L}, \\p{Sc}, \\p{IsGreek} and similar match Unicode categories, scripts, and blocks directly. By default \\d, \\w, \\s, and \\b are ASCII-only, so \\d matches only [0-9]; enabling Pattern.UNICODE_CHARACTER_CLASS (or inline (?U)) makes them Unicode-aware so \\w matches accented and non-Latin letters. Forgetting this flag silently rejects legitimate international input.","correct":"\\p{L}, \\p{Sc}, \\p{IsGreek} and similar match Unicode categories, scripts, and blocks directly.","distractors":["Capture groups (...) record the text they matched, retrievable by index via group(n) (group 0 is the whole match); named groups (?<name>...) retrieve by name via group(\"name\").","Wrap the input in a custom CharSequence whose charAt throws when the thread is interrupted or a deadline passes, then run the match on a worker thread that a watchdog interrupts.","Catastrophic backtracking is exponential-time matching caused by ambiguity in how quantifiers can split the input."],"distractorIds":["java/strings_and_text#33615f80a8a7","java/strings_and_text#b5f56acdb7a1","java/strings_and_text#7044a1da23d0"],"concepts":["isgreek","unicode","scripts","character","categories","similar"],"questionMd":"How do `\\p{...}` classes and `UNICODE_CHARACTER_CLASS` change matching?","correctMd":"`\\p{L}`, `\\p{Sc}`, `\\p{IsGreek}` and similar match Unicode categories, scripts, and blocks directly.","answerFullMd":"`\\p{L}`, `\\p{Sc}`, `\\p{IsGreek}` and similar match Unicode categories, scripts, and blocks directly. By default `\\d`, `\\w`, `\\s`, and `\\b` are ASCII-only, so `\\d` matches only `[0-9]`; enabling `Pattern.UNICODE_CHARACTER_CLASS` (or inline `(?U)`) makes them Unicode-aware so `\\w` matches accented and non-Latin letters. Forgetting this flag silently rejects legitimate international input.","distractorsMd":["Capture groups `(...)` record the text they matched, retrievable by index via `group(n)` (group 0 is the whole match); named groups `(?<name>...)` retrieve by name via `group(\"name\")`.","Wrap the input in a custom `CharSequence` whose `charAt` throws when the thread is interrupted or a deadline passes, then run the match on a worker thread that a watchdog interrupts.",null]},{"id":"java/strings_and_text#41331adb8e72","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"regex_engine_and_redos.md","difficulty":"advanced","question":"What do the MULTILINE and DOTALL flags do?","answerFull":"MULTILINE ((?m)) makes ^ and $ match at every line boundary rather than only the string's ends; DOTALL ((?s)) makes . also match line terminators like \\n. They are independent — you often want both when parsing multi-line logs. The default where . stops at \\n is a frequent cause of a pattern that \"works on one line but not the whole file.\"","correct":"MULTILINE ((?m)) makes ^ and $ match at every line boundary rather than only the string's ends; DOTALL ((?s)) makes .","distractors":["Wrap the input in a custom CharSequence whose charAt throws when the thread is interrupted or a deadline passes, then run the match on a worker thread that a watchdog interrupts.","\"abc\" is a compile-time literal; the JVM automatically interns it — the first time it is loaded, one object is created in the string table and all references to \"abc\" share it.","Java's matcher implements each node's match recursively, so matching consumes JVM stack frames proportional to match depth."],"distractorIds":["java/strings_and_text#b5f56acdb7a1","java/strings_and_text#13fb844fb6d2","java/strings_and_text#fc0d1f08025e"],"concepts":["dotall","multiline","ends","flags","boundary","line"],"questionMd":"What do the `MULTILINE` and `DOTALL` flags do?","answerFullMd":"`MULTILINE` (`(?m)`) makes `^` and `$` match at every line boundary rather than only the string's ends; `DOTALL` (`(?s)`) makes `.` also match line terminators like `\\n`. They are independent — you often want both when parsing multi-line logs. The default where `.` stops at `\\n` is a frequent cause of a pattern that \"works on one line but not the whole file.\"","distractorsMd":["Wrap the input in a custom `CharSequence` whose `charAt` throws when the thread is interrupted or a deadline passes, then run the match on a worker thread that a watchdog interrupts.","`\"abc\"` is a compile-time literal; the JVM automatically interns it — the first time it is loaded, one object is created in the string table and all references to `\"abc\"` share it.","Java's matcher implements each node's `match` recursively, so matching consumes JVM stack frames proportional to match depth."]},{"id":"java/strings_and_text#fc0d1f08025e","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"regex_engine_and_redos.md","difficulty":"advanced","question":"Why can a Java regex throw StackOverflowError instead of merely running slowly?","answerFull":"Java's matcher implements each node's match recursively, so matching consumes JVM stack frames proportional to match depth. A pattern like (a|b)* against a multi-megabyte input can exhaust the thread stack and throw StackOverflowError — which is an Error, not an Exception, so catch (Exception e) blocks miss it. This is a distinct failure mode from the exponential-time hang and is triggered by long input rather than pattern nesting alone.","correct":"Java's matcher implements each node's match recursively, so matching consumes JVM stack frames proportional to match depth.","distractors":["Lookahead (?=...) / (?!...) asserts what follows the current position; lookbehind (?<=...) / (?<!...) asserts what precedes it — neither consumes input.","Pattern is immutable and fully thread-safe, but Matcher is not — it holds mutable state (current position, region bounds, captured groups).","Compact Strings (Java 9, JEP 254) store String content in a byte[] instead of char[]."],"distractorIds":["java/strings_and_text#7cccdd9bfd20","java/strings_and_text#54fc8bef7cf5","java/strings_and_text#20c0536888f9"],"concepts":["slowly","matcher","consumes","recursively","merely","proportional"],"questionMd":"Why can a Java regex throw `StackOverflowError` instead of merely running slowly?","correctMd":"Java's matcher implements each node's `match` recursively, so matching consumes JVM stack frames proportional to match depth.","answerFullMd":"Java's matcher implements each node's `match` recursively, so matching consumes JVM stack frames proportional to match depth. A pattern like `(a|b)*` against a multi-megabyte input can exhaust the thread stack and throw `StackOverflowError` — which is an `Error`, not an `Exception`, so `catch (Exception e)` blocks miss it. This is a distinct failure mode from the exponential-time hang and is triggered by long input rather than pattern nesting alone.","distractorsMd":["Lookahead `(?=...)` / `(?!...)` asserts what follows the current position; lookbehind `(?<=...)` / `(?<!...)` asserts what precedes it — neither consumes input.","`Pattern` is immutable and fully thread-safe, but `Matcher` is not — it holds mutable state (current position, region bounds, captured groups).","Compact Strings (Java 9, JEP 254) store `String` content in a `byte[]` instead of `char[]`."]},{"id":"java/strings_and_text#dddc70e29c95","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"regex_engine_and_redos.md","difficulty":"advanced","question":"What is regex injection and how do you prevent it?","answerFull":"Regex injection happens when user input is concatenated into a pattern (Pattern.compile(\"id=\" + userInput)), letting the user inject metacharacters — including ReDoS-triggering nested quantifiers. Prevent it by wrapping the user text in Pattern.quote(userInput), which escapes all metacharacters, or by using the LITERAL compile flag. Never build a live pattern from untrusted input without quoting.","correct":"Regex injection happens when user input is concatenated into a pattern (Pattern.compile(\"id=\" + userInput)), letting the user inject metacharacters — including ReDoS-triggering nested quantifiers.","distractors":["Pattern is immutable and fully thread-safe, but Matcher is not — it holds mutable state (current position, region bounds, captured groups).","RE2/J (com.google.re2j) is a pure-Java port of Google's RE2 that runs in guaranteed linear time using an automaton that never backtracks, making it immune to ReDoS.","Catastrophic backtracking is exponential-time matching caused by ambiguity in how quantifiers can split the input."],"distractorIds":["java/strings_and_text#54fc8bef7cf5","java/strings_and_text#104c1ede9f42","java/strings_and_text#7044a1da23d0"],"concepts":["metacharacters","userinput","redos","quantifiers","triggering","concatenated"],"correctMd":"Regex injection happens when user input is concatenated into a pattern (`Pattern.compile(\"id=\" + userInput)`), letting the user inject metacharacters — including ReDoS-triggering nested quantifiers.","answerFullMd":"Regex injection happens when user input is concatenated into a pattern (`Pattern.compile(\"id=\" + userInput)`), letting the user inject metacharacters — including ReDoS-triggering nested quantifiers. Prevent it by wrapping the user text in `Pattern.quote(userInput)`, which escapes all metacharacters, or by using the `LITERAL` compile flag. Never build a live pattern from untrusted input without quoting.","distractorsMd":["`Pattern` is immutable and fully thread-safe, but `Matcher` is not — it holds mutable state (current position, region bounds, captured groups).","RE2/J (`com.google.re2j`) is a pure-Java port of Google's RE2 that runs in guaranteed linear time using an automaton that never backtracks, making it immune to ReDoS.",null]},{"id":"java/strings_and_text#104c1ede9f42","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"regex_engine_and_redos.md","difficulty":"advanced","question":"What is RE2/J and when should you choose it over java.util.regex?","answerFull":"RE2/J (com.google.re2j) is a pure-Java port of Google's RE2 that runs in guaranteed linear time using an automaton that never backtracks, making it immune to ReDoS. Choose it whenever the pattern or the input is attacker-controlled — user-defined search filters, WAF rules, config-driven validators. The cost is that it drops backreferences and lookaround, which is an acceptable trade for DoS-sensitive endpoints; its API mirrors Pattern/Matcher for an easy swap.","correct":"RE2/J (com.google.re2j) is a pure-Java port of Google's RE2 that runs in guaranteed linear time using an automaton that never backtracks, making it immune to ReDoS.","distractors":["Greedy (a+) matches as much as possible then backtracks; lazy (a+?) matches as little as possible then takes more; possessive (a++) matches as much as possible and never gives back.","Regex injection happens when user input is concatenated into a pattern (Pattern.compile(\"id=\" + userInput)), letting the user inject metacharacters — including ReDoS-triggering nested quantifiers.","Java's matcher implements each node's match recursively, so matching consumes JVM stack frames proportional to match depth."],"distractorIds":["java/strings_and_text#7f044043be84","java/strings_and_text#dddc70e29c95","java/strings_and_text#fc0d1f08025e"],"concepts":["immune","re2j","automaton","backtracks","re2","redos"],"questionMd":"What is RE2/J and when should you choose it over `java.util.regex`?","correctMd":"RE2/J (`com.google.re2j`) is a pure-Java port of Google's RE2 that runs in guaranteed linear time using an automaton that never backtracks, making it immune to ReDoS.","answerFullMd":"RE2/J (`com.google.re2j`) is a pure-Java port of Google's RE2 that runs in guaranteed linear time using an automaton that never backtracks, making it immune to ReDoS. Choose it whenever the pattern or the input is attacker-controlled — user-defined search filters, WAF rules, config-driven validators. The cost is that it drops backreferences and lookaround, which is an acceptable trade for DoS-sensitive endpoints; its API mirrors `Pattern`/`Matcher` for an easy swap.","distractorsMd":["Greedy (`a+`) matches as much as possible then backtracks; lazy (`a+?`) matches as little as possible then takes more; possessive (`a++`) matches as much as possible and never gives back.","Regex injection happens when user input is concatenated into a pattern (`Pattern.compile(\"id=\" + userInput)`), letting the user inject metacharacters — including ReDoS-triggering nested quantifiers.","Java's matcher implements each node's `match` recursively, so matching consumes JVM stack frames proportional to match depth."]},{"id":"java/strings_and_text#f0cfe69752b0","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"regex_engine_and_redos.md","difficulty":"advanced","question":"What does Matcher.region() do and why use it?","answerFull":"region(start, end) restricts matching to a sub-range of the input without allocating a substring, so anchors and find() operate only within that window. It avoids the copy that input.substring(start, end) would create, which matters when scanning large buffers repeatedly. You can also tune anchoring behavior at region boundaries with useAnchoringBounds and useTransparentBounds.","correct":"region(start, end) restricts matching to a sub-range of the input without allocating a substring, so anchors and find() operate only within that window.","distractors":["Pattern is immutable and fully thread-safe, but Matcher is not — it holds mutable state (current position, region bounds, captured groups).","Catastrophic backtracking is exponential-time matching caused by ambiguity in how quantifiers can split the input.","Java's matcher implements each node's match recursively, so matching consumes JVM stack frames proportional to match depth."],"distractorIds":["java/strings_and_text#54fc8bef7cf5","java/strings_and_text#7044a1da23d0","java/strings_and_text#fc0d1f08025e"],"concepts":["allocating","matcher","substring","anchors","restricts","operate"],"questionMd":"What does `Matcher.region()` do and why use it?","correctMd":"`region(start, end)` restricts matching to a sub-range of the input without allocating a substring, so anchors and `find()` operate only within that window.","answerFullMd":"`region(start, end)` restricts matching to a sub-range of the input without allocating a substring, so anchors and `find()` operate only within that window. It avoids the copy that `input.substring(start, end)` would create, which matters when scanning large buffers repeatedly. You can also tune anchoring behavior at region boundaries with `useAnchoringBounds` and `useTransparentBounds`.","distractorsMd":["`Pattern` is immutable and fully thread-safe, but `Matcher` is not — it holds mutable state (current position, region bounds, captured groups).",null,"Java's matcher implements each node's `match` recursively, so matching consumes JVM stack frames proportional to match depth."]},{"id":"java/strings_and_text#beba697f2064","section":"java","module":"java/strings_and_text","moduleName":"strings and text","sourceFile":"regex_engine_and_redos.md","difficulty":"advanced","question":"What is the gotcha with $ and \\ in replaceAll's replacement string?","answerFull":"In the replacement argument, $ introduces a group reference ($1) and \\ escapes, so a literal $ or \\ in the output must be written as \\\\$ and \\\\\\\\. Passing user text directly as the replacement can throw IllegalArgumentException or inject unintended group references; wrap it in Matcher.quoteReplacement(str) to treat it literally. This is separate from Pattern.quote, which protects the *pattern* side.","correct":"In the replacement argument, $ introduces a group reference ($1) and \\ escapes, so a literal $ or \\ in the output must be written as \\\\$ and \\\\\\\\.","distractors":["\"abc\" is a compile-time literal; the JVM automatically interns it — the first time it is loaded, one object is created in the string table and all references to \"abc\" share it.","Compact Strings (Java 9, JEP 254) store String content in a byte[] instead of char[].","== compares object identity (reference equality) — use only when comparing interned strings or the exact same reference."],"distractorIds":["java/strings_and_text#13fb844fb6d2","java/strings_and_text#20c0536888f9","java/strings_and_text#fbc282a9f395"],"concepts":["gotcha","replaceall","introduces","escapes","literal","replacement"],"questionMd":"What is the gotcha with `$` and `\\` in `replaceAll`'s replacement string?","correctMd":"In the replacement argument, `$` introduces a group reference (`$1`) and `\\` escapes, so a literal `$` or `\\` in the output must be written as `\\\\$` and `\\\\\\\\`.","answerFullMd":"In the replacement argument, `$` introduces a group reference (`$1`) and `\\` escapes, so a literal `$` or `\\` in the output must be written as `\\\\$` and `\\\\\\\\`. Passing user text directly as the replacement can throw `IllegalArgumentException` or inject unintended group references; wrap it in `Matcher.quoteReplacement(str)` to treat it literally. This is separate from `Pattern.quote`, which protects the *pattern* side.","distractorsMd":["`\"abc\"` is a compile-time literal; the JVM automatically interns it — the first time it is loaded, one object is created in the string table and all references to `\"abc\"` share it.","Compact Strings (Java 9, JEP 254) store `String` content in a `byte[]` instead of `char[]`.","`==` compares object identity (reference equality) — use only when comparing interned strings or the exact same reference."]},{"id":"java/structured_concurrency_and_loom#4852a1c9ae4d","section":"java","module":"java/structured_concurrency_and_loom","moduleName":"structured concurrency and loom","sourceFile":"README.md","difficulty":"core","question":"What is a virtual thread and how does it differ from a platform thread?","answerFull":"A virtual thread is a JVM-managed thread stored as a heap object (continuation) rather than a native OS thread. Platform threads map 1:1 to OS threads and cost ~1 MB of OS-allocated stack; virtual threads cost ~few KB of heap and are multiplexed by the JVM's ForkJoinPool scheduler onto a small pool of platform \"carrier\" threads (default size = number of CPU cores). When a virtual thread blocks on I/O, the JVM saves its stack state to the heap and mounts a different virtual thread on the carrier — the OS thread never blocks. This enables 100,000+ concurrent virtual threads at a fraction of the memory cost of equivalent platform threads.","correct":"A virtual thread is a JVM-managed thread stored as a heap object (continuation) rather than a native OS thread.","distractors":["Thread.ofVirtual().unstarted(Runnable task) creates a virtual thread without starting it.","The default carrier thread pool is a ForkJoinPool with parallelism set to Runtime.getRuntime().availableProcessors() — typically the number of vCPUs.","With platform thread pools (bounded, e.g., 200 threads), ThreadLocal state is bounded: at most 200 copies of each ThreadLocal value exist."],"distractorIds":["java/structured_concurrency_and_loom#835867e5164c","java/structured_concurrency_and_loom#8114a8a24a4b","java/structured_concurrency_and_loom#2098d21e5d9d"],"concepts":["continuation","differ","managed","platform","native","virtual"],"answerFullMd":"A virtual thread is a JVM-managed thread stored as a heap object (continuation) rather than a native OS thread. Platform threads map 1:1 to OS threads and cost ~1 MB of OS-allocated stack; virtual threads cost ~few KB of heap and are multiplexed by the JVM's `ForkJoinPool` scheduler onto a small pool of platform \"carrier\" threads (default size = number of CPU cores). When a virtual thread blocks on I/O, the JVM saves its stack state to the heap and mounts a different virtual thread on the carrier — the OS thread never blocks. This enables 100,000+ concurrent virtual threads at a fraction of the memory cost of equivalent platform threads.","distractorsMd":["`Thread.ofVirtual().unstarted(Runnable task)` creates a virtual thread without starting it.","The default carrier thread pool is a `ForkJoinPool` with parallelism set to `Runtime.getRuntime().availableProcessors()` — typically the number of vCPUs.","With platform thread pools (bounded, e.g., 200 threads), `ThreadLocal` state is bounded: at most 200 copies of each `ThreadLocal` value exist."]},{"id":"java/structured_concurrency_and_loom#fe5c7de81c8f","section":"java","module":"java/structured_concurrency_and_loom","moduleName":"structured concurrency and loom","sourceFile":"README.md","difficulty":"core","question":"What is carrier thread pinning, how do you detect it, and how do you fix it?","answerFull":"Pinning occurs when a virtual thread cannot be unmounted from its carrier platform thread. The two causes are: (1) the virtual thread is inside a synchronized block or method when it blocks; (2) the virtual thread calls a native method that blocks. When all carrier threads are pinned, no new virtual threads can run — throughput degrades to platform-thread levels. Detect with -Djdk.tracePinnedThreads=full (prints a stack trace on each pin event). Fix by replacing synchronized + wait/notify with ReentrantLock + Condition.await(), which releases the virtual thread without pinning the carrier.","correct":"Pinning occurs when a virtual thread cannot be unmounted from its carrier platform thread.","distractors":["Virtual thread stacks start small (the JDK allocates initial chunks of ~200 bytes or the minimum needed for the first frames) and grow on demand.","With platform thread pools (bounded, e.g., 200 threads), ThreadLocal state is bounded: at most 200 copies of each ThreadLocal value exist.","The default carrier thread pool is a ForkJoinPool with parallelism set to Runtime.getRuntime().availableProcessors() — typically the number of vCPUs."],"distractorIds":["java/structured_concurrency_and_loom#d16378277710","java/structured_concurrency_and_loom#2098d21e5d9d","java/structured_concurrency_and_loom#8114a8a24a4b"],"concepts":["pinning","unmounted","carrier","fix","detect","platform"],"answerFullMd":"Pinning occurs when a virtual thread cannot be unmounted from its carrier platform thread. The two causes are: (1) the virtual thread is inside a `synchronized` block or method when it blocks; (2) the virtual thread calls a native method that blocks. When all carrier threads are pinned, no new virtual threads can run — throughput degrades to platform-thread levels. Detect with `-Djdk.tracePinnedThreads=full` (prints a stack trace on each pin event). Fix by replacing `synchronized` + `wait/notify` with `ReentrantLock` + `Condition.await()`, which releases the virtual thread without pinning the carrier.","distractorsMd":[null,"With platform thread pools (bounded, e.g., 200 threads), `ThreadLocal` state is bounded: at most 200 copies of each `ThreadLocal` value exist.","The default carrier thread pool is a `ForkJoinPool` with parallelism set to `Runtime.getRuntime().availableProcessors()` — typically the number of vCPUs."]},{"id":"java/structured_concurrency_and_loom#53e16bea2bd0","section":"java","module":"java/structured_concurrency_and_loom","moduleName":"structured concurrency and loom","sourceFile":"README.md","difficulty":"core","question":"Should you pool virtual threads? Why or why not?","answerFull":"No. Virtual threads are cheap to create (~few KB, microseconds to start), so the reason for pooling platform threads — limiting a scarce, expensive resource — does not apply. Pooling virtual threads re-introduces an artificial concurrency ceiling and defeats the model: pooled virtual threads can carry stale ThreadLocal state across tasks, and the pool size limit throttles throughput without providing any benefit. The correct pattern is Executors.newVirtualThreadPerTaskExecutor(), which creates exactly one virtual thread per submitted task with no upper limit.","correct":"No. Virtual threads are cheap to create (~few KB, microseconds to start), so the reason for pooling platform threads — limiting a scarce, expensive resource — does not apply.","distractors":["Virtual thread stacks start small (the JDK allocates initial chunks of ~200 bytes or the minimum needed for the first frames) and grow on demand.","The default carrier thread pool is a ForkJoinPool with parallelism set to Runtime.getRuntime().availableProcessors() — typically the number of vCPUs.","Scenario: A service migrated to spring.threads.virtual.enabled=true on Spring Boot 3.2."],"distractorIds":["java/structured_concurrency_and_loom#d16378277710","java/structured_concurrency_and_loom#8114a8a24a4b","java/structured_concurrency_and_loom#48c9b86e1d30"],"concepts":["microseconds","scarce","pooling","limiting","reason","cheap"],"answerFullMd":"No. Virtual threads are cheap to create (~few KB, microseconds to start), so the reason for pooling platform threads — limiting a scarce, expensive resource — does not apply. Pooling virtual threads re-introduces an artificial concurrency ceiling and defeats the model: pooled virtual threads can carry stale `ThreadLocal` state across tasks, and the pool size limit throttles throughput without providing any benefit. The correct pattern is `Executors.newVirtualThreadPerTaskExecutor()`, which creates exactly one virtual thread per submitted task with no upper limit.","distractorsMd":[null,"The default carrier thread pool is a `ForkJoinPool` with parallelism set to `Runtime.getRuntime().availableProcessors()` — typically the number of vCPUs.","Scenario: A service migrated to `spring.threads.virtual.enabled=true` on Spring Boot 3.2."]},{"id":"java/structured_concurrency_and_loom#8cc6c3bd4e88","section":"java","module":"java/structured_concurrency_and_loom","moduleName":"structured concurrency and loom","sourceFile":"README.md","difficulty":"core","question":"What is StructuredTaskScope and what problem does it solve that CompletableFuture.allOf() does not?","answerFull":"StructuredTaskScope (JEP 453) enforces that all forked subtasks complete or are cancelled before the enclosing scope exits. It solves three problems with CompletableFuture.allOf(): (1) cancellation: if one subtask fails, ShutdownOnFailure automatically cancels remaining subtasks — allOf() requires manual cancellation wiring; (2) leak prevention: subtasks cannot outlive their scope (the try block guarantees all threads are done); (3) structured nesting: scopes can be nested with predictable ownership — parent scopes wait for child scopes, forming a tree analogous to structured statement nesting. StructuredTaskScope also preserves the virtual-thread-centric blocking style: scope.join() blocks the parent virtual thread without pinning a carrier.","correct":"StructuredTaskScope (JEP 453) enforces that all forked subtasks complete or are cancelled before the enclosing scope exits.","distractors":["ShutdownOnSuccess shuts down the scope (signalling cancellation) the moment the first subtask completes successfully.","A virtual thread is a JVM-managed thread stored as a heap object (continuation) rather than a native OS thread.","JDBC Connection.execute() calls involve I/O (network to database)."],"distractorIds":["java/structured_concurrency_and_loom#38c8ddf51e77","java/structured_concurrency_and_loom#4852a1c9ae4d","java/structured_concurrency_and_loom#c57845615b9d"],"concepts":["453","forked","allof","structuredtaskscope","subtasks","enclosing"],"questionMd":"What is `StructuredTaskScope` and what problem does it solve that `CompletableFuture.allOf()` does not?","correctMd":"`StructuredTaskScope` (JEP 453) enforces that all forked subtasks complete or are cancelled before the enclosing scope exits.","answerFullMd":"`StructuredTaskScope` (JEP 453) enforces that all forked subtasks complete or are cancelled before the enclosing scope exits. It solves three problems with `CompletableFuture.allOf()`: (1) **cancellation**: if one subtask fails, `ShutdownOnFailure` automatically cancels remaining subtasks — `allOf()` requires manual cancellation wiring; (2) **leak prevention**: subtasks cannot outlive their scope (the `try` block guarantees all threads are done); (3) **structured nesting**: scopes can be nested with predictable ownership — parent scopes wait for child scopes, forming a tree analogous to structured statement nesting. `StructuredTaskScope` also preserves the virtual-thread-centric blocking style: `scope.join()` blocks the parent virtual thread without pinning a carrier.","distractorsMd":["`ShutdownOnSuccess` shuts down the scope (signalling cancellation) the moment the first subtask completes successfully.",null,"JDBC `Connection.execute()` calls involve I/O (network to database)."]},{"id":"java/structured_concurrency_and_loom#2ec15aeac42f","section":"java","module":"java/structured_concurrency_and_loom","moduleName":"structured concurrency and loom","sourceFile":"README.md","difficulty":"core","question":"Explain the difference between ScopedValue and ThreadLocal. When would you choose each?","answerFull":"ThreadLocal is mutable — any code can set() and remove() the value; in a pooled environment, a stale value from the previous task may be visible to the next. ScopedValue (JEP 446) is immutable within its scope: once bound via ScopedValue.where(KEY, value).run(...), the value is fixed and automatically visible to all child scopes and StructuredTaskScope forks — no manual inheritance, no InheritableThreadLocal boilerplate. After the run() block exits, the binding disappears automatically. Use ScopedValue for request-scoped context (security principal, transaction, trace ID) in virtual-thread services. Keep ThreadLocal only where mutable per-thread state is genuinely needed (e.g., SimpleDateFormat caching in Java 8 code, where ScopedValue is unavailable).","correct":"ThreadLocal is mutable — any code can set() and remove() the value; in a pooled environment, a stale value from the previous task may be visible to the next.","distractors":["Thread.ofVirtual().unstarted(Runnable task) creates a virtual thread without starting it.","The default carrier thread pool is a ForkJoinPool with parallelism set to Runtime.getRuntime().availableProcessors() — typically the number of vCPUs.","With platform thread pools (bounded, e.g., 200 threads), ThreadLocal state is bounded: at most 200 copies of each ThreadLocal value exist."],"distractorIds":["java/structured_concurrency_and_loom#835867e5164c","java/structured_concurrency_and_loom#8114a8a24a4b","java/structured_concurrency_and_loom#2098d21e5d9d"],"concepts":["pooled","explain","difference","threadlocal","remove","stale"],"questionMd":"Explain the difference between `ScopedValue` and `ThreadLocal`. When would you choose each?","correctMd":"`ThreadLocal` is mutable — any code can `set()` and `remove()` the value; in a pooled environment, a stale value from the previous task may be visible to the next.","answerFullMd":"`ThreadLocal` is mutable — any code can `set()` and `remove()` the value; in a pooled environment, a stale value from the previous task may be visible to the next. `ScopedValue` (JEP 446) is immutable within its scope: once bound via `ScopedValue.where(KEY, value).run(...)`, the value is fixed and automatically visible to all child scopes and `StructuredTaskScope` forks — no manual inheritance, no `InheritableThreadLocal` boilerplate. After the `run()` block exits, the binding disappears automatically. Use `ScopedValue` for request-scoped context (security principal, transaction, trace ID) in virtual-thread services. Keep `ThreadLocal` only where mutable per-thread state is genuinely needed (e.g., `SimpleDateFormat` caching in Java 8 code, where `ScopedValue` is unavailable).","distractorsMd":["`Thread.ofVirtual().unstarted(Runnable task)` creates a virtual thread without starting it.","The default carrier thread pool is a `ForkJoinPool` with parallelism set to `Runtime.getRuntime().availableProcessors()` — typically the number of vCPUs.","With platform thread pools (bounded, e.g., 200 threads), `ThreadLocal` state is bounded: at most 200 copies of each `ThreadLocal` value exist."]},{"id":"java/structured_concurrency_and_loom#3f08b9914aa8","section":"java","module":"java/structured_concurrency_and_loom","moduleName":"structured concurrency and loom","sourceFile":"README.md","difficulty":"intermediate","question":"What happens to a virtual thread when it calls a blocking I/O operation?","answerFull":"The JVM's I/O infrastructure intercepts blocking calls (socket reads, file reads, database JDBC) at the NIO layer. Instead of blocking the OS thread, the JVM: (1) saves the virtual thread's current call stack as a heap-allocated Continuation object; (2) removes (unmounts) the virtual thread from its carrier platform thread; (3) submits an async I/O request to the OS; (4) when the I/O completes, the scheduler enqueues the Continuation for re-mounting; (5) a carrier picks it up, restores the stack state, and resumes execution from exactly where it left off. The OS thread was free for other virtual threads throughout steps 2–4 — this is the core efficiency gain.","correct":"The JVM's I/O infrastructure intercepts blocking calls (socket reads, file reads, database JDBC) at the NIO layer.","distractors":["Virtual thread stacks start small (the JDK allocates initial chunks of ~200 bytes or the minimum needed for the first frames) and grow on demand.","A virtual thread is a JVM-managed thread stored as a heap object (continuation) rather than a native OS thread.","With platform thread pools (bounded, e.g., 200 threads), ThreadLocal state is bounded: at most 200 copies of each ThreadLocal value exist."],"distractorIds":["java/structured_concurrency_and_loom#d16378277710","java/structured_concurrency_and_loom#4852a1c9ae4d","java/structured_concurrency_and_loom#2098d21e5d9d"],"concepts":["nio","intercepts","jdbc","socket","infrastructure","happens"],"answerFullMd":"The JVM's I/O infrastructure intercepts blocking calls (socket reads, file reads, database JDBC) at the NIO layer. Instead of blocking the OS thread, the JVM: (1) saves the virtual thread's current call stack as a heap-allocated `Continuation` object; (2) removes (unmounts) the virtual thread from its carrier platform thread; (3) submits an async I/O request to the OS; (4) when the I/O completes, the scheduler enqueues the `Continuation` for re-mounting; (5) a carrier picks it up, restores the stack state, and resumes execution from exactly where it left off. The OS thread was free for other virtual threads throughout steps 2–4 — this is the core efficiency gain.","distractorsMd":[null,null,"With platform thread pools (bounded, e.g., 200 threads), `ThreadLocal` state is bounded: at most 200 copies of each `ThreadLocal` value exist."]},{"id":"java/structured_concurrency_and_loom#2098d21e5d9d","section":"java","module":"java/structured_concurrency_and_loom","moduleName":"structured concurrency and loom","sourceFile":"README.md","difficulty":"intermediate","question":"What are the risks of using ThreadLocal at scale with virtual threads?","answerFull":"With platform thread pools (bounded, e.g., 200 threads), ThreadLocal state is bounded: at most 200 copies of each ThreadLocal value exist. With virtual threads (unbounded), 100,000 concurrent tasks × a 64 KB ThreadLocal<byte[]> = 6.4 GB of ThreadLocal data. Additionally, if virtual threads are ever pooled (accidentally or via a third-party wrapper), stale ThreadLocal values from one task leak into the next. The fix: use local variables for task-scoped state, and ScopedValue for inherited context. If ThreadLocal is unavoidable, call remove() explicitly before the virtual thread ends.","correct":"With platform thread pools (bounded, e.g., 200 threads), ThreadLocal state is bounded: at most 200 copies of each ThreadLocal value exist.","distractors":["A virtual thread is a JVM-managed thread stored as a heap object (continuation) rather than a native OS thread.","The default carrier thread pool is a ForkJoinPool with parallelism set to Runtime.getRuntime().availableProcessors() — typically the number of vCPUs.","Scenario: A service migrated to spring.threads.virtual.enabled=true on Spring Boot 3.2."],"distractorIds":["java/structured_concurrency_and_loom#4852a1c9ae4d","java/structured_concurrency_and_loom#8114a8a24a4b","java/structured_concurrency_and_loom#48c9b86e1d30"],"concepts":["pools","risks","threadlocal","copies","exist","bounded"],"questionMd":"What are the risks of using `ThreadLocal` at scale with virtual threads?","correctMd":"With platform thread pools (bounded, e.g., 200 threads), `ThreadLocal` state is bounded: at most 200 copies of each `ThreadLocal` value exist.","answerFullMd":"With platform thread pools (bounded, e.g., 200 threads), `ThreadLocal` state is bounded: at most 200 copies of each `ThreadLocal` value exist. With virtual threads (unbounded), 100,000 concurrent tasks × a 64 KB `ThreadLocal<byte[]>` = 6.4 GB of ThreadLocal data. Additionally, if virtual threads are ever pooled (accidentally or via a third-party wrapper), stale ThreadLocal values from one task leak into the next. The fix: use local variables for task-scoped state, and `ScopedValue` for inherited context. If `ThreadLocal` is unavoidable, call `remove()` explicitly before the virtual thread ends.","distractorsMd":[null,"The default carrier thread pool is a `ForkJoinPool` with parallelism set to `Runtime.getRuntime().availableProcessors()` — typically the number of vCPUs.","Scenario: A service migrated to `spring.threads.virtual.enabled=true` on Spring Boot 3.2."]},{"id":"java/structured_concurrency_and_loom#38c8ddf51e77","section":"java","module":"java/structured_concurrency_and_loom","moduleName":"structured concurrency and loom","sourceFile":"README.md","difficulty":"intermediate","question":"How does StructuredTaskScope.ShutdownOnSuccess work and when is it useful?","answerFull":"ShutdownOnSuccess shuts down the scope (signalling cancellation) the moment the first subtask completes successfully. Subsequent forks receive InterruptedException and terminate. This implements the \"race\" or \"hedge\" pattern: submit the same request to two or more backends, accept the fastest successful response, cancel the rest. Use cases: CDN failover, primary vs read-replica queries, A/B latency comparison. After scope.join(), call scope.result() to retrieve the winning value. Unlike CompletableFuture.anyOf(), losers are guaranteed to be cancelled and no thread resources leak.","correct":"ShutdownOnSuccess shuts down the scope (signalling cancellation) the moment the first subtask completes successfully.","distractors":["StructuredTaskScope (JEP 453) enforces that all forked subtasks complete or are cancelled before the enclosing scope exits.","A virtual thread is a JVM-managed thread stored as a heap object (continuation) rather than a native OS thread.","JDBC Connection.execute() calls involve I/O (network to database)."],"distractorIds":["java/structured_concurrency_and_loom#8cc6c3bd4e88","java/structured_concurrency_and_loom#4852a1c9ae4d","java/structured_concurrency_and_loom#c57845615b9d"],"concepts":["shutdownonsuccess","shuts","signalling","subtask","structuredtaskscope","cancellation"],"questionMd":"How does `StructuredTaskScope.ShutdownOnSuccess` work and when is it useful?","correctMd":"`ShutdownOnSuccess` shuts down the scope (signalling cancellation) the moment the first subtask completes successfully.","answerFullMd":"`ShutdownOnSuccess` shuts down the scope (signalling cancellation) the moment the first subtask completes successfully. Subsequent forks receive `InterruptedException` and terminate. This implements the \"race\" or \"hedge\" pattern: submit the same request to two or more backends, accept the fastest successful response, cancel the rest. Use cases: CDN failover, primary vs read-replica queries, A/B latency comparison. After `scope.join()`, call `scope.result()` to retrieve the winning value. Unlike `CompletableFuture.anyOf()`, losers are guaranteed to be cancelled and no thread resources leak.","distractorsMd":["`StructuredTaskScope` (JEP 453) enforces that all forked subtasks complete or are cancelled before the enclosing scope exits.",null,"JDBC `Connection.execute()` calls involve I/O (network to database)."]},{"id":"java/structured_concurrency_and_loom#49bd937efc65","section":"java","module":"java/structured_concurrency_and_loom","moduleName":"structured concurrency and loom","sourceFile":"README.md","difficulty":"intermediate","question":"Can virtual threads improve CPU-bound throughput?","answerFull":"No. Virtual threads improve concurrency for I/O-bound work by freeing OS threads from waiting. For CPU-bound tasks (cryptography, image processing, scientific computing), the bottleneck is CPU cores, not threads. Each CPU-bound virtual thread holds its carrier for the entire computation — with N CPU cores and N+1 CPU-bound virtual threads, one must wait. The correct tool for CPU-bound parallel work is ForkJoinPool.commonPool() or a work-stealing executor sized to the number of cores. Virtual threads and ForkJoinPool are complementary: I/O-bound work → virtual threads; CPU-bound work → ForkJoinPool.","correct":"No. Virtual threads improve concurrency for I/O-bound work by freeing OS threads from waiting.","distractors":["With platform thread pools (bounded, e.g., 200 threads), ThreadLocal state is bounded: at most 200 copies of each ThreadLocal value exist.","Scenario: A service migrated to spring.threads.virtual.enabled=true on Spring Boot 3.2.","Virtual thread stacks start small (the JDK allocates initial chunks of ~200 bytes or the minimum needed for the first frames) and grow on demand."],"distractorIds":["java/structured_concurrency_and_loom#2098d21e5d9d","java/structured_concurrency_and_loom#48c9b86e1d30","java/structured_concurrency_and_loom#d16378277710"],"concepts":["freeing","improve","concurrency","throughput","waiting","virtual"],"answerFullMd":"No. Virtual threads improve concurrency for I/O-bound work by freeing OS threads from waiting. For CPU-bound tasks (cryptography, image processing, scientific computing), the bottleneck is CPU cores, not threads. Each CPU-bound virtual thread holds its carrier for the entire computation — with N CPU cores and N+1 CPU-bound virtual threads, one must wait. The correct tool for CPU-bound parallel work is `ForkJoinPool.commonPool()` or a work-stealing executor sized to the number of cores. Virtual threads and ForkJoinPool are complementary: I/O-bound work → virtual threads; CPU-bound work → ForkJoinPool.","distractorsMd":["With platform thread pools (bounded, e.g., 200 threads), `ThreadLocal` state is bounded: at most 200 copies of each `ThreadLocal` value exist.","Scenario: A service migrated to `spring.threads.virtual.enabled=true` on Spring Boot 3.2.",null]},{"id":"java/structured_concurrency_and_loom#c57845615b9d","section":"java","module":"java/structured_concurrency_and_loom","moduleName":"structured concurrency and loom","sourceFile":"README.md","difficulty":"intermediate","question":"How do you use virtual threads with JDBC/HikariCP safely?","answerFull":"JDBC Connection.execute() calls involve I/O (network to database). With virtual threads, each call unmounts the virtual thread while waiting for the database response — the carrier thread is free. However, the connection pool itself must not use synchronized internally: older synchronized-based pool implementations would pin the carrier during pool acquisition. HikariCP 5.1.0+ replaced its internal synchronized sections with StampedLock, making it virtual-thread-safe. Spring Boot 3.2+ sets spring.datasource.hikari.keepalive-time and uses spring.threads.virtual.enabled=true together safely. Important: connection pool size still limits database concurrency — 100,000 virtual threads will still queue if the HikariCP pool is set to 10 connections.","correct":"JDBC Connection.execute() calls involve I/O (network to database).","distractors":["A virtual thread is a JVM-managed thread stored as a heap object (continuation) rather than a native OS thread.","With platform thread pools (bounded, e.g., 200 threads), ThreadLocal state is bounded: at most 200 copies of each ThreadLocal value exist.","Scenario: A service migrated to spring.threads.virtual.enabled=true on Spring Boot 3.2."],"distractorIds":["java/structured_concurrency_and_loom#4852a1c9ae4d","java/structured_concurrency_and_loom#2098d21e5d9d","java/structured_concurrency_and_loom#48c9b86e1d30"],"concepts":["involve","jdbc","safely","hikaricp","execute","virtual"],"correctMd":"JDBC `Connection.execute()` calls involve I/O (network to database).","answerFullMd":"JDBC `Connection.execute()` calls involve I/O (network to database). With virtual threads, each call unmounts the virtual thread while waiting for the database response — the carrier thread is free. However, the connection pool itself must not use `synchronized` internally: older `synchronized`-based pool implementations would pin the carrier during pool acquisition. HikariCP 5.1.0+ replaced its internal `synchronized` sections with `StampedLock`, making it virtual-thread-safe. Spring Boot 3.2+ sets `spring.datasource.hikari.keepalive-time` and uses `spring.threads.virtual.enabled=true` together safely. Important: connection pool size still limits database concurrency — 100,000 virtual threads will still queue if the HikariCP pool is set to 10 connections.","distractorsMd":[null,"With platform thread pools (bounded, e.g., 200 threads), `ThreadLocal` state is bounded: at most 200 copies of each `ThreadLocal` value exist.","Scenario: A service migrated to `spring.threads.virtual.enabled=true` on Spring Boot 3.2."]},{"id":"java/structured_concurrency_and_loom#8114a8a24a4b","section":"java","module":"java/structured_concurrency_and_loom","moduleName":"structured concurrency and loom","sourceFile":"README.md","difficulty":"advanced","question":"What is the carrier thread pool size and can you change it?","answerFull":"The default carrier thread pool is a ForkJoinPool with parallelism set to Runtime.getRuntime().availableProcessors() — typically the number of vCPUs. This matches CPU-bound carrier work (e.g., running non-pinned virtual thread code between I/O waits). You can override it with -Djdk.virtualThreadScheduler.parallelism=N and the maximum pool size with -Djdk.virtualThreadScheduler.maxPoolSize=N (default: 256 to bound OS resource use). Changing these is rarely needed and usually signals a pinning problem rather than a configuration problem.","correct":"The default carrier thread pool is a ForkJoinPool with parallelism set to Runtime.getRuntime().availableProcessors() — typically the number of vCPUs.","distractors":["Pinning occurs when a virtual thread cannot be unmounted from its carrier platform thread.","Virtual thread stacks start small (the JDK allocates initial chunks of ~200 bytes or the minimum needed for the first frames) and grow on demand.","A virtual thread is a JVM-managed thread stored as a heap object (continuation) rather than a native OS thread."],"distractorIds":["java/structured_concurrency_and_loom#fe5c7de81c8f","java/structured_concurrency_and_loom#d16378277710","java/structured_concurrency_and_loom#4852a1c9ae4d"],"concepts":["availableprocessors","vcpus","getruntime","forkjoinpool","carrier","parallelism"],"correctMd":"The default carrier thread pool is a `ForkJoinPool` with parallelism set to `Runtime.getRuntime().availableProcessors()` — typically the number of vCPUs.","answerFullMd":"The default carrier thread pool is a `ForkJoinPool` with parallelism set to `Runtime.getRuntime().availableProcessors()` — typically the number of vCPUs. This matches CPU-bound carrier work (e.g., running non-pinned virtual thread code between I/O waits). You can override it with `-Djdk.virtualThreadScheduler.parallelism=N` and the maximum pool size with `-Djdk.virtualThreadScheduler.maxPoolSize=N` (default: 256 to bound OS resource use). Changing these is rarely needed and usually signals a pinning problem rather than a configuration problem."},{"id":"java/structured_concurrency_and_loom#d16378277710","section":"java","module":"java/structured_concurrency_and_loom","moduleName":"structured concurrency and loom","sourceFile":"README.md","difficulty":"advanced","question":"How does the JVM handle virtual thread stack growth?","answerFull":"Virtual thread stacks start small (the JDK allocates initial chunks of ~200 bytes or the minimum needed for the first frames) and grow on demand. The stack is stored as a linked list of heap-allocated chunks — there is no pre-committed large allocation. When a method call would overflow the current chunk, the JVM allocates a new chunk and chains it. When frames pop and a chunk is empty, it is released back to the heap. This contrasts with platform thread stacks, which are pre-committed OS memory (typically 256 KB–1 MB). The effective maximum virtual thread stack depth is limited by heap space, not a fixed stack size.","correct":"Virtual thread stacks start small (the JDK allocates initial chunks of ~200 bytes or the minimum needed for the first frames) and grow on demand.","distractors":["With platform thread pools (bounded, e.g., 200 threads), ThreadLocal state is bounded: at most 200 copies of each ThreadLocal value exist.","The default carrier thread pool is a ForkJoinPool with parallelism set to Runtime.getRuntime().availableProcessors() — typically the number of vCPUs.","Pinning occurs when a virtual thread cannot be unmounted from its carrier platform thread."],"distractorIds":["java/structured_concurrency_and_loom#2098d21e5d9d","java/structured_concurrency_and_loom#8114a8a24a4b","java/structured_concurrency_and_loom#fe5c7de81c8f"],"concepts":["growth","stacks","grow","allocates","demand","initial"],"distractorsMd":["With platform thread pools (bounded, e.g., 200 threads), `ThreadLocal` state is bounded: at most 200 copies of each `ThreadLocal` value exist.","The default carrier thread pool is a `ForkJoinPool` with parallelism set to `Runtime.getRuntime().availableProcessors()` — typically the number of vCPUs.",null]},{"id":"java/structured_concurrency_and_loom#48c9b86e1d30","section":"java","module":"java/structured_concurrency_and_loom","moduleName":"structured concurrency and loom","sourceFile":"README.md","difficulty":"advanced","question":"Describe a production incident caused by virtual thread pinning and how to resolve it.","answerFull":"Scenario: A service migrated to spring.threads.virtual.enabled=true on Spring Boot 3.2. Under load, response latency increased from 50 ms to 4 seconds and the carrier pool saturated. Root cause: the service used lettuce Redis client version 6.1, which used synchronized blocks internally when executing commands. With 100 concurrent requests, all 8 carrier threads (= CPU count) were pinned by lettuce's synchronized I/O calls. Resolution: (1) detected via -Djdk.tracePinnedThreads=full showing lettuce frames in stack traces; (2) upgraded lettuce to 6.3+ (rewrote critical sections with StampedLock); (3) as interim fix, increased carrier pool size with -Djdk.virtualThreadScheduler.maxPoolSize=50. Lesson: audit all I/O-touching library dependencies for synchronized on blocking code before migrating to virtual threads.","correct":"Scenario: A service migrated to spring.threads.virtual.enabled=true on Spring Boot 3.2.","distractors":["Virtual thread stacks start small (the JDK allocates initial chunks of ~200 bytes or the minimum needed for the first frames) and grow on demand.","The default carrier thread pool is a ForkJoinPool with parallelism set to Runtime.getRuntime().availableProcessors() — typically the number of vCPUs.","Pinning occurs when a virtual thread cannot be unmounted from its carrier platform thread."],"distractorIds":["java/structured_concurrency_and_loom#d16378277710","java/structured_concurrency_and_loom#8114a8a24a4b","java/structured_concurrency_and_loom#fe5c7de81c8f"],"concepts":["describe","migrated","resolve","pinning","scenario","caused"],"correctMd":"Scenario: A service migrated to `spring.threads.virtual.enabled=true` on Spring Boot 3.2.","answerFullMd":"Scenario: A service migrated to `spring.threads.virtual.enabled=true` on Spring Boot 3.2. Under load, response latency increased from 50 ms to 4 seconds and the carrier pool saturated. Root cause: the service used `lettuce` Redis client version 6.1, which used `synchronized` blocks internally when executing commands. With 100 concurrent requests, all 8 carrier threads (= CPU count) were pinned by lettuce's `synchronized` I/O calls. Resolution: (1) detected via `-Djdk.tracePinnedThreads=full` showing lettuce frames in stack traces; (2) upgraded lettuce to 6.3+ (rewrote critical sections with `StampedLock`); (3) as interim fix, increased carrier pool size with `-Djdk.virtualThreadScheduler.maxPoolSize=50`. Lesson: audit all I/O-touching library dependencies for `synchronized` on blocking code before migrating to virtual threads.","distractorsMd":[null,"The default carrier thread pool is a `ForkJoinPool` with parallelism set to `Runtime.getRuntime().availableProcessors()` — typically the number of vCPUs.",null]},{"id":"java/structured_concurrency_and_loom#835867e5164c","section":"java","module":"java/structured_concurrency_and_loom","moduleName":"structured concurrency and loom","sourceFile":"README.md","difficulty":"advanced","question":"What is Thread.ofVirtual().unstarted() used for?","answerFull":"Thread.ofVirtual().unstarted(Runnable task) creates a virtual thread without starting it. This is useful when you need a Thread reference for passing to APIs that accept Thread (e.g., some testing frameworks, shutdown hooks, or thread-registry monitoring). After obtaining the reference, call .start() to run it. In contrast, Thread.startVirtualThread(task) is the fire-and-forget shortcut. The unstarted() pattern also enables pre-configuring the thread name, daemon status, and UncaughtExceptionHandler before starting — useful for observability in production systems where thread names appear in stack traces and APM tools.","correct":"Thread.ofVirtual().unstarted(Runnable task) creates a virtual thread without starting it.","distractors":["Pinning occurs when a virtual thread cannot be unmounted from its carrier platform thread.","The default carrier thread pool is a ForkJoinPool with parallelism set to Runtime.getRuntime().availableProcessors() — typically the number of vCPUs.","With platform thread pools (bounded, e.g., 200 threads), ThreadLocal state is bounded: at most 200 copies of each ThreadLocal value exist."],"distractorIds":["java/structured_concurrency_and_loom#fe5c7de81c8f","java/structured_concurrency_and_loom#8114a8a24a4b","java/structured_concurrency_and_loom#2098d21e5d9d"],"concepts":["ofvirtual","unstarted","starting","runnable","virtual","creates"],"questionMd":"What is `Thread.ofVirtual().unstarted()` used for?","correctMd":"`Thread.ofVirtual().unstarted(Runnable task)` creates a virtual thread without starting it.","answerFullMd":"`Thread.ofVirtual().unstarted(Runnable task)` creates a virtual thread without starting it. This is useful when you need a `Thread` reference for passing to APIs that accept `Thread` (e.g., some testing frameworks, shutdown hooks, or thread-registry monitoring). After obtaining the reference, call `.start()` to run it. In contrast, `Thread.startVirtualThread(task)` is the fire-and-forget shortcut. The `unstarted()` pattern also enables pre-configuring the thread name, daemon status, and `UncaughtExceptionHandler` before starting — useful for observability in production systems where thread names appear in stack traces and APM tools.","distractorsMd":[null,"The default carrier thread pool is a `ForkJoinPool` with parallelism set to `Runtime.getRuntime().availableProcessors()` — typically the number of vCPUs.","With platform thread pools (bounded, e.g., 200 threads), `ThreadLocal` state is bounded: at most 200 copies of each `ThreadLocal` value exist."]},{"id":"java/testing_junit_mockito#1a97ccb892dc","section":"java","module":"java/testing_junit_mockito","moduleName":"testing junit mockito","sourceFile":"README.md","difficulty":"core","question":"What is the difference between @Mock and @Spy in Mockito?","answerFull":"@Mock creates a complete mock — all methods return defaults (null, 0, false, empty) unless explicitly stubbed. The real class is never called. @Spy wraps a real object — all methods execute their real implementation unless stubbed with doReturn(). Use @Mock when you want to fully control a collaborator and don't need its real behavior. Use @Spy when you want to call the real implementation but override specific methods or verify interactions. Rule: if you find yourself stubbing most methods of a spy, use a mock instead.","correct":"@Mock creates a complete mock — all methods return defaults (null, 0, false, empty) unless explicitly stubbed.","distractors":["@Mock creates a completely fake object — all methods return default values (null, 0, false) unless stubbed.","Do not mock: (1) Value objects and simple data classes — use real instances, they have no side effects.","Mockito tries three strategies in order: (1) Constructor injection — finds the largest constructor whose parameters can be satisfied by available mocks and spies; injects them."],"distractorIds":["java/testing_junit_mockito#09cb83769e5c","java/testing_junit_mockito#655c8fa44e50","java/testing_junit_mockito#3aa376df5561"],"concepts":["stubbed","spy","mockito","difference","defaults","mock"],"questionMd":"What is the difference between `@Mock` and `@Spy` in Mockito?","correctMd":"`@Mock` creates a complete mock — all methods return defaults (null, 0, false, empty) unless explicitly stubbed.","answerFullMd":"`@Mock` creates a complete mock — all methods return defaults (null, 0, false, empty) unless explicitly stubbed. The real class is never called. `@Spy` wraps a real object — all methods execute their real implementation unless stubbed with `doReturn()`. Use `@Mock` when you want to fully control a collaborator and don't need its real behavior. Use `@Spy` when you want to call the real implementation but override specific methods or verify interactions. Rule: if you find yourself stubbing most methods of a spy, use a mock instead.","distractorsMd":["`@Mock` creates a completely fake object — all methods return default values (null, 0, false) unless stubbed.",null,null]},{"id":"java/testing_junit_mockito#3aa376df5561","section":"java","module":"java/testing_junit_mockito","moduleName":"testing junit mockito","sourceFile":"README.md","difficulty":"core","question":"How does @InjectMocks choose an injection strategy?","answerFull":"Mockito tries three strategies in order: (1) Constructor injection — finds the largest constructor whose parameters can be satisfied by available mocks and spies; injects them. (2) Setter injection — calls setters that match mock types and names. (3) Field injection — sets fields directly via reflection that match mock types and names. If constructor injection succeeds, setters and fields are skipped. If none succeeds, an instance is created with the no-arg constructor and injection silently fails (fields remain null). This is a common source of NPEs — prefer constructor injection in production code so @InjectMocks always uses the explicit constructor.","correct":"Mockito tries three strategies in order: (1) Constructor injection — finds the largest constructor whose parameters can be satisfied by available mocks and spies; injects them.","distractors":["@Mock creates a complete mock — all methods return defaults (null, 0, false, empty) unless explicitly stubbed.","@Mock creates a completely fake object — all methods return default values (null, 0, false) unless stubbed.","@Nested defines an inner class inside a test class, with its own lifecycle and grouping."],"distractorIds":["java/testing_junit_mockito#1a97ccb892dc","java/testing_junit_mockito#09cb83769e5c","java/testing_junit_mockito#a4e34e9093c7"],"concepts":["mocks","spies","satisfied","largest","mockito","tries"],"questionMd":"How does `@InjectMocks` choose an injection strategy?","answerFullMd":"Mockito tries three strategies in order: (1) Constructor injection — finds the largest constructor whose parameters can be satisfied by available mocks and spies; injects them. (2) Setter injection — calls setters that match mock types and names. (3) Field injection — sets fields directly via reflection that match mock types and names. If constructor injection succeeds, setters and fields are skipped. If none succeeds, an instance is created with the no-arg constructor and injection silently fails (fields remain null). This is a common source of NPEs — prefer constructor injection in production code so `@InjectMocks` always uses the explicit constructor.","distractorsMd":["`@Mock` creates a complete mock — all methods return defaults (null, 0, false, empty) unless explicitly stubbed.","`@Mock` creates a completely fake object — all methods return default values (null, 0, false) unless stubbed.","`@Nested` defines an inner class inside a test class, with its own lifecycle and grouping."]},{"id":"java/testing_junit_mockito#655c8fa44e50","section":"java","module":"java/testing_junit_mockito","moduleName":"testing junit mockito","sourceFile":"README.md","difficulty":"core","question":"When should you NOT use mocks?","answerFull":"Do not mock: (1) Value objects and simple data classes — use real instances, they have no side effects. (2) The class you're testing (don't mock the subject under test). (3) Standard JDK classes like String, List, Optional — they're simple and safe to use directly. (4) Collaborators where you'd end up mocking the majority of their behavior — that's a sign the design is too tightly coupled; use a fake instead. (5) Third-party APIs you don't own — you might mock them incorrectly; use integration tests or a test server.","correct":"Do not mock: (1) Value objects and simple data classes — use real instances, they have no side effects.","distractors":["verify(mock).method(arg) checks that a method was called with a specific argument — but arg must be a known value or an ArgumentMatcher.","@Mock creates a complete mock — all methods return defaults (null, 0, false, empty) unless explicitly stubbed.","Mockito tries three strategies in order: (1) Constructor injection — finds the largest constructor whose parameters can be satisfied by available mocks and spies; injects them."],"distractorIds":["java/testing_junit_mockito#49dee87fe3f7","java/testing_junit_mockito#1a97ccb892dc","java/testing_junit_mockito#3aa376df5561"],"concepts":["mocks","effects","mock","simple","side","instances"],"answerFullMd":"Do not mock: (1) Value objects and simple data classes — use real instances, they have no side effects. (2) The class you're testing (don't mock the subject under test). (3) Standard JDK classes like `String`, `List`, `Optional` — they're simple and safe to use directly. (4) Collaborators where you'd end up mocking the majority of their behavior — that's a sign the design is too tightly coupled; use a fake instead. (5) Third-party APIs you don't own — you might mock them incorrectly; use integration tests or a test server.","distractorsMd":["`verify(mock).method(arg)` checks that a method was called with a specific argument — but `arg` must be a known value or an `ArgumentMatcher`.","`@Mock` creates a complete mock — all methods return defaults (null, 0, false, empty) unless explicitly stubbed.",null]},{"id":"java/testing_junit_mockito#818afcd7ecc6","section":"java","module":"java/testing_junit_mockito","moduleName":"testing junit mockito","sourceFile":"README.md","difficulty":"core","question":"How do you test concurrent code without races in the tests themselves?","answerFull":"Use CountDownLatch to synchronize thread start and completion: (1) Create a start latch initialized to 1; all worker threads call start.await() before doing work. (2) Create a done latch initialized to N (number of threads); each calls done.countDown() when finished. (3) Test thread calls start.countDown() to release all workers simultaneously, then done.await(timeout) to wait for completion. This maximizes contention (all threads start at the same time) and ensures assertions run only after all threads complete. Also assert with timeout to avoid hanging on deadlocks.","correct":"Use CountDownLatch to synchronize thread start and completion: (1) Create a start latch initialized to 1; all worker threads call start.await() before doing work.","distractors":["@Nested defines an inner class inside a test class, with its own lifecycle and grouping.","@RepeatedTest(n) runs the same test method n times with no argument variation.","Martin Fowler's taxonomy (from xUnit Test Patterns): 1."],"distractorIds":["java/testing_junit_mockito#a4e34e9093c7","java/testing_junit_mockito#e5ecb6b62938","java/testing_junit_mockito#0b700ae7a359"],"concepts":["latch","races","synchronize","countdownlatch","doing","themselves"],"correctMd":"Use `CountDownLatch` to synchronize thread start and completion: (1) Create a start latch initialized to 1; all worker threads call `start.await()` before doing work.","answerFullMd":"Use `CountDownLatch` to synchronize thread start and completion: (1) Create a start latch initialized to 1; all worker threads call `start.await()` before doing work. (2) Create a done latch initialized to N (number of threads); each calls `done.countDown()` when finished. (3) Test thread calls `start.countDown()` to release all workers simultaneously, then `done.await(timeout)` to wait for completion. This maximizes contention (all threads start at the same time) and ensures assertions run only after all threads complete. Also assert with timeout to avoid hanging on deadlocks.","distractorsMd":["`@Nested` defines an inner class inside a test class, with its own lifecycle and grouping.","`@RepeatedTest(n)` runs the same test method n times with no argument variation.",null]},{"id":"java/testing_junit_mockito#55b831b3b220","section":"java","module":"java/testing_junit_mockito","moduleName":"testing junit mockito","sourceFile":"README.md","difficulty":"intermediate","question":"What is the difference between a Stub and a Mock (Meszaros taxonomy)?","answerFull":"Stub: an object that provides canned answers to calls made during the test. The test makes no assertions about how the stub was used. Example: when(repo.findById(1L)).thenReturn(user) — you only care that when asked, it returns the right thing. Mock: a pre-programmed object with expectations about which calls it will receive. The test fails if the mock wasn't called as expected. Example: verify(emailService).send(...) — you're asserting the mock was called. In Mockito, every mock can act as both stub and mock: stub with when(), verify with verify(). Best practice: stub for state verification (check the return value), verify for behavior verification (check interactions).","correct":"Stub: an object that provides canned answers to calls made during the test.","distractors":["@Nested defines an inner class inside a test class, with its own lifecycle and grouping.","@Mock creates a complete mock — all methods return defaults (null, 0, false, empty) unless explicitly stubbed.","@RepeatedTest(n) runs the same test method n times with no argument variation."],"distractorIds":["java/testing_junit_mockito#a4e34e9093c7","java/testing_junit_mockito#1a97ccb892dc","java/testing_junit_mockito#e5ecb6b62938"],"concepts":["taxonomy","canned","stub","difference","mock","made"],"answerFullMd":"Stub: an object that provides canned answers to calls made during the test. The test makes no assertions about how the stub was used. Example: `when(repo.findById(1L)).thenReturn(user)` — you only care that when asked, it returns the right thing. Mock: a pre-programmed object with expectations about which calls it will receive. The test fails if the mock wasn't called as expected. Example: `verify(emailService).send(...)` — you're asserting the mock was called. In Mockito, every mock can act as both stub and mock: stub with `when()`, verify with `verify()`. Best practice: stub for state verification (check the return value), verify for behavior verification (check interactions).","distractorsMd":["`@Nested` defines an inner class inside a test class, with its own lifecycle and grouping.","`@Mock` creates a complete mock — all methods return defaults (null, 0, false, empty) unless explicitly stubbed.","`@RepeatedTest(n)` runs the same test method n times with no argument variation."]},{"id":"java/testing_junit_mockito#6635906d7731","section":"java","module":"java/testing_junit_mockito","moduleName":"testing junit mockito","sourceFile":"README.md","difficulty":"intermediate","question":"How do you test time-dependent code?","answerFull":"The key is to never call Instant.now(), LocalDate.now(), or System.currentTimeMillis() directly in production code. Instead, inject a java.time.Clock dependency. In production, pass Clock.systemUTC(). In tests, pass Clock.fixed(someInstant, someZone). This makes tests fully deterministic and removes any real-time dependency. The Clock.tick() variant lets you simulate a clock that advances at a controlled rate. Alternative for legacy code: extract to a thin wrapper interface and mock that wrapper.","correct":"The key is to never call Instant.now(), LocalDate.now(), or System.currentTimeMillis() directly in production code.","distractors":["Stub: an object that provides canned answers to calls made during the test.","Martin Fowler's taxonomy (from xUnit Test Patterns): 1.","@RepeatedTest(n) runs the same test method n times with no argument variation."],"distractorIds":["java/testing_junit_mockito#55b831b3b220","java/testing_junit_mockito#0b700ae7a359","java/testing_junit_mockito#e5ecb6b62938"],"concepts":["currenttimemillis","localdate","dependent","instant","now","production"],"correctMd":"The key is to never call `Instant.now()`, `LocalDate.now()`, or `System.currentTimeMillis()` directly in production code.","answerFullMd":"The key is to never call `Instant.now()`, `LocalDate.now()`, or `System.currentTimeMillis()` directly in production code. Instead, inject a `java.time.Clock` dependency. In production, pass `Clock.systemUTC()`. In tests, pass `Clock.fixed(someInstant, someZone)`. This makes tests fully deterministic and removes any real-time dependency. The `Clock.tick()` variant lets you simulate a clock that advances at a controlled rate. Alternative for legacy code: extract to a thin wrapper interface and mock that wrapper.","distractorsMd":[null,null,"`@RepeatedTest(n)` runs the same test method n times with no argument variation."]},{"id":"java/testing_junit_mockito#49dee87fe3f7","section":"java","module":"java/testing_junit_mockito","moduleName":"testing junit mockito","sourceFile":"README.md","difficulty":"intermediate","question":"What does ArgumentCaptor provide that verify() doesn't?","answerFull":"verify(mock).method(arg) checks that a method was called with a specific argument — but arg must be a known value or an ArgumentMatcher. ArgumentCaptor<T>.capture() records the actual argument passed during the real call and makes it available after verification via .getValue() (or .getAllValues() for multiple calls). Use ArgumentCaptor when: the argument is a complex object created inside the method under test (you can't predict its exact instance), and you need to assert multiple fields of that object. Example: capturing an EmailMessage built inside service.sendWelcome(userId) to assert its subject, recipients, and attachments.","correct":"verify(mock).method(arg) checks that a method was called with a specific argument — but arg must be a known value or an ArgumentMatcher.","distractors":["Do not mock: (1) Value objects and simple data classes — use real instances, they have no side effects.","@Mock creates a completely fake object — all methods return default values (null, 0, false) unless stubbed.","@Mock creates a complete mock — all methods return defaults (null, 0, false, empty) unless explicitly stubbed."],"distractorIds":["java/testing_junit_mockito#655c8fa44e50","java/testing_junit_mockito#09cb83769e5c","java/testing_junit_mockito#1a97ccb892dc"],"concepts":["argumentmatcher","arg","provide","mock","doesn","verify"],"questionMd":"What does `ArgumentCaptor` provide that `verify()` doesn't?","correctMd":"`verify(mock).method(arg)` checks that a method was called with a specific argument — but `arg` must be a known value or an `ArgumentMatcher`.","answerFullMd":"`verify(mock).method(arg)` checks that a method was called with a specific argument — but `arg` must be a known value or an `ArgumentMatcher`. `ArgumentCaptor<T>.capture()` records the actual argument passed during the real call and makes it available after verification via `.getValue()` (or `.getAllValues()` for multiple calls). Use `ArgumentCaptor` when: the argument is a complex object created inside the method under test (you can't predict its exact instance), and you need to assert multiple fields of that object. Example: capturing an `EmailMessage` built inside `service.sendWelcome(userId)` to assert its subject, recipients, and attachments.","distractorsMd":[null,"`@Mock` creates a completely fake object — all methods return default values (null, 0, false) unless stubbed.","`@Mock` creates a complete mock — all methods return defaults (null, 0, false, empty) unless explicitly stubbed."]},{"id":"java/testing_junit_mockito#dd20cb390456","section":"java","module":"java/testing_junit_mockito","moduleName":"testing junit mockito","sourceFile":"README.md","difficulty":"intermediate","question":"What is assertAll() and when should you use it?","answerFull":"assertAll(heading, Executable... executables) runs all the provided assertions even if some fail, then reports ALL failures at once. Regular assertEquals() stops at the first failure. Use assertAll() when a single test logically validates multiple aspects of one result — e.g., asserting multiple fields of a returned object. Without assertAll(), you'd need one test per field or accept that only the first failure is reported. Do not overuse: if the assertions are logically independent, they belong in separate tests. assertAll() is for \"this single result must satisfy ALL these conditions simultaneously.\"","correct":"assertAll(heading, Executable...","distractors":["@Mock creates a complete mock — all methods return defaults (null, 0, false, empty) unless explicitly stubbed.","@Mock creates a completely fake object — all methods return default values (null, 0, false) unless stubbed.","@Nested defines an inner class inside a test class, with its own lifecycle and grouping."],"distractorIds":["java/testing_junit_mockito#1a97ccb892dc","java/testing_junit_mockito#09cb83769e5c","java/testing_junit_mockito#a4e34e9093c7"],"concepts":["assertall","heading","executable"],"questionMd":"What is `assertAll()` and when should you use it?","answerFullMd":"`assertAll(heading, Executable... executables)` runs all the provided assertions even if some fail, then reports ALL failures at once. Regular `assertEquals()` stops at the first failure. Use `assertAll()` when a single test logically validates multiple aspects of one result — e.g., asserting multiple fields of a returned object. Without `assertAll()`, you'd need one test per field or accept that only the first failure is reported. Do not overuse: if the assertions are logically independent, they belong in separate tests. `assertAll()` is for \"this single result must satisfy ALL these conditions simultaneously.\"","distractorsMd":["`@Mock` creates a complete mock — all methods return defaults (null, 0, false, empty) unless explicitly stubbed.","`@Mock` creates a completely fake object — all methods return default values (null, 0, false) unless stubbed.","`@Nested` defines an inner class inside a test class, with its own lifecycle and grouping."]},{"id":"java/testing_junit_mockito#ca7e73554ce8","section":"java","module":"java/testing_junit_mockito","moduleName":"testing junit mockito","sourceFile":"README.md","difficulty":"intermediate","question":"How do you handle unchecked exceptions thrown inside ExecutorService tasks?","answerFull":"When a Runnable submitted to an ExecutorService throws an unchecked exception, it is silently swallowed — the task dies, the exception is stored in the Future, and nothing else happens. To surface it: (1) Use Future.get() — it throws ExecutionException wrapping the original. (2) Set a Thread.UncaughtExceptionHandler on the ThreadFactory: factory.newThread(r) wraps r in a try-catch that logs/reports. (3) Use CompletableFuture.runAsync() — unhandled exceptions complete the CF exceptionally, accessible via .exceptionally() or .handle(). Always set an uncaught exception handler when using thread pools with Runnables.","correct":"When a Runnable submitted to an ExecutorService throws an unchecked exception, it is silently swallowed — the task dies, the exception is stored in the Future, and nothing else happens.","distractors":["@Nested defines an inner class inside a test class, with its own lifecycle and grouping.","@Mock creates a complete mock — all methods return defaults (null, 0, false, empty) unless explicitly stubbed.","@Mock creates a completely fake object — all methods return default values (null, 0, false) unless stubbed."],"distractorIds":["java/testing_junit_mockito#a4e34e9093c7","java/testing_junit_mockito#1a97ccb892dc","java/testing_junit_mockito#09cb83769e5c"],"concepts":["executorservice","swallowed","unchecked","dies","submitted","thrown"],"questionMd":"How do you handle unchecked exceptions thrown inside `ExecutorService` tasks?","correctMd":"When a `Runnable` submitted to an `ExecutorService` throws an unchecked exception, it is silently swallowed — the task dies, the exception is stored in the `Future`, and nothing else happens.","answerFullMd":"When a `Runnable` submitted to an `ExecutorService` throws an unchecked exception, it is silently swallowed — the task dies, the exception is stored in the `Future`, and nothing else happens. To surface it: (1) Use `Future.get()` — it throws `ExecutionException` wrapping the original. (2) Set a `Thread.UncaughtExceptionHandler` on the `ThreadFactory`: `factory.newThread(r)` wraps `r` in a try-catch that logs/reports. (3) Use `CompletableFuture.runAsync()` — unhandled exceptions complete the CF exceptionally, accessible via `.exceptionally()` or `.handle()`. Always set an uncaught exception handler when using thread pools with `Runnable`s.","distractorsMd":["`@Nested` defines an inner class inside a test class, with its own lifecycle and grouping.","`@Mock` creates a complete mock — all methods return defaults (null, 0, false, empty) unless explicitly stubbed.","`@Mock` creates a completely fake object — all methods return default values (null, 0, false) unless stubbed."]},{"id":"java/testing_junit_mockito#a4e34e9093c7","section":"java","module":"java/testing_junit_mockito","moduleName":"testing junit mockito","sourceFile":"README.md","difficulty":"advanced","question":"What is @Nested and when is it useful?","answerFull":"@Nested defines an inner class inside a test class, with its own lifecycle and grouping. It's useful for expressing hierarchical test structure: group tests by method being tested, or by pre-condition. Example: UserServiceTest has @Nested class FindById with @Nested class WhenUserExists and @Nested class WhenUserDoesNotExist. Each nested class can have its own @BeforeEach setup. The outer class's @BeforeEach runs first, then the nested class's — providing additive setup layers. This makes tests more readable and reflects the structure of the domain.","correct":"@Nested defines an inner class inside a test class, with its own lifecycle and grouping.","distractors":["@RepeatedTest(n) runs the same test method n times with no argument variation.","Stub: an object that provides canned answers to calls made during the test.","Martin Fowler's taxonomy (from xUnit Test Patterns): 1."],"distractorIds":["java/testing_junit_mockito#e5ecb6b62938","java/testing_junit_mockito#55b831b3b220","java/testing_junit_mockito#0b700ae7a359"],"concepts":["grouping","useful","lifecycle","inner","nested","defines"],"questionMd":"What is `@Nested` and when is it useful?","correctMd":"`@Nested` defines an inner class inside a test class, with its own lifecycle and grouping.","answerFullMd":"`@Nested` defines an inner class inside a test class, with its own lifecycle and grouping. It's useful for expressing hierarchical test structure: group tests by method being tested, or by pre-condition. Example: `UserServiceTest` has `@Nested class FindById` with `@Nested class WhenUserExists` and `@Nested class WhenUserDoesNotExist`. Each nested class can have its own `@BeforeEach` setup. The outer class's `@BeforeEach` runs first, then the nested class's — providing additive setup layers. This makes tests more readable and reflects the structure of the domain.","distractorsMd":["`@RepeatedTest(n)` runs the same test method n times with no argument variation.",null,null]},{"id":"java/testing_junit_mockito#e5ecb6b62938","section":"java","module":"java/testing_junit_mockito","moduleName":"testing junit mockito","sourceFile":"README.md","difficulty":"advanced","question":"What is @RepeatedTest useful for and how does it differ from @ParameterizedTest?","answerFull":"@RepeatedTest(n) runs the same test method n times with no argument variation. Use it for: (1) Tests with random inputs (property-based testing style) where you want to exercise many random values. (2) Tests for eventually consistent behavior where you want to verify it passes consistently. (3) Performance tests or flakiness detection runs. @ParameterizedTest is for explicit different inputs — each invocation gets a distinct argument combination. @RepeatedTest is for running the same test repeatedly. Each repetition has a RepetitionInfo parameter you can inject to track which repetition is running.","correct":"@RepeatedTest(n) runs the same test method n times with no argument variation.","distractors":["Martin Fowler's taxonomy (from xUnit Test Patterns): 1.","@Nested defines an inner class inside a test class, with its own lifecycle and grouping.","Stub: an object that provides canned answers to calls made during the test."],"distractorIds":["java/testing_junit_mockito#0b700ae7a359","java/testing_junit_mockito#a4e34e9093c7","java/testing_junit_mockito#55b831b3b220"],"concepts":["repeatedtest","variation","useful","differ","argument","times"],"questionMd":"What is `@RepeatedTest` useful for and how does it differ from `@ParameterizedTest`?","correctMd":"`@RepeatedTest(n)` runs the same test method n times with no argument variation.","answerFullMd":"`@RepeatedTest(n)` runs the same test method n times with no argument variation. Use it for: (1) Tests with random inputs (property-based testing style) where you want to exercise many random values. (2) Tests for eventually consistent behavior where you want to verify it passes consistently. (3) Performance tests or flakiness detection runs. `@ParameterizedTest` is for explicit different inputs — each invocation gets a distinct argument combination. `@RepeatedTest` is for running the same test repeatedly. Each repetition has a `RepetitionInfo` parameter you can inject to track which repetition is running.","distractorsMd":[null,"`@Nested` defines an inner class inside a test class, with its own lifecycle and grouping.",null]},{"id":"java/testing_junit_mockito#09cb83769e5c","section":"java","module":"java/testing_junit_mockito","moduleName":"testing junit mockito","sourceFile":"README.md","difficulty":"advanced","question":"What is the difference between @Mock and @Spy in Mockito, and when should you use each?","answerFull":"@Mock creates a completely fake object — all methods return default values (null, 0, false) unless stubbed. No real code from the original class is ever called. @Spy wraps a real instance — unstubbed methods invoke the real implementation; only explicitly stubbed methods are intercepted. Use @Mock by default for collaborators you want to fully control. Use @Spy when you need the real implementation for most methods but want to intercept specific ones — typically for partial mocking of legacy code or when you need to verify real side effects while controlling one method: java @Mock UserRepository mockRepo; // all methods return null unless stubbed @Spy EmailService spyEmail = new EmailService(); // real methods called unless stubbed // Stub only sendAlert(), let everything else run real doNothing().when(spyEmail).sendAlert(any()); // use doNothing/doReturn with Spy, not when().thenReturn() // BROKEN with Spy: when(spyEmail.sendAlert(any())).thenReturn(...) calls the real method before stubbing","correct":"@Mock creates a completely fake object — all methods return default values (null, 0, false) unless stubbed.","distractors":["@Mock creates a complete mock — all methods return defaults (null, 0, false, empty) unless explicitly stubbed.","Stub: an object that provides canned answers to calls made during the test.","Do not mock: (1) Value objects and simple data classes — use real instances, they have no side effects."],"distractorIds":["java/testing_junit_mockito#1a97ccb892dc","java/testing_junit_mockito#55b831b3b220","java/testing_junit_mockito#655c8fa44e50"],"concepts":["stubbed","fake","spy","mockito","completely","difference"],"questionMd":"What is the difference between `@Mock` and `@Spy` in Mockito, and when should you use each?","correctMd":"`@Mock` creates a completely fake object — all methods return default values (null, 0, false) unless stubbed.","answerFullMd":"`@Mock` creates a completely fake object — all methods return default values (null, 0, false) unless stubbed. No real code from the original class is ever called. `@Spy` wraps a real instance — unstubbed methods invoke the real implementation; only explicitly stubbed methods are intercepted. Use `@Mock` by default for collaborators you want to fully control. Use `@Spy` when you need the real implementation for most methods but want to intercept specific ones — typically for partial mocking of legacy code or when you need to verify real side effects while controlling one method: ```java @Mock UserRepository mockRepo; // all methods return null unless stubbed @Spy EmailService spyEmail = new EmailService(); // real methods called unless stubbed // Stub only sendAlert(), let everything else run real doNothing().when(spyEmail).sendAlert(any()); // use doNothing/doReturn with Spy, not when().thenReturn() // BROKEN with Spy: when(spyEmail.sendAlert(any())).thenReturn(...) calls the real method before stubbing ```","distractorsMd":["`@Mock` creates a complete mock — all methods return defaults (null, 0, false, empty) unless explicitly stubbed.",null,null]},{"id":"java/testing_junit_mockito#0b700ae7a359","section":"java","module":"java/testing_junit_mockito","moduleName":"testing junit mockito","sourceFile":"README.md","difficulty":"advanced","question":"What are the five test double types and when is each appropriate?","answerFull":"Martin Fowler's taxonomy (from xUnit Test Patterns): 1. Dummy — passed as a placeholder, never called. Use for required method parameters that are irrelevant to the test. 2. Stub — returns canned values. Use to isolate the SUT from slow/external collaborators (DB, HTTP). when(repo.findById(1L)).thenReturn(Optional.of(user)). 3. Fake — a working implementation with simplified logic (e.g., HashMap-backed in-memory repository). Use when Stub becomes complex or for integration-style unit tests. 4. Spy — wraps a real object and records calls. Use to verify that specific methods were called on an otherwise real implementation. 5. Mock — a pre-programmed object with strict expectations; fails if unexpected calls happen or expected calls don't occur. Mockito's @Mock is a stub by default; add verify() to make it a mock. Practical rule: prefer stubs for \"tell-don't-ask\" APIs; use mocks (with verify) only for side effects you must assert on (emails sent, events published, DB writes). Over-mocking — asserting on every method call — makes tests brittle to refactoring.","correct":"Martin Fowler's taxonomy (from xUnit Test Patterns): 1.","distractors":["@Nested defines an inner class inside a test class, with its own lifecycle and grouping.","Stub: an object that provides canned answers to calls made during the test.","@RepeatedTest(n) runs the same test method n times with no argument variation."],"distractorIds":["java/testing_junit_mockito#a4e34e9093c7","java/testing_junit_mockito#55b831b3b220","java/testing_junit_mockito#e5ecb6b62938"],"concepts":["xunit","fowler","martin","taxonomy","appropriate","double"],"answerFullMd":"Martin Fowler's taxonomy (from xUnit Test Patterns): 1. **Dummy** — passed as a placeholder, never called. Use for required method parameters that are irrelevant to the test. 2. **Stub** — returns canned values. Use to isolate the SUT from slow/external collaborators (DB, HTTP). `when(repo.findById(1L)).thenReturn(Optional.of(user))`. 3. **Fake** — a working implementation with simplified logic (e.g., `HashMap`-backed in-memory repository). Use when Stub becomes complex or for integration-style unit tests. 4. **Spy** — wraps a real object and records calls. Use to verify that specific methods were called on an otherwise real implementation. 5. **Mock** — a pre-programmed object with strict expectations; fails if unexpected calls happen or expected calls don't occur. Mockito's `@Mock` is a stub by default; add `verify()` to make it a mock. **Practical rule**: prefer stubs for \"tell-don't-ask\" APIs; use mocks (with `verify`) only for side effects you must assert on (emails sent, events published, DB writes). Over-mocking — asserting on every method call — makes tests brittle to refactoring.","distractorsMd":["`@Nested` defines an inner class inside a test class, with its own lifecycle and grouping.",null,"`@RepeatedTest(n)` runs the same test method n times with no argument variation."]}]