[{"id":"backend/api_gateway_patterns#6b1fe9e7da98","section":"backend","module":"backend/api_gateway_patterns","moduleName":"api gateway patterns","qIndex":1,"difficulty":"core","question":"What is an API gateway and what responsibilities does it typically handle?","answerFull":"An API gateway is the single entry point for all external traffic in a microservices architecture. It handles routing (directing requests to the correct service), authentication and authorization offloading (JWT validation before the request reaches a service), rate limiting, SSL termination, request and response transformation, logging, and metrics collection. The key benefit is centralizing cross-cutting concerns that would otherwise be duplicated across every service.","correct":"An API gateway is the single entry point for all external traffic in a microservices architecture.","distractors":["API composition aggregates responses from multiple services into a single response for the client.","Kong is an Nginx-based API gateway with a plugin architecture.","The gateway is a high-value attack target."]},{"id":"backend/api_gateway_patterns#8788e75d5ddb","section":"backend","module":"backend/api_gateway_patterns","moduleName":"api gateway patterns","qIndex":3,"difficulty":"core","question":"How does Spring Cloud Gateway differ from Zuul 1?","answerFull":"Spring Cloud Gateway is fully reactive, built on Project Reactor and Netty, using non-blocking I/O. It handles thousands of concurrent connections with a small, fixed thread pool. Zuul 1 uses a blocking, thread-per-connection model: each request ties up a thread for the duration of the call. Under high concurrency (thousands of concurrent slow upstream services), Zuul 1 exhausts the thread pool and backs up. Spring Cloud Gateway scales better under high concurrency and integrates naturally with reactive Spring WebFlux.","correct":"Spring Cloud Gateway is fully reactive, built on Project Reactor and Netty, using non-blocking I/O.","distractors":["Kong is an Nginx-based API gateway with a plugin architecture.","An API gateway is the single entry point for all external traffic in a microservices architecture.","Spring Cloud Gateway uses the RequestRateLimiter filter backed by Redis."]},{"id":"backend/api_gateway_patterns#eaf7b9d67a9b","section":"backend","module":"backend/api_gateway_patterns","moduleName":"api gateway patterns","qIndex":4,"difficulty":"core","question":"Explain Spring Cloud Gateway predicates and filters.","answerFull":"Predicates are conditions that a request must match for a route to apply: Path=/api/v1/users/, Header=X-API-Version: 2, Method=GET, Host=api.example.com. Filters transform the request before routing or the response after: StripPrefix removes path segments, AddRequestHeader injects headers, CircuitBreaker wraps the route with a circuit breaker, RequestRateLimiter enforces rate limits via Redis, RewritePath transforms the URL. GlobalFilter beans apply to every route, while per-route filters apply only to matching routes.","correct":"Predicates are conditions that a request must match for a route to apply: Path=/api/v1/users/, Header=X-API-Version: 2, Method=GET, Host=api.example.com.","distractors":["Spring Cloud Gateway uses the RequestRateLimiter filter backed by Redis.","Configure a CircuitBreaker filter with a fallbackUri for every route.","Route predicates select routes based on URL path (Path=/api/v2/) or headers (Header=X-API-Version: 2)."]},{"id":"backend/api_gateway_patterns#94109685e8e1","section":"backend","module":"backend/api_gateway_patterns","moduleName":"api gateway patterns","qIndex":5,"difficulty":"core","question":"How do you prevent the API gateway from becoming a single point of failure?","answerFull":"Run multiple gateway instances (minimum 3) behind a load balancer (AWS ALB, Nginx). Each instance is stateless — rate limit state and session state live in Redis. Use circuit breakers on every route so a failing upstream service returns a fallback response rather than causing the gateway to hang. Health checks on each gateway instance allow the load balancer to route around a failed instance. Use rolling deployments so gateway updates do not cause downtime.","correct":"Run multiple gateway instances (minimum 3) behind a load balancer (AWS ALB, Nginx).","distractors":["Kong is an Nginx-based API gateway with a plugin architecture.","Spring Cloud Gateway uses the RequestRateLimiter filter backed by Redis.","Spring Cloud Gateway is fully reactive, built on Project Reactor and Netty, using non-blocking I/O."]},{"id":"backend/api_gateway_patterns#0c7ac1352ff0","section":"backend","module":"backend/api_gateway_patterns","moduleName":"api gateway patterns","qIndex":7,"difficulty":"intermediate","question":"What is API composition at the gateway level?","answerFull":"API composition aggregates responses from multiple services into a single response for the client. For a product detail page, the gateway calls catalog, pricing, inventory, and review services in parallel using reactive composition (Mono.zip), merges the responses, and returns one JSON object to the client. This reduces client round trips from 4 to 1. The tradeoff: the gateway now calls multiple services per request, and the gateway's response time is the maximum of all upstream calls.","correct":"API composition aggregates responses from multiple services into a single response for the client.","distractors":["An API gateway is the single entry point for all external traffic in a microservices architecture.","Route predicates select routes based on URL path (Path=/api/v2/) or headers (Header=X-API-Version: 2).","Predicates are conditions that a request must match for a route to apply: Path=/api/v1/users/, Header=X-API-Version: 2, Method=GET, Host=api.example.com."]},{"id":"backend/api_gateway_patterns#651ae5cdd0de","section":"backend","module":"backend/api_gateway_patterns","moduleName":"api gateway patterns","qIndex":8,"difficulty":"intermediate","question":"How do you implement rate limiting in Spring Cloud Gateway?","answerFull":"Spring Cloud Gateway uses the RequestRateLimiter filter backed by Redis. The token bucket algorithm allows a replenishRate (steady-state tokens added per second) and a burstCapacity (maximum tokens in the bucket, allowing short bursts). A KeyResolver bean determines the rate limit key: by user ID for authenticated endpoints (extracted from the JWT-populated X-User-Id header), by API key for partner endpoints, or by IP for public unauthenticated endpoints. When the bucket is empty, the gateway returns 429 Too Many Requests.","correct":"Spring Cloud Gateway uses the RequestRateLimiter filter backed by Redis.","distractors":["An API gateway is the single entry point for all external traffic in a microservices architecture.","Configure a CircuitBreaker filter with a fallbackUri for every route.","The gateway is a high-value attack target."]},{"id":"backend/api_gateway_patterns#5e1b76002fed","section":"backend","module":"backend/api_gateway_patterns","moduleName":"api gateway patterns","qIndex":10,"difficulty":"intermediate","question":"How do you handle versioning at the API gateway?","answerFull":"Route predicates select routes based on URL path (Path=/api/v2/) or headers (Header=X-API-Version: 2). Version 1 routes to the old service; version 2 routes to a new service or to the same service with a rewritten path. The gateway allows running multiple API versions simultaneously during migration without requiring all clients to upgrade at once. Deprecate old versions by returning a Deprecation response header with the sunset date.","correct":"Route predicates select routes based on URL path (Path=/api/v2/) or headers (Header=X-API-Version: 2).","distractors":["Predicates are conditions that a request must match for a route to apply: Path=/api/v1/users/, Header=X-API-Version: 2, Method=GET, Host=api.example.com.","A Global filter checks for an X-Correlation-ID header.","Kong is an Nginx-based API gateway with a plugin architecture."]},{"id":"backend/api_gateway_patterns#e0396f7287df","section":"backend","module":"backend/api_gateway_patterns","moduleName":"api gateway patterns","qIndex":11,"difficulty":"advanced","question":"What is Kong and how does it differ from Spring Cloud Gateway?","answerFull":"Kong is an Nginx-based API gateway with a plugin architecture. Core functionality (rate limiting, JWT auth, OAuth2, request transformation, logging) is provided by plugins written in Lua. Configuration is declarative via YAML files managed by the deck CLI. Kong is language-agnostic — it sits in front of any backend technology. Spring Cloud Gateway is JVM-native, configured in Java and YAML, integrates deeply with the Spring ecosystem (Eureka, Spring Security, Micrometer), and is better suited for Spring Boot shops that want full programmatic control. Kong is better for polyglot environments where the gateway team wants plugin-based configuration without writing Java code.","correct":"Kong is an Nginx-based API gateway with a plugin architecture.","distractors":["API composition aggregates responses from multiple services into a single response for the client.","Spring Cloud Gateway is fully reactive, built on Project Reactor and Netty, using non-blocking I/O.","Run multiple gateway instances (minimum 3) behind a load balancer (AWS ALB, Nginx)."]},{"id":"backend/api_gateway_patterns#1ceb474ad81d","section":"backend","module":"backend/api_gateway_patterns","moduleName":"api gateway patterns","qIndex":12,"difficulty":"advanced","question":"How does AWS API Gateway work with Lambda authorizers?","answerFull":"A Lambda authorizer is a Lambda function that runs before the backend Lambda (or HTTP endpoint). It receives the request token (JWT, API key, or custom header) and returns an IAM policy document that allows or denies access to the API resource. AWS API Gateway caches the authorizer response for a configurable TTL (300 seconds default). Usage plans and API keys enforce rate limits and quotas per client. This model is fully serverless — no always-on gateway instances to manage.","correct":"A Lambda authorizer is a Lambda function that runs before the backend Lambda (or HTTP endpoint).","distractors":["Spring Cloud Gateway uses the RequestRateLimiter filter backed by Redis.","API composition aggregates responses from multiple services into a single response for the client.","Run multiple gateway instances (minimum 3) behind a load balancer (AWS ALB, Nginx)."]},{"id":"backend/api_gateway_patterns#b32acb342ec5","section":"backend","module":"backend/api_gateway_patterns","moduleName":"api gateway patterns","qIndex":13,"difficulty":"advanced","question":"How do you propagate correlation IDs through the gateway?","answerFull":"A Global filter checks for an X-Correlation-ID header. If absent (i.e., the request originates from an external client), the filter generates a UUID and sets it. If present (internal service-to-service call relayed through the gateway), it reuses the existing ID. The filter adds the correlation ID to the outgoing request headers (forwarded to downstream services), adds it to the response headers (returned to the client for support tickets), and writes it to the MDC for structured logging. All downstream services must propagate this header to their own outbound calls.","correct":"A Global filter checks for an X-Correlation-ID header.","distractors":["Route predicates select routes based on URL path (Path=/api/v2/) or headers (Header=X-API-Version: 2).","Run multiple gateway instances (minimum 3) behind a load balancer (AWS ALB, Nginx).","An API gateway is the single entry point for all external traffic in a microservices architecture."]},{"id":"backend/api_gateway_patterns#d5608a241724","section":"backend","module":"backend/api_gateway_patterns","moduleName":"api gateway patterns","qIndex":14,"difficulty":"advanced","question":"What are the security risks of an API gateway and how do you mitigate them?","answerFull":"The gateway is a high-value attack target. Risks: DDoS (mitigate with rate limiting and WAF), injection via headers or query params (validate and sanitize at the gateway), internal header spoofing (strip X-User-Id and X-User-Roles from incoming external requests before JWT validation; re-set them from validated JWT claims), JWT algorithm confusion attacks (explicitly specify allowed algorithms, reject \"none\" alg). Always run the gateway behind a WAF (AWS WAF, Cloudflare) for layer 7 protection. Run the gateway in a private subnet; expose it only via a load balancer with HTTPS.","correct":"The gateway is a high-value attack target.","distractors":["API composition aggregates responses from multiple services into a single response for the client.","Run multiple gateway instances (minimum 3) behind a load balancer (AWS ALB, Nginx).","Kong is an Nginx-based API gateway with a plugin architecture."]},{"id":"backend/api_gateway_patterns#b35164a2269b","section":"backend","module":"backend/api_gateway_patterns","moduleName":"api gateway patterns","qIndex":15,"difficulty":"advanced","question":"How do you implement graceful degradation when a downstream service is unavailable?","answerFull":"Configure a CircuitBreaker filter with a fallbackUri for every route. The fallback endpoint returns either a cached response (from Redis or an in-memory cache), a degraded response (indicating reduced functionality), or a clear error message. For example, if the recommendation service is down, return an empty recommendations list rather than failing the product page. Design fallback responses with the client's user experience in mind: which features can be silently degraded versus which require explicit error messaging.","correct":"Configure a CircuitBreaker filter with a fallbackUri for every route.","distractors":["Route predicates select routes based on URL path (Path=/api/v2/) or headers (Header=X-API-Version: 2).","Spring Cloud Gateway uses the RequestRateLimiter filter backed by Redis.","Predicates are conditions that a request must match for a route to apply: Path=/api/v1/users/, Header=X-API-Version: 2, Method=GET, Host=api.example.com."]},{"id":"backend/async_and_concurrency_patterns#9ee2e622fc60","section":"backend","module":"backend/async_and_concurrency_patterns","moduleName":"async and concurrency patterns","qIndex":1,"difficulty":"core","question":"How do you size a thread pool for an IO-bound service?","answerFull":"Use the formula: N = N_cpu * (1 + W/C) where W is average wait time and C is average CPU time per request. For a typical service with 50ms database wait and 5ms processing on an 8-core server: N = 8 * (1 + 10) = 88 threads. This keeps all CPU cores busy while threads are waiting for I/O. For virtual threads (Java 21), you do not need to size a pool — one virtual thread per request is the model, and the JVM manages carrier thread allocation.","correct":"Use the formula: N = N_cpu * (1 + W/C) where W is average wait time and C is average CPU time per request.","distractors":["CallerRunsPolicy: when the thread pool is exhausted (queue full, max threads busy), the task is executed by the calling thread instead of the pool thread.","The ForkJoinPool.commonPool() has a fixed parallelism equal to N_cpu - 1.","Active threads (currently executing tasks), queue size (tasks waiting), completed tasks (throughput), rejected tasks (pool overloaded), thread pool utilization = active / pool_size."]},{"id":"backend/async_and_concurrency_patterns#fde1c425ad90","section":"backend","module":"backend/async_and_concurrency_patterns","moduleName":"async and concurrency patterns","qIndex":2,"difficulty":"core","question":"What is the difference between thenApply and thenCompose?","answerFull":"thenApply transforms a CompletableFuture's result with a synchronous function: CF<A> → CF<B>. thenCompose chains an asynchronous function that itself returns a CompletableFuture: CF<A> → CF<B> (where the function returns CF<B>). If you use thenApply with an async function, you get CF<CF<B>> — a nested future that requires .join() to unwrap. thenCompose flattens this: always use thenCompose for functions that return CompletableFuture.","correct":"thenApply transforms a CompletableFuture's result with a synchronous function: CF<A> → CF<B>.","distractors":["CompletableFuture.allOf() starts all futures and waits for all to complete.","Active threads (currently executing tasks), queue size (tasks waiting), completed tasks (throughput), rejected tasks (pool overloaded), thread pool utilization = active / pool_size.","Backpressure is a signal from consumer to producer: \"I can only process N items at this rate.\" Without backpressure, a fast producer overwhelms a slow consumer, causing unbounded memory growth."]},{"id":"backend/async_and_concurrency_patterns#d881e9f40b86","section":"backend","module":"backend/async_and_concurrency_patterns","moduleName":"async and concurrency patterns","qIndex":3,"difficulty":"core","question":"What is virtual thread pinning and how do you detect it?","answerFull":"Virtual thread pinning occurs when a virtual thread is inside a synchronized block (or synchronized method). The JVM cannot unmount a virtual thread from its carrier (platform) thread while the carrier holds the object monitor. The carrier thread blocks until the synchronized block exits. With many pinned virtual threads, carrier threads are all blocked, reducing throughput to platform-thread levels. Detection: -Djdk.tracePinnedThreads=full logs pinning events; JFR jdk.VirtualThreadPinned event records them. Fix: replace synchronized with ReentrantLock.","correct":"Virtual thread pinning occurs when a virtual thread is inside a synchronized block (or synchronized method).","distractors":["Active threads (currently executing tasks), queue size (tasks waiting), completed tasks (throughput), rejected tasks (pool overloaded), thread pool utilization = active / pool_size.","In a ForkJoinPool, each thread has a deque (double-ended queue) of tasks.","CallerRunsPolicy: when the thread pool is exhausted (queue full, max threads busy), the task is executed by the calling thread instead of the pool thread."]},{"id":"backend/async_and_concurrency_patterns#5b39e05d3250","section":"backend","module":"backend/async_and_concurrency_patterns","moduleName":"async and concurrency patterns","qIndex":4,"difficulty":"core","question":"What is the bulkhead pattern and how does it prevent cascade failures?","answerFull":"The bulkhead pattern isolates thread pools per dependency. Each downstream service (database A, external API B) gets a fixed thread pool. When dependency B becomes slow and exhausts its thread pool, only calls to B are affected — calls to A, database C, and other services continue using their own pools. Without bulkhead, all dependencies share a pool: one slow dependency consumes all threads and blocks everything. Resilience4j ThreadPoolBulkhead implements this pattern.","correct":"The bulkhead pattern isolates thread pools per dependency.","distractors":["Virtual thread pinning occurs when a virtual thread is inside a synchronized block (or synchronized method).","Signs: increasing request latency (tasks queuing), increasing error rate (if queue is bounded and rejects), thread dump showing all threads WAITING in queue.take() with many queued tasks.","In a ForkJoinPool, each thread has a deque (double-ended queue) of tasks."]},{"id":"backend/async_and_concurrency_patterns#4bbec34a5f96","section":"backend","module":"backend/async_and_concurrency_patterns","moduleName":"async and concurrency patterns","qIndex":5,"difficulty":"core","question":"Explain backpressure in reactive streams.","answerFull":"Backpressure is a signal from consumer to producer: \"I can only process N items at this rate.\" Without backpressure, a fast producer overwhelms a slow consumer, causing unbounded memory growth. Project Reactor implements backpressure through the Reactive Streams specification: the Subscriber requests N items at a time; the Publisher sends at most N. When the Subscriber's buffer is full, it stops requesting, and the Publisher must hold or drop items. Strategies: BUFFER (store excess), DROP (discard new items), LATEST (keep newest, drop older), ERROR (signal overflow).","correct":"Backpressure is a signal from consumer to producer: \"I can only process N items at this rate.\" Without backpressure, a fast producer overwhelms a slow consumer, causing unbounded memory growth.","distractors":["Reactive Streams (java.util.concurrent.Flow in Java 9+) defines four interfaces: Publisher (produces items), Subscriber (consumes items), Subscription (link between them), Processor (both).","Signs: increasing request latency (tasks queuing), increasing error rate (if queue is bounded and rejects), thread dump showing all threads WAITING in queue.take() with many queued tasks.","Active threads (currently executing tasks), queue size (tasks waiting), completed tasks (throughput), rejected tasks (pool overloaded), thread pool utilization = active / pool_size."]},{"id":"backend/async_and_concurrency_patterns#d3ccb0d333df","section":"backend","module":"backend/async_and_concurrency_patterns","moduleName":"async and concurrency patterns","qIndex":6,"difficulty":"intermediate","question":"What happens when ForkJoinPool.commonPool is saturated?","answerFull":"The ForkJoinPool.commonPool() has a fixed parallelism equal to N_cpu - 1. When all threads are busy with blocking I/O, new tasks submitted to commonPool wait in the queue. CPU-bound parallel operations (parallel streams, ForkJoinTask) also wait. This can cause complete starvation: if IO-bound CompletableFuture tasks fill commonPool, parallel stream computations starve until those futures complete. Always use dedicated, separate thread pools for IO-bound and CPU-bound work.","correct":"The ForkJoinPool.commonPool() has a fixed parallelism equal to N_cpu - 1.","distractors":["In a ForkJoinPool, each thread has a deque (double-ended queue) of tasks.","Use the formula: N = N_cpu * (1 + W/C) where W is average wait time and C is average CPU time per request.","Active threads (currently executing tasks), queue size (tasks waiting), completed tasks (throughput), rejected tasks (pool overloaded), thread pool utilization = active / pool_size."]},{"id":"backend/async_and_concurrency_patterns#e68a963ee8b9","section":"backend","module":"backend/async_and_concurrency_patterns","moduleName":"async and concurrency patterns","qIndex":7,"difficulty":"intermediate","question":"How does structured concurrency differ from CompletableFuture.allOf?","answerFull":"CompletableFuture.allOf() starts all futures and waits for all to complete. If one fails, the returned CF completes exceptionally but the other futures continue running until completion (or cancellation must be manual). Structured concurrency (Java 21 StructuredTaskScope) defines a scope: all forked tasks are owned by the scope. When the scope closes, all tasks must complete. ShutdownOnFailure scope cancels all tasks when any one fails. ShutdownOnSuccess cancels when any one succeeds. Lifetimes are lexically scoped — no orphaned tasks, no resource leaks from forgotten futures.","correct":"CompletableFuture.allOf() starts all futures and waits for all to complete.","distractors":["thenApply transforms a CompletableFuture's result with a synchronous function: CF<A> → CF<B>.","Java 9+ provides orTimeout(long, TimeUnit): if the future does not complete within the specified time, it completes with a TimeoutException.","Active threads (currently executing tasks), queue size (tasks waiting), completed tasks (throughput), rejected tasks (pool overloaded), thread pool utilization = active / pool_size."]},{"id":"backend/async_and_concurrency_patterns#64b9a00f2692","section":"backend","module":"backend/async_and_concurrency_patterns","moduleName":"async and concurrency patterns","qIndex":8,"difficulty":"intermediate","question":"What is work stealing in ForkJoinPool?","answerFull":"In a ForkJoinPool, each thread has a deque (double-ended queue) of tasks. Work stealing allows an idle thread to \"steal\" tasks from the tail of another thread's deque while the owner works on tasks from its own head. This reduces idle time and improves throughput when tasks have unequal sizes. ForkJoinPool is designed for recursive, divide-and-conquer tasks. It is less appropriate for IO-bound work (where threads spend most time waiting, and stealing provides no benefit).","correct":"In a ForkJoinPool, each thread has a deque (double-ended queue) of tasks.","distractors":["The ForkJoinPool.commonPool() has a fixed parallelism equal to N_cpu - 1.","The bulkhead pattern isolates thread pools per dependency.","CallerRunsPolicy: when the thread pool is exhausted (queue full, max threads busy), the task is executed by the calling thread instead of the pool thread."]},{"id":"backend/async_and_concurrency_patterns#469499086db7","section":"backend","module":"backend/async_and_concurrency_patterns","moduleName":"async and concurrency patterns","qIndex":9,"difficulty":"intermediate","question":"How do you implement a timeout for a CompletableFuture?","answerFull":"Java 9+ provides orTimeout(long, TimeUnit): if the future does not complete within the specified time, it completes with a TimeoutException. Or completeOnTimeout(defaultValue, long, TimeUnit): complete with a default value instead of exception. Internally, these use the JVM's delay mechanism (ScheduledExecutorService). java CompletableFuture<User> user = CompletableFuture .supplyAsync(() -> fetchUser(id), ioExecutor) .orTimeout(5, TimeUnit.SECONDS) .exceptionally(ex -> defaultUser(id));","correct":"Java 9+ provides orTimeout(long, TimeUnit): if the future does not complete within the specified time, it completes with a TimeoutException.","distractors":["thenApply transforms a CompletableFuture's result with a synchronous function: CF<A> → CF<B>.","CompletableFuture.allOf() starts all futures and waits for all to complete.","Use the formula: N = N_cpu * (1 + W/C) where W is average wait time and C is average CPU time per request."]},{"id":"backend/async_and_concurrency_patterns#6de030bf7f63","section":"backend","module":"backend/async_and_concurrency_patterns","moduleName":"async and concurrency patterns","qIndex":11,"difficulty":"advanced","question":"What metrics should you monitor for a thread pool?","answerFull":"Active threads (currently executing tasks), queue size (tasks waiting), completed tasks (throughput), rejected tasks (pool overloaded), thread pool utilization = active / pool_size. Alerts: queue > 0 consistently (pool is bottleneck), utilization consistently > 80% (approaching exhaustion), rejection rate > 0 (tasks being dropped). Use Micrometer's ThreadPoolTaskExecutorMetrics or register via executor.recordStats() for a ThreadPoolExecutor.","correct":"Active threads (currently executing tasks), queue size (tasks waiting), completed tasks (throughput), rejected tasks (pool overloaded), thread pool utilization = active / pool_size.","distractors":["Signs: increasing request latency (tasks queuing), increasing error rate (if queue is bounded and rejects), thread dump showing all threads WAITING in queue.take() with many queued tasks.","In a ForkJoinPool, each thread has a deque (double-ended queue) of tasks.","The bulkhead pattern isolates thread pools per dependency."]},{"id":"backend/async_and_concurrency_patterns#9c70ffff6736","section":"backend","module":"backend/async_and_concurrency_patterns","moduleName":"async and concurrency patterns","qIndex":13,"difficulty":"advanced","question":"How do you detect thread pool exhaustion in production?","answerFull":"Signs: increasing request latency (tasks queuing), increasing error rate (if queue is bounded and rejects), thread dump showing all threads WAITING in queue.take() with many queued tasks. Monitor: hikaricp_connections_pending (DB pool), executor_queue_size (task queue depth), executor_active_count approaching executor_pool_size. Alert on: queue depth growing, utilization consistently above 80%, rejection count > 0. Preventive: circuit breakers that stop sending work when downstream is slow.","correct":"Signs: increasing request latency (tasks queuing), increasing error rate (if queue is bounded and rejects), thread dump showing all threads WAITING in queue.take() with many queued tasks.","distractors":["Active threads (currently executing tasks), queue size (tasks waiting), completed tasks (throughput), rejected tasks (pool overloaded), thread pool utilization = active / pool_size.","The bulkhead pattern isolates thread pools per dependency.","CallerRunsPolicy: when the thread pool is exhausted (queue full, max threads busy), the task is executed by the calling thread instead of the pool thread."]},{"id":"backend/async_and_concurrency_patterns#fde21b0ae1c3","section":"backend","module":"backend/async_and_concurrency_patterns","moduleName":"async and concurrency patterns","qIndex":14,"difficulty":"advanced","question":"What is the difference between CallerRunsPolicy and AbortPolicy?","answerFull":"CallerRunsPolicy: when the thread pool is exhausted (queue full, max threads busy), the task is executed by the calling thread instead of the pool thread. This provides backpressure — the caller is blocked executing the task, slowing the rate of task submission. AbortPolicy (default): throws RejectedExecutionException immediately when the pool is exhausted. Use AbortPolicy for fail-fast behavior (return 503 to client). Use CallerRunsPolicy for producer-consumer pipelines where the caller should slow down naturally.","correct":"CallerRunsPolicy: when the thread pool is exhausted (queue full, max threads busy), the task is executed by the calling thread instead of the pool thread.","distractors":["Active threads (currently executing tasks), queue size (tasks waiting), completed tasks (throughput), rejected tasks (pool overloaded), thread pool utilization = active / pool_size.","Signs: increasing request latency (tasks queuing), increasing error rate (if queue is bounded and rejects), thread dump showing all threads WAITING in queue.take() with many queued tasks.","In a ForkJoinPool, each thread has a deque (double-ended queue) of tasks."]},{"id":"backend/async_and_concurrency_patterns#b2ea26093bd4","section":"backend","module":"backend/async_and_concurrency_patterns","moduleName":"async and concurrency patterns","qIndex":15,"difficulty":"advanced","question":"What is the Reactive Streams specification and how does it enable backpressure?","answerFull":"Reactive Streams (java.util.concurrent.Flow in Java 9+) defines four interfaces: Publisher (produces items), Subscriber (consumes items), Subscription (link between them), Processor (both). The protocol: Subscriber.onSubscribe() receives a Subscription. Subscriber calls subscription.request(N) to signal it can receive N items. Publisher calls Subscriber.onNext() at most N times. After consuming N items, Subscriber calls request(N) again. Publisher never sends more than requested — this is backpressure. The Publisher must buffer, drop, or signal error for items exceeding the requested amount.","correct":"Reactive Streams (java.util.concurrent.Flow in Java 9+) defines four interfaces: Publisher (produces items), Subscriber (consumes items), Subscription (link between them), Processor (both).","distractors":["Backpressure is a signal from consumer to producer: \"I can only process N items at this rate.\" Without backpressure, a fast producer overwhelms a slow consumer, causing unbounded memory growth.","Java 9+ provides orTimeout(long, TimeUnit): if the future does not complete within the specified time, it completes with a TimeoutException.","Active threads (currently executing tasks), queue size (tasks waiting), completed tasks (throughput), rejected tasks (pool overloaded), thread pool utilization = active / pool_size."]},{"id":"backend/auth_and_authorization_systems#09be2c9baa02","section":"backend","module":"backend/auth_and_authorization_systems","moduleName":"auth and authorization systems","qIndex":1,"difficulty":"core","question":"What is the difference between OAuth 2.0 and OpenID Connect?","answerFull":"OAuth 2.0 is an authorization framework — it allows an application to obtain delegated access to resources on behalf of a user. It does not define how to authenticate the user or what the user's identity is. OpenID Connect (OIDC) is an identity layer built on top of OAuth 2.0. It adds an ID token (a JWT) that contains verified identity claims (sub, email, name) and standardizes the UserInfo endpoint. In practice: use OAuth 2.0 for API access delegation, use OIDC when you need to know who the user is (login/SSO).","correct":"OAuth 2.0 is an authorization framework — it allows an application to obtain delegated access to resources on behalf of a user.","distractors":["Token introspection is an endpoint on the authorization server that resource servers call to validate an opaque token: POST /introspect, token=<opaque_token>.","Access tokens are short-lived credentials (5–60 minutes) used to call protected APIs.","Include a tenantId claim in the JWT when the user authenticates."]},{"id":"backend/auth_and_authorization_systems#3714f6d38f66","section":"backend","module":"backend/auth_and_authorization_systems","moduleName":"auth and authorization systems","qIndex":2,"difficulty":"core","question":"What is PKCE and why is it required for public clients?","answerFull":"PKCE (Proof Key for Code Exchange) prevents authorization code interception attacks. A public client (browser app, mobile app) cannot hold a client secret securely — the secret would be visible in source code or the app bundle. Without a client secret, anyone who intercepts the authorization code can exchange it for tokens. PKCE replaces the secret with a per-request proof: the client generates a random code_verifier, sends SHA256(code_verifier) as code_challenge in the authorization request, then sends the raw code_verifier in the token exchange. Only the original client knows the code_verifier.","correct":"PKCE (Proof Key for Code Exchange) prevents authorization code interception attacks.","distractors":["OAuth 2.0 is an authorization framework — it allows an application to obtain delegated access to resources on behalf of a user.","Token introspection is an endpoint on the authorization server that resource servers call to validate an opaque token: POST /introspect, token=<opaque_token>.","A JWT claim is a key-value pair in the token payload making a statement about the subject or the token itself."]},{"id":"backend/auth_and_authorization_systems#45813b17d9f4","section":"backend","module":"backend/auth_and_authorization_systems","moduleName":"auth and authorization systems","qIndex":3,"difficulty":"core","question":"Explain the alg:none attack on JWT.","answerFull":"JWT headers contain an alg field. Some early implementations read the algorithm from the token header and then performed signature verification accordingly. If a library accepted alg: none, an attacker could remove the signature, set alg to none, and modify any claim (e.g., change role from user to admin). The library would see alg: none, skip signature verification, and accept the modified token. Prevention: always specify the exact expected algorithm when parsing — never derive it from the token header. Use libraries that require an explicit algorithm parameter and reject none.","correct":"JWT headers contain an alg field.","distractors":["JWT expiry (exp) is an absolute Unix timestamp.","Include a tenantId claim in the JWT when the user authenticates.","A JWT claim is a key-value pair in the token payload making a statement about the subject or the token itself."]},{"id":"backend/auth_and_authorization_systems#63ee4113d783","section":"backend","module":"backend/auth_and_authorization_systems","moduleName":"auth and authorization systems","qIndex":4,"difficulty":"core","question":"What is the difference between access tokens and refresh tokens?","answerFull":"Access tokens are short-lived credentials (5–60 minutes) used to call protected APIs. They should be validated on every API call. Because they are short-lived, if stolen they are only valid briefly. Refresh tokens are long-lived credentials (days to weeks) used only to obtain new access tokens when the current one expires. They are exchanged directly with the authorization server's token endpoint, not sent to resource servers. Refresh tokens must be stored more securely than access tokens and support revocation.","correct":"Access tokens are short-lived credentials (5–60 minutes) used to call protected APIs.","distractors":["In refresh token rotation, each successful use of a refresh token issues a new refresh token and immediately invalidates the old one.","The client credentials flow is used for machine-to-machine API calls where no user is involved.","Token introspection is an endpoint on the authorization server that resource servers call to validate an opaque token: POST /introspect, token=<opaque_token>."]},{"id":"backend/auth_and_authorization_systems#35071e4a5c58","section":"backend","module":"backend/auth_and_authorization_systems","moduleName":"auth and authorization systems","qIndex":5,"difficulty":"core","question":"How does refresh token rotation work and what does it protect against?","answerFull":"In refresh token rotation, each successful use of a refresh token issues a new refresh token and immediately invalidates the old one. If an attacker steals a refresh token and uses it, two events occur: the attacker receives a new token pair, and when the legitimate client's old token is presented, the server detects a token reuse (the old token is no longer valid). At this point, the server revokes the entire token family (all refresh tokens for that session), forcing the user to re-authenticate. This limits the damage of a stolen refresh token to the window between theft and the legitimate client's next use.","correct":"In refresh token rotation, each successful use of a refresh token issues a new refresh token and immediately invalidates the old one.","distractors":["A JWT claim is a key-value pair in the token payload making a statement about the subject or the token itself.","Token introspection is an endpoint on the authorization server that resource servers call to validate an opaque token: POST /introspect, token=<opaque_token>.","Access tokens are short-lived credentials (5–60 minutes) used to call protected APIs."]},{"id":"backend/auth_and_authorization_systems#1350e50405f5","section":"backend","module":"backend/auth_and_authorization_systems","moduleName":"auth and authorization systems","qIndex":6,"difficulty":"intermediate","question":"What is the difference between RBAC and ABAC?","answerFull":"RBAC assigns permissions to roles, and users are assigned roles. Authorization decisions are based solely on which roles the user holds — hasRole('MANAGER'). RBAC is simple, performant, and easy to audit. ABAC makes decisions based on attributes: the subject's attributes (department, clearance level), the resource's attributes (classification, owner, data sensitivity), the action, and the environment (time, location). ABAC is more flexible and can enforce fine-grained, context-aware policies, but requires a policy engine and is more complex to manage.","correct":"RBAC assigns permissions to roles, and users are assigned roles.","distractors":["A JWT claim is a key-value pair in the token payload making a statement about the subject or the token itself.","Access tokens are short-lived credentials (5–60 minutes) used to call protected APIs.","Generate keys using a cryptographically secure random number generator (SecureRandom in Java)."]},{"id":"backend/auth_and_authorization_systems#df0f6827b969","section":"backend","module":"backend/auth_and_authorization_systems","moduleName":"auth and authorization systems","qIndex":7,"difficulty":"intermediate","question":"How do you revoke a JWT before it expires?","answerFull":"JWTs are stateless by design and cannot be revoked by changing a flag in a database — the resource server does not look anything up. Revocation requires either: (1) maintaining a blocklist in Redis of revoked JWT IDs (jti claim); the resource server checks this on every request — one Redis lookup per request; (2) short expiry windows (5–15 minutes) so stolen tokens are naturally short-lived; (3) using opaque tokens instead of JWT for high-security contexts where instant revocation is required. The Redis blocklist approach is the most common for JWTs requiring revocation, with TTLs set to the remaining token lifetime to avoid unbounded growth.","correct":"JWTs are stateless by design and cannot be revoked by changing a flag in a database — the resource server does not look anything up.","distractors":["Include a tenantId claim in the JWT when the user authenticates.","Token introspection is an endpoint on the authorization server that resource servers call to validate an opaque token: POST /introspect, token=<opaque_token>.","JWT headers contain an alg field."]},{"id":"backend/auth_and_authorization_systems#201b7b0ea40a","section":"backend","module":"backend/auth_and_authorization_systems","moduleName":"auth and authorization systems","qIndex":8,"difficulty":"intermediate","question":"How would you secure API keys?","answerFull":"Generate keys using a cryptographically secure random number generator (SecureRandom in Java). Hash the key with SHA-256 before storing in the database. Return the raw key to the user exactly once (at creation); it is never retrievable again. Store only the hash in the DB — treat it like a password. Add a human-readable prefix (e.g., sk_live_) for easy identification in logs and for automated secret scanning tools (GitHub's secret scanning hooks on known prefixes). Scope each key to minimum required permissions. Track last-used timestamp. Allow users to delete/rotate keys. Rate limit by API key. Notify users of unusual geographic or volume patterns.","correct":"Generate keys using a cryptographically secure random number generator (SecureRandom in Java).","distractors":["The client credentials flow is used for machine-to-machine API calls where no user is involved.","A JWT claim is a key-value pair in the token payload making a statement about the subject or the token itself.","Access tokens are short-lived credentials (5–60 minutes) used to call protected APIs."]},{"id":"backend/auth_and_authorization_systems#798e9f5ec0f5","section":"backend","module":"backend/auth_and_authorization_systems","moduleName":"auth and authorization systems","qIndex":9,"difficulty":"intermediate","question":"What is the difference between symmetric and asymmetric JWT signing?","answerFull":"HS256 is symmetric: the same secret key is used to sign and to verify. Any party that can verify can also forge tokens. This works for a single service but fails in microservices — sharing the secret means any service can forge tokens for any other service. RS256 and ES256 are asymmetric: the authorization server signs with its private key; resource servers verify with the public key. Resource servers never have signing capability. The public key is published at a JWKS endpoint. This is the correct approach for distributed systems.","correct":"HS256 is symmetric: the same secret key is used to sign and to verify.","distractors":["JWT headers contain an alg field.","PKCE (Proof Key for Code Exchange) prevents authorization code interception attacks.","JWT expiry (exp) is an absolute Unix timestamp."]},{"id":"backend/auth_and_authorization_systems#f1bfe73e170e","section":"backend","module":"backend/auth_and_authorization_systems","moduleName":"auth and authorization systems","qIndex":10,"difficulty":"intermediate","question":"Explain the OAuth2 client credentials flow and when to use it.","answerFull":"The client credentials flow is used for machine-to-machine API calls where no user is involved. A backend service authenticates using its client_id and client_secret (or a signed JWT assertion) and receives an access token scoped to what that service is allowed to do. Use it for: cron jobs calling an internal API, service A calling service B in a microservices architecture, a data pipeline accessing a storage API. Never use it when a human user's identity or delegated permissions are needed.","correct":"The client credentials flow is used for machine-to-machine API calls where no user is involved.","distractors":["OAuth 2.0 is an authorization framework — it allows an application to obtain delegated access to resources on behalf of a user.","Include a tenantId claim in the JWT when the user authenticates.","Access tokens are short-lived credentials (5–60 minutes) used to call protected APIs."]},{"id":"backend/auth_and_authorization_systems#c3ed4f33cb21","section":"backend","module":"backend/auth_and_authorization_systems","moduleName":"auth and authorization systems","qIndex":11,"difficulty":"advanced","question":"How does OIDC differ from SAML for SSO?","answerFull":"Both enable federated SSO, but use different protocols and token formats. SAML uses XML-based assertions over HTTP POST redirects — heavy, but mature and widely supported by enterprise identity providers (Okta, ADFS, Ping). OIDC uses JSON/JWT over OAuth2 flows — lighter, REST-friendly, designed for modern web and mobile apps. SAML requires specific libraries for XML signature validation; OIDC uses standard HTTP and JWT libraries. For new systems integrating with enterprise SSO, SAML is still commonly required; for consumer or cloud-native apps, OIDC is preferred.","correct":"Both enable federated SSO, but use different protocols and token formats.","distractors":["In refresh token rotation, each successful use of a refresh token issues a new refresh token and immediately invalidates the old one.","Token introspection is an endpoint on the authorization server that resource servers call to validate an opaque token: POST /introspect, token=<opaque_token>.","A JWT claim is a key-value pair in the token payload making a statement about the subject or the token itself."]},{"id":"backend/auth_and_authorization_systems#90d5255a4c34","section":"backend","module":"backend/auth_and_authorization_systems","moduleName":"auth and authorization systems","qIndex":12,"difficulty":"advanced","question":"What is a JWT claim and which claims should you always validate?","answerFull":"A JWT claim is a key-value pair in the token payload making a statement about the subject or the token itself. Always validate: exp (expiration time — reject expired tokens), iat (issued at — reject tokens with iat far in the past if clock skew is a concern), iss (issuer — must match the expected authorization server URL), aud (audience — must include this service's identifier), alg (algorithm — must match expected algorithm, validated before signature check). Additionally validate nbf (not before) if present. Never use a JWT that fails any of these checks.","correct":"A JWT claim is a key-value pair in the token payload making a statement about the subject or the token itself.","distractors":["In refresh token rotation, each successful use of a refresh token issues a new refresh token and immediately invalidates the old one.","PKCE (Proof Key for Code Exchange) prevents authorization code interception attacks.","Include a tenantId claim in the JWT when the user authenticates."]},{"id":"backend/auth_and_authorization_systems#33ea2f11bdd1","section":"backend","module":"backend/auth_and_authorization_systems","moduleName":"auth and authorization systems","qIndex":13,"difficulty":"advanced","question":"How would you implement tenant isolation in a multi-tenant API using JWT?","answerFull":"Include a tenantId claim in the JWT when the user authenticates. In the resource server filter, extract tenantId from the validated token and store it in a request-scoped context (e.g., ThreadLocal or Spring RequestAttributes). Every data access layer method reads tenantId from context and adds it as a WHERE clause condition. Use Spring Data JPA's @Filter or row-level security in PostgreSQL to enforce this automatically. Never accept tenantId as a request parameter from the client — always derive it from the token. Validate that the requested resource's tenantId matches the token's tenantId at the service layer.","correct":"Include a tenantId claim in the JWT when the user authenticates.","distractors":["JWT headers contain an alg field.","JWT expiry (exp) is an absolute Unix timestamp.","A JWT claim is a key-value pair in the token payload making a statement about the subject or the token itself."]},{"id":"backend/auth_and_authorization_systems#c0433356cdbe","section":"backend","module":"backend/auth_and_authorization_systems","moduleName":"auth and authorization systems","qIndex":14,"difficulty":"advanced","question":"What are the security implications of storing a JWT in a cookie vs localStorage?","answerFull":"localStorage is accessible by JavaScript on the same origin. An XSS vulnerability on any page of the application can read and exfiltrate the token. Cookies with HttpOnly flag are not accessible to JavaScript, which prevents XSS-based token theft. However, cookies are automatically sent by the browser on cross-origin requests, making them susceptible to CSRF — mitigated with SameSite=Strict or SameSite=Lax plus a CSRF token for state-changing requests. Best practice: store access tokens in memory (JavaScript variable) for the shortest lived; use HttpOnly; Secure; SameSite=Strict cookies for refresh tokens that need to survive page refresh.","correct":"localStorage is accessible by JavaScript on the same origin.","distractors":["HS256 is symmetric: the same secret key is used to sign and to verify.","JWT headers contain an alg field.","A JWT claim is a key-value pair in the token payload making a statement about the subject or the token itself."]},{"id":"backend/auth_and_authorization_systems#2fe7dc21171e","section":"backend","module":"backend/auth_and_authorization_systems","moduleName":"auth and authorization systems","qIndex":15,"difficulty":"advanced","question":"How do you handle token clock skew between services?","answerFull":"JWT expiry (exp) is an absolute Unix timestamp. If the issuing server and the validating server have different system clocks, a token that is technically valid may be rejected due to a small time difference. Standard practice: accept a configurable clock skew tolerance of 30–60 seconds in the validator (new JWTClaimsSetVerifier.Builder().maximumClockSkew(60)...). Use NTP (Network Time Protocol) to keep server clocks synchronized within a few milliseconds — the clock skew tolerance is a fallback, not the primary mechanism. Kubernetes node clocks are typically synchronized via NTP automatically.","correct":"JWT expiry (exp) is an absolute Unix timestamp.","distractors":["In refresh token rotation, each successful use of a refresh token issues a new refresh token and immediately invalidates the old one.","Include a tenantId claim in the JWT when the user authenticates.","Token introspection is an endpoint on the authorization server that resource servers call to validate an opaque token: POST /introspect, token=<opaque_token>."]},{"id":"backend/auth_and_authorization_systems#88f4530020d6","section":"backend","module":"backend/auth_and_authorization_systems","moduleName":"auth and authorization systems","qIndex":16,"difficulty":"advanced","question":"What is token introspection (RFC 7662) and when is it used?","answerFull":"Token introspection is an endpoint on the authorization server that resource servers call to validate an opaque token: POST /introspect, token=<opaque_token>. The authorization server returns whether the token is active and its associated metadata (scope, sub, exp). It is used when: (1) tokens are opaque (not JWTs) and cannot be validated locally; (2) immediate revocation is required and maintaining a blocklist is impractical; (3) token metadata is too large for a JWT and must be fetched on demand. Downside: every API request requires a call to the introspection endpoint, adding latency and creating a central bottleneck. Cache introspection results with a TTL shorter than the token's expiry to reduce load.","correct":"Token introspection is an endpoint on the authorization server that resource servers call to validate an opaque token: POST /introspect, token=<opaque_token>.","distractors":["PKCE (Proof Key for Code Exchange) prevents authorization code interception attacks.","Access tokens are short-lived credentials (5–60 minutes) used to call protected APIs.","In refresh token rotation, each successful use of a refresh token issues a new refresh token and immediately invalidates the old one."]},{"id":"backend/backend_security_owasp#dedd47f397c2","section":"backend","module":"backend/backend_security_owasp","moduleName":"backend security owasp","qIndex":1,"difficulty":"core","question":"What is the difference between authentication and authorization, and how does OWASP A01 relate?","answerFull":"Authentication verifies identity — who you are. Authorization verifies what you are allowed to do. A01 (Broken Access Control) specifically covers authorization failures: a user is authenticated but can access resources or perform actions beyond their permitted scope. Examples include IDOR, privilege escalation via URL manipulation, and missing function-level access checks.","correct":"Authentication verifies identity — who you are.","distractors":["(1) Authentication: is a valid token required?","CSP is an HTTP response header that specifies which sources the browser is allowed to load scripts, styles, images, and other resources from.","CSRF (Cross-Site Request Forgery) tricks an authenticated user's browser into submitting a state-changing request to a site where the user is logged in, without the user's knowledge."]},{"id":"backend/backend_security_owasp#975104a1244f","section":"backend","module":"backend/backend_security_owasp","moduleName":"backend security owasp","qIndex":2,"difficulty":"core","question":"Explain SQL injection with an example and give two prevention strategies.","answerFull":"SQL injection occurs when user-supplied input is concatenated into an SQL query, allowing the attacker to alter the query's logic. Example: SELECT * FROM users WHERE name = '' OR '1'='1' returns all rows when the attacker supplies ' OR '1'='1. Prevention: (1) use PreparedStatement with parameter binding — the driver escapes input so it is treated as data, not syntax; (2) use an ORM like Hibernate that uses parameterized queries by default. Input validation as a secondary control (allowlist characters) but never as the primary defense.","correct":"SQL injection occurs when user-supplied input is concatenated into an SQL query, allowing the attacker to alter the query's logic.","distractors":["In a dependency confusion attack, an attacker publishes a malicious package to a public registry (npm, PyPI, Maven Central) with the same name as an internal private package, but a higher version number.","SSRF (Server-Side Request Forgery) occurs when an attacker controls a URL that the server fetches, allowing requests to internal services, cloud metadata endpoints (169.254.169.254), or other restricted targets.","CSRF (Cross-Site Request Forgery) tricks an authenticated user's browser into submitting a state-changing request to a site where the user is logged in, without the user's knowledge."]},{"id":"backend/backend_security_owasp#65395e71e79f","section":"backend","module":"backend/backend_security_owasp","moduleName":"backend security owasp","qIndex":3,"difficulty":"core","question":"What is SSRF and how do you prevent it?","answerFull":"SSRF (Server-Side Request Forgery) occurs when an attacker controls a URL that the server fetches, allowing requests to internal services, cloud metadata endpoints (169.254.169.254), or other restricted targets. Prevention: (1) allowlist of permitted target hosts; (2) block private IP ranges after DNS resolution; (3) use an egress proxy with enforced allowlist; (4) apply IMDSv2 on AWS (requires session token, so simple HTTP fetches to metadata endpoint fail).","correct":"SSRF (Server-Side Request Forgery) occurs when an attacker controls a URL that the server fetches, allowing requests to internal services, cloud metadata endpoints (169.254.169.254), or other restricted targets.","distractors":["SQL injection occurs when user-supplied input is concatenated into an SQL query, allowing the attacker to alter the query's logic.","CSRF (Cross-Site Request Forgery) tricks an authenticated user's browser into submitting a state-changing request to a site where the user is logged in, without the user's knowledge.","In a dependency confusion attack, an attacker publishes a malicious package to a public registry (npm, PyPI, Maven Central) with the same name as an internal private package, but a higher version number."]},{"id":"backend/backend_security_owasp#3af43d6937c5","section":"backend","module":"backend/backend_security_owasp","moduleName":"backend security owasp","qIndex":4,"difficulty":"core","question":"Why should you use BCrypt for password hashing rather than SHA-256?","answerFull":"SHA-256 is a fast cryptographic hash designed for high throughput — GPUs can compute 10+ billion SHA-256 hashes per second, making brute-force attacks feasible. BCrypt is a deliberately slow adaptive hash: cost factor 12 means 2^12 = 4096 rounds, yielding ~300-400ms per hash on modern hardware. GPUs cannot parallelize BCrypt efficiently due to its sequential nature. As hardware improves, increase the cost factor to maintain the same work factor. Argon2id is the current recommendation for new systems (winner of the Password Hashing Competition), but BCrypt at cost factor 12 remains secure and widely supported.","correct":"SHA-256 is a fast cryptographic hash designed for high throughput — GPUs can compute 10+ billion SHA-256 hashes per second, making brute-force attacks feasible.","distractors":["(1) Authentication: is a valid token required?","Authentication verifies identity — who you are.","CSP is an HTTP response header that specifies which sources the browser is allowed to load scripts, styles, images, and other resources from."]},{"id":"backend/backend_security_owasp#e0f43bfeab30","section":"backend","module":"backend/backend_security_owasp","moduleName":"backend security owasp","qIndex":5,"difficulty":"core","question":"What is the alg:none attack on JWT and how do you prevent it?","answerFull":"JWT headers contain an alg field. If a server accepts alg: none, an attacker can remove the signature, set alg to none, and modify claims (e.g., elevate role to admin). The server verifies a \"signature\" that is an empty string — which always passes. Prevention: when decoding a JWT, explicitly specify the expected algorithm(s) rather than reading it from the token header. Libraries like java-jwt and nimbus-jose-jwt accept an algorithm parameter; never use overloads that accept any algorithm.","correct":"JWT headers contain an alg field.","distractors":["In a dependency confusion attack, an attacker publishes a malicious package to a public registry (npm, PyPI, Maven Central) with the same name as an internal private package, but a higher version number.","(1) Authentication: is a valid token required?","Authentication verifies identity — who you are."]},{"id":"backend/backend_security_owasp#7eec35cb8296","section":"backend","module":"backend/backend_security_owasp","moduleName":"backend security owasp","qIndex":6,"difficulty":"intermediate","question":"Explain CSRF and two ways to prevent it.","answerFull":"CSRF (Cross-Site Request Forgery) tricks an authenticated user's browser into submitting a state-changing request to a site where the user is logged in, without the user's knowledge. Since the browser automatically includes cookies, the server cannot distinguish the legitimate user from the attacker's forged request. Prevention: (1) SameSite=Strict or SameSite=Lax cookie attribute — browsers refuse to send the session cookie on cross-origin requests; (2) CSRF synchronizer token — a random token included in every state-changing form/request, verified server-side. For REST APIs using Bearer token authentication (no cookies), CSRF is not applicable.","correct":"CSRF (Cross-Site Request Forgery) tricks an authenticated user's browser into submitting a state-changing request to a site where the user is logged in, without the user's knowledge.","distractors":["SQL injection occurs when user-supplied input is concatenated into an SQL query, allowing the attacker to alter the query's logic.","SAST (Static Application Security Testing) analyzes source code or bytecode without executing the application.","SSRF (Server-Side Request Forgery) occurs when an attacker controls a URL that the server fetches, allowing requests to internal services, cloud metadata endpoints (169.254.169.254), or other restricted targets."]},{"id":"backend/backend_security_owasp#0e95d3c3cf0e","section":"backend","module":"backend/backend_security_owasp","moduleName":"backend security owasp","qIndex":7,"difficulty":"intermediate","question":"What is the difference between SAST and DAST?","answerFull":"SAST (Static Application Security Testing) analyzes source code or bytecode without executing the application. It runs in the IDE or CI pipeline against code at rest — detects SQL injection patterns, hardcoded secrets, insecure API usage. Examples: SonarQube, Semgrep. DAST (Dynamic Application Security Testing) tests a running application by sending attack payloads to HTTP endpoints. It finds runtime issues that SAST cannot — authentication bypasses, server-side logic flaws, misconfigured headers. Examples: OWASP ZAP, Burp Suite. Best practice: both in CI, with SAST on every commit and DAST on the deployed staging environment.","correct":"SAST (Static Application Security Testing) analyzes source code or bytecode without executing the application.","distractors":["CSRF (Cross-Site Request Forgery) tricks an authenticated user's browser into submitting a state-changing request to a site where the user is logged in, without the user's knowledge.","(1) Authentication: is a valid token required?","Authentication verifies identity — who you are."]},{"id":"backend/backend_security_owasp#892819479769","section":"backend","module":"backend/backend_security_owasp","moduleName":"backend security owasp","qIndex":8,"difficulty":"intermediate","question":"How would you manage secrets in a Spring Boot microservice deployed to Kubernetes?","answerFull":"Mount secrets from a dedicated secrets manager — not from environment variables baked into container images. Options: (1) HashiCorp Vault with the Vault Agent Injector — injects secrets as files into the pod at startup; Spring Cloud Vault reads them via spring.cloud.vault.kv; (2) AWS Secrets Manager with AWS Secrets and Configuration Provider — mounts secrets as files via a CSI driver; (3) Kubernetes Secrets (encrypted at rest with KMS) — use External Secrets Operator to sync from Vault or AWS SM. Never hardcode secrets in application.properties, never log them, and set JAVA_TOOL_OPTIONS to mask secrets from heap dumps.","correct":"Mount secrets from a dedicated secrets manager — not from environment variables baked into container images.","distractors":["CSP is an HTTP response header that specifies which sources the browser is allowed to load scripts, styles, images, and other resources from.","(1) Authentication: is a valid token required?","Authentication verifies identity — who you are."]},{"id":"backend/backend_security_owasp#1910f48eb084","section":"backend","module":"backend/backend_security_owasp","moduleName":"backend security owasp","qIndex":9,"difficulty":"intermediate","question":"What is dependency confusion / supply chain attack and how do you defend against it?","answerFull":"In a dependency confusion attack, an attacker publishes a malicious package to a public registry (npm, PyPI, Maven Central) with the same name as an internal private package, but a higher version number. Build tools that check public registries first download the malicious package. Defense: (1) pin exact versions and verify checksums; (2) use a private artifact proxy (Nexus, Artifactory) configured to prefer internal packages; (3) publish namespace-protected packages in the public registry to claim the name; (4) use Sigstore/cosign to verify artifact provenance.","correct":"In a dependency confusion attack, an attacker publishes a malicious package to a public registry (npm, PyPI, Maven Central) with the same name as an internal private package, but a higher version number.","distractors":["Verbose error messages expose stack traces, class names, DB table names, SQL queries, framework versions, and internal hostnames — all of which aid an attacker during reconnaissance.","SQL injection occurs when user-supplied input is concatenated into an SQL query, allowing the attacker to alter the query's logic.","SSRF (Server-Side Request Forgery) occurs when an attacker controls a URL that the server fetches, allowing requests to internal services, cloud metadata endpoints (169.254.169.254), or other restricted targets."]},{"id":"backend/backend_security_owasp#6b76d0f0faf6","section":"backend","module":"backend/backend_security_owasp","moduleName":"backend security owasp","qIndex":10,"difficulty":"intermediate","question":"Explain Content Security Policy (CSP) and when it mitigates XSS.","answerFull":"CSP is an HTTP response header that specifies which sources the browser is allowed to load scripts, styles, images, and other resources from. Content-Security-Policy: default-src 'self'; script-src 'self' tells the browser to only execute scripts loaded from the same origin — even if an attacker injects <script src=\"https://evil.com/steal.js\">, the browser refuses to load it. CSP mitigates reflected and stored XSS where the attacker can inject script tags, but does not help if the attacker can inject inline event handlers without unsafe-inline being blocked. The most effective CSP uses nonces or hashes instead of unsafe-inline.","correct":"CSP is an HTTP response header that specifies which sources the browser is allowed to load scripts, styles, images, and other resources from.","distractors":["Mount secrets from a dedicated secrets manager — not from environment variables baked into container images.","SAST (Static Application Security Testing) analyzes source code or bytecode without executing the application.","SSRF (Server-Side Request Forgery) occurs when an attacker controls a URL that the server fetches, allowing requests to internal services, cloud metadata endpoints (169.254.169.254), or other restricted targets."]},{"id":"backend/backend_security_owasp#d1fff2b5504e","section":"backend","module":"backend/backend_security_owasp","moduleName":"backend security owasp","qIndex":11,"difficulty":"advanced","question":"What is the principle of least privilege and give three concrete examples in a backend system?","answerFull":"Least privilege means every entity operates with only the minimum permissions required for its function. Examples: (1) Database account for the user-service has SELECT/INSERT/UPDATE on the users table only — not DROP, not access to other schemas; (2) IAM role for an EC2 instance running the payments service has GetSecret on the specific Secrets Manager ARN, not secretsmanager:*; (3) Kubernetes service account has get/list on its own ConfigMap only, not cluster-wide access. When a component is compromised, least privilege limits the blast radius to what that component actually needed.","correct":"Least privilege means every entity operates with only the minimum permissions required for its function.","distractors":["(1) Authentication: is a valid token required?","Authentication verifies identity — who you are.","CSP is an HTTP response header that specifies which sources the browser is allowed to load scripts, styles, images, and other resources from."]},{"id":"backend/backend_security_owasp#3c2d4dc4aafb","section":"backend","module":"backend/backend_security_owasp","moduleName":"backend security owasp","qIndex":13,"difficulty":"advanced","question":"What are the risks of verbose error messages and how do you handle errors securely?","answerFull":"Verbose error messages expose stack traces, class names, DB table names, SQL queries, framework versions, and internal hostnames — all of which aid an attacker during reconnaissance. Secure error handling: (1) catch all unhandled exceptions at a global handler (@ControllerAdvice with @ExceptionHandler(Exception.class)); (2) log the full stack trace internally with a correlation ID; (3) return only a generic error code and the correlation ID to the client ({\"error\":\"INTERNAL_ERROR\",\"traceId\":\"abc123\"}); (4) never return raw exception messages or stack traces in API responses; (5) configure Spring to disable the /error Whitelabel Error Page in production.","correct":"Verbose error messages expose stack traces, class names, DB table names, SQL queries, framework versions, and internal hostnames — all of which aid an attacker during reconnaissance.","distractors":["SQL injection occurs when user-supplied input is concatenated into an SQL query, allowing the attacker to alter the query's logic.","SSRF (Server-Side Request Forgery) occurs when an attacker controls a URL that the server fetches, allowing requests to internal services, cloud metadata endpoints (169.254.169.254), or other restricted targets.","In a dependency confusion attack, an attacker publishes a malicious package to a public registry (npm, PyPI, Maven Central) with the same name as an internal private package, but a higher version number."]},{"id":"backend/backend_security_owasp#8ae285b973bd","section":"backend","module":"backend/backend_security_owasp","moduleName":"backend security owasp","qIndex":14,"difficulty":"advanced","question":"What is HTTP Strict Transport Security (HSTS) and what is HSTS preloading?","answerFull":"HSTS tells browsers to only access the site over HTTPS for a specified duration. The header Strict-Transport-Security: max-age=31536000; includeSubDomains means: for the next year, never send HTTP requests to this domain or any subdomain — upgrade them to HTTPS automatically. HSTS preloading goes further: the domain is submitted to a browser-maintained list (hstspreload.org) that is shipped with Chrome, Firefox, and Safari. Even on first visit (before the HSTS header is received), the browser uses HTTPS. This eliminates the first-connection vulnerability. Warning: preloading is very difficult to undo — all subdomains must support HTTPS before submitting.","correct":"HSTS tells browsers to only access the site over HTTPS for a specified duration.","distractors":["SAST (Static Application Security Testing) analyzes source code or bytecode without executing the application.","CSRF (Cross-Site Request Forgery) tricks an authenticated user's browser into submitting a state-changing request to a site where the user is logged in, without the user's knowledge.","CSP is an HTTP response header that specifies which sources the browser is allowed to load scripts, styles, images, and other resources from."]},{"id":"backend/backend_security_owasp#1d86b6e8bf7f","section":"backend","module":"backend/backend_security_owasp","moduleName":"backend security owasp","qIndex":15,"difficulty":"advanced","question":"Describe a security review checklist for a new REST API endpoint.","answerFull":"(1) Authentication: is a valid token required? (2) Authorization: does server-side code verify the caller owns the resource? (3) Input validation: are all parameters validated for type, length, format, and range before use? (4) Parameterized queries: no string concatenation in SQL? (5) Output encoding: are responses correctly encoded to prevent XSS if rendered in a browser? (6) Rate limiting: is there a per-user or per-IP rate limit to prevent brute force or abuse? (7) Sensitive data: does the response include fields the caller should not see (PII, hashed passwords, internal IDs)? (8) Error handling: do errors return generic messages? (9) Logging: is there an audit log entry for sensitive actions? (10) SSRF: if this endpoint fetches external URLs, is there an allowlist?","correct":"(1) Authentication: is a valid token required?","distractors":["Authentication verifies identity — who you are.","SAST (Static Application Security Testing) analyzes source code or bytecode without executing the application.","Least privilege means every entity operates with only the minimum permissions required for its function."]},{"id":"backend/backend_testing_strategies#01780bb6f96e","section":"backend","module":"backend/backend_testing_strategies","moduleName":"backend testing strategies","qIndex":1,"difficulty":"core","question":"What is the testing pyramid and why does it matter?","answerFull":"The testing pyramid recommends a distribution: many unit tests (70%), fewer integration tests (20%), minimal E2E tests (10%). Unit tests are fast (milliseconds), isolated, and cheap to maintain. Integration tests are slower (seconds to minutes) but verify real interactions with databases, caches, and queues. E2E tests are slowest and most brittle but confirm complete user flows. The pyramid shape ensures the test suite runs in minutes, giving fast feedback. An inverted pyramid (many E2E, few unit) results in a fragile, slow suite that developers stop running.","correct":"The testing pyramid recommends a distribution: many unit tests (70%), fewer integration tests (20%), minimal E2E tests (10%).","distractors":["(1) Testing implementation: verifying internal method call sequences rather than observable output.","Consumer-driven contract testing (Pact) lets each consumer team define the exact subset of the provider's API they depend on.","Mutation testing automatically introduces bugs (mutations) into the source code — changing > to >=, negating booleans, removing method calls — and then runs the test suite."]},{"id":"backend/backend_testing_strategies#2f22b490e7ac","section":"backend","module":"backend/backend_testing_strategies","moduleName":"backend testing strategies","qIndex":2,"difficulty":"core","question":"What is the difference between a mock and a stub?","answerFull":"A stub provides pre-programmed responses to method calls but does not verify how it was called — it exists only to let the code under test proceed. A mock is pre-programmed with expectations and verifies at the end that specific interactions happened. In Mockito: when(repo.findById(1)).thenReturn(order) is stubbing behavior. verify(emailService).sendEmail(\"user@example.com\") is mock verification. The distinction matters: stubs set up state, mocks verify behavior. Use stubs when you need collaborators to return specific values; use mocks when the test's correctness depends on a specific interaction occurring.","correct":"A stub provides pre-programmed responses to method calls but does not verify how it was called — it exists only to let the code under test proceed.","distractors":["Three approaches: (1) @Transactional on test methods — Spring rolls back the transaction after each test; works well for @DataJpaTest but does not test rollback-related behavior.","When measuring response time in a load test, if the system slows down and the test tool waits for each response before sending the next request, the tool naturally reduces its request rate.","@Mock (Mockito) creates a mock object outside the Spring context."]},{"id":"backend/backend_testing_strategies#ae5c3ebd396c","section":"backend","module":"backend/backend_testing_strategies","moduleName":"backend testing strategies","qIndex":3,"difficulty":"core","question":"What is consumer-driven contract testing and why is it superior to integration testing against a shared environment?","answerFull":"Consumer-driven contract testing (Pact) lets each consumer team define the exact subset of the provider's API they depend on. Consumers write tests that generate a contract file describing expected request/response pairs. The provider runs tests against all consumer contracts in CI. If a provider change would break any consumer contract, the build fails before deployment. This is superior to shared-environment integration testing because: no coordination needed for test environment availability, contracts are versioned in the Pact Broker, provider changes that break consumers are caught immediately, and tests run in parallel against mock consumers.","correct":"Consumer-driven contract testing (Pact) lets each consumer team define the exact subset of the provider's API they depend on.","distractors":["The testing pyramid recommends a distribution: many unit tests (70%), fewer integration tests (20%), minimal E2E tests (10%).","Property-based testing generates hundreds of random inputs satisfying defined constraints and verifies that invariants hold for all of them.","Mutation testing automatically introduces bugs (mutations) into the source code — changing > to >=, negating booleans, removing method calls — and then runs the test suite."]},{"id":"backend/backend_testing_strategies#645be0621333","section":"backend","module":"backend/backend_testing_strategies","moduleName":"backend testing strategies","qIndex":4,"difficulty":"core","question":"What is mutation testing and what does mutation score tell you?","answerFull":"Mutation testing automatically introduces bugs (mutations) into the source code — changing > to >=, negating booleans, removing method calls — and then runs the test suite. If the suite catches the bug (a test fails), the mutant is \"killed.\" Mutation score = killed mutants / total mutants. A score below 70% means your tests are not sensitive enough to catch real bugs. A service can have 90% line coverage with 40% mutation score if tests only assert that methods return non-null. PIT is the standard Java mutation testing tool; it's slow (can take hours for large codebases) so run it on CI nightly, not on every commit.","correct":"Mutation testing automatically introduces bugs (mutations) into the source code — changing > to >=, negating booleans, removing method calls — and then runs the test suite.","distractors":["(1) Testing implementation: verifying internal method call sequences rather than observable output.","A stub provides pre-programmed responses to method calls but does not verify how it was called — it exists only to let the code under test proceed.","Property-based testing generates hundreds of random inputs satisfying defined constraints and verifies that invariants hold for all of them."]},{"id":"backend/backend_testing_strategies#59b875560168","section":"backend","module":"backend/backend_testing_strategies","moduleName":"backend testing strategies","qIndex":5,"difficulty":"core","question":"When should you use @SpringBootTest vs @WebMvcTest vs @DataJpaTest?","answerFull":"@SpringBootTest loads the entire application context including all beans, auto-configurations, and database connections. Use it for full integration tests and E2E tests but expect 10-30 second startup. @WebMvcTest loads only Spring MVC components: controllers, @ControllerAdvice, filters, security configuration, and WebMvcConfigurer. Service and repository beans are not created — you must @MockBean them. Use for testing HTTP request handling, validation, and JSON serialization. @DataJpaTest loads only JPA-related beans: entity manager, repositories, and an embedded or Testcontainers DB. Use for testing repository queries, entity lifecycle, and database constraints without web overhead.","correct":"@SpringBootTest loads the entire application context including all beans, auto-configurations, and database connections.","distractors":["Use @WebMvcTest with @WithMockUser(roles = \"ADMIN\") or @WithMockUser(username = \"user1\") annotations to simulate authenticated users.","Three approaches: (1) @Transactional on test methods — Spring rolls back the transaction after each test; works well for @DataJpaTest but does not test rollback-related behavior.","@Mock (Mockito) creates a mock object outside the Spring context."]},{"id":"backend/backend_testing_strategies#5f69847aa9cc","section":"backend","module":"backend/backend_testing_strategies","moduleName":"backend testing strategies","qIndex":6,"difficulty":"intermediate","question":"What is the coordinated omission problem in performance testing?","answerFull":"When measuring response time in a load test, if the system slows down and the test tool waits for each response before sending the next request, the tool naturally reduces its request rate. This masks the actual latency tail: slow responses cause a gap, then normal-speed responses follow, making the histogram look better than it is. Real users don't wait — they keep arriving regardless of system speed. Tools like wrk2, Gatling with constant arrival rate scenarios, and k6 with constant-arrival-rate executor avoid this by sending requests at a fixed rate regardless of pending responses. Always verify your load test tool uses a fixed arrival rate.","correct":"When measuring response time in a load test, if the system slows down and the test tool waits for each response before sending the next request, the tool naturally reduces its request rate.","distractors":["Consumer-driven contract testing (Pact) lets each consumer team define the exact subset of the provider's API they depend on.","Property-based testing generates hundreds of random inputs satisfying defined constraints and verifies that invariants hold for all of them.","Three approaches: (1) @Transactional on test methods — Spring rolls back the transaction after each test; works well for @DataJpaTest but does not test rollback-related behavior."]},{"id":"backend/backend_testing_strategies#ff2957d48e2a","section":"backend","module":"backend/backend_testing_strategies","moduleName":"backend testing strategies","qIndex":7,"difficulty":"intermediate","question":"How do you test asynchronous behavior (e.g., Kafka consumer processing)?","answerFull":"Use Awaitility for polling assertions: await().atMost(10, SECONDS).pollInterval(500, MILLISECONDS).until(() -> orderRepository.count() == 1). For Kafka consumer tests, use the embedded Kafka from spring-kafka-test (@EmbeddedKafka) or a Testcontainers Kafka container for production-fidelity. Publish a test message to the topic, then assert with Awaitility that the consumer processed it. For unit tests of consumer logic, test the @KafkaListener method directly without Kafka infrastructure. For verifying outbox-to-Kafka flow, use Awaitility after publishing to assert the message appeared on the Kafka topic using a test consumer.","correct":"Use Awaitility for polling assertions: await().atMost(10, SECONDS).pollInterval(500, MILLISECONDS).until(() -> orderRepository.count() == 1).","distractors":["Mutation testing automatically introduces bugs (mutations) into the source code — changing > to >=, negating booleans, removing method calls — and then runs the test suite.","Three approaches: (1) @Transactional on test methods — Spring rolls back the transaction after each test; works well for @DataJpaTest but does not test rollback-related behavior.","Consumer-driven contract testing (Pact) lets each consumer team define the exact subset of the provider's API they depend on."]},{"id":"backend/backend_testing_strategies#364bbf81c14e","section":"backend","module":"backend/backend_testing_strategies","moduleName":"backend testing strategies","qIndex":8,"difficulty":"intermediate","question":"What are the key test anti-patterns to avoid?","answerFull":"(1) Testing implementation: verifying internal method call sequences rather than observable output. (2) Over-mocking: mocking so much that the test only verifies Mockito configuration, not real behavior. (3) Non-deterministic tests: Thread.sleep(), new Date(), Math.random() in test assertions — use Clock injection and Instant.now(fixedClock). (4) Test interdependence: tests relying on order of execution or shared state. (5) Meaningless assertions: assertNotNull(result) when the result could be any non-null value. (6) Ignoring or disabling tests: @Disabled test methods accumulate, giving a false sense of coverage.","correct":"(1) Testing implementation: verifying internal method call sequences rather than observable output.","distractors":["Mutation testing automatically introduces bugs (mutations) into the source code — changing > to >=, negating booleans, removing method calls — and then runs the test suite.","A stub provides pre-programmed responses to method calls but does not verify how it was called — it exists only to let the code under test proceed.","When measuring response time in a load test, if the system slows down and the test tool waits for each response before sending the next request, the tool naturally reduces its request rate."]},{"id":"backend/backend_testing_strategies#f730f32dab64","section":"backend","module":"backend/backend_testing_strategies","moduleName":"backend testing strategies","qIndex":9,"difficulty":"intermediate","question":"How do you achieve test isolation with databases in integration tests?","answerFull":"Three approaches: (1) @Transactional on test methods — Spring rolls back the transaction after each test; works well for @DataJpaTest but does not test rollback-related behavior. (2) @Sql annotation to run cleanup SQL before each test: @Sql(scripts = \"/cleanup.sql\", executionPhase = BEFORE_TEST_METHOD). (3) Testcontainers with database-per-test-class: each test class gets a fresh schema; slower but fully isolated. For Kafka: use unique topic names per test class (include UUID.randomUUID() in topic name) to prevent message contamination between tests.","correct":"Three approaches: (1) @Transactional on test methods — Spring rolls back the transaction after each test; works well for @DataJpaTest but does not test rollback-related behavior.","distractors":["Mutation testing automatically introduces bugs (mutations) into the source code — changing > to >=, negating booleans, removing method calls — and then runs the test suite.","When measuring response time in a load test, if the system slows down and the test tool waits for each response before sending the next request, the tool naturally reduces its request rate.","The testing pyramid recommends a distribution: many unit tests (70%), fewer integration tests (20%), minimal E2E tests (10%)."]},{"id":"backend/backend_testing_strategies#dded2626c606","section":"backend","module":"backend/backend_testing_strategies","moduleName":"backend testing strategies","qIndex":10,"difficulty":"intermediate","question":"What is property-based testing and when should you use it?","answerFull":"Property-based testing generates hundreds of random inputs satisfying defined constraints and verifies that invariants hold for all of them. Instead of assertEquals(15, priceOf(10, 1.5)), you assert for all positive price p and tax rate t in [0, 0.5]: totalPrice(p, t) >= p. Libraries like jqwik (Java) automatically shrink failing examples to the minimal case. Use it for: numeric calculations (pricing, discounts), data format parsing and serialization (any valid input should round-trip correctly), sorting and data structure invariants (sorted list always has first element <= last), and business rule boundaries (order total is always >= 0).","correct":"Property-based testing generates hundreds of random inputs satisfying defined constraints and verifies that invariants hold for all of them.","distractors":["The testing pyramid recommends a distribution: many unit tests (70%), fewer integration tests (20%), minimal E2E tests (10%).","(1) Testing implementation: verifying internal method call sequences rather than observable output.","Mutation testing automatically introduces bugs (mutations) into the source code — changing > to >=, negating booleans, removing method calls — and then runs the test suite."]},{"id":"backend/backend_testing_strategies#ab4cdb69e1a9","section":"backend","module":"backend/backend_testing_strategies","moduleName":"backend testing strategies","qIndex":11,"difficulty":"advanced","question":"How do you test Spring Security configuration?","answerFull":"Use @WebMvcTest with @WithMockUser(roles = \"ADMIN\") or @WithMockUser(username = \"user1\") annotations to simulate authenticated users. For JWT-based auth, create a SecurityMockMvcRequestPostProcessors.jwt() post-processor: mockMvc.perform(get(\"/api/admin\").with(jwt().authorities(new SimpleGrantedAuthority(\"ROLE_ADMIN\")))). Test: authenticated requests to protected endpoints return 200, unauthenticated requests return 401, correctly authenticated but unauthorized requests return 403. Also test CSRF token handling for POST endpoints.","correct":"Use @WebMvcTest with @WithMockUser(roles = \"ADMIN\") or @WithMockUser(username = \"user1\") annotations to simulate authenticated users.","distractors":["Mutation testing automatically introduces bugs (mutations) into the source code — changing > to >=, negating booleans, removing method calls — and then runs the test suite.","When measuring response time in a load test, if the system slows down and the test tool waits for each response before sending the next request, the tool naturally reduces its request rate.","Three approaches: (1) @Transactional on test methods — Spring rolls back the transaction after each test; works well for @DataJpaTest but does not test rollback-related behavior."]},{"id":"backend/backend_testing_strategies#ac829edee97a","section":"backend","module":"backend/backend_testing_strategies","moduleName":"backend testing strategies","qIndex":12,"difficulty":"advanced","question":"What is the difference between @Mock and @MockBean?","answerFull":"@Mock (Mockito) creates a mock object outside the Spring context. It works in unit tests with @ExtendWith(MockitoExtension.class). The mock is injected into the class under test via @InjectMocks. @MockBean (Spring Boot Test) creates a Mockito mock and registers it as a Spring bean in the application context, replacing any existing bean of that type. Use @MockBean in @WebMvcTest and @SpringBootTest to replace real beans (service, repository) with mocks without loading the entire application graph. @MockBean causes Spring to restart its context (unless the same mock configuration is cached), so use it sparingly.","correct":"@Mock (Mockito) creates a mock object outside the Spring context.","distractors":["Three approaches: (1) @Transactional on test methods — Spring rolls back the transaction after each test; works well for @DataJpaTest but does not test rollback-related behavior.","@SpringBootTest loads the entire application context including all beans, auto-configurations, and database connections.","(1) Testing implementation: verifying internal method call sequences rather than observable output."]},{"id":"backend/caching_strategies_deep_dive#bba452f74629","section":"backend","module":"backend/caching_strategies_deep_dive","moduleName":"caching strategies deep dive","qIndex":1,"difficulty":"core","question":"What is cache-aside and how does it differ from read-through?","answerFull":"Cache-aside (lazy loading): the application checks the cache, handles miss by fetching from DB and populating the cache. The application controls all cache interactions. Read-through: the cache itself handles misses by calling the underlying data store — the application only interacts with the cache. Cache-aside gives finer control (only populate what you need); read-through simplifies application code but requires a cache that can call the data store.","correct":"Cache-aside (lazy loading): the application checks the cache, handles miss by fetching from DB and populating the cache.","distractors":["Stripe uses event-driven invalidation: when an object (charge, customer, subscription) is updated, the write handler explicitly evicts the cache key.","Local cache (Caffeine): latency ~100 nanoseconds vs Redis ~1ms.","XFetch (probabilistic early expiry) triggers cache refresh before the item expires."]},{"id":"backend/caching_strategies_deep_dive#45222612a95d","section":"backend","module":"backend/caching_strategies_deep_dive","moduleName":"caching strategies deep dive","qIndex":2,"difficulty":"core","question":"What is a cache stampede (thundering herd) and how do you prevent it?","answerFull":"A cache stampede occurs when a popular cached item expires simultaneously and many concurrent requests all miss the cache, all query the database in parallel, overwhelming it. Prevention: (1) Mutex lock — only one request fetches; others wait and read the result. (2) Probabilistic early expiry (XFetch) — randomly refresh before expiry so the stampede is avoided proactively. (3) Stale-while-revalidate — serve stale content while refreshing in the background.","correct":"A cache stampede occurs when a popular cached item expires simultaneously and many concurrent requests all miss the cache, all query the database in parallel, overwhelming it.","distractors":["Cache-aside (lazy loading): the application checks the cache, handles miss by fetching from DB and populating the cache.","Sorted Set (ZSet) per user: each feed item's ID is the member, timestamp is the score.","Local cache (Caffeine): latency ~100 nanoseconds vs Redis ~1ms."]},{"id":"backend/caching_strategies_deep_dive#1ed77bf5f002","section":"backend","module":"backend/caching_strategies_deep_dive","moduleName":"caching strategies deep dive","qIndex":3,"difficulty":"core","question":"What Redis data structures would you use for a social media feed?","answerFull":"Sorted Set (ZSet) per user: each feed item's ID is the member, timestamp is the score. ZADD for adding new items, ZREVRANGE for reading the most recent N items, ZREMRANGEBYSCORE for removing old items. For large feeds: limit the ZSet to the last 1,000 items. TRIM with ZREMRANGEBYRANK after insert. User ID as part of the key: feed:{userId}.","correct":"Sorted Set (ZSet) per user: each feed item's ID is the member, timestamp is the score.","distractors":["(1) TTL-based: set a short TTL; stale data is eventually evicted.","A cache stampede occurs when a popular cached item expires simultaneously and many concurrent requests all miss the cache, all query the database in parallel, overwhelming it.","A hot key is a single Redis key receiving disproportionately high traffic (millions of ops/second)."]},{"id":"backend/caching_strategies_deep_dive#e6f9fcf64b92","section":"backend","module":"backend/caching_strategies_deep_dive","moduleName":"caching strategies deep dive","qIndex":4,"difficulty":"core","question":"Explain the difference between LRU and LFU eviction.","answerFull":"LRU (Least Recently Used) evicts the item that was accessed least recently — it assumes recently accessed items will be accessed again. LFU (Least Frequently Used) evicts the item accessed least often — it handles cases where an item was accessed frequently historically but is now rarely needed. LFU is better for skewed access patterns where a small set of hot items dominate. LRU is better for temporal locality (recent items tend to be accessed again soon).","correct":"LRU (Least Recently Used) evicts the item that was accessed least recently — it assumes recently accessed items will be accessed again.","distractors":["Stripe uses event-driven invalidation: when an object (charge, customer, subscription) is updated, the write handler explicitly evicts the cache key.","A cache stampede occurs when a popular cached item expires simultaneously and many concurrent requests all miss the cache, all query the database in parallel, overwhelming it.","XFetch (probabilistic early expiry) triggers cache refresh before the item expires."]},{"id":"backend/caching_strategies_deep_dive#4f90126e1f13","section":"backend","module":"backend/caching_strategies_deep_dive","moduleName":"caching strategies deep dive","qIndex":5,"difficulty":"core","question":"How do you invalidate a cache when data changes?","answerFull":"(1) TTL-based: set a short TTL; stale data is eventually evicted. Simple but allows staleness window. (2) Event-driven: the write path explicitly deletes/updates the cache key. Strong consistency but requires all write paths to be aware of the cache. (3) CDC (Change Data Capture): a background process (Debezium) reads DB change logs and invalidates cache keys asynchronously. Decouples invalidation from write path. (4) Write-through: cache and DB always updated together — strongest consistency.","correct":"(1) TTL-based: set a short TTL; stale data is eventually evicted.","distractors":["The HTTP Vary header tells CDNs/proxies to cache separate responses based on specific request headers.","Cache-aside (lazy loading): the application checks the cache, handles miss by fetching from DB and populating the cache.","A cache stampede occurs when a popular cached item expires simultaneously and many concurrent requests all miss the cache, all query the database in parallel, overwhelming it."]},{"id":"backend/caching_strategies_deep_dive#efb3aac6d997","section":"backend","module":"backend/caching_strategies_deep_dive","moduleName":"caching strategies deep dive","qIndex":6,"difficulty":"intermediate","question":"What is a hot key in Redis and how do you solve it?","answerFull":"A hot key is a single Redis key receiving disproportionately high traffic (millions of ops/second). Redis is single-threaded — one key can only be processed as fast as one core allows (~100k simple ops/s). Mitigations: (1) Local read-through cache: each app instance caches the hot key in-process for 1 second, greatly reducing Redis load. (2) Key replication: write to hotkey:1, hotkey:2, ..., hotkey:N; reads randomly pick one replica. (3) Redis Cluster: shard the hotkey across multiple slots. For write-heavy hot keys (counters), use local aggregation and periodic sync.","correct":"A hot key is a single Redis key receiving disproportionately high traffic (millions of ops/second).","distractors":["Standalone: single instance, simple, not HA.","Stripe uses event-driven invalidation: when an object (charge, customer, subscription) is updated, the write handler explicitly evicts the cache key.","Local cache (Caffeine): latency ~100 nanoseconds vs Redis ~1ms."]},{"id":"backend/caching_strategies_deep_dive#aa8efd2d1266","section":"backend","module":"backend/caching_strategies_deep_dive","moduleName":"caching strategies deep dive","qIndex":7,"difficulty":"intermediate","question":"What is the difference between Redis standalone, Sentinel, and Cluster?","answerFull":"Standalone: single instance, simple, not HA. Sentinel: monitoring/failover system — N Sentinel processes watch a primary+replicas; if primary dies, Sentinel elects a replica as new primary. Applications use the Sentinel endpoint for transparent failover. No horizontal scaling. Cluster: shards data across multiple primary nodes (16384 hash slots). Horizontal scale for both read and write. Requires client-side cluster awareness. Use Sentinel for HA with simple data; Cluster for horizontal scaling.","correct":"Standalone: single instance, simple, not HA.","distractors":["A hot key is a single Redis key receiving disproportionately high traffic (millions of ops/second).","Local cache (Caffeine): latency ~100 nanoseconds vs Redis ~1ms.","(1) TTL-based: set a short TTL; stale data is eventually evicted."]},{"id":"backend/caching_strategies_deep_dive#86087e5d376c","section":"backend","module":"backend/caching_strategies_deep_dive","moduleName":"caching strategies deep dive","qIndex":8,"difficulty":"intermediate","question":"When would you use a local in-process cache vs Redis?","answerFull":"Local cache (Caffeine): latency ~100 nanoseconds vs Redis ~1ms. Use for immutable or rarely-changing data (country codes, feature flags, configuration). The cache is per-instance — no sharing across instances. Cache consistency requires invalidation broadcast (Redis Pub/Sub) when data changes. Redis: use for data that must be consistent across instances (session data, rate limit counters, shared state). The network hop (~1ms) is acceptable for most use cases.","correct":"Local cache (Caffeine): latency ~100 nanoseconds vs Redis ~1ms.","distractors":["A hot key is a single Redis key receiving disproportionately high traffic (millions of ops/second).","A cache stampede occurs when a popular cached item expires simultaneously and many concurrent requests all miss the cache, all query the database in parallel, overwhelming it.","Stripe uses event-driven invalidation: when an object (charge, customer, subscription) is updated, the write handler explicitly evicts the cache key."]},{"id":"backend/caching_strategies_deep_dive#b799152c0e0e","section":"backend","module":"backend/caching_strategies_deep_dive","moduleName":"caching strategies deep dive","qIndex":9,"difficulty":"intermediate","question":"What is write-behind (write-back) caching and when is it appropriate?","answerFull":"Write-behind: the application writes to the cache, and the cache writes to the database asynchronously (batched, with delay). The application gets near-instant write acknowledgment. Appropriate for: write-heavy workloads where some data loss is acceptable (analytics events, session data), or writes that can be batched efficiently (many small writes merged into one bulk insert). Not appropriate for: financial transactions, orders, any data where loss is unacceptable.","correct":"Write-behind: the application writes to the cache, and the cache writes to the database asynchronously (batched, with delay).","distractors":["The HTTP Vary header tells CDNs/proxies to cache separate responses based on specific request headers.","Stripe uses event-driven invalidation: when an object (charge, customer, subscription) is updated, the write handler explicitly evicts the cache key.","XFetch (probabilistic early expiry) triggers cache refresh before the item expires."]},{"id":"backend/caching_strategies_deep_dive#6bc1f0ac4220","section":"backend","module":"backend/caching_strategies_deep_dive","moduleName":"caching strategies deep dive","qIndex":10,"difficulty":"intermediate","question":"How does the XFetch algorithm prevent cache stampedes?","answerFull":"XFetch (probabilistic early expiry) triggers cache refresh before the item expires. The probability of early refresh increases as the item approaches expiry: P(refresh) = -β * δ * ln(U) where β = tuning factor (default 1), δ = time to recompute the value, U = uniform random [0,1). As remaining TTL decreases, ln(U) must be smaller (U closer to 1) to trigger refresh — meaning early refresh becomes more likely. Only one request triggers early refresh (the one that \"wins the lottery\"). All others still get the cached value. No lock needed, no stampede.","correct":"XFetch (probabilistic early expiry) triggers cache refresh before the item expires.","distractors":["Cache: session:{token_hash} → {userId, permissions, expires_at}.","The HTTP Vary header tells CDNs/proxies to cache separate responses based on specific request headers.","LRU (Least Recently Used) evicts the item that was accessed least recently — it assumes recently accessed items will be accessed again."]},{"id":"backend/caching_strategies_deep_dive#d52f24584a8a","section":"backend","module":"backend/caching_strategies_deep_dive","moduleName":"caching strategies deep dive","qIndex":11,"difficulty":"advanced","question":"What cache invalidation strategy does Stripe use for their API?","answerFull":"Stripe uses event-driven invalidation: when an object (charge, customer, subscription) is updated, the write handler explicitly evicts the cache key. They also use short TTLs (60–300 seconds) as a safety net. For read paths, they use cache-aside. For their rate limiting implementation, they use Redis Lua scripts for atomic sliding window counters. They do not use write-through because payment objects have complex consistency requirements.","correct":"Stripe uses event-driven invalidation: when an object (charge, customer, subscription) is updated, the write handler explicitly evicts the cache key.","distractors":["Write-behind: the application writes to the cache, and the cache writes to the database asynchronously (batched, with delay).","A cache stampede occurs when a popular cached item expires simultaneously and many concurrent requests all miss the cache, all query the database in parallel, overwhelming it.","Cache-aside (lazy loading): the application checks the cache, handles miss by fetching from DB and populating the cache."]},{"id":"backend/caching_strategies_deep_dive#4a3f7deb51a0","section":"backend","module":"backend/caching_strategies_deep_dive","moduleName":"caching strategies deep dive","qIndex":12,"difficulty":"advanced","question":"How would you design caching for product inventory counts?","answerFull":"Inventory counts must be accurate (cannot oversell). Options: (1) Cache with write-through: all inventory decrements update both DB and cache atomically (using a DB transaction + cache update). (2) Don't cache inventory counts — read from DB with SELECT FOR UPDATE during checkout. (3) Cache with short TTL (5 seconds) for display purposes; always verify from DB before deducting. (4) Redis atomic DECR for inventory with a background sync to DB — Redis as the source of truth for inventory. Option 4 is used by high-scale systems (Redis DECR is atomic, preventing oversell at cache level).","correct":"Inventory counts must be accurate (cannot oversell).","distractors":["(1) TTL-based: set a short TTL; stale data is eventually evicted.","A cache stampede occurs when a popular cached item expires simultaneously and many concurrent requests all miss the cache, all query the database in parallel, overwhelming it.","A hot key is a single Redis key receiving disproportionately high traffic (millions of ops/second)."]},{"id":"backend/caching_strategies_deep_dive#2c2e67a272a9","section":"backend","module":"backend/caching_strategies_deep_dive","moduleName":"caching strategies deep dive","qIndex":13,"difficulty":"advanced","question":"What is the Vary header in HTTP caching and how does it relate to backend caches?","answerFull":"The HTTP Vary header tells CDNs/proxies to cache separate responses based on specific request headers. Vary: Accept-Encoding means: cache one version for gzip clients and one for uncompressed. This applies to CDN/proxy caches. Backend application caches (Redis) do not use HTTP Vary — but the same concept applies: a product response for user A (with their discount) should not be returned for user B. Include the user-specific factors in the cache key: product:{id}:user:{userId} for personalized responses, or product:{id} for public responses.","correct":"The HTTP Vary header tells CDNs/proxies to cache separate responses based on specific request headers.","distractors":["Cache-aside (lazy loading): the application checks the cache, handles miss by fetching from DB and populating the cache.","Write-behind: the application writes to the cache, and the cache writes to the database asynchronously (batched, with delay).","Cache: session:{token_hash} → {userId, permissions, expires_at}."]},{"id":"backend/caching_strategies_deep_dive#4e9637253dad","section":"backend","module":"backend/caching_strategies_deep_dive","moduleName":"caching strategies deep dive","qIndex":14,"difficulty":"advanced","question":"How do you design a cache for user authentication tokens?","answerFull":"Cache: session:{token_hash} → {userId, permissions, expires_at}. TTL = token expiry time. On each request: check cache first (Redis GET); if hit, verify expires_at and use userId. If miss: validate token cryptographically (JWT signature) or look up in DB (opaque token). Revocation: when token is revoked, delete from cache immediately. For JWT: maintain a revocation list in Redis (bloom filter for efficiency: check if token hash is in revocation set before signature verification). Cache hit rate should be >99% — nearly every authenticated request reuses a recently checked token.","correct":"Cache: session:{token_hash} → {userId, permissions, expires_at}.","distractors":["Write-behind: the application writes to the cache, and the cache writes to the database asynchronously (batched, with delay).","Sorted Set (ZSet) per user: each feed item's ID is the member, timestamp is the score.","Local cache (Caffeine): latency ~100 nanoseconds vs Redis ~1ms."]},{"id":"backend/chaos_engineering#1858219764c1","section":"backend","module":"backend/chaos_engineering","moduleName":"chaos engineering","qIndex":1,"difficulty":"core","question":"What is chaos engineering and how does it differ from random testing?","answerFull":"Chaos engineering is a disciplined, hypothesis-driven practice of injecting known fault conditions into a system to verify that resilience mechanisms work as designed. It is not random destruction. Each experiment starts with a steady-state hypothesis (measurable baseline of normal behavior), injects a specific realistic fault, and observes whether the system maintains steady state. Random testing has no hypothesis and no learning objective. Chaos engineering is closer to a scientific experiment: you predict the outcome and learn whether your prediction was correct.","correct":"Chaos engineering is a disciplined, hypothesis-driven practice of injecting known fault conditions into a system to verify that resilience mechanisms work as designed.","distractors":["Circuit breakers, bulkheads, retries, and fallbacks are designed resilience mechanisms.","A Game Day is a scheduled chaos exercise where the on-call team, engineers, and SREs gather to simulate failure scenarios together.","Blameless postmortems where findings from chaos experiments are shared openly."]},{"id":"backend/chaos_engineering#d4be3544f94f","section":"backend","module":"backend/chaos_engineering","moduleName":"chaos engineering","qIndex":3,"difficulty":"core","question":"How do you minimize blast radius in chaos experiments?","answerFull":"Start with the smallest possible scope: one replica of one service, in a staging environment, during off-peak hours. Expand gradually: canary instance (1 of 20 pods), one availability zone, eventually full production. Use traffic shadowing or feature flags to limit which users are affected. Set automated stop conditions: if Prometheus alert fires (error rate > 1%), the chaos agent automatically removes the fault. Define the maximum acceptable blast radius before starting and do not exceed it in a single experiment.","correct":"Start with the smallest possible scope: one replica of one service, in a staging environment, during off-peak hours.","distractors":["A Game Day is a scheduled chaos exercise where the on-call team, engineers, and SREs gather to simulate failure scenarios together.","Start with the most realistic failure modes for your system.","Chaos engineering focuses on small, targeted fault injection to test resilience mechanisms and discover unknown weaknesses."]},{"id":"backend/chaos_engineering#05163839dc8f","section":"backend","module":"backend/chaos_engineering","moduleName":"chaos engineering","qIndex":4,"difficulty":"core","question":"What is a Game Day and how do you run one?","answerFull":"A Game Day is a scheduled chaos exercise where the on-call team, engineers, and SREs gather to simulate failure scenarios together. Before: define hypothesis, establish steady-state baseline, prepare runbook with rollback steps, brief all participants on the plan and their roles. During: chaos operator injects faults; monitor lead watches dashboards; others observe behavior. After: compare actual behavior to hypothesis, document timeline, identify action items. Game Days serve two purposes: discovering system weaknesses and training the team to respond to incidents under controlled conditions.","correct":"A Game Day is a scheduled chaos exercise where the on-call team, engineers, and SREs gather to simulate failure scenarios together.","distractors":["Chaos engineering is a disciplined, hypothesis-driven practice of injecting known fault conditions into a system to verify that resilience mechanisms work as designed.","Blameless postmortems where findings from chaos experiments are shared openly.","Chaos engineering focuses on small, targeted fault injection to test resilience mechanisms and discover unknown weaknesses."]},{"id":"backend/chaos_engineering#9a06ffc4d964","section":"backend","module":"backend/chaos_engineering","moduleName":"chaos engineering","qIndex":5,"difficulty":"core","question":"What types of faults should you inject first?","answerFull":"Start with the most realistic failure modes for your system. For a service that calls three external dependencies, start with: one dependency returning 500 errors, then one dependency with 500ms added latency, then one dependency completely unreachable. These are the most common production failure patterns. Network partitions and instance failures come next. CPU and memory pressure are useful for validating autoscaling and GC behavior. Avoid starting with catastrophic faults (entire region down) until you have confidence in smaller-scope resilience.","correct":"Start with the most realistic failure modes for your system.","distractors":["A Game Day is a scheduled chaos exercise where the on-call team, engineers, and SREs gather to simulate failure scenarios together.","Start with the smallest possible scope: one replica of one service, in a staging environment, during off-peak hours.","Blameless postmortems where findings from chaos experiments are shared openly."]},{"id":"backend/chaos_engineering#bb4a7be93607","section":"backend","module":"backend/chaos_engineering","moduleName":"chaos engineering","qIndex":6,"difficulty":"intermediate","question":"How does chaos engineering relate to resilience patterns like circuit breakers?","answerFull":"Circuit breakers, bulkheads, retries, and fallbacks are designed resilience mechanisms. Chaos engineering tests whether they actually work in practice. Engineers often configure circuit breakers but never see them open in production. A chaos experiment that makes a dependency respond with 100% errors for 2 minutes validates: does the circuit breaker open? At what failure rate? How long does it stay open? Does it transition to half-open correctly? Are fallback responses returned to users? Without chaos testing, circuit breakers can have misconfigured thresholds that never trigger, or fallback paths that have silent bugs.","correct":"Circuit breakers, bulkheads, retries, and fallbacks are designed resilience mechanisms.","distractors":["A Game Day is a scheduled chaos exercise where the on-call team, engineers, and SREs gather to simulate failure scenarios together.","Chaos engineering is a disciplined, hypothesis-driven practice of injecting known fault conditions into a system to verify that resilience mechanisms work as designed.","Chaos engineering focuses on small, targeted fault injection to test resilience mechanisms and discover unknown weaknesses."]},{"id":"backend/chaos_engineering#afb1b0edb149","section":"backend","module":"backend/chaos_engineering","moduleName":"chaos engineering","qIndex":7,"difficulty":"intermediate","question":"What is the difference between chaos engineering and disaster recovery testing?","answerFull":"Chaos engineering focuses on small, targeted fault injection to test resilience mechanisms and discover unknown weaknesses. It runs frequently (weekly or continuously) with limited blast radius. Disaster recovery testing validates recovery procedures for catastrophic failures (data center loss, complete service outage, database corruption). DR testing validates RTOs (Recovery Time Objective) and RPOs (Recovery Point Objective). DR tests run infrequently (quarterly or annually), involve the entire operations team, and often involve actual data restoration from backups. Chaos engineering is ongoing operational practice; DR testing is periodic validation of catastrophic recovery procedures.","correct":"Chaos engineering focuses on small, targeted fault injection to test resilience mechanisms and discover unknown weaknesses.","distractors":["Circuit breakers, bulkheads, retries, and fallbacks are designed resilience mechanisms.","Chaos engineering is a disciplined, hypothesis-driven practice of injecting known fault conditions into a system to verify that resilience mechanisms work as designed.","A Game Day is a scheduled chaos exercise where the on-call team, engineers, and SREs gather to simulate failure scenarios together."]},{"id":"backend/chaos_engineering#ee0d685219ee","section":"backend","module":"backend/chaos_engineering","moduleName":"chaos engineering","qIndex":8,"difficulty":"intermediate","question":"How do you handle chaos experiments that go wrong?","answerFull":"Every experiment must have a documented rollback procedure executable in under 2 minutes. For network faults: tc qdisc del dev eth0 root. For killed processes: kubectl scale deployment order-service --replicas=5. For AWS FIS experiments: the stop condition automatically triggers rollback. For Spring Boot Chaos Monkey: POST /actuator/chaosmonkey/disable. The kill switch must be tested before the experiment (verify rollback works in staging). If the kill switch itself fails, the on-call team must know how to recover manually. All experiments must have an abort trigger based on steady-state violation metrics.","correct":"Every experiment must have a documented rollback procedure executable in under 2 minutes.","distractors":["Chaos engineering focuses on small, targeted fault injection to test resilience mechanisms and discover unknown weaknesses.","Blameless postmortems where findings from chaos experiments are shared openly.","Chaos engineering is a disciplined, hypothesis-driven practice of injecting known fault conditions into a system to verify that resilience mechanisms work as designed."]},{"id":"backend/chaos_engineering#56c5a49b4a6b","section":"backend","module":"backend/chaos_engineering","moduleName":"chaos engineering","qIndex":9,"difficulty":"intermediate","question":"What makes a good chaos engineering culture?","answerFull":"Blameless postmortems where findings from chaos experiments are shared openly. Treating chaos experiment failures as system weaknesses to fix, not human failures. Having engineering teams run their own chaos experiments rather than a separate \"chaos team\" — ownership of resilience belongs to service teams. Celebrating discovered weaknesses: finding a circuit breaker misconfiguration via chaos is much better than finding it during a real incident. Continuous chaos experiments integrated into deployment pipelines so that every significant deployment is validated under fault conditions.","correct":"Blameless postmortems where findings from chaos experiments are shared openly.","distractors":["Chaos engineering is a disciplined, hypothesis-driven practice of injecting known fault conditions into a system to verify that resilience mechanisms work as designed.","A Game Day is a scheduled chaos exercise where the on-call team, engineers, and SREs gather to simulate failure scenarios together.","Chaos engineering focuses on small, targeted fault injection to test resilience mechanisms and discover unknown weaknesses."]},{"id":"backend/connection_pooling_deep_dive#540166ee76fd","section":"backend","module":"backend/connection_pooling_deep_dive","moduleName":"connection pooling deep dive","qIndex":1,"difficulty":"core","question":"What is a connection pool and why is it necessary?","answerFull":"A connection pool pre-establishes and maintains a set of database connections for reuse. Creating a JDBC connection involves TCP handshake, TLS (if SSL is enabled), authentication (username/password), session setup — totaling 20–100ms. For applications handling 100+ requests/second, creating a connection per request is prohibitively expensive. A pool reduces this to microseconds per borrow by reusing established connections.","correct":"A connection pool pre-establishes and maintains a set of database connections for reuse.","distractors":["HikariCP validates connections before handing them out if the connection has been idle for more than 500ms.","The HikariCP formula is: pool_size = (core_count * 2) + effective_spindle_count.","PgBouncer is a PostgreSQL connection pooler that sits between application servers and PostgreSQL."]},{"id":"backend/connection_pooling_deep_dive#34f65f8e7631","section":"backend","module":"backend/connection_pooling_deep_dive","moduleName":"connection pooling deep dive","qIndex":2,"difficulty":"core","question":"How does HikariCP's ConcurrentBag work?","answerFull":"ConcurrentBag is a custom concurrent data structure optimized for borrow/return patterns. It uses three tiers: a ThreadLocal list of previously used connections (first check for fast, uncontested borrow), a CopyOnWriteArrayList of all connections (scanned with CAS operations), and a SynchronousTransferQueue for threads waiting when all connections are in use (direct handoff from returning thread to waiter without queue traversal). This design minimizes lock contention and achieves microsecond borrow times.","correct":"ConcurrentBag is a custom concurrent data structure optimized for borrow/return patterns.","distractors":["HikariCP validates connections before handing them out if the connection has been idle for more than 500ms.","The HikariCP formula is: pool_size = (core_count * 2) + effective_spindle_count.","Use separate HikariCP pools — one for the primary (read-write), one per read replica (read-only)."]},{"id":"backend/connection_pooling_deep_dive#f9248a17f74f","section":"backend","module":"backend/connection_pooling_deep_dive","moduleName":"connection pooling deep dive","qIndex":3,"difficulty":"core","question":"What is the optimal database connection pool size?","answerFull":"The HikariCP formula is: pool_size = (core_count * 2) + effective_spindle_count. For a 4-core database server with SSD (effective_spindle = 1), optimal pool ≈ 9 connections per application server. This is based on research showing that more connections than ~2x cores causes database context switching overhead that reduces overall throughput. The default HikariCP maximumPoolSize of 10 is deliberately conservative and appropriate for most workloads.","correct":"The HikariCP formula is: pool_size = (core_count * 2) + effective_spindle_count.","distractors":["When leakDetectionThreshold is set (e.g., 10000 ms), HikariCP starts a timer when a connection is borrowed.","When all connections are in use (count == maximumPoolSize), new borrow requests wait up to connectionTimeout (default 30s).","Use separate HikariCP pools — one for the primary (read-write), one per read replica (read-only)."]},{"id":"backend/connection_pooling_deep_dive#ebf880cb44aa","section":"backend","module":"backend/connection_pooling_deep_dive","moduleName":"connection pooling deep dive","qIndex":4,"difficulty":"core","question":"What happens when the connection pool exhausts?","answerFull":"When all connections are in use (count == maximumPoolSize), new borrow requests wait up to connectionTimeout (default 30s). If no connection becomes available within that time, HikariCP throws CannotGetJdbcConnectionException. The application should treat this as a 503 Service Unavailable, not a retriable error. Pool exhaustion indicates either the pool is too small (increase if DB can handle it) or queries are too slow (optimize queries or fix downstream issue).","correct":"When all connections are in use (count == maximumPoolSize), new borrow requests wait up to connectionTimeout (default 30s).","distractors":["Database connections are a scarce resource (pool of 10).","HikariCP validates connections before handing them out if the connection has been idle for more than 500ms.","ConcurrentBag is a custom concurrent data structure optimized for borrow/return patterns."]},{"id":"backend/connection_pooling_deep_dive#7703683024f4","section":"backend","module":"backend/connection_pooling_deep_dive","moduleName":"connection pooling deep dive","qIndex":5,"difficulty":"core","question":"How does HikariCP detect connection leaks?","answerFull":"When leakDetectionThreshold is set (e.g., 10000 ms), HikariCP starts a timer when a connection is borrowed. If the connection is not returned within the threshold, HikariCP logs a warning with the stack trace of where the connection was borrowed. This identifies code paths that hold connections too long (transaction spanning external HTTP calls, forgot close, caught exception before finally). The connection continues to function; the leak detection only warns.","correct":"When leakDetectionThreshold is set (e.g., 10000 ms), HikariCP starts a timer when a connection is borrowed.","distractors":["The HikariCP formula is: pool_size = (core_count * 2) + effective_spindle_count.","This warning fires when a borrowed connection is not returned within leakDetectionThreshold milliseconds.","maxLifetime sets the maximum age of a connection in the pool."]},{"id":"backend/connection_pooling_deep_dive#d360b0781dde","section":"backend","module":"backend/connection_pooling_deep_dive","moduleName":"connection pooling deep dive","qIndex":6,"difficulty":"intermediate","question":"What is the maxLifetime setting and why is it important?","answerFull":"maxLifetime sets the maximum age of a connection in the pool. When a connection reaches its maxLifetime, HikariCP retires it and creates a new one. This prevents accumulation of ancient connections that may have accumulated state, and prevents connections from being closed mid-request by load balancers or firewalls that have their own idle connection timeouts. maxLifetime must be set shorter than the database firewall or load balancer idle timeout to avoid getting a connection closed just as it is being handed out.","correct":"maxLifetime sets the maximum age of a connection in the pool.","distractors":["When leakDetectionThreshold is set (e.g., 10000 ms), HikariCP starts a timer when a connection is borrowed.","This warning fires when a borrowed connection is not returned within leakDetectionThreshold milliseconds.","Database connections are a scarce resource (pool of 10)."]},{"id":"backend/connection_pooling_deep_dive#f34d554948ab","section":"backend","module":"backend/connection_pooling_deep_dive","moduleName":"connection pooling deep dive","qIndex":7,"difficulty":"intermediate","question":"What is PgBouncer and when should you use it?","answerFull":"PgBouncer is a PostgreSQL connection pooler that sits between application servers and PostgreSQL. In transaction mode, it borrows a PostgreSQL connection only for the duration of a transaction and returns it immediately — allowing thousands of application connections to multiplex through tens of PostgreSQL connections. Use PgBouncer when you have many application server instances, each with its own HikariCP pool, and the total connection count would exceed PostgreSQL's maximum (~100-500 connections for good performance).","correct":"PgBouncer is a PostgreSQL connection pooler that sits between application servers and PostgreSQL.","distractors":["When leakDetectionThreshold is set (e.g., 10000 ms), HikariCP starts a timer when a connection is borrowed.","This warning fires when a borrowed connection is not returned within leakDetectionThreshold milliseconds.","maxLifetime sets the maximum age of a connection in the pool."]},{"id":"backend/connection_pooling_deep_dive#c95d7dc057f8","section":"backend","module":"backend/connection_pooling_deep_dive","moduleName":"connection pooling deep dive","qIndex":8,"difficulty":"intermediate","question":"How should HikariCP be configured to work behind AWS RDS?","answerFull":"AWS RDS has a connection idle timeout (default varies, but commonly 3600s for RDS Proxy, shorter for direct connections). Configure: maxLifetime = 1740000 (29 min, safely below RDS's 30-min idle timeout), keepaliveTime = 60000 (60s keepalive prevents idle connection death from RDS side), connectionTimeout = 5000 (fail fast, do not wait 30s). For RDS Proxy: the proxy manages connection pooling, so application pool can be smaller; ensure maxLifetime < RDS Proxy's connectionBorrowTimeout.","correct":"AWS RDS has a connection idle timeout (default varies, but commonly 3600s for RDS Proxy, shorter for direct connections).","distractors":["The HikariCP formula is: pool_size = (core_count * 2) + effective_spindle_count.","HikariCP validates connections before handing them out if the connection has been idle for more than 500ms.","Use separate HikariCP pools — one for the primary (read-write), one per read replica (read-only)."]},{"id":"backend/connection_pooling_deep_dive#e88e811d0ecf","section":"backend","module":"backend/connection_pooling_deep_dive","moduleName":"connection pooling deep dive","qIndex":10,"difficulty":"intermediate","question":"Why should you avoid holding connections during external service calls?","answerFull":"Database connections are a scarce resource (pool of 10). If a @Transactional method calls an external HTTP API that takes 500ms, the connection is held idle during that 500ms. With 10 connections and 500ms lock time, the service can only process 10 / 0.5 = 20 requests/second through this code path — even if the database could handle 1,000. This is connection pool starvation from external latency. Keep transactions short: fetch data, close transaction, call external service, open new transaction to save results.","correct":"Database connections are a scarce resource (pool of 10).","distractors":["When all connections are in use (count == maximumPoolSize), new borrow requests wait up to connectionTimeout (default 30s).","AWS RDS has a connection idle timeout (default varies, but commonly 3600s for RDS Proxy, shorter for direct connections).","The HikariCP formula is: pool_size = (core_count * 2) + effective_spindle_count."]},{"id":"backend/connection_pooling_deep_dive#3a306d622ba4","section":"backend","module":"backend/connection_pooling_deep_dive","moduleName":"connection pooling deep dive","qIndex":11,"difficulty":"advanced","question":"How does connection validation work in HikariCP?","answerFull":"HikariCP validates connections before handing them out if the connection has been idle for more than 500ms. Validation uses JDBC4's Connection.isValid(timeoutSeconds) which is a lightweight network round-trip to check liveness. If isValid() returns false, the connection is closed and a new one is created. For drivers that do not support JDBC4 isValid(), set connectionTestQuery = \"SELECT 1\". With keepaliveTime set, HikariCP also validates idle connections periodically, proactively replacing stale ones before they are borrowed.","correct":"HikariCP validates connections before handing them out if the connection has been idle for more than 500ms.","distractors":["Database connections are a scarce resource (pool of 10).","The HikariCP formula is: pool_size = (core_count * 2) + effective_spindle_count.","Use separate HikariCP pools — one for the primary (read-write), one per read replica (read-only)."]},{"id":"backend/connection_pooling_deep_dive#634b83548e73","section":"backend","module":"backend/connection_pooling_deep_dive","moduleName":"connection pooling deep dive","qIndex":12,"difficulty":"advanced","question":"What causes \"Apparent connection leak detected\" in HikariCP?","answerFull":"This warning fires when a borrowed connection is not returned within leakDetectionThreshold milliseconds. Common causes: (1) Code path exits via exception without closing the connection (fix: try-with-resources); (2) Long-running transaction (heavy computation or external calls inside @Transactional, fix: minimize transaction scope); (3) Forgotten close in unit tests (fix: @Transactional on test method or explicit cleanup). The stack trace in the warning points to the exact location where the connection was borrowed.","correct":"This warning fires when a borrowed connection is not returned within leakDetectionThreshold milliseconds.","distractors":["Use separate HikariCP pools — one for the primary (read-write), one per read replica (read-only).","A connection pool pre-establishes and maintains a set of database connections for reuse.","HikariCP validates connections before handing them out if the connection has been idle for more than 500ms."]},{"id":"backend/connection_pooling_deep_dive#6c124bb431ae","section":"backend","module":"backend/connection_pooling_deep_dive","moduleName":"connection pooling deep dive","qIndex":13,"difficulty":"advanced","question":"How does read replica routing work with connection pooling?","answerFull":"Use separate HikariCP pools — one for the primary (read-write), one per read replica (read-only). Route read-only queries (SELECT without transaction) to the read pool and writes to the primary pool. In Spring: configure two DataSources and use an AbstractRoutingDataSource with a ThreadLocal to switch. Alternatively, PgBouncer or ProxySQL can do read/write splitting at the proxy layer based on SQL parsing. Ensure the read pool's maxLifetime is shorter than the replication lag threshold (a connection pointing to a replica that has fallen behind should be detected).","correct":"Use separate HikariCP pools — one for the primary (read-write), one per read replica (read-only).","distractors":["HikariCP validates connections before handing them out if the connection has been idle for more than 500ms.","A connection pool pre-establishes and maintains a set of database connections for reuse.","maxLifetime sets the maximum age of a connection in the pool."]},{"id":"backend/container_and_deployment_patterns#99eeb8cfaac0","section":"backend","module":"backend/container_and_deployment_patterns","moduleName":"container and deployment patterns","qIndex":1,"difficulty":"core","question":"What is a Docker multi-stage build and why do you use it?","answerFull":"A multi-stage build uses multiple FROM instructions in a Dockerfile, each creating an intermediate image. You build the application in a JDK image (full development tools), then copy only the compiled artifacts to a JRE-only runtime image. The final image does not contain the JDK, Maven, source code, or test dependencies — only the JRE and the compiled JAR. This reduces image size from ~800MB (JDK) to ~180MB (JRE-only), reduces attack surface (no compiler/package manager in production), and speeds up image pulls. Layer caching: copy pom.xml and download dependencies before copying source code; the dependency layer is cached and only re-built when pom.xml changes.","correct":"A multi-stage build uses multiple FROM instructions in a Dockerfile, each creating an intermediate image.","distractors":["A PDB specifies the minimum number (or percentage) of pods of a replicated application that must be available at any time.","Always use -XX:+UseContainerSupport (default in Java 11+) to make the JVM container-aware — it reads cgroup memory limits instead of host total RAM.","CPU requests are used by the scheduler to find a node with sufficient available CPU — the pod is guaranteed this amount."]},{"id":"backend/container_and_deployment_patterns#e14ae6cbfd1e","section":"backend","module":"backend/container_and_deployment_patterns","moduleName":"container and deployment patterns","qIndex":2,"difficulty":"core","question":"What is the difference between maxUnavailable and maxSurge in rolling updates?","answerFull":"maxUnavailable sets the maximum number of pods that can be unavailable during the rolling update. Setting it to 0 means no pod goes down until a replacement is ready and passing readiness checks — guarantees zero downtime but requires capacity for surge pods. maxSurge sets the maximum number of extra pods beyond the desired replica count. Setting maxUnavailable=0, maxSurge=1 means: start a new pod (6 total), wait for it to be ready, then terminate an old pod (back to 5) — one pod at a time, always at least 5 healthy. maxUnavailable=1, maxSurge=0 means: terminate one old pod first (4 total), then start a replacement — faster but with temporarily reduced capacity.","correct":"maxUnavailable sets the maximum number of pods that can be unavailable during the rolling update.","distractors":["A PDB specifies the minimum number (or percentage) of pods of a replicated application that must be available at any time.","A multi-stage build uses multiple FROM instructions in a Dockerfile, each creating an intermediate image.","Always use -XX:+UseContainerSupport (default in Java 11+) to make the JVM container-aware — it reads cgroup memory limits instead of host total RAM."]},{"id":"backend/container_and_deployment_patterns#f180ee0ac938","section":"backend","module":"backend/container_and_deployment_patterns","moduleName":"container and deployment patterns","qIndex":3,"difficulty":"core","question":"What is the difference between CPU requests and CPU limits in Kubernetes?","answerFull":"CPU requests are used by the scheduler to find a node with sufficient available CPU — the pod is guaranteed this amount. CPU limits are enforced by CFS (Completely Fair Scheduler) cgroups at runtime: if a container exceeds its limit within a CFS period (100ms), it is throttled until the next period. For JVM applications, GC and JIT compilation cause brief CPU spikes. With a CPU limit set too low, these spikes trigger throttling, causing latency spikes visible as high p99 latency. Best practice for latency-sensitive JVM services: set CPU requests (for scheduling fairness) but omit CPU limits. Set memory limits always (prevent unbounded growth; OOM kill is predictable and recoverable via restart).","correct":"CPU requests are used by the scheduler to find a node with sufficient available CPU — the pod is guaranteed this amount.","distractors":["A PDB specifies the minimum number (or percentage) of pods of a replicated application that must be available at any time.","HPA (Horizontal Pod Autoscaler) queries the metrics API (CPU from metrics-server, custom metrics from Prometheus Adapter or KEDA) every 15 seconds, calculates the desired replica count, and adjusts.","Always use -XX:+UseContainerSupport (default in Java 11+) to make the JVM container-aware — it reads cgroup memory limits instead of host total RAM."]},{"id":"backend/container_and_deployment_patterns#a5f0683d5030","section":"backend","module":"backend/container_and_deployment_patterns","moduleName":"container and deployment patterns","qIndex":4,"difficulty":"core","question":"How should you size JVM heap in a Kubernetes container?","answerFull":"Always use -XX:+UseContainerSupport (default in Java 11+) to make the JVM container-aware — it reads cgroup memory limits instead of host total RAM. Set -XX:MaxRAMPercentage=75.0 to allocate 75% of the container's memory limit to the heap. Reserve 25% for: JVM overhead (metaspace, code cache, JIT buffers), off-heap memory (direct buffers, Netty allocations), and OS/container overhead. Example: 1GB container limit → 768MB heap, 256MB for JVM overhead. Without these flags on Java < 11 or with UseContainerSupport disabled, the JVM sees the host's 64GB RAM and tries to use 16GB for heap, causing immediate OOM kill.","correct":"Always use -XX:+UseContainerSupport (default in Java 11+) to make the JVM container-aware — it reads cgroup memory limits instead of host total RAM.","distractors":["A PDB specifies the minimum number (or percentage) of pods of a replicated application that must be available at any time.","A multi-stage build uses multiple FROM instructions in a Dockerfile, each creating an intermediate image.","CPU requests are used by the scheduler to find a node with sufficient available CPU — the pod is guaranteed this amount."]},{"id":"backend/container_and_deployment_patterns#efae68a2992a","section":"backend","module":"backend/container_and_deployment_patterns","moduleName":"container and deployment patterns","qIndex":5,"difficulty":"core","question":"What is a Pod Disruption Budget (PDB) and why is it important?","answerFull":"A PDB specifies the minimum number (or percentage) of pods of a replicated application that must be available at any time. During voluntary disruptions (node drains for maintenance, cluster upgrades, rolling updates), Kubernetes respects PDBs: it will not drain a node if doing so would violate the PDB. Without a PDB, a node drain can terminate all pods of a deployment simultaneously if they all happen to be on the same node — causing a complete outage. Set minAvailable to at least the majority of replicas: for 5 replicas, set minAvailable=3. This ensures at least 3 pods are always serving traffic during any rolling operation.","correct":"A PDB specifies the minimum number (or percentage) of pods of a replicated application that must be available at any time.","distractors":["HPA (Horizontal Pod Autoscaler) queries the metrics API (CPU from metrics-server, custom metrics from Prometheus Adapter or KEDA) every 15 seconds, calculates the desired replica count, and adjusts.","maxUnavailable sets the maximum number of pods that can be unavailable during the rolling update.","CPU requests are used by the scheduler to find a node with sufficient available CPU — the pod is guaranteed this amount."]},{"id":"backend/container_and_deployment_patterns#05890b3bbd0f","section":"backend","module":"backend/container_and_deployment_patterns","moduleName":"container and deployment patterns","qIndex":7,"difficulty":"intermediate","question":"How does HPA work and what are common pitfalls?","answerFull":"HPA (Horizontal Pod Autoscaler) queries the metrics API (CPU from metrics-server, custom metrics from Prometheus Adapter or KEDA) every 15 seconds, calculates the desired replica count, and adjusts. Formula: desiredReplicas = ceil(currentReplicas * (currentMetric / targetMetric)). Common pitfalls: (1) flapping — CPU oscillates around the target, causing constant scale up/down; fix with stabilizationWindowSeconds=300 for scale-down. (2) Slow reaction — by default HPA reacts in 15s intervals; for bursty traffic, set scaleUp.stabilizationWindowSeconds=0 and high maxSurge. (3) Scale-down thrashing during rolling updates — temporarily inflated pod count confuses HPA; HPA should not overlap with deployment rollouts. (4) Not working with Karpenter/cluster autoscaler — HPA must add replicas before cluster autoscaler can add nodes; configure adequate pending pod time.","correct":"HPA (Horizontal Pod Autoscaler) queries the metrics API (CPU from metrics-server, custom metrics from Prometheus Adapter or KEDA) every 15 seconds, calculates the desired replica count, and adjusts.","distractors":["CPU requests are used by the scheduler to find a node with sufficient available CPU — the pod is guaranteed this amount.","A PDB specifies the minimum number (or percentage) of pods of a replicated application that must be available at any time.","A multi-stage build uses multiple FROM instructions in a Dockerfile, each creating an intermediate image."]},{"id":"backend/container_and_deployment_patterns#94e908a5fe55","section":"backend","module":"backend/container_and_deployment_patterns","moduleName":"container and deployment patterns","qIndex":8,"difficulty":"intermediate","question":"What is the 12-factor app methodology and which factors are most important for containerized Java services?","answerFull":"The 12-factor app is a methodology for building cloud-native applications. The most critical factors for containerized Java services: Factor III (Config via environment variables — no config in image; inject via Kubernetes ConfigMap/Secret), Factor VI (Processes — stateless; store session in Redis, not in-memory), Factor IX (Disposability — fast startup for rapid scaling; graceful shutdown for zero-downtime deploys), Factor X (Dev/Prod parity — use Testcontainers to have dev/test match production infrastructure), Factor XI (Logs as streams — write to stdout/stderr; let the container platform capture and ship logs). These directly affect deployment reliability, scalability, and operability.","correct":"The 12-factor app is a methodology for building cloud-native applications.","distractors":["Always use -XX:+UseContainerSupport (default in Java 11+) to make the JVM container-aware — it reads cgroup memory limits instead of host total RAM.","A PDB specifies the minimum number (or percentage) of pods of a replicated application that must be available at any time.","A multi-stage build uses multiple FROM instructions in a Dockerfile, each creating an intermediate image."]},{"id":"backend/database_internals_and_indexing#b487e9950449","section":"backend","module":"backend/database_internals_and_indexing","moduleName":"database internals and indexing","qIndex":1,"difficulty":"core","question":"Describe the structure of a B+tree index and how it enables efficient queries.","answerFull":"A B+tree has internal nodes (routing only, store keys for navigation) and leaf nodes (store all key-value pairs with pointers to the heap row, linked in a doubly-linked list). To find a row, traverse from root to leaf in O(log N) steps. For range scans: find the first matching leaf, then traverse the linked list. For a 1-billion row table with branching factor 100, height is ~5 — all lookups require ~5 page reads regardless of table size. This makes B-tree indexes extremely efficient for both equality and range queries.","correct":"A B+tree has internal nodes (routing only, store keys for navigation) and leaf nodes (store all key-value pairs with pointers to the heap row, linked in a doubly-linked list).","distractors":["MVCC (Multi-Version Concurrency Control) stores multiple versions of each row — one for the current state and old versions for active transactions that read an earlier snapshot.","The visibility map is a per-page bitmap recording whether every row on a page is visible to all current transactions.","An Index Scan traverses the B+tree to find row pointers, then fetches each matching row from the heap file (potentially random I/O)."]},{"id":"backend/database_internals_and_indexing#1a06b09e9e73","section":"backend","module":"backend/database_internals_and_indexing","moduleName":"database internals and indexing","qIndex":2,"difficulty":"core","question":"What is WAL (Write-Ahead Logging) and why is it important?","answerFull":"WAL is a durability mechanism: every database modification is first written to the WAL (a sequential append-only log) before being applied to data files. If the server crashes, on restart the database replays WAL from the last checkpoint to recover all committed transactions. WAL enables: crash recovery (replay lost changes), streaming replication (replicas receive and replay WAL), point-in-time recovery (replay WAL to any past point), and logical decoding (CDC tools like Debezium read WAL).","correct":"WAL is a durability mechanism: every database modification is first written to the WAL (a sequential append-only log) before being applied to data files.","distractors":["The planner estimates the cost of each plan based on: table size (pages), number of matching rows (from statistics/histograms), page read costs (random vs sequential), and whether pages are in the buffer pool.","The visibility map is a per-page bitmap recording whether every row on a page is visible to all current transactions.","A B+tree has internal nodes (routing only, store keys for navigation) and leaf nodes (store all key-value pairs with pointers to the heap row, linked in a doubly-linked list)."]},{"id":"backend/database_internals_and_indexing#e56ffbad8603","section":"backend","module":"backend/database_internals_and_indexing","moduleName":"database internals and indexing","qIndex":3,"difficulty":"core","question":"Explain MVCC and how it enables concurrent reads without blocking writes.","answerFull":"MVCC (Multi-Version Concurrency Control) stores multiple versions of each row — one for the current state and old versions for active transactions that read an earlier snapshot. When a row is updated, the old version (with xmax set to the updating transaction ID) coexists with the new version (with xmin set to the same). Readers choose which version to see based on their transaction snapshot. Since readers access old versions, they never block writers who create new versions. The tradeoff: dead row versions accumulate until VACUUM reclaims them.","correct":"MVCC (Multi-Version Concurrency Control) stores multiple versions of each row — one for the current state and old versions for active transactions that read an earlier snapshot.","distractors":["Table bloat is the accumulation of dead row versions (from UPDATEs and DELETEs) that have not been reclaimed by VACUUM.","The visibility map is a per-page bitmap recording whether every row on a page is visible to all current transactions.","BRIN (Block Range Index) stores the minimum and maximum values for each contiguous range of heap pages (block range, default 128 pages)."]},{"id":"backend/database_internals_and_indexing#763523e063f2","section":"backend","module":"backend/database_internals_and_indexing","moduleName":"database internals and indexing","qIndex":4,"difficulty":"core","question":"What is the difference between an Index Scan and an Index Only Scan?","answerFull":"An Index Scan traverses the B+tree to find row pointers, then fetches each matching row from the heap file (potentially random I/O). An Index Only Scan satisfies the query entirely from the index — no heap fetch. This requires: (1) all columns in the SELECT list are in the index (covering index); (2) the visibility map confirms the heap page is all-visible (so the planner does not need to check MVCC visibility from the heap). Index Only Scans are dramatically faster for large result sets that hit many heap pages.","correct":"An Index Scan traverses the B+tree to find row pointers, then fetches each matching row from the heap file (potentially random I/O).","distractors":["BRIN (Block Range Index) stores the minimum and maximum values for each contiguous range of heap pages (block range, default 128 pages).","A B+tree has internal nodes (routing only, store keys for navigation) and leaf nodes (store all key-value pairs with pointers to the heap row, linked in a doubly-linked list).","The heap file is the main data file — it stores all rows, in insertion order (no sorting)."]},{"id":"backend/database_internals_and_indexing#6d81106f527a","section":"backend","module":"backend/database_internals_and_indexing","moduleName":"database internals and indexing","qIndex":5,"difficulty":"core","question":"What is a covering index and when should you create one?","answerFull":"A covering index includes all columns needed by a query — the key columns (in the B-tree order) plus any additional columns via INCLUDE. When an index-only scan is possible, the database reads only the index (sequential), not the heap (random I/O). Create covering indexes for high-frequency queries with a predictable column set. Trade-off: larger indexes with more write overhead. In PostgreSQL, use INCLUDE for non-key columns: CREATE INDEX ON orders(user_id, status) INCLUDE (amount, created_at).","correct":"A covering index includes all columns needed by a query — the key columns (in the B-tree order) plus any additional columns via INCLUDE.","distractors":["A B+tree has internal nodes (routing only, store keys for navigation) and leaf nodes (store all key-value pairs with pointers to the heap row, linked in a doubly-linked list).","A partial index indexes only a subset of rows (rows matching a WHERE condition).","The heap file is the main data file — it stores all rows, in insertion order (no sorting)."]},{"id":"backend/database_internals_and_indexing#0ba4180d0f75","section":"backend","module":"backend/database_internals_and_indexing","moduleName":"database internals and indexing","qIndex":6,"difficulty":"intermediate","question":"What is table bloat and how is it caused?","answerFull":"Table bloat is the accumulation of dead row versions (from UPDATEs and DELETEs) that have not been reclaimed by VACUUM. MVCC keeps dead versions until no active transaction can see them. Until VACUUM runs, dead rows remain in the heap and indexes, wasting space and slowing sequential scans. Bloat causes: slow sequential scans (scanning dead rows), slow index scans (more pages), wasted disk space. Prevention: ensure autovacuum is tuned aggressively for busy tables (smaller scale_factor thresholds).","correct":"Table bloat is the accumulation of dead row versions (from UPDATEs and DELETEs) that have not been reclaimed by VACUUM.","distractors":["Most common causes: (1) Table grew significantly and selectivity of the queried column dropped below the planner's threshold for index preference.","A B+tree has internal nodes (routing only, store keys for navigation) and leaf nodes (store all key-value pairs with pointers to the heap row, linked in a doubly-linked list).","The planner estimates the cost of each plan based on: table size (pages), number of matching rows (from statistics/histograms), page read costs (random vs sequential), and whether pages are in the buffer pool."]},{"id":"backend/database_internals_and_indexing#de4e1f1c2a4a","section":"backend","module":"backend/database_internals_and_indexing","moduleName":"database internals and indexing","qIndex":7,"difficulty":"intermediate","question":"What is the difference between a partial index and a covering index?","answerFull":"A partial index indexes only a subset of rows (rows matching a WHERE condition). Example: CREATE INDEX ON users(email) WHERE deleted_at IS NULL — indexes only non-deleted users. Reduces index size and is only used by queries that include the partial index's condition. A covering index includes all columns needed by a query to enable index-only scans. These are orthogonal — you can have a partial covering index: CREATE INDEX ON orders(user_id, created_at) INCLUDE (amount) WHERE status = 'active'.","correct":"A partial index indexes only a subset of rows (rows matching a WHERE condition).","distractors":["An Index Scan traverses the B+tree to find row pointers, then fetches each matching row from the heap file (potentially random I/O).","GIN (Generalized Inverted Index) is designed for multi-valued data: arrays, JSONB, full-text tsvectors.","Most common causes: (1) Table grew significantly and selectivity of the queried column dropped below the planner's threshold for index preference."]},{"id":"backend/database_internals_and_indexing#7230cc4c8c78","section":"backend","module":"backend/database_internals_and_indexing","moduleName":"database internals and indexing","qIndex":8,"difficulty":"intermediate","question":"How does the query planner decide between Seq Scan and Index Scan?","answerFull":"The planner estimates the cost of each plan based on: table size (pages), number of matching rows (from statistics/histograms), page read costs (random vs sequential), and whether pages are in the buffer pool. If a query matches >15-20% of rows (low selectivity), a sequential scan is often cheaper than random I/O from index scans. The planner uses statistics (from ANALYZE) to estimate row counts. Outdated statistics cause wrong plan choices. Random page cost (random_page_cost) is set at 4.0 for spinning disks and 1.1 for SSDs — this significantly affects when the planner prefers index over seq scan.","correct":"The planner estimates the cost of each plan based on: table size (pages), number of matching rows (from statistics/histograms), page read costs (random vs sequential), and whether pages are in the buffer pool.","distractors":["The visibility map is a per-page bitmap recording whether every row on a page is visible to all current transactions.","A covering index includes all columns needed by a query — the key columns (in the B-tree order) plus any additional columns via INCLUDE.","BRIN (Block Range Index) stores the minimum and maximum values for each contiguous range of heap pages (block range, default 128 pages)."]},{"id":"backend/database_internals_and_indexing#cb36f248b948","section":"backend","module":"backend/database_internals_and_indexing","moduleName":"database internals and indexing","qIndex":9,"difficulty":"intermediate","question":"What is the visibility map and why does it matter for Index Only Scans?","answerFull":"The visibility map is a per-page bitmap recording whether every row on a page is visible to all current transactions. VACUUM sets the visibility map after cleaning dead rows. For an Index Only Scan, if a page's visibility map bit is set, the planner knows any row version on that page is visible — no need to check the heap for MVCC visibility. If the bit is not set, the planner falls back to fetching the heap page to verify visibility, degrading to a regular Index Scan. Stale visibility maps (from infrequent VACUUM) prevent Index Only Scans.","correct":"The visibility map is a per-page bitmap recording whether every row on a page is visible to all current transactions.","distractors":["The planner estimates the cost of each plan based on: table size (pages), number of matching rows (from statistics/histograms), page read costs (random vs sequential), and whether pages are in the buffer pool.","A covering index includes all columns needed by a query — the key columns (in the B-tree order) plus any additional columns via INCLUDE.","Table bloat is the accumulation of dead row versions (from UPDATEs and DELETEs) that have not been reclaimed by VACUUM."]},{"id":"backend/database_internals_and_indexing#a7afc668733c","section":"backend","module":"backend/database_internals_and_indexing","moduleName":"database internals and indexing","qIndex":10,"difficulty":"intermediate","question":"Explain transaction ID wraparound in PostgreSQL.","answerFull":"PostgreSQL uses 32-bit transaction IDs (xids). The comparison function is modular arithmetic: a transaction is \"newer\" if its xid is within 2 billion of the current xid. Once a table's oldest unfrozen transaction (datfrozenxid) is more than 2 billion transactions behind the current xid, the database cannot distinguish old from new and enters emergency mode (refuses all writes). Prevention: autovacuum freezes old row versions by replacing their xmin with a special FrozenTransactionId. Monitor with SELECT age(datfrozenxid) FROM pg_database — alert at 1.5 billion.","correct":"PostgreSQL uses 32-bit transaction IDs (xids).","distractors":["A B+tree has internal nodes (routing only, store keys for navigation) and leaf nodes (store all key-value pairs with pointers to the heap row, linked in a doubly-linked list).","A covering index includes all columns needed by a query — the key columns (in the B-tree order) plus any additional columns via INCLUDE.","A partial index indexes only a subset of rows (rows matching a WHERE condition)."]},{"id":"backend/database_internals_and_indexing#35b8f81d5991","section":"backend","module":"backend/database_internals_and_indexing","moduleName":"database internals and indexing","qIndex":11,"difficulty":"advanced","question":"What is a GIN index and when should you use it?","answerFull":"GIN (Generalized Inverted Index) is designed for multi-valued data: arrays, JSONB, full-text tsvectors. An inverted index maps each element value to the rows containing it — like a book's index mapping words to page numbers. For a JSONB column, GIN can quickly find all rows containing {\"color\": \"red\"} by looking up \"color=red\" in the inverted index. Use GIN for JSONB containment queries, array element containment, and full-text search. GIN is expensive to update (entire document must be re-indexed on update) — use where reads dominate writes.","correct":"GIN (Generalized Inverted Index) is designed for multi-valued data: arrays, JSONB, full-text tsvectors.","distractors":["A covering index includes all columns needed by a query — the key columns (in the B-tree order) plus any additional columns via INCLUDE.","MVCC (Multi-Version Concurrency Control) stores multiple versions of each row — one for the current state and old versions for active transactions that read an earlier snapshot.","Most common causes: (1) Table grew significantly and selectivity of the queried column dropped below the planner's threshold for index preference."]},{"id":"backend/database_internals_and_indexing#aa13dbd52629","section":"backend","module":"backend/database_internals_and_indexing","moduleName":"database internals and indexing","qIndex":12,"difficulty":"advanced","question":"How does BRIN work and when is it appropriate?","answerFull":"BRIN (Block Range Index) stores the minimum and maximum values for each contiguous range of heap pages (block range, default 128 pages). For a query WHERE timestamp BETWEEN X AND Y, BRIN checks which block ranges could contain matching rows and scans only those. Effective only when data is physically ordered by the indexed column (timestamps inserted in order, sequential IDs). BRIN is tiny (bytes per block range vs bytes per row for B-tree) — a 10 billion row time-series table has a BRIN index of ~100 KB vs ~10 GB for B-tree. Use BRIN for append-only time-series tables; use B-tree for random-access patterns.","correct":"BRIN (Block Range Index) stores the minimum and maximum values for each contiguous range of heap pages (block range, default 128 pages).","distractors":["A B+tree has internal nodes (routing only, store keys for navigation) and leaf nodes (store all key-value pairs with pointers to the heap row, linked in a doubly-linked list).","An Index Scan traverses the B+tree to find row pointers, then fetches each matching row from the heap file (potentially random I/O).","The heap file is the main data file — it stores all rows, in insertion order (no sorting)."]},{"id":"backend/database_internals_and_indexing#db1fe33f4abb","section":"backend","module":"backend/database_internals_and_indexing","moduleName":"database internals and indexing","qIndex":13,"difficulty":"advanced","question":"What is the difference between VACUUM and VACUUM FULL?","answerFull":"VACUUM removes dead row versions, reclaims space for reuse within the same table file, and updates the visibility map. It does NOT return space to the OS. It takes a lightweight lock (non-blocking for reads/writes). VACUUM FULL rewrites the entire table into a new file (defragmenting it), returning space to the OS. It takes an exclusive lock (blocks all access). VACUUM FULL is rarely needed — use it only to recover disk space after deleting large portions of a table, and only during maintenance windows.","correct":"VACUUM removes dead row versions, reclaims space for reuse within the same table file, and updates the visibility map.","distractors":["MVCC (Multi-Version Concurrency Control) stores multiple versions of each row — one for the current state and old versions for active transactions that read an earlier snapshot.","A B+tree has internal nodes (routing only, store keys for navigation) and leaf nodes (store all key-value pairs with pointers to the heap row, linked in a doubly-linked list).","The planner estimates the cost of each plan based on: table size (pages), number of matching rows (from statistics/histograms), page read costs (random vs sequential), and whether pages are in the buffer pool."]},{"id":"backend/database_internals_and_indexing#d60c1021b941","section":"backend","module":"backend/database_internals_and_indexing","moduleName":"database internals and indexing","qIndex":15,"difficulty":"advanced","question":"What causes an execution plan to suddenly change from Index Scan to Seq Scan?","answerFull":"Most common causes: (1) Table grew significantly and selectivity of the queried column dropped below the planner's threshold for index preference. (2) Statistics became stale after a bulk load — planner underestimates row count, chooses wrong join type or access method. (3) random_page_cost too high for SSD — planner overestimates cost of index scan. (4) seq_page_cost too low. (5) A new autovacuum ran that updated statistics differently. Fix: run ANALYZE, check pg_stats for the column, adjust random_page_cost for SSDs (1.1), and consider adding hints via pg_hint_plan if needed.","correct":"Most common causes: (1) Table grew significantly and selectivity of the queried column dropped below the planner's threshold for index preference.","distractors":["VACUUM removes dead row versions, reclaims space for reuse within the same table file, and updates the visibility map.","An Index Scan traverses the B+tree to find row pointers, then fetches each matching row from the heap file (potentially random I/O).","A covering index includes all columns needed by a query — the key columns (in the B-tree order) plus any additional columns via INCLUDE."]},{"id":"backend/database_internals_and_indexing#c33b901f9303","section":"backend","module":"backend/database_internals_and_indexing","moduleName":"database internals and indexing","qIndex":16,"difficulty":"advanced","question":"What is the difference between the heap file and an index in PostgreSQL?","answerFull":"The heap file is the main data file — it stores all rows, in insertion order (no sorting). Pages are 8 KB. Rows within a page are referenced by ctid (page number + slot offset). An index is a separate B-tree (or other structure) that stores (indexed_key, ctid) pairs in sorted order. When a row is updated, both the heap and all indexes must be updated. The heap grows as rows are inserted/updated (old versions stay until VACUUM); indexes also accumulate dead entries. They must be VACUUMed together.","correct":"The heap file is the main data file — it stores all rows, in insertion order (no sorting).","distractors":["BRIN (Block Range Index) stores the minimum and maximum values for each contiguous range of heap pages (block range, default 128 pages).","A covering index includes all columns needed by a query — the key columns (in the B-tree order) plus any additional columns via INCLUDE.","A B+tree has internal nodes (routing only, store keys for navigation) and leaf nodes (store all key-value pairs with pointers to the heap row, linked in a doubly-linked list)."]},{"id":"backend/database_migrations#47b15185b5e1","section":"backend","module":"backend/database_migrations","moduleName":"database migrations","qIndex":1,"difficulty":"core","question":"What is the expand-contract pattern for zero-downtime migrations?","answerFull":"Expand-contract (also called parallel change) is a three-phase approach: (1) Expand: add the new schema element (new column, table) while keeping the old. Deploy code that writes to both. (2) Migrate: backfill existing data to the new schema. (3) Contract: deploy code that reads/writes only the new schema; remove the old schema. This ensures that at every point, both the old and new application code can function correctly. It eliminates the race condition where a migration removes a column before all instances of the old code are updated.","correct":"Expand-contract (also called parallel change) is a three-phase approach: (1) Expand: add the new schema element (new column, table) while keeping the old.","distractors":["CREATE INDEX CONCURRENTLY builds an index without taking an exclusive table lock.","gh-ost creates a \"ghost\" table with the desired new schema, copies rows from the original table to the ghost table, and simultaneously applies changes from MySQL's binary log (binlog) to keep the ghost table in sync.","Direct ALTER TABLE ADD COLUMN NOT NULL DEFAULT X requires rewriting the entire table in older databases."]},{"id":"backend/database_migrations#b56fcb4bb2dc","section":"backend","module":"backend/database_migrations","moduleName":"database migrations","qIndex":2,"difficulty":"core","question":"How does Flyway track which migrations have been applied?","answerFull":"Flyway maintains a flyway_schema_history table in the target database. Each row records: version number, description, script filename, checksum (SHA-256 of the file content), whether it succeeded, and the timestamp. On application startup, Flyway reads this table, compares against the available migration files, and applies any pending migrations in version order. If a migration file's checksum does not match the stored checksum, Flyway throws an error — preventing silent script modifications.","correct":"Flyway maintains a flyway_schema_history table in the target database.","distractors":["Expand-contract (also called parallel change) is a three-phase approach: (1) Expand: add the new schema element (new column, table) while keeping the old.","Baseline is for databases that already have a schema (pre-Flyway).","Direct ALTER TABLE ADD COLUMN NOT NULL DEFAULT X requires rewriting the entire table in older databases."]},{"id":"backend/database_migrations#fe277723ff19","section":"backend","module":"backend/database_migrations","moduleName":"database migrations","qIndex":3,"difficulty":"core","question":"What is CREATE INDEX CONCURRENTLY and when must you use it?","answerFull":"CREATE INDEX CONCURRENTLY builds an index without taking an exclusive table lock. It performs multiple passes: first scan creates the index structure without blocking writes; subsequent passes catch up with changes made during the first scan. The downside: takes significantly longer than regular CREATE INDEX. Always use CONCURRENTLY for adding indexes to production tables with significant write traffic — a regular CREATE INDEX on a busy table blocks all writes until the index builds, potentially causing timeouts and cascading failures.","correct":"CREATE INDEX CONCURRENTLY builds an index without taking an exclusive table lock.","distractors":["Direct ALTER TABLE ADD COLUMN NOT NULL DEFAULT X requires rewriting the entire table in older databases.","gh-ost creates a \"ghost\" table with the desired new schema, copies rows from the original table to the ghost table, and simultaneously applies changes from MySQL's binary log (binlog) to keep the ghost table in sync.","Flyway maintains a flyway_schema_history table in the target database."]},{"id":"backend/database_migrations#015968458cc2","section":"backend","module":"backend/database_migrations","moduleName":"database migrations","qIndex":4,"difficulty":"core","question":"How do you handle a migration that fails in production?","answerFull":"(1) Flyway marks failed migrations as failed in the history table. The application will not start until the migration is repaired. (2) Fix the issue: if the migration partially applied, manually revert the partial change and mark it repaired (flyway repair). (3) Create a corrected migration with a new version number (do NOT modify the failed migration file — its checksum is tracked). (4) In testing environments: flyway clean + remigrate is acceptable; never run flyway clean in production (drops all tables).","correct":"(1) Flyway marks failed migrations as failed in the history table.","distractors":["(1) Unit: test each migration file's SQL syntax with a real database (Testcontainers PostgreSQL/MySQL).","Expand-contract (also called parallel change) is a three-phase approach: (1) Expand: add the new schema element (new column, table) while keeping the old.","Use an init container that runs flyway migrate before the main container starts."]},{"id":"backend/database_migrations#42120ec20dd0","section":"backend","module":"backend/database_migrations","moduleName":"database migrations","qIndex":5,"difficulty":"core","question":"What is the difference between Flyway's versioned and repeatable migrations?","answerFull":"Versioned migrations (V1, V2) are applied once, in order, and never re-applied. Repeatable migrations (Rviews.sql) are re-applied whenever their checksum changes. Use repeatable for database objects that should always reflect their latest definition: views, stored procedures, functions, and seed data. Every time Rviews.sql changes, Flyway re-runs it, replacing the previous version of the views.","correct":"Versioned migrations (V1, V2) are applied once, in order, and never re-applied.","distractors":["Baseline is for databases that already have a schema (pre-Flyway).","Use an init container that runs flyway migrate before the main container starts.","Flyway maintains a flyway_schema_history table in the target database."]},{"id":"backend/database_migrations#3ab7ffbafe96","section":"backend","module":"backend/database_migrations","moduleName":"database migrations","qIndex":6,"difficulty":"intermediate","question":"How do you safely add a NOT NULL column to a large table?","answerFull":"Direct ALTER TABLE ADD COLUMN NOT NULL DEFAULT X requires rewriting the entire table in older databases. PostgreSQL 11+ makes this metadata-only if there is a DEFAULT. For other databases or NULL-with-backfill scenarios: (1) Add the column as nullable: ALTER TABLE t ADD COLUMN new_col TYPE. (2) Backfill in batches: UPDATE t SET new_col = default_val WHERE new_col IS NULL LIMIT 10000 (repeat until done). (3) Add NOT NULL constraint as NOT VALID (no lock): ALTER TABLE t ADD CONSTRAINT chk_not_null CHECK (new_col IS NOT NULL) NOT VALID. (4) Validate: ALTER TABLE t VALIDATE CONSTRAINT chk_not_null (ShareUpdateExclusiveLock, allows reads/writes).","correct":"Direct ALTER TABLE ADD COLUMN NOT NULL DEFAULT X requires rewriting the entire table in older databases.","distractors":["Expand-contract (also called parallel change) is a three-phase approach: (1) Expand: add the new schema element (new column, table) while keeping the old.","Baseline is for databases that already have a schema (pre-Flyway).","(1) Flyway marks failed migrations as failed in the history table."]},{"id":"backend/database_migrations#e856f6a119e5","section":"backend","module":"backend/database_migrations","moduleName":"database migrations","qIndex":7,"difficulty":"intermediate","question":"How would you rename a column with zero downtime?","answerFull":"Use expand-contract: (1) Add the new column name. (2) Update application code to write to both the old and new column. (3) Backfill: update all rows where the new column is null to copy from the old. (4) Deploy code that reads from the new column only (writes to both). (5) Deploy code that reads and writes only the new column (drop writes to old). (6) Drop the old column once no running instances reference it.","correct":"Use expand-contract: (1) Add the new column name.","distractors":["Direct ALTER TABLE ADD COLUMN NOT NULL DEFAULT X requires rewriting the entire table in older databases.","Expand-contract (also called parallel change) is a three-phase approach: (1) Expand: add the new schema element (new column, table) while keeping the old.","gh-ost creates a \"ghost\" table with the desired new schema, copies rows from the original table to the ghost table, and simultaneously applies changes from MySQL's binary log (binlog) to keep the ghost table in sync."]},{"id":"backend/database_migrations#5403df81a16a","section":"backend","module":"backend/database_migrations","moduleName":"database migrations","qIndex":8,"difficulty":"intermediate","question":"What is gh-ost and how does it enable online schema changes in MySQL?","answerFull":"gh-ost creates a \"ghost\" table with the desired new schema, copies rows from the original table to the ghost table, and simultaneously applies changes from MySQL's binary log (binlog) to keep the ghost table in sync. When the copy and sync are complete, gh-ost atomically swaps the original and ghost table names. Unlike triggers (used by pt-osc), gh-ost reads the binlog independently — no write amplification from triggers. It includes throttling mechanisms (pause on high load, replica lag) to prevent impacting production traffic.","correct":"gh-ost creates a \"ghost\" table with the desired new schema, copies rows from the original table to the ghost table, and simultaneously applies changes from MySQL's binary log (binlog) to keep the ghost table in sync.","distractors":["Use expand-contract: (1) Add the new column name.","Baseline is for databases that already have a schema (pre-Flyway).","(1) Flyway marks failed migrations as failed in the history table."]},{"id":"backend/database_migrations#65ec2606c1d4","section":"backend","module":"backend/database_migrations","moduleName":"database migrations","qIndex":9,"difficulty":"intermediate","question":"How do you test database migrations in a CI pipeline?","answerFull":"(1) Unit: test each migration file's SQL syntax with a real database (Testcontainers PostgreSQL/MySQL). (2) Integration: run the full migration sequence against a clean schema; verify table structure, indexes, and constraints match expectations. (3) Data migration: if the migration transforms data, load representative test data, run the migration, assert the transformed data is correct. (4) Rollback: if using Liquibase with rollback, test the rollback script. (5) Performance: for large-table migrations, test execution time on a copy of production data.","correct":"(1) Unit: test each migration file's SQL syntax with a real database (Testcontainers PostgreSQL/MySQL).","distractors":["Flyway maintains a flyway_schema_history table in the target database.","(1) Flyway marks failed migrations as failed in the history table.","Versioned migrations (V1, V2) are applied once, in order, and never re-applied."]},{"id":"backend/database_migrations#74c42e723444","section":"backend","module":"backend/database_migrations","moduleName":"database migrations","qIndex":11,"difficulty":"advanced","question":"How do you run Flyway in a Kubernetes deployment without running it in every pod?","answerFull":"Use an init container that runs flyway migrate before the main container starts. Only the init container runs migrations. All main application pods start only after the init container succeeds. This prevents race conditions from multiple pods running migrations simultaneously. The Kubernetes Job resource is an alternative: run migrations as a one-time job before rolling out the main Deployment. Ensure the migration user has flyway_schema_history write permissions; the application user can have read-only schema access.","correct":"Use an init container that runs flyway migrate before the main container starts.","distractors":["(1) Flyway marks failed migrations as failed in the history table.","Baseline is for databases that already have a schema (pre-Flyway).","Flyway maintains a flyway_schema_history table in the target database."]},{"id":"backend/database_migrations#d4a554ccfe98","section":"backend","module":"backend/database_migrations","moduleName":"database migrations","qIndex":12,"difficulty":"advanced","question":"What are baseline migrations in Flyway?","answerFull":"Baseline is for databases that already have a schema (pre-Flyway). When baseline-on-migrate is true, if Flyway finds no history table and the database is not empty, it creates the history table and marks all existing migrations up to baseline-version as already applied — without executing them. This allows Flyway to manage a pre-existing database. All migrations after the baseline version will run normally. Never use baseline on a fresh empty database; that is the default starting scenario for Flyway.","correct":"Baseline is for databases that already have a schema (pre-Flyway).","distractors":["gh-ost creates a \"ghost\" table with the desired new schema, copies rows from the original table to the ghost table, and simultaneously applies changes from MySQL's binary log (binlog) to keep the ghost table in sync.","(1) Flyway marks failed migrations as failed in the history table.","Versioned migrations (V1, V2) are applied once, in order, and never re-applied."]},{"id":"backend/database_types_deep_dive#37c62b179bac","section":"backend","module":"backend/database_types_deep_dive","moduleName":"database types deep dive","qIndex":1,"difficulty":"core","question":"What is the difference between a B+tree and an LSM-tree and what does each optimize for?","answerFull":"A B+tree is a balanced tree where all data is in leaf nodes. Writes require random I/O (find the right page, update it, write it back). Reads are efficient (O(log n) binary search). B+trees are optimized for read-heavy, random access workloads. LSM-tree (Log-Structured Merge Tree) writes are always sequential (append to MemTable → flush to SSTable). This makes writes extremely fast. Reads are slower because they may need to check multiple SSTables. LSM-trees are optimized for write-heavy, append-mostly workloads. PostgreSQL uses B+tree; Cassandra, RocksDB, and DynamoDB use LSM-tree variants.","correct":"A B+tree is a balanced tree where all data is in leaf nodes.","distractors":["Cassandra's LSM-tree storage engine makes writes sequential — every write goes to the CommitLog (sequential I/O) and the MemTable (in-memory), regardless of which partition is being written.","Redis Cluster distributes data across multiple nodes using 16384 hash slots.","Ask three questions: (1) What are the access patterns?"]},{"id":"backend/database_types_deep_dive#e5e36ef0bcf7","section":"backend","module":"backend/database_types_deep_dive","moduleName":"database types deep dive","qIndex":2,"difficulty":"core","question":"What is MVCC and how does it work in PostgreSQL?","answerFull":"MVCC (Multi-Version Concurrency Control) allows reads and writes to proceed concurrently without blocking each other. PostgreSQL stores xmin (transaction ID that created the row) and xmax (transaction ID that deleted/updated the row) in every tuple. When a transaction reads, it creates a snapshot of currently active transaction IDs. A row is visible to the transaction if xmin is committed and less than the snapshot's xmax, and xmax is either 0 (not deleted) or not yet committed. Updates do not modify rows in-place; they create new row versions with the old version having xmax set. Dead row versions (old versions no longer visible to any snapshot) are reclaimed by VACUUM.","correct":"MVCC (Multi-Version Concurrency Control) allows reads and writes to proceed concurrently without blocking each other.","distractors":["Cassandra's LSM-tree storage engine makes writes sequential — every write goes to the CommitLog (sequential I/O) and the MemTable (in-memory), regardless of which partition is being written.","HTAP (Hybrid Transactional/Analytical Processing) is the ability to run both OLTP (transactional) and OLAP (analytical) workloads on the same data without ETL delay.","A B+tree is a balanced tree where all data is in leaf nodes."]},{"id":"backend/database_types_deep_dive#9877657ff6c2","section":"backend","module":"backend/database_types_deep_dive","moduleName":"database types deep dive","qIndex":3,"difficulty":"core","question":"Why is Cassandra better than PostgreSQL for write-heavy time-series data?","answerFull":"Cassandra's LSM-tree storage engine makes writes sequential — every write goes to the CommitLog (sequential I/O) and the MemTable (in-memory), regardless of which partition is being written. There is no read-modify-write cycle. At 100K writes/sec, Cassandra spreads load across a ring of nodes by partition key. PostgreSQL writes require B+tree page lookups (random I/O) plus WAL writes. For truly high-cardinality time-series (millions of unique metrics), PostgreSQL's VACUUM struggles to keep up with dead tuple accumulation. Cassandra's TTL-based deletion (tombstones that TWCS compaction removes entirely) is cleaner for time-bounded data.","correct":"Cassandra's LSM-tree storage engine makes writes sequential — every write goes to the CommitLog (sequential I/O) and the MemTable (in-memory), regardless of which partition is being written.","distractors":["RDB (Redis Database Backup) creates point-in-time snapshots by forking the process and writing the current dataset to disk.","MVCC (Multi-Version Concurrency Control) allows reads and writes to proceed concurrently without blocking each other.","A B+tree is a balanced tree where all data is in leaf nodes."]},{"id":"backend/database_types_deep_dive#555ae2669c8f","section":"backend","module":"backend/database_types_deep_dive","moduleName":"database types deep dive","qIndex":4,"difficulty":"core","question":"What is the cardinality problem in Elasticsearch and how do you avoid it?","answerFull":"Cardinality in Elasticsearch refers to the number of unique values for a field. High-cardinality fields (user IDs, email addresses, request IDs) in aggregations cause two problems: (1) Fielddata — loading all unique values from a text field into heap causes OOM; fixed by using keyword type which uses DocValues (on-disk). (2) Metric labels — using high-cardinality values as aggregation terms returns thousands of buckets, causing memory pressure on coordinating nodes. Solutions: use keyword type for fields used in aggregations, avoid aggregating on user IDs, use cardinality aggregation (HyperLogLog-based) for counting unique values rather than terms aggregation.","correct":"Cardinality in Elasticsearch refers to the number of unique values for a field.","distractors":["In a relational database, finding A's friends requires a JOIN on the friendships table — an index lookup that is O(log n) where n is the total number of relationships.","A B+tree is a balanced tree where all data is in leaf nodes.","Ask three questions: (1) What are the access patterns?"]},{"id":"backend/database_types_deep_dive#6530f661e418","section":"backend","module":"backend/database_types_deep_dive","moduleName":"database types deep dive","qIndex":5,"difficulty":"core","question":"What is index-free adjacency in Neo4j and why does it matter for graph traversal?","answerFull":"In a relational database, finding A's friends requires a JOIN on the friendships table — an index lookup that is O(log n) where n is the total number of relationships. For depth-2 traversal (friends of friends), cost is O(n log n). This grows with total data size, not local graph density. Neo4j uses index-free adjacency: each node record stores a pointer to its first relationship. Each relationship record stores pointers to the next relationship for both the start and end nodes. Traversal follows these pointers directly — O(1) per hop regardless of graph size. For a 6-hop social network query, Neo4j's cost is proportional to the traversed subgraph, not the total database size. At millions of nodes, the difference between O(1) and O(log n) per hop compounds to orders of magnitude.","correct":"In a relational database, finding A's friends requires a JOIN on the friendships table — an index lookup that is O(log n) where n is the total number of relationships.","distractors":["Cardinality in Elasticsearch refers to the number of unique values for a field.","RDB (Redis Database Backup) creates point-in-time snapshots by forking the process and writing the current dataset to disk.","A B+tree is a balanced tree where all data is in leaf nodes."]},{"id":"backend/database_types_deep_dive#964afc088733","section":"backend","module":"backend/database_types_deep_dive","moduleName":"database types deep dive","qIndex":7,"difficulty":"intermediate","question":"What is the Cassandra partition key and why is design critical?","answerFull":"The partition key determines which node stores a row and how data is physically organized on disk. A good partition key has three properties: (1) high cardinality (many unique values to distribute data across many nodes — avoid low-cardinality keys like status='ACTIVE'), (2) even write distribution (avoid keys that all map to the same hash range — e.g., sequential IDs can hot-spot), (3) query isolation (the most common queries should specify the full partition key to target a single node). A bad partition key creates a \"hot partition\" — one node receives all writes while others are idle, defeating Cassandra's linear scalability. The worst pattern: using a timestamp as the partition key — all recent writes go to one partition.","correct":"The partition key determines which node stores a row and how data is physically organized on disk.","distractors":["Cassandra's LSM-tree storage engine makes writes sequential — every write goes to the CommitLog (sequential I/O) and the MemTable (in-memory), regardless of which partition is being written.","Consistent hashing assigns each node a position on a circular token ring from 0 to 2^64-1.","RDB (Redis Database Backup) creates point-in-time snapshots by forking the process and writing the current dataset to disk."]},{"id":"backend/database_types_deep_dive#7cbf306ce241","section":"backend","module":"backend/database_types_deep_dive","moduleName":"database types deep dive","qIndex":8,"difficulty":"intermediate","question":"What is Redis Cluster and what are its limitations?","answerFull":"Redis Cluster distributes data across multiple nodes using 16384 hash slots. Each key is assigned a slot via CRC16(key) % 16384. Each cluster node is responsible for a range of slots. When a client connects to the wrong node, it receives a MOVED redirect to the correct node. Limitations: (1) Multi-key operations (MGET, MSET, sorted set unions) only work if all keys hash to the same slot — use hash tags {user:123} to force co-location; (2) Lua scripts must touch keys on the same slot; (3) Maximum 1000 nodes (practical limit is lower); (4) cluster-require-full-coverage=yes means if any slot loses all its replicas, the entire cluster refuses writes; (5) Adding/removing nodes requires slot migration which has temporary double memory usage.","correct":"Redis Cluster distributes data across multiple nodes using 16384 hash slots.","distractors":["RDB (Redis Database Backup) creates point-in-time snapshots by forking the process and writing the current dataset to disk.","A B+tree is a balanced tree where all data is in leaf nodes.","Ask three questions: (1) What are the access patterns?"]},{"id":"backend/database_types_deep_dive#564f9da37f41","section":"backend","module":"backend/database_types_deep_dive","moduleName":"database types deep dive","qIndex":9,"difficulty":"intermediate","question":"How does Spanner achieve external consistency across globally distributed nodes?","answerFull":"Spanner uses TrueTime, a globally synchronized clock provided by Google's infrastructure (GPS receivers + atomic clocks at each data center). TrueTime provides a time interval [earliest, latest] with ~1-7ms uncertainty. When committing a transaction, Spanner waits until TT.now().earliest is past the commit timestamp — this \"commit wait\" ensures that the commit timestamp is definitely in the past. Any subsequent transaction that starts observably later (from any node's perspective) will have a higher timestamp. This property (if transaction T1 commits before T2 starts, then commit_ts(T1) < commit_ts(T2)) is external consistency — stronger than serializability. It enables globally consistent snapshots at any timestamp.","correct":"Spanner uses TrueTime, a globally synchronized clock provided by Google's infrastructure (GPS receivers + atomic clocks at each data center).","distractors":["A B+tree is a balanced tree where all data is in leaf nodes.","Redis Cluster distributes data across multiple nodes using 16384 hash slots.","Ask three questions: (1) What are the access patterns?"]},{"id":"backend/database_types_deep_dive#9e6ae5fe1993","section":"backend","module":"backend/database_types_deep_dive","moduleName":"database types deep dive","qIndex":10,"difficulty":"intermediate","question":"What is HTAP and which databases support it?","answerFull":"HTAP (Hybrid Transactional/Analytical Processing) is the ability to run both OLTP (transactional) and OLAP (analytical) workloads on the same data without ETL delay. Traditional architectures copy data from the OLTP database to a data warehouse via ETL, introducing hours of latency. HTAP databases maintain both row-oriented (for fast transactional reads/writes) and column-oriented (for fast analytical scans) representations simultaneously. TiDB with TiFlash: TiKV (row-oriented) + TiFlash (columnar replica, real-time sync from TiKV); analytical queries route to TiFlash. SingleStore (MemSQL): in-memory row store + columnar store. TimescaleDB: PostgreSQL row storage + columnar compression for recent-to-historical data. Use HTAP when you need real-time analytics on operational data without the delay of traditional ETL pipelines.","correct":"HTAP (Hybrid Transactional/Analytical Processing) is the ability to run both OLTP (transactional) and OLAP (analytical) workloads on the same data without ETL delay.","distractors":["MVCC (Multi-Version Concurrency Control) allows reads and writes to proceed concurrently without blocking each other.","A B+tree is a balanced tree where all data is in leaf nodes.","Ask three questions: (1) What are the access patterns?"]},{"id":"backend/database_types_deep_dive#6606778795c6","section":"backend","module":"backend/database_types_deep_dive","moduleName":"database types deep dive","qIndex":11,"difficulty":"advanced","question":"What is the difference between Redis RDB and AOF persistence, and which should you use?","answerFull":"RDB (Redis Database Backup) creates point-in-time snapshots by forking the process and writing the current dataset to disk. It is compact and fast to restore but can lose up to save_interval seconds of data (e.g., 15 minutes with save 900 1). AOF (Append Only File) logs every write command. With appendfsync everysec, you lose at most 1 second of data on crash. AOF files grow large and require periodic rewriting (bgrewriteaof). AOF is slower for write-intensive workloads but provides much better durability. For a cache that can be rebuilt from the source of truth: disable persistence (save \"\", no AOF). For session storage or rate-limiting counters: AOF with everysec. For data that cannot be lost: AOF with appendfsync always (highest durability, lowest throughput).","correct":"RDB (Redis Database Backup) creates point-in-time snapshots by forking the process and writing the current dataset to disk.","distractors":["The partition key determines which node stores a row and how data is physically organized on disk.","Redis Cluster distributes data across multiple nodes using 16384 hash slots.","In a relational database, finding A's friends requires a JOIN on the friendships table — an index lookup that is O(log n) where n is the total number of relationships."]},{"id":"backend/database_types_deep_dive#a2ac3faff0ed","section":"backend","module":"backend/database_types_deep_dive","moduleName":"database types deep dive","qIndex":12,"difficulty":"advanced","question":"How do you decide between a relational database and a document database for a new service?","answerFull":"Ask three questions: (1) What are the access patterns? If every query retrieves one document (by ID or known key), a document DB is well-suited. If queries JOIN multiple entities or require ad-hoc filtering on many attributes, relational is better. (2) How stable is the schema? Frequent additive changes (new optional fields) favor document DBs (no migration). Structural changes or strict schema validation favor relational. (3) What are the consistency requirements? Multi-document transactions in MongoDB work but are slower than single-document operations. If business logic requires atomic updates across multiple related entities, relational ACID transactions are more natural. For a product catalog: document DB (variable attributes per category). For an order system with inventory, payment, and shipping: relational.","correct":"Ask three questions: (1) What are the access patterns?","distractors":["In a relational database, finding A's friends requires a JOIN on the friendships table — an index lookup that is O(log n) where n is the total number of relationships.","RDB (Redis Database Backup) creates point-in-time snapshots by forking the process and writing the current dataset to disk.","A B+tree is a balanced tree where all data is in leaf nodes."]},{"id":"backend/database_types_deep_dive#f3f02d8aca35","section":"backend","module":"backend/database_types_deep_dive","moduleName":"database types deep dive","qIndex":13,"difficulty":"advanced","question":"What is consistent hashing in the context of Cassandra, and how do virtual nodes work?","answerFull":"Consistent hashing assigns each node a position on a circular token ring from 0 to 2^64-1. Each key is hashed to a token, and the key belongs to the first node clockwise from that token. When a node is added, it takes responsibility for a portion of an existing node's range — only those keys need to be moved. Without virtual nodes (vnodes): each node has one token position; adding a node only distributes load from one neighboring node; uneven data distribution when nodes have different capacities. With vnodes (default in Cassandra 3.0+, num_tokens=256): each node is assigned 256 small, non-contiguous token ranges distributed around the ring; adding a node takes small pieces from many nodes; better load balance and faster rebalancing.","correct":"Consistent hashing assigns each node a position on a circular token ring from 0 to 2^64-1.","distractors":["Redis Cluster distributes data across multiple nodes using 16384 hash slots.","Cassandra's LSM-tree storage engine makes writes sequential — every write goes to the CommitLog (sequential I/O) and the MemTable (in-memory), regardless of which partition is being written.","The partition key determines which node stores a row and how data is physically organized on disk."]},{"id":"backend/database_types_deep_dive#65eb6d42475e","section":"backend","module":"backend/database_types_deep_dive","moduleName":"database types deep dive","qIndex":15,"difficulty":"advanced","question":"What is write amplification and why does it matter for SSDs?","answerFull":"Write amplification is the ratio of actual data written to storage vs data the application intended to write. LSM-tree compaction multiplies writes: data written once to MemTable is flushed to L0, then merged to L1, then L2, etc. A Cassandra write may be written 10-30x due to compaction. SSDs have finite write endurance (typically 200-1000 TB written over device lifetime). High write amplification from a Cassandra cluster with LCS compaction can burn through SSD write endurance in years rather than the device's rated lifetime. Mitigation: choose TWCS or STCS over LCS for write-heavy workloads (lower write amplification); use higher-endurance SSD tiers for Cassandra nodes; monitor disk_compaction_bytes_written per node.","correct":"Write amplification is the ratio of actual data written to storage vs data the application intended to write.","distractors":["Cassandra's LSM-tree storage engine makes writes sequential — every write goes to the CommitLog (sequential I/O) and the MemTable (in-memory), regardless of which partition is being written.","A B+tree is a balanced tree where all data is in leaf nodes.","Ask three questions: (1) What are the access patterns?"]},{"id":"backend/distributed_system_operational_patterns#dfd623cbab91","section":"backend","module":"backend/distributed_system_operational_patterns","moduleName":"distributed system operational patterns","qIndex":1,"difficulty":"core","question":"What is the bulkhead pattern and how does it differ from a circuit breaker?","answerFull":"The bulkhead pattern isolates resources to prevent a failure in one area from exhausting resources needed by other areas. In software, this means separate thread pools or connection pools per downstream service. If one dependency is slow, only its thread pool fills up; other dependencies remain available. A circuit breaker monitors failure rates for a specific dependency and opens (stops sending requests) when the failure rate exceeds a threshold. They complement each other: circuit breakers detect and stop failures, bulkheads limit the blast radius when failures do occur. Use both: circuit breaker for fast failure detection, bulkhead for resource isolation.","correct":"The bulkhead pattern isolates resources to prevent a failure in one area from exhausting resources needed by other areas.","distractors":["The strangler fig pattern incrementally replaces a monolith by building new functionality as separate services and routing traffic to them, gradually strangling the monolith.","An ACL is a translation layer that converts between a legacy system's (or external system's) data model and your internal domain model.","Graceful shutdown ensures that when a pod is terminated, it completes in-flight requests before shutting down."]},{"id":"backend/distributed_system_operational_patterns#406334cb8be0","section":"backend","module":"backend/distributed_system_operational_patterns","moduleName":"distributed system operational patterns","qIndex":2,"difficulty":"core","question":"What is the strangler fig pattern and what are the key implementation steps?","answerFull":"The strangler fig pattern incrementally replaces a monolith by building new functionality as separate services and routing traffic to them, gradually strangling the monolith. Steps: (1) introduce a facade (API gateway or reverse proxy) in front of the monolith — all traffic routes through it to the monolith initially; (2) identify the first bounded context to extract (choose high-value, low-coupling); (3) build the new service independently with its own data store; (4) route specific paths/features to the new service via the gateway; (5) migrate data using CDC sync during the parallel-run period; (6) cut over and decommission monolith code. Critical: each step must be independently rollbackable (change routing back at the gateway).","correct":"The strangler fig pattern incrementally replaces a monolith by building new functionality as separate services and routing traffic to them, gradually strangling the monolith.","distractors":["The bulkhead pattern isolates resources to prevent a failure in one area from exhausting resources needed by other areas.","An ACL is a translation layer that converts between a legacy system's (or external system's) data model and your internal domain model.","Graceful shutdown ensures that when a pod is terminated, it completes in-flight requests before shutting down."]},{"id":"backend/distributed_system_operational_patterns#bca9aaad6b54","section":"backend","module":"backend/distributed_system_operational_patterns","moduleName":"distributed system operational patterns","qIndex":3,"difficulty":"core","question":"What is an anti-corruption layer (ACL) and when do you need it?","answerFull":"An ACL is a translation layer that converts between a legacy system's (or external system's) data model and your internal domain model. You need it when integrating with systems that have different naming conventions, data formats, or domain concepts that would pollute your clean domain if imported directly. Without an ACL, legacy field names (cust_no, order_total_amount) leak into your domain classes, and future changes to the legacy system require changes throughout your codebase. The ACL encapsulates all translation logic in one place; your domain never imports legacy types.","correct":"An ACL is a translation layer that converts between a legacy system's (or external system's) data model and your internal domain model.","distractors":["Graceful shutdown ensures that when a pod is terminated, it completes in-flight requests before shutting down.","HTTP: implement OncePerRequestFilter that reads X-Correlation-ID from incoming request (or generates a UUID if absent), puts it in MDC, and adds it to the outgoing response header.","The bulkhead pattern isolates resources to prevent a failure in one area from exhausting resources needed by other areas."]},{"id":"backend/distributed_system_operational_patterns#eb65888f88aa","section":"backend","module":"backend/distributed_system_operational_patterns","moduleName":"distributed system operational patterns","qIndex":4,"difficulty":"core","question":"How do you propagate correlation IDs across service boundaries including async messaging?","answerFull":"HTTP: implement OncePerRequestFilter that reads X-Correlation-ID from incoming request (or generates a UUID if absent), puts it in MDC, and adds it to the outgoing response header. Inject the correlation ID into outgoing HTTP calls via RestTemplate interceptor or WebClient filter. Kafka: when publishing, add the correlation ID as a Kafka message header. When consuming, extract the header and put it in MDC before processing. Async tasks: capture MDC.getCopyOfContextMap() before submitting to an executor, restore it at the start of the task. The key constraint: the same correlation ID must flow through the entire request path across all services.","correct":"HTTP: implement OncePerRequestFilter that reads X-Correlation-ID from incoming request (or generates a UUID if absent), puts it in MDC, and adds it to the outgoing response header.","distractors":["Use a dedicated feature flag service (Unleash, LaunchDarkly) that evaluates flags server-side based on context (user ID, percentage, environment).","An ACL is a translation layer that converts between a legacy system's (or external system's) data model and your internal domain model.","Graceful shutdown ensures that when a pod is terminated, it completes in-flight requests before shutting down."]},{"id":"backend/distributed_system_operational_patterns#91970ddb27d7","section":"backend","module":"backend/distributed_system_operational_patterns","moduleName":"distributed system operational patterns","qIndex":5,"difficulty":"core","question":"What is the difference between sidecar and ambassador patterns?","answerFull":"Both patterns deploy a helper container alongside the main application, but they serve different purposes. The sidecar extends or enhances the main application's capabilities: log shipping, config sync, distributed tracing agent, service mesh proxy. The sidecar is specific to this application's infrastructure needs. The ambassador acts as a proxy for outbound connections from the main application: it handles retry logic, circuit breaking, and service discovery on behalf of the main container. Ambassador is useful when the main container is a legacy application that cannot be modified — the ambassador adds modern networking capabilities transparently. In practice, Envoy in a service mesh acts as both sidecar (receives inbound) and ambassador (handles outbound).","correct":"Both patterns deploy a helper container alongside the main application, but they serve different purposes.","distractors":["An ACL is a translation layer that converts between a legacy system's (or external system's) data model and your internal domain model.","Graceful shutdown ensures that when a pod is terminated, it completes in-flight requests before shutting down.","HTTP: implement OncePerRequestFilter that reads X-Correlation-ID from incoming request (or generates a UUID if absent), puts it in MDC, and adds it to the outgoing response header."]},{"id":"backend/distributed_system_operational_patterns#e0cf68003be9","section":"backend","module":"backend/distributed_system_operational_patterns","moduleName":"distributed system operational patterns","qIndex":6,"difficulty":"intermediate","question":"What is graceful shutdown and why is the preStop hook important in Kubernetes?","answerFull":"Graceful shutdown ensures that when a pod is terminated, it completes in-flight requests before shutting down. In Spring Boot, server.shutdown=graceful stops the Tomcat acceptor from accepting new connections and waits for active requests to complete (up to timeout-per-shutdown-phase). The Kubernetes preStop hook is important because Kubernetes sends SIGTERM to the container and simultaneously removes the pod from Service endpoints. iptables rules take 1-5 seconds to propagate across all nodes. Without a preStop sleep (typically 5-10 seconds), traffic continues arriving at the pod from load balancers that haven't yet received the endpoint update, resulting in connection reset errors for in-flight requests.","correct":"Graceful shutdown ensures that when a pod is terminated, it completes in-flight requests before shutting down.","distractors":["An ACL is a translation layer that converts between a legacy system's (or external system's) data model and your internal domain model.","HTTP: implement OncePerRequestFilter that reads X-Correlation-ID from incoming request (or generates a UUID if absent), puts it in MDC, and adds it to the outgoing response header.","The bulkhead pattern isolates resources to prevent a failure in one area from exhausting resources needed by other areas."]},{"id":"backend/distributed_system_operational_patterns#2b103daadc0f","section":"backend","module":"backend/distributed_system_operational_patterns","moduleName":"distributed system operational patterns","qIndex":7,"difficulty":"intermediate","question":"How do you implement feature flags safely at scale?","answerFull":"Use a dedicated feature flag service (Unleash, LaunchDarkly) that evaluates flags server-side based on context (user ID, percentage, environment). The application makes an SDK call that evaluates the flag locally against downloaded rules — no network call per request. Implement per-user or percentage-based targeting for gradual rollouts. Each flag must have: an owner, a description, an expected removal date, and a default value for when the flag service is unavailable. Use flags for dark launches (deployed but off), canary releases (10% of users), and kill switches (disable a misbehaving feature without a deployment). Clean up flags after full rollout — stale flags are technical debt.","correct":"Use a dedicated feature flag service (Unleash, LaunchDarkly) that evaluates flags server-side based on context (user ID, percentage, environment).","distractors":["HTTP: implement OncePerRequestFilter that reads X-Correlation-ID from incoming request (or generates a UUID if absent), puts it in MDC, and adds it to the outgoing response header.","An ACL is a translation layer that converts between a legacy system's (or external system's) data model and your internal domain model.","Graceful shutdown ensures that when a pod is terminated, it completes in-flight requests before shutting down."]},{"id":"backend/distributed_transactions_and_consistency#971d16871106","section":"backend","module":"backend/distributed_transactions_and_consistency","moduleName":"distributed transactions and consistency","qIndex":1,"difficulty":"core","question":"What is the blocking problem in 2PC and why does it make 2PC unsuitable for microservices?","answerFull":"In 2PC, once a participant votes VOTE_COMMIT, it cannot unilaterally abort — it must hold all its locks until it hears the coordinator's decision. If the coordinator crashes after the prepare phase, participants are blocked indefinitely, holding locks that prevent other transactions from proceeding. In a microservices environment, services are independently deployable and may use different datastores that do not expose an XA interface. The coordinator is a single point of failure, network partitions can leave participants waiting for minutes, and lock hold times across network round-trips degrade throughput unacceptably for most SLAs.","correct":"In 2PC, once a participant votes VOTE_COMMIT, it cannot unilaterally abort — it must hold all its locks until it hears the coordinator's decision.","distractors":["First, all compensation commands must be sent to queues with configured dead-letter queues (DLQs) and retry policies (exponential backoff with max retries).","In a choreography saga, each service reacts to domain events published by the previous step, with no central coordinator.","At-most-once delivery means messages may be lost but are never duplicated; achieved by committing consumer offsets before processing (enable.auto.commit=true with short intervals, or manual pre-processing commit)."]},{"id":"backend/distributed_transactions_and_consistency#51d4ac6f0862","section":"backend","module":"backend/distributed_transactions_and_consistency","moduleName":"distributed transactions and consistency","qIndex":2,"difficulty":"core","question":"How does the saga pattern achieve atomicity without distributed locking?","answerFull":"A saga replaces distributed atomic commits with a sequence of local transactions and compensating transactions. Each service performs an ACID local transaction and publishes an event or responds to a command. If any step fails, all previously completed steps are reversed by executing their compensating transactions in reverse order. There are no distributed locks; intermediate states are visible (sacrificing isolation), and compensating transactions must be idempotent to handle retries. The guarantee achieved is ACD (Atomicity, Consistency, Durability) without Isolation — sometimes called the ACI problem of sagas.","correct":"A saga replaces distributed atomic commits with a sequence of local transactions and compensating transactions.","distractors":["Sagas execute as a sequence of local transactions; between steps, intermediate state is committed to each service's database and is visible to other requests.","If the orchestrator is stateless (saga state persisted entirely in the database), a rolling deployment is safe: new orchestrator instances pick up saga processing by reading state from the DB.","In a choreography saga, each service reacts to domain events published by the previous step, with no central coordinator."]},{"id":"backend/distributed_transactions_and_consistency#6c1f830908a9","section":"backend","module":"backend/distributed_transactions_and_consistency","moduleName":"distributed transactions and consistency","qIndex":3,"difficulty":"core","question":"What is the difference between choreography and orchestration sagas? When would you choose each?","answerFull":"In a choreography saga, each service reacts to domain events published by the previous step, with no central coordinator. The workflow emerges from event subscriptions. In an orchestration saga, a central orchestrator explicitly sends commands to each service and receives replies, maintaining the workflow state in a persistent state machine. Choreography favors loose coupling and suits simpler linear flows with 3–4 steps. Orchestration suits complex workflows with many branches, conditional paths, and compensation sequences, because all business logic is in one place and much easier to debug and monitor. Most production-grade payment and order fulfillment systems use orchestration.","correct":"In a choreography saga, each service reacts to domain events published by the previous step, with no central coordinator.","distractors":["In 2PC, once a participant votes VOTE_COMMIT, it cannot unilaterally abort — it must hold all its locks until it hears the coordinator's decision.","If the orchestrator is stateless (saga state persisted entirely in the database), a rolling deployment is safe: new orchestrator instances pick up saga processing by reading state from the DB.","Semantic locking is an application-level lock that signals to other transactions that a record is in a pending state as part of an in-flight saga."]},{"id":"backend/distributed_transactions_and_consistency#c0b5aa9868b6","section":"backend","module":"backend/distributed_transactions_and_consistency","moduleName":"distributed transactions and consistency","qIndex":4,"difficulty":"core","question":"Why is dual write dangerous and how does the transactional outbox pattern solve it?","answerFull":"Dual write means writing to a database and publishing to a message broker in two separate operations. Any crash between them leaves the two systems inconsistent — the DB committed but the event never published, or the event was published but the DB write was rolled back. The outbox pattern solves this by writing both the domain entity and an outbox_events record in a single local ACID database transaction. A separate relay process reads the unpublished outbox records and publishes them to the broker, then marks them published. Because the relay uses at-least-once publishing and consumers are idempotent, the system achieves end-to-end exactly-once semantics.","correct":"Dual write means writing to a database and publishing to a message broker in two separate operations.","distractors":["The polling outbox is simpler to operate: one scheduled job, no external infrastructure beyond the existing DB and broker.","At-most-once delivery means messages may be lost but are never duplicated; achieved by committing consumer offsets before processing (enable.auto.commit=true with short intervals, or manual pre-processing commit).","CDC captures every insert, update, and delete from a database by tailing its write-ahead log or binary log — the same log the database uses for replication."]},{"id":"backend/distributed_transactions_and_consistency#b4960c569aea","section":"backend","module":"backend/distributed_transactions_and_consistency","moduleName":"distributed transactions and consistency","qIndex":5,"difficulty":"core","question":"What is Change Data Capture (CDC) and how does Debezium work?","answerFull":"CDC captures every insert, update, and delete from a database by tailing its write-ahead log or binary log — the same log the database uses for replication. Debezium connects as a logical replication client to PostgreSQL (using the pgoutput or wal2json output plugin) or as a binlog consumer for MySQL. It reads row-level change events and publishes them to Kafka topics. This means applications do not need to poll an outbox table; Debezium detects changes within milliseconds of the WAL flush with sub-100ms latency. It requires no application code changes for the outbox emit step — only the outbox table structure matters.","correct":"CDC captures every insert, update, and delete from a database by tailing its write-ahead log or binary log — the same log the database uses for replication.","distractors":["If the orchestrator is stateless (saga state persisted entirely in the database), a rolling deployment is safe: new orchestrator instances pick up saga processing by reading state from the DB.","Sagas execute as a sequence of local transactions; between steps, intermediate state is committed to each service's database and is visible to other requests.","A true database rollback is a physical undo: the exact bytes written are removed, as if the operation never happened."]},{"id":"backend/distributed_transactions_and_consistency#49767188fb84","section":"backend","module":"backend/distributed_transactions_and_consistency","moduleName":"distributed transactions and consistency","qIndex":6,"difficulty":"intermediate","question":"What makes a compensating transaction \"approximately correct\" rather than a true rollback?","answerFull":"A true database rollback is a physical undo: the exact bytes written are removed, as if the operation never happened. A compensating transaction is a new forward operation with the opposite business effect. \"Approximately correct\" means the end business state is acceptable, but some side effects cannot be reversed. For example, if step 3 sent a confirmation email to a customer and step 4 fails, the compensation cannot \"un-send\" the email. The order is cancelled (compensated at the DB level), but the customer received a confirmation that is now incorrect. The business must decide whether to send a follow-up cancellation email or accept the inconsistency. Compensation design must explicitly document these irreversible side effects.","correct":"A true database rollback is a physical undo: the exact bytes written are removed, as if the operation never happened.","distractors":["If the orchestrator is stateless (saga state persisted entirely in the database), a rolling deployment is safe: new orchestrator instances pick up saga processing by reading state from the DB.","Dual write means writing to a database and publishing to a message broker in two separate operations.","CDC captures every insert, update, and delete from a database by tailing its write-ahead log or binary log — the same log the database uses for replication."]},{"id":"backend/distributed_transactions_and_consistency#60324ee3401a","section":"backend","module":"backend/distributed_transactions_and_consistency","moduleName":"distributed transactions and consistency","qIndex":7,"difficulty":"intermediate","question":"What are idempotency keys and how should they be scoped and expired?","answerFull":"An idempotency key is a client-generated unique identifier (typically UUID v4) attached to a mutating request. The server uses this key to detect and deduplicate retries, returning the original response without reprocessing. Keys should be scoped per operation type (not reused across different endpoint types). They should be stored with the operation's result in a single atomic transaction. Expiry should match the retry window: 24 hours for payment APIs (matching typical client retry timeouts), 7 days for long-running workflows. After expiry, the same key can be used for a genuinely new request. Keys stored without TTL cause unbounded table growth.","correct":"An idempotency key is a client-generated unique identifier (typically UUID v4) attached to a mutating request.","distractors":["A saga replaces distributed atomic commits with a sequence of local transactions and compensating transactions.","A true database rollback is a physical undo: the exact bytes written are removed, as if the operation never happened.","At-most-once delivery means messages may be lost but are never duplicated; achieved by committing consumer offsets before processing (enable.auto.commit=true with short intervals, or manual pre-processing commit)."]},{"id":"backend/distributed_transactions_and_consistency#357ded7b6038","section":"backend","module":"backend/distributed_transactions_and_consistency","moduleName":"distributed transactions and consistency","qIndex":8,"difficulty":"intermediate","question":"What is the difference between at-least-once, at-most-once, and exactly-once delivery, and how do you achieve each in Kafka?","answerFull":"At-most-once delivery means messages may be lost but are never duplicated; achieved by committing consumer offsets before processing (enable.auto.commit=true with short intervals, or manual pre-processing commit). At-least-once means messages are never lost but may be duplicated; achieved with acks=all, retries=MAX_INT, and committing offsets only after successful processing. Exactly-once within Kafka is achieved with idempotent producers (enable.idempotence=true, which adds sequence numbers to prevent broker-side duplication) and Kafka transactions (transactional.id, isolation.level=read_committed). End-to-end exactly-once with external systems (databases) requires the transactional inbox pattern because Kafka transactions do not span external systems.","correct":"At-most-once delivery means messages may be lost but are never duplicated; achieved by committing consumer offsets before processing (enable.auto.commit=true with short intervals, or manual pre-processing commit).","distractors":["A true database rollback is a physical undo: the exact bytes written are removed, as if the operation never happened.","Dual write means writing to a database and publishing to a message broker in two separate operations.","If the orchestrator is stateless (saga state persisted entirely in the database), a rolling deployment is safe: new orchestrator instances pick up saga processing by reading state from the DB."]},{"id":"backend/distributed_transactions_and_consistency#665ec956549c","section":"backend","module":"backend/distributed_transactions_and_consistency","moduleName":"distributed transactions and consistency","qIndex":9,"difficulty":"intermediate","question":"How do you handle a saga that is stuck in the compensating state?","answerFull":"First, all compensation commands must be sent to queues with configured dead-letter queues (DLQs) and retry policies (exponential backoff with max retries). A scheduled monitoring job should query for sagas in COMPENSATING state for longer than a threshold (e.g., 30 minutes) and alert an oncall engineer. The system should provide an operator console to manually trigger individual compensation commands or mark a saga as FAILED after human review. For truly unrecoverable compensations (downstream service permanently gone), the saga is marked FAILED with audit trail, and a data reconciliation report is generated for manual resolution. The key principle: never silently drop compensation failures.","correct":"First, all compensation commands must be sent to queues with configured dead-letter queues (DLQs) and retry policies (exponential backoff with max retries).","distractors":["Sagas execute as a sequence of local transactions; between steps, intermediate state is committed to each service's database and is visible to other requests.","In 2PC, once a participant votes VOTE_COMMIT, it cannot unilaterally abort — it must hold all its locks until it hears the coordinator's decision.","If the orchestrator is stateless (saga state persisted entirely in the database), a rolling deployment is safe: new orchestrator instances pick up saga processing by reading state from the DB."]},{"id":"backend/distributed_transactions_and_consistency#b3e763584fca","section":"backend","module":"backend/distributed_transactions_and_consistency","moduleName":"distributed transactions and consistency","qIndex":10,"difficulty":"intermediate","question":"What is causal consistency and when is it not sufficient?","answerFull":"Causal consistency guarantees that if operation B is causally dependent on operation A (B happened after A, and B's author saw A's result), then all observers see A before B. It is not sufficient when independent concurrent writes must be reconciled — causal consistency does not impose an order on causally unrelated operations. Example: if user A and user B both independently update their profile photos at the same time, causal consistency does not guarantee all nodes see these updates in the same order. For applications requiring a global total order of operations (financial ledgers, counter increments), linearizability or sequential consistency is required, at higher latency cost.","correct":"Causal consistency guarantees that if operation B is causally dependent on operation A (B happened after A, and B's author saw A's result), then all observers see A before B.","distractors":["A true database rollback is a physical undo: the exact bytes written are removed, as if the operation never happened.","A saga replaces distributed atomic commits with a sequence of local transactions and compensating transactions.","An idempotency key is a client-generated unique identifier (typically UUID v4) attached to a mutating request."]},{"id":"backend/distributed_transactions_and_consistency#d58f7b6895a3","section":"backend","module":"backend/distributed_transactions_and_consistency","moduleName":"distributed transactions and consistency","qIndex":12,"difficulty":"advanced","question":"What is the ACI problem of sagas (isolation) and how do microservices deal with it?","answerFull":"Sagas execute as a sequence of local transactions; between steps, intermediate state is committed to each service's database and is visible to other requests. Unlike a database transaction with SERIALIZABLE isolation, there is no saga-level isolation. This creates anomalies: a second request might read the inventory reservation made in step 2 of a saga, but the saga later compensates step 2 (reverses the reservation). The second request made a decision based on state that no longer exists — a \"dirty read\" at the saga level. Mitigations include: semantic locking (reserving records with a PENDING status that other operations treat as unavailable), optimistic locking with version numbers, and commutative operations that are safe to reorder.","correct":"Sagas execute as a sequence of local transactions; between steps, intermediate state is committed to each service's database and is visible to other requests.","distractors":["A true database rollback is a physical undo: the exact bytes written are removed, as if the operation never happened.","In a choreography saga, each service reacts to domain events published by the previous step, with no central coordinator.","Semantic locking is an application-level lock that signals to other transactions that a record is in a pending state as part of an in-flight saga."]},{"id":"backend/distributed_transactions_and_consistency#6ccc4ac0f064","section":"backend","module":"backend/distributed_transactions_and_consistency","moduleName":"distributed transactions and consistency","qIndex":13,"difficulty":"advanced","question":"Compare the operational complexity of Debezium CDC outbox vs. polling outbox.","answerFull":"The polling outbox is simpler to operate: one scheduled job, no external infrastructure beyond the existing DB and broker. It introduces polling latency (500ms to 5s typical), load on the DB with repeated queries, and potential for stale reads if the partial index is not properly maintained. Debezium CDC requires Kafka Connect infrastructure (at least 3 nodes for production HA), connector management, schema registry for Avro/Protobuf serialization, WAL retention configuration (PostgreSQL wal_level=logical, max_replication_slots, max_wal_senders), and slot lag monitoring (unbounded WAL growth if the slot consumer falls behind). In exchange, Debezium achieves sub-100ms latency, zero polling DB load, and supports any table change not just the outbox. Choose polling for simplicity and low volume; choose CDC for latency-sensitive, high-volume, or multi-table change capture.","correct":"The polling outbox is simpler to operate: one scheduled job, no external infrastructure beyond the existing DB and broker.","distractors":["CDC captures every insert, update, and delete from a database by tailing its write-ahead log or binary log — the same log the database uses for replication.","Dual write means writing to a database and publishing to a message broker in two separate operations.","A saga replaces distributed atomic commits with a sequence of local transactions and compensating transactions."]},{"id":"backend/distributed_transactions_and_consistency#16335b1226c3","section":"backend","module":"backend/distributed_transactions_and_consistency","moduleName":"distributed transactions and consistency","qIndex":14,"difficulty":"advanced","question":"What is semantic locking in the context of sagas, and give a concrete example?","answerFull":"Semantic locking is an application-level lock that signals to other transactions that a record is in a pending state as part of an in-flight saga. Unlike a database lock, it is implemented by setting a status field. For example, when a saga begins an inventory reservation, the inventory record is set to status = PENDING_RESERVATION. Other sagas or queries that attempt to reserve the same inventory check for this status and either wait (polling) or fail fast (return \"temporarily unavailable\"). When the saga completes or compensates, the status changes to RESERVED or AVAILABLE. This prevents concurrent sagas from operating on the same resource simultaneously without database-level locking.","correct":"Semantic locking is an application-level lock that signals to other transactions that a record is in a pending state as part of an in-flight saga.","distractors":["Sagas execute as a sequence of local transactions; between steps, intermediate state is committed to each service's database and is visible to other requests.","A saga replaces distributed atomic commits with a sequence of local transactions and compensating transactions.","If the orchestrator is stateless (saga state persisted entirely in the database), a rolling deployment is safe: new orchestrator instances pick up saga processing by reading state from the DB."]},{"id":"backend/distributed_transactions_and_consistency#2d102beb148c","section":"backend","module":"backend/distributed_transactions_and_consistency","moduleName":"distributed transactions and consistency","qIndex":15,"difficulty":"advanced","question":"What happens to in-flight sagas during a rolling deployment of the orchestrator service?","answerFull":"If the orchestrator is stateless (saga state persisted entirely in the database), a rolling deployment is safe: new orchestrator instances pick up saga processing by reading state from the DB. The key requirements are: (1) saga state transitions must be optimistically locked (version column) to prevent two instances from processing the same saga concurrently during overlap, (2) all commands and events must carry the saga ID so any orchestrator instance can handle replies, (3) idempotency keys prevent duplicate command dispatch if a timeout triggers a retry and a new instance also attempts the same step. Frameworks like Temporal handle this with native workflow state management and versioning to support backward-compatible changes to running workflow logic.","correct":"If the orchestrator is stateless (saga state persisted entirely in the database), a rolling deployment is safe: new orchestrator instances pick up saga processing by reading state from the DB.","distractors":["A saga replaces distributed atomic commits with a sequence of local transactions and compensating transactions.","In a choreography saga, each service reacts to domain events published by the previous step, with no central coordinator.","At-most-once delivery means messages may be lost but are never duplicated; achieved by committing consumer offsets before processing (enable.auto.commit=true with short intervals, or manual pre-processing commit)."]},{"id":"backend/event_driven_fundamentals#4e85ad7b7b4e","section":"backend","module":"backend/event_driven_fundamentals","moduleName":"event driven fundamentals","qIndex":1,"difficulty":"core","question":"What is the difference between an event, a command, and a query?","answerFull":"An event is a record of something that has already happened — it is past tense, has no single intended recipient, and expects no response. A command is an instruction directed at a specific service — it expects execution. A query is a read-only request for data — it expects a data response and must not change state. This distinction is the foundation of CQS (Command-Query Separation) at the method level and CQRS at the architectural level.","correct":"An event is a record of something that has already happened — it is past tense, has no single intended recipient, and expects no response.","distractors":["CQRS separates the write model (command side) from the read model (query side) at the architectural level.","A policy (pink sticky) is an automation rule: \"When [event] THEN [command]\".","Inject the correlationId as the distributed trace ID in every event envelope."]},{"id":"backend/event_driven_fundamentals#a89aa8d5f1fc","section":"backend","module":"backend/event_driven_fundamentals","moduleName":"event driven fundamentals","qIndex":2,"difficulty":"core","question":"What are the four types of events in event taxonomy?","answerFull":"Domain events are internal to a bounded context and carry rich domain meaning. Integration events are published across service boundaries and are deliberately schema-stable for external consumers. Notification events (thin events) carry only identifiers and require consumers to call back for current state. Event-Carried State Transfer events carry the full current state of the aggregate and eliminate the need to call back, at the cost of larger payloads and potential staleness under consumer lag.","correct":"Domain events are internal to a bounded context and carry rich domain meaning.","distractors":["Fine-grained events (OrderItemAdded, ShippingAddressUpdated) give consumers maximum flexibility to react only to what changed, but increase event volume and complexity.","A notification event carries only the aggregate identifier (and event type), no payload data.","A policy (pink sticky) is an automation rule: \"When [event] THEN [command]\"."]},{"id":"backend/event_driven_fundamentals#f50c60a2b5e3","section":"backend","module":"backend/event_driven_fundamentals","moduleName":"event driven fundamentals","qIndex":3,"difficulty":"core","question":"What is the difference between choreography and orchestration?","answerFull":"In choreography, services react autonomously to events with no central coordinator — each service subscribes to relevant topics and decides its reaction independently. In orchestration, a central saga orchestrator sends commands to participant services and listens for response events to drive the flow forward. Choreography is simpler for small stable flows; orchestration is preferable for complex multi-step flows where compensation logic is significant and traceability is required.","correct":"In choreography, services react autonomously to events with no central coordinator — each service subscribes to relevant topics and decides its reaction independently.","distractors":["You add a Process Manager (also called a Saga) that listens to events and tracks the overall flow state, but still communicates via events rather than direct commands.","Domain events are optimized for internal bounded context semantics.","Fine-grained events (OrderItemAdded, ShippingAddressUpdated) give consumers maximum flexibility to react only to what changed, but increase event volume and complexity."]},{"id":"backend/event_driven_fundamentals#4a632ebd5b8c","section":"backend","module":"backend/event_driven_fundamentals","moduleName":"event driven fundamentals","qIndex":5,"difficulty":"core","question":"Why must event consumers tolerate unknown fields?","answerFull":"This is the principle of forward compatibility. When a producer adds a new field to an event schema, old consumers that have not yet been deployed with the new schema must not break. Jackson's @JsonIgnoreProperties(ignoreUnknown = true) and Avro's schema evolution rules both support this. If consumers throw on unknown fields, every schema change requires coordinated simultaneous deployment of all consumers before the producer, which defeats the purpose of decoupled services.","correct":"This is the principle of forward compatibility.","distractors":["Backward compatibility means the new version of a schema can read data written with the old schema — achieved by only adding optional fields with defaults, never removing required fields.","An event is a record of something that has already happened — it is past tense, has no single intended recipient, and expects no response.","Fine-grained events (OrderItemAdded, ShippingAddressUpdated) give consumers maximum flexibility to react only to what changed, but increase event volume and complexity."]},{"id":"backend/event_driven_fundamentals#cce10c5b2dc1","section":"backend","module":"backend/event_driven_fundamentals","moduleName":"event driven fundamentals","qIndex":6,"difficulty":"intermediate","question":"What is the difference between backward and forward schema compatibility?","answerFull":"Backward compatibility means the new version of a schema can read data written with the old schema — achieved by only adding optional fields with defaults, never removing required fields. Forward compatibility means the old version of a schema can read data written with the new schema — achieved because consumers ignore unknown fields. Full compatibility (FULL mode in Schema Registry) requires both. In practice, always aim for FULL compatibility for integration events.","correct":"Backward compatibility means the new version of a schema can read data written with the old schema — achieved by only adding optional fields with defaults, never removing required fields.","distractors":["This is the principle of forward compatibility.","CQRS separates the write model (command side) from the read model (query side) at the architectural level.","A notification event carries only the aggregate identifier (and event type), no payload data."]},{"id":"backend/event_driven_fundamentals#e8647b61dff6","section":"backend","module":"backend/event_driven_fundamentals","moduleName":"event driven fundamentals","qIndex":7,"difficulty":"intermediate","question":"What is event storming and when do you use it?","answerFull":"Event storming is a collaborative workshop technique invented by Alberto Brandolini where business and technical stakeholders collaboratively map domain events on a timeline using color-coded sticky notes. Orange stickies are domain events, blue are commands, yellow are aggregates, pink are policies, and purple are external systems. It is used when designing new services, decomposing a monolith, or aligning business and technical understanding of a complex domain. A well-run event storming session can surface bounded context boundaries and aggregate designs in a day that would take weeks of documentation-only analysis.","correct":"Event storming is a collaborative workshop technique invented by Alberto Brandolini where business and technical stakeholders collaboratively map domain events on a timeline using color-coded sticky notes.","distractors":["A policy (pink sticky) is an automation rule: \"When [event] THEN [command]\".","Fine-grained events (OrderItemAdded, ShippingAddressUpdated) give consumers maximum flexibility to react only to what changed, but increase event volume and complexity.","In choreography, services react autonomously to events with no central coordinator — each service subscribes to relevant topics and decides its reaction independently."]},{"id":"backend/event_driven_fundamentals#1c6df92a0f07","section":"backend","module":"backend/event_driven_fundamentals","moduleName":"event driven fundamentals","qIndex":8,"difficulty":"intermediate","question":"What is the notification event pattern and when should you use it over ECST?","answerFull":"A notification event carries only the aggregate identifier (and event type), no payload data. The consumer must call back to the producer's API to fetch current state. Use it when the consumer always needs the absolute latest state (not the state at the time of the event), when events are high-frequency and payload size matters, or when multiple events for the same aggregate may be batched and only the final state matters. Use ECST (Event-Carried State Transfer) when you want maximum consumer autonomy and can tolerate slight staleness.","correct":"A notification event carries only the aggregate identifier (and event type), no payload data.","distractors":["An event is a record of something that has already happened — it is past tense, has no single intended recipient, and expects no response.","Inject the correlationId as the distributed trace ID in every event envelope.","Event storming is a collaborative workshop technique invented by Alberto Brandolini where business and technical stakeholders collaboratively map domain events on a timeline using color-coded sticky notes."]},{"id":"backend/event_driven_fundamentals#0ec9ca5abd86","section":"backend","module":"backend/event_driven_fundamentals","moduleName":"event driven fundamentals","qIndex":9,"difficulty":"intermediate","question":"How do you handle long-running choreographed flows that span many services?","answerFull":"You add a Process Manager (also called a Saga) that listens to events and tracks the overall flow state, but still communicates via events rather than direct commands. At the point where a choreographed flow has grown beyond 4–5 services, the alternative is to refactor to an orchestrated saga with an explicit state machine. The key signal is when engineers can no longer confidently describe the full flow without reading multiple codebases — that is the tipping point for orchestration.","correct":"You add a Process Manager (also called a Saga) that listens to events and tracks the overall flow state, but still communicates via events rather than direct commands.","distractors":["In choreography, services react autonomously to events with no central coordinator — each service subscribes to relevant topics and decides its reaction independently.","Fine-grained events (OrderItemAdded, ShippingAddressUpdated) give consumers maximum flexibility to react only to what changed, but increase event volume and complexity.","Domain events are optimized for internal bounded context semantics."]},{"id":"backend/event_driven_fundamentals#eb00bce59e3d","section":"backend","module":"backend/event_driven_fundamentals","moduleName":"event driven fundamentals","qIndex":10,"difficulty":"intermediate","question":"What is the causationId and why is it important?","answerFull":"The causationId is the eventId of the event that caused the current event to be emitted. It creates a causal chain: UserRegistered (causationId: null) → WelcomeEmailSent (causationId: UserRegistered.eventId) → EmailDeliveryConfirmed (causationId: WelcomeEmailSent.eventId). Combined with correlationId (which spans the entire flow), this allows you to reconstruct exactly what happened and why during incident investigation. Without causationId, you know what happened but not why.","correct":"The causationId is the eventId of the event that caused the current event to be emitted.","distractors":["Fine-grained events (OrderItemAdded, ShippingAddressUpdated) give consumers maximum flexibility to react only to what changed, but increase event volume and complexity.","An event is a record of something that has already happened — it is past tense, has no single intended recipient, and expects no response.","A policy (pink sticky) is an automation rule: \"When [event] THEN [command]\"."]},{"id":"backend/event_driven_fundamentals#759a4742a68b","section":"backend","module":"backend/event_driven_fundamentals","moduleName":"event driven fundamentals","qIndex":11,"difficulty":"advanced","question":"How do you trace an end-to-end event flow in a choreographed system?","answerFull":"Inject the correlationId as the distributed trace ID in every event envelope. Configure your message consumers to extract the correlationId and set it as the active span in your distributed tracing system (Jaeger, Zipkin, OpenTelemetry). All log statements, database queries, and outgoing events in that consumer execution are tagged with the same correlationId. In Jaeger, you can then search by correlationId and see the entire trace spanning all services, topics, and time delays.","correct":"Inject the correlationId as the distributed trace ID in every event envelope.","distractors":["Event storming is a collaborative workshop technique invented by Alberto Brandolini where business and technical stakeholders collaboratively map domain events on a timeline using color-coded sticky notes.","Fine-grained events (OrderItemAdded, ShippingAddressUpdated) give consumers maximum flexibility to react only to what changed, but increase event volume and complexity.","A notification event carries only the aggregate identifier (and event type), no payload data."]},{"id":"backend/event_driven_fundamentals#4e906ff588c4","section":"backend","module":"backend/event_driven_fundamentals","moduleName":"event driven fundamentals","qIndex":12,"difficulty":"advanced","question":"What is the risk of publishing domain events as integration events directly?","answerFull":"Domain events are optimized for internal bounded context semantics. They may reference domain objects, use internal identifiers, carry fields meaningful only within the context, or change structure frequently as the domain evolves. Publishing them directly to external consumers creates tight coupling between your internal model and external contracts. The fix is the anti-corruption layer: internal domain events are translated to explicitly versioned, deliberately designed integration events at the boundary of the context.","correct":"Domain events are optimized for internal bounded context semantics.","distractors":["Domain events are internal to a bounded context and carry rich domain meaning.","In choreography, services react autonomously to events with no central coordinator — each service subscribes to relevant topics and decides its reaction independently.","You add a Process Manager (also called a Saga) that listens to events and tracks the overall flow state, but still communicates via events rather than direct commands."]},{"id":"backend/event_driven_fundamentals#1fe9c8101b0a","section":"backend","module":"backend/event_driven_fundamentals","moduleName":"event driven fundamentals","qIndex":13,"difficulty":"advanced","question":"How do you decide event granularity — fine-grained vs coarse-grained events?","answerFull":"Fine-grained events (OrderItemAdded, ShippingAddressUpdated) give consumers maximum flexibility to react only to what changed, but increase event volume and complexity. Coarse-grained events (OrderUpdated carrying full state) are simpler to consume but trigger unnecessary processing when consumers only care about specific changes. The rule of thumb: use fine-grained events for high-value business facts that many consumers react to differently; use coarse-grained ECST events when most consumers need the full state anyway.","correct":"Fine-grained events (OrderItemAdded, ShippingAddressUpdated) give consumers maximum flexibility to react only to what changed, but increase event volume and complexity.","distractors":["A notification event carries only the aggregate identifier (and event type), no payload data.","An event is a record of something that has already happened — it is past tense, has no single intended recipient, and expects no response.","Event storming is a collaborative workshop technique invented by Alberto Brandolini where business and technical stakeholders collaboratively map domain events on a timeline using color-coded sticky notes."]},{"id":"backend/event_driven_fundamentals#9624456be454","section":"backend","module":"backend/event_driven_fundamentals","moduleName":"event driven fundamentals","qIndex":14,"difficulty":"advanced","question":"What is the difference between a policy and a reaction in event storming?","answerFull":"A policy (pink sticky) is an automation rule: \"When [event] THEN [command]\". It is a business rule, not a technical one. \"When PaymentFailed THEN NotifyCustomer AND CancelOrder.\" A reaction is the technical implementation of a policy — the consumer code that executes when the event arrives. The distinction matters in event storming because policies belong on the business level and should be validated with domain experts, not assumed by engineers.","correct":"A policy (pink sticky) is an automation rule: \"When [event] THEN [command]\".","distractors":["The causationId is the eventId of the event that caused the current event to be emitted.","Event storming is a collaborative workshop technique invented by Alberto Brandolini where business and technical stakeholders collaboratively map domain events on a timeline using color-coded sticky notes.","Inject the correlationId as the distributed trace ID in every event envelope."]},{"id":"backend/event_driven_fundamentals#30bde13c3283","section":"backend","module":"backend/event_driven_fundamentals","moduleName":"event driven fundamentals","qIndex":15,"difficulty":"advanced","question":"How does CQRS relate to events, commands, and queries?","answerFull":"CQRS separates the write model (command side) from the read model (query side) at the architectural level. Commands flow to the write model which validates business invariants, changes state, and emits domain events. Those events are consumed by projectors that build denormalized read models optimized for specific query patterns. Queries flow only to the read models and never touch the write model. This enables the write side to be strongly consistent and the read side to be eventually consistent, independently scalable, and freely denormalized.","correct":"CQRS separates the write model (command side) from the read model (query side) at the architectural level.","distractors":["Domain events are internal to a bounded context and carry rich domain meaning.","Fine-grained events (OrderItemAdded, ShippingAddressUpdated) give consumers maximum flexibility to react only to what changed, but increase event volume and complexity.","In choreography, services react autonomously to events with no central coordinator — each service subscribes to relevant topics and decides its reaction independently."]},{"id":"backend/event_sourcing_and_cqrs#2aefc5f484f1","section":"backend","module":"backend/event_sourcing_and_cqrs","moduleName":"event sourcing and cqrs","qIndex":1,"difficulty":"core","question":"What is event sourcing and how does it differ from traditional state persistence?","answerFull":"In traditional state persistence, you store the current state of an entity and update it in-place: UPDATE orders SET status = 'SHIPPED'. In event sourcing, you store the sequence of events that led to the current state and never update records: you append OrderShipped{orderId, trackingNumber, shippedAt}. Current state is derived by replaying all events for an aggregate from the beginning. The difference is that traditional persistence answers \"what is the current state?\" while event sourcing answers \"what happened and how did we get here?\"","correct":"In traditional state persistence, you store the current state of an entity and update it in-place: UPDATE orders SET status = 'SHIPPED'.","distractors":["When a command handler loads an aggregate, it knows the current version (the version of the last event).","Axon Framework is a Java framework for implementing CQRS and event sourcing.","Event replay is re-processing historical events from the event store."]},{"id":"backend/event_sourcing_and_cqrs#664bb6443660","section":"backend","module":"backend/event_sourcing_and_cqrs","moduleName":"event sourcing and cqrs","qIndex":2,"difficulty":"core","question":"What is CQRS and why would you use it?","answerFull":"CQRS separates the write model (command side) from the read model (query side). The command model enforces business invariants and maintains consistency. The read model is denormalized and optimized for specific query patterns — no joins, no complex aggregations at query time. You use CQRS when: reads and writes have vastly different load patterns (10:1 or higher read-to-write ratio), reads require different shapes than the write model (list view vs detail view vs reporting), or different consistency requirements exist (reads can be eventually consistent, writes must be strongly consistent).","correct":"CQRS separates the write model (command side) from the read model (query side).","distractors":["Axon Framework is a Java framework for implementing CQRS and event sourcing.","In a microservices architecture, each service naturally has its own write model (its aggregate) and can publish events that other services project into their own read models.","By default, CQRS with async projections means a write followed immediately by a read may not see the written data."]},{"id":"backend/event_sourcing_and_cqrs#be5d0dc0a0e5","section":"backend","module":"backend/event_sourcing_and_cqrs","moduleName":"event sourcing and cqrs","qIndex":3,"difficulty":"core","question":"What is an aggregate in the context of event sourcing?","answerFull":"An aggregate is a cluster of domain objects treated as a single unit for data consistency. It is the boundary of transactional consistency — all changes within an aggregate are atomic. In event sourcing, each aggregate has its own ordered event stream. The aggregate root is the only entry point for modifying the aggregate — all commands go through it. The aggregate validates business invariants before emitting events. Examples: Order aggregate (contains OrderItems, ShippingAddress, PaymentDetails), BankAccount aggregate (contains transactions, balance).","correct":"An aggregate is a cluster of domain objects treated as a single unit for data consistency.","distractors":["In a microservices architecture, each service naturally has its own write model (its aggregate) and can publish events that other services project into their own read models.","A snapshot is a point-in-time serialization of an aggregate's state, stored alongside the event log.","Choreography: each service reacts to domain events published by other services and takes action."]},{"id":"backend/event_sourcing_and_cqrs#9c914faee615","section":"backend","module":"backend/event_sourcing_and_cqrs","moduleName":"event sourcing and cqrs","qIndex":4,"difficulty":"core","question":"What are snapshots in event sourcing and when should you use them?","answerFull":"A snapshot is a point-in-time serialization of an aggregate's state, stored alongside the event log. When loading an aggregate, instead of replaying all events from the beginning, you load the most recent snapshot and only replay events that occurred after the snapshot's version. Use snapshots when: aggregate has more than 50-100 events (replay time > 50ms), or for aggregates with high event velocity. The snapshot threshold is typically 50-100 events. Snapshots trade storage space (snapshot table) for faster aggregate loading. They do not change the event log — events remain the source of truth.","correct":"A snapshot is a point-in-time serialization of an aggregate's state, stored alongside the event log.","distractors":["An aggregate is a cluster of domain objects treated as a single unit for data consistency.","Axon Framework is a Java framework for implementing CQRS and event sourcing.","In traditional state persistence, you store the current state of an entity and update it in-place: UPDATE orders SET status = 'SHIPPED'."]},{"id":"backend/event_sourcing_and_cqrs#19f30f15d36d","section":"backend","module":"backend/event_sourcing_and_cqrs","moduleName":"event sourcing and cqrs","qIndex":5,"difficulty":"core","question":"How do you handle schema evolution in event sourcing?","answerFull":"Events are immutable once written, so you cannot change their schema retroactively. Use event upcasters: a transformer that converts old event format to new format at read time. When loading events, the upcaster chain runs before the event reaches the aggregate or projection. An upcaster matches events by type and revision, transforms the payload (adds a new field with default value, renames a field, splits one event into two), and outputs the new format. Rules: adding optional fields is backward-compatible; removing or renaming fields requires an upcaster; changing semantics requires a new event type and a migration strategy.","correct":"Events are immutable once written, so you cannot change their schema retroactively.","distractors":["By default, CQRS with async projections means a write followed immediately by a read may not see the written data.","In a microservices architecture, each service naturally has its own write model (its aggregate) and can publish events that other services project into their own read models.","A snapshot is a point-in-time serialization of an aggregate's state, stored alongside the event log."]},{"id":"backend/event_sourcing_and_cqrs#833b5670f9c3","section":"backend","module":"backend/event_sourcing_and_cqrs","moduleName":"event sourcing and cqrs","qIndex":6,"difficulty":"intermediate","question":"What is the difference between choreography and orchestration in event-driven systems?","answerFull":"Choreography: each service reacts to domain events published by other services and takes action. There is no central coordinator — services are loosely coupled through events. Example: OrderCreated event → InventoryService reserves stock → publishes StockReserved → PaymentService charges → publishes PaymentCompleted. Orchestration: a central saga orchestrator sends commands to services and waits for replies, managing the workflow state machine. Example: OrderSagaOrchestrator sends ReserveInventoryCommand to InventoryService, receives StockReservedEvent, then sends ChargePaymentCommand. Choreography is more loosely coupled but harder to trace; orchestration makes the flow explicit and visible but introduces a coupled coordinator.","correct":"Choreography: each service reacts to domain events published by other services and takes action.","distractors":["Axon Framework is a Java framework for implementing CQRS and event sourcing.","In a microservices architecture, each service naturally has its own write model (its aggregate) and can publish events that other services project into their own read models.","When a command handler loads an aggregate, it knows the current version (the version of the last event)."]},{"id":"backend/event_sourcing_and_cqrs#01c381fbed89","section":"backend","module":"backend/event_sourcing_and_cqrs","moduleName":"event sourcing and cqrs","qIndex":7,"difficulty":"intermediate","question":"How do you implement \"read your own writes\" consistency with CQRS?","answerFull":"By default, CQRS with async projections means a write followed immediately by a read may not see the written data. Solutions: (1) Return the new state directly from the command handler response — skip the read model for the immediate redirect. (2) Synchronous projection: update the read model in the same transaction as the command (strong consistency, same DB required). (3) Wait-for-projection: after command, poll the read model with a timeout until the new entity appears (Awaitility-style in the UI). (4) Client-side cache: the frontend holds the just-written data locally and uses it for the immediate display without a round-trip.","correct":"By default, CQRS with async projections means a write followed immediately by a read may not see the written data.","distractors":["In a microservices architecture, each service naturally has its own write model (its aggregate) and can publish events that other services project into their own read models.","CQRS separates the write model (command side) from the read model (query side).","An aggregate is a cluster of domain objects treated as a single unit for data consistency."]},{"id":"backend/event_sourcing_and_cqrs#d4e72a484448","section":"backend","module":"backend/event_sourcing_and_cqrs","moduleName":"event sourcing and cqrs","qIndex":8,"difficulty":"intermediate","question":"What is event replay and what are its use cases?","answerFull":"Event replay is re-processing historical events from the event store. Use cases: (1) Build a new projection from existing data — e.g., add a new read model for a reporting dashboard by replaying all events from the beginning. (2) Fix a corrupted projection — if a bug in an event handler caused incorrect read model state, fix the bug, delete the read model, replay all events. (3) Temporal queries — replay events up to a specific timestamp to reconstruct historical state. (4) Debugging — replay events to reproduce a specific state and investigate a bug. Event replay is one of event sourcing's most powerful capabilities — it makes the event log a complete historical record that can be re-interpreted.","correct":"Event replay is re-processing historical events from the event store.","distractors":["Events are immutable once written, so you cannot change their schema retroactively.","Choreography: each service reacts to domain events published by other services and takes action.","In a microservices architecture, each service naturally has its own write model (its aggregate) and can publish events that other services project into their own read models."]},{"id":"backend/event_sourcing_and_cqrs#f6213d57dbef","section":"backend","module":"backend/event_sourcing_and_cqrs","moduleName":"event sourcing and cqrs","qIndex":9,"difficulty":"intermediate","question":"What is the optimistic concurrency check in an event store?","answerFull":"When a command handler loads an aggregate, it knows the current version (the version of the last event). When saving new events, it includes the expected version. The event store checks: is the current version of the aggregate in storage still equal to the expected version? If another process appended an event between load and save, the version will not match, and the save fails with an optimistic concurrency exception. The command handler then retries: reload the aggregate (now with the new event), re-validate invariants, re-apply the command. This is identical to JPA's @Version-based optimistic locking but for event streams.","correct":"When a command handler loads an aggregate, it knows the current version (the version of the last event).","distractors":["In traditional state persistence, you store the current state of an entity and update it in-place: UPDATE orders SET status = 'SHIPPED'.","A snapshot is a point-in-time serialization of an aggregate's state, stored alongside the event log.","An aggregate is a cluster of domain objects treated as a single unit for data consistency."]},{"id":"backend/event_sourcing_and_cqrs#48a5f51cb04f","section":"backend","module":"backend/event_sourcing_and_cqrs","moduleName":"event sourcing and cqrs","qIndex":10,"difficulty":"intermediate","question":"How does CQRS relate to microservices?","answerFull":"In a microservices architecture, each service naturally has its own write model (its aggregate) and can publish events that other services project into their own read models. Order service owns OrderAggregate and publishes OrderCreated, OrderShipped events. ShippingService subscribes to OrderShipped and maintains its own shipment read model. ReportingService subscribes to all order events and builds denormalized reporting tables. Each service owns its read model, denormalized for its specific queries. This avoids cross-service JOINs and lets each service's read model be independently scaled and optimized.","correct":"In a microservices architecture, each service naturally has its own write model (its aggregate) and can publish events that other services project into their own read models.","distractors":["An aggregate is a cluster of domain objects treated as a single unit for data consistency.","Choreography: each service reacts to domain events published by other services and takes action.","A snapshot is a point-in-time serialization of an aggregate's state, stored alongside the event log."]},{"id":"backend/event_sourcing_and_cqrs#bd751b699ad5","section":"backend","module":"backend/event_sourcing_and_cqrs","moduleName":"event sourcing and cqrs","qIndex":11,"difficulty":"advanced","question":"What is the Axon Framework and what problems does it solve?","answerFull":"Axon Framework is a Java framework for implementing CQRS and event sourcing. It provides: @Aggregate annotation for aggregates with automatic event sourcing, @CommandHandler for command handling, @EventSourcingHandler for state reconstruction, @EventHandler for projections, CommandGateway for dispatching commands, QueryGateway for querying projections, and AxonServer as the event store and message routing infrastructure. It solves the boilerplate of: event serialization, aggregate loading (snapshot + replay), optimistic concurrency, event publishing, and projection catch-up. It integrates natively with Spring Boot.","correct":"Axon Framework is a Java framework for implementing CQRS and event sourcing.","distractors":["When a command handler loads an aggregate, it knows the current version (the version of the last event).","Event replay is re-processing historical events from the event store.","A snapshot is a point-in-time serialization of an aggregate's state, stored alongside the event log."]},{"id":"backend/fault_tolerance_patterns#a415b2bded80","section":"backend","module":"backend/fault_tolerance_patterns","moduleName":"fault tolerance patterns","qIndex":1,"difficulty":"core","question":"What are the three states of a circuit breaker and what triggers each transition?","answerFull":"CLOSED is the normal state where all requests pass through; it transitions to OPEN when the failure rate exceeds the threshold over the minimum required calls. OPEN rejects all requests immediately without calling the downstream service; it transitions to HALF_OPEN after the waitDurationInOpenState expires. HALF_OPEN allows a limited number of probe calls through; it transitions back to CLOSED if the probe failure rate is below the threshold, or back to OPEN if it is still above. The most commonly forgotten transition is HALF_OPEN back to OPEN, which requires the probe calls to still be failing.","correct":"CLOSED is the normal state where all requests pass through; it transitions to OPEN when the failure rate exceeds the threshold over the minimum required calls.","distractors":["Set minimumNumberOfCalls high enough that the circuit breaker does not trip on the first few calls during startup.","Amdahl's Law states that parallelism is limited by the sequential portion of a program.","A retry storm occurs when a large number of clients simultaneously retry requests to a recovering service, overwhelming it before it can fully start serving traffic."]},{"id":"backend/fault_tolerance_patterns#98c518063757","section":"backend","module":"backend/fault_tolerance_patterns","moduleName":"fault tolerance patterns","qIndex":2,"difficulty":"core","question":"Why is fixed-delay retry dangerous in distributed systems?","answerFull":"Fixed-delay retry causes synchronized retry storms. If 10,000 clients all encounter a failure at the same time (e.g., a brief service restart), they all retry at exactly t+1s, t+2s, t+3s. This creates a thundering herd that can overwhelm the recovering service with 10,000 simultaneous requests every second, potentially preventing it from ever recovering. The fix is exponential backoff with full jitter, which desynchronizes retries by randomizing the delay across the range [0, exponential_cap].","correct":"Fixed-delay retry causes synchronized retry storms.","distractors":["A retry storm occurs when a large number of clients simultaneously retry requests to a recovering service, overwhelming it before it can fully start serving traffic.","A fallback is an immediate alternative response that is returned when the primary call fails — it does not undo anything, it just provides a substitute result.","Adaptive throttling (Google's approach) tracks the ratio of accepted to total requests from the client side and self-throttles before sending requests that are statistically likely to be rejected."]},{"id":"backend/fault_tolerance_patterns#c60d752ffcbb","section":"backend","module":"backend/fault_tolerance_patterns","moduleName":"fault tolerance patterns","qIndex":3,"difficulty":"core","question":"What is the difference between COUNT_BASED and TIME_BASED sliding windows?","answerFull":"COUNT_BASED tracks the last N calls regardless of when they happened; TIME_BASED tracks all calls that occurred in the last N seconds. COUNT_BASED is simpler and has fixed memory overhead but treats a single failure in 1 second the same as a single failure spread over 10 minutes. TIME_BASED is more accurate for bursty traffic and gives more weight to recent failures, but has variable memory usage because the number of calls in a window depends on traffic volume.","correct":"COUNT_BASED tracks the last N calls regardless of when they happened; TIME_BASED tracks all calls that occurred in the last N seconds.","distractors":["CLOSED is the normal state where all requests pass through; it transitions to OPEN when the failure rate exceeds the threshold over the minimum required calls.","Adaptive throttling (Google's approach) tracks the ratio of accepted to total requests from the client side and self-throttles before sending requests that are statistically likely to be rejected.","Set minimumNumberOfCalls high enough that the circuit breaker does not trip on the first few calls during startup."]},{"id":"backend/fault_tolerance_patterns#91f57bb2b20d","section":"backend","module":"backend/fault_tolerance_patterns","moduleName":"fault tolerance patterns","qIndex":4,"difficulty":"core","question":"When should you use a semaphore bulkhead vs. a thread pool bulkhead?","answerFull":"Use a semaphore bulkhead when your code is already non-blocking or reactive, since it only limits concurrency without adding threads. The semaphore bulkhead does not actually isolate the caller's thread — if the call blocks, the caller's thread is still tied up, just limited in count. Use a thread pool bulkhead when making blocking synchronous calls (JDBC, blocking HTTP clients) and you need full thread isolation. The thread pool bulkhead executes the call in a separate thread pool, so a hung downstream call only consumes a thread in that pool, not in the main application thread pool.","correct":"Use a semaphore bulkhead when your code is already non-blocking or reactive, since it only limits concurrency without adding threads.","distractors":["The circuit breaker throws CallNotPermittedException immediately, without making any network call.","A fallback is an immediate alternative response that is returned when the primary call fails — it does not undo anything, it just provides a substitute result.","A retry storm occurs when a large number of clients simultaneously retry requests to a recovering service, overwhelming it before it can fully start serving traffic."]},{"id":"backend/fault_tolerance_patterns#97e1bea7de19","section":"backend","module":"backend/fault_tolerance_patterns","moduleName":"fault tolerance patterns","qIndex":5,"difficulty":"core","question":"Why do you need both a connect timeout and a read timeout? Is one sufficient?","answerFull":"They protect against different failure modes. A connect timeout fires if the TCP handshake takes too long — this catches firewall drops, unreachable hosts, and network partitions that prevent the connection from being established. A read timeout fires if the connection is established but the server stops sending data — this catches servers that accept connections but hang internally (e.g., during a GC pause, deadlock, or resource exhaustion). A service can connect in 100ms but then hang for 30 seconds without sending any data. Without a read timeout, this hangs the calling thread indefinitely.","correct":"They protect against different failure modes.","distractors":["The slowCallDurationThreshold must be set lower than the HTTP client's read timeout.","CLOSED is the normal state where all requests pass through; it transitions to OPEN when the failure rate exceeds the threshold over the minimum required calls.","A fallback is an immediate alternative response that is returned when the primary call fails — it does not undo anything, it just provides a substitute result."]},{"id":"backend/fault_tolerance_patterns#1594780b2686","section":"backend","module":"backend/fault_tolerance_patterns","moduleName":"fault tolerance patterns","qIndex":6,"difficulty":"intermediate","question":"How does the circuit breaker's slow-call threshold interact with timeouts?","answerFull":"The slowCallDurationThreshold must be set lower than the HTTP client's read timeout. If the HTTP read timeout fires at 5 seconds and throws an IOException, the call is recorded as a failure. The slow-call counter is never triggered because the call never lasted long enough to be classified as slow — it was classified as failed. To have slow-call rate protection work correctly, the slow-call threshold should be set to a value less than the read timeout (e.g., slowCallDurationThreshold=2s with readTimeout=5s).","correct":"The slowCallDurationThreshold must be set lower than the HTTP client's read timeout.","distractors":["Set minimumNumberOfCalls high enough that the circuit breaker does not trip on the first few calls during startup.","A fallback is an immediate alternative response that is returned when the primary call fails — it does not undo anything, it just provides a substitute result.","Adaptive throttling (Google's approach) tracks the ratio of accepted to total requests from the client side and self-throttles before sending requests that are statistically likely to be rejected."]},{"id":"backend/fault_tolerance_patterns#43e0877663dc","section":"backend","module":"backend/fault_tolerance_patterns","moduleName":"fault tolerance patterns","qIndex":7,"difficulty":"intermediate","question":"What is a retry storm and how do you prevent it?","answerFull":"A retry storm occurs when a large number of clients simultaneously retry requests to a recovering service, overwhelming it before it can fully start serving traffic. The recovery attempt fails under load, which triggers more retries, creating a feedback loop. Prevention strategies: full jitter on backoff delays to desynchronize retries; circuit breakers to stop retrying once the failure rate is high enough; server-side rate limiting to shed excess retry load; and exponential backoff caps to limit maximum retry frequency.","correct":"A retry storm occurs when a large number of clients simultaneously retry requests to a recovering service, overwhelming it before it can fully start serving traffic.","distractors":["Adaptive throttling (Google's approach) tracks the ratio of accepted to total requests from the client side and self-throttles before sending requests that are statistically likely to be rejected.","CLOSED is the normal state where all requests pass through; it transitions to OPEN when the failure rate exceeds the threshold over the minimum required calls.","Fixed-delay retry causes synchronized retry storms."]},{"id":"backend/fault_tolerance_patterns#05599c40d318","section":"backend","module":"backend/fault_tolerance_patterns","moduleName":"fault tolerance patterns","qIndex":8,"difficulty":"intermediate","question":"How do you make retries safe for non-idempotent operations?","answerFull":"Use idempotency keys. The client generates a unique UUID per logical operation and sends it with every attempt. The server stores processed idempotency keys (in Redis or a database) with a TTL and returns the cached response if it receives a key it has already processed. This allows the client to safely retry on network errors without risk of double processing. Payment processors like Stripe, Braintree, and Adyen all support idempotency keys. A secondary approach is to make the operation idempotent by design: instead of \"debit $50\", send \"debit $50 for order-id-123\" with a unique order ID that is rejected on duplicate submission.","correct":"Use idempotency keys. The client generates a unique UUID per logical operation and sends it with every attempt.","distractors":["Use a semaphore bulkhead when your code is already non-blocking or reactive, since it only limits concurrency without adding threads.","The slowCallDurationThreshold must be set lower than the HTTP client's read timeout.","Adaptive throttling (Google's approach) tracks the ratio of accepted to total requests from the client side and self-throttles before sending requests that are statistically likely to be rejected."]},{"id":"backend/fault_tolerance_patterns#0010d09fe0a9","section":"backend","module":"backend/fault_tolerance_patterns","moduleName":"fault tolerance patterns","qIndex":9,"difficulty":"intermediate","question":"What happens when a Resilience4j circuit breaker is OPEN and a request comes in?","answerFull":"The circuit breaker throws CallNotPermittedException immediately, without making any network call. The exception propagates up the call stack. If a fallback is configured (via .recover(CallNotPermittedException.class, ...) in Try or via @CircuitBreaker(fallbackMethod=...) in Spring), the fallback is invoked. If no fallback is configured, the exception propagates to the caller. Metrics are recorded: the circuit breaker emits a call.not.permitted event which Micrometer exposes as resilience4j.circuitbreaker.calls with kind=not_permitted.","correct":"The circuit breaker throws CallNotPermittedException immediately, without making any network call.","distractors":["Use a semaphore bulkhead when your code is already non-blocking or reactive, since it only limits concurrency without adding threads.","Set minimumNumberOfCalls high enough that the circuit breaker does not trip on the first few calls during startup.","Use Resilience4j's CircuitBreakerRegistry to manually transition the circuit breaker to OPEN for testing fallback paths."]},{"id":"backend/fault_tolerance_patterns#7d85c6d98008","section":"backend","module":"backend/fault_tolerance_patterns","moduleName":"fault tolerance patterns","qIndex":10,"difficulty":"intermediate","question":"How should you configure circuit breakers for startup and warmup scenarios?","answerFull":"Set minimumNumberOfCalls high enough that the circuit breaker does not trip on the first few calls during startup. A common mistake is leaving minimumNumberOfCalls=1 (or a low value), causing the circuit to open immediately if the first health-check call to a warming-up service fails. For services that take 10–30 seconds to warm up, combine: a startup health gate (don't accept traffic until the service is ready), a higher minimumNumberOfCalls (e.g., 50), and waitDurationInOpenState long enough for the downstream service to fully start (e.g., 90s vs. default 60s).","correct":"Set minimumNumberOfCalls high enough that the circuit breaker does not trip on the first few calls during startup.","distractors":["The circuit breaker throws CallNotPermittedException immediately, without making any network call.","Use Resilience4j's CircuitBreakerRegistry to manually transition the circuit breaker to OPEN for testing fallback paths.","CLOSED is the normal state where all requests pass through; it transitions to OPEN when the failure rate exceeds the threshold over the minimum required calls."]},{"id":"backend/fault_tolerance_patterns#e27324540517","section":"backend","module":"backend/fault_tolerance_patterns","moduleName":"fault tolerance patterns","qIndex":11,"difficulty":"advanced","question":"What is the Bulkhead pattern's relationship to Amdahl's Law?","answerFull":"Amdahl's Law states that parallelism is limited by the sequential portion of a program. In the context of bulkheads, if one integration's thread pool is saturated, the overall system throughput is limited by that bottleneck regardless of how many threads other components have. Bulkheads enforce explicit capacity partitions so that one slow integration cannot capture more than its allotted share of resources. This is directly analogous to Amdahl's sequential bottleneck — without bulkheads, one synchronous dependency can serialize all requests through a single bottleneck.","correct":"Amdahl's Law states that parallelism is limited by the sequential portion of a program.","distractors":["Use a semaphore bulkhead when your code is already non-blocking or reactive, since it only limits concurrency without adding threads.","A fallback is an immediate alternative response that is returned when the primary call fails — it does not undo anything, it just provides a substitute result.","A retry storm occurs when a large number of clients simultaneously retry requests to a recovering service, overwhelming it before it can fully start serving traffic."]},{"id":"backend/fault_tolerance_patterns#ad6f7c5ab671","section":"backend","module":"backend/fault_tolerance_patterns","moduleName":"fault tolerance patterns","qIndex":12,"difficulty":"advanced","question":"What is the difference between a fallback and a compensating transaction?","answerFull":"A fallback is an immediate alternative response that is returned when the primary call fails — it does not undo anything, it just provides a substitute result. A compensating transaction is a business-level mechanism that undoes a previously committed operation when a subsequent operation in the same saga fails (e.g., if payment succeeded but inventory reservation failed, the compensating transaction refunds the payment). Fallbacks are a resilience pattern; compensating transactions are a data consistency pattern in distributed transactions and sagas.","correct":"A fallback is an immediate alternative response that is returned when the primary call fails — it does not undo anything, it just provides a substitute result.","distractors":["Use Resilience4j's CircuitBreakerRegistry to manually transition the circuit breaker to OPEN for testing fallback paths.","The circuit breaker throws CallNotPermittedException immediately, without making any network call.","A retry storm occurs when a large number of clients simultaneously retry requests to a recovering service, overwhelming it before it can fully start serving traffic."]},{"id":"backend/fault_tolerance_patterns#bd98785b0f22","section":"backend","module":"backend/fault_tolerance_patterns","moduleName":"fault tolerance patterns","qIndex":14,"difficulty":"advanced","question":"How do you test circuit breaker behavior in integration tests?","answerFull":"Use Resilience4j's CircuitBreakerRegistry to manually transition the circuit breaker to OPEN for testing fallback paths. Use WireMock or MockServer to simulate downstream failures (502, 503, connection refused, slow responses via response delays). Test all three states explicitly: CLOSED with success, CLOSED with failures accumulating, OPEN with fallback, HALF_OPEN with probe failures (stays OPEN), and HALF_OPEN with probe successes (transitions to CLOSED). Use Testcontainers with Toxiproxy to simulate network failures in realistic container-to-container calls.","correct":"Use Resilience4j's CircuitBreakerRegistry to manually transition the circuit breaker to OPEN for testing fallback paths.","distractors":["CLOSED is the normal state where all requests pass through; it transitions to OPEN when the failure rate exceeds the threshold over the minimum required calls.","A fallback is an immediate alternative response that is returned when the primary call fails — it does not undo anything, it just provides a substitute result.","Set minimumNumberOfCalls high enough that the circuit breaker does not trip on the first few calls during startup."]},{"id":"backend/fault_tolerance_patterns#40f1011bd660","section":"backend","module":"backend/fault_tolerance_patterns","moduleName":"fault tolerance patterns","qIndex":15,"difficulty":"advanced","question":"What is adaptive throttling and when does it outperform static circuit breakers?","answerFull":"Adaptive throttling (Google's approach) tracks the ratio of accepted to total requests from the client side and self-throttles before sending requests that are statistically likely to be rejected. The formula is: throttle_probability = max(0, (requests - K * accepts) / (requests + 1)) where K is typically 2. When the server starts rejecting requests, the client automatically reduces its request rate proportionally. This is superior to static circuit breakers in scenarios with partial degradation — if the server is rejecting 30% of requests, adaptive throttling reduces client load by 30% proportionally, whereas a static circuit breaker only trips after the failure rate exceeds a fixed threshold (e.g., 50%).","correct":"Adaptive throttling (Google's approach) tracks the ratio of accepted to total requests from the client side and self-throttles before sending requests that are statistically likely to be rejected.","distractors":["Use Resilience4j's CircuitBreakerRegistry to manually transition the circuit breaker to OPEN for testing fallback paths.","Set minimumNumberOfCalls high enough that the circuit breaker does not trip on the first few calls during startup.","COUNT_BASED tracks the last N calls regardless of when they happened; TIME_BASED tracks all calls that occurred in the last N seconds."]},{"id":"backend/graphql#79de50ff2bc9","section":"backend","module":"backend/graphql","moduleName":"graphql","qIndex":1,"difficulty":"core","question":"What is GraphQL and what problem does it solve?","answerFull":"GraphQL is an API query language and runtime that lets clients specify exactly what data they need. It solves over-fetching (REST returns fixed response shapes with more data than needed) and under-fetching (needing multiple REST calls for a single view). Clients describe the shape of the data they want; the server returns exactly that structure. This makes GraphQL particularly useful for mobile clients and complex data models.","correct":"GraphQL is an API query language and runtime that lets clients specify exactly what data they need.","distractors":["GraphQL schemas evolve additively: add new fields, types, and operations freely — existing clients are unaffected (they only request what they know about).","GraphQL always returns HTTP 200, with a data field for results and an errors array for errors.","Subscriptions are real-time operations where the server pushes updates to clients."]},{"id":"backend/graphql#57b99d908d9c","section":"backend","module":"backend/graphql","moduleName":"graphql","qIndex":2,"difficulty":"core","question":"What is the N+1 problem in GraphQL and how do you solve it?","answerFull":"When resolving a list of objects with a related field (e.g., 10 users and their departments), each field resolver executes independently — causing 1 query for users and 10 queries for departments = 11 total. DataLoader solves this by batching all pending loads within a single request execution: after resolving all 10 users, DataLoader collects all 10 department IDs and fetches them in one SELECT IN query. DataLoader also caches within the request so duplicate IDs are fetched once.","correct":"When resolving a list of objects with a related field (e.g., 10 users and their departments), each field resolver executes independently — causing 1 query for users and 10 queries for departments = 11 total.","distractors":["Persisted queries associate a hash (SHA-256 of the query string) with the full query on the server.","GraphQL schemas evolve additively: add new fields, types, and operations freely — existing clients are unaffected (they only request what they know about).","Queries can execute root-level resolvers in parallel (the spec allows this for optimization)."]},{"id":"backend/graphql#d5e9076d4791","section":"backend","module":"backend/graphql","moduleName":"graphql","qIndex":3,"difficulty":"core","question":"How does GraphQL handle errors differently from REST?","answerFull":"GraphQL always returns HTTP 200, with a data field for results and an errors array for errors. Errors include a message, locations in the query, and a path to the failing field. Partial responses are possible: some fields may resolve successfully while others fail. This differs fundamentally from REST where HTTP status codes communicate success/failure. For monitoring, you must parse the errors array, not rely on HTTP status codes.","correct":"GraphQL always returns HTTP 200, with a data field for results and an errors array for errors.","distractors":["When resolving a list of objects with a related field (e.g., 10 users and their departments), each field resolver executes independently — causing 1 query for users and 10 queries for departments = 11 total.","Introspection allows clients to query the schema itself: what types exist, what fields they have, what arguments each field takes.","GraphQL schemas evolve additively: add new fields, types, and operations freely — existing clients are unaffected (they only request what they know about)."]},{"id":"backend/graphql#6105a16ab02c","section":"backend","module":"backend/graphql","moduleName":"graphql","qIndex":4,"difficulty":"core","question":"What are GraphQL subscriptions and how are they implemented?","answerFull":"Subscriptions are real-time operations where the server pushes updates to clients. They are typically implemented over WebSocket (using graphql-ws or subscriptions-transport-ws protocols). The client subscribes with a subscription operation; the server publishes events when underlying data changes (via a pub/sub system like Redis). In production, you need a stateful connection manager — WebSocket connections cannot be horizontally scaled without shared state (Redis pub/sub or similar).","correct":"Subscriptions are real-time operations where the server pushes updates to clients.","distractors":["Persisted queries associate a hash (SHA-256 of the query string) with the full query on the server.","GraphQL is an API query language and runtime that lets clients specify exactly what data they need.","GraphQL always returns HTTP 200, with a data field for results and an errors array for errors."]},{"id":"backend/graphql#ac0f1f9627a1","section":"backend","module":"backend/graphql","moduleName":"graphql","qIndex":5,"difficulty":"core","question":"What is the difference between schema stitching and Apollo Federation?","answerFull":"Schema stitching merges multiple GraphQL schemas at the gateway level using shared types and remote execution. It is older, requires more manual configuration, and can create tight coupling. Apollo Federation is a specification for a distributed graph where each service owns part of the schema and can extend types defined in other services using @key and @external directives. The Apollo Router (or Gateway) composes them automatically. Federation is the modern approach for microservices.","correct":"Schema stitching merges multiple GraphQL schemas at the gateway level using shared types and remote execution.","distractors":["Queries can execute root-level resolvers in parallel (the spec allows this for optimization).","GraphQL is an API query language and runtime that lets clients specify exactly what data they need.","GraphQL schemas evolve additively: add new fields, types, and operations freely — existing clients are unaffected (they only request what they know about)."]},{"id":"backend/graphql#10b8ae5512a0","section":"backend","module":"backend/graphql","moduleName":"graphql","qIndex":7,"difficulty":"intermediate","question":"What are persisted queries and why are they used?","answerFull":"Persisted queries associate a hash (SHA-256 of the query string) with the full query on the server. Clients send only the hash at runtime. Benefits: (1) reduced request size; (2) GET requests with hash + variables are cacheable by CDN (unlike POST with full query body); (3) security — reject any query not in the registry, preventing query injection; (4) performance — queries can be pre-validated and pre-analyzed. Used in production by most large GraphQL deployments.","correct":"Persisted queries associate a hash (SHA-256 of the query string) with the full query on the server.","distractors":["Queries can execute root-level resolvers in parallel (the spec allows this for optimization).","GraphQL is an API query language and runtime that lets clients specify exactly what data they need.","When resolving a list of objects with a related field (e.g., 10 users and their departments), each field resolver executes independently — causing 1 query for users and 10 queries for departments = 11 total."]},{"id":"backend/graphql#0242d5f69395","section":"backend","module":"backend/graphql","moduleName":"graphql","qIndex":8,"difficulty":"intermediate","question":"How do you implement pagination in GraphQL?","answerFull":"Relay-style connections are the standard: a Connection type with edges (cursor + node) and pageInfo (hasNextPage, endCursor). Query: users(first: 20, after: \"cursor\"). This enables cursor-based pagination, consistent even with concurrent writes. Simple pagination: users(limit: 20, offset: 0) is simpler but has offset performance problems at scale. Use Relay connections for user-facing paginated lists; offset for admin interfaces.","correct":"Relay-style connections are the standard: a Connection type with edges (cursor + node) and pageInfo (hasNextPage, endCursor).","distractors":["GraphQL always returns HTTP 200, with a data field for results and an errors array for errors.","Schema stitching merges multiple GraphQL schemas at the gateway level using shared types and remote execution.","GraphQL schemas evolve additively: add new fields, types, and operations freely — existing clients are unaffected (they only request what they know about)."]},{"id":"backend/graphql#be14c40666c3","section":"backend","module":"backend/graphql","moduleName":"graphql","qIndex":9,"difficulty":"intermediate","question":"What is GraphQL introspection and should you disable it?","answerFull":"Introspection allows clients to query the schema itself: what types exist, what fields they have, what arguments each field takes. It powers GraphiQL, Apollo Sandbox, and code generators. In production, disable it for public APIs to prevent schema reconnaissance: GraphQL.newGraphQL().introspection(false). For internal APIs with authenticated access, it's acceptable to leave enabled. Always ensure query depth limits are set before enabling introspection to prevent introspection-based DoS.","correct":"Introspection allows clients to query the schema itself: what types exist, what fields they have, what arguments each field takes.","distractors":["When resolving a list of objects with a related field (e.g., 10 users and their departments), each field resolver executes independently — causing 1 query for users and 10 queries for departments = 11 total.","Schema stitching merges multiple GraphQL schemas at the gateway level using shared types and remote execution.","GraphQL is an API query language and runtime that lets clients specify exactly what data they need."]},{"id":"backend/graphql#f9817b037898","section":"backend","module":"backend/graphql","moduleName":"graphql","qIndex":10,"difficulty":"intermediate","question":"How does GraphQL handle schema evolution compared to REST?","answerFull":"GraphQL schemas evolve additively: add new fields, types, and operations freely — existing clients are unaffected (they only request what they know about). Deprecate old fields with @deprecated. Never remove a field without checking usage metrics first. Breaking changes (removing fields, changing types, renaming arguments) require versioning. Unlike REST, GraphQL has no built-in versioning mechanism — additive evolution is the primary strategy. Apollo Studio tracks field usage to safely identify when deprecated fields can be removed.","correct":"GraphQL schemas evolve additively: add new fields, types, and operations freely — existing clients are unaffected (they only request what they know about).","distractors":["Subscriptions are real-time operations where the server pushes updates to clients.","Schema stitching merges multiple GraphQL schemas at the gateway level using shared types and remote execution.","GraphQL is an API query language and runtime that lets clients specify exactly what data they need."]},{"id":"backend/graphql#d7e87a709181","section":"backend","module":"backend/graphql","moduleName":"graphql","qIndex":11,"difficulty":"advanced","question":"What is the difference between queries and mutations in GraphQL execution?","answerFull":"Queries can execute root-level resolvers in parallel (the spec allows this for optimization). Mutations execute sequentially — the spec requires that each root-level mutation completes before the next starts. This ensures mutations like createOrder followed by sendConfirmation execute in order. Nested resolvers within a single mutation execute normally (DataLoader still batches). For multiple independent mutations, clients should send separate requests.","correct":"Queries can execute root-level resolvers in parallel (the spec allows this for optimization).","distractors":["When resolving a list of objects with a related field (e.g., 10 users and their departments), each field resolver executes independently — causing 1 query for users and 10 queries for departments = 11 total.","Introspection allows clients to query the schema itself: what types exist, what fields they have, what arguments each field takes.","GraphQL is an API query language and runtime that lets clients specify exactly what data they need."]},{"id":"backend/graphql#99aa9db66645","section":"backend","module":"backend/graphql","moduleName":"graphql","qIndex":12,"difficulty":"advanced","question":"How do you design a GraphQL schema for a social network feed?","answerFull":"Define a FeedItem interface with implementing types (Post, Story, Share, AdUnit). The feed query: feed(userId: ID!, first: Int!, after: String): FeedConnection! uses cursor-based pagination. FeedItems include only the fields needed for the feed list view; full post content is in a separate Post type fetched on demand. Use DataLoader for author resolution, reaction counts (batched to a counting service), and media metadata. Subscriptions for real-time new items.","correct":"Define a FeedItem interface with implementing types (Post, Story, Share, AdUnit).","distractors":["Schema stitching merges multiple GraphQL schemas at the gateway level using shared types and remote execution.","GraphQL schemas evolve additively: add new fields, types, and operations freely — existing clients are unaffected (they only request what they know about).","Introspection allows clients to query the schema itself: what types exist, what fields they have, what arguments each field takes."]},{"id":"backend/grpc_and_protobuf#dd37e540c967","section":"backend","module":"backend/grpc_and_protobuf","moduleName":"grpc and protobuf","qIndex":1,"difficulty":"core","question":"What is gRPC and how does it differ from REST?","answerFull":"gRPC is an RPC framework using Protocol Buffers for serialization and HTTP/2 for transport. It differs from REST in: using a binary format (compact, fast) vs JSON (human-readable), having strict schema enforcement via .proto files vs optional OpenAPI, built-in streaming support (4 modes) vs REST's single request-response, and generated type-safe stubs vs manual HTTP client code. gRPC is preferred for internal service-to-service communication; REST for public/browser-facing APIs.","correct":"gRPC is an RPC framework using Protocol Buffers for serialization and HTTP/2 for transport.","distractors":["gRPC reflection allows clients to query the server for service definitions at runtime, without having the .proto files.","gRPC supports a service config JSON with retry policy: maxAttempts (max total attempts), initialBackoff, maxBackoff, backoffMultiplier, and retryableStatusCodes (e.g., UNAVAILABLE, DEADLINE_EXCEEDED).","gRPC-Web is a variant of the gRPC protocol that browsers can use."]},{"id":"backend/grpc_and_protobuf#d8903ab20b43","section":"backend","module":"backend/grpc_and_protobuf","moduleName":"grpc and protobuf","qIndex":2,"difficulty":"core","question":"Explain Protocol Buffers wire format and field numbering.","answerFull":"Protobuf serializes each field as a tag-value pair. The tag encodes both the field number (1–536,870,911) and the wire type (0=varint, 1=64-bit, 2=length-delimited, 5=32-bit). Field names are not in the wire format — only numbers. This means field numbers must never be reused after a field is removed (doing so causes old clients to misinterpret new fields). Varint encoding uses variable-length encoding: values 0–127 fit in 1 byte.","correct":"Protobuf serializes each field as a tag-value pair.","distractors":["Safe changes: add new optional fields with new numbers, add new enum values, add new RPCs, rename a field (changes the name only, not the number — no wire impact).","gRPC-Web is a variant of the gRPC protocol that browsers can use.","gRPC is an RPC framework using Protocol Buffers for serialization and HTTP/2 for transport."]},{"id":"backend/grpc_and_protobuf#59d8dccdec11","section":"backend","module":"backend/grpc_and_protobuf","moduleName":"grpc and protobuf","qIndex":3,"difficulty":"core","question":"What are the four gRPC RPC modes?","answerFull":"Unary: one request, one response (like REST). Server-streaming: one request, stream of responses (useful for real-time data, large result sets). Client-streaming: stream of requests, one response (useful for bulk uploads, aggregation). Bidirectional streaming: both sides stream independently (useful for real-time chat, collaborative editing, game state sync). All modes use HTTP/2 streams, just with different DATA frame patterns.","correct":"Unary: one request, one response (like REST).","distractors":["An interceptor is middleware that runs before/after RPC handling.","gRPC supports both client-side and proxy load balancing.","gRPC supports a service config JSON with retry policy: maxAttempts (max total attempts), initialBackoff, maxBackoff, backoffMultiplier, and retryableStatusCodes (e.g., UNAVAILABLE, DEADLINE_EXCEEDED)."]},{"id":"backend/grpc_and_protobuf#0f55fa6d0c35","section":"backend","module":"backend/grpc_and_protobuf","moduleName":"grpc and protobuf","qIndex":4,"difficulty":"core","question":"How does deadline propagation work in gRPC?","answerFull":"The client sets a deadline (absolute timestamp). gRPC encodes the remaining deadline as grpc-timeout in the request metadata. The downstream service receives the deadline and propagates it to its own outbound calls. If any service in the chain exceeds the deadline, it returns DEADLINE_EXCEEDED. The parent service, on receiving DEADLINE_EXCEEDED from its child, also cancels its work and propagates the error. This prevents resource waste in partial-failure scenarios.","correct":"The client sets a deadline (absolute timestamp).","distractors":["gRPC supports both client-side and proxy load balancing.","gRPC uses Status codes (similar to HTTP but gRPC-specific).","gRPC reflection allows clients to query the server for service definitions at runtime, without having the .proto files."]},{"id":"backend/grpc_and_protobuf#9442a1f4fb2b","section":"backend","module":"backend/grpc_and_protobuf","moduleName":"grpc and protobuf","qIndex":5,"difficulty":"core","question":"What is a gRPC interceptor?","answerFull":"An interceptor is middleware that runs before/after RPC handling. Server interceptors wrap service method invocations; client interceptors wrap outbound calls. Common uses: authentication (extract and validate JWT from metadata), distributed tracing (inject/extract trace context), logging (log request/response), metrics (record call latency and error rates), retry with backoff. Interceptors chain and each calls next.interceptCall() to pass control.","correct":"An interceptor is middleware that runs before/after RPC handling.","distractors":["gRPC uses Status codes (similar to HTTP but gRPC-specific).","gRPC reflection allows clients to query the server for service definitions at runtime, without having the .proto files.","Google defined a standard health checking service (grpc.health.v1.Health) with a single Check RPC that returns SERVING, NOT_SERVING, or SERVICE_UNKNOWN."]},{"id":"backend/grpc_and_protobuf#3ef6a4af4347","section":"backend","module":"backend/grpc_and_protobuf","moduleName":"grpc and protobuf","qIndex":6,"difficulty":"intermediate","question":"How do you handle errors in gRPC?","answerFull":"gRPC uses Status codes (similar to HTTP but gRPC-specific). Return a StatusRuntimeException on the server with the appropriate code (NOT_FOUND, INVALID_ARGUMENT, UNAUTHENTICATED, etc.). The gRPC framework sends it as trailing metadata grpc-status and grpc-message. For structured error details, use the google.rpc.Status type with google.rpc.ErrorInfo, google.rpc.BadRequest etc. from the googleapis/googleapis error.proto definitions. On the client, catch StatusRuntimeException and inspect the Status.","correct":"gRPC uses Status codes (similar to HTTP but gRPC-specific).","distractors":["gRPC is an RPC framework using Protocol Buffers for serialization and HTTP/2 for transport.","gRPC reflection allows clients to query the server for service definitions at runtime, without having the .proto files.","Google defined a standard health checking service (grpc.health.v1.Health) with a single Check RPC that returns SERVING, NOT_SERVING, or SERVICE_UNKNOWN."]},{"id":"backend/grpc_and_protobuf#698bf489d050","section":"backend","module":"backend/grpc_and_protobuf","moduleName":"grpc and protobuf","qIndex":7,"difficulty":"intermediate","question":"What is the Health Checking Protocol in gRPC?","answerFull":"Google defined a standard health checking service (grpc.health.v1.Health) with a single Check RPC that returns SERVING, NOT_SERVING, or SERVICE_UNKNOWN. Kubernetes liveness/readiness probes use grpc-health-probe, which calls this service. Load balancers use it for backend health checks. Implement it by registering HealthStatusManager on the server and calling setStatus() when the service starts/stops.","correct":"Google defined a standard health checking service (grpc.health.v1.Health) with a single Check RPC that returns SERVING, NOT_SERVING, or SERVICE_UNKNOWN.","distractors":["gRPC supports a service config JSON with retry policy: maxAttempts (max total attempts), initialBackoff, maxBackoff, backoffMultiplier, and retryableStatusCodes (e.g., UNAVAILABLE, DEADLINE_EXCEEDED).","gRPC reflection allows clients to query the server for service definitions at runtime, without having the .proto files.","An interceptor is middleware that runs before/after RPC handling."]},{"id":"backend/grpc_and_protobuf#97689a9bea94","section":"backend","module":"backend/grpc_and_protobuf","moduleName":"grpc and protobuf","qIndex":8,"difficulty":"intermediate","question":"How do you evolve a protobuf schema without breaking clients?","answerFull":"Safe changes: add new optional fields with new numbers, add new enum values, add new RPCs, rename a field (changes the name only, not the number — no wire impact). Breaking changes: remove a field (old clients send it; new server ignores — OK for requests; old clients may expect it in response), change a field's type, reuse a field number. Best practice: use buf breaking --against in CI to automatically detect breaking changes.","correct":"Safe changes: add new optional fields with new numbers, add new enum values, add new RPCs, rename a field (changes the name only, not the number — no wire impact).","distractors":["Protobuf serializes each field as a tag-value pair.","gRPC reflection allows clients to query the server for service definitions at runtime, without having the .proto files.","A real-time bidirectional order matching engine: traders send market orders and receive execution notifications in real-time."]},{"id":"backend/grpc_and_protobuf#eaa7d04b0351","section":"backend","module":"backend/grpc_and_protobuf","moduleName":"grpc and protobuf","qIndex":9,"difficulty":"intermediate","question":"What is the difference between proto2 and proto3?","answerFull":"Proto3 is the current version. Key differences: proto3 has no required fields (all are optional), default values are the zero value (cannot distinguish set vs unset unless using google.protobuf.FieldMask or wrapper types), proto3 supports JSON mapping by default. Proto2 had required fields (dangerous — adding a required field to a message breaks all old senders), had explicit optional with default values, and had extension ranges. Use proto3 for all new projects.","correct":"Proto3 is the current version.","distractors":["A real-time bidirectional order matching engine: traders send market orders and receive execution notifications in real-time.","An interceptor is middleware that runs before/after RPC handling.","Google defined a standard health checking service (grpc.health.v1.Health) with a single Check RPC that returns SERVING, NOT_SERVING, or SERVICE_UNKNOWN."]},{"id":"backend/grpc_and_protobuf#c6c9480ec1c9","section":"backend","module":"backend/grpc_and_protobuf","moduleName":"grpc and protobuf","qIndex":10,"difficulty":"intermediate","question":"What is gRPC-Web and when do you need it?","answerFull":"gRPC-Web is a variant of the gRPC protocol that browsers can use. Browsers cannot use HTTP/2 trailers (gRPC uses trailers for the grpc-status code), so gRPC-Web encodes trailing metadata in a special data frame. An Envoy sidecar or nginx gRPC-Web proxy translates between gRPC and gRPC-Web. gRPC-Web only supports unary and server-streaming; bidirectional streaming is not supported. Use gRPC-Web when browser clients need to call gRPC services directly.","correct":"gRPC-Web is a variant of the gRPC protocol that browsers can use.","distractors":["gRPC supports a service config JSON with retry policy: maxAttempts (max total attempts), initialBackoff, maxBackoff, backoffMultiplier, and retryableStatusCodes (e.g., UNAVAILABLE, DEADLINE_EXCEEDED).","Google defined a standard health checking service (grpc.health.v1.Health) with a single Check RPC that returns SERVING, NOT_SERVING, or SERVICE_UNKNOWN.","gRPC supports both client-side and proxy load balancing."]},{"id":"backend/grpc_and_protobuf#17940cb19f8d","section":"backend","module":"backend/grpc_and_protobuf","moduleName":"grpc and protobuf","qIndex":11,"difficulty":"advanced","question":"How does gRPC handle load balancing?","answerFull":"gRPC supports both client-side and proxy load balancing. Client-side: the gRPC channel resolves DNS, gets all backend IPs, and applies a load balancing policy (round-robin, pick-first). This is common with Kubernetes headless services. Proxy load balancing: a proxy (Envoy, Nginx, AWS NLB) receives all connections and distributes them. With HTTP/2, a single gRPC connection multiplexes many RPCs — L4 load balancers see one connection per client; L7 load balancers can distribute individual RPCs.","correct":"gRPC supports both client-side and proxy load balancing.","distractors":["The client sets a deadline (absolute timestamp).","gRPC reflection allows clients to query the server for service definitions at runtime, without having the .proto files.","gRPC is an RPC framework using Protocol Buffers for serialization and HTTP/2 for transport."]},{"id":"backend/grpc_and_protobuf#7e062b627400","section":"backend","module":"backend/grpc_and_protobuf","moduleName":"grpc and protobuf","qIndex":12,"difficulty":"advanced","question":"What is the maximum message size in gRPC and how do you change it?","answerFull":"The default maximum inbound message size is 4 MB (4,194,304 bytes). The maximum outbound message size has no default limit. Change via: server-side: ServerBuilder.maxInboundMessageSize(50 * 1024 * 1024) (50 MB). Client-side: ManagedChannelBuilder.maxInboundMessageSize(50 * 1024 * 1024). Better approach for large payloads: stream messages to avoid sending one large message; or store data in object storage and pass a reference URI in the message.","correct":"The default maximum inbound message size is 4 MB (4,194,304 bytes).","distractors":["gRPC uses Status codes (similar to HTTP but gRPC-specific).","gRPC reflection allows clients to query the server for service definitions at runtime, without having the .proto files.","Google defined a standard health checking service (grpc.health.v1.Health) with a single Check RPC that returns SERVING, NOT_SERVING, or SERVICE_UNKNOWN."]},{"id":"backend/grpc_and_protobuf#824a7be7b15d","section":"backend","module":"backend/grpc_and_protobuf","moduleName":"grpc and protobuf","qIndex":13,"difficulty":"advanced","question":"Describe a scenario where gRPC bidirectional streaming provides value REST cannot easily match.","answerFull":"A real-time bidirectional order matching engine: traders send market orders and receive execution notifications in real-time. REST would require polling (latency), WebSocket (adds complexity and library overhead), or SSE (unidirectional only). gRPC bidirectional streaming provides full-duplex communication with protobuf efficiency and built-in flow control. Each side streams independently — the trader sends new orders, the server sends execution confirmations and market data updates — over one persistent connection with back-pressure from HTTP/2 flow control.","correct":"A real-time bidirectional order matching engine: traders send market orders and receive execution notifications in real-time.","distractors":["Google defined a standard health checking service (grpc.health.v1.Health) with a single Check RPC that returns SERVING, NOT_SERVING, or SERVICE_UNKNOWN.","gRPC reflection allows clients to query the server for service definitions at runtime, without having the .proto files.","gRPC supports both client-side and proxy load balancing."]},{"id":"backend/grpc_and_protobuf#f878f6786bda","section":"backend","module":"backend/grpc_and_protobuf","moduleName":"grpc and protobuf","qIndex":14,"difficulty":"advanced","question":"What is the grpc reflection protocol?","answerFull":"gRPC reflection allows clients to query the server for service definitions at runtime, without having the .proto files. Tools like grpcurl and evans use reflection to discover available services and methods. Enable it on the server with ProtoReflectionService. Disable in production if you do not want to expose your API schema to clients (reflection is informational only — it does not grant access, but may expose schema information to attackers).","correct":"gRPC reflection allows clients to query the server for service definitions at runtime, without having the .proto files.","distractors":["Google defined a standard health checking service (grpc.health.v1.Health) with a single Check RPC that returns SERVING, NOT_SERVING, or SERVICE_UNKNOWN.","gRPC supports both client-side and proxy load balancing.","gRPC uses Status codes (similar to HTTP but gRPC-specific)."]},{"id":"backend/grpc_and_protobuf#6ce97eebfccd","section":"backend","module":"backend/grpc_and_protobuf","moduleName":"grpc and protobuf","qIndex":15,"difficulty":"advanced","question":"How do you implement retries in gRPC?","answerFull":"gRPC supports a service config JSON with retry policy: maxAttempts (max total attempts), initialBackoff, maxBackoff, backoffMultiplier, and retryableStatusCodes (e.g., UNAVAILABLE, DEADLINE_EXCEEDED). This is specified in the service config loaded by the name resolver. For Java, configure via ManagedChannelBuilder with a default service config. Retries are transparent to the application code — the channel handles them. Only retry idempotent RPCs or explicitly idempotent operations.","correct":"gRPC supports a service config JSON with retry policy: maxAttempts (max total attempts), initialBackoff, maxBackoff, backoffMultiplier, and retryableStatusCodes (e.g., UNAVAILABLE, DEADLINE_EXCEEDED).","distractors":["Google defined a standard health checking service (grpc.health.v1.Health) with a single Check RPC that returns SERVING, NOT_SERVING, or SERVICE_UNKNOWN.","gRPC supports both client-side and proxy load balancing.","gRPC uses Status codes (similar to HTTP but gRPC-specific)."]},{"id":"backend/http_protocols#eddbb4d96470","section":"backend","module":"backend/http_protocols","moduleName":"http protocols","qIndex":2,"difficulty":"core","question":"Explain the head-of-line blocking problem in HTTP/2.","answerFull":"HTTP/2 multiplexes all streams over one TCP connection. If a TCP segment is lost, TCP's in-order delivery guarantee means no data from any stream can be delivered to the application until the lost segment is retransmitted and received. All HTTP/2 streams stall, even those whose data arrived successfully. This is TCP-level HoL blocking. HTTP/3 solves this by running over QUIC, where each stream is independently sequenced.","correct":"HTTP/2 multiplexes all streams over one TCP connection.","distractors":["HTTP/2 server push allowed a server to proactively send resources (CSS, JS) to the client before it requests them, using PUSH_PROMISE frames.","Long polling: the client sends an HTTP request; the server holds it open until data is available (or timeout), then responds; the client immediately sends another request.","SETTINGS frames are exchanged at connection setup and can be sent anytime to update settings."]},{"id":"backend/http_protocols#982c53c490da","section":"backend","module":"backend/http_protocols","moduleName":"http protocols","qIndex":3,"difficulty":"core","question":"How does TLS 1.3 reduce latency compared to TLS 1.2?","answerFull":"TLS 1.2 requires 2 RTTs for a full handshake (1 RTT for TCP, 2 for TLS = 3 RTTs before data). TLS 1.3 reduced this to 1 RTT for TLS (2 RTTs total). TLS 1.3 also supports 0-RTT session resumption (sending application data in the first packet). TLS 1.3 mandatory forward secrecy eliminated export-grade ciphers and simplified cipher suite negotiation, improving security alongside performance.","correct":"TLS 1.2 requires 2 RTTs for a full handshake (1 RTT for TCP, 2 for TLS = 3 RTTs before data).","distractors":["ALPN (Application-Layer Protocol Negotiation) is a TLS extension allowing the client to advertise supported application protocols (h2, http/1.1, h3) in the ClientHello.","HTTP/2 multiplexes all streams over one TCP connection.","SNI (Server Name Indication) is a TLS extension where the client includes the target hostname in the ClientHello (before TLS is established)."]},{"id":"backend/http_protocols#76f4c521599f","section":"backend","module":"backend/http_protocols","moduleName":"http protocols","qIndex":4,"difficulty":"core","question":"What is ALPN and why is it needed?","answerFull":"ALPN (Application-Layer Protocol Negotiation) is a TLS extension allowing the client to advertise supported application protocols (h2, http/1.1, h3) in the ClientHello. The server picks the best supported protocol and includes it in the ServerHello. Without ALPN, a client would need a separate round trip to negotiate the application protocol, or use a different port per protocol. ALPN enables HTTP/2 to be selected during the TLS handshake on port 443.","correct":"ALPN (Application-Layer Protocol Negotiation) is a TLS extension allowing the client to advertise supported application protocols (h2, http/1.1, h3) in the ClientHello.","distractors":["HTTP/2 server push allowed a server to proactively send resources (CSS, JS) to the client before it requests them, using PUSH_PROMISE frames.","HTTP/2 multiplexes all streams over one TCP connection.","Long polling: the client sends an HTTP request; the server holds it open until data is available (or timeout), then responds; the client immediately sends another request."]},{"id":"backend/http_protocols#8bd0968dbb70","section":"backend","module":"backend/http_protocols","moduleName":"http protocols","qIndex":5,"difficulty":"core","question":"What does the HTTP Cache-Control: max-age directive do, and how does it differ from Expires?","answerFull":"Cache-Control: max-age=N specifies that the response is fresh for N seconds from when it was served. Expires provides an absolute date-time. max-age takes precedence over Expires when both are present. Prefer Cache-Control because it is relative to serving time (robust to clock skew), and because Expires is a legacy header from HTTP/1.0.","correct":"Cache-Control: max-age=N specifies that the response is fresh for N seconds from when it was served.","distractors":["HTTP/2 has flow control at two levels: per-connection and per-stream.","HTTP/2 multiplexes all streams over one TCP connection.","HTTP/2 server push allowed a server to proactively send resources (CSS, JS) to the client before it requests them, using PUSH_PROMISE frames."]},{"id":"backend/http_protocols#a947ef7c98d4","section":"backend","module":"backend/http_protocols","moduleName":"http protocols","qIndex":6,"difficulty":"intermediate","question":"What is an ETag and how does it enable conditional requests?","answerFull":"An ETag is a server-generated identifier representing the version of a resource (hash, version number, or timestamp). The server includes it in the response: ETag: \"abc123\". On subsequent requests, the client sends If-None-Match: \"abc123\". If the resource hasn't changed, the server responds 304 Not Modified with no body — saving bandwidth. ETags must change whenever the resource changes.","correct":"An ETag is a server-generated identifier representing the version of a resource (hash, version number, or timestamp).","distractors":["Long polling: the client sends an HTTP request; the server holds it open until data is available (or timeout), then responds; the client immediately sends another request.","SNI (Server Name Indication) is a TLS extension where the client includes the target hostname in the ClientHello (before TLS is established).","HTTP/2 server push allowed a server to proactively send resources (CSS, JS) to the client before it requests them, using PUSH_PROMISE frames."]},{"id":"backend/http_protocols#62274557a16e","section":"backend","module":"backend/http_protocols","moduleName":"http protocols","qIndex":7,"difficulty":"intermediate","question":"What is SNI and why is it necessary for modern HTTPS?","answerFull":"SNI (Server Name Indication) is a TLS extension where the client includes the target hostname in the ClientHello (before TLS is established). This allows a server to present different certificates for different hostnames on the same IP address. Without SNI, a server could only host one certificate per IP — impractical when IPv4 addresses are scarce. CDNs, hosting providers, and cloud load balancers all depend on SNI for multi-tenant certificate management.","correct":"SNI (Server Name Indication) is a TLS extension where the client includes the target hostname in the ClientHello (before TLS is established).","distractors":["HSTS (Strict-Transport-Security) tells browsers to only connect via HTTPS for the duration specified by max-age.","HTTP/2 server push allowed a server to proactively send resources (CSS, JS) to the client before it requests them, using PUSH_PROMISE frames.","Long polling: the client sends an HTTP request; the server holds it open until data is available (or timeout), then responds; the client immediately sends another request."]},{"id":"backend/http_protocols#11e5e3119e26","section":"backend","module":"backend/http_protocols","moduleName":"http protocols","qIndex":8,"difficulty":"intermediate","question":"What does the HSTS header do and what is the preload list?","answerFull":"HSTS (Strict-Transport-Security) tells browsers to only connect via HTTPS for the duration specified by max-age. If a user types http://example.com, the browser upgrades to HTTPS locally before making any network request — preventing SSL stripping. The preload list is a browser-shipped list of domains that must always use HTTPS, protecting even first-time visitors before any HSTS header is received.","correct":"HSTS (Strict-Transport-Security) tells browsers to only connect via HTTPS for the duration specified by max-age.","distractors":["301 (Moved Permanently) is cacheable and instructs browsers to update bookmarks.","The Vary header tells caches to store separate responses for different values of the listed headers.","Cache-Control: max-age=N specifies that the response is fresh for N seconds from when it was served."]},{"id":"backend/http_protocols#49609b143311","section":"backend","module":"backend/http_protocols","moduleName":"http protocols","qIndex":9,"difficulty":"intermediate","question":"What is the difference between HTTP 301 and 302 redirects, and how do they affect caching?","answerFull":"301 (Moved Permanently) is cacheable and instructs browsers to update bookmarks. Subsequent requests go directly to the new URL. 302 (Found, temporary redirect) is not permanently cacheable — the browser asks the original URL each time (though some browsers cache 302 with a short duration). Use 301 for permanent moves (old API versions, www to non-www). Use 302 for temporary moves or feature flags. Incorrect use of 301 makes rollbacks painful (cached redirect).","correct":"301 (Moved Permanently) is cacheable and instructs browsers to update bookmarks.","distractors":["HSTS (Strict-Transport-Security) tells browsers to only connect via HTTPS for the duration specified by max-age.","Long polling: the client sends an HTTP request; the server holds it open until data is available (or timeout), then responds; the client immediately sends another request.","HTTP/2 server push allowed a server to proactively send resources (CSS, JS) to the client before it requests them, using PUSH_PROMISE frames."]},{"id":"backend/http_protocols#5a7da5df9597","section":"backend","module":"backend/http_protocols","moduleName":"http protocols","qIndex":10,"difficulty":"intermediate","question":"How does HTTP/2 server push work, and why was it deprecated in Chrome?","answerFull":"HTTP/2 server push allowed a server to proactively send resources (CSS, JS) to the client before it requests them, using PUSH_PROMISE frames. In theory, this eliminated round trips for critical resources. In practice, servers couldn't know what was already in the browser cache — they would push resources the browser already had, wasting bandwidth. Chrome removed server push in Chrome 106. The preload link header with <link rel=\"preload\"> is more effective.","correct":"HTTP/2 server push allowed a server to proactively send resources (CSS, JS) to the client before it requests them, using PUSH_PROMISE frames.","distractors":["SNI (Server Name Indication) is a TLS extension where the client includes the target hostname in the ClientHello (before TLS is established).","An ETag is a server-generated identifier representing the version of a resource (hash, version number, or timestamp).","HTTP/2 has flow control at two levels: per-connection and per-stream."]},{"id":"backend/http_protocols#241aa42c7889","section":"backend","module":"backend/http_protocols","moduleName":"http protocols","qIndex":11,"difficulty":"advanced","question":"What are the HTTP methods and which are idempotent?","answerFull":"GET, HEAD, OPTIONS, PUT, DELETE are idempotent (same request N times has same effect as once). POST and PATCH are not idempotent (submitting the same POST twice creates two resources). Safe methods (GET, HEAD, OPTIONS) do not modify server state. Idempotency is critical for retry logic in distributed systems — safely retrying a PUT or DELETE after a network failure cannot create inconsistency.","correct":"GET, HEAD, OPTIONS, PUT, DELETE are idempotent (same request N times has same effect as once).","distractors":["Long polling: the client sends an HTTP request; the server holds it open until data is available (or timeout), then responds; the client immediately sends another request.","HTTP/2 server push allowed a server to proactively send resources (CSS, JS) to the client before it requests them, using PUSH_PROMISE frames.","HTTP/2 has flow control at two levels: per-connection and per-stream."]},{"id":"backend/http_protocols#10c821256627","section":"backend","module":"backend/http_protocols","moduleName":"http protocols","qIndex":12,"difficulty":"advanced","question":"What is the Vary header and when does it cause problems?","answerFull":"The Vary header tells caches to store separate responses for different values of the listed headers. Vary: Accept-Encoding causes caches to store different responses for gzip, br, and uncompressed clients. Vary: User-Agent causes caches to store thousands of responses per URL (one per User-Agent), destroying cache hit rates. Vary: * means nothing can be cached. Only include headers in Vary that genuinely produce different responses.","correct":"The Vary header tells caches to store separate responses for different values of the listed headers.","distractors":["HSTS (Strict-Transport-Security) tells browsers to only connect via HTTPS for the duration specified by max-age.","301 (Moved Permanently) is cacheable and instructs browsers to update bookmarks.","ALPN (Application-Layer Protocol Negotiation) is a TLS extension allowing the client to advertise supported application protocols (h2, http/1.1, h3) in the ClientHello."]},{"id":"backend/http_protocols#80ddfac65a31","section":"backend","module":"backend/http_protocols","moduleName":"http protocols","qIndex":13,"difficulty":"advanced","question":"What is the difference between HTTP long polling and WebSocket?","answerFull":"Long polling: the client sends an HTTP request; the server holds it open until data is available (or timeout), then responds; the client immediately sends another request. It uses standard HTTP semantics but creates connection churn and overhead. WebSocket: the client upgrades the connection (101 Switching Protocols), and then both sides can send frames at any time over the persistent connection. WebSocket has lower overhead per message, better performance, but requires explicit infrastructure support (load balancers, proxies).","correct":"Long polling: the client sends an HTTP request; the server holds it open until data is available (or timeout), then responds; the client immediately sends another request.","distractors":["SNI (Server Name Indication) is a TLS extension where the client includes the target hostname in the ClientHello (before TLS is established).","HTTP/2 multiplexes all streams over one TCP connection.","HTTP/2 has flow control at two levels: per-connection and per-stream."]},{"id":"backend/http_protocols#e11d7bf4ba06","section":"backend","module":"backend/http_protocols","moduleName":"http protocols","qIndex":14,"difficulty":"advanced","question":"How does HTTP/2 flow control work?","answerFull":"HTTP/2 has flow control at two levels: per-connection and per-stream. Each stream has an initial window size (default 65,535 bytes). When the receiver processes DATA frames, it sends WINDOW_UPDATE frames to increase the window. The sender cannot send more data than the window allows. Connection-level flow control aggregates all streams. This prevents a fast sender from overwhelming a slow receiver's buffers, analogous to TCP's receive window but at the application layer.","correct":"HTTP/2 has flow control at two levels: per-connection and per-stream.","distractors":["Cache-Control: max-age=N specifies that the response is fresh for N seconds from when it was served.","SETTINGS frames are exchanged at connection setup and can be sent anytime to update settings.","ALPN (Application-Layer Protocol Negotiation) is a TLS extension allowing the client to advertise supported application protocols (h2, http/1.1, h3) in the ClientHello."]},{"id":"backend/http_protocols#5336b2ead8ef","section":"backend","module":"backend/http_protocols","moduleName":"http protocols","qIndex":15,"difficulty":"advanced","question":"What is the HTTP/2 SETTINGS frame and what can it configure?","answerFull":"SETTINGS frames are exchanged at connection setup and can be sent anytime to update settings. Key parameters: HEADER_TABLE_SIZE (HPACK dynamic table size, default 4096), ENABLE_PUSH (server push, 0 to disable), MAX_CONCURRENT_STREAMS (default unlimited; typically 100-1000 in practice), INITIAL_WINDOW_SIZE (flow control window, default 65535), MAX_FRAME_SIZE (max DATA frame, default 16384 bytes), MAX_HEADER_LIST_SIZE (max header set size). Misconfiguring these causes 429/431 errors or poor performance.","correct":"SETTINGS frames are exchanged at connection setup and can be sent anytime to update settings.","distractors":["HTTP/2 server push allowed a server to proactively send resources (CSS, JS) to the client before it requests them, using PUSH_PROMISE frames.","ALPN (Application-Layer Protocol Negotiation) is a TLS extension allowing the client to advertise supported application protocols (h2, http/1.1, h3) in the ClientHello.","301 (Moved Permanently) is cacheable and instructs browsers to update bookmarks."]},{"id":"backend/kafka_deep_dive#6113cea5da8e","section":"backend","module":"backend/kafka_deep_dive","moduleName":"kafka deep dive","qIndex":1,"difficulty":"core","question":"What is the role of a partition in Kafka and how does it enable parallelism?","answerFull":"A partition is the fundamental unit of parallelism and ordering in Kafka. Each topic is split into N partitions, each an independent ordered log stored on a single broker (the partition leader). Within a consumer group, each partition is consumed by exactly one consumer at a time. Therefore, a topic with 12 partitions can be consumed by at most 12 consumers in parallel within one group. Ordering is guaranteed within a partition but not across partitions. Choose a partition key (e.g., orderId) that maps the records you need ordered together to the same partition.","correct":"A partition is the fundamental unit of parallelism and ordering in Kafka.","distractors":["Consumer lag = Log End Offset - Consumer Committed Offset per partition.","You need: producer-side EOS (enable.idempotence=true, transactional.id, acks=all) to guarantee the event is written exactly once to Kafka.","Consumer group rebalancing is a runtime event triggered when a consumer joins or leaves a group, or when partition count changes."]},{"id":"backend/kafka_deep_dive#1c7cccdcda77","section":"backend","module":"backend/kafka_deep_dive","moduleName":"kafka deep dive","qIndex":2,"difficulty":"core","question":"What is the ISR (In-Sync Replicas) and how does it relate to acks=all?","answerFull":"The ISR is the set of replicas that are fully caught up with the partition leader within replica.lag.time.max.ms (default 30 seconds). With acks=all, the producer waits for all replicas in the ISR to confirm the write. If min.insync.replicas=2 and the ISR has 3 replicas, all 3 must confirm. If one broker is slow and falls out of the ISR, the producer only waits for the remaining ISR members (as long as ISR size >= min.insync.replicas). If the ISR shrinks below min.insync.replicas, the producer receives a NotEnoughReplicasException.","correct":"The ISR is the set of replicas that are fully caught up with the partition leader within replica.lag.time.max.ms (default 30 seconds).","distractors":["Consumer group rebalancing is a runtime event triggered when a consumer joins or leaves a group, or when partition count changes.","The producer accumulates records in an in-memory batch per partition (up to batch.size bytes, default 16384 = 16 KB).","Kafka guarantees order within a single partition."]},{"id":"backend/kafka_deep_dive#adb91fa0d936","section":"backend","module":"backend/kafka_deep_dive","moduleName":"kafka deep dive","qIndex":3,"difficulty":"core","question":"What is the difference between at-most-once, at-least-once, and exactly-once delivery semantics in Kafka?","answerFull":"At-most-once commits the offset before processing — if processing fails, the message is lost but never duplicated. At-least-once commits after processing — if the consumer crashes after processing but before committing, the message is reprocessed on restart, potentially causing duplicates. Exactly-once is achieved by combining three features: enable.idempotence=true on the producer (eliminates duplicates caused by producer retries), transactional.id + producer.beginTransaction() / commitTransaction() for atomic multi-partition writes, and isolation.level=read_committed on consumers so they only see committed data. Without all three, you cannot guarantee exactly-once.","correct":"At-most-once commits the offset before processing — if processing fails, the message is lost but never duplicated.","distractors":["You need: producer-side EOS (enable.idempotence=true, transactional.id, acks=all) to guarantee the event is written exactly once to Kafka.","The idempotent producer assigns a producer ID (PID) and a monotonically increasing sequence number to each message.","Consumer lag = Log End Offset - Consumer Committed Offset per partition."]},{"id":"backend/kafka_deep_dive#069ca95eaf90","section":"backend","module":"backend/kafka_deep_dive","moduleName":"kafka deep dive","qIndex":4,"difficulty":"core","question":"What does enable.idempotence=true do in the producer?","answerFull":"The idempotent producer assigns a producer ID (PID) and a monotonically increasing sequence number to each message. The broker tracks the last sequence number per (PID, partition). If the producer retries a message (e.g., due to a network timeout), the broker detects the duplicate sequence number and discards the duplicate, returning success to the producer. This eliminates duplicates caused by producer retries within a single producer session. Note: the PID is reassigned on producer restart, so idempotence is per-session only. For cross-session deduplication, use transactional producers or consumer-side idempotency.","correct":"The idempotent producer assigns a producer ID (PID) and a monotonically increasing sequence number to each message.","distractors":["These two settings jointly control when the producer sends a batch.","The producer accumulates records in an in-memory batch per partition (up to batch.size bytes, default 16384 = 16 KB).","Producer metrics: record-error-rate (should be 0), record-send-rate, request-latency-avg."]},{"id":"backend/kafka_deep_dive#04160d867655","section":"backend","module":"backend/kafka_deep_dive","moduleName":"kafka deep dive","qIndex":5,"difficulty":"core","question":"What is log compaction and when would you use it instead of the default delete policy?","answerFull":"Log compaction retains the latest value for each record key indefinitely, deleting older records with the same key. A null value (tombstone) causes the key to be deleted entirely after the compaction runs. Use it for topics that represent current state rather than event history — for example, a product.prices topic where only the latest price matters, or a Kafka Streams changelog topic backing a state store. Use the delete policy when events are time-bounded and older events are irrelevant after a retention period.","correct":"Log compaction retains the latest value for each record key indefinitely, deleting older records with the same key.","distractors":["The ISR is the set of replicas that are fully caught up with the partition leader within replica.lag.time.max.ms (default 30 seconds).","Consumer lag = Log End Offset - Consumer Committed Offset per partition.","In ZooKeeper-based Kafka, ZooKeeper manages controller election, stores topic metadata, broker registrations, and consumer group offsets (in older versions)."]},{"id":"backend/kafka_deep_dive#d2c3f1069f84","section":"backend","module":"backend/kafka_deep_dive","moduleName":"kafka deep dive","qIndex":6,"difficulty":"intermediate","question":"What is the difference between KStream and KTable in Kafka Streams?","answerFull":"A KStream represents an unbounded stream of events where each record is an independent fact. Multiple records with the same key coexist and are all processed. A KTable represents a changelog stream where each record is an update to a keyed value — only the latest value per key matters, similar to a database table. Internally, a KTable is backed by a state store (RocksDB by default). Use KStream for event processing (every occurrence matters). Use KTable for current-state lookups (latest value per key). A KStream can be aggregated into a KTable.","correct":"A KStream represents an unbounded stream of events where each record is an independent fact.","distractors":["The Schema Registry is a centralized service that stores and enforces schemas for Kafka messages.","A GlobalKTable is replicated to every Kafka Streams instance in the application, regardless of which partitions that instance is assigned.","Producer metrics: record-error-rate (should be 0), record-send-rate, request-latency-avg."]},{"id":"backend/kafka_deep_dive#9aabfbe0525c","section":"backend","module":"backend/kafka_deep_dive","moduleName":"kafka deep dive","qIndex":7,"difficulty":"intermediate","question":"What is a GlobalKTable and when should you use it instead of a KTable?","answerFull":"A GlobalKTable is replicated to every Kafka Streams instance in the application, regardless of which partitions that instance is assigned. This means any instance can join any record against a GlobalKTable without co-partitioning requirements. Use it for small-to-medium reference data (country codes, product catalog with <100k entries) that every instance needs. Never use GlobalKTable for large tables — the full dataset is stored locally on every instance. For large tables with co-partitioned keys, use a regular KTable join.","correct":"A GlobalKTable is replicated to every Kafka Streams instance in the application, regardless of which partitions that instance is assigned.","distractors":["The transaction.id is a static, application-assigned identifier that enables the broker to fence zombie producers.","Kafka guarantees order within a single partition.","You need: producer-side EOS (enable.idempotence=true, transactional.id, acks=all) to guarantee the event is written exactly once to Kafka."]},{"id":"backend/kafka_deep_dive#a1b2ece1cf01","section":"backend","module":"backend/kafka_deep_dive","moduleName":"kafka deep dive","qIndex":8,"difficulty":"intermediate","question":"Explain the producer batching mechanism and how to tune it.","answerFull":"The producer accumulates records in an in-memory batch per partition (up to batch.size bytes, default 16384 = 16 KB). When the batch is full or linger.ms elapses (default 0), the batch is sent. With linger.ms=0, each record is sent as soon as possible (low latency, poor batching). With linger.ms=5, the producer waits 5ms for additional records to fill the batch (higher latency, better throughput and compression ratio). For throughput-optimized pipelines: set batch.size=65536 (64 KB), linger.ms=5–20, and enable compression. For latency-sensitive pipelines: keep linger.ms at 0–1.","correct":"The producer accumulates records in an in-memory batch per partition (up to batch.size bytes, default 16384 = 16 KB).","distractors":["Log compaction retains the latest value for each record key indefinitely, deleting older records with the same key.","The ISR is the set of replicas that are fully caught up with the partition leader within replica.lag.time.max.ms (default 30 seconds).","These two settings jointly control when the producer sends a batch."]},{"id":"backend/kafka_deep_dive#dd6800d01630","section":"backend","module":"backend/kafka_deep_dive","moduleName":"kafka deep dive","qIndex":9,"difficulty":"intermediate","question":"What is cooperative-sticky rebalancing and why is it better than eager rebalancing?","answerFull":"In eager rebalancing, all consumers in a group revoke all partitions simultaneously, then the coordinator reassigns all partitions. This causes a full stop-the-world pause — no consumer processes any message during the rebalance, which can take 10–30+ seconds in large groups. In cooperative-sticky rebalancing (CooperativeStickyAssignor), only the partitions that need to move are revoked, and only the affected consumers pause briefly. Unaffected consumers continue processing uninterrupted. The rebalance runs in multiple rounds. This dramatically reduces the impact of rebalancing caused by rolling deployments or scaling events.","correct":"In eager rebalancing, all consumers in a group revoke all partitions simultaneously, then the coordinator reassigns all partitions.","distractors":["In ZooKeeper-based Kafka, ZooKeeper manages controller election, stores topic metadata, broker registrations, and consumer group offsets (in older versions).","A GlobalKTable is replicated to every Kafka Streams instance in the application, regardless of which partitions that instance is assigned.","Consumer group rebalancing is a runtime event triggered when a consumer joins or leaves a group, or when partition count changes."]},{"id":"backend/kafka_deep_dive#f7c8f8a9bf2b","section":"backend","module":"backend/kafka_deep_dive","moduleName":"kafka deep dive","qIndex":10,"difficulty":"intermediate","question":"What is the Schema Registry and what compatibility modes does it support?","answerFull":"The Schema Registry is a centralized service that stores and enforces schemas for Kafka messages. Producers register a schema and receive a numeric schema ID; the ID and serialized bytes are published to Kafka. Consumers fetch the schema by ID and deserialize. Compatibility modes: BACKWARD — new schema can read data written with old schema (safe: add optional fields with defaults); FORWARD — old schema can read data written with new schema (safe: only add fields that old consumers will ignore); FULL — both backward and forward; BACKWARD_TRANSITIVE / FORWARD_TRANSITIVE / FULL_TRANSITIVE — check against all historical versions, not just the latest. Use FULL_TRANSITIVE for the strongest guarantee.","correct":"The Schema Registry is a centralized service that stores and enforces schemas for Kafka messages.","distractors":["You need: producer-side EOS (enable.idempotence=true, transactional.id, acks=all) to guarantee the event is written exactly once to Kafka.","A partition is the fundamental unit of parallelism and ordering in Kafka.","In ZooKeeper-based Kafka, ZooKeeper manages controller election, stores topic metadata, broker registrations, and consumer group offsets (in older versions)."]},{"id":"backend/kafka_deep_dive#8bbde46c0c7b","section":"backend","module":"backend/kafka_deep_dive","moduleName":"kafka deep dive","qIndex":11,"difficulty":"advanced","question":"How does Kafka handle message ordering guarantees?","answerFull":"Kafka guarantees order within a single partition. Records with the same partition key always land in the same partition (hash(key) % numPartitions) and are consumed in order by the assigned consumer. There is no ordering guarantee across partitions. To maintain order for an entity (e.g., all events for order-123), always use the entity ID as the partition key. With enable.idempotence=true, setting max.in.flight.requests.per.connection=5 (up from 1) is safe because the idempotent producer reorders retried batches correctly using sequence numbers.","correct":"Kafka guarantees order within a single partition.","distractors":["The ISR is the set of replicas that are fully caught up with the partition leader within replica.lag.time.max.ms (default 30 seconds).","The Schema Registry is a centralized service that stores and enforces schemas for Kafka messages.","Consumer lag = Log End Offset - Consumer Committed Offset per partition."]},{"id":"backend/kafka_deep_dive#588751b74f7b","section":"backend","module":"backend/kafka_deep_dive","moduleName":"kafka deep dive","qIndex":12,"difficulty":"advanced","question":"What is the purpose of the transaction.id configuration in the producer?","answerFull":"The transaction.id is a static, application-assigned identifier that enables the broker to fence zombie producers. If a producer instance crashes and a new instance starts with the same transaction.id, the broker increments the producer epoch and rejects writes from the old instance (the zombie). This prevents two producer instances from writing to the same transactional stream simultaneously, which would break the exactly-once guarantee. The transaction.id must be unique per partition subset the producer writes to and stable across restarts.","correct":"The transaction.id is a static, application-assigned identifier that enables the broker to fence zombie producers.","distractors":["The idempotent producer assigns a producer ID (PID) and a monotonically increasing sequence number to each message.","You need: producer-side EOS (enable.idempotence=true, transactional.id, acks=all) to guarantee the event is written exactly once to Kafka.","A GlobalKTable is replicated to every Kafka Streams instance in the application, regardless of which partitions that instance is assigned."]},{"id":"backend/kafka_deep_dive#7516a8ef1ea4","section":"backend","module":"backend/kafka_deep_dive","moduleName":"kafka deep dive","qIndex":13,"difficulty":"advanced","question":"How would you implement a consumer that processes messages exactly once, end-to-end?","answerFull":"You need: producer-side EOS (enable.idempotence=true, transactional.id, acks=all) to guarantee the event is written exactly once to Kafka. Consumer-side isolation.level=read_committed so the consumer only reads committed transactional records. If the consumer writes results to Kafka (Kafka-to-Kafka), use consumer-producer transactions: consumer.poll(), process, producer.beginTransaction(), produce result, send offsets with producer.sendOffsetsToTransaction(offsets, groupMetadata), producer.commitTransaction(). This atomically commits both the result and the offset. If the consumer writes to an external database, use idempotent upserts keyed on the Kafka record's offset+partition as the idempotency key.","correct":"You need: producer-side EOS (enable.idempotence=true, transactional.id, acks=all) to guarantee the event is written exactly once to Kafka.","distractors":["The Schema Registry is a centralized service that stores and enforces schemas for Kafka messages.","Consumer group rebalancing is a runtime event triggered when a consumer joins or leaves a group, or when partition count changes.","These two settings jointly control when the producer sends a batch."]},{"id":"backend/kafka_deep_dive#cde9a0a86ce1","section":"backend","module":"backend/kafka_deep_dive","moduleName":"kafka deep dive","qIndex":14,"difficulty":"advanced","question":"What metrics should you monitor in a production Kafka deployment?","answerFull":"Producer metrics: record-error-rate (should be 0), record-send-rate, request-latency-avg. Consumer metrics: consumer group lag per partition (most critical — alert at 10k+ records), fetch-rate, commit-rate. Broker metrics: UnderReplicatedPartitions (should be 0 — indicates ISR degradation), ActiveControllerCount (should be 1), OfflinePartitionsCount (should be 0), disk utilization, network throughput, RequestHandlerAvgIdlePercent (below 30% indicates broker is overloaded). Topic metrics: message rate per partition, bytes in/out per broker.","correct":"Producer metrics: record-error-rate (should be 0), record-send-rate, request-latency-avg.","distractors":["A GlobalKTable is replicated to every Kafka Streams instance in the application, regardless of which partitions that instance is assigned.","A KStream represents an unbounded stream of events where each record is an independent fact.","The idempotent producer assigns a producer ID (PID) and a monotonically increasing sequence number to each message."]},{"id":"backend/kafka_deep_dive#397098cbb097","section":"backend","module":"backend/kafka_deep_dive","moduleName":"kafka deep dive","qIndex":15,"difficulty":"advanced","question":"What is the difference between consumer group rebalancing and partition reassignment?","answerFull":"Consumer group rebalancing is a runtime event triggered when a consumer joins or leaves a group, or when partition count changes. It redistributes partition assignments among the live consumers in the group without moving data. Partition reassignment (via kafka-reassign-partitions.sh or Admin API) is an administrative operation that moves partition replicas between brokers — it physically copies partition data to new brokers. Partition reassignment is used for broker decommissioning, rack-aware rebalancing, or restoring replication factor after broker failure.","correct":"Consumer group rebalancing is a runtime event triggered when a consumer joins or leaves a group, or when partition count changes.","distractors":["In ZooKeeper-based Kafka, ZooKeeper manages controller election, stores topic metadata, broker registrations, and consumer group offsets (in older versions).","In eager rebalancing, all consumers in a group revoke all partitions simultaneously, then the coordinator reassigns all partitions.","Kafka guarantees order within a single partition."]},{"id":"backend/kafka_deep_dive#476eaffeb50c","section":"backend","module":"backend/kafka_deep_dive","moduleName":"kafka deep dive","qIndex":16,"difficulty":"advanced","question":"How does KRaft mode change Kafka's architecture compared to ZooKeeper-based Kafka?","answerFull":"In ZooKeeper-based Kafka, ZooKeeper manages controller election, stores topic metadata, broker registrations, and consumer group offsets (in older versions). The active controller is a single broker elected via ZooKeeper. In KRaft mode, a subset of brokers designated as controllers form a Raft quorum. The active controller is elected via Raft consensus. All cluster metadata is stored in an internal cluster_metadata topic replicated via the Raft log. Eliminating ZooKeeper removes an external operational dependency, reduces the number of processes to manage, improves controller failover speed from seconds to sub-seconds, and removes the ~200k partition scalability limit that ZooKeeper imposed.","correct":"In ZooKeeper-based Kafka, ZooKeeper manages controller election, stores topic metadata, broker registrations, and consumer group offsets (in older versions).","distractors":["Log compaction retains the latest value for each record key indefinitely, deleting older records with the same key.","A partition is the fundamental unit of parallelism and ordering in Kafka.","Consumer group rebalancing is a runtime event triggered when a consumer joins or leaves a group, or when partition count changes."]},{"id":"backend/kafka_deep_dive#961737563d1c","section":"backend","module":"backend/kafka_deep_dive","moduleName":"kafka deep dive","qIndex":17,"difficulty":"advanced","question":"What is the significance of the linger.ms and batch.size settings together?","answerFull":"These two settings jointly control when the producer sends a batch. batch.size sets the maximum size of a batch in bytes — the batch is sent immediately when full. linger.ms sets the maximum time the producer waits for the batch to fill before sending regardless of size. They work together: with batch.size=64KB and linger.ms=5, the producer sends when either 64KB is accumulated OR 5ms elapses, whichever comes first. At high throughput, batches fill quickly (batch.size dominates — near-zero extra latency). At low throughput, linger.ms governs (adds up to 5ms latency but groups more records together for compression efficiency). Setting both linger.ms=0 and a large batch.size is counterproductive — batches will rarely fill.","correct":"These two settings jointly control when the producer sends a batch.","distractors":["You need: producer-side EOS (enable.idempotence=true, transactional.id, acks=all) to guarantee the event is written exactly once to Kafka.","The producer accumulates records in an in-memory batch per partition (up to batch.size bytes, default 16384 = 16 KB).","Producer metrics: record-error-rate (should be 0), record-send-rate, request-latency-avg."]},{"id":"backend/kafka_deep_dive#0619a5f8528c","section":"backend","module":"backend/kafka_deep_dive","moduleName":"kafka deep dive","qIndex":18,"difficulty":"advanced","question":"How do you monitor and alert on consumer lag in production?","answerFull":"Consumer lag = Log End Offset - Consumer Committed Offset per partition. Monitor it via JMX (kafka.consumer:type=consumer-fetch-manager-metrics,client-id=*,attribute=records-lag-max), Burrow (LinkedIn's consumer lag monitor), or by querying the Kafka Admin API. Export to Prometheus via the Kafka JMX Exporter. Set Grafana alerts: warn at 10,000 records lag, critical at 100,000. For Kubernetes deployments, use KEDA (Kubernetes Event-Driven Autoscaling) with the Kafka scaler to automatically scale consumer pod count based on lag. Treat lag as a latency SLA — if your SLA is 30-second processing freshness, 300,000 records at 10,000 records/sec processing speed means 30 seconds of lag before SLA breach.","correct":"Consumer lag = Log End Offset - Consumer Committed Offset per partition.","distractors":["A partition is the fundamental unit of parallelism and ordering in Kafka.","Consumer group rebalancing is a runtime event triggered when a consumer joins or leaves a group, or when partition count changes.","The producer accumulates records in an in-memory batch per partition (up to batch.size bytes, default 16384 = 16 KB)."]},{"id":"backend/load_and_performance_testing#1ae3518a2282","section":"backend","module":"backend/load_and_performance_testing","moduleName":"load and performance testing","qIndex":1,"difficulty":"core","question":"What is the difference between load testing and stress testing?","answerFull":"Load testing validates system behavior at expected production load — you simulate the peak RPS you expect on a busy day and verify that latency and error rate meet SLOs. Stress testing pushes beyond capacity to find the breaking point and understand failure mode. Does the service fail fast (circuit breaker opens, returns 503 quickly), fail slowly (timeout after 30 seconds), or fail silently (returns incorrect data)? Stress tests inform capacity planning: if the service breaks at 3000 RPS and you expect 1000 RPS, you have a 3x headroom — healthy. If it breaks at 1200 RPS, you need more capacity.","correct":"Load testing validates system behavior at expected production load — you simulate the peak RPS you expect on a busy day and verify that latency and error rate meet SLOs.","distractors":["Run the breakpoint test to find the maximum sustainable RPS (where error rate first exceeds 1%).","A soak test runs a sustained load (typically 60-80% of peak capacity) for an extended period (8-24 hours).","When a load test uses a virtual-user (VU) model, each VU sends one request and waits for the response before sending the next."]},{"id":"backend/load_and_performance_testing#727387f55ab5","section":"backend","module":"backend/load_and_performance_testing","moduleName":"load and performance testing","qIndex":2,"difficulty":"core","question":"What is the coordinated omission problem?","answerFull":"When a load test uses a virtual-user (VU) model, each VU sends one request and waits for the response before sending the next. If the server slows down and takes 1 second per request, a 100-VU test only sends 100 RPS instead of the intended 1000 RPS. The slow period is under-sampled because fewer requests arrive. The latency histogram looks better than real production behavior where arrivals continue regardless of server speed. The fix is to use a constant arrival rate model (k6's constant-arrival-rate executor, Gatling's constantUsersPerSec) which maintains the target RPS even when the server is slow.","correct":"When a load test uses a virtual-user (VU) model, each VU sends one request and waits for the response before sending the next.","distractors":["Application layer: request rate (RPS), latency percentiles (p50, p95, p99, p999), error rate by type (4xx vs 5xx), active concurrent connections.","A soak test runs a sustained load (typically 60-80% of peak capacity) for an extended period (8-24 hours).","Establish a baseline: run a 10-minute load test at fixed RPS (e.g., 500 RPS) on the main branch and record p50/p95/p99."]},{"id":"backend/load_and_performance_testing#e533273ede99","section":"backend","module":"backend/load_and_performance_testing","moduleName":"load and performance testing","qIndex":3,"difficulty":"core","question":"How do you identify a database bottleneck during a load test?","answerFull":"Monitor connection pool metrics (active connections, pending connections) — if pending connections exceed 0 for sustained periods, the pool is exhausted. Check pg_stat_activity for long-running queries. Enable pg_stat_statements and query it for highest mean execution time. Check pg_locks for lock contention. Correlate GC pauses (JVM) vs DB query time (application timer) to determine if latency spikes are JVM-side or DB-side. A definitive signal: if adding more application instances does not improve throughput (RPS stays flat), the bottleneck is shared infrastructure (DB, Redis, external API).","correct":"Monitor connection pool metrics (active connections, pending connections) — if pending connections exceed 0 for sustained periods, the pool is exhausted.","distractors":["A soak test runs a sustained load (typically 60-80% of peak capacity) for an extended period (8-24 hours).","Establish a baseline: run a 10-minute load test at fixed RPS (e.g., 500 RPS) on the main branch and record p50/p95/p99.","Load testing validates system behavior at expected production load — you simulate the peak RPS you expect on a busy day and verify that latency and error rate meet SLOs."]},{"id":"backend/load_and_performance_testing#818679948528","section":"backend","module":"backend/load_and_performance_testing","moduleName":"load and performance testing","qIndex":4,"difficulty":"core","question":"What metrics should you track during a performance test?","answerFull":"Application layer: request rate (RPS), latency percentiles (p50, p95, p99, p999), error rate by type (4xx vs 5xx), active concurrent connections. JVM layer: heap usage, GC pause duration and frequency, GC allocation rate (MB/s), thread states (blocked threads indicate lock contention). Database: active connections, query execution time, lock wait events, index hit rate (should be > 99%), I/O wait. Operating system: CPU utilization (> 80% = CPU bound), memory (swap = bad), network bandwidth, disk I/O latency. The combination tells you where the bottleneck is.","correct":"Application layer: request rate (RPS), latency percentiles (p50, p95, p99, p999), error rate by type (4xx vs 5xx), active concurrent connections.","distractors":["Run the breakpoint test to find the maximum sustainable RPS (where error rate first exceeds 1%).","Load testing validates system behavior at expected production load — you simulate the peak RPS you expect on a busy day and verify that latency and error rate meet SLOs.","Establish a baseline: run a 10-minute load test at fixed RPS (e.g., 500 RPS) on the main branch and record p50/p95/p99."]},{"id":"backend/load_and_performance_testing#5121c1feab41","section":"backend","module":"backend/load_and_performance_testing","moduleName":"load and performance testing","qIndex":5,"difficulty":"core","question":"How do you implement performance regression detection in CI?","answerFull":"Establish a baseline: run a 10-minute load test at fixed RPS (e.g., 500 RPS) on the main branch and record p50/p95/p99. On each PR, run the same test and compare. Fail the build if p99 regresses more than 10% vs baseline, or error rate increases by more than 0.1%. Store results in a time-series database (InfluxDB) with build metadata (commit SHA, branch, date). Gatling's CI plugin outputs a baseline comparison report. k6 threshold assertions fail the exit code so CI pipelines can detect failures natively. Run performance tests in a separate, dedicated environment with production-equivalent resources.","correct":"Establish a baseline: run a 10-minute load test at fixed RPS (e.g., 500 RPS) on the main branch and record p50/p95/p99.","distractors":["When a load test uses a virtual-user (VU) model, each VU sends one request and waits for the response before sending the next.","Load testing validates system behavior at expected production load — you simulate the peak RPS you expect on a busy day and verify that latency and error rate meet SLOs.","A soak test runs a sustained load (typically 60-80% of peak capacity) for an extended period (8-24 hours)."]},{"id":"backend/load_and_performance_testing#72c328c43ba1","section":"backend","module":"backend/load_and_performance_testing","moduleName":"load and performance testing","qIndex":6,"difficulty":"intermediate","question":"What is a soak test and what does it detect?","answerFull":"A soak test runs a sustained load (typically 60-80% of peak capacity) for an extended period (8-24 hours). It detects slow degradation that only becomes visible over time: memory leaks (heap grows by 10MB/hour, eventually causes OOM after 10 hours), connection leaks (connection pool gradually fills as connections are not returned to pool), thread leaks (thread count grows monotonically), database file handle leaks, and performance cliffs where query plans degrade as table statistics change. Load tests miss these because they are too short. A service that passes a 30-minute load test may fail after 12 hours in production.","correct":"A soak test runs a sustained load (typically 60-80% of peak capacity) for an extended period (8-24 hours).","distractors":["When a load test uses a virtual-user (VU) model, each VU sends one request and waits for the response before sending the next.","Run the breakpoint test to find the maximum sustainable RPS (where error rate first exceeds 1%).","Establish a baseline: run a 10-minute load test at fixed RPS (e.g., 500 RPS) on the main branch and record p50/p95/p99."]},{"id":"backend/load_and_performance_testing#0327a770860b","section":"backend","module":"backend/load_and_performance_testing","moduleName":"load and performance testing","qIndex":7,"difficulty":"intermediate","question":"How do you interpret a flamegraph from a performance profile?","answerFull":"A flamegraph shows the call stack of CPU samples. The x-axis is time (proportional width = % of CPU time spent in that method and its callees). The y-axis is stack depth. Wide frames at the top of the stack are hot code paths — the wider, the more CPU time spent. Find the widest frames that are application code (not JVM internals). If JsonSerializer.serialize() is 30% wide, JSON serialization is a bottleneck — consider a faster library (Jackson streaming API vs ObjectMapper). If DatabaseConnectionPool.acquire() is 20% wide, connection pool contention is the issue. If GC-related frames are wide, memory allocation is the problem.","correct":"A flamegraph shows the call stack of CPU samples.","distractors":["A soak test runs a sustained load (typically 60-80% of peak capacity) for an extended period (8-24 hours).","Application layer: request rate (RPS), latency percentiles (p50, p95, p99, p999), error rate by type (4xx vs 5xx), active concurrent connections.","Establish a baseline: run a 10-minute load test at fixed RPS (e.g., 500 RPS) on the main branch and record p50/p95/p99."]},{"id":"backend/load_and_performance_testing#91e9a468d113","section":"backend","module":"backend/load_and_performance_testing","moduleName":"load and performance testing","qIndex":8,"difficulty":"intermediate","question":"What is your process for capacity planning from load test results?","answerFull":"Run the breakpoint test to find the maximum sustainable RPS (where error rate first exceeds 1%). Calculate required headroom: if you expect 10K RPS peak and one instance handles 3K RPS at 60% CPU, you need ceil(10000/3000) = 4 instances, plus 20% spare = 5 instances. Account for: gradual traffic ramp (HPA needs 2-3 minutes to add instances), traffic skew (some instances may receive 2x average), headroom for unexpected spikes (provision for 150% of expected peak). Validate by running the load test against the capacity-planned cluster. Re-run quarterly or when traffic doubles.","correct":"Run the breakpoint test to find the maximum sustainable RPS (where error rate first exceeds 1%).","distractors":["Establish a baseline: run a 10-minute load test at fixed RPS (e.g., 500 RPS) on the main branch and record p50/p95/p99.","Load testing validates system behavior at expected production load — you simulate the peak RPS you expect on a busy day and verify that latency and error rate meet SLOs.","Application layer: request rate (RPS), latency percentiles (p50, p95, p99, p999), error rate by type (4xx vs 5xx), active concurrent connections."]},{"id":"backend/messaging_patterns#4a0398afb8af","section":"backend","module":"backend/messaging_patterns","moduleName":"messaging patterns","qIndex":1,"difficulty":"core","question":"What is the dual-write problem and how does the outbox pattern solve it?","answerFull":"The dual-write problem occurs when a service must write to two systems atomically — for example, update a database and publish a Kafka message. These two writes cannot be wrapped in a single ACID transaction spanning different systems. If the DB write succeeds but Kafka publish fails, the event is lost (downstream services miss the update). If Kafka publish succeeds but DB write fails (or rolls back), a phantom event is published for a transaction that never happened. The outbox pattern solves this by writing the event to an outbox_events table in the same DB transaction as the business data. A separate relay (polling or CDC) then reads the outbox and publishes to Kafka. The relay can retry until success — events are published at least once after the DB transaction commits.","correct":"The dual-write problem occurs when a service must write to two systems atomically — for example, update a database and publish a Kafka message.","distractors":["The transactional inbox stores a record of processed message IDs in the same database as the application state, with the deduplication INSERT and business logic update in the same transaction.","Kafka is log-based: messages are retained for a configured duration (default 7 days) and consumers maintain their own offset.","Kafka guarantees ordering within a partition, not across partitions."]},{"id":"backend/messaging_patterns#b76aadc0714e","section":"backend","module":"backend/messaging_patterns","moduleName":"messaging patterns","qIndex":2,"difficulty":"core","question":"What is exactly-once delivery and how do you achieve it with Kafka?","answerFull":"Exactly-once delivery means each message is processed precisely once, producing the same result as if processed once — no duplicates, no losses. Achieving this requires coordination at producer, broker, and consumer levels. Producer: enable.idempotence=true (sequence numbers prevent duplicates from retries), transactional API for atomic multi-partition writes. Broker: acks=all, min.insync.replicas=2. Consumer: isolation.level=read_committed (skip uncommitted transactional messages). Application level: transactional inbox with deduplication table. Full end-to-end exactly-once within Kafka Streams is achievable with processing.guarantee=exactly_once_v2. Across service boundaries (Kafka + external DB), exactly-once requires the transactional outbox + inbox pattern.","correct":"Exactly-once delivery means each message is processed precisely once, producing the same result as if processed once — no duplicates, no losses.","distractors":["The dual-write problem occurs when a service must write to two systems atomically — for example, update a database and publish a Kafka message.","Kafka guarantees ordering within a partition, not across partitions.","BACKWARD compatibility means a newer schema version can read data written with an older schema version."]},{"id":"backend/messaging_patterns#b8a449976356","section":"backend","module":"backend/messaging_patterns","moduleName":"messaging patterns","qIndex":3,"difficulty":"core","question":"What is a dead letter queue and how should you handle messages in it?","answerFull":"A DLQ (dead letter topic/queue) receives messages that have exhausted all retry attempts. Messages end up in the DLQ because they are unprocessable: bad payload format, invalid data, downstream service unavailable for too long, or application bugs. DLQ consumers should: (1) log and alert on every DLQ message — DLQ depth > 0 is always an alert condition, (2) analyze root cause — is it a bad message, an application bug, or an infrastructure issue, (3) after fixing the root cause, replay DLQ messages back to the original topic in controlled batches. Never delete DLQ messages until you understand why they failed. Keep DLQ messages for at least 7 days.","correct":"A DLQ (dead letter topic/queue) receives messages that have exhausted all retry attempts.","distractors":["Kafka is log-based: messages are retained for a configured duration (default 7 days) and consumers maintain their own offset.","BACKWARD compatibility means a newer schema version can read data written with an older schema version.","Kafka guarantees ordering within a partition, not across partitions."]},{"id":"backend/messaging_patterns#3e3cfce767ec","section":"backend","module":"backend/messaging_patterns","moduleName":"messaging patterns","qIndex":4,"difficulty":"core","question":"What is the transactional inbox pattern and when is it necessary?","answerFull":"The transactional inbox stores a record of processed message IDs in the same database as the application state, with the deduplication INSERT and business logic update in the same transaction. If the consumer crashes after processing but before committing the Kafka offset, Kafka will redeliver the message. Without an inbox, the business logic runs twice (double charge, double inventory decrement). With the inbox, the second processing attempt finds the message_id already in the inbox table and skips processing. It is necessary for non-idempotent operations: financial transactions, inventory decrements, counters, and any operation where \"process twice\" has different results than \"process once.\"","correct":"The transactional inbox stores a record of processed message IDs in the same database as the application state, with the deduplication INSERT and business logic update in the same transaction.","distractors":["The dual-write problem occurs when a service must write to two systems atomically — for example, update a database and publish a Kafka message.","A DLQ (dead letter topic/queue) receives messages that have exhausted all retry attempts.","BACKWARD compatibility means a newer schema version can read data written with an older schema version."]},{"id":"backend/messaging_patterns#bd57c8fa4d4f","section":"backend","module":"backend/messaging_patterns","moduleName":"messaging patterns","qIndex":5,"difficulty":"core","question":"What is Avro BACKWARD compatibility and why should you default to it?","answerFull":"BACKWARD compatibility means a newer schema version can read data written with an older schema version. When you add an optional field with a default value, new consumers can read old messages (the field defaults to null/0) and new messages (the field has a value). Deploy consumers first with the new schema (they can read old messages with defaults), then deploy producers that start writing the new field. This is the standard deployment order: consumers before producers. FORWARD compatibility means the opposite (old schema reads new data) — needed when you must deploy producers first. FULL is both directions and is the safest but most restrictive.","correct":"BACKWARD compatibility means a newer schema version can read data written with an older schema version.","distractors":["PostgreSQL creates a replication slot for Debezium to read WAL changes.","Kafka is log-based: messages are retained for a configured duration (default 7 days) and consumers maintain their own offset.","A DLQ (dead letter topic/queue) receives messages that have exhausted all retry attempts."]},{"id":"backend/messaging_patterns#4d498bd1bb9a","section":"backend","module":"backend/messaging_patterns","moduleName":"messaging patterns","qIndex":6,"difficulty":"intermediate","question":"What is the difference between Kafka and RabbitMQ for event-driven architectures?","answerFull":"Kafka is log-based: messages are retained for a configured duration (default 7 days) and consumers maintain their own offset. Any consumer can replay from any offset. Kafka preserves order within a partition. Throughput is very high (1M+ msg/s per cluster). Kafka is ideal for event streaming, audit logs, and scenarios requiring replay. RabbitMQ is queue-based: messages are removed from the queue once acknowledged. Complex routing is available (topic exchanges, header exchanges). Throughput is lower (~50K msg/s) but latency is lower (push-based vs poll-based). RabbitMQ is better for task queues, RPC patterns, and complex routing requirements. Use Kafka when you need replay and high throughput; use RabbitMQ when you need complex routing and low latency task distribution.","correct":"Kafka is log-based: messages are retained for a configured duration (default 7 days) and consumers maintain their own offset.","distractors":["Kafka guarantees ordering within a partition, not across partitions.","The dual-write problem occurs when a service must write to two systems atomically — for example, update a database and publish a Kafka message.","A DLQ (dead letter topic/queue) receives messages that have exhausted all retry attempts."]},{"id":"backend/messaging_patterns#0097ef66d781","section":"backend","module":"backend/messaging_patterns","moduleName":"messaging patterns","qIndex":7,"difficulty":"intermediate","question":"How do you handle message ordering with Kafka when multiple consumers process in parallel?","answerFull":"Kafka guarantees ordering within a partition, not across partitions. To maintain order for a specific entity (e.g., all events for order-123), always use the entity ID as the partition key: kafkaTemplate.send(topic, orderId.toString(), payload). All events for the same order will go to the same partition, processed in order by the same consumer thread. For consumers, within a partition, messages are processed sequentially. Across partitions, messages are processed in parallel. Never change the partition count of an existing topic — it changes the hash mapping and breaks ordering guarantees for existing consumers until they process through the re-partitioned data.","correct":"Kafka guarantees ordering within a partition, not across partitions.","distractors":["The dual-write problem occurs when a service must write to two systems atomically — for example, update a database and publish a Kafka message.","The transactional inbox stores a record of processed message IDs in the same database as the application state, with the deduplication INSERT and business logic update in the same transaction.","Kafka is log-based: messages are retained for a configured duration (default 7 days) and consumers maintain their own offset."]},{"id":"backend/messaging_patterns#4eee9c5b1ce8","section":"backend","module":"backend/messaging_patterns","moduleName":"messaging patterns","qIndex":8,"difficulty":"intermediate","question":"What happens when the Debezium CDC connector loses its replication slot?","answerFull":"PostgreSQL creates a replication slot for Debezium to read WAL changes. If Debezium is down for too long, PostgreSQL cannot clean up WAL files because Debezium's slot is holding them. This causes disk space to fill up. If disk fills, PostgreSQL stops accepting writes — a production outage. Mitigations: (1) set max_slot_wal_keep_size in PostgreSQL to limit WAL retention per slot; (2) monitor replication slot lag — alert when lag exceeds 1GB; (3) if Debezium is down for more than a few hours, manually drop the slot and let Debezium reinitialize (this causes the outbox relay approach to be safer as a fallback). Always monitor pg_replication_slots in production.","correct":"PostgreSQL creates a replication slot for Debezium to read WAL changes.","distractors":["BACKWARD compatibility means a newer schema version can read data written with an older schema version.","A DLQ (dead letter topic/queue) receives messages that have exhausted all retry attempts.","Kafka guarantees ordering within a partition, not across partitions."]},{"id":"backend/microservices_fundamentals#d50c021aa505","section":"backend","module":"backend/microservices_fundamentals","moduleName":"microservices fundamentals","qIndex":1,"difficulty":"core","question":"What is the difference between a microservice and a monolith, and when would you choose one over the other?","answerFull":"A monolith is a single deployable unit where all business logic runs in one process sharing a database. A microservice architecture decomposes the system into independently deployable services, each owning its data. Choose a monolith for early-stage products, small teams (under 10 engineers), or when domain boundaries are not yet clear. Choose microservices when you have multiple teams needing independent deployments, well-understood domain boundaries, and varying scaling requirements. The modular monolith is a pragmatic middle ground.","correct":"A monolith is a single deployable unit where all business logic runs in one process sharing a database.","distractors":["A distributed monolith is a system deployed as separate services but tightly coupled via synchronous call chains, shared databases, or shared code libraries.","Unit testing individual services is straightforward.","The strangler fig pattern migrates a monolith incrementally by routing new or extracted features behind an API gateway to new microservices, while the monolith handles remaining features."]},{"id":"backend/microservices_fundamentals#78cdcd0be479","section":"backend","module":"backend/microservices_fundamentals","moduleName":"microservices fundamentals","qIndex":2,"difficulty":"core","question":"What is the database-per-service pattern and why is it critical?","answerFull":"Each service owns its own database and no other service accesses it directly. This is critical because it enforces loose coupling at the data layer. Without it, a schema change in one team's database breaks another team's queries, creating de facto deployment coupling. It is the hardest microservices rule to follow because it forces you to handle data aggregation at the API level, deal with eventual consistency, and implement patterns like sagas for cross-service transactions.","correct":"Each service owns its own database and no other service accesses it directly.","distractors":["Option 1: Service A calls Service B's API at request time (synchronous).","Without the outbox pattern, a service might save data to the database and then publish an event to Kafka.","Use the Saga pattern. In a choreography-based saga, each service publishes an event on success; downstream services listen and react, publishing their own events or compensation events on failure."]},{"id":"backend/microservices_fundamentals#658bf4cd6757","section":"backend","module":"backend/microservices_fundamentals","moduleName":"microservices fundamentals","qIndex":3,"difficulty":"core","question":"What is a distributed monolith and how do you avoid it?","answerFull":"A distributed monolith is a system deployed as separate services but tightly coupled via synchronous call chains, shared databases, or shared code libraries. It has the worst properties of both architectures: the complexity of distributed systems with the coupling of a monolith. Avoid it by enforcing database-per-service, preferring asynchronous communication for non-blocking workflows, keeping shared libraries to pure utilities (no domain logic), and measuring actual deployment independence (can a team deploy without coordinating with another team?).","correct":"A distributed monolith is a system deployed as separate services but tightly coupled via synchronous call chains, shared databases, or shared code libraries.","distractors":["Option 1: Service A calls Service B's API at request time (synchronous).","Unit testing individual services is straightforward.","A monolith is a single deployable unit where all business logic runs in one process sharing a database."]},{"id":"backend/microservices_fundamentals#0d791395565b","section":"backend","module":"backend/microservices_fundamentals","moduleName":"microservices fundamentals","qIndex":4,"difficulty":"core","question":"Explain the strangler fig pattern.","answerFull":"The strangler fig pattern migrates a monolith incrementally by routing new or extracted features behind an API gateway to new microservices, while the monolith handles remaining features. Over time, features are extracted one by one until the monolith is fully replaced. This avoids the risk of a big-bang rewrite. The key is to start with the strangler fig from day one — retroactively adding a gateway to a tightly coupled monolith is itself a large project.","correct":"The strangler fig pattern migrates a monolith incrementally by routing new or extracted features behind an API gateway to new microservices, while the monolith handles remaining features.","distractors":["The API gateway is the single entry point for all clients.","Use the Saga pattern. In a choreography-based saga, each service publishes an event on success; downstream services listen and react, publishing their own events or compensation events on failure.","Without the outbox pattern, a service might save data to the database and then publish an event to Kafka."]},{"id":"backend/microservices_fundamentals#b9294aa90a49","section":"backend","module":"backend/microservices_fundamentals","moduleName":"microservices fundamentals","qIndex":5,"difficulty":"core","question":"How do you handle data consistency across services when you cannot use a single ACID transaction?","answerFull":"Use the Saga pattern. In a choreography-based saga, each service publishes an event on success; downstream services listen and react, publishing their own events or compensation events on failure. In an orchestration-based saga, a central coordinator sends commands to services and handles compensation. The outbox pattern ensures events are reliably published: write the event to a local outbox table in the same transaction as the domain change, then relay the outbox to the message broker asynchronously. This guarantees at-least-once delivery without distributed transactions.","correct":"Use the Saga pattern. In a choreography-based saga, each service publishes an event on success; downstream services listen and react, publishing their own events or compensation events on failure.","distractors":["A distributed monolith is a system deployed as separate services but tightly coupled via synchronous call chains, shared databases, or shared code libraries.","If Service A calls B, B calls C, C calls D, all synchronously, the total latency is the sum of all hops: 20ms + 15ms + 30ms + 25ms = 90ms plus overhead.","Unit testing individual services is straightforward."]},{"id":"backend/microservices_fundamentals#47d62e0554e8","section":"backend","module":"backend/microservices_fundamentals","moduleName":"microservices fundamentals","qIndex":6,"difficulty":"intermediate","question":"What is a bounded context in DDD and how does it map to a microservice?","answerFull":"A bounded context is a boundary within which a domain model is consistent and unambiguous. The word \"order\" in the order context means one thing (items, shipping address, status); in the inventory context it may mean something else. Each bounded context should map to one microservice (or a small cluster of services). The mapping ensures teams have clear ownership and domain models do not bleed across service boundaries.","correct":"A bounded context is a boundary within which a domain model is consistent and unambiguous.","distractors":["A distributed monolith is a system deployed as separate services but tightly coupled via synchronous call chains, shared databases, or shared code libraries.","A monolith is a single deployable unit where all business logic runs in one process sharing a database.","Amazon's rule: if a team cannot be fed by two pizzas (6-8 people), it is too large."]},{"id":"backend/microservices_fundamentals#7c0e777f98de","section":"backend","module":"backend/microservices_fundamentals","moduleName":"microservices fundamentals","qIndex":7,"difficulty":"intermediate","question":"What is the difference between REST and gRPC for inter-service communication?","answerFull":"REST uses HTTP/1.1 or HTTP/2 with JSON. gRPC uses HTTP/2 with Protocol Buffers (binary serialization). gRPC is faster (binary encoding, header compression, multiplexing) and supports streaming. REST is easier to debug and more universally supported. Use gRPC for internal, high-throughput service-to-service calls where you control both sides. Use REST for public APIs or when client libraries in all languages are required. gRPC requires a schema (proto file), which enforces API contracts and enables code generation.","correct":"REST uses HTTP/1.1 or HTTP/2 with JSON.","distractors":["Option 1: Service A calls Service B's API at request time (synchronous).","Use the Saga pattern. In a choreography-based saga, each service publishes an event on success; downstream services listen and react, publishing their own events or compensation events on failure.","If Service A calls B, B calls C, C calls D, all synchronously, the total latency is the sum of all hops: 20ms + 15ms + 30ms + 25ms = 90ms plus overhead."]},{"id":"backend/microservices_fundamentals#b1ff45a393c7","section":"backend","module":"backend/microservices_fundamentals","moduleName":"microservices fundamentals","qIndex":8,"difficulty":"intermediate","question":"How do you decompose a monolith using business capabilities?","answerFull":"Identify what the business does, not how the code is organized. A typical e-commerce system has capabilities: product catalog, order management, payment processing, inventory, shipping, customer management, notifications, search. Each capability becomes a service candidate. Validate by asking: does this capability have a clear owner? Does it have well-defined inputs and outputs? Can it be deployed independently? Would different teams reasonably own it? If yes to all, it is a valid service boundary.","correct":"Identify what the business does, not how the code is organized.","distractors":["A distributed monolith is a system deployed as separate services but tightly coupled via synchronous call chains, shared databases, or shared code libraries.","The strangler fig pattern migrates a monolith incrementally by routing new or extracted features behind an API gateway to new microservices, while the monolith handles remaining features.","A monolith is a single deployable unit where all business logic runs in one process sharing a database."]},{"id":"backend/microservices_fundamentals#bb9d7d828b4e","section":"backend","module":"backend/microservices_fundamentals","moduleName":"microservices fundamentals","qIndex":9,"difficulty":"intermediate","question":"What is the two-pizza team rule and why does it matter for microservices?","answerFull":"Amazon's rule: if a team cannot be fed by two pizzas (6-8 people), it is too large. In microservices, this means one team owns one service end-to-end: development, deployment, on-call. Too many people on one service creates coordination overhead. Too few and you cannot sustain the operational burden. The rule enforces that each service is small enough for a small team to own completely, driving autonomous deployment and clear accountability.","correct":"Amazon's rule: if a team cannot be fed by two pizzas (6-8 people), it is too large.","distractors":["The strangler fig pattern migrates a monolith incrementally by routing new or extracted features behind an API gateway to new microservices, while the monolith handles remaining features.","A bounded context is a boundary within which a domain model is consistent and unambiguous.","A distributed monolith is a system deployed as separate services but tightly coupled via synchronous call chains, shared databases, or shared code libraries."]},{"id":"backend/microservices_fundamentals#6e653ab03373","section":"backend","module":"backend/microservices_fundamentals","moduleName":"microservices fundamentals","qIndex":10,"difficulty":"intermediate","question":"How do you handle a scenario where Service A needs data owned by Service B?","answerFull":"Option 1: Service A calls Service B's API at request time (synchronous). Simple but creates a runtime dependency — if B is down, A is degraded. Option 2: Service A subscribes to events from Service B and maintains a read-model (local cache of B's data). A is independent at request time but data is eventually consistent. Option 3: API composition at the gateway level aggregates data from A and B for the client. The right choice depends on consistency requirements: if A needs real-time data from B for a critical operation, use synchronous; if A needs reference data for display, use async with local read-model.","correct":"Option 1: Service A calls Service B's API at request time (synchronous).","distractors":["The strangler fig pattern migrates a monolith incrementally by routing new or extracted features behind an API gateway to new microservices, while the monolith handles remaining features.","Use the Saga pattern. In a choreography-based saga, each service publishes an event on success; downstream services listen and react, publishing their own events or compensation events on failure.","A distributed monolith is a system deployed as separate services but tightly coupled via synchronous call chains, shared databases, or shared code libraries."]},{"id":"backend/microservices_fundamentals#4c8de0e1855a","section":"backend","module":"backend/microservices_fundamentals","moduleName":"microservices fundamentals","qIndex":11,"difficulty":"advanced","question":"What are the main challenges when testing microservices?","answerFull":"Unit testing individual services is straightforward. Integration testing is harder: you need contract tests (Pact) to verify service A's API calls match service B's contract without standing up both services. End-to-end testing requires a full environment with all services running. Consumer-driven contract testing (Pact) addresses the integration test problem: the consumer defines the contract, the provider verifies it, without both needing to be deployed simultaneously. Testcontainers enables integration tests with real databases and message brokers.","correct":"Unit testing individual services is straightforward.","distractors":["A monolith is a single deployable unit where all business logic runs in one process sharing a database.","The strangler fig pattern migrates a monolith incrementally by routing new or extracted features behind an API gateway to new microservices, while the monolith handles remaining features.","Use the Saga pattern. In a choreography-based saga, each service publishes an event on success; downstream services listen and react, publishing their own events or compensation events on failure."]},{"id":"backend/microservices_fundamentals#88de03ea24f8","section":"backend","module":"backend/microservices_fundamentals","moduleName":"microservices fundamentals","qIndex":12,"difficulty":"advanced","question":"What is the purpose of an API gateway in a microservices architecture?","answerFull":"The API gateway is the single entry point for all clients. It handles cross-cutting concerns: routing (path to service mapping), authentication and authorization (JWT validation before requests reach services), rate limiting, SSL termination, request/response transformation, and observability. It prevents each individual service from re-implementing auth, rate limiting, and logging. A gateway also enables the BFF pattern (Backend For Frontend) where separate gateway instances serve mobile, web, and partner clients with tailored request/response shapes.","correct":"The API gateway is the single entry point for all clients.","distractors":["A monolith is a single deployable unit where all business logic runs in one process sharing a database.","Option 1: Service A calls Service B's API at request time (synchronous).","The strangler fig pattern migrates a monolith incrementally by routing new or extracted features behind an API gateway to new microservices, while the monolith handles remaining features."]},{"id":"backend/microservices_fundamentals#d86efdb76f95","section":"backend","module":"backend/microservices_fundamentals","moduleName":"microservices fundamentals","qIndex":13,"difficulty":"advanced","question":"How do synchronous call chains cause latency problems in microservices?","answerFull":"If Service A calls B, B calls C, C calls D, all synchronously, the total latency is the sum of all hops: 20ms + 15ms + 30ms + 25ms = 90ms plus overhead. A ten-hop chain with 20ms average per hop adds 200ms to every request — before any business logic. Solutions: fan-out parallel calls where possible (A calls B and C simultaneously if they are independent), use async messaging to decouple non-critical downstream processing, aggregate at the gateway level, or co-locate services that are always called together (which may indicate they belong in the same bounded context).","correct":"If Service A calls B, B calls C, C calls D, all synchronously, the total latency is the sum of all hops: 20ms + 15ms + 30ms + 25ms = 90ms plus overhead.","distractors":["Use the Saga pattern. In a choreography-based saga, each service publishes an event on success; downstream services listen and react, publishing their own events or compensation events on failure.","Option 1: Service A calls Service B's API at request time (synchronous).","Without the outbox pattern, a service might save data to the database and then publish an event to Kafka."]},{"id":"backend/microservices_fundamentals#32938c09e3d3","section":"backend","module":"backend/microservices_fundamentals","moduleName":"microservices fundamentals","qIndex":14,"difficulty":"advanced","question":"What is the outbox pattern and why is it needed?","answerFull":"Without the outbox pattern, a service might save data to the database and then publish an event to Kafka. If the application crashes between the two operations, the data is saved but the event is never published — downstream services never learn of the change. The outbox pattern solves this by writing the event to an \"outbox\" table in the same local database transaction as the domain change. A separate relay process reads the outbox and publishes to Kafka. The relay guarantees at-least-once delivery. Consumers must be idempotent.","correct":"Without the outbox pattern, a service might save data to the database and then publish an event to Kafka.","distractors":["The strangler fig pattern migrates a monolith incrementally by routing new or extracted features behind an API gateway to new microservices, while the monolith handles remaining features.","Each service owns its own database and no other service accesses it directly.","Use the Saga pattern. In a choreography-based saga, each service publishes an event on success; downstream services listen and react, publishing their own events or compensation events on failure."]},{"id":"backend/microservices_fundamentals#f099208ae930","section":"backend","module":"backend/microservices_fundamentals","moduleName":"microservices fundamentals","qIndex":15,"difficulty":"advanced","question":"How do you observe and debug a microservices system in production?","answerFull":"Three pillars: logs, metrics, traces. Logs: structured JSON logs with correlation ID (X-Correlation-ID) and service name in every line, aggregated to a central store (ELK, Loki). Metrics: each service exposes /actuator/prometheus; Prometheus scrapes and Grafana dashboards show latency, error rate, and throughput per service. Traces: Micrometer Tracing or OpenTelemetry generates a trace ID for each request, propagated to all downstream services via headers. Jaeger or Zipkin visualizes the full call tree with timing. Without all three, debugging a multi-service latency issue is nearly impossible.","correct":"Three pillars: logs, metrics, traces.","distractors":["The strangler fig pattern migrates a monolith incrementally by routing new or extracted features behind an API gateway to new microservices, while the monolith handles remaining features.","A bounded context is a boundary within which a domain model is consistent and unambiguous.","A distributed monolith is a system deployed as separate services but tightly coupled via synchronous call chains, shared databases, or shared code libraries."]},{"id":"backend/observability_and_monitoring#a9228e4d8d84","section":"backend","module":"backend/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":1,"difficulty":"core","question":"What are the three pillars of observability and how do they differ?","answerFull":"Metrics are aggregated numeric measurements (counters, gauges, histograms) with low cardinality labels — cheap to store, fast to query, but lose per-request detail. Logs are discrete timestamped events with arbitrary fields — rich detail but expensive to index and query at scale. Distributed traces track a single request's path through multiple services — essential for latency attribution but require context propagation and sampling to be practical. The key is to use all three together: metrics for alerting, traces for diagnosis, logs for detailed investigation.","correct":"Metrics are aggregated numeric measurements (counters, gauges, histograms) with low cardinality labels — cheap to store, fast to query, but lose per-request detail.","distractors":["Track consumer lag via kafka.consumer.fetch-manager-metrics exposed through Micrometer's Kafka metrics binder.","Elasticsearch indexes every field of every log entry — enabling fast full-text search on any field.","OpenTelemetry (OTel) is a vendor-neutral CNCF project providing a single SDK for metrics, traces, and logs that can export to any observability backend (Prometheus, Jaeger, Datadog, etc.)."]},{"id":"backend/observability_and_monitoring#ce324c4ebd45","section":"backend","module":"backend/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":2,"difficulty":"core","question":"Why is cardinality so important in metrics?","answerFull":"Each unique combination of label values creates a separate time series. A metric with labels {status=\"200\", method=\"GET\", userId=\"12345\"} creates one series per user. With 1 million users, that's 1 million time series. Prometheus stores each series in memory; high cardinality exhausts heap, increases scrape latency, and can crash the TSDB. Solution: only use low-cardinality, finite-set values as metric labels. High-cardinality values belong in trace span attributes or structured log fields.","correct":"Each unique combination of label values creates a separate time series.","distractors":["OpenTelemetry (OTel) is a vendor-neutral CNCF project providing a single SDK for metrics, traces, and logs that can export to any observability backend (Prometheus, Jaeger, Datadog, etc.).","Track consumer lag via kafka.consumer.fetch-manager-metrics exposed through Micrometer's Kafka metrics binder.","Metrics are aggregated numeric measurements (counters, gauges, histograms) with low cardinality labels — cheap to store, fast to query, but lose per-request detail."]},{"id":"backend/observability_and_monitoring#a6e5348e62d4","section":"backend","module":"backend/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":3,"difficulty":"core","question":"What is the difference between head-based and tail-based sampling in distributed tracing?","answerFull":"Head-based sampling makes the decision at the trace root (the first span): sample this trace or not. It has low overhead since downstream services inherit the decision. However, it samples blindly — a 1% rate may miss the one slow request in 1000. Tail-based sampling buffers the entire trace and makes the decision after all spans arrive: always keep error traces and traces over p99 latency threshold, sample normal traces at 1%. It has higher memory cost but ensures important traces are always retained. Jaeger Collector supports tail-based sampling via the adaptive sampler.","correct":"Head-based sampling makes the decision at the trace root (the first span): sample this trace or not.","distractors":["The trace context (trace ID, span ID, sampling flag) is injected into outgoing requests as HTTP headers.","When a service publishes to Kafka, it injects the trace context into Kafka message headers using OpenTelemetry's TextMapSetter.","TRACE: every method entry/exit, variable values — development only, never production (too verbose)."]},{"id":"backend/observability_and_monitoring#8c2fd92c90c0","section":"backend","module":"backend/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":4,"difficulty":"core","question":"How do you propagate trace context across service boundaries?","answerFull":"The trace context (trace ID, span ID, sampling flag) is injected into outgoing requests as HTTP headers. B3 propagation uses X-B3-TraceId, X-B3-SpanId, X-B3-ParentSpanId, X-B3-Sampled. W3C TraceContext (standardized) uses a single traceparent: 00-{traceId}-{parentSpanId}-{flags} header. OpenTelemetry Java auto-instrumentation handles propagation automatically for RestTemplate, WebClient, HttpClient, and Kafka. For custom transports, use W3CTraceContextPropagator.inject(context, carrier, setter) manually.","correct":"The trace context (trace ID, span ID, sampling flag) is injected into outgoing requests as HTTP headers.","distractors":["MDC is ThreadLocal, so it does not propagate across thread switches automatically.","TRACE: every method entry/exit, variable values — development only, never production (too verbose).","When a service publishes to Kafka, it injects the trace context into Kafka message headers using OpenTelemetry's TextMapSetter."]},{"id":"backend/observability_and_monitoring#fe3fdd8dfb06","section":"backend","module":"backend/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":5,"difficulty":"core","question":"What is the difference between SLI, SLO, and SLA?","answerFull":"SLI (Service Level Indicator) is the actual measured metric — e.g., 99.95% of requests returned 2xx status in the last 30 days. SLO (Service Level Objective) is the internal target — e.g., maintain 99.9% success rate. SLA (Service Level Agreement) is the contractual commitment with customers, with defined remedies for violations — e.g., if monthly availability drops below 99.5%, customers receive a 25% service credit. SLOs should be stricter than SLAs to give an error budget buffer. The error budget (1 - SLO = 0.1%) determines how aggressively you can deploy changes.","correct":"SLI (Service Level Indicator) is the actual measured metric — e.g., 99.95% of requests returned 2xx status in the last 30 days.","distractors":["The trace context (trace ID, span ID, sampling flag) is injected into outgoing requests as HTTP headers.","When a service publishes to Kafka, it injects the trace context into Kafka message headers using OpenTelemetry's TextMapSetter.","Avoid using averages — a p50 latency of 50ms looks fine even if p99 is 5 seconds (5% of users experience 5s latency)."]},{"id":"backend/observability_and_monitoring#7ed8e962e246","section":"backend","module":"backend/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":6,"difficulty":"intermediate","question":"How do you implement MDC-based correlation ID propagation across async boundaries?","answerFull":"MDC is ThreadLocal, so it does not propagate across thread switches automatically. When using CompletableFuture.supplyAsync() or @Async, the new thread has a blank MDC. Solution: copy MDC context explicitly. Create a wrapper: Map<String, String> context = MDC.getCopyOfContextMap(); CompletableFuture.supplyAsync(() -> { MDC.setContextMap(context); try { return doWork(); } finally { MDC.clear(); } }). Spring's TaskDecorator interface allows configuring a thread pool to automatically copy MDC: implement TaskDecorator and set it on the ThreadPoolTaskExecutor.","correct":"MDC is ThreadLocal, so it does not propagate across thread switches automatically.","distractors":["Head-based sampling makes the decision at the trace root (the first span): sample this trace or not.","Avoid using averages — a p50 latency of 50ms looks fine even if p99 is 5 seconds (5% of users experience 5s latency).","Each unique combination of label values creates a separate time series."]},{"id":"backend/observability_and_monitoring#8f39bc1fcf95","section":"backend","module":"backend/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":7,"difficulty":"intermediate","question":"What is the coordinated omission problem in performance monitoring?","answerFull":"When measuring latency, if the system under test is too slow, the measurement tool may wait before sending the next request. This makes the system appear faster than it is because slow requests prevent new requests from piling up. Real users do not wait — they keep arriving regardless. Tools like wrk2 and Gatling use a constant arrival rate to avoid this. When analyzing latency percentiles from load tests, verify the test tool was sending at a constant rate, not waiting for each response before sending the next.","correct":"When measuring latency, if the system under test is too slow, the measurement tool may wait before sending the next request.","distractors":["TRACE: every method entry/exit, variable values — development only, never production (too verbose).","Avoid using averages — a p50 latency of 50ms looks fine even if p99 is 5 seconds (5% of users experience 5s latency).","Metrics are aggregated numeric measurements (counters, gauges, histograms) with low cardinality labels — cheap to store, fast to query, but lose per-request detail."]},{"id":"backend/observability_and_monitoring#8497fc8d56c4","section":"backend","module":"backend/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":8,"difficulty":"intermediate","question":"How would you instrument a Kafka consumer for observability?","answerFull":"Track consumer lag via kafka.consumer.fetch-manager-metrics exposed through Micrometer's Kafka metrics binder. Alert when records-lag-max exceeds threshold (e.g., > 10000 messages). For tracing, extract the B3/W3C trace context from Kafka message headers using TextMapGetter and create a child span per message. For logging, put the topic, partition, offset, and key in MDC. Monitor: messages consumed per second, processing duration per message (Timer), error rate (Counter), and consumer group rebalances.","correct":"Track consumer lag via kafka.consumer.fetch-manager-metrics exposed through Micrometer's Kafka metrics binder.","distractors":["OpenTelemetry (OTel) is a vendor-neutral CNCF project providing a single SDK for metrics, traces, and logs that can export to any observability backend (Prometheus, Jaeger, Datadog, etc.).","When a service publishes to Kafka, it injects the trace context into Kafka message headers using OpenTelemetry's TextMapSetter.","Metrics are aggregated numeric measurements (counters, gauges, histograms) with low cardinality labels — cheap to store, fast to query, but lose per-request detail."]},{"id":"backend/observability_and_monitoring#ce399aa116ff","section":"backend","module":"backend/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":9,"difficulty":"intermediate","question":"What log level should you use for which scenarios?","answerFull":"TRACE: every method entry/exit, variable values — development only, never production (too verbose). DEBUG: conditional decision points, cache hit/miss, DB query parameters — can enable in production temporarily for debugging with dynamic log level via /actuator/loggers. INFO: meaningful business events (order created, user logged in, payment processed) — always on in production. WARN: unexpected but handled situations (retry attempt, degraded mode, deprecated API call) — always on. ERROR: unexpected failures requiring attention (unhandled exception, external service down, data corruption) — triggers alerts. Never log sensitive data (passwords, tokens, PII) at any level.","correct":"TRACE: every method entry/exit, variable values — development only, never production (too verbose).","distractors":["Elasticsearch indexes every field of every log entry — enabling fast full-text search on any field.","When a service publishes to Kafka, it injects the trace context into Kafka message headers using OpenTelemetry's TextMapSetter.","The trace context (trace ID, span ID, sampling flag) is injected into outgoing requests as HTTP headers."]},{"id":"backend/observability_and_monitoring#3c9bdb166194","section":"backend","module":"backend/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":10,"difficulty":"intermediate","question":"How does distributed tracing work with async messaging (Kafka)?","answerFull":"When a service publishes to Kafka, it injects the trace context into Kafka message headers using OpenTelemetry's TextMapSetter. When the consumer reads the message, it extracts the context using TextMapGetter and creates a new root span with a FOLLOWS_FROM link to the producer span (not a parent-child relationship, since the consumer may run minutes later). This links the producer and consumer traces in the UI for async correlation. Micrometer Tracing handles this automatically with Spring Kafka when using the OTel bridge.","correct":"When a service publishes to Kafka, it injects the trace context into Kafka message headers using OpenTelemetry's TextMapSetter.","distractors":["The trace context (trace ID, span ID, sampling flag) is injected into outgoing requests as HTTP headers.","Track consumer lag via kafka.consumer.fetch-manager-metrics exposed through Micrometer's Kafka metrics binder.","Head-based sampling makes the decision at the trace root (the first span): sample this trace or not."]},{"id":"backend/observability_and_monitoring#c260be268c4c","section":"backend","module":"backend/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":11,"difficulty":"advanced","question":"What is the difference between Grafana Loki and Elasticsearch for logs?","answerFull":"Elasticsearch indexes every field of every log entry — enabling fast full-text search on any field. This makes it powerful but expensive: indexing overhead, high disk usage (inverted index), and significant memory. Loki only indexes configured labels (service, environment, log level) and stores log content as compressed chunks. Querying log content requires scanning compressed chunks (slower for regex across large volumes). Loki is 10x cheaper to operate than Elasticsearch for log aggregation where most queries filter by label first. Choose Elasticsearch for compliance log search (complex queries), Loki for operational troubleshooting (label-filtered queries).","correct":"Elasticsearch indexes every field of every log entry — enabling fast full-text search on any field.","distractors":["OpenTelemetry (OTel) is a vendor-neutral CNCF project providing a single SDK for metrics, traces, and logs that can export to any observability backend (Prometheus, Jaeger, Datadog, etc.).","Metrics are aggregated numeric measurements (counters, gauges, histograms) with low cardinality labels — cheap to store, fast to query, but lose per-request detail.","TRACE: every method entry/exit, variable values — development only, never production (too verbose)."]},{"id":"backend/observability_and_monitoring#8b634c2a2ae5","section":"backend","module":"backend/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":12,"difficulty":"advanced","question":"How do you avoid creating dashboards that look healthy during incidents?","answerFull":"Avoid using averages — a p50 latency of 50ms looks fine even if p99 is 5 seconds (5% of users experience 5s latency). Always show p95 and p99. Avoid success-rate dashboards that aggregate across services — a healthy service can mask a broken one. Show error rates per service and endpoint. Include the error budget burn rate — if the error budget is burning 3x faster than expected, alert before the SLO is breached. Use multi-window burn rate alerts (short window for fast detection, long window for sustained burn) per the Google SRE workbook.","correct":"Avoid using averages — a p50 latency of 50ms looks fine even if p99 is 5 seconds (5% of users experience 5s latency).","distractors":["When measuring latency, if the system under test is too slow, the measurement tool may wait before sending the next request.","Each unique combination of label values creates a separate time series.","Elasticsearch indexes every field of every log entry — enabling fast full-text search on any field."]},{"id":"backend/observability_and_monitoring#d5828aed5675","section":"backend","module":"backend/observability_and_monitoring","moduleName":"observability and monitoring","qIndex":13,"difficulty":"advanced","question":"What is OpenTelemetry and why was it created?","answerFull":"OpenTelemetry (OTel) is a vendor-neutral CNCF project providing a single SDK for metrics, traces, and logs that can export to any observability backend (Prometheus, Jaeger, Datadog, etc.). Before OTel, each vendor had its own SDK — switching from Jaeger to Zipkin required code changes. OTel's auto-instrumentation Java agent instruments popular frameworks (Spring MVC, Kafka, JDBC, gRPC, Redis) with zero code changes using bytecode manipulation. The OTel Collector acts as a pipeline: receives OTLP from services, processes (batch, filter, enrich), exports to multiple backends simultaneously.","correct":"OpenTelemetry (OTel) is a vendor-neutral CNCF project providing a single SDK for metrics, traces, and logs that can export to any observability backend (Prometheus, Jaeger, Datadog, etc.).","distractors":["When a service publishes to Kafka, it injects the trace context into Kafka message headers using OpenTelemetry's TextMapSetter.","Track consumer lag via kafka.consumer.fetch-manager-metrics exposed through Micrometer's Kafka metrics binder.","Metrics are aggregated numeric measurements (counters, gauges, histograms) with low cardinality labels — cheap to store, fast to query, but lose per-request detail."]},{"id":"backend/osi_model_and_networking#0be2aeccec78","section":"backend","module":"backend/osi_model_and_networking","moduleName":"osi model and networking","qIndex":2,"difficulty":"core","question":"What is the difference between a hub, switch, and router?","answerFull":"A hub is a Layer 1 device — it broadcasts all traffic to all ports. A switch is a Layer 2 device — it forwards frames only to the MAC address destination, learning the MAC-to-port mapping. A router is a Layer 3 device — it forwards packets between different networks using IP routing tables.","correct":"A hub is a Layer 1 device — it broadcasts all traffic to all ports.","distractors":["A VLAN is a Layer 2 logical segmentation — it separates broadcast domains on the same physical switch infrastructure using 802.1Q tags in Ethernet frames.","The Content Addressable Memory (CAM) table maps MAC addresses to switch ports.","A Layer 4 LB routes TCP/UDP flows based on IP:port without inspecting payload — faster and protocol-agnostic."]},{"id":"backend/osi_model_and_networking#75164433e27c","section":"backend","module":"backend/osi_model_and_networking","moduleName":"osi model and networking","qIndex":3,"difficulty":"core","question":"How does ARP work and what is a gratuitous ARP?","answerFull":"ARP resolves an IP address to a MAC address within a subnet. A host broadcasts \"who has IP X?\" and the owner replies with its MAC. A gratuitous ARP is an unsolicited ARP reply announcing an IP-to-MAC mapping — used by hosts to update ARP caches during IP changes (e.g., failover) or by attackers for ARP poisoning.","correct":"ARP resolves an IP address to a MAC address within a subnet.","distractors":["Unicast: one sender, one receiver (specific MAC/IP).","NAT (Network Address Translation) allows multiple internal hosts to share a single public IP by maintaining a port-mapping table.","The Content Addressable Memory (CAM) table maps MAC addresses to switch ports."]},{"id":"backend/osi_model_and_networking#50dce0f5f04f","section":"backend","module":"backend/osi_model_and_networking","moduleName":"osi model and networking","qIndex":4,"difficulty":"core","question":"What is MTU and why does it matter for backend services?","answerFull":"MTU is the maximum frame size a link can carry. Ethernet MTU is 1500 bytes. TCP MSS is 1460 bytes (1500 - 20 IP - 20 TCP). If PMTUD is broken (ICMP filtered), large TCP transfers silently hang. Backend services doing bulk data transfers should verify MTU discovery works end-to-end.","correct":"MTU is the maximum frame size a link can carry.","distractors":["When an IP packet exceeds the MTU of the outgoing link, IP fragments it into multiple smaller packets (if the DF bit is not set).","A Layer 4 LB routes TCP/UDP flows based on IP:port without inspecting payload — faster and protocol-agnostic.","A VLAN is a Layer 2 logical segmentation — it separates broadcast domains on the same physical switch infrastructure using 802.1Q tags in Ethernet frames."]},{"id":"backend/osi_model_and_networking#78511a051b09","section":"backend","module":"backend/osi_model_and_networking","moduleName":"osi model and networking","qIndex":5,"difficulty":"core","question":"What is the difference between a Layer 4 and Layer 7 load balancer?","answerFull":"A Layer 4 LB routes TCP/UDP flows based on IP:port without inspecting payload — faster and protocol-agnostic. A Layer 7 LB parses the application protocol (HTTP headers, URLs, cookies) — enables intelligent routing but has higher CPU overhead. Use Layer 7 for HTTP services needing content-based routing or SSL termination.","correct":"A Layer 4 LB routes TCP/UDP flows based on IP:port without inspecting payload — faster and protocol-agnostic.","distractors":["NAT (Network Address Translation) allows multiple internal hosts to share a single public IP by maintaining a port-mapping table.","BGP (Border Gateway Protocol) is the routing protocol that runs the internet — it exchanges reachability information between autonomous systems (large networks like ISPs, cloud providers).","A VLAN is a Layer 2 logical segmentation — it separates broadcast domains on the same physical switch infrastructure using 802.1Q tags in Ethernet frames."]},{"id":"backend/osi_model_and_networking#5c22d3ccf14b","section":"backend","module":"backend/osi_model_and_networking","moduleName":"osi model and networking","qIndex":6,"difficulty":"intermediate","question":"How does DNS resolution work, and what is a DNS TTL?","answerFull":"A client queries a recursive resolver, which follows the chain: root → TLD → authoritative nameserver. The authoritative server returns the record with a TTL. The resolver caches the result for TTL seconds. Clients should not cache TTLs longer than the record specifies. Before migration, lower TTL to 60–300s to enable fast cutover.","correct":"A client queries a recursive resolver, which follows the chain: root → TLD → authoritative nameserver.","distractors":["ICMP carries control messages: ping (Echo Request/Reply), traceroute (TTL Exceeded), and — critically for backends — Fragmentation Needed (Type 3 Code 4) for PMTUD.","Traceroute sends UDP or ICMP packets with incrementing TTL values (1, 2, 3, ...).","A Layer 4 LB routes TCP/UDP flows based on IP:port without inspecting payload — faster and protocol-agnostic."]},{"id":"backend/osi_model_and_networking#4f1c26b7310a","section":"backend","module":"backend/osi_model_and_networking","moduleName":"osi model and networking","qIndex":7,"difficulty":"intermediate","question":"What is IP fragmentation and when does it occur?","answerFull":"When an IP packet exceeds the MTU of the outgoing link, IP fragments it into multiple smaller packets (if the DF bit is not set). Reassembly happens at the destination. Fragmentation is expensive and fragmented UDP is common in DNS over UDP (responses >512 bytes, though EDNS0 allows up to ~4096 bytes).","correct":"When an IP packet exceeds the MTU of the outgoing link, IP fragments it into multiple smaller packets (if the DF bit is not set).","distractors":["ICMP carries control messages: ping (Echo Request/Reply), traceroute (TTL Exceeded), and — critically for backends — Fragmentation Needed (Type 3 Code 4) for PMTUD.","Traceroute sends UDP or ICMP packets with incrementing TTL values (1, 2, 3, ...).","NAT (Network Address Translation) allows multiple internal hosts to share a single public IP by maintaining a port-mapping table."]},{"id":"backend/osi_model_and_networking#0afd8776173c","section":"backend","module":"backend/osi_model_and_networking","moduleName":"osi model and networking","qIndex":8,"difficulty":"intermediate","question":"What is NAT and what are its limitations?","answerFull":"NAT (Network Address Translation) allows multiple internal hosts to share a single public IP by maintaining a port-mapping table. Limitations: breaks protocols that embed IP addresses in payload (FTP active mode, SIP), connection table exhaustion under high load, complicates peer-to-peer connectivity, prevents unsolicited inbound connections (which is also a benefit).","correct":"NAT (Network Address Translation) allows multiple internal hosts to share a single public IP by maintaining a port-mapping table.","distractors":["When an IP packet exceeds the MTU of the outgoing link, IP fragments it into multiple smaller packets (if the DF bit is not set).","A Layer 4 LB routes TCP/UDP flows based on IP:port without inspecting payload — faster and protocol-agnostic.","The Content Addressable Memory (CAM) table maps MAC addresses to switch ports."]},{"id":"backend/osi_model_and_networking#289ae0f23dc5","section":"backend","module":"backend/osi_model_and_networking","moduleName":"osi model and networking","qIndex":9,"difficulty":"intermediate","question":"What is ICMP and why should backends care about it?","answerFull":"ICMP carries control messages: ping (Echo Request/Reply), traceroute (TTL Exceeded), and — critically for backends — Fragmentation Needed (Type 3 Code 4) for PMTUD. Blocking all ICMP at firewalls breaks PMTUD, causing mysterious connection hangs for large payloads. Only block ICMP flood attacks, not diagnostic ICMP.","correct":"ICMP carries control messages: ping (Echo Request/Reply), traceroute (TTL Exceeded), and — critically for backends — Fragmentation Needed (Type 3 Code 4) for PMTUD.","distractors":["Traceroute sends UDP or ICMP packets with incrementing TTL values (1, 2, 3, ...).","A Layer 4 LB routes TCP/UDP flows based on IP:port without inspecting payload — faster and protocol-agnostic.","A VLAN is a Layer 2 logical segmentation — it separates broadcast domains on the same physical switch infrastructure using 802.1Q tags in Ethernet frames."]},{"id":"backend/osi_model_and_networking#75778523c0d7","section":"backend","module":"backend/osi_model_and_networking","moduleName":"osi model and networking","qIndex":10,"difficulty":"intermediate","question":"What is a VLAN and how does it differ from a subnet?","answerFull":"A VLAN is a Layer 2 logical segmentation — it separates broadcast domains on the same physical switch infrastructure using 802.1Q tags in Ethernet frames. A subnet is a Layer 3 construct — an IP address range. VLANs typically correspond to subnets but they are distinct concepts. Traffic between VLANs must pass through a Layer 3 router or firewall.","correct":"A VLAN is a Layer 2 logical segmentation — it separates broadcast domains on the same physical switch infrastructure using 802.1Q tags in Ethernet frames.","distractors":["ARP resolves an IP address to a MAC address within a subnet.","The Content Addressable Memory (CAM) table maps MAC addresses to switch ports.","A Layer 4 LB routes TCP/UDP flows based on IP:port without inspecting payload — faster and protocol-agnostic."]},{"id":"backend/osi_model_and_networking#47424e46d168","section":"backend","module":"backend/osi_model_and_networking","moduleName":"osi model and networking","qIndex":11,"difficulty":"advanced","question":"What is the difference between unicast, multicast, and broadcast?","answerFull":"Unicast: one sender, one receiver (specific MAC/IP). Broadcast: one sender, all receivers in a segment (MAC: FF:FF:FF:FF:FF:FF, IP: 255.255.255.255 or subnet broadcast). Multicast: one sender, a group of receivers that have subscribed (MAC range 01:00:5E:xx:xx:xx, IP range 224.0.0.0–239.255.255.255). Switches flood broadcasts; multicast requires IGMP snooping to avoid flooding.","correct":"Unicast: one sender, one receiver (specific MAC/IP).","distractors":["A VLAN is a Layer 2 logical segmentation — it separates broadcast domains on the same physical switch infrastructure using 802.1Q tags in Ethernet frames.","The Content Addressable Memory (CAM) table maps MAC addresses to switch ports.","ARP resolves an IP address to a MAC address within a subnet."]},{"id":"backend/osi_model_and_networking#ae2093ec5081","section":"backend","module":"backend/osi_model_and_networking","moduleName":"osi model and networking","qIndex":12,"difficulty":"advanced","question":"How does traceroute work?","answerFull":"Traceroute sends UDP or ICMP packets with incrementing TTL values (1, 2, 3, ...). Each router that decrements TTL to 0 sends back an ICMP \"TTL Exceeded\" message revealing its IP. This maps the path. Each hop typically shows 3 probes for RTT measurement. Firewalls that block ICMP cause \"* * *\" in the output.","correct":"Traceroute sends UDP or ICMP packets with incrementing TTL values (1, 2, 3, ...).","distractors":["ICMP carries control messages: ping (Echo Request/Reply), traceroute (TTL Exceeded), and — critically for backends — Fragmentation Needed (Type 3 Code 4) for PMTUD.","When an IP packet exceeds the MTU of the outgoing link, IP fragments it into multiple smaller packets (if the DF bit is not set).","A Layer 4 LB routes TCP/UDP flows based on IP:port without inspecting payload — faster and protocol-agnostic."]},{"id":"backend/osi_model_and_networking#b213da075058","section":"backend","module":"backend/osi_model_and_networking","moduleName":"osi model and networking","qIndex":13,"difficulty":"advanced","question":"What is the CAM table on a network switch?","answerFull":"The Content Addressable Memory (CAM) table maps MAC addresses to switch ports. When a frame arrives, the switch records source MAC → incoming port. When forwarding, it looks up destination MAC. If not found, it floods the frame out all ports except the incoming one. CAM table overflow (via MAC flooding attack) causes the switch to flood all traffic like a hub.","correct":"The Content Addressable Memory (CAM) table maps MAC addresses to switch ports.","distractors":["Unicast: one sender, one receiver (specific MAC/IP).","A hub is a Layer 1 device — it broadcasts all traffic to all ports.","NAT (Network Address Translation) allows multiple internal hosts to share a single public IP by maintaining a port-mapping table."]},{"id":"backend/osi_model_and_networking#39d6f266c13a","section":"backend","module":"backend/osi_model_and_networking","moduleName":"osi model and networking","qIndex":14,"difficulty":"advanced","question":"What is BGP and why does it matter for backend engineers?","answerFull":"BGP (Border Gateway Protocol) is the routing protocol that runs the internet — it exchanges reachability information between autonomous systems (large networks like ISPs, cloud providers). Backend engineers care because anycast routing (used by CDNs, DNS providers, Cloudflare) uses BGP to route requests to the geographically nearest node sharing the same IP prefix.","correct":"BGP (Border Gateway Protocol) is the routing protocol that runs the internet — it exchanges reachability information between autonomous systems (large networks like ISPs, cloud providers).","distractors":["A Layer 4 LB routes TCP/UDP flows based on IP:port without inspecting payload — faster and protocol-agnostic.","A VLAN is a Layer 2 logical segmentation — it separates broadcast domains on the same physical switch infrastructure using 802.1Q tags in Ethernet frames.","A client queries a recursive resolver, which follows the chain: root → TLD → authoritative nameserver."]},{"id":"backend/performance_profiling#427a1465d5e7","section":"backend","module":"backend/performance_profiling","moduleName":"performance profiling","qIndex":1,"difficulty":"core","question":"How do you find the cause of high CPU usage in a Java application?","answerFull":"Take a 30-60 second CPU profile with async-profiler (-e cpu). Generate a flamegraph. The widest frames near the top of stacks are the CPU hotspots. Look for business logic (your code) vs framework code vs GC. If GC is wide, investigate allocation rate. If serialization or parsing is wide, optimize the data format. If a specific method is wide, inspect it for inefficiencies (unnecessary object creation, redundant computation, unoptimized algorithms).","correct":"Take a 30-60 second CPU profile with async-profiler (-e cpu).","distractors":["High CPU utilization without high throughput (CPU-intensive processing).","Take a thread dump with jstack.","(1) Take a heap histogram: jcmd <pid> GC.heap_info or jmap -histo:live <pid>."]},{"id":"backend/performance_profiling#1b055617088f","section":"backend","module":"backend/performance_profiling","moduleName":"performance profiling","qIndex":2,"difficulty":"core","question":"What is safepoint bias and why does it matter?","answerFull":"Traditional JVM profilers (VisualVM, JProfiler with some modes) take stack samples only at JVM safepoints — points where the JVM can pause all threads (method boundaries, loop back-edges, before allocations). Tight loops without safepoints appear to consume zero CPU in these profiles. async-profiler uses AsyncGetCallTrace which can sample at any point in execution, including inside tight loops. Safepoint bias matters when you have CPU-intensive tight loops — traditional profilers will incorrectly show them as not consuming CPU.","correct":"Traditional JVM profilers (VisualVM, JProfiler with some modes) take stack samples only at JVM safepoints — points where the JVM can pause all threads (method boundaries, loop back-edges, before allocations).","distractors":["Take a 30-60 second CPU profile with async-profiler (-e cpu).","(1) Take a heap histogram: jcmd <pid> GC.heap_info or jmap -histo:live <pid>.","CPU profiling (-e cpu) samples threads only when they are running on CPU (RUNNABLE state)."]},{"id":"backend/performance_profiling#f6c517650d8e","section":"backend","module":"backend/performance_profiling","moduleName":"performance profiling","qIndex":3,"difficulty":"core","question":"How do you diagnose a memory leak in a Java application?","answerFull":"(1) Take a heap histogram: jcmd <pid> GC.heap_info or jmap -histo:live <pid>. Look for unexpected object counts growing over time. (2) Compare two heap histograms taken minutes apart — objects growing are suspects. (3) Take a full heap dump and analyze in Eclipse MAT: the Dominator Tree shows which objects hold the most memory; Leak Suspects report automates analysis. (4) Look for classic patterns: static collections growing, ThreadLocal not removed, listeners not unregistered, session objects accumulating.","correct":"(1) Take a heap histogram: jcmd <pid> GC.heap_info or jmap -histo:live <pid>.","distractors":["Take a 30-60 second CPU profile with async-profiler (-e cpu).","async-profiler is a low-overhead profiler for Java that uses AsyncGetCallTrace (a JVM internal API) and Linux perf_events to sample call stacks.","Take a thread dump with jstack."]},{"id":"backend/performance_profiling#ebe13a69b4be","section":"backend","module":"backend/performance_profiling","moduleName":"performance profiling","qIndex":4,"difficulty":"core","question":"How do you read a flamegraph?","answerFull":"The x-axis represents time (or sample count) — width means how much time was spent. The y-axis is call stack depth — bottom is the thread entry point, top is the currently executing method. Wide frames near the top are hot methods. To find the bottleneck: identify the widest frame in the visible stack area. If many narrow top frames converge to a wide bottom frame, the bottleneck is that common ancestor. Colors (in async-profiler) represent categories: yellow = Java code, green = C++ JVM code, red = kernel/native.","correct":"The x-axis represents time (or sample count) — width means how much time was spent.","distractors":["async-profiler is a low-overhead profiler for Java that uses AsyncGetCallTrace (a JVM internal API) and Linux perf_events to sample call stacks.","JDK 17+: -Xlog:gc*:file=/tmp/gc.log:time,uptime,level,tags:filecount=10,filesize=20m.","(1) Check GC logs for the spike: what GC event type (Young, Mixed, Full)?"]},{"id":"backend/performance_profiling#52f72377f8e0","section":"backend","module":"backend/performance_profiling","moduleName":"performance profiling","qIndex":5,"difficulty":"core","question":"What are the JVM flags for GC logging in production?","answerFull":"JDK 17+: -Xlog:gc*:file=/tmp/gc.log:time,uptime,level,tags:filecount=10,filesize=20m. This logs all GC events with timestamps, rotates after 20MB, keeping 10 files. Key events to watch: Pause duration (> MaxGCPauseMillis is a warning), Full GC events (severe latency impact), heap occupancy after GC (approaching heap limit means memory pressure). For G1: also log -Xlog:gc+phases* for detailed phase breakdown.","correct":"JDK 17+: -Xlog:gc*:file=/tmp/gc.log:time,uptime,level,tags:filecount=10,filesize=20m.","distractors":["Traditional JVM profilers (VisualVM, JProfiler with some modes) take stack samples only at JVM safepoints — points where the JVM can pause all threads (method boundaries, loop back-edges, before allocations).","async-profiler is a low-overhead profiler for Java that uses AsyncGetCallTrace (a JVM internal API) and Linux perf_events to sample call stacks.","The x-axis represents time (or sample count) — width means how much time was spent."]},{"id":"backend/performance_profiling#4120fe3f83c4","section":"backend","module":"backend/performance_profiling","moduleName":"performance profiling","qIndex":6,"difficulty":"intermediate","question":"How do you profile memory allocation rate?","answerFull":"Use async-profiler with -e alloc: samples allocation sites, showing which code paths allocate the most. Alternatively, JFR with the ObjectAllocationInNewTLAB event records allocations. Key metrics: allocation rate (MB/s), top allocation sites, largest allocated types. A high allocation rate (>500 MB/s for typical workloads) causes frequent young GC pauses. Fix: reduce object creation in hot paths (use primitives, reuse objects, use object pools for expensive-to-create objects).","correct":"Use async-profiler with -e alloc: samples allocation sites, showing which code paths allocate the most.","distractors":["async-profiler is a low-overhead profiler for Java that uses AsyncGetCallTrace (a JVM internal API) and Linux perf_events to sample call stacks.","CPU profiling (-e cpu) samples threads only when they are running on CPU (RUNNABLE state).","Traditional JVM profilers (VisualVM, JProfiler with some modes) take stack samples only at JVM safepoints — points where the JVM can pause all threads (method boundaries, loop back-edges, before allocations)."]},{"id":"backend/performance_profiling#7ac2cd37a3f6","section":"backend","module":"backend/performance_profiling","moduleName":"performance profiling","qIndex":7,"difficulty":"intermediate","question":"What is the difference between heap dump and heap histogram?","answerFull":"A heap histogram (jmap -histo or jcmd GC.heap_info) lists object counts and sizes by class — fast (seconds), low impact. Good for initial diagnosis. A full heap dump (jmap -dump or -XX:+HeapDumpOnOutOfMemoryError) captures the entire heap with object references — can take seconds to minutes for large heaps, requires significant disk space (heap size bytes), and pauses the JVM during dump (for jmap; JFR heap dumps are asynchronous). Use histogram first; full dump for deep investigation.","correct":"A heap histogram (jmap -histo or jcmd GC.heap_info) lists object counts and sizes by class — fast (seconds), low impact.","distractors":["Take a thread dump with jstack.","async-profiler is a low-overhead profiler for Java that uses AsyncGetCallTrace (a JVM internal API) and Linux perf_events to sample call stacks.","(1) Take a heap histogram: jcmd <pid> GC.heap_info or jmap -histo:live <pid>."]},{"id":"backend/performance_profiling#6507a2a4a68e","section":"backend","module":"backend/performance_profiling","moduleName":"performance profiling","qIndex":8,"difficulty":"intermediate","question":"How do you diagnose thread contention?","answerFull":"Take a thread dump with jstack. Count threads in BLOCKED state — high BLOCKED count indicates lock contention. Multiple threads BLOCKED on the same object address indicate a hot lock. Use async-profiler with -e lock to identify which locks cause the most contention and which methods hold them. Fix: reduce synchronized scope, replace synchronized with ReentrantLock (tryLock with timeout), use concurrent data structures (ConcurrentHashMap), or striped locks (Guava Striped).","correct":"Take a thread dump with jstack.","distractors":["Traditional JVM profilers (VisualVM, JProfiler with some modes) take stack samples only at JVM safepoints — points where the JVM can pause all threads (method boundaries, loop back-edges, before allocations).","Take a 30-60 second CPU profile with async-profiler (-e cpu).","(1) Take a heap histogram: jcmd <pid> GC.heap_info or jmap -histo:live <pid>."]},{"id":"backend/performance_profiling#33dc15bf42bf","section":"backend","module":"backend/performance_profiling","moduleName":"performance profiling","qIndex":10,"difficulty":"intermediate","question":"What is async-profiler and how does it avoid safepoint bias?","answerFull":"async-profiler is a low-overhead profiler for Java that uses AsyncGetCallTrace (a JVM internal API) and Linux perf_events to sample call stacks. AsyncGetCallTrace can be called at any signal (including from a signal handler triggered by a timer), not just at JVM safepoints. This eliminates safepoint bias. async-profiler also uses perf_events to sample native and kernel frames, giving a complete picture including time spent in system calls and native libraries — which traditional JVM profilers miss entirely.","correct":"async-profiler is a low-overhead profiler for Java that uses AsyncGetCallTrace (a JVM internal API) and Linux perf_events to sample call stacks.","distractors":["Traditional JVM profilers (VisualVM, JProfiler with some modes) take stack samples only at JVM safepoints — points where the JVM can pause all threads (method boundaries, loop back-edges, before allocations).","Take a 30-60 second CPU profile with async-profiler (-e cpu).","Use async-profiler with -e alloc: samples allocation sites, showing which code paths allocate the most."]},{"id":"backend/performance_profiling#a5bc8b830843","section":"backend","module":"backend/performance_profiling","moduleName":"performance profiling","qIndex":11,"difficulty":"advanced","question":"How do you find the root cause of a garbage collection spike?","answerFull":"(1) Check GC logs for the spike: what GC event type (Young, Mixed, Full)? What was the heap occupancy before and after? (2) Full GC: old generation full — memory pressure, possibly a leak. (3) Young GC spike: high allocation rate, possibly a hot code path creating many objects. Use async-profiler -e alloc to find the allocation site. (4) Check promotion rate — if young objects are surviving to old gen quickly, the survivor spaces are too small (increase -XX:SurvivorRatio) or objects live too long. (5) Check for humongous object allocations (objects > half a G1 region, default 1 MB/2 = 512 KB) — these bypass young gen and go directly to old gen.","correct":"(1) Check GC logs for the spike: what GC event type (Young, Mixed, Full)?","distractors":["(1) Take a heap histogram: jcmd <pid> GC.heap_info or jmap -histo:live <pid>.","A heap histogram (jmap -histo or jcmd GC.heap_info) lists object counts and sizes by class — fast (seconds), low impact.","CPU profiling (-e cpu) samples threads only when they are running on CPU (RUNNABLE state)."]},{"id":"backend/performance_profiling#14c8440def41","section":"backend","module":"backend/performance_profiling","moduleName":"performance profiling","qIndex":12,"difficulty":"advanced","question":"What is the difference between wall-clock profiling and CPU profiling?","answerFull":"CPU profiling (-e cpu) samples threads only when they are running on CPU (RUNNABLE state). It shows where CPU time is spent. Wall-clock profiling (-e wall) samples all threads regardless of state — including threads waiting for I/O, blocked on locks, or sleeping. For latency investigations where a request is slow but CPU usage is low (waiting for database, waiting for network, waiting for a lock), wall-clock profiling reveals where the wall time is spent. CPU profiling would show almost nothing for a primarily I/O-bound request.","correct":"CPU profiling (-e cpu) samples threads only when they are running on CPU (RUNNABLE state).","distractors":["Traditional JVM profilers (VisualVM, JProfiler with some modes) take stack samples only at JVM safepoints — points where the JVM can pause all threads (method boundaries, loop back-edges, before allocations).","High CPU utilization without high throughput (CPU-intensive processing).","Use async-profiler with -e alloc: samples allocation sites, showing which code paths allocate the most."]},{"id":"backend/performance_profiling#d4b06fffa757","section":"backend","module":"backend/performance_profiling","moduleName":"performance profiling","qIndex":13,"difficulty":"advanced","question":"How do you detect and fix high allocation pressure?","answerFull":"Detection: async-profiler -e alloc shows top allocation sites. JFR's jdk.ObjectAllocationInNewTLAB shows allocations per code path. GC frequency > once every 5 seconds for a typical service indicates high allocation. Fix strategies: (1) Object pooling for frequently created/destroyed objects (e.g., StringBuilder, ByteBuffer, database result objects). (2) Replace boxed types with primitives in hot paths (Integer → int, HashMap<Long,Long> → use a primitive map library). (3) Reduce intermediate object creation in streams/lambdas (use traditional for-loops for allocation-sensitive code). (4) Escape analysis: the JVM may already eliminate allocations (stack allocation) for short-lived objects — check with JFR AllocationInOldGen event.","correct":"Detection: async-profiler -e alloc shows top allocation sites.","distractors":["async-profiler is a low-overhead profiler for Java that uses AsyncGetCallTrace (a JVM internal API) and Linux perf_events to sample call stacks.","High CPU utilization without high throughput (CPU-intensive processing).","Use async-profiler with -e alloc: samples allocation sites, showing which code paths allocate the most."]},{"id":"backend/performance_profiling#ab0c0013e9a7","section":"backend","module":"backend/performance_profiling","moduleName":"performance profiling","qIndex":14,"difficulty":"advanced","question":"What production signals indicate a need for profiling?","answerFull":"High CPU utilization without high throughput (CPU-intensive processing). Increasing heap usage over time (memory leak). GC throughput < 95% (too much time in GC). Latency p99 >> p50 (long-tail latency from GC pauses, lock contention, or occasional slow I/O). Thread pool rejection or queue depth growing (thread pool exhaustion or slow tasks). JVM OOM errors in logs. Service response time degrading after running for hours (heap pressure, leak, fragmentation).","correct":"High CPU utilization without high throughput (CPU-intensive processing).","distractors":["CPU profiling (-e cpu) samples threads only when they are running on CPU (RUNNABLE state).","Take a 30-60 second CPU profile with async-profiler (-e cpu).","(1) Check GC logs for the spike: what GC event type (Young, Mixed, Full)?"]},{"id":"backend/query_optimization#80ae77f7ba84","section":"backend","module":"backend/query_optimization","moduleName":"query optimization","qIndex":1,"difficulty":"core","question":"How do you read a PostgreSQL EXPLAIN ANALYZE output?","answerFull":"Each line is a plan node (operation). Read from the innermost (deepest indentation) outward — inner nodes execute first. Key fields: cost=X..Y (estimated startup..total cost), rows=N (estimated), actual time=A..B ms (measured startup..total), actual rows=M. Large gap between rows=N (estimated) and actual rows=M indicates outdated statistics. The widest actual time lines are bottlenecks. Look for Seq Scan on large tables (should be Index Scan for high-selectivity queries), Sort with Disk methods (sort exceeded work_mem), and Hash Join with memory pressure.","correct":"Each line is a plan node (operation).","distractors":["PostgreSQL: (1) Enable pg_stat_statements — aggregates all executed queries with total time, execution count, and average time.","Best performance: COPY (PostgreSQL) or LOAD DATA INFILE (MySQL) — bypasses row-by-row WAL overhead.","work_mem (PostgreSQL) is the memory available per sort or hash operation per query execution."]},{"id":"backend/query_optimization#e06da4056348","section":"backend","module":"backend/query_optimization","moduleName":"query optimization","qIndex":2,"difficulty":"core","question":"What is the N+1 problem and how do you detect it in a Spring application?","answerFull":"N+1: a query fetches N entities (1 query), then for each entity fetches a related collection (N queries) = N+1 queries total. In JPA, this occurs with LAZY-loaded collections accessed outside the repository. Detection: enable hibernate.generate_statistics=true, use datasource-proxy to count queries per HTTP request, or use p6spy to log all SQL with stack traces. In tests, assert a maximum query count per operation.","correct":"N+1: a query fetches N entities (1 query), then for each entity fetches a related collection (N queries) = N+1 queries total.","distractors":["PreparedStatement separates query planning from execution.","When JOIN FETCHing multiple collections on the same entity (e.g., User FETCH JOIN orders FETCH JOIN tags), the SQL JOIN multiplies the result set: each user row is combined with each order and each tag.","Systematic approach: (1) Get the execution plan from production: EXPLAIN (ANALYZE, BUFFERS) <query>."]},{"id":"backend/query_optimization#35e13859ae66","section":"backend","module":"backend/query_optimization","moduleName":"query optimization","qIndex":3,"difficulty":"core","question":"How would you fix N+1 for a User with Orders in Spring Data JPA?","answerFull":"Option 1: @Query(\"SELECT u FROM User u LEFT JOIN FETCH u.orders WHERE u.id IN :ids\") — JOIN FETCH fetches users and orders in one query. Option 2: @EntityGraph(attributePaths = \"orders\") on the repository method. Option 3: For multiple collections (orders + tags), use separate queries with IN: fetch all users, collect IDs, SELECT o FROM Order o WHERE o.userId IN :userIds, map by userId in Java. Avoid JOIN FETCH for multiple collections simultaneously (Cartesian product).","correct":"Option 1: @Query(\"SELECT u FROM User u LEFT JOIN FETCH u.orders WHERE u.id IN :ids\") — JOIN FETCH fetches users and orders in one query.","distractors":["N+1: a query fetches N entities (1 query), then for each entity fetches a related collection (N queries) = N+1 queries total.","work_mem (PostgreSQL) is the memory available per sort or hash operation per query execution.","INNER JOIN: only returns rows where the join condition matches in both tables."]},{"id":"backend/query_optimization#3a23049b3c4c","section":"backend","module":"backend/query_optimization","moduleName":"query optimization","qIndex":4,"difficulty":"core","question":"What is the performance cliff with OFFSET pagination?","answerFull":"OFFSET N requires the database to generate all rows 0 through N+LIMIT-1 and discard 0 through N-1. For OFFSET 1,000,000 LIMIT 20, the database generates 1,000,020 rows and discards 1,000,000. Performance is O(OFFSET) — doubling the page number doubles the query time. At deep pages (export, large dataset), this becomes unbearably slow. Keyset pagination avoids this: WHERE id > last_id LIMIT 20 uses the index directly, O(1) regardless of depth.","correct":"OFFSET N requires the database to generate all rows 0 through N+LIMIT-1 and discard 0 through N-1.","distractors":["Keyset pagination uses the values from the last row of the current page as the cursor for the next page.","Best performance: COPY (PostgreSQL) or LOAD DATA INFILE (MySQL) — bypasses row-by-row WAL overhead.","INNER JOIN: only returns rows where the join condition matches in both tables."]},{"id":"backend/query_optimization#ec31a381cdab","section":"backend","module":"backend/query_optimization","moduleName":"query optimization","qIndex":5,"difficulty":"core","question":"How does keyset pagination work?","answerFull":"Keyset pagination uses the values from the last row of the current page as the cursor for the next page. For a list sorted by (created_at DESC, id DESC), the next page query is: WHERE (created_at, id) < (last_created_at, last_id) ORDER BY created_at DESC, id DESC LIMIT 20. The composite WHERE condition acts as a cursor. An index on (created_at DESC, id DESC) makes this O(1). The tradeoff: cannot jump to arbitrary pages — you can only page forward (or backward with reversed comparison).","correct":"Keyset pagination uses the values from the last row of the current page as the cursor for the next page.","distractors":["Best performance: COPY (PostgreSQL) or LOAD DATA INFILE (MySQL) — bypasses row-by-row WAL overhead.","When JOIN FETCHing multiple collections on the same entity (e.g., User FETCH JOIN orders FETCH JOIN tags), the SQL JOIN multiplies the result set: each user row is combined with each order and each tag.","work_mem (PostgreSQL) is the memory available per sort or hash operation per query execution."]},{"id":"backend/query_optimization#2eac6be7baab","section":"backend","module":"backend/query_optimization","moduleName":"query optimization","qIndex":6,"difficulty":"intermediate","question":"What JDBC patterns should you use for bulk inserts?","answerFull":"Best performance: COPY (PostgreSQL) or LOAD DATA INFILE (MySQL) — bypasses row-by-row WAL overhead. For general use: JDBC batch execute (PreparedStatement.addBatch(); executeBatch()) groups statements for fewer round trips. With Hibernate: set hibernate.jdbc.batch_size=100, hibernate.order_inserts=true, and use saveAll(). Never: loop calling save() individually — this is N round trips for N rows.","correct":"Best performance: COPY (PostgreSQL) or LOAD DATA INFILE (MySQL) — bypasses row-by-row WAL overhead.","distractors":["Use datasource-proxy or a custom JDBC connection wrapper.","Keyset pagination uses the values from the last row of the current page as the cursor for the next page.","When JOIN FETCHing multiple collections on the same entity (e.g., User FETCH JOIN orders FETCH JOIN tags), the SQL JOIN multiplies the result set: each user row is combined with each order and each tag."]},{"id":"backend/query_optimization#abd85fa753fc","section":"backend","module":"backend/query_optimization","moduleName":"query optimization","qIndex":7,"difficulty":"intermediate","question":"How does a PreparedStatement improve performance?","answerFull":"PreparedStatement separates query planning from execution. First call: prepareStatement(sql) sends the SQL to the database for parsing and planning, returning a statement handle. Subsequent executions: send only the handle and parameters — the database uses the cached plan. Benefits: (1) eliminates repeated parse and plan overhead for the same query shape; (2) prevents SQL injection (parameters cannot change the query structure). With PostgreSQL: after the 5th execution of the same prepared statement, the server switches from parameter-specific plans to generic plans that are cached more aggressively.","correct":"PreparedStatement separates query planning from execution.","distractors":["N+1: a query fetches N entities (1 query), then for each entity fetches a related collection (N queries) = N+1 queries total.","Option 1: @Query(\"SELECT u FROM User u LEFT JOIN FETCH u.orders WHERE u.id IN :ids\") — JOIN FETCH fetches users and orders in one query.","PostgreSQL: (1) Enable pg_stat_statements — aggregates all executed queries with total time, execution count, and average time."]},{"id":"backend/query_optimization#c642b5c9d0cf","section":"backend","module":"backend/query_optimization","moduleName":"query optimization","qIndex":8,"difficulty":"intermediate","question":"What is a Cartesian product in JPA and when does it occur?","answerFull":"When JOIN FETCHing multiple collections on the same entity (e.g., User FETCH JOIN orders FETCH JOIN tags), the SQL JOIN multiplies the result set: each user row is combined with each order and each tag. A user with 100 orders and 20 tags produces 100 * 20 = 2,000 rows, which Hibernate deduplicates in memory. The network traffic and memory used is 2,000 rows even though only 120 entities exist. Fix: use separate queries for each collection, connected via IN clause.","correct":"When JOIN FETCHing multiple collections on the same entity (e.g., User FETCH JOIN orders FETCH JOIN tags), the SQL JOIN multiplies the result set: each user row is combined with each order and each tag.","distractors":["Best performance: COPY (PostgreSQL) or LOAD DATA INFILE (MySQL) — bypasses row-by-row WAL overhead.","INNER JOIN: only returns rows where the join condition matches in both tables.","Keyset pagination uses the values from the last row of the current page as the cursor for the next page."]},{"id":"backend/query_optimization#6b9a3c6943dc","section":"backend","module":"backend/query_optimization","moduleName":"query optimization","qIndex":9,"difficulty":"intermediate","question":"How do you find slow queries in production?","answerFull":"PostgreSQL: (1) Enable pg_stat_statements — aggregates all executed queries with total time, execution count, and average time. SELECT query, total_exec_time/calls AS avg_ms, calls FROM pg_stat_statements ORDER BY total_exec_time DESC LIMIT 20. (2) log_min_duration_statement = 1000 logs all queries taking >1s. (3) auto_explain automatically logs plans for slow queries. MySQL: slow query log with long_query_time. Application: micrometer timer on repository methods, distributed tracing span for DB calls.","correct":"PostgreSQL: (1) Enable pg_stat_statements — aggregates all executed queries with total time, execution count, and average time.","distractors":["work_mem (PostgreSQL) is the memory available per sort or hash operation per query execution.","Systematic approach: (1) Get the execution plan from production: EXPLAIN (ANALYZE, BUFFERS) <query>.","N+1: a query fetches N entities (1 query), then for each entity fetches a related collection (N queries) = N+1 queries total."]},{"id":"backend/query_optimization#81ec7c953aaa","section":"backend","module":"backend/query_optimization","moduleName":"query optimization","qIndex":10,"difficulty":"intermediate","question":"What is the work_mem setting and when does it cause disk spills?","answerFull":"work_mem (PostgreSQL) is the memory available per sort or hash operation per query execution. If a sort exceeds work_mem, PostgreSQL spills to disk (external merge sort). EXPLAIN ANALYZE shows \"Sort Method: external merge Disk: XKIB\". Signs of work_mem pressure: sorts and hash joins taking unexpectedly long, high temp file usage in pg_stat_database. Fix: increase work_mem for specific queries (SET LOCAL work_mem = '256MB'), or add an index to eliminate the sort, or redesign the query. Be careful: work_mem is per-operation per-query, and a complex query can have many operations.","correct":"work_mem (PostgreSQL) is the memory available per sort or hash operation per query execution.","distractors":["Each line is a plan node (operation).","Option 1: @Query(\"SELECT u FROM User u LEFT JOIN FETCH u.orders WHERE u.id IN :ids\") — JOIN FETCH fetches users and orders in one query.","N+1: a query fetches N entities (1 query), then for each entity fetches a related collection (N queries) = N+1 queries total."]},{"id":"backend/query_optimization#816341672205","section":"backend","module":"backend/query_optimization","moduleName":"query optimization","qIndex":11,"difficulty":"advanced","question":"What query hints are available in PostgreSQL?","answerFull":"PostgreSQL has limited native hints. enable_seqscan = off (session level) disables seq scans — forces index use. enable_hashjoin = off disables hash joins. These are global settings and dangerous in production. Better: use pg_hint_plan extension for per-query hints: /*+ SeqScan(orders) */ or /*+ IndexScan(orders orders_user_id_idx) */. Also: SET LOCAL enable_nestloop = off inside a transaction. For most cases, fixing statistics (ANALYZE) or adjusting planner cost parameters is better than hints.","correct":"PostgreSQL has limited native hints.","distractors":["PreparedStatement separates query planning from execution.","Systematic approach: (1) Get the execution plan from production: EXPLAIN (ANALYZE, BUFFERS) <query>.","N+1: a query fetches N entities (1 query), then for each entity fetches a related collection (N queries) = N+1 queries total."]},{"id":"backend/query_optimization#9109f4054e7d","section":"backend","module":"backend/query_optimization","moduleName":"query optimization","qIndex":12,"difficulty":"advanced","question":"How do you count queries in a Spring test to prevent N+1 regression?","answerFull":"Use datasource-proxy or a custom JDBC connection wrapper. Configure a QueryCountHolder ThreadLocal that counts SQL executions. In tests: java QueryCountHolder.clear(); userService.getUsersWithOrders(); assertThat(QueryCountHolder.getGrandTotal()).isLessThanOrEqualTo(2); // 1 query for users, 1 for orders = max 2 This prevents regression: if someone adds a lazy access that triggers N+1, the test fails. Run these in all repository/service integration tests.","correct":"Use datasource-proxy or a custom JDBC connection wrapper.","distractors":["PostgreSQL: (1) Enable pg_stat_statements — aggregates all executed queries with total time, execution count, and average time.","N+1: a query fetches N entities (1 query), then for each entity fetches a related collection (N queries) = N+1 queries total.","Best performance: COPY (PostgreSQL) or LOAD DATA INFILE (MySQL) — bypasses row-by-row WAL overhead."]},{"id":"backend/query_optimization#1726fce3fb6c","section":"backend","module":"backend/query_optimization","moduleName":"query optimization","qIndex":13,"difficulty":"advanced","question":"What is the difference between INNER JOIN and LEFT JOIN performance-wise?","answerFull":"INNER JOIN: only returns rows where the join condition matches in both tables. LEFT JOIN: returns all rows from the left table, with NULLs for unmatched right table rows. Performance: INNER JOIN generally performs better — it can eliminate non-matching rows earlier in the plan. LEFT JOIN must preserve all left rows, preventing some filter pushdowns. When you use LEFT JOIN but then filter on the right table's columns in WHERE (making it effectively an INNER JOIN), use INNER JOIN explicitly to allow the planner to optimize better.","correct":"INNER JOIN: only returns rows where the join condition matches in both tables.","distractors":["When JOIN FETCHing multiple collections on the same entity (e.g., User FETCH JOIN orders FETCH JOIN tags), the SQL JOIN multiplies the result set: each user row is combined with each order and each tag.","Best performance: COPY (PostgreSQL) or LOAD DATA INFILE (MySQL) — bypasses row-by-row WAL overhead.","OFFSET N requires the database to generate all rows 0 through N+LIMIT-1 and discard 0 through N-1."]},{"id":"backend/query_optimization#366955eb26a6","section":"backend","module":"backend/query_optimization","moduleName":"query optimization","qIndex":14,"difficulty":"advanced","question":"How do you debug a query that is suddenly slow in production?","answerFull":"Systematic approach: (1) Get the execution plan from production: EXPLAIN (ANALYZE, BUFFERS) <query>. (2) Compare estimated vs actual rows — large gap = stale statistics. Run ANALYZE tablename. (3) Check if an index is being used: look for Seq Scan where an Index Scan is expected. (4) Check buffer hit rate in EXPLAIN BUFFERS output: low hit rate = working set exceeds buffer pool. (5) Check for lock contention: SELECT * FROM pg_locks JOIN pg_stat_activity ON pg_locks.pid = pg_stat_activity.pid WHERE granted = false. (6) Compare with a known-good plan: EXPLAIN (ANALYZE) from before the slowdown and compare cardinality estimates.","correct":"Systematic approach: (1) Get the execution plan from production: EXPLAIN (ANALYZE, BUFFERS) <query>.","distractors":["Each line is a plan node (operation).","PostgreSQL: (1) Enable pg_stat_statements — aggregates all executed queries with total time, execution count, and average time.","PreparedStatement separates query planning from execution."]},{"id":"backend/rate_limiting_in_depth#1a693547947a","section":"backend","module":"backend/rate_limiting_in_depth","moduleName":"rate limiting in depth","qIndex":1,"difficulty":"core","question":"What is the boundary burst problem with fixed window counters and how do sliding windows solve it?","answerFull":"In a fixed window counter, a client can send limit requests in the last millisecond of one window and limit requests in the first millisecond of the next window — effectively 2 * limit requests in a very short time. This happens because the counter resets hard at the boundary. A sliding window tracks requests over a rolling window ending at the current moment, so the window always contains at most limit requests in the most recent windowSize duration, regardless of where the clock boundary falls.","correct":"In a fixed window counter, a client can send limit requests in the last millisecond of one window and limit requests in the first millisecond of the next window — effectively 2 * limit requests in a very short time.","distractors":["You must decide: fail open (allow all requests when Redis is down) or fail closed (reject all requests when Redis is down).","Use a two-level approach: a local Redis cluster per data center enforces a fraction of the total limit (total_limit / num_datacenters).","Google's adaptive throttling tracks requests and accepts on the client side."]},{"id":"backend/rate_limiting_in_depth#e47df57cf70f","section":"backend","module":"backend/rate_limiting_in_depth","moduleName":"rate limiting in depth","qIndex":2,"difficulty":"core","question":"Explain the token bucket algorithm. How does it differ from a leaky bucket?","answerFull":"The token bucket accumulates tokens at a fixed rate up to a maximum capacity. Each request consumes one or more tokens. Requests are allowed when tokens are available and rejected when the bucket is empty. This allows bursts up to the bucket capacity while enforcing a long-run average equal to the refill rate. The leaky bucket processes requests at a constant outflow rate regardless of input rate — it does not allow bursts, it smooths them by queueing. Token bucket: variable output, constant average, burst-friendly. Leaky bucket: constant output, no burst, smoothing-focused.","correct":"The token bucket accumulates tokens at a fixed rate up to a maximum capacity.","distractors":["Rate limiting enforces a hard cap: once the limit is exceeded, requests are rejected with a 429.","In a fixed window counter, a client can send limit requests in the last millisecond of one window and limit requests in the first millisecond of the next window — effectively 2 * limit requests in a very short time.","Standard request count rate limiting does not work well for long-lived connections."]},{"id":"backend/rate_limiting_in_depth#11046ac705ab","section":"backend","module":"backend/rate_limiting_in_depth","moduleName":"rate limiting in depth","qIndex":3,"difficulty":"core","question":"How do you implement atomic rate limiting in Redis without race conditions?","answerFull":"Use a Lua script. Redis executes Lua scripts atomically in a single-threaded manner, making the entire check-and-update a single uninterruptible operation. The alternative is Redis transactions (MULTI/EXEC), but they do not prevent other clients from modifying keys between WATCH and EXEC, requiring retry logic. Lua scripts are the correct approach: wrap ZREMRANGEBYSCORE, ZCARD, ZADD, and PEXPIRE in a single Lua script loaded with SCRIPT LOAD and called with EVALSHA.","correct":"Use a Lua script. Redis executes Lua scripts atomically in a single-threaded manner, making the entire check-and-update a single uninterruptible operation.","distractors":["You must decide: fail open (allow all requests when Redis is down) or fail closed (reject all requests when Redis is down).","Assign a cost to each field and operation based on complexity (number of database queries it triggers, depth of nested resolvers, number of objects returned).","Enterprise customers often have hundreds or thousands of employees behind a shared corporate NAT or proxy, meaning all their traffic originates from a single public IP address."]},{"id":"backend/rate_limiting_in_depth#97da0f26be49","section":"backend","module":"backend/rate_limiting_in_depth","moduleName":"rate limiting in depth","qIndex":4,"difficulty":"core","question":"Why is rate limiting by IP address problematic for enterprise customers?","answerFull":"Enterprise customers often have hundreds or thousands of employees behind a shared corporate NAT or proxy, meaning all their traffic originates from a single public IP address. Applying per-IP rate limits treats the entire enterprise as a single user. The correct approach is to rate limit by API key or authenticated user ID for all authenticated traffic, and only fall back to IP-based limiting for unauthenticated endpoints as a DDoS backstop.","correct":"Enterprise customers often have hundreds or thousands of employees behind a shared corporate NAT or proxy, meaning all their traffic originates from a single public IP address.","distractors":["Standard request count rate limiting does not work well for long-lived connections.","Rate limiting enforces a hard cap: once the limit is exceeded, requests are rejected with a 429.","The token bucket accumulates tokens at a fixed rate up to a maximum capacity."]},{"id":"backend/rate_limiting_in_depth#c4126ddbc63b","section":"backend","module":"backend/rate_limiting_in_depth","moduleName":"rate limiting in depth","qIndex":5,"difficulty":"core","question":"What rate limit response headers should you return and what do each mean?","answerFull":"X-RateLimit-Limit: the maximum requests allowed in the window. X-RateLimit-Remaining: how many requests the client can still make in the current window. X-RateLimit-Reset: Unix timestamp when the window resets and the full limit is available again. Retry-After: on a 429 response, the number of seconds the client must wait before retrying. These headers allow well-behaved clients to pre-emptively back off before exhausting their limit and to retry at exactly the right time rather than polling.","correct":"X-RateLimit-Limit: the maximum requests allowed in the window.","distractors":["The token bucket accumulates tokens at a fixed rate up to a maximum capacity.","In a fixed window counter, a client can send limit requests in the last millisecond of one window and limit requests in the first millisecond of the next window — effectively 2 * limit requests in a very short time.","Standard request count rate limiting does not work well for long-lived connections."]},{"id":"backend/rate_limiting_in_depth#8487d056c822","section":"backend","module":"backend/rate_limiting_in_depth","moduleName":"rate limiting in depth","qIndex":6,"difficulty":"intermediate","question":"How would you rate limit a GraphQL API where requests have variable cost?","answerFull":"Assign a cost to each field and operation based on complexity (number of database queries it triggers, depth of nested resolvers, number of objects returned). Limit by total cost points per window rather than by request count. For example, a simple field lookup costs 1 point; a paginated list query costs 10 points per page; a query that fetches nested relationships costs multiplicatively. A client with a budget of 1,000 points per minute can make many simple queries but only a few complex ones. GitHub's GraphQL API uses this exact approach.","correct":"Assign a cost to each field and operation based on complexity (number of database queries it triggers, depth of nested resolvers, number of objects returned).","distractors":["Standard request count rate limiting does not work well for long-lived connections.","Use a two-level approach: a local Redis cluster per data center enforces a fraction of the total limit (total_limit / num_datacenters).","The token bucket accumulates tokens at a fixed rate up to a maximum capacity."]},{"id":"backend/rate_limiting_in_depth#d0fb32c3165e","section":"backend","module":"backend/rate_limiting_in_depth","moduleName":"rate limiting in depth","qIndex":8,"difficulty":"intermediate","question":"How does Google's adaptive throttling work and why is it superior to static rate limiting for preventing cascading failures?","answerFull":"Google's adaptive throttling tracks requests and accepts on the client side. The client probabilistically skips sending requests when throttle_probability = max(0, (requests - K * accepts) / (requests + 1)) is high. When the server starts rejecting requests, the client automatically reduces its send rate proportionally — before the server is overwhelmed with retries. Static server-side rate limiting returns 429 errors, which well-behaved clients retry after a delay. Adaptive throttling prevents the retry storm itself: clients that are already seeing rejections do not send new requests, reducing load automatically.","correct":"Google's adaptive throttling tracks requests and accepts on the client side.","distractors":["In a fixed window counter, a client can send limit requests in the last millisecond of one window and limit requests in the first millisecond of the next window — effectively 2 * limit requests in a very short time.","The token bucket accumulates tokens at a fixed rate up to a maximum capacity.","Rate limiting enforces a hard cap: once the limit is exceeded, requests are rejected with a 429."]},{"id":"backend/rate_limiting_in_depth#56d42b1980d0","section":"backend","module":"backend/rate_limiting_in_depth","moduleName":"rate limiting in depth","qIndex":9,"difficulty":"intermediate","question":"How do you handle rate limiting for long-polling or streaming connections?","answerFull":"Standard request count rate limiting does not work well for long-lived connections. Instead, rate limit by bandwidth (bytes per second), by number of concurrent connections per identity, or by the number of events emitted per second. For WebSocket connections, rate limit the initial handshake (connection rate limiting) and then rate limit messages within the connection. A client that maintains 100 WebSocket connections to circumvent per-connection rate limits should be detected and limited at the identity level by tracking total concurrent connections per API key.","correct":"Standard request count rate limiting does not work well for long-lived connections.","distractors":["Rate limiting enforces a hard cap: once the limit is exceeded, requests are rejected with a 429.","The token bucket accumulates tokens at a fixed rate up to a maximum capacity.","Assign a cost to each field and operation based on complexity (number of database queries it triggers, depth of nested resolvers, number of objects returned)."]},{"id":"backend/rate_limiting_in_depth#10be388e94ff","section":"backend","module":"backend/rate_limiting_in_depth","moduleName":"rate limiting in depth","qIndex":10,"difficulty":"intermediate","question":"What is the difference between rate limiting and throttling?","answerFull":"Rate limiting enforces a hard cap: once the limit is exceeded, requests are rejected with a 429. Throttling slows requests down: it artificially delays processing (e.g., sleeping before processing) to stay within capacity. Rate limiting is more common for API quotas because it is simple and deterministic for clients. Throttling is used in queue-based systems and leaky bucket implementations where requests are deferred rather than dropped. In practice, the terms are often used interchangeably in API contexts.","correct":"Rate limiting enforces a hard cap: once the limit is exceeded, requests are rejected with a 429.","distractors":["Use a two-level approach: a local Redis cluster per data center enforces a fraction of the total limit (total_limit / num_datacenters).","X-RateLimit-Limit: the maximum requests allowed in the window.","You must decide: fail open (allow all requests when Redis is down) or fail closed (reject all requests when Redis is down)."]},{"id":"backend/rate_limiting_in_depth#bf6b066c6d2b","section":"backend","module":"backend/rate_limiting_in_depth","moduleName":"rate limiting in depth","qIndex":11,"difficulty":"advanced","question":"How would you implement rate limiting across multiple data centers without requiring cross-DC synchronization on every request?","answerFull":"Use a two-level approach: a local Redis cluster per data center enforces a fraction of the total limit (total_limit / num_datacenters). Each data center enforces its local fraction without cross-DC calls. Periodically synchronize counts across data centers (every 5–10 seconds) to rebalance. This means clients can exceed the global limit by up to (N-1) / N * limit in the worst case during a synchronization interval, but eliminates cross-DC latency on every request. This is the approach used by Cloudflare and Fastly for their edge rate limiting.","correct":"Use a two-level approach: a local Redis cluster per data center enforces a fraction of the total limit (total_limit / num_datacenters).","distractors":["Rate limiting enforces a hard cap: once the limit is exceeded, requests are rejected with a 429.","The token bucket accumulates tokens at a fixed rate up to a maximum capacity.","You must decide: fail open (allow all requests when Redis is down) or fail closed (reject all requests when Redis is down)."]},{"id":"backend/rate_limiting_in_depth#b341cd90d6e0","section":"backend","module":"backend/rate_limiting_in_depth","moduleName":"rate limiting in depth","qIndex":12,"difficulty":"advanced","question":"What happens to your rate limiter when Redis is unavailable?","answerFull":"You must decide: fail open (allow all requests when Redis is down) or fail closed (reject all requests when Redis is down). Fail open is typically correct for API rate limiting: a brief Redis outage should not take down the entire API. However, fail open during an extended outage may allow abuse. A good compromise: fail open for authenticated users (who have agreed to terms of service) and fail closed for unauthenticated endpoints (to prevent DDoS amplification during outages). Always alert immediately when Redis is unreachable so the outage is detected quickly.","correct":"You must decide: fail open (allow all requests when Redis is down) or fail closed (reject all requests when Redis is down).","distractors":["Use a two-level approach: a local Redis cluster per data center enforces a fraction of the total limit (total_limit / num_datacenters).","Google's adaptive throttling tracks requests and accepts on the client side.","The token bucket accumulates tokens at a fixed rate up to a maximum capacity."]},{"id":"backend/rest_api_design#42eab53e290d","section":"backend","module":"backend/rest_api_design","moduleName":"rest api design","qIndex":2,"difficulty":"core","question":"What is the difference between PUT and PATCH?","answerFull":"PUT replaces the entire resource with the provided representation — fields not included in the request are set to null/default. PATCH applies a partial update — only the fields provided are changed. PUT is idempotent; PATCH can be idempotent or not depending on the patch format (JSON Patch per RFC 6902 is idempotent; a \"increment counter\" PATCH is not). For most REST APIs, PATCH is more practical for updates.","correct":"PUT replaces the entire resource with the provided representation — fields not included in the request are set to null/default.","distractors":["Idempotency means sending the same request N times has the same side effect as sending it once.","(1) Add fields without removing (clients ignore unknown fields if using lenient deserialization).","401 Unauthorized means the client is not authenticated — no valid credentials were provided (missing or invalid token)."]},{"id":"backend/rest_api_design#ebc20ed89b18","section":"backend","module":"backend/rest_api_design","moduleName":"rest api design","qIndex":3,"difficulty":"core","question":"How do you version a REST API and what are the tradeoffs?","answerFull":"URI versioning (/v1/, /v2/) is most common: visible, easy to route at the CDN/load balancer, easy to test in a browser. Header versioning (Accept: application/vnd.api.v2+json) is more RESTful but harder to test and less visible. Query parameter (?version=2) is easy to add but not standard. For public APIs, URI versioning is the pragmatic choice. Never remove a version without at least 6-12 months deprecation notice with Sunset headers.","correct":"URI versioning (/v1/, /v2/) is most common: visible, easy to route at the CDN/load balancer, easy to test in a browser.","distractors":["For mutable resources: ETag (version identifier) + Cache-Control: no-cache (must revalidate, but can store) or Cache-Control: max-age=60, private.","HATEOAS (Hypermedia As The Engine Of Application State) means responses include links to related resources and available actions, enabling clients to navigate the API without out-of-band documentation.","Implement at the API gateway or a filter/middleware layer."]},{"id":"backend/rest_api_design#58ca403c9d47","section":"backend","module":"backend/rest_api_design","moduleName":"rest api design","qIndex":4,"difficulty":"core","question":"What is idempotency and why does it matter for API design?","answerFull":"Idempotency means sending the same request N times has the same side effect as sending it once. GET, PUT, DELETE are idempotent. POST is not. Idempotency matters because network timeouts can cause clients to retry — a non-idempotent POST retry creates duplicate resources. Adding an Idempotency-Key header to POST endpoints allows clients to safely retry: the server deduplicates based on the key and returns the original response for subsequent calls.","correct":"Idempotency means sending the same request N times has the same side effect as sending it once.","distractors":["401 Unauthorized means the client is not authenticated — no valid credentials were provided (missing or invalid token).","PUT replaces the entire resource with the provided representation — fields not included in the request are set to null/default.","Implement at the API gateway or a filter/middleware layer."]},{"id":"backend/rest_api_design#e0a2f9089827","section":"backend","module":"backend/rest_api_design","moduleName":"rest api design","qIndex":5,"difficulty":"core","question":"What is the difference between 401 and 403?","answerFull":"401 Unauthorized means the client is not authenticated — no valid credentials were provided (missing or invalid token). Despite the name, it really means \"unauthenticated.\" The client should authenticate and retry. 403 Forbidden means the client is authenticated but not authorized — the identity is known but lacks permission for the requested resource. The client should not retry with the same credentials.","correct":"401 Unauthorized means the client is not authenticated — no valid credentials were provided (missing or invalid token).","distractors":["HATEOAS (Hypermedia As The Engine Of Application State) means responses include links to related resources and available actions, enabling clients to navigate the API without out-of-band documentation.","Idempotency means sending the same request N times has the same side effect as sending it once.","The client uses the Accept header to specify acceptable response formats: Accept: application/json, application/xml;q=0.9."]},{"id":"backend/rest_api_design#17e2b3f41462","section":"backend","module":"backend/rest_api_design","moduleName":"rest api design","qIndex":6,"difficulty":"intermediate","question":"How would you design pagination for a high-volume feed?","answerFull":"Use cursor-based (keyset) pagination: instead of OFFSET N, use WHERE id > last_seen_id LIMIT 20. This is O(1) regardless of page depth. The cursor is an opaque base64-encoded value (e.g., JSON with id and timestamp) so the implementation can change. Provide a next_cursor field in responses. This is what Twitter/X, Instagram, and GitHub use for their feed APIs.","correct":"Use cursor-based (keyset) pagination: instead of OFFSET N, use WHERE id > last_seen_id LIMIT 20.","distractors":["(1) Add fields without removing (clients ignore unknown fields if using lenient deserialization).","401 Unauthorized means the client is not authenticated — no valid credentials were provided (missing or invalid token).","Clients need a user and their 50 orders."]},{"id":"backend/rest_api_design#f3f3bbde0c0f","section":"backend","module":"backend/rest_api_design","moduleName":"rest api design","qIndex":7,"difficulty":"intermediate","question":"What is RFC 7807 Problem Details?","answerFull":"RFC 7807 defines a standard error response format for HTTP APIs. It includes: type (URI identifying the error class), title (human-readable summary), status (HTTP status code), detail (specific explanation for this occurrence), and instance (URI of the specific request). Additional fields can be added. Using Problem Details ensures all errors have a consistent, machine-parseable format across endpoints.","correct":"RFC 7807 defines a standard error response format for HTTP APIs.","distractors":["The client uses the Accept header to specify acceptable response formats: Accept: application/json, application/xml;q=0.9.","(1) Add fields without removing (clients ignore unknown fields if using lenient deserialization).","401 Unauthorized means the client is not authenticated — no valid credentials were provided (missing or invalid token)."]},{"id":"backend/rest_api_design#f50b30e2b06f","section":"backend","module":"backend/rest_api_design","moduleName":"rest api design","qIndex":8,"difficulty":"intermediate","question":"How do you design a REST API for a file upload?","answerFull":"For small files (<10 MB): multipart/form-data POST with the file as a form field. For large files: use a signed URL pattern — client requests a signed upload URL from the API, uploads directly to object storage (S3, GCS), then notifies the API of completion. This offloads bandwidth from the API server and enables resumable uploads. Never buffer large files in the API server's memory.","correct":"For small files (<10 MB): multipart/form-data POST with the file as a form field.","distractors":["POST /jobs returns 202 Accepted with a Location header pointing to a job status URL (/jobs/123).","HATEOAS (Hypermedia As The Engine Of Application State) means responses include links to related resources and available actions, enabling clients to navigate the API without out-of-band documentation.","Implement at the API gateway or a filter/middleware layer."]},{"id":"backend/rest_api_design#472f9064237d","section":"backend","module":"backend/rest_api_design","moduleName":"rest api design","qIndex":9,"difficulty":"intermediate","question":"What HTTP caching headers should a REST API set?","answerFull":"For mutable resources: ETag (version identifier) + Cache-Control: no-cache (must revalidate, but can store) or Cache-Control: max-age=60, private. For immutable versioned resources: Cache-Control: max-age=31536000, immutable. For sensitive data: Cache-Control: no-store. Always set Vary: Accept-Encoding if responses are compressed. The Vary header tells caches which request headers affect the response.","correct":"For mutable resources: ETag (version identifier) + Cache-Control: no-cache (must revalidate, but can store) or Cache-Control: max-age=60, private.","distractors":["HATEOAS (Hypermedia As The Engine Of Application State) means responses include links to related resources and available actions, enabling clients to navigate the API without out-of-band documentation.","RFC 7807 defines a standard error response format for HTTP APIs.","PUT replaces the entire resource with the provided representation — fields not included in the request are set to null/default."]},{"id":"backend/rest_api_design#ab06cc960128","section":"backend","module":"backend/rest_api_design","moduleName":"rest api design","qIndex":10,"difficulty":"intermediate","question":"What is HATEOAS and is it practical?","answerFull":"HATEOAS (Hypermedia As The Engine Of Application State) means responses include links to related resources and available actions, enabling clients to navigate the API without out-of-band documentation. In theory it allows client-server evolution. In practice it is rarely implemented because: (1) generating links is complex, especially for conditional actions; (2) most clients use generated SDKs from OpenAPI specs rather than following links; (3) it does not solve versioning problems. Understand it for interviews; use OpenAPI in production.","correct":"HATEOAS (Hypermedia As The Engine Of Application State) means responses include links to related resources and available actions, enabling clients to navigate the API without out-of-band documentation.","distractors":["Clients need a user and their 50 orders.","401 Unauthorized means the client is not authenticated — no valid credentials were provided (missing or invalid token).","Implement at the API gateway or a filter/middleware layer."]},{"id":"backend/rest_api_design#90b1fc45349e","section":"backend","module":"backend/rest_api_design","moduleName":"rest api design","qIndex":11,"difficulty":"advanced","question":"How do you design an async long-running operation endpoint?","answerFull":"POST /jobs returns 202 Accepted with a Location header pointing to a job status URL (/jobs/123). The client polls GET /jobs/123 until status is \"completed\" or \"failed\". The response includes the result URL when done. Alternative: use webhooks — client registers a callback URL, server POSTs the result when done. Webhooks require the client to have a publicly accessible endpoint.","correct":"POST /jobs returns 202 Accepted with a Location header pointing to a job status URL (/jobs/123).","distractors":["For small files (<10 MB): multipart/form-data POST with the file as a form field.","The client uses the Accept header to specify acceptable response formats: Accept: application/json, application/xml;q=0.9.","(1) Add fields without removing (clients ignore unknown fields if using lenient deserialization)."]},{"id":"backend/rest_api_design#a5a178611051","section":"backend","module":"backend/rest_api_design","moduleName":"rest api design","qIndex":12,"difficulty":"advanced","question":"What is the N+1 problem in REST APIs?","answerFull":"Clients need a user and their 50 orders. Naive approach: GET /users/123, then GET /orders/123, GET /orders/124... = 51 requests. Solutions: (1) include sub-resources in the response via an include or embed query parameter; (2) provide a batch endpoint (POST /orders/batch with list of IDs); (3) switch to GraphQL which resolves this structurally. The N+1 problem in REST APIs is the main argument for GraphQL in complex data-access scenarios.","correct":"Clients need a user and their 50 orders.","distractors":["RFC 7807 defines a standard error response format for HTTP APIs.","(1) Add fields without removing (clients ignore unknown fields if using lenient deserialization).","HATEOAS (Hypermedia As The Engine Of Application State) means responses include links to related resources and available actions, enabling clients to navigate the API without out-of-band documentation."]},{"id":"backend/rest_api_design#36a61346e4f7","section":"backend","module":"backend/rest_api_design","moduleName":"rest api design","qIndex":13,"difficulty":"advanced","question":"How do you handle breaking changes in a REST API?","answerFull":"(1) Add fields without removing (clients ignore unknown fields if using lenient deserialization). (2) Never change field types. (3) Never remove required request fields. (4) Deprecate via Sunset header: Sunset: Sat, 01 Jan 2027 00:00:00 GMT + Deprecation: true. (5) When breaking changes are unavoidable, increment the major version (/v2/). (6) Run both versions in parallel for at least 6 months. (7) Monitor usage of deprecated endpoints via metrics.","correct":"(1) Add fields without removing (clients ignore unknown fields if using lenient deserialization).","distractors":["Clients need a user and their 50 orders.","PUT replaces the entire resource with the provided representation — fields not included in the request are set to null/default.","HATEOAS (Hypermedia As The Engine Of Application State) means responses include links to related resources and available actions, enabling clients to navigate the API without out-of-band documentation."]},{"id":"backend/rest_api_design#d8c5e5671cf8","section":"backend","module":"backend/rest_api_design","moduleName":"rest api design","qIndex":14,"difficulty":"advanced","question":"What is content negotiation in REST?","answerFull":"The client uses the Accept header to specify acceptable response formats: Accept: application/json, application/xml;q=0.9. The server responds with the best match and includes Content-Type in the response. This allows one endpoint to serve multiple formats without separate URLs. Java Spring MVC's content negotiation via produces on @RequestMapping handles this automatically.","correct":"The client uses the Accept header to specify acceptable response formats: Accept: application/json, application/xml;q=0.9.","distractors":["RFC 7807 defines a standard error response format for HTTP APIs.","HATEOAS (Hypermedia As The Engine Of Application State) means responses include links to related resources and available actions, enabling clients to navigate the API without out-of-band documentation.","POST /jobs returns 202 Accepted with a Location header pointing to a job status URL (/jobs/123)."]},{"id":"backend/rest_api_design#4a9f2b190aca","section":"backend","module":"backend/rest_api_design","moduleName":"rest api design","qIndex":15,"difficulty":"advanced","question":"How do you implement rate limiting in a REST API?","answerFull":"Implement at the API gateway or a filter/middleware layer. Return 429 Too Many Requests when the limit is exceeded, with headers: X-RateLimit-Limit: 100, X-RateLimit-Remaining: 0, X-RateLimit-Reset: 1735689600 (epoch when window resets), Retry-After: 60 (seconds). Use sliding window or token bucket algorithms. Limit by API key, user ID, or IP depending on the use case.","correct":"Implement at the API gateway or a filter/middleware layer.","distractors":["HATEOAS (Hypermedia As The Engine Of Application State) means responses include links to related resources and available actions, enabling clients to navigate the API without out-of-band documentation.","(1) Add fields without removing (clients ignore unknown fields if using lenient deserialization).","401 Unauthorized means the client is not authenticated — no valid credentials were provided (missing or invalid token)."]},{"id":"backend/service_mesh_and_service_discovery#5fd59ac1e707","section":"backend","module":"backend/service_mesh_and_service_discovery","moduleName":"service mesh and service discovery","qIndex":1,"difficulty":"core","question":"What is a service mesh and what problems does it solve?","answerFull":"A service mesh is a dedicated infrastructure layer for service-to-service communication. It solves: (1) mTLS between services without code changes (zero-trust security), (2) centralized retry/timeout/circuit breaker policy applied uniformly, (3) automatic distributed tracing and metrics for all service calls, (4) traffic control (canary releases, A/B testing, traffic mirroring) at the infrastructure level. Without a mesh, each service implements these concerns differently or not at all. The mesh uses a sidecar proxy (Envoy) injected into each pod; iptables rules redirect all pod traffic through the sidecar, making it transparent to applications.","correct":"A service mesh is a dedicated infrastructure layer for service-to-service communication.","distractors":["In client-side discovery, the service queries a registry (Eureka, Consul) for available instances and performs load balancing itself using a client-side library (Spring Cloud LoadBalancer).","xDS (x Discovery Service) is the API between Istio's control plane (Istiod) and Envoy sidecar proxies.","Istio's DestinationRule outlierDetection configuration implements circuit breaking at the Envoy sidecar level."]},{"id":"backend/service_mesh_and_service_discovery#bba6d36fdba1","section":"backend","module":"backend/service_mesh_and_service_discovery","moduleName":"service mesh and service discovery","qIndex":2,"difficulty":"core","question":"What is the difference between liveness and readiness probes?","answerFull":"Liveness determines if the process is alive — failure triggers a container restart. Check only that the JVM is running and not deadlocked: GET /actuator/health/liveness returning 200 is sufficient. Readiness determines if the service is ready to accept traffic — failure removes the pod from the Service's endpoints (no new requests). Check all dependencies: DB connectivity, cache availability, any required warmup. The critical distinction: if a database goes down, all pods should fail readiness (removed from LB, no new traffic) but pass liveness (not restarted). If liveness was tied to DB health, all pods would restart simultaneously, potentially causing a cascading failure.","correct":"Liveness determines if the process is alive — failure triggers a container restart.","distractors":["The startup probe prevents liveness and readiness probes from interfering with slow-starting applications.","A service mesh is a dedicated infrastructure layer for service-to-service communication.","In client-side discovery, the service queries a registry (Eureka, Consul) for available instances and performs load balancing itself using a client-side library (Spring Cloud LoadBalancer)."]},{"id":"backend/service_mesh_and_service_discovery#4a65e25bd5c3","section":"backend","module":"backend/service_mesh_and_service_discovery","moduleName":"service mesh and service discovery","qIndex":3,"difficulty":"core","question":"What is the difference between client-side and server-side service discovery?","answerFull":"In client-side discovery, the service queries a registry (Eureka, Consul) for available instances and performs load balancing itself using a client-side library (Spring Cloud LoadBalancer). The client knows about all instances and can implement sophisticated routing (zone-aware, latency-based). The downside is logic in every service and a tightly coupled registry. In server-side discovery, the client makes a request to a load balancer or DNS name; the load balancer queries the registry. The client is simpler but there is an extra network hop. Kubernetes Services are a form of server-side discovery using iptables/IPVS at the node level, which adds minimal latency.","correct":"In client-side discovery, the service queries a registry (Eureka, Consul) for available instances and performs load balancing itself using a client-side library (Spring Cloud LoadBalancer).","distractors":["xDS (x Discovery Service) is the API between Istio's control plane (Istiod) and Envoy sidecar proxies.","Mutual TLS (mTLS) means both the client and server authenticate each other using TLS certificates, unlike regular HTTPS where only the server is authenticated.","A service mesh is a dedicated infrastructure layer for service-to-service communication."]},{"id":"backend/service_mesh_and_service_discovery#bd68fc4acf5a","section":"backend","module":"backend/service_mesh_and_service_discovery","moduleName":"service mesh and service discovery","qIndex":4,"difficulty":"core","question":"How does Istio implement circuit breaking without code changes?","answerFull":"Istio's DestinationRule outlierDetection configuration implements circuit breaking at the Envoy sidecar level. You configure: consecutiveGatewayErrors (how many 5xx responses before ejecting an instance), interval (how often to evaluate), baseEjectionTime (how long to eject for), and maxEjectionPercent (max percentage of instances to eject simultaneously). When Envoy detects that a specific upstream instance is returning errors, it stops routing traffic to that instance for the ejection period, then gradually re-admits it. This works for any service regardless of language or framework.","correct":"Istio's DestinationRule outlierDetection configuration implements circuit breaking at the Envoy sidecar level.","distractors":["xDS (x Discovery Service) is the API between Istio's control plane (Istiod) and Envoy sidecar proxies.","A service mesh is a dedicated infrastructure layer for service-to-service communication.","In client-side discovery, the service queries a registry (Eureka, Consul) for available instances and performs load balancing itself using a client-side library (Spring Cloud LoadBalancer)."]},{"id":"backend/service_mesh_and_service_discovery#bfadbbd7a1ba","section":"backend","module":"backend/service_mesh_and_service_discovery","moduleName":"service mesh and service discovery","qIndex":5,"difficulty":"core","question":"What is mTLS in a service mesh and why is it important?","answerFull":"Mutual TLS (mTLS) means both the client and server authenticate each other using TLS certificates, unlike regular HTTPS where only the server is authenticated. In a service mesh, Istio's Citadel issues certificates to each service (based on its Kubernetes ServiceAccount). When service A calls service B, both sides present and validate certificates. This provides: authentication (only services with valid certificates can communicate), encryption (traffic is encrypted in transit — protects against eavesdropping within the cluster), and authorization (Istio's AuthorizationPolicy can restrict which service identities can call which endpoints). This implements zero-trust networking inside the cluster.","correct":"Mutual TLS (mTLS) means both the client and server authenticate each other using TLS certificates, unlike regular HTTPS where only the server is authenticated.","distractors":["xDS (x Discovery Service) is the API between Istio's control plane (Istiod) and Envoy sidecar proxies.","A service mesh is a dedicated infrastructure layer for service-to-service communication.","In client-side discovery, the service queries a registry (Eureka, Consul) for available instances and performs load balancing itself using a client-side library (Spring Cloud LoadBalancer)."]},{"id":"backend/service_mesh_and_service_discovery#daad47988fa4","section":"backend","module":"backend/service_mesh_and_service_discovery","moduleName":"service mesh and service discovery","qIndex":6,"difficulty":"intermediate","question":"How does xDS protocol work in Istio?","answerFull":"xDS (x Discovery Service) is the API between Istio's control plane (Istiod) and Envoy sidecar proxies. It consists of several sub-APIs: LDS (Listener Discovery Service) configures what ports Envoy listens on; RDS (Route Discovery Service) configures routing rules (VirtualService); CDS (Cluster Discovery Service) defines upstream service clusters (DestinationRule); EDS (Endpoint Discovery Service) provides healthy endpoint lists for each cluster. Istiod watches Kubernetes Service and Pod resources, merges Istio CRD configurations (VirtualService, DestinationRule), and pushes the derived xDS configuration to all relevant Envoy proxies. This is how a change to a VirtualService takes effect across all pods within seconds without restart.","correct":"xDS (x Discovery Service) is the API between Istio's control plane (Istiod) and Envoy sidecar proxies.","distractors":["Istio's DestinationRule outlierDetection configuration implements circuit breaking at the Envoy sidecar level.","In client-side discovery, the service queries a registry (Eureka, Consul) for available instances and performs load balancing itself using a client-side library (Spring Cloud LoadBalancer).","A service mesh is a dedicated infrastructure layer for service-to-service communication."]},{"id":"backend/service_mesh_and_service_discovery#0cb1ff9fccb7","section":"backend","module":"backend/service_mesh_and_service_discovery","moduleName":"service mesh and service discovery","qIndex":7,"difficulty":"intermediate","question":"What is the startup probe in Kubernetes and when should you use it?","answerFull":"The startup probe prevents liveness and readiness probes from interfering with slow-starting applications. Before the startup probe succeeds, liveness and readiness probes are not evaluated. Configure failureThreshold * periodSeconds to cover the maximum possible startup time (Spring Boot cold start: 10-30 seconds). Once the startup probe succeeds, it is no longer checked — liveness and readiness take over. Without a startup probe, you would set initialDelaySeconds=60 on liveness, meaning if the JVM hangs after startup, Kubernetes waits 60 seconds before restarting. With a startup probe, liveness reacts immediately after startup completes.","correct":"The startup probe prevents liveness and readiness probes from interfering with slow-starting applications.","distractors":["Liveness determines if the process is alive — failure triggers a container restart.","A service mesh is a dedicated infrastructure layer for service-to-service communication.","In client-side discovery, the service queries a registry (Eureka, Consul) for available instances and performs load balancing itself using a client-side library (Spring Cloud LoadBalancer)."]},{"id":"backend/tcp_ip_deep_dive#00d7ada6ca70","section":"backend","module":"backend/tcp_ip_deep_dive","moduleName":"tcp ip deep dive","qIndex":1,"difficulty":"core","question":"Describe the TCP 3-way handshake and why each step is necessary.","answerFull":"Client sends SYN with its initial sequence number. Server responds with SYN+ACK, acknowledging the client's sequence and sending its own sequence. Client sends ACK, acknowledging the server's sequence. Three steps are required because both sides need to agree on initial sequence numbers independently. A 2-way handshake would not establish the server's sequence number acknowledgment before data flows.","correct":"Client sends SYN with its initial sequence number.","distractors":["Nagle's algorithm holds small TCP writes in a buffer until a full MSS is available or all outstanding data is ACKed, reducing small-packet overhead.","TCP buffers out-of-order segments at the receiver.","TCP Keep-Alive sends probe packets on idle connections to detect if the other end is still alive."]},{"id":"backend/tcp_ip_deep_dive#165290708ef3","section":"backend","module":"backend/tcp_ip_deep_dive","moduleName":"tcp ip deep dive","qIndex":2,"difficulty":"core","question":"What is TIME_WAIT and why does it exist?","answerFull":"TIME_WAIT is the state the active closer enters after sending the final ACK, lasting 2*MSL (60–240 seconds on Linux). It serves two purposes: ensuring the final ACK reaches the other side (if lost, the passive closer retransmits FIN and the active closer re-sends ACK), and ensuring all duplicate packets from the previous connection expire before the port pair is reused (preventing data corruption in a new connection).","correct":"TIME_WAIT is the state the active closer enters after sending the final ACK, lasting 2*MSL (60–240 seconds on Linux).","distractors":["CUBIC (Linux default) reacts to packet loss as a congestion signal — it reduces cwnd on each loss event.","FIN initiates a graceful close: the sender has finished sending but can still receive data.","High-throughput servers making many short-lived outbound connections accumulate TIME_WAIT sockets that hold ports until expiry."]},{"id":"backend/tcp_ip_deep_dive#a42a50fad0eb","section":"backend","module":"backend/tcp_ip_deep_dive","moduleName":"tcp ip deep dive","qIndex":3,"difficulty":"core","question":"How does TIME_WAIT cause problems on busy servers and how do you fix it?","answerFull":"High-throughput servers making many short-lived outbound connections accumulate TIME_WAIT sockets that hold ports until expiry. If the ephemeral port range is exhausted, new connections fail with EADDRNOTAVAIL. Fix: (1) use persistent connections/connection pooling to reduce connection churn; (2) widen ephemeral port range to 1024–65000; (3) enable tcp_tw_reuse=1 for outbound connections; (4) never use tcp_tw_recycle (broken with NAT).","correct":"High-throughput servers making many short-lived outbound connections accumulate TIME_WAIT sockets that hold ports until expiry.","distractors":["TCP Keep-Alive sends probe packets on idle connections to detect if the other end is still alive.","Slow start begins each new connection with cwnd=1 MSS and doubles it per RTT until reaching the slow start threshold (ssthresh).","The SYN backlog holds half-open connections (SYN received, SYN+ACK sent, waiting for final ACK)."]},{"id":"backend/tcp_ip_deep_dive#037c8a34d71f","section":"backend","module":"backend/tcp_ip_deep_dive","moduleName":"tcp ip deep dive","qIndex":4,"difficulty":"core","question":"What is Nagle's algorithm and when should you disable it?","answerFull":"Nagle's algorithm holds small TCP writes in a buffer until a full MSS is available or all outstanding data is ACKed, reducing small-packet overhead. Disable it (TCP_NODELAY=true) for request-response protocols like Redis, JDBC, or gRPC, where a single small request must be sent immediately. Without TCP_NODELAY, Nagle's interacts with delayed ACK to add 40ms of artificial latency to every request.","correct":"Nagle's algorithm holds small TCP writes in a buffer until a full MSS is available or all outstanding data is ACKed, reducing small-packet overhead.","distractors":["Slow start begins each new connection with cwnd=1 MSS and doubles it per RTT until reaching the slow start threshold (ssthresh).","High-throughput servers making many short-lived outbound connections accumulate TIME_WAIT sockets that hold ports until expiry.","TCP buffers out-of-order segments at the receiver."]},{"id":"backend/tcp_ip_deep_dive#aed49c82bfe1","section":"backend","module":"backend/tcp_ip_deep_dive","moduleName":"tcp ip deep dive","qIndex":5,"difficulty":"core","question":"Explain TCP slow start and congestion avoidance.","answerFull":"Slow start begins each new connection with cwnd=1 MSS and doubles it per RTT until reaching the slow start threshold (ssthresh). This avoids overwhelming the network immediately. Once ssthresh is reached, congestion avoidance takes over and increases cwnd by 1 MSS per RTT (linear). On packet loss (triple duplicate ACK), ssthresh is halved and cwnd is reduced; on timeout, cwnd is reset to 1 MSS (full slow start restart).","correct":"Slow start begins each new connection with cwnd=1 MSS and doubles it per RTT until reaching the slow start threshold (ssthresh).","distractors":["Nagle's algorithm holds small TCP writes in a buffer until a full MSS is available or all outstanding data is ACKed, reducing small-packet overhead.","ECN is a mechanism where routers mark packets (setting ECE/CWR bits in the IP header) to signal congestion before dropping packets.","A half-open connection occurs when one end has closed (process crash, network partition) but the other end's socket is still ESTABLISHED."]},{"id":"backend/tcp_ip_deep_dive#5708c6f98663","section":"backend","module":"backend/tcp_ip_deep_dive","moduleName":"tcp ip deep dive","qIndex":6,"difficulty":"intermediate","question":"What is the difference between FIN and RST?","answerFull":"FIN initiates a graceful close: the sender has finished sending but can still receive data. The other side may still send data. RST is an abrupt close: immediately terminates the connection with no data loss tolerance. RST is sent when a packet arrives for a closed socket, when a connection must be immediately aborted (e.g., SO_LINGER with l_onoff=1, l_linger=0), or to reject a SYN for a port with no listener.","correct":"FIN initiates a graceful close: the sender has finished sending but can still receive data.","distractors":["A half-open connection occurs when one end has closed (process crash, network partition) but the other end's socket is still ESTABLISHED.","If the server process exits without calling close() on sockets, the OS sends RST to all connected clients.","TCP Keep-Alive sends probe packets on idle connections to detect if the other end is still alive."]},{"id":"backend/tcp_ip_deep_dive#4390a6136cb8","section":"backend","module":"backend/tcp_ip_deep_dive","moduleName":"tcp ip deep dive","qIndex":7,"difficulty":"intermediate","question":"What is the SYN backlog and how does SYN flooding exploit it?","answerFull":"The SYN backlog holds half-open connections (SYN received, SYN+ACK sent, waiting for final ACK). A SYN flood attack sends many SYNs from spoofed IPs without completing handshakes, exhausting the backlog so legitimate connections are rejected. Defense: SYN cookies (tcp_syncookies=1) encode connection state in the ISN so no queue entry is needed until the ACK arrives.","correct":"The SYN backlog holds half-open connections (SYN received, SYN+ACK sent, waiting for final ACK).","distractors":["TIME_WAIT is the state the active closer enters after sending the final ACK, lasting 2*MSL (60–240 seconds on Linux).","TCP Keep-Alive sends probe packets on idle connections to detect if the other end is still alive.","The second argument to listen() sets the maximum length of the accept queue (completed connections waiting for accept())."]},{"id":"backend/tcp_ip_deep_dive#12ce03d11ab2","section":"backend","module":"backend/tcp_ip_deep_dive","moduleName":"tcp ip deep dive","qIndex":8,"difficulty":"intermediate","question":"How does TCP flow control work?","answerFull":"The receiver advertises its available buffer space as the window size (rwnd) in each ACK. The sender never sends more bytes than the current rwnd. If rwnd reaches 0, the sender stops and sends 1-byte probes (window probes) periodically. Once the receiver drains its buffer and sends a window update with rwnd > 0, the sender resumes. This prevents the sender from overwhelming the receiver's buffer.","correct":"The receiver advertises its available buffer space as the window size (rwnd) in each ACK.","distractors":["TCP buffers out-of-order segments at the receiver.","TIME_WAIT is the state the active closer enters after sending the final ACK, lasting 2*MSL (60–240 seconds on Linux).","TCP Keep-Alive sends probe packets on idle connections to detect if the other end is still alive."]},{"id":"backend/tcp_ip_deep_dive#129f335ccadc","section":"backend","module":"backend/tcp_ip_deep_dive","moduleName":"tcp ip deep dive","qIndex":9,"difficulty":"intermediate","question":"What is TCP Keep-Alive and when should you use it?","answerFull":"TCP Keep-Alive sends probe packets on idle connections to detect if the other end is still alive. Configured via tcp_keepalive_time (default 7200s), tcp_keepalive_intvl (probe interval, default 75s), tcp_keepalive_probes (number of probes before declaring dead, default 9). Use it when connections must traverse stateful firewalls or NAT gateways that expire idle sessions, and when detecting half-open connections from crashed peers. Application-level heartbeats are more reliable and should be preferred.","correct":"TCP Keep-Alive sends probe packets on idle connections to detect if the other end is still alive.","distractors":["FIN initiates a graceful close: the sender has finished sending but can still receive data.","Client sends SYN with its initial sequence number.","TCP buffers out-of-order segments at the receiver."]},{"id":"backend/tcp_ip_deep_dive#962ab255469f","section":"backend","module":"backend/tcp_ip_deep_dive","moduleName":"tcp ip deep dive","qIndex":10,"difficulty":"intermediate","question":"What is BBR and how does it differ from CUBIC?","answerFull":"CUBIC (Linux default) reacts to packet loss as a congestion signal — it reduces cwnd on each loss event. BBR (Bottleneck Bandwidth and RTT) probes for available bandwidth and RTT to model the network's operating point, then maintains cwnd at the bandwidth-delay product. BBR handles high-latency, high-bandwidth links better because it does not interpret every packet loss as congestion. On lossy wireless links, BBR can be too aggressive and crowd out loss-based algorithms.","correct":"CUBIC (Linux default) reacts to packet loss as a congestion signal — it reduces cwnd on each loss event.","distractors":["TIME_WAIT is the state the active closer enters after sending the final ACK, lasting 2*MSL (60–240 seconds on Linux).","Slow start begins each new connection with cwnd=1 MSS and doubles it per RTT until reaching the slow start threshold (ssthresh).","Nagle's algorithm holds small TCP writes in a buffer until a full MSS is available or all outstanding data is ACKed, reducing small-packet overhead."]},{"id":"backend/tcp_ip_deep_dive#2182e1ff5b2d","section":"backend","module":"backend/tcp_ip_deep_dive","moduleName":"tcp ip deep dive","qIndex":11,"difficulty":"advanced","question":"What is the difference between SO_REUSEADDR and SO_REUSEPORT?","answerFull":"SO_REUSEADDR allows binding a socket to a port that is in TIME_WAIT state (common for servers restarting quickly — without it, bind fails for 60–240 seconds). SO_REUSEPORT allows multiple sockets (in different processes or threads) to bind to the same IP:port; the kernel load-balances incoming connections across them. Nginx and modern Java servers use SO_REUSEPORT for worker-per-core architectures.","correct":"SO_REUSEADDR allows binding a socket to a port that is in TIME_WAIT state (common for servers restarting quickly — without it, bind fails for 60–240 seconds).","distractors":["TIME_WAIT is the state the active closer enters after sending the final ACK, lasting 2*MSL (60–240 seconds on Linux).","High-throughput servers making many short-lived outbound connections accumulate TIME_WAIT sockets that hold ports until expiry.","A half-open connection occurs when one end has closed (process crash, network partition) but the other end's socket is still ESTABLISHED."]},{"id":"backend/tcp_ip_deep_dive#438e243a1bd0","section":"backend","module":"backend/tcp_ip_deep_dive","moduleName":"tcp ip deep dive","qIndex":12,"difficulty":"advanced","question":"What are half-open connections and how do they cause memory leaks?","answerFull":"A half-open connection occurs when one end has closed (process crash, network partition) but the other end's socket is still ESTABLISHED. The OS holds the socket and its buffers indefinitely because no signal (FIN or RST) was received. This leaks file descriptors and memory. Detection: ss -tan | grep ESTABLISHED growing without bound. Fix: TCP Keep-Alive with aggressive timers, application heartbeats, and connection idle timeouts.","correct":"A half-open connection occurs when one end has closed (process crash, network partition) but the other end's socket is still ESTABLISHED.","distractors":["High-throughput servers making many short-lived outbound connections accumulate TIME_WAIT sockets that hold ports until expiry.","FIN initiates a graceful close: the sender has finished sending but can still receive data.","SO_REUSEADDR allows binding a socket to a port that is in TIME_WAIT state (common for servers restarting quickly — without it, bind fails for 60–240 seconds)."]},{"id":"backend/tcp_ip_deep_dive#573e86fb4f8a","section":"backend","module":"backend/tcp_ip_deep_dive","moduleName":"tcp ip deep dive","qIndex":13,"difficulty":"advanced","question":"What happens to in-flight data during a server restart?","answerFull":"If the server process exits without calling close() on sockets, the OS sends RST to all connected clients. If the server calls close() gracefully (sends FIN), clients receive the FIN and can read remaining buffered data. For zero-downtime restarts: (1) stop accepting new connections but finish processing in-flight requests; (2) use SO_REUSEPORT for the new process to accept new connections immediately; (3) drain the old process with a timeout.","correct":"If the server process exits without calling close() on sockets, the OS sends RST to all connected clients.","distractors":["High-throughput servers making many short-lived outbound connections accumulate TIME_WAIT sockets that hold ports until expiry.","Client sends SYN with its initial sequence number.","FIN initiates a graceful close: the sender has finished sending but can still receive data."]},{"id":"backend/tcp_ip_deep_dive#85c40f596c19","section":"backend","module":"backend/tcp_ip_deep_dive","moduleName":"tcp ip deep dive","qIndex":14,"difficulty":"advanced","question":"What is ECN (Explicit Congestion Notification)?","answerFull":"ECN is a mechanism where routers mark packets (setting ECE/CWR bits in the IP header) to signal congestion before dropping packets. TCP supports ECN (IANA-registered TCP option). Benefits: faster congestion response without packet loss, lower latency. Requirement: both endpoints and all routers in the path must support ECN. In practice, some routers drop ECN-marked packets, requiring fallback. Linux enables ECN via net.ipv4.tcp_ecn=1.","correct":"ECN is a mechanism where routers mark packets (setting ECE/CWR bits in the IP header) to signal congestion before dropping packets.","distractors":["CUBIC (Linux default) reacts to packet loss as a congestion signal — it reduces cwnd on each loss event.","TCP Keep-Alive sends probe packets on idle connections to detect if the other end is still alive.","A half-open connection occurs when one end has closed (process crash, network partition) but the other end's socket is still ESTABLISHED."]},{"id":"backend/tcp_ip_deep_dive#f173eaa1106c","section":"backend","module":"backend/tcp_ip_deep_dive","moduleName":"tcp ip deep dive","qIndex":15,"difficulty":"advanced","question":"How does TCP handle out-of-order delivery?","answerFull":"TCP buffers out-of-order segments at the receiver. The receiver sends ACKs for the last contiguous byte received. If segment 1000–1999 arrives, then 3000–3999 arrives before 2000–2999, the receiver ACKs only 2000 (edge of contiguous delivery) but buffers 3000–3999. With SACK, the receiver can also report 3000–3999 as received, allowing the sender to retransmit only 2000–2999.","correct":"TCP buffers out-of-order segments at the receiver.","distractors":["The receiver advertises its available buffer space as the window size (rwnd) in each ACK.","TCP Keep-Alive sends probe packets on idle connections to detect if the other end is still alive.","Nagle's algorithm holds small TCP writes in a buffer until a full MSS is available or all outstanding data is ACKed, reducing small-packet overhead."]},{"id":"backend/tcp_ip_deep_dive#d815e7dd61ae","section":"backend","module":"backend/tcp_ip_deep_dive","moduleName":"tcp ip deep dive","qIndex":16,"difficulty":"advanced","question":"What is the significance of the sequence number space and why are initial sequence numbers (ISN) random?","answerFull":"The 32-bit sequence number wraps around at 4 GB. ISNs are randomized to prevent blind packet injection attacks: an attacker who knows the IP:port pair cannot inject forged segments without guessing the current sequence number. The ISN randomization makes this guessing computationally infeasible, preventing TCP hijacking attacks.","correct":"The 32-bit sequence number wraps around at 4 GB.","distractors":["Client sends SYN with its initial sequence number.","The receiver advertises its available buffer space as the window size (rwnd) in each ACK.","A half-open connection occurs when one end has closed (process crash, network partition) but the other end's socket is still ESTABLISHED."]},{"id":"backend/tcp_ip_deep_dive#a983cc86e584","section":"backend","module":"backend/tcp_ip_deep_dive","moduleName":"tcp ip deep dive","qIndex":17,"difficulty":"advanced","question":"What happens when a server calls listen(sockfd, backlog) with a small backlog?","answerFull":"The second argument to listen() sets the maximum length of the accept queue (completed connections waiting for accept()). If the backlog is 5 and 10 connections complete the handshake before the application calls accept(), 5 will be queued and the other 5 will have their final ACKs silently dropped (or RST sent). The client will see a timeout or reset. Under burst traffic, a small backlog causes connection failures even when the server is healthy. Always set backlog to at least 1024, and increase somaxconn/tcp_max_syn_backlog accordingly.","correct":"The second argument to listen() sets the maximum length of the accept queue (completed connections waiting for accept()).","distractors":["TCP Keep-Alive sends probe packets on idle connections to detect if the other end is still alive.","The SYN backlog holds half-open connections (SYN received, SYN+ACK sent, waiting for final ACK).","Nagle's algorithm holds small TCP writes in a buffer until a full MSS is available or all outstanding data is ACKed, reducing small-packet overhead."]},{"id":"backend/tcp_ip_deep_dive#88a7e7a23dda","section":"backend","module":"backend/tcp_ip_deep_dive","moduleName":"tcp ip deep dive","qIndex":18,"difficulty":"advanced","question":"Why do load balancers sometimes send RST to upstream connections?","answerFull":"Load balancers have idle connection timeouts. When a backend connection has been idle for the configured timeout (commonly 60–300 seconds), the load balancer closes it, often by sending RST. If the backend application has no connection validation and reuses the connection, the next request will fail with a broken pipe error. This is why connection pools should have maxLifetime shorter than the LB idle timeout, and why keepalive probes should be shorter than the LB timeout.","correct":"Load balancers have idle connection timeouts.","distractors":["The second argument to listen() sets the maximum length of the accept queue (completed connections waiting for accept()).","TCP Keep-Alive sends probe packets on idle connections to detect if the other end is still alive.","Slow start begins each new connection with cwnd=1 MSS and doubles it per RTT until reaching the slow start threshold (ssthresh)."]},{"id":"backend/tcp_ip_deep_dive#3e7ee3b7c44c","section":"backend","module":"backend/tcp_ip_deep_dive","moduleName":"tcp ip deep dive","qIndex":19,"difficulty":"advanced","question":"Explain the relationship between TCP window size and throughput.","answerFull":"Throughput = window_size / RTT. A TCP window of 64 KB over a 100ms RTT link gives 640 KB/s maximum throughput, regardless of bandwidth. The TCP window scale option (RFC 1323) extends the window to 1 GB. For high-BDP (bandwidth-delay product) links, increasing socket buffers is critical. net.core.rmem_max and net.core.wmem_max control maximum buffer sizes.","correct":"Throughput = window_size / RTT.","distractors":["TCP buffers out-of-order segments at the receiver.","Nagle's algorithm holds small TCP writes in a buffer until a full MSS is available or all outstanding data is ACKed, reducing small-packet overhead.","TCP Keep-Alive sends probe packets on idle connections to detect if the other end is still alive."]},{"id":"backend/udp_and_quic#11ff0456f19c","section":"backend","module":"backend/udp_and_quic","moduleName":"udp and quic","qIndex":1,"difficulty":"core","question":"What does UDP provide that IP does not?","answerFull":"UDP adds port numbers (multiplexing — distinguishing applications on the same host) and a checksum (error detection — verifying payload integrity). That is all. IP provides addressing and routing; UDP provides nothing beyond those two additions.","correct":"UDP adds port numbers (multiplexing — distinguishing applications on the same host) and a checksum (error detection — verifying payload integrity).","distractors":["UDP is connectionless, so the source IP in a packet is trivially spoofed.","QUIC connections are identified by a Connection ID, not by the IP:port 4-tuple.","DTLS (Datagram TLS) adapts TLS for UDP."]},{"id":"backend/udp_and_quic#29230f14fe10","section":"backend","module":"backend/udp_and_quic","moduleName":"udp and quic","qIndex":2,"difficulty":"core","question":"Why is UDP preferred for DNS?","answerFull":"DNS queries and responses typically fit in a single datagram. A DNS query takes 1 RTT over UDP — just the network round-trip time. TCP would add at least 1 RTT for the handshake before any data flows. For millions of DNS lookups per second, the latency and state overhead of TCP is unacceptable. Large DNSSEC responses use TCP as a fallback.","correct":"DNS queries and responses typically fit in a single datagram.","distractors":["DTLS (Datagram TLS) adapts TLS for UDP.","HTTP/2 multiplexes many streams over a single TCP connection.","HTTP/2 streams are application-level multiplexing inside a single TCP connection."]},{"id":"backend/udp_and_quic#dd50d424080a","section":"backend","module":"backend/udp_and_quic","moduleName":"udp and quic","qIndex":3,"difficulty":"core","question":"What is the head-of-line blocking problem in HTTP/2 over TCP?","answerFull":"HTTP/2 multiplexes many streams over a single TCP connection. If a TCP segment is lost, TCP's in-order delivery guarantee stalls all streams — even those whose data arrived successfully — until the lost segment is retransmitted and received. HTTP/3 over QUIC solves this: each QUIC stream is independent, so a lost packet only stalls the stream it belongs to.","correct":"HTTP/2 multiplexes many streams over a single TCP connection.","distractors":["recvmmsg() is a Linux system call that receives multiple datagrams in a single system call (batch receive).","On the first connection, QUIC completes a 1-RTT handshake.","QUIC connections are identified by a Connection ID, not by the IP:port 4-tuple."]},{"id":"backend/udp_and_quic#537c143d903e","section":"backend","module":"backend/udp_and_quic","moduleName":"udp and quic","qIndex":4,"difficulty":"core","question":"How does QUIC achieve 0-RTT connection establishment?","answerFull":"On the first connection, QUIC completes a 1-RTT handshake. The server sends a session ticket containing keying material. On subsequent connections from the same client, the client uses the cached ticket to encrypt early data (0-RTT data) and sends application data in the first packet, before the handshake completes. The server can process this data immediately (with replay protection considerations).","correct":"On the first connection, QUIC completes a 1-RTT handshake.","distractors":["HTTP/2 streams are application-level multiplexing inside a single TCP connection.","QUIC connections are identified by a Connection ID, not by the IP:port 4-tuple.","HTTP/2 multiplexes many streams over a single TCP connection."]},{"id":"backend/udp_and_quic#b1bc87ac4b53","section":"backend","module":"backend/udp_and_quic","moduleName":"udp and quic","qIndex":5,"difficulty":"core","question":"What is connection migration in QUIC and why does it matter?","answerFull":"QUIC connections are identified by a Connection ID, not by the IP:port 4-tuple. When a mobile client's IP changes (WiFi to LTE), the client sends a packet with the same Connection ID from the new address. The server validates the new path and migrates the connection without re-establishing TLS. For mobile users, this eliminates reconnect overhead on network switches.","correct":"QUIC connections are identified by a Connection ID, not by the IP:port 4-tuple.","distractors":["UDP adds port numbers (multiplexing — distinguishing applications on the same host) and a checksum (error detection — verifying payload integrity).","HTTP/2 streams are application-level multiplexing inside a single TCP connection.","HTTP/2 multiplexes many streams over a single TCP connection."]},{"id":"backend/udp_and_quic#1140007b79e9","section":"backend","module":"backend/udp_and_quic","moduleName":"udp and quic","qIndex":6,"difficulty":"intermediate","question":"What are the security implications of 0-RTT data?","answerFull":"0-RTT data can be replayed by an attacker who captures the ClientHello and reuses it. The server must treat 0-RTT data as idempotent — only safe operations (HTTP GET, HEAD) should be accepted. HTTP/3 prohibits 0-RTT for non-idempotent requests. Applications must not process 0-RTT data for state-changing operations without idempotency guarantees.","correct":"0-RTT data can be replayed by an attacker who captures the ClientHello and reuses it.","distractors":["On the first connection, QUIC completes a 1-RTT handshake.","DNS queries and responses typically fit in a single datagram.","DTLS (Datagram TLS) adapts TLS for UDP."]},{"id":"backend/udp_and_quic#438f104b62bc","section":"backend","module":"backend/udp_and_quic","moduleName":"udp and quic","qIndex":7,"difficulty":"intermediate","question":"What is DTLS and where is it used?","answerFull":"DTLS (Datagram TLS) adapts TLS for UDP. It adds replay protection (sequence numbers), retransmission for lost handshake messages, and record epoch tracking for out-of-order datagrams. DTLS is used in WebRTC for peer-to-peer data channels, SRTP key negotiation, and VPN solutions over UDP.","correct":"DTLS (Datagram TLS) adapts TLS for UDP.","distractors":["UDP is connectionless, so the source IP in a packet is trivially spoofed.","DNS queries and responses typically fit in a single datagram.","UDP adds port numbers (multiplexing — distinguishing applications on the same host) and a checksum (error detection — verifying payload integrity)."]},{"id":"backend/udp_and_quic#a1c5ad76ced0","section":"backend","module":"backend/udp_and_quic","moduleName":"udp and quic","qIndex":8,"difficulty":"intermediate","question":"How does UDP amplification work and how does QUIC prevent it?","answerFull":"UDP is connectionless, so the source IP in a packet is trivially spoofed. An attacker sends small UDP requests (e.g., DNS queries) with the victim's IP as the source. Servers respond with large replies to the victim, amplifying traffic. QUIC prevents amplification by limiting server-to-client traffic to 3x received bytes until the client's address is validated (via the handshake completing), preventing QUIC servers from being used as amplifiers.","correct":"UDP is connectionless, so the source IP in a packet is trivially spoofed.","distractors":["On the first connection, QUIC completes a 1-RTT handshake.","DTLS (Datagram TLS) adapts TLS for UDP.","UDP adds port numbers (multiplexing — distinguishing applications on the same host) and a checksum (error detection — verifying payload integrity)."]},{"id":"backend/udp_and_quic#4f976595434d","section":"backend","module":"backend/udp_and_quic","moduleName":"udp and quic","qIndex":9,"difficulty":"intermediate","question":"What is the difference between QUIC streams and HTTP/2 streams?","answerFull":"HTTP/2 streams are application-level multiplexing inside a single TCP connection. A single TCP segment loss stalls all HTTP/2 streams (TCP HoL blocking). QUIC streams are transport-level multiplexing — each stream is independently sequenced and acknowledged at the QUIC layer. A single QUIC packet loss only stalls the stream(s) whose data was in that packet. QUIC also provides per-stream and per-connection flow control independently.","correct":"HTTP/2 streams are application-level multiplexing inside a single TCP connection.","distractors":["UDP adds port numbers (multiplexing — distinguishing applications on the same host) and a checksum (error detection — verifying payload integrity).","QUIC connections are identified by a Connection ID, not by the IP:port 4-tuple.","recvmmsg() is a Linux system call that receives multiple datagrams in a single system call (batch receive)."]},{"id":"backend/udp_and_quic#0bd78fa72fdf","section":"backend","module":"backend/udp_and_quic","moduleName":"udp and quic","qIndex":11,"difficulty":"advanced","question":"How does recvmmsg() improve UDP server performance?","answerFull":"recvmmsg() is a Linux system call that receives multiple datagrams in a single system call (batch receive). For high-rate UDP applications (10+ Gbps network), context switch overhead of one system call per datagram is prohibitive. recvmmsg() with batch size 64 reduces system calls by 64x, significantly improving throughput. sendmmsg() provides the same benefit for batch sends.","correct":"recvmmsg() is a Linux system call that receives multiple datagrams in a single system call (batch receive).","distractors":["UDP adds port numbers (multiplexing — distinguishing applications on the same host) and a checksum (error detection — verifying payload integrity).","DNS queries and responses typically fit in a single datagram.","HTTP/2 multiplexes many streams over a single TCP connection."]},{"id":"backend/udp_and_quic#63f00ccbbcd1","section":"backend","module":"backend/udp_and_quic","moduleName":"udp and quic","qIndex":12,"difficulty":"advanced","question":"What is selective reliability and why do game engines implement it over UDP?","answerFull":"Game engines need to send high-frequency position updates (60–120 per second) where each update supersedes the previous. Reliable delivery of stale positions is useless and wasteful. But some events (player damage, item pickup, game state changes) must be delivered reliably. UDP allows game engines to implement selective reliability: unreliable delivery for frequent state updates, and a simple stop-and-wait or sliding window for critical events. This is more efficient than TCP, which would reliably deliver every stale position update.","correct":"Game engines need to send high-frequency position updates (60–120 per second) where each update supersedes the previous.","distractors":["DTLS (Datagram TLS) adapts TLS for UDP.","UDP adds port numbers (multiplexing — distinguishing applications on the same host) and a checksum (error detection — verifying payload integrity).","UDP is connectionless, so the source IP in a packet is trivially spoofed."]},{"id":"backend/websockets_and_sse#aafa474a01ce","section":"backend","module":"backend/websockets_and_sse","moduleName":"websockets and sse","qIndex":1,"difficulty":"core","question":"How does the WebSocket upgrade handshake work?","answerFull":"The client sends an HTTP/1.1 GET request with headers: Upgrade: websocket, Connection: Upgrade, Sec-WebSocket-Key: <random-base64>, Sec-WebSocket-Version: 13. The server responds with 101 Switching Protocols and Sec-WebSocket-Accept: <SHA1(key + GUID) base64>. The key computation proves the server understands WebSocket (not just reflecting headers). After 101, the TCP connection is reused as a WebSocket channel.","correct":"The client sends an HTTP/1.1 GET request with headers: Upgrade: websocket, Connection: Upgrade, Sec-WebSocket-Key: <random-base64>, Sec-WebSocket-Version: 13.","distractors":["SockJS is a JavaScript library that provides a WebSocket-like API but falls back to long polling, XHR streaming, or other transports when WebSocket is blocked by corporate proxies or firewalls.","WebSocket connections are stateful — a client is connected to a specific server.","Ping and Pong are WebSocket control frames (opcodes 9 and 10)."]},{"id":"backend/websockets_and_sse#12d40dc928f5","section":"backend","module":"backend/websockets_and_sse","moduleName":"websockets and sse","qIndex":2,"difficulty":"core","question":"What is the difference between WebSocket and SSE?","answerFull":"WebSocket is bidirectional — both client and server can send frames at any time. SSE is unidirectional — only the server can push events. SSE uses standard HTTP; browsers handle reconnection automatically via the EventSource API and Last-Event-ID. WebSocket requires manual reconnection logic. SSE works with HTTP/2 multiplexing natively; WebSocket does not (WebSocket over HTTP/2 is defined but rarely deployed). Use SSE for server-push-only scenarios; WebSocket for full-duplex communication.","correct":"WebSocket is bidirectional — both client and server can send frames at any time.","distractors":["WebSocket connections cannot send custom headers in the initial browser WebSocket API (the JavaScript API does not support custom headers on the Upgrade request).","Ping and Pong are WebSocket control frames (opcodes 9 and 10).","STOMP (Simple Text Oriented Messaging Protocol) is a simple message protocol with SEND, SUBSCRIBE, UNSUBSCRIBE, and ACK semantics."]},{"id":"backend/websockets_and_sse#325b20155fdb","section":"backend","module":"backend/websockets_and_sse","moduleName":"websockets and sse","qIndex":3,"difficulty":"core","question":"How do you scale WebSocket connections horizontally?","answerFull":"WebSocket connections are stateful — a client is connected to a specific server. Horizontal scaling requires either: (1) sticky sessions — load balancer routes all requests from a client to the same server (by IP or cookie); (2) shared pub/sub backend — each server publishes messages to Redis Pub/Sub, all servers subscribe, and each delivers to its locally connected clients. Option 2 is more resilient to server failure. Sticky sessions can cause imbalanced load if some connections are more active.","correct":"WebSocket connections are stateful — a client is connected to a specific server.","distractors":["Ping and Pong are WebSocket control frames (opcodes 9 and 10).","SockJS is a JavaScript library that provides a WebSocket-like API but falls back to long polling, XHR streaming, or other transports when WebSocket is blocked by corporate proxies or firewalls.","WebSocket connections cannot send custom headers in the initial browser WebSocket API (the JavaScript API does not support custom headers on the Upgrade request)."]},{"id":"backend/websockets_and_sse#0e10f1f2525b","section":"backend","module":"backend/websockets_and_sse","moduleName":"websockets and sse","qIndex":4,"difficulty":"core","question":"What is the WebSocket ping-pong mechanism?","answerFull":"Ping and Pong are WebSocket control frames (opcodes 9 and 10). Either party can send a Ping with up to 125 bytes of payload. The recipient must respond with a Pong containing the same payload as soon as possible. This serves as a keep-alive: if no Pong is received within a timeout, the connection is considered dead and closed. Spring's STOMP heartbeat uses STOMP-level heartbeats (not WS ping-pong) for higher-level keep-alive.","correct":"Ping and Pong are WebSocket control frames (opcodes 9 and 10).","distractors":["WebSocket is bidirectional — both client and server can send frames at any time.","Rate limit at the load balancer or API gateway: max WebSocket upgrade requests per IP per second.","SockJS is a JavaScript library that provides a WebSocket-like API but falls back to long polling, XHR streaming, or other transports when WebSocket is blocked by corporate proxies or firewalls."]},{"id":"backend/websockets_and_sse#9ac6a7144c43","section":"backend","module":"backend/websockets_and_sse","moduleName":"websockets and sse","qIndex":5,"difficulty":"core","question":"How does SSE automatic reconnection work?","answerFull":"The browser's EventSource API automatically reconnects when the connection closes. After reconnection, it sends the Last-Event-ID: <id> header with the id of the last received event. The server should use this to resume delivery from that point, preventing missed events. If the SSE stream includes a retry: <ms> field, the browser uses that interval for reconnection (default is a few seconds). This makes SSE inherently more reliable than WebSocket, which requires manual reconnection logic.","correct":"The browser's EventSource API automatically reconnects when the connection closes.","distractors":["SockJS is a JavaScript library that provides a WebSocket-like API but falls back to long polling, XHR streaming, or other transports when WebSocket is blocked by corporate proxies or firewalls.","The client sends an HTTP/1.1 GET request with headers: Upgrade: websocket, Connection: Upgrade, Sec-WebSocket-Key: <random-base64>, Sec-WebSocket-Version: 13.","WebSocket connections cannot send custom headers in the initial browser WebSocket API (the JavaScript API does not support custom headers on the Upgrade request)."]},{"id":"backend/websockets_and_sse#15e59f31de98","section":"backend","module":"backend/websockets_and_sse","moduleName":"websockets and sse","qIndex":6,"difficulty":"intermediate","question":"What is STOMP and why is it used over WebSocket?","answerFull":"STOMP (Simple Text Oriented Messaging Protocol) is a simple message protocol with SEND, SUBSCRIBE, UNSUBSCRIBE, and ACK semantics. By itself, WebSocket provides only a raw bidirectional channel — you must build message routing, pub/sub, and acknowledgment on top. STOMP adds this structure. Spring's SockJS/STOMP integration provides a complete pub/sub system over WebSocket, with message broker support (in-memory or external RabbitMQ/ActiveMQ).","correct":"STOMP (Simple Text Oriented Messaging Protocol) is a simple message protocol with SEND, SUBSCRIBE, UNSUBSCRIBE, and ACK semantics.","distractors":["WebSocket connections cannot send custom headers in the initial browser WebSocket API (the JavaScript API does not support custom headers on the Upgrade request).","Schema: WebSocket connection per document, authenticated during handshake.","WebSocket connections are stateful — a client is connected to a specific server."]},{"id":"backend/websockets_and_sse#e4f4d526851f","section":"backend","module":"backend/websockets_and_sse","moduleName":"websockets and sse","qIndex":7,"difficulty":"intermediate","question":"How do you authenticate WebSocket connections?","answerFull":"WebSocket connections cannot send custom headers in the initial browser WebSocket API (the JavaScript API does not support custom headers on the Upgrade request). Solutions: (1) send token as a URL query parameter (e.g., wss://api.example.com/ws?token=xxx) — validate in the server's handshake interceptor; (2) use a cookie for authentication (HTTP cookies are sent with the Upgrade request); (3) for STOMP, authenticate in the CONNECT frame (send token as a STOMP header) — the server validates before allowing subscriptions. Option 1 is most common despite token visibility in logs (use HTTPS/WSS to prevent interception).","correct":"WebSocket connections cannot send custom headers in the initial browser WebSocket API (the JavaScript API does not support custom headers on the Upgrade request).","distractors":["WebSocket connections are stateful — a client is connected to a specific server.","Ping and Pong are WebSocket control frames (opcodes 9 and 10).","The browser's EventSource API automatically reconnects when the connection closes."]},{"id":"backend/websockets_and_sse#b2b8eab19a68","section":"backend","module":"backend/websockets_and_sse","moduleName":"websockets and sse","qIndex":8,"difficulty":"intermediate","question":"What happens to a WebSocket connection when the server restarts?","answerFull":"A server restart closes all TCP connections (sends RST or FIN). The client receives a WebSocket Close frame or a TCP error. Without reconnection logic, the client's connection dies. The client must implement exponential backoff reconnection: retry after 1s, 2s, 4s, 8s, ... with a maximum interval. After reconnecting, the client must re-subscribe to channels/rooms. This is why SSE is often preferred — EventSource reconnection is automatic and built-in.","correct":"A server restart closes all TCP connections (sends RST or FIN).","distractors":["The client sends an HTTP/1.1 GET request with headers: Upgrade: websocket, Connection: Upgrade, Sec-WebSocket-Key: <random-base64>, Sec-WebSocket-Version: 13.","Rate limit at the load balancer or API gateway: max WebSocket upgrade requests per IP per second.","WebSocket is bidirectional — both client and server can send frames at any time."]},{"id":"backend/websockets_and_sse#4f6b188f687b","section":"backend","module":"backend/websockets_and_sse","moduleName":"websockets and sse","qIndex":9,"difficulty":"intermediate","question":"How do you prevent WebSocket connections from being used for DDoS?","answerFull":"Rate limit at the load balancer or API gateway: max WebSocket upgrade requests per IP per second. Rate limit at the application layer: max messages per second per connection. Validate authentication during the handshake — unauthenticated connections should be rejected immediately, not after messages arrive. Implement message size limits. Use connection-level rate limiters to disconnect clients sending too many messages. Monitor connection counts per IP.","correct":"Rate limit at the load balancer or API gateway: max WebSocket upgrade requests per IP per second.","distractors":["Schema: WebSocket connection per document, authenticated during handshake.","The client sends an HTTP/1.1 GET request with headers: Upgrade: websocket, Connection: Upgrade, Sec-WebSocket-Key: <random-base64>, Sec-WebSocket-Version: 13.","SockJS is a JavaScript library that provides a WebSocket-like API but falls back to long polling, XHR streaming, or other transports when WebSocket is blocked by corporate proxies or firewalls."]},{"id":"backend/websockets_and_sse#fc0bea776eb4","section":"backend","module":"backend/websockets_and_sse","moduleName":"websockets and sse","qIndex":10,"difficulty":"intermediate","question":"What is SockJS and when do you need it?","answerFull":"SockJS is a JavaScript library that provides a WebSocket-like API but falls back to long polling, XHR streaming, or other transports when WebSocket is blocked by corporate proxies or firewalls. Spring's SockJS integration automatically handles the negotiation. Use SockJS when supporting corporate network environments that block WebSocket. For modern consumer applications where WebSocket is universally available, SockJS adds unnecessary complexity.","correct":"SockJS is a JavaScript library that provides a WebSocket-like API but falls back to long polling, XHR streaming, or other transports when WebSocket is blocked by corporate proxies or firewalls.","distractors":["Schema: WebSocket connection per document, authenticated during handshake.","WebSocket connections cannot send custom headers in the initial browser WebSocket API (the JavaScript API does not support custom headers on the Upgrade request).","WebSocket connections are stateful — a client is connected to a specific server."]},{"id":"backend/websockets_and_sse#3f19d4422ad8","section":"backend","module":"backend/websockets_and_sse","moduleName":"websockets and sse","qIndex":11,"difficulty":"advanced","question":"How does Spring SseEmitter work and what are its lifecycle methods?","answerFull":"SseEmitter wraps a Server-Sent Events response. You return it from a controller method. The emitter stays open until: emitter.complete() is called (graceful end), emitter.completeWithError(Exception) is called (error), or the timeout expires (configured in the constructor). Lifecycle callbacks: onCompletion(), onTimeout(), onError() — use these to clean up references in registries. Sending events via emitter.send(event) is not thread-safe for concurrent sends — synchronize or use a single-thread sender.","correct":"SseEmitter wraps a Server-Sent Events response.","distractors":["Last-Event-ID is a header sent by the EventSource on reconnection containing the id field of the last event the client received.","A server restart closes all TCP connections (sends RST or FIN).","WebSocket is bidirectional — both client and server can send frames at any time."]},{"id":"backend/websockets_and_sse#fa3d56849890","section":"backend","module":"backend/websockets_and_sse","moduleName":"websockets and sse","qIndex":12,"difficulty":"advanced","question":"How would you design a live collaboration system using WebSocket?","answerFull":"Schema: WebSocket connection per document, authenticated during handshake. Client joins via STOMP SUBSCRIBE /topic/doc.{docId}. Edits sent to STOMP /app/doc.{docId}.edit. Server applies Operational Transforms (OT) or CRDTs, broadcasts transformed edit to /topic/doc.{docId}. Scaling: Redis Pub/Sub channel per document; any server instance can handle edits from any user. For conflict resolution: use CRDTs (e.g., Yjs, Automerge) client-side with eventual consistency rather than OT server-side. State recovery: when a user reconnects, fetch full document state via REST, then subscribe to WebSocket for incremental updates.","correct":"Schema: WebSocket connection per document, authenticated during handshake.","distractors":["Rate limit at the load balancer or API gateway: max WebSocket upgrade requests per IP per second.","SockJS is a JavaScript library that provides a WebSocket-like API but falls back to long polling, XHR streaming, or other transports when WebSocket is blocked by corporate proxies or firewalls.","The browser's EventSource API automatically reconnects when the connection closes."]},{"id":"backend/websockets_and_sse#cfb941fa7c6e","section":"backend","module":"backend/websockets_and_sse","moduleName":"websockets and sse","qIndex":13,"difficulty":"advanced","question":"What is the Last-Event-ID in SSE and how does it enable reliability?","answerFull":"Last-Event-ID is a header sent by the EventSource on reconnection containing the id field of the last event the client received. The server stores the current event ID and, on reconnection, can replay all events after the provided ID. This requires the server to buffer recent events (in memory or a message queue) for replay. Without this, events sent while the client was disconnected are lost. For high-reliability systems, buffer events for at least the maximum expected reconnection time (commonly 60–120 seconds).","correct":"Last-Event-ID is a header sent by the EventSource on reconnection containing the id field of the last event the client received.","distractors":["The browser's EventSource API automatically reconnects when the connection closes.","The client sends an HTTP/1.1 GET request with headers: Upgrade: websocket, Connection: Upgrade, Sec-WebSocket-Key: <random-base64>, Sec-WebSocket-Version: 13.","SseEmitter wraps a Server-Sent Events response."]}]