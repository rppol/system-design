[{"id":"lld/behavioral#94be856a476a","section":"lld","module":"lld/behavioral","moduleName":"behavioral","qIndex":1,"difficulty":"core","question":"Strategy vs State: they look identical structurally — what is the actual difference?","answerFull":"In Strategy, the client chooses which strategy to use and the strategy object never changes the selection itself. In State, the state object decides its own transitions — the object changes its own currentState reference without client involvement. Strategy = external algorithm selection by the caller; State = object's internal lifecycle driven by the state objects themselves. The interview gotcha: drawing a UML of either pattern produces an identical diagram — the difference is only in who holds a reference to the next strategy/state and who has the authority to change it.","correct":"In Strategy, the client chooses which strategy to use and the strategy object never changes the selection itself.","distractors":["Deep copies of complex object graphs are expensive in both memory and CPU.","Java uses single dispatch — the method that runs is determined solely by the runtime type of the receiver object, but the overload selected is determined at compile time by the declared type of the argument.","Each state is a class; transitions are methods on the state class (or the context delegates to the state, which returns the next state object)."]},{"id":"lld/behavioral#2450fa539110","section":"lld","module":"lld/behavioral","moduleName":"behavioral","qIndex":2,"difficulty":"core","question":"Observer vs Mediator: both handle communication between objects — when do you pick each?","answerFull":"Observer is 1-to-many: a subject broadcasts to all subscribers without knowing who they are. Mediator is many-to-many: all participants communicate through a central hub, and no participant knows about the others. Use Observer for event notification where subscribers react independently (UI refresh, domain events). Use Mediator when N objects all need to communicate with each other — the alternative is N-squared direct connections; Mediator reduces that to N connections to the hub. The failure mode for overusing Observer is N objects each subscribing to each other, which is the symptom that tells you to switch to Mediator.","correct":"Observer is 1-to-many: a subject broadcasts to all subscribers without knowing who they are.","distractors":["Mediator reduces N² connections to N, but if the mediator handles too many peers, it centralizes all coordination logic in one class that knows about every participant's behavior — violating SRP.","ApplicationEvent is the event object (the subject's state change expressed as a value).","Chain of Responsibility breaks down in three ways."]},{"id":"lld/behavioral#9d82fa536978","section":"lld","module":"lld/behavioral","moduleName":"behavioral","qIndex":3,"difficulty":"core","question":"Command vs Strategy: both encapsulate behavior in an object — what is different?","answerFull":"Strategy encapsulates an algorithm to be used right now — sort this list using this comparator. Command encapsulates an action to be executed later: it carries the receiver, the action name, and its parameters as a self-contained unit. Command enables undo (store the inverse operation), queuing (pass the Command to an ExecutorService), logging (serialize the Command), and macro recording (replay a sequence of Commands). Strategy has none of these concerns — it is stateless and side-effect-free by contract. If you find yourself needing to store or replay behavior, you need Command, not Strategy.","correct":"Strategy encapsulates an algorithm to be used right now — sort this list using this comparator.","distractors":["Interpreter requires a grammar tree where each node is an object, so parsing a large expression creates a deep object graph — memory-intensive and slow to evaluate.","Deep copies of complex object graphs are expensive in both memory and CPU.","Mediator reduces N² connections to N, but if the mediator handles too many peers, it centralizes all coordination logic in one class that knows about every participant's behavior — violating SRP."]},{"id":"lld/behavioral#0767a6add2f7","section":"lld","module":"lld/behavioral","moduleName":"behavioral","qIndex":4,"difficulty":"core","question":"Template Method vs Strategy: both vary part of an algorithm — which and when?","answerFull":"Template Method uses inheritance: the abstract base class defines the algorithm skeleton and calls hook methods that subclasses override. Strategy uses composition: the context holds a strategy reference that can be swapped at runtime without changing the context class. Effective Java Item 18 prefers composition (Strategy) over inheritance (Template Method): inheritance creates tight coupling to the base class, makes the subclass fragile when the base changes, and makes unit testing harder because you cannot mock an abstract parent. Prefer Template Method only when the framework forces it (e.g., HttpServlet) or when the hook methods form a cohesive algorithm that makes no sense separated from the skeleton.","correct":"Template Method uses inheritance: the abstract base class defines the algorithm skeleton and calls hook methods that subclasses override.","distractors":["In Strategy, the client chooses which strategy to use and the strategy object never changes the selection itself.","Java uses single dispatch — the method that runs is determined solely by the runtime type of the receiver object, but the overload selected is determined at compile time by the declared type of the argument.","Strategy encapsulates an algorithm to be used right now — sort this list using this comparator."]},{"id":"lld/behavioral#f95f0ddbfad7","section":"lld","module":"lld/behavioral","moduleName":"behavioral","qIndex":5,"difficulty":"core","question":"When does Chain of Responsibility become an antipattern?","answerFull":"Chain of Responsibility breaks down in three ways. First, silent drops: no default/terminal handler exists, so unmatched requests fall off the end of the chain without error — a bug that is almost impossible to detect in logs. Second, ordering bugs: handlers are assembled in the wrong order (authentication check placed after business logic, or a logging handler placed before auth so it logs unauthenticated requests). Third, dynamic chain construction that makes ordering non-deterministic. Fix: always include a terminal handler that either processes or explicitly rejects; make chain order part of explicit configuration; write an integration test that sends an unrecognized request and asserts on the rejection.","correct":"Chain of Responsibility breaks down in three ways.","distractors":["ApplicationEvent is the event object (the subject's state change expressed as a value).","Deep copies of complex object graphs are expensive in both memory and CPU.","Each state is a class; transitions are methods on the state class (or the context delegates to the state, which returns the next state object)."]},{"id":"lld/behavioral#a14c238a3d45","section":"lld","module":"lld/behavioral","moduleName":"behavioral","qIndex":6,"difficulty":"intermediate","question":"How does Java's Iterator protocol implement the Iterator pattern?","answerFull":"java.util.Iterator with hasNext() and next() is a textbook Iterator. Java's enhanced for-each desugars to: Iterator<T> it = collection.iterator(); while (it.hasNext()) { T e = it.next(); ... }. The pattern separates traversal state from the collection, so you can have multiple concurrent iterators over the same collection, each with its own cursor. The primary pitfall is ConcurrentModificationException: if the collection is structurally modified (add/remove) while iterating, the iterator detects the modification via a modCount check and throws. Fix: use Iterator.remove() (the only safe remove during iteration), copy the collection before iterating, or use a concurrent collection such as CopyOnWriteArrayList.","correct":"java.util.Iterator with hasNext() and next() is a textbook Iterator.","distractors":["Each state is a class; transitions are methods on the state class (or the context delegates to the state, which returns the next state object).","Iterator is external iteration — the caller drives hasNext()/next() and controls when to advance.","Java uses single dispatch — the method that runs is determined solely by the runtime type of the receiver object, but the overload selected is determined at compile time by the declared type of the argument."]},{"id":"lld/behavioral#4969b951e1ee","section":"lld","module":"lld/behavioral","moduleName":"behavioral","qIndex":7,"difficulty":"intermediate","question":"How does Spring ApplicationEvent implement Observer?","answerFull":"ApplicationEvent is the event object (the subject's state change expressed as a value). ApplicationEventPublisher.publishEvent() is the notify call. @EventListener methods are the observers — Spring registers them via classpath scanning and reflection at startup. Key difference from raw Observer: the publisher does not hold a list of listeners; the ApplicationContext acts as the event bus and maintains the registry, so the publisher is decoupled from all subscribers. Async variant: annotate with @Async @EventListener to run the listener in a separate thread pool thread, keeping the publisher thread unblocked. Pitfall: without @Async, a slow listener blocks the publisher synchronously.","correct":"ApplicationEvent is the event object (the subject's state change expressed as a value).","distractors":["Observer is 1-to-many: a subject broadcasts to all subscribers without knowing who they are.","Deep copies of complex object graphs are expensive in both memory and CPU.","In Strategy, the client chooses which strategy to use and the strategy object never changes the selection itself."]},{"id":"lld/behavioral#48cd245c171d","section":"lld","module":"lld/behavioral","moduleName":"behavioral","qIndex":8,"difficulty":"intermediate","question":"Memento with a large object graph — what breaks?","answerFull":"Deep copies of complex object graphs are expensive in both memory and CPU. If the graph contains non-serializable fields, open I/O streams, references to external resources, or circular references, Cloneable and manual copying both fail silently or incompletely. Storing one Memento per action in a long-lived undo history causes memory bloat proportional to object size times history depth. Fix: snapshot only the relevant state fields rather than the full object; use Command with undo() instead (stores the delta — what changed — rather than a full snapshot); or adopt Event Sourcing for the large-scale equivalent, where the current state is always derived by replaying a log of events.","correct":"Deep copies of complex object graphs are expensive in both memory and CPU.","distractors":["ApplicationEvent is the event object (the subject's state change expressed as a value).","In Strategy, the client chooses which strategy to use and the strategy object never changes the selection itself.","Java uses single dispatch — the method that runs is determined solely by the runtime type of the receiver object, but the overload selected is determined at compile time by the declared type of the argument."]},{"id":"lld/behavioral#46afbcbd7ae7","section":"lld","module":"lld/behavioral","moduleName":"behavioral","qIndex":9,"difficulty":"intermediate","question":"Visitor pattern: why is double-dispatch needed?","answerFull":"Java uses single dispatch — the method that runs is determined solely by the runtime type of the receiver object, but the overload selected is determined at compile time by the declared type of the argument. In a hierarchy of Shape, Circle, Rectangle, calling visitor.visit(shape) where shape is declared as Shape always calls visit(Shape), even at runtime when shape is actually a Circle. Double-dispatch solves this: the element calls accept(visitor), and inside accept() — where this is the concrete type known to the compiler — calls visitor.visit(this). Now the correct overload (visit(Circle) or visit(Rectangle)) is resolved. Without double-dispatch, the Visitor must use instanceof chains, eliminating the type-safety benefit of the pattern.","correct":"Java uses single dispatch — the method that runs is determined solely by the runtime type of the receiver object, but the overload selected is determined at compile time by the declared type of the argument.","distractors":["Interpreter requires a grammar tree where each node is an object, so parsing a large expression creates a deep object graph — memory-intensive and slow to evaluate.","Template Method uses inheritance: the abstract base class defines the algorithm skeleton and calls hook methods that subclasses override.","Deep copies of complex object graphs are expensive in both memory and CPU."]},{"id":"lld/behavioral#67e5d8227bf6","section":"lld","module":"lld/behavioral","moduleName":"behavioral","qIndex":11,"difficulty":"advanced","question":"How does the State pattern handle complex FSMs with many states?","answerFull":"Each state is a class; transitions are methods on the state class (or the context delegates to the state, which returns the next state object). For 5 to 10 states this is clean and readable. For 50 or more states the class explosion makes the codebase unmanageable — 50 classes, each with transition methods for every possible event. Alternatives: encode the FSM as a state-transition table (a Map<State, Map<Event, Transition>> drives the engine); use a library (Spring State Machine for complex hierarchical FSMs, Stateless4j for lightweight ones); or define the FSM in a configuration file and generate the state classes. The State pattern shines when each state has complex behavior with many methods — it is overkill for simple flag-based or two-state logic.","correct":"Each state is a class; transitions are methods on the state class (or the context delegates to the state, which returns the next state object).","distractors":["Interpreter requires a grammar tree where each node is an object, so parsing a large expression creates a deep object graph — memory-intensive and slow to evaluate.","In Strategy, the client chooses which strategy to use and the strategy object never changes the selection itself.","Iterator is external iteration — the caller drives hasNext()/next() and controls when to advance."]},{"id":"lld/behavioral#c768a6eea905","section":"lld","module":"lld/behavioral","moduleName":"behavioral","qIndex":12,"difficulty":"advanced","question":"Iterator vs Stream in modern Java — when to use each?","answerFull":"Iterator is external iteration — the caller drives hasNext()/next() and controls when to advance. Stream is internal iteration — the stream library drives the pipeline, and the caller provides functions describing what to do at each step. Use Iterator when you need to pause traversal mid-way (lazy producer/consumer handoff, cooperative multitasking, generators), early-exit with complex stateful conditions, or resume iteration across method boundaries. Use Stream for functional pipelines (filter/map/reduce/collect) — it is more composable, concise, and can be parallelized with parallelStream(). The key pitfall: a Stream is single-use and cannot be reset or restarted after terminal operation; an Iterator can be partially consumed, stored, and resumed across calls.","correct":"Iterator is external iteration — the caller drives hasNext()/next() and controls when to advance.","distractors":["Java uses single dispatch — the method that runs is determined solely by the runtime type of the receiver object, but the overload selected is determined at compile time by the declared type of the argument.","Each state is a class; transitions are methods on the state class (or the context delegates to the state, which returns the next state object).","java.util.Iterator with hasNext() and next() is a textbook Iterator."]},{"id":"lld/behavioral#7541004520c2","section":"lld","module":"lld/behavioral","moduleName":"behavioral","qIndex":14,"difficulty":"advanced","question":"When does the Mediator pattern turn into a God Object?","answerFull":"Mediator reduces N² connections to N, but if the mediator handles too many peers, it centralizes all coordination logic in one class that knows about every participant's behavior — violating SRP. Signs: the mediator has dozens of if (sender instanceof X) branches, participants call the mediator for things that don't need coordination, and modifying any participant requires modifying the mediator. Fix: extract sub-mediators by domain boundary; or replace peer-to-peer coordination with domain events (Observer) so the mediator doesn't need to know about every peer.","correct":"Mediator reduces N² connections to N, but if the mediator handles too many peers, it centralizes all coordination logic in one class that knows about every participant's behavior — violating SRP.","distractors":["In Strategy, the client chooses which strategy to use and the strategy object never changes the selection itself.","Each state is a class; transitions are methods on the state class (or the context delegates to the state, which returns the next state object).","ApplicationEvent is the event object (the subject's state change expressed as a value)."]},{"id":"lld/behavioral#7862421d4b61","section":"lld","module":"lld/behavioral","moduleName":"behavioral","qIndex":15,"difficulty":"advanced","question":"Why is Interpreter the least-used GoF pattern in production systems?","answerFull":"Interpreter requires a grammar tree where each node is an object, so parsing a large expression creates a deep object graph — memory-intensive and slow to evaluate. Modern alternatives: use a parsing library (ANTLR, PEG parsers) that produces a compact AST and separates grammar from evaluation; or use an existing expression language (SpEL, JEXL, Aviator) rather than building your own. Interpreter is appropriate only for very small, fixed grammars (e.g., a 5-operator calculator, a simple boolean rule engine) where the simplicity of the implementation outweighs the performance cost.","correct":"Interpreter requires a grammar tree where each node is an object, so parsing a large expression creates a deep object graph — memory-intensive and slow to evaluate.","distractors":["ApplicationEvent is the event object (the subject's state change expressed as a value).","Deep copies of complex object graphs are expensive in both memory and CPU.","Each state is a class; transitions are methods on the state class (or the context delegates to the state, which returns the next state object)."]},{"id":"lld/behavioral#07a9155cbbd7","section":"lld","module":"lld/behavioral","moduleName":"behavioral","qIndex":16,"difficulty":"advanced","question":"Memento vs Event Sourcing — same idea, different scale?","answerFull":"Yes, and understanding the distinction is important. Memento captures and restores the state of a single object at interaction time (undo/redo in a text editor). Event Sourcing captures every state change across an entire system as an append-only event log, from which any past state can be reconstructed. Memento is a local, in-memory pattern; Event Sourcing is a distributed systems architecture. Both share the insight that storing history (rather than current state) enables time-travel. If you need Memento across a distributed system, you're building Event Sourcing.","correct":"Yes, and understanding the distinction is important.","distractors":["ApplicationEvent is the event object (the subject's state change expressed as a value).","Chain of Responsibility breaks down in three ways.","Deep copies of complex object graphs are expensive in both memory and CPU."]},{"id":"lld/concurrency_patterns#55f87a253228","section":"lld","module":"lld/concurrency_patterns","moduleName":"concurrency patterns","qIndex":2,"difficulty":"core","question":"Enum Singleton vs Holder idiom vs DCL + volatile — which is \"correct\" and when do you use each?","answerFull":"Enum (Effective Java Item 3) is the definitive answer: thread-safe by JVM guarantee, handles serialization and reflection attacks automatically, and is the simplest. Holder idiom (private static class Holder) is the preferred choice when enum is not possible (e.g., the class has to extend another class, or the singleton must implement a more complex initialization). DCL + volatile is correct but verbose — it exists for cases where the singleton must be lazily initialized AND the class cannot use enum or Holder for some reason. In practice: enum if possible; Holder if you need a class-based singleton.","correct":"Enum (Effective Java Item 3) is the definitive answer: thread-safe by JVM guarantee, handles serialization and reflection attacks automatically, and is the simplest.","distractors":["Use an Enum singleton. It satisfies all three requirements natively.","Java's serialization mechanism uses Enum.valueOf(enumClass, name) to deserialize enum constants instead of calling the constructor.","A write to a volatile variable happens-before all subsequent reads of that variable."]},{"id":"lld/concurrency_patterns#34f1fa862e3d","section":"lld","module":"lld/concurrency_patterns","moduleName":"concurrency patterns","qIndex":5,"difficulty":"core","question":"What is write starvation in Read-Write Lock and how does the fairness parameter fix it?","answerFull":"In an unfair ReentrantReadWriteLock, new read lock requests can be granted even when a writer is waiting — because readers don't block other readers. If reads are continuous, writers wait indefinitely (starvation). Fix: new ReentrantReadWriteLock(true) (fair mode) uses a FIFO ordering: once a writer is waiting, new readers must queue behind it. Cost: throughput drops because reads that could be concurrent are now serialized behind the waiting writer. StampedLock provides optimistic reads as an alternative: read without acquiring the lock, then validate; if invalid, upgrade to a full read lock.","correct":"In an unfair ReentrantReadWriteLock, new read lock requests can be granted even when a writer is waiting — because readers don't block other readers.","distractors":["Singleton and Read-Write Lock: still fully relevant — virtual threads have the same memory visibility concerns and can still race on shared state.","A write to a volatile variable happens-before all subsequent reads of that variable.","When a virtual thread enters a synchronized block, it is pinned to its carrier platform thread for the duration of the block."]},{"id":"lld/concurrency_patterns#1b902a0c2e48","section":"lld","module":"lld/concurrency_patterns","moduleName":"concurrency patterns","qIndex":6,"difficulty":"intermediate","question":"Thread pool rejection policies — when does each apply?","answerFull":"AbortPolicy (default): throws RejectedExecutionException. Use when the caller must know the task was rejected. CallerRunsPolicy: the submitting thread executes the task directly, providing natural backpressure (the caller slows down). Use when you'd rather slow the producer than lose tasks. DiscardPolicy: silently drops the task. Use only when tasks are truly expendable (e.g., monitoring samples). DiscardOldestPolicy: drops the oldest queued task and retries. Use when newest tasks are more valuable than oldest (e.g., real-time position updates).","correct":"AbortPolicy (default): throws RejectedExecutionException.","distractors":["Enum (Effective Java Item 3) is the definitive answer: thread-safe by JVM guarantee, handles serialization and reflection attacks automatically, and is the simplest.","When a virtual thread enters a synchronized block, it is pinned to its carrier platform thread for the duration of the block.","A write to a volatile variable happens-before all subsequent reads of that variable."]},{"id":"lld/concurrency_patterns#4b6c7e37ec1b","section":"lld","module":"lld/concurrency_patterns","moduleName":"concurrency patterns","qIndex":7,"difficulty":"intermediate","question":"Virtual threads (Java 21): do these concurrency patterns still apply?","answerFull":"Singleton and Read-Write Lock: still fully relevant — virtual threads have the same memory visibility concerns and can still race on shared state. Producer-Consumer backpressure: still needed for CPU-bound consumers (virtual threads don't help with CPU saturation). Thread Pool: partially replaced — for I/O-bound tasks, a virtual thread per task is now viable. Executors.newVirtualThreadPerTaskExecutor() replaces a fixed thread pool for I/O tasks. But for CPU-bound work with known parallelism needs, a fixed platform-thread pool (sized to CPU cores) is still preferred.","correct":"Singleton and Read-Write Lock: still fully relevant — virtual threads have the same memory visibility concerns and can still race on shared state.","distractors":["A write to a volatile variable happens-before all subsequent reads of that variable.","Enum (Effective Java Item 3) is the definitive answer: thread-safe by JVM guarantee, handles serialization and reflection attacks automatically, and is the simplest.","When a virtual thread enters a synchronized block, it is pinned to its carrier platform thread for the duration of the block."]},{"id":"lld/concurrency_patterns#cb28e267b612","section":"lld","module":"lld/concurrency_patterns","moduleName":"concurrency patterns","qIndex":8,"difficulty":"intermediate","question":"How does BlockingQueue implement both the Producer-Consumer and the backpressure patterns?","answerFull":"BlockingQueue.put() blocks the producer when the queue is at capacity. BlockingQueue.take() blocks the consumer when the queue is empty. The bounded capacity (new ArrayBlockingQueue<>(1000)) acts as a buffer: it absorbs speed bursts. When the buffer is full, put() blocks — the producer slows to consumer speed automatically (backpressure). offer(item, timeout) gives producers a timeout to avoid indefinite blocking. LinkedBlockingQueue with no capacity limit is an unbounded queue — it provides buffering but no backpressure and can grow until OOM under sustained producer excess.","correct":"BlockingQueue.put() blocks the producer when the queue is at capacity.","distractors":["Holder relies on JVM class loading (which is inherently synchronized) rather than explicit synchronized blocks or volatile.","A write to a volatile variable happens-before all subsequent reads of that variable.","AbortPolicy (default): throws RejectedExecutionException."]},{"id":"lld/concurrency_patterns#6066fb40c053","section":"lld","module":"lld/concurrency_patterns","moduleName":"concurrency patterns","qIndex":9,"difficulty":"intermediate","question":"ForkJoinPool vs ThreadPoolExecutor — when do you use each?","answerFull":"ThreadPoolExecutor: general-purpose; best for independent tasks of uniform size (HTTP requests, DB queries, event processing). ForkJoinPool: designed for divide-and-conquer tasks that recursively fork subtasks and join results. Uses work-stealing: idle threads steal tasks from busy threads' queues, keeping all CPUs busy even when subtask sizes vary. ForkJoinPool.commonPool() backs CompletableFuture.supplyAsync() and parallel streams. Use ForkJoinPool for recursive computation (merge sort, tree traversal, parallel streams); use ThreadPoolExecutor for independent task execution.","correct":"ThreadPoolExecutor: general-purpose; best for independent tasks of uniform size (HTTP requests, DB queries, event processing).","distractors":["In an unfair ReentrantReadWriteLock, new read lock requests can be granted even when a writer is waiting — because readers don't block other readers.","A write to a volatile variable happens-before all subsequent reads of that variable.","AbortPolicy (default): throws RejectedExecutionException."]},{"id":"lld/concurrency_patterns#f707664baa5e","section":"lld","module":"lld/concurrency_patterns","moduleName":"concurrency patterns","qIndex":11,"difficulty":"advanced","question":"What is the synchronized pinning problem with virtual threads and how do you avoid it?","answerFull":"When a virtual thread enters a synchronized block, it is pinned to its carrier platform thread for the duration of the block. If the virtual thread then blocks on I/O inside the synchronized block, the carrier platform thread is also blocked — eliminating the scalability benefit of virtual threads. On Java 21, this means a synchronized-heavy codebase does not benefit from newVirtualThreadPerTaskExecutor(). Fix: replace synchronized with ReentrantLock. ReentrantLock.lock()/unlock() allows the virtual thread to unmount from the carrier while waiting, freeing the carrier to run other virtual threads.","correct":"When a virtual thread enters a synchronized block, it is pinned to its carrier platform thread for the duration of the block.","distractors":["Enum (Effective Java Item 3) is the definitive answer: thread-safe by JVM guarantee, handles serialization and reflection attacks automatically, and is the simplest.","Singleton and Read-Write Lock: still fully relevant — virtual threads have the same memory visibility concerns and can still race on shared state.","In an unfair ReentrantReadWriteLock, new read lock requests can be granted even when a writer is waiting — because readers don't block other readers."]},{"id":"lld/concurrency_patterns#b55298ce22d2","section":"lld","module":"lld/concurrency_patterns","moduleName":"concurrency patterns","qIndex":1,"difficulty":"core","question":"Why is DCL broken without volatile?","answerFull":"Without volatile, the JVM can reorder object construction steps so the reference is published before the constructor runs. Another thread reading the non-null reference gets a partially constructed object.","correct":"Without volatile, the JVM can reorder object construction steps so the reference is published before the constructor runs.","distractors":["Java's serialization mechanism uses Enum.valueOf(enumClass, name) to deserialize enum constants instead of calling the constructor.","Holder relies on JVM class loading (which is inherently synchronized) rather than explicit synchronized blocks or volatile.","Enum (Effective Java Item 3) is the definitive answer: thread-safe by JVM guarantee, handles serialization and reflection attacks automatically, and is the simplest."]},{"id":"lld/concurrency_patterns#d863fa864bea","section":"lld","module":"lld/concurrency_patterns","moduleName":"concurrency patterns","qIndex":2,"difficulty":"core","question":"What makes the Holder idiom better than DCL?","answerFull":"Holder relies on JVM class loading (which is inherently synchronized) rather than explicit synchronized blocks or volatile. It achieves the same thread safety with no runtime overhead and is harder to implement incorrectly.","correct":"Holder relies on JVM class loading (which is inherently synchronized) rather than explicit synchronized blocks or volatile.","distractors":["A write to a volatile variable happens-before all subsequent reads of that variable.","BlockingQueue.put() blocks the producer when the queue is at capacity.","When a virtual thread enters a synchronized block, it is pinned to its carrier platform thread for the duration of the block."]},{"id":"lld/concurrency_patterns#83f69c04f385","section":"lld","module":"lld/concurrency_patterns","moduleName":"concurrency patterns","qIndex":3,"difficulty":"core","question":"How does Enum prevent serialization attacks?","answerFull":"Java's serialization mechanism uses Enum.valueOf(enumClass, name) to deserialize enum constants instead of calling the constructor. This ensures the existing constant is returned, never creating a new instance.","correct":"Java's serialization mechanism uses Enum.valueOf(enumClass, name) to deserialize enum constants instead of calling the constructor.","distractors":["Without volatile, the JVM can reorder object construction steps so the reference is published before the constructor runs.","Use an Enum singleton. It satisfies all three requirements natively.","Enum (Effective Java Item 3) is the definitive answer: thread-safe by JVM guarantee, handles serialization and reflection attacks automatically, and is the simplest."]},{"id":"lld/concurrency_patterns#3bea62945200","section":"lld","module":"lld/concurrency_patterns","moduleName":"concurrency patterns","qIndex":4,"difficulty":"core","question":"Can you break an Enum singleton via reflection?","answerFull":"No. Constructor.newInstance() explicitly checks if the class is an enum and throws IllegalArgumentException. This is enforced by the JVM, not user code.","correct":"No. Constructor.newInstance() explicitly checks if the class is an enum and throws IllegalArgumentException.","distractors":["Without volatile, the JVM can reorder object construction steps so the reference is published before the constructor runs.","Use an Enum singleton. It satisfies all three requirements natively.","Holder relies on JVM class loading (which is inherently synchronized) rather than explicit synchronized blocks or volatile."]},{"id":"lld/concurrency_patterns#193feeefb981","section":"lld","module":"lld/concurrency_patterns","moduleName":"concurrency patterns","qIndex":5,"difficulty":"core","question":"What is the happens-before guarantee provided by volatile?","answerFull":"A write to a volatile variable happens-before all subsequent reads of that variable. All actions before a volatile write are visible to all threads after they read the volatile variable. This establishes a memory barrier that prevents both reordering and CPU cache staleness.","correct":"A write to a volatile variable happens-before all subsequent reads of that variable.","distractors":["Singleton and Read-Write Lock: still fully relevant — virtual threads have the same memory visibility concerns and can still race on shared state.","Enum (Effective Java Item 3) is the definitive answer: thread-safe by JVM guarantee, handles serialization and reflection attacks automatically, and is the simplest.","Holder relies on JVM class loading (which is inherently synchronized) rather than explicit synchronized blocks or volatile."]},{"id":"lld/concurrency_patterns#05b83b4cc05f","section":"lld","module":"lld/concurrency_patterns","moduleName":"concurrency patterns","qIndex":6,"difficulty":"intermediate","question":"How would you implement a singleton that supports lazy initialization AND is safe under serialization AND reflection?","answerFull":"Use an Enum singleton. It satisfies all three requirements natively. Alternatively, use the Holder idiom with readResolve() for serialization safety (but reflection safety requires additional checks in the constructor: throw new IllegalStateException(\"Already instantiated\")).","correct":"Use an Enum singleton. It satisfies all three requirements natively.","distractors":["Singleton and Read-Write Lock: still fully relevant — virtual threads have the same memory visibility concerns and can still race on shared state.","Enum (Effective Java Item 3) is the definitive answer: thread-safe by JVM guarantee, handles serialization and reflection attacks automatically, and is the simplest.","Java's serialization mechanism uses Enum.valueOf(enumClass, name) to deserialize enum constants instead of calling the constructor."]},{"id":"lld/creational#5b4916f125f8","section":"lld","module":"lld/creational","moduleName":"creational","qIndex":2,"difficulty":"core","question":"Thread-safe Singleton: DCL + volatile, enum, or Holder idiom — which is correct?","answerFull":"All three are correct in Java 5+, but enum is the preferred idiom (Effective Java Item 3): it handles serialization automatically, prevents reflection-based instantiation, and requires no volatile. The Holder idiom (static class Holder { static final X INSTANCE = new X(); }) achieves lazy initialization via class-loading semantics without synchronization overhead. DCL + volatile is correct but verbose. Never use DCL without volatile — without it, the JVM's instruction reordering can publish a partially-constructed instance.","correct":"All three are correct in Java 5+, but enum is the preferred idiom (Effective Java Item 3): it handles serialization automatically, prevents reflection-based instantiation, and requires no volatile.","distractors":["Enum serialization is handled by the JVM itself — the serialized form is just the enum name, and deserialization always returns the existing instance via Enum.valueOf().","Cloneable is a marker interface that changes the behavior of Object.clone() — but clone() is defined on Object, not Cloneable, so there's no type contract.","Spring beans are singleton-scoped by default."]},{"id":"lld/creational#ca9dd4a9c806","section":"lld","module":"lld/creational","moduleName":"creational","qIndex":3,"difficulty":"core","question":"What is the \"Cloneable problem\" in Java, and how does Prototype avoid it?","answerFull":"Cloneable is a marker interface that changes the behavior of Object.clone() — but clone() is defined on Object, not Cloneable, so there's no type contract. clone() performs a shallow copy by default (references are copied, not objects), throws CloneNotSupportedException (a checked exception on a protected method), and requires covariant return type overriding to be usable. The idiomatic Java fix: use a copy constructor (new Foo(Foo other)) or a static factory (Foo.copyOf(Foo other)). Both give explicit control over deep vs shallow copy without the Cloneable contract problems.","correct":"Cloneable is a marker interface that changes the behavior of Object.clone() — but clone() is defined on Object, not Cloneable, so there's no type contract.","distractors":["An object pool maintains a collection of pre-initialized objects (prototypes) and leases them to callers.","All three are correct in Java 5+, but enum is the preferred idiom (Effective Java Item 3): it handles serialization automatically, prevents reflection-based instantiation, and requires no volatile.","Strategy selects an algorithm (behavior)."]},{"id":"lld/creational#ee3ee32e4ad6","section":"lld","module":"lld/creational","moduleName":"creational","qIndex":4,"difficulty":"core","question":"Factory Method vs Abstract Factory — when does a factory of factories make sense?","answerFull":"Use Abstract Factory when you need to create families of related objects that must be compatible with each other. Example: a UI library with Button, Checkbox, and Dialog — you want either all Windows-style or all Linux-style widgets, never a mix. Factory Method creates ONE type of product; the subclass decides the concrete class. Abstract Factory creates MULTIPLE types of products that belong to the same family. Abstract Factory is often implemented by composing multiple Factory Methods.","correct":"Use Abstract Factory when you need to create families of related objects that must be compatible with each other.","distractors":["An object pool maintains a collection of pre-initialized objects (prototypes) and leases them to callers.","Abstract Factory defines the set of products it creates (e.g., createButton(), createCheckbox()).","Shallow copy copies references, not the referenced objects."]},{"id":"lld/creational#da70b13b48c9","section":"lld","module":"lld/creational","moduleName":"creational","qIndex":6,"difficulty":"intermediate","question":"What is the static factory method idiom (Effective Java Item 1) and how does it differ from GoF Factory Method?","answerFull":"Static factory method (Item 1) is naming a static method of(), from(), valueOf(), newInstance(), etc. to create instances — LocalDate.of(2024, 1, 1), Optional.of(x), List.of(...). Benefits: descriptive names, ability to return cached instances, can return a subtype. GoF Factory Method is a class-hierarchy pattern: a base class defines a createProduct() method that subclasses override to vary the concrete product type. These are unrelated patterns that share a naming similarity.","correct":"Static factory method (Item 1) is naming a static method of(), from(), valueOf(), newInstance(), etc. to create instances — LocalDate.of(2024, 1, 1), Optional.of(x), List.of(...).","distractors":["Use Abstract Factory when you need to create families of related objects that must be compatible with each other.","Abstract Factory defines the set of products it creates (e.g., createButton(), createCheckbox()).","Enum serialization is handled by the JVM itself — the serialized form is just the enum name, and deserialization always returns the existing instance via Enum.valueOf()."]},{"id":"lld/creational#c15674068895","section":"lld","module":"lld/creational","moduleName":"creational","qIndex":7,"difficulty":"intermediate","question":"How does Spring use the Prototype pattern?","answerFull":"Spring beans are singleton-scoped by default. With @Scope(\"prototype\"), every getBean() call returns a new instance — the bean is \"prototyped\" from its definition. This is used for stateful beans that must not be shared (e.g., a per-request helper with mutable state). Gotcha: injecting a prototype bean into a singleton bean breaks the prototype behavior — the singleton holds one reference, so the prototype is only created once. Fix: inject ApplicationContext and call getBean() each time, or use @Lookup method injection.","correct":"Spring beans are singleton-scoped by default.","distractors":["Abstract Factory defines the set of products it creates (e.g., createButton(), createCheckbox()).","All three are correct in Java 5+, but enum is the preferred idiom (Effective Java Item 3): it handles serialization automatically, prevents reflection-based instantiation, and requires no volatile.","An object pool maintains a collection of pre-initialized objects (prototypes) and leases them to callers."]},{"id":"lld/creational#94758ec98cbf","section":"lld","module":"lld/creational","moduleName":"creational","qIndex":8,"difficulty":"intermediate","question":"Abstract Factory vs Strategy — both select an implementation at runtime. What's different?","answerFull":"Strategy selects an algorithm (behavior). Abstract Factory selects a family of object creators (construction). A Strategy doesn't create objects — it performs an operation on objects that already exist. An Abstract Factory creates objects but doesn't define what to do with them. The confusion arises because Abstract Factory is often selected at runtime based on configuration (e.g., \"Linux\" vs \"Windows\") — which looks like Strategy's algorithm selection — but the output is factories, not computed results.","correct":"Strategy selects an algorithm (behavior).","distractors":["Static factory method (Item 1) is naming a static method of(), from(), valueOf(), newInstance(), etc. to create instances — LocalDate.of(2024, 1, 1), Optional.of(x), List.of(...).","Use Abstract Factory when you need to create families of related objects that must be compatible with each other.","Abstract Factory defines the set of products it creates (e.g., createButton(), createCheckbox())."]},{"id":"lld/creational#f98acbd5d85c","section":"lld","module":"lld/creational","moduleName":"creational","qIndex":9,"difficulty":"intermediate","question":"When does a Builder become over-engineering?","answerFull":"When the object has 2–3 non-optional parameters and no future extensibility requirement. A plain constructor is more readable and avoids the boilerplate of a separate Builder class. Builder shines for: 4+ parameters (especially optional ones), immutable objects, objects requiring validation across fields at construction time, and objects where construction order matters. Lombok's @Builder annotation generates the Builder class automatically, which makes it low-overhead when using Lombok.","correct":"When the object has 2–3 non-optional parameters and no future extensibility requirement.","distractors":["Static factory method (Item 1) is naming a static method of(), from(), valueOf(), newInstance(), etc. to create instances — LocalDate.of(2024, 1, 1), Optional.of(x), List.of(...).","An object pool maintains a collection of pre-initialized objects (prototypes) and leases them to callers.","Cloneable is a marker interface that changes the behavior of Object.clone() — but clone() is defined on Object, not Cloneable, so there's no type contract."]},{"id":"lld/creational#19c5ee5a6499","section":"lld","module":"lld/creational","moduleName":"creational","qIndex":10,"difficulty":"intermediate","question":"How does the Prototype pattern enable the \"object pool\" pattern?","answerFull":"An object pool maintains a collection of pre-initialized objects (prototypes) and leases them to callers. When the caller returns the object, the pool resets it (restores to prototype state) and returns it to the pool. HikariCP connection pooling, thread pool reuse, and Netty ByteBuf pooling all apply this principle. Prototype provides the initial template; the pool manages the lifecycle of copies.","correct":"An object pool maintains a collection of pre-initialized objects (prototypes) and leases them to callers.","distractors":["When the object has 2–3 non-optional parameters and no future extensibility requirement.","Cloneable is a marker interface that changes the behavior of Object.clone() — but clone() is defined on Object, not Cloneable, so there's no type contract.","Shallow copy copies references, not the referenced objects."]},{"id":"lld/creational#040695b6b1ec","section":"lld","module":"lld/creational","moduleName":"creational","qIndex":12,"difficulty":"advanced","question":"Prototype deep copy vs shallow copy — what breaks with shallow?","answerFull":"Shallow copy copies references, not the referenced objects. If the original and the copy share a List<Order> reference, modifying one's list modifies both. This is a classic defensive copy failure. Fix: implement deep copy manually — clone each mutable field recursively, or serialize/deserialize the object. Immutable fields (String, Integer, LocalDate) are safe to share. Mutable fields (List, Map, Date, arrays) must be deep-copied. The alternative: make the shared state immutable by design, making shallow copy safe.","correct":"Shallow copy copies references, not the referenced objects.","distractors":["An object pool maintains a collection of pre-initialized objects (prototypes) and leases them to callers.","Use Abstract Factory when you need to create families of related objects that must be compatible with each other.","Abstract Factory defines the set of products it creates (e.g., createButton(), createCheckbox())."]},{"id":"lld/creational#22b3e1cb40d6","section":"lld","module":"lld/creational","moduleName":"creational","qIndex":13,"difficulty":"advanced","question":"How does Java's enum-based Singleton handle serialization?","answerFull":"Enum serialization is handled by the JVM itself — the serialized form is just the enum name, and deserialization always returns the existing instance via Enum.valueOf(). You never get a second instance through deserialization. With a traditional Singleton, you must implement readResolve() to return the existing instance after deserialization, or a deserialized copy breaks the single-instance guarantee. Enum eliminates this requirement entirely.","correct":"Enum serialization is handled by the JVM itself — the serialized form is just the enum name, and deserialization always returns the existing instance via Enum.valueOf().","distractors":["Static factory method (Item 1) is naming a static method of(), from(), valueOf(), newInstance(), etc. to create instances — LocalDate.of(2024, 1, 1), Optional.of(x), List.of(...).","All three are correct in Java 5+, but enum is the preferred idiom (Effective Java Item 3): it handles serialization automatically, prevents reflection-based instantiation, and requires no volatile.","Spring beans are singleton-scoped by default."]},{"id":"lld/creational#7f4cb64a9258","section":"lld","module":"lld/creational","moduleName":"creational","qIndex":15,"difficulty":"advanced","question":"Why does adding a new product type require modifying all Abstract Factories?","answerFull":"Abstract Factory defines the set of products it creates (e.g., createButton(), createCheckbox()). Adding a new product (e.g., createDialog()) requires adding that method to the factory interface AND to every concrete factory. This is the \"Open/Closed Principle violation\" in Abstract Factory — it's closed to modification (you can add new concrete factories freely) but not open to new product types without modifying the interface. Mitigation: use a generic factory (create(Class<T> type)) or accept that product type extension is rare and the modification cost is justified.","correct":"Abstract Factory defines the set of products it creates (e.g., createButton(), createCheckbox()).","distractors":["Use Abstract Factory when you need to create families of related objects that must be compatible with each other.","Static factory method (Item 1) is naming a static method of(), from(), valueOf(), newInstance(), etc. to create instances — LocalDate.of(2024, 1, 1), Optional.of(x), List.of(...).","All three are correct in Java 5+, but enum is the preferred idiom (Effective Java Item 3): it handles serialization automatically, prevents reflection-based instantiation, and requires no volatile."]},{"id":"lld/design_principles#19ffc368176e","section":"lld","module":"lld/design_principles","moduleName":"design principles","qIndex":1,"difficulty":"core","question":"DRY says \"don't repeat yourself\" — when is duplication actually acceptable?","answerFull":"When two pieces of code happen to look the same but represent different concepts that will evolve independently — forcing them into one abstraction creates unwanted coupling. The Rule of Three: wait for the third occurrence before abstracting. Example: two HTTP error handlers may look identical today but handle different business contexts; abstracting too early means every future change must accommodate both. DRY is about knowledge, not text — the test is \"if the rule changes, how many places must I update?\"","correct":"When two pieces of code happen to look the same but represent different concepts that will evolve independently — forcing them into one abstraction creates unwanted coupling.","distractors":["DRY applies to knowledge, not just code.","Each copy drifts independently: a bug fixed in one copy isn't fixed in the others.","Build what solves the current known requirement."]},{"id":"lld/design_principles#94b6b1aae482","section":"lld","module":"lld/design_principles","moduleName":"design principles","qIndex":2,"difficulty":"core","question":"What is the Law of Demeter violation in user.getAddress().getCity().getName()?","answerFull":"Each .get() call adds a dependency to an object you didn't directly receive. If Address changes to store city differently, User's callers break even though User didn't change. Fix: add user.getCityName() — delegate the traversal to User, which owns the navigation path. Benefit: callers are decoupled from Address and City internals. In practice, LoD violations are detected by \"train wreck\" chains and are the root cause of \"fragile code that breaks in unexpected places.\"","correct":"Each .get() call adds a dependency to an object you didn't directly receive.","distractors":["When a base class is modified, subclasses break even though they didn't change — because they inherited implementation details that the base class author didn't intend to expose.","When the interface has exactly one implementation and no conceivable second implementation in any realistic future — the interface adds indirection without value.","Build what solves the current known requirement."]},{"id":"lld/design_principles#99217bd24ae9","section":"lld","module":"lld/design_principles","moduleName":"design principles","qIndex":3,"difficulty":"core","question":"YAGNI vs forward compatibility — how do you know what NOT to build?","answerFull":"Build what solves the current known requirement. Don't build generalization, configurability, or extensibility points until a second concrete use case arrives. The cost of premature abstraction is high: the wrong abstraction is worse than duplication — you can remove duplication; you can't easily remove a load-bearing wrong abstraction. Exception: genuinely irreversible decisions (public API contracts, database schema) — there, some forward-thinking is justified because the cost of changing later is extreme.","correct":"Build what solves the current known requirement.","distractors":["DRY applies to knowledge, not just code.","Each .get() call adds a dependency to an object you didn't directly receive.","Each copy drifts independently: a bug fixed in one copy isn't fixed in the others."]},{"id":"lld/design_principles#420737d8ef5e","section":"lld","module":"lld/design_principles","moduleName":"design principles","qIndex":4,"difficulty":"core","question":"Composition over Inheritance: give a concrete Java example where inheritance breaks and composition fixes it.","answerFull":"Stack extends Vector in Java is the canonical failure. Stack inherits all of Vector's mutation methods (add(), set(), remove()) even though a stack should only expose push()/pop()/peek(). Callers can bypass the stack discipline via inherited Vector methods. Fix: Stack should have a Vector field (has-a) and only expose stack-appropriate operations, delegating to the field internally. Effective Java Item 18: prefer composition over inheritance when a subclass would inherit methods it shouldn't expose.","correct":"Stack extends Vector in Java is the canonical failure.","distractors":["Build what solves the current known requirement.","DRY applies to knowledge, not just code.","Each .get() call adds a dependency to an object you didn't directly receive."]},{"id":"lld/design_principles#82f746105b2c","section":"lld","module":"lld/design_principles","moduleName":"design principles","qIndex":5,"difficulty":"core","question":"Program to Interface: when does coding to an interface actually hurt?","answerFull":"When the interface has exactly one implementation and no conceivable second implementation in any realistic future — the interface adds indirection without value. Example: UserRepositoryInterface with only JpaUserRepository as the implementation, in a codebase that will never use anything other than JPA. Fix: code directly to the concrete class until a second implementation emerges. The test: \"Would I ever inject a different implementation in production or in tests?\" If yes (test doubles count), the interface is justified.","correct":"When the interface has exactly one implementation and no conceivable second implementation in any realistic future — the interface adds indirection without value.","distractors":["When a base class is modified, subclasses break even though they didn't change — because they inherited implementation details that the base class author didn't intend to expose.","Each .get() call adds a dependency to an object you didn't directly receive.","Build what solves the current known requirement."]},{"id":"lld/design_principles#0c2d7cf098ea","section":"lld","module":"lld/design_principles","moduleName":"design principles","qIndex":7,"difficulty":"intermediate","question":"How do DRY violations compound over time in a codebase?","answerFull":"Each copy drifts independently: a bug fixed in one copy isn't fixed in the others. After 18 months, you have 5 slightly different versions of the same logic, each with its own bugs. The team doesn't know which is canonical. Adding a feature requires updating all 5 — but developers only find 3. This is \"shotgun surgery\" (Martin Fowler): a single change requires edits in many places. DRY violations are detected by asking \"if this rule changed, how many files would I need to touch?\"","correct":"Each copy drifts independently: a bug fixed in one copy isn't fixed in the others.","distractors":["DRY applies to knowledge, not just code.","When two pieces of code happen to look the same but represent different concepts that will evolve independently — forcing them into one abstraction creates unwanted coupling.","Build what solves the current known requirement."]},{"id":"lld/design_principles#f7b39be7dee8","section":"lld","module":"lld/design_principles","moduleName":"design principles","qIndex":8,"difficulty":"intermediate","question":"Law of Demeter in Spring: why is applicationContext.getBean(MyService.class) everywhere a violation?","answerFull":"It's service-locator pattern: every class that calls getBean() depends on the entire ApplicationContext (a global registry). The dependencies are hidden — you can't see from the constructor what a class needs. Fix: constructor injection. The class declares its dependencies explicitly; Spring injects them. getBean() is legitimate only in framework code or in cases where the dependency cannot be known at startup (e.g., a factory that creates instances with runtime-determined types).","correct":"It's service-locator pattern: every class that calls getBean() depends on the entire ApplicationContext (a global registry).","distractors":["When a base class is modified, subclasses break even though they didn't change — because they inherited implementation details that the base class author didn't intend to expose.","Build what solves the current known requirement.","DRY applies to knowledge, not just code."]},{"id":"lld/design_principles#7b65366c58a1","section":"lld","module":"lld/design_principles","moduleName":"design principles","qIndex":9,"difficulty":"intermediate","question":"Composition over Inheritance and the \"fragile base class\" problem.","answerFull":"When a base class is modified, subclasses break even though they didn't change — because they inherited implementation details that the base class author didn't intend to expose. super.method() calls mean subclasses are coupled to the order of operations in the base class. Composition avoids this: the component (the \"has-a\") has no access to the outer class's internals; the outer class calls the component's public API only. Changes to the component are encapsulated.","correct":"When a base class is modified, subclasses break even though they didn't change — because they inherited implementation details that the base class author didn't intend to expose.","distractors":["Each .get() call adds a dependency to an object you didn't directly receive.","It's service-locator pattern: every class that calls getBean() depends on the entire ApplicationContext (a global registry).","When the interface has exactly one implementation and no conceivable second implementation in any realistic future — the interface adds indirection without value."]},{"id":"lld/design_principles#53055e6178fe","section":"lld","module":"lld/design_principles","moduleName":"design principles","qIndex":10,"difficulty":"intermediate","question":"DRY applied to configuration and data — not just code.","answerFull":"DRY applies to knowledge, not just code. A database schema that stores the same customer address in three tables violates DRY (update one, get inconsistency). A configuration file that hard-codes port 8080 in 12 places violates DRY. A validation rule that exists in the UI, the service layer, AND the database trigger violates DRY. The fix: single source of truth. For config: a single constants class or environment variable. For validation: server-side as the authoritative source; client-side as a UX convenience only.","correct":"DRY applies to knowledge, not just code.","distractors":["When two pieces of code happen to look the same but represent different concepts that will evolve independently — forcing them into one abstraction creates unwanted coupling.","Build what solves the current known requirement.","Each .get() call adds a dependency to an object you didn't directly receive."]},{"id":"lld/solid_principles#52bab901f29f","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":1,"difficulty":"core","question":"What is the Dependency Inversion Principle?","answerFull":"DIP states that high-level modules should not depend on low-level modules — both should depend on abstractions. Additionally, abstractions should not depend on details; details should depend on abstractions. In practice, this means business logic classes should reference interfaces rather than concrete implementations, with concrete objects supplied via constructor injection.","correct":"DIP states that high-level modules should not depend on low-level modules — both should depend on abstractions.","distractors":["ISP states that no client should be forced to depend on methods it does not use.","LSP states that a subclass must be substitutable for its base class without breaking the program's correctness.","ISP: no client should depend on methods it does not use."]},{"id":"lld/solid_principles#46b27e553c68","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":2,"difficulty":"core","question":"What is the difference between DIP and Dependency Injection?","answerFull":"DIP is a design principle about what to depend on — prefer abstractions over concretions. Dependency Injection is a technique for supplying those dependencies from outside the class rather than instantiating them inside. DI is the mechanism; DIP is the goal. You can use DI without following DIP (injecting concrete classes), and you can follow DIP without a DI framework (manual constructor injection).","correct":"DIP is a design principle about what to depend on — prefer abstractions over concretions.","distractors":["DIP states that high-level modules should not depend on low-level modules — both should depend on abstractions.","Strategy is one of the primary design patterns that embodies OCP.","ISP states that no client should be forced to depend on methods it does not use."]},{"id":"lld/solid_principles#7f4bf0138cda","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":3,"difficulty":"core","question":"Why should new ConcreteClass() inside a service be a red flag?","answerFull":"Because it creates a hard coupling between the service and that specific implementation. The service can no longer be tested without the concrete class, cannot swap implementations without modifying the service, and gains an additional reason to change whenever the concrete class changes. Constructing dependencies is a separate concern from using them — DIP says delegate construction to a composition root.","correct":"Because it creates a hard coupling between the service and that specific implementation.","distractors":["UserService depends on UserRepository (an interface from Spring Data), not on JpaUserRepository (the concrete JPA implementation).","OCP. The existing PaymentService.process() method must be modified every time a new payment type is added.","It means you can add new behavior to a system by writing new code — new classes, new implementations — without changing the existing code that already works and is already tested."]},{"id":"lld/solid_principles#ea11ba9bc85b","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":4,"difficulty":"core","question":"What is a composition root?","answerFull":"The single place in the application where all concrete implementations are wired to their abstractions — typically main(), an application context, or a DI container configuration. All new ConcreteImplementation() calls happen here. Everything else depends only on interfaces.","correct":"The single place in the application where all concrete implementations are wired to their abstractions — typically main(), an application context, or a DI container configuration.","distractors":["A class should have only one reason to change, meaning it should be responsible to a single actor or stakeholder.","[Tell a concrete story about a notification system or report generator with a type switch.","DIP states that high-level modules should not depend on low-level modules — both should depend on abstractions."]},{"id":"lld/solid_principles#a82073a792d9","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":1,"difficulty":"core","question":"What is the Interface Segregation Principle?","answerFull":"ISP states that no client should be forced to depend on methods it does not use. It means interfaces should be narrow and cohesive — tailored to the needs of specific client roles rather than designed as broad contracts that bundle unrelated operations.","correct":"ISP states that no client should be forced to depend on methods it does not use.","distractors":["Ask: \"Is there any implementor that legitimately cannot implement some of these methods?\" and \"Is there any client that never calls some of these methods?\" If yes to either, the interface is a candidate for splitting.","LSP states that a subclass must be substitutable for its base class without breaking the program's correctness.","A fat interface has too many methods serving too many different clients."]},{"id":"lld/solid_principles#864a5f37637d","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":2,"difficulty":"core","question":"What is a \"fat interface\" and why is it a problem?","answerFull":"A fat interface has too many methods serving too many different clients. It forces implementors to write stub or exception-throwing implementations for methods they do not support, and it forces clients to depend on methods they never call. When one part of the interface changes, all clients are affected — even those that had nothing to do with the change.","correct":"A fat interface has too many methods serving too many different clients.","distractors":["They are not the same. A class can have many methods — all of them contributing to one actor's concerns — and still comply with SRP.","ISP states that no client should be forced to depend on methods it does not use.","UserService depends on UserRepository (an interface from Spring Data), not on JpaUserRepository (the concrete JPA implementation)."]},{"id":"lld/solid_principles#948dd9dc9274","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":3,"difficulty":"core","question":"How do you decide when an interface is too big?","answerFull":"Ask: \"Is there any implementor that legitimately cannot implement some of these methods?\" and \"Is there any client that never calls some of these methods?\" If yes to either, the interface is a candidate for splitting. Also look at test complexity — if mocking the interface requires 10+ stub methods to test a single path, it is too big.","correct":"Ask: \"Is there any implementor that legitimately cannot implement some of these methods?\" and \"Is there any client that never calls some of these methods?\" If yes to either, the interface is a candidate for splitting.","distractors":["UserService depends on UserRepository (an interface from Spring Data), not on JpaUserRepository (the concrete JPA implementation).","A fat interface has too many methods serving too many different clients.","They are not the same. A class can have many methods — all of them contributing to one actor's concerns — and still comply with SRP."]},{"id":"lld/solid_principles#a658315f6093","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":4,"difficulty":"core","question":"Is ISP the same as SRP?","answerFull":"They are related but distinct. SRP applies to classes and says a class should have one reason to change. ISP applies to interfaces and says no client should depend on methods it does not use. Both push toward cohesion and reduced coupling. ISP can be thought of as SRP for interfaces.","correct":"They are related but distinct.","distractors":["SRP: a class should have only one reason to change — meaning it serves one stakeholder.","SRP is the formal articulation of high cohesion.","ISP states that no client should be forced to depend on methods it does not use."]},{"id":"lld/solid_principles#a1f8c061737e","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":5,"difficulty":"core","question":"How does ISP relate to the Dependency Inversion Principle?","answerFull":"DIP says depend on abstractions, not concretions. ISP ensures those abstractions are narrow and focused. If the abstraction is a fat interface, DIP-compliant code still ends up coupled to operations it does not need. ISP makes DIP's abstractions genuinely useful.","correct":"DIP says depend on abstractions, not concretions.","distractors":["The single place in the application where all concrete implementations are wired to their abstractions — typically main(), an application context, or a DI container configuration.","DIP is the principle: high-level modules should not depend on low-level modules; both should depend on abstractions.","DIP states that high-level modules should not depend on low-level modules — both should depend on abstractions."]},{"id":"lld/solid_principles#e96b065f40a9","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":1,"difficulty":"core","question":"What is the Liskov Substitution Principle?","answerFull":"LSP states that a subclass must be substitutable for its base class without breaking the program's correctness. This means the subclass must honor the full behavioral contract of the parent: not strengthening preconditions, not weakening postconditions, and not violating class invariants.","correct":"LSP states that a subclass must be substitutable for its base class without breaking the program's correctness.","distractors":["Throwing UnsupportedOperationException from an overridden method is almost always an LSP violation.","DIP states that high-level modules should not depend on low-level modules — both should depend on abstractions.","LSP: subtypes must be substitutable for their base type without altering correctness."]},{"id":"lld/solid_principles#ed300b28db72","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":2,"difficulty":"core","question":"Explain the Rectangle/Square problem.","answerFull":"Mathematically, a square is a rectangle. But in OOP, making Square extend Rectangle causes issues because Rectangle has an implicit contract: setting width does not change height. Square must break this to maintain its own invariant (all sides equal). Code written against Rectangle that assumes independent width/height mutation will break when given a Square. The fix is to use an interface or immutable design that does not involve mutable setters.","correct":"Mathematically, a square is a rectangle.","distractors":["A \"reason to change\" maps to a stakeholder or actor whose requirements drive that change — not to a method count.","A class should have only one reason to change, meaning it should be responsible to a single actor or stakeholder.","A common real-world example is a UserService that handles authentication, profile management, email notifications, and database persistence."]},{"id":"lld/solid_principles#dd6b3a293813","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":3,"difficulty":"core","question":"How is LSP different from polymorphism?","answerFull":"Polymorphism is the mechanism; LSP is the constraint that makes polymorphism safe and correct. Polymorphism allows you to write Shape s = new Circle(). LSP says: if you do that, Circle must behave in a way consistent with what Shape promised. Without LSP, polymorphism compiles but produces surprising runtime failures.","correct":"Polymorphism is the mechanism; LSP is the constraint that makes polymorphism safe and correct.","distractors":["Throwing UnsupportedOperationException from an overridden method is almost always an LSP violation.","LSP states that a subclass must be substitutable for its base class without breaking the program's correctness.","LSP violations are the hardest to detect because they don't show up as a structural error — the code compiles and often passes unit tests."]},{"id":"lld/solid_principles#bb82412b6592","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":4,"difficulty":"core","question":"What does UnsupportedOperationException tell you about LSP?","answerFull":"Throwing UnsupportedOperationException from an overridden method is almost always an LSP violation. It means a subclass is inheriting a contract it cannot fulfill. The right fix is to redesign the hierarchy — either use a narrower interface that does not include the unsupported operation, or switch to composition.","correct":"Throwing UnsupportedOperationException from an overridden method is almost always an LSP violation.","distractors":["Polymorphism is the mechanism; LSP is the constraint that makes polymorphism safe and correct.","LSP states that a subclass must be substitutable for its base class without breaking the program's correctness.","LSP violations are the hardest to detect because they don't show up as a structural error — the code compiles and often passes unit tests."]},{"id":"lld/solid_principles#5f698c66954f","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":5,"difficulty":"core","question":"What is behavioral subtyping?","answerFull":"Behavioral subtyping means \"is-a\" in the sense of the behavior contract, not just the structural type. A Square is structurally a Rectangle (same fields and methods), but it is not a behavioral subtype because it cannot fulfill Rectangle's contract. LSP defines when a subtype relationship is behaviorally valid.","correct":"Behavioral subtyping means \"is-a\" in the sense of the behavior contract, not just the structural type.","distractors":["It means you can add new behavior to a system by writing new code — new classes, new implementations — without changing the existing code that already works and is already tested.","LSP violations are the hardest to detect because they don't show up as a structural error — the code compiles and often passes unit tests.","A \"reason to change\" maps to a stakeholder or actor whose requirements drive that change — not to a method count."]},{"id":"lld/solid_principles#e638ed41d625","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":1,"difficulty":"core","question":"What does \"open for extension, closed for modification\" mean?","answerFull":"It means you can add new behavior to a system by writing new code — new classes, new implementations — without changing the existing code that already works and is already tested. The stability of the existing code is protected while the system remains flexible.","correct":"It means you can add new behavior to a system by writing new code — new classes, new implementations — without changing the existing code that already works and is already tested.","distractors":["LSP states that a subclass must be substitutable for its base class without breaking the program's correctness.","Behavioral subtyping means \"is-a\" in the sense of the behavior contract, not just the structural type.","The single place in the application where all concrete implementations are wired to their abstractions — typically main(), an application context, or a DI container configuration."]},{"id":"lld/solid_principles#241ebf423e81","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":2,"difficulty":"core","question":"How do you achieve OCP in Java?","answerFull":"Through interfaces and polymorphism. Define a stable interface representing the concept that varies (e.g., Shape, PaymentGateway, NotificationSender). When a new variant is needed, write a new implementation of that interface. The client code, which depends only on the interface, never needs to change.","correct":"Through interfaces and polymorphism.","distractors":["Strategy is one of the primary design patterns that embodies OCP.","Polymorphism is the mechanism; LSP is the constraint that makes polymorphism safe and correct.","OCP. The existing PaymentService.process() method must be modified every time a new payment type is added."]},{"id":"lld/solid_principles#12e2df3ef3ae","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":3,"difficulty":"core","question":"Isn't it impossible to be completely closed for modification?","answerFull":"Yes — and that's acknowledged. The goal is strategic closure: identify the axes along which your system is most likely to change, and close for modification along those axes. You cannot close for all possible changes, so you prioritize the most likely ones.","correct":"Yes — and that's acknowledged.","distractors":["OCP: open for extension, closed for modification.","Ask: \"Is there any implementor that legitimately cannot implement some of these methods?\" and \"Is there any client that never calls some of these methods?\" If yes to either, the interface is a candidate for splitting.","A \"reason to change\" maps to a stakeholder or actor whose requirements drive that change — not to a method count."]},{"id":"lld/solid_principles#18a32fc0cf3f","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":4,"difficulty":"core","question":"What is the relationship between OCP and the Strategy pattern?","answerFull":"Strategy is one of the primary design patterns that embodies OCP. The context class (e.g., PaymentProcessor) is closed — it never changes. The strategy (e.g., PaymentGateway) is the extension point. Adding a new payment method means writing a new strategy class, not touching the processor.","correct":"Strategy is one of the primary design patterns that embodies OCP.","distractors":["DIP is a design principle about what to depend on — prefer abstractions over concretions.","OCP: open for extension, closed for modification.","OCP. The existing PaymentService.process() method must be modified every time a new payment type is added."]},{"id":"lld/solid_principles#aa12bc2511ee","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":5,"difficulty":"core","question":"Can you give an example of OCP violation and how you refactored it?","answerFull":"[Tell a concrete story about a notification system or report generator with a type switch. Describe adding a new notification type by writing a new class versus by opening the switch statement. Emphasize the regression risk of the latter and the zero-risk nature of the former.]","correct":"[Tell a concrete story about a notification system or report generator with a type switch.","distractors":["Strategy is one of the primary design patterns that embodies OCP.","The single place in the application where all concrete implementations are wired to their abstractions — typically main(), an application context, or a DI container configuration.","Throwing UnsupportedOperationException from an overridden method is almost always an LSP violation."]},{"id":"lld/solid_principles#1543f460ae4d","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":1,"difficulty":"core","question":"Explain SRP and give a class that violates it. How do you fix it?","answerFull":"SRP: a class should have only one reason to change — meaning it serves one stakeholder. A UserService class that both persists users to a database AND sends a welcome email violates SRP: a change in the email template forces a recompile/redeploy of the persistence code, and a database schema change risks breaking the email path. Fix: extract UserRepository for persistence and EmailService for notification. The UserService orchestrates them without owning either concern.","correct":"SRP: a class should have only one reason to change — meaning it serves one stakeholder.","distractors":["LSP states that a subclass must be substitutable for its base class without breaking the program's correctness.","A \"reason to change\" maps to a stakeholder or actor whose requirements drive that change — not to a method count.","SRP is the formal articulation of high cohesion."]},{"id":"lld/solid_principles#424e55159152","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":2,"difficulty":"core","question":"What is a \"reason to change\" in SRP? Is it \"one method\" or \"one class\"?","answerFull":"A \"reason to change\" maps to a stakeholder or actor whose requirements drive that change — not to a method count. A class with 20 methods that all serve the same business domain (e.g., all manipulate Order state for the order management team) satisfies SRP. A class with 2 methods that serve two different stakeholders (finance and logistics) violates SRP. Uncle Bob's test: \"For whom does this class change?\" If the answer is two different groups of people, split the class.","correct":"A \"reason to change\" maps to a stakeholder or actor whose requirements drive that change — not to a method count.","distractors":["SRP, because it's the most gradual violation.","SRP: a class should have only one reason to change — meaning it serves one stakeholder.","Throwing UnsupportedOperationException from an overridden method is almost always an LSP violation."]},{"id":"lld/solid_principles#fd7b6c203de5","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":3,"difficulty":"core","question":"How does OCP work without modifying existing code? Show a concrete Java example.","answerFull":"OCP: open for extension, closed for modification. Payment processing: instead of if (type == CREDIT_CARD) { ... } else if (type == PAYPAL) { ... }, define interface PaymentProcessor { void process(Payment p); }. Add CreditCardProcessor and PayPalProcessor as implementations. Adding Bitcoin support means adding BitcoinProcessor — the existing PaymentService is never touched. The abstraction (interface) is the extension point; the if-else chain is the closed-for-extension design.","correct":"OCP: open for extension, closed for modification.","distractors":["LSP violations are the hardest to detect because they don't show up as a structural error — the code compiles and often passes unit tests.","Strategy is one of the primary design patterns that embodies OCP.","[Tell a concrete story about a notification system or report generator with a type switch."]},{"id":"lld/solid_principles#e9e5091fc3d2","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":4,"difficulty":"core","question":"Square extends Rectangle: why does this violate LSP? Show the broken code.","answerFull":"LSP: subtypes must be substitutable for their base type without altering correctness. Rectangle has setWidth(int) and setHeight(int) as independent operations. Square must keep width == height, so overriding setWidth to also set height changes the postcondition. Broken: java Rectangle r = new Square(); r.setWidth(5); r.setHeight(4); assert r.area() == 20; // FAILS: Square sets both to 4, area = 16 Fix: don't model Square as a subtype of Rectangle. Model them as separate implementations of a Shape interface, or use a factory that returns the right shape for the dimensions.","correct":"LSP: subtypes must be substitutable for their base type without altering correctness.","distractors":["Throwing UnsupportedOperationException from an overridden method is almost always an LSP violation.","LSP violations are the hardest to detect because they don't show up as a structural error — the code compiles and often passes unit tests.","Polymorphism is the mechanism; LSP is the constraint that makes polymorphism safe and correct."]},{"id":"lld/solid_principles#10b7c9c85782","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":5,"difficulty":"core","question":"What is ISP and how does it prevent the \"fat interface\" problem?","answerFull":"ISP: no client should depend on methods it does not use. A Animal interface with eat(), fly(), swim(), and run() forces Dog to implement fly() by throwing UnsupportedOperationException. Fix: split into Flyable, Swimmable, Runnable interfaces. Dog implements Swimmable and Runnable. Bird implements Flyable. Each class depends only on the methods it actually uses. Benefit: adding Drone implements Flyable never forces a change to Dog, Fish, or any unrelated class.","correct":"ISP: no client should depend on methods it does not use.","distractors":["UserService depends on UserRepository (an interface from Spring Data), not on JpaUserRepository (the concrete JPA implementation).","DIP is a design principle about what to depend on — prefer abstractions over concretions.","DIP states that high-level modules should not depend on low-level modules — both should depend on abstractions."]},{"id":"lld/solid_principles#c4e95401554d","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":6,"difficulty":"intermediate","question":"How does DIP differ from Dependency Injection? Which is the principle and which is the pattern?","answerFull":"DIP is the principle: high-level modules should not depend on low-level modules; both should depend on abstractions. Dependency Injection is a pattern that implements DIP: instead of the class creating its own dependency (new JpaUserRepository()), the dependency is passed in (injected) from outside. Spring's @Autowired is a DI mechanism. You can follow DIP without a DI framework (pass dependencies via constructor); you can use a DI framework without following DIP (inject concrete classes instead of interfaces). DIP is the goal; DI is the most common way to achieve it.","correct":"DIP is the principle: high-level modules should not depend on low-level modules; both should depend on abstractions.","distractors":["DIP says depend on abstractions, not concretions.","The single place in the application where all concrete implementations are wired to their abstractions — typically main(), an application context, or a DI container configuration.","ISP states that no client should be forced to depend on methods it does not use."]},{"id":"lld/solid_principles#c017072ac229","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":7,"difficulty":"intermediate","question":"Give a Spring example of DIP in action.","answerFull":"UserService depends on UserRepository (an interface from Spring Data), not on JpaUserRepository (the concrete JPA implementation). Spring injects the concrete impl at runtime. In tests, inject InMemoryUserRepository instead — no changes to UserService. The high-level policy (UserService) is isolated from the low-level detail (JPA, database). Adding a MongoDB-backed MongoUserRepository requires zero changes to UserService. This is DIP enforced by the Spring container.","correct":"UserService depends on UserRepository (an interface from Spring Data), not on JpaUserRepository (the concrete JPA implementation).","distractors":["DIP says depend on abstractions, not concretions.","[Tell a concrete story about a notification system or report generator with a type switch.","DIP states that high-level modules should not depend on low-level modules — both should depend on abstractions."]},{"id":"lld/solid_principles#5ca2879f8046","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":8,"difficulty":"intermediate","question":"Which SOLID principle is violated when you add if (type == CREDIT_CARD) to handle a new payment type?","answerFull":"OCP. The existing PaymentService.process() method must be modified every time a new payment type is added. After 10 payment types, the method has 10 branches, each requiring a regression test, and all sharing the risk of a bug in one branch breaking the others. The correct design: PaymentProcessor interface + one class per payment type. Adding a new type adds a new class; PaymentService is never touched.","correct":"OCP. The existing PaymentService.process() method must be modified every time a new payment type is added.","distractors":["Strategy is one of the primary design patterns that embodies OCP.","It means you can add new behavior to a system by writing new code — new classes, new implementations — without changing the existing code that already works and is already tested.","DIP is the principle: high-level modules should not depend on low-level modules; both should depend on abstractions."]},{"id":"lld/solid_principles#a24383460161","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":9,"difficulty":"intermediate","question":"Which SOLID principle is most commonly violated in legacy codebases, and why?","answerFull":"SRP, because it's the most gradual violation. A class starts with one responsibility. A developer adds \"just a small change\" that introduces a second responsibility. Another adds a third. After 3 years, the class has 50 methods serving 6 different stakeholders. No single change felt wrong at the time. Detection: a class with more than one reason to appear in a sprint's JIRA tickets is likely violating SRP.","correct":"SRP, because it's the most gradual violation.","distractors":["They are not the same. A class can have many methods — all of them contributing to one actor's concerns — and still comply with SRP.","SRP: a class should have only one reason to change — meaning it serves one stakeholder.","SRP: small, focused classes are easy to test in isolation."]},{"id":"lld/solid_principles#544834c76797","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":11,"difficulty":"advanced","question":"How do the five SOLID principles relate to testability?","answerFull":"SRP: small, focused classes are easy to test in isolation. OCP: extension via interface means you can inject test doubles without changing production code. LSP: if subtypes are substitutable, tests written against the base type work for all subtypes. ISP: narrow interfaces make test setup minimal — mock only the methods the class actually uses. DIP: inject dependencies via constructor, and tests can pass fakes/mocks instead of real infrastructure. Together: SOLID principles are the single biggest enabler of unit testing in object-oriented code.","correct":"SRP: small, focused classes are easy to test in isolation.","distractors":["SRP, because it's the most gradual violation.","SRP: a class should have only one reason to change — meaning it serves one stakeholder.","It means you can add new behavior to a system by writing new code — new classes, new implementations — without changing the existing code that already works and is already tested."]},{"id":"lld/solid_principles#ac27b753a552","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":12,"difficulty":"advanced","question":"What is the most subtle SOLID violation to detect in code review?","answerFull":"LSP violations are the hardest to detect because they don't show up as a structural error — the code compiles and often passes unit tests. A subclass that weakens a postcondition (returns a narrower set of values than the parent promises), strengthens a precondition (requires more than the parent accepts), or throws an exception the parent never throws — all violate LSP silently. They cause failures at call sites that use polymorphism, which are often far from the class definition. Clue: a test that passes for the parent type but fails when you substitute the subclass.","correct":"LSP violations are the hardest to detect because they don't show up as a structural error — the code compiles and often passes unit tests.","distractors":["Behavioral subtyping means \"is-a\" in the sense of the behavior contract, not just the structural type.","Throwing UnsupportedOperationException from an overridden method is almost always an LSP violation.","It means you can add new behavior to a system by writing new code — new classes, new implementations — without changing the existing code that already works and is already tested."]},{"id":"lld/solid_principles#ccfc32c34a63","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":1,"difficulty":"core","question":"What is the Single Responsibility Principle?","answerFull":"A class should have only one reason to change, meaning it should be responsible to a single actor or stakeholder. If two different teams' requirements can cause a class to change, it has multiple responsibilities and should be split.","correct":"A class should have only one reason to change, meaning it should be responsible to a single actor or stakeholder.","distractors":["The single place in the application where all concrete implementations are wired to their abstractions — typically main(), an application context, or a DI container configuration.","A \"reason to change\" maps to a stakeholder or actor whose requirements drive that change — not to a method count.","DIP is a design principle about what to depend on — prefer abstractions over concretions."]},{"id":"lld/solid_principles#4110d1d629e8","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":2,"difficulty":"core","question":"How is \"one reason to change\" different from \"one method\"?","answerFull":"They are not the same. A class can have many methods — all of them contributing to one actor's concerns — and still comply with SRP. The question is not about method count but about whether multiple independent stakeholders drive changes to the class.","correct":"They are not the same. A class can have many methods — all of them contributing to one actor's concerns — and still comply with SRP.","distractors":["SRP, because it's the most gradual violation.","SRP: a class should have only one reason to change — meaning it serves one stakeholder.","A \"reason to change\" maps to a stakeholder or actor whose requirements drive that change — not to a method count."]},{"id":"lld/solid_principles#90fe1834d585","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":3,"difficulty":"core","question":"Can you give an example of an SRP violation you have encountered?","answerFull":"A common real-world example is a UserService that handles authentication, profile management, email notifications, and database persistence. Changing the email template requires touching the same class as changing the login algorithm. Extracting EmailNotificationService, UserRepository, and AuthService gives each its own reason to change.","correct":"A common real-world example is a UserService that handles authentication, profile management, email notifications, and database persistence.","distractors":["SRP: a class should have only one reason to change — meaning it serves one stakeholder.","Throwing UnsupportedOperationException from an overridden method is almost always an LSP violation.","SRP, because it's the most gradual violation."]},{"id":"lld/solid_principles#aabf6d7f8b9c","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":4,"difficulty":"core","question":"Does SRP lead to too many small classes?","answerFull":"It can if applied mechanically. The goal is cohesion toward a single actor, not minimizing class size. A PayCalculator with five private helper methods all serving Finance is fine. Over-splitting into one-method classes is \"class explosion\" and is just as harmful. SRP is a judgment call guided by \"who changes this, and for what reason?\"","correct":"It can if applied mechanically.","distractors":["SRP: small, focused classes are easy to test in isolation.","SRP: a class should have only one reason to change — meaning it serves one stakeholder.","It means you can add new behavior to a system by writing new code — new classes, new implementations — without changing the existing code that already works and is already tested."]},{"id":"lld/solid_principles#7fed54798676","section":"lld","module":"lld/solid_principles","moduleName":"solid principles","qIndex":5,"difficulty":"core","question":"How does SRP relate to the concept of cohesion?","answerFull":"SRP is the formal articulation of high cohesion. High cohesion means the methods and data of a class are strongly related and serve a unified purpose. SRP adds precision by anchoring that purpose to a real-world actor, making it actionable during design.","correct":"SRP is the formal articulation of high cohesion.","distractors":["DIP states that high-level modules should not depend on low-level modules — both should depend on abstractions.","SRP: a class should have only one reason to change — meaning it serves one stakeholder.","SRP: small, focused classes are easy to test in isolation."]},{"id":"lld/structural#5993e614b431","section":"lld","module":"lld/structural","moduleName":"structural","qIndex":1,"difficulty":"core","question":"Decorator vs Proxy: they have exactly the same structure — what distinguishes them?","answerFull":"Proxy controls access to the real object transparently: the client doesn't know or care it's talking to a proxy; the proxy decides whether and how to forward the call (lazy init, security check, remote forwarding). Decorator adds behavior the client explicitly requests: the client constructs the decorator stack itself (new BufferedReader(new FileReader(path))). Intent is the difference: Proxy = access control; Decorator = behavior extension. In Spring, @Transactional adds a transaction Proxy the client is unaware of; wrapping an OutputStream in a GZIPOutputStream is a Decorator the caller chose.","correct":"Proxy controls access to the real object transparently: the client doesn't know or care it's talking to a proxy; the proxy decides whether and how to forward the call (lazy init, security check, remote forwarding).","distractors":["If you design an Adapter, it means you already have two incompatible things and you're gluing them together.","When a Spring bean calls one of its own @Transactional methods directly (this.someMethod()), it calls the real object, not the proxy — so the transaction advice never runs.","A remote proxy represents an object that lives in a different JVM or address space."]},{"id":"lld/structural#7e45f4911ee7","section":"lld","module":"lld/structural","moduleName":"structural","qIndex":2,"difficulty":"core","question":"Adapter vs Bridge: one retrofits, the other is designed upfront — why does it matter?","answerFull":"If you design an Adapter, it means you already have two incompatible things and you're gluing them together. The existence of an Adapter is often a sign of an architectural seam between legacy and new code. Bridge requires upfront foresight: you recognize that an abstraction (e.g., \"rendering\") will have multiple implementations (e.g., OpenGL, DirectX) and you architect the hierarchy to avoid N×M subclasses. Adapter = reactive; Bridge = proactive. In practice, most developers reach for Adapter far more often than Bridge.","correct":"If you design an Adapter, it means you already have two incompatible things and you're gluing them together.","distractors":["Java doesn't support multiple inheritance of classes, so the GoF class adapter (which inherits from both the target interface and the adaptee class simultaneously) isn't possible.","Without Bridge: N abstractions x M implementations = N×M subclasses.","Adapter converts one existing interface to another (1:1, fixing an incompatibility)."]},{"id":"lld/structural#b848b5090c69","section":"lld","module":"lld/structural","moduleName":"structural","qIndex":3,"difficulty":"core","question":"How does Spring AOP implement the Proxy pattern? What are the two mechanisms?","answerFull":"Spring creates a proxy around the target bean at startup. If the target implements an interface, Spring uses java.lang.reflect.Proxy (JDK dynamic proxy) — the proxy implements the same interface and delegates all calls. If the target does NOT implement an interface (or if proxyTargetClass=true is set), Spring uses CGLIB to subclass the target class and override methods. This is why @Transactional on a final method silently fails with CGLIB — final methods can't be overridden, so the proxy can't intercept them. Fix: declare all proxied methods on an interface, or remove final.","correct":"Spring creates a proxy around the target bean at startup.","distractors":["Proxy controls access to the real object transparently: the client doesn't know or care it's talking to a proxy; the proxy decides whether and how to forward the call (lazy init, security check, remote forwarding).","When a Spring bean calls one of its own @Transactional methods directly (this.someMethod()), it calls the real object, not the proxy — so the transaction advice never runs.","A remote proxy represents an object that lives in a different JVM or address space."]},{"id":"lld/structural#c6a1a0750a7b","section":"lld","module":"lld/structural","moduleName":"structural","qIndex":4,"difficulty":"core","question":"What is the difference between Facade and Adapter?","answerFull":"Adapter converts one existing interface to another (1:1, fixing an incompatibility). Facade creates a new, simplified interface over multiple subsystem components (N:1, reducing complexity). Adapter doesn't add or remove functionality. Facade hides parts of the subsystem and may combine multiple operations into one call. Arrays.asList() is an Adapter (converts array to List). JdbcTemplate is a Facade (hides Connection, PreparedStatement, ResultSet, exception handling behind simple query() and update() methods).","correct":"Adapter converts one existing interface to another (1:1, fixing an incompatibility).","distractors":["Java doesn't support multiple inheritance of classes, so the GoF class adapter (which inherits from both the target interface and the adaptee class simultaneously) isn't possible.","If you design an Adapter, it means you already have two incompatible things and you're gluing them together.","A remote proxy represents an object that lives in a different JVM or address space."]},{"id":"lld/structural#1f688df62e11","section":"lld","module":"lld/structural","moduleName":"structural","qIndex":6,"difficulty":"intermediate","question":"Flyweight: what is the intrinsic/extrinsic state distinction and why does it matter?","answerFull":"Intrinsic state is shared — it doesn't vary between instances and can be stored in the Flyweight (e.g., a character's glyph bitmap, a bullet sprite image). Extrinsic state is context-specific — it varies per use and must be passed by the client (e.g., the character's position on screen, the bullet's current velocity). The Flyweight only stores intrinsic state; extrinsic state is passed in on each operation. This distinction matters because: (a) shared state must be immutable (thread-safe by design); (b) if you accidentally put extrinsic state into the Flyweight, the shared object reflects one context's state and corrupts all others.","correct":"Intrinsic state is shared — it doesn't vary between instances and can be stored in the Flyweight (e.g., a character's glyph bitmap, a bullet sprite image).","distractors":["Java doesn't support multiple inheritance of classes, so the GoF class adapter (which inherits from both the target interface and the adaptee class simultaneously) isn't possible.","Proxy controls access to the real object transparently: the client doesn't know or care it's talking to a proxy; the proxy decides whether and how to forward the call (lazy init, security check, remote forwarding).","Integer.valueOf(n) caches Integer instances for values -128 to 127 (configurable up to a JVM flag maximum)."]},{"id":"lld/structural#2826eb1d444d","section":"lld","module":"lld/structural","moduleName":"structural","qIndex":8,"difficulty":"intermediate","question":"What is the \"self-invocation problem\" with Spring Proxy, and how do you fix it?","answerFull":"When a Spring bean calls one of its own @Transactional methods directly (this.someMethod()), it calls the real object, not the proxy — so the transaction advice never runs. This is because the proxy wraps the bean externally; internal calls bypass it. Fix options: (a) inject the bean into itself (@Autowired MyService self — works but looks odd), (b) fetch the proxy from ApplicationContext at call time, (c) extract the method to a separate bean, (d) use AspectJ weaving instead of proxy-based AOP (which applies advice at the bytecode level, not the proxy level).","correct":"When a Spring bean calls one of its own @Transactional methods directly (this.someMethod()), it calls the real object, not the proxy — so the transaction advice never runs.","distractors":["A remote proxy represents an object that lives in a different JVM or address space.","Spring creates a proxy around the target bean at startup.","Proxy controls access to the real object transparently: the client doesn't know or care it's talking to a proxy; the proxy decides whether and how to forward the call (lazy init, security check, remote forwarding)."]},{"id":"lld/structural#ab6be09f7926","section":"lld","module":"lld/structural","moduleName":"structural","qIndex":9,"difficulty":"intermediate","question":"How does Java's Integer.valueOf() implement the Flyweight pattern?","answerFull":"Integer.valueOf(n) caches Integer instances for values -128 to 127 (configurable up to a JVM flag maximum). Calls with values in this range return the same cached instance; calls outside the range create a new instance each time. This is the Flyweight: the Integer objects in the cache are the shared flyweights (intrinsic state = the integer value). The extrinsic state is the calling context — the same Integer(42) instance is returned to every caller who asks for 42. Pitfall: Integer a = 200; Integer b = 200; a == b is false (outside cache range); Integer a = 100; Integer b = 100; a == b is true. Always use .equals(), not ==, for Integer comparison.","correct":"Integer.valueOf(n) caches Integer instances for values -128 to 127 (configurable up to a JVM flag maximum).","distractors":["A remote proxy represents an object that lives in a different JVM or address space.","Java doesn't support multiple inheritance of classes, so the GoF class adapter (which inherits from both the target interface and the adaptee class simultaneously) isn't possible.","Intrinsic state is shared — it doesn't vary between instances and can be stored in the Flyweight (e.g., a character's glyph bitmap, a bullet sprite image)."]},{"id":"lld/structural#7de533abcd6c","section":"lld","module":"lld/structural","moduleName":"structural","qIndex":10,"difficulty":"intermediate","question":"How does the Bridge pattern prevent class-hierarchy explosion?","answerFull":"Without Bridge: N abstractions x M implementations = N×M subclasses. Example: Shape with Circle, Square, Triangle (3) x OpenGL, DirectX, Metal (3) = 9 concrete classes. With Bridge: Shape has a reference to a Renderer (the implementation). Circle, Square, Triangle extend Shape (3 classes). OpenGL, DirectX, Metal implement Renderer (3 classes). Total: 6 classes, not 9. For 5 shapes x 5 renderers: 25 classes without Bridge; 10 with Bridge. The saving grows quadratically.","correct":"Without Bridge: N abstractions x M implementations = N×M subclasses.","distractors":["Java doesn't support multiple inheritance of classes, so the GoF class adapter (which inherits from both the target interface and the adaptee class simultaneously) isn't possible.","A remote proxy represents an object that lives in a different JVM or address space.","Adapter converts one existing interface to another (1:1, fixing an incompatibility)."]},{"id":"lld/structural#4217ed361d76","section":"lld","module":"lld/structural","moduleName":"structural","qIndex":11,"difficulty":"advanced","question":"What is a remote proxy, and how does it relate to Java RMI?","answerFull":"A remote proxy represents an object that lives in a different JVM or address space. The proxy exposes the same interface as the real object; method calls on the proxy are serialized, transmitted to the remote JVM, executed on the real object, and the result is serialized back. Java RMI (java.rmi.Remote) is the classic implementation: a stub (proxy) on the client side implements the remote interface; the skeleton on the server side unwraps calls. Modern equivalents: gRPC stubs, Feign clients, and Spring's @FeignClient are all remote proxies at the HTTP/protobuf level.","correct":"A remote proxy represents an object that lives in a different JVM or address space.","distractors":["Proxy controls access to the real object transparently: the client doesn't know or care it's talking to a proxy; the proxy decides whether and how to forward the call (lazy init, security check, remote forwarding).","Integer.valueOf(n) caches Integer instances for values -128 to 127 (configurable up to a JVM flag maximum).","When a Spring bean calls one of its own @Transactional methods directly (this.someMethod()), it calls the real object, not the proxy — so the transaction advice never runs."]},{"id":"lld/structural#90cd68771da1","section":"lld","module":"lld/structural","moduleName":"structural","qIndex":12,"difficulty":"advanced","question":"When is Facade the wrong pattern to reach for?","answerFull":"When the subsystem needs to remain fully accessible to some clients. Facade simplifies for the common case but doesn't prevent direct access — so you end up with two APIs (the facade and the raw subsystem). If different clients need different subsets of the subsystem, a Facade that tries to serve all of them becomes a God Class. Alternative: use distinct Facades for distinct client types (a CustomerFacade vs AdminFacade), or use Adapter per client type to present exactly the right interface.","correct":"When the subsystem needs to remain fully accessible to some clients.","distractors":["A remote proxy represents an object that lives in a different JVM or address space.","Adapter converts one existing interface to another (1:1, fixing an incompatibility).","If you design an Adapter, it means you already have two incompatible things and you're gluing them together."]},{"id":"lld/structural#6badb5a45908","section":"lld","module":"lld/structural","moduleName":"structural","qIndex":13,"difficulty":"advanced","question":"Object adapter vs class adapter in Java — which do you use and why?","answerFull":"Java doesn't support multiple inheritance of classes, so the GoF class adapter (which inherits from both the target interface and the adaptee class simultaneously) isn't possible. Java uses object adapter: the adapter holds a reference to an adaptee instance and delegates calls. Benefit of object adapter: you can adapt an object of any subclass of adaptee (not just one specific class). Benefit of class adapter (where available, e.g., C++): direct method access without delegation overhead. In Java, always use object adapter.","correct":"Java doesn't support multiple inheritance of classes, so the GoF class adapter (which inherits from both the target interface and the adaptee class simultaneously) isn't possible.","distractors":["Adapter converts one existing interface to another (1:1, fixing an incompatibility).","If you design an Adapter, it means you already have two incompatible things and you're gluing them together.","Proxy controls access to the real object transparently: the client doesn't know or care it's talking to a proxy; the proxy decides whether and how to forward the call (lazy init, security check, remote forwarding)."]},{"id":"lld/structural#69692d27099c","section":"lld","module":"lld/structural","moduleName":"structural","qIndex":15,"difficulty":"advanced","question":"Composite: can a Composite contain other Composites? Show the design.","answerFull":"Yes — that's the point. A FileSystem Composite: FileSystemComponent is the interface with getName(), getSize(), print(). File is a Leaf implementing it. Directory is a Composite holding List<FileSystemComponent> — it can contain both File and other Directory objects recursively. getSize() on a Directory sums getSize() across all children recursively. This models the part-whole hierarchy. In Spring: CompositeCacheManager holds a List<CacheManager>, each of which might itself be a CompositeCacheManager.","correct":"Yes — that's the point. A FileSystem Composite: FileSystemComponent is the interface with getName(), getSize(), print().","distractors":["Java doesn't support multiple inheritance of classes, so the GoF class adapter (which inherits from both the target interface and the adaptee class simultaneously) isn't possible.","Adapter converts one existing interface to another (1:1, fixing an incompatibility).","If you design an Adapter, it means you already have two incompatible things and you're gluing them together."]},{"id":"lld/system_design_problems#91e25e6fe478","section":"lld","module":"lld/system_design_problems","moduleName":"system design problems","qIndex":1,"difficulty":"core","question":"How do you start a 30-minute LLD interview? What's your first sentence?","answerFull":"Ask clarifying questions before drawing anything. \"Before I start, I want to make sure I'm solving the right problem — can I ask a few questions about scope?\" Then: single machine or distributed? How many concurrent users? What are the must-have use cases vs nice-to-haves? Is this interview more interested in class design, state machine, or concurrency handling? This opening shows senior-level judgment — junior candidates jump straight to coding.","correct":"Ask clarifying questions before drawing anything.","distractors":["A HashMap alone gives O(1) key lookup but no ordering — you can't efficiently find \"the least recently used entry\" without an O(n) scan.","A switch statement puts all state logic in one class, violating OCP — adding a new state requires modifying the switch.","Each move is a Command object: MoveCommand(piece, fromSquare, toSquare, capturedPiece)."]},{"id":"lld/system_design_problems#5387f83d11b0","section":"lld","module":"lld/system_design_problems","moduleName":"system design problems","qIndex":2,"difficulty":"core","question":"Parking Lot: how do you handle two cars arriving simultaneously for the last spot?","answerFull":"If on a single machine: use AtomicReference<SpotStatus> with compareAndSet(AVAILABLE, OCCUPIED) — only one thread wins the CAS; the other retries and finds no available spot. If distributed: use optimistic locking in the database (add a version column; UPDATE spot SET status='OCCUPIED', version=version+1 WHERE id=? AND version=? — exactly one update succeeds). The key insight: spot assignment must be atomic. Do NOT check availability and then reserve in two separate operations — this is a classic check-then-act race condition.","correct":"If on a single machine: use AtomicReference<SpotStatus> with compareAndSet(AVAILABLE, OCCUPIED) — only one thread wins the CAS; the other retries and finds no available spot.","distractors":["The naive approach scans every available driver and computes Euclidean distance to the rider's pickup location, picking the nearest one — O(N) per request.","The naive approach rescans the entire board after every move — O(N^2) per move, so O(N^2) work just to check 4 lines through the last-placed cell.","A HashMap alone gives O(1) key lookup but no ordering — you can't efficiently find \"the least recently used entry\" without an O(n) scan."]},{"id":"lld/system_design_problems#8c0159dcfa3c","section":"lld","module":"lld/system_design_problems","moduleName":"system design problems","qIndex":3,"difficulty":"core","question":"Vending Machine: why is the State pattern better than a switch statement for state transitions?","answerFull":"A switch statement puts all state logic in one class, violating OCP — adding a new state requires modifying the switch. As states accumulate, the switch becomes unreadable and error-prone. State pattern: each state is a class; transitions are method calls that replace the current state object. Adding a new state means adding a new class and modifying only the states that transition to it — not the entire machine. The State pattern also makes illegal transitions explicit: an OutOfStockState simply doesn't implement acceptMoney() with success behavior.","correct":"A switch statement puts all state logic in one class, violating OCP — adding a new state requires modifying the switch.","distractors":["Use the Strategy pattern for search: SearchStrategy interface with implementations like TitleSearch, AuthorSearch, ISBNSearch, GenreSearch.","If on a single machine: use AtomicReference<SpotStatus> with compareAndSet(AVAILABLE, OCCUPIED) — only one thread wins the CAS; the other retries and finds no available spot.","Show that the design is open for extension without modification (OCP)."]},{"id":"lld/system_design_problems#09776c261fb9","section":"lld","module":"lld/system_design_problems","moduleName":"system design problems","qIndex":4,"difficulty":"core","question":"Chess: how does the Command pattern enable undo of moves?","answerFull":"Each move is a Command object: MoveCommand(piece, fromSquare, toSquare, capturedPiece). execute() moves the piece; undo() moves it back and restores the captured piece. A Deque<MoveCommand> is the history stack. Ctrl+Z pops the stack and calls undo(). The benefit: the Board class doesn't need any undo logic — it just responds to move() and restore() calls. The history management is entirely in the MoveCommand and the client. Chess engines also use this for \"what-if\" analysis: execute a speculative move, evaluate the board, undo it.","correct":"Each move is a Command object: MoveCommand(piece, fromSquare, toSquare, capturedPiece).","distractors":["SCAN (also called the \"elevator algorithm\"): the elevator moves in one direction, stopping at all requested floors, then reverses.","Use the Strategy pattern for search: SearchStrategy interface with implementations like TitleSearch, AuthorSearch, ISBNSearch, GenreSearch.","The naive approach rescans the entire board after every move — O(N^2) per move, so O(N^2) work just to check 4 lines through the last-placed cell."]},{"id":"lld/system_design_problems#21809e37a781","section":"lld","module":"lld/system_design_problems","moduleName":"system design problems","qIndex":5,"difficulty":"core","question":"ATM: what happens if power fails mid-transaction? How do you design for recovery?","answerFull":"Each transaction must be idempotent: if the ATM dispenses cash and then power fails before writing the debit to the ledger, the debit should be recorded on recovery. Design: log the transaction intent to durable storage (a transaction log) BEFORE dispensing cash. On power-on, replay uncommitted transactions. This is the same write-ahead log (WAL) pattern used by databases. For the interview: mention that the ATM state machine must have a DISPENSING state that, on recovery, either completes the dispense or rolls back the debit — never leaves the account in an ambiguous state.","correct":"Each transaction must be idempotent: if the ATM dispenses cash and then power fails before writing the debit to the ledger, the debit should be recorded on recovery.","distractors":["Show that the design is open for extension without modification (OCP).","Option 1 (optimistic locking): add a version field to the seat record; the booking transaction does UPDATE seat SET status='BOOKED', version=version+1 WHERE id=?","A HashMap alone gives O(1) key lookup but no ordering — you can't efficiently find \"the least recently used entry\" without an O(n) scan."]},{"id":"lld/system_design_problems#e33fa487b4d6","section":"lld","module":"lld/system_design_problems","moduleName":"system design problems","qIndex":6,"difficulty":"intermediate","question":"Online Booking System: how do you prevent double-booking of the same seat?","answerFull":"Option 1 (optimistic locking): add a version field to the seat record; the booking transaction does UPDATE seat SET status='BOOKED', version=version+1 WHERE id=? AND status='AVAILABLE' AND version=?. If 0 rows updated, another transaction won the race — return a conflict error. Option 2 (pessimistic locking): SELECT * FROM seat WHERE id=? FOR UPDATE — acquires a row-level lock, serializing concurrent bookings. Optimistic is preferred for high read-to-write ratios; pessimistic is preferred when conflicts are frequent. For the interview: mention both and explain the tradeoff.","correct":"Option 1 (optimistic locking): add a version field to the seat record; the booking transaction does UPDATE seat SET status='BOOKED', version=version+1 WHERE id=?","distractors":["Each transaction must be idempotent: if the ATM dispenses cash and then power fails before writing the debit to the ledger, the debit should be recorded on recovery.","A HashMap alone gives O(1) key lookup but no ordering — you can't efficiently find \"the least recently used entry\" without an O(n) scan.","A switch statement puts all state logic in one class, violating OCP — adding a new state requires modifying the switch."]},{"id":"lld/system_design_problems#9781a3f3a3f3","section":"lld","module":"lld/system_design_problems","moduleName":"system design problems","qIndex":7,"difficulty":"intermediate","question":"Library Management: how do you notify members about overdue books without polling the database?","answerFull":"Schedule a daily job (Spring @Scheduled, cron, or a batch job) that queries all unreturned books past their due date and publishes overdue events. Observers (email sender, SMS sender, in-app notification) consume the events. Decoupling: adding a new notification channel (push notification) means adding a new Observer — no change to the scheduler or query logic. Alternative: event-driven — on each book checkout, schedule a future event (ScheduledExecutorService.schedule() or a job queue) that fires on the due date. The event-driven approach doesn't require daily polling.","correct":"Schedule a daily job (Spring @Scheduled, cron, or a batch job) that queries all unreturned books past their due date and publishes overdue events.","distractors":["A HashMap alone gives O(1) key lookup but no ordering — you can't efficiently find \"the least recently used entry\" without an O(n) scan.","Show that the design is open for extension without modification (OCP).","A switch statement puts all state logic in one class, violating OCP — adding a new state requires modifying the switch."]},{"id":"lld/system_design_problems#3078157565ab","section":"lld","module":"lld/system_design_problems","moduleName":"system design problems","qIndex":8,"difficulty":"intermediate","question":"Elevator System: what scheduling algorithm should you use?","answerFull":"SCAN (also called the \"elevator algorithm\"): the elevator moves in one direction, stopping at all requested floors, then reverses. LOOK variant: reverse when no more requests in the current direction (don't go to the top floor if the last request is floor 7). FCFS (First Come First Served) is simple but causes large variance in wait time. For the interview: mention SCAN as the baseline, note that modern elevators use destination dispatch (you enter your destination floor before entering the elevator, grouping passengers going to the same floor). The State pattern models the elevator's direction (MOVING_UP, MOVING_DOWN, IDLE) and door state (DOOR_OPEN, DOOR_CLOSED).","correct":"SCAN (also called the \"elevator algorithm\"): the elevator moves in one direction, stopping at all requested floors, then reverses.","distractors":["A HashMap alone gives O(1) key lookup but no ordering — you can't efficiently find \"the least recently used entry\" without an O(n) scan.","A switch statement puts all state logic in one class, violating OCP — adding a new state requires modifying the switch.","Ask clarifying questions before drawing anything."]},{"id":"lld/system_design_problems#2a9e386d409b","section":"lld","module":"lld/system_design_problems","moduleName":"system design problems","qIndex":9,"difficulty":"intermediate","question":"How do you represent money in ATM or Booking System? Why not float?","answerFull":"Use BigDecimal for exact decimal arithmetic, or represent money as the smallest currency unit in a long (e.g., cents for USD). float and double are binary floating-point and cannot represent 0.1 exactly (0.1 + 0.2 != 0.3 in IEEE 754). For monetary calculations, rounding errors compound: a 0.0001 error per transaction multiplied by 10 million transactions equals thousands of dollars in discrepancy. BigDecimal(String) (not BigDecimal(double)) is precise; RoundingMode.HALF_EVEN (banker's rounding) minimizes systematic bias. The Money pattern (Fowler) wraps BigDecimal with a Currency to prevent mixing USD and EUR accidentally.","correct":"Use BigDecimal for exact decimal arithmetic, or represent money as the smallest currency unit in a long (e.g., cents for USD).","distractors":["Each transaction must be idempotent: if the ATM dispenses cash and then power fails before writing the debit to the ledger, the debit should be recorded on recovery.","Option 1 (optimistic locking): add a version field to the seat record; the booking transaction does UPDATE seat SET status='BOOKED', version=version+1 WHERE id=?","A HashMap alone gives O(1) key lookup but no ordering — you can't efficiently find \"the least recently used entry\" without an O(n) scan."]},{"id":"lld/system_design_problems#b4938f5d78a2","section":"lld","module":"lld/system_design_problems","moduleName":"system design problems","qIndex":10,"difficulty":"intermediate","question":"When asked to \"add a feature\" mid-interview, how do you handle it gracefully?","answerFull":"Show that the design is open for extension without modification (OCP). Example: \"add a premium parking spot tier.\" If the design uses a SpotType enum + Strategy for pricing, adding premium means: add PREMIUM to the enum, add a PremiumPricingStrategy class — nothing else changes. If the design used if (type == COMPACT) price = 2 hard-coded, adding premium requires touching that method. Use the \"add a feature\" moment to demonstrate OCP compliance, not to improvise. The best answer: \"I anticipated extensibility here — let me show you how this works.\"","correct":"Show that the design is open for extension without modification (OCP).","distractors":["Option 1 (optimistic locking): add a version field to the seat record; the booking transaction does UPDATE seat SET status='BOOKED', version=version+1 WHERE id=?","A HashMap alone gives O(1) key lookup but no ordering — you can't efficiently find \"the least recently used entry\" without an O(n) scan.","A switch statement puts all state logic in one class, violating OCP — adding a new state requires modifying the switch."]},{"id":"lld/system_design_problems#68bc74abe77c","section":"lld","module":"lld/system_design_problems","moduleName":"system design problems","qIndex":11,"difficulty":"advanced","question":"How do you handle the Library Management \"search\" feature in the class design?","answerFull":"Use the Strategy pattern for search: SearchStrategy interface with implementations like TitleSearch, AuthorSearch, ISBNSearch, GenreSearch. The Library.search(String query, SearchStrategy strategy) method delegates to the strategy. Adding a new search type (publication year, keywords) means adding a new strategy class — no change to Library. Alternative for simple cases: one Catalog class with multiple overloaded findBy*() methods. The Strategy approach is justified when search algorithms differ significantly in implementation (linear scan vs inverted index vs external search engine).","correct":"Use the Strategy pattern for search: SearchStrategy interface with implementations like TitleSearch, AuthorSearch, ISBNSearch, GenreSearch.","distractors":["Show that the design is open for extension without modification (OCP).","A switch statement puts all state logic in one class, violating OCP — adding a new state requires modifying the switch.","A HashMap alone gives O(1) key lookup but no ordering — you can't efficiently find \"the least recently used entry\" without an O(n) scan."]},{"id":"lld/system_design_problems#451422874c02","section":"lld","module":"lld/system_design_problems","moduleName":"system design problems","qIndex":12,"difficulty":"advanced","question":"Ride Sharing: how do you design driver-matching, and why is the naive approach a problem at scale?","answerFull":"The naive approach scans every available driver and computes Euclidean distance to the rider's pickup location, picking the nearest one — O(N) per request. This is fine for a 30-minute interview demo with a handful of drivers, but at city scale (tens of thousands of drivers) it's too slow. The production fix is geo-indexing: bucket drivers into geohash cells or an S2/quadtree grid, then only scan drivers in the rider's cell and its neighbors. For the interview: implement the O(N) scan, but explicitly call out the geo-indexing upgrade path — see design_uber and design_proximity_service for the HLD-scale answer. Also discuss the RideState machine — every transition (REQUESTED -> ACCEPTED -> DRIVER_ARRIVED -> IN_PROGRESS -> COMPLETED) should be validated server-side to reject out-of-order client messages.","correct":"The naive approach scans every available driver and computes Euclidean distance to the rider's pickup location, picking the nearest one — O(N) per request.","distractors":["The naive approach rescans the entire board after every move — O(N^2) per move, so O(N^2) work just to check 4 lines through the last-placed cell.","If on a single machine: use AtomicReference<SpotStatus> with compareAndSet(AVAILABLE, OCCUPIED) — only one thread wins the CAS; the other retries and finds no available spot.","Show that the design is open for extension without modification (OCP)."]},{"id":"lld/system_design_problems#bf143d1d5a98","section":"lld","module":"lld/system_design_problems","moduleName":"system design problems","qIndex":13,"difficulty":"advanced","question":"LRU Cache: why do you need a doubly-linked list AND a HashMap — why not just one?","answerFull":"A HashMap alone gives O(1) key lookup but no ordering — you can't efficiently find \"the least recently used entry\" without an O(n) scan. A linked list alone gives ordering (move-to-front on access, evict from the tail) but O(n) lookup by key. Combining them gives O(1) for both: the HashMap maps key -> Node, and the node is already wired into the doubly-linked list, so get() does a HashMap lookup then an O(1) pointer-relinking to move the node to the front. A *singly*-linked list doesn't work either — removing a node from the middle requires its prev pointer to relink prev.next, which a singly-linked list doesn't have without an O(n) walk. This is the detail that separates a working O(1) LRU from an accidentally-O(n) one.","correct":"A HashMap alone gives O(1) key lookup but no ordering — you can't efficiently find \"the least recently used entry\" without an O(n) scan.","distractors":["SCAN (also called the \"elevator algorithm\"): the elevator moves in one direction, stopping at all requested floors, then reverses.","Show that the design is open for extension without modification (OCP).","The naive approach rescans the entire board after every move — O(N^2) per move, so O(N^2) work just to check 4 lines through the last-placed cell."]},{"id":"lld/system_design_problems#6a0e8714e3c1","section":"lld","module":"lld/system_design_problems","moduleName":"system design problems","qIndex":15,"difficulty":"advanced","question":"Tic-Tac-Toe: how do you make win-checking work for an NxN board without it becoming the bottleneck?","answerFull":"The naive approach rescans the entire board after every move — O(N^2) per move, so O(N^2) work just to check 4 lines through the last-placed cell. The fix is incremental counters: maintain rowCounts[N], colCounts[N], and two diagonal counters, each storing a running sum where X contributes +1 and O contributes -1 (or separate counters per symbol). Placing a move updates at most 4 counters in O(1), and a win is detected the instant |counter| == N. At N=1000, that's the difference between 1,000,000 cell reads per move and 4 integer increments. This incremental-counter technique generalizes to any \"check all lines through a point\" problem — it's the same idea as maintaining row/column sums for a live spreadsheet.","correct":"The naive approach rescans the entire board after every move — O(N^2) per move, so O(N^2) work just to check 4 lines through the last-placed cell.","distractors":["The naive approach scans every available driver and computes Euclidean distance to the rider's pickup location, picking the nearest one — O(N) per request.","Show that the design is open for extension without modification (OCP).","Each move is a Command object: MoveCommand(piece, fromSquare, toSquare, capturedPiece)."]}]