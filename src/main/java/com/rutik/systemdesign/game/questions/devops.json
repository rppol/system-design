[{"id":"devops/artifact_and_registry_management#d25303f66cf6","section":"devops","module":"devops/artifact_and_registry_management","moduleName":"artifact and registry management","qIndex":1,"difficulty":"core","question":"Why promote by digest instead of tag?","answerFull":"Tags are mutable — app:1.4 or app:latest can be re-pushed to point at different bytes, so you can't guarantee what's running or reproduce a build. A digest (sha256:...) is content-addressed and immutable: it always refers to exactly the same bytes. Promoting and deploying by digest gives traceability (\"this exact digest is in prod\"), reproducibility, and a stable target to sign and verify.","correct":"Tags are mutable — app:1.4 or app:latest can be re-pushed to point at different bytes, so you can't guarantee what's running or reproduce a build.","distractors":["It decides \"what can run in production.\" Risks: pulling unverified public images (malware/typosquats), mutable tags swapped to malicious content, weak access control letting attackers push images, and missing provenance.","Tag immutability rejects re-pushing an existing tag, so once app:1.4.0 exists it can never be changed to point at different content.","Because every release is an immutable, retained, digest-identified artifact, rolling back is \"deploy the previous digest\" — which is guaranteed to be the exact bytes that previously worked."]},{"id":"devops/artifact_and_registry_management#53cb4324e208","section":"devops","module":"devops/artifact_and_registry_management","moduleName":"artifact and registry management","qIndex":3,"difficulty":"core","question":"Why use a pull-through cache / proxy repository?","answerFull":"It caches artifacts from public registries (Docker Hub, npm, Maven Central) locally, so builds don't depend on the public internet for every pull. Benefits: resilience to public-registry outages and removals (yanked packages), immunity to anonymous rate limits (Docker Hub throttles unauthenticated pulls, breaking busy CI), faster pulls, and a control point for scanning/allow-listing what enters your environment.","correct":"It caches artifacts from public registries (Docker Hub, npm, Maven Central) locally, so builds don't depend on the public internet for every pull.","distractors":["It decides \"what can run in production.\" Risks: pulling unverified public images (malware/typosquats), mutable tags swapped to malicious content, weak access control letting attackers push images, and missing provenance.","Hosted (local) repos store artifacts your org publishes.","Because every release is an immutable, retained, digest-identified artifact, rolling back is \"deploy the previous digest\" — which is guaranteed to be the exact bytes that previously worked."]},{"id":"devops/artifact_and_registry_management#e9b8d324557a","section":"devops","module":"devops/artifact_and_registry_management","moduleName":"artifact and registry management","qIndex":4,"difficulty":"core","question":"How do you stop a registry's storage from growing forever?","answerFull":"Apply retention/lifecycle policies: expire untagged images after N days (CI churn), keep only the last N tagged releases per repo, and exempt signed/prod artifacts needed for rollback and audit. Without this, every CI push accumulates, ballooning storage cost. Cloud registries (ECR lifecycle policies) and Artifactory/Nexus all support rule-based cleanup.","correct":"Apply retention/lifecycle policies: expire untagged images after N days (CI churn), keep only the last N tagged releases per repo, and exempt signed/prod artifacts needed for rollback and audit.","distractors":["It caches artifacts from public registries (Docker Hub, npm, Maven Central) locally, so builds don't depend on the public internet for every pull.","It decides \"what can run in production.\" Risks: pulling unverified public images (malware/typosquats), mutable tags swapped to malicious content, weak access control letting attackers push images, and missing provenance.","Hosted (local) repos store artifacts your org publishes."]},{"id":"devops/artifact_and_registry_management#a1bfbb6256f3","section":"devops","module":"devops/artifact_and_registry_management","moduleName":"artifact and registry management","qIndex":5,"difficulty":"core","question":"Why is the registry a supply-chain security concern?","answerFull":"It decides \"what can run in production.\" Risks: pulling unverified public images (malware/typosquats), mutable tags swapped to malicious content, weak access control letting attackers push images, and missing provenance. Defenses: scan on push (Trivy), sign images and attach SBOMs (cosign/syft), enforce tag immutability, RBAC on push/pull, and cluster admission that only runs signed, scan-clean digests from your registry.","correct":"It decides \"what can run in production.\" Risks: pulling unverified public images (malware/typosquats), mutable tags swapped to malicious content, weak access control letting attackers push images, and missing provenance.","distractors":["It caches artifacts from public registries (Docker Hub, npm, Maven Central) locally, so builds don't depend on the public internet for every pull.","On push (or promotion), CI scans the image, generates an SBOM (syft), signs the image and attaches the SBOM as a signed attestation (cosign attest).","Tags are mutable — app:1.4 or app:latest can be re-pushed to point at different bytes, so you can't guarantee what's running or reproduce a build."]},{"id":"devops/artifact_and_registry_management#65052420e37f","section":"devops","module":"devops/artifact_and_registry_management","moduleName":"artifact and registry management","qIndex":6,"difficulty":"intermediate","question":"Artifactory/Nexus vs a cloud container registry — when each?","answerFull":"Universal managers (Artifactory/Nexus) handle many package types (Docker, npm, Maven, PyPI, Helm, generic) with promotion, replication, and proxying — ideal for polyglot orgs wanting one system of record. Cloud container registries (ECR/GAR/ACR) are simpler, IAM-integrated, and managed, ideal when you mostly need OCI images in one cloud. Many orgs use cloud registries for images plus an artifact manager for language packages.","correct":"Universal managers (Artifactory/Nexus) handle many package types (Docker, npm, Maven, PyPI, Helm, generic) with promotion, replication, and proxying — ideal for polyglot orgs wanting one system of record.","distractors":["It caches artifacts from public registries (Docker Hub, npm, Maven Central) locally, so builds don't depend on the public internet for every pull.","On push (or promotion), CI scans the image, generates an SBOM (syft), signs the image and attaches the SBOM as a signed attestation (cosign attest).","Apply retention/lifecycle policies: expire untagged images after N days (CI churn), keep only the last N tagged releases per repo, and exempt signed/prod artifacts needed for rollback and audit."]},{"id":"devops/artifact_and_registry_management#e80b62b8409d","section":"devops","module":"devops/artifact_and_registry_management","moduleName":"artifact and registry management","qIndex":7,"difficulty":"intermediate","question":"How does artifact management enable reliable rollback?","answerFull":"Because every release is an immutable, retained, digest-identified artifact, rolling back is \"deploy the previous digest\" — which is guaranteed to be the exact bytes that previously worked. This requires retention policies that keep enough prior releases and not deleting prod artifacts. Mutable tags or aggressive cleanup that removes old releases break rollback.","correct":"Because every release is an immutable, retained, digest-identified artifact, rolling back is \"deploy the previous digest\" — which is guaranteed to be the exact bytes that previously worked.","distractors":["Tags are mutable — app:1.4 or app:latest can be re-pushed to point at different bytes, so you can't guarantee what's running or reproduce a build.","It caches artifacts from public registries (Docker Hub, npm, Maven Central) locally, so builds don't depend on the public internet for every pull.","Apply retention/lifecycle policies: expire untagged images after N days (CI churn), keep only the last N tagged releases per repo, and exempt signed/prod artifacts needed for rollback and audit."]},{"id":"devops/artifact_and_registry_management#594ed3f84333","section":"devops","module":"devops/artifact_and_registry_management","moduleName":"artifact and registry management","qIndex":8,"difficulty":"intermediate","question":"What is tag immutability and why enable it?","answerFull":"Tag immutability rejects re-pushing an existing tag, so once app:1.4.0 exists it can never be changed to point at different content. This prevents silent prod changes (a re-pushed latest altering what runs on the next pull), supports reproducibility, and is a prerequisite for trustworthy signing. Cloud registries (ECR IMMUTABLE) and Harbor support it.","correct":"Tag immutability rejects re-pushing an existing tag, so once app:1.4.0 exists it can never be changed to point at different content.","distractors":["Tags are mutable — app:1.4 or app:latest can be re-pushed to point at different bytes, so you can't guarantee what's running or reproduce a build.","It decides \"what can run in production.\" Risks: pulling unverified public images (malware/typosquats), mutable tags swapped to malicious content, weak access control letting attackers push images, and missing provenance.","Apply retention/lifecycle policies: expire untagged images after N days (CI churn), keep only the last N tagged releases per repo, and exempt signed/prod artifacts needed for rollback and audit."]},{"id":"devops/artifact_and_registry_management#0af2d7f75fe3","section":"devops","module":"devops/artifact_and_registry_management","moduleName":"artifact and registry management","qIndex":9,"difficulty":"intermediate","question":"How do SBOMs and signing fit into the registry workflow?","answerFull":"On push (or promotion), CI scans the image, generates an SBOM (syft), signs the image and attaches the SBOM as a signed attestation (cosign attest). The registry stores these alongside the image. Later, cluster admission (Kyverno/Connaisseur) verifies the signature and attestation before allowing the image to run — so only artifacts with known provenance and a clean bill of materials reach production (see devsecops_and_supply_chain_security).","correct":"On push (or promotion), CI scans the image, generates an SBOM (syft), signs the image and attaches the SBOM as a signed attestation (cosign attest).","distractors":["It decides \"what can run in production.\" Risks: pulling unverified public images (malware/typosquats), mutable tags swapped to malicious content, weak access control letting attackers push images, and missing provenance.","Apply retention/lifecycle policies: expire untagged images after N days (CI churn), keep only the last N tagged releases per repo, and exempt signed/prod artifacts needed for rollback and audit.","Universal managers (Artifactory/Nexus) handle many package types (Docker, npm, Maven, PyPI, Helm, generic) with promotion, replication, and proxying — ideal for polyglot orgs wanting one system of record."]},{"id":"devops/artifact_and_registry_management#954230e34441","section":"devops","module":"devops/artifact_and_registry_management","moduleName":"artifact and registry management","qIndex":10,"difficulty":"intermediate","question":"What are hosted, proxy, and virtual repositories?","answerFull":"Hosted (local) repos store artifacts your org publishes. Proxy (remote) repos are pull-through caches of external public registries. Virtual (aggregated) repos present a single URL that resolves across multiple hosted and proxied repos, so consumers configure one endpoint and the manager decides where each artifact comes from. This structure gives one clean interface while controlling both internal and external sources.","correct":"Hosted (local) repos store artifacts your org publishes.","distractors":["Apply retention/lifecycle policies: expire untagged images after N days (CI churn), keep only the last N tagged releases per repo, and exempt signed/prod artifacts needed for rollback and audit.","It caches artifacts from public registries (Docker Hub, npm, Maven Central) locally, so builds don't depend on the public internet for every pull.","Because every release is an immutable, retained, digest-identified artifact, rolling back is \"deploy the previous digest\" — which is guaranteed to be the exact bytes that previously worked."]},{"id":"devops/ci_cd_fundamentals#25552173a693","section":"devops","module":"devops/ci_cd_fundamentals","moduleName":"ci cd fundamentals","qIndex":1,"difficulty":"core","question":"Define CI, Continuous Delivery, and Continuous Deployment.","answerFull":"CI automatically builds and tests every commit and integrates changes frequently, catching breakage early. Continuous Delivery extends this so every passing change is *deployable* to production on demand — a human presses the button. Continuous Deployment removes that human gate: every change passing the pipeline is *automatically* released to production. The progression is about how far automation extends and whether a human approves the prod release.","correct":"CI automatically builds and tests every commit and integrates changes frequently, catching breakage early.","distractors":["An immutable, versioned artifact — typically a container image — built once and identified by content (the image digest) or commit SHA, not a mutable tag like latest.","Flaky tests (random pass/fail) train developers to reflexively re-run, which masks genuine failures and lets real bugs through, destroying trust in the suite.","Use affected-target detection (Bazel, Nx, Turborepo) to build and test only the projects a commit actually touches, rather than the whole repo."]},{"id":"devops/ci_cd_fundamentals#2e9b25c8d88b","section":"devops","module":"devops/ci_cd_fundamentals","moduleName":"ci cd fundamentals","qIndex":2,"difficulty":"core","question":"Why \"build once, promote the same artifact\"?","answerFull":"Because rebuilding per environment means prod runs a different binary than the one tested in staging, reintroducing the \"works in staging, breaks in prod\" class of bugs. You build the artifact once, version it (by commit SHA/digest), and deploy that exact immutable artifact through environments, injecting differences as *configuration at deploy time*. What you tested is precisely what ships.","correct":"Because rebuilding per environment means prod runs a different binary than the one tested in staging, reintroducing the \"works in staging, breaks in prod\" class of bugs.","distractors":["An immutable, versioned artifact — typically a container image — built once and identified by content (the image digest) or commit SHA, not a mutable tag like latest.","Caching (reuse dependencies and build layers), parallelism (run independent jobs concurrently so wall-clock = slowest job, not the sum), running only affected tests, and ordering fast cheap checks first.","Shift left means running checks as early (and cheaply) as possible in the lifecycle."]},{"id":"devops/ci_cd_fundamentals#b941a3c6013c","section":"devops","module":"devops/ci_cd_fundamentals","moduleName":"ci cd fundamentals","qIndex":3,"difficulty":"core","question":"What makes a pipeline fast, and why does speed matter?","answerFull":"Caching (reuse dependencies and build layers), parallelism (run independent jobs concurrently so wall-clock = slowest job, not the sum), running only affected tests, and ordering fast cheap checks first. Speed matters because slow pipelines erode trust: developers batch changes, skip checks, and lose the tight feedback loop that makes small, safe, frequent deploys possible — the behavior that correlates with high-performing teams.","correct":"Caching (reuse dependencies and build layers), parallelism (run independent jobs concurrently so wall-clock = slowest job, not the sum), running only affected tests, and ordering fast cheap checks first.","distractors":["Flaky tests (random pass/fail) train developers to reflexively re-run, which masks genuine failures and lets real bugs through, destroying trust in the suite.","CI automatically builds and tests every commit and integrates changes frequently, catching breakage early.","Store them in the platform's encrypted secret store (never in the repo or logs), mask them in output, and scope them to the minimum jobs."]},{"id":"devops/ci_cd_fundamentals#4eb643895b06","section":"devops","module":"devops/ci_cd_fundamentals","moduleName":"ci cd fundamentals","qIndex":4,"difficulty":"core","question":"What are ephemeral runners and why prefer them?","answerFull":"Ephemeral runners are fresh, isolated execution environments (container/VM) created per job and destroyed after. They guarantee reproducibility (no leftover state from a previous build), prevent cross-job contamination and secret leakage, and scale horizontally. Persistent/static runners risk \"works because of leftover state\" bugs and are a security/cleanliness liability — used only for special hardware needs.","correct":"Ephemeral runners are fresh, isolated execution environments (container/VM) created per job and destroyed after.","distractors":["Caching (reuse dependencies and build layers), parallelism (run independent jobs concurrently so wall-clock = slowest job, not the sum), running only affected tests, and ordering fast cheap checks first.","An immutable, versioned artifact — typically a container image — built once and identified by content (the image digest) or commit SHA, not a mutable tag like latest.","Because rebuilding per environment means prod runs a different binary than the one tested in staging, reintroducing the \"works in staging, breaks in prod\" class of bugs."]},{"id":"devops/ci_cd_fundamentals#a1c94f81e644","section":"devops","module":"devops/ci_cd_fundamentals","moduleName":"ci cd fundamentals","qIndex":5,"difficulty":"core","question":"How should secrets be handled in CI/CD?","answerFull":"Store them in the platform's encrypted secret store (never in the repo or logs), mask them in output, and scope them to the minimum jobs. Best practice for cloud access is OIDC federation: the runner exchanges a short-lived identity token for a cloud role, so no long-lived cloud keys live in CI at all — eliminating the most commonly leaked secret. Rotate any secret that must be static.","correct":"Store them in the platform's encrypted secret store (never in the repo or logs), mask them in output, and scope them to the minimum jobs.","distractors":["Caching (reuse dependencies and build layers), parallelism (run independent jobs concurrently so wall-clock = slowest job, not the sum), running only affected tests, and ordering fast cheap checks first.","Use affected-target detection (Bazel, Nx, Turborepo) to build and test only the projects a commit actually touches, rather than the whole repo.","An immutable, versioned artifact — typically a container image — built once and identified by content (the image digest) or commit SHA, not a mutable tag like latest."]},{"id":"devops/ci_cd_fundamentals#10cfdf29cd21","section":"devops","module":"devops/ci_cd_fundamentals","moduleName":"ci cd fundamentals","qIndex":6,"difficulty":"intermediate","question":"What is \"shift left\" and give examples?","answerFull":"Shift left means running checks as early (and cheaply) as possible in the lifecycle. Examples: linting and unit tests on every commit/PR, SAST and dependency scanning in CI before merge, and infrastructure policy checks before apply. Catching a bug or vulnerability at PR time is far cheaper than in production, and early fast checks give developers immediate feedback.","correct":"Shift left means running checks as early (and cheaply) as possible in the lifecycle.","distractors":["Because rebuilding per environment means prod runs a different binary than the one tested in staging, reintroducing the \"works in staging, breaks in prod\" class of bugs.","CI automatically builds and tests every commit and integrates changes frequently, catching breakage early.","Caching (reuse dependencies and build layers), parallelism (run independent jobs concurrently so wall-clock = slowest job, not the sum), running only affected tests, and ordering fast cheap checks first."]},{"id":"devops/ci_cd_fundamentals#161413cf10a9","section":"devops","module":"devops/ci_cd_fundamentals","moduleName":"ci cd fundamentals","qIndex":7,"difficulty":"intermediate","question":"How do flaky tests damage a pipeline, and how do you handle them?","answerFull":"Flaky tests (random pass/fail) train developers to reflexively re-run, which masks genuine failures and lets real bugs through, destroying trust in the suite. Handle them by quarantining flaky tests out of the gating path, fixing the root cause (timing, shared mutable state, real network/sleep), and tracking flakiness as a defect with metrics — not by adding retries that hide the problem.","correct":"Flaky tests (random pass/fail) train developers to reflexively re-run, which masks genuine failures and lets real bugs through, destroying trust in the suite.","distractors":["Because rebuilding per environment means prod runs a different binary than the one tested in staging, reintroducing the \"works in staging, breaks in prod\" class of bugs.","CI automatically builds and tests every commit and integrates changes frequently, catching breakage early.","Caching (reuse dependencies and build layers), parallelism (run independent jobs concurrently so wall-clock = slowest job, not the sum), running only affected tests, and ordering fast cheap checks first."]},{"id":"devops/ci_cd_fundamentals#3cc711a5eab4","section":"devops","module":"devops/ci_cd_fundamentals","moduleName":"ci cd fundamentals","qIndex":8,"difficulty":"intermediate","question":"What's the difference between Continuous Delivery and Continuous Deployment, and when choose each?","answerFull":"Both fully automate up to production-readiness; the difference is the final gate. Continuous Delivery keeps a human approval before prod (appropriate when test confidence/observability isn't yet sufficient, or compliance requires sign-off). Continuous Deployment auto-ships every passing change (appropriate when you have strong automated tests, progressive delivery, and fast rollback). Many teams start with Delivery and graduate to Deployment as confidence grows.","correct":"Both fully automate up to production-readiness; the difference is the final gate.","distractors":["The four DORA metrics are deployment frequency, lead time for changes, change failure rate, and time to restore service.","An immutable, versioned artifact — typically a container image — built once and identified by content (the image digest) or commit SHA, not a mutable tag like latest.","Because rebuilding per environment means prod runs a different binary than the one tested in staging, reintroducing the \"works in staging, breaks in prod\" class of bugs."]},{"id":"devops/ci_cd_fundamentals#205f10b5fdb1","section":"devops","module":"devops/ci_cd_fundamentals","moduleName":"ci cd fundamentals","qIndex":9,"difficulty":"intermediate","question":"How do you keep CI fast in a large monorepo?","answerFull":"Use affected-target detection (Bazel, Nx, Turborepo) to build and test only the projects a commit actually touches, rather than the whole repo. Combine with remote build caching (reuse outputs across runs/developers) and parallelism. Without this, monorepo CI time grows with the repo, not the change — the classic monorepo scaling failure.","correct":"Use affected-target detection (Bazel, Nx, Turborepo) to build and test only the projects a commit actually touches, rather than the whole repo.","distractors":["Store them in the platform's encrypted secret store (never in the repo or logs), mask them in output, and scope them to the minimum jobs.","CI automatically builds and tests every commit and integrates changes frequently, catching breakage early.","Caching (reuse dependencies and build layers), parallelism (run independent jobs concurrently so wall-clock = slowest job, not the sum), running only affected tests, and ordering fast cheap checks first."]},{"id":"devops/ci_cd_fundamentals#cc1eb220ad28","section":"devops","module":"devops/ci_cd_fundamentals","moduleName":"ci cd fundamentals","qIndex":10,"difficulty":"intermediate","question":"What artifact should move through the pipeline, and how is it identified?","answerFull":"An immutable, versioned artifact — typically a container image — built once and identified by content (the image digest) or commit SHA, not a mutable tag like latest. This gives full traceability (which commit is in prod), reproducibility (the digest is exact bits), and supply-chain integrity (you can sign/scan that specific artifact and gate on it — see devsecops_and_supply_chain_security).","correct":"An immutable, versioned artifact — typically a container image — built once and identified by content (the image digest) or commit SHA, not a mutable tag like latest.","distractors":["Use affected-target detection (Bazel, Nx, Turborepo) to build and test only the projects a commit actually touches, rather than the whole repo.","CI produces and verifies the artifact (build, test, scan, push image).","CI automatically builds and tests every commit and integrates changes frequently, catching breakage early."]},{"id":"devops/ci_cd_fundamentals#21a9a5a3eb3c","section":"devops","module":"devops/ci_cd_fundamentals","moduleName":"ci cd fundamentals","qIndex":11,"difficulty":"advanced","question":"What are the DORA metrics and how do they relate to CI/CD?","answerFull":"The four DORA metrics are deployment frequency, lead time for changes, change failure rate, and time to restore service. High performers score well on all four, and a fast, reliable, automated CI/CD pipeline is the primary enabler: it shortens lead time, raises deployment frequency, and (with good tests + fast rollback) lowers change failure rate and restore time. They're the standard way to measure delivery performance.","correct":"The four DORA metrics are deployment frequency, lead time for changes, change failure rate, and time to restore service.","distractors":["CI automatically builds and tests every commit and integrates changes frequently, catching breakage early.","An immutable, versioned artifact — typically a container image — built once and identified by content (the image digest) or commit SHA, not a mutable tag like latest.","Because rebuilding per environment means prod runs a different binary than the one tested in staging, reintroducing the \"works in staging, breaks in prod\" class of bugs."]},{"id":"devops/ci_cd_fundamentals#86abf2e7c09e","section":"devops","module":"devops/ci_cd_fundamentals","moduleName":"ci cd fundamentals","qIndex":12,"difficulty":"advanced","question":"How does CI/CD relate to GitOps?","answerFull":"CI produces and verifies the artifact (build, test, scan, push image). CD can be push-based (the pipeline runs kubectl apply) or pull-based GitOps (CI updates a manifests repo, and an in-cluster agent like ArgoCD reconciles). GitOps separates \"build/verify\" (CI) from \"deploy\" (Git-driven reconciliation), giving auditability and drift detection (see gitops_argocd_flux). CI/CD fundamentals underpin both models.","correct":"CI produces and verifies the artifact (build, test, scan, push image).","distractors":["Caching (reuse dependencies and build layers), parallelism (run independent jobs concurrently so wall-clock = slowest job, not the sum), running only affected tests, and ordering fast cheap checks first.","An immutable, versioned artifact — typically a container image — built once and identified by content (the image digest) or commit SHA, not a mutable tag like latest.","Use affected-target detection (Bazel, Nx, Turborepo) to build and test only the projects a commit actually touches, rather than the whole repo."]},{"id":"devops/ci_cd_platforms#6dddeaced0d2","section":"devops","module":"devops/ci_cd_platforms","moduleName":"ci cd platforms","qIndex":2,"difficulty":"core","question":"SaaS (GitHub Actions/GitLab CI) vs Jenkins — core tradeoff?","answerFull":"SaaS platforms are low-ops (no control plane to run/patch), tightly integrated with the SCM, and YAML-configured, at the cost of flexibility and some lock-in. Jenkins is self-hosted and infinitely extensible via plugins and Groovy pipelines (any language, on-prem, custom workflows), at the cost of operating, securing, and patching the server and its plugin sprawl. Choose SaaS for convenience, Jenkins for control.","correct":"SaaS platforms are low-ops (no control plane to run/patch), tightly integrated with the SCM, and YAML-configured, at the cost of flexibility and some lock-in.","distractors":["The workflow requests an OIDC token from GitHub's identity provider, and the cloud (e.g., AWS via an IAM OIDC trust) exchanges it for temporary credentials scoped to a specific role, typically valid ~1 hour.","It decides \"what can run in production.\" Risks: pulling unverified public images (malware/typosquats), mutable tags swapped to malicious content, weak access control letting attackers push images, and missing provenance.","EKS provides a managed, Multi-AZ Kubernetes control plane (API server, etcd) that AWS patches and scales, while you manage the worker nodes (or use Fargate/managed node groups)."]},{"id":"devops/ci_cd_platforms#39db3b88e9ba","section":"devops","module":"devops/ci_cd_platforms","moduleName":"ci cd platforms","qIndex":3,"difficulty":"core","question":"What's distinctive about Kubernetes-native CI (Tekton/Argo)?","answerFull":"Pipelines are Kubernetes CRDs and each step runs as a Pod, so CI/CD shares the cluster's RBAC, secrets, autoscaling, networking, and observability with your applications — no separate runner fleet to manage. It's ideal when your platform is already Kubernetes. The tradeoff is added conceptual complexity and that you need a cluster to run pipelines at all.","correct":"Pipelines are Kubernetes CRDs and each step runs as a Pod, so CI/CD shares the cluster's RBAC, secrets, autoscaling, networking, and observability with your applications — no separate runner fleet to manage.","distractors":["Knative is a Kubernetes-based serverless framework that brings scale-to-zero, request-driven autoscaling, and eventing to your own cluster, so functions/containers run portably on any Kubernetes (any cloud or on-prem).","Conftest runs Rego policies against any structured file — Kubernetes YAML, a terraform show -json plan, Dockerfiles, JSON config — at CI time before the resource reaches a cluster.","EKS provides a managed, Multi-AZ Kubernetes control plane (API server, etcd) that AWS patches and scales, while you manage the worker nodes (or use Fargate/managed node groups)."]},{"id":"devops/ci_cd_platforms#9f6691181f3b","section":"devops","module":"devops/ci_cd_platforms","moduleName":"ci cd platforms","qIndex":5,"difficulty":"core","question":"How do you avoid copy-pasted pipelines across many repos?","answerFull":"Use the platform's reuse mechanism: reusable workflows and composite actions (GitHub Actions), include/extends/templates (GitLab), or shared libraries (Jenkins), and reusable Tasks (Tekton). Define the build/test/deploy logic once in a central, versioned location and reference it from each repo, so a single change updates everyone and pipelines can't drift.","correct":"Use the platform's reuse mechanism: reusable workflows and composite actions (GitHub Actions), include/extends/templates (GitLab), or shared libraries (Jenkins), and reusable Tasks (Tekton).","distractors":["A module is a reusable, parameterized bundle of resources with variable inputs and output outputs — you write a VPC or EKS pattern once and instantiate it per environment.","A chart's Chart.yaml lists dependencies (other charts + versions + repos), optionally toggled by condition.","A ConstraintTemplate defines reusable Rego logic plus a parameter schema (the \"what kind of check\"), and a Constraint instantiates that template with concrete values and a scope (the \"applied here with these settings\")."]},{"id":"devops/ci_cd_platforms#b9f2bcc5010d","section":"devops","module":"devops/ci_cd_platforms","moduleName":"ci cd platforms","qIndex":6,"difficulty":"intermediate","question":"Why is the CI/CD platform a high-value security target?","answerFull":"It holds the credentials to deploy to production and executes arbitrary code (your build scripts, third-party actions/plugins). A compromised pipeline, a malicious dependency in a build, or a vulnerable Jenkins plugin is a direct route to prod. Defenses: OIDC + least-privilege deploy roles (no static admin keys), pinned/reviewed third-party actions, patched control plane, isolated ephemeral runners, and secret masking.","correct":"It holds the credentials to deploy to production and executes arbitrary code (your build scripts, third-party actions/plugins).","distractors":["When you run everything in containers, the container image *is* the configuration — you build it with a Dockerfile and deploy immutable images, so in-host CM is redundant (see containers_and_docker).","Use affected-target detection (Bazel, Nx, Turborepo) to build and test only the projects a commit actually touches, rather than the whole repo.","Deploy = the new code is running in production; release = users actually experience it."]},{"id":"devops/ci_cd_platforms#5bac9047ab28","section":"devops","module":"devops/ci_cd_platforms","moduleName":"ci cd platforms","qIndex":8,"difficulty":"intermediate","question":"What does \"pipeline as code\" buy you, and do all platforms support it?","answerFull":"It means the pipeline definition lives in the repo (YAML/Groovy/CRD), versioned and code-reviewed alongside the application. Benefits: history/auditability, review of pipeline changes, reproducibility, and the ability to branch pipeline changes. All modern platforms support it — GitHub .github/workflows, GitLab .gitlab-ci.yml, Jenkins Jenkinsfile, Tekton CRDs — though older Jenkins setups with UI-clicked freestyle jobs are an anti-pattern to migrate away from.","correct":"It means the pipeline definition lives in the repo (YAML/Groovy/CRD), versioned and code-reviewed alongside the application.","distractors":["An immutable, versioned artifact — typically a container image — built once and identified by content (the image digest) or commit SHA, not a mutable tag like latest.","SAST analyzes source code statically for vulnerable patterns; DAST probes a running application with crafted requests; SCA inspects your dependency manifests against a CVE database.","Because the entire desired state lives in Git, recovering a cluster is \"deploy a fresh ArgoCD/Flux and point it at the repo\" — the agent reconciles everything back."]},{"id":"devops/ci_cd_platforms#9c9fc02dd0ec","section":"devops","module":"devops/ci_cd_platforms","moduleName":"ci cd platforms","qIndex":10,"difficulty":"intermediate","question":"GitLab CI needs: vs stages — what's the difference?","answerFull":"Stages enforce sequential ordering (all test jobs finish before any build job starts). needs: creates a directed acyclic graph: a job starts as soon as its specific dependencies complete, regardless of stage, enabling more parallelism and faster pipelines. For example, a deploy-docs job needing only build-docs can run while unrelated test jobs are still going, rather than waiting for the whole test stage.","correct":"Stages enforce sequential ordering (all test jobs finish before any build job starts).","distractors":["Caching (reuse dependencies and build layers), parallelism (run independent jobs concurrently so wall-clock = slowest job, not the sum), running only affected tests, and ordering fast cheap checks first.","Use the platform's reuse mechanism: reusable workflows and composite actions (GitHub Actions), include/extends/templates (GitLab), or shared libraries (Jenkins), and reusable Tasks (Tekton).","Store them in the platform's encrypted secret store (never in the repo or logs), mask them in output, and scope them to the minimum jobs."]},{"id":"devops/cloud_cost_optimization_finops#006b55c7a3d3","section":"devops","module":"devops/cloud_cost_optimization_finops","moduleName":"cloud cost optimization finops","qIndex":1,"difficulty":"core","question":"What is FinOps and what are its three lifecycle phases?","answerFull":"FinOps is a cross-functional operating model — engineering, finance, and product together — for managing cloud cost as a continuous discipline that maximizes business value, not just minimizes spend. Its lifecycle is Inform (visibility and cost allocation via tagging, showback, budgets), Optimize (rightsizing, eliminating waste, applying commitment discounts), and Operate (governance, automation, anomaly detection, culture). The loop is iterative — you continuously inform, optimize, and operate as usage and the business evolve.","correct":"FinOps is a cross-functional operating model — engineering, finance, and product together — for managing cloud cost as a continuous discipline that maximizes business value, not just minimizes spend.","distractors":["Showback reports each team or product its cloud cost for visibility and accountability without moving money, while chargeback actually bills those costs back to the team's budget or P&L.","Shift left means running checks as early (and cheaply) as possible in the lifecycle.","ERROR for actionable failures that need attention, WARN for degraded-but-handled conditions, INFO for significant business/lifecycle milestones, and DEBUG for verbose developer detail."]},{"id":"devops/cloud_cost_optimization_finops#a3d8bede3820","section":"devops","module":"devops/cloud_cost_optimization_finops","moduleName":"cloud cost optimization finops","qIndex":2,"difficulty":"core","question":"Compare On-Demand, Spot, Reserved Instances, and Savings Plans.","answerFull":"On-Demand is pay-per-use with no commitment at the highest unit price, for spiky/unpredictable workloads. Spot uses spare capacity at up to 90% off but can be reclaimed with a 2-minute warning, so it's for fault-tolerant, interruptible workloads. Reserved Instances commit to a specific instance family/Region for 1 or 3 years for ~40-72% off, while Savings Plans commit to a $/hour spend for ~30-66% off with more flexibility across families/Regions. The strategy is to cover the steady baseline with commitments, burst on On-Demand, and run interruptible work on Spot.","correct":"On-Demand is pay-per-use with no commitment at the highest unit price, for spiky/unpredictable workloads.","distractors":["API Gateway provides rich API features — request validation, throttling, API keys, usage plans, authorizers (JWT/IAM/Cognito), and caching — and is the default for public APIs, but costs more per request at high volume.","In a distributed system, workloads run on ephemeral pods/VMs that are created and destroyed constantly, so local logs disappear when the container dies — exactly when you need them post-incident.","Requests are a scheduling guarantee (cpu.weight / reserved memory used by the scheduler to place the pod); limits are enforcement — cpu.max (CFS quota, causes throttling) and memory.max (causes OOM kill on breach)."]},{"id":"devops/cloud_cost_optimization_finops#7134f73e25d0","section":"devops","module":"devops/cloud_cost_optimization_finops","moduleName":"cloud cost optimization finops","qIndex":4,"difficulty":"core","question":"What's the difference between showback and chargeback?","answerFull":"Showback reports each team or product its cloud cost for visibility and accountability without moving money, while chargeback actually bills those costs back to the team's budget or P&L. Showback is lower-friction and often enough to change behavior (teams optimize once they see their bill), whereas chargeback creates harder financial accountability but more organizational friction and disputes. Many organizations start with showback and graduate to chargeback as the FinOps practice matures.","correct":"Showback reports each team or product its cloud cost for visibility and accountability without moving money, while chargeback actually bills those costs back to the team's budget or P&L.","distractors":["An IDP is a self-service layer of tooling and golden paths that lets product engineers ship software without mastering the underlying infrastructure (Kubernetes, Terraform, IAM, networking).","Chaos engineering deliberately injects failures (kill instances, blackhole a region, add latency) into production-like systems to validate that resilience mechanisms actually work before a real disaster tests them.","Symptoms (high error rate, latency, SLO burn) are what users actually experience, so alerting on them catches every problem that matters and ignores causes that have no user impact."]},{"id":"devops/cloud_cost_optimization_finops#f1a59b8baace","section":"devops","module":"devops/cloud_cost_optimization_finops","moduleName":"cloud cost optimization finops","qIndex":9,"difficulty":"intermediate","question":"How do you catch cost overruns before the monthly invoice?","answerFull":"Enable cost anomaly detection (AWS Cost Anomaly Detection or equivalent) to alert on unusual spend patterns within hours, and set per-team AWS Budgets with threshold alerts (e.g., at 80% of the monthly budget). Tie budgets to cost-allocation tags so each team is alerted on its own overspend. This shifts cost from a month-end surprise to a near-real-time signal engineers can act on immediately.","correct":"Enable cost anomaly detection (AWS Cost Anomaly Detection or equivalent) to alert on unusual spend patterns within hours, and set per-team AWS Budgets with threshold alerts (e.g., at 80% of the monthly budget).","distractors":["You alert when the error-budget burn rate exceeds a threshold over both a long window and a short confirmation window simultaneously, ANDed together.","Store backups in a separate AWS account with cross-account replication, enable S3 Object Lock (WORM) in compliance mode so even root cannot delete before retention expires, and use MFA-delete.","FinOps is a cross-functional operating model — engineering, finance, and product together — for managing cloud cost as a continuous discipline that maximizes business value, not just minimizes spend."]},{"id":"devops/cloud_fundamentals_and_aws#407552a19453","section":"devops","module":"devops/cloud_fundamentals_and_aws","moduleName":"cloud fundamentals and aws","qIndex":1,"difficulty":"core","question":"What is the difference between a Region and an Availability Zone, and why does it matter for design?","answerFull":"A Region is a geographic area (e.g., us-east-1); an Availability Zone is one or more discrete data centers within a Region with independent power/cooling/networking, and each Region has at least 3 AZs. It matters because AZs fail independently, so spreading instances and using Multi-AZ managed services gives you fault tolerance against a data-center-level outage. Always deploy across at least two AZs for any production workload.","correct":"A Region is a geographic area (e.g., us-east-1); an Availability Zone is one or more discrete data centers within a Region with independent power/cooling/networking, and each Region has at least 3 AZs.","distractors":["An Auto Scaling Group has min/desired/max sizes and scaling policies — target tracking (e.g., keep CPU at 50%), step scaling on CloudWatch alarms, or scheduled scaling.","S3 stores objects redundantly across multiple devices in at least three AZs, yielding 99.999999999% (11 nines) durability — statistically you'd lose one object in 10 million every 10,000 years.","Multi-AZ maintains a synchronous standby in another AZ for high availability with automatic failover (~60-120s) but the standby is not readable."]},{"id":"devops/cloud_fundamentals_and_aws#addb613c2a4d","section":"devops","module":"devops/cloud_fundamentals_and_aws","moduleName":"cloud fundamentals and aws","qIndex":2,"difficulty":"core","question":"Explain IAM roles vs IAM users and when to use each.","answerFull":"An IAM user has long-lived credentials (password/access keys) tied to a human or legacy app; an IAM role has no permanent credentials and is *assumed* via STS to get temporary tokens (default 1-hour). Use roles for workloads (EC2 instance profiles, EKS IRSA, cross-account access) and for humans via SSO, because temporary credentials drastically reduce the blast radius of a leak. Reserve users for break-glass or systems that genuinely can't assume roles, and rotate their keys.","correct":"An IAM user has long-lived credentials (password/access keys) tied to a human or legacy app; an IAM role has no permanent credentials and is *assumed* via STS to get temporary tokens (default 1-hour).","distractors":["A Region is a geographic area (e.g., us-east-1); an Availability Zone is one or more discrete data centers within a Region with independent power/cooling/networking, and each Region has at least 3 AZs.","An Auto Scaling Group has min/desired/max sizes and scaling policies — target tracking (e.g., keep CPU at 50%), step scaling on CloudWatch alarms, or scheduled scaling.","A NAT Gateway lets instances in private subnets make outbound internet connections (e.g., to download packages) without being inbound-reachable."]},{"id":"devops/cloud_fundamentals_and_aws#00a4b19a4774","section":"devops","module":"devops/cloud_fundamentals_and_aws","moduleName":"cloud fundamentals and aws","qIndex":3,"difficulty":"core","question":"Security groups vs network ACLs — what's the difference?","answerFull":"Security groups are stateful, instance/ENI-level, allow-only rules — return traffic is automatically permitted. NACLs are stateless, subnet-level, and support both allow and explicit deny, so you must define inbound and outbound rules separately. Use security groups as the primary control (they're easier and stateful) and NACLs for coarse subnet-wide deny rules like blocking a bad CIDR.","correct":"Security groups are stateful, instance/ENI-level, allow-only rules — return traffic is automatically permitted.","distractors":["EKS provides a managed, Multi-AZ Kubernetes control plane (API server, etcd) that AWS patches and scales, while you manage the worker nodes (or use Fargate/managed node groups).","A NAT Gateway lets instances in private subnets make outbound internet connections (e.g., to download packages) without being inbound-reachable.","A Region is a geographic area (e.g., us-east-1); an Availability Zone is one or more discrete data centers within a Region with independent power/cooling/networking, and each Region has at least 3 AZs."]},{"id":"devops/cloud_fundamentals_and_aws#0093301808f2","section":"devops","module":"devops/cloud_fundamentals_and_aws","moduleName":"cloud fundamentals and aws","qIndex":4,"difficulty":"core","question":"RDS Multi-AZ vs read replicas — what problem does each solve?","answerFull":"Multi-AZ maintains a synchronous standby in another AZ for high availability with automatic failover (~60-120s) but the standby is not readable. Read replicas are asynchronous copies you can read from to scale read traffic, and they can be promoted to standalone primaries. They solve different problems — HA vs read scaling — and are often used together: Multi-AZ for resilience plus replicas for read-heavy workloads.","correct":"Multi-AZ maintains a synchronous standby in another AZ for high availability with automatic failover (~60-120s) but the standby is not readable.","distractors":["A Region is a geographic area (e.g., us-east-1); an Availability Zone is one or more discrete data centers within a Region with independent power/cooling/networking, and each Region has at least 3 AZs.","EKS provides a managed, Multi-AZ Kubernetes control plane (API server, etcd) that AWS patches and scales, while you manage the worker nodes (or use Fargate/managed node groups).","A NAT Gateway lets instances in private subnets make outbound internet connections (e.g., to download packages) without being inbound-reachable."]},{"id":"devops/cloud_fundamentals_and_aws#a575692877e4","section":"devops","module":"devops/cloud_fundamentals_and_aws","moduleName":"cloud fundamentals and aws","qIndex":5,"difficulty":"core","question":"How do you choose between ALB and NLB?","answerFull":"Choose ALB for HTTP/HTTPS/gRPC when you need Layer-7 features: path/host-based routing, WAF integration, TLS termination, and sticky sessions. Choose NLB for Layer-4 TCP/UDP when you need ultra-low latency, millions of requests per second, a static IP, or to preserve the client source IP. A common pattern is NLB in front of ALB only when you specifically need NLB's static IP plus ALB's routing.","correct":"Choose ALB for HTTP/HTTPS/gRPC when you need Layer-7 features: path/host-based routing, WAF integration, TLS termination, and sticky sessions.","distractors":["A NAT Gateway lets instances in private subnets make outbound internet connections (e.g., to download packages) without being inbound-reachable.","A Region is a geographic area (e.g., us-east-1); an Availability Zone is one or more discrete data centers within a Region with independent power/cooling/networking, and each Region has at least 3 AZs.","An Auto Scaling Group has min/desired/max sizes and scaling policies — target tracking (e.g., keep CPU at 50%), step scaling on CloudWatch alarms, or scheduled scaling."]},{"id":"devops/cloud_fundamentals_and_aws#bb996a97fc91","section":"devops","module":"devops/cloud_fundamentals_and_aws","moduleName":"cloud fundamentals and aws","qIndex":6,"difficulty":"intermediate","question":"What gives S3 its durability, and what are the storage classes for?","answerFull":"S3 stores objects redundantly across multiple devices in at least three AZs, yielding 99.999999999% (11 nines) durability — statistically you'd lose one object in 10 million every 10,000 years. Storage classes trade retrieval latency/cost for storage cost: Standard for hot data, Standard-IA and Glacier Instant for infrequent access, and Glacier Flexible/Deep Archive for archives (minutes to 12 hours retrieval). Use lifecycle policies to transition data automatically and cut cost by up to ~95% on cold data.","correct":"S3 stores objects redundantly across multiple devices in at least three AZs, yielding 99.999999999% (11 nines) durability — statistically you'd lose one object in 10 million every 10,000 years.","distractors":["A Region is a geographic area (e.g., us-east-1); an Availability Zone is one or more discrete data centers within a Region with independent power/cooling/networking, and each Region has at least 3 AZs.","A NAT Gateway lets instances in private subnets make outbound internet connections (e.g., to download packages) without being inbound-reachable.","An Auto Scaling Group has min/desired/max sizes and scaling policies — target tracking (e.g., keep CPU at 50%), step scaling on CloudWatch alarms, or scheduled scaling."]},{"id":"devops/cloud_fundamentals_and_aws#26216a160158","section":"devops","module":"devops/cloud_fundamentals_and_aws","moduleName":"cloud fundamentals and aws","qIndex":7,"difficulty":"intermediate","question":"How does EC2 Auto Scaling decide when and how to scale?","answerFull":"An Auto Scaling Group has min/desired/max sizes and scaling policies — target tracking (e.g., keep CPU at 50%), step scaling on CloudWatch alarms, or scheduled scaling. It uses health checks (EC2 status or ELB) to replace unhealthy instances and launches new ones from a launch template across the configured AZs. Set the health check type to ELB so an instance failing the application health check (not just the hypervisor check) gets replaced.","correct":"An Auto Scaling Group has min/desired/max sizes and scaling policies — target tracking (e.g., keep CPU at 50%), step scaling on CloudWatch alarms, or scheduled scaling.","distractors":["A Region is a geographic area (e.g., us-east-1); an Availability Zone is one or more discrete data centers within a Region with independent power/cooling/networking, and each Region has at least 3 AZs.","An IAM user has long-lived credentials (password/access keys) tied to a human or legacy app; an IAM role has no permanent credentials and is *assumed* via STS to get temporary tokens (default 1-hour).","A NAT Gateway lets instances in private subnets make outbound internet connections (e.g., to download packages) without being inbound-reachable."]},{"id":"devops/cloud_fundamentals_and_aws#f5866e2f9426","section":"devops","module":"devops/cloud_fundamentals_and_aws","moduleName":"cloud fundamentals and aws","qIndex":8,"difficulty":"intermediate","question":"What's the role of a NAT Gateway, and how can it bite you on cost?","answerFull":"A NAT Gateway lets instances in private subnets make outbound internet connections (e.g., to download packages) without being inbound-reachable. It charges per hour (~$0.045) plus ~$0.045 per GB processed, so high-volume egress — especially to S3 or other AWS services routed through it — gets expensive fast. Use VPC Gateway Endpoints for S3/DynamoDB (free) and Interface Endpoints (PrivateLink) for other services to bypass NAT.","correct":"A NAT Gateway lets instances in private subnets make outbound internet connections (e.g., to download packages) without being inbound-reachable.","distractors":["An IAM user has long-lived credentials (password/access keys) tied to a human or legacy app; an IAM role has no permanent credentials and is *assumed* via STS to get temporary tokens (default 1-hour).","A Region is a geographic area (e.g., us-east-1); an Availability Zone is one or more discrete data centers within a Region with independent power/cooling/networking, and each Region has at least 3 AZs.","An Auto Scaling Group has min/desired/max sizes and scaling policies — target tracking (e.g., keep CPU at 50%), step scaling on CloudWatch alarms, or scheduled scaling."]},{"id":"devops/cloud_fundamentals_and_aws#1377467cc8b4","section":"devops","module":"devops/cloud_fundamentals_and_aws","moduleName":"cloud fundamentals and aws","qIndex":10,"difficulty":"intermediate","question":"How does EKS differ from running Kubernetes on EC2 yourself?","answerFull":"EKS provides a managed, Multi-AZ Kubernetes control plane (API server, etcd) that AWS patches and scales, while you manage the worker nodes (or use Fargate/managed node groups). Self-managed Kubernetes on EC2 gives full control but you operate the control plane, including etcd backups, upgrades, and HA. Choose EKS to offload the hardest operational part (the control plane) and integrate with IAM via IRSA — see kubernetes_architecture.","correct":"EKS provides a managed, Multi-AZ Kubernetes control plane (API server, etcd) that AWS patches and scales, while you manage the worker nodes (or use Fargate/managed node groups).","distractors":["Security groups are stateful, instance/ENI-level, allow-only rules — return traffic is automatically permitted.","Multi-AZ maintains a synchronous standby in another AZ for high availability with automatic failover (~60-120s) but the standby is not readable.","A NAT Gateway lets instances in private subnets make outbound internet connections (e.g., to download packages) without being inbound-reachable."]},{"id":"devops/cloud_networking_and_cdn#a5ea8316e661","section":"devops","module":"devops/cloud_networking_and_cdn","moduleName":"cloud networking and cdn","qIndex":1,"difficulty":"core","question":"Why isn't VPC peering transitive, and what do you use instead at scale?","answerFull":"VPC peering is a direct one-to-one connection that doesn't forward traffic through an intermediate VPC, so peering A-B and B-C does not give A-C — by design, to keep routing explicit and secure. At scale, a full mesh requires N(N-1)/2 peerings (50 VPCs = 1225 connections), which is unmanageable. Use a Transit Gateway, a regional hub that provides transitive routing and centralized route segmentation across many VPCs, VPNs, and Direct Connect links.","correct":"VPC peering is a direct one-to-one connection that doesn't forward traffic through an intermediate VPC, so peering A-B and B-C does not give A-C — by design, to keep routing explicit and secure.","distractors":["A GCP VPC is a global resource with regional subnets, whereas AWS VPCs and Azure VNets are regional.","VPC endpoints (Gateway for S3/DynamoDB, Interface/PrivateLink for others) keep traffic on the AWS network without traversing the NAT gateway.","They provide weighted traffic routing: the mesh/ingress can send, say, 5% of requests to the canary Service and 95% to stable, and adjust the weights as the rollout progresses."]},{"id":"devops/cloud_networking_and_cdn#b289c351e38b","section":"devops","module":"devops/cloud_networking_and_cdn","moduleName":"cloud networking and cdn","qIndex":4,"difficulty":"core","question":"What is a cache key and why does getting it wrong destroy a CDN's value?","answerFull":"The cache key is the set of attributes (path, plus chosen headers/query strings/cookies) the CDN uses to decide whether a request matches a cached object. If the key is too broad — e.g., including Authorization or a unique session cookie — every request looks unique, the hit ratio collapses toward 0%, and traffic floods the origin. If too narrow on personalized content, one user could receive another's cached response, so you must cache only on attributes that genuinely vary the response and exclude per-user data.","correct":"The cache key is the set of attributes (path, plus chosen headers/query strings/cookies) the CDN uses to decide whether a request matches a cached object.","distractors":["Each Dockerfile instruction produces a layer cached by the instruction plus the state of its inputs (e.g., the files a COPY references).","Use a mature community operator (Prometheus Operator, cert-manager, CloudNativePG) whenever one fits — operators are subtle (idempotency, finalizers, status, leader election, edge cases) and a buggy one can destroy data.","The thinnest viable platform is the smallest abstraction that measurably helps — sometimes a wiki page of golden-path docs plus a shared Terraform module library, not a full portal."]},{"id":"devops/cloud_networking_and_cdn#d704467c1800","section":"devops","module":"devops/cloud_networking_and_cdn","moduleName":"cloud networking and cdn","qIndex":7,"difficulty":"intermediate","question":"Why are overlapping CIDR ranges a problem, and how do you avoid them?","answerFull":"Two VPCs with overlapping CIDRs (e.g., both 10.0.0.0/16) can't be peered or share a Transit Gateway because the router can't unambiguously decide where a given IP lives. You avoid it by planning a non-overlapping address scheme up front — typically with an IPAM tool that allocates distinct ranges per VPC/account/Region — because renumbering a live, in-use VPC is disruptive. Treat IP planning as a foundational design step, not an afterthought.","correct":"Two VPCs with overlapping CIDRs (e.g., both 10.0.0.0/16) can't be peered or share a Transit Gateway because the router can't unambiguously decide where a given IP lives.","distractors":["Prometheus scrapes targets so it can observe target health directly (the synthetic up metric is 0 when a scrape fails), keep scrape configuration centralized, and avoid every target needing to know where to send data.","Monorepo enables atomic cross-service changes in one commit/PR and shared tooling, but needs affected-target CI (Bazel/Nx) to avoid building everything, and access control is coarser.","The cache key is the set of attributes (path, plus chosen headers/query strings/cookies) the CDN uses to decide whether a request matches a cached object."]},{"id":"devops/cloud_networking_and_cdn#991ae7201f0b","section":"devops","module":"devops/cloud_networking_and_cdn","moduleName":"cloud networking and cdn","qIndex":9,"difficulty":"intermediate","question":"How do you handle cache invalidation after a deploy?","answerFull":"The cleanest approach is to avoid invalidation entirely by versioning asset URLs (e.g., app.a1b2c3.js) so a deploy produces new URLs and old cached objects simply age out — you can then cache assets for a year. When you must invalidate (e.g., a fixed path like /index.html), issue a CDN invalidation, but it's slower and, beyond the free tier, costs per path (~$0.005 on CloudFront). Prefer content-hashed filenames for static assets and short TTLs only for genuinely dynamic content.","correct":"The cleanest approach is to avoid invalidation entirely by versioning asset URLs (e.g., app.a1b2c3.js) so a deploy produces new URLs and old cached objects simply age out — you can then cache assets for a year.","distractors":["The PVC↔PV binding is independent of any Pod, so when a (StatefulSet) Pod is rescheduled, its PVC and the underlying volume detach from the old node and reattach to the new one — the data follows the Pod.","With strategy: RollingUpdate and maxUnavailable: 0, maxSurge: 1, the controller creates one new Pod (surge), waits for its readiness probe to pass, then terminates one old Pod — repeating until all are new.","Deploy = the new code is running in production; release = users actually experience it."]},{"id":"devops/configuration_management#d61d3e033915","section":"devops","module":"devops/configuration_management","moduleName":"configuration management","qIndex":1,"difficulty":"core","question":"What is idempotency in configuration management and why does it matter?","answerFull":"Idempotency means applying the same configuration repeatedly produces the same end state, with runs after the first reporting zero changes. It matters because CM tools re-run constantly (on a schedule or every deploy), and a non-idempotent task — like echo line >> file — would corrupt state by appending every run. You achieve it by declaring desired state (\"ensure the package is present,\" \"ensure the line exists once\") instead of issuing imperative commands, which is exactly what modules like apt, template, and lineinfile provide.","correct":"Idempotency means applying the same configuration repeatedly produces the same end state, with runs after the first reporting zero changes.","distractors":["Declarative means you specify the desired end state (spec), not the steps to get there; controllers compute and apply the diff.","It's encoding operational/domain expertise as a custom controller that watches custom resources and reconciles them to desired state — applying Kubernetes' \"declare + reconcile\" model to complex software.","Each Dockerfile instruction produces a layer cached by the instruction plus the state of its inputs (e.g., the files a COPY references)."]},{"id":"devops/configuration_management#0247337f31b0","section":"devops","module":"devops/configuration_management","moduleName":"configuration management","qIndex":2,"difficulty":"core","question":"Push vs pull configuration management — explain the difference and a use case for each.","answerFull":"In push (Ansible), a control node connects out over SSH and runs tasks on demand, with no agent on the targets — ideal for orchestration, bootstrap, and CI-driven runs where you want to trigger changes explicitly. In pull (Puppet/Chef), an agent on each node polls a master on an interval (e.g., every 30 minutes) and self-converges, which gives continuous drift correction across large always-on fleets. Push is simpler to start (agentless) and great for ad-hoc fleet tasks; pull scales to thousands of long-lived nodes that must stay continuously compliant.","correct":"In push (Ansible), a control node connects out over SSH and runs tasks on demand, with no agent on the targets — ideal for orchestration, bootstrap, and CI-driven runs where you want to trigger changes explicitly.","distractors":["Idempotency means applying the same configuration repeatedly produces the same end state, with runs after the first reporting zero changes.","git revert the offending commit and push — the agent reconciles the cluster back to the previous good state, and the revert is itself an audited commit.","A dashboard is a *pull* tool — a human looks at it to investigate or maintain situational awareness — while an alert is a *push* tool that interrupts a human because action is needed."]},{"id":"devops/configuration_management#0aa2f08fb7ce","section":"devops","module":"devops/configuration_management","moduleName":"configuration management","qIndex":4,"difficulty":"core","question":"How does Packer fit into the IaC and CM ecosystem?","answerFull":"Packer builds golden machine images (AMIs, VM images, container images) by booting a base, running provisioners (shell, Ansible, Chef), and snapshotting the result into a versioned, immutable artifact. It sits between CM and provisioning: CM tooling provisions the image, and Terraform then references that AMI to launch instances or auto-scaling groups. This pipeline — Packer bakes, Terraform deploys, ASG replaces — is the canonical immutable-infrastructure workflow.","correct":"Packer builds golden machine images (AMIs, VM images, container images) by booting a base, running provisioners (shell, Ansible, Chef), and snapshotting the result into a versioned, immutable artifact.","distractors":["An immutable, versioned artifact — typically a container image — built once and identified by content (the image digest) or commit SHA, not a mutable tag like latest.","Distroless images contain only your app and its runtime dependencies — no shell, no package manager, no busybox.","When you run everything in containers, the container image *is* the configuration — you build it with a Dockerfile and deploy immutable images, so in-host CM is redundant (see containers_and_docker)."]},{"id":"devops/configuration_management#726c6d99bc93","section":"devops","module":"devops/configuration_management","moduleName":"configuration management","qIndex":8,"difficulty":"intermediate","question":"How do you safely roll out a configuration change across a large fleet?","answerFull":"Test it first: lint (ansible-lint), dry-run (--check --diff or --noop), and verify a role in isolation (Molecule/Test Kitchen). Then roll it out gradually — a canary host or small percentage first, with health checks gating the rest (Ansible serial: 10%) — rather than a big-bang run that can take the whole tier down at once. Watch metrics between batches and have a rollback path (re-converge prior config, or relaunch the prior image under immutable infra).","correct":"Test it first: lint (ansible-lint), dry-run (--check --diff or --noop), and verify a role in isolation (Molecule/Test Kitchen).","distractors":["In push (Ansible), a control node connects out over SSH and runs tasks on demand, with no agent on the targets — ideal for orchestration, bootstrap, and CI-driven runs where you want to trigger changes explicitly.","Use a Deployment for stateless workloads: Pods are interchangeable (random name suffixes), share/lack storage, and roll out/back freely.","When you run everything in containers, the container image *is* the configuration — you build it with a Dockerfile and deploy immutable images, so in-host CM is redundant (see containers_and_docker)."]},{"id":"devops/configuration_management#9e3b7b97b179","section":"devops","module":"devops/configuration_management","moduleName":"configuration management","qIndex":9,"difficulty":"intermediate","question":"Why is \"mutable server drift into snowflakes\" a problem, and how do you prevent it?","answerFull":"Long-lived hosts patched by ad-hoc SSH commands accumulate undocumented differences, so no two servers are identical, none can be reproduced, and incidents are debuggable only by whoever made the last change. You prevent it by enforcing that all changes flow through reviewed, idempotent CM in Git (so the host is reproducible from code) and, more strongly, by adopting immutable infrastructure where SSH-and-edit is forbidden and changes ship as new images. The goal is \"cattle, not pets.\"","correct":"Long-lived hosts patched by ad-hoc SSH commands accumulate undocumented differences, so no two servers are identical, none can be reproduced, and incidents are debuggable only by whoever made the last change.","distractors":["The API server is the front door and sole hub — it authenticates, authorizes, admits, validates, and persists every change, and is the only component that talks to etcd.","It's the assumption that a container *will* be compromised, so you architect to minimize what that achieves rather than only trying to prevent it.","An IAM user has long-lived credentials (password/access keys) tied to a human or legacy app; an IAM role has no permanent credentials and is *assumed* via STS to get temporary tokens (default 1-hour)."]},{"id":"devops/configuration_management#550252d4e1e6","section":"devops","module":"devops/configuration_management","moduleName":"configuration management","qIndex":10,"difficulty":"intermediate","question":"When would you skip configuration management entirely?","answerFull":"When you run everything in containers, the container image *is* the configuration — you build it with a Dockerfile and deploy immutable images, so in-host CM is redundant (see containers_and_docker). You also skip CM for provisioning the infrastructure itself (that's Terraform's job) and for fully managed/serverless platforms where there's no host to configure. CM remains valuable for image-baking provisioners, on-prem fleets, bootstrap, and compliance enforcement on long-lived VMs.","correct":"When you run everything in containers, the container image *is* the configuration — you build it with a Dockerfile and deploy immutable images, so in-host CM is redundant (see containers_and_docker).","distractors":["Use nsenter --target <host-pid> --mount --net --pid to enter the container's namespaces with host tools, or kubectl debug ephemeral containers.","The cleanest approach is to avoid invalidation entirely by versioning asset URLs (e.g., app.a1b2c3.js) so a deploy produces new URLs and old cached objects simply age out — you can then cache assets for a year.","An immutable, versioned artifact — typically a container image — built once and identified by content (the image digest) or commit SHA, not a mutable tag like latest."]},{"id":"devops/container_runtimes_and_oci#eb1a98e391bc","section":"devops","module":"devops/container_runtimes_and_oci","moduleName":"container runtimes and oci","qIndex":1,"difficulty":"core","question":"Walk through what happens when the kubelet starts a pod.","answerFull":"The kubelet calls the CRI runtime (containerd/CRI-O) over gRPC to create a pod sandbox, pull/unpack the image into an OCI bundle (rootfs + config.json) via a snapshotter, and start containers. containerd spawns a per-container shim and invokes the OCI runtime (runc), which makes the clone() syscall with namespace flags and writes cgroup limits, then execves the entrypoint. The shim keeps the container alive independent of the containerd daemon.","correct":"The kubelet calls the CRI runtime (containerd/CRI-O) over gRPC to create a pod sandbox, pull/unpack the image into an OCI bundle (rootfs + config.json) via a snapshotter, and start containers.","distractors":["Define a RuntimeClass mapping to a containerd handler (e.g., runsc for gVisor), then set runtimeClassName on the Pod.","The Container Runtime Interface is a gRPC API the kubelet uses to manage pods/containers/images, decoupling Kubernetes from any specific runtime.","The shim is a small process that parents the container so it survives restarts/upgrades of the containerd daemon — you can upgrade the runtime without killing running pods."]},{"id":"devops/container_runtimes_and_oci#0e35430ad649","section":"devops","module":"devops/container_runtimes_and_oci","moduleName":"container runtimes and oci","qIndex":2,"difficulty":"core","question":"What is the CRI and why does it exist?","answerFull":"The Container Runtime Interface is a gRPC API the kubelet uses to manage pods/containers/images, decoupling Kubernetes from any specific runtime. Before CRI, runtime support was hardcoded; with CRI, any compliant runtime (containerd, CRI-O) plugs in interchangeably. It's why \"removing dockershim\" was possible without breaking workloads.","correct":"The Container Runtime Interface is a gRPC API the kubelet uses to manage pods/containers/images, decoupling Kubernetes from any specific runtime.","distractors":["The kubelet calls the CRI runtime (containerd/CRI-O) over gRPC to create a pod sandbox, pull/unpack the image into an OCI bundle (rootfs + config.json) via a snapshotter, and start containers.","All runc containers share the host kernel, so the kernel is the trust boundary: a privilege-escalation or container-escape kernel bug can compromise the host and every container on it.","The shim is a small process that parents the container so it survives restarts/upgrades of the containerd daemon — you can upgrade the runtime without killing running pods."]},{"id":"devops/container_runtimes_and_oci#8345d9136b82","section":"devops","module":"devops/container_runtimes_and_oci","moduleName":"container runtimes and oci","qIndex":3,"difficulty":"core","question":"Did \"Docker removal from Kubernetes\" break container images?","answerFull":"No. Images are OCI-standard, and containerd (which Docker itself uses) already ran them. What was removed (1.24) was *dockershim* — the kubelet's special adapter to the Docker daemon. Clusters switched the kubelet to talk CRI directly to containerd/CRI-O; the same images run unchanged. Only docker-CLI-on-the-node workflows had to move to crictl.","correct":"No. Images are OCI-standard, and containerd (which Docker itself uses) already ran them.","distractors":["A snapshotter manages how image layers are assembled into a container rootfs — overlayfs is the default.","The kubelet calls the CRI runtime (containerd/CRI-O) over gRPC to create a pod sandbox, pull/unpack the image into an OCI bundle (rootfs + config.json) via a snapshotter, and start containers.","The shim is a small process that parents the container so it survives restarts/upgrades of the containerd daemon — you can upgrade the runtime without killing running pods."]},{"id":"devops/container_runtimes_and_oci#47bfac6fedf2","section":"devops","module":"devops/container_runtimes_and_oci","moduleName":"container runtimes and oci","qIndex":5,"difficulty":"core","question":"runc vs gVisor vs Kata — how do they differ in isolation?","answerFull":"runc uses host-kernel namespaces/cgroups — fast but shares the kernel (a kernel exploit can escape). gVisor (runsc) interposes a user-space kernel that intercepts syscalls, isolating the host kernel at the cost of syscall overhead and partial compatibility. Kata runs each container in a lightweight VM (Firecracker/QEMU), giving hardware-virtualization isolation at the cost of VM boot time and per-container memory.","correct":"runc uses host-kernel namespaces/cgroups — fast but shares the kernel (a kernel exploit can escape).","distractors":["Define a RuntimeClass mapping to a containerd handler (e.g., runsc for gVisor), then set runtimeClassName on the Pod.","crun is a C implementation of the OCI runtime (vs runc's Go), with lower memory per container and faster startup.","Firecracker is a minimal VMM that boots a stripped microVM in ~125 ms with a tiny memory footprint, giving hardware-level isolation without a full VM's overhead."]},{"id":"devops/container_runtimes_and_oci#095037d1ebb5","section":"devops","module":"devops/container_runtimes_and_oci","moduleName":"container runtimes and oci","qIndex":6,"difficulty":"intermediate","question":"Why does a container runtime use a shim process?","answerFull":"The shim is a small process that parents the container so it survives restarts/upgrades of the containerd daemon — you can upgrade the runtime without killing running pods. It also reports the container's exit status back to containerd and handles I/O streams. Without it, the container's lifecycle would be tied to the daemon's.","correct":"The shim is a small process that parents the container so it survives restarts/upgrades of the containerd daemon — you can upgrade the runtime without killing running pods.","distractors":["Define a RuntimeClass mapping to a containerd handler (e.g., runsc for gVisor), then set runtimeClassName on the Pod.","Use crictl (CRI-level: crictl ps, crictl logs, crictl inspect, crictl exec) or ctr -n k8s.io (containerd-native).","crun is a C implementation of the OCI runtime (vs runc's Go), with lower memory per container and faster startup."]},{"id":"devops/container_runtimes_and_oci#e2db0c25a8ab","section":"devops","module":"devops/container_runtimes_and_oci","moduleName":"container runtimes and oci","qIndex":7,"difficulty":"intermediate","question":"How do you run a specific workload under a different runtime?","answerFull":"Define a RuntimeClass mapping to a containerd handler (e.g., runsc for gVisor), then set runtimeClassName on the Pod. This lets a single cluster run trusted workloads on runc and untrusted/multi-tenant ones on gVisor/Kata, choosing isolation per workload rather than per cluster.","correct":"Define a RuntimeClass mapping to a containerd handler (e.g., runsc for gVisor), then set runtimeClassName on the Pod.","distractors":["Use crictl (CRI-level: crictl ps, crictl logs, crictl inspect, crictl exec) or ctr -n k8s.io (containerd-native).","The kubelet calls the CRI runtime (containerd/CRI-O) over gRPC to create a pod sandbox, pull/unpack the image into an OCI bundle (rootfs + config.json) via a snapshotter, and start containers.","The shim is a small process that parents the container so it survives restarts/upgrades of the containerd daemon — you can upgrade the runtime without killing running pods."]},{"id":"devops/container_runtimes_and_oci#7f747c1ff66d","section":"devops","module":"devops/container_runtimes_and_oci","moduleName":"container runtimes and oci","qIndex":8,"difficulty":"intermediate","question":"You're on a node with no docker command — how do you inspect containers?","answerFull":"Use crictl (CRI-level: crictl ps, crictl logs, crictl inspect, crictl exec) or ctr -n k8s.io (containerd-native). These talk to the containerd socket directly. On distroless container images there's no shell, so you'd use kubectl debug ephemeral containers or host-side nsenter into the process namespaces.","correct":"Use crictl (CRI-level: crictl ps, crictl logs, crictl inspect, crictl exec) or ctr -n k8s.io (containerd-native).","distractors":["Firecracker is a minimal VMM that boots a stripped microVM in ~125 ms with a tiny memory footprint, giving hardware-level isolation without a full VM's overhead.","Define a RuntimeClass mapping to a containerd handler (e.g., runsc for gVisor), then set runtimeClassName on the Pod.","All runc containers share the host kernel, so the kernel is the trust boundary: a privilege-escalation or container-escape kernel bug can compromise the host and every container on it."]},{"id":"devops/container_runtimes_and_oci#75715887a515","section":"devops","module":"devops/container_runtimes_and_oci","moduleName":"container runtimes and oci","qIndex":9,"difficulty":"intermediate","question":"What is a snapshotter and why does it matter?","answerFull":"A snapshotter manages how image layers are assembled into a container rootfs — overlayfs is the default. Pluggable snapshotters enable features like lazy/\"stargz\" pulling (start the container before the whole image downloads), which slashes cold-start time for large images. The choice affects pull time, disk usage, and start latency.","correct":"A snapshotter manages how image layers are assembled into a container rootfs — overlayfs is the default.","distractors":["All runc containers share the host kernel, so the kernel is the trust boundary: a privilege-escalation or container-escape kernel bug can compromise the host and every container on it.","The Container Runtime Interface is a gRPC API the kubelet uses to manage pods/containers/images, decoupling Kubernetes from any specific runtime.","crun is a C implementation of the OCI runtime (vs runc's Go), with lower memory per container and faster startup."]},{"id":"devops/container_runtimes_and_oci#0563d6596a25","section":"devops","module":"devops/container_runtimes_and_oci","moduleName":"container runtimes and oci","qIndex":10,"difficulty":"intermediate","question":"crun vs runc — when would you switch?","answerFull":"crun is a C implementation of the OCI runtime (vs runc's Go), with lower memory per container and faster startup. On high pod-density nodes or latency-sensitive scale-up, crun's lower overhead measurably improves container start time and node capacity. Functionally it's a drop-in OCI runtime; you select it via the runtime handler.","correct":"crun is a C implementation of the OCI runtime (vs runc's Go), with lower memory per container and faster startup.","distractors":["A snapshotter manages how image layers are assembled into a container rootfs — overlayfs is the default.","The shim is a small process that parents the container so it survives restarts/upgrades of the containerd daemon — you can upgrade the runtime without killing running pods.","The Container Runtime Interface is a gRPC API the kubelet uses to manage pods/containers/images, decoupling Kubernetes from any specific runtime."]},{"id":"devops/container_runtimes_and_oci#574a2b00fb5d","section":"devops","module":"devops/container_runtimes_and_oci","moduleName":"container runtimes and oci","qIndex":11,"difficulty":"advanced","question":"What are the security tradeoffs of shared-kernel containers?","answerFull":"All runc containers share the host kernel, so the kernel is the trust boundary: a privilege-escalation or container-escape kernel bug can compromise the host and every container on it. Mitigations include dropping capabilities, seccomp/AppArmor profiles, non-root users, and — for untrusted code — moving to gVisor/Kata which don't share the host kernel directly.","correct":"All runc containers share the host kernel, so the kernel is the trust boundary: a privilege-escalation or container-escape kernel bug can compromise the host and every container on it.","distractors":["The Container Runtime Interface is a gRPC API the kubelet uses to manage pods/containers/images, decoupling Kubernetes from any specific runtime.","A snapshotter manages how image layers are assembled into a container rootfs — overlayfs is the default.","runc uses host-kernel namespaces/cgroups — fast but shares the kernel (a kernel exploit can escape)."]},{"id":"devops/container_runtimes_and_oci#565fc3d7938c","section":"devops","module":"devops/container_runtimes_and_oci","moduleName":"container runtimes and oci","qIndex":12,"difficulty":"advanced","question":"How does Firecracker enable both isolation and density?","answerFull":"Firecracker is a minimal VMM that boots a stripped microVM in ~125 ms with a tiny memory footprint, giving hardware-level isolation without a full VM's overhead. It underpins AWS Lambda and Fargate, where each function/task gets VM-grade isolation yet the platform still packs thousands per host — bridging the container-density vs VM-isolation gap.","correct":"Firecracker is a minimal VMM that boots a stripped microVM in ~125 ms with a tiny memory footprint, giving hardware-level isolation without a full VM's overhead.","distractors":["The shim is a small process that parents the container so it survives restarts/upgrades of the containerd daemon — you can upgrade the runtime without killing running pods.","Use crictl (CRI-level: crictl ps, crictl logs, crictl inspect, crictl exec) or ctr -n k8s.io (containerd-native).","crun is a C implementation of the OCI runtime (vs runc's Go), with lower memory per container and faster startup."]},{"id":"devops/containers_and_docker#dad3a139b936","section":"devops","module":"devops/containers_and_docker","moduleName":"containers and docker","qIndex":1,"difficulty":"core","question":"What is a container image, structurally?","answerFull":"It's an ordered stack of read-only filesystem layers (each a content-addressed tarball of changes) plus a JSON config holding the entrypoint, env, and metadata. At runtime the engine unions the layers (overlayfs) and adds a thin writable layer for the running container. Layers are shared and cached across images, so pulling a new image only downloads layers you don't already have.","correct":"It's an ordered stack of read-only filesystem layers (each a content-addressed tarball of changes) plus a JSON config holding the entrypoint, env, and metadata.","distractors":["A tag (node:20) is a mutable pointer — the same tag can be re-pushed to point at different content, so builds aren't reproducible and a supply-chain attacker could swap it.","An image is the immutable, layered template (build artifact); a container is a running (or stopped) instance of an image — the read-only layers plus a writable scratch layer and a process with its own namespaces/cgroups.","Root in the container is root in its namespaces; combined with a kernel container-escape vulnerability or a host mount, it escalates the blast radius of any compromise."]},{"id":"devops/containers_and_docker#dcca4db1ca22","section":"devops","module":"devops/containers_and_docker","moduleName":"containers and docker","qIndex":2,"difficulty":"core","question":"How does the build cache work and how do you exploit it?","answerFull":"Each Dockerfile instruction produces a layer cached by the instruction plus the state of its inputs (e.g., the files a COPY references). If an early layer's inputs change, all subsequent layers are rebuilt. You exploit this by ordering instructions from least- to most-frequently-changing: base image and dependency installs early, source code copy and build late — so a code edit rebuilds only the cheap final layers.","correct":"Each Dockerfile instruction produces a layer cached by the instruction plus the state of its inputs (e.g., the files a COPY references).","distractors":["An image is the immutable, layered template (build artifact); a container is a running (or stopped) instance of an image — the read-only layers plus a writable scratch layer and a process with its own namespaces/cgroups.","The build \"context\" (everything sent to the daemon for COPY) defaults to the whole directory.","It separates the build environment from the runtime environment."]},{"id":"devops/containers_and_docker#ed5e33d5bf4c","section":"devops","module":"devops/containers_and_docker","moduleName":"containers and docker","qIndex":3,"difficulty":"core","question":"What does a multi-stage build buy you?","answerFull":"It separates the build environment from the runtime environment. You compile in a fat \"builder\" stage with the full toolchain, then COPY --from=builder only the final artifact into a minimal runtime image. The toolchain, source code, and intermediate files never ship — yielding a much smaller, more secure final image (e.g., a 900 MB builder producing an 80 MB runtime).","correct":"It separates the build environment from the runtime environment.","distractors":["A multi-arch image is a manifest list mapping each platform (linux/amd64, linux/arm64) to a platform-specific image; the runtime pulls the one matching its CPU.","The build \"context\" (everything sent to the daemon for COPY) defaults to the whole directory.","Distroless images contain only your app and its runtime dependencies — no shell, no package manager, no busybox."]},{"id":"devops/containers_and_docker#8a0114933e8e","section":"devops","module":"devops/containers_and_docker","moduleName":"containers and docker","qIndex":4,"difficulty":"core","question":"Why is running as root in a container dangerous, and how do you avoid it?","answerFull":"Root in the container is root in its namespaces; combined with a kernel container-escape vulnerability or a host mount, it escalates the blast radius of any compromise. Avoid it by creating a non-root user (USER 10001) in the image and enforcing runAsNonRoot: true plus readOnlyRootFilesystem: true in the Kubernetes securityContext.","correct":"Root in the container is root in its namespaces; combined with a kernel container-escape vulnerability or a host mount, it escalates the blast radius of any compromise.","distractors":["An image is the immutable, layered template (build artifact); a container is a running (or stopped) instance of an image — the read-only layers plus a writable scratch layer and a process with its own namespaces/cgroups.","A multi-arch image is a manifest list mapping each platform (linux/amd64, linux/arm64) to a platform-specific image; the runtime pulls the one matching its CPU.","A tag (node:20) is a mutable pointer — the same tag can be re-pushed to point at different content, so builds aren't reproducible and a supply-chain attacker could swap it."]},{"id":"devops/containers_and_docker#dc5b6b31f206","section":"devops","module":"devops/containers_and_docker","moduleName":"containers and docker","qIndex":5,"difficulty":"core","question":"Tag vs digest — why pin by digest?","answerFull":"A tag (node:20) is a mutable pointer — the same tag can be re-pushed to point at different content, so builds aren't reproducible and a supply-chain attacker could swap it. A digest (node:20@sha256:...) is immutable and content-verified, guaranteeing you get exactly the bits you tested. Pin by digest for reproducible builds and supply-chain integrity.","correct":"A tag (node:20) is a mutable pointer — the same tag can be re-pushed to point at different content, so builds aren't reproducible and a supply-chain attacker could swap it.","distractors":["It's an ordered stack of read-only filesystem layers (each a content-addressed tarball of changes) plus a JSON config holding the entrypoint, env, and metadata.","A multi-arch image is a manifest list mapping each platform (linux/amd64, linux/arm64) to a platform-specific image; the runtime pulls the one matching its CPU.","Alpine is tiny (~5 MB) but uses musl libc instead of glibc."]},{"id":"devops/containers_and_docker#c469291e978e","section":"devops","module":"devops/containers_and_docker","moduleName":"containers and docker","qIndex":6,"difficulty":"intermediate","question":"Alpine vs Debian-slim — what's the catch with Alpine?","answerFull":"Alpine is tiny (~5 MB) but uses musl libc instead of glibc. This causes real problems: different DNS resolution behavior, Python packages that ship glibc wheels needing recompilation (slower builds), and occasional subtle runtime bugs. Debian-slim is larger but glibc-compatible and avoids these surprises — many teams choose slim for reliability over Alpine's size.","correct":"Alpine is tiny (~5 MB) but uses musl libc instead of glibc.","distractors":["A multi-arch image is a manifest list mapping each platform (linux/amd64, linux/arm64) to a platform-specific image; the runtime pulls the one matching its CPU.","A tag (node:20) is a mutable pointer — the same tag can be re-pushed to point at different content, so builds aren't reproducible and a supply-chain attacker could swap it.","An image is the immutable, layered template (build artifact); a container is a running (or stopped) instance of an image — the read-only layers plus a writable scratch layer and a process with its own namespaces/cgroups."]},{"id":"devops/containers_and_docker#07080d6b0e23","section":"devops","module":"devops/containers_and_docker","moduleName":"containers and docker","qIndex":7,"difficulty":"intermediate","question":"How do you keep build secrets out of the final image?","answerFull":"Never use ARG or ENV for secrets — they persist in image history/layers. Use BuildKit's --mount=type=secret, which exposes the secret only during a specific RUN and never writes it to a layer. For the image itself, inject runtime secrets via the orchestrator (env from a Secret, mounted file), not baked in.","correct":"Never use ARG or ENV for secrets — they persist in image history/layers.","distractors":["It separates the build environment from the runtime environment.","An image is the immutable, layered template (build artifact); a container is a running (or stopped) instance of an image — the read-only layers plus a writable scratch layer and a process with its own namespaces/cgroups.","The build \"context\" (everything sent to the daemon for COPY) defaults to the whole directory."]},{"id":"devops/containers_and_docker#90bb56395506","section":"devops","module":"devops/containers_and_docker","moduleName":"containers and docker","qIndex":8,"difficulty":"intermediate","question":"Why is .dockerignore important?","answerFull":"The build \"context\" (everything sent to the daemon for COPY) defaults to the whole directory. Without .dockerignore, COPY . . ships node_modules, .git, .env, and logs into the image — bloating size, slowing builds, and leaking secrets/history. .dockerignore excludes them, keeping the context and image lean and safe.","correct":"The build \"context\" (everything sent to the daemon for COPY) defaults to the whole directory.","distractors":["Each Dockerfile instruction produces a layer cached by the instruction plus the state of its inputs (e.g., the files a COPY references).","It separates the build environment from the runtime environment.","An image is the immutable, layered template (build artifact); a container is a running (or stopped) instance of an image — the read-only layers plus a writable scratch layer and a process with its own namespaces/cgroups."]},{"id":"devops/containers_and_docker#db1adc94eb29","section":"devops","module":"devops/containers_and_docker","moduleName":"containers and docker","qIndex":9,"difficulty":"intermediate","question":"What is distroless and what's the tradeoff?","answerFull":"Distroless images contain only your app and its runtime dependencies — no shell, no package manager, no busybox. This minimizes attack surface and CVE count dramatically. The tradeoff is debuggability: you can't kubectl exec … sh into them, so you debug via ephemeral debug containers, host-side nsenter, or by reproducing locally.","correct":"Distroless images contain only your app and its runtime dependencies — no shell, no package manager, no busybox.","distractors":["A multi-arch image is a manifest list mapping each platform (linux/amd64, linux/arm64) to a platform-specific image; the runtime pulls the one matching its CPU.","It separates the build environment from the runtime environment.","A tag (node:20) is a mutable pointer — the same tag can be re-pushed to point at different content, so builds aren't reproducible and a supply-chain attacker could swap it."]},{"id":"devops/containers_and_docker#d41935c73174","section":"devops","module":"devops/containers_and_docker","moduleName":"containers and docker","qIndex":10,"difficulty":"intermediate","question":"How do multi-arch images work and why care?","answerFull":"A multi-arch image is a manifest list mapping each platform (linux/amd64, linux/arm64) to a platform-specific image; the runtime pulls the one matching its CPU. docker buildx build --platform ... builds them in one command. It matters because devs run Apple Silicon (arm64), CI may be x86, and prod may use ARM nodes (AWS Graviton) — one tag serves all.","correct":"A multi-arch image is a manifest list mapping each platform (linux/amd64, linux/arm64) to a platform-specific image; the runtime pulls the one matching its CPU.","distractors":["An image is the immutable, layered template (build artifact); a container is a running (or stopped) instance of an image — the read-only layers plus a writable scratch layer and a process with its own namespaces/cgroups.","Distroless images contain only your app and its runtime dependencies — no shell, no package manager, no busybox.","Never use ARG or ENV for secrets — they persist in image history/layers."]},{"id":"devops/containers_and_docker#2e42abf90252","section":"devops","module":"devops/containers_and_docker","moduleName":"containers and docker","qIndex":11,"difficulty":"advanced","question":"What's the difference between an image and a container?","answerFull":"An image is the immutable, layered template (build artifact); a container is a running (or stopped) instance of an image — the read-only layers plus a writable scratch layer and a process with its own namespaces/cgroups. Many containers can run from one image; deleting a container doesn't affect the image.","correct":"An image is the immutable, layered template (build artifact); a container is a running (or stopped) instance of an image — the read-only layers plus a writable scratch layer and a process with its own namespaces/cgroups.","distractors":["The build \"context\" (everything sent to the daemon for COPY) defaults to the whole directory.","A multi-arch image is a manifest list mapping each platform (linux/amd64, linux/arm64) to a platform-specific image; the runtime pulls the one matching its CPU.","Each Dockerfile instruction produces a layer cached by the instruction plus the state of its inputs (e.g., the files a COPY references)."]},{"id":"devops/deployment_strategies#cdb0cefcda79","section":"devops","module":"devops/deployment_strategies","moduleName":"deployment strategies","qIndex":1,"difficulty":"core","question":"Compare rolling, blue-green, and canary.","answerFull":"Rolling replaces instances incrementally (zero downtime with readiness gates, cheap, but blast radius grows as the roll proceeds and rollback means rolling back). Blue-green runs two full environments and switches all traffic at once — instant rollback by switching back, at ~2x resource cost during cutover and 100% blast radius at the switch. Canary routes a small % of traffic to the new version first, limiting blast radius and enabling metric-gated promotion, at the cost of needing traffic-splitting and metric analysis.","correct":"Rolling replaces instances incrementally (zero downtime with readiness gates, cheap, but blast radius grows as the roll proceeds and rollback means rolling back).","distractors":["A controller shifts a small traffic weight to the canary and, during pauses, queries metric providers (e.g., Prometheus) for success conditions — error rate ≥ 99%, p95 latency < threshold, or business KPIs.","A complete strategy has a *fast, tested* path to the previous good state: blue-green switch-back, canary shift-to-zero, or feature-flag toggle — ideally automated and exercised regularly.","Favor canary/progressive delivery to minimize blast radius (a payment bug must not hit 100% of users), gated on strict metrics (error rate, latency, and payment-success KPIs) with automated rollback."]},{"id":"devops/deployment_strategies#43dd8e35a1bd","section":"devops","module":"devops/deployment_strategies","moduleName":"deployment strategies","qIndex":3,"difficulty":"core","question":"How do deploy and release differ, and why does it matter?","answerFull":"Deploy = the new code is running in production; release = users actually experience it. Feature flags (and canary weighting) separate them: you can deploy code dark and toggle exposure at runtime per cohort. This lets you ship continuously, decouple risky features from deploy cadence, dark-launch for testing, and turn a misbehaving feature off in sub-seconds without a rollback deploy.","correct":"Deploy = the new code is running in production; release = users actually experience it.","distractors":["Blue-green keeps the entire previous version (blue) running and warm; rollback is just pointing the router back to it — effectively instant.","Favor canary/progressive delivery to minimize blast radius (a payment bug must not hit 100% of users), gated on strict metrics (error rate, latency, and payment-success KPIs) with automated rollback.","A complete strategy has a *fast, tested* path to the previous good state: blue-green switch-back, canary shift-to-zero, or feature-flag toggle — ideally automated and exercised regularly."]},{"id":"devops/deployment_strategies#a5f693cbbc57","section":"devops","module":"devops/deployment_strategies","moduleName":"deployment strategies","qIndex":4,"difficulty":"core","question":"Why is blue-green's rollback faster than rolling's?","answerFull":"Blue-green keeps the entire previous version (blue) running and warm; rollback is just pointing the router back to it — effectively instant. Rolling has already replaced old instances incrementally, so rolling back means deploying the old version again (re-pulling images, restarting pods, re-warming), which takes minutes. Blue-green trades 2x resource cost during cutover for that instant rollback.","correct":"Blue-green keeps the entire previous version (blue) running and warm; rollback is just pointing the router back to it — effectively instant.","distractors":["Favor canary/progressive delivery to minimize blast radius (a payment bug must not hit 100% of users), gated on strict metrics (error rate, latency, and payment-success KPIs) with automated rollback.","Rolling replaces instances incrementally (zero downtime with readiness gates, cheap, but blast radius grows as the roll proceeds and rollback means rolling back).","Under GitOps, the desired state (including the Rollout/canary spec) lives in Git, and a controller (ArgoCD + Argo Rollouts) reconciles it; a promotion or rollback can be a Git change or an automated metric-gated step."]},{"id":"devops/deployment_strategies#df93307338bf","section":"devops","module":"devops/deployment_strategies","moduleName":"deployment strategies","qIndex":5,"difficulty":"core","question":"What's the danger of running two versions simultaneously, and how do you handle schema changes?","answerFull":"During any rolling/canary/blue-green transition, v1 and v2 run at once and may share a database, so a backward-incompatible schema change breaks the version that doesn't expect it (500s, data corruption). Use expand-contract: first expand (additive, nullable changes both versions tolerate), deploy code using the new shape, then contract (remove old columns) only after all old pods are gone — never in the same release.","correct":"During any rolling/canary/blue-green transition, v1 and v2 run at once and may share a database, so a backward-incompatible schema change breaks the version that doesn't expect it (500s, data corruption).","distractors":["Blue-green keeps the entire previous version (blue) running and warm; rollback is just pointing the router back to it — effectively instant.","A complete strategy has a *fast, tested* path to the previous good state: blue-green switch-back, canary shift-to-zero, or feature-flag toggle — ideally automated and exercised regularly.","Under GitOps, the desired state (including the Rollout/canary spec) lives in Git, and a controller (ArgoCD + Argo Rollouts) reconciles it; a promotion or rollback can be a Git change or an automated metric-gated step."]},{"id":"devops/deployment_strategies#be257611ca25","section":"devops","module":"devops/deployment_strategies","moduleName":"deployment strategies","qIndex":6,"difficulty":"intermediate","question":"How does an automated canary decide to promote or roll back?","answerFull":"A controller shifts a small traffic weight to the canary and, during pauses, queries metric providers (e.g., Prometheus) for success conditions — error rate ≥ 99%, p95 latency < threshold, or business KPIs. If the conditions hold across the analysis, it advances the weight; if they fail (beyond a failure limit), it shifts traffic back to stable and halts, alerting. The gates are codified (AnalysisTemplate), not a human eyeballing dashboards.","correct":"A controller shifts a small traffic weight to the canary and, during pauses, queries metric providers (e.g., Prometheus) for success conditions — error rate ≥ 99%, p95 latency < threshold, or business KPIs.","distractors":["A complete strategy has a *fast, tested* path to the previous good state: blue-green switch-back, canary shift-to-zero, or feature-flag toggle — ideally automated and exercised regularly.","Blue-green keeps the entire previous version (blue) running and warm; rollback is just pointing the router back to it — effectively instant.","They provide weighted traffic routing: the mesh/ingress can send, say, 5% of requests to the canary Service and 95% to stable, and adjust the weights as the rollout progresses."]},{"id":"devops/deployment_strategies#552e08b8b596","section":"devops","module":"devops/deployment_strategies","moduleName":"deployment strategies","qIndex":9,"difficulty":"intermediate","question":"How does a service mesh or ingress enable canaries?","answerFull":"They provide weighted traffic routing: the mesh/ingress can send, say, 5% of requests to the canary Service and 95% to stable, and adjust the weights as the rollout progresses. Argo Rollouts/Flagger drive these weights via the mesh (Istio/Linkerd) or ingress API. Without traffic-splitting capability, \"canary\" degrades to replica-ratio approximations, which are coarser and less precise.","correct":"They provide weighted traffic routing: the mesh/ingress can send, say, 5% of requests to the canary Service and 95% to stable, and adjust the weights as the rollout progresses.","distractors":["A controller shifts a small traffic weight to the canary and, during pauses, queries metric providers (e.g., Prometheus) for success conditions — error rate ≥ 99%, p95 latency < threshold, or business KPIs.","A complete strategy has a *fast, tested* path to the previous good state: blue-green switch-back, canary shift-to-zero, or feature-flag toggle — ideally automated and exercised regularly.","Under GitOps, the desired state (including the Rollout/canary spec) lives in Git, and a controller (ArgoCD + Argo Rollouts) reconciles it; a promotion or rollback can be a Git change or an automated metric-gated step."]},{"id":"devops/deployment_strategies#ca3de24bed58","section":"devops","module":"devops/deployment_strategies","moduleName":"deployment strategies","qIndex":10,"difficulty":"intermediate","question":"What's a complete rollback strategy, and why isn't \"redeploy the old version\" always enough?","answerFull":"A complete strategy has a *fast, tested* path to the previous good state: blue-green switch-back, canary shift-to-zero, or feature-flag toggle — ideally automated and exercised regularly. \"Redeploy the old version\" can be slow (image pulls, restarts), and if the bad release made an incompatible schema change, redeploying old code may not even work. Rollback must account for data/schema and be proven, not assumed.","correct":"A complete strategy has a *fast, tested* path to the previous good state: blue-green switch-back, canary shift-to-zero, or feature-flag toggle — ideally automated and exercised regularly.","distractors":["A controller shifts a small traffic weight to the canary and, during pauses, queries metric providers (e.g., Prometheus) for success conditions — error rate ≥ 99%, p95 latency < threshold, or business KPIs.","Blue-green keeps the entire previous version (blue) running and warm; rollback is just pointing the router back to it — effectively instant.","Under GitOps, the desired state (including the Rollout/canary spec) lives in Git, and a controller (ArgoCD + Argo Rollouts) reconciles it; a promotion or rollback can be a Git change or an automated metric-gated step."]},{"id":"devops/deployment_strategies#fab181f1a0af","section":"devops","module":"devops/deployment_strategies","moduleName":"deployment strategies","qIndex":11,"difficulty":"advanced","question":"How do you choose a strategy for a high-traffic payment service?","answerFull":"Favor canary/progressive delivery to minimize blast radius (a payment bug must not hit 100% of users), gated on strict metrics (error rate, latency, and payment-success KPIs) with automated rollback. Combine with feature flags for risky logic (toggle off instantly) and strict expand-contract schema discipline. Blue-green is an option if instant full rollback is required and 2x cost is acceptable, but gradual exposure is usually safer for money flows.","correct":"Favor canary/progressive delivery to minimize blast radius (a payment bug must not hit 100% of users), gated on strict metrics (error rate, latency, and payment-success KPIs) with automated rollback.","distractors":["Under GitOps, the desired state (including the Rollout/canary spec) lives in Git, and a controller (ArgoCD + Argo Rollouts) reconciles it; a promotion or rollback can be a Git change or an automated metric-gated step.","Deploy = the new code is running in production; release = users actually experience it.","Rolling replaces instances incrementally (zero downtime with readiness gates, cheap, but blast radius grows as the roll proceeds and rollback means rolling back)."]},{"id":"devops/deployment_strategies#ff24a3e06a02","section":"devops","module":"devops/deployment_strategies","moduleName":"deployment strategies","qIndex":12,"difficulty":"advanced","question":"How do deployment strategies relate to GitOps?","answerFull":"Under GitOps, the desired state (including the Rollout/canary spec) lives in Git, and a controller (ArgoCD + Argo Rollouts) reconciles it; a promotion or rollback can be a Git change or an automated metric-gated step. The strategy (canary/blue-green) is declared as a Rollout CR, and rollback is git revert or the controller's auto-abort — combining progressive delivery with Git as the audited source of truth (see gitops_argocd_flux).","correct":"Under GitOps, the desired state (including the Rollout/canary spec) lives in Git, and a controller (ArgoCD + Argo Rollouts) reconciles it; a promotion or rollback can be a Git change or an automated metric-gated step.","distractors":["A complete strategy has a *fast, tested* path to the previous good state: blue-green switch-back, canary shift-to-zero, or feature-flag toggle — ideally automated and exercised regularly.","Favor canary/progressive delivery to minimize blast radius (a payment bug must not hit 100% of users), gated on strict metrics (error rate, latency, and payment-success KPIs) with automated rollback.","A controller shifts a small traffic weight to the canary and, during pauses, queries metric providers (e.g., Prometheus) for success conditions — error rate ≥ 99%, p95 latency < threshold, or business KPIs."]},{"id":"devops/devsecops_and_supply_chain_security#0117f7c264b2","section":"devops","module":"devops/devsecops_and_supply_chain_security","moduleName":"devsecops and supply chain security","qIndex":1,"difficulty":"core","question":"What is the difference between SAST, DAST, and SCA?","answerFull":"SAST analyzes source code statically for vulnerable patterns; DAST probes a running application with crafted requests; SCA inspects your dependency manifests against a CVE database. SAST catches injection-style bugs in your own code early but produces false positives, DAST catches runtime-only issues like misconfigured auth but needs a deployed environment, and SCA catches the vast majority of real-world risk because most code is third-party. Run all three but gate hardest on SCA since vulnerable dependencies are the most common attack vector.","correct":"SAST analyzes source code statically for vulnerable patterns; DAST probes a running application with crafted requests; SCA inspects your dependency manifests against a CVE database.","distractors":["The workflow requests an OIDC token from GitHub's identity provider, and the cloud (e.g., AWS via an IAM OIDC trust) exchanges it for temporary credentials scoped to a specific role, typically valid ~1 hour.","Provenance is signed metadata describing how an artifact was built — the source commit, builder identity, and inputs — bound to the artifact's digest.","First assess exploitability in your context — is the vulnerable code path reachable?"]},{"id":"devops/devsecops_and_supply_chain_security#f936e31e8ccd","section":"devops","module":"devops/devsecops_and_supply_chain_security","moduleName":"devsecops and supply chain security","qIndex":2,"difficulty":"core","question":"Why is keyless signing considered more secure than key-based signing?","answerFull":"Keyless signing eliminates the long-lived private key, which is the single most stolen secret in signing systems. cosign requests a 10-minute certificate from Fulcio bound to an OIDC identity, signs with an ephemeral key, and records the event in Rekor; there is no key sitting in a vault to exfiltrate. The tradeoff is a runtime dependency on Sigstore infrastructure, so for critical paths you mirror Rekor or run a private Sigstore deployment.","correct":"Keyless signing eliminates the long-lived private key, which is the single most stolen secret in signing systems.","distractors":["An SBOM is a machine-readable inventory of every component in an artifact, typically in CycloneDX or SPDX format.","Failing closed means that when the verifier cannot confirm a signature — Rekor is down, the cert is invalid, no signature exists — the pod is denied rather than admitted.","First assess exploitability in your context — is the vulnerable code path reachable?"]},{"id":"devops/devsecops_and_supply_chain_security#f6f913843d2e","section":"devops","module":"devops/devsecops_and_supply_chain_security","moduleName":"devsecops and supply chain security","qIndex":3,"difficulty":"core","question":"What is an SBOM and why does it matter operationally?","answerFull":"An SBOM is a machine-readable inventory of every component in an artifact, typically in CycloneDX or SPDX format. Its operational value appears during an incident like Log4Shell: instead of guessing which of 400 services are affected, you query your SBOM store for log4j-core < 2.17 and get an exact list in seconds. Generate SBOMs with syft at build time and store them queryably; an SBOM you cannot search is compliance theater.","correct":"An SBOM is a machine-readable inventory of every component in an artifact, typically in CycloneDX or SPDX format.","distractors":["Scope every token to least privilege: a job that builds repo A should hold a token that can push only to repo A's registry path with a short TTL, not org-wide write.","The workflow requests an OIDC token from GitHub's identity provider, and the cloud (e.g., AWS via an IAM OIDC trust) exchanges it for temporary credentials scoped to a specific role, typically valid ~1 hour.","Provenance is signed metadata describing how an artifact was built — the source commit, builder identity, and inputs — bound to the artifact's digest."]},{"id":"devops/devsecops_and_supply_chain_security#cfd4ffd2aed0","section":"devops","module":"devops/devsecops_and_supply_chain_security","moduleName":"devsecops and supply chain security","qIndex":5,"difficulty":"core","question":"What does Rekor provide that a signature alone does not?","answerFull":"Rekor is an append-only transparency log that provides tamper-evident, publicly auditable proof that a signature existed at a point in time. A bare signature can be created and discarded silently; a Rekor entry (with its UUID and inclusion proof) means an attacker cannot retroactively sign a malicious artifact without leaving a permanent public record. Verifiers should require Rekor inclusion so that signing events are auditable and non-repudiable.","correct":"Rekor is an append-only transparency log that provides tamper-evident, publicly auditable proof that a signature existed at a point in time.","distractors":["Failing closed means that when the verifier cannot confirm a signature — Rekor is down, the cert is invalid, no signature exists — the pod is denied rather than admitted.","An SBOM is a machine-readable inventory of every component in an artifact, typically in CycloneDX or SPDX format.","First assess exploitability in your context — is the vulnerable code path reachable?"]},{"id":"devops/devsecops_and_supply_chain_security#f40d1826992c","section":"devops","module":"devops/devsecops_and_supply_chain_security","moduleName":"devsecops and supply chain security","qIndex":6,"difficulty":"intermediate","question":"How does GitHub Actions OIDC remove the need for stored cloud credentials?","answerFull":"The workflow requests an OIDC token from GitHub's identity provider, and the cloud (e.g., AWS via an IAM OIDC trust) exchanges it for temporary credentials scoped to a specific role, typically valid ~1 hour. There is no long-lived AWS_SECRET_ACCESS_KEY in repo secrets to leak, and the trust policy can restrict which repo, branch, or environment may assume the role. This same OIDC identity also feeds Fulcio, binding signatures to the exact workflow that produced them.","correct":"The workflow requests an OIDC token from GitHub's identity provider, and the cloud (e.g., AWS via an IAM OIDC trust) exchanges it for temporary credentials scoped to a specific role, typically valid ~1 hour.","distractors":["Scope every token to least privilege: a job that builds repo A should hold a token that can push only to repo A's registry path with a short TTL, not org-wide write.","An SBOM is a machine-readable inventory of every component in an artifact, typically in CycloneDX or SPDX format.","Provenance is signed metadata describing how an artifact was built — the source commit, builder identity, and inputs — bound to the artifact's digest."]},{"id":"devops/devsecops_and_supply_chain_security#9f911aee90b1","section":"devops","module":"devops/devsecops_and_supply_chain_security","moduleName":"devsecops and supply chain security","qIndex":7,"difficulty":"intermediate","question":"Why must admission verification fail closed?","answerFull":"Failing closed means that when the verifier cannot confirm a signature — Rekor is down, the cert is invalid, no signature exists — the pod is denied rather than admitted. Failing open creates false confidence: operators believe verification is protecting them while compromised images sail through during any outage, which is exactly when an attacker would strike. Pair fail-closed with a tested break-glass annotation and monitoring on Sigstore availability so you are not frozen during a legitimate outage.","correct":"Failing closed means that when the verifier cannot confirm a signature — Rekor is down, the cert is invalid, no signature exists — the pod is denied rather than admitted.","distractors":["Rekor is an append-only transparency log that provides tamper-evident, publicly auditable proof that a signature existed at a point in time.","An SBOM is a machine-readable inventory of every component in an artifact, typically in CycloneDX or SPDX format.","First assess exploitability in your context — is the vulnerable code path reachable?"]},{"id":"devops/devsecops_and_supply_chain_security#d5b99dbbf911","section":"devops","module":"devops/devsecops_and_supply_chain_security","moduleName":"devsecops and supply chain security","qIndex":8,"difficulty":"intermediate","question":"A dependency has a CRITICAL CVE but no fix is available. How do you handle the gate?","answerFull":"First assess exploitability in your context — is the vulnerable code path reachable? If unreachable, use --ignore-unfixed or a documented VEX (Vulnerability Exploitability eXchange) statement to suppress it with a recorded justification, rather than blanket-ignoring all CRITICALs. The goal is to keep the gate meaningful: unconditional suppression trains developers to ignore the scanner, so every waiver must be time-boxed and reviewed.","correct":"First assess exploitability in your context — is the vulnerable code path reachable?","distractors":["SAST analyzes source code statically for vulnerable patterns; DAST probes a running application with crafted requests; SCA inspects your dependency manifests against a CVE database.","Scope every token to least privilege: a job that builds repo A should hold a token that can push only to repo A's registry path with a short TTL, not org-wide write.","An SBOM is a machine-readable inventory of every component in an artifact, typically in CycloneDX or SPDX format."]},{"id":"devops/devsecops_and_supply_chain_security#08b5fa609950","section":"devops","module":"devops/devsecops_and_supply_chain_security","moduleName":"devsecops and supply chain security","qIndex":9,"difficulty":"intermediate","question":"What is provenance and why is it stronger than scanning?","answerFull":"Provenance is signed metadata describing how an artifact was built — the source commit, builder identity, and inputs — bound to the artifact's digest. It is stronger than scanning because scanning only finds known-bad things present today, while provenance lets you reject any artifact that did not come from your trusted builder, defending against unknown and future threats. Scanning answers \"is this dirty?\"; provenance answers \"is this even mine?\".","correct":"Provenance is signed metadata describing how an artifact was built — the source commit, builder identity, and inputs — bound to the artifact's digest.","distractors":["The workflow requests an OIDC token from GitHub's identity provider, and the cloud (e.g., AWS via an IAM OIDC trust) exchanges it for temporary credentials scoped to a specific role, typically valid ~1 hour.","An SBOM is a machine-readable inventory of every component in an artifact, typically in CycloneDX or SPDX format.","SAST analyzes source code statically for vulnerable patterns; DAST probes a running application with crafted requests; SCA inspects your dependency manifests against a CVE database."]},{"id":"devops/devsecops_and_supply_chain_security#c638be1c031e","section":"devops","module":"devops/devsecops_and_supply_chain_security","moduleName":"devsecops and supply chain security","qIndex":10,"difficulty":"intermediate","question":"How do you prevent a single compromised CI step from poisoning all artifacts?","answerFull":"Scope every token to least privilege: a job that builds repo A should hold a token that can push only to repo A's registry path with a short TTL, not org-wide write. Use OIDC federation instead of static secrets, separate the signing step into an isolated job with its own narrow identity, and require SLSA L3 so the provenance itself is generated outside the user-controlled job. This way a compromised build step cannot forge provenance or reach other repositories.","correct":"Scope every token to least privilege: a job that builds repo A should hold a token that can push only to repo A's registry path with a short TTL, not org-wide write.","distractors":["Start the policy in warn mode so it logs violations without denying, sign all artifacts in CI, then watch the warning rate drop toward zero as old unsigned images age out.","Keyless signing eliminates the long-lived private key, which is the single most stolen secret in signing systems.","First assess exploitability in your context — is the vulnerable code path reachable?"]},{"id":"devops/devsecops_and_supply_chain_security#091df1ce68e9","section":"devops","module":"devops/devsecops_and_supply_chain_security","moduleName":"devsecops and supply chain security","qIndex":11,"difficulty":"advanced","question":"What is the practical difference between CycloneDX and SPDX?","answerFull":"Both are standard SBOM formats; CycloneDX (OWASP) is security-focused with first-class vulnerability and VEX support, while SPDX (Linux Foundation, ISO/IEC 5962) is license-and-compliance-focused and broader in scope. In practice CycloneDX is more common in security tooling pipelines and SPDX in legal/compliance contexts. syft emits both, so generate the format your downstream consumers expect rather than betting on one.","correct":"Both are standard SBOM formats; CycloneDX (OWASP) is security-focused with first-class vulnerability and VEX support, while SPDX (Linux Foundation, ISO/IEC 5962) is license-and-compliance-focused and broader in scope.","distractors":["An SBOM is a machine-readable inventory of every component in an artifact, typically in CycloneDX or SPDX format.","Scope every token to least privilege: a job that builds repo A should hold a token that can push only to repo A's registry path with a short TTL, not org-wide write.","Failing closed means that when the verifier cannot confirm a signature — Rekor is down, the cert is invalid, no signature exists — the pod is denied rather than admitted."]},{"id":"devops/devsecops_and_supply_chain_security#88e0612c6328","section":"devops","module":"devops/devsecops_and_supply_chain_security","moduleName":"devsecops and supply chain security","qIndex":12,"difficulty":"advanced","question":"How would you roll out signature enforcement without breaking existing deploys?","answerFull":"Start the policy in warn mode so it logs violations without denying, sign all artifacts in CI, then watch the warning rate drop toward zero as old unsigned images age out. Once warnings are near zero and a break-glass path is tested, flip the policy to enforce per-namespace, starting with low-risk namespaces. This staged rollout surfaces unsigned legacy workloads before they cause an outage and gives teams time to onboard their signing pipelines.","correct":"Start the policy in warn mode so it logs violations without denying, sign all artifacts in CI, then watch the warning rate drop toward zero as old unsigned images age out.","distractors":["Failing closed means that when the verifier cannot confirm a signature — Rekor is down, the cert is invalid, no signature exists — the pod is denied rather than admitted.","Rekor is an append-only transparency log that provides tamper-evident, publicly auditable proof that a signature existed at a point in time.","An SBOM is a machine-readable inventory of every component in an artifact, typically in CycloneDX or SPDX format."]},{"id":"devops/disaster_recovery_and_resilience#f5f7ed7d5d5e","section":"devops","module":"devops/disaster_recovery_and_resilience","moduleName":"disaster recovery and resilience","qIndex":1,"difficulty":"core","question":"Define RTO and RPO and explain how each drives architecture.","answerFull":"RTO is the maximum acceptable downtime (time to restore service); RPO is the maximum acceptable data loss measured in time. RTO pushes you toward standby infrastructure and automated failover (warm/active-active shrink RTO to minutes/seconds), while RPO pushes you toward replication frequency and synchronicity (sync replication or sub-second async gives RPO near zero). Set both from business impact first, then pick the cheapest DR tier that satisfies both.","correct":"RTO is the maximum acceptable downtime (time to restore service); RPO is the maximum acceptable data loss measured in time.","distractors":["RPO equals the replication lag at the moment of failure for async systems, or the backup interval for backup-restore.","Synchronous replication acknowledges a write only after the replica has it, giving RPO 0 but adding the cross-region round-trip (10–80 ms) to every write and coupling availability to the remote site.","A successful backup job only proves bytes were written, not that they are restorable, complete, or that your runbook works."]},{"id":"devops/disaster_recovery_and_resilience#5eb34ee266ff","section":"devops","module":"devops/disaster_recovery_and_resilience","moduleName":"disaster recovery and resilience","qIndex":4,"difficulty":"core","question":"Why is \"the backup succeeded\" insufficient, and what do you do instead?","answerFull":"A successful backup job only proves bytes were written, not that they are restorable, complete, or that your runbook works. GitLab in 2017 had five backup methods all silently broken and lost 6 hours of data. Run scheduled restore drills (game days) that restore from cold into a clean environment and measure the actual wall-clock RTO and the data gap (RPO).","correct":"A successful backup job only proves bytes were written, not that they are restorable, complete, or that your runbook works.","distractors":["RPO equals the replication lag at the moment of failure for async systems, or the backup interval for backup-restore.","At a regional outage every tenant fails over at once, so the DR region's spare capacity is in contention and your scale-from-2-to-20 request may be throttled or denied exactly when you need it.","Chaos engineering deliberately injects failures (kill instances, blackhole a region, add latency) into production-like systems to validate that resilience mechanisms actually work before a real disaster tests them."]},{"id":"devops/disaster_recovery_and_resilience#3f4f3df55f3a","section":"devops","module":"devops/disaster_recovery_and_resilience","moduleName":"disaster recovery and resilience","qIndex":5,"difficulty":"core","question":"What RPO does Aurora Global Database provide and how?","answerFull":"Typically under 1 second, via dedicated storage-layer replication (not binlog) that ships changes from the primary region's storage volume to secondary regions with sub-second lag. Managed planned failover promotes a secondary to writer in under a minute; unplanned failover is an API call. Monitor AuroraGlobalDBReplicationLag and alert if it exceeds your RPO budget.","correct":"Typically under 1 second, via dedicated storage-layer replication (not binlog) that ships changes from the primary region's storage volume to secondary regions with sub-second lag.","distractors":["Synchronous replication acknowledges a write only after the replica has it, giving RPO 0 but adding the cross-region round-trip (10–80 ms) to every write and coupling availability to the remote site.","Store backups in a separate AWS account with cross-account replication, enable S3 Object Lock (WORM) in compliance mode so even root cannot delete before retention expires, and use MFA-delete.","At a regional outage every tenant fails over at once, so the DR region's spare capacity is in contention and your scale-from-2-to-20 request may be throttled or denied exactly when you need it."]},{"id":"devops/disaster_recovery_and_resilience#65b1e3ec7900","section":"devops","module":"devops/disaster_recovery_and_resilience","moduleName":"disaster recovery and resilience","qIndex":6,"difficulty":"intermediate","question":"What is the difference between synchronous and asynchronous replication for DR?","answerFull":"Synchronous replication acknowledges a write only after the replica has it, giving RPO 0 but adding the cross-region round-trip (10–80 ms) to every write and coupling availability to the remote site. Asynchronous replication acknowledges locally and ships changes in the background, keeping write latency low but making RPO equal to the replication lag at failure. Use sync within a region (multi-AZ) and async across regions.","correct":"Synchronous replication acknowledges a write only after the replica has it, giving RPO 0 but adding the cross-region round-trip (10–80 ms) to every write and coupling availability to the remote site.","distractors":["Typically under 1 second, via dedicated storage-layer replication (not binlog) that ships changes from the primary region's storage volume to secondary regions with sub-second lag.","RPO equals the replication lag at the moment of failure for async systems, or the backup interval for backup-restore.","Chaos engineering deliberately injects failures (kill instances, blackhole a region, add latency) into production-like systems to validate that resilience mechanisms actually work before a real disaster tests them."]},{"id":"devops/disaster_recovery_and_resilience#425c93a1e309","section":"devops","module":"devops/disaster_recovery_and_resilience","moduleName":"disaster recovery and resilience","qIndex":7,"difficulty":"intermediate","question":"What is split-brain in failover and how do you prevent it?","answerFull":"Split-brain is when both the primary and the promoted secondary accept writes simultaneously (e.g., the primary recovers but the secondary was already promoted), producing divergent, conflicting data. Prevent it with fencing — ensure the old primary is demoted/isolated before promoting the secondary — and use a single source of truth for \"who is writer\" (a managed failover API rather than ad-hoc promotion). Gate failback through a deliberate, reviewed process with replication catch-up.","correct":"Split-brain is when both the primary and the promoted secondary accept writes simultaneously (e.g., the primary recovers but the secondary was already promoted), producing divergent, conflicting data.","distractors":["Typically under 1 second, via dedicated storage-layer replication (not binlog) that ships changes from the primary region's storage volume to secondary regions with sub-second lag.","Synchronous replication acknowledges a write only after the replica has it, giving RPO 0 but adding the cross-region round-trip (10–80 ms) to every write and coupling availability to the remote site.","A successful backup job only proves bytes were written, not that they are restorable, complete, or that your runbook works."]},{"id":"devops/disaster_recovery_and_resilience#b9ed0ed620f2","section":"devops","module":"devops/disaster_recovery_and_resilience","moduleName":"disaster recovery and resilience","qIndex":8,"difficulty":"intermediate","question":"Your warm standby runs 2 nodes and must scale to 20 during a regional failover. What can go wrong?","answerFull":"At a regional outage every tenant fails over at once, so the DR region's spare capacity is in contention and your scale-from-2-to-20 request may be throttled or denied exactly when you need it. Mitigate with On-Demand Capacity Reservations, EC2 warm pools (pre-initialized stopped instances), or running closer to full capacity. Pre-baked AMIs also cut the per-node boot time materially.","correct":"At a regional outage every tenant fails over at once, so the DR region's spare capacity is in contention and your scale-from-2-to-20 request may be throttled or denied exactly when you need it.","distractors":["Chaos engineering deliberately injects failures (kill instances, blackhole a region, add latency) into production-like systems to validate that resilience mechanisms actually work before a real disaster tests them.","Synchronous replication acknowledges a write only after the replica has it, giving RPO 0 but adding the cross-region round-trip (10–80 ms) to every write and coupling availability to the remote site.","Typically under 1 second, via dedicated storage-layer replication (not binlog) that ships changes from the primary region's storage volume to secondary regions with sub-second lag."]},{"id":"devops/disaster_recovery_and_resilience#d6e66cc5fd08","section":"devops","module":"devops/disaster_recovery_and_resilience","moduleName":"disaster recovery and resilience","qIndex":9,"difficulty":"intermediate","question":"How do you protect backups against ransomware and accidental deletion?","answerFull":"Store backups in a separate AWS account with cross-account replication, enable S3 Object Lock (WORM) in compliance mode so even root cannot delete before retention expires, and use MFA-delete. This breaks the blast radius — a compromised production credential cannot reach the immutable backup copy. Combine with versioning and at least one offline or logically-airgapped copy for tier-0 data.","correct":"Store backups in a separate AWS account with cross-account replication, enable S3 Object Lock (WORM) in compliance mode so even root cannot delete before retention expires, and use MFA-delete.","distractors":["RPO equals the replication lag at the moment of failure for async systems, or the backup interval for backup-restore.","Typically under 1 second, via dedicated storage-layer replication (not binlog) that ships changes from the primary region's storage volume to secondary regions with sub-second lag.","Synchronous replication acknowledges a write only after the replica has it, giving RPO 0 but adding the cross-region round-trip (10–80 ms) to every write and coupling availability to the remote site."]},{"id":"devops/disaster_recovery_and_resilience#86d78dd03825","section":"devops","module":"devops/disaster_recovery_and_resilience","moduleName":"disaster recovery and resilience","qIndex":10,"difficulty":"intermediate","question":"What is chaos engineering and how does it relate to DR?","answerFull":"Chaos engineering deliberately injects failures (kill instances, blackhole a region, add latency) into production-like systems to validate that resilience mechanisms actually work before a real disaster tests them. It turns DR from an untested assumption into a continuously verified property — Netflix's Chaos Kong fails entire regions on a schedule. Start small (one instance) with a defined blast radius and abort conditions, then escalate to region evacuation game days.","correct":"Chaos engineering deliberately injects failures (kill instances, blackhole a region, add latency) into production-like systems to validate that resilience mechanisms actually work before a real disaster tests them.","distractors":["Synchronous replication acknowledges a write only after the replica has it, giving RPO 0 but adding the cross-region round-trip (10–80 ms) to every write and coupling availability to the remote site.","Typically under 1 second, via dedicated storage-layer replication (not binlog) that ships changes from the primary region's storage volume to secondary regions with sub-second lag.","At a regional outage every tenant fails over at once, so the DR region's spare capacity is in contention and your scale-from-2-to-20 request may be throttled or denied exactly when you need it."]},{"id":"devops/disaster_recovery_and_resilience#b70f9edec33f","section":"devops","module":"devops/disaster_recovery_and_resilience","moduleName":"disaster recovery and resilience","qIndex":11,"difficulty":"advanced","question":"How do you calculate the RPO you are actually achieving in production?","answerFull":"RPO equals the replication lag at the moment of failure for async systems, or the backup interval for backup-restore. Continuously measure the lag metric (ReplicaLag, AuroraGlobalDBReplicationLag, or replication queue depth) and the age of the most recent successful backup, then alert when either exceeds the target. Your worst-case RPO is the maximum observed lag, not the average — design and alert against the tail.","correct":"RPO equals the replication lag at the moment of failure for async systems, or the backup interval for backup-restore.","distractors":["Synchronous replication acknowledges a write only after the replica has it, giving RPO 0 but adding the cross-region round-trip (10–80 ms) to every write and coupling availability to the remote site.","Chaos engineering deliberately injects failures (kill instances, blackhole a region, add latency) into production-like systems to validate that resilience mechanisms actually work before a real disaster tests them.","Typically under 1 second, via dedicated storage-layer replication (not binlog) that ships changes from the primary region's storage volume to secondary regions with sub-second lag."]},{"id":"devops/event_streaming_operations#9683f53a5a2d","section":"devops","module":"devops/event_streaming_operations","moduleName":"event streaming operations","qIndex":1,"difficulty":"core","question":"How do you guarantee no acknowledged message is ever lost in Kafka?","answerFull":"Three settings must align: replication factor 3, min.insync.replicas=2 on the topic, and acks=all on the producer. With acks=all, the producer's write is only acknowledged once all *in-sync* replicas have it; min.insync.replicas=2 means the broker refuses the write unless at least 2 replicas are in sync, so an acked message lives on ≥2 brokers. Lose one broker and the data survives and the topic stays writable; lose two and writes block (correctly — blocking beats silent data loss). The classic bug is RF=3 with min.insync.replicas=1: a leader can ack a write it hasn't replicated, then die, and the message is gone despite acks=all.","correct":"Three settings must align: replication factor 3, min.insync.replicas=2 on the topic, and acks=all on the producer.","distractors":["Roll the brokers one at a time, waiting for full ISR recovery between each, so the cluster never drops below min.insync.replicas.","A Schema Registry (Confluent, Apicurio, AWS Glue Schema Registry) stores the Avro/Protobuf/JSON schema for each topic and enforces compatibility rules as producers evolve their data.","Because Kafka's read performance comes from the OS page cache, not the JVM heap — recently written segments stay in page cache and are served to consumers without disk I/O."]},{"id":"devops/event_streaming_operations#3a4e7dd89ef0","section":"devops","module":"devops/event_streaming_operations","moduleName":"event streaming operations","qIndex":2,"difficulty":"core","question":"How do you decide how many partitions a topic needs?","answerFull":"Take the max of two requirements: throughput and consumer parallelism. For throughput, divide target peak write rate by a measured per-partition write limit (often ~10–25 MB/s) — 600 MB/s ÷ 20 = 30 partitions. For parallelism, partitions must be ≥ the maximum number of consumers you want active in any one group, since a partition is consumed by exactly one consumer in a group. Round up for headroom because *increasing* partitions later is disruptive (it breaks key-to-partition ordering and triggers reassignment). But don't over-provision: each partition costs open file handles, memory, replication traffic, and longer rebalance/leader-election times, with a practical ceiling around a few thousand partitions per broker.","correct":"Take the max of two requirements: throughput and consumer parallelism.","distractors":["Simultaneous spike across all partitions points to a group-wide event, not a single slow partition.","A new broker starts empty — it holds no partitions until you reassign some to it.","Lag is log-end-offset − committed-offset summed across a group's partitions — how many messages the consumer is behind the head of the log."]},{"id":"devops/event_streaming_operations#0ec5025e4d12","section":"devops","module":"devops/event_streaming_operations","moduleName":"event streaming operations","qIndex":3,"difficulty":"core","question":"What is consumer lag and why is it the most important streaming metric?","answerFull":"Lag is log-end-offset − committed-offset summed across a group's partitions — how many messages the consumer is behind the head of the log. It's the key metric because it directly measures whether consumers are keeping up: flat near zero is healthy, steadily rising means consume rate < produce rate. The danger is hitting the retention edge — if lag grows until the oldest un-consumed messages expire past retention.ms, you permanently lose data the consumer never read. So you alert on rising lag *before* it approaches the retention window, which buys time to scale consumers or fix the bottleneck. Throughput alone can look healthy while lag silently grows.","correct":"Lag is log-end-offset − committed-offset summed across a group's partitions — how many messages the consumer is behind the head of the log.","distractors":["Simultaneous spike across all partitions points to a group-wide event, not a single slow partition.","KRaft replaces the external ZooKeeper ensemble with an internal Raft-based controller quorum that stores cluster metadata in a Kafka log itself.","A rebalance triggers whenever group membership changes — a consumer joins, leaves, crashes, or a deploy restarts pods."]},{"id":"devops/event_streaming_operations#c5575b1e9d96","section":"devops","module":"devops/event_streaming_operations","moduleName":"event streaming operations","qIndex":4,"difficulty":"core","question":"Explain what happens during a consumer-group rebalance and why frequent rebalances hurt.","answerFull":"A rebalance triggers whenever group membership changes — a consumer joins, leaves, crashes, or a deploy restarts pods. With the default eager protocol, the group coordinator revokes all partition assignments from all consumers, then reassigns them; during this stop-the-world pause, no one consumes, so lag spikes. Frequent rebalances (e.g., on every deploy of a large group) cause repeated pauses and unstable consumption. Mitigations: cooperative/incremental rebalancing (only the moving partitions are revoked, not all), and static membership (group.instance.id) so a consumer that briefly restarts rejoins with its old assignment without triggering a reassignment at all.","correct":"A rebalance triggers whenever group membership changes — a consumer joins, leaves, crashes, or a deploy restarts pods.","distractors":["Lag is log-end-offset − committed-offset summed across a group's partitions — how many messages the consumer is behind the head of the log.","Simultaneous spike across all partitions points to a group-wide event, not a single slow partition.","Take the max of two requirements: throughput and consumer parallelism."]},{"id":"devops/event_streaming_operations#fad1448a54f6","section":"devops","module":"devops/event_streaming_operations","moduleName":"event streaming operations","qIndex":5,"difficulty":"core","question":"Why is rack awareness critical and what breaks without it?","answerFull":"Rack awareness tells Kafka which AZ each broker is in (broker.rack), so it places the replicas of a partition across *different* racks/AZs. Without it, Kafka can place all 3 replicas of a partition on brokers in the same AZ — so when that AZ fails (a common cloud event), the partition goes fully offline despite RF=3, and you've paid for replication that bought you nothing. With rack awareness, RF=3 across 3 AZs survives a whole-AZ outage with 2 ISR remaining and the topic still writable. In Strimzi you set rack.topologyKey: topology.kubernetes.io/zone.","correct":"Rack awareness tells Kafka which AZ each broker is in (broker.rack), so it places the replicas of a partition across *different* racks/AZs.","distractors":["Simultaneous spike across all partitions points to a group-wide event, not a single slow partition.","KRaft replaces the external ZooKeeper ensemble with an internal Raft-based controller quorum that stores cluster metadata in a Kafka log itself.","Because Kafka's read performance comes from the OS page cache, not the JVM heap — recently written segments stay in page cache and are served to consumers without disk I/O."]},{"id":"devops/event_streaming_operations#55acef8100e8","section":"devops","module":"devops/event_streaming_operations","moduleName":"event streaming operations","qIndex":6,"difficulty":"intermediate","question":"How do you add a broker and rebalance data without impacting clients?","answerFull":"A new broker starts empty — it holds no partitions until you reassign some to it. The reassignment copies partition data over the network, which, unthrottled, saturates NICs and starves producer/consumer traffic. The safe procedure: use Cruise Control (or kafka-reassign-partitions.sh) to generate a balanced plan, execute it with a replication throttle (e.g., --throttle 50000000 for 50 MB/s) so client traffic is preserved, and let it move gradually. Cruise Control automates this with capacity/rack goals and built-in throttling, which is why LinkedIn built it. Verify under-replicated partitions return to zero before removing the throttle.","correct":"A new broker starts empty — it holds no partitions until you reassign some to it.","distractors":["Lag is log-end-offset − committed-offset summed across a group's partitions — how many messages the consumer is behind the head of the log.","Simultaneous spike across all partitions points to a group-wide event, not a single slow partition.","Rack awareness tells Kafka which AZ each broker is in (broker.rack), so it places the replicas of a partition across *different* racks/AZs."]},{"id":"devops/event_streaming_operations#79854deb0c4b","section":"devops","module":"devops/event_streaming_operations","moduleName":"event streaming operations","qIndex":7,"difficulty":"intermediate","question":"Why do you keep the Kafka JVM heap small on a large-memory broker?","answerFull":"Because Kafka's read performance comes from the OS page cache, not the JVM heap — recently written segments stay in page cache and are served to consumers without disk I/O. If you set the heap to most of the machine's RAM (say -Xmx24g on 32GB), you starve the page cache, force disk reads, and tank throughput, while also creating long GC pauses. The standard guidance is a modest heap (~6–8 GB) and leaving the bulk of RAM to the page cache. This is counterintuitive to people who tune other JVM apps by maximizing heap.","correct":"Because Kafka's read performance comes from the OS page cache, not the JVM heap — recently written segments stay in page cache and are served to consumers without disk I/O.","distractors":["Rack awareness tells Kafka which AZ each broker is in (broker.rack), so it places the replicas of a partition across *different* racks/AZs.","A new broker starts empty — it holds no partitions until you reassign some to it.","KRaft replaces the external ZooKeeper ensemble with an internal Raft-based controller quorum that stores cluster metadata in a Kafka log itself."]},{"id":"devops/event_streaming_operations#e4de46f39a8f","section":"devops","module":"devops/event_streaming_operations","moduleName":"event streaming operations","qIndex":8,"difficulty":"intermediate","question":"KRaft vs ZooKeeper — what changed and why does it matter operationally?","answerFull":"KRaft replaces the external ZooKeeper ensemble with an internal Raft-based controller quorum that stores cluster metadata in a Kafka log itself. Operationally it means one fewer distributed system to run, secure, and upgrade; faster controller failover and metadata propagation; and a much higher partition ceiling (millions cluster-wide) because metadata is no longer bottlenecked on ZK. KRaft is the default since Kafka 3.5+, and ZooKeeper support was removed in Kafka 4.0, so all new clusters should be KRaft. The migration path for existing ZK clusters is a documented, staged process — but greenfield is always KRaft now.","correct":"KRaft replaces the external ZooKeeper ensemble with an internal Raft-based controller quorum that stores cluster metadata in a Kafka log itself.","distractors":["A Schema Registry (Confluent, Apicurio, AWS Glue Schema Registry) stores the Avro/Protobuf/JSON schema for each topic and enforces compatibility rules as producers evolve their data.","Because Kafka's read performance comes from the OS page cache, not the JVM heap — recently written segments stay in page cache and are served to consumers without disk I/O.","Roll the brokers one at a time, waiting for full ISR recovery between each, so the cluster never drops below min.insync.replicas."]},{"id":"devops/event_streaming_operations#92cd032306f4","section":"devops","module":"devops/event_streaming_operations","moduleName":"event streaming operations","qIndex":9,"difficulty":"intermediate","question":"How does tiered storage change capacity planning?","answerFull":"Tiered storage (KIP-405; MSK Tiered Storage) lets brokers keep only recent (\"hot\") segments on local EBS/NVMe and offload older segments to object storage (S3). This decouples retention from broker disk: you can keep months of data cheaply in S3 while broker disks stay small (hours of hot data). Capacity planning splits into hot disk sizing (avg_ingest × hot_window × RF) and cold object-storage cost (cheap per GB). The tradeoff is that reads of cold data are slower (fetched from S3) — fine for replay/backfill, not for latency-critical consumers. It's the standard way to offer long retention without a fleet of huge, expensive broker disks.","correct":"Tiered storage (KIP-405; MSK Tiered Storage) lets brokers keep only recent (\"hot\") segments on local EBS/NVMe and offload older segments to object storage (S3).","distractors":["Roll the brokers one at a time, waiting for full ISR recovery between each, so the cluster never drops below min.insync.replicas.","Because Kafka's read performance comes from the OS page cache, not the JVM heap — recently written segments stay in page cache and are served to consumers without disk I/O.","A Schema Registry (Confluent, Apicurio, AWS Glue Schema Registry) stores the Avro/Protobuf/JSON schema for each topic and enforces compatibility rules as producers evolve their data."]},{"id":"devops/event_streaming_operations#9ed87ef124bf","section":"devops","module":"devops/event_streaming_operations","moduleName":"event streaming operations","qIndex":10,"difficulty":"intermediate","question":"A consumer group's lag suddenly spiked across all partitions at the same time. What are the likely causes?","answerFull":"Simultaneous spike across all partitions points to a group-wide event, not a single slow partition. Most likely: (1) a rebalance — a deploy or a crashed consumer triggered a stop-the-world reassignment that paused everyone; (2) a downstream dependency the consumers all write to (a database, an API) slowed or went down, so processing stalled uniformly; (3) a poison message or code bug causing all consumers to retry/block. I'd check rebalance metrics and consumer logs first (was there a deploy?), then downstream health. A single-partition lag spike, by contrast, suggests a hot key or one stuck consumer. The diagnostic split — all partitions vs one — immediately narrows the cause.","correct":"Simultaneous spike across all partitions points to a group-wide event, not a single slow partition.","distractors":["Rack awareness tells Kafka which AZ each broker is in (broker.rack), so it places the replicas of a partition across *different* racks/AZs.","Roll the brokers one at a time, waiting for full ISR recovery between each, so the cluster never drops below min.insync.replicas.","A new broker starts empty — it holds no partitions until you reassign some to it."]},{"id":"devops/event_streaming_operations#89043b9308a4","section":"devops","module":"devops/event_streaming_operations","moduleName":"event streaming operations","qIndex":11,"difficulty":"advanced","question":"How do you do a zero-downtime Kafka version upgrade?","answerFull":"Roll the brokers one at a time, waiting for full ISR recovery between each, so the cluster never drops below min.insync.replicas. Strimzi automates this respecting PodDisruptionBudgets and ISR — it restarts a broker, waits until all its partitions are back in-sync, then moves to the next. You also stage the inter.broker.protocol.version and log.message.format.version: upgrade binaries first while keeping the old protocol version, confirm stability, then bump the protocol version in a second roll. The cardinal sin is restarting two brokers of an RF=3/min-ISR=2 topic simultaneously — that drops ISR below the minimum and blocks writes. The whole point of the slow, one-at-a-time roll is preserving the ISR invariant.","correct":"Roll the brokers one at a time, waiting for full ISR recovery between each, so the cluster never drops below min.insync.replicas.","distractors":["KRaft replaces the external ZooKeeper ensemble with an internal Raft-based controller quorum that stores cluster metadata in a Kafka log itself.","Tiered storage (KIP-405; MSK Tiered Storage) lets brokers keep only recent (\"hot\") segments on local EBS/NVMe and offload older segments to object storage (S3).","Three settings must align: replication factor 3, min.insync.replicas=2 on the topic, and acks=all on the producer."]},{"id":"devops/event_streaming_operations#71eed8f21333","section":"devops","module":"devops/event_streaming_operations","moduleName":"event streaming operations","qIndex":13,"difficulty":"advanced","question":"Why does a streaming platform need a Schema Registry, and what breaks without one?","answerFull":"A Schema Registry (Confluent, Apicurio, AWS Glue Schema Registry) stores the Avro/Protobuf/JSON schema for each topic and enforces compatibility rules as producers evolve their data. Without it, a producer that adds a required field or renames one silently ships messages that every consumer fails to deserialize — a cluster-wide outage triggered by one team's deploy, discovered only when consumers start erroring. The registry prevents this by rejecting incompatible schema changes at publish time (e.g., BACKWARD compatibility means new schemas can read old data, so consumers can upgrade after producers). Operationally it decouples producer and consumer release cycles, which is the entire point of an event-streaming platform — teams evolve independently. It also shrinks messages (the schema ID replaces inline schema) and gives you a governance audit trail. Skipping it works until the first breaking change, then it's a painful incident.","correct":"A Schema Registry (Confluent, Apicurio, AWS Glue Schema Registry) stores the Avro/Protobuf/JSON schema for each topic and enforces compatibility rules as producers evolve their data.","distractors":["Because Kafka's read performance comes from the OS page cache, not the JVM heap — recently written segments stay in page cache and are served to consumers without disk I/O.","KRaft replaces the external ZooKeeper ensemble with an internal Raft-based controller quorum that stores cluster metadata in a Kafka log itself.","Three settings must align: replication factor 3, min.insync.replicas=2 on the topic, and acks=all on the producer."]},{"id":"devops/gcp_and_azure_essentials#64f6e034f132","section":"devops","module":"devops/gcp_and_azure_essentials","moduleName":"gcp and azure essentials","qIndex":3,"difficulty":"core","question":"Why is GCP's VPC model different, and what's the practical consequence?","answerFull":"A GCP VPC is a global resource with regional subnets, whereas AWS VPCs and Azure VNets are regional. The practical consequence is that instances in different regions of the same GCP VPC can communicate over Google's private backbone without VPC peering, simplifying global architectures. On AWS/Azure you'd create regional VPCs/VNets and connect them with peering or Transit Gateway — see cloud_networking_and_cdn.","correct":"A GCP VPC is a global resource with regional subnets, whereas AWS VPCs and Azure VNets are regional.","distractors":["GCP uses Workload Identity, which binds a Kubernetes service account to a GCP service account via the cluster's workload identity pool, so pods get short-lived GCP credentials with no static keys.","Choose Azure when the organization is heavily invested in Microsoft — Active Directory, Office 365, Windows Server, .NET — because Entra ID integrates natively and licensing (Azure Hybrid Benefit) is cheaper.","For private connectivity to services without traversing the public internet: AWS PrivateLink maps to GCP Private Service Connect and Azure Private Link."]},{"id":"devops/gcp_and_azure_essentials#91ffd6f4c87e","section":"devops","module":"devops/gcp_and_azure_essentials","moduleName":"gcp and azure essentials","qIndex":4,"difficulty":"core","question":"What is Cloud Run and how does it compare to AWS Fargate and Lambda?","answerFull":"Cloud Run is a fully managed serverless platform that runs containers, scales to zero, and bills per request/CPU-time, handling concurrency (multiple requests per instance). Compared to Lambda it accepts any container and supports higher concurrency per instance; compared to Fargate it requires no ECS/EKS cluster — it's truly managed. Use Cloud Run for stateless HTTP services where you want serverless economics without the Lambda packaging model.","correct":"Cloud Run is a fully managed serverless platform that runs containers, scales to zero, and bills per request/CPU-time, handling concurrency (multiple requests per instance).","distractors":["For private connectivity to services without traversing the public internet: AWS PrivateLink maps to GCP Private Service Connect and Azure Private Link.","BigQuery, a serverless data warehouse that separates storage and compute and scales query compute automatically with no cluster to manage.","Standardize on portable layers — Kubernetes (EKS/GKE/AKS), containers, object storage, Terraform IaC — so workloads move with minimal change, and isolate provider-specific services behind interfaces."]},{"id":"devops/gcp_and_azure_essentials#775742ab401c","section":"devops","module":"devops/gcp_and_azure_essentials","moduleName":"gcp and azure essentials","qIndex":6,"difficulty":"intermediate","question":"What is the GCP/Azure equivalent of AWS IRSA for giving pods cloud permissions?","answerFull":"GCP uses Workload Identity, which binds a Kubernetes service account to a GCP service account via the cluster's workload identity pool, so pods get short-lived GCP credentials with no static keys. Azure uses Workload Identity (federated) or Managed Identities, mapping a Kubernetes service account to an Entra ID identity via the cluster's OIDC issuer. All three (IRSA, Workload Identity) achieve the same goal: per-pod least-privilege cloud access without embedding keys.","correct":"GCP uses Workload Identity, which binds a Kubernetes service account to a GCP service account via the cluster's workload identity pool, so pods get short-lived GCP credentials with no static keys.","distractors":["BigQuery, a serverless data warehouse that separates storage and compute and scales query compute automatically with no cluster to manage.","For private connectivity to services without traversing the public internet: AWS PrivateLink maps to GCP Private Service Connect and Azure Private Link.","Standardize on portable layers — Kubernetes (EKS/GKE/AKS), containers, object storage, Terraform IaC — so workloads move with minimal change, and isolate provider-specific services behind interfaces."]},{"id":"devops/gcp_and_azure_essentials#a1f63c545dcf","section":"devops","module":"devops/gcp_and_azure_essentials","moduleName":"gcp and azure essentials","qIndex":7,"difficulty":"intermediate","question":"When would you choose Azure over AWS or GCP?","answerFull":"Choose Azure when the organization is heavily invested in Microsoft — Active Directory, Office 365, Windows Server, .NET — because Entra ID integrates natively and licensing (Azure Hybrid Benefit) is cheaper. Azure is also strong for hybrid cloud via Azure Arc and in regulated/government sectors. The deciding factor is usually existing enterprise identity and Microsoft ecosystem rather than raw technical capability.","correct":"Choose Azure when the organization is heavily invested in Microsoft — Active Directory, Office 365, Windows Server, .NET — because Entra ID integrates natively and licensing (Azure Hybrid Benefit) is cheaper.","distractors":["For private connectivity to services without traversing the public internet: AWS PrivateLink maps to GCP Private Service Connect and Azure Private Link.","A GCP VPC is a global resource with regional subnets, whereas AWS VPCs and Azure VNets are regional.","GCP uses Workload Identity, which binds a Kubernetes service account to a GCP service account via the cluster's workload identity pool, so pods get short-lived GCP credentials with no static keys."]},{"id":"devops/gcp_and_azure_essentials#a7f4ffa5cd58","section":"devops","module":"devops/gcp_and_azure_essentials","moduleName":"gcp and azure essentials","qIndex":8,"difficulty":"intermediate","question":"What's GCP's standout service, and why do data teams favor it?","answerFull":"BigQuery, a serverless data warehouse that separates storage and compute and scales query compute automatically with no cluster to manage. Data teams favor it because you load data and query petabytes with standard SQL while paying per query/storage, avoiding the cluster provisioning and tuning that Redshift historically required. It exemplifies GCP's data/analytics strength.","correct":"BigQuery, a serverless data warehouse that separates storage and compute and scales query compute automatically with no cluster to manage.","distractors":["GCP uses Workload Identity, which binds a Kubernetes service account to a GCP service account via the cluster's workload identity pool, so pods get short-lived GCP credentials with no static keys.","Cloud Run is a fully managed serverless platform that runs containers, scales to zero, and bills per request/CPU-time, handling concurrency (multiple requests per instance).","Standardize on portable layers — Kubernetes (EKS/GKE/AKS), containers, object storage, Terraform IaC — so workloads move with minimal change, and isolate provider-specific services behind interfaces."]},{"id":"devops/gcp_and_azure_essentials#ae9856a00d5f","section":"devops","module":"devops/gcp_and_azure_essentials","moduleName":"gcp and azure essentials","qIndex":9,"difficulty":"intermediate","question":"How do you approach a multi-cloud architecture without doubling operational pain?","answerFull":"Standardize on portable layers — Kubernetes (EKS/GKE/AKS), containers, object storage, Terraform IaC — so workloads move with minimal change, and isolate provider-specific services behind interfaces. Keep one cloud as primary and the other as DR or for specific best-of-breed services (e.g., BigQuery), rather than running everything everywhere. Be deliberate: multi-cloud doubles IAM, networking, monitoring, and egress costs, so adopt it only when resilience or negotiation leverage justifies the complexity.","correct":"Standardize on portable layers — Kubernetes (EKS/GKE/AKS), containers, object storage, Terraform IaC — so workloads move with minimal change, and isolate provider-specific services behind interfaces.","distractors":["Cloud Run is a fully managed serverless platform that runs containers, scales to zero, and bills per request/CPU-time, handling concurrency (multiple requests per instance).","GCP uses Workload Identity, which binds a Kubernetes service account to a GCP service account via the cluster's workload identity pool, so pods get short-lived GCP credentials with no static keys.","For private connectivity to services without traversing the public internet: AWS PrivateLink maps to GCP Private Service Connect and Azure Private Link."]},{"id":"devops/gcp_and_azure_essentials#8c56c4aca85c","section":"devops","module":"devops/gcp_and_azure_essentials","moduleName":"gcp and azure essentials","qIndex":10,"difficulty":"intermediate","question":"What are the cross-cloud equivalents for private connectivity and CDN?","answerFull":"For private connectivity to services without traversing the public internet: AWS PrivateLink maps to GCP Private Service Connect and Azure Private Link. For CDN: AWS CloudFront maps to GCP Cloud CDN and Azure Front Door/CDN. These map one-to-one conceptually, though features (e.g., edge compute, WAF integration) differ in detail — see cloud_networking_and_cdn for how they're used in a global architecture.","correct":"For private connectivity to services without traversing the public internet: AWS PrivateLink maps to GCP Private Service Connect and Azure Private Link.","distractors":["A GCP VPC is a global resource with regional subnets, whereas AWS VPCs and Azure VNets are regional.","GCP uses Workload Identity, which binds a Kubernetes service account to a GCP service account via the cluster's workload identity pool, so pods get short-lived GCP credentials with no static keys.","Standardize on portable layers — Kubernetes (EKS/GKE/AKS), containers, object storage, Terraform IaC — so workloads move with minimal change, and isolate provider-specific services behind interfaces."]},{"id":"devops/gitops_argocd_flux#7983f78260ba","section":"devops","module":"devops/gitops_argocd_flux","moduleName":"gitops argocd flux","qIndex":1,"difficulty":"core","question":"What is GitOps and what are its core principles?","answerFull":"GitOps is an operational model where the desired state of the system is declared in Git (the single source of truth) and an in-cluster agent continuously reconciles the actual state to match. Its four principles: the state is declarative, versioned/immutable in Git, pulled automatically by an agent, and continuously reconciled (drift is detected and corrected). Deploy = commit; rollback = revert; the cluster is downstream of Git.","correct":"GitOps is an operational model where the desired state of the system is declared in Git (the single source of truth) and an in-cluster agent continuously reconciles the actual state to match.","distractors":["Because Git, not the cluster, is the source of truth.","GitOps provides the source of truth and reconciliation; progressive delivery (Argo Rollouts/Flagger) provides metric-gated canary/blue-green.","In push-based CD the pipeline holds cluster credentials and runs kubectl apply — credentials are exposed in CI, there's no drift detection, and the audit trail is just pipeline logs."]},{"id":"devops/gitops_argocd_flux#c38388c8b686","section":"devops","module":"devops/gitops_argocd_flux","moduleName":"gitops argocd flux","qIndex":2,"difficulty":"core","question":"Push-based CD vs pull-based GitOps — what's the difference and why prefer pull?","answerFull":"In push-based CD the pipeline holds cluster credentials and runs kubectl apply — credentials are exposed in CI, there's no drift detection, and the audit trail is just pipeline logs. In pull-based GitOps an in-cluster agent watches Git and reconciles, so no external system needs cluster access, drift is continuously detected and corrected, and Git history is a complete audit log. Pull improves security (no external creds), auditability, and recoverability.","correct":"In push-based CD the pipeline holds cluster credentials and runs kubectl apply — credentials are exposed in CI, there's no drift detection, and the audit trail is just pipeline logs.","distractors":["git revert the offending commit and push — the agent reconciles the cluster back to the previous good state, and the revert is itself an audited commit.","Because Git, not the cluster, is the source of truth.","Because the entire desired state lives in Git, recovering a cluster is \"deploy a fresh ArgoCD/Flux and point it at the repo\" — the agent reconciles everything back."]},{"id":"devops/gitops_argocd_flux#f16b8a4ee3df","section":"devops","module":"devops/gitops_argocd_flux","moduleName":"gitops argocd flux","qIndex":3,"difficulty":"core","question":"How does drift detection and self-healing work?","answerFull":"The GitOps agent continuously diffs the desired state (Git) against the actual cluster state. If they differ — because someone made an out-of-band change or a resource was deleted — it marks the app OutOfSync and, with self-heal enabled, re-applies the Git state to correct the drift. This guarantees the cluster matches Git and that manual changes don't silently persist; the only durable change is a Git change.","correct":"The GitOps agent continuously diffs the desired state (Git) against the actual cluster state.","distractors":["Because Git, not the cluster, is the source of truth.","Because the entire desired state lives in Git, recovering a cluster is \"deploy a fresh ArgoCD/Flux and point it at the repo\" — the agent reconciles everything back.","git revert the offending commit and push — the agent reconciles the cluster back to the previous good state, and the revert is itself an audited commit."]},{"id":"devops/gitops_argocd_flux#b1470b6d3ac7","section":"devops","module":"devops/gitops_argocd_flux","moduleName":"gitops argocd flux","qIndex":4,"difficulty":"core","question":"How do you roll back under GitOps?","answerFull":"git revert the offending commit and push — the agent reconciles the cluster back to the previous good state, and the revert is itself an audited commit. ArgoCD also offers argocd app rollback to revert the live Application to a previously synced revision for immediate relief. Either way, the rollback is declarative and traceable, unlike re-running a push pipeline.","correct":"git revert the offending commit and push — the agent reconciles the cluster back to the previous good state, and the revert is itself an audited commit.","distractors":["In push-based CD the pipeline holds cluster credentials and runs kubectl apply — credentials are exposed in CI, there's no drift detection, and the audit trail is just pipeline logs.","The GitOps agent continuously diffs the desired state (Git) against the actual cluster state.","Because Git, not the cluster, is the source of truth."]},{"id":"devops/gitops_argocd_flux#7f0504e58276","section":"devops","module":"devops/gitops_argocd_flux","moduleName":"gitops argocd flux","qIndex":5,"difficulty":"core","question":"Why shouldn't you kubectl edit production under GitOps?","answerFull":"Because Git, not the cluster, is the source of truth. A manual kubectl change is drift that the agent (with self-heal) reverts on the next sync — so your fix is undone, often reigniting the incident. The correct action is to change the Git repo (commit the fix), which the agent then applies durably. For emergencies you can temporarily pause auto-sync, but you must reconcile Git promptly.","correct":"Because Git, not the cluster, is the source of truth.","distractors":["GitOps provides the source of truth and reconciliation; progressive delivery (Argo Rollouts/Flagger) provides metric-gated canary/blue-green.","Because the entire desired state lives in Git, recovering a cluster is \"deploy a fresh ArgoCD/Flux and point it at the repo\" — the agent reconciles everything back.","In push-based CD the pipeline holds cluster credentials and runs kubectl apply — credentials are exposed in CI, there's no drift detection, and the audit trail is just pipeline logs."]},{"id":"devops/gitops_argocd_flux#14617245b243","section":"devops","module":"devops/gitops_argocd_flux","moduleName":"gitops argocd flux","qIndex":6,"difficulty":"intermediate","question":"ArgoCD vs Flux — how do they differ?","answerFull":"ArgoCD is app-centric with a rich web UI, the Application CRD, app-of-apps and ApplicationSets, and strong visualization/diffing — popular where teams want a GUI and clear sync status. Flux is a set of composable GitOps Toolkit controllers (Source, Kustomize, Helm, Image automation) driven via CRDs/CLI, favoring a more GitOps-native, automation-first style with minimal UI. Both implement the same pull-based reconciliation; choice is largely workflow/UI preference.","correct":"ArgoCD is app-centric with a rich web UI, the Application CRD, app-of-apps and ApplicationSets, and strong visualization/diffing — popular where teams want a GUI and clear sync status.","distractors":["Because the entire desired state lives in Git, recovering a cluster is \"deploy a fresh ArgoCD/Flux and point it at the repo\" — the agent reconciles everything back.","An ArgoCD pattern where a single root Application points at a directory of child Application definitions, so one Application manages many.","Because Git, not the cluster, is the source of truth."]},{"id":"devops/gitops_argocd_flux#4d5b56e66cfd","section":"devops","module":"devops/gitops_argocd_flux","moduleName":"gitops argocd flux","qIndex":7,"difficulty":"intermediate","question":"How do you handle secrets in GitOps if everything is in Git?","answerFull":"You never commit plaintext secrets. Options: SOPS-encrypted manifests (the agent decrypts via a KMS key), Sealed Secrets (encrypted to a cluster-specific key only the in-cluster controller can decrypt), or the External Secrets Operator (commit a *reference*; ESO fetches the real value from Vault/cloud at runtime). The Git repo holds ciphertext or references, never the secret value itself.","correct":"You never commit plaintext secrets.","distractors":["GitOps is an operational model where the desired state of the system is declared in Git (the single source of truth) and an in-cluster agent continuously reconciles the actual state to match.","GitOps provides the source of truth and reconciliation; progressive delivery (Argo Rollouts/Flagger) provides metric-gated canary/blue-green.","The GitOps agent continuously diffs the desired state (Git) against the actual cluster state."]},{"id":"devops/gitops_argocd_flux#d05dd92bdc21","section":"devops","module":"devops/gitops_argocd_flux","moduleName":"gitops argocd flux","qIndex":8,"difficulty":"intermediate","question":"What is the app-of-apps pattern?","answerFull":"An ArgoCD pattern where a single root Application points at a directory of child Application definitions, so one Application manages many. Committing to that directory onboards or removes entire applications declaratively — used to bootstrap a whole cluster (ingress, monitoring, cert-manager, team apps) from one root. ApplicationSets generalize this further with templating across clusters/environments.","correct":"An ArgoCD pattern where a single root Application points at a directory of child Application definitions, so one Application manages many.","distractors":["ArgoCD is app-centric with a rich web UI, the Application CRD, app-of-apps and ApplicationSets, and strong visualization/diffing — popular where teams want a GUI and clear sync status.","GitOps is an operational model where the desired state of the system is declared in Git (the single source of truth) and an in-cluster agent continuously reconciles the actual state to match.","Because the entire desired state lives in Git, recovering a cluster is \"deploy a fresh ArgoCD/Flux and point it at the repo\" — the agent reconciles everything back."]},{"id":"devops/gitops_argocd_flux#b34c902ba255","section":"devops","module":"devops/gitops_argocd_flux","moduleName":"gitops argocd flux","qIndex":10,"difficulty":"intermediate","question":"What's the risk of prune and how do you mitigate it?","answerFull":"With prune enabled, removing a manifest from Git deletes the corresponding live resource — so an accidental deletion or bad refactor can wipe production resources. Mitigate with config-repo protections (required reviews, CODEOWNERS), diff previews before sync, sync windows, and options like PruneLast/manual confirmation for critical apps. Prune is powerful (keeps the cluster matching Git) but demands repo discipline.","correct":"With prune enabled, removing a manifest from Git deletes the corresponding live resource — so an accidental deletion or bad refactor can wipe production resources.","distractors":["GitOps is an operational model where the desired state of the system is declared in Git (the single source of truth) and an in-cluster agent continuously reconciles the actual state to match.","Because Git, not the cluster, is the source of truth.","git revert the offending commit and push — the agent reconciles the cluster back to the previous good state, and the revert is itself an audited commit."]},{"id":"devops/gitops_argocd_flux#5ab0ddbc8d54","section":"devops","module":"devops/gitops_argocd_flux","moduleName":"gitops argocd flux","qIndex":11,"difficulty":"advanced","question":"How does GitOps improve disaster recovery?","answerFull":"Because the entire desired state lives in Git, recovering a cluster is \"deploy a fresh ArgoCD/Flux and point it at the repo\" — the agent reconciles everything back. The cluster is reproducible from Git rather than a hand-tuned snowflake. Combined with etcd/PVC backups for stateful data, GitOps makes the *configuration* portion of DR fast and deterministic (see disaster_recovery_and_resilience).","correct":"Because the entire desired state lives in Git, recovering a cluster is \"deploy a fresh ArgoCD/Flux and point it at the repo\" — the agent reconciles everything back.","distractors":["GitOps is an operational model where the desired state of the system is declared in Git (the single source of truth) and an in-cluster agent continuously reconciles the actual state to match.","ArgoCD is app-centric with a rich web UI, the Application CRD, app-of-apps and ApplicationSets, and strong visualization/diffing — popular where teams want a GUI and clear sync status.","GitOps provides the source of truth and reconciliation; progressive delivery (Argo Rollouts/Flagger) provides metric-gated canary/blue-green."]},{"id":"devops/gitops_argocd_flux#dc5227219350","section":"devops","module":"devops/gitops_argocd_flux","moduleName":"gitops argocd flux","qIndex":12,"difficulty":"advanced","question":"How does GitOps relate to progressive delivery?","answerFull":"GitOps provides the source of truth and reconciliation; progressive delivery (Argo Rollouts/Flagger) provides metric-gated canary/blue-green. They compose: you declare a Rollout CR in Git, ArgoCD/Flux reconciles it, and the Rollouts controller executes the canary with automated analysis and rollback. The release strategy is declarative and audited in Git, and an auto-abort is reflected as the controller reverting to the stable version (see deployment_strategies).","correct":"GitOps provides the source of truth and reconciliation; progressive delivery (Argo Rollouts/Flagger) provides metric-gated canary/blue-green.","distractors":["Because Git, not the cluster, is the source of truth.","GitOps is an operational model where the desired state of the system is declared in Git (the single source of truth) and an in-cluster agent continuously reconciles the actual state to match.","The GitOps agent continuously diffs the desired state (Git) against the actual cluster state."]},{"id":"devops/helm_and_package_management#02df66cb0d95","section":"devops","module":"devops/helm_and_package_management","moduleName":"helm and package management","qIndex":1,"difficulty":"core","question":"What problem does Helm solve over raw kubectl apply?","answerFull":"A real app is many interrelated manifests that differ per environment. Helm packages them into a versioned, templated chart with default values you override per environment, manages them as one named release, and provides atomic upgrade and rollback. Raw YAML forces you to copy-paste per-env variants and manually track what's installed — Helm gives you parameterization, packaging, and release lifecycle.","correct":"A real app is many interrelated manifests that differ per environment.","distractors":["Helm 3 removed Tiller — the in-cluster server component that was a major security concern (it held broad cluster permissions).","Use helm upgrade --install --atomic --wait: --install handles first-time installs, --wait blocks until resources are Ready, and --atomic automatically rolls back if the upgrade fails (so you never end up half-applied).","GitOps tools (ArgoCD/Flux) natively render Helm charts or Kustomize overlays from a Git repo and reconcile the cluster to match."]},{"id":"devops/helm_and_package_management#b8a08971c3e3","section":"devops","module":"devops/helm_and_package_management","moduleName":"helm and package management","qIndex":2,"difficulty":"core","question":"Helm vs Kustomize — how do they differ fundamentally?","answerFull":"Helm uses Go templating: values are substituted into templates to produce manifests, and it tracks releases for rollback. Kustomize is template-free: you patch a base with overlays (strategic-merge/JSON patches), so the output is base+patch — predictable and diffable, with no logic. Helm offers power and packaging; Kustomize offers transparency and simplicity. They're often combined.","correct":"Helm uses Go templating: values are substituted into templates to produce manifests, and it tracks releases for rollback.","distractors":["Use helm upgrade --install --atomic --wait: --install handles first-time installs, --wait blocks until resources are Ready, and --atomic automatically rolls back if the upgrade fails (so you never end up half-applied).","A real app is many interrelated manifests that differ per environment.","Over-templating: cramming so much conditional logic and indirection into templates that the chart becomes unreadable and the rendered output is hard to predict — the opposite of Kustomize's transparency."]},{"id":"devops/helm_and_package_management#a6ebc0d17db3","section":"devops","module":"devops/helm_and_package_management","moduleName":"helm and package management","qIndex":3,"difficulty":"core","question":"What is a Helm release and why does it matter?","answerFull":"A release is a named, versioned instance of a chart installed in the cluster. Each install/upgrade creates a new revision, and Helm stores the rendered manifests, enabling helm rollback to atomically revert to a prior known-good revision. This release lifecycle — not templating — is Helm's distinguishing capability versus plain manifests or Kustomize.","correct":"A release is a named, versioned instance of a chart installed in the cluster.","distractors":["Helm 3 removed Tiller — the in-cluster server component that was a major security concern (it held broad cluster permissions).","GitOps tools (ArgoCD/Flux) natively render Helm charts or Kustomize overlays from a Git repo and reconcile the cluster to match.","Use helm upgrade --install --atomic --wait: --install handles first-time installs, --wait blocks until resources are Ready, and --atomic automatically rolls back if the upgrade fails (so you never end up half-applied)."]},{"id":"devops/helm_and_package_management#1914e1062192","section":"devops","module":"devops/helm_and_package_management","moduleName":"helm and package management","qIndex":4,"difficulty":"core","question":"How do you safely perform a Helm upgrade?","answerFull":"Use helm upgrade --install --atomic --wait: --install handles first-time installs, --wait blocks until resources are Ready, and --atomic automatically rolls back if the upgrade fails (so you never end up half-applied). Preview changes first with the helm diff plugin, pin chart versions, and keep --timeout sane so a stuck rollout fails cleanly.","correct":"Use helm upgrade --install --atomic --wait: --install handles first-time installs, --wait blocks until resources are Ready, and --atomic automatically rolls back if the upgrade fails (so you never end up half-applied).","distractors":["Helm 3 removed Tiller — the in-cluster server component that was a major security concern (it held broad cluster permissions).","Helm uses Go templating: values are substituted into templates to produce manifests, and it tracks releases for rollback.","GitOps tools (ArgoCD/Flux) natively render Helm charts or Kustomize overlays from a Git repo and reconcile the cluster to match."]},{"id":"devops/helm_and_package_management#f73a37e73dca","section":"devops","module":"devops/helm_and_package_management","moduleName":"helm and package management","qIndex":5,"difficulty":"core","question":"How do you handle secrets in Helm charts?","answerFull":"Never put secrets in values.yaml — it ends up in Git. Reference an externally-managed Kubernetes Secret via secretKeyRef, and populate that Secret from an external store (Vault) using the External Secrets Operator, or encrypt values with SOPS/helm-secrets if they must live in Git. The chart should reference secret *names*, not secret *values*.","correct":"Never put secrets in values.yaml — it ends up in Git.","distractors":["Helm 3 removed Tiller — the in-cluster server component that was a major security concern (it held broad cluster permissions).","Helm uses Go templating: values are substituted into templates to produce manifests, and it tracks releases for rollback.","A chart's Chart.yaml lists dependencies (other charts + versions + repos), optionally toggled by condition."]},{"id":"devops/helm_and_package_management#baf86a825900","section":"devops","module":"devops/helm_and_package_management","moduleName":"helm and package management","qIndex":6,"difficulty":"intermediate","question":"What changed between Helm 2 and Helm 3?","answerFull":"Helm 3 removed Tiller — the in-cluster server component that was a major security concern (it held broad cluster permissions). Helm 3 is client-only, using your kubeconfig credentials and RBAC, storing release state as Secrets in the namespace. It also added OCI registry support, improved upgrade strategy, and three-way merge for better drift handling.","correct":"Helm 3 removed Tiller — the in-cluster server component that was a major security concern (it held broad cluster permissions).","distractors":["Helm uses Go templating: values are substituted into templates to produce manifests, and it tracks releases for rollback.","Use helm upgrade --install --atomic --wait: --install handles first-time installs, --wait blocks until resources are Ready, and --atomic automatically rolls back if the upgrade fails (so you never end up half-applied).","A release is a named, versioned instance of a chart installed in the cluster."]},{"id":"devops/helm_and_package_management#99bec17b7bcd","section":"devops","module":"devops/helm_and_package_management","moduleName":"helm and package management","qIndex":8,"difficulty":"intermediate","question":"How do chart dependencies (subcharts) work?","answerFull":"A chart's Chart.yaml lists dependencies (other charts + versions + repos), optionally toggled by condition. helm dependency update vendors them into charts/. On install, subcharts render with their own (overridable) values. This lets an app chart bundle its datastore (e.g., a PostgreSQL subchart) — but pin versions and review subcharts, as they're third-party code in your cluster.","correct":"A chart's Chart.yaml lists dependencies (other charts + versions + repos), optionally toggled by condition.","distractors":["A release is a named, versioned instance of a chart installed in the cluster.","Over-templating: cramming so much conditional logic and indirection into templates that the chart becomes unreadable and the rendered output is hard to predict — the opposite of Kustomize's transparency.","Never put secrets in values.yaml — it ends up in Git."]},{"id":"devops/helm_and_package_management#fc846ceae0e1","section":"devops","module":"devops/helm_and_package_management","moduleName":"helm and package management","qIndex":9,"difficulty":"intermediate","question":"What's a common Helm anti-pattern?","answerFull":"Over-templating: cramming so much conditional logic and indirection into templates that the chart becomes unreadable and the rendered output is hard to predict — the opposite of Kustomize's transparency. Other anti-patterns: secrets in values, unpinned dependency versions, and skipping --atomic. Keep charts as simple as the use case allows; helm template to inspect rendered output.","correct":"Over-templating: cramming so much conditional logic and indirection into templates that the chart becomes unreadable and the rendered output is hard to predict — the opposite of Kustomize's transparency.","distractors":["Helm uses Go templating: values are substituted into templates to produce manifests, and it tracks releases for rollback.","A release is a named, versioned instance of a chart installed in the cluster.","Helm 3 removed Tiller — the in-cluster server component that was a major security concern (it held broad cluster permissions)."]},{"id":"devops/helm_and_package_management#d2a64349a475","section":"devops","module":"devops/helm_and_package_management","moduleName":"helm and package management","qIndex":10,"difficulty":"intermediate","question":"How do Helm/Kustomize fit into GitOps?","answerFull":"GitOps tools (ArgoCD/Flux) natively render Helm charts or Kustomize overlays from a Git repo and reconcile the cluster to match. A common split: third-party software via Helm charts (values in Git), first-party apps via Kustomize overlays. The Git repo is the source of truth and provides revisioning, so a git revert rolls back the change (see gitops_argocd_flux).","correct":"GitOps tools (ArgoCD/Flux) natively render Helm charts or Kustomize overlays from a Git repo and reconcile the cluster to match.","distractors":["Over-templating: cramming so much conditional logic and indirection into templates that the chart becomes unreadable and the rendered output is hard to predict — the opposite of Kustomize's transparency.","Helm uses Go templating: values are substituted into templates to produce manifests, and it tracks releases for rollback.","A release is a named, versioned instance of a chart installed in the cluster."]},{"id":"devops/incident_management_and_oncall#8af0e4d38cde","section":"devops","module":"devops/incident_management_and_oncall","moduleName":"incident management and oncall","qIndex":2,"difficulty":"core","question":"What is the role of an Incident Commander, and what should they NOT do?","answerFull":"The IC owns coordination and decision-making during an incident — declaring severity, assigning roles, deciding to escalate or page more people, and keeping the response organized so SMEs don't duplicate or conflict. Critically, the IC should NOT personally fix the bug, because diving into debugging means no one is coordinating, comms go dark, and decisions stall. The IC stays at the high-level view and delegates the technical work to subject-matter experts.","correct":"The IC owns coordination and decision-making during an incident — declaring severity, assigning roles, deciding to escalate or page more people, and keeping the response organized so SMEs don't duplicate or conflict.","distractors":["Give every action item a named owner, a due date, and a tracked ticket, and review overdue items in regular ops/SRE meetings so they don't silently rot — an untracked action item means the same incident recurs in months.","Severity drives it: a SEV1 needs proactive, regular external communication (status page updates every 15–30 minutes) plus internal exec updates, while a SEV3 may only need an internal channel note.","MTTD (mean time to detect) is from problem-start to someone-knowing, improved by better alerting — especially SLO burn-rate alerts that page on user-impacting symptoms quickly."]},{"id":"devops/incident_management_and_oncall#9252a097b141","section":"devops","module":"devops/incident_management_and_oncall","moduleName":"incident management and oncall","qIndex":4,"difficulty":"core","question":"What makes a postmortem blameless, and why does it matter?","answerFull":"A blameless postmortem analyzes the systemic and process gaps that allowed the failure — why the canary didn't catch it, why there was no auto-rollback — rather than blaming the person who triggered it. It matters because the moment people fear punishment they hide mistakes and withhold the information you need to learn, which guarantees the failure recurs; psychological safety is what makes honest, complete analysis possible. The output is concrete, owned action items, not a scapegoat.","correct":"A blameless postmortem analyzes the systemic and process gaps that allowed the failure — why the canary didn't catch it, why there was no auto-rollback — rather than blaming the person who triggered it.","distractors":["A runbook is a step-by-step guide for diagnosing and mitigating a specific alert — symptom, ordered diagnosis steps, mitigation actions, escalation criteria, and how to verify resolution.","Declaring early spins up the structure — IC, channel, roles, comms — while the problem is still small, so if it escalates you already have coordination in place instead of scrambling mid-crisis.","Give every action item a named owner, a due date, and a tracked ticket, and review overdue items in regular ops/SRE meetings so they don't silently rot — an untracked action item means the same incident recurs in months."]},{"id":"devops/incident_management_and_oncall#3ca96b5c2217","section":"devops","module":"devops/incident_management_and_oncall","moduleName":"incident management and oncall","qIndex":5,"difficulty":"core","question":"Explain MTTD vs MTTR and how you improve each.","answerFull":"MTTD (mean time to detect) is from problem-start to someone-knowing, improved by better alerting — especially SLO burn-rate alerts that page on user-impacting symptoms quickly. MTTR (mean time to resolve) is from detection to service-restored, improved by good runbooks, clear roles, automation (auto-rollback), and mitigate-first discipline. Both directly affect availability: cutting MTTR via a fast rollback can turn a budget-incinerating 40-minute outage into a 5-minute blip.","correct":"MTTD (mean time to detect) is from problem-start to someone-knowing, improved by better alerting — especially SLO burn-rate alerts that page on user-impacting symptoms quickly.","distractors":["Incident Commander (coordination/decisions), Operations/SMEs (technical fix), Communications Lead (stakeholder and status-page updates), and Scribe (timeline and decisions log).","Severity drives it: a SEV1 needs proactive, regular external communication (status page updates every 15–30 minutes) plus internal exec updates, while a SEV3 may only need an internal channel note.","Declaring early spins up the structure — IC, channel, roles, comms — while the problem is still small, so if it escalates you already have coordination in place instead of scrambling mid-crisis."]},{"id":"devops/incident_management_and_oncall#3acdbd6c1953","section":"devops","module":"devops/incident_management_and_oncall","moduleName":"incident management and oncall","qIndex":6,"difficulty":"intermediate","question":"What is a runbook and why is it the highest-leverage MTTR artifact?","answerFull":"A runbook is a step-by-step guide for diagnosing and mitigating a specific alert — symptom, ordered diagnosis steps, mitigation actions, escalation criteria, and how to verify resolution. It's the highest-leverage MTTR artifact because it turns a stressful 3am scramble (where a tired engineer has to reason from scratch) into following a checklist that a previous responder already worked out. Linking every alert annotation to its runbook means the responder is one click from \"here's exactly what to try first.\"","correct":"A runbook is a step-by-step guide for diagnosing and mitigating a specific alert — symptom, ordered diagnosis steps, mitigation actions, escalation criteria, and how to verify resolution.","distractors":["Use a primary plus secondary (the secondary is the escalation target and backup), with an escalation policy that pages the next responder if the primary doesn't ack within ~10 minutes, and a manager as the final tier.","A blameless postmortem analyzes the systemic and process gaps that allowed the failure — why the canary didn't catch it, why there was no auto-rollback — rather than blaming the person who triggered it.","Declaring early spins up the structure — IC, channel, roles, comms — while the problem is still small, so if it escalates you already have coordination in place instead of scrambling mid-crisis."]},{"id":"devops/incident_management_and_oncall#e59835358349","section":"devops","module":"devops/incident_management_and_oncall","moduleName":"incident management and oncall","qIndex":7,"difficulty":"intermediate","question":"How should on-call rotations be structured to stay sustainable?","answerFull":"Use a primary plus secondary (the secondary is the escalation target and backup), with an escalation policy that pages the next responder if the primary doesn't ack within ~10 minutes, and a manager as the final tier. Keep page volume low (fight alert fatigue so on-call isn't woken for non-actionable noise), rotate fairly (commonly weekly) with adequate compensation/time-off, and use follow-the-sun scheduling across time zones so engineers are paged during their workday rather than overnight. Sustainable on-call is a retention and reliability issue, not just a logistics one.","correct":"Use a primary plus secondary (the secondary is the escalation target and backup), with an escalation policy that pages the next responder if the primary doesn't ack within ~10 minutes, and a manager as the final tier.","distractors":["Severity drives it: a SEV1 needs proactive, regular external communication (status page updates every 15–30 minutes) plus internal exec updates, while a SEV3 may only need an internal channel note.","A runbook is a step-by-step guide for diagnosing and mitigating a specific alert — symptom, ordered diagnosis steps, mitigation actions, escalation criteria, and how to verify resolution.","A blameless postmortem analyzes the systemic and process gaps that allowed the failure — why the canary didn't catch it, why there was no auto-rollback — rather than blaming the person who triggered it."]},{"id":"devops/incident_management_and_oncall#f5c07085fe79","section":"devops","module":"devops/incident_management_and_oncall","moduleName":"incident management and oncall","qIndex":8,"difficulty":"intermediate","question":"What are the ICS roles and why separate them?","answerFull":"Incident Commander (coordination/decisions), Operations/SMEs (technical fix), Communications Lead (stakeholder and status-page updates), and Scribe (timeline and decisions log). You separate them so that under stress nobody is doing two jobs poorly — if the IC is also debugging, coordination collapses; if the engineer fixing the bug is also writing status updates, both suffer. For small incidents one person can hold multiple roles, but for SEV1/SEV2 the separation prevents the chaos of everyone doing everything.","correct":"Incident Commander (coordination/decisions), Operations/SMEs (technical fix), Communications Lead (stakeholder and status-page updates), and Scribe (timeline and decisions log).","distractors":["MTTD (mean time to detect) is from problem-start to someone-knowing, improved by better alerting — especially SLO burn-rate alerts that page on user-impacting symptoms quickly.","Give every action item a named owner, a due date, and a tracked ticket, and review overdue items in regular ops/SRE meetings so they don't silently rot — an untracked action item means the same incident recurs in months.","Declaring early spins up the structure — IC, channel, roles, comms — while the problem is still small, so if it escalates you already have coordination in place instead of scrambling mid-crisis."]},{"id":"devops/incident_management_and_oncall#b1dc7a358189","section":"devops","module":"devops/incident_management_and_oncall","moduleName":"incident management and oncall","qIndex":10,"difficulty":"intermediate","question":"How do you decide what to communicate during an incident, and to whom?","answerFull":"Severity drives it: a SEV1 needs proactive, regular external communication (status page updates every 15–30 minutes) plus internal exec updates, while a SEV3 may only need an internal channel note. The Communications Lead owns this so the engineers fixing the problem aren't distracted, and the principle is to over-communicate during high-severity incidents — customers and stakeholders trust transparency far more than silence. Always communicate impact and ETA-to-update (not necessarily ETA-to-fix, which you often don't know).","correct":"Severity drives it: a SEV1 needs proactive, regular external communication (status page updates every 15–30 minutes) plus internal exec updates, while a SEV3 may only need an internal channel note.","distractors":["Incident Commander (coordination/decisions), Operations/SMEs (technical fix), Communications Lead (stakeholder and status-page updates), and Scribe (timeline and decisions log).","Use a primary plus secondary (the secondary is the escalation target and backup), with an escalation policy that pages the next responder if the primary doesn't ack within ~10 minutes, and a manager as the final tier.","MTTD (mean time to detect) is from problem-start to someone-knowing, improved by better alerting — especially SLO burn-rate alerts that page on user-impacting symptoms quickly."]},{"id":"devops/incident_management_and_oncall#8fbf7c701eaa","section":"devops","module":"devops/incident_management_and_oncall","moduleName":"incident management and oncall","qIndex":11,"difficulty":"advanced","question":"What's the value of declaring an incident early versus waiting?","answerFull":"Declaring early spins up the structure — IC, channel, roles, comms — while the problem is still small, so if it escalates you already have coordination in place instead of scrambling mid-crisis. The cost of a \"false alarm\" declaration is low (you downgrade and close it), while the cost of declaring late is a disorganized response to a now-large incident. The guidance is to err toward declaring: it's cheaper to stand down an incident than to stand one up under fire.","correct":"Declaring early spins up the structure — IC, channel, roles, comms — while the problem is still small, so if it escalates you already have coordination in place instead of scrambling mid-crisis.","distractors":["Incident Commander (coordination/decisions), Operations/SMEs (technical fix), Communications Lead (stakeholder and status-page updates), and Scribe (timeline and decisions log).","Give every action item a named owner, a due date, and a tracked ticket, and review overdue items in regular ops/SRE meetings so they don't silently rot — an untracked action item means the same incident recurs in months.","MTTD (mean time to detect) is from problem-start to someone-knowing, improved by better alerting — especially SLO burn-rate alerts that page on user-impacting symptoms quickly."]},{"id":"devops/incident_management_and_oncall#03b68a1390d4","section":"devops","module":"devops/incident_management_and_oncall","moduleName":"incident management and oncall","qIndex":12,"difficulty":"advanced","question":"How do you ensure postmortem action items actually reduce future incidents?","answerFull":"Give every action item a named owner, a due date, and a tracked ticket, and review overdue items in regular ops/SRE meetings so they don't silently rot — an untracked action item means the same incident recurs in months. Prioritize them against the roadmap (the error-budget policy can force this after a budget-burning incident), and measure the loop: are repeat incidents declining? A postmortem without followed-through action items is theater; the action items are the entire point of doing the postmortem.","correct":"Give every action item a named owner, a due date, and a tracked ticket, and review overdue items in regular ops/SRE meetings so they don't silently rot — an untracked action item means the same incident recurs in months.","distractors":["Incident Commander (coordination/decisions), Operations/SMEs (technical fix), Communications Lead (stakeholder and status-page updates), and Scribe (timeline and decisions log).","Severity drives it: a SEV1 needs proactive, regular external communication (status page updates every 15–30 minutes) plus internal exec updates, while a SEV3 may only need an internal channel note.","A blameless postmortem analyzes the systemic and process gaps that allowed the failure — why the canary didn't catch it, why there was no auto-rollback — rather than blaming the person who triggered it."]},{"id":"devops/infrastructure_as_code_terraform#bf71ef9f3933","section":"devops","module":"devops/infrastructure_as_code_terraform","moduleName":"infrastructure as code terraform","qIndex":3,"difficulty":"core","question":"Why is remote state with locking necessary for teams?","answerFull":"Local state can't be safely shared, and concurrent applies against one state file corrupt it — two engineers each write a partial state, producing duplicated or orphaned resources. Remote backends (S3 + DynamoDB) serialize applies via a lock: the second apply waits until the first releases, and everyone reads the same authoritative state. This is the single most important step to move Terraform from solo use to team use.","correct":"Local state can't be safely shared, and concurrent applies against one state file corrupt it — two engineers each write a partial state, producing duplicated or orphaned resources.","distractors":["count creates resources indexed by integer ([0], [1]), so removing a middle element re-indexes the rest — Terraform destroys and recreates resources that merely shifted position.","Use the declarative import block (Terraform 1.5+): write an import { to = ..., id = ...","Any sensitive value passed to a resource is written unencrypted into the state JSON — sensitive = true only masks CLI output, not the stored value."]},{"id":"devops/infrastructure_as_code_terraform#ee79f62d7955","section":"devops","module":"devops/infrastructure_as_code_terraform","moduleName":"infrastructure as code terraform","qIndex":4,"difficulty":"core","question":"What's the difference between count and for_each, and which is safer?","answerFull":"count creates resources indexed by integer ([0], [1]), so removing a middle element re-indexes the rest — Terraform destroys and recreates resources that merely shifted position. for_each keys instances by a stable map/set key ([\"api\"], [\"worker\"]), so adding or removing one leaves the others untouched. Prefer for_each for anything where identities matter; reserve count for simple \"N identical copies\" or conditional creation (count = var.enabled ? 1 : 0).","correct":"count creates resources indexed by integer ([0], [1]), so removing a middle element re-indexes the rest — Terraform destroys and recreates resources that merely shifted position.","distractors":["state mv renames or moves a resource within state (e.g., after refactoring aws_instance.old to aws_instance.new) so Terraform updates the address instead of destroying and recreating the real resource.","Use the declarative import block (Terraform 1.5+): write an import { to = ..., id = ...","-/+ means Terraform will destroy the existing resource and create a new one because you changed an immutable attribute (an AMI, certain RDS engine settings, a resource name)."]},{"id":"devops/infrastructure_as_code_terraform#af54bf8d0801","section":"devops","module":"devops/infrastructure_as_code_terraform","moduleName":"infrastructure as code terraform","qIndex":5,"difficulty":"core","question":"What does it mean when a plan shows -/+ (replace), and why is it dangerous?","answerFull":"-/+ means Terraform will destroy the existing resource and create a new one because you changed an immutable attribute (an AMI, certain RDS engine settings, a resource name). For stateless resources it's fine; for stateful ones (databases, EBS volumes) it can mean data loss and downtime. Always scrutinize -/+ lines, protect critical resources with lifecycle { prevent_destroy = true }, and use create_before_destroy where a replacement must avoid a gap.","correct":"-/+ means Terraform will destroy the existing resource and create a new one because you changed an immutable attribute (an AMI, certain RDS engine settings, a resource name).","distractors":["Any sensitive value passed to a resource is written unencrypted into the state JSON — sensitive = true only masks CLI output, not the stored value.","Drift is when the real cloud resource diverges from what state/HCL describes — usually because someone changed it manually in the console.","Workspaces give one config multiple state files (terraform workspace select prod) — cheap and DRY, but they share the same backend and config, so a bad change can hit all environments and isolation is weak."]},{"id":"devops/infrastructure_as_code_terraform#e5868fec136f","section":"devops","module":"devops/infrastructure_as_code_terraform","moduleName":"infrastructure as code terraform","qIndex":6,"difficulty":"intermediate","question":"How do you handle secrets in Terraform given that state stores them in plaintext?","answerFull":"Any sensitive value passed to a resource is written unencrypted into the state JSON — sensitive = true only masks CLI output, not the stored value. So you encrypt state at rest (KMS on the S3 bucket), lock down bucket access, and never commit state to Git. Better, source secrets at apply time from an external store like Vault or AWS Secrets Manager (see secrets_management) so the secret of record lives outside Terraform, and use dynamic credentials where possible.","correct":"Any sensitive value passed to a resource is written unencrypted into the state JSON — sensitive = true only masks CLI output, not the stored value.","distractors":["Drift is when the real cloud resource diverges from what state/HCL describes — usually because someone changed it manually in the console.","Workspaces give one config multiple state files (terraform workspace select prod) — cheap and DRY, but they share the same backend and config, so a bad change can hit all environments and isolation is weak.","-/+ means Terraform will destroy the existing resource and create a new one because you changed an immutable attribute (an AMI, certain RDS engine settings, a resource name)."]},{"id":"devops/infrastructure_as_code_terraform#1b94f8131273","section":"devops","module":"devops/infrastructure_as_code_terraform","moduleName":"infrastructure as code terraform","qIndex":7,"difficulty":"intermediate","question":"What is drift and how do you detect and remediate it?","answerFull":"Drift is when the real cloud resource diverges from what state/HCL describes — usually because someone changed it manually in the console. Detect it with terraform plan -refresh-only (shows cloud-vs-state differences without config changes) or a nightly terraform plan -detailed-exitcode in CI (exit code 2 means drift). Remediate by either accepting the change into HCL (commit it) or running apply to revert the cloud back to the declared state — the discipline of \"change infra via PR, not console\" prevents it in the first place.","correct":"Drift is when the real cloud resource diverges from what state/HCL describes — usually because someone changed it manually in the console.","distractors":["Workspaces give one config multiple state files (terraform workspace select prod) — cheap and DRY, but they share the same backend and config, so a bad change can hit all environments and isolation is weak.","Any sensitive value passed to a resource is written unencrypted into the state JSON — sensitive = true only masks CLI output, not the stored value.","state mv renames or moves a resource within state (e.g., after refactoring aws_instance.old to aws_instance.new) so Terraform updates the address instead of destroying and recreating the real resource."]},{"id":"devops/infrastructure_as_code_terraform#0aab9700b471","section":"devops","module":"devops/infrastructure_as_code_terraform","moduleName":"infrastructure as code terraform","qIndex":8,"difficulty":"intermediate","question":"How do you bring an existing, manually-created resource under Terraform management?","answerFull":"Use the declarative import block (Terraform 1.5+): write an import { to = ..., id = ... } block and a matching resource block, then terraform plan -generate-config-out=... can scaffold the HCL and apply records it in state. The older imperative terraform import <addr> <id> still works but doesn't generate config. After import, run a plan and reconcile until it shows no changes — proving your HCL matches the live resource exactly.","correct":"Use the declarative import block (Terraform 1.5+): write an import { to = ..., id = ...","distractors":["Workspaces give one config multiple state files (terraform workspace select prod) — cheap and DRY, but they share the same backend and config, so a bad change can hit all environments and isolation is weak.","A module is a reusable, parameterized bundle of resources with variable inputs and output outputs — you write a VPC or EKS pattern once and instantiate it per environment.","In August 2023 HashiCorp relicensed Terraform from MPL 2.0 to the BUSL (a source-available, non-compete license), prompting the Linux Foundation-backed OpenTofu fork that keeps the MPL and is a drop-in CLI replacement."]},{"id":"devops/infrastructure_as_code_terraform#2fbbe8e8cc17","section":"devops","module":"devops/infrastructure_as_code_terraform","moduleName":"infrastructure as code terraform","qIndex":9,"difficulty":"intermediate","question":"What are Terraform modules and when should you write one?","answerFull":"A module is a reusable, parameterized bundle of resources with variable inputs and output outputs — you write a VPC or EKS pattern once and instantiate it per environment. Write a module when you'll repeat a pattern, want to enforce standards (tagging, encryption defaults), or need to hide complexity behind a clean interface. Keep modules focused and composable; avoid the \"one giant module that does everything,\" which becomes as hard to change as the infrastructure it replaced.","correct":"A module is a reusable, parameterized bundle of resources with variable inputs and output outputs — you write a VPC or EKS pattern once and instantiate it per environment.","distractors":["Any sensitive value passed to a resource is written unencrypted into the state JSON — sensitive = true only masks CLI output, not the stored value.","-/+ means Terraform will destroy the existing resource and create a new one because you changed an immutable attribute (an AMI, certain RDS engine settings, a resource name).","In August 2023 HashiCorp relicensed Terraform from MPL 2.0 to the BUSL (a source-available, non-compete license), prompting the Linux Foundation-backed OpenTofu fork that keeps the MPL and is a drop-in CLI replacement."]},{"id":"devops/infrastructure_as_code_terraform#b780ccfb530e","section":"devops","module":"devops/infrastructure_as_code_terraform","moduleName":"infrastructure as code terraform","qIndex":10,"difficulty":"intermediate","question":"Workspaces vs directory-per-environment — how do you isolate environments?","answerFull":"Workspaces give one config multiple state files (terraform workspace select prod) — cheap and DRY, but they share the same backend and config, so a bad change can hit all environments and isolation is weak. Directory-per-environment (envs/dev, envs/prod, each with its own backend, ideally separate AWS accounts) gives strong blast-radius isolation at the cost of some duplication. For serious prod/dev separation, separate accounts/backends win; workspaces suit ephemeral or similar-shaped environments.","correct":"Workspaces give one config multiple state files (terraform workspace select prod) — cheap and DRY, but they share the same backend and config, so a bad change can hit all environments and isolation is weak.","distractors":["Drift is when the real cloud resource diverges from what state/HCL describes — usually because someone changed it manually in the console.","A module is a reusable, parameterized bundle of resources with variable inputs and output outputs — you write a VPC or EKS pattern once and instantiate it per environment.","-/+ means Terraform will destroy the existing resource and create a new one because you changed an immutable attribute (an AMI, certain RDS engine settings, a resource name)."]},{"id":"devops/infrastructure_as_code_terraform#905f66591e83","section":"devops","module":"devops/infrastructure_as_code_terraform","moduleName":"infrastructure as code terraform","qIndex":12,"difficulty":"advanced","question":"What's the purpose of .terraform.lock.hcl and should you commit it?","answerFull":"It's the dependency lockfile recording the exact provider versions and their checksums that were selected, analogous to package-lock.json. Commit it so every engineer and CI runner resolves identical provider versions and gets byte-identical plans — without it, a >= 5.0 constraint could silently pull a newer provider that changes defaults and proposes surprise diffs. Update it deliberately with terraform init -upgrade and review the resulting plan.","correct":"It's the dependency lockfile recording the exact provider versions and their checksums that were selected, analogous to package-lock.json.","distractors":["Any sensitive value passed to a resource is written unencrypted into the state JSON — sensitive = true only masks CLI output, not the stored value.","Drift is when the real cloud resource diverges from what state/HCL describes — usually because someone changed it manually in the console.","count creates resources indexed by integer ([0], [1]), so removing a middle element re-indexes the rest — Terraform destroys and recreates resources that merely shifted position."]},{"id":"devops/infrastructure_as_code_terraform#b362d982fa26","section":"devops","module":"devops/infrastructure_as_code_terraform","moduleName":"infrastructure as code terraform","qIndex":13,"difficulty":"advanced","question":"How do terraform state mv and terraform state rm differ, and when do you use them?","answerFull":"state mv renames or moves a resource within state (e.g., after refactoring aws_instance.old to aws_instance.new) so Terraform updates the address instead of destroying and recreating the real resource. state rm removes a resource from state without deleting it in the cloud — used to hand a resource off to another config or stop managing it. Both are state surgery: always terraform state pull > backup.tfstate first, because a mistake can orphan or duplicate real infrastructure.","correct":"state mv renames or moves a resource within state (e.g., after refactoring aws_instance.old to aws_instance.new) so Terraform updates the address instead of destroying and recreating the real resource.","distractors":["Use the declarative import block (Terraform 1.5+): write an import { to = ..., id = ...","Drift is when the real cloud resource diverges from what state/HCL describes — usually because someone changed it manually in the console.","Local state can't be safely shared, and concurrent applies against one state file corrupt it — two engineers each write a partial state, producing duplicated or orphaned resources."]},{"id":"devops/infrastructure_as_code_terraform#ceadcb52af4b","section":"devops","module":"devops/infrastructure_as_code_terraform","moduleName":"infrastructure as code terraform","qIndex":15,"difficulty":"advanced","question":"What changed with the Terraform-to-OpenTofu license fork, and does it affect your code?","answerFull":"In August 2023 HashiCorp relicensed Terraform from MPL 2.0 to the BUSL (a source-available, non-compete license), prompting the Linux Foundation-backed OpenTofu fork that keeps the MPL and is a drop-in CLI replacement. Your HCL, providers, and workflow are largely identical, so migration is mostly swapping the binary and re-running init; the divergence to watch is newer features landing differently in each. Choose based on licensing comfort and the specific features (e.g., state encryption, early variable evaluation) each ships — covered in terraform_advanced_and_alternatives.","correct":"In August 2023 HashiCorp relicensed Terraform from MPL 2.0 to the BUSL (a source-available, non-compete license), prompting the Linux Foundation-backed OpenTofu fork that keeps the MPL and is a drop-in CLI replacement.","distractors":["Use the declarative import block (Terraform 1.5+): write an import { to = ..., id = ...","Any sensitive value passed to a resource is written unencrypted into the state JSON — sensitive = true only masks CLI output, not the stored value.","count creates resources indexed by integer ([0], [1]), so removing a middle element re-indexes the rest — Terraform destroys and recreates resources that merely shifted position."]},{"id":"devops/kubernetes_architecture#411ca743c9ba","section":"devops","module":"devops/kubernetes_architecture","moduleName":"kubernetes architecture","qIndex":1,"difficulty":"core","question":"Describe the Kubernetes control plane components and their roles.","answerFull":"The API server is the front door and sole hub — it authenticates, authorizes, admits, validates, and persists every change, and is the only component that talks to etcd. etcd is the Raft-based key-value store holding all cluster state (the source of truth). The scheduler binds unscheduled pods to nodes. The controller-manager runs the reconciliation loops (Deployment, ReplicaSet, Node, etc.). The cloud-controller-manager integrates with the cloud (LBs, volumes, node lifecycle).","correct":"The API server is the front door and sole hub — it authenticates, authorizes, admits, validates, and persists every change, and is the only component that talks to etcd.","distractors":["Managed (EKS/GKE/AKS) offloads API server + etcd operation, scaling, patching, and backups to the cloud, removing the riskiest ops burden, at a per-cluster fee and with less control over control-plane internals/versions.","etcd needs a majority quorum to commit writes (Raft).","It centralizes authentication, authorization, admission, validation, and auditing in one place, and gives every component a consistent, watchable view of state via etcd."]},{"id":"devops/kubernetes_architecture#44484e9fcb0f","section":"devops","module":"devops/kubernetes_architecture","moduleName":"kubernetes architecture","qIndex":2,"difficulty":"core","question":"Walk through exactly what happens when you kubectl apply a Deployment with 3 replicas.","answerFull":"kubectl POSTs to the API server, which runs authn → authz (RBAC) → mutating/validating admission → schema validation, then writes the Deployment to etcd. The Deployment controller observes it and creates a ReplicaSet; the ReplicaSet controller sees desired=3, actual=0 and creates 3 Pod objects (Pending). The scheduler picks nodes and writes each Pod's nodeName. Each node's kubelet sees its bound Pod, calls the CRI runtime to pull the image and start the container, then reports status; the EndpointSlice controller adds ready pod IPs to the Service.","correct":"kubectl POSTs to the API server, which runs authn → authz (RBAC) → mutating/validating admission → schema validation, then writes the Deployment to etcd.","distractors":["Managed (EKS/GKE/AKS) offloads API server + etcd operation, scaling, patching, and backups to the cloud, removing the riskiest ops burden, at a per-cluster fee and with less control over control-plane internals/versions.","etcd needs a majority quorum to commit writes (Raft).","Declarative means you specify the desired end state (spec), not the steps to get there; controllers compute and apply the diff."]},{"id":"devops/kubernetes_architecture#6e65aabf2511","section":"devops","module":"devops/kubernetes_architecture","moduleName":"kubernetes architecture","qIndex":3,"difficulty":"core","question":"What does \"declarative\" and \"level-triggered\" mean, and why does it matter?","answerFull":"Declarative means you specify the desired end state (spec), not the steps to get there; controllers compute and apply the diff. Level-triggered means controllers act on the *current* state rather than reacting to a specific event. Together they make the system self-healing: a controller that crashes, misses an event, or restarts simply re-reconciles current vs desired on its next pass and converges anyway — no lost-event fragility.","correct":"Declarative means you specify the desired end state (spec), not the steps to get there; controllers compute and apply the diff.","distractors":["kube-proxy programs node-level networking so that a Service's stable virtual IP load-balances to the current set of backing pod IPs.","It centralizes authentication, authorization, admission, validation, and auditing in one place, and gives every component a consistent, watchable view of state via etcd.","etcd is the strongly-consistent, Raft-based key-value store holding all cluster state."]},{"id":"devops/kubernetes_architecture#44b08a587d3c","section":"devops","module":"devops/kubernetes_architecture","moduleName":"kubernetes architecture","qIndex":4,"difficulty":"core","question":"Why do components communicate only through the API server?","answerFull":"It centralizes authentication, authorization, admission, validation, and auditing in one place, and gives every component a consistent, watchable view of state via etcd. Direct component-to-component calls would fragment security and consistency. The cost is that the API server (and etcd) is the scalability and reliability bottleneck — which is why protecting it (rate limits, efficient watches) is critical.","correct":"It centralizes authentication, authorization, admission, validation, and auditing in one place, and gives every component a consistent, watchable view of state via etcd.","distractors":["kubectl POSTs to the API server, which runs authn → authz (RBAC) → mutating/validating admission → schema validation, then writes the Deployment to etcd.","spec is the user's declared desired state (e.g., replicas: 3); status is the system's observed actual state (e.g., readyReplicas: 2), written by controllers.","The API server is the front door and sole hub — it authenticates, authorizes, admits, validates, and persists every change, and is the only component that talks to etcd."]},{"id":"devops/kubernetes_architecture#276a5f7fa567","section":"devops","module":"devops/kubernetes_architecture","moduleName":"kubernetes architecture","qIndex":5,"difficulty":"core","question":"What is etcd, and what happens if it fails or fills up?","answerFull":"etcd is the strongly-consistent, Raft-based key-value store holding all cluster state. Lose it without a backup and you lose the cluster's desired state (Deployments, Secrets, everything). It has a default DB size quota (~8 GB); exceeding it raises a NOSPACE alarm and puts etcd read-only, so the cluster stops accepting writes — including self-healing — until you compact/defragment and clear the alarm. Hence: HA (3/5 nodes), scheduled snapshots, tested restores, and size monitoring.","correct":"etcd is the strongly-consistent, Raft-based key-value store holding all cluster state.","distractors":["Managed (EKS/GKE/AKS) offloads API server + etcd operation, scaling, patching, and backups to the cloud, removing the riskiest ops burden, at a per-cluster fee and with less control over control-plane internals/versions.","Declarative means you specify the desired end state (spec), not the steps to get there; controllers compute and apply the diff.","The API server is the front door and sole hub — it authenticates, authorizes, admits, validates, and persists every change, and is the only component that talks to etcd."]},{"id":"devops/kubernetes_architecture#2da24bee9988","section":"devops","module":"devops/kubernetes_architecture","moduleName":"kubernetes architecture","qIndex":6,"difficulty":"intermediate","question":"Why 3 or 5 etcd nodes and not 2 or 4?","answerFull":"etcd needs a majority quorum to commit writes (Raft). With N nodes, quorum is ⌊N/2⌋+1: 3 → quorum 2 (tolerates 1 failure), 5 → quorum 3 (tolerates 2). An even count adds no extra fault tolerance (4 still only tolerates 1, same as 3) while adding write latency and split-vote risk — so you always use odd numbers.","correct":"etcd needs a majority quorum to commit writes (Raft).","distractors":["The API server is the front door and sole hub — it authenticates, authorizes, admits, validates, and persists every change, and is the only component that talks to etcd.","It centralizes authentication, authorization, admission, validation, and auditing in one place, and gives every component a consistent, watchable view of state via etcd.","kubectl POSTs to the API server, which runs authn → authz (RBAC) → mutating/validating admission → schema validation, then writes the Deployment to etcd."]},{"id":"devops/kubernetes_architecture#c5bf7e2ca02c","section":"devops","module":"devops/kubernetes_architecture","moduleName":"kubernetes architecture","qIndex":7,"difficulty":"intermediate","question":"What's the difference between spec and status on an object?","answerFull":"spec is the user's declared desired state (e.g., replicas: 3); status is the system's observed actual state (e.g., readyReplicas: 2), written by controllers. The entire job of a controller is to drive status toward spec. When you debug \"why isn't this happening,\" you compare the two and find which controller isn't closing the gap.","correct":"spec is the user's declared desired state (e.g., replicas: 3); status is the system's observed actual state (e.g., readyReplicas: 2), written by controllers.","distractors":["etcd is the strongly-consistent, Raft-based key-value store holding all cluster state.","Declarative means you specify the desired end state (spec), not the steps to get there; controllers compute and apply the diff.","It centralizes authentication, authorization, admission, validation, and auditing in one place, and gives every component a consistent, watchable view of state via etcd."]},{"id":"devops/kubernetes_architecture#c7bc8657da21","section":"devops","module":"devops/kubernetes_architecture","moduleName":"kubernetes architecture","qIndex":8,"difficulty":"intermediate","question":"How does the kubelet fit in — does it talk to etcd?","answerFull":"No. The kubelet watches the API server for Pods bound to its node, then uses the CRI to pull images and start/stop containers via the runtime, runs probes, and reports Pod status back to the API server. It never touches etcd directly — only the API server does. It's the node-level reconciler ensuring its node's actual containers match the PodSpecs assigned to it.","correct":"No. The kubelet watches the API server for Pods bound to its node, then uses the CRI to pull images and start/stop containers via the runtime, runs probes, and reports Pod status back to the API server.","distractors":["etcd is the strongly-consistent, Raft-based key-value store holding all cluster state.","The API server is the front door and sole hub — it authenticates, authorizes, admits, validates, and persists every change, and is the only component that talks to etcd.","The Node controller stops receiving heartbeats, marks the node NotReady after a grace period, and (after the pod-eviction timeout) the pods on it are marked for deletion."]},{"id":"devops/kubernetes_architecture#a34831dbb091","section":"devops","module":"devops/kubernetes_architecture","moduleName":"kubernetes architecture","qIndex":9,"difficulty":"intermediate","question":"What does kube-proxy do?","answerFull":"kube-proxy programs node-level networking so that a Service's stable virtual IP load-balances to the current set of backing pod IPs. It watches Services/EndpointSlices and installs rules via iptables (default), IPVS (better at large Service counts), or is replaced entirely by eBPF dataplanes (Cilium). It implements Service routing; it does not proxy most traffic itself in iptables/IPVS modes (the kernel does).","correct":"kube-proxy programs node-level networking so that a Service's stable virtual IP load-balances to the current set of backing pod IPs.","distractors":["The Node controller stops receiving heartbeats, marks the node NotReady after a grace period, and (after the pod-eviction timeout) the pods on it are marked for deletion.","Declarative means you specify the desired end state (spec), not the steps to get there; controllers compute and apply the diff.","It centralizes authentication, authorization, admission, validation, and auditing in one place, and gives every component a consistent, watchable view of state via etcd."]},{"id":"devops/kubernetes_architecture#e1266d178a48","section":"devops","module":"devops/kubernetes_architecture","moduleName":"kubernetes architecture","qIndex":12,"difficulty":"advanced","question":"Managed vs self-managed control plane — tradeoffs?","answerFull":"Managed (EKS/GKE/AKS) offloads API server + etcd operation, scaling, patching, and backups to the cloud, removing the riskiest ops burden, at a per-cluster fee and with less control over control-plane internals/versions. Self-managed (kubeadm) gives full control and avoids the fee but makes you responsible for etcd HA/backups, upgrades, and availability — the failure modes most teams shouldn't own.","correct":"Managed (EKS/GKE/AKS) offloads API server + etcd operation, scaling, patching, and backups to the cloud, removing the riskiest ops burden, at a per-cluster fee and with less control over control-plane internals/versions.","distractors":["kubectl POSTs to the API server, which runs authn → authz (RBAC) → mutating/validating admission → schema validation, then writes the Deployment to etcd.","The API server is the front door and sole hub — it authenticates, authorizes, admits, validates, and persists every change, and is the only component that talks to etcd.","etcd is the strongly-consistent, Raft-based key-value store holding all cluster state."]},{"id":"devops/kubernetes_architecture#704b4c384c86","section":"devops","module":"devops/kubernetes_architecture","moduleName":"kubernetes architecture","qIndex":14,"difficulty":"advanced","question":"How does Kubernetes self-heal when a node dies?","answerFull":"The Node controller stops receiving heartbeats, marks the node NotReady after a grace period, and (after the pod-eviction timeout) the pods on it are marked for deletion. Their owning controllers (ReplicaSet/StatefulSet) observe replicas below desired and create replacement pods, which the scheduler places on healthy nodes. The level-triggered loops make recovery automatic — no human action needed.","correct":"The Node controller stops receiving heartbeats, marks the node NotReady after a grace period, and (after the pod-eviction timeout) the pods on it are marked for deletion.","distractors":["kube-proxy programs node-level networking so that a Service's stable virtual IP load-balances to the current set of backing pod IPs.","Declarative means you specify the desired end state (spec), not the steps to get there; controllers compute and apply the diff.","It centralizes authentication, authorization, admission, validation, and auditing in one place, and gives every component a consistent, watchable view of state via etcd."]},{"id":"devops/kubernetes_networking#942a4545677c","section":"devops","module":"devops/kubernetes_networking","moduleName":"kubernetes networking","qIndex":1,"difficulty":"core","question":"What are the rules of the Kubernetes network model?","answerFull":"Every Pod gets its own IP; all Pods can communicate with all other Pods across nodes without NAT; nodes can reach all Pods without NAT; and a Pod sees its own IP as the same one others use to reach it. This flat model is implemented by the CNI plugin and is what lets Services and DNS work uniformly regardless of which node a Pod lands on.","correct":"Every Pod gets its own IP; all Pods can communicate with all other Pods across nodes without NAT; nodes can reach all Pods without NAT; and a Pod sees its own IP as the same one others use to reach it.","distractors":["It assigns each Pod a real IP from the VPC subnet (via ENIs), so Pod IPs consume subnet address space directly.","A headless Service (clusterIP: None) has no virtual IP; DNS returns the individual Pod IPs instead.","kube-proxy watches Services and EndpointSlices and programs the node's dataplane so Service VIPs route to ready Pod IPs."]},{"id":"devops/kubernetes_networking#413c966bd720","section":"devops","module":"devops/kubernetes_networking","moduleName":"kubernetes networking","qIndex":2,"difficulty":"core","question":"What is a ClusterIP, and what's actually listening on it?","answerFull":"A ClusterIP is a *virtual* IP for a Service — no machine or NIC holds it. kube-proxy installs iptables/IPVS rules so packets destined for the ClusterIP are DNAT'd to one of the Service's ready Pod IPs. Nothing listens on the ClusterIP itself; the kernel rewrites the destination. Understanding this resolves most \"I can't reach my Service\" confusion.","correct":"A ClusterIP is a *virtual* IP for a Service — no machine or NIC holds it.","distractors":["A headless Service (clusterIP: None) has no virtual IP; DNS returns the individual Pod IPs instead.","Check the EndpointSlices: kubectl get endpointslices -l kubernetes.io/service-name=<svc>.","Cilium uses eBPF programs attached to kernel hooks to implement Service load balancing, NetworkPolicy, and observability directly in the kernel datapath — bypassing iptables entirely (it can replace kube-proxy)."]},{"id":"devops/kubernetes_networking#ec857e58cb7e","section":"devops","module":"devops/kubernetes_networking","moduleName":"kubernetes networking","qIndex":3,"difficulty":"core","question":"What does the CNI do?","answerFull":"The Container Network Interface plugin is invoked by the kubelet when a Pod is created to allocate the Pod's IP and wire up connectivity (routes, overlay/VXLAN, or BGP) so the network-model rules hold. Different CNIs implement it differently — AWS VPC CNI hands out real VPC IPs, Calico uses BGP/overlay, Cilium uses eBPF — and some (Calico/Cilium) also enforce NetworkPolicy.","correct":"The Container Network Interface plugin is invoked by the kubelet when a Pod is created to allocate the Pod's IP and wire up connectivity (routes, overlay/VXLAN, or BGP) so the network-model rules hold.","distractors":["A headless Service (clusterIP: None) has no virtual IP; DNS returns the individual Pod IPs instead.","kube-proxy watches Services and EndpointSlices and programs the node's dataplane so Service VIPs route to ready Pod IPs.","Every Pod gets its own IP; all Pods can communicate with all other Pods across nodes without NAT; nodes can reach all Pods without NAT; and a Pod sees its own IP as the same one others use to reach it."]},{"id":"devops/kubernetes_networking#defe5415ea7b","section":"devops","module":"devops/kubernetes_networking","moduleName":"kubernetes networking","qIndex":4,"difficulty":"core","question":"How does kube-proxy work, and what are its modes?","answerFull":"kube-proxy watches Services and EndpointSlices and programs the node's dataplane so Service VIPs route to ready Pod IPs. In iptables mode (default) it installs chains the kernel evaluates; in IPVS mode it uses a kernel hash-table load balancer (better at thousands of Services); with Cilium/eBPF, kube-proxy is replaced by eBPF programs entirely. In iptables/IPVS modes it doesn't proxy bytes itself — the kernel forwards.","correct":"kube-proxy watches Services and EndpointSlices and programs the node's dataplane so Service VIPs route to ready Pod IPs.","distractors":["A headless Service (clusterIP: None) has no virtual IP; DNS returns the individual Pod IPs instead.","Every Pod gets its own IP; all Pods can communicate with all other Pods across nodes without NAT; nodes can reach all Pods without NAT; and a Pod sees its own IP as the same one others use to reach it.","By default all Pod-to-Pod traffic is allowed."]},{"id":"devops/kubernetes_networking#5109e1177fcf","section":"devops","module":"devops/kubernetes_networking","moduleName":"kubernetes networking","qIndex":6,"difficulty":"intermediate","question":"How does service discovery / DNS work in Kubernetes?","answerFull":"CoreDNS runs as a Service and resolves names like orders.default.svc.cluster.local to the Service's ClusterIP (and headless Services to individual Pod IPs). Pods get /etc/resolv.conf pointing at CoreDNS with a search list and ndots:5. Apps call Services by name; CoreDNS returns the stable VIP, decoupling clients from churning Pod IPs.","correct":"CoreDNS runs as a Service and resolves names like orders.default.svc.cluster.local to the Service's ClusterIP (and headless Services to individual Pod IPs).","distractors":["As new Pods pass readiness, the EndpointSlice controller adds their IPs to the Service's endpoints; as old Pods are terminated and fail readiness/are deleted, their IPs are removed.","Check the EndpointSlices: kubectl get endpointslices -l kubernetes.io/service-name=<svc>.","kube-proxy watches Services and EndpointSlices and programs the node's dataplane so Service VIPs route to ready Pod IPs."]},{"id":"devops/kubernetes_networking#ae55abe3beac","section":"devops","module":"devops/kubernetes_networking","moduleName":"kubernetes networking","qIndex":7,"difficulty":"intermediate","question":"Explain the ndots:5 problem.","answerFull":"ndots:5 means any name with fewer than 5 dots is tried against each search domain before being queried as-is. So api.vendor.com (2 dots) triggers 4 lookups (3 failed search-domain attempts + the real one). Across many requests this multiplies CoreDNS load and adds latency. Fixes: trailing-dot FQDNs, NodeLocal DNSCache, or lowering ndots via Pod dnsConfig.","correct":"ndots:5 means any name with fewer than 5 dots is tried against each search domain before being queried as-is.","distractors":["Check the EndpointSlices: kubectl get endpointslices -l kubernetes.io/service-name=<svc>.","A ClusterIP is a *virtual* IP for a Service — no machine or NIC holds it.","A LoadBalancer Service provisions one cloud LB per Service (costly, L4)."]},{"id":"devops/kubernetes_networking#54b75b962f56","section":"devops","module":"devops/kubernetes_networking","moduleName":"kubernetes networking","qIndex":8,"difficulty":"intermediate","question":"A Service exists but clients get \"connection refused.\" How do you debug?","answerFull":"Check the EndpointSlices: kubectl get endpointslices -l kubernetes.io/service-name=<svc>. If empty, either the Service selector doesn't match any Pod labels, or no Pod is passing its readiness probe (only ready Pods become endpoints). Verify Pod labels match the selector and that readiness succeeds. If endpoints exist but it still fails, check NetworkPolicy and the targetPort.","correct":"Check the EndpointSlices: kubectl get endpointslices -l kubernetes.io/service-name=<svc>.","distractors":["Cilium uses eBPF programs attached to kernel hooks to implement Service load balancing, NetworkPolicy, and observability directly in the kernel datapath — bypassing iptables entirely (it can replace kube-proxy).","As new Pods pass readiness, the EndpointSlice controller adds their IPs to the Service's endpoints; as old Pods are terminated and fail readiness/are deleted, their IPs are removed.","A headless Service (clusterIP: None) has no virtual IP; DNS returns the individual Pod IPs instead."]},{"id":"devops/kubernetes_networking#5fa7d5a9b156","section":"devops","module":"devops/kubernetes_networking","moduleName":"kubernetes networking","qIndex":9,"difficulty":"intermediate","question":"How does NetworkPolicy work and what's the default?","answerFull":"By default all Pod-to-Pod traffic is allowed. The moment a NetworkPolicy selects a Pod for a direction (Ingress/Egress), that direction becomes default-deny for that Pod, and only the policy's explicit from/to rules are permitted. Policies are additive (union of allows). Crucially, NetworkPolicy is only enforced if the CNI supports it — Flannel doesn't, AWS VPC CNI needs an add-on.","correct":"By default all Pod-to-Pod traffic is allowed.","distractors":["It assigns each Pod a real IP from the VPC subnet (via ENIs), so Pod IPs consume subnet address space directly.","A headless Service (clusterIP: None) has no virtual IP; DNS returns the individual Pod IPs instead.","CoreDNS runs as a Service and resolves names like orders.default.svc.cluster.local to the Service's ClusterIP (and headless Services to individual Pod IPs)."]},{"id":"devops/kubernetes_networking#07817f670c3f","section":"devops","module":"devops/kubernetes_networking","moduleName":"kubernetes networking","qIndex":10,"difficulty":"intermediate","question":"Ingress vs LoadBalancer Service vs Gateway API?","answerFull":"A LoadBalancer Service provisions one cloud LB per Service (costly, L4). Ingress lets one ingress controller (behind a single LB) route HTTP by host/path to many Services with TLS termination — far cheaper and L7-aware. Gateway API is the newer, more expressive successor (role-separated Gateway/HTTPRoute, native traffic splitting and header matching), addressing Ingress's annotation-driven limitations.","correct":"A LoadBalancer Service provisions one cloud LB per Service (costly, L4).","distractors":["As new Pods pass readiness, the EndpointSlice controller adds their IPs to the Service's endpoints; as old Pods are terminated and fail readiness/are deleted, their IPs are removed.","A headless Service (clusterIP: None) has no virtual IP; DNS returns the individual Pod IPs instead.","A ClusterIP is a *virtual* IP for a Service — no machine or NIC holds it."]},{"id":"devops/kubernetes_networking#0260b7d8f733","section":"devops","module":"devops/kubernetes_networking","moduleName":"kubernetes networking","qIndex":11,"difficulty":"advanced","question":"Why does AWS VPC CNI risk IP exhaustion?","answerFull":"It assigns each Pod a real IP from the VPC subnet (via ENIs), so Pod IPs consume subnet address space directly. A /24 subnet (~250 usable IPs) supports only ~250 Pods regardless of node count, so dense clusters exhaust subnets and Pods get stuck Pending with IP-allocation errors. Mitigations: larger/secondary CIDRs, prefix delegation (assign /28 prefixes per ENI), or an overlay CNI.","correct":"It assigns each Pod a real IP from the VPC subnet (via ENIs), so Pod IPs consume subnet address space directly.","distractors":["The Container Network Interface plugin is invoked by the kubelet when a Pod is created to allocate the Pod's IP and wire up connectivity (routes, overlay/VXLAN, or BGP) so the network-model rules hold.","kube-proxy watches Services and EndpointSlices and programs the node's dataplane so Service VIPs route to ready Pod IPs.","Every Pod gets its own IP; all Pods can communicate with all other Pods across nodes without NAT; nodes can reach all Pods without NAT; and a Pod sees its own IP as the same one others use to reach it."]},{"id":"devops/kubernetes_networking#5005fc688740","section":"devops","module":"devops/kubernetes_networking","moduleName":"kubernetes networking","qIndex":12,"difficulty":"advanced","question":"What happens to a Service's traffic during a rolling update?","answerFull":"As new Pods pass readiness, the EndpointSlice controller adds their IPs to the Service's endpoints; as old Pods are terminated and fail readiness/are deleted, their IPs are removed. kube-proxy updates the dataplane accordingly. With readiness probes and maxUnavailable: 0, only ready Pods ever receive traffic, so the Service load-balances seamlessly across the transition.","correct":"As new Pods pass readiness, the EndpointSlice controller adds their IPs to the Service's endpoints; as old Pods are terminated and fail readiness/are deleted, their IPs are removed.","distractors":["By default all Pod-to-Pod traffic is allowed.","kube-proxy watches Services and EndpointSlices and programs the node's dataplane so Service VIPs route to ready Pod IPs.","A LoadBalancer Service provisions one cloud LB per Service (costly, L4)."]},{"id":"devops/kubernetes_networking#23afe409aa91","section":"devops","module":"devops/kubernetes_networking","moduleName":"kubernetes networking","qIndex":14,"difficulty":"advanced","question":"What is a headless Service and when do you need it?","answerFull":"A headless Service (clusterIP: None) has no virtual IP; DNS returns the individual Pod IPs instead. It's used by StatefulSets so each Pod has a stable, directly-addressable DNS name (db-0.db.ns.svc), which clustered systems need to reach specific members (e.g., a database primary) rather than load-balancing blindly across all replicas.","correct":"A headless Service (clusterIP: None) has no virtual IP; DNS returns the individual Pod IPs instead.","distractors":["The Container Network Interface plugin is invoked by the kubelet when a Pod is created to allocate the Pod's IP and wire up connectivity (routes, overlay/VXLAN, or BGP) so the network-model rules hold.","Every Pod gets its own IP; all Pods can communicate with all other Pods across nodes without NAT; nodes can reach all Pods without NAT; and a Pod sees its own IP as the same one others use to reach it.","By default all Pod-to-Pod traffic is allowed."]},{"id":"devops/kubernetes_networking#bcd0e5dce8b6","section":"devops","module":"devops/kubernetes_networking","moduleName":"kubernetes networking","qIndex":15,"difficulty":"advanced","question":"How does eBPF (Cilium) change the dataplane?","answerFull":"Cilium uses eBPF programs attached to kernel hooks to implement Service load balancing, NetworkPolicy, and observability directly in the kernel datapath — bypassing iptables entirely (it can replace kube-proxy). This scales far better at high Service/endpoint counts (no linear iptables chains), enables identity-based policy and L7 visibility (Hubble), and reduces latency from rule traversal.","correct":"Cilium uses eBPF programs attached to kernel hooks to implement Service load balancing, NetworkPolicy, and observability directly in the kernel datapath — bypassing iptables entirely (it can replace kube-proxy).","distractors":["CoreDNS runs as a Service and resolves names like orders.default.svc.cluster.local to the Service's ClusterIP (and headless Services to individual Pod IPs).","A ClusterIP is a *virtual* IP for a Service — no machine or NIC holds it.","Check the EndpointSlices: kubectl get endpointslices -l kubernetes.io/service-name=<svc>."]},{"id":"devops/kubernetes_operators_and_crds#e97e65653818","section":"devops","module":"devops/kubernetes_operators_and_crds","moduleName":"kubernetes operators and crds","qIndex":2,"difficulty":"core","question":"What is the operator pattern?","answerFull":"It's encoding operational/domain expertise as a custom controller that watches custom resources and reconciles them to desired state — applying Kubernetes' \"declare + reconcile\" model to complex software. Instead of an SRE manually backing up, failing over, and upgrading a database, an operator does it automatically in response to a declarative CR. It captures day-2 operations as code.","correct":"It's encoding operational/domain expertise as a custom controller that watches custom resources and reconciles them to desired state — applying Kubernetes' \"declare + reconcile\" model to complex software.","distractors":["Use a mature community operator (Prometheus Operator, cert-manager, CloudNativePG) whenever one fits — operators are subtle (idempotency, finalizers, status, leader election, edge cases) and a buggy one can destroy data.","Because the loop runs repeatedly — on every relevant event and on periodic resyncs — and may re-run after a controller restart.","When you run multiple operator replicas for availability, only one should actively reconcile at a time — otherwise two controllers fight over the same resources."]},{"id":"devops/kubernetes_operators_and_crds#aa00f9287fcb","section":"devops","module":"devops/kubernetes_operators_and_crds","moduleName":"kubernetes operators and crds","qIndex":4,"difficulty":"core","question":"Why must reconciliation be idempotent?","answerFull":"Because the loop runs repeatedly — on every relevant event and on periodic resyncs — and may re-run after a controller restart. If reconciliation has non-idempotent side effects (e.g., unconditionally creating a child object), you get duplicates and drift. Idempotent reconciliation (create-or-update, skip-if-already-correct) means running it once or a hundred times converges to the same correct state.","correct":"Because the loop runs repeatedly — on every relevant event and on periodic resyncs — and may re-run after a controller restart.","distractors":["When you run multiple operator replicas for availability, only one should actively reconcile at a time — otherwise two controllers fight over the same resources.","It's encoding operational/domain expertise as a custom controller that watches custom resources and reconciles them to desired state — applying Kubernetes' \"declare + reconcile\" model to complex software.","Edge-triggered reacts to specific events (the \"create\" happened); level-triggered acts on the current observed state regardless of which event led there."]},{"id":"devops/kubernetes_operators_and_crds#984e2157218b","section":"devops","module":"devops/kubernetes_operators_and_crds","moduleName":"kubernetes operators and crds","qIndex":5,"difficulty":"core","question":"What is level-triggered vs edge-triggered, and why does it matter for operators?","answerFull":"Edge-triggered reacts to specific events (the \"create\" happened); level-triggered acts on the current observed state regardless of which event led there. Operators must be level-triggered: a controller restart replays no events, and watch streams can drop events, so reconciling from current state (with periodic resync) is the only way to reliably converge and self-heal. Edge-triggered operators silently leave resources unmanaged after restarts.","correct":"Edge-triggered reacts to specific events (the \"create\" happened); level-triggered acts on the current observed state regardless of which event led there.","distractors":["It's encoding operational/domain expertise as a custom controller that watches custom resources and reconciles them to desired state — applying Kubernetes' \"declare + reconcile\" model to complex software.","Use a mature community operator (Prometheus Operator, cert-manager, CloudNativePG) whenever one fits — operators are subtle (idempotency, finalizers, status, leader election, edge cases) and a buggy one can destroy data.","By updating the CR's status subresource (e.g., status.phase: Ready, conditions, observed replicas)."]},{"id":"devops/kubernetes_operators_and_crds#5ecbed324b57","section":"devops","module":"devops/kubernetes_operators_and_crds","moduleName":"kubernetes operators and crds","qIndex":6,"difficulty":"intermediate","question":"What are finalizers and why do operators need them?","answerFull":"A finalizer is a key on an object that blocks its deletion until removed. Operators use finalizers to perform external cleanup before a CR is deleted — e.g., delete the S3 backup bucket, cloud volumes, or DNS records a PostgresCluster created. Without a finalizer, deleting the CR orphans those external resources, leaking cost and creating drift between Kubernetes and the cloud.","correct":"A finalizer is a key on an object that blocks its deletion until removed.","distractors":["Use a mature community operator (Prometheus Operator, cert-manager, CloudNativePG) whenever one fits — operators are subtle (idempotency, finalizers, status, leader election, edge cases) and a buggy one can destroy data.","A CRD includes an OpenAPI v3 schema; the API server validates every custom resource against it at admission (types, required fields, enums, min/max), rejecting malformed CRs before they're stored or reconciled.","A Helm chart is a one-time (or upgrade-time) template render — it installs objects but has no ongoing logic."]},{"id":"devops/kubernetes_operators_and_crds#0f4cc975f051","section":"devops","module":"devops/kubernetes_operators_and_crds","moduleName":"kubernetes operators and crds","qIndex":7,"difficulty":"intermediate","question":"When should you write your own operator vs use an existing one?","answerFull":"Use a mature community operator (Prometheus Operator, cert-manager, CloudNativePG) whenever one fits — operators are subtle (idempotency, finalizers, status, leader election, edge cases) and a buggy one can destroy data. Write your own only for genuinely novel needs: a custom platform abstraction, proprietary software with no operator, or organization-specific automation. Building an operator is a real engineering investment to maintain.","correct":"Use a mature community operator (Prometheus Operator, cert-manager, CloudNativePG) whenever one fits — operators are subtle (idempotency, finalizers, status, leader election, edge cases) and a buggy one can destroy data.","distractors":["Edge-triggered reacts to specific events (the \"create\" happened); level-triggered acts on the current observed state regardless of which event led there.","When you run multiple operator replicas for availability, only one should actively reconcile at a time — otherwise two controllers fight over the same resources.","By updating the CR's status subresource (e.g., status.phase: Ready, conditions, observed replicas)."]},{"id":"devops/kubernetes_operators_and_crds#be292fc48fb5","section":"devops","module":"devops/kubernetes_operators_and_crds","moduleName":"kubernetes operators and crds","qIndex":8,"difficulty":"intermediate","question":"How do you build an operator?","answerFull":"Typically with Kubebuilder/controller-runtime (Go), which scaffolds CRD types, a manager, informers, workqueues, and a Reconcile method you implement. The Operator SDK wraps this and also supports Ansible/Helm-based operators; kopf (Python) lowers the barrier. You define the CRD schema, implement idempotent reconciliation, manage finalizers, update status, and use leader election so only one replica reconciles at a time.","correct":"Typically with Kubebuilder/controller-runtime (Go), which scaffolds CRD types, a manager, informers, workqueues, and a Reconcile method you implement.","distractors":["When you run multiple operator replicas for availability, only one should actively reconcile at a time — otherwise two controllers fight over the same resources.","A CRD includes an OpenAPI v3 schema; the API server validates every custom resource against it at admission (types, required fields, enums, min/max), rejecting malformed CRs before they're stored or reconciled.","Use a mature community operator (Prometheus Operator, cert-manager, CloudNativePG) whenever one fits — operators are subtle (idempotency, finalizers, status, leader election, edge cases) and a buggy one can destroy data."]},{"id":"devops/kubernetes_operators_and_crds#491397059d40","section":"devops","module":"devops/kubernetes_operators_and_crds","moduleName":"kubernetes operators and crds","qIndex":9,"difficulty":"intermediate","question":"What's the difference between a CRD-with-operator and a Helm chart?","answerFull":"A Helm chart is a one-time (or upgrade-time) template render — it installs objects but has no ongoing logic. A CRD+operator provides *continuous* reconciliation and day-2 automation: it keeps watching and acting (failover, backup, scaling) long after install. Use a chart when you just need to deploy static manifests; use an operator when the software needs ongoing operational logic.","correct":"A Helm chart is a one-time (or upgrade-time) template render — it installs objects but has no ongoing logic.","distractors":["When you run multiple operator replicas for availability, only one should actively reconcile at a time — otherwise two controllers fight over the same resources.","Use a mature community operator (Prometheus Operator, cert-manager, CloudNativePG) whenever one fits — operators are subtle (idempotency, finalizers, status, leader election, edge cases) and a buggy one can destroy data.","Typically with Kubebuilder/controller-runtime (Go), which scaffolds CRD types, a manager, informers, workqueues, and a Reconcile method you implement."]},{"id":"devops/kubernetes_operators_and_crds#0fa799e4ab2f","section":"devops","module":"devops/kubernetes_operators_and_crds","moduleName":"kubernetes operators and crds","qIndex":10,"difficulty":"intermediate","question":"How does an operator report progress, and why does status matter?","answerFull":"By updating the CR's status subresource (e.g., status.phase: Ready, conditions, observed replicas). This lets users (kubectl get pgc) and other controllers observe the real state without inspecting internals, and enables tools/automation to wait on readiness. The status subresource also allows separate RBAC and avoids spec/status update conflicts. Reflecting reality in status is part of being a good API citizen.","correct":"By updating the CR's status subresource (e.g., status.phase: Ready, conditions, observed replicas).","distractors":["Edge-triggered reacts to specific events (the \"create\" happened); level-triggered acts on the current observed state regardless of which event led there.","Use a mature community operator (Prometheus Operator, cert-manager, CloudNativePG) whenever one fits — operators are subtle (idempotency, finalizers, status, leader election, edge cases) and a buggy one can destroy data.","When you run multiple operator replicas for availability, only one should actively reconcile at a time — otherwise two controllers fight over the same resources."]},{"id":"devops/kubernetes_operators_and_crds#f0549886e1ff","section":"devops","module":"devops/kubernetes_operators_and_crds","moduleName":"kubernetes operators and crds","qIndex":11,"difficulty":"advanced","question":"What is leader election in an operator and why is it needed?","answerFull":"When you run multiple operator replicas for availability, only one should actively reconcile at a time — otherwise two controllers fight over the same resources. Leader election (via a Lease object) ensures exactly one replica is the active leader; the others stand by and take over if the leader fails. controller-runtime provides this out of the box; forgetting it causes duplicate, conflicting actions.","correct":"When you run multiple operator replicas for availability, only one should actively reconcile at a time — otherwise two controllers fight over the same resources.","distractors":["Because the loop runs repeatedly — on every relevant event and on periodic resyncs — and may re-run after a controller restart.","By updating the CR's status subresource (e.g., status.phase: Ready, conditions, observed replicas).","It's encoding operational/domain expertise as a custom controller that watches custom resources and reconciles them to desired state — applying Kubernetes' \"declare + reconcile\" model to complex software."]},{"id":"devops/kubernetes_operators_and_crds#0976f35e726a","section":"devops","module":"devops/kubernetes_operators_and_crds","moduleName":"kubernetes operators and crds","qIndex":12,"difficulty":"advanced","question":"How do CRDs get schema validation, and why use it?","answerFull":"A CRD includes an OpenAPI v3 schema; the API server validates every custom resource against it at admission (types, required fields, enums, min/max), rejecting malformed CRs before they're stored or reconciled. This catches user errors early (e.g., replicas: 0 where minimum is 1) with clear messages, rather than the operator having to defensively handle garbage input at runtime.","correct":"A CRD includes an OpenAPI v3 schema; the API server validates every custom resource against it at admission (types, required fields, enums, min/max), rejecting malformed CRs before they're stored or reconciled.","distractors":["Typically with Kubebuilder/controller-runtime (Go), which scaffolds CRD types, a manager, informers, workqueues, and a Reconcile method you implement.","Because the loop runs repeatedly — on every relevant event and on periodic resyncs — and may re-run after a controller restart.","It's encoding operational/domain expertise as a custom controller that watches custom resources and reconciles them to desired state — applying Kubernetes' \"declare + reconcile\" model to complex software."]},{"id":"devops/kubernetes_scheduling_and_autoscaling#9fd9edc33559","section":"devops","module":"devops/kubernetes_scheduling_and_autoscaling","moduleName":"kubernetes scheduling and autoscaling","qIndex":2,"difficulty":"core","question":"Why are requests the most important resource setting?","answerFull":"The scheduler bin-packs based on requests, not actual usage. Under-request and nodes look empty, so it over-packs and real usage causes contention/OOM/throttling; over-request and nodes look full while idle, wasting money. Requests also determine QoS class and HPA's CPU-utilization denominator. Setting them close to measured real usage is the single highest-leverage tuning decision.","correct":"The scheduler bin-packs based on requests, not actual usage.","distractors":["Cluster Autoscaler scales predefined node groups (ASGs) up/down based on unschedulable Pods and underutilization — simple but constrained to the instance types you pre-defined.","HPA scales the number of Pod replicas based on CPU/memory/custom metrics.","Guaranteed (requests==limits for all containers) Pods are evicted last under node memory pressure; Burstable (requests<limits) are in the middle; BestEffort (no requests/limits) are evicted first."]},{"id":"devops/kubernetes_scheduling_and_autoscaling#80f1f7257ac7","section":"devops","module":"devops/kubernetes_scheduling_and_autoscaling","moduleName":"kubernetes scheduling and autoscaling","qIndex":3,"difficulty":"core","question":"Explain QoS classes and their effect.","answerFull":"Guaranteed (requests==limits for all containers) Pods are evicted last under node memory pressure; Burstable (requests<limits) are in the middle; BestEffort (no requests/limits) are evicted first. QoS controls the kubelet's eviction order and oom_score_adj, so latency-critical Pods should be Guaranteed to be the least likely to be killed when a node is squeezed.","correct":"Guaranteed (requests==limits for all containers) Pods are evicted last under node memory pressure; Burstable (requests<limits) are in the middle; BestEffort (no requests/limits) are evicted first.","distractors":["The scheduler bin-packs based on requests, not actual usage.","Use topologySpreadConstraints (and/or Pod anti-affinity) to spread replicas across topology.kubernetes.io/zone and kubernetes.io/hostname, so no single node or AZ holds too many.","A CPU limit causes CFS throttling: once a Pod uses its quota within a 100ms period, it's paused, spiking tail latency even when the node has spare CPU."]},{"id":"devops/kubernetes_scheduling_and_autoscaling#319b3f36f86d","section":"devops","module":"devops/kubernetes_scheduling_and_autoscaling","moduleName":"kubernetes scheduling and autoscaling","qIndex":4,"difficulty":"core","question":"HPA vs VPA vs Cluster Autoscaler — what does each scale?","answerFull":"HPA scales the number of Pod replicas based on CPU/memory/custom metrics. VPA adjusts a Pod's requests/limits (right-sizing), recreating Pods to apply changes. Cluster Autoscaler (and Karpenter) add/remove nodes when Pods can't schedule or nodes are underused. They compose: HPA adds Pods, the node autoscaler adds nodes to hold them. Don't run HPA and VPA on the same metric — they fight.","correct":"HPA scales the number of Pod replicas based on CPU/memory/custom metrics.","distractors":["Use topologySpreadConstraints (and/or Pod anti-affinity) to spread replicas across topology.kubernetes.io/zone and kubernetes.io/hostname, so no single node or AZ holds too many.","A CPU limit causes CFS throttling: once a Pod uses its quota within a 100ms period, it's paused, spiking tail latency even when the node has spare CPU.","The scheduler bin-packs based on requests, not actual usage."]},{"id":"devops/kubernetes_scheduling_and_autoscaling#5cc87f6caedf","section":"devops","module":"devops/kubernetes_scheduling_and_autoscaling","moduleName":"kubernetes scheduling and autoscaling","qIndex":5,"difficulty":"core","question":"How does HPA actually compute the desired replica count?","answerFull":"desiredReplicas = ceil(currentReplicas * (currentMetricValue / targetMetricValue)). For CPU utilization, the metric is a percentage of the *requested* CPU averaged across Pods — which is why CPU requests must be set or HPA can't compute it. The controller syncs ~every 15s and applies a stabilization window (default 5min on scale-down) to avoid flapping.","correct":"desiredReplicas = ceil(currentReplicas * (currentMetricValue / targetMetricValue)).","distractors":["HPA scales the number of Pod replicas based on CPU/memory/custom metrics.","KEDA enables event-driven scaling on external sources HPA can't natively use — queue depth (SQS/RabbitMQ), Kafka consumer lag, cron schedules, Prometheus queries — and crucially supports scale-to-zero.","A CPU limit causes CFS throttling: once a Pod uses its quota within a 100ms period, it's paused, spiking tail latency even when the node has spare CPU."]},{"id":"devops/kubernetes_scheduling_and_autoscaling#82fed8d8be45","section":"devops","module":"devops/kubernetes_scheduling_and_autoscaling","moduleName":"kubernetes scheduling and autoscaling","qIndex":6,"difficulty":"intermediate","question":"What are taints and tolerations, and a use case?","answerFull":"A taint on a node repels Pods that don't explicitly tolerate it; a matching toleration on a Pod lets it schedule there. Use them to dedicate nodes: taint GPU nodes nvidia.com/gpu:NoSchedule so only GPU workloads (which add the toleration) land there, keeping expensive accelerators free of general Pods. Effects: NoSchedule, PreferNoSchedule, NoExecute (also evicts existing Pods).","correct":"A taint on a node repels Pods that don't explicitly tolerate it; a matching toleration on a Pod lets it schedule there.","distractors":["Use topologySpreadConstraints (and/or Pod anti-affinity) to spread replicas across topology.kubernetes.io/zone and kubernetes.io/hostname, so no single node or AZ holds too many.","Cluster Autoscaler scales predefined node groups (ASGs) up/down based on unschedulable Pods and underutilization — simple but constrained to the instance types you pre-defined.","A PDB limits how many Pods of a set can be *voluntarily* disrupted at once (minAvailable/maxUnavailable)."]},{"id":"devops/kubernetes_scheduling_and_autoscaling#2f7d2d718f96","section":"devops","module":"devops/kubernetes_scheduling_and_autoscaling","moduleName":"kubernetes scheduling and autoscaling","qIndex":7,"difficulty":"intermediate","question":"How do you ensure replicas survive a node or AZ failure?","answerFull":"Use topologySpreadConstraints (and/or Pod anti-affinity) to spread replicas across topology.kubernetes.io/zone and kubernetes.io/hostname, so no single node or AZ holds too many. With maxSkew: 1 across zones, an AZ loss degrades capacity by roughly its share rather than taking the service down. Pair with a PodDisruptionBudget to protect against voluntary disruptions.","correct":"Use topologySpreadConstraints (and/or Pod anti-affinity) to spread replicas across topology.kubernetes.io/zone and kubernetes.io/hostname, so no single node or AZ holds too many.","distractors":["HPA scales the number of Pod replicas based on CPU/memory/custom metrics.","A taint on a node repels Pods that don't explicitly tolerate it; a matching toleration on a Pod lets it schedule there.","Guaranteed (requests==limits for all containers) Pods are evicted last under node memory pressure; Burstable (requests<limits) are in the middle; BestEffort (no requests/limits) are evicted first."]},{"id":"devops/kubernetes_scheduling_and_autoscaling#89514f58dd08","section":"devops","module":"devops/kubernetes_scheduling_and_autoscaling","moduleName":"kubernetes scheduling and autoscaling","qIndex":8,"difficulty":"intermediate","question":"What is a PodDisruptionBudget and when is it essential?","answerFull":"A PDB limits how many Pods of a set can be *voluntarily* disrupted at once (minAvailable/maxUnavailable). It's essential during node drains (cluster upgrades, autoscaler scale-down): without it, draining a node can evict all replicas simultaneously and cause an outage during a routine operation. With minAvailable: 2, drains block until they can proceed without dropping below 2 ready Pods.","correct":"A PDB limits how many Pods of a set can be *voluntarily* disrupted at once (minAvailable/maxUnavailable).","distractors":["A taint on a node repels Pods that don't explicitly tolerate it; a matching toleration on a Pod lets it schedule there.","Cluster Autoscaler scales predefined node groups (ASGs) up/down based on unschedulable Pods and underutilization — simple but constrained to the instance types you pre-defined.","Guaranteed (requests==limits for all containers) Pods are evicted last under node memory pressure; Burstable (requests<limits) are in the middle; BestEffort (no requests/limits) are evicted first."]},{"id":"devops/kubernetes_scheduling_and_autoscaling#8406086e827c","section":"devops","module":"devops/kubernetes_scheduling_and_autoscaling","moduleName":"kubernetes scheduling and autoscaling","qIndex":9,"difficulty":"intermediate","question":"Cluster Autoscaler vs Karpenter?","answerFull":"Cluster Autoscaler scales predefined node groups (ASGs) up/down based on unschedulable Pods and underutilization — simple but constrained to the instance types you pre-defined. Karpenter provisions nodes just-in-time, choosing instance types/sizes that best fit pending Pods directly (no node groups), blends spot/on-demand, and aggressively consolidates — typically faster scale-up and lower cost, at the price of being newer/more complex.","correct":"Cluster Autoscaler scales predefined node groups (ASGs) up/down based on unschedulable Pods and underutilization — simple but constrained to the instance types you pre-defined.","distractors":["A PDB limits how many Pods of a set can be *voluntarily* disrupted at once (minAvailable/maxUnavailable).","A CPU limit causes CFS throttling: once a Pod uses its quota within a 100ms period, it's paused, spiking tail latency even when the node has spare CPU.","Use topologySpreadConstraints (and/or Pod anti-affinity) to spread replicas across topology.kubernetes.io/zone and kubernetes.io/hostname, so no single node or AZ holds too many."]},{"id":"devops/kubernetes_scheduling_and_autoscaling#f172affb16b1","section":"devops","module":"devops/kubernetes_scheduling_and_autoscaling","moduleName":"kubernetes scheduling and autoscaling","qIndex":10,"difficulty":"intermediate","question":"What does KEDA add over HPA?","answerFull":"KEDA enables event-driven scaling on external sources HPA can't natively use — queue depth (SQS/RabbitMQ), Kafka consumer lag, cron schedules, Prometheus queries — and crucially supports scale-to-zero. Idle workers cost nothing and wake on backlog. Under the hood KEDA drives an HPA, translating external triggers into scaling decisions.","correct":"KEDA enables event-driven scaling on external sources HPA can't natively use — queue depth (SQS/RabbitMQ), Kafka consumer lag, cron schedules, Prometheus queries — and crucially supports scale-to-zero.","distractors":["HPA scales the number of Pod replicas based on CPU/memory/custom metrics.","A CPU limit causes CFS throttling: once a Pod uses its quota within a 100ms period, it's paused, spiking tail latency even when the node has spare CPU.","A PDB limits how many Pods of a set can be *voluntarily* disrupted at once (minAvailable/maxUnavailable)."]},{"id":"devops/kubernetes_scheduling_and_autoscaling#c6304432b663","section":"devops","module":"devops/kubernetes_scheduling_and_autoscaling","moduleName":"kubernetes scheduling and autoscaling","qIndex":11,"difficulty":"advanced","question":"Why might you omit CPU limits but keep memory limits?","answerFull":"A CPU limit causes CFS throttling: once a Pod uses its quota within a 100ms period, it's paused, spiking tail latency even when the node has spare CPU. For latency-sensitive services, omitting the CPU limit (keeping the request for scheduling) lets the Pod burst into idle CPU. Memory limits are kept because exceeding memory triggers an OOM kill, and unbounded memory growth can take down a node.","correct":"A CPU limit causes CFS throttling: once a Pod uses its quota within a 100ms period, it's paused, spiking tail latency even when the node has spare CPU.","distractors":["A taint on a node repels Pods that don't explicitly tolerate it; a matching toleration on a Pod lets it schedule there.","Guaranteed (requests==limits for all containers) Pods are evicted last under node memory pressure; Burstable (requests<limits) are in the middle; BestEffort (no requests/limits) are evicted first.","Use topologySpreadConstraints (and/or Pod anti-affinity) to spread replicas across topology.kubernetes.io/zone and kubernetes.io/hostname, so no single node or AZ holds too many."]},{"id":"devops/kubernetes_security#213089264f1e","section":"devops","module":"devops/kubernetes_security","moduleName":"kubernetes security","qIndex":1,"difficulty":"core","question":"How does authentication work in Kubernetes — is there a user database?","answerFull":"No — Kubernetes has no built-in user store. The API server authenticates requests via external mechanisms: client certificates, OIDC tokens (from an IdP), bearer tokens, or ServiceAccount tokens (for workloads). It extracts a username and groups from the credential and hands those to authorization. \"Users\" are an external concept; only ServiceAccounts are first-class API objects.","correct":"No — Kubernetes has no built-in user store.","distractors":["Pod Security Admission (PSA) with Pod Security Standards (Privileged/Baseline/Restricted), enforced per namespace via labels (pod-security.kubernetes.io/enforce: restricted).","Use workload identity federation: EKS IRSA or GKE Workload Identity bind a Kubernetes ServiceAccount to a cloud IAM role via a projected OIDC token.","A Role grants permissions within a single namespace; a ClusterRole grants them cluster-wide or for cluster-scoped resources."]},{"id":"devops/kubernetes_security#5d5d7967e057","section":"devops","module":"devops/kubernetes_security","moduleName":"kubernetes security","qIndex":2,"difficulty":"core","question":"Explain RBAC: Role vs ClusterRole, RoleBinding vs ClusterRoleBinding.","answerFull":"A Role grants permissions within a single namespace; a ClusterRole grants them cluster-wide or for cluster-scoped resources. A RoleBinding grants a Role's (or ClusterRole's) permissions to subjects *within a namespace*; a ClusterRoleBinding grants a ClusterRole cluster-wide. RBAC is default-deny — subjects have no permissions until bound. The dangerous combination is binding broad ClusterRoles (especially cluster-admin) to widely-used subjects.","correct":"A Role grants permissions within a single namespace; a ClusterRole grants them cluster-wide or for cluster-scoped resources.","distractors":["By default Secrets are only base64-encoded and stored as plaintext in etcd — anyone with get secrets RBAC or etcd access can read them.","Use workload identity federation: EKS IRSA or GKE Workload Identity bind a Kubernetes ServiceAccount to a cloud IAM role via a projected OIDC token.","By default every Pod auto-mounts its namespace's default ServiceAccount token at a known path."]},{"id":"devops/kubernetes_security#2bb2afdff24a","section":"devops","module":"devops/kubernetes_security","moduleName":"kubernetes security","qIndex":3,"difficulty":"core","question":"Why is the ServiceAccount token the most dangerous default credential?","answerFull":"By default every Pod auto-mounts its namespace's default ServiceAccount token at a known path. If the Pod is compromised, the attacker has that token and can call the API server with whatever RBAC the SA has. If the SA is over-privileged (or default was granted broad rights), this is direct cluster escalation. Mitigate by automountServiceAccountToken: false where unneeded and scoping every SA's RBAC to least privilege.","correct":"By default every Pod auto-mounts its namespace's default ServiceAccount token at a known path.","distractors":["Use workload identity federation: EKS IRSA or GKE Workload Identity bind a Kubernetes ServiceAccount to a cloud IAM role via a projected OIDC token.","By default all Pods can talk to all Pods (flat network), enabling lateral movement after a breach.","A Role grants permissions within a single namespace; a ClusterRole grants them cluster-wide or for cluster-scoped resources."]},{"id":"devops/kubernetes_security#d48616e2a027","section":"devops","module":"devops/kubernetes_security","moduleName":"kubernetes security","qIndex":5,"difficulty":"core","question":"What replaced PodSecurityPolicy and how does it work?","answerFull":"Pod Security Admission (PSA) with Pod Security Standards (Privileged/Baseline/Restricted), enforced per namespace via labels (pod-security.kubernetes.io/enforce: restricted). PSP was removed in 1.25 because it was hard to use and inconsistent. PSA is simpler (three built-in profiles, with warn/audit modes for safe rollout) but less flexible — for custom rules you layer OPA Gatekeeper or Kyverno.","correct":"Pod Security Admission (PSA) with Pod Security Standards (Privileged/Baseline/Restricted), enforced per namespace via labels (pod-security.kubernetes.io/enforce: restricted).","distractors":["A Role grants permissions within a single namespace; a ClusterRole grants them cluster-wide or for cluster-scoped resources.","Use workload identity federation: EKS IRSA or GKE Workload Identity bind a Kubernetes ServiceAccount to a cloud IAM role via a projected OIDC token.","By default every Pod auto-mounts its namespace's default ServiceAccount token at a known path."]},{"id":"devops/kubernetes_security#dcf6cff7bfe3","section":"devops","module":"devops/kubernetes_security","moduleName":"kubernetes security","qIndex":6,"difficulty":"intermediate","question":"Are Kubernetes Secrets encrypted? How do you actually protect them?","answerFull":"By default Secrets are only base64-encoded and stored as plaintext in etcd — anyone with get secrets RBAC or etcd access can read them. To protect them: enable encryption-at-rest with a KMS provider (envelope encryption) so etcd holds ciphertext; tightly scope RBAC on secrets; and prefer an external store (Vault) via the External Secrets Operator so the sensitive material never lives in etcd at all.","correct":"By default Secrets are only base64-encoded and stored as plaintext in etcd — anyone with get secrets RBAC or etcd access can read them.","distractors":["Baseline blocks known privilege escalations (no host namespaces, no privileged containers, no hostPath) while staying broadly compatible — a sane default for most apps.","Use workload identity federation: EKS IRSA or GKE Workload Identity bind a Kubernetes ServiceAccount to a cloud IAM role via a projected OIDC token.","Pod Security Admission (PSA) with Pod Security Standards (Privileged/Baseline/Restricted), enforced per namespace via labels (pod-security.kubernetes.io/enforce: restricted)."]},{"id":"devops/kubernetes_security#237c653b6c33","section":"devops","module":"devops/kubernetes_security","moduleName":"kubernetes security","qIndex":7,"difficulty":"intermediate","question":"How do admission controllers enforce security, and what's the risk?","answerFull":"After authn/authz, mutating then validating admission webhooks intercept the request before persistence. Validating webhooks (Gatekeeper/Kyverno/PSA) reject non-compliant objects — e.g., Pods running as root, missing limits, or using unsigned images. The risk: a slow or unavailable webhook can block all writes (if failurePolicy: Fail) or silently allow violations (if Ignore), so webhook latency, availability, and failure policy must be designed carefully.","correct":"After authn/authz, mutating then validating admission webhooks intercept the request before persistence.","distractors":["Pod Security Admission (PSA) with Pod Security Standards (Privileged/Baseline/Restricted), enforced per namespace via labels (pod-security.kubernetes.io/enforce: restricted).","A Role grants permissions within a single namespace; a ClusterRole grants them cluster-wide or for cluster-scoped resources.","Baseline blocks known privilege escalations (no host namespaces, no privileged containers, no hostPath) while staying broadly compatible — a sane default for most apps."]},{"id":"devops/kubernetes_security#699762717af6","section":"devops","module":"devops/kubernetes_security","moduleName":"kubernetes security","qIndex":8,"difficulty":"intermediate","question":"How do you avoid storing long-lived cloud credentials in the cluster?","answerFull":"Use workload identity federation: EKS IRSA or GKE Workload Identity bind a Kubernetes ServiceAccount to a cloud IAM role via a projected OIDC token. The Pod assumes the role and gets short-lived, automatically-rotated cloud credentials — no static access keys in Secrets to leak. This eliminates the most commonly exfiltrated secret type.","correct":"Use workload identity federation: EKS IRSA or GKE Workload Identity bind a Kubernetes ServiceAccount to a cloud IAM role via a projected OIDC token.","distractors":["A Role grants permissions within a single namespace; a ClusterRole grants them cluster-wide or for cluster-scoped resources.","By default every Pod auto-mounts its namespace's default ServiceAccount token at a known path.","Pod Security Admission (PSA) with Pod Security Standards (Privileged/Baseline/Restricted), enforced per namespace via labels (pod-security.kubernetes.io/enforce: restricted)."]},{"id":"devops/kubernetes_security#67ac1eaf673b","section":"devops","module":"devops/kubernetes_security","moduleName":"kubernetes security","qIndex":9,"difficulty":"intermediate","question":"How does NetworkPolicy contribute to security?","answerFull":"By default all Pods can talk to all Pods (flat network), enabling lateral movement after a breach. NetworkPolicy lets you default-deny and explicitly allow only required flows (e.g., api→db on 5432), containing a compromised Pod. It's only enforced if the CNI supports it (Calico/Cilium). Remember to allow DNS egress when default-denying egress (see kubernetes_networking).","correct":"By default all Pods can talk to all Pods (flat network), enabling lateral movement after a breach.","distractors":["Pod Security Admission (PSA) with Pod Security Standards (Privileged/Baseline/Restricted), enforced per namespace via labels (pod-security.kubernetes.io/enforce: restricted).","Baseline blocks known privilege escalations (no host namespaces, no privileged containers, no hostPath) while staying broadly compatible — a sane default for most apps.","By default Secrets are only base64-encoded and stored as plaintext in etcd — anyone with get secrets RBAC or etcd access can read them."]},{"id":"devops/kubernetes_security#76f38cb981db","section":"devops","module":"devops/kubernetes_security","moduleName":"kubernetes security","qIndex":10,"difficulty":"intermediate","question":"What's the difference between Baseline and Restricted Pod Security Standards?","answerFull":"Baseline blocks known privilege escalations (no host namespaces, no privileged containers, no hostPath) while staying broadly compatible — a sane default for most apps. Restricted is hardened further: requires non-root, drops all capabilities, requires seccomp RuntimeDefault, disallows privilege escalation, and restricts volume types — appropriate for security-sensitive or multi-tenant workloads, at the cost of needing apps to be well-behaved.","correct":"Baseline blocks known privilege escalations (no host namespaces, no privileged containers, no hostPath) while staying broadly compatible — a sane default for most apps.","distractors":["By default every Pod auto-mounts its namespace's default ServiceAccount token at a known path.","Pod Security Admission (PSA) with Pod Security Standards (Privileged/Baseline/Restricted), enforced per namespace via labels (pod-security.kubernetes.io/enforce: restricted).","By default all Pods can talk to all Pods (flat network), enabling lateral movement after a breach."]},{"id":"devops/kubernetes_security#cb39c88f380e","section":"devops","module":"devops/kubernetes_security","moduleName":"kubernetes security","qIndex":15,"difficulty":"advanced","question":"What's \"assume breach\" and how does it shape design?","answerFull":"It's the assumption that a container *will* be compromised, so you architect to minimize what that achieves rather than only trying to prevent it. Concretely: non-root + read-only + dropped caps (limit local damage), least-privilege RBAC + no auto-mounted token (limit cluster access), default-deny NetworkPolicy (limit lateral movement), encrypted/external secrets (limit data theft), and runtime detection (catch what gets through). Defense in depth so no single failure is catastrophic.","correct":"It's the assumption that a container *will* be compromised, so you architect to minimize what that achieves rather than only trying to prevent it.","distractors":["By default all Pods can talk to all Pods (flat network), enabling lateral movement after a breach.","A Role grants permissions within a single namespace; a ClusterRole grants them cluster-wide or for cluster-scoped resources.","After authn/authz, mutating then validating admission webhooks intercept the request before persistence."]},{"id":"devops/kubernetes_storage_and_state#23ed156f8410","section":"devops","module":"devops/kubernetes_storage_and_state","moduleName":"kubernetes storage and state","qIndex":1,"difficulty":"core","question":"Explain PV, PVC, and StorageClass.","answerFull":"A PersistentVolume (PV) is a cluster resource representing real storage with its own lifecycle. A PersistentVolumeClaim (PVC) is a Pod's request for storage (size, access mode). A StorageClass defines a *type* of storage (provisioner + parameters + reclaim policy) and enables dynamic provisioning — a PVC referencing it auto-creates a matching PV via the CSI driver. The separation lets Pods request abstract storage without knowing the backend.","correct":"A PersistentVolume (PV) is a cluster resource representing real storage with its own lifecycle.","distractors":["It controls what happens to the underlying storage when the PVC is deleted: Delete destroys the volume (data gone); Retain keeps it for manual recovery.","The Container Storage Interface is a standard plugin API decoupling Kubernetes from storage backends.","The PVC↔PV binding is independent of any Pod, so when a (StatefulSet) Pod is rescheduled, its PVC and the underlying volume detach from the old node and reattach to the new one — the data follows the Pod."]},{"id":"devops/kubernetes_storage_and_state#43360394c7d6","section":"devops","module":"devops/kubernetes_storage_and_state","moduleName":"kubernetes storage and state","qIndex":2,"difficulty":"core","question":"Why does a PVC outlive the Pod, and why does that matter?","answerFull":"The PVC↔PV binding is independent of any Pod, so when a (StatefulSet) Pod is rescheduled, its PVC and the underlying volume detach from the old node and reattach to the new one — the data follows the Pod. This is the whole point of persistent storage: Pods are disposable, but their state survives reschedules, node failures, and restarts.","correct":"The PVC↔PV binding is independent of any Pod, so when a (StatefulSet) Pod is rescheduled, its PVC and the underlying volume detach from the old node and reattach to the new one — the data follows the Pod.","distractors":["hostPath mounts a node's filesystem path into the Pod.","emptyDir is scratch space tied to the Pod's lifetime — fine for caches, temp files, or sharing data between containers in a Pod, but it's destroyed when the Pod is deleted or rescheduled.","The cloud still considers the volume attached to the dead node until the node is confirmed gone and the volume is force-detached, which can take minutes."]},{"id":"devops/kubernetes_storage_and_state#5ed13df69eb3","section":"devops","module":"devops/kubernetes_storage_and_state","moduleName":"kubernetes storage and state","qIndex":4,"difficulty":"core","question":"Why is volumeBindingMode: WaitForFirstConsumer important in multi-AZ clusters?","answerFull":"Cloud block volumes are zone-scoped. With Immediate binding, the volume is provisioned (in some AZ) before the Pod is scheduled, and if the scheduler later places the Pod in a different AZ, the volume can't attach and the Pod hangs Pending. WaitForFirstConsumer delays provisioning until the scheduler picks a node, then creates the volume in that node's AZ, guaranteeing they match.","correct":"Cloud block volumes are zone-scoped.","distractors":["The cloud still considers the volume attached to the dead node until the node is confirmed gone and the volume is force-detached, which can take minutes.","A PersistentVolume (PV) is a cluster resource representing real storage with its own lifecycle.","It controls what happens to the underlying storage when the PVC is deleted: Delete destroys the volume (data gone); Retain keeps it for manual recovery."]},{"id":"devops/kubernetes_storage_and_state#5d5da180b7ad","section":"devops","module":"devops/kubernetes_storage_and_state","moduleName":"kubernetes storage and state","qIndex":5,"difficulty":"core","question":"What does the reclaim policy control, and which should production use?","answerFull":"It controls what happens to the underlying storage when the PVC is deleted: Delete destroys the volume (data gone); Retain keeps it for manual recovery. Production stateful workloads should use Retain so an accidental PVC deletion (or helm uninstall) doesn't irrecoverably destroy data — combined with snapshots/backups.","correct":"It controls what happens to the underlying storage when the PVC is deleted: Delete destroys the volume (data gone); Retain keeps it for manual recovery.","distractors":["The Container Storage Interface is a standard plugin API decoupling Kubernetes from storage backends.","A PersistentVolume (PV) is a cluster resource representing real storage with its own lifecycle.","With a CSI driver supporting them: a VolumeSnapshot object triggers the driver to snapshot the underlying volume; you restore by creating a PVC with dataSource referencing the snapshot."]},{"id":"devops/kubernetes_storage_and_state#b710ff8a5979","section":"devops","module":"devops/kubernetes_storage_and_state","moduleName":"kubernetes storage and state","qIndex":6,"difficulty":"intermediate","question":"Why can a Pod using an RWO volume fail to move quickly after a node dies?","answerFull":"The cloud still considers the volume attached to the dead node until the node is confirmed gone and the volume is force-detached, which can take minutes. Until then, a replacement Pod on another node gets a \"Multi-Attach\" / attach-timeout error because RWO permits only one node. This is why RWO failover isn't instant and why truly HA stateful systems replicate data rather than relying on volume re-attach.","correct":"The cloud still considers the volume attached to the dead node until the node is confirmed gone and the volume is force-detached, which can take minutes.","distractors":["With a CSI driver supporting them: a VolumeSnapshot object triggers the driver to snapshot the underlying volume; you restore by creating a PVC with dataSource referencing the snapshot.","Cloud block volumes are zone-scoped.","The PVC↔PV binding is independent of any Pod, so when a (StatefulSet) Pod is rescheduled, its PVC and the underlying volume detach from the old node and reattach to the new one — the data follows the Pod."]},{"id":"devops/kubernetes_storage_and_state#7416e6266f81","section":"devops","module":"devops/kubernetes_storage_and_state","moduleName":"kubernetes storage and state","qIndex":7,"difficulty":"intermediate","question":"What is CSI and why does it matter?","answerFull":"The Container Storage Interface is a standard plugin API decoupling Kubernetes from storage backends. Vendors ship CSI drivers (EBS, EFS, Ceph) implementing provision/attach/mount/snapshot/expand operations. It means new storage systems integrate without changing Kubernetes core, and features like snapshots and volume expansion are available uniformly across backends that support them.","correct":"The Container Storage Interface is a standard plugin API decoupling Kubernetes from storage backends.","distractors":["With a CSI driver supporting them: a VolumeSnapshot object triggers the driver to snapshot the underlying volume; you restore by creating a PVC with dataSource referencing the snapshot.","A PersistentVolume (PV) is a cluster resource representing real storage with its own lifecycle.","You back up two things: the Kubernetes objects (manifests/etcd) and the PVC data."]},{"id":"devops/kubernetes_storage_and_state#4460f240401e","section":"devops","module":"devops/kubernetes_storage_and_state","moduleName":"kubernetes storage and state","qIndex":9,"difficulty":"intermediate","question":"emptyDir vs persistent volume — when each?","answerFull":"emptyDir is scratch space tied to the Pod's lifetime — fine for caches, temp files, or sharing data between containers in a Pod, but it's destroyed when the Pod is deleted or rescheduled. Use a PVC-backed volume for anything you can't lose. A frequent incident is someone using emptyDir for a database and losing all data on the first reschedule.","correct":"emptyDir is scratch space tied to the Pod's lifetime — fine for caches, temp files, or sharing data between containers in a Pod, but it's destroyed when the Pod is deleted or rescheduled.","distractors":["With a CSI driver supporting them: a VolumeSnapshot object triggers the driver to snapshot the underlying volume; you restore by creating a PVC with dataSource referencing the snapshot.","hostPath mounts a node's filesystem path into the Pod.","It controls what happens to the underlying storage when the PVC is deleted: Delete destroys the volume (data gone); Retain keeps it for manual recovery."]},{"id":"devops/kubernetes_storage_and_state#8772334cef79","section":"devops","module":"devops/kubernetes_storage_and_state","moduleName":"kubernetes storage and state","qIndex":10,"difficulty":"intermediate","question":"How do volume snapshots and expansion work?","answerFull":"With a CSI driver supporting them: a VolumeSnapshot object triggers the driver to snapshot the underlying volume; you restore by creating a PVC with dataSource referencing the snapshot. Expansion requires allowVolumeExpansion: true on the StorageClass — you edit the PVC's requested size upward and the driver grows the volume and filesystem (online for many drivers). Shrinking is not supported.","correct":"With a CSI driver supporting them: a VolumeSnapshot object triggers the driver to snapshot the underlying volume; you restore by creating a PVC with dataSource referencing the snapshot.","distractors":["You back up two things: the Kubernetes objects (manifests/etcd) and the PVC data.","The cloud still considers the volume attached to the dead node until the node is confirmed gone and the volume is force-detached, which can take minutes.","The PVC↔PV binding is independent of any Pod, so when a (StatefulSet) Pod is rescheduled, its PVC and the underlying volume detach from the old node and reattach to the new one — the data follows the Pod."]},{"id":"devops/kubernetes_storage_and_state#d6880ba4b053","section":"devops","module":"devops/kubernetes_storage_and_state","moduleName":"kubernetes storage and state","qIndex":11,"difficulty":"advanced","question":"What's the risk of hostPath volumes?","answerFull":"hostPath mounts a node's filesystem path into the Pod. It ties the Pod to that specific node's data (no portability), bypasses storage abstraction, and is a security risk (a compromised Pod can read/write the host filesystem). It's appropriate for node-level agents (DaemonSets needing /var/log or /proc) but not for application data.","correct":"hostPath mounts a node's filesystem path into the Pod.","distractors":["The cloud still considers the volume attached to the dead node until the node is confirmed gone and the volume is force-detached, which can take minutes.","The PVC↔PV binding is independent of any Pod, so when a (StatefulSet) Pod is rescheduled, its PVC and the underlying volume detach from the old node and reattach to the new one — the data follows the Pod.","Cloud block volumes are zone-scoped."]},{"id":"devops/kubernetes_storage_and_state#084245784b13","section":"devops","module":"devops/kubernetes_storage_and_state","moduleName":"kubernetes storage and state","qIndex":12,"difficulty":"advanced","question":"How does storage factor into Kubernetes disaster recovery?","answerFull":"You back up two things: the Kubernetes objects (manifests/etcd) and the PVC data. Velero captures both — taking CSI snapshots of PVCs and exporting object definitions — enabling restore into a new cluster. Reclaim policy Retain, cross-region snapshot copies, and tested restores are the pillars; relying on volume re-attach alone is not DR (see disaster_recovery_and_resilience).","correct":"You back up two things: the Kubernetes objects (manifests/etcd) and the PVC data.","distractors":["With a CSI driver supporting them: a VolumeSnapshot object triggers the driver to snapshot the underlying volume; you restore by creating a PVC with dataSource referencing the snapshot.","It controls what happens to the underlying storage when the PVC is deleted: Delete destroys the volume (data gone); Retain keeps it for manual recovery.","A PersistentVolume (PV) is a cluster resource representing real storage with its own lifecycle."]},{"id":"devops/kubernetes_workloads_and_objects#ccfa4bdff1c3","section":"devops","module":"devops/kubernetes_workloads_and_objects","moduleName":"kubernetes workloads and objects","qIndex":1,"difficulty":"core","question":"What is a Pod and why not deploy bare Pods?","answerFull":"A Pod is the smallest deployable unit: one or more containers sharing a network namespace (same IP, localhost) and volumes, always co-scheduled. You don't deploy bare Pods in production because they have no self-healing — if the node dies, the Pod is gone. A controller (Deployment/StatefulSet) recreates Pods to maintain desired count, giving you self-healing, scaling, and rolling updates.","correct":"A Pod is the smallest deployable unit: one or more containers sharing a network namespace (same IP, localhost) and volumes, always co-scheduled.","distractors":["There's no readiness probe (or it's misconfigured), so the pod joins the Service's endpoints as soon as the container process starts — before the app has loaded config/warmed up.","When you need exactly one Pod per node (or per matching node): log shippers (Fluent Bit), metrics/node exporters, CNI agents, security agents.","requests reserve resources for scheduling and determine QoS class (Guaranteed if requests==limits for all containers, else Burstable, else BestEffort), influencing eviction order."]},{"id":"devops/kubernetes_workloads_and_objects#30d563ab9811","section":"devops","module":"devops/kubernetes_workloads_and_objects","moduleName":"kubernetes workloads and objects","qIndex":2,"difficulty":"core","question":"Deployment vs StatefulSet — when each?","answerFull":"Use a Deployment for stateless workloads: Pods are interchangeable (random name suffixes), share/lack storage, and roll out/back freely. Use a StatefulSet when Pods need stable identity (ordinal names like db-0), stable per-Pod persistent storage (via volumeClaimTemplates), and ordered startup/rollout — i.e., databases, Kafka, Zookeeper. Using a StatefulSet for a stateless app just adds slower, ordered rollouts for no benefit.","correct":"Use a Deployment for stateless workloads: Pods are interchangeable (random name suffixes), share/lack storage, and roll out/back freely.","distractors":["A Service has a label selector; the EndpointSlice controller watches Pods matching that selector and maintains EndpointSlices listing the IPs of *ready* Pods.","kubectl rollout undo deployment/web reverts to the previous ReplicaSet (Kubernetes keeps old ReplicaSets per revisionHistoryLimit).","A Pod is the smallest deployable unit: one or more containers sharing a network namespace (same IP, localhost) and volumes, always co-scheduled."]},{"id":"devops/kubernetes_workloads_and_objects#ff3d70fa9488","section":"devops","module":"devops/kubernetes_workloads_and_objects","moduleName":"kubernetes workloads and objects","qIndex":3,"difficulty":"core","question":"Readiness vs liveness vs startup probes?","answerFull":"Readiness answers \"can this pod serve traffic now?\" — failure removes it from Service endpoints (no restart). Liveness answers \"is this pod permanently broken?\" — failure restarts the container. Startup answers \"has it finished booting?\" — it holds off liveness checks until the app is up, preventing slow-starting apps from being killed mid-boot. Conflating readiness and liveness causes either dropped traffic or restart storms.","correct":"Readiness answers \"can this pod serve traffic now?\" — failure removes it from Service endpoints (no restart).","distractors":["When you need exactly one Pod per node (or per matching node): log shippers (Fluent Bit), metrics/node exporters, CNI agents, security agents.","A Service has a label selector; the EndpointSlice controller watches Pods matching that selector and maintains EndpointSlices listing the IPs of *ready* Pods.","A headless Service (clusterIP: None) returns the individual Pod IPs via DNS instead of a single virtual IP."]},{"id":"devops/kubernetes_workloads_and_objects#5bfef7416e2f","section":"devops","module":"devops/kubernetes_workloads_and_objects","moduleName":"kubernetes workloads and objects","qIndex":4,"difficulty":"core","question":"How does a zero-downtime rolling update work?","answerFull":"With strategy: RollingUpdate and maxUnavailable: 0, maxSurge: 1, the controller creates one new Pod (surge), waits for its readiness probe to pass, then terminates one old Pod — repeating until all are new. Because old Pods stay in the Service until new ones are Ready (gated by readiness), there's always the full capacity of ready Pods serving traffic, so no requests are dropped.","correct":"With strategy: RollingUpdate and maxUnavailable: 0, maxSurge: 1, the controller creates one new Pod (surge), waits for its readiness probe to pass, then terminates one old Pod — repeating until all are new.","distractors":["A Pod is the smallest deployable unit: one or more containers sharing a network namespace (same IP, localhost) and volumes, always co-scheduled.","Readiness answers \"can this pod serve traffic now?\" — failure removes it from Service endpoints (no restart).","A Service has a label selector; the EndpointSlice controller watches Pods matching that selector and maintains EndpointSlices listing the IPs of *ready* Pods."]},{"id":"devops/kubernetes_workloads_and_objects#e3c0ebddfc60","section":"devops","module":"devops/kubernetes_workloads_and_objects","moduleName":"kubernetes workloads and objects","qIndex":5,"difficulty":"core","question":"How do Services find their Pods, and what is an EndpointSlice?","answerFull":"A Service has a label selector; the EndpointSlice controller watches Pods matching that selector and maintains EndpointSlices listing the IPs of *ready* Pods. kube-proxy programs routing from the Service's virtual IP to those Pod IPs. If labels don't match (or no Pods are ready), the EndpointSlice is empty and connections fail — a common \"Service routes to nothing\" bug.","correct":"A Service has a label selector; the EndpointSlice controller watches Pods matching that selector and maintains EndpointSlices listing the IPs of *ready* Pods.","distractors":["There's no readiness probe (or it's misconfigured), so the pod joins the Service's endpoints as soon as the container process starts — before the app has loaded config/warmed up.","With strategy: RollingUpdate and maxUnavailable: 0, maxSurge: 1, the controller creates one new Pod (surge), waits for its readiness probe to pass, then terminates one old Pod — repeating until all are new.","A headless Service (clusterIP: None) returns the individual Pod IPs via DNS instead of a single virtual IP."]},{"id":"devops/kubernetes_workloads_and_objects#4315f3b912c5","section":"devops","module":"devops/kubernetes_workloads_and_objects","moduleName":"kubernetes workloads and objects","qIndex":6,"difficulty":"intermediate","question":"What's the difference between a Secret and a ConfigMap, and is a Secret encrypted?","answerFull":"Both inject config (as env vars or mounted files), but Secrets are intended for sensitive data and are base64-*encoded*, not encrypted, by default — anyone with read RBAC or etcd access can decode them. For real protection you enable encryption-at-rest for etcd, restrict RBAC, and/or use an external store (Vault) via the External Secrets Operator. ConfigMaps are for non-sensitive config.","correct":"Both inject config (as env vars or mounted files), but Secrets are intended for sensitive data and are base64-*encoded*, not encrypted, by default — anyone with read RBAC or etcd access can decode them.","distractors":["There's no readiness probe (or it's misconfigured), so the pod joins the Service's endpoints as soon as the container process starts — before the app has loaded config/warmed up.","A Pod is the smallest deployable unit: one or more containers sharing a network namespace (same IP, localhost) and volumes, always co-scheduled.","A Service has a label selector; the EndpointSlice controller watches Pods matching that selector and maintains EndpointSlices listing the IPs of *ready* Pods."]},{"id":"devops/kubernetes_workloads_and_objects#d459b2a7ebf2","section":"devops","module":"devops/kubernetes_workloads_and_objects","moduleName":"kubernetes workloads and objects","qIndex":7,"difficulty":"intermediate","question":"When do you use a DaemonSet?","answerFull":"When you need exactly one Pod per node (or per matching node): log shippers (Fluent Bit), metrics/node exporters, CNI agents, security agents. The DaemonSet controller adds a Pod when a node joins and removes it when the node leaves, so coverage tracks the node set automatically.","correct":"When you need exactly one Pod per node (or per matching node): log shippers (Fluent Bit), metrics/node exporters, CNI agents, security agents.","distractors":["Readiness answers \"can this pod serve traffic now?\" — failure removes it from Service endpoints (no restart).","There's no readiness probe (or it's misconfigured), so the pod joins the Service's endpoints as soon as the container process starts — before the app has loaded config/warmed up.","A Service has a label selector; the EndpointSlice controller watches Pods matching that selector and maintains EndpointSlices listing the IPs of *ready* Pods."]},{"id":"devops/kubernetes_workloads_and_objects#edfdab0d2cd3","section":"devops","module":"devops/kubernetes_workloads_and_objects","moduleName":"kubernetes workloads and objects","qIndex":9,"difficulty":"intermediate","question":"A new pod gets traffic before it's ready and returns 5xx. Why and fix?","answerFull":"There's no readiness probe (or it's misconfigured), so the pod joins the Service's endpoints as soon as the container process starts — before the app has loaded config/warmed up. Add a readiness probe hitting a real health endpoint; combined with maxUnavailable: 0, the old pod keeps serving until the new one reports ready, eliminating the gap.","correct":"There's no readiness probe (or it's misconfigured), so the pod joins the Service's endpoints as soon as the container process starts — before the app has loaded config/warmed up.","distractors":["Readiness answers \"can this pod serve traffic now?\" — failure removes it from Service endpoints (no restart).","A headless Service (clusterIP: None) returns the individual Pod IPs via DNS instead of a single virtual IP.","When you need exactly one Pod per node (or per matching node): log shippers (Fluent Bit), metrics/node exporters, CNI agents, security agents."]},{"id":"devops/kubernetes_workloads_and_objects#ed8c704bc3f1","section":"devops","module":"devops/kubernetes_workloads_and_objects","moduleName":"kubernetes workloads and objects","qIndex":10,"difficulty":"intermediate","question":"What do requests and limits do at the object level?","answerFull":"requests reserve resources for scheduling and determine QoS class (Guaranteed if requests==limits for all containers, else Burstable, else BestEffort), influencing eviction order. limits are hard ceilings: a CPU limit throttles, a memory limit triggers an OOM kill on breach. They map directly to cgroup settings on the node (see linux_and_os_fundamentals).","correct":"requests reserve resources for scheduling and determine QoS class (Guaranteed if requests==limits for all containers, else Burstable, else BestEffort), influencing eviction order.","distractors":["A Pod is the smallest deployable unit: one or more containers sharing a network namespace (same IP, localhost) and volumes, always co-scheduled.","A Service has a label selector; the EndpointSlice controller watches Pods matching that selector and maintains EndpointSlices listing the IPs of *ready* Pods.","A headless Service (clusterIP: None) returns the individual Pod IPs via DNS instead of a single virtual IP."]},{"id":"devops/kubernetes_workloads_and_objects#484d7b8d8b22","section":"devops","module":"devops/kubernetes_workloads_and_objects","moduleName":"kubernetes workloads and objects","qIndex":11,"difficulty":"advanced","question":"How do you roll back a bad Deployment?","answerFull":"kubectl rollout undo deployment/web reverts to the previous ReplicaSet (Kubernetes keeps old ReplicaSets per revisionHistoryLimit). kubectl rollout history shows revisions; --to-revision=N targets a specific one. Because the old ReplicaSet still exists (scaled to 0), rollback is just scaling it back up and the new one down — fast and safe.","correct":"kubectl rollout undo deployment/web reverts to the previous ReplicaSet (Kubernetes keeps old ReplicaSets per revisionHistoryLimit).","distractors":["Use a Deployment for stateless workloads: Pods are interchangeable (random name suffixes), share/lack storage, and roll out/back freely.","With strategy: RollingUpdate and maxUnavailable: 0, maxSurge: 1, the controller creates one new Pod (surge), waits for its readiness probe to pass, then terminates one old Pod — repeating until all are new.","A Pod is the smallest deployable unit: one or more containers sharing a network namespace (same IP, localhost) and volumes, always co-scheduled."]},{"id":"devops/kubernetes_workloads_and_objects#63a26b3e7ec9","section":"devops","module":"devops/kubernetes_workloads_and_objects","moduleName":"kubernetes workloads and objects","qIndex":12,"difficulty":"advanced","question":"What is a headless Service and why do StatefulSets use one?","answerFull":"A headless Service (clusterIP: None) returns the individual Pod IPs via DNS instead of a single virtual IP. StatefulSets pair with one so each Pod gets a stable DNS name (pg-0.pg-headless.ns.svc), letting clients address specific members — essential for clustered systems where you must reach a particular replica (e.g., a primary) rather than load-balance across all.","correct":"A headless Service (clusterIP: None) returns the individual Pod IPs via DNS instead of a single virtual IP.","distractors":["When you need exactly one Pod per node (or per matching node): log shippers (Fluent Bit), metrics/node exporters, CNI agents, security agents.","A Service has a label selector; the EndpointSlice controller watches Pods matching that selector and maintains EndpointSlices listing the IPs of *ready* Pods.","Readiness answers \"can this pod serve traffic now?\" — failure removes it from Service endpoints (no restart)."]},{"id":"devops/linux_and_os_fundamentals#10f6fca8c59a","section":"devops","module":"devops/linux_and_os_fundamentals","moduleName":"linux and os fundamentals","qIndex":1,"difficulty":"core","question":"What actually is a container at the kernel level?","answerFull":"A container is an ordinary Linux process placed into its own set of namespaces and constrained by cgroups, optionally with reduced capabilities and a pivoted root filesystem. There is no \"container\" object in the kernel — docker run calls clone() with CLONE_NEW* flags, writes cgroup limits, and execve()s the entrypoint. This is why containers share the host kernel and start in milliseconds, unlike VMs.","correct":"A container is an ordinary Linux process placed into its own set of namespaces and constrained by cgroups, optionally with reduced capabilities and a pivoted root filesystem.","distractors":["It computes oom_score per process, roughly proportional to memory footprint, adjusted by oom_score_adj (-1000 = never kill, +1000 = kill first).","It's the per-process soft limit on open file descriptors (default often 1024).","Use nsenter --target <host-pid> --mount --net --pid to enter the container's namespaces with host tools, or kubectl debug ephemeral containers."]},{"id":"devops/linux_and_os_fundamentals#daf33111c9c9","section":"devops","module":"devops/linux_and_os_fundamentals","moduleName":"linux and os fundamentals","qIndex":2,"difficulty":"core","question":"A pod is OOMKilled but kubectl top node shows free memory. Why?","answerFull":"The container exceeded its cgroup memory.max (its resources.limits.memory), triggering a *cgroup-local* OOM kill independent of node-level free memory. Requests affect scheduling and QoS; limits are a hard ceiling enforced per cgroup. Check dmesg for \"Memory cgroup out of memory\" and memory.events oom_kill count.","correct":"The container exceeded its cgroup memory.max (its resources.limits.memory), triggering a *cgroup-local* OOM kill independent of node-level free memory.","distractors":["Use nsenter --target <host-pid> --mount --net --pid to enter the container's namespaces with host tools, or kubectl debug ephemeral containers.","v2 is a single unified hierarchy with better memory pressure control (memory.high throttles before memory.max OOMs) and accurate accounting.","Requests are a scheduling guarantee (cpu.weight / reserved memory used by the scheduler to place the pod); limits are enforcement — cpu.max (CFS quota, causes throttling) and memory.max (causes OOM kill on breach)."]},{"id":"devops/linux_and_os_fundamentals#7090b72fb9ec","section":"devops","module":"devops/linux_and_os_fundamentals","moduleName":"linux and os fundamentals","qIndex":3,"difficulty":"core","question":"Difference between a process's requests and limits, and how do they map to the kernel?","answerFull":"Requests are a scheduling guarantee (cpu.weight / reserved memory used by the scheduler to place the pod); limits are enforcement — cpu.max (CFS quota, causes throttling) and memory.max (causes OOM kill on breach). Requests never throttle or kill; limits do.","correct":"Requests are a scheduling guarantee (cpu.weight / reserved memory used by the scheduler to place the pod); limits are enforcement — cpu.max (CFS quota, causes throttling) and memory.max (causes OOM kill on breach).","distractors":["v2 is a single unified hierarchy with better memory pressure control (memory.high throttles before memory.max OOMs) and accurate accounting.","It's the per-process soft limit on open file descriptors (default often 1024).","SIGTERM (15) is catchable: a well-behaved app traps it, stops accepting new work, drains in-flight requests, then exits."]},{"id":"devops/linux_and_os_fundamentals#814495cd5911","section":"devops","module":"devops/linux_and_os_fundamentals","moduleName":"linux and os fundamentals","qIndex":4,"difficulty":"core","question":"SIGTERM vs SIGKILL — why does graceful shutdown sometimes fail?","answerFull":"SIGTERM (15) is catchable: a well-behaved app traps it, stops accepting new work, drains in-flight requests, then exits. SIGKILL (9) is uncatchable — the kernel destroys the process immediately. Shutdown \"fails\" (drops requests) when the app ignores SIGTERM or takes longer than terminationGracePeriodSeconds, after which Kubernetes escalates to SIGKILL.","correct":"SIGTERM (15) is catchable: a well-behaved app traps it, stops accepting new work, drains in-flight requests, then exits.","distractors":["Requests are a scheduling guarantee (cpu.weight / reserved memory used by the scheduler to place the pod); limits are enforcement — cpu.max (CFS quota, causes throttling) and memory.max (causes OOM kill on breach).","A container is an ordinary Linux process placed into its own set of namespaces and constrained by cgroups, optionally with reduced capabilities and a pivoted root filesystem.","A zombie (<defunct>) is a terminated child whose exit status hasn't been reaped by its parent via wait()."]},{"id":"devops/linux_and_os_fundamentals#06e13af908da","section":"devops","module":"devops/linux_and_os_fundamentals","moduleName":"linux and os fundamentals","qIndex":5,"difficulty":"core","question":"What does the OOM killer use to choose a victim?","answerFull":"It computes oom_score per process, roughly proportional to memory footprint, adjusted by oom_score_adj (-1000 = never kill, +1000 = kill first). Under whole-node pressure it kills the highest scorer; under cgroup pressure it kills within the offending cgroup. Kubernetes sets oom_score_adj so BestEffort pods die before Guaranteed ones.","correct":"It computes oom_score per process, roughly proportional to memory footprint, adjusted by oom_score_adj (-1000 = never kill, +1000 = kill first).","distractors":["v2 is a single unified hierarchy with better memory pressure control (memory.high throttles before memory.max OOMs) and accurate accounting.","A container is an ordinary Linux process placed into its own set of namespaces and constrained by cgroups, optionally with reduced capabilities and a pivoted root filesystem.","Requests are a scheduling guarantee (cpu.weight / reserved memory used by the scheduler to place the pod); limits are enforcement — cpu.max (CFS quota, causes throttling) and memory.max (causes OOM kill on breach)."]},{"id":"devops/linux_and_os_fundamentals#b9f431af8729","section":"devops","module":"devops/linux_and_os_fundamentals","moduleName":"linux and os fundamentals","qIndex":6,"difficulty":"intermediate","question":"Why might a process show 100% CPU but the service is slow, vs low CPU but slow?","answerFull":"High CPU + slow = genuinely CPU-bound (profile the hot path). Low CPU + slow + rising cpu.cfs_throttled = CFS throttling against a CPU limit. Low CPU + slow + high iowait (vmstat/iostat) = blocked on disk/network I/O. The metrics distinguish the three.","correct":"High CPU + slow = genuinely CPU-bound (profile the hot path).","distractors":["Linux uses free RAM as page cache for file I/O; tools that report \"used\" memory including cache look alarming but the cache is reclaimable.","It computes oom_score per process, roughly proportional to memory footprint, adjusted by oom_score_adj (-1000 = never kill, +1000 = kill first).","A container is an ordinary Linux process placed into its own set of namespaces and constrained by cgroups, optionally with reduced capabilities and a pivoted root filesystem."]},{"id":"devops/linux_and_os_fundamentals#2e1a18a13514","section":"devops","module":"devops/linux_and_os_fundamentals","moduleName":"linux and os fundamentals","qIndex":7,"difficulty":"intermediate","question":"What is a zombie process and how does it arise in containers?","answerFull":"A zombie (<defunct>) is a terminated child whose exit status hasn't been reaped by its parent via wait(). In containers, if PID 1 is a shell or an app that spawns children but never reaps them, zombies accumulate and can exhaust the PID table. Fix: use an init like tini/dumb-init as PID 1, or shareProcessNamespace with a proper init.","correct":"A zombie (<defunct>) is a terminated child whose exit status hasn't been reaped by its parent via wait().","distractors":["Use nsenter --target <host-pid> --mount --net --pid to enter the container's namespaces with host tools, or kubectl debug ephemeral containers.","A container is an ordinary Linux process placed into its own set of namespaces and constrained by cgroups, optionally with reduced capabilities and a pivoted root filesystem.","It computes oom_score per process, roughly proportional to memory footprint, adjusted by oom_score_adj (-1000 = never kill, +1000 = kill first)."]},{"id":"devops/linux_and_os_fundamentals#ce29a9f63bd3","section":"devops","module":"devops/linux_and_os_fundamentals","moduleName":"linux and os fundamentals","qIndex":8,"difficulty":"intermediate","question":"How do you debug a running container from the host without a shell inside it?","answerFull":"Use nsenter --target <host-pid> --mount --net --pid to enter the container's namespaces with host tools, or kubectl debug ephemeral containers. Distroless/scratch images have no shell, so host-side nsenter, crictl, strace -p, and /proc/<pid>/ inspection are essential.","correct":"Use nsenter --target <host-pid> --mount --net --pid to enter the container's namespaces with host tools, or kubectl debug ephemeral containers.","distractors":["Linux uses free RAM as page cache for file I/O; tools that report \"used\" memory including cache look alarming but the cache is reclaimable.","Capabilities split root's powers into ~40 units (CAP_NET_BIND_SERVICE, CAP_SYS_ADMIN, etc.).","The container exceeded its cgroup memory.max (its resources.limits.memory), triggering a *cgroup-local* OOM kill independent of node-level free memory."]},{"id":"devops/linux_and_os_fundamentals#9406094cecc4","section":"devops","module":"devops/linux_and_os_fundamentals","moduleName":"linux and os fundamentals","qIndex":9,"difficulty":"intermediate","question":"What are Linux capabilities and why drop them?","answerFull":"Capabilities split root's powers into ~40 units (CAP_NET_BIND_SERVICE, CAP_SYS_ADMIN, etc.). Dropping all and adding back only what's needed shrinks the blast radius of a compromise — a container that only needs to bind port 80 gets CAP_NET_BIND_SERVICE and nothing else. securityContext.capabilities.drop: [\"ALL\"] is the hardened default.","correct":"Capabilities split root's powers into ~40 units (CAP_NET_BIND_SERVICE, CAP_SYS_ADMIN, etc.).","distractors":["A container is an ordinary Linux process placed into its own set of namespaces and constrained by cgroups, optionally with reduced capabilities and a pivoted root filesystem.","Use nsenter --target <host-pid> --mount --net --pid to enter the container's namespaces with host tools, or kubectl debug ephemeral containers.","Linux uses free RAM as page cache for file I/O; tools that report \"used\" memory including cache look alarming but the cache is reclaimable."]},{"id":"devops/linux_and_os_fundamentals#2c2df7ba6cc4","section":"devops","module":"devops/linux_and_os_fundamentals","moduleName":"linux and os fundamentals","qIndex":10,"difficulty":"intermediate","question":"cgroups v1 vs v2 — why does it matter for Kubernetes?","answerFull":"v2 is a single unified hierarchy with better memory pressure control (memory.high throttles before memory.max OOMs) and accurate accounting. Kubernetes added GA cgroup v2 support in 1.25; on cgroup v2 nodes, memory QoS and the OOM behavior are more predictable. Mismatches between the runtime's expectation and the node's cgroup version cause subtle resource-enforcement bugs.","correct":"v2 is a single unified hierarchy with better memory pressure control (memory.high throttles before memory.max OOMs) and accurate accounting.","distractors":["A container is an ordinary Linux process placed into its own set of namespaces and constrained by cgroups, optionally with reduced capabilities and a pivoted root filesystem.","Requests are a scheduling guarantee (cpu.weight / reserved memory used by the scheduler to place the pod); limits are enforcement — cpu.max (CFS quota, causes throttling) and memory.max (causes OOM kill on breach).","High CPU + slow = genuinely CPU-bound (profile the hot path)."]},{"id":"devops/linux_and_os_fundamentals#8dca35db8b7a","section":"devops","module":"devops/linux_and_os_fundamentals","moduleName":"linux and os fundamentals","qIndex":11,"difficulty":"advanced","question":"What does ulimit -n control and where does it bite in production?","answerFull":"It's the per-process soft limit on open file descriptors (default often 1024). Sockets are FDs, so high-connection servers (proxies, brokers, DB pools) hit EMFILE (\"too many open files\") and stop accepting connections while CPU/memory look fine. Raise it via systemd LimitNOFILE or pod limits, and monitor open FDs against the ceiling.","correct":"It's the per-process soft limit on open file descriptors (default often 1024).","distractors":["v2 is a single unified hierarchy with better memory pressure control (memory.high throttles before memory.max OOMs) and accurate accounting.","Linux uses free RAM as page cache for file I/O; tools that report \"used\" memory including cache look alarming but the cache is reclaimable.","A container is an ordinary Linux process placed into its own set of namespaces and constrained by cgroups, optionally with reduced capabilities and a pivoted root filesystem."]},{"id":"devops/linux_and_os_fundamentals#4e07cab7f723","section":"devops","module":"devops/linux_and_os_fundamentals","moduleName":"linux and os fundamentals","qIndex":12,"difficulty":"advanced","question":"How does the page cache affect \"memory usage\" readings?","answerFull":"Linux uses free RAM as page cache for file I/O; tools that report \"used\" memory including cache look alarming but the cache is reclaimable. free -m distinguishes used from buff/cache and available. In cgroups, page cache counts toward memory.current, which is why a file-heavy container can approach memory.max from cache alone and trigger reclaim.","correct":"Linux uses free RAM as page cache for file I/O; tools that report \"used\" memory including cache look alarming but the cache is reclaimable.","distractors":["A container is an ordinary Linux process placed into its own set of namespaces and constrained by cgroups, optionally with reduced capabilities and a pivoted root filesystem.","v2 is a single unified hierarchy with better memory pressure control (memory.high throttles before memory.max OOMs) and accurate accounting.","Use nsenter --target <host-pid> --mount --net --pid to enter the container's namespaces with host tools, or kubectl debug ephemeral containers."]},{"id":"devops/ml_platform_and_gpu_infrastructure#06451913efad","section":"devops","module":"devops/ml_platform_and_gpu_infrastructure","moduleName":"ml platform and gpu infrastructure","qIndex":1,"difficulty":"core","question":"Why can't you request a fraction of a GPU like you request 0.5 CPU in Kubernetes?","answerFull":"Because GPUs are exposed as extended resources, which Kubernetes only supports as non-overcommittable integers — request must equal limit and must be a whole number. CPU and memory are first-class divisible resources the kubelet can throttle via cgroups; a GPU has no equivalent kernel-level fractional throttling that K8s understands. Fractional sharing is therefore faked at the device-plugin layer: MIG advertises smaller named resources (nvidia.com/mig-1g.10gb), and time-slicing advertises N integer replicas of one physical card. In practice you pick MIG when you need isolation and time-slicing when you need packing.","correct":"Because GPUs are exposed as extended resources, which Kubernetes only supports as non-overcommittable integers — request must equal limit and must be a whole number.","distractors":["Allocation (nvidia.com/gpu checked out) is not utilization.","NVLink is NVIDIA's high-bandwidth GPU-to-GPU interconnect — ~600 GB/s on A100 vs ~64 GB/s over PCIe Gen4.","Scale-to-zero (via KServe/Knative) removes all replicas of an idle model endpoint so you stop paying for the GPU when there's no traffic — essential when you serve hundreds of rarely-used models."]},{"id":"devops/ml_platform_and_gpu_infrastructure#9a3e32070e19","section":"devops","module":"devops/ml_platform_and_gpu_infrastructure","moduleName":"ml platform and gpu infrastructure","qIndex":3,"difficulty":"core","question":"MIG vs time-slicing — when do you choose each?","answerFull":"MIG gives hardware-enforced memory and fault isolation by physically partitioning the GPU into fixed-size instances (e.g., seven 1g.10gb slices on an A100). Time-slicing gives no isolation — it oversubscribes one GPU as N logical replicas that share all memory and SMs, round-robin time-multiplexed. Choose MIG for multi-tenant production inference where one tenant must never crash or read another's memory, accepting fixed slice sizes and some fragmentation. Choose time-slicing for trusted, bursty, or dev/notebook workloads where you want maximum packing and can tolerate noisy-neighbor latency variance. Never time-slice a strict-latency-SLA endpoint — p99 becomes unpredictable.","correct":"MIG gives hardware-enforced memory and fault isolation by physically partitioning the GPU into fixed-size instances (e.g., seven 1g.10gb slices on an A100).","distractors":["At thousands of nodes, etcd write throughput, API-server request latency, kube-scheduler decision time, CoreDNS QPS, and CNI IP allocation all hit limits — OpenAI documented exactly this past ~7,500 nodes.","NVLink is NVIDIA's high-bandwidth GPU-to-GPU interconnect — ~600 GB/s on A100 vs ~64 GB/s over PCIe Gen4.","Allocation (nvidia.com/gpu checked out) is not utilization."]},{"id":"devops/ml_platform_and_gpu_infrastructure#6285f372c509","section":"devops","module":"devops/ml_platform_and_gpu_infrastructure","moduleName":"ml platform and gpu infrastructure","qIndex":5,"difficulty":"core","question":"Your GPU dashboard shows 100% allocation but the ML team complains training is slow. How do you diagnose?","answerFull":"Allocation (nvidia.com/gpu checked out) is not utilization. I'd look at DCGM: DCGM_FI_DEV_GPU_UTIL (SM busy %) and DCGM_FI_DEV_FB_USED (memory). If GPU util is low (say 15%) while allocated, the bottleneck is upstream — usually the data pipeline (CPU-bound preprocessing, slow storage reads starving the GPU) or small batch sizes. I'd check the input pipeline throughput, dataloader worker count, and whether data sits on slow EBS vs a local NVMe/FSx cache. The card is reserved but starved; the fix is feeding it faster, not adding more GPUs.","correct":"Allocation (nvidia.com/gpu checked out) is not utilization.","distractors":["NVLink is NVIDIA's high-bandwidth GPU-to-GPU interconnect — ~600 GB/s on A100 vs ~64 GB/s over PCIe Gen4.","Spot instances can be reclaimed with a 2-minute warning, which would otherwise throw away hours of training.","MIG gives hardware-enforced memory and fault isolation by physically partitioning the GPU into fixed-size instances (e.g., seven 1g.10gb slices on an A100)."]},{"id":"devops/ml_platform_and_gpu_infrastructure#ae0df457dda4","section":"devops","module":"devops/ml_platform_and_gpu_infrastructure","moduleName":"ml platform and gpu infrastructure","qIndex":7,"difficulty":"intermediate","question":"Why separate training and serving onto different node pools?","answerFull":"They have opposite profiles. Training is long, bursty, fault-tolerant, wants whole/many GPUs, and is happy on cheap Spot. Serving is steady, latency-sensitive, wants fractional GPUs (MIG), and needs stable On-Demand capacity. On a shared pool, a big training job either starves serving of GPUs or forces you to over-provision; Spot interruptions meant for training would kill serving Pods; and consolidation churn destabilizes endpoints. Separate pools let you price each correctly (Spot for training, On-Demand/Savings Plans for serving) and isolate their failure modes.","correct":"They have opposite profiles.","distractors":["Spot instances can be reclaimed with a 2-minute warning, which would otherwise throw away hours of training.","Allocation (nvidia.com/gpu checked out) is not utilization.","At thousands of nodes, etcd write throughput, API-server request latency, kube-scheduler decision time, CoreDNS QPS, and CNI IP allocation all hit limits — OpenAI documented exactly this past ~7,500 nodes."]},{"id":"devops/ml_platform_and_gpu_infrastructure#0c8cf8c9f3b0","section":"devops","module":"devops/ml_platform_and_gpu_infrastructure","moduleName":"ml platform and gpu infrastructure","qIndex":8,"difficulty":"intermediate","question":"What's the risk of running GPUs on Spot for training, and how do you mitigate it?","answerFull":"Spot instances can be reclaimed with a 2-minute warning, which would otherwise throw away hours of training. Mitigation is checkpointing: save model + optimizer state to durable storage (S3/FSx) every N steps or minutes, and on restart resume from the latest checkpoint. Combine with Karpenter's Spot handling and a node-termination handler that catches the interruption signal and triggers a final checkpoint. For very large jobs use elastic training (PyTorch Elastic / torchrun) so the job survives losing a worker by rescaling rather than dying. The ~70% Spot discount is worth it precisely because training is restartable; serving usually is not.","correct":"Spot instances can be reclaimed with a 2-minute warning, which would otherwise throw away hours of training.","distractors":["Because GPUs are exposed as extended resources, which Kubernetes only supports as non-overcommittable integers — request must equal limit and must be a whole number.","MIG gives hardware-enforced memory and fault isolation by physically partitioning the GPU into fixed-size instances (e.g., seven 1g.10gb slices on an A100).","Allocation (nvidia.com/gpu checked out) is not utilization."]},{"id":"devops/ml_platform_and_gpu_infrastructure#90fe64c3ac6f","section":"devops","module":"devops/ml_platform_and_gpu_infrastructure","moduleName":"ml platform and gpu infrastructure","qIndex":10,"difficulty":"intermediate","question":"Why does the GPU control plane become the bottleneck before the GPUs at very large scale?","answerFull":"At thousands of nodes, etcd write throughput, API-server request latency, kube-scheduler decision time, CoreDNS QPS, and CNI IP allocation all hit limits — OpenAI documented exactly this past ~7,500 nodes. GPU scheduling is low-volume but the surrounding churn (Pod creates/deletes, status updates, DNS lookups, metrics scrapes) scales with node and Pod count. Mitigations: tune etcd (separate disks, raise quota), shard or scale the API server, reduce scheduler load with gang scheduling, cap metrics cardinality, and move heavy DNS to NodeLocal DNSCache. The lesson: a GPU cluster is a Kubernetes-control-plane scaling problem wearing a GPU costume.","correct":"At thousands of nodes, etcd write throughput, API-server request latency, kube-scheduler decision time, CoreDNS QPS, and CNI IP allocation all hit limits — OpenAI documented exactly this past ~7,500 nodes.","distractors":["Scale-to-zero (via KServe/Knative) removes all replicas of an idle model endpoint so you stop paying for the GPU when there's no traffic — essential when you serve hundreds of rarely-used models.","NVLink is NVIDIA's high-bandwidth GPU-to-GPU interconnect — ~600 GB/s on A100 vs ~64 GB/s over PCIe Gen4.","Because GPUs are exposed as extended resources, which Kubernetes only supports as non-overcommittable integers — request must equal limit and must be a whole number."]},{"id":"devops/ml_platform_and_gpu_infrastructure#ab1be6bf698d","section":"devops","module":"devops/ml_platform_and_gpu_infrastructure","moduleName":"ml platform and gpu infrastructure","qIndex":11,"difficulty":"advanced","question":"What is NVLink and why should the scheduler care about GPU topology?","answerFull":"NVLink is NVIDIA's high-bandwidth GPU-to-GPU interconnect — ~600 GB/s on A100 vs ~64 GB/s over PCIe Gen4. For multi-GPU jobs doing frequent all-reduce (data-parallel training), GPUs connected by NVLink exchange gradients ~10× faster than over PCIe or across nodes via the network. So a topology-unaware scheduler that scatters a 4-GPU Pod across PCIe-only or cross-node placements can bottleneck on interconnect. Topology-aware scheduling (Volcano, or the device plugin's topology hints) packs co-dependent GPUs onto the same NVLink domain. For single-GPU inference, topology is irrelevant.","correct":"NVLink is NVIDIA's high-bandwidth GPU-to-GPU interconnect — ~600 GB/s on A100 vs ~64 GB/s over PCIe Gen4.","distractors":["Scale-to-zero (via KServe/Knative) removes all replicas of an idle model endpoint so you stop paying for the GPU when there's no traffic — essential when you serve hundreds of rarely-used models.","MIG gives hardware-enforced memory and fault isolation by physically partitioning the GPU into fixed-size instances (e.g., seven 1g.10gb slices on an A100).","Allocation (nvidia.com/gpu checked out) is not utilization."]},{"id":"devops/ml_platform_and_gpu_infrastructure#986794ce946e","section":"devops","module":"devops/ml_platform_and_gpu_infrastructure","moduleName":"ml platform and gpu infrastructure","qIndex":13,"difficulty":"advanced","question":"What does scale-to-zero mean for GPU serving and what's the catch?","answerFull":"Scale-to-zero (via KServe/Knative) removes all replicas of an idle model endpoint so you stop paying for the GPU when there's no traffic — essential when you serve hundreds of rarely-used models. The catch is cold start: the first request after scale-down must wait for a Pod to schedule, a GPU node to possibly be provisioned by Karpenter (minutes), the driver to load, and the multi-GB model weights to download and load into VRAM — easily tens of seconds to minutes. Mitigations: keep a warm pool of GPU nodes, pre-stage images and weights on the node (or a fast FSx/EFS cache), and use a minReplicas=1 for latency-critical models while letting the long tail scale to zero. It's a cost-vs-cold-start tradeoff decided per model.","correct":"Scale-to-zero (via KServe/Knative) removes all replicas of an idle model endpoint so you stop paying for the GPU when there's no traffic — essential when you serve hundreds of rarely-used models.","distractors":["Allocation (nvidia.com/gpu checked out) is not utilization.","NVLink is NVIDIA's high-bandwidth GPU-to-GPU interconnect — ~600 GB/s on A100 vs ~64 GB/s over PCIe Gen4.","MIG gives hardware-enforced memory and fault isolation by physically partitioning the GPU into fixed-size instances (e.g., seven 1g.10gb slices on an A100)."]},{"id":"devops/ml_platform_and_gpu_infrastructure#fb16309a60e1","section":"devops","module":"devops/ml_platform_and_gpu_infrastructure","moduleName":"ml platform and gpu infrastructure","qIndex":14,"difficulty":"advanced","question":"How do you detect and respond to a failing GPU in production?","answerFull":"DCGM exports health signals: ECC double-bit errors (DCGM_FI_DEV_ECC_DBE_VOL_TOTAL), XID errors, throttling (DCGM_FI_DEV_CLOCK_THROTTLE_REASONS), and temperature. A rising uncorrectable ECC count or repeated XID 79 (GPU has fallen off the bus) means the card is degrading. The response: cordon the node, drain GPU Pods (rescheduled elsewhere by the operator/controllers), label it for the GPU Operator's health checks to keep it out of rotation, and replace the instance. NVIDIA's GPU feature discovery + the operator's validation can auto-taint unhealthy GPUs. The principle is the same as any SRE practice — detect via metrics, fence the bad resource, reschedule, replace — but the blast radius is bigger because each card is so expensive.","correct":"DCGM exports health signals: ECC double-bit errors (DCGM_FI_DEV_ECC_DBE_VOL_TOTAL), XID errors, throttling (DCGM_FI_DEV_CLOCK_THROTTLE_REASONS), and temperature.","distractors":["Scale-to-zero (via KServe/Knative) removes all replicas of an idle model endpoint so you stop paying for the GPU when there's no traffic — essential when you serve hundreds of rarely-used models.","Allocation (nvidia.com/gpu checked out) is not utilization.","NVLink is NVIDIA's high-bandwidth GPU-to-GPU interconnect — ~600 GB/s on A100 vs ~64 GB/s over PCIe Gen4."]},{"id":"devops/networking_for_devops#2813dae9f079","section":"devops","module":"devops/networking_for_devops","moduleName":"networking for devops","qIndex":2,"difficulty":"core","question":"L4 vs L7 load balancer — when each?","answerFull":"L4 (NLB) routes on IP:port, is protocol-agnostic, lowest latency, gives a static IP — use for gRPC/TCP, non-HTTP, or ultra-low-latency. L7 (ALB) parses HTTP, enabling path/host routing, header-based rules, WAF, redirects, and sticky sessions — use for web APIs and microservice routing. You often have both: NLB for raw ingress, L7 (ingress controller) inside.","correct":"L4 (NLB) routes on IP:port, is protocol-agnostic, lowest latency, gives a static IP — use for gRPC/TCP, non-HTTP, or ultra-low-latency.","distractors":["openssl s_client -connect host:443 -servername host shows the presented chain, negotiated protocol/cipher, and validity dates; openssl x509 -noout -dates checks expiry.","The LB only routes to targets passing the health check; a failing check removes a target from rotation.","A NAT gateway SNATs many hosts behind one IP; each outbound connection to a given destination consumes a source port from ~64K."]},{"id":"devops/networking_for_devops#fc85b25e151b","section":"devops","module":"devops/networking_for_devops","moduleName":"networking for devops","qIndex":3,"difficulty":"core","question":"Explain CIDR /24 vs /16 and a Kubernetes implication.","answerFull":"/N means N network bits, so 2^(32-N) addresses: /24 = 256, /16 = 65,536. With the AWS VPC CNI, each pod takes a real subnet IP, so a /24 subnet supports ~250 pods. Large clusters exhaust subnet space — you size subnets for peak pod count, and subnet sizing is hard to change later (a one-way door).","correct":"/N means N network bits, so 2^(32-N) addresses: /24 = 256, /16 = 65,536.","distractors":["Edge termination (at the LB) is simplest, offloads crypto, and centralizes cert management, but traffic is plaintext inside the network.","VPC endpoints (Gateway for S3/DynamoDB, Interface/PrivateLink for others) keep traffic on the AWS network without traversing the NAT gateway.","A NAT gateway SNATs many hosts behind one IP; each outbound connection to a given destination consumes a source port from ~64K."]},{"id":"devops/networking_for_devops#794d03624944","section":"devops","module":"devops/networking_for_devops","moduleName":"networking for devops","qIndex":4,"difficulty":"core","question":"Why does \"the request reaches the server but the client times out\"?","answerFull":"Asymmetric connectivity: the inbound path is allowed but the return path isn't. Common causes — a stateless NACL allowing inbound but not outbound ephemeral ports, a missing egress rule, or asymmetric routing where responses take a path with no return route. Security groups avoid this because they're stateful (return traffic is auto-allowed).","correct":"Asymmetric connectivity: the inbound path is allowed but the return path isn't.","distractors":["A security group is stateful: allow inbound on a port and the response is automatically permitted.","openssl s_client -connect host:443 -servername host shows the presented chain, negotiated protocol/cipher, and validity dates; openssl x509 -noout -dates checks expiry.","Edge termination (at the LB) is simplest, offloads crypto, and centralizes cert management, but traffic is plaintext inside the network."]},{"id":"devops/networking_for_devops#58b1258b51e2","section":"devops","module":"devops/networking_for_devops","moduleName":"networking for devops","qIndex":5,"difficulty":"core","question":"Stateful security group vs stateless NACL?","answerFull":"A security group is stateful: allow inbound on a port and the response is automatically permitted. A NACL is stateless and subnet-wide: you must explicitly allow both inbound *and* the outbound ephemeral-port return traffic. NACLs are coarse-grained guardrails; SGs are the primary instance-level control.","correct":"A security group is stateful: allow inbound on a port and the response is automatically permitted.","distractors":["L4 (NLB) routes on IP:port, is protocol-agnostic, lowest latency, gives a static IP — use for gRPC/TCP, non-HTTP, or ultra-low-latency.","A NAT gateway SNATs many hosts behind one IP; each outbound connection to a given destination consumes a source port from ~64K.","Asymmetric connectivity: the inbound path is allowed but the return path isn't."]},{"id":"devops/networking_for_devops#d01937531756","section":"devops","module":"devops/networking_for_devops","moduleName":"networking for devops","qIndex":6,"difficulty":"intermediate","question":"Where should you terminate TLS, and what are the tradeoffs?","answerFull":"Edge termination (at the LB) is simplest, offloads crypto, and centralizes cert management, but traffic is plaintext inside the network. End-to-end/mTLS encrypts the whole path and provides identity (zero-trust) at the cost of cert lifecycle complexity. Re-encryption terminates at the edge for inspection/routing then re-encrypts to the backend — the compliance-friendly middle ground.","correct":"Edge termination (at the LB) is simplest, offloads crypto, and centralizes cert management, but traffic is plaintext inside the network.","distractors":["VPC endpoints (Gateway for S3/DynamoDB, Interface/PrivateLink for others) keep traffic on the AWS network without traversing the NAT gateway.","Asymmetric connectivity: the inbound path is allowed but the return path isn't.","/N means N network bits, so 2^(32-N) addresses: /24 = 256, /16 = 65,536."]},{"id":"devops/networking_for_devops#c5941623a7ce","section":"devops","module":"devops/networking_for_devops","moduleName":"networking for devops","qIndex":7,"difficulty":"intermediate","question":"What is NAT source-port exhaustion and how do you fix it?","answerFull":"A NAT gateway SNATs many hosts behind one IP; each outbound connection to a given destination consumes a source port from ~64K. Thousands of connections to the *same* endpoint exhaust the pool, so new egress connections fail/time out while bandwidth is unused. Fixes: VPC endpoints (PrivateLink) to reach AWS services without NAT, connection pooling/reuse, or more NAT IPs.","correct":"A NAT gateway SNATs many hosts behind one IP; each outbound connection to a given destination consumes a source port from ~64K.","distractors":["L4 (NLB) routes on IP:port, is protocol-agnostic, lowest latency, gives a static IP — use for gRPC/TCP, non-HTTP, or ultra-low-latency.","A security group is stateful: allow inbound on a port and the response is automatically permitted.","VPC endpoints (Gateway for S3/DynamoDB, Interface/PrivateLink for others) keep traffic on the AWS network without traversing the NAT gateway."]},{"id":"devops/networking_for_devops#e36746fcb0bf","section":"devops","module":"devops/networking_for_devops","moduleName":"networking for devops","qIndex":8,"difficulty":"intermediate","question":"How does DNS TTL affect failover, and what's the tradeoff?","answerFull":"TTL is how long resolvers cache a record. Low TTL (e.g., 30–60s) means failover/changes propagate quickly but generates more queries (and cost); high TTL reduces query load but means clients keep hitting a dead/old IP after a change. For failover-critical records, keep TTL low; for stable records, keep it higher.","correct":"TTL is how long resolvers cache a record.","distractors":["/N means N network bits, so 2^(32-N) addresses: /24 = 256, /16 = 65,536.","A NAT gateway SNATs many hosts behind one IP; each outbound connection to a given destination consumes a source port from ~64K.","A security group is stateful: allow inbound on a port and the response is automatically permitted."]},{"id":"devops/networking_for_devops#f001f1fc8db5","section":"devops","module":"devops/networking_for_devops","moduleName":"networking for devops","qIndex":9,"difficulty":"intermediate","question":"What's the ndots:5 problem in Kubernetes?","answerFull":"CoreDNS injects a search list and ndots:5 into pod /etc/resolv.conf. A name with fewer than 5 dots and no trailing dot is tried against each search domain first, so api.example.com triggers several failed lookups (api.example.com.ns.svc.cluster.local, …) before the real one — adding latency and DNS load. Fix: use FQDNs (trailing dot) for external names or tune ndots.","correct":"CoreDNS injects a search list and ndots:5 into pod /etc/resolv.conf.","distractors":["/N means N network bits, so 2^(32-N) addresses: /24 = 256, /16 = 65,536.","A NAT gateway SNATs many hosts behind one IP; each outbound connection to a given destination consumes a source port from ~64K.","A security group is stateful: allow inbound on a port and the response is automatically permitted."]},{"id":"devops/networking_for_devops#987c1eac8de0","section":"devops","module":"devops/networking_for_devops","moduleName":"networking for devops","qIndex":10,"difficulty":"intermediate","question":"How do you debug a TLS handshake failure?","answerFull":"openssl s_client -connect host:443 -servername host shows the presented chain, negotiated protocol/cipher, and validity dates; openssl x509 -noout -dates checks expiry. Common failures: expired cert, missing intermediate (incomplete chain), SNI mismatch (-servername matters), or protocol/cipher mismatch (old client vs TLS 1.3-only server).","correct":"openssl s_client -connect host:443 -servername host shows the presented chain, negotiated protocol/cipher, and validity dates; openssl x509 -noout -dates checks expiry.","distractors":["L4 (NLB) routes on IP:port, is protocol-agnostic, lowest latency, gives a static IP — use for gRPC/TCP, non-HTTP, or ultra-low-latency.","/N means N network bits, so 2^(32-N) addresses: /24 = 256, /16 = 65,536.","A NAT gateway SNATs many hosts behind one IP; each outbound connection to a given destination consumes a source port from ~64K."]},{"id":"devops/networking_for_devops#8739250dc511","section":"devops","module":"devops/networking_for_devops","moduleName":"networking for devops","qIndex":11,"difficulty":"advanced","question":"What does a health check actually control, and what's a safe configuration?","answerFull":"The LB only routes to targets passing the health check; a failing check removes a target from rotation. A too-strict check (auth-required path, 1 failure = drain) can empty the whole pool and cause a self-inflicted outage. Safe config: a dedicated unauthenticated /healthz that reflects real readiness, a sane interval (10–15s), and an unhealthy threshold > 1 to tolerate transient blips.","correct":"The LB only routes to targets passing the health check; a failing check removes a target from rotation.","distractors":["L4 (NLB) routes on IP:port, is protocol-agnostic, lowest latency, gives a static IP — use for gRPC/TCP, non-HTTP, or ultra-low-latency.","/N means N network bits, so 2^(32-N) addresses: /24 = 256, /16 = 65,536.","A NAT gateway SNATs many hosts behind one IP; each outbound connection to a given destination consumes a source port from ~64K."]},{"id":"devops/networking_for_devops#139c33fef8f3","section":"devops","module":"devops/networking_for_devops","moduleName":"networking for devops","qIndex":12,"difficulty":"advanced","question":"Why prefer VPC endpoints over routing AWS traffic through a NAT gateway?","answerFull":"VPC endpoints (Gateway for S3/DynamoDB, Interface/PrivateLink for others) keep traffic on the AWS network without traversing the NAT gateway. Benefits: lower cost (NAT data-processing charges avoided), no source-port exhaustion against high-volume AWS endpoints, and traffic that never leaves the AWS backbone (security/compliance).","correct":"VPC endpoints (Gateway for S3/DynamoDB, Interface/PrivateLink for others) keep traffic on the AWS network without traversing the NAT gateway.","distractors":["A NAT gateway SNATs many hosts behind one IP; each outbound connection to a given destination consumes a source port from ~64K.","/N means N network bits, so 2^(32-N) addresses: /24 = 256, /16 = 65,536.","Edge termination (at the LB) is simplest, offloads crypto, and centralizes cert management, but traffic is plaintext inside the network."]},{"id":"devops/observability_logging#d30d99e73bf2","section":"devops","module":"devops/observability_logging","moduleName":"observability logging","qIndex":1,"difficulty":"core","question":"Why do you need centralized log aggregation instead of reading logs on the host?","answerFull":"In a distributed system, workloads run on ephemeral pods/VMs that are created and destroyed constantly, so local logs disappear when the container dies — exactly when you need them post-incident. Aggregation ships every line off-host to a durable, searchable central store so you can query across the whole fleet and retain logs past a pod's lifetime. It also enables cross-service correlation, which is impossible when logs are scattered per host.","correct":"In a distributed system, workloads run on ephemeral pods/VMs that are created and destroyed constantly, so local logs disappear when the container dies — exactly when you need them post-incident.","distractors":["ELK/EFK builds a full-text inverted index over every log line, enabling fast arbitrary search at the cost of high storage (index roughly the size of the data) and a heavy Elasticsearch cluster to operate.","Sampling keeps only a fraction of logs to cut volume — for example, keep 1 in 10 successful 2xx responses while keeping 100% of errors and warnings.","ERROR for actionable failures that need attention, WARN for degraded-but-handled conditions, INFO for significant business/lifecycle milestones, and DEBUG for verbose developer detail."]},{"id":"devops/observability_logging#173f32b099f8","section":"devops","module":"devops/observability_logging","moduleName":"observability logging","qIndex":2,"difficulty":"core","question":"Compare ELK/EFK with Loki.","answerFull":"ELK/EFK builds a full-text inverted index over every log line, enabling fast arbitrary search at the cost of high storage (index roughly the size of the data) and a heavy Elasticsearch cluster to operate. Loki indexes only a small set of labels (Prometheus-style) and stores compressed log bodies in object storage, then greps within a label-and-time-scoped slice — far cheaper and lower-ops, but search is constrained to label selectors plus LogQL regex. Choose ELK when arbitrary full-text search is a hard requirement (security/SIEM); choose Loki when you have high volume and want cost efficiency.","correct":"ELK/EFK builds a full-text inverted index over every log line, enabling fast arbitrary search at the cost of high storage (index roughly the size of the data) and a heavy Elasticsearch cluster to operate.","distractors":["Never log them in the first place — don't include authorization headers, tokens, full payloads, or personal data in log statements.","Structured logging emits each event as machine-parseable key/value data (typically JSON) with stable field names instead of a free-text sentence.","Inject the trace_id (and span_id) into every log line, so a log entry can be pivoted directly to its distributed trace and vice versa."]},{"id":"devops/observability_logging#49f672a223a0","section":"devops","module":"devops/observability_logging","moduleName":"observability logging","qIndex":3,"difficulty":"core","question":"What is structured logging and why does it matter?","answerFull":"Structured logging emits each event as machine-parseable key/value data (typically JSON) with stable field names instead of a free-text sentence. It lets you query and aggregate by field (status:500 AND amount>40) without fragile, version-brittle regex parsing at query time. It also makes redaction, enrichment, and correlation (injecting trace_id) reliable, so it's the foundation of a usable logging pipeline.","correct":"Structured logging emits each event as machine-parseable key/value data (typically JSON) with stable field names instead of a free-text sentence.","distractors":["You *can* compute log-derived metrics (e.g. Loki sum(rate({app=\"api\"} |= \"error\" [5m])) or ELK threshold alerts), and it's useful when the only signal is in the log text.","The 12-factor convention treats logs as event streams the app writes to stdout/stderr, leaving collection, rotation, and shipping to the platform.","ELK/EFK builds a full-text inverted index over every log line, enabling fast arbitrary search at the cost of high storage (index roughly the size of the data) and a heavy Elasticsearch cluster to operate."]},{"id":"devops/observability_logging#c9f3d50c3aa1","section":"devops","module":"devops/observability_logging","moduleName":"observability logging","qIndex":4,"difficulty":"core","question":"How do you correlate logs with traces and metrics?","answerFull":"Inject the trace_id (and span_id) into every log line, so a log entry can be pivoted directly to its distributed trace and vice versa. Metrics carry the same dimensional labels (service, route), and exemplars can link a metric spike to a representative trace. The result is a three-pillar workflow: a metric alert points you at a service, you jump to the trace for the slow/failing request, then to the exact logs for that trace_id to read the error.","correct":"Inject the trace_id (and span_id) into every log line, so a log entry can be pivoted directly to its distributed trace and vice versa.","distractors":["ClickHouse fits very high-volume logs where you mostly run aggregations and filtered scans (request logs, HTTP analytics) and want SQL with excellent compression and columnar speed over hundreds of billions of rows.","The 12-factor convention treats logs as event streams the app writes to stdout/stderr, leaving collection, rotation, and shipping to the platform.","Never log them in the first place — don't include authorization headers, tokens, full payloads, or personal data in log statements."]},{"id":"devops/observability_logging#901b8b5a2b06","section":"devops","module":"devops/observability_logging","moduleName":"observability logging","qIndex":5,"difficulty":"core","question":"What's the cost driver in logging, and how do you control it?","answerFull":"The dominant cost is ingest volume multiplied by indexing and retention — full-text indexing can roughly double on-disk size, and keeping everything hot for months scales linearly with traffic. Control it by indexing only what you query (labels in Loki, selective fields in ELK), sampling chatty success logs while keeping all errors, and tiering retention (hot → warm → cold → delete via ILM/TTL). These are deliberate design choices, not afterthoughts, because logs are the most expensive observability pillar per unit of insight.","correct":"The dominant cost is ingest volume multiplied by indexing and retention — full-text indexing can roughly double on-disk size, and keeping everything hot for months scales linearly with traffic.","distractors":["ELK/EFK builds a full-text inverted index over every log line, enabling fast arbitrary search at the cost of high storage (index roughly the size of the data) and a heavy Elasticsearch cluster to operate.","Never log them in the first place — don't include authorization headers, tokens, full payloads, or personal data in log statements.","Sampling keeps only a fraction of logs to cut volume — for example, keep 1 in 10 successful 2xx responses while keeping 100% of errors and warnings."]},{"id":"devops/observability_logging#57241b107270","section":"devops","module":"devops/observability_logging","moduleName":"observability logging","qIndex":6,"difficulty":"intermediate","question":"Why log to stdout/stderr in containerized environments?","answerFull":"The 12-factor convention treats logs as event streams the app writes to stdout/stderr, leaving collection, rotation, and shipping to the platform. The container runtime captures these to node files (/var/log/containers/*.log) where a DaemonSet collector tails and enriches them with pod/namespace metadata. This decouples the app from log infrastructure, avoids in-app file management and rotation bugs, and works uniformly across all workloads.","correct":"The 12-factor convention treats logs as event streams the app writes to stdout/stderr, leaving collection, rotation, and shipping to the platform.","distractors":["ELK/EFK builds a full-text inverted index over every log line, enabling fast arbitrary search at the cost of high storage (index roughly the size of the data) and a heavy Elasticsearch cluster to operate.","Inject the trace_id (and span_id) into every log line, so a log entry can be pivoted directly to its distributed trace and vice versa.","Structured logging emits each event as machine-parseable key/value data (typically JSON) with stable field names instead of a free-text sentence."]},{"id":"devops/observability_logging#5b8f7d56a651","section":"devops","module":"devops/observability_logging","moduleName":"observability logging","qIndex":7,"difficulty":"intermediate","question":"How should you handle PII and secrets in logs?","answerFull":"Never log them in the first place — don't include authorization headers, tokens, full payloads, or personal data in log statements. Add a defense-in-depth redaction stage in the pipeline (drop or mask fields like authorization, card numbers) and enforce RBAC on the log store since logs are widely readable and long-retained. Keep compliance/audit logs in a separate, tightly-scoped store with their own retention.","correct":"Never log them in the first place — don't include authorization headers, tokens, full payloads, or personal data in log statements.","distractors":["The 12-factor convention treats logs as event streams the app writes to stdout/stderr, leaving collection, rotation, and shipping to the platform.","In a distributed system, workloads run on ephemeral pods/VMs that are created and destroyed constantly, so local logs disappear when the container dies — exactly when you need them post-incident.","Inject the trace_id (and span_id) into every log line, so a log entry can be pivoted directly to its distributed trace and vice versa."]},{"id":"devops/observability_logging#44abdc4f7169","section":"devops","module":"devops/observability_logging","moduleName":"observability logging","qIndex":8,"difficulty":"intermediate","question":"What is log sampling and when is it safe?","answerFull":"Sampling keeps only a fraction of logs to cut volume — for example, keep 1 in 10 successful 2xx responses while keeping 100% of errors and warnings. It's safe for high-volume, low-information success paths where you only need representative examples, but you must never sample out errors, security events, or audit records. Implement it in the collector/aggregator (Vector sample, Fluentd) with an explicit exclude for error severities.","correct":"Sampling keeps only a fraction of logs to cut volume — for example, keep 1 in 10 successful 2xx responses while keeping 100% of errors and warnings.","distractors":["In a distributed system, workloads run on ephemeral pods/VMs that are created and destroyed constantly, so local logs disappear when the container dies — exactly when you need them post-incident.","ELK/EFK builds a full-text inverted index over every log line, enabling fast arbitrary search at the cost of high storage (index roughly the size of the data) and a heavy Elasticsearch cluster to operate.","The 12-factor convention treats logs as event streams the app writes to stdout/stderr, leaving collection, rotation, and shipping to the platform."]},{"id":"devops/observability_logging#dc23b6230d2f","section":"devops","module":"devops/observability_logging","moduleName":"observability logging","qIndex":10,"difficulty":"intermediate","question":"When would you choose ClickHouse over ELK or Loki for logs?","answerFull":"ClickHouse fits very high-volume logs where you mostly run aggregations and filtered scans (request logs, HTTP analytics) and want SQL with excellent compression and columnar speed over hundreds of billions of rows. It's cheaper than full-text ELK and faster for analytical queries than Loki's grep model, at the cost of not being a turnkey full-text search engine. Choose it when your access pattern is \"aggregate and filter structured events at massive scale,\" not \"free-text search everything.\"","correct":"ClickHouse fits very high-volume logs where you mostly run aggregations and filtered scans (request logs, HTTP analytics) and want SQL with excellent compression and columnar speed over hundreds of billions of rows.","distractors":["The dominant cost is ingest volume multiplied by indexing and retention — full-text indexing can roughly double on-disk size, and keeping everything hot for months scales linearly with traffic.","You *can* compute log-derived metrics (e.g. Loki sum(rate({app=\"api\"} |= \"error\" [5m])) or ELK threshold alerts), and it's useful when the only signal is in the log text.","Sampling keeps only a fraction of logs to cut volume — for example, keep 1 in 10 successful 2xx responses while keeping 100% of errors and warnings."]},{"id":"devops/observability_logging#9cb66a97d9ca","section":"devops","module":"devops/observability_logging","moduleName":"observability logging","qIndex":11,"difficulty":"advanced","question":"What log levels should you use and how in production?","answerFull":"ERROR for actionable failures that need attention, WARN for degraded-but-handled conditions, INFO for significant business/lifecycle milestones, and DEBUG for verbose developer detail. In production keep DEBUG off by default (it's high-volume and costly) and make the level dynamically adjustable so you can temporarily raise verbosity during an incident. Consistent, meaningful levels let you sample and alert by severity and keep the signal-to-noise ratio sane.","correct":"ERROR for actionable failures that need attention, WARN for degraded-but-handled conditions, INFO for significant business/lifecycle milestones, and DEBUG for verbose developer detail.","distractors":["Never log them in the first place — don't include authorization headers, tokens, full payloads, or personal data in log statements.","You *can* compute log-derived metrics (e.g. Loki sum(rate({app=\"api\"} |= \"error\" [5m])) or ELK threshold alerts), and it's useful when the only signal is in the log text.","ELK/EFK builds a full-text inverted index over every log line, enabling fast arbitrary search at the cost of high storage (index roughly the size of the data) and a heavy Elasticsearch cluster to operate."]},{"id":"devops/observability_logging#561cbbf0c06e","section":"devops","module":"devops/observability_logging","moduleName":"observability logging","qIndex":12,"difficulty":"advanced","question":"How do you build alerts or metrics from logs, and should you?","answerFull":"You *can* compute log-derived metrics (e.g. Loki sum(rate({app=\"api\"} |= \"error\" [5m])) or ELK threshold alerts), and it's useful when the only signal is in the log text. But prefer emitting a real metric at the source for anything you alert on, because metric pipelines are cheaper, faster, and more reliable than parsing/aggregating logs at query time. Use log-based alerts as a fallback or for one-off patterns, not as the backbone of your alerting (that belongs to observability_metrics_prometheus).","correct":"You *can* compute log-derived metrics (e.g. Loki sum(rate({app=\"api\"} |= \"error\" [5m])) or ELK threshold alerts), and it's useful when the only signal is in the log text.","distractors":["In a distributed system, workloads run on ephemeral pods/VMs that are created and destroyed constantly, so local logs disappear when the container dies — exactly when you need them post-incident.","ClickHouse fits very high-volume logs where you mostly run aggregations and filtered scans (request logs, HTTP analytics) and want SQL with excellent compression and columnar speed over hundreds of billions of rows.","ELK/EFK builds a full-text inverted index over every log line, enabling fast arbitrary search at the cost of high storage (index roughly the size of the data) and a heavy Elasticsearch cluster to operate."]},{"id":"devops/observability_metrics_prometheus#b44f37a2d50f","section":"devops","module":"devops/observability_metrics_prometheus","moduleName":"observability metrics prometheus","qIndex":1,"difficulty":"core","question":"Why is Prometheus pull-based instead of push-based, and when do you push?","answerFull":"Prometheus scrapes targets so it can observe target health directly (the synthetic up metric is 0 when a scrape fails), keep scrape configuration centralized, and avoid every target needing to know where to send data. Push makes sense only for short-lived jobs that exit before a scrape can reach them — those use the Pushgateway, which Prometheus then scrapes. Use pull for everything long-lived; reserve push for batch jobs.","correct":"Prometheus scrapes targets so it can observe target health directly (the synthetic up metric is 0 when a scrape fails), keep scrape configuration centralized, and avoid every target needing to know where to send data.","distractors":["Recording rules precompute a PromQL expression on a schedule and store the result as a new series (named level:metric:operation), so expensive queries reused across dashboards and alerts run once instead of per-query.","kubernetes_sd_configs queries the Kubernetes API for pods/services/endpoints/nodes and turns them into targets with meta_kubernetes_* meta-labels.","up is a synthetic gauge Prometheus emits per target: 1 if the last scrape succeeded, 0 if it failed."]},{"id":"devops/observability_metrics_prometheus#b812a519325a","section":"devops","module":"devops/observability_metrics_prometheus","moduleName":"observability metrics prometheus","qIndex":2,"difficulty":"core","question":"What is cardinality and why does it dominate Prometheus capacity planning?","answerFull":"Cardinality is the number of distinct time series, which equals the product of label-value combinations for each metric. Prometheus keeps the active series index and head block in memory (roughly 1–2 KB per series), so a label like user_id with 5M values creates 5M series and OOM-kills the server. You plan RAM around active series count and aggressively keep labels bounded; per-request identifiers go to traces/logs, not labels.","correct":"Cardinality is the number of distinct time series, which equals the product of label-value combinations for each metric.","distractors":["up is a synthetic gauge Prometheus emits per target: 1 if the last scrape succeeded, 0 if it failed.","Recording rules precompute a PromQL expression on a schedule and store the result as a new series (named level:metric:operation), so expensive queries reused across dashboards and alerts run once instead of per-query.","Prometheus scrapes targets so it can observe target health directly (the synthetic up metric is 0 when a scrape fails), keep scrape configuration centralized, and avoid every target needing to know where to send data."]},{"id":"devops/observability_metrics_prometheus#cfaaafc587e4","section":"devops","module":"devops/observability_metrics_prometheus","moduleName":"observability metrics prometheus","qIndex":3,"difficulty":"core","question":"Explain counter vs gauge vs histogram vs summary.","answerFull":"A counter only increases and resets to zero on restart (requests, errors) — you query its rate(). A gauge moves up and down (memory, queue depth). A histogram buckets observations and exposes _bucket/_sum/_count, letting you compute aggregatable quantiles across instances with histogram_quantile. A summary computes quantiles client-side, which are cheaper but cannot be aggregated across instances (you can't average percentiles) — prefer histograms for fleet-wide latency.","correct":"A counter only increases and resets to zero on restart (requests, errors) — you query its rate().","distractors":["Use histogram_quantile(0.99, sum by (le) (rate(http_request_duration_seconds_bucket[5m]))): you rate() each bucket, sum by (le) to aggregate buckets across instances, then interpolate the 99th percentile.","rate(counter[5m]) computes the per-second average increase over the window, turning a meaningless monotonic value into a trend you can graph and alert on.","You alert when the error-budget burn rate exceeds a threshold over both a long window and a short confirmation window simultaneously, ANDed together."]},{"id":"devops/observability_metrics_prometheus#cdaeb5a3da8b","section":"devops","module":"devops/observability_metrics_prometheus","moduleName":"observability metrics prometheus","qIndex":4,"difficulty":"core","question":"Why use rate() on counters, and how does it handle restarts?","answerFull":"rate(counter[5m]) computes the per-second average increase over the window, turning a meaningless monotonic value into a trend you can graph and alert on. It automatically detects counter resets (a value dropping to a lower number implies a process restart) and corrects for them, so a service restart doesn't show as a huge negative spike. Use rate for alerts/graphs, increase for \"how many in this window,\" and avoid irate for alerts because it's noisy.","correct":"rate(counter[5m]) computes the per-second average increase over the window, turning a meaningless monotonic value into a trend you can graph and alert on.","distractors":["A counter only increases and resets to zero on restart (requests, errors) — you query its rate().","for: requires the alert expression to remain true continuously for that duration before the alert transitions from pending to firing, so a one-off spike that clears within the window never pages anyone.","You alert when the error-budget burn rate exceeds a threshold over both a long window and a short confirmation window simultaneously, ANDed together."]},{"id":"devops/observability_metrics_prometheus#83fdd67d136f","section":"devops","module":"devops/observability_metrics_prometheus","moduleName":"observability metrics prometheus","qIndex":5,"difficulty":"core","question":"What's the difference between recording rules and alerting rules?","answerFull":"Recording rules precompute a PromQL expression on a schedule and store the result as a new series (named level:metric:operation), so expensive queries reused across dashboards and alerts run once instead of per-query. Alerting rules evaluate a boolean condition and, when true for the for: duration, emit an alert to Alertmanager. The pattern is: compute the SLI ratio as a recording rule, then write cheap, fast alerting rules on top of that precomputed series.","correct":"Recording rules precompute a PromQL expression on a schedule and store the result as a new series (named level:metric:operation), so expensive queries reused across dashboards and alerts run once instead of per-query.","distractors":["First identify the offenders with topk(20, count by (name)({name=~\".+\"})) and count by (label) queries (or pint/tsdb tools) to find which metrics/labels dominate.","A counter only increases and resets to zero on restart (requests, errors) — you query its rate().","Cardinality is the number of distinct time series, which equals the product of label-value combinations for each metric."]},{"id":"devops/observability_metrics_prometheus#fc01aec3a8cf","section":"devops","module":"devops/observability_metrics_prometheus","moduleName":"observability metrics prometheus","qIndex":6,"difficulty":"intermediate","question":"How does the for: clause prevent alert flapping?","answerFull":"for: requires the alert expression to remain true continuously for that duration before the alert transitions from pending to firing, so a one-off spike that clears within the window never pages anyone. For example for: 10m on a 5% error-rate condition means the error rate must stay above 5% for 10 straight minutes. Combine it with multi-window burn-rate logic for SLO alerts so you get both fast detection on severe burns and de-flapping on transient blips.","correct":"for: requires the alert expression to remain true continuously for that duration before the alert transitions from pending to firing, so a one-off spike that clears within the window never pages anyone.","distractors":["You alert when the error-budget burn rate exceeds a threshold over both a long window and a short confirmation window simultaneously, ANDed together.","Use histogram_quantile(0.99, sum by (le) (rate(http_request_duration_seconds_bucket[5m]))): you rate() each bucket, sum by (le) to aggregate buckets across instances, then interpolate the 99th percentile.","Recording rules precompute a PromQL expression on a schedule and store the result as a new series (named level:metric:operation), so expensive queries reused across dashboards and alerts run once instead of per-query."]},{"id":"devops/observability_metrics_prometheus#520d7adf59c2","section":"devops","module":"devops/observability_metrics_prometheus","moduleName":"observability metrics prometheus","qIndex":7,"difficulty":"intermediate","question":"How do multi-window, multi-burn-rate SLO alerts work?","answerFull":"You alert when the error-budget burn rate exceeds a threshold over both a long window and a short confirmation window simultaneously, ANDed together. A fast page fires at ~14.4x burn over 1h confirmed by a 5m window (consumes ~2% of a 30-day budget in an hour); a slower ticket fires at ~6x over 6h confirmed by 30m. The long window ensures the burn is real and sustained; the short window ensures you alert promptly and stop alerting quickly once it resolves.","correct":"You alert when the error-budget burn rate exceeds a threshold over both a long window and a short confirmation window simultaneously, ANDed together.","distractors":["All three add long-term storage and horizontal/global scale on top of Prometheus.","A counter only increases and resets to zero on restart (requests, errors) — you query its rate().","Recording rules precompute a PromQL expression on a schedule and store the result as a new series (named level:metric:operation), so expensive queries reused across dashboards and alerts run once instead of per-query."]},{"id":"devops/observability_metrics_prometheus#0c343c8e5603","section":"devops","module":"devops/observability_metrics_prometheus","moduleName":"observability metrics prometheus","qIndex":8,"difficulty":"intermediate","question":"How does Prometheus store data, and what limits single-node retention?","answerFull":"Samples land in an in-memory head block (backed by a WAL for crash recovery), which is cut to immutable 2-hour on-disk blocks and then compacted into larger blocks. Retention is bounded by local disk (--storage.tsdb.retention.time, commonly 15–90 days) and there's no native cross-instance aggregation or HA dedup. For long-term and global views you add Thanos (sidecar ships blocks to object storage) or remote-write into Cortex/Mimir.","correct":"Samples land in an in-memory head block (backed by a WAL for crash recovery), which is cut to immutable 2-hour on-disk blocks and then compacted into larger blocks.","distractors":["All three add long-term storage and horizontal/global scale on top of Prometheus.","up is a synthetic gauge Prometheus emits per target: 1 if the last scrape succeeded, 0 if it failed.","Recording rules precompute a PromQL expression on a schedule and store the result as a new series (named level:metric:operation), so expensive queries reused across dashboards and alerts run once instead of per-query."]},{"id":"devops/observability_metrics_prometheus#0bccce2f3605","section":"devops","module":"devops/observability_metrics_prometheus","moduleName":"observability metrics prometheus","qIndex":9,"difficulty":"intermediate","question":"Compare Thanos, Cortex, and Mimir.","answerFull":"All three add long-term storage and horizontal/global scale on top of Prometheus. Thanos uses a sidecar that ships local TSDB blocks to object storage with a Querier that fans out across sidecars and a Store Gateway, plus a Compactor for dedup and downsampling — minimal change to existing Prometheus. Cortex and its successor Mimir take Prometheus remote_write into a clustered, multi-tenant ingest/query system designed for tens of millions of series per tenant; Mimir is the actively-developed, operationally simpler evolution. Choose Thanos for an additive sidecar model, Mimir for a fully clustered remote-write backend.","correct":"All three add long-term storage and horizontal/global scale on top of Prometheus.","distractors":["You alert when the error-budget burn rate exceeds a threshold over both a long window and a short confirmation window simultaneously, ANDed together.","Prometheus scrapes targets so it can observe target health directly (the synthetic up metric is 0 when a scrape fails), keep scrape configuration centralized, and avoid every target needing to know where to send data.","up is a synthetic gauge Prometheus emits per target: 1 if the last scrape succeeded, 0 if it failed."]},{"id":"devops/observability_metrics_prometheus#b9f58288a9f7","section":"devops","module":"devops/observability_metrics_prometheus","moduleName":"observability metrics prometheus","qIndex":10,"difficulty":"intermediate","question":"What's the difference between relabel_configs and metric_relabel_configs?","answerFull":"relabel_configs runs before the scrape and operates on the target's meta-labels — it selects which targets to scrape (keep/drop) and rewrites target labels like address and namespace. metric_relabel_configs runs after the scrape on the resulting samples — it's where you drop noisy or high-cardinality series (labeldrop, drop) before they hit the TSDB. Together they are your primary cardinality and cost-control levers.","correct":"relabel_configs runs before the scrape and operates on the target's meta-labels — it selects which targets to scrape (keep/drop) and rewrites target labels like address and namespace.","distractors":["up is a synthetic gauge Prometheus emits per target: 1 if the last scrape succeeded, 0 if it failed.","kubernetes_sd_configs queries the Kubernetes API for pods/services/endpoints/nodes and turns them into targets with meta_kubernetes_* meta-labels.","First identify the offenders with topk(20, count by (name)({name=~\".+\"})) and count by (label) queries (or pint/tsdb tools) to find which metrics/labels dominate."]},{"id":"devops/observability_metrics_prometheus#5d6d82de5b93","section":"devops","module":"devops/observability_metrics_prometheus","moduleName":"observability metrics prometheus","qIndex":11,"difficulty":"advanced","question":"How do you compute a p99 latency across all instances of a service?","answerFull":"Use histogram_quantile(0.99, sum by (le) (rate(http_request_duration_seconds_bucket[5m]))): you rate() each bucket, sum by (le) to aggregate buckets across instances, then interpolate the 99th percentile. This works because histogram buckets are additive across instances, unlike summary quantiles. Make sure your bucket boundaries straddle the SLO threshold, or the interpolation across a too-wide bucket gives an inaccurate quantile.","correct":"Use histogram_quantile(0.99, sum by (le) (rate(http_request_duration_seconds_bucket[5m]))): you rate() each bucket, sum by (le) to aggregate buckets across instances, then interpolate the 99th percentile.","distractors":["A counter only increases and resets to zero on restart (requests, errors) — you query its rate().","You alert when the error-budget burn rate exceeds a threshold over both a long window and a short confirmation window simultaneously, ANDed together.","rate(counter[5m]) computes the per-second average increase over the window, turning a meaningless monotonic value into a trend you can graph and alert on."]},{"id":"devops/observability_metrics_prometheus#5622915fd676","section":"devops","module":"devops/observability_metrics_prometheus","moduleName":"observability metrics prometheus","qIndex":12,"difficulty":"advanced","question":"How does Prometheus handle service discovery in Kubernetes?","answerFull":"kubernetes_sd_configs queries the Kubernetes API for pods/services/endpoints/nodes and turns them into targets with meta_kubernetes_* meta-labels. relabel_configs then filters to opted-in targets (e.g. keep pods with a prometheus.io/scrape: \"true\" annotation), sets the scrape port and path, and copies useful meta-labels (namespace, pod) onto the series. The Prometheus Operator abstracts this with ServiceMonitor/PodMonitor CRDs so teams declare scraping without editing the central config.","correct":"kubernetes_sd_configs queries the Kubernetes API for pods/services/endpoints/nodes and turns them into targets with meta_kubernetes_* meta-labels.","distractors":["relabel_configs runs before the scrape and operates on the target's meta-labels — it selects which targets to scrape (keep/drop) and rewrites target labels like address and namespace.","All three add long-term storage and horizontal/global scale on top of Prometheus.","up is a synthetic gauge Prometheus emits per target: 1 if the last scrape succeeded, 0 if it failed."]},{"id":"devops/observability_metrics_prometheus#52d517a30df0","section":"devops","module":"devops/observability_metrics_prometheus","moduleName":"observability metrics prometheus","qIndex":13,"difficulty":"advanced","question":"What is the up metric and why is it useful?","answerFull":"up is a synthetic gauge Prometheus emits per target: 1 if the last scrape succeeded, 0 if it failed. It's the foundation of target-health alerting — up == 0 for: 5m tells you a target is down or unreachable, independent of any application metric. Because pull-based scraping owns the connection, up is reliable in a way push systems can't replicate (a silent target simply stops being scrapeable).","correct":"up is a synthetic gauge Prometheus emits per target: 1 if the last scrape succeeded, 0 if it failed.","distractors":["Recording rules precompute a PromQL expression on a schedule and store the result as a new series (named level:metric:operation), so expensive queries reused across dashboards and alerts run once instead of per-query.","relabel_configs runs before the scrape and operates on the target's meta-labels — it selects which targets to scrape (keep/drop) and rewrites target labels like address and namespace.","Cardinality is the number of distinct time series, which equals the product of label-value combinations for each metric."]},{"id":"devops/observability_metrics_prometheus#bfc206c14af5","section":"devops","module":"devops/observability_metrics_prometheus","moduleName":"observability metrics prometheus","qIndex":14,"difficulty":"advanced","question":"How would you reduce cardinality on an already-overloaded Prometheus?","answerFull":"First identify the offenders with topk(20, count by (name)({name=~\".+\"})) and count by (label) queries (or pint/tsdb tools) to find which metrics/labels dominate. Then drop unbounded labels at ingest with metric_relabel_configs labeldrop/drop, replace ID labels with bounded route templates in instrumentation, and move per-request identifiers to traces/logs. For sustained scale, shard scraping functionally or remote-write into Mimir/Cortex.","correct":"First identify the offenders with topk(20, count by (name)({name=~\".+\"})) and count by (label) queries (or pint/tsdb tools) to find which metrics/labels dominate.","distractors":["kubernetes_sd_configs queries the Kubernetes API for pods/services/endpoints/nodes and turns them into targets with meta_kubernetes_* meta-labels.","Cardinality is the number of distinct time series, which equals the product of label-value combinations for each metric.","Prometheus scrapes targets so it can observe target health directly (the synthetic up metric is 0 when a scrape fails), keep scrape configuration centralized, and avoid every target needing to know where to send data."]},{"id":"devops/observability_tracing_and_otel#62f9b6f913af","section":"devops","module":"devops/observability_tracing_and_otel","moduleName":"observability tracing and otel","qIndex":1,"difficulty":"core","question":"What is a trace, a span, and how are they linked?","answerFull":"A trace is one request's end-to-end journey across services; a span is a single unit of work within it (an HTTP handler, a DB query) with a start time, duration, status, and attributes. All spans in a trace share a trace_id, and each child span carries a parent_span_id pointing at the span that caused it, so the backend reconstructs a causal tree and timeline. That tree is what lets you see which hop consumed the latency for a specific request.","correct":"A trace is one request's end-to-end journey across services; a span is a single unit of work within it (an HTTP handler, a DB query) with a start time, duration, status, and attributes.","distractors":["Head sampling decides whether to keep a trace at its start (the root), so it's cheap and simple but blind to outcome — it can discard the one trace that errored.","Instrumentation adds per-span CPU/memory and the network cost of exporting spans, plus storage on the backend — all scaling with request volume.","Inject the trace_id into every log line so you can jump from a span to the exact logs that request emitted, and attach exemplars to latency histograms so a metric spike links to a representative trace."]},{"id":"devops/observability_tracing_and_otel#376e865bc3f9","section":"devops","module":"devops/observability_tracing_and_otel","moduleName":"observability tracing and otel","qIndex":3,"difficulty":"core","question":"What is OpenTelemetry and why did it win?","answerFull":"OpenTelemetry is the CNCF vendor-neutral standard for generating and collecting telemetry — SDKs/API for instrumentation, the OTLP wire protocol, semantic conventions, and the Collector. It won by merging the previously competing OpenTracing and OpenCensus efforts and by being backend-agnostic: you instrument once and export to Jaeger, Tempo, or any vendor that accepts OTLP, so you're never locked in. That decoupling (instrument once, export anywhere) is its central value proposition.","correct":"OpenTelemetry is the CNCF vendor-neutral standard for generating and collecting telemetry — SDKs/API for instrumentation, the OTLP wire protocol, semantic conventions, and the Collector.","distractors":["Semantic conventions are OpenTelemetry's standardized attribute names and structures — http.request.method, db.system, db.statement, service.name — so telemetry is consistent regardless of language or library.","The Collector is a standalone process with a receivers → processors → exporters pipeline that receives OTLP, then batches, redacts, samples, and exports to one or more backends.","Jaeger is a mature CNCF backend that indexes traces in Cassandra or Elasticsearch, giving rich search by service/operation/tags at the cost of running and storing those indexes."]},{"id":"devops/observability_tracing_and_otel#c7a1c6b68732","section":"devops","module":"devops/observability_tracing_and_otel","moduleName":"observability tracing and otel","qIndex":4,"difficulty":"core","question":"Explain head sampling vs tail sampling and the tradeoff.","answerFull":"Head sampling decides whether to keep a trace at its start (the root), so it's cheap and simple but blind to outcome — it can discard the one trace that errored. Tail sampling buffers the complete trace and decides after seeing everything, so you can keep 100% of error and slow traces while sampling only the boring fast successes. The tradeoff is that tail sampling needs memory to buffer traces and a gateway tier that receives all of a trace's spans, but it's far smarter for debugging incidents.","correct":"Head sampling decides whether to keep a trace at its start (the root), so it's cheap and simple but blind to outcome — it can discard the one trace that errored.","distractors":["A trace is one request's end-to-end journey across services; a span is a single unit of work within it (an HTTP handler, a DB query) with a start time, duration, status, and attributes.","Inject the trace_id into every log line so you can jump from a span to the exact logs that request emitted, and attach exemplars to latency histograms so a metric spike links to a representative trace.","Instrumentation adds per-span CPU/memory and the network cost of exporting spans, plus storage on the backend — all scaling with request volume."]},{"id":"devops/observability_tracing_and_otel#0b8c515e882d","section":"devops","module":"devops/observability_tracing_and_otel","moduleName":"observability tracing and otel","qIndex":7,"difficulty":"intermediate","question":"What does the OpenTelemetry Collector do and why use it instead of exporting directly?","answerFull":"The Collector is a standalone process with a receivers → processors → exporters pipeline that receives OTLP, then batches, redacts, samples, and exports to one or more backends. Using it instead of exporting directly from apps decouples your code from any specific backend (switch Jaeger→Tempo with zero app changes), centralizes policy (sampling, PII redaction), and provides buffering/retries so a backend outage doesn't block your services. It's the policy and resilience layer of the tracing platform.","correct":"The Collector is a standalone process with a receivers → processors → exporters pipeline that receives OTLP, then batches, redacts, samples, and exports to one or more backends.","distractors":["Instrumentation adds per-span CPU/memory and the network cost of exporting spans, plus storage on the backend — all scaling with request volume.","Semantic conventions are OpenTelemetry's standardized attribute names and structures — http.request.method, db.system, db.statement, service.name — so telemetry is consistent regardless of language or library.","The Collector's spanmetrics connector derives RED metrics (Rate, Errors, Duration histograms) from the spans flowing through it, dimensioned by service and operation."]},{"id":"devops/observability_tracing_and_otel#5591b636e5d2","section":"devops","module":"devops/observability_tracing_and_otel","moduleName":"observability tracing and otel","qIndex":8,"difficulty":"intermediate","question":"How do you correlate a trace with logs and metrics?","answerFull":"Inject the trace_id into every log line so you can jump from a span to the exact logs that request emitted, and attach exemplars to latency histograms so a metric spike links to a representative trace. The workflow is: a metric alert points at a service, you open a representative trace to find the slow/failing span, then query logs by that trace_id for the precise error and payload. This three-pillar pivot is the core value of unified observability.","correct":"Inject the trace_id into every log line so you can jump from a span to the exact logs that request emitted, and attach exemplars to latency histograms so a metric spike links to a representative trace.","distractors":["Head sampling decides whether to keep a trace at its start (the root), so it's cheap and simple but blind to outcome — it can discard the one trace that errored.","A trace is one request's end-to-end journey across services; a span is a single unit of work within it (an HTTP handler, a DB query) with a start time, duration, status, and attributes.","Semantic conventions are OpenTelemetry's standardized attribute names and structures — http.request.method, db.system, db.statement, service.name — so telemetry is consistent regardless of language or library."]},{"id":"devops/observability_tracing_and_otel#bafead02a45b","section":"devops","module":"devops/observability_tracing_and_otel","moduleName":"observability tracing and otel","qIndex":9,"difficulty":"intermediate","question":"What are OTel semantic conventions and why follow them?","answerFull":"Semantic conventions are OpenTelemetry's standardized attribute names and structures — http.request.method, db.system, db.statement, service.name — so telemetry is consistent regardless of language or library. Following them means backends, dashboards, and queries are portable and auto-instrumentation produces uniform data you can build generic alerts and span-metrics on. Inventing your own attribute names fragments your data and breaks vendor/tooling integrations.","correct":"Semantic conventions are OpenTelemetry's standardized attribute names and structures — http.request.method, db.system, db.statement, service.name — so telemetry is consistent regardless of language or library.","distractors":["The Collector's spanmetrics connector derives RED metrics (Rate, Errors, Duration histograms) from the spans flowing through it, dimensioned by service and operation.","Inject the trace_id into every log line so you can jump from a span to the exact logs that request emitted, and attach exemplars to latency histograms so a metric spike links to a representative trace.","OpenTelemetry is the CNCF vendor-neutral standard for generating and collecting telemetry — SDKs/API for instrumentation, the OTLP wire protocol, semantic conventions, and the Collector."]},{"id":"devops/observability_tracing_and_otel#62bd482c8e5b","section":"devops","module":"devops/observability_tracing_and_otel","moduleName":"observability tracing and otel","qIndex":10,"difficulty":"intermediate","question":"How can you get latency metrics without instrumenting metrics separately?","answerFull":"The Collector's spanmetrics connector derives RED metrics (Rate, Errors, Duration histograms) from the spans flowing through it, dimensioned by service and operation. You then scrape those as Prometheus metrics, getting per-service p50/p99 latency and error rates \"for free\" from your traces with one fewer instrumentation path to maintain. The tradeoff is the metrics are only as complete as your sampled spans, so combine with head/tail sampling awareness or use a pre-sampling tap.","correct":"The Collector's spanmetrics connector derives RED metrics (Rate, Errors, Duration histograms) from the spans flowing through it, dimensioned by service and operation.","distractors":["Jaeger is a mature CNCF backend that indexes traces in Cassandra or Elasticsearch, giving rich search by service/operation/tags at the cost of running and storing those indexes.","OpenTelemetry is the CNCF vendor-neutral standard for generating and collecting telemetry — SDKs/API for instrumentation, the OTLP wire protocol, semantic conventions, and the Collector.","A trace is one request's end-to-end journey across services; a span is a single unit of work within it (an HTTP handler, a DB query) with a start time, duration, status, and attributes."]},{"id":"devops/observability_tracing_and_otel#7accd39efd8f","section":"devops","module":"devops/observability_tracing_and_otel","moduleName":"observability tracing and otel","qIndex":11,"difficulty":"advanced","question":"What's the difference between Jaeger and Tempo?","answerFull":"Jaeger is a mature CNCF backend that indexes traces in Cassandra or Elasticsearch, giving rich search by service/operation/tags at the cost of running and storing those indexes. Tempo stores traces in cheap object storage (S3/GCS) indexed essentially only by trace_id (plus TraceQL search), trading some search richness for dramatically lower storage cost and operational simplicity. Choose Jaeger for rich attribute search, Tempo for cheap retention at scale where you usually arrive with a trace_id from a metric/log pivot.","correct":"Jaeger is a mature CNCF backend that indexes traces in Cassandra or Elasticsearch, giving rich search by service/operation/tags at the cost of running and storing those indexes.","distractors":["The Collector's spanmetrics connector derives RED metrics (Rate, Errors, Duration histograms) from the spans flowing through it, dimensioned by service and operation.","Instrumentation adds per-span CPU/memory and the network cost of exporting spans, plus storage on the backend — all scaling with request volume.","OpenTelemetry is the CNCF vendor-neutral standard for generating and collecting telemetry — SDKs/API for instrumentation, the OTLP wire protocol, semantic conventions, and the Collector."]},{"id":"devops/observability_tracing_and_otel#10376df08cc3","section":"devops","module":"devops/observability_tracing_and_otel","moduleName":"observability tracing and otel","qIndex":12,"difficulty":"advanced","question":"What overhead does tracing add and how do you keep it low?","answerFull":"Instrumentation adds per-span CPU/memory and the network cost of exporting spans, plus storage on the backend — all scaling with request volume. Keep it low by sampling (tail sampling keeps errors/slow but discards most successes), batching exports in the Collector, running an agent tier to offload export work from the app, and avoiding excessive manual spans on hot loops. Done right, overhead is a small single-digit percentage while you still keep 100% of the diagnostically valuable traces.","correct":"Instrumentation adds per-span CPU/memory and the network cost of exporting spans, plus storage on the backend — all scaling with request volume.","distractors":["The Collector's spanmetrics connector derives RED metrics (Rate, Errors, Duration histograms) from the spans flowing through it, dimensioned by service and operation.","Jaeger is a mature CNCF backend that indexes traces in Cassandra or Elasticsearch, giving rich search by service/operation/tags at the cost of running and storing those indexes.","A trace is one request's end-to-end journey across services; a span is a single unit of work within it (an HTTP handler, a DB query) with a start time, duration, status, and attributes."]},{"id":"devops/performance_and_load_testing#76e981443628","section":"devops","module":"devops/performance_and_load_testing","moduleName":"performance and load testing","qIndex":1,"difficulty":"core","question":"What's the difference between load, stress, spike, and soak tests?","answerFull":"Each answers a different question. A load test drives expected/peak traffic and verifies SLOs (latency, error rate) hold — \"are we good for normal peak?\" A stress test pushes *past* peak until something breaks, to find the breaking point and observe *how* it fails (graceful degradation vs cascading collapse). A spike test applies a sudden sharp jump (e.g., 100→5000 RPS in seconds) to verify autoscaling reacts in time and the system survives the transient. A soak (endurance) test holds moderate load for hours or days to surface slow problems — memory leaks, connection-pool exhaustion, disk fill — that short tests never reveal. Running \"a load test\" without picking the type is a common mistake; the load profile must match the question.","correct":"Each answers a different question.","distractors":["Coordinated omission is a measurement bug in closed-model (VU-based) load tests: each virtual user sends a request, waits for the response, then sends the next.","First suspect the load generator, not the service.","Because the system's performance is data-dependent in ways that synthetic data hides."]},{"id":"devops/performance_and_load_testing#d7ede4b6b354","section":"devops","module":"devops/performance_and_load_testing","moduleName":"performance and load testing","qIndex":2,"difficulty":"core","question":"Why measure percentiles instead of average latency?","answerFull":"Because users experience the tail, not the mean. An average of 60ms can hide a p99 of 3 seconds — meaning 1% of requests (often millions/day, and disproportionately your power users with the most data) get a terrible experience. Averages are also easily skewed: a handful of fast cache hits drag the mean down while real failures hide in the tail. SLOs are written against percentiles (p95/p99/p99.9) precisely because they describe the worst experience a defined fraction of users tolerate. You report and gate on percentiles; the average is nearly useless for reliability decisions.","correct":"Because users experience the tail, not the mean.","distractors":["The open model (constant arrival rate) is the default for latency truth because it mimics real users who keep arriving regardless of how slow you are.","With great care, because you can hurt real users.","Because the system's performance is data-dependent in ways that synthetic data hides."]},{"id":"devops/performance_and_load_testing#820b599ef86c","section":"devops","module":"devops/performance_and_load_testing","moduleName":"performance and load testing","qIndex":3,"difficulty":"core","question":"Explain coordinated omission and how you avoid it.","answerFull":"Coordinated omission is a measurement bug in closed-model (VU-based) load tests: each virtual user sends a request, waits for the response, then sends the next. When the system stalls, the VUs *also* stall — so the tool simply never issues the backlog of requests that would have piled up, and therefore never measures the long latencies those requests would have suffered. The reported p99 looks fine while real users (who keep arriving regardless) are timing out. You avoid it by using an open model — a constant arrival rate executor (constant-arrival-rate/ramping-arrival-rate in k6) that sends a fixed RPS independent of response time, so requests queue up and the true tail latency is captured. It's the single most important correctness concept in load testing.","correct":"Coordinated omission is a measurement bug in closed-model (VU-based) load tests: each virtual user sends a request, waits for the response, then sends the next.","distractors":["First suspect the load generator, not the service.","Because the system's performance is data-dependent in ways that synthetic data hides.","Because users experience the tail, not the mean."]},{"id":"devops/performance_and_load_testing#0823718dbcdc","section":"devops","module":"devops/performance_and_load_testing","moduleName":"performance and load testing","qIndex":4,"difficulty":"core","question":"The team says \"our service maxes out at 2000 RPS\" but you suspect the test is wrong. How do you check?","answerFull":"First suspect the load generator, not the service. A single runner box hits its own limits — CPU saturation, ephemeral port exhaustion (~28k ports by default), open file descriptors, or NIC bandwidth — and plateaus, making it *look* like the service can't go faster. I'd monitor the generator's own CPU, FDs, socket count, and network during the test; if any is maxed, the generator is the bottleneck. The fix is distributed load generation (k6-operator or Locust master/worker across multiple pods/machines), each well under its own limits, with load split across them. Only once the generators have clear headroom can you trust that 2000 RPS is the *service's* ceiling.","correct":"First suspect the load generator, not the service.","distractors":["Coordinated omission is a measurement bug in closed-model (VU-based) load tests: each virtual user sends a request, waits for the response, then sends the next.","The first minute or so of a test is unrepresentative: JIT compilers haven't optimized hot paths, caches are cold, connection pools are empty and filling, and the autoscaler is still ramping replicas.","Run a capacity test: pin replicas to 1 (disable the HPA), step the arrival rate up, and record p99 and error rate at each step."]},{"id":"devops/performance_and_load_testing#a7e205e6e623","section":"devops","module":"devops/performance_and_load_testing","moduleName":"performance and load testing","qIndex":5,"difficulty":"core","question":"Open model vs closed model — when would you actually want closed?","answerFull":"The open model (constant arrival rate) is the default for latency truth because it mimics real users who keep arriving regardless of how slow you are. But the closed model (fixed virtual users) is correct when you're modeling a system with genuinely *fixed concurrency* — e.g., a fixed pool of backend worker threads, a batch job with a set number of parallel workers, or a client with a hard connection-pool cap. In those cases the real world *does* throttle itself to N in-flight, and a closed model with N VUs accurately represents it. The mistake is using closed-model to measure user-facing API latency under saturation, where it produces coordinated omission.","correct":"The open model (constant arrival rate) is the default for latency truth because it mimics real users who keep arriving regardless of how slow you are.","distractors":["Run a capacity test: pin replicas to 1 (disable the HPA), step the arrival rate up, and record p99 and error rate at each step.","Coordinated omission is a measurement bug in closed-model (VU-based) load tests: each virtual user sends a request, waits for the response, then sends the next.","Because users experience the tail, not the mean."]},{"id":"devops/performance_and_load_testing#371b76ab873c","section":"devops","module":"devops/performance_and_load_testing","moduleName":"performance and load testing","qIndex":6,"difficulty":"intermediate","question":"How do you derive autoscaling targets from a load test?","answerFull":"Run a capacity test: pin replicas to 1 (disable the HPA), step the arrival rate up, and record p99 and error rate at each step. Find the \"knee\" — the load just before p99 breaks the SLO; that's your max sustainable RPS per replica. Then set the HPA target so a replica runs at ~60–70% of that ceiling, leaving headroom for the scale-up lag (an HPA takes ~15s to sync plus pod start time). For peak capacity, divide peak RPS by the per-replica sustainable rate and add headroom: 11,000 RPS ÷ 550/replica ≈ 20 replicas, provision ~25. This turns autoscaling from a guess into a measured number, and feeds directly into cost models.","correct":"Run a capacity test: pin replicas to 1 (disable the HPA), step the arrival rate up, and record p99 and error rate at each step.","distractors":["Coordinated omission is a measurement bug in closed-model (VU-based) load tests: each virtual user sends a request, waits for the response, then sends the next.","First suspect the load generator, not the service.","Define thresholds in the test (k6: http_req_duration: ['p(95)<300'], http_req_failed: ['rate<0.001']) so the tool exits non-zero when an SLO is breached, which fails the pipeline step."]},{"id":"devops/performance_and_load_testing#e2d9deff242d","section":"devops","module":"devops/performance_and_load_testing","moduleName":"performance and load testing","qIndex":7,"difficulty":"intermediate","question":"How do you wire a performance test into CI as a gate?","answerFull":"Define thresholds in the test (k6: http_req_duration: ['p(95)<300'], http_req_failed: ['rate<0.001']) so the tool exits non-zero when an SLO is breached, which fails the pipeline step. The flow: PR opens → deploy to an ephemeral/preview environment → run the load test against it → thresholds pass/fail the build → optionally compare against a stored baseline and fail on regression beyond a percentage (e.g., p95 worse by >10%). The key principle is that a test that doesn't gate is just a dashboard — to prevent regressions it must be able to *block the merge*. You keep the gate test short (a few minutes, focused) and run longer soak/stress tests on a schedule rather than per-PR.","correct":"Define thresholds in the test (k6: http_req_duration: ['p(95)<300'], http_req_failed: ['rate<0.001']) so the tool exits non-zero when an SLO is breached, which fails the pipeline step.","distractors":["The first minute or so of a test is unrepresentative: JIT compilers haven't optimized hot paths, caches are cold, connection pools are empty and filling, and the autoscaler is still ramping replicas.","Run a capacity test: pin replicas to 1 (disable the HPA), step the arrival rate up, and record p99 and error rate at each step.","The open model (constant arrival rate) is the default for latency truth because it mimics real users who keep arriving regardless of how slow you are."]},{"id":"devops/performance_and_load_testing#cee0e55e7649","section":"devops","module":"devops/performance_and_load_testing","moduleName":"performance and load testing","qIndex":8,"difficulty":"intermediate","question":"Why is a warm-up period important, and what do you do with it?","answerFull":"The first minute or so of a test is unrepresentative: JIT compilers haven't optimized hot paths, caches are cold, connection pools are empty and filling, and the autoscaler is still ramping replicas. Measuring from t=0 inflates latency and pollutes your percentiles with transient cold-start behavior that doesn't reflect steady state. So you include an explicit warm-up stage (ramp to load, hold briefly) and then discard it from analysis, measuring only the steady-state \"hold at peak\" window. This is also why a 30-second test is nearly worthless — it's almost all warm-up.","correct":"The first minute or so of a test is unrepresentative: JIT compilers haven't optimized hot paths, caches are cold, connection pools are empty and filling, and the autoscaler is still ramping replicas.","distractors":["Run a capacity test: pin replicas to 1 (disable the HPA), step the arrival rate up, and record p99 and error rate at each step.","Define thresholds in the test (k6: http_req_duration: ['p(95)<300'], http_req_failed: ['rate<0.001']) so the tool exits non-zero when an SLO is breached, which fails the pipeline step.","Because the system's performance is data-dependent in ways that synthetic data hides."]},{"id":"devops/performance_and_load_testing#43856f185278","section":"devops","module":"devops/performance_and_load_testing","moduleName":"performance and load testing","qIndex":10,"difficulty":"intermediate","question":"How would you load-test safely against production?","answerFull":"With great care, because you can hurt real users. Techniques: dark traffic / shadowing (mirror real requests to a parallel test stack that doesn't affect users), squeeze testing (slowly raise real or synthetic traffic to a single instance/canary to find its limit while able to abort instantly), strict blast-radius limits (rate caps, circuit breakers, a kill switch), running during low-traffic windows, isolating test data so you don't corrupt real records, and tagging synthetic traffic so it's excluded from business metrics. Netflix's squeeze testing is the canonical pattern. The default should still be a production-*like* staging environment; prod testing is for behaviors you genuinely can't reproduce elsewhere.","correct":"With great care, because you can hurt real users.","distractors":["Because users experience the tail, not the mean.","First suspect the load generator, not the service.","Define thresholds in the test (k6: http_req_duration: ['p(95)<300'], http_req_failed: ['rate<0.001']) so the tool exits non-zero when an SLO is breached, which fails the pipeline step."]},{"id":"devops/performance_and_load_testing#f57613e0b284","section":"devops","module":"devops/performance_and_load_testing","moduleName":"performance and load testing","qIndex":13,"difficulty":"advanced","question":"Why does test-data realism matter so much, and what goes wrong with naive synthetic data?","answerFull":"Because the system's performance is data-dependent in ways that synthetic data hides. Hitting the same record or key repeatedly makes every request a cache hit, so the test reports latency that production (with a cold, diverse working set) never achieves. A uniform key distribution misses hot keys — the celebrity user or popular product that creates a partition/lock/shard hotspot in production. Sequential or unrealistic IDs can produce index access patterns that don't match real queries. Tiny test datasets fit in memory while production tables spill to disk. The fixes: replay anonymized production traffic where possible, or generate data with realistic cardinality and skew (Zipfian, not uniform), size the dataset to production scale, and exercise the full breadth of endpoints in production-like proportions. A load test against unrealistic data is precise but inaccurate — it measures something, just not the thing you'll ship.","correct":"Because the system's performance is data-dependent in ways that synthetic data hides.","distractors":["Run a capacity test: pin replicas to 1 (disable the HPA), step the arrival rate up, and record p99 and error rate at each step.","Define thresholds in the test (k6: http_req_duration: ['p(95)<300'], http_req_failed: ['rate<0.001']) so the tool exits non-zero when an SLO is breached, which fails the pipeline step.","The first minute or so of a test is unrepresentative: JIT compilers haven't optimized hot paths, caches are cold, connection pools are empty and filling, and the autoscaler is still ramping replicas."]},{"id":"devops/platform_engineering_and_idp#c037d4bba857","section":"devops","module":"devops/platform_engineering_and_idp","moduleName":"platform engineering and idp","qIndex":1,"difficulty":"core","question":"What is an Internal Developer Platform and what problem does it solve?","answerFull":"An IDP is a self-service layer of tooling and golden paths that lets product engineers ship software without mastering the underlying infrastructure (Kubernetes, Terraform, IAM, networking). It solves the twin problems of inconsistency (every team reinventing deployment differently and insecurely) and the DevOps-as-ticket-queue bottleneck. The goal is to reduce cognitive load on stream-aligned teams so their attention goes to product value, not plumbing.","correct":"An IDP is a self-service layer of tooling and golden paths that lets product engineers ship software without mastering the underlying infrastructure (Kubernetes, Terraform, IAM, networking).","distractors":["It means treating the platform as a product with real customers (your developers), a roadmap, adoption metrics, and a feedback loop — not as a one-off internal project.","A golden path is the opinionated, fully-supported, easiest route to production that you make so frictionless developers want to use it, while still leaving the underlying primitives accessible for edge cases.","Pipelines are Kubernetes CRDs and each step runs as a Pod, so CI/CD shares the cluster's RBAC, secrets, autoscaling, networking, and observability with your applications — no separate runner fleet to manage."]},{"id":"devops/platform_engineering_and_idp#b04243d6d60d","section":"devops","module":"devops/platform_engineering_and_idp","moduleName":"platform engineering and idp","qIndex":2,"difficulty":"core","question":"Explain \"platform-as-product\" and why it matters.","answerFull":"It means treating the platform as a product with real customers (your developers), a roadmap, adoption metrics, and a feedback loop — not as a one-off internal project. It matters because if the golden path is slower or more painful than rolling their own, developers route around it and the platform fails regardless of its technical elegance. Success is measured by adoption and DORA outcomes, not by how clever the internals are.","correct":"It means treating the platform as a product with real customers (your developers), a roadmap, adoption metrics, and a feedback loop — not as a one-off internal project.","distractors":["Flaky tests (random pass/fail) train developers to reflexively re-run, which masks genuine failures and lets real bugs through, destroying trust in the suite.","Cloud Run is a fully managed serverless platform that runs containers, scales to zero, and bills per request/CPU-time, handling concurrency (multiple requests per instance).","for: requires the alert expression to remain true continuously for that duration before the alert transitions from pending to firing, so a one-off spike that clears within the window never pages anyone."]},{"id":"devops/platform_engineering_and_idp#c19818201a95","section":"devops","module":"devops/platform_engineering_and_idp","moduleName":"platform engineering and idp","qIndex":3,"difficulty":"core","question":"What is the difference between a golden path and a golden cage?","answerFull":"A golden path is the opinionated, fully-supported, easiest route to production that you make so frictionless developers want to use it, while still leaving the underlying primitives accessible for edge cases. A golden cage is an over-rigid path with no escape hatch, forcing teams with legitimate non-standard needs to either suffer or bypass the platform entirely. Always provide an off-ramp; make the right thing easy, not mandatory.","correct":"A golden path is the opinionated, fully-supported, easiest route to production that you make so frictionless developers want to use it, while still leaving the underlying primitives accessible for edge cases.","distractors":["An IDP is a self-service layer of tooling and golden paths that lets product engineers ship software without mastering the underlying infrastructure (Kubernetes, Terraform, IAM, networking).","Flaky tests (random pass/fail) train developers to reflexively re-run, which masks genuine failures and lets real bugs through, destroying trust in the suite.","It means treating the platform as a product with real customers (your developers), a roadmap, adoption metrics, and a feedback loop — not as a one-off internal project."]},{"id":"devops/platform_engineering_and_idp#e3a83fe1b99b","section":"devops","module":"devops/platform_engineering_and_idp","moduleName":"platform engineering and idp","qIndex":6,"difficulty":"intermediate","question":"What are the four DORA metrics and what do they measure?","answerFull":"Deployment frequency (how often you ship), lead time for changes (commit to production), change failure rate (percentage of deploys causing a failure), and time to restore service (MTTR). The first two measure velocity, the last two measure stability — elite performers achieve both simultaneously, deploying multiple times per day with lead time under an hour and change failure rate under 15%. They are the outcome metrics that tell you whether a platform is actually accelerating safe delivery.","correct":"Deployment frequency (how often you ship), lead time for changes (commit to production), change failure rate (percentage of deploys causing a failure), and time to restore service (MTTR).","distractors":["CI automatically builds and tests every commit and integrates changes frequently, catching breakage early.","An IDP is a self-service layer of tooling and golden paths that lets product engineers ship software without mastering the underlying infrastructure (Kubernetes, Terraform, IAM, networking).","The Collector's spanmetrics connector derives RED metrics (Rate, Errors, Duration histograms) from the spans flowing through it, dimensioned by service and operation."]},{"id":"devops/platform_engineering_and_idp#d00717237b84","section":"devops","module":"devops/platform_engineering_and_idp","moduleName":"platform engineering and idp","qIndex":10,"difficulty":"intermediate","question":"What is a \"thinnest viable platform\" and when is building Backstage premature?","answerFull":"The thinnest viable platform is the smallest abstraction that measurably helps — sometimes a wiki page of golden-path docs plus a shared Terraform module library, not a full portal. Building Backstage is premature for a 2–3 team startup where the cognitive-load problem does not yet exist and you lack a team to run the portal; that is gold-plating. Start thin, measure demand, and invest in heavier abstractions only where adoption and pain justify them.","correct":"The thinnest viable platform is the smallest abstraction that measurably helps — sometimes a wiki page of golden-path docs plus a shared Terraform module library, not a full portal.","distractors":["A golden path is the opinionated, fully-supported, easiest route to production that you make so frictionless developers want to use it, while still leaving the underlying primitives accessible for edge cases.","An IDP is a self-service layer of tooling and golden paths that lets product engineers ship software without mastering the underlying infrastructure (Kubernetes, Terraform, IAM, networking).","The cache key is the set of attributes (path, plus chosen headers/query strings/cookies) the CDN uses to decide whether a request matches a cached object."]},{"id":"devops/policy_as_code_and_compliance#128c25c39427","section":"devops","module":"devops/policy_as_code_and_compliance","moduleName":"policy as code and compliance","qIndex":1,"difficulty":"core","question":"What is policy as code and how does it differ from a compliance checklist?","answerFull":"Policy as code expresses governance rules as version-controlled, testable code that executes automatically against every resource, whereas a checklist is a manual document sampled by a human at a point in time. The code runs identically in CI and at admission, covering 100% of changes and producing an audit trail for free, while a checklist covers only what the reviewer happened to inspect. Adopt policy as code when you need continuous, consistent enforcement and auditable evidence rather than periodic manual review.","correct":"Policy as code expresses governance rules as version-controlled, testable code that executes automatically against every resource, whereas a checklist is a manual document sampled by a human at a point in time.","distractors":["With failurePolicy: Fail, if the webhook pod is unavailable, every matching create/update request is rejected, which can block cluster-wide writes — including the pods needed to recover the webhook itself.","Conftest runs Rego policies against any structured file — Kubernetes YAML, a terraform show -json plan, Dockerfiles, JSON config — at CI time before the resource reaches a cluster.","Exceptions must be explicit, version-controlled, owned, and time-boxed with an expiry date, never an undocumented skip flag."]},{"id":"devops/policy_as_code_and_compliance#e77e2ec29b75","section":"devops","module":"devops/policy_as_code_and_compliance","moduleName":"policy as code and compliance","qIndex":2,"difficulty":"core","question":"Explain the Gatekeeper ConstraintTemplate vs Constraint distinction.","answerFull":"A ConstraintTemplate defines reusable Rego logic plus a parameter schema (the \"what kind of check\"), and a Constraint instantiates that template with concrete values and a scope (the \"applied here with these settings\"). This two-layer design lets one template like \"allowed repositories\" be reused by many constraints with different repo lists per namespace. It mirrors a class-versus-instance relationship and avoids duplicating Rego for every variation.","correct":"A ConstraintTemplate defines reusable Rego logic plus a parameter schema (the \"what kind of check\"), and a Constraint instantiates that template with concrete values and a scope (the \"applied here with these settings\").","distractors":["Choose Kyverno when your team prefers Kubernetes-native YAML over learning Rego and when you need mutation or generation, not just validation.","Conftest runs Rego policies against any structured file — Kubernetes YAML, a terraform show -json plan, Dockerfiles, JSON config — at CI time before the resource reaches a cluster.","Deploy it first in audit/dryrun mode so it records violations in status without denying anything, then work through the existing violation backlog and fix or waive each."]},{"id":"devops/policy_as_code_and_compliance#fbbc929c4301","section":"devops","module":"devops/policy_as_code_and_compliance","moduleName":"policy as code and compliance","qIndex":3,"difficulty":"core","question":"When would you choose Kyverno over Gatekeeper?","answerFull":"Choose Kyverno when your team prefers Kubernetes-native YAML over learning Rego and when you need mutation or generation, not just validation. Kyverno can inject secure defaults (mutate) and create dependent resources like default NetworkPolicies (generate), which Gatekeeper does not natively do as cleanly. Choose Gatekeeper when you need the full expressiveness of Rego for complex cross-field logic or want to reuse existing OPA policies.","correct":"Choose Kyverno when your team prefers Kubernetes-native YAML over learning Rego and when you need mutation or generation, not just validation.","distractors":["A ConstraintTemplate defines reusable Rego logic plus a parameter schema (the \"what kind of check\"), and a Constraint instantiates that template with concrete values and a scope (the \"applied here with these settings\").","The CIS Kubernetes Benchmark is a set of roughly 120 hardening controls spanning the API server, etcd, kubelet, RBAC, and pod security configuration.","Conftest runs Rego policies against any structured file — Kubernetes YAML, a terraform show -json plan, Dockerfiles, JSON config — at CI time before the resource reaches a cluster."]},{"id":"devops/policy_as_code_and_compliance#4f94d5b8d3d9","section":"devops","module":"devops/policy_as_code_and_compliance","moduleName":"policy as code and compliance","qIndex":5,"difficulty":"core","question":"What is the danger of failurePolicy: Fail on an admission webhook?","answerFull":"With failurePolicy: Fail, if the webhook pod is unavailable, every matching create/update request is rejected, which can block cluster-wide writes — including the pods needed to recover the webhook itself. The fix is to exclude control-plane namespaces via a namespaceSelector and scope the webhook rules narrowly to only the resources the policy evaluates. Always monitor webhook health and have a break-glass path to remove the configuration during an incident.","correct":"With failurePolicy: Fail, if the webhook pod is unavailable, every matching create/update request is rejected, which can block cluster-wide writes — including the pods needed to recover the webhook itself.","distractors":["Conftest runs Rego policies against any structured file — Kubernetes YAML, a terraform show -json plan, Dockerfiles, JSON config — at CI time before the resource reaches a cluster.","Policy as code expresses governance rules as version-controlled, testable code that executes automatically against every resource, whereas a checklist is a manual document sampled by a human at a point in time.","The CIS Kubernetes Benchmark is a set of roughly 120 hardening controls spanning the API server, etcd, kubelet, RBAC, and pod security configuration."]},{"id":"devops/policy_as_code_and_compliance#b193c569e917","section":"devops","module":"devops/policy_as_code_and_compliance","moduleName":"policy as code and compliance","qIndex":6,"difficulty":"intermediate","question":"How do you roll out a new enforcement policy without breaking existing workloads?","answerFull":"Deploy it first in audit/dryrun mode so it records violations in status without denying anything, then work through the existing violation backlog and fix or waive each. Once violations are near zero, flip enforcementAction to deny, ideally per-namespace starting with low-risk environments. This staged approach surfaces pre-existing non-compliance safely instead of breaking deploys the moment enforcement is enabled.","correct":"Deploy it first in audit/dryrun mode so it records violations in status without denying anything, then work through the existing violation backlog and fix or waive each.","distractors":["Policy as code expresses governance rules as version-controlled, testable code that executes automatically against every resource, whereas a checklist is a manual document sampled by a human at a point in time.","Each policy is tagged to specific control IDs — for example \"no privileged containers\" maps to CIS K8s 5.2.1, SOC2 CC6.1, and PCI-DSS 2.2 — in a maintained traceability matrix.","Write unit tests using OPA's built-in test runner, providing mock input for both an allowed case and a denied case and asserting the violation count."]},{"id":"devops/policy_as_code_and_compliance#1401fcf9e7fb","section":"devops","module":"devops/policy_as_code_and_compliance","moduleName":"policy as code and compliance","qIndex":7,"difficulty":"intermediate","question":"How does policy as code map to frameworks like SOC2 or PCI-DSS?","answerFull":"Each policy is tagged to specific control IDs — for example \"no privileged containers\" maps to CIS K8s 5.2.1, SOC2 CC6.1, and PCI-DSS 2.2 — in a maintained traceability matrix. This lets auditors see exactly which automated control satisfies which requirement and lets you prove continuous enforcement rather than a snapshot. The practical payoff is that audit evidence becomes a query against policy results instead of a manual document hunt.","correct":"Each policy is tagged to specific control IDs — for example \"no privileged containers\" maps to CIS K8s 5.2.1, SOC2 CC6.1, and PCI-DSS 2.2 — in a maintained traceability matrix.","distractors":["Policy as code expresses governance rules as version-controlled, testable code that executes automatically against every resource, whereas a checklist is a manual document sampled by a human at a point in time.","The CIS Kubernetes Benchmark is a set of roughly 120 hardening controls spanning the API server, etcd, kubelet, RBAC, and pod security configuration.","A ConstraintTemplate defines reusable Rego logic plus a parameter schema (the \"what kind of check\"), and a Constraint instantiates that template with concrete values and a scope (the \"applied here with these settings\")."]},{"id":"devops/policy_as_code_and_compliance#4111019480f3","section":"devops","module":"devops/policy_as_code_and_compliance","moduleName":"policy as code and compliance","qIndex":8,"difficulty":"intermediate","question":"How do you test Rego policies?","answerFull":"Write unit tests using OPA's built-in test runner, providing mock input for both an allowed case and a denied case and asserting the violation count. For example, a privileged-container test feeds a privileged pod and asserts exactly one violation, plus a clean pod asserting zero. Run opa test in CI so an untested or broken policy — which can silently allow everything — fails the build before it ships.","correct":"Write unit tests using OPA's built-in test runner, providing mock input for both an allowed case and a denied case and asserting the violation count.","distractors":["Deploy it first in audit/dryrun mode so it records violations in status without denying anything, then work through the existing violation backlog and fix or waive each.","A ConstraintTemplate defines reusable Rego logic plus a parameter schema (the \"what kind of check\"), and a Constraint instantiates that template with concrete values and a scope (the \"applied here with these settings\").","Conftest runs Rego policies against any structured file — Kubernetes YAML, a terraform show -json plan, Dockerfiles, JSON config — at CI time before the resource reaches a cluster."]},{"id":"devops/policy_as_code_and_compliance#61dbd1f07e5f","section":"devops","module":"devops/policy_as_code_and_compliance","moduleName":"policy as code and compliance","qIndex":9,"difficulty":"intermediate","question":"What is Conftest and where does it fit?","answerFull":"Conftest runs Rego policies against any structured file — Kubernetes YAML, a terraform show -json plan, Dockerfiles, JSON config — at CI time before the resource reaches a cluster. It shifts policy left so a developer sees \"security group opens 0.0.0.0/0 on port 22\" at the PR instead of after apply. It is a fast guardrail; pair it with admission enforcement since CI checks can be bypassed.","correct":"Conftest runs Rego policies against any structured file — Kubernetes YAML, a terraform show -json plan, Dockerfiles, JSON config — at CI time before the resource reaches a cluster.","distractors":["The CIS Kubernetes Benchmark is a set of roughly 120 hardening controls spanning the API server, etcd, kubelet, RBAC, and pod security configuration.","A ConstraintTemplate defines reusable Rego logic plus a parameter schema (the \"what kind of check\"), and a Constraint instantiates that template with concrete values and a scope (the \"applied here with these settings\").","Policy as code expresses governance rules as version-controlled, testable code that executes automatically against every resource, whereas a checklist is a manual document sampled by a human at a point in time."]},{"id":"devops/policy_as_code_and_compliance#59345546e386","section":"devops","module":"devops/policy_as_code_and_compliance","moduleName":"policy as code and compliance","qIndex":10,"difficulty":"intermediate","question":"How should exceptions and waivers be handled?","answerFull":"Exceptions must be explicit, version-controlled, owned, and time-boxed with an expiry date, never an undocumented skip flag. In Gatekeeper you scope constraints with match excludedNamespaces or labels; in Kyverno you use policy exceptions resources — both reviewable in git. The goal is that every waiver is auditable and automatically expires, so temporary holes do not become permanent.","correct":"Exceptions must be explicit, version-controlled, owned, and time-boxed with an expiry date, never an undocumented skip flag.","distractors":["Policy as code expresses governance rules as version-controlled, testable code that executes automatically against every resource, whereas a checklist is a manual document sampled by a human at a point in time.","Conftest runs Rego policies against any structured file — Kubernetes YAML, a terraform show -json plan, Dockerfiles, JSON config — at CI time before the resource reaches a cluster.","A ConstraintTemplate defines reusable Rego logic plus a parameter schema (the \"what kind of check\"), and a Constraint instantiates that template with concrete values and a scope (the \"applied here with these settings\")."]},{"id":"devops/policy_as_code_and_compliance#f82dc5beeba0","section":"devops","module":"devops/policy_as_code_and_compliance","moduleName":"policy as code and compliance","qIndex":11,"difficulty":"advanced","question":"What does the CIS Kubernetes Benchmark cover and how do you automate it?","answerFull":"The CIS Kubernetes Benchmark is a set of roughly 120 hardening controls spanning the API server, etcd, kubelet, RBAC, and pod security configuration. You automate it with kube-bench, which runs as a Job on nodes and outputs pass/fail per control, feeding SOC2 and compliance evidence. Combine it with admission policies so the benchmark covers node/control-plane config while Gatekeeper/Kyverno cover workload config.","correct":"The CIS Kubernetes Benchmark is a set of roughly 120 hardening controls spanning the API server, etcd, kubelet, RBAC, and pod security configuration.","distractors":["With failurePolicy: Fail, if the webhook pod is unavailable, every matching create/update request is rejected, which can block cluster-wide writes — including the pods needed to recover the webhook itself.","Choose Kyverno when your team prefers Kubernetes-native YAML over learning Rego and when you need mutation or generation, not just validation.","Each policy is tagged to specific control IDs — for example \"no privileged containers\" maps to CIS K8s 5.2.1, SOC2 CC6.1, and PCI-DSS 2.2 — in a maintained traceability matrix."]},{"id":"devops/secrets_management#83cb8e45480c","section":"devops","module":"devops/secrets_management","moduleName":"secrets management","qIndex":2,"difficulty":"core","question":"Why aren't native Kubernetes Secrets actually secure by default?","answerFull":"Kubernetes Secret objects are only base64-encoded in etcd, not encrypted, so anyone with etcd access or sufficient RBAC (kubectl get secret -o yaml) can read the plaintext. To make them safe you must enable etcd encryption-at-rest with a KMS provider, tightly scope RBAC, and ideally source the values from an external backend (Vault/cloud) via the External Secrets Operator or CSI driver rather than committing them. Treating base64 as encryption is one of the most common Kubernetes security mistakes.","correct":"Kubernetes Secret objects are only base64-encoded in etcd, not encrypted, so anyone with etcd access or sufficient RBAC (kubectl get secret -o yaml) can read the plaintext.","distractors":["Choose Kyverno when your team prefers Kubernetes-native YAML over learning Rego and when you need mutation or generation, not just validation.","By default Secrets are only base64-encoded and stored as plaintext in etcd — anyone with get secrets RBAC or etcd access can read them.","Check the EndpointSlices: kubectl get endpointslices -l kubernetes.io/service-name=<svc>."]},{"id":"devops/secrets_management#656842446115","section":"devops","module":"devops/secrets_management","moduleName":"secrets management","qIndex":7,"difficulty":"intermediate","question":"Why are secrets in Terraform state a problem, and how do you mitigate it?","answerFull":"Any sensitive value Terraform manages is written unencrypted into the state JSON, and sensitive = true only masks CLI output, not the stored value — so anyone who can read the state bucket can read the password. Mitigate by KMS-encrypting the state backend, locking down bucket access, never committing state to Git, and sourcing secrets at runtime from Vault/Secrets Manager instead of passing them through Terraform (see infrastructure_as_code_terraform). The residual risk is why state access must be treated as secret access.","correct":"Any sensitive value Terraform manages is written unencrypted into the state JSON, and sensitive = true only masks CLI output, not the stored value — so anyone who can read the state bucket can read the password.","distractors":["By default Secrets are only base64-encoded and stored as plaintext in etcd — anyone with get secrets RBAC or etcd access can read them.","OpenTofu is a fork of Terraform created after HashiCorp relicensed Terraform from MPL 2.0 to the BUSL in 2023; it's MPL-licensed, Linux Foundation-backed, and a drop-in CLI replacement.","Conftest runs Rego policies against any structured file — Kubernetes YAML, a terraform show -json plan, Dockerfiles, JSON config — at CI time before the resource reaches a cluster."]},{"id":"devops/secrets_management#0720634b37dc","section":"devops","module":"devops/secrets_management","moduleName":"secrets management","qIndex":10,"difficulty":"intermediate","question":"What's the principle of least privilege applied to secrets, and how do you enforce it?","answerFull":"Each workload should be able to read only the specific secrets it needs and nothing else, so a compromise of one service can't read another's credentials. Enforce it with scoped policies (Vault policies bound to a specific path and auth role, IAM policies limiting secretsmanager:GetSecretValue to specific secret ARNs) and per-service identities rather than a shared \"app\" role. Combined with short TTLs and audit logging, least privilege keeps the blast radius of any single compromise small.","correct":"Each workload should be able to read only the specific secrets it needs and nothing else, so a compromise of one service can't read another's credentials.","distractors":["The workflow requests an OIDC token from GitHub's identity provider, and the cloud (e.g., AWS via an IAM OIDC trust) exchanges it for temporary credentials scoped to a specific role, typically valid ~1 hour.","GCP uses Workload Identity, which binds a Kubernetes service account to a GCP service account via the cluster's workload identity pool, so pods get short-lived GCP credentials with no static keys.","Monorepo enables atomic cross-service changes in one commit/PR and shared tooling, but needs affected-target CI (Bazel/Nx) to avoid building everything, and access control is coarser."]},{"id":"devops/serverless_and_faas#389b74abdae9","section":"devops","module":"devops/serverless_and_faas","moduleName":"serverless and faas","qIndex":1,"difficulty":"core","question":"What is a cold start, what causes it, and how do you mitigate it?","answerFull":"A cold start is the added latency (~100ms-1s) when the platform must initialize a new execution environment — downloading code, starting the runtime, and running your init code — because no warm one is available. It's worsened by large deployment packages, heavy module-level initialization, and (historically) VPC ENI attachment. Mitigate by minimizing package size, reusing SDK clients at module scope, using provisioned concurrency for latency-critical synchronous paths, and SnapStart for Java; for truly latency-sensitive endpoints, keep instances warm.","correct":"A cold start is the added latency (~100ms-1s) when the platform must initialize a new execution environment — downloading code, starting the runtime, and running your init code — because no warm one is available.","distractors":["Provisioned concurrency keeps a configured number of execution environments initialized and ready, so requests hitting them incur no cold start.","When there are no events, the platform destroys idle execution environments so you pay nothing for compute — ideal for intermittent workloads.","Each function gets its own IAM execution role with a policy scoped to exactly the actions and resources it needs — for example, dynamodb:PutItem on one table and s3:GetObject on one bucket prefix — and nothing more."]},{"id":"devops/serverless_and_faas#2b7e1e952440","section":"devops","module":"devops/serverless_and_faas","moduleName":"serverless and faas","qIndex":2,"difficulty":"core","question":"Why must serverless functions be idempotent?","answerFull":"Because event sources like SQS, Kinesis, and EventBridge guarantee at-least-once delivery, so the same event can invoke your function more than once. A non-idempotent handler would double-charge a card or double-insert a record. Make handlers idempotent by deduplicating on a stable key with a conditional write to DynamoDB or by using idempotency keys (e.g., Lambda Powertools), so reprocessing a duplicate is a no-op.","correct":"Because event sources like SQS, Kinesis, and EventBridge guarantee at-least-once delivery, so the same event can invoke your function more than once.","distractors":["Each function gets its own IAM execution role with a policy scoped to exactly the actions and resources it needs — for example, dynamodb:PutItem on one table and s3:GetObject on one bucket prefix — and nothing more.","Memory and CPU are coupled — increasing memory proportionally increases allocated vCPU, so a CPU-bound function can run faster (and sometimes cheaper) at higher memory because it finishes sooner.","Knative is a Kubernetes-based serverless framework that brings scale-to-zero, request-driven autoscaling, and eventing to your own cluster, so functions/containers run portably on any Kubernetes (any cloud or on-prem)."]},{"id":"devops/serverless_and_faas#10cf01131de7","section":"devops","module":"devops/serverless_and_faas","moduleName":"serverless and faas","qIndex":4,"difficulty":"core","question":"How does Lambda's memory setting affect performance and cost?","answerFull":"Memory and CPU are coupled — increasing memory proportionally increases allocated vCPU, so a CPU-bound function can run faster (and sometimes cheaper) at higher memory because it finishes sooner. Billing is memory-GB times execution-milliseconds plus per-request, so the cost-optimal setting is found by tuning (e.g., AWS Lambda Power Tuning). Don't assume the smallest memory is cheapest; faster execution at higher memory can lower total cost.","correct":"Memory and CPU are coupled — increasing memory proportionally increases allocated vCPU, so a CPU-bound function can run faster (and sometimes cheaper) at higher memory because it finishes sooner.","distractors":["Each function gets its own IAM execution role with a policy scoped to exactly the actions and resources it needs — for example, dynamodb:PutItem on one table and s3:GetObject on one bucket prefix — and nothing more.","Knative is a Kubernetes-based serverless framework that brings scale-to-zero, request-driven autoscaling, and eventing to your own cluster, so functions/containers run portably on any Kubernetes (any cloud or on-prem).","Because event sources like SQS, Kinesis, and EventBridge guarantee at-least-once delivery, so the same event can invoke your function more than once."]},{"id":"devops/serverless_and_faas#63bfc24dfa68","section":"devops","module":"devops/serverless_and_faas","moduleName":"serverless and faas","qIndex":6,"difficulty":"intermediate","question":"How do you handle failures in an event-driven serverless pipeline?","answerFull":"Configure dead-letter queues (DLQs) on async invocations and SQS event sources with a maxReceiveCount so poison messages move to the DLQ after N failed attempts instead of vanishing, and alarm on DLQ depth. Use partial batch failure reporting for SQS so one bad record doesn't fail the whole batch, and make handlers idempotent so retries are safe. After fixing the root cause, replay messages from the DLQ.","correct":"Configure dead-letter queues (DLQs) on async invocations and SQS event sources with a maxReceiveCount so poison messages move to the DLQ after N failed attempts instead of vanishing, and alarm on DLQ depth.","distractors":["Because event sources like SQS, Kinesis, and EventBridge guarantee at-least-once delivery, so the same event can invoke your function more than once.","Knative is a Kubernetes-based serverless framework that brings scale-to-zero, request-driven autoscaling, and eventing to your own cluster, so functions/containers run portably on any Kubernetes (any cloud or on-prem).","A cold start is the added latency (~100ms-1s) when the platform must initialize a new execution environment — downloading code, starting the runtime, and running your init code — because no warm one is available."]},{"id":"devops/serverless_and_faas#6e02930558c3","section":"devops","module":"devops/serverless_and_faas","moduleName":"serverless and faas","qIndex":7,"difficulty":"intermediate","question":"API Gateway vs ALB in front of Lambda — how do you choose?","answerFull":"API Gateway provides rich API features — request validation, throttling, API keys, usage plans, authorizers (JWT/IAM/Cognito), and caching — and is the default for public APIs, but costs more per request at high volume. An ALB invoking Lambda is cheaper at very high request volumes and integrates with existing VPC/WAF setups but lacks API-management features. Use API Gateway (HTTP API for lower cost) for managed public APIs and ALB-to-Lambda when you need cheap, high-volume routing alongside other targets.","correct":"API Gateway provides rich API features — request validation, throttling, API keys, usage plans, authorizers (JWT/IAM/Cognito), and caching — and is the default for public APIs, but costs more per request at high volume.","distractors":["Each function gets its own IAM execution role with a policy scoped to exactly the actions and resources it needs — for example, dynamodb:PutItem on one table and s3:GetObject on one bucket prefix — and nothing more.","Knative is a Kubernetes-based serverless framework that brings scale-to-zero, request-driven autoscaling, and eventing to your own cluster, so functions/containers run portably on any Kubernetes (any cloud or on-prem).","A cold start is the added latency (~100ms-1s) when the platform must initialize a new execution environment — downloading code, starting the runtime, and running your init code — because no warm one is available."]},{"id":"devops/serverless_and_faas#e0bcb5c55993","section":"devops","module":"devops/serverless_and_faas","moduleName":"serverless and faas","qIndex":8,"difficulty":"intermediate","question":"What is provisioned concurrency and when do you use it?","answerFull":"Provisioned concurrency keeps a configured number of execution environments initialized and ready, so requests hitting them incur no cold start. You use it for latency-sensitive synchronous endpoints (user-facing APIs) where the occasional ~500ms cold start is unacceptable, accepting that you pay for the warm capacity even when idle. Reserved concurrency, by contrast, caps maximum concurrency (and guarantees a floor) but doesn't pre-warm.","correct":"Provisioned concurrency keeps a configured number of execution environments initialized and ready, so requests hitting them incur no cold start.","distractors":["When there are no events, the platform destroys idle execution environments so you pay nothing for compute — ideal for intermittent workloads.","A cold start is the added latency (~100ms-1s) when the platform must initialize a new execution environment — downloading code, starting the runtime, and running your init code — because no warm one is available.","Each function gets its own IAM execution role with a policy scoped to exactly the actions and resources it needs — for example, dynamodb:PutItem on one table and s3:GetObject on one bucket prefix — and nothing more."]},{"id":"devops/serverless_and_faas#8f3eb8eaaf67","section":"devops","module":"devops/serverless_and_faas","moduleName":"serverless and faas","qIndex":9,"difficulty":"intermediate","question":"How does scale-to-zero work and what's the tradeoff?","answerFull":"When there are no events, the platform destroys idle execution environments so you pay nothing for compute — ideal for intermittent workloads. The tradeoff is that the next request after a quiet period hits a cold start. Platforms like Cloud Run and Lambda let you set a minimum/provisioned instance count above zero to trade some cost for guaranteed warm capacity, balancing cost against tail latency.","correct":"When there are no events, the platform destroys idle execution environments so you pay nothing for compute — ideal for intermittent workloads.","distractors":["Knative is a Kubernetes-based serverless framework that brings scale-to-zero, request-driven autoscaling, and eventing to your own cluster, so functions/containers run portably on any Kubernetes (any cloud or on-prem).","Provisioned concurrency keeps a configured number of execution environments initialized and ready, so requests hitting them incur no cold start.","A cold start is the added latency (~100ms-1s) when the platform must initialize a new execution environment — downloading code, starting the runtime, and running your init code — because no warm one is available."]},{"id":"devops/serverless_and_faas#8eb9744a5189","section":"devops","module":"devops/serverless_and_faas","moduleName":"serverless and faas","qIndex":10,"difficulty":"intermediate","question":"What is Knative and why might you choose it over Lambda?","answerFull":"Knative is a Kubernetes-based serverless framework that brings scale-to-zero, request-driven autoscaling, and eventing to your own cluster, so functions/containers run portably on any Kubernetes (any cloud or on-prem). You'd choose it to avoid vendor lock-in, to run serverless alongside existing Kubernetes workloads, or to meet on-prem/regulatory constraints — at the cost of operating the cluster yourself, which managed FaaS abstracts away.","correct":"Knative is a Kubernetes-based serverless framework that brings scale-to-zero, request-driven autoscaling, and eventing to your own cluster, so functions/containers run portably on any Kubernetes (any cloud or on-prem).","distractors":["Memory and CPU are coupled — increasing memory proportionally increases allocated vCPU, so a CPU-bound function can run faster (and sometimes cheaper) at higher memory because it finishes sooner.","API Gateway provides rich API features — request validation, throttling, API keys, usage plans, authorizers (JWT/IAM/Cognito), and caching — and is the default for public APIs, but costs more per request at high volume.","Each function gets its own IAM execution role with a policy scoped to exactly the actions and resources it needs — for example, dynamodb:PutItem on one table and s3:GetObject on one bucket prefix — and nothing more."]},{"id":"devops/serverless_and_faas#96e163a2e811","section":"devops","module":"devops/serverless_and_faas","moduleName":"serverless and faas","qIndex":12,"difficulty":"advanced","question":"How do you give a Lambda least-privilege access to other AWS services?","answerFull":"Each function gets its own IAM execution role with a policy scoped to exactly the actions and resources it needs — for example, dynamodb:PutItem on one table and s3:GetObject on one bucket prefix — and nothing more. Avoid sharing a broad role across many functions, because that widens the blast radius if one function is compromised. Use IAM Access Analyzer and per-function roles, and reference secrets_management for any credentials the function needs.","correct":"Each function gets its own IAM execution role with a policy scoped to exactly the actions and resources it needs — for example, dynamodb:PutItem on one table and s3:GetObject on one bucket prefix — and nothing more.","distractors":["Memory and CPU are coupled — increasing memory proportionally increases allocated vCPU, so a CPU-bound function can run faster (and sometimes cheaper) at higher memory because it finishes sooner.","Because event sources like SQS, Kinesis, and EventBridge guarantee at-least-once delivery, so the same event can invoke your function more than once.","Knative is a Kubernetes-based serverless framework that brings scale-to-zero, request-driven autoscaling, and eventing to your own cluster, so functions/containers run portably on any Kubernetes (any cloud or on-prem)."]},{"id":"devops/shell_scripting_and_automation#5e584b7a7fff","section":"devops","module":"devops/shell_scripting_and_automation","moduleName":"shell scripting and automation","qIndex":1,"difficulty":"core","question":"What does set -euo pipefail do and why is each flag important?","answerFull":"-e exits on any non-zero command so failures don't cascade; -u errors on unset variables, catching typos like $DIR vs $DIRR; -o pipefail makes a pipeline fail if *any* stage fails, not just the last. Together they convert silent, dangerous continuation into immediate, debuggable failure — the single most important line in any production script.","correct":"-e exits on any non-zero command so failures don't cascade; -u errors on unset variables, catching typos like $DIR vs $DIRR; -o pipefail makes a pipeline fail if *any* stage fails, not just the last.","distractors":["shellcheck is a static analyzer that flags unquoted variables, missing set flags, useless cat, and dozens of common bugs.","By default $? of a | b | c is c's exit code, so a failure in a or b is hidden.","Idempotent = running it N times yields the same end state as running it once."]},{"id":"devops/shell_scripting_and_automation#0ded0e0d1c97","section":"devops","module":"devops/shell_scripting_and_automation","moduleName":"shell scripting and automation","qIndex":2,"difficulty":"core","question":"Why quote variables, and what breaks without it?","answerFull":"Unquoted $var undergoes word-splitting (on IFS) and glob expansion. rm -rf $DIR/* with an empty DIR becomes rm -rf /*; a path with spaces becomes multiple arguments. \"$var\" preserves it as a single literal token. The rule: quote unless you specifically want splitting.","correct":"Unquoted $var undergoes word-splitting (on IFS) and glob expansion.","distractors":["shellcheck is a static analyzer that flags unquoted variables, missing set flags, useless cat, and dozens of common bugs.","-e exits on any non-zero command so failures don't cascade; -u errors on unset variables, catching typos like $DIR vs $DIRR; -o pipefail makes a pipeline fail if *any* stage fails, not just the last.","By default $? of a | b | c is c's exit code, so a failure in a or b is hidden."]},{"id":"devops/shell_scripting_and_automation#6d13a5f63519","section":"devops","module":"devops/shell_scripting_and_automation","moduleName":"shell scripting and automation","qIndex":3,"difficulty":"core","question":"What makes a script idempotent and why does it matter for automation?","answerFull":"Idempotent = running it N times yields the same end state as running it once. It matters because automation is retried (CI re-runs, cron, failed-then-resumed deploys). Use mkdir -p, kubectl apply over create, check-before-append, and conditional creation. Non-idempotent scripts duplicate resources or fail on the second run.","correct":"Idempotent = running it N times yields the same end state as running it once.","distractors":["When you need data structures, JSON/YAML manipulation, HTTP calls with retries, concurrency, or precise error handling — or when the script passes ~100 lines.","-e exits on any non-zero command so failures don't cascade; -u errors on unset variables, catching typos like $DIR vs $DIRR; -o pipefail makes a pipeline fail if *any* stage fails, not just the last.","By default $? of a | b | c is c's exit code, so a failure in a or b is hidden."]},{"id":"devops/shell_scripting_and_automation#d3a206f11333","section":"devops","module":"devops/shell_scripting_and_automation","moduleName":"shell scripting and automation","qIndex":4,"difficulty":"core","question":"When should you stop using bash and switch to Python?","answerFull":"When you need data structures, JSON/YAML manipulation, HTTP calls with retries, concurrency, or precise error handling — or when the script passes ~100 lines. Bash excels at gluing CLIs and short pipelines; beyond that, its lack of real types, error handling, and testing makes Python/Go far safer.","correct":"When you need data structures, JSON/YAML manipulation, HTTP calls with retries, concurrency, or precise error handling — or when the script passes ~100 lines.","distractors":["-e exits on any non-zero command so failures don't cascade; -u errors on unset variables, catching typos like $DIR vs $DIRR; -o pipefail makes a pipeline fail if *any* stage fails, not just the last.","By default $? of a | b | c is c's exit code, so a failure in a or b is hidden.","Idempotent = running it N times yields the same end state as running it once."]},{"id":"devops/shell_scripting_and_automation#463145269949","section":"devops","module":"devops/shell_scripting_and_automation","moduleName":"shell scripting and automation","qIndex":5,"difficulty":"core","question":"How do exit codes flow through a pipeline, and how does pipefail change it?","answerFull":"By default $? of a | b | c is c's exit code, so a failure in a or b is hidden. set -o pipefail makes the pipeline's exit code the rightmost non-zero one, surfacing upstream failures so set -e can act on them.","correct":"By default $? of a | b | c is c's exit code, so a failure in a or b is hidden.","distractors":["-e exits on any non-zero command so failures don't cascade; -u errors on unset variables, catching typos like $DIR vs $DIRR; -o pipefail makes a pipeline fail if *any* stage fails, not just the last.","Idempotent = running it N times yields the same end state as running it once.","Unquoted $var undergoes word-splitting (on IFS) and glob expansion."]},{"id":"devops/shell_scripting_and_automation#7056581a574c","section":"devops","module":"devops/shell_scripting_and_automation","moduleName":"shell scripting and automation","qIndex":6,"difficulty":"intermediate","question":"What is trap used for in production scripts?","answerFull":"trap 'cmd' SIGNAL registers cleanup/handlers. trap cleanup EXIT guarantees temp files are removed and locks released on any exit path; trap 'graceful_shutdown' TERM INT lets an entrypoint forward shutdown to the app. It's how scripts stay clean under both success and interruption.","correct":"trap 'cmd' SIGNAL registers cleanup/handlers.","distractors":["-e exits on any non-zero command so failures don't cascade; -u errors on unset variables, catching typos like $DIR vs $DIRR; -o pipefail makes a pipeline fail if *any* stage fails, not just the last.","By default $? of a | b | c is c's exit code, so a failure in a or b is hidden.","Idempotent = running it N times yields the same end state as running it once."]},{"id":"devops/shell_scripting_and_automation#5987407d7e6f","section":"devops","module":"devops/shell_scripting_and_automation","moduleName":"shell scripting and automation","qIndex":7,"difficulty":"intermediate","question":"Why exec \"$@\" at the end of a container entrypoint?","answerFull":"exec replaces the shell process with the app, so the app becomes PID 1 and receives signals (SIGTERM) directly instead of the shell swallowing them. Without exec, the shell stays PID 1, doesn't forward signals, and graceful shutdown breaks.","correct":"exec replaces the shell process with the app, so the app becomes PID 1 and receives signals (SIGTERM) directly instead of the shell swallowing them.","distractors":["Idempotent = running it N times yields the same end state as running it once.","-e exits on any non-zero command so failures don't cascade; -u errors on unset variables, catching typos like $DIR vs $DIRR; -o pipefail makes a pipeline fail if *any* stage fails, not just the last.","By default $? of a | b | c is c's exit code, so a failure in a or b is hidden."]},{"id":"devops/shell_scripting_and_automation#28af4d35218f","section":"devops","module":"devops/shell_scripting_and_automation","moduleName":"shell scripting and automation","qIndex":9,"difficulty":"intermediate","question":"How do you parse JSON safely in a shell pipeline?","answerFull":"Use jq: jq -r '.items[].metadata.name'. It understands JSON structure, handles escaping, and survives whitespace/order changes that break grep|awk|cut. For building JSON, jq -n --arg/--argjson escapes values correctly, avoiding injection from unescaped input.","correct":"Use jq: jq -r '.items[].metadata.name'.","distractors":["exec replaces the shell process with the app, so the app becomes PID 1 and receives signals (SIGTERM) directly instead of the shell swallowing them.","When you need data structures, JSON/YAML manipulation, HTTP calls with retries, concurrency, or precise error handling — or when the script passes ~100 lines.","-e exits on any non-zero command so failures don't cascade; -u errors on unset variables, catching typos like $DIR vs $DIRR; -o pipefail makes a pipeline fail if *any* stage fails, not just the last."]},{"id":"devops/shell_scripting_and_automation#955405376313","section":"devops","module":"devops/shell_scripting_and_automation","moduleName":"shell scripting and automation","qIndex":10,"difficulty":"intermediate","question":"What is shellcheck and where does it fit?","answerFull":"shellcheck is a static analyzer that flags unquoted variables, missing set flags, useless cat, and dozens of common bugs. Running it as a required CI gate catches the exact classes of error (quoting, exit-code handling) that cause production incidents, before merge.","correct":"shellcheck is a static analyzer that flags unquoted variables, missing set flags, useless cat, and dozens of common bugs.","distractors":["Unquoted $var undergoes word-splitting (on IFS) and glob expansion.","-e exits on any non-zero command so failures don't cascade; -u errors on unset variables, catching typos like $DIR vs $DIRR; -o pipefail makes a pipeline fail if *any* stage fails, not just the last.","By default $? of a | b | c is c's exit code, so a failure in a or b is hidden."]},{"id":"devops/sre_principles_and_slos#346b93716c5b","section":"devops","module":"devops/sre_principles_and_slos","moduleName":"sre principles and slos","qIndex":2,"difficulty":"core","question":"What is an error budget and why is it powerful?","answerFull":"An error budget is the allowed unreliability, computed as 1 − SLO (a 99.9% SLO gives a 0.1% budget = 43.2 min/30 days). It's powerful because it converts the perennial reliability-vs-velocity argument into a shared number: if budget remains, dev ships features and takes risks; if it's exhausted, the policy freezes risky launches and everyone focuses on reliability. Reliability becomes a measurable, spendable resource that both dev and ops are jointly accountable for, removing the political tug-of-war.","correct":"An error budget is the allowed unreliability, computed as 1 − SLO (a 99.9% SLO gives a 0.1% budget = 43.2 min/30 days).","distractors":["Burn rate is how fast you're consuming the error budget relative to \"evenly over the window\" — a burn rate of 1x exactly exhausts the budget at the window's end, 2x in half the time, and so on.","A blameless postmortem analyzes an incident focusing on the systemic and process causes — what made the failure possible and what controls were missing — rather than blaming the individual who triggered it.","A single short window flaps on transient spikes; a single long window detects severe burns far too slowly."]},{"id":"devops/sre_principles_and_slos#211c6ad687c0","section":"devops","module":"devops/sre_principles_and_slos","moduleName":"sre principles and slos","qIndex":5,"difficulty":"core","question":"What is burn rate, and where does the 14.4x number come from?","answerFull":"Burn rate is how fast you're consuming the error budget relative to \"evenly over the window\" — a burn rate of 1x exactly exhausts the budget at the window's end, 2x in half the time, and so on. The 14.4x fast-page threshold comes from wanting to alert when a 30-day budget would be gone in about 2 days (30/2 ≈ 15, rounded to Google's canonical 14.4x), which means in one hour you'd consume ~2% of the budget. You alert on high burn rates over short windows because that's the only way to detect \"we'll breach the SLO soon\" before it actually happens.","correct":"Burn rate is how fast you're consuming the error budget relative to \"evenly over the window\" — a burn rate of 1x exactly exhausts the budget at the window's end, 2x in half the time, and so on.","distractors":["You compute the ratio of good events to total over the window — e.g. sum(rate(http_requests_total{status!~\"5..\"}[30d])) / sum(rate(http_requests_total[30d])) for request success.","A single short window flaps on transient spikes; a single long window detects severe burns far too slowly.","An error budget is the allowed unreliability, computed as 1 − SLO (a 99.9% SLO gives a 0.1% budget = 43.2 min/30 days)."]},{"id":"devops/sre_principles_and_slos#a203c3d3a4a7","section":"devops","module":"devops/sre_principles_and_slos","moduleName":"sre principles and slos","qIndex":6,"difficulty":"intermediate","question":"Why use multi-window, multi-burn-rate alerts instead of a single threshold?","answerFull":"A single short window flaps on transient spikes; a single long window detects severe burns far too slowly. Pairing a long window (confirms the burn is real and sustained) with a short confirmation window (ensures it's still happening and lets the alert clear quickly) gives both fast detection on severe burns and low noise. You then set multiple tiers — 14.4x → page fast, 6x → page/ticket, 1x → slow ticket — so severity matches how urgently the budget is being threatened.","correct":"A single short window flaps on transient spikes; a single long window detects severe burns far too slowly.","distractors":["Burn rate is how fast you're consuming the error budget relative to \"evenly over the window\" — a burn rate of 1x exactly exhausts the budget at the window's end, 2x in half the time, and so on.","You compute the ratio of good events to total over the window — e.g. sum(rate(http_requests_total{status!~\"5..\"}[30d])) / sum(rate(http_requests_total[30d])) for request success.","A blameless postmortem analyzes an incident focusing on the systemic and process causes — what made the failure possible and what controls were missing — rather than blaming the individual who triggered it."]},{"id":"devops/sre_principles_and_slos#0c6db8ab30b9","section":"devops","module":"devops/sre_principles_and_slos","moduleName":"sre principles and slos","qIndex":7,"difficulty":"intermediate","question":"What is toil and why cap it at 50%?","answerFull":"Toil is manual, repetitive, automatable, reactive operational work that scales linearly with the service and produces no lasting value — restarts, manual scaling, ticket-driven provisioning. Google caps it at 50% of an SRE's time because if toil consumes everything, the team never builds the automation that would reduce future toil, and ops load grows unbounded with the service. Capping it forces continuous investment in automation so the team's capacity scales sub-linearly with the system.","correct":"Toil is manual, repetitive, automatable, reactive operational work that scales linearly with the service and produces no lasting value — restarts, manual scaling, ticket-driven provisioning.","distractors":["Traditional ops is largely manual and reactive, with reliability as a vague aspiration and a hard org boundary between dev (ship) and ops (stabilize).","A blameless postmortem analyzes an incident focusing on the systemic and process causes — what made the failure possible and what controls were missing — rather than blaming the individual who triggered it.","A single short window flaps on transient spikes; a single long window detects severe burns far too slowly."]},{"id":"devops/sre_principles_and_slos#15f997ebd04c","section":"devops","module":"devops/sre_principles_and_slos","moduleName":"sre principles and slos","qIndex":11,"difficulty":"advanced","question":"What's a blameless postmortem and why does it matter for reliability?","answerFull":"A blameless postmortem analyzes an incident focusing on the systemic and process causes — what made the failure possible and what controls were missing — rather than blaming the individual who triggered it. It matters because blame drives people to hide mistakes and withhold information, which prevents the organization from learning, whereas a blameless culture surfaces the real contributing factors and produces durable fixes. The output is concrete, owned, tracked action items that reduce the chance of recurrence (covered in incident_management_and_oncall).","correct":"A blameless postmortem analyzes an incident focusing on the systemic and process causes — what made the failure possible and what controls were missing — rather than blaming the individual who triggered it.","distractors":["Traditional ops is largely manual and reactive, with reliability as a vague aspiration and a hard org boundary between dev (ship) and ops (stabilize).","A single short window flaps on transient spikes; a single long window detects severe burns far too slowly.","An error budget is the allowed unreliability, computed as 1 − SLO (a 99.9% SLO gives a 0.1% budget = 43.2 min/30 days)."]},{"id":"devops/sre_principles_and_slos#5529f30da17c","section":"devops","module":"devops/sre_principles_and_slos","moduleName":"sre principles and slos","qIndex":12,"difficulty":"advanced","question":"How do you measure an availability SLI in PromQL, and what's a common mistake?","answerFull":"You compute the ratio of good events to total over the window — e.g. sum(rate(http_requests_total{status!~\"5..\"}[30d])) / sum(rate(http_requests_total[30d])) for request success. The common mistake is measuring something that isn't the user's experience, like a load-balancer health check that passes while real requests fail, which reports a falsely perfect SLI. Measure on actual user-facing request outcomes as close to the user as feasible (edge metrics or synthetic/client probes), and define \"good\" precisely (which status codes, which latency threshold).","correct":"You compute the ratio of good events to total over the window — e.g. sum(rate(http_requests_total{status!~\"5..\"}[30d])) / sum(rate(http_requests_total[30d])) for request success.","distractors":["Burn rate is how fast you're consuming the error budget relative to \"evenly over the window\" — a burn rate of 1x exactly exhausts the budget at the window's end, 2x in half the time, and so on.","A single short window flaps on transient spikes; a single long window detects severe burns far too slowly.","A blameless postmortem analyzes an incident focusing on the systemic and process causes — what made the failure possible and what controls were missing — rather than blaming the individual who triggered it."]},{"id":"devops/sre_principles_and_slos#258e9550bd1b","section":"devops","module":"devops/sre_principles_and_slos","moduleName":"sre principles and slos","qIndex":15,"difficulty":"advanced","question":"How does SRE differ from traditional ops/DevOps?","answerFull":"Traditional ops is largely manual and reactive, with reliability as a vague aspiration and a hard org boundary between dev (ship) and ops (stabilize). SRE treats operations as a software problem: it sets explicit reliability targets (SLOs), measures them, manages an error budget that quantifies the velocity-vs-reliability tradeoff, caps manual toil so engineers automate, and runs blameless postmortems. DevOps is the broader cultural movement to break the dev/ops wall; SRE is a specific, prescriptive *implementation* of those principles with concrete practices (error budgets, the 50% toil cap, golden signals) — \"class SRE implements interface DevOps.\"","correct":"Traditional ops is largely manual and reactive, with reliability as a vague aspiration and a hard org boundary between dev (ship) and ops (stabilize).","distractors":["Toil is manual, repetitive, automatable, reactive operational work that scales linearly with the service and produces no lasting value — restarts, manual scaling, ticket-driven provisioning.","A blameless postmortem analyzes an incident focusing on the systemic and process causes — what made the failure possible and what controls were missing — rather than blaming the individual who triggered it.","A single short window flaps on transient spikes; a single long window detects severe burns far too slowly."]},{"id":"devops/terraform_advanced_and_alternatives#ae93b87b9f96","section":"devops","module":"devops/terraform_advanced_and_alternatives","moduleName":"terraform advanced and alternatives","qIndex":1,"difficulty":"core","question":"What problem does Terragrunt solve that plain Terraform doesn't?","answerFull":"Terragrunt eliminates the repetition of backend, provider, and inputs blocks across many environments by generating them from a shared root and letting each child supply only its deltas. It also runs many states in dependency order (run-all) and wires outputs between them via dependency blocks. Use it when you have enough environments/states that copy-paste causes silent divergence; for a single small stack it's unnecessary overhead.","correct":"Terragrunt eliminates the repetition of backend, provider, and inputs blocks across many environments by generating them from a shared root and letting each child supply only its deltas.","distractors":["AWS CDK synthesizes CloudFormation templates and deploys via AWS, so it's AWS-only and uses CloudFormation state; CDKTF synthesizes Terraform configuration and uses Terraform state and providers, so it's multi-cloud.","Use defer terraform.Destroy(t, opts) placed immediately after the options struct so teardown runs even when assertions panic, and run tests in a dedicated sandbox account with strict budget alarms.","Policy-as-code expresses governance rules (\"no public S3,\" \"only approved instance types,\" \"all resources tagged\") as code evaluated automatically against a Terraform plan."]},{"id":"devops/terraform_advanced_and_alternatives#3481f759b322","section":"devops","module":"devops/terraform_advanced_and_alternatives","moduleName":"terraform advanced and alternatives","qIndex":3,"difficulty":"core","question":"CloudFormation/CDK vs Terraform — what's the tradeoff?","answerFull":"CloudFormation is AWS-native with managed stacks, automatic rollback on failure, and built-in drift detection, and CDK lets you synthesize those stacks from real languages — but both lock you to AWS. Terraform is multi-cloud and provider-agnostic with a richer module ecosystem, at the cost of managing your own state and lacking native stack rollback. Choose CloudFormation/CDK for AWS-only shops that value native integration; choose Terraform when multi-cloud or provider breadth matters.","correct":"CloudFormation is AWS-native with managed stacks, automatic rollback on failure, and built-in drift detection, and CDK lets you synthesize those stacks from real languages — but both lock you to AWS.","distractors":["Use defer terraform.Destroy(t, opts) placed immediately after the options struct so teardown runs even when assertions panic, and run tests in a dedicated sandbox account with strict budget alarms.","OpenTofu is a fork of Terraform created after HashiCorp relicensed Terraform from MPL 2.0 to the BUSL in 2023; it's MPL-licensed, Linux Foundation-backed, and a drop-in CLI replacement.","AWS CDK synthesizes CloudFormation templates and deploys via AWS, so it's AWS-only and uses CloudFormation state; CDKTF synthesizes Terraform configuration and uses Terraform state and providers, so it's multi-cloud."]},{"id":"devops/terraform_advanced_and_alternatives#66787298c5e3","section":"devops","module":"devops/terraform_advanced_and_alternatives","moduleName":"terraform advanced and alternatives","qIndex":4,"difficulty":"core","question":"What is OpenTofu and why does it exist?","answerFull":"OpenTofu is a fork of Terraform created after HashiCorp relicensed Terraform from MPL 2.0 to the BUSL in 2023; it's MPL-licensed, Linux Foundation-backed, and a drop-in CLI replacement. Migration is largely swapping the binary and re-running init, since HCL/providers/workflow are shared. It has begun shipping its own features (e.g., client-side state encryption, enhanced for_each), so the engines are slowly diverging — choose based on licensing comfort and the specific features each offers.","correct":"OpenTofu is a fork of Terraform created after HashiCorp relicensed Terraform from MPL 2.0 to the BUSL in 2023; it's MPL-licensed, Linux Foundation-backed, and a drop-in CLI replacement.","distractors":["When the problem they solve doesn't exist yet: Terragrunt for one environment, Terratest on every commit for a stable module, or a policy engine before you have any policies are all premature.","AWS CDK synthesizes CloudFormation templates and deploys via AWS, so it's AWS-only and uses CloudFormation state; CDKTF synthesizes Terraform configuration and uses Terraform state and providers, so it's multi-cloud.","Policy-as-code expresses governance rules (\"no public S3,\" \"only approved instance types,\" \"all resources tagged\") as code evaluated automatically against a Terraform plan."]},{"id":"devops/terraform_advanced_and_alternatives#15db3ecd4a2c","section":"devops","module":"devops/terraform_advanced_and_alternatives","moduleName":"terraform advanced and alternatives","qIndex":6,"difficulty":"intermediate","question":"What is policy-as-code and where in the pipeline does it run?","answerFull":"Policy-as-code expresses governance rules (\"no public S3,\" \"only approved instance types,\" \"all resources tagged\") as code evaluated automatically against a Terraform plan. It runs at plan time in CI — OPA/Conftest on the plan JSON, or Sentinel natively in Terraform Cloud — so non-compliant changes are blocked before any resource is created. Running it pre-apply (shift-left) is the whole point; catching a public bucket after it's live is a security incident, not a policy check.","correct":"Policy-as-code expresses governance rules (\"no public S3,\" \"only approved instance types,\" \"all resources tagged\") as code evaluated automatically against a Terraform plan.","distractors":["AWS CDK synthesizes CloudFormation templates and deploys via AWS, so it's AWS-only and uses CloudFormation state; CDKTF synthesizes Terraform configuration and uses Terraform state and providers, so it's multi-cloud.","OpenTofu is a fork of Terraform created after HashiCorp relicensed Terraform from MPL 2.0 to the BUSL in 2023; it's MPL-licensed, Linux Foundation-backed, and a drop-in CLI replacement.","When the problem they solve doesn't exist yet: Terragrunt for one environment, Terratest on every commit for a stable module, or a policy engine before you have any policies are all premature."]},{"id":"devops/terraform_advanced_and_alternatives#b5d22a8c566a","section":"devops","module":"devops/terraform_advanced_and_alternatives","moduleName":"terraform advanced and alternatives","qIndex":8,"difficulty":"intermediate","question":"How do you keep Terratest from leaking expensive cloud resources?","answerFull":"Use defer terraform.Destroy(t, opts) placed immediately after the options struct so teardown runs even when assertions panic, and run tests in a dedicated sandbox account with strict budget alarms. Add a scheduled orphan-sweeper that deletes test-tagged resources older than a threshold, and keep tests small and parallel-safe. Real-deploy tests cost real money and minutes, so run them on a schedule or for module changes, not on every commit.","correct":"Use defer terraform.Destroy(t, opts) placed immediately after the options struct so teardown runs even when assertions panic, and run tests in a dedicated sandbox account with strict budget alarms.","distractors":["Pin the current Terraform version and back up state, then install the OpenTofu binary and run tofu init against the same backend — the state format is compatible at the fork point, so existing state is read directly.","Policy-as-code expresses governance rules (\"no public S3,\" \"only approved instance types,\" \"all resources tagged\") as code evaluated automatically against a Terraform plan.","When the problem they solve doesn't exist yet: Terragrunt for one environment, Terratest on every commit for a stable module, or a policy engine before you have any policies are all premature."]},{"id":"devops/terraform_advanced_and_alternatives#b9ad450fdc0d","section":"devops","module":"devops/terraform_advanced_and_alternatives","moduleName":"terraform advanced and alternatives","qIndex":9,"difficulty":"intermediate","question":"When does adding these advanced tools become over-engineering?","answerFull":"When the problem they solve doesn't exist yet: Terragrunt for one environment, Terratest on every commit for a stable module, or a policy engine before you have any policies are all premature. Each tool adds a moving part to learn and maintain, so adopt them in response to a felt pain — divergence, a production surprise, a compliance requirement — not preemptively. The senior move is matching the layer (engine/orchestration/testing/policy) to the actual problem.","correct":"When the problem they solve doesn't exist yet: Terragrunt for one environment, Terratest on every commit for a stable module, or a policy engine before you have any policies are all premature.","distractors":["Terragrunt eliminates the repetition of backend, provider, and inputs blocks across many environments by generating them from a shared root and letting each child supply only its deltas.","Policy-as-code expresses governance rules (\"no public S3,\" \"only approved instance types,\" \"all resources tagged\") as code evaluated automatically against a Terraform plan.","AWS CDK synthesizes CloudFormation templates and deploys via AWS, so it's AWS-only and uses CloudFormation state; CDKTF synthesizes Terraform configuration and uses Terraform state and providers, so it's multi-cloud."]},{"id":"devops/terraform_advanced_and_alternatives#97449a7df7ac","section":"devops","module":"devops/terraform_advanced_and_alternatives","moduleName":"terraform advanced and alternatives","qIndex":10,"difficulty":"intermediate","question":"How does CDKTF differ from AWS CDK?","answerFull":"AWS CDK synthesizes CloudFormation templates and deploys via AWS, so it's AWS-only and uses CloudFormation state; CDKTF synthesizes Terraform configuration and uses Terraform state and providers, so it's multi-cloud. Both let you write infrastructure in TypeScript/Python/Go/Java with type-checking and IDE support. Choose CDKTF when you want real-language ergonomics but need Terraform's provider breadth and multi-cloud reach; choose AWS CDK when you're committed to AWS-native tooling.","correct":"AWS CDK synthesizes CloudFormation templates and deploys via AWS, so it's AWS-only and uses CloudFormation state; CDKTF synthesizes Terraform configuration and uses Terraform state and providers, so it's multi-cloud.","distractors":["Use defer terraform.Destroy(t, opts) placed immediately after the options struct so teardown runs even when assertions panic, and run tests in a dedicated sandbox account with strict budget alarms.","OpenTofu is a fork of Terraform created after HashiCorp relicensed Terraform from MPL 2.0 to the BUSL in 2023; it's MPL-licensed, Linux Foundation-backed, and a drop-in CLI replacement.","CloudFormation is AWS-native with managed stacks, automatic rollback on failure, and built-in drift detection, and CDK lets you synthesize those stacks from real languages — but both lock you to AWS."]},{"id":"devops/terraform_advanced_and_alternatives#57b4711a060f","section":"devops","module":"devops/terraform_advanced_and_alternatives","moduleName":"terraform advanced and alternatives","qIndex":11,"difficulty":"advanced","question":"How do you safely migrate from Terraform to OpenTofu (or vice versa)?","answerFull":"Pin the current Terraform version and back up state, then install the OpenTofu binary and run tofu init against the same backend — the state format is compatible at the fork point, so existing state is read directly. Run tofu plan and confirm it shows no changes (proving parity) before applying anything, and update CI to call the new binary. Watch for version-specific feature divergence as the projects evolve, and migrate one state at a time rather than all at once.","correct":"Pin the current Terraform version and back up state, then install the OpenTofu binary and run tofu init against the same backend — the state format is compatible at the fork point, so existing state is read directly.","distractors":["Terragrunt eliminates the repetition of backend, provider, and inputs blocks across many environments by generating them from a shared root and letting each child supply only its deltas.","Use defer terraform.Destroy(t, opts) placed immediately after the options struct so teardown runs even when assertions panic, and run tests in a dedicated sandbox account with strict budget alarms.","AWS CDK synthesizes CloudFormation templates and deploys via AWS, so it's AWS-only and uses CloudFormation state; CDKTF synthesizes Terraform configuration and uses Terraform state and providers, so it's multi-cloud."]},{"id":"devops/version_control_and_git_workflows#90c9c9e284a8","section":"devops","module":"devops/version_control_and_git_workflows","moduleName":"version control and git workflows","qIndex":1,"difficulty":"core","question":"What is a Git commit, internally?","answerFull":"A commit is an immutable object containing a pointer to a tree (the directory snapshot), pointer(s) to parent commit(s), author/committer metadata, and a message — all hashed to produce its SHA. Branches are just movable pointers to commits, and HEAD points to the current branch. Changing any content changes the hash, which is why history is content-addressed and tamper-evident.","correct":"A commit is an immutable object containing a pointer to a tree (the directory snapshot), pointer(s) to parent commit(s), author/committer metadata, and a message — all hashed to produce its SHA.","distractors":["They are easier to review, revert (git revert <sha> cleanly undoes one change), bisect (finer granularity localizes regressions faster), and cherry-pick (port a fix to a release branch).","Merge preserves true history and creates a merge commit (non-linear graph), good for integrating shared branches.","git reflog records every movement of HEAD (and branch tips) with the old SHAs, even for \"rewritten\" history, until garbage collection."]},{"id":"devops/version_control_and_git_workflows#e553500f4a3f","section":"devops","module":"devops/version_control_and_git_workflows","moduleName":"version control and git workflows","qIndex":2,"difficulty":"core","question":"merge vs rebase — when each, and the golden rule?","answerFull":"Merge preserves true history and creates a merge commit (non-linear graph), good for integrating shared branches. Rebase replays commits onto a new base for linear history but rewrites SHAs. The golden rule: rebase only your *own* unpushed/private branches; never rebase or force-push branches others have based work on, or you erase their reachable history.","correct":"Merge preserves true history and creates a merge commit (non-linear graph), good for integrating shared branches.","distractors":["A commit is an immutable object containing a pointer to a tree (the directory snapshot), pointer(s) to parent commit(s), author/committer metadata, and a message — all hashed to produce its SHA.","Monorepo enables atomic cross-service changes in one commit/PR and shared tooling, but needs affected-target CI (Bazel/Nx) to avoid building everything, and access control is coarser.","Short-lived branches merge to main continuously, minimizing divergence and merge debt, and keeping main always-deployable."]},{"id":"devops/version_control_and_git_workflows#730bee4d0dad","section":"devops","module":"devops/version_control_and_git_workflows","moduleName":"version control and git workflows","qIndex":3,"difficulty":"core","question":"Why is trunk-based development preferred for CD?","answerFull":"Short-lived branches merge to main continuously, minimizing divergence and merge debt, and keeping main always-deployable. Unfinished work hides behind feature flags rather than long-lived branches. This enables frequent, low-risk releases — the essence of continuous delivery — whereas GitFlow's long-lived develop/release branches batch and delay integration.","correct":"Short-lived branches merge to main continuously, minimizing divergence and merge debt, and keeping main always-deployable.","distractors":["Merge preserves true history and creates a merge commit (non-linear graph), good for integrating shared branches.","Under GitOps the Git repo holds the *desired state* of the system (manifests/Helm/Terraform); an in-cluster agent (ArgoCD/Flux) continuously reconciles the cluster to match main.","--force overwrites the remote branch unconditionally, potentially erasing commits someone else pushed after your last fetch."]},{"id":"devops/version_control_and_git_workflows#d9a9f4b1fcee","section":"devops","module":"devops/version_control_and_git_workflows","moduleName":"version control and git workflows","qIndex":4,"difficulty":"core","question":"How do you recover commits after a bad rebase/reset?","answerFull":"git reflog records every movement of HEAD (and branch tips) with the old SHAs, even for \"rewritten\" history, until garbage collection. Find the pre-mistake SHA and git reset --hard <sha> (or git cherry-pick the lost commits). This is why almost no work is truly lost — Git's objects persist until GC.","correct":"git reflog records every movement of HEAD (and branch tips) with the old SHAs, even for \"rewritten\" history, until garbage collection.","distractors":["git bisect binary-searches the commit range between a known-good and known-bad commit to find the one that introduced a regression in O(log n) steps.","Under GitOps the Git repo holds the *desired state* of the system (manifests/Helm/Terraform); an in-cluster agent (ArgoCD/Flux) continuously reconciles the cluster to match main.","First, rotate the secret — assume it's compromised the moment it hit a remote, because clones and history retain it."]},{"id":"devops/version_control_and_git_workflows#5d8c6a4806f7","section":"devops","module":"devops/version_control_and_git_workflows","moduleName":"version control and git workflows","qIndex":5,"difficulty":"core","question":"A secret was committed and pushed. What do you do?","answerFull":"First, rotate the secret — assume it's compromised the moment it hit a remote, because clones and history retain it. Then purge it from history with git filter-repo (or BFG) and force-push, coordinating with the team since this rewrites history. Add gitleaks/detect-secrets as a pre-commit and CI gate to prevent recurrence. Scrubbing without rotating is a false sense of security.","correct":"First, rotate the secret — assume it's compromised the moment it hit a remote, because clones and history retain it.","distractors":["git reflog records every movement of HEAD (and branch tips) with the old SHAs, even for \"rewritten\" history, until garbage collection.","Merge preserves true history and creates a merge commit (non-linear graph), good for integrating shared branches.","--force overwrites the remote branch unconditionally, potentially erasing commits someone else pushed after your last fetch."]},{"id":"devops/version_control_and_git_workflows#f95ade044439","section":"devops","module":"devops/version_control_and_git_workflows","moduleName":"version control and git workflows","qIndex":6,"difficulty":"intermediate","question":"Monorepo vs polyrepo tradeoffs?","answerFull":"Monorepo enables atomic cross-service changes in one commit/PR and shared tooling, but needs affected-target CI (Bazel/Nx) to avoid building everything, and access control is coarser. Polyrepo gives per-service isolation, granular permissions, and naturally scoped CI, but cross-service changes require coordinating multiple PRs/repos. Choose based on coupling and tooling investment.","correct":"Monorepo enables atomic cross-service changes in one commit/PR and shared tooling, but needs affected-target CI (Bazel/Nx) to avoid building everything, and access control is coarser.","distractors":["A commit is an immutable object containing a pointer to a tree (the directory snapshot), pointer(s) to parent commit(s), author/committer metadata, and a message — all hashed to produce its SHA.","Merge preserves true history and creates a merge commit (non-linear graph), good for integrating shared branches.","git bisect binary-searches the commit range between a known-good and known-bad commit to find the one that introduced a regression in O(log n) steps."]},{"id":"devops/version_control_and_git_workflows#666bd8ce0a84","section":"devops","module":"devops/version_control_and_git_workflows","moduleName":"version control and git workflows","qIndex":7,"difficulty":"intermediate","question":"What is git bisect and when is it invaluable?","answerFull":"git bisect binary-searches the commit range between a known-good and known-bad commit to find the one that introduced a regression in O(log n) steps. With git bisect run <script> it's fully automated — the script's exit code marks each tested commit good/bad. It's the fastest way to localize \"it worked last week, broke somewhere since\".","correct":"git bisect binary-searches the commit range between a known-good and known-bad commit to find the one that introduced a regression in O(log n) steps.","distractors":["Merge preserves true history and creates a merge commit (non-linear graph), good for integrating shared branches.","A commit is an immutable object containing a pointer to a tree (the directory snapshot), pointer(s) to parent commit(s), author/committer metadata, and a message — all hashed to produce its SHA.","git reflog records every movement of HEAD (and branch tips) with the old SHAs, even for \"rewritten\" history, until garbage collection."]},{"id":"devops/version_control_and_git_workflows#374242d17a31","section":"devops","module":"devops/version_control_and_git_workflows","moduleName":"version control and git workflows","qIndex":8,"difficulty":"intermediate","question":"How does Git underpin GitOps?","answerFull":"Under GitOps the Git repo holds the *desired state* of the system (manifests/Helm/Terraform); an in-cluster agent (ArgoCD/Flux) continuously reconciles the cluster to match main. A merge is the deploy, a git revert is the rollback, and the commit history is a complete, signed audit log of every production change — no out-of-band kubectl apply.","correct":"Under GitOps the Git repo holds the *desired state* of the system (manifests/Helm/Terraform); an in-cluster agent (ArgoCD/Flux) continuously reconciles the cluster to match main.","distractors":["They are easier to review, revert (git revert <sha> cleanly undoes one change), bisect (finer granularity localizes regressions faster), and cherry-pick (port a fix to a release branch).","Short-lived branches merge to main continuously, minimizing divergence and merge debt, and keeping main always-deployable.","git reflog records every movement of HEAD (and branch tips) with the old SHAs, even for \"rewritten\" history, until garbage collection."]},{"id":"devops/version_control_and_git_workflows#c83a3f775bd2","section":"devops","module":"devops/version_control_and_git_workflows","moduleName":"version control and git workflows","qIndex":9,"difficulty":"intermediate","question":"Why are small, atomic commits valuable operationally?","answerFull":"They are easier to review, revert (git revert <sha> cleanly undoes one change), bisect (finer granularity localizes regressions faster), and cherry-pick (port a fix to a release branch). A giant commit mixing ten changes can't be partially reverted and obscures which change caused a regression.","correct":"They are easier to review, revert (git revert <sha> cleanly undoes one change), bisect (finer granularity localizes regressions faster), and cherry-pick (port a fix to a release branch).","distractors":["Monorepo enables atomic cross-service changes in one commit/PR and shared tooling, but needs affected-target CI (Bazel/Nx) to avoid building everything, and access control is coarser.","A commit is an immutable object containing a pointer to a tree (the directory snapshot), pointer(s) to parent commit(s), author/committer metadata, and a message — all hashed to produce its SHA.","git bisect binary-searches the commit range between a known-good and known-bad commit to find the one that introduced a regression in O(log n) steps."]},{"id":"devops/version_control_and_git_workflows#beeb6c71d00a","section":"devops","module":"devops/version_control_and_git_workflows","moduleName":"version control and git workflows","qIndex":10,"difficulty":"intermediate","question":"What's the difference between --force and --force-with-lease?","answerFull":"--force overwrites the remote branch unconditionally, potentially erasing commits someone else pushed after your last fetch. --force-with-lease only overwrites if the remote is still at the SHA you last saw, aborting if someone else pushed in between — so it protects against clobbering others' work. Even so, reserve force-pushing for your own branches.","correct":"--force overwrites the remote branch unconditionally, potentially erasing commits someone else pushed after your last fetch.","distractors":["First, rotate the secret — assume it's compromised the moment it hit a remote, because clones and history retain it.","They are easier to review, revert (git revert <sha> cleanly undoes one change), bisect (finer granularity localizes regressions faster), and cherry-pick (port a fix to a release branch).","git reflog records every movement of HEAD (and branch tips) with the old SHAs, even for \"rewritten\" history, until garbage collection."]},{"id":"devops/visualization_and_alerting#ab2e696e773f","section":"devops","module":"devops/visualization_and_alerting","moduleName":"visualization and alerting","qIndex":1,"difficulty":"core","question":"What's the difference between a dashboard and an alert, and when do you use each?","answerFull":"A dashboard is a *pull* tool — a human looks at it to investigate or maintain situational awareness — while an alert is a *push* tool that interrupts a human because action is needed. Use dashboards for investigation, trends, capacity, and showing SLO/error-budget status (RED/USE methods); use paging alerts only for urgent, user-impacting, actionable conditions. Conflating them (dashboarding what should page, or paging what should be a dashboard) is the root of most observability dysfunction.","correct":"A dashboard is a *pull* tool — a human looks at it to investigate or maintain situational awareness — while an alert is a *push* tool that interrupts a human because action is needed.","distractors":["Enable cost anomaly detection (AWS Cost Anomaly Detection or equivalent) to alert on unusual spend patterns within hours, and set per-team AWS Budgets with threshold alerts (e.g., at 80% of the monthly budget).","for: requires the alert expression to remain true continuously for that duration before the alert transitions from pending to firing, so a one-off spike that clears within the window never pages anyone.","Alert fatigue is the desensitization that happens when on-call receives frequent non-actionable alerts, so they start ignoring or auto-acking pages and eventually miss a real incident."]},{"id":"devops/visualization_and_alerting#3858535a7311","section":"devops","module":"devops/visualization_and_alerting","moduleName":"visualization and alerting","qIndex":2,"difficulty":"core","question":"Why alert on symptoms rather than causes?","answerFull":"Symptoms (high error rate, latency, SLO burn) are what users actually experience, so alerting on them catches every problem that matters and ignores causes that have no user impact. A cause like CPU > 80% pages constantly even when the autoscaler absorbs it or it's a harmless batch job, training on-call to ignore pages — which eventually causes a real page to be missed. Page on symptoms tied to user impact; keep causes on dashboards for investigation once a symptom alert fires.","correct":"Symptoms (high error rate, latency, SLO burn) are what users actually experience, so alerting on them catches every problem that matters and ignores causes that have no user impact.","distractors":["You alert when the error-budget burn rate exceeds a threshold over both a long window and a short confirmation window simultaneously, ANDed together.","A controller shifts a small traffic weight to the canary and, during pauses, queries metric providers (e.g., Prometheus) for success conditions — error rate ≥ 99%, p95 latency < threshold, or business KPIs.","Favor canary/progressive delivery to minimize blast radius (a payment bug must not hit 100% of users), gated on strict metrics (error rate, latency, and payment-success KPIs) with automated rollback."]},{"id":"devops/visualization_and_alerting#170281e3b64d","section":"devops","module":"devops/visualization_and_alerting","moduleName":"visualization and alerting","qIndex":3,"difficulty":"core","question":"What is alert fatigue and how do you fight it?","answerFull":"Alert fatigue is the desensitization that happens when on-call receives frequent non-actionable alerts, so they start ignoring or auto-acking pages and eventually miss a real incident. Fight it by ensuring every page is actionable and urgent, deleting or converting noisy cause-based alerts to tickets, grouping correlated alerts so one outage is one page, using SLO burn-rate alerting instead of static thresholds, and auditing alert-to-action ratios (if nobody acts on an alert, kill it). The counterintuitive truth is that fewer, higher-quality alerts make you safer.","correct":"Alert fatigue is the desensitization that happens when on-call receives frequent non-actionable alerts, so they start ignoring or auto-acking pages and eventually miss a real incident.","distractors":["MTTD (mean time to detect) is from problem-start to someone-knowing, improved by better alerting — especially SLO burn-rate alerts that page on user-impacting symptoms quickly.","Give every action item a named owner, a due date, and a tracked ticket, and review overdue items in regular ops/SRE meetings so they don't silently rot — an untracked action item means the same incident recurs in months.","ERROR for actionable failures that need attention, WARN for degraded-but-handled conditions, INFO for significant business/lifecycle milestones, and DEBUG for verbose developer detail."]},{"id":"devops/visualization_and_alerting#ad85582706d1","section":"devops","module":"devops/visualization_and_alerting","moduleName":"visualization and alerting","qIndex":6,"difficulty":"intermediate","question":"Explain grouping, inhibition, and silencing with examples.","answerFull":"Grouping collapses alerts sharing labels into a single notification — group_by: [alertname, cluster] turns 50 PodDown alerts on one cluster into one page. Inhibition suppresses dependent alerts when a parent fires — a ClusterDown alert inhibits all the PodDown alerts for that cluster so you get the meaningful page only. Silencing mutes matching alerts for a window — you silence a service's alerts during a planned migration so expected noise doesn't page anyone.","correct":"Grouping collapses alerts sharing labels into a single notification — group_by: [alertname, cluster] turns 50 PodDown alerts on one cluster into one page.","distractors":["Alert fatigue is the desensitization that happens when on-call receives frequent non-actionable alerts, so they start ignoring or auto-acking pages and eventually miss a real incident.","GitOps is an operational model where the desired state of the system is declared in Git (the single source of truth) and an in-cluster agent continuously reconciles the actual state to match.","kubernetes_sd_configs queries the Kubernetes API for pods/services/endpoints/nodes and turns them into targets with meta_kubernetes_* meta-labels."]},{"id":"devops/visualization_and_alerting#ec874642c3ec","section":"devops","module":"devops/visualization_and_alerting","moduleName":"visualization and alerting","qIndex":8,"difficulty":"intermediate","question":"What are the RED and USE methods, and when do you use each?","answerFull":"RED (Rate, Errors, Duration) is for request-driven services — it tells you how busy a service is, how often it fails, and how slow it is, which maps directly to user experience. USE (Utilization, Saturation, Errors) is for resources like CPU, disk, and network — it tells you how full and stressed the infrastructure is. Use RED for service dashboards and (its symptoms) for alerts; use USE for infrastructure dashboards and capacity investigation, generally not for paging.","correct":"RED (Rate, Errors, Duration) is for request-driven services — it tells you how busy a service is, how often it fails, and how slow it is, which maps directly to user experience.","distractors":["-e exits on any non-zero command so failures don't cascade; -u errors on unset variables, catching typos like $DIR vs $DIRR; -o pipefail makes a pipeline fail if *any* stage fails, not just the last.","A trace is one request's end-to-end journey across services; a span is a single unit of work within it (an HTTP handler, a DB query) with a start time, duration, status, and attributes.","For private connectivity to services without traversing the public internet: AWS PrivateLink maps to GCP Private Service Connect and Azure Private Link."]}]