<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Platform Material theme (no AppCompat/Material dependency needed), no
         action bar, pure black window so the WebView's dark UI has no flash. -->
    <style name="Theme.SDDaily" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
