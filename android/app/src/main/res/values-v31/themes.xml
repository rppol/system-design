<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ system splash: make the black background deliberate so the
         launch sequence is black end-to-end (splash -> window -> WebView -> page
         pre-paint script), with the launcher icon centered on it. -->
    <style name="Theme.SDDaily" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowSplashScreenBackground">@android:color/black</item>
    </style>
</resources>
